编译器级「向量元素自动析构」Feature 2 完整版 + 语义分析.cn 弹出去显式释放

编译器级注入（2026-08-25，用户批准执行）：
- emitClassMethod 对 向量$T 方法体前调 injectVectorElemDestroy（ir_oop.cpp）：
  当实例化元素 T 为有析构类时注入元素析构。
- ~向量/清空：全量析构循环——idx Alloca 槽 0..元素数量，
  Call T$析构函数（this=数据 + idx*typeSizeOf(T)），销毁后原体释放数组。
- 删除(位置)：单元素析构——守卫 位置<元素数量 后析构 数据+位置*步长。
- 无析构类元素（标量/结构体）不注入（保持原语义）。
- 效果：作用域栈 弹出 仅需 删除(末尾) 即自动销毁 映射 元素——
  CN语言编译器v2/语义分析.cn 表弹出 已移除显式 顶层.释放内部数组()。

新增 E2E 115_Feature2_向量元素自动析构（删除+1/清空+2/无死循环）；
113 容器持有类对象 PASS（去显式释放后仍正确）。
回归 E2E 115/117（78/79 已知遗留内存超限）、单测 1196/1196。
plans/014 已更新。
