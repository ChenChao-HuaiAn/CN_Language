# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（S1 服务轮·M3 采样第四窗+s269 抢救·2026-09-19·免号窄通道）

**背景**：同机 431-a 会话（C20 批3）开发线在飞——按家机常设角色「锁被持仍采样」转 S1 服务轮（免号·写面=tests/cnsmith_hits/ 窄通道）。与 431-a 写集零交叠（cnsmith432/ 采样产物 ∥ p431 扫描+audit2 E2E·全程零冲突实证）。

1. **s269.cn 抢救入库**（plans/026 §2.9 交付项销账）：`tests/cnsmith_hits/diff/m31_01_负移位常量位运算.cn`（D31 首采+D32 立案双身份·md5 与 target/ 四副本一致）——入库动作本会话执行；2bb33b16 系另一 cron 自动任务会话误判本会话已结束所代落库（用户已当场叫停该会话；本会话 8bd9741e 收尾时同内容 patch-id 去重自动跳过=预言闭环）。
2. **第四窗采样**（种子 1789807605/606）：新样本 500/500 全一致零分歧；负向 400/400 全拒绝；回归轨 121=118 一致+3 非一致全有既有定性（**优化器差分歧=0·零新回归**）。
3. **T53 跨平台实证**：m53_01（i128 容器追加段错误）在 win 侧复现 rc=0xC0000005——原案 linux（331-a 立案）；**T53=平台无关 IR 面**，修复在未集成堆积链 337-a；337-a 集成后此样本应转绿（集成验证点·已写入日志）。

### 二、接续点

1. daemon 常驻仍待认领（S1 剩余交付·本轮仍为手动单轮串行三轨编排 target/p432_seeds.txt）；
2. 337-a 集成后复跑 m53_01（回归轨自动覆盖·观察「已修」事件）；
3. 431-a 收尾（另一会话）：try-build 请求已发 7fc116b2（集成序 429→424→431）·批3 命名口径勘误 eb86b702。

### 三、坑与边界（本会话沉淀）

- **WindowsApps python 桩双进程假象**：Get-CimInstance 对 Store alias python 显示桩+真身两条同秒同命令记录——是同一逻辑进程，勿误判「误起两次」（16:21 scan 双进程即此假象·当日两犯）；
- **同机双会话同 develop 窄通道竞争**：两会话都在主工作树切 develop 直推时互相看见对方的 HEAD 跳变（ff abort 假象）——窄通道落盘应改 worktree 隔离（本节即 target/wt_s1 产出）；
- **回归轨 build_err 锚的「已修」判读**：build_err 类样本按 meta 预言态维持=正常（如 m38_01 转拒绝形态即 T38 归零），非「命中」——判读须逐例对照 meta 说明。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-19 ✅ **435-b 收工（E2E 453=451/0/2 零失败·79/456 全绿·链就绪待 win 回签集成）**。

### 一、本轮做了什么

1. **79"回归"一击定性＝守卫补丁遗留**（433-b 续作 TEMP 未撤即提交；撤补丁后自举编译 rc=0 实证·非代码回归）。
2. **456 上架 PASS**（v2闭环.txt 退出码=27＋coverage_map 实现面登记）。
3. **390 撤守卫崩溃定位（新增铁证）**：撤守卫态 390（向量<整32> 负下标防线）崩于 **向量$整32$预留 内 this 被 32 位截断**（r10=0x55802d0＝真堆指针 0x5555555802d0 低 32 位）——T=整32 容器的某处宽度泄漏把指针当 32 位处理（嫌疑=整32 元素容器的 this/字段寻址宽度污染）。gdb 现场+asm 已固化（/tmp/m390.asm 临时件不持久，复现命令见下）。
4. 复现命令：撤守卫（主.cn TEMP）→ `guardoff_主 tests/e2e/390_容器负下标防线/主.cn linux-x86_64` → as+g++ 链接 runtime objs → gdb run。

### 二·补（435-b 续·390 撤守卫新证据）

- **390 撤守卫全绿 rc=0**（负下标拒 2/2＋合法面 11/22/15/16 全对）——本轮连修两层：①实例布局登记 指针字段槽规范化（大小/对齐=8＋类型文本补星）＋布局 登记字段类型 星号同步；②发射类字段读取 指针字段读取宽度恒 T_整64（字段值=8 字节指针；原按字段宽度表=所指元素宽 4 加载=截断）。
- 456 同款探针 盒堆<整32> rc=27 ✓；123 撤守卫 rc=85 复验 ✓；守卫恢复态 123 rc=0 ✓。
- coverage_map 补登记 develop 既存孤儿 11 用例（431/432/433/440~448 系）→ strict 零孤儿 PASS。

### 二、门禁（本链历史最优）

全量 E2E **453=451/0/2 零失败**（含 456 rc=27、78/79 自举链双 PASS）＋strict 100%＋matrix 83/83＋守卫恢复态 123 rc=0＋撤守卫态 123 rc=85。

### 二·B（435-b 续·撤守卫前沿普查）

**撤守卫编译巡检（87 个 v2 闭环用例源码级编译）：52/87 通过**。失败 35 例绝大多数为**负测**（语义拒绝/穷举负测/引用逃逸拒绝等——编译失败＝用例本意，其诊断文本需与 .expected 对齐才构成真失败）。正测真前沿集中于运行级逐用例链接验证（123/390/456 已全绿）。下一轮：跑 runner 级撤守卫全量（负测按诊断文本比对）得出**真失败清单**再逐修。

### 二·C（436-b·runner 级撤守卫全量比对完成——真失败清单 29 例）

撤守卫 v2p（TEMP436b）跑全量：**464=433/29/2**。失败三族：
- **族A 编译中止 rc=1（约 20 例）**：容器拷贝构造/容器持有类对象/静态容器/结构体元素容器/嵌套容器/转移浅交接/借出视图族（223/224/227/229 按诊断文本比对）/双编译对照族（156/228/234/238/240/283/285/441）/generic 族（68）——stdlib 源码级加载后 各程序的 [ir2] 诊断家族（装箱语境/字段偏移/方法签名）仍在更广程序面上触发；
- **族B 运行期 SEGV（3 例）**：67/219/221（rc=-11）＋237 值差（251≠255）；
- **族C 自举（2 例）**：78/79 fix_p（v2 树自身嵌套泛型实例参数序错位·撤守卫固有面）。
**下一轮排序**：族A 按诊断家族分组逐修（复用 433/435 方法论：最小复现→IR 转储→gdb）→族B→族C→撤守卫全绿→评估去守卫化。

### 二·D（437-b·族A 根因普查完成——四异构根）

- **根① 类对象拷贝初始化未支持**（覆盖 111/68/203/127 外的大部分族A）：`向量<整64> b = a;` 声明位拷贝形态→IR 诊断"仅支持构造调用"——修=声明位类对象拷贝→拷贝构造调用（46-a 成员整体写同构）。最大单根。
- **根② 测试源结构体的实例内 类型大小 miss**（126：向量$词条$插入 的 类型大小(词条)→宽度 0）——词条=主.cn 声明的结构体·实例布局/结构体表未覆盖测试源结构体被泛型引用的面。
- **根③ 嵌套泛型实例参数序错位**（127：向量$映射$整64$整64 进函数路径——与族C 同根·剥实例参数段按 $ 拆分错位）。
- **根④ 结构体成员赋值右值（调用返回）判定缺**（441：调用返回右值非结构体返回值）。
- **修序建议**：根①（最大覆盖）→根③（与族C 同修）→根②→根④。

### 二·E（438-b 续·根① 已实施并真验）

- **根① 声明位类对象拷贝初始化已实施**（生成类.cn 变量声明类分支）：分配新对象+源求值+拷贝构造(this=新,其他=源)——拷贝构造缺失维持既有拒绝。撤守卫真验：**111/68/203 三例编译通过**；203 运行 rc=1（须核对其预期退出码）；**111/68 运行 SEGV＝拷贝构造内部下一层**（其他=引用形参的 `其他.数据[索引]` 成员下标读/复制链——下轮以 IR 转储定位）。
- 守卫已恢复：生产态 123 rc=0 ✓。

### 二·F（438-b 续二·推导层两修＋111/68 运行残留）

- 推导成员链类型ID 两修已落地：①成员分支 父层指针（尾'*'）剥层（其他=引用→其字段寻址）；②下标分支 指针父层剥星取所指元素（其他.数据[i] 原落字符串回退）；③成员分支 结构体键改 实例感知的 解析成员链结构体ID（查结构体类型 表不含实例合成名）。
- **111/68 运行 SEGV 残留**：撤守卫态仍崩于拷贝构造内部（复现=TEMP 撤守卫+编译 111 链接运行）——下轮以 IR 转储（TEMP438b2 现成）定位 取/下标读 的基址寄存器来源。
- 守卫已恢复（当前树=守卫开+推导两修未含 TEMP）。

### 二·G（438-b 续三·111 运行崩点 asm 新证据）

- 111 撤守卫运行崩点＝cn_main 内（主）首 打印 前：`mov -0x1c8(%rbp),%edi; call X`——**字符串/对象实参经 32 位 edi 装载**（指针截断）——嫌疑=打印 内置展开的实参宽度分派在撤守卫多参打印路径按整32 处理（123 的 打印(整数) 同内置但单参路径无此问题）。下轮：定位 打印 展开的实参宽度来源（寄存器类型登记 vs 实参类型码）。
- TEMP438c 撤守卫补丁当前在树（收工前必须恢复）；IR 转储过滤器=字符串子串前缀匹配（向量$整64$向量）。

### 二·H（439-b 续·111 崩点最终铁证）

- 撤守卫态 111：首打印"甲大小: 3"成功（拷贝构造本体工作✓）——崩于**元素值 10 被当指针解引用**（`mov (%r10),%r10`，r10=0xa=追加的首元素值）——即 乙.元素(0)/大小() 读链把**元素值当地址**。下轮：IR 转储（TEMP439b 现成）对 乙.元素(0) 的 生成下标访问/生成方法接收者 序列——查 推导/宽度分派把"元素值当地址"（嫌疑=下标读的对象基址错拿元素值·栈序/寄存器混淆）。
- 守卫已恢复：生产态 123 rc=0 ✓。

### 二·I（440-b 续·崩点指令精确定位）

- 撤守卫 111 崩点指令＝`mov (%r10),%r10`（r10=0xa=甲[0] 元素值）——**IR_指针加载(基址=元素值 10)**，崩点前序列：`-0x1f0 加载→+r11→-0x1f8 存→重取→(r10) 解引用`＝标准下标读形态（基址+下标×步进→指针加载）——**基址寄存器内容=元素值 10**＝下标读的"对象值"拿了元素值。位置=cn_main（主）内、`乙 = 甲` 拷贝之后、`乙.大小()` 打印前——嫌疑=拷贝初始化后的 乙.大小()/乙.元素(0) 接收者求值链把"元素值"当"对象"（寄存器复用/槽混淆）。
- TEMP440b 已恢复（生产态 123 rc=0 ✓）。

### 三、下一步

1. **集成**：integrate.py 队列挂起中——try-build 请求已发看板通告段（点名 win 回签 ec37b3a·arm64 周末豁免）——回签后 `--try-build-done` 集成 428~435 链。
2. **390 撤守卫残留**（下轮）：预留 this 32 位截断——从 整32 元素容器的 this/字段寻址宽度分派入手（456 同款最小复现法：盒堆<整32> 两元素+负下标即可复现）。
3. plans/021 排班队列继续销项。

### 四、踩过的坑

1. TEMP 补丁未撤即提交＝整轮假回归（79 定性耗时即此·第三次同族·须升权重）。
2. grep "错误" 会命中基线警告字样（582 条）——统计诊断数用 "ir2]" 精确前缀。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
