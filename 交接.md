# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（438-a 开发轮·T68 原始/多行字符串四形态·规范 01b 全量·2026-09-19·分支 任务/家机-438-原始字符串字面量·wt438 隔离）

1. **T68 销项**（09 缺口续·436-a 登记）：`原始"..."`（路径/正则反斜杠密集场景）v2 不支持——**规范 01b 第四节四形态全量落地**：普通 / 原始（反斜杠原样）/ 多行（跨行·内转义同普通）/ 原始多行（完全原样）。
2. **实现（词法三层）**：①**解码字符串内容** 单点函数抽取（普通/多行共用·原内联慢路径等价搬移·含 \u{} 三态诊断）；②**分词字符串扩展**（四形态扫描矩阵：单行原始=到首引号不跳反斜杠；多行=三重引号跨行；原始多行=完全原样）；③**主循环前缀识别**（id∈{原始,多行,原始多行}+紧邻引号→扩展分词·引号字节判定前置=存量零开销）。
3. **验证**：四形态探针 v2p 编译过+运行与宿主逐行一致+**459_字符串四形态矩阵** 双编译 PASS（expected 实跑固化）+**反证**（撤词法→「语句缺少分号（行 2）」复现）+09 推进（行 163 消失·错误 9→6）。

### 二、接续点

1. **09 下一层=函数指针面**（行 41 原型/行 237 函数指针类型参数·规范 04·**大特性**——下轮候选首优或需拆分）；
2. 438 try-build 请求（v2 树写集→点名深度机 linux）；集成队列全等回签（429→…→438）；
3. S1 daemon 常驻；M3 第五窗。

### 三、坑与边界（本会话沉淀）

- **heredoc/Write 脚本铁律再验证**：含反斜杠路径+省略号+三连引号的文档内容**一律 Write 直写 .py**（本轮 heredoc 两犯：`\d` 转义毁六要素·三连引号提前终止毁回填串）；
- **行切补丁须保留到文件尾**（本轮 `lines[531:535]` 手误截尾→git 恢复重跑——切分脚本写完先 `wc -l` 验）；
- **规范 01b 四形态语义**：普通串 \n=真换行；原始=反斜杠原样；多行=跨行+内转义；原始多行=完全原样（459 expected 从宿主实跑固化=四形态字节级基准）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-19 ✅ **服务轮第二波：堆积链收编完成（331/337/338/345~353 四链→develop f9e91ae7·用户裁决 arm64 豁免）**。

### 一、本轮做了什么（给无上下文的会话）

1. **用户裁决（收编波）**：授权 331/337/338/345~353 堆积链按「331-a 优先、346-a 先于 347-a」rebase 后经门禁集成（arm64 豁免同甲案·仅限本周末）。
2. **四链集成**：331→07bb9e0c（T11/T49/T50/T51/T52·单测 1353→1363）；337→25e600a5（T53+440）；338→f87bb26e（C19+OOM 备案+名单扩面）；353→f9e91ae7（345~353 全链 merge 三方并编：D33/T57/T58/T33+443/D11+444/T35/除法保护+446/T60+447）。
3. **353 链实战经验（重要）**：44 提交逐个 rebase 重放**不可靠**——循环脚本文档判定缺陷曾误丢 T33 代码（指纹审计抓获·重置重来）；改用 **merge 三方并编**（冲突面从 44 提交缩到 7 文件）：345/346 段与 develop 已有内容同源自动归一、run_e2e 取 develop 演进版、解析类.cn 语义合成（349-a 运算符检测 × 414-a 析构器段并立·~ 先判）。
4. **T60 遗留缺陷根治（机制级备案）**：353-a 收工时修复代码未 add 入库（447 用例+文档已入库）——收编门禁暴露 447-6a 红后补齐四处（生成层负号整数面产物码随操作数+登记·宿主 0-x 同域对齐；三后端 32 位分派 win/x64l neg eax+movsxd·arm64 neg w9+sxtw）。反证：回退复现红/恢复复绿。
5. **integrate.py 三处机制缺陷修复（随 331 入库·广播）**：产物路径错位（补 target/ 兜底）+E2E 漏传 --jobs（固定 8）+--target 平台名错位（linux-x64→linux-x86_64 映射）——此前该脚本 linux 侧从未真跑通过 E2E 步骤。
6. **教训**：①共享文档 rebase/merge 冲突解决后必须 grep 冲突标记+核对行数（checkout --ours/theirs 失败被 2>/dev/null 吞过两次·污染进过提交）；②收工提交前 git status 必查「预期写集=实际写集」（353-a 修复代码就是这么丢的）；③跨文档重构期（HANDOFF→交接/lessons→项目记忆）集成旧分支：rename 跟踪不可全信，文档冲突整文件取 develop 侧最稳。

### 二、验证链（全绿·develop f9e91ae7）

- 全量 E2E `--jobs 8` = **463=461/0/2 零红**（426/447 双转绿·2 跳=平台固有）；单测 **1363/1363**；自举链 78_v2/79_v2 双 PASS；每链集成时 integrate.py 全量门禁（合并结果上）逐链通过；strict/matrix 在甲案服务轮已过（本轮零规范面改动）。

### 三、下一步

1. **单位机周一（09-21）**：对 develop≥f9e91ae7 全量 E2E 补验（覆盖两波豁免全部内容）+329-D12 自行集成（看板通告已点名）；
2. **427-b 攻坚线**（另一会话在飞）：族② THIS 字段偏移链→族③装箱语境→撤守卫+390 终验收；
3. 家机 424/422/364 挂起链：等各自回签状态核实后另行处理（未在本轮授权范围）。

### 四、诚实边界

- arm64 侧验证=周一单位机补验（用户裁决豁免口径）——win 侧本轮也未独立复验（豁免口径只要求本机 linux 门禁·用户裁决明文「rebase 后经门禁集成」）；
- 353 链 merge 后 445/441/442 等早期新用例与 develop 既有用例的用例号占用关系未逐一审计（E2E 463 全绿兜底）。

## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
