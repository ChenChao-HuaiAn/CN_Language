# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（530-a 开发轮·D1 滚动=生成类对象局部拆分·2026-09-21·分支 任务/家机-530-d1parseclass·wt530 隔离·基线 48aa42d3）

1. **目标变更（诚实定性）**：原目标=解析类声明 185——实测为主循环「} 否则 如果」链式结构+方法
   分支双返回约束（位置+计数）——**不宜机械切片强拆**（三次试错被断言/编译器全拦·git checkout
   回滚零污染）——定性=链式解析函数需专用设计轮（已登记 021 §五 530-a 行）。改目标=生成类对象
   局部 147（生成类.cn·顺序块）。
2. **拆分**：147→主函数 93 行+`发射类拷贝构造初始化`（438-b 拷贝构造分支整块迁移·**不安全 函数
   修饰随迁**（指针下标写安全区边界）·槽c 参数显式传递）。
3. **等价铁证**：同宿主双 v2p 重建——五产 asm 探针 md5 逐字节一致（t_call/t_copy/**t_cpy 拷贝
   构造=2dd4131a**〔G1 直接消费面〕/t_lex/230 全链）。
4. **验证链**：Ninja 零警告（122s）+check_fn_length 出列+双文档门禁；集成时全量门禁+develop 现态
   补跑兜底（v2 树写集·522/529 同款诚实披露）。
5. **前半程（同会话 522/529）**：编码对象调用ARM64 拆分（eeac863c·门禁 4/4 全绿）+分词拆分
   （1005ac33·门禁补跑 3 假红串行全 PASS）+三链集成让位协调+僵尸 gate_lock 陈锁×3 清除。

### 二、接续点

1. 530 集成排队中；分支已推（拆分 2ed37b4e+收尾六件套），任意会话 `integrate.py --try-build-done`
   即可；**看板 530 行未直推**（全表 7>6 行数门禁超限·分支 push=认领已生效）——行位空出后补直推
   或随收工广播一并处理；
2. **D1 滚动新定性**：链式解析函数（解析类声明 185/解析选择语句 136 等）=专用设计轮候选（非机械
   切片）；顺序块候选余=生成下标左值地址 170（深度机 B1 面·待其集成后认领）/主 132（多面汇聚·
   谨慎）；
3. 动态锚点版切片脚本（wt530/target/split530b.py）=后续 D1 滚动模板（不依赖硬编码行号）。

### 三、坑与边界

- 链式 if-else 的「} 否则 如果」中段提取=结构重构非机械搬移——识别特征=闭合 `}` 在下一分支首行；
- 「不安全 函数」修饰必须随被拆函数迁移（指针下标写=安全区边界硬错误·编译器当场拦截）；
- 子函数参数表=主函数局部引用逐一核对（槽c 漏传编译器拦截）；动态锚点（next+内容断言）替代
  硬编码行号（行号漂移三次试错教训）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-21 ✅ **535-a 解析类声明设计方案轮（零代码·方案定稿登记待实施）**。

### 一、本轮做了什么（535-a·分支 `任务/深度机-535-解析类声明设计`）

1. **方案定稿**（整读 185 行结构分析）：五子函数划分——①继承跳过 ②可见性标签 ③方法成员（最大块·实施轮可再分）④字段成员 ⑤布局收口；**状态传递**=位置值返回+池/符号表引用+布局三元组（字段偏移/最大对齐/最大字段）引用参数+段可见性值输入+保底推进守卫留主函数。
2. **等价验证策略**：新旧 v2p 编译同一改前输入 asm md5（类全形态探针+全树）+布局累加变异反证+解析类用例族全量覆盖。
3. 零代码收工（方案文档落盘 plans/025 §2.115·登记待实施）。

### 二、接续点

1. **522+526 合批挂等三平台回签**；2. 513~517 挂等他家回签；3. 329-D12 双签齐待集成（集成后假红清零丁字账复验）；4. **解析类声明实施轮=下一会话首选拾取**（方案已定稿 §2.115）；5. B 区待用户裁决项维持。

### 三、坑与边界

1. 设计型函数拆分前置检查三清单：扁平分派 vs 嵌套流程/跨层返回终结语义/共享可变状态面（533 沉淀）。
2. 解析类声明的「位置」全程推进=子函数返回值设计（不传引用·防误用）；布局三元组跨分支累加=引用参数。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
