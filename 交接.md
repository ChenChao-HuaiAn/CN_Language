# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、会话总交付（429-a 收尾+431-a 开发轮·C20 批D 批2 收官+批3 上架·2026-09-19·分支 任务/家机-431-C20批3）

**429-a 收尾（分支 任务/家机-429-C20批D批2·头 89b6704b·已收工等回签）**：批2 上架 471~482+D36 销项确认成果已提交 push；rebase 最新 develop（并入 413~425 链）+解 021 戳冲突+**win 门禁 worktree 隔离重跑=零警告+单测 1353+E2E 464=463/1/0 零新增红**（唯一红 406 T43 既有）；搭车修复 develop 既有缺陷（check_own_matrix.py 漏注册 check_registry.json·426-a 遗留·机制级 §8.4）；try-build 请求+win 终态结论均已直推 develop（点名深度机 linux 回签·arm64 按用户周末裁决豁免）。

**431-a 主交付（C20 批3·分支 任务/家机-431-C20批3）**：①**356-a 式定性扫描本机重建**（68 例负测面四桶矩阵：sem5 26/parse 25/特殊 7+302 1/过 9）；②**防线对齐桶 12 例 v2 闭环负测上架**（sem5 10+parse 2·诊断准确例·12/12 单跑 PASS+反证抽例闭环）；③**新立 T63**（误导性缺分号诊断 11 例·D39 残余）+**新立 T64**（宿主拒 v2 过防线缺口 9 例·T6/C18/T42/D5 的 v2 侧 E2E 实证化清单）；④T62 复活定性（135 又 302=349-a 欠账链未集成实证·非新缺陷）。零 src/ 零 v2 树零 runner。

### 二、验证链

- 431：12/12 单跑 PASS+反证（140 配置改 0→FAIL→还原→PASS）+单测 1353/1353+全量 E2E（worktree 隔离重跑中·收尾回填）；429：win 终态 464=463/1/0 已回签通告。

### 三、接续点（按序）

1. 429+431 双分支等 try-build 回签（深度机 linux）→ `--try-build-done` 集成（序 429→431·coverage_map 并集）；
2. 批4 候选=候选池余 21 例；T63 误导诊断根治/T64 防线缺口族移植（D38 模式）已排 plans/021；
3. T47 口径裁决仍待用户（346 expected 101012→109912 须批）。

### 四、坑与边界（本会话沉淀·重要）

- **同机并行会话共用工作树实测干扰**：对方会话中途 `git checkout develop` 致本会话 429 门禁 E2E 跑在错误分支树（471~482 空壳假红 12）+431 首次全量门禁作废——**多例验证/门禁一律 worktree 隔离**（git worktree add --detach）；
- **v2p 文档捷径「入口」是占位符非字面参数**：真实调用=`v2p <源绝对路径> <平台>` 两参数；
- **孪生排除须跨号按主体名**（275 孪生=277_v2）；
- **清目录脚本按编号清单误删同号存量**（210 清单号删掉 210_v2_返回拥有字符串·已恢复）——批量清理须全名精确匹配；
- runner 固化捷径：expected 锚行路径必须=runner 隔离键路径（v2src<编号>_v2）·多文件用例依赖 .cn/.toml 全拷。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-19 ✅ **服务轮：甲案裁决执行+集成对账+develop 集成后验证（develop 基线 402e7ff·免分支免轮次号）**。

### 一、本轮做了什么（给无上下文的会话）

1. **背景**：单位机（ARM64）周末不工作，深度机 413~425 收工链的 try-build 卡在 arm64 回签。**用户裁决（甲案）**：本周末已获 win+linux 双签的收工链正常集成（arm64 事前回签豁免·仅限本周末）；单位机周一第一优先对最新 develop 全量 E2E 补验；329-D12 留单位机周一自行集成。
2. **集成对账（关键发现）**：413→414→416→417→418→419→420→421/422→423→425-b 链**此前已随集成队列滚动入 develop，无需新集成**。三证据：①448~455 新用例全在 develop 树；②T57 修复指纹「生成容器拷贝构造」命中 v2/IR/生成声明.cn；③421/423 旧分支头 vs develop 树 diff 仅 3~8 文件且方向=develop 领先（旧分支为滞后快照）。**教训**：看板通告的「集成序 413→…→421」是发出时点快照，集成队列滚动后不回改历史通告——接手必须用内容指纹对账（用例树+源码指纹+树 diff 方向），不能只看分支 SHA 包含关系（接力 rebase 使 SHA 不对应）。
3. **develop 集成后验证（甲案验收）**：见下验证链，全绿零新增红。
4. **剩余欠账对账（呈报待授权）**：430~447 用例仍未入 develop＝331/337/338/345~353 堆积链（家机 09-19 盘点通告仍现行）。收编顺序敏感：331-a 先（T50 让 426 转绿）、346-a 先于 347-a。**已呈报用户待授权，未经授权不擅自收编**（裁决语字面范围仅 413→…→423）。
5. **在飞不动**：深度机行 427-b 族② 攻坚（421 分支续作）为另一会话在飞轮，本轮未触碰。

### 二、验证链（全绿·develop 402e7ff）

- 构建零警告；单测 **1353/1353**；全量 E2E `--jobs 8` = **452=449/1/2**（唯一红 426=基线既有 T50·其修复在未集成的 331 链·2 跳=平台固有）；strict 100%；matrix 83/83；自举链 78_v2/79_v2 双 PASS。

### 三、下一步

1. **等用户授权**：331~353 堆积链收编（回签状态先核实，顺序敏感如上）；
2. **单位机周一**：对 develop≥本轮提交 全量 E2E 补验+329-D12 自行集成（看板通告已点名）；
3. **427-b 攻坚线**（另一会话）：族② THIS 字段偏移链→族③装箱语境→撤守卫+390 终验收（不受本轮影响）。

### 四、诚实边界

- 本轮零行为面改动（纯服务轮：对账+验证+落盘）；426 红与 430~447 缺用例均为**已知既有欠账**非本轮引入；
- 家机 424/422/364 挂起链未在裁决语字面范围内，未动，待用户进一步指令。

## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
