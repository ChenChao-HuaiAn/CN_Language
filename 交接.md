# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（481-a 开发轮·plans/021 清账轮·2026-09-20·分支 任务/家机-481-021清账·wt481）

1. **清账动因（用户质询「做完了的任务没销账」）**：§3 队列区（定位「只列未完成」）实测残留 **31 行已标 ✅ 销项/收官/撤案**
   的条目（S1／D28/D29/D34/D35/D37/D38/D46/D47／F2-36a／G 区 20 行）+ **C19/T11/T43/T53 实质完成但条目未标**
   （四证据：338-a 实施+堆积链收编／260-a+331-a／437-a+453-a／337 链收编）——「标而不迁」逐轮累积无门禁拦截。
2. **清账**：31 行迁 §4.2（8 条已有台账行直删原行，其余 23 条压缩迁入）+C19/T11/T43/T53 补销项迁 §4.2（台账共 +29 行）
   +T47/T48 呈报迁 §3-B（`> **B` 单行格式·生命周期铁律）+D33（422-a 实施完毕挂等集成·不虚销）/D45（去「撤案维持」改活条目）
   /D8/D10/F1-26（479-a 波1 在飞）状态刷新+G 区收敛为**活案表**（只留 T1/T10/T35/T69/T70+两条活跃观察记录）。
3. **结构修复**：§五索引表被 §六导航拦腰截断→恢复连续单表（§六后 3 行归位·引用块移表后）+L190 无编号断行删+C13 行补列
   +文首戳刷 481+在飞刷新。021 全文 410→391 行。
4. **门禁防复发**：check_handoff.py 新增「§3 队列区禁完成标记」检查（正则 ✅\s*(全销项|销项|实质销项|销项确认|收官)|撤案·
   区间=§三~§四·行内代码排除·子项进度注记不误伤）——反例注入拦截+正例还原 PASS 双向实证；看板行上限 4→6
   （照抄 459 分支同文=rebase 自动合并）；棘轮基线收紧兑现 429 暂放行承诺（021 620→450·025 850→700·实际 391/532）。
5. **顺带**：025 家机节按「整节替换」纪律清掉 20+ 历史轮小节（807→532 行·回棘轮内）。

### 二、接续点

1. 本轮写集=纯文档+平台无关 py（§8.3）→ 快速门禁即集成，无需 try-build；check_handoff.py 属三机共用门禁——
   他机 pull 后即新版（§3 注入完成标记会被拦），**深度机 474（D46 销项）等分支 rebase 时若 021 §3-D/§4.2 同区冲突，取本清账版**；
2. 家机存量挂起链不动（各归各轮）：468=九链重测在飞／459/452/456=深度机已回签挂等集成／471/476=挂等回签
   （476 已获深度机回签·可 `--try-build-done`）；
3. 479-a（深度机 F1-26 波1）win 回签未做——留待后续服务轮跑全量门禁后回签。

### 三、坑与边界

- **协议v2试点条目未销**：v2 已常态化实战但 plans/027 §六试点复盘回填未查证——不虚构销项，留待查证后处理；
- C13 销项台账行曾为 3 列（与 4 列表头不齐）——本轮补齐；台账行一律 4 列；
- check_handoff「单行 ≤400 字符」会拦「状态注记堆进长行」——D8/D10 行刷新时两次触线，压缩指针化后通过（写长行前先数）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **本会话六轮：459/467 服务轮回签+470 D10 双侧销项（挂等回签）+474 D46 第一段（挂等回签）+475/476 D46 二三段（已集成·家族终态）+478 F1-26 侦察设计定稿**。

### 一、本轮做了什么

1. **服务轮**：459-a linux 回签+451 集成后复验备案+467-a linux 回签（准予集成）。
2. **470-a D10 双侧销项**（挂等家机 win 回签）：v2 泛型实例体重放语义检查+457 负测+宿主 recheck 诊断窄放行——try-build 请求已发（8415a43f）。
3. **474-a D46 第一段**（挂等家机 win 回签）：返回语句位裸名拒绝+458 负测——try-build 请求已发（94596572）。
4. **475/476-a D46 二三段（已集成·家族终态）**：声明初始化位（475·b740de54）+赋值位（476·19ec879b）+59 行实测定案（callee/裸名共用路径维持放行）。
5. **478-a F1-26 侦察设计定稿**：opt 层基建盘点（DomTree/SSAPass/Phi 降级 80% 在位）+量化（-O3 用户段内存槽访存 48/177≈27%）+真 mem2reg 三遍算法设计+三波实施拆波（025 §2.87）——实施轮待认领。
6. **477-a D10 波2**（⏸ 挂等）：实现强依赖 470 重放块——六要素已落盘·470 集成后实施。

### 二、验证链

- 各轮全量门禁全绿（477=475/0/2、476=476 例 474/0/2 口径·零新增红·数字见各提交信息）+反证双态四组（457/458/p9/p10 全套）。
- F1-26 量化：objdump 统计可复现（025 §2.87 回填命令级）。

### 三、下一步

- **470/474 家机 win 回签→--try-build-done 集成→看板清行**（接手会话第一动作：fetch 看板通告段）。
- 477（D10 波2）挂等 470 集成后实施（六要素已落盘 §2.86）。
- 478 波1（mem2reg 基建）实施轮待认领（设计已定稿 §2.87）。
- 469 撤守卫残余定性（同机他会话在飞）。

### 四、踩过的坑

1. 59 行=callee/裸名共用路径（475 实测定案·构造名裸名防线只能语句级单点）。
2. 语义检查主分派委托结构（变量声明真身在语义检查语句.cn）。
3. 负测 expected 行号=runner 真跑口径（三轮同坑）。
4. **cn build --output 产最终二进制非 asm**（量化统计须 objdump 反汇编 ELF）。
5. 轮次号销账清行后的时序窗口=他机可复用同号（476 深度机集成销账后家机认领 476-T71·历史重复无害·分支名可辨）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
