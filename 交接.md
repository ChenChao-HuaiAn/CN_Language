# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（722-a·崩溃指令精确定位·纯侦查·分支 `任务/家机-722-设置PROC定点修复`·wt698 树·接 721）

dumpbin /DISASM 全反汇编 cn_self.exe（140 万行）对位 VEH 捕获的崩溃 RVA 0x31256：崩溃指令=**设置 克隆体重哈希循环的 `mov [rax],rcx`，rax=链数组+重哈希×8=NULL**（对应 映射集合.cn 设置 的 `链数组[重哈希] = 桶数组[h]`）——即存在**「元素数量>0 而 链数组=NULL」的坏映射对象**。来源收敛=**元素 返回 T 拷贝路径的拷贝构造克隆体字段错位产物**（表查已转移 读层映射→元素 返回拷贝→坏拷贝后续被 设置 触发重哈希崩）。调用链三帧全对位（语义分析多文件→表查已转移→元素→设置）。

### 二、下一棒

**723=拷贝构造克隆体三深拷循环对拍**（wt698 树续）：dump 拷贝构造 PROC（桶/键/值/链 四循环）的基址装载/步进/边界发射 vs 宿主版→找「链数组=NULL 但 元素数量>0」的字段错位点→定点修复→cn_self t4 跑通→**t4→0/fix_s 67157→0 判据解锁**→-7 固定点→056 传址化→709=055+113→零红→终章→001 销项。顺带：integrate.py 写集分类补 CN语言编译器v2/。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①dumpbin /DISASM 输出 140 万行带 RVA——配合 VEH 的 RIP-RVA 打印=无调试器精确定位崩溃指令的完整闭环；②MASM hex 符号名+map 符号区间（≤RVA 最近符号）双层对位=崩溃函数秒级定位。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-25 服务轮（家机 721-a 验收回签·窄通道②·零分支零轮次号·基线 develop **87870e7c**）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **选题依据**：家机 721-a 验收请求点名深度机（基线 79573fc4·src 写集=src/runtime/io_api.cpp VEH 增强·「runtime io_api 变更面全量复跑＋请复验 VEH 输出形态」）——develop 前进 3 提交（15e70b86/79573fc4/87870e7c）后 fetch 即见，按服务轮职责应答。
2. **变更面代码级定性（先核后跑）**：721-a 新增 34 行（ASLR RVA 换算 GetModuleHandleExW＋rbp 链回溯 16 层 ReadProcessMemory）**全部位于 io_api.cpp 既有 `#ifdef _WIN32` 守卫段内**（585→667 行）；runtime.cpp entry() 两调用点已由 722-a 同名守卫收口——**linux/arm64 侧零行为面＋零编译面影响**（GCC 条件编译不进入该段）。
3. **独立树全量门禁（铁证面）**：worktree wtsvc721（87870e7c·距 Documents 两级）全新构建——零警告（GCC -Wall -Wextra -Werror 下构建成功即证）＋单测 **1405/1405**＋strict **181/181**＋matrix **83/83**＋E2E **528=523/3/2**——红名单与已知基线完全同构（113 值差/127_v2 rc206/79_v2 fix_s rc1·零新增红零回归）；E2E 显式 --jobs 8·gate_lock 串行锁全程。
4. **VEH 输出形态复验口径（诚实边界）**：VEH/UEF 是 Windows 运行时机制，输出格式（[veh] code/RIP/RSP/fault/base/RVA＋frame%02d ret RVA）的运行级复验归 win 侧（家机自有面）；linux 可验面=「变更不破坏 linux 构建与测试」已闭环＋代码审读确认输出字段与 721-a 广播一致。
5. **B8 巡视（服务轮默认动作）**：cnsmith daemon 存活；今日已有一轮完整采样（01:2x·新轨 40/40 一致＋回归轨 56 零新缺陷类别·m27 x64 转绿=319-a 复验确认·m42 存量复现）——M3 计时连续，本轮巡视确认无新写入需要处置。

### 二、接续点

1. **深度机侧零待办**（721-a 回签已闭环）；下一棒=722 设置/元素 PROC 指令对位→定点修复（家机 wt698 接力·看板行已有）；
2. 550 阶段二呈报仍挂用户裁决（&容器变量 语义澄清·材料 025 §2.124）；M5/M6 补测挂家机 708-e 链集成后重跑 bench；
3. 上轮挂等核查轮接续点维持：@家机 561/499 分支保全处置；040/045/033 摘取 rebase 集成（480-a 已保全可 fetch）。

### 三、坑与边界

1. **服务轮验收的「先核后跑」**：跨平台变更面先做守卫段代码级定性（本例 io_api.cpp 全在 _WIN32 内）再跑门禁——若变更真触及 linux 编译路径，独立树全新构建才能兜住（复用树的缓存/残留假绿在本轮依然适用独立树纪律）；
2. （历轮在册仍有效：挂账分支核查须含 ls-remote 存在性＋树级特征码双道／他机资产分支引用不可代动／分支长期挂等定期重验／探针重建四连坑／rebase 丢纯文档提交看板断链／v2p 产物相对 cwd／gate_lock 陈锁 rm 接续／直推前树 diff 核验／门禁命令裸跑取 $?／integrate.py 写集分类对 CN语言编译器v2/ 失效须自行补跑全量。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


