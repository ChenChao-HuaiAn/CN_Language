# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（522-a 开发轮·D1 滚动=编码对象调用ARM64 拆分·2026-09-20·分支 任务/家机-522-d1objcall·wt522 隔离·基线 e83be7f2）

1. **拆分**：`编码对象调用ARM64` 153 行→主分派器 16 行+五子函数（分配对象/结构体拷贝/删除对象/
   调用参数化〔内拆 装载栈实参/装载结果〕）——python 行号切片+20 边界断言（断言当场拦截两次切片
   失误〔漏 706 行前文件体/漏 809 闭合括号〕=断言即防线）；逻辑逐字迁移注释逐字保留。
2. **等价铁证**：同宿主双 v2p 重建（develop 基线树 vs 拆分树）——三产 asm 探针 md5 逐字节一致
   （t_call 调用参数化四面=4e259ec6/t_copy=05a55749/230 字段所有权 493 节点=b07b8f1d）；
   t_obj/235 两侧行为逐字符一致（v2p 类字段语法已知缺口·非本轮面）。
3. **验证链**：Ninja+sccache 零警告构建 PASS+check_fn_length 出列+双文档门禁 PASS；集成时全量门禁兜底。
4. **前半程（同会话）**：三链集成攻坚让位实录——483 集成让位 -i2 会话（其 cherry-pick 收编本会话
   解的 rebase/滚转/coverage_map 补登记）；494 让位他会话（其「集成补刀」已入 develop）；497 本会话
   rebase fe30216b+skip 过时补登记（36a99001 已推·integrate 排队中）；三链集成之争的教训=多会话
   同攻一链时以看板/树内痕迹（分支名 -i2 后缀）让位优先。
5. **僵尸 gate_lock 陈锁×3**（11856/24020/3248·均 tasklist 实证持锁进程已死）当场清除并看板通报
   ——疑某会话门禁循环崩溃遗留。

### 二、接续点

1. 497 集成（本会话启动排队）若本会话消亡：分支 任务/家机-497-d1return 头 36a99001 已 rebase
   fe30216b+滚转+补登记齐备，任意会话 `integrate.py --try-build-done` 即可；
2. D1 滚动下一列=分词 151（词法分析.cn）/克隆节点指针 138（IR类.cn）——深度机面与 F1-26 面交接；
3. v2p 手工构建三坑（cwd=仓库根/asm=target/v2asm.asm 相对 cwd/stdlib 在仓库根）已入册教训。

### 三、坑与边界

- subprocess list 形态传 cmd /c 长命令会双重引号转义——shell=True 单串；
- python（pythoncore 3.14）对 junction Path.exists() 误报 False（cmd 确认创建成功）——判定改 rc；
- keep-both 解 coverage_map 单元行冲突会产生重复行（解析器只认其一）——单元行冲突应「以一方为基
  注入另一方增量」。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **524-a D25 gcov 复验（零代码服务轮·已收工）**〔本会话累计=470/474/477 三链集成+518/520/523 三轮 D1 拆分集成+329-D12 linux 回签+521 D11 侦查+524 gcov 复验；522 挂等三平台回签〕。

### 一、本轮做了什么（524-a·分支 `任务/深度机-524-d25gcov`）

1. 官方 `CN_ENABLE_COVERAGE` 设施（250-a 建）插桩构建（target/build-cov·产物 target/cov/）+单测 1387+全量 E2E 522 真跑。
2. **D25 复验数字**：ir_verify 行 86.58%（283-a 基线 85.59→+0.99pp）/ast_printer 行 68.78%（471-a 前 14.6→大幅提升）——471-a 语料效果 linux 实证；全项目行 79.21%/分支 45.84%。
3. 注记：总行<基线=门禁语料步未跑的低估（E2E 假红中止·gcda 聚合完整·非真劣化）。

### 二、接续点

1. **522 挂等三平台回签**（497 转正）→回签即 integrate；2. 501/513~517 挂等他家回签；3. 329-D12 双签齐待集成（集成后假红清零丁字账复验）；4. D25 剩余=ast_printer 深层分支（取正 36.54%）语料可选补强；5. D1 剩余=编码算术Win 206（避让 501）/解析类声明 185（设计型）。

### 三、坑与边界

1. coverage 官方设施=`cmake -DCN_ENABLE_COVERAGE=ON`（产物 target/cov/·勿用手拼 --coverage flags——输出目录会污染常规构建）。
2. check_source_coverage 全链在 E2E 假红时 rc=1 中止→`--skip-build --skip-unit --skip-e2e --skip-matrix` 直接聚合现有 gcda（gcda 累积自单测+E2E 真跑）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
