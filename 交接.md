# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（699-a·任务 002 存量单侧面全量分型+首批转批·分支 `任务/家机-699-002双测收口`·wt699）

任务 002 的存量面完成**终态定量**：137 例 host-only + 9 例 v2闭环 逐例实测（工具=`target/scan_v2.py` v2p 编译期 6 并发扫描 + `target/probe_dual.py` 完整双测探测；**首扫误判根因=缺 stdlib junction**，v2「标准库模块守卫」按相对 cwd 读 stdlib/，修正后编译通过面 20→28）。
- 分型：**编译通过 30 / 编译失败 103 / 跳过 3**（78_v2/79_v2/151_check=自举链/全量检查通道，非 host-only 面）。
- **可转 6 例已上架**（正式 runner 逐例 PASS）：26_generics / 385_常量位域回绕 / 386_常量位域回绕_O0 / 434_货舱特性空数组边界 / 500_T96i128三元与变量推断 / 58_generic_field——配置=退出码 0+断言输出=是+借链=否。
- **v2 机制级修复 2 处**（§8.4 当场修·系统性核查确证仅此两处）：`语义/语义分析.cn` `是基础类型文本` 补 `正128`；`语义/语义检查初始化.cn` `状态是值类型`「标量 14 型」补 `正128/浮32/浮64`（对齐宿主 `isDefInitScalarType` 前缀判定）→ 编译通过面 28→30（04_types/11_i128 解锁）。
- **余 131 例归 17 族**（023 细化依据·完整族清单+用例号=025 §1.9 收工回填）。
> 〔并行轮次 700-a（051 第二层·已集成）〕要点=元素写深拷落地（分支保全）·218 假平衡暴露析构链缺口·元素读取语义呈报；明细=025 §1.9。
> 〔并行轮次 702~704（051 析构链/79_v2 链接符号/cn_self 形参登记·已集成）〕要点见 025 §1.10/§1.11。
> 〔并行轮次（051 主线 702~704·已集成）〕要点见 025 §1.9~§1.12。

### 二、下一棒（按杠杆排序）

1. **023 G-F stdlib `IO::打印` 限定名导入族 10 例**（高杠杆主攻）：用例 `导入 IO::打印;` 后调 `打印(...)`，v2 报「未声明标识符: IO::打印」——定位=限定名导入解析；**主攻前先评估与 001 波3（stdlib 源码级加载）的耦合**。
2. **023 G-N 解析缺口 23 例逐例细分**（已定位 3 例：10_struct_value 嵌套数组字面量 `分数={1,2,3}`／134 成员自增 `q.x++`／161 父挂子模块限定调用 `数学::几何::面积`）。
3. **001-001/051 主线面**（勿重复派工）：容器元素链段错误 6 例（110/115/116/117/155/203=0xC0000005）+ 运行期值错 7 例（07 指针成员访问/24 预处理/352/366/379/390/406/495）+ ml64 汇编 2 例（93/483）+ 链接 2 例（31/43）+ v2 缺负测检查 3 例（299/81/435）。
4. 探测脚本（`target/scan_v2.py`/`probe_dual.py`/`write_dual_cfg.py`/`group_gaps.py`/`check_typenames.py`）=临时产物，**下轮决定转正（建议 `scripts/`）或删除**；`check_typenames.py` 建议转正为类型名列表完整性门禁（防同类漏项复发）。
> 〔并行轮次 700-a（051 第二层·已集成）〕要点=元素写深拷落地（分支保全）·218 假平衡暴露析构链缺口·元素读取语义呈报；明细=025 §1.9。
> 〔并行轮次 702~704（051 析构链/79_v2 链接符号/cn_self 形参登记·已集成）〕要点见 025 §1.10/§1.11。
> 〔并行轮次（051 主线 702~704·已集成）〕要点见 025 §1.9~§1.12。

### 三、踩过的坑（教训）

1. **stdlib junction 缺失致批量扫描假红**：v2 的「标准库模块守卫」与 IR签名 扫描按**相对 cwd** 读 `stdlib/`——批扫工作目录未建 junction 时，名单内模块（容器/核心等）误报「无法打开依赖模块」+ 连锁「语法错误」假红（首扫 54 例语法错误中大半源于此）。凡批量试编译涉 stdlib 必须建 junction（对齐 runner 111-a/1594 mklink /J）。
2. **Windows multiprocessing spawn 全局变量不传递**：扫描脚本用模块级全局 `v2p` → 子进程重新导入模块后为 None → `FileNotFoundError: WinError 2`（且 `None` 被当作可执行名）。修法=路径随参数传递，勿依赖跨进程全局。
3. **强杀后台任务留残迹**：探测脚本被强杀时在用例目录留下 `双编译对照.txt`（15_expressiveness 一例）——收工前必须 `git status` 核对清理（025 计划已预判此项并在收工时验证执行）。
> 〔并行轮次 698-a（051 专项·已集成）〕要点=探针干净矩阵 target/p698+mini_dbg4 断点式+650-a 拷贝构造源实参装配机制级修复+T& 形参契约=指向句柄槽的指针（两跳）非壳地址；第二层（113/127_v2/497/79_v2）接力轮在飞=700；明细=025 §1.9/§1.8。
> 〔并行轮次 700-a（051 第二层·已集成）〕要点=元素写深拷落地（分支保全）·218 假平衡暴露析构链缺口·元素读取语义呈报；明细=025 §1.9。
> 〔并行轮次 702~704（051 析构链/79_v2 链接符号/cn_self 形参登记·已集成）〕要点见 025 §1.10/§1.11。
> 〔并行轮次（051 主线 702~704·已集成）〕要点见 025 §1.9~§1.12。

## 深度机 linux-x86_64 节
## 深度机 linux-x86_64 节

**交接时间**: 2026-09-23 晚 🛠 **657-a 开发轮（021 任务 024/D43 整数/小数内置别名 v2 全链收口+02_control 阻塞面真根因修复·分支 `任务/深度机-657-D43别名全链`·wt657）**。

### 一、本轮做了什么

1. **02_control 阻塞面真根因实锤+修复（机制级·随证据备案）**：02_control 的 v2 阻塞非别名本身（402-a 已通）——插桩五轮锁定 **413-a 检查选择语义层漏「类型过滤=节点_情况标签」**：情况段区间=[情况值+体语句+体块+标签] 全部节点（解析层子起点=吃 `{` 后池位置·生成层两处消费点均有过滤·语义层漏）——无过滤把如果/当节点字段 [文本,附加)（=[条件区+真块区]）当值表达式区间在**外层作用域**查名→嵌套块局部声明伪报「未声明标识符」。修复=况i 循环加类型过滤；探针矩阵 5/5 双侧逐值一致（宿主 cn build 对照全同）+反证B（撤过滤→p2×3/p2b×2 伪报复现→恢复复绿）。
2. **024/D43 收口销项（09-20 用户裁决甲落地）**：①内置别名基名规范化=唯一权威定义点（解析类型文本 基名级挂接·字符* 借用返回+A2 合规·调用方 字符串复制 单次拷贝）；②是别名关键字 谓词单一归属（5 处散点换接）；③IR布局 是标量类型文本 删「整数/小数」不可达防御分支（反证A=撤挂接伪报 ×3 复现背书源头完备性）；④02_control 转双编译对照上架 **runner PASS**（宿主-only 存量 -1）。
3. **门禁全链**：单测 1405/1405+strict 100% rc=0+matrix 83/83+E2E 全量 **528=522/4/2**（4 红=113/127_v2/497/79_v2 撤守卫态已知基线红·与 629-a 广播/单位机验收口径一致=零新增红；78_v2 PASS；选择语句族 448/450/66/85 零回归）。
4. 看板棘轮 201>200→深度机通告段已毕条目整段清偿（546~603 广播·影响面已执行完毕）188 行达标；教训 2 条入册（checkout 误丢未提交改动=402-a 二犯/管道吞 rc 四犯×旧产物缓存假绿复合）。

### 二、接续点

1. **550 阶段二呈报**仍挂用户裁决（&容器变量 语义澄清）；2. **524 win 单侧红**待侦查修复轮；3. 001-001 主攻面=家机 652/653 在飞（拷贝构造 IR 逐指令+runner 遗欠）；4. 集成后请两机对选择语句面（450/66/85/448/02_control）异步验收（v2p 重建生效）。

### 三、坑与边界

1. **git checkout 恢复误丢未提交改动**（402-a 教训二犯）：反证实验恢复=反向 Edit 或先提交再实验；2. **管道吞 rc×旧产物缓存假绿复合**：构建验证一律 `cmd > log 2>&1; echo rc=$?` + rm 旧产物再建；3. 沿用：gate_lock 陈锁 rm 接续/直推前树 diff 核验/单跑定性假红。

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


