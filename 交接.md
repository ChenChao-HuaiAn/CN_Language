# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（458-a 开发轮·D8/F1-28 第三步=x64 结果写入直写·2026-09-20·分支 任务/家机-458-x64冗余mov消除·worktree=wt458·基于最新 develop 集成序独立）

**背景**：451/457 两轮（arm64 面·挂起等回签）已完成 ARM64 冗余 mov 消除 -97.6%。本轮移植同模式到 **x64（win 主平台）**——453（437 cherry-pick）已集成 8f5e1cb2=写集障碍解除。

1. **基线取证**：win 产物中转 mov（正则=`mov r12~r15, rax/eax/rcx/rdx`）291 用例 **91197 条**（超 arm64 的 88637）。
2. **实施**：`physRegOf(value)` 辅助+五发射方法直写——emitIntBinary（64 位计算寄存器 rax→dst；8/16 位扩展分支 movsx/movzx 直落 dst 32 位形态；**冲突判据**=op2 与 dst 同物理寄存器〔分配器死点复用〕退回原三段——两地址 `op r12, r12` 先覆盖 op2 原值=错）、emitShift（cl 固定约束不冲突）、emitConstLoad（64 位整型/字符串 LEA/函数地址 LEA）、emitLoadStore Load（扩展直落+64 位单条 mem→reg）、emitAddrOf（LEA 直装）。
3. **必要搬移定性（不改）**：Call 结果接收（rax=ABI·自举链残余主体=__cn_str_* 调用密集）、idiv/div 商余固定 eax/edx、i128 双槽、浮点、32 位常量装载（前人已直写）。
4. **成果**：**91197→22960 条（-74.8%）**；探针 asm 铁证 `add r12, 12` 直写。
5. **验证链**：零警告+单测 1363/1363+**E2E 464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线·零新增红）+asm 宽度 289×3 PASS+**反证闭环**（stash 撤改→复现 9 条→恢复→2 条=Call 接收必要）。
6. **诚实边界**：x64 装载面（mov eax, r12d 型·统计口径外）、PtrLoadStore 地址面（[rax] 硬编码多·收益/成本比低）、Cast 细面（xmm0/eax 混合）=后续轮；arm64 运行级 bench=跨机复验轮。

### 二、接续点

1. **try-build 请求挂起中**（写集=x64 后端=三平台共享层）：点名深度机 linux 回签+单位机周一 arm64 侧补验；回签后 `--try-build-done`（与 451/457 同批回签·集成序 451→457→458·458 独立可先）。
2. **下一轮候选**：arm64 oop/dispatch 装载收尾（2125 残余）或 x64 装载面（mov eax, r12d 型）。
3. 同机 454/456/452 状态见看板；453 已集成 8f5e1cb2（406/431 清零=develop 全绿基线）。

### 三、坑与边界（本会话沉淀）

- **x86 两地址 ISA 的直写冲突判据**：op2 与 dst 同物理寄存器（分配器死点复用 op2 死于本指令）时 `mov dst, op1; op dst, dst` 会先覆盖 op2 原值——必须退回原三段；arm64（RISC 三地址·操作数走固定临时装载）无此面=451 模式不能盲目移植；
- **mem→mem mov 禁止**：未分配态 dst 是槽文本，src 亦可能是槽——必须经 rax 中转（原三段保持）；
- **div/idiv 商余寄存器硬件固定**（eax/edx）——尾部 mov dst, eax=必要搬移非冗余；
- sccache 跨树命中诊断会显示他树路径（include 链缓存条目）——非 CMakeCache 污染（源路径须查 CMakeCache.txt 确认）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **本会话四轮连续：459/467 服务轮回签+470 D10 双侧销项（挂等家机回签）+474 D46 销项（挂等家机回签）+475 D46 残余收口**。

### 一、本轮做了什么

1. **服务轮**：459-a linux 回签（准予集成）+451 集成后复验备案+467-a linux 回签（1369/1369+E2E 477=475/0/2 零新增红）。
2. **470-a D10 双侧销项**（挂等家机 win 回签·头 bbe06c6c）：v2 泛型实例体重放语义检查+457 负测+反证+宿主 recheck 诊断窄放行——try-build 请求已发。
3. **474-a D46 销项**（挂等家机 win 回签·头 36b7d900）：v2 构造名裸名返回语句位拒绝+458 负测+反证——try-build 请求已发。
4. **475-a D46 残余收口**：声明初始化位构造名裸名拒绝（语义检查语句.cn +17 行·p9 rc 0→1+反证双态）——**59 行实测定案**=callee/裸名共用路径维持放行（方案 A 24 族 callee 位回归实证→方案 C 语句级单点·演进全记录 025 §2.84）；再残余=赋值位/实参位登记 021。

### 二、验证链

- 475：p9 双侧对照+健康面（24 族 7/7+211_v2）+门禁 476=474/0/2 零新增红+strict/matrix+反证双态。
- 470/474：两轮门禁各 477=475/0/2 零新增红（详见各通告）。

### 三、下一步

- 470/474 家机 win 回签→`--try-build-done` 集成→看板清行（两个分支）。
- 队列续：赋值位/实参位裸名（D46 再残余）/波2 环境映射（D10 残余）/F1-26 性能线候选。

### 四、踩过的坑

1. **语义检查分派结构**：检查块 主分派 对 节点_变量声明 委托 语义检查语句.cn 的 检查变量声明语句（语义检查.cn 818 行的标注+循环块=其他语句类型·首插错位 p9 无效）——语句级插入先 grep 函数真身。
2. **59 行=callee/裸名共用路径**（71-a 注释原意正确）——「改放行为拒绝」必回归 callee 位（24 族实证）·构造名裸名防线只能语句级单点。
3. 负测 expected 行号=runner 真跑口径（三轮同坑）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
