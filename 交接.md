# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（609-a 开发轮·021 003 收尾三件=425/123 规则1/170·分支 `任务/家机-609-003负测`·wt609 隔离·基线 develop 44ea9ab4→d2946eef·与 608〔已集成〕并行）

1. **425 登记模式重做**：v2 空类型参与运算拒绝双面——二元运算面（检查二元运算表达式 内·表达式类型ID 静默诊断缓冲推导·文案对齐宿主）+声明/赋值/返回面（检查标量隐式转换 单点·调用源精确空类型判定+早退）；转 dual 负形态；174 十四行诊断零双报。
2. **123 规则1**：v2 表达式语句根调用「结果 丢弃未检查」检查（对齐宿主 checkResultDiscard）——stdlib 容器方法经 IR签名 签名表回退（键=基名.方法名.实参数·原文 结果</可选< 前缀 7 字节判定·收窄单点防声明位误伤）；123 合规化（设1/设2 接收式·退出码 21333 不变）转 dual 正测+84 死配置矫正转 dual 负形态。
3. **170 前置标注核查→根治**：宿主 mergeModuleDecls 五类过滤点私有符号存在性登记（Program::modulePrivateSymbols 新载体）→collectModulePublicSymbols 并进 moduleAllSymbols_/knownModules_——591-a 导入位私有拒绝死代码激活；转 dual 负形态。
4. **附带机制级修复**：runner dual 通道宿主退出码断言 win 全值/linux %256 平台分派（123=21333 首例 win 假红）。
5. **实测**：四例单跑全绿+反证双态全命中+全量门禁 单测 1383/1383+E2E 526=526/0/0（含 78/79 自举链）+零警告。

### 二、发现的问题与定性

- **误伤两轮（CI 实锤·同轮根治）**：①34_file=`写句柄 != 无`（空类型* 指针变量）经 表查找类型ID 返回所指类型裸「空类型」→判定收紧为调用形态末端；②196/197/203/208 转移族=静默推导二次到达 检查转移调用 提前触发已转移标记（**非诊断类状态副作用·静默缓冲防不住**）→新增 是副作用内置被调（转移/复制/释放）三处排除（其返回永非 空类型/结果·零漏判）。
- 021 003「已落地 4」实为宿主单侧通道绿（v2 侧未固化自动化断言）——14 例残留单侧通道清单入 025（归 002 C24 收口面）。
- ast_decl.hpp 共享头变更后 ninja 增量曾现布局不一致段错误——共享头变更后须冷建验证。

### 三、卡点

- 无。

### 四、下一步

- 003 已销项（随 609-a 集成）；002 C24 终态收口清残留单侧通道 10 例；001-001 续=614-a 在飞（N2 asm 三件套→机制级修复）。

### 五、踩过的坑

- v2p 缓存指纹只认源码内容——改语义 .cn 后跑 dual 须先失效缓存（删 v2p.exe+build_key）否则旧 v2p 假结论；
- re.sub 替换串 
 写入真换行→C2001（插桩串一律 ASCII）；
- stash 恢复的是 stash 时点状态——消融实验后 stash 会把消融版当原版存取（对照实验先 commit 锚点）。


## 深度机 linux-x86_64 节

**交接时间**: 2026-09-22 中午 🧪🛠 **服务轮+603 机制级修复轮（拉取+协议 v3 补验收+strict 双红当场修+采样补轮·免号+603）**。

### 一、本轮做了什么

1. **拉取+补验收（协议 v3·独立树 wtv598 全新构建）**：基线 d3737cdf→e1935ae4（期间 591-a/592-a/597/601 四链代码集成均无 linux 验证）——**验收全绿**=零警告构建＋单测 1383/1383（601 删 11 个 v1 组件单测）＋E2E **526=524/0/2 零失败**（2 跳=62/69）＋matrix 83/83；win 侧 050/524 两备案红在 linux 均绿=**win 单侧现象**（归属不变·见接续点）。期间 develop 高频前进（c35b527f→e1935ae4），验收 E2E 换基重跑一次。
2. **603 机制级修复轮（strict 双红·已集成 c5bf935d·双远程已推）**：验收前置 strict 抓出 develop 双红——①050 登记行（ec4c2064 补）被 591-a「净差异版」集成再丢失→复登记（**登记丢失族第二例·随证据备案**）；②601 裁决甲 9 例 v1 化石下线连带簿记=[2.1]/[附录A] 两正例引用+70~76 七豁免行清理+[2.1] 正例格入 EXCLUDED_CELLS（随裁决甲·v2 等价补位后应移除）——strict rc 1→0（178/178 指针有效+零孤儿）；integrate 合并结果全量门禁绿（E2E 526=524/0/2+单测 1383）。六要素=plans/025 §2.130。
3. **附带清偿**：看板 549/551 过时挂等行删除（`merge-base --is-ancestor` 核实均已是 develop 祖先）；M3 采样补轮（当日 06:01 后守护未跑·主树重建 target/cn+清 v2p 缓存后 `cnsmith_daemon.py --rounds 1`）=322 样本分歧 0 零新命中·指针新鲜度已核（stat target/cn 12:20 > HEAD 12:05）。
4. 本机 🧵 验收基线行→c5bf935d。

### 二、接续点

1. **524 win 单侧红**（551/D12 域·条件编译 win 面——linux 绿）待我机侦查修复轮；2. **050 win 单侧段错误 ICE**（P1·B-T81 域·家机立案——linux/arm64 绿）；3. **550 阶段二呈报**仍挂用户裁决；4. **001-001 架构轮**（撤守卫容器双模收敛·P0·可认领）；5. **integrate 写集分类缺陷**（@581 备案·可认领）。

### 三、坑与边界

1. **gate_lock 陈锁新形态**：TaskStop 杀 gate_lock 包装会留「死持锁者」锁目录（等待者按 4h 心跳接管太慢）——正确姿势=核实 info.json 持锁 PID 已死+零构建活动后 rm 锁目录（527-a 先例第二次实证）。
2. **登记丢失族第二例**：净差异版/squash 集成对 coverage_map 登记区行有系统性丢失风险（591-a 再丢 050）——各机集成后 strict 自检必须真跑。
3. 沿用：命令链 rc 硬拦截/回签前树真含写集核验/单跑定性假红/分支收工 check_handoff 必跑。

## 单位机 ARM64 节

**交接时间**：2026-09-22 596+604 双轮（v3 补验收服务轮转红灯根治→用户令响应轮）·arm64 全量首次零失败。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **596 红灯根治**（arm64 补验收实锤 develop 红灯）：v2p 自举编译器全输入崩（空指针/段错误两形态）——bisect+组合树对照双实锤引入=**525-a**（随 572 收拢越挂等入干·arm64 回签从未完成）；两处半截改动当场根治：①emitFieldAddr 零偏移分支 res 未写入（storeVirtualResult 直写 base）②emitParamSetup 删 mov 中转未改循环体 [x10] 硬编码（改用 srcReg）。**456 随修转绿=fdef8ae9 P1 同根闭环**（批间分叉=未初始化寄存器垃圾二级表象·定性勘误）。B9=operandSourceReg 22 消费点穷尽审（条件绕过型仅此两处）。
2. **604 runner 根治**（用户令质询响应）：568-a「并行恒定」从未入 develop（586-a 摘取集成只带 tests 转批·runner 三增强遗漏在 568 分支）——本轮①--jobs 默认 CPU 核数+删串行路径②线程池→**进程池**（POSIX 多线程 fork 死锁实锤：worker exec 前 futex 挂死 36 分钟）③worker 外层保险 1800s。
3. **终验**：develop tip 全量 **535=533/0/2 首次零失败**（并行进程池 8 worker·约 25 分钟）+单测 1394+零警告；78/79、120_v2、050/532/502/503/346/504/505 契约抽验全绿。

### 二、接续点

1. 604 集成（runner 契约变更广播随看板通告段——他机 pull 后首跑自然验证+重点复跑 -j 默认态）；
2. **050**（已立账）=568-a 遗欠恢复（超时防线 26 处+dual rc·从 a0ba39ab 摘取对齐 604 进程池版）；
3. 598-a 全量口径差异核对：win 533/2（红 050/524）vs arm64 533/0——050/524 两红 arm64 侧绿·win 侧归属复核归家机；
4. 常态：M3 采样/B8 巡视（本轮红灯处置占满未跑 cnsmith 差分）。

### 三、踩坑新增

1. **gate_lock 陈锁**：TaskStop 杀门禁任务→finally 未及执行→锁残留（4h 心跳太慢）——rm target/gate.lock 接续（先确认无构建活动；表象=「等待中 NNNs」刷屏而产物零更新）。
2. **看板窄通道直推携带**（596 已披露违规）：直推 HEAD:develop 前必须核 HEAD 链只含窄通道白名单文件——分支上有代码提交时用 cherry-pick 法（detach develop→cherry-pick 看板提交→push）。
3. **bisect 对树同源提交无区分力**：纯文档提交与代码提交同树→good/bad 判定外溢——good 基准必须实测（本轮 bedc8534 假 good 教训）。
4. runner 产物继承：run_e2e 主进程退出后 worker 泄漏可致 `| tail` 管道 EOF 不至——排查先 ps 池进程。

