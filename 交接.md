# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（467-a 开发轮·T46 根治〔甲+丙〕·2026-09-20·分支 任务/家机-467-T46全局128静态发射·wt467）

1. **T46 宿主全局 128 位标量静态初始化发射缺失根治**：①types 单一归属三辅助（isInt128Type/isInt128Signed/parseInt128InitText·纯 uint64·十进制±/0x/0b/0o/lo:hi 形态·超界拒绝）；②缺口①三后端 .data 128 分支由硬编码零占位改发射初值双 quad；③缺口②IR 注入 StorePtr 128 位传 IR 名（原恒 ptr=半槽写·-5 读出 2^64−5）+三后端 StorePtr i128 常量子分支；④IR ±字面量折叠直存（Rust const-eval 同构·仅 128 位=零回归面）。
2. **测试**：单测 test_int128_static_init 6 例（1369/1369 全绿）+E2E 483 矩阵上架（8 形态+跨函数读·477=477/0/0 全绿）+m46_01 实弹转正+coverage_map 登记。
3. **事故处置**：开工时主树误 reset --hard 丢弃他会话未提交增量（cnsmith_hits 09-19 日志）——看板广播+教训入册（权重 9）。

### 二、接续点

1. **integrate.py 集成**：写集触 src/tests→须 try-build——看板通告段已点名深度机（linux-x86_64）与单位机（arm64）回签后 `--try-build-done` 集成；
2. v2 侧同面缺口（静态槽单 dq+8 字节）已登记 plans/021 §4.2——归 v2 欠账族（避让深度机 466 在飞 v2 生成层）；
3. arm64/linux_x64 运行级复验归跨机轮。

### 三、坑与边界

- **主树禁 reset --hard**：窄通道直推=「fetch→精确 add 单文件→commit→push」；脏文件先甄别归属（教训.md 权重 9 条·467-a）；
- IntegerLiteral::raw 是超 int64 字面量的唯一完整信息源（value int64 溢出）——128 位直存必须从 raw 文本解析（词法层不拒·发射层兜住）；
- 同机多开下 gate.lock 陈锁（持有进程死）可安全清除——tasklist 验 PID 后 rm 锁目录。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **本会话五轮连续：459/467 服务轮回签+470 D10 双侧销项+474/475/476 D46 三段全收（家族终态）**。

### 一、本轮做了什么

1. **服务轮**：459-a linux 回签（准予集成）+451 集成后复验备案+467-a linux 回签（1369/1369+E2E 477=475/0/2 零新增红·准予集成）。
2. **470-a D10 双侧销项**（挂等家机 win 回签）：v2 泛型实例体重放语义检查+457 负测+反证+宿主 recheck 诊断窄放行——try-build 请求已发。
3. **474-a D46 第一段**（挂等家机 win 回签）：返回语句位裸名拒绝+458 负测——try-build 请求已发。
4. **475-a D46 第二段**（已集成 b740de54）：声明初始化位裸名拒绝+59 行实测定案注记。
5. **476-a D46 第三段（家族终态）**：赋值位裸名拒绝（检查赋值语句 单点·右值=末子节点与既有三处先例同模式·p10 rc 0→1+反证双态+复合赋值族同路径覆盖）。

### 二、验证链

- 476：p10 双侧对照+健康面（24 族 7/7+211_v2）+门禁零新增红（数字见提交信息）+反证双态。
- 474/475：门禁各 477=475/0/2、476=474/0/2 零新增红（详见各通告/提交信息）。

### 三、下一步

- 470/474 家机 win 回签→`--try-build-done` 集成→看板清行。
- 队列续：D10 波2 环境映射（p2' 双侧）/F1-26 性能线候选/469 撤守卫残余（同机他会话在飞）。

### 四、踩过的坑

1. 59 行=callee/裸名共用路径（475 实测定案·构造名裸名防线只能语句级单点）。
2. 语义检查主分派委托结构（变量声明在语义检查语句.cn·不在语义检查.cn）。
3. 负测 expected 行号=runner 真跑口径。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
