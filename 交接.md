# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（732-a·映射未构造防御·stdlib/映射集合.cn·分支 `任务/家机-732-结构体ID守卫修复`·wt698 树·接 731）

①**机理闭合（fault 反推金矿再显）**：fault=0x1FD0=1018×8=散列值 h=1018——**静态映射「结构体大小表」未构造（零初始化态：桶数量=0·桶数组=无）→ 桶索引掩码 (0-1)=全1 → h=散列 → 桶数组[h] 读 [NULL+h×8]→崩**。②**修复**（映射集合.cn 链查找）：**未构造/空桶防御**（桶数量==0 或 桶数组==无 → 返回 -1·视为空映射——Rust 风格未初始化容器安全失败）。③**验收**：守卫态全量 **528=524/4 零回归** ✓；「写回触发+守卫」组合实验=**崩点前移**（fault 0x1FD0→**0x8**·RVA=0x210d01=新 NULL+8 读）——第一崩点被挡住、露出缺陷链下一环。

### 二、下一棒

**733=0x210d01 对位+下一守卫**（wt698 树续）：新 map 重链→对位 0x210d01 的 NULL+8 读函数→下一守卫/修复→**迭代至 v2p t4 零错**→全量 524/4→写回+元素引用双件回归→t4→0→fix_s 67157→0→-7 固定点→056 传址化→709=055+113→零红→终章→001 销项。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①**fault 值=散列值×8** 的反推=未构造映射的铁证（h=散列 因掩码全1）；②**守卫迭代法**：每加一个未构造/空指针防御→崩点前移一环（0x1FD0→0x8）——**缺陷链逐环剥开**的标准化流程；③守卫=独立正确的防御（零回归）可即刻集成·实验件（写回）暂撤。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-25 服务轮（728 收账后·B8 巡视＋静态门禁复核＋棘轮滚转·窄通道②·零分支零轮次号·基线 develop **0d51fbd0**）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **选题依据**：距上轮服务轮收尾（05:27）34 分钟，家机 727/728 链四提交入库（ed7f554c→0d51fbd0）；fetch 后看板/远端分支核对=家机 wt698 在飞（下一棒 729 拷贝构造克隆体对拍）·无深度机待应答项；上轮 724-a 复验已回签闭环——按服务轮默认动作（B8 巡视＋静态门禁）做一轮轻量巡视落账。
2. **增量写集树级核验（先核后跑）**：ed7f554c→0d51fbd0 四提交 diff 树级核对**全=窄通道纯文档**（plans/021/025＋看板＋交接＋更新日志）；`-- src/ tests/ CMakeLists.txt` 面唯一变更=tests/cnsmith_hits/日志/2026-09-25.md（采样命中日志·服务轮写面本身非用例）——**行为面零增量**，上轮 724-a 全量复验（906e3647 系：单测 1405＋E2E 528=523/3/2 红名单同构）**结论传递有效**，无需重复全量。
3. **主树快进＋快速门禁**：`git merge --ff-only` 至 0d51fbd0；check_handoff rc=0＋check_progress_sync rc=0（总表戳与 git 最新轮次同步·家机 728-a 戳保留未动）。
4. **编译器新鲜性核验（滞后基线必重建纪律）**：`target/cn`（9-24 11:38）本体输入零变化（src 唯一近期变更=runtime 两文件 9-25 04:30·724-a 面·编译器本体之外·上轮已重编 cn_runtime）＝产物对本树有效。
5. **静态门禁三件（他机盲区复核）**：strict 181/181=100%（B3 口径）＋matrix 83/83（未测 0·已探针 43·已转正 40）＋philosophy PASS——全绿。
6. **M3 采样不重跑**：距上轮（05:27·322=319 一致＋0 分歧＋3 存量命中 m38/m42/t71）仅 34 分钟且行为面零增量——结论传递有效（诚实留痕·非漏跑）。
7. **棘轮滚转（先滚转再追加）**：plans/021 §五 历史表段前部 13 行（84-a~g~324-a/b·均 413-a 前轮次·上次滚转残留）滚入**新切片** `项目记忆/归档/plans021-轮次索引-历史段二-2026-09.md`（旧切片已封存不改动）；§五 追加本轮 🧪 索引行；主表 450（贴线）→437/450。
8. **看板深度机通告段换新**：上轮 B8 巡视条目影响面已毕删除，换入本轮条目。

### 二、接续点

1. **深度机侧零待办**；家机 wt698 链在飞（729=拷贝构造克隆体语句级对拍→定点修复→t4→0→fix_s 判据）——若发验收请求按服务轮职责应答（重点面=v2p 重建+全量红名单同构）；
2. 550 阶段二呈报仍挂用户裁决（&容器变量 语义澄清·材料 025 §2.124）；M5/M6 补测挂家机 708-e 链集成后重跑 bench；
3. 上轮挂等核查轮接续点维持：@家机 561/499 分支保全处置；040/045/033 摘取 rebase 集成（480-a 已保全可 fetch·属开发轮性质）。

### 三、坑与边界

1. **服务轮不刷 plans/021 文首戳**：文首戳承载家机轮次号（check_progress_sync 按其比对 git 最新轮次）——服务轮无轮次号，刷戳=「丢家机新戳」事故形态（上轮 44137ca4 教训），本轮只追加 §五 索引行；
2. **棘轮贴线必先滚转**：主表 450=基线 450 贴线，追加任何行前先滚转历史区（§6.7「先滚转再追加」）——滚转对象选历史表段最老行，新开切片不动已封存旧切片；
3. （历轮在册仍有效：服务轮「先核后跑」写集分类前置／增量构建 up-to-date≠产物新鲜·必须核验输入／挂账分支核查 ls-remote+树级特征码双道／他机资产分支引用不可代动／rebase 丢纯文档提交看板断链／v2p 产物相对 cwd／gate_lock 陈锁 rm 接续／直推前树 diff 核验／门禁命令裸跑取 $?／integrate.py 写集分类对 CN语言编译器v2/ 失效须自行补跑全量。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


