# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节


### 一、本轮做了什么（655-a 开发轮·asm 分析准备·分支 `任务/家机-655-asm分析`·wt628 树复用·基线 develop 9aa12bb5）

1. **985 探针重建验证**（类字段容器+stderr 步号）：宿主 rc=0 全对/v2 fast-fail@元素(0) 取回——崩点稳定复现。
2. **asm 分析准备**：v2p 产物「asm 写文件失败」（中文入口名/路径问题待解）——下轮解决后产 v2asm.asm 解读。
3. **051 主线**：甲方向维持〔基准=019〕·645dump 已撤。

### 二、发现的问题

- 撤守卫态 3 红维持（113/79_v2=asm 级缺陷·524=条件编译后解析面）——诚实降级。

### 三、下一棒

- **asm 产物路径修正+解读**（fresh context 专项）→修复→113/79_v2/524 清偿→零红→借链终章→销项。

### 四、踩过的坑

- Write 工具的 `\n` 在 JSON 层被解析为真换行——CN 源码的 `\n` 转义须双反斜杠或字节级写入。
- v2p 产物「asm 写文件失败」=中文入口名（主.cn→主.asm）——ASCII 拷贝法可解。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-22 中午 🧪🛠 **服务轮+603 机制级修复轮（拉取+协议 v3 补验收+strict 双红当场修+采样补轮·免号+603）**。

### 一、本轮做了什么

1. **拉取+补验收（协议 v3·独立树 wtv598 全新构建）**：基线 d3737cdf→e1935ae4（期间 591-a/592-a/597/601 四链代码集成均无 linux 验证）——**验收全绿**=零警告构建＋单测 1383/1383（601 删 11 个 v1 组件单测）＋E2E **526=524/0/2 零失败**（2 跳=62/69）＋matrix 83/83；win 侧 050/524 两备案红在 linux 均绿=**win 单侧现象**（归属不变·见接续点）。期间 develop 高频前进（c35b527f→e1935ae4），验收 E2E 换基重跑一次。
2. **603 机制级修复轮（strict 双红·已集成 c5bf935d·双远程已推）**：验收前置 strict 抓出 develop 双红——①050 登记行（ec4c2064 补）被 591-a「净差异版」集成再丢失→复登记（**登记丢失族第二例·随证据备案**）；②601 裁决甲 9 例 v1 化石下线连带簿记=[2.1]/[附录A] 两正例引用+70~76 七豁免行清理+[2.1] 正例格入 EXCLUDED_CELLS（随裁决甲·v2 等价补位后应移除）——strict rc 1→0（178/178 指针有效+零孤儿）；integrate 合并结果全量门禁绿（E2E 526=524/0/2+单测 1383）。六要素=plans/025 §2.130。
3. **附带清偿**：看板 549/551 过时挂等行删除（`merge-base --is-ancestor` 核实均已是 develop 祖先）；M3 采样补轮（当日 06:01 后守护未跑·主树重建 target/cn+清 v2p 缓存后 `cnsmith_daemon.py --rounds 1`）=322 样本分歧 0 零新命中·指针新鲜度已核（stat target/cn 12:20 > HEAD 12:05）。
4. 本机 🧵 验收基线行→c5bf935d。

### 二、接续点

1. **524 win 单侧红**（551/D12 域·条件编译 win 面——linux 绿）待我机侦查修复轮；2. **050 win 单侧段错误 ICE**（P1·B-T81 域·家机立案——linux/arm64 绿）；3. **550 阶段二呈报**仍挂用户裁决；4. **001-001 架构轮**（撤守卫容器双模收敛·P0·可认领）；5. **integrate 写集分类缺陷**（@581 备案·可认领）。

### 三、坑与边界

1. **gate_lock 陈锁新形态**：TaskStop 杀 gate_lock 包装会留「死持锁者」锁目录（等待者按 4h 心跳接管太慢）——正确姿势=核实 info.json 持锁 PID 已死+零构建活动后 rm 锁目录（527-a 先例第二次实证）。
2. **登记丢失族第二例**：净差异版/squash 集成对 coverage_map 登记区行有系统性丢失风险（591-a 再丢 050）——各机集成后 strict 自检必须真跑。
3. 沿用：命令链 rc 硬拦截/回签前树真含写集核验/单跑定性假红/分支收工 check_handoff 必跑。

## 单位机 ARM64 节

**交接时间**：2026-09-23 服务轮（协议 v3 集成后异步验收·45c4e8a5→5ed202f7 补验 159 提交）·arm64 与 develop 撤守卫正式化态对齐、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **补验收（服务轮主任务）**：本机基线停在 45c4e8a5（09-22）落后 develop 159 提交——快进至 5ed202f7 后**清场重建**（rm target/build→cmake→8 核并行构建零错误）＋单测 **1397/1397**＋E2E 全量 **528=522/4/2**（总数 +2=611 def-init 新例 533/534 入全量；2 跳=62/69 平台固有）。
2. **4 红定性（零特有红）**：113/127_v2/497/79_v2 全 v2 产物段错误（-11）——与家机 624 广播 win 侧 528=523/5 的同 4 例段错误完全同族＝撤守卫正式化态环境敏感 UB（618-a 定性·001-001 主攻面·家机 9 在飞轮正攻中），非 arm64 新引入；本机**不另开修复分支**（避撞在飞写集）。
3. **611 影响面抽验全绿**：533/534（def-init 放行/拒绝矩阵）双 PASS＋78_v2 自举 PASS＋050/524 两 win 备案红 arm64 侧绿（win 单侧现象再证）＋596 修复面（v2p 正常产出）实证。
4. **看板**：基线行 45c4e8a5→**5ed202f7** 登记；单位机通告段写验收广播并删除 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 001-001 修复轮（家机 626~635 族）集成后：本机对**新基线复验 113/127_v2/497/79_v2 四例**（应随 UB 根治转绿）；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮被全量门禁占满未跑 cnsmith 差分）。

### 三、踩坑新增

1.（无新增·历轮在册三条仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


