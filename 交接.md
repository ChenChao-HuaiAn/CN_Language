# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（586-a 清偿轮·021 🔄 挂等回签清偿+quotepath 缺陷根治·2026-09-21·分支 任务/家机-586-挂等链清偿 系·wt447/wt494/wt496/wt504 多树）

1. **579 红灯恢复核验 ✅**：574 src 七文件被 517 merge 冲掉（develop 回归）——核验时发现 579 会话已自行入 develop（a3135542 系三提交·树级 diff=零差异），红灯解除；579 门禁结论（全量 528=528/0/0）对现态 src/tests 有效（后续仅纯文档增量）。
2. **568 转批面摘取集成 ✅（83de088a）**：三分判定证实 568 分支唯一未入 develop 面=51 例正测转批（42 v2loop→双编译对照+9 仅宿主）；runner 三增强 develop 已含（569 直入系）不取。摘取 93 文件零冲突（develop 自 568 基线未动过这些路径）。**补验=对 develop 现态全量门禁 4/4 全绿（E2E 528=528/0/0+asm 347×3 零+自举链双 PASS）**。
3. **integrate.py quotepath 缺陷根治 ✅（63611b1c·机制级 §8.4）**：写集分类对中文路径失效（startswith 失配→93 个 tests 文件被判纯文档**全量门禁静默跳过**直推 develop）；git -c core.quotepath=false+strip 防御修复；反证=修复后同写集分类=须全量 True；修复后 504 集成正确触发全量门禁（实测）。全文教训已入册（权重 9）。
4. **041〔原T70〕复验销项 ✅**：517 链已集成（7e0ff3c2 深度机）；586-a 补验门禁 E2E 528=528/0/0（含 127/496/497 T70 系）=复验证据；021 行迁 §4.2。
5. **012〔原T48〕504 链集成 🏃（收尾随 586）**：504 分支（behind 467）rebase 最新 develop 两轮解冲突（coverage_map 双侧并编·弃过时 502/503 行/跳过 develop 已有的搭车提交）→单提交 f371795a→integrate 全量门禁跑中（修复后的分类正确触发）。集成绿后 012 销项。
6. **批量核对（修正版三分判定）**：25 条挂等链逐一判定——零独有可销账=555/539/581/502（后续拆分覆盖）/529/554/564/526/571/567；真有未吸收内容=497/499（stdlib 迭代器本体未入！）/500/505/527/534（⊂561 取 561）/538/542/544/556/561/563/522/551（与 504 同文件）；020=583 会话集成 ✅（23d9fd2b）。

### 二、发现的问题

1. **integrate.py quotepath 缺陷是系统性漏洞**：自设施启用以来所有中文路径写集集成都被降级快速门禁——他机需回溯自查（看板通告段已广播）。
2. **582 会话集成 565 时 502/503 用例目录未入 develop**（登记行也被 da8e931f 拆除·现态自洽无孤儿）——565 的 T33/T36 验收面缺 502/503 实弹，归 582/583 链协调。
3. **check_handoff 管道洗退出码三犯**（565-a 口诀「>log;RC=$? 三段式」未执行）——教训已入册升级。

### 三、卡点

无（504 全量门禁在跑·结果随看板通告）。

### 四、下一步

1. 504 集成绿→012 销项+看板清行（本会话或下一棒）。
2. 挂等链剩余批次（待认领·按写集交叠分批）：批A=499 迭代器（stdlib 三件+501 用例）+522（497 转正用例）零交叠可合批；批B=497/500/505/527（各自单文件 src 写集）；批C=538/542/544/556/561/563。每批一次全量门禁。
3. 他机 quotepath 回溯自查（通告段请求）。

### 五、踩过的坑

1. check_handoff 报错被 `| tail` 洗退出码→带病提交（fix-forward 补救）——门禁命令禁入管道链。
2. git 中文路径 quotepath 转义→pathspec/integrate 分类双坑（-c core.quotepath=false 通解）。
3. wt 树 checkout 分支被其他树占用（git 不允许双树同分支）——先查 wt.py list+branch 占用。
4. integrate.py 在错误分支上误跑 rebase（wt496 checkout 失败后 && 链继续）——操作前确认当前分支。
5. python -c 内嵌含反引号代码被 bash 命令替换——含转义/反引号一律 Write 直写 .py（教训再犯）。


## 深度机 linux-x86_64 节

**交接时间**: 2026-09-22 凌晨 🧪→🏃 **585-a 开发轮（D42甲 波3 终章接力=001·用户令「立即全量完成」）·收工挂等集成**。

### 一、近期做了什么

1. **585-a 撤守卫收口接力（分支 `任务/深度机-585-撤守卫收口`·基点 develop 7ad6343a·wt585）**：接手家机 578 同计划（其组合树经树级 diff 核验零独有实质内容仅接缝空行）。TEMP 撤守卫（主.cn:64 恒假）→撤守卫态全量首跑 **528=505/21/2**（守卫态基线 526/0/2）→暴露面 21 红逐根。
2. **四根因二留二回**：①（留）`签名返回字节数` 结果空 12→16（268-a 口径漏同步·retbuf 帧预留差 8B 被调 sret 写踩槽·asm 铁证）；②（留）`生成类对象局部` 补 IR_分配（帧预留缺失→句柄被 retbuf 覆盖归零·gdb [rsi]=0 铁证·同族全树扫描唯一漏点）；③字段载荷容器排除/④容器参数句柄语义——分层实验两难（③修 441 破 228·④修 113 破 127/497）**回滚**。
3. **负测锚七例适配（留）**：197/223/224/227/229/464/283 主.expected「已解析…主.cn 节点=N」去节点数尾巴（撤守卫态=全树合并节点数含 stdlib·锚死单模块数必 miss；v2 侧实际已正确拒绝=纯锚行漂移·6 例转绿）。
4. **本质定性（立案 047）**：撤守卫态容器实例**内联/句柄双模并存**（元素/字段=内联 517-a 宿主对齐 vs 局部/参数=句柄 生产态模型）——T70+585 两轮实证打地鼠不可持续，收敛=架构级大轮（P0·001 波3 剩余）。
5. **生产态零回归（本轮写集验证）**：守卫恢复态全量门禁全绿=零警告+单测 1394/1394+E2E **528=526/0/2 零失败**+strict 179/179（100%）+matrix 83/83（gate_lock 串行·数字与 develop 基线逐项一致）。

### 二、接续点

1. **585 集成**（写集=v2 树 3 文件修复①②+tests 七例 expected+六件套·生产态零回归已证·integrate 走协议 v3：win/arm64 集成后异步验收）；2. **047 架构轮**（撤守卫容器双模收敛·P0·001 波3 剩余·清偿后撤守卫落地+借链 51 例转自洽解锁）；3. 撤守卫态终测精确红名单（025 §2.129 回填尾注·本会话终测后台跑中）；4. 存量挂等链不变（546/547/549/550/551/565 各行自理）。

### 三、坑与边界

1. **撤守卫态负测「已解析…节点=N」锚**：全树合并节点数≠单模块节点数——expected 锚节点数必 miss（本轮七例适配先例·后续撤守卫用例同款处理）。
2. **「产物通配符复跑」假象**：`v2out_NN_*` 通配可能匹配旧 hash 产物——行为判定必须先核产物 mtime 与 v2p 构建序（本轮 113/228 两次误读实证）。
3. **修复分层实验纪律**：两难修复（修 A 破 B）必须分层回滚归因+多例复跑（单次单例不可信·构建间布局漂移会伪装因果）。

## 单位机 ARM64 节

**交接时间**：2026-09-21 560（F2-36b 首步=bench v2 口径+arm64 基线入库）·558/557/559 三链收工挂等三平台回签·M3 采样+T96 立案。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **329-D12 让位处置**：D12 主面让位 546；增量面拆 557；T50 已由 331-a 兑现。
2. **558 紧急修复**（develop arm64 红根治）：457-a 两处发射缺陷（emitPtrLoadStore 7 处丢 `[`+emitLoadStore 浮点 w9 截断）——522=515/5/2+78/79 自举链 PASS+反证双态。假绿教训入册（权重 9）。
3. **557 增量面**：T55 runner 隔离键+T54 宿主货舱 [特性]+用例 495/434/435——组合态 524=521/1/2·**隔离键自愈实证**（4 互踩假红消）。
4. **559 T96 三层根治**：语义类型写回 AST 通道+三元槽按语义类型+arm64 emitCopy 双半（三后端对齐）——组合态 523=516/5/2·500 号 O0/O3 双 PASS。
5. **560 F2-36b 首步**（bench v2 口径三平台改造）：范围修正（原测 v1 组件链已推倒=对象过时→v2 全树 fix_p≡fix_s 固定点口径）+--platform 三平台工具链分派（win=ml64/link 保留·linux=as/g++）。**arm64 基线入库**：M1=22295.7ms（C++ 宿主 Debug）/M2=42608.4ms（CN 版·比值 1.91x vs C++ Debug）/**M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建快 24%**/M6 fix_p≡fix_s 逐字节+绑定自检 ✓。报告=target/bench/性能基线报告.md（产物不入库·数值誊 021）。
6. **M3 采样**：622 样本（558 修复态）+T96 立案。**呈报待批**：推断面完整化（i1/浮点形态·184 expected 契约·320-a T47 同模式）。

### 二、卡点与下一步

1. **三链挂等三平台 try-build 回签**（558→557→559 集成序·组合态门禁已预跑）；他机回签后依次 integrate。
2. **560 集成**：平台无关 python 写集=快速门禁·免 try-build（§8.3）——可在三链集成后直接走 integrate。
3. **下一轮候选**：T33 v2 窄宽读侧（施工图=025 §3.23）/D42甲波3（stdlib 源码加载）/F2-36b 续（win 复验归家机·x64l 基线归深度机）。

### 三、踩坑新增

1. **cn build linux 产物链**：--output X 产 X.s+X.o+ELF 三件——X.o=锚定链借链的「容器符号提供者」（fix_p 缺跨模块符号定义·grep 实锤）；bench M4 借链错用 fix_p 自汇编=undefined 符号。
2. **ls | head 截断**导致误判「文件消失」——长目录列表用 grep 定位。
3. **语义注记文本=源码类型名**（「整128」）非 IR 码（i128）——跨层判定必须 mapType 归一后比对。
