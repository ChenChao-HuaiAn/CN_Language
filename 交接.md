# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（456-a 开发轮·D1 拆分滚动·2026-09-20·分支 任务/家机-456-D1拆分·worktree=wt456）

**背景**：D1 首列 handleClassCallExpr（ir_oop_call.cpp 421 行·58 超百行之首）拆分。纯机械重构零行为变更——验收=锚定链产物逐字节等价。同机多会话：451（arm64mov）/452（本会话前轮 C20批5·已收工 try-build 挂起）/453（437 拆分集成）。

1. **拆分结构**：主分派器 22 行+9 族函数——tryInterfaceMethodCall（接口·P3-19 去虚拟化+CFI）/emitConstructorCall（构造族：resolveGenericCtorInstanceName 泛型实例名映射+findCtorMember 构造查找+emitCtorInvoke 发射）/emitMemberMethodCall+emitStaticMethodCall（静态·**三态返回 1/0/-1**）/emitInstanceMethodCall（实例·emitVirtualMethodCall 虚调用+emitDirectMethodCall 非虚含 retbuf）。
2. **控制流语义逐点保持**（拆分最大风险面）：接口段未命中=fall-through 构造/成员路径；**静态段原 `return false`=交回原路径（非 fall-through）**——三态 int 表达；构造 ci==nullptr/抽象类 false=交回原路径。所有迁移段注释逐字保留。
3. **等价性铁证**：v2p.asm 拆分前后 **md5 逐字节一致**（0683ee1e）——宿主 IR 生成零变更最强证据（obj/exe md5 差异=内嵌产物路径/时间戳，不作判据）；Ninja /W4 /WX 零警告；超百行 58→57·ir_oop_call 出清（新首列=v2 树 编码算术Win 206·深度机面）。
4. **手术三失误当场修**（python 行号切片法）：①SEG_A 漏切 L373 if 壳关闭→函数嵌套（C2601 连锁=嵌套定义信号——**看首错辨结构**）；②C4/C5 段 void 化残留 return true（C2562）；③resolve 的 const& 参数被原文赋值→改值传参。

### 二、接续点

1. **全量门禁**：gate_lock 排队跑——终态随 try-build 通告回填（预期 E2E 464=462/2/0 基线红 406/431 同签名零新增+单测 1363）。
2. **try-build 请求**：src 面=三平台共享层（ir_oop_call.cpp+ir.hpp）→点名深度机 linux 回签；**集成序=448→447→450→452→456**（ir.hpp 共享头串行集成）。
3. 下一轮候选：①v2 树 编码算术Win 206/编码算术X64L 201（深度机面）；②checkSingleMethodBody 190（semantic 面）；③T63 诊断对齐（454 预登记·避让）；④F2-33 迭代器（stdlib——注意与 D42 stdlib 源码加载攻坚语义耦合·建议 448 收口后再排）。

### 三、坑与边界

- **C2601「局部函数定义非法」=大括号失衡的信号**（不是声明顺序问题）——行号切片手术必须每段边界断言+壳关闭行精确核验（漏 1 个 `    }` 全文件连锁）。
- **obj/exe md5 不作重构等价判据**（内嵌路径/时间戳天然不同）——asm 逐字节才是 IR 层等价铁证。
- ir.hpp 加成员函数声明需 ClassInfo 前向声明（ir.hpp 只有 ClassMemberInfo 前向声明·semantic.hpp 定义）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **本会话七轮：服务轮三签+470/474 挂等回签+475/476 D46 已集成（家族终态）+478 F1-26 设计定稿（已集成）+479 F1-26 波1 Mem2RegPass 落地（挂等 try-build）+477 挂等 470**。

### 一、本轮做了什么

1. **服务轮**：459-a linux 回签+451 集成后复验备案+467-a linux 回签（准予集成）。
2. **470/474**（挂等家机 win 回签·请求 8415a43f/94596572）：D10 双侧销项+D46 第一段。
3. **475/476（已集成）**：D46 二三段家族终态（b740de54/19ec879b）。
4. **478（已集成 758286be）**：F1-26 侦察设计定稿（量化 27%+三遍算法+三波拆波）。
5. **477（⏸ 挂等 470）**：D10 波2 环境映射六要素已落盘（强依赖 470 重放块）。
6. **479-a F1-26 波1**（挂等 try-build·头待推）：**Mem2RegPass 落地**（mem2reg.hpp/cpp ~230 行·逃逸判据〔AddrOf/Ptr 降级→Call 摸不到可提升槽=跨块直继正确性前提〕+环境不动点交集+读侧直递 Load→result 替换·Store 保留）——-O3 注册 SSAPass 后；正确性=全量门禁 477=475/0/2 零新增红+自举链双 PASS；量化如实=q1 环主导样本 22%→22%（波1 交集保守边界·收益主力=波2）。

### 二、验证链

- 479：构建零警告+单测+全量 E2E 477=475/0/2+strict/matrix+自举链双 PASS+dbg 首验直递命中记录（i$2 单点）。
- 全会话：四组反证双态（457/458/p9/p10）+三轮集成（475/476/478）+两签发。

### 三、下一步

- **470/474 家机 win 回签→集成→清行**（接手第一动作 fetch 看板通告段）。
- 477 解挂（470 集成后）→实施环境映射。
- 479 波2（Phi 全量化+Store 消亡·收益主力）待认领；479 集成需家机 win 回签（src 改动）+arm64 周一。

### 四、踩过的坑

1. cn build --output=ELF 非 asm（量化=objdump）。
2. pass_manager fixpoint：changed=true→全管线重跑（Pass 幂等性必须）。
3. 负测 expected 行号=runner 真跑口径；59 行=callee/裸名共用路径。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
