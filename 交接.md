# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（541-a 开发轮·D1 滚动专用设计轮=解析选择语句拆分·2026-09-21·分支 任务/家机-541-d1switch·wt541 隔离·基线 a536460b）

1. **设计落地**：`解析选择语句` 136 行→主骨架+两分支子函数（解析选择默认分支/解析选择情况分支）
   ——**v2 `整64*` 指针参数传 情况计数**（`情况计数[0]` 读写·寄存器计数[0] 同款惯例）解除
   530-a 定性的「双返回约束」——链式/多返回解析函数拆分的设计模式由此沉淀（021 §五 541-a）。
2. **等价铁证**：同宿主双 v2p 重建——六产 asm 探针 md5 逐字节一致（t_call=4e259ec6/t_copy=
   05a55749/t_lex=932e40ca/**t_switch 选择语句实弹=b21d3b63**（拆分直接消费面）/t_cpy=2dd4131a/
   230 全链=b07b8f1d）。
3. **验证链**：Ninja 零警告构建+check_fn_length 出列（136 档位消）+双文档门禁；集成时全量门禁+
   develop 现态补跑兜底。
4. **同会话前半程（530-a）**：生成类对象局部 147 拆分（目标变更自解析类声明·集成 1fe04938）+
   check_win_asm.py 注册缺口机械修复（501 链已知欠账·4a076163）+develop 门禁重跑 4/4 全绿。
5. **再前半程（522/529）**：编码对象调用ARM64 拆分（eeac863c）+分词拆分（1005ac33）+三链集成
   让位协调+僵尸 gate_lock 陈锁×3 清除。

### 二、接续点

1. 541 集成排队中（integrate.py）；分支已推（拆分 a28e72a1+收尾六件套）；
2. D1 滚动候选=生成下标左值地址 170（生成表达式.cn·深度机 B1 链集成后认领）/生成函数 153
   （代码生成.cn·501 复签后）——多返回约束场景统一走 整64* 指针模式；
3. 链式结构（解析类声明 162 余量）=深度机 540 在飞·让位。

### 三、坑与边界

- 切片 off-by-one（体末/闭合行 0-based 换算）反复出现——**动态锚点+内容断言**仍为防线但需在
  写断言时统一「行 N=lines[N-1]」换算（本轮三处试错全被拦截）；
- v2 函数拆分检查单：①不安全 修饰随指针下标迁移；②主函数局部变量逐一核对传参；③内联块
  「继续」→「返回」；④链式「} 否则」中段=专用设计轮（整64* 模式）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-21 🏃 **537-a 解析类声明部分拆分（185→162·挂等呈报态·未集成）**。

### 一、本轮做了什么（537-a·分支 `任务/深度机-537-解析类声明实施`）

1. **部分拆分**（§2.115 方案边缘①预授权）：继承跳过+布局收口两边界块出列（子函数 布局收口 带是否公开/池/类键基/类体起点参数）——主函数 185→162 行**未出列**（方法成员五形态嵌套链+字段成员块=下一轮）。
2. **等价铁证**：md5 双对拍（探针 3982d9f7+全树改前文本 111825 节点 9eb05e11 双一致）。
3. **★456 runner 环境红之谜（挂等呈报·未解不进 develop）**：456-6「v2 产物运行 rc=14（期望 27）」单跑稳定复现+清 audit2 v2p 缓存重建仍红；但**同一 v2 源手动构建 v2p 直接编译运行 rc=27 正确**（v2p_base/new 双侧一致·产物 md5 82fc1dc0）——runner 产物 aaec0624 vs 手动 82fc1dc0 **仅 1 行 IR 常量差**（mov r10, 8 vs 0=结构体大小查询）——非 537 引入（基线 v2p 同法 rc=27）。嫌疑=audit2 v2p 缓存构建/v2src 拷贝/建节点参数求值顺序。

### 二、接续点

1. **456 红之谜定位优先**（嫌疑清单=audit2 v2p 缓存构建环境/v2src 拷贝残留/建节点参数求值顺序）——定位后 537 解除挂等集成；2. 522+526 挂等三平台回签；3. 513~517 挂等他家回签；4. 329-D12 双签齐待集成（集成后假红清零丁字账复验）；5. 解析类声明方法/字段块拆分余量（162→≤100）。

### 三、坑与边界

1. **门禁红分类纪律实证**：单跑稳定红≠互踩假红——456 单跑两次稳定 rc=14=真红嫌疑·但产物级对照（手动编译运行 rc=27+md5 拆分前后一致）证明非拆分引入→「runner 环境 vs 手动环境」差异=定位方向。
2. 诚实边界：537 保持挂等不集成（未解之谜不进 develop）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
