# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交接（570-a 文档治理轮·看板膨胀根治·2026-09-21·用户批准实施）

1. **做了什么**：①诊断=五份滚转型台账全在棘轮内（治理有效），真凶=看板不在棘轮清单·通告段「用完即删」无机械门禁（4 天 230→832）；②清理=逐条 merge-base 判完结性，832→153 行（活跃在飞 17+1 条全保留：567-a 红定性/537·542·544·561·563·534·527·505·504·499·502·546+547 挂等请求/489 阻塞归因/T81·T84 呈报+三机状态指针行）；③门禁=看板纳入棘轮基线 200 行+check_handoff 增每机通告段 ≤1 检查（反证双向实证）；④教训入册高权重区第 20 条；⑤补登 T81/T84 呈报入 021 §三-B（此前漏登·看板单点承载风险）。
2. **写集**：三机任务看板.md／scripts/check_handoff.py／scripts/文档棘轮基线.txt／项目记忆/教训.md／plans/021（戳+B 区两行）／plans/025 本机节／更新日志/交接 本机节——纯文档+门禁 py=快速门禁面·零 src/tests。
3. **删他机历史通告段声明（§8.5 修正通道）**：单位机四段（558 红处置/557 让位/332-a 广播/329-a）=三链已随 572 集成+广播已交付；深度机段删已集成链回签/456 侦查串（562 热修 883e4253 已终局 win/linux 面·arm64 面转单位机状态行）——全量原文 git 可考（本分支 diff 前态）。

### 二、接续点与挂起

1. **567-a ⏸ 维持挂起**（非本轮范围）：D1 拆分对拍阻塞于「存量警告计入 v2p 构建失败」定性——下一棒=警告计入 bisect（f53d7067→HEAD 宿主语义域）→对拍续验→收工。
2. **挂等中**：563-a/561-a/544-a 等 try-build 回签（详参看板家机活跃通告段）；568/569（C25 波1/波2）在飞。
3. **下一棒候选**：若深度机/单位机对活跃请求回签，按各请求判据 `--try-build-done` 集成；021 §三-B T81/T84 待用户裁决。

### 三、坑与边界

1. **完结性判定工具**：`git merge-base --is-ancestor <分支头> gitcode/develop` 是「是否已集成」唯一判据（grep 提交信息会误命中提及）；判定模糊一律保留。
2. **呈报类双载体铁律**：看板通告段发呈报的同时必须登记 021 §三-B——本案 T81/T84 漏登（499-a 起 4 天），清理轮若未甄别即丢用户裁决材料。
3. 看板通告段引言第 67 行「影响面执行完毕后删除本机通告段」自此有机械校验兜底（≤1 段+全文 ≤200 行棘轮）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-21 🧪 **服务轮（537 重发头 try-build 复验+develop 红定性补充+M3 采样·免号）**。

### 一、本轮做了什么

1. **537 重发头复验回签（家机 176 行点名·旧头 fb839411 已由深度机前会话回签·后 938 红暴露→家机出新头 6f5203dc）**：合并结果 e96e9dc6（537 六提交 rebase 最新 develop f82ac14f——938 修复/看板 6→8 两提交 develop 已有等价落地 cd1f133e/2720acfa 自动跳过；plans/025 归档/交接/更新日志/check_handoff 四处文档冲突按「保留 develop 现行+分支增量」代解）。**回签=通过准予集成**：零警告+单测 1394/1394+E2E 528=526/0/2（537/538 双负测+78/79 自举链+456 全 PASS）+matrix。**门禁在飞期间 571 已 revert be6bbcaa（0125be5b·验证本机归因）**——验证树回退版与 revert 后 develop 逐字节一致（git diff 空）·结论直接适用于新 develop faa1aa9d＋537；537 会话 rebase 最新 develop 后即可 `--try-build-done`。
2. **567 红根因实锤补充（@567-a·revert 前 f82ac14f 实测·供重做参考）**：revert 前 develop linux 侧 E2E 预热红（PRE-1 v2 全树 rc1）真凶**不是「警告计入失败」**（185 条警告不阻断·driver 仅 hasErrors 计 rc），而是 **567-a WIP（be6bbcaa）拆分的两子函数签名缺参**——生成容器调用赋值/生成容器拷贝赋值 均漏 `整64* 槽计数` 形参+`指令`/`左名ID`/`左类型ID` 局部声明未迁=65 错连锁；单文件回退 be6bbcaa^ 后 rc=0。错误清单（按文件:行聚合 9 类）在 537 合并树与 develop 逐条一致=537 零额外。已发看板深度机通告段。
3. **572 集成缺口备案**：strict 4 孤儿（434/435/495/500）=572「组合树 checkout」集成漏带 coverage_map 登记增量（557/559 回签两平台已指出的缺口·fa59f316 未随带入）——看板@572 会话补齐或机制级修复分支。
4. **M3 采样（linux 列）**：188 新样本（seed=260921170234）+回归轨 134=322——**分歧 0**（对照 09:55 单位机 arm64 轮 5 分歧全为 arm64 列存量=559 根治后 x64 干净佐证）+m38/m42 防线锚保持+t71 语义正确——零新缺陷类别·第 5 天续绿。已记采样日志 17:02 轮（daemon 记录的基线指针行是内部缓存指针 155bbe794·实际编译器=f82ac14f 构建·以本节为准）。
5. **旧协调销案**：github/develop=b605303a 为 gitcode 纯祖先（镜像落后非分叉）·535 直推分叉已自然消解；§2.115 方案文档已在 gitcode develop。

### 二、接续点

1. 537 集成前置已满足（571 revert）·挂等家机 rebase+集成；2. 565 等 arm64 回签（家机已签·周一窗口）；3. 522+526/513~517 挂等回签不变；4. **解析类声明实施轮=下一会话首选拾取**（方案 §2.115 已定稿）；5. strict 4 孤儿修复若 572 会话不接·下轮服务轮可开机制级修复分支（coverage_map 单文件）。

### 三、坑与边界

1. **E2E 预热失败诊断被 `[:200]` 截断**——首条报「警告…」误导定性为警告计入问题，真错误（`错误:` 级 65 条）在截断之后；归因必须实跑 `cn build` 看全量 stderr+grep 错误级，不能只信 runner 首行。
2. 565 回签（e924c99b）E2E 绿与 develop 预热红无矛盾——565 分支树 b1d5f5a9 **不含** be6bbcaa（晚 4 提交）；跨链比对必须核对提交祖先关系。
3. rebase 代解他家分支文档冲突=保留 develop 现行+分支增量（回签场景合规·代码冲突零容忍须语义核对——本轮解析语句.cn 两侧语义等价取 develop 版）。

## 单位机 ARM64 节

**交接时间**：2026-09-21 560（F2-36b 首步=bench v2 口径+arm64 基线入库）·558/557/559 三链收工挂等三平台回签·M3 采样+T96 立案。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **329-D12 让位处置**：D12 主面让位 546；增量面拆 557；T50 已由 331-a 兑现。
2. **558 紧急修复**（develop arm64 红根治）：457-a 两处发射缺陷（emitPtrLoadStore 7 处丢 `[`+emitLoadStore 浮点 w9 截断）——522=515/5/2+78/79 自举链 PASS+反证双态。假绿教训入册（权重 9）。
3. **557 增量面**：T55 runner 隔离键+T54 宿主货舱 [特性]+用例 495/434/435——组合态 524=521/1/2·**隔离键自愈实证**（4 互踩假红消）。
4. **559 T96 三层根治**：语义类型写回 AST 通道+三元槽按语义类型+arm64 emitCopy 双半（三后端对齐）——组合态 523=516/5/2·500 号 O0/O3 双 PASS。
5. **560 F2-36b 首步**（bench v2 口径三平台改造）：范围修正（原测 v1 组件链已推倒=对象过时→v2 全树 fix_p≡fix_s 固定点口径）+--platform 三平台工具链分派（win=ml64/link 保留·linux=as/g++）。**arm64 基线入库**：M1=22295.7ms（C++ 宿主 Debug）/M2=42608.4ms（CN 版·比值 1.91x vs C++ Debug）/**M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建快 24%**/M6 fix_p≡fix_s 逐字节+绑定自检 ✓。报告=target/bench/性能基线报告.md（产物不入库·数值誊 021）。
6. **M3 采样**：622 样本（558 修复态）+T96 立案。**呈报待批**：推断面完整化（i1/浮点形态·184 expected 契约·320-a T47 同模式）。

### 二、卡点与下一步

1. **三链挂等三平台 try-build 回签**（558→557→559 集成序·组合态门禁已预跑）；他机回签后依次 integrate。
2. **560 集成**：平台无关 python 写集=快速门禁·免 try-build（§8.3）——可在三链集成后直接走 integrate。
3. **下一轮候选**：T33 v2 窄宽读侧（施工图=025 §3.23）/D42甲波3（stdlib 源码加载）/F2-36b 续（win 复验归家机·x64l 基线归深度机）。

### 三、踩坑新增

1. **cn build linux 产物链**：--output X 产 X.s+X.o+ELF 三件——X.o=锚定链借链的「容器符号提供者」（fix_p 缺跨模块符号定义·grep 实锤）；bench M4 借链错用 fix_p 自汇编=undefined 符号。
2. **ls | head 截断**导致误判「文件消失」——长目录列表用 grep 定位。
3. **语义注记文本=源码类型名**（「整128」）非 IR 码（i128）——跨层判定必须 mapType 归一后比对。
