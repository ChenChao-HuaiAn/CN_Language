# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（554-a 开发轮·D1 滚动=生成下标左值地址拆分·2026-09-21·分支 任务/家机-554-d1indexlval·wt554 隔离）

1. **拆分**：`生成下标左值地址` 170 行（生成表达式.cn:498-667）→主函数骨架+三子函数（生成下标
   左值标识符分支/成员下标分支/裸字段分支·整块迁移·下标对象左值 参数显式传递）——**配对计数
   动态边界**（body_range）替代行号硬编码=一次通过（此前行号硬编码三次同型失误根治）。
2. **等价铁证**：同宿主双 v2p 重建——六产 asm 探针 md5 逐字节一致（t_call=4e259ec6/t_copy=
   05a55749/t_lex=932e40ca/**t_idx=98f9835b**（下标左值实弹·拆分直接消费面）/t_cpy=2dd4131a/
   230 全链=b07b8f1d）。
3. **验证链**：Ninja 零警告（132s）+单测 1394/1394+check_fn_length 出列+双文档门禁；集成时全量
   门禁+develop 现态补跑兜底。
4. **前半程（同会话）**：522/529/530 三轮集成闭环+541 拆分→缺陷 revert→T83 复核定性（两层假象
   剥离：运行时6=实验缺 stdlib 假象/79_v2 红=并发互踩假红）→re-apply 复绿（cd1f133e）+
   check_win_asm 注册修复+行数门禁 6→8 放宽（待追认）+僵尸锁×3 清除。

### 二、接续点

1. 554 集成排队中（integrate·快速门禁）；develop 现态补跑兜底；
2. 542 挂等 arm64 回签（周一窗口·linux 已 PASS）——回签齐归属会话 integrate；
3. D1 余列=生成容器对象整体赋值 176（深度机 B1 面确认后）/生成函数 153/主 132——**解析类声明
   162/解析选择语句外余量=深度机 540 在飞让位**。

### 三、坑与边界

- **切片边界=配对计数动态定位**（body_range），行号硬编码三次同型失误（行 N=lines[N-1] 换算+
  文件尾 lines[i_x+3:] 保留）——边界列表化+内容断言=防线；
- 子函数参数=主函数局部逐一核对（下标对象左值 漏传编译器拦截）；
- 撞号竞态应对=fetch+grep+改行+commit+push 一气呵成（552 撞深度机 F1-26 当场改 554）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-21 🏃 **562 热修轮（develop 红 fix-forward=456 一行回退）**·分支 `任务/深度机-562-456热修`·wt562 隔离。

### 一、本轮做了什么（562 热修轮·§8.6 破坏者优先响应）

1. **develop 红定性**：`456_泛型指针字段下标` v2 产物 rc=14≠27（稳定复现·阻塞 537/536/489/549 及 546+547 链集成排队）——根因=**本机 537-a（baf7b2c4）机械拆分时误引入行为行变更** `整64 类体起点 = 池.大小() + 1;`（537 前态 9d760efb=`池.大小()`·`git log -S` 单点命中）→节点_类 首子节点被跳过→泛型实例布局登记/查询键错位→类型大小查表 miss 返 0→`槽[数量]` 步进 0（7+7=14）。
2. **修复**=一行回退（与原码逐字一致）+ 原地注释「类体起点=类节点自身入池前池大小」。**零 src/零 tests**（测试文件未动·原「runner 环境红之谜」随本修复消解——非 runner 问题）。
3. **验证链**：456 单跑 2/2 PASS（修前 rc=14 / 修后 rc=27 反证双态）+零警告构建+单测 1394/1394+全量 E2E+strict/matrix+自举链 78_v2/79_v2。
4. **教训入册（权重 9）**：机械拆分的「顺手修正」＝静默行为变更；对拍样本必须覆盖被拆函数完整消费面（拆分 解析类声明→样本须含泛型类形态）；拆分轮收工先串行复验再判「零新增红」（并行假红族的解释权不可用于掩盖本轮真红）；develop 红了第一动作=`git log -S<可疑行>` 自查自家近轮提交。
5. **集成后解锁**：537/536/489/549 及 546+547 链的 integrate 排队（家机 537 会话两次被 456 拦截）。

### 二、接续点（下轮候选·按 A 区优先核过）

1. **546+547 接力链**（D12 预处理+456 根治·win 已回签 1f6f08a6）挂等 arm64 回签——**注**：本热修已含其 `解析类.cn` hunk（该 hunk 在 rebase 时呈「已应用」·属预期摩擦，勿判冲突）。
2. **待我机回签队列**（家机在飞链·挂等深度机 linux）：502/504/499/489/505/527/553/555/556/561/544/542（通告段逐条）——服务轮可按集成紧迫度排序批量跑。
3. **A 区可做项**：T33 v2 窄宽读侧（施工图=025 §3.23·挂点=生成语句赋值.cn 硬编码 T_整64+三后端 编码LoadStore 宽度分派）／D36+T47 架构轮（符号作用域栈化·§3-C）／未初始化读 def-init 编译期拒绝（§3-C·F1-26 波2 mem2reg 解锁前提·用户「宜先排」）。

### 三、坑与边界

1. 本机 E2E 口径：`--jobs 8` 并行 + 已知互踩族（210_v2/457/458/459）单跑才准；**先串行复验再判红**（562 教训②）。
2. 546+547 链与本热修同文件同行——集成序无强制，但先集成者后到者 rebase 时该 hunk 为空。

## 单位机 ARM64 节

**交接时间**：2026-09-21 560（F2-36b 首步=bench v2 口径+arm64 基线入库）·558/557/559 三链收工挂等三平台回签·M3 采样+T96 立案。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **329-D12 让位处置**：D12 主面让位 546；增量面拆 557；T50 已由 331-a 兑现。
2. **558 紧急修复**（develop arm64 红根治）：457-a 两处发射缺陷（emitPtrLoadStore 7 处丢 `[`+emitLoadStore 浮点 w9 截断）——522=515/5/2+78/79 自举链 PASS+反证双态。假绿教训入册（权重 9）。
3. **557 增量面**：T55 runner 隔离键+T54 宿主货舱 [特性]+用例 495/434/435——组合态 524=521/1/2·**隔离键自愈实证**（4 互踩假红消）。
4. **559 T96 三层根治**：语义类型写回 AST 通道+三元槽按语义类型+arm64 emitCopy 双半（三后端对齐）——组合态 523=516/5/2·500 号 O0/O3 双 PASS。
5. **560 F2-36b 首步**（bench v2 口径三平台改造）：范围修正（原测 v1 组件链已推倒=对象过时→v2 全树 fix_p≡fix_s 固定点口径）+--platform 三平台工具链分派（win=ml64/link 保留·linux=as/g++）。**arm64 基线入库**：M1=22295.7ms（C++ 宿主 Debug）/M2=42608.4ms（CN 版·比值 1.91x vs C++ Debug）/**M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建快 24%**/M6 fix_p≡fix_s 逐字节+绑定自检 ✓。报告=target/bench/性能基线报告.md（产物不入库·数值誊 021）。
6. **M3 采样**：622 样本（558 修复态）+T96 立案。**呈报待批**：推断面完整化（i1/浮点形态·184 expected 契约·320-a T47 同模式）。

### 二、卡点与下一步

1. **三链挂等三平台 try-build 回签**（558→557→559 集成序·组合态门禁已预跑）；他机回签后依次 integrate。
2. **560 集成**：平台无关 python 写集=快速门禁·免 try-build（§8.3）——可在三链集成后直接走 integrate。
3. **下一轮候选**：T33 v2 窄宽读侧（施工图=025 §3.23）/D42甲波3（stdlib 源码加载）/F2-36b 续（win 复验归家机·x64l 基线归深度机）。

### 三、踩坑新增

1. **cn build linux 产物链**：--output X 产 X.s+X.o+ELF 三件——X.o=锚定链借链的「容器符号提供者」（fix_p 缺跨模块符号定义·grep 实锤）；bench M4 借链错用 fix_p 自汇编=undefined 符号。
2. **ls | head 截断**导致误判「文件消失」——长目录列表用 grep 定位。
3. **语义注记文本=源码类型名**（「整128」）非 IR 码（i128）——跨层判定必须 mapType 归一后比对。
