# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（712-a·708-d 语义层重上·056 甲实施链·分支 `任务/家机-712-708d语义层重上`·wt698 树·接 711）

①**〔711 勘误（先行披露）〕**：711 收工提交 a1fb2e71 称「零代码」**失实**——711 分支历史携带 cherry 提交 bd529179（& 化 88 处·7 语义文件）随 ff 合并入库 develop。行为面安全（711 全部验证即在此形态）；流程教训=**分支集成=整个分支历史入库，写集分类与落账声明必须以 `git log 起点..HEAD` 全程核对**（快速门禁的写集分类按最后提交判定=漏洞）。②**诊断写穿面 & 化蔓延 52 处**：impl_712_refspread.py 状态机（签名块精确扫描·dry-run→落盘→diff 抽查零误伤）——65 个 诊断 形参全 & 化+符号表残留清零；audit_711_sig.py 验收**断裂位点=0**。③**验证链全绿**：v2p 蔓延态重建 rc=0（警告基线 196 不变·asm 新锚 e4a7cb3b9dd7）+**asm 性能锚 +0.372%**（562665 vs 560577 行·引用槽一次间接·<0.5% 预判）+**全量 E2E 528=524/4**（gate_lock 内·4 红已知基线恒定零新增）+79_v2 LNK1120 形态不劣化=cn_self 判据链口径达成（语义错面全消维持+形态固定点）。

### 二、下一棒

**708-e 生成器层**（056 in-out 写回机制实施本体）：v2p 容器按值实参传址化（移除逐字段聚合拷贝·fix_p.asm 527008 实锤拷贝点）+v2 树 IR/生成*.cn 容器形参同法 & 化→79_v2 -6 链接冲突清偿→**t4→0/fix_s 67157→0 判据解锁**（当前被 -6 物理阻塞）→-7 固定点→709=055+113→零红→借链终章→001 销项。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①**分支集成=分支历史全程入库**：任务分支上 cherry-pick/merge 带入的中间提交随 integrate 一并入 develop——收工落账的写集声明与 integrate 写集分类必须扫分支全程（`git log 基线..HEAD --stat`），只看最后一次提交=「零代码」假象（711 实证·712 勘误补登）。②exe md5 对拍假差异：PE 嵌 TimeDateStamp——产物确定性验证必须下沉 asm 层（711 实证）。③跨 runner 进程并发同号用例必互踩产物（LNK1104·7/8 红实证）——全量红先辨并行形态再定性。④check_handoff 经管道 tail 洗 rc 已二犯——门禁命令一律输出落文件再取 rc。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5235e83c→63c460d2 批量补验 698~703 系 8 提交 v2 树批次）·**净增绿：4 红→3 红（497 转绿）·零新增红**。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务·主树直验）**：快进至 63c460d2（701 波E）＋零警告构建＋单测 **1405/1405**＋strict **181/181**（179→181 全过）＋matrix 83/83＋E2E 全量 **528=523/3/2**——**497 转绿实锤**（698-a 拷贝构造源实参装配修复 linux 侧生效）。
2. **三红形态变化逐条对表（与家机/arm64 广播完全同构）**：113 段错误→**值差**（运行输出不一致第 1 行·待 052 裁决）；79_v2 段错误→**fix_s 编译全树失败 rc=1**（自举前进至 -6·703-a cn_self 类方法形参登记失效定性吻合）；127_v2 段错误→**退出码 206**（值差族·归属不变 001 主攻面）；524 linux 绿维持（win 单侧现象再证）。红名单与 arm64 侧 523/3/2 完全同构。
3. **重点验收面全 PASS**：699-a 六例双编译新通道（26_generics/385/386/434/500/58_generic_field）＋702-a 218 析构链复绿＋483 host-only。
4. **128 位波A/波D 抽验**：本轮重建 v2p 产 483 静态段四形态逐项正确（丙 2^127-1/丁 -2^127/戊 DEADBEEFCAFEBABE 高位归零＝修前污染消除/壬 2^64+1）＝进位链修复产物级实证；抽验产物已清理。
5. **看板**：深度机通告段写本批验收广播；交接本机节整节替换（顺带修复节标题重复行残迹）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 随 001 主攻面复验；
2. 550 阶段二呈报仍挂用户裁决（&容器变量 语义澄清）；T94 阶段一 try-build 挂等（wt550·基点早·需对 develop 头重验）；
3. 701 波E 两处配套（求值层选码+打印分支）集成后：483 v2 通道转绿复验归 053。

### 三、坑与边界

1. v2p 产物路径固定为相对 cwd 的 `target/v2asm.*`（本轮实测落 `v2asm.asm` MASM 形态＝默认 win 后端文本·GAS 侧形态待 053 实施 483 转双编译时入 E2E 门禁）——抽验须在项目根目录跑、产物即用即清；
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续/直推前树 diff 核验/门禁命令裸跑取 $?/单跑定性假红。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


