# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（718-a·运行时错误退出路径修复+崩溃处理器设施·分支 `任务/家机-718-WER抓dump定点修复`·wt698 树·接 717）

①**行为面修复（src/runtime/io_api.cpp）**：__cn_runtime_error 的 std::exit(1)→ExitProcess(1)（+fflush）——堆损坏场景 std::exit 的 atexit/stdio flush 依赖堆锁内部死锁挂住（717「错误码 4 后挂住」实测根因·ExitProcess 不依赖用户堆）。②**cn_crash_auto_install 崩溃处理器设施**：UEF 抓 C0000005 等未处理异常向 stderr 直写 code/addr/RIP/RSP/fault（WriteFile 不经 stdio 锁）·静态初始化自动安装·宿主 cn.exe 同样生效无害。③**侦查三线收束**：WER LocalDumps 受阻（HKCU 不生效/HKLM 无管理员）落账；CreateProcess DEBUG 堆改变分配形态（717 已证）；**UEF 静态初始化在 cn_self 未执行**（无 installed 输出·链接入口 shim〔WinMain→cn_main 粘合〕不跑 _initterm——cn_self 入口细节=下轮切入点）。④另实锤 **v2 树「外部 函数」语义登记缺口**：解析外部函数声明只消费 token 不登记（592-a 只做语法接受·62_ffi host-only 佐证）——v2 侧外部函数全链缺口登记（归 002/C25 收口域）。

### 二、下一棒

**719=UEF 实装改道 cn_main shim 显式调用**（wt698 树续）：定位 cn_self 入口 shim（runtime.cpp WinMain→cn_main 粘合·待 grep）→插入 crash handler 显式调用（不经静态初始化）→重链→跑 t4 抓 C0000005 现场 RIP→**map（79_a0306084_self_link.map）对位符号**→定点修复→t4→0/fix_s 67157→0 判据解锁→-7 固定点→056 传址化→709=055+113→零红→终章→001 销项。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①src/runtime 的运行时 objs 有**两套构建**：ninja Debug（MDd·cn_runtime.dir 深处）与 CMakeLists cn_runtime_79_objs（/utf-8 /MT→target/ 根·79 链接用）——手编必须 /utf-8 /MT 对齐（缺 /utf-8 中文源按 GBK 解析=C4819+格式串错位假错误）。②python -c 写含 \n 的 C 字符串会被转义吃掉反斜杠变真换行（C2001 常量中有换行符）——一律 Read+Edit 工具直改。③fastfail（C0000409）绕过 UEF/VEH 全部用户态处理——可观测化必须源头拦截（invalid parameter/purecall/abort handler 家族）或防患于未然。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-25 服务轮（550-T94 阶段一分支 rebase 至 develop 头重验·分支 `任务/深度机-550-t94指针字段` 重推 **539b6c46**·wt550）·零新增红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **550 分支 rebase+语义合并（落后 965 提交收拢）**：分支基点 22de37cb（09-21）→ develop 头 02628218；ir_expr_unary.cpp 与 588-a（005 甲案取地址一致化三分支链）同锚点冲突——语义合并=T94 引用变量分支列 **refWrapAddr 之后**（保 A-1 内部引用契约字节级）/类分支之前；判据 API 适配（lookupVar 双参出参已演进→`types::isReference(lookupSrcType(name))`·与 588-a 判据同源）；025 旧文档提交被 rebase 丢弃（取 develop 侧变空）→ §2.124 原文恢复回填+重验结论（分支提交 539b6c46）。
2. **T94 重验实证（正反证闭环·影响 550 后续处置）**：**T94 原始缺陷面已被 develop 侧 588-a 系覆盖**——探针重建三件（/tmp/p550re/：p3 容器引用版/p4 绑定检查版/p5 标量引用版）修复/无修复两态全同（p5=42 rc=0 双态·p3=-4096 rc=0 双态）+**asm 铁证=无修复版 `&v` 已发射 Load 非 lea**——550 阶段一修复对 develop 头=**行为零差异的显式化冗余补丁**（方向一致·判据更精确·保留待阶段二一并决策）；**阶段二残留面收窄且形态恶化**：p3（容器引用→指针成员调用）=静默错值 -4096 rc=0（550 收工时 rc=2 运行时防护接管·现防护未接住——静默错值更危险）——「指针成员调用双解引用」残留仍实·**阶段二呈报裁决必要性增强**。
3. **门禁全绿（合并结果·wt550 全新构建）**：零警告 ✓＋单测 **1405/1405**＋E2E 全量 **528=523/3/2**（红名单=113/127_v2/79_v2 与已知基线完全同构·零新增）＋strict PASS＋matrix PASS（346×3 后端零失败）；分支已 force-with-lease 重推（9bfb1f8b→539b6c46）。

### 二、接续点

1. **550 阶段二呈报仍挂用户裁决**（&容器变量 语义澄清·句柄槽地址 vs 堆对象地址）——本轮实证残留面=容器引用静默错值（p3 -4096）·呈报材料在 025 §2.124（含甲/乙两案对比）；
2. **550 分支命运随阶段二裁决一并定**（保留显式化补丁随阶段二实施 / 或判定被 588-a 系完全覆盖后撤分支）；
3. M5/M6 补测仍挂家机 708-e 链集成后重跑 bench；待认领队列项不变：040（480-a 分支）／045（561-a 补丁）／033（499-a 分支）。

### 三、坑与边界

1. **分支长期挂等必须定期对 develop 头重验**（本轮首例实践）：965 提交跨度里 550 的目标缺陷面已被 588-a 系静默覆盖（分支价值漂移）——try-build 挂等行应带重验节奏，防「挂等中缺陷被他人根治」的账实不符；
2. **探针重建四连坑**（无入库探针重建时对照）：手动 cn build 须显式 `--target linux-x86_64`（默认 win-x64→产物目录 `target\` 反斜杠失败）；无返回类型=`空类型` 非 `无`；类成员默认私有须 `公开:`；容器 API 返回结果须检查（`结果<空类型,整32> 加 = 表.追加(10);`）；裸指针解引用须 `不安全 函数` 修饰；
3. rebase 丢弃纯文档提交（取 develop 侧变空提交被 drop）后，**看板行引用的 025 节会断链**——须从旧提交提取原文回填（本轮 §2.124 即此处理）；
4. （历轮在册仍有效：v2p 产物相对 cwd `target/v2asm.*`／gate_lock 陈锁 rm 接续／直推前树 diff 核验／门禁命令裸跑取 $?／单跑定性假红／integrate.py 写集分类对 CN语言编译器v2/ 失效须自行补跑全量门禁。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


