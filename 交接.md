# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（495-a 开发轮·G 区 T10 收口·2026-09-20·分支 任务/家机-495-t10口径·wt495 隔离·基线 a09d10ec）

1. **行为矩阵十例实测**（探针 target/p495/）：越界长度→钳位串尾（`子串(s,2,99)`="c"）/越界起始→空串/
   负起始→钳 0/负长度→空串/起始=长→空串/空串安全/多字节**按字节劈开**（"甲乙丙"(1,3)=半字符乱码）——
   全部 rc=0 静默（安全方向·零诊断）。
2. **实现归属确认**：`__cn_str_sub`（runtime/string_api.cpp）唯一归属——边界处理与实现逐条一致。
3. **spec 02 口径条文落盘**（设计决策块·行为文档化零语义变更）：字节级口径+钳位规则+劈开警示+
   Rust get(..)=None/[..] panic 对照（CN 静默钳位=T9 整数回绕同族哲学）。
4. **021 G 区 T10 销项**（迁 §4.2）+文首戳刷 495。**勘误登记**：string_api.cpp:97「多字节按整字符保留
   不拆字节」注释与实现矛盾（实现=纯字节 memcpy）——随 src 触碰轮顺带修。

### 二、接续点

1. 纯文档轮=快速门禁即集成；spec specs 三机共享——他机 pull 后 02 字符串节新增口径条目（无契约面变化）；
2. string_api.cpp:97 注释勘误已登记（021 §4.2 T10 行）——谁先触碰该文件谁顺带修；
3. 家机挂等链不动：483（D8 x64 装载直读）/494（206-e 侦查转修复）try-build 请求中待深度机/单位机回签。

### 三、坑与边界

- **wt.py create 的标识参数生成小写分支名**——看板行必须写实际分支名（本轮 495-t10口径 第二次踩同坑
  ·483 教训复发=看板行中文标识 vs 分支小写标识不一致触发开工自检拦截——建议 wt.py 后续支持中文标识
  或认领时先 create 后写板）；
- spec 02 字符串节设计决策块=字符串行为口径唯一落点（含拥有型/返回契约/驻留/字节级四条已有）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 506-a（T70 B1 三层①根治·成员通道误抢容器字段赋值）

### 一、本轮做了什么（506-a·分支 `任务/深度机-506-t70b1b3`·基点=505 分支接力〔含 505 全部内容〕·wt506 隔离）

1. **复现**：撤守卫态 **127_v2_嵌套容器与容器字段与静态与引用** v2p rc=1（`[ir2] 结构体成员赋值右值（调用返回）非结构体返回值`×3）。
2. **定位（诊断上下文增强一次命中）**：原诊断只打空文本 → 增强为 `字段=/消费=/源寄存器=/查源=/目标=` → 立刻得 **字段=表 ×3**（容器字段）。
3. **根因**：`尝试成员结构体整体写` 守卫只查 `查结构体类型(字段类型ID)!=0`——**撤守卫态容器实例布局登记**（423-b 裸键直设）使「向量<整64> 字段」也解析出结构体 ID → 本通道误抢**容器成员赋值**（`b.表 = <容器调用>`）→ 调用路径查 `结构体返回临时们` miss → 误诊断。生产态容器字段无结构体登记=天然 0（零回归）。
4. **修复**：通道**顶**（全右值形态生效·首版插调用分支内=只覆盖调用右值·当场改位）加 `是容器类/是用户类` 排除——让位既有容器深拷/接管通道（T57/346-a）；同轮保留诊断上下文增强。
5. **验证**：①撤守卫态 **127 v2p rc=1（3 IR 错误）→ 编译通过**（下一层=运行 rc=139=`映射$整64$整64$链查找` 段错误·gdb 帧 0 铁证）；②族级双态对照（505 态 vs 506 态）124 rc=0/0·126 rc=213/213·285 rc=7/7·228/441/442 v2p 错误码6 双侧同态=**零回归**；③生产态门禁：零警告＋单测 **1387/1387**＋全量 E2E **520=518/0/2**（517 过+**1 已知并行假红 210_v2**〔编号撞车 family·单跑 2/2 PASS〕+2 跳）＋strict 100%＋matrix 83/83。
6. **顺带修登记漂移**：`313_v2_错误传播非结果类型拒绝`（431-a）/`458_负数枚举值`（436-a）两条**孤儿用例**（coverage_map 条目缺失·`--strict` 红）补登记并广播——他机若已修请以最新为准。

### 二、接续点

1. **本分支已推 gitcode**（含 505 全部内容）——try-build 请求点名家机 win+单位机 arm64；回签后 `integrate.py --try-build-done`（505→506 接力·若 505 未集成则 506 一并带入，505 行可清）。
2. **B1 三层剩余（精确指针）**：②**126 断言 213**=删除移位错——v2 输出中 `向量$T$移动元素/析构元素/析构被移除` **注入体为空**（v2 缺容器元素钩子注入设施；宿主实现=`src/cn_compiler/ir/ir_oop_container.cpp` 全量·**建议下轮主攻**）；③**127 运行段错误**=`映射$整64$整64$链查找`（gdb 帧 0·撤守卫态映射内部状态面）；④**228/441/442**=v2p 自身「运行时错误(错误码6): 无效参数」（撤守卫态编译 stdlib）。
3. **撤守卫复现链（下轮直接可用）**：`/tmp/p505/run_case.sh <用例目录> <v2p> <标记>`（已 sed 为 wt506 路径）+ 主.cn `是标准库模块` 首插 `返回 假` + `target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/p506/v2p_g --stdlib <wt>/stdlib`；族级对照 = `/tmp/p505/run_case.sh` 双 v2p（505 态=wt505/target/p505/v2p_g2·506 态=wt506/target/p506/v2p_g）。
4. **挂等回签（非本轮）**：501（八链超集）·505（同机接力·506 含其内容）·470/474/477（三链合一）。

### 三、坑与边界

- **构建坑**：`cmake --build target/build` 在 gtest 发现步骤（`gtest_discover_tests` 跑测试二进制）失败会**连带不产出 `cn_unit_tests`**——需单独 `cmake --build target/build --target cn_unit_tests` 重建（本轮实证）；主机 `target/cn` 不受影响。
- **静态门禁每轮必跑**：本轮 `check_spec_coverage --strict` 抓出 develop 既有两条孤儿（313_v2/458）——「只挂 win ci.ps1=他机盲区」再现（教训 86 同源）。
- **诊断上下文=定位加速器**：增 字段=/查源=/消费= 后一次命中根因（原诊断空文本→两轮盲猜）。
- **撤守卫态 v2p 运行段错误先 gdb 取符号**：v2 产物符号 UTF-8 hex（`_E698A0...`=映射）——`gdb -batch -ex run -ex "bt 12"` 直出帧名。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
