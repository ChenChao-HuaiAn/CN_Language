# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（452-a 开发轮·C20 批5·2026-09-20·分支 任务/家机-452-C20批5·worktree=wt452）

**背景**：C20 双测口径续轮（批4=450-a 上架 11 例后·候选池重扫）。同机三并行：451-arm64冗余mov（另一会话·src arm64）+450 批4/447 九链挂起链（等深度机 448 收口）+本轮 452（wt452 隔离·Ninja+sccache 自建宿主与 v2p）。

1. **负测面重扫（基点 develop 含 435 链）**：静态筛排除 develop 带 v2 配置 236 例+批1~4 宿主号 47 → 30 候选 → cn check 宿主拒 30 → v2p 同源编译四桶=**sem5 11/parse 14/过 5**（扫描脚本 wt452/target/p452/scan.py 不入库）。
2. **批5 上架 11 例（sem5 桶全清）**：192/195/222（混合符号运算/赋值/解引用·Rust 对齐方案A）+197（转移后使用）+198（引用逃逸三形态）+201（常量引用写三防线）+209（字符串借出初始化）+223/224/227/229（借出视图失效/容器先亡/扩展边界/类方法体）——**plans/019 编译期安全线 v2 侧持久门禁成族补齐**。v2 诊断与宿主同形态（措辞不精确锚定·450-a 先例口径）。
3. **甄别不固 17 例**：parse 桶 T63 误导 12（「语句缺少分号」噪音诊断·107/131/268/270/300/301/321/328/336/361/378/53）+252（v2 类成员修饰缺口拒）+269（拒绝对但诊断形态异）+过桶 5（299/417/422/425/81=v2 防线缺口立案面·431-a 口径）。
4. **固化方法学**：runner 同款负路径实跑（v2src<号>_v2 整树拷+v2work cwd+stdlib junction+输出路径归一→全量非空行=主.expected·锚行主段相对形式）——非手工编造。
5. **验证**：11/11 runner 单跑 PASS+反证抽例 197（v2闭环.txt 退出码改 0→FAIL「v2p 运行失败(退出码1)」→还原→PASS）+coverage_map 11 行+plans/021 C20 行与文首戳 452。

### 二、接续点

1. **全量门禁**：gate_lock 排队跑（ci.ps1 内置锁·与同机 451 串行）——终态随 try-build 通告回填（预期基线红=深度机 435 链在途用例·零新增红为验收线）。
2. **try-build 请求**：写集触 tests/**→点名深度机 linux 回签（arm64 周末豁免·周一补验）；集成序=448 收口→447（九链）→450（批4）→452（批5）。
3. **C20 候选池近枯竭**：本轮 30 例中防线对齐桶全清——余=T63 误导桶 12（须 v2 诊断改进后才可固）+过桶 5（须先立案根治 v2 缺口）+孪生面。后续 C20 轮建议转「D40 缺口桶定性」或「T63 诊断对齐」前置轮。
4. 下一开发轮候选：①T63 误导诊断对齐（v2 parse 诊断形态改进=机制级）；②过桶 5 例的 v2 防线缺口立案轮；③D1 拆分滚动（handleClassCallExpr 362）；④F2-33 stdlib 三项（随机数/迭代器/正则）。

### 三、坑与边界

- **多开会话撞号实例**：本会话欲认领 451 时另一会话已直推看板占号（arm64 冗余mov）——fetch 后改 452 免撞；Edit「modified since read」=对方活跃信号（§8.8 教训实锤）。
- **反证 tail 误读**：runner 失败时失败详情行在汇总行之后——`tail -1` 只截到详情尾（「[sem5]」）造成 PASS 假象——判结果必须看「总计」行或 `--verbose` 全量。
- worktree 自建 v2p 全链（10 运行时 obj 由 Ninja 构建产出+cn build v2 树+audit2 隔离）——不依赖主树 audit2/v2p.exe（防同机并发重建互踩）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **本会话收官（用户令：本轮后不再接新任务）——九轮开发+七项服务动作，全部闭环或到达协议等待点**。

### 一、总账

1. **服务轮六签**（全部准予集成）：459-a/467-a/家机 476-T71/家机 471-D25/家机 483-D8第四件/家机 494-波4修复 linux 回签（494 含 famscan 代跑 0/47）+451 集成后复验备案。
2. **已集成四轮**：475/476（D46 家族终态）+478（F1-26 设计定稿）+479（F1-26 波1 Mem2RegPass·a726a6f1）。
3. **三链合一挂等家机 win 回签**（请求 acb10493·替代原 8415a43f/94596572）：验证分支 `验证/深度机-三链合一`（头 313edffb=477 含 470 全部+474+develop merge 树）——**本机合并树门禁已全绿**（481=479/0/2+strict/matrix+自举链+四组反证）。**接手第一动作：fetch 查该请求回签→三链依次 `integrate --try-build-done`（470→474→477）→看板清行**。
4. **477-a D10 波2**（接力 470 分支·已收工 4cb7b09a）：重放环境映射 T→实参（p2' rc 0→1+459 负测+反证）。
5. **482-a 定案**（分支留档）：F1-26 波2 回退（undef 双雷）——**B-未初始化读呈报已落 021 §三-B（a1323f80·甲=def-init 拒绝推荐·待用户裁决）**。
6. **新单移交**：489-T87 的 try-build 请求点名深度机回签——**本会话按用户令不接**，留给接手会话/其他深度机会话。

### 二、验证链总账

九轮开发门禁全绿零新增红+反证双态六组+量化三组+服务轮六签各附合并树门禁+famscan 代跑 0/47。

### 三、接手会话清单（优先序）

1. 三链合一回签→集成→清行（470/474/477）。
2. 477 集成后：B-未初始化读呈报呈用户裁决（021 §三-B）；裁决后 D10 宿主残余/F1-26 波2 重做。
3. 489-T87 回签（未接）；480/484/486/488/490/491/493/495 同机会话在飞勿撞。

### 四、踩过的坑（新沉淀）

cn build 产 ELF 非 asm；负测行号=runner 真跑口径；59 行=callee/裸名共用路径；语义主分派委托在语义检查语句.cn；v2 静态标量写标 不安全；worktree 分支占用=detach 钉提交接力；mem2reg 前置查 IR 语义原语（undef）；**多链合一验证树=coverage_map 多侧拼接易丢登记（strict 孤儿对账·各分支行抄录并集）**；轮次号销账后时序窗口=他机可复用同号。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
