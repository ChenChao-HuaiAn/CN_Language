# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（738·001 主线移交深度机·交接包落盘·纯文档轮·分支 `任务/家机-738-001移交交接包`）

**用户裁决「深度机立即全盘接手」**——001（泛型类源码编译全链）主线自本轮起移交深度机全盘推进。本轮产出=**全盘交接包（025 §1.45）**：①状态基线（develop 66620485·全量 524/4·定位链索引 §1.38→§1.44·全平台设施三件=VEH 寄存器增强/realloc 防御/映射未构造防御）；②**复现链**（写回单件精确 diff+t4.cn 全文+判据对照三态=基线 4 错/v2p 崩现场/写回后 t4→0）；③定位结论（缺陷形态=「偏移 8 被当地址解引用」RAX=8·崩溃点=收集EXTERN符号+0x14ea·上游链·已排除三侧配对矩阵）；④深度机接手四步；⑤诚实边界（win 侧现场物在 wt698 target/ 未入库·需经看板索取）。

### 二、下一棒

**深度机全盘接手 001**（linux-x86_64）：按 025 §1.45 复现链先行验证 linux 侧形态同构性（720-a 时 linux 侧=语义错 67223 形态·与 win 崩溃链不同）→复现则按定位结论修复（收集EXTERN符号+0x14ea）→修复验证（v2p t4 零错+全量门禁+双件回归）→后续链 t4→0→fix_s 67157→0→-7 固定点→709=055+113→零红→借链终章→001 销项。家机侧移交后**无未完事项**；win 侧现场物（全反汇编/map/探针）如深度机需要请经看板通告段索取。任务 057 📣（挂账 9 分支）仍待用户裁决。

### 三、坑与教训

①**跨机接力的三件必交**：复现链（精确 diff+探针全文+判据对照）、定位结论（形态+崩溃点+已排除项）、诚实边界（未入库的现场物清单）——缺任一件接手机都要重建开销；②移交≠销账：001 仍未完成（t4→0/fix_s 判据/056/零红/终章均在），交接轮只改变承担机器。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-26 776 轮（550 阶段二·p3 真根因终极定位·两笔尝试回滚·零回归态）·分支 `任务/深度机-776-550阶段二续`（wt550 树）·基点 develop 7b9137b3。
 **〔788 治理轮收工补记（2026-09-26·任务/深度机-788-文档滚转治理·wt788·另会话）〕**：纯文档治理轮已集成 develop 6cac13aa——棘轮贴线集中处置（教训 §二 老索引 288 行+019 三区间 259 行滚转归档·看板 78 条已完结通告清理〔保留语义裁决呈报@用户+挂等核查@家机〕·021 老化五行处置·基线收紧 600/300/125）+本文件重复「深度机节」事故修正（旧节删·343-a 同族）；详 025 §2.149。776/784 主体交接不受影响。

### 一、776 轮成果（p3 真根因终极定案）

1. **q2 初始化已对**（772 AddressOf 容器扩面生效·q2 槽=堆地址 gdb 证实）；**真病灶=q2.大小() 的 this 装配走「指针→初始化来源回溯」**（asm：this=Load(源形参槽)=表槽地址·而非 Load(q2 槽)=堆地址）→大小(表槽)=0。回溯路径在容器方法分派的接收者装配（待精确定位）。
2. **两笔修复尝试（A 分支排除引用类型+C 容器引用两跳）已回滚**：修好 q2 初始化但 p8 仍红（**this 回溯未触及**）+**113 v2p 启动 SEGV**。**776-h 二分终极定案（干净环境）**：①仅 C 两跳→113 PASS ✓（无害）②仅 A 排除→113 v2p SEGV ✗=**A 排除=回归源**（排除后 wrap 节点 refWrapAddr=真 从 A-Load 改落 B-AddrOf·**A-1 wrapRefArgs 契约破坏**→v2p 符号表 设置 链崩）——**修复须三件原子不可拆**（this 形态随裁决+A 排除连动 B 契约重定义+C 两跳）·**语义裁决呈报维持**（看板通告段）。回滚后 113 PASS+p6 绿=零回归。
2b. **【776-c 终版修正】「this 回溯」假写作废**——三层打标（handleClassCall 接收者=id:q2 ✓/thisArg=genExpr(q2)=Load(reg4) 发射正确）+gdb 复测：**真缺陷=槽寻址错位**——同一 unique（q2$3）的 Store 写 -0x38、Load 读 -0x70（源形参槽）=**含引用形参函数的变量槽分配/寻址错位族**（p9b main 内一致 ✓·错位仅在含引用形参函数域）。776 续修复方向=二分定位分配器错位单点（最小函数：形参+局部指针声明）。
3. 探针资产：/tmp/p772/{p6,p8,p8b,p9,p9b}.cn（四形态矩阵：&局部 ✓/&引用形参 ✗/字段存储 ✗）。

### 二、776 续接力（修复本体三件一体·前置=语义裁决）

1. **呈报裁决**：&容器引用 语义（甲=两跳对象地址 vs 单跳 place 地址）——对 v2 树 708 & 化存量的影响面；
2. **裁决后三件一体**：①this 装配去回溯（指针成员调用接收者=genExpr(指针变量) 本值·回溯点在容器方法分派接收者装配·待精确定位）②A 分支排除引用类型③C 两跳形态随裁决；
3. 判据：p8/p8b 绿（大小=2）+p1/p3/p4/p6 全 0+113/242 系维持绿+525_v2 上架+全量门禁+integrate；
4. 001 主线：波2（055 宿主 ABI·家机 706 域）→波3（乙面④+丙面·cn_self 链）→波4（借链 51 例→001 销项）。

### 三、踩过的坑

- **E2E 用例副本漂移三犯**（30/31→113）——「宿主绿 v2 红」先 diff 副本与主库；
- **撤笔归因法**：多变量纠缠逐笔回退单变量实测；
- **号域雷**：寄存器号作表键跨语句复用=概率误命中；
- **插桩+checkout 互坑**：git checkout 清打标·打标/还原须一次原子完成；
- **p8/p8b/p9 形态混淆**：q2 只在 p8b/p9——探针命名与形态映射须先对齐再推理。

### 784-a 轮补记（2026-09-26·p8/p10 族清偿·语义呈报撤销·随本集成）

1. **776-j/j+ 定性推翻+真根因确诊**：asm 逐条对位证 codegen 槽映射完全正确——真因=**Load 寄存器操作数（id>=0）语义两态错位**（codegen emitLoadStore 对其发射寄存器槽复制不解引用·776-i 两跳第二跳解引用意图落空·q2 得 place 栈地址·gdb 铁证）。
2. **修复两件**（ir_expr_unary.cpp）：①第二跳 Load→LoadPtr（甲案语义落地）；②refWrapAddr 契约分支提前+wrap 产物按操作数二分（普通变量=AddrOf(槽)·引用形参=Load(源槽)=被绑定 place 地址）——v2 树 88 处 & 化实参链精确保持（首轮仅①时 v2p 编 113 自身空指针崩·重排后复绿）。
3. **语义呈报撤销**：776 续「甲/乙语义分歧」技术前提被根因消解（机制级·随证据备案）。
4. **验证链**：探针族 p8/p8b/p10/p9b/p6 全绿+反证双态+113 PASS+E2E 528=525/1/2 零回归+单测 1410+strict/matrix/philosophy/win-asm 全绿。插桩全撤（含 776-j+ 残留）。
5. **下一棒**：127_v2（052 波2/3）→055 宿主 ABI→乙面④+丙面（cn_self 链）→全量+借链 51 例+撤守卫零红→001 销项。


## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


