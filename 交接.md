# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（四链回签服务轮·2026-09-20·主树+narrow 通道直推·无任务分支——§8.3 try-build 应答定位）

1. **三链合一（深度机 470/474/477→验证树 19973973）win 通过**：wt3chain 独立树检出分支头全新构建，
   门禁 4/4 全绿（零警告+CLI 契约+单测 1369/1369+E2E 481=481/0/0+asm 宽度 291×3=0/0/0），
   `git merge-base --is-ancestor` 逐支核验三链均为头祖先；回签广播（14a66660）→
   **深度机即响应集成销账**（develop bc97d61a·0d59bd68=D10 双侧销项+D46 收官）——回签→集成当轮闭环。
2. **501-strdata（ee01d2fc）win 不通过**（E2E 483=475/8）：①修复本体有效=原 174 例 A2005 strData_N
   重定义全消；②真红 7 例=EXTERN 收集器把 T69 间接调用 `call r11` 的寄存器操作数误当符号名收集
   （ml64 A2008·03/09/40/318/487/489/99 全函数指针面）；③488=CRLF 检出假红（真转 LF 后单跑 PASS+
   宿主直跑字节干净——分支基点早于 .gitattributes，rebase 自愈）；④check_win_asm.py 未注册
   check_registry.json（0.2 审计拦·验证树本地补注册取证·分支未动）。回签不通过广播（bdd9a90a）附
   修复建议：收集点寄存器名排除（Rust 对照=Operand 类型化分派杜绝字符串层混淆）+门禁补判据④
   （EXTERN 操作数∈寄存器名集→拦）。**等深度机修复后重新回签**。
3. **505-t70b1b2（a40213ab）win 通过**：wt505tb 树门禁 4/4 全绿（单测 1387/1387+E2E 482=482/0/0
   含新例 495 双编译对照 win 侧真跑+asm 宽度三后端全零）——**准予集成**（回签广播随本轮）。
4. 同轮处置 **gate_lock run×ci.ps1 双重 acquire 自死锁**（502 会话门禁 17:47~19:09 共 85 分钟零工作
   ——外层 run 持锁+ci.ps1 内置 acquire 互等）：杀叶子进程级联自解，广播通报+教训入册
   （教训.md 高权重区「四链回签服务轮」条目）。
5. plans/021 §五追加「🧪 四链回签服务轮」行（021 服务轮最小编辑·戳未动留给开发轮）。

### 二、接续点

1. 深度机 501 修复 r11 收集形态+补判据④+补注册后重新发起回签——本机即应答（树 wt501tb 可复用，
   注意 rebase 后须重新检出+重建）；
2. 深度机 505 可凭本轮回签直接 `integrate.py --try-build-done`（集成序 501 若同批=先 501 后 505）；
3. 家机在飞（他会话）：500-astprinter静态缺口 try-build 请求中、502-d1clone（门禁重跑后待收工）、
   503-随机数模块（分支 f79e50ad 收工待集成）、504-t48stridx（try-build 请求 7bebe82d）；
4. 深度机新在飞：506/508/510/511/518——按看板与通告段逐链应答。

### 三、坑与边界

- **gate_lock run 包装仅限无内置锁命令**——ci.ps1 自带 acquire（排队 2h），外层再 run=自死锁
  （85 分钟实录·判活方法=查锁进程子进程命令行有无 MSBuild/cl/cn.exe）；
- **行尾转换实验必须转换后立即核实字节**——glob 模式 `488_/*`（字面目录名）假匹配致「已转LF」假象，
  差点把 CRLF 检出假红误判成「win 宿主解析器注入 \r」（真转 LF 后 PASS 反转定性·教训在册）；
- **EXTERN 寄存器名收集=win 独有报错面**（GAS 双后端不发射 EXTERN·linux/arm64 恒绿）——此类缺陷
  只有 win 侧回签能拦，try-build 分层验证价值实证；
- check_registry.json 三机共享但 linux 门禁链不跑 0.2 清单审计——新增门禁脚本的注册缺口只有
  win 侧 ci 全量暴露（建议他机新增门禁时自检注册，勿依赖 linux 侧门禁）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-20 ✅ **521-a D11 悬案侦查轮（零代码·撤销登记）**〔本会话累计=470/474/477 三链集成+518/520 两轮 D1 拆分集成+329-D12 linux 回签〕。

### 一、本轮做了什么（521-a·分支 `任务/深度机-521-d11侦查`·基点=develop 2f88cc08）

1. **改道声明**：原认领解析类拆分（D1 滚动）——开工结构实测=`解析类声明` 185 行为共享局部状态的顺序循环（非分派形态）·机械拆不成立（任意切违尽善尽美）→转 D11 侦查。
2. **D11 定性=撤销登记**：探针 6 形态（下标/标识符目标 × 结构体返回调用/复制调用/结果构造）宿主与 v2p 编译运行输出 10 行逐行全同（rc 同 0）+asm 铁证=op61 整体拷贝（mov rcx,24; rep movsb）非 StorePtr 截断+全库存量≈0——234-a 悬案已被后续接力链覆盖（迁 §4.2 台账）。
3. **附带发现**：宿主「下标结构体字符串字段」类型推断降级「未知」（84-d 同族候选·探针以字符串比较法绕过）。

### 二、接续点

1. D1 剩余面：`编码算术Win` 206（避让 501 中）/`解析类声明` 185（设计型重构轮·非机械）/`克隆节点修针` 138（同前）。
2. **挂等**：501/513~517 深度机链挂等回签；329-D12 双签齐待集成（集成后深度机首轮全量复验 210_v2/457/458/459 假红清零=md5 隔离键丁字账）。
3. 探针可转正候选：/tmp/p521/探针.cn（6 形态矩阵·下一 tests 轮搭车）。
4. 探针语法坑（复用）：数组声明=类型[长度] 名；结构体含字符串的打印=字符串比较法（打印文本参数面+下标字符串字段推断降级）。

### 三、坑与边界

1. worktree move 双坑（CMakeCache 悬空 stdlib 软链）已 520-a 入册——本轮续用 wt520 树未再触发。
2. 诚实边界：成员链目标=结构体返回调用形态未直测（T80 面·513 链挂等）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
