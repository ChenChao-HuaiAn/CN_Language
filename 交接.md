# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（729-a·v2p 崩溃链符号对位·分支 `任务/家机-729-v2p崩溃链符号对位`·wt698 树·接 728）

**方法学突破**：手动重链 v2p 加 /MAP（79 rsp 模板换 obj）——原 rsp 的 /MAP 被同次链接覆盖→**79_a0306084_self_link.map 06:01 版=5157 符号的 v2p 映射**——**崩溃链 9 帧全对位**：`代码生成$代码生成(+0xc7 崩·fault=0x1FD0)←代码生成$是裸符号操作数←生成语句赋值$尝试下标结构体整体写←生成类$生成方法×2←生成类$发射类拷贝构造初始化←生成表达式$生成表达式×2`。**语义链**：v2p 生成层自崩溃=发射 映射 拷贝构造方法体（含 `桶数组[桶索引]=...` 整体写）→**尝试下标结构体整体写**命中（生成语句赋值.cn:750）→发射 **IR_结构体拷贝**→**代码生成层 +0xc7 崩**（读表 NULL+0x1FD0）——**畸形 IR（宽度/操作数异常）致分派崩**。与 728 的 fault=0x1FD0 完全同址（执行期/编译期两触发路径）。

### 二、下一棒

**730=代码生成 +0xc7 定位+整体写 IR_结构体拷贝 发射段修复**（wt698 树续）：读 代码生成.cn 代码生成 函数前 30 行（+0xc7 读哪个表）+尝试下标结构体整体写 的 IR_结构体拷贝 参数构造（源句柄/目标地址/**宽度**——宽度=垃圾嫌疑）→定点修复（守卫+值构造校正）→v2p 跑 t4 零错→全量 524/4→双件（写回+元素引用）回归→t4→0→fix_s 67157→0→-7 固定点→056 传址化→709=055+113→零红→终章→001 销项。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①**RVA 对位无 map 的解法**：手动重链加 /MAP（rsp 模板换 obj·原 /MAP 覆盖法副产品）——5157 符号映射一次到位；②**同一 fault 地址（0x1FD0）在编译期与执行期出现**=同一代码缺陷的两条触发路径（v2p 编译期发射崩/v2p 产物执行期崩）——多路径同址=缺陷定位的最强信号。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-25 服务轮（第 724 认领后·B8 巡视＋M3 采样补轮＋724-a linux 复验回签·窄通道②·零分支零轮次号·基线 develop **906e3647 系**）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **选题依据**：家机 723/724 已看板先行认领（拷贝构造对拍链·wt698 在飞）但无深度机待应答项；87870e7c→b6b0781a 增量核对=纯文档（看板先行/勘误/上轮回签落账）——行为面零增量，按写集分类走快速门禁，无需全量复跑；服务轮默认动作（B8 巡视＋M3 采样）补一轮（距上轮 01:2x 约 3.5 小时）。
2. **主树编译器新鲜性核验（滞后基线必重建纪律的执行）**：`target/cn`（9-24 11:38）增量构建显示 up-to-date——核验其输入：期间 src/ 仅 runtime 两文件变更（io_api.cpp/runtime.cpp·均编译器本体之外），编译器本体源码零变化＝产物有效；cn_runtime 增量重编至当前树（io_api.cpp.o 05:04）。
3. **M3 采样一轮（cnsmith_daemon --rounds 1·ulimit -v 6GB 防线·21.7s）**：**322（新轨 188 seed=260925050559＋回归轨 134=命中库全量 diff56/build_err3/run_err75）＝319 一致＋0 分歧＋3 存量命中**——m38_01 循环头作用域泄漏（build_err「未声明的标识符 '甲'」）/m42_01 空类型算术（build_err·形态与 09-24 逐字一致）/t71 组合段错误复现语料（run_err rc=1）；**零新存档零新缺陷类别·M3 计时连续**（判据=连续 4 周零新缺陷类别·自 09-17 存量迁移起算）。
4. **看板深度机通告段清理**：旧条目（✔ 698~721 系各批验收＋🚨 09-23 红灯定性＋657 广播）影响面均已毕（后续多轮验收已覆盖红灯形态演变），按「影响面执行完毕后删除本机通告段」规则清理，换入本轮 B8 巡视条目。
5. **ⓘ daemon 文档字符串与实现的偏差备案**：`cnsmith_daemon.py` 文档写 `--once` 参数但 argparse 未实现（报 unrecognized arguments）——现用 `--rounds 1` 等价；已在本机节留痕（纯观测备案·不改代码·如需修属机制级小改可随后续轮顺带）。
6. **724-a cn_realloc 防御 linux 复验回签（采样期间家机 724 收工发出验收请求·本轮并入应答）**：io_api.cpp +8 行=size>2GB 可观测拒绝（平台无关代码·fprintf+NULL·Rust alloc 合同同款）——零警告构建＋单测 **1405/1405**＋strict/matrix 全绿＋E2E 全量 **528=523/3/2**＝红名单与已知基线**完全同构**（113 值差/127_v2 rc206/79_v2 fix_s rc1·零新增红零回归）＝防御分支对合法路径零影响。
7. **rebase 解冲突修正家机 021 误编辑（越权修正·提交信息留痕）**：家机 724 收工链对 plans/021 的编辑含两处误动作——①把已滚转入归档切片的 554-a 行加回 §五（主表+归档两存·违反滚转语义）→维持滚转态未保留；②删除了深度机 🧪 721-a 回签服务轮索引行（§五 索引行丢失·562 行同族先例）→已恢复。

### 二、接续点

1. **深度机侧零待办**（724-a 复验已回签）；家机 725 在飞（t4 4 错根因修复=标量形参装载失效·707 本体）——若发验收请求按服务轮职责应答；
2. 550 阶段二呈报仍挂用户裁决（&容器变量 语义澄清·材料 025 §2.124）；M5/M6 补测挂家机 708-e 链集成后重跑 bench；
3. 上轮挂等核查轮接续点维持：@家机 561/499 分支保全处置；040/045/033 摘取 rebase 集成（480-a 已保全可 fetch·属开发轮性质）。

### 三、坑与边界

1. **服务轮「先核后跑」的写集分类前置**：本轮行为面零增量（87870e7c→头纯文档）→快速门禁即可，未重复全量构建——上轮独立树 wtsvc721 全量门禁（528=523/3/2 同构）对 develop 头传递有效；
2. **增量构建「up-to-date」≠产物新鲜**：必须核验输入源码时间戳/变更面（本轮 target/cn mtime 9-24 但输入零变化=有效；若输入有变而 mtime 旧=滞后基线必重建）；
3. （历轮在册仍有效：挂账分支核查须含 ls-remote 存在性＋树级特征码双道／他机资产分支引用不可代动／分支长期挂等定期重验／探针重建四连坑／rebase 丢纯文档提交看板断链／v2p 产物相对 cwd／gate_lock 陈锁 rm 接续／直推前树 diff 核验／门禁命令裸跑取 $?／integrate.py 写集分类对 CN语言编译器v2/ 失效须自行补跑全量。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


