# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（734-a·三侧配对矩阵全案+733 勘误·纯侦查·分支 `任务/家机-734-056实施配对统一`·wt698 树·接 733）

**三侧配对矩阵（源码直读全案）**：①调用方登记（IR生成.cn:275·fix5b）= `!是容器类 && 查结构体大小!=0` → 标「结构体实参位」；②调用方发射（求值:1085 B8/513 B3）= 结构位命中 → **回扫取地址（传地址）**；③被调方装载（生成函数.cn:285·双模收敛根因5）= `!是容器类 && 结构大小!=0` → **多槽+prologue「从指针入参拷贝入槽」**（合同=地址）。**三侧判据与合同完全一致——配对无错位**。**733 的「形态错位」定性经本轮双实现对读证伪（诚实勘误）**——**732 的 fault=0x8=prologue 拷贝路径的入参=NULL**（拷贝第 2 个 8B 时读 [NULL+8] ✓ 吻合）。

### 二、下一棒

**735=入参 NULL 产出点追踪**（wt698 树续）：①回扫实参取地址 的产出（对「值形参实参」的 IR_取地址 源槽——空槽？）②this（rcx）方向（映射.设置 的 this=符号表末元素地址计算）——VEH 增强（打印 rcx/rdx 值）或源码级插桩 设置 入口 → 定位 NULL 产出点 → 定点修复 → v2p t4 零错 → 全量 524/4 → 双件回归 → t4→0→fix_s 67157→0→-7 固定点→709=055+113→零红→终章→001 销项。任务 057 📣 仍待用户裁决。

### 三、坑与教训

①**跨轮定性必须经双实现对读复核**：733 的「配对错位」推断被 734 的三侧矩阵证伪（各自独立看都合理·合起来看一致）——**修订诚实入册**；②fault=0x8 的新解释（prologue 拷贝第 2 个 8B）=与 732 的 fault 反推一致（读的是入参指针+8）。

## 深度机 linux-x86_64 节

**交接时间**：2026-09-25 服务轮 736（B8 巡视＋静态门禁＋stdlib 守卫变更 linux 侧全量复验）＋737 修复轮（cnsmith_daemon --once 机制级当场修·集成 **aa3cd180**）·窄通道②收工·基线 develop **e8c9340d**（收工时含 737）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **增量写集树级核验（先核后跑·本轮抓到关键差异）**：bd7c2354→e8c9340d 增量 12 提交（家机 731~735 链），`git diff --stat` 树级核对＝11 提交纯文档＋**8c2efc69（732-a）stdlib/映射集合.cn 行为面变更**（链查找未构造/空桶守卫→返回 -1）——**提交信息自称「守卫态全量 528=524/4 零回归」但只覆盖 win 侧，且 stdlib/ 不在 integrate.py try-build 触发面、732 收工未发两机复跑请求**＝linux-x64/arm64 侧无复验证据。
2. **linux 侧全量复验（服务轮主任务）**：主树 ff 至 e8c9340d；src 零变更＝target/cn 本体有效无需重编；gate_lock＋`--jobs 8` 全量 **E2E 528=523/3/2 红名单同构零新增**（3 红=113 值差/127_v2 退出码 206/79_v2 fix_s 编译失败·形态与基线一致；2 跳=62/69 平台固有）——**732-a 守卫变更 linux 侧复验通过**（看板通告段已广播@家机）。
3. **静态门禁三件（他机盲区）**：strict 181/181＋matrix 83/83（=B8 面台账机械核验·未测 0·已探针 43·已转正 40）＋philosophy PASS——全绿。
4. **M3 采样补轮**：上轮采样（05:06·基线 b6b0781a）早于 stdlib 变更入库＝编译器输入已变不适用结论传递→e8c9340d 基线补跑 **322=319 一致＋0 分歧＋2 build_err＋1 run_err（全存量 m38/m42/t71）**同构零新缺陷；日文件两轮已记（07:07 补轮＋07:12 --once 验证轮）。
5. **737-a 机制级当场修（§8.4 随证据备案）**：跑采样撞到 `cnsmith_daemon.py --once` 帮助文本承诺但 argparse 未注册（`unrecognized arguments`·--rounds 1 等价未阻塞）——看板先行直推认领（07766d05）→修复分支补注册+once 钳 rounds=1→运行级验证（--once 真跑恰 1 轮 rc=0）→integrate 快速门禁集成 **aa3cd180**（gitcode+github 双推·任务分支自动删）。
6. 棘轮核对：021=438/450·025=696/720·看板=189/200 均在限内；**教训.md 797/800 贴线**（下轮任何追加前先滚转）。

### 二、接续点

1. 家机 wt698 链在飞（735=入参 NULL 产出点追踪→定点修复）——若发验收请求按服务轮职责应答（重点=v2p 重建+全量红名单同构）；732-a stdlib 守卫的 arm64 侧复验归单位机（本机通告段广播已含结论可参照）；
2. 550 阶段二呈报仍挂用户裁决（&容器变量 语义澄清·材料 025 §2.124）；M5/M6 补测挂家机 708-e 链集成后重跑 bench；
3. 历轮挂等维持：@家机 561/499 分支保全处置；040/045/033 摘取 rebase 集成（480-a 已保全·开发轮性质）。

### 三、坑与边界

1. **「自称纯文档」必须树级 diff 复核**：本轮 12 提交中 git log 文本初看 8 条（`-8` 截断）且多数自称纯文档——树级 diff 抓出 stdlib 行为面变更（grep 分类面须含 stdlib/，仅 src/tests/CMakeLists 会漏）；
2. **stdlib 不在 try-build 触发面**＝家机集成的 stdlib 变更默认只有 win 侧证据——他机遇 stdlib 变更须补本机全量（integrate.py 写集分类已知缺陷家族·教训 218/备案 @581 的姊妹面）；
3. **服务轮不刷 021 文首戳**（文首戳承载最新轮次号·刷戳=「丢他机新戳」事故形态）——本轮戳系 737 开发轮集成时随分支刷新（§8.5 ①合规路径），非服务轮所刷；
4. （历轮在册仍有效：服务轮「先核后跑」写集分类前置／增量构建 up-to-date≠产物新鲜·必须核验输入／挂账分支核查 ls-remote+树级特征码双道／他机资产分支引用不可代动／rebase 丢纯文档提交看板断链／v2p 产物相对 cwd／gate_lock 陈锁 rm 接续／直推前树 diff 核验／门禁命令裸跑取 $?／integrate.py 写集分类对 CN语言编译器v2/ 失效须自行补跑全量／daemon 有 git pull·收工文档必须在采样后写。）

## 单位机 ARM64 节

**交接时间**：2026-09-24 服务轮（协议 v3 集成后异步验收·5ed202f7→2a96c036 批量补验 698-a/702-a/703-a 三轮 v2 树修复）·零新增红、零特有红。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **批量验收（服务轮主任务）**：快进至 2a96c036（703-a）后**清场重建**＋单测 **1405/1405**（+8 例）＋E2E 全量 **528=523/3/2**——**净增绿：基线 4 红→3 红（497 转绿·698-a 装配修复 arm64 实证）**。
2. **红面形态变化逐条对表（全与家机广播一致）**：113 段错误→**值差**（全局[1] 101 变 -1·702-a 广播一致·待 052 裁决）；79_v2 段错误→**fix_s 编译失败 rc=1**（703-a 定性 cn_self 类方法形参登记失效吻合·025 §1.11 专项）；127_v2 段错误→**退出码 206**（红维持·值差族形态·归属不变 001 主攻面）；524 arm64 侧绿维持（win 单侧）；218 容器元素所有权归一化 **PASS**（702-a 复绿同侧成立）。
3. **容器析构面回归全绿**：108/111/112 系/115/116/121/123_v2/126_v2/155/214/219 全 PASS。
4. **M3 采样补跑一轮**（626 波2 后首次·两日空窗补记）：**零新缺陷类别**——m27_03/04/05=T27 已知族复现（m27_03 与 09-21 meta 逐值一致 O0=-2147483648/O3=0·m27_04 O0 逐值无当日对照=诚实边界）+m38/m42/t71 存量；日文件 2026-09-24.md 已记。
5. **看板**：基线行 5ed202f7→**2a96c036** 登记；单位机通告段写三轮批量验收广播并删 596+601 旧通告（影响面已毕）。

### 二、接续点

1. 052（元素读取语义·甲已裁）实施轮集成后：复验 **113（值差）**；025 §1.11（cn_self 类方法形参登记失效）专项集成后：复验 **79_v2**；127_v2 形态 206 待与家机对表后随 001 主攻面复验；
2. 050（已立账·568-a 遗欠）恢复后随全量自然覆盖；
3. 常态：M3 采样/B8 巡视（本轮已补一轮·daemon 未常驻）。

### 三、踩坑新增

1. cn CLI 单文件验证：`cn build` 不接受 `-o`（多余源文件参数报错）·产物恒落 `target/` 直下（同名 .cn/.o/.s 三件）——手动样本验证后须清 `target/<样本名>*`；且必须在项目根目录调用（运行时 io_api.cpp 相对根解析·历轮在册再证）。
2.（历轮在册仍有效：gate_lock 陈锁 rm 接续／直推前 `git diff --stat gitcode/develop HEAD` 核纯窄通道／bisect good 基准必须实测。）


