# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（529-a 开发轮·D1 滚动=分词拆分·2026-09-20·分支 任务/家机-529-d1tokenizer·wt529 隔离·基线 9738fac7）

1. **拆分**：`分词` 151 行（词法分析.cn:182-332）→主循环+两分支函数（分词字符字面量/分词标识符·
   对齐 R3 分支函数惯例签名「收 源码/位置/长度/行号/输出 返回新位置」）——语义等价变换点=内联块
   「继续」→函数尾「返回 位置」/前缀字符串分支「继续」→「返回 位置」；python 行号切片+14 边界断言
   （切片边界三次试错被断言逐条拦截=264 是 `}`/315 是 if 闭合/体末行号理解——断言即防线）。
2. **等价铁证**：同宿主双 v2p 重建——四产 asm 探针 md5 逐字节一致（t_call=4e259ec6/t_copy=05a55749/
   **t_lex 词法敏感=932e40ca**〔字符字面量+汉字标识符+字符串·拆分两块直接消费面〕/230 全链=b07b8f1d）；
   t_obj/235 两侧行为逐字符一致（v2p 已知语法缺口·非本轮面）。
3. **验证链**：Ninja 零警告构建 PASS（85s）+check_fn_length 出列+双文档门禁 PASS；集成时全量门禁
   +develop 现态补跑兜底（v2 树写集不在全量触发面·522-a 同款诚实披露）。
4. **前半程（同会话 522-a）**：编码对象调用ARM64 153 行拆分已集成入 develop（eeac863c·md5 铁证·
   develop 现态全量门禁补跑 4/4 全绿=单测 1387+E2E 522 全 PASS）+看板销账广播。

### 二、接续点

1. 529 集成排队中（integrate.py）；若本会话消亡：分支已推（拆分提交 186f670d+收尾六件套随分支），
   任意会话 fetch 后 `integrate.py --try-build-done` 即可；
2. D1 滚动下一列=克隆节点指针 138（IR类.cn·深度机 F1-26 面交接）；
3. 多会话让位实录（本会话）：483→-i2 会话/494→补刀会话/497→他会话/501 回签→他会话——认领前先查
   worktree 残留（wt528 撞历史号/wt529 残留树分支名恰为目标直接复用）。

### 三、坑与边界

- wt.py create 失败残留=目录已存在且带同名分支——`git worktree prune`+rm 后重建；认领前先查
  worktree 残留与 git 历史轮次（528 撞历史 t47d36 当场换 529）；
- 主树 detached HEAD 事故：wt.py create 失败链导致主树 HEAD 悬空、commit 落 detached——`git
  checkout develop`+`merge --ff-only <commit>` 接管补救（9738fac7）；
- v2p 手工构建三坑（cwd=仓库根/asm=target/v2asm.asm 相对 cwd/stdlib 在仓库根）+subprocess
  shell=True 单串（list 形态 cmd /c 双重转义）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-21 ✅ **533-a 生成方法调用侦查轮（零代码·改判设计型·已收工）**。

### 一、本轮做了什么（533-a·分支 `任务/深度机-533-生成方法调用拆分`）

1. 整读 生成方法调用 151 行=**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·5 层嵌套+跨层 `返回;` 终结+共享可变状态）——机械拆不可行（嵌套提取需外层条件转子函数守卫·跨层返回转译风险高）→**改判设计型重构候选池**（与解析类声明/解析选择语句同类）。
2. **D1 机械可拆红利收尽**：算术三后端族（518/520/530）+克隆节点修针（523）+生成复制展开（532）五函数全部出列——剩余大函数=设计型池（解析类声明 185/解析选择语句 136/生成方法调用 146）+src 层 5 例（避让家机中）。

### 二、接续点

1. **522+526 合批挂等三平台回签**；2. 513~517 挂等他家回签；3. 329-D12 双签齐待集成（集成后假红清零丁字账复验）；4. D1 设计型池=需独立设计轮（非机械）；5. B 区待用户裁决项维持。

### 三、坑与边界

1. 深度嵌套流程函数的拆分前置检查清单：①扁平分派 vs 嵌套流程 ②跨层 返回; 终结语义 ③共享可变状态面——任一不满足即登记设计型（不硬拆）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
