# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮做了什么（589-a 开发轮·005 甲案 v2 树同构+050 双测上架=全链销项·2026-09-22·分支 任务/家机-589-t81v2sync·wt588 复用树）

1. **v2 同构三层**：生成一元运算 取地址分支（&类变量→IR_加载(槽) T_对象）+生成声明 类分派/
   结构体分派补 指针层数==0 排除（`点* p2` 误入类对象/结构体路径·此前编译拒绝=纯新增零回归）。
2. **050 双编译对照上架**（501 号已被 499 链占用改 050）——宿主+v2p 双侧 42/42/99。
3. **005 销项**（迁 §4.2）：全链完成（588 宿主+589 v2+050+条文）；591=解锁面验证随 033 接力。

### 二、接续点

1. **591（033 接力）**：迭代器持容器指针解锁验证（499-a T81 阻塞项）+深度机 550-T94 阶段二收编确认；
2. 他机验收：影响面=v2 产物（取地址/类指针声明语义）——重点 050+自举链 78/79。

### 三、坑与边界

- **跨轮复用 worktree 必须先重编宿主再跑全量**（branch 切换后 target/cn.exe=旧源码产物·
  首轮 346/504 假红实锤——陈旧 exe 定性后再重跑=45 分钟学费）；
- v2 类模型要求显式构造器；用例号上架前查重（501 被占）；
- 其余沿用 588-a 交接。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-22 中午 🧪🛠 **服务轮+603 机制级修复轮（拉取+协议 v3 补验收+strict 双红当场修+采样补轮·免号+603）**。

### 一、本轮做了什么

1. **拉取+补验收（协议 v3·独立树 wtv598 全新构建）**：基线 d3737cdf→e1935ae4（期间 591-a/592-a/597/601 四链代码集成均无 linux 验证）——**验收全绿**=零警告构建＋单测 1383/1383（601 删 11 个 v1 组件单测）＋E2E **526=524/0/2 零失败**（2 跳=62/69）＋matrix 83/83；win 侧 050/524 两备案红在 linux 均绿=**win 单侧现象**（归属不变·见接续点）。期间 develop 高频前进（c35b527f→e1935ae4），验收 E2E 换基重跑一次。
2. **603 机制级修复轮（strict 双红·已集成 c5bf935d·双远程已推）**：验收前置 strict 抓出 develop 双红——①050 登记行（ec4c2064 补）被 591-a「净差异版」集成再丢失→复登记（**登记丢失族第二例·随证据备案**）；②601 裁决甲 9 例 v1 化石下线连带簿记=[2.1]/[附录A] 两正例引用+70~76 七豁免行清理+[2.1] 正例格入 EXCLUDED_CELLS（随裁决甲·v2 等价补位后应移除）——strict rc 1→0（178/178 指针有效+零孤儿）；integrate 合并结果全量门禁绿（E2E 526=524/0/2+单测 1383）。六要素=plans/025 §2.130。
3. **附带清偿**：看板 549/551 过时挂等行删除（`merge-base --is-ancestor` 核实均已是 develop 祖先）；M3 采样补轮（当日 06:01 后守护未跑·主树重建 target/cn+清 v2p 缓存后 `cnsmith_daemon.py --rounds 1`）=322 样本分歧 0 零新命中·指针新鲜度已核（stat target/cn 12:20 > HEAD 12:05）。
4. 本机 🧵 验收基线行→c5bf935d。

### 二、接续点

1. **524 win 单侧红**（551/D12 域·条件编译 win 面——linux 绿）待我机侦查修复轮；2. **050 win 单侧段错误 ICE**（P1·B-T81 域·家机立案——linux/arm64 绿）；3. **550 阶段二呈报**仍挂用户裁决；4. **001-001 架构轮**（撤守卫容器双模收敛·P0·可认领）；5. **integrate 写集分类缺陷**（@581 备案·可认领）。

### 三、坑与边界

1. **gate_lock 陈锁新形态**：TaskStop 杀 gate_lock 包装会留「死持锁者」锁目录（等待者按 4h 心跳接管太慢）——正确姿势=核实 info.json 持锁 PID 已死+零构建活动后 rm 锁目录（527-a 先例第二次实证）。
2. **登记丢失族第二例**：净差异版/squash 集成对 coverage_map 登记区行有系统性丢失风险（591-a 再丢 050）——各机集成后 strict 自检必须真跑。
3. 沿用：命令链 rc 硬拦截/回签前树真含写集核验/单跑定性假红/分支收工 check_handoff 必跑。

## 单位机 ARM64 节

**交接时间**：2026-09-21 560（F2-36b 首步=bench v2 口径+arm64 基线入库）·558/557/559 三链收工挂等三平台回签·M3 采样+T96 立案。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么

1. **329-D12 让位处置**：D12 主面让位 546；增量面拆 557；T50 已由 331-a 兑现。
2. **558 紧急修复**（develop arm64 红根治）：457-a 两处发射缺陷（emitPtrLoadStore 7 处丢 `[`+emitLoadStore 浮点 w9 截断）——522=515/5/2+78/79 自举链 PASS+反证双态。假绿教训入册（权重 9）。
3. **557 增量面**：T55 runner 隔离键+T54 宿主货舱 [特性]+用例 495/434/435——组合态 524=521/1/2·**隔离键自愈实证**（4 互踩假红消）。
4. **559 T96 三层根治**：语义类型写回 AST 通道+三元槽按语义类型+arm64 emitCopy 双半（三后端对齐）——组合态 523=516/5/2·500 号 O0/O3 双 PASS。
5. **560 F2-36b 首步**（bench v2 口径三平台改造）：范围修正（原测 v1 组件链已推倒=对象过时→v2 全树 fix_p≡fix_s 固定点口径）+--platform 三平台工具链分派（win=ml64/link 保留·linux=as/g++）。**arm64 基线入库**：M1=22295.7ms（C++ 宿主 Debug）/M2=42608.4ms（CN 版·比值 1.91x vs C++ Debug）/**M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建快 24%**/M6 fix_p≡fix_s 逐字节+绑定自检 ✓。报告=target/bench/性能基线报告.md（产物不入库·数值誊 021）。
6. **M3 采样**：622 样本（558 修复态）+T96 立案。**呈报待批**：推断面完整化（i1/浮点形态·184 expected 契约·320-a T47 同模式）。

### 二、卡点与下一步

1. **三链挂等三平台 try-build 回签**（558→557→559 集成序·组合态门禁已预跑）；他机回签后依次 integrate。
2. **560 集成**：平台无关 python 写集=快速门禁·免 try-build（§8.3）——可在三链集成后直接走 integrate。
3. **下一轮候选**：T33 v2 窄宽读侧（施工图=025 §3.23）/D42甲波3（stdlib 源码加载）/F2-36b 续（win 复验归家机·x64l 基线归深度机）。

### 三、踩坑新增

1. **cn build linux 产物链**：--output X 产 X.s+X.o+ELF 三件——X.o=锚定链借链的「容器符号提供者」（fix_p 缺跨模块符号定义·grep 实锤）；bench M4 借链错用 fix_p 自汇编=undefined 符号。
2. **ls | head 截断**导致误判「文件消失」——长目录列表用 grep 定位。
3. **语义注记文本=源码类型名**（「整128」）非 IR 码（i128）——跨层判定必须 mapType 归一后比对。
