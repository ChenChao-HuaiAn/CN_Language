链式方法调用与类值链式访问宿主缺陷根治（exprSrcType 补全）

两个叠加缺陷（2026-08-25，用户批准执行）：
① 类值链式字段访问：结果<类>.值.字段（查.值.编号）生成 调用 0（不读字段）——
   exprSrcType 的 MemberExpr 分支不识别 结果/可选 成员映射（值 不是合成结构体
   直接字段，在联合体）-> exprSrcType(查.值) 返回空 -> handleClassMemberExpr
   未识别类字段访问 -> 走 findStruct(资源=类)=null 防御返回 0（117 开发实测打印 0）。
② 链式方法调用：表.元素(0).设置(1,100) 被降级为通用间接调用（this=0 崩溃
   0xC0000005）——exprSrcType 的 CallExpr 分支只处理 IdentifierExpr 被调函数
   （安全除法(a,b)），不处理 MemberExpr 被调方法（表.元素(0)）-> 对象类型推导
   失败 -> handleClassCallExpr 未识别类方法调用。

修复（ir_oop.cpp exprSrcType）：
- MemberExpr 分支补 结果/可选 成员映射（.值 -> T、.错误 -> E、.正常/.有值 -> 布尔）。
- CallExpr 分支补 对象.方法() 返回类型解析（lookupClassMember 沿继承链）。

新增 E2E 118_宿主缺陷_链式方法调用与类值链式访问（类值链式 5、
元素(0).设置 777、读取(0).值.编号 11/22）。60/114 回归 PASS。
全量 E2E 118/120（78/79 已知遗留内存超限）、单测 1196/1196。
plans/014 已更新（三 未完善 移除链式方法调用，宿主缺陷已全部根治）。
