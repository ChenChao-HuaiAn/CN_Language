# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十三轮收官（深度系统 x86_64）——**plans/017 呈报①方案A 第五轮：第 16 枚【根因级】根治（v2 指针参数三重缺陷）+ 九探针三方对拍 8/9 + 第 17 枚立案**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、第 16 枚根因定位（插桩对拍三轮夹逼 + watchpoint 收口）
1. **方法**：[d16] 打印注入 IR容器/IR签名 → v2p 与 v2self 跑同一插桩源码逐行 diff stderr。三轮夹逼：pre1/e1 之间 → 替换函数全程正常（e1a/e1b/e1c/r 全干净）→ 崩在返回后查方法签名读 pre2；gdb watchpoint 抓写者 `mov %r11,(%r10)`，r10=**实参们槽地址+7**。
2. **根因两层**：
   - **毒地址**：`出.参数串=`（出=方法签名*）左值发射 ①指针参数误走 IR_取地址（少一层解引用）②成员偏移 -1 未校验直发 IR_字段地址.附加=-1 → 目标=实参们槽+7 → 8 字节写把返回值 LE 首字节 0xd0 叠入句柄槽最高字节 → 句柄 0xd00000000177eca0 解引用崩。
   - **参数槽分配**：方法签名* 被当结构体值参数（32B 4 槽+prologue 解引用拷贝）→ 出.基槽=*签 首 8 字节（=签.参数串 值 0）而非签地址。

### 二、修复五处（方案A，Rust 准绳：单一语义+fail fast+零开销）
1. 语法分析.cn 三处：变量结构体表 登记放宽 `附加<=256`（指针形态登记→偏移真值标注）。
2. IR生成.cn：指针参数槽们 机制（映射+槽是指针参数+清空+登记）。
3. 参数收集：指针参数单槽直存（拷贝字节=0、槽数=1）。
4. 生成左值地址：三态基址（引用槽→栈顶/指针槽→IR_加载/其余→取地址）+偏移分桶兜底+毒值 IR层诊断拒绝。
5. 右值成员访问：指针参数与引用参数同款+同款兜底拒绝。
- Rust 对齐：p.字段≡(*p).字段（v2.1 裁决 IR 层落地）；偏移未知=编译期错误绝不发射毒指令；单槽直存零开销。

### 三、验证（全绿）
- 毒指令（`add $0xffffffffffffffff,%r10`）全文 60→0 清零。
- 插桩对拍 [d16] 序列 v2p vs v2self 56 行逐行一致。
- **v2self 编译 f2.cn 全链 exit=0**（修复前必崩 139）——第 16 枚闭环。
- **九探针三方对拍**（host/v2p/v2self 三方编译→运行）：min1/f2/f3/alias/vieww/retown/elecalc/freg **8 项 PASS**——**v2self 首次直编容器系探针并运行成功**。
- min1 产物 vs v2p 逐字节一致保持。
- 门禁：构建零警告+单测 1240/1240+E2E 157 用例 130 过/0 败/27 平台跳过。

## 第 17 枚立案（v2self 第 9 层，下轮主线）
mapref2（`灌(映射<整64,整64>& 表,...)`+`表.设置/表.获取`）v2p 产物正常，**v2self 产物运行时 链查找 this=0（运行时错误3）**。IR dump 对拍实证：v2p 发 `op=40→op=44`（引用双层解引用），v2self **漏发 op=44**。解析一致（103 节点）、池一致、前 17 条 IR 一致——`槽是引用` 判定 v2self 运行时 miss，嫌疑=编译器内部 引用参数槽们 映射运行时状态异常（第 16 枚同族内存问题另一形态）。**下轮**：v2 源码插桩 dump 引用参数槽们 登记序/槽号/读取序定位 miss 根因；随后呈报③方案A（IR 名带参数签名）与 T3/T4。

## 灰色点披露
- 九探针 mapref2 一项 FAIL（第 17 枚，见上）——其余 8 项三方 rc+输出全一致。
- IR容器.cn 单组件子链冒烟 exit=1 系既有状态（上轮已披露，主链全绿）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径）。
- 三方对拍配方：v2 产物 .o + 宿主编译同源 .o（objcopy 改名 cn_main=cn_main_h 补容器实现，muldefs 先者胜）+ v2self_rt/*.o；脚本痕迹在 /tmp/v2dbg/tri/（不入库）。
- 本轮 v2self/v2p 干净产物已重建在 target/dbg/（v2p 可执行 18:03 后构建，含修复）。

## 踩过的坑（本轮，已入 lessons 权重 10）
- **毒指令模式审计**：`add $0xffffffffffffffff,%r10`=IR_字段地址.附加=-1 物化形态，修复后必须全文 grep 清零（60→0）。
- **插桩对拍法**：编译器分叉缺陷首选，v2p/v2self 同源码同输入 diff stderr，比 gdb 单打快一个量级。
- **watchpoint 值形态判读**：污染=旧低位+新单字节=1 字节写；hw watchpoint 报告 PC=写指令下一条，SIGSEGV 混入伪触发（本轮 malloc+39 误报曾误导）。
- **重建链原子性**：v2p_rt.o 重编必须重链 v2p 可执行——漏链=「修复无效」假象（浪费一次全链对拍）。
- **指针层信息传递**：类型节点附加存指针层，权威表存类型文本（无星）会丢——跨表传递指针语义必须显式（指针参数槽们）。
