# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十轮（**深度机 X64L**，linux-x86_64 Deepin 25）——无符号语义轮：**46-f 用户裁决方案A 实施，v2 全链对齐 Rust u32/u64 硬件无符号语义（T_正32=70/T_正64=71 独立类型码+三后端 div/shr/setb 族分派+正32 存储截断+零扩展读回）+ 宿主侧假阳性定性修正（真分叉探针实证健全，零改动）**。全量门禁全绿 + E2E 191/191_v2 双通道 PASS + v2self 固定点复锚（**235667 行 = X64L 新锚定**，上轮 232743+2924）+ 组件对拍 29/29 + 运行级 3/3。**下一跨机轮主线仍是单位机 arm64 复锚（302433 行口径因两轮 v2 变更预期增长）。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **宿主侧定性修正（实施前复核，零改动）**：第四十九轮呈报「宿主无符号比较静默错误」实为**矩阵探针断言方向读反的假阳性**——`如果 (大 > 100) { 返回 11; }` rc=11 是进了分支=比较为真=行为正确。真分叉探针（u_cmp2：`大 < 2^63-1` 有符号/无符号解释结果相异+u32 max 边界+2^63/3 全精度除法）实证宿主 emitCompare setb/seta 族（修复4）、div（修复3）、shr、u32→u64 零扩展 Cast 全部健全，**宿主零源码改动**。教训入 lessons 权重 7（正/负断言方向显式核对+鉴别形态必须两侧解释相异）。
2. **v2 侧根治（方案 A 全链）**：
   - IR常量.cn：T_正32=70 / T_正64=71（尾部空区顺延，零同值防御同值常量缺陷）。
   - 类型传播：类型码() 归一拆分（正32/正64 出 T_整64）+ 类型宽度码 拆分（正32 字段 4 字节）+ 生成表达式 二元指令类型字段=操作数登记类型（原恒 T_整64）。
   - 三后端按码分派：X64L/win——div（原 idiv）、shr（原 sar）、setb/seta/setbe/setae（原 setl 族）；ARM64——udiv/lsr/lo·hi·ls·hs。
   - 正32存储截断 助手（生成声明.cn）：目标=正32 时存储前 IR_位与 0xFFFFFFFF（wrapping mod 2^32 对齐宿主 4 字节槽）；接入变量声明+赋值两存储点。
   - 三后端访存/类型转换 T_正32 并入 32 位分支（零扩展读回；原 movsxd/sxtw 符号扩展会把 4000000000 读成负）。
3. **E2E 191/191_v2 双通道 PASS**：宿主 stdout 六锚（2^63/3 全精度、2^63>>1、mod 2^32 截断、三比较边界）/ v2 rc=127 七面校验和。注册于 run_e2e.py v2闭环用例们。
4. **全量门禁**：清场重建 GCC 零警告 rc=0 + 单测 **1251/1251** + 全量 E2E **197 用例 193 过/0 败/4 跳**（跳=62/69/78/79 既有平台限制；197=上轮 195+2）。
5. **v2self 锚定链（X64L 复锚）**：fix_p.s **235667 行**（232743+2924）→ 链接 cn_self50 → 绑定自检 cn_main=0x4d701c ∈ [0x402586,0x4daa4e) → fix_s.s 235667 行 → **固定点逐字节一致** + 运行级三用例 3/3 + 组件对拍 29/29。
6. 文档：plans/014 状态表第五十轮行+46-f 行定性修正与根治记录+更新日志+lessons 一条（权重7）+本 HANDOFF。

## 二、下一轮任务（按序）

1. **【单位机】arm64 跨机轮**：`git pull` 本提交 → 全量门禁复验 → E2E 189/189_v2/190/190_v2/191/191_v2 六用例 arm64 复跑（**191_v2 首验 ARM64 后端 udiv/lsr/lo 族无符号发射**——本轮静态论证，动态验证挂此）→ v2self 固定点复锚（**arm64 口径 302433 行因第四十九/五十轮 v2 变更预期增长**）+绑定自检+组件对拍 29/29+运行级三用例。win 侧同理（235968 行待重锚；191_v2 win 后端 setb 族首验同挂）。
2. **观察项延续**：混合符号比较严格化（u32 vs i64 变量——Rust 对齐=拒绝，涉规范变更须用户审批，随类型系统严格轮）；联合体构造字面量双侧锚定（随构造字面量覆盖轮）；标量推断 IR 类型面（布尔打印分叉 184 expected 待批/调用返回大值截断）。

## 三、验证链（下轮接手可复跑，脚本留 target/p50x64l/ 不入库）

```bash
uname / 系统确认                # 深度机 Deepin 25 x86_64（本行口径）
git pull origin develop         # 拉取本提交（第五十轮）
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j8   # 清场重建（零警告 rc=0）
./target/cn_unit_tests          # 单测 1251/1251（产物在 target/ 根，非 target/build/）
python3 tests/e2e/run_e2e.py --cn target/cn > target/e2e_full.log 2>&1; echo rc=$?    # 全量 E2E（197 用例）
# v2self 锚定链（E2E 完成后跑——audit2/v2p_linuxx64 由 E2E 步骤1 产出）：
bash target/p50x64l/run_chain50.sh     # rt48 现编+fix_p+链接 cn_self50+绑定自检+fix_s+固定点
python3 target/p50x64l/map_check50.py target/p50x64l/cn_self50.map   # 绑定自检单独复跑
bash target/p50x64l/runlevel50.sh      # 运行级三用例 hello/119/173
bash target/p50x64l/cmp29.sh           # 组件对拍 29/29
```

注：v2p_linuxx64(.o)=E2E 产出的第五十轮 v2 全树编译器（新鲜度以 E2E 时段时间戳核对）；运行时源码 src/runtime 零改动，rt48 objs 现编走清场纪律。

## 四、本轮踩坑（已入 lessons）

- **正/负断言方向读反=假阳性定性（权重7）**：`如果 (大 > 100) { 返回 11; }` 的 rc=11 是正断言触发（比较为真=正确），第四十九轮误读为「判假缺陷」并写入呈报——第五十轮实施前复核识破，避免了对健全宿主的无用改造。预防：探针每个返回码的触发条件显式注释「何时返回」；定性缺陷前必须构造两侧解释结果相异的鉴别形态（同真/同假形态零鉴别力）；呈报证据链要可独立复核。
- 承前轮纪律全部兑现：v2 源码改动先跑「v2p 编译 v2 全树 rc=0」冒烟（本轮 01:07 首编 rc=0 一次过）；rm target/v2asm.s 防旧产物续用；逐命令独立核对 rc。

## 五、诚实边界

- 本轮源码改动面：v2 七文件（IR/IR常量.cn、IR/IR容器.cn、IR/IR布局.cn、IR/生成表达式.cn、IR/生成声明.cn、IR/生成语句.cn、代码生成三后端三文件）+tests/e2e/run_e2e.py（用例注册）；**宿主 C++ 零改动**（定性修正）；全部门禁与锚定链在本机（X64L）实物复验通过。
- **win/ARM64 后端无符号发射=静态论证**（汇编形态与宿主既有分派对称），动态验证=跨机轮（191_v2 win/arm64 首验挂账）。
- **arm64 侧复验/固定点复锚（302433 行口径预期增长）= 单位机下一跨机轮**；win 侧（235968 行）同理待重锚。
- 混合符号比较维持宽松宽化现状（严格化须规范审批）；组件对拍基线 29；探针与脚本留 target/p50x64l/ 不入库。
