# HANDOFF 交接文档

**交接时间**: 2026-09-09 第四十六轮**收口**（单位机 麒麟 ARM64 开工 WIP → 深度机 linux-x86_64 续作完成）
**轮任务**: 第四十五轮挂账回归特性开发 = 观察项①（v2 比较层拆两级）+ 观察项③（v2 构造字面量赋值位）；途中按缺陷零容忍立案根治**宿主构造字面量三缺陷 D1/D2/D3**（第四十三轮呈报「宿主表达式位已支持」与事实不符，在此二次更正）。WIP 期唯一已知红 E2E 186 已于本轮**双根因根治**并全链收口。

**✅ 当前状态：第四十六轮 X64L 侧收口完成。** 全量门禁绿（清场重建 GCC 零警告+单测 1251/1251+E2E 189 用例 185 过/0 败/4 跳）、v2self 固定点 fix_p≡fix_s（227614 行 X64L 锚定）、组件对拍 29/29 逐字节归零、运行级三用例全对齐。**诚实边界：win 侧门禁（ci.ps1）与 arm64 侧复验/固定点复锚 = 下一跨机轮。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **续作 9a9425d（第四十六轮 WIP，arm64 实测）**：宿主 D1/D2/D3 根治+v2 观察项①（比较层拆两级 6=相等/7=比较/8=移位/9=加减/10=乘除/原子=11）+③主体（解析构造字段赋值们 单一助手+Form A/B 钩子+嵌套下钻展平）+E2E 183~187 建测注册；唯一已知红 = 186（rc=68≠84，arm64 轮初判根因「取成员宽度码 成员对象无分支」）。
2. **深度机续作发现 186 是双根因**：
   - **实施初判修复后 186 仍 rc=68**——初判只是表象层。
   - **最小探针二分**：t1 Form A（rc=16 ✓）/t2 手写链读（rc=1=「成员字段偏移未知」可见失败=观察项 46-b，**非本轮修复面**）/t3 Form B（全 4 字节 ✓）→ asm 锁定：嵌套声明位下钻 `r.左上.x=1/2` 4 字节 ✓ 而 `r.右下.x=3/4` 8 字节 ✗（同构链一过一败）。
   - **真根因（解析层）**：解析构造字段赋值们 嵌套递归返回后 链名们/链偏移们 **未弹出本字段**——第二嵌套字段起合成**残链 AST**（`r.左上.右下.x`）；地址偏移 0+8 恰逢前层偏移=0 **数值巧合掩盖**，宽度按残链查 点.右下 miss 落 T_整64 → 8 字节越界写踩邻（arm64 轮探针 F 单嵌套字段未暴露，186 双嵌套暴露）。**修复=递归返回后弹出链两元素**。
   - **表根因（IR 层）**：取成员宽度码 对象只认 标识符/调用 两形态，合法成员链写也落 T_整64。**修复=新增 解析成员链结构体ID**（标识符/成员递归/调用三形态统一；对旧两形态逐点等价）+主干替换。
   - 两处缺一不可；修复后 **186 rc=84** ✓+错误 8 字节写清零。
3. **全量门禁（清场重建，linux-x86_64）**：GCC 零警告 rc=0+单测 **1251/1251**+全量 E2E **189 用例 185 过/0 败/4 跳**（跳=62/69/78/79 既有平台限制；新增 183~187 全过零回归）。
4. **v2self 锚定链（X64L 侧）**：v2p 编译 v2 自身→fix_p.s（**227614 行**）→链接 cn_self46（cn_self46.o 在前+v2p_linuxx64.o 借链+rt objs 现编+`-Wl,-z,muldefs`）→**绑定自检** cn_main=0x4cf96d ∈ cn_self46.o .text [0x402586,0x4D3363)（`-Wl,-Map` 决定性证据）→cn_self46 编译 v2 自身→fix_s.s→**固定点 fix_p≡fix_s 裸逐字节一致**（227614 行=X64L 新锚定；上轮 224118+3496=两修复+①③新特性净增量）→运行级 hello rc=0/119 rc=14/173 rc=173 全对齐。
5. **组件对拍 29/29 全量逐字节归零**（v2p vs cn_self46，零 DIFF）。
6. **文档收口**：plans/014 第四十六轮状态行+观察项四项立案（三表 46-a~d）+更新日志+plans/018 工作流1 §5.9.1/§5.9.7 复选框核对勾选（条文已在 plans/001）。

## 二、下一跨机轮（按序）

1. **家机 win-x64**：`git pull` 本提交 → win 门禁 ci.ps1（构建零警告+单测 1251+全量 E2E）=**宿主 D1/D2/D3 与 v2 ①③+186 双修复的 win 侧首验** → v2self win 侧锚定（v2p(win) 编 v2 自身→fix_p→cn_self 链接 `/FORCE:MULTIPLE`→fix_s→固定点逐字节，**win 侧口径首立，行数不跨后端比较**；绑定自检用 map 查 cn_main 落点）→组件对拍 29/29。
2. **单位机 arm64**：全量门禁复验+E2E 183~187 复跑+v2self 固定点复锚（302433 行口径预期增长）+组件对拍 29/29。
3. **plans/014 观察项四项（本轮立案，未根治勿忘）**：
   - **46-a 成员结构体作值读写段错误**：`r.左上 = a` / `点 t = r.左上` 零构造字面量参与即 rc=139（探针 v2_nE.cn 实锤）——独立缺陷轮立案，优先级最高（内存安全面）；
   - **46-b 手写链式成员读**：`a.b.c` 读解析层偏移不可标注→IR 层可见失败（非静默）；写宽度已由本轮 解析成员链结构体ID 可解析，读路径偏移标注随语法覆盖轮；
   - **46-c `变量` 关键字分歧**：v2 词法表有（词法分析.cn:78）解析层无声明分支，宿主支持；
   - **46-d 全局位构造** `点 g = 点{...}`：双侧一致拒绝，低优先。
4. 观察项延续（非阻塞）：v2 无符号类型面缺 正32/正64（随类型系统轮）。

## 三、验证链（下轮接手可复跑）

```bash
uname -m   # x86_64=深度机（本行口径）/ aarch64=单位机 / win=家机
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j8   # 零警告
./target/cn_unit_tests 2>&1 | tail -1                                    # 1251/1251
python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn        # 189 用例 185 过/0 败/4 跳
# v2self 锚定链（rt objs 现编；186 双修复后 X64L 固定点=227614 行）：
mkdir -p target/rt46 && for m in io_api intern_api runtime string_api i128_api math_api input_api file_api time_api system_api; do g++ -c -std=c++17 -fno-exceptions -fno-rtti -DCNRT_LINUX_MAIN -Isrc src/runtime/$m.cpp -o target/rt46/$m.o & done; wait
./target/audit2/v2p_linuxx64 CN语言编译器v2/主.cn linux-x86_64 && cp target/v2asm.s target/fix_p.s
as target/fix_p.s -o target/cn_self46.o && g++ -no-pie target/cn_self46.o target/audit2/v2p_linuxx64.o target/rt46/*.o -o target/cn_self46 -Wl,-z,muldefs -Wl,-Map=/tmp/m.map
# 绑定自检：/tmp/m.map 查 cn_main 地址 ∈ cn_self46.o .text 区间（本轮 0x4cf96d ∈ [0x402586,0x4D3363)）
./target/cn_self46 CN语言编译器v2/主.cn linux-x86_64 && cp target/v2asm.s target/fix_s.s
cmp target/fix_p.s target/fix_s.s    # 逐字节一致=固定点
# 组件对拍 29：for f in $(find CN语言编译器v2 -name "*.cn" ! -name "主.cn") 两编译器各产 asm 后 cmp
# 运行级：cn_self46 编 tests/e2e/01_hello/hello.cn（注意文件名是 hello.cn 非 主.cn）→as→g++ 链接→rc=0；119_v2_多文件链接闭环=14；173_v2_目录包根撞名成员=173
```

## 四、本轮踩坑（防再犯）

1. **「&& 链+管道 tail」掩盖真实 rc 再演**（lessons 第四十三轮权重7）：探针批量跑 `v2p 编译 && as ... && 运行; echo rc=$?`——v2p 编译失败时 `&&` 短路，echo 取到的是**编译 rc**，误读成「运行 rc=1」白排查一轮。**每步 rc 独立判定并留 log**；产物消费前 `ls` 核对 v2asm.s 新鲜度（编译失败不清旧产物=消费陈旧产物假象）。
2. **探针设计必须只走「已立案可用通道」**：验证成员链写不可用 `r.左上.x` 手写链读（46-b 可见失败直接编译期拦截）——运行级唯一通道=成员实参（`求和(r.左上)`，E2E 186 据此设计）。
3. **「修复后仍红」=初判根因非唯一根因的强信号**：按原方案修复后 rc 不变时，立刻做**最小探针二分**（Form A/Form B/嵌套声明位 各自单测）而非加重插桩——本轮 t1/t3 全绿+186 红直接把问题收敛到「嵌套下钻+多字段」组合。
4. **地址对≠链对**：残链 `r.左上.右下.x` 地址偏移 0+8+0 与正确链 `r.右下.x` 8+0 数值相同（前层偏移=0 巧合）——asm 对拍只看写地址会漏判，必须核对写**宽度**与链的**节点结构**。

## 五、诚实边界

- 本提交 = 第四十六轮收口：宿主+v2 源码改动（解析语句.cn/IR生成.cn + arm64 轮的宿主三文件与解析表达式.cn/解析语句.cn）均已 linux-x86_64 实测；win/arm64 侧=下一跨机轮（第「二」节清单）。
- 组件对拍基线 29（6 包根+23 成员；历史口径 30→29 系第四十一轮删除 abi辅助.cn，find 实数核对）。
- 固定点行数锚定分后端口径：X64L=224118 行（第四十五轮）→**227614 行（本轮，含 186 双修复+①③新特性）**/ arm64=302433 行（第四十三轮，待复锚）——行数比较只在同后端内有效。
- 探针产物 /tmp/probe46x/、target/probe46x、cn_self46、rt46、fix_p.s/fix_s.s 不入库（target/ 已 gitignore）。
