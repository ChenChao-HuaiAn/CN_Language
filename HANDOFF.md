# HANDOFF 交接文档

**交接时间**: 2026-09-05 第十五轮（家机 win-x64）——**呈报①②方案A实施轮：cn check 假绿根治 + 常量诡异触发归因反转根治**。用户裁决两项均方案 A（命令行入口全量检查 / 专项深挖根治）。

## 本轮已完成（全部有实证）

### 一、呈报②A：cn check 假绿根治（簇⑥，E2E 151 红→绿闭环）
- **根因**：`mergeModules` 入口判定=`模块名=="主"||单模块特判`——命令行入口为非主.cn 且含导入时被误判为「被导入模块」，私有函数体被可见性过滤静默丢弃=「检查通过」假绿。E2E 26（单文件形态）与簇⑥（组件自检形态）**同根两发作**，第一次修法（单模块特判）是症状补丁。
- **根治**：`ModuleUnit::isEntryUnit` 命令行入口标记（[module.hpp](src/cn_compiler/module/module.hpp)——loadModuleTree 根调用 `isRootCall=true` 写入，递归依赖恒 false）+ `mergeModules` 据此判定（[module.cpp](src/cn_compiler/module/module.cpp)）——特判与文件名判定一并归一删除。`isEntryModule` 保留为规范08-四 文件名约定查询（非入口语义依据，单测覆盖不变）。
- **E2E**：run_e2e.py 新增 `期望check失败.txt` 通道（与 期望编译失败.txt 同构：首行被检文件名+次行期望诊断子串）+ 用例 **151_check非入口全量检查**（非主.cn+导入本地依赖+私有函数含未声明标识符）——修复前红（假绿被抓）、修复后绿。
- **测试面披露**：test_module.cpp `makeUnit` 加一行 `isEntryUnit=(moduleName=="主")`（单测直构图无 loadModuleTree，按 主.cn 约定模拟命令行入口——**判定源等价迁移，断言零变化**）。

### 二、呈报①A：常量诡异触发——**归因反转 + 根治**（六链冒烟全绿）
- **归因反转（重大）**：原定性「IR常量.cn 新增任意常量对使三链冒烟多 1 条诊断」**完全错误**。现场 DBG 三层下钻（解析方法对象类型 miss → 对象=成员节点 值正确 → 基类型 函数IR 解析成功 ID=281 → **查结构体大小=0**）+ 结构体登记流水实证（冒烟链全程仅登记 Token/方法签名，函数IR/IR指令 从未登记）锁定真根因：
- **真根因**：**IR容器.cn 反向引用 IR生成.cn 的 IR指令/函数IR 结构体**（IR生成 导入 IR容器=反向不可导；主链靠多文件合并全局可见掩盖；冒烟子链——语法分析/语义分析/IR容器 入口，链上无 IR生成.cn——登记源缺失 → `函数信息.容器字段局部们.元素(i)` 方法对象解析查结构体大小 miss →「方法调用的对象类型未知: 元素」误诊）。
- **潜伏实证**：v2@13 基线复核（checkout 9efb5d8 重编 v2p）**同样红**——缺陷自 IR容器.cn 诞生（P7b 2026-09-01）起长期潜伏：历史冒烟只统计 sem5，ir2 计数纪律第十三轮才建立，第一次照见它却被归因给当轮新增的常量对。**第十四轮消融「删常量对不触发」系假绿**（未删旧 asm/检查面不全）。
- **根治**：IR指令/函数IR 结构体定义自 IR生成.cn **下沉 IR容器.cn**（引用方 IR生成/代码生成族/主 的导入链恒含 IR容器——登记源在任意子链可达；程序IR 留 IR生成.cn 因其引用方链恒含 IR生成）。
- **原子化消融证伪**：修复版+T_测试甲=98/乙=99 常量对 → 四链 0 诊断（快照→单变量→回滚）——「常量对是开关」假设正式证伪。
- **连带根治**：miss 根治后语义分析链跑得更远，暴露**结构体初始化第三形态**（`符号 找到 = 查.值;` 结果<T>.值 整体接收=成员表达式）不在白名单——扩 节点_成员 形态（生成表达式 成员分支压字段地址→IR_结构体拷贝 源）。
- **验证**：六链冒烟全绿（语法分析/语义分析/IR容器/IR生成/IR签名/词法分析 全部 exit=0+ir2=0+asm 产出+**ml64 全过**；IR生成链 asm 6.36MB 与十三轮基线一致——**asm 断供恢复**）。

### 三、实验方法论踩坑（入 lessons 详录）
- `公开:` 标签**粘性作用域**：复现簇⑥ 用例时把「私有」函数写在 公开: 段后=实为公开，假绿不触发——对照实验自相矛盾的根源。可见性类实验先实测前提。
- 冒烟/消融的「绿」必须验证**产物新鲜度**（删旧 asm 再跑）——第十四轮假绿复现的教训。

## 下一步
1. **代码生成.cn/代码生成共用.cn/代码生成ARM64.cn/主.cn 冒烟推进**（v2 自举最后四个组件）——本轮六链全绿后，冒烟通道已完全可用；「v2 编译 v2 自身」对拍（79 同款）是终点。
2. **linux-arm64 复验轮**（单位机）：147~151 五用例 linux 侧对齐+本轮变更全量 ARM64 复验（isEntryUnit 为宿主层变更双平台同码；结构体下沉/第三形态为 v2 层变更走 v2p 双后端）。
3. **B1a 契约分叉**（stdlib 包装 vs 内置直调）——随 stdlib 并入专项收口（既有裁决不变）。

## 灰色点披露（更新）
- ~~呈报①常量诡异触发~~（本轮根治：归因反转+结构体下沉）；~~呈报②check 假绿~~（本轮根治：isEntryUnit）；~~结构体初始化第三形态~~（本轮连带根治）。
- 78/79 v1 链内存失控——已知遗留勿翻案。
- v2 组件拆分（规则3 单一职责，IR生成.cn 超标）：**方案A——自举闭环后统一拆分**（用户批准，不变）。

## 踩过的坑（本轮，全部入 lessons 详录）
- **「改 A 出 B」的第一假设应是「B 本来就坏只是检查面没照见」**，而非「A 触发 B」——本轮 ir2 纪律照见的老缺陷被归因给新常量。
- **消融的「不触发/绿」须先验证产物新鲜度**（删旧产物再跑）——陈旧 asm 存在性检查=假绿。
- **入口/身份判定以「来源事实」为准（命令行显式指定），不以「命名约定」为准**（文件名==主）——同根两发作的根源。
- **可见性标签粘性**（公开: 段后的函数=公开）——实验前提先实测。
- **stash 栈与 checkout 目录回退交错后必须实质核对基线**（grep 特性标记）——本轮三处修复曾做在 v2@13 旧基线上（第十四轮特性丢失），靠 T_正8 缺失当场识破、停门禁重做（错误基线上的六链绿/门禁结果全部作废重跑）。

## 门禁状态（第十五轮最终，家机 win-x64）
- 构建零警告（MSVC /W4 /WX）+ 单测 **1223/1223** + E2E 全量 **153 用例 151 过/2 败**（78=4657MB、79=4134MB 已知 OOM 家族与基线一致零回归；新增 151_check非入口全量检查 绿）。本轮曾因基线事故（见坑④）作废一轮门禁，最终数字为正确基线上重跑。
- **六链冒烟全绿**：语法分析/语义分析/IR容器/IR生成/IR签名/词法分析 全部 exit=0+ir2=0+asm 产出+ml64 通过（IR生成链 6.59MB 含第十四轮特性=基线正确佐证；第十四轮「冒烟回归异常」条目就此关闭）。
