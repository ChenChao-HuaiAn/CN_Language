# HANDOFF 交接文档

**交接时间**: 2026-09-05 第十八轮（深度系统 x86_64 本机）——**v2p 本机冒烟全 12 组件完整编译 + linux_x64 参数拷贝临时寄存器缺陷根治（方案A 用户裁决）**。

## 本轮已完成（全部有实证）

### 一、拉取 7650713（第十七轮家机呈报复核轮）本机门禁复验
构建零警告 + 单测 1238/1238 + E2E 154 用例 127 过/0 败/27 跳过——与第十六轮基线完全一致，第十七轮删除性变更在 linux-x86_64 侧零回归实证。

### 二、里程碑：v2 自举编译器全 12 组件首次完整编译（本机 linux-x86_64）
- 宿主一次编译 v2 全链（主.cn 导入 12 组件 8441 行）构建 v2p 成功——linux-x86_64 后端扛住自举编译器规模。
- **v2p 冒烟最后四组件全绿**（HANDOFF 下一步② 完成）：代码生成共用（asm 365641 行）/ 代码生成ARM64（380544 行）/ 代码生成（381453 行）/ **主（441591 行=v2 编译 v2 自身完整产物）**——全部 exit=0+语义 0 错+asm 产出。
- 主.cn 缺口 `系统::参数个数/系统::参数` 未注册当轮收口：IR容器.cn 内置符号表+返回类型表按第五波限定名模式扩两名（`__cn_argc`/`__cn_argv`，宿主 ir_call.cpp/semantic.cpp regSysFn 对齐）——语义/IR 层零改动。

### 三、宿主缺陷根治：emitParamSetup 拷贝临时寄存器 r9 冲突（呈报后用户裁决方案A）
- **现象**：v2p 冒烟 词法分析.cn 段错误 exit 139；同源码 win/arm64 产物六链全绿。
- **根因**：`emitParamSetup` 结构体按值/i128 参数拷贝用 r9 当数据临时——r9 是 SysV 第 6 整型参数寄存器；参数在前+整型参数满 6 时第 6 参数被拷贝残留覆盖（v2「生成如果」：指令引用=参数6 被覆盖为节点池容量 41，解引用崩）。gdb 五层下钻闭环。
- **三后端对照**：win（rsi/rdi/rcx rep movsb，参数寄存器 4 个）与 ARM64（x10/x11/x12）结构性安全——仅 linux_x64 冲突。
- **修复**：三处拷贝数据临时 r9→r11（prologue 阶段 r11 无职责；与 ARM64 同构）。
- **测试**：E2E 新增 **153_linuxx64_参数拷贝临时寄存器**（两形态×满 6 参数位）**红→绿→红→绿闭环**（修复前错值 706549=拷贝残留）+ 单测 StructParamCopyMustNotClobberR9。

## 门禁状态（第十八轮最终，深度系统 x86_64）
构建零警告 + 单测 **1239/1239**（+1）+ E2E 全量 **155 用例 128 过/0 败/27 跳过**（新增 153 PASS）。

## 下一步
1. **v2p 产物汇编验证（跨机）**：主.cn asm 441591 行（win-x64 MASM）家机 ml64 汇编 + 链接运行——「v2 编译 v2 自身」产物级对拍；ARM64 单位机跑 v2p（linux-arm64 目标）同验。
2. **v2 linux-x86_64 代码生成专项立项**（代码生成X64Linux.cn）——本机 v2 闭环 23 用例解锁的钥匙，需专项立项呈报待批（既有裁决方向不变）。
3. **B1a 契约分叉收口、v2 组件拆分**（既有裁决不变：拆分=自举闭环后统一拆分）。
4. E2E 153 在 win/arm64 侧复验一轮（纯语义用例，预期三平台一致绿）。

## 灰色点披露
- 78/79 v1 链内存失控——已知遗留勿翻案。
- v2p 冒烟的 asm 产出仅验证「产出存在+行数」，MASM/arm64 汇编通过需跨机（本机无 ml64/aarch64 工具链）。
- 递归同函数多次大返回调用共享同一 retbuf 帧区——已知限制（顺序 IR 下安全）。
- 寄存器分配器（regalloc）linux-x86_64 未接入（沿 arm64 阶段C 决策默认关）。

## 踩过的坑（本轮，全入 lessons）
- **手写 ABI 实现的拷贝/中转临时寄存器必须取「参数寄存器集合之外」的空闲寄存器，且按阶段核对职责**——r9 在 win 后端不在参数寄存器集合（4 个）内，直译到 SysV（6 个）后落入集合；临时寄存器安全性是「阶段」属性（prologue 阶段 r11 空闲≠执行阶段空闲）。
- **触发条件多重合取的缺陷常规 E2E 抓不住，首个真实大规模程序（自举编译器）是最强探针**——冒烟崩溃先按 ABI 实现审计（寄存器角色/参数区布局）再怀疑被编译程序。
- **崩溃地址是小整数（41）=「整数值被当指针解引用」的指纹**——从崩点槽位向上追写入者（objdump 全量反汇编 grep 槽写入点）比向下猜形态快。
