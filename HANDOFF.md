# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-17——**第一百五十三轮（153-a）：波 6 起步——v2 侧挂点基础设施 + B11**。
①**修饰不再丢弃**（plans/023 §八.2 地基）：节点结构体加「修饰」字段 + 解析函数声明回填（顶层传 1/0）
+ 语义层「当前函数不安全」静态（检查函数 设/复位——与宿主 currentFnUnsafe_ 同构）；②**B11 v2 硬错误**：
「无」字面量附加=1 标记 + 解引用分支表诊断（宿主文案同构）；③**验证全绿**：v2 树 check 0/0 + **锚定链
重锚 406317**（+220 行——节点字段/B11 新产物；fix_p≡fix_s 自洽）+ 对拍 44/44 + 运行级 3/3 + 单测 1315 +
E2E 290/292；④下轮=v2 挂点续（A 组 5 类 + B6~B10）/ 类方法修饰（节点_方法）/ 波 6 收口准备。

### 一、本会话全景（111-a ~ 145-a，家机 win-x64）

| 战线 | 成果 |
|---|---|
| 111-a ~ 133-a | 文件级拆分 + 函数级七波 + win 门禁跃变 |
| 134-a | **波 1：宿主 字符* 拼接误报根治（75→0 + 反证）** |
| 135-a | **波 2：`plans/023` 清单 v1（11 类）——待批准规范** |
| 136~143-a | 波 3：基线→探摸→尝试/回退→向量族→全形态+栈族→链式族→Bisect 澄清（四族全齐） |
| 144-a（另会话） | 关键字体系审计 + 联合体方案 A+D（`plans/024`） |
| **145-a** | **泛化专项崩因链定位 + 构造体修复（泛化暂缓：前置 uF 待修）** |

### 二、plans/022 波次执行状态（2026-09-13 用户全部批准）

| 波 | 状态 |
|---|---|
| 波 1 修 字符* 拼接误报 | ✅ 已完成（134-a） |
| 波 2 不安全操作清单完整化 | ✅ 编制完成（135-a）——**待用户批准规范** |
| 波 3 资源字段统一化 | 🏃 实施中（**四族全齐** + 构造体修复）；**下一步=修 uF（泛化前置）**→ 用户类泛化 → 元素级深拷（前置=波 4）→ v2 → H7/H11 |
| 波 4~8+ | ⬜ 按 §七 |

### 三、下一步（新会话按序）

1. **146-a：修 uF**（泛化前置）——细查两候选：①`ir_expr_assign.cpp` 的「标识符目标」路径（类方法内
   裸字段名）对「类对象字段」的处理（是否浅拷/双删）；②拷贝构造体生成的 IR 路径（IR dump 缺失之谜）；
   修复后：uF PASS → 用户类字段泛化（单形态→逐形态 + 多次复跑）→ 泛化启用。
2. **元素级深拷**（前置=波 4 `复制(x)`）→ v2 同构 → H7/H11 收口 → 波 3 收尾核对。
3. **呈报用户**：`plans/023` 规范草案（波 2 验收）+ B7-a 子裁决 + **144-a 的关键字体系裁决落盘确认**。
4. **D1 函数级续波**（12 超百行）。
5. **单位机 arm64 跨机轮**（93-a ~ 145-a）未做；X64L/ARM64 窄返回规范化动态复验待跨机轮（111-a 遗留）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第一百零八轮（**深度机 linux-x86_64**）——**108-a D5 剩余面固化**：
赋值位结果/可选 构造器保守拒绝负测（264_v2 门禁，零源码改动）；D5 面完整处理链收口
（107-a 修复 + 108-a 固化 + 架构边界登记）。本会话累计 93-a~108-a 共 16 个开发轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **决策**：107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——
   完整实现需「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需
   联合体条件分支=面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ **保守拒绝维持 +
   负测固化**（不为其打单形态补丁；若将来真实需求出现，按已定位路径专项立项）。
2. **门禁**：E2E **264_v2_赋值位结果构造器拒绝**（v2 负测：诊断锚行「构造器 正常 须用于
   结果/可选 语境（返回位或 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——
   防将来误放行致静默截断/泄漏。
3. **D5 面完整处理链（107-a + 108-a 收口）**：
   | 形态 | 处理 |
   |---|---|
   | `返回 无`（段错误） | 107-a 修复（返回位零初始化；E2E 263 + 反证双件） |
   | `可选<T> r = 无;`（原拒绝） | 107-a 修复（语义放行 + 声明位 IR 零初始化） |
   | `r = 正常(...)` / `r = 无`（赋值位） | 108-a 固化（负测 264）+ 完整实现登记（联合体条件释放设施） |
   | `盒.r.正常`/`盒.r.值`（嵌套成员链） | 两侧一致保守=诚实边界 |
4. 全量细节：`plans/019` 第一百零八轮行 + `plans/020` **第四十七节** + `lessons.md` 108-a 段
   （预防 153）。

### 二、本轮验证（零源码改动）

- v2p md5 `b6d8c387c86a066d4c1b1e62b710756c`（与 107-a 一致）；宿主 md5
  `f1484d79b0e5e85d64550b144842208a`（零改动）
- 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）
- 无需锚定链/对拍（v2 源码未变；107-a 终值：固定点 387693 行 / 对拍 34/34）

### 三、下一轮任务（按序）

1. **D1 行数存量整改（长期，本机可实施主要面）**：宿主余 7 文件（ir_expr 2798 / semantic 2101 /
   semantic_expr 1889 / ir_oop 1380 / ir_oop_call 1154 / semantic_call 1092 / ast.hpp 1032）+
   v2 3 文件 + 19 超百行函数——按"每轮 1~2 文件"滚动（验证口径=v2p md5/固定点不变 + 全量零回归；
   族边界勘定纪律见 §四⑬）。
2. **联合体条件释放设施专项（登记）**：结果/可选 全释放面（值字段条件释放）——赋值位覆盖写、
   结果/可选 结构体字段、结果/可选 容器元素等形态在此设施上统一收口（84-e 边界 + 107-a/108-a
   定位的实施路径）。
3. **可选重构（登记）**：v2「无」的整数字面量表示（彻底方案=新节点类型「空字面量」）。
4. **登记面余项**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表缺位；
   D3（未初始化槽 preFree 观察项）/ D4（静态槽不登记自动释放=设计选择）维持登记；C2/C13=106-a
   评估完成（维持排程/登记）。
5. **跨机轮（家机 win-x64 / 单位机 arm64）**：**93-a~108-a 全量复跑**（两侧编译器已多次变更需重建；
   **注意 103-a 契约变更**：v2 诊断带行号 → 迁移后的 .expected 为准；**107-a v2 产物已变**：
   win/arm64 侧 v2p 重建即可）+ 家机立案 **148_v2 win 崩溃缺陷**（三方案待裁决）。
6. **呈报清单余项**：B3、B4、B5（待用户裁决）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；**对拍/锚定链必须独立串行且不得相互交错**（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p107x64l/run_chain107.sh` + **组件对拍** `bash target/p107x64l/cmp_107.sh`
> （口径标杆=107-a 终值；若后续轮改 v2 源码，按该轮新建 p<N>x64l 目录）。
> **手工 v2p 重建（改 v2 源码后）**：`target/cn build CN语言编译器v2/主.cn --target linux-x86_64
> --output target/audit2/v2p_linuxx64`（同时产 .o；E2E runner 指纹变更会自动重建）。
> **手工 v2 探针链**（/tmp/p92probe `diff101.sh` 逐行 diff；/tmp/p105probe `run_d5.sh <名>`：
> 宿主 build + v2p + as + g++ + 逐行 diff）：cwd 需有 `stdlib` 软链 + `target/` 目录。
> **契约变更提示（103-a）**：v2 语义诊断现带「（行=N）」后缀 → 负测用例的 `.expected` 已迁移
> （20 个）；新增 v2 负测写 expected 时须含行号（可用脚本重跑取实测）。
> **调试/验证纪律（累计 13 条）**：①v2 `IR层诊断` 会中止编译——结论须"干净版复核"；②v2 共享
> 结构体加字段 grep 全部构造点；③元素步进统一 `semantic_->typeSizeOf`；④门禁形态须区分"旧机制
> 巧合正确"；⑤反证缺失位数预计算；⑥类型位后缀消费顺序显式设计；⑦探针先做全形态矩阵；⑧共享
> 推导改动回归同族面；⑨反证未命中先疑"改动冗余"；⑩对拍独立串行；⑪契约类改动（.expected/runner/
> 规范）须 §8.4 广播 + 全量迁移；⑫契约迁移脚本须模拟 runner 的真实调用形态；⑬纯重构类改动先勘
> 定族边界依赖（105-a：static 辅助函数跨族即禁拆该边界；等价性由 v2p md5 单点证据承担）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote；**未配置 `github` 镜像
> remote**，github.com 直连超时——镜像补推需网络可达的机器代做。
```

### 五、诚实边界

- 108-a 为**零源码改动轮**（负测固化 + 决策留痕）：赋值位结果/可选 支持仍为 v2 保守拒绝
  （与宿主差异=已登记等价性缺口，非缺陷）；完整实现需联合体条件释放设施（专项登记）。
- 前轮登记维持（静态槽不登记自动释放 / D1 长期滚动 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位 /
  未初始化槽 preFree 观察项 D3 / `1.5f` 后缀缺口）；
  **B2=93-a、C6=94-a、C7=95-a（+C3）、C10=96-a、C8=97-a、C9=98-a、C11=99-a（+H4）、C12=100-a、
  C1=101-a、C4=102-a（+H5）、D2=103-a、D1 长期滚动（首波 104-a + 第二波 105-a）、
  C2/C13=106-a 评估完成、D5=107-a/108-a 收口（残余=登记边界）**（销项台账见 `plans/021` §4.2）。
- 反证注入均已恢复并以注入前 md5 复核；探针留 /tmp/p105probe、备份 /tmp/p107bak 不入库。

## 单位机 ARM64 节

**最近交接**：2026-09-14——**跨机验证轮（93-a~108-a 深度机 + 111-a~146-a 家机 全量 ARM64
动态复验；基线 d25f487）**。验证矩阵全过（单测 1315/1315 + E2E 292/288 过/1 败〔=新立案
「跨机-b」〕/3 跳 + 110-a 9 参数栈参数探针 rc=0 双跑 + 锚定链固定点 **552331 行**逐字节一致 +
对拍 44/44 + 运行级 3/3）；**新立案两缺陷呈报待裁（见下）**；零源码净改动（探针改名已还原）。

### 一、本轮结论（写给无上下文的新会话）

1. **验证范围**：深度机 93-a~108-a（16 轮）+ 家机 111-a~146-a（36 轮）——两机所有宿主/v2 树/
   stdlib/tests 改动首次在 ARM64 动态验证。口径对账：+64 新用例全过；194 既有待批已修复通过；
   78_chain_build 通过（第六十八轮 OOM 败面消失）；**唯一败=14_integration2（「跨机-b」）**。
2. **「跨机-a」GCC 9 拒绝中文 C++ 标识符（构建瘫痪缺陷）**：
   - **现象**：本机（麒麟 GCC 9.3.0）清场构建失败 `error: stray '\346'`；
   - **根因**：家机 110-a（`obj有效`）/118-a（`交换`/`左文本`/`右文本`）/134-a（`拼接加`）+
     深度机 77-a（`k向量定义`/`k映射定义`/`k货架类定义`）在 C++ 源码引入中文标识符——MSVC 与
     GCC≥10（深度机）接受、GCC 9 不支持（UTF-8 标识符 GCC 10 才落地）；违反既有纪律
     「C++ 标识符一律 ASCII」；fb265dd 基线实测 src/ 零中文标识符=跨机回归实锤；
   - **影响面**：5 文件 9 名（ir_decl.cpp / semantic_expr_op.cpp / x64_instructions_cast.cpp /
     linux_x64_instructions.cpp / test_borrowview_check.cpp）——全部为局部变量/常量名；
   - **修复面完备性**：探针改名（objValid/concatAdd/swapped/leftText/rightText/kVectorDef/
     kMapDef/kShelfClassDef）后 GCC 9.3 零警告零错误构建通过+全量门禁绿=无其他 GCC 9 不兼容项。
3. **「跨机-b」字符串字典序返回幅值平台分叉（跨平台确定性缺陷）**：
   - **现象**：14_integration2 单行差异——期望 `1` 实得 `56`（`打印(字符串字典序(李四,张三))`）；
   - **根因链（三层，全部实证）**：①运行时 `__cn_str_cmp` 直接返回 `std::strcmp` 原始值
     （string_api.cpp:127）；②C 标准只保证符号、幅值实现定义——麒麟 glibc 2.31 aarch64 的
     strcmp 对**指针按对齐分叉**：未对齐（rodata 字面量）走字节路径返回差值 1、16 对齐
     （malloc 堆指针）走 SIMD 路径返回 56（LD_PRELOAD 拦截同库同串实证 1 vs 56）；③波3
     （136~143-a 结构体字段拥有化）使 `员工[N].姓名` 从 rodata 指针变堆拷贝指针→触发 SIMD
     路径（fb265dd 旧编译器对照实验打印 1=历史通过的解释；win/x64l 平台 strcmp 幅值恒 ±1
     掩盖）；
   - **定性**：规范 §10.1 定义「返回 <0/0/>0」=符号语义，两侧符号均正确；`.expected` 的 `1`
     幅值过度指定；但「同一 CN 程序三平台输出不同」违反跨平台确定性原则（双编译对照理念的
     底层前提）；12_string_extra 字面量直传（rodata 路径）侥幸通过。
4. **意外事件（已定性，非删除）**：验证中途 target/ 出现约 15 分钟空目录视图后**原文件带原
   时间戳全部回归**——本地 ext4、无网络挂载、无其他会话/进程、AV 日志无命中；疑 Bash 沙箱
   挂载命名空间瞬变；期间新建的影子目录（p146arm 首版）随视图消失。教训：视图异常先跨命令/
   延时复查再响应，勿立即破坏性重建。

### 二、验证链命令（本机复现口径）

```
# 构建+单测：rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
#   cmake --build target/build -j 8（查 warning=0）→ ./target/cn_unit_tests（1315/1315）
# E2E：python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --jobs 8
#   （v2p/rt objs 按指纹自动现建 target/audit2/v2p_linux）
# 锚定链：bash target/p146arm/chain.sh（固定点 552331 行=ARM64 新口径）
# 对拍：bash target/p146arm/cmp44.sh（44/44）；运行级：bash target/p146arm/runlevel.sh（3/3）
# 110-a 探针：target/probe110arm/probe158x.cn（work 目录 cwd+stdlib 软链形态）
# 缺陷复现：跨机-a=直接构建；跨机-b=LD_PRELOAD 拦截器（/tmp/intercept2.c 形态在案）
```

### 三、下一轮任务（按序）

1. **两缺陷裁决后实施**（用户呈报待复）：跨机-a（推荐=9 标识符 ASCII 改名〔探针版 sed 命令
   在更新日志/呈报中〕+防复发门禁）；跨机-b（推荐=运行时 `__cn_str_cmp` 归一化 -1/0/+1
   〔Rust Ordering 同构〕，宿主/v2 产物单点全覆盖，.expected 三平台全对）；实施须看板认领
   开发轮（家机 148-a 进行中注意锁）+三平台门禁+锚定链重锚（跨机-b 改运行时=rt objs 指纹
   自动失效重编，固定点行数预期不变——asm 不含 strcmp 实现；跨机-a 改名不进产物预期不变）。
2. **「跨机-a」防复发机制**随修复落地：CI 侧 ASCII 标识符扫描（Rust `#![deny(non_ascii_idents)]`
   同构）或 AGENTS.md §3 显式规则化。
3. X64L 侧两缺陷同步面：深度机（GCC 版本待确认≥10 则跨机-a 不阻塞其构建，但纪律面同修）；
   跨机-b 深度机 x86_64 glibc strcmp 幅值恒 ±1 侥幸不触发，修复后天然对齐。
4. 排班 A2（本轮）完成销项；78 通过归因可下轮顺带核查（runner 内存上限行为）。

### 四、诚实边界

- 两缺陷**未修复**（呈报待用户裁决——遵「征询无答复不得单方面落实」纪律，探针改名已还原
  工作树=干净）；
- 本轮全部验证基于探针改名版构建（9 个局部/常量名 ASCII 化——不进编译器产物，锚定链两代
  逐字节一致佐证行为零影响；裁决后无论何方案，验证结论均有效）；
- 78_chain_build 通过但归因未深究（败面消失方向非回归）；win 侧 v2 并行隔离仍挂（家机轮）；
- 探针与脚本留 target/p146arm/、target/probe110arm/ 不入库；fb265dd 对照工作树已删
  （/tmp/wt/fb265dd）；LD_PRELOAD 拦截器源码 /tmp/intercept2.c 形态记录于呈报。
