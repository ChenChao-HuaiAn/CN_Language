# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-12 深夜——**第八十六~八十八轮跨机验证轮**（拉取 9403e29=深度机 86-a/
87-a/88-a，基线 efc0c8c）。本轮为**纯验证轮（零源码改动）**，含一个新缺陷的完整诊断链（待修）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **背景**：上一会话（三机协议落地）登记的「88-a win 全量门禁」并行会话在 E2E 阶段被中断
   （`target/e2e_88a_win.log` 0 字节、E2E 产物停在 23:38）；本会话接手重跑。
   构建（零警告）+ 单测 1306/1306 由前序会话完成并留日志（`target/gate_88a_win.log`）——
   源码等价（efc0c8c 为纯文档提交），故直接复用 `target/Debug/cn.exe` 重跑 E2E。
2. **验证链（全量）**：
   - **E2E 275 用例 255 过 / 20 败 / 0 跳**（日志 `target/e2e_88a_win_rerun.log`）。
     败=17 双编译对照（win 平台限制：13 既存 + 新增 243/244/246/248）+ 78/79 OOM（win 固有
     败面）+ **148_v2 崩溃（新缺陷，见二）**。
   - **锚定链复锚**：`python target/p46win/self_chain.py` → fix_p ≡ fix_s **348908 行**
     逐字节一致（新口径；55 轮 340703 → +8205=56~88-a v2 源码）+ 绑定自检
     cn_main∈cn_self46.obj ✓。
   - **运行级 3/3**（`target/p46win/run_level.py`）+ **组件对拍 34/34**
     （`target/p46win/comp_diff.py`，DIFF=0）。
   - **win 实物复验（本轮新增）**：`target/p89win/run89.py` 把 4 个双编译对照用例
     （243/244/246/248，win runner 平台守卫跳过）在 win 上手工两侧对照（宿主 build +
     v2p 编译→ml64→link），**全部 rc=255 两侧一致**——86-a 三元聚合 / 86-a 嵌套深拷 /
     87-a 静态变量全链 / 88-a 短路全链的 win 实物首次验证。

### 二、新缺陷：148_v2 win 侧崩溃（0xC0000005，待修）

**现象**：E2E 148_v2_函数返回结构体 的 v2 产物运行退出码=3221225477（0xC0000005），期望 0；
55 轮该用例通过 → **回归**。

**诊断链（本轮完成，可复现）**：
1. **复现**：`target/p89win/repro148.py`（v2p 编译→ml64→link→运行，稳定 0xC0000005）。
2. **宿主维度 bisect**（手法：4 个不同宿主编译**同一份 55 轮 v2 树**；见
   `target/p89win/try_host.py` + worktree `target/wt_base|wt86|wt87`）：
   55 轮宿主 ✓ 正常 / **86-a 宿主 ✓ 正常** / **87-a 宿主 ✗ 崩** / 当前宿主 ✗ 崩
   → **87-a 的宿主改动引入**（与 v2 源码树无关——四组同树）。
3. **最小复现**：`target/p89win/min3/主.cn`（结构体按值实参 × 嵌套调用
   `取和(造点(30, 40))`）→ cn_main 帧 **384（正常）/368（崩版）**（min/min2 不触发）。
4. **机制**：崩版该调用点**缺 retbuf 隐藏参数**——正常版 `lea rcx,[rbp-384]`（retbuf）+
   返回后 rep movsb 物化拷贝；崩版 `mov rcx,30`（实参直传）→ 被调方（造点）把实参 30
   当 retbuf 指针写入 → **0xC0000005**。
5. **深层特征（重要）**：v2 编译器的「返回物化判定」**对内存状态敏感**——同一个 v2p
   （55 轮宿主构建）在「仅源」（E2E 调用方式）与「源+平台」两种命令行参数下判定**翻转**
   （384↔368）；87-a 的静态初始化改动（emitStaticInitsAtEntry/ALIGN 8/初值直存）改变了
   win 侧 v2p 的内存布局 → 在「仅源」下稳定进入「无物化」分支。
6. **平台分叉**：linux 侧不触发（深度机 E2E 271 过含 148_v2）——**win-x64 特有的跨机轮
   拦截成果**。

**未确证（留开发轮）**：具体「未初始化读取/脆弱判定」点在 v2 源码何处（本轮验证轮零源码
改动）。**已排除**：静态布尔初值错写（两版 .data 均为 0）、诊断开关误置（两者均未输出
[ir1]/[cg1]）、查寄存器类型未登记返回 0（实现安全）、.data 字符串初值伪指针（87-a 后恒
dq 0，且 55 轮旧形态 dq "主" 反而正常）。

**三方案（呈报，待用户裁决）**：
- **甲（推荐）**：根除「内存状态依赖」——定位并修复 v2 编译器未初始化读取/脆弱判定
  （Rust 对照：rustc 确定性编译 + Miri 未初始化检测；性能零开销）。
- **乙**：确定性门禁强化——同输入重复编译逐字节一致 + 参数模式交叉比对（仅防线，不修复）。
- **丙**：三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

### 三、下一步（新会话按序）

1. **缺陷修复（需开发轮看板认领）**：按用户裁决方案执行；入口=min3 最小复现 +
   `target/p89win/` 探针脚本（不入库）；`target/wt_base|wt86|wt87`（三个宿主 worktree）
   可用于继续 bisect 到 87-a 的具体代码块。
2. 单位机 arm64 侧对 86~88-a 的跨机复验（arm64 尚未拉取本轮）。

### 四、本轮踩过的坑

- **worktree 构建需 googletest junction**：`target/wt*` 的 CMake 回退路径是
  `<worktree>/target/build/_deps/googletest-src/googletest`——建 junction 指向
  `C:/Users/ChenChao/Documents/third-party/unittest/googletest` 即可（`mklink /J`，
  用 python subprocess 调 cmd 避免 Git Bash 转义）。
- **`build` 子命令产物名**：`--output <路径>`（不带扩展）产 `<路径>`（无扩展可执行）+
  `<路径>.obj`；Windows 下执行前须改名 .exe。
- **ml64/link 直调在 Git Bash 被 `/nologo` 路径转换坑**：一律用 python subprocess 调用
  （不要经 bash 拼接命令）。
- **v2p 的 `--verbose` 需第 3 参数**（`<源> <平台> --verbose`）；「仅源」与「源+平台」
  会改变 v2p 行为（本轮缺陷的关键观察面）。
- **E2E 结果读取**：runner 输出全缓冲（重定向时中途 0 字节属正常），用产物时间戳/进程判活。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第九十二轮（**深度机 linux-x86_64**）——**92-a 宿主浮32 面缺陷族根治
（H2 销项）+ v2 侧浮点宽化面分叉收口**。上轮 91-a 立案 H2（浮32 算术/存储路径），本轮专项根治
并顺藤以移植纪律 10「两侧同缺一次修全」收口 v2 侧三处分叉。下一轮任务与验证链口径见 §三/§四。

### 一、本轮做了什么（写给无上下文的新会话）

1. **宿主四缺陷根治**（探针 P92 十三件；**一律 `-O0` 取纯 codegen 行为**——O2 常量折叠把常量
   可见的 `甲+乙` 折成正确值，会掩盖类型缺陷，见 lessons 新增 110）：
   - **D1 二元算术结果类型**（`src/cn_compiler/ir/ir_expr.cpp`）：浮点结果类型恒 `"f64"`（阶段一
     简化）→ `浮32 op 浮32` 的 movss 装载（xmm 高 96 位清零）+ addsd 把 f32 位模式当 f64 读
     （值域退化 ~1e-315；`甲/乙`=0.994152=位模式比），且结果 8 字节存 4 字节槽（**栈溢出**）。
     修复=新增 `floatCommon`（f32 op f32=单精度运算；跨类型统一 f64 保持=整32 无损转安全方向）。
   - **D2 调用实参提升**（`ir_call.cpp`）：无条件 f32→f64 提升（注释「Win x64 ABI 浮点参数按
     xmm 传双精度」=错误假设——float 形参在 xmm **低 32 位**）→ 被调方按 f32 读 double 位模式
     低 32 位=**参数恒 0**。修复=按形参类型分派（浮64 提升/浮32 原样/变参未知提升）。
   - **D3 访存宽度前缀**（`codegen/linux_x64/linux_x64_instructions.cpp`）：`emitPtrLoadStore`
     LoadPtr/StorePtr 浮点分支写死 `qword ptr` → f32 发射 `movss`+qword=**非法汇编编译失败**。
     修复=按类型分派（win-x64 同函数本已正确=**linux_x64 漏网**；arm64 用 `ldr s/d` 亦正确）。
   - **D4 自增/自减**（`ir_expr.cpp`）：结果类型硬编码 `"ptr"` → 浮点走整型加法（位模式 +1 只改
     尾数最低位，`甲++` 静默失效）+ 写回 8 字节。修复=结果类型/增量常量（浮点 1.0 按精度）/写回
     宽度全按操作数类型。
2. **v2 侧三处分叉收口**（移植纪律 10）：①混合运算「单侧浮点取该侧」把整型**窄化到 f32**
   （有损，违反规范「仅允许无损或宽化」；`甲+16777217` 宿主 16777218.5 / v2 16777218.0）→
   浮码升 f64；②`发射自增自减` 恒 T_整64 → 类型码分派（新 `发射自增增量`：浮64 常量 1.0 经
   `IR_类型转换` 降精度，**零后端改动**；static/引用参数/普通槽三分支同步）；③赋值/初始化 +
   拼接路径缺浮点宽化（`浮64 宽 = 浮32(1.5);` 读回 0、`"值=" + 浮32变量` 恒 0.000000）→
   新 `浮点宽化存储` 助手（生成声明/生成语句两存储点，与 `正32存储截断` 同款单点）+ 拼接浮32
   侧先宽化；④另修「仅非浮点侧插入转换」漏 f32→f64（双侧浮点混合 f32+f64 原有同款漏网）。
3. **三方案呈报与裁决留痕**（用户令「性能第一、安全第二、参考 Rust」）：**甲（采纳）**=IR 层
   浮点类型精确化 + ABI 修正（零额外转换指令=**性能最优**；消除 8 存 4 栈溢出 + 严格 IEEE754
   单精度=Rust f32=**最安全**）/ 乙=统一提升 f64 计算（C `FLT_EVAL_METHOD=1` 风格：转换指令税 +
   中间精度污染）/ 丙=收窄为仅存储转换打印（功能缺失）。裁决依据=用户令两条同时满足。
4. **防线 E2E 251**（移植纪律 9 **第二十个**双编译对照用例，八形态折 255）+ **反证三件全命中**
   （A 宿主 D1 回退→宿主 **8**〈形态操作数经 `折半()` 打破 O2 折叠强化后：①~⑧除④全捕获；
   强化前 207〉；B 宿主 D2 回退→宿主 **239**；C v2 宽化存储回退→v2 **247**）。
5. 全量细节：提交说明 + `plans/019` §7.1 第九十二轮行 + `plans/020` **第三十二节** + `lessons.md`
   92-a 段（预防新增 **108~112**）+ `plans/021`（戳/数字/队列/H2 销项/里程碑/索引）；探针与反证
   脚本留 `/tmp/p92probe`（不入库）。

### 二、本轮验证链（全绿，本机复现口径）

- 清场重建零警告（`-Wall -Wextra -Werror`）+ 单测 **1306/1306**
- E2E **278 用例 274 过 / 0 失败 / 0 未实现 / 4 跳**（+251；其余 277 过=零回归）
- 锚定链固定点 **361684 行**（新口径，91-a 后 360800）两代逐字节一致；对拍 **34/34**（DIFF=0）
- 关键产物 md5：宿主 `f4f6999049ed98efda99e7b3b02c22af`、v2p `99e97e9ea8627148663988c2c1ff8d6e`
- **反证纪律（本轮加强，lessons 111）**：备份清单必须覆盖**全部待注入文件**（本轮教训：反证 B
  只备份一个文件，注入第二个时无备份→靠注入脚本逆向恢复）；恢复复核基准=注入前 md5 存档。

### 三、下一轮任务（按序）

1. **跨机轮（家机 win-x64）**：91-a 浮点面实物（246/249/250）+ **92-a 实物**（宿主侧跑 E2E 251；
   v2 侧手工对照——runner 的双编译对照仅 linux 支持）+ 88-a/87-a 遗留实物 + **148_v2 win 崩溃
   缺陷**（家机立案：87-a 宿主引入的 v2 编译器内存状态依赖）+ 全量门禁复验。注：**D3 为
   linux_x64 后端专属缺陷**（win-x64 本已按类型分派）；win 侧受益 D1/D2/D4（IR 共用层）。
2. **跨机轮（单位机 arm64）**：浮点发射实物（250 + 92-a 面）+ arm64 侧 H1 复验（本已正确=回归
   确认）+ 全量门禁。
3. **v2 数组声明语法缺口**（本轮新登记，`plans/021` §3 C6）：`浮32[2] 表;`（规格书 3.5
   `类型[长度] 名`）v2 解析报「语句缺少分号」——节点_下标/后缀设施在，缺**声明位**数组语法。
4. **v2 侧登记面（承接）**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表
   缺位；v2 诊断无行号；行数存量违规面。
5. **呈报清单余项**（前轮维持）：B2 类静态成员析构崩溃、B35 字段容器接收者、甲（参数 move）/
   丙（返回位拒绝）、阶段 4 第二层第三批、v2 静态字符语法、多文件静态初始化注入。
   （完整排班队列见 `plans/021` §3。）

### 四、验证链（本机复现口径）

```

> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **手工 v2 探针链**（/tmp/p92probe）：cwd 需有 `stdlib` 软链 + `target/` 目录 → `v2p <入口.cn>
> linux-x86_64` → `as target/v2asm.s -o 主.o` → `g++ -no-pie 主.o target/audit2/{runtime,intern_api,
> io_api,string_api,i128_api,math_api,input_api,file_api,time_api,system_api}.o` → 运行取退出码
> （对照脚本 /tmp/p92probe/run_case.sh 双侧一次跑）。**反证手法（92-a 加强版）**：①**先**备份
> **全部待注入文件** + md5 存档 → ②python 注入（记录注入前后文本，供逆向恢复）→ ③重建（宿主
> cmake / v2p 独立路径）→ ④跑用例应复现诊断 → ⑤恢复后**以①的 md5** 复核（禁与 bak 自证）；
> **探针/用例验证 codegen 缺陷一律 `-O0`**（O2 折叠会掩盖，lessons 110）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote（主仓库推送正常）；**未配置
> `github` 镜像 remote**，且 github.com 直连超时（curl 15s 无响应；无代理/gh CLI/凭据文件）——
> 镜像补推需网络可达的机器代做，或先配置 URL+凭据（同 gitcode 账号名 `ChenChao_GitCode`）。

### 五、诚实边界

- 92-a 修复为**双侧**（宿主 4 处 + v2 5 处）；E2E 251 面内为**两侧共有语义面**（浮32 字面量经
  `浮32(x)` 构造——v2 `1.5f` 后缀缺口维持登记）；宿主 `1.5f` 后缀面由 04_types/09_integration
  既有用例覆盖。
- **新登记：v2 数组声明语法缺口**（`浮32[2] 表;` v2 解析报错；见 §三 3）。
- win/arm64 实物=跨机轮（D1~D4 位于 IR 层/共用层，三后端同构受益；**D3 为 linux_x64 专属**=
  该后端实物面本轮已覆盖）。
- 前轮登记维持（v2 静态字符语法 / 多文件静态注入 / 静态槽不登记自动释放 / v2 诊断无行号 /
  行数存量违规面 / 未初始化槽 preFree 观察项 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位）；
  **H2 已于 92-a 销项**（销项台账见 `plans/021` §4.2）。
- 反证注入已全部恢复并以**注入前 md5 基准**复核（宿主 `ir_expr.cpp 588a3403…`、
  `ir_call.cpp 6ea3209b…`、v2 `生成声明.cn f8535cd7…` 逐字节一致）；探针留 /tmp/p92probe 不入库。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
