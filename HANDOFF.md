# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🏃 340-a 开发轮：D38 批3b=325/347/363 收官·基线 620831b·分支 任务/家机-340-D38批3b·2026-09-18）

**D38 十七项防线全闭合**（16 拒/1 缺=317 销账锚）——经 326-a 批1/329-a 批2/339-a 批3a/340-a 批3b 四轮完成。

1. **325**：已加载 值编码（1 完成/2 加载中）区分——循环依赖在途再入诊断（宿主措辞对齐）+失败/完成双出栈；
2. **347**：解析层段可见性记录（字段节点子起点位·缺省公开维持存量）+语义层成员访问设防（类节点索引表·私有字段跨类拒·宿主措辞逐字对齐）；
3. **363**：顶层「外部 函数」分支+解析外部函数声明（带体拒·签名不登记=单池合并语义）。

**验证**：d38_scan 16 拒/1 缺+三探针措辞逐字对齐+零误伤反证（p1~p3 过/p4 拒·构造器内裸名赋值放行）+v2 树零错误编译+单测 1353/1353+全量 E2E（数字见提交信息·含 78/79 自举链）。

**本会话四轮沿革**：329-a D38 批2（集成 2f6c7b6）→338-a T56（a9dcfe0）→339-a 批3a（433bceba）→340-a 批3b（本轮）。

### 二、下一轮（按序）

1. 波次7 余项=D33/D36/T33/C19/D10~D12；波次8=C20 批C/D 双测上架；
2. B1（185_v2 处置）/T43/T46/T47/T48 待用户裁决。

### 三、坑与边界（本会话重要工程教训）

- **★heredoc 中文编码损伤链（本会话最大教训）**：Git Bash heredoc 到 Windows python 的 stdin 脚本对含中文与反斜杠的内容存在多层损坏（unicode 转义吞字「整32」变「敳32」/反斜杠折叠致真换行/字符级破坏）——**大段中文代码编辑一律用 Write 工具生成补丁脚本再执行**，禁 heredoc 直写；损坏症状=「敳32」类吞字/诊断行真换行/注释代码错位拼接；
- **347 侦查反转**：目录缺 stdlib junction=「拒」假象（stdlib 缺失=预置类型未登记=语义崩）——v2p 探针 junction 必建，「拒」结果先辨真因；
- v2p 对绝对路径参数在部分 cwd 下「打开失败」——探针统一 cwd=项目根+相对路径传参；
- 类缺省可见性 v2=公开（存量维持）vs 宿主=私有——诚实边界随宿主对齐轮收紧；363 诊断行尾无换行符（避反斜杠·输出连排小瑕疵）。
## 深度机 linux-x86_64 节

**交接时间**: 2026-09-18 ⚡ **334-a A12 里程碑标签锚点收工**（轻量轮·4 个 annotated tags 推 gitcode+github·命名规范 `里程碑/<YYYY-MM-DD>-<事件简称>`·明细=plans/025 §2.3）＋ **331-a（T11 静默防线·五项销项）集成挂起等 try-build 回签**（分支 `任务/深度机-331-T11静默防线`·头 da490f4·linux 列门禁全绿：单测 1363+E2E 445=443/0/2+反证双件·**其完整交接节在该分支·集成时并入**）。

### 一、334-a 本轮做了什么（A12·里程碑锚点）

1. **命名规范落定**：`里程碑/<YYYY-MM-DD>-<事件简称>`（「/」命名空间·`git tag -l '里程碑/*'` 可列举·annotated 带说明）。
2. **4 个 tags 建立并推送两远程**：①v2 自举重建立项（75f3c2b·2026-08-25）②v2 固定点 361684（14197dd·2026-09-13）③三机协作协议 v1（47f2c69·2026-09-16）④三机协作协议 v2（4d41d34·2026-09-18）；`git ls-remote --tags` 核验（含 peeled `^{}`）；tags=独立 refs 不碰 develop 分支。
3. **无 src/tests 写集** → 不触发三平台 try-build（快速门禁即可集成）。

### 二、下一轮任务（按序）

1. **331-a 集成**（等家机 win / 单位机 arm64 try-build 回签 → `integrate.py --try-build-done`）；回签要点=零警告构建+单测 1363+全量 E2E+**MSVC C4062 编译期守卫实证**（临时删 dispatch case 应构建失败）；
2. **T53 修复**（P0 安全·i128 元素容器追加段错误 rc=139·**根因已定位代码级**=`ptrElemStride`(ir.cpp:496) 泛型方法内 `T*` 未单态化替换〔typeSizeOf("T") 兜底 8·整64 巧合正确·i128 需 16 越界〕·修复=步进前走 `substGenericType`·实弹 run_err/m53_01）；
3. T46 待用户裁决（128 位静态初值发射·v2 侧已查正确=宿主单侧）；T49 残留（v2 侧同型面待查）；T33/D10~D12 波次7 续。

### 三、诚实边界

- tags 只打 4 个（历史里程碑提交检索耗时·**增量补打机制随规范**·后续轮按需补）；
- github 首推第 4 条 tags 输出截断（下次 push 核验补推）；
- 331-a 的完整交接内容（T11/T49/T50/T51/T52 五项明细+服务轮 9 项）在其分支的 HANDOFF 深度机节——集成时并入本节点；
- 本机无 win/arm64 工具链（交叉验证依赖回签·已按 §8.3 挂起说明）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
