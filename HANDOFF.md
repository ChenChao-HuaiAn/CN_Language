# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-16——**248-a：D21/D22/D23 三缺陷根治（方案A·74-a 先例自行裁决）**：三后端析构链逐级发射+初始化列表限自身构造+构造默认参数四处链路；新用例 375~378+D21 反证命中。D24 转 v2 欠账族独立轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **D21 类继承析构链断裂根治**（P1 资源面）：三后端（x64/arm64/linux_x64）`emitDeleteObject` 从「沿链找第一个 isDestructor」改为**沿 baseName 链逐级调用各级自身析构**（派生先于基类，spec 06 §三 C++ 同款）。原缺陷=methods 为 unordered_map（继承并入含全部祖先析构副本），遍历序不稳定——**基类析构随机缺失**（同一代码两次编译分别命中 ~乙/~甲，反证实证）。判据=各级自身析构键恒为「~本级类名」（精确匹配排除并入副本）。**反证命中**：回退单文件后 375 失败且析构序列改变。
2. **D22 三级链初始化列表误拒根治**：`checkSingleMethodBody` 初始化列表校验限定**自身构造**（`mi.name == info.name`）——继承并入的祖先构造副本已在原类语境校验，提升语境（info=提升目标类）比对必误拒三级链。378 负例（真非法目标仍拒）反证防线。
3. **D23 构造函数默认参数根治（四处链路）**：①`ClassMemberInfo.defaultCount` 字段+collectClassMembers 收集；②checkCtorCall 匹配放宽「实参个数+可补全」（原严格个数→带默认构造少参调用静默落「无构造=默认构造」）；③**visitProgram 预收集**构造默认值（类方法体提升晚于主函数生成——生成期收集会 miss 调用点查表）+handleClassCallExpr 构造展开补缺（构造调用提前 return 绕过 visitCallExpr 通用补缺段）；④初始化列表父构造匹配放宽（语义层+IR 层同口径）。
4. **新用例 375~378**（E2E 394→398）：375 继承析构链/376 三级链初始化列表/377 构造默认参数/378 初始化列表非法目标拒绝（负例）。
5. **D24 转 v2 欠账族独立轮**（plans/021 §三-D）：v2 语法层连类继承均不支持（248-a 实测），完整 wrapper=AST 池级合成大工程——归 D12 同族独立轮排班；宿主侧无缺陷。

### 二、验证链（全绿）

全量 ci.ps1：零警告构建+单测 1327/1327+E2E 398=398/0/0+覆盖率 --strict PASS（可测格 176/176 维持）+双文档门禁。D21 反证：单文件回退→375 失败复现→恢复修复。锚定链：本轮改动不触 v2 树（无类继承语义），78_v2/79_v2 自洽验证随 CI。

### 三、下一步（新会话按序）

1. **M3 CN-Smith 扩展**（支柱二）：生成面扩展（浮点/容器/字符串）+宿主 vs v2 跨实现差分脚本化。
2. **D24（v2 欠账族独立轮）**：v2 侧 主 返回 结果 的 wrapper 合成（或先止血=编译期明确诊断）+「主 返回 错误(3)→rc=3」双编译对照用例转正——摸透 v2 主流程后出轮。
3. **M1 余项呈报**：规范冻结清单（宏名登记制收紧/每配置门禁/D12 v2 结构化谓词——须用户批准）。
4. D18 升硬错误评估（stdlib 已清零）。

### 四、诚实边界

- D21 的 v2 面不可达（v2 不支持类继承=编译期拒绝）——继承析构链在 v2 无运行时缺陷面；v2 类继承支持=既有 v2 语法缺口（另行排班，非本轮范围）。
- D23 的**成员方法**（非构造）默认参数面未单测——本轮只修构造链路；方法默认参数若存在同族缺口随 CN-Smith 差分暴露后立案。
- D24 未本轮完成（转欠账族）：v2 静默崩溃面仍存在（主 返回 结果 的 v2 编译产物）——使用口径=该形态勿用于 v2 自举树（v2 树自身无此形态，自举链无风险）。
- 锚定链绝对行数预期不变（v2 树无类继承/构造默认参数面）——以 CI 78_v2/79_v2 自洽为准。


## 深度机 linux-x86_64 节

**交接时间**: 2026-09-16 第两百三十四轮（**深度机 linux-x86_64**）——**234-a：A7 本体根治收口**
（基线 476b7d2；认领 ecae5ed rebase 后 8bb5511）。上轮 233-a 插队修复轮见 git 476b7d2。

### 一、本轮做了什么（写给无上下文的新会话）

1. **第一层根治（A7 本体）：共享 AST 写回注记跨实例污染**。实例化类（向量$X）方法体 AST 为
   全实例共享，语义检查写回注记（resolvedType 等）被「最后检查实例」残留，IR 生成全读残留值
   （v2 树 9 处 `复制(下标链)` resolvedType 全=IR指令 56B → 误走分派④）。根治=
   `recheckGenericMethodBody`：emitClassMethod 生成泛型实例方法体前按本实例 typeArgs **重放**
   方法体检查（checkSingleMethodBody 自 checkClassMethods 原样提取），诊断经
   `Diagnostics::Snapshot` 快照回滚。方案裁决：重放（方案U）胜实例化深拷 AST（rustc 同构）——
   详见 plans/025 §二/ plans/020 第七十五节 §一。
2. **第二层根治（连带发现）**：`数据[索引] = 复制(...)` 元素槽被存 tmp 槽**地址** 8 字节——
   `emitStructWholeAssign` 源分派漏复制调用形态（79-a srcIsCall 注释原就预期=半截机制收口）。
   **收窄纪律**：仅复制调用（无控制流）进新分支——宽版补丁曾编坏 v2p（285 失败），stash 分组
   二分定位后收窄；v2 树既有发射面零变化。
3. **stdlib 六处深拷切换重新应用**（206-c 立案收口）：向量/栈/链表/队列/映射键/映射值（映射
   范围收 [0,元素数量)）；E2E 285 切换态双侧全绿。
4. **新立案**：D10 泛型函数体零检查（`复制(源)` 落普通调用链接期爆）、D11 一般调用返回整体
   赋值待查——plans/021 §三-D；另家机探针轮 T1~T10 已在 §三-G 待批阅。

### 二、验证链（本机复现口径）

```
> 全量门禁：rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc) → ./target/cn_unit_tests（1327/1327）→
> python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8（313 用例：311 过/0 失败/2 跳）。
> E2E 285（切换态）：宿主 15 = v2 同值 ✓。
> 锚定链：410460 行 fix_p≡fix_s（md5 f169a55e8036ba54…）与 225-a 后基线一致（v2 树零扰动）。
> valgrind 切换态（v2p 编 123_v2_容器）：6 errors/2 contexts=回退态基线（A7 差值 7 归零；
> 剩余=「读取文件行」uninit 独立案，另轮）。
> 反证双件：回退第二层补丁 → 探针 rc=136 复现；第一层插桩双态 IR（9 处全 IR指令→各实例正确）。
> stdlib 态变更后必 rm target/audit2/v2p_*（指纹盲区·lessons 204）——本轮已执行。
```

### 三、下一轮任务（按序）

1. **「读取文件行」uninit 独立案**（225-a 遗留·valgrind 6 errors 归零=验收）。
2. **D10 泛型函数体零检查**（同族根治：泛型函数体检查入口+emitGenericFuncInstance 前重放）。
3. 家机探针轮 T1~T10（plans/021 §三-G 待用户批阅排班）/ D11 存量盘点 / D1 续波照旧。
4. **协议提醒**：动态优先级认领——全表无 🏃 时空闲机即可认领开发轮。

### 四、诚实边界

- 重检查为编译期成本增量（实例方法体检查×2）——v2 树编译 14s 量级无感，未做性能锚。
- 一般调用返回整体赋值（非复制）维持既有 StorePtr 发射——存量形态正确性未盘点（D11）。
- github 镜像仍推不动（本机无凭据/仓库未建，待用户）。

## 单位机 ARM64 节

**最近交接**：2026-09-16——**256-a 收工：F1-26 方案 A 第一步落地（用户裁决「选 A」）**（基线 9f7783d·认领 df797eb）

### 一、本会话总交付（250-a → 256-a 七轮，前六轮已推送；本轮见 git log）

1. **250-a/251-a 检查网第 3/9 层**（f6cef13/1f5ba50）——十层检查网第 1/2/3/5/9 层建成。
2. **252-a/255-a stdlib 双模块**（c4c9dc5/9f7783d）：随机数（xoshiro256++）+正则（递归回溯引擎）+D30/T30 根治。
3. **253-a/254-a D25 补强+F1-26 呈报 B5**（393bf9f/7d49dad）。
4. **256-a（本轮）F1-26 方案 A 第一步**：
   - **探针兑现**：Phi 生成激活（-O3 实测 7 处）；**无使用点重写=无消费者=现状侥幸安全**；多形态 -O0/-O3 行为一致。
   - **Phi 降级**：前驱块尾并行拷贝+交换环打破（新 `Opcode::Copy`·arm64/x64/linux_x64 三后端发射）；途中自查修「临时寄存器 id 越界致 RA 表访问段错误」。
   - **保守使用点重写**：汇合块内 Load 槽→Phi 结果（-O3 IR 实证 Load 消除）；**IR 验证器升级**（块内 def-before-use→函数级定义存在+块内顺序·配合方案 A 的跨块值流）。
   - **runner「编译选项.txt」用例级旗标通道**（第 2 层契约扩展·§8.4 广播）；E2E **384**（-O3 多形态）；单测 `test_phi_lower.cpp` **4/4**；cnsmith 差分脚本跨平台化（原硬编码 win-x64）。
   - **CN-Smith 差分（500 采样）首采即捕获既有缺陷 D31**（-O3 常量折叠产出超位宽常量→arm64 32 位路径非法汇编·stash 对照实证「非本轮引入」）——**已呈报用户**（三方案·见 plans/021）。
5. **累计根治缺陷**：D30/T30（宿主字符串字段下标）+ D31 立案（-O3 常量超位宽·等裁决）。
6. **锚定链**（255-a 重锚口径）：569231 行 fix_p≡fix_s（本机 arm64）。

### 二、发现的问题（已处置/呈报）

- **D31（呈报中·等用户裁决）**：-O3 常量折叠/强度削减产出「数值超其类型位宽」常量→arm64 32 位发射路径 `movk wN,#…,lsl#32/48` 非法——方案 A=折叠出口按位宽截断（推荐）/B=后端防御/C=A+B 双防线。
- 本轮三坑三修（临时 id 越界/验证器跨块值流/自引用操作数惯例）已入 plans/025 §三.8。

### 三、下一步

1. **D31 裁决与根治**（等用户选择方案）。
2. **257-a 候选**：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/ F2-33 拆分之三迭代器 / D28/D29 专项。
3. 跨机：锚定链 569231 口径 win/深度机复验。

### 四、验证链（本机复现口径）

```
python3 tests/e2e/run_e2e.py --cn target/cn --target linux-arm64 --jobs 4   # 全量（见提交）
./target/cn_unit_tests                                                       # 1342/1342（含 PhiLower 4）
python3 scripts/cnsmith_diff.py --cn target/cn --dir target/cnsmith --out target/cnsmith/work  # -O0/-O3 差分
# 384 用例（-O3 旗标通道）：测试目录含 编译选项.txt（每行一个旗标）
# 锚定链：target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm（569231 行 / 8013ae3a…）
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- **D31 未根治**（呈报中）；**F1-26 剩余子项**=激进 SSA 管线化（SSA 态持久+支配子树重写+真 mem2reg+Phi 进 RA 偏好）——本轮=降级能力+保守重写闭环。
- 手构交换环=防御能力（自然不可达）；性能锚定量化=后续性能轮。
- github 镜像本机无凭据未推（170-a 起累积）。
