# HANDOFF 交接文档

**交接时间**: 2026-09-01（P7b 容器类型实例化与容器方法调用全链路，Windows 本机全量验证）

## 当前任务（2026-09-01）

**v2 自举重建（plans/013 P7 / plans/014）**：上轮 P7a（顶层常量 + 宿主四缺陷，c9a0c69）。
本轮完成 **P7b：HANDOFF 第三步之②**——v2 编译器自身实现容器支持（此前 IR 层遇成员
方法调用直接 运行时错误(7)、解析类型 只认 标识符+*）。注意：HANDOFF 旧表述「IR 层
运行时错误(7)」指 **v2 自己的 IR生成.cn:425**（非宿主——宿主链路 E2E 30/31 一直通）。

## 本轮完成

### 一、方案与实现（rustc 预编译 std 模式）

v2 生成对宿主符号的直接调用（向量$整64$追加#整64 -> nameMangle -> ?E59091...@@YJ@Z），
实现链接 `cn build` 编译 v2 组件时产出的 **v2p.obj**（stdlib 源码级并入、内含全部容器
方法机器码）。③ 对拍（v2 编译 8 组件 -> 链接）直接沿用此模式（/FORCE:MULTIPLE +
v2asm.obj 在前 + map 自检——79 防虚假验收同款）。

- **语法分析.cn**（815 行）：泛型类型解析（$ 合成名，嵌套扁平对齐宿主 generics.cpp）、
  泛型闭合位置 约束扫描前瞻（防 a<b 误判）、构造调用识别、& 引用登记、返回类型泛型、
  变量容器表 + 查容器类型 导出；词法 字符串/字符字面量剥引号。
- **IR容器.cn（新组件，567 行——7 组件变 8）**：方法签名表（全容器公开方法 × 返回
  形态标量/结果空12B/结果值+值偏移）、对象大小表（向量24/映射56/链表48/队列48/栈24/
  集合24——⚠ stdlib 字段变更须同步）、构造/析构/方法符号合成、预扫容器声明（入口零
  初始化）、发射容器析构（RAII）；IR追加/建IR指令/类型码/常量折叠辅助移入（行数规则）。
- **IR生成.cn**（974 行）：IR_分配对象=60 / IR_结构体拷贝=61 / IR_删除对象=62（空安全）/
  IR_调用参数化=63（this 位0 + retbuf 隐藏返回指针（共享单 retbuf 复用）+ rcx..r9）；
  容器预扫+入口零初始化；构造（NewObject+无参构造，H7 同款）；方法调用（表达式栈方法
  标记 -(10^6+名ID)）；结果变量 2 槽内联 + .正常/.值 偏移访问 + CopyStruct 接收；
  每 IR_返回 前发 RAII 析构；字符串字面量 IR_常量字符串。
- **代码生成.cn**（614 行）：宿主同款 nameMangle（# 切分+类型编码 J/H/PAX/_T/A&/@Z）；
  4 新指令展开；retbuf 帧区；布尔宽度加载（movzx）；EXTERN 自动收集（call 符号-本地
  PROC 差集）；字符串常量 .code 段内 strData_<ID> 数据行（主函数末尾扫描 IR 发射）。

### 二、实测根治 5 缺陷（全记 lessons 高权重，接手必读）

1. **MASM 247 符号限制**：9 参函数 mangled 超限 -> A2043 -> PROC 注册失败 -> bb 标签
   泄漏全局连锁 A2005（症状迷惑：报的是标签重定义）。解法=语法层预标注回退 8 参。
2. **多槽向上写越界**（retbuf 与结果变量两处）：槽地址公式槽号越大地址越低，多字节
   写入基址必须取最低地址（最大槽号）——否则覆盖相邻对象指针 -> SIGSEGV。
3. **向量<字符串> 跨向量搬移悬垂**：元素拥有式（RAII 释放），搬指针后源析构即悬垂
   -> asm 乱码。解法=零搬移。
4. **AST 节点.子终点 复用伪造子区间**：语义层当区间遍历 -> 未声明误报。解法=导出
   查询函数，子终点 恒 0。
   另有：容器白名单误含 结果/可选；objdel 标签多容器重复（目标=唯一序号）。

### 三、E2E 与验证

- 新增 **123_v2_容器**（六步闭环 退出码 21333）：向量 构造/追加/元素/大小 + 映射
  设置/获取/.正常/.值/清空 + 结果接收 + 向量<字符串> + RAII 双容器。run_e2e.py
  执行v2闭环 扩展 链接v2pobj 模式 + map 双向自检（cn_main 绑 v2asm.obj、向量方法
  绑 v2p.obj）。
- **全量门禁**：构建零警告 + 单测 **1196/1196** + E2E **125 用例 123 通过**
  （仅 78/79 已知遗留内存超限，与本轮无关）；119/120/123 全绿。

## 踩过的坑

- bash `$?` 是 8 位截断（退出码 300 显示 44）——验证退出码用 python subprocess。
- CN 源码字符串字面量里的 
 经 bash heredoc+python 写入会被吃成真实换行——用
  python 时拼接 chr(92)+'n'，或用 Edit 工具直写。
- MASM EXTERN 必须在段内（.code 后）；call 前向引用外部符号可过汇编但行为依 EXTERN。
- 手搓 MASM 最小程序 + 同 obj 链接，是隔离「v2 生成代码 vs 链接环境」的高效手段。
- 调试产物 target/dbg/p7b1|p7b2|p7b3|p7b123（不入库）；manual.asm 手搓对照仍在。

## 下一步（②b→③）

1. **②b 结构体元素容器**（③ 前最大缺口）：向量<Token>/向量<节点>——v2 结构体布局
   对齐宿主 typeSizeOf（整32=4 紧凑+对齐，当前 v2 全 8 字节/字段）、结构体按值传参
   ABI（隐藏指针/结构体参数槽）、元素(i) 类元素视图语义（H8-4/5：返回地址非拥有视图）、
   追加/删除 的结构体元素深拷贝与析构注入。
2. 嵌套容器（向量<映射<整64,符号>>——语义分析.cn 符号表）、容器作结构体字段（函数IR）、
   顶层静态容器（常量表/变量结构体表——v2 IR 层静态支持）、& 引用参数语义（传地址+
   被调方间接访问）、容器按值传参/按值返回。
3. v2 剩余语法（③ 前清单）：指针下标 p[0]（IR 无 分支）、字符串下标/拼接 +/三元 ?:/+=、
   内置函数符号映射（驻留->__cn_intern、打印到错误、文件::*）、枚举、类型大小。
4. **③ v2 编译 8 组件自举对拍**（对标 79；链接模式已就绪）。
5. 顺带（不阻塞）：宿主 元素(0).字段 链式取值缺陷（plans/014 三表，复现 target/dbg/c9、
   c4d）——本轮主体超时未做，仍待根治；P4 代码清理（结果.值 临时绑定规避可去除）。
