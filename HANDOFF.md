# HANDOFF 交接文档

**交接时间**: 2026-09-07 第二十七轮收官（深度系统 x86_64）——**plans/017 v2self 下一代重编锚定：双键登记后自举+字节级固定点达成+8 用例运行对拍全绿（纯验证轮，零代码改动）**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、新一代构建链
1. **v2p_gen2**：宿主 cn（HEAD 含第二十五轮重载消歧+第二十六轮双键登记修复）编译 v2 源码（linux-x86_64）。
2. **v2self_gen2 重编**：v2p_gen2 编译 v2 全链源码 rc=0 → **192288 行 asm**（上代 191956，+332=签名键/双键登记/自检显示名净增量）→ as → g++ 链接成功（v2self obj 在前+muldefs，对齐 run_e2e.py 编排）。
3. **12 组件+min1 冒烟逐项一致**（IR容器.cn rc=1 失败形态逐字一致=第二十二轮既有灰色点，非回归）。

### 二、对拍三锚定（核心成果）
1. **组件产物对拍 12+1 全 PASS**：5 文件（词法/语法/IR常量/abi辅助/min）裸逐字节一致；7 文件（语义分析/IR签名/IR生成/代码生成系）差异 **100%=strData_N 驻留 ID 平移**（宿主与 v2 字符串驻留序差 1，产物内自洽零语义影响），按定义内容重映射规范化后全部逐字节一致（脚本 /tmp/v2dbg/p27/norm.py）。
2. **全链自举产物对拍**：v2self_gen2 编 v2 源码（192288 行）vs v2p_gen2 编 v2 源码规范化后逐字节一致。
3. **字节级固定点（里程碑）**：g3（=v2self_gen2 编 v2 源码产物）链接为 v2self_gen3 → 再编 v2 源码 → g4，**g3 与 g4 裸逐字节一致**——自举代际完全稳定（gN=gN+1）；上代锚定口径为探针级（min1/min18+九探针），本轮升级为**全链+代际+固定点**三级锚定。

### 三、8 用例运行级对拍 8/8 PASS
v2self 产物 as→链接→运行 rc 对比预期+产物 v2p vs v2self 规范化一致：119 多文件（14）/123 容器（85）/126 供给（0）/128 内置（0）/130 指针（0）/148 retbuf（0）/**150 引用返回（第二十六轮修复锚定）**/158 SysV ABI（0）——补齐「v2self 产物可正确运行」实证。

### 四、验证（全绿）
构建零警告（GCC -Wall -Wextra -Werror，HEAD 零改动）+ 单测 1240/1240 + E2E linux-x86_64 **160 用例 156 过/0 败/4 平台跳过**（与第二十六轮基线一致，零回归）。

## 下一步
1. **跨机轮**：win（家机）/linux-arm64（单位机）全量复验——158 三平台跑+arm64 路径动态零变化实证+**两侧 v2self 重编锚定**（本轮为 x86_64 侧）。
2. **呈报①②③+第 7~18 枚+T3/T4+v2self 三级锚定全部收口**。
3. P6b 模块系统（模块名$ 前缀+纯名调用遮蔽语义对齐）：**新增语法须用户审批后方可启动**。
4. 远期观察项：宿主-v2 字符串驻留序对齐（若要求两编译器产物逐字节一致的终极对拍专项）。

## 灰色点披露（持续有效）
- IR容器.cn 单组件子链冒烟 exit=1 系既有状态（第二十二轮披露，主链全绿；v2self 与 v2p 失败形态逐字一致）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径；asm 输出 target/v2asm.s 亦相对 cwd）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- **strData 驻留 ID 宿主-v2 差 1**（本轮新观察项，非缺陷）：特定文件（语义分析/IR签名/IR生成/代码生成系）字符串池 ID 整体平移——产物内自洽零语义影响；组件级 asm 对比首次执行才暴露，非本轮回归；v2self 家族内部（g3=g4）已字节级稳定。
- **v2 编译器入口打开失败回退内置源码**（[p6f] 打印后继续，rc=0 产 asm）：对无参自检模式是设计特性，但对编排错误有掩盖性——手工编排跑 v2 编译器必须核对 [p6h] 入口行+[p6f] 回退行+[ir1] 函数数。
- 本轮对拍脚本与产物在 /tmp/v2dbg/p27/（不入库；norm.py=规范化对拍+probe.sh=8 用例对拍+g3/g4=固定点产物）；新一代编译器产物 target/dbg/{v2p_gen2,v2self_gen2}。
- 验证跑管道纪律：基线对拍必须重定向到文件再 grep，禁用管道截断（第二十六轮实操踩到）。

## 踩过的坑（已入 lessons 权重 10）
- **假对拍第三案——入口回退掩盖**：对拍脚本入口路径双拼（target/target/）→ v2 编译器回退内置回归源码 → 8 用例假「全绿」（rc=19=内置断言值）。识别纪律：编排对拍必须核对 [p6h] 入口行+[p6f] 回退行+函数数；三案谱系（cwd 陷阱/判定行自比/入口回退）共同根因=「编译成功」信号被劫持。
- 历史：平台跳过掩盖（解锁日=欠账日）+假对拍 cwd 陷阱+fail fast 双拦截+调用节点子区间 +1 偏移约定（见 lessons 各轮条目）。
