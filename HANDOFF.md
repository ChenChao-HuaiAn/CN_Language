# HANDOFF 交接文档

**交接时间**: 2026-09-01（P7a 顶层常量全链路 + 宿主四缺陷根治，Windows 本机全量验证）

## 当前任务（2026-09-01）

**v2 自举重建（plans/013 P7 / plans/014）**：上轮完成 P6h（多源文件，13ee8cc）。
本轮完成 **P7a：顶层常量声明全链路**（HANDOFF 第三步之①），途中根治 **4 个叠加宿主缺陷**
（E2E 119 自 P6h 起从未真正通过——P6h 在 Linux 开发无法运行 v2p，CI「通过」是误信，
本轮 Windows 本机重跑暴露并全部根治）。

## 本轮完成

### 一、宿主四缺陷根治（src/cn_compiler + stdlib，全部最小复现验证）

1. **无初始化器顶层静态容器双模型不一致**（v2 崩溃主因）：有初始化=指针入 .data 槽 /
   无初始化=对象内联 .data+构造打符号地址；读取统一 LoadPtr 只对指针模型正确。
   **统一指针槽模型** 5 处：ir_decl（主入口 NewObject+构造+StorePtr）、x64_codegen
   （.data 类静态 8 字节；结构体静态保持 typeSizeOf）、ir_oop_call（删接收者特例）、
   ir_stmt genVarDecl（拷贝构造支持静态源 srcAddr=?gstatic 槽地址）、ir_expr（类静态
   赋值右值标识符深拷贝）。
2. **分支未执行类局部被无条件 RAII 析构**（5 行复现 100% 崩；新鲜零页栈侥幸通过属假阴性）：
   genClassDestructorCalls 在**函数入口块最前**统一零初始化全部类局部槽 +
   codegen DeleteObject 空安全跳过（x64 test/je + arm64 cbz）。
3. **块顶中途 返回 被无视**（返回 5 实得 7，死代码进同一线性块覆盖返回值）：
   genBlock 块终止（返回/中断/继续）后跳过剩余语句。
4. **stdlib 文件.cn / IO.cn 漏 导入 核心::错误码**（单独 导入 文件::* 编译失败）：
   补导入（与 容器.cn/映射集合.cn 同款）。

### 二、P7a：v2 顶层常量四层

词法「常量」关键字（第 25 个）；语法 节点_常量声明=31 + 解析常量声明 + 局部常量分支；
语义 符号_常量=5（多文件两趟跨模块可见）；IR 静态常量表（名ID→值文本ID，入口清空+收集，
折叠零数值解析：正值复用字面量 Symbol ID / 负值 驻留("-"+文本)，标识符内联 IR_常量整数，
局部遮蔽正确）。代码生成/主.cn 零修改。

### 三、E2E 与验证

- 新增 **120_v2_顶层常量**（六步闭环退出码 62）、**121_宿主缺陷_静态容器与分支局部析构**
  （缺陷①②③三合一）、**122_宿主缺陷_stdlib模块独立导入**（缺陷④）；run_e2e.py 的
  执行119v2闭环 参数化为通用 执行v2闭环（119/120 共用）。
- **全量门禁 ci.ps1（Windows 本机）**：构建零警告 + 单测 **1196/1196** + E2E
  **124 用例 122 通过**（仅 78/79 内存超限=旧架构已知遗留，与本轮无关）；
  **E2E 119 本地真实首次通过**。
- 最小复现存于 target/dbg/（c1~c18，不入库）。

## 踩过的坑

- **「CI 通过」误信**：P6h 的 HANDOFF 把「push 后 CI 会跑 119」写成验证结论——实际从未跑通。
  接手会话必须本地重跑关键门禁用例再信基线（已记 lessons，权重 12）。
- **二分三要素**：删旧产物 + 显示构建输出 + 退出码断言；构建失败时测的是旧 exe（本轮踩过）。
- **假阴性复现**：未执行分支的栈垃圾在新鲜零页栈下恰为 0，崩溃类缺陷的复现须先污染栈。
- CN 源码文本编辑勿走 bash heredoc+python（\n 转义被吞），用 Edit 工具直写。
- E2E 打印用 `打印`（带换行）；`打印行` 不换行。
- 「结果」是关键字（变量名禁用）；结果.值 访问须 如果 (x.正常) 真分支模式。

## 下一步

HANDOFF 第三步之②：**容器类型 向量<T>/映射<...> 实例化与容器方法调用**——v2 组件源码
大量使用 .追加/.设置/.获取/.元素/.大小/.清空（IR 层当前 运行时错误(7)），含泛型类型解析
（解析类型 只认 标识符+*）。之后 ③ v2 编译 7 组件 → 7 asm → 链接 v2p2 对拍（对标 79）。
顺带（不阻塞）：链式字段取值缺陷 `向量.元素(0).字段`（两步写法正确；复现 target/dbg/c9、
c4d，已记 plans/014 三、表）；P4 代码清理（结果.值 临时绑定规避可去除）。
