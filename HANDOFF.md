# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-16——**248-a：D21/D22/D23 三缺陷根治（方案A·74-a 先例自行裁决）**：三后端析构链逐级发射+初始化列表限自身构造+构造默认参数四处链路；新用例 375~378+D21 反证命中。D24 转 v2 欠账族独立轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **D21 类继承析构链断裂根治**（P1 资源面）：三后端（x64/arm64/linux_x64）`emitDeleteObject` 从「沿链找第一个 isDestructor」改为**沿 baseName 链逐级调用各级自身析构**（派生先于基类，spec 06 §三 C++ 同款）。原缺陷=methods 为 unordered_map（继承并入含全部祖先析构副本），遍历序不稳定——**基类析构随机缺失**（同一代码两次编译分别命中 ~乙/~甲，反证实证）。判据=各级自身析构键恒为「~本级类名」（精确匹配排除并入副本）。**反证命中**：回退单文件后 375 失败且析构序列改变。
2. **D22 三级链初始化列表误拒根治**：`checkSingleMethodBody` 初始化列表校验限定**自身构造**（`mi.name == info.name`）——继承并入的祖先构造副本已在原类语境校验，提升语境（info=提升目标类）比对必误拒三级链。378 负例（真非法目标仍拒）反证防线。
3. **D23 构造函数默认参数根治（四处链路）**：①`ClassMemberInfo.defaultCount` 字段+collectClassMembers 收集；②checkCtorCall 匹配放宽「实参个数+可补全」（原严格个数→带默认构造少参调用静默落「无构造=默认构造」）；③**visitProgram 预收集**构造默认值（类方法体提升晚于主函数生成——生成期收集会 miss 调用点查表）+handleClassCallExpr 构造展开补缺（构造调用提前 return 绕过 visitCallExpr 通用补缺段）；④初始化列表父构造匹配放宽（语义层+IR 层同口径）。
4. **新用例 375~378**（E2E 394→398）：375 继承析构链/376 三级链初始化列表/377 构造默认参数/378 初始化列表非法目标拒绝（负例）。
5. **D24 转 v2 欠账族独立轮**（plans/021 §三-D）：v2 语法层连类继承均不支持（248-a 实测），完整 wrapper=AST 池级合成大工程——归 D12 同族独立轮排班；宿主侧无缺陷。

### 二、验证链（全绿）

全量 ci.ps1：零警告构建+单测 1327/1327+E2E 398=398/0/0+覆盖率 --strict PASS（可测格 176/176 维持）+双文档门禁。D21 反证：单文件回退→375 失败复现→恢复修复。锚定链：本轮改动不触 v2 树（无类继承语义），78_v2/79_v2 自洽验证随 CI。

### 三、下一步（新会话按序）

1. **M3 CN-Smith 扩展**（支柱二）：生成面扩展（浮点/容器/字符串）+宿主 vs v2 跨实现差分脚本化。
2. **D24（v2 欠账族独立轮）**：v2 侧 主 返回 结果 的 wrapper 合成（或先止血=编译期明确诊断）+「主 返回 错误(3)→rc=3」双编译对照用例转正——摸透 v2 主流程后出轮。
3. **M1 余项呈报**：规范冻结清单（宏名登记制收紧/每配置门禁/D12 v2 结构化谓词——须用户批准）。
4. D18 升硬错误评估（stdlib 已清零）。

### 四、诚实边界

- D21 的 v2 面不可达（v2 不支持类继承=编译期拒绝）——继承析构链在 v2 无运行时缺陷面；v2 类继承支持=既有 v2 语法缺口（另行排班，非本轮范围）。
- D23 的**成员方法**（非构造）默认参数面未单测——本轮只修构造链路；方法默认参数若存在同族缺口随 CN-Smith 差分暴露后立案。
- D24 未本轮完成（转欠账族）：v2 静默崩溃面仍存在（主 返回 结果 的 v2 编译产物）——使用口径=该形态勿用于 v2 自举树（v2 树自身无此形态，自举链无风险）。
- 锚定链绝对行数预期不变（v2 树无类继承/构造默认参数面）——以 CI 78_v2/79_v2 自洽为准。


## 深度机 linux-x86_64 节

**交接时间**: 2026-09-16 第 259-a 轮（**深度机 linux-x86_64**）——**OOM 归因诊断＋用户裁决方案 B 归零**（基线 4adc7a5；无认领·用户令「家机在做任务，本机全部归 0，不做开发任务」）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **三次 ZCode「退出」根因实证**（9/15 21:41、9/15 22:21、9/16 14:28）：journalctl 铁证=76_self_host_bo（E2E 76 号产物）三次吃到 28.9~29.8GB 触发内核 OOM killer，ZCode agent 被连带 SIGTERM（用户视角=ZCode 退出；进程挂 zcode 的 systemd scope 下被连坐）。
2. **stash 基线对照实验定位元凶**：工作区 235-a/236-a 在飞改动（ir_fields.cpp collectOwnedStrFields 双形态判定·13 行·从未 push）——含改动构建跑 76 号=**4285MB 失控＋段错误零输出**；stash 回退态=**30MB、退出码 0、输出与 expected 逐行一致**。结论=该改动引入运行时内存失控回归（机制层剖析随 C19 重做轮）。
3. **发现虚假验收记录**：被丢弃的在飞 dirty 中「236-a 收工：全量 E2E 313=311/0/2 绿」与 OOM 三连并存——验证链未完成即写全绿，违反「未完成不得声称完成」。已随归零丢弃，教训入 lessons 77。
4. **用户裁决执行（方案 B）**：丢弃全部在飞改动（diff 备份 /tmp/深度机235a236a在飞改动备份_20260916.patch）→ fast-forward 同步远程 54 提交（4adc7a5）→ 干净态重建零警告（grep 命中 4=既有口径文件名含 error 进度行）→ 76 号冒烟 30MB 全绿 → 看板本机行清空。**本机归 0，不持锁不开发**。
5. **E2E 覆盖口径盘点（用户质询立案 C20）**：挂 v1 组件链=70~77 全套自举族＋89_关键字穷举 共 9 用例＋单测 test_bootstrap_lexer.cpp；构成=v2 闭环 79＋双编译对照 35＋宿主-only 其余＋v1 挂账——收工汇报从未呈现此构成。

### 二、验证链（本机复现口径）

- 归零冒烟：干净态重建零警告＋76 号单跑（30MB／退出码 0／逐行一致）。
- 全量门禁未跑（本机归 0·家机 258-a 在飞）——下一开发轮认领后走全链（E2E＋单测＋锚定链＋valgrind；全局最新口径见 plans/021 §〇/§五）。

### 三、下一轮任务（按序）

1. C19 uninit 重做轮（任一空闲机认领）：形态二分单点判定＋全量回归面＋独立 scope 跑重验证。
2. C20 E2E 覆盖口径收口轮：v1 退役迁移（70~77＋89）＋收工汇报面构成呈现纪律。
3. 深度机待命：家机 258-a 收口后按三机角色动态认领（优先开发轮）。

### 四、诚实边界

- 内存失控的**机制层**（collectOwnedStrFields 改动为何无界增长）未剖析——随 C19 剖析；现象与归因已双实证（journal＋对照实验）。
- github 镜像沿既有口径（本机走 SSH 22 可达、仓库未建，待用户建仓后补推）。
## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
