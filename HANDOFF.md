# HANDOFF 交接文档

**交接时间**: 2026-08-20 16:41 CST（麒麟 ARM64 全量回归验证完成，平台抽象层方案已设计待实施）

## 我们在做什么任务

CN 语言编译器在**麒麟操作系统（ARM64/aarch64）** 上运行全量回归验证，发现并修复 ARM64 后端 BUG，处理平台限制，确保编译器在 ARM64 平台上的正确性和稳定性。当前已进入**自举（self-host）阶段**，但自举用例因 [`CN版代码生成.cn`](CN语言编译器/代码生成.cn) 100%硬编码为 x64 MASM 而受阻，已设计平台抽象层方案待实施。

## 已经完成了什么

### 1. 全量回归验证

- **编译**：GCC 7.5.0（aarch64）`-Wall -Wextra -Werror` 零警告
- **单元测试**：1189/1190 通过（1 个 MSVC 特定已知失败：`I128ApiTest.MulCrossTerms`）
- **E2E 测试**：96 通过 / 0 失败 / 7 跳过

### 2. 核心 BUG 修复

**BUG：ARM64 后端 `emitNewObject` 缺少接口分派区填充**
- **现象**：E2E `100_接口多态` 运行时报段错误（SIGSEGV）
- **根因**：[`arm64_codegen_oop.cpp`](src/cn_compiler/codegen/arm64/arm64_codegen_oop.cpp) 的 `emitNewObject` 在分配对象后只填充了虚表指针，未填充接口分派区。x64 后端有完整的接口分派区填充逻辑，ARM64 后端遗漏。
- **修复**：在 `emitNewObject` 中增加接口分派区填充——遍历 `classInterfaces`，为每个接口计算偏移，使用 `adrp + add + str` 写入接口分派表地址
- **验证**：`100_接口多态` 输出正确，E2E 全部通过

### 3. 平台限制处理

在 [`run_e2e.py`](tests/e2e/run_e2e.py) 中添加 `PLATFORM_SKIP` 字典，跳过 7 个平台限制用例：
- 33_io_input（依赖 stdin 交互输入）
- 34_file（依赖文件系统路径差异）
- 35_string_ext（依赖 Windows 编码行为）
- 36_time（依赖系统时间 API）
- 37_system（依赖 Windows 命令行参数编码）
- 38_tool（依赖 Windows 控制台编码）
- 39_chkstk（x64 专用 __chkstk 栈探测）

### 4. 编译警告修复

修复 GCC 7 严格性导致的编译警告：
- [`semantic_call.cpp`](src/cn_compiler/semantic/semantic_call.cpp)：移除未使用形参
- [`test_bootstrap_lexer.cpp`](tests/unit/semantic/test_bootstrap_lexer.cpp)：移除未使用变量

### 5. 平台抽象层方案设计（已设计待实施）

针对 [`CN版代码生成.cn`](CN语言编译器/代码生成.cn) 100%硬编码 x64 MASM 的问题，设计了平台抽象层方案：
- **后端接口.cn**：定义 `后端接口`（`生成汇编`、`寄存器名`、`指令` 等抽象方法）
- **IR遍历.cn**：平台无关的 IR 指令遍历引擎，调用后端接口生成汇编
- **x64后端.cn**：实现后端接口，输出 x64 MASM（从现有代码提取）
- **arm64后端.cn**：实现后端接口，输出 ARM64 GAS（新增）
- **后端工厂.cn**：按目标平台分发后端实现
- 用户已同意方案，待实施

## 总结发现的问题

1. **ARM64 后端 `emitNewObject` 缺少接口分派区填充**——与 x64 后端不对称，导致接口多态调用段错误。已修复 ✅
2. **GCC 7 严格性编译警告**——ARM64 上 GCC 7 的 `-Wall -Wextra -Werror` 比 MSVC 更严格，未使用形参/变量报错。已修复 ✅
3. **平台限制用例**——7 个 E2E 用例依赖 Windows 特定功能（stdin 交互/__chkstk/编码等），在 ARM64 Linux 上不可运行。已通过跳过机制处理 ✅
4. **自举用例受阻**——自举用例（75/76/78）在 ARM64 上因 [`CN版代码生成.cn`](CN语言编译器/代码生成.cn) 100%硬编码 x64 MASM 而无法运行。已设计平台抽象层方案，待实施 ⏳

## 当前卡在哪

**自举用例（75/76/78）在 ARM64 上无法运行**——根因是 [`CN版代码生成.cn`](CN语言编译器/代码生成.cn) 的 971 行代码 100%硬编码为 x64 MASM（寄存器名 `rax`/`rdx`/`rcx`/`r12`/`rbp`/`rsp`，指令 `mov`/`add`/`sub`/`call`/`ret` 等 MASM 语法）。已设计平台抽象层方案（后端接口.cn + IR遍历.cn + x64后端.cn + arm64后端.cn + 后端工厂.cn），用户已同意方案，但因换电脑（从麒麟 ARM64 切换到 Windows x86）暂停实施。

## 下一步计划

### 已完成（本地保留）

1. **007-P3 收尾增量（2026-08-19 全部完成）**：①引用返回（102/103）✅；②实例方法作值（104/107）✅；③接口传参 C2（105）✅；④去虚拟化 D3A + `--cfi` D4（008 + 106）✅；E2E ≥109、单测 1196、0 警告全绿
2. **B 任务第一阶段**：多文件组件拆分（词法/语法/语义）完成，E2E 79/79 全绿
3. **B 任务第二阶段**：拆分 IR生成.cn（1029 行，超 1000 行约束）、代码生成.cn（971 行）

### 在 Windows x86 电脑上实施平台抽象层重构

1. **阅读代码生成.cn完整结构**（Ask模式）——通读 [`CN版代码生成.cn`](CN语言编译器/代码生成.cn) 全部 971 行，提取 x64 MASM 硬编码模式
2. **创建后端接口.cn和IR遍历.cn**（平台无关层）——定义 `后端接口` 抽象类，实现 IR 指令遍历引擎
3. **创建x64后端.cn**（从现有提取）——实现后端接口，输出 x64 MASM
4. **创建arm64后端.cn**（新增 ARM64 GAS 支持）——实现后端接口，输出 ARM64 GAS
5. **串联集成**——更新 `包.cn`、`后端工厂.cn`，确保全链路可用
6. **全量验证**——编译 + 单测 + E2E + 自举（75/76/78）
7. Git 提交并推送 gitcode develop 分支

## 踩过的坑（绝对不要再踩）

1. **ARM64 后端 `emitNewObject` 必须填充接口分派区**——与 x64 后端对齐，遍历 `classInterfaces` 为每个接口写入分派表地址。遗漏此步骤会导致接口多态调用段错误。新增 ARM64 后端功能时，必须对照 x64 后端的完整实现逐行核对。
2. **平台限制用例应通过跳过机制处理**——在 `run_e2e.py` 中添加 `PLATFORM_SKIP` 字典，而非硬编码为失败或修改 expected 文件。跳过用例计入"跳过"计数，不影响"通过/失败"统计。
3. **ARM64 上 char 默认为 unsigned char**——GCC 7 在 ARM64 上 `char` 类型默认为 `unsigned char`，与 x64 的 `signed char` 不同，可能导致符号扩展相关的行为差异。涉及 char 比较/转换的代码须注意平台差异。
4. **GCC 7 严格性高于 MSVC**——`-Wall -Wextra -Werror` 下未使用形参/变量、未初始化成员等均报错。跨平台代码须同时满足 GCC 和 MSVC 的严格警告级别。
5. **跨后端功能对称性检查**——x64 后端已实现的功能（如接口分派区填充），ARM64 后端必须同步实现。新增功能后应 grep 检查两后端的实现是否对称。
6. **CN版代码生成.cn 100%硬编码 x64 MASM**——新增平台应通过抽象层（后端接口 + 后端工厂）而非直接修改现有代码。自举用例在 ARM64 上运行前必须先完成平台抽象层重构。

---

## 已知边界（下轮勿踩）

1. 外部模块按 pathStem 取名 → 跨组件同名子模块冲突取先加载者；子模块名须唯一且短（防 MASM mangling 超长）。
2. 组件内与 语义/内置、代码生成 同名的辅助函数（行类型/列表追加/函数名/参数数/解析整数）须组件内唯一化。
3. 程序化改名严禁全局字符串替换，须 git 恢复原始文件后按行/正则重建。
4. 代码生成拆分 → 0xC0000409 后台帧/GS bug，待 C++ 后端修复后再尝试（当前 971 行合规）。
5. run_e2e.py 改 UTF-16 stderr 解码：负用例断言子串从 cn.exe 诊断抓取。
6. ARM64 后端 `emitNewObject` 必须填充接口分派区（与 x64 对齐）。
7. 平台限制用例通过 `PLATFORM_SKIP` 跳过机制处理，不硬编码为失败。
8. CN版代码生成.cn 100%硬编码 x64 MASM，新增平台应通过抽象层而非直接修改。