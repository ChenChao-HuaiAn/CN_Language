# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-16——**258-a：D31 根治（方案C·单位机移交家机）+连带根治 256-a emitCopy 缺陷（win/linux_x64 两面）+ssa.cpp 交换环 erase 越界（MSVC Debug 崩）**。

### 一、本轮做了什么（写给无上下文的新会话）

1. **D31 方案C 全项落地**（用户已裁决·单位机通告 @家机 移交）：①**优化链出口位域归一化**
   （`ConstFoldPass::normalizeModuleConstWidths`——pass_manager fixpoint 收敛后全模块扫 ConstInt 文本
   与内联常量操作数，超域值按类型位宽回绕〔Rust wrapping 同构〕，域内恒等零影响）；②**arm64 硬防线**
   （`emitMovImm` wN 寄存器只发 lsl#0/16 两段——即使上游漏网也绝不产 `movk wN,lsl#32/48` 非法编码）；
   ③**`verifyConstWidths` 位宽不变量 driver 常开**（优化后无条件跑；违例=编译期硬错误 100% 机械暴露；
   i128 split/浮点/布尔不在检查面=口径闭环非妥协）；④**check_asm_width.py 双后端门禁**（E2E 宿主通道
   247 用例×2 后端 compile 出 .s 扫描：arm64 零非法编码+linux_x64「未支持操作码」静默丢值指纹零命中；
   自带正负样本自检；已注册 check_registry.json 第 11 件+ci.ps1 3.6/4 接入）。
2. **连带根治①：win emitCopy mem→mem 非法**（256-a Phi 降级遗留）：`Copy` 裸 `mov mem,mem` A2070
   雪崩——**384 用例 win 面首跑 18 处**（单位机 GCC/ARM64 绿=跨机矩阵拦截价值实证）；修复=mem↔mem
   经 eax/rax 中转（按类型宽度）。**连带根治②：linux_x64 Copy 落 default 静默丢值**（dispatch 无 Copy
   case——只出注释不搬运，比汇编失败更危险）；修复=补 emitCopy（r10 中转+窄型 64 位存+i128 双槽）。
   **连带根治③：ssa.cpp 交换环 erase 越界**（`PhiLower.SwapRingHandled` MSVC Debug 崩——ring 收集序
   沿 dst→src 链非索引序，「按 ring 序倒序删」乱序环必越界；GCC 不查=单位机静默 UB）；修复=索引降序
   排序后 erase。**三连缺陷定性**：256-a（Phi 降级/Copy 三后端）单机面验证漏网被家机面逐一捕获。**连带根治④：win 中文旗标 GBK 归一**
   （矩阵 --发布/--验证-ir 两格恒败——argv 为 ANSI/GBK 字节、源码旗标字面量为 UTF-8 字节永不命中，
   ASCII 别名=绕行非根治；修复=main 入口对 '-' 起头参数做 ansiToUtf8 归一〔已是 UTF-8 者防双转〕+
   删 --发布 重复死分支；路径参数不动〔工具链 ANSI 解释面〕）——**矩阵 48/50→50/50**。
3. **E2E 385/386 双级别回绕锚定**（新建）：超域字面量/折叠/传播形态（整32 超域回绕、乘法折叠回绕、
   整64 大数、正32 无符号除、整16 窄域回绕）——385 带「编译选项.txt」(-O3)、386 同源 -O0，
   **两份 expected 逐字节相同=折叠一致性机械锚定**。
4. **单测 +7**（ConstWidth 组）：手构违例拒绝×4/域内通过/i128+浮点+布尔豁免/全链路声明折叠域内。

### 二、验证链（全绿）

清零警告重建（MSVC /W4 /WX 警告+错误 0）+单测全量+全量 E2E（--jobs 8）+CLI 矩阵+check_asm_width
（247×2）+78_v2/79_v2 锚定链自洽+check_handoff/check_progress_sync——**最终数字见 258-a 提交信息**。
反证：emitCopy 修复前 384 复现 A2070（stash 对照）；ssa 环修复前 SwapRingHandled 复现 MSVC 断言；
check_asm_width 自检负样本命中+正样本漏判双向防御。

### 三、下一步（新会话按序）

1. **CN-Smith 500 采样复验**（M3 支柱二·arm64 编译面 0 命中已证、win 运行差分随采样轮）——本轮已跑
   500 样本 seed=20260916 arm64 编译零命中（机制面三层设防不依赖样本复现）。
2. **S1 采样轮**（待认领·验证轮角色）：cnsmith_daemon 自治循环+s269.cn 抢救入库（单位机本地件）。
3. **D28/D29 v2 欠账族**（单位机候选）/ **A12 里程碑标签**（轻量可搭车）。

### 四、诚实边界

- verifyConstWidths 常开若未来某 pass 新产超域常量=编译拒绝（设计意图=机械暴露），届时归因该 pass
  补归一化面，不得调阈值。
- Copy 的 f32/f64 浮点面未特测（Phi 降级当前只产整型 Copy）；i128 Copy 已实现双槽但无专项用例。
- D31 的 256-a 原始样本 s269.cn 不在手（单位机本地 target/ 未入库）——机制面按裁决口径根治不依赖
  样本；S1 采样轮建库后由差分网络回归兜底。
- 覆盖率红线（新代码分支）：win 无 gcov 口径——下轮跨机 coverage 覆盖（-Coverage 黄字跳过惯例）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-16 🧪 E2E 测试轮（**深度机 linux-x86_64**·不占轮次号·用户令「本机现在只做测试」；基线 4adc7a5）——**全量基线复验 + B7 波1 特性依赖盘点**（明细=plans/025 §二 2.1）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **全量 E2E 基线复验（干净态构建·v2p 强制重建防回归态残留）**：403 = **400 过 / 1 失败 / 2 跳**；单测 **1342/1342**。
2. **新发现缺陷·已定性·plans/021 §3-G T10**：384（-O3）产物 x86_64 损坏——同一二进制三跑三症状（全 0／垃圾值／死循环 1h45m·kill -9）=ASLR 非确定；**汇编铁证=-O3 .s 两处 `// 未支持操作码`**（256-a Phi 降级 Copy 链产出的拷贝 x86_64 发射器不识别→静默跳过→i/上限槽未初始化→cmp 读栈垃圾）；-O0 全对；arm64 发射器支持该形态（256-a 门禁过）。三缺陷面=优化链产出未支持形态／发射器静默跳过（93-a 同族）／IR 验证器无指令支持面校验——**待开发轮根治（与 258-a D31 同为 -O3 链建议联动）**。
3. **B7 波1 盘点（家机 C20 实施的输入数据）**：宿主-only 272 用例 × 干净 v2p 试编译 → **A=72**（v2 可编译·双测第一批候选）/ **B=129**（v2 特性缺口：parse 语法 70·sem5 stdlib/类型面 32·ir2 容器方法 14 等）/ C=53（负测·保持配对）/ **Cx=17**（负测但 v2 编过=**v2 漏检候选**）/ D=1。完整清单=plans/025 §二 2.1。
4. 顺带发现 v2p 小缺陷：asm 写文件失败仍退出码 0（仅 stderr 提示）——改进候选。

### 二、验证链（本机复现口径）

- 独立 systemd scope 跑（lessons 77·防 OOM 连坐）；E2E `--jobs 8`、单测 15.2s、盘点脚本 /tmp/e2e_inventory.py（复现法见 plans/025 §二 2.1）。
- 锚定链/valgrind 未跑（测试轮范围=全量 E2E+单测+盘点；重验证归开发轮）。

### 三、下一轮任务（按序）

1. 家机认领 C20（B7 波次路线已批准）：波1 断言升级+批次表（用本机盘点数据）→ 波2 v2 专用 79 转双测+v1 清除 → 波3+ 按 B 类缺口逐批转双测。
2. T10（384 x86_64·-O3 发射器未支持操作码静默跳过·已定性）随 258-a D31 同链联动根治。
3. 深度机待命：按用户令只做测试/探针轮，不认领开发。

### 四、诚实边界

- 盘点负测归类用目录名启发（含 拒绝/负/未实现 等）——C/Cx 边界由家机实施时逐个复核。
- B 类「其他 17」未逐个定性；锚定链/valgrind 本轮未覆盖。
## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
