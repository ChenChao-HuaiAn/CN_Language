# HANDOFF 交接文档

**交接时间**: 2026-09-09 第四十一轮（**家机 win-x64**）——按第四十轮挂账4 执行 v2self 长符号面自查，**先重建验证基线（家机 v2p.exe 系 9月5日 旧产物）再实证**：v2 win 后端两枚缺陷（超长符号无收缩 319>247、常量发射未归一）+宿主一枚同族缺陷（emitCompare imm32 域检查缺失 A2084）当轮根治，黄金对拍与宿主逐字节一致；挂账2 abi辅助.cn 删除收口。门禁全绿（构建零警告+单测 1251/1251+E2E 177 用例，败面=78/79 已知 OOM 家族零回归）。

---

## 一、本轮三枚缺陷（写给无上下文的新会话；详见 lessons.md 三条与 git 提交）

1. **v2 win 后端超长符号无收缩**（挂账4 本体， lessons 权重10）：50 汉字函数名探针（hex-mangle ≈319 字符>MASM 247）——v2p 产物 319 字符原样发射、ml64 **A2043 rc=1**（宿主第四十轮同款）；宿主产物 202 字符（$L319H 形态），两侧 mangled 原串同构实锤 v2 缺短化。修复：`符号修饰` 单点包裹 `超长收缩(符号修饰实现)`（宿主 nameMangle→shortenLongSymbol 逐环同构）。**FNV-1a 以纯算术逐位模拟**（v2 无位运算全链路——IR_位与/位或/位异或常量已备、语法/IR/后端零实现；`/ %` 正数域提取+每字节 8 次迭代异或，低频路径编译期微秒级）——**黄金对拍 `$L319HEFF9F723240DB1BE` 与宿主逐字节一致=纯算术模拟与 uint64 位运算逐位等价实证**。
2. **v2 win 后端常量发射未归一**：v2 词法原样存字面量文本（含 L/U 后缀+0x/0b/0o 前缀），win 直发——`mov rax, 0L` 触发 A2206、超 32 位十进制触发截断；linux/arm64 本地解析只认十进制（0x 静默解析为 0 潜伏缺陷）。修复：win 层 `常量立即数文本`（真/假→1/0+位模式剥离+fit32 十进制+域外 MASM hex 与宿主 uint64HexText 同构）；共用层新增 十六进制数值/解析无符号/字面量位模式/位模式十六进制 四纯函数（三后端单点）；linux/arm64 调用点换 `字面量位模式`（静态论证十进制行为逐点等价，**跨机轮动态复验**）。**组件对拍 30/30 全绿对此零暴露的根因=对拍输入面是编译器自身源码，自身源码恰好不用这些语法（结构性验证盲区，lessons 权重10）**。
3. **宿主 emitCompare imm32 域检查缺失**（A2084）：写 v2 修复代码（`高 < 2^55`）反哺实锤——宿主编译 v2 源码产物 `cmp rax, 36028797018963968` 即崩（x64 cmp reg64 仅 imm32 形态；2026-08 imul 同族修过、cmp 旁路漏——「同族修复旁路穷尽」再演）。修复：op2 域外先 `mov rcx, uint64HexText` 再 `cmp w, rcx`（emitBinary 同构）+op1 hex 归一+0x/0b/0o 前缀防御解析。

## 二、验证矩阵

- **黄金对拍**：长名探针 v2p 产物收缩符号与宿主**逐字节一致** + ml64 rc=0；
- **i64 探针全链 rc=7**：归一后 `mov rax, 14650FB0739D0383h` 与宿主同款+链接运行数值语义正确；
- **30 组件批量**（target/batch_check.py）：v2p 编译+ml64 全过；
- **新旧 v2 源码产物对拍**（worktree HEAD 重编 v2p_old vs 工作区 v2p_new，target/cmp_old_new.py）：词法/语法/语义/IR/货舱解析五包**逐字节一致**；代码生成包 328 共有函数中 324 仅确定性编号平移（strData/bb/cpcy 序号，新函数插入所致）、4=改动本体、+11 新函数/−2 删除函数账目严格吻合——**改动面外零扰动实锤**；
- **全量门禁**：构建零警告（MSVC /W4 /WX）+ 单测 1251/1251 + 全量 E2E win-x64 177 用例（败=78/79 已知 OOM 家族，零回归）。

## 三、挂账2 收口 + 遗留清理

- **abi辅助.cn 删除**（v1 遗留探针 13 行）——零引用+无用例覆盖需求（07/30 等正式 E2E 已锚定同能力）+污染代码生成包产物；文件+包.cn 挂载行移除+冒烟通过；
- 顶层遗留：commit_msg*.txt 三文件（历史提交信息误入库）git rm；test_input_api_*.txt/v2asm.obj（gitignore 级运行残留）本地删除。

## 四、挂账（按优先级）

1. **跨机轮三平台半程**：本轮改动=宿主 x64_instructions.cpp（emitCompare）+v2 代码生成四文件——宿主侧对 linux/arm64 零改动（x64_instructions.cpp 为 win 独立目录）；v2 侧 linux/arm64 的 字面量位模式 替换已静态论证等价，**深度机/单位机拉取后跑本机全量门禁动态收口**（深度机重点：组件对拍 30/30 与固定点 fix_p≡fix_s 应保持——strData 编号会平移需规范化对拍）。
2. **呈报待用户裁决（三项，见更新日志第五节）**：①v2 位运算/移位全链路缺失（规范有宿主有 v2 无，建议独立特性轮）；②v2 结构体构造字面量 `类型{字段=值}` 缺口（沿挂账3，建议随语法覆盖轮）；③v2 主.cn 路径文本匹配反斜杠脆弱（批量脚本 win 反斜杠路径→双载歧义 183 错、正斜杠 rc=0；建议路径身份归一 `/`）。
3. v2p 构建链未入库：target/rebuild_v2p.py / probe_run.py / batch_check.py / cmp_old_new.py 为本轮验证资产（target/ 不入库）——深度机侧组件对拍/固定点脚本已有同款（target/dbg/p29/ 家族），是否转正入库待用户定。

## 五、验证链（下轮接手先跑）

```bash
uname -m                                    # 环境识别（x86_64=深度机/aarch64=单位机/本行=家机win）
powershell -ExecutionPolicy Bypass -File scripts/ci.ps1    # 全量门禁（构建+单测+E2E）
# 长符号黄金对拍（win，探针在 target/probe_long/主.cn——不入库，公式=50汉字函数名）：
./target/Debug/cn.exe compile target/probe_long/主.cn --target win-x64 --output target/h.s
python target/rebuild_v2p.py && ./target/v2p.exe target/probe_long/主.cn win-x64
#   两侧产物收缩符号须逐字节一致（$L319H+16位hex）；v2p 产物 PROC 符号须全 ≤247
# 反斜杠路径复现（v2 模块系统挂账③）：
./target/v2p.exe 'CN语言编译器v2\语法\解析基础.cn' win-x64   # 反斜杠 rc=1 歧义 / 正斜杠 rc=0
```

## 六、本轮踩坑（已入 lessons.md）

1. **编译器自身源码=对拍输入面**（权重10）：组件对拍 30/30 全绿≠语法全验证——「自身源码不用某语法=该语法四层全盲」（0x 前缀/L 后缀/超长标识符均如此）；凡编译器支持的语法必须有自身源码之外的探针锚定后端发射。
2. **同族修复旁路穷尽再演**（权重8）：imul 修了 imm32 域检查、cmp 漏了——立即数编码域是全指令族约束，修一处须对全族对表；「写另一侧代码」（v2 源码新常量）是激活宿主潜伏缺陷的高概率事件。
3. **路径文本匹配平台分隔符脆弱**（权重6）：路径身份判定必须归一分隔符（统一 `/`）；跨机脚本统一 as_posix()；双形态对照实验（正反斜杠各跑）最快定位。
4. **探针长度须按成因公式核过阈值**：首轮 37 汉字探针产物 226<247 未触发缺陷——按「每汉字 6 字符」公式算够 50 汉字才复现。
