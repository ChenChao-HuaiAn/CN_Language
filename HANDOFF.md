# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十七轮（**深度系统 x86_64**）——**宿主 &&/|| 短路求值缺失根治**：上轮挂账「组件对拍 4 入口字符串池编号灰色点（未初始化读取嫌疑）」实为**宿主编译器把 &&/|| 发射为单条 And/Or 指令（全求值）**的语义缺陷指纹，本轮根因级根治（IR 生成期脱糖短路控制流，rustc 同构）。灰色点全归零（组件对拍 26/26 逐字节）+ 固定点复验 + **8 用例运行级对拍矩阵 8/8 收口**（上轮两项挂账一并完成）。门禁全绿（单测 1251/1251 + E2E 177 用例 173 过/0 败/4 跳，含新 175 短路副作用锚定）。

---

## 一、根因与根治（写给无上下文的新会话）

### 取证链（三步实锤，手法可复用）

1. **驻留轨迹插桩**（intern_api.cpp `__cn_intern`，**已永久入库**）：`CN_INTERN_TRACE=1`
   时逐条打印新驻留的 ID+内容+调用者返回地址；默认关闭零开销。v2p/cn_self 共链同一份
   运行时源码，一次插桩两侧通用。反解工具 scripts/intern_trace_resolve.py（nm 二分
   ra→CN 函数名）。
2. **轨迹 diff**：v2p/cn_self 编译 语法/包.cn 各 3265 条驻留仅 3 行 diff——`&` 在
   p 侧 ID 272、s 侧 274（与 词法分析$字符串/整64 顺序互换）——**非多驻留而是
   驻留顺序互换**，否定「多驻留/未初始化读取」假说。
3. **gdb+反汇编**：`&` 首驻留 p 侧 @解析类型、s 侧 @解析函数声明；v2p 的 解析类型
   bb11 **无条件 `call __cn_intern("*")`**——宿主 `A && B` 发射为 A/B 全求值再与；
   v2 发射为条件块短路。**驻留() 的池副作用让发射分歧可观测**（编号漂移），其余
   全部行为一致（布尔结果等价）——「良性灰色点」实为**两编译器语义分歧的指纹**。

### 根治（src/cn_compiler/ir/ir_expr.cpp visitBinaryExpr 头部）

AndAnd/OrOr 专用路径：IR 生成期脱糖**三块结构**——主块（LHS 求值→endBranch）→
{RHS 块（求值 RHS 存槽）｜默认块（Store 短路值「真」/「假」）} → 汇合块（Load i1）。
rustc HIR→THIR 的 &&→控制流脱糖同构；性能（跳过不需 RHS 求值）与安全（副作用按
短路语义）兼得。And/Or 指令保留给编译器内部纯值布尔组合（空/边界检查，语义等价）。

**两个发射契约（本轮踩坑，改动 IR 发射前必读）**：
- endBranch 的条件寄存器=挂到「块最后一条指令的最后一个操作数」，codegen
  emitTerminator 同位置读取——**条件求值后到 endBranch 之间不得夹入其他指令**
  （否则条件挂错指令、分支读错条件）。
- i1 常量文本必须是 `"真"`/`"假"`（loadOperandToX 仅识别「真」装 1，其余装 0）——
  `constant("1","i1")` 实际发射 `mov $0x0`。

## 二、验证（门禁全绿）

- 单测 **1251/1251**：LogicalIR 反转为短路形态锚定（块数≥3 + And/Or=0 + Not≥1）+
  新增 LogicalShortCircuit。
- E2E linux-x86_64 **177 用例 173 过/0 败/4 跳**：含新增 **175_短路求值副作用**
  （计数器四路矩阵：假&&/真|| 不调、真&&/假|| 各调一次，调用数=2；全求值回归即失败）。
- **灰色点归零**：v2p/cn_self 全组件对拍 **26/26 逐字节一致**（原 4 灰色点入口
  语法/包.cn、IR/包.cn、解析基础.cn、代码生成共用.cn 全归零）。
- **固定点 fix_p ≡ fix_s 逐字节**（短路发射后自举封闭性保持）。
- **8 用例运行级对拍矩阵 8/8**（119/123/126/128/130/148/150/158）：v2p 与 cn_self
  分别编译+链接+运行，asm 逐字节一致且运行 rc 一致（119=14、123/126=127、余=0）——
  上轮挂账收口。

## 三、挂账（按优先级）

1. **5 个组件文件单文件入口编译失败**（语义检查.cn/语义检查表达式.cn/代码生成三后端
   ——rc=1「未声明函数: 取语义诊断」等 137 错）：R3 拆分后包内兄弟符号对独立入口
   不可见（单文件入口无包根聚合，兄弟文件不在依赖闭包）。**worktree 基线对照实证
   与短路修复无关**（HEAD 即失败）——单文件入口的包上下文恢复机制待设计。
2. **跨机轮**：win/arm64 全量运行级复验（短路发射三后端共享 IR 层，已由共享层修复
   覆盖；单位机同步本轮改动后跑 arm64 门禁）。
3. specs/08 目录包/模块声明语义条文批注（顺延挂账）。
4. abi辅助.cn（v1 遗留探针 13 行）转正或删除。
5. v2 结构体构造字面量 `记录{...}` 语法缺口（随语法覆盖轮）。

## 四、验证链（下轮接手先跑）

```bash
uname -m                                    # 环境识别（x86_64=深度机 / aarch64=单位机）
cmake --build target/build -j8 && ./target/cn_unit_tests 2>&1 | tail -1
python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn 2>&1 | tail -3
# v2p 构建（深度机门禁=linux-x86_64；单位机=linux-arm64）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/v2p_x64
# v2p 产 asm + as + g++ 链接 cn_self（借链 v2p.o 提供标准库容器符号，-Wl,-z,muldefs，
#   cn_self.o 在前胜出；runtime objs 用 g++ -c -std=c++17 -fno-exceptions -fno-rtti
#   -DCNRT_LINUX_MAIN -Isrc 现编，intern_api.o 必须新鲜——含 CN_INTERN_TRACE 插桩）
# 驻留轨迹取证：CN_INTERN_TRACE=1 ./v2p <入口> linux-x86_64 2> trace.log
# 单用例调试：python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn --filter 175
```

## 五、本轮踩坑（已入 lessons.md 权重头部）

1. **CN &&/|| 短路求值依赖发射器正确性**——「单指令 And/Or」与「条件块」在无副作用
   时布尔等价，RHS 带副作用（驻留/调用/除法）即分歧；对拍灰色点类「良性」差异必须
   追到发射语义层定案，不能以「标签重编号」结案。
2. **endBranch 条件挂载契约 + i1 常量文本契约**（见第一节）——新增 IR 发射路径前
   先核对既有消费点（codegen）的读取位置与取值契约。
3. **cn build 不落盘 asm**（v2p 的 asm 是 cn 内部中间产物）——构建 cn_self 必须
   **运行 v2p 二进制**产 target/v2asm.s 再 as/链接；且 rc 判定独立重定向（管道后 $? 假绿，
   上轮教训本轮 `ls 产物` 双确认再次兜住一次）。
