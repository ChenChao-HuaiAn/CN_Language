# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🏃 326-a 开发轮：D38 批1=v2 负测防线补齐·词法/语法层 3 项·波次7 续·基线 5ce0b07·2026-09-18）

**侦查**（d38_scan·17 用例 v2p 直调实测）：**16 项防线缺失**（v2p 编过）/1 项已拒（313）——分组=词法/语法层 4 项+语义/模块层 12 项（批2/批3 待续）。

**批1 三项全转拒**（d38_scan 复跑 4 拒/13 缺）：
1. **323 未闭合块注释**（词法）：`/*` 无 `*/` 时原静默扫到 EOF——补 `注闭合` 判定 → `IO::打印到错误` 诊断行 + `词法错误加一()`（280-a 通道同族·主.cn 聚合守卫中止）。
2. **295 裸分号空语句**（语法）：**双层修复**——解析语句 顶部拒绝（防御纵深）+ **解析块 裸分号防御分支改拒绝**（真正拦截面——首轮实测解析语句 改点被解析块先拦·层级错位发现）；宿主诊断「预期表达式」对齐；plans/015 后独立 `;` 全非法（双分号 `;;` 同面拒绝）。
3. **296 当后分号**（语法）：解析当语句 体前加 `{` 检查——原 `;` 被防御跳过+后续语句静默吞进当体；宿主诊断「预期 '{'」对齐。

**验证**：v2 树零错误编译+单测 1353/1353+全量 E2E（详见提交信息）+**存量零误伤**（v2 树自身无裸分号=自举链实证·全量 v2 用例族全绿）。

**诚实边界**：批2/批3 待续（语义/模块层 13 项：103/109/304/305/317/325/341/343/347/350/363/372/374）；372（主带参）宿主为链接期拒绝——v2 侧入口合成链侦查更深·移批2。

### 二、下一轮（按序）

1. D38 批2（语义层主组：103/109/305/341/350/343/317）；锁被持=win 列采样。
2. T43/T46/T47/T48 待用户裁决；C24 已由深度机 324-d 复验销账 ✓。

### 三、坑与边界

- **改点层级错位**：解析语句 顶部的裸分号拒绝永远不被触达（解析块 先拦）——**「防御分支改拒绝」与「入口拒绝」须同面布置**（本轮双层皆拒=纵深）。
- **竞态观察（低频）**：预热重建 v2p 与用例兜底就绪检查可争 v2p.obj（本轮 79_v2 PASS 后 PermissionError 一次·重跑消失）——登记观察项·非本轮引入。
- v2 词法/语法模块的 `IO::打印到错误` 为内置限定名（无需导入）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-18 🏃 **337-a T53 修复收工（P0 安全·i128 元素容器追加段错误）** ＋ **331-a（T11 静默防线五项销项）集成挂起等 try-build 回签**（分支 `任务/深度机-331-T11静默防线`·头 da490f4）+ 334-a（A12 里程碑锚点）**已集成**（d03ae29）。

### 一、337-a 本轮做了什么（T53·P0 安全）

1. **真根因（实测推翻初判）**：初判=`ptrElemStride` 泛型 `T*` 未单态化 → 加临时探针实测 `src=整128* subst=整128*`（**步进本就正确**）→ 最小复现二分：指针下标写 i128 正常／i128 字段正常／**类方法 + i128 字面量实参 → SIGSEGV**（变量实参正常）→ **真根因=方法调用路径缺 i128 形参宽化**（字面量实参 IR 为窄整 i64 按值直传 → 被调方按 i128 指针解引用；构造/普通调用已修〔m45_03/316-a〕·实例方法与虚调用缺失）。
2. **修复**：单一归属 `widenI128Args`（按 `ClassMemberInfo.paramTypes` 判 128 位形参 Cast 宽化）+ **实例方法**与**虚调用**两路径接入（`handleClassCallExpr`）。
3. **验证**：族矩阵（普通/方法/虚/构造/容器）O0=O3 全对；**E2E 433 转正**；回归实弹 m44_01/m45_01 绿；全量 E2E **445=442/1/2**（唯一红=426 静态局部矩阵=**交叠面**：其修复=T50 在 331-a 分支）；单测 1353/1353（337-a 基线）。

### 二、下一轮任务（按序）

1. **331-a 集成**（等家机 win／单位机 arm64 try-build 回签：零警告构建+单测 1363+全量 E2E+**MSVC C4062 编译期守卫实证**）→ 集成后 426 转绿（T50）+445→更高；
2. **337-a 集成**（同 try-build 面·`ir.cpp/ir_oop_call.cpp` 与 331-a 同文件不同函数·rebase 易解；**集成顺序敏感**：331-a 先则 337-a 的 426 红自动转绿）；
3. T46 待用户裁决；T33/D10~D12 波次7 续；**T53 残余**=v2 侧链接失败（未生成 i128 容器实例符号·独立面）。

### 三、诚实边界

- **初判被实测推翻**（ptrElemStride 假设错·「立案描述≠根因」第五实例）——已如实记入 plans/025 §2.4 + T53 行；
- 虚调用路径**用例面未并入 433**（`导入 容器` 后 `自身.成员` 解析与安全边界交互→编译拒绝；探针实测通过）——登记后续 E2E 面补；
- v2 侧 i128 容器=链接失败（不同根因·未修）；本机无 win/arm64 工具链（try-build 依赖回签）；
- 426 红属**分支交叠面**（非回归·331-a 的 T50 修复即解）。


## 单位机 ARM64 节
## 单位机 ARM64 节

**交接时间**：2026-09-18 🏃 332-a 看板先行机械强制轮（用户令）＋ 329-a（D12 v2 词法预处理）在飞。**接手前先 `git fetch`——三机看板行常只在各自分支内，务必读远端分支的看板/plans（`git show gitcode/<分支>:三机任务看板.md`），别信本地文件。**

### 一、本轮做了什么（332-a·修复轮）

1. **用户令落地**（2026-09-18「必须强制 AI 实际工作前先修改看板填写本机任务，完成后顺便销账」）：AGENTS §8.1 新增「**看板先行**」强制条（开工第一动作＝直推看板本机行到 develop；收工集成成功后**立即**清行直推销账）＋ §8.5 直推窄通道改「**强制直推、即改即推**」（看板行/通告段/try-build 请求写进任务分支＝他机不可见＝等于没写）。
2. **check_handoff.py 新增「开工自检」**：当前分支为 `任务/*` 时看板必含该分支名，否则报错拦截「先动手后填板」——正例放行／反例拦截**双向实证**（含新结构下旧校验「🏃 行缺分支名」仍有效）。
3. **看板表结构变更**：单表三行 → **每机独立小节**（`### <机名>`+4 列单行表）——实测：单表三行相邻结构下**两机同时更新各自行 rebase 必冲突**（当日两次：本机 329-a 直推 × 深度机 331-a 连续直推）；小节间隔 4+ 行使三方合并友好。
4. **看板直推实战**（044423b）：本机 329-a 在飞行直推 develop——**发现三机看板行此前全在各自分支内、远端 develop 三行全空**（＝「三机不知彼此在干什么」的根因）。
5. plans/025 §3.20 六要素＋更新日志/HANDOFF/lessons 同步。

### 二、验证链

```
python3 scripts/check_handoff.py         # 正例：通过（退出码 0）
（反例A：看板不含当前分支名）             # 拦截 1 项「开工自检」·退出码 1
（反例B：🏃 行任务列缺分支名）             # 拦截（新结构下旧校验仍有效）·退出码 1
（还原后复跑）                           # 退出码 0
python3 scripts/integrate.py             # 集成（快速门禁——纯文档+脚本写集按 §8.3 不触发 try-build）
```

### 三、实测发现（三机协同实况·2026-09-18）

- **家机 329-a**（`任务/家机-329-D38批2`·v2 负测防线语义层 6 项）在飞；**深度机 331-a**（`任务/深度机-331-T11静默防线`·`src/` 三后端 dispatch＋IR 验证器）在飞且**已发 try-build 请求**（请家机 win／单位机 arm64 回签·头 0e879a5）。
- **单位机 329-a**（`任务/单位机-329-D12`·v2 词法预处理＋[特性] 货舱通道延伸）在飞；**主工作树有未提交改动**（并行会话在做的 `CN语言编译器v2/主.cn`／`货舱解析.cn`／`src/cn_compiler/driver/cargo_parser.cpp`／310 双编译对照——注释标注「D12（329-a）」）：**接手勿动、勿回滚**。
- **跨机轮次号撞号实锤**：家机与本机**同为 329-a**（「全表最大+1」三机同时取号必撞；「分支名唯一」只防同机）——机制缺陷待用户裁（候选：认领前先 fetch 看板行＝先到先得／机号段隔离／跳跃取号推广全机）。
- **单位机应答深度机 try-build**：待做（本 HANDOFF 落笔时未跑）。

### 四、下一轮候选

1. **应答深度机 331-a try-build**（arm64 门禁：fetch 分支→零警告构建＋单测＋E2E `--target linux-arm64`→通告段回签）。
2. **329-a 收尾**（D12：等并行会话的货舱特性延伸完成→提交→发 try-build 请求→integrate.py）。
3. C20 波2／C25（v1 残留清除·70~77＋89 用例处置）——待用户裁。
4. A12 里程碑标签（B6 已批·轻量搭车）。

### 五、诚实边界与坑

- **本轮自己也踩了同一个坑**：332-a 开工时未先直推看板（在分支里改的板）——制度修订前的旧习惯；332-a 的看板行随本批集成生效（结构与行同批）。
- 开工自检判据＝「分支名出现在看板任一行」：集成后清行再跑旧分支门禁会误报（罕见·可接受）；非 git 环境自动跳过。
- 看板结构变更＝共享契约变更：他机 rebase 按其行取值解冲突（332-a 通告段已广播）。
- **别信本地文件**：三机在飞状态必须 `git fetch` 后读**远端**分支/看板（332-a 直接教训：本地看板三行全空、实际两机在飞）。
