# HANDOFF 交接文档

**交接时间**: 2026-09-03 第七轮（③ 第二波三件套：**字符串下标 + 复合赋值五形态 +
方案A 登记点补全**，E2E 129 六步闭环绿；两项用户裁决方案A；连带根治两枚
（符号表 类型ID 双约定、检查块 双重处理嵌套语句）；65 ARM64 跳过摘除；
全量 131 用例 127 过/1 败（78 已知遗留）/3 跳零回归）

> 本轮上下文：用户令「继续后续任务」→ 按 HANDOFF 计划立项 E2E 129（字符串下标），
> 验证期按缺陷零容忍纪律探针实锤两簇缺陷，AskUserQuestion 两轮用户均裁决**方案A**：
> ① 登记点补全（推荐项）；② 复合赋值一次根治五形态（推荐项）。三件套同轮落地。

## 本轮做了什么（全部有实证）

### 一、E2E 129：③ 第二波三件套载体（六步闭环 PASS）
- **用例**：`tests/e2e/129_v2_字符串下标与复合赋值与登记补全/`（主.cn+供给.cn），
  注册 v2 闭环 `(["主.cn"], 0, True, ["供给.cn"])`——供给 映射$整64$字符串 符号
  （v2 组件只实例化 映射<整64,整64>/<整64,符号>，字符串值映射未实例化）。
- **断言 28 项**：下标 7 形态（ASCII/中文符号扩展 -28/循环求和 -376/负值补偿 228/
  二元表达式下标/参数下标/结构体字符串字段下标 -23）+ 复合赋值 7（五形态+
  嵌套右值+成员左值）+ 登记簇 8（成员打印/用户调用打印/四拼接形态/容器方法
  拼接打印/结果.值 拼接打印）。
- **验证**：宿主 sanity 先行（退出码 0，输出 题/名/词/甲——同源双编译器一致性
  锚定）→ 六步闭环 PASS → v2 产物运行时人眼审计：题/名/词/甲/全部通过。

### 二、字符串下标（宿主 A-1 语义对齐，plans/004）
- 语义权威：宿主 ir_expr.cpp:1582——UTF-8 字节视图步进 1 + `LoadPtr i8` 有符号
  扩展（双后端 movsx/ldrsb，与 C 的 s[i] 一致；65 用例 字节和=-376 实证），
  越界由调用方按 字符串长度 约束，结果类型 字符。
- v2 四层：语法 `[` 分支补记 子起点=对象索引/子终点=表达式终点（原全 0 丢失）；
  语义对象白名单（字符串标识符/字符串字段——变量结构体表→查字段类型ID），
  **其余形态与下标左值赋值显式拒绝**（半实现纪律）；IR 弹2→IR_加→IR_指针加载
  **T_字节**（新宽度码 13：ldrsb/movsx，独立于 B5 字段 T_字符 零扩展模型）；
  产物登记 T_整64。
- 探针实证：asm `ldrsb x9,[x9]`×3、断言全过退出码 0。

### 三、复合赋值五形态（用户裁决方案A：一次根治）
- **缺陷**：词法有 `+=` 单 token、解析层零分支 → 不消费 → 解析块循环保护**静默
  中断** → 块内后续语句丢弃（探针 `a += 2` 后 返回 0 消失、零诊断）；-= *= /= %=
  词法层无单 token 以双 token 进入同样断路。**上轮「词法分析.cn 冒烟语义 0 错误」
  实为假绿**——分词 在首个 += 后被截断（节点 488→556、asm 2046→2736 行实证）。
- **实现**：词法补四 token + 解析兜底统一分支 `L op= R ≡ L = L op R`——**回退重析
  左值**作右值首操作数（成员偏移解析期重标注），合成 二元+赋值 节点，**赋值/IR
  两层零改动**；标识符/成员/解引用左值全形态。
- 探针：五形态+嵌套右值+成员左值端到端全对。
- **边界**：宿主要求复合赋值数值操作数（字符串 += 拒绝），v2 语义浅检查不拦——
  用例按宿主语义用数值字段（分歧记录，v2 语义深检查 ③ 后续）。

### 四、方案A 登记点补全（缺陷①②同族复发根治）
- **实锤**（asm 探针）：`打印(名片.标题)`/`打印(取名())` → print_int 打指针值；
  `十六进制位(..)+十六进制位(..)`（符号修饰核心）→ 裸 add 指针+指针；
  `"x"+取名()` → from_int(指针) 转数字串。
- **修复**（IR生成.cn）：成员字段类型/用户函数返回（池预扫 函数返回类型们）/
  容器方法返回（签.返回文本）三登记点 + 拼接左侧末端推导兜底（普通二元节点补
  子起点/附加 标注）。
- **附带坑**：结果.值 载荷类型=合成名首实参（返回首 $ 后**全部**如"字符串$整32"）
  **须再经 段首 剥离**——漏则 T_未知 静默不登记而 .值 偏移恰好正确（极迷惑）。
- 探针：print_str×5/concat×3/零错误 from_int；`打印(取名2.值)`→「甲」（原 5493312）。

### 五、连带根治两枚（回归期揪出）
1. **符号表 类型ID 双约定**：局部/参数原存类型**节点索引**、静态/常量/函数存
   **名ID**——统一归一为名ID（守卫须 `>=0`：池索引 0 合法，`>0` 漏首个声明）。
2. **检查块 双重处理嵌套语句**（长期结构缺陷，被死代码掩盖）：父块区间线性含
   嵌套块语句——父层原始遍历+递归双重处理 → 嵌套声明泄漏注册到父层 → 复活的
   遮蔽检查**自注册误触发**（126/127 无真实遮蔽也报错）。修复：预收集嵌套体块
   区间（裸块 [子起点,自身)/当与如果 [体块.子起点,体块节点]）主循环跳过；真实
   遮蔽最小用例验证仍正确触发。**「从不触发的检查」从此按缺陷对待**。
- **65_string_index ARM64 跳过摘除**：宿主 ldrsb 符号扩展后原跳过依据失效，
  手动实测与期望逐行一致（字节和 -376），全量回归首跑通过。

### 六、门禁与冒烟
- 全量 E2E：**131 用例 127 过/1 败（78 已知遗留 OOM）/3 跳（62/69/79）零回归**
  （65 摘除后 ARM64 首次通过；129 新增绿；126/127 误报消除后绿）。单测 1196/1196。
- 词法分析.cn 冒烟**真绿**（分词 首次完整编译）。语法分析.cn 冒烟启动：
  依赖链四文件加载成功（词法→IR签名→IR容器→语法分析 8005 节点），语义 118 诊断
  → **下一缺口定位**（见下）。

## 用户决策记录（不可推翻，除非用户再裁）
1. 登记点三缺口 → 方案A 登记点补全（推荐项，含 129 同轮固化）。
2. 复合赋值 → 方案A 一次根治五形态（词法补四 token+解析统一分支；B 只 += 与
   C 回避留档两案被否）。
3. ②c 四项/B4 指针槽模型等历史裁决不变；本轮无新增语法规范（复合赋值/下标均为
   既有规范语法，v2 实现补齐）。

## 下一步（按 ③ 推进）
1. **E2E 130（指针下标读写）立项**：语法分析.cn 崩点=`整64* 位置` 出参的
   `位置[0]` 读+写（`函数 解析类型文本(向量<Token>, 整64* 位置)`——宿主
   ir_expr.cpp:1555 指针分支 `p[i] ≡ *(p+i)`，含**下标存储**——本轮语义层已
   显式拒绝，需扩展白名单至指针对象+左值地址生成支持下标节点）。
2. **IO::打印到错误/文件:: 限定名内置**（HANDOFF 前轮已预告；语法分析.cn 冒烟
   中 v2p 以 运行时错误(6) 中止=显式失败非静默）。
3. 之后逐组件冒烟续推（语义分析.cn → IR生成.cn → …）。
4. 家机（win-x64）待办：本轮 v2 改动（词法/语法/语义/IR生成+129 用例+run_e2e）
   的 MASM 侧验证与 ci.ps1 全量门禁——ARM64 已全绿，win 侧对称验证未运行（诚实边界）。

## 踩过的坑（本轮新增，接手必读——已入 lessons 高权重区）
- **登记点复发**（10.5）：登记表上线须按「字面量/标识符/成员/调用返回（内置+
  用户+容器方法）/运算产物/转换产物」六类清单验收；合成名函数拼装类型名先核对
  返回形态（首$后全部 vs 首段）。
- **静默丢句假绿**（10.0）：词法有 token 解析无分支=必静默断路；「冒烟绿」≠
  「编译对」——结论须核对节点数/函数数/asm 行数与源码规模相称（488 vs 556 铁证）。
- **检查块双重处理**（11.0）：父区间含子区间的树形结构遍历必须显式跳过嵌套区间；
  从不触发的检查=缺陷（最小正例验证会触发）。
- **池索引 0 合法**：`> 0` 守卫对索引语义错误（用 `>= 0`+节点类型判定）。
- v2p 尾部自检输出（`[sem5] 错误程序…未声明标识符: x`）是**内置语义自检**（喂坏
  源码断言能报错），非本次编译错误——读输出认准第一条 `[sem5] 正确程序`。
