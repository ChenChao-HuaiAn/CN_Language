# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（🧪 M3 采样测试轮·验证轮免锁·2026-09-17）

**任务**：用户令「本机测试轮·尤其不同采样的 M3 测试」——承接 284-a 遗留运行级终验「深度机 M3 回归轨复验（7/7→0 预期）」，本机提前以 win-x64 口径完成独立复验＋新种子采样。基线 de4c0b6（=97ca5c1 284-a 终态代码+深度机 285-a 看板认领文档提交）；深度机 🏃 285-a（380 用例改造）写集零交叠，全程免锁合规。

1. **前置确认**：git fetch 对齐 de4c0b6；`target/Debug/cn.exe` 全量重建（原产物 08:16 早于 97ca5c1 提交时刻 08:27，按「增量构建假重建陷阱」教训不轻信）；402_布尔短路矩阵O3＋402_验证IR旗标触达 E2E 双绿先行确认病灶② 根治在位。
2. **轮 A·回归轨复验（命中库 38 全量）**：总数 38｜一致 38｜分歧 0｜编译失败 0｜运行失败 0——**38 个历史命中样本全部已修（病灶② 284-a 预期 7/7→0 实证达成·win 口径）**。
3. **轮 B/C·不同采样（新种子）**：seed=2026091711 与 2026091712 各 100 新样本（避开深度机同日已用 01~03）——各自 总数 100｜一致 100｜分歧 0｜失败 0；C 轮清残留清单后复放二次验证一致。**三轮合计零分歧·零失败·零新命中·零新类别**（对照：病灶② 修复前新样本分歧率 10%、回归轨 7/7 分歧）。
4. **插曲（已闭环）**：首轮编排曾误读 `target/cnsmith/work/分歧清单.txt` 残留（mtime 09-16 19:53＝258-a 时代 A2070「mem←imm 无尺寸前缀」旧现场·diff 零分歧时不清理不覆盖该文件）致打印 1360 条假命中——逐项证伪（文件不在工作目录/汇总行为权威/复放全绿）后删除残留。**daemon 改进建议（待开发轮采纳）**：①`cnsmith_daemon` 支持 `--seed` 自定义（现写死 日期×100+轮号·三机同日必撞种子浪费算力）；②差分零分歧时也写空清单/清旧清单（防后读脚本误读）；③**daemon 的 `target/cn` 滞后基线陷阱**（深度机 285-a 教训自证）：拉取并重建() 只重建 `target/build` 而采样用 `target/cn`·HEAD 前进后不随建→假采样数据（深度机 daemon 08:50~08:53 四轮基线标 de4c0b6 仍报「分歧 17」即此假象）——daemon 应改为重建后同步刷新 `target/cn` 或直接用构建产物路径。
5. **交叉甄别（rebase 深度机 285-a 收工 6d61f19 后补·已呈报用户）**：深度机甄别称「s2026091705 仍分歧（变7=1/0·O0 对 O3 错）」；本机复核与规范核对提出**三重存疑**：①**规范判准**（plans/001 §位运算·移位量按位宽取模定义行为）手算 变7：变1=(-319<<196)+(-771)=(-319<<4)-771=-5875（与两侧变6 打印一致）；`-426 >> (-5875 mod 32 = 13)`=sar 13 位=**-1** → `1 <= -1`=假 → **变7=0 才符合规范**——即 win O0=O3=0 双对；深度机「O0 对（1）」的判定未核规范（默认 O0=参考假设·实际候选反转=「O3 对 O0 错」）。②本机 win 单样本复核 O0=O3=0（一致）。③本机出 linux-x86_64 O0/O3 .s（687/445 行）静态扫描：两份右移均为 **sar**（「shr 误发射」候选否定）·且 sar+硬件 mask（-5875&31=13）静态推导预期两侧都输出 0——**与深度机实测 linux O0=1 矛盾·静态推不出分歧源**（需深度机真机打印 O0 中间值：`(变5|425)+191`／`-426>>变1`／第二因子·再 marker/IR 对拍）。结论：**缺陷存在与否本身待定**（候选=深度机甄别口径反转／linux O0 直译路径未知缺陷／深度机甄别用编译器仍非真重建）——零容忍呈报待裁决，未根治前 s2026091705 保持在命中库回归轨。
6. **写集与清理**：入库改动＝采样日志 3 轮＋甄别注记＋本节＋更新日志本机节＋看板行；`target/m3_round_ai.py` 编排脚本用毕删除（s1705 甄别中间产物 s1705_*.s/.txt/.exe 留 target/ 待下轮甄别复用·不入库）；源码/测试/规范零改动（纯验证轮）。

**下一轮候选**：①**【缺陷·零容忍·已呈报】s2026091705「变7 单字符」分歧甄别根治**（第一步=深度机真机跑 linux O0 打印中间值锚点→按规范判准定位错侧→marker/IR 对拍根治；三机任一认领）→②daemon 三改造（--seed 自定义/零分歧清清单/target-cn 同步重建·可搭车开发轮）→③C20 波2 批B sem5 stdlib 32 上架（家机·前置已清）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 285-a 开发轮收工（看板交锁）——**380 升级 stdlib 导入形态（用户已批）+T12 回归轨独立复验甄别立案·全量 421=419/0/2 绿**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **380 升级 stdlib 导入形态**（用户已批测试文件修改·380 注释预埋的升级条件 D28+候选4 兜底均成立）：删内联随机数类约 70 行+两行导入（随机数生成器/播种扩散）——**双编译对照 PASS·校验和 31 不变**（stdlib 接口与原内联同构·调用面零变化；宿主+v2p 双侧各自走候选4 stdlib 兜底）。
2. **T12 回归轨独立复验甄别（立案不越界）**：重建编译器（含家机 284-a 修复）后——s2026091702/03/05 手动实证**已修**；**s2026091705 仍分歧**（变7=1/0·O0 对 O3 错）=家机 284-a「病灶② termCondition 字段化」修复未覆盖该形态——已在 plans/021 T12 行登记待家机按 marker 二分继续（深度机不越界家机刚根治面）。
3. **教训留痕**：daemon 采样编译器=target/cn 构建产物——HEAD 前进（家机修复推送）后**未重建则采样基线滞后**（本轮实测：重建前回归轨 7/7 复现·重建后部分已修/部分仍分歧）——重建后采样已入本轮日志。

### 二、验证链（本机复现口径）

- `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → **421=419/0/2**（家机 284-a 增 T12 用例·+380 升级改造）
- 单测 1351/1351+CLI 50/50+面覆盖 PASS（57/60）+双文档门禁
- 锚定链 416547 不变（v2 树与宿主编译器源码零改动·380 用例非 v2 闭环）
- 回归轨实证：/tmp/rg_o0、/tmp/x05o0.out（s2026091705 差异=变7 单字符）

### 三、下一轮任务（按序）

1. **家机**：T12 s2026091705 形态甄别（284-a 修复未覆盖·marker 二分继续）+280-a 撞号已改 284-a ✓（留痕完成）。
2. **深度机候选**：①380 升级已清偿②380 升级呈报项清零③T21 标识符字符集（待规范裁决）④daemon 常态化（病灶② 补丁推送后回归轨 10%→0 复验）。
3. **呈报面**：380 升级已获用户批准并实施完毕（本条闭环）。

### 四、诚实边界

- s2026091705 分歧形态的根因甄别归家机（深度机只做复验与立案·避免越界）。
- daemon 采样在重建前的两轮日志（10% 数据）=滞后基线产物·日志如实保留（教训已记本轮提交）。

## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
