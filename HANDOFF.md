# HANDOFF 交接文档

**交接时间**: 2026-09-03 第六轮（家机 win-x64）——**v2.1 语法规范变更：成员访问统一 "."，
废除 "->"**（用户提出并裁决：一级自动解引用 / 硬错误+迁移提示 / 本轮直接实施；验证期连带
揪出两处既有宿主缺陷当场根治；全量 138 用例 136 过零回归）。

## 本轮裁决依据（用户令）

- 用户提问「:: 和 . 怎么区分」→ 质疑「指针访问 CN 里也是 . 吧，没有 ->」→ 核对确认
  `->` 是 v2.0 转 C++ 风格时引入（旧版 Rust 风格无）→ 用户裁决「依照 Rust 与旧版 CN
  规范，访问形式统一 "."，去除 ->」。
- 三项实施裁决（AskUserQuestion）：**一级**自动解引用（Go 先例，多级报错）；
  残留 `->` **硬错误+迁移提示**（不留兼容别名）；**本轮直接实施**。

## 本轮已完成（全部有实证）

### 一、规范文档（变更获批后先行）
- plans/001 四处：4.4 运算符表（指针运算符 3→2）+ v2.1 变更说明块、运算符重载表
  删 `->`、4.5 优先级表 13 级后缀删 `->`、Task 2.7 记录标注「-> 已于 v2.1 废除」。
- cn-language-spec：05-二（EBNF `(实例|实例指针) "." 字段名`+示例+设计决策）、
  01b（5.6 指针运算符 3→2 + 5.7 `.` 注解 + 重载表 + 优先级表）、06（跨引用行）。
  **.zcode 与 .agents 双副本已同步**（02/04 两副本尚有历史分叉，见灰色点）。

### 二、宿主编译器（核心实现）
- **语义**：`MemberExpr.isArrow` 改名 `isDerefAccess` 并改为**语义层按对象类型置位**
  （`types::isPointer(objectType)`；解析层恒 false——语法上只剩 `.`）。structType
  判定统一：对象为指针剥一级（自身 this 专门分支并入）；多级指针（所指仍指针）报
  「多级指针不自动解引用：请显式 * 解引用一级」。refReturnLvalueBase 读标记
  （返回检查前 checkExpr 已置位——`返回 p.f`（p 局部指针）放行 / `返回 s.f`
  （s 局部值）拒，与旧 `->` 语义等价）。
- **解析**：后缀位 `->` 硬错误（消息含「成员访问已废除」+ 改写示例）；恢复策略按
  `.` 折叠 MemberExpr 防级联报错。Operator::Arrow 枚举/重载映射/ast_printer 全删。
- **IR**：逻辑零改动仅跟随改名——**例外** ir_oop_call.cpp 虚调用处改**纯类型驱动**
  剥指针（方法调用路径不经过 visitMemberExpr，标记不可靠——IrOopEmitTest.
  VirtualCallDispatch 首跑失败实证）；semantic_call clsName 同款类型驱动。

### 三、【连带揪出·当场根治】两处既有宿主缺陷（E2E 134 写回全形态锚定揪出）
- ① 嵌套成员链第一层为指针（`外层* wq; wq.内.x`）：ir_expr 嵌套分支
  `findStruct("外层*")` 不剥指针 → objSrcType 空 → decl==null → **字段读静默降级
  常量 0**（旧 `->` 同形态同缺陷；旧用例嵌套链均隔下标层走 IndexExpr 分支幸免）。
  修：lookupSrcType 后 `isPointer → pointeeOf` 剥一级。
- ② 显式解引用成员 `(*q).x`：objSrcType 分支表不认 UnaryExpr(Deref) 对象 → 同族
  静默常量 0。修：补 UnaryExpr 分支（对象=解引用 → 类型=所指）。
  （v2.1 后 `p.字段` ≡ `(*p).字段` 必须等价互换，两形态 134 四/五节锚定。）

### 四、v2 自举侧（零语义对齐工作——原 ./-> 本就同路径同语义）
- 语法分析.cn：成员分支只认 `.`；`->` 后缀位硬错误 `运行时错误(302)`（行号码面包屑
  惯例；硬错误无法进程内自检——拒绝能力由宿主 E2E 135 + v2 全源码零 `->`（128 链）
  双覆盖）。返回类型箭头分支（声明位）不受影响。
- IR容器.cn：15 处 `出->字段`/`签->字段` 改写为 `.`（出/签 均为 方法签名* 指针——
  宿主编译时走 isDerefAccess 路径与旧 `->` 同构）。

### 五、测试（E2E 先行 + 门禁）
- 新增：**134_成员访问统一点号**（读/写/自增/自减/复合赋值/嵌套链/解引用等价/值对象
  不变式，退出码 11~20 锚定）；**135_箭头成员访问拒绝**（负测，「成员访问已废除」）；
  **136_多级指针不自动解引用**（负测，「不自动解引用」）。
- 改写非法化语法 16 行：07（2）、09（12——含 `(名单+(n-1))->分数` 括号前缀形态，
  首轮 `[\w]->[\w]` 正则漏网）、28（1 类指针）。
- 单测 6 文件：isArrow 断言 → isDerefAccess；`->` 解析改拒绝负测
  （test_parser/test_parser_array_ptr）；IR/语义级嵌入源码同步 `.` 化。
- **全量门禁**：构建零警告 + 单测 **1207/1207** + E2E **138 用例 136 过 / 2 败**
  （78/79 已知遗留内存超限——78 单跑复现 4656MB 超限与上轮形态一致；全量跑偶现
  0xC0000005 为同一 OOM 波动死法，零回归）。

## 下一步（接手会话按序执行）
1. **三元运算符 `? :`**（词法分析.cn 冒烟最后截断点，规范 01b 有/v2 未实现；建议
   复用 125 短路「延迟求值+临时槽汇合」；**E2E 用例编号顺延 137+**——原计划的 134
   已被本轮成员访问统一占用）。三元后复验：词法分析.cn 冒烟 [parse] 归零+语义 0 错。
2. 语法分析.cn 冒烟（130 动因已消，三元后启动）——预计踩出下一批缺口。
3. ARM64 侧全量复验（单位机）：本轮变更（统一 ./废 ->）+ ::/真假无/前缀操作数位
   须 linux-arm64 全量 E2E 复验（单侧绿≠双侧对称）。

## 灰色点披露（待用户裁，未立案）
- ~~cn-language-spec .zcode 与 .agents 双副本在 02/04 历史分叉~~ **已收口（2026-09-03
  用户裁决「实施」）**：三向分叉定向同步——02 用 .zcode（字符串驻留条目）、04/06 用
  .agents（P3-18/23/19 细节），全目录 diff -rq 零差异；防复发纪律入 lessons
  （改任一副本必同步另一副本，提交前 diff 须零输出；分叉时逐文件对 plans/001 判
  方向，禁整目录单向覆盖）。
- v2 `打印(真)` 输出 `1`；v2 二元 `==/!=` 字符串指针比较；`*p += 1` 不支持；
  v2 整8/整16 强转白名单未覆盖；v2p 语义错误后仍继续 IR 生成（均沿用）。

## 踩过的坑（本轮新增，已入 lessons）
- **静态正则扫语法迁移会漏非 `\w` 前缀形态**（`))->分数`）——须用「去字符串/注释
  后一切 `->`」宽口径复核；**行号定位改写必须先 assert 目标行内容**（07 声明行
  被误覆盖成注释，跑挂才发现）。
- **objSrcType 分支表按对象节点形态枚举**（标识符/成员/下标/调用/二元）——新对象
  形态（解引用/指针链）落空即静默常量 0，属「防御分支静默降级」家族第三例
  （同族：BUG10b、2026-08-25 结果/可选 .值）——新形态接入时全表核查。
- **方法调用路径（visitCallExpr 查 object）不经过 visitMemberExpr**——依赖语义层
  AST 标记的 IR 分支须类型驱动（VirtualCallDispatch 首败实证）。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（build.ps1 Debug）零警告零错误；单测 **1207/1207**；
- 全量 E2E：**138 用例 136 过 / 2 败（78/79 已知遗留内存超限，形态与上轮一致）
  零回归**——134/135/136 新增全绿 + 07/09/28 改写全绿 + 128/133 自举链全绿。
