# HANDOFF - CN语言编译器项目交接文档

> **交接原因**：2026-08-14 会话结束（**阶段6「标准库」第2步已完成 ✅**——核心库 + 容器库（Task 6.1）：stdlib/核心.cn（错误码枚举 + 泛型 交换/最小/最大/断言）+ stdlib/容器.cn（向量<T>/链表<T>/栈<T>/队列<T>）。本轮修复 3 个编译器缺陷（结构体返回调用赋值 CopyStruct、x64 Store/emitCast 物理寄存器 32 位宽度），记录 1 个编译器缺陷规避（构造函数重载覆盖）。单测 987/987、E2E 32/32 全通过，构建 0 错误 0 警告）。
>
> **前序里程碑**：阶段3「OOP 与错误处理」（901/901 单测、28/28 E2E）；阶段A「Linux ARM64」（918/918 单测、28/28 E2E）；阶段B 优化补全（949/949 单测、28/28 E2E）；阶段C 寄存器分配/调试信息（964/964 单测、28/28 E2E）；reg_alloc -O2 崩溃缺陷修复（965/965 单测、28/28 E2E）；字符串转换函数命名优化（965/965 单测、28/28 E2E）；打印函数族命名统一方案C（965/965 单测、28/28 E2E）；**阶段6第1步：条件编译+数学库（987/987 单测、30/30 E2E）**；**阶段6第2步：核心库+容器库（987/987 单测、32/32 E2E，本交接）**。

## 一、我们在做什么任务

正在开发CN语言编译器——一门全中文语法的系统级编程语言，参考C++编程范式，用C++17从零编写编译器，直接生成汇编代码。当前处于**阶段6「标准库」**（plans/002-阶段6-标准库.md）：第1步（条件编译+数学库）与第2步（核心库+容器库）已完成，剩余 Task 6.2 IO库、Task 6.4 集合库、Task 6.5 字符串处理库（格式化）。

**本次任务**：阶段6第2步子任务——核心库（Task 6.1）：`stdlib/核心.cn`（错误码枚举 + 泛型 交换/最小/最大/断言）+ `stdlib/容器.cn`（向量/链表/栈/队列），全链路 E2E 验证（29_core + 30_container）。

## 二、已经完成了什么

### 2.0 本轮核心任务：核心库 + 容器库（Task 6.1）（已完成 ✅，2026-08-14）

**核心库（stdlib/核心.cn）**：

| 项 | 内容 |
|----|------|
| 错误码 | `枚举 错误码`：除零=1/越界=2/空指针=3/内存=4/文件=5/参数=6/未初始化=7/溢出=8（值域与规格书附录B一致） |
| 交换 | `泛型 <类型 T> 函数 交换(T* 左, T* 右)`：用 `左[0]` 下标写法规避行首 `*` 歧义 |
| 最小/最大 | `泛型 <类型 T> 函数 最小/最大(T 左, T 右) -> T`（编译期单态化） |
| 断言 | `函数 断言(布尔 条件, 字符串 消息)`：假条件调 `运行时错误(错误码.参数)` 终止 |
| E2E | `tests/e2e/29_core/`（主.cn + 核心.cn + 主.expected，通过） |

**容器库（stdlib/容器.cn）**：

| 类 | 字段与能力 |
|----|-----------|
| 向量<T> | T* 数据 + 整64 元素数量 + 整64 数组容量；扩容翻倍（0→4→8）；RAII 析构释放；追加/插入/删除/读取/元素/设置/清空/预留/大小/容量/为空 |
| 链表<T> | 索引链表：T* 值表 + 整64* 下一索引（-1 表链尾）+ 头/尾索引；追加/头部追加/删除头部/删除尾部/读取头部/读取尾部/清空/大小/为空 |
| 栈<T> | 独立 T* 动态数组（压入/弹出/栈顶/为空/大小） |
| 队列<T> | 独立索引链表（入队/出队/队首/清空/大小/为空） |
| E2E | `tests/e2e/30_container/`（主.cn + 容器.cn + 主.expected，通过） |

**验证结果**：构建 0 错误 0 警告（MSVC /W4 /WX）；单元测试 987/987；E2E 32/32（30+29_core+30_container）。

### 2.1 本轮修复的关键缺陷（✅ 已修复）

| # | 缺陷 | 现象 | 修复 |
|---|------|------|------|
| 1 | **结构体返回调用赋值缺陷（关键）** | `读取结果 = 数据.读取(99)` 赋值路径只存 8 字节 retbuf 地址（`mov [槽], rax`）而非 CopyStruct 12 字节内容 → `.正常`/`.值` 读到地址值、越界判断失效（链表/栈/队列全部错乱） | `ir.cpp` 赋值路径补 CallExpr 右值分支（结构体返回调用走 CopyStruct 按值拷贝，srcAddr=调用返回地址） |
| 2 | **x64 Store 物理寄存器宽度** | `mov eax, r12` 尺寸不匹配（A2022 汇编错误） | `x64_instructions.cpp` Store 路径 i32/u32 源为物理寄存器时转 32 位名（与 Load 路径对称） |
| 3 | **emitCast 物理寄存器源** | `i64→i32 截断` 等 Cast 路径 `mov eax, r12`（A2022） | `x64_instructions.cpp` emitCast 开头统一处理物理寄存器源 32 位名 |

### 2.2 本轮记录的编译器缺陷（⚠️ 规避，留待 Debug 子任务修复）

| # | 缺陷 | 现象 | 规避 | 修复方向 |
|---|------|------|------|---------|
| 1 | **构造函数重载覆盖** | 语义层 `ClassInfo.methods` 按方法名（name）索引，同名构造函数（无参/带参）互相覆盖，带参构造覆盖无参构造 → 无参调用 `向量<整32>()` 链接到带参构造体（读垃圾 rdx 当初始容量，字段写入垃圾值） | 容器库暂不用构造重载（预分配由 `预留()` 方法提供）；已移除 向量(整64 初始容量) 带参构造 | methods 表改按 sigKey（名#参数串）索引，或构造函数专用多版本存储 |
| 2 | **嵌套泛型字段 + 结构体返回方法崩溃** | 栈<T> 组合 向量<T> 字段 + 调用 `值表.追加()`（结构体返回）→ this 传递错误崩溃 0xC0000374 | 栈/队列改独立基础字段实现（T* 数组 / T* 值表 + 整64* 下一索引） | 嵌套泛型字段的 this 传递（ir_oop_call.cpp structReturn 场景） |

### 2.3 前序里程碑（已全部完成 ✅）

- 阶段0~3 全部（901/901 单测、28/28 E2E、5 项缺陷全修复）
- 阶段4 优化与 Win x64 完善（949/949 → 964/964 单测）
- 阶段5「Linux ARM64」阶段A（ARM64 后端 + 本机全链路，918/918 单测）
- reg_alloc -O2 崩溃缺陷修复（965/965 单测、28/28 E2E）
- 字符串转换函数命名优化（965/965 单测、28/28 E2E）
- 打印函数族命名统一方案C（965/965 单测、28/28 E2E）
- 阶段6第1步：条件编译+数学库（987/987 单测、30/30 E2E）

## 三、当前测试基线

| 指标 | 数值 |
|------|------|
| 单元测试 | **987/987**（Win x64） |
| E2E | **32/32**（含 29_core、30_container；Win x64） |
| 编译警告 | 0（MSVC /W4 /WX） |

## 四、关键架构约定（必须遵守）

1. **IR OOP 指令契约**：NewObject.extra=`类名|大小字节`；VirtualCall.extra=`类名.虚方法名`（operand[0]=this）；VtableAddr.extra=类名；DeleteObject.extra=类名
2. **方法链接符号 = 类名$sigKey**（sigKey=名#参数串）；虚表 dq/.quad 项与 classMethodSymbol 一致
3. **重写方法虚分派判据 = classVtableIndex >= 0**（不是 isVirtual）
4. **自身/父类/-> 一律剥指针取类类型**
5. **析构 name 规范为 ~类名**；DeleteObject 沿继承链解析实际析构名
6. **this 是第一参数**（静态无 this）；VirtualCall this 占 operand[0]
7. **i128 双槽约定**：字段偏移/stride/LoadPtr/StorePtr 按 16 字节；%vN=高64、%vN+1=低64（低地址槽）
8. **Win x64 ABI**：隐藏返回指针占 rcx；this 参与 paramOffset
9. **类实例布局**：`[vtable指针(8B) | 父类成员区 | 自身成员区]`；无虚函数类 hasVtable=false
10. **结果/可选降级**：`结果<T,E>` → 合成结构体；`可选<T>` → 合成结构体
11. **泛型实例化符号**：类名$实参；单模块文件（非 主）视为入口
12. **类对象赋值深拷贝**：禁止浅拷贝 Store 源指针（RAII double free）
13. **结构体赋值（含结构体返回调用）必须 CopyStruct**：`读取结果 = 数据.读取(99)` 右值为 CallExpr 时 srcAddr=调用返回地址，按 size 字节 rep movsb（ir.cpp 赋值路径）
14. **构造函数重载未支持**（规避）：同名构造函数在 methods 表按名覆盖，容器库不用构造重载（预留() 代替）
15. **打印函数族语义**：`打印(...)` = 结尾换行；`打印行(...)` = 不换行；`格式化(...)` = 格式化字符串。仅支持标量+字符串+字符变参
16. **条件编译指令**：`#定义`/`#如果定义(宏)`/`#否则`/`#结束如果`，行级裁剪保留行号，命令行 `-D 宏名` 注入
17. **数学库内置函数**：注册为带 `数学.` 前缀限定名，用户模块公开函数优先；`stdlib/数学.cn` 仅供导入使用
18. **运行时 .obj 清单**：cn_main.cpp compileRuntime 源文件数组 + linkExe .obj 列表必须同步
19. **标准库设计模式**：错误码枚举（`错误` 是内置构造器不可重定义）；指针解引用赋值用 `p[0]` 下标（行首 `*` 歧义）；元素按 8 字节槽存储；常量成员函数语法 `常量 函数 名() -> 类型`

## 五、踩过的坑（绝对不要再踩）

1. **打印函数族命名"行"字语义已反转**（权重15.0，✅已修复）：`打印行`=不换行（逐行连续输出语义），勿再按直觉理解为换行
2. **结构体返回调用赋值必须 CopyStruct**（新增，✅已修复）：`变量 = 结构体返回调用()` 若漏 CopyStruct 只存 8 字节地址，字段读垃圾。ir.cpp 赋值路径已补 CallExpr 右值分支——**后续新增结构体赋值场景务必检查此路径**
3. **构造函数重载会互相覆盖**（新增，⚠️规避）：语义层 methods 表按方法名索引，无参/带参同名构造函数后者覆盖前者，无参调用链接到带参体读垃圾。**标准库类不要写多个构造函数重载**（用无参构造 + 方法替代）
4. **嵌套泛型字段 + 结构体返回方法崩溃**（新增，⚠️规避）：组合类字段（栈 内 向量<T>）调用结构体返回方法（追加）this 传递错误崩溃。**组合类用独立基础字段实现**
5. **物理寄存器宽度 A2022**（新增，✅已修复）：codegen 中任何 `mov 32位寄存器, 物理寄存器` 必须转 32 位名（r12→r12d）。Load/Store/emitCast 三处已修——新增代码生成路径注意
6. **CRLF 宏名带 `\r`**（✅已修复）：终止字符集合必须含 `\r`/`\n`
7. **中文字节长度比较**（✅已修复）：`compare(0,N,"中文")` 的 N 必须是字节数
8. **内置函数与模块函数冲突**（✅已修复）：用户模块公开函数优先；stdlib 模块体内不能直调同名内置（自递归）
9. **运行时新增源文件两处同步**（✅已修复）：compileRuntime 源文件数组 + linkExe .obj 列表（x64/arm64）
10. **DeleteObject 析构符号**：子类无自身析构时符号是 `子类$~父类析构名`
11. **MASM .rdata**：ml64 用 `.const`；**GAS** 用 `.section .rodata`
12. **GAS `@` 前缀**：`@str0` 标签非法 → 用 `Lstr0`
13. **i128 返回 epilogue**：x19 保存的隐藏返回指针；恢复时仅 needHiddenRet 才 ldp x19
14. **变参展开参数类型必须与运行时函数签名一致**（权重8.5）：f32 必须 Cast 到 f64 再传参
15. **删除内置函数要全链路同步检查**：语义注册、x64/arm64 codegen 映射、E2E、单测、规范文档、lessons.md、更新日志

## 六、下一步计划

1. **阶段6「标准库」后续任务**（plans/002-阶段6-标准库.md）：
   - Task 6.2 IO库（stdlib/IO.cn + io_api.cpp 读取行/读取整数/读取浮点）
   - Task 6.4 集合库（stdlib/集合.cn：表.包含等）
   - Task 6.5 字符串处理库（stdlib/字符串.cn：长度/比较/连接/复制/查找/格式化）
   - 数学库 P1：对数/反三角/随机数（未做）
2. **Debug 子任务（编译器缺陷修复）**：
   - **构造函数重载**：ClassInfo.methods 按 sigKey 索引（当前按名覆盖，容器库已规避）
   - **嵌套泛型字段 this 传递**：栈/队列已改独立字段规避；彻底修复需查 ir_oop_call.cpp structReturn 嵌套场景
3. **阶段5 阶段B/C**（plans/002-阶段5-LinuxARM64.md）：交叉编译/QEMU 验证、ARM64 优化对齐
4. **遗留风险**：
   - `stdlib/数学.cn` 模块体内 `数学.xxx` 直调冲突（仅供外部导入使用）
   - 接口附加 vtable 预留（多接口场景，未实现）
   - ARM64 E2E 在 x86 主机需交叉工具链/QEMU
   - **x64_instructions.cpp 存量超行数**：单文件超 1000 行约束，待后续拆分

## 七、关键文件索引

| 模块 | 文件 |
|------|------|
| 核心库（新增） | [`stdlib/核心.cn`](stdlib/核心.cn)（错误码枚举 + 交换/最小/最大/断言） |
| 容器库（新增） | [`stdlib/容器.cn`](stdlib/容器.cn)（向量/链表/栈/队列，独立字段实现） |
| 结构体赋值修复 | [`src/cn_compiler/ir/ir.cpp`](src/cn_compiler/ir/ir.cpp:2589)（赋值路径 CallExpr 右值 CopyStruct） |
| 物理寄存器宽度修复 | [`src/cn_compiler/codegen/x64/x64_instructions.cpp`](src/cn_compiler/codegen/x64/x64_instructions.cpp:1058)（Store 路径）、[`x64_instructions.cpp`](src/cn_compiler/codegen/x64/x64_instructions.cpp:656)（emitCast） |
| 核心库 E2E | tests/e2e/29_core/（主.cn + 核心.cn + 主.expected） |
| 容器库 E2E | tests/e2e/30_container/（主.cn + 容器.cn + 主.expected） |
| 计划文档 | plans/002-阶段6-标准库.md（Task 6.1 已打勾 ✅） |
