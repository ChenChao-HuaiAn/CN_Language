# HANDOFF 交接文档

**交接时间**: 2026-09-07 第三十一轮下半程（深度系统 x86_64）——**P6b 宿主收口完成**：呈报一终裁 **B（Rust 语义）** 已全量实施（具名绑定+P1-1 废止+E0255 启用）、呈报二 A′ 错编根治已实施（函数链接键公式化）、宿主侧 E2E 五例+单测六项新增。门禁全绿：构建零警告 + 单测 **1249/1249** + E2E linux-x86_64 **165 用例 161 过/0 败/4 跳**（基线一致零回归）。**下一步 = plans/018 工作流 3（v2 全链，CN 源码六层）**。

---

## 一、本轮裁决与实施（用户面询结果：呈报一 = B）

1. **呈报一 B 实施（Rust 语义对齐）**：
   - visitImportDecl（semantic_decl.cpp）路径导入=具名绑定②：尾段符号真搬入本文件命名空间（绑定名 + itemAliasModules_ 来源模块完整路径=去尾段）；尾段为已加载模块（knownModules_ 消歧）→ 保持③模块导入；单段/模块重命名/未合并单文件保持旧行为；自导入（segments[0]==ownerModule）跳过绑定不构成 E0255。
   - **P1-1 废止**（semantic_call.cpp）：限定调用按「模块已加载」放行——knownModules_ = 声明 moduleName 全集 + `Program::loadedModules`（ast.hpp 新字段；driver_module.cpp 注入依赖图全部模块名+货舱 [依赖] 包名）。未加载模块仍报「未声明的标识符」。内置 prelude 限定名独立于加载判定恒可用。
   - **E0255 启用**：checkImportLocalConflicts 按现写法（路径导入=②）在 visitProgram 导入表构建后调用。
2. **呈报二 A′ 实施（错编根治）**：
   - `SemanticAnalyzer::functionLinkKey(模块名, 函数名, 签名键)` 全编译器唯一公式（semantic.hpp public）：`(模块名空/=="主"/函数名=="主"/__cn_ 前缀) ? 签名键 : 模块名$签名键`。
   - 三处同源：registerFunction 注册键（+公式键碰撞 fail fast）、checkFunctionBody 查找键（**旧按裸 sigKey 查漏跨模块条目=函数体静默跳检**，当轮修复）、ir_decl.cpp 定义侧 mangledName；调用侧 resolvedSignature=注册键（补前缀补丁块删除）；registerFunction 重载兼容循环键剥离适配。
   - resolveOverload 补：moduleName 空条目（单文件管线唯一模块）恒命中过滤器——单文件 `导入 主::版本;`+`主::版本()` 既有边界收口。
3. **B 落地暴露两缺陷当轮根治（绑定重写踩踏）**：①纯名别名重写覆盖限定调用 moduleFilter（44 实测）→ 限定调用过滤器优先；②itemAliasModules_ 按重写后名字查询误取同名片来源模块（91 实测）→ 按原绑定名查询回退重写名。
4. **测试适配（呈报一B 批准代价项，用户已批）**：E2E 52 删撞名导入行；单测 CrateIsolateNoParamQualifiedCall 删两行导入；SemanticQualifiedCallWithoutImport/-Regression 两测反转为 B 行为锚定（未加载负覆盖保留）。
5. **新增**：E2E 159（E0255 负测）/160（①×③ 遮蔽正测=A′ 错编形态 compile+link+run 锚定）/161（限定调用免导入正测）/162（定位收紧负测）/163（自动加载强语义负测）；单测 +6（E0255 四形态/FunctionLinkKeyFormula/LinkKeyDependencyEntrySameSigPureCall）。
6. **规范终裁化**：plans/001 §5.9.7 + specs/08 3.6 摘除「过渡期偏差」标注。

## 二、用户裁决状态总表（P6b 全程）

| # | 事项 | 状态 |
|---|---|---|
| 1 | P6b 总体实施方案 A 完整版（全量对齐 Rust、宿主 v2 双侧一步到位） | ✅ 已批 |
| 2 | 「模块」定位收紧（仅限包根聚合/父挂子+普通文件使用=诊断+自动加载强语义） | ✅ 已批 |
| 3 | 货舱.toml 纳入 v2 范围 | ✅ 已批（用户明示） |
| 4 | 实施步骤与提交切分三案、E2E 自 159 顺延 | ✅ 已批 |
| 5 | 呈报二：错编缺陷根治 = A′ 链接键单一公式化 | ✅ **已批已实施**（本轮） |
| 6 | 呈报一：「导入 m::符号」语义 | ✅ **终裁 B（Rust）已批已实施**（本轮，家机面询「我选Rust语言的做法」） |

## 三、验证证据

- 构建：GCC -Wall -Wextra -Werror 零警告。
- 单测：**1249/1249**（1243 基线 + 6 新增；3 个 P1-1 时代单测按裁决反转/适配）。
- E2E：linux-x86_64 **165 用例 161 过/0 败/4 跳**（跳过=62/69/78/79 平台限制，基线一致零回归；159~163 新增全绿）。
- v2 主链零影响实证：`cn build CN语言编译器v2/主.cn` 本轮改动 vs HEAD 两版产物运行输出逐行一致 rc=0。
- 诚实边界：win/arm64 复验=跨机轮（改动全在共享层 semantic/driver/ir/ast/driver_module）。

## 四、下一步（工作流 3：v2 全链，plans/018 第五节六层序）

1. **词法**：核对 `::`/`模块`/`作为`/`包`/`货舱` 记号完备（预计零改动）。
2. **语法分析**：`模块 名` 顶层声明解析（非法位置=诊断）；表达式 `::` 一般化到用户模块限定调用（含多级 `a::b::c`）；`作为` 别名真实绑定；导入项结构化（分段/形式/别名/通配符）。
3. **语义分析**：函数/结构体/常量三表按模块分桶（替换纯名平铺）；三级名称解析 + E0255/E0659 同款诊断；`公开:`/`私有:` 强制化（默认私有+交集规则）。
4. **IR 生成**：链接符号统一 `模块$名#参数串`（对齐宿主 functionLinkKey 公式）；`决议函数符号` 模块过滤（按三级）；类型/常量限定引用解析。
5. **三后端**：符号修饰逐字节对拍宿主 mangle（P7b 已支持，验证兜底）。
6. **加载器（主.cn）**：模块树加载（父挂子自动加载强语义）+ 包.cn 发现 + **货舱.toml 纯 CN 解析器**（[货舱] 名称/版本 + [依赖] 表）+ 依赖查找链。
7. **13 组件适配**：补 `公开:` 标注（可见性强制后）+ v2self 全源码跨文件同名排查 + 文件超千行拆子目录（≤1000 行纪律）。
8. **工作流 4**：v2 侧新 E2E、v2self 重编+三级锚定（组件对拍/全链规范化/字节级固定点）+ 8 用例运行级对拍+ 文档收口。

## 五、灰色点披露

- **CN语言编译器/（v1 旧自举物）4 组件 `cn check` 失败**（词法/语法/语义/IR生成 门面+语义分析/IR遍历/测试IR）：HEAD（b4dd073）即如此（stash 法实证），非本轮引入——定位收紧 isRootUnit 只试同级候选（入口文件挂自己的子目录子模块形态误判）+ 组件级既有缺口。v1 旧物不做修复；v2 真源码在 **CN语言编译器v2/**（主链构建+运行已验零影响）。
- 52_library 的 `导入 主::版本;` 为自导入形态（合法、不引入绑定名）——保留验证自导入语义。
- v2self 运行需大栈与项目根 cwd；strData 驻留 ID 宿主-v2 差 1（自洽零影响）；78/79 OOM 家族随 v2 解决。

## 六、踩过的坑（本轮新增/复验）

1. **测试前提被裁决废止时，测试是「行为反转锚」不是「须保卫的旧约」**：P1-1 两单测反转时保留负覆盖（未加载模块仍报错）——反转裁决≠删除保护。
2. **注册键改造必须 grep 全部 `functions_` 消费点**：checkFunctionBody 按裸 sigKey 查表在注册键公式化后会静默跳过函数体检查（本轮扫出即修）——改共享表的键格式，读写两侧同轮收口。
3. **绑定重写与既有重写链的次序冲突**：限定调用重写先设 moduleFilter，纯名别名重写（按绑定名查表）会二次覆盖——多来源重写链必须「先到先得」守卫 + 按原绑定名（非重写后名）查来源表。
4. HEAD 对照法（stash+重建+同命令复跑）仍是「是否本轮引入」判定金标准——本轮 4 组件 check 失败即此法实证为既有。

---
写给无上下文的新会话：接手后先读 plans/018（第五节六层序+第九节裁决记录）与本文件第四节，然后从工作流 3 第 1 层（词法核对）开始。
