# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-17——**第一百五十七轮（157-a）：波 6 收口——警告切硬错误 + 三缺陷根治 + 负测链**。
①**宿主收口**：`warnUnsafeBoundary` → `reportUnsafeBoundary`（**Error** + 文案去观察期 `[安全区边界] <kind>应在 不安全 函数 内（<detail>）`）；
②**v2 收口**：`错误不安全边界` 同款 + **错误计数不再排除**（154-a 的排除删除）；③**三缺陷同轮根治**（收口放大面）：
V2（v2 A2 数组下标写误报——253 双编译对照实证）/ V3（v2 A1 挂点全路径失效——判定改 `查语义指针变量`，v2 符号登记
指针=所指类型；下沉重挂）/ P1（宿主 parser `不安全 函数 原型;` 分号未消费）；④**用例迁移**：e2e 6 用例 47 处 +
209_v2 + **144 供给源**（盘点盲区：非入口文件——全量 387 文件复盘纠正）+ unit 13 文件 353 处（三遍形态扫描）；
⑤**负测链**：265（宿主负测）/ 266_v2（v2 负测）/ 267（双编译对照豁免正测——防过度收口）；⑥**反证 A/B 全命中**；
⑦**门禁**：单测 **1315/1315** + 全量 E2E **295 用例 293 过/2 败**（78/79 OOM 既有败面）+ 锚定链重锚 **408105 行**
（fix_p≡fix_s 自洽）+ 对拍 44/44 + 运行级 3/3；**plans/023 §十 步骤 5 ✅ = 波 6 收官**（观察期结束——安全区边界=
编译硬错误生效）。

### 一、本会话全景（111-a ~ 157-a，家机 win-x64）

| 战线 | 成果 |
|---|---|
| 111-a ~ 133-a | 文件级拆分 + 函数级七波 + win 门禁跃变 + H5/H8 族缺陷根治 |
| 134-a | 波 1：宿主 字符* 拼接误报根治（75→0 + 反证） |
| 135-a | 波 2：`plans/023` 清单 v1（11 类） |
| 136~145-a | 波 3：四族全齐 + 构造体修复（uF=泛化前置待修） |
| 150~152-a | 波 2 步骤 2（宿主 B6~B11 六类）+ 波 5 三面迁移清零（v2 树 87→0 / tests 735→0） |
| 153~156-a | 波 6：v2 挂点 9/9 面齐 |
| **157-a** | **波 6 收口**：警告切硬错误 + 三缺陷根治 + 负测链（观察期结束） |

### 二、plans/022 波次执行状态（2026-09-13 用户全部批准）

| 波 | 状态 |
|---|---|
| 波 1 修 字符* 拼接误报 | ✅ 已完成（134-a） |
| 波 2 不安全操作清单完整化 | ✅ 清单+规范（135-a）+ 宿主六类实装（150-a） |
| 波 3 资源字段统一化 | 🏃 四族全齐 + 构造体修复；**剩余=uF（泛化前置，149-a IR diff）** → 用户类泛化 → 元素级深拷（前置=波 4）→ H7/H11 |
| 波 4~8+ | ⬜ 按 §七（波 4 真 move + `复制(x)`；波 5 ✅；**波 6 ✅ 157-a 收官**） |

### 三、下一步（新会话按序）

1. **波 3 剩余：149-a IR diff**（chain 退化点 + uF 崩点）→ 泛化启用 → 元素级深拷（前置=波 4 `复制(x)`）→ H7/H11。
2. **A 组队列**（plans/021 §3-A）：A3 关键字清理（删 `自动`/`实现`）/ A4 联合体收口 / A5 六词上下文化 /
   A6 跨机-a/b（家机锁已释放——**单位机**认领实施）。
3. **波 4**：真 move 语义 + `复制(x)` 内置。
4. **D1 函数级续波**（12 超百行）。
5. **跨机轮**：156/157-a 的 v2 收口需 arm64/x64l 动态复验（win 锚定链 408105——**不跨后端比**；预期 arm64 重锚）。
6. **诚实边界**：**A5 释放=v2 面外**（`释放` 内置未在 v2 实现，266 实测「未声明标识符: 释放」）；`字符串*` 下标读
   按规范字面（仅 `字符*` 豁免）未扩面；**B7-a 子裁决**（字符* 解引用写是否豁免）仍待用户裁决；github 镜像本轮补推。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第一百零八轮（**深度机 linux-x86_64**）——**108-a D5 剩余面固化**：
赋值位结果/可选 构造器保守拒绝负测（264_v2 门禁，零源码改动）；D5 面完整处理链收口
（107-a 修复 + 108-a 固化 + 架构边界登记）。本会话累计 93-a~108-a 共 16 个开发轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **决策**：107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——
   完整实现需「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需
   联合体条件分支=面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ **保守拒绝维持 +
   负测固化**（不为其打单形态补丁；若将来真实需求出现，按已定位路径专项立项）。
2. **门禁**：E2E **264_v2_赋值位结果构造器拒绝**（v2 负测：诊断锚行「构造器 正常 须用于
   结果/可选 语境（返回位或 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——
   防将来误放行致静默截断/泄漏。
3. **D5 面完整处理链（107-a + 108-a 收口）**：
   | 形态 | 处理 |
   |---|---|
   | `返回 无`（段错误） | 107-a 修复（返回位零初始化；E2E 263 + 反证双件） |
   | `可选<T> r = 无;`（原拒绝） | 107-a 修复（语义放行 + 声明位 IR 零初始化） |
   | `r = 正常(...)` / `r = 无`（赋值位） | 108-a 固化（负测 264）+ 完整实现登记（联合体条件释放设施） |
   | `盒.r.正常`/`盒.r.值`（嵌套成员链） | 两侧一致保守=诚实边界 |
4. 全量细节：`plans/019` 第一百零八轮行 + `plans/020` **第四十七节** + `lessons.md` 108-a 段
   （预防 153）。

### 二、本轮验证（零源码改动）

- v2p md5 `b6d8c387c86a066d4c1b1e62b710756c`（与 107-a 一致）；宿主 md5
  `f1484d79b0e5e85d64550b144842208a`（零改动）
- 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）
- 无需锚定链/对拍（v2 源码未变；107-a 终值：固定点 387693 行 / 对拍 34/34）

### 三、下一轮任务（按序）

1. **D1 行数存量整改（长期，本机可实施主要面）**：宿主余 7 文件（ir_expr 2798 / semantic 2101 /
   semantic_expr 1889 / ir_oop 1380 / ir_oop_call 1154 / semantic_call 1092 / ast.hpp 1032）+
   v2 3 文件 + 19 超百行函数——按"每轮 1~2 文件"滚动（验证口径=v2p md5/固定点不变 + 全量零回归；
   族边界勘定纪律见 §四⑬）。
2. **联合体条件释放设施专项（登记）**：结果/可选 全释放面（值字段条件释放）——赋值位覆盖写、
   结果/可选 结构体字段、结果/可选 容器元素等形态在此设施上统一收口（84-e 边界 + 107-a/108-a
   定位的实施路径）。
3. **可选重构（登记）**：v2「无」的整数字面量表示（彻底方案=新节点类型「空字面量」）。
4. **登记面余项**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表缺位；
   D3（未初始化槽 preFree 观察项）/ D4（静态槽不登记自动释放=设计选择）维持登记；C2/C13=106-a
   评估完成（维持排程/登记）。
5. **跨机轮（家机 win-x64 / 单位机 arm64）**：**93-a~108-a 全量复跑**（两侧编译器已多次变更需重建；
   **注意 103-a 契约变更**：v2 诊断带行号 → 迁移后的 .expected 为准；**107-a v2 产物已变**：
   win/arm64 侧 v2p 重建即可）+ 家机立案 **148_v2 win 崩溃缺陷**（三方案待裁决）。
6. **呈报清单余项**：B3、B4、B5（待用户裁决）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；**对拍/锚定链必须独立串行且不得相互交错**（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p107x64l/run_chain107.sh` + **组件对拍** `bash target/p107x64l/cmp_107.sh`
> （口径标杆=107-a 终值；若后续轮改 v2 源码，按该轮新建 p<N>x64l 目录）。
> **手工 v2p 重建（改 v2 源码后）**：`target/cn build CN语言编译器v2/主.cn --target linux-x86_64
> --output target/audit2/v2p_linuxx64`（同时产 .o；E2E runner 指纹变更会自动重建）。
> **手工 v2 探针链**（/tmp/p92probe `diff101.sh` 逐行 diff；/tmp/p105probe `run_d5.sh <名>`：
> 宿主 build + v2p + as + g++ + 逐行 diff）：cwd 需有 `stdlib` 软链 + `target/` 目录。
> **契约变更提示（103-a）**：v2 语义诊断现带「（行=N）」后缀 → 负测用例的 `.expected` 已迁移
> （20 个）；新增 v2 负测写 expected 时须含行号（可用脚本重跑取实测）。
> **调试/验证纪律（累计 13 条）**：①v2 `IR层诊断` 会中止编译——结论须"干净版复核"；②v2 共享
> 结构体加字段 grep 全部构造点；③元素步进统一 `semantic_->typeSizeOf`；④门禁形态须区分"旧机制
> 巧合正确"；⑤反证缺失位数预计算；⑥类型位后缀消费顺序显式设计；⑦探针先做全形态矩阵；⑧共享
> 推导改动回归同族面；⑨反证未命中先疑"改动冗余"；⑩对拍独立串行；⑪契约类改动（.expected/runner/
> 规范）须 §8.4 广播 + 全量迁移；⑫契约迁移脚本须模拟 runner 的真实调用形态；⑬纯重构类改动先勘
> 定族边界依赖（105-a：static 辅助函数跨族即禁拆该边界；等价性由 v2p md5 单点证据承担）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote；**未配置 `github` 镜像
> remote**，github.com 直连超时——镜像补推需网络可达的机器代做。
```

### 五、诚实边界

- 108-a 为**零源码改动轮**（负测固化 + 决策留痕）：赋值位结果/可选 支持仍为 v2 保守拒绝
  （与宿主差异=已登记等价性缺口，非缺陷）；完整实现需联合体条件释放设施（专项登记）。
- 前轮登记维持（静态槽不登记自动释放 / D1 长期滚动 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位 /
  未初始化槽 preFree 观察项 D3 / `1.5f` 后缀缺口）；
  **B2=93-a、C6=94-a、C7=95-a（+C3）、C10=96-a、C8=97-a、C9=98-a、C11=99-a（+H4）、C12=100-a、
  C1=101-a、C4=102-a（+H5）、D2=103-a、D1 长期滚动（首波 104-a + 第二波 105-a）、
  C2/C13=106-a 评估完成、D5=107-a/108-a 收口（残余=登记边界）**（销项台账见 `plans/021` §4.2）。
- 反证注入均已恢复并以注入前 md5 复核；探针留 /tmp/p105probe、备份 /tmp/p107bak 不入库。

## 单位机 ARM64 节

**最近交接**：2026-09-14——**158-a 跨机-a/b 方案A 实施（开发轮，基线 8b43670，看板认领 4e2e635）**。
用户 2026-09-14 裁决的两缺陷（跨机-a 中文 C++ 标识符 GCC 9 构建瘫痪 / 跨机-b 字符串字典序幅值
平台分叉）本轮全部根治，门禁七面全绿 + 反证双态命中。排班 A6 销项。

### 一、本轮做了什么（写给无上下文的新会话）

1. **跨机-a 根治**：①9 名中文 C++ 标识符 ASCII 改名（5 文件 35 处，与上轮探针版同集——
   家机 148~157-a 新增面**零新增违例**）：`obj有效`→objValid / `拼接加`→concatAdd /
   `交换`→swapped / `左文本`→leftText / `右文本`→rightText / `k向量定义`→kVectorDef /
   `k映射定义`→kMapDef / `k货架类定义`→kShelfClassDef；**注释中的中文不动**（`交换` 在注释
   「交换装载」等处共存，带上下文精确替换规避）。②新增 `scripts/check_ascii_idents.py`
   扫描门禁接入 ci.ps1 **第 0 步**（快速失败）——Rust `deny(non_ascii_idents)` 同构：
   状态机剥离注释/字符串/字符字面量/原始字符串后对代码区做非 ASCII 检测（**BOM 豁免 +
   raw string 内部换行保留=行号保真**——首版两缺陷已修，教训入 lessons）；全树 **249 个
   C++ 文件重扫归零**。
2. **跨机-b 根治**：`src/runtime/string_api.cpp` `__cn_str_cmp` 归一化返回 -1/0/+1
   （`(r>0)?1:(r<0?-1:0)`——Rust `Ordering` 同构；string_api.cpp 单点覆盖宿主/v2 产物）；
   runtime.hpp 声明注释同步；函数注释补充幅值分叉实证依据。
3. **门禁七面全绿**：ASCII 门禁 249 文件合规 + **GCC 9.3 清场构建零警告 rc=0**（跨机-a
   修复实证——改名前同机 `stray '\346'` 瘫痪）+ 单测 **1315/1315** + 全量 E2E linux-arm64
   **295 用例 292 过/0 败/3 跳**（跳=62/69/79；**14_integration2 转绿** + 157-a 新增
   265/266/267 全过）+ 锚定链重锚 **555442 行**（arm64 口径；上轮 552331+3111=**157-a v2 树
   增量**〔家机 win 已重锚 408105 同源〕，非本轮所致——改名/strcmp 均不进产物 asm；
   fix_p≡fix_s 逐字节自洽+绑定自检 PASS）+ 对拍 **44/44** + 运行级 **3/3**。
4. **反证双态命中**：临时回退归一化→重建→14_integration2 实得 **56**（精确复现立案现象）；
   恢复修复版→与 expected **逐行一致 PASS**。回退/恢复均 git diff 核对。
5. **文档**：plans/014 观察项表两缺陷行 ✅ 已根治 / plans/019 §7.3 第一百五十八轮行 /
   plans/021（戳+A6 销项+台账+§5 索引+§2 arm64 口径）/ 更新日志单位机节 / lessons
   第一百五十八轮段 / 本节 / 看板收工。

### 二、验证链命令（本机复现口径）

```
# ASCII 门禁：python3 scripts/check_ascii_idents.py   （249 文件合规，退出码 0）
# 构建+单测：rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
#   cmake --build target/build -j 8（查 warning=0）→ ./target/cn_unit_tests（1315/1315）
# E2E：python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --jobs 8
#   （295 用例 292 过/0 败/3 跳；v2p/rt objs 按指纹自动现建）
# 锚定链：bash target/p146arm/chain.sh（固定点 555442 行=arm64 158-a 口径；先同步
#   cp target/audit2/*.o → target/p146arm/rt/〔rt objs 指纹重编后〕）
# 对拍：bash target/p146arm/cmp44.sh（44/44）；运行级：bash target/p146arm/runlevel.sh（3/3）
# 反证：回退 string_api.cpp 归一化（函数级完整回退）→ 重建 → cn run
#   tests/e2e/14_integration2/员工档案.cn --target linux-arm64 → 应得 56（在仓库根跑，
#   cn 以相对 cwd 找 src/runtime 源码）
```

### 三、下一轮任务（按序）

1. **跨机验证轮（待他机通告或排班）**：本轮修复的三平台复跑面——win-x64（家机）/linux-x86_64
   （深度机）按看板通告复跑全量门禁（本修复对两机为**纯改善**：跨机-a 门禁第 0 步+跨机-b
   三平台幅值归一，14_integration2 两机本已过、修复后仍过）。
2. **排班队列余项**：A3（关键字清理实施）/A4（联合体拥有型成员收口）/A5（关键字上下文化）
   ——均家机已批准待认领；C3 波次（波 3 残余=用户类泛化/元素级深拷前置波 4）。
3. 78_chain_build 通过归因顺带核查（上轮遗留，runner 内存上限行为）。

### 四、诚实边界

- 本机仅 arm64 实物验证；win-x64/linux-x86_64 门禁由家机/深度机复跑（看板已通告）；
- ci.ps1 第 0 步在本机以 python3 直跑脚本等价验证（ci.ps1 本体为 win 侧门禁，本机未跑
  PowerShell 全链）；check_ascii_idents.py 对 ci.ps1 的挂接未经 win 实跑验证（语法为
  既有模式复制，风险低）；
- 265/266_v2 负测的「故意违规」代码含裸不安全操作——ASCII 门禁只扫 C++ 源码不扫 .cn 用例，
  两者面不相交，无冲突；
- target/p146arm/ 链脚本与 rt objs 副本已同步本轮新 objs；反证期间的回退构建产物已被
  恢复版重建覆盖。
