# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-12 深夜——**第八十六~八十八轮跨机验证轮**（拉取 9403e29=深度机 86-a/
87-a/88-a，基线 efc0c8c）。本轮为**纯验证轮（零源码改动）**，含一个新缺陷的完整诊断链（待修）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **背景**：上一会话（三机协议落地）登记的「88-a win 全量门禁」并行会话在 E2E 阶段被中断
   （`target/e2e_88a_win.log` 0 字节、E2E 产物停在 23:38）；本会话接手重跑。
   构建（零警告）+ 单测 1306/1306 由前序会话完成并留日志（`target/gate_88a_win.log`）——
   源码等价（efc0c8c 为纯文档提交），故直接复用 `target/Debug/cn.exe` 重跑 E2E。
2. **验证链（全量）**：
   - **E2E 275 用例 255 过 / 20 败 / 0 跳**（日志 `target/e2e_88a_win_rerun.log`）。
     败=17 双编译对照（win 平台限制：13 既存 + 新增 243/244/246/248）+ 78/79 OOM（win 固有
     败面）+ **148_v2 崩溃（新缺陷，见二）**。
   - **锚定链复锚**：`python target/p46win/self_chain.py` → fix_p ≡ fix_s **348908 行**
     逐字节一致（新口径；55 轮 340703 → +8205=56~88-a v2 源码）+ 绑定自检
     cn_main∈cn_self46.obj ✓。
   - **运行级 3/3**（`target/p46win/run_level.py`）+ **组件对拍 34/34**
     （`target/p46win/comp_diff.py`，DIFF=0）。
   - **win 实物复验（本轮新增）**：`target/p89win/run89.py` 把 4 个双编译对照用例
     （243/244/246/248，win runner 平台守卫跳过）在 win 上手工两侧对照（宿主 build +
     v2p 编译→ml64→link），**全部 rc=255 两侧一致**——86-a 三元聚合 / 86-a 嵌套深拷 /
     87-a 静态变量全链 / 88-a 短路全链的 win 实物首次验证。

### 二、新缺陷：148_v2 win 侧崩溃（0xC0000005，待修）

**现象**：E2E 148_v2_函数返回结构体 的 v2 产物运行退出码=3221225477（0xC0000005），期望 0；
55 轮该用例通过 → **回归**。

**诊断链（本轮完成，可复现）**：
1. **复现**：`target/p89win/repro148.py`（v2p 编译→ml64→link→运行，稳定 0xC0000005）。
2. **宿主维度 bisect**（手法：4 个不同宿主编译**同一份 55 轮 v2 树**；见
   `target/p89win/try_host.py` + worktree `target/wt_base|wt86|wt87`）：
   55 轮宿主 ✓ 正常 / **86-a 宿主 ✓ 正常** / **87-a 宿主 ✗ 崩** / 当前宿主 ✗ 崩
   → **87-a 的宿主改动引入**（与 v2 源码树无关——四组同树）。
3. **最小复现**：`target/p89win/min3/主.cn`（结构体按值实参 × 嵌套调用
   `取和(造点(30, 40))`）→ cn_main 帧 **384（正常）/368（崩版）**（min/min2 不触发）。
4. **机制**：崩版该调用点**缺 retbuf 隐藏参数**——正常版 `lea rcx,[rbp-384]`（retbuf）+
   返回后 rep movsb 物化拷贝；崩版 `mov rcx,30`（实参直传）→ 被调方（造点）把实参 30
   当 retbuf 指针写入 → **0xC0000005**。
5. **深层特征（重要）**：v2 编译器的「返回物化判定」**对内存状态敏感**——同一个 v2p
   （55 轮宿主构建）在「仅源」（E2E 调用方式）与「源+平台」两种命令行参数下判定**翻转**
   （384↔368）；87-a 的静态初始化改动（emitStaticInitsAtEntry/ALIGN 8/初值直存）改变了
   win 侧 v2p 的内存布局 → 在「仅源」下稳定进入「无物化」分支。
6. **平台分叉**：linux 侧不触发（深度机 E2E 271 过含 148_v2）——**win-x64 特有的跨机轮
   拦截成果**。

**未确证（留开发轮）**：具体「未初始化读取/脆弱判定」点在 v2 源码何处（本轮验证轮零源码
改动）。**已排除**：静态布尔初值错写（两版 .data 均为 0）、诊断开关误置（两者均未输出
[ir1]/[cg1]）、查寄存器类型未登记返回 0（实现安全）、.data 字符串初值伪指针（87-a 后恒
dq 0，且 55 轮旧形态 dq "主" 反而正常）。

**三方案（呈报，待用户裁决）**：
- **甲（推荐）**：根除「内存状态依赖」——定位并修复 v2 编译器未初始化读取/脆弱判定
  （Rust 对照：rustc 确定性编译 + Miri 未初始化检测；性能零开销）。
- **乙**：确定性门禁强化——同输入重复编译逐字节一致 + 参数模式交叉比对（仅防线，不修复）。
- **丙**：三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

### 三、下一步（新会话按序）

1. **缺陷修复（需开发轮看板认领）**：按用户裁决方案执行；入口=min3 最小复现 +
   `target/p89win/` 探针脚本（不入库）；`target/wt_base|wt86|wt87`（三个宿主 worktree）
   可用于继续 bisect 到 87-a 的具体代码块。
2. 单位机 arm64 侧对 86~88-a 的跨机复验（arm64 尚未拉取本轮）。

### 四、本轮踩过的坑

- **worktree 构建需 googletest junction**：`target/wt*` 的 CMake 回退路径是
  `<worktree>/target/build/_deps/googletest-src/googletest`——建 junction 指向
  `C:/Users/ChenChao/Documents/third-party/unittest/googletest` 即可（`mklink /J`，
  用 python subprocess 调 cmd 避免 Git Bash 转义）。
- **`build` 子命令产物名**：`--output <路径>`（不带扩展）产 `<路径>`（无扩展可执行）+
  `<路径>.obj`；Windows 下执行前须改名 .exe。
- **ml64/link 直调在 Git Bash 被 `/nologo` 路径转换坑**：一律用 python subprocess 调用
  （不要经 bash 拼接命令）。
- **v2p 的 `--verbose` 需第 3 参数**（`<源> <平台> --verbose`）；「仅源」与「源+平台」
  会改变 v2p 行为（本轮缺陷的关键观察面）。
- **E2E 结果读取**：runner 输出全缓冲（重定向时中途 0 字节属正常），用产物时间戳/进程判活。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第九十三轮（**深度机 linux-x86_64**）——**93-a 类名/实例成员静态性不匹配
六形态编译期拒绝（B2 销项）**。上轮 92-a 之后，本轮把第六十一轮遗留的 B2 立案
（「类静态成员+析构写静态成员运行期崩溃」）经探针二分**定位真根因**并双侧根治——
真根因与类静态成员无关，而是 `类名.实例方法()` 形态语义层漏检。下一轮任务与验证链口径见 §三/§四。

### 一、本轮做了什么（写给无上下文的新会话）

1. **B2 真根因定位（探针四步）**：
   - ①**遗留产物复现**：本机 `target/` 保留第六十一轮探针产物 `tp`~`tp5`——`./target/tp3`
     **段错误（rc=139）**（tp4/tp5 正常）；从 `tp3.s` 符号反解（`_cn_static__计数次数` / `_计数读次数`）
     还原探针真形态=「类 计数{静态成员+析构写+读次数()方法} + 造两个() 内两对象」。
   - ②**重建探针**：当前宿主重建同形（含 `计数.读次数()`）→ 段错误稳定复现。
   - ③**变体矩阵二分**（逐要素消除）：两对象/独立函数/类静态/拷贝构造/静态方法/块作用域
     **全部排除**；**无静态成员的纯 `盒子.读值()` → 段错误**（真根因锁定）。
   - ④**结论**：B2 立案两要素（类静态成员、析构写静态）**均非根因**（tp3 探针偶然同时含
     「类静态成员」与「`类名.实例方法()`」两要素，当时归因偏移）。
2. **真根因与机制**：`类名.实例方法()` 语义层漏检——宿主 `semantic_call.cpp` 成员调用路径
   `checkExpr(对象)` 取类名类型 → `lookupClassMember` 命中 → **直接按实例方法调用生成（无 this）**
   → IR「间接调用 NULL」→ 段错误；v2 侧对象=未声明标识符（成员偏移标注静默失败）→ 退化生成
   → 输出垃圾值（`5548464235739974476`）。
3. **六形态一族（探针矩阵 M1~M10）**：①类名.实例方法()（宿主崩/v2 垃圾）②类名.实例字段读
   （既有拒绝/v2 垃圾）③类名.实例字段写（既有拒绝/v2 静默写）④实例.静态方法()（宿主崩）
   ⑤实例.静态字段（宿主静默错：读恒 0/写不生效）⑥接口名.方法()（宿主崩）⑦子类名.父类实例方法()
   （宿主崩，同①根因）。
4. **方案裁决（用户令「参考 Rust、性能第一、安全第二」）**：**甲（采纳）=编译期拒绝**——纯编译期
   零运行期开销 + 消除静默错误/崩溃；Rust E0599（关联函数不经实例）/E0061（带 self 须经实例）
   同款两形态拒绝。乙（支持 实例.静态成员=C++ 语义：需 IR 新分支+receiver 求值规则）/
   丙（仅修单形态）未采纳。裁决理由与代价分析留痕 plans/020 第三十三节。
5. **实施（宿主 4 处 + v2 1 处）**：宿主 `semantic_call.cpp` 成员调用路径新增 receiver 类别判定
   （IdentifierExpr 且 isClassType=类名语义；自身/父类 receiver 非类名→实例语义，20/21 用例
   `父类.名称`/`父类.叫声()` 不受影响）→ ①④拒绝；接口分支新增 ⑥拒绝；`semantic_expr.cpp`
   新增 ⑤拒绝（补齐既有「类名.实例成员」对侧）。v2 `语义/语义检查.cn` 新增 `检查类名成员访问`
   + 挂载 `检查表达式` 的 `节点_成员` 分支（区间线性遍历=字段读/写/方法调用全形态覆盖）；
   **遮蔽判据调试实证**=符号种类 ∈ {符号_变量, 符号_参数}（类名自身登记为 符号_结构体〈种类 4〉
   ——首版按「表查找类型ID != -1」判定永不命中的踩坑，调试注入定位）。
6. **防线**：E2E **252_类名实例成员拒绝**（宿主负测六形态）+ **252_v2_类名实例成员拒绝**
   （v2 负测三形态，`主.expected` 三诊断行固化）；单测 **+7**（六拒绝 + 一合法回归锚定）。
   **反证双件全命中**：A 宿主三处拒绝条件失效注入 → 252 失败 → 恢复（md5 复核）复绿；
   B v2 检查体 `返回 假` 注入 → 252_v2 失败 → 恢复（md5 复核）复绿。
7. 全量细节：提交说明 + `plans/019` §7.1 第九十三轮行（含立案行「已修复」标注）+ `plans/020`
   **第三十三节** + `lessons.md` 93-a 段（预防 **113~117**）+ `plans/021`（戳/数字/队列/台账/
   里程碑/索引）；锚定链脚本 `target/p93x64l/run_chain93.sh` 与组件对拍 `cmp_93.sh`（不入库）。

### 二、本轮验证链（全绿，本机复现口径）

- 清场重建零警告（`-Wall -Wextra -Werror`）+ 单测 **1313/1313**（1306+7）
- E2E **280 用例 276 过 / 0 失败 / 0 未实现 / 4 跳**（+252/252_v2；其余 278 过=零回归）
- 锚定链固定点 **362243 行**（新口径，92-a 后 361684；+559=v2 检查源码确定性）两代逐字节一致
  + 绑定自检（cn_main=0x54b255 ∈ cn_self93.o .text [0x402586, 0x54f838) + 运行级 smoke rc=0
- 组件对拍 **34/34**（DIFF=0）
- 关键产物 md5：宿主 `10607b8501aef9996ca5372e6602a590`、v2p `847e17c22ce0a32f4114ab383fcdab54`、
  cn_self93 `341d2e026dcb6df3afbaf69f450299d4`

### 三、下一轮任务（按序）

1. **跨机轮（家机 win-x64）**：91-a 浮点面实物（246/249/250）+ 92-a 实物（**251 宿主侧** + v2 手工
   对照）+ **93-a 实物**（E2E 252/252_v2——宿主改动平台无关，win 侧直接复跑即覆盖；v2p 已变需重建）
   + 88-a/87-a 遗留实物 + **148_v2 win 崩溃缺陷**（家机立案：87-a 宿主引入的 v2 编译器内存状态
   依赖，三方案待裁决）+ 全量门禁复验。
2. **跨机轮（单位机 arm64）**：浮点发射实物（250 + 92-a 面）+ arm64 侧 H1 复验 + **93-a 复跑**
   （语义层改动=平台无关）+ 全量门禁。
3. **v2 数组声明语法缺口**（`plans/021` §3 C6，92-a 登记）：`浮32[2] 表;`（规格书 3.5
   `类型[长度] 名`）v2 解析报「语句缺少分号」——节点_下标/后缀设施在，缺**声明位**数组语法；
   宿主侧数组支持完整（可作对齐参照）。
4. **v2 侧登记面（承接）**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表
   缺位；v2 诊断无行号；行数存量违规面。
5. **呈报清单余项**（前轮维持）：B3（B35 字段容器接收者——容器字段赋值语义 move vs 深拷）、
   B4（甲参数 move / 丙返回位拒绝）、B5（阶段 4 第二层第三批收口时点）、C1（阶段 4' 第三批）、
   C3（v2 静态字符语法）、C4（多文件静态初始化注入）。（完整排班队列见 `plans/021` §3。）

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p93x64l/run_chain93.sh`（v2p=E2E 产出 audit2/v2p_linuxx64 → fix_p.s →
> as+link cn_self93 → 绑定自检 → fix_s.s → cmp）+ **组件对拍** `bash target/p93x64l/cmp_93.sh`
> （34/34 DIFF=0）。
> **手工 v2 探针链**（/tmp/p92probe 或 /tmp/p93probe）：cwd 需有 `stdlib` 软链 + `target/` 目录 →
> `v2p <入口.cn> linux-x86_64` → `as target/v2asm.s -o 主.o` → `g++ -no-pie 主.o
> target/audit2/{runtime,intern_api,io_api,string_api,i128_api,math_api,input_api,file_api,time_api,
> system_api}.o` → 运行取退出码（注意：**不要**用 `target/audit2/*.o` 通配——含其它用例的
> v2asm_*.o 会 multiple definition 链接失败）。
> **反证手法**：①**先**备份**全部待注入文件** + md5 存档 → ②python 注入（记录注入前后文本，供
> 逆向恢复）→ ③重建（宿主 cmake / v2p 独立路径）→ ④跑用例应复现诊断 → ⑤恢复后**以①的 md5**
> 复核（禁与 bak 自证）；**探针/用例验证 codegen 缺陷一律 `-O0`**（O2 折叠会掩盖）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote（主仓库推送正常）；**未配置
> `github` 镜像 remote**，且 github.com 直连超时——镜像补推需网络可达的机器代做。

### 五、诚实边界

- 93-a 修复为**双侧**（宿主 4 处 + v2 1 处）；v2 侧 ④⑤⑥ 三形态**不可表达**（v2 类支持第一版无
  静态成员/接口/继承）——宿主侧拒绝已覆盖，随 v2 类能力扩展（第二版待做面）需同步接入本纪律。
- 泛型类名形态（`盒子<整32>.方法()`）宿主由模块解析既有拒绝（不崩）——非本族，未改。
- 变量名与类名同名遮蔽时按实例语义（两侧同款判定——与宿主 receiverIsTypeName 判定同构）。
- 跨机轮（win/arm64）复跑 E2E 即可覆盖 93-a 门禁（宿主改动在 semantic 层、v2 在语义层=平台无关；
  **v2p 二进制已变需重建**）；92-a 面（浮点）仍待家机/单位机实物。
- 前轮登记维持（v2 数组声明语法缺口 / v2 静态字符语法 / 多文件静态注入 / 静态槽不登记自动释放 /
  v2 诊断无行号 / 行数存量违规面 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位 / 未初始化槽 preFree
  观察项）；**B2 已于 93-a 销项**（销项台账见 `plans/021` §4.2）。
- 反证注入已全部恢复并以**注入前 md5 基准**复核（宿主 `semantic_call.cpp 58799ed0…`、
  v2 `语义检查.cn 51788a57…` 逐字节一致）；探针留 /tmp/p93probe 不入库。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
