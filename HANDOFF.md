# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十五轮收官（深度系统 x86_64）——**plans/017 呈报③方案A 实施：v2 函数重载消歧机制全链落地（用户裁决方案A）+ 新一代 v2self 自举锚定**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、呈报③方案A 实施（登记/决议/符号三层，三文件）
1. **IR容器.cn（+113 行）**：函数签名表（`函数签名个数们`/`函数签名参数串们`）+`函数参数串`（参数区间提取，指针 `*`/引用 `&` 后缀自类型节点附加重建）+`函数签名键`（空参=纯名，对齐宿主 signatureKey）+幂等登记/收集/清空。
2. **语义分析.cn（~60 行）**：`声明收集` 登记键改签名键+**函数重定义诊断**（原纯名键注册失败被 `如果 (忽略 == 0) { }` 静默吞=静默覆盖根除）；两入口清空+收集签名表；`检查表达式` 标识符/调用两分支签名表回退（取首个候选返回类型，精确决议在 IR 层）。
3. **IR生成.cn（~200 行）**：`生成函数` IR 名=签名键（三后端 符号修饰 已支持 `#参数串` 零改动）；`决议函数符号`（候选 0→纯名内置路径零变化/个数过滤→精确→字面量宽匹配→个数唯一兜底/**歧义与失败=IR层诊断 fail fast 绝不静默选**）；三张表（实参结构/引用/返回类型）**双键登记**（签名键恒有+纯名键同名唯一——既有纯名消费点零回归）；调用点集成。

### 二、fail fast 两次当场拦截（机制价值实证）
1. **参数计数假匹配**：候选参数个数初版误用实参数初始化（从未真数）→改名回归实测「决议失败」诊断 3 错中止拦住→改逗号数+1 真数。
2. **实参索引偏移**：调用节点 `[子起点]`=被调标识符（节点_标识符=10），实参从 `+1` 起→min18 探针 [d18] 插桩实证（实参0 段=[]+类型10）→假「决议歧义」拦住→修正。
- **教训**：fail fast 诊断是缺陷探测器——新机制上线时诊断触发优先怀疑机制 bug（静默版将产出错决议产物）。

### 三、验证（全绿）
1. **E2E 157_v2_函数重载消歧 双绿**：同文件重载（2/3 参+整32/整64+字面量宽匹配）+跨文件同名双版本——五断言 42/6/27/70/600 宿主与 v2 一致。注记：同名分居两文件纯名调用=宿主模块遮蔽语义（A-5），P6b 对齐项不在本轮。
2. **消歧符号 asm 实证**：`生成函数W`→`生成函数` 改名回归（源码规避手段撤销）——跨文件同名重载两签名独立符号+同签名跨文件逐字节一致。
3. **门禁**：构建零警告+单测 1240/1240+E2E **159 用例 132 过/0 败/27 平台跳过**。
4. **零回归**：九探针 v2p_re vs v2p_fix 对拍 9/9+min1 逐字节一致（单版本函数签名键=纯名，产物不变）。
5. **新一代自举锚定**：v2self_new=v2p_re 编译 v2 主.cn（191956 行 asm）链接成功；12 组件冒烟 PASS（IR容器.cn 单文件 exit=1 既有灰色点与基线诊断逐字一致非回归）；**min1/min18 产物 v2p_re vs v2self_new 逐字节一致+九探针真三方对拍 9/9**。

## 下一步
1. **T3/T4**：run_e2e.py v2 闭环参数化（23 例 linux-x86_64 解锁）/宿主构建对拍。
2. **P6b 模块系统**（远期）：模块名$ 前缀+纯名调用遮蔽语义对齐（重载消歧的完备方向，E2E 157 已注记边界）。
3. 呈报①②均已闭环；第 7~18 枚+呈报③全部收口。

## 灰色点披露（持续有效）
- IR容器.cn 单组件子链冒烟 exit=1 系既有状态（第二十二轮披露；本轮基线 v2p_fix 与 v2p_re 诊断逐字一致实证非回归），主链全绿。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径；**asm 输出 target/v2asm.s 亦相对 cwd**）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- 对拍脚本与产物在 /tmp/v2dbg/p25/（不入库；reg4.sh=真三方对拍脚本）；干净基线 v2p=target/dbg/v2p_fix。

## 踩过的坑（已入 lessons 权重 10）
- **假对拍 cwd 陷阱二度复现**：①/tmp cwd 下跑 v2self、cp 项目根 target/v2asm.s 旧产物——「逐字节一致」实为主.cn 产物自比；②对拍脚本 sed 改造把判定行改成同编译器自比（reg3.sh 9/9 假绿）。**对拍脚本每次改造后必须通读「编译→cp→判定」全链，确认两产物来自两个不同编译器**。
- **调用节点子区间约定**：`[子起点]`=被调、实参从 `[子起点+1]` 起（类型大小 折叠分支为权威先例）——消费实参节点区间的新代码必须 +1 偏移。
- **`如果 (查.正常 && 查.值 ...)` 形态不满足宿主检查器**（「访问 .值 前必须检查 .正常」不豁免短路）——必须嵌套 `如果 (查.正常) { 如果 (查.值 ...) }`。
