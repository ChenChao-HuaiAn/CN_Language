# HANDOFF 交接文档

**交接时间**: 2026-08-18（性能基线对比执行完成，plans/004 D-1 预留项闭环）

## 我们在做什么任务

CN 语言编译器「阶段7：自举」——用 CN 语言重写编译器自身（词法→语法→语义→IR→代码生成），
并用 CN 编译器编译 CN 编译器，达成**真实自举闭环**。

**上一轮（2026-08-17）核心成果**：自举从「部分自举（前端真实、末端伪汇编描述器）」升级为
**真实自举闭环**——CN 组件链 v2 完整改造（代码生成.cn 重写为真实 x64 MASM 后端）+ E2E 79
真实闭环用例验证（CN 自编译版产物与 C++ 版产物 `fc /b` 逐字节一致 + 符号自检通过）。

**本轮（2026-08-18）核心成果**：执行 plans/004 D-1 性能基线对比——自举后跑 CN 版编译器
对比 C++ 版，验证「性能达 C -O0」里程碑。新增 `scripts/bench_self_host.py` 性能测量脚本
（6 个测量点 M1~M6），实测 CN 版/C++ 版组件链编译比值 ≈1.04x（几乎 1:1），CN 版推演
6 万行 ≈12s，远优于 004 可接受边界（≤100min 硬上限 / 10min 理想目标），
**✅ 达到「性能达 C -O0」里程碑**。

## 已经完成了什么

### 1. CN 组件链 v2 完整自举改造（全部 ≤1000 行合规）

| 组件 | 行数 | 关键改造 |
|------|------|---------|
| `CN语言编译器/词法分析.cn` | 477 | 分词（标识符/61关键字/进制数字+后缀/浮点/字符串含转义/原始""/多行"""/字符含 \u{XXXX}/行注释/块注释/39运算符贪婪匹配/分隔符/行号跟踪） |
| `CN语言编译器/语法分析.cn` | 965 | 10 个二元层函数合并为层号驱动（原 1180 行） |
| `CN语言编译器/语义分析.cn` | 194 | 符号表（函数/参数/变量/类型）登记、重复声明检查、未声明标识符检查 |
| `CN语言编译器/IR生成.cn` | 997 | 双参数 `IR生成(源码, 模块名)`（模块名用于符号 mangling 前缀；原 1146 行） |
| `CN语言编译器/代码生成.cn` | 971 | **伪汇编 → 真实 x64 MASM 后端**（原 98 行描述器） |
| `CN语言编译器/包.cn` | 10 | 包入口 |

**代码生成.cn 真实 x64 MASM 后端**（对标 C++ 版 `src/cn_compiler/codegen/x64/x64_codegen.cpp`）：
- `.data` 字符串池（UTF-8 字节编码）+ `.code` 段
- `EXTERN` 声明 19+ 运行时符号（打印/字符串/内存等）
- `PROC`/`ENDP` 函数框架 + 栈帧（push r12 + push rbp / mov rbp,rsp / sub rsp,N）
- 求值栈 `[rbp-F+r12]`（r12 为栈顶指针）
- `__chkstk` 三段式栈探测（大栈帧）
- 参数寄存器映射 rcx/rdx/r8/r9 + 栈参 + 影子空间 + 16 字节对齐
- 符号 mangling `?UTF8hex@@Y码@Z`（与 C++ 版对齐）
- `END` 结尾

### 2. E2E 修复与新增（79/79 全绿）

- **74/75/76 主.cn 改双参数调用 + expected 重写**（75 采用 IR 全文 + ASM 特征摘要策略）
- **77_audit_dump**：补 expected，清理调试残留（保留 主.cn/主.expected/样例.cn）
- **77_self_host_bootstrap 半成品已删**
- **78_chain_build**：补 expected（5 行 `完成:模块名`）
- **新增 79_bootstrap_closed_loop 真实闭环用例**：
  C++ 版编译 79 主.cn -> 运行落盘 5 个 `*_链.asm`（第一次）-> ml64 汇编 5 个 .obj ->
  链接（链.obj 在前 + 入口 obj + 9 运行时 .obj）-> `cn_compiler_self.exe` -> 再次落盘
  （第二次）-> `fc /b` 逐字节比对一致 -> 符号自检（map 确认 5 链.obj 贡献符号 +
  cn_main 来自入口 obj）
- **run_e2e.py**：新增 79 闭环编排 + ML64/LINK 动态探测（覆盖 14.38/14.44 + Win Kits 最新版）

### 3. Debug 审查战果（虚假验收识别与修复）

- **识破虚假验收**：`/FORCE:MULTIPLE` 下入口 obj 在前导致组件符号全绑 C++ 版、链.obj
  零贡献、原 79 全绿为假验证
- **修复**：链接顺序（链.obj 提前）+ 新增符号保留方向自检 + 第二次落盘前删旧 .asm 防线
- **污染测试**：证明第二次落盘由 exe 内 CN 组件实时执行
- **判据纠正**：阶段7验收步骤2「产物行为一致」（第一次=C++版产物运行结果 vs
  第二次=CN自编译版运行结果，逐字节一致）

### 4. 单测与验证

- **单测**：`tests/unit/semantic/test_bootstrap_lexer.cpp` 契约断言同步 v2
  （IR生成 双参数 / 真实 MASM 断言）
- **全量回归**：构建 0 错误 0 警告（MSVC /W4 /WX）/ 单测 **1189/1189** / E2E **79/79**
- **阶段7 验收标准达成**（`plans/002-阶段7-自举.md:80`）：
  - 步骤1：C++ 版 cn.exe 编译 CN 版编译器源码（5 组件 + 包.cn）-> 可运行 ✓
  - 步骤2：CN 版编译器（cn_compiler_self.exe）再次编译自身源码 -> 产物与 C++ 版产物
    `fc /b` 逐字节一致 ✓
  - 全部 E2E 79/79 通过 ✓

### 5. 行数与产物字节数（E2E 76 实测）

- 行数：词法 2417 / 语法 1657 / 语义 82 / IR 1989 / 代码生成 7923
- 产物字节数：词法 129422 / 语法 308525 / 语义 54297 / IR 317229 / 代码生成 265384

### 6. 文档全套同步（本轮收尾）

- `plans/002-阶段7-自举.md`：7.5 描述改为「真实 x64 MASM 后端」、7.6 升级为真实闭环、
  新增 7.7 真实闭环、修正日期笔误（08-17/18 -> 实际日期 08-16/17）、验收标准标注已达成
- `plans/002 CN语言编译器实施计划.md`：总进度表阶段七打勾 ✅
- `plans/001 CN语言编译器设计规格书.md`：阶段七 4 个 `- [ ]` 全部打勾
- `plans/004-自举前置条件检查.md`：新增「2026-08-17 自举闭环修复」记录
- `README.md`：单测 1125->1189、E2E 59->79、测试基线更新
- `tests/e2e/README.md`：新增 70~79 自举用例说明
- `更新日志.md`：覆盖写入本轮自举真实闭环全链路完成
- `lessons.md`：第 10 层 2 条记录（虚假验收 9.6 / 增量编译 4.0）确认完整

### 7. 清理

- 删除 `tests/e2e/78_chain_build/` 下 15 个调试残留子目录（查AST/查IR/查Tok/查扫描主 等，
  保留 主.cn/主.expected）
- 删除 3 个历史调试 bat（asm_boot.bat/link_boot.bat/link_map.bat）——79 闭环已在
  run_e2e.py 内置 ml64/link 动态探测，bat 无独立价值
- 清理后 78 用例复验 PASS

### 8. 性能基线对比（本轮 2026-08-18，plans/004 D-1 预留项闭环）

- **新增 `scripts/bench_self_host.py`**：性能测量脚本（复用 run_e2e.py 79 闭环编排）
  - M1: C++版完整 build（cn.exe build 79主.cn，含编译+链接）
  - M2: C++版组件链落盘（运行 79 exe 第一次落盘 5 个 *_链.asm，3 次取中位数）
  - M3: ml64 汇编（5 个 .asm -> 5 个 .obj）
  - M4: link 链接（-> cn_compiler_self.exe）
  - M5: CN版组件链落盘（运行 cn_compiler_self.exe 第二次落盘，3 次取中位数）
  - M6: 正确性验证（两次落盘产物逐字节一致）
  - 计时用 `time.perf_counter()`（毫秒，3 位小数）；M2/M5 各测 3 次取中位数消除抖动
  - 找不到 ml64/link / 编译失败 / 链接失败时清晰报错并退出非零码
- **实测数据**（Release 版 cn.exe，MSVC 14.44.35207，输入 3604 行）：
  - M1: 2291.635 ms / M2: 697.714 ms / M3: 51.719 ms / M4: 45.086 ms / M5: 727.932 ms
  - M6: ✅ 两次产物逐字节一致
  - **CN版/C++版 组件链编译比值 ≈ 1.04x**（几乎 1:1）
  - CN 版推演 6 万行 ≈ **12s**，远优于 004 可接受边界（≤100min 硬上限 / 10min 理想目标）
  - **✅ 达到「性能达 C -O0」里程碑（优于 10min 理想目标）**
- **报告产物**：`target/bench/性能基线报告.md`（可复跑：`python scripts/bench_self_host.py`）
- **环境准备**：本机 gtest 源码缺失导致 CMake 配置失败，从 GitHub codeload 下载
  googletest v1.14.0 到 `C:\Users\ChenChao\Documents\third-party\unittest\googletest`
  （CMakeLists.txt 期望路径），Debug+Release 双配置构建成功

## 测试结果（最终基线）

- 构建：**0 错误 0 警告**（MSVC /W4 /WX）
- 单测：**1189/1189**（103 个测试套件）
- E2E：**79/79**（含 79_bootstrap_closed_loop 真实闭环用例）
- 性能基线：**CN版/C++版 ≈1.04x**（plans/004 D-1 达成，报告见 target/bench/性能基线报告.md）
- git：工作区干净，已推送 gitcode develop

## 当前卡在哪

无卡点。阶段7 自举真实闭环已达成，性能基线对比（D-1）已完成并推送 gitcode develop。

## 遗留问题清单（📌 已评估，非本轮缺陷）

1. **单文件编译器限制**：CN 组件链（词法/语法/语义/IR/代码生成）目前是单文件实现
   （各 ≤1000 行），未拆分多文件模块；后续若扩展需 CN 版 IR 生成跨模块符号解析
2. **bat 去留**：3 个历史调试 bat（asm_boot/link_boot/link_map）已删除——79 闭环已在
   run_e2e.py 内置 ml64/link 动态探测；如需手工复现指引可参考 run_e2e.py 的 79 编排
3. **ml64 路径探测范围**：run_e2e.py 79 闭环探测 MSVC 版本目录（14.38/14.44）+ Windows
   Kits 最新版；若环境 MSVC 版本不同需扩展探测列表
4. **向量方法符号来源**：79 闭环中向量方法符号来自入口 obj（C++ 版 cn build 产物内联），
   链.obj 只贡献 CN 组件函数符号；CN 版 IR 生成/代码生成尚未覆盖向量等标准库容器方法
   的跨模块符号解析（后续多文件组件拆分时需扩展）

## 下一步计划建议

1. **多文件组件拆分**：将 CN 组件链按模块拆分（词法/语法/语义/IR/代码生成 各自独立
   模块文件），需扩展 CN 版 IR 生成跨模块符号解析（向量方法等标准库容器符号）
2. **全程 CN 工具链**：C++ 版退化为纯引导编译器，后续编译器本体与标准库全部在 CN 源码
   中开发（自举后 CN 开发成本低）
3. ~~**性能基线对比**~~：✅ 已完成（2026-08-18，plans/004 D-1 闭环）——CN版/C++版 ≈1.04x，
   达到「性能达 C -O0」里程碑，报告见 target/bench/性能基线报告.md

## 踩过的坑绝对不要再踩（已同步 lessons.md）

- **gtest 源码缺失导致 CMake 配置失败**（本轮新增）：CMakeLists.txt 期望
  `Documents/third-party/unittest/googletest`（`${CMAKE_SOURCE_DIR}/../../third-party`），
  新机器/新环境无此目录时 `cmake -S . -B target/build` 直接 FATAL_ERROR。解决：从
  GitHub codeload 下载 googletest 源码解压到该路径（注意 v1.14.0 嵌套一层 googletest/，
  需将 src/include 上移一层）。**预防**：换机器/换环境先检查 gtest 源码路径是否存在
- **运行时 .obj 不要手动复制**（本轮新增）：cn build 的 compileRuntime 会自己编译
  target/ 下 9 个运行时 .obj（带缓存：obj 新于 cpp 则跳过）。手动复制 CMake 构建的
  .obj 到 target/ 会因时间戳较新导致缓存跳过，且 CMake 默认 /MD 与 cn build 链接
  /DEFAULTLIB:libcmt.lib 冲突（LNK2019 __imp_* 无法解析）。**预防**：让 cn build 自己
  编译运行时 .obj，不要手动复制
- **性能测量取中位数**（本轮新增）：M2/M5 单次测量波动大（冷启动/系统负载），
  3 次取中位数消除抖动，数据稳定复现
- **虚假验收链接顺序**（权重 9.6）：`/FORCE:MULTIPLE` 下重复符号保留「命令行靠前定义」，
  入口 obj 在前即覆盖链.obj -> 组件符号全绑 C++ 版、链.obj 被 LNK4006 整体忽略。
  凡 `/FORCE:MULTIPLE` 链接多份同名定义，必须验证符号保留方向（map/dumpbin）+ 做
  nochain 对照实验；自举闭环验收必须含「符号绑定 CN 产物」自检，不能只比对两次产物
- **MSVC 增量编译**（权重 4.0）：改 C++ 源后必须用正确构建目录（target/build）+ touch +
  确认 exe 时间戳更新后再跑测试；勿用 filter 误判「已修复」
- **Windows 大栈帧必须 __chkstk**：emitPrologue 栈帧 >4KB 发射三段式探测；单函数
  局部变量+打印控制在 <8KB
- **链接符号前缀规则定义侧（IR）与调用侧（语义）对称**：入口文件 主.cn 全部函数
  永不加 crate 前缀
- **E2E 迁移只改语法不改行为**：不改任何打印输出，expected 是契约
- **MSVC 增量编译时间戳粒度**：改 C++ 源后 touch（PowerShell LastWriteTime）+ 确认
  编译行再跑测试
