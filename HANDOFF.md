# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（287-a 开发轮·用户裁决方案A·2026-09-17）

**任务**：根治 T13（linux_x64 后端 O0 移位发射 i32 零扩展污染）——承接本会话前半 M3 采样测试轮的 s2026091705 定案（用户裁决「方案A」）。锁纪律：等单位机 286-a 交锁后认领（38c15fe 🏃·基线 7962579）。

1. **根治**：`LinuxX64CodeGenerator::emitShift`（linux_x64_instructions.cpp）i32 非常量路径补 `movsxd r10, r10d`——对齐 270-a T14（emitCast 整→浮）同族先例（「姊妹算符照妖镜」第 2 例：T14 修 cast 面时未审 shift 面）。免疫面实证：i8/i16 装载已 movsx、u*/i1/常量（mov reg,imm64）、shl（低 32 位）、shr（无符号）；win（sar eax）/arm64（asr w9 宽度化）/v2（D34 出口规范化）三后端零改动。
2. **验证链（全绿）**：构建零警告；单测 1351/1351；**E2E 全量 422=422/0/0**（含新 403+79 自举闭环=win 锚定链逐字节不变证明·win 后端零改动）；CLI 矩阵 50/50；面覆盖 57/60 PASS；修后探针 linux .s=4/4 sar 全部 movsxd 前置+修前修后 diff 仅 4 行插入（最小改动反证）；采样回归轨修后 38/38（无回归）。
3. **新 E2E 403_移位负值矩阵**：11 行矩阵（i32 负值变量/常量移位量 sar+shl+正值零回归+i16/i8 免疫+u32 shr+mod 取模+污染值判定面）——expected win 实跑固化·coverage_map 4.4 边界例列登记。
4. **甄别定案存档**（前半验证轮 82bdbdd）：s2026091705「变7=1/0」实为 **O3 对（0=规范值·plans/001 移位取模语义）O0 错（1）**——285-a「O0 对 O3 错」与 4ad94f6「修优化层」两次判定反转；真凶=零扩展污染非 T12 折叠路径。T12 行已在 plans/021 修正关闭、T13 行销项（287-a）。
5. **v2p 实证**：v2 对 s2026091705 的 linux asm（重建 p206c/v2p）=64 位装载+D34 槽值符号正确→sar 免疫·**v2 树零改动成立**。**附带观察项（新·未立案呈报）**：该 i32 移位掩码走 `and ecx,63`（mod 64）非 31——此样本巧合等值（13），移位量 ≥32 形态疑似 mod 语义分叉（v2 IR 类型推导面）——待独立甄别轮，本轮不动。
6. **写集**：src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp（emitShift 一处）｜tests/e2e/403_移位负值矩阵/（新）｜tests/e2e/coverage_map.md｜plans/021（戳+T12 修正+T13 销项+§5 索引+§6 瘦身）｜plans/025 §1.0｜HANDOFF/更新日志家机节｜看板。探针资产 target/p287probe/ 留档。

**踩坑与教训**：①「O0=参考实现」默认假设陷阱——O0 同样可能有直译层缺陷，规范才是唯一判准（深度机两次判定反转根因·lessons 已记）；②asm 甄别 grep 到指令名（sar）≠发射正确，必须核操作数宽度（r10 vs r10d）；③管道洗退出码再犯（check_handoff FAIL 被 `| tail &&` 洗掉·提交后才见——lessons 已有·执行面仍需警惕）；④v2p 输出路径硬编码 target/v2asm.asm·须以项目根为 cwd 位置参数调用。

**下一轮候选**：①深度机跨机项：X64L 锚定链重锚（linux 产物因 movsxd 变化）+s2026091705/403 运行级复验；②v2 移位量掩码 63/31 分支疑似 mod 语义分叉甄别（独立轮·探针=移位量 33 形态 win/v2p 对拍）；③T22 arm64 位立即数（单位机已呈报待裁）；④C20 波2 批B（家机·前置已清）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 291-a 开发轮收工（看板交锁）——**全项目文档同步至最新状态（用户令「各个文档严重落后」）+ 测试资产登记面修复 + T23 复验归零销项**。接手前先 `git fetch`。

### 一、本轮做了什么（291-a·基线 119b136·写集与家机 290-a/单位机采样轮最小交叠）

1. **活文档更新**：`README.md` 全面重写——测试基线旧口径（1317 单测/306 E2E）→ **当轮实测 1353 单测 / 423 E2E**；特性一览对账（上下文关键字 **17**、标准库 **12 模块**〔补随机数/正则〕、测试度量设施）；CLI 补 `--验证-ir`/`--opt`/`--发布` 等；**Linux 快速开始新增+实测须知「build 必须显式 `--target`（缺省目标=win-x64）」**；已知限制按 plans/021 §三 刷新。另 `tests/e2e/README.md`（编号 2~3 位/参数表补全/用例级控制文件/删「阶段零 SKIP」远古说明）+`plans/013` P7 状态更新（已达成固定点）+`plans/024` 数字口径注+`plans/005`/`plans/002-00` 现状注。
2. **用户点名 `plans/003`**：加「📌 文档状态」块 + **现状校正表**（14 行逐项对照：模式匹配/`?` 传播/迭代器/FFI/所有权借用/UAF 检测/智能指针/A 级缺陷/C 级缺口/性能口径 → 逐条现状，其中 `?` 传播=v2 侧仍缺失、映射集合仍 O(n) 等**未关闭项如实标注**）。
3. **历史文档统一状态戳 31 份**（plans 30+docs 1；四类措辞：v1 时代历史文档/已收官专项/v2 推进史/已采纳落地）+**002 索引死链修正**（阶段名"零/一A/二…"→实际文件名+"plans/"多余前缀；29 链接脚本核验全可达）+关键字 54→46+`.ai-coder`→`.zcode`。
4. **测试资产登记面修复**：coverage_map 补登记 `402_验证IR旗标触达`（283-a 本机欠账·豁免区）+`404_位运算立即数矩阵O3`（288-a 单位机欠账·4.4 边界例）——**修复前 `check_spec_coverage --strict` 红牌（孤儿 2·退出码 1）→ 修复后 PASS**；面台账统计行复核修正（🔶 20→18·机械口径 39+18+3=60）。
5. **T23 复验归零销项**（289-a 判据）：`python3 scripts/famscan.py --out /tmp/famscan_291` 实测 **0/47 命中**（16→0 达成——287-a emitShift movsxd 根治的运行级验证；**B9 制度首次「立案→根治→复跑归零」全链闭环**）；T23 从 §3-G 迁 §4.2 台账。
6. **呈报**：**402 用例编号撞车**（284-a `402_布尔短路矩阵O3` vs 283-a `402_验证IR旗标触达`·前者基线含后者仍占用）→ plans/021 §3-B 三方案（推荐 A=本机改 405）待用户裁决——**未改号**（测试文件纪律 §4）。

### 二、验证链（本机复现口径·全部当轮实测）

```
cmake --build target/build -j$(nproc)                 # 零警告（增量）
./target/cn_unit_tests                                # 1353/1353（115 套件）
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8  # 423=421/0/2
python3 scripts/check_spec_coverage.py --strict       # PASS（本轮修复；此前红牌）
python3 scripts/check_matrix_coverage.py              # PASS 57/60
python3 scripts/famscan.py --out /tmp/famscan_291     # 0/47（T23 归零判据）
python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py
```

### 三、下一轮任务（按序）

1. **用户裁决 402 撞车后**：按选中方案执行（改号/维持——测试文件纪律 §4 须用户同意）。
2. **家机 290-a（C20 双测上架）收工后**：`git fetch` 复跑全量（用例数将增）；深度机侧 C20 波2 批C/批D 候选。
3. **深度机候选**：①B8 面台账巡视续行（行 57 三后端平台面=长期格）②T21 待规范裁决 ③daemon 常态化（286-a 结构变更后 pull 重启）④T12/T13 跨机回归轨复验（家机 287-a 已根治·arm/win 侧）。
4. famscan 用例库随新立案滚动扩充（plans/026 §2.11 第 4 条）。

### 四、诚实边界

- 本轮为**文档轮**：编译器源码与测试用例文件**零改动**；实测数字=本机（linux-x86_64）口径（win 422=422/0/0·单位机 422=420/0/2 见各机节）。
- README「423 用例」含 403/404；家机 290-a 批A/批B 上架后总数再变——以 plans/021 §二 与各机 HANDOFF 为准。
- `check_spec_coverage` 红牌根因=283-a（本机）与 288-a（单位机）新用例漏登记（**非编译器缺陷**），本轮已修并实测转绿。
- `.github/workflows/ci.yml` 与 `scripts/ci.ps1` 门禁面差异（yml 为早期配置·未调全链）**未动**（非文档面·登记待办）。
- 402 双号当前不影响 runner（产物按目录名隔离）——但「用例编号」引用存在歧义，待裁决。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🧪 M3 正确性收敛采样轮收工（验证轮·不占号·不占锁）——daemon 2 轮全绿+B8 面台账 arm64 复验四行闭环+一项呈报（B10）。接手前先 `git fetch`。

### 一、本轮做了什么

1. **M3 采样 2 轮全绿**：daemon 新种子（时间戳·2609171046+）每轮 200 新样本+回归轨全量——293 样本×2 轮一致 293/分歧 0/编译失败 0/运行失败 0（月文件制正常记轮）=T22 修复零复发、零新缺陷类别。
2. **B8 面台账巡视（三矩阵三方对拍·gcc 9.3 C oracle vs CN-O0 vs CN-O3）**：
   - **行 9（T13 移位面）→✅**：famscan 同族 16 形态（静态/数组/字段/实参/返回值/三元/>>=/负字面量/INT_MIN/整64/窄型移位量/左移/链式/算术消费/数组复合赋值/局部）三方对拍全一致+403 正门单跑 PASS——287-a 根治的 arm64 运行级复验闭环。
   - **行 12（T12 布尔面）复验绿**：10 消费位×6 运行期输入矩阵（条件/值位/&&||!/三元包装/函数返回/复写传播/常量折叠/循环条件/算术嵌入）O0=O3=对拍全一致·285-a 常量折叠病灶形态不复现。
   - **行 52 连带（T16 负零）复验绿**：负零 6 形态（-变量/字面量/-(-0)/浮32）三方对拍一致·-(-0)=+0 IEEE 正确——273-a 修复 arm64 运行级确认。
   - **行 57（三后端平台面）⬜→🔶**：arm64 列 T12/T13/T16/T22 四缺陷复验闭环。
3. **呈报一项（plans/021 §3-B B10·待用户裁决）**：`a ? -b : c`（三元真分支以 -/+/!~/++/-- 开头）按后缀传播解析=解析失败须加括号。**定性非编译缺陷**（e3bb421 实现裁决与规范 01b §5.8 自洽·token 层歧义真实）；扩散矩阵 8 形态实测（假分支任意/函数调用/二元/括号起点通过·如果条件位与嵌套同面·浮点负字面量同拒）；候选方案=甲同层 `:` 前瞻消歧（推荐）/乙维持+规范明细化/丙回溯。
4. **台账文档刷新**：tests/matrix/面台账.md 行 6/9/12/52/57+覆盖统计（✅ 39→40｜⬜ 3→2）——行 52/6 系他机根治后台账未刷的漂移一并修正。

### 二、发现的问题

- **B10（呈报态·见上）**：唯一命中，非缺陷系设计裁决代价面；E2E 245 存量语料真分支全为变量=盲区实证。
- 面台账行 6/52 此前落后于根治事实（家机 279/284-a、270/272/273-a 销项后未刷台账行）——本轮一并刷新；提示：根治轮收工时台账行同步是 §B8 纪律「根治轮修复同轮转正 E2E+行标 ✅」的执行缺口。
- daemon 计数口径两观察项维持（重名合并差/-10 新样本·286-a 已登记不阻塞）。

### 三、下一轮候选

1. **M3 常态采样轮续**（用户令「多轮次不停」）：新种子采样+B8 台账候选行（现候选清零→转已立案行复验/行 58 注记刷新）。
2. **B10 裁决后实施**（若裁方案甲）：解析器 `:` 前瞻消歧+规范条文细化+E2E 转正——本机可认领开发轮。
3. A12 里程碑标签锚点（B6 已批·轻量可搭车）。

### 四、验证链（本机复现口径）

```
python3 scripts/cnsmith_daemon.py --rounds 2 --samples 200   # 293×2 全绿（月文件在案）
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --filter 403   # PASS
# 三矩阵三方对拍探针（/tmp 已清理·复现见本节命令与 plans/025 §三.12）
./target/cn build <探针.cn> --target linux-arm64 -O0/-O3 + gcc 对拍 diff 全零
python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py  # 双绿
> github 镜像本机无凭据未推（既有口径）。源码零改动→全量门禁不重跑（基线 46662c3=288-a 全绿承载）。
```

### 五、诚实边界与坑

- B10 的 v2 侧未验：本机 v2p 自举产物 CLI 为内置回归口径（无外部文件 check 通道）——待开发轮或深度机复验。
- T12/T16 修复面转正 E2E 未补（修复轮为家机写集·台账行已注记「转正 E2E 待补」）。
- T14 x64l 运行级复验仍归深度机（行 52 保持 🔶）。
- 403 expected 为 win 固化口径，本机复验以同语义三方对拍承载（行为级非逐字节）。
- 三元真分支负值探针首版触发 B10 解析失败——属「按设计拒绝」，探针已改 `(0-426)` 括号形态；勿误判为编译器缺陷。
