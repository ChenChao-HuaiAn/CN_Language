# HANDOFF 交接文档

**交接时间**: 2026-09-03 第六轮（单位机三项门禁收口 + ③ 自举对拍启动：
内置函数第一波全链落地，E2E 128 六步闭环绿；**用户审计轮揪出三缺陷当场根治**
——类型登记表重构/打印四分派/拼接双向隐式转换；全量 130 用例 125 过/1 败（78）/4 跳）

> 本轮上下文：用户令「现在是单位机（麒麟 ARM64），继续后续任务」→ 完成第五轮（家机）
> HANDOFF 列明的三项单位机待办（linux-arm64 全量复确认/118 复查/78 分诊），随后按用户
> 裁决（推荐项）启动 ③ v2 自举对拍：首组件（词法分析.cn）冒烟踩出内置函数缺口 →
> E2E 128 先行立项 → v2 四层实现 → 128 六步闭环全绿。冒烟继续推进定位到下一缺口
> （字符串下标），留 129 用例先行。

## 本轮做了什么（全部有实证）

### 一、三项单位机待办收口（全部闭环）
| # | 待办 | 结论 | 实证 |
|---|---|---|---|
| 1 | linux-arm64 全量 E2E 复确认 | **全绿**（129 用例 124 过/1 败（78）/4 平台跳过）——**127 新增 ARM64 首次运行时全绿**：A 嵌套容器/B 容器字段指针槽/C 顶层静态（GAS `.quad` `.data _cn_gstatic_N`）/D byRef——家机只对称实现未验证的部分全部运行时实证；119~126 零回归；逻辑非修正 GAS 形态绿（125） | /tmp/e2e_full.log（已收口进提交信息） |
| 2 | 118 Linux 侧输出不一致复查 | **绿**——上轮疑点消除（第四轮宿主缺陷簇改动未引入 118 回归） | 同上全量 |
| 3 | 78 Linux -11 段错误分诊 | **OOM 实锤，与 win 侧 201 内存超限同因**（v1 旧架构字符串行 IR 已知遗留，v2 完成后自然解决）。dmesg：`Killed process 78_chain_build total-vm:26623504kB anon-rss:11392060kB`（15GB 物理内存耗尽）；E2E 内 -11=分配失败解引用形态。「级联双重释放」假设**排除**：无 glibc double-free 报错（那会是 SIGABRT -6）；时间线上第三轮（第四轮宿主改动前）78 已红，非第四轮回归 | dmesg/journalctl OOM 记录 |

- 门禁基线：清场重建 GCC 零警告 + 单测 1196/1196 + 全量如上。

### 二、③ 自举对拍启动：内置函数第一波（E2E 128 载体，六步闭环绿）
- **冒烟踩缺口**：`v2p 编译 词法分析.cn` 实测——语义层 66 诊断（驻留 25/运行时错误 6/
  字符串长度 1/类型大小 1 等未声明）+ 链接期 `bl _hex(驻留)` 裸中文 hex 符号
  （undefined reference）。v2 组件自身用面统计（9 组件 6199 行 grep）：驻留 209/
  运行时错误 143/字符串比较 134/驻留文本 69/整数转字符串 63/字符串长度 52/
  字符串子串 36/类型大小 7。
- **E2E 128 立项**（`tests/e2e/128_v2_内置函数与字符串拼接/`）：立项期=**宿主通用
  路径+宿主实际输出期望**（宿主级回归）。⚠ 「空期望立项即绿」仅适用**已注册闭环
  条目**（v2p 退出码断言）；内置函数是**链接级缺口**（闭环必红）——本轮回退注册改走
  通用路径立项，实现绿后再注册闭环+期望改 `[sem5] 正确程序 错误数=0`。
- **v2 四层实现**（3 组件改动）：
  - **IR容器.cn**：`内置函数符号`（中文→`__cn_*` 映射，表驱动可扩）/`内置函数返回类型ID`
    /`类型名宽度`（原始类型表+结构体布局表）。**ABI 权威=宿主 asm**（v2p_linux.s 实查
    `bl __cn_intern` 裸 ASCII 直调；双后端符号修饰 `__cn_` 前缀直通已有，代码生成零改动）。
  - **语义分析.cn**：检查表达式 标识符/调用两分支白名单。
  - **IR生成.cn**：① 调用分支内置直调（IR_调用参数化 附加=ASCII 符号，字符串返回登记
    类型集合）；② **类型大小(类型) 编译期折叠**（实参=单标识符类型名；弹实参压栈残留后
    IR_常量整数——**操作数1 须为值文本驻留 ID**：`驻留(整数转字符串(宽度))`，传数值
    必得 0，实测踩坑）；③ **字符串拼接 +**（右末端节点字符串形态：字面量/解析节点类型ID
    三形态/内置调用返回 + 字符串寄存器集合（字面量/拼接产物/内置字符串返回三登记点），
    发射 `__cn_str_concat`，左结合链式 `"x"+a+"y"` 经集合命中）；④ **打印/打印行** 复合
    展开（逐参 `__cn_print_str/__cn_print_int`+打印尾随 `__cn_print_newline`，宿主
    ir_call 语义对齐；void 不压栈）。
- **验证**：128 六步闭环 PASS（v2p 编译→as→g++→运行退出码 0=10 类断言全过：驻留往返/
  同ID/字符串长度 UTF-8 字节/子串/整数转字符串/字符串比较/类型大小×4（原始+结构体）/
  拼接×3（字面量/变量+调用/链式）/打印族）；asm 实查 `bl __cn_*` 九种符号全直调；
  **全量回归 130 用例 125 过/1 败（78）/4 跳——119~127 零回归**。

### 二B、用户审计轮：三缺陷当场根治（同日，缺陷零容忍纪律执行）
用户质询「有没有明知缺陷而妥协绕开」——自查揪出三处（两处潜伏错误代码已探针实证、
一处明知推后），按用户裁决「全部当场根治」：
- **缺陷① 打印字符串变量错分派**：`打印(s)`（字符串变量）→ `bl __cn_print_int`
  打印指针值。根因：字符串性判定只查集合（登记点缺变量/参数）且函数级权威表 ②c
  只登容器/结果/结构体——**标量（字符串/浮点/布尔）局部与参数漏登**。修复：标量
  局部/参数入权威表 + 标识符加载登记类型码。
- **缺陷② 字符串+数值静默错误代码**：`"x"+42` 把 42 位模式当指针传 concat（运行时
  段错误）——半实现比未实现更糟。修复：**字符串寄存器集合升级为「寄存器→类型码
  登记表」**（登记点：整数/字符串字面量、标识符加载、拼接产物、内置返回、比较六运算
  与逻辑与/或/非产物 T_布尔）+ 拼接**双侧隐式转换**（非字符串侧先
  `__cn_str_from_int/float/bool`，宿主 Task 2.9 全对齐）+ 打印四分派
  （print_str/float/bool/int，布尔输出 真/假）。
- **缺陷③ 栈底负符号残留**：类型大小折叠原只弹 1 个残留（被调负符号留栈底，靠
  「相对栈顶」侥幸无害并推后——违反一劳永逸纪律）→ 弹 2 根除。
- **128 用例扩展固化**（27/28/29 失败码：`"v"+7`="v7"、`8+"w"`="8w"、`"b"+真`
  ="b真"；打印分派运行时载体：`打印(还原)`/`打印(还原," ",词ID)`）。
- **验证**：探针 p5/p6 asm 实证（print_str/from_int+concat）；128 闭环 PASS（13 类
  断言）；运行时人眼审计输出「词」「词 1」「内置函数与拼接: 全部通过」；全量回归
  见下。**边界**：无符号（正*）在 v2 类型码体系归并 T_整64（打印/转换按 int——
  类型系统级现状，③ 后续）；浮点字面量求值 v2 未实现（浮点分派经变量/参数类型登记
  已就位）。

### 三、首组件冒烟结论（③ 下一缺口定位）
- 词法分析.cn（291 行）：**语义 0 错误**（白名单生效）；IR 生成崩点=「字节值」函数的
  **字符串下标 `源码[位置]`**——v2 未实现语法（plans/014 P7b 后续清单「下标」项，
  v2 解析后缀链无 `[` 分支）。**下一轮：E2E 129 用例先行**（字符串下标语义=宿主
  65_string_index 对齐，注意该用例 ARM64 平台跳过的 char 符号扩展差异）再实现，
  之后继续逐组件冒烟（语法分析.cn 911 行）。

## 用户决策记录（不可推翻，除非用户再裁）
1. 三项收口后启动 ③ 首组件对拍（本轮 AskUserQuestion 用户选推荐项）。
2. ②c 四项/B4 指针槽模型等历史裁决不变；本轮无新增语法规范。

## 下一步（按 ③ 推进）
1. **E2E 129（字符串下标）立项**：宿主语义对齐（`字符串[i]` 返回什么类型/越界语义
   查 spec 02 与宿主 65 用例）→ v2 语法（解析后缀链 `[` 分支/下标节点）→ IR（指针+
   偏移字节加载——复用 B5 窄读宽度码）→ 双后端 → 词法分析.cn 冒烟续推。
2. 逐组件冒烟：语法分析.cn → 语义分析.cn → … → 主.cn 全链（对拍模式：v2p' 编译同
   输入 asm 与 v2p 对拍，对标 79 闭环）。
3. 后续可预期缺口（v2 组件用面统计）：字符串连接 `+` 数值侧隐式转换（128 只做字符串
   侧）、IO::打印到错误/文件:: 限定名内置（主.cn/IR生成.cn 用）、三元运算符、数组。
4. 家机（win-x64）待办：本轮 v2 改动（语义/IR生成/IR容器+128 用例+run_e2e.py）的
   MASM 侧验证与 ci.ps1 全量门禁——ARM64 已全绿，win 侧对称实现未运行验证（诚实边界）。

## 踩过的坑（本轮新增，接手必读）

- **「按用面缩窄」的半实现最危险**（用户审计轮核心教训）：只做「载体覆盖的子集」时，
  判定逻辑若对未覆盖形态**错误放行**（如 拼接判定命中但转换缺失），产物是静默错误代码
  而非报错——比未实现更糟。正确姿势：未覆盖形态要么显式拒绝（诊断），要么补全语义；
  「登记制」类机制（类型/符号表）上线时登记点必须覆盖全部产生路径（字面量/变量/参数/
  运算产物/调用返回），否则消费端必然错分派。
- **IR_常量整数 操作数1=值文本驻留 ID**（P7a 语义：代码生成经 驻留文本 还原立即数），
  传数值必得 0/错值——正确姿势 `驻留(整数转字符串(值))`。
- **v2 源码里的残缺占位行会静默通过**：`{ 未完 }` 被当合法调用语句、未定义标识符走
  函数名兜底负压栈，链接期才可能暴露（中文 hex 符号无定义表拦截）——编辑 v2 组件后
  必须「重建 v2p + 冒烟 + 全量回归」三连，不能只看构建成功。
- **「立项即绿」两种模式别混用**：闭环条目+空期望（v2p 退出码断言，127 模式）vs
  通用路径+宿主实际输出期望（119~126 模式）——链接级缺口（内置符号/未实现语法）只能
  用后者立项，实现到绿再切闭环注册。
- **v2p 的退出码只是编译成功与否**：手动冒烟 `./v2p xxx.cn` 的 `$?` 不代表被编译
  程序运行结果（要链接运行才算，runner 步骤 6）。
- **双机时区差**：git 提交时间 +0800、单位机本地时间为 UTC 时，构建产物 mtime 对不上
  提交序——判断产物新鲜度用「清场重建」而非时间戳考古。
