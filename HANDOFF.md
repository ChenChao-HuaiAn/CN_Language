# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-15——**第二百零四轮（204-a）：波 3 ⑤ H7/H11 收口——波 3 全收官**（基线 94f1a8b，认领 4f63b3c）。本会话累计=183-a（波 3 ②用户类泛化+190-a 错位根治）+204-a。

### 一、本轮做了什么（写给无上下文的新会话）

1. **靶子考古→现状实测**：H7/H11（78-a B35 登记「字段容器接收者入容器位归一化保守分支」）——宿主实测有析构接收者四来源（转移真 move/调用返回接管/字面量驻留/拥有局部值语义复制）**残留全 0**；v2 侧同款实测（v2p 编译+返回码承载）逐形态一致（0/22/0）。
2. **收口判定**：波 3 设施（容器字段三件套+甲通道+泛化）使保守分支前提（字段容器无释放面→接管即泄漏）消失——语义三通道齐备（转移=move/拥有返回=接管/其余=值语义复制·Rust Clone 同构）。
3. **反证命中**：关闭 injectFieldCascadeDestroy → E2E 283 校验和 31→0（五形态全泄漏复现·唯一变量）→恢复回 31。
4. **无析构面残留 2=登记设计语义**（无析构类不释放·两侧一致·Rust 无 Drop 同哲学）——非靶子，维持。
5. **防线**：E2E **283_字段容器接收者双编译对照**（五形态折校验和 31·借链=是）转正 PASS。

### 二、验证链

零警告 + 单测 1325/1325 + 全量 E2E **311 用例 311 过/0 败/0 跳**（283 入列）+ 反证双态。产物=target/p204win/（探针/v2run 双侧编排脚本）。

### 二.5、206-a 波 4 开工探明轮（零源码改动）

转移第一版盘点（声明位 AST 改写非零拷贝〔semantic_decl.cpp:107〕/表达式位拒绝〔semantic_call.cpp:210
checkTransferCall 仅指针字符串+标识符〕=§四清单第 6 项推倒对象）；`复制(x)` 双侧设计点位（宿主 semantic
特判+genCopyBuiltin 类型分派+泛型单态化路径；v2 内置识别+类型文本分派）；stdlib 切换清单（向量/链表/
队列下标写·映射桶键值；集合无拷贝构造维持）；**子轮序列 206-b（复制内置双侧）→206-c（stdlib 切换+元素
级深拷收口）→206-d（转移表达式位+真槽位交接+反证）→206-e（拥有形参归属+性能锚）**——详见 plans/020
第七十二节。**206-b 已完成（宿主收口）**：checkCopyBuiltinCall（semantic_call/semantic.hpp）+genCopyBuiltin（ir_fields/ir.hpp·五类型分派：字符串/标量/结构体临时槽深拷/容器 NewObject+拷贝构造/拥有来源接管——探针⑤实证「对拥有临时再 copy=泄漏」后加接管分支）；E2E 284_复制内置宿主正测 转正。**v2 侧=边界登记 206-b2 专项**（复制 语义检查路径多元〔声明位/赋值位/实参位各自展开〕+v2p 对「未声明： 复制」诊断程序堆损坏崩 0xC0000374 新案·回退干净代码仍崩=既有缺陷）——206-b2 须先修堆损坏再全路径补齐。**下一会话从 206-c（stdlib 切换）或 206-b2（v2 补齐）开工。**

### 三、下一步（新会话按序）

1. **波 4 子轮 206-b**：`复制(x)` 内置双侧实施（plans/020 第七十二节 §二 设计）。
2. **D1 续波**（修正口径 56~58 个；与深度机 201-a 错开）。
3. C2（维持排程）/波 7 NLL/波 8+（外依并发库）。
4. 跨机轮：arm64 侧 183~204-a 契约面复验（单位机）。

### 四、诚实边界

- 无析构类字段容器残留 2=登记设计语义（两侧一致），如需收归自动管理属**规范级变更**须先呈报。
- 190-a 错位根治已广播（§8.4）：linux/arm64 建议复跑 115/235/245+全量。
- github 镜像经 force-with-lease 对齐（94f1a8b；对齐前分叉头均为本人被 rebase 弃提交，零他人工作，已留痕）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-15 第两百一十五轮（**深度机 linux-x86_64**）——**215-a**：生成二元运算
主体（129 行·v2 IR/生成表达式运算.cn）拆分完成——主函数 90 行 + 判定字符串拼接 20 行 +
浮点混合转换 33 行（全 ≤100）。**D1 计数实测 41**。

### 一、本轮（215-a）做了什么（写给无上下文的新会话）

1. **判定+转换分层**：族① 拼接判定（3 项引用输出）/族② 浮点混合转换（左右寄存器引用输出·
   91-a/92-a 语义随体迁移）；主函数只剩弹参+op 映射+发射编排。
2. **引用输出分组惯例固化**：族内写、主函数后续读的变量必须引用传递（预防 177 推广）。
3. **一次快验拦截即修**（区间闭合冗余）——端点打印前置纪律生效，切片区间全对。
4. **验证链全绿**：单测 1325/1325 + E2E 312 用例 310 过/0 失败/2 跳 + 锚定链重锚
   403577→403776 行（fix_p≡fix_s 逐字节 `fe427134…`）+ 多重集缺失 0。

### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 312 用例 310 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403776 行·`fe427134…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 41 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 收口后 → 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 312 用例 310 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403577 行·`654fab5a…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 41 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 收口后 → 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 构建：cmake 零警告（rebase 后重建）；单测 1325/1325
- E2E：312 用例 310 过/0 失败/2 跳（+1 家机 284 新用例）
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403500 行·`09755608…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 42 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 `复制(x)`（家机 206-b 已收口）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 311 用例 309 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403300 行·`1cadaff3…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 42 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 311 用例 309 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403101 行·`dcf1471e…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 42 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 311 用例 309 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（403023 行·`a4b82b28…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 42 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集、
   handleClassCallExpr 362、arm64 后端族）。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 构建：cmake 零警告；单测 1325/1325
- E2E：311 用例 309 过/0 失败/2 跳
- 锚定链不变：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402927 行·`bf9410e8…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 43 个）**：用 check_fn_length.py 实测取列（继续避让家机在飞写集与
   handleClassCallExpr 362）。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 311 用例 309 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402927 行·`bf9410e8…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 44 个）**：首列候选=ir_error_ctor.cpp handleResultCtor 158（宿主）/
   代码生成ARM64.cn 编码对象调用ARM64 152（arm64=单位机领域避让）——取列时再避让。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 构建：cmake 零警告；单测 1325/1325
- E2E：311 用例 309 过/0 失败/2 跳
- 锚定链不变：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402835 行·`6ef3c06c…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 45 个）**：首列候选=解析原子 149（v2 语法/解析表达式.cn）——继续避让
   家机在飞写集与 handleClassCallExpr 362。
2. **C3**：波 4 `复制(x)`（家机 206-b 在飞）→ 元素级深拷 → 波 7 NLL。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 构建：cmake 零警告；单测 1325/1325
- E2E：310→311 用例（+1 家机新增）309 过/0 失败/2 跳
- 锚定链不变：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402835 行·`6ef3c06c…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 46 个）**：首列候选=semantic_stmt.cpp visitRangeForStmt 141（宿主）/
   解析原子 149（v2 语法）——继续避让家机在飞写集与 handleClassCallExpr 362。
2. **C3 波 3 收官后**：元素级深拷〔前置=波 4〕；波 4 `复制(x)` / 波 7 NLL 待排。
3. 其余登记：D7 / C2 / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 310 用例 308 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402835 行·`6ef3c06c…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 47 个）**：首列候选=semantic_sig.cpp resolveOverload 152（宿主侧）/
   semantic_stmt.cpp visitRangeForStmt 141（宿主侧）/ 解析原子 149（v2 语法）——继续避让
   家机在飞写集与 handleClassCallExpr 362。
2. **C3 波 3 剩余**：元素级深拷〔前置=波 4〕→ H7/H11（家机 204-a 进行中）。
3. 其余登记：D7 / C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325；E2E 310 用例 308 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402612 行·`0aae01c2…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 48 个）**：首列候选=语法分析.cn 解析结构体声明 140 / semantic_sig.cpp
   resolveOverload 152 / semantic_stmt.cpp visitRangeForStmt 141（继续避让家机 204-a 在飞
   写集与 handleClassCallExpr 362）。
2. **C3 波 3 剩余**：元素级深拷〔前置=波 4〕→ H7/H11（家机 204-a 进行中）。
3. 其余登记：D7 / C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补；丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 构建：`cmake --build target/build` 零警告（rebase 后重建）；单测 1325/1325
- E2E：`python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → 310 用例 308 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402394 行·`0f4cfc2c…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 49 个）**：首列候选=语义检查表达式.cn 检查调用表达式 158 / 语法分析.cn
   解析结构体声明 140 / semantic_sig.cpp resolveOverload 152（宿主侧；继续避让家机 204-a
   在飞写集与 handleClassCallExpr 362）。
2. **C3 波 3 剩余**：元素级深拷〔前置=波 4〕→ H7/H11（家机 204-a 进行中）。
3. 其余登记：D7 / C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **预防编号体系三方案待用户裁决**（lessons 201·补：甲/乙/丙，丙案先行）。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325（宿主零改动复用构建）
- E2E：`python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → 309 用例 307 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（402162 行·`b9f5582f…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 50 个）**：首列候选=IR容器释放.cn 发射资源字段释放于地址 156 / 语义检查
   表达式.cn 检查调用表达式 158 / 语法分析.cn 解析结构体声明 140（v2 侧；继续避让家机
   183-a ir_fields.cpp、handleClassCallExpr 362 与单位机 arm64 后端族）。
2. **C3 波 3 剩余**：用户类字段泛化重试（家机 183-a 进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11。
3. 其余登记：D7 / C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧契约面复验。### 二、本轮验证（linux-x86_64 口径）

- 单测 1325/1325（宿主零改动复用 199-a 构建）
- E2E：`python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → 309 用例 307 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（401904 行·`f2f7d104…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 51 个）**：首列候选=解析语句.cn 解析语句 162 / 语义检查表达式.cn 检查调用
   表达式 158 / IR容器释放.cn 发射资源字段释放于地址 156（v2 侧；继续避让家机 183-a
   ir_fields.cpp、handleClassCallExpr 362〔对象调用族〕与单位机 arm64 后端族）。
2. **C3 波 3 剩余**：用户类字段泛化重试（家机 183-a 进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11。
3. 其余登记：D7（196-a 新立·分配器循环携带值核查）/ C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧契约面复验（单位机 195-a arm64 接线落地后）。### 二、本轮验证（linux-x86_64 口径）

- 构建：`rm -rf target/build && cmake …` 零警告；单测 1325/1325
- E2E：`python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → 309 用例 307 过/0 失败/2 跳
- 锚定链：`target/audit2/selfwork79/fix_p.asm ≡ fix_s.asm`（401575 行·`fa7fc926…`）

### 三、下一轮任务（按序）

1. **D1 续波（实测 57 个）**：首列候选=v2 语义/生成侧次大函数（用 check_fn_length.py 实测取列；
   避让家机 183-a〔ir_oop/类字段〕与单位机在飞写集）。
2. **C3 波 3 剩余**：用户类字段泛化重试（家机 183-a 进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11。
3. 其余登记：C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧契约面复验（单位机 195-a arm64 接线落地后）。### 二、本轮验证（linux-x86_64 口径）

- 回退后源码零净变更（git status 干净）——无需门禁重跑（185-a/190-a/191-a 验证链仍有效）
- 170~197-a 历轮全记录见 git 提交与 plans/019 对应轮次行

### 三、下一轮任务（按序）

1. **D1 续波（实测 58 个）**：生成结构体整体赋值 170（**需按预防 183/184 重试**——
   程序化切片+守卫行号推导+切片后立即核验）→ 后续按清单避让在飞写集。
2. **C3 波 3 剩余**：甲通道实装（家机 180-a 完成）→ 用户类字段泛化重试（家机 183-a
   进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11 收口。
3. 其余登记：D6（单位机 188-a 已落地）/ C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧 161~198-a 契约面复验（单位机 195-a arm64 接线落地后）。### 二、本轮验证（linux-x86_64 口径）

### 二、本轮验证（linux-x86_64 口径）

### 二、本轮验证小结（linux-x86_64 口径）

- 零警告 + 单测 **1317/1317**；E2E **306 用例 304 过 / 0 失败 / 2 跳**（44 双编译对照全过）
- **锚定链重锚 400413→400763 行**（`49dae37e…`）——197-a 检查表达式拆分布局变化
- 170~191-a 历轮记录见 git 提交 a64e1ea / bc39bb4 / adfb8f3 / 0a2efab / c5c632f /
  fe3cfc7 / 8819c63 / ab75deb / 13f152f / 4706dd3 与 plans/019 对应轮次行

### 三、下一轮任务（按序）

1. **D1 续波（实测 53 个）**：ir_oop_call.cpp handleClassCallExpr 362
   （家机 183-a 写集相关，避让中）→ 后续按清单
   `python scripts/check_fn_length.py`「最大优先」避让在飞写集。
2. **C3 波 3 剩余**：甲通道实装（家机 180-a 完成）→ 用户类字段泛化重试
   （家机 183-a 进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11 收口。
3. 其余登记：D6（单位机 188-a）/ C2 / 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧 161~192-a 契约面复验（单位机）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；对拍/锚定链必须独立串行且不得相互交错（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（warning=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`（306 用例：304 过/0 败/2 跳）。
> **D1 度量**：`python scripts/check_fn_length.py`（172-a 已修口径；名字回溯对 parse 类
> 函数不可靠——以区间+构建验证为准）。
> **改 v2 源码的轮次（预防 158）**：v2p 必然变化 → ①行多重集缺失+重复双查 ②全量 E2E
> 双编译对照 ③锚定链 fix_p≡fix_s 重锚；asm md5 对拍不适用。
> **宿主纯重构轮（v2 树零变化）**：v2p 产物 .s md5 不变+锚定链不变+全量 E2E；
> 文件级纯搬运另加「搬移段与原文逐行一致」difflib 核验。
> **远程推送现状（2026-09-15 实测）**：gitcode 正常；github 已配置但本机无凭据——
> 待用户配置 token 或他机代推。
```

### 五、诚实边界

- **D1 台账口径修正（172-a）**：166~171-a 期间 v2 侧行数系虚高口径，已三处修正。
- **192-a 的三处迭代修正**（核验脚本边界 bug 假报/分支段尾多余闭合/族方法内
  i 引用）均由**多重集核验+构建**两级兜底拦截，未入库——方法学有效。
- github 镜像自 170-a 起未推送（本机无凭据）——170~192-a 二十二轮待补推。
- 探针/对拍临时件：/tmp/p17*、/tmp/p18*、/tmp/p19*（不入库）。


**交接时间**: 2026-09-15 第一百九十一轮（**深度机 linux-x86_64**）——**191-a D1 函数级续波：visitFunctionDecl 171→分派+2 流水段方法 ≤100 达标**（基线 3c57f00）。本会话 21 个开发轮（170-a 接手 + 172~191-a）。

### 一、上一轮（191-a）记录（191-a D1 函数级续波：visitFunctionDecl 171→分派+2 流水段方法）

1. **拆分面（三明治形态·纯搬运零改写）**：`ir_decl.cpp` 的 `visitFunctionDecl`（171 行）
   按注册流水线提取为 **2 个流水段族子方法**（ir.hpp +2 声明）：registerFunctionParams
   （参数循环+varStack_ 压栈+引用/结构体/函数指针参数形态+默认参数值收集，58 行）/
   finishFunctionEmit（析构 DeleteObject RAII+串释放+名单复位+IR 函数入列+作用域弹出，
   21 行）。主函数保留「原型拦截+基础信息/linkName/返回类型/structReturn/function_ 绑定+
   计数器复位+入口块+静态注入+函数体+默认返回」94 行。
   **多重集核验缺失 0 行**。**D1 计数实测 57**（visitFunctionDecl 出列）。
2. 一次构建失败即修正（keep_head 段内 func 声明重复+finishFunctionEmit 的 node 参数
   未使用——签名收窄+三处同步）。lessons 191 段（三明治形态正向记录）。

### 二、本轮验证（linux-x86_64 口径）

### 二、本轮验证（linux-x86_64 口径）

- 零警告构建 + 单测 **1317/1317**；**v2p 产物 .s md5 不变**（`3bebee1b…`=186-a 版本——宿主
  纯重构 v2 树零变化）；**锚定链不变**（395505 行，md5 `02d691aa…`）；全量 E2E **306 用例 304 过 /
  0 失败 / 2 跳**（44 双编译对照全过）
- 历轮全记录见 git 提交（a64e1ea / bc39bb4 / adfb8f3 / 0a2efab / c5c632f / fe3cfc7 /
  8819c63 / ab75deb / 13f152f / 4706dd3）与 plans/019 对应轮次行

### 三、下一轮任务（按序）

1. **D1 续波（实测 58 个）**：ir_oop_call.cpp handleClassCallExpr 362
   （家机 183-a 写集相关，避让中）→ v2 `语义检查语句.cn` 检查推断声明语句等
   （清单 `python scripts/check_fn_length.py`）。
   （认领前先 fetch 看板避免撞车。）

### 四、历史轮记录（170~189-a 摘要）

- **189-a** registerFunction 173→57（semantic_builtins.cpp 注册流水线 4 族方法）
- **186-a** parseClassMember（清单标签「运算符X」）198→47（parser_oop.cpp 成员形态 4 族方法）
- **185-a** 检查赋值语句 178→47（语义检查语句.cn 左值形态 2 族方法·锚定链重锚 395505）
- **184-a** 检查变量声明语句 180→92（语义检查语句.cn 2 族方法·类型ID/声明类型名ID 双引用输出·锚定链重锚 395347）
- **182-a** visitIdentifierExpr 198→46（semantic_expr.cpp 识别链 4 族方法）
- **181-a** visitUnaryExpr 204→42（semantic_expr_op.cpp switch 薄化 5 族方法·整族迁出 semantic_expr_unary.cpp）
- **180/183-a** 家机甲通道实装/用户类字段泛化重试（C3 波 3）
- **177~178-a** visitProgram 256→45 / lvalueAddress 225→30
- **175~176-a** visitAssignmentExpr 349→48 / visitBinaryExpr 269→34
- **172-a** v2 侧生成赋值语句拆分+check_fn_length.py 口径根治+10 文件行尾归一
- **170-a** visitCallExpr 族A struct 化 239→96+169-a 残留 GCC 构建缺陷根治

### 二、本轮验证（linux-x86_64 口径）

- 零警告构建 + 单测 **1317/1317**；**v2p 产物 .s md5 不变**（`3bebee1b…`=185-a 版本——宿主
  纯重构 v2 树零变化）；**锚定链不变**（395505 行，md5 `02d691aa…`）；全量 E2E **306 用例
  304 过 / 0 失败 / 2 跳**（44 双编译对照全过）
- 历轮全记录见 git 提交（a64e1ea / bc39bb4 / adfb8f3 / 0a2efab / c5c632f / fe3cfc7 /
  8819c63 / ab75deb / 13f152f / 4706dd3）与 plans/019 第一百七十~一百八十五轮行

### 三、下一轮任务（按序）

1. **D1 续波（修正口径 62 个）**：函数级首列=宿主 `semantic_builtins.cpp` registerFunction
   173 → `codegen/reg_alloc.cpp` computeLiveIntervals 178 → v2 `语义检查语句.cn`
   检查推断声明语句等（清单 `python scripts/check_fn_length.py`）。
   （认领前先 fetch 看板避免撞车。）
2. **C3 波 3 剩余**：甲通道实装（家机 180-a 进行中）→ 用户类字段泛化重试（家机 183-a
   进行中）→ 元素级深拷〔前置=波 4〕→ H7/H11 收口。
3. 其余登记：D6 / C2（维持排程）/ 波 4 `复制(x)` / 波 7 NLL / D3 / D4。
4. **跨机轮**：arm64 侧 161~186-a 契约面复验（单位机）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；对拍/锚定链必须独立串行且不得相互交错（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（warning=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`（306 用例：304 过/0 败/2 跳）。
> **D1 度量**：`python scripts/check_fn_length.py`（172-a 已修口径：显式行尾 + 剥字面量配对；
> 注意名字回溯对 parse 类函数不可靠——以区间+构建验证为准）。
> **改 v2 源码的轮次（验证链分野·预防 158）**：v2p 必然变化 → ①行多重集缺失+重复双查
> ②全量 E2E 双编译对照 ③锚定链 fix_p≡fix_s 重锚；asm md5 对拍不适用。
> **宿主纯重构轮（v2 树零变化）**：证据=①v2p 产物 .s md5 不变 ②锚定链不变 ③全量 E2E；
> 文件级纯搬运另加「搬移段与原文逐行一致」difflib 核验。
> **对拍脚本（编译器逻辑结构有改动的重构轮）**：`python3 scripts/refactor_parity.py
> target/cn_<轮>base target/cn_<轮>new`——对拍二进制必须放 target/ 下（预防 156）；
> 基线构建须编辑冻结态（预防 155）。
> **远程推送现状（2026-09-15 实测）**：gitcode 正常；github remote 已配置但本机无任何凭据
> （ssh/gh/credential store/.netrc 全缺）——镜像推送待用户配置 token 或他机代推。
```

### 五、诚实边界

- **D1 台账口径修正（172-a）**：166~171-a 期间 v2 侧超百行函数行数系 universal newlines
  虚高口径（约一倍），已在 plans/021 §3-D/§5 与脚本三处修正；宿主侧数字不受影响。
- **181-a 程序化切片重建方法论**（预防 168/169）与 **178/179 轮包装行清单**（预防 163/171）
  为本会话 D1 拆分的核心教训，后续拆分轮必须遵守。
- github 镜像自 170-a 起未推送（本机无凭据）——170~186-a 十六轮待补推。
- 探针/对拍临时件：/tmp/p170*、/tmp/p172*、/tmp/p184*、/tmp/p186*（不入库）。

## 单位机 ARM64 节

**最近交接**：2026-09-15——**第一百九十六轮（196-a）：arm64 寄存器分配接线尝试 → 按纪律回退（源码零净变更）**；
留档=接线图谱 + 3 项既有缺陷 + 根因（分配器循环携带值）+ 新立 **D7**。上轮 193-a（F1/F2 重评）见 git 1727cd7。

### 一、本轮（196-a）做了什么（写给无上下文的新会话）

1. **目标**（用户批阅优先级①）：arm64 启用 `LinearScanAllocator`（镜像 x64：-O2 联动、structReturn/i128 forceDisable）。
2. **已实现的接线点（图谱可直接复用）**：
   - 读侧统一入口 `loadOperandToX`（`operand.id>=0` 且已分配 → `mov`）；
   - **文本形态** `operandText` / `resultText` 必须感知分配（否则值流分叉）；
   - 结果落位 ≈25~40 处 → `storeResult(writer, inst.result, src, type)` 助手（**两类形态都要覆盖**：多行格式与 `dstOff` 局部变量）；
   - `id` 型取数旁路（含 `emitStackLoad(writer, regSlotOffset(x.id))`）统一走 `emitLoadVirtualTo`；
   - 返回位 = `emitTerminator`（`%vN`→槽文本转换处）+ `emitEpilogue` 需接受物理寄存器名；
   - 序言/尾声 = 被调用者保存对（x19~x28；隐藏返回指针场景 x19 已占 → forceDisable）；
   - **`stackParamBase()` 必须按 `calleeSavedSlots_*16` 补偿**（序言压栈在 `mov x29,sp` 之前）；
   - 开关 = `backend_factory.cpp` arm64 分支。
3. **接线期修掉的 3 个既有缺陷**：`resultText` 未感知分配 / `emitTerminator` 槽文本转换 / 批量替换自调用（ASAN 栈溢出）。
4. **阻断根因（★D7）**：`reg_alloc.cpp` 活跃区间对「**循环体内被重定义、跨块/跨迭代使用**」的值判定过短 → 同寄存器双占；
   实证 E2E **115**（`向量$资源.清空` 少析构 1 次）/ **235** / **245**（校验和 191≠255）；最小对照=简单循环正常。
5. **处置**：按 `plans/025` §三.2 §5 既定口径回退（`git checkout`，源码零净变更）；探针正确 + 单测 1325/1325 复验。

### 二、下一轮（**197-a** 预登记，`plans/025` §〇）——建议按此序

1. **D7 专项（建议优先）**：分配器活跃区间保守扩展（回边/循环携带值 → 扩展至循环尾）+ **x64 同族核查**
   （win-x64 同用该分配器：复跑 115/235/245 类用例）+ 反证探针；完成后即可重接 arm64（本轮图谱直接复用）；
2. **F2-33 纯 stdlib 三项**（随机数/迭代器/正则——零语言变更、与在飞任务零交叠）；
3. D1 续波（`scripts/check_fn_length.py` 实测 55 项）/ D3（未初始化槽加固）。

### 三、诚实边界

- **本轮未交付功能**（回退）；arm64 寄存器分配收益（探针实测访存 -64%）待 D7 后兑现；
- D7 的 x64 同族面**未实测**（win 侧 306/306 通过 → 形态可能未触发）——家机复跑可留意；
- ASAN 构建须隔离产物目录（本轮曾污染 `target/*.a`，已清场重建）；
- github 镜像本机无凭据未推。
