# HANDOFF 交接文档

**交接时间**: 2026-09-09 第四十八轮（**家机 win-x64**）——观察项 46-a/46-b 双侧根治（成员结构体值语义：宿主写静默损坏+v2 读段错误；手写链式读）+ 46-c/46-e 定性立案。E2E 188/188_v2 双通道新增并 PASS，探针矩阵双侧 20/20 全绿。**单位机 arm64 轮 = 下一跨机轮（v2 源码本轮已改，arm64 固定点复锚口径必然变化）。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **按挂账二.2 根治第四十六轮立案观察项**，E2E 先行（探针矩阵→根治→转正用例）。
2. **立案定性修正（重要）**：46-a「成员结构体作值读写段错误」**实为双侧缺陷**（原立案只报了 v2）——
   - **宿主**：成员结构体作值**写**（`r.左上 = a` / `r.右下 = r.左上`）**静默垃圾**（MemberExpr 赋值目标无结构体整体赋值通道，标量 StorePtr 只写 8 字节：源地址截断入首字段+其余字段栈残留；探针 11/22 打出 12582296/128）——**静默数据损坏，比段错误更危险**；读路径全好。
   - **v2**：声明位读 `点 t = r.左上` 产物**段错误**（win 0xC0000005=立案 rc=139 的 win 同族实锤；生成结构体局部 对成员源调 生成表达式，成员分支 LoadPtr 取 8 字节值被当源地址做结构体拷贝=解引用垃圾地址）；赋值位读右值限定标识符可见失败；v2 写路径已被第四十六轮顺带修复（PASS）。
3. **宿主修复（Rust place 拷贝同构）**：提取 `emitStructWholeAssign` 单一事实源助手（ir_expr.cpp+ir.hpp；源三形态取址+类拷贝构造深拷贝+CopyStruct 按语义大小），visitAssignmentExpr 的 MemberExpr 目标分支（D2 特判后新增，类型检查前置防拒绝路径双发 lvalueAddress）与 IndexExpr 目标分支（H8 机制原位改调，行为逐点不变）双分支接入。
4. **v2 修复（四处联动）**：①成员载荷是结构体 扩条件「成员字段类型是结构体即压地址」（实参回扫「字段地址形态」天然兼容，E 回归锚 PASS）；②生成字段访问 对象形态补 节点_成员 分支（解析成员链结构体ID 查偏移/宽度/类型码=46-b 读路径）；③顺序链成员地址 偏移兜底统一换 解析成员链结构体ID（多级左值链地址打通）；④生成结构体整体赋值 右值扩成员链形态 + 生成赋值语句 成员左值分支结构体整体拷贝拦截（两侧生成左值地址+IR_结构体拷贝；**>8 字节成员结构体赋值静默截断=修复过程披露的既有缺陷家族，一并根治**；构造字面量右值不拦截，186 通道不受扰）。
5. **联合体同族旁路探针**（穷尽纪律）：宿主 U1/U2/U3 全对零修复；v2 联合体声明语义层「未声明的类型」可见失败 → 立案 **46-e**（随类型系统/语法覆盖轮）。
6. **46-c 变量关键字实证定性**：宿主 PASS（`变量 x=41 → rc=42`）；v2 可见失败（fail fast 非静默）；v2 补齐=完整类型推断特性面（解析+语义+IR 三层联动，非小补丁）→ 归属语法覆盖轮。
7. **E2E 188_成员结构体值读写**（宿主通道：stdout 11/22/63 三锚）+**188_v2_成员结构体值读写**（v2 闭环：rc=63 六形态校验和 A写+1/+2 B读+4 C读+8 D+16 E锚+32）双通道 PASS。
8. 文档：plans/014 状态表第四十八轮行+观察项表（46-a/46-b ✅ 双侧根治、46-c 定性完成、新增 46-e）+更新日志+lessons 一条（父链索引误写假阴性）。

## 二、下一跨机轮（按序）

1. **单位机 arm64**：`git pull` 本提交 → 全量门禁复验（GCC 零警告+单测+E2E 全量）→ E2E 188/188_v2 arm64 复跑（**本轮新增双通道用例**）→ v2self 固定点复锚（**v2 源码本轮已改：win 口径 235968 行作废，arm64 302433 行预期增长**）+绑定自检+组件对拍 29/29。
2. **深度机 X64L**：同上复验（X64L 锚定 227614 行同作废）。
3. **观察项延续**：46-e 联合体类型面（v2，随类型系统轮）；46-c 变量推断（随语法覆盖轮）；46-d 全局位构造（低优先维持）；v2 无符号类型面缺 正32/正64（随类型系统轮）。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 家机 win-x64（本行口径）
rm -rf target/build && powershell -ExecutionPolicy Bypass -File scripts/ci.ps1   # 全量门禁（约 50 分钟）
# 46-a/46-b 探针矩阵（产物全落 target/p48win/）：
python target/p48win/probe48a.py 宿主 ; python target/p48win/probe48a.py v2p   # 形态矩阵 5/5×2
python target/p48win/probe48b.py 宿主 ; python target/p48win/probe48b.py v2p   # 消融矩阵 5/5×2（A2/B2/C2/D2/L1）
python target/p48win/probe48u.py                                              # 联合体同族旁路（宿主 PASS/v2 立案面 46-e）
python target/p48win/rebuild_v2p48.py    # v2 源码改动后重建 v2p_48（宿主编译 v2 全树→ml64→link）
python tests/e2e/run_e2e.py --filter 188 --cn target/Debug/cn.exe             # E2E 188+188_v2 双通道
# v2self win 锚定链（第四十七轮脚本，本轮 v2 源码已改——重跑即得新口径行数）：
python target/p46win/self_chain.py ; python target/p46win/comp_diff.py ; python target/p46win/run_level.py
```

注：v2p_48.exe=v2 修复后全树的编译器（本轮探针全部经它实物验证）；运行时 obj（target/{10 名}.obj）与 src/runtime 零改动可复用。

## 四、本轮踩坑

- **父链索引误写=拦截假阴性**（已入 lessons 权重7）：`解析成员链结构体ID(池, 左末自身索引)` 返回**左末字段自身类型**（.右下→点），父链类型须传 `左末.子起点`（范本=取成员宽度码）；双重解析必 miss=0→拦截永不命中且静默回落=「修复看似已实施实为死代码」。**拦截型修复必须先验证拦截命中（负形态）再验证拦截正确（正形态）**。识破手段=临时诊断打印双侧对照（语法层登记键基 vs IR 期查询值）。
- **门禁与前台验证并发互踩**：后台 ci.ps1 运行中不得在前台跑共用 target/ 产物的 E2E/构建（cn.exe 中途被换=门禁结果不可信；清场重建撞文件锁=新实例秒失败）。TaskStop 后用「日志 8 秒不增长」判停（进程查询 grep 模式自匹配会误报孤儿）。
- （承前轮）run_e2e.py 输出全缓冲：判活看 python 进程内存渐进增长；78/79 OOM 用例 3.8GB+ python 进程属正常。

## 五、诚实边界

- 本轮宿主+v2 源码均已改动；**win-x64 全量门禁（清场重建零警告+单测+E2E 全量）结果见提交信息**；arm64/X64L 侧复验与固定点复锚=下一跨机轮（v2 三文件改动 生成表达式/生成语句/语法分析 为平台无关组件，后端发射层零触碰）。
- 46-c/46-e 为「可见失败」的功能缺口定性立案（非静默错误、非内存安全），非搁置——证据链与归属轮次见 plans/014 观察项表。
- 组件对拍基线 29（6 包根+23 成员）；78/79 OOM 为 win 侧固有败面。
- 探针与脚本留 target/p48win/ 不入库。
