# HANDOFF 交接文档

**交接时间**: 2026-09-02 第五轮（②c 四项一轮收官：E2E 127 win-x64 全绿 + ③ 前三缺陷根治；
全量门禁复跑中——见「验证状态」）

> 本轮上下文：用户令「拉取最新远程仓库，读取 HANDOFF.md，接着完成后续任务」→ 完成上轮
> （第四轮）换电脑中断的「接手第一小时」验证，揪出并根治三项新缺陷（win 门禁欠账），
> 随后按已批准计划（第四轮用户裁决：②c 四项一轮收官，E2E 127 单用例四主题载体）完成
> A/B/C/D 全链实现，127 首次全绿。⚠ 两处用户未裁决项（AskUserQuestion 未获答复，按推荐项
> 执行并在此披露）：①126 主.expected 三处平台敏感行按 119-125 惯例修正（已验收测试文件
> 修改——断言语义未弱化：删的是零断言价值的平台 echo、行数本就不该硬编码）；②三项新缺陷
> 修复方案均取推荐项。用户审计时可推翻，git 可回退。

## 本轮做了什么（全部有实证）

### 一、接手第一小时收口（上轮中断项，三项新缺陷根治）
| # | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 125/126 win ml64 A2070×22 处 | v2 `代码生成.cn` IR_逻辑非 直接 `cmp 槽,0`——栈槽内存操作数 MASM 无法推断尺寸（GAS 寄存器形态合法故 Linux 未暴露） | 先 `mov rax,槽` 再 `cmp rax,0`（对齐同文件比较运算惯例） |
| 2 | 126 win 步骤3 缺期望行 | 主.expected 在单位机从 v2p 实际输出直抄（`linux-arm64`/`行数=1971`/`v2asm.s` 三处平台敏感行） | 按 119-125「win 口径+Linux 分支归一」惯例修 3 行 |
| 3 | 126 win 链接 LNK1181 乱码路径 | B7 供给产物目录中文名（供给126_供给）经 rsp 被 link.exe 按 GBK 误读 UTF-8 | 双平台供给路径一律 ASCII（supplyN_M，run_e2e.py 两分支） |
- 118 分诊：win 本就绿（上轮为 Linux 侧疑点，Linux 复确认留单位机）；78 分诊：win 为内存
  超限（=文档化已知遗留 v1 架构），上轮「级联双重释放」假设未获 win 证实——Linux -11 复查留单位机。

### 二、②c A/B/C/D 实现（8 组件，E2E 127 载体全绿）
- **A 嵌套容器**：`IR容器.取实例实参们` 按基名元数贪婪再切分（词扫描含数字——「整64」切成
  「整」的符号损坏根治）；`IR签名.替换类型参数带段们`（向量版，旧 naive split 删除）；
  **ABI 实测修正**：容器/类元素返回=指针标量（宿主 asm 实证 `元素 : ptr`，rax 返回元素指针、
  链式 this=rax 直传）——**勿再翻案为 sret**；`IR生成.解析节点类型ID/解析方法对象类型`
  统一递归类型推导（标识符/成员/调用 三形态全链：`b.表.追加` 的对象是成员节点！）。
- **B 容器字段（指针槽模型）**：`语法分析.登记容器字段` 四元组（类型大小与对齐 对容器合成名
  按对象大小 24/56 保留）；字段构造赋值复用成员赋值路径；marker 接收者=字段地址→指针加载；
  RAII：`函数IR.容器字段局部们/结构体们` + `IR容器.发射容器字段析构`（块出口/返回字段逆序
  空安全）；**拥有式供给契约 ABI**：含容器字段的结构体供给侧=类形态（H8-⑤）→ 宿主类元素
  指针步长 → v2 返回形态同按指针标量（查方法签名 分类：纯结构体才 sret）。
- **C 顶层静态**：`节点_静态声明`（裸形态）+ 解析期登记（容器→变量容器表/结构体→变量结构体表）
  + 语义全局符号 + `IR容器.收集静态/静态序号们` + 入口 主 注入（NewObject+构造+IR_静态存储）
  + 标识符 IR_静态加载 + 新操作码 65/66 双后端发射 + `.data _cn_gstatic_N`（MASM dq/GAS .quad）。
- **D & 引用参数**：`函数实参引用们` 表驱动回扫取址（B8 同款）；被调方 引用参数槽们/类型们
  ——读取分流（容器& 双重解引用/结构体& 槽内容即基址/标量& 解引用）、赋值/自增指针写回、
  成员基址复用、prologue 跳过多槽拷贝；解析参数 结构体& 登记修复（附加==256 纳入）。
- **③ 前潜伏缺陷**：解析期全局按名表跨函数同名异型（参数 t:向量& vs 局部 t:桶）串类型 →
  `IR生成.当前函数变量类型` **函数级权威表**（参数/容器/结果/结构体局部 全登记）。

### 三、E2E 127（tests/e2e/127_v2_嵌套容器与容器字段与静态与引用/）
- 主.cn 四主题断言（A 201-210/B 211-218/C 221-226/D 231-235）+ 供给.cn（类形态 词条 契约+
  向量$映射$整64$整64 全方法面实例化）+ 主.expected（v2p 自检行，win 口径）。
- **立项即绿**创新：立项期 主.expected 置空 → 通用路径（宿主编译运行）兼作**宿主级回归**
  （全部形状在宿主缺陷簇根治后保持绿，实测 PASS）；②c 落地后注册 v2 闭环+改写期望。
  （0f39be3 的「立项不注册门禁保持绿」说法经实测不成立——未注册目录走通用路径必红，勿再沿用。）

## 验证状态（诚实门禁）
- **家机 win-x64**：v2 全系 119/120/123/125/126/**127** 逐一回归 6/6 绿；**全量 ci.ps1 门禁
  （构建零警告+单测+E2E 全量）结果见最终提交信息**（本轮收尾时后台跑完，预期 127/129 中
  127 绿 + 78/79 已知遗留红）。MASM 产物核验：`.data _cn_gstatic_0/1 dq 0`、入口构造注入、
  字段级联析构、引用写回指令序列均在 target/v2asm.asm 实查。
- **单位机待办（下轮接手第一件）**：① linux-arm64 全量 E2E 复确认（上轮欠账+本轮 127 新增+
  ARM64 静态槽/.data 与 逻辑非修正的 GAS 路径实测——代码已对称实现未运行验证）；② 118
  Linux 侧输出不一致复查（win 绿）；③ 78 Linux -11 段错误分诊（「级联双重释放」假设）。
- **诚实边界（用户审计点）**：拥有式结构体的赋值/按值传参/返回**拷贝点未实现深拷贝**——宿主
  该三点为浅拷贝共享（对齐宿主=违反安全目标），超宿主需先定拷贝构造调用 ABI 且 127 无载体
  断言；留待 ③ 自举实测踩到时以专用用例（如 128）立项后实现。静态对象无退出析构（进程退出
  回收，与宿主一致）。`&静态`（静态取引用）未实现（127 无断言）。

## 用户决策记录（不可推翻，除非用户再裁）
1. ②c 四项一轮收官 + E2E 127 单用例载体（第四轮已批）；B4 指针槽模型（已批）；本轮无新增语法。
2. **本轮未经用户裁决、按推荐项执行的两处**（AskUserQuestion 未获答复，可推翻）：
   126 主.expected 三行修正（测试文件修改）；三新缺陷修复方案选择。

## 下一步（按 ③ 推进）
1. 单位机补跑 linux-arm64 全量（上述三项待办）。
2. **③ v2 编译 9 组件自举对拍**（下一轮主任务）：③ 硬前置本轮已就位——A 语义分析.cn 核心
   `向量<映射<整64,符号>>`（A 全绿）；C v2 组件遍布 `静态 映射/向量`（C 全绿）；D 主.cn:90
   `映射<整64,整64>& 已加载`（D 全绿——v2p 自身就是首个 D 用户）。对拍模式对标 79 闭环
   （宿主编译 v2 组件 → v2p 编译同组件 → 两侧 asm/exe 产物一致或对拍运行）。
3. ③ 踩到拥有式拷贝点/静态析构/&静态 等 127 未覆盖形态时：先立专用 E2E 用例（128+）再实现
   （E2E 先行纪律）；宿主语义不一致时按缺陷零容忍上报裁决。

## 踩过的坑（本轮新增，接手必读）
- **宿主 asm 是 ABI 唯一权威**：v2 接宿主符号的返回形态/参数约定，落码前先宿主编译最小同形
  源码看 asm（函数注释 `: ptr` 直接给分类）；「双平台一绿一红」优先怀疑平台差异而非逻辑。
- **MASM 内存操作数+立即数必须尺寸可推断**（cmp/test 等），栈槽一律先装载寄存器。
- **$ 扁平合成名切分按基名元数递归**，词扫描必须含数字（整64/浮32/整128）；naive '$' split
  对嵌套实参必错。
- **解析期按名全局表跨函数同名异型必串**：函数级权威表优先，全局表只做静态/兜底。
- **传给 ml64/link 的路径一律 ASCII**（rsp 按系统代码页读取，中文必乱码）。
- **v2 闭环用例期望文件**：win 口径 + 数值列置空 + 运行器 Linux 分支归一（119-125 惯例）；
  立项期空期望=宿主级回归（立项即绿），勿用「不注册」逃避。
- 本机（家机）无 as/g++：ARM64 侧改动只能静态对称实现，运行验证留单位机——HANDOFF 必须显式
  标注「已验证/未验证」边界。
