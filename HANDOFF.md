# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十二轮收官（深度系统 x86_64）——**plans/017 呈报①方案A 第四轮：v2self 第 7 层根治（第 14/15 枚）+ v2self 首次完整运行里程碑 + 第 16 枚立案**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、第 7 层根治两枚
1. **第 14 枚【根因级】元素() 返回含容器字段结构体→标量判定与宿主 sret 分叉**：旧判定「含容器字段→标量」系 E2E 127 供给类形态专属契约=**同名双 ABI**；宿主真语义三方 asm 实证 sret（rdi=retbuf/rsi=this/rdx=位置）。修复=IR容器.cn 判定改 `查结构体大小!=0` 一律 sret；配套 127 供给.cn 词条 类→结构体消灭双 ABI（check 绿+独立运行 exit=0）。Rust 准绳：类型即布局、布局即 ABI；sret 与 LLVM codegen 一致零开销。
2. **第 15 枚**：含容器字段结构体「调用返回初值」（`函数IR 扫函数 = 程序.函数们.元素(f)` sret 浅拷贝）被容器字段局部们无条件登记 RAII→出口级联析构=free 调用方容器→invalid free SIGABRT。修复=「初值末节点=调用且非构造→不登记」（与第 9 枚容器变量同款豁免）。

### 二、里程碑：v2self 首次完整运行
v2 编译的 v2 编译器（184139 行 asm）编译运行 min1.cn **全链 exit=0**，**产物与 v2p 逐字节一致**（asm diff 空）。

### 三、门禁全绿
构建零警告+单测 1240/1240+E2E 157 用例 130 过/0 败/27 平台跳过+v2p 主链冒烟全绿+九探针对拍一致全 PASS（mapref 修正导入后 引用和=99；vieww/elecalc/mapref 用自身源宿主 obj 供给容器实例化符号链接）。

## 下一步
1. **第 16 枚续挖（主线）**：容器系探针（f2/f3/alias/vieww/retown/elecalc/freg/mapref）v2self 内崩于 IR 生成（v2p 同源全绿）——查方法签名 帧实参们槽高 32 位被运行时小整数污染（低 48 位=上次有效指针）、写入点每轮实测干净+帧审计全正常——**拟 v2 源码分步打印插桩（dbg1~dbgN）定位写者**（比纯 gdb 快一个量级，lessons 方法学）。gdb 关键断点：替换类型参数带段们入口 0x410692 条件 `$rdx > 0xffffffff`。
2. **呈报③方案A**（已裁未做）：IR 名带参数签名（重载消歧机制层）。
3. T3/T4（runner 平台参数化+E2E v2 版用例）随 v2self 收口推进。
4. 跨机不变项：主.cn MASM 产物家机 ml64；E2E 153/154 win/arm64 复验；cqo 修复 win 运行级验证；**127 供给修订的 win/arm64 侧闭环复验**（本轮 linux-x86_64 侧 127 平台跳过未跑闭环）。

## 灰色点披露
- **IR容器.cn 单组件子链冒烟 exit=1**（节点_调用 未声明×2）——**既有状态非本轮回归**（git stash 实证 HEAD 同红）：上轮第 9 枚豁免代码引用 语法分析.cn 的 节点_调用，IR容器 子链无定义、主链合并可见；上轮「13 组件全绿」口径存疑（疑跑在豁免加入前）。主链全绿。若要根治：节点_* 常量族按 A2 单一归属原则收口至 IR常量.cn（注意主链重复定义冲突需先验）。
- v2self 完整运行达成度：min1/negdiv/ret_struct 全过；容器系探针崩（第 16 枚）——「逐层暴露逐层修」符合预期。
- 宿主变量名解析边界观察项（`加载荷` 三字名）待最小复现立案（上轮遗留）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径）。
- 探针/调试产物在 target/dbg/probe017/ 与 /tmp/v2dbg/（均不入库，/tmp 随系统清理）；本轮 v2self=target/dbg/v2self（含 v2self.o/v2p_rt.o/运行时 obj v2self_rt/）。

## 踩过的坑（本轮，已入 lessons 权重 10）
- **「供给契约」双态形态（声明×实现不同 ABI）是分叉温床**——历史规避性判定必须回溯宿主 asm 真语义（三方实证：被调方+调用方+直编运行）。
- **跨编译层豁免要成对**——容器变量豁免（第 9 枚）后同语义「含容器字段结构体」形态必在首个大规模程序暴露。
- **gdb 断点读参数必须断在 call 指令前**（断在下一条=读被调方执行后的残值，曾因此误判一轮）。
- **muldefs 双 obj 链接的可执行文件运行地址≠obj 偏移+基址**——反汇编定位一律用可执行文件本身。
- **watchpoint 是绝对地址跨函数存活**——栈复用的正常写入会触发（误报），须结合栈帧生命周期判读。
- **单组件子链冒烟与主链合并可见性不同**——加代码后必须重跑冒烟，「全绿」记录才可信。
