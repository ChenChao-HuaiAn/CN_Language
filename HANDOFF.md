# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🏃 329-a 开发轮：D38 批2=v2 负测防线语义层 6 项+317 定性销账·波次7 续·基线 865d157·分支 任务/家机-329-D38批2·协议v2 家机首分支·2026-09-18）

**侦查**：43 形态宿主行为矩阵三轮实测（target/p320a/d38b2_matrix*.py·宿主 check 逐形态）定规则表——隐式转换仅宽化/浮字面量按 f 后缀分型/布尔字符串源仅同型/目标字符特殊放行/复合赋值不拦/条件严格布尔（整数也拒）。

**六项全转拒**（d38_scan 复跑 10 拒/7 缺）：
1. **103 引用返回局部**：检查返回局部地址 扩「& 尾返回裸标识符/成员链」（引用参数/全局豁免）。
2. **109 类裸声明无默认构造**：重载感知构造统计表（个数>0 且无零参→拒）——IR类 构造串表单串覆盖式无法表达重载。
3. **305 返回类型匹配**：返回面标量隐式转换（隐式仅宽化）。
4. **341 条件严格布尔**：检查如果/当 双挂点（整数条件也拒）。
5. **350 浮点隐式转整**：声明/赋值/返回三面（浮字面量按尾 f 分型·目标字符特殊放行·复合赋值不拦）。
6. **343 显式转换实参**：类型名分支挂点（布尔目标仅收布尔源·字符串/结构体源拒）。

**317 定性销账**：Cx17 盘点唯一误列——正测用例（expected=输出比对·宿主不拒绝·240-a 创建·290-a 上架双测），盘点认名不认 expected。**D38 真缺=16 项非 17**。

**零误伤实证**：矩阵双端对拍 **65/65 一致**（宿主过=v2p 过·宿主拒=v2p 拒）；首轮对拍抓出 4 问题（343 调用节点子终点恒 0 陷阱/109 类型ID 索引↔ID 语义漂移/t10 比较二元布尔误伤/w07 重载覆盖误伤）——**单端验证全绿是假象·双端对拍是防线轮标配**。修复后 v2 树零错误编译+单测 1353/1353+全量 E2E 零回归（数字见提交信息·含 78/79 自举链）。

**顺带产出**：新立 **T49**（v2 词法 1.5f 后缀缺口·存量·矩阵 w11/w31 捕获·另案）；更新日志家机节 7 条重复记录存量漂移整节替换根治；lessons 追加 v2 语义检查三结构陷阱条目。

### 二、下一轮（按序）

1. D38 批3（304/325/347/363/372/374·入口/模块/私有/外部函数面·372 与 D24 入口合成链联动）——372 宿主为链接期拒绝·侦查更深。
2. T49 v2 词法 f 后缀（小轮·可与批3 拼轮）；T43/T46/T47/T48 待用户裁决。

### 三、坑与边界

- **调用节点 子终点解析期恒 0**（空闲位）——实参末位一律走 查调用实参末位 表；「子终点-1=实参末」惯例只对语句根区间成立。
- **类型ID 三种身份**（池索引/驻留ID/键基）——检查声明类型节点 引出参会把池索引改写为类型名ID，同函数内跨语义复用=静默守卫失败；正确源=声明类型名ID（-1=指针/引用/缺失）。
- **CN 结果 模式**：`查.正常 ? 查.值 : x` 宿主报错——三元不算正常性检查，必须 如果 形态。
- **比较/短路二元产物**：通用 推导二元类型ID 取左粒度会把布尔推成操作数类型——转换/条件面必须形态特判。
- v2 词法不认浮点 f 后缀（T49·基线同态非本轮回归）；顶层静态类实例裸声明不经 检查变量声明语句=诚实边界。


## 深度机 linux-x86_64 节

**交接时间**: 2026-09-18 ⚡ **334-a A12 里程碑标签锚点收工**（轻量轮·4 个 annotated tags 推 gitcode+github·命名规范 `里程碑/<YYYY-MM-DD>-<事件简称>`·明细=plans/025 §2.3）＋ **331-a（T11 静默防线·五项销项）集成挂起等 try-build 回签**（分支 `任务/深度机-331-T11静默防线`·头 da490f4·linux 列门禁全绿：单测 1363+E2E 445=443/0/2+反证双件·**其完整交接节在该分支·集成时并入**）。

### 一、334-a 本轮做了什么（A12·里程碑锚点）

1. **命名规范落定**：`里程碑/<YYYY-MM-DD>-<事件简称>`（「/」命名空间·`git tag -l '里程碑/*'` 可列举·annotated 带说明）。
2. **4 个 tags 建立并推送两远程**：①v2 自举重建立项（75f3c2b·2026-08-25）②v2 固定点 361684（14197dd·2026-09-13）③三机协作协议 v1（47f2c69·2026-09-16）④三机协作协议 v2（4d41d34·2026-09-18）；`git ls-remote --tags` 核验（含 peeled `^{}`）；tags=独立 refs 不碰 develop 分支。
3. **无 src/tests 写集** → 不触发三平台 try-build（快速门禁即可集成）。

### 二、下一轮任务（按序）

1. **331-a 集成**（等家机 win / 单位机 arm64 try-build 回签 → `integrate.py --try-build-done`）；回签要点=零警告构建+单测 1363+全量 E2E+**MSVC C4062 编译期守卫实证**（临时删 dispatch case 应构建失败）；
2. **T53 修复**（P0 安全·i128 元素容器追加段错误 rc=139·**根因已定位代码级**=`ptrElemStride`(ir.cpp:496) 泛型方法内 `T*` 未单态化替换〔typeSizeOf("T") 兜底 8·整64 巧合正确·i128 需 16 越界〕·修复=步进前走 `substGenericType`·实弹 run_err/m53_01）；
3. T46 待用户裁决（128 位静态初值发射·v2 侧已查正确=宿主单侧）；T49 残留（v2 侧同型面待查）；T33/D10~D12 波次7 续。

### 三、诚实边界

- tags 只打 4 个（历史里程碑提交检索耗时·**增量补打机制随规范**·后续轮按需补）；
- github 首推第 4 条 tags 输出截断（下次 push 核验补推）；
- 331-a 的完整交接内容（T11/T49/T50/T51/T52 五项明细+服务轮 9 项）在其分支的 HANDOFF 深度机节——集成时并入本节点；
- 本机无 win/arm64 工具链（交叉验证依赖回签·已按 §8.3 挂起说明）。


## 单位机 ARM64 节

**交接时间**：2026-09-18 332-a（看板先行制度·已集成 d00c08c）＋ 单位机 arm64 try-build 回签完成（d968794）＋ 329-D12 在飞（并行会话推进中）。**接手前先 `git fetch`——三机看板行/通告段常只在各自分支内，须读远端分支看板（`git show gitcode/<分支>:三机任务看板.md`）＋看板本机小节，别信本地文件。**

### 一、本轮做了什么（332-a 制度轮·已集成）

1. **用户令落地**（2026-09-18「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」）：AGENTS §8.1「看板先行」（开工第一动作＝直推看板本机行→才开分支；收工集成成功即清行直推销账）＋§8.5「强制直推、即改即推」。
2. **check_handoff「开工自检」**（含自证 bug 修正）：分支 `任务/*` 且未集成时看板必含该分支名，否则拦截；**豁免**「分支已集成」（`git merge-base --is-ancestor HEAD develop`）——收工销账清行后不再误拦。三情形实证（正例／销账态／未集成+无看板行）。
3. **看板表结构**：单表三行 → 三机独立小节（消除相邻行 rebase 必冲突·当日实测两次）。
4. **深度机 331-a try-build 回签**（§8.3·单位机 arm64 列）：**✅ PASS**——基线 **da490f4**（最新头；首轮基于 7d29d42 未含 T51/T52 增量，已重跑），零警告构建＋单测 **1363/1363**＋E2E 全量 **446=444/0/2**（0 失败·无新增红）。证据=独立 worktree 全量跑测（`/home/user/projects/cn_tb331`）。
5. **T50 撞车回应**：本机 329-D12「随载 T50」与深度机 331-a T50 为**同缺陷双实现**（本机=文本形态口径·双后端各改一份；深度机=`types::isStaticScalarInitType` 单一归属）——**已声明同意取深度机实现**（集成顺序孰先孰后均以其取齐）。

### 二、验证链

（332-a）check_handoff 正反例三情形＋integrate.py 快速门禁（两批集成 b45e4ae／d00c08c）；（331-a 回签）独立 worktree 全量：零警告构建＋`./target/cn_unit_tests` 1363/1363＋`run_e2e.py --cn target/cn --target linux-arm64 --jobs 8` 446=444/0/2。

### 三、实测发现（三机协同实况）

- **家机 329-a**（`任务/家机-329-D38批2`·v2 语义层 6 项）在飞；**深度机 331-a**（`任务/深度机-331-T11静默防线`·头 da490f4）**等家机 win 侧回签**（MSVC C4062 守卫实证）后集成——**arm64 侧已由本机回签 ✓**。
- **单位机 329-D12**：并行会话推进中——远端头 ffc3151（收工＋看板 try-build 请求），**本地尚有未推提交 e1afc7e（补③：验证面补强）**；主工作树有未提交改动（货舱特性延伸：`CN语言编译器v2/主.cn`／`货舱解析.cn`／`src/cn_compiler/driver/cargo_parser.cpp`／310 双编译对照）——**接手勿动、勿回滚**。
- **跨机轮次号撞号实锤**（家机与本机同为 329-a）——机制缺陷待用户裁（候选：认领前先 fetch 看板行＝先到先得／机号段隔离／跳跃取号推广全机）。
- **本机 329-D12 已发 try-build 请求**（随其收工提交）→ 待家机/深度机应答。

### 四、下一轮候选

1. **家机**：应答深度机 win 侧 try-build（C4062 守卫实证）＋单位机 329-D12 的 win 侧回签。
2. **深度机**：集成 331-a（`--try-build-done`）＋应答单位机 329-D12 的 linux 侧回签。
3. **单位机**：329-D12 收尾集成（T50 让位按深度机单一归属实现取齐）。
4. C20 波2／C25（v1 残留清除·70~77＋89 用例处置）；A12 里程碑标签。

### 五、诚实边界与坑

- **worktree 位置坑（332-a 实测）**：CMake 用 `${CMAKE_SOURCE_DIR}/../../third-party/unittest/googletest` 找本地 gtest——**worktree 必须放在 `/home/user/projects/` 深度**（`/tmp/xx` 会让 `../../` 解析错位→配置失败）；产物在 **`target/` 根**（不是 `target/build/`）：单测=`./target/cn_unit_tests`、编译器=`target/cn`。
- **Python 切片整节替换 off-by-one（332-a 自证 bug）**：`lines[:73]` 含 index 72 那行节标题，再拼新标题 → **标题重复**（HANDOFF/更新日志各一处·本批已修）；正确=切到标题行**前**（`lines[:72]`）。
- 开工自检判据已含「已集成豁免」（本轮自证 bug 根治）；非 git 环境自动跳过。
- **别信本地文件**：三机在飞状态必须 `git fetch` 后读**远端**分支/看板（332-a 直接教训：本地看板三行全空、实际两机在飞）。

