# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十九轮（**深度机 linux-x86_64**）——**plans/019 阶段3「常量 只读引用参数+借用互斥第一版」双侧落地**（Rust &T 只读借用纪律·CN 化语法=常量 前缀）。v2self 固定点复锚 **244605 行 = X64L 新口径** + 组件对拍 29/29 + 运行级 3/3 + 全量门禁 217 用例（唯一败=194 既有待批）+ 单测 1281/1281。

---

## 一、本轮做了什么

1. **语言语义**（规范同步 plans/001+specs/04）：`常量 类型& 名`=只读借用——赋值/复合赋值/成员链赋值拒、传可变拒、同调用可变×只读互斥（语句级第一版）、借出升级（作 T& 返回）拒。
2. **宿主六文件**：ParamDecl.isConstParam+parseParamDecl 常量 前缀；FunctionInfo.constParams+currentConstRefParams_；checkConstRefBorrowDiscipline（普通函数调用面，wrapRefArgs 前）；赋值面；借出升级拒。
3. **v2 三处**：解析参数 常量 前缀（类型节点附加 256→257——IR 层 >=256 判定天然按引用 ABI 零改动）；检查函数 收集；赋值面只读拒。
4. **E2E 200/201/200_v2/201_v2** 四用例双通道 + 单测 7/7。
5. 锚定链：fix_p 244605 行（244050+555）→固定点逐字节一致+绑定自检+对拍 29/29+运行级 3/3。

## 二、下一轮任务（按序）

1. **plans/019 阶段 3b 余项**：构造/方法/函数指针调用面接入借用纪律；可变×可变双别名拒；v2 传可变/互斥面（随实参边界记录——195 同族）；**转移浅拷贝优化**（IR 槽位交接——阶段3 性能主项）；NLL 跨语句活跃区间（按误报率实测决定深度）。
2. **plans/019 阶段 4：不安全区边界**（不安全 函数 修饰——详案 §5.3，含 stdlib/v2 迁移分批方案）。
3. **194 测试改写待批挂账维持**（第五十六轮呈报）。
4. **跨机轮（win/arm64）**：第五十七~五十九轮三轮变更复验与口径重锚。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 深度机 linux-x86_64（本行口径）
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1281/1281（+7 ConstRefCheckTest）
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 217 用例（败=194 待批，跳=62/69/78/79）
bash target/p59x64l/run_chain59.sh   # 锚定链（[3] readelf bug——map 手工判定
#   cn_main=0x4dee8f ∈ cn_self59.o .text [0x402586,0x4e291b)；[6] 段口径错——runlevel50.sh 口径；
#   173 入口实名 173_v2_目录包根撞名成员）
bash target/p59x64l/cmp29_59.sh      # 组件对拍 29/29
# 固定点 X64L 新口径=244605 行
```

## 四、本轮踩坑

- v2 池.设置 返回 结果 丢弃触发强制检查规则1（宿主编译 v2 全树 30 秒暴露）——v2 源码改动后全树冒烟纪律再次兑现。
- python 批量替换脚本按文件分断言（assert 失败=该文件未写盘=事务性），首脚本因锚文本在另一文件失败两次——锚文本先 grep 实际文件再写。

## 五、诚实边界

- 借用纪律接入面=普通函数调用；构造/方法/函数指针随 3b；可变×可变双别名不拒（防存量误伤，3b 实测后定）；互斥=语句级（跨语句随 NLL 3b）。
- v2 只读面=赋值位；传可变/互斥面随实参边界记录另轮。
- 194 待批维持；win/arm64 复验=下轮跨机轮；探针与脚本留 target/p59x64l/ 不入库。
