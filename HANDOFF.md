# HANDOFF 交接文档

**交接时间**: 2026-09-10 第六十四轮（**深度机 linux-x86_64**）——**plans/019 阶段4 第二层第二批**：泛型修饰语法+泛型类实例化拷贝补丁+v2 语法层认 不安全+v2 树/stdlib 全量迁移（警告 175→0 归零）。v2self 固定点复锚 **245450 行**。专项累计九轮（c924145~本轮）。

---

## 一、本轮做了什么

1. 宿主泛型函数修饰语法（泛型 <T> 不安全 函数）+逆序 补迁。
2. 泛型类实例化 mi.isUnsafe 拷贝补丁（tg 探针实证第六十三轮容器迁移未生效的根因）。
3. v2 词法+解析认 不安全（首版吞「函数」分支致 v2p 全解析瘫痪——全量 E2E 揪出+stash 二分定位+双分支恢复）。
4. v2 树 43 函数+stdlib 映射集合 9 方法迁移——**全链警告归零**。
5. E2E 206+全量 223 例 218 过+锚定链 245450 固定点一致+对拍 29/29+运行级 3/3。

## 二、下一轮任务（按序）

1. **第三批收口时点呈报**（迁移全量完成=收口前置就绪；建议真实使用反馈期后收口变硬错误+删观察期）。
2. **3b 残余**：v2 实参边界记录+传可变/互斥面（NLL 按误报率实测定深度）。
3. 立案清单：stdlib 泛型跨模块导入不可见（ti 配方）；类静态+析构写缺陷（tp3）；194 单行批准。
4. 阶段4'（两轮征询无答复挂账）；阶段5 等并发库立项；跨机轮（六十四轮变更复验）。

## 三、验证链

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 223 用例（败=194 待批，跳=62/69/78/79）
# 警告归零验证：target/cn check CN语言编译器v2/主.cn → grep 安全区 = 0
# 固定点 X64L=245450；v2 变更后锚定链范本 target/p64x64l/
```

## 四、本轮踩坑（重要）

- **v2 解析分支替换吞原分支**：python 整段替换把「函数」分支换成「不安全 函数」分支——普通函数声明失去分支→v2p 解析任何源码节点=0 全瘫。**预防：插入新分支的替换必须保留原分支（锚取原分支整体、new=新分支+原分支）**；全量 E2E 是 v2p 行为级唯一可信门禁（单文件 check 过≠v2p 工作）。
- **泛型类实例化拷贝清单**：ClassMemberInfo 新字段须同步 generics.cpp 实例化拷贝段（isConstMethod 旁）——登记侧+实例化侧两处清单。

## 五、诚实边界

- v2 认 不安全=语法放行零语义（观察期 v2 不查边界——v2 侧收口随第三批）。
- v2 类方法/泛型函数 修饰=宿主侧（v2 语法层仅顶层函数放行）。
- 194 待批维持；win/arm64 复验=下轮跨机轮；探针留 target/p64x64l/ 不入库。
