# HANDOFF 交接文档

**交接时间**: 2026-09-11 第七十二轮下半程收尾（**深度机 linux-x86_64**）——**上轮核实（成立）+ 瑕疵清理 4+1 + 类方法返回路径缺陷当轮根治 + 三层防线落地（E2E 213/214 探针转正 + plans/020 拥有权流转矩阵）**。验证链全绿，固定点口径 **266964** 维持。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **上轮核实**：复跑上轮（2970da0）全验证链+探针 66/86——结论**成立**（六枚缺陷诊断真实、修改正确、流程合规、声明全部复现：E2E 234 用例 229 过/固定点 266964/对拍 29/29）。静态审查另揪出 4 瑕疵+1 口径观察，向用户呈报后获「全部实施」授权。
2. **瑕疵清理 5 处**：①`(void)0` 死代码删除；②`LoopContext::scopeDepth` 改名 `classBase`（原命名失准），`genLoopJumpDestruct` 通用化为 `genJumpDestructFrom(stringBase, classBase)`；③genVarDecl 注释修正（污染名=登记+释放侧跳过双保险）；④泛型出口补五状态 clear（**先实证**：visitProgram 第 4 步批量生成不嵌套、clear 安全）；⑤**switch 中断 drop-on-jump**：`switchStack_` 改 `SwitchContext{exitLabel, classBase, stringBase}`（选择分支不走 genBlock，中断 跳出前按分支基线释放，对齐循环口径）。
3. **清理中发现真缺陷并根治（探针实证闭环）**：`emitClassMethod` 为三类函数级生成单元（visitFunctionDecl/emitGenericFuncInstance/emitClassMethod）中**独漏函数级兜底者**——类方法体内声明拥有串后中途 返回 绕过 genBlock 出口析构=泄漏（**探针 87 修复前残留 1**，补 genStringFrees/genClassDestructorCalls+clear 同构收尾后**归零**）。连带修正文档口径：块级析构实现实际覆盖**无析构类**的内存释放（原「限有析构函数类」声明落后于实现）。
4. **三层防线（用户裁决全实施）**：①探针转正——E2E **213_块级作用域RAII内存回归**（八形态残留全零：探针 66 六形态+选择体中断+类方法返回路径）+ **214_容器元素所有权回归**（探针 86 搅动形态）；②**plans/020 拥有权流转矩阵**建档（27 格位置×语义×验证×靶子，空白即靶子）；③NLL=治本归口。
5. **验证链全绿**：清场重建零警告+单测 1289/1289+E2E **236 用例 231 过**（唯一败=194 既有待批）/4 跳+锚定链 fix_p=fix_s=**266964** 逐字节一致（v2p 经新宿主重建后固定点维持）+绑定自检+运行级 3/3+对拍 29/29。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子**（空白即靶子，按价值排）：①v2 侧跳出路径（中断/继续）drop-on-jump 专项核验（v2 已有块出口析构+落空释放，跳出路径未核）；②入容器编译器级自动 copy/move（容器析构轮——A2 显式复制纪律的编译器接管）；③数组元素/装箱字段/结构体字段借用面（NLL 轮）；④函数级 genClassDestructorCalls 的 hasDtor 限制与块级口径统一（观察项）。
2. **语义观察项（须用户裁决，勿自行改）**：循环内选择体 `中断` 绑定循环（loopStack_ 优先）vs C 语义最近绑定选择——规格书 03 未明确嵌套绑定，改绑定属语义变更+规格书同步。
3. **呈报清单余项**：194 单行批准（已呈报四轮无答复）；win 平台 v2 并行隔离（家机轮）；71-a 后续可选题（构造器实参类型匹配/重定义诊断）。
4. **矩阵维护纪律**：每轮触碰所有权语义的改动必须同步更新 plans/020 对应格。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处 warning/error 匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 236 用例 231 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（宿主改动后须先删 v2p 缓存键重建）
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
```

## 四、本轮踩坑（已入 lessons 第七十二轮收尾段 ⑩~⑬）

- **探针实测与静态审查两条腿缺一不可**：探针测得出泄漏/悬垂（量化），测不出死代码/命名失准/注释不符/状态不对称——本轮类方法真缺陷恰由静态推演引出、探针 87 实证闭环；探针全绿≠diff 干净。
- **「对称补齐」前先核验生成单元的嵌套关系**：泛型实例=visitProgram 批量生成不嵌套（clear 安全）；若嵌套生成照抄 clear 反而清掉宿主活状态（stringTainted_ 即此类）——对称性以调用时机实证为准，不以「长得像」为准。
- **文档声明与实现不符时以探针实测为准并回改文档**（块级析构覆盖面声明落后于实现）。
- **CN 类体后 `}` 自终结不加 分号**（规范 03 规则1）——C++ 习惯 `};` 连踩两次（探针 87 与 E2E 213），写 CN 类型定义后自查。

## 五、诚实边界

- 函数级兜底 genClassDestructorCalls 仍限有析构类（块级已覆盖无析构类内存释放，统一口径=观察项在册 plans/020）；循环内选择体 中断 绑定=语义观察项（待裁决）；数组元素/装箱字段/结构体字段/静态=借用无 RAII（NLL/容器析构轮靶子）；v2 侧跳出路径 drop-on-jump 待专项核验。
- 构造器实参类型匹配不检查（v2 既有）；装箱结构体实参 CopyStruct 深拷内含容器字段浅共享（宿主同边界）；跨编译器结果 ABI 互操作未单独 E2E 锚定；v2 契约登记按纯名/键基回退查（重载族返回异型=近似）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。

## 六、关键产物位置（target/ 不入库）

- 深度机：target/p71x64l/（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **266964 行**、cn_self71）。
- 探针（/tmp/p72probe 临时，不入库）：探针 61~87（本轮新增 探针87_类方法返回路径：修复前后残留 1→0 实证）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——每轮触碰所有权语义必须同步更新）。
- E2E 新用例：tests/e2e/213_块级作用域RAII内存回归/ + tests/e2e/214_容器元素所有权回归/（内存安全回归防线，改 RAII 者必被此门禁拦截）。
