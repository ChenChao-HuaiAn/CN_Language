# HANDOFF 交接文档

**交接时间**: 2026-09-01（P7b 全部完成：容器全链路+三项裁决+__cn_intern 根治）

## 当前任务（2026-09-01）

v2 自举重建（plans/013 P7 / plans/014）。本轮完成：**P7b 容器类型实例化与方法
调用全链路**（E2E 123）+ 新规则三项裁决落地（任意参数数/块级析构/stdlib 签名表）
+ **宿主 __cn_intern 指针悬垂 UB 根治** + 链式字段取值根治（E2E 124）。

## 本轮要点（接手必读）

1. **新规则（AGENTS.md，用户令 2026-09-01）**：①v2 双目标——性能超 C++ 编译器、
   安全接近 Rust；②缺陷零容忍——任何遗留缺陷必须暂停汇报 ≥3 方案经裁决。
2. **__cn_intern 根治（lessons 13.0 权重）**：pool vector→deque（c_str 悬垂是
   「UB+复现失败+asm全对」三特征的首要怀疑对象）；体内二分定位法高效。
3. **v2 容器链路**：语法（$ 合成/前瞻/构造/方法标记）→ IR（60-64 操作码：分配
   对象/结构体拷贝/删除对象/调用参数化+调用实参 参数区）→ 代码生成（宿主同款
   nameMangle/EXTERN/字符串数据段）。**签名表**：IR签名.cn 扫描 stdlib（57 条），
   T/K/V 替换，硬编码已删。
4. **v2 组件 9 个**（+IR签名.cn），全部 <=1000 行。

## 下一步（②b→③）

1. **②b 结构体元素容器**（③ 前最大缺口）：向量<Token>——v2 结构体布局对齐宿主
   typeSizeOf（整32=4 紧凑+对齐）、结构体按值传参 ABI、元素(i) 视图语义（H8-4/5）、
   追加/删除 结构体元素深拷贝与析构注入。
2. 嵌套容器（向量<映射>）、容器作结构体字段（函数IR）、顶层静态容器（v2 IR 静态
   支持）、& 引用参数语义、容器按值传参/返回。
3. v2 剩余语法（③ 前）：指针下标 p[0]（IR 无分支）、字符串拼接 +/三元/+=、内置
   函数符号映射（驻留→__cn_intern 等已通；打印到错误/文件::*）、枚举、类型大小。
4. **③ v2 编译 9 组件自举对拍**（链接模式就绪：/FORCE:MULTIPLE+v2asm 在前+map
   双向自检）。
5. P4 代码清理（结果.值 临时绑定规避可去除）。

## 踩过的坑（本轮新增）

- bash $? 8 位截断（300 显示 44）——验证退出码用 python subprocess。
- python 写 CN 源码：字符串里的 
 必须拼 chr(92)+'n'（heredoc 内 '\n' 会变
  真实换行/裸反斜杠，本轮踩 5 次）；复杂注入优先 Write 工具直写。
- 结果.值 访问须「如果 (x.正常) 真分支」模式（前置 !正常 返回 不算）。
- 布尔 传 整数转字符串 需 ? 1 : 0。
- v2 词法关键字表 25 个——stdlib 的 泛型/类型 等不在内（token 是标识符）。
- MASM 247 符号限制：v2 函数 >=8 参须核算 mangled 长度。
- 调试产物 target/dbg/（p7b1~p7b12、c9 等，不入库）。
