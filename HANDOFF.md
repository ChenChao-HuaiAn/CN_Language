# HANDOFF 交接文档

**交接时间**: 2026-09-12 第七十九轮（**深度机 linux-x86_64**）——**79-a 聚合拥有型字符串字段
drop glue**（plans/020 矩阵靶子 #16 装箱字段接收 / #17 结构体字段持串销项；同族容器元素缺口
一并收口并发现两枚既有缺陷当轮根治；宿主新增 1 文件 + 改 5 文件、v2 改 11 文件）。
验证链全绿，固定点新口径 **309101**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 下一轮任务 #1「plans/020 矩阵靶子余项」之 #16/#17。用户令：遵守
   AGENTS.md 第3项纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **靶子实证**（探针 P1~P4，块出口 `内存::活动分配数()` 差值）：①结构体字段持串
   **残留 1（宿主/v2 同）**；②装箱（结果/可选）值字段 **残留 1（宿主/v2 同）**；
   ③数组元素残留 2（宿主；**v2 数组类型整体缺口**——语法层不解析 `数组[N]`，探针实证）；
   ④字段容器接收者残留 2（B35 靶子维持）。

3. **三方案裁决（性能第一 / 安全第二）**：
   - **甲（实施）聚合字段 drop glue 全链**——Rust drop glue 编译期展开：写入位归一化
     （转移=move／拥有返回=接管／字面量=驻留／其余=`__cn_str_copy` 落堆）+ 拷贝位深拷
     （preFree + memcpy + postCopy，**自赋值 no-op**）+ 消亡位释放（块出口/跳出/函数尾 +
     **返回移出豁免**）+ **联合体条件释放**（结果/可选 值/错误 同偏移，非激活分支值位
     =整数，无条件 free 即崩）。
   - 乙（只加释放面）排除：浅拷无静态唯一持有者判据（双端释放=悬垂 / 都不释放=泄漏）。
   - 丙（Move 化：赋值/传参/返回改 move）登记后续专项（性能最优但语言级语义变更面大）。

4. **实施（宿主）**：新增 `ir/ir_fields.cpp`（收集/释放发射/深拷三件套单一归属）；
   `ir.hpp` 名单与基线（`ownedFieldOrder_`/`scopeFieldBase_`/上下文 fieldBase/`fieldTainted_`）；
   `ir_stmt.cpp` 登记+块基线+声明位深拷；`ir_oop_call.cpp` 三处释放路径+归一化重构
   （`normalizeStringValueSource` **已求值版本——避免二次求值泄漏**）+入容器位字段污染；
   `ir_expr.cpp` 字段写入归一化（先求值+复制后 free 旧值）/字面量字段归一化/整体赋值深拷。

5. **实施（v2）**：资源字段族命名归一（容器字段→资源字段，字符串字段纳入同表同名单）+
   细分计数（`结构体串字段数`/`串字段数`）；语法层串字段登记；`IR容器.cn` 释放分派
   （字符串=free+清槽 / 嵌套结构体=递归 / 容器=原路径 / 结果可选=**条件释放**IR 条件分支）
   + `发射串字段清旧`/`发射串字段深拷`/`发射结构体整体拷贝`/`在资源字段名单`/
   `标记入容器结构体污染`；`生成声明/生成语句/生成表达式（求值）`三处归一化与
   浅拷（调用源接管）/深拷（标识符源）分派；`IR生成.cn` `登记串污染镜像`。

6. **两枚既有缺陷当轮根治**（探针 P5/P11，**基线 v2p/HEAD 复现确认非本轮引入**）：
   - 宿主 `可选<T> 空 = 无;`（NullLiteral 初始化）按 CopyStruct **从地址 0 拷贝=段错误**
     （gdb `mov r9,[rbp-200(=0)]` 实证）→ 改逐槽零初始化。
   - v2 `结果<字符串,E>.错误` **载荷类型误取 T**（`生成字段访问` 原按成员名不区分
     `.值`/`.错误`，一律用首实参类型码）+ 装箱构造写入侧联合体偏移按当前载荷单侧定
     （`错误(5)` 写偏移 4 / 读取偏移 8=读写不一致）→ **错误码判别静默失效**
     （三侧对照：宿主 5 / v2 空或垃圾）→ 按成员名分派 + max(T,E) 偏移读写两侧一致。

7. **防线**：E2E **230_双编译对照字段所有权**（移植纪律 9 **第六个**对照用例：八形态
   折校验和 **255** 两侧一致）+ **反证实证**（注入「宿主聚合登记失效」→ 255→**80**；
   注入「v2 资源字段登记失效」→ 255→**248**；两侧已恢复残留=0）。

8. **文档五件套**：plans/020（第十一节 + 矩阵格 #15/#16/#17/B35/既有缺陷登记 + 验证链）+
   plans/019（跟踪表行）+ lessons（第七十九轮段，权重 8，含预防 ㉞~㊳）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **v2 内置构造器识别缺陷（既有，本轮登记）**：v2 中 `结果/可选` 构造器（`正常/错误/某些`）
   在**全文件无结构体声明**时不展开为内置构造器 → 生成普通函数调用 → **未定义符号链接失败**
   （探针 P10/P10a/P10b 实证；基线 v2p 复现）。根因候选=合成结构体（结果$T$E）布局表/
   类型登记依赖结构体声明解析路径。宿主无此问题（handleResultCtor 恒识别）。
2. **容器元素 = 含串字段结构体专项**：入容器位深拷（现在标记源为字段污染）+ 容器元素字段串
   释放面（运行时辅助按元素步长+字段偏移平铺/链游）——销项后 B35 字段容器接收者归一化
   一并收口。
3. **v2 数组类型支持 + 两侧数组元素所有权**（矩阵 #15）：宿主侧数组元素持串无 RAII（靶子）；
   v2 语法层整体缺口（`类型[N]` 声明/初始化列表/下标读写）。
4. **plans/020 矩阵余项**：字符串键（`映射<字符串,...>`）支持轮；集合<有析构类> 删除前移的
   旧对象析构（既有边界）。
5. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区变硬
   错误）；v2 诊断无行号（架构级）。
6. **跨机轮**：arm64（单位机麒麟）复验本轮字段所有权与 E2E 230（linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）——字段所有权改动后重建（E2E 缓存指纹自动处理）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 257 用例 253 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 309101 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`
> （p71.s/s71.s/cn_self71），并发会踩出假警（串行复跑正常）。
> **全量 E2E 与手工 v2p/固定点操作不得并发**：本轮实测「E2E 运行中重建 v2p」→
> `PermissionError: v2p_linuxx64`（runner 正在执行该文件被替换）。先等门禁结束再手工操作。
> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时
> 删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p（验证假绿）。

## 四、本轮踩坑（已入 lessons 第七十九轮段）

- **㉞ 拥有型资源的裁决要按「存放位置形态」逐格核对**：规格书 10.3「字符串局部变量=拥有型」
  只覆盖局部槽——聚合字段是独立位置（写入/拷贝/消亡三路径各自缺一即泄漏或 UAF）。
- **㉟ 加释放面前先问「这个槽位的唯一持有者是谁」**：浅拷共享槽必须先归一化再释放；
  **深拷三件套的顺序**（先复制后释放）+ **自赋值 no-op** 是必备安全边（`甲=甲` 时
  preFree 释放待复制源 → memcpy 拷回已释放句柄 → UAF）。
- **㊱ 联合体字段的释放必须带条件**：值/错误 同偏移布局下非激活分支值位=整数，无条件 free 必崩。
- **㊲ 修改装箱构造/读取任一侧偏移时同步核对另一侧**：v2 写入偏移 4 / 读取偏移 8 分叉，
  症状是「错误码读垃圾」而非崩溃——只能靠三侧对照探针发现。
- **㊳ 二次求值=多余分配泄漏**：归一化助手必须提供「已求值版本」——把「求值+分级」捆在一个
  函数里，只能复用一半的调用点会默默多求值一次（P7 探针：字段写入泄漏 1，汇编可见两次
  `__cn_str_copy`）。
- **验证链并发禁忌**（第七十七轮 ㉙ 的复发形态）：E2E 与手工 v2p 操作并发 → 权限错误。

## 五、诚实边界

- **结构体按值传参**浅拷（参数=借出视图、被调方不释放=零拷贝，与字符串参数 A2 契约一致）；
  被调方写参数字段=归一化复制落堆（被调方不释放=泄漏，宁漏勿错）。跨函数数据流
  （参数/全局中转）字段所有权不健全。
- **容器元素=含串字段结构体**：入容器位标记源为字段污染（元素浅拷共享 + 元素字段串释放面
  未开=靶子）；深拷 + 元素字段释放随容器元素专项（下一轮 #2）。
- **数组元素持串**：宿主靶子维持；**v2 数组类型整体缺口**（独立立项）。
- **结构体容器字段拷贝**：浅拷共享对象指针（双释放风险，宿主/v2 同缺=既有边界）。
- **v2 内置构造器识别依赖文件含结构体声明**（既有缺陷，下一轮 #1）。
- v2 `结果值偏移` 两档近似（整32/其余）：T/E 两档内已按 max 收敛；结构体载荷（结果<结构体,E>）
  偏移近似=边界（宿主正确）。
- 缺陷零容忍登记：本轮发现的两枚既有缺陷已当轮根治；**v2 构造器识别缺陷（#1）为下一轮
  第一优先**——它是「静默失效→链接失败」形态（阻塞型，非静默运行期错误）。
- win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s=fix_s.s **309101 行**、cn_self71）。
- 探针（/tmp/p79probe，不入库）：P1~P4（靶子实证）、P5（宿主全形态）、P6（双侧全形态对照，
  已转正 E2E 230）、P7（字段写入 preFree 最小复现）、P8（释放计数验证）、P9~P14（v2 分形态定位）、
  P11（`.错误` 三侧对照）、v2p_base（HEAD 版 v2p 基线对照）、v2p_dbg（诊断版）。
- E2E 用例：`tests/e2e/230_双编译对照字段所有权/`（主.cn + 主.expected + 双编译对照.txt，
  八形态折校验和 255）。
- 宿主新文件：`src/cn_compiler/ir/ir_fields.cpp`（+ CMakeLists 注册）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（第十一节第七十九轮 + 矩阵格 #15/#16/#17/B35
  更新 + 既有缺陷登记 + 验证链；每轮触碰所有权语义必须同步更新）。
