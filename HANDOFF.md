# HANDOFF 交接文档

**交接时间**: 2026-09-04 第十一轮（单位机 麒麟 ARM64）——**语法分析.cn 冒烟 55 错四簇消化（超预期：5 组件冒烟 0 错）+ 簇⑥宿主星号泛型根治 + 3 新 E2E 全绿 + 分号专项立案**。上轮「下一步①（54 错三簇）」清零并超额完成。

## 本轮已完成（全部有实证）

### 一、冒烟 55 错四簇定位与根治（四项用户裁决：A2/B1/C1/B1a）
三层定位法（文件分摊→函数二分→形态探针矩阵）全程实证，四簇根因+消错：
- **簇①跨模块符号（35+14 错）→ A2 新建 IR常量.cn**：IR_*/T_*/节点_*/键位移/当前函数变量类型 单一归属（原散在 IR生成/语法分析 顶层，IR容器 引用不可导=循环，主链靠 P6h 全局合并掩盖）；七组件显式导入，依赖图单向无环。
- **簇②文件 API 导入项（4+级联 错）→ B1 导入项登记+裸名改写**：`导入项们` 表+调用形成时被调标识符（**参数起点-1**）原地改写限定名（第五波机制同构）；**权威登记点=解析程序 入口清空+重扫**（依赖先解析窗口会抹掉 扫描导入 登记——实测 4 错残留定位）。**B1a 边界入档**：宿主=stdlib 包装（结果<> 契约）vs v2=内置直调（裸指针契约）——方案陈述错误已向用户澄清，裁决维持+入 plans/014 灰色点，随「v2p 源码级并入 stdlib」专项收口。
- **簇③词法独缺 --（11 错）→ C1 自减全链**：词法 -- + 节点_自减=35 前后缀 + 语义常量/未声明拒绝 + IR `发射自增自减` 共享函数（局部/参数/引用写回/静态四路径）；运行时探针 a=3 b=98 静态=9 双绿。
- **簇④泛型实参星号后缀（分诊揪出）**：解析类型文本 加 是实参 参数（实参位消费 `*`/`&` 并入合成名；外层指针层数登记不抢吃——参数化是防 `整64* p` 层数归零误伤的实测修正）。

### 二、连带揪出并当场根治（簇⑤⑥，缺陷零容忍纪律）
- **簇⑤结果字面量初始化**（E2E 144 探针 P2 运行期 SIGSEGV）：`= 无` 等字面量初始化结果/容器变量错误放行+IR 结构体拷贝瞎发射——语义层显式拒绝（拷贝/直调形态放行=B1a/P7c 边界）；v2p 尾部负测自检固化。
- **簇⑥宿主星号泛型实例名误剥**（宿主缺陷优先纪律当场根治）：泛型实例名含实参星号（盒子$整64*）被三处「尾 *=对象指针」误剥（semantic_expr visitMemberExpr / semantic_call clsName / ir_oop_call canonObjForMethod）→ 查类失败。调试打印实证 varType 全程正确、丢点全在消费侧；修复=**原名查类命中即用原名**（真指针不命中类表自然入剥分支，v2.1 行为不回退）。全链探针（字段+方法+解引用+写透+向量<整64*> 容器）exit=0。**连带观察项**：cn check 对含导入的非入口文件名（≠主.cn）私有函数体被模块可见性过滤静默丢弃=**检查假绿**（定位中多个「检查通过」实为没检查；最小复现必须用 主.cn 文件名）——plans/014 已入档。

### 三、冒烟成果（超本轮目标）
**5 组件语义 0 错**：词法分析/IR签名/IR容器/语法分析（目标）+ **语义分析（连带达成**——IR常量 下沉使链上符号全齐）。IR生成 入口 96 错=**下一批冒烟目标**。

### 四、E2E 与门禁（单位机侧全量）
- 新增 **142_v2_自减全链 / 143_v2_导入项裸名改写 / 144_v2_泛型星号实参**（+供给 B7：向量$整64* 方法符号宿主实例化）六步闭环全绿；v2p 尾部四负测自检行（常量自减/未声明自减/未导入裸调/结果字面量初始化）固化。
- 宿主清场重建（gcc ARM64 Debug）**零警告**；单测 **1216/1216**；E2E 全量 **146 用例 142 过/1 败(78 已知遗留 SIGSEGV)/3 跳(62/69/79 平台限制) 零回归**。

### 五、规范级裁决与专项批复（plans/015 已批——家机实施）
- **语句边界模型**：用户裁决**方案B 强制分号（Rust 式）**；**待确认项已批复**（2026-09-04）：①导入语句 `导入 路径;` 加分号（Rust use 同款）；②顶层常量/静态声明加分号（`常量 T = 2;`，C++ const 同款；枚举/结构体/类体仍 `}` 自终结）——详见 plans/015 §五裁决记录。**实施=家机专项独占轮**（单位机本轮收尾推送后）。

## 下一步（家机会话接手——分号专项，plans/015 已批）
1. **前置门禁**：家机 ci.ps1 全量复验本轮单位机变更（宿主 3 处 .cpp 簇⑥ + v2 组件 9 文件含 IR常量.cn 新建 + 测试 4 文件含 142/143/144 三用例）——win 侧 143/144/145（现 146 用例）应 142 过/1 败(78)/3 跳与单位机一致；冒烟五组件 0 错同样复验（v2p 用家机 cn 重建）。
2. **分号专项实施（plans/015，裁决已批复：导入 路径; + 常量/静态 加分号）**，建议顺序：
   a. 规范侧先行：plans/001 与 cn-language-spec（01a/03）条目改写 + 迁移脚本（python 一次性，词法级注释/字符串/嵌套块感知，语句结尾 token 后跟边界补 ;）；
   b. 宿主 parser：语句分支消费 ; + 缺失诊断（可继续报全量位置）；
   c. v2 语法分析.cn：解析语句 各分支消费 ; + 解析块 按分号切分（块截断静默家族整体消亡）；
   d. 全库迁移：146 E2E 用例（含 expected 逐行比对）+12 v2 组件+10 stdlib+单测源码字符串；
   e. 门禁：ci.ps1 全量（零警告+单测+E2E 全绿）+ v2p 冒烟 5 组件 0 错；迁移 diff 随轮呈报。
3. **IR生成.cn 冒烟 96 错消化**（分号专项后，第 6 组件）：沿用三层定位法；IR 层 [ir2] 级诊断（结构体整体初始化/容器初始化/方法调用对象类型——本轮冒烟 IR 推进已预览）与「IR 层错误即中止」纪律未建立（139 只锚定语义层）。
4. **簇⑥连带观察项**：cn check 非入口文件名检查假绿（模块可见性过滤静默丢函数体）——后续轮呈报方案。

## 灰色点披露（待用户裁，未立案）
- **B1a 契约分叉**：宿主=stdlib 包装 结果<> vs v2=内置直调裸指针（plans/014 本轮行+plans/015 无关）——随 stdlib 并入专项收口，本轮维持（用户裁决）。
- v2 整8/整16 强转白名单边界/正8 正16 显式拒绝——上轮边界，不变。
- 78/79 v1 链内存失控——已知遗留，随 v2 重建自然解决，勿翻案。

## 踩过的坑（本轮）
- **探针文件名假绿**（簇⑥定位）：含导入的 .cn 用非「主」文件名 check，私有函数体被静默过滤——「检查通过」实为没检查；最小复现必须叫 主.cn。
- **登记点时序**（B1）：扫描导入（加载期）登记的导入项会被依赖文件的解析窗口抹掉——登记必须紧贴消费点（解析程序 入口清空+重扫）。
- **裸名改写取位**（B1 首版）：被调标识符在 参数起点-1（不是池尾——池尾是最后一个实参）。
- **后缀消费双义**（簇④首版）：泛型实参位消费 `*` 必须参数化——外层类型位的 `*` 归 解析类型 指针层数登记，无条件消费会把 `整64* p` 层数归零。
- **expected 行匹配范围**（142/143 首版）：v2 闭环用例的期望行全部须在 **v2p 进程输出**（stderr 自检行）——运行产物只断言退出码；程序 stdout 锚点行不能进 expected。

## 门禁状态（本轮最终，单位机 linux-arm64）
- 构建（gcc ARM64 清场重建）零警告 + 单测 **1216/1216** + 全量 E2E **146 用例 142 过/1 败(78 已知遗留)/3 跳(平台限制) 零回归**；新增 142/143/144 单跑全绿；5 组件冒烟 0 错；探针已清理（/tmp 系统临时区不入侵工作区）。
