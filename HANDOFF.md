# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-17——**🧪 T12 收尾施工图+D38 备料初探（免锁只读备料·深度机 274-a 在飞待锁）**（基线 c331fb6 后最新）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **T12 收尾施工图（marker 二分法·完备）**：emitStackLoad **34 调用点清单已枚举**
   （emit.cpp 4/codegen.cpp 6〔含 271-a 嫌疑段 648/669〕/i128 1/oop 7/instructions 16）
   ——marker 法=签名加 `const char* caller` 默认参数+各调用点传 `__LINE__`+
   offset==0 时 stderr 打 caller 行号——**一次重编精确定位 [rbp] 裸读调用者**
   （须开发锁轮实施）。
2. **D38 备料初探（抽查结论+295 行为验证实锤）**：341 浮点作条件/343 结构体转
   整数/305 返回类型不匹配的 **v2 语义检查面零对应检查**（grep 零命中）——
   **295 行为验证实锤**：v2p 编裸分号源 rc=0+产 v2asm.s（解析语句.cn:293 防御
   分支失效——注释存在但实际放行）。**根治=17 形态逐个在对应层新增/修正检查**
   （295=解析层防御分支失效修复+语义层新增·≈300~500 行 CN 新增+17 负测转正）——
   **专项轮体量**。
3. **T21 备料定性+提案细化**（待 §2.3 裁决）：宿主词法判定=`c >= 0x80` 全放行
   （lexer.cpp:226·全角字母 Ａ 过检实锤）+规范 01a「中文/字母」边界未严格限定。
   **提案细化**：①「中文」=汉字区段 U+4E00~U+9FFF+扩展A（U+3400~4DBF）+扩展B
   （U+20000~2A6DF）；②拒绝=全角形式拉丁（U+FF01~FF5E）/中文标点（U+3000~303F）/
   假名（3040~30FF）/emoji；③对照=Rust identifier=XID_Start/XID_Continue（业界
   标准）；④双侧修复=宿主 lexer.cpp:226 判定+V2 词法分析.cn 同构；⑤存量迁移面=
   **零实证**（stdlib+v2 树+全角/中文标点 grep 零命中——修复无存量破坏风险）。

2. **T16 扩展探针（回归面补强）**：负零字面量（z=-0.0 → -0.000000 ✓ 词法/
   常量面负零保留）+乘负一（m=x×-1.0 → -0.000000 ✓）+除负零（d=1.0/z=-0.0 →
   **-inf** ✓ IEEE 除负零语义）——**四形态全对**=273-a T16 修复的回归面验证
   补强（探针 t16c.cn 留档）。

### 二、验证链

零行为面修改（只读备料）；双门禁过。

### 三、下一步（新会话按序）

1. **T12 根治收尾（待开发锁）**：marker 二分法实施（一次重编定位）→按调用点
   修复→撤诊断→t12 探针指纹消失验证→全量门禁。
2. **D38 根治**（Cx17 负测防线·17 形态逐个挂点定位+修复·专项轮）。
3. T13（待深度机探针回传）；T19~T21；D35 波B（? 特性大轮）；C20 波2（待 D28/D29）。
4. T14/T15/T16 的 x64l 运行级复验=跨机口径（已登记）。

### 四、诚实边界

- T12/T13/D38 未根治（备料完备·待锁/待专项）；T21 待规范口径裁决（§2.3）。
- T14/T15/T16 的 x64l 运行级=跨机口径（已登记）。
- github 镜像挂账已清（d69d260..888ff7c 对齐）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 验证轮五连巡（免锁不占轮次号·基线 e2dc69f）——行52 浮点（T14~T16）→行54 容器（T17/T18）→行55 泛型组合（T19/T20）→行59/60 词法（T21+T5 扩充）→本轮 **S1 采样 daemon 建成（256-b 协议落地·M3 计时启动）**。家机 266-a D34 拆波波B 持锁在飞。

### 一、本轮（S1）做了什么

1. **三件套交付**：
   - `scripts/cnsmith_daemon.py`——§2.9 五步自治循环（fetch 拉取重建→gen 轮换种子新样本+命中库回归轨→cnsmith_diff 并行差分→命中存档入库→三件套日志）；`--rounds N/--samples N/--sleep S/--rounds 0 无限`。
   - `tests/cnsmith_hits/`——命中样本库（diff/ 31 样本+.meta〔基线/类别/说明〕·入库防 target/ 清场）。
   - `tests/cnsmith_hits/采样日志.md`——三件套日志=**M3 计时秒表已起算**（「连续 4 周零新类别」自首条日志）。
2. **首轮实证**：3 轮+闭环单轮（各 100 样本·7.4s/轮·--jobs 8）——分歧率 31%，**存档 31/31 闭环成功**。
3. **甄别（抽样 4/4）**：分歧全为 **T12 类反复**（-O3 `&&` 值位左假恒真）——s1703/s1705/s1712 直接形态+s1721（布尔条件经三元传播到移位量·asm 实证两级 `and ecx,31` 掩码在位=非移位面·值差恰 2 倍=移位量 1/0 之差）。**新类别=0**。T12 复现率 31%=根治价值实证；根治后回归轨每轮自动复验。
4. s269.cn 抢救=不可行（target/ 清场已失·如实记录）——命中库入库防再失即协议目的。

### 二、验证链（本机复现口径）

- `python3 scripts/cnsmith_daemon.py --rounds 3 --samples 100 --sleep 2`（实证命令）→ 三轮日志+存档。
- `python3 scripts/cnsmith_daemon.py --rounds 1 --samples 100`（正则修正后闭环）→ 存档 31/31。
- `python3 scripts/check_matrix_coverage.py` PASS（基线 3）；check_handoff/check_progress_sync 显式退出码见收工提交。
- 源码零改动——E2E/单测不受影响（未重跑）。

### 三、下一轮任务（按序·**全表空闲=按 AGENTS §8 空闲机优先认领开发轮**）

1. **T12 根治（开发轮·待认领·优先级被采样数据抬升）**：31% 复现率+每轮回归轨就位——根治同轮行 6/12 矩阵转正 E2E+命中库回归清零验证。
2. **daemon 常态化（任一空闲机）**：`--rounds 0` 长驻或每验证轮 `--rounds 1` 搭车；日志审阅→排班折入。
3. 其余开发轮候选：T14+T15／T17+T18／T19+T20（T19 须先呈报单态化方案）／T5 扩充+T21 词法面（小面快根治）。
4. 行 57 三后端平台面（长期格·随跨机复验收口）。

### 四、诚实边界

- 本轮无编译器源码改动；daemon 为新设施非修复。
- 首轮 3 轮日志「命中归类零命中」=分歧清单解析正则首版缺陷（冒号分隔未匹配）·第 4 轮修正后闭环——日志如实保留历史未回填。
- 甄别抽样 4/31（非全量逐条）——31 条全部入命中库可随时复验；全量归类待 T12 根治轮做（回归轨自动跑）。
- daemon 拉取重建分支（①）仅单测路径覆盖（本轮无新提交·重建分支未实跑）——首次远程更新时人工盯一轮。
- 前四轮完整记录见 git d69d260/921930e/ba2c53e/e2dc69f 提交信息。

## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
