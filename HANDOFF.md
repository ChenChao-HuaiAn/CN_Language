# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🧪 预置轮·不占号·基线 216f9aa·2026-09-17）：用户批量裁决落盘 + 清零波次启动 + 304-a 计划预置（B11 波次1）

**用户两项裁决**：①探测维持现状（三机采样/探针照旧·M3 计时不断）；②待裁决批按**方案甲批量批准**约 16 项（B11/B12/T21/T36~T42/T3/T4/T6~T8/T10/T1）。

**落盘**：plans/021（§3-B 清空+B11/B12 迁出+T21/T26~T28/T36~T42 处置更新+§3-A 清零波次行 8 波+速览+戳）｜plans/025 §1.0/§1.0a（304-a 六要素）+§〇两行｜lessons +2（B 区第四形态/C21 登记；探针确定性输入）｜看板 🧪 行。**源码/测试/门禁脚本零改动**。

**B11 侦查产出**：win 侧确定性真值表（`target/t26probe/p3.cn`·标记文件控制布尔·四组合）**O0=O3 全一致**——复验 T26 win 免疫成立；IR 形态=短路汇合→ssa.cpp 使用点重写+Phi 降级（`搬运 %vN 常量`）→使用点直用 %vN（平台无关·与 T26 铁证同签名）；**分叉点收敛 x64l 后端发射**（win ✓ / x64l 258-a 补齐 Copy / x64l default 仍静默=T11 残余面②）。**甄别记录**：初版时间戳探针「O3 恒假」=输入不确定假象（非缺陷）。

### 二、下一轮（按序）

1. **深度机 303-a 收工后 → 家机认领 304-a 波次1=B11**（计划六要素已落 §1.0a·待锁；若深度机先做 x64l 复现（用 p3.cn 形态）并提供 .s/IR，可显著提速）。
2. 备选搭车：**C21**（B 区门禁增强·小改 check_handoff）。
3. 波次 2~8 见 plans/021 §3-A 波次行（逐轮认领·六要素落 plans/025 本机节）。

### 三、坑与边界

- **B 区纪律**（用户现场质询）：§3-B「待呈报/待用户裁决」无条目=**完全空白**（不写「（无）」/迁出说明/纪律复述）；迁出信息只写迁移目标行。
- **探针教训**：非常量输入的对照探针（O0 vs O3）必须用**确定性源**（文件存在性/参数/固定文件）——时间戳/随机数会让对照退化为伪对照（本轮实例）。
- x64l 运行级复现/验证归深度机（家机无 Linux 环境）；T26 家机侧=win 复验锚（p3 形态）。
- 深度机 303-a 在飞（T28 族 scripts 面+T39 x64l 复验）——开发锁唯一，本机不抢锁只预置。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 **303-a 开发轮收工**（T28 族三实例根治+T39 x64l 运行级复验搭车）。基线 260a94e→认领 216f9aa。接手先 `git fetch`；daemon 已重启常态采样（T28 根治后日志恒红线内·无需人工压缩监控）。

### 一、本轮做了什么（303-a·全表空闲依 §8 动态认领认领开发轮）

1. **上轮 🧪 采样轮收尾落盘**（随认领前提交）：新命中 6 件入库+月日志 20 轮全量段压缩为汇总段（5891→4857 行回 5000 红线内）；汇总段含 **T39 修复后采样回归轨复验结论**（daemon 17:13 自动拉取 260a94e 重建后 6 轮命中集合与修复前完全一致=T39 零回归侧证）。
2. **T28 族三实例根治**（plans/021 §三-G 立案·方案甲·验收三件全绿）：
   - ①注入器（cnsmith_gen.py）：`# 注入类型` 标记行废除（预处理指令污染判据）+候选行筛选必真注入（含特征行随机选+kind 兜底 shuffle+全败跳过计数）+`.cn.neg` 伴随文件落注入位置——**400/400 全拒绝·0 崩溃·诊断行号零偏离（≤2 行容差）**。
   - ②归类判据（cnsmith_diff.py+cnsmith_daemon.py）：清单行带 `[build_err/run_err/diff]` 结构化标签+daemon 删「运行失败」文本反推——rc=-8/timeout 详情不再误归 diff/；**存量甄别迁移**：diff/ 内 4 件误归副本删除（m27_01/m37_01/s2609179004/9005·正版在 run_err//build_err/ 且 meta 更完整）+m27_04/05 从 run_err/ 归位 diff/（实测行为=输出分歧 rc=0 非 run_err·与上轮人工归类口径统一）。
   - ③汇总记法（记轮()）：消失回归改**增量语义**（上轮未命中回归集合比对差集·重启首轮基线化）——根治「93 已修回归样本每轮不再命中→消失回归恒非空→每轮退化全量记录」的日志爆炸根因；**验收=同基线连跑 3 轮仅汇总（日志增量 22 行 vs 根治前 144 行）**。
3. **T39 x64l 运行级复验=捕获同构回归并根治**（302-a 跨机复验请求闭环）：409 真跑 5 断言不一致——asm/位样双通道归因=**emitInt128Shift 右移路径 r10=新低半/r11=新高半与左移寄存器角色相反、存储行共用→右移双半写反**（6 断言实际值全部精确吻合「新低半进高槽」模型：大>>1→INT128_MIN/大>>32→2^96/大>>64→2^64/大>>65→0 双零对称/变量>>1→INT128_MIN/复>>=2→2^67）；根治=linux_x64_codegen_i128.cpp 三处存储按 isShl 分派（常量/变量小路/变量大路）；**arm64 同函数审查=免疫**（逐半直接 store 对应槽）·win=302-a 运行级已绿——单后端缺陷零扩散；修复前红/修复后绿=天然反证对。

### 二、验证链（本机复现口径·当轮实测）

```
python3 /tmp/cn_t28/verify.py                 # T28①验收：400/400 全拒绝+0 崩溃+0 诊断偏离（临时设施不入库）
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # 全量 427=425/0/2（跳=62_ffi/69 平台败面）·409 绿
./target/cn_unit_tests                        # 1353/1353
python3 scripts/check_spec_coverage.py --strict; python3 scripts/check_matrix_coverage.py  # 双双退出码 0（面覆盖 80/81）
python3 scripts/cnsmith_daemon.py --rounds 3 --samples 30 --sleep 3   # T28③验收+归类验证（后已重启常态轨）
python3 scripts/check_handoff.py; python3 scripts/check_progress_sync.py  # 提交前双门禁
```

### 三、下一轮任务（按序）

1. **daemon 常态采样续跑**（T28 根治后汇总记法生效·月日志恒红线内）——M3 不停；命中对账转立案。
2. 用户裁决 B11（T25+T26 合并根治）/B12（T27 并入 C18）后：对应根治轮（T27 含 i128 helper u128Abs INT128_MIN 特判+整64 字面÷通道）。
3. B8 面台账巡视（已立案行复验/行 57 平台面余列）。
4. T28 负向轨判据强化候选小轮：逐 kind 诊断文本精确匹配（现口径=行号 ≤2 行容差）。

### 四、诚实边界

- 记轮() 增量状态在 daemon 进程内存：重启首轮全量记录一轮（设计容忍·重建基线）。
- 负向轨「诊断指向注入位置」核验口径=诊断行号与注入行偏差 ≤2 行（更严的逐 kind 文本匹配=候选强化轮）。
- O3 侧编译失败归 run_err 为 cnsmith_diff 既有口径（O0 已证 ok 的家族语义）·本轮未动。
- T39 arm64 运行级复验仍归单位机（本轮 arm64 仅静态审查免疫结论）。
- m27_04/05 的 B12 实施清单 i128 helper 修正项（u128Abs INT128_MIN 特判）未实施——随 C18 根治轮。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🏃 297-a 全修轮收工（用户裁「按照你的方式，全部修复」）——**T25/T24/B10 三案当日根治**+406/407/408 转正+全量门禁绿。接手前先 `git fetch`。

### 一、本轮做了什么

1. **T25 根治（浮64 汇合赋值 -O3 归零）**：病灶=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支传 w9 → `str w9` 32 位截断写（高 32 位丢）。修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0）；asm 实证 `str d0` 64 位完整写；27 形态回归矩阵全绿（三元 9+汇合 9+最小 2+浮32 3+数组 2）。
2. **T24 根治（后缀自增值语义）**：`visitUnaryExpr` Inc/Dec 三路径+`handleClassFieldIncDec(+postfix)` 按后缀返回**旧值**（原忽略 postfix 一律新值）；16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照）。
3. **B10 根治（方案甲·同层 `:` 前瞻消歧）**：parser `ternaryColonAhead()` 替换 C-1 白名单判据（括号/嵌套 ? 深度感知；新判据⊇旧判据=存量成功程序零语义变化）——`a ? -b : c` 不再要求括号；后缀传播 `r?+1`=42 不回归。
4. **T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无同型截断）静态注记。
5. **规范条文**：plans/001 §4.5 优先级表（`++`/`--` 后缀 13/前缀 12 级+值语义）+三元消歧+错误传播节；spec 01b 五处同步。
6. **新 E2E 三件**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记。
7. **看板通告段广播**：v2 消歧同口径（家机 296-a v2 ? 链对齐）+T24 v2 对齐登记。

### 二、验证链（全绿）

```
cmake --build target/build -j 8          # 零警告（grep error|warning=0）
./target/cn_unit_tests                    # 1353/1353
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --jobs 8   # 426=424/0/2（跳=62_ffi/69 平台败面）
python3 tests/e2e/run_e2e.py --filter 60_error ...  # 传播回归 PASS
> 78_v2_自举链构建 PASS ＋ 79_v2_自举闭环 PASS（锚定链承载）
> 406/407/408 单跑 PASS；双门禁独立验退出码=0
```

### 三、发现的问题

- T26=x86_64 单侧（arm64 免疫实证）——精确形态归深度机 B11。
- T24/T25 的 **v2 树同型对齐**待家机 296-a 收工后独立轮（自举编译器也有这两缺陷·通告段已广播）。
- i1 Copy 走整数路径 32 位写=安全（布尔矩阵全绿实证·注记）。

### 四、下一轮候选

1. **M3 采样轮⑥**（用户令不停）：行 8/10/11 算术位运算比较面。
2. **T24/T25 v2 对齐轮**（家机 296-a 收工后）。
3. A12 标签锚点搭车。

### 五、诚实边界与坑

- win/x64l Copy 浮点路径运行级复验归各机（静态审查无同型截断·运行级未验）。
- 406~408 未配单测（E2E 为强门禁·单测面留后续）。
- 修复期间 E2E 全量 426=424/0/2 的 2 跳=62_ffi/69（既有平台败面·非本轮引入）。