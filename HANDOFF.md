# HANDOFF 交接文档

**交接时间**: 2026-09-12 第七十五轮（**深度机 linux-x86_64**）——**74-a 诚实边界清理三件套**（v2 真 move / 宿主污染单一入口 / 映射字符串元素释放）。验证链全绿，固定点新口径 **271876**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：回答用户对 74-a「诚实边界」段的优化提问，呈报三方案（甲 污染镜像+真 move／乙 函数上下文穿透／丙 维持现状+文档）后**用户裁决方案甲**，并全选三项相邻完善（补宿主借用来源防线、映射/嵌套元素释放、宿主污染单一入口）。

2. **v2 侧真 move**（74-a 登记的诚实边界销项）：
   - **结构性约束**：`归一化入容器实参` 在 `生成表达式求值`（深层）拿不到 `函数信息`；穿透要改表达式生成族 12 函数 + 22 调用点签名（风险大于收益）；静态结构体指针方案被阶段 2 逃逸检查拒绝（实测）。
   - **解法**：污染名单**镜像**（v2 惯例 `已装箱名们` 同款三件套）——静态 `当前串污染们` + **唯一写入入口** `登记串污染`（5 处污染登记点收口）+ `是当前串污染` 查询 + `清空函数级槽表` 函数级重置（与权威同生同灭）。
   - **真 move 三重判定**（全命中才真 move，任一不满足回落复制）：实参形态=`转移(标识符)` ∧ 源在**局部槽** ∧ 源**不在参数槽** ∧ 源不在污染镜像。
   - **实测**：预分配后 `转移(拥有局部)` 入容器分配增量 **0**；`转移(借用形参)` 走复制（调用方串经搅动完好，安全方向）。

3. **宿主污染名单收单一入口**：`markStringTainted`（ir.hpp），5 处登记点统一经此——纯可维护性重构，行为零变化。

4. **映射<K,V> 字符串键/值释放**（矩阵 A17 同族泄漏）：
   - 运行时 `__cn_map_free_strings(obj, 键偏移, 值偏移, 元素数量偏移, freeKeys, freeValues)`——**平铺 `[0,元素数量)`**（映射删除用「与末元素交换」→ 有效元素恒为前缀，无需链游）；释放后槽清零幂等；
   - 宿主 `injectContainerElemDestroy` 增映射分支，注入到 `~映射`/`清空` **方法体前**（清空先释放后 `元素数量=0`）——注入在方法体 → **嵌套 `向量<映射<…,字符串>>` 经元素析构链自动覆盖**；
   - 实测三形态（析构/清空/嵌套）校验 7/7。

5. **防线口径更新**：E2E **218** 五→**七形态**（31→**127**：+映射值串释放 32、+嵌套元素析构链 64）；E2E **219** 六→**七形态**（63→**127**：+转移真 move + 借用形参两子检查合并折 64）——**219 的新形态是 74-a 诚实边界的正式门禁**（v2 真 move 若回退为复制 → 增量≠0 → 对照失败）。两侧供给.cn 扩 映射/嵌套 符号。

6. **验证链全绿**：清场重建零警告（4 处匹配=文件名含 error 既有口径）+ 单测 1289/1289 + E2E **241 用例 236 过**（唯一败=194 既有待批）/4 跳 + 固定点 **271876 行**（新口径）逐字节一致 + 绑定自检 + 运行级 3/3 + 对拍 29/29 + 释放路径两侧对照。

7. **文档五件套**：plans/014（状态行）+ plans/019（跟踪表行）+ plans/020（A14 v2 列销项 + B30/B31 新格 + 防线表口径 + 第六节含方法论三条）+ lessons（第七十五轮段，权重 7，预防㉒~㉕）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子**：①**映射 `删除(键)` 释放被删项字符串**（本轮登记的诚实边界——交换式删除覆盖被删槽，需 stdlib 改动 + 新注入辅助方法，与 `向量.析构元素` 同模式）；②数组元素（`字符串 数组[N]`）借用面（NLL 轮）；③装箱字段接收（`r.值` 读出）无 RAII；④结构体字段持串无 RAII；⑤结构体容器字段「声明即用」既有边界（字段未自动构造）。
2. **呈报清单余项**：194 单行批准（已呈报多轮无答复）；win 平台 v2 并行隔离（家机轮）；71-a 后续可选题（构造器实参类型匹配/重定义诊断）；阶段 4 第二层第三批收口（不安全区变硬错误，建议真实使用反馈期后）。
3. **矩阵维护纪律**：每轮触碰所有权语义的改动必须同步更新 plans/020 对应格（本轮已按此执行）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处 warning/error 匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 241 用例 236 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（v2 树或宿主编译器改动后须删 v2p 缓存键重建）
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p（验证假绿）。

## 四、本轮踩坑（已入 lessons 第七十五轮段 ㉒~㉕）

- **v2 深层函数需要函数级上下文时，优先「静态镜像 + 唯一写入入口 + 函数级重置」而非穿透参数**：成本差一个数量级；但**前提是权威列表单调写入且无截断语义**（污染名单满足；`字符串变量们` 有块级截断→不适合镜像）。选镜像必须写清「漏项方向是否保守」：漏项=误判拥有=UAF（不保守）→ 必须强制唯一入口 + 门禁用例反证。
- **「容器持有元素」与「元素是堆资源」是两个独立释放义务**：只释放容器内部数组 ≠ 释放元素本身（映射、嵌套容器均如此）。
- **新增容器类型先确认删除策略再选释放遍历方式**：平铺数组（向量/栈）／链式槽（链表/队列，出队已转移所有权给调用方）／**交换式删除的前缀数组（映射，平铺安全）**。
- **释放注入放「容器方法体」而非「调用点」**：方法体注入覆盖面天然含全部持有者（嵌套元素析构链、字段容器），调用点插入需枚举持有者（易漏）。
- 用例断言辅助函数注意 CN 的 `结果` 检查规则（须规范形态 `如果 (r.正常) { ... } 否则 { ... }`，真分支内访问 `.值` 才合法）；v2 侧不支持 `IO::打印`（用例用退出码承载断言）。

## 五、诚实边界

- **映射 `删除(键)` 路径**：交换式删除把被删项的键/值槽覆盖丢弃（字符串键/值泄漏）——登记为下一轮靶子（需 stdlib 改动 + 新注入辅助方法，与 `向量.析构元素` 同模式）。
- **v2 侧真 move 判定依赖污染镜像**：镜像漏项方向不保守（误判拥有=UAF），故以「唯一写入入口 + 代码注释强制契约 + E2E 219⑦ 反证」三重防护；新增污染登记点必须经 `登记串污染`。
- 数组元素/装箱字段/结构体字段持串/静态面无 RAII（plans/020 靶子维持）；结构体容器字段「声明即用」=既有边界。
- 构造器实参类型匹配不检查（v2 既有）；跨编译器结果 ABI 互操作未单独 E2E 锚定；v2 契约登记按纯名/键基回退查（重载族返回异型=近似）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **271876 行**、cn_self71）。
- 探针（/tmp/p75probe* 临时，不入库）：v2 真 move 零拷贝验证、借用形参安全方向验证、映射三形态（析构/清空/嵌套）验证、映射分配分步诊断。
- E2E 用例：
  - `tests/e2e/218_容器元素所有权归一化/`（**七形态校验和 127**，宿主+v2 双通道；供给.cn 供 链表/栈/队列<字符串> + 映射<整64,字符串> + 向量<映射<…,字符串>> 符号）；
  - `tests/e2e/219_双编译对照容器元素所有权/`（**七形态，两侧退出码须一致=127**，含转移真 move 与借用形参门禁）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——A14 v2 列销项、B30/B31 新增；每轮触碰所有权语义必须同步更新）。
