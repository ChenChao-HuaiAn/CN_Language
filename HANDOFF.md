# HANDOFF 交接文档

**交接时间**: 2026-09-12 第五十五轮（**家机 win-x64**，纯验证轮零源码改动）——跨机轮 win 侧动态收口：拉取 16ad945（深度机第八十五轮，plans/019 编译期安全专项三十轮）后执行本机全量验证——**零警告 + 单测 1306/1306 + E2E 269 用例 252 过/17 败（逐项定性全部非缺陷）** + 85-a 返回位深拷 win 探针六形态全过（校验和 63）+ **v2self 固定点复锚 340703 行** + 组件对拍 **34/34** + 运行级 **3/3**。**待批 2 项已在轮内按纪律落实**（测试口径修正+复验 PASS，见二）。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **拉取范围**：gitcode/develop 新增 62 提交（21d8201→16ad945）——深度机推进到
   plans/019 第八十五轮「编译期安全专项」（阶段 1~4'、拥有权矩阵、内存安全防线），
   期间新增大量所有权/释放/检查器特性与 13 个双编译对照用例、5 个新单测文件。

2. **全量门禁（win-x64）**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**
   + E2E **269 用例 252 过/17 败/0 跳**。17 败逐项定性：
   - **78/79 OOM 家族 2**（4137MB/4113MB > 4096MB 自动终止；与第四十八轮基线
     4135MB 同族零回归=win 固有败面）；
   - **双编译对照 13**（217/219/221/226/228/230/234/236/237/238/240/241/242）——
     win-x64 平台限制（runner 设计上仅 linux-arm64/linux-x86_64 可跑，win 视为失败）
     → **长期改善项：v2 win 产物隔离后 win 也可跑对照**（见下一轮任务 4）；
   - **69_memory_management 期望过时**（待批 ①）；
   - **194_v2 win 退出码口径**（待批 ②）。

3. **85-a 特性 win 实物复验**（本轮核心）：`target/p55win/probe85`（六形态）——
   ①形参返回·字面量临时跨块 ②形参返回·标识符局部跨块 ③形参嵌套成员链
   ④拥有局部 move 对照 ⑤指针解引用（rc=139 修复回归锚）⑥字面量+调用返回链——
   折校验和 **63 全对**（每形态搅动+内容比对）；`probe85f` 借出视图豁免正常；
   `probe85e` 静态结构体复现（ml64 `undefined symbol : rbp0` 汇编失败=独立缺陷
   形态复现，登记维持）。

4. **v2self win 锚定链复锚**：宿主全树编译 rc=0 → v2p 重建 ml64+link rc=0 →
   fix_p.asm **340703 行**（246651→340703，+94052=三十轮 v2 侧安全特性源码
   确定性体现，**分后端口径不跨后端比较**）→ cn_self46 链接（cn_self46.obj 在前
   +v2p_46.obj 借链+/FORCE:MULTIPLE+/MAP）→ **绑定自检 cn_main ∈ cn_self46.obj ✓**
   → fix_s.asm（340703 行）→ **固定点 fix_p ≡ fix_s 逐字节一致**。

5. **组件对拍 34/34**（含新增 5 组件面——borrowview/const_ref/escape/move/
   unsafe_boundary 单测新增文件对应的编译对照全归零）+ **运行级 3/3**
   （hello 你好，世界/119 rc=14/173 rc=173）。

6. **文档**：plans/014 状态表第五十五轮行 + plans/019 跟踪表跨机行 + 更新日志 +
   本 HANDOFF。

## 二、待批 2 项的落实（AskUserQuestion 呈报无答复 → 按 AGENTS.md 第3项纪律自行裁决留痕；断言语义均不弱化）

> **已在轮内落实**：① 69 expected `100→201`/总分配 `4→5` + 用例源码注释同步 A2 口径；② 194_v2 v2闭环.txt `退出码=255→511`（文件头留痕注）。两项单跑复验 PASS；**全量 E2E 复跑 269 用例 254 过/15 败**（13 平台+78/79 OOM，零回归）。

### ① 69_memory_management 期望过时（已修正：expected `100→201`、`4→5`）

**证据链**（探针 69-a，target/p55win/probe69.cn，宿主与 v2 两侧同值）：
`字符串 s = "你好世界CN编译器"` 声明后活动分配 +1（A2 拥有化后字面量初始化
产生 `__cn_str_copy` 堆副本）；`字符串子串` 再 +1（子串自身分配）；显式
`字符串释放(子)` −1；块退出自动释放 s −1 → **归零（无泄漏、内容正确）**。
69 用例断言「使用中=1」为 A2 所有权改造前口径（当时 `s` 借驻地字面量不分配）。
宿主汇编实证：`call __cn_str_copy`（s 初始化）+ `call __cn_str_sub`（子串）各一次。
**已落实**：`主.expected` 第 7 行 `…201`、第 15 行 `…5`；用例源码注释同步
（「使用中=2（s 拷贝+子串）、释放后=1、块退出归零」）。复验 PASS。

### ② 194_v2 win 退出码口径（建议：v2闭环.txt `退出码=255→511`）

**证据链**：194_v2 九形态校验和逻辑值=511（累计 31+累计2 15×32）；POSIX 退出码
8 位截断 → Linux 观测 255（511%256）；win 32 位全值观测 **511**。runner 的
`执行v2闭环`（win 分支）步骤 6 直接比较全值 → 511≠255 失败。**实测 511=全位通过
（若任一形态失败会小于 511）——功能本体正常，纯口径**。
**已落实**：`194_v2_窄宽显式转换锚定/v2闭环.txt` 的 `退出码=511`（文件头留痕注）
——runner Linux 分支 `% 256` 后仍 255=恒成立；win 分支 511=511 ✓（两平台统一）。复验 PASS。

## 三、下一轮任务（按序）

1. **② 静态结构体支持（第一优先候选，已立案）**：`.data` 槽按类型尺寸分配
   （三后端 x64/linux64/arm64 各一处）+ 静态结构体字面量赋值路由
   `emitStructCopyWithFields`。三方案见 plans/020 第二十六节（甲 推荐／乙 main
   注入构造／丙 语义拒绝）；探针 p41b 与 iso/n*.cn 系列在深度机 /tmp/p82probe；
   **win 侧复现证据**：本轮 probe85e（`undefined symbol : rbp0` 汇编失败）。
2. **B35 字段容器接收者（待用户裁决）**：plans/020 第十七节三方案（甲 move／
   乙 深拷〈推荐〉／丙 维持+诊断）。
3. **win 平台 v2 产物隔离**（本轮 13 对照失败的根因项）：隔离后 win 也能跑双编译
   对照门禁（家机轮候选）。
4. **甲（参数 move 语义）/丙（返回位编译期拒绝）**：85-a 登记的语言级选项，待
   用户裁决或待 `结构体复制` 内置出现后重估。
5. **跨机轮**：arm64（单位机麒麟）复验 E2E 234~242（linux-arm64 平台）。
6. 呈报清单余项：阶段 4 第二层第三批收口（不安全区变硬错误）；v2 诊断无行号；
   未初始化槽 preFree 隐患（观察项维持）。

## 四、验证链（本机复现口径）

```bash
# 家机 win-x64（本行口径）
rm -rf target/build && powershell -ExecutionPolicy Bypass -File scripts/ci.ps1   # 全量门禁（约 65 分钟）
python target/p46win/self_chain.py          # v2p 重建 + 锚定链复锚（固定点 340703 行）
python target/p46win/comp_diff.py           # 组件对拍 34/34
python target/p46win/run_level.py           # 运行级 3/3
./target/Debug/cn.exe build target/p55win/probe85.cn --target win-x64 --output target/p55win/probe85.exe && ./target/p55win/probe85.exe   # 85-a 六形态校验和 63
```

注：运行时 obj（target/{10 名}.obj）与 src/runtime 联动（第五十六~八十五轮
io_api.cpp 有改动）——全量门禁会重编；手工操作前先跑门禁或确认 obj 新鲜。
**锚定链与门禁不得并发**（共享 target/ 与 v2 产物路径）。

## 五、本轮踩坑

- **探针断言值须按字节算**：中文 3 字节/字——写探针时「字符串长度」断言按
  字数算会全败（probe85 首版校验和 0/11，实际是断言算错不是缺陷；逐块打印
  内容后真相立现）。**教训：断言失败先分辨「数据错/断言错」**。
- **Edit 长行有拼接风险**：plans/019 表格行替换时 new_string 未带行尾状态列，
  导致新行与后续行粘连——**改每行后立即核对「行长度/首尾/列数」**（本轮已当场
  修复并复查）。
- （承前轮）中文 grep 不可靠用 python；MSYS 路径转换（ml64/link 直调须
  `MSYS_NO_PATHCONV=1` 或 PowerShell 转发）；78/79 OOM 勿误诊。

## 六、诚实边界

- 本轮**零源码改动**（纯验证轮）；win 侧三十轮安全特性全绿=实物动态实证。
- 69/194_v2 为**测试文件口径修正**（本轮已按 AGENTS.md 第3项纪律自行裁决落实并复验；
  断言语义不弱化）；13 对照失败为 win 平台结构性限制（非缺陷）；78/79 OOM 为 win 固有败面。
- 85-a 静态结构体（②）win 复现与深度机一致（登记维持=下一轮第一优先候选）。
- 探针与脚本留 target/p55win、p46win/ 不入库；固定点 340703 为 win 口径
  （X64L=327355、arm64=347685+ 各自口径，**不跨后端比较**）。
