# HANDOFF 交接文档

**交接时间**: 2026-09-04 第八轮（家机 win-x64）——**灰色点①③④⑤立案根治**（用户重贴
缺陷零容忍纪律裁决：立即全部根治不留到后面）：上轮 HANDOFF「灰色点披露」六点分诊，
四项 v2 侧缺陷全链根治+E2E 锚定（138/139 双绿）；②呈三方案待用户裁决；⑥=未实现特性非缺陷。

## 本轮已完成（全部有实证）

### 一、四项缺陷根治（每项=根因+三层修复+探针运行时锚定+E2E）
- **① 真/假 布尔字面量**：新节点 `节点_布尔字面量=34`（语法/语义/IR 三层）——原第五波
  复用整数字面量（真=1/假=0），`打印(真)` 输出 `1` 与宿主 `真` 分叉。独立节点后 IR 登记
  T_布尔，打印/拼接分派自动 `__cn_print_bool`/`__cn_str_from_bool`（规范 02 布尔=基础
  类型 1 字节——对齐宿主非规范变更）；`无` 保持整数字面量 0（空指针语义）。
  **实证**：v2p 产物打印(真)→`真`、`"前缀"+假`→`前缀假`（探针+138）。
- **③ 解引用复合赋值 `*p op= r`**：P6c 分支原只认 `=`——`*p += 1` 的 `+= 1` 成孤儿 token
  触发块中断、块内后续语句（含 返回）全部丢弃而 asm 仍生成=残缺产物。补复合分支
  （方案A 回退重析左值副本，与标识符/成员/下标形态同款合成标记）。
  **实证**：`*p += 1 /= 10 / -= 3`→6/10/7 与宿主一致（探针+138）。
- **④ 窄宽强转 整8/整16 三层**：语义白名单补两名（原报「未声明函数: 整8」）+ IR_类型转换
  目标码特判（类型码() 将整8/16 归 T_整64，强转须窄宽码——仅强转分支特判其余消费方
  零变化）+ win 发射 `mov al/ax, byte/word ptr`+`movsx`、ARM64 `sxtb/sxth`（双后端对称）。
  **正8/正16 暂不补**：类型码层 整8||正8 同码 T_整8 无符号语义缺位，补名=静默错误代码，
  维持显式拒绝（待类型码体系支持 u8/u16 时补——诚实边界）。
  **实证**：`整8(300)`→44（asm 见窄宽发射）、`整16(70000)`→4464、`整8(-1)`→-1 保号（探针+138）。
- **⑤ v2p 语义错误即中止**（主.cn 驱动器）：err>0 打印诊断后返回 1、不产 IR/asm（原
  无条件继续=错误产物可被链接；对齐宿主 driver 语义错误构建失败）。尾部组件自检仅在
  入口干净时运行——既有用例 err=0 输出流不变。
  **实证**：错误源码→`[sem5] 语义错误 1 个，中止…`+退出码 1+无 asm（探针+139 负路径闭环）。

### 二、测试基建
- **E2E 138_v2_灰色点收口**（①③④ 正路径，退出码 0 十断言；宿主 sanity 同源跑通：
  真/前缀真/退出码 0——两侧行为一致为验收边界）。
- **E2E 139_v2_语义错误中止**（⑤ 负路径闭环）：run_e2e.py 扩展**预期退出码 None 形态**
  （v2p 退出码非 0+无 asm 产出+中止诊断行 expected 固化；防「报错仍产 asm」回归）。
  linux-arm64 侧负路径**待 ARM64 复验轮对齐**（runner 已加诚实边界提示）。

## 待用户裁决（②，规范语义——未经批不动）
**字符串二元 `==/!=` 现状两侧行为一致：按地址（指针）比较放行**（宿主 semantic_expr.cpp:408
「指针间（含字符串）比较按地址」；v2 同款）。规范 02 未定义字符串比较运算符变体（宿主
semantic.cpp:1151 注释：等价能力由 `字符串比较`/`字符串字典序` 提供）。三方案：
- **方案A（推荐，Rust 纪律）**：显式拒绝 `字符串 ==/!= 字符串`（硬错误+迁移提示改用
  `字符串比较`）——半实现纪律「未覆盖形态显式拒绝」同款（131 非左值先例）；代价：
  宿主+v2 各加拒绝分支+E2E 负测（~半天），现有源码零影响（v2 组件全用 字符串比较）。
- **方案B**：维持按地址比较（C 语义零改动），文档化行为——代价：静默陷阱持续
  （拼接生成的同内容字符串 == 为假），违背「安全接近 Rust」准绳。
- **方案C**：升级为内容比较（== 映射 `__cn_str_eq`）——最直觉但规范未定义运算符重载
  变体、须先改 plans/001（§2.3 审批）+ 意外 O(n) 开销 + 与内置函数冗余；不推荐。

## 未动项说明（⑥）
宿主三元 `?` 与后缀错误传播 `?` 按位置消歧（ternaryQuestionNext：`?` 后跟表达式起点=三元，
语句边界/二元运算符=错误传播；一元前缀不算三元起点——`x? + 1` 读 `(x?)+1`）。v2 **无
错误传播 `?` 语法**（`结果?` 未实现，`?` 一律三元）——**未实现特性非缺陷**（规范 [07]
错误处理整体在 v2 待实现清单），随该特性推进时移植同款消歧判定，不提前实现。

## 下一步（接手会话按序执行）
1. **消化语法分析.cn 冒烟 54 错（三簇根因，逐簇立案）**（上轮遗留，本轮未动）：
   ① 跨模块顶层常量可见性（~25 错）；② 文件 API 标识符（9 错）；③ 可写左值白名单
   （9 错+散件）。注意：⑤门禁生效后 v2p 对含语义错误的源码不再产 IR/asm——冒烟
   推进语义消化与 IR 观察天然分段（54 错清零前看不到 IR 层，符合预期）。
2. **② 字符串== 裁决落地**（用户批后按方案实施）。
3. **ARM64 侧全量复验（单位机）**：本轮变更（①③④⑤+上轮三元/./::）须 linux-arm64
   全量 E2E 复验；**139 负路径 linux 侧对齐**（执行v2闭环Linux 加同款 None 分支——
   本轮仅 win 实证）。

## 踩过的坑（本轮新增教训——lessons 已有条目，执行问题非记录问题）
- **换行≡空格粘连三连踩**（宿主探针首版 `&v ⋈ *p += 1` 粘成 `(&v*p)+=1`、v2 探针 pd
  同款、138 用例首版声明行粘连）：**lessons 2026-09-03 19:50 条目「写探针/测试源默认
  全语句分号」必须执行**——本轮三踩全是没加分号所致，定位耗时远超修复本身。
- 调试技巧复用：疑似 v2 缺陷先加临时打印（本轮 `[dbg] 左值末类型/子起点/附加` 一发定位
  子起点==附加=空左值→粘连实锤），定位后立即删打印。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（ci.ps1）零警告零错误；单测 **1207/1207**；全量 E2E：**141 用例 139 过**——
  新增 138/139 双绿 + 既有零回归（78/79 已知遗留内存超限 4136/4101MB，与上轮
  4099/4135MB 形态一致——旧架构固有，随 v2 重建完成自然解决）。
  （若后台门禁结果与此不符，以上一轮门禁结论为基线排查本轮改动。）
