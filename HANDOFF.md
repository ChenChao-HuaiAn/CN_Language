# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十一轮（**深度机 linux-x86_64**）——**81-a 容器元素 =
含拥有型串字段结构体**（plans/020 矩阵靶子「容器元素=含串字段结构体」＝79-a 边界销项；
同轮根治 4 枚既有缺陷）。验证链全绿，固定点新口径 **308719**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 上一轮「下一轮任务 #1：容器元素 = 含串字段结构体专项」。
   用户令：遵守 AGENTS.md 第3项纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **探针实证（P1~P17，/tmp/p81probe 不入库）**——块出口 `内存::活动分配数()` 差值 +
   分步打点 + 变体矩阵，量化一族缺口：
   - 容器元素（结构体含串字段）字段串**每元素泄漏 1**（向量/链表/栈/队列/集合 ×
     析构/清空/删除/设置；宿主/v2 同缺）；
   - **移位走结构体整体赋值深拷**（79-a 后 `数据[移动] = 数据[移动+1]` = preFree +
     memcpy + `__cn_str_copy`）→ 多余分配 + 超范围槽句柄丢弃 = 泄漏 1/元素（P8）；
   - **方法调用 + 结构体字面量实参 = 空指针段错误 rc=139**（asm `mov rdx, 0`；普通
     调用路径 D3 已修、方法调用路径漏——P3 形态六）；
   - **调用返回实参的返回临时无释放面**（`表.追加(造盒())` 泄漏 1/次——P1⑥）；
   - **链表（有析构类元素）全量析构平铺**（越界槽二次析构 + 有效元素漏释——P7）；
   - **块终止截断字段名单 → 函数级兜底失效**（`存入(表,值)` 内 `盒子 局` + `返回 X;`
     → 泄漏 1，函数体零 `__cn_str_free`——P12/P13/P14 变体矩阵）；
   - **v2 帧尾共享 retbuf × 嵌套结构体返回实参**：单实参=泄漏、双实参=**数据损坏**
     （`合并(造盒(1),造盒(2))` 宿主 1 / v2 0——P15/P17）。

3. **方案裁决（三案，甲实施）**：甲＝**编译期展开 drop glue 全链**（元素释放面 +
   元素移动挂点 + 返回实参临时清理 + 字面量实参物化 + 名单不截断）；乙（运行时辅助 +
   元素释放函数指针）因 v2 无 `FuncAddr`/`CallIndirect` 落选；丙（字段偏移变参打包）
   因嵌套/联合体条件字段表达困难落选。

4. **实施（宿主 5 文件 + v2 2 文件）**：
   - 宿主 `ir_oop.cpp`：`emitContainerElemWalk`（元素遍历循环单点：平铺 `0..元素数量` /
     链式 `头索引` 沿 `下一索引` 链游 + 步数上限防环）/`emitSingleElemRelease`/
     `isOwnedStrFieldElemContainer`；`injectContainerElemDestroy` 新增含串结构体元素分支
     + **有析构类元素全量链游化**；`移动元素` 挂点注入（`CopyStruct` + 源槽字段清零）。
   - 宿主 `ir_call.cpp`：`materializeStructInitArg`（D3 提取共享助手 + 方法调用路径复用
     + 物化临时登记为含串字段聚合局部）+ `genArgValueWithCleanup`/`flushPendingArgCleanups`
     （返回实参临时清理：RAII 守卫单点 + 嵌套基准隔离 + 返回类型两路回退查）。
   - 宿主 `ir_oop_call.cpp`：`genStringFrees` 提前返回修复（原 `ownedSlots.empty() return`
     连带跳过 79-a 字段兜底段）+ `fieldTainted_` 死代码整链删除（79-a 双死代码）。
   - 宿主 `ir_stmt.cpp`：块终止**字段名单不截断**（73-a「不截断编译期名单」同款纪律）。
   - **stdlib**：新增挂点方法 `移动元素(目标,源)`（向量 + 集合；空体=编译器注入）；
     向量 插入/删除、集合 数组前移 改用挂点（集合 `数组前移` 由自由泛型函数并入类方法）。
   - v2：删除 `标记入容器结构体污染` 整链（合成表达式求值 + IR容器）；元素释放经供给
     obj 的 `~容器` 注入单点复用（v2 侧不生成结构体元素释放面）。

5. **防线（探针转正）**：E2E **234_双编译对照容器元素结构体字段**（移植纪律 9 **第七个**
   对照用例，八形态折 **255** 两侧一致）+ **235_容器元素实参形态**（宿主单侧八形态 **255**）；
   **反证实证**：宿主注入「元素判定恒假」→ 234 宿主 255→**128**；v2 注入「实参清理失效」
   → 234 v2 255→**223**（两侧注入已恢复并复验，残留=0）。

6. **文档五件套**：plans/019（跟踪表第八十一轮行）+ plans/020（第十三节 81-a 全记录 +
   矩阵 B36/B37 新增 + 79-a 缺陷补记 + **v2 retbuf 缺陷三方案呈报**）+ lessons（第八十一轮
   段，权重 8，预防 ㊹~㊿）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **v2 帧尾共享 retbuf 缺陷专项（第一优先，数据损坏级）**：`合并(造盒(1),造盒(2))`
   宿主 1 / v2 0（实参数据被外层调用覆盖）。三方案（plans/020 第十三节）：
   - **甲（推荐）**：实参位置结构体返回**物化到独立槽**（求值后立即 memcpy + 替换实参
     寄存器；释放由「实参临时清理」接管）——**前置**：先定 v2 槽分配纪律（本轮首版
     在生成期递增 `槽计数` → 与预扫预分配槽区冲突 → **E2E 148/158 回归**，已回退）；
   - 乙：retbuf 全量独立（帧尺寸=Σ 返回尺寸）；丙：所有返回调用后物化（改动最大）。
   **立案探针**：/tmp 重写 P15/P17（本轮探针不入库；形态：`表.追加(造盒())` 残留 1、
   `合并(造盒(1),造盒(2))` = 0）。
2. **B35 字段容器接收者**（残留 2=字段容器对象+元素串）：容器字段的写入/拷贝/消亡三件套
   涉**字段赋值语义（move vs 深拷）语言级裁决**（79-a 丙案同族）——须先向用户呈报裁决。
3. **stdlib 链表 `追加` 槽复用缺陷**：删除头部后用 `元素数量` 作新槽号与在链槽冲突
   （`链表<整64>`：追加 11/22/33 + 删头 + 追加 44 → 大小=3、头部=22、44 丢失、尾 33 被覆盖）。
   修法：空闲链 / 已用槽数（布局变更，需同步 v2 容器对象大小表与 `容器元素释放参数们` 硬编码偏移）。
4. **v2 结构体字面量作实参**（v2 语法层不支持 `表.追加(盒子{...})`）——v2 语法缺口。
5. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区
   变硬错误）；v2 诊断无行号（架构级）；未初始化槽 preFree 隐患（观察项，实测未触发）。
6. **跨机轮**：arm64（单位机麒麟）复验与 E2E 234/235（linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 262 用例 258 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 308719 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（含 81-a 新段）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`，
> 并发会踩出假警。**全量 E2E 与手工 v2p/固定点操作不得并发**（E2E 运行中重建 v2p →
> `PermissionError`）。E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换
> `target/audit2/v2p_linuxx64` 后须同时删缓存键**（`v2p_build_key_linuxx64.txt`）。
> **反证手法**：注入 → 重建 → 跑用例（应 FAIL）→ 恢复 → 复验（例：本轮 v2 注入用
> `cp CN语言编译器v2/IR/IR容器.cn /tmp/.../IR容器.cn.bak` + python 替换 + 复原）。

## 四、本轮踩坑（已入 lessons 第八十一轮段）

- **㊹ 槽位形态清单按「存放位置」穷举，不按「类型」二分**（字符串/有析构类 二分漏掉
  「元素=值语义聚合」第三类）。
- **㊺ 结构性写入（移位/搬移）不能用赋值语义**：CN 结构体赋值=深拷（79-a），移位应 move
  （memcpy + 源槽清零，Rust `ptr::copy`）；判据问句「复制一个值 vs 把元素搬到另一个槽」。
- **㊻ 名单式 RAII 的截断纪律**：终止路径一律**不截断**编译期名单（截断 → 函数级兜底
  看不到该项；幂等清槽使重复释放安全）。
- **㊼ 长函数中段 early return 会连带跳过兄弟段**（`genStringFrees` 实证）。
- **㊽ v2 帧尾共享 retbuf 的消费纪律**：任何「返回值跨越另一次调用」的形态必被覆盖；
  **生成期新增槽必须独立于预分配槽区**（首版教训：E2E 148/158 回归）。
- **㊾ 反证实验要逐注入点对应位**（宿主注入丢 ①~⑦=128、v2 注入丢 ⑥=223）。
- **㊿ 新用例命中既有登记边界时调整用例**（235 ⑧ 从「写参数字段」改为「读取面」）。

## 五、诚实边界

- **v2 帧尾共享 retbuf × 嵌套结构体返回实参**（单实参泄漏 / 双实参数据损坏）=下一轮
  第一优先（三方案见上）。
- **v2 结构体字面量作实参**=v2 语法缺口；**v2 侧结构体元素释放不在 v2 生成**（经供给
  obj 的 `~容器` 注入）=两侧机制不同（设计选择，三列核对按「v2 侧无此挂点」判读）。
- **B35 字段容器接收者**（残留 2）维持靶子（语言级裁决）；**stdlib 链表槽复用缺陷**
  登记；**被调方写按值参数字段**=归一化落堆不释放（79-a 边界维持，E2E 235 ⑧ 只覆盖
  读取面）；**未初始化槽 preFree 隐患**（依赖 `cn_free_tracked` 在册判定空安全）。
- 结构体容器字段浅拷共享、数组元素持串、装箱字段接收、静态面无 RAII 等 79-a 边界维持；
  win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s **308719 行**、cn_self71）。
- 探针（/tmp/p81probe，不入库）：P1~P17（容器元素全形态、分步打点、变体矩阵、字面量/
  返回实参、链表平铺、retbuf 共享）、p*.cn 源 + 编译产物、v2run.sh（v2 闭环复现脚本）、
  IR容器.cn.bak / ir_oop.cpp.bak（反证注入用备份）。
- E2E 用例：`tests/e2e/234_双编译对照容器元素结构体字段/`（主.cn + 供给.cn +
  双编译对照.txt + 主.expected）、`tests/e2e/235_容器元素实参形态/`（主.cn + 主.expected）。
- 修改文件：宿主 `src/cn_compiler/ir/{ir.hpp,ir_oop.cpp,ir_oop_call.cpp,ir_call.cpp,ir_stmt.cpp}`
  + `stdlib/容器.cn`、`stdlib/映射集合.cn` + `scripts/cn_release_parity.sh`；
  v2 `CN语言编译器v2/IR/{生成表达式求值.cn,IR容器.cn}`。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（第十三节 81-a + 矩阵 B36/B37 + 三方案；
  每轮触碰所有权/识别语义必须同步更新）。
