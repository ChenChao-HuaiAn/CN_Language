# HANDOFF 交接文档

**交接时间**: 2026-08-15 15:58（第 8 层完成后）

## 我们在做什么任务

CN 语言编译器「模块系统 v2.0 全面改造」共 10 层。当前已完成第 1-8 层：
- 第 1 层：规范更新（specs/08 v2.0 + 09-包与依赖.md 新增）✅
- 第 2 层：词法（ColonColon + 新关键字 模块/作为/包/货舱）✅
- 第 3 层：语法（ImportDecl 结构化 v2.0 + parseModuleDecl + 默认私有）✅
- 第 4 层：crate 重构（module.cpp 分桶 + 目录层级 + 语义分域 + use 导入表 + prelude + 内置 key `::` 化 + IR/codegen 包前缀 + 可见性交集）✅
- 第 5 层：货舱.toml 依赖管理（cargo_parser + CLI + 依赖查找 + 包.cn）✅
- 第 6 层：全量迁移（E2E 36/46 → 46/46 全绿）✅
- 第 7 层：新增 v2.0 特性测试（44-51 用例）✅ —— E2E 53/53、单测 1106/1106
- **第 8 层：全链路集成串联（52_library 图书管理系统）** ✅ —— E2E **54/54**、单测 **1108/1108**
- 剩余：第 9 层（Debug 全面审查：内置 key、crate 前缀、默认私有、prelude、交集规则、全量回归直至零缺陷）、第 10 层（收尾）

## 已经完成了什么（第 8 层）

1. **新增 E2E 52_library「图书管理系统」**——13 项 v2.0 全特性综合命令行工具：
   - `货舱.toml`：`[货舱] 名称="图书管理系统" 版本="1.0.0"` + `[依赖] 核心="内置" / 工具库="本地"`（自动发现）
   - `主.cn`（入口）：`导入 管理::馆藏::馆藏`（①::路径）、`导入 工具库::格式化::{价格, ISBN}`（②花括号）、`{价格 作为 格式价格, ISBN 作为 格式ISBN}`（③重命名）、`导入 管理::馆藏::*`（④通配符）、顶层 `常量 默认馆藏容量 = 3`（⑬）
   - `图书.cn`：模块公开类 `图书`（私有字段+公开方法 ⑪可见性交集）+ `创建图书` 公开函数 + `校验编号` 默认私有（⑩闭包合并）
   - `管理.cn`：`模块 馆藏` 声明（⑤模块树）+ `管理/馆藏.cn` 子目录（⑥）
   - `依赖/工具库/包.cn`：`模块 格式化` + `公开 导入 格式化::价格/ISBN`（⑦包.cn 再导出）
   - `依赖/工具库/格式化.cn`：`价格(浮64)->字符串`（%.2f）、`版本()=200`（⑫crate 隔离 vs 主 `版本()=100`）
   - 功能流程：创建 3 本图书 → 加入馆藏 → 馆藏数量 → 逐本打印（格式价格/格式ISBN）→ 馆藏清单（向量<整64> ID）→ 版本限定调用（主::版本()=100 vs 工具库::格式化::版本()=200）→ 顶层常量
2. **最小修复 2 处**（v2.0 实测缺陷，`semantic.cpp`）：
   - **无参函数跨模块 key 解析**：`格式化$版本`（无 `#`）的 keyModule 未提取 → resolveOverload/hasFunctionName base=`格式化$版本` ≠ `版本` → 报"未找到匹配"。修复：无 `#` 且调用名不含 `$`（区别于泛型实例 `排序$整32`）时剥离 `$` 前缀
   - **跨 crate 限定调用 moduleFilter 最后段匹配**：`工具库::格式化::版本()` subModule=`工具库::格式化` vs 外部模块 moduleName=`格式化`（文件主干）→ 精确匹配失败。修复：moduleFilter 末段 == entryModule 即视为同一模块
3. **新增回归单测 2 个**（test_module.cpp）：`CrateIsolateNoParamQualifiedCall` + `CrateIsolateCrossCrateLastSegmentFilter`

## 测试结果

- 全量单测：**1108/1108 全绿**（1106 + 2 新增回归）
- 编译警告：**0**（MSVC /W4 /WX，semantic.cpp/module.cpp 增量编译通过）
- E2E：**54/54 全绿**（53 原有 + 52_library，零失败零未实现）

## 总结发现的问题（编译器边界，非缺陷，E2E 按支持路径编写）

1. **向量<T> 8 字节槽**（lessons 记录）：类对象（>8B）存向量会越界 → 馆藏 类用手动动态数组（`整64*` + 容量/数量，stdlib 容器.cn 同款）；向量<整64> 由主.cn 局部变量承担
2. **跨模块泛型类字段**：`向量<整64>` 作类字段时构造返回 mangled 名 `向量$整64` 与字段声明 `向量<整64>` 类型不匹配 → 馆藏 类不组合泛型字段
3. **子目录模块无法导入父目录模块**（候选路径基于子目录）→ 图书 类经 主.cn 导入链全局合并，馆藏 类直接引用（同 crate 模块公开符号）
4. **crate 隔离同名函数纯名调用歧义**（resolveOverload 无 moduleFilter）→ 限定调用：`主::版本()`（自导入 `导入 主::版本`）+ `工具库::格式化::版本()`（多级路径）
5. **整路径重命名**（`导入 路径 作为 别名`）绑定模块级别名 → 重命名用花括号项内形式 `{价格 作为 格式价格}`（45 同款）

## 当前卡在哪

无卡点。第 8 层全部完成（提交待 gitcode 推送，403 保留本地）。

## 下一步计划（第 9 层）

Debug 全面审查：内置 key、crate 前缀、默认私有、prelude、交集规则、全量回归直至零缺陷。重点可关注：
- 52_library 发现的 2 处最小修复是否引入其他边界（resolveOverload 最后段匹配可能影响同包内 `模块X::子模块` 限定调用的严格性）
- 整路径重命名（`导入 路径 作为 别名`）绑定模块级别名——是否应支持符号别名（第 7 层已知边界）
- 花括号项别名 + 跨模块同名歧义（44 记录，moduleFilter 缺失）

## 踩过的坑绝对不要再踩（已同步 lessons.md）

- **E2E 不支持预期编译错误**：run_e2e.py 编译失败即 FAIL。错误类场景必须用**单测**验证语义报错；E2E 只测合法用法。
- **向量<T> 存类/结构体越界**：8 字节槽约束；类对象用指针数组或手动动态数组，向量只存标量（整64/字符串）。
- **跨模块泛型类字段类型不匹配**：`向量<整64>` 作类字段构造返回 mangled 名不匹配；泛型类实例化放局部变量。
- **重命名导入（作为）编译器实现边界**：花括号项重命名→纯名别名可用；整路径重命名→模块级别名；跨模块同名+别名→歧义。
- **crate 隔离同名函数纯名调用歧义**：必须限定调用（`主::版本()` 需自导入 `导入 主::版本`）。
- **跨 crate 限定调用 moduleFilter 不匹配**：外部依赖模块 moduleName=文件主干，调用路径 subModule=包名::模块——resolveOverload 最后段匹配已修复（第 8 层）。
- **子目录模块无法导入父目录模块**：依赖主导入链全局合并；子目录模块内的符号（如 容器）经 stdlib 兜底加载，需显式 `导入`。
