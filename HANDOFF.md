# HANDOFF 交接文档

**交接时间**: 2026-09-10 第六十一轮（**深度机 linux-x86_64**）——**plans/019 阶段3b 性能主项「转移浅拷贝优化」双侧落地**（容器/类转移=槽位交接：句柄直拷+源槽清零+空安全跳过=恰好一次析构）。v2self 固定点复锚 **245230 行 = X64L 新口径** + 对拍 29/29 + 运行级 3/3 + 全量门禁 220 用例 215 过（唯一败=194 既有待批）+ 单测 1283/1283。

编译期安全专项累计五轮：阶段1 显式转移（c924145）/ 阶段2 引用逃逸+两缺陷根治（e953f33）/ 阶段3 只读借用+互斥（9dea00f）/ 3b 语义面（b641f56）/ 3b 浅拷贝（aed69c3）。

---

## 一、本轮做了什么

1. **宿主浅交接**：transferDeclSources_（VarDecl 节点键→源名跨层通道）+ genVarDecl 浅分支（跳过 NewObject+拷贝构造→句柄直拷+源槽清零——源析构空安全跳过；条件分支两路径正确）。
2. **v2**：声明位转移中 标志+分级 2 放行=豁免模型零 IR 改动浅交接；v2 容器/类声明位转移放开。
3. **E2E 203**（析构计数=1 次；深拷对照=2）+**203_v2**（容器校验和 rc=7）+回归全绿。
4. 锚定链 245230 行固定点一致+对拍 29/29+运行级 3/3。
5. 【立案】类静态成员+析构写静态=运行期崩溃（tp3 独立既有缺陷）。

## 二、下一轮任务（按序）

1. **阶段 4 细则已呈报**（见 plans/019 §5.3 与本轮会话汇报）——**待用户批复后开工**（不安全 函数 修饰+安全区边界清单+stdlib/v2 迁移分批）。
2. **NLL 跨语句借用活跃区间 + v2 实参边界记录**（3b 剩余，按误报率实测定深度）。
3. **类静态+析构写缺陷三方案呈报**（tp3 复现配方：类带静态成员+析构函数写静态+两实例构造析构即崩）。
4. **194 测试改写待批维持**；**跨机轮**：第五十七~六十一轮五轮变更 win/arm64 复验。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 深度机 linux-x86_64（本行口径）
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1283/1283
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 220 用例（败=194 待批，跳=62/69/78/79）
bash target/p61x64l/run_chain61.sh   # 锚定链（[3] readelf bug——map 手工判定
#   cn_main=0x4df7c7 ∈ cn_self61.o .text [0x402586,0x4e3253)；[6] 段口径错——runlevel50.sh 口径）
bash target/p61x64l/cmp29_61.sh      # 组件对拍 29/29
# 固定点 X64L 新口径=245230 行
```

## 四、本轮踩坑（入 plans/014 第六十一轮行）

- **浅分支首插脚本锚失败未落**：python 批量替换按文件分断言，decl 锚失败导致 ir_stmt 替换未执行——构建通过（既有深拷贝仍编译过）=假象，**IR dump 实证仍 NewObject+拷贝构造**后补插。预防：行为级改动必须 IR dump/asm 级验证，构建过≠改动生效。
- **isTransferDecl 误置 private**：IR 层调用编译错——跨层查询函数按消费者定访问区。

## 五、诚实边界

- 浅交接覆盖=堆模型类型（容器/类）；结构体（值块）走 CopyStruct 行为不变（无优化空间）；指针/字符串本就值交接。
- v2 浅模型=豁免式（u 不析构+v 析构）与宿主（u 析构+v 空跳）执行者不同但均为恰好一次释放。
- 194 待批维持；类静态缺陷立案待呈报；win/arm64 复验=下轮跨机轮；探针留 target/p61x64l/ 不入库。
