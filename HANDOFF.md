# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-16——**260-a：D32 根治（真根因=SSA 占位 Phi 化·非 RA！）+工具链并行架构（用户令「每次运行太慢」）**。

### 一、本轮做了什么（写给无上下文的新会话）

1. **D32 根治（P1·用户 selected 最高优先）**——定位链有反转：s266「-O3 变量混淆」（-O0 变13=-91471 正确、-O3=37903 错）→ 双级别对拍 + `-O3 --no-regalloc` 正确（初判 RA）→ **IR 对拍反转：IR 层已错**（%v128 被 Phi 降级搬运定义与「加载变8」结果同号冲突 + bb13 病态轰炸 60+ 重复加载/搬运）→ **真根因=SSA Pass（256-a F1-26 方案A）占位 Phi 化**：汇合块 Load 的槽其 Store 位于其后顺序段（前驱块无 Store）时，「占位：沿用槽值」兜底在**前驱块尾注入 Load 读尚未 Store 的栈槽**（UB）→ 垃圾值经 Phi 降级拷贝污染。asm 面「读 r12（被变10 复用）」只是下游投影。
   **根治**=`buildFunction` Phi 化前提收紧（新增 `hasStoreTo` 全前驱判据：任一前驱无该槽 Store 即跳过 Phi 化、Load 保留原栈语义）；副作用=病态轰炸消除（IR 877→219 行）。
2. **样本转正**：`387_cnsmith_s266混淆O3`/`388_...O0`（显式 -O0/-O3 旗标·同源同 expected=双级别机械锚定；s266 源入库沿用 tests/cnsmith_cases/）；s268 复验构建运行正常。
3. **check_asm_width 扩维+并行化**：第三维 **win-x64 mov 宽度混配**扫描（r64↔r32 非法混配·内存操作数/movsxd 豁免·负样本自检 3 命中双向）；`--jobs` 并行 **344.7s→62.0s**；全量 **249×3 PASS**。
4. **cnsmith_diff 并行架构（用户令·插单）**：ThreadPoolExecutor 样本级并行 + **预热**（运行时 obj 缓存落 target/ 固定点，cache miss 并发写=踩踏，先串行建缓存）+ 有序聚合（输出与串行口径逐字节一致）+ 进度 flush；**500 采样 485s（≈8×）**。
5. **T11 联动+撞号修正**：深度机 x86_64 定性（384 -O3 三症状 ASLR 非确定）面①「优化链产出未支持形态」随本轮同根消除（384 x86_64 .s「未支持操作码」指纹清零实证）；深度机新条目原编 T10 与既有 T10（子串截断）**撞号→修正 T11**；残余面②（发射器 default 静默→硬错误收口）/面③（IR 验证器支持面）立案。

### 二、验证链（全绿）

零警告构建（MSVC /W4 /WX）+单测 **1351/1351**（+2 Phi 前提负例）+全量 E2E **407=407/0/0**（含 78_v2/79_v2 锚定链自洽）+CLI 矩阵 **50/50**+check_asm_width **249×3 PASS**（62s）+CN-Smith **500（seed=258）零分歧 500/500**+双文档门禁。
反证：修复前基线=s266 -O3 复现 37903（本机对拍留档 target/p260probe/）+单测新负例在修复前会失败（生成期 run=true 断言反转）；asm_width 负样本自检（A2022 形态 3 命中）双向防御。

### 三、下一步（新会话按序）

1. **C20 波1**（A 区首位·D32 后）：E2E 双测断言升级（盘点数据=深度机已交付 plans/025 §二：候选 A72/缺口 B129/负测 C53+Cx17）；波2 硬前置=D28/D29。
2. **T11 残余**：发射器 default 静默→编译期硬错误（linux_x64 dispatch 2 处：主 dispatch + OOP dispatch）+ IR 验证器指令支持面（可随 C20 搭车或独立小轮）。
3. **S1 采样轮**（免锁·任一空闲机）：cnsmith_diff 已并行化=差分轨就绪；cnsmith_daemon 自治循环+s269.cn 抢救入库。
4. 深度机复跑 **384 x86_64 运行级**（T11 面①独立复验·本机仅 compile 面扫描）。

### 四、诚实边界

- T11 残余面②③未实施（已立案；当前无实弹+check_asm_width 指纹网每轮全量兜住）。
- 384 在本机仅 compile 面扫描（运行级=深度机）；**386 用例注释「-O0 基线」实为默认 -O2**（258-a 措辞问题；388 已用显式 -O0 旗标纠正口径）。
- verifyConstWidths 常开（D31 边界）不变：未来某 pass 新产超域常量=编译拒绝，届时归因该 pass 补归一化面、不调阈值。
- 并行化未引入跨样本共享写（每样本产物名唯一；运行时 obj 预热后只读）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-16 🧪 测试轮（**深度机 linux-x86_64**·不占轮次号）——**构件面检测制度（B8）呈报** + 采样/T12/T13 收尾（用户三次质询驱动：「点还是面」「面不能只针对 &&」「**要制度，不能我提醒你才测**」）。

### 〇、B8 制度呈报：构件面检测制度（待用户裁决·全文）

> 反思：T12（面效果）与 T13（推广到其他算符）都是用户提醒后才补——根因=「面」只存在于用户脑中与 AI 临场发挥，仓库里没有「应该测什么」的权威清单与机械完成度保证。制度目标：**面检测成为不依赖任何人提醒的常设机制**。

1. **面台账（应该测什么的唯一权威）**：`tests/matrix/面台账.md`——从 plans/001 规格书**文法产生式系统导出**「构件×值域×消费位×优化级别」全部格子（不靠临场想起）。构件维度=词法字面量形态/全运算符/类型系统全域/声明形态/语句/聚合与构造/容器×所有权/函数与泛型单态化/模块与可见性/**标识符遮蔽与作用域**；值域=常规/边界（极值±1·零·负）/非法负测；消费位=值位/条件位/实参位/返回位/聚合字段位/容器元素位/静态初值位。每格状态 ⬜未测/🔶已探针/✅已转正。**新增语法必须同轮加格**（挂 §8.4 契约变更与新增语法审批）。
2. **巡视默认动作（谁都知道下一步测什么）**：空闲机做测试/探针轮时，**默认动作=取台账最靠前的未测格跑矩阵探针**（/tmp 三方对拍法已两轮验证：gen2/e/opscan）；用户指令=插队优先级而非方向来源。
3. **转正关死**：巡视命中→立案（T 系）；根治轮修复**同轮把该格矩阵转正 E2E**+格标 ✅（既有纪律入台账闭环）。
4. **完成度机械门禁（不靠记性靠机器）**：`scripts/check_matrix_coverage.py`——①台账未测格**只减不增**（新增格必须同轮带探针记录或转正）；②覆盖数（如「面覆盖 175/412」）**注入 plans/021 文首戳**——戳有机械校验，欠账每轮对用户可见；③初期欠账大先可见、后收紧进 ci。
5. **缺陷立案必修两问（存量纪律制度化）**：每立一案必答——①该构件台账格齐了吗？②同族构件（姊妹算符/同形态语句/另两后端）要不要加格？（lessons 93-a 第 9 条「变体矩阵/姊妹后端照妖镜」的制度化）。

**待裁决三点**：①制度五件套本身；②台账初版建格由开发轮（可搭 T12/T13 根治轮）还是文档轮承担；③AGENTS §8 角色职责修订（探针/E2E 轮加「巡视台账」默认职责）——批准后由对应轮次落地。

### 一、本轮做了什么（写给无上下文的新会话）

1. **CN-Smith 采样首跑**（seed=20260916×500）：165/500 分歧→T12（D32 后仍在=独立缺陷）；负向轨 1000/1000 必有诊断零崩溃；回归轨 s266/s268 绿。
2. **T12 面矩阵（逻辑构件 73 形态）**：O0 全对/O3 错 34——两病灶=①发射装槽读 `[rbp]` 左假恒真（IR 对）②编译期折叠 真&&真→假（IR 错）；`||`/`!`/条件位全对；E2E 全绿根因=语料无值位布尔形态。
3. **运算符全景（102 形态）**：97 全对+**T13 新立案**=`>>` 负数逻辑移位（shr 非 sar·-O0 全路径错·-O3 对·与 T12 反向·变量形态同错·`-2147483648` 64 位字面量路径分叉实证）；溢出观察 2 项归 C18。
4. 探针方法沉淀：/tmp/cn_matrix/{gen2,e,opscan}.py（生成+python 期望三方对拍）——巡视脚本 matrix_probe.py 的原型；opscan 须在项目根跑（cn 相对路径探测 src/runtime）。

### 二、验证链

采样=cnsmith_gen/cnsmith_diff --jobs 8；矩阵=上述三脚本；T13 复现 `打印行("R= ", (-12) >> 3);` O0=536870910/O3=-2。

### 三、下一轮任务

1. **B8 裁决后落地**（对应轮次）：台账建格+AGENTS 修订+check_matrix_coverage.py。
2. **T12+T13 根治（开发轮·可同轮）**：T12=装槽源可达校验（联动 T11 残余面②）+i1 折叠单测；T13=sar/shr 按符号分派；两案矩阵 E2E 转正+反证；生成器补布尔打印/负数移位形态。
3. 采样 daemon（256-b）+跨机复验（win/arm64 对 T12/T13）；深度机待命（用户令只做测试/探针·**B8 批准后巡视=默认动作**）。

### 四、诚实边界

仅 linux-x86_64 实测；矩阵已覆盖 175 形态（逻辑+标量算符）——台账其余构件（浮点/字符串/容器/聚合/泛型/遮蔽等）未测且尚未建格（待 B8）；T12/T13 源码行未定位（定性至层）；全量 E2E/单测/锚定链未跑。
## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
