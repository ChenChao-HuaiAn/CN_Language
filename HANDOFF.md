# HANDOFF 交接文档

**交接时间**: 2026-09-02 第四轮（②c 前置：宿主缺陷簇 1-4 根治——代码已落、回退定模后待验证；
换电脑中断，接手先读「接手第一小时」）

> 本轮上下文：用户令「继续 v2 自举后续任务」→ 计划批准 **②c 四项一轮收官**（A 嵌套容器 /
> B 容器作结构体字段·拥有式 / C 顶层静态容器 / D & 引用参数，E2E 127 单用例全覆盖作载体）。
> 第 0 步探针（target/dbg/probe127/p1~p24）实证出**宿主缺陷簇**，用户裁决**先根治宿主缺陷簇**
> 再做 B 全量。根治代码已全部落地，最后一次「回退内联模型→重建→验证」命令被取消（换电脑）。

## 接手第一小时（按序执行；单位机=Linux ARM64 / 家机=Win11 AMD64 皆适用）

1. **检查工作区脏文件**：`git status`。13 个已修改文件（12 代码 + HANDOFF）均为本轮有意的
   缺陷簇修复（v2 组件调试打印已全部还原干净）。⚠ 若本轮改动尚未推送而你在新机器上，
   先向用户确认改动是否已随 git 到位（见「验证状态」节末尾的探针转移说明）。
2. **重建**：
   - Linux：`cmake -S . -B target/build && cmake --build target/build -j8`
   - Windows：`powershell -ExecutionPolicy Bypass -File scripts/ci.ps1`（构建+门禁一步到位）
3. **验证**（全绿才算宿主缺陷簇根治收官）：
   - Linux 单位机：单测 `./target/cn_unit_tests`（1196/1196）+ E2E 全量
     `python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn`
     （基线口径：128 用例 = 123 过/4 平台跳过/1 失败=78 已知遗留——回退后全量**尚未跑完**，
     两次被打断；探针 17/17 与 v2p-119 冒烟已绿）
   - Windows 家机：`ci.ps1` 全量（win-x64 路径，②b 起欠着的门禁，本轮修复的 MASM 路径
     验证一并覆盖；重点用例清单见「验证状态」节）
4. **两个未诊断失败**（回退前 E2E 全量 117过/7败中的非 v2 项；v2 五例预期已随回退修复）：
   - **118_宿主缺陷_链式方法调用 输出不一致**：尚未看过 diff。高嫌疑=本轮成员表达式类字段
     LoadPtr 统一改动（ir_oop.cpp handleClassMemberExpr）影响其断言输出。
     分诊：`python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --filter 118 --verbose`。
   - **78_chain_build 段错误(-11)**（原为 -9 内存超限）：高嫌疑=**级联析构注入双重释放**
     （v1 组件后端对象可能已被既有路径删除，injectFieldCascadeDestroy 又注入一次）。分诊：
     临时注释 ir_oop.cpp emitClassMethod 内 `if (mi.isDestructor && !mi.isStatic)` 块验证因果；
     确认后按缺陷零容忍给用户 ≥3 方案裁决。

## 本次会话做了什么（宿主缺陷簇根治，全部有实证）

### 探针实证的缺陷簇（target/dbg/probe127/，全部保留可复跑）
| # | 现象 | 实证探针 | 根因 |
|---|---|---|---|
| 1 | 类容器字段「成员表达式路径」崩/静默 0 | p2/p6/p7 | ir_oop.cpp handleClassMemberExpr 类字段返回字段地址 vs 方法内直访 LoadPtr（A-4）两路径分裂 |
| 1' | 类/结构体对象指针字段下标步长兜底 8 | p8/p9 | ir.cpp lvalueAddress 与 ir_expr.cpp visitIndexExpr 的 MemberExpr 下标分支只查 StructDecl 数组字段 |
| 2 | 结构体容器字段段错误；布局把 24 字节容器算成 8 | p3（+v2p 崩溃链） | ① 语义趟序：结构体布局在 registerGenerics **之前**算（容器未实例化 typeSizeOf 兜底 8）② 结构体局部不零初始化 ③ 读写模型分裂（见「模型定案」） |
| 3 | 无字段级联析构原语；NewObject 裸 malloc 不清零 | p10/p11（20 万次循环 RSS 1.3MB 零泄漏实证修复后） | CN 无 删除/字段析构链；stdlib 全库被迫裸指针+分配/释放 规避 |
| 4 | stdlib 向量/映射拷贝构造下分配 | p8（b[2].丙=2 应 3） | 容器.cn:55 `分配(数组容量*8)` vs 同文件追加路径 `max(8,类型大小(T))` 不一致；映射同款 |

### 已落地的代码修改（12 文件，未提交）
- **stdlib/容器.cn:55 / 映射集合.cn:156,164**：拷贝构造分配改 `max(8, 类型大小(T/K/V))`（缺陷4）。
- **src/cn_compiler/ir/ir.cpp**：lvalueAddress 下标 MemberExpr 分支补指针/数组字段步长推导（缺陷1'）。
- **src/cn_compiler/ir/ir_expr.cpp**：visitIndexExpr 通用尾巴步长与元素形态两块同款补全（缺陷1'）；
  visitMemberExpr 结构体路径保留 LoadPtr（模型定案后恢复原样+注释）。
- **src/cn_compiler/ir/ir.hpp + ir_oop.cpp**：新增 `memberFieldSrcType(MemberExpr*)`（结构体/类
  统一字段源码类型查询）；handleClassMemberExpr 类值字段读改返回 LoadPtr 对象指针（缺陷1，
  对齐 A-4）；新增 `injectFieldCascadeDestroy`（缺陷3：~类名() 体后按字段逆序 DeleteObject 级联，
  空安全）。
- **src/cn_compiler/ir/ir_stmt.cpp**：genVarDecl 尾部结构体裸声明逐槽零初始化（缺陷2，对齐 v2 B1 语义）。
- **src/cn_compiler/semantic/semantic.cpp**：趟序重排 registerGenerics → 结构体字段实例化归一
  （resolveGenericTypeName，向量<IR指令>→向量$IR指令）→ computeLayout（缺陷2；**迭代稳定性：
  必须裸指针+索引，ASAN 实证 range-for 引用被 ensureLoweredType 的 structs.emplace_back 重分配打悬**）。
- **src/cn_compiler/semantic/class_resolver.cpp**：checkClassMethods 头部「拥有式字段未声明析构」
  **警告**（不能 Error——会打爆已验收 E2E 58 馆藏，教训已踩）。
- **src/runtime/io_api.cpp**：`__cn_object_new` malloc→calloc（缺陷3：未构造字段=null 空安全）。
- **CN语言编译器/x64后端.cn、arm64后端.cn**：类补声明空体 `函数 ~x64后端() { }` / `~arm64后端()`
  （上述警告的连带修复；级联注入会释放其 输出行 向量——原先是真泄漏）。
- 中途试过并**已回退**的方案：结构体容器字段内联模型（构造调用以字段地址为 this 直接构造 +
  emitCtorCallIntoAddr 辅助）——被 p24 最小复现证伪（见「模型定案」），相关代码已删净。

### ⭐ 模型定案（新会话不要再翻案）
**结构体/类容器字段一律「指针槽模型」**：字段区按 typeSizeOf(容器) 保留（向量 24/映射 56），
**首 8 字节存堆对象指针**——构造赋值 `f.表 = 向量<整64>()` 走 NewObject+构造+StorePtr 存指针；
读取 LoadPtr 得对象指针；方法调用 this=对象指针；`&f.表`（引用实参）=字段地址，byRef 机制
Load 槽→LoadPtr 解引用恰好得对象指针。**与类字段 A-4、byRef（isByRefCapture）、静态容器 P3-8
指针槽全线一致**。内联模型（字段区即容器本体）与 byRef 双层解引用机制冲突（p24：`填(f.a, 42)`
中 `v.追加` 接收者=LoadPtr(&f.a)=数据指针→空指针）——本轮第三轮返工的根因，勿再尝试。
v2 侧（②c B 步）实现时同样按此模型对齐：字段大小查 容器对象大小 表（24/56…）、宽度码=8/槽1。

## 验证状态（诚实门禁）+ 双机验证矩阵（换电脑：单位机=麒麟 ARM64 → 家机=Win11 AMD64）

- **单位机（麒麟 ARM64，本轮已验证）**：构建零警告 + 单测 1196/1196 + 探针 17/17 绿
  （p3/p12/p13 三例补构造初始化后过——原失败是探针写了被淘汰的内联模型预期，裸声明未构造
  字段=确定性错误码3 是设计行为）+ v2p-119 冒烟 PASS（回退指针槽模型后关键回归已修复）。
  **全量 E2E linux-arm64 回退后的复确认未跑完（两次启动均被打断）**——最后一次完整数据是
  内联模型期的 117过/7败；回退后 v2 五例预期回绿（v2p-119 冒烟已证），118/78 仍需分诊。
- **家机（Win11 AMD64）能做且必须做**（③ 前完成）：`powershell -ExecutionPolicy Bypass
  -File scripts/ci.ps1` 全量门禁 = MSVC 构建（/W4 /WX 零警告）+ 单测 1196 + E2E win-x64
  全量 128 用例。本轮 12 文件改动的 IR/语义层是平台无关的，win-x64 全量即其 MASM 路径验证。
  重点用例：**119/120/123/125/126**（v2 闭环走 函数IR.指令=向量<IR指令> 全链——本轮缺陷2/
  模型定案的直接回归）、**110/111/113~117**（类字段/拷贝构造/元素析构——本轮成员表达式统一+
  级联析构注入直接相关）、**58**（拥有式字段警告化后必须仍绿）、**78**（若 win 也段错误(-1073741819)
  则确认「级联析构双重释放」假设，按缺陷零容忍上报）、121/122/126。
- **家机做不了**：E2E linux-arm64 全量（需 Linux+aarch64 as/g++）——单位机（或下一台
  Linux ARM64）补跑复确认；`--target linux-arm64` 在 win 上无工具链。
- **探针 17 个在 target/dbg/probe127/（gitignore 不入库，换机不随 git 走）**：若整工作区
  拷贝则随行；若纯 git 克隆则没有——其断言价值大半已被上述 E2E 用例覆盖，关键形态
  （映射字段设置/字段引用实参/供给类契约）已记录在本 HANDOFF 与计划中，无需重建即可继续；
  E2E 127 立项时这些形态将转正为正式断言。

## 用户决策记录（不可推翻，除非用户再裁）

1. **范围**：②c 四项一轮收官（A/B/C/D 一次做完，E2E 127 单用例四主题全覆盖——126 模式）。
2. **B4 模型**：用户委派按双目标（性能超 C++/安全近 Rust）裁定 → 我裁指针槽模型（内联模型因
   byRef 机制冲突被实证淘汰，与最终宿主定模一致）。
3. **B 块深度**：用户选「**先根治宿主缺陷簇**」（比我推荐的「B 收敛局部拥有式」更彻底），
   然后做 B 全量含 向量<词条> 级联。
4. **缺陷 4**：本轮顺手根治（含链表/栈/队列/映射同款审计——已确认仅 向量/映射 有拷贝构造，
   其余类无此方法不触发）。
5. 新增语法规范仍须审批（本轮无新增语法，全部为既有语义根治）。

## 下一步（按批准计划继续 ②c）

1. 完成上面「接手第一小时」验证；118/78 分诊收尾（若 78 确系级联双重释放：方案=仅在「类未被
   既有路径显式删除」时注入？——需按缺陷零容忍给用户 ≥3 方案裁决，不得自行绕过）。
2. **E2E 127 立项**（0f39be3 模式：先立项不注册运行器，门禁保持绿）：
   `tests/e2e/127_v2_嵌套容器与容器字段与静态与引用/{主.cn, 供给.cn, 主.expected}`。
   断言四主题块 201 起编号（A 嵌套向量<映射>/B 结构体容器字段全生命周期+向量<词条>级联/
   C 静态容器跨函数/D 引用参数写回）。**供给类契约已探明**（p4/p6/p7 实证）：供给.cn 用类形态
   （值字段+构造里 `表 = 向量<整64>()` 初始化+拷贝构造逐元素法 `当(i<其他.表.大小()) 表.追加(其他.表.元素(i))`
   +声明空体析构），向量<词条> 追加即获 H8-⑤ 深拷贝+Feature2 级联（p7 池元素表=2 实证）；
   供给源拷为独立目录 主.cn 经宿主编译（B7 模式）。
3. **②c 实现**（v2 组件改造，改动面：语法分析/语义分析/IR生成/IR容器/代码生成共用/代码生成/
   代码生成ARM64/主.cn）：
   - A 嵌套容器：IR容器.cn `类型大小与对齐` 对 $ 合成名查 `容器对象大小`（映射56，消灭 8/8 兜底）；
     元素(i) 返回容器→容器返回_结构体（retbuf B6 复用）；`元素(i).设置(k,v)` 调用对象=CallExpr
     方法调用类型推导（对标宿主 E2E 118）；追加(映射) 实参=对象指针直传。
   - B 容器字段（v2 侧指针槽模型，见「模型定案」）：登记字段类型容器分支→宽度8/槽1（布局保留
     typeSizeOf）；结构体声明→各容器字段 NewObject+构造；拷贝点（赋值/按值传参 prologue/返回）
     调拷贝构造；块出口+返回注入析构（发射容器析构 通路泛化）；`a.表.追加()` 字段读=LoadPtr→this。
   - C 顶层静态：v2 四层全无。语法节点_静态声明（spec 03）；语义登记+归一；IR 主 入口注入
     NewObject+构造+StorePtr（P3-8 统一指针槽）；标识符读取符号地址+LoadPtr；双后端 .data 发射
     （GAS `_cn_gstatic_名`/MASM `?gstatic_名`——0c 探针**未做**，做时先与宿主产物对拍）。
   - D & 引用参数：v2 语法已识别（附加=引用*256）但语义未实现。byRef 槽存地址/读 LoadPtr/
     写 StorePtr（对齐宿主 A-1）；结构体& 登记 变量结构体表（修「附加==0 才登记」排除）+
     跳过 prologue 多槽拷贝；调用方实参回扫取址（扩展 B8 表驱动）。
4. 注册 127（`v2闭环用例们` 加 `("主.cn", 0, True, ["供给.cn"])`）→ 单测+E2E 全量零回归 →
   MASM 产出 asm 级核验（嵌套符号 vs 247 限制记录风险）。
5. 文档：plans/014 ②c 条目+状态表、更新日志、lessons（本轮至少 4 条新坑：迭代稳定性/模型分裂/
   ASAN 输出目录污染/调试打印切断跨行语句）、HANDOFF；中文提交推送 origin/develop；
   探针清理（target/dbg/probe127 任务完成时删）。
6. ③ v2 编译 9 组件自举对拍（下一轮）：③ 硬前置已盘点——A：语义分析.cn 核心=向量<映射<整64,符号>>；
   C：v2 组件遍布 `静态 映射<整64,整64> 结构体大小表` 等顶层静态；D：主.cn:90 `映射<整64,整64>& 已加载`。

## 踩过的坑（本轮新增，接手必读）

- **语义趟序重排的迭代稳定性**：visitProgram 里向 `node->structs` 追加元素（ensureLoweredType
  合成结构体）会触发 vector 重分配——range-for 的引用必悬（ASAN heap-use-after-free 实证，
  非确定段错误极难裸调）。修法：`.get()` 裸指针+索引循环（unique_ptr 重分配不移动堆本体）。
- **ASAN 构建污染**：`cmake -DCMAKE_CXX_FLAGS=-fsanitize=address` 建独立 build dir 时，
  CMAKE_RUNTIME_OUTPUT_DIRECTORY=target/ **共享**——ASAN 的 target/cn、lib*.a 会覆盖正常产物，
  用后必须 `rm -rf target/build-asan target/lib*.a target/cn target/cn_unit_tests` 再重建
  target/build（本轮踩过，已清场）。
- **调试打印勿切断跨行语句**：CN 调用实参常跨多行，往「函数 定义行后」插打印安全，往函数体
  内逐行插会把 `设置(整64(..),\n 整64(..))` 切断报「预期 ')'」（本轮三次返工）。
- **探针/供给入口必须命名 主.cn**（lessons 8.5 权重）：否则函数被静默过滤、cn_main 缺失。
- **E2E 已验收文件冻结**：新强制规则不得用 Error 打爆既有用例（本轮「拥有式字段须声明析构」
  先 Error 打爆 58，降 Warning 解决）。
- **宿主与 v2 的容器字段模型必须同向**：本轮三段返工（内联→p17 修→p24 证伪→回退指针槽）的
  教训：改模型前先查 byRef/RAII/静态槽三套机制都默认哪套模型。
- 工具：本机无 gdb；定位编译器 UB 用 ASAN build（注意上条污染坑）；定位 v2p 运行崩用「插打印
  二分」（v2 组件是源码不是冻结测试，可临时插打印，**事后必须 git checkout 还原**）。
