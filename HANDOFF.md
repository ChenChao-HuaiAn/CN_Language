# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十轮（**深度机 linux-x86_64**）——**80-a v2 池索引 0
哨兵槽**（79-a 登记的「缺陷 A：v2 内置构造器识别依赖文件含结构体声明」销项；真根因
实证为**池索引 0 哨兵冲突**，以 rustc `NodeId` 0 保留同款的哨兵槽根治，v2 树仅改
2 文件、1 行代码 + 1 个常量）。验证链全绿，固定点新口径 **309133**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 上一轮「下一轮任务 #1：v2 内置构造器识别缺陷」。用户令：遵守
   AGENTS.md 第3项纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **复现与根因三步收敛**（探针 P10b/P10c + 诊断注入 dbg/dbg2/dbg3 + 变体矩阵 V1~V6）：
   - 现象：`结果<字符串,整32>` 构造器 `错误(5)` 在「全文件无结构体声明」时生成
     `call _错误` → 链接失败。
   - 二分对照：加**任意**前置节点（结构体声明/垫语句/字符串声明）缺陷即消失——
     「结构体声明」是掩盖变量。
   - **真根因 = 池索引 0 哨兵冲突**：入口文件**首个入池节点**（首个函数体首条语句
     的最早叶子，如 `返回 错误(5);` 的 错误）恰为被调标识符时 `子起点 == 0`；语义层
     `检查调用表达式`（`调用起点 > 0`）与 IR 层 `生成调用` 三特判（构造器/转移/类型
     大小，`n.子起点 > 0`）把 0 当「无」哨兵 → 识别整段跳过。
   - 诊断实证：P10c 打印 `调用名=[错误] 附加=1 语境=63`，P10b 同位置**无该行**。

3. **可达性判定**（决定修复面）：0 **可达**仅调用族（被调名是首次出现的新引用、
   实参可为字面量/类型名）；下标/成员对象、赋值左值末、二元左末的索引**必为已声明
   变量**（其节点必在前）或右值起点 ≥1 ⇒ 0 不可达（P23/P26/P27 实证不可构造）。
   全树审计 `子起点 > 0` 23 处 + `子起点 <= 0` 4 处 + `附加 <= 0` 1 处；
   `池.元素(0)` 直接访问与全池遍历**零命中**（哨兵槽可行性前提）。

4. **三案裁决（乙实施，留痕 plans/020 第十二节）**：甲（24 处守卫逐处 `>= 0`）—
   隐患留存（新代码写 `> 0` 即复发）落选；**乙＝池索引 0 哨兵槽**（`节点_哨兵`，
   类型 0=未使用）——真实节点索引恒 ≥1，全树 `> 0` 与 `>= 0` **两写法同时正确**、
   零处守卫改动、零运行时开销；丙（池基址改 1 起）改动面大落选。

5. **实施（v2 树 2 文件）**：`IR/IR常量.cn` 新增 `常量 节点_哨兵 = 0` + 协议注释；
   `主.cn` 池创建后 `池追加(池, 建节点(节点_哨兵, 0, 0, 0, 0))`。

6. **首节点形态矩阵实测**（HEAD↔修复全链）：错误/正常/某些（P20/P21：HEAD 链接失败
   `_正常`/`_某些` → 修复退出码 0）、类型大小（P24：HEAD `_类型大小` → 修复 0）、
   普通调用（P25 两侧均通过=该路径不依赖被调索引，作回归对照）。

7. **防线**（探针转正三件套 + 反证）：E2E **231_v2_首节点构造器形态**（校验和 31）+
   **232_v2_首节点类型大小形态**（7）+ **233_v2_首节点构造器语境拒绝**（负测）；
   **反证实证**：stash 回退修复重建 v2p → 231/232 链接失败、233 v2p 静默放行退出码 0
   （三项全 FAIL）；恢复后三项全 PASS。

8. **文档五件套**：plans/019（跟踪表行）+ plans/020（第十二节 + 79-a 缺陷 A 条目
   标注已修复 + 矩阵登记更新）+ lessons（第八十轮段，权重 8，预防 ㊴~㊸ + 79-a 段
   条目标注已修复）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **容器元素 = 含串字段结构体专项**（矩阵靶子，79-a 边界）：入容器位深拷（现仅标记
   源为字段污染）+ 容器元素字段串释放面（运行时辅助按元素步长+字段偏移平铺/链游）；
   销项后 B35 字段容器接收者归一化一并收口。
2. **v2 数组类型支持 + 两侧数组元素所有权**（矩阵 #15）：宿主侧数组元素持串无 RAII
   （靶子）；v2 语法层整体缺口（`类型[N]` 声明/初始化列表/下标读写）。
3. **plans/020 矩阵余项**：字符串键（`映射<字符串,...>`）支持轮；集合<有析构类>
   删除前移的旧对象析构（既有边界）。
4. **注释口径清理（本轮新登记，低风险）**：①`语义/语义检查.cn` 推导二元类型ID 注释
   「子起点=右操作数起点、左末=子起点-1」与实际字段协议（`子起点`=右值/右操作数起点，
   实测 dbg3 确认）需复核措辞；②`IR/常量 节点_赋值 = 23` 注释「子起点=左值」实为
   **右值起点**（IR/生成语句 用 `生成左值地址(池, 附加, 子起点)` 佐证）——协议注释与
   实现不符会让后续 AI 误判，须以实测为准修正。
5. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全
   区变硬错误）；v2 诊断无行号（架构级）。
6. **跨机轮**：arm64（单位机麒麟）复验与 E2E 231/232/233（linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）——哨兵槽改动后重建（E2E 缓存指纹自动处理）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 260 用例 256 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 309133 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`
> （p71.s/s71.s/cn_self71），并发会踩出假警（串行复跑正常）。
> **全量 E2E 与手工 v2p/固定点操作不得并发**：E2E 运行中重建 v2p → `PermissionError`。
> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64`
> 后须同时删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p。
> **反证手法（本轮实证有效）**：`git stash push -- CN语言编译器v2` → 跑用例（应 FAIL）
> → `git stash pop`（E2E 指纹变化自动重建 v2p，无需手工删缓存键）。

## 四、本轮踩坑（已入 lessons 第八十轮段）

- **㊴ 「0 当哨兵」先证「0 不可达」；修法优先哨兵槽而非逐处改守卫**：索引域用 `> 0`
  判「有效」时，先问「0 是不是合法值」。根治=让 0 不可达（一处改动全族自愈，两种
  写法同时正确）；rustc `NodeId` 0 保留同款。
- **㊵ 「有 X 就正常、无 X 就失败」先做单变量二分再读代码**：X 可能是**时序副作用**
  （本例=结构体声明把首叶节点推离池索引 0）；按「X 的语义」猜根因会修错方向
  （79-a 的「构造器识别依赖结构体声明」即误判）。
- **㊶ 首节点/首元素形态用例的构造纪律（反证揪出）**：变量声明语句的**类型节点先入池**
  会抢占索引 0（232 首版因此未命中——反证跑出 PASS 暴露用例无效）；函数参数同样入池；
  可达形态只有「无前置依赖的调用」。**此类用例写完必须跑反证**。
- **㊷ 同族守卫修复前先做可达性判定，按可达面选方案**：不是所有 `> 0` 都是活缺陷；
  判定依据=字段协议**实测**（注释与实现不符时以实测为准）。
- **㊸ 诊断注入走既有诊断通道**：语义层无 IO 导入时 `表诊断(诊断, "[dbg] ...")` 直接进
  `[sem5] 正确诊断:` 输出流（无条件打印），零依赖定位；定位后立刻删除（判据 `git diff` 为空）。

## 五、诚实边界

- **转移/下标/成员形态的索引 0 在语言层面不可构造**（对象/实参必为已声明变量 ⇒ 索引
  ≥1）：修复对它们为「写法脆弱性消除」而非「活缺陷修复」，未加单独 E2E（哨兵槽使其
  自动正确）。
- **普通函数调用形态修复前也正常**（IR 普通调用路径不依赖被调索引）：仅作回归对照。
- **容器元素=含串字段结构体**：入容器位标记源为字段污染（元素浅拷共享 + 元素字段串
  释放面未开=靶子）；深拷 + 元素字段释放随容器元素专项（下一轮 #1）。
- **数组元素持串**：宿主靶子维持；**v2 数组类型整体缺口**（独立立项）。
- **结构体容器字段拷贝**：浅拷共享对象指针（双宿主/v2 同缺=既有边界）。
- **结构体按值传参**浅拷（参数=借出视图、被调方不释放；被调方写参数字段=归一化复制
  落堆=泄漏，宁漏勿错）。
- v2 `结果值偏移` 两档近似（整32/其余）：结构体载荷边界（宿主正确）。
- 协议注释与实现不符两处（二元/赋值节点 — 下一轮 #4 清理）。
- win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s **309133 行**、cn_self71）。
- 探针（/tmp/p80probe，不入库）：P10b/P10c（立案对照）、P20/P21/P24/P25（首节点形态
  矩阵）、P23/P26/P27（不可构造形态判定）、V1~V6（二分变体矩阵）、work/（手工
  编译+汇编+链接+运行验证工作区）。
- E2E 用例：`tests/e2e/231_v2_首节点构造器形态/`、`232_v2_首节点类型大小形态/`、
  `233_v2_首节点构造器语境拒绝/`（各含 主.cn + 主.expected + v2闭环.txt）。
- v2 修改文件（本轮全部改动）：`CN语言编译器v2/IR/IR常量.cn`（`节点_哨兵` + 协议注释）、
  `CN语言编译器v2/主.cn`（池哨兵插入）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（第十二节 80-a + 矩阵登记；每轮触碰
  所有权/识别语义必须同步更新）。
