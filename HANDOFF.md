# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十四轮收官（深度系统 x86_64）——**plans/017 呈报①方案A 第六轮：第 17 枚收口（根因=第 18 枚【根因级】结构体整体赋值单槽浅拷根治）+ 九探针对拍 9/9 全绿里程碑**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、第 17 枚（mapref2 引用槽判定 miss）收口——根因为第 18 枚
1. **定位（[d17] 插桩对拍四轮夹逼）**：登记序/查询序对拍——v2self **登记行整条缺失**（非映射运行时损坏）→ [d17d] 参数收集入口池 dump **正确**（槽0附加=256 双方一致）→ [d17g] `参数类型节点 = 参数类型候选` **结构体赋值后左值附加原值残留**（候选=256、赋值后=0）。
2. **根因（min17 最小探针）**：嵌套块内 24B `甲 = 乙`——宿主 r=99 全字段拷贝，v2 产物 r=1256。asm 实证：v2 赋值路径只发 `IR_加载→IR_存储` **单槽 8 字节浅拷**（首槽 类型+文本 以外字段残留）；声明位三形态已有 IR_结构体拷贝、唯赋值位漏。
3. **全链传导**：参数收集浅拷→`附加>=256` 恒假→引用参数不登记 引用参数槽们→`表.获取(k)` 漏发 op=44 双层解引用→链查找 this=0 错误码3（v2self 第 9 层崩点原发点）。

### 二、修复（IR生成.cn 赋值分支一处，63 行，Rust 准绳）
- 结构体类型左值赋值 → `IR_取地址/IR_加载`（引用槽→装载绑定对象地址、其余→取槽地址分流）×2 + `IR_结构体拷贝(类型=结构大小)` 整体字节拷贝（POD=逐字节 Copy、memmove 语义零开销）。
- 右值非同型变量标识符/大小不一致/槽缺失 → `IR层诊断` fail fast 拒绝（绝不静默浅拷=错误产物）。

### 三、验证（全绿）
- min17 宿主/v2 双绿（r=99）；**v2self 重编后编译 mapref2 与 v2p 逐字节一致（diff=0）+ 运行 引用和=99 与宿主一致**。
- **九探针 v2p/v2self 对拍 9/9 PASS**（min1/f2/f3/alias/vieww/retown/elecalc/freg/mapref2——上轮 8/9）；min1 产物逐字节一致保持。
- 新增 E2E `156_v2_结构体整体赋值`（嵌套块整体赋值三字段断言，宿主 sanity+v2 编译运行双绿）。
- 门禁：构建零警告 + 单测 1240/1240 + E2E 158 用例 131 过/0 败/27 平台跳过。

## 下一步
1. **呈报③方案A**（IR 名带参数签名·重载消歧机制）——跨文件同名函数重载缺口（plans/017 立项待裁）。
2. **T3/T4**（run_e2e.py 的 v2 闭环参数化 / 宿主构建对拍）随 v2self 收口推进。
3. 里程碑注记：v2self 自举产物**首次与 v2p 全探针逐字节/行为一致**——第 7~18 枚 12 层缺陷链全部闭环。

## 灰色点披露
- IR容器.cn 单组件子链冒烟 exit=1 系既有状态（第二十二轮已披露，主链全绿，未立案）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径；**asm 输出 target/v2asm.s 亦相对 cwd**）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- 本轮对拍脚本与产物在 /tmp/v2dbg/p17/（不入库）；干净 v2p=target/dbg/v2p_fix、v2self=/tmp/v2dbg/p17/v2self_fix。

## 踩过的坑（本轮，已入 lessons 权重 10）
- **静默浅拷传导三层才显形**：残缺拷贝比崩溃更危险——修「按值语义」任一点须同 grep 其余同语义位点（声明/赋值/传参/返回逐枚举审计）。
- **对拍脚本假 PASS 两陷阱**：asm 相对 cwd（项目根跑+即时 cp）；容器探针需宿主同源 ${p}_h_nom.o 补容器实现。
- **[d17g] 并列打印法**（赋值后左值 vs 右值）是拷贝类缺陷最快收口形态。
