# HANDOFF 交接文档

**交接时间**: 2026-09-11 第六十八~六十九轮**双机并行合并**——深度机（linux-x86_64）第六十八轮：**A2（阶段4' 第三批正文「返回拥有类型区分」）全量落地+自举缺陷当轮根治**（方案甲 用户批准）：`0ed437b`（轮1 宿主）+`83b26ac`（轮2 v2 侧）+`c668919`（缺陷根治），**验证链全绿，固定点新口径 262313**；单位机（麒麟 ARM64）第六十八轮：**跨机收口全链验证**（228 用例 223 过+200~209 首验全过+ARM64 并行首验+固定点 347685+对拍 29/29+运行级 3/3）+68-a 立案；第六十九轮：**68-a 用户裁决方案A 实施闭环**（E2E runner rt objs 指纹缓存根治 cargo fingerprint 同构+三态确定性验证+全量复跑对齐 223 过）。**除既有挂账（win 并行隔离/194 待批/手动释放重叠面）外无新增缺陷。**

---

## 一、两轮做了什么（写给无上下文的新会话）

1. **A2 语义两轮落地（深度机）**：宿主四件套（retOwnedString 决议标记/登记白名单∪返回类型契约/返回位借用拒绝[泛型豁免]/返回位字面量拥有化/字符*→字符串收紧/赋值位借出拒绝）+驻留文本改字符*+三树迁移（v2 96/v1 18/tests 5）+v2 侧同款四件套+210/210 返回借用拒绝/210_v2 三用例注册。
2. **自举第二代缺陷根治（深度机，方案甲 用户裁决）**：插桩实证→根因=「字符串入结构体/容器浅共享×源 RAII 释放=悬垂」两处（IR签名 收集类型参数序 出.追加→泛型序串悬垂→T 替换失效→45 错；IR容器 查方法签名 出.返回文本/出.参数串 同族）——第一代堆未复用=绿假象；存入显式复制修复。
3. **arm64 跨机收口（单位机第六十八轮）**：第六十二~六十七轮六轮改动全量门禁复验（228 用例 223 过与深度机对齐）+200~209 十八用例双通道 arm64 首验全过+ARM64 并行 --jobs 8 首验通过+固定点 arm64 重锚 347685 行+对拍 29/29+运行级 3/3；立案 68-a rt objs 新鲜度缺陷。
4. **68-a 方案A 实施（单位机第六十九轮，tests/e2e/run_e2e.py）**：新增 `计算运行时构建指纹`（src/runtime/*.cpp 路径/大小/mtime+cxx+编译标志串——命令行纳入指纹，与 v2p 指纹同构）；rt objs 段「缺则现编」改指纹制（`target/audit2/rt_build_key.txt` 不存在或指纹不符→**整批原子重编** 10 模块+写键；命中零重编纯只读；win 路径不经此函数零影响）+三态确定性验证+全量 E2E 复跑 228 用例 223 过对齐。

## 二、下一轮任务（按序）

1. **手动释放重叠面收尾批（深度机主战场，本轮进行）**：tests 18 文件 ~106 处 字符串释放（A2 登记后=双重释放，靠 cn_free_tracked 防御兜底——语义债务非设计意图）+4 函数体迁移（56 读名称/52 读取书名·读取ISBN·校验编号）。
2. 呈报清单余项：194 单行批准（`返回 整32(*q);` 改写方案已呈报）；win 平台 v2 并行隔离（家机轮）；第二层第三批收口时点。
3. 阶段4' 收尾评估：嵌套块声明块出口析构扩展（第三批残余）；阶段5 等并发库立项；NLL 等反馈期数据。
4. 单位机（arm64）下一跨机轮=深度机 A2/收尾批轮次的 arm64 复验+固定点复锚（347685 口径，v2 树变更预期增长）。

## 三、验证链（分机复现口径）

```bash
# 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 231 用例 226 过（194 既有）
bash target/p68x64l/run_chain68.sh   # 锚定链：fix_p 262313 →固定点一致+绑定自检+运行级 3/3（208v2/119/173）
bash target/p68x64l/cmp29_68.sh      # 对拍 29/29

# 单位机 麒麟 ARM64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告（grep 需排除文件名误匹配 error_*.cpp）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --jobs 8   # E2E 全量并行
# rt objs 指纹缓存（68-a）：target/audit2/rt_build_key.txt（src/runtime 变化自动整批重编；--verbose 显示「rt objs 缓存命中」）
bash target/probe68/chain68.sh  # 锚定链：fix_p 347685 →固定点一致（rt objs 已现编于 probe68/rt/——独立于 audit2 缓存）
bash target/probe68/runlevel68.sh # 运行级 3/3
bash target/probe68/cmp29_68.sh   # 对拍 29/29
```

## 四、两轮踩坑（已入 lessons 第六十八轮权重8+第六十九轮权重6）

- **「字符串入结构体/容器浅共享×源 RAII 释放=悬垂」家族四实例全修**：模块路径们 静态向量追加/IR容器 段 追加/查方法签名 出参字段/收集类型参数序 出参追加——**纪律：字符串存入结构体字段或容器一律显式 字符串复制（容器 move 语义随后续批次）**；第一代堆未复用=固定点绿假象（悬垂读运气），自举第二代是天然的压力测试。
- 未提交工作树上的 `git checkout --` = 迁移成果全失（lessons 权重8）；构建验证=rc+错误串双查。
- **分钟级 ls 时间戳掩盖同分钟内重编**：touch 验证指纹触发时「obj 时间不变」假象误判未触发——秒级（`--time-style=+%T`）+键值对比后才确证；缓存/增量逻辑验证禁用分钟级时间戳下结论。
- lessons 编辑纪律：Edit old_string 必须与文件原文逐字核对（改前先 grep 原文）。
- 01_hello 入口实名 hello.cn 非 主.cn；锚定链与 E2E 不可并行（竞写 target/v2asm.s）。

## 五、诚实边界

- v2 契约登记按纯名查（重载族返回异型=近似，对齐宿主首候选）；字符* 类型码与字符串同码（字符* 函数返回字面量=多一次 copy 泄漏安全）；泛型体内返回位/赋值位豁免（T 来源=容器元素借用）。
- 参数/静态维持借用；返回 字符串 变量于 字符* 函数=移出后泄漏（安全方向，随 NLL）。
- 手动释放重叠面（18 文件 106 处）防御兜底——收尾批清理（本轮进行）。
- 第二批遗留「二分禁用」注释（生成语句.cn 引用返回路径字符串释放——上轮既有，已随第六十八轮修复链复审无害：引用返回路径无字符串登记面）。
- win-x64 v2 并行未隔离（跨机轮）；194 待批维持；01_hello 不可作 v2 运行级载体（打印形态 v2 不支持=既有边界，208v2 替代）。

## 六、关键产物位置（target/ 不入库）

- 深度机：target/p68x64l/（run_chain68.sh、cmp29_68.sh、v2p68、cn_self68、fix_p.s/fix_s.s 262313 行）。
- 单位机：target/probe68/（chain68.sh、map_check68.py、cmp29_68.sh、runlevel68.sh、v2p68、cn_self68、fix_p.s/fix_s.s 347685 行、map68.txt、rt/ 10 objs 现编）；日志 /tmp/e2e69.log、/tmp/e2e68.log、/tmp/probe68a_run.log。
