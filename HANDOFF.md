# HANDOFF 交接文档

**交接时间**: 2026-09-09 第三十九轮（**深度系统 x86_64，纯文档轮**）——**specs/08 目录包语义条文批注收口**（第三十四轮挂账，顺延四轮后执行）：specs/08-模块系统.md 新增 **5.5 目录包（阶段目录包形态）** 立规（物理形态/包根=目录名/成员聚合声明序=加载序/自包导入合法非循环依赖/编译单元不变式=包上下文恢复/非成员文件封闭树/链接键实现自洽唯一公式）+ 第二节补自包导入规则行；只写已实现已裁决语义（第三十四轮形态裁决+第三十八轮语义钉死，组件对拍 30/30 与发射级等价实证背书），零代码改动。

---

## 一、上一轮（第三十八轮）根治内容摘要（详见 plans/014 状态表与 git 87be8d6）

挂账1「单文件入口 5 组件文件编译失败」全链根治：三缺陷同源（模块名=f(入口目录,文件路径)漂移）——①topoSort 假环 ②撞名成员被名字去重吞掉 ③兄弟不可达。宿主 C++ 与 v2 CN 双侧同构根治（pkgRoot 结构归属/findByPath 路径索引/包名::主干命名/restorePackageContext 包上下文恢复/末段匹配兼容/主() 返回值消费+契约重整）。组件对拍 30/30 全量逐字节+固定点+发射级等价。**下轮接手若需背景，读本文件 git 87be8d6 版本的第一/二/五节**。

---

## 一、根因与根治（写给无上下文的新会话）

### 三缺陷同源：模块名 = f(入口目录, 文件路径) 的结构性脆弱

同一文件在不同入口目录下模块名不同（整体构建主.cn 时 IR/IR布局.cn → `IR::IR布局`；单文件入口 语义检查.cn 时 → `IR布局` 主干名）。宿主模块系统的三处机制全部依赖命名形态 → 行为随入口漂移：

1. **假环**：目录包成员 `导入 自包名`（IR布局 导入 IR）+ 包根挂载边（IR→IR布局）构成拓扑环。整体构建**侥幸不爆**：topoSort 沿挂载边文本 `IR布局` 查模块表，带前缀节点名查不到被当「未加载」忽略；单文件形态两边恰好都能查到 → 「模块循环依赖: IR -> IR布局 -> IR」。
2. **撞名成员丢失**：代码生成/包.cn 声明 `模块 代码生成;`，单文件形态下成员主干名=`代码生成` 与包根目录名撞名 → loadModuleTree 的 findModule 去重把成员**永久吞掉**（永不加载）。
3. **兄弟不可达**（上轮挂账原文）：包内兄弟符号经合并全局池解析（无显式 导入），命令行入口绕过包根挂载链单独加载 → 兄弟文件不在依赖闭包 → 语义检查.cn 入口报「未声明函数: 取语义诊断」等 137 错（**v2 侧**；v2p/cn_self 组件对拍即此形态）。

v2 编译器（CN 实现）按**文件路径**去重 + 递归后序加载（无独立拓扑阶段），天然免疫缺陷①②；缺陷③两侧同病——所以两实现「包语义」并不同构，双侧都要修。

### 根治（rustc crate 模型同构：成员引用本 crate 符号无需跨 crate 依赖边）

**宿主 C++**（src/cn_compiler/）：
- `module.hpp/cpp`：ModuleUnit 加 `pkgRoot`（所属包根名，结构归属非文本推断）+ `cargoPkgRoot`（货舱依赖包根标记）；ModuleGraph 加 `findByPath` 路径索引 O(1) 去重（源文件=模块身份唯一真相，v2 路径ID 同构）。
- `module.cpp dfsTopo`：自包导入边豁免（`dep == unit->pkgRoot` 跳过）。
- `driver_module.cpp`：loadModuleTree 加 pkgPrefix/cargoDepRoot 参数——项目内目录包根挂载成员统一 `包名::主干` 命名（与整体形态逐字节对齐；货舱依赖包豁免保持 bare，E2E 47 符号面零变化）；自包豁免条件加 `dep == cur->pkgRoot`；**restorePackageContext** 包上下文恢复（入口所在目录 包.cn 声明入口主干挂载 → 入口先载/包根后聚合，兄弟补入依赖闭包；入口同文件经 findByPath 不双载；非成员文件不触发防误载）。
- `semantic.cpp`：决议末段匹配兼容包前缀条目名（v1 组件 `语义::语义::内置::行类型` 的末段兜底按条目名末段对齐——前缀化后 `语义::内置` 末段=`内置` 仍命中）。

**自举 v2 CN**（CN语言编译器v2/主.cn）：
- **恢复包上下文** 函数（宿主同构）：分词+扫描导入 只取包根挂载清单；主干==挂载名 才触发（`字符串比较` **真=相等**——首轮误加 `!` 变「不等即触发」，见踩坑）；编译文件 契约转换（0=败 1=成 → 0=成功 非0=失败）。
- **主() 返回值消费**：`入口结果 != 0 → 返回`——此前 `忽略编译` 丢弃返回值，入口不存在 rc=2 假绿（诊断打印了但空跑产空 asm）；初始化并编译入口 尾部 `返回 编译结果`（内部契约 1=成功）改 `返回 0`，本函数契约 0=成功/1=货舱失败/2=显式入口不存在。

## 二、验证（门禁全绿）

- 单测 **1251/1251**；E2E linux-x86_64 **177 用例 173 过/0 败/4 跳**（基线一致零回归）。
- **组件对拍 30/30**：v2 全部 30 个组件文件（6 包根+24 成员）分别以 v2p/cn_self 为编译器入口编译，**asm 逐字节一致**（上轮 26 项基础上扩展为全量，挂账 5 文件归零）。
- **固定点 fix_p ≡ fix_s 逐字节**。
- **cn_self 运行级**：hello 单文件 rc=0 / 119 多文件 rc=14 / 173 包形态 rc=173。
- **发射级等价**：git worktree 旧宿主（ca3c68c）与新宿主编译**同一份** v2 源码，产物逐字节一致——宿主改动对编译器语义零影响实锤。
- **失败语义双侧**：显式入口不存在 → v2p 与 cn_self 均 rc=2（且不产 asm）。
- **撞名成员加载实证**：代码生成/包.cn 入口产物含 `包::代码生成$` 前缀函数符号（修复前成员被吞永不编译）。

## 三、挂账（按优先级）

1. **跨机轮**：win/arm64 全量运行级复验（第三十八轮宿主模块系统+决议末段匹配改动，三后端共享 IR 层之上；单位机同步后跑 arm64 门禁）。
2. abi辅助.cn（v1 遗留探针 13 行）转正或删除。
3. v2 结构体构造字面量 `记录{...}` 语法缺口（随语法覆盖轮）。

## 四、验证链（下轮接手先跑）

```bash
uname -m                                    # 环境识别（x86_64=深度机 / aarch64=单位机）
cmake --build target/build -j8 && ./target/cn_unit_tests 2>&1 | tail -1
python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn 2>&1 | tail -3
# v2p 构建（深度机门禁=linux-x86_64；单位机=linux-arm64）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/v2p_new
# cn_self：v2p 产 target/v2asm.s → as + g++ 链接（rt objs 用 g++ -c -std=c++17
#   -fno-exceptions -fno-rtti -DCNRT_LINUX_MAIN -Isrc 现编；cn_self.o 与 v2p_new.o
#   在前，-Wl,-z,muldefs 借链容器符号）
# 组件对拍 30 项：for f in $(find CN语言编译器v2 -name "*.cn" ! -name "主.cn");
#   v2p_new/$f 与 cn_self/$f 各产 target/v2asm.s 后 cmp
# 单文件入口组件编译（本轮根治面）：./target/cn compile CN语言编译器v2/语义/语义检查.cn
#   --target linux-x86_64 --output target/t1.s（compile 模式不链接——组件无 主()）
# 失败语义：./target/v2p_new 不存在.cn linux-x86_64 → rc=2
```

## 五、本轮踩坑（已入 lessons.md 权重头部）

1. **模块身份靠路径文本推断=行为漂移之母**（权重9）：模块名随 entryDir 形态漂移时，前缀豁免/边文本匹配/名字去重全部漂移——「侥幸不爆」的整体形态换入口即爆；身份判定用结构关系（归属字段/路径索引）。
2. **「忽略」型返回值丢弃是契约冲突温床**（权重8）：主() 丢弃 初始化并编译入口 返回值让「入口不存在 rc=2」假绿存活至今；三层三契约（编译文件 0=败/初始化透传/主() 按 0=成功）在消费瞬间连环爆——返回值契约必须在函数头声明、跨层透传显式转换。
3. **CN `字符串比较` 真=相等**（权重6，非 strcmp 的 0=相等）：宿主 C++ `==` 翻译成 CN 条件时 `!` 最易写反（本轮恢复触发条件反写，靠 173/词法包根二分定位）；写条件以仓内同款用法为锚（主() --verbose 判定）。
4. **假绿的连锁性**：布尔条件错+返回值不消费两层叠加时对拍「全绿」不可信——修复一层让另一层暴露，分层修每层全量复验。
