# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-14——**第一百六十四轮（164-a）：A4 联合体拥有型成员收口（plans/023 §十二 方案 A+D）**。
本会话累计：161-a（A8 冒号后置移除）+ 162-a（A5+A3 关键字体系实施；与并行会话 163-a 78/79 迁移合并后
302 用例零失败）+ **164-a（A4 联合体收口）**。**下一步=A4 收尾已毕 → C16 规范落盘（前置=波 6 终验——缺
深度机复验）→ 波 3 剩余（149-a IR diff）→ 跨机轮（关键字契约 + A4 + 78_v2/79_v2 linux 首验）**。

### 一、本轮做了什么（164-a，写给无上下文的新会话）

1. **语义层成员类型限定**（方案 A）：`isTriviallyCopyable` 递归判定（标量/指针/数组/结果可选实参/
   纯标量聚合）；拥有型（字符串/容器类/类对象/含拥有型聚合）→ 编译期硬错误（诊断含 Rust 对照与
   逃生舱提示）；**合成联合体 `结果联合$` 豁免**（computeLayout↔typeSizeOf 迭代期追加——时序陷阱）。
2. **`手动释放` 逃生舱**（方案 D）：上下文词（联合体成员位识别，不占命名空间）；语义层放行；
   编译器不生成自动释放与写入归一化（清旧）；用户在 不安全 函数 内显式释放（ManuallyDrop 对标）。
3. **IR viaUnion 标记方案**（核心教训）：`OwnedStrField.viaUnion` ——释放面（`emitOwnedStrFieldFreesAt`/
   `emitOwnedStrFieldPreFree`）跳过、**新值写入/深拷面保留**（PostCopy 不跳过）+ **成员赋值位**
   （ir_expr_assign 的 `isOwnedFieldSlot` 分支）联合体目标跳过旧值释放——P13 泄漏/P14 误释放 UAF
   全路径结构性消除。**（失败教训：首发「collect 早退」把写入一起砍了 → asm 铁证 → 条目级精化）**
4. **v2 侧同构**：语法层 `手动释放` 识别 + 联合体键基/标注登记（新静态表）+ 语义层检查
   （`可平凡复制v2`：标量/指针/数组/结构体〔资源字段数+串字段数==0〕）+ 表诊断。
5. **用例**：275（宿主负测·子串「联合体成员」）/ 276（宿主正测·手动释放残留 0）/ 277_v2（v2 负测，
   诊断固化）/ 278（双编译对照·宿主/v2 同值 9）。
6. **反证双命中**：回退检查+早退 → 含串联合体放行（复现方案 A 前）+ P13 探针残留 1（覆写句柄泄漏精确复现）。
7. **规范**：plans/001 §3.5a「联合体成员类型」新增（批准文本落盘；plans/023 §12.4-6 的「§5.3」为
   文档编号偏差——实际落 §3.5a 复合类型族）。

### 二、门禁（本轮实测）

零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过/0 败/0 跳过** + 锚定链重锚 **410416**
（v2 树 A4 增量；fix_p≡fix_s 逐字节自洽）+ 对拍 **44/44** + 运行级 **3/3**。

### 三、下一步（新会话按序）

1. **C16 规范落盘（165-a）**：前置=波 6 终验（缺**深度机 linux-x86_64** 复验——arm64 已 158-a、win 已 157-a）；
   内容=plans/023 §九 → plans/001 §5.3 + cn-language-spec + spec 03 循环 EBNF 细化 + §8.4 广播。
   **注意**：本会话 161~164-a 的 v2 树/契约变更（关键字 46 词、A4 联合体）也待三机复跑（看板通告）。
2. **跨机轮（首要并行）**：深度机/单位机拉取后全量复跑——关键字契约（89/71/72/272/273/274 口径）+
   A4 用例（275~278）+ 78_v2/79_v2 linux 分支首验；预期 arm64 锚定链重锚（不跨后端比）。
3. **波 3 剩余**：149-a IR diff（uF 根因）→ 泛化 → 元素级深拷（前置=波 4）→ H7/H11 收口。
4. 排班余项：§3-C C2（跨语句 NLL）/C16；D1 函数级残余 12 超百行。

### 四、诚实边界

- **A4 的 v2 判定对「结果/可选」类型成员保守拒绝**（未识别→假）——宿主放行-v2 拒绝的差异面登记
  （v2 树/stdlib 5 处联合体全纯标量，零暴露；将来若需对齐，v2 需补结果/可选实参文本解析）；
- **`手动释放` 成员的「覆写旧值」=用户责任**（不自动释放——方案 A 删除写入归一化的语义定义；
  用例须遵守「一次一活跃成员」）；
- `\` 转义序列语义（162-a 实测 `"A\nB"` 双反斜杠+n=换行）**规范未定义**——规范空白登记（非缺陷）；
- github 镜像本会话全程双推成功（b502400/281abf3 后）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第一百零八轮（**深度机 linux-x86_64**）——**108-a D5 剩余面固化**：
赋值位结果/可选 构造器保守拒绝负测（264_v2 门禁，零源码改动）；D5 面完整处理链收口
（107-a 修复 + 108-a 固化 + 架构边界登记）。本会话累计 93-a~108-a 共 16 个开发轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **决策**：107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——
   完整实现需「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需
   联合体条件分支=面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ **保守拒绝维持 +
   负测固化**（不为其打单形态补丁；若将来真实需求出现，按已定位路径专项立项）。
2. **门禁**：E2E **264_v2_赋值位结果构造器拒绝**（v2 负测：诊断锚行「构造器 正常 须用于
   结果/可选 语境（返回位或 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——
   防将来误放行致静默截断/泄漏。
3. **D5 面完整处理链（107-a + 108-a 收口）**：
   | 形态 | 处理 |
   |---|---|
   | `返回 无`（段错误） | 107-a 修复（返回位零初始化；E2E 263 + 反证双件） |
   | `可选<T> r = 无;`（原拒绝） | 107-a 修复（语义放行 + 声明位 IR 零初始化） |
   | `r = 正常(...)` / `r = 无`（赋值位） | 108-a 固化（负测 264）+ 完整实现登记（联合体条件释放设施） |
   | `盒.r.正常`/`盒.r.值`（嵌套成员链） | 两侧一致保守=诚实边界 |
4. 全量细节：`plans/019` 第一百零八轮行 + `plans/020` **第四十七节** + `lessons.md` 108-a 段
   （预防 153）。

### 二、本轮验证（零源码改动）

- v2p md5 `b6d8c387c86a066d4c1b1e62b710756c`（与 107-a 一致）；宿主 md5
  `f1484d79b0e5e85d64550b144842208a`（零改动）
- 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）
- 无需锚定链/对拍（v2 源码未变；107-a 终值：固定点 387693 行 / 对拍 34/34）

### 三、下一轮任务（按序）

1. **D1 行数存量整改（长期，本机可实施主要面）**：宿主余 7 文件（ir_expr 2798 / semantic 2101 /
   semantic_expr 1889 / ir_oop 1380 / ir_oop_call 1154 / semantic_call 1092 / ast.hpp 1032）+
   v2 3 文件 + 19 超百行函数——按"每轮 1~2 文件"滚动（验证口径=v2p md5/固定点不变 + 全量零回归；
   族边界勘定纪律见 §四⑬）。
2. **联合体条件释放设施专项（登记）**：结果/可选 全释放面（值字段条件释放）——赋值位覆盖写、
   结果/可选 结构体字段、结果/可选 容器元素等形态在此设施上统一收口（84-e 边界 + 107-a/108-a
   定位的实施路径）。
3. **可选重构（登记）**：v2「无」的整数字面量表示（彻底方案=新节点类型「空字面量」）。
4. **登记面余项**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表缺位；
   D3（未初始化槽 preFree 观察项）/ D4（静态槽不登记自动释放=设计选择）维持登记；C2/C13=106-a
   评估完成（维持排程/登记）。
5. **跨机轮（家机 win-x64 / 单位机 arm64）**：**93-a~108-a 全量复跑**（两侧编译器已多次变更需重建；
   **注意 103-a 契约变更**：v2 诊断带行号 → 迁移后的 .expected 为准；**107-a v2 产物已变**：
   win/arm64 侧 v2p 重建即可）+ 家机立案 **148_v2 win 崩溃缺陷**（三方案待裁决）。
6. **呈报清单余项**：B3、B4、B5（待用户裁决）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；**对拍/锚定链必须独立串行且不得相互交错**（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p107x64l/run_chain107.sh` + **组件对拍** `bash target/p107x64l/cmp_107.sh`
> （口径标杆=107-a 终值；若后续轮改 v2 源码，按该轮新建 p<N>x64l 目录）。
> **手工 v2p 重建（改 v2 源码后）**：`target/cn build CN语言编译器v2/主.cn --target linux-x86_64
> --output target/audit2/v2p_linuxx64`（同时产 .o；E2E runner 指纹变更会自动重建）。
> **手工 v2 探针链**（/tmp/p92probe `diff101.sh` 逐行 diff；/tmp/p105probe `run_d5.sh <名>`：
> 宿主 build + v2p + as + g++ + 逐行 diff）：cwd 需有 `stdlib` 软链 + `target/` 目录。
> **契约变更提示（103-a）**：v2 语义诊断现带「（行=N）」后缀 → 负测用例的 `.expected` 已迁移
> （20 个）；新增 v2 负测写 expected 时须含行号（可用脚本重跑取实测）。
> **调试/验证纪律（累计 13 条）**：①v2 `IR层诊断` 会中止编译——结论须"干净版复核"；②v2 共享
> 结构体加字段 grep 全部构造点；③元素步进统一 `semantic_->typeSizeOf`；④门禁形态须区分"旧机制
> 巧合正确"；⑤反证缺失位数预计算；⑥类型位后缀消费顺序显式设计；⑦探针先做全形态矩阵；⑧共享
> 推导改动回归同族面；⑨反证未命中先疑"改动冗余"；⑩对拍独立串行；⑪契约类改动（.expected/runner/
> 规范）须 §8.4 广播 + 全量迁移；⑫契约迁移脚本须模拟 runner 的真实调用形态；⑬纯重构类改动先勘
> 定族边界依赖（105-a：static 辅助函数跨族即禁拆该边界；等价性由 v2p md5 单点证据承担）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote；**未配置 `github` 镜像
> remote**，github.com 直连超时——镜像补推需网络可达的机器代做。
```

### 五、诚实边界

- 108-a 为**零源码改动轮**（负测固化 + 决策留痕）：赋值位结果/可选 支持仍为 v2 保守拒绝
  （与宿主差异=已登记等价性缺口，非缺陷）；完整实现需联合体条件释放设施（专项登记）。
- 前轮登记维持（静态槽不登记自动释放 / D1 长期滚动 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位 /
  未初始化槽 preFree 观察项 D3 / `1.5f` 后缀缺口）；
  **B2=93-a、C6=94-a、C7=95-a（+C3）、C10=96-a、C8=97-a、C9=98-a、C11=99-a（+H4）、C12=100-a、
  C1=101-a、C4=102-a（+H5）、D2=103-a、D1 长期滚动（首波 104-a + 第二波 105-a）、
  C2/C13=106-a 评估完成、D5=107-a/108-a 收口（残余=登记边界）**（销项台账见 `plans/021` §4.2）。
- 反证注入均已恢复并以注入前 md5 复核；探针留 /tmp/p105probe、备份 /tmp/p107bak 不入库。

## 单位机 ARM64 节

**最近交接**：2026-09-14——**160-a 文档轮（基线 ebf99ab；源码零改动）**：三项用户批准落盘
（A5 扩围 +4 词 / A8 冒号后置移除 / C16 追加 spec 03 细化）。**实施均由家机认领执行**，
本节即家机实施入口；上轮 159-a（plans/023 漂移修正恢复）见 git ebf99ab。

### 一、本轮做了什么（写给无上下文的新会话）

1. **A5 扩围 +4 词（userselect 批准）**：`公开`/`私有`/`静态`/`常量` 随 A5 同轮上下文化
   （6 词 → 10 词；目标态保留字 **50 → 46**）。落盘四处：`plans/024` §10.5（批准批注 +
   `常量` 判据勘误=**前瞻 1 token**：后随标识符/`:` → 声明、后随运算符 → 表达式；声明位清单
   =顶层＋局部＋for-init；宿主局部常量=真实 isConst 语义 vs v2 防御分支按 整64——实施轮须
   盘点对齐）+ `plans/001` §2.1（总数/子类目〔声明 6→3、常量 4→3〕/上下文清单/变更记录）+
   `plans/021` §3-A A5 行扩围。
2. **新排班 §3-A A8（userselect 批准方案A）**：**冒号后置类型标注移除**。缺陷定性：宿主 3 处
   接受 `名: 类型`（参数 parser.cpp:361 / 变量·常量声明通道 parser.cpp:598-599 / 顶层常量
   parser.cpp:830-833；实测 `函数 加倍(x: 整32)`、`变量 局部: 整32 = …`、`常量 MAX: 整32 = 100`
   全通过），而 spec 全集与 plans/001 均无此语法且 spec 03 明确否决；v2 语法层零支持（双侧分叉）、
   仓库存量零使用（移除零迁移）。实施=移除 3 处 Colon 分支 + 负测锚定（冒号形态必须报错，宿主/v2
   双侧）+ 全链门禁（锚定链预期不动：负测不进既有产物面）。
3. **C16 追加（userselect 批准）**：spec 03 循环 EBNF 细化（init 位声明三形态：`变量`/`常量`/
   类型前缀）随 C16 规范落盘轮一并补写。附实测锚：循环 init 位 `常量` 语义自洽——
   `循环(常量 i = 0; i < 3; i += 1)` 报「不能给常量 'i' 赋值」（更新位拦截在位）。

### 二、家机实施入口（按序）

1. **A8 先行**（冒号移除，独立轮：看板认领 → 移除 3 处 → 双侧负测 → 全链门禁 → 六件套）；
2. **A5 随后**（10 词上下文化；判据不建在待移除形态上；契约广播 §8.4——`89_关键字穷举`
   用例串变更，三机复跑）；
3. **C16**（前置=波 6 终验：plans/023 §九 → plans/001 §5.3 + spec 同步 + 循环 EBNF 细化 +
   契约广播）；
4. 排班余项：A3（关键字清理实施）/A4（联合体收口）/C3 波次残余。

### 三、诚实边界

- 本轮纯文档零源码改动，无门禁面；探针实测（常量三形态/冒号形态/循环常量语义）均以
  `./target/cn check` 临时文件验证后清理，未入库；
- plans/001 §2.1 子类目计数（OOP5 含 `实现` 待移除的历史口径）未在本轮重排——A3/A5 实施轮
  落地时一并清算；
- github 镜像本机无凭据未推（沿 158-a/159-a 口径，下次提交补推）。
