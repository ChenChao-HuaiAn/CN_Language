# HANDOFF 交接文档

**交接时间**: 2026-09-07 第三十一轮上半程收官（单位机 麒麟 ARM64）——**P6b 模块系统启动轮，按用户指令停于「呈报一待终裁」交接点**：第 0 步实证、规范立规、定位收紧诊断、E0255 基建已完成；中途发现一枚独立既有宿主错编缺陷（呈报二，已裁 A′ 待实施）与一处规范-实现语义分歧（呈报一，用户先裁 A → 改选 B → 随即暂停「我还是不懂……到家再接着问 AI」，**终裁待定**）。当前树门禁全绿（单测 1243/1243 + E2E arm64 全量与基线一致）。

---

## 一、呈报一情况说明（**写给用户的通俗版，家机 AI 请先带用户读本节再裁决**）

### 1. 一句话背景

P6b 要给语言立一条规矩：「当两个模块里有同名的东西、你的代码里只写了名字（不带模块前缀）时，编译器该认哪一个」。立规矩时发现：**规格书上写的 `导入 模块::符号` 的一种理解和编译器多年来的实际行为不一致**，需要您裁决听谁的。

### 2. 具体是什么不一致（看例子）

假设有一个库文件 `工具库.cn`，里面有个公开函数 `版本()` 返回 200；您自己的 `主.cn` 里**也**定义了一个 `版本()` 返回 100。现在您在 主.cn 顶部写：

```
导入 工具库::版本;      ← 问题就出在这一行是什么意思
```

**理解一（规格书的理解，也是 Rust 的理解）**：这一行的意思是「把 工具库 里那个叫 版本 的函数，单独搬到我的文件里来用」——搬进来的 `版本` 和您自己定义的 `版本` **撞名了**。Rust 遇到这种撞名直接报编译错误（E0252「名字定义了两次」），逼您改名。这叫「具名绑定」。

**理解二（咱们编译器从 2026-08 模块系统 v2.0 落地以来的实际行为）**：这一行的意思被实现成了「把 工具库 这**整个模块**放进我的通讯录」——之后写 `工具库::任意公开函数()` 都放行。那行里的 `版本` 二字**并没有真的搬进来**，所以和您自己的 `版本` 不构成撞名，编译照常通过，您自己的 `版本()`（100）照常优先。这叫「模块级通配」。

### 3. 为什么会冒出来

P6b 按「批准过的规范条文」实现新诊断（E0255：具名导入与你本地的定义同名 = 编译错误）时，用真实用例一跑：E2E 52_library 和一个单测当场报错——因为它们用的 `导入 格式化::版本;` 在规格书理解下是撞名（应报错），在编译器实际行为下是合法遮蔽（应通过）。**规范和实现必有一个是错的，必须二选一。**

### 4. 两个方案（这就是请您裁的事）

| | 方案 A：改规范（向实现看齐） | 方案 B：改实现（向规格书/Rust 看齐） |
|---|---|---|
| 说法 | 承认现状：`导入 m::符号` = 搬整个模块；只有**花括号**形式 `导入 m::{a, b}` 才是逐个搬名字 | 把 `导入 m::符号` 改成真的逐个搬名字（Rust 一致） |
| 优点 | 零破坏，现有代码/用例全部照跑；改动只有规范文字 | 语义精确（搬什么就是什么）；与 Rust 完全一致，撞名保护覆盖最常用的写法 |
| 代价 | `导入 m::符号` 的语义比 Rust 宽（等价于把整个模块都搬来），撞名保护只覆盖花括号写法 | **动的是编译器核心解析链**：①限定调用（`工具库::版本()` 这种带前缀的写法）现行要求「先导入才能调」（P1-1），B 之后按 Rust 习惯改为「只要模块加载了就能调，导入只影响不带前缀的名字」；②E2E 52 和一个单测的源码要适配（**测试文件修改按纪律须您届时另行批准**）；③牵连改动多、回归面大 |

### 5. 与另一枚裁决（呈报二 A′）的关系

**互相独立，不捆绑。** 呈报二是另一件事：编译器有个老毛病——依赖库和你的主文件有同名同参数的函数、你又用不带前缀的名字调它时，链接会报「符号未定义」（汇编实证的错编，详见 plans/018 第九节）。您已裁决按 A′（链接键单一公式化）根治，这个不受呈报一影响，可以直接做。

### 6. 当前树里各处是什么状态（防误会）

- **代码：一行行为都没改。** 编译器今天的行为=理解二（旧行为）；新写的 E0255 检查代码存在但**停用**（semantic.cpp visitProgram 里被注释，代码注释还写着已被取代的「呈报一A」说法——以本文档与 plans/018 第九节为准）。
- **规范文本：暂按方案 B 的目标语义写的**（plans/001 §5.9.7 与 specs/08 3.6，含「宿主现状过渡期偏差」标注）——这是您改选 B 时的落盘，**但您随后暂停了裁决，所以它代表「B 方向的暂定稿」而非终裁**。若家机面询后您选 A，把这两处文本改回「花括号=具名」表述即可（五分钟文字工作）。
- plans/018 第九节有同样的状态记录。

---

## 二、用户裁决状态总表（P6b 全程）

| # | 事项 | 状态 |
|---|---|---|
| 1 | P6b 总体实施方案 A 完整版（全量对齐 Rust、宿主 v2 双侧一步到位） | ✅ 已批 |
| 2 | 「模块」定位收紧（仅限包根聚合/父挂子+普通文件使用=诊断+自动加载强语义） | ✅ 已批 |
| 3 | 货舱.toml 纳入 v2 范围 | ✅ 已批（用户明示） |
| 4 | 实施步骤与提交切分三案、E2E 自 159 顺延 | ✅ 已批 |
| 5 | **呈报二：错编缺陷根治 = A′ 链接键单一公式化** | ✅ 已批，**待实施** |
| 6 | **呈报一：「导入 m::符号」语义（A 规范收窄 / B 实现对齐）** | ⏳ **未定**——先裁 A → 改选 B → 用户暂停（「我还是不懂」），家机面询后终裁 |

---

## 三、本轮已完成（全部有实证，详见 plans/018）

1. **第 0 步实证**：`模块` 声明自动加载宿主已实现且触发源=声明本身（三探针：声明在→未导入子模块错误暴露 rc=1；声明删→rc=0；基线 build→run rc=0）。工作流 2 该项勾销。
2. **A1 翻案**：类型/常量跨模块隔离**已在 fecab2b（A-2）修复**（跨模块同名 rc=0；glob 歧义诊断函数/类型/常量三维度齐全 rc=1）。lessons 过时条目已标注 ✅（教训：锚点单测消失=状态已变的信号）。
3. **规范立规**：plans/001 新增 §5.9.7 名称解析（三级优先级+冲突遮蔽矩阵+链接符号契约）+ §5.9.1 模块定位收紧；specs/08 第二节/3.6/第七节三处同步（3.6 现为 B 暂定语义，见第一节第 6 点）。
4. **定位收紧诊断已实现**：driver_module.cpp 模块声明挂载收紧解析——普通文件声明非子模块 → 专用诊断「模块声明 'X' 非当前模块 'Y' 的子模块」（替换误导性「无法打开源文件」），基线 5 探针验证。
5. **E0255 基建写就（停用）**：ImportDecl.ownerModule 归属字段（merge 写入）+ checkImportLocalConflicts（semantic_decl.cpp）。启用前置条件随呈报一终裁（见 plans/018 工作流2 第 5 项）。
6. **新发现宿主错编缺陷**（呈报二，独立既有）：详见 plans/018 第九节——依赖与入口同名同签名+入口纯名调用 → 链接 `undefined reference 主$X`；根因=注册键归属顺序依赖（首注册占裸键）vs 定义侧「主 恒不加前缀」不对称；52 现通过属侥幸路径；汇编实证（base3 探针 `.globl` 三符号对照）。
7. **门禁（当前树，E0255 停用态）**：构建零警告 + 单测 1243/1243 + E2E linux-arm64 全量 160 用例 **156 过/1 败（78 已知 OOM 家族）/3 跳 = 与第三十轮基线完全一致，零回归**。

## 四、下一步（家机 win-x64，建议顺序）

1. **面询呈报一**：带用户读本文档第一节 → 终裁 A/B。
   - 裁 **A**：改回 plans/001 §5.9.7 与 specs/08 3.6 两处文本（约 10 行）→ E0255 的 bindings 提取收窄至花括号项分支（semantic_decl.cpp 一处条件）→ 启用检查 → 门禁。
   - 裁 **B**：按 plans/018 第九节实施宿主对齐（visitImportDecl 路径导入改具名绑定 + 限定调用校验链改造 + **P1-1 废止** + E2E 52 与单测 CrateIsolateNoParamQualifiedCall 源码适配【届时逐个向用户报批测试文件修改】）→ E0255 按现写法启用 → 门禁。
2. **呈报二 A′ 实施**（两案皆需，独立于呈报一）：新增全编译器唯一共享函数 `函数链接键(模块名, 签名键) = (模块名空 或 =="主") ? 签名键 : 模块名$签名键`；①semantic.cpp registerFunction 注册键改调它（~1485 行 crate 隔离分支，消顺序依赖）；②ir_decl.cpp:147 定义侧内联条件改调它；③semantic.cpp resolveOverload 预扫描/键剥离逻辑复核适配；④补 ①×③ 遮蔽正测（依赖+入口同名同签名+纯名调用 compile+run=100）钉死错编不复发。
3. **宿主侧新 E2E（159 起顺延）**：E0255 负测、①×③ 遮蔽正测、定位诊断负测、模块自动加载正/负测（负路径注册参考 149/145 先例）。
4. **门禁**：家机 ci.ps1 全量（本轮改动全在共享层 semantic/driver/ir，win 侧必须实证；arm64/x86_64 复验随跨机轮）。
5. 宿主收口后进 **工作流 3（v2 全链，含货舱.toml 纯 CN 解析器）**——大体量，按 plans/018 第五节六层序推进。

## 五、灰色点披露

- **E0255 停用态**（代码在、调用被注释，代码内注释仍引已被取代的 A 裁决——以本文档为准）。
- **错编缺陷未修前**：「依赖+入口同名同签名+入口纯名调用」形态会链接失败——现有 160 用例对该形态零覆盖（新雷区），A′ 修复前新增代码须避开此形态。
- **52_library 通过属侥幸路径**（汇编对照实证），A′ 修复后由专例锚定转为确定性。
- 规范文本（§5.9.7/3.6）为 B 暂定稿，与宿主现行行为存在**已声明的过渡期偏差**。
- 沿用既有：v2self 运行需大栈与项目根 cwd；strData 驻留 ID 宿主-v2 差 1（自洽零影响）；宿主 `结果` 不可作变量名；v2 arm64 字符串常量逐条 .data 发射（可读性观察项）；78/79 OOM 家族随 v2 解决。

## 六、踩过的坑（本轮新增，均已在实施中规避或待 A′ 根治）

1. **规范条文先于实现实证落地会当场翻车**：E0255 按批准条文实现即红 52/单测——写规范前必须先用最小探针实证宿主同形态真实行为（本轮工作流 2 全项先探针后动手，A1 过时条目即此法翻出）。
2. **lessons「已知限制」条目过时**：A1 实际已被 fecab2b 修复但条目仍挂「📌 已知限制」——锚点单测消失=状态已变的信号；已知限制修复后必须同步翻状态。
3. **链接 undefined reference 的中文名符号=UTF-8 hex**：`_E4B8BB24E78988E69CAC` 逐段解码（E4B8BB=主、24=$、E78988=版、E69CAC=本）即可读出 主$版本；配合 asm `.globl`/实现体对照快速夹逼「调用侧 vs 定义侧」劈叉。
4. **bash 管道吃掉退出码**：`cn check xx | head` 后 `$?` 是 head 的——门禁判定必须直取编译器退出码（本轮基线 3 首测误判 rc=0 即此坑）。
5. 宿主 build 链以相对路径编译 `src/runtime/io_api.cpp`——**必须项目根 cwd** 跑（target/ 下探针亦然）。
