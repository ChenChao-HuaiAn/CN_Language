# HANDOFF 交接文档

**交接时间**: 2026-09-06 第十九轮收官（深度系统 x86_64 本机）——**plans/017 v2 linux-x86_64 代码生成专项：T1/T2/T5/T7 完成 + 宿主重大缺陷「被调方栈参锚定基」根治 + v2 四缺陷根治（方案A 用户裁决；v2self 完整运行新缺陷呈报待裁）**。

## 本轮已完成（全部有实证）

### 一、专项主体（plans/017 方案A）
- **T1 代码生成X64Linux.cn**（约 570 行 SysV 纯发射器）+ **T2 主.cn 三分派+平台校验**，cn check 绿。
- **T5 冒烟 13 组件全绿**；**主.cn 产物（x86_64 GAS 418388 行）as 汇编+g++ 链接通过**——「v2 编译 v2 自身」产物首次在本机汇编链接；v2self 启动执行至读文件/分词/IR 阶段。
- **多形态探针宿主直编 vs v2 编译对拍一致全绿**：算术/控制流/递归/静态/负数除法/结构体返回（11,22,33,44）/读文件拼接（长度 132）/返回含向量字段结构体所有权（大小=2 计数=2）。
- **T7 门禁全绿**：构建零警告+单测 **1240/1240**+E2E linux-x86_64 **156 用例 129 过/0 败/27 跳**。

### 二、宿主重大缺陷根治（冒烟揪出，gdb+objdump 闭环）
- **linux_x64 被调方栈参锚定基缺 needHiddenRet 补偿**：push rbx 使栈参在 [rbp+24+8k]，旧锚 [rbp+16] 读**返回地址**当第 7 参（建IR指令 7 参+返回结构体正中→IR 全链污染）；修复=锚 16+8*paramOffset 三处；**E2E 154 红→绿**（420598354321→87654321）+单测 HiddenRetStackParamAnchor24。触发空白根因=v2 产物级验证历史空白+「同源码不同宿主后端构建的 v2p 行为分叉」（单位机 arm64 构建绿掩盖）。

### 三、v2 四缺陷根治（连带揪出）
静态赋值存储丢失／限定名内置 结果 接收 ABI 错配（裸 char* 当结构体）／返回含容器字段结构体所有权悬垂（析构器加跳过名ID=所有权转移）／跨文件同名函数符号冲突（生成函数→生成函数W）。

## 下一步
1. **呈报①方案A 续**（已裁，本轮已修第 6/7 枚）：剩余「容器& 引用参数的方法调用」路径缺陷（mapref 探针最小复现：灌 的 IR 中「表」读取未走 发射引用参数读=引用槽登记 miss+this 求值序错位）。
2. **呈报③方案A**（已裁未做）：IR 名带参数签名（重载消歧机制层，对齐容器方法 名#参数串 模式）。
3. T3/T4（runner 平台参数化+E2E v2 版用例）随 v2self 收口后推进。
4. 观察项：宿主「映射& 参数×构造调用」交互缺陷+「映射& 方法返回类型推导」失败（mapref 形态，待家机验证立案）。
5. 跨机不变项：主.cn MASM 产物家机 ml64；E2E 153/154 win/arm64 复验；cqo 修复 win 运行级验证。

## 灰色点披露
- v2self 完整运行未达成（呈报①）——探针级形态全对拍一致，属大规模特定形态残留。
- 观察：cn check 对 IR容器.cn 单文件「节点 不是结构体」（登记源单文件链不可达，主链绿不拦门禁）；IR生成.cn:2216 check「结果未处理错误分支」警告（既有同形态代码遍地，单行报警原因待查）。
- v2p 及 v2self 运行需大栈（ulimit -s unlimited；生成表达式 单帧 148KB+深递归）——「v2 编译器运行栈需求」工程化（大栈线程/runner ulimit）随 T3 一并处理。
- 探针/对照产物在 target/dbg/probe017/（不入库）；/tmp/v2self 等临时产物随系统清理。

## 踩过的坑（本轮，已入 lessons 权重 10）
- **「产出存在+语义 0 错」是假绿——产物验证必须到汇编+真实运行层**（本缺陷潜伏 3 轮跨机全漏网）。
- **栈参数错位 8 字节=读到返回地址**（污染值全为代码段地址=栈上方数据指纹）——被调方栈锚按 prologue 实际 push 序列重推导，多后端不能互背书。
- **同源码不同宿主后端构建的产物是不同测试对象**（单位机 arm64-v2p 绿≠本机 linux_x64-v2p 正确）。
- 返回值含资源字段的结构体，返回路径必须做所有权决策（浅拷+析构=悬垂）。
