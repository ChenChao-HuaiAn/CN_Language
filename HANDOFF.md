# HANDOFF 交接文档

**交接时间**: 2026-09-11 第七十三轮（**深度机 linux-x86_64**）——**plans/020 矩阵靶子核验揪出并根治两枚 v2 侧 P0 静默缺陷**（块级 RAII 从未生效 + 跳出路径无 drop-on-jump，用户裁决方案A）。验证链全绿，固定点新口径 **266845**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：按上轮 HANDOFF 任务 #1（plans/020 矩阵第 27 格「v2 侧跳出路径 drop-on-jump 专项核验」）起手，探针实测中揪出的问题比靶子更深——**v2 侧块级作用域 RAII 从未生效**。

2. **探针实证（探针 89/90/91，全部可复现）**：20 万轮循环体声明拥有串，活动分配数 **199,999**（继续/自然回边/如果体 三形态同——即**不限跳出路径**）；容器 2 万轮对象未析构；30000 轮 ×14.4KB 泄漏上限 411MB（OOM 实证）。生成物层：跳出路径无 `__cn_str_free`。

3. **根因（诊断注入实证）**：
   - **缺陷甲（P0）**：`预扫容器声明` 在函数体生成**之前**函数级一次性登记释放名单，而 `生成块` 入口取基线=名单大小 → 基线恒等 → 块出口析构**永远无新增项可释放**（诊断输出：`登记拥有串: 体内` 在预扫阶段、随后 `块出口析构: 字符串基线=1 现名单=1` 零释放）。对照=结构体容器字段（生成期登记）机制正常。
   - **缺陷乙（P0）**：`中断`/`继续` 只发跳转无 drop-on-jump（宿主 72-a 已有）。

4. **用户裁决方案 A**（呈报含 4 方案，性能第一/安全第二/Rust 语义）+ **实施（v2 IR 4 文件）**：
   - 登记时机对齐宿主（预扫收敛为槽分配+入口零初始化+类型登记；名单追加移至生成期 + `名单追加去重`）；
   - `函数IR` 三枚循环体基线 + `循环跳出析构`（按最近循环体基线释放，编译期名单**不截断**）；
   - **「释放+清零」幂等模型**：首版（不截断+不清零）触发容器跳出 **SIGSEGV**（跳出释放+函数级兜底双释，gdb 实证）→ 补 `函数IR.零寄存器`（预扫回填）+ 三处释放点槽清零；
   - `块出口析构` 新增 `截断` 参数（块出口=真/跳出=假）；不截断模式用下标递减防死循环；
   - 连带：v2 内置表补 `内存::活动分配数`（E2E 断言口径）。

5. **成效（归零闭环）**：五形态 20 万轮级残留 **199,999 → 0**；v2p 编译 v2 树退出残留 2,755,596 → 2,757,891（同量级=编译器常驻结构）。**悬垂风险扫描 = 0 命中**（全树「块内声明→容器追加/静态存储/返回」零处）。

6. **新防线**：E2E **216_v2_块级作用域RAII内存回归**（七形态差值恒 0 + 内容校验，退出码 127）——宿主 213 的 v2 对应件，两侧对称。

7. **验证链全绿**：清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 1289/1289 + E2E **238 用例 233 过**（唯一败=194 既有待批）/4 跳 + 锚定链固定点 **266845**（新口径）逐字节一致 + 绑定自检 + 运行级 + 组件对拍 29/29。

8. **文档五件套**：plans/019（第七十三轮段+跟踪表行）+ plans/020（矩阵 A1/B27 格回改+新防线+观察项 3/4+方法论 5/6）+ lessons（第七十三轮段，权重 8）+ 更新日志 + 本文件。

## 一-B、移植纪律已入册（plans/020 三-B，7~10 条，**下轮起强制自检**）

本轮回答「为什么出错/怎么防」并把结论落为制度（用户要求写进 plans/020 维护纪律）：

- **第 7 条 移植清单两件套**：移植机制先写「机制主体 + 依赖不变量」两行；不变量逐条验证，禁止凭「看起来像冗余」跳过（73-a 首版 SIGSEGV 根因即漏「释放后槽清零」）。
- **第 8 条 释放路径两侧对照**：跑 `bash scripts/cn_release_parity.sh`（本轮新建），人工核对「时机 / 条件 / 不变量」三列；脚本末尾有清零幂等两测计数报警。
- **第 9 条 同结构双编译器对照**：同一最小程序两侧各编一次，比对生成物层释放调用 + 运行级 `内存::活动分配数()` 差值；防线不对称=缺陷候选；矩阵格声明须带验证状态（实测通过/未验证靶子）。
- **第 10 条 两侧同缺一次修全**：改任一侧释放机制时当场核对另一侧同族缺口。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子**（空白即靶子）：①入容器编译器级自动 copy/move（容器析构轮——A2 显式复制纪律的编译器接管）；②数组元素/装箱字段/结构体字段借用面（NLL 轮）；③**结构体容器字段「声明即用」既有边界**（本轮三方对照实证，靶子归属结构体字段初始化轮）；④函数级 `genClassDestructorCalls` 的 hasDtor 限制与块级口径统一（观察项）。
2. **呈报清单余项**：194 单行批准（已呈报五轮无答复）；win 平台 v2 并行隔离（家机轮）；71-a 后续可选题（构造器实参类型匹配/重定义诊断）。
3. **矩阵维护纪律**：每轮触碰所有权语义的改动必须同步更新 plans/020 对应格。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处 warning/error 匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 238 用例 233 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（v2 树或宿主编译器改动后须删 v2p 缓存键重建）
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
```

## 四、本轮踩坑（已入 lessons 第七十三轮段 ⑭~⑰）

- **「一次性预扫 + 生成期基线」互斥**：预扫先于函数体生成，若在预扫登记释放名单，生成块入口的基线（名单大小）恒等 → 块级 RAII **静默失效**（编译通过、运行不崩、只是不释放）。举凡「预扫做 A、生成期读 A 的大小作界」的设计，先问「A 的增长时机与读者时机谁先」。
- **修释放路径先问「同一槽会被几条路径经过」**：多路径共享槽必须选「截断名单」或「释放后清零」之一（宿主 72-a / v2 73-a 现同为「释放+清零」幂等模型），否则双释 SIGSEGV（本轮首版实证）。
- **旧修复未同步到对侧即复发**：宿主 72-a 的 drop-on-jump 与幂等模型，v2 侧隔一轮才补——双侧同缺的教训（第七十二轮预防⑥）仍需按「修复台账逐侧核对」执行。
- **既有边界 vs 新引入的三方对照**：修复后暴露的失败必须用「修复前版本同形态实测」对照（本轮结构体容器字段「声明即用」：修复前 v2p / 修复后 v2p / 宿主**均失败** → 既有边界，非本轮引入）。

## 五、诚实边界

- 结构体容器字段「声明即用」（`货架 s; s.表.追加(...)`）报「空指针解引用」=**既有边界**（三方对照实证，字段未自动构造）——靶子在册。
- 装箱字段/数组元素/静态面无 RAII（plans/020 靶子）；v2 侧「返回接收形态」不登记释放（原预扫豁免语义原样搬移）。
- 构造器实参类型匹配不检查（v2 既有）；跨编译器结果 ABI 互操作未单独 E2E 锚定；v2 契约登记按纯名/键基回退查（重载族返回异型=近似）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。

## 六、关键产物位置（target/ 不入库）

- 深度机：target/p71x64l/（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **266845 行**、cn_self71）。
- 探针（/tmp/p73probe 临时，不入库）：探针 89~92（壳 `hook/liveprobe.so` LD_PRELOAD 读 `__cn_alloc_live`；五形态探针在 leak/ 与 seg/ 子目录；诊断注入实验在 diag/v2tree）。**修复前后残留 199999→0 的量化证据在此**。
- E2E 新用例：tests/e2e/**216_v2_块级作用域RAII内存回归/**（v2 版 213，含 v2闭环.txt 配置：退出码=127、借链=是）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——每轮触碰所有权语义必须同步更新）。
