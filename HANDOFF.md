# HANDOFF 交接文档

**交接时间**: 2026-09-11 第七十轮（**深度机 linux-x86_64**）——**双机并行合并（9f5d22f）+ 70-a A2 契约根治（aa08481，方案A 用户裁决：retOwnedString 二分残留恢复+系统::参数 改字符\*+装箱 move 语义）+ 手动释放收尾批（A2 全链收官）**。**验证链全绿，固定点口径 262321**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **双机并行合并**：`git pull` 分叉（本地 A2 三提交 vs 远程单位机两提交）→ merge（项目惯例，保留双方哈希）→4 文档冲突手工调和（plans/014/019 状态行双侧保留、HANDOFF 双机融合、更新日志双轮摘要），源码零冲突（合并提交 9f5d22f）。
2. **70-a 立案→根治（用户裁决方案A 两步走）**：盘点手动释放收尾批时发现宿主 semantic_call.cpp:963 契约 `false &&` 二分残留（轮1 临时禁用忘恢复）→探针实证双侧语义分叉→呈报三方案（A 恢复契约小步验证/B 维持缩水/C 一步到位）→用户选 A。
3. **恢复契约后连环根治三枚潜伏缺陷**：①`系统::参数` argv 借用签名 字符串（类型谎言）→双侧改 字符\*+stdlib 包装复制+调用面适配（v2 主.cn 3 处+tests 37/38 六处）；②`取语义诊断` 容器元素借用返回（B 类轮1 漏修）→显式复制；③**装箱 move 语义**（真根因）：`正常(拥有字符串局部)` 装箱浅共享×返回块 free=悬垂（stdlib 读取行包装链→v2p 读源文件全空）→ir_error_ctor.cpp 值字段写入后源槽清零（Rust Ok(s) move 同款零拷贝）。
4. **验证链全绿**：v2p 复活（57054 节点）+清场重建零警告+单测 1289/1289+E2E 唯一败=194 既有+固定点 **262321** 逐字节一致+运行级 3/3+对拍 29/29。
5. **手动释放收尾批（A2 全链收官）**：70-a 契约使转字符串族自动登记（探针实证——「入拥有判定」项天然兑现）→实删 103 处双重释放（tests 17 文件 96+stdlib 7：白名单/转字符串族/stdlib 返回拥有面的 RAII 已接管面）+保留 10 处借用面（数组元素/装箱字段/69 计数断言/分割 输出契约）+stdlib 与 tests 契约注释同步 9 文件；门禁全绿（单测 1289+E2E 唯一败=194 既有+固定点 262321 维持+对拍 29/29）。

## 二、下一轮任务（按序）

1. **v2 侧装箱 move 对齐**：v2 IR 的 正常/某些 装箱（结果构造）对拥有字符串实参做源槽清零 move 语义（对齐宿主 ir_error_ctor——v2 现状签名表查不到 stdlib 包装=保守不登记侥幸安全，泄漏方向）。
2. 呈报清单余项：194 单行批准（`返回 整32(*q);` 改写方案已呈报两轮无答复）；win 平台 v2 并行隔离（家机轮）；第二层第三批收口时点（迁移全量完成=前置就绪）。
3. 阶段4' 收尾评估：嵌套块声明块出口析构扩展（第三批残余）；装箱临时值生命周期（`正常(f())` 形态临时串泄漏=语句级临时析构议题）；数组元素/装箱字段/参数/静态借用面 RAII（随 NLL/容器析构语义轮）；阶段5 等并发库立项；NLL 等反馈期数据。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（grep 排除 error_*.cpp 文件名误匹配）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量唯一败=194 既有待批
bash target/p68x64l/run_chain68.sh   # 锚定链：fix_p 262313→**262321（第七十轮新口径）** 固定点一致+绑定自检+运行级 3/3（208v2/119/173）
bash target/p68x64l/cmp29_68.sh      # 对拍 29/29
```

## 四、本轮踩坑（已入 lessons 第七十轮 权重8）

- **二分禁用残留复审纪律**：二分调试的禁用标记（false &&）散落源码，根因修复后无「全树 grep 复审清零」步骤——c668919 只修了 v2 侧假&&，宿主侧同族漏网两轮才被盘点暴露。
- **装箱浅共享×局部 RAII=悬垂家族第五实例**：`正常(拥有字符串局部)` 装箱（StorePtr 浅拷指针）×genStringFrees 返回块 free=装箱值悬垂——移出识别只认裸 Load 返回不认装箱形态。修复=装箱 move（源槽清零+空安全跳过）。**新启用 RAII/所有权面必须枚举「值经装箱/容器/结构体转交」全部分派点**。
- 契约类语义开关的验证载体=自举链运行级（v2p 编译 v2 树节点数），存量测试全显式化时它是唯一能暴露分叉的探针（E2E 全绿+固定点一致都被掩盖）。
- `cn run` 在 Linux 用 Windows 风格输出目录 `target\`（既有边界，E2E 走 compile+外部链接不经过）；`cn build` 运行时编译用相对路径须 cwd=项目根。
- v2p 的 stdlib 签名扫描按相对 cwd 读 stdlib/容器.cn——手动跑 v2p 须 cwd 内软链 stdlib。

## 五、诚实边界

- v2 侧装箱 move 未对齐（v2 签名表查不到 stdlib 包装=保守不登记，侥幸安全泄漏方向——随收尾批对齐）。
- `正常(拥有串)` 装箱后源使用无 use-after-move 语义检查（清零后读 NULL=空安全崩溃非悬垂——比悬垂安全，严格检查随语义轮）。
- `正常(f())` 嵌套调用装箱形态：临时串无人 free=泄漏（语句级临时值析构议题，随第三批残余）。
- v2 契约登记按纯名查（重载族返回异型=近似）；字符* 与字符串同码；泛型体内返回位/赋值位豁免；参数/静态维持借用。
- 手动释放重叠面已清偿（第七十轮下半程：实删 103 处，保留 10 处借用面=数组元素/装箱字段/69 计数/分割 输出契约）。
- win-x64 v2 并行未隔离（家机轮）；194 待批维持；01_hello 不可作 v2 运行级载体（208v2 替代）。

## 六、关键产物位置（target/ 不入库）

- 深度机：target/p68x64l/（run_chain68.sh、cmp29_68.sh、v2p68、cn_self68、fix_p.s/fix_s.s **262321 行**）。
- 单位机：target/probe68/（arm64 口径 347685 行）；rt objs 指纹缓存 target/audit2/rt_build_key.txt（68-a）。
- 70-a 探针（/tmp/p70probe 临时，不入库）：契约探针.cn（str_free 注入验证）、读文件探针.cn、v2p_dbg70/fix70.txt（v2p 复活实证）。
