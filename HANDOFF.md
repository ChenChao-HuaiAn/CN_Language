# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十六轮（**深度机 linux-x86_64**）——**86-a 复审三缺陷根治**
（用户令「独立复审 74-a~85-a（23 提交）」；四条代理审查线 + 验证链六项复跑 + 探针
P41~P47 实证）。本轮序列：复审（规定/规范/diff/纪律四线）→ 证伪两项静态存疑 →
实证揪出三缺陷（三元×聚合两侧 / 84-e v2 守卫顺序 / 85-a 豁免无类型约束）→ 全部根治
→ 探针连带发现两处既有缺陷（成员位 preFree 缺失 / 字面量字段=标识符标量路径）同轮修复
→ 四件套防线（E2E 243/244/245 + 反证五件）→ 文档五件套。验证链全绿。

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：用户令审查前面十几轮（74-a~85-a，23 提交，146 文件）是否有缺陷、合不合
   规。执行=四条并行代理审查线（项目规定 / CN 规范 / 关键 diff 缺陷审读 / 测试纪律与
   工作区清洁）+ 验证链六项复跑 + 定向探针。

2. **复审结论**：验证链声明**属实**（六项全绿）；CN 规范符合性与流程纪律**整体合规**
   （13 文件零违规；测试纪律四方面合规——唯一既有用例修改〈194，84761e0〉有用户批准留痕；
   依赖方向单向、注释 100% 中文；**行数超限为存量违规面**：宿主 14 文件超千行、19 函数
   超百行、v2 3 个 .cn 超千行——已登记长期整改）；**证伪两项静态存疑**（结果<盒,整32>
   装箱=内置构造器内部深拷掩护、映射 V=盒=编译期拒绝不可达）。

3. **实证揪出三缺陷（本轮根治）**：
   - **缺陷一：三元 × 聚合（两侧，最高优先）**——`返回 (条件 ? 借用形参 : 借用形参)`
     悬垂（P41 宿主乱码 / **v2 段错误 rc=139**）。根因三层：宿主返回位判定 default 把
     三元当拥有来源；两侧各消费位全面缺三元（赋值位标量 8 字节 / v2 声明位拒绝 /
     成员偏移未知 / 实参回扫误改临时槽地址）；v2 三元分支对聚合恒 8 字节 `IR_加载`。
     **修复（自行裁决甲=完整支持，语言面不收窄，留痕）=三元聚合「地址透传」**：
     宿主五处（`ir_oop.cpp` exprSrcType / `ir_expr.cpp` 赋值位+emitStructWholeAssign /
     `ir_fields.cpp` isBorrowedAggregateSource / `ir.cpp` memberObjStructType）；
     v2 七处（`IR生成.cn` 解析节点类型ID+回扫 T_对象 跳过；`生成表达式.cn`
     区间末为聚合左值+生成三元运算地址化+汇合 T_对象+成员偏移；`生成声明.cn` 声明位;
     `生成语句.cn` 赋值位白名单+成员位通道）。
   - **缺陷二：84-e 深拷守卫顺序（v2 单侧）**——`IR容器.cn` `发射串字段深拷` 的
     「查结构体大小>0→跳过」在递归分支**之前** → 嵌套递归深拷死代码（P42：宿主 RC=1
     vs v2 RC=0）。修复=两行交换（三处同序）。
   - **缺陷三：85-a 豁免无类型约束（两侧）**——用户顶层函数恰名「获取」+`返回 借用形参`
     被误豁免（P44 两侧乱码）。修复=豁免加容器类型前置（宿主 `currentClass_` ×
     `isStringElemContainer`/`isOwnedStrFieldElemContainer`/`isStringValuedMap`；
     v2 新增 `方法类名85`+`是容器类`）。

4. **扩展发现（探针连带，既有缺陷同轮修复）**：宿主成员位赋值 preFree 缺失
   （`emitStructWholeAssign` 调用点默认 false → 覆盖旧串泄漏，t244c 残留=1）；
   结构体字面量字段值=标识符落标量 8 字节路径（`emitStructInitTo` 原只处理嵌套字面量，
   t244v 实证）——均两侧同构对齐（v2 成员位通道 preFree 同步改真）。

5. **防线四件套**：
   - **正测**：E2E **243_双编译对照三元聚合**（移植纪律 9 **第十四个**对照用例：八形态
     ①赋值②声明③实参+形参返回④实参成员链⑤成员位⑥嵌套三元⑦三元成员基⑧返回三元借用，
     折 **255** 两侧一致）+ **244_双编译对照嵌套深拷与豁免**（**第十五个**：①嵌套整体赋值
     ②嵌套声明位③嵌套成员位④嵌套返回位⑤⑥⑦用户函数名 获取/元素/读取 返回借用⑧循环
     5 万轮，折 **255**）+ **245_三元聚合容器消费位**（宿主单侧八形态折 **255**）。
   - **反证五件（全部精准命中，注入均已恢复复验、零残留）**：A 宿主三元判定恒假 →
     243 宿主 **255→127**（丢 ⑧）；B v2 守卫顺序回退 → 244 v2 **255→246**（丢 ①④）；
     C v2 豁免去类型约束 → 244 v2 **255→143**（丢 ⑤⑥⑦）；D 宿主成员位 preFree 回退 →
     244 宿主 **255→251**（丢 ③）；E 宿主字面量字段整体拷贝失效 → 244 宿主 **255→127**（丢 ⑧）。
   - **全量 E2E**：272 用例 268 过 0 失败 4 跳（+243/244/245；其余 269 过=零回归）。

6. **验证链**：清场重建**零警告** + 单测 **1306/1306** + E2E **272 用例 268 过 0 失败**/4 跳
   + 锚定链固定点 **330412 行**（新口径）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

7. **文档五件套**：plans/019（跟踪表第八十六轮行）+ plans/020（**第二十七节 86-a 全节**
   + 第二十六节「结果/可选 宿主侧已覆盖」**表述更正**）+ lessons（86-a 段，权重 8，
   预防新增 82~86）+ 更新日志（**标题层级修正为 `##`**——复审发现的轻微偏差）+ 本文件。

## 二、下一轮任务（按序）

1. **② 静态结构体支持（登记，第一优先候选）**：85-a 登记维持（`.data` 槽按类型尺寸
   分配 + 静态结构体字面量赋值路由 `emitStructCopyWithFields`；三方案见 plans/020 第二十六节）。
2. **B35 字段容器接收者（待用户裁决）**：plans/020 第十七节三方案（甲 move／乙 深拷
   〈推荐〉／丙 维持+诊断）——裁决后实施。
3. **甲（参数 move 语义）/丙（返回位编译期拒绝）**：85-a 登记项维持（待用户裁决）。
4. **行数存量违规面长期整改**：宿主 `ir_expr.cpp`（2667 行，含 866 行的
   `visitAssignmentExpr`）等 14 文件、19 函数；v2 `IR容器.cn`（1629 行）等 3 文件——
   拆分须专项轮（有回归风险）。
5. **v2 容器插入位三元对照（可选）**：本轮 245 宿主单侧承载；如需 v2 侧，按供给机制
   （供给.cn）补。
6. **v2 结果/可选 返回三元**（84-e 边界）不在面内；实现槽前先补 v2 结果/可选 值字段
   资源登记。
7. **跨机轮**：arm64（单位机麒麟）复验 E2E 234~245；win-x64 v2 并行隔离（家机轮）。
8. **呈报清单余项**：阶段 4 第二层第三批收口（不安全区变硬错误）；v2 诊断无行号；
   未初始化槽 preFree 观察项（84-f 维持）；物化性能优化。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 272 用例 268 过/0 失败/4 跳
bash target/p71x64l/run_chain71.sh   # 锚定链（固定点两代逐字节一致）
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**
bash scripts/cn_release_parity.sh    # 释放路径两侧对照
```

> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **手工 v2 探针链**（本轮建立，/tmp/p86probe/v2work）：`v2p 主.cn linux-x86_64`（cwd
> 需有 `stdlib` 软链 + `target/` 目录）→ `as target/v2asm.s -o 主.o` → `g++ 主.o
> target/audit2/{io_api,intern_api,runtime,string_api,i128_api,math_api,input_api,
> file_api,time_api,system_api}.o`（容器面再前置 `target/audit2/v2p_linuxx64.o`——
> 但 `cn_main` 冲突，容器用例须走 E2E 供给机制）。
> **反证手法**：备份文件（本轮 /tmp/p86probe/*.final.bak）→ python 注入 → 重建
> （宿主 `cmake --build` / v2 删 `target/audit2/v2p_build_key_linuxx64.txt` 后 E2E 自动
> 重建）→ 跑用例应 FAIL → 恢复复验。

## 四、本轮踩坑（已入 lessons 第八十六轮段）

- **复审发现=新缺陷的合法来源；静态审查发现必须探针实证（证伪同价）**：4 真缺陷 + 3
  存疑 → 探针实证后 2 项证伪、3 项成立——没有探针的静态发现置信度不足。
- **条件表达式（三元）的值语义=选中 place 地址透传，但消费位必须全链补形态**：新值形态
  立项检查单=grep 全树「形态白名单」与「类型推导 switch」逐点确认（本轮宿主 5 + v2 7 处）。
- **豁免/清单类判定必须有第二维=类型约束**：纯名字匹配豁免=用户同名的隐形雷区（P44）。
- **守卫插入位置=分派链语义的一部分**：多分派点插入同款守卫必须逐点核对全链顺序；
  死分支不报错、只在特定形态静默失效。
- **局部修复必须全量 E2E 回归 + 探针矩阵**：本轮矩阵连带暴露两处既有缺陷（成员位 preFree /
  字面量字段标量路径）。

## 五、诚实边界

- **v2 容器插入位三元面**由 E2E 245 宿主单侧承载（v2 容器面需供给机制，与 242/243
  「不依赖容器符号」惯例一致）。
- **v2 结果/可选 返回三元**不在面内（84-e 边界维持）。
- **行数存量违规面**登记为长期整改（复审附带确认，非本轮引入）。
- 前轮登记维持：② 静态结构体、B35（待裁决）、甲/丙（待裁决）、物化性能、实参临时释放
  时机（84-g 证伪其「冗余复制」表述）、未初始化槽 preFree 观察项；win-x64 v2 并行未隔离
  （家机轮）；arm64 复验随跨机轮。
- 探针 P41~P47 留 /tmp/p86probe（不入库）；复审结论已写入项目记忆（cn-85a-audit-findings）。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s 新固定点、cn_self71）。
- 探针（/tmp/p86probe，不入库）：P41~P47（三元八形态矩阵/离析/形八最小）、t244 系列
  （成员位/字面量字段隔离）、*.final.bak（反证备份）、v2work/（v2 手工链工作区）。
- E2E 用例：`tests/e2e/243_双编译对照三元聚合/`、`244_双编译对照嵌套深拷与豁免/`、
  `245_三元聚合容器消费位/`。
- 修改文件（86-a）：宿主 `src/cn_compiler/ir/ir_fields.cpp`、`ir_expr.cpp`、`ir_oop.cpp`、
  `ir.cpp`；v2 `IR/IR容器.cn`、`IR/生成语句.cn`、`IR/生成表达式.cn`、`IR/生成声明.cn`、
  `IR/IR生成.cn`、`语义/语义检查.cn`。
