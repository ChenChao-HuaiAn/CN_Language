# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十一轮（**深度机 X64L**，linux-x86_64 Deepin 25）——类型系统严格化轮：**46-f 挂账混合符号二元运算拒绝（用户裁决方案A·Rust 对齐）双侧实施 + v2 类型转换发射族源类型分派根治（用户裁决方案甲·Rust as 语义对齐）**。全量门禁全绿 + E2E 192/192_v2/193/193_v2 四用例双通道 PASS + v2self 固定点复锚（**238017 行 = X64L 新锚定**，上轮 235667+2350）+ 组件对拍 29/29 + 运行级 3/3。**下一跨机轮主线仍是单位机 arm64 复锚（302433 行口径因三轮 v2 变更预期增长）。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **规范变更（用户审批后落地）**：plans/001 §3.7 新增「混合符号二元运算禁止」——有符号（整N）与无符号（正M）的**变量间**比较/算术/位运算编译期拒绝，须显式 `类型名(表达式)` 构造转换；**字面量豁免**（整数字面量含 `-1` 一元负号形态，按另一侧类型参与——Rust 字面量推断惯例，E2E 191「大 > 100」锚保留）；同符号混合宽度宽化维持。cn-language-spec specs/02-类型系统 同步。依据=四形态校验和探针（target/p50x64l/mixed_cmp）：宿主 rc=9 / v2 rc=11 / 数学真值 13——`整64(-1) < 正64(2^63)` 值域反转双侧同错、`正32(4e9) < 整64(-1)` 双侧分叉。
2. **宿主实施（方案A）**：semantic_expr.cpp visitBinaryExpr 前置检查（比较/算术/位运算三族共用；isIntLiteralExpr helper 判字面量豁免）。
3. **v2 实施（方案A）**：语义检查表达式.cn 新增 检查混合符号二元 + 三判定 helper；语义检查.cn 二元分支扩算术/位运算符。**实施中自查揪出假阳性并根治**：检查表达式 有诊断副作用非幂等，把原有带诊断递归扩到算术符导致 174 负测合集诊断数 14→15（`未声明标识符: x` 双报，全量 E2E 揪出）——修复=带诊断递归限定回比较符（既有行为契约）+ 混合检查走 表达式类型ID 推导族+静默诊断缓冲（语句根线性遍历=权威诊断点）。教训入 lessons 权重 6。
4. **方案甲（v2 类型转换发射族根治）**：193_v2 正测红暴露既有缺陷——IR_类型转换 指令类型字段只承载目标码，发射层按它选读取扩展方式：`整64(正32变量)` 落 movsxd 兜底出负（4000000000→-294967296）、`整64(正64变量)` 低32截断（2^63→0），宿主 emitCast 按源分派正确=同源码两侧不同结果。呈报三方案后用户裁决方案甲：IR 层（生成转换或构造调用）目标 64 位码时查源寄存器登记类型改标指令类型字段（源正32→T_正32 零扩展读/源整64·正64→T_正64 64 位直拷/其余维持兜底）+原目标码供产物寄存器登记（正32(x) 产物参与二元无符号性不丢）；三后端：X64L `mov r10, qword ptr`、win `mov rax` 直拷新分支，ARM64 x 寄存器 64 位直通=直拷（注释固化）。探针实证双侧一致 4000000000/-9223372036854775808。
5. **E2E 四用例双通道 PASS**：192（宿主负测 期望编译失败.txt 固化诊断子串「混合符号二元运算禁止」）/ 192_v2（v2 负路径 4 诊断行固化+中止纪律，run_e2e.py 注册 None）/ 193（宿主 stdout 双锚 3999999999/29）/ 193_v2（rc=29 五锚校验和）。
6. **全量门禁**：构建零警告 + 单测 **1251/1251** + 全量 E2E **201 用例 197 过/0 败/4 跳**（跳=62/69/78/79 既有平台限制；201=上轮 197+4）。宿主编译 v2 全树 rc=0（**存量零踩线实证**——v2 全树无变量间混合符号形态）。
7. **v2self 锚定链（X64L 复锚）**：v2p_new51d 编译 v2 自身→fix_p.s **238017 行**（235667+2350）→链接 cn_self51（cn_self51.o 在前+第五十轮 v2p.o 借链容器符号【第四十八轮跨代借链同款】+rt48 objs 现编+muldefs）→**绑定自检 cn_main=0x4d9027 ∈ [0x402586,0x4dca59)**→cn_self51 编译 v2 自身→fix_s.s 238017 行→**固定点逐字节一致**+运行级三用例 hello rc=0（你好，世界）/119 rc=14/173 rc=173 全对齐+组件对拍 **29/29**。
8. 文档：plans/014 状态表第五十一轮行+46-f 行根治记录+更新日志+lessons 两条（权重 6）+本 HANDOFF。

## 二、下一轮任务（按序）

1. **【单位机】arm64 跨机轮**：`git pull` 本提交 → 全量门禁复验 → 新用例 arm64 复跑（192/192_v2/193/193_v2 四用例首验 ARM64 后端；191/191_v2 等同挂）→ v2self 固定点复锚（**arm64 口径 302433 行因第四十九/五十/五十一轮 v2 变更预期增长**）+绑定自检+组件对拍+运行级三用例。win 侧同理（235968 行待重锚；方案甲 win 直拷分支 mov rax 首验同挂）。
2. **残余观察项（随类型系统严格轮，均已在 plans/014 立案）**：①宿主 canConvert 注释与实现不符（同秩跨符号放行+异秩不看符号放行）与 §3.7 新条文一致性收口；②v2 正8/正16→64 位目标转换源分派未标注（dword movsxd 兜底因值域小恰好正确，无独立用例锚定）；③**赋值初始化/函数传参面混合符号严格化**（本轮按挂账原文只拦二元运算；Rust 在赋值/传参面同样拒绝）；④整32→正64 语义=符号扩展位模式（Rust as 同款）规范文档化。
3. **观察项延续**：联合体构造字面量双侧锚定（随构造字面量覆盖轮）；标量推断 IR 类型面（布尔打印分叉 184 expected 待批/调用返回大值截断）。

## 三、踩坑备忘（详见 lessons.md）

- 检查表达式诊断副作用非幂等：二次递归=重复诊断；新增检查调用点须区分「要诊断」与「只要类型」（静默推导），扩检查范围后必须跑全量 E2E 看负测合集计数漂移。
- IR 指令字段双重语义（类型转换 类型字段=目标码兼发射读法选择器）：按源类型×目标类型矩阵探针验证，兜底分支旁路穷尽审计。

## 四、关键产物位置（target/ 不入库）

- 探针：target/p50x64l/mixed_cmp.cn（四形态校验和）、cast_probe.cn（转换双形态）、min_t.cn
- 锚定链：target/p51x64l_chain.sh、target/p50x64l/map_check51.py、fix_p.s/fix_s.s（238017 行）、cn_self51
- 运行级：target/p50x64l/runlevel51.sh；组件对拍：target/p50x64l/cmp29_51.sh
- 编译器：target/cn（宿主）、target/p50x64l/v2p_new51d（第五十一轮 v2p）、target/cn_self51（自举产物）
