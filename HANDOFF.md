# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十四轮（深度系统 x86_64）——**千行组件拆分 + 阶段目录包化**：v2 编译器顶层重组为 主.cn + 6 阶段目录（词法/语法/语义/IR/代码生成/货舱解析），四超线文件拆为九文件（≤1000 行，生成表达式 1261 挂账），宿主目录包加载四缺陷根治；宿主全量门禁全绿（1249 单测+170 过/0 败/4 跳）+ v2p 编译运行链通。**下一步 = v2self 锚定专项治理（段错误根因已定位两项，见下）**。

---

## 一、本轮核心实施

### 1. 阶段目录包化（2026-09-08 用户裁决形态A）

CN语言编译器v2/ 顶层 = **主.cn + 6 目录**（词法/语法/语义/IR/代码生成/货舱解析），每目录 `包.cn` 库包根聚合声明（宿主候选2b 目录包同款语义）。**关键设计：包.cn 挂载名保持原组件名** → 既有文件模块路径/键基/mangling 逐字节不变，回归面收缩到新拆文件。新拆子模块全部 `公开:` 段。

### 2. 千行拆分（四超线 → 九文件）

- 语法分析 1468 → 语法分析 626 + 解析表达式 573 + 解析语句 280 + **解析基础**（新，基础设施下沉破环）
- 语义分析 1620 → 语义分析 670 + **语义检查** 986（检查族+主流程搬入）
- IR容器 1299 → IR容器 807 + **IR布局** 538（布局表族+键基基础设施+常量表）
- IR生成 3182 → IR生成 861 + **生成表达式** 1261 + **生成语句** 853 + **生成函数** 274

**破环搬移**（宿主逐文件流水检查、依赖先解析）：键基基础设施 IR容器→IR布局；主流程 语义分析→语义检查；解析基础设施→解析基础。**包.cn 声明序=依赖序**：语法(基础→表达式→语句→分析)、语义(分析→检查)、IR(常量→签名→布局→容器→生成→表达式→语句→函数)；主.cn 导入序=词法→IR→语法→语义→代码生成→货舱解析。

**跨文件静态触点函数化 52 处**：静态表文件级私有，新拆文件触点全走 20 个公开访问函数（-1 哨兵未命中）。诚实边界：生成表达式.cn 1261 行（单函数 1080 行，函数级拆分挂账）。

### 3. v2 自举加载器（主.cn）

候选1b（入口目录/名/包.cn）+ 候选2b（当前文件目录逐级上溯）+ 导入改目录包名。主.cn 加载器与宿主 driver 同构。

### 4. 宿主四缺陷根治（目录包场景探针实证，全部本轮新发现）

1. **目录包根撞名**（driver）：包.cn moduleName=pathStem=「包」全包撞名 → findModule 去重把第二包起整树静默跳过。修复=moduleName=目录名。
2. **挂载去重误伤**（driver）：前置 findModule 把「成员挂载名==包根名」跳过 → 成员永不加载。修复=去重仅限非挂载依赖。
3. **自包导入拓扑环**（driver）：成员导入自包名与包根聚合边成环（IR→IR布局→IR）。修复=自包/祖先导入跳过（非挂载限定）。
4. **imports 自引用过滤误伤**（module.cpp）：`dep != moduleName` 丢弃目录包根同名成员挂载。修复=isModuleDecl 恒登记。
5. **语义尾段消歧扩展**（semantic_decl）：两段导入尾段=已加载 unit 名判③模块导入（74 修复）；三段保持原判定（47 收窄实测）；两段符号导入 itemAliasModules_ 置空哨兵（决议回退全局唯一命中，47 最终修复）。

## 二、验证（全绿）

- 单测 **1249/1249**；E2E linux-x86_64 **170 过/0 败/4 跳**（62/69/78/79=显式平台跳过清单，与第三十三轮基线逐字一致）。
- v2 主链宿主 check ✓；v2p build ✓；v2p 编译 130 用例 → as/g++ 链接 → 运行 rc=0 ✓；组件冒烟 **21/21**（abi辅助.cn=v1 遗留探针非组件，HEAD 同红）。

## 三、挂账呈报（下轮首务）：v2self 锚定专项治理

v2p 编译 v2 主链自身（v2self）段错误，**已定位两项**：

1. **栈溢出**：解析二元层↔解析二元循环 深递归，默认 8MB 栈 SIGSEGV；`ulimit -s unlimited` 后消失。需评估：默认栈上限/编译器递归深度治理（对比 HEAD v2p 的栈行为基线）。
2. **IR 层容器方法收录缺口**：栈解除后报 `[ir2] 容器方法未收录: 向量$Token.追加`——跨模块泛型实例（Token 来自 词法::词法分析，实例用在 语法::解析表达式）的签名表收录缺失；旧结构同文件场景无此形态。

治理路径建议：gdb+分步插桩对拍法（lessons 先例）→ 向量$Token 收录修复 → ulimit 语义决策（运行时栈上限是否编译器职责）→ 组件对拍 21/21 + 字节级固定点重建。

**教训**：本轮 stash 二分实验链（temp/temp2/t3 三次）曾把 driver/module 修复**静默回退**（git status 才发现）——**stash 实验后必须 git status/diff 核对改动集**，防「修复丢失假恢复」。

## 四、灰色点披露

- 生成表达式.cn 1261 行超线（单函数 1080 行所致，函数级拆分独立轮）。
- abi辅助.cn（13 行 v1 探针）check 单文件 2 错=HEAD 既有，非组件排除出冒烟；建议后续转正或删除。
- 语义尾段消歧的「两段模块导入优先于符号导入」在「同名函数与模块」极端形态下取模块语义（Rust 按 def 分派，我们按 knownModules_ 近似）——当前用例矩阵无冲突。

## 五、下一步（优先序）

1. **v2self 锚定专项治理**（见三）。
2. 生成表达式 函数级拆分（1080 行单函数）。
3. 跨机轮：win-x64/arm64 全量复验（宿主 C++ 与 v2 源码本轮均大改）。
4. specs/08 目录包/模块声明语义条文批注。

---

写给无上下文的新会话：接手先读本文件第三节（v2self 两项根因）+ plans/018 第五节注记。当前门禁全绿可随时提交；v2self 锚定未过前，**不要**宣称自举闭环达成。
