# HANDOFF 交接文档

**交接时间**: 2026-09-07 第二十九轮收官（单位机 麒麟 ARM64）——**跨机轮 arm64 半程收口**：拉取 7055176（21 提交：分号专项+plans/016/017 全部变更）后按上轮「下一步」执行复验，两枚跨机首跑暴露的缺陷经用户裁决（均方案A·Rust 准绳）当轮根治，v2self 三级锚定在 arm64 首次达成。

## 本轮已完成（全部有实证）

### 一、门禁基线（arm64）

清场重建（GCC 零警告 -Wall -Wextra -Werror）+ 单测 **1243/1243**（+3 锚定测）+
**全量 E2E linux-arm64 160 用例 156 过/1 败/3 跳**——败=78_chain_build（v1 链内存
超限 4126MB 被内存保护终止，已知遗留非回归）；跳=62/69/79（平台限制）。**24 例
v2 闭环（含 T3 解锁 23 例+158）首次 arm64 动态全绿；145/146 分号专项负路径 arm64
首验**。154 修复前基线为 155 过/2 败（78+154）。

### 二、缺陷 1 根治：E2E 154 arm64 首跑失败（第二十二轮 linux_x64 缺陷的 arm64 孪生）

1. **现象**：期望 87654321/8/87654321，实得 5497169789127654321/549716978912/
   87654321；垃圾值 0x7FFDAF6CE0=被调方保存的旧 x19（栈地址）。
2. **根因**：arm64 后端 emitParamSetup 标量/浮点栈参旁路**硬编码锚定基 16**，
   无视 needHiddenRet（sret/i128/u128 返回时 prologue 多压 `stp x19,xzr` 16B，
   真实栈参在 [x29,#32]）；结构体栈参路径恰走 parameterRegister=正确——
   第二十二轮修 linux_x64 三处时以其为「同构对照」漏查旁路（**对照点正确≠
   全路径正确**）。影响面：v2/stdlib 全源码扫描零踩中（建IR指令 7 参+retbuf
   恰 8 位全寄存器），仓库唯一触发面即 154 本身。
3. **修复（用户裁决方案A·单一锚定源）**：arm64 新增 `stackParamBase()`
   （needHiddenRet?32:16）三处收敛（parameterRegister+标量+浮点旁路）；浮点
   槽序改按参数位号 paramPos 与调用方 `v<位号>` 统一（旧用源序号 i=sret 下
   双重错位）；linux_x64 `intParameterRegister` 栈分支收敛
   `stackParamAnchorBase()`+被证伪注释归真（原「与 rbx 压栈无关」叙事）。
   新增单测 3 项（tests/unit/codegen/test_arm64_codegen.cpp）。

### 三、缺陷 2 根治：v2self 锚定链链接失败（v2/宿主静态槽非对齐）

1. **现象**：v2self_gen1.o 链接 `R_AARCH64_LDST64_ABS_LO12_NC against '.data'`
   截断——arm64 全链自举首次链接即现形。
2. **根因**：v2 ARM64 后端静态槽（本地标签 `_cn_gstatic_N`）紧跟任意长度字符串
   `.byte` 无对齐 → GAS 折叠成 `.data+非对齐addend`（readelf 实证 0x2b9a）→
   64 位 ldr/str 的 LO12 重定位要求 addend 8 对齐=链接必败；宿主同位置无对齐
   靠**全局符号**（命名重定位绕过汇编期检查）侥幸可链接，但运行期静态访存全
   非对齐（性能损耗）。x86_64 RIP 相对无此限制（深度机三轮全绿）=平台差异
   掩盖面第二例。
3. **修复（用户裁决方案A·双侧归正；Rust 对照：rustc/LLVM 逐全局按 align_of(T)
   发射 .align=发射期布局契约，非对齐访存=UB 绝不靠链接器宽容）**：v2
   代码生成ARM64.cn 静态槽区+宿主 arm64 后端静态槽循环前各补 `.align 3`。
   定位链：readelf -sW（符号落位）→ objdump -dr（引用点）→ 对位 .s 源 →
   手动插 .align 3 重汇编链接运行全通（假设验证闭环后才动源码）。

### 四、v2self 三级锚定 arm64 首达（本轮核心成果）

13 组件冒烟×2 全绿（**IR容器 子链转绿跨平台成立**——第二十八轮修复三平台一致）
→ 组件对拍 13/13（词法/IR常量/abi辅助/语法分析 4 裸逐字节 + 9 文件 strData
平移规范化一致）→ 全链对拍 PASS（v2p vs v2self_gen1 规范化一致）→ **字节级
固定点：g1=g2 裸逐字节一致（299587 行 asm）** → 8 用例运行级对拍 8/8
（119=14/123=85/126/128/130/148/150/158=0，rc 与产物规范化双对）。
锚定三件套（anchor.sh+norm.py+产物）target/dbg/p29/（不入库）；norm.py 规范
ID=**内容排序秩**（跨文件确定命名空间）。

### 五、文档收口

plans/014 第二十九轮行 + plans/017 第十九节 + 更新日志 + lessons 三条
（①同族修复旁路穷尽=对照点正确≠全路径正确；②静态对齐=发射期布局契约；
③假对拍第五案=规范化基准必须跨文件确定——本条为本轮 norm.py 首版基准
用「文件内首次出现 ID」致 9 文件假 FAIL，改内容排序秩后全过，假阴方向）。

## 下一步

1. **跨机轮 win 半程（家机）**：ci.ps1 全量门禁复验（构建零警告+单测+E2E
   win-x64 全量）——含本轮两修复的 win 侧回归确认：①linux_x64
   intParameterRegister 收敛为纯死分支加固（行为零变化）；②arm64 .align 3
   不触 win 后端；静态论证+家机门禁双确认。
2. P6b 模块系统（模块名$ 前缀+纯名调用遮蔽语义对齐）：**新增语法须用户审批
   后方可启动**。
3. 远期观察项：宿主-v2 strData 驻留序对齐（终极逐字节对拍专项）；宿主 arm64
   .data 浮点常量池（.word/.quad）同位置对齐（本轮仅静态槽区归正，浮点池
   非对齐访存=同族性能观察项，随数据段布局专项收口）。

## 灰色点披露（持续有效）

- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（asm 相对 cwd；
  IR签名 读 stdlib/ 相对路径）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- **strData 驻留 ID 宿主-v2 差 1**（既有观察项，非缺陷）：产物内自洽零语义
  影响；v2self 家族内部（g1=g2）字节级稳定。
- v2 arm64 后端字符串常量发射为逐常量一条 `.data` 段指令（数百条 .data 伪指令）
  ——GAS 合并无害，属可读性观察项。
- 编排对拍纪律：必须核对 `[p6h] 入口:` 行（路径正确）+ `[p6f]` 回退行不得出现；
  对拍三件套（脚本+判定工具+基线产物）必须同目录随迁；规范化工具的映射空间
  必须跨文件确定（内容派生而非位置派生）。

## 踩过的坑（已入 lessons）

- **同族缺陷修复旁路穷尽**（权重 9）：修 linux_x64 栈参锚定三处时引用 arm64
  parameterRegister 作「正确对照」即断言 arm64 无恙——漏查两条不走该函数的
  旁路；三后端同 IR 语义的 ABI 实现，修复后必须逐锚点/逐寄存器对表穷尽。
- **静态数据对齐**（权重 9）：本地符号引用被 GAS 折叠成 .data+addend，
  LO12 重定位有对齐硬检查；全局符号侥幸可链接≠布局正确；新数据段发射必须
  自带对齐指令（LLVM 逐全局 .align 模型）。
- **假对拍第五案**（权重 6）：规范化基准用「文件内首次出现 ID」=基准本身带
  平移 → 9 文件假 FAIL；规范 ID 必须由内容派生（排序秩）。
- 历史：平台跳过掩盖（解锁日=欠账日）+假对拍前四案（cwd 陷阱/判定行自比/
  入口回退/路径依赖未随迁）见 lessons 各轮条目。
