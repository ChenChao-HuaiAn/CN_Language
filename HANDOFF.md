# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：①本机节（自己机器最近交接）→ ②深度机节（开发主线最新进展）→
> ③根目录 `三机任务看板.md`（三机并行唯一看板，开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-12 夜——
1. **三机并行协作协议落地**（本会话，用户批准方案实施）：AGENTS.md 新增 §8（验证/开发二分 +
   看板认领 + 分机分区 + 契约广播 + 冲突兜底）+ §6.2/6.4/6.5 修订（更新日志/HANDOFF 按机器
   分节；**远程统一规范：`gitcode`=主仓库 + `github`=镜像，三机同名不设其他 remote 名，
   每次提交后两远程同时推送**，github 网络不通说明即可下次补推）；新建根目录 `三机任务看板.md`
   （**三机并行唯一看板**，与任务类型无关——用户裁决不放在 plans/019 专项文档里）+ plans/019
   跟踪表拆 7.1/7.2/7.3 三机分段；HANDOFF.md 与 更新日志.md 分机三节迁移（本文件即
   迁移后形态）。此后三机并行以看板为准；本机 develop 上游已切 gitcode/develop。
2. **88-a win 跨机验证轮进行中**（另一并行会话负责，勿重复启动）：拉取 9403e29（深度机 86-a/
   87-a/88-a 三轮）后 win 全量门禁重跑——基线：E2E 275 用例（win 侧已知败面=13 双编译对照
   平台限制 + 78/79 OOM 家族）、v2self win 锚定链固定点待复锚（X64L 口径 335263）。
   结论出来后由该会话按新协议写入本节 + plans/019 §7.2 家机段 + 看板行更新。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-12 第八十九轮（**深度机 linux-x86_64**）——**89-a 净栈契约出口校验落地
+ 浮点字面量全链根治**（立案来源=88-a 登记观察项「净栈契约出口校验」，HANDOFF 下一轮任务第 3
项）。本轮序列：探针两阶段（自举树全量 + 90 用例批量）验证零误伤 → **探针抓到 1 处真实契约
违反**（88-a 预期「违例面应为零」被证伪）→ 顺藤根治 v2 浮点字面量全链静默跳过缺陷 → 出口
校验正式化（IR 层诊断）→ 防线（E2E 249 双编译对照）→ 反证三件（全命中）→ 全量验证链 →
文档五件套。验证链全绿。

### 一、本轮做了什么（写给无上下文的新会话）

1. **探针零误伤验证（先探针、后转正）**：`生成表达式` 出口挂临时调试输出（入口记进入深度
   → 出口算 Δ → 多压 Δ>1 / 吞外层 Δ<0 / 零增量 Δ==0 分类 + 区间与节点明细）。
   - **零增量 37 处全部为合法 void 方法调用**（v2 树 3 处 = 三后端文件 `调用实参们.清空();`；
     用例 34 处同族）——`生成方法调用` 返回形态=空时 `目标寄存器==0` **不压栈**（「void 不
     压栈」设计）→ **契约口径确认为 Δ ∈ {0, +1}**（0=void 无值、+1=有值）。**若直接上「恒净
     +1」硬断言会把 37 处合法调用全部误杀**——探针先行避免了误伤。
   - **抓到 1 处真实契约违反**：246 用例 `标量浮 == 3.5` Δ=+2、区间 [133,136)、进入深度 0。

2. **缺陷根治：v2 浮点字面量全链（89-a）**：`节点_浮点字面量` 在 IR 层**全部分派点缺失**
   （主循环字面量分支 + `生成字面量` 无浮点分支）→ ①字面量零指令：二元弹栈判定退化（栈上仅
   1 项 → 左右操作数均取 0）→ 汇编 `mov r10,[rbp-8]; cmp r10,[rbp-8]`（寄存器 0 自比）**恒真**；
   ②净栈 +2 契约违反；③静态浮点初值注入落空（87-a 注入经 `生成表达式` 求值 → 无值可返回 →
   `.data` 恒 0 且无写回）。**246 v2 侧此前「通过」=双重侥幸**（恒真恰好命中分位 + 初值 0 未
   被断言）。**修复**：IR 主循环分派 + `生成字面量` 浮点分支（`IR_常量浮点`）+ 共用层
   `浮点文本净`/`收集浮点常量` + 三后端 `.double/REAL8` 文本直通（IEEE754 由汇编器解析——
   编译期零开销；x64 `.align 8` / ARM64 `.align 3` / win `ALIGN 8`）。

3. **出口校验正式化（88-b 转正；探针撤除）**：`生成表达式` 唯一正常出口加契约校验——
   多压（Δ>1）/ 吞外层（Δ<0）两条 `IR层诊断`（消息含 增量/区间/进入深度）→ IR 层错误即中止
   纪律（计数 → 主.cn 检查 >0 中止，不产 asm）；错误路径（`生成调用 返回 0`）不校验。
   性能：1 次函数调用 + 1 次减法 + 2 次比较（出口深度顺带回传省一次 `大小()`）——终版与
   基线**同时段**各 3 连测中位 **11.691s vs 11.611s**（+0.7%，单次波动 ±2% 大于差值=
   噪声内；跨时段 6+6 次测量中位数差 ≤1%）。

4. **防线**：E2E **249_双编译对照浮点字面量全链路**（第十八个对照用例，八形态折 **255**，
   含**真/假双向锚定**）；**反证三件全命中**（A 浮点分派回退 → 246 v2 `[ir2] 多压 增量=2
   区间=[133,136)`；B 吞外层注入 → v2 树 `[ir2] 吞外层 增量=-1` 44 条；C 回退版跑 249 →
   根因诊断 + 下游旧症状〈根因在前、下游在后〉），均已恢复复验。

5. **验证链**：清场重建**零警告** + 宿主产物 md5 逐字节一致（`56f55e1c…`/`6ffd9dc7…`=宿主
   零改动）+ 单测 **1306/1306** + E2E **276 用例 272 过 0 失败 0 未实现 4 跳** + 锚定链固定点
   **338025 行** 两代逐字节一致 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

6. **文档五件套**：plans/019（跟踪表第八十九轮行）+ plans/020（**第三十节 89-a 全节**）+
   lessons（89-a 段，权重 8，预防新增 96~99）+ 更新日志 + 本文件。

### 二、本轮踩坑（已入 lessons 第八十九轮段）

- **探针输入面必须含「用例批量」而非只烤自举树**：v2 树无浮点字面量语料——只烤自举树
  违例面=0（假绿），90 用例批量才 1 击命中潜伏缺陷（246）。
- **「不变的绿灯」也要查**：246 修复前后 v2 侧都是 255（通过），语义天壤之别（恒真比较 →
  真比较）——行为修复的验收必须给机器码级证据（本例 `cmp r10,[rbp-152]` 自比 → `cmp r10,
  [rbp-160]` 双操作数 + `floatData_53: .double 3.5` + 静态槽入口写回）。
- **反证注入的恢复步骤必须单独确认**：首次反证（A）的注入脚本恢复段因脚本串联问题未执行，
  文件留着注入版——教训：**反证后立即 md5 复核源文件与备份一致**（本轮后两次已按此纪律）。
- **对拍/锚定链运行期间严禁修改 v2 源码**：对拍（`cmp29_71.sh`）对每个组件做两次编译
  （v2p / cn_self71 各一次，间隔数百毫秒），而 v2p 编译组件时会**读取导入链上的其它 .cn**
  ——期间改 `生成表达式.cn` 会让两次编译读到不同源码 → asm DIFF（本轮实测 `DIFF IR类.cn`；
  事后同二进制复跑 10/10 一致 + 复现 diff=0 佐证为测量污染，非编译器缺陷）。**纪律：验证链
  串行且期间源码冻结**（文档文件除外）。
- **void 不压栈是合法 Δ=0**：契约口径 {0, +1}——写校验前先普查「合法例外」面（37 处实证），
  不然硬断言直接误杀。

### 三、下一轮任务（按序）

1. **跨机轮（家机 win-x64）**：89-a 浮点发射 win 侧实物验证（`REAL8`/`ALIGN 8`/`floatData_`
   装载三件 + E2E 249 + 246）+ 88-a（短路）/87-a（静态三后端 x64 侧）遗留实物 + 全量门禁复验。
2. **arm64（单位机麒麟）**：linux-arm64 后端复验（89-a `.align 3`/`.double`/`adrp+ldr` 浮点
   装载 + 88-a/87-a 遗留）。
3. **v2 浮点语义边界（新登记·待补面专项轮）**：v2 无浮点指令——面内=字面量装载 + 相等/不等
   按位模式比较；**面外=浮点算术（+ - * /）、大小比较（< >）、负号、打印**（与宿主真浮点
   〈ucomisd/addsd 族〉不等价，静默错值面）。评估：v2 IR 浮点操作码族 + 三后端 ss/sd 指令 +
   语义层浮点分派；或先以诊断拒绝（安全兜底）——**需呈报用户裁决面收窄 vs 补全**。
4. **v2 打印浮点恒 0.000000**（既有打印内置边界）：与 3 合并评估（打印浮点要走浮点格式化）。
5. **v2 静态字符语法**（`静态 字符 x = 'Z';` 语法层不识别——既有限制）：登记待评估。
6. **多文件静态初始化注入**：仅入口模块（含 主）注入——与类/容器静态既有同限制；如需跨模块，
   需「模块初始化函数」机制（登记）。
7. 呈报清单余项维持：B35 字段容器接收者（待用户裁决）、甲（参数 move）/丙（返回位拒绝）、
   阶段 4 第二层第三批（不安全区变硬错误）、v2 诊断无行号、未初始化槽 preFree 观察项、
   行数存量违规面长期整改。

### 四、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 276 用例 272 过/0 失败/4 跳
bash target/p71x64l/run_chain71.sh   # 锚定链（固定点 338025 行两代逐字节一致，md5 d063b698…）
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**
bash scripts/cn_release_parity.sh    # 释放路径两侧对照
# 清场重建（零警告门禁）
rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug && cmake --build target/build -j8
```

> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **手工 v2 探针链**（/tmp/p90probe/{t246,t249,fpret}）：cwd 需有 `stdlib` 软链 + `target/`
> 目录 → `v2p <入口.cn> linux-x86_64` → `as target/v2asm.s -o 主.o` → `g++ -no-pie 主.o
> target/audit2/{io_api,intern_api,runtime,string_api,i128_api,math_api,input_api,file_api,
> time_api,system_api}.o` → 运行取退出码。
> **反证手法**：备份文件（/tmp/p90probe/bak_exp89.cn 为 `生成表达式.cn` 修复版基准）→ python
> 注入 → 重建（**输出到独立路径**如 `/tmp/p90probe/v2p_invA`，避免覆盖正式 v2p）→ 跑用例应
> 复现诊断 → **恢复后立即 md5 复核**（`3f2afed2bf2adcc3a5013550ac3d3d87`）。
> **注意**：改 v2 源码后**必须重建 v2p**（否则测量错位）；调试打印删除后 grep
> `\[dbg-p88b\]\|净栈探针` 复核（本轮已清）。

### 五、诚实边界

- 本轮修复为 **v2 单侧**（宿主同形态恒正确=基准对照，宿主源码零改动——清场重建 md5 一致佐证）。
- **v2 浮点语义边界（新登记）**：面内=字面量装载 + 相等/不等（按位模式）；面外=算术/大小
  比较/负号/打印——**与宿主不等价**，需专项轮（呈报）。
- win-x64（家机）/arm64（单位机）侧 v2 实物复验=跨机轮（浮点发射三后端为同构修改，win/arm64
  实物未验；linux x86_64 已实物）。
- 锚定链固定点 **338025 行**为本轮新口径（88-a 后 335263；+2762=v2 侧 89-a 代码；产物
  `fix_p.s`≡`fix_s.s` md5 `d063b698…`，2026-09-13 复核）；不跨后端
  比较。
- **数字勘误（2026-09-13 第九十轮起点）**：本节原三处写「337973 行」（其一含增量 +2710）
  为笔误——以 `target/p71x64l/work/fix_p.s` 实测（`wc -l` = 338025，与 plans/019 跟踪表/
  plans/020 §30/更新日志三处一致）为准，本文件已改正。
- 前轮登记维持：v2 静态字符语法限制 / v2 打印浮点既有边界 / 多文件静态注入仅入口模块 /
  静态槽不登记自动释放（进程生命周期）/ v2 诊断无行号 / 行数存量违规面。
- 探针与反证脚本留 /tmp/p90probe（不入库）；反证注入均已恢复（`生成表达式.cn` md5
  `3f2afed2…` 复核零残留）。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
