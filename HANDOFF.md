# HANDOFF 交接文档

**交接时间**: 2026-09-09 第四十六轮**进行中**（单位机 麒麟 ARM64 → 家机 win-x64 续作）
**轮任务**: 第四十五轮挂账回归特性开发 = 观察项①（v2 比较层拆 8/7 两级）+ 观察项③（v2 构造字面量赋值位）；途中按缺陷零容忍立案根治**宿主构造字面量三缺陷 D1/D2/D3**（探针实锤的静默错误代码/段错误，第四十三轮呈报「宿主表达式位已支持」与事实不符——呈报事实错误在此更正）。

**⚠ 当前状态：WIP 未完成轮。** 唯一已知红 = E2E **186**（rc=68≠84），**根因已 asm 级钉死、修复方案已设计未实施**（见「二」，预计 30 行内）；其余全部绿（单测 1251/1251 + 179/181/182 回归 + 183/184/185/187）。**未跑：全量 E2E / v2self / 组件对拍 / 文档同步——全部在续作清单。**

---

## 一、已完成且已验证（arm64 实测）

### 1. 宿主 D1/D2/D3 根治（Rust 同构：构造字面量在任意值上下文 = 目标 place 逐字段写，单点 emitStructInitTo，零新 IR）

| 缺陷 | 形态 | 修复点 | 修复前行为 |
|---|---|---|---|
| D1 | `变量 q = 点{...}` 推断声明位 | ir_stmt.cpp `genVarDecl` 顶部回填 `node->typeName = init->typeName`（泛型实例化改写同点位惯例） | 推断枚举无 StructInitExpr 分支 → 单 i32 槽 + 空 srcType；563 分支按空 typeName 查表失败**无条件 return 静默零填**；成员寻址退化（读=常量0/写=丢偏移） |
| D2-ident | `p = 点{...}` 标识符赋值位 | ir_expr.cpp `visitAssignmentExpr` 标识符分支、类深拷贝块后新增特判（byRef 感知 AddrOf/Load → emitStructInitTo → lastExpr_=目标地址） | genExpr 空桩（常量0）落通用 Store 只写 8 字节 → 静默零填 |
| D2-member | `r.右下 = 点{...}` 成员赋值位 | MemberExpr 分支、genExpr 之前新增特判（`memberObjStructType` 推对象结构体 → 字段类型 → isStructType 才进入） | 同上静默零填 |
| D3 | `求和(点{...})` 实参位 | ir_call.cpp 实参循环头部物化 `__arginit` 临时（Alloca+registerVarSlots+emitStructInitTo→传地址；与返回位 `__ret` 同构） | 常量0 被 Cast 成 i64 当地址传 → **被调方解引用空指针段错误 rc=139（内存安全违例）** |

- 语义层本就完备（visitStructInitExpr 穷举纪律第四十三轮已立），缺陷全部在 IR 层 = 静默错误代码家族。
- 验证（target/probe46/，不入库）：assign_init（7/9/2/1/2/1 rc=21）、corrupt2（777/888/9——单槽错配越槽写形态）、arg_pos（34，段错误消除）、member_target（9/8/3）全绿；**缺字段负形态仍拒**（check rc=1+诊断）。
- 下标位（`a[1] = 点{...}`）/返回位/嵌套（宿主 emitStructInitTo 递归）既有 ✓——E2E 183 全矩阵锚定。

### 2. v2 ①比较层拆两级（规范 01b§6 对齐：8=比较 7=相等，关系更紧）

- `解析表达式.cn`：层级表 6=相等(==/!=)、7=比较(< > <= >=)、8=移位、9=加减、10=乘除、**原子=11**；层号硬编码 5 处同步（`层 == 11` 判定+前缀/位非递归 4 处）+注释更新。
- `a<b == c<d` 现按 `(a<b)==(c<d)` 分组（合并单层左结合曾与宿主相反）。E2E 185 绿（校验和 rc=14，合并层=13 可区分）。

### 3. v2 ③构造字面量赋值位（主体完成，收尾阻塞见「二」）

- `解析语句.cn` 新单一助手 **`解析构造字段赋值们`**（左值链全合成模板：标识符+模板成员链+字段成员；穷举/重复纪律与诊断文本与第四十三轮逐字一致，179/182 锚定）：
  - **声明位重构**：解析变量声明语句 内联循环改调助手（181/182 回归绿）；
  - **Form A 标识符目标** `p = 点{...}`：P6b 赋值分支钩子（守卫=RHS 标识符是已登记结构体+目标是结构体变量，否则保持旧路径可见失败）；探针 G 独立验证 rc=79 ✓；
  - **Form B 成员目标** `r.右下 = 点{...}`：表达式兜底 P6c 分支钩子（单跳成员+非方法标记；中间偏移复用解析成员后缀标注；字段类型经 `查字段类型ID`）；
  - **嵌套构造下钻**（186 踢出的新缺口，宿主 emitStructInitTo 递归同构）：字段值= `类型{...}` 且类型匹配 → 链+字段递归展平（Rust 嵌套字面量=链式字段投影）；探针 F 经成员实参通道验证写入全对（rc=210=期望 1234%256）。

### 4. E2E 183~187 已建+注册 run_e2e.py

183_构造字面量形态矩阵（宿主）/184_比较层分级（宿主）/185_v2_比较层分级（rc=14）/186_v2_构造赋值位（rc=84，**当前红**）/187_v2_构造赋值穷举负测（None 通道）——**183/184/185/187 绿**。

## 二、唯一阻塞：186 红（根因钉死，修复就绪未实施）

**现象**：186 rc=68≠84（s1=0：`p = 点{7,9}` 的写入被踩成 `{0,9}`）。
**根因（H7 探针 asm 级实锤，target/probe46/v2_nH7.cn）**：`取成员宽度码`（IR生成.cn ~143 行）只处理对象=标识符/调用两形态，**对象=节点_成员（链式）无分支 → 回退 T_整64 → 叶字段 64 位存储（`str x10`）越界 4 字节**；H7 中 6 的 8 字节存储落在 [-236] 恰覆写相邻 p.x（[-232]）→ p={0,9}。对照：1 跳成员写（p.x=7）是 `str w10` 32 位 ✓；链的**地址**全对（-248+8+4），**只有宽度**错。
**修复（约 30 行，IR生成.cn，设计已定稿）**：
1. 新增 `解析成员链结构体ID(池, 索引) -> 整64`：对象=标识符 → 既有 `解析对象结构体ID`；对象=成员 → **递归**解父链结构体ID → `查字段类型ID(父, m.文本)` → `查结构体类型`（字段类型表第四十三轮已全字段登记，键基域幂等）；对象=调用 → 既有 `解析调用返回结构体`（查结构体大小!=0 守卫保持）；其余 → 0。
2. `取成员宽度码` 主干替换：`结构体ID = 解析成员链结构体ID(池, m.子起点); 如果 (!=0) 返回 查字段宽度码(结构体ID, m.文本); 返回 T_整64;`——对旧 标识符/调用 形态行为逐点等价（大小==0 落 T_整64 不变），新增 成员 形态。
3. 修复面=grep 穷尽：`取成员宽度码` 消费点（生成语句.cn ~353 赋值存储宽度；如读路径同消费则同享修复）。

## 三、续作清单（家机 win-x64，按序）

1. `git pull` 本 WIP 提交。
2. 实施「二」修复 → 重建 v2p → **E2E 186 绿（rc=84）**；探针族复验：target/probe46/v2_nG.cn=79、v2_nF.cn=210、v2_nH5.cn=79（v2_nH7.cn 期望由 9 转 79）。
3. **win 门禁**（ci.ps1）：构建零警告 + 单测 1251 + 全量 E2E——宿主 D1/D2/D3 与 v2 ①③ 的 **win 侧首验**（诚实边界：本提交 arm64 实测）。
4. **v2self 锚定**：v2p(win) 编译 v2 自身 → fix_p → cn_self 链接（cn_self.o 在前+v2p.o 借链+`-Wl,-z,muldefs`/win `/FORCE:MULTIPLE`）→ fix_s → 固定点逐字节（**win 侧口径首立，行数不跨后端比较**；arm64 侧复锚=下轮单位机）；绑定自检用 `-Wl,-Map` 查 cn_main 落点。
5. 组件对拍 29/29（v2p vs cn_self，asm 逐字节）。
6. **文档收口**：plans/014 第四十六轮状态行 + 更新日志 + plans/018 工作流1 三个复选框核对勾选（§5.9.1/§5.9.7 条文**内容已在 plans/001**，仅勾选滞后）+ HANDOFF 重写 + 最终提交。
7. **本轮新发现的 v2 既有缺陷/边界，入 plans/014 观察项（未根治，勿忘）**：
   - a. **成员结构体作值读写段错误**：`r.左上 = a` / `点 t = r.左上`——零构造字面量参与即 rc=139（探针 v2_nE.cn 实锤）——v2 结构体值语义前漏洞，独立缺陷轮立案；
   - b. 链式成员访问 `a.b.c`：解析层偏移不可标注（第二跳扁平回退失配）→ IR 层「成员字段偏移未知」可见失败（既有 B5 注记已知面；本轮修复「二」后，**脱糖产物**带偏移链写可用，用户手写链仍走可见失败）；
   - c. `变量` 关键字在 v2 词法表（词法分析.cn:78）但解析层无声明分支——`变量 x = ...` 不可解析，宿主支持（语法面分歧）；
   - d. 全局位构造字面量 `点 g = 点{...}`：宿主解析层拒绝（顶层声明不接受类型前置结构体变量）——双侧一致边界，低优先。

## 四、验证链（arm64 已验命令，win 侧对应改写 ml64/link）

```bash
# 宿主快速验证（arm64）
cmake --build target/build -j8; ./target/cn_unit_tests 2>&1 | tail -1
python3 tests/e2e/run_e2e.py --filter 183_ --cn target/cn --target linux-arm64   # 183~187 同式
# v2p 手动重建+探针（E2E 185/186/187 步骤1 自动重建 v2p，单跑即含）
./target/cn build CN语言编译器v2/主.cn --target linux-arm64 --output target/probe46/v2p46
./target/probe46/v2p46 <探针.cn> linux-arm64 && as target/v2asm.s -o x.o
g++ -no-pie x.o target/rt46/*.o -o x && ./x   # rt objs 现编：g++ -c -std=c++17 -fno-exceptions -fno-rtti -DCNRT_LINUX_MAIN -Isrc src/runtime/<模块>.cpp
```

## 五、诚实边界

- 本提交 = WIP：186 已知红（根因+方案钉死）；全量门禁/v2self/组件对拍未跑，续作清单 3~5 补。
- 宿主改动与 v2 改动均 arm64 实测；win 侧首验=续作家机轮；X64L 侧=深度机轮。
- 探针产物 target/probe46/、rt46/ 不入库（target/ 已 gitignore）。

## 六、本轮踩坑（防再犯）

1. **管道 tail 掩盖 rc 三连复发**（lessons 第四十三轮权重7）：`v2p 编译 | tail -1 && as ...` 编译失败仍链旧产物——rc 必须独立判定（`$?` 在管道后取的是 tail 的）。
2. **python 受控替换前必须核对 old_string 与文件当前状态**：一次 restore 后 old_string 仍带着已删插桩行 → count=0 静默跳过——替换计数必须打印并核对预期。
3. **嵌套构造写入的运行级验证唯一通道=成员实参**（`求和(r.左上)`）：成员作值读（三.7a）与链式读（三.7b）都不可用——E2E 186 据此设计。
4. v2 表/键域速查（本轮实证）：查结构体类型/查变量结构体键/查字段类型ID 全在键基域幂等；`查变量结构体键` 未命中返回 **-1**（不是 0）；成员方法标记=-2；`取成员宽度码` 对象=成员曾是盲区（本轮修复点）。
