# HANDOFF 交接文档

**交接时间**: 2026-09-03 第八轮（③ 第三波：**指针下标读写**，E2E 130 六步闭环绿；
**宿主内存模型统一 C 布局**+验证期四簇缺陷当场根治（用户重申开发纪律令：
宿主缺陷优先/全部根治后再推进/禁止下轮再说）；容器/数组重载回归绿；
全量门禁结果见文末）

> 本轮上下文：家机拉取单位机 1e8e6ea → win-x64 对称门禁首跑踩出 129 win 首败
> （v2 movsxd 缺 dword ptr——GAS 无此歧义、单位机仅跑 ARM64）→ 按 HANDOFF 计划
> 立项 E2E 130（指针下标）→ 验证期按缺陷零容忍纪律连根治四簇 → v2 四层实现 →
> 六步闭环 PASS。**同日第二次踩「池索引 0」坑（lessons 复发）**。

## 本轮做了什么（全部有实证）

### 一、E2E 130：③ 第三波载体（六步闭环 PASS）
- **用例**：`tests/e2e/130_v2_指针下标读写/`（主.cn+主.expected），注册 v2 闭环
  `(["主.cn"], 0)`（不链 v2p.obj——纯标量/结构体+内置，无容器方法依赖）。
- **断言 17 项**：出参簇（&传参+读+循环推进 `当(位置[0]<上限)`）、整32 步进 4
  全链（p[0..2] 写结构体字段+读回+二元下标 `p[3-1]`+**负下标 q[-1]/q[-2]**+
  复合赋值 `p[1] += 5`+写透函数 `写入(&t.甲,77)`）、整64 步进 8、强转操作数
  `整64(p[0])`（语法分析.cn:653 同款）。
- **一致性锚定**：宿主 sanity 退出码 0 输出 `4/77/全部通过` ↔ v2 闭环产物运行
  输出**逐字节一致**（同源双编译器）。

### 二、宿主内存模型统一 C 布局（缺陷③，最深一枚）
- **现象**：`&t.甲`（整32 紧凑字段 0/4/8）的 `p[1]` 读到 丙 的值+垃圾（步进 8）。
- **根因**：三套咬合——数组局部 8 槽/元素 × ptrElemStride 兜底 8「双错互洽」
  （05 的 `q+1` 恰同错而对）vs 结构体/.data/字段内数组 C 紧凑 vs 容器堆
  `max(类型大小,8)` 槽分配。**单改步进必炸**（30_container/113 级腐坏）——
  asm diff（好/坏两构建 3.3MB 仅 7 行 `shl rax,3→2`）锁定 **ir_expr A-3 隐式
  类字段读分支**自带标量 8 槽步进（读走它、写走 ptrElemStride，读写不一致）。
- **改动**：ptrElemStride 统一 typeSizeOf(pointee)；registerVarSlots 数组槽数
  ceil(总大小/8)；数组 init/读/lvalue 三处步进 typeSizeOf；补零按元素宽度
  （i128/浮点/指针保持 i64）；**stdlib 容器.cn+映射集合.cn 9 处分配紧凑化**；
  A-3 读分支同批统一。
- **回归**：30（向量/链表/栈/队列 整32 重载）/113/111/05/09/121 绿。

### 三、三枚汇编/栈缺陷（①②④）+ db 超限（⑤）
1. **宿主窄宽 StorePtr 常量**（x64_instructions emitPtrLoadStore）：`整8(7)` 显式
   强转折叠直达 → `movsx rcx, byte ptr 7`（A2070，movsx 不收立即数）——常量特判
   `mov rcx, <imm>`（写 cl/cx 取低字节=补码截断，负常量同正确）。
2. **v2 MASM movsxd 缺 dword ptr**（代码生成.cn IR_类型转换）：129 win 首败根因；
   **双后端对称验证价值实证**（单位机仅 GAS 侧绿≠win 绿）。
3. **v2 表达式栈卫生家族**：① 取地址分支「清空全栈」弹掉 `f(&x)` 的被调负符号
   （[ir2] 调用被调对象非函数名）；② 负号/逻辑非/解引用操作数「线性+嵌套」双份
   残留——`q[-1]` 弹2取残留当地址 → 0xC0000005（asm `add rax,[常量槽]` 铁证）。
   根治：前缀一元操作数区间并入 取址区间们 预扫跳过 + 全部分支改「弹到进入前
   深度」。**残留长期无害的原因：语句级消费取栈顶掩盖——「弹N」新消费点必翻车**。
4. **池索引 0 守卫复发**（IR生成 参数循环 `>0` 漏首类型节点→指针登记漏）：
   `>=0`+节点类型判定；最小源码（无结构体前置）是天然触发器。
5. **v2 MASM db 单语句操作数上限 48**（129 win 二段错误：首跑被 movsxd 先拦、
   修后才暴露的 53 字节中文字面量 A2042；GAS .byte 无此限）——分段续行发射 +
   **剥离行尾悬挂逗号**（悬挂逗号=不完整操作数同 A2042，48 值行隔离复测铁证）。

### 四、v2 指针下标四层（130 载体）
- 语义：`静态 指针变量表`（名ID→所指类型名ID；类型节点附加=指针层数 1..255，
  参数/局部登记；检查函数 清空）+所指白名单 `指针下标所指合法`（整系/正系/布尔/
  字符/字符串；浮点/结构体/容器/结果显式拒绝=IR LoadPtr 宽度覆盖面）+下标左值
  放行（指针对象；字符串只读不变）+复合赋值左值类型取所指。
- IR：`静态 指针变量类型们`（生成函数 清空；参数循环+变量声明登记）+生成表达式
  下标指针路径（步进=类型名宽度、1 免乘；LoadPtr 类型宽度码；登记 类型码）+
  生成左值地址 下标早退分支（Load 对象槽值+下标×步进）+赋值存储宽度
  `取下标元素宽度码`。
- 自检：v2p 尾部两负测（`数[0]` 整64 拒/`s[0]=65` 字符串写拒）+130 期望固化。

## 用户决策记录（不可推翻，除非用户再裁）
1. 本轮用户重申开发纪律全文（尽善尽美/缺陷零容忍强制汇报/宿主缺陷优先/一劳永逸）
   ——四簇缺陷均按该令当场根治（AskUserQuestion 一次未获回复，按纪律令方向
   执行：宿主缺陷优先根治、禁止规避手段），**未单独获批——已在此披露请追认**。
2. ②c 四项/B4 指针槽模型/方案A 登记点/复合赋值五形态等历史裁决不变；
   本轮无新增语法规范（指针下标为既有规范运算符，v2/宿主实现补齐+内存模型统一）。
3. 容器堆分配策略变更（`max(类型大小,8)`→紧凑）为缺陷③根治必要组成
   （2026-09-02 缺陷4 的分配策略随「指针步进按元素大小」统一而收口）——同上披露。

## 下一步（按 ③ 推进）
1. **语法分析.cn 冒烟续推**：129/130 后崩点应前移——`IO::打印到错误/文件::`
   限定名内置（HANDOFF 前轮已预告，v2p 以 运行时错误(6) 中止=显式失败）。
2. 逐组件冒烟（语义分析.cn → IR生成.cn → …），踩出即立项（131+ 用例先行）。
3. **单位机（linux-arm64）待办**：本轮宿主内存模型统一+四缺陷根治的 ARM64 侧
   门禁复跑（GAS 后端同 IR 改动自动生效，但 30/113/111 等运行时未在 ARM64 复验
   ——诚实边界）；129/130 ARM64 闭环复验。
4. 观察项（未立案，待用户裁）：`a * a = 7` 语句歧义（声明路径解析、有报错，
   C 式 lexer hack 歧义+换行≡空格规范行为）——是否需要诊断增强由用户定。

## 踩过的坑（本轮新增，接手必读——已入 lessons 高权重区）
- **互洽双错**（12.0）：布局/步进/分配三角改动必须三处+读写两侧+静态/栈两侧
  全枚举；asm diff（好/坏构建）是定位行为漂移最快手段。
- **表达式栈卫生**（9.5）：嵌套生成+线性遍历双轨制节点必须预扫跳过或深度相对
  弹栈，禁止清空全栈；新增「弹N」消费点必审计一切生产者压栈数不变量。
- **双后端对称**（8.5）：单侧绿只是半绿；movsx/movzx 立即数×、内存需尺寸前缀。
- **池索引 0**：同日第二次发作——新写池索引守卫统一模板
  `>=0 && <池.大小() && 节点类型匹配`；新功能探针含无结构体变体。
- v2 顶层声明需 `静态` 前缀（映射 等）；静态无初始化（P3-8 入口构造）。
- 换行≡空格（规范）：`*p = v`/`&x` 行首歧义是规范行为——探针程序语句间加分号。

## 门禁状态（本轮最终）
- 全量 ci.ps1（构建零警告+单测+E2E 全量）结果见 `target/ci_run3.log`
  （若 78/79 内存超限败=已知遗留 v1 字符串行 IR；其余须全绿）。
