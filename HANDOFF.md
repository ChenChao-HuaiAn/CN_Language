# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（🧪 M3 采样测试轮·验证轮免锁·2026-09-17）

**任务**：用户令「本机测试轮·尤其不同采样的 M3 测试」——承接 284-a 遗留运行级终验「深度机 M3 回归轨复验（7/7→0 预期）」，本机提前以 win-x64 口径完成独立复验＋新种子采样。基线 de4c0b6（=97ca5c1 284-a 终态代码+深度机 285-a 看板认领文档提交）；深度机 🏃 285-a（380 用例改造）写集零交叠，全程免锁合规。

1. **前置确认**：git fetch 对齐 de4c0b6；`target/Debug/cn.exe` 全量重建（原产物 08:16 早于 97ca5c1 提交时刻 08:27，按「增量构建假重建陷阱」教训不轻信）；402_布尔短路矩阵O3＋402_验证IR旗标触达 E2E 双绿先行确认病灶② 根治在位。
2. **轮 A·回归轨复验（命中库 38 全量）**：总数 38｜一致 38｜分歧 0｜编译失败 0｜运行失败 0——**38 个历史命中样本全部已修（病灶② 284-a 预期 7/7→0 实证达成·win 口径）**。
3. **轮 B/C·不同采样（新种子）**：seed=2026091711 与 2026091712 各 100 新样本（避开深度机同日已用 01~03）——各自 总数 100｜一致 100｜分歧 0｜失败 0；C 轮清残留清单后复放二次验证一致。**三轮合计零分歧·零失败·零新命中·零新类别**（对照：病灶② 修复前新样本分歧率 10%、回归轨 7/7 分歧）。
4. **插曲（已闭环）**：首轮编排曾误读 `target/cnsmith/work/分歧清单.txt` 残留（mtime 09-16 19:53＝258-a 时代 A2070「mem←imm 无尺寸前缀」旧现场·diff 零分歧时不清理不覆盖该文件）致打印 1360 条假命中——逐项证伪（文件不在工作目录/汇总行为权威/复放全绿）后删除残留。**daemon 改进建议（待开发轮采纳）**：①`cnsmith_daemon` 支持 `--seed` 自定义（现写死 日期×100+轮号·三机同日必撞种子浪费算力）；②差分零分歧时也写空清单/清旧清单（防后读脚本误读）；③**daemon 的 `target/cn` 滞后基线陷阱**（深度机 285-a 教训自证）：拉取并重建() 只重建 `target/build` 而采样用 `target/cn`·HEAD 前进后不随建→假采样数据（深度机 daemon 08:50~08:53 四轮基线标 de4c0b6 仍报「分歧 17」即此假象）——daemon 应改为重建后同步刷新 `target/cn` 或直接用构建产物路径。
5. **交叉甄别→【定案】（rebase 深度机 285-a 收工 6d61f19 + merge 4ad94f6 后·asm 三件套实证）**：深度机两次判定（285-a 收工「O0 对 O3 错·284-a 修复未覆盖」→4ad94f6「O3 IR=常量布尔无条件存储 __sc·继续修=优化层补路径」）**均经本机实证反转**。定案=**新缺陷：linux_x64 后端 O0 移位发射操作数宽度缺陷（零扩展污染）**——实证三件：①本机出 s2026091705 的 linux-x86_64 O0 .s（315 行附近）：`mov r10d, [rbp-592]`（**32 位装载=零扩展**·负数 -426 → 大正数 4294956821）+`and ecx,31; sar r10, cl`（**64 位移位**）→ 结果大正数 524286 → `1<=524286` 真 → 变7=1（与深度机 linux O0 实测吻合）；②同 O3 .s：`mov r10, -426`（**64 位符号装载**）+sar → -1 → 变7=0（**规范正确**·plans/001 位运算条「移位量按位宽取模」：-5875 mod 32=13·-426 sar 13=-1）；③win O0：`sar eax, cl`（32 位寄存器）→ -1 → 变7=0（win O0=O3=0 双对）。**判定反转**：变7 规范值=0；错侧=linux O0（非 O3）·修复面=linux_x64 后端发射层移位装载（32 位值须用 32 位 sar 或 movsxd 符号扩展·非优化层折叠路径·与 284-a termCondition 无关——深度机 4ad94f6 被「__sc 无条件存储」IR 形态误导）；缺陷面初判=**有符号右移（sar）32 位负值·O0 直译路径**（shl 取低 32 位免疫零扩展·win 免疫·arm64 待查）。**E2E 420 全绿未捕获=存量用例无「32 位负值 >> 运行时移位量」linux 运行级形态——M3 采样独家捕获价值实证**。样本留命中库回归轨·修复轮后应 7/7→0 达成（win 已达成·linux 待修）。plans/021 T12 行深度机判定留档（4ad94f6）需开发轮修正注记（验证轮写面外·本轮以 HANDOFF 定案承载）。
6. **写集与清理**：入库改动＝采样日志 3 轮＋甄别注记＋本节＋更新日志本机节＋看板行＋merge 4ad94f6；`target/m3_round_ai.py` 编排脚本用毕删除（s1705 甄别中间产物 s1705_*.s/.txt/.exe 留 target/ 待修复轮复用·不入库）；源码/测试/规范零改动（纯验证轮）。

**下一轮候选**：①**【缺陷·零容忍·已呈报】linux_x64 后端 O0 移位发射零扩展污染根治**（32 位负值 sar 装载路径·家机可修——深度机 4ad94f6 亦判「源码跨平台·家机可修」·修后运行级复验归深度机·同轮姊妹面排查=arm64 后端同形态+左移/无符号右移免疫性实证+E2E 负测转正+plans/021 T12 行判定修正）→②daemon 三改造（--seed 自定义/零分歧清清单/target-cn 同步重建·可搭车）→③C20 波2 批B sem5 stdlib 32 上架（家机·前置已清）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 缺陷族面扩散检验轮收工（验证轮·免锁不占轮次号·基线 3e21ca0·与单位机 287-a 预登记/家机 M3 轮零交叠）。接手前先 `git fetch`。

### 一、本轮做了什么（用户令「点→线→面」制度性检验首案）

1. **方法论建成（famscan·/tmp/cn_family/famscan.py）**：用户判断 021 缺陷多为随机发现的**点缺陷**，真根因的**面缺陷**无制度性发现机制。本轮建成「缺陷族面扩散探针」：已知点缺陷→提炼根因模式→按扩散轴（**求值位置×类型位宽×消费指令×符号性×优化级别**）枚举同族全形态→**双 oracle**（gcc 同语义 C 程序对拍+python C 语义模拟互检）→CN O0/O3 实测比对→跨后端 asm 静态定性。
2. **首案实测（4 族 47 用例）**：以 s2026091705（287-a 侦查点）/T13/T14 三点缺陷为种子扩散——**T23 立案：linux_x64 有符号右移负值 14/17 形态错**（详见 plans/021 §三-G T23 行）：11 形态 O0+O3 双错（局部=已知点＋**静态变量/数组元素/结构体字段/函数实参/三元值位/负移位量/整16×2/整8/INT_MIN/整16边界 10 个新形态**）+3 形态 O0-only 错（返回值/复合赋值>>=/负字面量——IR 实证 O0 不折叠走运行时发射，O3 折叠语义正确）。
3. **根因与修复边界（asm 铁证）**：17 处 sar 发射全同模板 `mov r10d`（32 位零扩展装载）+`sar r10,cl`（64 位消费）=**emitShift 单点病根**（面宽但挂点同源，单点修复可覆盖）；窄型（整8/16）另缺类型位宽分派；win（`sar eax`）/arm64（`asr w9`）32 位寄存器运算免疫（asm 静态实证；窄型跨机运行级待复验）。**无病面实证 31 用例**（修复不扩大化）：整64 右移/左移全家/除法/取模/乘法/比较/位运算/判真/转整64/转浮64/浮32（T14 修复在位）/**无符号姊妹面正32/正64 全 12 例**（div/取模/右移/64 位高位比较/超大值/回绕/转换全对）。
4. **立案与呈报**：T23 落 plans/021 §三-G＋处置建议（287-a 修复轮必带 16 形态扩面复验+famscan 复跑归零+E2E 转正）；制度固化三落点呈报 §三-B **B9**（AGENTS §8 扩条/探针转正 scripts//plans/026 §2.11——与 B8 面台账互补：B8=构件面自顶向下，本制度=根因面自底向上）。
5. **对家机 M3 定案的印证与细化**：本轮实证支持家机 s2026091705 定案（linux O0 移位发射零扩展污染），并细化两点——①**变量路径 O3 同错**（s2026091705 的 O3 对是因其左操作数为字面量走立即数装载；变量装载双级别同模板）；②位置/类型面远宽于侦查点。T12 行既有留档修正归开发轮（本轮不越界）。

### 二、验证链（本机复现口径）

```
python3 /tmp/cn_family/famscan.py     # 4 族 47 用例×O0/O3×gcc 对拍（命中清单=fam_hits.json）
# 原始输出留档：rt_a.out(O0)/rt_o3.out(O3)/rt_c.out(C)——asm: fam_o0.s/fam_o3.s/fam_win.asm/fam_arm.s
# 基线：target/cn @3e21ca0（285-a 终态源码·3e21ca0 纯文档轮无需重建）
```

### 三、下一轮任务（按序）

1. **287-a 修复轮（家机或单位机·已预登记）**：emitShift 符号扩展单点修复+窄型位宽分派——**必带 T23 的 16 形态复验**（修复后 famscan 复跑应 0 命中）+E2E 矩阵用例转正+T13 随同根销项核验。
2. **深度机候选**：①287-a 修复后的运行级复验（linux 侧口径）②B9 制度获批后的 famscan 转正实施③面台账 B8 巡视续行。
3. 跨机复验请求：窄型（整8/16）移位运行级=win 家机/arm64 单位机（跑 /tmp/cn_family/fam.cn 三分钟出·asm 已留档佐证）。

### 四、诚实边界

- 双 oracle 中的 python 期望曾写反一处（F3_07 比较）——被 C 对拍当场纠正（双 oracle 机制的价值实证）；C 参考首版 printf 提升缺陷曾致 5 条假命中，已修复并三方原始输出直比定案（rt_*.out 留档）。
- 轮间曾现「同源码同级别输出互换」假象=旧二进制残留混读，已以「重编译两次一致+原始输出存档」排除非确定性；教训=多轮探针实验须每轮落 .out 存档再比对。
- 窄型跨机（win/arm 运行级）未验——asm 静态推断留复验请求；v2 侧（v2p）同形态未测（287-a 静态审查判免疫·随修复轮复跑覆盖）。
- famscan 在 /tmp 未入库（B9 待批）；本轮写面=021 最小局部编辑+HANDOFF/更新日志本机节+看板本机行，源码零改动。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🏃 286-a 开发轮收工（看板交锁）——**S1b 采样日志分文件化根治（用户质询+令「现在修改」）+M3 采样 arm64 首轨（独家捕获 T22·呈报待裁）**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **S1b 根治（用户质询单文件爆炸·2509 行/108KB·≈1~3 万行/天趋势）**：①daemon 日志改**按月分文件**（tests/cnsmith_hits/日志/YYYY-MM.md·翻月自动建头）+「变化时记/定时汇总」记法（连续零新存/零已修/命中集合同/基线未变的轮次合并一行汇总；任何变化独立成轮完整记录）+命中列表去重；②存量 2583 行原样迁移 2026-09.md（家机/深度机在飞增量折入+残条补注）；③采样日志.md 改**固定指针**（≤15 行·外部引用路径不失效）；④check_handoff.py 扩采样日志门禁（指针行数/轮次条目/月文件名/单月 5000 行上限）——**反例三连拦截+正例 PASS 双向实证**；⑤两防御（家机 492e976 建议采纳）：差分前删旧分歧清单（防零分歧残留清单假命中 1360 条教训）+重建后 target/cn 新鲜度核验（防假重建滞后基线）；⑥种子改时间戳到秒（根治重启撞号——深度机 seed=01/02 反复实证同批样本重采）。
2. **M3 采样 arm64 首轨（三轮·每轮 100 新样本+回归轨）**：新样本 351·输出分歧 0·编译失败 0·**运行失败 65 样本=独家捕获 T22**；回归轨在 arm64 上 11 命中（全部=T22 形态·非 T12 复发）/28 未命中（T12 形态 arm64 本就不呈现）；31 命中样本自动入库 run_err/（T22 证据+根治轮现成回归轨）。**arm64 移位零扩展定点探针**（82bdbdd「arm64 待查」补位）：-426>>13 O0/O3 双=-1 对=**免疫实证**（零扩展污染=linux_x64 O0 单侧）。
3. **T22 立案呈报（缺陷零容忍·方案待用户裁决）**：arm64 -O3 位运算常量立即数非法汇编——orr/and/eor 直发 #425/#524/#688/#0 等非 bitmask 常量→as「立即数越界」**拒编译**；O0 全对·win/linux_x64 免疫；最小复现=甲|425/甲&0 O3 as 崩·O0 4950/0 对；挂点=arm64 逻辑立即数编码器缺失（52-a movk 取片/D20 x64 移位立即数同家族）。详见 plans/021 §3-G T22 行。
4. **收尾**：plans/021（戳 286-a+T22 立案+S1b 销项 §4.2+§5 索引+B 区裁决迁出）+本表+更新日志+看板收工+通告段（daemon 结构变更广播）。

### 二、发现的问题

- **T22（新缺陷类别·呈报待裁）**：见上；31 样本已入库=根治轮现成回归轨素材。
- daemon 两观察项（不阻塞）：①cnsmith_diff 把汇编失败记为「O3 运行失败」（分类应区分编译面失败）；②回归轨=命中库全量→滚雪球 38→100（机制自洽=防复发语义·但新样本配额被挤压）。
- 本节原有 `## 单位机 ARM64 节` 标题连续两行粘连残留——本轮整节替换顺带根治。

### 三、下一轮候选

1. **T22 根治**（用户裁决方案后任一机认领；arm64 运行级复验归本机·31 样本回归轨现成）。
2. s2026091705（linux_x64 O0 移位零扩展·82bdbdd 定案）根治后跨机复验。
3. daemon 两观察项+新种子轨常态轮（本机轨=arm64 后端互补证据·采样量有限）。

### 四、验证链（本机复现口径）

```
python3 -m py_compile scripts/cnsmith_daemon.py scripts/check_handoff.py    # 语法
python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py  # 双门禁 PASS
python3 scripts/cnsmith_daemon.py --rounds 1 --samples 50                   # 采样单轮（月文件+指针+门禁联动）
# 编译器源码零改动（286-a 全程）→ E2E/单测基线=82bdbdd（285-a 全绿 421=419/0/2）未重跑；
# 构建=target/build 全量重建 09:16（rm -rf 清场·探针 rc=0 实跑验证）
> github 镜像本机无凭据未推（170-a 起累积·既有口径待他机代推）。
```

### 五、诚实边界与坑

- **批量编辑文档必须先备份+行级锚点断言**：本轮 plans/021 曾因正则 `[^|]+` 跨行吞并+拼接漏尾段**截断文档尾部 98 行**（git checkout 恢复重做）——教训：`[^|]+` 会匹配换行符、字符串拼接必带尾段、大批量编辑前 cp 备份。
- arm64 采样三轮仅 ~5 分钟（vs 深度机 8s/轮）——本机采样量有限，M3 计时主力仍在深度机 daemon，本机轨=**arm64 后端互补证据**（首轮即抓到 x86_64/win 双免疫的 T22=互补价值实证）。
- 回归轨 1 样本与新种子样本撞名（s2026091721·两代生成器名空间重叠）·对结论无影响。
