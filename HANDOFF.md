# HANDOFF 交接文档

**交接时间**: 2026-09-07 第二十八轮收官（深度系统 x86_64）——**plans/017 两灰色点根治（协议常量+节点布局单一归属补全 与 显式入口 fail fast）+ 文档收口**。用户裁决两项均方案A·Rust 准绳：性能第一/安全第二。

## 本轮已完成（全部有实证）

### 一、缺陷 1 根治：IR容器 子链冒烟 exit=1（第二十二轮披露灰色点闭环）
1. **根因两层**：①簇①A2（协议常量单一归属，2026-09-04）迁移遗漏 5 常量（节点_调用17/节点_成员18/节点_下标19/成员方法标记-2/节点_二元运算15 滞留语法分析.cn）；②布局层——常量补迁后语义过、暴露 IR 层 64 错「成员字段偏移未知」（与 节点 五字段完全一致）：`结构体 节点` 布局滞留语法分析.cn，登记源不在 IR容器 导入链。
2. **修复**：5 常量+节点布局整体迁入 IR常量.cn「AST 节点类型」区（协议常量与协议布局同区，呈报①A 下沉模式）；语法分析.cn 删除本地定义。Rust 对齐 single source of truth+编译期折叠零开销+缺定义=编译期错误。
3. **验证**：13 组件+min 冒烟 **13/13 全绿（IR容器 子链首次转绿）**；组件产物对拍 vs 上代：5 文件裸逐字节一致+7 文件差异 100%=strData 平移（规范化后一致）。

### 二、缺陷 2 根治：入口回退掩盖编排错误（第二十七轮观察项闭环）
主.cn 新增 显式入口 标记：显式入口打开失败 → rc=2 中止不产 asm（对齐 rustc fail fast）；无参自检模式保留回退（设计特性）。验证：rc=2+无 asm ✓/无参 rc=0+回退+asm ✓/E2E 零影响。

### 三、v2self 新一代重编锚定复验
v2p_gen3→v2self_gen3 重编 193068 行（+780）→ 全链规范化一致 + **家族字节级固定点保持（g4=g5 裸逐字节一致）** + 8 用例运行级对拍 8/8 PASS。

### 四、文档收口
plans/017 复选框补勾 T1/T2/T5/T6/T7/T8；plans/016 遗留项勾销注记（两项均已闭环）；plans/014 状态表第二十八轮行；lessons 假对拍第四案。

### 五、门禁
构建零警告+单测 1240/1240+E2E linux-x86_64 全量见提交信息。

## 下一步
1. **跨机轮**：win（家机）/linux-arm64（单位机）全量复验——158 三平台+arm64 动态零变化+两侧 v2self 重编锚定+**本轮修复的两侧冒烟复验**（IR容器 子链转绿应在三平台一致成立）。
2. P6b 模块系统（模块名$ 前缀+纯名调用遮蔽语义对齐）：**新增语法须用户审批后方可启动**。
3. 远期观察项：宿主-v2 字符串驻留序对齐（若要求两编译器产物逐字节一致的终极对拍专项）。

## 灰色点披露（持续有效）
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径；asm 输出 target/v2asm.s 亦相对 cwd）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- **strData 驻留 ID 宿主-v2 差 1**（既有观察项，非缺陷）：产物内自洽零语义影响；本轮节点布局下沉使「组件产物 vs 上代」差异面扩大（7 文件），但全部=驻留 ID 平移（规范化后逐字节一致）；v2self 家族内部（g4=g5）字节级稳定。
- 本轮对拍脚本与产物在 /tmp/v2dbg/p28/（smoke.sh=13 组件冒烟/probe.sh=8 用例对拍/g3~g5=自举代际产物）；新一代编译器产物 target/dbg/{v2p_gen3,v2self_gen3,v2self_gen4}。
- 验证跑管道纪律：基线对拍必须重定向到文件再 grep，禁用管道截断；**复用历史对拍脚本须三件套随迁（脚本+判定工具+基线产物）——norm.py 未随迁致 8 例假 FAIL 已入 lessons（假对拍第四案）**。

## 踩过的坑（已入 lessons）
- **假对拍第四案——路径依赖未随迁**：脚本目录迁移（p27→p28）后 `python3 $D/norm.py` 依赖未随迁 → 8 用例假 FAIL（假阴方向未污染结论）。四案谱系：cwd 陷阱/判定行自比/入口回退/路径依赖未随迁——共同根因=判定链完整性依赖未随脚本迁移。
- 历史：平台跳过掩盖（解锁日=欠账日）+fail fast 双拦截+调用节点子区间 +1 偏移约定（见 lessons 各轮条目）。
