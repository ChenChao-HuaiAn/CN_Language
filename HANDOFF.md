# HANDOFF 交接文档

**交接时间**: 2026-09-03 第九轮（观察项裁决落地：**缺陷② 非左值赋值立案根治——宿主侧已完成**，
单位机 linux-arm64 冒烟绿；**v2 侧未开工**；验证期新发现两枚 ARM64 既有缺陷待分诊）
> **紧急交接**：用户换机（单位机→家机 win-x64）。本轮工作**未完成**，接手会话按下方
> 「下一步」续推。上一轮完整记录见 git log 6d1bedf 提交信息与 plans/014 第三波行。

## 本轮裁决（用户令，不可推翻）

观察项 `a * a = 7` 歧义家族三子项裁决（详见下「背景」）：
**② 非左值赋值静默接受 → 立案根治（宿主+v2 对齐+负测固化）；③ 报错误导 → 顺带做
（rustc「想写分号」同款提示）；① 歧义本身（声明优先解析+换行≡空格）→ 维持规范行为不动。**

## 背景（为什么做这件事）

- 探针实测：`a + 1 = 7`、`a + 1 += 2`、`5 = a`、`常量 c = 1; c = 2`、`c++`、`(a+1)++`
  宿主语义层**全部静默通过**（退出码 0）——visitAssignmentExpr 的 else 分支只查类型不查
  左值性（注释原话「后续Task实现」）；常量无跟踪；++/-- 只查类型不查左值。
- 换行≡空格规范下 `b = a` 换行 `*a = 7` 粘连成 `b = ((a*a) = 7)` 亦静默通过（存储被丢弃）。
- Rust 实测对照（rustc 1.96.1）：E0070 硬错误 +「you might have meant to write a semicolon
  here」提示；rustc 对行首 `*` 同样粘连（无行首一元特权）——②+③ 正是 rustc 在保持 C 式
  续行行为下的实际做法，① 不动与 rustc 立场一致。

## 本轮已完成（全部有实证）

### 一、E2E 用例先行（已创建+注册）
- `tests/e2e/131_非左值赋值拒绝/`：负 e2e（期望编译失败.txt=「赋值目标必须是可赋值的左值」），
  含八种负形态（二元/复合/字面量/自增/常量赋值/常量自增/歧义形态/粘连形态）。**已验证通过**。
- `tests/e2e/132_v2_可写左值全形态闭环/`：v2 闭环正例（变量/成员/解引用/指针下标/复合/自增
  全形态不误伤）+ 主.expected 固化两行 v2p 自检（「[sem5] 非左值赋值负测通过」「[sem5] 常量
  赋值负测通过」）。**当前必败（预期内）——v2 侧未实现，自检行还不存在**。已在 run_e2e.py
  v2闭环用例们 注册 `(["主.cn"], 0)`。

### 二、宿主语义层（四文件，已完成+已验证）
1. `semantic.hpp`：新增 `scopeConsts_`（与 scopes_ 平行的各作用域常量名集）+
   `isConstVarName()` + `reportNonLvalueTarget()` 声明。
2. `semantic.cpp`：push/popScope 镜像维护 scopeConsts_；isConstVarName 实现（与 lookupVar
   同序内→外；局部 常量 查 scopeConsts_，顶层常量按 globalConstValues_ 裸名命中于 scopes_[0]；
   内层变量遮蔽外层常量按内层解析）。
3. `semantic_decl.cpp`：visitVarDecl 尾部 declareVar 成功且 isConst → 登记当前作用域常量集。
4. `semantic_expr.cpp`：
   - **visitAssignmentExpr 左值白名单重构**：标识符（+常量拒绝）/下标/成员/解引用（仅
     Operator::Deref，注意**枚举无 Star，解引用=Deref**——已踩一次编译错）/引用返回调用
     （原逻辑保留）之外的形态（二元/字面量/三元/强转/嵌套赋值/非 Deref 一元）一律
     reportNonLvalueTarget 硬错误；左值失败仍检查右值后提前返回（lastType_=未知）。
   - **reportNonLvalueTarget**：错误信息含目标种类名（运算结果表达式/字面量/…）+
     **诊断③**：赋值号行号>目标起始行号（且已知位置）→ 附「提示：赋值语句跨行——CN 换行
     等同空格，行首运算符会并入上一行；若本意为两条语句，请在上一行末尾加分号」（换行内嵌
     于消息串，渲染为第二行缩进）。
   - **visitUnaryExpr Increment/Decrement**：目标须为左值（标识符[非常量]/下标/成员/解引用；
     引用返回调用不支持与 C 一致）+ 常量自增/自减拒绝。
- 验证：构建零错误；负探针八形态全部正确拒绝（消息+提示实测见本轮会话）；正探针全形态
  （变量/常量读/成员/解引用/指针下标/复合/后缀--/数组下标赋值）语义全过；**E2E 冒烟子集
  05/30/113/130/131 全绿（130=宿主编译 v2 全部源码=误伤检测最强用例）+ 单测 1196/1196**。

### 三、验证期新发现：两枚 ARM64 侧既有缺陷（非本轮回归，stash 对照实证）
正探针运行期发现（git stash 对照：**改动前代码产出同样的错**）：
- **缺陷 A**：`p.y--`（成员后缀自减）存储无效——p.y 保持 3 不变（应 2）。前缀/整型变量
  的 ++/-- 正常（甲++ 26 正确）；仅成员后缀 -- 未生效。
- **缺陷 B**：`数组[2] += 数组[1]`（局部数组下标复合赋值，两侧都是下标读）产出垃圾值
  （547874439173，每次运行不同=未初始化内存）。`数组[1] = 5` 单独赋值正常。
- 定性线索：上轮（家机 win-x64）内存模型统一 C 布局的 **ARM64 侧运行时从未复验**（上轮
  HANDOFF「下一步#3 诚实边界」原文：30/113/111 等未在 ARM64 复验）——大概率是那批改动的
  ARM64 未验证面（或双侧皆有但 win 用例未覆盖此形态）。**按缺陷零容忍纪律须立项分诊**：
  最小探针=/tmp 已删，重写：结构体点{整64 x,整64 y}+p.y--+打印；整64[3] 数组+数组[1]=5+
  数组[2]+=数组[1]+打印。先跑 win-x64 判定单侧/双侧，再 asm diff 定位（lessons 互洽双错条）。

## 下一步（接手会话按序执行）
1. **v2 侧四件（缺陷②对齐，防宿主拒/v2 收判定分歧）**——`CN语言编译器v2/语义分析.cn`：
   a. 新增 `表查找种类(符号表, 名ID)`（仿 表查找类型ID 的作用域栈内→外走查，返回 种类，-1 未找到；
      符号结构体已有 种类 字段，符号_常量=5 已存在）；
   b. 节点_赋值 分支 `n.文本 != 0`（标识符左值）：声明检查后加 种类==符号_常量 →
      「[错误] 不能给常量赋值: 名」；
   c. 节点_赋值 分支 `n.文本 == 0 && n.附加 > 0`（复合/解引用/兜底左值）：现有下标逻辑后加
      左值末.类型 白名单 {节点_标识符（复合合成形态）, 节点_成员, 节点_解引用, 节点_下标（既有
      指针白名单逻辑保留）}——其余（节点_二元运算/整数字面量/负号/取地址等）→「[错误] 赋值
      目标必须是可写左值（变量/成员/下标/解引用）」。注意：左值末=池.元素(n.子起点-1)（区间
      末节点=左值根）；语法分析.cn:928 兜底分支任意表达式+`=` 都造赋值节点=本检查主战场。
   d. 节点_自增 分支：表查找种类==符号_常量 → 拒（v2 解析器仅支持 ++标识符/标识符++，结构
      性无歧义，仅需常量检查）。
2. **v2 主.cn 尾部两负测自检**（仿 源码3/源码4 既有模式）：源码5=「函数 坏5(整64 a) -> 整32
   { a + 1 = 7 返回 0 }」err>=1 → 打印「[sem5] 非左值赋值负测通过」；源码6=「常量 甲 = 5
   函数 坏6() -> 整32 { 甲 = 7 返回 0 }」err>=1 → 打印「[sem5] 常量赋值负测通过」。
   **132 用例的 主.expected 已固化这两行**——做完此处 132 即绿。
3. **宿主单测**：tests/unit/semantic/test_lvalue_check.cpp（仿 test_semantic_error.cpp 的
   analyzeSource 模式）——各负形态精确消息断言（八形态+跨行提示串「在上一行末尾加分号」+
   合法形态零误伤）。注册进 tests/unit/semantic/CMakeLists.txt。
4. **缺陷 A/B 分诊**（上文三），win 侧先跑——家机正合适。
5. **全量门禁**：家机 ci.ps1（构建零警告+单测+E2E 全量）——注意 132 在 v2 完成前必败属预期，
   完成后须全绿（78/79 内存超限已知遗留除外）；单位机侧本轮已冒烟（05/30/113/130/131+单测
   1196 全绿）。
6. 文档同步：plans/014 加行打勾、更新日志.md、lessons（本轮坑：Operator 枚举无 Star 是
   Deref；探针语句间必须加分号——本轮连踩两次）、HANDOFF。

## 灰色点披露（待用户裁，未立案）
- **v2p 语义错误后仍继续 IR 生成**（主.cn:196-218：err 打印后无条件 IR生成/代码生成）——
  现行行为，E2E 只喂正确程序故未暴露；加「err>0 中止」会改 v2p 行为，需单独立项裁决。
- v2 整8/整16 强转白名单未覆盖（上轮诚实边界，不变）。

## 踩过的坑（本轮新增）
- **Operator 枚举里解引用是 `Deref`，没有 `Star`**（词法 token 才叫 Star）——写语义检查
  判 `u->op == Operator::Star` 编译错，改 Deref。
- **探针程序语句间必须加分号**（lessons 已有，本轮自己连踩两次）：行尾标识符/`&p.x` +
  行首 `*` 必粘连；数组声明语法是 `整64[3] 数组`（长度贴类型）不是 `整64 数组[3]`。
- 宿主 build 命令在项目根 cwd 跑（运行时 src/runtime/*.cpp 相对路径）。

## 门禁状态（本轮最终，单位机侧）
- 构建（gcc ARM64）零错误 + 单测 **1196/1196** + E2E 冒烟子集 **05/30/113/130/131 全绿**；
  全量 E2E 未跑（时间截断——上轮单位机全量基线 129 用例 124 过/1 败(78)/4 跳；本轮改动
  为纯语义检查，冒烟子集含最重赋值用例，回归风险低但**全量以家机 ci.ps1 为准**）。
- 132 用例当前**必败（预期内）**：v2 侧未实现，expected 两行自检不存在。
