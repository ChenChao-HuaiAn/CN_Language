# HANDOFF - CN语言编译器项目交接文档

> **交接原因**：2026-08-13 会话结束（**"语句终止与续行"规则文档化 + 测试补充 最终收尾**——临时文件清理、plans 打勾、更新日志/lessons/HANDOFF 三文档同步、git 提交推送）。本文档写给完全没有上下文的新会话看，帮助快速恢复开发。
>
> **前序里程碑**：基础语言缺陷完善A（i128 + 结构体按值 + CopyStruct）、Task 2.8 字符串系统完善B（13 API）、优化器增强完善C（5 Pass + -O2/-O3）、集成验证（14_integration2 + 6 BUG）、Debug 全面审查（9 用例 + 5 BUG）、Task 2.9 语言表达力增强、Task 2.10 函数特性增强、Task 2.9/2.10 Debug 审查（8 BUG）、缺陷修复（lambda 捕获语义 + 无符号打印，2 缺陷 + 1 顺带 BUG）、缺陷修复针对性复核（lambda 结构体值捕获 + 嵌套 lambda，2 缺陷）、**本轮"语句终止与续行"规则文档化 + 测试补充（17 单测 + 1 E2E）**，单测 **755/755**、E2E **19/19**、编译零警告（/W4 /WX）。

## 一、我们在做什么任务

正在开发CN语言编译器——一门全中文语法的系统级编程语言，参考C++编程范式，用C++17从零编写编译器，直接生成汇编代码（Win x64 MASM，ml64 汇编）。当前完成**阶段2全部（Task 0.1~2.10）**+ 缺陷完善 A/B/C + Debug 审查 + Task 2.9 表达力 + Task 2.10 函数特性增强 + Debug 审查 + 缺陷修复 + 复核 + **"语句终止与续行"规则文档化收尾**。下一步：Task 2.11（或阶段3 OOP 准备，见 plans 阶段2 文档与阶段3 文档）。

## 二、已经完成了什么

### 2.1 本轮核心任务：语句终止与续行规则文档化 + 测试补充（已完成 ✅）
**核心结论（语言设计层面）**：CN 语言语句**既不是"回车结尾"也不是"分号结尾"**，真实机制是：
1. **换行在词法层被当空白丢弃**——无语法意义，等同空格，天然支持续行（跨行表达式自动合并）
2. **语句无终止符**，语句边界靠**下一条语句起始关键字**识别（`变量`/`返回`/`当`/`选择` 等）
3. **分号 `;` 可选**——单行多语句用显式分号分隔；裸分号/双分号/`当(x);` 报错
4. **循环头 `循环(...;...;...)` 的分号是唯一强制分号**（三段缺一不可）

### 2.2 改动文件清单
**文档（4 处）**：
1. [`plans/001 CN语言编译器设计规格书.md`](plans/001 CN语言编译器设计规格书.md:230)：新增 **4.6 节"语句终止与续行"**
2. [`.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md`](.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md:53)：新增第三节"语句终止与续行"，后续章节序号顺延
3. [`.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md`](.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md:10)：新增第零节"语句终止与续行"
4. [`.ai-coder/skills/cn-language-spec/SKILL.md`](.ai-coder/skills/cn-language-spec/SKILL.md:95)：索引表更新（01a/03 描述 + EBNF 计数）

**测试（2 处）**：
1. [`tests/unit/parser/test_parser.cpp`](tests/unit/parser/test_parser.cpp)：第8节新增 **17 个单测**（无分号声明/返回/表达式、可选分号、关键字边界 `变量 x = 1 整32 y = 2`、跨行续行 调用/下标/分组/行尾运算符、行首运算符续行、单行多语句、裸分号/双分号非法、`当(x);` 非法、空循环体合法、循环头三段分号）
2. [`tests/e2e/19_语句终止/语句终止.cn`](tests/e2e/19_语句终止/语句终止.cn) + `.expected`：新建 **E2E 用例**（9 行期望输出）

### 2.3 测试基线
| 指标 | 数值 |
|------|------|
| 单元测试 | **755/755**（62 套件，738+17） |
| E2E 用例 | **19/19**（新增 19_语句终止） |
| 编译警告 | 0（/W4 /WX） |

### 2.4 本轮收尾动作（已完成 ✅）
- **临时文件清理**：删除根目录 `build_verify.log`/`e2e_results.log`/`unit_test_results.log`（验证过程临时产物，未跟踪，非项目正式文件）
- **plans 打勾**：`plans/002-阶段2-核心语言.md` 222-226 行 `✅ 语句终止与续行测试锁定（2026-08-13）` 已打勾，验证数字 755/755 + 19/19 已记录；规格书 4.6 节为规范正文无需勾选
- **更新日志.md**：覆盖写入本轮全部内容（核心结论 + 4 文档 + 2 测试 + 基线）
- **lessons.md**：新增"E2E 空循环体必须用恒假条件"（权重 9.4，逻辑错误）
- **git 提交推送**：develop 分支（中文提交）

## 三、当前测试基线

| 指标 | 数值 |
|------|------|
| 单元测试 | **755/755**（62 套件） |
| E2E 用例 | **19/19** |
| 编译警告 | 0（/W4 /WX） |

## 四、关键架构约定（必须遵守）

1. **i128/正128 双寄存器约定**：`%vN`=高64位槽、`%vN+1`=低64位槽；凡 `result.type` 为 i128/u128 的指令结果占 2 槽，`maxRegIdIn`/`registerVarSlots`/数组 stride/指针步进均按 16 字节处理
2. **结构体按值**：≤8 字节寄存器传递，>8 字节调用方栈分配 + 传指针；被调方 `emitParamSetup` 按 `structParamIndexes` 从指针 rep movsb 拷贝到参数槽；返回走隐藏返回指针（rcx→r12）
3. **lambda 捕获语义**（规格书04-一D）：`[=]` 值捕获=定义处快照（结构体须深拷贝）、`[&]` 引用捕获=存地址指针（体内 LoadPtr 读/StorePtr 写回）；捕获实参在 `genVarDecl` 定义处固化，调用点只展开
4. **无符号整数打印/拼接**：正8~正64 走 `__cn_print_uint`/`__cn_str_from_uint`（%llu），禁止统一 Cast i64 走 %lld
5. **Win x64 ABI**：隐藏返回指针占 rcx（参数位后移 paramOffset）；浮点参数独立 xmmN；被调方栈参数 rbp 锚定（[rbp+48] 为第5参数位）；调用前预留 32 字节影子空间
6. **变参函数（printf 家族）**：浮点参数按参数位 xmmN + `movq` 位模式到同参数位 GPR，否则 va_arg 读垃圾

## 五、踩过的坑（绝对不要再踩）

1. **E2E 空循环体必须用恒假条件**（本轮新坑，权重 9.4）：`当 (次数 >= 3) { }` 恒真空循环体死循环挂起进程；空循环体演示统一用 `当 (次数 > 99) { }`，并真实运行验证进程能退出
2. **lambda 值捕获结构体/数组必须"定义处深拷贝"**，不能只存地址指针（破坏快照语义 + 闭包参数槽类型错配读垃圾）
3. **语义层表达式展开 switch 必须覆盖所有节点类型**（尤其 LambdaExpr 嵌套闭包），漏 case 导致捕获集缺失、跨函数槽引用（`[rbp0]` 非法汇编）
4. **codegen 结构体参数拷贝依赖 `structParamIndexes` 标记**，凡"以指针传入需拷贝到参数槽"的参数（含 lambda 结构体值捕获参数）都要登记
5. **捕获实参求值时机 = 定义处**（非调用点），否则 `[=]` 退化为 `[&]`
6. **无符号整数打印/拼接必须走无符号路径**（%llu），禁止 Cast i64 走 %lld
7. **i128 双槽约定全覆盖**：maxRegIdIn/数组 stride/指针步进/Cast 矩阵/LoadPtr/StorePtr 都要按 16 字节
8. **emitCast 分支按"更具体类型优先"排序**（i128 目标先于通用 !toFloat）；同类型与 u64↔i64 转换矩阵必须覆盖
9. **十六进制生成统一走 `uint64HexText`**（含前导0逻辑），禁止各自 `%llX`（负浮点 A2006）
10. **Win x64 C 变参**：浮点参数必须同时设 xmmN 并 movq 到同参数位 GPR（位模式），否则 va_arg 读垃圾
11. **无符号字面量需值域自适应的位宽提升**（正32→正64→正128），codegen 超32位立即数用十六进制
12. **被调方栈参数恒用 rbp 锚定**（[rbp+48] 为第5参数位），禁止 rsp 锚定（被调方 sub frameSize 后读垃圾）
13. **i128 双槽值是"寄存器值"非"指针"**，取低64位必须 Cast 而非 LoadPtr；结构体数组字段必须退化为地址 + 按元素大小步进 + 越界检查
