# HANDOFF 交接文档

**交接时间**: 2026-09-10 第六十五轮（**深度机 linux-x86_64**）——**plans/019 阶段3b 残余「v2 传可变/互斥面」对齐落地**（实参末位表+借用位表+调用借用纪律）。v2self 固定点复锚 **247461 行**。专项累计十轮（c924145~本轮）。

---

## 一、本轮做了什么

1. v2 调用实参末位表（解析期逐实参记录——195 时代「无逐实参区间」结构性缺口收口）。
2. v2 函数参数借用位表（签名键→引用位/常量位）。
3. 检查调用借用纪律双挂点（传可变拒+互斥——宿主对齐）。
4. E2E 207_v2+全量 224 例 219 过+锚定链 247461 固定点一致+对拍 29/29+运行级 3/3。

## 二、下一轮任务（按序）

1. **阶段4' 拥有型字符串**（推荐A）——三轮征询无答复挂账；**第三批收口时点**呈报（建议反馈期后）。
2. **NLL 跨语句**——真实使用反馈期数据前提（观察期收集误报样本后定深度）。
3. 立案清单：stdlib 泛型跨模块导入不可见（ti 配方）；类静态+析构写（tp3）；194 单行批准；跨机轮（六十五轮变更复验）。
4. 阶段5 等并发库立项。

## 三、验证链

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 224 用例（败=194 待批，跳=62/69/78/79）
# 固定点 X64L=247461；v2 变更后锚定链范本 target/p65x64l/
```

## 四、本轮踩坑

- 批量脚本 assert 中断事务性双刃再现：三段替换失败于第三段=前两段（头部函数+主路径挂点）丢失——**多段替换逐段独立脚本**（每段失败不殃及已验证段）。

## 五、诚实边界

- v2 借用纪律被调键基取签名表首候选（重载族精确决议在 IR 层）；主路径键基近似=被调名（非主模块精确位由回退段覆盖）。
- NLL 维持排程（真实使用反馈前提）；194 待批维持；win/arm64 复验=下轮跨机轮。
