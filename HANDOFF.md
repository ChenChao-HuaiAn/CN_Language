# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：①本机节（自己机器最近交接）→ ②深度机节（开发主线最新进展）→
> ③根目录 `三机任务看板.md`（三机并行唯一看板，开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-12 深夜——**第八十六~八十八轮跨机验证轮**（拉取 9403e29=深度机 86-a/
87-a/88-a，基线 efc0c8c）。本轮为**纯验证轮（零源码改动）**，含一个新缺陷的完整诊断链（待修）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **背景**：上一会话（三机协议落地）登记的「88-a win 全量门禁」并行会话在 E2E 阶段被中断
   （`target/e2e_88a_win.log` 0 字节、E2E 产物停在 23:38）；本会话接手重跑。
   构建（零警告）+ 单测 1306/1306 由前序会话完成并留日志（`target/gate_88a_win.log`）——
   源码等价（efc0c8c 为纯文档提交），故直接复用 `target/Debug/cn.exe` 重跑 E2E。
2. **验证链（全量）**：
   - **E2E 275 用例 255 过 / 20 败 / 0 跳**（日志 `target/e2e_88a_win_rerun.log`）。
     败=17 双编译对照（win 平台限制：13 既存 + 新增 243/244/246/248）+ 78/79 OOM（win 固有
     败面）+ **148_v2 崩溃（新缺陷，见二）**。
   - **锚定链复锚**：`python target/p46win/self_chain.py` → fix_p ≡ fix_s **348908 行**
     逐字节一致（新口径；55 轮 340703 → +8205=56~88-a v2 源码）+ 绑定自检
     cn_main∈cn_self46.obj ✓。
   - **运行级 3/3**（`target/p46win/run_level.py`）+ **组件对拍 34/34**
     （`target/p46win/comp_diff.py`，DIFF=0）。
   - **win 实物复验（本轮新增）**：`target/p89win/run89.py` 把 4 个双编译对照用例
     （243/244/246/248，win runner 平台守卫跳过）在 win 上手工两侧对照（宿主 build +
     v2p 编译→ml64→link），**全部 rc=255 两侧一致**——86-a 三元聚合 / 86-a 嵌套深拷 /
     87-a 静态变量全链 / 88-a 短路全链的 win 实物首次验证。

### 二、新缺陷：148_v2 win 侧崩溃（0xC0000005，待修）

**现象**：E2E 148_v2_函数返回结构体 的 v2 产物运行退出码=3221225477（0xC0000005），期望 0；
55 轮该用例通过 → **回归**。

**诊断链（本轮完成，可复现）**：
1. **复现**：`target/p89win/repro148.py`（v2p 编译→ml64→link→运行，稳定 0xC0000005）。
2. **宿主维度 bisect**（手法：4 个不同宿主编译**同一份 55 轮 v2 树**；见
   `target/p89win/try_host.py` + worktree `target/wt_base|wt86|wt87`）：
   55 轮宿主 ✓ 正常 / **86-a 宿主 ✓ 正常** / **87-a 宿主 ✗ 崩** / 当前宿主 ✗ 崩
   → **87-a 的宿主改动引入**（与 v2 源码树无关——四组同树）。
3. **最小复现**：`target/p89win/min3/主.cn`（结构体按值实参 × 嵌套调用
   `取和(造点(30, 40))`）→ cn_main 帧 **384（正常）/368（崩版）**（min/min2 不触发）。
4. **机制**：崩版该调用点**缺 retbuf 隐藏参数**——正常版 `lea rcx,[rbp-384]`（retbuf）+
   返回后 rep movsb 物化拷贝；崩版 `mov rcx,30`（实参直传）→ 被调方（造点）把实参 30
   当 retbuf 指针写入 → **0xC0000005**。
5. **深层特征（重要）**：v2 编译器的「返回物化判定」**对内存状态敏感**——同一个 v2p
   （55 轮宿主构建）在「仅源」（E2E 调用方式）与「源+平台」两种命令行参数下判定**翻转**
   （384↔368）；87-a 的静态初始化改动（emitStaticInitsAtEntry/ALIGN 8/初值直存）改变了
   win 侧 v2p 的内存布局 → 在「仅源」下稳定进入「无物化」分支。
6. **平台分叉**：linux 侧不触发（深度机 E2E 271 过含 148_v2）——**win-x64 特有的跨机轮
   拦截成果**。

**未确证（留开发轮）**：具体「未初始化读取/脆弱判定」点在 v2 源码何处（本轮验证轮零源码
改动）。**已排除**：静态布尔初值错写（两版 .data 均为 0）、诊断开关误置（两者均未输出
[ir1]/[cg1]）、查寄存器类型未登记返回 0（实现安全）、.data 字符串初值伪指针（87-a 后恒
dq 0，且 55 轮旧形态 dq "主" 反而正常）。

**三方案（呈报，待用户裁决）**：
- **甲（推荐）**：根除「内存状态依赖」——定位并修复 v2 编译器未初始化读取/脆弱判定
  （Rust 对照：rustc 确定性编译 + Miri 未初始化检测；性能零开销）。
- **乙**：确定性门禁强化——同输入重复编译逐字节一致 + 参数模式交叉比对（仅防线，不修复）。
- **丙**：三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

### 三、下一步（新会话按序）

1. **缺陷修复（需开发轮看板认领）**：按用户裁决方案执行；入口=min3 最小复现 +
   `target/p89win/` 探针脚本（不入库）；`target/wt_base|wt86|wt87`（三个宿主 worktree）
   可用于继续 bisect 到 87-a 的具体代码块。
2. 单位机 arm64 侧对 86~88-a 的跨机复验（arm64 尚未拉取本轮）。

### 四、本轮踩过的坑

- **worktree 构建需 googletest junction**：`target/wt*` 的 CMake 回退路径是
  `<worktree>/target/build/_deps/googletest-src/googletest`——建 junction 指向
  `C:/Users/ChenChao/Documents/third-party/unittest/googletest` 即可（`mklink /J`，
  用 python subprocess 调 cmd 避免 Git Bash 转义）。
- **`build` 子命令产物名**：`--output <路径>`（不带扩展）产 `<路径>`（无扩展可执行）+
  `<路径>.obj`；Windows 下执行前须改名 .exe。
- **ml64/link 直调在 Git Bash 被 `/nologo` 路径转换坑**：一律用 python subprocess 调用
  （不要经 bash 拼接命令）。
- **v2p 的 `--verbose` 需第 3 参数**（`<源> <平台> --verbose`）；「仅源」与「源+平台」
  会改变 v2p 行为（本轮缺陷的关键观察面）。
- **E2E 结果读取**：runner 输出全缓冲（重定向时中途 0 字节属正常），用产物时间戳/进程判活。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-12 第八十八轮（**深度机 linux-x86_64**）——**88-a v2 实参位短路栈失衡根治**
（立案来源=87-a 登记项「v2 实参位短路栈失衡」下一轮第一优先）。本轮序列：栈模型普查（隐式
「表达式求值净栈 +1」契约）→ 探针 P88 形态矩阵（12 消费位两侧对照）→ 根因定位（**两层叠加**：
语法层短路区间左操作数缺失 + IR 层净栈契约破坏）→ 实施（语法层左起点显式传递与分层 + IR 层
净栈契约归位原语）→ 首版回归（三处）纠正 → 防线（E2E 248 双编译对照）→ 反证三件（全命中）
→ 全量验证链 → 文档五件套。验证链全绿。

### 一、本轮做了什么（写给无上下文的新会话）

1. **栈模型普查**（先做，再动手）：确认 v2 的隐式契约——`生成表达式(区间)` 净栈增量恒 **+1**
   （结果=栈顶）；消费点两类（相对栈顶弹 N 个 / 弹到进入前深度归位）。定位破坏者两处：
   - **S1 语法层**：`解析二元链`/`解析二元层` 的「左起点」= `池.大小()`（左操作数**之后**——
     左操作数已由 `解析表达式` 前段解析）→ `||`（层1）/`&&`（层2）短路节点左操作数区间
     `[子起点, 附加)` **恒为空**（探针 dbg：`标 || 假` 收集 `子起点=28=附加`，左操作数在 27）。
     连锁：左操作数被主循环**平铺求值**（多压一项残留）+ 短路求值内左递归空区间退化为
     「取栈顶」（**侥幸拿到左操作数值** → 赋值位/条件位「看似正确」是巧合）。
   - **S2 IR 层**：`生成短路求值`「只压不弹」（左+1/右+1/末尾结果+1 = 净 **+3**）破坏净栈
     契约（探针弹前栈 `[1 2 -30 3 4 8]`：实参弹 2 后被调取到 `3`，应为 `-30`）。
   - 症状：`f(a || b)` → `[ir2] 调用被调对象非函数名/方法` + 运行时错误6（宿主同形态正确）。

2. **形态矩阵（探针 P88 f_/g_/h_ 系列，12 消费位两侧对照）**：实参位（用户函数/打印内置/
   `&&`/双实参）修复前 ✗ 中止 → 修复后两侧一致（果=4/真/0/10）；赋值位/返回位/条件位/
   当条件位/短路嵌套/一元嵌套/三元嵌套 ✓（修复前=巧合正确）；惰性求值 ✓（副作用计=0）/
   左操作数只求值一次 ✓（副作用计=1）/循环 2000 轮 ✓（和=5984）。

3. **实施**（v2 单侧）：
   - **语法层**（`语法/解析表达式.cn`）：`解析二元链` 增 `表达式起点` 参数 + `解析二元层`
     左起点参数化（链入口=表达式起点 / 左操作数路径=父层左起点 / 右操作数路径=父层右起点）
     + `解析层_子` 增 `子层左起点` 并**分层**（`层 <= 1` 用「左操作数起点」；层3+ 保持
     「左操作数之后」= `池.大小()`）。
   - **IR 层**（`IR/IR生成.cn` + `IR/生成表达式.cn`）：新节**净栈契约单一事实源原语**
     （`表达式栈弹到深度` / `表达式栈归位压回`；注释固化契约 + 「禁止裸循环弹栈复制」纪律）
     + `生成短路求值` 归位（进入深度 → 左/右递归后弹到深度 → 末尾压唯一结果 = 净 +1）。
   - **注释更新**：`生成语句.cn` 静态 preFree 预计算的过时约束注释撤销（预计算保留=性能
     选择；原「不可在实参位直接写 `||`」约束随缺陷根治失效）。

4. **防线**：E2E **248_双编译对照短路求值全链路**（第十七个对照用例，八形态折 **255**）；
   **反证三件全命中**（A 语法层左起点回退 → 248 v2 侧复现 `[ir2]`；B IR 净栈归位回退 →
   同复现；C 分层回退 → 192_v2 语义错误消失退出码 0），均已恢复复验。

5. **验证链**：清场重建**零警告** + 单测 **1306/1306** + E2E **275 用例 271 过 0 失败**
   0 未实现 /4 跳 + 锚定链固定点 **335263 行**（新口径）两代逐字节一致 + 运行级 3/3
   + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

6. **文档五件套**：plans/019（跟踪表第八十八轮行）+ plans/020（**第二十九节 88-a 全节**）+
   lessons（88-a 段，权重 8，预防新增 92~95）+ 更新日志 + 本文件。

### 二、本轮踩坑（已入 lessons 第八十八轮段）

- **隐式不变量必须有单一事实源**：「净栈 +1」契约无文档无校验，短路求值破坏它（净 +3）
  长期无人发现——以「归位原语 + 契约注释」收口（Rust 对照：MIR def-use + validate-mir）。
- **字段多用途时改语义先普查消费者**：`子起点` 同载两契约（IR 短路区间左端 vs 语义层
  `子起点-1`=左末端）——首版全层一刀切 → 141_v2/190_v2/192_v2 三处 E2E 回归 → 分层纠正。
- **「看似正确」是巧合正确**：赋值位/条件位短路「通过」= 空区间退化取栈顶恰好=左操作数——
  只测一个消费位等于没测（消费位矩阵铺面才暴露「只有弹栈消费错位」）。
- **区间标注=层间契约**：语法层一个 `左起点` 错误，症状是 IR 层「调用被调对象非函数名/
  方法」（跨三层）——排查须双向（自上而下 dbg + 自下而上核对区间语义）。
- **调试输出用 `IO::打印到错误` + 事后必删**：本轮曾漏删一处 dbg（`生成表达式求值.cn`），
  锚定链日志暴露后清理——**删除后 grep `\[dbg\]` 复核**。

### 三、下一轮任务（按序）

1. **跨机轮（家机 win-x64）**：88-a v2 侧修复 win 实物验证（E2E 248 + v2 树自编译）+ 87-a
   三后端中 x64（win）侧未实物验证项（槽尺寸/标量初值/`ALIGN 8`）+ 全量门禁复验。
2. **arm64（单位机麒麟）**：linux-arm64 后端复验（88-a 短路面 + 87-a 静态段 `.align 3`/
   `.zero`/`dq` 实物）。
3. **净栈契约出口校验（观察项候选）**：在 `生成表达式` 出口加「净增量 ≤ +1 / 不吞外层」
   断言（IR 层诊断或调试输出），把契约违反从「下游症状」提前到「当场暴露」——需先以
   探针/全量 E2E 确认零误伤（本轮已全绿，违例面应为零）。
4. **v2 静态字符语法**（`静态 字符 x = 'Z';` 语法层不识别——既有限制）与 **v2 打印浮点
   恒 0.000000**（既有打印内置限制）：登记待评估。
5. **多文件静态初始化注入**：仅入口模块（含 主）注入——与类/容器静态既有同限制；如需
   跨模块，需「模块初始化函数」机制（登记）。
6. 呈报清单余项维持：B35 字段容器接收者（待用户裁决）、甲（参数 move）/丙（返回位拒绝）、
   阶段 4 第二层第三批（不安全区变硬错误）、v2 诊断无行号、未初始化槽 preFree 观察项、
   行数存量违规面长期整改。

### 四、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 275 用例 271 过/0 失败/4 跳
bash target/p71x64l/run_chain71.sh   # 锚定链（固定点 335263 行两代逐字节一致）
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**
bash scripts/cn_release_parity.sh    # 释放路径两侧对照
# 清场重建（零警告门禁）
rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug && cmake --build target/build -j8
```

> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **手工 v2 探针链**（/tmp/p88probe/{g2,t248v2}）：`v2p 主.cn linux-x86_64`（cwd 需有
> `stdlib` 软链 + `target/` 目录）→ `as target/v2asm.s -o 主.o` → `g++ -no-pie 主.o
> target/audit2/{io_api,intern_api,runtime,string_api,i128_api,math_api,input_api,file_api,
> time_api,system_api}.o` → 运行取退出码。
> **反证手法**：备份文件（本轮 /tmp/p88probe/bak*_*.cn）→ python 注入 → 重建 v2p（**输出到
> 独立路径**如 `/tmp/p88probe/v2p_invX`，避免覆盖正式 v2p）→ 跑用例应 FAIL → 恢复复验。
> **注意**：改 v2 源码后**必须重建 v2p**（否则测量错位）；调试打印删除后 grep `\[dbg\]` 复核。

### 五、诚实边界

- 本轮修复为 **v2 单侧**（宿主同形态恒正确=基准对照，宿主源码零改动）。
- 87-a 规避写法（`生成语句.cn` 静态 preFree 预计算布尔）**保留**——性能选择（一次求值供
  多个调用点复用），非缺陷规避；对应过时注释已更新。
- win-x64（家机）/arm64（单位机）侧 v2 实物复验=跨机轮（本机仅 linux-x86_64）。
- 锚定链固定点 **335263 行**为本轮新口径（87-a 后 334821；+442=v2 侧 88-a 代码）；不跨
  后端比较。
- 前轮登记维持：v2 静态字符语法限制 / v2 打印浮点既有边界 / 多文件静态注入仅入口模块 /
  静态槽不登记自动释放（进程生命周期，Rust static 同款）/ v2 诊断无行号 / 行数存量违规面。
- 探针与脚本留 /tmp/p88probe（不入库）；反证注入均已恢复（工作区仅本轮 3 文件改动 + 新用例 248）。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
