# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十五轮（**深度机 linux-x86_64**）——**85-a 聚合返回位
所有权链断裂根治**（借用来源返回深拷归一化，宿主 + v2 两侧同构）＋ 84-g 登记项
**复核证伪**（无冗余复制，差异=释放时机）。本轮序列：84-a 字面量作实参 → 84-b 字段
校验 → 84-c 嵌套字面量 → 84-d 成员链嵌套深度 → 84-e 嵌套串释放面收口 → 84-f preFree
核验 → 84-g P32-2 销项 → **85-a 返回位所有权根治**。验证链全绿，固定点 **327355**。

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：84-g 登记的「宿主字面量实参每字段多一次 `__cn_str_copy`（性能项）」。

2. **登记项复核 → 证伪**（探针 P36/P37）：宿主/v2「构造后增量 +9 vs +6」的差 3 不是复制
   次数，而是**释放时机**——两侧**分配事件数完全相同**（9 = 4×`字符串复制` + 4×元素深拷
   + 1 数组）；v2 在调用后立即释放实参临时字段串（`清理调用实参临时`），宿主按 79-a
   名单推迟到块出口。汇编核对：宿主 `cn_main` 4 处 + 容器方法 4 处 = v2asm.s 4 处 +
   供给 obj 4 处（运行时同为 8 次）。⇒ 该优化项**不成立**（plans/020 第二十五节已加更正
   标注）；实参临时释放时机**本轮不改**（提前会加剧下一条缺陷）。

3. **真缺陷（本轮根治，宿主与 v2 同现）**：**聚合返回位所有权链断裂**——返回类型含拥有型
   串字段（结构体/结果/可选）而 `返回 <借用来源>`（按值形参=调用方 place 的浅拷副本／
   全局静态／成员链／下标／解引用）时只做按位 memcpy → 返回值句柄与调用方 place
   **共享**；调用方各接收位一律按 owned 处理（声明/赋值=浅拷接管、入容器=元素槽独立）
   → 调用方释放其持有者后返回值字段**悬垂**（探针 P38：跨块读取 + 搅动后打印乱码
   `[?v]`/`[2e]`；数据正确性/安全级）。

4. **三方案裁决（自行裁决乙，留痕）**：甲＝**参数 move 语义**（零拷贝、Rust 原生，但形参
   不再是借用视图、调用方实参变 moved-from＝语言级语义变更）→ **登记待裁决**；
   **乙＝返回位深拷归一化**（借用来源 → 独立副本；拥有局部/调用返回/字面量保持零拷贝）
   ⇒ **实施**；丙＝**编译期拒绝**（A2/E2E 210N 同款）→ 聚合无简洁显式复制语法
   （无 `结构体复制` 内置）→ **登记不推荐**。

5. **附带揪出两个独立缺陷**：
   - **④ `返回 *p` 基线段错误 rc=139**（修复前即崩：`genExpr` 对解引用 place 读值降级=
     把结构体首字段当地址）→ **同轮修复**（源地址一律取 `lvalueAddress`）。
   - **② 静态结构体不支持**（`.data` 槽恒 8 字节 `.quad 0`；`静态盒 = 盒子{…}` 编译为
     `store 0`）→ **登记为下一轮第一优先候选**（三方案见 plans/020 第二十六节）。

6. **借出视图方法豁免（关键约束）**：容器读出接口（元素/读取/栈顶/队首/头部元素/读取头部/
   读取尾部/获取）**设计上**返回容器内句柄的借出视图（调用方按污染名处理、**不登记释放**；
   生命周期由 77-a 检查器保证）＝Rust `Vec::get -> &T` 同款、与 A2 字符串「泛型单态化体内
   豁免」同理。宿主把 `isBorrowViewMethod` **上提 public**（单一事实源）；v2 因 **IR 层
   不得反向依赖语义层** 而并列一份并互相注释。**漏此豁免 → 235/234/238/240 残留断言全失
   （实测 212/152/77/183）**——本约束是本轮最大的坑（全量 E2E 才暴露）。

7. **实施（两侧同构）**：
   - 宿主：`ir/ir_fields.cpp` 新增 `isBorrowedAggregateSource`（借用来源判定 + 借出方法
     豁免）+ `genOwnedAggregateReturn`（`Alloca` 临时 + 左值地址取源 + `emitStructCopyWithFields
     (preFree=false, deepCopy=true)`；临时槽**不登记**释放名单＝句柄随返回值移出）；
     `ir/ir_stmt.cpp` `visitReturnStmt` 单点挂接；`semantic/semantic.hpp` 上提共享。
   - v2：`IR/生成语句.cn` 新增 `方法段名` / `是借出方法名85` / `是借用来源返回`；结构体返回位
     由按位 `IR_结构体拷贝` 改为借用来源 → `发射结构体整体拷贝(深拷=真, 先释放=假)`
     （**直接写调用方 retbuf＝sret 就地构造**，零额外 memcpy）。

8. **防线（探针转正）**：E2E **242_双编译对照聚合返回位所有权**（移植纪律 9 **第十三个**
   对照用例：八形态折校验和 **255** 两侧一致；每形态含跨块读取 + 搅动 + 内容比较 + 残留
   归零）+ **三项反证实证**：A 宿主借用判定恒假 → 242 宿主 rc=**-11**；C v2 借用判定恒假
   → 242 v2 **160**（丢失 ①②③④⑤⑦、保留 ⑥⑧ 对照位）；B 宿主借出豁免恒假 → 235 **212**
   （期望 255）。三注入均已恢复复验（残留=0）。

9. **验证链**：清场重建**零警告** + 单测 **1306/1306** + E2E **269 用例 265 过 0 失败**/4 跳
   （+242，其余 264 过＝零回归）+ 锚定链固定点 **327355 行**（84-g 后 326126；+1229=v2 侧
   返回位代码）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

10. **文档五件套**：plans/019（跟踪表第八十五轮行）+ plans/020（第二十六节 85-a 全节 +
    第二十五节 84-g **更正标注**）+ lessons（85-a 段，权重 8，预防 78~81）+ 更新日志 +
    本文件。

## 一之补：84-g 摘要（上一轮·核验轮，**结论已由 85-a 更正**）

- **销项**：84-f 的「P32-2 +7 额外 3 分配」= **核算误差**（分步 P33/P34 + 汇编 `__cn_str_copy`
  计数 8 次逐项吻合）：首追 +3（数组 1+字面量复制 1+元素副本 1）、后续 +2/次、删除逐次 −1。
- ~~**新定位（性能项）**：宿主「构造后 +9 vs v2 +6」= 宿主每字段多一次复制 → 登记宿主优化项~~
  → **⚠ 85-a 复核证伪**：两侧**分配事件数相同**（9 次），差 3 = **释放时机**（v2 调用后立即释放
  实参临时字段串，宿主按 79-a 名单到块出口）；该优化项**不成立**（plans/020 第二十五节更正标注、
  第二十六节分解式）。复核期间揪出真缺陷 **85-a（聚合返回位所有权链断裂）** 并根治。
- **验证链**：84-g 时点=E2E 268·264·0 / 固定点 326126；85-a 后=**269·265·0 / 327355**。

## 二、下一轮任务（按序）

1. **② 静态结构体支持（登记，第一优先候选）**：`.data` 槽按类型尺寸分配（三后端
   x64/linux64/arm64）＋ 静态结构体字面量赋值路由 `emitStructCopyWithFields`。三方案见
   plans/020 第二十六节（甲 推荐／乙 main 注入构造／丙 语义拒绝）；探针 p41b（`静态盒`
   返回）与 iso/n*.cn 系列已在 /tmp/p82probe。
2. **B35 字段容器接收者（待用户裁决）**：plans/020 第十七节三方案（甲 move／乙 深拷
   〈推荐〉／丙 维持+诊断）——裁决后实施（含 E2E 门禁与反证）。
3. **甲（参数 move 语义）/丙（返回位编译期拒绝）**：本轮登记的语言级/政策级选项，
   待用户裁决或待 `结构体复制` 内置出现后重估。
4. **实参临时释放时机**（v2 调用后即时 vs 宿主块出口）：本轮登记为后续评估项
   （需先论证所有 callee 不保留句柄；本缺陷修复后才可能安全）。
5. **物化性能优化（可选）**：延迟物化 + 活跃性分析（当前=每次真结构体返回 1 次 memcpy
   + 帧尺寸增长）；**结果/可选 返回在 v2 侧不在面内**（资源字段未登记=84-e 边界）。
6. **跨机轮**：arm64（单位机麒麟）复验 E2E 234~242（linux-arm64 平台）。
7. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区
   变硬错误）；v2 诊断无行号；未初始化槽 preFree 隐患（观察项维持）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 269 用例 265 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 327355 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（含 82-a 新段）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`，
> 并发会踩出假警。**全量 E2E 与手工 v2p/固定点操作不得并发**。E2E runner 的 v2p 构建缓存按
> v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时删缓存键**。
> **反证手法**：注入 → 重建 v2p → 跑用例（应 FAIL）→ 恢复（本轮用 `cp IR容器.cn /tmp/...bak`
> + python 替换 + 复原）。

## 四、本轮踩坑（已入 lessons 第八十五轮段）

- **「返回值所有权」是独立检查面**：写入位（声明/赋值/入容器/字段）早已归一化（79-a），
  **返回位**漏了同一套来源分级 → UAF。凡「按位拷贝返回」都要先问「句柄归谁」。
- **新规则必须比对「设计上的借用返回」清单**：容器读出接口（元素/获取…）设计上返回
  借出视图（调用方不登记释放）——被新规则拥有化即台账冲突（235=212 等）。**单跑新用例
  看不出来，必须全量 E2E**；豁免是否必需用**注入反证**（B）证明。
- **同类检查面「上提共享」而非并列复制**：语义层 `isBorrowViewMethod` 上提 public 供 IR
  层复用（单一事实源）；v2 受依赖方向约束（IR 不得依赖 语义）→ 并列一份 + 双向注释。
- **复合缺陷先做形态矩阵**：P40 八形态把「该修的借用来源」与「设计豁免的借出视图」一次
  划清，并分离出两个独立缺陷（④ 基线段错误、② 静态结构体）。
- **需要「地址」时一律用 `lvalueAddress`**：`genExpr` 对 place 的读值语义不定（解引用形态
  降级为「把首字段当地址」→ rc=139）——84-d「类型推导失败→静默降级」范式的近亲。

## 五、诚实边界

- **84-g 的性能登记项已证伪**（无冗余复制；差异=实参临时释放时机）——plans/020 第二十五
  节有更正标注。
- **甲（参数 move 语义）**：零拷贝但属语言级语义变更（形参不再是借用视图、调用方实参变
  moved-from）→ 待用户裁决；**丙（编译期拒绝）**：聚合无显式复制语法 → 用户无法写等价
  代码 → 登记不推荐（若将来新增 `结构体复制` 内置可重估）。
- **② 静态结构体**：槽尺寸 8 字节 + 字面量赋值丢值（登记，下一轮第一优先候选）。
- **v2 结果/可选 返回**不在本轮面内（v2 结果/可选 值字段未登记资源字段=84-e 边界）。
- 实参临时释放时机未改（保持 79-a 块出口）；借出视图 × 容器失效的检查仍由 77-a 承担
  （本轮的豁免即尊重该设计）。
- 其余 79-a/81-a/82-a/84-* 诚实边界维持（容器字段浅拷共享对象指针、数组元素持串、装箱
  字段接收、静态面无 RAII、跨函数数据流字段所有权不健全等）；win-x64 v2 并行未隔离
  （家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s **327355 行**、cn_self71）。
- 探针（/tmp/p82probe，不入库）：P35/P36/P37（84-g 复核：分步增量 + 总分配次数对照）、
  P38/P39/P40 与 p41a~p41f（返回位形态矩阵）、iso*/m*/n*（嵌套字面量/静态结构体离析）、
  ir_fields.final.bak 与 生成语句.final.bak（反证备份）、e235.cn（插桩副本，不改原用例）。
- E2E 用例：`tests/e2e/242_双编译对照聚合返回位所有权/`（主.cn + 双编译对照.txt + 主.expected）。
- 修改文件（85-a）：宿主 `src/cn_compiler/ir/ir_fields.cpp`、`ir/ir_stmt.cpp`、`ir/ir.hpp`、
  `semantic/semantic.hpp`；v2 `CN语言编译器v2/IR/生成语句.cn`。
