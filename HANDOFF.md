# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、会话总交付（🏃 多轮：329-a/338-a/339-a/340-a/352-a/354-a/358-a·364-a·2026-09-18~19）

**已集成六轮**：329-a D38批2（2f6c7b6）/338-a T56（a9dcfe0）/339-a 批3a（433bceba）/340-a 批3b 收官（5d560646）/352-a D36 侦查（8d8bafe3）/354-a T33 销项确认（00159192·计划文档另经 893d6d31）。另两窗 M3 采样（ae08dee/日志）。

**挂起等回签/解锁（364-a 分支·头见远端 任务/家机-364-D10）**：
1. **D36 根治已实施**：v2 IR 局部槽块作用域化（块新名清单栈平铺+边界）——346 出块恢复修复（asm 铁证 常量重内联·101→12）·零回归（E2E 445=444/1·单测 1353·d38_scan 16 拒）;
2. **D10 根治已实施**：recheckGenericFuncBody 确定性诊断保留（泛型体未声明调用 链接爆→build 语义拒·负例正例双向实证）·**深度机已回签通过**（linux 444=439/3/2 零新增红）；
3. **集成阻塞=406**（T43/D41 折叠浮点·深度机 363-a 修复过渡态红）——深度机推送修复后本分支 `--try-build-done` 即集成（组合态已分层全绿实证·无需重验）。

### 二、接续点（按序）

1. 364-a 集成（待 406 解锁·`integrate.py --try-build-done`）；
2. D36 实施设计（352-b 落盘 plans/025 §1.1·块新名清单栈方案·输入齐备）——若 364-a 集成含 D36 则跳过；
3. 波次7 余项=C19/D10~D11（D10 本分支已含）/波次8 批C/D；B1/T43/T46/T47/T48 待用户裁决。

### 三、坑与边界（本会话沉淀）

- **heredoc 中文损伤链**：Git Bash heredoc→Windows python stdin 对中文/反斜杠多层损坏（吞字/真换行/字符破坏）——大段中文编辑一律 Write 工具生成补丁脚本；
- **v2 探针三件套**：stdlib junction 必建（缺=预置类型未登记=假拒）/独立 cwd+相对路径传参/拒结果先辨环境真因；
- **推导兜底污染**：v2 检查调用表达式 整64 兜底——类型敏感检查只消费可靠推导面；
- **try-build 等回签期间**：ci.ps1 对 E2E 任一失败即红（含他机过渡态）——develop 红期间 src 层集成全挂起（§8.6 全员停）。
## 深度机 linux-x86_64 节

**交接时间**: 2026-09-19 ✅ **422-a 收工（族① 达成+族② 定位深化·守卫恢复·待集成）**。

### 一、本轮做了什么（给无上下文的会话）

1. **族① 达成（类型大小 K/V ×48 全消）**：当前生成符号 静态打印（首测残留「核心$断言」暴露普通函数路径缺设置·补类方法标记）→48 全锁定 **映射$整64$整64 克隆体四方法**→**类版标识符替换块补回**（420 末清理 dbg 时连带误删·本轮补回+去重）。
2. **族② 定位深化**：实例布局已登记（dbg 布局：映射 7 字段 56 字节 ✓）——元素数量（非指针字段）×9 消（变量结构体表登记生效）——**剩指针字段 ×72**（值数组/桶数组/链数组/键数组）＝**引用参数自身的结构体解析链**（兜底结构体ID=0 dbg 铁证）——422-b 审计 查变量结构体类型/解析对象结构体ID 的引用参数键域。
3. 族③ 装箱语境 ×12 待族② 后。**守卫恢复**。

### 二、验证链（全绿·收工态）

- 全量 E2E `--jobs 8` = **452=449/1/2**（唯一红 426=基线既有 T50）；单测 **1353/1353**；strict/matrix ✓；自举链 78/79 双 PASS。

### 三、下一轮（422-b）+ 在飞协调

1. **422-b=引用参数结构体解析链审计**（查变量结构体类型/解析对象结构体ID 的引用参数键域）→族② 清零→族③ 装箱语境→**撤守卫+390 终验收**（波 3 终收口）；
2. **集成序**：413→414→416→417→418→419→420→421→422（接力链）；try-build 通告已发。

### 四、诚实边界

- 族②③ 未清零（撤守卫推迟）·泛型函数实例调用返回类型 MVP=整32·同名参数跨类误替换；
- **教训入册**：清理 dbg 连带误删正式块（420）——**清理脚本逐块 diff 审查**；dbg 计数自增先于打印（421）。

## 单位机 ARM64 节

**交接时间**：2026-09-18 晚 341-a（轮次号先到先得·已集成）＋332-a/333-a（看板先行+门禁扩面·已集成）＋329-D12 在飞（并行会话）。**接手先 `git fetch`，读远端看板/分支（别信本地文件）。**

### 一、本轮做了什么（341-a·制度轮）

1. **用户裁决方案①落地**（轮次号撞号根治「认领前先看板、先到先得」）：AGENTS §8.1 占用权威＝远端 develop 看板在飞行（不看 git 历史）；check_handoff 新增「**在飞轮次号唯一性**」机械检查（跨机同号即拦·正反例实证；正则须覆盖大写/中文标识——首版漏 `340-D38`/`329-D12`，反例未拦→当场修）。
2. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase「追加而非替换」）——标题级去重保留最新版本（止血；根因防线=子节标题重复检测·下轮加）。
3. **本轮按新制度完整实践**：先 `git fetch` → 看板直推占号 341 → 才开分支动手（新制度首次完整闭环）。
4. **arm64 侧两次 try-build/自验**（服务轮）：①**深度机 338-a（C19）回签**=❌ **依赖顺序待解**（非 338-a 缺陷）——338-a 基线在 331-a 集成前（不含 T50），426 期望却是 T50 后版本 → **331-a 须先集成**，之后 338-a rebase 自动转绿；②**单位机 329-D12 自验**=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。

### 二、验证链

```
（341-a）check_handoff 正例 0／反例（家机行改 341）拦截「在飞轮次号 341 被多机占用」／还原 0
（plans/025）去重断言零重复子节标题·1780→493 行
（338-a）arm64 分支 1 红（426·依赖顺序）／合并结果演练 1 红（331-a 未集成）→ 结论=331-a 先集成
（329-D12）arm64：零警告+单测 1353/1353+E2E 454=452/0/2（/tmp/tb329_gate.log）
```

### 三、实测发现（三机实况）

- 家机 340-a（D38批3b）／深度机 338-a（C19·try-build）／单位机 329-D12（并行会话在飞）+341-a（本轮）——**三机同时活跃**。
- **依赖顺序发现（338-a↔331-a）**：426 期望含 T50 后输出，而 331-a（含 T50）挂起等家机 win 回签 → 338-a 无论怎么 rebase 都红，直到 331-a 集成。**跨分支依赖顺序是分层验证的新盲区类别**（值得入 lessons）。
- **文档膨胀面扩大**：plans/025（1780·4 份拼接·本批止血）与 更新日志.md 家机节（320-a×4·333-a 已修）——**共享文档「追加而非替换」已成第二类高频事故**（第一类=广播写进分支·332-a 修）。

### 四、下一轮候选

1. plans/021 瘦身（卡 600 上限·用户已提）。
2. 子节标题重复门禁（plans/025 膨胀根因防线·与 333-a 的节标题重复检测同族）。
3. 329-D12 收尾集成（并行会话·T50 让位按深度机实现）。
4. C20 波2／C25（v1 残留清除·70~77+89 用例处置）。

### 五、诚实边界与坑

- **回签≠放行**：338-a 的 arm64 回签结论是「依赖顺序待解」——try-build 本次抓出的正是「分支孤证 vs 合并结果」差异。
- worktree 位置坑（332-a）：CMake 靠 `../../third-party` 找 gtest，worktree 须在 `/home/user/projects/` 深度；产物在 `target/` 根（单测 `./target/cn_unit_tests`·编译器 `target/cn`）。
- plans/021 恰 600 行（下轮必须瘦身，否则任何加行触门禁）。
