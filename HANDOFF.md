# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（296-a 开发轮·2026-09-17·基线 6217fca→认领 1903a2f）

**任务**：D35 波B——v2 错误传播运算符 `?` 全链实现（364/60/344 三实弹双编译转正验收）。认领依据=plans/021 §3-D D35 行+295-a 收工预登记；深度机 🧪 daemon 采样轮在飞（不占锁）→本机认领开发轮。**宿主 src/cn_compiler、runner、stdlib 零改动（纯 v2 树对齐轮）**。

1. **修前指纹**（v2p 全链实录，/tmp 级探针 target/p296a）：364 出 `链式好: 0`（应 11=残缺三元）；344 出 `962132692608`（应 7=? 解包读未写区垃圾）；60 段错误 0xC0000005——与 295-a 定案完全一致。
2. **六文件实现**：①`语法/解析表达式.cn`：问号后表达式起点判定（宿主 ternaryQuestionNext 同构）+`?` 分流（不能开始表达式→建 节点_错误传播；能→三元现状）+**建节点后二元链续接**（`(x?) + 1`——宿主 postfix 优先级同语义，60 行 30 假缺分号即此）；②`IR/IR常量.cn`：节点_错误传播=48；③`IR/生成表达式.cn`：收集错误传播区间+主循环分派；④`IR/生成表达式运算.cn`：生成错误传播——操作数两形态分流（调用=栈顶即 retbuf 地址/标识符·成员·解引用=生成左值地址）+IR_条件跳转 分块+**err 块 retbuf 就地构造**（槽1 指针→标志=0@0 T_整32→错误值 读操作数 E 宽/IR_类型转换/写返回 E' 宽@联合偏移→IR_返回(retbuf)+跳尾块）+ok 块读值压栈；读/写偏移各自 `max(结果值偏移(T),结果值偏移(E))`（装箱构造写侧同公式）；⑤`IR/生成函数.cn`：当前函数尾块ID 全局登记（不安全 访问器·err 块跳尾块用）；⑥`语义/语义检查.cn`：检查错误传播表达式——操作数须 结果/可选+当前函数返回类型须 结果/可选 双诊断+n.附加=操作数类型ID/n.文本=返回类型ID 回填标注（装箱构造 子终点 标注同款「语义驱动 IR」）。
3. **修后甄别**：364=`链式好: 11`/`链式坏: 6`✓；344=`7`✓；60 八形态全集逐行对齐 .expected（正常传播/错误传播/两层链/传播后运算+1/宽错误码整32→整64/可选传播有值/可选无值/实参位）✓。三用例上架双编译对照.txt（断言输出=是·借链=否·无容器泛型），runner 双测通道单跑 PASS。
4. **验证链全绿**：单测 **1353/1353**+全量 E2E **423=423/0/0**（--jobs 8·含 3 新上架+78_v2/79_v2 自举闭环=v2 树改动后锚定链自洽）+CLI 矩阵 **50/50**+面覆盖 **61/62**（1→1 稳定）+spec `--strict` **176/176**+双文档门禁 PASS。
5. **写集**：CN语言编译器v2/六文件（上列）｜tests/e2e/364+344+60 三目录双编译对照.txt（上架）｜plans/021（D35 销项 §3-D→§4.2+T32 完整闭环+§5 索引 296-a+戳）｜plans/025 §1.0 回填｜HANDOFF/更新日志家机节｜lessons.md（两条）｜看板本机行。
6. **踩坑与教训**（已入册 lessons 权重 8）：①**v2 节点池「子终点=建节点时 池.大小()=本节点自身索引」惯例**——按宿主嵌套 AST 端点直觉写「操作数段=[子起点,子终点-1)」把实参节点当操作数末节点（误报 整32）；判据=先读同构节点（三元）的生产/消费两侧再定端点。②**平铺解析的后缀优先级须显式续接**——宿主 `?` 在 parsePostfix 消费故 `x? + 1` 自然成立；v2 `?` 判定在二元链之后，错误传播建节点后直接返回会漏掉 `+ 1`（假缺分号），修法=建节点后重复二元链判定（左起点=起始）。
7. **诚实边界**：①操作数=变量（`结果<...> 好 = ...; 好?`）形态已实现（生成左值地址分流）但 E2E 语料未覆盖（60 全为调用形态）——负测补面随后续轮；②`?` 在条件/循环等复杂嵌套语境未专项矩阵（三元区间+传播区间嵌套组合）——靠全量 E2E 承载；③err 块 IR_返回 类型码标 0（retbuf 形态约定·与 发射普通函数调用 调用侧一致）。

**下一轮候选**（预登记 plans/025 §〇）：①T33 读侧窄宽三后端设施（编码LoadStoreWin movsx·385/386 转正）；②T35 专项（v2p 自身错误 6 例）；③C20 批C（parse 70 v2 语法缺口大轮）；④B10/B11/B12/T24/T25 用户裁决后根治。
## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 M3 正确性收敛采样轮（验证轮·不占开发锁·源码零改动）。接手前先 `git fetch`。

### 一、本轮做了什么（🧪 采样轮·基线 9b2e2fd→rebase 至远程最新·写面=tests/cnsmith_hits/+面台账+plans/021）

1. **daemon 常态采样 23 轮**：`python3 scripts/cnsmith_daemon.py --rounds 0 --samples 250 --sleep 90`——343 样本/轮（250 新时间戳种子+93 命中库回归轨），**7889 次差分全一致·零命中**（生成器盲区：不打裸布尔/极值形态）；另跑**深语句轨**（500 样本 `--stmts 30`）全一致。
2. **定向形态探针六族 900 实例**（补生成器盲区·设施 `/tmp/cn_probe/{gen_probe,run_probe}.py` 临时不入库）：极值/移位/布尔/无符号/转换/回绕——**命中 2 类**：
   - **T26（P1·x86_64 单侧·-O3）**：布尔 `&&`/`||` **值位被常量化**——左操作数为非常量（布尔变量/取非/函数调用/含变量的混合）时 `||`→恒真、`&&`→恒假；值位消费（打印/算术嵌入/比较/赋值）全挂，条件位（如果/当 直判）正确，短路副作用正确；**仅 -O3**（O0/O1/O2 全对）；**左操作数为比较表达式时免疫**（常量折叠消除短路分支）；**v2 免疫**。最小复现=`布尔 甲=假;布尔 乙=假;布尔 丙=甲||乙;打印(丙);`（O3 出「真」应「假」·文件名 /tmp/cn_probe/min/m2.cn）。**铁证**：`./target/cn ir <f> -O3` 显示 bb1/bb2 尾注入「搬运 %v6 <源>」后 **bb3 使用点被替换为常量 `真`**；asm 使用点 `mov rdi, 1`（正确形态 `mov edi, dword ptr [rbp-48]` 槽读）。**根因**=256-a F1-26「SSA 使用点重写×多前驱 Copy 注入（占位 Phi 化）」常量传播缺多前驱一致性检查——**与单位机 T25（浮点三元 -O3 归零）同根因族**→ §3-B **B11 合并根治呈报**（待用户裁决）。
   - **T27（P1·崩溃族）**：`INT_MIN / -1` 与 `INT_MIN % -1` **SIGFPE 崩溃**（rc=136）：宿主 -O0 **32/64 位均崩**、-O3 64 位崩（32 位常量折叠路径 rc=0）；v2 自举链 64 位崩、32 位不崩但输出 2147483648（未回绕）；对照=除零已有「运行时错误(错误码1)」防护✓，非 -1 分母极值除法全对。→ §3-B **B12 并入 C18 呈报**（待用户裁决）。
3. **B9 族扩散**：30+14 形态矩阵 × O0/O1/O2/O3 四级 × 消费位（值位/条件位/实参/返回/算术/三元）× v2 侧（v2p 自举链四样本对照）。
5. **批 2 采样（不同种子扩样·命中稳定性）**：bool 族 500 → **298 diff**（O3 值位·T26 稳定复现）+ extreme 族 500 → **27 SIGFPE**（T27 稳定复现）；累计命中 bool 387/650、extreme 35/650。
6. **批 3 负向诊断轨（面台账行 51 复验）→ 发现 T28（测试设施缺陷）**：`--negative` 400 样本全部拒绝，但**拒绝原因不纯**——注入器追加的 `# 注入类型: X` 行非 CN 注释语法（被报「未知预处理指令」），使全部样本必然失败于该行；**独立复验（去 # 行·5 类注入直测）**=删分号/缺右括号/缺右花括号/非法字符/截断 **5/5 rc=1 诊断精确**（正面结论成立·既有「1000/1000」机制无效）→ 立案 **T28**（双缺陷：①`#` 行污染 ②注入器**静默跳过**=随机行不含 `(`/`;` 时无操作致假注入；组合效应=负向轨检验力≈0）。**修正版独立复验（自建注入器·去 # 行+重试选行）400/400 全拒绝且诊断精确 ✓**（正面结论成立·机制无效）。
8. **批 4 形态扩散（T26 扩面）**：短路副作用（右侧函数调用）/循环体内值位/结构体字段构造位——**循环体内值位挂**（O3 真 vs O0 假）、副作用右侧与结构体字段位正确（=常量传播未触发路径）。
10. **命中库入库（防 target 清场丢失·根治轮复现素材）**：`tests/cnsmith_hits/diff/` s2609179001~003（T26 三形态）+ `run_err/` s2609179004~005（T27 32/64 位）——带 .meta（基线/轮/说明）；daemon 回归轨将自动持续追踪（修复后应转「已修」）。
11. **批 6 边界扩面（T26）**：函数返回值/结构体字段（作短路操作数）/比较位/函数调用参与短路**均挂**（`恒等(甲)||恒等(乙)`→O3 真、`箱.旗||甲`→真、`真==(甲||乙)`→真）；**条件位（如果 直判）正确**——值位消费面全挂·条件位免疫为准确边界。

9. **批 5 跨构件对照（T25 平台面）**：整数三元/if-else 汇合赋值/布尔三元/**浮64 三元（非常量条件·同 T25 形态）**在 **x86_64 侧 O0/O3 全对**（2.500000/3.500000）——**T25（浮点三元 -O3 归零）为 arm64 单侧**；T26 为 x64 单侧（v2 免疫）——两案同机制（256-a 使用点重写）但**表现平台不同**，B11 合并根治须三后端同查。
7. **落盘**：plans/021 T26/T27/T28 立案 + §3-G 处置段 + §3-B B11/B12；面台账行 58 ⬜→🔶（daemon 常态化实证）+**行 61/62 新加**（T26/T27 面行）+统计行（61/62 PASS）；plans/025 §二收工回填。

### 二、验证链（本机复现口径·当轮实测）

```
python3 scripts/cnsmith_daemon.py --rounds 0 --samples 250 --sleep 90   # 23 轮 7889 差分全一致
python3 /tmp/cn_probe/gen_probe.py --family <六族> --count 150 --seed 26091700 --out <dir>
python3 /tmp/cn_probe/run_probe.py --dir <dir> --cn ./target/cn --jobs 4   # 900 形态三方对拍
./target/cn build <f>.cn --target linux-x86_64 -O0/-O1/-O2/-O3 ...   # 四级复现
./target/cn ir <f>.cn -O3                                            # IR 铁证
python3 scripts/check_handoff.py / check_progress_sync.py / check_matrix_coverage.py  # 独立执行验退出码=0
```

### 三、下一轮任务（按序）

1. **T28 搭车**（测试设施轮·注入器标注 + 负向轨判据升级·可随 T26/T27 根治轮）。
2. **用户裁决 B11/B12 后**：T26 根治（Copy/搬运使用点重写路径·与单位机 T25 合并·三后端同查）+ T27（并入 C18 口径）——均带负测 E2E 转正 + 402 守护用例扩面（现用例布尔变量仅 真/假 各一值·恰好避开暴露形态）。
2. **T26 win/arm64 复验**（本机无 aarch64 工具链·同源代码路径——归家机/单位机）。
3. daemon 常态化续跑（生成器补布尔/移位形态=行 58 欠账·需开发锁）；B8 面台账巡视续行（行 57 平台面余列）。
4. 探针设施 `/tmp/cn_probe/` 可复用（六族生成器+三方对拍 runner）——根治轮作回归复核；不入库（/tmp）。

### 四、诚实边界

- 本轮**编译器源码/测试用例/门禁脚本零改动**（验证轮纪律）；命中全部**只立案不修码**。
- T26 仅 x86_64 实测（win/arm64 未测）；T27 arm64 未测；daemon 采样覆盖面受生成器盲区限制（裸布尔/极值不生成）。
- daemon 与同机 292-a 会话并发过一次（月文件 rebase 手工合并·daemon 短暂停机做 git 同步）；月文件与单位机 arm64 轨共享（并发追加·§8.5 兜底）。
- 采样日志体积：daemon 无变化轮合并「汇总」行（S1b 记法）；本轮新命中均在 /tmp 探针侧（daemon 侧零命中）。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🧪 M3 采样轮⑤收工（五连轮）——daemon 全绿+行 22/17 arm64 复验零命中。**在飞呈报待裁：T24（后缀自增值语义）/T25（浮点汇合赋值 -O3 归零·日常高频·紧急）**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **M3 采样 2 轮全绿**：293×2 一致/分歧 0/失败 0。
2. **行 22 常量折叠 5 形态+行 17 优先级结合性 10 关键对**三方对拍全一致（C vs CN-O0 vs CN-O3·O0=O3）——零命中；台账行 17/22 注记 arm64 增量复验绿。
3. 口径确认（非缺陷）：顶层常量=推断形态 `常量 名 = 值`（`常量 整32 x` 类型前缀形态按规范拒绝）。

### 二、本日五连轮总账（单位机 M3 正确性收敛·全部已提交推送）

- **轮①**（cb17b86）：daemon 293×2+B8 行 9/12/52/57 arm64 复验闭环（T12/T13/T16）+**B10 呈报**（三元 ? 歧义裁决代价面·§3-B）。
- **轮②**（a2a5b8d+修复 063e949）：daemon 393×2 大样本+行 14 一元算符面 **T24 立案**（后缀自增 i++≡++i·5 消费位分叉·规范缺口=01b 无 ++/--）。
- **轮③**（5d718d1）：daemon 293×2+行 13 三元扩面 **T25 立案**（浮64 三元 -O3 归零·IR+asm 槽位双铁证）。
- **轮④**（64f84d0）：daemon 293×2+行 52 浮点汇合族扩面——**T25 面重大扩充**（if/else 浮点同名单同挂·最小日常形态实锤·免疫面划清）。
- **轮⑤**（本轮）：daemon 293×2+行 22/17 零命中。
- **M3 计时轨**：daemon 累计 10 轮全绿（不同种子/样本规模 200/300·回归轨 93 持续归零）——T22 零复发。

### 三、下一轮候选

1. **M3 常态采样轮⑥**：行 8 算术/行 10 位运算/行 11 比较算符 arm64 矩阵（候选行渐清→回归已立案行复验+daemon 纯采样）。
2. **T24/T25 裁决后根治**（T25 紧急·浮点 Copy 发射链三后端同查+负测 E2E；T24+T25 可同轮=表达式值语义+发射链——本机可认领开发轮）。
3. A12 里程碑标签锚点搭车。

### 四、验证链（本机复现口径）

```
python3 scripts/cnsmith_daemon.py --rounds 2 --samples 200   # 293×2 全绿
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --filter 403   # PASS（轮①）
# 各矩阵探针=plans/025 §三.12~§三.16（/tmp 已清理）
python3 scripts/check_handoff.py; echo 退出码=$?   # 独立验证（教训落实）
> 源码零改动→全量门禁各轮均未重跑（基线链 46662c3→64f84d0 全绿承载·五轮一致）。
```

### 五、诚实边界与坑

- T25 win/x64l 同挂风险高（优化层重写平台无关）——各机复验登记；k02 对 vs m1 挂差异=根因甄别线索。
- T24 下标位/字面量宽化形态、T25 浮32 穷尽面=各自根治轮补。
- C 探针模板教训（本日两踩）：gcc 不认中文标识符（一律 ASCII）+必带 stdint.h；cn 必须项目根调用。
- github 镜像本机无凭据未推（既有口径）；五轮全程与他机零交叠。