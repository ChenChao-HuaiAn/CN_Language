# HANDOFF 交接文档

**交接时间**: 2026-09-11 第七十二轮（**深度机 linux-x86_64**）——**72-a 六枚缺陷全链根治（F1 跨模块拥有契约/F2 v2 落空路径/F3 宿主块级作用域 RAII/F4 赋值转移/F5 引用出参/F6 变参 AL）**。**六枚全部闭环，验证链全绿，固定点口径 266964**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案方式**：按上轮 HANDOFF「4'-三 收尾评估」清单**逐项写探针实测**（不读代码推断）——发现五枚静默缺陷，其中三枚 UAF/永久泄漏级别。
2. **F1 跨模块拥有契约失效（静默永久泄漏）**：`semantic_call.cpp:968` 用 `sigKey.find('$')` 区分「泛型单态化产物」，但跨模块函数链接键也是 `模块$名` 形态（`functionLinkKey` 公式）→ **凡导入模块的函数返回 字符串，调用方一律不登记 RAII**。IR 对照实证（同文件 `调用 [本地造串]` 有 `__cn_str_free` / 跨模块 `调用 [工具$造串]` 无）；运行级：stdlib `替换` 跨模块调用后活动分配残留 1。影响面=81 个含导入 E2E 用例 + stdlib 全部 `-> 字符串` 函数。修复=按 `genericFuncInstances_` 精确判定（rustc DefId 同款）；**v2 侧同族三处**（生成声明/生成语句/IR容器 纯名查）改 `查函数返回回退` 键基回退版。
3. **F4 赋值位转移非真 move（UAF）**：`目标 = 转移(源)` 原为共享句柄 + 源出口释放 → 目标悬垂。stdlib `字符串扩展.cn:72` `缓冲 = 转移(拼接)` 实测输出乱码（被 E2E 用例目录内的旧副本掩盖）。修复=`ir_expr.cpp` 赋值位新增真 move 分支（drop 旧目标 + 句柄直存 + 源槽清零；自转移豁免）。
4. **F5 引用参数出参悬垂（UAF）**：`字符串& 输出` 写入本地拥有串后本函数出口释放源局部（探针修复前后「73」vs「输出数据」；v2 树 解析货舱/拆限定名 两函数踩中，被 F1 修复暴露成 E2E 165 回归）。修复=Rust `*out = s` move 语义。
5. **F6 变参调用未设 AL（SysV ABI 违约）**：AMD64 SysV 要求变参调用点 AL=使用的向量寄存器个数（glibc `va_arg` 据此判定 xmm 保存区），CN x64 后端从未发射 → 靠 rax 残留侥幸（`格式化("¥%.2f", 29.50)` 经包装函数输出 ¥0.00）。修复=`linux_x64_instructions.cpp` 调用点按实参统计发 `mov eax, N`。
6. **F2 v2 落空路径零释放（永久泄漏）**：v2 只在 返回 语句路径发射释放，体末无 返回 语句（fall-through）的函数完全不释放（探针 asm 无 `__cn_str_free`）。修复=`生成函数.cn` 体块未终止时在函数尾块前发射出口释放——**首版无条件发射致 196_v2/210_v2/203_v2/208_v2/123_v2 等十用例回归（已返回路径二次释放），条件化后零回归**。
7. **F3 宿主块级作用域 RAII 落地（用户裁决方案A：实施+同步更新用例期望）**：宿主 RAII 由「函数级」升级为「块级作用域」（对齐 v2 块出口析构 / Rust 作用域 drop）——作用域基线栈 + `genBlockExitDestruct`（本块新增项逆序释放+名单截断）+ 中断/继续 `genLoopJumpDestruct`（drop-on-jump）；**释放采用「释放+槽清零」幂等模型**（多路径共享槽，清零后 free(nullptr) 空安全，杜绝双重释放）；函数级释放保留为返回/跳出兜底。**连带根治 v1 树既有缺陷**：块级 RAII 使「拥有串经函数浅共享入容器=元素悬垂」立即暴露（76 自举用例产物常量池退化为指针值）——按 A2 纪律迁移 6 处追加点显式复制（语义/符号表、IR生成/IR1、IR遍历、语法分析、x64后端、arm64后端）；实证为**既有缺陷**（F3 关闭时探针同样悬垂，仅靠内存未复用侥幸）。**用例期望同步（用户批准）**：115 总析构 3→6、116 块内计数全变（注释同步改块级 RAII 口径）；76 经 v1 迁移恢复原期望（1764）**未改测试**。探针实测：循环体/嵌套块/中断路径残留全归零（原 4 轮残留 3）。
8. **验证链全绿**：清场重建零警告 + 单测 1289/1289 + 全量 E2E **234 用例 229 过**（唯一败=194 既有待批）/4 跳 + 锚定链 fix_p **266964 行**（新口径，266459+505）≡fix_s 逐字节一致 + 绑定自检 + 运行级 3/3 + 组件对拍 29/29。
9. **E2E 新增**：212_跨模块契约与转移与引用参数（校验和 255）+ 212_v2_落空路径与拥有串释放（校验和 7）。

## 二、下一轮任务（按序）

1. **呈报清单余项**：194 单行批准（`返回 整32(*q);` 改写方案已呈报三轮无答复）；win 平台 v2 并行隔离（家机轮）；第二层第三批收口时点（迁移全量完成=前置就绪）。
2. **71-a 后续可选题**：构造器实参类型匹配检查；构造器名重定义诊断。
3. **其余 4'-三 收尾项**：装箱临时值生命周期（`正常(f())` 形态）；数组元素/装箱字段/参数/静态借用面 RAII（随 NLL 轮）；跨编译器结果 ABI 互操作 E2E 锚定；构造器负路径用例固化；**类对象块级析构扩展**（现限「有析构函数」类，无析构类不发射=与函数级口径一致）；**v2 侧块出口析构的对偶确认**（v2 已有块出口析构，但落空/跳出路径由函数级兜底——本轮 F2 已补落空，跳出路径的 drop-on-jump 待专项核验）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处 warning/error 匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 234 用例 229 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（宿主改动后须先删 v2p 缓存键重建）
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
```

## 四、本轮踩坑（已入 lessons 第七十二轮 权重8）

- **「新语义使旧潜伏缺陷暴露」的既有 vs 新引入甄别**：F3 落地后 76 自举用例产物常量池退化为指针值——**关闭 F3 复测探针 86 发现同样悬垂**（仅靠内存未复用侥幸）=既有缺陷（v1 树「字符串入容器浅共享」未按 A2 纪律迁移），非 F3 引入；判据=关特性复测。
- **多路径共享槽的释放用「释放+清零」幂等模型**：块出口/跳出/函数级兜底三路径交织，用「谁释放谁登记」的排除法必然出现双重释放（首版实证）；清零后 free(nullptr) 空安全。
- **测试期望更新逐条核对语义**：115 的 6=向量操作 3+块出口 3 逐项可解释，不是「跑出什么写什么」。
- **符号名模式判型**：`$` 既是泛型实例分隔符又是跨模块链接键分隔符（同一字符两义）——模式匹配必然误伤，且失效方向是「不登记 RAII」静默泄漏。判型必须查权威表（实例表/DefId）。
- **显式 move 语义须全位置确认**：转移语义只在声明位实现，赋值位/引用出参位留「值加载」形态=共享句柄（UAF）。
- **C ABI 变参契约**：变参调用点必须设 AL（clang/gcc 恒有 `xor eax,eax`/`mov al,imm`）；「能跑」=rax 残留侥幸属 UB。
- **RAII 出口按控制流出口枚举**：返回/落空 fall-through/中断/继续 每出口逐一确认；v2 首版无条件在尾块释放=已返回路径二次释放（十用例回归）。
- **宿主改动后必须删 v2p 缓存键**：`target/audit2/v2p_build_key_linuxx64.txt`（指纹含宿主编译器 mtime——但测试路径 `--filter` 与全量可能命中不同缓存态，判「v2 回归」前先清键重建 v2p）。
- **测试期望变更须用户批准**：F3 首次实施后先按纪律回退呈报（改变 115/116/76 断言语义），用户裁决方案A 后才同步更新期望并逐条核对新值语义。

## 五、诚实边界

- **F3 边界**：类对象块级析构限「有析构函数」类（无析构类不发射 DeleteObject=与函数级口径一致，其字段容器由预扫登记单独覆盖）；v2 侧块出口析构——落空路径本轮 F2 已补，跳出路径（中断/继续）drop-on-jump 待专项核验；v1 树迁移仅覆盖容器追加（其他逃逸形态如结构体字段持串随 NLL 轮）。
- 构造器实参类型匹配不检查（v2 语义层半实现面）；装箱结构体实参 CopyStruct 深拷内含容器字段浅共享（宿主同边界）；装箱载荷接收面=借用面无 RAII（宿主同边界）。
- 跨编译器结果 ABI 互操作（v2 编译代码调宿主 stdlib 结果返回函数）未单独 E2E 锚定；v2 契约登记按纯名/键基回退查（重载族返回异型=近似）。
- 数组元素/装箱字段/参数/静态=借用无 RAII（随 NLL/容器析构轮）；win-x64 v2 并行未隔离（家机轮）；194 待批维持。
- F1 的 stdlib 面（跨模块 `-> 字符串` 函数）现按契约登记 RAII——stdlib 内部若存在「返回借用视图但签名 字符串」的函数属类型谎言，本轮的键基回退会对其生效（E2E 全绿=当前 stdlib 无此形态，新增函数须遵守契约）。

## 六、关键产物位置（target/ 不入库）

- 深度机：target/p71x64l/（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **266964 行**、cn_self71）。
- 探针（/tmp/p72probe 临时，不入库）：探针61~86（临时装箱/RAII 覆盖面/释放粒度/严格测量/缺口矩阵/转移赋值/引用参数/变参 AL/逃逸形态/借用视图/容器浅共享）、iso1/ta~td/ts/ts5/t52c~e/t76/t116（隔离对照与插桩实验）。
- E2E 新用例：tests/e2e/212_跨模块契约与转移与引用参数/（主.cn+主.expected+字符串扩展.cn）+ tests/e2e/212_v2_落空路径与拥有串释放/（主.cn+主.expected+v2闭环.txt，数据驱动零注册）。
