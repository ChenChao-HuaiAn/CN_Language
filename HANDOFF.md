# HANDOFF 交接文档

**交接时间**: 2026-09-11 第六十八轮（**深度机 linux-x86_64**）——**A2（阶段4' 第三批正文）迁移面盘点呈报轮**：完成 stdlib/v2/tests 全树盘点与三方案设计，**呈报待用户裁决，未动任何代码**。上一轮（第六十七轮）为 A1 字符串借出初始化拒绝+E2E 三重改造，固定点新口径 **254771**。

---

## 一、本轮做了什么

1. **A2 迁移面全量盘点**（呈报前置，已落档 plans/019 跟踪表 4'-A2 呈报行）：
   - 全树 `-> 字符串` 函数 52 个：A 类全拥有 ~25（零改动）/B 类混合借用分支 15 处（须 字符串复制 显式拥有化）/C 类纯借用 2（当前文本/推导实参类型文本→改 `-> 字符*`）；
   - 内置规范变更：驻留文本 返回类型改 字符*（v2 树 186 调用中 65 处「字符串变量=驻留文本」须复制包裹，~10 处循环内热点须审计）+转字符串族入拥有判定；
   - **手动释放契约退役面（最大存量破坏）**：tests 18 文件 ~106 处 字符串释放 A2 后=双重释放须删除；4 个测试函数体须迁移（56 读名称/52 读取书名·读取ISBN·校验编号）——**全部测试文件修改须用户批准**；
   - 实现面：宿主 4 点（白名单∪返回类型判定/返回位借用拒绝/返回位字面量拥有化/字符*→字符串收紧）+v2 3 点。
2. 三方案呈报：甲=完整类型化+存量全迁移（推荐，两轮落地）/乙=仅登记不收紧（不可行——free 借用=UAF）/丙=字符串视图新类型（与方案A 裁决冲突）。

## 二、下一轮任务（按序）

0. **等用户裁决 A2 方案**（甲/乙/丙或其它意见）→ 批准后按批次实施（甲=轮1 宿主语义/IR+驻留文本改字符*+v2 树 65+15 处迁移+tests 适配+E2E 210；轮2 v2 侧机制+固定点重锚+对拍）。
1. 呈报清单余项：①194 单行批准；②win 平台 v2 并行隔离（跨机轮）；③第二层第三批收口时点（建议反馈期后）。
2. 阶段5 等并发库立项；NLL 等反馈期数据。

## 三、验证链

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量并行（18~28s；--jobs 1=串行）
# v2p 构建缓存：target/audit2/v2p_build_key_linuxx64.txt（v2 源码/编译器变化自动重建）
# 拥有语义验证：208_v2 用例 asm 含 __cn_str_copy/__cn_str_free（target/audit2/v2work208/）
bash target/p67x64l/run_chain67.sh   # 锚定链：fix_p 254365 →固定点一致+运行级 3/3
bash target/p67x64l/cmp29_67.sh      # 对拍 29/29
```

## 四、本轮踩坑（无新坑——盘点轮零代码改动）

- 盘点脚本两处正则误判当场识破修正：①字面量含 `[`（"[rbp-..." 拼接串）被误判成员/下标借出——分类器需先剥字符串字面量再匹配；②`返回 名` 类标识符须区分绑定（参数=借用直传/局部拥有=移出/静态=借用）——逐函数体核实后归类。

## 五、诚实边界

- A2 呈报数字口径：52 函数=「v2 树 30+stdlib 3+tests 19」按返回类型粗扫；B 类 15 处为函数体修改点（非函数数）；106 处手动释放含 13 处 stdlib 副本文件（35/38 字符串扩展.cn 自带）——批准后迁移时逐处复核。
- 字符串释放 函数定义未定位到（stdlib/宿主 common 无 grep 命中——疑内置映射 __cn_str_free，实施时确认）。
- 69 用例手动释放+第一批 RAII 现状重叠，靠 cn_free_tracked 运行时防御兜底（非设计意图）——A2 语义统一后消除。
- 用户函数返回字符串=借用（缺口②）维持现状直至 A2 落地；转字符串族不入白名单（38_tool 依赖）维持；194 待批维持。
- win-x64 的 v2 产物未隔离（--jobs>1 时 v2 桶串行兜底）——跨机轮验证项；ARM64 并行未验（脚本平台参数化已就绪）。
- 中间态谜团（未复现）：257033 行口径的 cn_self 曾现 45 个「成员字段偏移未知」（audit2 旧 .o 链接版本），全开版 254353/254365 闭环无此象——若复现按 lessons 第六十七轮排查路径走。
