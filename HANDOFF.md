# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🏃 339-a 开发轮：D38 批3a=入口函数签名检查三合一（304/372/374）·基线 1c58120·分支 任务/家机-339-D38批3a·2026-09-18）

**规则（宿主矩阵 z01/z02 实测定）**：主 返回白名单=整32 ∨ 结果<T,E>（T/E 均整数族）；参数必须空；无主即拒。**实现=自包含现场扫描**（语义分析多文件 聚合后扫顶层声明区——静态写须 不安全 函数会沿收集链传染·改零静态方案）。**验证**：d38_scan **13 拒/4 缺**（十七项防线余 4 真缺口）+单测 1353+全量 E2E 零新增回归（数字见提交信息）。

**本轮沿革**：338-a T56（词法 f/F 后缀·两端口径闭合）已集成 a9dcfe0；339-a 批3a 收工待集成。

### 二、下一轮（按序）

1. D38 批3b（325 模块循环/347 类私有可见性/363 外部语法面——特性缺口级·各需宿主矩阵先行）；
2. B1（185_v2 处置）/T43/T46/T47/T48 待用户裁决。

### 三、坑与边界

- **静态写传染**：v2 语义收集链（声明收集→收集函数声明）非 不安全——链上任何静态写被 安全区边界 拦且加 不安全 向上游传染——**检查类新逻辑优先自包含现场扫描**（参数带 池+程序节点们 即可）；
- 主 返回白名单规则来源=宿主 z01/z02 实测（非「任意整数可截断」直觉——整64 也拒）；
- 更新日志/HANDOFF 整节替换切片：新节列表**永不带节标题行**（lines[:起+1] 已含·332-a off-by-one 同日两犯教训已模板化）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-18 🏃 **351-a 423 SIGFPE 阶段二三后端闭环（arm64 除零+32 位 w 分派+win 双保护·接力 350-a）**。分支 `任务/深度机-351-除法保护三后端`（头 **6071e27**·基点=350-a 头 2ca36de）——**try-build 请求⑤已发（看板通告段·点名 家机 win/单位机 arm64），等回签后 `integrate.py --try-build-done` 集成，顺序 …349-a→350-a→351-a**。

### 一、351-a 本轮做了什么（T27 v2 侧同构·三后端闭环）

1. **背景**：423_整数溢出回绕统一 的 v2 产物 SIGFPE（349-a C20 批C 发现）——v2 除/取余发射无除零检查、无 INT_MIN/-1 保护、32 位类型走 64 位域。350-a 阶段一修 x64l；本轮（351-a）补 arm64/win 两后端=**维度矩阵 24 格全绿**（操作码 2×位宽 2×符号性 2×后端 3·lessons 347-a「维度全枚举」应用）。
2. **arm64**（`编码算术ARM64` 除/余合并分支）：①除零检查（`cbnz x10, divok_N` / `movz x0,#1` / `bl __cn_runtime_error` / ret 占位——ARM sdiv/udiv 除零返回 0=静默错值·错误码 1 对齐宿主）；②32 位 w 寄存器分派（`sdiv/udiv w9`+`msub w9`+出口 `sxtw/uxtw` 回填·264-a 惯例）；③**有符号 INT_MIN/-1 免特判**（ARM DDI 0487：SDIV 溢出商=被除数、w 算术 2³² 天然回绕=B12 甲语义·与 x64l/win 特判结果逐值一致——注释已落档防后人误补）。
3. **win**（`编码算术Win`）：与 x64l 350-a 版逐行同构三段式+**win 约定适配**——首参 `mov rcx,1`（非 rdi）+shadow space `sub/add rsp,32`+EXTERN 自动收集（`收集EXTERN符号` call 行扫描·P7b 机制）。
4. **探针取证先行（p1/p2·target/p351a 留档）**：p1 越界样例交叉 asm 观测**既有 IR 通道错误调用约定**（win=`sub rsp,32/mov rcx/call/add rsp,32`·arm64=`ldr x0/bl`）——发射层手插同款镜像，不另发明约定；p2 446 源交叉指纹（win 13 处保护序列+EXTERN=1·arm64 13/13 除法点全带 `cbnz`+位宽分派逐形态正确）。
5. **新用例 446_除法保护矩阵**（13 形态双编译·退出码=1）：INT_MIN/-1 除/余×整32/整64+普通负值（-100/-1、-7/3）+无符号通路（正64/正32 udiv/div）+**变量除零→运行时错误(错误码1)**；32/64 位形态全用**变量除数**（319-a 编译期常量除零硬错误只对双侧字面量触发·T60 常量提升面不混入）。
6. **连带交付（门禁扩面）**：check_handoff.py 开工自检补「**远端 develop 看板**」判据——接力分支基点早于看板直推提交（窄通道在 develop·不在接力基点内）时本地看板不含分支名=误拦（351-a 本机开工实测命中）；远端已含=直推已执行→放行；三情形双向实证（接力正例/旧判据正例/真未直推反例拦截）。

### 二、验证链（351-a·全绿）

| 项 | 结果 |
|---|---|
| 构建+单测 | 零警告 + **1353/1353** |
| 新用例 446 | **双编译 PASS**（宿主 rc=1 除零终止=v2 同输出同码·13 行 expected） |
| 全量 E2E | **450=444/4/2**（4 红全基线既有 125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·零新增红） |
| 自举链 | **78_v2/79_v2 双 PASS** |
| 交叉 asm 指纹 | win=`movsxd r10, dword ptr`+`cdq`+`idiv r11d`+`neg`+`cmp r11,-1`+shadow space+EXTERN=1；arm64=`cbnz x10`+`sdiv w9`+`msub w9`+`sxtw`/`udiv w`+`uxtw` 逐形态正确 |
| 反证 | 回退两文件→重建 v2p→win 旧 `idiv rcx`×9 复现/保护 0 处·arm64 旧 `sdiv x9`×5 复现/`cbnz` 0 处→恢复→446 复绿 ✓ |

### 三、诚实边界与下轮

- **arm64/win 运行级归 try-build 回签**（本机=x64l 真跑+两后端静态指纹；win 预期=INT_MIN/-1 回绕+除零 rc=1；arm64 预期=同宿主值+sdiv 回绕）；
- **T60 独立轮待排**（常量 -1 类型码非整32→公共提升整64·`a32 / -1` 字面量形态 64 位域=2147483648 vs 宿主 -2147483648——类型推断/公共提升面）；
- 性能：除法 +2 分支≈5-10% 除法开销（安全优先·自举链行为不变兜底）；
- 下一轮候选：T60（除法保护收尾）／352-a 家机已认领 D36（v2 遮蔽恢复——深度机不撞）／集成队列消化（331~351 九条分支全等回签）。

### 四、集成队列（9 条·顺序敏感）

**331-a → 337-a → 338-a → 346-a → 347-a → 348-a → 349-a → 350-a → 351-a**（后六条接力·本机同族；331-a 含 T50=426 转绿前提；全部等 try-build 回签后 `integrate.py --try-build-done`）。

### 五、踩过的坑（新会话必读·增量）

- **接力分支的看板自检误拦**：分支基点（上轮分支头）不含本轮看板直推提交（窄通道直推 develop）→ check_handoff 开工自检按本地看板判=误拦；已修（补查 `git show gitcode/develop:三机任务看板.md`）；
- **`cn build --output`/`cn run` 目录创建在中文路径下 shell 断裂**（`sh: Unterminated quoted string`+「无法创建输出目录 target\」）——绕过=输出到已存在目录（target/audit2/）或走 runner；宿主 CLI 缺陷待立案；
- **v2 树改后 423 类验证必须重建 v2p**（`target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64`·约 20 秒）再跑——旧 v2p 会得假结论；
- 历史坑（349-a 及以前）见 git 历史本节与 lessons.md。

## 单位机 ARM64 节

**交接时间**：2026-09-18 332-a（看板先行制度·已集成 d00c08c）＋ 单位机 arm64 try-build 回签完成（d968794）＋ 329-D12 在飞（并行会话推进中）。**接手前先 `git fetch`——三机看板行/通告段常只在各自分支内，须读远端分支看板（`git show gitcode/<分支>:三机任务看板.md`）＋看板本机小节，别信本地文件。**

### 一、本轮做了什么（332-a 制度轮·已集成）

1. **用户令落地**（2026-09-18「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」）：AGENTS §8.1「看板先行」（开工第一动作＝直推看板本机行→才开分支；收工集成成功即清行直推销账）＋§8.5「强制直推、即改即推」。
2. **check_handoff「开工自检」**（含自证 bug 修正）：分支 `任务/*` 且未集成时看板必含该分支名，否则拦截；**豁免**「分支已集成」（`git merge-base --is-ancestor HEAD develop`）——收工销账清行后不再误拦。三情形实证（正例／销账态／未集成+无看板行）。
3. **看板表结构**：单表三行 → 三机独立小节（消除相邻行 rebase 必冲突·当日实测两次）。
4. **深度机 331-a try-build 回签**（§8.3·单位机 arm64 列）：**✅ PASS**——基线 **da490f4**（最新头；首轮基于 7d29d42 未含 T51/T52 增量，已重跑），零警告构建＋单测 **1363/1363**＋E2E 全量 **446=444/0/2**（0 失败·无新增红）。证据=独立 worktree 全量跑测（`/home/user/projects/cn_tb331`）。
5. **T50 撞车回应**：本机 329-D12「随载 T50」与深度机 331-a T50 为**同缺陷双实现**（本机=文本形态口径·双后端各改一份；深度机=`types::isStaticScalarInitType` 单一归属）——**已声明同意取深度机实现**（集成顺序孰先孰后均以其取齐）。

### 二、验证链

（332-a）check_handoff 正反例三情形＋integrate.py 快速门禁（两批集成 b45e4ae／d00c08c）；（331-a 回签）独立 worktree 全量：零警告构建＋`./target/cn_unit_tests` 1363/1363＋`run_e2e.py --cn target/cn --target linux-arm64 --jobs 8` 446=444/0/2。

### 三、实测发现（三机协同实况）

- **家机 329-a**（`任务/家机-329-D38批2`·v2 语义层 6 项）在飞；**深度机 331-a**（`任务/深度机-331-T11静默防线`·头 da490f4）**等家机 win 侧回签**（MSVC C4062 守卫实证）后集成——**arm64 侧已由本机回签 ✓**。
- **单位机 329-D12**：并行会话推进中——远端头 ffc3151（收工＋看板 try-build 请求），**本地尚有未推提交 e1afc7e（补③：验证面补强）**；主工作树有未提交改动（货舱特性延伸：`CN语言编译器v2/主.cn`／`货舱解析.cn`／`src/cn_compiler/driver/cargo_parser.cpp`／310 双编译对照）——**接手勿动、勿回滚**。
- **跨机轮次号撞号实锤**（家机与本机同为 329-a）——机制缺陷待用户裁（候选：认领前先 fetch 看板行＝先到先得／机号段隔离／跳跃取号推广全机）。
- **本机 329-D12 已发 try-build 请求**（随其收工提交）→ 待家机/深度机应答。

### 四、下一轮候选

1. **家机**：应答深度机 win 侧 try-build（C4062 守卫实证）＋单位机 329-D12 的 win 侧回签。
2. **深度机**：集成 331-a（`--try-build-done`）＋应答单位机 329-D12 的 linux 侧回签。
3. **单位机**：329-D12 收尾集成（T50 让位按深度机单一归属实现取齐）。
4. C20 波2／C25（v1 残留清除·70~77＋89 用例处置）；A12 里程碑标签。

### 五、诚实边界与坑

- **worktree 位置坑（332-a 实测）**：CMake 用 `${CMAKE_SOURCE_DIR}/../../third-party/unittest/googletest` 找本地 gtest——**worktree 必须放在 `/home/user/projects/` 深度**（`/tmp/xx` 会让 `../../` 解析错位→配置失败）；产物在 **`target/` 根**（不是 `target/build/`）：单测=`./target/cn_unit_tests`、编译器=`target/cn`。
- **Python 切片整节替换 off-by-one（332-a 自证 bug）**：`lines[:73]` 含 index 72 那行节标题，再拼新标题 → **标题重复**（HANDOFF/更新日志各一处·本批已修）；正确=切到标题行**前**（`lines[:72]`）。
- 开工自检判据已含「已集成豁免」（本轮自证 bug 根治）；非 git 环境自动跳过。
- **别信本地文件**：三机在飞状态必须 `git fetch` 后读**远端**分支/看板（332-a 直接教训：本地看板三行全空、实际两机在飞）。
