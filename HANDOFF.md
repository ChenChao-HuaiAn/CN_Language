# HANDOFF 交接文档

**交接时间**: 2026-09-11 第七十四轮（**深度机 linux-x86_64**）——**plans/020 矩阵靶子 #1 销项：容器元素所有权全链归一化**（五枚静默缺陷同轮根治，双侧对称）。验证链全绿，固定点新口径 **271056**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：按上轮 HANDOFF 任务 #1（plans/020 矩阵靶子 #1「入容器编译器级自动 copy/move」）起手，核验中确定根因是**两个半截机制**：容器析构**无条件释放全部元素串**（假定「元素所有权归容器」），而**入容器位不接管所有权**（实参句柄被浅存）。两者各自看都正确，合起来是一族静默缺陷。

2. **探针实证（五枚形态，全部可复现）**：
   - ① **借用句柄入容器 → 容器析构释放调用方拥有的串**（探针 74-L：跨函数 UAF，打印「容器销毁后 本地」输出乱码）——**P0**；
   - ② 局部拥有串裸标识符入容器 → 源 RAII 释放 + 元素浅共享=悬垂（探针 74-A：3 元素内容全乱码）——**P0**；
   - ③ `转移(源)` 入容器非真 move（源槽未清零，探针 74-J）；
   - ④ **v2 侧容器元素从不释放**（v2 无任何元素释放机制=防线不对称，探针 74-G 残留 2）——P1；
   - ⑤ **链表/栈/队列元素串从不释放**（宿主只覆盖 `向量`，探针 74-N 各残留 1）——P1。

3. **方案裁决（用户未答复呈报）**：呈报四方案（A 完整归一化 / B 条件释放位图 / C 编译错误强制显式 / D 仅补 v2）后**用户未答复**（AskUserQuestion 返回「未提供答案」）——按 AGENTS.md 第 3 项纪律（①性能最好 ②最安全 ③参考 Rust）**自行裁决方案 A**，并在 plans/019、plans/014、更新日志、lessons 显式标注裁决依据与备选方案（可复核、可回退）。

4. **宿主实施**（`ir.hpp`/`ir_oop_call.cpp`/`io_api.cpp`）：
   - **入容器位所有权归一化**（Rust `Vec<String>` 对照）：字面量→驻留借用（零复制）；调用返回（拥有契约）→直接接管（零拷贝）；`转移(拥有局部)`→**真 move**（句柄直存+源槽清零，零拷贝）；其余（标识符/成员/下标/借出）→`__cn_str_copy` 落堆；
   - **元素释放面补全**：向量/栈=`__cn_vector_free_strings`（平铺）；**链表/队列=新增 `__cn_chain_free_strings`**（**按链游**——出队/删除头部已把元素所有权转移给调用方，槽序号可能 < 元素数量，平铺释放会误释放已移交的串）；两辅助均含**元素槽清零**幂等不变量；
   - 单点事实源 `emitContainerElemFreeFor` 统三处释放路径。

5. **v2 实施**（`IR容器.cn`/`生成表达式求值.cn`）：新增 `发射容器元素释放`（按模型分派，挂三处释放路径）+ `归一化入容器实参`（同宿主分级，`转移` 分支统一复制=诚实边界）。

6. **两个 v2 特有实施不变量（本轮踩出并固化在代码注释）**：
   - **调用发射时序**：插入式调用须在 `IR_调用实参` 收集**之前**发射（否则其 `IR_调用参数化` 把已收集实参一并展开，位数错乱）；
   - **节点池后缀序**：实参**根**节点在调用节点**前一位置**，不可用 `子起点+1+序`（探针 74-H：误判借用 → 多一次复制 + 原值泄漏，残留 3）。

7. **成效（量化归零）**：E2E 218 校验和 **24→31**；真 move 零拷贝实证（预分配后增量 **0**，深拷贝增量 1）；**E2E 219 双编译对照两侧 63 一致**；生成物层两侧均有平铺+链式释放调用（宿主 20+7 / v2 13+2）。

8. **验证链全绿**：清场重建零警告（4 处匹配=文件名含 error 既有口径）+ 单测 1289/1289 + E2E **241 用例 236 过**（唯一败=194 既有待批）/4 跳 + 固定点 **271056 行**（新口径）逐字节一致 + 绑定自检 + 运行级 3/3 + 对拍 29/29 + 移植纪律 8 三列核对。

9. **文档五件套**：plans/014（状态行）+ plans/019（跟踪表行）+ plans/020（A14 靶子销项+B28/B29 新格+防线表 218/219+第七十四轮节含方法论三条）+ lessons（第七十四轮段，权重 8，预防⑱~㉑）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **plans/020 矩阵剩余靶子**：①数组元素（`字符串 数组[N]`）借用面（NLL 轮）；②装箱字段接收（`r.值` 读出）无 RAII；③结构体字段持串无 RAII（v1 树迁移仅覆盖容器追加）；④结构体容器字段「声明即用」既有边界（`货架 s; s.表.追加(...)` 报空指针解引用，靶子归属结构体字段初始化轮）；⑤函数级 `genClassDestructorCalls` 的 hasDtor 限制与块级口径统一（观察项）。
2. **本轮登记的新后续项**：v2 侧 `转移(拥有局部)` 入容器的真 move 优化（需把 函数信息 穿透到 归一化入容器实参——表达式生成族 12 函数 + 22 调用点签名改造，独立轮次评估）。
3. **呈报清单余项**：194 单行批准（已呈报六轮无答复）；win 平台 v2 并行隔离（家机轮）；71-a 后续可选题（构造器实参类型匹配/重定义诊断）；阶段 4 第二层第三批收口（不安全区变硬错误，建议真实使用反馈期后）。
4. **矩阵维护纪律**：每轮触碰所有权语义的改动必须同步更新 plans/020 对应格（本轮已按此执行）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处 warning/error 匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 241 用例 236 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（v2 树或宿主编译器改动后须删 v2p 缓存键重建）
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时删缓存键**，否则 runner 会复用旧 v2p（本轮踩坑，见 lessons）。

## 四、本轮踩坑（已入 lessons 第七十四轮段 ⑱~㉑）

- **「A 处假定 X、B 处负责建立 X」的分工必须先写清三问**（谁建立/在哪一行建立/建立失败会怎样）：本轮「容器析构假定元素归容器」与「入容器位不接管所有权」各自独立看都正确，合起来是一族静默缺陷（悬垂/双释放/UAF，内容测试全抓不到）。**释放侧的假设必须与写入侧的约定成对出现**。
- **容器元素释放必须按元素模型分派**：凡「槽序号空间 ≠ 有效元素集合」的容器（链式容器的 出队/删除头部 已转移所有权给调用方）不能按计数平铺释放。新增容器类型时先问「有效元素怎么枚举」。
- **v2 侧实施前先确认两个不变量**：调用发射时序（插入式调用须在 `IR_调用实参` 收集之前）+ 节点池后缀序（实参**根**节点在调用节点前一位置）。判定失真的表现常是「多一次操作」而非崩溃，只能靠内存差值探针发现。
- **人工纪律不是门禁**：A2「字符串入容器须显式复制」是文档约定，编译器不强制 → 静默失效。凡「约定式安全」都要问「编译器能不能强制」。
- **手工替换 v2p 后必须删缓存键**：`target/audit2/v2p_build_key_linuxx64.txt`（否则 runner 复用旧 v2p，验证假绿）。

## 五、诚实边界

- **v2 侧 `转移(拥有局部)` 入容器未走真 move**：判定「源是否拥有」需 `函数信息.字符串变量们/污染们`，穿透需改表达式生成族 12 函数 + 22 调用点签名（风险大于收益）→ 统一复制，语义安全等价（容器独立拥有、源照常释放，无悬垂无泄漏），仅多一次分配；真 move 优化登记后续项。
- 嵌套容器元素（`向量<映射<整64,字符串>>`）不释放元素串（元素是容器对象，按 char* 语义释放对象指针会损坏堆）。
- 数组元素/装箱字段/结构体字段持串/静态面无 RAII（plans/020 靶子维持）；结构体容器字段「声明即用」=既有边界（三方对照实证）。
- 构造器实参类型匹配不检查（v2 既有）；跨编译器结果 ABI 互操作未单独 E2E 锚定；v2 契约登记按纯名/键基回退查（重载族返回异型=近似）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。
- **本轮方案为用户未答复下的自行裁决**（依据 AGENTS.md 第 3 项纪律）——若用户裁决与方案 A 不同，可据 plans/014/019、lessons、更新日志中的留痕快速回退（备选方案 B/C/D 已在呈报中列明）。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **271056 行**、cn_self71）。
- 探针（/tmp/p74probe 临时，不入库）：74-A（拥有串裸标识符入容器悬垂）、74-G（v2 元素不释放）、74-H（v2 后缀序节点定位失真）、74-J（转移非真 move）、74-L（借用句柄入容器 UAF）、74-N（链/栈/队元素不释放）、74-O/P/Q/R（逐容器与逐步分配数诊断、真 move 零拷贝实证）。
- E2E 新用例：
  - `tests/e2e/218_容器元素所有权归一化/`（宿主+v2 双通道：五形态校验和 31；含 `v2闭环.txt` + `供给.cn`——链表/栈/队列<字符串> 方法符号供给）；
  - `tests/e2e/219_双编译对照容器元素所有权/`（移植纪律 9 第二个对照用例：六形态，两侧退出码须一致=63）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——A14 靶子已销项，B28/B29 新增两格；每轮触碰所有权语义必须同步更新）。
