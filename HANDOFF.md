# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十五轮（**单位机 麒麟 ARM64**——注意不是深度机！git 最近提交是深度机的，本轮在单位机做）——**R0→R4 全链实施中**：用户审查源码目录发现重复导入等优化点，批准 R0-R4 五轮全链方案"不允许拖延"。R0/R1/R2 全部完成、R3 完成最大件（生成表达式.cn 1084→76 行 + 拆新文件），R3 剩余 13 个超线函数 + R4 三后端未做。**下一步 = R3 剩余函数拆分（编译文件/主/生成语句/检查表达式/检查语句/扫描签名文件/分词/生成函数/解析语句/解析后缀链/决议函数符号/回扫实参取地址/声明收集）→ R4 三后端 生成指令 拆编码函数组**。

---

## 一、本轮背景与方案

用户发现 `CN语言编译器v2/主.cn` 有重复导入（10-14 与 19-23 行五条导入重复），要求审查整个源码目录给优化报告。我做了全目录机械扫描（28 文件 11725 行），产出报告，用户**批准 R0→R4 全链方案**：

- **R0 测试面解耦**：v2 闭环 28 用例把编译器 stderr 诊断流固化进 expected（耦合），解耦为「expected 只锁契约行+退出码」
- **R1 速净**：删重复导入 + IR常量 删冗余导入 + 签名读文件 缺陷③对齐
- **R2 诊断分级**：内部进度打印 `--verbose` 开关化（默认静默），错误契约保留
- **R3 函数拆分**：18 个超 100 行函数按 AGENTS.md §3 纪律拆（同文件内提取优先，文件超 1000 才拆新文件）
- **R4 三后端**：生成指令 314/321/292 同构 → 同文件内提取编码函数组

## 二、已完成轮次

### R0 测试面解耦（完成，171/175 零回归）

**核心发现**：宿主 E2E 本来解耦（比对程序 stdout），但 v2 闭环 28 用例（119~172）走专门分支 `run_e2e.py:971` 把 v2p 编译器 stderr+stdout 合并流与 expected 比对——expected 全是编译器内部进度行（`[ir1]`/`[p6h] 已解析`/`[sem5] 正确程序错误数=0`），不是程序行为。成因：v2 早期无负路径机制，临时锚定诊断行滚成永久契约，反向绑架编译器（60 处打印不能清、`主()` 13 个内置自检每次编译全跑）。

**解耦实施**：
- `主.cn` 删 13 个内置负测自检块（源码2~源码13）+ 映射自检块 + 设当前模块路径复位死代码（共删 ~190 行）
- 新建 `tests/e2e/173_v2_语义拒绝合集/`（负路径用例，12 形态合集，承接删除自检的拒绝能力锚定——一个源码多错误形态，语义层全量收集诊断，expected 固化 13 条 `[错误]` 契约行+计数中止行）
- 28 个 v2 expected 收敛：7 负路径不动（139/141/146/149/168/169/170，149 删一行内部进度）；21 正路径重写为契约行（`[p6h] 已解析`=加载器行为锚定，164/165 等依赖它；其余收敛为空文件——纯退出码断言）
- `run_e2e.py` 注册表加 173 条目（机制零改动，匹配机制保留，只收敛内容）

### R1 速净（完成，171/175 零回归，同族穷尽确认）

- `主.cn` 删 10-14 重复导入块（保留 19-23 组+注释）
- `IR/IR常量.cn` 删冗余 `导入 容器::向量;`（文件自称"无依赖纯常量"，向量未用；映射集合 保留——`映射<整64,整64>` 需要）
- `IR/IR签名.cn` `签名读文件` 换行逻辑对齐缺陷③（2026-09-04 缺陷③：读取文件行 含换行契约→双换行行号翻倍；当时只修 `主.cn 读源文件`，IR签名 侧漏网——同族修复旁路穷尽补修）。同族消费点全 grep 确认：仅两处读行拼接，均已对齐。

### R2 诊断分级（完成，171/175 零回归）

- `IR/IR常量.cn` 加跨组件协议开关：`静态 布尔 详细诊断开关 = 假;` + `设详细诊断(布尔)` + `详细诊断开启() -> 布尔`（协议单一归属惯例，同 `当前函数变量类型`）
- `主.cn` 主() 加第 3 参数 `--verbose` 解析（`系统::参数(3)`）→ `设详细诊断(真)`
- 6 处内部进度打印开关化：`[p6h] 入口`/`[cga] 目标平台`/`[cg1] asm 行数+已写`/`[ir1] IR生成调用后+全函数每指令 dump`/`[sem5] 正确程序错误数=0`
- 错误契约/加载器行为行保留无条件：`[p6h] 已解析`（164/165 锚定）/`[p6b] 货舱已加载`/`[parse] 语法错误中止`/`[sem5] 正确诊断 [错误]`/`[ir2]`/`[p6f]/[p6b]` 失败诊断
- **实效**：v2p 编译 172 用例 stderr 从数千行降到 **2 行**（只剩 `[p6h] 已解析` 契约行）——自举输出税清零，双目标准绳"性能超 C++"解绑

散布组件打印定级（IR容器/IR布局/IR签名/语法 4 处）全为错误路径（`[parse]`/`[ir2]` 契约诊断），无需开关化。

### R3 生成表达式.cn 大拆分（完成，23/23 v2 用例快测零回归）

**最大件**：`生成表达式` 单函数 1084 行 → 76 行；文件 1263 行 → 拆为 `生成表达式.cn` 933 行 + 新文件 `生成表达式求值.cn` 440 行。

**拆分手法**（接口约定，可复用于 R3 剩余函数）：
- 预扫描三段 → 出参 `向量&` 的收集函数（容器按值返回禁用——lessons 15 枚句柄悬垂同族）
- 区间跳过三处（取址/短路/三元，步长 2/3/2）→ 统一 `区间终点(区间们, 位置, 步长) -> 整64`
- 字面量三分支 → `生成字面量(...)`
- 标识符/二元/三元/一元/成员/调用/下标 各分支 → 提取函数
- **分支接口约定**：分支函数收 `节点 n` + 上下文，返回消费 token 数（恒 1）；`0` = 整个 `生成表达式` 须返回 0（类型大小未知/决议失败两个函数级出口）
- **避坑**：标量引用参数（`整64&`）v2 无先例（只有容器 `向量&`/`映射&`），不赌语法——用值参 + 返回约定；出口 `位置++; 继续;` → 函数内 `返回;`（自然落出与 continue 两形态逐字节等价）
- **破环**：`生成下标左值地址` 调 `生成表达式`（递归），**留在原文件**避免循环依赖；新文件 6 函数均不调 `生成表达式`，单向依赖
- 顺带消重：左/右末端类型推导两段同构 → 合并 `推导末端类型码`；成员段三源布局查询同构段合并

**文件拆分**：`IR/包.cn` 加 `模块 生成表达式求值;`（在 `生成表达式` 前，依赖先解析）；`生成表达式.cn` 加 `导入 生成表达式求值;`；新文件 `公开:` 全文件公开。

## 三、未完成（R3 剩余 + R4）

### R3 剩余 13 个超线函数（接口约定已验证，机械重复但量大）

按大小排：
1. `IR/生成语句.cn:42` 生成语句 **708 行**（按语句类型拆：当/循环/如果/选择/中断/继续/表达式语句/声明）
2. `语义/语义检查.cn:108` 检查表达式 **334 行**
3. `语义/语义检查.cn:442` 检查语句 **265 行**
4. `语法/解析语句.cn:12` 解析语句 **247 行**
5. `IR/IR签名.cn:82` 扫描签名文件 **232 行**
6. `词法/词法分析.cn:113` 分词 **193 行**（按 token 类型拆：数字/字符串/标识符/运算符）
7. `主.cn:155` 编译文件 **185 行**（依赖查找链/挂载加载/快照收集三段——脚本切了但锚漂移失败未写入，**当前主.cn 是 R0-R2 后状态，编译文件未拆**）
8. `IR/生成函数.cn:16` 生成函数 **185 行**
9. `主.cn:361` 主() **~190 行**（R0 删自检后；参数解析/货舱/编译/语义IR-codegen 分派段）
10. `语法/解析表达式.cn:144` 解析后缀链 **147 行**
11. `IR/IR生成.cn:560` 回扫实参取地址 **114 行**
12. `IR/IR生成.cn:308` 决议函数符号 **102 行**
13. `语义/语义分析.cn:569` 声明收集 **105 行**

**手法**：同文件内提取辅助函数（零链接键变化，最小风险）；仅文件超 1000 才拆新文件（生成语句.cn 708 行不超线，同文件内拆即可）。

### R4 三后端 生成指令（未开始）

`代码生成.cn:89` 生成指令 314 / `代码生成ARM64.cn:180` 321 / `代码生成X64Linux.cn:156` 292——同构 switch。同文件内提取编码函数组（如 `编码算术X64L`/`编码访存X64L`/`编码控制流X64L`/`编码调用X64L`），每后端 4-6 个 ≤100 行函数。决策层 `代码生成共用.cn` 已有 260 行。

## 四、关键决策记录

1. **环境识别**：本轮在单位机麒麟 ARM64（`uname -m`=aarch64），门禁=`--target linux-arm64`；不是深度机 x86_64。我一开始误跑 linux-x86_64 导致大面积错位（x86 汇编落 arm64 as）——教训：换机先 `uname -m` 确认。
2. **78_chain_build 环境性失败**：本机内存紧张，78 运行内存超限 4112MB>4096MB 被保护性终止——非代码回归（深度机同代码 0 败）。基线口径=171 过/1 环境性败/3 跳。
3. **R0 解耦方案**：机制零改动（`run_e2e.py` 匹配机制保留），只收敛 expected 内容——最小改动最大收敛。负路径保留错误诊断行（对外契约，如 rustc E0XXX）。
4. **R3 接口约定**：分支函数返回消费 token 数（避免标量引用参数这一未验证语法）；`生成下值地址` 留原文件破循环依赖。

## 五、门禁状态（本轮提交前）

- 构建：`cmake --build target/build` 零警告 + `target/cn build CN语言编译器v2/主.cn --target linux-arm64` v2p 构建零错误
- 单测：1249/1249 PASS
- E2E linux-arm64 全量：175 用例 171 过 / 1 败（78 环境性 OOM）/ 3 跳——零回归
- v2 闭环 23 用例快测全绿（119/123/125/126/127/129/130/132/133/137/143/144/147/148/150/156/158/164/165/167/171/172/173）
- **x86_64 复验留深度机轮**（本机 arm64 不跑 x86_64）

## 六、踩坑入库（lessons 追加）

1. **脚本切块锚行号漂移**：用固定行号锚定切函数段，文件被前一步 Edit 改动后行号漂移→锚断言失败→脚本退出（文件未写入，无损但浪费时间）。**预防**：脚本切块前用 grep 重新定位行号，或用动态文本锚（`next(i for ... if 条件)`）而非固定索引。
2. **占位符笔误**：手写 Edit 时留 `池0占位()`/`新行占位 = 0;` 等占位符忘记替换 → 编译错误。**预防**：占位符用独特标记（如 `TODO_REPLACE`），提交前 grep 检查。
3. **环境识别**：换机先 `uname -m`——本机麒麟 ARM64 ≠ 深度机 x86_64，门禁目标平台必须对应。

## 七、文档同步状态

- ✅ HANDOFF.md（本文件，覆盖写）
- ✅ 更新日志.md（覆盖写本轮说明）
- ✅ lessons.md（追加三条踩坑）
- ⏳ plans/014 状态表（未更新——回家可补，或下轮补）
- ⏳ plans/018 第五节（未更新——R0-R4 方案落档，可补）

## 八、回家继续的下一步

1. 拉 `develop` 分支最新（本轮已推送）
2. 跑基线确认绿：`cmake --build target/build && target/cn_unit_tests && python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn`
3. R3 剩余 13 函数拆分（从 `生成语句` 708 行开始——最大件；手法见上"接口约定"）
4. R4 三后端 生成指令 拆编码函数组
5. 收尾：plans/014 + plans/018 文档同步 + 提交

**验证链**（每轮拆分后跑）：
```bash
target/cn build CN语言编译器v2/主.cn --target linux-arm64 --output target/audit2/v2p_linux  # v2p 构建
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --filter 127  # 快测容器用例
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn  # 全量门禁
```
