# HANDOFF 交接文档

**交接时间**: 2026-09-04 第十三轮（家机 win-x64）——**IR生成.cn 冒烟 96 错收口 + [ir2] 819→0 + IR 层错误即中止纪律**。上轮（单位机）HANDOFF 下一步①全链落地。

## 本轮已完成（全部有实证）

### 一、96 错根因三层定位（文件分摊+函数二分+探针矩阵 p1~p4）
- **单根因**：v2 语法层局部声明形态检测不识别「泛型类型 & 引用后缀」——`向量<IR指令>& 指令 = 函数信息.指令;`（IR生成.cn:1630/1985/2030 三处）泛型闭合后是 `&` 非标识符，`是泛型声明` 返回假 → 整条声明落表达式解析成垃圾；96 错（84 指令+9 向量未声明+3 左值）全为级联。
- **宿主 sanity 锚定**：同源探针 exit=42 全绿（宿主支持泛型引用局部）；裸类型引用（`项& q = p`）宿主亦不支持——双侧一致入档。

### 二、泛型引用局部三层实现（E2E 147 六步闭环）
- 语法层：`是泛型声明` 闭合后 `& 标识符` 同认；语义层：引用初始化纪律（必须初始化+左值白名单 标识符/成员/解引用——与 IR 生成左值地址能力对齐）；IR 层：引用局部分支（单槽=绑定对象左值地址，登记 引用参数槽们/类型们——读取分流/成员基址/赋值写回/再传引用实参数据流与引用参数**完全同构**复用）。
- **连带根治两枚**：①`预扫容器声明` 把引用局部误登 RAII 名单（绑定字段地址当 this 析构=堆损坏 0xC0000374，asm 审读定位）→ 附加==256 跳过；②`回扫实参取地址` 补指针加载形态（`IR_指针加载.操作数1` 即左值地址——引用局部读/容器字段取载两形态直接复用零新指令）。

### 三、[ir2] 819→0（语义 0 错后 IR 层缺口可见化，E2E 148 六步闭环）
- **用户函数返回结构体 retbuf ABI 全链**：函数IR.返回结构体ID 新字段 / 被调方槽 1=rcx retbuf 指针+返回语句 IR_结构体拷贝+rax / 调用点帧尾共享 retbuf+位 0 装载+实参起始位 1 / `解析调用返回结构体` 补普通函数分支（链式 `造点(1,2).x`）/ win+ARM64 双后端 prologue 对称（x0 同款）。
- **结构体初始化两形态**：调用返回（`IR指令 项 = 建IR指令(...)`）拷贝入基槽；标识符拷贝（`节点 参数类型节点 = 参数`——生成函数 参数循环自用形态）。
- **容器初始化两形态**：函数返回=接收转移（不 NewObject，所有权移交）；标识符拷贝=浅共享指针（对齐宿主「按值传 向量 共享数据指针」）。
- **回扫三误报根治**（实参数值本已正确）：调用返回 rax=retbuf 地址即按值实参地址（655 条）；`IR_取地址` &标识符引用位（26 条——`查方法签名(...,&签)`）；成员指针加载=字段地址。

### 四、IR 层错误即中止纪律（E2E 149 负路径闭环，139 同款）
- IR容器.cn 新增 `IR层诊断` 单点计数（13 处 [ir2] 诊断全部接入；**正则批量替换三连坑**：函数体自匹配递归+引号丢失+尾部 \n 双拼——修复后全点 grep 核对）；IR生成多文件 入口清空；主.cn IR 生成后 取IR层错误数()>0 → 中止诊断+返回 1 不产 asm。类型大小 未知分支的 运行时错误(6) 立即崩改诊断后继续收集（一次报全量对齐）。

### 五、冒烟与验证
- **IR生成.cn 冒烟全绿**：语义 0 错+IR 0 诊断+asm 6.35MB+ml64 汇编通过（1.37MB obj）——**v2 自举第 6 组件完整编译**（词法/语法/语义/IR签名/IR容器/IR生成 六组件全绿，仅剩 代码生成三件+主.cn）。
- 五组件冒烟维持 0 错；147/148/149 单跑绿；全量门禁见下。

## 下一步
1. **代码生成.cn/代码生成共用.cn/代码生成ARM64.cn/主.cn 冒烟推进**（v2 自举最后四个组件）：主.cn 入口=命令行参数，冒烟方式待定（以 代码生成.cn 为入口 v2p 编译——它导入 IR生成 全链）；「v2 编译 v2 自身」对拍（79 同款闭环）是终点站。
2. **簇⑥连带观察项呈报**（等用户裁决）：cn check 非入口文件名检查假绿（模块可见性过滤静默丢函数体）——方案呈报见下。
3. **linux-arm64 复验轮**（单位机）：147/148/149 三用例 linux 侧对齐+本轮变更全量 ARM64 复验。
4. **B1a 契约分叉**（宿主 stdlib 包装 结果<> vs v2 内置直调裸指针）——随 stdlib 并入专项收口（既有裁决不变）。

## 簇⑥观察项呈报（待裁，未实施）
**cn check 非入口文件名检查假绿**：`cn check 非主.cn 且含导入` 时私有函数体被模块可见性过滤静默丢弃=检查没发生但报通过（单位机簇⑥定位中 H1.cn「检查通过」实为没检查）。方案：
- **方案A（推荐）**：check 对单文件也全量检查（可见性过滤仅用于多文件合并的符号解析，不裁剪检查对象）——彻底但牵动模块可见性语义边界；
- 方案B：check 非入口文件时 stderr 提示「该文件按模块可见性过滤，建议以 主.cn 为入口」——最小改动，假绿可见化但检查仍不发生；
- 方案C：check 拒绝非主.cn 入口（硬错误）——改变 CLI 契约，v2 组件冒烟习惯（v2p 单文件入口）不受影响（v2p 不是 cn check）。
代价：A 约 1 轮（模块过滤逻辑+回归）；B/C 半轮。

## 灰色点披露（不变）
- 裸类型引用局部（`项& q = p`）宿主亦不支持——双侧一致；如需属宿主先行新特性另立案。
- 引用初始化下标形态待 IR 生成左值地址 支持下标时同步扩（语义白名单未含）。
- v2 整8/整16 强转白名单边界/正8 正16 显式拒绝——不变；78/79 v1 链内存失控——已知遗留勿翻案。

## 踩过的坑（本轮，全部入 lessons 详录）
- **「诊断与数据流不符」先实证再定性**：655 条「结构体实参须为标识符」实为误报（rax=retbuf 地址即按值实参地址——值已正确），按「缺口」实现会做无用功。
- **非拥有式绑定误入 RAII 名单**=双重释放家族（引用局部绑定字段被析构→堆损坏 0xC0000374）。
- **正则批量替换自伤三连**（同构函数体自匹配/引号丢失/换行双拼）——改后全点 grep 核对+assert 式替换。
- **heredoc 转义折损再发作**——结构修改一律临时 .py 文件（chr(92) 构造法实证可靠）。

## 门禁状态（第十三轮最终，家机 win-x64）
- 构建零警告（MSVC /W4 /WX）+ 单测 **1223/1223** + E2E 全量 **151 用例 149 过/2 败**（78/79 已知 OOM 家族与基线一致零回归：78=0xC0000005、79=内存超限 4124MB>4096MB——v1 旧架构字符串行 IR 遗留，随 v2 重建解决勿翻案）；新增 147/148/149 三用例随门禁全绿（含 v2p 尾部自检负测行）；五组件冒烟+IR生成.cn 冒烟全绿。
