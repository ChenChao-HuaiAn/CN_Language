# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-17——**265-a：D34 拆波·波A——语义层字面量类型+公共类型提升（178 移位面根治·零回归）**（基线 687926a）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **整字面量缺省类型=T_整32**（纯长度值域分流·≤10 位适配）。
2. **二元类型推导改公共类型提升**（替换 46-f「取左白名单」）：浮点优先分支（91-a
   单侧浮取该侧——**首轮遗漏致 250/251/253/303/08_opt 浮点回归·当轮定位修复**）；
   整型族=位宽大者胜+**无符号胜**（正64/正32 参与=T_正64/T_正32——178 形5 全一>>60
   的 shr 依赖·263-a 教训深化）+双整32=T_整32 唯一截断面。
3. **移位类型=左操作数类型覆盖**（C 语义·规范 01b§5.4 移位量取模按左类型位宽——
   不用公共提升）；**三后端移位段**取模（and ecx,31/63·ARM64 and #31/#63）+出口截断
   （sxtw/uxtw）——波A 范围修正：移位段属 178 面。
4. **178 双编译转正**（五形态全对：形3 5<<33=10/形5 全一>>60=15 无符号 shr）；
   191（264-a）维持+191_v2/380/194/204 哨兵全绿。

### 二、验证链（全绿）

全量 E2E **407=407/0/0**；单测 **1351/1351**；CLI 矩阵 **50/50**；面覆盖门禁 PASS
（53/60）；锚定链自洽（E2E 内嵌 78_v2/79_v2）；check_handoff+check_progress_sync 过。

### 三、下一步（新会话按序）

1. **波B 前置 🧪**：借用视图/下标指针面全形态盘点（261/130 耦合面清单化·免锁可先行）。
2. **波B 实施**（05 解引用+步进糖·依赖盘点结论）→**波C**（自增步进）。
3. T12/T13/T14~T16 根治（§3-G）；C20 波2（待 D28/D29）；github 镜像挂账已清。

### 四、诚实边界

- 05 面未根治（波B/C 排班）；~0 位非面类型码=整32 链路未登记（实测语义正确·保守
  不扩）；X64L/ARM64 运行级=跨机口径。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 验证轮四连巡（免锁不占轮次号·基线 ba2c53e）——行52 浮点（T14~T16）→行54 容器（T17/T18）→行55 泛型组合（T19/T20）→本轮**行59/60 词法边界双行合巡**（T21+T5 扩充）。**⬜ 巡视候选清零**。家机 266-a D34 拆波波B 持锁在飞。

### 一、本轮（行59/60）做了什么

1. **词法边界矩阵建成并跑通**：/tmp/cn_matrix/lexscan.py——33 形态（保留字声明×10/上下文词×5/超长名/中英混排/数字开头/下划线/扩展区汉字 𠀀/全角/中文标点 + 4 字节码点 𝄞/组合字符 e\u{0301}/emoji 串长度/\u{10FFFF} 上限/超限 110000/代理区 D800/空 \u{}/中字码点等式/中文串长度/BOM/坏字节注入）。
2. **正面确认**：保留字 8/8 拒绝✓；上下文词体系与规范 §5.9B 完全一致（遍历/常量/作为/模块/包 过检=正确——探针首版误判经规范核对归正）；数字开头拒绝✓；超长名/混排/下划线/扩展区汉字✓；Unicode 主体面全对（4 字节码点/组合字符字节口径/emoji 长度 3+4+3=10/\u{10FFFF}/中字==20013/中文串长度 6）。
3. **立案（plans/021 §三-G）**：
   - **T21（轻·词法字符集）**标识符字符集过宽：全角字母（Ａ）/中文标点（，）作变量名过检——01a EBNF「中文/字母/下划线」未严格限定。
   - **T5 扩充**（既有立案+两新形态）：\u{} 转义码点校验缺失族=超限 110000+**代理区 D800**（孤立代理=非法 UTF-8）+**空 \u{}** 三态均不拒绝。
   - 口径记录：BOM 开头容忍（合理）；孤立续字节注入 check 过（运行级未验）。
4. **台账与基线**：行 59/60 ⬜→🔶；面覆盖 **57/60（未测 3）**；覆盖基线 5→3；plans/021 戳刷新。**⬜ 巡视候选清零**（剩 16 溢出语义=C18 待实施/57 平台面=长期/58 daemon=256-b）。

### 二、验证链（本机复现口径）

- `python3 /tmp/cn_matrix/lexscan.py`（33 形态）→「14 对+4 立案素材+15 口径/单级」+ lx_results.json。
- 规范核对：01a §5 关键字体系+§5.9B 上下文词 18 项（「先查规范再动手」实证——遍历/常量 过检为正确行为）。
- `python3 scripts/check_matrix_coverage.py` PASS（基线 3）；check_handoff/check_progress_sync 显式退出码见收工提交。
- 源码零改动——E2E/单测不受影响（未重跑）。

### 三、下一轮任务（按序）

1. **S1 采样 daemon（验证轮·免锁·256-b 协议已批待实施）**——⬜ 清零后验证轮主方向；建成即 M3 计时启动。
2. **词法面根治（开发轮·待认领）**：T5 扩充（\u{} 三态校验）+T21（标识符字符集白名单）同轮——小面快根治。
3. 其余开发轮候选：T14+T15／T17+T18／T19+T20（T19 根治前须呈报单态化注册方案）。
4. 行 57 三后端平台面（长期格·随跨机复验收口）。

### 四、诚实边界

- 本轮无源码改动；T21 立案/T5 扩充非修复。
- 孤立续字节注入仅验 check 层（运行级字符串长度行为未验——轻记录）。
- 33 形态=行 59/60 首轮矩阵（保留字抽样 10/46+上下文词 5/18；原始/多行字符串字面量面未覆盖）——滚动扩展由台账门禁保证。
- 前三轮（行52/54/55）完整记录见 git d69d260/921930e/ba2c53e 提交信息。

## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
