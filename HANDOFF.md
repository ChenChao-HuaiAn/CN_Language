# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十一轮收官（深度系统 x86_64）——**plans/017 呈报①方案A 第三轮：容器所有权语义对齐根治六枚（第 8~13 枚）+ 观察项①②撤销 + v2self 推进至 IR 生成（第 7 层新缺陷呈报待续）**。

## 本轮已完成（全部有实证）

### 一、观察项①②撤销（家机验证）
第二十轮立案的两项「宿主缺陷」撤销——mapref 探针 `导入 容器;` 用映射是**源程序导入错误**（映射在映射集合模块）；宿主诊断正确，改导入后宿主全绿。

### 二、根治六枚（九探针全部「v2 编译→as→g++ 链接→运行」与宿主直编对拍一致）
1. **池 0 参数类型节点漏登记三处同族**（收集函数实参结构 288/权威表 2557/语义分析静态声明 155）：`>0`→`>=0`——mapref 灌 的 映射& 引用实参位漏登记→调用方不取址→被调双重解引用→映射$链查找 崩。
2. **调用返回接收却析构**：宿主元素(i)=内联元素地址视图（不析构）；v2 预扫排除「调用返回初值」入 RAII。
3. **标识符拷贝浅共享→深拷贝构造**：NewObject+拷贝构造（T& 传左值地址）；新增 IR_静态地址 指令（三后端）。
4. **返回容器仍被析构**：跳过名泛化（末节点=标识符即设）。
5. **结果<T=结构体> 大小/槽位截断**：结果大小/槽位数支持结构体载荷+逐槽分配+基槽修正。
6. **结果.值 结构体载荷被 LoadPtr**：压字段地址落地+回扫补 IR_字段地址 复用。

### 三、宿主容器值语义判明（四探针 asm 实证，=v2 对齐基线）
标识符拷贝=深拷贝／元素(i) 接收=视图（写共享、不析构）／追加=深拷贝／函数返回=转移不析构。

## 下一步
1. **呈报①方案A 续（第 7 层）**：v2self（v2 编译的 v2）在 **IR 生成阶段**崩——min1 四节点最简源（`函数 主() -> 整32 { 返回 0; }`）复现（sem5=0 后崩，stdout「运行时错误(错误码3)」）；gdb 显示 bb 块内 rep movsb rsi=小整数形态与「签名扫描打开失败」（v2 编译版限定内置 `文件::打开文件` 返回值坏嫌疑）。逐层修复不一次收口（lessons 规律）。
2. **呈报③方案A**（已裁未做）：IR 名带参数签名（重载消歧机制层）。
3. T3/T4（runner 平台参数化+E2E v2 版用例）随 v2self 收口后推进——**155 用例已为 v2 闭环预备**（四语义锚定）。
4. 跨机不变项：主.cn MASM 产物家机 ml64；E2E 153/154 win/arm64 复验；cqo 修复 win 运行级验证。

## 灰色点披露
- v2self 完整运行未达成（本轮从「映射自检崩」推进到「语义全绿+IR 生成中崩」）。
- 探针级九形态全对拍一致，属大规模 v2 编译版自身残留（第 7 层）。
- 宿主变量名解析边界观察项：`加载荷` 三字名声明后使用报「未声明的标识符」，改名即过（待最小复现立案）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径）。
- 探针/对照产物在 target/dbg/probe017/ 与 /tmp/v2dbg/（均不入库，/tmp 随系统清理）。

## 踩过的坑（本轮，已入 lessons 权重 10）
- **所有权以被调方 ABI 实证为准**（「接收=拥有」想当然在元素(i) 视图形态下=double free）。
- **大小-槽位-拷贝字节三量同源**（一处错=帧布局连环越界写穿相邻槽）。
- **对拍基线先验证探针源合法**（导入错误让 v2 假绿宿主真红）。
- **shell 管道吞退出码**（`cmd|tail` 后 `$?`=tail 的）——验证必须独立取 exit。
