# HANDOFF 交接文档

**交接时间**: 2026-09-04 第十轮（单位机 麒麟 ARM64）——**linux-arm64 复验轮**：
139/141 负路径闭环 linux 侧对齐（上轮诚实边界收口）+ 六轮家机变更全量 E2E 复验 +
缺陷 A/B ARM64 侧复验，全部完成。上轮「下一步②（ARM64 侧全量复验）」清零。

## 本轮已完成（全部有实证）

### 一、run_e2e.py linux 侧负路径闭环对齐（本轮唯一代码改动，src/ 零改动）
- `执行v2闭环Linux` 新增 `预期退出码 None` 分支（win64 run_e2e.py:986 同款三断言）：
  v2p 运行带 `linux-arm64` 第 2 参数（GAS 后端分派；win 默认后端无需参数）、
  退出码非 0、`target/v2asm.s` 不产出、中止诊断行（.expected 固化，平台适配
  replace `.asm`→`.s` 与正路径步骤 3 同款）。
- dispatch 删除「待 ARM64 复验轮对齐」占位拦截（原直接返回失败）。
- **E2E 139/141 单跑双绿**——linux 侧负路径闭环（v2p 语义错误即中止纪律）首次实证。

### 二、全量 E2E linux-arm64 复验（六轮家机变更首次 ARM64 复验）
- 范围：d6deb6f（缺陷②v2四件+缺陷A/B根治+裸节点双重处理）/e0857dc（限定名+前缀
  紧绑定）/be77434（`.` 统一+两静默常量0根治）/d117694（三元+静态自增）/c3d96a9
  （真/假+`*p op=`+整8/16 强转+v2p 中止）/ebd07cf（字符串比较拒绝）。
- 结果：**143 用例 139 过 / 1 败 / 3 跳，零回归**。
  - 跳过 3 = 62_ffi / 69_memory_management / 79_bootstrap（平台限制结构性跳过，
    与历史基线一致）。
  - 唯一败 78_chain_build：运行期 SIGSEGV(-11)。**单跑复证同形态**；src/ 本轮零改动、
    上轮单位机全量基线同为 1 败(78)——v1 链内存失控的 ARM64 表现（win 侧同族
    0xC0000005 / 4101MB OOM 变体，plans/014 三-表已知遗留），随 v2 重建自然解决。

### 三、缺陷 A/B ARM64 侧复验（上轮单位机发现、家机第四轮 IR 层根治）
- 缺陷 A（`p.y--` 成员后缀自减存储无效）：134 用例退出码断言（q.y-- 后须 ==19、
  q.x+=5 写回须 ==36）全绿 + /tmp 最小探针输出 **2** 双证——ARM64 写回生效。
- 缺陷 B（局部数组无初始化 + `数组[2]+=数组[1]` 垃圾值）：既有用例无此形态覆盖
  （grep 复核：数组声明均带初始化器），/tmp 探针补验输出 **5**（补零确定性）——
  ARM64 侧根治生效。探针验后即删（AGENTS.md 中间产物纪律）。

### 四、门禁（单位机侧全量）
- 清场重建（gcc ARM64 Debug，双机时区差防陈旧产物）零错误；
  单测 **1216/1216**（与家机 ci.ps1 基线一致）；E2E 见上。

## 下一步（接手会话按序执行，均已在两侧门禁绿的基础上）
1. **消化语法分析.cn 冒烟 54 错（三簇根因，逐簇立案）**（家机第六波启动、两轮未动，
   现两侧门禁全绿正是推进时机）：
   ① 跨模块顶层常量可见性（~25 错，T_*/IR_* 顶层常量跨模块引用）；
   ② 文件 API 标识符（9 错，打开/打开文件/关闭文件）；
   ③ 可写左值白名单（9 错+散件：名ID/当前函数变量类型）。
   注意：v2p 语义错误即中止已生效（139 锚定）——54 错清零前看不到 IR 层，天然分段；
   v2 侧字符串比较拒绝已生效——组件源码再引入 `==`/`!=` 字符串形态 v2p 会先拒。
2. v2 语义类型推导完善轮（成员字段/拼接产物推导收口）时：连带收口灰色点②的
   v2 检出面诚实边界（成员字段字符串比较检不出；宿主已全形态）。
3. 家机侧下一轮：语法分析.cn 冒烟推进的家机对拍（若本轮单位机动 v2 源码，
   家机 ci.ps1 全量门禁复验）。

## 灰色点披露（待用户裁，未立案）
- v2 整8/整16 强转白名单已补（第八轮），**正8/正16 暂不补**（类型码层同码无符号
  语义缺位，补名=静默错误代码，维持显式拒绝待类型码体系支持）——上轮边界，不变。
- 78/79 v1 链内存失控（ARM64 表现 SIGSEGV / win 表现 OOM 4.1GB）——已知遗留，
  随 v2 重建自然解决，不在旧组件治标（plans/014 三-表定案，勿翻案）。

## 踩过的坑（本轮）
- 无新坑（复验轮，一次通过）。78 的 SIGSEGV(-11) 与 win 侧 0xC0000005 同族
  （内存失控崩溃形态），判定依据=单跑复证+src/ 零改动+历史基线对照三证。

## 门禁状态（本轮最终，单位机 linux-arm64）
- 构建（gcc ARM64）零错误 + 单测 **1216/1216** + 全量 E2E **143 用例 139 过/
  1 败(78 已知遗留)/3 跳(平台限制)** 零回归；139/141 linux 负路径闭环单跑双绿。
- 家机侧本轮改动仅 tests/e2e/run_e2e.py（测试编排，非编译器源码）——家机下轮
  ci.ps1 全量时会自然复验（win 侧 139/141 已绿，改动仅动 linux 分支不影响 win 路径）。
