# HANDOFF 交接文档

**交接时间**: 2026-09-04 第九轮（家机 win-x64）——**灰色点②方案A落地：字符串比较运算符
显式拒绝**（用户裁决方案A，承接第八轮灰色点①③④⑤根治）。至此上轮 HANDOFF「灰色点披露」
六点全部收口：①③④⑤已根治（E2E 138/139）、②本轮方案A落地（E2E 140/141）、
⑥确认为未实现特性非缺陷（随 v2 错误处理特性推进对齐）。

## 本轮已完成（全部有实证）

### 一、灰色点②方案A：字符串比较运算符显式拒绝（双侧）
- **规范依据**（零规范变更）：plans/001 Task 2.8 已明文「比较运算符仅定义整型与浮点变体，
  字符串不参与运算符比较；等价能力由 字符串比较（相等）与 字符串字典序（全序）提供」
  ——本实施=让两侧实现对齐既有规范。
- **宿主**（semantic_expr.cpp 比较分支）：`字符串/字符*` 间 `==/!=/</>/>=/<=` 硬错误
  「字符串不支持 ==/!=/</> 等比较运算符（按地址比较而非内容）——相等用 字符串比较(a,b)，
  全序用 字符串字典序(a,b)」；**字符串/字符* 与 无（空类型*）判空保留**（133 EOF 载体）；
  真指针类型间按地址比较保留（判空/同址判定）。
- **v2**（语义分析.cn 检查表达式 新增 节点_二元运算 分支）：比较 op 且两操作数区间末端
  递归推导均为字符串 → 同款硬错误；返回 -1（下游推导无回归——三元末端二元原亦 -1 跳过）。
  **诚实边界**：拼接产物/成员字段末端推导 -1/整64 兜底=检不出（字面量/变量形态覆盖
  主踩坑面；随 v2 语义类型推导完善收口，宿主侧已全形态覆盖）。
- **v2 组件源码连带修正 1 处**（语法分析.cn:881）：`当前文本(记号, 位置) != "}"` 原按
  驻留地址比较（依赖驻留恒等的实现细节——恰为根治目标的陷阱用法，宿主拒绝后 v2p
  构建即打挂=连带揪出）——改 `!字符串比较(...)`。**全量复扫**：v2 组件/stdlib/既有
  E2E 用例源码无其他字符串比较运算符形态（预检+全量门禁双证）。
- **既有用例迁移 1 处（用户批准 2026-09-04）**：24_error_handling:31
  `如果 路径 == ""` → `如果 字符串比较(路径, "")`（参数 vs 空串形态——首版 `!字符串比较`
  取反写反致两分支对调，E2E 逐行比对当场抓住后修正；迁移后 expected 零变化复绿）。
  **教训入 lessons**（2026-09-04 02:30）：「零影响」结论必须全量门禁实测背书，抽样
  grep 模式缩窄=结构性漏检；布尔内置迁移须逐分支推演真值表（== 同向 / != 加非）。

### 二、测试
- 宿主单测 **test_string_compare_check.cpp**（新文件，CMake 已注册）**9 项全绿**：
  5 负形态精确消息（变量==/!=、字面量直比、顺序比较<、字符* 交叉）+ 4 合法零误伤
  （字符串比较/字典序 内置、`行 != 无` 判空、真指针判空、拼接语境）。
- E2E **140_字符串比较运算符拒绝**（宿主负测：期望编译失败.txt 固化诊断子串）+
  **141_v2_字符串比较拒绝**（v2 负路径闭环：139 同款 runner 预期退出码 None 形态——
  v2p 退出码 1+无 asm+诊断行 expected 固化）——双绿。
- 既有用例零误伤：133（`行.值 != 无` 判空）/34_file（`字符串 != 无`）等判空载体
  由全量 E2E 回归验证。

## 下一步（接手会话按序执行）
1. **消化语法分析.cn 冒烟 54 错（三簇根因，逐簇立案）**（两轮前遗留，未动）：
   ① 跨模块顶层常量可见性（~25 错）；② 文件 API 标识符（9 错）；③ 可写左值白名单
   （9 错+散件）。⑤门禁生效后：语义错误源码不再产 IR/asm，冒烟语义消化与 IR 观察
   天然分段（54 错清零前看不到 IR 层，符合预期）。**注意②的 v2 语义拒绝已生效**——
   组件源码若再引入字符串比较运算符形态，v2p 自举对拍会先拒（宿主同拒）。
2. **ARM64 侧全量复验（单位机）**：第八轮（①③④⑤）+本轮（②方案A）变更须
   linux-arm64 全量 E2E 复验；**139/141 负路径 linux 侧对齐**（执行v2闭环Linux
   加同款预期退出码 None 分支——本轮仅 win 实证）。
3. v2 语义类型推导完善轮（成员字段/拼接产物推导收口）时：连带收口②的 v2 检出面
   诚实边界（成员字段字符串比较检不出）。

## 踩过的坑（本轮）
- **方案A实施即自伤实证**：宿主拒绝上线 → v2p 构建立刻打挂（语法分析.cn:881 驻留地址
  比较惯用法）——「新语义检查落地时，先跑受影响代码库全量编译」是最快的自测；
  驻留地址恒等是实现细节不是语言语义，组件源码依赖它=潜伏陷阱（本轮被检查器揪出=好事）。
- **「现有源码零影响」评估被全量门禁打脸**（24_error_handling:31 普通变量 `路径 == ""`
  形态漏检）：抽样 grep 模式按已知样例缩窄=结构性漏检——已入 lessons（2026-09-04 02:30）。
- **布尔内置迁移真值表**：`==`→`字符串比较` 同向、`!=`→`!字符串比较` 反向——24 首版
  按惯性写反，E2E 逐行比对当场抓住（迁移后必须跑原用例 expected 复证，不能只过编译）。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（ci.ps1）零警告零错误；单测 **1216/1216**（新增 9）；全量 E2E：**143 用例 141 过**
  ——新增 140/141 双绿 + 24 迁移后复绿 + 既有零回归（78/79 已知遗留内存超限：
  79=4123MB 形态一致；78 全量跑中偶发 0xC0000005 为 OOM 变体，**单跑复证 4101MB
  内存超限=已知形态**——旧架构固有，随 v2 重建完成自然解决）。
  （若后台门禁结果与此不符，以上一轮门禁结论为基线排查本轮改动。）
