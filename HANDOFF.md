# HANDOFF 交接文档

**交接时间**: 2026-09-03 第五轮（家机 win-x64）——③ 第五波：**限定名内置
IO::/文件:: 全链 + 真/假/无 字面量 + 二元链操作数位一元前缀**（E2E 133 全链绿，
plans/014 序列下一缺口收口；验证期连带揪出二元链前缀缺失并根治——首版修复踩
二次坑致 123/129/130 回归，当场修正，最终全量 135 用例 133 过零回归）。

## 本轮裁决依据（用户令）

- 「继续后续任务」+ 重贴开发纪律原文（最高优先级：尽善尽美禁止妥协/缺陷零容忍
  强制汇报/一劳永逸）——按 HANDOFF「下一步②」plans/014 序列推进 IO::/文件::
  限定名内置；验证期缺陷当场根治，无搁置。

## 本轮已完成（全部有实证）

### 一、限定名内置 IO::/文件:: 四层（E2E 133 载体）
1. **语法层 `::` 独立分支**（原与 ./-> 同走成员分支：IO 崩「未声明标识符: IO」
   + 方法标记误路由「容器方法未收录」错误 6）：仅支持「标识符::标识符(」调用
   形态——池尾对象标识符原地改写为合成限定名（局部副本+池.设置 回写——
   元素(i).字段=x 写非宿主支持形态，124/126 实证读=地址视图/写=设置），随后
   `(` 自然形成 调用（被调=限定标识符，与容器构造合成名同构，零新节点类型）；
   非调用 `::` 中断后缀链（错误路径语义诊断可见化）。
2. **真/假/无 字面量**（词法关键字表已有、解析层零分支——谓词含 真/假 的源码
   必截断）：复用 整数字面量（真=1/假=0/无=0），语义/IR 零改动；无 判空走
   指针语义（宿主「指针只能与指针或空指针比较」实测锚定）。
3. **IR容器 双表扩 11 限定名**（宿主 ir_call.cpp:217~235 + semantic.cpp:
   1226~1257 对齐；空类型* 记 整64 指针单槽）；IR生成 内置直调统一走 打印名
   查表 + T_空类型 跳过登记；语义层经内置返回表自动放行（零改动）。
4. **v2 主.cn 源码8 限定名负测自检**（`不存在::打印(1)` 须拒——白名单命中制
   防 :: 合成静默放行）+ IO 正向锚点两行。

### 二、【连带揪出·当场根治】二元链操作数位一元前缀缺失（含二次坑修正）
- **现象**：133 首跑 `[parse]` 截断于 `追加行 == 无 || !字符串比较(...)`——
  探针矩阵 p1~p4 交叉二分：比较||比较过 / !调用单独过 / **组合必截断**；
  词法分析.cn 冒烟 3 处截断同族。
- **根因**：一元前缀（!/-/&/*）仅在 解析表达式 入口解析，二元链右操作数位
  落空（位置完整性缺口）。
- **首版修复踩二次坑**：前缀分支放 子(5) 入口无条件判定——而 v2 二元链是
  「**入口在运算符上、左操作数已由 解析表达式 前段解析**」的空左操作数设计，
  链入口的二元 `-`/`*` 被误当一元前缀——**减法/乘法全灭**（`0 - 28` 变
  [0,负号(28)]、`f() * 10000` 变 解引用(10000)；全量门禁 123/129/130 回归
  实证；133 侥幸绿——其算术全是 `+`）。
- **修正**：解析层_子 操作数位参数化（旗标经 子→二元层→子 下降链贯通）——
  二元循环右操作数位传 真（运算符已消费=操作数起点，判定无歧义）、链入口
  传 假。消融实验（禁用分支重建对照）+节点数同值（20==20）+探针矩阵三步锁凶。
  **lessons 10.5 权重成文**（含「改解析器必须跑含减法/乘法的既有用例——
  `+` 独大的新用例会漏检」教训）。

### 三、验证（最终态）
- 宿主 sanity：133 源 cn build+运行 退出码 0（17 字节写入/逐行内容比较/
  EOF 判空/文件大小/存在性负路径/追加模式全对）。
- **E2E 133 六步闭环 PASS**（expected 四行全命中）；v2 产物人眼审计三锚点齐全。
- **词法分析.cn 冒烟大幅推进**：3 处 [parse] 截断→**1 处**（=三元运算符
  `? :`，行 236，规范 01b 有、v2 未实现——**下一缺口**），语义 0 错误，
  **asm 7357 行完整产出且 ml64 汇编通过**（30KB obj——v2p 完整编译自家词法器）。
- **全量门禁**：构建零警告 + 单测 **1207/1207** + E2E **135 用例 133 过 /
  2 败（78/79 已知遗留内存超限，与上轮完全一致）零回归**。

## 下一步（接手会话按序执行）
1. **三元运算符 `? :`**（词法分析.cn 冒烟最后截断点，规范 01b 有/v2 未实现；
   建议复用 125 短路的「延迟求值+临时槽汇合」机制；E2E 用例 134 先行）。
   三元落地后复验：词法分析.cn 冒烟应 [parse] 归零 + 语义 0 错 + asm 全量。
2. 语法分析.cn 冒烟（130 动因已消，三元后启动）——预计踩出下一批缺口
   （函数指针调用/复杂成员链等）。
3. ARM64 侧全量复验（单位机）：本轮 v2 改动（::/真假无/操作数位前缀）+
   上轮缺陷 A/B 修复均须 linux-arm64 全量 E2E 复验（单侧绿≠双侧对称）。

## 灰色点披露（待用户裁，未立案）
- v2 `打印(真)` 输出 `1`（宿主输出 `真`）——真假复用整数字面量的已知近似，
  打印分派按 T_布尔 输出 真/假 需登记路径，本轮无载体。
- v2 二元 `==/!=` 对字符串是指针比较（内容比较须 字符串比较 内置）——既有
  行为，与宿主语义差异（宿主 == 对字符串亦指针比较，stdlib 全用 字符串比较，
  一致）；133 用例已按 字符串比较 编写。
- 沿用：`*p += 1` 解引用复合赋值不支持（stderr 可见截断）；v2 整8/整16 强转
  白名单未覆盖；v2p 语义错误后仍继续 IR 生成。

## 踩过的坑（本轮新增，已入 lessons 高权重）
- **空左操作数二元链**：v2 二元链入口位置=运算符（左操作数已解析）——与教
  科书「入口在左操作数」相反！在链入口位做前缀判定=减法/乘法全灭。**改解析器
  后必须跑含减法/乘法/复合比较的既有用例**（123/129/130 守门员）。
- **消融实验**（禁用新分支重建对照）是「无新形态却回归」类缺陷的定位利器。
- CN 语言边界三连：布尔字面量须 真/假（禁 0/1）；容器元素字段写=副本+设置
  两步；字符串内容比较=字符串比较 内置。lessons 5.0 权重成文。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（build.ps1 Debug）零警告零错误；单测 **1207/1207**；
- 全量 E2E：**135 用例 133 过 / 2 败（78/79 已知遗留内存超限，与上轮一致）
  零回归**——133 六步闭环 PASS + 123/129/130 回归修复实证；
- 词法分析.cn 冒烟：截断 3→1（余三元）、语义 0 错、asm 7357 行 ml64 汇编通过。
