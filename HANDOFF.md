# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（296-a 开发轮 + 🧪 M3 采样轮 298-a~301-a + 302-a T39 根治·2026-09-17）

**302-a（开发轮·用户授权接管·最新）**：**T39 根治销项——三后端 i128/u128 位运算/移位/Cast 通道补全**。根因=三后端 dispatch 对 Shl/Shr/BitAnd/BitOr/BitXor 缺 i128 分派（Add/Sub/Mul/Div/Mod/比较均有）→落 64 位通用路径=低 64 位槽从未被写（asm 铁证·三跑三值非确定）。实施：win `splitI128` 提取+`emitInt128Bitwise`（双半独立）+`emitInt128Shift`（完整 128 位·shld/shrd·移位量 mod 128〔plans/001:289〕·变量 cmp/jae 二分路·有/无符号 sar/shr）+dispatch 五类；x64l/arm64 同构（`Lsh128_b/e` 标签/lsl-lsr-asr）。修复中自查三处（移位量双槽读高半/窄 Cast 分支顺序/`widthFor` 兜底空写）+128→窄 Cast 扩展（`整16(整128(100))`=0→100）。**验证**：win 探针 8/8 全绿确定+新 E2E **409_整128位运算移位**（27 断言·O0=O3 逐字节）+全量 **424=424/0/0**+单测 1353+CLI 50/50+spec --strict 100%+面覆盖 68/69+双文档门禁。**v2 侧边界**：v2 不支持 `整128()` 转换（对齐欠账·409 宿主单侧）。**跨机复验请求**：x64l/arm64 运行级（409 真跑）归深度机/单位机。

**296-a（开发轮）**：D35 波B 销项——v2 错误传播运算符 `?` 全链（364/344/60 三实弹双编译转正+T32 闭环）。明细=git。

**🧪 M3 采样轮 298-a~301-a（验证轮·六轨·他机在飞期间）**：差分四轨 **2500/2500 全一致**+负向轨 400/400 全拒绝+**T25 win 根因链闭环**（「arm64 单侧」纠正=win+arm64 双挂·IR+asm 双链）+**T39 立案**（03-a 采样独家捕获·后经 302-a 根治）+win 列七立案复验（T24/T26/T27/T36/T37/T40/T25）+面联想矩阵。广播七轮推送。**探针设施**：target/p298a~p302a/（不入库·修复轮回归复核用）。

### 二、下一轮（按序）

1. **T39 跨机复验**（409 在 x64l/arm64 真跑·归深度机/单位机）+T39 修复后采样回归轨（深度机 daemon）。
2. **开发锁空闲时候选**：T33 读侧窄宽三后端设施（385/386）/T35 专项/v2 侧 i128 支持面（409 双编译对齐）。
3. **T24/T25/T36~T42 裁决后根治**（单位机 297-a 在飞·win 证据链全备）。

### 三、诚实边界与坑（本轮自省）

- **修复中三处自查**（教训沉淀）：①i128 移位量按双槽物化时**读高半=恒 0**（operandText 给 %vN 高半）→按操作数自身类型分流；②窄 Cast 分支**顺序**（i8/i16 通用截断分支在前会截胡 i128 源）→i128 分支前移；③`widthFor("i16", "[槽]")` **兜底返回 "eax"**（非寄存器名一律回退）→槽文本勿经 widthFor。
- **v2 侧 i128 支持面缺失**（`整128()` 转换未声明）=对齐欠账另登记（409 撤双编译对照=诚实边界）。
- **操作纪律再犯两次**（T38→T39 撞车让位·立案前应 fetch 查重；check_handoff 管道洗退出码）——均已当场修正。
- 全量 E2E 首轮 424=424/0/0 一次通过（无回归）；x64l/arm64 无法本机运行（无工具链/汇编器）——静态形态核验（shld/shrd=19·lsl/lsr/asr=104+strh）。



## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 M3 采样轮·增量5+B9 扩散轮（验证轮·不占开发锁·源码零改动）。接手前先 `git fetch`。当前 HEAD 478d804+收工提交；家机 🏃 296-a（D35 波B·v2 树）在飞中——本机继续 daemon 采样不停。

### 一、本轮做了什么（🧪 基线 1903a2f→478d804·家机 296-a 开发期间持续采集）

1. **daemon 在飞 67 轮对账**：命中集合不变 5 项=T26 布尔短路值位×3（s2609179001~003）+T27 除法溢出双位宽×2（s2609179004/9005·**9005 整64=族内新实弹**）——均为已立案缺陷的持续暴露（回归轨正常行为·待根治）。月日志曾膨胀至 6212 行（触发 5000 门禁）→ 按 S1b 记法本意手工压缩为汇总段（3939 行）。
2. **T28 族双新实例立案（plans/021 §三-G·设施判据与语义脱节族）**：
   - ①**daemon 归类判据文本反推断裂**：`cnsmith_diff.py:60` 清单行只写 `rc=-8`（run_err 标签不落清单文本）·`cnsmith_daemon.py:134` 靠含「运行失败」字样反推类别→落空→rc=-8 命中误归 diff/（应 run_err/）+已归档样本每轮重建 diff/ 重复副本（实测两次复现）。
   - ②**S1b 汇总记法结构性失效**：`记轮()` 无变化判据含 `not 消失回归`，而 93 个已修复回归样本每轮均不再命中→消失回归集合结构性非空→无变化永不成立→每轮退化全量记录（日志膨胀根因）。
   - 方案甲/乙/丙已列（甲=清单行带「运行失败:」前缀+消失回归改增量语义）——**待测试设施轮根治**（验证轮不改源码）；根治验收=rc=-8 样本落 run_err/+连跑 3 轮仅汇总。
3. **T27 B9 补轴扩散（面测试网·点→线→面实践）**：72 形态三方对拍（双位宽×÷/％×8 求值位置×变量/字面操作数形态×O0/O3·python 硬件语义 oracle·设施 /tmp/cn_t27 不入库）→ **26 形态分歧·两大新形态**：
   - **新形态①（级别间分叉）**：整32 变量形 `INT_MIN/-1` ——**O0 真 idiv SIGFPE 崩 vs O3 优化回绕不崩（-2147483648）**——同源程序双级别一崩一过（7 求值位置×÷/％=14 形态全部复现）。
   - **新形态②（字面静默错值·比崩溃更隐蔽）**：整64 字面形 `(0-9223372036854775808) ÷/% (0-1)` ——**O0=-2147483648（INT64_MIN 低 32 位符号扩展的精确位样）/O3=9223372036854775808（2^63 位样）或 0——双级别互斥且均非一致 64 位语义**（12 形态）。
   - **甄别链**：纯整64 字面打印/整64 变量打印 O0 全对→截断仅挂「字面常量直连÷/％」通道；`emitDivMod` 两后端（linux_x64/x64）均真 idiv（含除零检查）已排除→挂点收敛 **IR 构造/常量装载层**（精确行号留根治轮·遵守「立案描述≠根因」）。
   - **正面结论**：短路右支/真或右支惰性正确豁免溢出（4 形态）·整64 变量双级别崩一致·整32 字面双级别一致（字面升 64 位域=C 同款·语义注记非缺陷）·除零防护✓。
   - 甄别中剔除探针自身问题：4 形态字面复合赋值=生成器漏声明（build_err 双级别对称·非编译器缺陷）；oracle 未建模「字面升 64 位」曾产生假阳性组（已甄别排除）。
4. **立案落盘**：面台账行 62 扩注（扩散轴+两新形态+正面结论）；plans/021 T27 立案行扩注+**B12 呈报扩注**（扩散实证：O0 陷阱/O3 回绕/字面截断/位样直通四路行为不一致——乙/丙无法消除级别分叉与字面错值，**唯甲统一回绕同时消崩溃+分叉+错值**）；月日志新增「扩散轮」段完整记录。

### 二、验证链（本机复现口径·当轮实测）

```
python3 scripts/cnsmith_daemon.py --rounds 0 --samples 250 --sleep 90   # daemon 常态采样（重启后 PID 1303930 在跑）
python3 /tmp/cn_t27/probe.py                                            # B9 补轴 72 形态三方对拍（临时设施·不入库）
./target/cn build <f>.cn --target linux-x86_64 -O0/-O3 --output <exe>   # 双级别复现
python3 scripts/check_handoff.py / check_progress_sync.py / check_matrix_coverage.py  # 三门禁独立执行退出码=0
```

### 三、下一轮任务（按序）

1. **daemon 续跑+月日志压缩监控**（记法根治前每 ~45 分钟窗口压缩·红线 5000 行）——采样不停。
2. **T27 补轴实弹**：26 分歧形态中代表性样本待 B12 裁决后随 C18 实施转正 E2E。
3. **T28 族三实例根治轮**（daemon 归类判据+汇总记法+T28 注入器——测试设施轮·待用户排班或开发空窗认领；需开发锁）。
4. 用户裁决 B11/B12 后：T26（与单位机 T25 合并·三后端同查）+T27（并入 C18 口径）根治轮。
5. B8 面台账已清零（唯一 ⬜ 行 16=C18 待实施格）——巡视转已立案行复验/行 57 平台面余列。

### 四、诚实边界

- 本轮**编译器源码/测试用例/门禁脚本零改动**（验证轮纪律）；T28 族两实例只立案不修码（登记 §三-G 待设施轮）。
- 整64 字面 O3 侧 2^63 位样与 O0 侧截断的**确切生成层未读到行**（折叠通道与打印通道两假设并立·已注记为挂点假设非定论）；O0 字面组不崩的预处理层未定位（emitDivMod 已排除）。
- oracle 为自建硬件语义模型非 gcc 对拍——根治轮须双 oracle 复验；T27 arm64 侧仍未测（归单位机跨机项）。
- 汇总段注记曾误写「原始 67 轮见 git 历史」——实际 67 轮未入库（压缩发生在提交前）·已在月日志当轮修正。

## 单位机 ARM64 节
## 单位机 ARM64 节

**交接时间**：2026-09-17 🏃 297-a 全修轮收工（用户裁「按照你的方式，全部修复」）——**T25/T24/B10 三案当日根治**+406/407/408 转正+全量门禁绿。接手前先 `git fetch`。

### 一、本轮做了什么

1. **T25 根治（浮64 汇合赋值 -O3 归零）**：病灶=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支传 w9 → `str w9` 32 位截断写（高 32 位丢）。修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0）；asm 实证 `str d0` 64 位完整写；27 形态回归矩阵全绿（三元 9+汇合 9+最小 2+浮32 3+数组 2）。
2. **T24 根治（后缀自增值语义）**：`visitUnaryExpr` Inc/Dec 三路径+`handleClassFieldIncDec(+postfix)` 按后缀返回**旧值**（原忽略 postfix 一律新值）；16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照）。
3. **B10 根治（方案甲·同层 `:` 前瞻消歧）**：parser `ternaryColonAhead()` 替换 C-1 白名单判据（括号/嵌套 ? 深度感知；新判据⊇旧判据=存量成功程序零语义变化）——`a ? -b : c` 不再要求括号；后缀传播 `r?+1`=42 不回归。
4. **T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无同型截断）静态注记。
5. **规范条文**：plans/001 §4.5 优先级表（`++`/`--` 后缀 13/前缀 12 级+值语义）+三元消歧+错误传播节；spec 01b 五处同步。
6. **新 E2E 三件**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记。
7. **看板通告段广播**：v2 消歧同口径（家机 296-a v2 ? 链对齐）+T24 v2 对齐登记。

### 二、验证链（全绿）

```
cmake --build target/build -j 8          # 零警告（grep error|warning=0）
./target/cn_unit_tests                    # 1353/1353
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --jobs 8   # 426=424/0/2（跳=62_ffi/69 平台败面）
python3 tests/e2e/run_e2e.py --filter 60_error ...  # 传播回归 PASS
> 78_v2_自举链构建 PASS ＋ 79_v2_自举闭环 PASS（锚定链承载）
> 406/407/408 单跑 PASS；双门禁独立验退出码=0
```

### 三、发现的问题

- T26=x86_64 单侧（arm64 免疫实证）——精确形态归深度机 B11。
- T24/T25 的 **v2 树同型对齐**待家机 296-a 收工后独立轮（自举编译器也有这两缺陷·通告段已广播）。
- i1 Copy 走整数路径 32 位写=安全（布尔矩阵全绿实证·注记）。

### 四、下一轮候选

1. **M3 采样轮⑥**（用户令不停）：行 8/10/11 算术位运算比较面。
2. **T24/T25 v2 对齐轮**（家机 296-a 收工后）。
3. A12 标签锚点搭车。

### 五、诚实边界与坑

- win/x64l Copy 浮点路径运行级复验归各机（静态审查无同型截断·运行级未验）。
- 406~408 未配单测（E2E 为强门禁·单测面留后续）。
- 修复期间 E2E 全量 426=424/0/2 的 2 跳=62_ffi/69（既有平台败面·非本轮引入）。