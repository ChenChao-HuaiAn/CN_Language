# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十四轮（**深度机 linux-x86_64**）——**84-a v2 结构体
字面量作实参** ＋ **84-b 字段校验** ＋ **84-c 嵌套字面量** ＋ **84-d 成员链嵌套深度
（宿主数据正确性级缺陷修复）**。本轮序列：82-a v2 帧尾共享 retbuf 根治（数据损坏级）→
82-b 赋值位接收 → 83-a 链表/队列空闲链 → 84-a 字面量作实参 → 84-b 字段校验 → 84-c
嵌套字面量 → **84-d 成员链嵌套深度**。验证链全绿，固定点 **325982**（84-d 纯宿主改动=不变）。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：81-a 登记的「v2 帧尾共享 retbuf × 嵌套结构体返回实参」——单实参=句柄泄漏、
   双实参=**数据损坏**（`合并(造盒(1),造盒(2))` 宿主 1 / v2 0）。用户令：遵守 AGENTS.md
   第3项纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **根因三步收敛**（探针 P15/P17/P19/P20 + E2E 172 + gdb 内存 dump）：
   - **Ⅰ 共享单缓冲**：v2 全部结构体/结果返回共用帧尾 retbuf（`retbuf序号` +
     `累计retbuf字节数`）；宿主相反——每次调用独立 `__retbufN` Alloca 槽。
     触发条件收窄：**外层调用返回值也用 retbuf**（结构体/结果返回）时才覆盖——
     `取大(a, 造点(1,2))`（外层返回标量）修复前即正确。
   - **Ⅱ 槽基址约定**（首版修复失败点）：v2 结构体**基址槽 = `槽起 + 槽数 - 1`**
     （最大槽号=最低地址=结构体基址，②b B1 约定）——误用 `槽起` 当基址 → 写穿相邻
     变量槽（P20 实测物化槽 -704 与 t 槽 -696 重叠 8 字节）；且只发一条 `IR_分配` →
     `扫描函数` 漏统计（帧尺寸按 `附加` 最大槽）=帧尺寸不足。
   - **Ⅲ 跨模块返回类型**：函数键格式 `模块$名#参数串`，返回类型表只存**纯名**「记录」→
     调用方模块上下文解析到**同名异布局**类型（主模块 4B vs 甲模块 16B）→ retbuf 尺寸/
     物化长度错（E2E 172 实测 84 应 141）。

3. **方案裁决（三案，甲实施）**：甲＝**返回物化到独立槽**（结构体返回调用发射后立即
   memcpy 到新分配的生成期临时槽 + 压栈值换槽地址 = **LLVM sret 各自分配语义**）+
   实参位置调用后释放串字段 + 跨模块返回类型按定义模块限定；乙（retbuf 全量独立）因
   帧尺寸=Σ返回尺寸落选；丙（所有返回后物化）与甲实质等价（甲即全量物化，非实参位置
   不释放=句柄归接收方，零泄漏）。

4. **槽分配纪律四条（本轮订立）**：
   ① 预扫分配参数/变量槽（槽 1=retbuf 指针保留）；
   ② 生成期临时槽（物化槽）只用同一 `槽计数` 递增——不回溯、不复用、不交错；
   ③ retbuf 区由代码生成动态计算（`扫描函数` 的 `槽基数 = 最大槽`），随之上移 →
      **两者不可能重叠**；
   ④ 结构体**基址槽 = 槽起 + 槽数 - 1** 且**逐槽** `IR_分配`（扫描函数 按附加统计最大槽）。

5. **实施（v2 树 3 文件）**：
   - `IR/IR容器.cn`：`物化返回值到独立槽` / `清理调用实参临时` / `发射串字段释放顺序` /
     `是纯串字段结构体` / `限定返回类型ID`（键首 `$` 前=模块前缀 → 重查结构体大小命中才
     用限定名；**不改返回类型表**=零消费点影响）/ `子串位置` + 静态 `结构体返回临时们`。
   - `IR/生成表达式求值.cn`：`发射普通函数调用`/`生成方法调用`（签名加 `整64* 槽计数`）
     在 `IR_调用参数化` 后物化真结构体返回、以**槽地址**压栈；`生成调用` 两处调用点在外层
     调用发射后 `清理调用实参临时`。
   - `IR/IR生成.cn`：`清空函数级槽表` 加 `结构体返回临时们.清空()`。

6. **82-b（同轮后续）**：v2 **赋值位接收结构体返回**（`盒子 甲; 甲 = 造盒(9);`）原诊断
   中止（宿主支持）→ `生成结构体整体赋值` 新增「右值=节点_调用」分支：求值右值表达式序列
   （→ 82-a 物化 → 槽地址）→ 目标=左槽地址 → **浅拷接管**（零拷贝，物化槽为一次性返回值）
   + 目标拥有槽 preFree + 同型校验（物化登记表）；E2E 236 形四改版（赋值位接收 + 跨越，255）；
   反证=注入「调用返回右值分支失效」→ 236 v2 编译失败。**语法约束**：v2 的 `结果.值` 访问须在
   `如果 (r.正常) { ... }` 正向真分支直系（写含结果解包的新代码按此形态）。

7. **防线（探针转正）**：E2E **236_双编译对照结构体返回实参**（移植纪律 9 **第八个**
   对照用例：八形态折 **255**，②双实参为核心回归锚；不含容器=无供给、无需借链）+
   **E2E 234 形六改回「调用返回实参入容器」**（八形态折 255 不变）；**反证实证**：v2 注入
   「物化恒不物化」→ 236 v2 255→**68**（①②⑤⑥⑧ 位全失），恢复后两侧 255 ✓。
   （172 跨模块分桶在修复过程中一度回归 v2=199 → 根因Ⅲ 修复后复原 ✓）

8. **文档五件套**：plans/019（跟踪表第八十二轮 + 后续行）+ plans/020（第十四节 82-a + 矩阵 B38 +
   槽分配纪律 + 诚实边界）+ lessons（第八十二轮段，权重 9，预防含「续㊽」四条）+ 更新日志
   + 本文件。

## 一之补：84-d 摘要（本轮，**数据正确性级缺陷已销项**）

- **缺陷**（探针 P29/P30）：`深C{ d = 深B{ b = 深A{ a = 1 }, c = 2 }, e = 3 }` → `丙.d.b.a`
  读 **0**（应 1）、`收深(丙)`=5（应 6）；逐字段写读同款；二级/一级正常（=回归锚）。
- **根因**：宿主 `visitMemberExpr`（读取路径）对象类型推导**只手写「一层嵌套」** → 三级链
  `objSrcType` 退化空 → `decl==nullptr` → **字段读降级常量 0**；**既有递归辅助
  `memberObjStructType` 早已存在却未被读取路径使用**；v2 天然递归=仅宿主。
- **修复（甲案）**：空值兜底一行（`if (objSrcType.empty()) { objSrcType = memberObjStructType(node); }`）
  + `memberObjStructType` 裸指针统一剥一级；乙/丙登记。
- **防线**：**E2E 241_双编译对照成员链嵌套深度**（第十二个对照用例，八形态 **255** 两侧一致）；
  **反证**：注入「兜底失效」→ 241 宿主 255→**32**（⑥ 对照位保留=精准命中三级面）。
- **验证链**：零警告 + 单测 1306/1306 + E2E **268 用例 264 过 0 失败**/4 跳 + 固定点
  **325982**（不变）逐字节一致 + 对拍 34/34。

## 一之补：84-c 摘要（本轮）

- **缺陷**（探针 P24~P29）：嵌套字面量三处 v2 缺口 —— ① 嵌套结构体字段值按宽度存储
  （垃圾值：宿主 10 / v2 140735369840171）；② 嵌套字段未登记资源字段（嵌套串残留 3）；
  ③ 外层遍历把内层字段初值当自己的字段（二次求值+错偏移，残留 1）。
  **另揪出**：**宿主三层嵌套字面量丢最内层字段**（`丙.d.b.a` 读 0=**数据正确性级**，
  **下一轮第一优先**）；**v2 纯标量嵌套结构体局部引用 `类型$~类型`**（链接失败，下一轮）。
- **修复**（v2 三项）：字段类型分派（结构体字段整体拷贝）/构造遍历跳过嵌套区间/区间收集
  只留最外层。**嵌套字段资源登记=回退**（`资源字段数` 多处消费 → 回归 → 回退 + 登记；
  嵌套串维持泄漏方向）。
- **防线**：**E2E 240_双编译对照嵌套字面量**（第十一个对照用例，八形态 **255** 两侧一致）；
  **反证**：注入「嵌套区间跳过失效」→ 240 v2 255→**52**。
- **验证链**：零警告 + 单测 1306/1306 + E2E **267 用例 263 过 0 失败**/4 跳 + 固定点
  **325982**（+1609）逐字节一致 + 对拍 34/34。

## 一之补：84-b 摘要（本轮）

- **缺陷**：84-a 新路径（表达式位字面量）未带字段校验——宿主/旧脱糖位均有 **E0062（重复
  字段）/E0063（缺字段）= 编译错误**同构纪律 → 缺失即「静默语义偏差」（重复取后值/缺字段
  留零值）。**实施**：`解析原子` 字面量分支补两校验（与旧脱糖位逐字同诊断文本）。
- **防线**：**E2E 239_v2_字面量字段校验**（v2 闭环负测：重复/缺字段 → v2p 非零退出 +
  不产 v2asm.s + 三条锚行固化）；**反证**：注入「两校验同时失效」→ 239 FAIL（退出码 0
  静默放行），恢复后 PASS ✓。
- **验证链**：零警告 + 单测 1306/1306 + E2E **266 用例 262 过 0 失败**/4 跳 + 固定点
  **324373**（+623）逐字节一致 + 对拍 34/34。

## 一之补：84-a 摘要（本轮）

- **缺陷**（探针 P23）：`收(盒子{…})` / `表.追加(盒子{…})` 宿主 ✓、v2 ✗（`[parse] 语句
  缺少分号` 中止）。**根因**：v2 的结构体字面量是解析期脱糖，只在声明/赋值/成员赋值三位
  接入（表达式/实参位未接入）。
- **实施**（v2 树 4 文件）：新节点 `节点_结构体构造`(43)/`节点_字段初值`(44) + `解析原子`
  分支 + `生成结构体构造`（临时槽 + 零初始化 + 逐字段写 + 登记 `结构体返回临时们` → 复用
  82-a 实参清理）+ 主循环**构造区间跳过**（否则值表达式重复求值）。
- **防线**：**E2E 238_双编译对照结构体字面量实参**（护照纪律 9 第十个，八形态 **255** 两侧
  一致，供给=向量<盒子>）；**反证**：注入「解析原子 分支失效」→ 238 v2 解析失败（节点
  581→319、36 语法错误），恢复后 255 ✓。
- **验证链**：零警告 + 单测 1306/1306 + E2E **265 用例 261 过 0 失败**/4 跳 + 固定点
  **323750**（+3326）逐字节一致 + 对拍 34/34。

## 一之补：83-a 摘要

- **缺陷**（探针 P22）：`队列<整64>` 入队 11/22/33 + 出队 + 入队 44 → 出队序 **22/44/44**
  （33 丢失）；`链表<整64>` 同款（33 被覆盖 + `next` 自环）；`链表<字符串>` 头部内容错 +
  残留 1。**根因**：`新索引 = 元素数量` 假设「槽 0..元素数量-1 全占用」——删头/删尾破坏。
- **修复**：**空闲链复用 + 高水位计数**（布局 +2 字段 `空闲头`/`已用槽数`，追加在末尾→
  老偏移不变；`自身.取空闲槽()`/`自身.归还槽(槽)`；扩容按 已用槽数；清空重置两字段）；
  v2 `容器对象大小` 链表/队列 48→64 同步。
- **防线**：**E2E 237_双编译对照链表槽复用**（移植纪律 9 第九个，八形态 **255** 两侧一致，
  供给=链/队<整64/字符串>）；**反证**：注入「取空闲槽 退回旧逻辑」→ 237 宿主 255→**240**
  （①②③④位丢失），恢复后 255。
- **验证链**：零警告 + 单测 1306/1306 + E2E **264 用例 260 过 0 失败**/4 跳 + 固定点
  **320424**（不变）逐字节一致 + 对拍 34/34 + 释放路径对照 ✓。

## 二、下一轮任务（按序）

1. **【84-d 已销项】宿主三层嵌套字面量丢字段**（数据正确性级，探针 P29）——根因=读取
   路径类型推导只覆盖一层嵌套 → 字段读降级 0；修复=空值兜底 `memberObjStructType` +
   门禁 E2E 241 + 反证（255→32）。**余项**：v2 纯标量嵌套结构体局部引用 `类型$~类型`
   （供给无该类符号 → 链接失败）与 v2 嵌套结构体字段串释放面（须先 grep 梳理
   `资源字段数` 全部消费点再收口）——**登记下一轮**。
2. **【待用户裁决｜阻塞项】B35 字段容器接收者**（残留 2=字段容器对象+元素串）——
   **已呈报三方案于 plans/020 第十七节**（甲 move 语义〈性能最优但语言级语义变更〉/
   **乙 深拷〈推荐：零语义变更、与 79-a 字符串字段同哲学〉** / 丙 维持+诊断）；
   **须用户裁决后实施**（AGENTS.md §2.3 新增语法规范须审批）。原登记描述： **B35 字段容器接收者**（残留 2=字段容器对象+元素串）：容器字段的写入/拷贝/消亡三件套涉
   **字段赋值语义（move vs 深拷）语言级裁决**（79-a 丙案同族）——须先向用户呈报裁决。
3. **stdlib 链表 `追加` 槽复用缺陷（83-a 已修，此处为历史条目）**（删除头部后用 `元素数量` 作新槽号与在链槽冲突：
   追加 11/22/33+删头+追加 44 → 44 丢失）；修法需空闲链/已用槽数（布局变更，需同步 v2
   容器对象大小表与 `容器元素释放参数们` 硬编码偏移）。
3. **v2 结构体字面量补强（84-b 字段校验已完成；余项登记）**——v2 语法层不支持
   `收(盒子{...})` / `表.追加(盒子{...})`（探针 P23：`[parse] 语句缺少分号`）。根因：
   v2 的结构体字面量是**解析期脱糖**（`解析构造字段赋值们`），只在声明/赋值/成员赋值三位
   接入，**表达式/实参位未接入**。**实现方案见 plans/020 第十八节**（新节点
   `节点_结构体构造`/`节点_字段初值` + `解析原子` 分支 + `生成结构体构造`（临时槽复用
   82-a 物化/清理机制）+ E2E 238 门禁）。
4. **物化性能优化（可选）**：延迟物化 + 活跃性分析（当前=每次真结构体返回 +1 memcpy +
   帧尺寸增长；固定点 +10838 行）；**结果/可选 返回**是否纳入物化面（当前登记为不可达）。
5. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区
   变硬错误）；v2 诊断无行号；未初始化槽 preFree 隐患（观察项）。
6. **跨机轮**：arm64（单位机麒麟）复验与 E2E 234/235/236（linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 268 用例 264 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 325982 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（含 82-a 新段）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`，
> 并发会踩出假警。**全量 E2E 与手工 v2p/固定点操作不得并发**。E2E runner 的 v2p 构建缓存按
> v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时删缓存键**。
> **反证手法**：注入 → 重建 v2p → 跑用例（应 FAIL）→ 恢复（本轮用 `cp IR容器.cn /tmp/...bak`
> + python 替换 + 复原）。

## 四、本轮踩坑（已入 lessons 第八十二轮段）

- **返回值存放位置必须与「每次调用」绑定**（sret 语义），共享 retbuf 必被「跨越」覆盖。
- **多槽对象的基址槽约定先查再写**（v2：`槽起+槽数-1`=最大槽号=最低地址）+ **逐槽 `IR_分配`**
  （扫描函数 按附加统计最大槽=帧尺寸依据）。
- **跨模块同名类型必须按「定义模块」解析**（函数键 `模块$名#参数串`；症状「大小对一半」）。
- **诊断打印不要走计数型通道**（v2 `IR层诊断` +1 错误计数并触发中止；调试用 `IO::打印到错误`）。
- **修缺陷用「最小形态矩阵」逐项回归**（P17→P20→172 三层缺陷叠加，只跑单一探针会漏）。

## 五、诚实边界

- v2 **结果/可选 返回**不物化（合成布局；其返回值语言层不可作实参 → 覆盖不可达，登记）；
- `造盒(5).号`（调用返回直接成员访问）=两侧共同边界；**赋值位接收结构体返回**=v2 不支持（登记）；
- 物化代价=每真结构体返回 +1 memcpy + 帧尺寸增长（可在后续优化为延迟物化）；
- v2 结构体字面量作实参（语法缺口）、B35、链表槽复用、未初始化槽 preFree、数组元素/装箱
  字段接收/静态面/容器字段浅拷 等 81-a/79-a 边界维持；win-x64 v2 并行未隔离（家机轮）；
  arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_s.s **325982 行**、cn_self71）。
- 探针（/tmp/p81probe，不入库）：P15（追加+返回实参）、P17（双结构体返回实参）、P18/P19/P20
  （标识符/混合实参 + 逐步打点）、v2run.sh（v2 闭环复现）、IR容器.cn.82a.bak（反证备份）。
- E2E 用例：`tests/e2e/236_双编译对照结构体返回实参/`（主.cn + 双编译对照.txt + 主.expected）、
  `tests/e2e/234_双编译对照容器元素结构体字段/`（形六已改回调用返回实参）、
  `tests/e2e/235_容器元素实参形态/`。
- 修改文件（84-d）：`src/cn_compiler/ir/{ir_expr.cpp,ir.cpp}` + tests/e2e/241（新）。
- 修改文件（84-c）：`CN语言编译器v2/IR/{IR常量.cn,生成表达式求值.cn,生成表达式.cn,IR布局.cn}` + `CN语言编译器v2/语法/{解析表达式.cn,语法分析.cn,解析类.cn}` + tests/e2e/240（新）。
- 修改文件（84-b）：`CN语言编译器v2/语法/解析表达式.cn`（校验）+ tests/e2e/239（新，负测）。
- 修改文件（84-a）：`CN语言编译器v2/IR/{IR常量.cn,生成表达式求值.cn,生成表达式.cn}` + `CN语言编译器v2/语法/解析表达式.cn` + tests/e2e/238（新）。
- 修改文件（83-a）：`stdlib/容器.cn`（链表/队列 空闲链）+ `CN语言编译器v2/IR/IR容器.cn`（容器对象大小同步）+ tests/e2e/237（新）。
- 修改文件（82-a/82-b）：`CN语言编译器v2/IR/{IR容器.cn,生成表达式求值.cn,IR生成.cn,生成语句.cn}`
  + `IR/生成语句.cn`（82-b 赋值分支）+ `scripts/cn_release_parity.sh` + tests/e2e/234（形六）/236（新）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（第十四~二十二节 82-a~84-d + 矩阵 B38~B42；
  每轮触碰所有权/内存模型必须同步更新）。
