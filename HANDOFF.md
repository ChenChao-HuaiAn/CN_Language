# HANDOFF - CN语言编译器项目交接文档

> **交接原因**：2026-08-15 会话结束（**模块系统 v2.0 全面改造——第 1 层「规范文档更新」已完成 ✅**。依据用户已批复的《模块系统 v2.0 全面改造方案》12 项决策，更新全部规范文档使 v2.0 语法先行确立。本层纯文档更新，未修改编译器源码。gitcode 推送成功 `885cd60..f95d364`）。
>
> **前序里程碑**：阶段3「OOP 与错误处理」（901/901 单测、28/28 E2E）；阶段A「Linux ARM64」（918/918）；阶段B 优化补全（949/949）；阶段C 寄存器分配/调试信息（964/964）；阶段6「标准库」全面完善（1050/1050 单测、44/44 E2E，10 个 stdlib 模块 + 5 个运行时文件，4 项编译器缺陷根治：__chkstk/泛型函数指针/构造重载/泛型字段循环）；模块导入语法测试补全（1057/1057 单测、45/45 E2E）。

## 一、我们在做什么任务

正在开发CN语言编译器——一门全中文语法的系统级编程语言，参考C++编程范式，用C++17从零编写编译器，直接生成汇编代码。当前进入**「模块系统 v2.0 全面改造」**项目（plans/002-阶段6b-模块系统 v2.0」全面改造方案.md），共 **10 层实施计划**：

| 层 | 内容 | 模式 | 状态 |
|----|------|------|------|
| 1 | **规范更新**：specs/08 重写 v2.0（导入/::/模块/包/默认私有/**可见性交集章节**）+ 01a 关键字（删 从、增 模块/作为/包/货舱）+ 03/06 可见性 + 09-包与依赖.md | Code | ✅ **本轮完成** |
| 2 | 词法：ColonColon + 新关键字 4 个 + 删 `从` + 单测 | Code | ⏳ 下一步 |
| 3 | 语法：parseImportDecl（::/作为/花括号/通配符）+ parseModuleDecl + 默认私有（模块+类内）+ AST + 单测 | Code | |
| 4 | crate 重构：module.cpp 分桶 + 目录层级 + 语义分域 + use 导入表 + P1-1 + prelude + 内置 key `::` 化 + IR/codegen 包前缀 + 顶层常量/静态 + **可见性交集检查** + 单测 | Code | |
| 5 | 货舱.toml：解析器 + CLI + 依赖查找 + E2E | Code | |
| 6 | 全量迁移：8 E2E + 3 单测 + stdlib 10 模块（公开: 显式化）+ 2 个 `函数 使用()` 改名 | Code | |
| 7 | 新增测试：44_crate_isolate / 45_import_syntax / 46_module_tree / 47_package_cargo / 48_prelude / 49_default_private / 50_class_private / **51_visibility_intersection** + 单测 | Code | |
| 8 | 全链路集成串联：38_tool 迁移 v2.0 | Code | |
| 9 | Debug 全面审查：内置 key、crate 前缀、默认私有、prelude、交集规则、全量回归直至零缺陷 | Debug | |
| 10 | 收尾：plans + 更新日志 + HANDOFF + lessons + gitcode 推送 | 收尾 | |

## 二、已经完成了什么

### 2.0 本轮任务：模块系统 v2.0 第 1 层——规范文档更新（已完成 ✅，提交 f95d364）

**v2.0 核心决策（12 项，全部写入规范）**：路径分隔 `::`（替代 `.`）；新增 `模块`/`作为`/`包`/`货舱`、保留 `导入`、删除 `从`、不引入 `使用`；标签式可见性 + **模块内默认私有**；**类内访问控制默认私有**；crate 隔离（包=crate=命名空间边界）；`包.cn` 根 + 模块树；子目录 `网络/传输控制.cn` → `网络::传输控制`；核心包 prelude；导入全形式（路径/作为/花括号/通配符）；`货舱.toml`（`[货舱]` 名称/版本 + `[依赖]`）；**可见性交集规则**（取最严格 + 四象限矩阵）；不强制行尾 `;`。

**文档变更清单（15 文件，+941/-169）**：

| 文件 | 变更 |
|------|------|
| `.ai-coder/skills/cn-language-spec/specs/08-模块系统.md` | **全面重写 v2.0**（约 380 行）：设计原则、模块声明、`::` 路径、导入全形式 EBNF、默认私有、**可见性交集规则（四象限矩阵）**、crate 模型、包.cn、prelude、货舱.toml、迁移映射表、无强制分号 |
| `.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md` | 关键字 58→61：删除 `从`、新增 5.3b 模块系统关键字（模块/作为/包/货舱）；设计决策表 #8 标注 v2.0；变更记录 v2.0；顶层声明补模块/常量/静态 |
| `.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md` | 新增「三b、模块级可见性（默认私有，v2.0）」 |
| `.ai-coder/skills/cn-language-spec/specs/06-OOP面向对象.md` | 类内访问控制 **默认私有**（原默认公开）+ 可见性交集说明 |
| `.ai-coder/skills/cn-language-spec/specs/09-包与依赖.md` | **新增**：货舱.toml 完整规范、包.cn 结构、crate 边界（`包名$` 前缀）、核心包 prelude、依赖解析流程 |
| `plans/001 CN语言编译器设计规格书.md` | 决策表 #8 标注 v2.0；**5.9 模块系统章节全面重写**（5.9.1~5.9.6） |
| `.ai-coder/skills/cn-language-spec/SKILL.md` | 索引表（01a 61 关键字/06 默认私有/08 v2.0/新增 09）、查询关键词、差异表 v2.0 |
| `.ai-coder/skills/cn-language-spec/README.md` | 版本 v2.0、目录 10 文档、索引表、差异表 |
| `.ai-coder/skills/cn-language-spec/config/default.config.json` | specVersion v2.0、索引新增 09、关键词映射 |
| `.ai-coder/skills/cn-language-spec/templates/*.md`（3 个） | 版本标注 v1.0 → v2.0 |
| `更新日志.md` | 覆盖写入本轮说明 |

**测试基线**：本层纯文档更新，未修改编译器源码，编译/测试基线不变（单测 1057、E2E 45）。

**Git**：提交 `f95d364`（中文标题 + 描述，15 文件 +941/-169），**推送 gitcode develop 成功**（`885cd60..f95d364`，Git Hooks PASSED——gitcode 服务端 403 问题已恢复）。

## 三、总结发现的问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | gitcode 服务端 403 资源限制（前序 3 次重试失败，本地 10 提交待推） | ✅ 本次推送成功，全部同步（含阶段6 遗留提交 4644ca1/045853c 等 9 个 + 本轮 f95d364） |
| 2 | 本层规范更新发现的文档不一致：SKILL.md/README/config/templates 均有 v1.0 引用与旧索引表，需同步 v2.0 | ✅ 已全部同步 |
| 3 | 前序已知 P1 缺陷（未导入模块的限定调用被静默接受） | ⏳ 待第 4 层语义严格化解决（方案 P1-1） |

## 四、当前卡在哪

**无阻塞**。第 1 层规范更新已全部完成并推送。下一步是**第 2 层：词法改造**（ColonColon token + 4 个新关键字 `模块`/`作为`/`包`/`货舱` + 删除 `从` + 单测）。注意：第 2 层起开始修改编译器源码，需按用户规则编译验证通过后才能提交。

## 五、下一步计划

1. **第 2 层（Code）**：词法 `::`（ColonColon）+ 新关键字 4 个 + 删 `从` + 词法单测（test_lexer_keywords.cpp 更新）
2. **第 3 层（Code）**：语法 parseImportDecl（::/作为/花括号/通配符）+ parseModuleDecl + 默认私有 + AST + 单测
3. **第 4 层（Code）**：crate 重构（module.cpp 分桶 + 目录层级 + 语义分域 + use 导入表 + P1-1 + prelude + 内置 key `::` 化 + IR/codegen 包前缀 + 顶层常量/静态 + 可见性交集检查）
4. 第 5~10 层按方案实施计划推进（货舱.toml → 迁移 → 新增测试 → 集成 → Debug → 收尾）
5. 所有层完成后：plans/002-阶段6b 打勾 + 更新日志 + HANDOFF + lessons + gitcode 推送

## 六、踩过的坑（教训）

| # | 坑 | 教训 |
|---|----|------|
| 1 | 阶段6 大栈帧无 __chkstk 崩溃（已修复） | 单函数局部变量+打印总量控制栈帧 <8KB；大程序拆多小函数（≤100 行） |
| 2 | 泛型+函数指针、构造重载、泛型字段循环（已修复） | 泛型函数参数避免 C 风格函数指针类型；构造函数重载已支持（sigKey）；泛型类方法循环已支持 |
| 3 | Windows 命令行参数三连坑（已修复） | 用 `CommandLineToArgvW(GetCommandLineW())` + CP_UTF8，勿依赖 `__argc/__argv/__wargv` |
| 4 | 标准库模块默认公开（v1.0）→ v2.0 默认私有 | **迁移时 stdlib 10 模块文件顶部必须显式 `公开:`**（第 6 层全量迁移重点，最高风险②） |
| 5 | 模块导入测试曾只测一种形式（已补全） | 新增功能 E2E 必须覆盖全部公开语法形式 + 边界 |
| 6 | 内置函数限定名前缀不可用类型关键字 | 前缀必须是**非关键字标识符**（数学/IO/文件/时间/系统/解析 安全；`字符串`/`整数`/`结果` 禁止） |

## 七、关键约定（后续层实现必读）

- **v2.0 语法定义以 specs/08（重写版）+ specs/09（新增）为准**，词法/语法/语义实现直接引用。
- 关键字 61 个（v1.0 的 58 - 从 + 模块/作为/包/货舱）。
- 可见性交集：模块级公开/私有 × 类内公开/私有 四象限（specs/08 第四节矩阵）。
- crate 符号前缀 `包名$`；泛型实例化 `包名$类型$参数`（避免与 `$` 泛型混淆）。
- 内置函数归入核心包 prelude；内置限定 key 由 `.` 改 `::`。
- 无强制分号：声明以换行终止，`;` 仅同行多语句分隔。
- 测试基线：单测 1057、E2E 45、编译 0 警告（MSVC /W4 /WX）。
