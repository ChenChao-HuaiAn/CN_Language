# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十八轮（**深度机 linux-x86_64**）——**plans/019 阶段2「引用逃逸检查强化」双侧落地**（局部地址流入静态/全局/指针返回=编译期拒绝）+ **两枚立案缺陷根治**（①推断声明字符串拷贝打印错分派=方案A 回填扩字符串 / ②v2 返回类型 `*` 后缀）。v2self 固定点复锚 **244050 行 = X64L 新口径** + 组件对拍 29/29 + 运行级 3/3 + 全量门禁 213 用例（唯一败=194 既有待批）+ 单测 1274/1274。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **阶段 2 预审**：stdlib「= &局部」零命中；v2 引用局部 20 处全为 `向量<IR指令>& 指令 = 函数信息.指令`（绑参数引用=指向调用方存储，非拦截面）——零误伤双证。
2. **宿主实施五文件**：isLocalAddressValue + refLocalBases_/ptrLocalPointees_ 函数级表 + 赋值逃逸（非局部对象字段**自行解剖对象链**——refReturnLvalueBase 对 deref 短路会放行静态指针字段，实测漏检修正）+ 指向登记（赋值位+声明位）+ 指针返回局部地址拒绝（含经指针转接）。
3. **缺陷①方案A**：推断声明回填扩展字符串（`变量 乙 = 甲; 打印(乙)` 从指针值 4285000 → 「左」；布尔维持 184 契约）。
4. **v2 实施三文件**：是局部名 + 赋值/返回逃逸（返回检查**引用参数豁免**=静态集[类型节点附加==256 收集]，宿主 isRefParamForCurrentFn 对齐）+ **缺陷②** 返回类型 `*` 后缀并入。
5. **E2E 四用例双通道**：198（宿主负测五形态，真先红=子串锚新消息）+199（缺陷①正测）+198_v2（负路径实测固化）+199_v2（rc=3：指针返回 &引用参数+读写回）。
6. **单测** test_escape_check.cpp 10/10。

## 二、下一轮任务（按序）

1. **plans/019 阶段 3：常量 T& 只读借用 + 借用互斥第一版（语句级保守）+ 转移浅拷贝优化**（详案 plans/019 §5.2）——常量 引用类型语法（parser 扩展）+只读检查+语句级别名拒绝+IR 浅转移分派；rustc NLL 教训：渐进实测定深度。
2. **194 测试改写待批挂账维持**（第五十六轮呈报）。
3. **跨机轮（win/arm64）**：第五十七+五十八轮宿主与 v2 变更复验与口径重锚。
4. **v2 静态标量观察项**：函数内按栈槽 vs 宿主 .data（199_v2 探针实证）。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 深度机 linux-x86_64（本行口径）
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1274/1274（+10 EscapeCheckTest）
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 213 用例（败=194 待批，跳=62/69/78/79）
bash target/p58x64l/run_chain58.sh   # 锚定链（[3] readelf 解析 bug——map 手工判定
#   cn_main=0x4de72f ∈ cn_self58.o .text [0x402586,0x4e218e)；[6] 段口径错——运行级按
#   runlevel50.sh 口径；173 入口实名 173_v2_目录包根撞名成员）
bash target/p58x64l/cmp29_58.sh      # 组件对拍 29/29
# 固定点 X64L 新口径=244050 行（win/arm64 分后端口径不跨后端比较）
```

## 四、本轮踩坑（已入 lessons 第五十八轮条目）

- **负测假绿**：198 首版子串「局部变量的地址」被既有检查（引用返回 r）的错误撑起=PASS 假绿，掩盖新增四形态静默——负测子串必须取「新增检查独有消息片段」+check 实测确认当前不含。
- **v2 括号笔误**：`)))` 多一层——宿主编译 v2 全树 30 秒暴露三层连锁；v2 源码写完即全树冒烟。
- **v2 机制想当然×2**：引用参数豁免按「类型名尾 &」判失败（v2 符号类型ID=基名，引用性在类型节点.附加=256）；静态标量作指向源失败（v2 函数内按栈槽）。跨侧写判定先 grep v2 实际承载字段。

## 五、诚实边界

- 逃逸检不出面：指针间传递链 / 容器元素位（值语义深拷为主形态）/ 跨函数引用写回——阶段 3 随借用分析深化；v2 成员/下标目标位随引用面扩大对齐。
- 缺陷①根治范围=字符串（布尔维持）；缺陷②=单 \* 锚定（多级 \* 与 IR 下游未专项验证）。
- 194 待批维持；win/arm64 复验=下轮跨机轮；探针与脚本留 target/p58x64l/ 不入库。
