# HANDOFF 交接文档

**交接时间**: 2026-09-02 第三轮（②b 结构体元素容器收官：126 OOM 根治 + 四缺口一步到位）

## 本次会话做了什么

1. **126 运行时 OOM（SIGKILL）分诊确诊 + 根治**（用户裁决计划六项全部落地）：
   - 根因（三重实锤：ulimit 2GB 复现退出 201=追加 realloc 耗尽 → 死循环；asm 级 8 字节
     `str x10,[x9]`；n 基址-1096/i 槽-1072 逐字节对上）：**字段读写无宽度概念**——
     `n.子终点=3`（整32@20）8 字节写越出 24 字节结构体 4 字节清零 i 槽 → 当循环死循环。
   - B5 宽度读写+链式偏移收口 / B6 retbuf 按返回字节数定帧 / B7 符号供给机制 / B8 按值
     传参被调方多槽拷贝+普通调用传址（详见 plans/014 四节 ②b 收官条目与 更新日志）。
2. **连带新缺陷两枚根治**：① 裸块语句 `{}` 解析缺失（解析语句 无分支 → 解析块 循环保护
   静默中断 → 半个函数被吞、返回 0 丢失 → 126 垃圾退出码 20）；② 供给源经宿主编译须拷为
   主.cn 入口（规范08-四：仅 主.cn 为入口模块，非主命名入口函数被静默过滤——规范行为，
   lessons 已记）。
3. **B4 裁定（ABI 级事实，非规避）**：宿主「有析构」=显式声明析构方法（class_resolver:145）；
   字符串=驻留不朽指针无释放语义 → 词条 这类「整64+字符串」结构体**无析构可注入**（深拷贝=
   指针拷贝），126 ④a 零泄漏由绿色运行证实。B4 真正载体=拥有式字段（容器作结构体字段）在 ②c。
   **若用户对此裁定有异议（期望字符串字段有释放语义），②c 前提出重裁**。
4. **126 注册运行器并全绿**：v2闭环用例们 新增 ("主.cn", 0, True, ["供给.cn"])；expected
   自检数字行更新（节点 167→260——裸块修复后解析更多；asm 行数 950→1971）。

## 验证状态（诚实门禁，接手先看）

- 本机（Linux ARM64）：单测 1196/1196 + E2E linux-arm64 全量 128 用例 = **123 过 / 4 平台
  跳过（62/65/69/79）/ 1 失败 = 78 已知遗留（内存超限，旧架构固有，与本轮无关）**。
  v2 全系 119/120/123/125/**126** 5/5 全绿（126 首次）。
- **待 Windows 会话**：win-x64 全量门禁（ci.ps1）——重点 126（含供给路径 win 分支）与
  119/120/123/125 的 MASM 路径运行时。本机已做 MASM 产出 asm 级核验（宽度指令/retbuf 帧
  尺寸/被调方 rep movsb prologue 结构无损），**未 ml64/link 运行，不写假绿**。

## 发现的问题 / 立案

- **②c 待办（B4 真载体）**：容器作结构体字段时的拥有式析构注入——本轮裁定字符串字段
  无析构可注入（见上）；②c 实现容器字段时须 revisit。
- 嵌套结构体成员（a.b.c 链式）宽度解析：IR生成 取成员宽度码 已留 TODO（②c）。
- 循环保护中断静默（解析块）：已记 lessons 预防项——③ 前宜对中断处打诊断（裸块案例
  证明静默中断可吞半个函数）。
- 宿主入口模块静默过滤（非 主.cn 入口）：规范行为但无诊断——③ 自举对齐时建议入口
  识别打日志（lessons 8.5 权重条目）。

## 下一步

1. **②c**：嵌套容器（向量<映射>）/ 容器作结构体字段（**B4 真载体：拥有式字段析构注入**）/
   顶层静态容器 / & 引用参数。
2. ③ v2 编译 9+组件自举对拍（**ARM64 路径本机即可全程对拍**——链接 v2p_linux.o 模式已验证；
   B8 普通调用传址正是为此铺路：v2 自身 IR追加(指令,建IR指令(...)) 全是结构体按值实参）。
3. 性能 bench（双目标①）在 ③ 后跑 scripts/bench_self_host.py。
4. Windows 会话：win-x64 全量门禁（上节）。

## 踩过的坑（本会话新增，详见 lessons 2026-09-02 13:30/13:35/13:40 条目）

- 「死循环/OOM」分诊先查**写宽越界破坏相邻变量**再查循环逻辑（本例循环代码完全正确，
  i 槽被末字段 8 字节写击穿）；ulimit -v 限内存可把 OOM SIGKILL 转为可断言的干净退出码。
- 缓冲区尺寸凡按已知形态硬编码（retbuf 2 槽=16 字节），新增更大形态必炸——尺寸必须
  数据驱动（返回字节数查表）。
- 「编译成功但退出码是奇怪的小整数」= 返回值丢失信号 → 先查解析是否提前静默中断
  （裸块本案）；语义层 [子起点,子终点) 区间约定：节点_成员.子终点 不可复用存类型ID
  （曾致误报，本次宽度解析走 变量结构体表 而非节点字段，规避同坑）。
- 宿主 cn build 入口必须命名 主.cn（规范08-四），否则函数被静默当导入模块私有声明过滤。
