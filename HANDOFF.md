# HANDOFF 交接文档

**交接时间**: 2026-09-07 第三十二轮（深度系统 x86_64）——**P6b 工作流 3 后半+工作流 4 锚定闭环**：v2 语义/IR 模块分桶+三级名称解析+可见性强制+链接键统一落地；v2self 三级锚定（组件对拍 13/13+8 用例对拍+字节级固定点）全绿；双侧同构缺陷（静态容器引用实参）宿主+v2 双侧根治。**下一步 = 类型/常量 IR 布局分桶（对齐宿主 E2E 56）+千行组件拆分+跨机轮复验**。

---

## 一、本轮核心实施（工作流 3 第 3/4/5/7 层+工作流 4）

1. **语义分桶+三级解析**（语义分析.cn 核心）：全局符号表键升级「键基」（IR容器.键基合成：主模块=纯名、其余 `模块$名`——宿主 functionLinkKey 同源公式；**单文件/主模块形态逐字节不变=v2self 对拍硬基准**）。三级：①本模块（成员索引）＞②显式导入（构建文件导入上下文 预解析，E0255 同名/私有/存在性诊断；同源幂等/自导入豁免对齐宿主）＞③**合并全局可见**（加载树全部模块公开符号——初版「仅直接导入」被 IR生成 用 IR签名 的 段首 传递依赖 222 错实测纠正，宿主 driver「合并阶段全局可见」为事实标准）。③同名多命中按签名消歧：同签名多模块=真歧义（E0659 缓冲诊断+三级缓存去重）；不同签名=重载共存（实参数可辨选其桶[按实参数选桶]，否则首候选——跨桶重载部分支持，诚实边界）。
2. **可见性强制**：`公开:`/`私有:` 标签块解析（语法层 顶层公开平铺们+顶层可见性登记——**此前标签被顶层循环静默跳过，语义从未消费**）；符号默认私有，跨模块 ②③/限定调用只认公开；13 组件头部公开段实测覆盖全部跨组件引用（零补标需求）。
3. **IR 链接键（第 4 层）**：定义侧 生成函数 与调用侧 决议函数符号 同公式（键基#参数串）；调用决议键基们（语义驱动 IR：语义三级决议写入 IR容器 静态表，IR 查表锁定候选桶后重载决议——宿主 resolvedSignature 同构，零反向依赖）；收集函数签名/实参结构/返回类型 键基化（纯名双键仅主模块）；用户模块限定调用 m::x 打通（语义 标识符/调用两分支限定解析——语句根区间线性检查会把被调合成名当裸标识符送达，两分支都要有）。
4. **双侧同构缺陷根治（缺陷零容忍，lessons 新条目）**：静态容器引用实参取地址语义丢失——**宿主** ir_expr.cpp AddressOf 分支补顶层静态操作数（wrapRefArgs 包的 &静态 落 genExpr 读值兜底→句柄值当地址→空容器错误码3/非空读写错位；**局部容器按值语义天然发 AddrOf=侥幸路径，同模块形态从未暴露**）；**v2** 回扫实参取地址 补回扫5（IR_静态加载→改发 IR_静态地址 同序号）。Rust 准绳：&static 恒指向存储位置本身。
5. **平铺纪律落地（lessons 15 枚同族二例）**：嵌套容器（向量<向量>/向量<映射>）静态+临时值追加=句柄悬垂——四快照改标量平铺（导入项平铺总表+边界向量们+公开平铺总表）；`查.值.类型ID` 链式两层=[ir2] 拒发——逐步提取惯例。
6. **加载器模块路径链（主.cn）**：编译文件 加 本模块路径 参数（入口=主/依赖=名/库包根=包/挂载=父路径聚合或 `父路径::名`），收集 模块路径们/平铺总表/边界们/公开平铺总表；内置回退路径同构补快照。
7. **E2E 167~171**（v2 闭环）：三级解析遮蔽/E0255 导入同名/glob 歧义/私有不可导入/跨模块同名共存 asm 隔离（对应宿主 159~163/160）；run_e2e 负路径补整树复制（正路径同构先例）。

## 二、验证证据（全绿）

- 单测 **1249/1249**；E2E linux-x86_64 全量（171 用例级，含新 167~171 五例）零回归；v2 闭环 28/28。
- v2p 重建 **14 组件冒烟**（含 货舱解析）全 PASS。
- **v2self 三级锚定**：①组件对拍 13/13；②8 用例（119/123/126/128/130/148/150/158）v2p↔v2self 产物规范化逐字节一致+链接运行 rc 一致（/tmp/v2dbg/p32/probe.sh+norm.py 三件套，supply 编译=宿主 cn build --output 副产品 .o）；③**字节级固定点**：v2self 编译 v2 主链=v2p 产物（fix_p/fix_s 规范化一致，双双零 [ir2]）。
- 宿主 AddressOf 修复经全量 E2E+单测零回归。

## 三、诚实边界

1. **结构体/常量 IR 布局仍按纯名**（结构体大小/字段偏移/常量表）——跨模块同名结构体布局混用=旧状；类型全链分桶（对齐宿主 E2E 56 甲::记录）=下轮首务。
2. **③跨桶重载部分支持**：同名不同签名多模块命中时按实参数个数选桶，个数不可辨回落首候选（跨键基完整重载决议随类型分桶轮评估）。
3. **限定调用私有诊断双报**（标识符分支+调用分支对同一节点各报一次）——错误计数正确、方向正确；去重随诊断体系完善。
4. **千行拆分挂账**（语法分析.cn 1320+/IR生成.cn 3107+）——本轮大改刚落地立即拆分放大回归面，随类型分桶轮一并。
5. win-x64/arm64 复验=跨机轮（本轮改动全在共享层 v2 CN 源码+宿主 ir_call/ir_expr）。

## 四、下一步（优先序）

1. **类型/常量全链分桶**：结构体大小/字段偏移/常量表按键基（对齐宿主 A1/E2E 56 甲::记录 限定类型语法）——注意类型文本在 IR 指令与代码生成符号的全链传导。
2. **千行组件拆分**（第 8 层挂账）：语义分析/IR生成 拆子目录。
3. **跨机轮**：win-x64/arm64 全量门禁复验（携带本轮宿主 ir_call/ir_expr 修复+v2 全部改动）。
4. plans/001 §5.9.7 ③级「合并全局可见」条文本轮按宿主事实标准修正（原「仅显式导入模块」条文），specs/08 同步随下轮文档批注轮。

## 五、本轮踩坑（详见 lessons 三条新条目）

1. **静态容器引用实参双侧同构缺陷**（权重 10）：引用语义=传存储位置地址——语义包装/IR 发射/回扫白名单/代码生成每层都要有静态变量分支；验收=「静态容器+局部容器+表达式」三形态探针。
2. **嵌套容器非成熟路径**（15 枚同族二例）：跨组件传结构化数据=标量平铺+边界向量；链式成员两层禁用。
3. **增量构建假象**（假对拍第六案同族）：grep 过滤吞编译失败信号——构建验证必须双查 `错误零命中+Built target` 行。
4. **探针编排三件套**（p28 第四案同族复验）：supply 编译=宿主 cn build --output（副产品 .o 才含实例化方法；v2p 预编译 std 模式不注入）；cn build 无 --output 时 asm=target/主.s 且**覆盖 target/主**——探针 v2p 必须独立拷贝。
5. **受控错误码也是崩**：v2 运行时错误码 3 等经 __cn_runtime_error 优雅退出——gdb 断它而非等 SIGSEGV。

## 六、灰色点披露

- 126 用例 probe 首版 rc=127/4 均为编排错（供给 obj 用错编译器+cn build 覆盖 target/主），非编译器缺陷——修正后 8/8 全绿。
- v1 旧物 CN语言编译器/ 4 组件 cn check 失败=HEAD 既有（上轮已披露），归入 v2 侧一并处理原则不变。

---
写给无上下文的新会话：接手后先读 plans/018（第五节六层序勾选状态+第九节）与本文件第四节，然后从类型/常量全链分桶开始。
