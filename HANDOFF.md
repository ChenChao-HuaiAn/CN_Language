# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十六轮（**深度机 linux-x86_64**）——55-c 方案A 落地：**赋值/传参/返回面混合符号拒绝（用户裁决·Rust E0308 完整对齐）**双侧实施+字面量豁免全链+规范 §3.7 条文升级+E2E 195 双通道+v2self 固定点复锚 **239301 行**+组件对拍 29/29+运行级 3/3。**唯一挂账：E2E 194 测试文件单行修改待用户批准（方案已呈报）。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. 用户裁决第五十五轮挂账 55-c=**方案A**。宿主实施：canConvert 整数族收紧（跨符号同秩/异秩一律拒绝隐式，type_system.cpp）+字面量豁免单点 **canConvertWithLiteral**（semantic.cpp，源/目标均整数族+isIntLiteralExpr[含 -1]→放行）主面全接入：声明初始化/赋值（visitAssignmentExpr）/返回（visitReturnStmt）/传参五形态（semantic_call.cpp 构造/接口/方法/函数逐参/函数指针）+次要面（结构体字段/数组元素）。
2. **重载决议豁免**：conversionLevel/resolveOverload 增 argIsLiteral 参数（semantic.hpp/semantic.cpp/semantic_call.cpp:575/582/808）——`读值(100)` 传 正32 形参按宽化级决议放行；决议后逐参检查 canConvertWithLiteral 同步（防「决议豁免、逐参误报」分裂）。
3. **二元兼容检查豁免**：visitBinaryExpr 比较分支 `大 > 100` 形态（canConvertType 跨符号拒后原报「比较运算操作数类型不兼容」）加字面量豁免守卫。
4. **专用诊断 reportMixedSignAssign**（主三面：声明/赋值/返回）：「混合符号赋值禁止：'X' 与 'Y' —— 须显式转换…字面量豁免（Rust 对齐，2026-09-10 方案A）」与二元面对仗；传参面决议失败报「未找到匹配的函数」（语义等价 Rust E0308）。
5. **v2 实施**：检查混合符号赋值（语义检查表达式.cn 尾部，与 检查混合符号二元 同构——左类型为已知目标类型名ID+右末端 表达式类型ID 推导+静默诊断缓冲+字面量豁免）三面接入 语义检查.cn：声明位（守卫=类型节点.附加==0——指针/引用声明的类型节点文本=基类型名，`正8* q` 按基名误入整数族会误报取地址右值）/赋值位（左值三形态：简单=表查找文本、复合合成标识符=表查找左值末、成员=推导成员类型ID；解引用/下标/引用返回调用左值=检不出诚实边界）/返回位（检查语句 返回分支+「当前函数返回类型」文件级静态——检查函数 设置（节点_函数.附加=返回类型名ID，引用后缀已并入名文本天然安全）/复位 -1）。
6. **取地址右值守卫**：`&x` 右值跳过（取地址产物为指针，检查表达式 对其兜底 整64 与真整64 不可区分——`正8* q = &p.窄` 形态误报实证后加，探针 target/p55x64l/min_assign*/m4/m5 消融全绿）。
7. **规范**：plans/001 §3.7 条文升级「混合符号隐式转换禁止」（二元运算/赋值初始化与赋值/传参与返回三族；字面量豁免；同符号宽化维持；字符↔整数不受影响）+cn-language-spec specs/02 同步（EBNF 行+设计决策条）。
8. **E2E 195/195_v2**：195 宿主负测（期望编译失败.txt 固化「混合符号赋值禁止」，四形态：声明/赋值/传参/返回）+195_v2（v2 负路径 None 通道，3 诊断行+中止行固化——v2 三面）双通道 PASS。
9. **单测改写（随裁决批准）**：test_types_full.cpp ImplicitConversionIntWiden——同秩跨符号 2 断言反转 EXPECT_FALSE+新增异秩跨符号/跨符号窄化/128 特例 6 断言。
10. **豁免缺口修复（首跑 E2E 拦截）**：176 用例 `整64 戊 = 7U;`——声明位字面量豁免原只认无后缀（literalTypeOf==「整32」），带后缀字面量跨符号被拒；扩展=isIntLiteral（旧无后缀窄化）|| canConvertWithLiteral（§3.7「字面量按另一侧类型参与」不分后缀）。
11. 文档：plans/014 状态表第五十六轮行+55-c 行根治记录+更新日志+本 HANDOFF。

## 二、下一轮任务（按序）

1. **194 测试文件修改批准（唯一挂账，最高优先）**：E2E 194_窄宽显式转换锚定/主.cn 第 10 行 `返回 *q;`（读窄(正8* q) -> 整32 函数内）被新规则**正确拒绝**（变量间跨符号返回，Rust 同形态同拒）——用例源码（第五十五轮宽松时代）与新规范冲突。**修改方案已呈报（AskUserQuestion 无答复）：第 10 行改 `返回 整32(*q);`（显式构造转换，运行语义等价，校验和 15/12345 断言不变）**。批准后单行修改→E2E 205 用例全绿。当前口径=**205 用例 200 过/1 败（194 待批）/4 跳**。
2. **v2 传参面补全（另轮）**：调用 AST 实参边界未显式记录（节点_调用 附加=实参数、无逐实参区间）——语义层无法精确对齐形参做混合符号检查。随解析层「实参区间记录」增强一并落地（宿主侧已全形态覆盖含重载决议豁免）。
3. **跨机轮（win/arm64）**：宿主语义层 6 文件+v2 语义 2 文件改动复验；固定点新口径 **239301**（win=246651→待重锚/arm64=322112→待重锚——本轮 v2 源码实质变更）。
4. 观察项延续：窄宽四类型类型码独立拆分（窄宽专项轮）；联合体构造字面量双侧锚定；标量推断 IR 类型面。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 深度机 linux-x86_64（本行口径）
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8   # 零警告
./target/cn_unit_tests          # 单测 1251/1251
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 205 用例 200 过/1 败（194 待批）/4 跳
# v2p 由 E2E v2 闭环步骤重建（target/audit2/v2p_linuxx64=当前源码）
bash target/p55x64l/run_chain56.sh   # 锚定链（[3] 绑定自检 readelf 段有 bug 用 map 手工判定：
#   cn_main=0x4da347 ∈ cn_self56.o .text [0x402586,0x4ddd79)；[6] 运行级三用例口径
#   错误（直接跑编译器），按 target/p50x64l/runlevel50.sh=编译→as→g++ 链接 rt→运行产物）
bash target/p55x64l/cmp29_56.sh      # 组件对拍 29/29
# 固定点 X64L 口径=239301 行（分后端口径不跨后端比较）
```

注：rt objs（target/rt48）src/runtime 零改动可复用；误报消融探针 target/p55x64l/min_assign.cn/min_assign2.cn/min_assign3.cn/m4.cn/m5.cn（全过=声明守卫+取地址守卫正确性）。

## 四、本轮踩坑

- **E2E 首跑两红=呈报「零破坏」审计漏项**：第五十五轮呈报「既有 E2E 全为字面量初始化形态（豁免后零破坏）」——首跑实锤两处漏审：①176 `整64 戊 = 7U;`（带后缀字面量跨符号——豁免缺口=实现缺陷，已修）；②194 `返回 *q;`（解引用变量返回=真跨符号形态——新规则正确拒绝，测试改写待批）。**教训：兼容面审计必须逐用例编译验证而非 grep 形态推断**（grep「正M 变量」搜不到 解引用/带后缀字面量 形态）。
- **宿主编译 v2 产物路径误用**：`cn build 主.cn` 的 asm=target/主.s；target/v2asm.s 是 v2p（自举编译器）的产物路径——误链 2005 字节残留 v2asm.s 得「194_v2 rc=0、195_v2 零诊断」假绿（lessons 第四十三轮旧产物续用同族）。判别：v2asm.s 仅 2KB~7KB 必残缺，正常全树 asm=9.7MB。
- **循环验证漏带平台参数**：v2p 不带 linux-x86_64 第 2 参数默认 win 后端——产物路径不同（v2asm.asm）致后续 as 拿旧 .s——循环脚本每步显式核对产物新鲜度（时间戳/大小）。
- **AskUserQuestion 期望行固化笔误**：195_v2 expected 中止行误加「正确诊断:」前缀（对照 192_v2 范本：前 3 行诊断行有前缀、末行中止行无前缀）——首跑 FAIL 修正。

## 五、诚实边界

- 194 测试文件未改（待批）；E2E 口径含此 1 败如实记录。
- v2 传参面未查（解析层实参边界缺失，另轮）；v2 解引用/下标/引用返回调用左值=检不出（推导兜底面，宿主已全形态覆盖）。
- 组件对拍基线 29；78/79 OOM 为旧架构固有败面（深度机 0 败面）；239301=X64L 新口径（win/arm64 待跨机轮重锚）；探针与脚本留 target/p55x64l/ 不入库。
