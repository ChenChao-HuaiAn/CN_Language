# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（284-a·2026-09-17·T12 根治收尾）

1. **病灶①甄别（诚实边界清偿）**：marker 就位确认（emitStackLoad callerLine 29 调用点+stderr 输出已在库）；反证实验（git apply -R 摘 0a37d31→重建→修前栈帧 80 vs 修后 96=两 __sc$ 槽未登记→槽位移漂移读邻槽）实证两补丁真实有效：275-b（emitCopy 常量源立即数装载）+0a37d31（Alloca 扫描补 Copy 变量名登记）。**0a37d31 提交信息失实披露**：+9 行核心代码搭「🧪 行数上限修正」文档提交入库（登记 279-a「根治收尾 409=409/0/0」的 90f5e7b 实为纯文档提交=虚假收工）——真实记录以 plans/021 284-a 行为准。
2. **病灶②根因（asm+IR 双层实锤）**：优化层 SSA Phi 化把汇合块 Load 降级为前驱块 Copy→汇合块变空块·其条件跳转条件寄存器在块内无定义（旧契约=条件寄生存挂载块尾指令 operands）→codegen 空块防御 mov r10,0→条件恒假→「真&&真 误折假」（mini 探针：O0 对/O3 错·200 缺席+P10=假）。O0 不跑 Phi 化故对；win/linux_x64 双后端同病（T12 立案口径修正——非 linux 单侧）。
3. **根治（方案 A 最彻底面）**：IRBlock.termCondition 字段化——条件值显式存块终止字段，摆脱块尾寄生：①ir.hpp 字段+endBranch 单点写入（删挂载）②三后端 emitTerminator 读侧（linux_x64/x64/arm64）③reg_alloc termUsedRegIds ④DCE 活引用计数 ⑤SSA rewriteUsesInJoinBlock 删 Load 连带改写 ⑥replaceTermCondition 辅助接入 algebraic_simplify/copy_propagation/global_value/cse 四 pass ⑦const_fold 块末条件常量化（保 cross_block_dce 折叠能力）⑧foldConstantBranch 改读字段 ⑨ir_verify 空条件硬错误（防回归）⑩driver dump 条件输出。
4. **win 后端同族潜伏缺陷暴露与窄化根治（121 回归归因）**：全量 E2E 曾现 121（静态容器与分支局部析构）ml64 A2006 [rbp0]——回归归因=契约改动改变优化产物，暴露 win 后端同族潜伏缺陷（优化链残留「引用但 Alloca 已消去」的悬空变量→varSlotOf=0）。根治=win/linux 两侧 registerVarSlot 扫描对 **Load/Store/Copy 三种指令**的变量名引用统一登记（0a37d31 Copy 专用版的窄化推广——**全指令泛化版被否决**：多登记白涨栈帧致 v2p 深递归 SEGV·二分定位后窄化，78/79 自举闭环复绿）——121 复绿输出与 expected 一致。
5. **测试与用例**：3 个旧契约单测同步（test_ir_verify 条件字段+makeBranchBlock 构造）；新 E2E **402_布尔短路矩阵O3**（编译选项.txt=-O3 通道·布尔 &&/||/! 全消费位矩阵·12 行 expected 实跑固化）；撤 [T12marker] stderr 诊断（callerLine 参数保留为防线）。
6. **门禁基础设施顺带根治（ci.ps1 BOM 修复后首次全链真跑·258-a 诚实边界清偿）**：ci.ps1 补 UTF-8 BOM（编码坏致 PowerShell 解析崩·258-a 后门禁全链从未真跑）；暴露并当场清偿三项既有漂移：①check_registry.json 补注册 check_matrix_coverage.py（262-a 深度机建门禁未注册）②coverage_map.md 登记 401/402+20 孤儿用例豁免分组清偿（多机历轮积压）③402 撞号改号（392 被占）。

**验证链**：mini 探针 O0/O3 一致（200+P10=真）；深度机 31 采样 linux 打印序列 31/31+win 运行级 31/31；单测 1351/1351；E2E 420=420/0/0（含 402）；CLI 矩阵 50/50；面覆盖 57/60 PASS；ci.ps1 全链 CI_EXIT=0；双文档门禁+check_progress_sync 过。**运行级 x86_64 终验=深度机 M3 回归轨（预期 7/7→0）**。

**踩坑与教训**：①工作树恢复纪律——反证摘补丁后必须立即恢复（本轮曾运行在摘除态源码上）；②轮次号撞车三连（280/281/282 被深度机连占·收工再撞 283→终改 284-a）；③「提交信息=变更全量声明」失守的代价（0a37d31 隐瞒代码改动→后世甄别成本）；④**增量构建假重建陷阱**——stash 态 build 未删 target/build 致两轮对拍假阴性（假 diff=0 误导二分·强制 rm -rf target/build 全量重建才得铁证）；⑤v2 无此病（v2 条件跳转=指令操作数字段非寄生）=双实现契约对比是寄生契约缺陷的有效探针。

**下一轮候选**：①跨机：深度机 M3 回归轨复验（病灶②根治推送后 7/7→0 预期）→②C20 波2→③D38 负测防线。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 283-a 开发轮收工（看板交锁）——**D25 首步（ir_verify 0%→85.59%+深度机 x86_64 覆盖率基线入库）·全量 420=418/0/2 绿**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **E2E 402 验证IR旗标触达**（编译选项.txt=--验证-ir·runner 261-a 旗标通道零代码改动）：多形态语料（分支/循环/嵌套块/两函数/递归）刺激 CFG/def-use 校验路径——**ir_verify.cpp 行覆盖 0%→85.59%（101/118）·分支 57.41%**（target/coverage/report.json）。
2. **深度机 x86_64 覆盖率口径基线入库**：`check_source_coverage.py --update-baseline` 全链通过——行 80.57%/分支 47.23%（平台键 linux-x86_64/gcc-12.3.0·132 源文件）。
3. 家机 280-a T12 收尾在飞避让（编译器源码零改动·纯语料+基线轮）。

### 二、验证链（本机复现口径）

- `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8` → **420=418/0/2**（+402）
- 单测 1351/1351+CLI 50/50+面覆盖 PASS（57/60）+`check_source_coverage.py` 全链通过+双文档门禁
- 覆盖率读数：target/coverage/report.json（ir_verify 行 85.59%·分支 57.41%）

### 三、下一轮任务（按序）

1. **家机**：280-a T12 根治收尾（在飞·病灶② =采样分歧率 10% 剩余面）。
2. **深度机候选**：①380 升级 stdlib 导入形态（**须呈报用户同意**·测试文件修改）②D25 后续语料轮（ir_verify 错误分支需故障注入通道/ast_printer 14.6%）③T21 标识符字符集（待规范裁决）④280-a 轮次号撞车留痕跟进（家机侧）。
3. **daemon 常态化**：家机 T12 病灶② 根治后回归轨自动复验（分歧率 10%→0 预期）。

### 四、诚实边界

- ir_verify 错误分支（验证失败路径）覆盖率封顶=源码级无非法 IR 注入通道（需编译器内部故障注入设施=后续专项）。
- x86_64 覆盖率基线低于 win 口径=平台差异（arm64 用例在 x86_64 不运行·平台键控不互比）。
- 280-a 轮次号撞车待家机侧改号留痕。

## 单位机 ARM64 节
## 单位机 ARM64 节

**最近交接**：2026-09-16——**257-a 插队修复轮收工：plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**（基线 f0f0696·认领 9818aa8）

### 一、本会话总交付（257-a 一轮）

1. **修复面**：plans/021 §3-B 清空 B5「已裁决原文留档」（裁决/实施记录在 §3-F F1-26 行/§5 索引，指针齐备）；§3-G 同型滞留 T2/T9 迁 §3-C **C17**（案戊五层收口·⏸暂缓语义保留）/ **C18**（整数溢出方案 D·待排班），处置建议段同步。
2. **规则面**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」（用户令落盘·三犯后立）——条目一经用户裁决**当轮迁出**（A/C/§4.2），本区禁留任何已裁决内容含「原文留档」；呈报类条目（含看板通告段发起的）必须同步登记 §3-B。
3. **机械面**：check_handoff.py 扩 §3-B 完成标记拦截（已裁决/已批/已选定/✅）——**反例**（修复前 B5 L80 双标记命中·退出码 1）/ **正例**（修复后 PASS）双向实证；新检查连注记行里「已裁决」字样都自拦截过（措辞改「完成裁决的条目」）=拦截有效实证。
4. **B6 闭环**：用户批复「同意」→当轮迁 §3-A **A12**（里程碑标签锚点建立·tags 强推·待认领）；B 区回空态。

### 二、发现的问题（已处置）

- B5 三犯根因=规则只在 AI 记忆未进 AGENTS.md 明文+check_handoff 无此面检查——双修复完成，lessons 在册（权重 6）。
- 「呈报只挂看板通告段未登记 §3-B」（B6 初始形态）=同族另一缺失面，铁律条文已覆盖。

### 三、下一步

1. **A12 里程碑标签锚点**（B6 已批·待认领·轻量可随任意轮搭车）。
2. 下一轮候选（plans/025 §三 预登记）：F1-26 后续子项（激进 SSA 管线化/mem2reg 全量）/F2-33 拆分之三迭代器/D28/D29 专项。
3. S1 采样轮协议实施（任一台空闲机·验证轮角色·免锁）。

### 四、验证链（本机复现口径）

```
python3 scripts/check_handoff.py    # 含新 §3-B 铁律检查（反例/正例双向实证过）
python3 scripts/check_progress_sync.py
# 源码零改动→无需构建/E2E/锚定链
> 远程推送现状（2026-09-16 实测）：gitcode 正常；github 本机无凭据（沿既有口径待他机代推）。
```

### 五、诚实边界与坑

- 机械检查为字面拦截非语义判定（B 区措辞须避开完成态词——宁可严格，命中即重写）。
- github 镜像本机无凭据未推（170-a 起累积）。
