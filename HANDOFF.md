# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十六轮（**深度系统 x86_64**，`uname -m`=x86_64，门禁=`--target linux-x86_64`）——**R3 剩余超线函数全数拆分 + R4 三后端完成，v2 源码千行纪律全量收口**：全仓库超100行函数 **0 个**、文件全部 ≤1000 行（最大 生成表达式.cn 933）。门禁全绿（单测 1250/1250 + E2E x86_64 176 用例 172 过/0 败/4 跳 + **三后端 asm 字节级 A/B 对拍 6/6 一致**）。**R0-R4 全链至此完成。**

---

## 一、本轮实施（R3 收口 + R4）

### R3 剩余 13 个超线函数拆分（接单位机第三十五轮交接）

| 函数 | 原行数 | 拆分结果 |
|---|---|---|
| 生成语句（IR/生成语句.cn） | 708 | 主干分派 + 同文件 6 辅助（扫描控制流体块/语句块跳过位置/在裸块区间/生成返回语句/生成赋值语句/生成结构体整体赋值）+ **新文件 IR/生成声明.cn**（生成变量声明/生成引用局部/生成容器局部/生成容器拷贝构造/生成结果局部/生成结构体局部 6 函数） |
| 检查表达式（语义/语义检查.cn） | 334 | 主干（字面量/成员/取地址解引用/二元/三元）+ **新文件 语义/语义检查表达式.cn**（检查标识符表达式/检查限定标识符/检查下标表达式/检查调用表达式/检查限定调用 5 函数） |
| 检查语句（语义/语义检查.cn） | 265 | 同文件 3 辅助（检查变量声明语句/检查赋值语句/检查复合赋值操作数） |
| 解析语句（语法/解析语句.cn） | 247 | 同文件 5 辅助（解析如果语句/解析变量声明语句/解析解引用语句/解析自增自减语句/解析表达式兜底语句） |
| 扫描签名文件（IR/IR签名.cn） | 232 | 同文件 4 辅助（收集类型参数序[向量<字符串>& 出参]/跳过泛型自由函数/扫描类体/提取并登记方法签名） |
| 分词（词法/词法分析.cn） | 193 | 同文件 3 辅助（分词字符串/分词数字/分词运算符——输出向量按值传+追加浅共享可见，关键字ID表 先例） |
| 编译文件（主.cn） | 186 | 同文件 5 辅助（加载导入依赖/上溯查找依赖/货舱依赖加载/加载挂载声明/收集文件快照） |
| 主()（主.cn） | 160 | 同文件 3 辅助（解析目标平台/初始化并编译入口/语义检查阶段） |
| 生成函数（IR/生成函数.cn） | 185 | 同文件 2 辅助（登记参数槽/注入主函数静态初始化——指针参数 `槽计数[0]` 解引用同 生成语句 惯例） |
| 解析后缀链（语法/解析表达式.cn） | 147 | 同文件 3 辅助（解析调用后缀/解析成员后缀/解析限定后缀——限定命中=位置+2、不命中=原位，主干以位置变化判延续，零新增出参协议） |

接口约定（延续 R3 已验证手法）：同文件内提取优先，仅文件超千行拆新文件；分支出口 `位置++;继续;` 由主循环统一；块终止经布尔返回值；标量出参一律指针（`整64*` + `[0]` 解引用）或返回值约定，容器出参 `向量&`。新文件 2 个：`IR/生成声明.cn`（407 行）+ `语义/语义检查表达式.cn`（326 行），各自 包.cn 注册模块声明（依赖先解析序）。

### R4 三后端 生成指令 拆编码函数组

- 代码生成.cn（win 314）/ 代码生成ARM64.cn（321）/ 代码生成X64Linux.cn（292）：各拆 **8 个编码函数**——编码数据移动/编码算术/编码比较一元/编码类型转换/编码访存/编码控制流/编码对象调用/编码静态（后缀 Win/ARM64/X64L），返回 `布尔=该操作码是否由本组处理`；生成指令 主干收敛为分派器（~25 行）。**纯机械搬移**（切片+`返回;`→`返回 真;` 替换），零逻辑变化。

## 二、验证（门禁全绿 + 三后端字节级对拍）

- 单测 1250/1250；E2E linux-x86_64 176 用例 172 过/0 败/4 跳（零回归）；v2 闭环 24 用例**每步拆分后快测全绿**；v2p 构建零错误。
- **三后端 asm 字节级 A/B 对拍**（新验证手法）：`git worktree add /tmp/r4base e1f7ee0` 取基线版源码 → 宿主编译器分别构建基线/本轮 v2p → 各生成 127/148/119 三用例 asm → diff：**ARM64 3/3 + win-x64 3/3 逐字节一致**（linux-x86_64 由 E2E 运行级覆盖）。机械拆分的发射级等价性无需等跨机轮即实锤。

## 三、本轮踩坑（已入 lessons.md 权重头部）

1. **git checkout 回滚单文件=丢弃该文件全部未提交轮次修改**（checkout 恢复到 commit 版非「上一步」）——同文件多轮未提交时禁用，回滚前 `git diff --stat` 盘点；内容锚断言脚本可重放性让损失=重跑一个命令。
2. **管道后 `$?` 是最后命令的返回码**——构建 rc 判定必须 `cmd > log 2>&1; rc=$?` 独立执行；且 grep "错误" 会命中警告文本「错误分支」字样，须 `grep -v 警告` 分级。
3. **Python 行切片含头不含尾 vs 行号闭区间混用**——切片函数统一封装（1-based 闭区间），首尾行内容断言强制；拆分脚本四类高频笔误清单化（闭括号 [:-1] 误删/切片含尾/外层变量未随迁/指针参数解引用漏改）。
4. **函数行数统计口径**：注释头与字符串内花括号（`驻留("{")`）都会干扰计数——精确口径=签名行到闭括号+剥注释与字符串。

## 四、下一步（按优先级）

1. **组件对拍 4 入口字符串池编号灰色点专项**（字符串池快照插桩定位 v2p=269/cn_self=271 稳定初值差——未初始化读取嫌疑）。
2. **8 用例运行级对拍矩阵收口**（119/123/126/128/130/148/150/158，含 min1 逐字节）。
3. **跨机轮**：win/arm64 全量运行级复验（本轮 R4 三后端已字节级对拍补强，跨机轮补运行级）；单位机（麒麟 ARM64）同步本轮改动后跑 arm64 门禁。
4. specs/08 目录包/模块声明语义条文批注（顺延挂账）。
5. abi辅助.cn（v1 遗留探针 13 行）转正或删除。
6. v2 结构体构造字面量 `记录{...}` 语法缺口（随语法覆盖轮）。

## 五、验证链（下轮接手先跑）

```bash
uname -m                                    # 环境识别（x86_64=深度机 / aarch64=单位机）
cmake --build target/build -j8 && ./target/cn_unit_tests 2>&1 | tail -1
python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn 2>&1 | tail -3
# v2p 构建（深度机门禁=linux-x86_64；单位机=linux-arm64）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/r3check/v2p_x64
# 单用例调试：python3 tests/e2e/run_e2e.py --target linux-x86_64 --cn target/cn --filter 127
```

写给无上下文的新会话：接手先读本文件第一、二节（R3/R4 完成状态+对拍手法）与第三节（踩坑）。R0-R4 已全部完成，自举主链锚定达成（深度机第三十五轮：默认栈+固定点+不动点）；剩余挂账集中在第四节 1/2（灰色点+矩阵，都是 cn_self 对拍维度）与跨机轮。源码千行纪律已收口——后续改动请保持「函数 ≤100 行、文件 ≤1000 行」（拆分手法见 lessons+本轮脚本模式：内容锚断言+可重放）。
