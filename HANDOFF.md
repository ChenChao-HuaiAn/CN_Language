# HANDOFF 交接文档

**交接时间**: 2026-09-07 第三十轮收官（家机 win-x64）——**跨机轮 win 半程收口，跨机轮（第二十九轮 arm64 + 第三十轮 win）全半程闭环**：拉取 605fc3a（第二十九轮）后按上轮「下一步」执行家机复验，纯验证轮零代码改动，静态论证+家机门禁双确认第二十九轮两修复对 win 零回归。

## 本轮已完成（全部有实证）

### 一、拉取与静态论证（三点确认两修复对 win 零改动）

拉取 gitcode develop：7650713 → 605fc3a（第二十九轮，跨机轮 arm64 侧）。

1. **win_x64 后端目录零改动**：`git diff 7650713 605fc3a -- src/cn_compiler/codegen/win_x64/` 为空——第二十九轮变更面仅 arm64 后端 3 文件 + linux_x64 后端 2 文件 + 单测 2 文件 + run_e2e.py 平台参数化 + v2 CN 源码 9 文件 + 文档。
2. **共享层（ir/driver/frontend/opt）零改动**：宿主 win 链路（词法→语法→语义→IR→win 代码生成→ml64/link）输入完全不变。
3. **两修复本身与 win 无交集**：①linux_x64 `stackParamAnchorBase()`（needHiddenRet?24:16）与原 `16+8*paramOffset` 数学等价（0→16/1→24）=纯死分支加固行为零变化，且仅在 linux_x64 独立目录；②arm64 `.align 3` 不触 win 后端（MASM 无 .data LO12 对齐重定位问题面）。

### 二、家机全量门禁（ci.ps1）：全绿

- **构建零警告**（MSVC /W4 /WX）。
- **单测 1243/1243**（与 arm64 侧一致；第二十九轮新增 3 项 arm64 锚定测为纯代码生成文本断言，win 上同跑同过）。
- **E2E win-x64 全量 160 用例 158 过/2 败/0 跳**——败仅 78_chain_build（工作集 4131MB）/79（4132MB）已知 OOM 家族（基线 4115/4097MB，零回归）。门禁日志 target/dbg/p29_win/（不入库）。

### 三、新增 6 用例 win 侧首验全过（本轮门禁增量面）

- **153/154**（linuxx64_ 参数拷贝临时寄存器/隐藏返回指针栈参数锚定）：win-x64 无平台跳过规则，以普通用例在 win 后端全链编译运行 PASS——运行级实证「win 后端栈参锚定本来就正确」+「linux_x64 收敛对 win 零影响」（第二十二轮 win 缺陷与 arm64 孪生缺陷均已被锚定用例覆盖三平台）。
- **155 容器引用参数与所有权语义**（跨平台）win 首验 PASS。
- **156/157/158**（v2 结构体整体赋值/函数重载消歧/SysV ABI 探针——158 在 win 走 ml64/link v2 闭环原生路径）全过——**v2 闭环 26 例 win 侧动态全绿**。

### 四、跨机轮收官：三平台门禁矩阵

| 平台 | 轮次 | E2E 全量 | 唯一败因 |
|---|---|---|---|
| linux-x86_64（深度机） | 第二十六~二十八轮 | 160 用例 156 过/0 败/4 跳 | 无（4 跳=62/69/78/79） |
| linux-arm64（单位机） | 第二十九轮 | 160 用例 156 过/1 败/3 跳 | 78 已知 OOM |
| win-x64（家机） | 第三十轮（本轮） | 160 用例 158 过/2 败/0 跳 | 78/79 已知 OOM |

三平台唯一失败面收敛于已知 OOM 家族（旧架构固有，v2 重建动机）。第二十九轮两修复经三平台动态验证零回归；v2self 三级锚定（组件对拍 13/13+全链+字节级固定点 g1=g2）已在 arm64 与 x86_64 双架构达成。

### 五、文档收口

plans/014 状态表第三十轮行 + 更新日志 + 本 HANDOFF。零代码改动，无新 lessons 条目（无新踩坑）。

## 下一步

1. **P6b 模块系统**（模块名$ 前缀+纯名调用遮蔽语义对齐）：**新增语法须用户审批后方可启动**——呈报用户裁决后再动工。
2. 远期观察项：宿主-v2 strData 驻留序对齐（终极逐字节对拍专项）；宿主 arm64 .data 浮点常量池（.word/.quad）同位置对齐（随数据段布局专项收口）；宿主-v2 strData 驻留 ID 差 1（产物内自洽零语义影响）。
3. 78/79 已知 OOM：旧架构固有，随 v2 完成自然解决，不做治标修补。

## 灰色点披露（持续有效）

- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（asm 相对 cwd；IR签名 读 stdlib/ 相对路径）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- **strData 驻留 ID 宿主-v2 差 1**（既有观察项，非缺陷）：产物内自洽零语义影响；v2self 家族内部（g1=g2）字节级稳定。
- v2 arm64 后端字符串常量发射为逐常量一条 `.data` 段指令（数百条 .data 伪指令）——GAS 合并无害，属可读性观察项。
- 编排对拍纪律：必须核对 `[p6h] 入口:` 行（路径正确）+ `[p6f]` 回退行不得出现；对拍三件套（脚本+判定工具+基线产物）必须同目录随迁；规范化工具的映射空间必须跨文件确定（内容派生而非位置派生）。
- E2E 用例按字典序运行（`'122_' < '12_string_extra'` 因 '2'<'_'），产物时间戳乱序属正常。

## 踩过的坑（历史条目见 lessons）

- 同族修复旁路穷尽（第二十九轮）：对照点正确≠全路径正确，三后端同 IR 语义的 ABI 实现修复后必须逐锚点/逐寄存器对表穷尽。
- 静态数据对齐=发射期布局契约（第二十九轮）：本地符号引用被 GAS 折叠成 .data+addend，LO12 重定位有对齐硬检查；新数据段发射必须自带对齐指令（LLVM 逐全局 .align 模型）。
- 假对拍五案（cwd 陷阱/判定行自比/入口回退/路径依赖未随迁/规范化基准文件内首次 ID）见 lessons。
- 平台跳过的用例不是不存在的用例——解锁日=欠账日（第二十六轮 lessons 权重 10）。
