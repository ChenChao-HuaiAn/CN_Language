# HANDOFF 交接文档

**交接时间**: 2026-08-15 14:45（第 6 层完成后）

## 我们在做什么任务

CN 语言编译器「模块系统 v2.0 全面改造」共 10 层。当前已完成第 1-6 层：
- 第 1 层：规范更新（specs/08 v2.0 + 09-包与依赖.md 新增）✅
- 第 2 层：词法（ColonColon + 新关键字 模块/作为/包/货舱）✅
- 第 3 层：语法（ImportDecl 结构化 v2.0 + parseModuleDecl + 默认私有）✅
- 第 4 层：crate 重构（module.cpp 分桶 + 目录层级 + 语义分域 + use 导入表 + prelude + 内置 key `::` 化 + IR/codegen 包前缀 + 可见性交集）✅
- 第 5 层：货舱.toml 依赖管理（cargo_parser + CLI + 依赖查找 + 包.cn）✅
- **第 6 层：全量迁移（本次完成）** ✅ —— E2E 36/46 → **46/46 全绿**
- 剩余：第 7 层（新增 v2.0 特性测试 44-51）、第 8-10 层

## 已经完成了什么（第 6 层）

1. **E2E 10 个预期失败全部恢复**：
   - 旧语法 8 个（27/29/30/31/32/35/38/43）：`导入 模块.符号` → `导入 模块::符号`、`从 模块 导入 名1, 名2` → `导入 模块::{名1, 名2}`、限定调用 `模块.函数()` → `模块::函数()`
   - 默认私有 2 个（41/42）：类内补 `公开:` 标签（字段 `私有:` + 方法 `公开:`）
2. **stdlib 10 模块公开性显式化**：算法.cn 补 `公开:`（原无标签）；其余 9 模块顶部已有；头部注释"使用方式"示例统一 v2.0；模块函数体内置调用 `数学.`→`数学::` 等统一
3. **内置调用 `::` 统一**：25_math/33_io/34_file/36_time/37_system/38_tool 全部 `数学.`/`文件.`/`IO.`/`时间.`/`系统.`/`解析.` → `::`（编译器 IR 双映射兼容，统一新语法）
4. **单测迁移**：test_module.cpp 5 处点号限定调用（`数学.平方根` 等）→ `数学::平方根`；保留 `BuiltinQualifiedDotCompat`（专门验证旧点号兼容）
5. **缺陷修复**：入口文件 `主.cn` 辅助函数 crate 前缀缺陷（见下坑）

## 测试结果

- 全量单测：**1097/1097 全绿**
- 编译警告：**0**（MSVC /W4 /WX）
- E2E：**46/46 全绿**（36/46 → 46/46，10 个预期失败全部恢复）

## 总结发现的问题（坑）

1. **入口文件 主.cn 辅助函数 crate 前缀缺陷（LNK2019，本轮暴露）**：
   - 现象：38_tool 迁移后链接失败——17 个辅助函数（统计计算/解析单行/工具主体等）`LNK2019 无法解析的外部符号`。
   - 根因：第 4 层 crate 前缀机制给入口 `主.cn` 的辅助函数定义侧加了 `主$` 前缀（`主$解析单行`），但同文件调用侧按纯名重写（`解析单行`）→ 符号不匹配。**被旧语法编译失败掩盖至今**（38_tool 此前从未进入链接阶段）。
   - 修复：IR 层 `visitFunctionDecl` 加前缀条件补 `node->moduleName != "主"`（与语义层调用侧 semantic.cpp:2613 已有判断对齐）。
   - 教训：**入口文件（主.cn）的函数永不加 crate 前缀**——语义层调用重写按纯名，IR 定义侧须一致。
2. **E2E 迁移不改行为**：迁移 43_module_import 时误把输出标签 `从导入乘` 改成 `花括号导入乘`，与 expected 不匹配（输出不一致 FAIL）。恢复后通过——**只改语法（导入语句/限定调用），不改打印输出**。
3. **内置调用 `::` 统一需编译器支持确认**：IR 层已对 24 个内置做 `::`/`.` 双映射（`数学::平方根` || `数学.平方根` → `__cn_sqrt`），迁移安全。

## 当前卡在哪

无卡点。第 6 层全部完成并推送 gitcode（`cde57c2..ec55818`，33 文件 +300/-275）。

## 下一步计划（第 7 层）

新增 v2.0 特性测试：
- 44_crate_isolate（crate 隔离）/ 45_import_syntax（导入语法全形式）/ 46_module_tree（模块树）/ 47_package_cargo（货舱）/ 48_prelude（核心包 prelude）/ 49_default_private（默认私有）/ 50_class_private（类内私有）/ **51_visibility_intersection（可见性交集规则）**
- 相应单测
- 同时注意 plans 中第 6 层原计划还有"2 个 `函数 使用()` 改名"未执行（若有冲突函数名可随第 7 层处理）

## 踩过的坑绝对不要再踩（已同步 lessons.md）

- **入口文件 主.cn 的辅助函数永不加 crate 前缀**（IR 定义侧与语义调用侧都要跳过 moduleName=="主"）——否则同文件调用纯名 vs 定义带前缀 → LNK2019。
- **不要整体转换 Windows argv 为 UTF-8**——工具链（ml64/link/ml64 汇编）按 ANSI 代码页解释路径；只在 driver 层转 UTF-8。
- **依赖/、stdlib/ 目录内的文件必须视为外部 crate 模块**，否则与入口 crate 模块树混淆。
- **编译验证必须用 VS 自带 cmake 完整路径**（系统 PATH 无 cmake）：`C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`
- **E2E 迁移只改语法不改行为**——expected 文件是契约，打印标签不能改。
