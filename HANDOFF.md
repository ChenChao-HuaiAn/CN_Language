# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十三轮（深度系统 x86_64）——**P6b 类型/常量 IR 布局全链分桶**：布局表/常量表键基化+三级布局查询+限定类型语法（甲::记录）+类型引用三级解析与诊断 fail fast 落地；v2self 三级锚定（组件对拍 13/13+字节级固定点）全绿；E2E 172 落库。**下一步 = 千行组件拆分（四文件超线，下轮首务）+ 跨机轮复验**。

---

## 一、本轮核心实施（工作流 3 第 4 层诚实边界收口）

上轮挂账「结构体/常量 IR 布局仍纯名——跨模块同名结构体布局混用=旧状」本轮根治。**红基线探针实证**（/tmp/p33probe/t1，不入库）：

| 断言 | v2 修复前 | v2 修复后 | 宿主 |
|---|---|---|---|
| 甲版本（跨模块同名常量） | **200（串桶）** | 100 | 100 |
| 甲端到端（16B 结构体跨函数传返） | **281458003872474（ABI 错乱）** | 141 | 141 |
| 主数量/主版本 | 7/200 | 7/200 | 7/200 |

1. **布局表键基化**（IR容器.cn）：结构体大小表/对齐表/字段类型表/字段偏移表们/容器字段表们/结构体容器字段数/常量表 键全部升级「键基」（键基合成(当前模块路径,名)——主模块=纯名，单文件形态逐字节不变）。登记结构体布局 加公开位参数+纯名候选平铺（③ 数据源：候选键基/公开/计数三表）。
2. **三级布局查询**：`结构体布局键基()`——限定文本（甲::记录）拆分直查＞①本模块键基＞②直查（主模块公开设防+**键基幂等直通**：候选数=0 判键基形态）＞③纯名候选唯一公开（多候选防御首公开，语义层诊断兜底）。**查询函数本体升级**：查结构体大小 20+ 消费点零改动自动三级化；查字段偏移/查字段类型ID 改两参签名（结构体维度+字段名，内部幂等解析），IR生成 10 处+语义分析 2 处机械替换；容器字段数/取容器字段们 内部幂等。
3. **常量分桶**：收集常量 加模块路径参数（IR生成多文件 按文件传，单文件="主"）；登记键=键基+候选平铺；IR生成 消费点改 `查常量值()` 三级（限定引用 甲::版本 直查）；常量表清空点同步清候选表。
4. **当前模块路径单一归属**（IR容器 静态+设值/取值函数）：语法解析窗口（主.cn 编译文件 解析程序 前设值——依赖先解析递归返回后父文件重设，无窗口交错）与 IR 生成窗口（生成函数 开头，**原 IR生成 本地静态 当前IR模块路径 归入**防双静态漂移）共用；回退内置链/自检链复位"主"。
5. **限定类型语法**（宿主 E2E 56 甲::记录 对齐——本轮探针实证 v2 此前完全不支持）：解析类型文本 支持 `标识符(:: 标识符)+` 合成限定文本（多段并入；泛型实参位同样生效——向量<甲::记录> 合成 向量$甲::记录，$ 切分不受影响）；解析语句 变量声明检测加「标识符 :: 标识符 标识符」三标识符消歧分支。
6. **类型引用三级解析与诊断**（语义层 fail fast，宿主 A-2/E0659 对齐）：`类型键基查找()`（①本模块成员索引＞②文件导入键基＞③合并全局公开候选——**③ 段不做函数签名过滤**，与 三级键基查找 的语义差异点=类型键基不在签名表）+`检查类型引用()`（剥尾部 &/* 后缀[E2E 150 误报根治]→基础类型清单→$ 合成名放行→限定直查/纯名三级）；三调用点=变量声明/参数/返回类型（主流程 函数声明处）；未声明/私有/歧义三维度诊断，负测探针验证（不存在→未声明的类型；甲::秘密 私有→类型 '秘密' 是私有的（模块 甲））。
7. **节点公开位**：解析结构体声明/解析常量声明 加是否公开参数（解析程序 顶层可见性标签传入）；常量声明节点附加位（原 0 空闲）写公开位——收集常量 候选登记/语义可见性同源。
8. **E2E 172_v2_类型常量分桶**（v2 闭环）：甲 16B 记录（整64 标识;整64 数值）vs 主 4B 记录（整32 数量）布局独立+同名常量 100/200+限定类型声明 甲::记录+跨模块结构体实参——五断言 rc 校验（100+141+200+7+141-589=0）；run_e2e.py v2闭环用例们 注册。

## 二、验证证据（全绿，严格串行）

- 单测 **1249/1249**；E2E linux-x86_64 全量 **174 用例 170 过/0 败/4 跳**（含新 172，基线一致零回归）。
- **13 组件冒烟** 13/13（v2self 18 万行源码全量过类型诊断零误报）。
- **v2self 三级锚定**：组件对拍 13/13（v2p_gen33c vs v2self_gen33 产物规范化逐字节一致，norm.py 三件套复用）+**字节级固定点**（fix_p=v2p 编译主链产物 ≡ fix_s=v2self 编译主链产物）。
- 探针（类型分桶+限定类型）v2 与宿主输出逐行一致；类型诊断负测两形态全拒。

## 三、本轮踩坑（详见 lessons 三条新条目+一条呈报）

1. **布局表纯名平铺串桶**（权重 10）：函数分桶轮只改了符号面，数据面六张表漏网——「键域迁移」必须盘点全部以名为键的表；布局串桶可观测形态是 **ABI 变形**（sret↔标量）而非仅数值错；跨模块同名探针必须含跨函数结构体传返全链（单函数内偏移自洽=侥幸假绿）。
2. **类型文本后缀误报**（权重 7）：新诊断的放行清单必须枚举文本全部合成形态（剥后缀→基础名→$ 合成名→限定名）；新诊断上线=全量 E2E 是验收门（150 即全量兜出）。
3. **并行任务共享中间产物互踩**（权重 8，假对拍第六案同族）：宿主 E2E 全量与组件对拍并行后台跑，两者都在项目根写 target/v2asm.s——E2E 126「未生成」+对拍 代码生成 产物 1443 行（被调包）两失败同根；**v2 编排产物路径在同一工作区是全局单例，任何两个涉及 v2 编译的任务禁止并行**；对拍产物行数量级突变=「产物被调包」指纹（编译日志正常+产物异常→查写者）。串行复跑全部转绿。
4. **v2 结构体构造字面量语法缺口呈报**（挂账）：`记录 r = 记录{...}`（宿主 E2E 56 形态）v2 解析失败——仅支持 声明+逐字段赋值（148 模式）。修复=语法构造分支+IR 聚合初始化，随语法覆盖轮排期（自举刚需面外）。

## 四、灰色点披露

- E2E 172 首版断言算术错误（甲求和(42,99)=141 误当 42，rc=99 暴露）——断言常数修正 589，测试文件修改属用例创建轮内修正（尚未验收基线），不违反测试文件不可擅自修改纪律。
- 查字段宽度码 内部经 查字段类型ID（键基入再幂等解析一次）——多一次 O(1) 映射查询，性能无损但链路略绕（收口换简洁，符合单一归属）。
- 拆限定名 对多段 a::b::c 取 a::b+c（宿主尾段绑定同构）——类型限定文本多段形态当前无用例（工具库::子::类型），三级查询按同构处理未单独锚定。

## 五、诚实边界

1. **千行拆分继续挂账**（下轮首务）：IR生成.cn 3182/语义分析.cn 1607/语法分析.cn 1468/IR容器.cn 1299 四文件超线。第三十二轮「随类型分桶轮一并」的裁决在类型分桶落地后复核：拆分=纯机械搬移但组件清单/v2self 锚定基线全变，与本轮大改叠加放大回归面——独立成轮。
2. **跨桶重载部分支持**（③同名不同签名按实参数选桶）维持上轮状态，未动。
3. **限定私有诊断双报**（标识符分支+调用分支）维持上轮状态，未动。
4. **win-x64/arm64 复验=跨机轮**（本轮改动全在共享层 v2 CN 源码）。
5. 宿主 `类型大小(记录)` 内置对跨模块同名类型解析到先注册模块（探针实测主记录大小=16 而非 4）——宿主自身缺口，E2E 56 锚定形态（构造/字段/常量隔离）不含类型大小内置；v2 修复后行为=①本模块键基命中（正确），**双侧形态差异**记录备查，随宿主类型大小解析轮对齐。

## 六、下一步（优先序）

1. **千行组件拆分**：IR生成（3182）拆 生成表达式/生成函数与收集/入口；语义分析（1607）、语法分析（1468）、IR容器（1299）按职责拆子目录；拆后组件清单更新冒烟/对拍/supply 链基线。
2. **跨机轮**：win-x64/arm64 全量门禁复验（携带本轮 v2 全部改动；宿主 C++ 本轮零改动）。
3. **v2 结构体构造字面量**语法覆盖（呈报挂账）。
4. specs/08 同步：类型/常量分桶+限定类型语法条文批注（随文档批注轮）。

---
写给无上下文的新会话：接手后先读 plans/018（第五节第 4 层类型分桶注记）与本文件第五/六节，然后从千行组件拆分开始。探针红/绿基线配方见 lessons 第三十三轮条目与 /tmp/p33probe（临时，不入库）。
