# HANDOFF 交接文档

**交接时间**: 2026-09-12 第七十七轮（**深度机 linux-x86_64**）——**77-a 借出视图生命周期检查**
（plans/020 矩阵 A21 格靶子「借出视图 × 容器移除（NLL/借用检查轮）」销项；宿主 + v2 两侧落地）。
验证链全绿，固定点新口径 **278677**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 下一轮首选靶子「A21 借出视图 × 容器移除」。用户令：遵守 AGENTS.md 第3项
   纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **缺陷实证**（探针 77/77-2，宿主侧，`/tmp/p77probe` 不入库）——借出视图 = 字符串元素容器的
   `元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取` 返回值（容器内元素句柄的**浅拷**，
   与容器槽共享所有权）。七形态内容损坏（校验位 0 = UAF）：
   - ①向量 元素×删除 ②向量 读取×设置覆盖 ③栈顶×弹出 ④队首×出队 ⑤链表 头部元素×删除头部
   ⑥映射 获取×删除（同作用域失效）；⑦**跨作用域（容器先亡）**——块内容器析构释放元素后，
   外层借出视图仍用。
   - 对照（正确行为）：借出变量在块出口未被误判拥有（校验位 1）；借出→使用→再修改（顺序正确）
     校验位 1。

3. **三方案裁决（性能第一 / 安全第二）**：
   - **甲（实施）编译期「借出视图生命周期检查」**——语义层来源登记 + **NLL 顺序近似**（引用活跃
     区间 = 绑定 → 最后一次使用）；**零运行时开销**（生成物零变化、纯编译期），Rust 借用检查器
     E0502/E0597 语义同构。
   - 乙 借出方法返回拥有副本：每次借出堆分配+拷贝（违背性能第一）+ 破坏容器视图语义 → 落选。
   - 丙 视图类型（指针+世代）+ 运行时检查：8→16 字节新类型（ABI 全链改动）+ 每次访问比较 →
     落选（登记为阶段 4' 拥有型字符串的潜在扩展）。

4. **检查器（两侧同构）**：
   - 借出方法 8 个（元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取）；失效方法 11 个按容器头
     分派（向量 删除/设置；集合 删除；链表 删除头部/删除尾部/清空；队列 出队/清空；栈 弹出；
     映射 删除/设置/清空/释放内部数组）。
   - **不在面内（语义判定）**：扩容类（追加/插入/压入/入队/头部追加/添加——字符串是独立堆块，
     槽位搬移不使句柄失效）；向量 清空（仅计数归零）；值语义元素容器（元素() 返回深拷贝）。
   - 判据：失效点落在（绑定，最后一次使用）之间 → 错误；使用发生在容器已出作用域 → 错误。
   - **包裹形态防护**：`字符串复制(表.元素(0))` 不登记（绑定位须「顶层表达式即该借出调用」——
     宿主按 AST 节点指针比对、v2 按后缀序末节点解剖；探针 77 形态九初版误报实证后修正）。

5. **实施**：
   - **宿主**：新文件 `src/cn_compiler/semantic/semantic_borrowview.cpp`；`semantic.hpp` 状态与接口；
     六处挂点（`visitCallExpr` 方法分支 / `visitVarDecl` 初始化位 / `visitAssignmentExpr` 赋值位含
     复合赋值 / `visitIdentifierExpr` 读值根拦截点 / `checkFunctionBody` + `checkClassMethods` 结算，
     方法体用保存-恢复保证嵌套安全）；变量身份 ID（`declareVar` 分配 + 作用域链解析，同名遮蔽防
     误配容器）；`types::isStringElemContainer/isStringValuedMap` 上提共享（IR 层
     `IRGenerator::*` 改委托调用 = 单一事实源）。
   - **v2**：新模块 `CN语言编译器v2/语义/借出检查.cn`（预扫描块区间+声明表 → 主扫描三类点 → 结算
     两形态；**节点索引序代替源码行序**——v2 节点无行号）；`语义/包.cn` 加 `模块 借出检查;`；
     `语义/语义检查.cn` 加导入 + `检查函数` 尾部挂点。

6. **防线（6 个 E2E + 1 单测文件）**：
   - E2E **223 / 223_v2**（同作用域失效六形态负测；v2 侧 6 诊断+中止行固化）；
   - E2E **224 / 224_v2**（容器先亡三形态负测）；
   - E2E **225**（合法九形态正测，校验和 511，含「借出未被误判拥有」）；
   - E2E **226_双编译对照借出视图合法形态**（移植纪律 9 **第四个**对照用例，八形态两侧退出码
     一致 255）；
   - 单测 `tests/unit/semantic/test_borrowview_check.cpp` **13/13**（负 5 + 正 8）。

7. **反证实证（门禁有效性）**：宿主注入「借出方法识别恒假」→ E2E 223 失败；v2 注入
   「是借出方法名 恒假」→ E2E 223_v2 失败。两侧注入已恢复并复验（残留检测=0）。

8. **全树零存量违规**：作用域感知审计（v2/v1/stdlib/tests）零真命中；宿主编译 v2 树零诊断；
   v2p 编译 v2 树零诊断 → **检查器落地零迁移成本**。

9. **文档五件套**：plans/020（A21 格改「靶子已销项」+ 第八节第七十七轮更新 + 防线表）+
   plans/019（跟踪表行）+ lessons（第七十七轮段，权重 8，含四条新预防）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子（余项）**：① 数组元素（`字符串 数组[N]`）借用面；② 装箱字段接收
   （`r.值` 读出）无 RAII；③ 结构体字段持串无 RAII；④ 结构体容器字段「声明即用」既有边界；
   ⑤ 集合<有析构类> 删除前移的旧对象析构（既有边界）；⑥ 字符串键（`映射<字符串,...>`）支持轮。
2. **77-a 诚实边界收口候选**：借出视图**跨函数别名**（借出经实参传入函数、被调函数内修改容器）
   ——Rust 靠生命周期参数检查；CN 若做需函数级借用协议（跨函数数据流，工作量级较大，建议先
   登记讨论）；接收者为**成员链**的借出登记（`程序.函数们.元素(i)`——第一版只覆盖标识符接收者）。
3. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区变硬错误）。
4. **跨机轮**：arm64（单位机麒麟）复验本轮的借出检查与 6 个新用例（linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告
./target/cn_unit_tests          # 单测 1302/1302（1289+13 本轮新增）
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 251 用例 247 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 278677 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 30/30（DIFF=0）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时
> 删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p（验证假绿）。

## 四、本轮踩坑（已入 lessons 第七十七轮段）

- **NLL「活跃区间=到最后一次使用」是零误报的关键**：保守到作用域末会误报全树大量合法形态
  （结果变量消费后再修改容器、循环内每轮重借）；判据设计必须与「全树当语料」的误报实测一起做。
- **包裹形态必须按 AST 节点身份判定**：`字符串复制(表.元素(0))` 的内层借出调用会把「最近一次是
  借出」标记残留到外层（宿主初版误报实证）——绑定位须要求「顶层表达式就是该调用」。
- **v2 侧字段语义先小样实测再写**：赋值节点 `文本`=左值名ID、`子起点`=右值区间起点（初版按
  `子起点` 取左值节点 → 跨作用域形态**漏检**；实测修正后三形态全拦截）。
- **两侧对齐的成本分布**：宿主（C++）零误报一次通过；v2 侧三轮修正（字符串拼接 `+` 漏写、
  赋值节点字段语义、节点索引序近似成立性）——v2 首版即全绿不现实，预留实测迭代余量。
- **「新增防线」的验证口径 = 负测（能拦）+ 正测（不多拦）+ 反证（门禁有效）+ 全树零存量违规**，
  四者缺一不可。

## 五、诚实边界

- **跨函数别名检不出**：借出视图经实参传入函数、由被调函数修改容器（Rust 靠生命周期参数；
  CN 第一版不覆盖）。
- 接收者为**成员链**（`程序.函数们.元素(i)`）或指针形态不登记（第一版只覆盖标识符接收者 =
  局部/参数/字段直名）。
- v2 侧**类方法体**不经借出检查（与 v2 既有语义检查面同口径；宿主侧经 `checkClassMethods` 覆盖）。
- 值语义元素容器（`向量<整64>`/`向量<结构体>`）不在面内（元素() 返回深拷贝，非借用）；
  向量「清空」不在失效面（仅计数归零不释放，泄漏面另计）。
- 本轮为**新增防线**（存量代码零违规），非既有缺陷修复——A21 的存量违规形态（v1 组件 IR4.cn
  先读后弹）已在 76-a 修复。
- win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **278677 行**、cn_self71）。
- 探针（/tmp/p77probe，不入库）：probe.cn（同作用域六形态+对照）、probe2.cn（跨作用域形态G +
  借出未被误判拥有四形态）、mini.cn、v2test/（v2 侧最小负测）、bv.bak/bv2.bak（反证注入前的两侧副本）。
- E2E 用例：
  - `tests/e2e/223_借出视图失效拒绝/`（宿主负测，锚「容器失效点」）+ `223_v2_.../`（v2 负测）；
  - `tests/e2e/224_借出视图容器先亡拒绝/`（宿主负测，锚「容器作用域结束后」）+ `224_v2_.../`；
  - `tests/e2e/225_借出视图生命周期合法形态/`（宿主正测 511）；
  - `tests/e2e/226_双编译对照借出视图合法形态/`（两侧 255 一致；配置 `双编译对照.txt`）。
- 单测：`tests/unit/semantic/test_borrowview_check.cpp`（13 例）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——A21 格改「靶子已销项」；
  每轮触碰所有权语义必须同步更新）。
