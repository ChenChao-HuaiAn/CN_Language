# HANDOFF 交接文档

**交接时间**: 2026-09-03 第四轮（家机 win-x64）——**单位机交接全部收口**：缺陷② v2 侧四件
+ 132 转绿 + 宿主单测 11 项 + 缺陷 A/B 双侧根治 + 验证期新发现 v2 结构性缺陷「检查块
裸节点双重处理」当场根治（用户令「任何缺陷及时完善/参考 Rust/隐患拦在编译期」全程执行）。

## 本轮裁决依据（用户令）

- 单位机裁决「②立案+③顺带+①不动」继续有效（Rust E0070 权威锚定）。
- 家机令（2026-09-03）：拉取后续推；**任何发现的缺陷及时完善、多参考 Rust 同类操作、
  错误边界全去除、安全隐患拦在编译期**——本轮三簇缺陷（裸节点双重处理/A/B）均按此令
  当场立案根治，无搁置。

## 本轮已完成（全部有实证）

### 一、缺陷② v2 侧四件（CN语言编译器v2/语义分析.cn）+ 132 全链绿
1. `表查找种类`（作用域栈内→外，-1 未找到；与 表查找类型ID 同序=遮蔽语义一致）；
2. 节点_赋值 标识符左值（文本!=0）+ 复合标识符形态（左值末=标识符）：`种类==符号_常量`
   →「不能给常量赋值（常量初始化后不可修改）」；
3. 节点_赋值 非标识符左值（文本==0，语法分析.cn 兜底分支任意表达式+`=` 都造赋值节点=
   主战场）：左值末（=池.元素(子起点-1)）白名单 {标识符（复合合成）/成员/解引用/
   下标（既有指针白名单）}——其余（二元/字面量/负号/取地址/调用/**空左值粘连形态**）
   硬拒「赋值目标必须是可写左值（变量/成员/下标/解引用）」；
4. 节点_自增 常量拒；v2 主.cn 尾部 源码6（a+1=7）/源码7（常量赋值）两负测自检。
   **E2E 132 六步闭环 PASS（退出码 0）**，expected 三行（错误数=0+两自检行）全命中。

### 二、【新缺陷】v2 检查块 裸节点双重处理 当场根治（lessons 11.0 权重成文）
- **现象**：132 首跑 `未声明标识符: 指针`（stash 对照=既有缺陷非本轮回归）。
- **根因**：块区间线性含各语句 RHS 裸节点，原逐节点派发使裸 取地址 等在**声明根注册前**
  先查操作数——自引用（`&p.x` ⋈ `*指针` 粘成 `&(p.x*指针)`）误报未声明；前向引用
  `&q` 同错**双诊断**（p16 探针两次实测）。与 ③ 第二波「父块双重处理嵌套块」同族。
- **根治**：检查块 主循环仅派发语句根节点类型（裸节点由所属语句根区间统一检查，每节点
  恰在其作用域访问一次）；检查语句 一元分支死代码删除；解析器裸 `*表达式` 语句补
  节点_表达式语句 根（原 IR 零分支=静默死代码）；解析块 静默中断 stderr 可见化。
- **探针实证**：p1/p5 粘连恰一条正确诊断（非左值拒）；p13/p14 干净形态 0 错；p16 单诊断
  （原双报）；p17 裸 `*未声明x` 仍拒；p18 `*p += 1` 静默截断变可见 `[parse]` 诊断。

### 三、缺陷 A/B（单位机遗留）双侧根治（win 实证=公共 IR 层非 ARM64 专属）
- **A** `p.y--` 成员后缀自减存储无效：ir_expr.cpp Increment/Decrement 写回仅
  IdentifierExpr——成员/下标/解引用只读不写。修复：lvalueAddress+Add/Sub+StorePtr 写回
  （宽度按操作数类型）。probeA `p.y--`→**2**；probeC 全形态 `--p.y`/`p.x++`/`数组[1]++`/
  `(*q)++`/`--(*q)` = **2/11/6/9/8** 全对。
- **B** `数组[2] += 数组[1]` 垃圾值：ir_stmt.cpp 无初始化器补零仅结构体分支，数组漏。
  修复：registerVarSlots 登记槽数逐槽 Store 0。probeB 输出 **5** 正确。
- **注意**：A/B 修复在公共 IR 层（双后端同享）；win-x64 全量实证，**ARM64 侧待单位机
  全量复验**（修复非后端专属，风险低）。

### 四、宿主单测 + 门禁
- tests/unit/semantic/test_lvalue_check.cpp（**11 项**）：九负形态精确消息（cn check 实测
  锚定）+ 跨行「想写分号」提示串 + 合法全形态零误伤；注册 tests/unit/CMakeLists.txt。
- 单测 **1207/1207**（原 1196+新增 11）；构建零警告。
- 全量 E2E（win-x64）：**134 用例 132 过/2 败（78/79 已知遗留内存超限，与上轮一致）
  零回归**——131 负测+132 v2 闭环双绿。

### 五、132 源码修正说明（测试文件改动披露）
- `tests/e2e/132_.../主.cn` 第 30 行 `&p.x` 后补分号+注释：换行≡空格规范下
  `&p.x` ⋈ `*指针 = 100` 粘连成 `&(p.x * 指针)`+空左值 `= 100`——源码意图（解引用
  赋值语句）与解析语义不符=**源码语法错误**（AGENTS.md 允许直接修正的类别），非断言
  语义改动。131 同族形态（粘连）在 131 用例中由编译器正确拒绝+提示，规范行为未动。

## 下一步（接手会话按序执行）
1. **ARM64 侧全量复验**（单位机）：缺陷 A/B 修复虽在公共 IR 层，仍须 linux-arm64 全量
   E2E 复验（上轮教训：单侧绿≠双侧对称）+ 新增 [parse] 诊断行不破坏 ARM64 用例
   （.expected 为子串匹配，额外行无害，理论无影响）。
2. 下一自举缺口按 plans/014 序列推进（IO::/文件:: 限定名内置或用户裁决项）。
3. v2 已知边界（沿用，未立案）：`*p += 1` 解引用复合赋值解析不支持（现 stderr 可见
   截断诊断，非静默）；v2 整8/整16 强转白名单未覆盖；v2p 语义错误后仍继续 IR 生成。

## 灰色点披露（待用户裁，未立案）
- v2 解析块 中断诊断走 stderr 而非诊断列表（v2 语义诊断通道是 向量<字符串>，解析层
  无通道）——中断信息不进 err 计数。若要求「解析失败=编译失败」需解析层诊断通道改造。
- 宿主自增自减作为**表达式值**时前后缀均返回新值（C 应为后缀返回旧值）——语句级用法
  （绝大多数）无影响，表达式级语义为既有行为，未动。

## 踩过的坑（本轮新增，已入 lessons 高权重）
- **换行≡空格粘连一日三踩**（`&v`⋈`(*q)++`/`100`⋈`(`/`3`⋈`--`）——探针与测试源
  **全语句加分号**；测试源先 `cn check` 预期零错再进断言（防把测试源自身 bug 当编译器
  误伤）。lessons 5.0 权重成文。
- 裸 `*表达式` 语句曾无语句根（IR 静默死代码+语义裸派发兜底互为因果）——新解析分支
  收尾必问「这条路径的节点有没有根」。
- `e2e_full_result.txt` 是 8 月旧文件勿当本轮结果（本轮结果以 ci/命令输出为准）。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（build.ps1 Debug）零警告零错误；单测 **1207/1207**；
- E2E 132 六步闭环 PASS；探针 A/B/C 全对；
- 全量 E2E（win-x64，与 ci.ps1 三步等价：build.ps1 零警告 + 单测 + run_e2e 全量）：
  **134 用例 132 过 / 2 败（78/79 已知遗留内存超限，与上轮完全一致）零回归**；
  131 负测 + 132 v2 闭环双绿；新增 [parse] 诊断行无副作用（.expected 子串匹配）。
