# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-12 深夜——**第八十六~八十八轮跨机验证轮**（拉取 9403e29=深度机 86-a/
87-a/88-a，基线 efc0c8c）。本轮为**纯验证轮（零源码改动）**，含一个新缺陷的完整诊断链（待修）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **背景**：上一会话（三机协议落地）登记的「88-a win 全量门禁」并行会话在 E2E 阶段被中断
   （`target/e2e_88a_win.log` 0 字节、E2E 产物停在 23:38）；本会话接手重跑。
   构建（零警告）+ 单测 1306/1306 由前序会话完成并留日志（`target/gate_88a_win.log`）——
   源码等价（efc0c8c 为纯文档提交），故直接复用 `target/Debug/cn.exe` 重跑 E2E。
2. **验证链（全量）**：
   - **E2E 275 用例 255 过 / 20 败 / 0 跳**（日志 `target/e2e_88a_win_rerun.log`）。
     败=17 双编译对照（win 平台限制：13 既存 + 新增 243/244/246/248）+ 78/79 OOM（win 固有
     败面）+ **148_v2 崩溃（新缺陷，见二）**。
   - **锚定链复锚**：`python target/p46win/self_chain.py` → fix_p ≡ fix_s **348908 行**
     逐字节一致（新口径；55 轮 340703 → +8205=56~88-a v2 源码）+ 绑定自检
     cn_main∈cn_self46.obj ✓。
   - **运行级 3/3**（`target/p46win/run_level.py`）+ **组件对拍 34/34**
     （`target/p46win/comp_diff.py`，DIFF=0）。
   - **win 实物复验（本轮新增）**：`target/p89win/run89.py` 把 4 个双编译对照用例
     （243/244/246/248，win runner 平台守卫跳过）在 win 上手工两侧对照（宿主 build +
     v2p 编译→ml64→link），**全部 rc=255 两侧一致**——86-a 三元聚合 / 86-a 嵌套深拷 /
     87-a 静态变量全链 / 88-a 短路全链的 win 实物首次验证。

### 二、新缺陷：148_v2 win 侧崩溃（0xC0000005，待修）

**现象**：E2E 148_v2_函数返回结构体 的 v2 产物运行退出码=3221225477（0xC0000005），期望 0；
55 轮该用例通过 → **回归**。

**诊断链（本轮完成，可复现）**：
1. **复现**：`target/p89win/repro148.py`（v2p 编译→ml64→link→运行，稳定 0xC0000005）。
2. **宿主维度 bisect**（手法：4 个不同宿主编译**同一份 55 轮 v2 树**；见
   `target/p89win/try_host.py` + worktree `target/wt_base|wt86|wt87`）：
   55 轮宿主 ✓ 正常 / **86-a 宿主 ✓ 正常** / **87-a 宿主 ✗ 崩** / 当前宿主 ✗ 崩
   → **87-a 的宿主改动引入**（与 v2 源码树无关——四组同树）。
3. **最小复现**：`target/p89win/min3/主.cn`（结构体按值实参 × 嵌套调用
   `取和(造点(30, 40))`）→ cn_main 帧 **384（正常）/368（崩版）**（min/min2 不触发）。
4. **机制**：崩版该调用点**缺 retbuf 隐藏参数**——正常版 `lea rcx,[rbp-384]`（retbuf）+
   返回后 rep movsb 物化拷贝；崩版 `mov rcx,30`（实参直传）→ 被调方（造点）把实参 30
   当 retbuf 指针写入 → **0xC0000005**。
5. **深层特征（重要）**：v2 编译器的「返回物化判定」**对内存状态敏感**——同一个 v2p
   （55 轮宿主构建）在「仅源」（E2E 调用方式）与「源+平台」两种命令行参数下判定**翻转**
   （384↔368）；87-a 的静态初始化改动（emitStaticInitsAtEntry/ALIGN 8/初值直存）改变了
   win 侧 v2p 的内存布局 → 在「仅源」下稳定进入「无物化」分支。
6. **平台分叉**：linux 侧不触发（深度机 E2E 271 过含 148_v2）——**win-x64 特有的跨机轮
   拦截成果**。

**未确证（留开发轮）**：具体「未初始化读取/脆弱判定」点在 v2 源码何处（本轮验证轮零源码
改动）。**已排除**：静态布尔初值错写（两版 .data 均为 0）、诊断开关误置（两者均未输出
[ir1]/[cg1]）、查寄存器类型未登记返回 0（实现安全）、.data 字符串初值伪指针（87-a 后恒
dq 0，且 55 轮旧形态 dq "主" 反而正常）。

**三方案（呈报，待用户裁决）**：
- **甲（推荐）**：根除「内存状态依赖」——定位并修复 v2 编译器未初始化读取/脆弱判定
  （Rust 对照：rustc 确定性编译 + Miri 未初始化检测；性能零开销）。
- **乙**：确定性门禁强化——同输入重复编译逐字节一致 + 参数模式交叉比对（仅防线，不修复）。
- **丙**：三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

### 三、下一步（新会话按序）

1. **缺陷修复（需开发轮看板认领）**：按用户裁决方案执行；入口=min3 最小复现 +
   `target/p89win/` 探针脚本（不入库）；`target/wt_base|wt86|wt87`（三个宿主 worktree）
   可用于继续 bisect 到 87-a 的具体代码块。
2. 单位机 arm64 侧对 86~88-a 的跨机复验（arm64 尚未拉取本轮）。

### 四、本轮踩过的坑

- **worktree 构建需 googletest junction**：`target/wt*` 的 CMake 回退路径是
  `<worktree>/target/build/_deps/googletest-src/googletest`——建 junction 指向
  `C:/Users/ChenChao/Documents/third-party/unittest/googletest` 即可（`mklink /J`，
  用 python subprocess 调 cmd 避免 Git Bash 转义）。
- **`build` 子命令产物名**：`--output <路径>`（不带扩展）产 `<路径>`（无扩展可执行）+
  `<路径>.obj`；Windows 下执行前须改名 .exe。
- **ml64/link 直调在 Git Bash 被 `/nologo` 路径转换坑**：一律用 python subprocess 调用
  （不要经 bash 拼接命令）。
- **v2p 的 `--verbose` 需第 3 参数**（`<源> <平台> --verbose`）；「仅源」与「源+平台」
  会改变 v2p 行为（本轮缺陷的关键观察面）。
- **E2E 结果读取**：runner 输出全缓冲（重定向时中途 0 字节属正常），用产物时间戳/进程判活。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第一百零二轮（**深度机 linux-x86_64**）——**102-a 多文件静态初始化注入
（C4）销项 + 宿主缺陷 H5 根治**（类/容器静态有初值路径缺 StorePtr→空指针崩溃）。上轮 101-a（C1）
之后，本轮完成 C4 核验销项 + H5 根治（宿主层）。

### 一、本轮做了什么（写给无上下文的新会话）

1. **C4 核验**：原登记「仅入口模块注入」在现架构下不成立（宿主=全量合并单模块 + v2 全树合并）
   → 多文件静态初值已覆盖全部模块（探针 multi4 双侧一致）；**冗余的跨模块名单改动已回退**
   （反证 B 证明：回退后 E2E 仍通过）。
2. **宿主缺陷 H5 根治**：类/容器静态「有初始化表达式」路径缺 StorePtr（对象构造但指针未入
   .data 槽 → 空指针崩溃；对任何模块成立）→ 有初值路径补 StorePtr。
3. **门禁**：E2E **262**（八形态折 255，借链=是）+ **反证双件**（A H5 回退→退出码 1；B 冗余
   回退→仍通过）+ **探针 multi4**（三形态静态双侧一致）。
4. **对拍纪律**：首跑 13 DIFF=**与锚定链交错的状态污染假象**（手动复现 diff=0 + 独立重跑 34/34）
   → 入 lessons 146。
5. 全量细节：`plans/019` §7.1 第一百零二轮行 + `plans/020` **第四十二节** + `lessons.md` 102-a 段
   （预防 146~147）+ `plans/021`（戳/数字/C4 销项台账）；锚定链 `target/p102x64l/`。

### 二、本轮验证链（全绿）

- 清场重建零警告；宿主 md5 `a9b7e55fab4dd7a7c176168fe8f86764`
- 单测 **1313/1313**；E2E **290 用例 286 过 / 0 失败 / 0 未实现 / 4 跳**（+262；零回归）
- 锚定链固定点 **384552 行**（与 101-a 相同）两代逐字节一致 + 绑定自检 + 运行级 smoke rc=0
- 组件对拍 **34/34**（DIFF=0；首跑假 DIFF 已澄清）；v2p md5 `df88ef2c8b7fa3fb7595e8fa0b4dc235`、
  cn_self102 md5 `641bffe3c846b5c0f57947b987315792`

### 三、下一轮任务（按序）

1. **C2（跨语句 NLL 活跃区间）**：前提=真实使用反馈期数据（当前维持排程，条件不成熟）；
   可先做**不动语义的探针评估**（误报率实测框架）——或转向登记面。
2. **登记面（本机可实施）**：C13（A2 风格精化，可选）；`1.5f` 后缀语法缺口；`%` 浮点语义层
   未拒绝；数学库 `数学::` 内置表缺位；v2 诊断无行号（D2）；行数存量违规面（D1）；D3~D5 维持。
3. **跨机轮（家机 win-x64 / 单位机 arm64）**：实体清单见看板通告（**93-a~102-a 全量复跑**；
   **两侧编译器均已多次变更需重建**）+ 家机立案 **148_v2 win 崩溃缺陷**（三方案待裁决）。
4. **呈报清单余项**：B3（B35 字段容器接收者）、B4（甲参数 move / 丙返回位拒绝）、B5（阶段 4
   第二层第三批收口时点）。（完整排班队列见 `plans/021` §3。）

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p102x64l/run_chain102.sh` + **组件对拍** `bash target/p102x64l/cmp_102.sh`
> （34/34 DIFF=0）。
> **手工 v2 探针链**（/tmp/p92probe：`diff101.sh` 双侧逐行 diff）：cwd 需有 `stdlib` 软链 +
> `target/` 目录 → `v2p <入口.cn> linux-x86_64` → `as target/v2asm.s -o 主.o` → `g++ -no-pie
> 主.o target/audit2/{runtime,intern_api,io_api,string_api,i128_api,math_api,input_api,file_api,
> time_api,system_api}.o` → 运行取退出码（**不要**用 `target/audit2/*.o` 通配）。
> **反证手法**：①先备份全部待注入文件 + md5 存档 → ②python 注入（含反斜杠锚点用 raw 字符串；
> 长文本避免英文双引号与 Python 定界冲突）→ ③重建 → ④跑用例应复现诊断 → ⑤恢复后**以①的 md5** 复核。
> **调试/验证纪律（累计 10 条）**：①`IR层诊断` 在 v2 会中止编译——结论须"干净版复核"；
> ②v2 共享结构体加字段 grep 全部构造点；③"元素步进/大小"统一 `semantic_->typeSizeOf`；
> ④门禁形态须能区分「旧机制巧合正确」与「新机制真正正确」；⑤反证"缺失位数"预先计算；
> ⑥类型位多形态后缀消费顺序显式设计；⑦双侧探针先做"全形态矩阵"；⑧改动共享推导回归同族面；
> ⑨**反证未命中即得结论要谨慎**（可能=改动冗余而非用例弱——102-a 反证 B）；⑩**对拍必须独立串行
> 且不得与锚定链交错**（102-a 假 DIFF 教训）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote；**未配置 `github` 镜像
> remote**，github.com 直连超时——镜像补推需网络可达的机器代做。

### 五、诚实边界

- 102-a 为**宿主层修复轮**（v2 零改动→固定点不变）；静态聚合（数组等）初值注入面维持「宁漏勿错」；
  静态槽不登记自动释放（D4 设计选择）。
- 前轮登记维持（静态槽不登记自动释放 / v2 诊断无行号 / 行数存量违规面 / v2 `%` 浮点未拒绝 /
  v2 数学库内置表缺位 / 未初始化槽 preFree 观察项 / `1.5f` 后缀缺口 / C13 A2 风格精化 / C2 NLL）；
  **B2=93-a、C6=94-a、C7=95-a（+C3）、C10=96-a、C8=97-a、C9=98-a、C11=99-a（+H4）、C12=100-a、
  C1=101-a、C4=102-a（+H5）已销项**（销项台账见 `plans/021` §4.2）。
- 反证注入已全部恢复并以**注入前 md5 基准**复核（`ir_decl.cpp e8b68d7d…` 逐字节一致）；
  探针留 /tmp/p92probe 不入库。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
