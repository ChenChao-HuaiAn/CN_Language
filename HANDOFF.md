# HANDOFF 交接文档

**交接时间**: 2026-08-15 16:38（第 10 层 收尾层 完成后）

## 我们在做什么任务

CN 语言编译器「模块系统 v2.0 全面改造」共 10 层，**已全部完成**：
- 第 1 层：规范更新（specs/08 v2.0 + 09-包与依赖.md 新增）✅
- 第 2 层：词法（ColonColon + 新关键字 模块/作为/包/货舱 + 删 从）✅
- 第 3 层：语法（ImportDecl 结构化 v2.0 + parseModuleDecl + 默认私有）✅
- 第 4 层：crate 重构（module.cpp 分桶 + 目录层级 + 语义分域 + use 导入表 + prelude + 内置 key `::` 化 + IR/codegen 包前缀 + 可见性交集）✅
- 第 5 层：货舱.toml 依赖管理（cargo_parser + CLI + 依赖查找 + 包.cn）✅
- 第 6 层：全量迁移（E2E 36/46 → 46/46 全绿）✅
- 第 7 层：新增 v2.0 特性测试（44-51 用例）✅ —— E2E 53/53、单测 1106/1106
- 第 8 层：全链路集成串联（52_library 图书管理系统）✅ —— E2E 54/54、单测 1108/1108
- 第 9 层：Debug 全面审查 ✅ —— E2E **54/54**、单测 **1110/1110**、编译 0 警告
- **第 10 层：收尾** ✅ —— plans/更新日志/HANDOFF/lessons 确认 + gitcode 推送（11 提交）

## 已经完成了什么（第 10 层收尾）

1. **plans 文档确认**：
   - `plans/002-阶段6b-模块系统 v2.0」全面改造方案.md`：第 10 层补 ✅，第 1~10 层全部勾选完成
   - `plans/002 CN语言编译器实施计划.md`：总进度表补「阶段6b：模块系统 v2.0」行 ✅（2026-08-15，10 层全部完成）
   - `plans/002-阶段6-标准库.md`：Task 6.1~6.13 全部 ✅ 状态确认（与 v2.0 无冲突）
2. **更新日志.md**：覆盖为第 10 层收尾说明（交付总览 + 测试基线 + 10 项决策对照 + 已知限制清单 + 推送状态）
3. **HANDOFF.md**：本文件更新为 v2.0 完成状态
4. **lessons.md**：25 条记录状态标注完整（✅ 已修复 / 📌 已评估已知限制），无需改动
5. **临时文件清理**：删除 `test_input_api_0~9.txt`（单测运行时在根目录重新生成，.gitignore 已忽略规则 `test_input_api_*.txt`）
6. **gitcode 推送**：11 个提交（第 3 层 `47ad923` 起至第 10 层收尾）推送 develop 成功，工作区干净，ahead 0

## 测试结果（最终基线）

- 全量单测：**1110/1110 全绿**
- 编译警告：**0**（MSVC /W4 /WX）
- E2E：**54/54 全绿**（01_hello ~ 52_library 全部通过）

## 用户 10 项决策落地对照

| # | 决策 | 落地 |
|---|------|------|
| 1 | 路径分隔 `::` | ✅ `导入 模块::符号` 全链路 |
| 2 | 保留 `导入`、删 `从` | ✅ 关键字删除 + 全部迁移 |
| 3 | 标签式可见性 + 模块内默认私有 | ✅ `公开:` 标签式，默认私有 |
| 3b | 类内访问控制默认私有 | ✅ 类内默认私有 |
| 4 | crate 隔离 | ✅ 函数跨模块同名隔离（类型/常量见已知限制） |
| 5 | 包.cn 根 + 模块树 | ✅ 包根聚合 + 再导出 |
| 6 | 子目录 | ✅ `网络/传输控制.cn` → `网络::传输控制` |
| 7 | 核心包 prelude | ✅ 内置函数无需导入 |
| 8 | 模块声明 | ✅ `模块 网络` |
| 9 | 重命名/花括号/通配符 | ✅ `作为`/`{a,b}`/`*` |
| 10 | 货舱.toml | ✅ [货舱]/[依赖] 解析 + 依赖查找 |
| 11 | 可见性交集 | ✅ 类内公开不突破模块级私有 |
| 12 | 不强制行尾 `;` | ✅ 声明以换行终止 |

## 已知限制清单（📌 已评估，非缺陷，编译器边界）

1. **跨模块同名类型/常量未隔离**（最高优先级后续项）：merge 已分桶允许（CrateTypeBuckets），但语义层 typeNames_/globalConstValues_/declareVar 全局去重 → 报「重复声明类型/变量」；锚点单测 `SemanticTypeConstCrossModuleNotIsolated`；根治 = 类型级 crate 分桶 + 限定名解析（中高风险 30+ 处）
2. **花括号项别名跨模块同名歧义**：别名重写未携带 moduleFilter（44 注释）
3. **父模块名限定调用子模块函数不匹配**：moduleFilter=父模块 vs moduleName=父::子（46 注释）
4. **向量<T> 8 字节槽**：类对象（>8B）存向量越界 → 手动动态数组
5. **跨模块泛型类字段**：`向量<整64>` 作类字段 mangled 名不匹配 → 泛型实例化放局部变量
6. **子目录模块无法导入父目录模块**：依赖主导入链全局合并
7. **crate 隔离同名函数纯名调用歧义**：限定调用 `主::版本()`（自导入）+ 多级路径
8. **整路径重命名绑定模块级别名**：重命名用花括号项内形式（45 注释）

## 当前卡在哪

无卡点。v2.0 全面改造 10 层全部完成并推送 gitcode develop。

## 下一步计划（阶段7 自举）

1. **类型级 crate 分桶**（A1 限制根治）：类型表按 moduleName 分域 + 限定调用类型解析 + 类型 mangling（IR/codegen 30+ 处，中高风险）
2. **阶段 7 自举**：`plans/002-阶段7-自举.md`——用 CN 语言重写编译器自身；v2.0 语法是自举编写基础（:: 路径/模块树/默认私有/货舱.toml 全部就绪）

## 踩过的坑绝对不要再踩（已同步 lessons.md，25 条）

- **顶层静态必须全链路落地**：语义登记 + IR 消费（visitProgram 遍历 globals + 读/写/自增路径）+ codegen 发射三件套；「语义登记 + IR 零消费」= 孤立代码
- **merge 分桶 ≠ 语义层隔离**：改动类型系统须三处同步（merge/语义/IR）
- **链接符号前缀规则定义侧（IR）与调用侧（语义）对称**：入口文件 主.cn 全部函数永不加 crate 前缀
- **`$` 在符号名中多义**（模块前缀/泛型实例/内部唯一名）：剥离模块前缀必须结合 `#` 存在性判定
- **Windows 大栈帧必须 __chkstk**：emitPrologue 栈帧 >4KB 发射三段式探测；单函数局部变量+打印控制在 <8KB
- **Windows 命令行参数用 CommandLineToArgvW(GetCommandLineW()) + CP_UTF8**：勿依赖 __argc/__argv/__wargv
- **内置限定名前缀禁止类型关键字**：`字符串`/`整数`/`浮点` 等不可作前缀（用 `解析`/`数学`/`IO` 等）
- **E2E 迁移只改语法不改行为**：不改任何打印输出，expected 是契约
- **E2E 不支持预期编译错误**：错误类场景用单测；E2E 只测合法路径
- **MSVC 增量编译时间戳粒度**：改 C++ 源后 touch（PowerShell LastWriteTime）+ 确认编译行再跑测试
- **Windows 中文路径三重编码**：argv GBK / 源码 UTF-8 / 磁盘 UTF-8 → driver 层统一 UTF-8 + 工具链保持 GBK
- **泛型函数/类方法参数禁用 C 风格函数指针**（T 内嵌无法单态化）；泛型类方法体禁止循环遍历第一个泛型字段（已根治但保持警惕）
