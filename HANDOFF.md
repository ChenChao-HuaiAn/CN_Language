# HANDOFF 交接文档

**交接时间**: 2026-09-03 第七轮（家机 win-x64）——**v2 三元条件运算符 `? :` 全链实现
（③ 第六波）**：词法分析.cn 冒烟 [parse] 截断归零（自举对拍首组件完整编译到 asm）+
语法分析.cn 冒烟启动（[parse] 全过，语义 54 错=下一批缺口三簇）；连带根治两处既有
缺陷（表达式入口前缀紧绑定 + 静态自增静默丢句）。

## 本轮已完成（全部有实证）

### 一、三元条件运算符（E2E 137 载体，HANDOFF 上轮下一步①）
- **语法**（语法分析.cn）：新节点 `节点_三元运算=33`（子起点=条件段起点/文本=真值段
  起点/附加=假值段起点/子终点=段末+1，三段连续区间）。解析在 解析表达式 尾段包装
  （二元链之后，优先级 1.5 低于逻辑或高于赋值）：真值/假值=完整表达式递归（假值
  递归=右结合）；`:` 缺失不消费（防御惯例）；v2 无后缀传播 `?`，一律三元。词法层
  零改动（`?` 兜底单字符既切出，注释补录）。
- **语义**（语义分析.cn）：检查表达式 三元分支——两分支类型经段末端节点递归推导 +
  **类型可合并判定**（同ID / 整数族宽化：整8/16/32/64、正*、布尔、字符互通；浮点族
  互通；跨类拒绝：整vs浮、字符串vs整数——Rust if 表达式纪律）；-1 跳过（级联防）；
  条件段类型不强制（条件跳转按非零判）。
- **IR**（IR生成.cn）：预扫3 三元三段区间整段跳过 + 主循环三元 case **复用 125 短路
  方案**：条件寄存器→临时槽（IR_分配 帧登记）→三块（真/假/汇合）条件跳转→选中
  分支求值+IR_存储（**惰性求值**）→汇合块 IR_加载（类型=真分支寄存器登记，兜底
  T_整64）→登记压栈。栈平衡=各段递归后弹到前深度再压汇合（一元模式净+1）。
- **测试**：E2E **137_v2_三元运算符** 六步闭环 PASS（20 类断言：基本/右结合三形态/
  优先级（`假||真 ? 7 : 8`）/惰性副作用双向锚定/除零保护/整数族宽化/字符串分支/
  各语句位置）+ v2p 组件 主.cn 源码9 负测自检（字符串vs整数跨类须拒，expected 固化）。
  宿主 sanity 先行（退出码 0）。

### 二、【连带揪出·当场根治】两处既有缺陷
- ① **表达式入口一元前缀操作数紧绑定**（语法分析.cn）：原前缀段操作数「递归
  解析表达式（完整）」把后随 `?`/低层二元吞进操作数——`!x ? a : b` 变
  `!(x?a:b)`、`!x + 1` 变 `!(x+1)`（背离 C 一元优先级 12；三元接入后可见化）。
  统一改 解析层_子(层5,真) 紧绑定，与第五波二元链操作数位前缀收敛为全路径一致。
  （lessons 10.0 成文——与 10.5 互为镜像，同根因「前缀操作数边界」）
- ② **静态变量自增静默丢句**（IR生成.cn 节点_自增）：原仅认局部/参数槽，静态
  目标零 IR 零诊断静默跳过（探针 IR dump 实证；「防御分支静默降级」家族第四例）。
  补静态槽自增：IR_静态加载 +1 → IR_静态存储（②c C 设施）。（lessons 9.0 成文）

### 三、冒烟推进（HANDOFF 上轮下一步①②完成）
- **词法分析.cn 冒烟 [parse] 截断归零**（最后截断点 行 236 三元已支持）：1595 节点
  完整解析、语义 0 错、**IR 生成 10 函数成功**（上轮「调用被调对象非函数名/方法」
  确认为三元截断级联，已消失）、**asm 16345 行、ml64 汇编通过（60863 字节 obj）**
  ——自举对拍首组件首次完整编译全绿。
- **语法分析.cn 冒烟启动**：[parse] 全过（4 文件——语法分析.cn 导入链拉入 词法分析
  1595/IR签名 3386/IR容器 6655/语法分析 11684 节点全解析成功），语义 54 错。

## 下一步（接手会话按序执行）
1. **消化语法分析.cn 冒烟 54 错（三簇根因，逐簇立案）**：
   - ① **跨模块顶层常量可见性**（约 25 错：T_整64/T_对象/IR_加载 等——常量表
     单文件级，语法分析.cn 用 IR容器.cn 的常量报未声明；注意 120_v2_顶层常量 用例
     是"用例源码引组件常量"方向，本轮是"组件引组件"方向，语义分析多文件 的
     常量收集面待查）；
   - ② **文件 API 标识符**（9 错：打开/打开文件/关闭文件——查 IR签名.cn/语法分析.cn
     里的用法形态，可能是 限定名/局部函数 的某形态缺口）；
   - ③ **可写左值白名单**（9 错「赋值目标必须是可写左值」+散件 名ID/当前函数变量
     类型——某赋值左值形态不在白名单）。
   消化后继续 语义分析.cn/IR生成.cn 冒烟（对拍推进到更多组件）。
2. **ARM64 侧全量复验（单位机）**：本轮变更（三元三层+前缀紧绑定+静态自增）+
   上轮（./统一、::、真假无）须 linux-arm64 全量 E2E 复验（单侧绿≠双侧对称）。

## 灰色点披露（待用户裁，未立案）
- v2 `打印(真)` 输出 `1`；v2 二元 `==/!=` 字符串指针比较；`*p += 1` 不支持；
  v2 整8/整16 强转白名单未覆盖；v2p 语义错误后仍继续 IR 生成（均沿用）。
- 宿主三元 `?` 与后缀错误传播按位置区分（ternaryQuestionNext），v2 无后缀传播
  `?`（结果? 语法未实现）——两侧行为差异随 v2 错误处理特性推进时对齐。

## 踩过的坑（本轮新增，已入 lessons）
- **表达式入口前缀「递归完整表达式」=隐藏优先级吞噬**（!x+1 变 !(x+1)）——新增
  优先级层次时必须重审既有各位置操作数解析路径（lessons 10.0）。
- **静态自增静默丢句**——目标解析链新增存储位置时全量核查所有「按名取目标」分支
  （lessons 9.0，家族第四例）。
- 用例侧：宿主顶层**不支持变量声明**（用 静态）；宿主 `||`/`&&` 要求**布尔操作数**
  （非 C 非零语义）；字符串比较 返回布尔不能 `!= 0`（直接作条件）。

## 门禁状态（本轮最终，家机 win-x64）
- 构建（ci.ps1）零警告零错误；单测全绿；全量 E2E：**新增 137 全绿 + 既有零回归**
（78/79 已知遗留内存超限除外——形态与上轮一致）。
（若后台门禁结果与此不符，以上一轮门禁结论为基线排查本轮改动。）
