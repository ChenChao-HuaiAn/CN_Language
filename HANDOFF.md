# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（287-a 开发轮·用户裁决方案A·2026-09-17）

**任务**：根治 T13（linux_x64 后端 O0 移位发射 i32 零扩展污染）——承接本会话前半 M3 采样测试轮的 s2026091705 定案（用户裁决「方案A」）。锁纪律：等单位机 286-a 交锁后认领（38c15fe 🏃·基线 7962579）。

1. **根治**：`LinuxX64CodeGenerator::emitShift`（linux_x64_instructions.cpp）i32 非常量路径补 `movsxd r10, r10d`——对齐 270-a T14（emitCast 整→浮）同族先例（「姊妹算符照妖镜」第 2 例：T14 修 cast 面时未审 shift 面）。免疫面实证：i8/i16 装载已 movsx、u*/i1/常量（mov reg,imm64）、shl（低 32 位）、shr（无符号）；win（sar eax）/arm64（asr w9 宽度化）/v2（D34 出口规范化）三后端零改动。
2. **验证链（全绿）**：构建零警告；单测 1351/1351；**E2E 全量 422=422/0/0**（含新 403+79 自举闭环=win 锚定链逐字节不变证明·win 后端零改动）；CLI 矩阵 50/50；面覆盖 57/60 PASS；修后探针 linux .s=4/4 sar 全部 movsxd 前置+修前修后 diff 仅 4 行插入（最小改动反证）；采样回归轨修后 38/38（无回归）。
3. **新 E2E 403_移位负值矩阵**：11 行矩阵（i32 负值变量/常量移位量 sar+shl+正值零回归+i16/i8 免疫+u32 shr+mod 取模+污染值判定面）——expected win 实跑固化·coverage_map 4.4 边界例列登记。
4. **甄别定案存档**（前半验证轮 82bdbdd）：s2026091705「变7=1/0」实为 **O3 对（0=规范值·plans/001 移位取模语义）O0 错（1）**——285-a「O0 对 O3 错」与 4ad94f6「修优化层」两次判定反转；真凶=零扩展污染非 T12 折叠路径。T12 行已在 plans/021 修正关闭、T13 行销项（287-a）。
5. **v2p 实证**：v2 对 s2026091705 的 linux asm（重建 p206c/v2p）=64 位装载+D34 槽值符号正确→sar 免疫·**v2 树零改动成立**。**附带观察项（新·未立案呈报）**：该 i32 移位掩码走 `and ecx,63`（mod 64）非 31——此样本巧合等值（13），移位量 ≥32 形态疑似 mod 语义分叉（v2 IR 类型推导面）——待独立甄别轮，本轮不动。
6. **写集**：src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp（emitShift 一处）｜tests/e2e/403_移位负值矩阵/（新）｜tests/e2e/coverage_map.md｜plans/021（戳+T12 修正+T13 销项+§5 索引+§6 瘦身）｜plans/025 §1.0｜HANDOFF/更新日志家机节｜看板。探针资产 target/p287probe/ 留档。

**踩坑与教训**：①「O0=参考实现」默认假设陷阱——O0 同样可能有直译层缺陷，规范才是唯一判准（深度机两次判定反转根因·lessons 已记）；②asm 甄别 grep 到指令名（sar）≠发射正确，必须核操作数宽度（r10 vs r10d）；③管道洗退出码再犯（check_handoff FAIL 被 `| tail &&` 洗掉·提交后才见——lessons 已有·执行面仍需警惕）；④v2p 输出路径硬编码 target/v2asm.asm·须以项目根为 cwd 位置参数调用。

**下一轮候选**：①深度机跨机项：X64L 锚定链重锚（linux 产物因 movsxd 变化）+s2026091705/403 运行级复验；②v2 移位量掩码 63/31 分支疑似 mod 语义分叉甄别（独立轮·探针=移位量 33 形态 win/v2p 对拍）；③T22 arm64 位立即数（单位机已呈报待裁）；④C20 波2 批B（家机·前置已清）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 289-a 开发轮收工（看板交锁）——**B9 缺陷族面扩散检验制度落地（用户批准「批准」）**。接手前先 `git fetch`。

### 一、本轮做了什么（289-a·基线 217d132·写集与 287-a/288-a 零交叠）

1. **B9 三落点全落地**：①**`scripts/famscan.py` 转正**（参数化 `--cn/--out/--keep`+gcc 探测自动降级单 oracle+T23 首案 47 用例固化=**复验基准在库**——287-a 修复轮判据=`python3 scripts/famscan.py` 命中 **16→0**）；②**AGENTS §8.1 扩条**「缺陷族面扩散检验（B9·立案与根治双挂点）」——新点缺陷立案须附族扩散矩阵+根治轮 famscan 复跑归零；③**plans/026 §2.11** 四件套条文（扩散探针/立案挂点/根治归零/用例库滚动·与 B8 互补自顶向下 vs 自底向上）。
2. **转正过程修掉工具自身两 bug**（首案遗留）：①命中清单报告**三列错位**（tuple 索引 vs 标签——首案控制台显示的「CN-O0/CN-O3/C」列实为 C/O0/O3 值，**flags 判定与命中集合一直正确**，16 命中定案不受影响）；②C 生成器对 `%.6f` 浮点用例误包 `(long long)`（gcc 收到整数→0.000000 假 oracle 分叉）——修复后**实测 16 命中与首案定案逐项一致+oracle 零分叉**（/tmp/famscan_verify2 留档）。
3. **plans/021**：B9 迁出 B 区（生命周期铁律）+§4.2 销项+§5 索引+文首戳 289-a+T23 行工具指针切 scripts 版。
4. **验证**：`python3 -m py_compile` ✓+famscan 实测 16 命中复现 ✓+check_handoff/check_progress_sync 双绿 ✓。

### 二、验证链（本机复现口径）

```
python3 scripts/famscan.py --out /tmp/famscan_verify2   # 16 命中（T23 缺陷在=预期基线）
python3 -m py_compile scripts/famscan.py && python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py
```

### 三、下一轮任务（按序）

1. **287-a（家机·在飞）收工后**：深度机跑 `python3 scripts/famscan.py` 复验 16→0（T23 销项判据）+窄型跨机面（win/arm 运行级跑 fam.cn）。
2. **深度机候选**：①B8 面台账巡视续行②daemon 常态化（286-a 结构变更后 pull 重启·看单位机通告段）③T21 待规范裁决。
3. famscan 用例库随新立案滚动扩充（026 §2.11 第 4 条）。

### 四、诚实边界

- famscan 的 C 对拍需 gcc（无则自动降级单 oracle 并标注）；win 侧建议 asm 静态比对替代（026 §2.11 已注明 linux 优先）。
- 16 命中=缺陷未修的预期基线（287-a 在飞）——非工具失败；修复后应归零。
- 并行锁说明：本轮为用户批准 B9 实施的第三并行轮（家机 287-a/单位机 288-a 系用户此前授权），写集零文件交叠+共享文档分区隔离，push 已零冲突。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🏃 286-a 开发轮收工（看板交锁）——**S1b 采样日志分文件化根治（用户质询+令「现在修改」）+M3 采样 arm64 首轨（独家捕获 T22·呈报待裁）**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **S1b 根治（用户质询单文件爆炸·2509 行/108KB·≈1~3 万行/天趋势）**：①daemon 日志改**按月分文件**（tests/cnsmith_hits/日志/YYYY-MM.md·翻月自动建头）+「变化时记/定时汇总」记法（连续零新存/零已修/命中集合同/基线未变的轮次合并一行汇总；任何变化独立成轮完整记录）+命中列表去重；②存量 2583 行原样迁移 2026-09.md（家机/深度机在飞增量折入+残条补注）；③采样日志.md 改**固定指针**（≤15 行·外部引用路径不失效）；④check_handoff.py 扩采样日志门禁（指针行数/轮次条目/月文件名/单月 5000 行上限）——**反例三连拦截+正例 PASS 双向实证**；⑤两防御（家机 492e976 建议采纳）：差分前删旧分歧清单（防零分歧残留清单假命中 1360 条教训）+重建后 target/cn 新鲜度核验（防假重建滞后基线）；⑥种子改时间戳到秒（根治重启撞号——深度机 seed=01/02 反复实证同批样本重采）。
2. **M3 采样 arm64 首轨（三轮·每轮 100 新样本+回归轨）**：新样本 351·输出分歧 0·编译失败 0·**运行失败 65 样本=独家捕获 T22**；回归轨在 arm64 上 11 命中（全部=T22 形态·非 T12 复发）/28 未命中（T12 形态 arm64 本就不呈现）；31 命中样本自动入库 run_err/（T22 证据+根治轮现成回归轨）。**arm64 移位零扩展定点探针**（82bdbdd「arm64 待查」补位）：-426>>13 O0/O3 双=-1 对=**免疫实证**（零扩展污染=linux_x64 O0 单侧）。
3. **T22 立案呈报（缺陷零容忍·方案待用户裁决）**：arm64 -O3 位运算常量立即数非法汇编——orr/and/eor 直发 #425/#524/#688/#0 等非 bitmask 常量→as「立即数越界」**拒编译**；O0 全对·win/linux_x64 免疫；最小复现=甲|425/甲&0 O3 as 崩·O0 4950/0 对；挂点=arm64 逻辑立即数编码器缺失（52-a movk 取片/D20 x64 移位立即数同家族）。详见 plans/021 §3-G T22 行。
4. **收尾**：plans/021（戳 286-a+T22 立案+S1b 销项 §4.2+§5 索引+B 区裁决迁出）+本表+更新日志+看板收工+通告段（daemon 结构变更广播）。

### 二、发现的问题

- **T22（新缺陷类别·呈报待裁）**：见上；31 样本已入库=根治轮现成回归轨素材。
- daemon 两观察项（不阻塞）：①cnsmith_diff 把汇编失败记为「O3 运行失败」（分类应区分编译面失败）；②回归轨=命中库全量→滚雪球 38→100（机制自洽=防复发语义·但新样本配额被挤压）。
- 本节原有 `## 单位机 ARM64 节` 标题连续两行粘连残留——本轮整节替换顺带根治。

### 三、下一轮候选

1. **T22 根治**（用户裁决方案后任一机认领；arm64 运行级复验归本机·31 样本回归轨现成）。
2. s2026091705（linux_x64 O0 移位零扩展·82bdbdd 定案）根治后跨机复验。
3. daemon 两观察项+新种子轨常态轮（本机轨=arm64 后端互补证据·采样量有限）。

### 四、验证链（本机复现口径）

```
python3 -m py_compile scripts/cnsmith_daemon.py scripts/check_handoff.py    # 语法
python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py  # 双门禁 PASS
python3 scripts/cnsmith_daemon.py --rounds 1 --samples 50                   # 采样单轮（月文件+指针+门禁联动）
# 编译器源码零改动（286-a 全程）→ E2E/单测基线=82bdbdd（285-a 全绿 421=419/0/2）未重跑；
# 构建=target/build 全量重建 09:16（rm -rf 清场·探针 rc=0 实跑验证）
> github 镜像本机无凭据未推（170-a 起累积·既有口径待他机代推）。
```

### 五、诚实边界与坑

- **批量编辑文档必须先备份+行级锚点断言**：本轮 plans/021 曾因正则 `[^|]+` 跨行吞并+拼接漏尾段**截断文档尾部 98 行**（git checkout 恢复重做）——教训：`[^|]+` 会匹配换行符、字符串拼接必带尾段、大批量编辑前 cp 备份。
- arm64 采样三轮仅 ~5 分钟（vs 深度机 8s/轮）——本机采样量有限，M3 计时主力仍在深度机 daemon，本机轨=**arm64 后端互补证据**（首轮即抓到 x86_64/win 双免疫的 T22=互补价值实证）。
- 回归轨 1 样本与新种子样本撞名（s2026091721·两代生成器名空间重叠）·对结论无影响。
