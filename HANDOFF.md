# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（296-a 开发轮 + 🧪 M3 采样轮 298-a/299-a·2026-09-17·基线 6217fca→c4dcdff→40a4633）

**296-a（开发轮·已收工）**：**D35 波B 销项——v2 错误传播运算符 `?` 全链**（六文件）——364/344/60 三实弹双编译转正上架+T32 完整闭环；单测 1353+全量 E2E 423=423/0/0（78_v2/79_v2 自洽）+CLI 50/50+面覆盖+spec --strict 176+双文档门禁；教训两条入册（v2 节点池「子终点=自身索引」惯例）。明细=更新日志家机节（git 承载）。

**🧪 M3 采样轮 298-a（验证轮·不占号）——win 列补齐**：单位机 🏃 297-a+深度机 🧪 在飞期间按免锁验证轮采集。
1. cnsmith 500 样本差分（新种子·726.8s）：**一致 500/500**。
2. **面台账行 57 win 列定向探针**（target/p298a/probes/）：**T25 win 同挂实锤**（消费位全挂+k04 复合赋值全族——「arm64 单侧」纠正=win+arm64 双挂）+**asm 铁证**（k01 双槽分叉 vs k02 一致书写）+T24/T27/T36 win 同挂确认+T26 win 免疫确认（x86_64 单侧成立）。
3. 广播=看板家机通告段轮次推送（单位机 297-a 修复轮实时可见）。

**🧪 M3 采样轮 299-a（验证轮·不占号）——不同采样维度 + T25 根因链闭环**：
1. **负向诊断轨修正版 400 样本**（T28 口径：去 # 行+重试选行·5 类注入）：**400/400 全拒绝且诊断精确**（win 侧闭环）。
2. **深语句轨 500 样本**（--stmts 30）：**一致 500/500**（738.3s）。
3. **T25 win 根因链闭环（IR+asm 双链）**：`cn ir -O3` 对照 k01/k02——k01 bb1 尾「搬运 %v9 %v7 (f64)」（=256-a Opcode::Copy/Phi 降级）+bb3 使用点重写直用 %v9·该搬运 asm 发射 **4 字节 mov**（i32 宽度·应 movsd）→槽半写→0.000000；k02 三分支未触发重写（保持「加载 a$1」）→对——**修复轮直接根因**。
4. **面联想矩阵**（t25_face.cn·载体扩散）：变量载体全挂/结构体字段+数组元素载体免疫/嵌套内层例外——**载体面边界完整**。
5. 落盘：面台账行 13/52/57/63 win 列+plans/021 T25/T36 行+看板通告段四轮增量（f41c2f5→354c3cb→9716997→40a4633）+plans/025 §1.0 回填。**源码/测试用例/门禁脚本零改动** ✓。

### 二、下一轮（按序）

1. **M3 采样轮续**（用户令不停·默认动作——他机持开发锁期间）：不同维度=更大样本 1000/权重重分布/复合语句轨/k04 族 win-arm64 差异（需 arm64 数据归单位机）。
2. **开发锁空闲时**：T33 读侧窄宽三后端设施（编码LoadStoreWin movsx·385/386 转正）或 T35 专项（v2p 自身错误 6 例）。
3. **B10/B11/B12/T24/T25 裁决后根治**（单位机 297-a 在飞·win 根因链+边界图已备齐——修复轮直接可用）。

### 三、诚实边界与坑

- **HANDOFF.md 单位机节存在重复标题行（`## 单位机 ARM64 节` ×2）**——他机分区按 §8.3 不越区修改，登记待开发轮修；check_handoff 未拦截 `## ` 级重复标题=门禁缺口（同轮登记·开发轮扩门禁）。
- T25 win 侧 v04 比较位「对」为巧合正确（a4=1.5 与 0.0 均满足 >2.0 为假）；k03 循环携带形态条件永假未进循环——均不判对。
- w02 嵌套内层「对」的边界机制未深挖（重写触发条件与 CFG 结构相关性=修复轮甄别）。
- 探针设施不在库：target/p298a/{probes,run_probes.py}、target/p299a/{probes,inject.py,k01,k02,*.ir}——修复轮回归复核复用。
- T27 arm64 侧仍未测（归单位机跨机项）；T25 修复后 win/arm64 运行级复验=跨机项。



## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🧪 M3 采样轮·增量5+B9 扩散轮（验证轮·不占开发锁·源码零改动）。接手前先 `git fetch`。当前 HEAD 478d804+收工提交；家机 🏃 296-a（D35 波B·v2 树）在飞中——本机继续 daemon 采样不停。

### 一、本轮做了什么（🧪 基线 1903a2f→478d804·家机 296-a 开发期间持续采集）

1. **daemon 在飞 67 轮对账**：命中集合不变 5 项=T26 布尔短路值位×3（s2609179001~003）+T27 除法溢出双位宽×2（s2609179004/9005·**9005 整64=族内新实弹**）——均为已立案缺陷的持续暴露（回归轨正常行为·待根治）。月日志曾膨胀至 6212 行（触发 5000 门禁）→ 按 S1b 记法本意手工压缩为汇总段（3939 行）。
2. **T28 族双新实例立案（plans/021 §三-G·设施判据与语义脱节族）**：
   - ①**daemon 归类判据文本反推断裂**：`cnsmith_diff.py:60` 清单行只写 `rc=-8`（run_err 标签不落清单文本）·`cnsmith_daemon.py:134` 靠含「运行失败」字样反推类别→落空→rc=-8 命中误归 diff/（应 run_err/）+已归档样本每轮重建 diff/ 重复副本（实测两次复现）。
   - ②**S1b 汇总记法结构性失效**：`记轮()` 无变化判据含 `not 消失回归`，而 93 个已修复回归样本每轮均不再命中→消失回归集合结构性非空→无变化永不成立→每轮退化全量记录（日志膨胀根因）。
   - 方案甲/乙/丙已列（甲=清单行带「运行失败:」前缀+消失回归改增量语义）——**待测试设施轮根治**（验证轮不改源码）；根治验收=rc=-8 样本落 run_err/+连跑 3 轮仅汇总。
3. **T27 B9 补轴扩散（面测试网·点→线→面实践）**：72 形态三方对拍（双位宽×÷/％×8 求值位置×变量/字面操作数形态×O0/O3·python 硬件语义 oracle·设施 /tmp/cn_t27 不入库）→ **26 形态分歧·两大新形态**：
   - **新形态①（级别间分叉）**：整32 变量形 `INT_MIN/-1` ——**O0 真 idiv SIGFPE 崩 vs O3 优化回绕不崩（-2147483648）**——同源程序双级别一崩一过（7 求值位置×÷/％=14 形态全部复现）。
   - **新形态②（字面静默错值·比崩溃更隐蔽）**：整64 字面形 `(0-9223372036854775808) ÷/% (0-1)` ——**O0=-2147483648（INT64_MIN 低 32 位符号扩展的精确位样）/O3=9223372036854775808（2^63 位样）或 0——双级别互斥且均非一致 64 位语义**（12 形态）。
   - **甄别链**：纯整64 字面打印/整64 变量打印 O0 全对→截断仅挂「字面常量直连÷/％」通道；`emitDivMod` 两后端（linux_x64/x64）均真 idiv（含除零检查）已排除→挂点收敛 **IR 构造/常量装载层**（精确行号留根治轮·遵守「立案描述≠根因」）。
   - **正面结论**：短路右支/真或右支惰性正确豁免溢出（4 形态）·整64 变量双级别崩一致·整32 字面双级别一致（字面升 64 位域=C 同款·语义注记非缺陷）·除零防护✓。
   - 甄别中剔除探针自身问题：4 形态字面复合赋值=生成器漏声明（build_err 双级别对称·非编译器缺陷）；oracle 未建模「字面升 64 位」曾产生假阳性组（已甄别排除）。
4. **立案落盘**：面台账行 62 扩注（扩散轴+两新形态+正面结论）；plans/021 T27 立案行扩注+**B12 呈报扩注**（扩散实证：O0 陷阱/O3 回绕/字面截断/位样直通四路行为不一致——乙/丙无法消除级别分叉与字面错值，**唯甲统一回绕同时消崩溃+分叉+错值**）；月日志新增「扩散轮」段完整记录。

### 二、验证链（本机复现口径·当轮实测）

```
python3 scripts/cnsmith_daemon.py --rounds 0 --samples 250 --sleep 90   # daemon 常态采样（重启后 PID 1303930 在跑）
python3 /tmp/cn_t27/probe.py                                            # B9 补轴 72 形态三方对拍（临时设施·不入库）
./target/cn build <f>.cn --target linux-x86_64 -O0/-O3 --output <exe>   # 双级别复现
python3 scripts/check_handoff.py / check_progress_sync.py / check_matrix_coverage.py  # 三门禁独立执行退出码=0
```

### 三、下一轮任务（按序）

1. **daemon 续跑+月日志压缩监控**（记法根治前每 ~45 分钟窗口压缩·红线 5000 行）——采样不停。
2. **T27 补轴实弹**：26 分歧形态中代表性样本待 B12 裁决后随 C18 实施转正 E2E。
3. **T28 族三实例根治轮**（daemon 归类判据+汇总记法+T28 注入器——测试设施轮·待用户排班或开发空窗认领；需开发锁）。
4. 用户裁决 B11/B12 后：T26（与单位机 T25 合并·三后端同查）+T27（并入 C18 口径）根治轮。
5. B8 面台账已清零（唯一 ⬜ 行 16=C18 待实施格）——巡视转已立案行复验/行 57 平台面余列。

### 四、诚实边界

- 本轮**编译器源码/测试用例/门禁脚本零改动**（验证轮纪律）；T28 族两实例只立案不修码（登记 §三-G 待设施轮）。
- 整64 字面 O3 侧 2^63 位样与 O0 侧截断的**确切生成层未读到行**（折叠通道与打印通道两假设并立·已注记为挂点假设非定论）；O0 字面组不崩的预处理层未定位（emitDivMod 已排除）。
- oracle 为自建硬件语义模型非 gcc 对拍——根治轮须双 oracle 复验；T27 arm64 侧仍未测（归单位机跨机项）。
- 汇总段注记曾误写「原始 67 轮见 git 历史」——实际 67 轮未入库（压缩发生在提交前）·已在月日志当轮修正。

## 单位机 ARM64 节
## 单位机 ARM64 节

**交接时间**：2026-09-17 🧪 M3 采样轮⑤收工（五连轮）——daemon 全绿+行 22/17 arm64 复验零命中。**在飞呈报待裁：T24（后缀自增值语义）/T25（浮点汇合赋值 -O3 归零·日常高频·紧急）**。接手前先 `git fetch`。

### 一、本轮做了什么

1. **M3 采样 2 轮全绿**：293×2 一致/分歧 0/失败 0。
2. **行 22 常量折叠 5 形态+行 17 优先级结合性 10 关键对**三方对拍全一致（C vs CN-O0 vs CN-O3·O0=O3）——零命中；台账行 17/22 注记 arm64 增量复验绿。
3. 口径确认（非缺陷）：顶层常量=推断形态 `常量 名 = 值`（`常量 整32 x` 类型前缀形态按规范拒绝）。

### 二、本日五连轮总账（单位机 M3 正确性收敛·全部已提交推送）

- **轮①**（cb17b86）：daemon 293×2+B8 行 9/12/52/57 arm64 复验闭环（T12/T13/T16）+**B10 呈报**（三元 ? 歧义裁决代价面·§3-B）。
- **轮②**（a2a5b8d+修复 063e949）：daemon 393×2 大样本+行 14 一元算符面 **T24 立案**（后缀自增 i++≡++i·5 消费位分叉·规范缺口=01b 无 ++/--）。
- **轮③**（5d718d1）：daemon 293×2+行 13 三元扩面 **T25 立案**（浮64 三元 -O3 归零·IR+asm 槽位双铁证）。
- **轮④**（64f84d0）：daemon 293×2+行 52 浮点汇合族扩面——**T25 面重大扩充**（if/else 浮点同名单同挂·最小日常形态实锤·免疫面划清）。
- **轮⑤**（本轮）：daemon 293×2+行 22/17 零命中。
- **M3 计时轨**：daemon 累计 10 轮全绿（不同种子/样本规模 200/300·回归轨 93 持续归零）——T22 零复发。

### 三、下一轮候选

1. **M3 常态采样轮⑥**：行 8 算术/行 10 位运算/行 11 比较算符 arm64 矩阵（候选行渐清→回归已立案行复验+daemon 纯采样）。
2. **T24/T25 裁决后根治**（T25 紧急·浮点 Copy 发射链三后端同查+负测 E2E；T24+T25 可同轮=表达式值语义+发射链——本机可认领开发轮）。
3. A12 里程碑标签锚点搭车。

### 四、验证链（本机复现口径）

```
python3 scripts/cnsmith_daemon.py --rounds 2 --samples 200   # 293×2 全绿
python3 tests/e2e/run_e2e.py --target linux-arm64 --cn target/cn --filter 403   # PASS（轮①）
# 各矩阵探针=plans/025 §三.12~§三.16（/tmp 已清理）
python3 scripts/check_handoff.py; echo 退出码=$?   # 独立验证（教训落实）
> 源码零改动→全量门禁各轮均未重跑（基线链 46662c3→64f84d0 全绿承载·五轮一致）。
```

### 五、诚实边界与坑

- T25 win/x64l 同挂风险高（优化层重写平台无关）——各机复验登记；k02 对 vs m1 挂差异=根因甄别线索。
- T24 下标位/字面量宽化形态、T25 浮32 穷尽面=各自根治轮补。
- C 探针模板教训（本日两踩）：gcc 不认中文标识符（一律 ASCII）+必带 stdint.h；cn 必须项目根调用。
- github 镜像本机无凭据未推（既有口径）；五轮全程与他机零交叠。