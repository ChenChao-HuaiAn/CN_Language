# HANDOFF 交接文档

**交接时间**: 2026-08-18（B 任务第一阶段：多文件组件拆分完成）

## 我们在做什么任务

CN 语言编译器「B 任务：多文件组件拆分」——将 CN 组件链（词法/语法/语义/IR/代码生成）从
单文件实现拆分为「门面 + 子模块」多文件结构。**本阶段（第一阶段）只拆 词法分析.cn、
语法分析.cn、语义分析.cn 3 个组件**，IR生成.cn、代码生成.cn 留到下一阶段。

**上一轮（2026-08-17/18）核心成果**：阶段7 自举真实闭环完成（E2E 79/79），CN 组件链 v2
完整改造（代码生成.cn 重写为真实 x64 MASM 后端）+ 性能基线对比（CN 版/C++ 版 ≈1.04x）。

**本轮（2026-08-18）核心成果**：B 任务第一阶段拆分完成——3 个组件拆为「门面 + 子模块」，
E2E 79/79 全绿，79 自举闭环仍成立（CN 版组件链成功编译拆分后的门面+子模块合并源码）。

## 已经完成了什么

### 1. 前置验证与扩展（用户已审批）

- **前置验证**：C++ 版 cn 支持 `模块::函数` 限定名调用 ✅；CN 版语法分析.cn/IR生成.cn
  **不支持** `::` 限定名调用 ❌（`关键字::查找关键字` 被解析为两个独立标识符）
- **用户审批**：同意扩展 CN 版语法分析.cn + IR生成.cn 支持 `::` 限定名调用
- **扩展语法分析.cn**：`解析后缀` 增加 `::` 分支，输出 `成员|::|名` 行
- **扩展 IR生成.cn**：`处理成员节点` 增加 `::` 分支，生成 `模块名$函数名` 符号
  （忽略调用文本中的模块名，用当前模块名——79 闭环合并门面+子模块源码后统一 IR 生成）

### 2. 拆分结果（全部 ≤1000 行）

| 组件 | 门面文件 | 子模块 |
|------|---------|--------|
| 词法分析.cn（478 行） | 词法分析.cn（158 行）：分词 + 记录 + 字节值 | 词法/关键字.cn（64 行）、词法/运算符.cn（94 行）、词法/扫描.cn（181 行） |
| 语法分析.cn（966 行） | 语法分析.cn（113 行）：解析 + token 工具 | 语法/表达式.cn（287 行）、语法/语句.cn（221 行）、语法/声明.cn（210 行）、语法/顶层.cn（176 行） |
| 语义分析.cn（194 行） | 语义分析.cn（98 行）：语义检查 + 检查 | 语义/符号表.cn（33 行）、语义/内置.cn（74 行） |

### 3. 模块系统适配

- 新增模块声明文件：**词法.cn、语法.cn、语义.cn**（`模块 关键字` 等声明挂载子模块，
  52_library 的 `管理.cn` 模式）
- 更新 **包.cn** 导入全部子模块（词法::关键字 等 9 个子模块）
- **子模块间不写导入**（避免 语法->语句->语法 循环依赖），函数经门面导入链合并后
  全局可见，纯名调用
- **语义门面辅助函数用完整三级路径限定名**（`语义::内置::行类型` 等），避免与
  IR生成.cn/代码生成.cn 同名函数（行类型/行文本/列表追加/函数名）纯名歧义

### 4. 79/78 主.cn 改造（多文件编译）

- `编译模块` 增加 `向量<字符串> 子模块路径们` 参数：读门面源码 + 合并子模块源码后
  统一 IR 生成（保证调用符号与定义符号匹配）
- 主函数为每个组件传入子模块路径列表（词法 3 个、语法 4 个、语义 2 个、IR/代码生成 空）
- 补全 `结果.正常` 检查的 `否则` 分支（C++ 版语义分析器强制检查）

### 5. 验证结果

- **E2E 70~79 全部通过（79/79）**
- **76 expected 更新**（门面文件变小，行数变化：词法 2417→717、语法 1657→492、
  语义 82→50、IR 1989→451、代码生成 7923→1764）
- **79 自举闭环通过**：CN 版组件链成功编译拆分后的门面+子模块合并源码，两次产物
  逐字节一致（自举固定点仍成立）

## 总结发现的问题

1. **CN 版组件链不支持 `::` 限定名调用**（语法分析.cn 解析为独立标识符、IR生成.cn
   硬编码容器方法）——已扩展支持
2. **子模块导入父模块形成循环依赖**（语法->语句->语法）——子模块间不写导入，纯名调用
3. **辅助函数名跨模块冲突**（语义/内置.cn 的 行类型 与 IR生成.cn 同名、语义/符号表.cn
   的 列表追加 与 代码生成.cn 同名）——语义门面用完整三级路径限定名
4. **门面局部变量遮蔽模块名**（`向量<字符串> 符号表` 与模块名 符号表 冲突）——
   用 `语义::符号表::` 完整路径区分

## 当前卡在哪

无阻塞。B 任务第一阶段（词法/语法/语义拆分）已完成，E2E 79/79 全绿。

## 下一步计划

1. **B 任务第二阶段**：拆分 IR生成.cn（1029 行，超 1000 行约束）、代码生成.cn（971 行）
2. 拆分后同步更新 79/78 主.cn 的子模块路径列表、76 expected 行数
3. 全量 E2E 回归 + 文档同步 + Git 提交

## 踩过的坑（绝对不要再踩）

1. **CN 版组件链不支持 `::` 限定名调用**——拆分门面文件前必须先扩展语法分析.cn +
   IR生成.cn，否则 79 闭环编译门面文件失败
2. **子模块导入父模块形成循环依赖**——子模块间不写导入，靠门面导入链合并后纯名调用
3. **辅助函数名跨模块冲突**——语义/内置.cn、语义/符号表.cn 的函数名与 IR生成.cn/
   代码生成.cn 同名，纯名调用歧义，须用完整三级路径限定名
4. **门面局部变量遮蔽模块名**——局部变量名避开模块名，或限定名用完整路径
5. **PowerShell 正则替换易引入反引号**——批量替换后必须重新读取文件确认无非法字符
6. **`结果.正常` 检查后必须处理 `否则` 分支**——C++ 版语义分析器强制检查，否则编译失败
