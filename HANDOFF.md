# HANDOFF 交接文档

**交接时间**: 2026-09-12 第七十六轮（**深度机 linux-x86_64**）——**76-a 移除-覆盖-清空路径所有权全链补齐**（plans/020 靶子「映射 删除(键) 释放被删项字符串」销项 + 同族缺口一族全修）。验证链全绿，固定点新口径 **272094**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 下一轮首选靶子「映射 `删除(键)` 释放被删项字符串」。探针扫描中发现**同族缺口一族**（八枚），按「一劳永逸」一次修全。

2. **八枚缺陷探针实证**（`内存::活动分配数()` 差值 + 内容校验 + 搅动复用压力，修复前后对照）：
   - 映射：删除(键)（交换式覆盖被删槽）/ 设置覆盖写 / 释放内部数组（H8-5 路径漏注入）；
   - 集合<T>：析构/清空（元素串原无释放面）；
   - 向量：删除(位置)/设置覆盖（移位浅拷覆盖旧句柄）；
   - 链表：删除头部/删除尾部/清空（被摘槽出链、链头归 -1）；队列：清空；
   - **映射入容器位未归一化**：借用来源句柄浅存 → 容器析构释放调用方串（**UAF**，74-a 缺陷①在映射上的重演）。

3. **修复设计（Rust 容器所有权模型 + C++ erase/insert_or_assign 就地析构对照）**：
   - **入容器位归一化扩到 映射（值侧）与 集合**（宿主 `isStringValuedMap` 新增、`isStringElemContainer` 加集合；v2 同款）——UAF 销项；
   - **stdlib 挂点按「槽的持有者状态」分工**：
     - `析构被移除(索引)`（新增）= **唯一持有者槽**（被移除槽）——字符串注入 `__cn_seq_free_slot`；有析构类零注入（零行为变化）；
     - 映射 `析构键值/析构值`（删除/覆盖写单槽，`__cn_map_free_slot`）；
     - 既有 `析构元素` 保持「有析构类」语义——**浅拷模型下移位目标槽/末尾移出槽与有效槽重复引用，字符串不注入**（首版修复悬垂实证：探针 76-V1 元素内容损坏）；
   - 全量路径扩到 集合 + 映射 释放内部数组 并入；所有释放点沿用「释放后槽清零」幂等不变量。

4. **连带发现并修复（重要）**：v1 组件 `IR4.cn`「先 `类型栈顶` 后 `类型弹出`」= **借出视图跨容器移除点悬垂**（新释放语义下）——74/75/76 自举链用例实测回归（IR dump 乱码 + 产物行数漂移 117→110、1764→1358）；修复：`字符串复制(类型栈顶(...))` 先取拥有副本再弹出。矩阵新增 **A21 格**（借出视图 × 容器移除，NLL/借用检查轮靶子）。

5. **防线**：E2E **220**（宿主八形态折校验和 **255**）+ **221**（双编译对照，移植纪律 9 第三个；两侧 255 一致；供给.cn 供 集合/映射/链表/队列<字符串> 符号含新挂点方法）。**反证实证**：删除缺释放 → 宿主 254；v2 归一化失效 → v2 侧 127（位128 捕获 UAF）。

6. **验证链全绿**：清场重建零警告（4 处匹配=文件名含 error 既有口径）+ 单测 1289/1289 + E2E **243 用例 238 过**（唯一败=194 既有待批）/4 跳 + 固定点 **272094 行**（新口径）逐字节一致 + 绑定自检 + 对拍 29/29 + 释放路径两侧对照（脚本已扩 76-a 挂点清单与清零计数）。

7. **文档五件套**：plans/020（矩阵 A21/B32/B33 + A14/B30 更新 + 防线表 + 第七十六轮段）+ plans/019（跟踪表行）+ lessons（第七十六轮段，权重 8/8/7/6）+ 更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子**：① **A21 借出视图 × 容器移除**（NLL/借用检查轮——本轮登记的最大安全面；v1 组件已修，全树审计问句「借出视图在容器被修改前还用不用」）；② 数组元素（`字符串 数组[N]`）借用面；③ 装箱字段接收（`r.值` 读出）无 RAII；④ 结构体字段持串无 RAII；⑤ 结构体容器字段「声明即用」既有边界；⑥ 集合<有析构类> 删除前移的旧对象析构（既有边界）；⑦ 字符串键（`映射<字符串,...>`）支持轮（键侧归一化/释放）。
2. **呈报清单余项**：194 单行批准（已呈报多轮无答复）；win 平台 v2 并行隔离（家机轮）；71-a 后续可选题；阶段 4 第二层第三批收口（不安全区变硬错误）。
3. **矩阵维护纪律**：每轮触碰所有权语义的改动必须同步更新 plans/020 对应格。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（4 处匹配=文件名含 error 进度行既有口径）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 243 用例 238 过/唯一败=194 既有待批/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 锚定链（v2 树或宿主编译器改动后须删 v2p 缓存键重建）→ 固定点 272094
bash target/p71x64l/cmp29_71.sh      # 对拍 29/29
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（含 76-a 挂点清单，人工核对三列）
```

> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p（验证假绿）。

## 四、本轮踩坑（已入 lessons 第七十六轮段）

- **浅拷赋值模型下「移位槽/末尾移出槽」是重复引用——释放即悬垂**（首版修复实证：给所有移位槽注入释放 → 元素内容立即损坏）。**释放只允许发生在「句柄的最后持有者」槽**；新增释放点必须先回答「这个槽的句柄还有没有别的持有者？」
- **「容器拥有元素」×「借出视图」是必须显式处理的交互边**：`元素()/读取()/栈顶()` 返回容器内借出视图，跨容器移除点存活即 UAF（v1 组件「先读后弹」实证）。Rust 借用检查器静态拒绝；CN 现阶段=显式复制（NLL 轮靶子）。
- **stdlib 语义改动的连带面含「参照产物绑定型用例」**：74~77 自举链用例的期望与 v1 组件行为/产物形状绑定——改 stdlib 语义后必须复跑；**基线对照实验**（`git stash` + 重编译 + 复跑）是区分「既有失败 vs 本轮回归」的唯一可靠手段（本轮凭它确认 74/75/76 是回归）。
- **挂点命名承载语义**（`析构元素`=移位槽 / `析构被移除`=唯一持有者槽）——同名或含糊会把注入方向写错（双重析构或悬垂，均有静默形态）；宿主分派用布尔变量显式命名（`strOwnedSlot`）。
- 用例：`结果` 检查须规范形态（`如果 (!r.正常) { ... }` 或 `如果 (r.正常) { } 否则 { }`——后者才无「未处理错误分支」警告）；`集合<T>` 无元素读出接口（仅 添加/包含/删除/清空/大小）。

## 五、诚实边界

- **A21（借出视图 × 容器移除）**：CN 无借用检查 → 使用者责任（跨容器修改点先复制）；全树已扫描（v1 修 1 处、v2 树 A2 纪律 0 命中），但**新模式可能出现**——新增代码须自审该问句；NLL 轮系统解决。
- 集合<有析构类> 删除前移的旧对象析构（既有边界）；数组元素/装箱字段/结构体字段持串/静态面无 RAII（plans/020 靶子维持）；字符串键当前不可编译（键侧随支持轮）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。
- **本轮反证实证用的缺陷副本已恢复**（检测注入残留=0）；探针留 `/tmp/p76probe` 不入库。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **272094 行**、cn_self71）。
- 探针（/tmp/p76probe，不入库）：probe.cn/probe2~6（各形态基线）、probe4（映射/集合 A~G）、probe5（容器族 V1/V2/L1~L3/Q1/S1）、e5（向量删除分解）、反证副本（映射集合.cn.bak、IR容器.cn.bak）。
- E2E 用例：
  - `tests/e2e/220_映射集合所有权移除路径/`（**八形态折校验和 255**，宿主侧；退出码恒 0）；
  - `tests/e2e/221_双编译对照映射集合所有权/`（**八形态，两侧退出码须一致=255**；供给.cn 含新挂点方法符号）。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（长期维护台账——A21/B32/B33 新增、A14/B30 更新；每轮触碰所有权语义必须同步更新）。
