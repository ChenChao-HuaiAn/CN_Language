# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本会话总交付（287-a 开发轮·用户裁决方案A·2026-09-17）

**任务**：根治 T13（linux_x64 后端 O0 移位发射 i32 零扩展污染）——承接本会话前半 M3 采样测试轮的 s2026091705 定案（用户裁决「方案A」）。锁纪律：等单位机 286-a 交锁后认领（38c15fe 🏃·基线 7962579）。

1. **根治**：`LinuxX64CodeGenerator::emitShift`（linux_x64_instructions.cpp）i32 非常量路径补 `movsxd r10, r10d`——对齐 270-a T14（emitCast 整→浮）同族先例（「姊妹算符照妖镜」第 2 例：T14 修 cast 面时未审 shift 面）。免疫面实证：i8/i16 装载已 movsx、u*/i1/常量（mov reg,imm64）、shl（低 32 位）、shr（无符号）；win（sar eax）/arm64（asr w9 宽度化）/v2（D34 出口规范化）三后端零改动。
2. **验证链（全绿）**：构建零警告；单测 1351/1351；**E2E 全量 422=422/0/0**（含新 403+79 自举闭环=win 锚定链逐字节不变证明·win 后端零改动）；CLI 矩阵 50/50；面覆盖 57/60 PASS；修后探针 linux .s=4/4 sar 全部 movsxd 前置+修前修后 diff 仅 4 行插入（最小改动反证）；采样回归轨修后 38/38（无回归）。
3. **新 E2E 403_移位负值矩阵**：11 行矩阵（i32 负值变量/常量移位量 sar+shl+正值零回归+i16/i8 免疫+u32 shr+mod 取模+污染值判定面）——expected win 实跑固化·coverage_map 4.4 边界例列登记。
4. **甄别定案存档**（前半验证轮 82bdbdd）：s2026091705「变7=1/0」实为 **O3 对（0=规范值·plans/001 移位取模语义）O0 错（1）**——285-a「O0 对 O3 错」与 4ad94f6「修优化层」两次判定反转；真凶=零扩展污染非 T12 折叠路径。T12 行已在 plans/021 修正关闭、T13 行销项（287-a）。
5. **v2p 实证**：v2 对 s2026091705 的 linux asm（重建 p206c/v2p）=64 位装载+D34 槽值符号正确→sar 免疫·**v2 树零改动成立**。**附带观察项（新·未立案呈报）**：该 i32 移位掩码走 `and ecx,63`（mod 64）非 31——此样本巧合等值（13），移位量 ≥32 形态疑似 mod 语义分叉（v2 IR 类型推导面）——待独立甄别轮，本轮不动。
6. **写集**：src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp（emitShift 一处）｜tests/e2e/403_移位负值矩阵/（新）｜tests/e2e/coverage_map.md｜plans/021（戳+T12 修正+T13 销项+§5 索引+§6 瘦身）｜plans/025 §1.0｜HANDOFF/更新日志家机节｜看板。探针资产 target/p287probe/ 留档。

**踩坑与教训**：①「O0=参考实现」默认假设陷阱——O0 同样可能有直译层缺陷，规范才是唯一判准（深度机两次判定反转根因·lessons 已记）；②asm 甄别 grep 到指令名（sar）≠发射正确，必须核操作数宽度（r10 vs r10d）；③管道洗退出码再犯（check_handoff FAIL 被 `| tail &&` 洗掉·提交后才见——lessons 已有·执行面仍需警惕）；④v2p 输出路径硬编码 target/v2asm.asm·须以项目根为 cwd 位置参数调用。

**下一轮候选**：①深度机跨机项：X64L 锚定链重锚（linux 产物因 movsxd 变化）+s2026091705/403 运行级复验；②v2 移位量掩码 63/31 分支疑似 mod 语义分叉甄别（独立轮·探针=移位量 33 形态 win/v2p 对拍）；③T22 arm64 位立即数（单位机已呈报待裁）；④C20 波2 批B（家机·前置已清）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-17 🏃 292-a 开发轮收工（用户裁决方案 A）——**402→405 用例改号（解除编号撞车）**。接手前先 `git fetch`。
**⚠️ 同机双会话提示**：本机同刻另有一会话的 🧪 M3 采样轮在飞（daemon `--rounds 0 --samples 250 --sleep 90`·写月日志/命中库/面台账/§2.1）——其收工将整节替换本节；292-a 明细以 git 提交与本轮记录为准。

### 一、本轮做了什么（292-a·基线 3cdf945·用户对 §3-B 呈报裁决方案 A）

1. **改号落地**：`tests/e2e/402_验证IR旗标触达` → **`405_验证IR旗标触达`**（git mv·用例零逻辑改动；405=403/404 后下一空闲号）；家机 `402_布尔短路矩阵O3` 保持不动——编号序列 402/403/404/405 四用例无重号，撞车解除。
2. **登记面同步**：coverage_map 豁免区登记名 402→405+改号注记；lessons 第 291-a 轮条目加改号注记；旧产物 `target/402_验证IR旗标触达{,.o,.s}` 清理；`tests/matrix/覆盖基线.txt` 随门禁自动刷（3→2）。
3. **021**：§3-B 呈报迁出（生命周期铁律）+§4.2 销项台账+§5 索引+文首戳 292-a。
4. **rebase 插曲（已闭环）**：`--autostash` 与采样轮 daemon 的在飞月日志冲突——手工合并（两侧轮次块全保留+时间排序·18 块单调有序），该文件保持未提交（属采样轮写集）。

### 二、验证链（本机复现口径·全部当轮实测）

```
ls tests/e2e/ | grep -E '^40[2-6]'                            # 402_布尔短路/403/404/405 无重号
python3 tests/e2e/run_e2e.py --cn target/cn --filter 405       # PASS
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8           # 423=421/0/2
python3 scripts/check_spec_coverage.py --strict                # PASS（405 登记·退码 0）
python3 scripts/check_matrix_coverage.py                       # PASS 58/60（未测 2）
python3 scripts/check_handoff.py && python3 scripts/check_progress_sync.py
grep -r "402_验证IR" --exclude-dir=target .                    # 仅历史记录性引用
```

### 三、下一轮任务（按序）

1. **下一空闲编号 = 406**（403/404/405 已占）——新增 E2E 用例从 406 起编号。
2. 家机 290-a（C20 双测上架）收工后：`git fetch` 复跑全量（用例数将增）；深度机侧 C20 波2 批C/批D 候选。
3. 深度机候选：①T21 待规范裁决 ②B8 面台账巡视续行 ③daemon 常态化（M3 采样轮在飞中）。
4. 用户待裁项：单位机 **B10**（三元 ? 歧义代价面）呈报在 `plans/021` §3-B。

### 四、诚实边界

- 本轮为**用例改名轮**：编译器源码与其他测试用例零改动；三文件 git 识别为 rename（逐字节不变）。
- **同机双会话的协议空白**：AGENTS §8 看板模型假设「一机一会话」——本轮以「复合行（🏃+🧪）+写集隔离+收工恢复采样轮行」兜底；**建议后续补「同机多会话」条款**（登记待办）。
- 405 编号已在 coverage_map 豁免区登记为 405——他机若引用旧名 402_验证IR 需注意（家机/单位机收工时 fetch 即见）。

## 单位机 ARM64 节

**交接时间**：2026-09-17 🧪 M3 采样轮③收工——daemon 全绿+B8 行 13 三元扩面**命中 T25**（浮64 三元 -O3 值归零·IR+asm 双铁证·立案呈报待裁）。接手前先 `git fetch`。

### 一、本轮做了什么

1. **M3 采样 2 轮全绿**：293 样本×2 轮（200 新种子+93 回归轨）一致 293/分歧 0/编译失败 0/运行失败 0。
2. **B8 行 13 三元 15 形态三方对拍**（gcc C oracle vs CN-O0 vs CN-O3）：右结合/括号嵌套/值位消费/布尔结果/逻辑组合条件——O0 全对=C 一致。
3. **命中 T25**：**浮64 三元（非常量条件）-O3 结果静默归零** 6 形态（直印/赋值/假条件/变量分支/算术/乘法——`c ? 1.5 : 2.5` 出 0.000000 应 1.5·O0 全对）；例外面=整32 ✓/浮32 变量赋值 ✓/常量条件折叠 ✓。
4. **双铁证**：①O3 IR——优化层把汇合 Load 重写为前驱块尾「搬运 %v7 (f64)」（256-a F1-26 SSA 保守使用点重写路径）；②O3 asm——bb1/bb2 的浮点 Copy 写槽 [#x29-88] 而 bb3 消费 `ldr d0, [x29,#-64]`=**槽位分派不一致·读未初始化槽=0**（256-a 重写×arm64 RA 交界缺陷）。
5. **附带定性（非缺陷）**：三元条件布尔强类型=无数值→布尔隐转（Rust 同款·与如果/当口径一致）——01b §5.8 未明写条件须布尔=文档缺口归 F2-37；探针 3 形态按设计拒绝后布尔化重跑。
6. **T25 立案**（plans/021 §3-G+处置段）：修复=Copy 浮点发射链三后端同查（fmov/槽一致性·同族旁路穷尽纪律）+负测 E2E 转正；面台账行 13 注记。

### 二、发现的问题

- **T25（呈报待裁）**：P1 优化缺陷·静默错误代码（比崩溃危险）——arm64 实测，优化层重写疑平台无关，win/x64l 发射槽分派各异须各机复验。
- **T24（上轮·维持呈报待裁）**：后缀自增表达式值语义。与 T25 同挂「表达式值语义+发射链」面。

### 三、下一轮候选

1. **M3 常态采样轮④**（用户令不停）：B8 候选行 22（编译期常量）/行 17（优先级结合性）/行 8（算术面）arm64 矩阵。
2. **T24/T25 裁决后根治**（可同轮：表达式值语义+浮点 Copy 发射链·本机可认领开发轮）。
3. A12 里程碑标签锚点搭车。

### 四、验证链（本机复现口径）

```
python3 scripts/cnsmith_daemon.py --rounds 2 --samples 200   # 293×2 全绿（月文件在案）
# 三元 15 形态+浮点族 9 形态三方对拍+mini IR/asm 对比（矩阵=plans/025 §三.14）
python3 scripts/check_handoff.py; echo 退出码=$?   # 独立验证（292-a 教训）
git diff --cached | grep -c 峰形冲突标记        # push 前=0（字面标记略·防示例误报）
> github 镜像本机无凭据未推（既有口径）。源码零改动→全量门禁不重跑（c077630 基线承载）。
```

### 五、诚实边界与坑

- T25 浮32「h07 对」可能为形态巧合（变量赋值路径）——根治轮须穷尽浮32 全消费位；下标/返回位浮点三元未入矩阵。
- T25 根因定位到「重写×RA 交界」证据链完整，但**修复方案（改重写侧 or 改 RA 槽分派侧）待根治轮甄别**——呈报方案时给两路对比。
- 三元条件布尔强类型=按设计（非缺陷）——勿在后续巡视中重复立案；规范条文缺口已归 F2-37。