# HANDOFF 交接文档

**交接时间**: 2026-09-04 第十四轮（家机 win-x64）——**缺陷零容忍收口轮：引用返回全链+裸引用+下标绑定+无符号窄宽**。用户审计揪出上轮 HANDOFF「另立案/待同步扩/不变」式搁置条目违反尽善尽美/缺陷零容忍/一劳永逸——四件立即根治；连带根治宿主引用返回读值缺陷；新立案呈报一件（根因未明）。

## 本轮已完成（全部有实证）

### 一、引用返回全链（宿主 P3-18 对齐，E2E 150 六步闭环）
- 语法层：返回类型 `&` 并入（`-> 整64&`）；语义层：`是引用返回调用` 判定（符号表返回类型尾 &）+赋值目标/引用初始化两白名单扩（普通调用仍硬拒，负路径实测「赋值目标须为可写左值」）。
- IR 层：函数IR.返回引用 标记；返回语句 生成左值地址（被调方 rax=左值地址）；调用点默认右值读（IR_指针加载+剥 & 基类型登记）；生成左值地址 调用分支（回扫 IR_指针加载.操作数1=左值地址——赋值目标/复合赋值/绑定复用）。

### 二、【连带揪出·当场根治】宿主引用返回读值缺陷
- 现象：`整64 a = 取值(p);`（引用返回调用作右值）读出**地址**当值（sanity exit=11 实锤）——visitCallExpr 对引用返回调用返回裸 ptr，被一切右值消费=静默错误代码；宿主 P3-18 原只实现赋值目标路径。
- 修复：`suppressRefDeref_` 标志（ir.hpp）——读值默认 lvalue-to-rvalue（LoadPtr，C++ 语义）；赋值目标/复合赋值（ir_expr）/返回转发（ir_stmt isRefReturnFn）/引用局部绑定（ir_stmt callInit）四处上下文抑制。

### 三、裸类型引用局部（宿主 parser 形式6 + v2 对齐）
- 宿主：`typeRefVar`（`标识符 & 标识符`；a&b 位与歧义与 a*b 同款既有惯例）——exit=207 全对（结构体引用读写/引用传参/标量引用写回）。
- v2：局部声明检测 `标识符 & 标识符 =` 形态（`=` 前置消歧，较宿主更严）+变量结构体表引用登记。

### 四、引用初始化下标 + 正8/正16 无符号窄宽
- `整64& 绑定 = 指针[0]`：语义白名单扩（IR 生成左值地址 指针下标分支既有）。
- 正8/正16（原「显式拒绝」系无符号语义缺位搁置）：T_正8=18/T_正16=19 零扩展标注+win movzx/ARM64 uxtb 对称——正8(200)=200。

### 五、呈报挂起①：v2 常量系统同值常量诡异触发（根因未明，等用户裁决）
- **现象**：IR常量.cn 新增任意常量对（八组名×值消融：14/15/16/17/18/19/98/99 × T_正8/U_正8/U正8/U正九/U8/零宽码甲/U正宽8）使冒烟链（语法分析/语义分析/IR容器——共用 IR容器.cn 链）IR 生成各多 1 条「方法调用的对象类型未知: 元素」（DBG 定位 fn=发射容器字段析构）→ IR 层中止纪律触发 → 冒烟 asm 断供。**147~150 全 PASS（产品正确性不受影响）**。
- **归因困难**：八组消融全触发、唯一不触发组合（T_测试甲=98/乙=99）系消融污染态假反例（该组实验时代码生成已被前序消融字面量化）；初判「撞值 14/15（IR_取余/IR_位与）」被换值 16~19 仍触发推翻；最小复现探针（独立源码）不复现=上下文依赖（链序/全局表）。
- **方案**：A 专项深挖（dump 节点池/字段类型表/签名表现场定位 解析方法对象类型 返回 0 的路径，约半轮~一轮）；B 常量值域隔离纪律（T_/IR_ 值域文档化+新常量值唯一性约定，规避不根治）；C 常量收集加重复值编译期校验（防患但不解释既有现象）。**推荐 A**。

### 六、呈报挂起②：簇⑥ cn check 非入口文件假绿（上轮遗留，等用户裁决）
- `cn check 非主.cn 且含导入` 时私有函数体被模块可见性过滤静默丢弃=检查没发生但报通过。方案：A 单文件也全量检查（约 1 轮，推荐）；B stderr 提示可见化（半轮）；C 拒绝非主.cn 入口（半轮）。

### 七、裁决入档
- **v2 组件拆分（规则3 单一职责，IR生成.cn 2535 行等超标）**：用户批准**方案 A——自举闭环后统一拆分**（演进期拆分边界不稳定，拆两次成本更高）。

## 下一步
1. **呈报①②用户裁决**（常量诡异触发三方案/check 假绿三方案）。
2. **代码生成.cn/代码生成共用.cn/代码生成ARM64.cn/主.cn 冒烟推进**（v2 自举最后四个组件）——「v2 编译 v2 自身」对拍（79 同款）是终点。
3. **linux-arm64 复验轮**（单位机）：147~150 四用例 linux 侧对齐+本轮变更（含 uxtb/uxth）全量 ARM64 复验。
4. **B1a 契约分叉**（stdlib 包装 vs 内置直调）——随 stdlib 并入专项收口（既有裁决不变）。

## 灰色点披露（更新）
- ~~裸类型引用局部~~（本轮根治：宿主形式6+v2 对齐）；~~引用初始化下标~~（本轮根治）；~~正8/正16~~（本轮根治：movzx/uxtb）；~~v2 引用返回~~（本轮根治）。
- v2 引用返回**引用返回调用的引用初始化已支持**；宿主白名单另有「解引用」形态 v2 已有——两白名单现已对齐（宿主含 引用返回调用，v2 亦含）。
- 78/79 v1 链内存失控——已知遗留勿翻案。

## 踩过的坑（本轮，全部入 lessons 详录）
- **消融实验必须原子化**（快照→单变量→回滚）——本轮跨组状态泄漏产生假反例，误导归因 2 小时。
- **「改 A 出 B」先跑独立最小复现**——探针不复现=上下文依赖，直接 dump 中间状态比黑盒消融快一个量级。
- **批量字面量化/重命名的恢复须全仓引用清单对账**（代码生成两文件漏恢复致 movzx 死代码追查半轮）。
- **宿主特性移植 v2 前先跑宿主全形态 sanity**（P3-18 只验了赋值目标，读值缺陷第一版 sanity 即暴露）。

## 门禁状态（第十四轮最终，家机 win-x64）
- 构建零警告（MSVC /W4 /WX）+ 单测 **1223/1223** + E2E 全量 **152 用例 150 过/2 败**（78/79 已知 OOM 家族与基线一致零回归：78=0xC0000005、79=内存超限 4137MB）；新增 **150_v2_引用返回与裸引用与无符号窄宽** 随门禁绿（147/148/149 前轮用例维持绿）；词法/IR签名/IR生成 冒烟 0 诊断维持；**冒烟回归异常**：语法分析/语义分析/IR容器 各 1 条「方法调用的对象类型未知: 元素」（呈报挂起①，asm 断供影响冒烟 QA 不影响产品）。
