# HANDOFF 交接文档

**交接时间**: 2026-09-12 第七十八轮（**深度机 linux-x86_64**）——**78-a v2 用户自定义类支持全链**
（77-a 扩展③遗留「v2 类支持专项」销项：探针 D 实证 v2 对用户自定义类整体不支持；
v2 侧新增 4 文件 + 改 9 文件，**宿主零改动**）。验证链全绿，固定点新口径 **301619**。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案**：HANDOFF 下一轮任务 #1「v2 类支持专项（77-a 扩展③遗留，探针 D 实证）」。
   用户令：遵守 AGENTS.md 第3项纪律、参考 Rust 经验给最适合方案（**性能第一、安全第二**）。

2. **缺口实证**（探针 D 复现）：v2p 编译最小类程序报 3 错（未声明的类型: 盒子 / 赋值目标
   未声明: 值 / 未声明标识符: 值）——根因=**v2 语法层不解析 `类` 声明**（AST 节点表只有
   节点_结构体；顶层循环 `否则 { 位置++ }` 静默跳过）。对照：同结构体程序 v2 全链通过。

3. **三方案裁决（性能第一 / 安全第二）**：
   - **甲（实施）v2 类支持全链**——类声明（语法）→ 类型登记（布局复用结构体表）→ 字段表 →
     方法体检查（第三类函数级作用域单元）→ IR 生成（this 位0/字段读写/自身/构造/方法调用/
     RAII/析构字段级联）→ 代码生成（**零改动**复用 IR_分配对象/IR_删除对象/IR_调用参数化）。
     对象模型与容器同款（堆对象指针+方法符号），Rust 对象模型经验。
   - 乙（结构体+命名约定模拟类）：编译器级改写层新分叉面 → 落选。
   - 丙（只做语义检查面）：类程序仍编译不通过（无实际可用面）→ 落选（违反全链纪律）。

4. **实施（v2 侧新增 4 文件 + 改 9 文件；宿主零改动）**：
   - 新增：`IR/IR类.cn`（类元数据表）、`语法/解析类.cn`、`语义/语义检查类.cn`（检查方法）、
     `IR/生成类.cn`（生成方法/类对象局部/调用发射/析构字段级联注入）。
   - 节点协议：节点_类(40) 子区间=类体；节点_方法(41) 与函数节点同布局（函数参数串/参数登记复用）；
     节点_类字段(42) 与方法参数节点区分。
   - 布局登记**完全复用结构体设施**（登记结构体布局/字段类型/字段偏移/容器字段）；字段访问/
     对象大小/宽度/RAII 全链零新代码。
   - 方法符号两侧同公式：类$方法[#参数串]/构造=类$类名[#参数串]/析构=类$~类名。
   - 借出检查挂载方法体（77-a 扩展③收口）：新增「借出检查类名ID」上下文 + 查声明类型 类字段回退
     （`表.元素(0)` 的 表=字段现可解析）。
   - 修复：类对象成员访问的**指针语义**（槽存对象指针→基址=Load；结构体值语义走取槽地址）——
     首版段错误实证（E2E 228 gdb 栈 `向量$字符串$追加`）。

5. **防线**：E2E **228_双编译对照类支持**（移植纪律 9 **第五个**对照用例，六形态折校验和 31
   两侧一致）+ **229/229_v2 类方法体借出视图拒绝**（同源负测）+ **227_v2 期望同步**（类缺口
   销项后两边界均真拦截——用例价值提升，72-a 先例：实施+同步更新用例期望）。
   **反证实证**：注入「是用户类 恒假」→ 228 失败；注入「借出检查方法体挂载失效」→ 229_v2 失败
   （两侧已恢复，残留=0）。**两侧对照实测**（探针 H1~H11 各编各跑：退出码/输出/分级分配计数
   全一致）。

6. **文档五件套**：plans/020（第十节第七十八轮更新 + 矩阵格 B34/B35 + A21 补注 + 防线表）+
   plans/019（跟踪表行）+ lessons（第七十八轮段，权重 8，含五条新预防 ㉚~㉞ + 验证链口径）+
   更新日志 + 本文件。

## 二、下一轮任务（按序）

1. **plans/020 矩阵靶子（余项）**：① 数组元素（`字符串 数组[N]`）借用面；② 装箱字段接收
   （`r.值` 读出）无 RAII；③ 结构体字段持串无 RAII；④ 结构体容器字段「声明即用」既有边界；
   ⑤ 集合<有析构类> 删除前移的旧对象析构（既有边界）；⑥ 字符串键（`映射<字符串,...>`）支持轮；
   ⑦ **本轮新登记**：字段容器接收者的入容器位归一化（`甲.记录.追加(...)` 走保守复制——H7/H11
   两侧同残留 1；靶子=拥有契约接管，须两侧同改）。
2. **v2 类支持第二版（可选专项）**：继承/虚函数/接口/泛型类/运算符重载/访问控制强制/类对象
   拷贝构造/方法返回结构体——第一版均显式诊断（未支持面不静默）；实现面参考宿主
   parser_oop/class_resolver/ir_oop 族（vtable 首槽+接口分派区布局）。
3. **呈报清单余项**：win 平台 v2 并行隔离（家机轮）；阶段 4 第二层第三批收口（不安全区变硬
   错误）；v2 诊断无行号（v2 节点池无行号字段——架构级小改动牵动面广，需评估）。
4. **跨机轮**：arm64（单位机麒麟）复验本轮类支持与 3 个新用例（228/229/229_v2，linux-arm64 平台）。

## 三、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）——类支持改动后重建（E2E 缓存指纹自动处理）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306（宿主零改动）
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 256 用例 252 过/0 失败/4 跳
rm -f target/audit2/v2p_build_key_linuxx64.txt && bash target/p71x64l/run_chain71.sh   # 固定点 301619 逐字节一致
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**（共享 work 目录）
bash scripts/cn_release_parity.sh    # 移植纪律 8：释放路径两侧对照（人工核对三列）
```

> **验证链命令一律串行**：`cmp29_71.sh` 与 `run_chain71.sh` 共享 `target/p71x64l/work/`
> （p71.s/s71.s/cn_self71），并发会踩出假警（串行复跑正常）。
> 注意：E2E runner 的 v2p 构建缓存按 v2 源码指纹；**手工替换 `target/audit2/v2p_linuxx64` 后须同时
> 删缓存键**（`target/audit2/v2p_build_key_linuxx64.txt`），否则 runner 复用旧 v2p（验证假绿）。

## 四、本轮踩坑（已入 lessons 第七十八轮段）

- **㉚ 对象语义的成员访问必须分派「槽里存对象还是对象地址」**：v2 结构体=值语义（取槽地址）、
  容器/类=指针语义（Load 指针值）；类对象沿用值语义路径=把栈地址当 this 传 → 首版段错误
  （H1~H3 单形态未暴露，228 多形态组合触发）。
- **㉛ 级联释放必须注入「析构体内部」而非释放调用点**：注入到 IR_删除对象 展开处=析构体访问
  字段时字段已释放（顺序反了）；宿主 injectFieldCascadeDestroy 在 ~类 体末（C++ 成员析构语义）。
- **㉜ 新函数级单元接入既有检查器先问「哪些解析入口依赖『局部声明』假设」**：借出检查的接收者
  类型解析只认局部声明 → 类方法体字段名 miss → 整段检查静默失效（H5 初测 v2 编译成功而宿主拒绝）。
- **㉝ 用例期望的「缺口固化」须在缺口销项时同步并甄别方向**：227_v2 期望含 3 条类缺口诊断 →
  销项后全量 E2E 唯一失败即此；判据=新诊断更强/更准（本例升级为两条真拦截）→ 同步更新。
- **㉞ v2 新文件首编译「未声明标识符 'X::设置'（模块 'X' 未加载）」= 函数参数漏写**：诊断文本
  与根因错位族；先对齐形参与调用点实参表。
- **㉟ 新资源类型先找「形状相同的既有 RAII 路径」**：类对象析构复用容器 RAII（三列核对一致）→
  移植成本近零；析构体内容（字段级联）单独注入。

## 五、诚实边界

- **v2 类支持第一版不支持面（显式诊断）**：继承/虚函数/接口/泛型类/静态成员/常量成员/运算符
  重载/访问控制强制（语法层）；类对象拷贝构造/方法返回结构体/结构体字段整体赋值（IR 层）。
- 字段级联仅覆盖容器/结果/有析构用户类字段（字符串字段不级联=宿主同口径）。
- 类对象空指针字段访问=段错误（宿主=受控错误码3）；字段容器接收者入容器位归一化=保守复制
  （H7/H11 两侧同残留 1，靶子登记）。
- 借出检查跨函数别名：内联形态 `f(表, 表.元素(0))` 与经结构体/全局中转不覆盖（77-a 既有边界）。
- win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。

## 六、关键产物位置（target/ 不入库）

- 深度机：`target/p71x64l/`（run_chain71.sh、cmp29_71.sh、work/fix_p.s=fix_s.s **301619 行**、cn_self71）。
- 探针（/tmp/p78probe，不入库）：D1/D2（缺口实证）、H1~H11（两侧对照：H1 基线/H2 析构/H3 带参构造+
  自身/H4 容器字段/H5 借出失效/H6 宿主级联 asm/H7 内存断言/H8~H10 对照/H11 分级打点）、
  v2p_test（新 v2p 副本）、IR类.bak/语义检查类.bak（反证注入副本）。
- E2E 用例：
  - `tests/e2e/228_双编译对照类支持/`（六形态 31 两侧一致；配置 `双编译对照.txt`）；
  - `tests/e2e/229_类方法体借出视图拒绝/`（宿主负测，锚「借出视图 '借出'（来源容器 '表'…行15 删除…行17」）；
  - `tests/e2e/229_v2_类方法体借出视图拒绝/`（v2 负测，诊断锚+中止行）；
  - `tests/e2e/227_v2_借出视图扩展边界拒绝/`（期望同步：两边界真拦截）。
- v2 新文件：`CN语言编译器v2/IR/IR类.cn`、`语法/解析类.cn`、`语义/语义检查类.cn`、`IR/生成类.cn`。
- **plans/020-拥有权流转矩阵与内存安全防线.md**（第十节第七十八轮更新——B34/B35 格、
  A21 补注、防线表；每轮触碰所有权语义必须同步更新）。
