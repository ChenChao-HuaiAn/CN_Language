# HANDOFF 交接文档

**交接时间**: 2026-09-11 第六十八轮（**深度机 linux-x86_64**）——**A2（阶段4' 第三批正文）两轮落地**（方案甲 用户批准「全部实施」）：轮1 宿主侧 `0ed437b` + 轮2 v2 侧 `83b26ac`。**1 个缺陷立案待用户裁决**（自举第二代）。

---

## 一、本轮做了什么

1. **A2 盘点呈报→用户裁决方案甲「全部实施」→ 两轮落地**：
   - **轮1（宿主，0ed437b）**：语义四件套（retOwnedString 决议标记全路径置位+登记白名单∪返回类型契约+返回位借用拒绝[泛型豁免]+返回位字面量拥有化+字符*→字符串收紧+赋值位借出拒绝）+驻留文本改字符*+三树迁移（v2 96/v1 18/tests 5）+静态向量追加悬垂根因修复（模块路径们——多文件符号全瘫六用例）+E2E 210 正负测+门禁全绿。
   - **轮2（v2，83b26ac）**：IR容器 登记恢复+契约+同码/IR布局 驻留文本字符*+字符串复制内置/生成声明·语句 契约/语义检查 返回位+收紧/返回位字面量拥有化+210_v2（校验和 63）。
2. **门禁终态**：清场重建零警告+单测 1289/1289+E2E 231 用例 226 过（唯一败=194 既有待批，4 平台跳）。
3. **文档**：plans/019 三行（呈报/落地/立案）+更新日志+lessons 权重8（git 纪律+调试基线）+本 HANDOFF。

## 二、下一轮任务（按序）

0. **【等用户裁决】自举第二代缺陷三方案**：甲=专门轮深潜（v2 层插桩 dump 字段偏移表登记/查询键，推荐）/乙=登记面挂起收口（保守泄漏）/丙=v2 侧整体回退重排。
1. 缺陷裁决后：固定点锚定链（target/p68x64l/run_chain68.sh）+组件对拍 29+运行级补 hello（119/173 已 ✓）。
2. 手动释放重叠面收尾批：tests 18 文件 ~106 处 字符串释放（当前靠 cn_free_tracked 防御兜底双重释放——语义债务非设计意图）。
3. 呈报清单余项：194 单行批准；win 平台 v2 并行隔离（跨机轮）；第二层第三批收口时点。
4. 阶段5 等并发库立项；NLL 等反馈期数据。

## 三、验证链

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8; echo $?  # 零警告（rc 双查！）
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 231 用例 226 过（194 既有）
# A2 语义验证：210（六形态）/210 返回借用拒绝（七负形态）/210_v2（校验和 63）
bash target/p68x64l/run_chain68.sh   # 锚定链：当前第二代红（缺陷立案）——裁决后修复
```

## 四、本轮踩坑（已入 lessons 第六十八轮 权重8）

- **未提交工作树上的 `git checkout --` = 迁移成果全失**（asm 二分脚本当恢复机制用）——任何破坏性 git 操作前必须 commit/stash 且核对。
- **中间态二进制对比实验的时间黑洞**：产物无法追溯源码态的结论一律作废（v2p_g 坏/v2p_i 好=宿主中间态伪影）；每次实验前 git add -A+构建后查 rc。
- v2 树加内置（字符串复制）须同步 IR布局 两表（符号+返回类型）——v2 语义未声明豁免=返回类型≠0（驻留文本 假&& 即报未声明实证）。
- 「字符串入容器（向量追加）=指针浅共享」——A2 登记方 free 后元素悬垂（模块路径们 根因）——显式复制纪律，容器 move 语义随后续批次。

## 五、诚实边界

- **自举第二代缺陷**（立案）：cn_self68 编 v2 全树 IR 层 45 个字段偏移未知——第一代全绿/两代解析一致/无堆破坏/单变量二分不根治——固定点+对拍挂起。
- v2 契约登记按纯名查（重载族返回异型=近似，对齐宿主首候选口径）；字符* 类型码与字符串同码（字符* 函数返回字面量=多一次 copy 泄漏安全）；泛型体内返回位/赋值位豁免（T 来源=容器元素借用）。
- 参数/静态维持借用；返回 字符串 变量于 字符* 函数=移出后泄漏（安全方向，生命周期分析随 NLL）；转字符串族入拥有后 tests 手动释放=双重释放靠运行时注册表防御兜底（收尾批清理）。
- 第二批遗留：「二分禁用」注释在 引用返回路径字符串释放（生成语句.cn:241——上轮既有非本轮引入，随自举缺陷一并审）。
- win-x64 v2 并行未隔离（跨机轮）；194 待批维持。
