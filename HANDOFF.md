# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十七轮（**深度机 linux-x86_64**）——**编译期安全专项启动**：plans/019 立项（用户裁决方案 B 值语义强化+渐进检查器路线）+ **阶段 1「显式转移 转移() + 已转移使用检查」双侧全链落地**（E0382 对标·CN 化语法零 Rust 记号）。v2self 固定点复锚 **242022 行 = X64L 新口径** + 组件对拍 29/29 + 运行级 3/3 + 全量门禁 209 用例（唯一败=194 既有待批）+ 单测 1264/1264。

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **plans/019 立项**（用户裁决「按最优解裁决，先写报告再全部实施」）：五阶段路径（①转移✅→②引用逃逸强化→③常量 T& 只读借用+互斥+浅转移优化→④不安全区 不安全 函数 修饰→⑤并发；4' 拥有型字符串另案呈报）+ 安全区分层模型 + 三语法点裁决记录（转移() 内置函数/常量 T&/不安全 函数级修饰）。**第七节跟踪表+诚实边界是阶段推进的权威状态**。
2. **规范同步**（经用户批准）：plans/001 §5.3.1「显式转移 转移()」+ specs/04 第五节内置构造器四构造器并列（正常/错误/某些/转移）。
3. **宿主实施六文件**：scopeMoved_ 作用域平行表（同序解析=遮蔽正确）+ visitCallExpr 表达式位特判（指针/字符串放行、标量拒、拥有类型拒、再转移拒）+ visitVarDecl 声明位 AST 改写（initializer=实参标识符走深拷贝既有路径，**豁免窗口 inTransferRewrite_** 防改写产物常规检查误报）+ visitIdentifierExpr 使用根拦截 + visitAssignmentExpr 左值拒绝 + 转移 内置占位注册 + ir_call 零指令展开。
4. **v2 实施四文件**：符号.已转移 第 5 字段 + 表标记转移/表查已转移/转移实参分级（语义指针变量表先排除指针基名误判——**指针变量符号类型ID=基类型名**）+ 检查调用表达式 特判（**线性顺序天然不误报首次转移**：实参先查后标记）+ 生成调用 转移展开（类型大小折叠同位，弹 2 压回实参值）。
5. **E2E 四用例双通道**：196_显式转移（宿主正测 stdout 2/1/左/55）/197_转移后使用拒绝（宿主负测五形态）/196_v2（rc=7 三形态校验和）/197_v2（负路径 5 诊断+中止行实测固化）。
6. **单测** test_move_check.cpp 13/13（负九正四）。
7. **锚定链**：fix_p 242022 行（239301+2721）→cn_self57→fix_s 242022 行→**固定点逐字节一致** + 绑定自检 cn_main=0x4dca4a∈cn_self57.o .text + 对拍 29/29 + 运行级 3/3。

## 二、下一轮任务（按序）

1. **plans/019 阶段 2：引用逃逸检查强化**（详案在 plans/019 §5.1）——引用局部绑定逃逸（存字段/容器/全局）+返回引用多级间接形态；**stdlib/v2 全树引用面 559 处零误伤预审必须先做**。
2. **两枚既有缺陷立案呈报待用户裁决**（本轮新发现、非转移引入）：①宿主「推断声明+字符串标识符拷贝」打印错分派（`变量 乙 = 甲;` → 打出指针值；显式 `字符串 乙 = 甲;` 正常；根因=推断类型未传 IR 打印分派，46-c 结构体回填同族；修复涉 184 布尔行为契约须裁决）；②v2 语法层不支持 `-> 整64*` 指针返回类型（宿主支持；v2 返回类型解析缺口）。
3. **194 测试改写待批挂账维持**（第五十六轮呈报：第 10 行改 `返回 整32(*q);`，无答复未擅改）。
4. **跨机轮（win/arm64）**：本轮宿主六文件+v2 四文件变更的 win/arm64 复验与口径重锚。

## 三、验证链（下轮接手可复跑）

```bash
uname / 系统确认                # 深度机 linux-x86_64（本行口径）
cmake --build target/build -j 8   # 零警告（本轮增量构建已验证）
./target/cn_unit_tests          # 单测 1264/1264（1251+13 新增 MoveCheckTest）
python3 tests/e2e/run_e2e.py --cn target/cn   # E2E 209 用例（败=194 待批，跳=62/69/78/79）
# v2p 由 E2E v2 闭环步骤重建（target/audit2/v2p_linuxx64=当前源码）
bash target/p57x64l/run_chain57.sh   # 锚定链（注意：[3] readelf 解析段 bug——map 手工判定
#   cn_main=0x4dca4a ∈ cn_self57.o .text [0x402586,0x4E037C)；[6] 段口径错——运行级须按
#   runlevel50.sh 口径=编译→as→g++链接 rt48→运行产物；173 入口实名 173_v2_目录包根撞名成员）
bash target/p57x64l/cmp29_57.sh      # 组件对拍 29/29
# 固定点 X64L 新口径=242022 行（win=246651/arm64=322112 分后端口径不跨后端比较）
```

## 四、本轮踩坑（已入 lessons 第五十七轮条目）

- **打印/打印行语义与直觉颠倒**：打印=println（换行）、打印行=print（不换行）——196 首版按直觉选 打印行 致期望比对失败；写输出类用例先看同形态既有用例（02_control 范本：全用 打印+多行 expected）。
- **锚定链脚本继承已知 bug**：从 run_chain55.sh 复制的 [6] 段口径错（直接运行 cn_self 当被编译程序）；复用跨轮脚本必须通读，当场按 runlevel50.sh 口径修正。
- **v2 语法面未探先写用例**：197_v2 首版含 `-> 整64*`（v2 不支持）与单行函数体（v2 不支持）——为 v2 写用例先过最小探针（宿主≠v2 能力等价）。
- **E2E expected 空文件**：v2 正路径闭环用例 expected 可为空文件（仅校验退出码，194_v2 先例）。

## 五、诚实边界

- 转移支持面：**指针/字符串=任意位置；容器/类/结构体/结果/可选=仅声明初始化位（宿主深拷贝语义）；标量/布尔/字符/枚举/函数指针=拒绝；v2 第一版=指针/字符串全位置（容器/类位全拒随阶段 3 对齐）**。浅转移零拷贝与析构跳过=阶段 3（IR 拷贝路径分派工程）。
- 第一版转移()零运行时指令（纯编译期标记）；容器/类声明位运行行为=深拷贝（与直接标识符初始化逐字节一致）。
- v2 已转移检查面：读值（标识符根）/赋值左值/传参/返回经表达式覆盖；成员/下标左值末端推导兜底检不出=与 195 同族诚实边界（宿主全形态覆盖）。
- 两枚既有缺陷立案待裁（见 二.2）；194 待批维持；win/arm64 复验=下轮跨机轮。
- 探针与脚本留 target/p57x64l/ 不入库。
