# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

### 一、本轮交付（🏃 326-a 开发轮：D38 批1=v2 负测防线补齐·词法/语法层 3 项·波次7 续·基线 5ce0b07·2026-09-18）

**侦查**（d38_scan·17 用例 v2p 直调实测）：**16 项防线缺失**（v2p 编过）/1 项已拒（313）——分组=词法/语法层 4 项+语义/模块层 12 项（批2/批3 待续）。

**批1 三项全转拒**（d38_scan 复跑 4 拒/13 缺）：
1. **323 未闭合块注释**（词法）：`/*` 无 `*/` 时原静默扫到 EOF——补 `注闭合` 判定 → `IO::打印到错误` 诊断行 + `词法错误加一()`（280-a 通道同族·主.cn 聚合守卫中止）。
2. **295 裸分号空语句**（语法）：**双层修复**——解析语句 顶部拒绝（防御纵深）+ **解析块 裸分号防御分支改拒绝**（真正拦截面——首轮实测解析语句 改点被解析块先拦·层级错位发现）；宿主诊断「预期表达式」对齐；plans/015 后独立 `;` 全非法（双分号 `;;` 同面拒绝）。
3. **296 当后分号**（语法）：解析当语句 体前加 `{` 检查——原 `;` 被防御跳过+后续语句静默吞进当体；宿主诊断「预期 '{'」对齐。

**验证**：v2 树零错误编译+单测 1353/1353+全量 E2E（详见提交信息）+**存量零误伤**（v2 树自身无裸分号=自举链实证·全量 v2 用例族全绿）。

**诚实边界**：批2/批3 待续（语义/模块层 13 项：103/109/304/305/317/325/341/343/347/350/363/372/374）；372（主带参）宿主为链接期拒绝——v2 侧入口合成链侦查更深·移批2。

### 二、下一轮（按序）

1. D38 批2（语义层主组：103/109/305/341/350/343/317）；锁被持=win 列采样。
2. T43/T46/T47/T48 待用户裁决；C24 已由深度机 324-d 复验销账 ✓。

### 三、坑与边界

- **改点层级错位**：解析语句 顶部的裸分号拒绝永远不被触达（解析块 先拦）——**「防御分支改拒绝」与「入口拒绝」须同面布置**（本轮双层皆拒=纵深）。
- **竞态观察（低频）**：预热重建 v2p 与用例兜底就绪检查可争 v2p.obj（本轮 79_v2 PASS 后 PermissionError 一次·重跑消失）——登记观察项·非本轮引入。
- v2 词法/语法模块的 `IO::打印到错误` 为内置限定名（无需导入）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-18 🏃 **331-a 开发轮（T11 静默防线三步收官）收工**——协议 v2 分支 `任务/深度机-331-T11静默防线`·基线 865d157·待三平台 try-build 回签后 integrate。本轮触 `src/**`+`tests/**`（三后端发射器+验证器+driver+类型系统）。

### 一、本轮做了什么（331-a·T11 残余双面全销项+B9 双案）

1. **面②（T11·发射器静默→硬错误）**：三后端顶层 dispatch（`x64_instructions.cpp` / `linux_x64_codegen_dispatch.cpp` / `arm64_codegen_dispatch.cpp`）**移 default 改硬错误**——case 体 `break`→`return`（守卫代码置 switch 未匹配路径），`Branch` 显式补 case；**编译期守卫**=GCC -Wswitch / MSVC C4062（删任 case → 构建失败✓ 反证）。**首版踩坑**：守卫代码放 switch 之后而 case 体仍 break → 所有 case 落穿（单测 12 红当场暴露）→ 已入 lessons。
2. **面③（验证器支持面）**：`ir_verify.cpp` 新增 `verifyKnownOpcodes`（44 值全清单·isKnownOpcode 无 default 全覆盖）；driver **双路径**接入——顺带发现并修复 **driver_module.cpp 零验证器调用**（半截机制：模块路径此前绕过 verifyIRModule/verifyConstWidths）。
3. **同族加固（B9 族扩散）**：三后端 `emitOopInstruction` 内层 default（顶层守卫抓不到的内层形态）→ 硬错误；**linux_x64 `emitVirtualCall` 栈参数区根治（T49 ✅ 销项）**——先改硬错误（拒绝不产错值）→ 构造触发形态实证（基类方法内 `自身.虚方法(6 整型参数)`）→ 移植栈参数区（位置分配+`sub rsp`+`[rsp+seq*8]` 写入+恢复·契约与 emitCall 一致）+族矩阵 91/204/285 O0=O3+**E2E 431 转正**。
4. **T50 ✅ 销项**（B9 扩散副产品·426 暴露）：**函数内静态标量初值静默丢**（`静态 整64 甲=11111` → linux_x64/arm64 `.quad 0`、win 正确）——真根因=判定口径分叉：函数内静态 stType 是 IR 名（`i64`），原 `types::isInteger` 只认中文名 → 恒 0；修复=**单一归属** `types::isStaticScalarInitType`（中文名+IR 名双口径）三后端统一调用；**426 转绿**（双编译对照 246/247 两侧同错互证=盲区，已入 lessons）。
5. **T51/T52 ✅ 同轮销项**（服务轮探针发现·i128 族）：T51=i128 复合赋值/自增在 **-O0** 静默失效（`值 += 字面量`/`值++` 值不变·O1+ 正确=级别分叉）——根因=复合赋值六路（标识符/静态/成员/指针解引用）+自增自减公共路径的右值/delta 恒窄类型未宽化（普通二元路径有 Cast·复合路径缺）；修复=`widenCompoundRhs` 单一归属（128 位目标且右值类型不同→插 转换）+六路+自增接入；T52=函数内静态 整128 槽尺寸 8 字节（IR 名 `i128` 未入双 quad 判定=T50 判定分叉同族）→三后端双口径修复（`.quad 0×2` 16 字节实证）；残余=**T46**（128 位静态**初值**发射缺失·待用户裁决·本轮不擅动）。
6. **门禁**：零警告构建 + **单测 1363/1363**（+7 OpcodeDefense +3 StaticInitType）+ **E2E 445=443/0/2**（含 431 新用例）+ 锚定链 78/79 绿 + 防线零触发（E2E 日志零「未支持操作码/形态」）+ CLI 注入反证（exit=1·诊断精确·无产物）。

### 二、验证链（当轮实测）

```
cmake --build target/build -j8                                        # 零警告（GCC -Wall -Wextra -Werror）
./target/cn_unit_tests                                                # 1363/1363 PASS
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8                  # 445=443/0/2（含 431 虚调用栈参数矩阵·2 跳=平台固有）
# 反证①：临时删 arm64 Phi case → error: enumeration value 'Phi' not handled in switch [-Werror=switch]
# 反证②：IR 注入 static_cast<Opcode>(9999) → cn build → exit=1「主:bb0: 未知操作码（枚举值 9999）」+无产物
# T50 探针：静态 整64 甲=11111/乙=100000000000/整32 丙=22222 → .quad 直存·运行 1111110000000000022222
```

### 三、下一轮任务（按序）

1. **T49 残留边界收口**（v2 侧虚调用栈参数同型面核查 + linux_x64 i128 栈参常量实参口径）；
2. **try-build 回签等待**：家机 win（**MSVC C4062 编译期守卫实证**：删 case 应构建失败）+单位机 arm64 → 回签后 `python3 scripts/integrate.py --try-build-done` 集成；
3. 集成后：daemon 常态化采样续跑（x64l 列·本轮期间未跑）；波次7 续候选=T33 窄读泛化 或 D10 泛型函数体零检查。

### 四、诚实边界

- **MSVC C4062 触发未自证**（本机仅 GCC -Wswitch 实证）——win 编译期守卫的最终证据归家机 try-build；
- **T49 已根治**（栈参数区补齐·E2E 431）；残留=v2 侧同型面未核查（v2 生成层是否有同款栈参数缺口待专项）+i128 栈参数常量实参口径（沿既有寄存器路径同口径·未扩面）；
- 函数内静态 i128 槽尺寸口径分叉（IR 名 `i128` 未入「整128」双 quad 分支→8 字节槽不足）=观察项（登记 T50 行内）；
- 谓词默认值族（setccFor/setccmnemonic `default: setne` 等 8 处）合法不可达（调用点受上层守卫限定）=扩散矩阵记录·未加固；
- 本机无 win/arm64 产物运行能力——win/arm64 侧口径归家机/单位机复核。

## 单位机 ARM64 节

**交接时间**：2026-09-18 🏃 328-a 协议 v2 落地轮收工（用户批准方案）——AGENTS §8 重写+integrate.py+plans/027；本轮自身走新流程（分支 任务/单位机-328-协议v2 开发+integrate.py 集成演练）。接手前先 `git fetch`。

### 一、本轮做了什么

1. **三机并行协同协议 v2 落地**（用户 2026-09-18 批准·三项裁决=缺陷分级处置/按风险分层 try-build/文档随分支走）：开发锁废止——**分支即开发**（`任务/<机>-<轮次>-<标识>` 推 gitcode=认领·可多机并行·分支名唯一防撞号）+**队列即集成**（`scripts/integrate.py`=develop 唯一入关口·fetch→rebase→门禁跑在合并结果上→push ff-only CAS 自动重试≤3）+缺陷分级（机制级当场修分支/语义级先呈报）+分层 try-build（src/tests 等平台产物面=三平台预验证）+fix-forward 职权（「不在职权范围」废除）。
2. **AGENTS.md §8 全文重写**（v1 单锁协议废止；B8/B9/六纪律/B 区铁律/禁 force push 等全部承继；§3 缺陷零容忍补分级注记；§6 三处引用同步）。
3. **scripts/integrate.py 新增**（合并队列七步·--dry-run/--try-build-done·平台感知门禁：win=ci.ps1、linux=cmake+单测+E2E --target）。
4. **check_handoff.py 扩展**：看板 🏃 行必含 任务/ 分支名（轮次号 ≥328 强制·<328 旧协议在飞轮过渡豁免）——**反例（330 号无分支名）拦截+正例（326 豁免/328 合规）放行双向实证**。
5. **plans/027 方案文档**（诊断/业界对照 rustc-bors·Chromium CQ·Google trunk/用户裁决记录/细则/试点安排与复盘区）。
6. 看板说明区改 v2 口径+通告段广播协议生效与试点邀请；plans/021 戳/里程碑/§三-A 试点行/§五索引；lessons 追加设计依据条目。

### 二、验证链

```
python3 -m py_compile scripts/integrate.py scripts/check_handoff.py   # 语法 OK
python3 scripts/check_handoff.py                                       # 正例通过（退出码 0）
python3 scripts/check_handoff.py（构造 330 号 🏃 行无分支名）           # 反例拦截（退出码 1）·已还原
python3 scripts/integrate.py --dry-run                                 # 合并队列演练
```
（写集=纯文档+平台无关脚本 ⇒ 按 §8.3 分类不触发 try-build、不重跑 E2E——基线 93b11df=家机 326-a 实施提交承载）

### 三、发现的问题

- 家机 326-a（旧协议末轮）收工时若撞新校验：收工=清行即不触发；中途撞=任务列补分支名（校验信息已附处置指引）。
- integrate.py 首版 try-build 回签靠看板通告段人工确认+旗标声明，自动解析回签留试点复盘后迭代（plans/025 §三 3.18 边界③）。

### 四、下一轮候选

1. **协议 v2 试点**（plans/027 §六）：另两机各开真实任务分支并行开发一轮→各自 integrate.py 集成→复盘回填。
2. M3 采样轮续（B8 候选行+daemon 常态）。
3. T24/T25 v2 树对齐轮（随家机 v2 面轮次）。

### 五、诚实边界与坑

- 本轮为协议落地轮，**并行开发实效未验证**——试点轮（plans/027 §六）承载；回退路径=恢复旧 §8+删 integrate.py（零代码风险）。
- 中文变量名带空格=Python 语法错误（integrate.py 首版 `try_build 触发模式`/`push 命令` 两处——py_compile 当场拦截后修正）；python 脚本的中文标识符必须下划线连接。
- 集成门禁增量判定（竞争重试轮全量门禁跳过条件=develop 增量纯文档）为逻辑设计，尚未经历真实竞争场景检验——试点轮观察。