# HANDOFF 交接文档

**交接时间**: 2026-09-12 第八十七轮（**深度机 linux-x86_64**）——**87-a 静态变量全链路根治**
（立案来源=85-a/86-a 登记项 ②「静态结构体」第一优先候选；探针 P50 系列铺开形态矩阵后发现
缺陷面比登记项**更宽**：两侧 × 全形态 × 全链条共 10 处）。本轮序列：探针实证（宿主基线校验位
0 全败）→ 宿主侧实施（IR + 三后端）→ v2 侧实施（静态表/地址语义/注入/字段 drop + 三后端）
→ 防线（E2E 246 双编译对照 255 + 247 宿主单侧 63）→ 反证五件（全命中+恢复）→ 全量验证链 →
文档五件套。验证链全绿。

## 一、本轮做了什么（写给无上下文的新会话）

1. **立案与扩面**：85-a 登记项 ② 只写「槽尺寸 + 字面量赋值」两处，实测**全链 10 处**——
   ①宿主 `.data` 槽恒 8 字节（linux_x64/arm64；结构体越界写相邻符号，P50b rc=139）；
   ②宿主静态**标量初值**被忽略（恒 `.quad 0`；x64 侧正确=三后端分叉）；③语句位结构体赋值落
   8 字节 StorePtr；④声明位初值零初始化；⑤静态结构体字段读取基址落无效槽；
   ⑥v2 静态结构体被降级为**函数局部栈槽**（跨函数不共享、静态槽零写）；⑦v2 布尔/浮点/字符串
   初值折叠失败落 0；⑧v2 静态字符串赋值无 drop（残留 +600）+ 借用来源共享句柄；
   ⑨v2 成员位字段 preFree 缺静态槽；⑩两处后端分叉类（x64 vs linux_x64/arm64）。

2. **宿主实施**（`src/cn_compiler`）：
   - `ir/ir_decl.cpp`：静态登记扩展（结构体/字符串/字符字面量/兜底表达式 → 入口注入）+
     新 `emitStaticInitsAtEntry()`（结构体=**原地逐字段构造**；其他表达式=整体拷贝，调用返回
     浅拷接管；字符串=求值+来源分级；类容器=P3-8 指针槽）。
   - `ir/ir_expr.cpp`：静态赋值分支加「结构体整体值写」（preFree 恒真）+「静态字符串赋值」
     （drop 旧 + 来源分级）；静态标识符读取=地址语义（结构体）；`emitStructWholeAssign` 静态源；
     `memberObjStructType` 静态回退。
   - `ir/ir.cpp`：`lvalueAddress` 静态分支。
   - codegen 三后端：槽按 `typeSizeOf` + 标量初值直存 + 每槽对齐（`ALIGN 8` / `.align 8`
     〈x86 GAS 语义=字节数〉/ `.align 3`〈AArch64 语义=2^N〉）。

3. **v2 实施**（`CN语言编译器v2`）：
   - `IR/IR容器.cn`：静态表扩展 `静态初值起点们/终点们`、`静态需注入们` + 查询函数。
   - `IR/生成表达式.cn`：静态结构体读取 → `IR_静态地址` + 登记 `T_对象`；类型码按静态类型表
     分派（字符串=T_字符串）；`重取成员对象基址` + `顺序链标识符地址` 加静态分支。
   - `IR/生成语句.cn`：`发射结构体目标地址` 助手 + `生成结构体整体赋值` 字面量右值分支 +
     静态 preFree（**预计算布尔**，避开实参位短路）+ `归一化静态串赋值` + 成员位 preFree 含静态。
   - `IR/IR生成.cn`：`解析标识符类型ID` 加静态表级。
   - `IR/生成函数.cn`：`注入主函数静态初始化` 扩展（结构体初值 + 需注入标量/字符串）。
   - 三后端静态段：槽按类型尺寸 + 对齐。

4. **防线**：E2E **246**（双编译对照，第十六个：八形态折 **255**）+ **247**（宿主单侧六形态折
   **63**）；**反证五件全命中**（A 槽尺寸回退→246 宿主 **-11**；B 标量初值回退→247 校验和
   **62**；C v2 读取回退→246 v2 **-11**；D v2 字段 preFree 回退→246 v2 **127**；E 宿主静态串
   drop 回退→247 残留 **500**/校验和 **57**），均已恢复复验（注入残留检测=0）。

5. **验证链**：清场重建**零警告** + 单测 **1306/1306** + E2E **274 用例 270 过 0 失败**/4 跳
   + 锚定链固定点 **334821 行**（新口径）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）
   + 释放路径两侧对照。

6. **文档五件套**：plans/019（跟踪表第八十七轮行）+ plans/020（**第二十八节 87-a 全节**）+
   lessons（87-a 段，权重 8，预防新增 87~91）+ 更新日志 + 本文件。

## 二、本轮踩坑（已入 lessons 第八十七轮段）

- **登记项≠缺陷面边界**：登记只写 2 处，实测 10 处——**以形态矩阵铺面（全类型 × 全消费位）**。
- **后端分叉是缺陷温床**：同族发射点（.data 段）三后端必须同治（x64 有尺寸逻辑、linux/arm64 没有）。
- **静态槽无出口 → drop 必须在写入点自带**（重复赋值 preFree/drop，否则每次泄漏）。
- **「值即地址」形态必须配套回扫豁免**（`T_对象` 登记与消费端跳过成对出现）。
- **既有缺陷被新代码触发时的处置**：新代码改预计算避开（更清晰、无性能损失）→ **栈基线净化
  尝试未解决即回退**（不做半吊子修复）→ 探针+诊断链完整登记为下一轮第一优先。
- **`.align` 语义分平台**：x86 GAS `.align N`=对齐到 N 字节；AArch64 `.align N`=2^N —— 写错即
  「alignment not a power of 2」汇编失败（本轮 linux_x64 首版踩中）。
- **临时诊断会污染 IR 错误计数**：v2 的 `IR层诊断` 计数 → 达阈值即中止代码生成（调试输出须用
  `IO::打印到错误` + 事后必删；本轮 DBG 诊断曾导致「33 个 IR 错误中止」）。

## 三、下一轮任务（按序）

1. **v2 实参位短路栈失衡（新登记，第一优先）**：`f(a || b)`（短路运算在调用实参位）→
   `生成调用` 弹实参后被调取到残留寄存器 → `[ir2] 调用被调对象非函数名/方法` + 运行时错误6。
   探针 /tmp/p87probe/v2w（`取值(甲, 甲 + 2, 判(甲) || 标)`）；诊断已定位：表达式栈存在
   **函数级残留**（变量声明等不弹栈）+ 短路求值净 +3（只压不弹）→ 外层调用弹后栈=3/被调=3。
   **根治方向=统一「表达式求值净栈 +1」不变量契约为单一事实源**（先做栈模型普查：谁压谁弹、
   语句边界是否清栈）。
2. **跨机轮（家机 win-x64）**：87-a 三后端中 x64（win）侧未实物验证（槽尺寸/标量初值/对齐
   `ALIGN 8` 语法）——本机只有 linux-x86_64；win 侧跑 E2E 246/247 + 全量门禁复验。
3. **arm64（单位机麒麟）**：linux-arm64 后端静态段（`.align 3` + `.zero`/`dq`）实物复验 E2E 246。
4. **v2 静态字符语法**（`静态 字符 x = 'Z';` 语法层不识别——既有限制）与 **v2 打印浮点恒
   0.000000**（既有打印内置限制）：登记待评估（E2E 247 宿主单侧承载字符面）。
5. **多文件静态初始化注入**：仅入口模块（含 主）注入——与类/容器静态既有同限制；如需跨模块，
   需「模块初始化函数」机制（登记）。
6. 呈报清单余项维持：B35 字段容器接收者（待用户裁决）、甲（参数 move）/丙（返回位拒绝）、
   阶段 4 第二层第三批（不安全区变硬错误）、v2 诊断无行号、未初始化槽 preFree 观察项、
   行数存量违规面长期整改。

## 四、验证链（本机复现口径）

```bash
uname / 系统确认                # 深度机 linux-x86_64
# 宿主编译 v2 树（v2p）
./target/cn build CN语言编译器v2/主.cn --target linux-x86_64 --output target/audit2/v2p_linuxx64
./target/cn_unit_tests          # 单测 1306/1306
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量 274 用例 270 过/0 失败/4 跳
bash target/p71x64l/run_chain71.sh   # 锚定链（固定点 334821 行两代逐字节一致）
bash target/p71x64l/cmp29_71.sh      # 对拍 34/34（DIFF=0）——**须与锚定链串行**
bash scripts/cn_release_parity.sh    # 释放路径两侧对照
```

> 验证链命令一律串行；全量 E2E 与手工 v2p/固定点操作不得并发。
> **手工 v2 探针链**（/tmp/p87probe/v2w）：`v2p 主.cn linux-x86_64`（cwd 需有 `stdlib` 软链 +
> `target/` 目录）→ `as target/v2asm.s -o 主.o` → `g++ 主.o target/audit2/{io_api,intern_api,
> runtime,string_api,i128_api,math_api,input_api,file_api,time_api,system_api}.o`。
> **反证手法**：备份文件（本轮 /tmp/p87probe/bak）→ python 注入 → 重建（宿主
> `cmake --build target/build` / v2 重建 v2p）→ 跑用例应 FAIL → 恢复复验。
> **注意**：改宿主源码后**必须重建**（`target/cn` 是构建产物直出，不重建则测量错位——本轮
> 反证 C 首次测量即踩此坑）。

## 五、诚实边界

- 本轮 v2 侧**避开**了实参位短路形态（调用点改预计算布尔）；该既有缺陷**未根治**（登记第一优先）。
- v2 静态字符（语法限制）与 v2 打印浮点（打印内置）为**既有**边界，非本轮引入；E2E 用例设计
  已规避（246 不含字符/浮点打印；247 字符面宿主单侧承载）。
- E2E 246 为双编译对照（两侧一致）；247 为宿主单侧（口径同 235/245）。
- 锚定链固定点 **334821 行**为本轮新口径（86-a 后 330412；+4409=v2 侧 87-a 代码）；不跨后端比较。
- 探针与脚本留 /tmp/p87probe（不入库）；备份文件 /tmp/p87probe/bak。
