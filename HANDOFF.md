# HANDOFF 交接文档

**交接时间**: 2026-09-08 第三十五轮（深度系统 x86_64）——**v2self 锚定专项治理**：上轮两项挂账当轮根治，且 gdb 分步插桩实证**纠偏了上轮的根因定位**——栈溢出真根因是**宿主 IR 层寄存器号跨函数累积导致栈帧线性膨胀**（非解析深递归），已根因级根治；③合并假歧义（模块集合未去重）同步根治。**v2self 默认 8MB 栈全链 rc=0（历史首次）+ 字节级固定点 fix_p≡fix_s + 三级不动点 g2≡g3**。门禁全绿（单测 1250+175 用例 171 过/0 败/4 跳）。

---

## 一、本轮核心实施（两项根治）

### 1. ③合并假歧义根治（v2 语义检查.cn）

v2self 报「符号歧义: 货舱查找依赖/解析货舱/代码生成（来自 货舱解析/货舱解析）」。插桩实证**来源们=同一模块自拼接**：`模块路径们` 是文件级快照（每文件一条），阶段目录包化后候选1b 包根（包.cn 路径=模块名）与撞名成员（代码生成.cn/货舱解析.cn 挂载名同名）产生重复路径文本 → `全部模块ID们` 未去重 → ③ 遍历同模块双计数 → 同签名假报 E0659。**修复=构建 全部模块ID们 加 映射 O(1) 查重**（宿主 findModule 模块表天然唯一化的 v2 对应物）。

### 2. 栈溢出真根因=宿主 regCounter_ 跨函数累积【根因级根治】

**上轮误诊纠偏**：gdb 回溯实证段错误时**全栈仅 31 帧**（非深递归）；`解析二元层` 帧 `sub rsp, 0x48880`=**290KB**；20 函数同构探针帧 80B→1152B **线性膨胀**。根因=`IRGenerator::regCounter_` 仅 generate() 模块级清零、跨函数累积，computeFrameSize 按 maxRegId 定帧 → **越靠后的函数帧越大**。ulimit unlimited 侥幸（内核栈自动增长容纳巨帧）掩盖真因。

**修复**（Rust 对照 rustc MIR LocalId 每函数独立；v2 先例=寄存器计数每函数从 1 起）：regCounter_ 与 blockCounter_ 同点每函数复位三处——`ir_decl.cpp visitFunctionDecl` / `ir_oop.cpp emitClassMethod` / `ir_generic_func.cpp 泛型实例`。虚拟寄存器=函数内 SSA 值跨函数仅符号名引用，复位语义安全；标签唯一性由 currentBlockPrefix_ 函数名前缀既有保证。三后端共享 IR 层同源受益。单测 `IRFunctionTest.RegIdResetPerFunction` 锚定。

上轮「向量$Token.追加 容器方法未收录」**未再复现**=假歧义中止后 IR 表不完整的中间形态假象，随①消失。

### 3. E2E 173（目录包根撞名成员假歧义锚定）

`设备/包.cn`（包根）+`设备/设备.cn`（成员挂载名同名）+主程序 glob 导入后纯名调用同名字函数。修复前负验证实锤报「符号歧义: 设备（来自 设备/设备）」，修复后 rc=173。

## 二、v2self 锚定全链（重建达成）

- **默认 8MB 栈** v2p 编译 v2 全集 rc=0（历史首次无需 ulimit）。
- cn_self（自举编译器，容器实现借链 v2p.o muldefs——E2E 同款 rustc 预编译 std 模式）运行级：单文件 42、多文件 119=14。
- **二级自举** cn_self 编译 v2 rc=0；**字节级固定点 fix_p≡fix_s**；**三级不动点 g2≡g3**。
- 组件对拍 24/28 逐字节一致。

## 三、灰色点披露（下轮专项）

1. **组件对拍 4 入口**（语法/包.cn、IR/包.cn、代码生成/代码生成共用.cn、语法/解析基础.cn）差异=「&」字符串的池编号差 2（v2p=269 / cn_self=271）：诊断流其余全同、池内容一致、运行 rc 一致——**纯标签重编号，语义零差**。两编译器各自自洽（排除 ASLR）、argv[0] 排除；指向某处与链接布局相关的稳定初值差（未初始化读取嫌疑）。下轮用字符串池快照插桩定位。
2. **8 用例运行级对拍矩阵**（119/123/126/128/130/148/150/158）下轮补跑收口。
3. 生成表达式.cn 1261 行超线（单函数 1080 行）函数级拆分挂账。
4. v2 结构体构造字面量 `记录{...}` 语法缺口挂账（随语法覆盖轮）。
5. abi辅助.cn（v1 遗留探针）转正或删除。

## 四、教训（已入 lessons.md 权重头部）

- 「段错误+ulimit 可消除」≠栈溢出：巨大单帧与深递归在 unlimited 下同样「消失」——gdb bt 看实际帧数一秒定性。
- 计数器类静态/成员变量新增必须回答生命周期，与同文件既有复位点对表。
- **取证对象必须是本轮构建链产物**（假对拍家族第七案）：陈旧链接产物让 objdump「定位」了不存在的差异。
- 快照维度（文件级/模块级）与消费语义维度必须在接口注释显式声明。

## 五、下一步（优先序）

1. 组件对拍 4 入口字符串池编号灰色点专项（字符串池快照插桩）。
2. 8 用例运行级对拍矩阵收口（含 min1 逐字节）。
3. specs/08 目录包/模块声明语义条文批注（上轮顺延）。
4. 跨机轮：win-x64/arm64 全量复验（宿主 IR 层改动三后端共享；win 同病风险已被共享层修复覆盖，动态复验待跨机）。
5. 生成表达式 函数级拆分（1080 行单函数）。

---

写给无上下文的新会话：接手先读本文件第一节（两项根治+误诊纠偏）与第三节（灰色点）。v2self 主链锚定已达成（默认栈+固定点），**但自举完备性宣称仍需 8 用例矩阵+跨机轮**后方可；测试期望文件与断言未动（新增 173 为新用例注册）。
