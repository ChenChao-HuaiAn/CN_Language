# HANDOFF 交接文档

**交接时间**: 2026-09-06 第二十六轮收官（深度系统 x86_64）——**plans/017 T3/T4 实施：run_e2e.py v2 闭环平台参数化+v2 闭环 23 例本机解锁+解锁暴露两枚回归根治（用户裁决均方案A）**。用户指令准绳：遇缺陷参考 Rust 经验提方案（性能第一/安全第二）。

## 本轮已完成（全部有实证）

### 一、T3：runner 平台参数化（tests/e2e/run_e2e.py）
1. `执行v2闭环Linux` 新增 `目标平台` 参数（linux-arm64 | linux-x86_64）：宿主编译 `--target`×2（v2p 构建+供给源编译）+v2p 运行第 2 参数×2（负路径+步骤3）随平台分派；产物命名 **arm64 保持 `v2p_linux*` 原名（行为零变化）**、x86_64 用 `v2p_linuxx64*`/`v2asm_linuxx64.o`/`v2out_linuxx64`（防两平台先后全量互踩中间产物）。
2. `执行v2闭环` linux 分支并入 x86_64；`执行用例` v2 闭环守卫扩三平台；**平台跳过清单 `linux-x86_64` 摘除 v2 闭环 23 例**（119~150），跳过仅剩 62/69/78/79 四例。

### 二、T4：E2E 158_v2_linuxx64_系统V调用约定（原拟编号 154 已占用，顺延 158）
v2 版 SysV AMD64 ABI 分歧面专项（三平台语义一致，重点锚定 X64L 后端；注册 `(["主.cn"], 0)`）：①7 整型参数（第 7 走栈 [rbp+16+8k]）；②结构体返回（rdi 隐藏 retbuf）；③结构体按值参数×满 6 整型寄存器位（宿主第十八轮 r9 拷贝临时缺陷 v2 侧同款探针）+嵌套调用实参；④结构体返回+7 参数（宿主 154 同款触发面）；⑤递归对齐。返回码 0=全过+11~20 失败码定位。

### 三、解锁即暴露两枚回归（第二十五轮引入被平台跳过掩盖，均用户裁决方案A 当轮根治）
1. **【150 语义回归】**：声明收集登记键改签名键后，`是引用返回调用`（语义分析.cn:51，独立查纯名表、未随调用分支一起加签名表回退）恒 -1 判假——引用返回赋值目标/复合赋值/引用初始化三形态全拒（150 解锁首跑 3 错中止）。**v2p_fix 基线对拍实锤**：旧 v2p 语义 0 错 rc=0/新 v2p 3 错 rc=1；win 侧同源码同红=非平台特异。**修复=声明收集双键登记**（签名键恒登记+**纯名键同名唯一时补登记**——与 IR生成.cn 三张表双键同构；真重载候选>1 不登纯名键仍走签名表回退，157 行为不变；跨文件同纯名先文件赢=旧静默吞同款=模块遮蔽语义，P6b 对齐项；Rust 准绳：唯一绑定优先查询更快+歧义绝不引入错误绑定）。
2. **【125 自检行】**：打印函数IR 打 IR 名（=签名键）带 `#整64` 后缀 vs 期望行固化旧文本（非运行缺陷，其余断言全过）。**修复=主.cn `自检显示名` 剥首个 `#` 后缀打声明名**（签名键=名#参数串，CN 标识符无 # 剥离安全；125 期望文件零改动）。

### 四、验证（全绿）
1. 150/125/157/158 单跑四绿（157 重载消歧零回归锚定；158 五分歧面 rc=0）。
2. 全量 E2E linux-x86_64 **160 用例 156 过/0 败/4 跳过**（解锁 23 例全绿+158；159→160=新增 158）。
3. 构建零警告（GCC -Wall -Wextra -Werror）+单测 1240/1240（C++ 侧零改动，本轮变更=v2 源码 语义分析.cn/主.cn+runner+新用例）。

## 下一步
1. **P6b 模块系统**（远期）：模块名$ 前缀+纯名调用遮蔽语义对齐（重载消歧完备方向；本轮双键登记的「跨文件同纯名先文件赢」已与旧实现行为对齐并注记）。
2. **跨机轮**：win（家机）/linux-arm64（单位机）全量复验——含 158 三平台跑与 arm64 路径动态零变化实证；v2self 下一代重编锚定（含双键登记修复）随跨机轮或下轮收口。
3. 呈报①②③+第 7~18 枚+T3/T4 全部收口。

## 灰色点披露（持续有效）
- IR容器.cn 单组件子链冒烟 exit=1 系既有状态（第二十二轮披露，主链全绿）。
- v2self/v2p 运行需大栈（ulimit -s unlimited）与项目根 cwd（IR签名 读 stdlib/ 相对路径；asm 输出 target/v2asm.s 亦相对 cwd）。
- 宿主解析器 `结果` 不可作变量名（结果<T,E> 泛型保留）——用例命名避开。
- 对拍脚本与产物在 /tmp/v2dbg/p25/（不入库；reg4.sh=真三方对拍脚本）；干净基线 v2p=target/dbg/v2p_fix（第二十五轮前源码——注意其语义层**不含**本轮双键登记修复）。
- 本轮验证跑管道具：管道 `| head` 会截断被测程序输出（SIGPIPE 中途杀死致 asm 缺产）——**基线对拍必须重定向到文件再 grep，禁用管道截断**（本轮实操踩到，重跑修正）。

## 踩过的坑（已入 lessons 权重 10）
- **平台跳过的用例不是不存在的用例——解锁日=欠账日**：影响所有程序语义/符号/输出文本的机制性改动，落地轮必须全平台矩阵验证或显式审计被跳过用例的形态覆盖面；新机制上线自查「哪些既有消费点直接查表？各自有没有回退链？」（本轮 `是引用返回调用` 正是独立查表无回退的漏网点）。
- 历史：假对拍 cwd 陷阱（权重 10）+fail fast 双拦截+调用节点子区间 +1 偏移约定（见 lessons 第二十五轮条目）。
