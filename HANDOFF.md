# HANDOFF 交接文档

**交接时间**: 2026-09-04 第十二轮（家机 win-x64）——**plans/015 强制分号语句边界改造（方案B·Rust 式）专项独占轮实施完成**。上轮（单位机第十一轮）「下一步②分号专项五步实施序」全链落地。

## 本轮已完成（全部有实证）

### 一、规范级反转（plans/001 + cn-language-spec 双侧）
- 语句边界从「可选分号+换行≡空格+关键字边界」反转为「**语句必须 `;` 终结**」；表达式内部换行仍≡空白（运算符悬挂续行不受影响）；循环头三段分号保持分隔符语义。
- 裁决落地：`导入 路径;`（Rust use 同款）/顶层 `常量 T = 2;`/`静态 类型 名 = 值;`（C++ const 同款）强制；枚举/结构体/类/接口体 `}` 自终结。
- 改动面：plans/001 §4.6 整体重写+§5.9.6+§754；spec 01a 第三节/03 第零节+37 示例行/08 第八节+对照表/09 示例（32 处）。.zcode 单一事实源（.agents junction 同步）。

### 二、双侧实现
- **宿主**：`consumeSemicolon()` 强制化（缺失=「语句缺少分号 ';'」诊断+不消费，一次编译报全量）；可选点改 `match`（结构体字段/模块声明/接口签名/函数原型后——未裁决项维持可选）；顶层常量/静态补 require（**原无任何分号消费**——迁移后 `;` 顶层孤儿报错的根因）；parseBlockStmt 零消费防御；连带根治 parseSwitchStmt 冗余消费（新语义下双重要求误报）。
- **v2**：`吃分号`（诊断+语法错误数 计数+不推进）18 处接入（16 语句分支+顶层常量/静态）；主.cn 语法错误即中止（返回 1 不产 IR/asm——对齐宿主纪律与 c3d96a9 同族）；**连带根治 解析结构体声明 字段分号死循环**（127 用例 v2p 编译卡死 40 分钟实证：tasklist 零编译活动+CIM 定位子进程→限时复现→字段循环零前进）；回退源码合规。

### 三、全库存量迁移（token 级引擎，一次性脚本已删）
- **271 个 .cn 文件 15038 行**：E2E 194（**expected 零漂移**）/v2 组件 12/v1 组件 45（**连带**——ModuleDriverTest 编译 CN语言编译器/包.cn 在用）/stdlib 10。
- 单测嵌入源码 2085 处（raw string `R"CN(`带定界/普通串/相邻拼接序列——拼接整体 token 级+分号按字符归属拆回）；72-75 自举内嵌源码字符串（`\n` 切段；**71 词法用例除外**——token 流计数锚定）；19 语句终止用例人工改造（单行双语句+注释文案反转，expected 零变化）。
- 引擎要点：注释/字符串感知（`\n` 转义归空白）；花括号语境栈 block/lambda/literal/enum 四态（函数定义头参数表回扫/lambda `](` 链/继承头/无括号控制流头各自判定）；跨行语句起点门槛（`名=`/`名[`/`名++`/`名 op=` 标识符形态**仅跨行判**——同行歧义 2 处人工修）；行内块/`表达式?` 尾/`外部` 上下文关键字双语义。

### 四、测试（+7 单测+2 E2E）
- 单测：StmtMissingSemicolon*（变量/返回/表达式+诊断子串）+TopLevelConst/Static/Import 负测+全形态正测——**1216→1223 全绿**。
- E2E：**145_语句分号强制拒绝**（宿主负测六形态一次报全量）+**146_v2_缺分号拒绝**（v2 负路径闭环：退出码 1+无 asm+三诊断行——139/141 同款 None 形态注册）。
- v2p 冒烟五组件（词法/语法/语义/IR签名/IR容器）**语法 0 错+语义 0 错**。

### 五、门禁（终轮数字见下方「门禁状态」）
- 前置门禁（单位机变更复验）：构建+单测 1216+135 用例绿零回归（6 败=本轮中间态撞车非单位机回归已收口；78/79 OOM 已知遗留）。

## 下一步
1. **IR生成.cn 冒烟 96 错消化**（第 6 组件，单位机立项遗留）：三层定位法；IR 层 [ir2] 级诊断与「IR 层错误即中止」纪律未建立（139 只锚定语义层）。
2. **簇⑥连带观察项**：cn check 非入口文件名检查假绿（模块可见性过滤静默丢函数体）——呈报方案待裁。
3. **命名清理（低优先）**：StmtNoSemicolon* 等 5 个单测名称语义漂移（源码迁移后断言过但名称与行为错位）——待用户裁决是否改名。
4. **linux-arm64 复验轮**（单位机）：146 新负路径 linux 侧对齐（139/141 已对齐同款）；分号改造全量 ARM64 复验。

## 灰色点披露（待用户裁，未立案）
- B1a 契约分叉（宿主 stdlib 包装 结果<> vs v2 内置直调裸指针）——随 stdlib 并入专项，不变。
- v2 整8/整16 强转白名单边界/正8 正16 显式拒绝——不变。
- 78/79 v1 链内存失控——已知遗留，随 v2 重建解决，勿翻案。
- **本轮新增**：单测 2 处单行双语句人工补分号（token 级同行歧义保守跳过的已知边界）；145 负测「一次报全量」依赖错误恢复链路（诊断顺序=文件顺序无排序保证——expected 只锚定子串不受影响）。

## 踩过的坑（本轮，全部入 lessons 详录）
- **目录级 git checkout 误伤**：迁移中途回滚 .cn 目录把 v2 功能改造一并回滚（假绿）；**回滚前必须确认目录无未提交功能改动**。
- **v2 结构体字段分号死循环**：原「可选无消费」的 token 在新语法下成零前进死循环（40 分钟卡死）；规范反转须列「谁消费它」清单。
- **python -c/heredoc 转义折损**（6 次 3 损）：`\\n`/`\\(` 在 bash→python→Edit 多层间折损——结构修改一律写临时 .py 文件。
- **token 级迁移引擎歧义家族**：函数头/lambda/继承头同构（参数表回扫区分）；`外部` 双语义（下一 token 分流）；同行 `名 名 =` 声明 vs 新语句（跨行门槛）；嵌套字面量内层 `}` 防误插。
- **后台 E2E 挂起判定**：tasklist 查编译器进程（零 cn.exe=卡死）+CIM 查子进程命令行（定位到 v2p.exe 卡 127 用例）。

## 门禁状态（本轮最终，家机 win-x64）
- 构建零警告（MSVC /W4 /WX）+ 单测 **1223/1223**（1216+新增 7）+ E2E 全量 **148 用例 146 过/2 败**——唯二失败 78/79 均为已知遗留 OOM 家族（全量轮 78=0xC0000005 访问违例变体+79=4124MB 超限；**78 单跑复证=4110MB 内存超限同族形态确认非回归**）；五组件 v2p 冒烟语法 0 错+语义 0 错；145/146 单跑双绿。
- 迁移正确性证据：E2E 194 用例 expected 零漂移（72 AST dump/76 词法计数两处合法更新随轮说明：72 结构体字段分号撤除=该 dump 链对字段分号有缺陷观察项入档、76 词法 token 数 717→784=分号计入新规范产物）。
