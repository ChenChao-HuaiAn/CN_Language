# HANDOFF 交接文档

> **分机分区（2026-09-12 起，AGENTS.md §8.3）**：本文件按机器分节，每机**只整体替换自己节**，
> 他机节不动（两机并行改动落在不同区域，git 三方合并自动成功）。
> 新会话必读顺序：⓪根目录 `plans/021-任务进度观察表.md`（**全项目任务进度唯一总入口**：总目标/
> 当前进度/排班队列/里程碑——开工先读、收尾必更，见 AGENTS.md §2.2/§6.3）→ ①本机节（自己机器
> 最近交接）→ ②深度机节（开发主线最新进展）→ ③根目录 `三机任务看板.md`（三机并行唯一看板，
> 开工前先认领/查看）。

## 家机 win-x64 节

**最近交接**：2026-09-13 深夜——**第一百一十一轮（111-a）：E2E runner win 双编译对照原生支持（C15）+ size 说明符扫描门禁（C14）+ 新缺陷 221 双侧收口**。
本轮把 plans/021 §3 队列两项 win 侧登记任务落地，并在**31 个双编译对照用例首次 win 真跑**时捕获新缺陷
（221 窄返回值高位污染），双侧根治；期间以**锚定链为判据**修正了 v2 侧首版方案的语义错误（整32 须
**符号扩展**而非零扩展）。**win 门禁由「292 用例 259 过/33 败」跃为「290 过/2 败」**（仅剩 78/79 OOM
既有败面——31 个平台守卫用例全部转真跑通过）。

### 一、本轮交付（C15 + C14）

**C15：E2E runner win 双编译对照原生支持**（`tests/e2e/run_e2e.py`）：
- **win 分支 v2 产物按用例隔离**（`target/audit2/v2work<编号>/`，对齐 linux 分支做法）：v2 驱动器
  asm 输出为相对 cwd 的 `target/v2asm.asm`——此前共享项目根路径 → v2 用例被迫单线程串行；现
  每用例独立工作目录（cwd 隔离）+ stdlib junction（`mklink /J`，不需管理员权限）+ 入口绝对路径
  （日志锚行比对前把绝对前缀适配回相对——v2 输出路径已归一为正斜杠）。
- **双编译对照放开 win-x64**（守卫由「仅 linux 双平台」改为三平台）：宿主侧产物 `hostout.exe`
  （win 上无后缀 exe 不可运行——实测 WinError 2）+ 宿主产物 asm 扫描。
- **win 侧 v2p 预热 + 统一并行**：抽 `确保v2p就绪win` 函数（工具链/LIB/运行时 obj/v2p 构建缓存），
  主程序池启动前单线程预热（消除并发首建竞态）；win 调度由「v2 桶串行」放开为全量统一并行。

**C14：asm size 说明符静态扫描门禁**（runner 内联，ml64 前挂载）：
- 扫描器 `扫描浮点访存说明符`（movsd/movss 裸内存操作数 = 缺 qword/dword ptr → A2070）+ 挂点：
  ① win v2 闭环产物（ml64 前）② 双编译对照宿主产物（hostout.asm）。
- **探测器的自检**：`asm说明符扫描器自检`（正/负样本内联）在 E2E 启动时运行——扫描器失效先于
  被测物报错（110-a 教训：扫描器首版计数 bug 经注入反证才暴露）。

### 二、新缺陷 221（双侧根治 + 两轮修正）

**现象**：`221_双编译对照映射集合所有权` win 侧 v2 退出码 **254**（期望 255）→ 形态①（映射
`删除(键)` + `包含`）校验位丢失。linux 侧此前全绿（win 门禁原为平台守卫）。

**根因（双侧对照铁证）**：宿主 win 后端 **epilogue 返回装载缺类型宽度分派**：
- `包含` 实现（宿主编译 obj）返回布尔：`mov [rbp-112], eax`（4 字节写）+ **`mov rax, [rbp-112]`**
  （8 字节读）→ **rax 高 32 位携带栈垃圾**（ABI 只保证窄返回值低位有效）。
- 宿主 **linux/arm64 后端**均按类型宽度装载（`emitStackLoad(..., currentReturnType_)` /
  linux 实测 `mov eax, [slot]`）→ **win 后端单侧分叉**。
- v2 win 后端消费：`cmp rax, 0`（64 位全比较）→ 被高位垃圾污染 → `!包含(1)` 判定翻转。

**修复（双侧，性能第一/安全第二）**：
1. **宿主**（`src/cn_compiler/codegen/x64/x64_codegen.cpp` emitEpilogue 通用分支）：
   `mov rax, [slot]` → **`mov <widthFor(returnType)>, [slot]`**（≤32 位类型用 eax 读 = 32 位写
   自动清零 rax 高 32 位）——对齐 linux/arm64 后端与 Rust/LLVM zeroext 语义。
2. **v2 三后端**（调用返回装载后按返回类型码规范化；面向自举链接 cl.exe 运行时 obj 的防御）：
   `T_布尔` → `movzx eax, al` / ARM64 `uxtb w0, w0`；**`T_整32` → `movsxd rax, eax`（符号扩展）**
   / ARM64 `sxtw x0, w0`；`T_正32` → `mov eax, eax` / ARM64 `mov w0, w0`。

**第二轮修正（锚定链为判据——本轮最重要教训）**：v2 侧**首版对 T_整32 用零扩展**（`mov eax, eax`）
→ **自编译退化**：cn_self111（v2 自编译产物）编译 v2 全树只产出 **12969/406115 行空壳函数**
（IR 生成为空）——定位法=停用/启用二分 + 锚定链判绿。根因：**v2 内部整32 值为符号扩展表示**
（类型转换生成 `mov eax`+`movsxd`；探针实证 `整32(-1)` 经调用返回 = **-1**），零扩展把负值变成
大正数（4294967295）破坏语义。改 `movsxd`（符号扩展）后固定点恢复。

**反证双向命中**（缺陷复现 + 修复有效，不依赖旧二进制）：
- 脏返回 obj（`supply_dirty.obj`：asm 还原 `mov eax`→`mov rax` 重新汇编）+ v2 **无**修复
  （产物删 `movsxd/movzx` 行模拟）→ **254 复现**；
- + v2 **有**修复 → **255** ✓。

### 三、验证（全绿口径）

| 项 | 结果 |
|---|---|
| 宿主构建 | `build.ps1`（`/W4 /WX`）**零警告** |
| 单测 | **1313/1313** |
| 全量 E2E（292 用例） | **290 过 / 2 败 / 0 未实现**（2 败 = **78/79 OOM 既有败面**〈内存保护 4096MB〉——**31 个双编译对照用例全部 win 真跑通过**；对比 110-a 的 259 过/33 败） |
| 锚定链 | 固定点 **406097 行** 两代逐字节一致（fix_p ≡ fix_s）+ 绑定自检 cn_main∈cn_self111.obj ✓ |
| 组件对拍 | **34/34**（DIFF=0） |
| 运行级 | **3/3**（hello rc=0 / 119 rc=14 / 173 rc=173） |
| 221 定向 | 双侧 255（宿主 + v2）；反证双件命中（254/255） |
| X64L/ARM64 静态 | 产物含规范化指令（X64L 10 处 `movzx eax, al` / ARM64 10 处 `uxtb w0, w0`）——**动态复验待跨机轮** |

### 四、踩坑（入 lessons 第一百一十一轮段）

- **规范化扩展方式必须匹配被优化方的内部值表示**：v2 整32 = 符号扩展（`movsxd`），零扩展破坏
  负值语义 → **自编译退化为空壳函数**（锚定链 = 唯一判据；E2E 全绿也发现不了——v2p 正常）。
- **「自编译退化」诊断法**：症状=自编译产物编译大输入时函数体为空壳 → **停用/启用二分 +
  锚定链判绿**（每轮 ~4 分钟）精确定位；辅以「残缺产物函数行数分布」判「IR 空 vs 代码生成空」。
- **反证双向对照法**：改 asm **产物**模拟脏返回（`mov eax`→`mov rax` 重汇编）+ 删产物指令模拟
  无修复——不依赖旧二进制即可精确复现与验证。
- **win 无后缀 exe 不可直接运行**（`CreateProcess` WinError 2）——双编译对照宿主侧须 `.exe`；
  宿主 build 中间 asm 落在输出路径旁（stem + `.asm`）。
- **E2E 产物隔离 = 并行安全的前提**：win 侧隔离后 31 用例转真跑 + 统一并行，全量 ~35 分钟。

### 五、下一步（新会话按序）

1. **单位机 arm64 跨机轮**（93-a~111-a）未做（家机已清账；arm64 侧本机不能跑）。
2. **X64L/ARM64 窄返回规范化修复的动态复验**（本轮为静态产物验证）——建议深度机/单位机跨机轮
   跑全量 E2E + 锚定链（新固定点：win 406097；X64L/ARM64 口径由该机新锚）。
3. **plans/021 §3 队列余项**：B3/B4/B5（**待用户裁决**——本轮已呈报）；C2（跨语句 NLL，
   维持排程）；D1（行数整改后续波次）；D3/D4 维持登记；E1 待并发标准库立项；F 组待重评。
4. **深度机侧排程**：D1 后续波次 / 联合体条件释放设施专项（其 HANDOFF 节）。

## 深度机 linux-x86_64 节

**交接时间**: 2026-09-13 第一百零八轮（**深度机 linux-x86_64**）——**108-a D5 剩余面固化**：
赋值位结果/可选 构造器保守拒绝负测（264_v2 门禁，零源码改动）；D5 面完整处理链收口
（107-a 修复 + 108-a 固化 + 架构边界登记）。本会话累计 93-a~108-a 共 16 个开发轮。

### 一、本轮做了什么（写给无上下文的新会话）

1. **决策**：107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——
   完整实现需「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需
   联合体条件分支=面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ **保守拒绝维持 +
   负测固化**（不为其打单形态补丁；若将来真实需求出现，按已定位路径专项立项）。
2. **门禁**：E2E **264_v2_赋值位结果构造器拒绝**（v2 负测：诊断锚行「构造器 正常 须用于
   结果/可选 语境（返回位或 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——
   防将来误放行致静默截断/泄漏。
3. **D5 面完整处理链（107-a + 108-a 收口）**：
   | 形态 | 处理 |
   |---|---|
   | `返回 无`（段错误） | 107-a 修复（返回位零初始化；E2E 263 + 反证双件） |
   | `可选<T> r = 无;`（原拒绝） | 107-a 修复（语义放行 + 声明位 IR 零初始化） |
   | `r = 正常(...)` / `r = 无`（赋值位） | 108-a 固化（负测 264）+ 完整实现登记（联合体条件释放设施） |
   | `盒.r.正常`/`盒.r.值`（嵌套成员链） | 两侧一致保守=诚实边界 |
4. 全量细节：`plans/019` 第一百零八轮行 + `plans/020` **第四十七节** + `lessons.md` 108-a 段
   （预防 153）。

### 二、本轮验证（零源码改动）

- v2p md5 `b6d8c387c86a066d4c1b1e62b710756c`（与 107-a 一致）；宿主 md5
  `f1484d79b0e5e85d64550b144842208a`（零改动）
- 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）
- 无需锚定链/对拍（v2 源码未变；107-a 终值：固定点 387693 行 / 对拍 34/34）

### 三、下一轮任务（按序）

1. **D1 行数存量整改（长期，本机可实施主要面）**：宿主余 7 文件（ir_expr 2798 / semantic 2101 /
   semantic_expr 1889 / ir_oop 1380 / ir_oop_call 1154 / semantic_call 1092 / ast.hpp 1032）+
   v2 3 文件 + 19 超百行函数——按"每轮 1~2 文件"滚动（验证口径=v2p md5/固定点不变 + 全量零回归；
   族边界勘定纪律见 §四⑬）。
2. **联合体条件释放设施专项（登记）**：结果/可选 全释放面（值字段条件释放）——赋值位覆盖写、
   结果/可选 结构体字段、结果/可选 容器元素等形态在此设施上统一收口（84-e 边界 + 107-a/108-a
   定位的实施路径）。
3. **可选重构（登记）**：v2「无」的整数字面量表示（彻底方案=新节点类型「空字面量」）。
4. **登记面余项**：`1.5f` 后缀语法缺口；`%` 浮点语义层未拒绝；数学库 `数学::` 内置表缺位；
   D3（未初始化槽 preFree 观察项）/ D4（静态槽不登记自动释放=设计选择）维持登记；C2/C13=106-a
   评估完成（维持排程/登记）。
5. **跨机轮（家机 win-x64 / 单位机 arm64）**：**93-a~108-a 全量复跑**（两侧编译器已多次变更需重建；
   **注意 103-a 契约变更**：v2 诊断带行号 → 迁移后的 .expected 为准；**107-a v2 产物已变**：
   win/arm64 侧 v2p 重建即可）+ 家机立案 **148_v2 win 崩溃缺陷**（三方案待裁决）。
6. **呈报清单余项**：B3、B4、B5（待用户裁决）。

### 四、验证链（本机复现口径）

```
> 验证链命令一律串行；**对拍/锚定链必须独立串行且不得相互交错**（102-a 假 DIFF 教训）。
> **全量门禁**：`rm -rf target/build && cmake -S . -B target/build -DCMAKE_BUILD_TYPE=Debug &&
> cmake --build target/build -j $(nproc)`（查 warning 计数=0）→ `./target/cn_unit_tests` →
> `python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8`。
> **锚定链**：`bash target/p107x64l/run_chain107.sh` + **组件对拍** `bash target/p107x64l/cmp_107.sh`
> （口径标杆=107-a 终值；若后续轮改 v2 源码，按该轮新建 p<N>x64l 目录）。
> **手工 v2p 重建（改 v2 源码后）**：`target/cn build CN语言编译器v2/主.cn --target linux-x86_64
> --output target/audit2/v2p_linuxx64`（同时产 .o；E2E runner 指纹变更会自动重建）。
> **手工 v2 探针链**（/tmp/p92probe `diff101.sh` 逐行 diff；/tmp/p105probe `run_d5.sh <名>`：
> 宿主 build + v2p + as + g++ + 逐行 diff）：cwd 需有 `stdlib` 软链 + `target/` 目录。
> **契约变更提示（103-a）**：v2 语义诊断现带「（行=N）」后缀 → 负测用例的 `.expected` 已迁移
> （20 个）；新增 v2 负测写 expected 时须含行号（可用脚本重跑取实测）。
> **调试/验证纪律（累计 13 条）**：①v2 `IR层诊断` 会中止编译——结论须"干净版复核"；②v2 共享
> 结构体加字段 grep 全部构造点；③元素步进统一 `semantic_->typeSizeOf`；④门禁形态须区分"旧机制
> 巧合正确"；⑤反证缺失位数预计算；⑥类型位后缀消费顺序显式设计；⑦探针先做全形态矩阵；⑧共享
> 推导改动回归同族面；⑨反证未命中先疑"改动冗余"；⑩对拍独立串行；⑪契约类改动（.expected/runner/
> 规范）须 §8.4 广播 + 全量迁移；⑫契约迁移脚本须模拟 runner 的真实调用形态；⑬纯重构类改动先勘
> 定族边界依赖（105-a：static 辅助函数跨族即禁拆该边界；等价性由 v2p md5 单点证据承担）。
> **远程推送现状（2026-09-13 实测）**：本机只有 `gitcode` remote；**未配置 `github` 镜像
> remote**，github.com 直连超时——镜像补推需网络可达的机器代做。
```

### 五、诚实边界

- 108-a 为**零源码改动轮**（负测固化 + 决策留痕）：赋值位结果/可选 支持仍为 v2 保守拒绝
  （与宿主差异=已登记等价性缺口，非缺陷）；完整实现需联合体条件释放设施（专项登记）。
- 前轮登记维持（静态槽不登记自动释放 / D1 长期滚动 / v2 `%` 浮点未拒绝 / v2 数学库内置表缺位 /
  未初始化槽 preFree 观察项 D3 / `1.5f` 后缀缺口）；
  **B2=93-a、C6=94-a、C7=95-a（+C3）、C10=96-a、C8=97-a、C9=98-a、C11=99-a（+H4）、C12=100-a、
  C1=101-a、C4=102-a（+H5）、D2=103-a、D1 长期滚动（首波 104-a + 第二波 105-a）、
  C2/C13=106-a 评估完成、D5=107-a/108-a 收口（残余=登记边界）**（销项台账见 `plans/021` §4.2）。
- 反证注入均已恢复并以注入前 md5 复核；探针留 /tmp/p105probe、备份 /tmp/p107bak 不入库。

## 单位机 ARM64 节

（占位）最近一次单位机收口=第七十轮 arm64 跨机复验（2026-09-11，plans/019 §7.3 单位机段：
清场零警告 + 单测 1289/1289 + E2E arm64 228 用例 223 过 + 200~209 双通道 arm64 首验全过 +
锚定链 arm64 口径 347685 固定点逐字节一致 + 对拍 29/29 + 运行级 3/3；另立案 68-a 已销项）。
下次单位机轮由当班会话整体替换本节。
