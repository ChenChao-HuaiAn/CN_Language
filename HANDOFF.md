# HANDOFF 交接文档

**交接时间**: 2026-09-10 第五十二轮（**单位机 麒麟 ARM64** 跨机轮，纯验证轮零源码改动）——按 HANDOFF 挂账二.1 拉取 f478ab0（第四十七~五十一轮）执行 arm64 动态收口：全量门禁复验 + E2E 189~193 双通道 arm64 首验 + v2self 固定点复锚（**322167 行 = arm64 新锚定**，第四十三轮 302433+三轮净增 19734）+ 绑定自检 + 组件对拍 29/29 + 运行级 3/3 + 方案甲 arm64 双锚实证。**E2E 首验拦截 1 枚新缺陷（52-a v2 ARM64 取片负片→movk 立即数越界），已立案三方案呈报待用户裁决——修复是下一轮第一优先级。**

---

## 一、本轮做了什么（写给无上下文的新会话）

1. **全量门禁**：清场重建 GCC 零警告 rc=0 + 单测 **1251/1251** + 全量 E2E linux-arm64 **201 用例 195 过/3 败/3 跳**。跳=62/69/79 既有平台限制；败=78 已知 OOM 4157MB（基线 4108~4149MB 同族零回归）+ **191_v2/193_v2 两红=新暴露 52-a**。189/189_v2/190/190_v2/192/192_v2/191/193 双通道其余全过=第四十九~五十一轮 ARM64 后端新发射（udiv/lsr/lo·hi·ls·hs/ldr w 零扩展读/uxtw/正32 存储截断/方案甲直拷）动态验证通过。
2. **52-a 缺陷（新暴露，立案待裁，勿自行实施）**：现象=191_v2/193_v2 as 汇编失败「立即数越界」（`movk x9, #-32768, lsl #48`；GNU as movk 域=无符号 16 位）。根因=代码生成ARM64.cn **取片() 有符号除法/取模保号出负片** × 常量整数负值补码分支（2^63 是唯一 bit63 置位位模式，字面量位模式 读负→`0-值` 回绕仍负）——**位模式正确、发射文本越域**。影响面=全树唯一爆点装载 0x8000000000000000（取片→装载立即数 消费点穷尽审计：常量整数/槽地址/字段地址，后两者值域恒小）；X64L/win mov 全范围立即数免疫=深度机三轮全绿掩盖；宿主 emitMovImm uint64_t 无符号提取天然免疫。**三方案呈报（A 取片 位与 65535 归一·宿主同构·推荐/B 片文本十六进制化/C 负值分支特判·不推荐）——AskUserQuestion 征询无答复，遵缺陷裁决纪律未实施**。取片 为 ARM64 层独有函数，修复零跨后端影响。
3. **v2self 锚定链（arm64 复锚）**：v2p52（宿主编译 v2 全树 rc=0；rt52 objs 现编 10/10）→fix_p.s **322167 行**→as rc=0（v2 全树自身源码不含 2^63 形态=52-a 盲区佐证）→链接 cn_self52（cn_self52.o 在前+v2p52.o 借链+muldefs）→**绑定自检 cn_main=0x4e6574 ∈ cn_self52.o .text [0x4020bc,0x4ea094)**→cn_self52 编译 v2 自身→fix_s.s 322167 行→**固定点 fix_p≡fix_s 裸逐字节一致**+运行级三用例 hello rc=0（你好，世界）/119 rc=14/173 rc=173+组件对拍 **29/29 全量逐字节归零**。
4. **方案甲 arm64 动态实证**：探针（target/probe52/cast，值域避开 2^63 防与 52-a 混淆）——整64(正32)=4000000000 零扩展读+整64(正64)=10^18 全精度直拷，宿主/v2p52 双侧逐字节一致+asm 实证 uxtw 在场/sxtw=0/ldr x9 64 位直通。2^63 重解释锚（-9223372036854775808）随 52-a 修复后补锚。
5. 文档：plans/014 状态表第五十二轮行+观察项 **52-a** 立案行+更新日志+lessons 一条（编译器缺陷权重 7：取片有符号取模+边界探针缺位63形态+平台结构性免疫三因）+本 HANDOFF。

## 二、下一轮任务（按序）

1. **【最高优先级】52-a 用户裁决后实施**（三方案见 plans/014 观察项表 52-a 行；方案 A 推荐=取片 位与 65535 位掩码归一）。修复轮闭环清单：v2 单点改（代码生成ARM64.cn 取片）→宿主编译 v2 全树 rc=0→单测+全量 E2E（191_v2/193_v2 arm64 转绿；X64L/宿主零影响静态论证+抽查）→cast 探针补 2^63 形态（整64(正64)=2^63 应出 -9223372036854775808）→v2p 重建+锚定链重锚（fix_p 行数预期微增）+绑定自检+组件对拍+运行级。
2. **残余观察项（随类型系统严格轮，plans/014 已立案）**：①宿主 canConvert 注释与实现不符与 §3.7 一致性收口；②v2 正8/正16→64 位目标转换源分派未标注；③赋值初始化/函数传参面混合符号严格化（Rust 同样拒绝，须规范审批）；④整32→正64 语义文档化。
3. **观察项延续**：联合体构造字面量双侧锚定（随构造字面量覆盖轮）；标量推断 IR 类型面（布尔打印分叉 184 expected 待批/调用返回大值截断）。
4. **win 侧**：第五十一轮方案甲 mov rax 直拷新分支动态首验=家机轮；52-a 修复后 win 零影响静态论证。

## 三、踩坑备忘（详见 lessons.md）

- **01_hello 入口实名 hello.cn 非 主.cn**（早期用例命名）——引用 主.cn 得 rc=2「打开失败」，第五十二轮运行级首跑即踩。
- **位模式类运算（分片/掩码/提取）在有符号整64 世界的 / % 不是位模式语义**——负 v 取模保号出负片；归一律：位模式运算一律无符号语义表达（52-a 根因）。
- 管道 tail 掩盖真实 rc（权重 7 沿用）；长命令一律 `cmd > log 2>&1; echo rc=$?`。
- 清场重建+rt objs 现编纪律（audit2/旧 .o 不可续用）；v2p 运行写固定路径 target/v2asm.s——**锚定链与 E2E 不可并行**（竞写）。

## 四、关键产物位置（target/ 不入库）

- 锚定链脚本：target/probe52/chain52.sh（fix_p→链接→绑定自检→fix_s→固定点）、map_check52.py、cmp29_52.sh、runlevel52.sh
- 探针：target/probe52/cast/主.cn（方案甲双锚，值域避开 2^63）
- 产物：target/probe52/（v2p52、cn_self52、fix_p.s/fix_s.s 322167 行、map52.txt、rt/ 10 objs）
- 编译器：target/cn（宿主，清场重建）；E2E 日志 target/e2e52_arm64.log、单测 target/unit52.log、构建 target/build52.log
