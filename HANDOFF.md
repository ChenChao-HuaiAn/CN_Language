# HANDOFF 交接文档

**交接时间**: 2026-09-11 第六十七轮（**深度机 linux-x86_64**）——**三主线**：①plans/019 阶段4' 第二批「v2 侧拥有型字符串 RAII」；②E2E 运行器三重改造（用户裁决：太慢/去硬编码/全量并行）；③**A1 字符串借出初始化拒绝**（方案A 用户批准——三探针实证呈报后裁决）。专项累计十二轮。固定点新口径 **254771**。

---

## 一、本轮做了什么

1. **第二批 v2 侧 RAII**（方案A 镜像）：预扫白名单登记+槽预分配+入口零初始化；初始化/赋值拥有化（free 旧 drop+先 copy 后 free 自赋值安全）；双挂点释放（返回块+块出口截断）；污染传播（非白名单调用/下标借出/嵌套同名）。修正上会话半成品三处致命伤：登记死代码位置、IR_调用(52) 无处理器、**函数IR 新字段未入 生成函数 显式初始化清单（v2p 全崩根因）**。
2. **E2E 三重改造**：v2p 构建缓存（v2 全树指纹）；v2闭环.txt 数据驱动（58 用例配置迁移，注册表出 runner）；--jobs N 全量统一并行（Linux v2 产物按用例隔离 v2work cwd+stdlib 软链+日志适配；池前 v2p 预热）。全量 18.3s（热）/27.7s（冷），原串行估 20+ 分钟。
3. E2E 208_v2_拥有字符串（七形态校验和 127 全对）；全量 226 用例 221 过（1 败=194 既有）。

## 二、下一轮任务（按序）

0. **A2（第三批正文，方案A 已批）**：字符* 借用视图类型化+返回 字符串=拥有/字符*=借用（缺口②用户函数返回泄漏根治）——先出 stdlib/v2 树迁移面盘点呈报再分批实施。
1. **阶段4' 第三批**：裸指针形态显式化（字符串 vs 字符* 两类型）+用户函数返回拥有类型区分（返回 字符串=拥有/字符*=借用——**新语义须用户审批**）+嵌套块声明块出口析构扩展（当前平铺预扫全登记、函数体块出口统一释放）。
2. **呈报清单**：①用户函数返回=借用（泄漏边界——`字符串 c = 造句(...)` 返回堆串无人 free——第三批类型区分根治）；②宿主**初始化位下标借出**缺口（`字符串 s = arr[i]` 不 copy 不污染→返回块 free 悬垂——**推断未实证，先写探针实证再呈报**）；③194 单行批准；④win 平台 v2 并行隔离（跨机轮）。
3. 阶段5 等并发库立项；NLL 等反馈期数据。

## 三、验证链

```bash
uname / 系统确认                # 深度机 linux-x86_64
rm -rf target/build && cmake -S . -B target/build && cmake --build target/build -j 8  # 零警告
./target/cn_unit_tests          # 单测 1289/1289
python3 tests/e2e/run_e2e.py --cn target/cn --jobs 8   # E2E 全量并行（18~28s；--jobs 1=串行）
# v2p 构建缓存：target/audit2/v2p_build_key_linuxx64.txt（v2 源码/编译器变化自动重建）
# 拥有语义验证：208_v2 用例 asm 含 __cn_str_copy/__cn_str_free（target/audit2/v2work208/）
bash target/p67x64l/run_chain67.sh   # 锚定链：fix_p 254365 →固定点一致+运行级 3/3
bash target/p67x64l/cmp29_67.sh      # 对拍 29/29
```

## 四、本轮踩坑（已入 lessons 第六十七轮 权重7）

- **v2 结构体加字段必须同步 生成函数 的显式初始化清单**——裸声明字段无默认构造，漏了=映射/向量字段垃圾指针全崩（上会话半成品 v2p 损坏的根因，掩盖为环境问题）。
- **v2 调用发射只有 IR_调用实参+IR_调用参数化 对形态**——IR_调用(52) 在 v2 代码生成无处理器=静默丢指令。
- **并行化先盘点共享面三类**：固定路径产物（target/v2asm.s）、相对 cwd 依赖（v2p 的 stdlib 签名扫描）、共享名产物（v2asm_X.o/v2out_X）——逐一隔离或池前预热。
- 测试 runner 用例注册表=「每新增用例改代码」反模式——配置文件进用例目录（数据驱动）。

## 五、诚实边界

- 用户函数返回字符串=借用（不登记不 free——返回拥有类型区分随第三批）；转字符串族不入白名单；参数/静态=借用。
- 宿主初始化位下标借出（`字符串 s = arr[i]`）疑似缺口待实证呈报；194 待批维持。
- win-x64 的 v2 产物未隔离（--jobs>1 时 v2 桶串行兜底）——跨机轮验证项；ARM64 并行未验（脚本平台参数化已就绪）。
- 中间态谜团（未复现）：257033 行口径的 cn_self 曾现 45 个「成员字段偏移未知」（audit2 旧 .o 链接版本），全开版 254353/254365 闭环无此象——若复现按 lessons 第六十七轮排查路径走。
