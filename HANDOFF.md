# HANDOFF 交接文档

**交接时间**: 2026-09-02 第二轮（v2 三缺陷根治 + 阶段A GAS 后端 + 后端架构方案1）

## 本次会话做了什么

1. **本机基线恢复**（用户已追认并入 f6258b9）：io_api.cpp 待释放→to_free（GCC 9/7 不支持
   UTF-8 标识符，Linux 构建全链路中断）；单测 fd 操作宏化（TEST_DUP 系）。GCC 零警告 +
   单测 1196/1196 + E2E linux-arm64 基线摸底。
2. **v2 三缺陷根治**（用户裁决「先根治已立案缺陷」）：
   - 中途回退穿透：函数尾块 + IR_返回 后跳转 + 块终止感知（生成语句/块/如果/当 返回布尔）；
   - &&/|| 短路（规范 930）：语法层短路层操作数区间标注 + IR 延迟求值降低（&& 真求值右 /
     || 假求值右——方向相反勿混；双逻辑非规范化 0/1；生成表达式 签名扩 块计数/槽计数 指针）；
   - 词法转义：字符串字面量实时反转义（\n\t\r\0 + 后继字符式；\u{} 暂缓，③前对齐）。
   - 载体：**E2E 125_v2_控制流与短路与转义**（退出码 0；断言失败 201~217）。
3. **阶段A：v2 Linux ARM64 GAS 后端**（用户裁决）：新组件 代码生成ARM64.cn + 主.cn 可选
   参数 2（目标平台，默认 win-x64 不变）+ run_e2e.py 执行v2闭环Linux（as/g++/运行时.o
   现编/muldefs+顺序绑定 cn_main/nm 双向自检/POSIX 退出码%256）。**119/120/123/125 在
   linux-arm64 运行器内真实通过**——v2 闭环本机全程可运行时验证。
4. **后端架构方案1**（用户质询两后端并列后裁决）：新组件 代码生成共用.cn（249 行：mangle
   工具/帧扫描/帧大小/槽偏移/调用编址平台参数化/标签唯一化/字符串收集）；两后端瘦身为纯
   平台发射器（472/542 行），第三平台只需纯发射器。重构后全量 E2E 零回归。

## 验证状态（诚实门禁，接手先看）

- 本机（Linux ARM64）：GCC 零警告 + 单测 1196/1196 + E2E 127 用例 = **122 过 / 4 平台跳过
  （62/65/69/79）/ 1 失败 = 78 已知遗留（内存超限，旧架构固有）**。
- **待 Windows 会话**：win-x64 全量门禁（ci.ps1）——重点 119/120/123 的 MASM 路径运行时
  （本会话改了 IR 层：尾块/短路/标签 bb<fn>_<id>，MASM 产出已 asm 级核验结构无损但未运行）；
  125 的 MASM 路径首验。**不写假绿**。
- 转义内容级运行时断言：待 内置函数符号映射（v2 普通调用只发裸名符号，带参 stdlib 函数
  如 字符串长度 链接不上——③前特性）；125 暂以容器追加通路覆盖发射路径。

## 发现的问题 / 立案

- v2 词法 \u{XXXX} 暂不解码（宿主解码）——③ 前特性补齐轮对齐（125 注释已标注）。
- v2 普通函数调用无参数类型编码符号（带参 stdlib 函数不可调）——③ 前「内置函数符号映射」。
- lessons 2026-09-02 11:30：GAS 后端四坑（符号字符集/PROC 标签作用域/空参数串假 _T/
  POSIX 退出码 8 位）。

## 本轮 B1-B3 进行中状态（接手先读）

②b 已落地（语法分析/IR容器/IR生成）：B1 布局（字段真实大小+自然对齐+结构体大小/对齐/字段类型表
置 IR容器 避免导入环+结构体局部多槽+零初始化）；B2 容器返回_结构体 形态（元素(i)->T 走 retbuf，
调用后 rax/x0=结构体地址作链式 .字段 基址——宿主 epilogue mov x0,x19 已核实）；B3 结构体按值实参
传地址（裸标识符实参 IR_加载 回扫取槽改发 IR_取地址；表达式实参诊断）。**回归 119/120/123/125
全绿**。**卡点：126 运行时 SIGKILL（OOM 疑似）**——分诊建议：按段裁剪 126 源（先只留 POD 追加+大小
→ 加 元素读 → 加 设置 → 加 词条/删除）定位；重点嫌疑：B3 回扫取地址后的实参装载、元素 retbuf 路径、
多槽基址（局部槽登记=槽起+槽数-1，IR_取地址 同槽）。stdlib 侧深拷贝/删除析构注入已确认完备
（类型大小(T) 槽大小钳制+析构元素）。

## 下一步

1. **②b 结构体元素容器（收尾）**（用户裁决一步到位含资源字段结构体）：向量<Token>——结构体布局
   对齐宿主 typeSizeOf（语法层 字段偏移表按真实大小+对齐，语义层成员类型不再一律整64）、
   元素(i) 视图语义（对齐宿主 ir_expr.cpp:1707/1762 结构体元素返地址）、按值传参 ABI
   （双后端：决策进 代码生成共用.cn）、追加深拷贝 / 删除(i) 析构注入 / 整体 RAII。
   新 E2E **126_v2_结构体元素容器**（E2E 124 为宿主侧参考形状）。
2. 嵌套容器/容器作结构体字段/顶层静态容器/& 引用参数（②c）→ ③ v2 编译 9+组件自举对拍
   （**ARM64 路径本机即可全程对拍**——链接 v2p_linux.o 模式已验证；对标 79 闭环）。
3. 性能 bench（双目标①）在 ③ 后跑 scripts/bench_self_host.py。

## 踩过的坑（本会话新增，详见 lessons 2026-09-02 条目）

- 符号类链接失败先 nm 对拍 + hex 解码两侧符号，勿凭肉眼；GAS 符号仅 字母/数字/._$。
- 分割参数串("") 产出 1 个空段——空参数串必须先判空。
- POSIX 退出码 8 位：E2E 期望值 >255 时 linux 比对 %256。
- 改动前存基线 asm（target/dbg/baseline/ 已存 119/120/123 改前 MASM；target/ 已 gitignore）。
- python 批量替换 CN 源码后再用 Edit 会「File has been modified」——重读再编辑。
