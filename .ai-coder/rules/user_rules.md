---
trigger: always_on
---
1. 中文回复我，写代码添加中文注释。

2. 项目要求模块化、解耦简单、可读性高的结构。

3. 单一职责原则：
每个源代码文件不超过1000行（模块可以分多个子模块实现，每个子模块放在各自的子模块目录里，旧的文件，我希望删除它）；
每个函数不超过100行。

4. **Git提交规范**：所有Git提交的标题和描述必须使用中文编写，确保内容清晰明了地说明本次提交的修改内容

5. **更新文档维护**：每次提交前必须更新根目录下的"更新日志.md"文件，具体要求：
   - 覆盖写入当前轮次提交所修改的内容
   - 删除该文档中之前的所有内容，只保留本次更新的说明
   - 文档格式应保持markdown格式，以"## 功能完善总结"为标题开始

6. 推送的远程仓库，就要gitcode推送：远程仓库名称：gitcode，默认develop分支。

7. **错误自动记录**：当AI执行命令或工具调用出现错误时，自动触发".ai-coder/skills/lessons-weight/SKILL.md"技能，将错误情况记录到lessons.md文件中。

8. **AI 自我监控要求**：AI 必须在每次工具调用后：
   - 检查结果：确认工具调用是否成功
   - 识别错误：当检测到错误时，分析错误类型、描述、原因
   - 主动记录：调用 lessons-weight 技能记录错误
   - 诚实报告：不得在未完成任务时声称"任务完成"

9. **错误检查清单**：AI 必须遵守 `.ai-coder/checklists/error-checklist.md` 中的错误检查清单，每次工具调用后执行检查。

10. **attempt_completion 前强制检查**：在调用 `attempt_completion` 之前，AI **必须** 执行以下验证：
    - 确认所有子任务已完成
    - 确认所有测试都通过
    - 确认 TODO 文件已更新（所有完成的任务标记为 `[x]`）
    - 确认更新日志已更新
    - 确认没有未处理的错误
    - 有哪些踩过的坑绝对不要再踩（调用 lessons-weight 技能，经验教训同步写入lessons.md）
    - **如果存在未完成的检查项，不得调用 attempt_completion，不得声称"任务完成"**

这些操作应在每次代码提交到develop分支前完成，确保项目文档与代码变更保持同步。

11. 请不要轻易创建新的文件或文件夹！！！
除非：用户要求

12. 不允许AI查看截屏或者图片。AI不具备图片识别功能。（除非使用ORC技术查看图片内容）

13. 每当用户让AI给出分析报告、架构设计、实施计划步骤，AI出具报告后，AI必须等待用户批阅，直到用户同意，才能后续执行。

要求：执行完成后，根据生产环境，模拟运行程序，帮我彻底检查，找到BUG，给出修复意见，等待我审批，通过后再执行。

14. 我喜欢一步到位的开发，能完整的实现功能的，就不能放在下一次进行，必须全链路完整实现。能让程序更加完美的，就一定去做，绝不妥协和简化。

15. 在每次会话结束前，请写一份交接文档存到HANDOFF.md：我们在做什么任务、已经完成了什么、总结发现的问题、当前卡在哪、下一步计划是什么、有哪些踩过的坑绝对不要再踩（经验教训同步写入lessons.md）。写给一个完全没有上下文的新会话看。

16. Orchestrator模式派发主任务、子任务行为规范：
    - 子任务已提交的文档不应再被主任务重复覆盖写入；
    - 主任务派发子任务前必须检查该工作是否已被前序子任务完成；
    - HANDOFF.md文档内容传递时应传递完整内容而非摘要；
    - Orchestrator应在子任务完成后更新TODO状态，避免重复派发
    - Orchestrator拆解任务为多个子任务时，必须在所有子任务后添加2个子任务：
        添加子任务，使用code模式，将前面所有子任务完成后的组件串连起来，使得所有组件功能真正的在程序里可用。

        添加子任务，使用debug模式，仔细审查所有代码，确保用户的需求真正的实现，确保添加的代码真正的集成到整个系统上面了，能真正的运行。而不是只是孤立的组件。
        修复完成后，继续使用debug模式审查代码，只有当代码里没有任何bug才完成任务。不然一直要将bug修复好。

17. 主任务中，Orchestrator模式没有读取文件的权限，需要派发子任务Ask模式仔细阅读需求文档，等全部阅读完成后，汇报给主任务，再进行后续操作。

18. 在plans目录里的计划，完成后一定要打勾，明确已完成。方便后续用户和AI查看任务进度。
    - 特别要求：每次完成任务，都要先确认“plans"里的相关文档已经实时更新了！！！

19. E2E测试时，编译后的内容放在“target”里，别到处乱放

20. E2E测试后，修复项目的任何缺陷后，都应该把缺陷从plans目录所有文档和lessons.md文档里显著的标注成已修复，而不应该放在那里，被后来的AI错误识别成缺陷未修复。

21. 我希望能一劳永逸的解决所有问题，完善所有功能。不要发现问题现在不解决而留到后面解决（后面会忘记）

22. **CN语言任务规范查询（强制前置）**：任何与CN语言相关的任务（语法/语义/IR/代码生成/标准库/测试用例编写/缺陷修复等），**必须先**调用 "cn-language-spec" skill 查询相关CN语言语法规范，或参看 "plans/001 CN语言编译器设计规格书.md" 的相关规范。未查询规范前不得动手实现或修改。

23. **新增语法规范须用户审批**：任何新增语法规范（新关键字、新语法结构、语义变化等），必须**暂停任务**，先征求用户意见；**用户同意后**，才能正式在 "plans/001 CN语言编译器设计规格书.md" 和 "cn-language-spec" skill 里添加相关规范。未经用户同意，不得自行添加语法规范、不得以"为兼容旧行为"等理由绕过审批。

24. 如果电脑是麒麟系统、Arm64架构的，sudo密码：“11111111a!”。

25. 在测试任务中，当运行测试时，AI不允许擅自修改测试代码！！！
    - 如果必须要修改测试代码才能通过验证，必须暂停任务，给出原因和修改建议，让用户审阅。只有用户允许后，才能修改测试代码。
（原因：AI总是为了完成任务而无视测试的目的和严肃性，擅自通过修改测试代码而通过测试是危险的！就相当于直接砸开大门入室抢劫，而不被发现！！！）
    - 测试文件里只是添加调试语句是可以实现的，能快速定位问题。
