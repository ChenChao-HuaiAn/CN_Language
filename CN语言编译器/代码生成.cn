// 自举后端抽象层：代码生成 门面（Task 009 平台抽象层重构 第四步）
// 原 971 行 x64 MASM 硬编码逻辑已拆分为：
//   后端接口.cn（抽象接口） + IR遍历.cn（平台无关遍历引擎）
//   + x64后端.cn（Win x64 MASM） + arm64后端.cn（Linux ARM64 GAS）
//   + 后端工厂.cn（按目标平台分发后端）
// 本文件仅保留门面入口：创建后端 -> IR 遍历 -> 返回汇编行列表。
// 入口签名 代码生成(向量<字符串> IR行) -> 向量<字符串> 保持不变，
// 外部调用者（E2E 75/76/77/78/79）无需修改。
// 依据：spec 06 OOP 接口实现 + spec 08 模块系统（默认私有，公开 API 显式 公开:）。
导入 容器::向量
导入 后端接口::后端接口
导入 后端工厂::创建后端
导入 IR遍历::IR遍历

公开:

// 代码生成入口（默认平台）：IR 行 -> 汇编行列表
// 默认目标平台 win-x64（保持原行为，E2E 75 期望 MASM 输出）
函数 代码生成(向量<字符串> IR行) -> 向量<字符串> {
    返回 代码生成平台(IR行, "win-x64")
}

// 代码生成入口（指定平台）：IR 行 + 目标平台 -> 汇编行列表
// 目标平台: "win-x64"（MASM）/ "linux-arm64"（GAS）
函数 代码生成平台(向量<字符串> IR行, 字符串 目标) -> 向量<字符串> {
    后端接口 后端 = 创建后端(目标)
    向量<字符串> 输出 = IR遍历(IR行, 后端)
    返回 输出
}
