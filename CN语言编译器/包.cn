// CN语言编译器 库 crate 根（包.cn，规格书09-三 包.cn 结构）
// ---------------------------------------------------------------
// 角色（[09]-包.cn 结构，09-包与依赖.md 第三节）：
//   1. 聚合模块（3.1 职责①）：『模块 名』声明引用包内模块，加载同名 .cn /
//      目录模块树（:: 对应 / 层级，[08]-crate 模型 5.2）。
//   2. 再导出公共 API（3.1 职责②）：『公开 导入』把门面入口提升为包级 API，
//      外部经 导入 CN语言编译器::符号 使用（[08]-5.1 包.cn 根文件）。
// 消费方（自举 e2e 70~79）：
//   - 导入 CN语言编译器::词法分析          （整模块导入：分词 纯名可用）
//   - 导入 CN语言编译器::词法分析::分词     （模块::符号 路径导入）
//   - 导入 CN语言编译器::IR生成 等         （模块名=入口函数名：IR生成）
// 子模块链（词法::关键字、语法::表达式、语义::符号表…）由各门面文件内部
//   『导入』触发（词法分析.cn 等已含 导入 词法::关键字 …），无需在包根重复导入。
// 后端抽象层（Task 009 平台抽象层重构）：后端接口/IR遍历/x64后端/arm64后端/
//   后端工厂 为内部实现模块，由 代码生成 门面内部导入，无需包级再导出。
// 可见性：本文件无需『公开:』标签——模块声明仅建立模块树引用，符号可见性
//   由各门面文件内『公开:』决定（[08]-四，默认私有 + 交集规则）。

// ---- 1. 聚合模块（模块树引用）----
模块 词法分析
模块 语法分析
模块 语义分析
模块 词法
模块 语法
模块 语义
模块 IR生成
模块 代码生成
模块 后端接口
模块 IR遍历
模块 x64后端
模块 arm64后端
模块 后端工厂

// ---- 2. 再导出公共 API（外部 导入 CN语言编译器::分词 可用）----
公开 导入 词法分析::分词
公开 导入 语法分析::解析
公开 导入 语义分析::语义检查
公开 导入 IR生成::IR生成
公开 导入 代码生成::代码生成
