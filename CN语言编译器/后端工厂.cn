// 自举后端抽象层：后端工厂（Task 009 平台抽象层重构 第四步）
// 按目标平台分发后端实现：win-x64 -> x64后端（MASM）、linux-arm64 -> arm64后端（GAS）。
// 门面 代码生成.cn 经 `导入 后端工厂::创建后端` 引入，按目标平台获取后端实例，
// 再交给 IR 遍历引擎（IR遍历.cn）生成汇编。
// 依据：spec 06 OOP 接口实现（类 -> 接口 隐式转换）+ spec 08 模块系统（默认私有）。
导入 后端接口::后端接口;
导入 x64后端::x64后端;
导入 arm64后端::arm64后端;

公开:

// 创建后端 — 按目标平台分发后端实现
// 参数: 目标 目标平台名（"win-x64" / "linux-arm64"）
// 返回: 后端接口 实例（接口值类型，多态分派）
// 未知平台：运行时错误（错误码6=无效参数），回退 x64后端（不可达兜底）
函数 创建后端(字符串 目标) -> 后端接口 {
    如果 (字符串比较(目标, "win-x64")) {
        返回 x64后端();
    } 否则 如果 (字符串比较(目标, "linux-arm64")) {
        返回 arm64后端();
    }
    运行时错误(6);
    返回 x64后端();
}
