P3-18 引用返回 + P3-23 实例方法作值（绑定 this）：007-P3 收尾第一阶段（E2E 109/109、单测 1194/1194 全绿）

引用返回（函数 获取() -> 整32&）：
- 语义 registerFunction 放行 T& 返回（FunctionInfo.isRefReturn）+ visitReturnStmt 校验返回须左值
  且禁止返回本函数局部变量/按值参数地址（悬垂引用，e2e 103 负向）；checkFunctionBody 记录
  引用参数集与函数局部作用域起点；调用方支持 整32& r=获取() / 获取()=值 / &获取()
  （CallExpr.isRefReturnCall + funcReturnsRef）；各调用点写回引用返回标记。
- IR：引用返回函数返回类型映射 ptr（默认 rax epilogue）+ return 左值地址化（调用链复用地址）；
  引用变量条目类型 = 被引用基础类型（byRef 读解引用返回基础值）。
- 单测 RefReturn* 5 项（替换旧「引用返回不支持」锚点，净 +4）；e2e 102 正 / 103 负。
- 规范 skill 04-一F2 +《001》同步；005/007/更新日志/README/HANDOFF/lessons 四件套。

实例方法作值（对象.方法）：
- 语义 visitMemberExpr 标记 MemberExpr.isMethodValue（函数指针签名）；D2 诊断禁止
  绑定方法值作裸 fnptr 实参（调用实参循环，e2e 107 负向）。
- IR 复用 lambda 闭包机制：genVarDecl 对方法值初始值登记 closureInfo_（captureArgs=对象地址
  Load 槽）；cb(实参) 展开 this 直接 Call 方法符号（this+用户实参）；引用捕获读最新对象。
- 规范 skill 04-四 方法作值 +《001》同步；文档四件套。

C2/D3A 锁定：e2e 105_接口参数多态、106_接口去虚拟化 已随本批纳入全量（先绿）。
