函数 主程序() {
    整数 i = 0;
    整数 sum = 0;
    
    // 验证 for 循环
    从 (i = 1; i <= 10; i = i + 1) {
        sum = sum + i;
    }
    
    // 验证 if-else 与比较运算
    如果 (sum == 55) {
        打印("循环测试通过: 55\n");
    } 否则 {
        打印("循环测试失败\n");
    }
    
    // 验证 while 循环
    整数 j = 5;
    当 (j > 0) {
        j = j - 1;
    }
    
    如果 (j == 0) {
        打印("当循环测试通过\n");
    }
    
    返回 0;
}
