函数 主程序() {
    整数 a = 10;
    整数* p = &a;
    打印(*p);
    *p = 20;
    打印(a);

    数组 arr = [1, 2, 3, 4];
    整数* ap = &arr[1];
    *ap = 99;
    打印(arr[1]);
    ap = ap + 1;
    打印(*ap);
    打印(ap - &arr[1]);

    整数& r = a;
    r = 30;
    打印(a);
    打印(r);
    返回 0;
}

