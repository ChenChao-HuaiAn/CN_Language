// 测试常量字段和常量参数

结构体 配置 {
    常量 整数 最大值;
    整数 当前值;
    常量 字符串 名称;
}

函数 更新配置(常量 整数 新最大值, 整数 新当前值) {
    // 不能修改常量参数
    // 新最大值 = 100;  // 这行会报错
    
    打印整数(新当前值);
}

函数 测试() {
    变量 cfg = 配置 { 最大值: 100, 当前值: 50, 名称: "测试配置" };
    
    // 可以修改普通字段
    cfg.当前值 = 60;
    
    // 不能修改常量字段
    // cfg.最大值 = 200;  // 这行会报错
    // cfg.名称 = "新名称";  // 这行也会报错
    
    打印整数(cfg.当前值);
}

函数 主函数() {
    测试();
    返回 0;
}
