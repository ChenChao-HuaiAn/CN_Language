// CN Language 中断处理示例
// 演示中断处理的基本语法和使用

// 定义中断向量号常量（模拟硬件定时器中断）
// 实际中断向量号应根据具体硬件平台定义

// 中断处理函数：定时器中断0  
// 语法：中断处理 <向量号> () { 函数体 }
中断处理 0 () {
    // 在中断处理函数中执行简单的计数
    // 注意：中断处理函数不应执行复杂或耗时的操作
    打印("定时器中断0被触发\n");
}

// 中断处理函数：键盘中断
中断处理 2 () {
    打印("键盘中断被触发\n");
}

// 主程序
函数 主程序() {
    打印("中断处理示例启动\n");
    打印("中断处理函数已自动注册\n");
    
    // 注意：实际的中断启用和触发需要硬件支持
    // 这里仅演示语法，运行时库提供了测试用的软件触发功能
    
    返回 0;
}
