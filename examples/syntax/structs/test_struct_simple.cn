// 简单结构体测试
函数 主程序() {
    // 结构体定义
    结构体 人员 {
        字符串 姓名;
        整数 年龄;
    }
    
    // 创建结构体实例
    变量 张三 = 人员{姓名 : "张三", 年龄: 25};
    
    // 访问结构体字段
    打印("姓名: " + 张三.姓名);
    打印("年龄: " + 张三.年龄);
    
    返回 0;
}
