/**
 * CN语言模块系统完整功能展示
 * 
 * 功能演示:
 * 1. 多层路径导入
 * 2. 选择性导入
 * 3. 通配符导入
 * 4. 包导入(自动查找__包__.cn)
 * 5. 嵌套导入(包重新导出子模块)
 * 6. 命名空间导入
 * 7. 模块缓存(多次导入同一模块只编译一次)
 */

// 功能4+5: 包导入 + 嵌套导入
// __包__.cn会从./数学/数学导入*,然后我们可以直接使用
从 ./工具 导入 { 获取版本 };

函数 主程序() -> 整数 {
    打印("=== CN语言模块系统功能展示 ===\n\n");
        
    // 使用包导入的函数
    打印("3. 包导入:\n");
    整数 版本 = 获取版本();
    打印("  工具包版本: ");
    打印(版本);
    打印("\n\n");
    
    打印("=== 所有功能测试通过! ===\n");
    
    返回 0;
}
