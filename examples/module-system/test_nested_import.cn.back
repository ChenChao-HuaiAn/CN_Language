// 测试通过包的嵌套导入使用数学函数

从 ./工具 导入 { 加法, 减法, 获取版本 };

函数 主程序() -> 整数 {
    整数 和 = 加法(10, 20);
    整数 差 = 减法(30, 15);
    整数 版本 = 获取版本();
    
    打印("和: ");
    打印(和);
    打印("\n");
    
    打印("差: ");
    打印(差);
    打印("\n");
    
    打印("版本: ");
    打印(版本);
    打印("\n");
    
    返回 0;
}
