// 运行时全面测试示例
函数 主程序() {
    变量 文本 = "  你好, CN_Language!  ";
    变量 修整后的文本 = 文本; // 目前编译器可能还不支持 trim 语法，我们先测试基础调用
    
    打印(文本);
    打印("\n");
    
    变量 长度值 = 长度(文本);
    打印("文本长度: ");
    // 目前打印仅支持字符串，所以我们依赖后端映射
    
    变量 数组 = [1, 2, 3, 4, 5];
    变量 数组长度 = 长度(数组);
    
    返回 0;
}
