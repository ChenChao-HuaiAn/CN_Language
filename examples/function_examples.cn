// CN语言函数示例
// 用于词法分析器测试

// 基本函数定义
函数 计算加法(整数 a, 整数 b) {
    返回 a + b;
}

// 带返回值的函数
函数 计算乘法(整数 x, 整数 y) {
    变量 结果 = x * y;
    返回 结果;
}

// 无参数函数
函数 获取常量() {
    返回 42;
}

// 主程序入口
函数 主程序() {
    变量 和 = 计算加法(3, 5);
    变量 积 = 计算乘法(4, 6);
    变量 var_常量 = 获取常量();
    打印(和);
    打印(积);
    打印(var_常量);
    返回 0;
}
