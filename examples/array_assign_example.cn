函数 主程序() {
    变量 arr = [10, 20, 30];
    
    // 修改数组元素
    arr[0] = 100;
    arr[1] = 200;
    arr[2] = 300;
    
    // 读取修改后的值
    变量 a = arr[0];
    变量 b = arr[1];
    变量 c = arr[2];
    
    返回 0;
}
