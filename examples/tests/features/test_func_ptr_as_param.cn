// 测试：函数指针作为函数参数
// 验证函数指针能否作为函数参数传递
// 注意：当前CN语言可能不支持函数指针作为函数参数

// 打印处理函数
函数 打印元素(整数 值) { 返回 值 * 2; }

// 主函数测试 - 使用局部函数指针变量
函数 主函数() {
    // 函数指针变量声明并初始化
    整数(*处理)(整数) = 打印元素;
    
    // 通过函数指针调用
    变量 结果 = 处理(5);
    
    // 返回测试结果：期望 10
    返回 结果;
}
