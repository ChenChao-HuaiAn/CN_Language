// 测试用例：静态变量类型推断
// 目的：验证使用"变量"关键字的静态变量类型推断
// 期望：静态变量能够正确推断类型

函数 生成ID() -> 整数 {
    静态 变量 id = 0;  // 类型推断为整数
    id = id + 1;
    返回 id;
}

函数 主程序() -> 整数 {
    变量 r1 = 生成ID();  // 期望：1
    变量 r2 = 生成ID();  // 期望：2
    
    返回 r1 + r2;  // 返回 3 表示测试通过
}
