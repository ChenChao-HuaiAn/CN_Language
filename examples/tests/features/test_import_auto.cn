// 测试导入语句的自动符号导入功能

// 定义一个数学模块
模块 数学 {
    整数 圆周率 = 314;
    整数 自然数 = 271;
}

// 导入数学模块
导入 数学;

// 主函数：测试可以直接访问导入的符号（无需模块前缀）
函数 主程序() {
    // 直接使用导入的符号，无需 "数学." 前缀
    变量 pi = 圆周率;
    变量 e = 自然数;
    
    变量 结果 = pi + e;
    
    返回 结果;
}
