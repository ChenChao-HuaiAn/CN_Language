// 模块可见性控制测试
// 默认行为：模块成员默认为私有，只有显式标记为"公开:"的成员才是公开的

模块 测试模块 {
    公开: 整数 公开变量 = 100;
    私有: 整数 私有变量 = 200;
    整数 默认变量 = 300;  // 默认为私有
}

函数 主程序() {
    // 访问公开成员 - 应该成功
    变量 值1 = 测试模块.公开变量;
    
    // 访问私有成员 - 应该报错
    // 变量 值2 = 测试模块.私有变量;
    
    // 访问默认可见性成员 - 应该报错（默认为私有）
    // 变量 值3 = 测试模块.默认变量;
    
    返回 值1;
}
