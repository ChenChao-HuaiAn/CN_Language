// 模块别名冲突测试
// 测试别名与现有符号冲突的场景

模块 模块A {
    公开: 整数 数据A = 1;
}

模块 模块B {
    公开: 整数 数据B = 2;
}

// 场景1：别名与变量名冲突
整数 别M = 10;
导入 模块A(别M);  // 应该报错：别名冲突

// 场景2：两个模块使用相同别名
导入 模块A(共同别名);
导入 模块B(共同别名);  // 应该报错：别名已存在

函数 主程序() {
    返回 0;
}
