// 测试模块成员默认为私有

模块 配置 {
    公开: 整数 版本号 = 1;  // 显式公开
    整数 内部计数器 = 0;      // 默认私有，不应该被外部访问
}

函数 主程序() {
    // 访问公开成员 - 应该成功
    变量 v = 配置.版本号;
    
    // 尝试访问默认成员（私有） - 应该报错
    变量 c = 配置.内部计数器;
    
    返回 v;
}
