# 测试 freestanding 模式违规检查示例
# 此文件包含在 freestanding 模式下禁止使用的函数

函数 主函数(): 整数 {
    # 以下函数在 freestanding 模式下应该被禁止
    
    # 文件 I/O - 应该被检测到
    变量 文件 = 文件打开("test.txt");
    
    # 控制台输入 - 应该被检测到
    变量 输入 = 读取行();
    
    # 字符串高级操作 - 应该被检测到
    变量 文本 = "  示例  ";
    变量 处理后 = 去空格(文本);
    
    返回 0;
}
