/*
 * CN Language 最小内核示例
 * 用于测试语法正确性
 * 更新 (2026-01-27): 迁移至新的运行时API
 */

// 全局变量
变量 计数器 = 0;

// 中断处理程序 (向量0)
函数 定时器中断() {
    计数器 = 计数器 + 1;
}

// 主函数
函数 kernel_main() {
    打印("CN Kernel\n");
    
    // 注册中断处理程序
    cn_rt_interrupt_register(0, 定时器中断);
    
    返回 0;
}
