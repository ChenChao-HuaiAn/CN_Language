// 测试选择性导入功能

模块 数学工具 {
    公开: 整数 常量A = 100;
    公开: 整数 常量B = 200;
    私有: 整数 私有常量 = 999;
    
    公开: 函数 加法(整数 x, 整数 y) {
        返回 x + y;
    }
    
    公开: 函数 减法(整数 x, 整数 y) {
        返回 x - y;
    }
}

// 选择性导入：只导入 常量A 和 加法
导入 数学工具 { 常量A, 加法 };

函数 主程序() {
    // 可以直接访问导入的成员
    变量 x = 常量A;  // 应该成功：100
    变量 y = 加法(10, 20);  // 应该成功：30
    
    // 不能直接访问未导入的成员（应该报错或需要使用模块前缀）
    // 变量 z = 常量B;  // 如果取消注释，应该报错
    
    // 仍可使用模块前缀访问其他公开成员
    变量 z = 数学工具.常量B;  // 应该成功：200
    变量 w = 数学工具.减法(50, 20);  // 应该成功：30
    
    返回 0;
}
