// 枚举测试示例

// 定义一个简单的枚举
枚举 颜色 {
    红色,
    绿色,
    蓝色
}

// 定义一个带显式值的枚举
枚举 状态 {
    未开始 = 0,
    进行中 = 1,
    已完成 = 2
}

// 定义一个非连续值的枚举
枚举 优先级 {
    低 = 1,
    中 = 5,
    高 = 10
}

函数 主程序() {
    变量 c = 红色;
    变量 s = 进行中;
    变量 p = 高;
    
    返回 0;
}
