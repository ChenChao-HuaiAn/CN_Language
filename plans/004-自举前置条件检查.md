# CN语言编译器实施计划（第四批入口：自举前置条件检查）

> 前序文档：[plans/003-与C++和Rust对比分析报告.md](plans/003-与C++和Rust对比分析报告.md)（批次 A/B/C 全部完成，第四批第 15 项）
> 后续文档：[plans/002-阶段7-自举.md](plans/002-阶段7-自举.md)（阶段七：自举 7.1~7.6）
> 基于设计规格书：[plans/001 CN语言编译器设计规格书.md](plans/001%20CN语言编译器设计规格书.md) + cn-language-spec（C++风格版 v2.0）

## 一、目标

自举（阶段七）**前置条件检查**：验证 CN 语言/标准库/工具链对「用 CN 写 CN 编译器」的完备性，输出**缺口检查报告**，明确每项能力 **自举前必须补 / 可后置** 的判定与执行顺序，为阶段 7（7.1~7.6）扫清障碍。

**当前基线**（批次 A/B/C 完成后）：单测 1159/1159、E2E 63/63、编译零警告（MSVC /W4 /WX）。

**核心判据**：自举成功后编译器本体与标准库全部在 **CN 源码** 中开发，C++ 版退化为引导编译器。因此：
- 「自举时/自举后立即需要」→ 自举前必须补（✅ 前置）；
- 「自举源码可绕开或自举后 CN 开发成本低」→ 可后置（📌 后置）。

---

## 二、检查范围与分工判定（2026-08 用户批复写入）

### A. 编译器 5 大件语言完备性（规格书阶段七前置盘点）

| # | 检查项 | 内容 | 判定 |
|---|--------|------|:---:|
| A-1 | 词法分析能力 | 字符串/字符处理、UTF-8、大数组、多文件模块读取 | ✅ 前置 |
| A-2 | 语法分析能力 | 递归、lambda 回调、错误恢复、字符串比较、遍历 迭代 | ✅ 前置 |
| A-3 | 语义分析能力 | **符号表（哈希映射）**、泛型、类/继承、结果/可选、引用参数 | ✅ 前置 |
| A-4 | IR 生成能力 | 结构体数组、指针运算、位操作、i128、字符串常量池 | ✅ 前置 |
| A-5 | 代码生成能力 | 大函数、嵌套控制流、枚举选择、fall-through、虚表 | ✅ 前置 |

**检查方法**：小样实验——用 CN 写微型词法器/符号表/表达式求值原型（对 1000 行级源码编译），验证 5 大件依赖的语言特性均可表达且性能可接受。

### B. 标准库依赖（自举源码运行所需）

| # | 检查项 | 现状 | 判定 |
|---|--------|------|:---:|
| B-1 | **哈希映射 映射<T,K> O(1)** | ❌ 映射/集合为 O(n) 线性查找 | ✅ **自举硬前置**（符号表/作用域链/字符串池核心；6 万行源码 O(n) 查找不可用） |
| B-2 | 字符串处理（拼接/比较/查找/子串/格式化） | ✅ 已有（字符串扩展） | ✅ 前置（检查覆盖度） |
| B-3 | 向量/链表容器 + 遍历...中每个 | ✅ 已有（A-3 槽修复 + C-2 遍历） | ✅ 前置 |
| B-4 | 文件 IO（读源码/写产物） | ✅ 已有 | ✅ 前置 |
| B-5 | 随机数 | ❌ 缺失 | 📌 后置（哈希 seed 可固定；自举后 stdlib 补） |
| B-6 | 正则表达式 | ❌ 缺失 | 📌 后置（词法分析手写状态机，不需要） |
| B-7 | 线程/并发 + 原子 | ❌ 缺失 | 📌 后置（单线程可自举） |
| B-8 | 数学库补全（对数/复数） | ⚠️ 部分 | 📌 后置（编译器仅需基础算术/i128） |

### C. 内存管理（自举源码可维护性）

| # | 检查项 | 现状 | 判定 |
|---|--------|------|:---:|
| C-1 | 字符串/容器自动释放（RAII 基础） | ⚠️ 手写析构 + 手动释放 | ✅ 建议前置（自举源码内存安全底线） |
| C-2 | 智能指针（unique_ptr/shared_ptr 等价物） | ❌ 缺失 | 📌 可后置（编译器为一次性进程，可用 arena/池分配模式绕开；Lua/早期 GCC 先例） |
| C-3 | 泄漏检测手段（ASan 集成/分配计数跟踪） | ❌ 无 | ✅ 建议前置（在 C++ 版上先做，自举后 CN 编译器同样受益） |

### D. 工具链与验证

| # | 检查项 | 现状 | 判定 |
|---|--------|------|:---:|
| D-1 | **性能基准（对标 C 编译器 -O0）** | ✅ 已执行（2026-08-18，见执行情况四） | ✅ 前置基线（自举前跑 C++ 版基线，自举后跑 CN 版对比，验证「性能达 C -O0」里程碑） |
| D-2 | Sanitizer/泄漏检测集成 | ❌ 无 | ✅ 建议前置（同 C-3） |
| D-3 | 增量编译 | ❌ 无 | 📌 后置（开发体验优化，非自举依赖） |
| D-4 | DWARF 调试信息 | ⚠️ 仅预留行号注释 | 📌 后置 |

### E. 生态（长期，自举后）

| # | 检查项 | 现状 | 判定 |
|---|--------|------|:---:|
| E-1 | LSP/语法高亮 | ❌ 无 | 📌 后置（自举后用 CN 写，宣传点） |
| E-2 | 文档站/包注册中心 | ❌ 无 | 📌 后置 |

---

## 三、两阶段自举验证方案（阶段 7 Task 7.6 前置确认）

1. **步骤 1**：C++ 版 cn 编译器编译 CN 版编译器源码 → CN 版编译器可运行（引导）；
2. **步骤 2**：CN 版编译器再次编译自身源码 → 产物行为一致（自举成功）；
3. **回归门槛**：全部 E2E 用例在自举编译器下通过 + 单测全绿 + 零警告。

---

## 四、输出：自举前置条件检查报告

检查完成后输出报告（写入本计划"执行情况"节 + 更新日志），报告须包含：

1. 每项状态：✅ 已具备 / ⚠️ 部分具备（列出缺口）/ ❌ 缺失；
2. 前置/后置判定及理由（对照第二节判定表）；
3. **缺口清单**（自举前必须补的项 + 建议前置项 + 可后置项）；
4. 修复建议与执行顺序（供用户批复）；
5. 两阶段验证方案的可行性确认。

---

## 五、任务清单

- [x] A. 5 大件语言完备性盘点（小样实验：微型词法器/符号表原型）
- [x] B. 标准库依赖检查（B-1 哈希映射缺口实测：映射<T,K> 线性查找性能数据）
- [x] C. 内存管理检查（C-1/C-3 现状盘点 + 一次性内存模式可行性验证）
- [x] D. 工具链检查（D-1 性能基线：C++ 版 cn 编译自举小样计时数据）
- [x] E. 生态检查（现状确认，标注后置）
- [x] 输出「自举前置条件检查报告」（本计划执行情况 + 更新日志 + plans 打勾）
- [x] 中文提交（gitcode develop，SSH 推送）

> ✅ 已完成（2026-08-16 用户批复后执行，检查报告见下）


---

## 执行情况（2026-08-16 检查完成 ✅）

### A 组：5 大件语言完备性（小样实验：target/exp/自举检查/主.cn 编译运行通过）

小样覆盖：A-2 递归（阶乘10=3628800 ✓）、A-3 结果+?传播（除法链=6、除零错误码=1 ✓）、
A-1 字符串处理（切词数=7、UTF-8 字节长=45 ✓）、A-4 结构体数组+遍历（平均分=90 ✓）、
A-5 枚举分发（类型=数字 ✓）。

**发现缺口 4 项（自举前须修）：**

| # | 缺口 | 实测 | 影响 | 修复建议 |
|---|------|------|------|---------|
| A-1 | **无 字符串[i] 逐字节访问** | 仅 字符串查找/子串（子串每次分配新串） | 词法器每字符一次 malloc = 百万级分配，不可接受 | 新增 字符串[i]（O(1) 字节访问）或 字符串→字符数组 转换 |
| A-2 | **选择 全分支返回不识别** | 类型名() 用 选择 全分支返回误报"缺少返回语句"（bodyGuaranteesReturn 不识别 选择） | 编译器常见代码模式（枚举分发+返回）受阻 | bodyGuaranteesReturn 增加 选择 全分支返回识别 |
| A-3a | **泛型实例化类型不能作函数参数** | 函数 词频统计(向量<字符串> 词表) 解析失败"预期参数名，实际为 '<'"（parseParamDecl 无模板判定） | 容器参数是编译器常态，自举源码大面积受阻 | parseParamDecl 支持模板类型（Less 判定+parseTypeNameEx） |
| A-3b | **自定义泛型类实例作返回类型类型不匹配** | 返回 输出表（向量<字符串>）报"无法将 '向量$字符串' 转换为 '向量<字符串>'" | 返回容器/自定义泛型不可用 | 返回类型模板名统一（复用 A-4 机制） |

### B 组：标准库依赖

- **B-1 哈希映射缺口实测**（时钟毫秒计时）：映射<整64,整64> 2000 条插入 **11ms**、2000 次查找 **9ms**（O(n) 线性表）。推演：自举符号表 5 万符号 → 插入约 3.4s + 每标识符引用查找 ×2.5万 比较 → **总耗时分钟级，不可用**。✅ **哈希映射=自举硬前置确认**（与判定表一致）
- B-2 字符串处理 ✅（15 内置 + 扩展 13 函数，小样验证）；B-3 容器+遍历 ✅；B-4 文件 IO ✅
- B-5 随机数 / B-6 正则 / B-7 线程：❌ 确认缺失，📌 后置（不阻塞自举）

### C 组：内存管理

- stdlib 释放/析构点统计：**32 处**（字符串扩展 13、容器 10、映射集合 5、系统/时间/IO/文件 各 1）——「手写析构 + 手动释放」模式确认，无 RAII 自动管理
- **一次性进程 arena 模式可行性** ✅：编译器内存模式（长生命周期+一次分配）适配，智能指针可后置
- 建议前置：C-1 字符串/容器自动释放（自举源码内存安全底线）、C-3 泄漏检测（分配计数）

### D 组：工具链

- **D-1 性能基线（C++ 版 cn）**：纯编译 30 行无依赖 **39ms**；含 映射集合 模块依赖 **330ms**。推演 6 万行自举源码 ≈ 80s~5min（C++ 版）；CN 版 -O0 级估算 10~100min（可接受边界，**需哈希映射 + 优化器支撑**）
- D-2 Sanitizer/泄漏检测 ❌ 前置建议；D-3 增量编译 ❌ 后置；D-4 DWARF ⚠️ 仅行号预留

### E 组：生态

- LSP / 文档站 / 包注册中心：❌ 确认无，📌 后置（自举后 CN 源码开发）

---

## 缺口修复建议顺序（供用户批复）

1. **哈希映射**（B-1，硬前置）：重写 stdlib/映射集合.cn 为 O(1) 哈希（桶+链，泛型 K 哈希函数）+ E2E 性能回归
2. **语言 4 缺口一批**（A-1/A-2/A-3a/A-3b）：字符串[i] + 选择全分支返回 + 泛型参数 + 泛型返回统一，各配单测+E2E
3. **内存管理**（C-1 自动释放 + C-3 分配计数泄漏检测）
4. **阶段 7 自举**（7.1~7.6，两阶段验证 + 性能基线对比）

> 检查产物：target/exp/自举检查/主.cn（小样，可复跑）、本报告。

---

## 执行情况（2026-08-16 用户批复后，三项修复全部完成 ✅）

### 修复 1：哈希映射（B-1，硬前置）✅ 已提交 a89fbab

- stdlib/映射集合.cn：映射<K,V> 线性表 → **O(1) 哈希表**（Knuth 乘法散列 + 2 的幂桶 + 链数组 + 负载 3/4 扩容重哈希 + 交换删除）
- 自举符号表规模（5 万符号）毫秒级（线性表推演分钟级）
- **顺带修复核心缺陷**：泛型类实例化生成的 结果/可选 合成结构体漏 `push_back(错误值联合)` 字段（ensureLoweredType）→ 结构体拷贝丢 整64 高 4 字节（获取 返回垃圾）；结果/可选 返回统一走隐藏返回指针协议（含空类型结果 result.type=void 场景），返回缓冲移入帧内固定区防悬垂
- E2E 64_hash_map（2000 键扩容/重哈希/删除/负数键/性能断言）+ 单测 3 项修正（帧 +16）

### 修复 2：语言 4 缺口（A-1/A-2/A-3a/A-3b）✅ 已提交 d788dd2

- **A-1 字符串[i] 逐字节 O(1) 访问**：语义 字符串 下标 -> 字符；IR 标识符/字段/成员三路径 字符* 步进 1（词法器逐字符不再每字符一次 malloc）
- **A-2 选择 全分支返回识别**：stmtGuaranteesReturn（选择 全分支+默认、多值分组 情况 a,b:、如果/否则 双分支）
- **A-3a 泛型实例化类型作函数参数**：parseParamDecl 模板判定 + registerFunction 实例符号名统一（向量<字符串> -> 向量$字符串）
- **A-3b 泛型类实例作返回类型**：canConvertType 模板/实例名统一
- **顺带修复 3 个深层缺陷**：① visitFunctionDecl 漏弹参数作用域（varStack_ 泄漏 -> 字段访问误判局部变量 rbp0）；② 第二趟a 重复检查实例化类（共享 AST 引用参数双重包装）；③ 返回类对象时局部析构双重释放（0xC0000374）——返回块按返回值溯源跳过所有权转移变量析构
- E2E 65_string_index / 66_switch_all_return / 67_generic_param / 68_generic_return + 单测 11 项

### 修复 3：内存管理（C-1 自动释放 + C-3 泄漏检测）✅ 已提交

- **C-3 分配计数泄漏检测**：cn_alloc/cn_free/cn_realloc 全路径原子计数（活动分配数/累计次数）；字符串/时间/文件 API 内部 malloc 统一改走计数辅助（*_tracked）——此前 __cn_str_free 减计数而分配未加计数导致计数失真
- **C-1 进程竞技场**：一次性 bump 分配（块链 64KB、8 字节对齐、超块扩容），竞技场重置 整块释放——适配编译器内存模式，自举源码无需逐对象释放（杜绝泄漏/双重释放）
- CN 内置：内存::活动分配数 / 内存::总分配次数 / 内存::竞技场分配 / 内存::竞技场重置 / 内存::竞技场活动字节（规格书 plans/001 10.2 已更新）
- E2E 69_memory_management（容器 RAII 自动释放/字符串释放/竞技场/无泄漏检查）+ 单测 8 项（MemoryApiTest）

**全量回归：单测 1178 + E2E 69 全绿。剩余：阶段 7 自举（7.1~7.6）。**

---

## 执行情况（2026-08-17 自举闭环修复完成 ✅）

### 自举真实闭环达成（阶段 7 验收标准全部满足）

**背景**：阶段 7 早期（2026-08-16）的 `代码生成.cn` 输出的是「简化 x64 伪汇编描述字符串」
（`段|`/`函数|`/`汇编|` 前缀 + 运算符号原样当文本），ml64 三态实测全部失败——无法产出
可运行二进制，「用 CN 编译器编译自身并运行产物」的严格自举闭环不成立（前序审查裁决：
⚠️ 部分自举成功）。

**2026-08-17 修复（全部完成 ✅）**：

1. **代码生成.cn 伪汇编 → 真实 x64 MASM 后端**（971 行，≤1000 合规）：
   - `.data` 字符串池（UTF-8 字节编码）+ `.code` 段
   - `EXTERN` 声明 19+ 运行时符号
   - `PROC`/`ENDP` 函数框架 + 栈帧（push r12 + push rbp / mov rbp,rsp / sub rsp,N）
   - 求值栈 `[rbp-F+r12]`（r12 为栈顶指针）
   - `__chkstk` 三段式栈探测（大栈帧）
   - 参数寄存器映射 rcx/rdx/r8/r9 + 栈参 + 影子空间 + 16 字节对齐
   - 符号 mangling `?UTF8hex@@Y码@Z`（与 C++ 版 x64_codegen 对齐）
2. **IR生成.cn 双参数**：`IR生成(源码, 模块名)`（997 行，模块名用于符号 mangling 前缀）
3. **E2E 升级真实闭环**：新增 `79_bootstrap_closed_loop`——C++ 版编译 79 主.cn -> 运行
   落盘 5 个 `*_链.asm`（第一次）-> ml64 汇编 5 个 .obj -> 链接（链.obj 在前 + 入口 obj
   + 9 运行时 .obj）-> `cn_compiler_self.exe` -> 运行再次落盘（第二次）-> `fc /b`
   逐字节比对一致 -> 符号自检（map 确认 5 个链.obj 贡献符号 + cn_main 来自入口 obj）
4. **虚假验收识别与修复**（Debug 审查战果）：
   - 识破 `/FORCE:MULTIPLE` 下入口 obj 在前导致组件符号全绑 C++ 版、链.obj 零贡献、
     原 79 全绿为假验证
   - 修复链接顺序（链.obj 提前）+ 新增符号保留方向自检 + 第二次落盘前删旧 .asm 防线
   - 污染测试证明第二次落盘由 exe 内 CN 组件实时执行
   - 判据纠正为阶段7验收步骤2「产物行为一致」（第一次=C++版产物运行结果 vs
     第二次=CN自编译版运行结果，逐字节一致）
5. **全量回归**：构建 0 错误 0 警告（MSVC /W4 /WX）/ 单测 1189/1189 / E2E 79/79 全绿
   （含 79 真实闭环用例）

**结论**：阶段 7 验收标准（plans/002-阶段7-自举.md:80）已达成——步骤1 C++ 版编译 CN 版
编译器源码 -> 可运行 ✓；步骤2 CN 版再次编译自身 -> 产物行为一致（fc /b 逐字节一致）✓；
全部 E2E 79/79 通过 ✓。**自举前置条件检查（本计划）所列缺口全部闭环。**

---

## 执行情况四：性能基线对比（2026-08-18 D-1 预留项执行完成 ✅）

### 背景

自举真实闭环达成后，执行 D-1 性能基线对比：自举后跑 CN 版编译器对比 C++ 版，
验证「性能达 C -O0」里程碑。新增测量脚本 `scripts/bench_self_host.py`
（复用 run_e2e.py 79 闭环编排，聚焦性能测量）。

### 测量点（口径对齐 79 闭环）

| 测量点 | 说明 |
|--------|------|
| M1 | C++版完整 build（cn.exe build 79主.cn，含编译+链接） |
| M2 | C++版组件链落盘（运行 79 exe 第一次落盘 5 个 *_链.asm，3 次取中位数） |
| M3 | ml64 汇编（5 个 .asm -> 5 个 .obj） |
| M4 | link 链接（-> cn_compiler_self.exe） |
| M5 | CN版组件链落盘（运行 cn_compiler_self.exe 第二次落盘，3 次取中位数） |
| M6 | 正确性验证（两次落盘产物逐字节一致，保证性能对比有意义） |

### 实测数据（Release 版 cn.exe，MSVC 14.44.35207）

输入规模：5 组件源码 **3604 行**（词法 477 / 语法 965 / 语义 194 / IR 997 / 代码生成 971）

| 测量点 | 耗时(ms) |
|--------|---------:|
| M1 C++版完整 build | 2291.635 |
| M2 C++版组件链落盘（中位数） | 697.714 |
| M3 ml64 汇编 | 51.719 |
| M4 link 链接 | 45.086 |
| M5 CN版组件链落盘（中位数） | 727.932 |
| M6 正确性验证 | ✅ 逐字节一致 |

### 结论

- **CN版/C++版 组件链编译比值 ≈ 1.04x**（几乎 1:1，CN 版性能与 C++ 版持平）
- CN 版推演 6 万行 ≈ **12s**，远优于 004 可接受边界（≤100min 硬上限 / 10min 理想目标）
- **✅ 达到「性能达 C -O0」里程碑（优于 10min 理想目标）**
- 与 004 已有数据对比：C++ 版纯编译 30 行 39ms / 含映射集合依赖 330ms；
  本轮 C++ 版组件链编译 3604 行 697.714ms（含 5 组件全链，口径不同但量级一致）
- 报告产物：`target/bench/性能基线报告.md`（可复跑：`python scripts/bench_self_host.py`）

### 审查确认（2026-08-18 Debug 审查 ✅）

- **测量真实性**：M2/M5 运行前删除旧 `*_链.asm`（存在=本次真的写了）；M5 运行
  `cn_compiler_self.exe`（map 确认 5 个 chain.obj 贡献符号 + cn_main 来自入口 obj，
  非误用 C++ 版 exe）；M6 两次落盘产物逐字节比对真实执行
- **3 次取中位数**：M2/M5 各测 3 次排序取中间值（非平均值/最大值），样本已写入报告可审计
- **口径对齐**：M2/M5 同口径（均运行 exe 落盘 5 个 `*_链.asm`），比值 = M5/M2
- **可复跑**：`python scripts/bench_self_host.py --cn target/Release/cn.exe` 复跑成功，
  报告重新生成，M6 逐字节一致，E2E 79 回归通过
- **推演假设**：6 万行推演按「耗时与行数成正比」线性外推（`耗时 × 60000/3604`），
  报告已标注假设与局限（哈希映射 O(1) 查找增长影响小，量级判断不受影响）
- **与 004 推演差异**：004 基于 30 行小样本外推（39ms/330ms → 80s~5min），
  本轮基于 3604 行实测组件链（更接近真实规模），推演值更低更可信
