# CN语言编译器设计规格书

> 版本：v1.0  
> 日期：2026-08-12  
> 状态：待审批  
> 基准：以本规格书定义的语法风格为准，参考C++编程范式，用C++从零编写编译器

---

## 一、语言设计概述

### 1.1 语言定位
CN语言是一门全中文语法的系统级编程语言，参考C++的编程范式，面向需要底层控制力的应用场景。语言设计遵循"中文优先、手动控制、零成本抽象"的原则。

### 1.2 设计决策汇总

| # | 决策项 | 确认结果 |
|---|--------|---------|
| 1 | 语法基准 | 以本规格书定义的语法风格为准，Rust参考仅作技术借鉴 |
| 2 | 编译器实现 | 从零用C++重写，不依赖现有C代码 |
| 3 | 代码生成后端 | 直接生成汇编代码，无外部依赖 |
| 4 | 目标平台 | 设计考虑全平台，初期实现 Win x64 + Linux ARM64 |
| 5 | 内存管理 | 指针+手动内存管理（malloc/free式），运行时API |
| 6 | 错误处理 | 错误码+可选值，去掉异常关键字，语法风格参照本规格书 |
| 7 | OOP | 完整保留（类/接口/继承/保护/虚拟/重写/抽象/实现/自身/父类） |
| 8 | 模块系统 | **v2.0**：文件即模块 + 包=crate，`导入`/`模块`/`作为` 关键字（`包`/`货舱` 死保留字已摘除，plans/018 2026-09-07），`::` 路径，导入全形式（花括号/通配符/重命名），公开:/私有:标签式 + **默认私有** + 可见性交集 |
| 9 | 构建系统 | CMake |
| 10 | 编译器架构 | 方案C（多趟+自定义IR），包含IR层和优化器 |
| 11 | 开发路径 | 参考 Rust参考/003 路线图，直到自举 |
| 12 | 类型命名 | 中文位宽命名（整8/正8/整16/正16...整128/正128/浮32/浮64） |
| 13 | 字面量增强 | 增加Unicode字符转义、原始字符串、多行字符串（不增加数字下划线） |

> **重要：本规格书为编译器实现的唯一基准文档。**

---

## 二、关键字体系

本规格书定义共**46个保留字**（2026-09-14 关键字审计裁决后口径：控制流10 + 类型21 + 声明3 + 常量3 + 安全1 + OOP5 + 错误处理2 + 泛型1；160-a 邻项扩围批准：`公开`/`私有`/`静态`/`常量` 四词再上下文化，50 → 46）。
**字面量前缀**（`原始`/`多行`）与**上下文关键字**（`运算符`/`遍历`/`每个`/`属于`/`对`/`模块`/`作为`/`保护`/`虚拟`/`重写`/`抽象`/`公开`/`私有`/`静态`/`常量`）**不属于保留字**——不占用用户标识符命名空间（仅在固定语法位置按上下文识别）。
> **实施状态**：本清单为裁决后**目标态**；`实现` 移除、`原始`/`多行` 非保留化与 **10 词**上下文化（6 词 + 160-a 批准邻项 4 词）的实现落地见 `plans/024`（实施轮，`plans/021` §3-A A5）。
> 裁决前实现为 **60** 个（含 `自动`/`实现`/`原始`/`多行` 与 6 词）。

### 2.1 关键字分类表

**控制流(10个)：**
如果、否则、当、循环、返回、中断、继续、选择、情况、默认

**类型关键字(21个)：**
- 默认类型(2个)：整数（=整32别名）、小数（=浮64别名）
- 有符号整数(5个)：整8、整16、整32、整64、整128
- 无符号整数(5个)：正8、正16、正32、正64、正128
- 浮点(2个)：浮32、浮64
- 其他(7个)：布尔、字符、字符串、空类型、结构体、联合体、枚举

**声明(3个)：**
函数、变量、导入

**安全区边界(1个，plans/019 阶段4)：**
不安全

**常量(3个)：**
真、假、无

**OOP(5个)：**
类、接口、自身、父类、友元

**错误处理(2个)：**
结果、可选

**泛型(1个)：**
泛型

### 2.1a 上下文关键字（非保留字，不占用命名空间）

> 原则（§2.2）：凡只在固定语法位置出现的词，一律上下文识别、不入保留字表（`plans/024` §十）。
> 识别判据（parser 文本判定，162-a 落地）：

| 词 | 语法位置（唯一） |
|---|---|
| 原始、多行 | 字符串字面量前缀位（后紧跟引号才是前缀，否则=标识符） |
| 公开、私有、保护 | 可见性标签位（`公开:`/`私有:`/`保护:`——顶层与类体内） |
| 静态、常量 | 声明前缀位（顶层＋局部语句＋for-init＋类成员修饰） |
| 虚拟、重写、抽象 | 类/接口成员修饰位（后随 函数） |
| 作为 | 重命名导入位（`导入 路径 作为 别名`） |
| 模块 | 顶层声明位（`模块 名`——后随标识符即声明，分号可选） |
| 运算符、遍历、每个、属于、对 | 既有上下文词（§2.2 先例） |

### 2.2 移除的关键字
- ~~尝试~~、~~捕获~~、~~抛出~~、~~最终~~（异常机制，改用错误码+可选值）
- ~~命名空间~~（模块系统已覆盖其功能）
- ~~从~~（v2.0 模块系统删除，2026-09-07 plans/018——本清单此前漏改，2026-09-14 随审计同步）
- ~~实现~~（**死关键字**：parser/semantic 零使用——接口实现语法为 `类 名 : 接口名`；2026-09-14 用户裁决移除，plans/024）
- ~~自动~~（与 `变量` 同义冗余：两者解析实现完全相同；2026-09-14 用户裁决二合一，**保留 `变量`**，plans/024）

**变更记录：**
- 基类 -> 父类（更名，2026-08-12，更符合中文面向对象表达习惯）
- 新增 `友元`（友元声明）、`泛型`/`自动`（泛型与 lambda 类型推导）、`常量`（常量成员函数/常量表达式）——2026-08-13 语言增强（对标 C++ 表达能力）
- **2026-09-14 关键字审计裁决**（`plans/024`；用户明确裁决 + userselect 批准）：① **移除 `自动`**（与 `变量` 同义二合一，保留 `变量`——原 lambda/实例方法赋值的 `自动 名 = ...` 写法迁移为 `变量 名 = ...`）；② **移除死关键字 `实现`**（parser/semantic 零使用——接口实现语法为 `类 名 : 接口名`）；③ **`原始`/`多行` 非保留化**（字面量前缀改为上下文识别，不再占用命名空间；`原始"..."`/`多行"""..."""`/`原始多行"..."` 功能零变化）；④ **清单同步**：补 `模块`/`作为`/`不安全`（v2.0 / plans/019 漏改），删已移除的 `从`（v2.0 遗留漏改）。净效果：保留字 **60 → 56**；四处清单单一事实源自检见 `plans/024` §7.5。
- **2026-09-14 关键字审计裁决（阶段 3 补录·userselect 批准）**：`保护`/`虚拟`/`重写`/`抽象`（类成员修饰位）、`作为`（重命名导入位）、`模块`（顶层声明位）六词**上下文化**——不再为保留字，仅在固定语法位置按上下文识别（同 `运算符`/`遍历` 先例）；语法与语义零变化。目标态保留字 **56 → 50**。实施设计见 `plans/024` §十。
- **2026-09-14 邻项扩围批准（160-a·userselect）**：`公开`/`私有`（冒号标签位，与 `保护` 同机制——消除「同类不对称」）+ `静态`/`常量`（声明前缀位）**随 A5 同轮上下文化**——语法与语义零变化，仅词法层放宽（非保留位新可用作标识符）；`常量` 判据=前瞻 1 token（后随标识符/`:` → 声明；后随运算符 → 表达式）；循环 init 位 `常量` 语义自洽（更新位修改被语义层拦截）。目标态保留字 **50 → 46**。裁决依据与分析见 `plans/024` §10.5。
- **2026-09-14 关键字体系实施收官（162-a，家机 win-x64）**：上两条裁决**落地完成**——保留字
  **60 → 46**（删 `自动`/`实现`；`原始`/`多行` 与 10 词上下文化为 §2.1a 上下文关键字）；四处清单经
  `scripts/check_keywords_sync.py` 门禁化（实施轮补盘第五处=v1 自举组件表）；存量迁移 6 文件 22 处
  （`自动`→`变量`，含审计粗扫漏检面 17/18/71 用例）；负测（`自动 x = 1;` 拒）+ 正测（272 原语法形态 /
  273 标识符自由双编译对照）落地。
- **不引入** `别名`（类型别名）关键字与功能——类型等价替换能力由现有 `整数`/`小数` 内置别名覆盖，用户自定义别名暂缓
- **不引入** `运算符` 保留关键字——运算符重载采用上下文识别：函数定义位置出现 `函数 运算符+()` 时，"运算符"按**上下文关键字**处理（非保留字，不占用用户标识符命名空间），与 C++ 的 `operator` 保留字策略不同

---

## 三、类型系统

### 3.1 默认类型（别名，保留中文命名习惯）

| 类型关键字 | 等价于 | 说明 |
|-----------|--------|------|
| 整数 | 整32 | 默认整数类型 |
| 小数 | 浮64 | 默认浮点类型 |

### 3.2 位宽整数类型

| 类型 | 大小 | 范围 | 说明 |
|------|------|------|------|
| 整8 | 1字节 | -128 ~ 127 | 8位有符号整数 |
| 整16 | 2字节 | -32768 ~ 32767 | 16位有符号整数 |
| 整32 | 4字节 | -2^31 ~ 2^31-1 | 32位有符号整数 |
| 整64 | 8字节 | -2^63 ~ 2^63-1 | 64位有符号整数 |
| 整128 | 16字节 | -2^127 ~ 2^127-1 | 128位有符号整数 |
| 正8 | 1字节 | 0 ~ 255 | 8位无符号整数 |
| 正16 | 2字节 | 0 ~ 65535 | 16位无符号整数 |
| 正32 | 4字节 | 0 ~ 2^32-1 | 32位无符号整数 |
| 正64 | 8字节 | 0 ~ 2^64-1 | 64位无符号整数 |
| 正128 | 16字节 | 0 ~ 2^128-1 | 128位无符号整数 |

### 3.3 浮点类型

| 类型 | 大小 | 说明 |
|------|------|------|
| 浮32 | 4字节 | IEEE 754 单精度 |
| 浮64 | 8字节 | IEEE 754 双精度 |

### 3.4 其他基础类型

| 类型 | 大小 | 说明 |
|------|------|------|
| 布尔 | 1字节 | 真/假 |
| 字符 | 4字节 | Unicode标量值（支持中文字符） |
| 字符串 | - | 字符串类型（以\0结尾） |
| 空类型 | 0字节 | void，用于函数返回类型和空指针 |

### 3.5 复合类型

| 类型 | 语法 | 说明 |
|------|------|------|
| 指针 | 类型* | 指向类型的指针 |
| 数组 | 类型[长度] | 固定长度数组 |
| 结构体 | 结构体 名 { ... } | 自定义结构体 |
| 枚举 | 枚举 名 { ... } | 枚举类型 |
| 联合体 | 联合体 名 { ... } | 所有字段共享同一内存区域，同一时间只有一个字段有效 |
| 函数指针 | 返回类型(*名)(参数) | 函数指针 |

### 3.5a 联合体成员类型（164-a·plans/023 §十二 方案 A+D，2026-09-14 用户批准）

联合体（`联合体 名 { ... }`）成员类型限定为**可平凡复制类型**——标量（数值/布尔/字符/空类型）、
指针（含 `字符*` 借用视图）、数组（元素可平凡复制）、枚举、**递归判定的纯标量聚合**（结构体/
嵌套联合体其成员全可平凡复制）。

**拥有型类型**（`字符串`、容器、类对象、含拥有型的聚合）作成员 → **编译期硬错误**：
联合体「共享偏移 + 无 tag」语义下无法判定活跃成员，自动释放/写入归一化不可实现
（原实现「按偏移 0 无条件释放」= 泄漏/误释放 UAF 的根因，p13/p14 探针实证）。
**对标**：Rust `union` 成员必须 `Copy`。

**逃生舱「手动释放」**（对标 Rust `ManuallyDrop<T>`）：成员可显式标注——

```
联合体 数值 {
    手动释放 字符串 名;    // 编译器不生成自动释放；责任移交用户
    整64 数;
}
```

标注成员：语义层放行拥有型；**编译器不生成自动释放与写入归一化（清旧）**——用户在
`不安全 函数` 内以显式原语（`字符串释放` 等）自行释放；成员的新值写入/深拷仍正常。
字段访问仍受 `不安全` 区边界约束（联合体字段访问=不安全操作，§5.2 三语法点）。

### 3.6 错误处理类型（新增）

| 类型 | 语法 | 说明 |
|------|------|------|
| 结果 | 结果<正常类型, 错误类型> | 正常值或错误值 |
| 可选 | 可选<类型> | 有值或无值 |

### 3.6B 类型大小 内建（2026-08，A-3）

```
类型大小(类型)    // 编译期字节数常量（整64），C++ sizeof 等价物
```
> **内建表达式而非函数**：编译器直接求值，不产生调用、不可取地址、不可作函数指针；参数可为基础类型/结构体/数组/指针/限定类型/泛型参数（按实例化实参求值）。典型用途：容器按元素真实大小分配（`容量 * 类型大小(T)`）。

### 3.7 隐式类型转换

- 字符 ↔ 整数
- 字符* ↔ 字符串
- 字符串 ↔ 字符*
- 空类型* ↔ 任意指针
- 整数 -> 整64（宽化）
- 浮32 -> 浮64（宽化）
- **数值 -> 字符串（仅 `+` 拼接上下文）**：当 `+` 左操作数为 `字符串`/`字符*` 时，右操作数为整数/浮点/布尔/字符/枚举，自动隐式转换为字符串后再拼接（等价于 `整数转字符串/浮点转字符串/字符转字符串` 运行时转换）。布尔转换结果为 `"真"`/`"假"`。此转换**仅**在字符串拼接语境触发，不参与其他算术/比较上下文。
- **混合符号隐式转换禁止（2026-09-10 方案A，Rust 对齐；第五十一轮二元面+第五十六轮赋值/传参/返回面）**：有符号（整N）与无符号（正M）的**变量间**混合符号交互编译期拒绝，须显式 `类型名(表达式)` 构造转换。覆盖三族：①**二元运算**（比较 `== != < > <= >=`、算术 `+ - * / %`、位运算 `& | ^ << >>`）；②**赋值初始化/赋值**（`正64 b = 整32变量` / `整64 x = 正32变量` 拒绝）；③**函数传参/返回**（整N 实参传 正M 形参、正M 变量自 整N 函数返回拒绝）。依据：隐式宽化在混合符号下静默改变值语义（实证 `正64(2^63)` 与 `整64(-1)` 比较值域反转；`正32` 与 `整64` 两侧编译器行为分叉——探针 target/p50x64l/mixed_cmp 宿主 rc=9 / v2 rc=11 / 数学真值 13；`整32(-1)` 赋 `正64` 出 `2^64-1`）。**字面量豁免**：一侧为整数字面量（含一元负号字面量 `-1`）时豁免，字面量按另一侧类型参与运算/赋值/传参（同 Rust 字面量推断惯例，`大 > 100`、`正64 b = 5`、`读值(100)` 等惯用形态不受影响）。同符号混合宽度（正32 与 正64、整32 与 整64）维持既有宽化放行；字符 ↔ 整数互通不受本条影响。
- **显式类型转换的位模式语义（2026-09-10 第五十五轮 ④文档化，Rust `as` 对齐）**：`类型名(表达式)` 构造转换在整数族间的行为定义为位模式变换（零运行时开销，与 Rust `as` 逐条同款）：**升宽**——有符号源符号扩展（`整64(-1)` 即位模式全 1；`正64(整32(-1))` = `2^64-1`，Rust `i32 as u64` 同款）；无符号源零扩展（`整64(正32(4000000000))` = `4000000000`）。**同宽跨符号**=位模式重解释（`整64(正64(2^63))` = `-2^63`，Rust `u64 as i64` 同款）。**降宽**=低位截断（`整32(正64大值)` 取低 32 位）。窄宽（整8/整16/正8/正16）参与转换先按其位宽扩展到目标宽度再按上述规则。双侧实现锚定：宿主 emitCast（`movsx`/`movzx` 按源分派）与 v2 类型转换发射族（方案甲源类型分派）第五十一轮探针逐字节一致（4000000000 / -9223372036854775808）。

---

### 3.8 编译期内建常量（2026-09-16 新增·239-a 用户批准）

**调试模式**（布尔常量）：编译期已知的构建模式旗标——

- 默认（调试构建）值为 `真`；命令行 `--发布`（ASCII 别名 `--release`）时值为 `假`
- 用法：普通 `如果 (调试模式) { ... } 否则 { ... }`——**两个分支都经过完整语义检查**
  （与 §4.7 条件编译的本质区别：错误代码在两种构建配置下都会被拒绝，不存在裁剪盲区）
- 未选中分支由编译器常量条件直取消除（生成代码中不存在死分支指令与字符串常量，
  与条件编译产物等价的零运行时开销）——实测锚定：调试/发布双配置 asm 互斥包含分支常量
- 本常量为编译器内建：用户同名顶层常量定义优先（不覆盖）

## 四、词法结构

### 4.1 源文件
- 扩展名：.cn
- 编码：UTF-8（必须）
- 入口文件：主.cn
- **说明：入口文件命名为 `主.cn` 仅为约定，其内部须定义 `主` 函数作为程序入口。文件名与函数名无强制绑定关系。**

### 4.2 注释
- `//` 单行注释
- `/* */` 块注释（支持嵌套）

### 4.3 字面量

**整数字面量：**
- 十进制：`42`
- 十六进制：`0xFF`
- 二进制：`0b1010`
- 八进制：`0o777`
- 类型后缀与中文位宽类型的映射：
  - `f` -> 浮点（浮32）
  - `L` -> 整64
  - `LL` -> 整128
  - `U` -> 正32
  - `UL` -> 正64
  - `ULL` -> 正128
- 不支持数字下划线分隔

**浮点字面量：**
- 小数形式：`3.14`
- 科学计数法：`1.5e10`

**字符字面量：**
- 普通字符：`'A'`、`'中'`
- 转义字符：`'\n'`、`'\t'`、`'\0'`
- Unicode转义（新增）：`'\u{4E2D}'`（等同于'中'）

**字符串字面量：**
- 普通字符串：`"你好"`
- 原始字符串（新增）：`原始"^\d+\.\d+$"`（反斜杠不需转义）
- 多行字符串（新增）：`多行"""第一行\n第二行\n第三行"""`
- 转义语义规则：多行字符串内部仍处理 `\n`、`\t` 等转义字符。如需完全原始的多行文本，可组合使用 `原始` 和 `多行` 前缀：`原始多行"""..."""`

### 4.4 运算符

**算术运算符：** `+` `-` `*` `/` `%`
**比较运算符：** `==` `!=` `<` `>` `<=` `>=`
**逻辑运算符：** `&&` `||` `!`
**位运算符：** `&` `|` `^` `~` `<<` `>>`
**赋值运算符：** `=` `+=` `-=` `*=` `/=` `%=` `&=` `|=` `^=` `<<=` `>>=`
**指针运算符：** `&`（取地址）`*`（解引用）
**其他：** `.`（成员访问）`[]`（下标）
**条件运算符（三元）：** `条件 ? 真值 : 假值`（右结合，惰性求值，仅对选中分支求值）

> **成员访问统一为 `.`（v2.1，2026-09-03 用户裁决，对齐 Rust/Go 与旧版 Rust 风格 CN）**：
> 废除 `->` 成员访问形式（编译硬错误并附迁移提示「请改用 .」）；`.` 左侧为指针时
> **自动解引用一级**（`点* q` 可写 `q.x`，等价 `(*q).x`；Go 先例）。多级指针（`点**`）
> 不继续解引用，报错；类/结构体/联合体/接口指针行为一致统一。注意：`->` 记号本身
> 仍保留用于**函数返回类型**（`-> 整32`）与 lambda 返回标注，与成员访问无关。
> 迁移前写法 `指针->字段` 已于 2026-09-03 全量下线（含 v2 自举源码与全部测试）。

> **成员包含判断使用标准库函数 `包含()` 而非运算符**（如 `表.包含(键)`），"在"不是运算符。

> **位运算语义（2026-09-09 用户裁决，对齐 Rust 定义化纪律）**：位运算符仅适用**整型**（浮点不允许位运算）。右移 `>>`：有符号=算术右移（符号扩展）、无符号=逻辑右移（补零）。**移位量溢出为定义行为**：移位量先按操作数类型位宽取模再移位（`a << k` ≡ `a << (k mod 位宽)`；Rust release 掩码语义同构——Rust debug 构建陷落需构建模式概念，CN 单一模式取定义掩码分支；编译期常量折叠与三后端发射逐点一致）。`~x` = 按位非（位模式 ≡ `x ^ -1`）。复合赋值 `&= |= ^= <<= >>=` 逐条同上。

> **运算符重载**：类/结构体可定义成员 `函数 运算符X(参数) -> 返回类型` 自定义运算符语义（`X` ∈ `+ - * / % == != < > <= >= && || ! & | ^ ~ << >> [] ()`）。重载决议规则：① 优先精确匹配内置运算符；② 无内置匹配时按左操作数类型查找成员 `运算符X`；③ 仍无匹配则报"类型不兼容"错误。字符串 `+` 拼接由编译器隐式调用 `整数转字符串/浮点转字符串/字符转字符串` 转换 + `字符串连接` 实现，无需用户重载。（v2.1：可重载集合删除 `->`——该运算符已随成员访问统一废除）

### 4.5 运算符优先级（13级，从高到低）

| 优先级 | 运算符 | 说明 |
|--------|--------|------|
| 13（最高） | `()` `[]` `.` `?`（后缀错误传播，2026-08 C-1；v2.1 删 `->`） | 后缀 |
| 12 | `!` `~` `-`（负）`*`（解引用）`&`（取地址） | 一元 |
| 11 | `*` `/` `%` | 乘除 |
| 10 | `+` `-` | 加减 |
| 9 | `<<` `>>` | 移位 |
| 8 | `<` `<=` `>` `>=` | 比较 |
| 7 | `==` `!=` | 相等 |
| 6 | `&` | 按位与 |
| 5 | `^` | 按位异或 |
| 4 | `|` | 按位或 |
| 3 | `&&` | 逻辑与 |
| 2 | `||` | 逻辑或 |
| 1.5 | `条件 ? 真值 : 假值` | 三元条件（右结合） |
| 1（最低） | `=` `+=` `-=` 等 | 赋值 |

### 4.6 语句终止与续行

> **核心事实（2026-09-04 方案B 反转，plans/015 用户裁决）：CN 语言的语句必须以分号 `;` 终结（Rust/C++ 同款「表达式语句必须 `;`」模型）。换行（`\n`/`\r`）在词法层仍与空格、Tab 一样被当作空白直接跳过，不产生换行 token——换行在表达式**内部**仍无语法意义（续行不受影响），但**不再是语句边界**；语句边界唯一由强制分号标记，行尾与下行行首词法可拼导致的静默粘连事故链（本项目实测多起）就此结构性根除。**

#### 4.6.1 三层机制

| 层 | 行为 | 判定位置 |
|----|------|---------|
| 词法层 | 换行 `\n`/`\r` 与空格、Tab 完全当作空白跳过，不产生换行 token（表达式内部续行天然支持） | 词法分析器 |
| 语法层 | 分号 `;` 是**强制**终止符：语句解析完成后必须消费 `;`——缺失即报错「语句缺少分号 ';'」（错误即诊断，杜绝静默粘连），诊断后继续解析保证一次编译报全量位置 | 语法分析器 `consumeSemicolon()` |
| 边界识别 | 语句边界=分号；`}` 自终结（块/如果/当/循环/函数体/结构体/类/接口/枚举体后**不加**分号） | 语法分析器 `parseStmt()` |

#### 4.6.2 EBNF

```
语句 = 表达式语句 | 声明语句 | 返回语句 | 中断语句 | 继续语句
      ";"                              // 分号强制（语句终结符）
控制流语句 = 如果语句 | 当语句 | 循环语句 | 遍历语句 | 选择语句      // 块体 } 自终结，不加分号
```

#### 规则1：语句终止（强制分号）

- 语句**必须**以分号 `;` 终结：变量声明、赋值、自增自减、复合赋值、表达式语句、返回、中断、继续、顶层常量与静态声明、导入语句 均须 `;`
- `}` 自终结：块/如果/当/循环/选择体、函数体、结构体/类/接口/枚举体后**不加**分号（`}` 即边界）
- 行内块 `{ x = 1; }` 内部语句同样须 `;`
- 缺失分号是**编译错误**（含行列定位的诊断），不再依赖关键字边界推断

#### 规则2：续行（换行≡空格，表达式内部天然续行）

- 换行无语法意义，**表达式内部任何位置换行都天然续行**，不存在"括号未闭合才可续行"的限制
- 括号 `()`/`[]`/`{}` 未闭合时跨行是**结构上安全**的（函数调用参数、下标、表达式分组天然粘合）
- 语句已由分号终结，跨行表达式只需保证行首 token 不与上行行尾粘连出歧义（建议二元运算符留在**行尾**：`a +` 换行 `b;`）

> ⚠️ **注意：行首运算符仍会被并入上一行**（表达式内部换行≡空白）：`变量 a = 1 +` 换行 `2;` 与 `变量 a = 1 + 2;` 等价。若在**语句终结后**行首出现运算符（如上行缺分号），解析器会在语句终结处报「语句缺少分号」而非静默粘连。

#### 规则3：单行多语句（分号分隔）

- 单行多语句由 `;` 分隔：`变量 i = 0; ++i; --i;` 合法
- 每条语句独立终结，行内最后一条语句同样须 `;`

#### 规则4：空语句与空循环体（裸分号非法）

- 裸 `;` 作为空语句**不合法**（报"预期表达式"），CN 语言没有空语句
- `当(x);` 是**语法错误**（循环体强制要求 `{`），合法空循环体写 `当(x) { }`（空块合法）
- 循环头 `循环(整32 j = 0; j < 10; j++)` 中的分号是**三段分隔符**（与语句终结符语义无关，保持不变）

#### 规则5：模块级声明的分号（2026-09-04 plans/015 裁决）

- 导入语句：`导入 路径;`（对齐 Rust `use ...;`）
- 顶层常量/静态声明：`常量 名 = 值;`（类型推导，实装口径 E2E 96） / `静态 类型 名 = 值;`（对齐 C++ `const`/`static` 声明；238-a 措辞修正——原文「常量 T = 2」易误读为显式类型标注）
- 枚举声明、结构体/类/接口体：`}` 自终结，**不加分号**

#### 示例代码

```
变量 a = 1;        // 分号强制（缺失报错）
变量 b = 2; 整32 c = 3;   // 单行多语句：分号分隔

变量 i = 0; ++i; --i;    // 单行多语句

// 跨行书写长表达式：二元运算符留在行尾（表达式内部换行≡空白，续行不受影响）
变量 总和 = a +
           b +
           c;

当(x) { }        // 合法：空循环体（空块）
// 当(x);        // ❌ 语法错误：循环体强制要求 {
// ;             // ❌ 错误：空语句非法（预期表达式）
// 变量 d = 4    // ❌ 错误：语句缺少分号 ';'

循环(整32 j = 0; j < 10; j++) {   // 循环头分号是三段分隔符（与语句终结无关）
    打印行(j);
}
```

> **注意**：循环头 `循环(初始化; 条件; 更新)` 中的两个分号是语法必需的分隔符，与语句终止符无关；缺失任何一段都会报错。

---

### 4.7 词法预处理（条件编译）（2026-09-16 新增·B2 方案甲用户批准）

> 章节来源：237-a 支柱一规范覆盖矩阵发现「实装有、规范无」（E2E 24 用例实测工作而本规范无章节）——
> 2026-09-16 用户裁决方案甲：纳入规范。全部语法形态以 E2E 24（`tests/e2e/24_preprocessor/`）实测为准。

**指令清单：**

| 指令 | 形态 | 说明 |
|---|---|---|
| `#定义 宏名` | 行首起 | 定义宏（**无值旗标**，仅存在性判定） |
| `#如果定义(宏名)` 或 `#如果定义 宏名` | 行首起 | 条件开始：**括号形式与空白形式等价** |
| `#否则` | 行首起 | 条件分支 |
| `#结束如果` | 行首起 | 条件结束（与 #如果定义 严格配对；纯空分支合法） |
| 命令行 `-D 宏名` | `cn build 源.cn -D 宏名` | 编译期注入宏（等价源内 #定义） |

**语义（词法层裁剪模型）：**

- 条件编译发生在**词法层**：被裁剪分支不产生任何 token——**即使内含语法错误也不诊断**
  （实测锚定：24 用例第 2 节「非法语法在未定义分支被裁剪不报错」）
- 宏为无值旗标：**不支持**带参宏、宏体展开、`#包含`、`#取消定义`、常量表达式条件（与 C 预处理器的能力边界——CN 用模块系统与常量表达式承担这些职责）
- 支持**嵌套**条件编译（实测锚定：24 用例第 3 节两层嵌套）

**示例（实测自 E2E 24）：**

```
#定义 发布模式

函数 主() -> 整32 {
    #如果定义 发布模式
    打印("发布模式: 开启");
    #否则
    打印("发布模式: 关闭");
    #结束如果
    返回 0;
}
```

**特性（声明制，2026-09-16 239-a 增补）：**

宏名的受控来源之一——货舱.toml 新增 `[特性]` 节：

```toml
[特性]
启用 = ["网络", "压缩"]
```

- 每个启用特性即一个条件编译旗标：`#如果定义(网络)` 命中；未启用特性（如 `图形`）被裁剪
- 与命令行 `-D` 同通道（特性并入注入宏集合）；声明制=宏名有单一事实来源（货舱清单），
  对齐 Rust Cargo features 声明式管理（未声明宏名的收紧登记制另行排班）

**实装状态（诚实边界）：** 宿主编译器 ✓（`src/cn_compiler/lexer/preprocessor.cpp`，E2E 24 实测）；
**v2 自举树未实现**（v2 词法无预处理面——缺口已登记 plans/021 §3-D 待 v2 侧对齐）。

---

## 五、语法规范

### 5.1 程序结构

```
程序 = 顶层声明*
顶层声明 = 函数声明 | 结构体声明 | 枚举声明 | 类声明 | 接口声明 | 导入声明 | 全局变量声明
```

### 5.2 变量声明（类型前置）

```
变量 x = 10           // 类型推断
整数 y = 20           // 显式类型
整32 z = 30           // 位宽类型
常量 PI = 3.14        // 常量
静态 整32 计数 = 0    // 静态变量
```

### 5.3 函数定义

```
函数 加(整32 a, 整32 b) -> 整32 {
    返回 a + b
}

// 无返回值时省略 -> 类型
函数 打印问候() {
    打印行("你好")
}

// 函数原型声明（前向引用）
函数 计算(整32 n) -> 整32
```

#### 函数重载

```
函数 加(整32 a, 整32 b) -> 整32 { 返回 a + b }
函数 加(浮64 a, 浮64 b) -> 浮64 { 返回 a + b }
函数 加(字符串 a, 字符串 b) -> 字符串 { 返回 a + b }
```
> 同名函数按**参数类型与个数**区分（重载决议）；仅返回类型不同不构成重载。名称修饰（mangling）按签名编码（规格书附录C）。

#### 默认参数

```
函数 问候(字符串 名字 = "世界") -> 空类型 {
    打印行("你好，" + 名字);
}
问候();          // 你好，世界
问候("张三");    // 你好，张三
```
> 默认参数从右向左连续声明；调用时按位置补缺省实参；默认参数在原型声明中给出（避免重复）。

#### lambda 表达式（匿名函数）

```
变量 加倍 = [](整32 x) -> 整32 { 返回 x * 2; };
整32 结果 = 加倍(21);   // 42
```
> lambda 支持 `[捕获]`（`[]` 不捕获 / `[=]` 值捕获 / `[&]` 引用捕获 / `[变量]` 显式捕获）；无返回类型标注时由编译器推导（单一 `返回` 表达式或空类型）。

#### 强制类型转换

```
浮64 比例 = 浮64(3) / 4;        // 显式转浮点
整32 截断 = 整32(3.99);         // 浮点转整（截断）
指针 原始 = 指针(地址);          // 整数转指针
整64 地址 = 整64(指针);          // 指针转整数
```
> 提供 C 风格构造转换 `类型名(表达式)`，语义同 static_cast/位重解释组合；宽化/窄化/浮整转换/指针↔整数 均显式触发，避免隐式危险转换。

#### 引用参数（2026-08，A-1）

```
函数 交换(整32& a, 整32& b) -> 空类型 {
    整32 临时 = a
    a = b
    b = 临时
}

泛型 <类型 T>
函数 引用交换(T& a, T& b) -> 空类型 {
    T 临时 = a
    a = b
    b = 临时
}
```
> `类型& 参数名` 为引用参数（C++ 引用语义）：实参传左值地址，读取自动解引用、赋值经指针写回调用方；引用可用于**函数参数**（含泛型 `T&`）、**引用变量**（`整32& r = x`，P3-18 e2e 94）与**函数返回类型 `T&`**（返回被引用左值地址，e2e 102；`返回 局部变量地址` 编译报错，e2e 103）；引用默认值编译报错；实参须为左值；支持友元引用参数访问私有成员。规格详见 cn-language-spec 04-一F/一F2。

##### 常量 只读引用参数（2026-09-10，plans/019 阶段3 · 编译期安全）

```
函数 求和(常量 向量<整64>& 数据) -> 整64 { ... }   // 只读借用（Rust &T 对标）
```
> `常量 类型& 名` 为**只读借用**参数（常量 修饰引用参数——常量成员函数先例同关键字）：①体内对该参数（含经成员访问链）赋值/复合赋值=编译错误；②传给可变引用形参（`T&`）=编译错误（只读借用不能借出可变）；③同一调用中，某变量的可变引用实参与常量引用实参解析到同一基础变量=编译错误（借用互斥第一版：语句级；跨语句活跃区间分析随阶段3b）。普通函数调用面已接入（构造/方法/函数指针调用面随阶段3b）。

#### 显式转移 转移()（2026-09-10，plans/019 阶段1 · 编译期安全）

```
变量 v = 向量<整64>();
变量 u = 转移(v);        // 所有权转移：u 接管 v 的值，v 进入「已转移」状态
打印行(v.大小());        // ✗ 编译错误：'v' 已转移，不能继续使用

变量 p = 分配内存(8);    // 指针交接（零拷贝，槽位值）
变量 q = 转移(p);        // q 持有，p 禁用——释放归属唯一化
```
> `转移(变量)` 为**内置函数**（零新关键字；内置构造器 正常()/错误()/某些() 同位；C++ `std::move` 同构）。规则：①实参须为**变量标识符**（成员/下标/解引用形态编译报错「转移目标须为变量」）；②实参类型须为**资源语义类型**（指针/字符串/容器/类对象/结构体/结果/可选），**标量（整数/浮点/布尔/字符）拒绝**——复制语义无转移意义（对齐 Rust Copy 类型惯例，诊断「'X' 具有复制语义，无需转移」）；③求值结果=实参的值（交接），**源变量自该点起任何使用均为编译错误**（读值/赋值目标/复合赋值/自增自减/取地址/解引用/成员与下标对象侧/调用实参/返回/再次转移，对标 Rust E0382），诊断含转移点定位（「'v' 已转移，不能继续使用（转移发生在 行N）」）；④已转移状态不因作用域退出恢复；内层同名**新声明**是新变量不受影响（遮蔽语义）；⑤第一版运行行为零变更（语义层 AST 改写，指针即值天然零拷贝；容器/类仍按拷贝语义，浅转移零拷贝优化随 plans/019 阶段3）；安全保证=编译期拦截「转移后使用」（UAF 主形态）与「转移后再释放」（双释放主形态）。规格详见 cn-language-spec 04-五。

#### 不安全 函数 修饰（2026-09-10 立；2026-09-14 清单完整化 plans/023 §九；**2026-09-17 收口——编译硬错误生效**）

```
不安全 函数 裸改(整64* p) -> 整64 {
    整64* q = p + 1;      // 指针算术：不安全函数内合法
    q[0] = 7;             // 指针下标写：同上
    返回 q[0];
}
函数 主() -> 整32 {
    整64 v = 裸改(基);     // 安全函数调不安全函数=允许（责任上移）
    返回 0;                // 联合体访问/外部函数调用/裸释放 在安全函数内
}                          // = 观察期警告（分批收口后变错误）
```
> `不安全 函数` 修饰（不安全=真关键字，plans/019 阶段4 用户批准的三语法点之三；Rust `unsafe fn` 同模型）：
> 以下操作**仅限 `不安全 函数` 体内**——安全函数体内=**编译错误**（2026-09-17 收口完成：观察期结束，
> 原「警告不报错」语义废除；文案 `[安全区边界] <kind>应在 不安全 函数 内（<detail>）`）：
>
> | # | 操作 | 形态 |
> |---|---|---|
> | 1 | 指针算术 | 裸指针 `p ± 整数`（含数组退化） |
> | 2 | 指针下标写 | 裸指针 `p[i] = x` |
> | 3 | 裸指针解引用读 | `*p`（读） |
> | 4 | 裸指针解引用写 | `*p = x` |
> | 5 | 指针下标读 | 裸指针 `p[i]`（读） |
> | 6 | 指针成员访问 | 裸指针 `p.字段`（隐含解引用） |
> | 7 | 联合体字段访问 | `u.字段`（含结构体内联合体嵌套） |
> | 8 | 可变静态变量写 | `静态` 变量赋值/复合赋值/自增自减 |
> | 9 | 外部函数调用 | `外部 函数`（FFI/C 边界） |
> | 10 | 裸释放 | `释放(裸指针)` |
>
> **豁免与例外**：①`字符串`/`字符*` 为字符串视图（非裸指针）——上表 1/3/5/6 对其**不适用**
> （字符串拼接、视图读、下标读均为安全操作），**解引用写（`*s = c`）亦豁免**（2026-09-14 B7-a 子裁决＝豁免，
> `isStringSemanticType` 六类统一豁免，以实装为准）；②常量空指针使用（`无` 解引用/成员访问）=
> **编译期硬错误**（不属清单，独立于不安全区）；③调用 `不安全 函数` 允许（责任上移；审计面=不安全函数清单）；
> ④指针比较、指针类型转换、取地址 = **安全**（Rust 对照）。
>
> **判例边界（负例集，均逐块实测 2026-09-14）**：
> ```cn
> 不安全 函数 试() -> 整64 {
>     整64 v = *无;       // 错误：编译期常量空指针解引用（B11——不因不安全区豁免）
>     返回 v;
> }
> 函数 读(整64* p) -> 整64 {
>     返回 *p;            // 违规（B6）：裸指针解引用读（安全函数→编译错误）
> }
> ```
> **B11 判据（终态，2026-09-15 第一百八十八轮 188-a 落地）**：① **`无` 字面量**（`*无`）；
> ② **变量常量传播形态**——变量经 `无` 字面量初始化/赋值且**全函数内无其他写入**（失格面=非空写入/复合赋值/
> 自增自减/取地址/引用参数实参；含 `p = q` 传播闭包不动点）时，其**解引用/成员访问/下标访问**在编译期判为
> **硬错误**（宿主 Error / v2 表诊断，双侧同文案：`编译期常量空指针<解引用|成员访问|下标访问>（确定性错误；
> plans/023 B11）`）。判定为**流不敏感**保守分析（全函数视角）——零假阳性优先，保守漏报（流敏感未做、
> 跨函数/静态全局、多级别名写入）为登记边界；Rust 对照=`deref_nullptr` lint（deny-by-default）。
> **字面量形态三面对齐**：`无.x` / `无[0]` 同报 B11 专文诊断（原为间接类型错误）。

#### 外部 函数 声明（FFI，2026-08，C-3）

```
外部 函数 strlen(字符串 s) -> 整64
外部 函数 toupper(整32 c) -> 整32
外部 函数 GetTickCount64() -> 正64
```
> FFI 最小集：声明 C 链接外部函数（无函数体），链接符号 = 函数名本身（无重载 mangling/模块前缀）；声明带函数体报错；同名同签名重复声明允许（C 语义）；附加链接库用 `--链接 <库>`（别名 `--link-lib`）。规格详见 cn-language-spec 04-一G。

### 5.4 错误处理（替代异常）

```
// 结果类型：正常返回值 或 错误码
函数 除法(整32 a, 整32 b) -> 结果<整32, 整32> {
    如果 b == 0 {
        返回 错误(1)  // 错误码1: 除零错误
    }
    返回 正常(a / b)
}

// 使用时检查
除法结果 = 除法(10, 0)
如果 除法结果.正常 {
    打印行(除法结果.值)
} 否则 {
    打印行("除零错误: ", 除法结果.错误)
}

// 可选类型
函数 查找(整32 键) -> 可选<整32> {
    如果 表.包含(键) {
        返回 某些(表[键])
    }
    返回 无
}
```

> **注意：所有关键字均为保留字，不可用作标识符。**

**内置构造器身份说明：** `正常()`、`错误()`、`某些()` 为内置构造器函数，由编译器预置符号表，非保留关键字，用户不可重定义。

#### 结果/可选强制检查规则
- 规则1：`结果<T,E>` 类型的返回值如果不被检查就丢弃，编译器报**错误**
- 规则2：检查 `结果.正常` 后未处理错误分支，编译器报**警告**
- 规则3：`可选<T>` 类型的值在访问 `.值` 前必须检查 `.有值`，否则编译器报**错误**

#### 错误传播运算符 表达式?（2026-08，C-1）

```
函数 安全除法(整32 a, 整32 b) -> 结果<整32, 整32> {
    如果 b == 0 { 返回 错误(1) }
    返回 正常(a / b)
}

// 传播：正常取 .值，失败沿调用链上抛（Rust ? 同语义）
函数 除法传播(整32 a, 整32 b) -> 结果<整32, 整32> {
    整32 商 = 安全除法(a, b)?
    返回 正常(商)
}
```
> 后缀 `?` 错误传播（优先级13，与三元 `?` 按后随 token 区分）：操作数须为 `结果<T,E>`/`可选<T>`，正常/有值 → 取 .值；失败 → 构造错误结果并从当前函数返回。当前函数返回类型须兼容（结果值类型 T 精确一致、错误类型 E 可转换；可选 T 一致）。规格详见 cn-language-spec 07-一B。

### 5.5 控制流

```
// 条件
如果 x > 0 {
    打印行("正数")
} 否则 如果 x < 0 {
    打印行("负数")
} 否则 {
    打印行("零")
}

// 当循环
整数 i = 0
当 i < 10 {
    打印行(i)
    i++
}

// 循环（for风格）
循环 (整32 j = 0; j < 10; j++) {
    打印行(j)
}

// 选择语句
选择(值) {
    情况 1:
        打印行("一")
        中断
    情况 2:
        打印行("二")
        中断
    默认:
        打印行("其他")
}

// 遍历...中每个 迭代（2026-08，C-2，对标 C++ range-for）
整32[5] 数据 = { 10, 20, 30, 40, 50 }
遍历 数据 中 每个 x {
    打印行(x)          // 10 20 30 40 50
}
向量<整32> 数表 = 向量<整32>()
遍历 数表 中 每个 v { 打印行(v) }   // 类容器：大小() + 元素(整64)

// 选择 增强（2026-08，C-4）：字符串 / 多值 / 裸枚举成员
字符串 命令 = "开始"
选择 (命令) {
    情况 "开始", "继续":     // 字符串 + 多值分组
        打印行("执行")
        中断
    默认:
        打印行("未知")
}
枚举 颜色 { 红, 绿, 蓝 }
颜色 c = 颜色.蓝
选择 (c) {
    情况 红:                  // 裸枚举成员（按条件枚举类型解析）
        打印行("红色")
        中断
    情况 蓝:
        打印行("蓝色")
        中断
}

// 中断和继续
循环 (整32 k = 0; k < 100; k++) {
    如果 k % 2 == 0 {
        继续
    }
    如果 k > 50 {
        中断
    }
    打印行(k)
}
```

> **选择语句 fall-through 与 C 语言一致**（用户裁决 2026-08）：分支体不写 中断/返回 自动贯穿下一分支；多值分组 `情况 0, 1:` 前序标签体为空即利用贯穿。`对`/`属于` 为上下文关键字（不占保留字）。规格详见 cn-language-spec 03-3.3B/3.4。

> **`中断` 绑定最近进入的选择或循环**（用户裁决 2026-09-11 方案B·C 语义）：`中断` 跳出**最近进入**的一层 选择 或 循环（谁近绑谁，与 C 的 `break` 一致）——循环内选择体写 `中断` 只跳出该选择、循环继续下一轮；从选择内跳出外层循环无直接语法（与 C 相同，须经由条件变量或重构）。`继续` 仅对循环有意义（选择体内的 `继续` 绑定最近循环，与 C 一致）。存量代码该形态扫描=0 处（2026-09-11 实测 tests/stdlib/v1 树/v2 树全量零命中）。规格详见 cn-language-spec 03-3.5。

### 5.6 OOP语法

```
// 类定义与继承
类 动物 {
公开:
    字符串 名称
    虚拟 函数 叫声() -> 字符串 {
        返回 "..."
    }
保护:
    整32 年龄
私有:
    整32 内部ID
}

类 狗 : 动物 {
公开:
    重写 函数 叫声() -> 字符串 {
        返回 "汪汪"
    }
    
    函数 狗(字符串 名) {
        名称 = 名
    }
    
    函数 ~狗() {
        // 析构函数
    }
}

// 接口定义与实现
接口 可绘制 {
    虚拟 函数 绘制() -> 空类型
}

类 圆形 : 可绘制 {
公开:
    浮64 半径
    重写 函数 绘制() -> 空类型 {
        // 实现绘制
    }
}

// 自身和父类访问
类 子类 : 父类 {
公开:
    重写 函数 方法() -> 空类型 {
        自身.字段 = 10      // 访问自身成员
        父类.方法()         // 调用父类方法
    }
}
```

#### 运算符重载（成员）

```
类 复数 {
公开:
    浮64 实部
    浮64 虚部

    函数 运算符+(复数 右) -> 复数 {
        返回 复数{实部 + 右.实部, 虚部 + 右.虚部}
    }

    函数 运算符==(复数 右) -> 布尔 {
        返回 实部 == 右.实部 且 虚部 == 右.虚部
    }
}

复数 a = 复数{1.0, 2.0}
复数 b = 复数{3.0, 4.0}
复数 c = a + b        // 调用 复数.运算符+
布尔 相同 = a == b    // 调用 复数.运算符==
```
> 重载运算符仅限成员函数，左操作数必须是本类实例；单目运算符（`-`/`!`/`~`）无参数，双目运算符单参数（右操作数）。

#### 静态成员

```
类 计数器 {
公开:
    静态 整32 总数 = 0        // 类静态字段（全类共享）

    函数 计数器() {
        总数++;               // 类内直接访问
    }

    静态 函数 获取总数() -> 整32 {
        返回 总数;
    }
}

计数器.总数 = 10;              // 类外通过 类名.静态成员 访问
整32 当前 = 计数器.获取总数();
```

#### 常量成员函数

```
类 只读包装 {
私有:
    整32 值
公开:
    函数 只读包装(整32 v) { 值 = v; }

    常量 函数 获取() -> 整32 {   // 常量成员：不修改任何成员
        返回 值;
    }
}
```
> `常量` 修饰的成员函数承诺不修改成员；常量对象仅能调用常量成员函数；违反时编译器报错。

#### 友元

```
类 账户 {
私有:
    整64 余额
公开:
    友元 函数 审计(账户& 账)   // 声明友元函数，可访问私有成员
}

函数 审计(账户& 账) -> 整64 {
    返回 账.余额;                // 友元可访问私有
}
```
> `友元` 授予外部函数或类访问本类私有/保护成员的权限（最小授权原则，谨慎使用）。

#### 泛型 / 模板

```
泛型 <类型 T>
类 向量 {
私有:
    T* 数据
    整64 长度
公开:
    函数 向量(整64 容量) { 数据 = 分配(T 大小 * 容量); 长度 = 0; }
    函数 推入(T 值) { 数据[长度] = 值; 长度++; }
    函数 获取(整64 索引) -> T { 返回 数据[索引]; }
}

向量<整32> 整数列表 = 向量<整32>(10);
整数列表.推入(42);

泛型 <类型 T>
函数 交换(T& a, T& b) -> 空类型 {
    T 临时 = a;
    a = b;
    b = 临时;
}
```
> 泛型采用**编译期单态化**（每实例化生成一份具体类型代码）；`泛型 <类型 T>` 声明类型参数，使用 `类型名<实参>` 实例化；支持泛型类与泛型函数；类型参数可约束为接口（`泛型 <类型 T : 接口>`）。

### 5.7 结构体与枚举

```
// 结构体
结构体 点 {
    浮64 x
    浮64 y
}

// 枚举
枚举 颜色 {
    红,
    绿,
    蓝,
    自定义 = 100
}

// 枚举支持负数
枚举 方向 {
    上 = -1,
    中 = 0,
    下 = 1
}
```

### 5.8 函数指针

```
// 声明
整32(*回调)(整32, 整32)

// 赋值
回调 = 加

// 调用
整32 结果 = 回调(10, 20)
```

> **说明：`整32(*回调)(整32, 整32)` 采用C风格声明语法，这是为了与C/C++函数指针语法保持一致，便于底层编程人员理解。**

> **方法作值（P3-23，2026-08）**：静态方法 `类名.静态方法` 可直接作函数指针值（e2e 98）；实例方法 `对象.实例方法` 作值为**绑定 this 的闭包**——经闭包变量调用（`自动 cb = 对象.方法; cb(实参)`，捕获对象地址、引用语义读最新状态，e2e 104），不可直接作裸 fnptr 实参（裸地址无法携带 this，编译报错，e2e 107 负向）。规格详见 cn-language-spec 04-四 方法作值。

### 5.9 模块系统（v2.0）

> **v2.0 变更**（依据《模块系统 v2.0 全面改造方案》12 项决策）：路径分隔 `.` → `::`；删除 `从`；新增 `模块`/`作为`/`包`/`货舱` 关键字；可见性**默认私有**（模块级 + 类内）；新增包=crate 模型、包.cn 根、核心包 prelude、货舱.toml、可见性交集规则。**〔2026-09-07 plans/018 用户裁决摘除 `包`/`货舱` 死保留字（无语法消费点，保留字最小化），关键字 61→59；`模块`/`作为` 有真实语法保留〕**（分号模型 2026-09-04 plans/015 方案B 反转：语句强制 `;` 终结，导入 `导入 路径;` 加分号——见 §4.6/§5.9.6。）

#### 5.9.1 模块声明与路径

```
模块声明 = "模块" 标识符
路径 = 标识符 ("::" 标识符)*
// 示例：网络/传输控制.cn → 模块 网络::传输控制
```

- **定位收紧（2026-09-07 plans/018 裁决，Rust `mod` 对齐）**：`模块` 声明**仅限两种树结构位置**——
  ① **包根成员聚合**（`包.cn` 内 `模块 网络;`，声明包的组成，不引入名字）；
  ② **父模块挂子模块**（`网络.cn` 内 `模块 传输控制;`，建立 `网络::传输控制` 完整路径）。
  **普通源文件使用 `模块` 声明 = 编译诊断**（普通文件即模块，其模块路径由文件名+目录层级唯一决定，无需声明）。
- **自动加载强语义（2026-09-07 宿主实证，plans/018 第 0 步）**：被 `模块` 声明引用的模块文件**恒被加载编译**（Rust `mod foo;` 必编译 `foo.rs` 同款）——哪怕无人 `导入` 它，其内错误也在库自身编译期暴露（fail fast，库完整性保证）；触发源即 `模块` 声明本身，非目录扫描（未声明的同目录杂散 `.cn` 文件不参与编译，封闭树）。

#### 5.9.2 导入语法（全形式）

```
导入声明 = "导入" 路径 ["作为" 标识符]
          | "导入" 路径 "::" "{" 导入项 ("," 导入项)* "}"
          | "导入" 路径 "::" "*"
导入项 = 标识符 ["作为" 标识符]

// 示例
导入 核心::可选::{某些, 无}
导入 核心::集合
导入 核心::列表 作为 动态数组
导入 核心::集合::*
导入 包::网络::连接
公开 导入 网络::连接        // 再导出（包.cn 聚合 API）
```

#### 5.9.3 可见性（标签式 + 默认私有 + 交集规则）

```
公开:
    函数 公开函数() { }
私有:
    函数 内部函数() { }
函数 默认私有() { }          // 无标签 → 默认私有
```

- **默认私有**（v2.0 变更）：模块级与类内均默认私有；公共 API 须显式 `公开:`。
- **可见性交集规则**：符号最终可见性 = 各层级可见性交集（取最严格）。模块私有块内的类，即使类内标 `公开:`，跨文件导入时整个类不可见。

| 类层级 | 成员层级 | 同文件 | 同包跨文件 | 跨包 |
|--------|---------|--------|-----------|------|
| 模块公开 | 类内公开 | ✅ | ✅ | ✅ |
| 模块公开 | 类内私有 | ✅ | ❌ | ❌ |
| 模块私有 | 类内公开 | ✅ | ❌ | ❌ |
| 模块私有 | 类内私有 | ✅ | ❌ | ❌ |

#### 5.9.4 crate 模型（包 = crate）

```
// 包.cn（库 crate 根）
模块 网络
模块 存储
公开 导入 网络::连接        // 再导出

// 跨包导入
导入 网络库::连接
导入 网络库::请求::获取
```

- 包 = crate = 命名空间边界，跨包同名符号不冲突（IR/codegen 符号加 `包名$` 前缀）。
- 核心包 prelude：核心包基础项默认可见，`导入 核心::*` 可显式扩展。

#### 5.9.5 货舱.toml

```toml
[货舱]
名称 = "我的程序"
版本 = "0.1.0"
[依赖]
核心 = "内置"
网络库 = "1.2"
```

#### 5.9.6 分号模型（2026-09-04 plans/015 方案B 反转）

- 语句强制 `;` 终结（见 §4.6）；模块级声明中：**导入声明 `导入 路径;` 须分号**（Rust `use` 同款）、**顶层常量/静态声明须分号**（C++ `const`/`static` 同款）；模块声明（`模块 名`）与 结构体/类/接口/枚举体 `}` 自终结，不加分号。

#### 5.9.7 名称解析（遮蔽与歧义，2026-09-07 plans/018 新增·Rust 对照）

> 此前规范空白（宿主行为是事实标准无条文），本轮立规。Rust 对照：item > named use > glob 三级 +
> E0255（导入与本地定义同名）/E0659（glob 歧义）显式报错、绝不静默选（歧义绝不引入错误绑定）。

**三级优先级**（纯名引用——不带 `::` 的标识符——按此决议唯一绑定）：

| 优先级 | 名字来源 | CN 形式 | Rust 对应 |
|---|---|---|---|
| 1（最高） | 本模块显式定义项 | 函数/结构体/常量/静态声明 | item in current module |
| 2 | 显式导入（具名绑定） | `导入 m::符号`、`导入 m::{a, b}`、`导入 m::符号 作为 别名`（符号以原名/别名绑定纯名） | named `use` |
| 3（最低） | 通配引入 | `导入 m;`（模块整体导入=其公开 API 批量引入）、`导入 m::*` | glob `use m::*` |

> **模块级别名**：`导入 m 作为 别名` / `导入 a::b 作为 别名` 绑定的是**模块路径本身**
> （供 `别名::符号` 限定访问），不引入纯名——等价 Rust `use m as n`。
>
> **限定调用恒明确（2026-09-07 呈报一B 裁决：对齐 Rust「路径恒可用、导入只影响纯名」）**：
> `m::X`（含多级）指向**已加载模块**的公开符号即可用，**不要求先导入**；宿主现行 P1-1
> （未导入限定调用报错）随之废止（✅ 已实施：plans/018 工作流2，第三十一轮下半程，
> E2E 161 正测 + 单测负覆盖锚定）。

**冲突与遮蔽判定矩阵**（同为纯名、签名相同的前提下）：

| 冲突面 | 判定 | 诊断 |
|---|---|---|
| ① × ② 同名 | **编译错误** | 「导入与本地定义同名」（E0255 对应；改名或删其一） |
| ② × ② 同名 | **编译错误** | 「多次显式导入同名」 |
| ③ × ③ 同名 | **使用点歧义错误**（仅当该名字被实际引用时报） | 「符号歧义：来自 模块A/模块B」（E0659 对应） |
| ① 遮蔽 ③ | **合法**（唯一允许的遮蔽） | 无诊断（本模块项优先，glob 项被遮蔽） |
| ② 遮蔽 ③ | 合法 | 无诊断 |
| 重载（同名不同签名） | 不适用本矩阵 | 按函数重载决议（签名精确/字面量宽匹配），歧义同报错 |

- **限定调用恒明确**：`模块::符号(...)`（含多级 `a::b::c`）直接决议到指定模块的公开符号，不参与遮蔽，永无歧义；未导入的模块路径引用=「未声明的标识符」（P1-1 严格化，prelude 内置例外）。
- **跨模块同名同签名合法共存**：链接符号在多模块场景加前缀 `模块全路径$名#参数串`（函数/结构体/常量统一），单文件/主模块不加前缀——编译期一次拼接，运行时零开销。
- **决议失败绝不静默**：歧义/未命中一律编译诊断，禁止「先收集文件赢」式静默绑定（静默吞=错误绑定，违背安全准绳）。

```
// 示例：本模块项遮蔽整体导入（合法）
导入 工具库;                        // ③ 整体导入，工具库 也有 公开 函数 版本()
函数 版本() -> 整32 { 返回 100; }   // ① 本模块项
// 纯名 版本() -> 100（①遮蔽③）；工具库::版本() -> 200（限定调用直达）

// 示例：显式导入与本模块项同名 = 编译错误
导入 工具库::版本;                  // ② 具名绑定与上方 ① 同名 → 编译错误「导入与本地定义同名」
// 对照：导入 工具库; 或 导入 工具库::*;（③ 通配）与本地 版本 同名 = 合法遮蔽
```

> 完整规范见 `.zcode/skills/cn-language-spec/specs/08-模块系统.md`（v2.0）与 `specs/09-包与依赖.md`。

---

## 六、编译器架构

### 6.1 编译流水线总览

```
CN源代码(.cn)
    │
    ▼
词法分析器(Lexer) -> Token流
    │
    ▼
语法分析器(Parser) -> AST树
    │
    ▼
语义分析器(Semantic) -> 带类型AST + 符号表
    │
    ▼
IR生成器(IRGenerator) -> CN-IR
    │
    ▼
优化器(Optimizer) -> 优化后的CN-IR
    │
    ├─────────────┐
    ▼             ▼
Win x64 Codegen  Linux ARM64 Codegen
    │             │
    ▼             ▼
.asm文件        .asm文件
    │             │
    ▼             ▼
汇编器+链接器 -> 可执行文件
```

### 6.2 各阶段职责

#### 阶段1：词法分析器（Lexer）
- 输入：.cn源文件（UTF-8编码）
- 输出：Token流
- 职责：UTF-8中文字符识别、字面量解析、运算符与分隔符识别、注释剥离、行号/列号追踪
- 关键设计：中文关键字最长匹配，关键字后必须有空格或符号分隔

#### 阶段2：语法分析器（Parser）
- 输入：Token流
- 输出：AST
- 职责：递归下降解析、运算符优先级处理（Pratt parser）、AST节点构建、语法错误报告
- 关键设计：两趟解析——第一趟收集顶层声明，第二趟解析函数体，支持前向引用

#### 阶段3：语义分析器（Semantic Analyzer）
- 输入：AST
- 输出：带类型标注的AST + 全局符号表
- 职责：符号表构建与作用域管理、类型检查与推断、隐式类型转换验证、继承与接口实现验证、函数重载解析、错误码传播分析（结果/可选强制处理）、常量折叠预计算
- 关键设计：分模块语义分析——名称解析模块（NameResolution）、类型检查模块（TypeChecker）、OOP验证模块（OOPValidator）、错误码分析模块（ErrorAnalysis）；语义错误分级（错误/警告/提示），错误带源码位置与修复建议

#### 阶段4：IR生成器（IR Generator）
- 输入：带类型标注的AST + 符号表
- 输出：CN-IR（静态单赋值形式 SSA）
- 职责：AST降级为CFG基本块、类型化指令生成、虚拟寄存器分配、SSA Phi节点构建、内存访问显式化（指针运算、结构体字段偏移计算）
- 关键设计：每个函数一个控制流图（CFG），指令三地址码形式，类型信息内嵌于每个虚拟寄存器

#### 阶段5：优化器（Optimizer）
- 输入：CN-IR（SSA形式）
- 输出：优化后的CN-IR
- 职责：常量传播、死代码消除、公共子表达式消除、循环优化（不变量外提/强度削减）、内联优化、尾调用优化、指令合并与简化
- 关键设计：优化级别 -O0（无优化，调试用）、-O1（基础优化）、-O2（默认，含循环优化）、-O3（激进优化，含内联）；每个优化Pass独立可插拔，支持打印Pass执行前后IR用于调试

#### 阶段6：代码生成器（Code Generator）
- 输入：优化后的CN-IR
- 输出：平台汇编文件（.asm）
- 职责：指令选择（IR指令映射为目标ISA指令）、寄存器分配（线性扫描/图着色）、指令调度、ABI调用约定处理（Win64/ARM64）、栈帧布局、虚函数表（vtable）生成、全局数据段（.data）与代码段（.text）生成
- 关键设计：后端抽象层（Backend接口）——IR指令通过目标描述文件（TD）映射，Win x64与Linux ARM64共享同一IR与优化器，仅代码生成后端不同；汇编输出经平台汇编器（MASM/GAS）与链接器（link/ld）生成可执行文件

---

## 七、CN-IR中间表示设计

### 7.1 设计目标
- **可移植性**：与目标平台无关，Win x64 与 Linux ARM64 共享同一 IR 与优化器
- **可优化性**：采用静态单赋值（SSA）形式，Phi 节点统一汇合点，便于数据流分析
- **可调试性**：IR 保留源码位置信息（文件/行/列），支持 -emit-ir 选项导出可读 IR 文本
- **类型化**：每个虚拟寄存器带类型标注，代码生成时无需回查符号表

### 7.2 IR 结构

```
模块(Module)
├── 全局变量表
├── 函数列表(Function)
│   ├── 函数签名（名称/参数/返回值/链接属性）
│   ├── 基本块列表(BasicBlock)
│   │   ├── 入口标签
│   │   ├── 指令序列(Instruction)
│   │   │   ├── 操作码
│   │   │   ├── 源操作数列表（虚拟寄存器/常量/全局符号）
│   │   │   ├── 目标虚拟寄存器
│   │   │   ├── 类型标注
│   │   │   └── 源码位置
│   │   └── 终止指令（跳转/条件跳转/返回/选择跳转表）
│   └── Phi 节点集合
└── 全局字符串常量池
```

### 7.3 指令分类

| 类别 | 指令 | 说明 |
|------|------|------|
| 算术 | 加/减/乘/除/取余 | 整型与浮点变体（加浮32/加浮64等） |
| 位运算 | 位与/位或/位异或/位非/左移/右移 | 整型专用 |
| 比较 | 相等/不等/小于/小于等于/大于/大于等于 | 结果为布尔，整型与浮点变体 |
| 逻辑 | 逻辑与/逻辑或/逻辑非 | 短路求值语义 |
| 内存 | 加载/存储/取地址 | 含原子性标注（预留） |
| 转换 | 扩展/截断/浮整转换/位重解释 | 显式类型转换 |
| 控制 | 跳转/条件跳转/返回/选择表跳转 | CFG 终止指令 |
| 调用 | 直接调用/间接调用（函数指针） | 含调用约定与返回约定标注 |
| 对象 | 虚调用/新建对象/删除对象/字段加载/字段存储 | OOP 相关，虚调用经 vtable 间接跳转 |
| 错误 | 结果构造/结果检查/可选构造/可选检查 | 错误处理类型降级 |

### 7.4 虚拟寄存器与 SSA

- 虚拟寄存器编号：`%v0`、`%v1`、`%v2`...
- 每个虚拟寄存器恰好赋值一次（SSA 性质）
- 汇合点使用 Phi 指令合并多个入边值
- 基本块 ID：`块0`、`块1`、`块2`...
- IR 文本示例：

```
函数 加(整32 %v0, 整32 %v1) -> 整32 {
块0:
    %v2 = 加 整32 %v0, %v1
    返回 %v2
}
```

### 7.5 类型表示

| IR类型 | 对应CN类型 | 大小 |
|--------|-----------|------|
| i8/i16/i32/i64/i128 | 整8/整16/整32/整64/整128 | 1/2/4/8/16字节 |
| u8/u16/u32/u64/u128 | 正8/正16/正32/正64/正128 | 1/2/4/8/16字节 |
| f32/f64 | 浮32/浮64 | 4/8字节 |
| i1 | 布尔 | 1字节 |
| ptr | 指针/字符串/字符* | 8字节（64位平台） |
| struct | 结构体/类 | 布局后大小 |
| void | 空类型 | 0字节 |

### 7.6 结果/可选类型降级约定
- `结果<正常, 错误>` 降级为 `结构体 { 布尔 是否正常; 联合体 { 正常类型 值; 错误类型 错误值 } }`
- `可选<类型>` 降级为 `结构体 { 布尔 是否某些; 类型 值 }`
- 检查成员 `.正常`/`.某些` 对应结构体首字段读取，未检查即访问 `.值` 由语义分析器在阶段3提前拦截
- **联合体仅用于编译器内部IR降级，源语言层面也可使用**（见3.5节复合类型定义）

---

## 八、代码生成

### 8.1 目标平台与ABI

| 目标 | 汇编器 | 链接器 | ABI规范 | 备注 |
|------|--------|--------|---------|------|
| Win x64 | MASM（ml64） | link.exe | Microsoft x64 ABI | 初期主目标 |
| Linux ARM64 | GNU as（GAS） | ld | AAPCS64 | 初期次目标 |
| Linux x64 | GNU as（GAS） | ld | System V AMD64 ABI | 预留 |
| macOS x64/ARM64 | 预留 | 预留 | Apple ABI | 预留 |

### 8.2 指令选择策略
- IR指令与目标指令的映射采用**模式匹配**：针对常见IR模式（如"加法+比较+条件跳转"）匹配目标平台的组合指令
- 乘除/取余等复杂运算：整型宽度 ≤ 64 位映射为原生指令；整128位宽用运行时辅助函数（如 `__cn_mul_i128`）模拟
- 浮点运算映射为SSE/NEON指令集指令

### 8.3 寄存器分配
- **Win x64**：采用线性扫描寄存器分配器（针对-O2），-O0/-O1时全栈帧分配（调试友好）
- 预留寄存器：`rbx/rbp/r12~r15` 为被调用者保存寄存器，`rax/rcx/rdx/r8~r11` 为调用者保存寄存器
- **Linux ARM64**：x19~x28为被调用者保存，x0~x18为调用者保存；浮点寄存器 d0~d31
- 溢出（spill）：临时值溢出到栈槽，栈帧统一管理
- **阶段C 实现（2026-08-14，Task 4.3/4.4）**：
  - 线性扫描寄存器分配器 `codegen/reg_alloc.cpp`：活跃区间计算（def-use + 跨块活跃传播）、
    区间按 start 线性扫描分配、溢出到栈槽
  - 仅对 **i64/u64/ptr** 虚拟寄存器参与分配（保守：浮点/i128/小位宽保持栈槽，避免与发射器宽度假设冲突）
  - 仅分配**被调用者保存寄存器**（x64 rbx/r12~r15；arm64 x19~x28），序言保存、尾声恢复
  - -O2 起启用（`--no-regalloc` 可关）；-O0/-O1 全栈帧不变
  - 调试信息 `codegen/debug_info.cpp`：汇编嵌入源码位置注释（`; src:` / `// src:`），
    DWARF 节生成接口预留（`mappings()` + `dwarfLineProgram()`）

### 8.4 栈帧布局（Win x64）

```
高地址
┌─────────────────────────┐
│ 返回地址（调用者压栈）      │
├─────────────────────────┤
│ 被调用者保存寄存器区        │
├─────────────────────────┤
│ 局部变量区（含溢出槽）      │
├─────────────────────────┤
│ 参数栈传递区（>4个参数）    │
├─────────────────────────┤
│ 影子空间（32字节，预留）    │
└─────────────────────────┘
低地址  ← rsp
```

### 8.5 调用约定
- 参数传递：前4个整型参数用 `rcx/rdx/r8/r9`，前4个浮点参数用 `xmm0~xmm3`（Win x64）
- 返回值：整型/指针用 `rax`，浮点用 `xmm0`
- 大于8字节的结构体：调用方在栈上分配并传入指针
- 栈对齐：调用前栈16字节对齐

### 8.6 OOP运行时布局
- 类实例布局：`[vtable指针 | 父类成员区 | 自身成员区]`（单继承）
- 虚函数表（vtable）：函数指针数组，子类重写时覆盖对应槽位；接口实现采用附加vtable（B1 全局槽位，P3-19 已实现：对象首 8 字节虚表指针后接接口分派区）。接口类型可作函数参数/返回（C2 多态传参，P3 收尾已锁定）；A1 同地址语义（值/指针零指针调整，经接口方法修改即写回原对象）。
- 构造/析构：构造函数返回对象指针；析构函数内联清理资源后调用父类析构

### 8.7 全局数据与字符串常量
- 全局变量进入 `.data`/`.bss` 段
- 字符串常量合并去重后进入 `.rdata`/`.rodata` 段
- 全局符号命名：源码中文符号经内部名字修饰（mangling）后生成唯一汇编符号，规则见附录C

### 8.8 汇编输出示例（Win x64）

```asm
; 函数 加(整32 a, 整32 b) -> 整32
.code
?加@@YAHHH@Z PROC
    ; prologue
    push    rbp
    mov     rbp, rsp
    ; 参数：ecx = a, edx = b
    mov     eax, ecx
    add     eax, edx
    ; epilogue
    pop     rbp
    ret
?加@@YAHHH@Z ENDP
END
```

---

## 九、优化器

### 9.1 优化级别

| 级别 | 说明 | 适用场景 |
|------|------|---------|
| -O0 | 无优化，全栈帧分配，保留调试信息 | 开发调试 |
| -O1 | 基础优化（常量折叠、死代码消除） | 快速构建 |
| -O2 | 默认级别，含循环优化与内联 | 生产发布 |
| -O3 | 激进优化，含激进内联与向量化预留 | 性能敏感场景 |

### 9.2 优化Pass清单

| Pass | 类别 | 说明 |
|------|------|------|
| 常量折叠与传播 | 基础 | 编译期常量计算与传播 |
| 死代码消除（DCE） | 基础 | 删除无副作用且结果未使用的指令 |
| 公共子表达式消除（CSE） | 基础 | 相同表达式复用 |
| 复写传播 | 基础 | 消除冗余复制指令 |
| 指令合并 | 基础 | 合并可组合指令 |
| 循环不变量外提（LICM） | 循环 | 将循环内不变量移到循环前 |
| 强度削减 | 循环 | 乘法/除法替换为移位/加法 |
| 循环展开（预留） | 循环 | 小循环体展开减少分支开销 |
| 函数内联 | 函数 | 小函数内联展开（启发式阈值） |
| 尾调用优化（TCO） | 函数 | 尾递归转为循环 |
| 选择表生成 | 控制 | 密集选择语句生成跳转表 |
| 空块合并 | 控制 | 合并空基本块、消除冗余跳转 |

### 9.3 Pass框架设计
- 每个Pass实现统一接口：`Pass::运行(IR模块) -> 是否修改`
- Pass管理器按依赖顺序调度，重复运行直至收敛（fixpoint）
- 每个Pass可通过 `-打印ir=pass名` 输出执行前后IR对比
- 优化前后正确性验证（预留）：`-验证-ir` 检查SSA性质不变量（支配关系、Phi节点合法性）

---

## 十、运行时设计

### 10.1 运行时库结构

```
cnrt（CN Runtime）
├── 内存管理：分配/释放/重新分配/复制
├── 字符串操作：长度/比较/连接/复制/查找
│   （Task 2.8 补充：子串/字典序/大小写/前缀/后缀/包含/修剪/反转/
│     整数转字符串/浮点转字符串/字符转字符串/释放——按常见字符串库补充，动态分配，调用方负责释放）
├── 格式化与IO：格式化输出/标准输出/标准错误/文件IO
├── 数学库：浮点运算辅助/128位整数运算辅助
├── OOP支持：vtable辅助/类型信息（预留RTTI）
└── 程序入口：启动代码（crt0风格，调用主函数）
```

### 10.2 内存管理API

| API | 说明 | 对应C库 |
|-----|------|---------|
| 分配(大小) | 分配内存块 | malloc |
| 释放(指针) | 释放内存块 | free |
| 重新分配(指针, 新大小) | 调整内存块大小 | realloc |
| 复制内存(目标, 源, 大小) | 内存复制 | memcpy |
| 置零内存(目标, 大小) | 内存置零 | memset |

> **扩展（2026-08 自举前置 C-1/C-3）：分配计数 + 进程竞技场**
> - `内存::活动分配数()` -> 整64：当前未释放块数（原子计数，泄漏检测基线）；
>   `内存::总分配次数()` -> 整64：累计分配次数。分配/释放/重新分配 全路径
>   计数（含字符串/时间/文件 API 内部 malloc，统一经计数辅助）。
> - `内存::竞技场分配(大小)` -> 空类型*：一次性进程竞技场 bump 分配（8 字节
>   对齐，块链 64KB，超块自动扩容）；`内存::竞技场重置()`：一次性释放全部块；
>   `内存::竞技场活动字节()` -> 整64：已分配总字节（含块头）。
>   适配编译器内存模式（长生命周期 + 一次分配）：自举源码无需逐对象释放，
>   杜绝泄漏/双重释放；智能指针可后置（plans/004 C 组判定）。

### 10.3 字符串表示
- 字符串采用UTF-8编码，以`\0`结尾
- 字符串字面量在编译期进入只读数据段，字符串变量为指针
- 字符串连接等操作调用运行时API完成
- **拥有型字符串（2026-09-10 plans/019 阶段4' 方案A 用户裁决，替代 Task 2.8 值语义）**：
  `字符串` 局部变量为**拥有类型**——①字面量/标识符拷贝初始化经 `__cn_str_copy` 落堆
  （变量一律拥有堆串，Rust `"x".to_string()` 同款）；②赋值=free 旧值再拥有化新值
  （drop 旧+move 新）；③函数返回块自动注入 `__cn_str_free`（RAII 空安全；返回字符串
  变量=所有权移出跳过释放）；④`转移()` 零拷贝衔接（句柄直拷+源清零）；⑤裸指针视图
  用 `字符*`（FFI/只读降级）。**拥有判定白名单**（第一版）：字面量/标识符/内置分配族
  调用（复制/连接/拼接/子串/大写/小写/修剪/反转）；其余调用形态（`驻留文本` 返回驻留表
- **返回拥有契约（A2 2026-09-11 方案甲 用户批准）**：返回类型 `字符串`=拥有（调用方按返回类型登记 RAII 自动释放——用户函数返回堆串泄漏根治，Rust `fn f() -> String` 同款）；返回类型 `字符*`=借用（调用方零拷贝不释放，`&str` 同款）。返回位借用形态（参数直传/成员/下标/解引用/字符* 调用/全局静态）编译错误——须 字符串复制(...) 或改 字符* 返回。隐式转换收紧：拥有→借用自动；借用→拥有（字符* 初始化/赋值 字符串 变量）须显式 字符串复制(...)。返回 字符串 函数的字面量返回由编译器自动 copy 落堆。
  指针=借用、用户函数可能返回借用包装）整变量退出 RAII（宁可不自动释放保安全——
  用户函数返回拥有语义随第二批类型区分落地）。参数=借用（被调方不释放）；
  顶层静态字符串=只读语义不 RAII。
- **比较语义（Task 2.8）**：比较运算符（`==`/`!=`/`<`/`>`/`<=`/`>=`）仅定义整型与浮点变体
  （见运算符表），字符串不参与运算符比较；等价能力由 `字符串比较`（相等）与
  `字符串字典序`（全序比较，返回 <0/0/>0）提供
- **跨类型拼接语义（增强）**：`字符串 + 数值` 表达式触发隐式拼接——右操作数为整数/浮点/布尔/字符/枚举时，
  编译器自动插入 `整数转字符串`/`浮点转字符串`/`字符转字符串` 转换后再 `字符串连接`；
  布尔转换为 `"真"`/`"假"`。多操作数左结合：`"a" + 1 + 2` 等价 `(("a" + 1) + 2)`，结果为 `"a12"`。
  混合类型拼接结果由调用方负责 `字符串释放`。
- **字符串驻留（2026-08-25 用户批准，自举重建 P1；对标 rustc Symbol / LLVM StringPool）**：
  `驻留(字符串)` -> 整64：字符串内容加入全局驻留表并返回唯一整数 ID（Symbol）。
  相同内容返回同一 ID，字符串内容只存一份；`驻留文本(整64 ID)` -> 字符*：ID 反查文本（调试/错误消息；A2 2026-09-11 方案甲——驻留表指针=借用视图，持有须 字符串复制(...) 落堆）；`驻留计数()` -> 整64：驻留唯一内容数。
  驻留表生命周期为编译器进程级，不随 内存::释放全部() 释放（防 reset 破坏符号引用）。
  用途：Token/AST/IR/符号表文本字段一律用整型 ID（O(1) 比较/哈希），消除百万级重复字符串分配。

### 10.6 格式化与输入输出API

> **打印函数族（方案C 2026-08-14 ✅ 已修复）**：打印函数族**只有 `打印` / `打印行` 两个变参函数**（外加 `格式化`），
> 旧版遗留的 `打印行整数` / `打印行浮点` **已删除（缺陷修复）**。
> - **"行" 的语义统一为：不追加换行符**（`打印行` 名称中"行"表示逐行连续输出语义）。
> - 遗留函数 `打印行整数(整64)`（`printf "%lld\n"`，换行）与 `打印行`（不换行）语义**恰好相反**，
>   造成命名二义性——已彻底删除，统一用变参 `打印`/`打印行` 表达：
>   `打印行整数(42)` → `打印(42)`（输出 `42\n`，换行行为完全一致）；
>   `打印行浮点(3.5)` → `打印(3.5)`（输出 `3.500000\n`）。
> - 整128/正128 打印同样由 `打印` 变参展开（`__cn_print_i128`/`__cn_print_u128`，完整128位）。

| API | 说明 | 对应C库 |
|-----|------|---------|
| 打印(任意值...) | 变参打印，`字符串 + 数值` 拼接或逐参展开为打印序列，末尾**自动换行** | printf（子集） + "\n" |
| 打印行(任意值...) | 同 `打印` 但末尾**不自动换行**（名称中"行"表示逐行连续输出语义，不追加换行符） | printf（子集） |
| 格式化(格式字符串, 参数...) | sprintf 风格格式化，返回新字符串（调用方释放） | sprintf |
| 读取行() | 从标准输入读取一行，返回字符串（不含换行，调用方释放） | fgets |
| 读取整数() / 读取浮点() | 从标准输入读取数值 | scanf（子集） |

> **`格式化()` 占位符**：`%d`（整型）、`%u`（无符号）、`%f`（浮点）、`%s`（字符串）、`%c`（字符）、`%x`/`%X`（十六进制）、`%o`（八进制）、`%p`（指针）。变参 ABI 在 IR 层展开为运行时 `__cn_format` 调用。

### 10.4 程序入口约定
- 入口函数名：`主`，签名：`函数 主() -> 整32`（返回0表示成功）
- 运行时启动代码负责：初始化堆、调用全局构造器、调用`主`、传递返回值给操作系统
- 命令行参数（预留）：`函数 主(整32 参数个数, 字符** 参数值) -> 整32`

### 10.5 128位整数运算辅助
- 整128/正128的乘除取余等运算由运行时辅助函数实现
- 命名约定：`__cn_mul_i128`、`__cn_div_i128`、`__cn_div_u128` 等
- 与目标平台无关，统一用C++实现并随编译器分发

---

## 十一、开发路线图

> 参考 Rust参考/003 路线图，结合C++从零实现的技术选型调整。

### 路线图阶段 × 编译器架构阶段对照表

| 路线图阶段 | 对应编译器架构阶段 |
|-----------|------------------|
| 阶段零 | 工程搭建（CMake配置） |
| 阶段一 | 词法分析器 + 语法分析器 |
| 阶段二 | 语义分析器 |
| 阶段三 | OOP语义 + 模块系统 |
| 阶段四 | IR生成器 + 优化器 + 代码生成器 |
| 阶段五 | ARM64后端 + 优化增强 |
| 阶段六 | 标准库开发 |
| 阶段七 | 自举（全部阶段用CN重写） |

### 阶段零：工程搭建
- [ ] 项目初始化：CMake工程、目录结构、构建脚本
- [ ] CLI框架：编译命令参数解析（-o/目标/优化级别/-emit-ir等）
- [ ] 诊断基础设施：错误/警告报告、源码位置、颜色输出
- [ ] 里程碑：`cn编译 --版本` 可运行

### 阶段一：基础编译器
- [x] 词法分析器：UTF-8中文分词、Token定义、字面量与运算符识别（Task 1.1/1.1a/1.2 ✅）
- [x] 语法分析器：递归下降+Pratt、AST定义、两趟解析（Task 1.3/1.4 ✅）
- [x] 基础类型系统：整8~整128、正8~正128、浮32/浮64、布尔、字符（Task 1.5 ✅）
- [x] 代码生成（最小子集）：常量/变量/算术/调用/返回 → Win x64汇编（Task 1.7 ✅）
- [x] 链接与运行：调用平台汇编器/链接器生成可执行文件（Task 1.9 ✅，ml64/link）
- [x] 里程碑：Hello World可运行（2026-08-12 ✅：`cn build` → `hello.exe` → 输出"你好，世界"，E2E 1/1、单元测试 181/181）

### 阶段二：核心语言
- [ ] 控制流：如果/否则/当/循环/选择
- [x] 函数：定义/调用/原型/递归
- [x] 结构体与枚举（Task 2.7 ✅ 2026-08-12：结构体定义/实例化/字段访问、枚举定义/负数/选择case、联合体新关键字，433/433测试+E2E 7/7；字段访问原含 `->` 指针形式，**已于 2026-09-03 v2.1 成员访问统一为 `.`（指针自动解引用一级）时废除**）
- [x] 字符串系统与运行时API（Task 2.5 ✅ 2026-08-12：字面量4形态/字符串类型/连接/运行时API/打印行多参数，390/390测试+E2E 6/6）
- [x] 数组与指针运算（Task 2.4 ✅）
- [ ] 错误处理：结果/可选类型、传播分析
- [ ] 里程碑：可编写基本算法与数据结构

### 阶段三：OOP与模块
- [ ] 类/继承/多态：vtable生成、虚拟调用
- [ ] 接口与抽象类
- [ ] 构造/析构/自身/父类
- [ ] 模块系统：导入/从、路径解析、可见性
- [ ] 静态变量与全局变量
- [ ] 里程碑：可编写中型面向对象程序

### 阶段四：优化与后端
- [x] SSA构造与Phi节点（2026-08-14 阶段B 完成：opt/cfg 支配树 + opt/ssa 汇合点 Phi，挂 -O3）
- [x] 基础优化Pass：常量折叠、DCE、CSE、代数简化、复写传播、全局值传播（2026-08-13 完善C 完成）
- [x] 循环优化：LICM、强度削减（2026-08-14 阶段B 完成：opt/licm + opt/strength_reduce，挂 -O2）
- [x] 函数内联（2026-08-14 阶段B 完成：opt/inline，启发式阈值 16 指令，挂 -O3）
- [x] 尾调用优化 TCO（2026-08-14 阶段B 完成：opt/tail_call 尾递归转循环，挂 -O2）
- [x] 寄存器分配器（线性扫描）（2026-08-14 阶段C 完成：codegen/reg_alloc.cpp，-O2 起启用）
- [x] Linux ARM64后端（2026-08-14 阶段A 完成，28/28 E2E）
- [x] 优化级别框架（-O0~-O3）（-O0/-O2/-O3 三级别 E2E 28/28 一致，2026-08-14 阶段B 验证）
- [ ] 里程碑：CN语言编译器性能达到C编译器（-O0）水平

### 阶段五：标准库与工具链
- [ ] 核心标准库：集合（列表/字典/集合）、IO、文件系统
- [ ] 字符串处理与格式化
- [ ] 数学库与128位整数辅助
- [ ] 构建系统集成（CMake脚本生成）
- [ ] 测试框架（E2E测试基础设施）
- [ ] 里程碑：可开发命令行工具

### 阶段六：生产就绪
- [ ] 错误信息优化：中文错误、错误恢复、修复建议
- [ ] 增量编译与并行编译
- [ ] 性能基准：与C/C++性能对比
- [ ] 完整文档：语言参考、标准库文档、教程
- [ ] 稳定性保证：向后兼容承诺
- [ ] 里程碑：v1.0正式发布

### 阶段七：自举
- [x] 编译器自身用CN语言重写词法分析器
- [x] 逐步重写语法分析器与语义分析器
- [x] 重写IR与代码生成
- [x] 里程碑：CN语言编译器自举成功（用CN语言编译CN编译器）

---

## 十二、测试策略

### 12.1 测试层级

| 层级 | 范围 | 工具 | 目标 |
|------|------|------|------|
| 单元测试 | 单个模块（词法/语法/IR等） | C++单元测试框架（如GoogleTest） | 模块级正确性 |
| 集成测试 | 多阶段组合（词法+语法+语义） | 自定义测试框架 | 阶段间接口正确性 |
| E2E测试 | 完整编译运行（源文件→可执行→运行→输出验证） | 脚本驱动 | 端到端功能可用性 |
| 回归测试 | 全量测试集 | CI集成 | 防止功能回退 |

### 12.2 E2E测试原则（遵循项目规范）
- **E2E先行**：每个功能必须先写E2E测试，测试通过后才算完成
- **禁止.skip()**：所有测试必须真正运行，不使用跳过
- **真实配置**：所有测试使用真实配置，不使用Mock
- **渐进式**：分阶段实施，每阶段独立交付价值
- **全链路验证**：E2E测试必须通过真实编译流程（源文件 → cn编译 → 汇编 → 链接 → 运行 → 检查输出），禁止绕过生产代码直接实例化组件

### 12.3 测试目录结构

```
tests/
├── unit/           # 单元测试（对应src各模块）
│   ├── lexer_test.cpp
│   ├── parser_test.cpp
│   ├── ir_test.cpp
│   └── optimizer_test.cpp
├── integration/    # 集成测试
│   ├── lexer_parser_test.cpp
│   └── semantic_test.cpp
├── e2e/            # E2E测试
│   ├── cases/      # 测试用例源码
│   │   ├── 基础.cn
│   │   ├── 控制流.cn
│   │   ├── OOP.cn
│   │   └── 错误处理.cn
│   ├── expected/   # 期望输出
│   └── run_e2e.py  # E2E测试驱动脚本
└── fixtures/       # 共享测试数据
```

### 12.4 测试用例设计要点
- 每个关键字、运算符、类型必须有对应的正反用例
- 中文边界测试：中文标识符、中文错误信息、UTF-8多字节字符处理
- 错误路径测试：语法错误、类型错误、未定义符号、除零等运行时错误
- 平台差异测试：Win x64 与 Linux ARM64 分别验证
- 优化正确性测试：-O0 与 -O2 输出结果必须一致
- 回归测试：每个缺陷修复后必须新增对应测试用例

### 12.5 编译验证要求
- 任何代码修改后，必须先通过编译验证（CMake构建）才能完成任务
- attempt_completion 前必须确认：所有子任务完成、所有测试通过、无未处理错误

---

## 十三、项目结构

### 13.1 顶层目录

```
CN_Language_C/
├── CMakeLists.txt          # 顶层构建配置
├── include/cnlang/         # 公共头文件
│   ├── frontend/           # 词法/语法/语义
│   ├── ir/                 # CN-IR
│   ├── opt/                # 优化器
│   ├── codegen/            # 代码生成后端
│   └── runtime/            # 运行时接口
├── src/
│   ├── frontend/           # 前端实现
│   │   ├── lexer/          # 词法分析器
│   │   ├── parser/         # 语法分析器
│   │   └── semantic/       # 语义分析器
│   ├── ir/                 # IR生成与表示
│   ├── opt/                # 优化Pass
│   ├── codegen/            # 后端（WinX64/ARM64）
│   │   ├── win_x64/        # Win x64后端
│   │   └── linux_arm64/    # Linux ARM64后端
│   ├── runtime/            # 运行时库（cnrt）
│   ├── driver/             # 编译驱动与CLI
│   └── main.cpp            # 入口
├── tests/                  # 测试套件
├── tools/                  # 辅助工具（E2E脚本等）
├── plans/                  # 设计文档
└── target/                 # 编译输出（E2E测试产物）
```

### 13.2 模块依赖约束
- `frontend` → `ir` → `opt` → `codegen`（单向依赖，禁止反向）
- `driver` 依赖所有模块，是唯一组合入口
- 每个源文件不超过1000行，每个函数不超过100行（遵循项目规范）
- 模块可拆分为子模块目录，旧文件及时删除

---

## 附录

### 附录A：命令行接口

```
cn编译 [选项] 源文件...
选项：
  -o <文件>        指定输出文件
  -目标 <目标>     目标平台（win-x64 / linux-arm64）
  -O0/-O1/-O2/-O3  优化级别（默认-O2）
  -emit-ir         输出IR文本
  -emit-asm        输出汇编文本（默认）
  -S               只编译不链接，生成.asm
  -c               只编译不链接，生成目标文件
  -打印ir=<pass>   打印指定Pass执行前后IR
  -验证-ir         验证IR不变量
  -v / --版本      显示版本信息
  -h / --帮助      显示帮助
```

### 附录B：错误码约定（结果<正常, 错误>的错误值）

| 错误码 | 含义 |
|--------|------|
| 1 | 除零错误 |
| 2 | 数组越界 |
| 3 | 空指针解引用 |
| 4 | 内存分配失败 |
| 5 | 文件打开失败 |
| 6 | 无效参数 |
| 7 | 资源未初始化 |
| 8 | 溢出错误 |

**入口函数返回 `结果<T, 整32>` 的进程退出码（2026-09-16 新增·246-a 用户裁决·Rust Termination 同构）：**

- `主` 返回 `结果<整32, E>`（E 为整型）时：正常 → 退出码取 `.值`（截断为 int）；错误 → 退出码取 `.错误` 码
- 与运行时错误码（上表）同一契约——用户态与运行态退出码语义全线统一
- 实现方式：编译器自动合成整32 包装入口（调 主 后按分支取值），调用约定与运行时入口对齐
- 其他聚合类型（可选/结构体等）与 `.值` 非数值的 `结果` 暂不支持（编译期诊断——退出码语义待规范定义）

### 附录C：名字修饰规则（mangling）
- 源码中文符号名统一转换为 UTF-8 字节序列的十六进制编码，前缀 `?`，后缀按函数签名编码
- 全局变量：`?变量名@@3A` 形式
- 函数：`?函数名@@Y<返回编码><参数编码>@Z` 形式（Win x64 MSVC风格）
- 静态/局部符号：模块内唯一即可，不参与全局修饰
- 设计目标：保证同名不同签名的函数可共存（重载支持），避免符号冲突

### 附录D：参考资料
1. Rust参考/002 CN语言设计规格书
2. Rust参考/003 CN语言开发路线图
3. Rust参考/004 CN语言挑战与风险分析
4. Microsoft x64 ABI 规范
5. ARM AAPCS64 调用约定规范
