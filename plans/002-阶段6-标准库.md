# CN语言编译器实施计划（阶段六：标准库）

> 前序文档：[`plans/002-00-总览与约束.md`](plans/002-00-总览与约束.md)（目标/架构/技术栈/全局约束/执行策略）
> 前序文档：[`plans/002-阶段五-LinuxARM64.md`](plans/002-阶段五-LinuxARM64.md)（阶段五：Linux ARM64）
> 后续文档：[`plans/002-阶段七-自举.md`](plans/002-阶段七-自举.md)（阶段七：自举）
> 基于设计规格书：[`plans/001 CN语言编译器设计规格书.md`](plans/001%20CN语言编译器设计规格书.md)

## 阶段六：标准库

> 目标：核心标准库（集合/IO/文件系统）、字符串处理与格式化、数学库与128位辅助、构建系统集成、测试框架完善。里程碑：可开发命令行工具（规格书阶段五）。

### Task 6.1: 核心库 ✅ 已完成（2026-08-14）
**文件：**
- Create: `stdlib/核心.cn`（错误码枚举 + 泛型 交换/最小/最大/断言，规格书10.2）✅
- Create: `stdlib/容器.cn`（向量/链表/栈/队列，规格书10.3）✅
- Create: `tests/e2e/29_core/`（核心库 E2E：主.cn + 核心.cn + 主.expected）✅
- Create: `tests/e2e/30_container/`（容器库 E2E：主.cn + 容器.cn + 主.expected）✅

> **✅ 实施记录（2026-08-14，第2步子任务）**：已完成 核心库 + 容器库 全链路：
> - **核心.cn**：`错误码` 枚举（除零=1/越界=2/空指针=3/内存=4/文件=5/参数=6/未初始化=7/溢出=8，值域与规格书附录B一致）+ 泛型 `交换(T* 左, T* 右)`（用 `左[0]` 下标写法规避行首 `*` 歧义）+ `最小/最大(T 左, T 右)` + `断言(布尔, 字符串)`（假条件调 `运行时错误(错误码.参数)` 终止）
> - **容器.cn**：`向量<T>`（T* 数据 + 整64 元素数量 + 整64 数组容量；扩容翻倍 0->4->8；RAII 析构释放；追加/插入/删除/读取/元素/设置/清空/预留/大小/容量/为空）、`链表<T>`（索引链表：T* 值表 + 整64* 下一索引，-1 表链尾；追加/头部追加/删除头部/删除尾部/读取头尾/清空）、`栈<T>`（独立 T* 动态数组）、`队列<T>`（独立索引链表）
> - **编译器缺陷修复（本轮）**：
>   1. `x64_instructions.cpp` Store 路径物理寄存器 32 位宽度（`mov eax, r12` → `mov eax, r12d`，A2022）
>   2. `x64_instructions.cpp` emitCast 物理寄存器源 32 位名转换（i64→i32 截断）
>   3. **结构体返回调用赋值缺陷（关键）**：`ir.cpp` 赋值路径漏 CallExpr 右值——`读取结果 = 数据.读取(99)` 只存 8 字节 retbuf 地址而非 CopyStruct 12 字节内容，导致 `.正常`/`.值` 读到地址值、越界判断失效（链表/栈/队列全部错乱）。修复：结构体返回调用右值走 CopyStruct 按值拷贝
> - **编译器缺陷规避（记录，留待 Debug 子任务修复）**：~~**构造函数重载缺陷**——语义层 `ClassInfo.methods` 按方法名（name）索引，同名构造函数（无参/带参）互相覆盖~~ **✅ 已修复（2026-08-15 Debug 子任务）**：构造/析构 methods 表改按 sigKey（名#参数串）作 key 存储（多版本共存），语义层/IR 层构造查找按实参个数+类型匹配最优；回归 E2E 41_ctor_overload（无参/带参分别实例化验证）。容器库现可用构造重载（预分配仍保留 预留() 方法兼容）
> - **实现决策（Task 6.1 实测边界）**：错误码用枚举（`错误` 是内置构造器不可重定义）；指针解引用赋值 `*p = v` 行首歧义（换行≡空格）统一用下标 `p[0]`；元素按 8 字节槽存储；常量成员函数语法 `常量 函数 名() -> 类型`；栈/队列不组合 向量<T>/链表<T> 字段（规避嵌套泛型字段 + 结构体返回方法 this 传递缺陷，改独立基础字段实现）
> - 验证：单测 987/987、E2E 32/32 全通过、编译 0 警告

### Task 6.2: IO库（含输入） ✅ 已完成（2026-08-14）
**文件：**
- Create: `stdlib/IO.cn`（标准输入/标准错误：读取行/读取整数/读取浮点/打印到错误）✅
- Create: `src/runtime/input_api.cpp`（`__cn_read_line`/`__cn_read_int`/`__cn_read_float`/`__cn_print_err`，规格书 10.6）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册 `IO.读取行` 等输入 API）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（`IO.xxx` → `__cn_read_*` 名称映射 + 结果类型映射）✅
- Modify: `src/runtime/runtime.hpp`（声明输入 API）+ `CMakeLists.txt`/`src/cn_main.cpp`（cn_runtime 与 .obj 清单两处同步）✅
- Modify: `tests/e2e/run_e2e.py`（支持同名 `.input` 文件 stdin 注入，Task 6.2 输入用例）✅
- Create: `tests/e2e/33_io_input/`（IO 输入 E2E：主.cn + 主.input + 主.expected）✅
- Create: `tests/unit/runtime/test_input_api.cpp`（11 用例：读取行/读取整数/读取浮点/打印到错误）✅

**验证标准：** `打印`/`打印行`/`格式化` 输出；`读取行()` 从标准输入读一行（不含换行）；`读取整数()`/`读取浮点()` 读数值（strtoll/strtod 整行解析，成功标志经整32* 输出）；`打印到错误` stderr 输出。

> **✅ 实施记录（2026-08-14，第4步子任务）**：已完成 IO 输入（Task 6.2 输入部分）+ 文件读写（Task 6.8）全链路：
> - **IO 输入**：`input_api.cpp`（__cn_read_line 动态分配 UTF-8 不含换行/EOF 返回 nullptr；__cn_read_int strtoll 整行解析 + 整32* 成功标志；__cn_read_float strtod；__cn_print_err fprintf stderr 不换行）
> - **文件 API**：`file_api.cpp`（__cn_file_open 模式 1=读/2=写截断/3=追加；__cn_file_read fread；__cn_file_write fwrite；__cn_file_read_line fgets 动态分配含换行/EOF nullptr；__cn_file_size fseek end+ftell+恢复；__cn_file_close fclose；__cn_file_exists 试读）；**中文路径 UTF-8 → MultiByteToWideChar(CP_UTF8) + _wfopen_s**（lessons.md 已验证方案）
> - **stdlib 模块**：`IO.cn`（读取行/读取整数/读取浮点 → 结果<T,整32> 包装，错误码.文件=5；打印到错误）+ `文件.cn`（打开/读取/写入/读取文件行/大小/关闭/存在，句柄=空类型*）
> - **语义注册 + IR 映射（五处同步）**：registerBuiltins 注册 `IO.读取行`/`IO.读取整数`/`IO.读取浮点`/`IO.打印到错误` + `文件.打开文件` 等 7 个（带 `IO.`/`文件.` 前缀限定名，数学库同模式）；ir.cpp 名称映射（11 处）+ 结果类型映射（读取行→ptr、读取整数→i64、读取浮点→f64、打开文件→ptr、读写/大小→i64、读取文件行→ptr、文件存在→i1）
> - **运行时五处同步**：runtime.hpp 声明 + CMakeLists cn_runtime 源文件 + cn_main.cpp compileRuntime 源文件数组 + linkExe .obj/.o 列表（x64/arm64）
> - **编译器缺陷修复（本轮）**：语义层比较运算 `字符串/字符*` 与 `空类型*`（无）比较被误拒——字符串本质是 char*，读取行 EOF 返回 nullptr 判定必须 `行 == 无`；修复：isPointerType 判断补充 字符串/字符*，指针间比较按地址放行
> - **E2E stdin 注入**：run_e2e.py 扩展——同名 `.input` 文件存在时经 subprocess input 参数注入（33_io_input 验证 读取行/读取整数/读取浮点 + EOF 语义）
> - **实测边界**：E2E 直接调用内置限定名（`IO.读取行` 等，25_math 数学库同模式）——stdlib 模块公开函数与内置限定名在"模块已导入"时冲突（用户模块函数优先），模块体内 `IO.xxx` 走内置；**变量名避免以类型关键字开头**（`整数成功` 词法拆分为 关键字+标识符 报错）
> - 验证：单测 **1005/1005**（987 前序 + 18 新增）、E2E **36/36**（34 前序 + 33_io_input + 34_file）、编译 0 警告（MSVC /W4 /WX）

### Task 6.3: 数学库 ✅ 已完成（2026-08-14）
**文件：**
- Create: `stdlib/数学.cn`（浮点运算辅助/128位整数运算辅助/常用数学函数，规格书10.5）
- Modify: `src/runtime/math_api.cpp`（`__cn_mul_i128`、`__cn_div_i128`、`__cn_div_u128` 等 + `数学.平方根`/`数学.幂`/`数学.正弦`/`数学.余弦`/`数学.正切`/`数学.绝对值`/`数学.向上取整`/`数学.向下取整` 运行时封装，对应 `__cn_sqrt/pow/sin/cos/tan/fabs/ceil/floor`）
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册数学 API）
- Create: `tests/unit/runtime/test_math_api.cpp`

**验证标准：** 128 位整数运算辅助正确；`数学.平方根(16.0) == 4.0`、`数学.幂(2, 10) == 1024`、`数学.绝对值(-5) == 5`、三角函数精度达标。

> **✅ 实施记录（2026-08-14，第1步子任务）**：已完成 8 个数学函数（平方根/幂/正弦/余弦/正切/绝对值/向上取整/向下取整）全链路：
> - 运行时 `src/runtime/math_api.cpp`（`__cn_sqrt/pow/sin/cos/tan/fabs/ceil/floor`，封装 `<cmath>`，英文 API）
> - `runtime.hpp` 声明 + CMake `cn_runtime` 加入源文件 + `cn_main.cpp` 运行时 .obj 编译/链接清单加入 `math_api`（两处：compileRuntime 源文件数组 + linkExe .obj 列表，x64/arm64 同步）
> - `semantic.cpp` registerBuiltins 注册 8 个中文限定名内置函数（`数学.平方根` 等，key 带 `数学.` 前缀防与用户模块公开函数纯名冲突）；visitCallExpr 模块限定重写特判——用户模块公开函数优先，无同名用户函数时保留限定名走内置路径（无需导入）
> - `ir.cpp` 名称映射（`数学.平方根`→`__cn_sqrt` 等 8 处）+ 结果类型映射（8 函数结果类型 f64）
> - `stdlib/数学.cn` 新建：公开 8 个中文函数（平方根/幂/正弦/余弦/正切/绝对值/向上取整/向下取整），体内调用内置 `数学.xxx`
> - 单测 `tests/unit/runtime/test_math_api.cpp`（9 用例）；E2E `tests/e2e/25_math/主.cn`（11 行输出，直接内置直调）
> - 设计决策：内置名 `数学.平方根` 与模块名 `数学` 协调——模块函数与内置同名共存时以用户模块函数优先（stdlib/数学.cn 体内直接 `数学.平方根(值)` 会触发用户函数自递归，故标准库模块仅供导入后使用 `从 数学 导入 平方根` 或 `数学.平方根`（无同名用户函数时走内置））
> - 验证：单测 987/987、E2E 30/30 全通过、编译 0 警告

### Task 6.4: 集合库 ✅ 已完成（2026-08-15）
**文件：**
- Create: `stdlib/映射集合.cn`（映射<K,V> 线性表 + 集合<T>，规格书4.4「表.包含(键)」）✅
- Create: `tests/e2e/31_map_set/`（映射集合库 E2E：主.cn + 映射集合.cn + 主.expected）✅

> **✅ 实施记录（2026-08-15，第3步子任务）**：已完成 映射<K,V> + 集合<T>：
> - **映射<K,V>**：K* 键数组 + V* 值数组 平行线性表（O(n)，P0 不做哈希）；设置（键存在→覆盖值/不存在→追加扩容翻倍 0→4→8）/ 获取（找不到→错误码.未初始化=7）/ 包含（规格书4.4）/ 删除（前移）/ 清空 / 大小 / 为空 / RAII 析构；键比较须支持 ==（P0 数值/字符/枚举键，字符串键 P1，规格书10.3 字符串比较运算符未定义）
> - **集合<T>**：独立 T* 数据数组（值唯一，添加幂等），不组合 映射<T,布尔> 字段（规避嵌套泛型缺陷）
> - **编译器缺陷规避（Task 6.4 实测）**：~~泛型类方法体内「循环遍历第一个泛型字段」运行时损坏（N/P/Y/AC/AG 实验复现垃圾值）~~ **✅ 已修复（2026-08-15 Debug 子任务）**：IR 层 emitClassMethod 为泛型实例化类设置 genericTypeParams_（T->实参）——原缺失导致 `T 总和` 局部变量映射为 ptr、`总和+数据[索引]` 被当指针算术（值左移3当地址）；同时语义层 resolveGenericTypeName 补裸类型参数 T/T* 替换。回归 E2E 42_generic_field_loop（循环遍历求和 + 打印变参内嵌字段下标，原规避方案可取消）
> - 验证：单测 987/987、E2E 34/34 全通过、编译 0 警告

### Task 6.5: 字符串处理库（含格式化）✅ 已完成（2026-08-15）
**文件：**
- Create: `stdlib/字符串扩展.cn`（字符串转整数/转浮点/转布尔/替换/填充左/填充右/分割，对标 C++ string 解析能力）✅
- Modify: `src/runtime/string_api.cpp`（`__cn_str_to_int`/`__cn_str_to_double`/`__cn_str_to_bool` 解析函数，strtoll/strtod 整串解析 + 整32* 成功标志）✅
- Create: `tests/e2e/35_string_ext/`（字符串扩展 E2E：主.cn + 字符串扩展.cn + 主.expected）✅
- Create: `tests/unit/runtime/test_string_parse_api.cpp`（24 用例）✅

**验证标准：** `格式化("值%d 浮点%f", 42, 3.14)` 正确；`布尔转字符串(真)` 返回 `"真"`；`正数转字符串(正64)` 正确转换无符号整数；格式化返回字符串由调用方释放。

> **✅ 实施记录（2026-08-15，第5步子任务）**：见 Task 6.9/6.10 实施记录（字符串扩展 + 时间 + 系统同轮完成，五处同步/命名冲突解决/平台差异详见该节）。

### Task 6.9: 时间库 ✅ 已完成（2026-08-15）
**文件：**
- Create: `stdlib/时间.cn`（当前时间戳/单调时钟毫秒/格式化时间，对标 C++ chrono/ctime）✅
- Create: `src/runtime/time_api.cpp`（`__cn_time`/`__cn_clock_ms`/`__cn_time_format`，Windows QueryPerformanceCounter/localtime_s + POSIX clock_gettime/localtime_r）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册 `时间.当前时间戳` 等）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（`时间.xxx` → `__cn_time` 等名称映射 + 结果类型映射）✅
- Modify: `src/runtime/runtime.hpp` + `CMakeLists.txt`/`src/cn_main.cpp`（声明 + 两处同步）✅
- Create: `tests/e2e/36_time/`（时间 E2E：内部断言输出 真/假，规避固定时间文本比对）✅
- Create: `tests/unit/runtime/test_time_api.cpp`（9 用例）✅

**验证标准：** 当前时间戳>0 且两次调用递增；单调时钟毫秒两次调用递增；格式化时间 "%Y-%m-%d" 长度10 且第4/7位为 '-'；非法时间戳返回 nullptr。

### Task 6.10: 系统库 ✅ 已完成（2026-08-15）
**文件：**
- Create: `stdlib/系统.cn`（参数个数/参数，对标 C++ argv，规格书10.4）✅
- Create: `src/runtime/system_api.cpp`（`__cn_argc`/`__cn_argv`，Windows CommandLineToArgvW Unicode 命令行解析 + UTF-8 转换缓存）✅
- Modify: `src/runtime/runtime.cpp`（entry 入口缓存 argc/argv；WinMain 转发 CRT 全局参数）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册 `系统.参数个数` 等）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（`系统.xxx` → `__cn_argc` 等名称映射 + 结果类型映射）✅
- Modify: `src/runtime/runtime.hpp` + `CMakeLists.txt`/`src/cn_main.cpp`（声明 + 两处同步 + shell32 链接）✅
- Modify: `tests/e2e/run_e2e.py`（支持 `.args` 文件注入命令行参数，与 .input 同模式）✅
- Create: `tests/e2e/37_system/`（系统 E2E：主.cn + 主.args + 主.expected，中文参数 UTF-8 验证）✅
- Create: `tests/unit/runtime/test_system_api.cpp`（10 用例）✅

**验证标准：** 参数个数含可执行文件名本身（至少 1）；参数按索引取 argv（argv[0]=exe 名）；越界返回 nullptr；中文参数 UTF-8 无乱码。

> **✅ 实施记录（2026-08-15，第5步子任务：字符串扩展 + 时间 + 系统）**：
> - **字符串扩展（Task 6.5 字符串处理库补充）**：运行时 string_api.cpp 新增 3 个解析函数 `__cn_str_to_int`/`__cn_str_to_double`/`__cn_str_to_bool`（strtoll/strtod 整串解析 + 整32* 成功标志，IO 库同模式）；**命名冲突解决**：内置注册为 `解析.转整数`/`解析.转浮点`/`解析.转布尔`（带 `解析.` 前缀限定名）——**不能用 `字符串.` 前缀**（`字符串` 是类型关键字 Kw_String，词法器将 `字符串.转整数` 拆为 关键字+标识符 报"预期表达式"）；stdlib/字符串扩展.cn 公开纯名 `字符串转整数` 等与内置限定名不冲突
> - **stdlib/字符串扩展.cn**：`字符串转整数/转浮点/转布尔`（结果<T,整32> 包装，失败 错误(6)）+ `替换`（循环 字符串查找+子串+连接，目标不存在返回原样副本，安全计数器防无限循环）+ `填充左/右`（字符转字符串 + 循环连接）+ `分割`（字符串* 数组 + 数组长度，返回数量，数组不足 错误(6)——CN 数组长度须编译期常量故用 C 风格数组参数）；**注意**：`结果` 是关键字（Kw_Result）不可作变量名，局部变量用 `缓冲`
> - **时间（Task 6.9）**：time_api.cpp 新增 `__cn_time`（time(nullptr) 秒级）/`__cn_clock_ms`（Windows QueryPerformanceCounter / POSIX clock_gettime(CLOCK_MONOTONIC) 单调时钟毫秒）/`__cn_time_format`（localtime_s/localtime_r + strftime，失败 nullptr，动态分配）；stdlib/时间.cn 公开 3 函数（格式化时间 结果<字符串,整32> 包装，失败 错误(6)）
> - **系统（Task 6.10）**：system_api.cpp 新增 `__cn_argc`/`__cn_argv`（全局缓存 argc/argv）；**平台差异坑**：① Windows `/ENTRY:WinMainCRTStartup` 下 WinMain 形参无 argc/argv（第4参是 nCmdShow），原 entry(0, nullptr) 导致 argc 恒 0——改转发 MSVC CRT 全局 `__argc`/`__argv`；② `__argv` 是 ANSI（GBK 代码页）中文参数乱码、`__wargv` 仅在 wmain 入口初始化（WinMain 下 nullptr）——改 `GetCommandLineW` + `CommandLineToArgvW` 从进程原始命令行解析，`WideCharToMultiByte(CP_UTF8)` 转 UTF-8 缓存（依赖 shell32.lib，cn_main.cpp linkExe 与 CMake 同步补充）；stdlib/系统.cn 公开 2 函数（参数 越界 错误(6)）
> - **E2E 运行器扩展（37_system）**：run_e2e.py 支持同名 `.args` 文件（每行一个参数，首行=argv[1]，argv[0]=exe 名）注入命令行参数，与 .input stdin 注入同模式——37_system 完整验证 参数个数=3 + 中文参数 CN语言 UTF-8 无乱码
> - **E2E 固定输出处理（36_time）**：时间戳/日期内容随运行时刻变化——E2E 程序内部断言（时间戳递增、格式化时间长度=10、分隔符 '-'、年份 4 位数字），只输出 真/假（1/0）布尔结果；字符串分隔符比较用 `字符串比较` 内置（字符串 == 运算符规格书未定义）
> - **验证**：单测 **1048/1048**（1005 前序 + 43 新增：字符串解析24 + 时间9 + 系统10）、E2E **39/39**（36 前序 + 35_string_ext + 36_time + 37_system）、编译 0 警告（MSVC /W4 /WX）

### Task 6.6: 条件编译与预处理 ✅ 已完成（2026-08-14）

> **背景**：对标 C/C++ 预处理器的条件编译能力，支撑跨平台与调试/发布分支。

**文件：**
- Create: `src/cn_compiler/lexer/preprocessor.hpp/cpp`（条件编译预处理器：逐行裁剪，保留行号；条件栈支持嵌套）✅
- Modify: `src/cn_compiler/lexer/lexer.cpp`（tokenize 先经 Preprocessor 裁剪再切分；支持命令行 `-D 宏名` 注入）✅
- Modify: `src/cn_main.cpp`（CLI 新增 `-D` 选项；DriverOptions.macros 透传）✅
- Modify: `src/cn_compiler/driver/`、`src/cn_compiler/module/module.cpp`（宏集合传参链路）✅
- Create: `tests/unit/lexer/test_preprocessor.cpp`（13 用例：分支选择/嵌套/行号保留/错误诊断/CRLF）✅
- Create: `tests/e2e/24_preprocessor/条件编译.cn` + `条件编译.expected` ✅

**验证标准：** `#如果定义(调试)` 分支按宏定义裁剪；`#否则`/`#结束如果` 正确嵌套；未定义宏的块被跳过（不产生语法错误）。

> **✅ 实施记录（2026-08-14，第1步子任务）**：
> - 指令集：`#定义 宏名` / `#如果定义(宏名)`（或 `#如果定义 宏名`）/ `#否则` / `#结束如果`
> - 设计：行级裁剪（不活跃行替换为空白保留换行 → 词法/语法/IR 行号与源码一致）；条件栈（parentActive/branchTaken/active）支持任意嵌套；父层不活跃时内层 `#定义` 不生效（C 语义）
> - 宏来源：命令行 `-D 宏名`（可多次，经 CliOptions→DriverOptions→Lexer 传递）+ 源码内 `#定义`
> - 修复：① 指令行前导空白缩进；② 中文字节长度常量比较（"如果定义"=12字节）；③ **CRLF 行尾宏名带 `\r`**（Windows 源文件 `\r\n`，宏名收集终止集合补 `\r`）——已由 `-D DEBUG` 实测验证
> - 验证：单测 987/987、E2E 30/30 全通过、编译 0 警告

### Task 6.7: 算法库 ✅ 已完成（2026-08-15）
**文件：**
- Create: `stdlib/算法.cn`（排序/线性查找/二分查找/逆序/变换/计数/最大元素，对标 C++ <algorithm>）✅
- Create: `tests/e2e/32_algorithm/`（算法库 E2E：主.cn + 算法.cn + 主.expected）✅

**验证标准：** 整32数组排序 {5,2,8,1,9,3} → 升序 {1,2,3,5,8,9}；线性查找 找到索引/找不到 -1；二分查找 升序数组查目标；逆序 {1,2,3} → {3,2,1}；变换 ×2；计数出现次数；最大元素。

> **✅ 实施记录（2026-08-15，第3步子任务）**：
> - **算法.cn**：`排序整32/排序整64`（冒泡升序，比较内联）、泛型 `线性查找<T>`（找到→索引，找不到→-1）、`二分查找整32/整64`（前提：数组已升序）、泛型 `逆序<T>`、`变换整32/变换整64`（原地 ×2）、泛型 `计数<T>`、泛型 `最大元素<T>`
> - **技术风险点实测结论（泛型+函数指针回调）**：~~`泛型 <类型 T> 函数 排序(T* 数组, 整64 长度, 整32(*比较)(T, T))` 编译失败——单态化后签名不匹配~~ **✅ 已修复（2026-08-15 Debug 子任务）**：根因是 substTypeParam/substGenericType 的模板分支丢弃 `<...>` 之后的后缀（函数指针参数列表 `(T,T)`），实例化签名缺函数指针参数（`函数指针<整32>` vs `函数指针<整32>(整32,整32)`）；修复保留后缀并递归替换内嵌类型参数。回归 E2E 40_generic_funptr（泛型冒泡排序 + 函数指针回调升序）。原 P0 降级方案（具体类型重载）可保留作为性能选择
> - **映射集合.cn（Task 6.4）**：映射<K,V>（K*+V* 平行线性表 O(n)，设置覆盖/追加扩容翻倍/获取/包含/删除前移/清空/RAII）+ 集合<T>（独立 T* 数组，添加幂等）；~~**泛型类方法内循环遍历第一个泛型字段运行时损坏**（实测）~~ **✅ 已修复（2026-08-15 Debug 子任务）**——IR emitClassMethod 设 genericTypeParams_ + 语义 resolveGenericTypeName 裸参数替换，原规避（循环逻辑放模块级泛型自由函数）可取消
> - 验证：单测 987/987、E2E 34/34 全通过、编译 0 警告

### Task 6.8: 文件库 ✅ 已完成（2026-08-14）
**文件：**
- Create: `stdlib/文件.cn`（打开/读取/写入/读取文件行/大小/关闭/存在，对标 C++ fstream）✅
- Create: `src/runtime/file_api.cpp`（`__cn_file_open`/`__cn_file_read`/`__cn_file_write`/`__cn_file_read_line`/`__cn_file_size`/`__cn_file_close`/`__cn_file_exists`，中文路径 `MultiByteToWideChar(CP_UTF8)` + `_wfopen_s`）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册 `文件.打开文件` 等 7 个 API）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（`文件.xxx` → `__cn_file_*` 名称映射 + 结果类型映射）✅
- Modify: `src/runtime/runtime.hpp`（声明文件 API）+ `CMakeLists.txt`/`src/cn_main.cpp`（cn_runtime 与 .obj 清单两处同步）✅
- Create: `tests/e2e/34_file/`（文件读写 E2E：主.cn + 主.expected，中文文件名 `target/测试文件.txt` 验证中文路径）✅
- Create: `tests/unit/runtime/test_file_api.cpp`（7 用例：写读/大小/原始读取/追加/中文文件名/打开失败/非法模式）✅

**验证标准：** 打开（写模式2 截断）→ 写入中文多行 → 关闭 → 文件存在=真；打开（读模式1）→ 按行读取 → 文件大小（UTF-8 字节数）正确；中文文件名路径读写通过（`_wfopen_s` 宽路径）。

> **✅ 实施记录（2026-08-14，第4步子任务）**：见 Task 6.2 实施记录（IO 输入 + 文件读写同轮完成，五处同步/中文路径/stdin 注入/编译器缺陷修复详见该节）。

**阶段六完成条件：** 全部标准库E2E通过 + 更新 `plans/002` 阶段六打勾 + Git提交。
> **增量增强（2026-08-13）**：Task 6.2/6.3/6.5 补充输入 API、数学函数、格式化；新增 Task 6.6 条件编译。
> **增量增强（2026-08-15）**：新增 Task 6.7 算法库（排序/查找/逆序/变换/计数/最大元素）。
> **增量增强（2026-08-14）**：新增 Task 6.8 文件库（打开/读取/写入/按行/大小/关闭/存在，中文路径 _wfopen_s）。

### Task 6.11: 全链路集成串联（综合命令行工具）✅ 已完成（2026-08-15）
**文件：**
- Create: `tests/e2e/38_tool/`（学生成绩统计工具 E2E：主.cn + 成绩数据.txt + 主.args + 主.expected）✅
- Create: `tests/e2e/38_tool/容器.cn`/`映射集合.cn`/`算法.cn`/`字符串扩展.cn`（stdlib 模块副本，与 35_string_ext 同模式）✅

**验证标准：** 综合命令行工具 E2E（38_tool）通过，证明 容器/映射集合/算法/数学/IO/文件/字符串扩展/时间/系统 全部串联可用；全量回归通过（单测 1048/1048、E2E 40/40）；编译零警告。

> **✅ 实施记录（2026-08-15，第6步子任务）**：已完成 全链路集成串联——学生成绩统计工具：
> - **工具功能**：命令行参数校验（系统.参数个数 ≥3 → 用法提示退出）→ 打开数据文件（文件.打开文件 模式1）→ 逐行读取（文件.读取文件行）→ 分割/解析（字符串扩展.分割 + 解析.转整数，非法行跳过计数）→ 存储（向量<整64>/集合<整64>/映射<整64,整64>）→ 统计（算法.排序整64/最大元素/计数/二分查找/线性查找 + 数学.平方根 标准差）→ 时间戳断言（时间.当前时间戳/格式化时间）→ 报告写入（文件.打开文件 模式2 + 整数转字符串）→ 读回验证（文件.读取文件行）
> - **测试数据**：5 行有效（张三240/李四240/王五55/赵六180/周八270）+ 1 行非法（孙七:abc）→ 平均=197 最高=270 最低=55 及格=4 集合去重=4 映射分布(240)=2 二分查找240=2 标准差=76.785415
> - **新发现编译器缺陷（⚠️ 已规避，留待 Debug 子任务修复）**：~~**大栈帧无 __chkstk 栈探测崩溃**~~ **✅ 已修复（2026-08-15 Debug 子任务）**：codegen `emitPrologue` 栈帧 >4KB 时发射 MSVC 三段式 `mov rax, N / call __chkstk / sub rsp, rax`（__chkstk 按 4KB 页探测提交栈空间，符号由 libcmt.lib 提供），`EXTERN __chkstk:PROC` 声明 + `emitParamSetup` 提前到 chkstk 之前（chkstk 破坏 rcx 参数寄存器）。回归：单测 BigFrameEmitsChkstk/SmallFrameNoChkstk + E2E 39_chkstk（1200 局部变量 >9KB 栈帧，修复前崩溃修复后正常）。**原规避方案（拆函数 <8KB）可取消**
> - **已知边界（复用）**：跨模块重复声明 `错误码` 枚举冲突（容器.cn + 映射集合.cn 同目录导入）→ 用例目录副本改 导入 容器.错误码 共享；`结果` 关键字不可作变量名（统计结果 变量用 `统计`）；模块级变量/泛型类参数不支持 → 数据经 数组指针参数 传递；结构体按值返回/传参正常（统计结果 40 字节）
> - 验证：单测 **1048/1048**、E2E **40/40**（39 前序 + 38_tool）、编译 0 警告（MSVC /W4 /WX，cn build 路径）

### Task 6.12: Debug 全面审查（编译器缺陷修复）✅ 已完成（2026-08-15）
**文件：**
- Create: `tests/e2e/39_chkstk/`（大栈帧 __chkstk 栈探测回归：1200 局部变量 >9KB 栈帧）✅
- Create: `tests/e2e/40_generic_funptr/`（泛型函数 + 函数指针回调回归：泛型冒泡排序）✅
- Create: `tests/e2e/41_ctor_overload/`（构造函数重载回归：无参/带参分别实例化）✅
- Create: `tests/e2e/42_generic_field_loop/`（泛型类方法循环遍历泛型字段 + 打印变参内嵌字段）✅
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（__chkstk 栈探测三段式 + emitParamSetup 提前）✅
- Modify: `src/cn_compiler/semantic/generics.cpp`（substTypeParam 保留函数指针后缀 + resolveGenericTypeName 裸参数替换 + 泛型类构造 sigKey key）✅
- Modify: `src/cn_compiler/ir/ir_generic_func.cpp`（substGenericType 保留函数指针后缀）✅
- Modify: `src/cn_compiler/semantic/class_resolver.cpp`（构造/析构 methods 表 sigKey key + 查重按 sigKey）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（构造查找按实参匹配 + findGeneric public）✅
- Modify: `src/cn_compiler/ir/ir_oop_call.cpp`（构造查找按 resolvedSignature/实参个数）✅
- Modify: `src/cn_compiler/ir/ir_oop.cpp`（emitClassMethod 设置 genericTypeParams_）✅

> **✅ 实施记录（2026-08-15，Debug 子任务）**：4 项前序记录缺陷全部根治：
> 1. **大栈帧 __chkstk 栈探测崩溃（高）**：`emitPrologue` 栈帧 >4KB 发射 MSVC 三段式（mov rax,N / call __chkstk / sub rsp,rax），EXTERN __chkstk 声明，emitParamSetup 提前到 chkstk 前（chkstk 破坏 rcx）——E2E 39_chkstk 1200 变量 >9KB 栈帧正常运行
> 2. **泛型 + 函数指针回调**：substTypeParam/substGenericType 模板分支丢弃 `<...>` 后函数指针参数列表后缀 `(T,T)` → 保留后缀并递归替换；实例化签名补全函数指针参数——E2E 40_generic_funptr 泛型排序回调升序正确
> 3. **构造函数重载覆盖**：构造/析构 methods 表 key 改为 sigKey（名#参数串），语义/IR 构造查找按实参个数+类型匹配最优——E2E 41_ctor_overload 无参/带参分别实例化正确
> 4. **泛型类方法循环遍历泛型字段**：IR emitClassMethod 为泛型实例化类设置 genericTypeParams_（T->实参），语义 resolveGenericTypeName 补裸参数 T/T* 替换——E2E 42_generic_field_loop 循环求和 + 打印变参内嵌字段全部正确
> - 验证：单测 **1050/1050**（1048 + 2 新增 __chkstk）、E2E **44/44**（40 + 39~42）、编译 0 警告（MSVC /W4 /WX）
