# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 256 轮（2026-09-16，单位机 ARM64）——256-a 收工回填：F1-26 方案 A 第一步落地（Phi 降级+使用点重写+验证器升级·单测 4/4·384 双形态）；下一轮预登记 257-a。

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **449-a（开发轮·🏃 本机并行基建·分支 任务/家机-449-本机并行基建·与 447 集成轮同机双开试点〔主树 447 集成+wt449 本轮〕）** | worktree 任务池制度化（wt.py 生命周期+两级深度校验）+gate_lock 全量门禁串行锁+build.ps1 --parallel+sccache 本地编译缓存（纯注入零入库改动）+看板多任务行适配+AGENTS §8.8 本机多开规约 | 见 §1.0 实施计划（449-a） | 同机 447 集成轮=看板/025/交接 分区共存零代码交叠（双开试点对象）；深度机 448-D42 波3 续、单位机 329-D12 均零交叠（本轮写集不触 src/tests 运行面） | §1.0 | 🏃 在飞 |
| 深度机 linux-x86_64 | **🧪 314-a M3 采样轮·x64l 列** | 第五新种子域 310000001 差分 500+负向 400（310100001）+回归轨 114 全库+T44 x64l 跨机复验（t44scan/ext/if2）+日志日文件制首写 | 看板本机行、plans/025 本节、HANDOFF/更新日志深度机节、tests/cnsmith_hits/日志/2026-09-17.md（新）、命中样本（如有）、plans/021（T44 行证据回填·最小局部） | 家机 313-b 🏃（daemon/日志拆日写集）互认零碰；日志日文件与其拆日同向；单位机 ⬜ 空闲 | §2.1 | 🧪 在飞 |
| 单位机 ARM64 | **328-a（开发轮·已收工·分支 任务/单位机-328-协议v2 已集成 e6e22aa）** | 三机并行协同协议 v2 落地（用户批准）：分支即开发+integrate.py 队列集成+缺陷分级+分层 try-build+文档随分支走——AGENTS §8 重写/plans/027 方案/check_handoff 扩分支名校验 | 见 §3.18 收工回填 | 家机 326-a 收工与本案真实碰撞一次（rebase 消解=v2 首个实战案例）；深度机 ⬜ 零交叠 | §三.18 | ✅ 收工 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **258-a**（下一轮候选·待认领） | ①**A12 里程碑标签锚点**（B6 已批·轻量可搭车）②F1-26 后续子项（激进 SSA 管线化/mem2reg 全量·性能主线）③F2-33 拆分之三 迭代器 ④D28/D29 根治专项；257-a 已收工（§3-B 铁律根治） | 计划=开工时落盘本表 |
| 家机 win-x64 | **450-a（下一轮候选·待认领）** | 依 plans/021 §3-A 队列：M3 win 列采样（用户令不停）/清零波次余项/C20 批C·D parse 桶上架——开工以看板最新为准 | plans/021 排班+看板 |
| 深度机 linux-x86_64 | **414-a（下一轮·D42 甲案波 1）** | **泛型类源码编译全链·波1=解析+语义层**（2026-09-19 用户裁决「同意甲」·plans/021 §三-A D42甲行）：①v2 解析器 `类 名<T>` 类型参数表（解析类型文本/泛型闭合位置 既有 P7b 泛型函数设施复用）；②实例类型合成名（名$T$…·与泛型函数同款）；③语义检查类表挂接；④探针=v2p 编译 含 `类 盒子<T>` 源码不崩+实例声明语义通过。波2（后续轮）=IR/生成单态化；波3=stdlib 源码加载路线+390 根治 | 395-a 破案=plans/025 §2.46·呈报三案=plans/021 B2（已迁 §三-A） |

---

## 一、家机 win-x64 节（本机维护）

### 1.0 实施计划（**453-a 集成轮·🏃 437 拆分集成：src r10d 修复+断言迁移 cherry-pick——清 406/431 win 基线红解锁 450**·基线 0d9a6259·2026-09-20·分支 任务/家机-453-437拆分集成·wt453）

**1. 目标与验收**：435 链集成的 win 门禁基线红 406/431（A2070 族·T43+T67）阻塞 450/447 集成链；437-a 修复在 447 分支（挂起等 448）——**拆分集成**：437 的 src 两文件（447 ASCII 修复版=paramSlot）+vtable 断言迁移单独成轮先行集成（§8.4 机制级·437 内容=深度机已签 4fe54df 组成部分）。**验收**=X64VtableTest 6/6+406/431 单跑双转绿+全量门禁零新增红（预期全绿）。
**2. 写集（实测）**：`src/cn_compiler/codegen/x64/{x64_codegen_oop.cpp,x64_instructions.cpp}`（447 版=437+paramSlot）｜`tests/unit/codegen/test_x64_vtable.cpp`（447 版=断言迁移）｜025 本节+看板。**零 v2 树**（与深度机 448-b 零交叠）。
**3. 实施步骤**：①checkout 447 版三文件→②构建+单测+406/431 单跑验证→③push→④integrate --try-build-done（437 内容=深度机已签链尾 4fe54df 组成·回签有效）。
**4. 验证链（实测）**：X64VtableTest 6/6 ✓+406 PASS ✓+431 PASS ✓（wt453 全新构建）。
**5. 边缘**：447 分支本体保持挂起（112_v2 红=独立面·归 448-b）；本拆分不预判 435 链其他潜在红（wt-base 硬证据=仅 406/431 两红）。
**6. 交叠**：深度机 448-b（v2 树 IR类.cn）零交叠；449（scripts）零交叠；450（tests 上架）零交叠。**集成后动作**：450 解锁（立即集成）→447 待 448-b。

### 1.0 收工回填（**450-a 开发轮·✅ C20 批4：防线对齐桶 11 例 v2 闭环负测上架**·基线 7510b60a·2026-09-20·分支 任务/家机-450-C20批4·wt450 隔离）

**1. 目标与验收（回填实测·全达成）**：①扫描重建（target/p450/ 不入库）：静态筛 116→cn check 实测宿主拒 111→v2p 四桶（sem5 58/parse 45/过 8）；②甄别=已上架 36 号（D38 批1/2/3+C20 批1/2/3）+develop 孪生 _v2 目录+v2 用例本体+T63 桶 13（误导缺分号）+T64 桶 5（过桶）排除→待看 14→完整输出逐一甄别→**上架 11 例**（135/159/163/265/275/279/319/326/356/82/83_v2）+**不固 4 例定性**（101=v2 类继承缺口拒〔D40 族〕/339=v2 运算符重载缺口拒〔349-a 已定性〕/312=ir2 容器方法收录〔**435 链区·归 448 轮**〕/279 保留上架）；③11/11 单跑 PASS+反证抽例（265 配置改 0→FAIL→还原→PASS）。
**2. 写集（实测）**：`tests/e2e/<号>_v2_<主体名>` 11 新目录｜`tests/e2e/coverage_map.md`（+11 矩阵行）｜plans/021（C20 戳 450+L395 历史行压缩超长修复）｜plans/025 本节｜交接/更新日志本机节｜看板。**零 src/ 零 v2 树零 runner**。
**3. 定位方法学（回填）**：①上架集甄别口径=「v2 诊断准确且同形态拒」（v2 特性缺口拒≠防线对齐——101/339 不固）；②多文件用例全拷（159 工具库/163 工具件/82·83 模块甲）；③固化=runner 同款相对路径（target/audit2/v2src<号>/主.cn）+「宿主同构=」新字段；④新 worktree 跑 v2 闭环须先备运行时 obj 十件（target/*.obj——从同源构建树拷贝或构建）。
**4. 验证链（回填实测）**：11/11 单跑 PASS+反证抽例闭环+coverage_map 同步。全量 E2E 终态随 try-build 通告回填（预期基线红=406/431〔435 链 win 基线·449 wt-base 硬证据〕零新增）。
**5. 边缘与诚实边界（回填）**：①扫描基点=develop（无九链/批1-3 用例）——批4 上架面与 431 预登记「21 例」口径不同源（431 扫描基于其分支树·两基线的 v2 行为差异=435 链改动）——如实按本扫描定稿 11 例；②027/280 等孪生面已排除不重复锚。
**6. 与他机交叠（回填）**：零交叠维持（深度机 448=v2 树 IR类.cn·本批零 v2 树）。**收工态**：try-build 请求已发（写集触 tests/**）→挂起等回签；**集成序=448 收口→447（九链含 437 清 406/431 基线红）→450**（450 门禁须在 develop 恒绿后跑·否则基线红阻塞）。

### 1.0 实施计划（**450-a 开发轮·🏃 C20 批4：候选池余 21 例 v2 闭环负测上架（431-a 预登记①续·波次8）**·基线 7510b60a·2026-09-20·分支 任务/家机-450-C20批4·wt450 隔离）

**1. 目标与验收**：按 431-a 收工回填预登记①「批4=候选池余 21 例」——重建 356-a 式定性扫描（431 扫描设施 target/p431/ 不入库·本机重建）→四桶矩阵复核→甄别「诊断准确」的防线对齐面→v2 闭环负测上架（414_v2/424~431 批次先例模式）。**验收**=上架 N 例单跑全 PASS+反证抽例+coverage_map 矩阵行+全量 E2E 零新增红+单测全过。
**2. 写集（预判）**：`tests/e2e/<宿主号>_v2_<主体名>/` 新用例目录 N 个｜`tests/e2e/coverage_map.md` 矩阵行｜plans/021（C20 行批4+戳）｜plans/025 本节+交接/更新日志本机节+看板。**零 src/ 零 v2 树零 runner**（纯用例上架轮）。
**3. 实施步骤**：①静态筛负测面（期望编译失败.txt/expected 诊断锚）→②cn check 逐例实测确认（rc≠0）→③v2p 同源编译聚类四桶（sem5/parse/特殊+302/过）→④对齐 431 批3 已上架 12 例+T63（11 例误导诊断不下架原则）+T64（9 例过桶）排除→⑤上架集逐例固化（runner 同款 v2src<编号>_v2 环境·expected 实跑固化）→⑥门禁+try-build 请求。
**4. 验证链**：N 例单跑 PASS+反证抽例（配置改 0→FAIL→还原→PASS）+全量 E2E 零新增红+单测+自举链（78_v2/79_v2）。
**5. 边缘与诚实边界（预判）**：①扫描基点=develop（无九链）——112_v2 等 431 用例不在本树（447 挂起待 448）——批4 候选池口径按 431 回填清单执行·若实测候选≠21 例如实回填；②误导诊断例（T63 桶）不固化；③v2p 首建≈3min。
**6. 与他机交叠**：深度机 448（IR类.cn 收口·v2 树）零交叠（本轮零 v2 树）；449（scripts 基建）零交叠；447（挂起·tests 面）——本批用例号段与 431 批3 已上架面（136/140/112/274/311/313/338/353/90/210/179/162_v2）+424 批1（453~464_v2）+429 批2（471~482_v2）零重叠（按 v2闭环.txt 宿主同源负测字段排除）。


**收工回填（451-a·实测）**：①**冗余 mov 88637→35 条（-99.96%·291 用例同口径统计·编译零失败）**——11 发射方法结果写入点直写分配寄存器（ConstLoad 整型/布尔/字符串/FuncAddr·Copy 整型·IntBinary 目标/源分离·DivMod·Shift·Cast 六个 64 位分支〔浮→整64/位重解释/i1→64/sxtw/u32 零扩展/窄扩展〕·LoadStore Load·AddrOf·FieldAddr 全链（装载+空指针检查+偏移加算）·PtrLoad 64 位）+`resultTargetReg` 辅助；②**首轮遗漏两漏点实测补齐**（统计 1696 阶段捕获）：emitShift（lsl x9+mov x19,x9 中转）+emitCast 窄扩展 from 窄 to=64 位真实分配面（ldrsb x9+mov x21,x9 实证——「from 窄则不分配」预判错误·to=i64/u64 才是分配判据）；③**等价性设计**：未分配/未启用路径 fallback 原默认槽=产物逐字节不变（32 位/浮点/i1/i128 不参与分配全原样·分配器仅 i64/u64/ptr）；④探针 asm 铁证（target/p451/probe64）：`mul x20,x10,x11`/`add x19,x10,#12`/`sdiv x20,x9,x10`/`lsl x19,x9,#2` 直写——唯一 `mov x19,x0`=Call ABI 接收（合法非冗余）；⑤门禁：Ninja+sccache 零警告构建+单测 **1363/1363**+E2E 全量 **464=462/2/0**（2 红=406/431 基线既有·与 449-a wt-base 纯基线树硬证据同签名·**零新增红**）+asm 宽度 **289×3 PASS**（arm64 零非法编码）；⑥**反证闭环**：stash 撤改→重建→探针中转完整复现（mul x10+mov x20,x10/sdiv x9+mov x20,x9/lsl x9+mov x19,x9）→stash pop 恢复→重建→零中转复绿；⑦**诚实边界**：i128 双槽面+oop 虚表装载面（35 条残余所在·写集纪律不扩文件）、操作数装载面（`mov x9,x21` 型已分配操作数→临时槽装载·三地址源直用=后续轮）、Call ABI 接收搬移（不可消除）、**x64 同族面**（win 后端同构 storeVirtualResult 中转=同模式移植候选）、arm64 运行级 bench（本机无实机=跨机复验轮）；⑧下一轮候选预登记：**457-a x64 同族面直写**（同模式移植 win 后端·456 号已被同机另一会话认领 D1 拆分·避让）或操作数装载面。

### 1.0 实施计划（**449-a 开发轮·🏃 本机并行基建（worktree 任务池+sccache+门禁串行锁）·用户三裁决=两形态并存+引入 sccache+本轮落盘·基线 71079b6c→af1fdd54 看板先行·2026-09-20·分支 任务/家机-449-本机并行基建·worktree=wt449**）

**1. 目标与验收**：根治同机并行互踩（429-a/431-a 实录根因=多会话共享一工作树与 target/：checkout 带走未提交内容/E2E 中途 expected 消失/cn.exe 占用 LNK1104/产物踩踏）——**验收实测**：①主树（447 集成轮）+wt449（本轮）双开零互踩；②wt449 冷构建计时+sccache --show-stats 命中率数字；③gate_lock 双进程并发=一持锁一排队；④wt449 全量门禁全绿；⑤try-build 回签后 integrate.py 集成。
**2. 写集**：scripts/wt.py（新）｜scripts/gate_lock.py（新）｜scripts/ci.ps1（挂锁）｜scripts/integrate.py（挂锁）｜build.ps1（--parallel）｜scripts/check_handoff.py（看板多任务行）｜AGENTS.md（§8.8）｜plans/025 本节｜看板本机行（窄通道）｜收工回填四件套。
**3. 实施步骤**：①看板先行+wt449 自举试点；②装 sccache（Documents/tools/sccache·SCCACHE_DIR=Documents/sccache-cache·5GB）+/W4 /WX /utf-8 兼容实测；③wt.py=create（fetch→develop 建树+分支→**两级深度校验**〔../../third-party gtest 可达·本轮首建 wt/449 三级即翻车的实证〕→sccache 注入 -DCMAKE_CXX_COMPILER_LAUNCHER）/list/remove（集成校验后删）；④gate_lock.py+ci.ps1/integrate.py 挂锁；⑤build.ps1 --parallel；⑥check_handoff 多任务行；⑦AGENTS §8.8+机器规格实测定默认并行度；⑧try-build（写集触 build.ps1/ci.ps1=触发面）。
**4. 验证链**：check_handoff+check_progress_sync｜wt449 全量门禁（构建零警告+单测+E2E --jobs 8）｜sccache 冷/热计时对照｜gate_lock 双进程实测｜双开 git status 各自干净实证｜integrate.py（写集触 ci.ps1/build.ps1→--try-build-done）。
**5. 边缘与诚实边界**：①sccache 若与 /WX 极小概率不兼容→诚实回退仅 --parallel（预判透传 cl 参数零问题·实测为准）；②主树保持原构建配置不动（447 在飞不扰动）；③看板试点期单行双任务（顿号），check_handoff 适配后转多行；④容器方案已否决（win 门禁死绑 MSVC+MASM，镜像 7GB+/许可/漂移，隔离收益 worktree 已覆盖）。
**6. 与他机交叠**：同机 447=共享文档分区共存（v2 串行集成消解）；深度机 448/单位机 329-D12 零交叠；本写集零 src/tests 改动但触 build.ps1/ci.ps1=三平台 try-build 触发面（点名两机在分支跑各自平台门禁）。
**收工回填（449-a·实测）**：①**sccache 兼容链三坑全踩平**：VS 生成器忽略 `CMAKE_CXX_COMPILER_LAUNCHER`（vcxproj ClCompile 原生任务·平台事实）→ Ninja 落地；Ninja+MSVC 默认 `/Zi` 多 cl 并发写同一 PDB=C1041（/FS 拦不住）→ `-DCMAKE_CXX_FLAGS_DEBUG:STRING="/Z7 /Ob0 /Od /RTC1"`；`-DCMAKE_CXX_FLAGS` 覆盖顶掉默认 /EHsc→C4530×/WX=C2220（且 CMakeCache 残留须清树重配）。②**计时**（28 逻辑核/128GB 实测）：VS `--parallel` 冷全量 **127s**｜Ninja+sccache 冷 **23s**（248 miss 入库）｜同树删树热重建 **10.7s**（216 hits 全命中）｜跨树首建 **~21s/48% 命中**（wt-cachetest 实测：共享路径第三方〔gtest/运行时 obj〕命中、树内绝对路径源码 miss——sccache hash 含预处理 linemarker 路径）。③**gate_lock 双进程实测**：A 持锁 8s、B 排队总 8.4s 切入（首版 30s 轮询切换太粗→2s 轮询+30s 打印节流修复）；ci.ps1 集成=acquire/finally-release（Ctrl+C 覆盖·首跑失败路径释放实证）。④**双开零互踩实证**：主树（他人服务轮残留 1 文件）+wt447（九链集成轮）+wt449 三树并行——各自 `target/Debug/cn.exe` 并存（1685MB/树）、git status 互不侵扰、wt447 对 wt449 门禁全程无感。⑤**wt.py 生命周期闭环**：create/list/remove 实测（含两坑修复：cmd 模板注释须纯 ASCII、remove 空分支〔==develop 头〕误报已并入）。⑥**develop 基线红顺手修**：check_own_matrix.py 漏注册（426-a 建脚本·门禁 0.2/4 拦截发现）→ check_registry.json 补登（文档轮组）。⑦**看板多任务行**：新校验 1~4 行随分支集成；develop 看板转两行在集成后直推（防中间态红）。⑧**全量门禁（VS 口径含锁全链·wt449）**：审计/ASCII/关键字/规范覆盖 ✓｜构建零警告 ✓（--parallel）｜单测 **1363/1363** ✓｜E2E **464=462/2/0**（2 红=406_浮点汇合矩阵O3〔T43 在档基线红〕+431_虚调用栈参数矩阵〔深度机链在途〕——**零代码新增红·硬证据=wt-base 纯基线树〔e05eeb8a·Ninja+sccache 25s 构建〕同签名复现两红**）｜asm 宽度 0/0/0 ✓（19.5s·28 并行）｜gate_lock 全链实证（acquire→门禁失败 finally 释放→复跑正常）。

### 1.0 收工回填（**354-a 开发轮·🏃 T33 销项确认轮：385/386 PASS 实测=已实质销项·零代码改动**·基线 79f1066·2026-09-18·分支 任务/家机-354-T33阶段一）

**1. 侦查结论（反转）**：认领后实测 **385/386 双编译对照 PASS**（整16 f 打印 -25536 正确）——读侧窄宽 movsx 设施**已在库**（v2asm 实证 `mov ax, word ptr [rbp-232]; movsx rax, ax`=293-a 后续轮实现）——T33 立案面（整16 f 打印错值）**已实质销项**，plans/021 T33 行漂移未勾（290-a 立案至 354-a 间他机实现未回写）。**354-a 定性=销项确认轮·零代码改动**。
**2. 产出**：T33 行勾销（勾据=runner 385/386 PASS 实测+asm movsx 指纹）+T33 侦查注记（前轮落盘）归档。
**3. 交叠**：零交叠。**4. 下一轮候选**：①win 列 M3 采样；②B1/T43/T46/T47/T48 待用户裁决；③波次7 余项=D36 实施大轮（352-a 方案就绪）/C19/D10~D11；④波次8 批C/D。### 1.0 实施计划（**354-a 开发轮·🏃 T33 阶段一：v2 读侧窄宽 win 后端**·基线 79f1066·2026-09-18·分支 任务/家机-354-T33阶段一）

**1. 目标与验收**：T33 残余（读侧窄宽）渐进式第一阶段——**win 后端**编码LoadStoreWin 的 IR_加载 按指令.类型分派窄宽符号扩展装载（movsx rax, word/byte ptr 槽·T_整16/整8/字符；T_整32 movsxd dword ptr；正系 movzx；缺省=T_整64 维持现状）——**验收**=①385/386 的 整16 f 打印 -25536 正确（现 40000）+两用例全段输出一致；②零回归=v2 树编译+78/79 自举+全量 E2E+单测（**加载分派改窄=存量窄类型槽读面全面变化——E2E 是主安全网·预判存量槽写入已由存储侧保证窄值有效**）；③X64L/ARM64 留阶段二三（诚实边界=单后端先行·跨机轮对齐）。
**2. 写集**：CN语言编译器v2/代码生成/代码生成.cn（编码LoadStoreWin 加载分派）｜文档六件套+看板。
**3. 实施步骤**：①侦查 385 数据路径（存储侧写入形态→加载侧读出形态→打印消费）+IR 指令.类型 在加载点的可用性；②实现加载窄宽分派（movsx/movzx 按类型）；③三探针（385/386/存量窄类型用例）+全链验证。
**4. 验证链**：v2 树零错误编译｜385/386 输出正确｜d38_scan 16 拒保持｜crosscheck｜78/79 自举｜全量 E2E+单测。
**5. 边缘与诚实边界**：①存储侧槽写入形态依赖侦查结论（若存储已 64 位符号扩展则加载窄读冗余——以实测为准调整方案）；②布尔类型加载（T_布尔→byte）随族处理；③X64L/ARM64=阶段二三；④指针加载（B5）不涉（类型=指针·维持 64 位）。
**6. 与他机交叠**：353-a 深度机（T60 负号·IR 生成面）/329-D12 单位机——零交叠预期。

### 1.0 收工回填（**352-a 开发轮·🏃 D36 侦查轮：遮蔽恢复读错槽=架构级定性·实施留独立大轮**·基线 6389371·2026-09-18·分支 任务/家机-352-D36）

**1. 侦查结论（根因实锤·比合槽假说更深）**：346 复现 ✓（v2 出块 上限+下限=101 应 12）。根因=**v2 IR 生成的 局部槽 为函数级平铺映射（名ID→槽）无块作用域**——块内 整32 上限 声明注册 局部槽[上限]=sN 后**出块不弹出**；出块后 读 上限 的 IR 生成查 局部槽 **命中块内 sN**（读 99）——**常量读取路径（查常量值内联 10）被局部槽抢先**。分配标量声明槽（生成声明.cn 92 区）的「预扫已分配」机制=预扫函数级分配=**预扫即合槽源头**（跨块同名复用同槽）。
**2. 修法定性=架构级**：符号作用域栈化（局部槽 出块弹出块内声明名+预扫作用域化+读点优先级链）——涉 IR 生成 预扫/主遍/读点全链+宿主对照（宿主=作用域符号表弹出）——**独立大轮**（同 T33 量级·非拼轮）。**最小修不可行论证**：读点无块信息无法区分「块内读（变量 99）」与「出块读（常量 10）」——常量查先行会错块内·局部槽先行会错出块（双向错）。
**3. 本轮产出**：根因链完整定性（假说合槽→实证平铺无作用域+读点抢占）+修法架构方案+最小修不可行论证——**为实施轮铺垫完整**（下轮直接按 §2 方案开工）。源码零改动（侦查轮·保基线）。
**4. 交叠**：零交叠。**5. 下一轮候选**：①D36 实施大轮（按本轮方案）；②T33（侦查注记已落盘）；③win 列 M3 采样。### 1.0 实施计划（**352-a 开发轮·🏃 D36：v2 遮蔽恢复读错槽（同名合槽假说侦查修复）**·基线 6389371·2026-09-18·分支 任务/家机-352-D36）

**1. 目标与验收**：346 复现（v2 出块后 上限+下限=101 应 12）——**假说=v2 同名合槽**（块内 整32 上限 与顶层常量/外层同名槽共用·P7c 前已知限制面）。侦查实证假说→修复（块内同名声明开新槽=真遮蔽）→v2 输出对齐 101012 的第三段 12（前两段 10/99 现状即对）。**验收**=①346 v2 输出三段=101012（双编译对照口径下 v2 侧正确·expected 不动——T47 常量口径裁决不受影响：本修只动变量槽分配不动常量内联语义）；②零误伤（同名遮蔽是合法语义·存量用例全量 E2E 兜底）；③v2 树编译+78/79 自举+全量 E2E+单测。
**2. 写集**：CN语言编译器v2/IR 层槽分配相关文件（侦查定）+文档六件套+看板。
**3. 实施步骤**：①IR dump/verbose 侦查 346 槽分配实证合槽；②定位槽分配同名判定逻辑；③作用域感知新槽（真遮蔽）；④三向验证。
**4. 验证链**：v2 树编译｜346 v2 输出=101012｜d38_scan 16 拒保持｜crosscheck｜78/79 自举｜全量 E2E+单测。
**5. 边缘与诚实边界**：①容器同名遮蔽仍拒（P7c 前置·既有）；②跨函数同名不受影响（独立槽域）；③expected 101012 的 T47 口径问题（宿主常量内联 vs 变量语义）不在本轮——本修只保证 v2 出块读外层**变量语义**正确。
**6. 与他机交叠**：337-a 深度机（宿主 ir）/329-D12 单位机——零交叠预期（侦查后复核）。

### 1.0 收工回填（**340-a 开发轮·🏃 D38 批3b：325 模块循环/347 类私有可见性/363 外部函数带体——D38 收官批**·基线 620831b·2026-09-18·分支 任务/家机-340-D38批3b）

**1. 目标与验收（回填实测·全达成）**：d38_scan **16 拒/1 缺（317=已销账对照锚）——D38 十七项防线全闭合**；三探针诊断与宿主逐字对齐（325=「[parse] 模块循环依赖: 循环乙.cn」/347=「无法访问 成员 '密码'（保险箱 的私有成员）」且构造器内 密码=甲 放行=零豁免设计生效/363=「[parse] 外部 函数 声明不能有函数体」）；零误伤反证 p1/p2/p3（无段/公开段类）全过+p4 拒；v2 树零错误编译；单测 1353/1353；全量 E2E（数字见提交信息·含 78/79 自举链）。
**2. 写集（实测）**：CN语言编译器v2/语法/语法分析.cn（顶层 外部 分支+解析外部函数声明）｜CN语言编译器v2/主.cn（编译文件 已加载值编码 1 完成/2 加载中+循环诊断+双出栈点）｜CN语言编译器v2/语法/解析类.cn（段可见性变量+字段节点子起点位+解析类字段 签名扩参）｜CN语言编译器v2/语义/语义分析.cn（类节点索引表+.设置 登记+查类节点索引）｜CN语言编译器v2/语义/语义检查.cn（检查成员访问表达式 可见性设防）｜plans/021（D38 收官+戳）｜文档六件套+看板。
**3. 定位方法学（回填）**：①347 侦查反转（b347v2 目录忘建 stdlib junction=「拒」假象——**stdlib 缺失=预置类型未登记=语义崩**·junction 必建才有效）；②325 根因=已加载 去重递归前标记静默吞循环（值编码 1 完成/2 加载中 区分·零签名改动）；③363 根因=顶层兜底 静默吞「外部」（lessons 326-a ②「静默防御分支复审」再实证）。
**4. 边缘（回填）**：①类缺省可见性=公开（存量语义维持·宿主缺省私有的差异=诚实边界随对齐轮）；②方法可见性=诚实边界（347 目标=字段面·方法调用面随演进而防）；③363 外部签名不登记（单池合并语义）；④363 诊断行尾无换行符（避反斜杠·输出连排小瑕疵）。
**5. 交叠**：零交叠。
**5b. 循环检测演进（340-b~h·五补丁实证）**：初版「在途再入即拒」误伤自举树 23+1 处（79_v2 红）——v2 树目录包模式=子组件导入包根的聚合回引（既有静默语义）非循环。**宿主真判据（双向实证）**=目标目录存在 名/包.cn（目录包形态）→聚合回引静默；纯平铺互引（325）→拒。配套=编译文件 入口路径规范化（双斜杠折叠——同物理文件多键使去重失准）+候选1b/2a/2b 三路径豁免全覆盖+聚合判定目标包=当前目录 包.cn。终态：自举树编译 rc=0 零误伤+325 拒+347/363/p1~p4 全对齐。
**6. 下一轮候选（预登记）**：①win 列 M3 采样（他机开发在飞时）；②B1/T43/T46/T47/T48 待用户裁决；③D38 收官后波次7 余项=D33/D36/C19/D10~D12（D33 深度机 345-a 已认领·D12 单位机在飞·363/347 已由 340-a 销项）；


### 1.0 收工回填（**339-a 开发轮·🏃 D38 批3a：入口函数签名检查三合一（304/372/374）**·基线 1c58120·2026-09-18·分支 任务/家机-339-D38批3a）

**1. 目标与验收（回填实测·全达成）**：d38_scan **13 拒/4 缺**（批3a 三项 304/372/374 全转拒）；探针逐项（z01 主->整64 拒/z02 结果<整32,整32> 过）；crosscheck 全一致；v2 树零错误编译；单测 1353/1353；全量 E2E（数字见提交信息·含 78/79 自举链·零新增回归）。
**2. 写集（实测）**：CN语言编译器v2/语义/语义检查.cn（检查入口函数签名/结果退出码合法 两新函数+语义分析多文件 聚合后挂点）｜plans/021（D38 行批3a+戳 339）｜文档六件套+看板。**语义分析.cn 零改动**（静态方案回滚——静态写须 不安全 函数=沿收集链传染，改聚合点现场扫描 自包含）。
**3. 定位方法学（回填）**：①宿主矩阵先行（z01 主->整64 **拒**=白名单非「任意整数」；z02 结果<整32,整32> 过=wrapper 退出码语义 344 对齐——374 的 结果<字符串,整32> 拒=T 非整数族）；②静态写传染预判（语义分析 收集链非 不安全=静态写被 安全区边界 拦——**改自包含现场扫描**（程序节点们 顶层遍历）零静态零传染）；③宿主 check vs build 分级（372 宿主=链接期拒 check 过——v2 编译期拒=改进·诊断自拟清晰）。
**4. 边缘（回填）**：①多模块同名 主 取首个=诚实边界；②`函数 主()` 无返回标注（附加=0）→拒（合理面）；③**文件级扫描**=顶层声明区（函数嵌套无·主必顶层）✓；④批3b 残余=325 模块循环/347 类私有可见性/363 外部语法面（特性缺口级·单独轮）。
**5. 交叠**：零交叠。
**6. 下一轮候选（预登记）**：①D38 批3b（325/347/363·特性缺口级）；②win 列 M3 采样；③B1/T43/T46/T47/T48 待用户裁决项。

### 1.0 收工回填（**338-a 开发轮·🏃 T56：v2 词法浮点 f/F 后缀识别**·基线 e24819e·2026-09-18·分支 任务/家机-338-T56）

**1. 目标与验收（回填实测·全达成）**：w11/w31 对拍转一致（`浮32 f = 1.5f;` v2p 编译转绿）；**数值面铁证=v2p 产物数据段 `floatData_33 REAL8 1.5`/`floatData_38 REAL8 0.5`（f 后缀剥净·.double 文本直通·与宿主同源 IEEE754 确定性）**；v2 树零错误编译；d38_scan 保持 10 拒/7 缺（338-a 改动零干扰）；crosscheck 全一致；单测 1353/1353；全量 E2E（数字见提交信息·含 78/79 自举链）。
**2. 写集（实测）**：CN语言编译器v2/词法/词法分析.cn（分词数字 后缀循环浮点分支扩 f/F）｜CN语言编译器v2/代码生成/代码生成共用.cn（浮点文本净 剥尾扩 f/F）｜plans/021（T56 销项+戳）｜文档六件套+看板。**语义分型面零改动**（329-a 的 推导源类型文本/隐式转换合法 已按文本尾 f 分型——本轮只补词法吞后缀+生成剥尾两处）。
**3. 定位方法学（回填）**：消费链逆查——令牌文本→IR_常量浮点（文本 Symbol ID 直通）→浮点文本净（数据段发射前剥尾）——**f 后缀留文本内**（对齐宿主形态·保 329-a 语义分型判据有效）+生成端剥净（.double 合法文本）=两端口径闭合；asm 数据段指纹=数值面验收铁证（REAL8 直通=IEEE754 由汇编器解析·宿主同源）。
**4. 边缘（回填）**：①整数形态 f 后缀（`5f`）不消费=维持 parse 错误（宿主行为未测·不扩面）；②「浮32 原生解析」未做（值恒浮64 编码+转换链·精度边界=double→float 舍入与宿主一致·词法令牌无类型字段=诚实边界）；③运行级数值验证经 runner v2闭环与手工链接均受阻（[p6h] 混流/CRT 符号）——以 asm 数据段指纹+同源确定性替代（铁证充分）。
**5. 交叠**：零交叠（337-a=宿主 ir 面·329-D12 写集不含 v2 树·diff 实测）。
**6. 下一轮候选（预登记）**：①D38 批3（304/325/347/363/372/374·入口/模块/私有/外部函数面）；②win 列 M3 采样（他机开发在飞时）；③B1 裁决落地（185_v2 处置·待用户）。

### 1.0 收工回填（**329-a 开发轮·🏃 D38 批2：v2 负测防线语义层 6 项+317 定性销账**·基线 865d157·2026-09-18·波次7 续·分支 任务/家机-329-D38批2·协议v2 家机首分支）

**1. 目标与验收（回填实测·全达成）**：d38_scan 复跑 **10 拒/7 缺**（批2 六项 103/109/305/341/350/343 全转拒+既有 295/296/313/323；缺=批3 六项 304/325/347/363/372/374+317 销账）；**43 形态宿主矩阵双端对拍 65/65 一致零误伤**；v2 树零错误编译；单测 1353/1353；全量 E2E（数字见提交信息·含 78/79 自举链）；**317 定性销账**（Cx17 盘点唯一误列=正测用例·宿主不拒绝·240-a 创建·290-a 上架双测——盘点只认名字未认 expected·261-a 复核只实锤 v2p 编过未复核宿主拒绝前提）。
**2. 写集（实测）**：CN语言编译器v2/语义/语义检查表达式.cn（9 新函数+343 挂点）｜CN语言编译器v2/语义/语义检查语句.cn（109+声明面/赋值面 350·含下标左值类型推导）｜CN语言编译器v2/语义/语义检查.cn（返回面 305+如果/当 341 挂点+103 扩展）｜CN语言编译器v2/语义/语义分析.cn（重载感知构造统计表+查询函数）｜plans/021（D38 行/T49 立案/文首戳/§5 索引）｜更新日志/HANDOFF 家机节｜lessons 追加｜看板本机行｜plans/025 本节。侦查器/矩阵/对拍 target/p320a/ 不入库可复跑。
**3. 定位方法学（回填）**：①宿主行为矩阵先行（43+7 形态四轮实测定规则表·防线粒度=逐形态对齐）；②双端对拍收口（矩阵脚本宿主判定 vs v2p 编译逐形态比对——累计抓出 343 子终点恒 0 陷阱/109 类型ID 语义漂移/t10 比较二元布尔误伤/w07 重载覆盖误伤/**79_v2 自举链 38 处推导兜底误伤** 五问题·单端验证全绿是假象）；③插桩定位（临时诊断区分「块未进/状态异常」两级·验证后即删）；④**79_v2 自举链=v2 语义检查器的唯一实弹靶**（d38_scan 的 v2 树编译走宿主语义·测不到 v2p 自身语义检查的误伤——79_v2 的 fix_p=v2p 编译 v2 全树才经过）；⑤误伤收敛=推导源类型ID静默 收紧到可靠面（字面量直判/非常量标识符表查找/比较短路=布尔）·调用/成员/下标/拼接/常量=「检查调用表达式 整64 兜底」污染面一律放行（诚实边界·lessons 329-a ⑤⑥）。
**4. 边缘（回填）**：①比较/短路二元产物=布尔（推导源类型ID静默 特判·通用 推导二元类型ID 取左粒度不够）；②复合赋值面不拦（宿主 w19 放行）；③成员/下标左值拦（宿主 w32/w33 拒·下标元素类型经语义指针变量表 94-a 登记零副作用推导）；④顶层静态类实例裸声明不经检查变量声明语句=诚实边界；⑤批3 残余六项+**新立 T49**（v2 词法 1.5f 后缀缺口·存量·矩阵 w11/w31 捕获）。
**5. 交叠**：零交叠（深度机停机·单位机空闲·远程无他机任务分支）。
**6. 下一轮候选（预登记）**：①D38 批3（304/325/347/363/372/374·入口/模块/私有/外部函数面·372 与 D24 入口合成链联动）；②T49 v2 词法 f 后缀（小轮·与批3 可拼）；③他机开发在飞=win 列 M3 采样。

### 1.1 上一轮收工回填（**326-a 开发轮·🏃 D38 批1：v2 负测防线补齐（词法/语法层 3 项：323/295/296）**·基线 5ce0b07·2026-09-18·波次7 v2 欠账族）

**1. 目标与验收（回填实测·全达成）**：三批1 三项全转拒（d38_scan 复跑 4 拒/13 缺）+v2 树零错误编译+单测 1353/1353+全量 E2E 零误伤（存量 v2 用例族全绿·v2 树自身无裸分号=自举链实证）。
**2. 写集（实测）**：CN语言编译器v2/词法/词法分析.cn（块注释未闭合）｜CN语言编译器v2/语法/解析语句.cn（解析语句 顶部+解析当语句 体检查+解析块 防御改拒绝）｜侦查器 target/p320a/d38_scan.py（不入库·可复跑）。
**3. 定位方法学**：侦查先行（17 用例 v2p 直调实测分级）→ 分批（词法/语法→语义/模块）→ **层级错位首跑发现**（解析语句 改点被解析块先拦·295 仍编过→双层布防）。
**4. 边缘（回填）**：双分号 `;;` 同面拒绝（吃分号 只吃一个·语义正确）；批2 13 项待续（372 宿主为链接期拒绝·v2 入口合成链侦查更深）。
**5. 交叠**：零交叠。
**6. 下一轮候选（预登记）**：①D38 批2（语义层主组 103/109/305/341/350/343/317）；②锁被持=win 列采样。


**1. 目标与验收**：D38（261-a 立案·深度机盘点 17/17·326-a 侦查实测 16 缺/1 已拒〔313〕）——**批1**=词法/语法层 3 项：①323 未闭合块注释（词法静默→诊断+计数）；②295 裸分号空语句（解析块防御分支静默跳过→拒绝）；③296 当后分号（当体须块·`当(...);` 显式拒绝）。**验收**：①d38_scan 复跑 3 项转「已拒」（4 拒/13 缺）；②v2 树编译零错误；③78/79_v2 自举链不回归；④全量 E2E+单测 1353。

**2. 写集**：CN语言编译器v2/词法/词法分析.cn（块注释未闭合）｜CN语言编译器v2/语法/解析语句.cn（解析语句 裸分号拒绝+解析当语句 体须块+解析块 裸分号防御改拒绝）｜文档六件套+看板。

**3. 实施步骤**：①词法块注释加 `注闭合` 判定→`IO::打印到错误`+`词法错误加一()`（280-a 通道同族·主.cn 聚合中止）；②解析语句 顶部裸分号拒绝（防御纵深）；③**解析块 裸分号防御分支改拒绝**（真正拦截面——解析语句 顶层改点被解析块先拦·首轮实测 295 仍编过发现层级错位）；④解析当语句 体前 `{` 检查（原 `;` 被防御跳过+后续语句吞进当体）。

**4. 验证链**：v2 树编译（CN 代码语法）｜d38_scan 复跑｜78/79_v2 自举链（v2 树自身无裸分号=零误伤实证）｜全量 E2E（存量 v2 用例零裸分号=零误伤检验）｜单测 1353。

**5. 边缘与诚实边界（开工预判→回填）**：①解析块 裸分号防御原注「语句分号已由 吃分号 消费」残留兜底——改拒绝后若存量用例触发即 E2E 暴露（**实测零误伤**·回填）；②双分号 `;;` 同面（吃分号 只吃一个）→ 拒绝 ✓ 语义正确；③**批2/批3 待续**（语义层 12 项：103/109/304/305/317/325/341/343/347/350/363/372/374——**372 移批2** 入口/语义链侦查更深）。

**6. 与他机交叠**：深度机 daemon 采样只读；单位机 ⬜；写集=v2 词法/语法双文件（零交叠）。

**7. 流程注记**：本节实施先行后补认领（全表空闲窗口·无并发冲突——认领提交随收工批一并 push·下不为例）。


### 1.0 收工回填（**325-a 开发轮·🏃 D37：v2 win 后端 __chkstk 大帧栈探测发射**·基线 25cda2b·2026-09-18·波次7 v2 欠账族）

**1. 目标与验收（回填实测·全达成）**：①v2 win 后端大帧（>4096）三段式发射（`mov rax,N / call __chkstk / sub rsp,rax`）+入口影子空间预存（rcx/rdx/r8/r9）+retbuf/参数改从影子读（[rbp+16+位*8]）；②**39_chkstk 上架双编译对照**（新增 双编译对照.txt·断言输出=是）→ **双编译 PASS**；③v2 树编译成功（我改的 CN 代码零警告）+78/79 自举链不回归；④**v2 产物 asm 指纹**（v2work39：`mov rax, 48064 / call __chkstk / sub rsp, rax` 三段式 + `EXTERN __chkstk:PROC` 自动收集〔P7b〕+无参函数不预存〔判据正确〕）；⑤全量 E2E+单测 1353。**顺带根治**：runner v2闭环 分支漏传「断言输出」配置键（261-a 遗留·85 存量用例零变更·**契约变更随 §8.4 广播**）。

**2. 写集（实测）**：CN语言编译器v2/代码生成/代码生成.cn（prologue 三改点：影子预存+三段式+retbuf/参数源）｜tests/e2e/39_chkstk/双编译对照.txt（新增·上架）｜tests/e2e/run_e2e.py（漏传修复 1 行）｜文档六件套+看板。

**3. 定位方法学**：「39 用例」揭义=**用例编号 39_chkstk**（非 39 个用例——D37 立案措辞歧义首轮排查确认）；宿主蓝本（x64_codegen.cpp:435-500）逐细节映射（**chkstk 只 touch 页不改 rsp**·返回后 rax 保持——MSVC 契约·宿主注释纠正两处直觉误解）；双编译对照 vs v2闭环 的选择=**D37 验收原文「双编译转正」**直配（且顺带断言宿主侧，更强）。

**4. 边缘（回填）**：①无参无 retbuf 大帧函数不预存（`用影子参` 判据同宿主·39 实测正确）；②浮点 xmm 不在 chkstk volatile（直读保持）；③v2 被调用者保存寄存器压栈顺序不变（三段式后 rsp 即终值）；④「EXTERN __chkstk」无需手加（P7b 自动扫 call 行）实测命中。

**5. 交叠**：零交叠（深度机 daemon 采样/V2 只读；单位机 ⬜）。

**6. 下一轮候选（预登记）**：①波次7 续（D33/D36/D38/T33/C19/D10~D12）；②锁被持=win 列采样。

### 1.0 收工回填（**324-c 开发轮·🏃 C24：T44/T27 修复补 linux_x64 后端**·基线 0a9aef8·2026-09-18）

**1. 目标与验收（回填实测·全达成——本机静态面）**：①槽补登记（Store.extra+$s1 两段·win 316-a 第二轮映射）；②emitCopy i128 双半（判据扩 copySrcType+变量形态 id<0 双槽）；③`cmp r9d,-1`（32 位特判宽度对齐）；④窄→128 movsxd（i32 源符号扩展）。**验证=x64l .s 四点指纹全过**（c24_probe2 双级别：i128 双半成对 mov 全在场/无裸 `[rbp]` 偏移 0/`cmp r9d, -1`/`movsxd r10, r10d`）+零警告构建+单测 1353/1353+check_asm_width 四维 PASS（271×3）+win 全量 E2E 444=443/1（唯一败 406=T43 待裁决）。**运行级复验归深度机**（p314a/p314b 矩阵+回归轨归零·C24 验收原文·本机无 x64l 运行能力）。

**2. 写集（实测）**：src/cn_compiler/codegen/linux_x64/linux_x64_codegen.cpp（槽补登记）｜src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp（emitCopy/emitDivMod/emitCast 三处）｜探针 target/p320a/c24_probe2.cn（不入库）｜文档六件套+看板。

**3. 定位方法学**：win 蓝本 diff（316-a fa1db7c/319-a 8919d4b）逐 hunk 映射——**先读装载路径再定补点**（i8/i16 经 movsx 已 64 位扩展·无需补——避免双扩展）；探针须用**运行时值**（输入 API）防常量折叠（首版全折叠致零命中假象）。

**4. 边缘（回填）**：emitCopy 混合形态（寄存器↔变量）防御兜底（IR 层已拆双半·不可达）；槽补登记须在全部 Alloca 后（邻接序 ABI·236 教训沿用）。

**5. 交叠**：零交叠（深度机 daemon 采样只读·单位机 ⬜）。

**6. 下一轮候选（预登记）**：①深度机 x64l 运行级复验（C24 交付面）；②波次7=v2 欠账族；③锁被持=win 列采样。

### 1.0 收工回填（**320-a 开发轮·🏃 清零波次6 五项全清**·基线 d6ac72a→0eaff2b·2026-09-18）

**1. 目标与验收（全达成）**：①T37 分派正确（负字面+括号折叠）；②T38 泄漏读拒+同名两循环过；③T40 码点编码（好/A）；④T41 2_4_6（甄别=覆盖缺口）；⑤T42 编译期拒；⑥附=A2022 收缩族第三犯修复（条件跳转+emitCompareInt·O3 regAlloc 暴露）；⑦E2E 424（15 值 O0=O3）/425 转正。
**2. 写集（实测）**：parser_stmt.cpp（T37）+semantic_stmt.cpp（T38）+semantic_expr_op.cpp+semantic_sig.cpp（T42）+runtime/string_api.cpp（T40）+ir_stmt_decl.cpp+ir_expr.cpp+ir.cpp+ir_expr_assign_ident.cpp+ir.hpp（T41 三路径）+x64_instructions.cpp+x64_instructions_cast.cpp（A2022）+tests/e2e/424+425/+coverage_map+plans/021/025+HANDOFF/更新日志+lessons+看板。
**3. 坑（lessons 入册）**：A2022 收缩族三犯规律（regAlloc 写 32 读全名）；静态局部三路径（读/左值/赋值）须同轮齐。
**4. 边缘（回填）**：T37 常量表达式窄面（嵌套/乘除留后续）；T41 标量整数+字面量初值限定（其余诊断拒绝）；T40 编码点=runtime 单点（发射侧不动）。
**5. 交叠**：零交叠。
**6. 下一轮候选（预登记）**：①清零波次7=v2 欠账族；②锁被持=win 列采样。
**7. 终验期增补（收工门禁首跑暴露·四项）**：①**T41 写面半截实锤根治**（272 用例 `桩+=1` 编译为 `*桩=1` 死循环·复合赋值丢运算+自增自减 regId=-1 静默不写回→三写路径齐读-算-写回·findVarEntry 辅助·E2E 426 O0=O3）；②**A2022 收缩族出口面（第四犯）+统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；③**T40 与 214 用例冲突根治**（双来源兼容=字节直放/码点编码·根因=`s[i]` int8 符号扩展 0xFFFFFFE5 绕 ≤0xFF 判定·字节级铁证）；④**随载立案 T47（常量遮蔽语义空白）/T48（字符串下标语义空白）**——三方案呈报待裁决；⑤**v2p 构建指纹根治**（计算v2构建指纹 补 src/runtime/*.cpp——原漏 runtime 致改 runtime 后 v2p 复用旧 obj·256 用例假红 18≠31 实证·删缓存复绿·runner 契约变更随 §8.4 广播）；


### 1.0 收工回填（**319-a 开发轮·🏃 清零波次5=C18+B12 整数语义统一**·基线 e9b4a09→8919d4b·2026-09-18）

**1. 目标与验收（全达成）**：①编译期=常量除零+溢出硬错误（T8+C18①）；②变量面全位宽回绕（t27 八测 O0==O3·原 SIGFPE 分叉消除）；③T36 双口径统一；④规范 §3.7a+§8.4 通告；⑤E2E 422/423 转正+主表映射 179/179；CLI 除零格样本适配（50/50）。
**2. 写集（实测）**：semantic_expr_op.cpp（常量预演）+semantic_sig.cpp（canConvertWithLiteral 值域）+semantic_expr.cpp（intLiteralFitsType）+semantic_decl.cpp（声明位）+semantic.hpp｜x64_instructions.cpp+linux_x64_instructions.cpp（emitDivMod -1 特判）｜scripts/check_cli_contract.py（样本适配）｜tests/e2e/422+423/+coverage_map（3.7a 主表行）｜plans/001（§3.7a）｜看板（§8.4 通告段）｜plans/021/025｜HANDOFF/更新日志｜lessons。
**3. 定性更正（重要）**：m27_02/03=整128 域内数学正确（提升链既有语义·非错值——规范注明）；i128 u128Abs 修正项不必要（现实现取负回绕已正确）。
**4. 边缘（回填）**：x64l 运行级复验归跨机；arm64 sdiv 硬件免疫（注记）；v2 侧=波次7；无符号/整128 常量预演跳过（承载宽于 int64·不误报）。
**5. 交叠**：零交叠。
**6. 下一轮候选（预登记）**：①清零波次6=T37+T38+T40+T41+T42；②锁被持=win 列采样。


### 1.0 收工回填（**318-a 开发轮·🏃 T19①③ 收官=波次4 全清**·基线 75e1c3a→924a4a4·2026-09-18）

**1. 目标与验收（全达成）**：①T19①=handleClassCallExpr 泛型分支 args 逐段 substGenericType（向量$T→向量$·32——原 NewObject 丢失·构造错误码当指针段错误）——t19a「0 1」正确；②T19③ 三件套=族③ innerFunc 分叉（构造 函数指针<返回>(参) 类型）+node->name 重写实例名（FuncAddr mangle 匹配）+registerGenericFuncInstance（值使用点登记——无调用路径则函数体不生成）+checkFunctionNameValue 纯名键直查——v1/v3 探针「2」正确；③T12copy 调试残留清理；④E2E 421 扩七形态（+返回容器+fnptr 回调·O0=O3）。
**2. 写集（实测）**：src/cn_compiler/ir/ir_oop_call.cpp（inner 替换）｜src/cn_compiler/semantic/semantic_expr.cpp（族③分叉+重写+登记+直查）｜src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp（T12copy 清）｜tests/e2e/421_泛型容器组合/（扩形态+expected 重固化）｜coverage_map 注记｜plans/021（T19 全销项+波次4 ✅+台账+里程碑+索引+戳）｜plans/025 本节｜HANDOFF/更新日志家机节｜lessons｜看板。
**3. 坑（lessons 入册）**：探针语法笔误（无参模板形态误当 fnptr 声明——规范唯一 C 形态）；值使用点与调用点的实例化登记路径差异。
**4. 交叠**：零交叠（深度机停机·单位机 ⬜）。
**5. 下一轮候选（预登记）**：①清零波次5=C18+B12（整数语义统一）；②锁被持=win 列采样。


### 1.0 收工回填（**317-a 开发轮·🏃 清零波次4 部分**·基线 fa1db7c→150e9b9·2026-09-18）：**T19②+T20① 根治+两 UB 根治——T19①③ 续轮登记**

**1. 目标与验收（回填）**：**达成=T20① 全根治+T19② 全根治**：①T20① 双层根因（体零检查→间接调用 0 + srcType 裸形态查不到类表）经 recheckGenericFuncBody（A7 同构：字段级借用+快照回滚）+resolveGenericInstanceType 物化双修——t20a 从 SIGSEGV 转正确（输出 1）；②T19② 推断段（纯泛型参判据）+不动点实例循环——t19b 从链接爆转正确（输出 50）；③两 UB（functions_ 迭代器/vector 扩容悬垂·0xC0000005 实锤链）值拷贝根治；④E2E **421** 转正（四形态·O0=O3）；⑤主通路推断（infer_main）与普通函数健康路径（t20plain）零回归。
**3.5 终验期回归两起+pristine 修复**：全量 E2E 首跑暴露 29_core/99_泛型fnptr 错值（浮64 经泛型实例被整型化 3.5→3.0）——
根因=**A7 教训重演**：visitVarDecl 的推断声明机制把类型参数 T **改写为推断类型写回共享 AST**（首实例交换$整32 写回「整32」），
第二实例重放时 T 原文丢失=类型映射失效——修复=recheckGenericFuncBody 加 **pristine 备份**
（首次进入收集体内全部 VarDecl.typeName·每实例重放前恢复原文）；29/99 双复绿。
**未竟（诚实边界·续轮登记）**：T19① 返回 向量<T> 链接过但 asm 层运行崩（IR 全链正确·win 发射的构造+ptr 返回链待挖·探针 t19a）；T19③ 实例赋 fnptr 编译期拒绝未修（探针 t19c）；T20② x64l 复验欠账（深度机停机）。

**2. 写集（实测）**：src/cn_compiler/semantic/{generics.cpp〔recheckGenericFuncBody〕·semantic_call.cpp〔推断段+helper〕·semantic.hpp·semantic_decl.cpp〔值拷贝〕}｜src/cn_compiler/ir/{ir_generic_func.cpp〔recheck 挂点+物化〕·ir_decl.cpp〔不动点循环〕·ir.cpp〔mapType 物化兜底〕·ir.hpp}｜tests/e2e/421_泛型容器组合/｜coverage_map｜plans/021（T19 部分/T20 销项+台账 2+里程碑+索引+戳）｜plans/025 本节｜HANDOFF/更新日志家机节｜lessons｜看板。

**3. 定位方法学（lessons 入册）**：段错误类缺陷的打点链（本轮 13 个插桩点定位到 vector 扩容悬垂）+清理脚本事故（无结束注释块吞真实代码→checkout+重放恢复）。

**4. 边缘（回填）**：推断段保守判据（纯泛型参才推断·混合形态显式 <> 可用）；recheck 字段级借用不跨实例残留（A7 教训对齐）。

**5. 交叠**：深度机停机·单位机 ⬜——全程零交叠。

**§1.0b 下一轮候选（预登记）**：①T19①③ 续轮（asm 返回链+fnptr 通道）；②清零波次5=C18+B12；③锁被持=win 列采样。


### 1.0 收工回填（**316-a 开发轮·🏃 C22+C23（T44/T45 用户裁决波次清零）**·基线 0a66108→addb1c7 认领·2026-09-17）

**1. 目标与验收（回填实测·全达成）**：
- **C22 三合一**：①甲·双层根因=**T12 槽登记只看 operands/result 漏 Store.extra**〔**236 回归弯路实录**：首版在指令流内登记 Store.extra→236 双编译用例死循环（rc=124）——结构体局部的零化 Store 在 Alloca **之前**，提前登记抢占基名浅槽→**多槽变量 $sN 与基名的邻接序**（结构体/数组「基址+字段偏移」寻址的隐含 ABI·Alloca 段「元素先浅/基名后深」）被打破→零化与字段访问错位；二分定位（撤 emitCopy 仍挂/撤 T12 即绿 255）+新旧 asm diff（内$1 基址 -96→-88）实证；终版=**第二轮专扫**（Alloca/T12 全部完成后只补仍无槽者·新槽追加最深=i128 双槽独立寻址无邻接依赖）→236 回归修复 255 全对+T44 修复保持〕（-O3 SSA 汇合临时 __ternary$N 无 Alloca→未登记→varSlotOf 回 0 拼 [rbp0]·A2006）+**emitCopy 无 i128 双半分支**（-O3 搬运单 mov 只搬高半=低半错值）——修复=登记补 Store.extra 与 128 位 $s1（inst.type 或 v.type 任一 128 判据）+emitCopy i128 双半（寄存器/变量两形态）；②乙=E2E **419** 七形态（三元/嵌套/实参/算术/返回位/双分支静默错值/正128）转正·**O0=O3 实跑等价**；③丙=check_asm_width **维4 [rbp0] 扫描**（正负自检 4 态+全库 267×3 零命中）。验证=t44scan（8 类型×三元/if 双形态）+t44ext（6 形态）矩阵全绿+m44_01 实弹转正。
- **C23 甲+丙**：①甲=**paramIs128 定标**（ir_call 直调路径无条件查形参〔原仅 argIs128 时查〕+窄整实参宽化 Cast）+ir_oop 构造路径同款（buildCallArgsOop 原只做结构体物化）+**发射器窄整→128 宽化分支**（cqo 符号扩展/零扩展双半·原缺失落默认 32 位 mov=错值——i128→窄/同款/f→i128 均有唯独窄→128 缺）；②丙=落法 **E2E 420 行为守护**（asm/IR 指纹式扫描对值直传不可靠〔i64 参数合法立即数同形〕=虚设门禁不做·文档明示）。验证=m45_01~03 三实弹全转正（三跑确定同值）+420 七形态（变量健康对照+表达式折叠+负值 cqo）+O0=O3 等价+409 既有 i128 门禁零回归。
- **顺带**：plans/021 §三-C 补 C22/C23 行（ae875a3 迁移漏落点·文档漂移修复）。

**2. 写集（实测最终清单）**：src/cn_compiler/codegen/x64/x64_codegen.cpp（T12 登记）+x64_instructions.cpp（emitCopy i128）+x64_instructions_cast.cpp（宽化分支）｜src/cn_compiler/ir/ir_call.cpp（定标）+ir_oop_call.cpp（构造定标）｜tests/e2e/{419_i128汇合Copy矩阵·420_i128字面量实参ABI}/+coverage_map｜scripts/check_asm_width.py（维4+自检）｜plans/021（C22/C23/T44/T45 销项+台账 2+里程碑+索引+戳）｜plans/025 本节｜看板本机行。

**3. 验证链（实测）**：实弹复现（m44_01 A2006/m45_01 SIGSEGV/m45_02 静默错值/m45_03 SIGSEGV 修复前基线）→修复后全转正+三跑确定；t44scan/t44ext 探针矩阵全绿；check_asm_width 维4 自检+全库零命中；零警告构建+单测+全量 E2E（436→438）+双文档门禁。

**4. 边缘情况与诚实边界（回填）**：①emitCopy i128 混合形态（寄存器↔变量）走通用单 64 位中转=防御性兜底（IR 层 Load/Store 已拆双半·该形态不可达）；②x64l 运行级复验归深度机（修复平台无关预判同修·GAS 版 [rbp] 裸偏移 0 因 GAS 合法性不做扫描指纹）；③arm64 同构面复验归单位机；④C23 丙指纹扫描不可靠性已文档化（行为守护替代）；⑤间接调用（fnptr）i128 形参场景=签名匹配语义层约束·定标两路覆盖直调+构造即全部可达路径。

**5. 与他机交叠（回填）**：深度机 314-d 🧪 采样在飞全程零交叠（实弹 m44/m45 为其入库资产·只读消费）；单位机 ⬜；**跨机复验请求**：x64l（T44 汇合 Copy/T45 实参 ABI 运行级）归深度机、arm64 归单位机——修复在 IR 层+发射器公共层，平台无关预判同修，以各自全量门禁+实弹复验确认。

**§1.0b 下一轮候选（预登记·防撞车）**：①清零波次4=T19+T20（泛型×容器）；②锁被持=win 列 M3 采样续；③C20 批C/D。


### 1.0 收工回填（**315-a 开发轮·🏃 清零波次3**·基线 8e2865e→收工·2026-09-17）：**T6+T7 嵌套健壮性根治 + 共享文档事故与门禁同轮根治——全链闭环**

**1. 目标与验收（回填实测·全达成）**：
- **T6**：统一 AST 深度上限 128（AstDepthGuard·四挂点=parseExpr/parseBlockStmt 入口+赋值/三元/一元链 Rec 受控递归点）；实测崩点全形态→友好诊断（括号 156〔原立案 175·当前基线实测更早〕/千层块嵌套语义 visitor 崩/一元链 3000 崩）；语法错误深嵌套不崩；存量深度扫描（E2E+stdlib+v2 树全库）最大 **<60**=128 零误伤；E2E **417**（负测锚「嵌套过深」）+**418**（限内 60 块+40 括号运行输出 41）转正。
- **T7（定性反转）**：真根因=**cfg.cpp 支配者集合算法 O(n³)**（vector 全集+std::find 线性交集·LICM 每函数 dom.rebuild 触发；cn check 默认 optLevel=2 走全链到代码生成=放大器；原立案「语义检查」定性错误——分层插桩实测语义 1~4ms 无罪）。根治=①**CHK 迭代 idom 重写**（逆后序+链上溯 intersect·近线性·rustc/LLVM 同款；显式栈 DFS；不可达前驱不参与=顺带修正原支配集污染；dominates 改 idom 链上溯·dom_/domSet 零消费删除）②**regAlloc 活跃性迭代正序→逆序**（后向数据流链式 CFG 一轮收敛·原每轮只回传一块=O(n) 轮）。实测：500 层如果 102s→超限快速拒绝（限内亚秒）；平铺 2000 块 **33s→0.72s**·4000 块 **2.0s**（近线性）。
- **同轮根治**：①HANDOFF:32 深度机节标题行内粘连×6（21a6d77/308-a 引入·带病 8 轮）修复——修正他机分区=已认领开发轮+提交信息说明；②check_handoff 两增强=标题行内粘连检测（剥离行首前缀复检·增强版当场抓到本事故=探测器自证）+**C21**（B 区非空行必须 `> **B` 条目格式）——反例自检四件全过；③AGENTS §8.3 空白铁律口径。

**2. 写集（实测最终清单）**：src/cn_compiler/parser/{parser.hpp·parser.cpp·parser_expr.cpp}｜src/cn_compiler/opt/{cfg.hpp·cfg.cpp}（CHK 重写）｜src/cn_compiler/codegen/reg_alloc.cpp（逆序迭代）｜tests/e2e/{417_嵌套深度上限·418_深嵌套限内运行}/（各 2 文件）+coverage_map｜scripts/check_handoff.py｜HANDOFF.md（深度机节标题行+家机节）｜AGENTS.md（§8.3）｜plans/021（T6/T7/C21 销项+波次行+台账 3 行+里程碑+索引+戳）｜plans/025 本节｜更新日志家机节｜lessons 315-a 段｜看板本机行。

**3. 验证链（实测）**：反证双件（stash 回退→旧版括号 200 崩 0xC00000FD+500 层 101.5s 复现→恢复→诊断+亚秒）+零警告构建+单测 1353/1353+全量 E2E（434→436 含 417/418·详见提交信息）+check_handoff（增强版·含新反例）/check_progress_sync 双过。**定位方法学沉淀**（lessons 315-a）：十轮插桩链=token/ast 分层→变体矩阵（空体/声明/调用/裸块/平铺）→管线五段→pass 逐个→真凶 dom.rebuild；「ast vs check 差值推语义」陷阱=两命令不同入口（check=runModulePipeline 全链）。

**4. 边缘情况与诚实边界（回填）**：①预判②「长链不误伤」验证=平铺 300 条如果/长二元链均平铺不计数（进出平衡）✓（实测平铺 4000 块 2s）；②v2 树（自举编译器）深度防护未测=诚实边界（波次7 v2 欠账族既有安排·不越界抢修）；③CHK 与原支配集迭代的数学同解由全量 E2E+锚定链背书（优化产物等价）；不可达前驱不再污染支配计算=行为差异面（原实现被污染属错误侧·DCE 后不可达块不进 LICM）；④一元链「帧小不设限」先验被实测推翻（3000 崩）→三链全挂守卫=教训入 lessons。

**5. 与他机交叠（回填）**：深度机 314-a 🧪 在飞全程零交叠（其只读源码+写 cnsmith 日志/自己分区；唯一越区=HANDOFF 深度机节标题行事故修复·已按 §8.3 认领轮合法程序+提交信息说明）；单位机 ⬜ 空闲；**跨机复验请求**：cfg.cpp/reg_alloc.cpp 改动平台无关（opt/codegen 公共层）——x64l/arm64 各自全量门禁自然覆盖；78/79 自举锚定链按各自口径复验。

**§1.0b 下一轮候选（预登记·防撞车）**：①**清零波次4=T19+T20**（泛型×容器：单态化注册接入+返回类型 T 替换+形参 ABI·宿主·v2 未测）；②锁被持时=win 列 M3 采样续（种子域 311000001·daemon win 版常跑）；③C20 批C/D（parse 70 上架）。


### 1.1 （⏸ 待定区·2026-09-16 用户令暂缓：plans/026 体系优先，视情况再启动）案戊阶段 1——type_system 下沉 types/ 基础层 + 静态库物理边界 + 层依赖门禁（T2 用户批准案戊）＋并行 **V2 编译器优化检查**（只读）

> **状态（2026-09-16 更正）**：计划已落盘（六要素见下）；**阶段 1 代码改动曾试做（提交 67127bc）、经
> 77c0ae4 revert 撤下、未入 develop**（验证链未跑）；**实施与收口（check_layer_deps 门禁 + 验证链 +
> 文档六件套）待后续机器按本计划认领执行**。并行项 V2 优化检查已完成·产出=plans/021 §三-G2（待排班）。

**1. 目标与验收判据**

- **案戊阶段 1**（开发轮）：`type_system.{hpp,cpp}`（559 行，被 45 文件跨层消费）从 `semantic/` 下沉到
  `types/` 基础层；CMake 拆独立静态库（链接方向物理固化）；新增 `scripts/check_layer_deps.py` 层依赖门禁接入 ci.ps1。
  验收=零警告构建 + 单测全过 + 全量 E2E 全绿 + **锚定链逐字节不变**（234-a 新基线 410460 行）。
- **V2 优化检查**（只读探针）：v2p 自编译计时/产物指令统计/算法模式静态审查，优化机会落 plans/021 待定项。

**2. 写集（精确清单）**

- `src/cn_compiler/types/type_system.hpp`、`src/cn_compiler/types/type_system.cpp`（git mv 自 semantic/）
- 45 处 `#include cn_compiler/semantic/type_system.*` → `cn_compiler/types/type_system.*`（ir 18/semantic 19/codegen 6/parser 2）
- `CMakeLists.txt`（types 静态库 target + 语义库链接 types）
- `scripts/check_layer_deps.py`（新增）+ `scripts/ci.ps1`（接入）
- 文档六件套（本节/HANDOFF 家机节/更新日志/plans/021/看板/lessons 如有坑）

**3. 实施步骤**

1. `git mv` 两文件 → types/；批量改写 45 处 include 路径（python 正则改写，逐文件核对计数）；
2. CMakeLists：新增 `cn_types` 静态库（type_system.cpp）+ `cn_semantic`（或现聚合目标）链接它——保持产物二进制等价；
3. `check_layer_deps.py`：①types/ 不得 include parser/semantic/ir/codegen ②parser 不得 include semantic.hpp（type_system 除外——阶段 2 收口）③层 3 过程目录互不 include（按阶段渐进启用）；
4. 验证链全跑；5. V2 优化检查（v2p 计时/指令统计/静态审查）。

**4. 验证链**：清场重建零警告 → 单测 → 全量 E2E（313 用例口径）→ 锚定链逐字节比对（410460 基线）
→ check_layer_deps 反例自检（构造违规 include 确认拦截）→ 双文档门禁。

**5. 边缘情况与诚实边界（开工预判）**：① 234-a 刚改 v2 树+stdlib 并重锚 410460——本轮基线取其上，
   锚定链比对用新基线；② 45 处 include 中 semantic 目录内部 19 处为「同库内搬移」性质（不跨层），
   仍统一改写保一致；③ CMake 若现结构为单聚合目标，则先建 types 库+目标间链接，不动其余聚合结构
   （阶段 2~4 逐层拆）；④ check_layer_deps 首版规则面从窄（types 纯净性+parser 白名单），阶段 2~4 扩。

**6. 与他机交叠分析**：写集=src/cn_compiler 头文件路径+CMake+scripts——与深度机（234-a 已收工·下轮
候选=读取文件行 uninit/D1）、单位机（mem2reg/D8/F2-33·codegen 与 reg_alloc 面）**写集不相交**；
锚定链口径同步用 234-a 新基线，语义零变化（纯搬移），他机无需复跑。

---

## 二、深度机 linux-x86_64 节（本机维护）

> 历史轮次明细（274-a~295-a）由 git 提交信息+plans/021 §5 索引承载——本节按整节替换惯例轮出。

### 2.1 收工回填（**314-a 🧪 M3 采样轮·x64l 列**·基线 a33e3aa〔=src 与 312-a 基线 ce8089c 等价〕·2026-09-17 收工）：**双域零命中+T44 复验+★T45 立案**

**1. 达成与验收（实测）**：①第五种子域 310000001×500（33s）+第六 311000001×1000（70s）差分 O0/O3 **全一致零命中**（x64l 列首测·比 win 快 ~19 倍）；②负向 400（310100001）**400/400 全拒绝·0 崩溃·0 无行号·0 行号偏离**（连续第五轮）；③回归轨全库 124 件命中全旧类零新类别（T27×8+T37/T38/T42·含跨平台呈现差注记：m27_02~05 x64l 分歧/win 一致）；④**T44 x64l 复验完成**（家机点名）：三元族双级 SIGSEGV（asm `mov [rbp],r10` 高半偏移 0 铁证）+双分支族免疫（静默错值=win 单侧）+三前驱✓——T44 行证据回填；⑤**★T45 立案**（§三-G·B9 联想扩面撞出）：128 位字面量实参 ABI 契约分叉（mov 值传 vs lea 取地址对照铁证）→双级 SIGSEGV/UB 双态·挂面 7 形态+健康对照全测·实弹 run_err/m45_01/02+面台账行 82+方案甲/乙/丙（推荐甲+丙）。

**2. 写集（实测最终清单）**：看板本机行+通告段｜plans/025 本节+总览行｜HANDOFF.md 深度机节（**整节替换根治 306-a 标题六连写粘连事故**）｜更新日志.md 深度机节｜tests/cnsmith_hits/日志/2026-09-17.md（追加 314-a 段·日文件制）｜tests/cnsmith_hits/run_err/m45_01/02（.cn+.meta）｜plans/021（T45 行+T44 行回填+文首戳+§5 索引行+处置建议）｜tests/matrix/面台账.md（行 82+覆盖行 82/82）｜lessons.md（三坑段）。

**3. 边缘情况与诚实边界（回填）**：①T45 win/arm64 复验未做（归家机/单位机）；②采样双域零命中=生成器盲区佐证（行 58 欠账维持）；③daemon 收工前按 313-b 新版重启常态化（x64l 快·轮间睡眠可缩——daemon 参数调整属行为面留开发轮）；④check_handoff「标题连写」盲区登记（lessons 坑③+看板通告·扩条待开发轮）。

**4. 与他机交叠分析（回填）**：家机 313-b 收工（91dd6f1）与本轮认领 rebase 一次（无冲突·日志日文件同向合并成功）；写集与 313-b 零碰撞（其收工含存量拆日+check_handoff·本机只追加新段）；单位机 ⬜ 全程零交叠。

### 2.2 下一轮候选（预登记·防撞车）

- **daemon 常态化采样续跑**（已重启新版·新种子域 312000001 起自动轮换）；🔶 行扩面巡视（候选=行 53 字符串下标/子串·行 54 容器边界 T17/T18）；
- 波次3=T6+T7（嵌套深度防护+语义复杂度）；波次5=C18+B12（含 T27 全族+i128 helper 修正项）；T44/T45 修复待用户裁决（建议同轮波次清零·写集零交叠）。

### 2.3 A12 里程碑标签锚点（**334-a 开发轮·轻量·已收工**·2026-09-18·分支 `任务/深度机-334-A12里程碑锚点`）

**1. 目标与验收判据（达成）**：develop 单主干维持 + 关键里程碑打 annotated tags（独立 refs 不碰分支）——零成本快照锚点（未来 revert/对照/diff 基线）。验收=`git tag -l '里程碑/*'` 列出 4 个 annotate tags + 两远程推送（gitcode ✓/github ✓·ls-remote 确认）+ plans/021 A12 销项 ✓。

**2. 写集（实测）**：git tags（非文件·4 个）｜plans/025 §2.3｜plans/021（A12 移出 §3-A→§4.2）｜HANDOFF/更新日志深度机节｜看板本机行。

**3. 实施步骤**：①**命名规范**=`里程碑/<YYYY-MM-DD>-<事件简称>`（「/」命名空间便于 `git tag -l '里程碑/*'` 列举·annotated 带说明）；②精选里程碑（有明确提交 SHA 者优先）；③`git tag -a` + `git push <remote> refs/tags/里程碑/*`；④文档。

**4. 验证链**：`git tag -l '里程碑/*'`（4）+ `git ls-remote --tags gitcode`（4 条+ peeled ^{} 核验）+ 双门禁。

**5. 边缘情况与诚实边界**：①历史里程碑（v1 自举闭环/编译期安全专项启动等）提交检索耗时 → 本轮只打 4 个有明确 SHA 者（75f3c2b v2 重建立项/14197dd 固定点 361684/47f2c69 协议v1/4d41d34 协议v2），**其余登记增量补打机制**（规范已定·后续轮按需补）；②github 首推 3 条成功（第 4 条输出截断·下次补推核验）；③tags 无 src/tests 改动 → 不触发 try-build（快速门禁即可集成）。

**6. 与他机交叠分析**：tags=独立 refs（不碰 develop/任务分支）→ **零交叠**；文档分机分区（plans/025 本机节 §2.3 / HANDOFF/更新日志深度机节 / 看板本机行）→ 合并友好 ✓。

### 2.64 条件产物存储轮（**D45 根治+D44 正体收口 双销项**＝贯穿恒真三修+选择语句宿主对齐重写——旧占位定性「合成链系统差」被推翻）——413-a 收工回填（分支 `任务/深度机-413-条件产物存储`·基点=412 链 61b351e·2026-09-19）

**1. 目标与验收（达成）**：D45 根治（贯穿恒真修复）→D44 最终收口（选择 fall-through）。验收=ft2 转绿+贯穿矩阵+双编译对照+全量门禁。**全达成**（见 §4 验证链）。
**2. D45 本体定案（★推翻 409-a「净栈对齐」与 412-a「产物槽存储缺失」两轮定性）**：贯穿恒真真根因=**409-a 把合成 ‖ 的 子终点 从「自身位置」改成「自身+1」（注释自称「收集短路区间 识别键」）**——生成表达式 主循环的短路区间跳转 [子起点,子终点) 恰把 ‖ 节点一并跳过→**生成短路求值零发射**（IR 铁证：条件区间求值 0 指令·If 条件跳转 o1=0 读未初始化槽 0）→恒真。409-a「净栈 21→1→0」=**求值被删后的假平衡**（校验器无指令可查）；「fall-through 生效实证（fall1+fall2）」=恒真假阳性（条件恒真恰使贯穿显现）。412-a asm 终审的「产物槽存储缺失」定性=零发射现象的误归因（v2 If 条件走寄存器无需槽·ref 直写 ‖ 实证短路产物寄存器直供条件跳转）。连带根治两枚 406-a 残缺合成：①贯穿=真 赋值段**重复合成两段**（第二段少合成左值标识符且右值区间空=[赋值自身,赋值自身)）→删；②首段右值区间 `池.大小()-2` 把左值标识符误包进右值区间（净压 2→[ir2] 多压）→`-1`（对齐 883 初始化段正确口径）。D45 三修=解析语句.cn：‖子终点=自身位置（对齐 解析二元循环 层<=2 正常口径）+删残缺段+右值区间-1。
**3. D44 正体收口（宿主对齐重写·贯穿标志模型作废）**：406-a「降糖 If 否则链+贯穿标志」模型三缺口不可自洽收口（①无 默认 时否则链不回填=If 平铺；②中断的选择出口块全项目无设置点=恒 0 死分派；③贯穿标志与否则链互斥——链下前 case 落后不评估后续条件，贯穿永不触发）→**推倒重写为宿主 genSwitch 判断链模型**（ir_stmt.cpp:332 同构）：解析期产出真 节点_选择语句+节点_情况标签（IR常量.cn 50/51 新增；默认=值区间空 [0,0) 的标签·子终点=1=字符串情况值）；生成期 生成选择语句=选择值求值一次槽化→判断链 cmp+条件跳转→**体块未终止直落 下一体块/默认/出口**（真 C fall-through·无贯穿标志）→中断绑定 选择出口块（既有分派激活）+生成当/生成循环 暂存清 0=**最近绑定**（C break 语义）；语义期 检查选择+块遍历分派；生成语句 预扫描 扫描控制流体块 补选择体块收集（跳过面）。**字符串情况值**走 字符串比较→__cn_str_eq（宿主 C-4 对齐）。
**4. 写集（最终）**：CN语言编译器v2/IR/IR常量.cn（+节点 50/51）｜CN语言编译器v2/语法/解析语句.cn（解析选择语句 重写 173→99 行·删 选择如果索引们 静态）｜CN语言编译器v2/IR/生成语句.cn（+生成选择语句 ~120 行·主循环分派·扫描控制流体块 选择分支·生成当/生成循环 暂存对·临时探针已删）｜CN语言编译器v2/语义/语义检查.cn（+检查选择·分派）｜tests/e2e/450_选择贯穿矩阵/（新·主.cn+expected+双编译对照.txt）｜tests/e2e/coverage_map.md（450 登记 5.5）｜plans/021/025/更新日志/HANDOFF/lessons。零意外文件。
**5. 验证链（全绿）**：ft2（v=2 恒真现场）v2=「二」=宿主 ✓；fall3（贯穿直落+中断）v2=「一二」=宿主 ✓；mix（默认/嵌套最近绑定/字符串 case/当内选择）v2 与宿主**逐行一致**（8 行）✓；**全量 E2E --jobs 8：447=444/1/2**——唯一红 426=基线既有（T50·修复在未集成 331-a）·零新增红 ✓ 含 78_v2/79_v2 自举链 PASS+450 双编译对照 PASS ✓；单测 **1353/1353** ✓；静态双门禁：check_spec_coverage --strict 100% ✓+check_matrix_coverage 83/83 ✓。
**6. 诚实边界（回填）**：①情况值常量性未限定（规范 755=常量·v2 现接受整型表达式=超宿主面·已注 检查选择 诚实边界·收窄需规范口径裁决）；②枚举成员 case（宿主 isEnumMember）未特判（372-a 枚举 MVP 交互面·登记观察）；③旧 §2.64 占位定性「合成链节点字段语义系统差·非单点可解」**被本轮推翻**（净栈违=贯穿赋值段字段错单点·合成 ‖ 零发射=子终点错单点——「系统差」假象=两枚单点叠加）；④未集成在飞队列（331/337/338/345/355/388/395 等）与本分支的合并序=集成轮处理。
**7. 下一轮候选（§〇 预登记）**：**D42 甲案波 1**（2026-09-19 用户裁决「同意甲：泛型类源码编译全链，分波推进」）——波1=解析/语义层 `类 名<T>` 源码形态（详见 §〇 本机行）。

### 2.65 D42 甲案波 1（**泛型类源码形态·解析+语义层**＝`泛型 <类型 T> 类 名 { }` 声明解析+参数序登记+类型参数名豁免）——414-a 开发轮（分支 `任务/深度机-414-D42甲波1`·基点=413 收工头 d1e3efb 接力〔347-a 基点=346-a 先例〕·worktree 隔离〔主树由守护自动化共享·375-a/391-a 纪律〕·2026-09-19）

**1. 目标与验收判据**：v2 解析器+语义层接受泛型类**源码声明形态**（`泛型 <类型 T[, 类型 U[: 约束]]> 类 名 { 类体 }`——规范 06-十三·stdlib/容器.cn 实际形态；~~`类 名<T>`~~ 为预登记文案笔误·引用形态 `名<T>`→`名$T` 合成已由 P7b 解析类型文本 763 通）。验收=①新 E2E 用例 451（泛型类源码形态·双编译对照可运行形态）PASS；②**硬指标=v2p check stdlib/容器.cn 全 12 模块零诊断**（真实 stdlib 源=最全形态集）；③全量门禁零新增红+自举链 78_v2/79_v2 PASS+单测 1353。
**2. 写集（预登记）**：CN语言编译器v2/语法/语法分析.cn（解析程序 顶层 `泛型` 分支）｜CN语言编译器v2/语法/解析类.cn（+解析泛型类声明·消费 参数表+内接 解析类声明+参数序登记）｜CN语言编译器v2/语法/解析基础.cn（+静态表 泛型类参数序们+清空挂点）｜CN语言编译器v2/语义/语义分析.cn（检查类型引用 参数名豁免——查 泛型类参数序们 精确分段匹配）｜tests/e2e/451_泛型类源码形态/（新）｜tests/e2e/coverage_map.md（451 登记）｜plans/021/025/更新日志/HANDOFF/lessons。零意外文件。
**3. 实施步骤**：①解析程序 顶层循环 `类` 分支前加 `泛型` 分支（是词 文本匹配·不动词法关键字表——IR签名 扫描器按文本匹配假设维持）；②解析泛型类声明：消费 泛型→`<`→循环（词「类型」+标识符=参数名→序；可选 `:` 标识符=约束·消费登记不校验〔波1 边界〕）→`>`→`类`→解析类声明 照常（产 节点_类·基名·类体全解析）；③登记 静态 映射<整64,整64> 泛型类参数序们（键=类键基·值=参数序串「T,U」驻留·对齐 IR签名 扫描器 类类型参数们 同构独立·波3 合流）——清空挂点=解析程序 入口（变量结构体表 同款纪律）；④`泛型 <...> 函数` 形态=显式诊断「v2 暂不支持泛型函数声明」+跳过到声明尾（**绝不静默吞**·对齐 继承 诊断模式）——泛型函数=后续轮；⑤语义层 检查类型引用：$ 合成放行已有（513 行），补参数名豁免（文本 精确=某参数序分段成员→放行）。**设计决策**：Rust 对照=decl 期泛型参数即进入作用域（rustc resolve）；v2 波1 取语义层豁免而非解析层合成 `基名$T`（后者须给 解析类型文本 全调用链传类上下文=侵入面大）——**越界放行边界**（类外用 T 作类型名不诊断）登记诚实边界·波2 单态化重写类型绑定时收口。
**4. 验证链**：①探针三态（/tmp/p414）：L1=泛型类声明+主 不实例化→v2p check 零诊断+build 运行（类无实例=零生成影响·实测确证）；L2=+函数内 `盒子<整32> x;` 声明不用→check 通过（$ 放行）+build 行为实测记录；L3=IR 布局急切布局风险实测（未实例化泛型类是否布局→若崩=IR 层最小豁免「未实例化泛型类不布局」随轮落地）；②硬指标 v2p check stdlib/容器.cn（+其余 11 模块）零诊断；③451 E2E 双编译对照 PASS；④全量 E2E --jobs 8 零新增红+单测+strict/matrix+自举链。
**5. 边缘情况预判**：①词法「泛型」非关键字 token（是词 匹配·IR签名 假设不破）；②类体 T 字段类型文本=T（解析类型文本 扁平）→语义豁免面；③未实例化泛型类 IR 布局行为未知（L3 实测定）；④stdlib 12 模块非容器形态杂项（泛型函数/约束形态）触发④诊断路径=预期；⑤双登记（IR签名 扫描器 vs 解析层）并存不冲突（独立通道·波3 合流）。**诚实边界（开工预判）**：约束不校验/泛型函数不支持/越界放行/实例 build 单态化缺（波2）——逐项收工回填实测。
**6. 与他机交叠**：家机 364-D10（写集=宿主 ir 泛型函数诊断·src/ 侧）零交叠；单位机 329-D12（自验轮）零交叠；413 分支=本分支基点（接力非并行）。集成序=413 先于 414（顺序敏感）。

**【收工回填（实测）·414-a】**：
**1'. 验收（达成·口径修正两处如实声明）**：①451_泛型类源码形态 双编译对照上架双链绿 ✓；②**硬指标修正**＝原「v2p check stdlib 12 模块零诊断」**过宽不可达**（实测：单模块编译跨模块符号未加载=固有「未声明」噪音〔错误码/分配 系/解析::系〕+泛型方法体 T* 下标/类型大小(T) 语义=波2 单态化面〔容器.cn 语义 146 诊断构成：~50 跨模块固有+~96 波2 面〕）——修正为「**解析层零诊断**」：容器.cn **0 条 parse** ✓·映射集合.cn 仅 2 条=泛型函数显式诊断（行 24/37·预判④形态·预期内）✓；③全量门禁零新增红 ✓（见 4'）。**声明形态另一误判纠正**：预登记「类 名<T>」实为**引用形态**（P7b 已通）——声明形态=stdlib 实际 `泛型 <类型 T> 类 名` 前缀式（§2.65 开工节已纠正）。
**2'. 写集（最终）**：与预登记一致+2 枚顺带根治（解析类.cn 常量 函数/不安全 析构器 缺口）＝语法分析.cn｜解析类.cn｜解析基础.cn｜语义分析.cn｜主.cn（清空挂点）｜tests/e2e/451_泛型类源码形态/（新 3 文件）｜tests/e2e/coverage_map.md（451 登记 5.6）｜三机任务看板.md（develop 直推+工作树同步）｜plans/021/025/更新日志/HANDOFF/lessons。零意外文件。
**3'. 实施增量（预登记外当场裁决·「一劳永逸」纪律）**：stdlib 容器.cn 实测暴露三枚**与泛型正交的类体解析缺口**——不修则波3 stdlib 源码加载必然重撞，随轮根治：①`常量 函数`（const 成员函数·17 处）解析消费+零语义（this 常量性=宿主面·诚实边界）；②`常量 不安全 函数` 双修饰（映射集合.cn 194）；③`不安全 函数 ~名()` 析构器——**原不安全分支直接取标识符，遇 ~ 运算符 token 名 ID 取错+位置错位→方法体解析中断**（probe6 铁证）——补 ~ 形态（合成名 "~名" 对齐 函数 分支）。
**4'. 验证链（全绿）**：探针 10 态=probe1~10 全链实测（声明/双参/T* 字段/无 赋值/拷贝构造/不安全 析构/双修饰/实例声明/**方法调用边界**）；451 双链绿+runner PASS；**全量 E2E --jobs 8：448=445/1/2**（唯一红 426=基线既有 T50·零新增红·78_v2/79_v2 自举链双 PASS=v2 树自身不用泛型类零影响实证）；单测 1353/1353；strict 100%+matrix 83/83+check_handoff ✓。
**5'. 边缘情况（回填）**：预判①②③⑤成立；**③实测定案**=未实例化泛型类不触发 IR 布局（probe1 build 运行通过）·实例声明零使用亦通过（probe2）；**新发现④'**=实例方法调用 IR 层 fail fast「方法调用的对象类型未知」（语义层过·IR 层止=正确快败非崩非静默）——**波2 单态化的实测边界锚点**。诚实边界（随轮登记）：约束不校验/泛型函数声明不支持（显式诊断）/参数名越界放行（类外同名类型名不诊断）/this 常量性零语义/方法调用单态化缺（波2）——全部带根治路径（波2/波3）。
**6'. 下一轮候选（§〇 预登记）**：**416-a=D42 甲案波 2**（415 号已被家机 D11 侦查先行认领·341-a 先到先得顺延）（IR/生成层单态化实例：实例类型 类表挂接+方法调用解析+布局按实例参数·验收=451 扩面方法调用转绿+向量<整32> 添加/遍历运行级全链）。


### 2.66 D42 甲案波 2（**IR/生成层单态化实例**＝实例收集→实例注册为完整用户类→方法 AST 克隆生成）——416-a 开发轮（分支 `任务/深度机-416-D42甲波2`·基点=414 收工头 e2376c7 接力·worktree 隔离·2026-09-19）

**1. 目标与验收判据**：用户泛型类实例（`盒子<整32> b;`）获得完整对象语义——声明构造/方法调用/字段访问/RAII 析构全链。验收=①451 扩面（方法调用形态转绿·用例内 返回 b.大小() 路径）＋新增 452 泛型实例运行级矩阵（构造赋值/方法调用返回/双实例独立/字段访问·双编译对照）；②**向量<整32> 声明+添加+遍历运行级全链探针**（stdlib 容器路径对泛型实例声明的兼容实证）；③全量门禁零新增红+自举链双 PASS。
**2. 写集（预登记）**：CN语言编译器v2/IR/IR类.cn（+实例表/收集/实例化注册/剥取辅助）｜CN语言编译器v2/IR/生成函数.cn（IR生成多文件 收集+注册调用点）｜CN语言编译器v2/IR/生成类.cn（生成类方法们 泛型分派+克隆生成）｜CN语言编译器v2/IR/生成声明.cn（如分派需调序）｜CN语言编译器v2/语法/解析类.cn（如需补泛型基类登记）｜tests/e2e/451（扩面）/452_泛型实例矩阵（新）｜coverage_map｜plans/021/025/更新日志/HANDOFF/lessons。零意外文件。
**3. 实施设计（★方案 B＝AST 克隆单态化·rustc instantiate 同构·既有链零修改复用）**：核心洞察=变量声明分派链第一关 `是用户类(类型文本)`（生成声明.cn 96）→生成类对象局部（分配+构造+RAII 全自动）；方法调用 `是用户类(类名)`→发射用户类方法调用；字段访问走布局表——**只要实例合成名（盒子$整32）被注册为「完整用户类」（类名表+布局表+方法表+构造/析构位），既有用户类全链零修改复用**。三步：①**实例收集**＝线性扫池内全部 节点_类型 节点（O(池)·零递归），文本含 $ 且剥基名命中 用户类们→实例表登记（去重；容器/结果合成名被 是容器类/是结果类型 前置分派自过滤）；②**实例注册**＝从类节点 AST 读字段清单（节点_类字段）→字段类型文本按参数序替换（T→整32）→偏移/对齐重算（复用解析类字段 同款算法）→登记 布局表+字段类型表（键=实例ID）+用户类们+构造/析构位（从基类拷）；③**方法单态化**＝生成类方法们 对泛型基类遍历实例表→**克隆方法 AST**（池尾追加·子区间基址平移·类型节点文本替换）→生成方法(克隆)（符号自动=实例$方法·方法表登记实例键）——调用侧 零改（发射用户类方法调用 查实例方法表/符号自然含实例参数）。**技术路线对照 Rust**：rustc 按需单态化（instantiate）+ MIR 复制重写——本方案=AST 克隆重写·生成器无感知（性能=编译期一次性克隆·运行期零开销=与 rustc 同判据）。
**4. 验证链**：①探针=实例声明+方法调用+字段访问+双实例独立（/tmp/p414/probe11 系）；②451 扩面+452 新用例双编译 PASS；③向量<整32> 运行级全链；④全量 E2E --jobs 8 零新增红+单测 1353+strict/matrix+自举链 78/79。
**5. 边缘预判**：①克隆体入池尾=程序区间遍历不重扫（安全）；②线性扫误收集（未用实例多态化）=冗余但正确·去重兜底；③方法签名含 T（-> T）克隆替换后登记实例方法表（发射侧查返回类型 ✓）；④静态泛型字段/泛型方法局部类=波3 面（登记）；⑤语义层方法体检查用基类 AST（T 类型位已豁免·值位 T 不存在）；⑥克隆区间平移对 附加（类型节点索引/区间索引）字段的处理须逐节点类型核对（最大风险点·用 probe 矩阵实测）。
**6. 与他机交叠**：家机 415-D11 侦查（emitStructWholeAssign 整体赋值分派·读 src/ 宿主侧）写集不相交；单位机 329-D12 自验轮无冲突。集成序=413→414→416（接力链顺序敏感）。

**【收工回填（实测）·416-a】**：
**1'. 验收（达成）**：①451 扩面方法调用转绿（宿主+v2 双链同退出码 7）✓；②452_泛型实例矩阵 上架（双实例独立校验和 224·双编译 PASS）✓；③**向量<整32> 运行级全链 242 ✓**（新 v2p 编译+旧 v2p.obj 借链=方法符号格式兼容实证）；④全量 E2E 449=446/1/2 零新增红+单测 1353+strict/matrix+自举链 78/79 双 PASS ✓。
**2'. 写集（最终）**：与预登记一致+新增 tests/e2e/452_泛型实例矩阵/主.expected（runner 要求存在·空文件=无输出断言）。实改=IR类.cn（+泛型实例块 ~250 行：表/剥基名/收集/替换/克隆器/布局登记/方法表登记/编排）｜生成函数.cn（收集+注册挂点）｜生成类.cn（泛型分派+构造符号基名修正 ×2）｜生成声明/生成表达式求值（调试临时·已删零 diff 净）——**以 git diff 为准**。零意外文件。
**3'. 实施增量（预判外当场修·三枚）**：①**方法符号(类型文本, 类型文本, "") 构造引用侧对泛型实例产四段符号**（类型文本×2）与定义侧克隆体链符号（实例$基名）错位→引用侧方法名改 剥实例基名（×2 处：生成类.cn 383/412）——**连带暴露②剥实例基名 无 $ 返回空串**：非泛型类构造符号被写成 类$（8 用例链接红：50/228/283/369/380/382/383/394）→改为无 $ 返回原文+收集侧显式判 $（双实例语义修复）；③451 映射对补显式默认构造（v2 第一版边界=类必须提供构造器·宿主隐式默认构造=差异登记）。**克隆器两遍法**（值拷贝+索引映射→子区间平移/类型节点索引/类型文本替换）**零事故一次通过**——预判⑥「附加语义盘点」风险被两遍+类型化修针表消解（节点_类字段/节点_参数 附加=类型节点索引·节点_方法 附加=返回类型ID·其余原样）。
**4'. 边缘情况（回填）**：预判①②④成立；③签名含 T 替换后登记实例方法表 ✓（发射侧查返回 ✓）；⑤语义层方法体检查用基类 AST ✓（零改动）；**新增发现**：实例化收集对「未用实例」也注册（线性扫全池类型节点）=冗余方法生成（编译期·正确性无损·去重已有）——性能观察项登记。
**5'. 诚实边界（回填）**：嵌套泛型实参（盒子$T 内层）不替换=波3；跨模块基类（基类不在本编译单元）静默零注册=波3 stdlib 源码加载面；约束仍不校验；泛型静态字段/泛型函数声明未做。**下一轮候选（§〇 预登记）**：**417-a=D42 甲案波 3**（stdlib 源码加载路线：IR签名 扫描器替换为源码级解析加载+390 通道根治验收）——或按 plans/021 排班队列优先级调整。


### 2.67 D42 甲案波 3 前置件①（**泛型函数声明+泛型体检查延后+撤守卫侦察**＝stdlib 源码级加载缺口清单实测立案）——417-a 开发轮（分支 `任务/深度机-417-D42甲波3`·基点=416 收工头 d0aef98 接力·worktree 隔离·2026-09-19）

**1. 目标（本轮=波 3 前置侦察+前置件①）**：撤 是标准库模块 守卫实测 stdlib 源码级加载缺口全集；落地 泛型函数声明解析+泛型体检查延后（撤守卫硬前置）。
**2. 写集（最终）**：CN语言编译器v2/语法/解析类.cn（解析泛型声明 函数分支）｜CN语言编译器v2/IR/IR类.cn（登记泛型基类参数序 删类存在防御——函数名共用表）｜CN语言编译器v2/语义/语义检查.cn（泛型函数/基类方法体检查延后）｜CN语言编译器v2/主.cn（TEMP 守卫撤/恢复·净零 diff）｜tests/e2e/453_泛型函数声明/（新）｜coverage_map｜plans/021/025/更新日志/HANDOFF/lessons。
**3. 侦察战果（撤守卫实测·123 容器用例源码级加载缺口阶梯）**：①解析层=泛型函数声明 ×5（**本轮修复**）→②语义层=T 表达式位诊断（泛型函数体/基类方法体——**本轮=检查延后** rustc 同构）→③同模块枚举成员访问未解析（核心.cn 错误码.参数·**v2 无枚举支持=D40 桶成员·新立案独立轮**）→④函数版单态化生成+调用点 名<实参>(...) 解析=波 3 主件（418+）。
**4. 验证链（全绿）**：453_泛型函数声明 双编译 PASS（泛型函数声明+泛型类并存+实例方法调用·退出码 53）；全量 E2E **450=447/1/2** 零新增红+单测 1353+strict/matrix+自举链 78/79 双 PASS。
**5. 诚实边界（回填）**：守卫已恢复（stdlib 仍链接期供给模型）——撤守卫推迟至枚举全链+函数单态化落地（418+）；泛型体检查延后=检查空缺（实例克隆体语义检查未做——宿主对齐=实例化后检查·登记 418）；**教训入册**：登记泛型基类参数序 的「类存在防御」在函数版共用时静默拦截登记（防御条件与共用表设计冲突——共用表的防御必须对全部合法键形态成立）。
**6. 下一轮候选**：**418-a=枚举全链**（声明/登记/成员访问/导入跨模块·D40 桶最大子项·stdlib 加载硬前置）→**419-a=函数版单态化+撤守卫+390 根治验收**（波 3 收口）。

### 2.68 枚举全链（**D40 最大子项**＝枚举声明/登记/成员链常量折叠/跨模块导入·stdlib 源码级加载硬前置）——418-a 开发轮（分支 `任务/深度机-418-枚举全链`·基点=417 收工头 d0aef98 接力·worktree 隔离·2026-09-19）

**1. 目标与验收**：v2 枚举全链（规范 C 风格枚举·stdlib 核心 错误码 同款）。验收=①454_枚举全链 上架（跨模块导入+成员链折叠+类型位+转换·v2 闭环 运行级 126）✓；②probe14（单模块枚举+转换 6）✓；③跨模块探针 26 ✓；④全量门禁零新增红+自举链双 PASS ✓。
**2. 写集（最终）**：IR布局.cn（枚举表 存在/成员值+数字文本转数值·不安全）｜IR常量.cn（节点_枚举 53/节点_枚举成员 54）｜语法分析.cn（顶层 枚举 分派+解析枚举声明+顶层可见性登记白名单补枚举）｜语义分析.cn（检查类型引用 枚举放行+收集枚举声明·模块成员键基/公开登记）｜语义检查表达式.cn（标识符枚举放行=整32+转换源判定补枚举）｜生成表达式.cn（成员链枚举常量折叠 生成枚举成员常量 不安全辅助+标识符枚举名不压栈）｜主.cn（清空枚举表挂点）｜tests/e2e/454_枚举全链/（新 4 文件）｜coverage_map｜plans/021/025/更新日志/HANDOFF/lessons。零意外文件。
**3. 实施要点**：①键口径=枚举名ID*键位移+成员名ID（驻留 ID 全局唯一→跨模块直查·零模块键参与）；②成员值=编译期数字文本转数值（数字文本转数值·全库首例编译期转数·字面量数值历来由汇编器处理）+IR 常量折叠走 文本 ID 直传=整数字面量同机制；③跨模块=收集枚举声明 登记 模块成员键基们/公开们（三级决议复用）+顶层可见性登记 白名单补枚举；④安全区边界=字符串/指针下标读写须 不安全 函数（数字文本转数值/生成枚举成员常量 隔离为 不安全 辅助）。
**4. 验证链（全绿）**：probe14（单模块 6）+跨模块探针（26）+454（126）+全量 E2E **451=448/1/2** 零新增红+单测 1353+strict/matrix+自举链 78/79 双 PASS。
**5. 诚实边界（回填）**：枚举成员值域=非负整数字面量（负值/表达式值不支持）；枚举类型变量=整32 族标量槽（非独立类型系统·转换检查靠源判定补枚举族）；同模块同名枚举冲突未查重；枚举作函数参数/返回类型未专项测试（类型位放行已覆盖·运行级随 419 撤守卫实测）。
**6. 下一轮候选**：**419-a=D42 波 3 收口**（函数版单态化+调用点 名<实参>(...) 解析+撤守卫+stdlib 源码级加载全通+390 根治验收）。

### 2.69 D42 波 3 收口（**函数版单态化+调用点解析+撤守卫+stdlib 源码级加载全通+390 根治验收**）——419-a 开发轮（分支 `任务/深度机-419-D42波3收口`·基点=418 收工头 d0aef98 接力·worktree 隔离·2026-09-19）

**1. 目标与验收**：①函数版单态化=泛型函数克隆生成（克隆器函数版·418 枚举/波 2 克隆器复用）+调用点 名<实参>(...) 解析（解析表达式 后缀链·callee 文本合成 名$实参）；②撤 是标准库模块 守卫→stdlib 源码级加载全通（枚举/泛型前置 418 已齐）；③**390 根治验收**=v2p 编译 stdlib 实例用户程序（导入 容器::向量 等）**无 v2p.obj 借链**自洽链接运行。验收=455_泛型函数单态化（新·双编译）+456_stdlib源码加载（新·v2闭环 借链=否）+全量门禁零新增红+自举链双 PASS。
**2. 写集（预登记）**：CN语言编译器v2/语法/解析类.cn（泛型声明 函数分支已通·417）｜CN语言编译器v2/语法/解析表达式.cn（调用点 <实参> 后缀解析）｜CN语言编译器v2/IR/IR类.cn（函数实例收集+克隆）｜CN语言编译器v2/IR/生成函数.cn（生成遍历函数实例分派）｜CN语言编译器v2/主.cn（撤守卫）｜tests/e2e/455/456（新）｜coverage_map｜plans/021/025/更新日志/HANDOFF/lessons。
**3. 实施设计**：①调用点解析=解析表达式 后缀标识符位 '<' 泛型闭合位置 防御扫描→实参类型文本→callee 改写 名$实参1$实参2（合成名直拼——调用/收集/生成三面统一）；②函数实例收集=线性扫 扩展（节点_调用 被调文本含 $ 且基名∈泛型函数参数序表→实例表）；③生成=生成函数 遍历对泛型函数跳过本体+对实例克隆体生成（克隆器节点_函数 版·附加=返回类型ID 替换）；④撤守卫=是标准库模块 返回 假（417 TEMP 实测过的路径·枚举/泛型已齐）+IR签名 扫描器保留兼容（452/123 等借链用例**配置改借链=否**随新能力迁移——既有借链用例保留借链=渐进迁移不回退）。
**4. 验证链**：探针（泛型函数显式实例化调用运行级）+455/456 双编译+全量 E2E --jobs 8 零新增红+单测 1353+strict/matrix+自举链 78/79。
**5. 边缘预判**：①调用点 < 实参 > 与比较表达式歧义（泛型闭合位置 已有防御）；②实例 callee 改写与 B1 导入改写交互（导入泛型函数=登记待办·MVP 同模块）；③撤守卫后 stdlib 非泛型模块（时间/文件/正则等）语义缺口未知=探针实测逐个修或登记；④函数实例去重。
**6. 与他机交叠**：无新在飞交叠（家机 415-D11 已收口集成·单位机 329-D12 自验轮）。

**【收工回填（本体根治达成）·420-a】**：
**1'. 根治（本体达成）**：**克隆体参数槽偏移根因=克隆区间漏块节点**——函数节点.子终点=块索引**单点语义**（非区间终点），克隆泛型函数节点 的区间 [旧起,旧终) 漏块→克隆体无体块→生成块 拿参数节点当块=空体/块错——**子终点+1 闭式含块**（419 的 [rbp+288]/读垃圾 196/长度读 0 三态同源）。**probe15=5/probe17=3 全链回绿** ✓（T* 指针参数下标+参数传递+克隆体单态化运行级全通）。
**2'. 顺带修**：①函数实例收集过滤（被调标识符在 调用节点.子起点——节点自身文本=0）+基名过滤扩函数参数序表；②函数分派遍历改函数节点表自身（**全集索引错位**的潜在越界——419 的「数组越界」实为此+哨兵混用两枚叠加）；③语义放行返回值修正（-1 哨兵→真实类型 整32·**哨兵逃逸成越界索引**）；④函数实例签名表登记（克隆体·调用侧 决议函数符号 与定义侧 同公式——缺失=调用裸名 vs 定义类型编码的链接 undefined）。
**3'. stdlib 源码级加载 IR 段缺口清单（撤守卫实测·421-a 攻坚面）**：类型大小 K/V 表达式位 ×48（克隆器标识符位替换缺）+成员字段偏移 miss ×72（映射/集合克隆体布局链）+装箱语境 ×12——**守卫恢复原名单**（撤守卫推迟至 IR 段清零·421-a）。
**4'. 验证链（全绿）**：455_泛型函数单态化 上架（双编译·退出码 5）+probe16=88/probe17=3 回归 ✓+全量 E2E **452=449/1/2** 零新增红+单测 1353+strict/matrix+自举链 78/79 双 PASS。
**5'. 下一轮**：**421-a=stdlib 源码级 IR 段攻坚**（克隆器标识符位替换+实例布局链审计+装箱语境）→422-a=撤守卫+390 根治验收（波 3 终收口）。


**【收工回填（诚实·部分完成）·419-a】**：
**1'. 达成面**：①调用点解析 ✅（名<实参>(...) 合成名 首个$整32 解析层已通）；②语义放行 ✅（调用/标识符双点·修正放行返回值=真实类型 整32——**-1 哨兵返回被上层当类型索引进向量=数组越界**·gdb __cn_runtime_error 抓栈归因）；③收集扩展 ✅（节点_调用 被调标识符文本——调用节点自身文本=0）；④克隆器函数版+编排+生成遍历分派（单文件/多文件双版）✓——**常量返回探针 88 ✓**（调用/返回协议全通）；⑤守卫恢复原名单（stdlib 供给模型维持）。
**2'. 未达成（诚实立案 420-a）**：**函数实例克隆体的参数槽偏移错位**——克隆体 prologue 参数装载读 [rbp+288]（槽序错位至 ~30·帧仅 4 槽）→`返回 数组[0]` 读垃圾 196/`返回 长度` 读 0——**gdb bt 铁证**=__cn_runtime_error←向量$整64$元素_J←IR生成多文件（克隆体生成路径的槽计数/帧布局两处计数不同源）。**根治方向**：生成函数 的 参数槽序与帧总槽数在克隆体路径的对齐审计（嫌疑=克隆体参数区遍历把非参数节点计入槽序——两遍克隆器的区间边界需与 生成函数 参数遍历口径逐节点核对）。
**3'. 写集（最终）**：语法/解析类.cn（函数分支·417 已含）｜IR/IR类.cn（收集扩展+克隆泛型函数节点+实例化函数注册）｜IR/生成函数.cn（收集挂点+单/多文件双版泛型分派）｜语义/语义检查表达式.cn（调用/标识符双点放行·返回 整32）｜主.cn（守卫恢复原名单）｜453（417 已上架·本轮零改）。**新用例 455/456 未上架**（未达验收——诚实不入）。
**4'. 验证链（全绿·按实际达成面）**：probe16 常量返回 88 ✓（协议层）；全量 E2E **451=448/1/2 零新增红** ✓；单测 1353 ✓；strict/matrix ✓；自举链 78/79 双 PASS ✓。
**5'. 下一轮**：**420-a=克隆体参数槽偏移根治**（gdb 铁证在手·根因域已锁定）→**421-a=波 3 收口重试**（455/456+撤守卫+390）。


### 2.71 stdlib 源码级 IR 段攻坚（**克隆器标识符位替换+实例布局链审计+装箱语境**＝420 实测缺口三族 ×132）——421-a 开发轮（分支 `任务/深度机-421-stdlibIR段攻坚`·基点=420 收工头 d0aef98 接力·worktree 隔离·2026-09-19）

**1. 目标与验收**：撤守卫实测的 IR 段缺口三族清零——①**类型大小 K/V 表达式位 ×48**=克隆器标识符位替换缺（类型大小(K) 的 K=标识符节点·克隆器只换 节点_类型）；②**成员字段偏移 miss ×72**（映射/集合 克隆体布局链）；③**装箱语境 ×12**（构造器装箱类型语境未知: 正常）。验收=**撤守卫下 123 容器用例源码级编译零诊断**（含链接运行）+probe 全链回归+全量门禁零新增红+自举链双 PASS。
**2. 写集（预登记）**：CN语言编译器v2/IR/IR类.cn（克隆器标识符位替换）｜CN语言编译器v2/IR/IR布局.cn 或生成侧（布局链审计修）｜CN语言编译器v2/IR/生成表达式*.cn（装箱语境）｜tests/e2e（455 系扩/新用例随需）｜coverage_map｜六件套。零意外文件。
**3. 实施设计**：①克隆器遍 1 加 **节点_标识符 文本∈参数名→替换为实参文本 ID**（精确匹配·与 节点_类型 同点——越界放行边界同波 1）；②布局链审计=实例化布局登记 的字段遍历对 映射（K*/V*/整64* 混合指针字段）逐字段核（嫌疑=克隆字段 类型节点附加 平移或 字段名 ID 键域）；③装箱语境=构造器装箱（返回 正常()）的 装箱语境类型ID 静态在**克隆体**生成时未设置（419 生成方法 的 装箱语境 静态按基类设·克隆体同函数复用——审计挂点）。
**4. 验证链**：撤守卫 123 零诊断+452/455 回归+全量门禁+自举链。
**5. 边缘预判**：①标识符替换对 用户同名变量=越界放行（波 1 边界延续）；②布局链=克隆体 字段名 ID 与 消费侧 字段名 ID 同驻留 ✓；③装箱语境=静态全局在多函数间污染面（生成期单线程·设置/清除时序）。
**6. 与他机交叠**：无。

**【收工回填（族① 达成+族② 定位深化·诚实）·422-a】**：
**1'. 达成**：①**宿主精确定位达成**（当前生成符号 静态+取函数·不安全 辅助——首测=残留「核心$断言」（普通函数路径缺设置·已补类方法标记）→二次=全 48 锁定 **映射$整64$整64 克隆体四方法**（~映射×16/释放内部数组×16/映射#映射&×12/映射×4））；②**类型大小 K/V ×48 全消**（族① 补插生效——420 末清理曾误删类版标识符替换块·本轮补回+去重）✓；③**族② 部分收敛**：元素数量（整64 非指针字段）×9 消（变量结构体表登记生效）——**剩指针字段 ×72**（值数组/桶数组/链数组/键数组=K*/V*/整64*——**引用参数自身的结构体解析链**待 422-b）✓；④**守卫恢复**·全量 **452=449/1/2** 零新增红+单测 1353+自举链双 PASS ✓。
**2'. 族② 定位深化（铁证·423-b 攻坚面）**：dbg 偏移=兜底结构体ID=0（引用参数 自身 的结构体解析链 miss——**实例布局已登记**（dbg 布局：映射 7 字段 56 字节 ✓·向量 3 字段 24 ✓）——**消费侧 引用参数 的三级解析**未触达实例标记（422-b 审计 查变量结构体类型/解析对象结构体ID 的引用参数键域）。
**3'. 下一轮（423-b）**：引用参数结构体解析链审计（查变量结构体类型 的实例标记命中）→族② 清零→族③ 装箱语境→撤守卫+390 终验收。
**4'. 教训**：清理 dbg 时连带误删类版正式替换块（420）——**清理脚本必须逐块 diff 审查**（dbg 块与正式块相邻时整块 replace 的误伤面）。


**【收工回填（诚实·攻坚未完成→拆分立案）·421-a】**：
**1'. 达成**：①族① 克隆器标识符位替换落地（类版+函数版·类型大小(K) 的 K 标识符/表达式位替换·铁证=克隆类 dbg 标识换=4/8）✓；②452=449/1/2 零新增红+单测 1353+自举链双 PASS+strict/matrix ✓（守卫恢复后收工态）。
**2'. 未达成（诚实）**：撤守卫下 123 零诊断 **未达**——IR 段三族缺口攻坚不完整：**族①标识符替换已落地但 类型大小(K/V) 诊断依旧 48**（铁证矛盾=映射 克隆 标识换=8（8 处全换）而诊断仍报 K/V——**存在未替换的平行生成路径**（嫌疑=基类本体经某路径仍生成/或诊断来自非克隆路径——待下轮 gdb+增量打印定位））；族② 字段偏移 ×72 未动（依赖族①定位）；族③ 装箱语境 ×12 未动。**守卫恢复**（撤守卫推迟）。
**3'. 写集（最终）**：IR/IR类.cn（族① 类版+函数版标识符替换）｜主.cn（守卫撤/恢复·净 diff=注释更新）｜dbg 全清（IR类/生成类/生成函数 零残留）。**新用例未上架**（验收未达）。
**4'. 下一轮**：**421-b=定位「类型大小 K/V 的未替换平行生成路径」**（dbg 类分派 已证实 生成类分派 只进 2 次/克隆类表=3/实例数=4——**4 实例 vs 3 克隆的 1 差**+**向量基类递归未进** 两条线索）→族②③→422-a 撤守卫+390。
**5'. 教训（会话级）**：三族缺口一次性攻坚预算失判（族①表面落地但存在平行路径·族②③依赖族①定位）——**大缺口轮应先「逐族最小复现」再动刀**。

**【429-b 定位深化回填（-1 消费精确分布）】**：**「其他」的 -1 消费精确分布**＝~映射 ×132／释放内部数组 ×132／映射（构造）×33／映射#映射& ×104 权威=276 命中——**「每方法清空×仅拷贝构造登记」的结构性错配确认**——**429-b 正式攻坚**＝**「其他」的登记扩展到全部方法**（对齐「自身」的每方法开头登记·生成类 129 行惯例）——**族① 维持（类型大小 K/V ×48 清零）**——守卫恢复·全量 452=449/1/2 零新增红+单测 1353+自举链双 PASS。


**【425-b 一击定位达成回填】**：
**1'. 根因锁定（dbg 两端同帧）**：**解析类声明（423 分支的 IR类.cn 340 行 实例化布局登记+395 行 登记用户类）在 stdlib 源码级加载（撤守卫）下「零执行」**——铁证=dbg解析类（函数头）零输出+dbg登记端 零输出——**而 收集泛型实例文本（dbg收文）打了**（表大小=0·收集端在 IR 生成期 跑）——**断裂点=「解析类声明 尾部的 登记用户类/变量结构体/布局」的登记数据**在 **IR 收集期 全部不可见**——**用户类们/大小表/候选表 全部 0**。
**2'. 结构性结论（升级）**：**stdlib 源码级加载 的「解析期登记」全部丢失**——**IR 收集期 的「解析产物」（池+程序节点）在**（dbg 收文 打=池里有类型节点 ✓）——**表断裂**=**解析期 的静态表（用户类们/大小表）在 IR 期 空**——**静态表 被「任务入口清空」或「表实例分裂」（语法包静态 vs IR包静态 的二副本嫌疑）**——**425-b 正式审计=IR类 的 用户类们/大小表 静态 与 语法包 的 登记调用 的表——是否同一静态**（v2 静态 的跨包同表性——**419 时代 probe11 的收集成功**=**单文件形态 表同**——**多文件形态**（123）**表断裂**——**嫌疑收敛=多文件加载 的表重置/快照**）。
**3'. 验证链（全绿·收工态）**：452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS+handoff/sync ✓（dbg 全清·守卫恢复）。
**4'. 下一轮（426-b·426 被家机占·顺延）**：**多文件加载 的静态表生命周期审计**（加载器 的 每文件处理 对 静态表 的影响——**嫌疑=「每文件解析后 的 表重置」**——**419 时代单文件形态 表同** 的结论复核）→族②清零→族③→撤守卫+390。
**5'. 教训**：**多文件形态 与 单文件形态 的行为差异**是本轮才发现的维度——**「单文件验证通过的修复」不能外推「多文件也通过」**——全量门禁的 123/452 覆盖了多文件形态但 IR 段缺口的诊断只在撤守卫实测出现。


**【425-a 定位轮回填（收工态全绿·定位成果保存）】**：
**1'. 铁证升级（dbg 登记端/收集端 对照）**：**解析类声明 尾部的 登记用户类+dbg登记类（dbg 未打）**——**收集泛型实例文本（dbg收文）打了但 表大小=0**——**两端的执行/表状态矛盾未解**——**下轮用「登记端打印+收集端打印 同帧」最小复现**（解析类声明 尾 dbg+收集泛型实例文本 dbg 同批重编同跑）。
**2'. 决定性线索（本轮新增）**：dbg 收文 的 **表大小=0**（用户类们 在 IR 收集期=0）——**登记期 表非空**（登记后立即查 零失败·424-a 探针）——**表在解析后 IR 前被清或表实例断裂**——**唯一公开清空点=主.cn 749（入口一次）**——** suspect=静态表 的跨阶段可见性（语法包登记→IR 包查询 的表实例）**。
**3'. 验证链（全绿·收工态）**：452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS+handoff/sync ✓（dbg 全清·守卫恢复）。
**4'. 下一轮**：**425-b=表实例同一性一击定位**（解析类声明 尾打印 用户类们 的 **池地址/大小**+收集泛型实例 同帧——**或** IR类 的 用户类们 改「设置/获取 走 主.cn 传引用」单例化）→族②③清零→撤守卫+390。
**5'. 教训**：**跨阶段静态表断裂的最快定位=「登记端/收集端 同帧打印 表大小」**（两端 大小 差异即断裂实锤·一轮出结果）。


**【424-a 定位轮收工回填（决定性铁证链完整·收口保存）】**：
**1'. 铁证链（dbg 三层对照）**：
- **A**：dbg 收文=收集的三实例 **向量$整64/映射$整64$整64/向量$字符串 的「基名剥取正确（基名=向量/映射）」但「是用户类(基名)=0」**——**用户类们 表在收集期查不到基类登记**（基类登记=解析类声明 395 行 ✓ 执行 ✓——**表或 ID 域不一致待 424-b 锁定**）。
- **B**：dbg 类注=**实例化注册 进·实例数=0**（收集 0 个→注册 0→布局 0→方法表 0）——**波 2 实例化链在 stdlib 源码级加载形态下整体未触发**。
- **C**：dbg 偏移=兜底ID=0（对象=其他·引用参数——方法参数登记 423-b 已落地但消费=解析对象结构体ID 查权威=-1）。
**2'. 结构性结论**：**stdlib 源码级加载形态下，「用户类登记（解析期）」与「收集期判定」的可见性断裂**——嫌疑=①收集/注册的调用时机（IR 生成期）与 解析期表 的**跨阶段表快照机制**（主.cn 每文件解析后快照拼接——**用户类们 是否入快照**）；②**是标准库模块 守卫撤销后的加载路径**与 **加载导入依赖 候选4** 的登记差异。
**3'. 验证链（全绿·收工态）**：452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS+strict/matrix ✓（dbg 全清·守卫恢复）。
**4'. 下一轮（424-b）**：**用户类登记的跨阶段可见性审计**（用户类们 快照/清空 时序——铁证 A 的 是用户类(基名)=0 一击定位）→族②③→撤守卫+390。
**5'. 教训**：dbg 收文 的 是用户类=0 **三层嵌套打印一次锁定**（文本/基名/用户类 判定三分量同帧）——**分层打印方法论的实战验证**。


**【424-a 前置定位收工回填（铁证升级·代码已存分支）】**：
**1'. 决定性数据（dbg 登记/消费对照·123 源码级）**：**登记端**=`其他→实例ID 276`（拷贝构造 参数遍历 ✓ 1 次）；**消费端**=**`其他` 权威=-1 ×297**（非拷贝构造 的生成路径）+`其他 权威=276 ×104`（拷贝构造 命中 ✓）——**「其他」的 -1 消费 297 次发生在非拷贝构造 的生成上下文**——**当前函数变量类型 表的「每方法清空」+「仅拷贝构造 登记了 其他」→其他方法 的生成期查 其他=-1**——**真正的生成链**=**基类本体方法的生成仍在某路径执行**（421-b 的克隆体分派只覆盖 生成类方法们 一条链——**单文件版/多文件版/克隆体 三链中至少一条未覆盖**）。
**2'. 修正确认**：方法参数变量类型登记（423-b 落地）**拷贝构造 路径已生效**（104 次命中 276）——**登记本身正确**；**缺口=其他生成链未走 423-b 的登记**（每方法清空后无人重登）。
**3'. 根治方向（424-a 正式）**：**全生成链的克隆体标记**——dbg 类分派 三链（单文件版 IR生成 375 区/多文件版 424 区/**第三链**）逐一审计 生成类方法们 的调用覆盖——**或正解**：**基类本体方法生成全面禁用+克隆体全覆盖**（生成类方法们 唯一入口化）。
**4'. 验证链（全绿·收工态）**：452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS+strict/matrix ✓（守卫恢复态）。


**【421-c 收工回填（部分完成·代码已保存）】**：①**替换实例类型文本 $ 分段感知**落地（查找值位置$T → 查找值位置$整64 段级替换·IR类.cn）；②**函数实例延迟展开段**落地（实例化函数注册 尾·延迟实例判定[实参=泛型参数名]→定位基函数→类实例绑定展开→克隆+签名登记·含 定位泛型基函数 辅助）——**编码完成编译通过**；③撤守卫实测：123 诊断依旧 132（类型大小 K/V ×48 未消）——**分段感知对「类方法体内的类型大小(K)」的宿主路径未覆盖**（K 标识符的替换挂点=克隆器遍 1 已在——**平行路径仍未清**——419 的单文件版/多文件版之外疑有第三条·待 422 gdb 精确断点）。守卫恢复·诚实验收未达。
**下一轮（422-a）**：**IR 诊断宿主精确定位**（诊断处加 当前生成函数名/克隆体标记 静态打印——一次定位）→按宿主逐个修→撤守卫+390。


**【421-b 定位轮回填（铁证）】**：**平行生成路径定位达成**——dbg 类递归 铁证：泛型实例表含 **查找值位置$T**（**类参数作泛型函数实参的「延迟绑定」缺失**——T 未绑定的函数实例被收集+注册+克隆（T→T 替换无效））；**4 实例=[查找值位置$T, 向量$整64, 映射$整64$整64, 向量$字符串]**（3 克隆类节点=函数实例无类节点 ✓ 差额解释）。**48 个类型大小(K/V) 的宿主**=查找值位置$T 克隆体+映射 类方法体的类型大小(K/V) 经 T 参数化后未绑定链。**421-c 立案（根治方案）**：**函数实例延迟绑定**——收集阶段对「实参段含类参数名」的函数实例不立即注册，改为**在外层类实例克隆时按外层实参展开**（调用点 callee=查找值位置$整64 与定义侧同键）；配套=收集过滤加「实参段含参数名跳过注册」。守卫维持至 421-c+族②③。



### 2.72 IR 诊断宿主精确定位＋按宿主逐修（**类型大小 K/V ×48 的真正生成链锁定**→撤守卫+390 终验收）——422-a 开发轮（分支 `任务/深度机-422-IR诊断宿主定位`·基点=421 收工头 a3d8b09 接力·worktree 隔离·2026-09-19）

**1. 目标与验收**：①诊断宿主锁定（生成类型大小折叠 诊断处加 当前生成符号 静态打印·一次定位 48 诊断的宿主函数/实例）；②按宿主逐修→撤守卫下 123 源码级**零诊断**；③全量门禁零新增红+自举链双 PASS；④456_泛型函数延迟绑定（新用例·视 123 进度）。
**2. 写集（预登记）**：IR/生成函数.cn（当前生成符号 静态设置）｜IR/生成表达式求值.cn（诊断处打印宿主）｜IR/IR类.cn 或 生成类.cn（按宿主修）｜主.cn（撤守卫）｜tests/e2e（新用例随需）｜coverage_map｜六件套。
**3. 实施设计**：**定位先行**——生成函数 入口设 静态 字符串 当前生成符号=函数信息.名字（克隆体=实例合成名·一目了然）→类型大小诊断处打印宿主→**按宿主逐修**（宿主分支预判：①基类本体方法生成（分派跳过失效的第三条链）②实例克隆体（替换漏）③函数实例克隆体）。逐修后撤守卫（是标准库模块 返回 假）→123 零诊断验收→456 视情上架。
**4. 验证链**：123 零诊断（撤守卫）+probe15~17 回归+452/455 回归+全量门禁+自举链。
**5. 边缘预判**：静态打印=调试临时代码（定位后删）·克隆体符号含 $ 可直读。
**6. 与他机交叠**：无。

### 2.73 族②③ IR 段收口（**克隆器修针类型感知全集＋实例表配对越界＋裸字段下标左值＋装箱语境兜底**＝撤守卫 123 终收口）——430-b 开发轮（分支 `任务/深度机-430-族2偏移链收口`·基点=429-b 收口头 0a61b03 接力·worktree /tmp/wt414 隔离·2026-09-19）

**1. 目标与验收**：撤守卫实测 123 容器用例源码级编译零诊断（含链接运行）＋390 根治验收＋全量门禁零新增红＋自举链双 PASS（波 3 终收口）。
**2. 写集（最终）**：IR/IR类.cn（克隆节点修针 共享函数·类/函数两克隆器接入）｜IR/生成表达式.cn（裸字段下标左值路径）｜IR/生成表达式求值.cn（装箱语境兜底）｜IR/生成类.cn（当前函数返回结果名ID 静态+设/取）｜IR/生成函数.cn（同静态设置）｜plans/021/025/更新日志/HANDOFF/看板。**零 src/ 改动**（宿主等价）。
**3. 实施战果（四层根因落地·dbg 两端同帧+gdb 真栈实证）**：
- **第一层（族② 真根因）**：克隆器 遍 2 只修 子起/子终/类字段参数附加/方法返回类型——**控制流（如果/当/循环/选择）/三元/赋值复合左值 的指针字段全漏重映射**。字段语义全集已固化入 克隆节点修针 注释（如果=文本条件起+子起条件终+附加真块+子终否则链；当=文本+子起+附加体块；循环=文本init+子起cond+子终step+附加体块；选择=文本值起+附加值终+情况段；三元=子起条件+文本真段+附加假段+子终段末；赋值复合左值=附加左值起+子起左值终+子终右值终）。漏修后果=克隆体条件起点 0→[0,X) 全池野区间生成＋附加基索引→体块递归基类体。**实测：dbg偏移 99→0**。
- **第二层**：裸字段下标左值（桶数组[i]=v·字段=指针/数组）无编译路径→新路径落地（字段地址经 当前方法字段偏移+发射类字段地址·指针字段 Load 字段值/数组字段地址即基址 94-a 口径·步进=所指宽）。**实测 ×20→0**。
- **第三层（族③）**：装箱语境兜底=当前函数返回 结果/可选 类型名ID 静态（设当前函数返回结果名ID·生成方法/生成函数 每函数设置含 0 复位·不安全 函数）——克隆体语义标注发生在实例化前故 子终点=0。**实测类克隆 ×8→0**。
- **第四层（编译器自身越界崩溃）**：生成类方法们 按 泛型实例们（收集表）下标直配 泛型实例类节点们（注册表）——实例化注册双跳过路径（参数序缺失/基类不在本编译单元）=两表长度分叉→注册表 .元素 OOB（gdb 真栈：__cn_runtime_error←向量$整64$元素←生成类方法们）。修复=注册表自遍历（克隆类节点自身实例名剥基名匹配）。
**4. 验证链（进行中）**：撤守卫实测命令=`主 <123主.cn> linux-x86_64` 2>log 数 [ir2] 行；诊断阶梯 99 偏移+20 下标+8 装箱+4 KV+1 OOB → 残 15（全收敛至 raw-$T 函数克隆与向量类克隆零星野走）；守卫恢复 rc=0 ✓；全量门禁跑测中。
**5. 边缘预判→回填（诚实·431-b 继续）**：①**向量类克隆体仍有零星基区野走**（类型大小 T ×9·dbg走 实测野区间 [439,3211)=基克混合·嫌疑=延迟绑定展开克隆或未覆盖的指针字段族）②**raw-$T 函数克隆（421-c 延迟绑定中间态）不该生成而被生成**（查找值位置$T 残诊断·生成策略=仅绑定实例入 生成遍历）③123 零诊断+390 终验收未达④WIP 提交 4b813ee 含全部门禁印记（已清·见 6'）。
**6'. 下一轮（431-b）收口清单**：①向量子克隆野走定位（生成如果 文本字段疑未重映射于延迟绑定路径）②raw-$T 克隆跳过生成策略③撤守卫 123 零诊断复测④456 用例上架（泛型函数延迟绑定·含 下标左值/装箱语境/类型大小 三族负测转正）⑤全量门禁+自举链双 PASS+六件套+integrate。
**7. 与他机交叠**：无（写集全在 CN语言编译器v2/ 深度机独占攻坚面+本机文档分区）。

### 2.74 向量类克隆野走定位＋raw-$T 跳过＋撤守卫 123 零诊断终收口——432-b 开发轮（分支 `任务/深度机-432-向量野走收口`·基点=430 收工头 db6c546 接力·worktree /tmp/wt414 隔离·2026-09-19；430 轮后跳号=家机已占 431）

**1. 目标与验收**：①向量类克隆体零星基区野走定位+根治（类型大小 T ×9·野区间 [439,3211) 基克混合·基 读取 体被走）；②raw-$T 函数克隆（421-c 延迟绑定中间态）跳过生成；③**撤守卫下 123 容器用例源码级编译零诊断（含链接运行）＋390 根治验收**；④456 用例上架（三族负测转正视进度）；⑤全量门禁零新增红＋自举链双 PASS＋六件套。
**2. 写集（预登记）**：CN语言编译器v2/IR/IR类.cn（修针补漏/raw-$T 跳过）｜CN语言编译器v2/IR/生成*.cn（若定位指向消费侧）｜plans/021/025/更新日志/交接/看板。零 src/ 改动。
**3. 实施设计**：①定位=克隆节点修针 的 如果/当 分支加 TEMP 前后值打印（区间+实例过滤）→一跑锁定漏映射字段/路径；②按根因修（嫌疑序=延迟绑定展开克隆路径→未覆盖指针字段族→消费侧基区读取）；③raw-$T=实例化函数注册/延迟绑定展开处 中间态实例（实参含泛型参数名）不入 泛型实例函数节点们（生成遍历只吃绑定实例）；④撤守卫=是标准库模块 返回 偙→123 逐用例实测→恢复。
**4. 验证链**：撤守卫 123 零诊断+452/455 回归+全量门禁+自举链+strict/matrix。
**5. 边缘预判**：raw-$T 跳过后调用侧决议须仍命中绑定实例（签名键 同源公式）；野走若出自基类本体生成残留路径则补分派守卫。
**6. 与他机交叠**：无（家机 431=C20批3 写集零交叠·bf80f36 对账）。

**【收工回填（434-b）】**：**79"回归"定性=守卫补丁遗留假象**（433-b 续作重上 撤守卫 TEMP 未撤即提交→v2p 重建带守卫关→自举编译加载 stdlib 源码→嵌套泛型实例 向量$映射$整64$符号 进函数路径参数序错位〔撤守卫前沿固有·非七修代码回归〕）——撤补丁后自举编译全净 rc=0。**全量 E2E 453=451/0/2 零失败**（含 456 上架 PASS·79/78 自举链双 PASS）＋strict 100%＋matrix 83/83＋守卫恢复态 123 rc=0。**残留（已登记非阻断）**：撤守卫态 390 编译零诊断但运行段错误（源码级加载前沿下一层）。教训：TEMP 补丁与提交必须同轮对账（第三次同族）。

### 2.75 撤守卫 123 运行期收口＋390 终验收＋456 上架——433-b 开发轮（分支 `任务/深度机-433-追加store收口`·基点=432 收工头 ffe9e95 接力·worktree /tmp/wt414 隔离·2026-09-19）

**1. 目标与验收**：①撤守卫态 123 运行期 向量$整64$追加 残留 store 崩溃收口；②123 链接运行全通＋390 根治验收；③456 用例上架（视进度）；④全量门禁零新增红＋自举链双 PASS；⑤六件套＋integrate 428/429/430/432 链。
**2. 写集（预登记）**：CN语言编译器v2/IR/*.cn（按根因）｜tests/e2e/456_*（视进度）｜六件套。零 src/。
**3. 实施设计**：撤守卫复现→gdb 崩点上下文（追加 this/字段值/扩容序列）→对照宿主 asm 或与 v2p 同方法→按根因修→全链复测。
**4. 验证链**：123 链接运行 rc=85＋452 全量＋自举链＋strict/matrix＋handoff/sync。
**5. 边缘预判**：残留崩溃或牵出 构造调用链/扩容重新分配 的第二层（修一层验一层）。
**6. 与他机交叠**：无。

### 2.76 撤守卫运行级收口（**接收者求值链容器/值结构体分派完整修**＝撤守卫态 111/390 SEGV 终验收·D42 波 3 收口）——448 开发轮（分支 `任务/深度机-448-撤守卫运行级收口`·基点=435 链收工头 942cb62 接力·worktree /tmp/wt414 隔离·2026-09-19；446/447 已被占用=本机 UAF 修复分支/家机九链集成轮）

**1. 目标与验收判据**：**撤守卫（stdlib 源码级加载）态 111/390 运行级全绿**——445-b 残留收口。**验收**：①撤守卫态 111 运行 rc=0 且输出与宿主对照一致（445-b 铁证=元素值 10 被当指针解引用·乙.元素(0) 读链）；②撤守卫态 390 运行 rc=0（同族）；③123/456 撤守卫态复验不回归；④生产态（守卫恢复）全量 E2E `--jobs 8` 零新增红+自举链 78/79 双 PASS+strict 100%+matrix 83/83；⑤反证（回退分派修→111 复现 SEGV→恢复复绿）；⑥六件套+integrate 集成。

**2. 写集（精确文件清单·预登记）**：CN语言编译器v2/IR/IR类.cn（IR 181~183 解码分派·443-b 修法挂点）｜CN语言编译器v2/IR/生成*.cn（若定位指向消费侧）｜CN语言编译器v2/语义检查类相关（若需类型判定辅助）｜plans/025 本节+总览行｜plans/021（戳+索引+残留销项）｜交接/更新日志本机节｜看板本机行。**零 src/ 改动**。

**3. 实施步骤**：①撤守卫态复现 111 运行 SEGV（TEMP441b IR 转储就绪·282 条）；②按 443-b 定性修=**接收者/字段读分派容器优先**——IR 181~183 解码处值结构体槽路径误用于容器实例（读帧内槽区·值 10 被当基址）：解码时先查接收者静态类型是否容器/类实例（查当前函数变量类型+布局表）→容器实例走容器路径（句柄槽=对象指针）·值结构体走槽区路径；③逐层复测 111→390→123→456（修一层验一层）；④生产态守卫恢复全量门禁；⑤反证+六件套+integrate。

**4. 验证链**：撤守卫态 `v2p tests/e2e/111_*/主.cn` 运行 rc=0 对照宿主｜390 同｜生产态全量 E2E --jobs 8｜自举链 78_v2/79_v2｜check_spec_coverage --strict｜check_matrix_coverage｜check_handoff｜check_progress_sync｜反证（回退→SEGV 复现→恢复→rc=0）。

**5. 边缘情况与诚实边界（预判）**：①接收者链可能牵出第二层（方法返回容器/嵌套成员链/容器字段读链）——修一层验一层·若新层面新立案如实登记；②撤守卫态与生产态全量门禁分开跑（守卫切换改 v2p 指纹→全量重编译 ~30 分钟·445-b 教训维持）；③解码分派须查「当前生成函数变量类型」跨阶段可见性（444-b 双源类型解析同源）——若类型不可见走兜底分派需保守（宁走容器路径不野解引用）；④dbg 打印拼布尔先转数值（坑维持）。

**6. 与他机交叠分析**：家机 447 集成轮（429~439 九分支串行集成+446 merge+437 单测迁移+459_v2 期望迁移）——其 v2 树写集=434 解析语句.cn/436 语法分析.cn/438 词法分析.cn/446 词法分析.cn；**本机写集=IR/IR类.cn（435 链文件·429~439 分支未触及）→文件级零交叠**；语义耦合=家机 447 集成后 develop 的 v2p 词法/解析变更与本轮 IR 解码分派正交（词法管源→token·IR 解码管 IR→汇编）；单位机离线（周一 329-D12 自集成+补验）。集成序协调=家机 447 先（其 rebase 我收编的 bbded82）·本机 448 随后（rebase 最新 develop）。


**【448 首战回填（侦察轮·WIP）】**：撤守卫态 111 SEGV rc=139 复现 ✓；**崩链 asm 三层铁证**（元素方法克隆体 bb3_8：44#1=this.数据 字段读✓→**44#2 多余解引用**（基址=44#1 结果=数据指针→得数据[0]=10）→常量 8→imul→add→44#3 SEGV（r10=0xa gdb 实证））——**根因=「数据」指针字段读后、下标基址前多解一次引用·44#2 发射点=克隆体平行生成路径**（421-a 定性延续）；排除清单五项 dbg 实证（94-a 数组桶/重取基址否则桶/下标访问指针路径/越界检查/字段读次数）全清；TEMP438c 转储未覆盖克隆体元素方法（282 条无乘/加）——**449=克隆体方法生成入口锁定（实例化注册 类实例分支）→44#2 对号→修复→撤守卫 111/390 全绿**。dbg 全清+守卫恢复+工作树零残留。

**【448-b 收工回填（定位轮→44#2 对号根治 ✅·2026-09-20）】**：
**1'. 根因定案（dbg 三层+asm 铁证·一击闭环）**：**「克隆体平行生成路径」真相=类对象拷贝初始化实参 ABI 分叉**——撤守卫态（stdlib 源码级加载）下 向量$整64 实例注册为**用户类**（416-a 波 2），主函数 `向量<整64> 乙 = 甲;` 萰 **生成类对象局部 419 分支**（438-b 根①·类对象路径）而非 容器路径（生成容器拷贝构造）——该分支实参位1 **直传 生成表达式 结果（甲 的对象指针）**；而克隆体形参侧 `向量& 其他` 引用槽模型（发射引用参数读 容器& 分支=**双重解引用**：Load 槽→LoadPtr=对象指针）——**两侧 ABI 分叉**→形参侧把对象指针多解一跳（读得 数据 字段值=数据指针）→`其他.数据[索引]` 下标基址=元素值 10→SEGV（r10=0xa·与 448-a 铁证逐字节吻合）。**普通方法调用不崩的原因**=其引用实参走 回扫结构体与引用实参 表驱动（函数实参引用们·917 行）取地址回扫=两侧自洽；**唯独类对象拷贝初始化分支绕过引用 ABI**。定位法=逐节点 dbg（生成表达式 主循环 [dbgN]）+字段访问 dbg（[dbgFA]）+引用参数读分支 dbg（[dbgRef]）+调用点/克隆体 asm 对照（7440 行 call 实参 mov rsi=[rbp-176]（对象指针）vs 形参侧 LoadPtr）——**dbg 打印纪律两坑**（教训入册）：①cn build --output 产物已存在时**失败也留旧产物=缓存假绿**（dbg 三元类型错导致真编译失败三次被掩盖·验证 dbg 生效须核产物 hex 字节串/删产物强编）；②长拼接链中的**三元表达式运行时静默失败**（dbgN 复杂版零输出·最简版 825 条恢复——dbg 一律最简短打印）。
**2'. 修复（生成类.cn 419 分支·+44 行）**：实参位1 改传**源左值地址**（宿主 ABI 同款 &源槽）——源装载三分支（**生成容器拷贝构造 同款单一归属**）：标识符源=引用槽 Load/静态 静态地址/普通槽 取地址；非标识符右值源=hold 槽物化+取地址（③ 容器拷贝构造分支 同构·指针地址化零对象拷贝=性能无损）。
**3'. 验证链（全绿）**：①撤守卫态 111 运行 **rc=0 全输出正确**（甲3/乙3/乙[0]=10/乙[2]=30/深拷语义逐行）+390 rc=0（13 行）+123 rc=85（预期 21333 低 8 位）+456 rc=27（预期）——**撤守卫族 A 运行级全通**；②**反证双态**：回退修复→111 rc=139 SEGV 复现→恢复→rc=0 复绿；③生产态（守卫恢复）构建零警告+单测 **1363/1363**+全量 E2E `--jobs 8` **464=462/0/2 零新增红**（2 跳=平台固有）+自举链 78/79 双 PASS；④dbg 全清（TEMP448b 实验件净零·TEMP435b2/438c=分支既有 WIP 载荷维持）+守卫恢复原名单。
**4'. 写集（最终）**：CN语言编译器v2/IR/生成类.cn（419 分支实参 ABI 对齐·+44 行）｜plans/021（戳+§五索引+448 行回填）｜plans/025 本节｜交接/更新日志本机节｜看板本机行。零 src/。
**5'. 诚实边界**：①455 同轮并行确认 T62 实质销项（349-a 已修·登记漂移·零代码）——455 分支承载其行勾；②419 分支 hold 物化分支（非标识符右值源）未被 111/390 实弹触达（现有用例全=标识符源）——形态覆盖诚实边界·同构 ③ 分支已生产验证（E2E 全量覆盖）兜底；③win/arm64 复验归家机/单位机（try-build）。
**6'. 下一轮候选**：D42 波 3 终验收（撤守卫 390 链接运行+455/456 全绿收口·改借链迁移评估）或 stdlib 供给模型切换（stdlib/ 目录逐步源码级）。

### 2.77 112_v2 期望迁移+dbg 残留全清（**家机 447 呈报红处置**＝§8.6 归 448 轮·448-b 同分支续作）——448-c 开发轮（分支 `任务/深度机-448-撤守卫运行级收口`·头 61d3ec2·worktree /tmp/wt448 隔离·2026-09-20）

**1. 目标与验收**：家机 447 集成轮呈报唯一真红 `112_v2_方案A_有析构无拷贝构造负测`（红区=IR类.cn 收集/守卫+[dbg收文] 打印残留 IR类.cn:295 stderr）归本机 448 撤守卫收口轮处置。**验收**：①112_v2 在 448 生产态单跑 PASS；②dbg 残留四件全清（stderr 卫生）；③全量门禁零新增红+自举链双 PASS；④反证双态。
**2. 根因定案（复现实测）**：431-a 上架 112_v2 时期望固化=v2p 当时行为 sem5「未声明标识符: 释放（行=11）」×2；**435 链 stdlib 源码级加载后「释放」已声明**（dbg 收文铁证=释放 基名ID=40 入表）→sem5 零诊断=**正确演进非缺陷**；负测语义保持=`资源 乙 = 甲;` 拷贝初始化仍被拒——**ir2「类对象初始化仅支持构造调用」错误码6**（拦截点后移·防线未失守）。[dbg收文]×5+TEMP435b2/TEMP438c/TEMP441b 三块 IR dump=435/438/441 攻坚区遗留（448-b 回填 3'④「分支既有 WIP 载荷维持」的债务兑现）——污染所有 v2p 编译 stderr。
**3. 处置（两件）**：①112_v2 主.expected 迁移（**459_v2 同款先例·实跑固化**）：新锚=[p6h] 节点=30+[ir2] 类对象初始化仅支持构造调用（类 资源）+运行时错误(错误码6) 三行；②dbg 残留四件全清：IR类.cn:295 [dbg收文] 无条件打印行+生成类.cn 盒堆/向量 IR dump 两块（TEMP435b2/TEMP438c）+生成函数.cn 主函数 IR dump（TEMP441b）——grep tests/scripts 零依赖确认（期望行无一引用 dbg 输出=零误伤）。
**4. 写集（最终）**：CN语言编译器v2/IR/IR类.cn（-1 行）｜CN语言编译器v2/IR/生成类.cn（-16 行）｜CN语言编译器v2/IR/生成函数.cn（-9 行）｜tests/e2e/112_v2_方案A_有析构无拷贝构造负测/（新目录 3 文件·v2闭环.txt+主.cn 自 431-a 原样·主.expected 迁移）｜plans/025 本节。零 src/。
**5. 验证链**：①单跑 112_v2 PASS（dbg 清理前后两态实测：FAIL 复现→清理+固化→PASS 复绿=**反证双态**）；②全量门禁（生产态）单测 1363/1363+E2E `--jobs 8` 465=463/0/2（**含 112_v2 首次入本分支门禁集**·零失败零新增红）+strict 100%+matrix 83/83+自举链 78/79 双 PASS。
**6. 与他机交叠**：**431-a 分支同文件交叠**（tests/e2e/112_v2/ 主.expected）——448 集成先则 develop 已有迁移版，431 链 rebase 时该文件 modify/add 冲突**取 448-c 版**（期望随 435/448 演进·@家机注意）；447 rebase 重测=112_v2 红即消（家机集成序 448→447 维持）；456/450/452（家机）写集零交叠。
### 2.78 T62 三例无诊断硬错误→可恢复诊断改造（**v2p 302/rc=1 零诊断残余面收口**）——455 开发轮（分支 `任务/深度机-455-T62优雅拒绝`·基点=develop 21e5669·worktree /tmp/wt455 隔离·2026-09-20·同机多开②）

**1. 目标与验收**：T62（plans/021 §三-G·356-a 立案）——349-a 修两路径（运算符函数名+`->` 后缀位）后仍有第三触发：**135_箭头成员访问／340_运算符重载参数边界／92_单目重载 → v2p 编译 rc=1 零诊断**（违反优雅拒绝基线）。**验收**：①三例 v2p 编译产出可恢复诊断（诊断+语法错误加一+聚合中止·349-a 语义）；②新负测用例门禁化（或 445 系扩面）；③全量门禁零新增红＋自举链双 PASS；④反证（回退改动→rc=1 零诊断复现→恢复→诊断出现）。
**2. 写集（预登记）**：CN语言编译器v2/语法/解析表达式.cn（或解析语句.cn·按探针定位）｜tests/e2e/新负测用例｜coverage_map.md｜六件套（plans/021/025/交接/更新日志/看板行）。零 src/。
**3. 实施步骤**：①三例探针复现（v2p 编译 135/340/92 确认 rc=1 零诊断+记录触发源片段）；②探针二分定位触发 token 序（最小源片段收缩）；③349-a 模式改造（该 token 位=可恢复诊断+位置前进防死循环+聚合中止）；④负测用例（期望=诊断锚行）；⑤全量门禁+反证。
**4. 验证链**：三例单跑（诊断文本断言）＋全量 E2E `--jobs 8`＋单测 1363＋strict/matrix＋自举链 78/79＋反证双态。
**5. 边缘情况与诚实边界（预判）**：①诊断改造不得误伤既有合法形态（后缀链/成员访问存量面全量回归把关）；②349-a 已修两路径的 445 用例不回归；③三例若触发点不同源则逐例修（不强行单一归属）；④与家机 451-T63（v2 parse 缺分号判定·解析语句.cn 面）潜在同目录交叠——开工前 grep 其在飞分支写集核对，若同文件则看板协调或让位。
**6. 与他机交叠**：448（本机同开·IR/生成面）文件级零交叠；家机 451-T63 待核对（语法目录同域·不同判定域）；家机 452-C20批5（纯 tests）零交叠。

**【455-a 收工回填（侦察即收工·T62 实质销项确认·零代码）】**：开工探针（develop 生产态 v2p）三例实测——135_箭头成员访问=`[parse] -> 成员访问已废除（统一 .，指针自动解引用一级）`+340/92=`[parse] v2 暂不支持运算符重载声明（类 -/+/!）`全 rc=1 负路径优雅拒绝——**349-a 两路径修复已完整覆盖 T62 所述「第三触发」**（T62 立案时点早于 349-a 集成=旧口径）。**定性=与 T33 先例同款「修复已发生·登记未勾」登记漂移**——零代码确认销项·021 T62 行已勾。诚实边界：未加新负测（三例诊断由 349-a 时点用例承载=445 系+135/340/92 宿主负测既有面·v2 侧诊断锚已在库）。

**【455-b 门禁语义修正（机制级·全机集成阻塞根治·契约广播）】**：integrate.py 快速门禁 check_progress_sync 对 455 实测拦截——**git 历史轮次基准把他机窄通道直推（家机 456/457 看板先行·未集成的认领号）计入「最新轮次」**，而 021 戳随开发轮集成更新→窄通道天然快于集成→**任何开发轮集成被结构性拦截**（448 集成亦必撞）。修正=提取时排除信息含「（窄通道）」的提交（语义修正非放松：窄通道不改变 021 状态·他机未集成认领号非本机欠账）。反证双态（还原→rc=1 复现/修正→rc=0）+正例 455 同步 ✓。**契约广播（§8.6）**：check_progress_sync.py 三机共用——贵机 pull 后即新版；正在集成的分支若遇同款拦截 pull 即解。

### 2.79 撤守卫态全量 E2E 终验收（**D42 波 3 收口最后一步**＝stdlib 源码级加载态全量实测暴露面→逐缺口定性→全绿则提生产态切换）——460 开发轮（分支 `任务/深度机-460-撤守卫全量终验收`·基点=develop ae41452·worktree /tmp/wt460 隔离·2026-09-20）

**1. 目标与验收判据**：448-b 撤守卫态四例（111/390/123/456）运行级全通后，**stdlib 源码级加载（撤守卫）态的全量 E2E 实测**是 D42 波 3 收口的最后一块。验收=①**暴露面清单完整**（撤守卫态全量 E2E `--jobs 8` 红用例逐例定性：A=期望漂移〔v2 行为正确演进·459_v2/112_v2 先例·迁移+反证〕B=v2 真缺口〔立案 plans/021 排班·不在本轮硬修〕C=环境噪音〔重跑甄别〕）；②**若零红**→生产态切换直做（主.cn 守卫名单撤除=v2 树正改·生产态全量门禁+自举链全绿随轮集成）；③**若有红**→守卫恢复零残留（实验态不入库·缺口清单立案排班·诚实边界收口）；④反证=守卫恢复态全量绿对照（既有基线）。
**2. 写集（预登记）**：CN语言编译器v2/主.cn（守卫撤/恢复实验位·终态按实测定）｜tests/e2e/（仅当暴露面含期望漂移 A 类·逐例迁移+反证）｜plans/025 §2.79｜plans/021（戳+§五索引）｜交接/更新日志本机节｜看板行。零 src/。
**3. 实施步骤**：①wt460 撤守卫（是标准库模块 返回 假·TEMP 注记）；②重建 v2p（指纹变化·stdlib 10 模块源码级编译入 v2p）；③**撤守卫态全量 E2E `--jobs 8`**（大实验纪律：ulimit -v 6GB 包裹+runner 内存名单）；④红用例逐例单跑定性（v2 产物输出 vs 宿主 vs .expected 三方对照）；⑤缺口三分类处置；⑥全绿→生产态切换+全量门禁；⑦六件套+integrate。
**4. 验证链**：撤守卫态全量 E2E `--jobs 8`＋单测 1363＋strict/matrix＋自举链 78_v2/79_v2（撤守卫态=v2 编译 v2 树自身 stdlib 也源码级·自举链行为实测）＋反证双态（撤/恢复对照）＋红例单跑定性记录。
**5. 边缘情况与诚实边界（预判）**：①撤守卫 v2p 构建耗时+（stdlib 源码级全编译）；②stdlib 内部模块依赖加载序风险（容器←?）；③自举链 78/79 撤守卫态行为未知（本轮实测点）；④**暴露面若 >20 红→本轮收口为定性清单轮**（根治拆后轮·不硬凑）；⑤E2E 大实验内存保护（教训 306-a OOM·runner 名单+ulimit）。
**6. 与他机交叠**：家机 454-T63（v2 parse）/459-波4（宿主 src）/457/458（arm64/x64 codegen）写集零交叠（本机=v2 树 主.cn 守卫位+tests 期望面）；448 已集成=基线干净；本分支独立可随时集成（写集触 v2 树+tests→全量门禁·三平台 try-build 点名）。

### 2.80 T70 面①②根治（**撤守卫生产化逐面收口**＝泛型方法实例类型大小替换+容器赋值类路径右值判定）——462 开发轮（分支 `任务/深度机-462-T70面1面2根治`·基点=develop 815e368·worktree /tmp/wt462 隔离·2026-09-20）

**1. 目标与验收判据**：T70（460-a 立案·plans/021 §三-G）面①（240=泛型方法实例内 类型大小 表达式位替换缺口·ir2「未知类型 盒子〔宿主=向量$盒子$插入#整64,盒子〕」）+面②（283/441/442_v2=容器赋值类路径「结构体赋值右值（调用返回）非结构体返回值」判定缺口）撤守卫态根治。验收=①撤守卫 v2p 下 240/283/285/441/442_v2 单跑全 PASS；②生产态（守卫恢复）全量门禁零新增红+自举链双 PASS；③反证双态（回退→红复现/恢复→绿）；④零误伤（26_generics 族/442_v2 生产态族不回归）。
**2. 写集（预登记）**：CN语言编译器v2/IR/{IR类.cn 或 IR布局.cn 或 生成类.cn}（面①类型大小替换·按定位）｜CN语言编译器v2/IR/生成语句赋值.cn 或 生成类.cn（面②右值判定·按定位）｜tests/e2e/（若需新形态负测/正测补强·最小）｜plans/025 §2.80｜plans/021（T70 行更新+戳）｜交接/更新日志本机节｜看板行。零 src/。
**3. 实施步骤**：①撤守卫 v2p 探针复现（240 完整 ir2 诊断+源位定位）；②面①定位=方法实例克隆体的 类型大小 消费点（422-a 类版标识符替换块同构·克隆器标识符位 vs 表达式位）；③面②定位=容器赋值类路径右值判定（生成语句赋值·类对象路径对调用返回右值的分派）；④修复+撤守卫态逐例验证（240→441→442_v2→283→285）；⑤恢复守卫+生产态全量门禁；⑥反证+六件套+integrate。
**4. 验证链**：撤守卫 v2p 五例单跑+生产态全量 E2E `--jobs 8`+单测 1363+strict/matrix+自举链 78/79+反证双态。
**5. 边缘情况与诚实边界（预判）**：①面①若牵出 泛型函数实例 同族缺口（67/68·面⑤）→定性登记不在本轮硬修；②面②修复不得破坏 448-b 实参位修复（419 分支回归面重点）；③237（面④值差）若与面①②同根顺带销·否则维持立案；④285（面③复制内置）视面②修复后状态（可能同根顺带消）。
**6. 与他机交叠**：家机 454/458/459 写集（v2 parse/x64 codegen/宿主 src）零交叠；本机 448 已集成；集成序=独立（写集 v2 树+可能 tests→全量门禁·三平台 try-build）。

**【462-a 收工回填（面①根治+面②两处根治·5 例销项）】**：
**1'. 三处修复（写集=v2 树 2 文件）**：①**面①根治**（生成类.cn 克隆递归）：生成类方法们 的克隆递归分支模块路径实参 基类文件段路径→**"主"**——克隆体生成窗口=实例化点模块（合并单模块语义·C4 核验口径）·与泛型函数实例生成传 "主"（398 行）同构——克隆体内类型查询（类型大小/字段布局分桶键）不再按容器模块域解析（240 外盒 miss 根因）。②**面②根治 A**（生成表达式求值.cn ④ 分支）：生成复制展开 结构体分支物化基址**补登记 结构体返回临时们**（与 生成调用 1152·82-a 链同款单一归属）——漏登记=348-a D11 三通道查源 miss 根因。③**面②根治 B**（同文件 432-b）：复制 展开实参类型推导兜底改**实参段末（链尾）推导**+池界防御（子终点 双语义·越域回退链根零回归）——原传链根只推得形参类型（向量$外盒≠元素 外盒）→③ 容器分支错入。
**2'. 验证（撤守卫态）**：**10 红→5 红**（240/283/285/237/442 五例销——含面③④⑤顺带销：285 复制内置/237 链表值差/67·68 编译面）；手动 v2p 单跑 240 零诊断。**生产态零回归**：单测 1363/1363+全量 E2E 476=474/0/2 零失败+自举链 78/79 双 PASS。**反证**：面①回退→撤守卫 240「类型大小: 未知类型」×4 复现→恢复复绿。
**3'. 残余 5 红（再定性·T70 行更新）**：①**面②'**（441）=T57 通道（生成容器对象整体赋值）右值形态守卫=标识符 only——`装 = 向量<整64>()` 构造调用右值/成员左值形态缺位（生产态容器内置路径不触发）→扩形态独立轮；②**面⑤'**（67/68）=编译已通**产物运行 SEGV**（460-a 定性收窄：非实例化交互·运行级）；③**面⑥**（78/79）=fix_p 编 v2 全树 rc=1（解析 IR常量.cn 6067 节点后死·自举撤守卫硬前置）。
**4'. 诚实边界**：①链尾推导对「标注成功但不可信」形态回退链根=保守（错位形态维持原行为·不劣化）；②467/68 运行级未逐指令归因；③面⑥ 死因粒度同 460-a。
**5'. 下一轮候选**：464=T70 残余（面②' T57 通道扩构造调用/成员左值形态+面⑤' 运行级定位+面⑥ 自举 fix_p 专项）——撤守卫生产化收口续。

### 2.81 T70 残余收口（**撤守卫生产化最后一公里**＝面②' T57 通道扩形态+面⑤' 产物运行级定位+面⑥ 自举 fix_p 专项）——464 开发轮（分支 `任务/深度机-464-T70残余收口`·基点=develop d617f33·worktree /tmp/wt464 隔离·2026-09-20）

**1. 目标与验收判据**：462-a 后撤守卫态残余 5 红（441/67/68/78/79）收口。验收=①面②'：T57 通道（生成容器对象整体赋值）扩「右值=构造调用」与「成员左值」形态→441 撤守卫态转绿；②面⑥：fix_p 自举死因定位→根治（若单点）或精确立案（若系统性）；③面⑤'：67/68 产物运行 SEGV 定位→根治或精确立案；④生产态全量门禁零回归+反证。
**2. 写集（预登记）**：CN语言编译器v2/IR/生成语句赋值.cn（面②' 通道扩形态）｜CN语言编译器v2/其他 IR 文件（面⑥ 按定位）｜plans/025 §2.81｜plans/021（T70 行+戳）｜交接/更新日志本机节｜看板行。零 src/。
**3. 实施步骤**：①面⑥ 侦察（撤守卫 v2p 手动编译 v2 全树拿完整死因·dbg 单点）；②面②' 实施（构造调用右值=指针接管·成员左值=基址寻址；对齐 T57 既有先释放语义）；③面⑤' 定位（67 产物运行 gdb/输出二分）；④撤守卫态全量复测（残余→0 为目标·不可达则精确立案）；⑤生产态全量门禁+反证；⑥六件套+integrate。
**4. 验证链**：撤守卫态全量 E2E `--jobs 8`（残余清零进度）+生产态全量 E2E+单测 1363+strict/matrix+自举链 78/79+反证双态。
**5. 边缘情况与诚实边界（预判）**：①面⑥ 若=系统性缺口（自举撤守卫需多轮）→本轮交付精确定性+路线图（诚实边界收口）；②面②' 构造调用接管须防临时 RAII 双释放（88-a 净栈契约）；③面⑤' SEGV 若涉 82-a retbuf 深水面→立案不硬修。
**6. 与他机交叠**：家机 454/458/459 写集零交叠；本分支独立集成（v2 树→本机门禁+他机 pull 自然验证）。

**【464-a 收工回填（面⑥ 推进两层+嵌套拆段根治）】**：
**1'. 两修复**：①**sem5 模块名根放行**（语义检查表达式.cn）：stdlib 源码级模块的内部实现调内置成员（IO.cn 行 44 `IO.打印到错误`·根=内置模块名）——检查标识符 补 是标准库模块名(名文本)→-1 占位放行（成员决议=IR 内置表·生产态 stdlib 源码不编译=零触发·对齐 IO.cn 设计注释「内置走限定名直调」）——fix_p 自举 sem5 层通过。②**嵌套泛型感知拆段**（IR类.cn 剥实例参数段）：段首命中基类参数序(段数 k)则吞 k-1 后续段——`向量$映射$整64$符号`→实参=[映射$整64$符号]（原纯 $ 拆=[映射,整64,符号] 错位→「实参与参数序不匹配」错误码6）——**生产态用户泛型类嵌套实例同款受益**（既有拆错·无用例触达=顺带根治）。
**2'. fix_p 推进轨迹**：sem5「未声明标识符: IO」（行 44=IO.cn 自身实现）→sem5 过→ir2「泛型实例实参不匹配」→过→**ir2 三缺**（①「结构体赋值右值（调用返回）…导入项平铺名们/导入项平铺限定们」×2=容器赋值×调用右值=**面②' 同根**〔441 同修〕②「方法调用的对象类型未知: 打印到错误」=IR 层模块根成员调用点形态缺位）。
**3'. 验证**：生产态单测 1363/1363+全量 E2E 476=474/0/2 零失败+自举链双 PASS（两修复零回归）。
**4'. 下一轮（466）路线**：面②' 容器/类对象赋值扩「右值=调用（构造/一般）」形态（接管/深拷·覆盖 441+fix_p 双缺）+IR 模块根成员内置直调（fix_p 第三缺）+面⑤' 67/68 运行级——三件齐=fix_p 全通→撤守卫态自举链绿→撤守卫生产化进入实测切换评估。
**5'. 诚实边界**：面⑥ 自举=连环缺口逐层暴露（每修一层进一层）——系统性收口按轮推进·本轮两层+独立缺陷一枚为完整闭环交付。

## 三、单位机 linux-arm64 节（本机维护）

### 3.21 本轮（**341-a 制度轮·🏃 轮次号「先到先得」落地（用户裁决方案①）**·基线 620831b·分支 `任务/单位机-341-撞号防线`·2026-09-18）——开工计划（六要素）

**1. 目标与验收**：跨机轮次号撞号根治（用户 2026-09-18 裁决方案①「认领前先看板、先到先得」）。**验收**：①AGENTS §8.1 占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；②check_handoff 新增「在飞轮次号唯一性」机械检查（跨机同号拦截）+正反例实证；③**plans/025 膨胀事故同轮根治**（实测 1780 行·§二/§三 被拼接 4 份→标题级去重保留最新→493 行）；④本轮自身按新制度实践（**先直推看板占号 341 再开工**——新制度首次完整闭环）。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 分支命名条）｜scripts/check_handoff.py（查看板() 扩「在飞轮次号唯一性」）｜plans/025（去重+本节）｜plans/021（戳+§五索引）｜HANDOFF.md／更新日志.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①看板直推占号 341（**已完成**）；②AGENTS 条文（占用权威=看板在飞行）；③check_handoff 检查实现+实证——**首版正则漏大写字母致反例未拦**（`340-D38`/`329-D12` 提取为 []）→ 修为 `[a-zA-Z]|中文` 后三情形实证过；④plans/025 去重（标题级·保留最后出现版本·断言零重复标题）；⑤门禁+integrate 集成。

**4. 验证链**：check_handoff 正例 0／反例（家机行改 341）拦截 1（报「在飞轮次号 341 被多机占用」）／还原 0；plans/025 去重断言（无重复 `###` 标题）；integrate.py 快速门禁（纯文档+平台无关脚本写集·按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界**：①轮次号提取正则须覆盖大小写/中文标识（本次首版 bug 当场实证发现并修）；②plans/021 恰 600 行上限（瘦身登记下轮·用户已提）；③plans/025 膨胀根因=集成/rebase 时「拼接而非替换」（本轮去重为止血；机械防线=`### 标题重复`检测建议下轮加——与已上线「## 节标题重复」检测同族）；④去重保留「最后出现」版本，三机节内容请两机集成后复核。
**6. 与他机交叠**：家机 340-a／深度机 338-a 在飞（写集=v2 树/src 面）与本轮（文档+门禁脚本）**零文件交叠**；plans/025 三机共用（§一/§二 保留最新版·集成后在通告段请两机复核本机节）。


### 3.20 本轮（**332-a 修复轮·🏃 看板先行机械强制（用户令）**·基线 044423b→集成 b45e4ae·分支 `任务/单位机-332-看板强制`·2026-09-18）收工回填：**制度缺陷根治（开工先填板/收工即销账·机械强制）**

**收工回填（实测·目标全达成）**：①AGENTS §8.1「看板先行」（开工第一动作=直推看板本机行→才开分支；收工集成成功即清行直推）＋§8.5「强制直推、即改即推」（看板行/通告段/try-build 请求三类）落盘；②check_handoff「开工自检」上线——**双向实证**：正例（本分支 0 通过）／反例A（看板不含当前分支名→拦截·含「开工自检」字样）／反例B（🏃 行缺分支名→拦截·证明新小节结构下旧校验仍有效）／还原复跑 0；**判据修正（自建制度首日自证 bug·当场根治）**：收工销账清行后分支名自然不在看板 → 加「**分支已集成（`git merge-base --is-ancestor HEAD develop`）则放行**」豁免，三情形实证（正例放行／销账态放行／未集成+无看板行拦截）；③看板表拆三机独立小节＋查看板() 适配（4 列识别+节内恰 1 行）；④集成 b45e4ae（integrate.py 快速门禁通过；纯文档+平台无关脚本写集按 §8.3 不触发 try-build）；⑤**本轮自证实测两类冲突**：单表三行结构下与深度机看板连续直推撞 rebase 冲突两次（手工解·两行版本并存）＋push CAS 竞争一次（重试复推）——结构拆分与 CAS 均为实证驱动修复。**边缘回填**：非 git 环境开工自检自动跳过（实装）；集成后清行再跑门禁的误报已由「已集成豁免」根治（本轮实证）。**诚实边界**：①332-a 自身开工时未先直推看板（旧习惯残留·已在 HANDOFF 记录并随结构同批生效）；②plans/021 达 600 行上限（卡线·下轮需瘦身）。

**1. 目标与验收判据**：把用户令（2026-09-18）「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」落为**机械强制**。**验收**：①AGENTS §8.1 新增「看板先行」强制条（开工第一动作＝直推看板本机行；收工即销账）+§8.5 直推窄通道改「强制直推、即改即推」；②check_handoff.py 新增「开工自检」（当前分支 `任务/*` 时看板必含该分支名）+**正反例双向实证**（正例放行／反例「先动手后填板」拦截）；③看板表拆为**三机独立小节**（防相邻行 rebase 冲突——332-a 当日实测两次）+查看板() 适配；④check_handoff 全绿。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 看板先行/认领/看板行格式三条·§8.5 直推窄通道+看板结构条）｜scripts/check_handoff.py（查看板() 适配小节结构＋查开工自检()＋主流程挂载）｜三机任务看板.md（结构拆分＋本机小节＋332-a 通告段）｜plans/025 本节｜更新日志.md／HANDOFF.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①AGENTS 条文（用户令原文为依据）；②check_handoff 查看板() 改小节识别（4 列）＋查开工自检()（判据＝当前分支名出现在看板任一行·`git rev-parse --abbrev-ref HEAD` 容错）；③看板结构拆分（三机节·内容原样迁移）；④门禁正反例实证；⑤文档六件套；⑥integrate.py 集成＋通告段广播结构变更。

**4. 验证链**：`python3 scripts/check_handoff.py`（正例 0）｜反例 A＝看板不含当前分支名（拦截 1·含「开工自检」）｜反例 B＝🏃 缺分支名（拦截 1·新结构下旧校验有效性）｜恢复后复跑 0｜集成门禁（integrate.py 快速门禁——纯文档+平台无关脚本写集，按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界（开工预判→回填）**：①开工自检判据＝「分支名出现在看板任一行」——跨机 try-build 在他机分支上跑时分支名在其机行内即满足（分支主人集成前不清行）；集成后清行再跑旧分支门禁会误报（罕见场景·可接受·回填实测）；②非 git 环境（打包校验）自检自动跳过；③结构变更＝三机共享契约变更 → 通告段广播，他机 rebase 时按其行取值解冲突；④本机 329-a 在飞可见性由 044423b 承载，332-a 行随本批集成更新（结构变更同批故不再单独直推）。

**6. 与他机交叠分析**：家机 329-a（v2 语义层·D38批2）不碰 AGENTS/check_handoff/看板 → 零交叠；深度机 331-a（`src/` 三后端+IR 验证器·try-build 请求中）改其本机行/通告段，与本轮「看板结构变更」**语义相邻**（他 rebase 时按新结构取本方行）——已在 332-a 通告段写明；其余零交叠。

### 3.18 上一轮（**328-a 开发轮·🏃 协议 v2 落地**·用户授权插队·基线 93b11df→7b84b93（rebase）·分支 `任务/单位机-328-协议v2`·2026-09-18）收工回填：**协议 v2 全链落地+本轮自身走新流程端到端演练**

**收工回填（实测·目标全达成）**：①AGENTS.md §8 全文重写（六总纲+§8.1~8.7；§6 三处旧引用同步+§3 缺陷零容忍分级注记）；②`scripts/integrate.py` 新增（合并队列七步·--dry-run/--try-build-done·平台感知门禁）——**实战两遇**：本地缓存（.zcode/plans/）挡前置自检→改「未跟踪文件警告放行」+.gitignore 补忽略；**家机 326-a 收工与本案真实碰撞**（develop 前进+3 文件冲突）→integrate.py 按设计自动 abort 报告→分支主人手工解冲突（plans/021 戳合并双方信息/lessons 双段保留/看板通告段双侧拼接）→rebase 完成→--dry-run 全步骤过（退出码 0）；③check_handoff.py 扩看板 🏃 行分支名校验——**反例（330 号无分支名）拦截+正例（326 豁免/328 合规）放行双向实证**；首版曾真实拦截家机 326-a 旧格式行→加「轮次号 <328 过渡豁免」（旧协议在飞轮无分支可标·收工清行自然消亡）；④plans/027 方案文档落盘（诊断/业界对照/裁决记录/细则/试点安排与复盘区）；⑤共享文档全套更新（看板说明区 v2 口径+生效广播+试点邀请/plans/021 戳+里程碑+试点行+§五索引〔瘦身 3 行回 600 内〕/lessons 设计依据条目/HANDOFF/更新日志单位机节）。

**1. 目标与验收判据（达成）**：六项验收全达成（①AGENTS 重写②integrate.py 演练过③校验双向实证④plans/027 落盘⑤本轮走新流程⑥双文档门禁 PASS·plans/021=599 行）。

**2. 写集（最终清单·11 文件）**：AGENTS.md｜scripts/integrate.py（新）｜scripts/check_handoff.py｜.gitignore（补 .zcode/plans/）｜plans/027-三机并行协同架构v2方案.md（新）｜三机任务看板.md（本机行+说明区+通告段）｜plans/021（戳/里程碑/§三-A 试点行/§五索引+瘦身）｜plans/025 本节｜HANDOFF.md/更新日志.md 单位机节｜lessons.md（追加+rebase 合并 326-a 段双保留）。

**3. 实施步骤（回填）**：按计划①~⑩全部执行；插入项=两处 integrate.py 实战修正（未跟踪文件警告放行/冲突自动 abort）+rebase 冲突解决（与家机 326-a 收工真实碰撞）。

**4. 闅证链（回填实测）**：py_compile 双脚本过；check_handoff+check_progress_sync 全过（独立验退出码·管道吞码教训在案）；反例拦截/正例放行双向实证；integrate.py --dry-run 全步骤过（前置自检/fetch/rebase 对齐/快速门禁双过/push --dry-run）；真集成=git push gitcode develop（ff-only）+github 镜像补推。写集纯文档+平台无关脚本 ⇒ 按 §8.3 分类不触发 try-build、不重跑 E2E（基线 7b84b93=家机 326-a 收工+看板修复提交承载）。

**5. 边缘情况与诚实边界（回填）**：①**并行开发实效未验证**——本轮单分支落地，试点轮（plans/027 §六）承载并行度实测；②integrate.py 首版 try-build 回签=人工通告段+旗标声明（自动解析留试点后迭代）；③集成门禁增量判定（竞争重试轮跳过全量门禁条件）尚未经历真实竞争检验；④python 中文标识符带空格=语法错误（首版两处·py_compile 当场拦截）；⑤看板 328 行在真集成成功后经窄通道清行（收工广播随通告段）。

**6. 与他机交叠分析（回填）**：家机 326-a 收工与本案**真实碰撞一次**（develop 前进+看板/plans021/lessons 三文件冲突）——按 v2 设计路径消解（分支主人自解冲突·rebase·非 develop 手解），**此碰撞即 v2 协议设计的第一个实战案例**（旧协议下=双直推冲突+可能冲突标记入库；新协议=隔离分支+集成时一次性解决）；深度机 ⬜ 全程零交叠。

**下一轮候选（预登记）**：①**协议 v2 试点**（plans/027 §六·另两机各开真实任务分支并行→复盘）；②M3 采样轮续（B8 候选行+daemon 常态）；③T24/T25 v2 树对齐轮（随家机 v2 面轮次）。

### 3.17 上一轮（**297-a 开发轮**·🏃 用户裁「按照你的方式，全部修复」·基线 478d804→认领 ab935b4·2026-09-17）收工回填：**T25/T24/B10 三案当日根治+T26 同族判定+规范条文+406/407/408 转正·全量门禁绿**

**收工回填（实测）**：①**T25 根治**：病灶精确定位=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支向 storeVirtualResult 传 w9 → emitStackStore 浮点分支 `str w9` 32 位截断写（高 32 位丢；1.5 低 32 位恰全零故读出 0.0）——修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0·asm 实证 `str d0` 64 位完整写）；②**T25 回归矩阵 27 形态全绿**（三元 9+汇合 9+最小 2+浮32 3+数组 2+显式转换口径·C 一致 O0=O3）；③**T24 根治**：visitUnaryExpr Inc/Dec 三路径（静态/成员下标解引用/主路径）+handleClassFieldIncDec(+postfix) 按后缀返回旧值（原忽略 postfix 一律新值）——16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照·O0=O3）；④**B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻（括号/嵌套 ? 深度感知·新判据⊇旧判据=存量成功程序零语义变化论证）替换 C-1 白名单——`a ? -b : c` 双级别全绿（u1~u5）+后缀传播 `r?+1`=42 不回归+E2E 60 单跑绿；⑤**T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无截断分派同型）静态审查注记；⑥**规范条文**：plans/001 §4.5 优先级表（++/-- 后缀 13/前缀 12 级+值语义）+三元消歧条文+错误传播节更新；spec 01b 五处同步；⑦**新 E2E 三件转正**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记；⑧**全量门禁绿**：零警告构建+单测 1353/1353+全量 E2E 426=424/0/2（跳=62_ffi/69 平台败面）+**78/79 自举锚定双 PASS**+E2E 60 传播复验绿；⑨看板通告段=v2 消歧同口径广播（家机 296-a 对齐）。

**1. 目标与验收判据（达成）**：六项全达成（T25/T26/T24/B10/规范/门禁——§三.17 计划①~⑥逐项 ✓）。

**2. 写集（最终清单）**：src/cn_compiler/codegen/arm64/arm64_instructions.cpp（emitCopy）、src/cn_compiler/ir/ir_expr_unary.cpp+ir_oop_field.cpp+ir.hpp（T24）、src/cn_compiler/parser/parser_expr.cpp+parser.hpp（B10）、tests/e2e/406/407/408（新·各 2~3 文件）、tests/e2e/coverage_map.md、plans/001、.zcode/skills/cn-language-spec/specs/01b、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段。**v2 树零改动** ✓（家机 296-a 写面）。

**3. 步骤（回填）**：认领（ab935b4）→T25 侦察（emitCopy+storeVirtualResult+emitStackStore 链·IR↔asm 逐环对位）→修→27 形态回归→T26 判定（arm64 短路复现=免疫）→T24 侦察（visitUnaryExpr 忽略 postfix 实锤）→修→16 形态→B10 实现（ternaryColonAhead）→6 形态+传播回归→规范五处→E2E 三件→全量门禁→销项。

**4. 验证链（回填实测）**：零警告构建（grep error|warning=0）+单测 1353/1353+全量 E2E 426=424/0/2+78/79 双 PASS+406/407/408 单跑+60 单跑+最小复现 O0/O3 双对+asm d0 写实证+双门禁（独立退出码）。

**5. 边缘情况与诚实边界（回填）**：①T26=x86_64 单侧·精确形态验证归深度机（本机 arm64 免疫实证+静态审查注记）；②win/x64l Copy 浮点路径运行级复验归各机（静态=结构不同无同型截断）；③T24 v2 树同型对齐=待家机 296-a 收工后独立轮（通告段已广播）；④三元负号+浮点汇合的单测未加（E2E 三件为强门禁·单测面留后续）；⑤i1 Copy 在 arm64 走整数路径（is64=false→w9 32 位写）——布尔 1 字节值 32 位写安全（高位无关）·实证布尔矩阵全绿。

**6. 与他机交叠分析（回填）**：家机 296-a（v2 树）零交叠 ✓；深度机 🧪（只读+cnsmith_hits）零交叠 ✓；coverage_map 潜在文本冲突=rebase 兜底未发生。

**下一轮候选（预登记）**：①**M3 采样轮⑥**（回归 B8 候选行+daemon 常态·用户令不停）；②**T24/T25 v2 树对齐轮**（家机 296-a 收工后·自举编译器同型三缺陷）；③T26 精确形态=深度机口径（B11）。

### 1.1 T33 侦查注记（340-a 顺带·为下轮铺垫）

**T33 残余实态（读侧窄宽）**：编码LoadStoreWin（代码生成.cn 46-60 区）IR_加载/IR_存储 均 64 位全宽读写（·无视 指令.类型）——385「整16 f 打印 40000 应 -25536」根因=窄类型槽读 8 字节带高位垃圾。**要点**：①293-a 存储侧截断泛化已在库（读侧配套须先读其槽写入形态）；②读侧 movsx/movzx 分派=全树高频指令改动=**产物指纹全面变化=锚定链须重锚**（大轮预算）；③三后端对称（win/X64L/ARM64 各自 LoadStore 编码函数）。**建议轮型**=独立大轮（非拼轮）·验收=385/386 双编译转正+锚定链重锚自洽+全量门禁。



### 1.1 D36 实施设计（352-b 侦查深化·为实施轮完整铺路·2026-09-18）

**局部槽消费面盘点（实测）**：获取/设置=生成声明.cn 95/102（标量分配）/263（容器）/317、IR生成.cn 534（标识符读）、生成语句结构体赋值.cn 260（读右）；块遍历挂点=生成语句.cn 66（节点_块 分派）+102/127/137（裸块/如果/当 体块收集）。
**实施设计（块新名清单栈方案）**：①静态 名清单栈 向量<向量<整64>>（生成声明.cn 文件级）——分配标量声明槽 的 获取 miss=新分配时 **push 当前栈顶清单**；②生成语句 的 节点_块 分派 **进入时 push 空清单**、遍历尾 **弹栈并逐名 局部槽.删除**；③嵌套块自然栈化（每块自清单）；④容器/结果/结构体/数组 局部 同款（生成声明 各族分配点补登记）；⑤**预扫对称**：预扫容器声明 的预分配同入块清单（标量预扫不分配=零改动）；⑥读点零改动（删除后 miss→常量查/参数槽/全局链自然回退）。
**风险与验证**：①删除后 miss 的名若被 **同函数后续块** 再声明=重新分配新槽 ✓ 语义正确；②**释放挂点**（字符串 RAII 名单/容器析构）不受影响（名单按名·槽释放由 IR_分配/块出口析构——**块出口析构顺序=删除前**须核（生成语句 块尾析构发射点在删除前执行）；③存量零误伤兜底=全量 E2E（同名遮蔽存量面=346/160 三级解析遮蔽等）+78/79 自举+锚定链重锚评估（槽号变化=产物指纹变——**须重锚评估**）。
**预算**=独立大轮（同 T33 量级）。本设计节+352-a 根因定性=实施轮输入齐备。
