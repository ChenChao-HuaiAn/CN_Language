# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 187 轮（2026-09-15，单位机 ARM64）——建表 + 三机在飞计划落盘 + 交叠/遗漏审计初版。
> （轮次号更正：本表建立轮曾写 185-a，因该号已被深度机先占，本机认领无效 → 更正为 **187-a**；D6 轮 = **188-a**。）

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **探针检测轮**（验证轮不占号） | 源码边界探测：漏洞/缺陷/规则合规/规范一致性——10 项发现落 plans/021 §三-G（3 项双侧同缺：递归结构体/字符串下标越界/超界码点；宿主单侧：嵌套栈溢出崩溃+语义 O(d³)；架构漂移：依赖方向待裁决） | 共享文档本机分区（源码零改动） | 与深度机 233-a/单位机零交叠（只读源码） | §一 | ✅ |
| 深度机 linux-x86_64 | **232-a** | 协作协议增补（用户令两项）：角色动态优先级（开发>探针>E2E）+ 看板纯状态板（空闲行干净化+门禁扩展） | `AGENTS.md` §8、三机任务看板.md、scripts/check_handoff.py、plans/021、plans/025、HANDOFF.md、更新日志.md、lessons.md | 与家机/单位机零交叠（双 ⬜） | §二 | ✅ |
| 单位机 ARM64 | **226-a** | **D8 寄存器感知发射尝试（→按纪律回退·源码零净变更）+ F1-26 调研**；下一轮候选=**F1-26 mem2reg** / D8 重做（别名·宽度契约定位）/ F2-33 | `src/cn_compiler/codegen/reg_alloc.{cpp,hpp}`、`codegen/arm64/arm64_codegen{,_emit,_dispatch}.cpp/.hpp`、`arm64_instructions.cpp`、`arm64_codegen_oop.cpp`、`arm64_codegen_i128.cpp`、`backend_factory.cpp`、`tests/unit/codegen/test_reg_alloc.cpp` | 与家机 206-c（stdlib/v2 语义检查）、深度机 213-a（v2 IR容器释放）**零交叠** | §三.2 | 🏃 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **222-a**（本轮在飞） | D7 修正 + F1-28 接线（见 §三.2）；下一轮候选：① F1-26（Phi 落地·arm64 regalloc 已接线后同链收口）② F2-33 纯 stdlib 三项（随机数/迭代器/正则）③ D1 续波 / D3 | 用户批阅优先级①→② |
| 家机 win-x64 | 待 183-a 收工自登记 | 候选：① uD 面（`emitStructWholeAssign` 成员级级联深拷）② 波 3 ④v2 同构／⑤H7·H11 收口 ③ 波 4（真 move + `复制(x)`） | HANDOFF 家机节「四、下一步」 |
| 深度机 linux-x86_64 | 待 191-a 收工自登记 | 候选：① D1 续波（`scripts/check_fn_length.py` 实测 58 项：`ir_oop_call.cpp::handleClassCallExpr` 362 / 其余——**避开家机 183-a 写集**）② 波 7 NLL 基础设施 | HANDOFF 深度机节 + 实测清单 |

---

## 一、家机 win-x64 节（本机维护）

### 1.1 本轮（**探针检测轮**，验证轮不占轮次号；基线 476b7d2）：**源码边界多手段探测——漏洞/缺陷/规则合规/CN 规范一致性**——✅ 已收工

**1. 目标与验收判据**：用户令「使用探针等各种方式，多轮次探测项目源码边界，找出漏洞、缺陷、不符合
『性能第一安全第二』原则、不符合项目规则、不符合 CN 语法规范的地方，落盘项目文档，给后续开发轮修复」。
验收判据=每项发现附**可复现实测证据**（命令+退出码/字节级输出）；结论落 `plans/021` §三-G 待定项；
只读源码零修改（探针检测轮纪律），行为面文件写集为空。

**2. 写集（最终，全为共享文档本机分区+台账）**：`plans/021` §三-G（追加待定项）／本节／`HANDOFF.md` 家机节／
`更新日志.md` 家机节（源码/E2E/规范 零改动；探针脚本与产物全在 `target/probe234/`，不入库）。

**3. 实施步骤（已执行）与发现清单**（10 项，详细证据=plans/021 §三-G + HANDOFF 家机节）：

1. **静态合规扫描**：`check_fn_length.py` 函数级超百行 **30** 个（首列 `handleClassCallExpr` 362，D1 既有面·计数较 224-a 的 40 下降）；**文件级 >1000 行 8 个**（IR容器.cn 1643／生成表达式求值.cn 1122／语义检查.cn 1103／ir.hpp 1048／semantic.hpp 1046／代码生成X64Linux.cn 1013／cn_main.cpp 1006／semantic_call.cpp 1004——**无任何门禁**，173-a 曾达标的 semantic_call.cpp 回涨 1004）→ **发现①**。
2. **依赖方向分析**（python 遍历 `#include`）：`ir→semantic` **37 文件**／`codegen→semantic` **18 文件**／`parser↔semantic` 双向（ast.hpp↔type_system.hpp）——与 AGENTS §3「frontend→ir→opt→codegen 单向」文本不符，属**架构级既成事实**→ **发现②（待用户裁决：改规则文本认类型系统为基础共享层，或架构重构）**。
3. **边界探针 9 组**（`target/probe234/`，cn.exe check/build/run 实测）：
   - **发现③（P1·双侧同缺）递归结构体不拒绝**：`节点{整32 值; 节点 下个;}` check/build/run 全过，帧差分测得 typeSize 静默≈8 字节/元素（真实语义=无穷大小应编译期拒绝，Rust E0072 同类）；v2p 同样接受运行。
   - **发现④（P1·双侧同缺）字符串下标无越界检查**：`s[99]` 对 3 字节串读垃圾（68）、`s[1000]`=58；数组下标有运行时检查（rc=2 越界）而字符串无——**防线不对称**；v2p 同样无检查。
   - **发现⑤（双侧同缺）`\u{110000}` 超码点上限不拒绝**：生成非法 UTF-8 字节 `f4 90 80 80`（>0x10FFFF），下游 UTF-8 消费者必错；v2p 同样乱码。
   - **发现⑥（宿主）表达式括号嵌套 ≥175 层编译器栈溢出崩溃**：rc=3221225725（0xC00000FD）无任何诊断；150 层过/175 层崩；语法错误输入同样先崩后报错——递归下降无深度保护。
   - **发现⑦（P1 性能·宿主单侧）语义检查 O(d³) 块嵌套深度**：如果嵌套 100→0.6s／200→4.4s／300→14.5s／400→34.8s／500→66.7s（相邻比值 7.09/3.30/2.40/1.92 ≈ 立方）；分阶段计时定位=check（语义）与 ir 命令同为 35s@400、ast 仅 0.1s→瓶颈在语义检查；**v2p 同输入 0.3s=v2 无此问题（修复轮可参考 v2 实现）**。
   - **发现⑧（轻）常量除零编译期不拒**：`10 / 0` check 过，运行时才报错(1)——有运行时防护 ✓ 但常量折叠面应编译期诊断。
   - **发现⑨（规范缺口）整数溢出静默回绕**：整64/整32 max+1 回绕无诊断无规范定义（Rust 对照：debug panic/release wrap）——需规范裁决语义。
   - **发现⑩（轻·宽容语义）字符串子串越界参数静默截断**：`字符串子串(s,2,99)` 对 3 字节返回 "c" 无诊断。
   - **正面确认（防线在）**：数组越界/负下标 rc=2 ✓、逃逸引用编译期拒绝 ✓、结果丢弃编译期拒绝 ✓、空向量读取走结果错误分支 ✓、空指针 rc=3 有诊断 ✓、除零运行时防护 ✓、300 字符中文标识符哈希短化 ✓。
4. **v2p 同面验证**（重建 v2p·126 警告/0 错误）：发现③④⑤**双侧同缺**（一次修全纪律）；发现⑦ v2 无（宿主单侧）。

**4. 验证链**：全部发现以实测命令+退出码/字节输出为证（复现命令与探针在 `target/probe234/*.py`，
逐项记录于 plans/021 §三-G）；增量重建零警告（BUILD_EXIT=0/warning=0）。

**5. 边缘情况与诚实边界**：① 发现⑥ v2 面未测（v2p 编译深嵌套源码本身受宿主②同款风险影响未展开）；
② 递归结构体 typeSize 精确值未深挖（差分≈8B，修复轮查 typeSizeOf 递归截断点）；③ 发现⑦ 立方根因
未定位到函数（阶段已锁定=语义检查，曲线数据足供修复轮 bisect）；④ 宿主 126 条 v2p 构建警告未评估
（历史上 v2p 构建警告口径未立案，不新增登记）。

**6. 与他机交叠分析**：本轮零源码修改、零行为面交叠——深度机 234-a 候选（A7 本体·stdlib/v2 语义面）、
单位机（mem2reg/D8/F2-33）写集均不受影响；发现③④⑤若与 A7（v2 语义面）修复同轮实施，需在开发轮
认领时合并排班（写集协商），已在 plans/021 §三-G 注明。

**下一轮预登记（本机候选）**：① 发现④ 字符串下标越界检查（双侧同缺·P1 安全面·首推）；
② 发现⑥+⑦ 同轮（嵌套深度门禁+语义复杂度根治·可参考 v2 实现）；③ F1-30 余项（ASan E2E 面）。

## 二、深度机 linux-x86_64 节（本机维护）

### 2.1 本轮（**233-a**，基线 5ab9fad；认领 257f32e）：**插队修复轮——看板说明列表化 + plans/021 冲突标记入库根治 + 断表修复 + 门禁扩展**——✅ 已收工

- **实测回填**：①看板两处说明块列表化（用户令③）；②plans/021 冲突标记根治（取 HEAD 侧删另一侧+3 标记，
  标记残留 grep=0）；③断表修复六处（D1 游离文本 11 行指针化归位 plans/019 §7/D7·D9 归齐 3 列/
  §4.1 里程碑续块并回主表+补 232-a 行）——表块扫描零报警；④check_handoff.py 扩四文件冲突标记检查——
  **反例（修复前 HEAD 版）exit=1 拦截、正例 exit=0**；lessons 212/213 入册。
- **写集（最终）**：三机任务看板.md／plans/021／scripts/check_handoff.py／plans/025／lessons.md／
  HANDOFF.md／更新日志.md（7 文件·与计划一致）。
- **边缘回填**：0a0c5bb 侧删除的详情权威源=plans/025 §1.1 历史/HANDOFF 家机节/提交信息（HANDOFF 诚实边界已注明，
  家机复核可持锁补指针）；D1 指针化仅删过程细节（原文自注权威源）。
- **下一轮预登记（本机 234-a 候选）**：A7 本体根治收口（定位数据 plans/020 第七十四节）；
  备选=读取文件行 uninit 独立案/D1 续波。

---

### 2.2 上一轮（232-a）收工存档（要点）

- **交付**：①角色动态认领（开发>探针>E2E·先到先得·无固定映射，AGENTS §8 四处+lessons 211）；
  ②看板纯状态板（5 列删通告列→通告区化、⬜ 行清空、收工=整行清空）+check_handoff 扩空闲行检查
  （反例拦截/正例过）；③补分隔行（5ab9fad）。认领 88254ae/收工 47f2c69，均已推 gitcode。

---

