# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 256 轮（2026-09-16，单位机 ARM64）——256-a 收工回填：F1-26 方案 A 第一步落地（Phi 降级+使用点重写+验证器升级·单测 4/4·384 双形态）；下一轮预登记 257-a。

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **261-a（已收工）** | C20 波1：runner 断言升级（断言输出键+宿主旗标/stdin/args+v2 入口按源文件）+A 类 63/72 上架双编译+9 分叉立案 D34~D37+Cx17 实锤 D38+v2p 旗标立案 D33+B 类批次表 | tests/e2e/run_e2e.py、tests/e2e/63 目录双编译对照.txt、plans/021、HANDOFF/更新日志家机节、看板本机行 | 与深度机（待命）/单位机（空闲）零交叠 | §1.0 | ✅ 收工 |
| 深度机 linux-x86_64 | **232-a** | 协作协议增补（用户令两项）：角色动态优先级（开发>探针>E2E）+ 看板纯状态板（空闲行干净化+门禁扩展） | `AGENTS.md` §8、三机任务看板.md、scripts/check_handoff.py、plans/021、plans/025、HANDOFF.md、更新日志.md、lessons.md | 与家机/单位机零交叠（双 ⬜） | §二 | ✅ |
| 单位机 ARM64 | **286-a（在飞）** | S1b 采样日志分文件化根治（用户令 09-17「现在修改」）：daemon 按月分文件+变化时记/汇总+命中去重+存量迁移+check_handoff 门禁双向实证+M3 采样验证轮（arm64 首轨·新种子 2026091721/22+38 回归轨复验） | scripts/cnsmith_daemon.py、tests/cnsmith_hits/（指针化+日志/月文件）、scripts/check_handoff.py、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段 | 家机🧪M3 采样（同文件语义耦合=采样日志·收工 rebase 折入+通告兜底）；深度机⬜；编译器源码零改动 | §三.10 | 🏃 在飞 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **258-a**（下一轮候选·待认领） | ①**A12 里程碑标签锚点**（B6 已批·轻量可搭车）②F1-26 后续子项（激进 SSA 管线化/mem2reg 全量·性能主线）③F2-33 拆分之三 迭代器 ④D28/D29 根治专项；257-a 已收工（§3-B 铁律根治） | 计划=开工时落盘本表 |
| 家机 win-x64 | **262-a（下一轮候选·待认领）** | ①D34 v2 整型语义族根治（178/191/05 三实弹同族）②T11 残余面②③（x64l 发射器 default→硬错误+IR 验证器支持面）③C20 波2（硬前置=D28/D29 未清则顺延）| 依据=plans/021 §三-D D34 行+§三-G T11 行+C20 波2 口径 |
| 深度机 linux-x86_64 | 待 191-a 收工自登记 | 候选：① D1 续波（`scripts/check_fn_length.py` 实测 58 项：`ir_oop_call.cpp::handleClassCallExpr` 362 / 其余——**避开家机 183-a 写集**）② 波 7 NLL 基础设施 | HANDOFF 深度机节 + 实测清单 |

---

## 一、家机 win-x64 节（本机维护）

### 1.0 下一轮（**287-a**，基线 82bdbdd·2026-09-17）：**linux_x64 后端 O0 移位发射零扩展污染根治**（用户裁决方案 A·M3 采样 s2026091705 定案）

> 认领依据：用户对 s2026091705 甄别定案呈报裁决「方案A」。侦查已完成（免锁
> 验证轮 82bdbdd·asm 三件套定案）：缺陷函数=LinuxX64CodeGenerator::emitShift
> （linux_x64_instructions.cpp:343）——移位量掩码正确（2026-09-09 面）·
> **左操作数符号扩展缺失**（32 位槽零扩展装载+64 位 sar）。win=widthFor 宽度化
> 寄存器（sar eax）免疫；arm64=w9/x9 宽度化免疫（静态审查）；v2=D34 出口
> 规范化惯例免疫（槽值已 movsxd 符号化·静态审查）。**锁纪律**：单位机
> 286-a 交锁前不实施（🧪 侦查完毕·持锁即转 🏃 认领）。

1. **目标与验收判据**：①mini 探针（整32 负值>>变量移位量：-426>>-5875 预期
   -1）修后 linux .s 出现 movsxd 符号扩展；②新 E2E 403 移位负值矩阵绿；
   ③全量门禁绿（构建零警告+单测+E2E+CLI 矩阵+面覆盖）；④**win 锚定链
   逐字节不变**（win 后端零改动的反证面）；⑤采样回归轨 s2026091705 复验
   （win 出 linux .s 语义=变7=0·运行级复验登记深度机跨机项）。
2. **写集（精确清单）**：src/cn_compiler/codegen/linux_x64/linux_x64_instructions.cpp
   （emitShift 一处）；tests/e2e/403_移位负值矩阵/（新用例·主.cn+主.expected）；
   tests/e2e/coverage_map.md（403 登记）；plans/021（新案销项+T12 行修正注记
   +三指标附注）；文档六件套（HANDOFF/更新日志家机节+看板+plans/025 本节）。
   **v2 树零改动**（预期·见边缘④升级条款）。
3. **实施步骤**：①emitShift 的 sar 分支补符号扩展——i32=`movsxd r10, r10d`/
   i16=`movsx r10, r10w`/i8=`movsx r10, r10b`（统一放 loadOperandToX 之后·
   常量路径同样覆盖无害·shl/shr 免疫分支零改动·共享装载设施 loadOperandToX
   零改动——284-a「全指令泛化版被否决」同款陷阱规避）②mini 探针 O0/O3 双
   级别 linux .s 复核（movsxd 在位+语义手算变7=0）③403 用例设计：整32 负值
   ×变量/常量移位量×右移/左移/无符号对照×i8/i16 形态·expected win 实跑固化
   ④全量门禁+win 锚定链不变证明 ⑤v2p 出 s2026091705 linux .s 实证 v2 免疫
   （D34 在位）⑥采样回归轨 win 侧复验+深度机跨机项登记。
4. **验证链**：cmake 零警告+单测全量+E2E 全量（421+1=422 口径以实跑为准）
   +CLI 矩阵+面覆盖+win 锚定链逐字节不变证明+check_asm_width（linux 维度）
   +双文档门禁。**深度机跨机项**：X64L 锚定链重锚（linux 产物必变）+运行级
   复验——登记 plans/025 §二 下一候选。
5. **边缘情况与诚实边界（预判）**：①shl/shr 数学免疫（左移取低 32 位·无符号
   右移零扩展即值语义）——不动，属证明性结论非观察搁置；②i1 布尔走 sar 分支
   补 movsxd 无害（0/1 非负）；③linux 产物变化→X64L 锚必变→重锚归深度机；
   ④v2p 实证若发现 D34 惯例有装载侧漏洞→升级 v2 树修复并呈报（预期零改动）；
   ⑤arm64 免疫=静态审查，运行级佐证归单位机 286-a 后采样轨。
6. **与他机交叠分析**：单位机 🏃 286-a（scripts/daemon+采样日志分文件+arm64
   采样轨）——零文件交叠零语义耦合；深度机 ⬜（X64L 复验登记其下一候选·
   非在飞冲突）；锁被持期间本计划仅落盘不实施。

### 1.1 （⏸ 待定区·2026-09-16 用户令暂缓：plans/026 体系优先，视情况再启动）案戊阶段 1——type_system 下沉 types/ 基础层 + 静态库物理边界 + 层依赖门禁（T2 用户批准案戊）＋并行 **V2 编译器优化检查**（只读）

> **状态（2026-09-16 更正）**：计划已落盘（六要素见下）；**阶段 1 代码改动曾试做（提交 67127bc）、经
> 77c0ae4 revert 撤下、未入 develop**（验证链未跑）；**实施与收口（check_layer_deps 门禁 + 验证链 +
> 文档六件套）待后续机器按本计划认领执行**。并行项 V2 优化检查已完成·产出=plans/021 §三-G2（待排班）。

**1. 目标与验收判据**

- **案戊阶段 1**（开发轮）：`type_system.{hpp,cpp}`（559 行，被 45 文件跨层消费）从 `semantic/` 下沉到
  `types/` 基础层；CMake 拆独立静态库（链接方向物理固化）；新增 `scripts/check_layer_deps.py` 层依赖门禁接入 ci.ps1。
  验收=零警告构建 + 单测全过 + 全量 E2E 全绿 + **锚定链逐字节不变**（234-a 新基线 410460 行）。
- **V2 优化检查**（只读探针）：v2p 自编译计时/产物指令统计/算法模式静态审查，优化机会落 plans/021 待定项。

**2. 写集（精确清单）**

- `src/cn_compiler/types/type_system.hpp`、`src/cn_compiler/types/type_system.cpp`（git mv 自 semantic/）
- 45 处 `#include cn_compiler/semantic/type_system.*` → `cn_compiler/types/type_system.*`（ir 18/semantic 19/codegen 6/parser 2）
- `CMakeLists.txt`（types 静态库 target + 语义库链接 types）
- `scripts/check_layer_deps.py`（新增）+ `scripts/ci.ps1`（接入）
- 文档六件套（本节/HANDOFF 家机节/更新日志/plans/021/看板/lessons 如有坑）

**3. 实施步骤**

1. `git mv` 两文件 → types/；批量改写 45 处 include 路径（python 正则改写，逐文件核对计数）；
2. CMakeLists：新增 `cn_types` 静态库（type_system.cpp）+ `cn_semantic`（或现聚合目标）链接它——保持产物二进制等价；
3. `check_layer_deps.py`：①types/ 不得 include parser/semantic/ir/codegen ②parser 不得 include semantic.hpp（type_system 除外——阶段 2 收口）③层 3 过程目录互不 include（按阶段渐进启用）；
4. 验证链全跑；5. V2 优化检查（v2p 计时/指令统计/静态审查）。

**4. 验证链**：清场重建零警告 → 单测 → 全量 E2E（313 用例口径）→ 锚定链逐字节比对（410460 基线）
→ check_layer_deps 反例自检（构造违规 include 确认拦截）→ 双文档门禁。

**5. 边缘情况与诚实边界（开工预判）**：① 234-a 刚改 v2 树+stdlib 并重锚 410460——本轮基线取其上，
   锚定链比对用新基线；② 45 处 include 中 semantic 目录内部 19 处为「同库内搬移」性质（不跨层），
   仍统一改写保一致；③ CMake 若现结构为单聚合目标，则先建 types 库+目标间链接，不动其余聚合结构
   （阶段 2~4 逐层拆）；④ check_layer_deps 首版规则面从窄（types 纯净性+parser 白名单），阶段 2~4 扩。

**6. 与他机交叠分析**：写集=src/cn_compiler 头文件路径+CMake+scripts——与深度机（234-a 已收工·下轮
候选=读取文件行 uninit/D1）、单位机（mem2reg/D8/F2-33·codegen 与 reg_alloc 面）**写集不相交**；
锚定链口径同步用 234-a 新基线，语义零变化（纯搬移），他机无需复跑。

---

## 二、深度机 linux-x86_64 节（本机维护）

> 历史轮次明细（274-a~285-a 收工回填）由 git 提交信息+plans/021 §5 索引承载——本节按整节替换惯例轮出。

### 2.1 本轮（**289-a**，基线 217d132·2026-09-17 🏃）：**B9 缺陷族面扩散检验制度落地（用户 09-17 批准）**

**1. 目标与验收判据**：B9 三落点入库——①`scripts/famscan.py` 转正（参数化+自包含+47 用例固化=T23 复验基准在库）②AGENTS §8.1 探针轮职责扩「缺陷族面扩散」条③plans/026 §2.11 制度条文；plans/021 B9 迁出 B 区（生命周期铁律）+销项进 §4.2。验收=转正 famscan 实测与首案同结果（16 命中·T23 缺陷在=预期基线）+py_compile+双文档门禁绿+零冲突 push。

**2. 写集（精确清单）**：`scripts/famscan.py`（新增·自 /tmp/cn_family/famscan.py 参数化转正）｜`AGENTS.md`（§8.1 扩条）｜`plans/026-语言成熟度验收体系.md`（§2.11）｜`plans/021`（B9 迁出+§4.2 销项+§5 索引+文首戳）｜本表 §二｜HANDOFF/更新日志 本机节｜看板本机行。**编译器源码与测试用例零改动**（纯制度+工具轮）。

**3. 实施步骤**：①famscan 参数化（`--cn`/`--out`/gcc 探测降级）+转正实测比对；②AGENTS §8.1「面台账巡视」段后追加「缺陷族面扩散」默认动作段；③plans/026 §2.11 条文（仿 §2.10 结构）；④021 迁出+销项+刷戳；⑤六件套+看板清行+双推。

**4. 验证链**：`python3 scripts/famscan.py --out /tmp/famscan_verify`（16 命中与首案一致）→`python3 -m py_compile scripts/famscan.py`→`check_handoff.py`+`check_progress_sync.py`→push gitcode+github。

**5. 边缘情况与诚实边界（预判）**：①C 对拍依赖 gcc——无 gcc 环境自动降级单 oracle（脚本内标注降级态）；②16 命中=T23 缺陷未修的预期基线（287-a 修复前）——**非工具失败**；修复轮复验判据=同命令 0 命中；③win 侧运行需 gcc 环境（Git Bash/WSL）——制度条文标注 linux 优先、win 以 asm 静态比对替代。

**6. 与他机交叠分析**：家机 🏃 287-a=linux_x64/*+tests/e2e/403；单位机 🏃 288-a=arm64/*+tests/e2e/404（两轮系用户授权并行）——本轮写集=scripts/ 新文件+AGENTS/026/021 自家区域+共享文档本机节=**零文件交叠**；plans/021 三机共笔但区域不同（本轮动 B 区+§4.2+戳·他们动 T12/T22 行）——push 被拒则 fetch+rebase 复推（§8.5 兜底）。

### 2.2 下一轮候选（预登记·防撞车·不占号）

- **深度机候选**：①287-a 修复后的 T23 运行级复验（famscan 复跑 16→0 判据）②B8 面台账巡视续行③daemon 常态化（286-a 结构变更后 pull 重启）④T21 标识符字符集（待规范裁决）。
- **家机**：287-a 收工后 C20 波2 批B sem5 stdlib 32 上架。
- **单位机**：288-a 收工后 M3 arm64 轨续采。

## 三、单位机 linux-arm64 节（本机维护）

### 3.11 本轮（**288-a**，基线 7962579·2026-09-17 🏃）：**T22 根治——arm64 逻辑立即数 bitmask 编码器（用户裁决方案 A）**

**用户令并行授权说明**：家机 🏃 287-a（linux_x64 emitShift 符号扩展·用户裁方案 A）在飞——本机按用户向本单位直授方案 A 即时实施 T22；写集=arm64 后端+用例 404（家机 287-a 用 403）+自家文档节=**零文件交叠·语义独立**（不同后端/不同指令族）。

**1. 目标与验收判据**：根治 T22（arm64 -O3 位运算常量立即数非法汇编·31 样本拒编译）——emitIntBinary 常量分支此前对 and/orr/eor 沿用 add/sub 的 12 位判定（#425/#0 直发→as「立即数越界」）。方案 A=①逻辑立即数 bitmask 编码器（元素铺满模型：连续 cnt∈[1,esize-1] 个 1 循环右移 r<esize 铺满·esize∈{2,4,8,16,32(,64)}·静态全集 ~1.3k/~4k 项·GNU as 实测校准：全 0/全 1 非法·单 bit/交替/跨字循环合法）；②恒等折叠（and 0→mov 0·orr/eor 0→直通零开销）；③可编码直发/不可编码 emitMovImm 装载+寄存器形式（LLVM/GCC 同款）。**验收**=①as 实测 28 边值与编码器判定全一致（单测固化）②复现探针 O0/O3 双对③新 E2E 404 位立即数矩阵 O3 三平台通过④31 个 run_err 样本回归轨复验=全部转已修⑤全量 E2E+单测+锚定链（预期不变：v2 树此前可编译=无非法形态·修复不改合法产物）。

**2. 写集（精确清单）**：`src/cn_compiler/codegen/arm64/arm64_codegen.hpp`（+isLogicalBitmaskImmediate 声明）、`arm64_instructions.cpp`（编码器+emitIntBinary 重构）、`tests/unit/codegen/test_arm64_codegen.cpp`（+编码器边值单测）、`tests/e2e/404_位运算立即数矩阵O3/`（新·编译选项.txt=-O3）、plans/021（T22 销项）、plans/025、HANDOFF/更新日志单位机节、看板本机行。**linux_x64/x64 后端零改动**（287-a 写集避让）。

**3. 实施步骤**：①看板认领 push→②编码器+emitIntBinary 重构→③增量构建→④单测+复现探针→⑤404 用例实跑固化→⑥全量 E2E（421+1）+锚定链→⑦daemon 回归轨复验（31 样本）→⑧六件套+收工。

**4. 验证链**：cmake --build 零警告→cn_unit_tests（含新单测）→404 单跑→run_e2e 全量→锚定链 fix_p≡fix_s 对照 416547→daemon --samples 0 回归轨 31→双文档门禁。

**5. 边缘情况与诚实边界**：①负常量按位宽截断无符号视图判定（-1=全 1 非法→装载路径自动覆盖）；②emitMovImm 对 w 寄存器截断 32 位装载（语义=值正确·高位无关）；③orr/eor 0 直通不发指令=新形态（原每分支必发指令）——storeVirtualResult 统一存回不受影响；④38 个 diff/ 老样本在 arm64 本就一致（T12 形态不呈现）·回归轨复验以 run_err/ 31 个为主；⑤家机 287-a 收工若后于本机·其 rebase 后全量复跑含 404。

**6. 与他机交叠分析**：家机 🏃 287-a=src/cn_compiler/codegen/linux_x64/*+403+§1.0——与本轮 arm64/*+404+§三.11 **零文件交叠**；语义耦合=无（不同后端不同指令族·锚定链各平台独立）；深度机 ⬜。**用户令=并行授权权威**。

### 3.10 上一轮（**286-a**，基线 c37e1ac→rebase 82bdbdd·认领 f4c6626·2026-09-17）收工回填：**S1b 采样日志分文件化根治（用户令「现在修改」）+ M3 采样 arm64 首轨（独家捕获 T22）**

**收工回填（实测）**：①daemon 分文件+汇总记法+去重落地——月文件 2026-09.md 收档（存量 2583 行原样迁移+三机在飞增量折入+残条补注）·指针文件 9 行·翻月自动建头；②check_handoff 扩门禁**反例三连拦截（指针超行/轮次条目/月文件超限）+正例 PASS 双向实证**；③两防御+时间戳种子落地；④M3 三轮（新样本 351·输出分歧 0·编译失败 0·运行失败 65 样本=**T22 独家捕获**·31 样本入库 run_err/）；⑤arm64 移位零扩展定点探针免疫实证；⑥plans/021 戳 286-a+T22 立案+S1b 销项 §4.2+§5 索引+B 区迁出（601→600 内）+看板收工+通告广播；⑦**诚实边界回填**：daemon 记轮首版 NameError 中断一轮（当场修复·残条补注）；plans/021 曾被脚本正则截断尾部 98 行（git 恢复重做·教训入 HANDOFF）；回归轨滚雪球与汇编失败分类两观察项登记。

**1. 目标与验收判据（达成情况）**：全部达成（分文件/汇总/去重/指针/迁移/门禁双向/试跑格式断言/采样落新结构/回归轨结论）——回归轨结论=arm64 口径 T12 形态不呈现（284-a 根治 arm64 有效）、11 命中全为 T22 新形态。

**2. 写集（最终清单）**：scripts/cnsmith_daemon.py、scripts/check_handoff.py、tests/cnsmith_hits/采样日志.md（指针化）、tests/cnsmith_hits/日志/2026-09.md（新+迁移）、tests/cnsmith_hits/run_err/31 样本+.meta（daemon 自动）、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段。**编译器源码零改动** ✓。

**3. 实施步骤（回填）**：按计划①~⑧执行；插入项=远程 rebase 时家机旧结构日志折入月文件、daemon 首版 bug 修复重跑、plans/021 截断事故恢复。

**4. 验证链（回填实测）**：py_compile 双脚本 ✓→daemon 试跑+三轮实测 ✓→门禁反例/正例双向 ✓→双文档门禁 PASS ✓→进度同步 PASS ✓。编译器源码零改动→E2E/单测基线=82bdbdd（285-a 全绿）未重跑（诚实边界已登记）。

**5. 边缘情况与诚实边界（回填）**：家机采样轮收工在先（492e976·其日志增量已折入月文件）→语义耦合消解；深度机旧 daemon 停→pull→重启=通告段已广播；汇总行不含轮级明细=设计取舍（git 承载）。

**6. 与他机交叠分析（回填）**：同文件语义耦合按预案消解（折入+通告）；零文本冲突。

**下一轮候选（预登记）**：①**T22 根治**（用户裁决后任一机认领·arm64 运行级复验归本机）②s2026091705 根治后跨机复验 ③daemon 两观察项 ④A12 里程碑标签（B6 已批·轻量可搭车）。

### 3.9 上一轮（**257-a 插队修复轮**，基线 f0f0696·认领 9818aa8）收工回填：**plans/021 §3-B 生命周期铁律根治（用户质询·B5 三犯立规）**

**收工回填（实测）**：①check_handoff.py 扩 §3-B 完成标记拦截——**反例**（修复前文件 B5 L80「已裁决」「✅」双标记命中·退出码 1）/ **正例**（修复后 PASS·退出码 0）双向实证；拦截词表=「已裁决/已批/已选定/✅」；②plans/021：B5 留档清空（指针在 §3-F F1-26 行/§5 索引）+§3-G T2/T9 迁 §3-C C17/C18+处置建议段同步+文首戳/§5 索引刷新；③AGENTS.md §8.3 铁律明文（含同型纪律：全表任何区不得以「已裁决」状态滞留）；④lessons 在册（权重 6）；⑤**轮中用户批复「同意」→B6 当轮迁 §3-A A12**（里程碑标签锚点·待认领）——铁律立规后首次「裁决即迁出」实战；⑥源码零改动；门禁=双文档门禁 PASS。

**1. 目标与验收判据**：用户质询「已裁决方案必须迁 §3-A/§3-C，§3-B 相关内容必须清空——已多次违反」。
①plans/021 §3-B：清空 B5 整段「原文留档」（已裁决「选 A」且 256-a 已实施·指针齐备于 §3-F F1-26 行/§5 轮次索引）；同区补登 **B6**（256-b 分支策略呈报·真待批·原只挂看板通告段未进本区=同型漂移）；
②同族穷尽：§3-G **T2/T9** 已裁决条目迁 §3-C（C17=案戊五层收口·保留⏸暂缓语义；C18=整数溢出方案 D），G 区删原行、处置建议段同步；
③**AGENTS.md §8.3 明文立规**：B 区生命周期铁律（用户令落盘——裁决后当轮迁出，B 区禁留已裁决内容含「原文留档」）；
④**check_handoff.py 扩机械门禁**：§3-B 区出现裁决完成标记（已裁决/已批准/已批复/已选定/✅）即拦截——**反例（修复前文件）拦截 + 正例（修复后）放行 双向实证**；⑤lessons.md 记录（三犯高权重）。
验收=双门禁 PASS（check_handoff.py 含新检查 + check_progress_sync.py）+ 反例拦截实证留痕。

**2. 写集**：`plans/021`（§3-B/§3-C/§3-G/文首戳/§5 索引）、`scripts/check_handoff.py`、`AGENTS.md`（§8.3 追加铁律）、`lessons.md`（追加）、`plans/025`（本表）、`HANDOFF.md`/`更新日志.md`（单位机节）、看板本机行。**源码零改动**（纯文档+门禁脚本轮）。

**3. 实施步骤**：①先扩 check_handoff.py → 对修复前文件跑=反例拦截（红 ✓ 实证）；②修 plans/021（B5 清空+B6 补登+T2/T9 迁 C17/C18）；③AGENTS.md §8.3 追加铁律；④复跑=正例放行（绿 ✓）；⑤lessons+六件套收尾+提交双推送。

**4. 验证链**：`python3 scripts/check_handoff.py`（新检查反例红/正例绿双向）+ `python3 scripts/check_progress_sync.py` + `python3 -m py_compile scripts/check_handoff.py`；源码零改动→无需构建/E2E/锚定链。

**5. 边缘情况与诚实边界**：①机械检查按「裁决完成标记」字面拦截，非语义判定（B 区文本措辞须避开完成态词——宁可严格，命中即重写）；②检查依赖「### B. 」章节标题格式稳定；③T2 迁 C17 时保留用户令「⏸ 暂缓（plans/026 体系优先）」语义不丢。

**6. 与他机交叠分析**：深度机🧪（写面=HANDOFF 深度机节/看板深度机行+target//tmp）与本轮写集零交叠 ✓；家机⬜空闲 ✓；check_handoff.py 为三机共用门禁——扩检查对他机在飞写面无影响（B 区仅 B5 残留）；plans/021 并发追加按 §8.3 push 前 fetch+rebase 复推兜底。

### 3.8 上一轮（**256-a**，基线 9f7783d·认领 df797eb）收工回填：**F1-26 方案 A 第一步落地（用户裁决「选 A」）**

**1. 目标与验收判据（回填实测）**：①**探针兑现**：Phi 生成激活（-O3 实测 7 处）+无消费者（侥幸安全）+多形态 -O0/-O3 一致；②**Phi 降级**：前驱块尾并行拷贝+交换环打破（新 `Opcode::Copy`·三后端发射；环打破=贪心可安全写者先行+环内读先全存临时）——**修复途中自查：临时寄存器 id 跳大（1000000）致 RA 表越界段错误（rc=139）→改「函数内最大 id+1」**；③**保守使用点重写**：汇合块内 Load 槽→Phi 结果（-O3 IR 实证：循环头比较指令直接用 Phi 结果·2 个 Load 消除）；④**IR 验证器升级**：块内 def-before-use→「函数级定义存在+块内顺序+自引用惯例保留」（配合 Phi 降级引入的跨块值流·方案 A 本质）；⑤**runner「编译选项.txt」用例级旗标通道**（三机共享契约·§8.4 广播面）；⑥单测 `test_phi_lower.cpp` **4/4**（汇合/循环携带/交换环防御/占位前驱）；⑦E2E **384**（-O3 多形态·期望逐行）PASS；⑧cnsmith -O0/-O3 差分 500 采样（脚本跨平台化——原硬编码 win-x64=家机口径）；⑨全量门禁。

**2. 写集（最终清单）**：`src/cn_compiler/ir/ir.hpp`（Opcode::Copy）+`ir.cpp`（名称）+`ir_verify.cpp`（验证器升级）；`src/cn_compiler/opt/ssa.cpp`+`ssa.hpp`（重写/降级/环打破/早退修）；`src/cn_compiler/codegen/arm64/*`+`x64/*`+`linux_x64/*`（Copy 发射·三后端）；`scripts/cnsmith_diff.py`（跨平台化）；`tests/e2e/run_e2e.py`（旗标通道）；`tests/unit/opt/test_phi_lower.cpp`（新）+`tests/unit/CMakeLists.txt`；`tests/e2e/384_Phi汇合优化/`（新）；`plans/021`（F1-26 进展+B5 裁决记录）；本表；HANDOFF/更新日志；看板本机行。

**3. 实施要点（回填·三坑三修）**：①临时 id 越界（RA 表按 id 索引·跳大 id→越界段错误）；②验证器与跨块值流不兼容（方案 A 本质使块内规则失效→函数级升级）+自引用操作数惯例（比较指令第 3 操作数=result id·登记顺序须保持）；③手构「源=Phi result」环=非自然 IR（环打破为防御能力·测试放宽验证器断言并注明）。

**4. 验证链（回填实测）**：探针（-O0/-O3 多形态·7 处 Phi 生成）→单测 4/4→384（-O3）PASS→cnsmith 500 差分（见提交）→全量 E2E（见提交）→覆盖率（红线）+锚定链（-O3 面改动·-O0 锚定预期不变）→双文档门禁。

**5. 边缘情况与诚实边界（回填）**：①**激进 SSA 管线化（支配子树全重写/真 mem2reg/Phi 进 RA 偏好）=F1-26 后续子项**（本轮=降级能力+保守重写闭环——SSA 态不持久）；②性能锚定量化=后续性能轮（本轮已备量化素材：Load 消除实测/Phi 降级前后指令形态）；③交换环自然不可达（防御保留）；④v2 侧无此面（宿主单侧）。

**6. 与他机交叠分析（回填）**：写集=ir/opt/codegen 三后端面+runner 契约（§8.4 广播：编译选项.txt 通道·无存量影响）+新用例——与深度机（🧪）/家机（空闲）零相交 ✓；**runner 变更须在看板通告段广播**（收工随提交）。

**下一轮候选（预登记）**：①**F1-26 后续子项**（激进 SSA 管线化/mem2reg 全量·性能主线）②F2-33 拆分之三（迭代器）③D28/D29 根治专项——开工时以看板最新状态为准。
