# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 256 轮（2026-09-16，单位机 ARM64）——256-a 收工回填：F1-26 方案 A 第一步落地（Phi 降级+使用点重写+验证器升级·单测 4/4·384 双形态）；下一轮预登记 257-a。

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **606-a（开发轮·🏃 plans/021 §三 任务树重组·分支 任务/家机-606-021tree）** | 平表→多级树+过时审计+门禁扩展 | plans/021+scripts/check_handoff.py+plans/025 本节+交接/更新日志本机节 | 家机 605（wt605·v2 树）代码面零交叠·共享文档面按集成序合并；他机零交叠 | §1.0 | 🏃 |
| 深度机 linux-x86_64 | **🧪 314-a M3 采样轮·x64l 列** | 第五新种子域 310000001 差分 500+负向 400（310100001）+回归轨 114 全库+T44 x64l 跨机复验（t44scan/ext/if2）+日志日文件制首写 | 看板本机行、plans/025 本节、HANDOFF/更新日志深度机节、tests/cnsmith_hits/日志/2026-09-17.md（新）、命中样本（如有）、plans/021（T44 行证据回填·最小局部） | 家机 313-b 🏃（daemon/日志拆日写集）互认零碰；日志日文件与其拆日同向；单位机 ⬜ 空闲 | §2.1 | 🧪 在飞 |
| 单位机 ARM64 | **328-a（开发轮·已收工·分支 任务/单位机-328-协议v2 已集成 e6e22aa）** | 三机并行协同协议 v2 落地（用户批准）：分支即开发+integrate.py 队列集成+缺陷分级+分层 try-build+文档随分支走——AGENTS §8 重写/plans/027 方案/check_handoff 扩分支名校验 | 见 §3.18 收工回填 | 家机 326-a 收工与本案真实碰撞一次（rebase 消解=v2 首个实战案例）；深度机 ⬜ 零交叠 | §三.18 | ✅ 收工 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **258-a**（下一轮候选·待认领） | ①**A12 里程碑标签锚点**（B6 已批·轻量可搭车）②F1-26 后续子项（激进 SSA 管线化/mem2reg 全量·性能主线）③F2-33 拆分之三 迭代器 ④D28/D29 根治专项；257-a 已收工（§3-B 铁律根治） | 计划=开工时落盘本表 |
| 家机 win-x64 | **450-a（下一轮候选·待认领）** | 依 plans/021 §3-A 队列：M3 win 列采样（用户令不停）/清零波次余项/C20 批C·D parse 桶上架——开工以看板最新为准 | plans/021 排班+看板 |
| 深度机 linux-x86_64 | **414-a（下一轮·D42 甲案波 1）** | **泛型类源码编译全链·波1=解析+语义层**（2026-09-19 用户裁决「同意甲」·plans/021 §三-A D42甲行）：①v2 解析器 `类 名<T>` 类型参数表（解析类型文本/泛型闭合位置 既有 P7b 泛型函数设施复用）；②实例类型合成名（名$T$…·与泛型函数同款）；③语义检查类表挂接；④探针=v2p 编译 含 `类 盒子<T>` 源码不崩+实例声明语义通过。波2（后续轮）=IR/生成单态化；波3=stdlib 源码加载路线+390 根治 | 395-a 破案=plans/025 §2.46·呈报三案=plans/021 B2（已迁 §三-A） |

---

> 〔495-a 段标题占位行已随 529 开工滚转（2026-09-20·内容 git 可考）〕

## 一、家机 win-x64 节（本机维护）

### 1.0 实施计划（**613-a 开发轮·🏃 001-001 续=波0 泛型实例布局注册时点前置**·看板先行 55ef0db5·基线 develop eeade827·2026-09-23·分支 `任务/家机-613-波0注册前置`·wt613 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 612-a（sret 判定错配根因=泛型实例布局注册时点：IR 生成中按需注册→被调生成时 查结构体大小 miss/调用方生成时 hit）。波 0=**注册时点前置**：解析完成后、IR 生成前，全量扫描已解析池（函数声明返回类型+形参类型+变量声明类型+类字段类型）中的泛型实例文本→**幂等触发布局注册**（复用 416-a 注册路径·不前置克隆体方法生成——布局/大小/字段偏移/类型/拷贝构造参数串 就绪即满足判定）。**验收**=①探针 M/L rc 2→0（sret 判定错配修复·K 保持绿）②113 单跑对照③撤守卫态全量无新增红④反证双态（撤前置→L 复现 rc=2）⑤生产态门禁 4/4。
**2. 写集（预登记）**：`CN语言编译器v2/IR/IR生成.cn` 或 IR 生成主入口（预扫描注入点）｜`CN语言编译器v2/IR/IR类.cn`（注册入口函数提取/封装·如需）｜六件套。TEMP 撤守卫不提交；宿主 src 零改动。
**3. 实施步骤**：①读 416-a 注册路径（IR类.cn 注册入口+克隆体生成挂点分离可能性）+IR 生成主入口结构（多文件逐文件循环前置点）②预扫描函数=遍历池收集「含 $ 的类型注解文本」（函数返回类型 附加/参数类型/变量声明类型/类字段）→逐个触发注册③注册幂等性确认（重复注册防御）④重建 v2p→M/L/K 探针→113→全量→反证→生产态→integrate。
**4. 验证链**：探针 M/L/K→113 单跑→撤守卫态全量→反证（撤前置→L 复现）→生产态 ci.ps1→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①预扫描只前置布局注册·克隆体方法生成仍按需（方法符号面 N2/成员链偏移另案不碰）②嵌套实例（向量$映射$整64,符号）递归收集③注册失败的实例文本（非泛型类/未声明类）防御跳过④N2 非泛型 sret 字段错位独立面不在本轮。
**6. 与他机交叠分析**：远端 develop=eeade827（本机 612 刚集成·最新）；同机 611-uninit（宿主语义+v2 树语义层·与本轮 IR 生成层零行级交叠）/609-003负测（tests 面）零交叠；深度机 550 呈报态零交叠。冲突判定：独立。


### 1.0 实施计划（**612-a 开发轮·🏃 001-001 续=所有权归一化设施第一波（按值形参副本+返回位转移）**·看板先行 2600ad3a·基线 develop 5b5116f2 系·2026-09-23·分支 `任务/家机-612-所有权归一波1`·wt612 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 610-a（asm 铁证=v2p 类对象按值形参/元素读取句柄浅传别名）。第一波落地面：①**按值形参副本**——被调方入口对「有拷贝构造的类对象形参」分配副本+拷贝构造调用+尾块析构副本（单点=生成函数 入口/出口·对照宿主按值形参 C++ 值语义）；②**返回位转移**——`返回 <类对象标识符>` 时该槽登记转移名单·尾块析构跳过（宿主 494-a「返回 转移直移」·Rust move out 对照）——二者合成后 `b=层拷(a)` 全链安全。**回归锚**=探针 C/K/L/D（608/610 资产重建）：C 绿保持/K 绿保持/**L 转 0**（副本内容不丢·不悬垂）/B 组合态转 0；113 单跑推进（预期仍余元素读取别名面=第二波·或部分改善）；撤守卫态全量无新增红。**验收**=①L→rc=0+B→rc=0②113 单跑对照③全量 523/3 维持或改善④生产态门禁 4/4。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成函数.cn`（入口副本+尾块析构）｜`CN语言编译器v2/IR/生成语句.cn`（返回位转移登记）｜`CN语言编译器v2/IR/IR基础.cn` 或 函数IR 结构定义文件（转移名单字段）｜六件套。TEMP 撤守卫不提交；宿主 src 零改动。
**3. 实施步骤**：①读 生成函数.cn 入口/尾块结构+参数类型文本获取（函数参数串/签名表）②入口：逐形参判定（类对象=是用户类ID+查结构体大小≠0+拷贝构造存在〔查类构造参数串(ID,1)≠0〕）→IR_分配对象 新槽+call 拷贝构造(新,原)+参数槽重绑定新槽③尾块：副本 IR_删除对象（独立堆对象·608 勘误口径不适用此处=删除对象正确面）④返回位：生成语句 返回 对类对象标识符登记转移名单（生成函数 传递）+尾块析构排除⑤重建 v2p→探针 C/K/L/D/B→113→全量→反证→生产态→integrate。
**4. 验证链**：探针回归（C 绿/K 绿/L→0/B→0）→113 单跑→撤守卫态全量（≥523/3 维持·若 113 转绿=524/79_v2 二红余）→反证双态（撤设施→L 复现 rc=2）→生产态 ci.ps1 4/4→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①第一波不含元素读取/入容器位归一化（第二波）——113 若余元素读取面=如实登记②递归函数形参副本=每层独立（语义正确性自证）③成员方法 this 不在形参副本面④返回位转移只覆盖「返回 标识符」直返形态（返回 表达式=调用构造面后续）。
**6. 与他机交叠分析**：远端 develop=2600ad3a（本机 612 看板修正·最新）；**同机 611-uninit 并行**（宿主语义层+v2 树语义层同构·与本轮 v2 树 IR 生成层〔生成函数/生成语句〕零行级交叠·集成序按各自收工）；609-003负测（tests 面）零交叠；深度机 550 呈报态零交叠。冲突判定：独立。

### 1.0 收工回填（**612-a=bisect 深化：泛型实例注册时点=前置的前置·第一波改道**·wt612·2026-09-23）

**1. 关键发现（本轮 bisect 推进）**：重建探针后新证据链——**K 返回形参+丢弃接收 rc=0**（返回 ABI 通道本身通）；**L rc=2**（声明初始化副本内容丢失）——asm 对照（610 资产）：调用方按 **sret** 调（lea rcx retbuf+rdx 实参）·被调 prologue **非 sret**（rcx 直接绑首形参）——**两侧「返回结构体」判定不一致=ABI 错配一档**。**M 探针（返回类型无空格书写）rc=2**=空格假设否掉。**N2（非泛型结构体返回）rc=3**（b.名ID 对·类型ID 错）=非泛型也有**独立字段错位面**（sret 拷贝字节数/字段序）。**O 探针（打印面）v2 rc=0 全对**=读取面正常·缺陷特定于比较/传递组合。**根因收敛=泛型实例布局注册时点**（IR 生成中按需注册→被调生成时 miss/调用方生成时 hit→判定时点差）——**注册时点前置（解析后·IR 生成前全量收集注册）=前置的前置·第一波改道先修此**。
**2. 撤守卫态全量回归（基线维护）**：本轮回归跑=526=520/6（113/127_v2/218/497 双析构族+524/79_v2——**与 608 撤回后同态**·v2 树零代码改动 ✓）。
**3. 下一棒（001-001 续·波次细化）**：①**波0=泛型实例布局注册时点前置**（解析后·IR 生成前全量收集函数签名/声明类型注解→幂等注册——修 sret 判定错配→L/K 面转绿）；②**波1=按值形参副本+返回位转移**（波0 后生效）；③**波2=元素读取/入容器位归一化**（113/127_v2/218/497 清偿）；④**N2 非泛型 sret 字段错位**（独立面·或随波0 显形重估）；⑤79_v2 二分；⑥524 随 592。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。
**4. 诚实边界（回填）**：本轮纯定性深化（探针七态 B/F/G/H/K/L/M+N2/O）零代码；探针资产 target/p612c 不入库（probe_run.py 流程脚本+源码 025 可考）；611-uninit 同机并行（写集零交叠·集成序互不阻塞）。


### 1.0 实施计划（**610-a 开发轮·🏃 001-001 续=拷贝链组合态 bisect（113 清偿推进）**·看板先行 5bb06ad7·基线 develop a4536157·2026-09-22·分支 `任务/家机-610-拷贝链bisect`·wt610 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 608-a（113 定性=映射 类对象拷贝链组合态缺陷·探针五态 B 组合态 rc=139）。本轮 **bisect 组合态**：重建 p608c 探针资产（608 树已删·源码 025 可考）→微缩分层 F/G/H（F=形参直返回无本地拷；G=F+本地声明拷；H=G+主函数声明初始化+设置）逐层定位首个崩溃操作→asm 对照（v2p 克隆体 vs 宿主拷贝构造 asm·副本次序/retbuf/句柄写点）→**若缺陷在 v2p 码生成=机制级当场修**；若在 stdlib 源码=stdlib 轮。**验收**=①B 组合态 rc 139→0（或精确新定性）②113 单跑对照推进③撤守卫态全量无新增红④反证双态⑤生产态门禁零影响。
**2. 写集（预登记）**：视定位结果=CN语言编译器v2/IR/*.cn 或 生成*.cn（机制级修复）｜探针资产 target/（不入库）｜六件套。TEMP 撤守卫=本地验证态不提交。
**3. 实施步骤**：①重建探针（B/C/D/E 复现+F/G/H 微缩）②崩层锁定→asm 三件套（v2 克隆体/宿主拷贝构造/崩点寄存器——WinDbg 或差分）③修复（性能第一·对照宿主语义）④113 单跑→全量→反证双态⑤生产态门禁+integrate。
**4. 验证链**：v2p 重建→分层探针 rc→113 单跑→撤守卫态全量→反证→生产态 ci.ps1→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①若 bisect 显示缺陷跨多层（返回 ABI+声明拷+设置三因叠加）→逐因修·每修一层全量回归②若触及 retbuf ABI=高风险面（585-a 前科）→修前锚定现产物 asm③113 之外 127_v2/218/497 的双析构面=所有权归一化设施（608 已立账·独立轮）④524/79_v2 不在本轮。
**6. 与他机交叠分析**：远端 develop=a4536157（本机 608 刚集成·最新）；家机 609-003负测（同机他会话·写集=tests 负测面·与本轮 v2 树/IR 零交叠）；深度机 550-T94 呈报态零交叠。冲突判定：独立。

### 1.0 收工回填（**610-a=拷贝链 bisect 收官→113 终局定性：v2p 类对象按值形参/元素读取句柄浅传（所有权归一化缺位）**·wt610·2026-09-22）

**1. bisect 实测（探针资产 target/p610c·不入库）**：B 组合态复现 0xC0000005 ✓；微缩分层 **F 形参直返回=段错误/G 本地声明拷+返回丢弃=段错误/H 声明初始化纯析构=段错误**——三者共同独有点=「返回 形参副本」；**K 返回形参+丢弃接收 rc=0**；**L 返回形参+声明初始化+大小检查 rc=2**（副本内容丢失）；J 暴露独立缺口=结果成员链访问 `.值.类型ID` 字段偏移未知（113 同款先拷局部绕过·另立案）。**单面反差**（608 C/D/E 绿 vs 610 F/G/H 红）之谜解开=**608 的 C/D/E 探针形态与 610 不同源**（608 B 内层拷带本地声明拷；610 F 直返形参）——组合态非「叠加」而是「别名」。
**2. asm 铁证（终局定性）**：cn_main 调 层拷 实参=`mov rdx,[rbp-272]`（=a 句槽句柄浅传·**无拷贝构造调用**）；层拷 体=`mov rax,[rbp-24]; ret`（直返 rcx）——**v2p 类对象按值形参/元素读取=句柄浅传别名**（宿主=真拷贝·218 波 3 设施）。113 崩溃链完整解释：弹出 的 `顶层=元素(i)`（别名）→`顶层.释放内部数组()` 释放**容器内元素**的内部数组→后续析构/删除=双释放 0xC0000374。
**3. 修复路径（对照宿主·Rust 语义准绳）**：类对象按值语义=**所有权归一化设施**（形参=拷贝构造副本或 move 语义·元素读取=拷贝接管·入容器位归一化——宿主 ir_call.cpp 218 波 3 全套）——**大设施立账（608 已预立·本轮 asm 铁证补全）**·非单轮可修。Rust 对照：Copy 类型按值隐拷、Move 类型传参即移（源失效）——CN 类对象默认=深拷语义（宿主口径）。
**4. 下一棒（001-001 续）**：①**所有权归一化设施**（v2p 按值形参拷贝/元素读取拷贝接管/入容器位归一化·多轮大设施·波次计划随立账细化）→113/127_v2/218/497 一揽子清偿；②79_v2 二分；③J 探针暴露的结果成员链偏移缺口另立案；④524 随 592 链。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。
**5. 诚实边界（回填）**：本轮纯定性（asm 铁证+探针）零代码修复——所有权归一化设施=多轮工作量；探针资产 target/p610c 不入库（源码形态 025 可考）。


### 1.0 实施计划（**608-a 开发轮·🏃 001-001 续=有析构类元素注入+113 堆损坏清偿**·看板先行 44ea9ab4·基线 develop b34d9fa1·2026-09-22·分支 `任务/家机-608-dtor注入`·wt608 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 607-a（撤守卫态 526=523/3·残余 113/524/79_v2）。本轮主刀 **有析构类元素注入**（宿主 injectClassElemDestroy 同构）：容器元素=有析构用户类（类+析构方法）时——全量（~容器/清空）经 IR 层元素遍历循环逐元素 Call 元素析构器（this=元素内联地址）；单槽=析构元素（参数 索引）/链表 删除头部/删除尾部（字段 头/尾索引）；**析构被移除=no-op**（有析构类被移除槽由 析构元素 链负责·宿主 76-a 口径）。**验收**=①113 转绿（0xC0000374→0·双编译对照）②撤守卫态全量 526=525/2（余 524/79_v2）③反证双态④生产态门禁 4/4 零影响。113 若注入后仍红=栈双 free 嫌疑 gdb 定位续刀（同轮）。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成类.cn`（注入主体+循环发射）｜六件套。TEMP 撤守卫=本地验证态不提交；src 零改动（纯 v2 树面·无新运行时辅助）。
**3. 实施步骤**：①读 v2 循环/条件发射基建（生成语句.cn 当循环/如果 的块发射形态+IR_条件跳转/IR_比较 指令序）②新函数 注入有析构类元素析构：判定（元素=用户类+类有析构）→取数组基址/元素数量→发射循环（i=0..数量：元素地址=数组基+i*步长→IR_删除对象）〔链式=链游循环〕③单槽直线（析构元素/删头/删尾·IR_删除对象 直发）④生成方法 挂接（注入②③/81-a 块后·同表匹配+互斥链：字符串→②③；结构体串字段→81-a；有析构类→本注入）⑤TEMP 撤守卫→重建→113 单跑→全量→反证→生产态 ci.ps1→integrate。
**4. 验证链**：v2p 重建→113 单跑（对照 rc）→撤守卫态全量（525/2 判定）→反证（stash 撤注入→113 复现堆损坏）→生产态 ci.ps1 4/4→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①循环发射为 v2 生成类.cn 首例——先以 生成当循环 的既有发射序为对照基线；②继承链析构（父类析构器）若 IR_删除对象 已展开则零额外面（对照 注入析构字段级联 消费同款）；③113 若注入后仍红→同轮 gdb（asm 堆写点+cn_free_tracked 配对）定位拷贝构造双 free；④524/79_v2 非本面照旧登记。
**6. 与他机交叠分析**：远端 develop=b34d9fa1（本机 607 刚集成·最新）；深度机 550-T94 呈报态（src/v2 语义面·与本轮生成类.cn 注入段无行级交叠）；单位机验收轮零交叠；同机 wt.py list 已核（wt607 已清）。冲突判定：独立。

### 1.0 阶段回填（**608-a 收工=有析构类元素注入落地+113 根因再修正（拷贝链组合态）**·wt608·2026-09-22）

**1. 注入落地（勘误两处后正确）**：生成类.cn 注入主体+注入元素析构体（全量 IR 遍历循环〔平铺 i∈[0,数量)/链式链游 idx+步数防环〕+单槽直线；跨块值槽化 88-a 口径；发射条件跳转/新块/切换块/发射块标签 生成当 同款基建）。**勘误①**：元素合成名必须刀位法取完整余部（首版 合成名首实参/段首 把「映射$整64,符号」截成「映射」→unresolved「映射$~映射」实测）；**勘误②**：**IR_删除对象 不可用于容器内联元素**（其展开=call 析构器+__cn_object_delete 释放对象本身——内联元素内存属容器数组=堆损坏风险；改 IR_调用参数化 直调 析构符号(实例合成名)〔码生成自动 符号修饰·宿主 dtorBody 同构·无对象删除〕）。
**2. 113 根因再修正（推翻 607 定性·探针五态铁证）**：注入正确落地后 113 仍 0xC0000374 同码——「有析构类元素注入缺失」非根因（或非唯一）。探针裁剪（target/p608c/ 资产）：**单面全绿**=C 形参拷/D 返回拷/E 声明初始化拷 各自 rc=0（宿主对照同绿）；**组合态段错误实锤**=层拷 完整链（形参拷+函数内声明拷+返回+主函数使用+设置）rc=139——**113 堆损坏真凶=映射 类对象多副本叠加的拷贝/析构次序缺陷**（v2p 拷贝构造克隆体或副本生命周期管理——比容器注入更深一层=v2p 编译器级）。
**3. 下一棒（001-001 续·修正）**：①映射 拷贝链组合态 bisect（B 探针 139 复现资产在案·asm 副本次序分析/对照宿主拷贝构造 asm）→113 清偿；②79_v2 fix_s 段错误二分（不变）；③524 随 592 链。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。
**4. 诚实边界（回填）**：①析构被移除 对有析构类 no-op（宿主 76-a 同口径）②结果/可选 联合体条件释放+容器字段（ClassObj）元素面=v2 诚实边界续登记③继承链元素析构（父类析构器）随 IR_调用参数化 直调天然走克隆体~链——未单独验证=登记。
**5. 收工决策（撤回注入·develop 恒绿）**：撤守卫态全量回归 526=520/6——**注入引入 3 新红**（127_v2/218/497 堆损坏）——定性：v2 撤守卫态类对象容器**此前「析构面缺失=泄漏但稳定」**；激活析构面暴露**上层所有权归一化设施缺位**（宿主波 3 设施：②调用返回接管/③转移真move/⑤复制落堆/形参被调接管——v2 全缺·句柄多槽共享=双析构）——**注入前置依赖=类对象容器所有权归一化设施**（宿主级大设施·另立前置）。608 代码撤回（生成类.cn 还原 607 态·注入代码 608 分支提交可考）——集成面=纯文档（定性修正+勘误知识+探针资产）。**勘误知识入册**：①元素合成名刀位法（段首截断）②IR_删除对象 禁用于内联元素（含 __cn_object_delete）③哈希短化名排查法（v2p.asm 注释头定位）④手工链五件套。


### 1.0 实施计划（**607-a 开发轮·🏃 001-001 续=81-a 结构体字段元素注入**·看板先行 cb7cb3dd·基线 develop a0042ead·2026-09-22·分支 `任务/家机-607-81a注入`·wt607 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 605-a（注入①②③后撤守卫态残余 6 红）。本轮主刀 **81-a 结构体字段元素注入**（宿主 injectOwnedStrFieldElemDestroy/emitOwnedStrFieldFreesAt 同构）：容器元素=含拥有型串字段结构体时，容器消亡/移除路径逐元素释放字段串。**第一刀**=一层结构体直串字段（全量 ~容器/清空+单槽 析构被移除/删头/删尾）——预期 234 残余（229→255）/238 ⑥（32 位）；**第二刀**=嵌套结构体递归（内盒/外盒 路径展开）——预期 240（8+64 位）。验收=①逐例 PASS（234/238⑥/240）②撤守卫态全量红名单收缩（6→≤3：余 113/524/79_v2）③反证双态（撤注入→红复现）④生产态门禁 4/4 零影响。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成类.cn`（注入主体）｜如需布局查询辅助=`CN语言编译器v2/IR/IR容器判定.cn` 或同文件新函数｜六件套（021/025/交接/更新日志）。TEMP 撤守卫=本地验证态不提交。
**3. 实施步骤**：①宿主对照精读（injectOwnedStrFieldElemDestroy+emitOwnedStrFieldFreesAt+isOwnedStrFieldElemContainer 判据=元素结构体含串字段〔含嵌套/结果可选联合体条件〕）②v2 布局收集=元素类型→串字段偏移路径表（查结构体布局键/字段偏移基建·一层先行）③IR 发射=方案甲编译期展开（Rust drop glue 同构·性能第一）：全量=发射 IR 循环（生成块/切换块/发射跳转基建）逐元素逐路径 LoadPtr→空判→释放；单槽=直线④重建 v2p→234/238 单跑→第二刀嵌套→240⑤反证双态+全量+生产态门禁+integrate。
**4. 验证链**：v2p 重建→单例（234/238/240）→撤守卫态全量（红名单收缩判定）→反证（撤注入→红复现）→生产态 ci.ps1 4/4→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①结果/可选 联合体条件释放若第一轮未覆盖=诚实边界+显式登记（零红目标内若无用例涉此则随 001-001 续轮收口）；②238 ⑧环境敏感 UB 归 81-a 后复测定位（若 81-a 后仍红=独立 bisect 轮）；③113/79_v2 非本写集面=本轮顺带归因不动代码（归因结论入 025）。
**6. 与他机交叠分析**：远端 develop=a0042ead（本机 605 刚集成·最新）；深度机 550-T94 呈报态（src/v2 写集·其 v2 语义面与本轮生成类.cn 注入段无行级交叠）；单位机验收轮零交叠。同机无其他在飞会话（wt.py list 核实）。冲突判定：独立。

### 1.0 阶段回填（**607-a 收工=81-a 结构体字段元素注入一次到位：234/238/240 三例全绿·撤守卫态 6→3 红**·wt607·2026-09-22）

**1. 实测（回填）**：①io_api.cpp 新增三运行时辅助（__cn_vector_free_field_strings 平铺全量/__cn_chain_free_field_strings 链游全量/__cn_seq_free_field_slot 单槽·fieldOffset 编译期常量参·74-a 幂等清槽模型·宿主不调用=纯撤守卫态 v2p 注入面）；②生成类.cn 注入主体（与②③同表匹配）+收集串字段路径（递归内联偏移叠加·深度 ≤4 防环·嵌套第二刀一次并入）——**234=255/238=255/240=255 三例全绿**；**反证双态**（stash 撤注入→234 复红 229 精确/恢复→255 绿）；③撤守卫态全量 **526=523/3**（红名单精确收缩=113/524/79_v2）；④**238 ⑧「环境敏感 UB」勘误**=字段串释放面缺失的表现形态（临时槽/容器元素串残留计数在 runner 环境显形）——81-a 补齐后 runner 与直跑两环境归一全绿·UB 定性撤销。
**2. 113 定性（本轮侦查·不动代码）**：0xC0000374 堆损坏=向量<映射<整64,符号>> 类对象容器+按值传参深拷+弹出 释放内部数组+删除交换式 复合面——嫌疑=v2p 缺**有析构类元素注入对照**（宿主 injectClassElemDestroy：析构元素 链 Call ~映射·v2 析构元素 空体零注入）+映射拷贝构造深拷面双 free 嫌疑——下一刀主攻（asm/gdb 定位）。
**3. 79_v2 定性（本轮侦查·不动代码）**：fix_p（v2p 编译 v2 全树）正常·**fix_s（cn_self=自举第二跳）在源码加载解析链中途 0xC0000005**（核心 78 节点→容器 1642→文件 1786→词法 5084→预处理 6050 后崩）——撤守卫态自举第二跳缺陷（v2p 生成代码缺陷或 cn_self 撤守卫内存面）——独立立案（gdb/二分）。
**4. 诚实边界（回填）**：①结果/可选 联合体条件释放面（宿主 condOffset/viaUnion）v2 未覆盖——234/238/240 验收面外·登记 001-001 续；②容器字段（类别 1·ClassObj）元素面同上；③113/79_v2 归因侦查完成·修复=下一轮。
**5. 下一棒（001-001 续）**：①有析构类元素注入（injectClassElemDestroy 同构）+113 堆损坏 gdb 定位；②79_v2 fix_s 段错误二分；③524 随 592 链。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。


### 1.0 实施计划＋阶段回填（**605-a 开发轮·🏃 001-001 接力=容器实参取地址三回扫源单点化→析构注入设施补齐**·看板先行 41d57364·rebase gitcode/develop 591ab091·2026-09-22·分支 `任务/家机-605-三回扫源`·wt605 隔离·用户令多轮不间断至 001 销项）

〔本节内容=605 在飞态整节承载（六要素 376b3dba 落盘+阶段回填续更）；rebase 与 606-a §1.0 整节替换冲突——606-a 已收官入 develop（fd5f7f13）·其内容 git 可考·按「一分区一记录」纪律本节让位当前在飞轮。交叠序兑付：606 先集成→605 收工在 021 树中直接更新 001-001 节点行。〕

### 1.0 实施计划（**605-a 开发轮·🏃 001-001 接力=容器实参取地址三回扫源单点化**·基线 develop 41d57364·看板先行 41d57364·2026-09-22·分支 `任务/家机-605-三回扫源`·wt605 隔离·用户令多轮不间断至 001 销项）
### 1.0 阶段回填（**605-a 第一刀=fix5c 重做实测：127_v2/497 转绿·7 红码值纹丝不动（非源①路径）·113 段错误→堆损坏（数据流通）·78/79 内存=runner 硬编码**·2026-09-22·wt605）

**1. 实测**：fix5c 直改源①（生成表达式求值:517 容器方法回扫补 !是容器类）+TEMP 撤守卫态全量 535=524/11——**127_v2/497 转绿**（容器元素链式调用族·正面）；**218/221/234/237/238/240/283 七红退出码逐位不变**（31/128/229/251/223/183/8——完全不走源①路径·归源②③或元素写释放面）；**113 从 0xC0000005 段错误→0xC0000374 堆损坏**（句柄直传后数据流通·剩堆写管理错——比纯段错误信息量大）。
**2. 新定性**：①78/79 内存红=**runner 对自举用例硬编码 内存上限MB默认（4096）**（run_e2e.py:740-744——CLI --max-mem-mb 传不进 78/79 内部判定）——撤守卫态 4.2~4.3GB 必撞·修复=该处改读 CLI 覆盖值或调默认（随撤守卫落地轮契约化）；②524 备案照旧。
**3. 下一棒（605 续）**：113 堆损坏 bisect（asm 堆写点）→七红逐例归因（源②③+元素写释放面）→78/79 runner 上限修→撤守卫态零红。
**4'. 注入设施①落地实测（605-a 续）**：生成类.cn 生成方法 体首注入映射释放两调用（__cn_map_free_strings 全量三方法/__cn_map_free_slot 单槽两方法·6 参 ABI 对齐宿主运行时 io_api）——**坑=是析构器 判定用「~」+全合成名比较·克隆体源码名「~映射」恒假→析构器漏注入**（首版 释放内部数组 命中/~映射 漏·探针 delta=1 不动）——改 方法名=="~"+剥实例基名 直接判定→**探针 delta=0 零泄漏+218 全例 PASS**+**221 码值 128→135**（注入部分生效·剩余 120=集合/嵌套面）。**诚实边界**：283（字段容器接收者·码 8 不动）/113（堆损坏）属其他族未收口；221 剩余面=集合/嵌套注入扩展；均下轮继续。
**5. 嵌套探针复测**：向量<映射<整64,字符串>> 追加+析构 delta=0（嵌套析构链已通——注入设施①覆盖）——221 剩余缺口在其专属形态（删除交换式/覆盖写面·下轮逐形态探针）。
**4. 218 归因（第一例收口）**：⑥⑦（映射值串释放链泄漏）=**宿主编译器注入设施族缺口**——stdlib 映射 的 ~映射/清空 源码本体只 free 四数组（值数组 free 不级联串）/清空 只清桶——值串释放靠宿主编译期注入（ir_oop_container.cpp：析构键值→__cn_map_free_slot 单槽释放注入〔仅删除路径〕+容器消亡/清空 注入）；v2p 编译 stdlib（撤守卫态）缺同款注入对照→值串泄漏。**v2 需补三设施**：①析构键值 的 __cn_map_free_slot 注入（生成类 钩子机制·移动元素 514-a 同款）②清空/析构 的值串释放注入 ③嵌套元素析构链（向量$映射$ 的元素映射对象析构级联）。七红其余例（221/234/237/238/240/283）疑同族（映射/链表 值串+槽复用释放链）——逐例对照注入设施清单收口。
**6. 注入设施②③落地实测（605-a 续·2b5a5d22）**：生成类.cn 容器族注入（宿主 injectStrElemDestroy 76-a 字符串元素路径同构）——**全量**（~类名/清空）：链表/队列=__cn_chain_free_strings(this,值表偏,下一索引偏,头索引偏,元素数量偏)〔5 参链游·83-a 空闲链槽复用安全〕、向量/栈/集合=__cn_vector_free_strings(this,数组偏,元素数量偏)〔3 参平铺〕；**单槽**（唯一持有者槽·76-V1）：析构被移除=参数 索引（向量/集合 删除/覆盖前）+链表 删除头部/删除尾部=字段 头/尾索引（IR_字段地址+IR_指针加载·体首注入=删除前旧值）→__cn_seq_free_slot；元素=字符串才注入（合成名首实参段判·向量$盒子 不命中）·析构元素=移位目标槽重复引用不注入。**实测：221=255 全对+283=31 全对+237=255 全对+234 197→229**；**反证双态**（stash 撤②③→221 复红 135/恢复→255 绿）；生产态零影响（容器走链接期供给不经 v2p 源码编译路径）。
**7. runner 内存上限 worker 传递修复（605-a 续·b661ffb4·§8.6 机制级）**：win spawn 模式进程池 worker 重导入模块→主进程 --max-mem-mb 对 内存上限MB默认 的 global 覆盖**不进 worker**（604-a 进程池改造回归面·第一刀「CLI 传不进」定性实锤）；修复=覆盖值随任务项 pickle 传递+worker 侧还原全局（752/819 等消费点一处覆盖全生效）——78_v2 --max-mem-mb 8192 实测 PASS（撤守卫态 4.2GB 不再误杀）。
**8. 撤守卫态全量实态+下一棒（605-a 收工态）**：注入①②③后撤守卫态全量基线 526=517/9（注入②③前口径）→逐例验证后当前红=**113（0xC0000374 堆损坏）/234（229·缺 2+8+16）/238（95·缺⑥32+⑧128）/240（183·缺 8+64）/524（592 备案）/79_v2（fix_s 编译 v2 全树 0xC0000005 段错误·内存修复后新暴露——78 已过/79 段错误）** 六红。**238 ⑧位环境敏感实锤**：runner 环境 rc=95（两次）vs 产物直跑 rc=223（三次）——同产物不同 rc=UB 嫌疑（归 81-a 面定位）。**下一棒（001-001 续·新轮认领）**：①**81-a 结构体字段元素注入**（宿主 injectOwnedStrFieldElemDestroy 同构=全量遍历逐元素释放字段串+单槽·含嵌套结构体递归/结果可选联合体条件释放——v2 IR 需发射循环+字段展开，或运行时辅助+偏移表方案〔性能对照后定〕）——预期消 234 残余/238 ⑥/240；②238 ⑧环境敏感 UB 定位；③113 堆损坏 bisect（asm 堆写点）；④79_v2 fix_s 段错误归因；⑤524 备案随 592 链。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。

**1. 目标与验收判据**：承接 595-a（集成 9d6e6e68·判定面单点化两批十件·撤守卫态 4 红转绿）。残余 12 红（113/127_v2/218/221/234/237/238/240/283/497/79+524 备案）全族定性=**容器方法容器实参取地址三回扫源**：①生成表达式求值.cn:517（容器方法调用·直查签.参数串段）②生成类.cn:570（用户类方法·回扫结构体与引用实参）③生成表达式求值.cn:1005（自由函数·同②辅助）。撤守卫态签.参数串段（T 单态化=容器实例名）查结构体大小命中→回扫取地址传栈槽→被调方 fix5 单槽当句柄→元素槽伪对象。**本轮目标**：三源判据统一=「容器实例实参恒句柄直传不取地址」单点化→撤守卫态 11 红清零（524 除外·备案）；**前置刀=v2p 错误 1024 定性**（fix5c 直改源①触发 v2p 编译期运行时错误 1024·已撤留案——最小复现+bisect 定根因后三源修复才能安全落地）。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成表达式求值.cn`（源①③判据统一）｜`生成类.cn`（源②）｜`IR容器判定.cn`（如需新单一判定辅助=是容器实参段）｜+tests/e2e 新例+coverage_map+六件套。TEMP 撤守卫仍为本地验证态不提交。
**3. 实施步骤**：① **1024 定性**：wt605 重做 fix5c 直改→v2p 重建→最小用户程序（单文件 导入 容器+一个容器实参调用）编译复现 1024→--verbose/语句裁剪 bisect 崩点（嫌疑=是容器类 字符串扫描对 签.参数串段 的边界形态·或短路求值面）→定性记录；② 按定性结果设计三源统一判据（方向=抽公共辅助函数 是容器实参段(段文本)——内部处理 & 尾缀/嵌套 $/边界·避免直嵌 是容器类 的潜在炸点）；③ 三源切换新判据+单点化；④ TEMP 撤守卫态全量 E2E（11 红清零判定）+生产态门禁；⑤ 反证双态+六件套+integrate。
**4. 验证链**：v2p 1024 复现/消除对照→TEMP 撤守卫态全量（526 基线+11 红名单）→生产态零警告+单测+E2E+strict/matrix+自举链→asm 宽度→反证（撤判据统一→伪对象红复现）。
**5. 边缘情况与诚实边界（预判）**：① 1024 若定性为 v2p 自举编译器深层缺陷（非本判定面）→独立立案·三源修复改用「无字符串扫描」实现（查结构体大小 前置白名单集合）；② 524 条件编译=592 备案非本面（allow-known-red）；③ 79 自举 fix_s 若涉签名表双轨=独立定性；④ 生产态等价红线同 595（判据显式化不改生产态发射）。
**6. 与他机交叠分析**：远端 develop=aa797d2b（本机 595 刚集成·最新）；深度机 592-a 271 锚归因/单位机 604 验收=非本写集；同机 592 会话已集成完毕。冲突判定：独立（v2 IR 层·595 同域续作）。

### 1.0 阶段回填（605-a 续更·wt605·2026-09-22）


**进度①（已提交分支）**：v2 函数原型声明解析层（语法分析.cn 解析函数声明·->类型 后非{ =前向原型·空体函数节点）——03 原型+定义从「吞全文件」→正常解析；591-a 已集成（634f41bd·170 落地+524 既有红确证）。
**待精确归因（592-a 续）**：271_v2 冒号后置负测——592 原型分支后 v2 节点计数 12→11（expected 锚失配·两次盲改均红）——**须先定位节点计数变化机理**（原型分支 vs 参数冒号解析交互·探针=单文件计数对照）再定锚值；配套缺口=函数指针变量调用未支持（03 的 回调 形态·独立立案）。
**后续轮次**：D40 桶续（类继承/成员修饰/运算符重载）→123/425（类型推导基础设施协调轮）→v1 收口（002 尾件）。

### 1.0 收工回填（**595-a ✅ 001-001 架构轮判定面单点化=两批十件落地·撤守卫态 13 红→12 红净修 4+定性归族**·2026-09-22·分支 任务/家机-595-双模收敛·wt595 隔离）

**1. 达成**：①**判定面单点化两批十件**（cc3bbd81+21e158bb）——fix1 局部声明分派容器前移〔根因1〕/fix2 容器方法返回恒标量〔根因2 调用方〕/fix2b 被调方返回判定同构〔生成类+生成函数〕/fix4 类型名宽度容器分支〔根因4〕/fix5 容器参数恒单槽〔根因5 被调方〕/fix5b 函数实参结构表两处排除〔根因5 调用侧〕/fix6 裸字段赋值容器守卫〔根因6·506-a 同款〕/fix7 字段释放容器显式分支〔根因7〕/fix8 下标写调用右值入整体写通道〔根因8〕/fix9 类源标识符源地址改句柄〔根因3 写侧〕；②**架构定性反转**：宿主本为双模设计（对象位置=句柄/元素位置=内联·侦察+宿主 asm 考古实证：元素 返回 rax=元素地址〔imul 56〕、拷贝构造 rax=句柄、genCopyBuiltin ③容器=NewObject+拷贝构造/④结构体=tmp 槽+CopyStruct）——v2 病根=撤守卫态容器实例双表登记（用户类们+结构体大小表）后 12 消费点判据翻转抢道，非双模本身；③**实证**：撤守卫态全量 535=523/12（585-a 基线 13 红中 **219/228/285/67 四红转绿**+78_v2 内存处置（--max-mem-mb 8192 调参·撤守卫态全树编译 4.2GB 超 4G 默认））。
**2. 残余定性（12 红全族归一）**：113/127_v2/218/221/234/237/238/240/283/497/79+524（592 备案·非本面）——**根因=容器方法容器实参取地址三回扫源**（生成表达式求值:517 直查签.参数串+生成类:570+生成表达式求值:1005 表驱动）：撤守卫态签.参数串 段（T 单态化=容器实例名）查结构体大小命中→回扫取地址传栈槽、被调方 fix5 单槽当句柄→元素槽=「句柄+栈垃圾」伪对象（链查找 this.桶数量=垃圾 实锤）；**fix5c 直改（!是容器类 排除）触发 v2p 自身编译期运行时错误 1024**（机制未明·已撤·立案注记在源码）——**596+ 主攻面**：三回扫源统一单点化+1024 定性。
**3. 坑**：①哨兵定位法陷阱：**运行时错误=立即退出（单点哨兵）+expected/rc 配置不符时 runner 在宿主侧 D2 拦截→v2out 不更新→旧产物假信号**（本轮两犯·代价 ~6 轮）——正确姿势=配置 退出码=宿主哨兵 rc+每次核 v2out mtime；②E2E 运行中改 v2 树源码=混合态跑（runner 指纹中途重建·红名单失真不可归因）；③同机并行会话直推竞速（592 集成中·看板 Edit 三次撞 mtime——python 脚本 read-modify-commit-push 原子化破解）。
**4. 下一棒**：596+（本会话续）=fix5c 三回扫源单点化主攻（含 v2p 1024 定性）→撤守卫态零红→守卫删除落地+借链 51 例转自洽→001 销项。

### 1.0 实施计划（**595-a 开发轮·🏃 001-001 撤守卫容器双模语义收敛=架构轮判定面单点化**·基线 develop b2603b96·看板先行 b2603b96·2026-09-22·分支 `任务/家机-595-双模收敛`·wt595 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：001-001（P0·585-a 定性立案·收敛方向已裁决=方案甲全内联对齐宿主·593-a 落案）。撤守卫态（TEMP 撤 主.cn:64 守卫·本地实验态不提交）全量 E2E 残余 13 红（113/218/219/221/228/234/237/238/240/283/285 容器所有权类支持族 11+67 泛型返回+79 自举 fix_s）**清零**；生产态（提交态）全量门禁零回归。架构定性（595 侦察实证）：宿主本为双模设计——对象位置（局部/参数/返回/字段/静态）=句柄 8B+NewObject，元素位置=内联整对象（stride=元素大小·拷贝构造原地构造）；v2 病根非双模存在，而是撤守卫态容器实例同时登记 用户类们+结构体大小表（IR类.cn 实例化注册）后 **12 消费点判据翻转抢道**（查结构体大小/是用户类 由 miss 变 hit）。验收=①生产态 ci.ps1 4/4 全绿+strict/matrix；②TEMP 撤守卫态全量 E2E 13 红名单全绿（其余不新增红）；③关键修复点反证双态；④113/283 等逐红单跑 PASS 实证。

**2. 写集（预登记·收工回填最终清单）**：`CN语言编译器v2/IR/IR容器判定.cn`（如需新增单一判定辅助）｜`IR布局.cn`（类型名宽度 补容器分支定性）｜`IR容器.cn`（签名返回字节数/返回形态 容器恒标量排除）｜`生成函数.cn`（登记参数槽 容器恒单槽句柄）｜`生成类.cn`（生成方法 参数登记同构）｜`生成声明.cn`（局部声明分派 容器优先/用户类排除容器实例）｜`生成语句赋值.cn`（裸字段赋值通道补容器守卫+下标写通道定性）｜`IR容器释放.cn`（字段释放面顺序/资源字段登记定性）｜`IR类.cn`（实例化布局登记 资源字段补登·如需）｜`生成表达式求值.cn`（元素读写/复制展开定性）｜+tests/e2e 新例（如判定面加锚）+coverage_map+六件套文档。**TEMP 撤守卫（主.cn 守卫恒假）仅为本地验证态·禁止提交**——撤守卫落地=001 波3 收尾轮（596+）。

**3. 实施步骤**：① wt595 复现：TEMP 撤守卫→v2p 重建→13 红逐例单跑归因（asm/IR 三件套证据·分类=判据翻转面/通道缺失面/期望漂移面）；② 判定面单点化实施（按根因序：局部声明分派 容器先于用户类→参数 ABI 容器恒单槽→方法返回容器恒标量→裸字段赋值容器守卫→字段释放面/资源字段→元素读写通道→类型名宽度 步进）——每处修复遵循「生产态判据显式化路径等价·撤守卫态对齐宿主语义」；③ TEMP 态逐红复测+全量 E2E（13 红清零判定）；④ 撤守卫还原→生产态全量门禁（gate_lock 串行）；⑤ 反证双态（选 2~3 关键修复点：撤修复→撤守卫态红复现→恢复→绿）；⑥ 六件套+integrate（协议 v3·本机门禁绿即集成+两机异步验收广播）。

**4. 验证链**：TEMP 撤守卫态全量 E2E（--jobs 并行·红名单对照 585-a 终态 528=513/13/2）→生产态零警告构建+单测+E2E 全量+strict 179/matrix 83+自举链 78/79→asm 宽度门禁→反证双态→11 红用例逐例单跑 PASS 清单。

**5. 边缘情况与诚实边界（预判）**：① 79 自举 fix_s 红=撤守卫自举（v2 编译 v2 全树含 stdlib 源码）——若属签名表双轨/符号公式面（根因10）可能超出本轮判定面范围，如实定性登记勿硬凑；② 67 泛型返回=T70 面⑤残余（585-a 注记·可能与返回物化同根）；③ 228/67 构建间布局漂移不稳定红（驻留序/哈希跨构建漂移）——单点化后应自然消解，若仍在=独立立案；④ 生产态字节级等价红线：判据显式化不得改变生产态任何发射路径（未分配 fallback 逐字节不变先例）——生产态对拍锚（asm md5）兜底；⑤ 050 宿主 ICE/300 默认参数/524 条件编译=592 轮备案红（非本任务面·allow-known-red 点名在案）。

**6. 与他机交叠分析**：深度机 549/550/551/585 均已收工挂等/集成完毕（看板行过时）；单位机 557/558/559 挂等回签中（写集=宿主语义+arm64 后端·与本轮 v2 IR 层不相交）；同机会话 592-protodecl 集成中（写集=v2 语法/解析层 原型声明+271 expected——与本轮 v2 IR/生成层**文件级不相交**·rebase 时序上 592 先集成则本分支 rebase 即可·语义交叠=v2 树整体节点数锚〔271〕由 592 维护·本轮若触 v2 树规模面以 592 后基线为准）；远端 develop 现态 b2603b96。冲突判定：独立（写集不相交·语义耦合=v2p 重建基线按集成时序对齐）。

### 1.0 收工回填（**589-a ✅ 005 甲案 v2 树同构+050 双测上架=全链销项**·2026-09-22·分支 任务/家机-589-t81v2sync·wt588 复用树）

**1. 达成**：① v2 取地址分支（生成一元运算）&类变量→IR_加载(槽) 产对象地址（T_对象·与宿主 588-a 同构）；② 生成声明 类分派+结构体分派补 指针层数==0 排除（`点* p2` 声明解析层=类型文本"点"+附加=1，误入类对象构造路径〔拷贝初始化拒绝〕/结构体整体初始化拒绝——**该形态此前编译拒绝=纯新增支持面零回归**）；③ 050 双编译对照上架（501 号被 499 链占用改 050——宿主+v2p 双侧 42/42/99）。
**2. 验证链**：050 双测 PASS+全量 E2E **534=534/0/0**（含 584 遮蔽面零回归——首轮两例红=wt588 宿主 exe 陈旧定性〔588 时代源码不含 584 修复〕重建后全绿·**多轮共用 worktree 必须先重编宿主再跑全量**）；自举链 78/79 绿。
**3. 坑**：① v2 类模型要求显式构造器（050 首版缺→补 `函数 点(){}`）；② 用例号 501 已被 499 链占用——新用例上架前查 tests/e2e/ 号占用；③ wt588 树跨 588/589 两轮复用——branch 切换后 target/cn.exe 是旧源码产物（git 不管理 target/）——全量门前必须 ninja 重编。
**4. 005 销项**：全链完成（宿主+v2+050+条文）迁 §4.2；591=解锁面验证（迭代器持容器指针·033 行接力）。
**5. 下一棒**：591（033 接力）。

### 1.0 实施计划（**589-a 开发轮·🏃 005 甲案接力=v2 树同构+050 双测上架**·基线 develop bc7e7a3b·看板先行 bc7e7a3b·2026-09-22·分支 `任务/家机-589-t81v2sync`·wt588 复用树·用户令多轮不停）

**1. 目标与验收判据**：v2 树 `&类变量` 语义对齐宿主 588-a（产对象地址 Load 槽·含静态类 LoadPtr 面）；050 双编译对照用例上架（probe_f1 场景·宿主+v2p 双侧 42/42/99）。验收=050 双测 PASS+全量门禁绿+v2self 自举链绿。
**2. 写集**：CN语言编译器v2/IR/生成表达式运算.cn+生成表达式.cn（按侦察）+tests/e2e/050_取地址类变量双编译/+021/025/交接/日志本机节+看板行。
**3. 步骤**：①v2 取地址分支加类变量判定→Load 槽（复用 生成表达式.cn:585 类基加载同款 IR_加载 T_对象）；②类指针字段读剥指针核查（v2 生成表达式字段.cn）；③050 用例上架（源码+expected 已固化 target/p588/501_候选→迁入改名）；④全量门禁→integrate。
**4. 验证链**：050 双编译单跑+全量 ci.ps1（gate_lock）+自举链 78/79。
**5. 边缘**：①v2 类型判定设施=指针变量类型们/静态面（查静态序号+查结构体大小）——类变量判定须新增或复用类表查询；②v2p 重建后 78/79 自举链锚定面（v2 树改动=v2p 源码变化→锚定链重锚可能）；③收工回填。
**6. 交叠**：v2 树 IR 目录与在飞链零交叠（585 已集成）；本机 586/578/567 等写集不同文件。

### 1.0 收工回填（**588-a ✅ 005〔原B-T81〕甲案宿主侧收官=取地址语义一致化+类指针字段读剥指针**·2026-09-21/22·分支 任务/家机-588-t81addr·wt588 隔离）

**1. 达成**：① **取地址语义一致化**（用户 09-21 裁决甲·宿主）：`&类变量`→Load 槽产**对象地址**＋`&静态类变量`→LoadPtr 符号槽＋`&结构体/标量` 维持 AddrOf——refWrapAddr 标志（UnaryExpr 新字段·wrapRefArgs 置位）保证引用形参链路 A-1 契约字节级不变（probe2 写回/读/方法/换绑 99/99/198/7 全对）；② **类指针对象字段读剥指针**（handleClassMemberExpr·emitInstanceMethodCall 同款剥法）——呈报缺陷实为两层叠加（取地址错位+字段读降级常量 0），probe1/3 实测 42/42/写回 99；③ 条文落盘 plans/001+spec 02；④ 集成验收修 551-a win ASCII 红灯（semantic_expr_access 物化类型→materializedType·§8.3 当场修·B9 族扫全库无残留）。
**2. 验证链**：单测 1394=1394；全量 E2E 528=528/0/0 零回归（分支上实跑）；integrate 全量门禁 PASS 两轮（合并结果上·第 1/2 次竞争输家重跑均绿）；探针四态资产 target/p588/；新旧宿主对照双态记录。
**3. 诚实边界**：① **v2 树同构挂 589**（生成一元运算 取地址分支+生成左值地址 类变量判定——v2 `&类变量` 现仍=槽地址同构错位）；**新用例（改号 050——501 已被 499 链占用）随 589 双编译对照上架**（双测铁律：不上架单侧用例·按六要素⑤④「或按规则报备」分支改道）；② 二级指针推导缺（新旧同态）立 048；安全区引用误报立 049；③ v2self 6 处 &静态容器实参=wrapRefArgs 包装场景零行为变化实证；v2 树唯一显式 &（签名返回字节数(&签)·签=结构体）行为不变。
**4. 坑**：① 声明初始化位取地址走 genExpr→visitUnaryExpr（无独立路径）——修取地址语义须标识符/静态/包装标志三处对齐；② 新旧宿主对照探针剥洋葱定位第二层缺口；③ 竞争高峰 rebase 三连撞（585 立号撞 047/582·584 收官撞行）——025/021 文档合并改用「重置 develop+代码提取+文档现态重做」法（精细 rebase 双层标记叠加不可靠·583 让位先例变体）；④ 501 用例号已被 499 链占用——新用例改 050（用例号与轮次号两空间·仍须查重）。
**5. 下一棒**：589=v2 树同构+050 双编译对照上架；591=迭代器持容器指针解锁+T94 阶段二收编；048/049 待认领。


### 1.0 实施计划＋收工回填（**584-a 开发轮·🏃✅ 011 遮蔽家族全链=T47 宿主遮蔽生效+011-001 v2 覆盖型+346 转双编译**·基线 develop 9054c18e·2026-09-22·分支 `任务/家机-584-004scope`·wt584 隔离）

**1. 目标与验收判据**：011 行实施（D36+T47 同根合流）——①宿主块内/形参遮蔽顶层常量语义生效（读点取变量非恒内联常量）；②v2 嵌套同名标量遮蔽独立槽（合槽污染根治）；③346 expected 改 109912〔已批〕+转双编译对照。**验收实测**=346 双编译 PASS（宿主 109912+v2 109912·修前宿主 101012）+504_参数遮蔽常量 PASS（修前宿主 6 错/v2 11 对）+505_嵌套变量遮蔽边界 PASS（修前 v2 22 错/宿主 21 对·修后双侧 21）+单测 1394/1394+451/452 泛型运行级复验（019 观察判据·全绿）+反证双态（回退 IR 层→346 复现 101012→恢复复绿 109912）。
**2. 写集（最终）**：src/cn_compiler/ir/ir_expr.cpp（visitIdentifierExpr 常量内联移至变量家族〔局部/静态局部/顶层静态〕之后）｜src/cn_compiler/semantic/semantic_expr.cpp（checkConstIdentifier 入口加 scopes_[1..] 同名守卫——顶层常量口径零变化）｜CN语言编译器v2/IR/生成声明.cn（块清单栈+块名旧槽平铺 平行向量：覆盖型=块内同名命中改新槽+旧绑定登记出块 设置 恢复；删除型照旧；字符串预扫复用面除外=诚实边界）｜tests/e2e/346_遮蔽恢复边界/（expected 109912+双编译对照.txt）｜tests/e2e/504_参数遮蔽常量/505_嵌套变量遮蔽边界/（新·双编译）｜tests/e2e/coverage_map.md（5.9.7 单元+说明表三行）｜plans/025 本节+021 收尾行+看板/交接/日志。
**3. 实施要点**：①语义层不整链换序（checkConstIdentifier 移位会令顶层常量引用走 lookupVar 全局层命中 declareVar 登记→lastType_ 从「按值文本猜」变「声明类型」=存量口径爆破）——改入口守卫（非全局层同名→return false 落变量路径）零口径变化；②IR 层常量块移 lookupVar miss 分支内（变量家族全优先）；③v2 合槽真因=分配标量声明槽 命中复用分支（非 396-a 删除型）——352-a「预扫即合槽源头」的标量面残余。
**4. 验证链（已跑）**：三例双编译单跑+单测+泛型复验+覆盖映射门禁（--strict 179/179）+反证双态——全量门禁随 integrate.py（合并结果上·全量 ci.ps1）。
**5. 边缘情况与诚实边界（回填）**：①嵌套同名字符串标量合槽残余（字符串命中分支维持预扫复用——预扫槽与遮蔽在 IR 层不可区分·诚实边界登记 011 行续）+容器/结构体块内声明出块死绑定（语义层块级弹出已挡出块使用·无害）；②同块重复声明=语义拒面不触遮蔽分支；③如果(常量) genIfConst 面=无遮蔽时仍走常量内联（isConstant 保持·239-a 调试模式不受影响）。
**6. 与他机交叠（回填）**：583 同名接力=深度机抢先集成 546 链（23d9fd2b·squash）→583 分支作废删除（内容被承载零损失·v3 竞争输家让位先例）；584 写集（宿主语义/IR+v2 生成声明+5.9.7 用例）与 546 链（主.cn/预处理.cn/4.7 行）零交叠；586 挂等链清偿（504 T48 rebase 集成）与 504_参数遮蔽常量 用例号同号不同空间（E2E 用例号与轮次号两空间·无冲突）。
### 1.0 收工回填（**586-a ✅ 021 🔄 挂等回签清偿轮+integrate.py quotepath 缺陷根治**·2026-09-21·分支 任务/家机-586-挂等链清偿 系·wt447/wt494/wt504 多树·协议 v3）

**达成**：①579 红灯恢复核验 ✅（579 会话已自行入 develop·574 七文件树级 diff 零差异）；②526-a……更正：**568 转批面摘取集成 83de088a ✅**（三分判定=568 唯一未入面 51 例转批·93 文件零冲突摘取·**develop 现态补验全量门禁 4/4 全绿=E2E 528=528/0/0+asm 347×3+自举链双 PASS**）；③**integrate.py quotepath 缺陷根治 63611b1c ✅**（写集分类中文路径 startswith 失配→全量门禁静默跳过·83de088a 实锤·quotepath=false+strip 双保险·反证=修复后同写集分类须全量 True·504 集成实测正确触发）；④041〔原T70〕复验销项 ✅（517 链已集成·补验门禁为复验证据·迁 §4.2）；⑤020〔原D12〕583 会话集成 ✅（23d9fd2b·我链让位协调）；⑥504〔原T48〕rebase 集成 🏃（两轮冲突解=coverage_map 并编/弃过时 502/503 行/跳过已有搭车·单提交 f371795a·全量门禁跑中·集成绿后 012 销项）。

**25 条挂等链三分判定清单（修正版：分支写集∧develop 未动=未吸收）**：
- **零独有销账**：555/539/581（0/0）·502（semantic_stmt.cpp 被后续拆分覆盖）·529/554/564/526（双方动=develop 已重做）·571（revert 已入）·567（⏸ 自持）·550（阶段二呈报态归深度机）·565/546/547（582/583 集成 ✅）。
- **真未吸收（待集成·按批）**：**批A=499**（stdlib 迭代器三件+501 用例——033 行迭代器本体未入！）**+522**（497 转正用例三件）零交叠；**批B=497**（ir.hpp/ir_stmt.cpp）+**500**（ast_printer.cpp）+**505**（semantic_decl.cpp+521 用例）+**527**（class_resolver_access.cpp）；**批C=538**（linux_x64 两文件）+**542**（class_resolver.cpp）+**544**（CMakeLists ASan 隔离）+**556**（diagnostics/driver/cn_main 八件）+**561**（error_analysis.cpp+562 用例·T92 终版·534 是其旧版弃）+**563**（539 用例+v2 语义检查·与 582 链协调后）。551（semantic_expr_access.cpp 与 504 同文件·504 后处理）。

**验证链**：补验全量门禁（输出存 wt447）+quotepath 反证+三分判定逐链 blob 对比（merge-base 法）。

**边缘与诚实边界**：①补验门禁=83de088a 时点（其后 develop 增量 546/547 系由 583 门禁覆盖）；②582 集成 565 的 502/503 用例目录未入 develop（登记也被拆·现态自洽）——565 验收面缺实弹归 582/583 协调；③管道洗退出码三犯（教训入册升级）。

**他机交叠**：582/583/584/588 写集全程零交叠（v2 词法/窄宽/作用域/T81 vs 本轮 tests 通道+scripts）；547 链由 583 集成，我链避让。

### 1.0 收工回填（**575-a ✅ T100 B 组 4/6 落地+170 未实施**·2026-09-21·分支 任务/家机-575-b2v2check·wt575 隔离）

**1. 达成**：① **422**：v2 编译期常量除零（检查二元运算表达式·除/余右字面量 0→拒·对齐宿主 T8）——负测双测绿；② **299**：常量成员函数修改成员检查全链（修饰位掩码扩〔0 普通/1 不安全/2 常量/3 常量+不安全·既有 ==1 消费点改 %2〕+当前方法常量槽+检查赋值语句消费〔查字段类型ID 字段判定·裸字段/自身.字段两形态〕）——负测双测绿；③ **81**：方法调用面私有可见性（修饰位 2=私有〔段可见性<<2·6 调用点·整32 强转〕+检查调用表达式成员被调私有扫段〔当前检查类豁免·340-a 措辞对齐〕——同模块类外私有调用同面顺带收口）——负测双测绿；④ **417**：表达式嵌套深度上限（解析原子括号递归计数·对齐宿主 128·超限诊断+剪枝）——负测双测绿。
**2. 425 回退挂账（诚实披露）**：首版表达式递归推导空类型=诊断副作用（174 的 x 未声明双报·全量 528=527/1）——**回退**；正确形态=调用返回空类型的使用点登记+语句级判定（恒空登记同款·语义分析.cn 需新签名表返回类型查询设施）——挂 T100 行 575 续做。
**3. 验证链**：全量 E2E **528=528/0/0**（终验+425 回退后双确认）+自举链 78/79 冒烟绿。
**4. 坑（回填）**：①v2 诊断串 
 须两字符（python 写入真实换行=「字符串字面量不能跨行」自举红·字节级修复）；②表达式递归推导有诊断副作用（重复诊断）——类型判定类检查须用无副作用推导；
**5. 待办移交**：123（规则1·独立小轮）/425（登记模式重做）/170（v1/stdlib 标注面核查）→T100 行下一棒。另=scripts/integrate.py 加 --win-verified 平台后验模式（2026-09-21 用户令「win 平台完成后直接集成·其他平台自己验证」·本批启用）。

### 1.0 实施计划（**580-a 开发轮·🏃 plans/021 编号体系重构：§三 废分区+全局唯一序号+全生命周期追踪**·基线 develop e7bca406·看板先行 e7bca406·2026-09-21·分支 `任务/家机-580-021编号重构`·wt580 隔离·用户已批方案：全新号段+旧号别名/单一平表+状态列+优先级列/限两级+随父销账）

**1. 目标与验收判据**：①§三 废除 A~G 七分区→单一平表，6 列=`| 号 | 优 | 状态 | 任务 | 下一棒 | 最新轮 |`；②全局唯一任务号 001~999 递增分配退役不复用（子任务 `父号-三位子序` 限两级·销账随父·存量 37 条活任务逐一映射+〔原Txx〕别名），§三 头注锚行「当前最大任务号=NNN」；③全生命周期动线=立账⬜→认领🏃→中间态（🔄挂等/📣待裁决/⏸挂起/🔬观察期）→§三删行→§4.2 销项行（含立案轮→销项轮+子任务清单）→棘轮/月末滚转 `项目记忆/归档/销项台账-YYYY-MM.md`。**验收**：check_handoff/check_progress_sync/check_own_matrix/check_registry_audit 四脚本全绿；四类负测样本（重号/孤儿子号/缺下一棒/📣含已裁决）全被拦截；平表每行四新列齐备；021 行数≤新棘轮基线。
**2. 写集（精确）**：`plans/021-任务进度观察表.md`（§三 重写+锚行+迁移 37 条+§4.2 头注+文首戳）、`scripts/check_handoff.py`（删查总表B区→新增查总表呈报态+查任务号；状态正则扩 `[🔄⬜⏸📣🔬]`）、`scripts/check_progress_sync.py`（老化审计读「最新轮」列）、`AGENTS.md`（§4 C24/D40/D43/T63 补新号别名+§6.8 条1/条2+§8.5 §三-B 铁律改📣态+021 更新时机段术语）、`scripts/文档棘轮基线.txt`（021 基线收紧）、`plans/025`（§〇 本机行+本节+头部 021 描述两处 §三-A/§三-C 术语）、`三机任务看板.md`（本机行已推+契约通告随收工删）、`交接.md`/`更新日志.md` 本机节。**零 src/tests/v2 树**。
**3. 实施步骤**：①门禁先行——check_handoff.py 删 `查总表B区`（`### B. `/`> **B` 挂点随分区消失）改 `查总表呈报态`（队列区 📣 行禁「已裁决/已批/已选定/✅」·当轮改状态铁律状态化）+新增 `查任务号`（§三+§4.2 唯一性/子号父号存在/锚行=实际最大）；②check_progress_sync.py 老化审计优先读行尾「最新轮」列（全文提取回退兼容）；③021 §三 平表重写：存量按原 A→G 顺序分配 001~NNN（组合号 D36+T47=父子形态〔架构轮父任务+常量遮蔽子任务〕·波次行=父任务+子任务族·B 区两条→📣·F/G 区按实态映射 P2）；④AGENTS/plans/025 条文同步；⑤棘轮按重构后实态收紧。
**4. 验证链**：`python scripts/check_handoff.py`+`python scripts/check_progress_sync.py`+`python scripts/check_own_matrix.py`+`python scripts/check_registry_audit.py` 全绿；负测反证=临时构造四类违规样本 021（副本不入库）逐一验证拦截后还原；棘轮 `check_handoff 查棘轮` 路径复核 021/025 行数。
**5. 边缘情况与诚实边界**：预判——① 021 现存单行超长（部分行>400 字符）平表迁移时须拆明细外链 025/git；②「波次」「未初始化读」无号条目分配新号后，他文档旧引用（含 AGENTS §4 的 C24/D40/D43/T63）靠〔原xx〕别名+迁移提交信息对照衔接，全仓旧引用不做机械替换（历史文档封存原则）；③ check_progress_sync 轮次正则 `\d{1,3}-[a-z]` 与新子号 `NNN-NNN` 无碰撞（已验证）；④ §三 平表行级追加与他机在飞分支的 021 改动=集成 rebase 冲突面（021 为共享契约·冲突取「平表语义无损」方向手解）。回填时如实记录实测边界。
**6. 交叠分析**：以 fetch 后远端看板为准——深度机 546/547/549/550/551/565 在飞（写集=src/v2 树/tests+文档本机节）、单位机 557/558/559 挂等回签：**零文件交叠**；check_handoff.py 最近写集（517 链）已集成，无同文件在飞交叠；plans/021 与所有在飞分支存在「行级追加 vs 平表重写」的语义耦合——本分支集成序若后到，rebase 时将他机追加行转为平表行（保留其状态/下一棒语义），已在看板契约通告广播。

### 1.0 收工回填（**580-a ✅ plans/021 编号体系重构落地**·2026-09-21·分支 任务/家机-580-021编号重构·wt580 隔离）

**1. 达成**：①§三 115→62 行单一平表（45 任务+2 子任务·001~045 全局唯一号+〔原Txx〕别名+锚行 045）；②门禁 check_handoff=查总表呈报态（📣 禁已裁决）+查总表任务号（唯一性/孤儿子号/锚行三校验）+「下一棒」列感知+状态正则六态；check_progress_sync 老化读「最新轮」列；③AGENTS §4/§6.8/§8.5 条文同步（P0 优先/📣 态铁律/立账取号刷锚）；④棘轮 021 450→400（实际 394）。
**2. 验证链**：四门禁 rc=0（handoff/progress_sync/own_matrix/registry_audit）+负测反证五类全拦截（重号/孤儿子号/缺下一棒/📣含已裁决/锚行失真·target/negtest_580.py 临时脚本 finally 还原）+正例绿。
**3. 边界（回填）**：①旧行超长描述精简外链（git 439 行版可考）；②025 他机节历史行旧分区引用未动（他机节只读）；③迁移中修正 044 行一处误判（T63 未确认销项·改中性引用）；④574-a 实施计划系滚转漏网残留随轮补搬归档（025 721→710 棘轮清偿）。
**4. 交叠（回填）**：零 src/tests 写集=快速门禁无 try-build；看板契约通告已发（@深度机/单位机）。

### 1.0 收工回填（**574-a ✅ T99/T100 裁决执行 A 组=宿主语义修复 4 项+转批 4 例**·2026-09-21·分支 任务/家机-574-t99ahost·wt573 复用）



**1. 达成**：① **199**：isLocalAddressValue 补引用参数豁免（对齐 v2 语义检查.cn:916 豁免集·引用参数地址不随本帧消亡）——199 转正测双测绿；② **231**：visitIfStmt 补 && 短路检查状态传导（!甲.正常&&甲.错误==5 形态·右侧/then 传导 错误 态）+**构造器目标上下文栈**（ctorTargetStack_·声明/赋值位压目标类型·构造器推断优先取之——Rust Err(7): Result<String,i32> 同款）——231 转正测双测绿（连带**用例笔误修正**：丙.正常→丙.有值〔可选无 .正常 成员·报备形态〕）；③ **211 归因定案**：宿主拒绝真因=声明位构造器推断缺目标上下文（非错误行 29 警告）——构造器上下文顺带根治·211 转正测双测绿；④ **459**：recheckGenericFuncBody 诊断保留面扩「无法将/返回类型」+排除 'T' 语境噪音（40/99/440 泛型函数指针未绑定语境继续回滚）——459 转负测双测绿。
**2. 验证链**：全量 E2E **525=525/0/0（E2E 计数 526→525·310 目录随 576 删）**+单测 1394+零警告 Ninja+反证双态（撤豁免 199 rc=1 复现·撤保留面 459 rc=0 复现·恢复双绿）+五处单例冒烟。
**3. 挂账与呈报**：170（导入位私有拒绝）→575-a 专办〔牵动 v1 组件/stdlib 公开标注核查·70~77 存量依赖面〕；264/445→D40（正测实况=宿主支持完整·v2 缺 84-e 设施/运算符重载声明）；143→stdlib 契约对齐设计；144→dual 通道宿主侧供给机制（runner 增强）。
**4. 坑（回填）**：①共享 hpp（semantic.hpp）成员变更后 Ninja 增量+scachache 出 ODR 半重编形态=segfault/挂死假象——**改共享头必须全量重建**（rm build-ninja 重 configure）；②批探针 ls 前缀匹配撞同名编号例（459_v2 vs 459）——探针须精确目录名。
**5. 交叠（回填）**：src semantic 域零在飞交叠；写集 src/**+tests 4 例配置=try-build 请求挂等三平台。

> 〔574-a 实施计划小节已滚转至 项目记忆/归档/任务回填-2026-09.md（574-a 收官二次清偿 728→~719·git 可考）〕
> 〔570-a/574-a 实施计划小节已滚转至 项目记忆/归档/任务回填-2026-09.md（574-a 收官棘轮清偿 728→~712·内容 git 可考）〕

### 1.0 实施计划（**567-a 开发轮·🏃 D1 滚动=生成语句赋值.cn 生成容器对象整体赋值 176 行拆分**·基线 develop 527af17f·看板先行 527af17f（wt567 直推·主树 566-a 在飞让位）·2026-09-21·分支 `任务/家机-567-d1assign`·wt567 隔离）

**1. 目标与验收判据**：check_fn_length 次列 生成容器对象整体赋值 176 行（IR/生成语句赋值.cn:149-324）出列——纯机械重构零行为变更。验收：①双 ≤100 行；②等价铁证=新旧 v2p 交叉编译冻结全树输入 asm md5 逐字节一致（564-a 同口径）；③176 档位消。
**2. 写集（精确清单）**：`CN语言编译器v2/IR/生成语句赋值.cn`（单文件）；文档六件套。
**3. 实施步骤**：①读函数体定切块（配对计数+内容断言）；②整块迁移子函数（参数=主函数局部核对·554 教训）；③构建+出列+对拍+反证（产物级注入·564 等效口径）。
**4. 验证链**：Ninja 零警告+check_fn_length 出列+双文档门禁+md5 全树对拍+反证双态；集成时全量门禁兜底（v2 树单文件=快速门禁先例）。
**5. 边缘情况（预判）**：①块自包含性核对后定参表；②递归形态（容器整体赋值可能递归调用自身——提取时保留递归入口）；③刀口 off-by-one（断言防线）。
**6. 与他机交叠分析**：生成语句赋值.cn 近改=472/466（已集成老轮）·挂等链/在飞（565 T33=语义域/566 纯文档/563 语义检查.cn）零交叠。

### 1.0 实施计划（**573-a 开发轮·🏃 T98 根治=v2 产物段错误族（110_H6/203_转移浅交接·0xC0000005）**·基线 develop d1852645·看板先行 d1852645·2026-09-21·分支 任务/家机-573-t98segv·wt573 隔离）

1. **目标与验收判据**：根治 T98——110_H6_引用参数成员访问与拷贝构造 与 203_转移浅交接 两例 v2 产物运行段错误（0xC0000005·双测转批暴露）；验收=两例双编译对照转绿+全量门禁零新增红+反证闭环（撤修复→段错误复现→恢复→绿）。
2. **写集**：`CN语言编译器v2/` 树若干 .cn（生成层根因定位后定·预计 IR/生成语句赋值或调用族单文件）+plans/025/021+看板/交接/更新日志本机节；tests/e2e 零改动（两例已在 569 转批挂账·修复后自然转绿）。
3. **实施步骤**：①最小复现（110/203 单跑取段错误现场+asm/IR 走查）→②根因定位（v2 生成层·对照宿主同源码行为）→③修复+单点验证→④反证双态→⑤全量门禁。
4. **验证链**：两例单跑绿+全量 E2E+单测+零警告+asm 宽度；try-build 挂等（写集触 v2 树=三平台产物相关）。
5. **边缘**：段错误根因可能在引用参数/转移交接的槽生命周期（569-a 时 110=引用参数成员访问+拷贝构造·203=转移浅交接——共同面=对象槽交接）；若根因超出生成层（语义/运行时）立案拆分如实呈报。
6. **交叠**：v2 树写集——深度机 546/549/550 链（挂等·v2 树多文件）潜在同区——集成序协调（看板通告）；家机 567 ⏸（暂停无在飞风险）。

> 〔569-a 收工回填+实施计划小节已滚转至 项目记忆/归档/任务回填-2026-09.md（573-a 棘轮清偿·内容 git 可考）〕

### 1.0 收工回填（**564-a 开发轮·✅ D1 滚动=代码生成.cn 生成函数 153 行拆分完成**·2026-09-21·分支 任务/家机-564-d1genfunc·wt564 隔离）

**1. 达成**：153 行→主骨架+生成函数参数装载 子函数（retbuf 槽+双计数参数装载循环整块迁移·参数=函数信息/最大寄存器/用影子参/返回结构体/输出 5 项·注释逐字保留）——check_fn_length 153 档位消（主 ~66 行+子 ~91 行双出列）。
**2. 等价铁证**：新旧 v2p 交叉编译**同一改前冻结输入**（HEAD 全树 worktree 检出·112103 节点·ASCII 入口 cv2/main.cn）win-x64 asm **md5 逐字节一致 b8e1bdfe6a58386d**（10.8MB 全树产物）；**反证双态**=产物级注入一行→ec2c9a8b≠基线（管道捕捉）→还原→b8e1bdfe 复绿。
**3. 验证链**：Ninja 零警告构建+452 双编译 PASS（v2p 双重建）+check_fn_length 出列+双文档门禁；集成时全量门禁+develop 现态补跑兜底（v2 树单文件写集·md5 等价=三平台产物零影响·沿 530/554 快速门禁先例）。
**4. 教训（回填）**：①v2p 手编冻结树三坑=产物路径 target/v2asm.s 相对 cwd（冻结树须 mkdir target/）+入口含中文段（argv GBK 陷阱→ASCII 镜像目录 cv2/入口 main.cn）+worktree 残留注册（prune 兜底）；②runner 452 指纹通道可作 v2p 双版本构建器（checkout 切换工作树→重建→备份）。
**5. 交叠（回填）**：预判成立——代码生成.cn 零在飞交叠；563 链（语义检查.cn）集成序无依赖。

> 〔529-a/525-a/522-a 系段已随 567-a 棘轮滚转归档（2026-09-21·内容 git 与归档切片可考·任务回填-2026-09.md）〕
### 1.0 实施计划（**481-a 开发轮·🏃 plans/021 清账轮：§3 已销项条目清账+结构修复+门禁防复发**·基线 25a8b9b7·看板先行 25a8b9b7·2026-09-20·分支 `任务/家机-481-021清账`）

**1. 目标与验收判据**：G 区 T10「字符串子串越界参数静默截断」（`字符串子串(s,2,99)` 对 3 字节串返回
"c" 无诊断——安全方向但无文档口径）处置=「随诊断轮顺带定文档口径」。本轮=①行为全集实测（起越界/
长度溢出/负值/空串/边界恰等）②宿主实现确认（截断语义唯一归属点）③规范文档口径落盘（cn-language-spec
skills specs 02-类型系统字符串节·行为文档化**非语义变更**——T10 处置已批准）④021 G 区 T10 销项。
**验收**：探针矩阵实测表+规范文档口径条文+021 销项台账行+双文档门禁 PASS。

**2. 写集（精确文件清单）**：`.zcode/skills/cn-language-spec/specs/02-类型系统.md`（字符串内置函数
口径节）｜`plans/021`（G 区 T10 销项迁 §4.2+文首戳）｜`plans/025` 本节+§〇本机行｜看板本机行（窄通道）｜
`交接.md`/`更新日志.md` 本机节。**零 src/ 零 tests/**——纯文档=快速门禁。

**3. 实施步骤**：①探针实测矩阵（起=0/中/越界×长度=0/恰好/溢出/负值×空串）②grep 宿主 子串 实现
（runtime/内置表）确认截断归属与实现一致性③spec 02 字符串节落口径条文（截断语义+边界定义+例）④
021 销项+文档六件套。

**4. 验证链**：探针实锤+`python scripts/check_handoff.py`+`python scripts/check_progress_sync.py`
+写集核对。快速门禁。

**5. 边缘情况与诚实边界（预判）**：①若实测发现「截断」之外的形态（负值回绕/崩溃）→机制级立案不混
本轮②spec 双拷贝（.zcode 与 .agents junction）单一事实源=.zcode③行为变更（如改 Rust 式 Option/panic）
不在本轮——如需另行语义级呈报。

**6. 与他机交叠分析**：纯文档轮零代码交叠；深度机 480/484/493（v2 树）与家机 483/494（挂等回签）
零文件交叠。spec specs 属三机共享契约——**行为文档化**（无契约面变化）照 §8.6 广播口径在收工时说明。

> 〔481-a 实施计划已滚转归档（471 集成会话·2026-09-20·清账轮已集成 git 可考）〕
### 1.0 实施计划（**476-a 开发轮·🏃 T71 专项根治：类无参构造实例化组合非确定段错误**·基线 e5011e75·看板先行 e2d5f9cf·2026-09-20·分支 任务/家机-476-T71组合段错误根治·worktree=wt476）

**1. 目标与验收判据**：T71（471-a 立案·plans/021 §3-G）——全形态组合源码（类显式无参构造实例化+结构体构造字面量+局部数组+泛型调用+循环/选择/三元）编译零诊断、运行同一产物**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突 UB）。消融三证据：去类+结构体绿（H）/去结构体单次绿后连跑崩（I）/结构体+数组+泛型最小组合 8 连跑绿（k/l）——触发面=类无参构造实例化与组合。**根治路径（Rust 对照=UB 先 ASan 定位再修·不求幸存）**：①**ASan 构建**（-DCN_ENABLE_ASAN=ON·229-a 已接入）编译复现语料→产物 ASan 运行直出 UB 报告（堆栈=越界/未初始化精确位置）——预期一步锁定根因；②最小复现收敛（逐形态增删至最小触发集）；③按根因修复（嫌疑面=局部类对象构造的槽分配/构造注入顺序/数组槽相邻踩踏）；④复现语料 **20 连跑全绿**+全形态版（484 完整版）转稳定+全量门禁零新增红。**验收**=ASan 报告归零+复现语料 20 连跑绿+全形态版 20 连跑绿+单测/E2E 全量绿+asm 反证（根因点前后指纹）。

**2. 写集（精确文件清单）**：以 ASan 定位结果为准（嫌疑面=src/cn_compiler 语义/IR 层类构造注入+代码生成槽分配）+tests/e2e（484 扩形态或新用例）+cnsmith_hits（T71 转正注记）+文档六件套。开工侦察后回填精确清单。

**3. 实施步骤**：①ASan 树构建（cmake -DCN_ENABLE_ASAN=ON·独立 build 目录）+复现语料编译运行采报告；②报告→根因定位（asm 三件套对照：崩点前后产物 diff）；③最小复现收敛（供回归用例）；④修复+反证（回退复现/修复消失）；⑤484 完整版转正评估+全量门禁；⑥T71 行销项+run_err 转正注记。**Rust 对照**：rustc 对 internal UB 的态度=ASan/UBSan 先行定位、修根因、加回归测试——同构。

**4. 验证链**：ASan 报告归零+复现语料 20 连跑绿+全形态 20 连跑绿+零警告构建+单测全量+E2E 全量（gate_lock 串行）+asm 指纹。

**5. 边缘情况与诚实边界**：①非确定崩溃的验证口径=连跑计数（20 次起）——单次绿不算绿（T71 铁证）；②ASan 树产物与生产锚定链无关（独立 build 目录·不影响默认构建）；③若 ASan 不报（栈读未初始化 ASan 弱项）→备选=MSVC /RTCs 或产物 diff 槽分析；④arm64/linux 侧复验归跨机轮。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470/474/475（v2 树/D46）零交叠（T71=宿主 src）；家机 468（九链重测集成·零代码）零交叠；471（挂等回签·写集=tests 冻结）——本轮写集=src+tests 新用例，与 471 的 coverage_map/用例编号可能相邻（484 已占·新用例取 485+）。

### 1.0 阶段回填（**476-a 根因定位阶段完成·修复实施续下轮**·2026-09-20）

**已完成**：①ASan 树构建（target/build-asan·需补 clang_rt.asan_dynamic DLL 至 exe 旁；产物 runtime 不吃 CN_ENABLE_ASAN=ASan 只插桩编译器自身——产物诊断须另走 cl /fsanitize 产物旗标通道·登记）；②**最小复现定案**（t71_min.cn=类无参构造+方法调用两要素·确定性崩·与数组/泛型/结构体无关）；③**定性勘误**：原「非确定」=单次绿误判（实为确定性 call 0）；④**asm/IR 双铁证**：IR `间接调用 %v5 0`（callee=常量 0·无 FuncAddr）→asm `lea r12,[rbp-608]`（this=变量槽地址·应读出对象指针）+`mov r11,0; call r11`；⑤语义层类型推断排除（checkCtorCall lastType_=className 正确回填）；⑥根因锁定=ir_oop_call.cpp handleClassCallExpr 某分支对「类实例方法调用」误发 CallIndirect（callee=0 兜底+this 装载形态错）——**嫌疑=T57 接管分支重设计（472 集成）改动面**（T57=objdel+写穿根治·触及方法调用接管分支）；⑦T71 行完整落盘 021 G 区（含勘误）。
**下一步（续轮精确指针）**：①读 handleClassCallExpr 情形B/C（类方法直接调用段）确认哪分支漏判（「计」类型=计数器 应走直接调用 emitClassMethodCall·为何进间接分支）——注意 472 集成的 T57 改动 diff（git show 359cd573 附近）；②修复=误判分支修正+callee=0 硬错误防线（T11 面②同款·IR verify 扩 CallIndirect callee 非零常量检查）；③t71_min 入 E2E（485）+T71 复现语料 20 连跑绿验收；④候选回归面=T57 三例+484 完整版+110 接口/类方法族。
**诚实边界**：本轮未产修复（根因定位阶段交付）——T71 已带完整证据链与精确续作指针，不存在无计划挂账。

### 1.0 收工回填（**476-a 修复阶段完成·✅ T71 根治：类类型回填+间接调用 0 防线双件**·2026-09-20·分支 任务/家机-476-T71组合段错误根治·wt476）

**根因修正与修复实施**：①**根因①修复**=semantic_decl.cpp 变量声明推断回填白名单补 isClassType（46-c 结构体先例同构·原白名单「结构体+字符串」漏类——`变量 计 = 计数器();` 推断 varType=计数器 正确但不回填 node->typeName→IR 按空 typeName 落单 i32 槽+插 ptr→i32 截断 Cast→成员调用走间接 fallback callee=0）；修复后 IR=`分配 %v0 (ptr)`+直接调用 `[计数器$取值]`（无截断/无间接）。②**防线（原计划②）**=ir_verify 新增 verifyCallIndirectTargets（CallIndirect callee=isConstant"0"→「符号解析失败被 0 兜底发射」编译期硬错误）+driver 无条件常开接线（与位宽/操作码检查同族）；语义层类型推断本身正确（471 轮嫌疑=T57 接管分支**排除**——实证与 T57 无关）。③E2E 485（最小回归·输出 0）+486（全形态组合·输出 200/测试串·返回 0 修 runner 判定）双上架+coverage_map 登记。
**关键实验教训（如实记录）**：①定位中误用 ASan 树旧编译器（target/Debug/cn.exe 无修复①）判 g3=139——**双构建树（Ninja target/cn.exe vs VS target/Debug/cn.exe）并存时实验必须核对 exe 时间戳**（旧 449-a「门禁跑在陈旧宿主」同族教训的实验版）；②python heredoc 写 fprintf 的 
 转义两次翻车（真实换行断字符串）——含转义的代码写入用 chr(92) 构造；③「非确定崩溃」原判=误判（单次绿=跑错 exe），本轮连跑口径核实为确定性崩。
**验证链（实测）**：修复后 t71_min rc=0（修前 139）+t71_full **20 连跑 rc=1 稳定**（修前 139×20·输出 200/测试串值正确）+构建零警告（防线加入后全量重验见门禁）。**边缘回填**：①ASan 产物诊断通道=CN_ENABLE_ASAN 只插桩编译器自身，产物 runtime 须 cl /fsanitize 通道（登记待办·本轮未走通）；②runner 判定含进程退出码（返回 非0=FAIL·用例设计须 返回 0）。
> 〔467-a 实施计划已滚转归档（489 集成会话·2026-09-20）〕
### 1.0 收工回填（**467-a 开发轮·✅ T46 根治（甲+丙）：宿主全局 128 位标量静态初始化发射缺失双缺口全消**·基线 20cee0f4·看板先行 4037a6cc·2026-09-20·分支 任务/家机-467-T46全局128静态发射·wt467 隔离）

**实测结论**：①**缺口①（.data 恒零占位）**：三后端 128 分支由硬编码 `dq 0, 0` 改为 initText 非空时经 types::parseInt128InitText 解析发射初值双 quad（低 64 位在前）——win 实测 `?gstatic_a dq 2Ah, 0h`/`?gstatic_b dq 0FFFFFFFFFFFFFFFBh, 0FFFFFFFFFFFFFFFFh`（−5 完整 128 位补码）/i128max/−2^127/0x 前缀/u128max/2^64+1 全对；linux_x64/arm64 .s 指纹 `.quad 0x2A`+`.quad 0x0` 全对（跨后端扫描口径·win 本机 as 不识 ELF=平台固有非缺陷）。②**缺口②（半槽写）**：IR 层注入 StorePtr 128 位传 IR 名（原恒 "ptr"）+win/linux/arm64 StorePtr i128 分支补常量子分支（parseInt 解析·splitI128 的 64 位 stoull 会截断负数符号故不用）；另 IR 层 ±字面量折叠进直存通道（Rust const-eval 同构·零运行时开销·仅 128 位走新折叠=既有 ≤64 位零回归）——−5 实测读出 −5（修前 18446744073709551611=2^64−5）。
**写集回填（实际）**：`src/cn_compiler/semantic/{type_system.hpp,type_system.cpp}`（isInt128Type/isInt128Signed/parseInt128InitText·支持十进制±/0x/0b/0o/lo:hi 常量池形态·超界 false·无符号负数拒绝）｜`src/cn_compiler/ir/ir_decl.cpp`（收集折叠+注入 StorePtr 类型）｜`src/cn_compiler/codegen/x64/{x64_codegen_data.cpp,x64_instructions_mem.cpp}`｜`src/cn_compiler/codegen/linux_x64/{linux_x64_codegen.cpp,linux_x64_instructions.cpp}`｜`src/cn_compiler/codegen/arm64/{arm64_codegen.cpp,arm64_instructions.cpp}`｜`tests/e2e/483_全局128静态初始化/`+`tests/e2e/coverage_map.md`｜`tests/unit/semantic/test_int128_static_init.cpp`+CMakeLists｜m46_01.meta 转正注记｜plans/021+025+教训.md+看板+交接+更新日志。**与计划差**：+StorePtr 常量子分支三处+coverage_map 登记（门禁拦截后补）——计划预判外两件（门禁/单测当场抓取·机制有效）。
**验证链（实测）**：Ninja+sccache 零警告（MSVC /W4 /WX·242/242）+单测 **1369/1369**（新 6 例：类型判定/十进制/前缀/超 int64 大字面量/超范围拒绝/常量池形态）+E2E 全量 **477=477/0/0**（483 单跑 PASS·锚定链含内零回归）+asm 宽度三后端 PASS+m46_01 实弹转正（「静态128小值 = 42」rc=0）。丙=扫描门禁判定：E2E 483 运行级断言即机械化门禁（恒零占位/半槽写每轮全量必转红）·check_asm_width 扩描重复其职责不另建。
**边缘回填**：①超 2^128 字面量→parseInt false→零占位（现状等价不劣化·编译期超界拒绝归 T9 方案 D 辖区·已注记）；②词法层 int64 溢出的大字面量（i128max/2^128−1 实测）词法不拒·发射层文本解析兜住=直存正确（IntegerLiteral::raw=唯一完整信息源·这一设计在本轮被实证为必要）；③**v2 侧同面缺口登记**：v2 代码生成.cn 静态槽「dq 初始值」单 quad+槽 8 字节（128 位槽尺寸不足·源码级定性·深度机 466 在飞 v2 生成层避让·归 v2 欠账族）——E2E 483 暂宿主侧锚定·v2 补齐后双测化；④arm64/linux_x64 运行级复验归跨机轮（平台固有分工）。
**交叠结论**：深度机 466/家机 460/挂起四分支零冲突（写集不同文件）——预期成立。
**事故记录**：开工时主树误执行 reset --hard 丢弃他会话未提交增量（cnsmith_hits 日志）——看板广播（4037a6cc）+教训入册（权重 9·主树禁 reset --hard/窄通道=精确 add 单文件口诀）。

### 1.0 实施计划（**471-a 开发轮·🏃 D25 后续语料轮：ir_verify 错误分支单测全 face+ast_printer 全节点 E2E 语料**·基线 339ba21d→f78e8cf7 看板先行·2026-09-20·分支 任务/家机-471-D25验证器语料·worktree=wt471）

**1. 目标与验收判据**：D25 剩余面（plans/021 §3-D·283-a 首步后续）——①**ir_verify 错误分支路径语料**：ir_verify.cpp（266 行）错误分支全 face 单测直调（verifyFunction 8 面=缺标签/重复标签/块未终止/跳转目标不存在/条件缺 termCondition/真目标不存在/假目标不存在/引用未定义寄存器+verifyConstWidths 2 面=ConstInt extra 超域/内联操作数超域含去重+verifyKnownOpcodes 1 面=未知操作码——11 面逐面构造坏 IR 断言诊断原文）；②**ast_printer 覆盖语料**：`cn ast` 命令全节点触达 E2E 语料（字面量五类/表达式全族/语句全族/类/泛型/联合体/函数指针形态）+打印输出锚定。**验收**=新单测全绿（逐面断言）+新 E2E PASS+全量门禁零新增红；诚实边界=覆盖率数字（gcov=linux 口径）提升由深度机下轮复测确认（win 无 gcov·本轮交付=语料+单测+实跑 PASS 证据）。

**2. 写集（精确文件清单）**：`tests/unit/ir/test_ir_verify_errors.cpp`（新）+`tests/unit/CMakeLists.txt`（注册）｜`tests/e2e/484_ast打印全节点语料/`（主.cn+主.expected）｜`tests/e2e/coverage_map.md`（登记）｜文档六件套。

**3. 实施步骤**：①单测：读 ir_verify.cpp 枚举错误分支（8+2+1=11 面）→IRModule/IRFunction 手工构造各违例形态→断言返回错误消息含函数/块/原因文本→健康 IR 空错误反证；②ast_printer：`cn ast` 对全语法形态源码实跑→输出固化 expected（逐节点形态在列）→472？编号取 484（483 已占）；③门禁+coverage_map 登记。**Rust 对照**：rustc verify 系（mir::verify）错误路径由 ui 测试+单测双面触达——CN 同构（E2E 触达健康面+单测直调错误面）。

**4. 验证链**：零警告构建+单测全量+E2E 全量（gate_lock 串行）+新单测逐面 PASS+新 E2E 单跑 PASS。

**5. 边缘情况与诚实边界**：①错误分支诊断文本为断言面=实现重构时须同轮迁移（先例 437 断言迁移）；②ast 输出格式=调试面非语言契约（.expected 锚定输出=回归锚·格式演进随实现迁移——注记登记）；③覆盖率数字归深度机 gcov 口径（win 无 gcov）；④verifyConstWidths 去重路径（同违例两次只报一次）显式用例。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470（v2 树）零交叠（本轮写集=tests）；家机 468（九链重测集成轮·零代码）零交叠；467（本会话挂等回签·src 写集冻结在分支）零交叠。**

### 1.0 收工回填（**471-a 开发轮·✅ D25 后续语料轮：ir_verify 11 face+ast_printer 5 例单测+484 运行锚+★T71 立案**·基线 339ba21d·看板先行 f78e8cf7·2026-09-20·分支 任务/家机-471-D25验证器语料·wt471 隔离）

**实测结论**：①ir_verify 错误分支单测 **13/13**（verifyFunction 8 面=缺标签/重复标签/未终止/跳转目标不存在/条件缺 termCondition/真假目标不存在/未定义寄存器+verifyConstWidths 2 面=extra 超域/内联操作数超域含块内去重+verifyKnownOpcodes 1 面=未知操作码+健康反证 2 例）——合法 IR 生成+定点变异直调（Rust mir verify 同构）；②ast_printer 节点触达单测 **5/5**（类/结构体/泛型/联合体/结果/表达式族/控制流族）——**缺口实证=cn ast 不遍历顶层静态声明**（printer 覆盖 14.6% 的构成性缺口·登记待 printer 补齐）；③E2E 484（结构体构造字面量+数组初始化列表+泛型实例调用+下标/成员访问·输出 43）**8 连跑稳定**+单跑 PASS。④★**T71 立案**（P1 嫌疑）：全形态组合源码（类显式无参构造实例化+结构体+数组+泛型+循环/选择/三元）编译零诊断、运行**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突）——消融三证据、复现语料 run_err 入库、484 以稳定子集上架。
**写集回填**：tests/unit/ir/test_ir_verify_errors.cpp（新）+tests/unit/parser/test_ast_printer_nodes.cpp（新）+tests/unit/CMakeLists.txt+tests/e2e/484_ast_dump/（主.cn+主.expected）+coverage_map.md+cnsmith_hits/run_err/t71（复现语料+meta）+021/025/看板/交接/更新日志。**与计划差**：ast 语料由 E2E 改单测承载（runner 不支持 ast 命令输出断言·计划预判外机制认知）；484 目录名 ASCII（runner 中文名产物实跑验证过·保守取稳）；+T71 立案（计划外捕获）。
**验证链**：零警告构建+单测 **1381/1381**（本分支基线 1363+ir_verify 13+ast 5）+E2E **477=477/0/0**+asm 宽度 PASS——4/4 全绿（gate_lock 串行）。
**边缘回填**：①runner 断言面=运行 stdout（ast 命令输出须单测承载·机制认知入册）；②ast 输出为调试面（expected 锚定=回归锚·格式演进随实现迁移）；③gcov 覆盖率数字归深度机 linux 口径（本轮交付语料+单测+实跑证据）；④printer 顶层静态缺口登记 D25 行。
**交叠结论**：全零（预期成立）；T71 触发面=宿主局部类对象构造（与深度机 v2 生产线零关）。

> 〔§1.0 尾部 458/459/453/456 系 09-20 老轮次小节已滚转至 项目记忆/归档/任务回填-2026-09.md（575-a 025 棘轮清偿 727→~700·git 可考）〕


## 二、深度机 linux-x86_64 节（本机维护）

> 〔2.103 T80-host 侦察/定位轮（**宿主侧「类内结构体字段」缺陷**＝最小复现+gdb+反汇编定性·零行为变更） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.100 T70 B1 三层剩余层**侦察/定位轮**（②元素钩子注入设施缺 ③127 映射链查找段错误 ④228/441/442 v2p 失败快 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.87 F1-26 侦察与设计轮（**真 mem2reg/真值传播**＝256-a 方案 A 续篇·实施前定稿） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.85 D46 第三段（**赋值位构造名裸名拒绝**＝`r = 正常;` 检查赋值语句单点） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
> 〔2.83 D46 已滚转归档（深度机 546-a 棘轮·2026-09-21·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.128 strict 四孤儿修复＋569 runner 平台硬编码修复（576/577 两案·分支 `任务/深度机-576-covmapfix`·基点=develop b294fa48·看板占号 2b76781e·分支头 8198fb02·2026-09-21·挂等三平台回签）

**1. 目标与验收判据**：①修复 b94c37bd「572 缺口备案」strict 红（4 孤儿+310→495 悬空指针）；②569 负测双测通道平台硬编码机制级修复（轻量 v2 负编译断言无条件 win 工具链+win-x64 目标——573 集成 2d283fdf 树级带入 569 runner 后 linux 首跑 71 例负测全炸「未找到 ml64/link」）。**验收实测**=分支头 8198fb02：strict rc 1→0（179/179=100% 指针有效+零孤儿）+matrix 83/83+零警告+单测 1394/1394+E2E **525=523/0/0 零失败**（修复前 452/71/2 对照·131 单跑转绿）。
**2. 写集（回填）**：`tests/e2e/coverage_map.md`（4 孤儿登记+指针同步）＋删 `tests/e2e/310_货舱特性声明制/`（残留目录·与 495 语义重复·557 改号成复制未删·E2E 计数 526→525·删除随 577-a 提交入库〔暂存时序·576-a 提交信息已勘误〕）＋`tests/e2e/run_e2e.py`（轻量断言分支平台分派·win 路径逐参数零改动）＋本节+看板本机行/通告段。
**3. 实施步骤（回填）**：①covmap 四例登记（500=fa59f316 原文恢复·434/435/495=T54 货舱族 557 链已验证内容入册）+4.7/5.9.5 指针同步+310 残留删除；②runner 修复=平台分派三处（就绪函数/平台参数/asm 产物名·linux=确保v2p与运行时就绪+GAS 参数+v2asm.s）。性质=登记恢复+平台分派补全，无新语义。
**4. 验证链（已跑）**：strict/matrix/零警告构建/单测/E2E 全量 --jobs 8/单跑转绿——数字见 1。反证=修复前全量 71 红对照。
**5. 边缘情况与诚实边界（回填）**：发现并修正预判外事实=310 目录两存（非单纯指针悬空而是目录残留）；569 runner 实际随 573 集成才入 develop 树——**上轮 569 回签（2aa213a3）树不含负测双测通道，回签结论被 573 集成架空，本分支 577-a 修复后需三平台重签**。诚实边界=win/arm64 复验走 try-build（分支已推·通告段请求已发）；E2E 计数 525 与他机 526 差 1=310 重复例消除（请求已注明）。
**6. 交叠分析（回填）**：写集与家机（569 收官/568 挂等/573 销账）+单位机（456 侦查）零交叠；语义耦合=strict 门禁（正向转绿）+runner 负测通道（569 作者 win 侧需复跑零回归）。

### 2.130 编号勘误轮（**任务 047 改挂 001 子任务=001-001**·纯文档零代码·用户裁决）——593-a 勘误轮（分支 `任务/深度机-593-047改号`·基点=develop bcf00290·wt593 隔离·2026-09-22）

**1. 目标与验收判据**：用户裁决——047（撤守卫容器双模语义收敛·585-a 立案）是 001 波3 衍生任务，编号应体现父子关系：改挂为 **001-001**（依 plans/021 规则「子任务=父号-三位子序」）；047 号退役防复用；收敛方向方案甲（全内联·用户 2026-09-22 裁决·与 Rust Vec 布局/宿主同构）写入 001-001 立案。**验收**=五文档 14 处引用全部改挂（grep 复核仅剩 025 历史记录 1 处+021 锚行退役注记）+双机械门禁绿。
**2. 写集**：plans/021（047 行→001-001 行移至 001 行下+001 行两处+锚行退役注记+§五 585-a 行两处）｜plans/025（§2.129 两处+本节）｜三机任务看板（通告段两处）｜交接（深度机节一处）｜更新日志（深度机节两行三处）｜看板本机行。
**3. 实施步骤**：①五文件精确替换逐处计数核销；②001-001 立案追加收敛方向裁决句；③锚行退役注记（新任务号自 050 起顺延）；④双机械门禁+提交+integrate（纯文档快速门禁）+镜像推送。
**4. 验证链**：check_handoff+check_progress_sync+grep 复核+integrate 快速门禁。
**5. 边缘情况**：025 72 行「585 立号撞 047/582」=历史事实记录保留原文（考古价值·勘误由本节+提交信息承载）。
**6. 与他机交叠**：纯文档写集·与在飞（家机 589/591/592/504 等）零语义交叠；develop 期间前进=rebase 自然消化（文档分区合并友好）。

### 2.129 D42甲 波3 终章接力（**撤守卫收口=stdlib 链接期供给→源码加载切换落地**·001 任务最后一步·家机 578 同计划深度机接力）——585-a 开发轮（分支 `任务/深度机-585-撤守卫收口`·基点=develop 7ad6343a·wt585 隔离·2026-09-21·用户令「立即全量完成 001」）

**1. 目标与验收判据**：D42甲 波3（plans/021 §三 001 行）最后一步——撤 `是标准库模块` 守卫（主.cn:64 名单），v2p 对 stdlib 10 模块改走源码级加载（候选4 通道已通·517 链 T70 三层修复已入 develop），链接期供给模型退役。**验收**=①撤守卫态 linux 全量 E2E（--jobs 8）零新增红+关键探针 126/127/390/228/441/442 全 rc=0；②自举链 78/79 双 PASS（撤守卫态=v2 编译 v2 树自身 stdlib 也源码级）；③借链面 51 例（借链=是·实测清单）转自洽逐例绿；④反证双态（守卫复活→390 链接失败复现→再撤→复绿）；⑤守卫落地形态=名单函数删除+消费点死路清理（最小死代码·非恒假 TEMP）。win/arm64 侧=协议 v3 集成后异步验收（575 先例）。
**2. 写集（预登记）**：`CN语言编译器v2/主.cn`（守卫名单/消费点）｜`CN语言编译器v2/IR/IR布局.cn` 等 `是标准库模块名` 消费点（按实测定性——生产态 stdlib 源码在库后其判定恒假路径的清理范围）｜`tests/e2e/*/v2闭环.txt|双编译对照.txt` 51 例（借链=是→否·AGENTS §4 测试升级随轮报备）｜`tests/e2e/coverage_map.md`（若有新形态）｜plans/021（001 行波3 收口+戳）｜plans/025 本节｜交接/更新日志本机节｜看板行。零 src/（宿主零改动预期——撤守卫纯 v2 树行为）。
**3. 实施步骤**：①基线冒烟（v2p 构建+关键探针·develop 现态含 517 链/574/575 全集）→②TEMP 撤守卫（名单恒假）+撤守卫态关键探针矩阵→③撤守卫态 linux 全量 E2E --jobs 8（暴露面逐例定性：A 期望漂移迁移+反证/B v2 真缺口立案/C 噪音重跑）→④守卫落地形态定案（名单函数删除+消费点清理·逐消费点实测定性）→⑤借链 51 例转自洽+逐例验证→⑥生产态全量门禁（gate_lock）+自举链 78/79+strict/matrix→⑦反证双态→⑧六件套+integrate（win/arm64 集成后异步验收广播）。
**4. 验证链**：v2p 构建+探针矩阵→撤守卫态全量 E2E --jobs 8→生产态全量门禁（零警告构建+单测 1394+E2E 全量+78/79+strict 179/matrix 83）→反证双态。
**5. 边缘情况与诚实边界（预判）**：①撤守卫态 linux 新暴露面（517 链后 develop 前进了 574/575 两轮 v2 语义/语法改动——撤守卫交互未实测）；②借链 51 例转自洽后链接面符号重复消解实测；③575 新增 4 拒绝面（422/299/81/417）在 stdlib 源码级编译下的交互（stdlib 源码自身须过新检查）；④自举链 78/79 撤守卫态=v2p 编译 v2 全树（460-a 时 fix_p 曾 rc=1·517 链已通·现态复验）；⑤strict 四孤儿已随 576/577 集成消解（develop 现态 strict 179/179 绿·无豁免需要）。
**6. 与他机交叠**：家机 578（同计划 wt578 在飞·**本分支=深度机接力·两家各按本机平台验收·集成取先者·后集者 rebase 自然去重**）——写集同源（主.cn 守卫位+tests 借链面）语义同向，非冲突；家机 579/581（T100 tail/协议v3·零交叠）；深度机 565（v2 树 4 文件·零文件交叠实测）；单位机 arm64（集成后 pull 自然验证）。本分支写集触 v2 树+tests→全量门禁。

**【585-a 收工回填（撤守卫态暴露面四根因二留二回·负测锚七例适配·残余定性）】**：
**1'. 实施实录（基线 develop 7ad6343a→TEMP 撤守卫→探针矩阵 4/6→全量暴露面 21 红→逐根修复→净正收敛）**：
- **暴露面首跑**：撤守卫态（主.cn:64 守卫恒假 TEMP）全量 E2E 528=505/21/2（守卫态基线 526/0/2）——21 红三类：运行错值/崩 14+负测锚行漂移 6+自举 1。
- **修复①（留）**`IR容器.cn 签名返回字节数`：结果空 12→16（268-a 口径漏同步——错误槽@8+宽 8；retbuf 帧预留差 8B→被调 sret 写踩相邻槽）。asm 铁证=追加 sret retbuf lea [rbp-136] 写 16B 覆盖 [rbp-128]。
- **修复②（留）**`生成类.cn 生成类对象局部`：补 IR_分配（帧预留以 IR_分配 声明性指令计数——漏发=槽不进帧→与 retbuf 区重叠→441 A3「新表」句柄被清零→拷贝构造读空。gdb=[rsi]=0 铁证）。同族扫描=全树唯一漏点（标量/引用/结果/数组/结构体局部均有）。
- **修复③（回滚）**`生成表达式字段 成员载荷是结构体` 容器排除：修 441 系（结构体容器字段压地址→load）但破坏 228（类对象.容器字段 链的「字段地址即 this」内联语义）——分层实验两难实锤后回滚。
- **修复④（回滚）**容器参数句柄语义三站点（实参结构登记×2+序言拷贝字节）：修 113 this=0 但破 127/497（517-a 容器元素内联存储依赖结构体按值通道）——回滚。
- **负测锚行适配（留·A 类期望漂移）**：197/223/224/227/229/464/283 七例 主.expected「已解析…主.cn 节点=N」去节点数尾巴（撤守卫态=全树合并节点数含 stdlib·单模块锚必 miss；路径锚保留·runner v2src 适配仍在·生产态前缀匹配兼容）——6 例转绿，283 另有运行面残余。
**2'. 本质定性（残余红收敛为架构级双模语义）**：撤守卫态「容器实例」**内联/句柄双模并存**——元素/字段存储=内联整对象（517-a·宿主对齐），局部变量/参数=句柄 8B（生产态 NewObject 模型）。T70 链（505~517）按内联侧逐例打补丁，585-a 按「容器=类实例化」让布局/类表命中后，**双模判定散点全面显形**（字段载荷 46-a、参数 ABI、方法返回物化……）——**非单点 bug，是语义模型收敛问题**：收敛方向二选一（全内联对齐宿主／全句柄）=架构级大轮（T70 链同量级），505~517+585 两轮教训=打地鼠不可持续。
**3'. 验证（回填·终测实测）**：①生产态零回归（修复①②+expected 七例·守卫恢复态）=零警告构建+单测 **1394/1394**+全量 E2E **528=526/0/2 零失败**+strict **179/179 rc=0（100%）**+matrix **83/83**（gate_lock 串行·与 develop 基线逐项一致）；②撤守卫态终测 **528=513/13/2**（首跑 21→13·净绿 8=441/442/147/127/497/197/223/224/227/229/464 增量减 228 波动）。反证=修复前后红名单对照（git b1b9eee2 历史+本段实录·441 探针 rc=139→0 铁证链）。
**4'. 残余清单（定性登记·001-001 承载·下轮接力·收敛方向已裁决=方案甲全内联〔用户 2026-09-22·与 Rust Vec 布局/宿主同构〕）**：终测 13 红=容器所有权/类支持族 11（113/218/219/221/228/234/237/238/240/283/285——双模收敛同族）+67（泛型返回·T70 面⑤' 残余）+79（自举 fix_s——撤守卫自举）。观测注记=228/67 存在构建间布局漂移不稳定红（驻留序/哈希跨构建漂移·爆发点漂移·047 单点化后自然消解）。
**5'. 诚实边界**：①撤守卫落地（守卫删除=生产态切换）本轮未做——前置=撤守卫态零红，残余族清偿后下轮执行；②借链 51 例转自洽未做（同前置）；③win/arm64 侧=协议 v3 集成后异步验收。

### 2.83 D46（**v2 结果构造名裸名编译期拒绝**＝`返回 正常;`/`返回 错误;` 语义层防线）——474-a 开发轮（分支 `任务/深度机-474-d46naked`·基点=develop aef637b6·wt474 隔离·2026-09-20）

### 2.127 T33 残余根治（**v2 窄宽值规范化四站点＋T36 v2 面字面量值域检查**＝宿主/v2 双编译分叉实锤矩阵）——565 开发轮（分支 `任务/深度机-565-t33窄宽回绕`·基点=develop 715f4e8a·wt565 隔离·2026-09-21）

**0. 立案证据（探针矩阵·宿主=target/cn vs v2=wt562 v2p·2026-09-21 全部实测）**：A 族（窄宽值入槽不规范化→静默错值）：op6 `整8 丙=整8(100)+整8(100)` 宿主 **-56**/v2 **200**；op7 `正16 60000+10000` 宿主 **4464**/v2 **70000**；b5 复合赋值 `甲+=整16(30000)` 宿主 **-5536**/v2 **60000**；b1 传参 `取(甲+乙)`（形参 整16）宿主 **-5536**/v2 **60000**；b2 返回位（->整16）宿主 **-5536**/v2 **60000**；h1 `整32 甲=4000000000` 宿主 **-294967296**/v2 **4000000000**；h5 `整32=6000000000` 宿主 **1705032704**/v2 **6000000000**。B 族（漏拒·宿主拒/v2 放行）：g1 `整16 甲=40000`、g2 赋值位 `甲=40000`、g4 `甲=0+40000`、g5 `正8=300`、g9 `整8=-200`、g10 `正8=-1`、h6 `正32=5000000000` 宿主全拒·v2 全放行。**已正确面（反证不误伤清单）**：显式强转（p3 六形态 ✓）、数组元素（b4 ✓）、结构体字段（c3 ✓·须显式构造器）、a1 变量窄化拒（双侧一致 ✓）、w32/正32 位与回绕 ✓、h2 整64 大字面量 ✓、f1/f2 整32 传参/返回 ✓、h7 整16=-32768 ✓。

**1. 目标与验收判据**：v2 对窄宽（整8/正8/整16/正16/整32）值汇（变量槽/形参槽/返回位）的写入必须规范化到目标宽度（回绕+符号扩展·Rust i16 wrapping 赋值同款语义），字面量对窄宽目标的隐式转换须值域检查（宿主 T36·319-a 同口径）。**验收**=①探针矩阵 A 族七例双侧逐行一致（v2 转绿）；②B 族七例 v2p 编译期拒绝（诊断含「无法将 '整32' 隐式转换为」同宿主措辞形态）；③新 E2E **502_窄宽回绕矩阵**（双编译对照·宿主=v2 逐行全等）+**503_窄宽字面量域外拒绝**（v2 负测·v2p 拒通道）上架单跑 PASS；④全量门禁零新增红+自举链 78/79 PASS+strict/matrix；⑤反证双态（撤 A 规范化→502 复现分叉→恢复复绿）。

**2. 写集（精确文件清单·预登记）**：`CN语言编译器v2/IR/生成声明.cn`（+窄宽值规范化 helper·:232 调用点换接）｜`CN语言编译器v2/IR/生成语句赋值.cn`（:121 调用点换接）｜`CN语言编译器v2/IR/生成函数.cn`（:163 区 返回窄宽码登记+形参槽入口规范化发射）｜`CN语言编译器v2/IR/IR容器.cn`（函数IR +返回窄宽码字段）｜`CN语言编译器v2/IR/生成语句.cn`（生成返回语句 标量返回规范化）｜`CN语言编译器v2/语义/语义检查表达式.cn`（隐式转换合法 字面量分支值域检查+负号字面量形态）｜`tests/e2e/502_窄宽回绕矩阵/`（新·双编译）｜`tests/e2e/503_窄宽字面量域外拒绝/`（新·v2 负测）｜`tests/e2e/coverage_map.md`（两例登记）｜文档六件套+看板。**零 src/**（宿主已正确=参照实现）。

**3. 实施步骤**：①A=生成声明.cn 增 窄宽值规范化（整8/整16/正8/正16→IR_类型转换 窄码〔窄读+符号/零扩展·与显式强转同机制三后端既有〕；整32→T_整32 转换〔movsxd 符号回绕〕；正32→既有 正32存储截断 位与零改；已规范寄存器=零开销直通），:232/:121 两调用点换接；②④=生成函数 形参登记循环后对窄宽标量形参发入口规范化三指令（槽加载→转换→槽回存·方法体同 routine 覆盖）；③⑤=生成返回语句 标量返回路径按 函数信息.返回窄宽码（新增字段·:163 区从返回类型文本登记）规范化；④B=隐式转换合法 整数字面量分支补 值域表（整8 [-128,127]/正8 [0,255]/整16 [-32768,32767]/正16 [0,65535]/正32 [0,4294967295]·整32/整64/正64 不检=h1/h5 宿主实测定案）+节点_负号+字面量形态（g9/g10）；⑤502/503 上架+coverage_map；⑥全量门禁+反证。

**4. 验证链（命令级）**：探针矩阵复验（/tmp/t33probe m/*.cn 全表 双侧逐行比对）→502/503 单跑→零警告构建→单测→全量 E2E `--jobs 8`（互踩族串行复验口径）→strict/matrix→自举链 78/79→`check_fn_length`（新函数 ≤100 行）→反证双态。

**5. 边缘情况与诚实边界（开工预判）**：①**B2 常量表达式形态**（`整16 甲=100-200` 宿主拒/v2 放行）＝v2 语义检查「检不出放行」**既有诚实边界**（推导源类型ID静默 不覆盖二元表达式类型推导）——本轮**不关**（表达式类型推导=独立能力缺口·立案 G 区 T98·非本轮半截）；②显式强转产物寄存器（窄码）经规范化入口=零开销直通（已规范守卫）→194 等显式转换用例零变化；③整32 回绕面=宿主「任意字面量放行+运行回绕」实测定案（h1/h5）——**不设值域检查**（与 整16 差异如实注记代码）；④正64 打印符号性（h3 宿主 2^63/v2 -2^63）=打印/字面量折叠面**独立缺陷另立案**（非存储面·本轮不触）；⑤形参规范化=被调方入口（覆盖全部调用点）；引用/指针/结构体形参不触。

**6. 与他机交叠分析**：写集=v2 树 5 文件（IR 生成层+语义）——家机在飞（D1 拆分=宿主 src/552 phi_lower/556/561/563/564 均不触 v2 树）、单位机 558/557/559（src/tests）**零文件交叠**；语义耦合=B 族为 v2 拒通道收紧（负路径）·正路径零变化面由 502 双编译锚定；A 族变更 v2 产物指令流（窄宽用例多 1~2 指令）——**影响面=v2 产物三平台**，集成后请家机/单位机按通告复验 194/385/386 等窄宽既有用例（本机全量先行）。
**【收工回填（实测）·565】**
**1\'. 验收（达成·含开工预判外当场根治两枚）**：①探针矩阵 A 族全绿——op6=-56/op7=4464/b1=-5536/b2=-5536/b5=-5536/h1=-294967296/h5=1705032704/h13=-294967296/h14/h15=-5536（全部与宿主逐值一致）；②B 族六形态 v2p 全部 sem5 编译期拒绝（g1/g2/g5/g9/g10/h6）·域内 g3/g6/g8/h7 零误伤；③502 双编译 PASS+503 负测 PASS（E2E runner 实跑）；④全量门禁（数字见提交信息）+194/130/05 回归全绿；⑤反证双态=修复前 v2p（develop+562）跑 502 同源输出 200/60000/70000/…/4000000000（分叉实锤·修后=期望值）。
**2\'. 写集（最终）**：CN语言编译器v2/IR/生成声明.cn（窄宽码自文本+窄宽值规范化 helper·声明站点+指针层守卫）｜CN语言编译器v2/IR/生成语句赋值.cn（赋值站点换接+复合脱糖标识符块·查指针变量类型 守卫）｜CN语言编译器v2/IR/生成表达式.cn（字面量适配整32 改值域判定）｜CN语言编译器v2/IR/生成函数.cn（返回窄宽码 登记+形参入口规范化）｜CN语言编译器v2/IR/IR容器.cn（函数IR +返回窄宽码）｜CN语言编译器v2/IR/生成语句.cn（返回位规范化）｜CN语言编译器v2/语义/语义检查表达式.cn（整字面量入域+整数字面量文本值·隐式转换合法 两分支挂接）｜tests/e2e/502_窄宽回绕矩阵/503_窄宽字面量域外拒绝+coverage_map。
**3\'. 实施增量（预判外当场修·三枚）**：①**指针声明落穿**——指针/引用声明在 生成变量声明 分派不早退·落穿 生成标量声明初值 且类型文本=基类型：站点①加 类型节点.附加==0 守卫·站点②/成员块加 查指针变量类型<0 守卫（**教训**：首版无守卫→194/130/05 全量 E2E 段错误·全量门禁当场拦截=网底价值再证；**既有着火点**：正32存储截断 对 正32* 声明同面为 46-f 起既有隐患·本轮守卫顺带消除）；②**查指针变量类型 未命中哨兵=-1 非 0**——首版 ==0 守卫误伤全部非指针赋值（b5/h13 回退实锤）·改 <0；③**复合赋值脱糖节点走成员路径**——文本=0 的合成 节点_赋值 分派进 生成成员解引用赋值·标识符左值在此补规范化（最初误判=纯赋值路径覆盖）。
**4\'. 边缘（回填）**：预判①成立（B2 常量表达式=T98 立案·g4 运行值已正确）；预判②成立（194 显式转换用例零回归·已规范直通守卫有效）；预判③成立（整32 值域不检=宿主 h1/h5 定案）；预判④成立（正64 打印符号性=T100 立案）；预判⑤成立（形参规范化被调方单点·194 读窄(正8*) 零回归）。**新发现**：v2 传参面隐式转换整体缺检=T99 立案（g11 实锤·运行值已由形参入口规范化纠正）。
**5\'. 交叠（回填）**：零冲突（写集=v2 树+tests 502/503·他机在飞不触）。

> 〔2.83 D46（**v2 结果构造名裸名编译期拒绝**＝返回位/声明位/赋值位三段·474/475/476-a） 已滚转归档（深度机 565 轮·2026-09-21·切片=plans025-深度机节-2026-09.md）〕
> 〔2.84 D46 残余收口（**构造名裸名全位拒绝**＝语义检查表达式.cn 59 行放行改拒绝） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 D10 v2 面 已滚转归档（深度机 546-a 棘轮·2026-09-21·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.1 收工回填（**314-a 🧪 M3 采样轮·x64l 列**·基线 a33e3aa〔=src 与 312-a 基线 ce8089c 等价〕·2026-09-17 收工）：**双域零命中+T44 复验+★T45 立案** 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕

**【收工回填（实测）·521-a】**
**1\'. 定性结论（三选一取「撤销登记」）**：①探针 6 形态（下标目标=取点(10)/对表[1]=取点(20)·标识符目标=取点(30)·下标=复制(源)·结果构造=正常(50)·对照健康面）——宿主与 v2p 编译运行输出 **10 行逐行全同、rc 同 0**；②asm 铁证=「下标目标=结构体返回调用」发射 **op=61 整体赋值通道**（`mov rcx,24; rep movsb` 24 字节）而非 StorePtr 8 字节截断——234-a 时期的「emitStructWholeAssign 无源分派」现状已被后续接力链（468 九链/435/441/477 等）覆盖；③全库存量扫描=显式「下标目标=函数调用」形态 E2E/v2 树/stdlib ≈0（零覆盖=既有事实）。
**2\'. 写集（最终）**：plans/021（D11 迁 §4.2+§五 521-a+戳）｜plans/025 本节｜交接/更新日志｜看板（清行）。零 src/v2 树/tests 代码（纯侦查）。
**3\'. 边缘（回填）**：①成员链目标（`外.内 = 取点()`）依赖类内结构体字段=T80 面（513 链挂等回签）——不构成 D11 独立缺口·已注记；②**附带发现（登记）**：宿主对「下标结构体字符串字段」类型推断降级「未知」（`打印(点表[2].名)` 报「参数类型不支持：未知」·`字符串 名=点表[2].名` 同病·84-d 同族候选）——探针以字符串比较法绕过；③探针可作 E2E 正测转正候选（下一 tests 轮搭车）。
**4\'. 交叠（回填）**：517 链（生成语句赋值.cn）零冲突（本轮只读）；全程零文件碰撞。

**【收工回填（实测）·523-a】**
**1\'. 验收（达成）**：①md5 双对拍=探针 3982d9f7+全树 111038 节点 c4106a2c（新旧 v2p 编译同一改前文本）✓；②行为级反证=查ac 恒等变异→v2p_mut 编译 456_泛型指针字段下标 产物 761102ae（变）→恢复→82fc1dc0 复绿 ✓——**克隆修针消费面=泛型实例化（v2p 编译用户程序）·自举编译（v2p 编译 v2 树自身）不触发**（全树/探针对修针变异零敏感=三轮反证迭代定位）；③check_fn_length 61→60 ✓；④门禁=单测 1387/1387+E2E 522=517/3/2（3 假红=同编号互踩·串行单跑全 PASS）+strict 零孤儿+matrix 83/83 ✓。
**2\'. 写集（最终）**：CN语言编译器v2/IR/IR类.cn（+167/-134·分派器 14 行+十子函数+返回行补齐——拆分后子函数须显式 返回 n2c·v2 非空返回类型纪律）｜文档六件套。
**3\'. 边缘（回填）**：①**侦查两改方案均被否**——四槽位族分组（基于 grep 截断误读）与 else-if 重排（丢兜底先行回填=行为变更）→最终=保守分派器拆分+兜底不返回；②首轮切片漏 7 行（分支收尾 `}` 与下行行首 `} 否则` 分离）→花括号失衡编译期当场暴露·边界断言补齐重切；③返回行补齐=v2 语义要求。
**4\'. 交叠（回填）**：全程零冲突。

**【收工回填（实测）·524-a】**
**1\'. 数字（gcov 命令级可复现）**：ir_verify.cpp 行 86.58%（149 可行行）·分支执行 88.95%/走向取正 64.21%；ast_printer.cpp 行 68.78%（474 可行行）·分支取正 36.54%；全项目行 79.21%/分支 45.84%（134 编译单元）。**对比**：ir_verify 较 283-a 基线 85.59% **+0.99pp**；ast_printer 较 471-a 前 14.6% **+54.18pp**——471-a 家机语料轮 linux 侧实证有效。
**2\'. 边缘（回填）**：①总行覆盖 79.21%<基线 80.57%=**门禁语料步未跑的低估**（check_source_coverage 全链在 E2E 步被已知假红族中止 rc=1·语料步未执行·gcda 聚合完整）——非真劣化；②gcov 单文件聚合命令：`gcov -b <build-cov>/CMakeFiles/<lib>.dir/src/**/<file>.cpp.gcda`。
**3\'. 交叠**：零代码零 tests·全程零冲突。

### 2.116 D1 设计型池实施（**解析类声明 185 行按 §2.115 定稿方案拆分**＝五子函数+布局三元组引用传递·纯机械零行为变更）——537-a 实施轮（分支 `任务/深度机-537-解析类声明实施`·基点=develop 最新·wt520 隔离·2026-09-21）
> 〔2.112 501 链处置小节已滚转至归档（574-a 二次清偿·链已作废纯历史·git 可考）〕

> 〔2.113 小节已滚转至归档（574-a 三次清偿·git 可考）〕

### 2.114 D1 滚动（**v2 树 `生成表达式求值.cn` 双函数拆分**＝生成复制展开 175+生成方法调用 146·纯机械零行为变更·501 链作废后避让解除）——532-a 开发轮（分支 `任务/深度机-532-生成求值拆分`·基点=develop 9b991aa1·wt520 隔离·2026-09-21）

**1. 目标与验收判据**：021 §3-D D1 滚动第 5 弹——`生成表达式求值.cn` 两函数：生成复制展开 175 行（151-325）+生成方法调用 146 行（444-589）各拆为分派器+子函数（同族形态按源文实测）。**验收**=①新旧 v2p 编译同一改前输入 asm 逐字节 md5 一致（x64l 探针+全树双对拍）；②反证双态；③check_fn_length 出列两函数；④linux 全量门禁零新增红。
**2. 写集**：CN语言编译器v2/IR/生成表达式求值.cn（唯一代码文件）｜文档六件套｜看板清行。零 src/零 tests。
**3. 步骤**：基线→侦查整读两函数→拆分→对拍→反证→门禁→六件套。
**4. 验证链**：md5→反证→构建零警告→单测→全量 E2E→strict/matrix→check_fn_length。
**5. 边缘（预判）**：①**侦查必须整读函数**（523 教训·grep 截断曾致误判）；②两函数为独立分派结构（复制展开=节点类型分派·方法调用=调用形态分派）——若实测为共享状态顺序循环则如实登记不拆（解析类先例）；③同号假红族照旧口径。
**6. 交叠**：生成表达式求值.cn 与 513~517（生成语句赋值/生成表达式字段/生成表达式/IR生成）不同文件零交叠；501 已作废。

**【收工回填（实测）·532-a】**
**1\'. 验收（达成·范围收窄如实声明）**：①生成复制展开 175→主函数（前置可信判定 71 行+四路分派）+四路子函数（字符串改写 6/标量直通 12/容器类 27/结构体物化 47）——13 条边界断言·逐字迁移 ✓；②md5 双对拍=探针 3982d9f7+全树改前文本 111751 节点 c4355400 ✓；③**行为级反证**=④槽偏移变异（-1 删除）→probe4 结构体复制消费面产物 1c85b603（变）→恢复 d71c7563 复绿 ✓；④check_fn_length 58→57 ✓；⑤门禁=单测 1387/1387+E2E 522=516/4/2（4 假红=同编号互踩已知）+strict/matrix ✓。
**2\'. 范围收窄声明**：生成方法调用 146 行=同文件第二函数**登记下一轮**（本轮单函数已足轮量·四路精密分支的拆分验证周期超单轮预算）。
**3\'. 边缘（回填）**：①**反证迭代三才发现消费面**（探针算术面/全树自举均不消费复制展开路径·标量直通返回变异=等价变异产物不变——AST 生成层文件的反证消费面=「含目标形态调用的用户程序」probe4）②行号切片一轮漏 复obj 首行（编译期「未找到匹配的函数 IR追加」当场暴露·断言补齐重切）。
**4\'. 交叠（回填）**：全程零冲突。


**【收工回填（实测·改判）·533-a】**
**改判=设计型重构候选（不拆）**：整读 生成方法调用（456-605·151 行）——**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·嵌套 5 层+跨层 `返回;` 终结语义+共享可变状态〔表达式栈/指令/计数指针〕）——机械拆分不可行（嵌套提取需把外层条件转子函数前置守卫·跨层 返回; 转译风险高）。**登记=设计型重构候选**（与 解析类声明 185/解析选择语句 136 同类·需独立设计轮）。零代码收工。

### 2.115 D1 设计型池·方案设计轮（**解析类声明 185 行重构方案**＝五子函数划分+状态传递设计+等价验证策略·零代码·登记待实施）——535-a 设计轮（分支 `任务/深度机-535-解析类声明设计`·基点=develop 603b4745·wt520 隔离·2026-09-21）

**1. 结构分析（整读定案）**：`解析类.cn:252-436` 解析类声明——两层循环（262 继承跳过当+277 类体主循环 当(真)）+19 个局部状态·其中**布局三元组**（字段偏移/最大对齐/最大字段）跨分支累加+**位置**全程推进+**段可见性**主循环内切换。类体主循环内四类成员形态：方法（300-333·含构造）/不安全析构+函数（343-392·414-a stdlib 析构形态）/字段（397-416）/默认段守卫。**改判依据复盘（533 先例延续）**：非扁平分派但**成员形态间互斥且有清晰边界**——可拆（与 解析选择语句 的跨层中断不同·本函数无跨层 中断 语义——262 循环的 中断 限于自身块内）。
**2. 子函数划分方案（五子函数）**：①`解析类继承跳过`(记号,位置)->位置——262-269 继承/接口列表显式诊断+跳过；②`解析类可见性标签`(记号,位置,段可见性)->(新可见性,新位置)——283-289；③`解析类方法成员`(池,记号,位置,段可见性,类键基,...)->(新位置,是否消耗)——300-392 方法/构造/不安全析构形态（最大块·内部可再按 414-a 析构形态/普通函数二次切分·实施轮定）；④`解析类字段成员`(池,记号,位置,段可见性,字段偏移&,最大对齐&,最大字段&,...)->(新位置,是否消耗)——397-416 字段+布局累加；⑤`解析类布局收口`(字段偏移,最大对齐,池,...)->位置——422-433 尾部对齐+登记校验。
**3. 状态传递设计**：位置=每子函数返回值（值传递）；池/符号表=引用参数（既有惯例）；布局三元组=引用参数（`整64& 字段偏移` 三连·对齐 登记函数参数们 的引用惯例）；段可见性=值输入；主循环的 保底推进（420 前位置防死循环）保留在主函数（防死循环守卫不入子函数=职责单一）。
**4. 等价验证策略**：①新旧 v2p 编译同一改前输入 asm 逐字节 md5（探针=类/接口/继承/构造析构/运算符重载成员全形态用例 20/21/22/23/95/100/108/333+全树双对拍·518/530 口径）；②反证=变异布局累加行→产物变→恢复复绿；③全量门禁（解析类用例族 20/21/22/23/28/95/100~108/333 全在 E2E 覆盖）。
**5. 实施轮预估**：方案已定稿·实施=行号切片+签名落地+对拍反证+门禁（约 1 轮·同 532 周期）·候选=下一会话首选拾取。
**6. 交叠**：解析类.cn 零占用（329=词法/主/货舱·513~517=IR 生成层）·与全部在飞零交叠。


### 2.123 F2-33 迭代器侦察定稿轮（**T93 宿主泛型实例指针合成缺陷立案**+迭代器设计定稿·零入库代码）——548-a 侦察轮（分支 `任务/深度机-548-迭代器`·基点=develop 9901fa47·wt548·2026-09-21）

### 2.121 D12 v2 词法预处理对齐（**指令四件+条件栈嵌套+裁剪模型+主.cn 三挂点+CLI -D 通道**＝规范 4.7 v2 侧补齐·24_preprocessor 升级双编译）——546-a 开发轮（分支 `任务/深度机-546-d12preproc`·基点=develop 9ab6cca0·2026-09-21）

**1. 目标与验收判据**：v2 自举树补齐规范 4.7 词法预处理（宿主 preprocessor.cpp 210 行同构移植·裁剪模型=词法层空白替换保换行〔行号一致〕）：①指令四件=`#定义 宏名`（无值旗标）／`#如果定义(宏名)` 与 `#如果定义 宏名` 等价／`#否则`／`#结束如果`（严格配对）；②条件栈 {父活/分支已取/活} 支持嵌套+父层不活跃时内层 #定义 不生效（C 语义）；③被裁剪分支不产生任何 token（内含语法错误不诊断）；④诊断三态=未知指令/缺宏名/未闭合（编译中止·对齐宿主+词法错误计数全局态先例）；⑤CLI `-D 宏名` 注入通道（全局态模式·三处 分词 调用链零签名变更）；⑥**验收**=24_preprocessor 升级双编译对照 PASS（v2 产物运行输出=expected 7 行）+v2 全树源码裁剪幂等（零 # 行·v2p 自举链双 PASS 不变）+反证双态（撤裁剪挂点→24 预期失败）+全量门禁零新增红。
**2. 写集（精确清单）**：CN语言编译器v2/词法/预处理.cn（**新文件**·函数族 ≤400 行）｜CN语言编译器v2/主.cn（三处挂点各 +1 行·-D 参数解析段 +15 行左右）｜tests/e2e/24_preprocessor/双编译对照.txt（**新**·退出码=0/源文件=条件编译.cn/借链=否）｜tests/e2e/coverage_map.md（4.7 行 v2 列登记）｜文档六件套（021 D12 行/025 回填/交接/更新日志/看板清行）。
**3. 实施步骤**：①词法/预处理.cn 函数族（字节级逐行扫描+指令判定〔# 起始+中文关键字字节比较〕+条件栈 向量<条件帧>+宏集合 映射<整64,布尔>〔驻留 ID 键〕+提取宏名〔括号/空白双形态〕+裁剪主入口）；②主.cn 三挂点（主文件/包.cn/内置回退源 分词 前 `源码 = 预处理裁剪(源码);`）；③主.cn -D 参数解析（argv 扫描 `-D` → 设注入宏 全局态）；④24 升级双编译+coverage_map；⑤反证双态；⑥全量门禁+自举链。
**4. 验证链**：24 双编译 PASS（单跑）→ 反证（挂点改直通→24 双编译 v2 侧失败/输出错→恢复）→ 构建零警告 → 单测全量 → E2E 全量 --jobs 8（已知互踩假红单跑复验口径）→ strict/matrix → 自举链（宿主建 v2p+v2p 编 v2 全树 fix_p 双 PASS=预处理代码自身过自举）→ 探针（-D 手动命令验证注入通道）。
**5. 边缘情况（预判）**：①指令行判定=行首允许前导空白+# 起始（对齐宿主 181 行口径）——字符串字面量内的 # 不在行首不触发（宿主同款边界）；②CRLF 源（\r 在宏名终止集）；③`#如果定义(宏名)` 括号闭合即止=尾随内容忽略（宿主同构）；④裁剪后源码参与正常分词——#指令行本身被消费不产 token（指令行输出=空行保换行）；⑤未知 # 指令（如 #包含）=诊断拒绝（宿主同款·336_未闭合条件编译拒绝 宿主列已有·v2 侧随本轮对齐）；⑥条件栈未闭合=报错位置在文件末（宿主同款）。
**6. 与他机交叠**：写集=v2 词法新文件+主.cn+tests/24——家机 543=stdlib/正则 零交叠；537-definit=v2/语义 零交叠；536=v2 生成声明+宿主 semantic 零交叠；538=src linux_x64 零交叠（本轮回签面·只读其分支）。**判定=独立**。

**【收工回填（实测·546-a）】**
**1'. 验收（达成）**：①24_preprocessor 升级双编译对照 **PASS**（断言输出=是·运行级逐行全等·v2p 编译→as→g++→运行全链）；②-D 注入双态实测（无 -D→200/#否则 分支；`-D 命令注入`→100/#如果定义 命中）；③反证双态（主.cn 挂点注释→24 v2 侧词法+语法双错误红→恢复复绿）；④诊断负路径（未知指令 #包含/未闭合 #如果定义→[预处理] 诊断行+聚合守卫中止不产 asm）；⑤336_未闭合条件编译拒绝 v2 闭环负测上架 PASS（退出码=负）；⑥构建零警告+单测 1394/1394+strict/matrix PASS；⑦**78_v2/79_v2 自举链 PASS**（fix_p 全树 30s·v2p 编译预处理代码自身过自举）。
**2'. 写集（最终）**：CN语言编译器v2/词法/预处理.cn（新·270 行：指令四件+条件栈+快路径+裁剪主入口）｜CN语言编译器v2/词法/包.cn（+1 行 模块 登记）｜CN语言编译器v2/主.cn（三挂点+-D 扫描函数+主函数调用+聚合守卫·四处）｜tests/e2e/24_preprocessor/双编译对照.txt（新）｜tests/e2e/336_未闭合条件编译拒绝/v2闭环.txt（新）｜coverage_map（4.7 行负例列补 336_v2 注记）｜文档六件套。**零 src/ 宿主改动**。
**3'. 实施增量（预判外当场修·三枚）**：①目录包机制三踩——子文件须包根 模块 登记（文件名=模块名隐式绑定·无需 文件内 模块 声明）+跨模块符号须 `公开:` 段（532-a 生成表达式求值.cn 先例同构）——首编「未声明的标识符」三查后定位；②**★预处理逐行字符串拼接=arena O(n²) 中间串留存**——v2p 编 v2 全树在 4GB RLIMIT 下 std::bad_alloc（runner fix_p 卡死同源）→**快路径根治**：源码含预处理指令(源码) 零分配字节级扫描·无指令文件 `返回 字符串复制(源码)` O(n) 直通（安全区检查器拦截借用返回=防线正面实证）——修复后 4GB 限全树 rc=0·教训入册；③456 终审三连对照实验证实 537-a 引入（见看板 6bda3558·非本轮写集）。
**4'. 验证链（全绿口径）**：单测 **1394/1394**+E2E 全量 **522=516/4/2→修复后 78/79 转绿·终态=517 过/456（537-a 既有欠账·547 根治）+4 互踩假红（单跑 4/4 PASS）/2 跳**+strict 179/179+matrix PASS+自举链 78/79 双 PASS+4GB RLIMIT 复验 PASS。
**5'. 边缘情况（回填）**：预判①~⑥全部命中并处理；预判外=内存膨胀（③·arena 教训）与包机制三踩（①）。诚实边界：命令行 -D 面按 24 用例头口径=手动命令验证（探针双态实测）未入 runner 自动门禁；`#如果定义(宏名) 尾随内容忽略` 宿主同构未负测锚定。
**6'. 与他机交叠（回填）**：全程零冲突；541-revert（3d05b703）与本轮主.cn 挂点 rebase 自动合并成功。

### 2.122 456 根治轮（**537-a 布局收口拆分回归修复**＝`类体起点` 池.大小()+1 静默行为变更回退·反证双态实锤）——547-a 开发轮（分支 `任务/深度机-547-456布局修复`·基点=546 头 72f77934 接力·2026-09-21）

**【收工回填（实测·547-a）】**
**1'. 根因与修复**：537-a（baf7b2c4）拆分混入唯一行为变更——`整64 类体起点 = 池.大小();` 被改为 `池.大小() + 1;`（无说明·违反纯机械声明）——节点_类.子起点+1 错位→泛型实例布局登记/查询键错→类型大小查表 miss 返 0（456 产物 op1 常量 8→0）→rc=14。**修复=回退该行至原版语义**（diff 全量复核=其余均为机械签名/调用迁移）。**反证双态**：+1 在=456 单跑稳定红 rc=14；回退后 456 单跑 **PASS**。
**2'. 验证链（全绿）**：构建零警告+单测 **1394/1394**+E2E 全量 **523=517/4/2**（**456 转绿**·余 4 红=210_v2/457/458/459 互踩假红族·单跑 4/4 PASS 复验）+strict 100%+matrix 83/83。
**3'. 写集**：CN语言编译器v2/语法/解析类.cn（1 行）+文档六件套。**教训**：机械拆分提交 diff 中任何非签名/非搬移的行为行变更=红旗须逐行说明；「md5 双对拍」对**含探针/登记类副作用路径**的输入面覆盖不足（456 泛型实例形态未进对拍样本）——补=拆分轮对拍样本须含泛型类声明形态。
**4'. 交叠**：接力 546 分支（含其全部写集）——回签一次覆盖两轮。

**【收工回填（实测·548-a）】**：①探针双侧分裂——迭代器最小形态（泛型类 向量迭代器{向量<T>* 源;游标}+绑定/有下一个/下一个+当循环）宿主 cn check 红「无法将 '向量<整64>*' 隐式转换为 '向量$整64*'」＝**T93 立案**（$ 合成名与 * 组合吃星号·src 类型系统合成名规范化缺口）；**v2p 同源 rc=0**（103 节点）→新用例走 v2 闭环形态（457 先例·头注宿主欠账）。②设计定稿=stdlib/迭代器.cn 泛型类 向量迭代器<T>{源指针+游标+绑定/有下一个/下一个}·525_v2_向量迭代器（借链=是）·映射集合迭代器=波2。③**549 接力计划**：宿主 T93 修复（src 类型系统独立轮）+525 上架+修后升双编译=消费者反证。④写集=零代码。⑤交叠零。
### 2.125 T95 修复轮（**泛型方法返回 T 推断缺口**＝成员访问基类型实例物化单点·编译面根治/运行面合流 T94）——551-a 修复轮（分支 `任务/深度机-551-t95泛型返回`·基点=develop 0cc770fb·wt551·2026-09-21）

**【收工回填（实测·551-a）】**：①复现精化——p2 探针编译双错：「类型 \'向量<整64>\' 不是结构体/联合体/类类型，无法访问成员 \'元素\'」（根因）＋「无法将 \'未知\' 隐式转换为返回类型」（连带）——**根因=泛型方法体引用参数（向量<T>& v）类型文本经 substTypeParam 替换保留尖括号形态（向量<整64>）未经实例物化·findClass miss**。②修复单点=semantic_expr_access.cpp 成员访问 findClass miss 前 resolveGenericTypeName 物化兜底（幂等：非泛型/已物化/命中路径零变化）——p2 check 由红转绿 ✓。③**运行面 SEGFAULT=与 T94 阶段二同根合流**（引用参数句柄链 this 双解引用——v 槽→表槽→堆对象 少一层）——编译面/运行面分案：编译面本轮根治·运行面随 T94 阶段二裁决后一并修。④门禁=单测 1394/1394+E2E 522=515/5/2（456=基点既有〔547 挂等〕+210_v2/457/458/459 互踩族）+strict/matrix 绿·零回归。⑤写集=semantic_expr_access.cpp 1 处+文档。⑥交叠零。
### 2.130 strict 双红根治轮（**050 登记复登记+601 裁决甲连带簿记**＝验收机当场修·机制级）——603-a 修复轮（分支 `任务/深度机-603-strictfix`·基点=develop cd9a1bdf·wt603·2026-09-22）

**1. 目标与验收判据**：develop 头 strict 门禁双红根治——①孤儿 050_双编译对照取地址类变量（592-a 验收机补登记 ec4c2064 被 591-a「净差异版」集成再丢失·登记丢失族第二例）；②映射有效性错误 9 个（601 裁决甲删 9 例 v1 化石用例 70~76/77/89 未清 coverage_map：[2.1] 正例→89/[附录A] 正例→77_audit_dump 两引用+70~76 七行豁免）。**验收**=`check_spec_coverage.py --strict` rc=0（178/178·指针有效+零孤儿）+matrix 83/83 不回归。
**2. 写集（预登记）**：`tests/e2e/coverage_map.md`（050 行复登记+9 处 601 连带清理）｜`scripts/check_spec_coverage.py`（EXCLUDED_CELLS 增 ("2.1","正例")·573 裁决甲连带——穷举正例 89 已随甲下线·v2 等价补位后应移除）｜plans/021（戳刷 603）｜plans/025 本节｜看板行（窄通道）。零 src/零 .cn。
**3. 实施步骤**：①验收 E2E 前置 strict 发现双红→归因（-S 追踪增删提交）→②看板先行直推 603 行+549/551 过时挂等行清偿（服务轮核实均已是 develop 祖先）→③coverage_map 四处编辑+脚本排除口径→④strict 树上复验转绿→⑤integrate（写集触 tests/**→全量门禁跑合并结果）→⑥看板通告段广播+交接本机节。
**4. 验证链**：strict rc=0（178/178）＋matrix 83/83＋integrate 全量门禁（零警告构建+单测 1383+E2E 全量 526 系·合并结果）。
**5. 边缘情况与诚实边界（预判）**：①[2.1] 正例格空缺=573 裁决甲的 sanction 后果（呈报乙「迁移 v2 等价」未被采纳）——排除口径注释已注明「v2 等价补位后应移除本条」，非静默豁免；②050 登记行连续两次丢失说明「净差异版/squash 集成对登记区行有系统性丢失风险」——integrate.py 581 增量判定缺陷备案同域，本轮不动底座、只随证据再备案；③601 收官销账若家机同步清理同区将产生覆盖_map 冲突——integrate rebase 解冲突取并集。
**6. 与他机交叠**：家机 601（已集成 e1935ae4·本修复=其裁决甲落地连带簿记·语义同向非冲突）；家机 595/601 在飞其他链（零写集交叠）；单位机 596（arm64 codegen·零交叠）；acceptance E2E 由本会话 wtv598 独立树并行承载（gate_lock 串行）。

### 2.2 下一轮候选（预登记·防撞车）

- **daemon 常态化采样续跑**（已重启新版·新种子域 312000001 起自动轮换）；🔶 行扩面巡视（候选=行 53 字符串下标/子串·行 54 容器边界 T17/T18）；
- 波次3=T6+T7（嵌套深度防护+语义复杂度）；波次5=C18+B12（含 T27 全族+i128 helper 修正项）；T44/T45 修复待用户裁决（建议同轮波次清零·写集零交叠）。

> 〔2.3 A12 里程碑标签锚点（**334-a 开发轮·轻量·已收工**·2026-09-18·分支 `任务/深度机-334-A12里程碑锚点`） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.64 条件产物存储轮（**D45 根治+D44 正体收口 双销项**＝贯穿恒真三修+选择语句宿主对齐重写 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.65 D42 甲案波 1（**泛型类源码形态·解析+语义层**＝`泛型 <类型 T> 类 名 { }` 声明解析+参数序登记+类型参数名豁免） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.66 D42 甲案波 2（**IR/生成层单态化实例**＝实例收集→实例注册为完整用户类→方法 AST 克隆生成） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.67 D42 甲案波 3 前置件①（**泛型函数声明+泛型体检查延后+撤守卫侦察**＝stdlib 源码级加载缺口清单实测立案） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.68 枚举全链（**D40 最大子项**＝枚举声明/登记/成员链常量折叠/跨模块导入·stdlib 源码级加载硬前置） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.69 D42 波 3 收口（**函数版单态化+调用点解析+撤守卫+stdlib 源码级加载全通+390 根治验收**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.71 stdlib 源码级 IR 段攻坚（**克隆器标识符位替换+实例布局链审计+装箱语境**＝420 实测缺口三族 ×132） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.72 IR 诊断宿主精确定位＋按宿主逐修（**类型大小 K/V ×48 的真正生成链锁定**→撤守卫+390 终验收） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.73 族②③ IR 段收口（**克隆器修针类型感知全集＋实例表配对越界＋裸字段下标左值＋装箱语境兜底**＝撤守卫 123 终收口） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.74 向量类克隆野走定位＋raw-$T 跳过＋撤守卫 123 零诊断终收口 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.75 撤守卫 123 运行期收口＋390 终验收＋456 上架 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.76 T62 三例无诊断硬错误→可恢复诊断改造（**v2p 302/rc=1 零诊断残余面收口**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 T70 面②' 扩形态（**生成容器对象整体赋值扩「右值=调用」接管**+IR 模块根成员内置直调+面⑤' 运行级定位） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.84 T57 重设计前置（**objdel 标签撞号根因定位**＝466-a 反证撤除的生产态 3 红源〔228/283 rc=1+79 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
> 〔2.85 T70 B1 专项（**撤守卫收敛尾段**＝克隆体推导链对用户主模块结构体元素 miss 根治） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
## 三、单位机 linux-arm64 节（本机维护）

### 3.21 本轮（**341-a 制度轮·🏃 轮次号「先到先得」落地（用户裁决方案①）**·基线 620831b·分支 `任务/单位机-341-撞号防线`·2026-09-18）——开工计划（六要素）

**1. 目标与验收**：跨机轮次号撞号根治（用户 2026-09-18 裁决方案①「认领前先看板、先到先得」）。**验收**：①AGENTS §8.1 占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；②check_handoff 新增「在飞轮次号唯一性」机械检查（跨机同号拦截）+正反例实证；③**plans/025 膨胀事故同轮根治**（实测 1780 行·§二/§三 被拼接 4 份→标题级去重保留最新→493 行）；④本轮自身按新制度实践（**先直推看板占号 341 再开工**——新制度首次完整闭环）。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 分支命名条）｜scripts/check_handoff.py（查看板() 扩「在飞轮次号唯一性」）｜plans/025（去重+本节）｜plans/021（戳+§五索引）｜HANDOFF.md／更新日志.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①看板直推占号 341（**已完成**）；②AGENTS 条文（占用权威=看板在飞行）；③check_handoff 检查实现+实证——**首版正则漏大写字母致反例未拦**（`340-D38`/`329-D12` 提取为 []）→ 修为 `[a-zA-Z]|中文` 后三情形实证过；④plans/025 去重（标题级·保留最后出现版本·断言零重复标题）；⑤门禁+integrate 集成。

**4. 验证链**：check_handoff 正例 0／反例（家机行改 341）拦截 1（报「在飞轮次号 341 被多机占用」）／还原 0；plans/025 去重断言（无重复 `###` 标题）；integrate.py 快速门禁（纯文档+平台无关脚本写集·按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界**：①轮次号提取正则须覆盖大小写/中文标识（本次首版 bug 当场实证发现并修）；②plans/021 恰 600 行上限（瘦身登记下轮·用户已提）；③plans/025 膨胀根因=集成/rebase 时「拼接而非替换」（本轮去重为止血；机械防线=`### 标题重复`检测建议下轮加——与已上线「## 节标题重复」检测同族）；④去重保留「最后出现」版本，三机节内容请两机集成后复核。
**6. 与他机交叠**：家机 340-a／深度机 338-a 在飞（写集=v2 树/src 面）与本轮（文档+门禁脚本）**零文件交叠**；plans/025 三机共用（§一/§二 保留最新版·集成后在通告段请两机复核本机节）。

### 3.20 本轮（**332-a 修复轮·🏃 看板先行机械强制（用户令）**·基线 044423b→集成 b45e4ae·分支 `任务/单位机-332-看板强制`·2026-09-18）收工回填：**制度缺陷根治（开工先填板/收工即销账·机械强制）**

**收工回填（实测·目标全达成）**：①AGENTS §8.1「看板先行」（开工第一动作=直推看板本机行→才开分支；收工集成成功即清行直推）＋§8.5「强制直推、即改即推」（看板行/通告段/try-build 请求三类）落盘；②check_handoff「开工自检」上线——**双向实证**：正例（本分支 0 通过）／反例A（看板不含当前分支名→拦截·含「开工自检」字样）／反例B（🏃 行缺分支名→拦截·证明新小节结构下旧校验仍有效）／还原复跑 0；**判据修正（自建制度首日自证 bug·当场根治）**：收工销账清行后分支名自然不在看板 → 加「**分支已集成（`git merge-base --is-ancestor HEAD develop`）则放行**」豁免，三情形实证（正例放行／销账态放行／未集成+无看板行拦截）；③看板表拆三机独立小节＋查看板() 适配（4 列识别+节内恰 1 行）；④集成 b45e4ae（integrate.py 快速门禁通过；纯文档+平台无关脚本写集按 §8.3 不触发 try-build）；⑤**本轮自证实测两类冲突**：单表三行结构下与深度机看板连续直推撞 rebase 冲突两次（手工解·两行版本并存）＋push CAS 竞争一次（重试复推）——结构拆分与 CAS 均为实证驱动修复。**边缘回填**：非 git 环境开工自检自动跳过（实装）；集成后清行再跑门禁的误报已由「已集成豁免」根治（本轮实证）。**诚实边界**：①332-a 自身开工时未先直推看板（旧习惯残留·已在 HANDOFF 记录并随结构同批生效）；②plans/021 达 600 行上限（卡线·下轮需瘦身）。

**1. 目标与验收判据**：把用户令（2026-09-18）「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」落为**机械强制**。**验收**：①AGENTS §8.1 新增「看板先行」强制条（开工第一动作＝直推看板本机行；收工即销账）+§8.5 直推窄通道改「强制直推、即改即推」；②check_handoff.py 新增「开工自检」（当前分支 `任务/*` 时看板必含该分支名）+**正反例双向实证**（正例放行／反例「先动手后填板」拦截）；③看板表拆为**三机独立小节**（防相邻行 rebase 冲突——332-a 当日实测两次）+查看板() 适配；④check_handoff 全绿。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 看板先行/认领/看板行格式三条·§8.5 直推窄通道+看板结构条）｜scripts/check_handoff.py（查看板() 适配小节结构＋查开工自检()＋主流程挂载）｜三机任务看板.md（结构拆分＋本机小节＋332-a 通告段）｜plans/025 本节｜更新日志.md／HANDOFF.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①AGENTS 条文（用户令原文为依据）；②check_handoff 查看板() 改小节识别（4 列）＋查开工自检()（判据＝当前分支名出现在看板任一行·`git rev-parse --abbrev-ref HEAD` 容错）；③看板结构拆分（三机节·内容原样迁移）；④门禁正反例实证；⑤文档六件套；⑥integrate.py 集成＋通告段广播结构变更。

**4. 验证链**：`python3 scripts/check_handoff.py`（正例 0）｜反例 A＝看板不含当前分支名（拦截 1·含「开工自检」）｜反例 B＝🏃 缺分支名（拦截 1·新结构下旧校验有效性）｜恢复后复跑 0｜集成门禁（integrate.py 快速门禁——纯文档+平台无关脚本写集，按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界（开工预判→回填）**：①开工自检判据＝「分支名出现在看板任一行」——跨机 try-build 在他机分支上跑时分支名在其机行内即满足（分支主人集成前不清行）；集成后清行再跑旧分支门禁会误报（罕见场景·可接受·回填实测）；②非 git 环境（打包校验）自检自动跳过；③结构变更＝三机共享契约变更 → 通告段广播，他机 rebase 时按其行取值解冲突；④本机 329-a 在飞可见性由 044423b 承载，332-a 行随本批集成更新（结构变更同批故不再单独直推）。

**6. 与他机交叠分析**：家机 329-a（v2 语义层·D38批2）不碰 AGENTS/check_handoff/看板 → 零交叠；深度机 331-a（`src/` 三后端+IR 验证器·try-build 请求中）改其本机行/通告段，与本轮「看板结构变更」**语义相邻**（他 rebase 时按新结构取本方行）——已在 332-a 通告段写明；其余零交叠。

### 3.18 上一轮（**328-a 开发轮·🏃 协议 v2 落地**·用户授权插队·基线 93b11df→7b84b93（rebase）·分支 `任务/单位机-328-协议v2`·2026-09-18）收工回填：**协议 v2 全链落地+本轮自身走新流程端到端演练**

**收工回填（实测·目标全达成）**：①AGENTS.md §8 全文重写（六总纲+§8.1~8.7；§6 三处旧引用同步+§3 缺陷零容忍分级注记）；②`scripts/integrate.py` 新增（合并队列七步·--dry-run/--try-build-done·平台感知门禁）——**实战两遇**：本地缓存（.zcode/plans/）挡前置自检→改「未跟踪文件警告放行」+.gitignore 补忽略；**家机 326-a 收工与本案真实碰撞**（develop 前进+3 文件冲突）→integrate.py 按设计自动 abort 报告→分支主人手工解冲突（plans/021 戳合并双方信息/lessons 双段保留/看板通告段双侧拼接）→rebase 完成→--dry-run 全步骤过（退出码 0）；③check_handoff.py 扩看板 🏃 行分支名校验——**反例（330 号无分支名）拦截+正例（326 豁免/328 合规）放行双向实证**；首版曾真实拦截家机 326-a 旧格式行→加「轮次号 <328 过渡豁免」（旧协议在飞轮无分支可标·收工清行自然消亡）；④plans/027 方案文档落盘（诊断/业界对照/裁决记录/细则/试点安排与复盘区）；⑤共享文档全套更新（看板说明区 v2 口径+生效广播+试点邀请/plans/021 戳+里程碑+试点行+§五索引〔瘦身 3 行回 600 内〕/lessons 设计依据条目/HANDOFF/更新日志单位机节）。

**1. 目标与验收判据（达成）**：六项验收全达成（①AGENTS 重写②integrate.py 演练过③校验双向实证④plans/027 落盘⑤本轮走新流程⑥双文档门禁 PASS·plans/021=599 行）。

**2. 写集（最终清单·11 文件）**：AGENTS.md｜scripts/integrate.py（新）｜scripts/check_handoff.py｜.gitignore（补 .zcode/plans/）｜plans/027-三机并行协同架构v2方案.md（新）｜三机任务看板.md（本机行+说明区+通告段）｜plans/021（戳/里程碑/§三-A 试点行/§五索引+瘦身）｜plans/025 本节｜HANDOFF.md/更新日志.md 单位机节｜lessons.md（追加+rebase 合并 326-a 段双保留）。

**3. 实施步骤（回填）**：按计划①~⑩全部执行；插入项=两处 integrate.py 实战修正（未跟踪文件警告放行/冲突自动 abort）+rebase 冲突解决（与家机 326-a 收工真实碰撞）。

**4. 闅证链（回填实测）**：py_compile 双脚本过；check_handoff+check_progress_sync 全过（独立验退出码·管道吞码教训在案）；反例拦截/正例放行双向实证；integrate.py --dry-run 全步骤过（前置自检/fetch/rebase 对齐/快速门禁双过/push --dry-run）；真集成=git push gitcode develop（ff-only）+github 镜像补推。写集纯文档+平台无关脚本 ⇒ 按 §8.3 分类不触发 try-build、不重跑 E2E（基线 7b84b93=家机 326-a 收工+看板修复提交承载）。

**5. 边缘情况与诚实边界（回填）**：①**并行开发实效未验证**——本轮单分支落地，试点轮（plans/027 §六）承载并行度实测；②integrate.py 首版 try-build 回签=人工通告段+旗标声明（自动解析留试点后迭代）；③集成门禁增量判定（竞争重试轮跳过全量门禁条件）尚未经历真实竞争检验；④python 中文标识符带空格=语法错误（首版两处·py_compile 当场拦截）；⑤看板 328 行在真集成成功后经窄通道清行（收工广播随通告段）。

**6. 与他机交叠分析（回填）**：家机 326-a 收工与本案**真实碰撞一次**（develop 前进+看板/plans021/lessons 三文件冲突）——按 v2 设计路径消解（分支主人自解冲突·rebase·非 develop 手解），**此碰撞即 v2 协议设计的第一个实战案例**（旧协议下=双直推冲突+可能冲突标记入库；新协议=隔离分支+集成时一次性解决）；深度机 ⬜ 全程零交叠。

**下一轮候选（预登记）**：①**协议 v2 试点**（plans/027 §六·另两机各开真实任务分支并行→复盘）；②M3 采样轮续（B8 候选行+daemon 常态）；③T24/T25 v2 树对齐轮（随家机 v2 面轮次）。

### 3.17 上一轮（**297-a 开发轮**·🏃 用户裁「按照你的方式，全部修复」·基线 478d804→认领 ab935b4·2026-09-17）收工回填：**T25/T24/B10 三案当日根治+T26 同族判定+规范条文+406/407/408 转正·全量门禁绿**

**收工回填（实测）**：①**T25 根治**：病灶精确定位=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支向 storeVirtualResult 传 w9 → emitStackStore 浮点分支 `str w9` 32 位截断写（高 32 位丢；1.5 低 32 位恰全零故读出 0.0）——修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0·asm 实证 `str d0` 64 位完整写）；②**T25 回归矩阵 27 形态全绿**（三元 9+汇合 9+最小 2+浮32 3+数组 2+显式转换口径·C 一致 O0=O3）；③**T24 根治**：visitUnaryExpr Inc/Dec 三路径（静态/成员下标解引用/主路径）+handleClassFieldIncDec(+postfix) 按后缀返回旧值（原忽略 postfix 一律新值）——16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照·O0=O3）；④**B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻（括号/嵌套 ? 深度感知·新判据⊇旧判据=存量成功程序零语义变化论证）替换 C-1 白名单——`a ? -b : c` 双级别全绿（u1~u5）+后缀传播 `r?+1`=42 不回归+E2E 60 单跑绿；⑤**T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无截断分派同型）静态审查注记；⑥**规范条文**：plans/001 §4.5 优先级表（++/-- 后缀 13/前缀 12 级+值语义）+三元消歧条文+错误传播节更新；spec 01b 五处同步；⑦**新 E2E 三件转正**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记；⑧**全量门禁绿**：零警告构建+单测 1353/1353+全量 E2E 426=424/0/2（跳=62_ffi/69 平台败面）+**78/79 自举锚定双 PASS**+E2E 60 传播复验绿；⑨看板通告段=v2 消歧同口径广播（家机 296-a 对齐）。

**1. 目标与验收判据（达成）**：六项全达成（T25/T26/T24/B10/规范/门禁——§三.17 计划①~⑥逐项 ✓）。

**2. 写集（最终清单）**：src/cn_compiler/codegen/arm64/arm64_instructions.cpp（emitCopy）、src/cn_compiler/ir/ir_expr_unary.cpp+ir_oop_field.cpp+ir.hpp（T24）、src/cn_compiler/parser/parser_expr.cpp+parser.hpp（B10）、tests/e2e/406/407/408（新·各 2~3 文件）、tests/e2e/coverage_map.md、plans/001、.zcode/skills/cn-language-spec/specs/01b、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段。**v2 树零改动** ✓（家机 296-a 写面）。

**3. 步骤（回填）**：认领（ab935b4）→T25 侦察（emitCopy+storeVirtualResult+emitStackStore 链·IR↔asm 逐环对位）→修→27 形态回归→T26 判定（arm64 短路复现=免疫）→T24 侦察（visitUnaryExpr 忽略 postfix 实锤）→修→16 形态→B10 实现（ternaryColonAhead）→6 形态+传播回归→规范五处→E2E 三件→全量门禁→销项。

**4. 验证链（回填实测）**：零警告构建（grep error|warning=0）+单测 1353/1353+全量 E2E 426=424/0/2+78/79 双 PASS+406/407/408 单跑+60 单跑+最小复现 O0/O3 双对+asm d0 写实证+双门禁（独立退出码）。

**5. 边缘情况与诚实边界（回填）**：①T26=x86_64 单侧·精确形态验证归深度机（本机 arm64 免疫实证+静态审查注记）；②win/x64l Copy 浮点路径运行级复验归各机（静态=结构不同无同型截断）；③T24 v2 树同型对齐=待家机 296-a 收工后独立轮（通告段已广播）；④三元负号+浮点汇合的单测未加（E2E 三件为强门禁·单测面留后续）；⑤i1 Copy 在 arm64 走整数路径（is64=false→w9 32 位写）——布尔 1 字节值 32 位写安全（高位无关）·实证布尔矩阵全绿。

**6. 与他机交叠分析（回填）**：家机 296-a（v2 树）零交叠 ✓；深度机 🧪（只读+cnsmith_hits）零交叠 ✓；coverage_map 潜在文本冲突=rebase 兜底未发生。

**下一轮候选（预登记）**：①**M3 采样轮⑥**（回归 B8 候选行+daemon 常态·用户令不停）；②**T24/T25 v2 树对齐轮**（家机 296-a 收工后·自举编译器同型三缺陷）；③T26 精确形态=深度机口径（B11）。

> 〔T33 侦查注记/D36 实施设计已滚转归档（471 集成会话·2026-09-20·D36 定性详见 021 D36 行）〕