# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 256 轮（2026-09-16，单位机 ARM64）——256-a 收工回填：F1-26 方案 A 第一步落地（Phi 降级+使用点重写+验证器升级·单测 4/4·384 双形态）；下一轮预登记 257-a。

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **481-a（开发轮·🏃 plans/021 清账轮·分支 任务/家机-481-021清账）** | §3 已销项 31 行清账迁 §4.2+C19/T43/T53 补销项+§五/§六交错修复+文首戳刷新 +check_handoff 扩「§3 禁完成标记」防复发 | plans/021+plans/025 本机节+§〇本机行+scripts/check_handoff.py+scripts/文档棘轮基线.txt+看板/交接/更新日志本机节 （零 src/tests/v2 树） | 深度机 470/474/479/480=零文件交叠（其写集=src+v2 树+tests）；check_handoff.py 与家机 459 分支同文件——「上限 4→6」照抄同文自动合并，新增检查为独立函数；§4.2 追加行与深度机 474（D46 销项态）潜在同区冲突——集成序后到者 rebase 取清账版 | §1.0 | 🏃 |
| 深度机 linux-x86_64 | **🧪 314-a M3 采样轮·x64l 列** | 第五新种子域 310000001 差分 500+负向 400（310100001）+回归轨 114 全库+T44 x64l 跨机复验（t44scan/ext/if2）+日志日文件制首写 | 看板本机行、plans/025 本节、HANDOFF/更新日志深度机节、tests/cnsmith_hits/日志/2026-09-17.md（新）、命中样本（如有）、plans/021（T44 行证据回填·最小局部） | 家机 313-b 🏃（daemon/日志拆日写集）互认零碰；日志日文件与其拆日同向；单位机 ⬜ 空闲 | §2.1 | 🧪 在飞 |
| 单位机 ARM64 | **328-a（开发轮·已收工·分支 任务/单位机-328-协议v2 已集成 e6e22aa）** | 三机并行协同协议 v2 落地（用户批准）：分支即开发+integrate.py 队列集成+缺陷分级+分层 try-build+文档随分支走——AGENTS §8 重写/plans/027 方案/check_handoff 扩分支名校验 | 见 §3.18 收工回填 | 家机 326-a 收工与本案真实碰撞一次（rebase 消解=v2 首个实战案例）；深度机 ⬜ 零交叠 | §三.18 | ✅ 收工 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **258-a**（下一轮候选·待认领） | ①**A12 里程碑标签锚点**（B6 已批·轻量可搭车）②F1-26 后续子项（激进 SSA 管线化/mem2reg 全量·性能主线）③F2-33 拆分之三 迭代器 ④D28/D29 根治专项；257-a 已收工（§3-B 铁律根治） | 计划=开工时落盘本表 |
| 家机 win-x64 | **450-a（下一轮候选·待认领）** | 依 plans/021 §3-A 队列：M3 win 列采样（用户令不停）/清零波次余项/C20 批C·D parse 桶上架——开工以看板最新为准 | plans/021 排班+看板 |
| 深度机 linux-x86_64 | **414-a（下一轮·D42 甲案波 1）** | **泛型类源码编译全链·波1=解析+语义层**（2026-09-19 用户裁决「同意甲」·plans/021 §三-A D42甲行）：①v2 解析器 `类 名<T>` 类型参数表（解析类型文本/泛型闭合位置 既有 P7b 泛型函数设施复用）；②实例类型合成名（名$T$…·与泛型函数同款）；③语义检查类表挂接；④探针=v2p 编译 含 `类 盒子<T>` 源码不崩+实例声明语义通过。波2（后续轮）=IR/生成单态化；波3=stdlib 源码加载路线+390 根治 | 395-a 破案=plans/025 §2.46·呈报三案=plans/021 B2（已迁 §三-A） |

---

> 〔495-a 段标题占位行已随 529 开工滚转（2026-09-20·内容 git 可考）〕
### 1.0 收工回填（**564-a 开发轮·✅ D1 滚动=代码生成.cn 生成函数 153 行拆分完成**·2026-09-21·分支 任务/家机-564-d1genfunc·wt564 隔离）

**1. 达成**：153 行→主骨架+生成函数参数装载 子函数（retbuf 槽+双计数参数装载循环整块迁移·参数=函数信息/最大寄存器/用影子参/返回结构体/输出 5 项·注释逐字保留）——check_fn_length 153 档位消（主 ~66 行+子 ~91 行双出列）。
**2. 等价铁证**：新旧 v2p 交叉编译**同一改前冻结输入**（HEAD 全树 worktree 检出·112103 节点·ASCII 入口 cv2/main.cn）win-x64 asm **md5 逐字节一致 b8e1bdfe6a58386d**（10.8MB 全树产物）；**反证双态**=产物级注入一行→ec2c9a8b≠基线（管道捕捉）→还原→b8e1bdfe 复绿。
**3. 验证链**：Ninja 零警告构建+452 双编译 PASS（v2p 双重建）+check_fn_length 出列+双文档门禁；集成时全量门禁+develop 现态补跑兜底（v2 树单文件写集·md5 等价=三平台产物零影响·沿 530/554 快速门禁先例）。
**4. 教训（回填）**：①v2p 手编冻结树三坑=产物路径 target/v2asm.s 相对 cwd（冻结树须 mkdir target/）+入口含中文段（argv GBK 陷阱→ASCII 镜像目录 cv2/入口 main.cn）+worktree 残留注册（prune 兜底）；②runner 452 指纹通道可作 v2p 双版本构建器（checkout 切换工作树→重建→备份）。
**5. 交叠（回填）**：预判成立——代码生成.cn 零在飞交叠；563 链（语义检查.cn）集成序无依赖。

### 1.0 收工回填（**529-a 开发轮·✅ D1 滚动=分词拆分完成**·2026-09-20·分支 任务/家机-529-d1tokenizer·wt529 隔离）

**1. 达成**：`分词` 151 行→主循环+两分支函数（分词字符字面量/分词标识符·对齐 R3 分支函数惯例签名）——check_fn_length 出列（151 档位消）。
**2. 写集（最终）**：`CN语言编译器v2/词法/词法分析.cn`+plans/025+交接/更新日志本机节。
**3. 等价铁证（回填实测）**：同宿主双 v2p 重建（develop 基线树 vs 拆分树）——**四产 asm 探针 md5 逐字节一致**：t_call=4e259ec6/t_copy=05a55749/**t_lex 词法敏感探针（字符字面量'中'/'A'+汉字标识符+字符串拼接）=932e40ca**（拆分两块直接消费面）/230 字段所有权 493 节点=b07b8f1d；t_obj/235 两侧行为逐字符一致（v2p 已知语法缺口·非本轮面）。语义等价变换点=内联块「继续」→函数尾「返回 位置」/前缀字符串分支「继续」→「返回 位置」（主循环 continue 语义保持）。脚本=target/p529_duipai.py（不入库）。
**4. 验证链（回填）**：Ninja 零警告构建 PASS（85s）+check_fn_length 出列+双文档门禁 PASS；集成时全量门禁+develop 现态补跑兜底（v2 树写集不在全量触发面·522-a 同款诚实披露）。
**5. 边缘与教训（回填）**：①切片边界三次试错（264 是 `}` 非 263 继续/315 是 if 闭合非 314 继续/体末「继续」→「返回 位置」等价变换点）——边界断言逐条拦截=防线生效；②wt529 为残留 worktree（分支名恰为目标·直接复用）；③538 历史残留号教训=认领前查 git worktree list+历史轮次（528 撞 t47d36 当场换 529）。
**6. 交叠（回填）**：预判成立——词法分析.cn 零冲突。

### 1.0 实施计划（**525-a 开发轮·🏃 D8 第五件=arm64 oop/dispatch 装载直读**·基线 develop cc9c5b51·看板先行 20eec9ea·2026-09-20·分支 `任务/家机-525-d8arm64oop`·wt525 隔离·Ninja+sccache 开发树）

1. **目标与验收判据**：消除 arm64 后端 oop/dispatch 发射面的冗余装载中转 mov（457-a 收口后统计残余 2125 条，其中 oop/虚调用/接口分派/方法表寻址面=本轮靶子；Call ABI 接收与错误路径固定寄存器=必要不可消除维持）。验收判据（全部实测回填）：①同口径冗余 mov 统计（451-a 统计脚本·291 用例）较 2125 基线下降且给出量化值；②全量门禁零新增红（构建零警告+单测+E2E 全量+asm 宽度 291×3）；③**未分配 fallback 产物逐字节不变红线**（X64StructTest 同族锚定+探针 asm 对照）；④反证闭环（撤改动→中转 mov 复现→恢复→消失）。
2. **写集（精确文件清单）**：`src/cn_compiler/codegen/arm64/{arm64_instructions.cpp, arm64_codegen_emit.cpp, arm64_codegen.hpp}`（若逐点增补回填）+`plans/025 本机节`+`plans/021 D8 行`+`交接.md/更新日志.md 本机节`+`三机任务看板`——**零 tests/ 零 v2 树零 runner**。
3. **实施步骤**：①残余分布统计：对全量用例 arm64 产物按发射函数分桶计数装载中转（`mov xN, [基址,偏移]` 后紧跟消费而非直读），定位 oop/dispatch 各点（虚调用装载循环/接口 itable 分派/方法表寻址/类成员调用链）；②逐发射面改造：延展 457-a `operandSourceReg(regId, fallback)` 辅助至各点——已分配寄存器→直读零发射；未分配→fallback 原路径产物逐字节不变；③结果写入直写复用 451-a `resultTargetReg`（若该面 451 未覆盖的点存在）；④反证+全量门禁+统计回填。
4. **验证链**：`cmd /c target\ninja_build.cmd` 零警告 → `target/build-ninja/cn_unit_tests.exe` → `python tests/e2e/run_e2e.py --cn <ninja cn> --jobs 8` 全量零新增红 → `check_asm_width` 291×3 → 冗余 mov 统计对比（基线 2125）→ 反证双态 → 集成时 ci.ps1 合并结果全量。
5. **边缘情况与诚实边界（预判·收工回填实测）**：①oop 槽在 -O0 槽模式下多数未分配——fallback 红线优先，直读只在已分配路径生效，故量化收益如实报告不虚高；②虚调用装载循环若含跨函数运行时 ABI 契约（偏移协议）不动发射序；③i128/浮点面 width 特例（浮点 v 寄存器不参与通用直读）单独甄别；④残余中 Call ABI/错误路径份额=诚实边界永久保留项。
6. **与他机交叠分析**：写集=宿主 `src/cn_compiler/codegen/arm64/` 三文件——深度机 506/508/513~517=v2 树 IR/语义面零文件交叠；深度机 520/522+家机 522=v2 树 `编码算术ARM64.cn/编码对象调用ARM64.cn`（**自举树代码**·与宿主后端不同代码库）零交叠；深度机 524=零代码服务轮；家机 505t88=parser 面；501 重验会话=零写集验证树——**全部独立零耦合** ✓。

### 1.0 收工回填（**525-a ✅ D8 第五件=arm64 装载直读收官**·2026-09-20·分支 `任务/家机-525-d8arm64oop`·头 0eeb4149）

1. **量化成果（同口径 488 用例·stat525.py 临时面 mov x9/x10/x11 统计）**：基线 **2359 → 4 条（-99.8%）**——stash 双态实测（撤全部改动→2359 复现；恢复→4）。
2. **改造点八处（写集最终清单=`src/cn_compiler/codegen/arm64/{arm64_instructions.cpp, arm64_codegen_dispatch.cpp, arm64_codegen_oop.cpp, arm64_codegen.cpp}` 四文件·六要素写集声明三文件→实际四处增补 arm64_codegen.cpp 回填）**：①CopyStruct 地址基址直读（容器/结构体赋值族=原大头·127_v2 单例 45→0）②emitCopy 源直读写回（窄/宽结果未分配态）③FieldAddr 基址直读（空检查+加算免 x9 中转）④Cast 窄截断 i8/i16/i32 源直读⑤Cast scvtf/ucvtf 宽源直读⑥VtableAddr 对象指针直读⑦Call/VirtualCall 栈参数直读（self_host 族 68×8=544 条主体）⑧prologue 结构体参数基址直读。
3. **验证链实测**：Ninja+sccache 树零警告构建+单测 **1387/1387**+全量 E2E **522=522/0/0 零失败**+asm 宽度 **297×3 PASS**+stash 双态反证闭环（判据④·撤改动→2359 复现→恢复→4）；未分配 fallback 逐字节不变红线=各改造点 fallback 保持原装载目标（FieldAddr fallback=res 修正过一版·基址未分配+结果已分配态与原实现逐字节一致）。
4. **边缘情况回填（预判 vs 实测）**：①「oop 槽 -O0 未分配居多」预判成立——直读只在已分配路径生效，收益主体来自**参数/地址值参与分配后的写回与基址消费**（非预判的「已分配 op 装载」单一形态）；②**条件跳转 condReg 直读经定性撤销**——i1 不参与分配（isAllocableType=isWideIntType）恒零收益，留定性注释不改；③Call ABI 接收（x0~x7）/除零检查/错误路径=必要保留（诚实边界）；④**残余 4 条**（104_实例方法作值×2/440/459 各 1·形态=字符串常量地址/实例字段链写窄槽）——收益递减停止改造·诚实边界保留项（如需归零须逐条 IR 级归因·本轮判非必要）。
5. **统计口径说明**：stat525.py 扫 tests/e2e 全部 488 目录主入口（451-a 口径 291 用例不同集——本表数字自洽性以本脚本为准·脚本不入库随 target/ 消失·复现命令 `python target/stat525.py target/cn.exe`）。
6. **深度机联动**：520/522（v2 树 编码算术/对象调用ARM64.cn 拆分）与本体零交叠——本轮后宿主 arm64 后端临时面中转基本清零，v2 自举树的同族面（编码算术ARM64 等）=深度机 D1 滚动领地，如需同法改造归其排程。

### 1.0 收工回填（**522-a 开发轮·✅ D1 滚动=编码对象调用ARM64 拆分完成**·2026-09-20·分支 任务/家机-522-d1objcall·wt522 隔离）

**1. 达成**：153 行→主分派器 16 行+五子函数（编码分配对象ARM64/编码结构体拷贝ARM64/编码删除对象ARM64/编码调用参数化ARM64〔内拆 编码调用装载栈实参ARM64/编码调用装载结果ARM64〕）——check_fn_length 出列（超百行清单 152 档位消）。
**2. 写集（最终）**：`CN语言编译器v2/代码生成/代码生成ARM64.cn`（90+/72-）+plans/025+交接/更新日志本机节。
**3. 等价铁证（回填实测）**：同宿主（wt522 cn.exe）双 v2p 重建（develop 基线树 vs 拆分树·cwd=仓库根+stdlib 复制+target/ 目录）——**三产 asm 探针 md5 逐字节一致**：t_call 调用参数化四面（整型寄存器/浮点 d0-d7/栈参>8/混合）=4e259ec6、t_copy CopyStruct=05a55749、230_双编译对照字段所有权（493 节点全链）=b07b8f1d；t_obj/235 两侧行为逐字符一致（v2p 类字段语法已知缺口 D40 族·不产 asm·非本轮变更面）。脚本=target/p522_duipai.py（不入库）。
**4. 验证链（回填）**：Ninja+sccache 零警告构建 PASS（104s）+check_fn_length 出列+check_handoff/check_progress_sync PASS；集成时全量门禁（单测+E2E 全量+asm 宽度）兜底。
**5. 边缘与教训（回填）**：①首次切片脚本漏 706 行前文件体+漏 809 闭合括号（20 边界断言当场拦截两次·回滚重做=断言即防线）；②v2p 手工构建三坑=cwd 须仓库根（io_api.cpp cl 退出码 2）/asm 输出=target/v2asm.asm 相对 cwd/stdlib 在仓库根非 v2 树——已入册；③诚实边界：NewObject/DeleteObject 单独面未产 asm md5（t_obj v2p 语法缺口）——由 E2E 全量（108_H7 族双编译）兜底。
**6. 交叠（回填）**：预判成立——零冲突；520-a 同文件算术拆分已入基点 fe30216b。

### 1.0 实施计划（**481-a 开发轮·🏃 plans/021 清账轮：§3 已销项条目清账+结构修复+门禁防复发**·基线 25a8b9b7·看板先行 25a8b9b7·2026-09-20·分支 `任务/家机-481-021清账`）

**1. 目标与验收判据**：G 区 T10「字符串子串越界参数静默截断」（`字符串子串(s,2,99)` 对 3 字节串返回
"c" 无诊断——安全方向但无文档口径）处置=「随诊断轮顺带定文档口径」。本轮=①行为全集实测（起越界/
长度溢出/负值/空串/边界恰等）②宿主实现确认（截断语义唯一归属点）③规范文档口径落盘（cn-language-spec
skills specs 02-类型系统字符串节·行为文档化**非语义变更**——T10 处置已批准）④021 G 区 T10 销项。
**验收**：探针矩阵实测表+规范文档口径条文+021 销项台账行+双文档门禁 PASS。

**2. 写集（精确文件清单）**：`.zcode/skills/cn-language-spec/specs/02-类型系统.md`（字符串内置函数
口径节）｜`plans/021`（G 区 T10 销项迁 §4.2+文首戳）｜`plans/025` 本节+§〇本机行｜看板本机行（窄通道）｜
`交接.md`/`更新日志.md` 本机节。**零 src/ 零 tests/**——纯文档=快速门禁。

**3. 实施步骤**：①探针实测矩阵（起=0/中/越界×长度=0/恰好/溢出/负值×空串）②grep 宿主 子串 实现
（runtime/内置表）确认截断归属与实现一致性③spec 02 字符串节落口径条文（截断语义+边界定义+例）④
021 销项+文档六件套。

**4. 验证链**：探针实锤+`python scripts/check_handoff.py`+`python scripts/check_progress_sync.py`
+写集核对。快速门禁。

**5. 边缘情况与诚实边界（预判）**：①若实测发现「截断」之外的形态（负值回绕/崩溃）→机制级立案不混
本轮②spec 双拷贝（.zcode 与 .agents junction）单一事实源=.zcode③行为变更（如改 Rust 式 Option/panic）
不在本轮——如需另行语义级呈报。

**6. 与他机交叠分析**：纯文档轮零代码交叠；深度机 480/484/493（v2 树）与家机 483/494（挂等回签）
零文件交叠。spec specs 属三机共享契约——**行为文档化**（无契约面变化）照 §8.6 广播口径在收工时说明。

> 〔481-a 实施计划已滚转归档（471 集成会话·2026-09-20·清账轮已集成 git 可考）〕
### 1.0 实施计划（**476-a 开发轮·🏃 T71 专项根治：类无参构造实例化组合非确定段错误**·基线 e5011e75·看板先行 e2d5f9cf·2026-09-20·分支 任务/家机-476-T71组合段错误根治·worktree=wt476）

**1. 目标与验收判据**：T71（471-a 立案·plans/021 §3-G）——全形态组合源码（类显式无参构造实例化+结构体构造字面量+局部数组+泛型调用+循环/选择/三元）编译零诊断、运行同一产物**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突 UB）。消融三证据：去类+结构体绿（H）/去结构体单次绿后连跑崩（I）/结构体+数组+泛型最小组合 8 连跑绿（k/l）——触发面=类无参构造实例化与组合。**根治路径（Rust 对照=UB 先 ASan 定位再修·不求幸存）**：①**ASan 构建**（-DCN_ENABLE_ASAN=ON·229-a 已接入）编译复现语料→产物 ASan 运行直出 UB 报告（堆栈=越界/未初始化精确位置）——预期一步锁定根因；②最小复现收敛（逐形态增删至最小触发集）；③按根因修复（嫌疑面=局部类对象构造的槽分配/构造注入顺序/数组槽相邻踩踏）；④复现语料 **20 连跑全绿**+全形态版（484 完整版）转稳定+全量门禁零新增红。**验收**=ASan 报告归零+复现语料 20 连跑绿+全形态版 20 连跑绿+单测/E2E 全量绿+asm 反证（根因点前后指纹）。

**2. 写集（精确文件清单）**：以 ASan 定位结果为准（嫌疑面=src/cn_compiler 语义/IR 层类构造注入+代码生成槽分配）+tests/e2e（484 扩形态或新用例）+cnsmith_hits（T71 转正注记）+文档六件套。开工侦察后回填精确清单。

**3. 实施步骤**：①ASan 树构建（cmake -DCN_ENABLE_ASAN=ON·独立 build 目录）+复现语料编译运行采报告；②报告→根因定位（asm 三件套对照：崩点前后产物 diff）；③最小复现收敛（供回归用例）；④修复+反证（回退复现/修复消失）；⑤484 完整版转正评估+全量门禁；⑥T71 行销项+run_err 转正注记。**Rust 对照**：rustc 对 internal UB 的态度=ASan/UBSan 先行定位、修根因、加回归测试——同构。

**4. 验证链**：ASan 报告归零+复现语料 20 连跑绿+全形态 20 连跑绿+零警告构建+单测全量+E2E 全量（gate_lock 串行）+asm 指纹。

**5. 边缘情况与诚实边界**：①非确定崩溃的验证口径=连跑计数（20 次起）——单次绿不算绿（T71 铁证）；②ASan 树产物与生产锚定链无关（独立 build 目录·不影响默认构建）；③若 ASan 不报（栈读未初始化 ASan 弱项）→备选=MSVC /RTCs 或产物 diff 槽分析；④arm64/linux 侧复验归跨机轮。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470/474/475（v2 树/D46）零交叠（T71=宿主 src）；家机 468（九链重测集成·零代码）零交叠；471（挂等回签·写集=tests 冻结）——本轮写集=src+tests 新用例，与 471 的 coverage_map/用例编号可能相邻（484 已占·新用例取 485+）。

### 1.0 阶段回填（**476-a 根因定位阶段完成·修复实施续下轮**·2026-09-20）

**已完成**：①ASan 树构建（target/build-asan·需补 clang_rt.asan_dynamic DLL 至 exe 旁；产物 runtime 不吃 CN_ENABLE_ASAN=ASan 只插桩编译器自身——产物诊断须另走 cl /fsanitize 产物旗标通道·登记）；②**最小复现定案**（t71_min.cn=类无参构造+方法调用两要素·确定性崩·与数组/泛型/结构体无关）；③**定性勘误**：原「非确定」=单次绿误判（实为确定性 call 0）；④**asm/IR 双铁证**：IR `间接调用 %v5 0`（callee=常量 0·无 FuncAddr）→asm `lea r12,[rbp-608]`（this=变量槽地址·应读出对象指针）+`mov r11,0; call r11`；⑤语义层类型推断排除（checkCtorCall lastType_=className 正确回填）；⑥根因锁定=ir_oop_call.cpp handleClassCallExpr 某分支对「类实例方法调用」误发 CallIndirect（callee=0 兜底+this 装载形态错）——**嫌疑=T57 接管分支重设计（472 集成）改动面**（T57=objdel+写穿根治·触及方法调用接管分支）；⑦T71 行完整落盘 021 G 区（含勘误）。
**下一步（续轮精确指针）**：①读 handleClassCallExpr 情形B/C（类方法直接调用段）确认哪分支漏判（「计」类型=计数器 应走直接调用 emitClassMethodCall·为何进间接分支）——注意 472 集成的 T57 改动 diff（git show 359cd573 附近）；②修复=误判分支修正+callee=0 硬错误防线（T11 面②同款·IR verify 扩 CallIndirect callee 非零常量检查）；③t71_min 入 E2E（485）+T71 复现语料 20 连跑绿验收；④候选回归面=T57 三例+484 完整版+110 接口/类方法族。
**诚实边界**：本轮未产修复（根因定位阶段交付）——T71 已带完整证据链与精确续作指针，不存在无计划挂账。

### 1.0 收工回填（**476-a 修复阶段完成·✅ T71 根治：类类型回填+间接调用 0 防线双件**·2026-09-20·分支 任务/家机-476-T71组合段错误根治·wt476）

**根因修正与修复实施**：①**根因①修复**=semantic_decl.cpp 变量声明推断回填白名单补 isClassType（46-c 结构体先例同构·原白名单「结构体+字符串」漏类——`变量 计 = 计数器();` 推断 varType=计数器 正确但不回填 node->typeName→IR 按空 typeName 落单 i32 槽+插 ptr→i32 截断 Cast→成员调用走间接 fallback callee=0）；修复后 IR=`分配 %v0 (ptr)`+直接调用 `[计数器$取值]`（无截断/无间接）。②**防线（原计划②）**=ir_verify 新增 verifyCallIndirectTargets（CallIndirect callee=isConstant"0"→「符号解析失败被 0 兜底发射」编译期硬错误）+driver 无条件常开接线（与位宽/操作码检查同族）；语义层类型推断本身正确（471 轮嫌疑=T57 接管分支**排除**——实证与 T57 无关）。③E2E 485（最小回归·输出 0）+486（全形态组合·输出 200/测试串·返回 0 修 runner 判定）双上架+coverage_map 登记。
**关键实验教训（如实记录）**：①定位中误用 ASan 树旧编译器（target/Debug/cn.exe 无修复①）判 g3=139——**双构建树（Ninja target/cn.exe vs VS target/Debug/cn.exe）并存时实验必须核对 exe 时间戳**（旧 449-a「门禁跑在陈旧宿主」同族教训的实验版）；②python heredoc 写 fprintf 的 
 转义两次翻车（真实换行断字符串）——含转义的代码写入用 chr(92) 构造；③「非确定崩溃」原判=误判（单次绿=跑错 exe），本轮连跑口径核实为确定性崩。
**验证链（实测）**：修复后 t71_min rc=0（修前 139）+t71_full **20 连跑 rc=1 稳定**（修前 139×20·输出 200/测试串值正确）+构建零警告（防线加入后全量重验见门禁）。**边缘回填**：①ASan 产物诊断通道=CN_ENABLE_ASAN 只插桩编译器自身，产物 runtime 须 cl /fsanitize 通道（登记待办·本轮未走通）；②runner 判定含进程退出码（返回 非0=FAIL·用例设计须 返回 0）。
> 〔467-a 实施计划已滚转归档（489 集成会话·2026-09-20）〕
### 1.0 收工回填（**467-a 开发轮·✅ T46 根治（甲+丙）：宿主全局 128 位标量静态初始化发射缺失双缺口全消**·基线 20cee0f4·看板先行 4037a6cc·2026-09-20·分支 任务/家机-467-T46全局128静态发射·wt467 隔离）

**实测结论**：①**缺口①（.data 恒零占位）**：三后端 128 分支由硬编码 `dq 0, 0` 改为 initText 非空时经 types::parseInt128InitText 解析发射初值双 quad（低 64 位在前）——win 实测 `?gstatic_a dq 2Ah, 0h`/`?gstatic_b dq 0FFFFFFFFFFFFFFFBh, 0FFFFFFFFFFFFFFFFh`（−5 完整 128 位补码）/i128max/−2^127/0x 前缀/u128max/2^64+1 全对；linux_x64/arm64 .s 指纹 `.quad 0x2A`+`.quad 0x0` 全对（跨后端扫描口径·win 本机 as 不识 ELF=平台固有非缺陷）。②**缺口②（半槽写）**：IR 层注入 StorePtr 128 位传 IR 名（原恒 "ptr"）+win/linux/arm64 StorePtr i128 分支补常量子分支（parseInt 解析·splitI128 的 64 位 stoull 会截断负数符号故不用）；另 IR 层 ±字面量折叠进直存通道（Rust const-eval 同构·零运行时开销·仅 128 位走新折叠=既有 ≤64 位零回归）——−5 实测读出 −5（修前 18446744073709551611=2^64−5）。
**写集回填（实际）**：`src/cn_compiler/semantic/{type_system.hpp,type_system.cpp}`（isInt128Type/isInt128Signed/parseInt128InitText·支持十进制±/0x/0b/0o/lo:hi 常量池形态·超界 false·无符号负数拒绝）｜`src/cn_compiler/ir/ir_decl.cpp`（收集折叠+注入 StorePtr 类型）｜`src/cn_compiler/codegen/x64/{x64_codegen_data.cpp,x64_instructions_mem.cpp}`｜`src/cn_compiler/codegen/linux_x64/{linux_x64_codegen.cpp,linux_x64_instructions.cpp}`｜`src/cn_compiler/codegen/arm64/{arm64_codegen.cpp,arm64_instructions.cpp}`｜`tests/e2e/483_全局128静态初始化/`+`tests/e2e/coverage_map.md`｜`tests/unit/semantic/test_int128_static_init.cpp`+CMakeLists｜m46_01.meta 转正注记｜plans/021+025+教训.md+看板+交接+更新日志。**与计划差**：+StorePtr 常量子分支三处+coverage_map 登记（门禁拦截后补）——计划预判外两件（门禁/单测当场抓取·机制有效）。
**验证链（实测）**：Ninja+sccache 零警告（MSVC /W4 /WX·242/242）+单测 **1369/1369**（新 6 例：类型判定/十进制/前缀/超 int64 大字面量/超范围拒绝/常量池形态）+E2E 全量 **477=477/0/0**（483 单跑 PASS·锚定链含内零回归）+asm 宽度三后端 PASS+m46_01 实弹转正（「静态128小值 = 42」rc=0）。丙=扫描门禁判定：E2E 483 运行级断言即机械化门禁（恒零占位/半槽写每轮全量必转红）·check_asm_width 扩描重复其职责不另建。
**边缘回填**：①超 2^128 字面量→parseInt false→零占位（现状等价不劣化·编译期超界拒绝归 T9 方案 D 辖区·已注记）；②词法层 int64 溢出的大字面量（i128max/2^128−1 实测）词法不拒·发射层文本解析兜住=直存正确（IntegerLiteral::raw=唯一完整信息源·这一设计在本轮被实证为必要）；③**v2 侧同面缺口登记**：v2 代码生成.cn 静态槽「dq 初始值」单 quad+槽 8 字节（128 位槽尺寸不足·源码级定性·深度机 466 在飞 v2 生成层避让·归 v2 欠账族）——E2E 483 暂宿主侧锚定·v2 补齐后双测化；④arm64/linux_x64 运行级复验归跨机轮（平台固有分工）。
**交叠结论**：深度机 466/家机 460/挂起四分支零冲突（写集不同文件）——预期成立。
**事故记录**：开工时主树误执行 reset --hard 丢弃他会话未提交增量（cnsmith_hits 日志）——看板广播（4037a6cc）+教训入册（权重 9·主树禁 reset --hard/窄通道=精确 add 单文件口诀）。

### 1.0 实施计划（**471-a 开发轮·🏃 D25 后续语料轮：ir_verify 错误分支单测全 face+ast_printer 全节点 E2E 语料**·基线 339ba21d→f78e8cf7 看板先行·2026-09-20·分支 任务/家机-471-D25验证器语料·worktree=wt471）

**1. 目标与验收判据**：D25 剩余面（plans/021 §3-D·283-a 首步后续）——①**ir_verify 错误分支路径语料**：ir_verify.cpp（266 行）错误分支全 face 单测直调（verifyFunction 8 面=缺标签/重复标签/块未终止/跳转目标不存在/条件缺 termCondition/真目标不存在/假目标不存在/引用未定义寄存器+verifyConstWidths 2 面=ConstInt extra 超域/内联操作数超域含去重+verifyKnownOpcodes 1 面=未知操作码——11 面逐面构造坏 IR 断言诊断原文）；②**ast_printer 覆盖语料**：`cn ast` 命令全节点触达 E2E 语料（字面量五类/表达式全族/语句全族/类/泛型/联合体/函数指针形态）+打印输出锚定。**验收**=新单测全绿（逐面断言）+新 E2E PASS+全量门禁零新增红；诚实边界=覆盖率数字（gcov=linux 口径）提升由深度机下轮复测确认（win 无 gcov·本轮交付=语料+单测+实跑 PASS 证据）。

**2. 写集（精确文件清单）**：`tests/unit/ir/test_ir_verify_errors.cpp`（新）+`tests/unit/CMakeLists.txt`（注册）｜`tests/e2e/484_ast打印全节点语料/`（主.cn+主.expected）｜`tests/e2e/coverage_map.md`（登记）｜文档六件套。

**3. 实施步骤**：①单测：读 ir_verify.cpp 枚举错误分支（8+2+1=11 面）→IRModule/IRFunction 手工构造各违例形态→断言返回错误消息含函数/块/原因文本→健康 IR 空错误反证；②ast_printer：`cn ast` 对全语法形态源码实跑→输出固化 expected（逐节点形态在列）→472？编号取 484（483 已占）；③门禁+coverage_map 登记。**Rust 对照**：rustc verify 系（mir::verify）错误路径由 ui 测试+单测双面触达——CN 同构（E2E 触达健康面+单测直调错误面）。

**4. 验证链**：零警告构建+单测全量+E2E 全量（gate_lock 串行）+新单测逐面 PASS+新 E2E 单跑 PASS。

**5. 边缘情况与诚实边界**：①错误分支诊断文本为断言面=实现重构时须同轮迁移（先例 437 断言迁移）；②ast 输出格式=调试面非语言契约（.expected 锚定输出=回归锚·格式演进随实现迁移——注记登记）；③覆盖率数字归深度机 gcov 口径（win 无 gcov）；④verifyConstWidths 去重路径（同违例两次只报一次）显式用例。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470（v2 树）零交叠（本轮写集=tests）；家机 468（九链重测集成轮·零代码）零交叠；467（本会话挂等回签·src 写集冻结在分支）零交叠。**

### 1.0 收工回填（**471-a 开发轮·✅ D25 后续语料轮：ir_verify 11 face+ast_printer 5 例单测+484 运行锚+★T71 立案**·基线 339ba21d·看板先行 f78e8cf7·2026-09-20·分支 任务/家机-471-D25验证器语料·wt471 隔离）

**实测结论**：①ir_verify 错误分支单测 **13/13**（verifyFunction 8 面=缺标签/重复标签/未终止/跳转目标不存在/条件缺 termCondition/真假目标不存在/未定义寄存器+verifyConstWidths 2 面=extra 超域/内联操作数超域含块内去重+verifyKnownOpcodes 1 面=未知操作码+健康反证 2 例）——合法 IR 生成+定点变异直调（Rust mir verify 同构）；②ast_printer 节点触达单测 **5/5**（类/结构体/泛型/联合体/结果/表达式族/控制流族）——**缺口实证=cn ast 不遍历顶层静态声明**（printer 覆盖 14.6% 的构成性缺口·登记待 printer 补齐）；③E2E 484（结构体构造字面量+数组初始化列表+泛型实例调用+下标/成员访问·输出 43）**8 连跑稳定**+单跑 PASS。④★**T71 立案**（P1 嫌疑）：全形态组合源码（类显式无参构造实例化+结构体+数组+泛型+循环/选择/三元）编译零诊断、运行**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突）——消融三证据、复现语料 run_err 入库、484 以稳定子集上架。
**写集回填**：tests/unit/ir/test_ir_verify_errors.cpp（新）+tests/unit/parser/test_ast_printer_nodes.cpp（新）+tests/unit/CMakeLists.txt+tests/e2e/484_ast_dump/（主.cn+主.expected）+coverage_map.md+cnsmith_hits/run_err/t71（复现语料+meta）+021/025/看板/交接/更新日志。**与计划差**：ast 语料由 E2E 改单测承载（runner 不支持 ast 命令输出断言·计划预判外机制认知）；484 目录名 ASCII（runner 中文名产物实跑验证过·保守取稳）；+T71 立案（计划外捕获）。
**验证链**：零警告构建+单测 **1381/1381**（本分支基线 1363+ir_verify 13+ast 5）+E2E **477=477/0/0**+asm 宽度 PASS——4/4 全绿（gate_lock 串行）。
**边缘回填**：①runner 断言面=运行 stdout（ast 命令输出须单测承载·机制认知入册）；②ast 输出为调试面（expected 锚定=回归锚·格式演进随实现迁移）；③gcov 覆盖率数字归深度机 linux 口径（本轮交付语料+单测+实跑证据）；④printer 顶层静态缺口登记 D25 行。
**交叠结论**：全零（预期成立）；T71 触发面=宿主局部类对象构造（与深度机 v2 生产线零关）。

### 1.0 实施计划（**458-a 开发轮·🏃 D8/F1-28 第三步=x64 结果写入直写（win 主平台）**·基点=最新 develop 独立集成序〔453 已集成·x64 写集障碍解除〕·2026-09-20·分支 任务/家机-458-x64冗余mov消除·worktree=wt458）

**1. 目标与验收判据**：451/457 两轮已完成 ARM64 面冗余 mov 消除（88637→2125·-97.6%）；本轮移植同模式到 **x64（win 主平台）**——x64 发射模式=`mov rax, op1; op rax, op2; mov dst, rax`（固定临时 rax 计算→搬分配寄存器 dst=r12~r15）。**基线取证=91197 条中转 mov**（正则=`mov r12~r15, rax/eax/rcx/ecx/rdx/edx`·291 用例 win-x64 产物）。**验收实测**：①中转 mov 显著下降（目标 -90%+）；②全量 E2E win 列零新增红（406/431 已随 453 集成清零→预期全绿基线）；③单测全绿+零警告（/W4 /WX）；④asm 宽度 PASS；⑤反证闭环。
**2. 写集**：`src/cn_compiler/codegen/x64/{x64_instructions.cpp,x64_instructions_mem.cpp,x64_instructions_cast.cpp,x64_codegen.cpp,x64_codegen.hpp}`（按需·结果写入点+resultTarget 辅助）｜plans/025 本节｜文档四件套。零 v2 树零 tests 面。**注**：453（437 cherry-pick）已集成 8f5e1cb2——x64_instructions.cpp/x64_codegen_oop.cpp 交叠障碍解除（本轮基于含 437 的新 develop）。
**3. 实施步骤**：①侦察 x64 发射方法结果写入点（emitIntBinary/DivMod/Shift/ConstLoad/LoadStore/Cast/Compare/Not——grep `mov dst, rax` 模式）；②新增 `resultTargetRegX(regId, fallback)` 类辅助（分配→r12~r15 名；未分配→原槽文本）；③逐方法直写改造：**分配态**计算寄存器 rax→dst 分配寄存器（`mov r12, op1; add r12, op2` 免尾部中转）；**未分配态**保持三段式（内存槽不可作运算目的·x86 语义）产物逐字节不变；④**冲突判据**：op2 文本=dst 分配寄存器（分配器死点复用）时退回原三段（x86 两地址 `add r12, r12` 若 op2=r12 且 op1≠r12 则先 mov r12,op1 后 add r12,r12=**错**——op2 值已被覆盖）；⑤verify：MASM 语法（win 产物本地 ml64 可汇编=E2E 真跑覆盖）；⑥验证链+反证。
**4. 验证链**：Ninja+sccache 零警告+单测+E2E 全量 win（gate_lock）+check_asm_width（win 维度）+中转 mov 前后统计+反证（stash 撤改→复现→恢复）。
**5. 边缘与诚实边界**：①x64 分配池=被调用者保存 r12~r15（池小于 arm64 x19~x28）——分配覆盖面比例不同·收益量级以实测为准；②隐含返回指针/结构体返回场景分配器关闭（既有）=零影响；③装载面（`mov rax, x12` 型）=后续轮（459 候选）；④Call ABI 接收/错误路径=必要不可消除；⑤x64 运行级=本机 win 原生（E2E 真跑即运行级·无跨机欠账）；⑥i128 面不动。
**6. 与他机交叠分析**：同机 454（T63 v2 树）/456（ir/ir_oop_call.cpp）零交叠（x64 目录）；456 的写集含 plans/001？看板行=ir_oop_call.cpp+文档——零交叠；深度机 448（v2 树）/455（T62 v2 树）零交叠；单位机 329-D12（v2 树+tests）零交叠。写集=x64 后端（三平台共享层）→三平台 try-build（点名深度机 linux 回签+单位机 arm64 周一补验）。

**收工回填（458-a·实测）**：①**win 冗余 mov 91197→22960 条（-74.8%·291 用例同口径）**——`physRegOf(value)` 辅助（分配→r12~r15 名；未分配空串）+五发射方法直写：emitIntBinary（64 位主路径计算寄存器 rax→dst·8/16 位扩展分支 movsx/movzx 直落 dst 32 位形态·**冲突判据**=op2Text 与 dst 同物理寄存器〔分配器死点复用〕退回原三段——两地址 op dst, dst 先覆盖 op2 原值=错）、emitShift（计算落 dst·cl 固定约束与 dst 无冲突面）、emitConstLoad（64 位整型立即数/字符串 LEA/函数地址 LEA 三分支直装）、emitLoadStore Load（movsx/movzx 扩展直落+64 位单条 mov dst, src〔mem→reg 合法〕·未分配 mem→mem 禁故保持 rax 中转）、emitAddrOf（LEA 直装）；②**必要搬移定性（不改）**：Call 结果接收（rax=ABI·自举链残余 1100 条/用例主体=__cn_str_* 字符串调用密集）、idiv/div 商余固定 eax/edx、i128 双槽、浮点（不分配）、32 位常量装载（既有直写·前人已做）；③门禁：零警告+单测 **1363/1363**+E2E 全量 **464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线·**零新增红**）+asm 宽度 **289×3 PASS**；④**反证闭环**：stash 撤改→探针复现 9 条→恢复→2 条（余=Call 接收必要）+探针 asm 铁证 `add r12, 12` 直写；⑤**诚实边界**：x64 装载面（mov eax, r12d 型·统计口径外）+PtrLoadStore 地址面（[rax] 硬编码多·收益/成本比低）+Cast 细面（xmm0/eax 混合）=后续轮；arm64 运行级 bench=跨机复验轮。
### 1.0 收工回填（**459-a 开发轮·✅ 波 4 首件 206-d：转移表达式位真槽位交接**·基线 eeda4bde·2026-09-20·分支 任务/家机-459-波4转移真move·worktree=wt459）

**1. 目标与验收（回填实测·全达成）**：①**现状矩阵实证**（target/p459/probe.py 五判）：kind 0/2 声明初始化位已是真槽位交接（alloc_diff=0 零拷贝·ir_stmt_decl 浅交接分派既有）——**plans/022「容器声明位=深拷」记载已过时**（如实改判）；真正缺口=kind 2 表达式位拒绝+实参位无源槽清零（双 free 靠 cn_free_tracked 注册表兜底侥幸）。②**改动两处**：语义层 checkTransferCall kind 2 表达式位放行（「仅声明初始化位」拒绝解除·Rust move 语义对照）+IR 转移展开补源槽清零（Store 0——与声明位浅交接分派同构）。③**探针五判全绿**：t4 容器实参位 rc=0（解锁）+四形态 alloc_diff=0（零拷贝）+t5 moved-from 再用编译拒绝（防线保持）。④**IR 铁证**：`加载 %v15 a$15 → 常量整数 %v16 [0] → 存储 %v16 [a$15] → 调用 吃向量`（句柄加载→源槽清零→移交）。⑤**反证**：stash 回退两改动→t4 编译拒绝复现（「无法将 '未知' 隐式转换」·红绿翻转）。⑥E2E 460_转移真槽位交接 上架（四形态矩阵+alloc_diff=0 断言·runner 单跑 PASS）。
**2. 写集（实测）**：`src/cn_compiler/semantic/semantic_call.cpp`（kind 2 放行·-9+6 行）｜`src/cn_compiler/ir/ir_call.cpp`（源槽清零·+19 行）｜`tests/unit/semantic/test_move_check.cpp`（RejectClassTransferAtExprPosition→Accept 断言迁移·459-a 设计变更使拒绝断言过期·437 先例同型·族 13/13 绿）｜`scripts/check_handoff.py`（看板行上限 4→6·多开 5 任务实态）｜`tests/e2e/460_转移真槽位交接/`（主.cn+主.expected 实跑固化）｜`tests/e2e/coverage_map.md`（+1 行）｜plans/021（C3 行波 4 进展+戳 459）｜plans/025 本节｜交接/更新日志本机节｜看板。**零 v2 树**（v2 侧同构=波 4 后续轮）。
**3. 空安全链核实（回填）**：源槽清零后 moved-from 析构全链空安全——字符串 __cn_str_free(nullptr)→cn_free_tracked null 忽略；容器 __cn_vector_free_strings obj==nullptr return（io_api.cpp:177）；类 DeleteObject test/je 空安全（入口零初始化前置）——「释放+清零幂等模型」（74-a 纪律 7）全库同构，本轮=转移语义接入该模型（结构性正确升级·摆脱 tracked 表侥幸依赖）。
**4. 验证链（回填·终态全绿 4/4）**：探针五判+IR 铁证+反证+460 单跑 PASS+**全量门禁 gate_exit=0**=构建零警告+单测 **1363/1363**（断言迁移后）+E2E **465=465/0/0 零失败**（465=464+460 新例·**406/431 基线红已随 453 集成 437 修复清零=win 全量首次恒绿**）+arm64 位宽编码门禁 PASS+asm 宽度 290×3=0/0/0。
**5. 边缘与诚实边界（回填）**：①成员/下标/返回位转移（六位置余下三位置）=波 4 后续轮；②拥有形参（206-e）不在本轮；③v2 侧真 move 同构=后续轮（本轮宿主侧立锚）；④声明位走语义改写（transferDeclSources_）路径不变——本轮仅表达式位接入清零。
**6. 与他机交叠（回填）**：零交叠维持（448=v2 生成类.cn/458=x64 codegen/453=437 x64/451=arm64）。src/semantic+ir 面本轮唯一持有。**收工态**：src 面→try-build 请求点名深度机（集成序 448→447→450→452→456→459）。

### 1.0 实施计划（**459-a 开发轮·🏃 plans/022 波 4 首件（206-d）：转移表达式位真槽位交接（B4 真 move 设施）·2026-09-20·分支 任务/家机-459-波4转移真move·worktree=wt459**）

**1. 目标与验收判据**：`转移(x)` 从「零指令值交接（浅拷语义）」升级为**真槽位交接**——①句柄直拷（拥有型字符串/容器：LoadPtr 源槽句柄→目的槽 StorePtr，替代现有深拷/复制调用路径）；②**源槽清零**（moved-from：源槽写 0=所有权移出标记）；③**析构跳过已转移槽**（双释放结构性消除）；④moved-from 全链拦截（已转移源再用=编译期拒绝——语义层 169 已有「已转移检查」，补槽位级）。**验收**=转移六位置零拷贝实证（分配增量 0 探针·分配计数 __cn_alloc 计数器）+析构跳过反证（注入失败即 double free 须被门禁捕获）+性能锚（大容器转发路径分配 N→0）+全量门禁零新增红+全树零误伤（stdlib/v2 树/ tests 零回归）。
**2. 写集（预估·收工回填精确清单）**：`src/cn_compiler/semantic/`（转移调用标记面·semantic.cpp/semantic_call.cpp）｜`src/cn_compiler/ir/`（真槽位交接发射·ir_call.cpp/ir_expr*.cpp/析构注入面 ir_oop.cpp）｜`tests/e2e/<新用例>`（零拷贝探针 E2E 化+反证）｜plans/021/025/交接/更新日志/看板。**零 v2 树**（v2 侧同构=波 4 后续轮）。
**3. 实施步骤**：①现状矩阵侦察（转移六位置×现有行为：声明初始化位/表达式位/实参位/返回位/成员位/下标位——每位置分配计数实测）；②设计定稿（槽位交接协议：哪些类型〔拥有型字符串/容器/有析构类〕×哪些位置）；③实施（语义层标记→IR 发射→析构跳过）；④探针四判+反证；⑤全量门禁；⑥try-build。
**4. 验证链**：分配计数探针（六位置矩阵）+反证双件（析构跳过失败=double free 须捕获/moved-from 再用=编译期拒绝）+全量门禁（构建零警告+单测+E2E 零新增红）+锚定链（v2 树不含本轮改动=fix_p≡fix_s 不变·或如实重锚）。
**5. 边缘情况与诚实边界**：①波 4 大工程——本轮=表达式位+实参位优先（声明初始化位 102-a 已有句柄交接雏形），成员/下标/返回位如实登记后续轮；②拥有形参（206-e）不在本轮；③v2 侧真 move=后续轮（本轮宿主侧先立锚）；④若侦察发现现状已是真交接（注释与 plans/022 记载矛盾）→如实改判+登记差异。
**6. 与他机交叠**：深度机 448=v2 生成类.cn（零交叠·本轮零 v2 树）；455 已收工；同机 458=x64 codegen/453=437 cherry-pick（x64）（semantic/ir 零交叠）；451=arm64 codegen（零交叠）。**同机门禁串行**（gate_lock）。

### 1.0 实施计划（**453-a 集成轮·🏃 437 拆分集成：src r10d 修复+断言迁移 cherry-pick——清 406/431 win 基线红解锁 450**·基线 0d9a6259·2026-09-20·分支 任务/家机-453-437拆分集成·wt453）

**1. 目标与验收**：435 链集成的 win 门禁基线红 406/431（A2070 族·T43+T67）阻塞 450/447 集成链；437-a 修复在 447 分支（挂起等 448）——**拆分集成**：437 的 src 两文件（447 ASCII 修复版=paramSlot）+vtable 断言迁移单独成轮先行集成（§8.4 机制级·437 内容=深度机已签 4fe54df 组成部分）。**验收**=X64VtableTest 6/6+406/431 单跑双转绿+全量门禁零新增红（预期全绿）。
**2. 写集（实测）**：`src/cn_compiler/codegen/x64/{x64_codegen_oop.cpp,x64_instructions.cpp}`（447 版=437+paramSlot）｜`tests/unit/codegen/test_x64_vtable.cpp`（447 版=断言迁移）｜025 本节+看板。**零 v2 树**（与深度机 448-b 零交叠）。
**3. 实施步骤**：①checkout 447 版三文件→②构建+单测+406/431 单跑验证→③push→④integrate --try-build-done（437 内容=深度机已签链尾 4fe54df 组成·回签有效）。
**4. 验证链（实测）**：X64VtableTest 6/6 ✓+406 PASS ✓+431 PASS ✓（wt453 全新构建）。
**5. 边缘**：447 分支本体保持挂起（112_v2 红=独立面·归 448-b）；本拆分不预判 435 链其他潜在红（wt-base 硬证据=仅 406/431 两红）。
**6. 交叠**：深度机 448-b（v2 树 IR类.cn）零交叠；449（scripts）零交叠；450（tests 上架）零交叠。**集成后动作**：450 解锁（立即集成）→447 待 448-b。
### 1.0 收工回填（**456-a 开发轮·✅ D1 拆分：handleClassCallExpr 421 行→分派器+四调用族**·基线 017bab76·2026-09-20·分支 任务/家机-456-D1拆分·worktree=wt456）

**1. 目标与验收（回填实测·全达成）**：①handleClassCallExpr（ir_oop_call.cpp 421 行·58 超百行之首）拆为**主分派器 22 行+9 个族函数**（tryInterfaceMethodCall 接口族/emitConstructorCall 构造族〔resolveGenericCtorInstanceName 泛型实例名+findCtorMember 构造查找+emitCtorInvoke 发射〕/emitMemberMethodCall 分派+emitStaticMethodCall 静态三态/emitInstanceMethodCall 实例〔emitVirtualMethodCall+emitDirectMethodCall〕）——全函数 ≤100 行；②**等价性铁证=v2p.asm 拆分前后逐字节一致**（0683ee1e==0683ee1e·宿主 IR 生成零变更的最强证据；obj/exe md5 差异=内嵌产物路径名/时间戳非内容）；③超百行 58→57·ir_oop_call 出清（新首列=v2 树 编码算术Win 206·深度机面）。
**2. 写集（实测）**：`src/cn_compiler/ir/ir_oop_call.cpp`（+310/-183）｜`src/cn_compiler/ir/ir.hpp`（ClassInfo 前向声明+10 方法声明·六要素漏估头文件——如实修正）｜plans/021（D1 行+戳 456）｜plans/025 本节｜交接/更新日志本机节｜看板。**零 v2 树零 tests 零 runner**。
**3. 实施要点（回填）**：①python 行号切片手术（边界断言 14 处防漂移）+注释逐字保留+缩进机械降级；②**控制流语义三态保持**：接口段未命中 fall-through/静态段 return false=交回原路径（非 fall-through！三态返回 1/0/-1 表达）/构造与实例段 false=交回原路径；③手术三失误当场修：SEG_A 漏切 L373 if 壳关闭（C2601 连锁=嵌套定义信号）+C4/C5 段 void 化残留 return true+resolve 的 const 参数被赋值改值传参。
**4. 验证链（回填实测·终态）**：Ninja /W4 /WX 零警告+v2p.asm 逐字节一致+check_fn_length 57 且 ir_oop_call 出清+**全量门禁（gate_lock 含锁全链·wt456）**=单测 **1363/1363**+E2E **464=462/2/0**（2 红=406/431 基线既有同签名·**零新增红**·78_v2/79_v2 自举链双 PASS=锚定链 fix_p≡fix_s 在拆分后宿主下照常）+asm 宽度 289×3=0/0/0（E2E 红中止后单独补跑）。
**5. 边缘与诚实边界（回填）**：①obj/exe md5 不作等价判据（路径/时间戳内嵌）；②静态辅助函数未做匿名命名空间——全为私有成员函数（需访问 semantic_/发射设施），符号面=成员函数不污染；③362 行（021 旧口径）→421 行（本轮实测）差异=337/353 链集成增量（D10/D11 根治扩入）——如实记录。
**6. 与他机交叠（回填）**：零交叠维持（448=v2 树 IR类/455=v2 语法面/451=codegen arm64/453=437 cherry-pick x64+vtable 单测）。**ir.hpp 为本轮新增写集**（三机共享头——集成序需与 447/450/452 串行；435 链已集成的 ir.hpp 版本=merge-base 干净）。**收工态**：src 面=三平台共享层→try-build 请求点名深度机（集成序 448→447→450→452→456）。

### 1.0 实施计划（**456-a 开发轮·🏃 D1 拆分滚动：handleClassCallExpr 421 行族提取（ir_oop_call.cpp:296-716）·纯机械重构零行为变更·2026-09-20·分支 任务/家机-456-D1拆分·worktree=wt456**）

**1. 目标与验收判据**：D1 首列 handleClassCallExpr 421 行（check_fn_length 实测 58 超百行之首）拆至 ≤100 行——族提取（构造分派/重载消歧/泛型实例/容器/字符串拼接等调用族各成静态/成员辅助函数，签名传参禁引用捕获悬挂）。**验收（等价性硬证据·零行为变更）**：①锚定链 fix_p≡fix_s 逐字节一致（v2p 产物 md5 前后对照=宿主重构不改行为的最强证据）；②全量门禁零新增红（构建零警告+单测 1363+E2E 464=462/2/0 基线红 406/431 同签名）+拆分后 check_fn_length 该函数消失。
**2. 写集（精确清单）**：`src/cn_compiler/ir/ir_oop_call.cpp`（唯一代码文件）｜`plans/021`（D1 行更新+文首戳 456）｜`plans/025` 本节｜`交接.md`/`更新日志.md` 本机节｜看板。**零 v2 树零 tests 零 runner**。
**3. 实施步骤**：①wt456 构建基线产物（锚定链 md5 对照基线）；②handleClassCallExpr 全文精读+调用族分块（按功能边界切族）；③族提取（每族一辅助函数·先提取后调用点替换·每步可编译）；④函数主体收敛为「分派器」（≤100 行）；⑤构建+锚定链 md5 对照（必须逐字节同）；⑥全量门禁（gate_lock 排队）；⑦try-build 请求（src 面=三平台共享层→点名深度机）。
**4. 验证链**：锚定链 fix_p≡fix_s+md5 逐字节+全量门禁+check_fn_length 复测（handleClassCallExpr 出清·58→57）。
**5. 边缘情况与诚实边界**：①纯重构不改变任何 IR 生成逻辑——若锚定链 md5 变化=引入行为变更，必须回查（拆分引入的求值顺序/短路/初始化顺序差异）；②静态辅助函数须匿名命名空间或类静态（防符号污染）；③362 行的 021 旧口径与本轮 421 实测差异=其他轮增量（如实记录）。
**6. 与他机交叠**：深度机 448=v2 树 IR/IR类.cn+455=v2 语法面（零交叠）；同机 451=codegen/arm64（零交叠）；同机 453=437 cherry-pick（x64_codegen_oop/x64_instructions+vtable 单测·零交叠）；同机 452（本会话前轮）=tests 面（已完成收工）。**ir_oop_call.cpp 全库唯一持有人=本轮**。

> 〔449-a 节已滚转归档（456 代集成会话·2026-09-20）〕
### 1.1 （⏸ 待定区·2026-09-16 用户令暂缓：plans/026 体系优先，视情况再启动）案戊阶段 1——type_system 下沉 types/ 基础层 + 静态库物理边界 + 层依赖门禁（T2 用户批准案戊）＋并行 **V2 编译器优化检查**（只读）

> **状态（2026-09-16 更正）**：计划已落盘（六要素见下）；**阶段 1 代码改动曾试做（提交 67127bc）、经
> 77c0ae4 revert 撤下、未入 develop**（验证链未跑）；**实施与收口（check_layer_deps 门禁 + 验证链 +
> 文档六件套）待后续机器按本计划认领执行**。并行项 V2 优化检查已完成·产出=plans/021 §三-G2（待排班）。

**1. 目标与验收判据**

- **案戊阶段 1**（开发轮）：`type_system.{hpp,cpp}`（559 行，被 45 文件跨层消费）从 `semantic/` 下沉到
  `types/` 基础层；CMake 拆独立静态库（链接方向物理固化）；新增 `scripts/check_layer_deps.py` 层依赖门禁接入 ci.ps1。
  验收=零警告构建 + 单测全过 + 全量 E2E 全绿 + **锚定链逐字节不变**（234-a 新基线 410460 行）。
- **V2 优化检查**（只读探针）：v2p 自编译计时/产物指令统计/算法模式静态审查，优化机会落 plans/021 待定项。

**2. 写集（精确清单）**

- `src/cn_compiler/types/type_system.hpp`、`src/cn_compiler/types/type_system.cpp`（git mv 自 semantic/）
- 45 处 `#include cn_compiler/semantic/type_system.*` → `cn_compiler/types/type_system.*`（ir 18/semantic 19/codegen 6/parser 2）
- `CMakeLists.txt`（types 静态库 target + 语义库链接 types）
- `scripts/check_layer_deps.py`（新增）+ `scripts/ci.ps1`（接入）
- 文档六件套（本节/HANDOFF 家机节/更新日志/plans/021/看板/lessons 如有坑）

**3. 实施步骤**

1. `git mv` 两文件 → types/；批量改写 45 处 include 路径（python 正则改写，逐文件核对计数）；
2. CMakeLists：新增 `cn_types` 静态库（type_system.cpp）+ `cn_semantic`（或现聚合目标）链接它——保持产物二进制等价；
3. `check_layer_deps.py`：①types/ 不得 include parser/semantic/ir/codegen ②parser 不得 include semantic.hpp（type_system 除外——阶段 2 收口）③层 3 过程目录互不 include（按阶段渐进启用）；
4. 验证链全跑；5. V2 优化检查（v2p 计时/指令统计/静态审查）。

**4. 验证链**：清场重建零警告 → 单测 → 全量 E2E（313 用例口径）→ 锚定链逐字节比对（410460 基线）
→ check_layer_deps 反例自检（构造违规 include 确认拦截）→ 双文档门禁。

**5. 边缘情况与诚实边界（开工预判）**：① 234-a 刚改 v2 树+stdlib 并重锚 410460——本轮基线取其上，
   锚定链比对用新基线；② 45 处 include 中 semantic 目录内部 19 处为「同库内搬移」性质（不跨层），
   仍统一改写保一致；③ CMake 若现结构为单聚合目标，则先建 types 库+目标间链接，不动其余聚合结构
   （阶段 2~4 逐层拆）；④ check_layer_deps 首版规则面从窄（types 纯净性+parser 白名单），阶段 2~4 扩。

**6. 与他机交叠分析**：写集=src/cn_compiler 头文件路径+CMake+scripts——与深度机（234-a 已收工·下轮
候选=读取文件行 uninit/D1）、单位机（mem2reg/D8/F2-33·codegen 与 reg_alloc 面）**写集不相交**；
锚定链口径同步用 234-a 新基线，语义零变化（纯搬移），他机无需复跑。

---
**收工回填（483-a·2026-09-20·全实测）**：①侦察定性反转：458 后残余主体非「槽装载」（仅 674 条·0.17%）
而是 **test/cmp 前装载中转 19869+4808 条**（RA 开启下操作数多已分配寄存器）+FieldAddr 偏移 `mov rcx,off` 中转
（立即数装载 74919 构成主力）——五发射面直读落地：**A** 条件跳转 `test Rw,Rw` 直读／**B** 比较	op1 直读
（物理寄存器；槽/常量保守不直读）／**C** 逻辑非 op 直读+**结果直写补 458 漏点**（movzx dstPhys）／**D** FieldAddr
基址直读+偏移 lea 直编码（**已分配**路径·LLVM 寻址同款）／**E** PtrLoadStore 地址直读（[rax]→[addrReg] 全路径）。
②量化：win 全用例口径（340 用例 `cn compile` .asm·recon 脚本 target/recon483/ 不入库）**395234→361895
（-33339·-8.44%）**——reg→reg -28345+立即数 -4994（lea 偏移直编码消 `mov rcx,off`）；槽装载 674 不变（未触碰 ✓）。
③红线：未分配 fallback 逐字节不变**严格执行**（FieldAddr 未分配态保持 `mov rcx+add rax` 原形态·混合态先拷贝
rax 基址只读；单测 X64StructTest.FieldAddrEmit 锚定未分配形态——**测试文件零改动**）。
④**三缺陷当场根治（实施中 E2E 实录·§8.4 机制级随证据备案）**：α FieldAddr `add addrReg,off` 污染仍存活基址
vreg（p.a/p.b 第二次寻址读到累加地址→01_hello v2p 段错误·反面实录；修复=lea 只读基址+探针 60 正确正面）；
β lea 以槽为目的=A2000 ×100（v2 组件编译 rc=1·修复=未分配经 rax 中转）；γ StorePtr i128 非常量高位段
`[rax+8]` 漏改 addrReg=野写 8B（420/433/440/14 四例段错误·修复后四例转绿）——**教训：批量文本替换须按
指令变体穷举（[rax]/[rax+8]/带注释变体）+发射形态改动必须 i128 双槽路径全形态过筛**。
⑤验证链：零警告 Ninja 构建+单测 1369/1369+全量 E2E --jobs 8 零新增红+asm 宽度三后端 PASS+反证三闭环（αβγ
各反面实录/修复转绿）+探针 target/recon483/反证字段寻址/ 输出 60 正确。
⑥诚实边界：call 实参 ABI 装载与链式中转未动（458 定性必要不改·自拷贝实测 0 条）；cmp 槽操作数直读未做
（尺寸前缀+宽度语义面·候选下轮）；-O0 性能无收益（未分配路径原形态）——本轮收益面=-O2 RA 开启态。
⑦try-build：写集触 src→三平台点名（深度机 482 在飞可应答·单位机周一补验后补签）。

### 1.0 实施计划（**495-a 开发轮·🏃 G 区 T10 收口=字符串子串越界口径文档化**·基线 a09d10ec·看板先行 a09d10ec·2026-09-20·分支 `任务/家机-495-t10口径`·wt495 隔离）
### 1.0 实施计划（**489-a 开发轮·🏃 T87：58 泛型类方法返回 结果<T,E> v2 生成缺口根治**·基线 2fb1aba3→6482fac3 看板先行·2026-09-20·分支 任务/家机-489-T87泛型方法返回结果·worktree=wt479tb 复用改名）

**1. 目标与验收判据**：T87（482 探针轮立案·T35 真残余）——`tests/e2e/58_generic_field` 泛型类（馆藏）方法返回 `结果<空类型,整32>` →v2 生成「ir2 用户类方法返回结构体/容器/结果暂不支持」+错误码 6（诊断语义混乱：拒绝走运行时错误通道）。**根因面**（482 定位）=v2 生成层类方法 retbuf 形态缺失（结果<T,E>=结构体布局·标志@0+载荷·与宿主 296-a 结果布局/459 波4 同族）。**验收**=58 双编译转正上架（v2 产物与宿主对照一致·退出码/输出同）+新增负测形态锚（若拒绝路径保留则诊断改编译期语义错误）+全量门禁零新增红+自举链 PASS。

**2. 写集（精确文件清单）**：侦察后回填（嫌疑面=CN语言编译器v2/ 的 方法调用生成/返回值段/IR生成类型.cn）+tests/e2e/58（expected 双测化）+coverage_map+文档六件套。

**3. 实施步骤**：①复现（wt476 target/p482/v2p.exe 已重建·直接跑 58 采完整诊断）；②侦察 v2 树方法返回生成代码（「暂不支持」诊断抛点+retbuf 设施现状——**先查 296-a/459 结果布局族是否已给 v2 侧留挂点**）；③修复=类方法返回 结构体/容器/结果 走隐藏返回指针 retbuf（宿主情形B structReturn 同构）；④58 双测转正+负测锚；⑤门禁全量。**Rust 对照**：Result<T,E> 返回=sret 隐藏指针 ABI（rustc 同构）。

**4. 验证链**：58 双编译对照 PASS+全量门禁（gate_lock 串行·零警告+单测+E2E）+自举链 PASS+asm 指纹。

**5. 边缘情况与诚实边界**：①「暂不支持」诊断抛点若在 ir2 验证器=生成前置拒——修复=生成支持而非删检查（检查改为防御断言）；②泛型类方法=单态化实例方法的返回（实例方法链）——与 D42 波2/3 交互面侦察确认；③480 T69（fnptr）已并入 develop——若同文件交叠 rebase 解；④arm64/linux 复验归跨机。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 486/488（T75 指针下标结构体元素·v2 生成层）——**同树不同面**（T75=下标地址推导/T87=方法返回 retbuf）·文件交叠可能在 生成表达式/方法调用 段——**开工前 git fetch 复查 488 分支写集**，若同文件则改在分支接力（459 先例）或错峰；家机 481（021 清账·零代码）零交叠。

### 1.0 阶段回填（**489-a 侦察阶段完成·修复实施续轮**·2026-09-20）

**侦察结论（修复模式全部在手）**：①抛点=生成类.cn `发射用户类方法调用`——聚合返回（查结构体大小!=0‖容器‖结果）显式拒绝（第一版边界注释明示）；②**v2 已有完整 retbuf 设施可套**：容器方法发射（生成表达式求值.cn 440-540）=「retbuf=retbuf序号（帧尾缓冲槽·上层传入）+实参起始位=2（位0=retbuf/位1=this）+IR_调用参数化 带 retbuf 字段+B6 累计retbuf字节数（帧尺寸回填·IR容器.cn 162）+结构体返回 物化返回值到独立槽」；③被调方待查项=用户类方法本体的 `返回 聚合表达式` 生成（生成类.cn 方法 Return 段——需写 retbuf 指针指向内存；容器方法本体=stdlib 由宿主编译不经此路径）；④语义层无恙（拒绝发生在 IR 生成层）。
**修复实施清单（续轮照单）**：①发射用户类方法调用 加 retbuf序号 形参（调用点链上层传——查上层分派与 retbuf序号 产生点）+聚合分支（retbuf 实参起始位=2+B6 登记+目标寄存器=retbuf 地址+表达式栈压 retbuf〔成员访问 .正常/.值 走字段偏移〕）；②被调方方法本体 Return 聚合写 retbuf；③「暂不支持」诊断改防御断言；④58 双测转正+expected 固化。
**诚实边界**：489-a=侦察交付（修复清单完整·不属挂账——482 定位→489 侦察→489-b 实施的分轮推进·每轮闭环）。

### 1.0 实施回填（**489-b 修复实施完成·✅ T87 v2p 编译通过（rc 1→0）**·2026-09-20·头 7e3c9dd0）

**修复落地**：①生成类.cn 发射用户类方法调用 补聚合返回 retbuf 分支（签名加 retbuf序号·实参起始位=2·B6 累计·目标寄存器=retbuf 地址压栈）；②生成表达式求值.cn 调用点 retbuf序号 透传。**被调方排除**=生成方法 已实现（返回结构体ID/结果名ID!=0→槽计数=2·Return 聚合写走 生成语句.cn 通用 retbuf 路径）——缺口唯调用方。
**验证（实测）**：**v2p 编译 58 rc 1→0**（修前 ir2 拒绝+错误码 6·修后全链零诊断）——T35 判据「v2p 编译通过」达成。**运行级验证 ✅（本机补齐·诚实边界解除）**：供给链卡点定位=v2p.obj（stdlib 源码级并入编译产物·E2E 链 633-636 行既有设计）——手动链 `/FORCE:MULTIPLE`+v2p.obj 供给（206-c 记忆同款）→**58 v2 产物运行输出与宿主逐行一致**（添加全部成功/数量 3/ID 101/202/303）+rc=0——retbuf 路径运行级正确实锤。T87 验收完整达成（编译 rc 1→0+运行级一致双证）。**流程教训**：双构建树/多分支并存时 compile 前必须核对分支态（本段 v2asm.asm=489 态编译·后续误切 452 分支致回填报错=同「实验核对 exe 时间戳」教训的分支版）。

### 1.0 实施计划（**481-a 开发轮·🏃 plans/021 清账轮：§3 已销项条目清账+结构修复+门禁防复发**·基线 25a8b9b7·看板先行 25a8b9b7·2026-09-20·分支 `任务/家机-481-021清账`）

**1. 目标与验收判据**：G 区 T10「字符串子串越界参数静默截断」（`字符串子串(s,2,99)` 对 3 字节串返回
"c" 无诊断——安全方向但无文档口径）处置=「随诊断轮顺带定文档口径」。本轮=①行为全集实测（起越界/
长度溢出/负值/空串/边界恰等）②宿主实现确认（截断语义唯一归属点）③规范文档口径落盘（cn-language-spec
skills specs 02-类型系统字符串节·行为文档化**非语义变更**——T10 处置已批准）④021 G 区 T10 销项。
**验收**：探针矩阵实测表+规范文档口径条文+021 销项台账行+双文档门禁 PASS。

**2. 写集（精确文件清单）**：`.zcode/skills/cn-language-spec/specs/02-类型系统.md`（字符串内置函数
口径节）｜`plans/021`（G 区 T10 销项迁 §4.2+文首戳）｜`plans/025` 本节+§〇本机行｜看板本机行（窄通道）｜
`交接.md`/`更新日志.md` 本机节。**零 src/ 零 tests/**——纯文档=快速门禁。

**3. 实施步骤**：①探针实测矩阵（起=0/中/越界×长度=0/恰好/溢出/负值×空串）②grep 宿主 子串 实现
（runtime/内置表）确认截断归属与实现一致性③spec 02 字符串节落口径条文（截断语义+边界定义+例）④
021 销项+文档六件套。

**4. 验证链**：探针实锤+`python scripts/check_handoff.py`+`python scripts/check_progress_sync.py`
+写集核对。快速门禁。

**5. 边缘情况与诚实边界（预判）**：①若实测发现「截断」之外的形态（负值回绕/崩溃）→机制级立案不混
本轮②spec 双拷贝（.zcode 与 .agents junction）单一事实源=.zcode③行为变更（如改 Rust 式 Option/panic）
不在本轮——如需另行语义级呈报。

**6. 与他机交叠分析**：纯文档轮零代码交叠；深度机 480/484/493（v2 树）与家机 483/494（挂等回签）
零文件交叠。spec specs 属三机共享契约——**行为文档化**（无契约面变化）照 §8.6 广播口径在收工时说明。

> 〔481-a 实施计划已滚转归档（471 集成会话·2026-09-20·清账轮已集成 git 可考）〕
### 1.0 实施计划（**476-a 开发轮·🏃 T71 专项根治：类无参构造实例化组合非确定段错误**·基线 e5011e75·看板先行 e2d5f9cf·2026-09-20·分支 任务/家机-476-T71组合段错误根治·worktree=wt476）

**1. 目标与验收判据**：T71（471-a 立案·plans/021 §3-G）——全形态组合源码（类显式无参构造实例化+结构体构造字面量+局部数组+泛型调用+循环/选择/三元）编译零诊断、运行同一产物**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突 UB）。消融三证据：去类+结构体绿（H）/去结构体单次绿后连跑崩（I）/结构体+数组+泛型最小组合 8 连跑绿（k/l）——触发面=类无参构造实例化与组合。**根治路径（Rust 对照=UB 先 ASan 定位再修·不求幸存）**：①**ASan 构建**（-DCN_ENABLE_ASAN=ON·229-a 已接入）编译复现语料→产物 ASan 运行直出 UB 报告（堆栈=越界/未初始化精确位置）——预期一步锁定根因；②最小复现收敛（逐形态增删至最小触发集）；③按根因修复（嫌疑面=局部类对象构造的槽分配/构造注入顺序/数组槽相邻踩踏）；④复现语料 **20 连跑全绿**+全形态版（484 完整版）转稳定+全量门禁零新增红。**验收**=ASan 报告归零+复现语料 20 连跑绿+全形态版 20 连跑绿+单测/E2E 全量绿+asm 反证（根因点前后指纹）。

**2. 写集（精确文件清单）**：以 ASan 定位结果为准（嫌疑面=src/cn_compiler 语义/IR 层类构造注入+代码生成槽分配）+tests/e2e（484 扩形态或新用例）+cnsmith_hits（T71 转正注记）+文档六件套。开工侦察后回填精确清单。

**3. 实施步骤**：①ASan 树构建（cmake -DCN_ENABLE_ASAN=ON·独立 build 目录）+复现语料编译运行采报告；②报告→根因定位（asm 三件套对照：崩点前后产物 diff）；③最小复现收敛（供回归用例）；④修复+反证（回退复现/修复消失）；⑤484 完整版转正评估+全量门禁；⑥T71 行销项+run_err 转正注记。**Rust 对照**：rustc 对 internal UB 的态度=ASan/UBSan 先行定位、修根因、加回归测试——同构。

**4. 验证链**：ASan 报告归零+复现语料 20 连跑绿+全形态 20 连跑绿+零警告构建+单测全量+E2E 全量（gate_lock 串行）+asm 指纹。

**5. 边缘情况与诚实边界**：①非确定崩溃的验证口径=连跑计数（20 次起）——单次绿不算绿（T71 铁证）；②ASan 树产物与生产锚定链无关（独立 build 目录·不影响默认构建）；③若 ASan 不报（栈读未初始化 ASan 弱项）→备选=MSVC /RTCs 或产物 diff 槽分析；④arm64/linux 侧复验归跨机轮。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470/474/475（v2 树/D46）零交叠（T71=宿主 src）；家机 468（九链重测集成·零代码）零交叠；471（挂等回签·写集=tests 冻结）——本轮写集=src+tests 新用例，与 471 的 coverage_map/用例编号可能相邻（484 已占·新用例取 485+）。

### 1.0 阶段回填（**476-a 根因定位阶段完成·修复实施续下轮**·2026-09-20）

**已完成**：①ASan 树构建（target/build-asan·需补 clang_rt.asan_dynamic DLL 至 exe 旁；产物 runtime 不吃 CN_ENABLE_ASAN=ASan 只插桩编译器自身——产物诊断须另走 cl /fsanitize 产物旗标通道·登记）；②**最小复现定案**（t71_min.cn=类无参构造+方法调用两要素·确定性崩·与数组/泛型/结构体无关）；③**定性勘误**：原「非确定」=单次绿误判（实为确定性 call 0）；④**asm/IR 双铁证**：IR `间接调用 %v5 0`（callee=常量 0·无 FuncAddr）→asm `lea r12,[rbp-608]`（this=变量槽地址·应读出对象指针）+`mov r11,0; call r11`；⑤语义层类型推断排除（checkCtorCall lastType_=className 正确回填）；⑥根因锁定=ir_oop_call.cpp handleClassCallExpr 某分支对「类实例方法调用」误发 CallIndirect（callee=0 兜底+this 装载形态错）——**嫌疑=T57 接管分支重设计（472 集成）改动面**（T57=objdel+写穿根治·触及方法调用接管分支）；⑦T71 行完整落盘 021 G 区（含勘误）。
**下一步（续轮精确指针）**：①读 handleClassCallExpr 情形B/C（类方法直接调用段）确认哪分支漏判（「计」类型=计数器 应走直接调用 emitClassMethodCall·为何进间接分支）——注意 472 集成的 T57 改动 diff（git show 359cd573 附近）；②修复=误判分支修正+callee=0 硬错误防线（T11 面②同款·IR verify 扩 CallIndirect callee 非零常量检查）；③t71_min 入 E2E（485）+T71 复现语料 20 连跑绿验收；④候选回归面=T57 三例+484 完整版+110 接口/类方法族。
**诚实边界**：本轮未产修复（根因定位阶段交付）——T71 已带完整证据链与精确续作指针，不存在无计划挂账。

### 1.0 收工回填（**476-a 修复阶段完成·✅ T71 根治：类类型回填+间接调用 0 防线双件**·2026-09-20·分支 任务/家机-476-T71组合段错误根治·wt476）

**根因修正与修复实施**：①**根因①修复**=semantic_decl.cpp 变量声明推断回填白名单补 isClassType（46-c 结构体先例同构·原白名单「结构体+字符串」漏类——`变量 计 = 计数器();` 推断 varType=计数器 正确但不回填 node->typeName→IR 按空 typeName 落单 i32 槽+插 ptr→i32 截断 Cast→成员调用走间接 fallback callee=0）；修复后 IR=`分配 %v0 (ptr)`+直接调用 `[计数器$取值]`（无截断/无间接）。②**防线（原计划②）**=ir_verify 新增 verifyCallIndirectTargets（CallIndirect callee=isConstant"0"→「符号解析失败被 0 兜底发射」编译期硬错误）+driver 无条件常开接线（与位宽/操作码检查同族）；语义层类型推断本身正确（471 轮嫌疑=T57 接管分支**排除**——实证与 T57 无关）。③E2E 485（最小回归·输出 0）+486（全形态组合·输出 200/测试串·返回 0 修 runner 判定）双上架+coverage_map 登记。
**关键实验教训（如实记录）**：①定位中误用 ASan 树旧编译器（target/Debug/cn.exe 无修复①）判 g3=139——**双构建树（Ninja target/cn.exe vs VS target/Debug/cn.exe）并存时实验必须核对 exe 时间戳**（旧 449-a「门禁跑在陈旧宿主」同族教训的实验版）；②python heredoc 写 fprintf 的 
 转义两次翻车（真实换行断字符串）——含转义的代码写入用 chr(92) 构造；③「非确定崩溃」原判=误判（单次绿=跑错 exe），本轮连跑口径核实为确定性崩。
**验证链（实测）**：修复后 t71_min rc=0（修前 139）+t71_full **20 连跑 rc=1 稳定**（修前 139×20·输出 200/测试串值正确）+构建零警告（防线加入后全量重验见门禁）。**边缘回填**：①ASan 产物诊断通道=CN_ENABLE_ASAN 只插桩编译器自身，产物 runtime 须 cl /fsanitize 通道（登记待办·本轮未走通）；②runner 判定含进程退出码（返回 非0=FAIL·用例设计须 返回 0）。
> 〔467-a 实施计划已滚转归档（489 集成会话·2026-09-20）〕
### 1.0 收工回填（**467-a 开发轮·✅ T46 根治（甲+丙）：宿主全局 128 位标量静态初始化发射缺失双缺口全消**·基线 20cee0f4·看板先行 4037a6cc·2026-09-20·分支 任务/家机-467-T46全局128静态发射·wt467 隔离）

**实测结论**：①**缺口①（.data 恒零占位）**：三后端 128 分支由硬编码 `dq 0, 0` 改为 initText 非空时经 types::parseInt128InitText 解析发射初值双 quad（低 64 位在前）——win 实测 `?gstatic_a dq 2Ah, 0h`/`?gstatic_b dq 0FFFFFFFFFFFFFFFBh, 0FFFFFFFFFFFFFFFFh`（−5 完整 128 位补码）/i128max/−2^127/0x 前缀/u128max/2^64+1 全对；linux_x64/arm64 .s 指纹 `.quad 0x2A`+`.quad 0x0` 全对（跨后端扫描口径·win 本机 as 不识 ELF=平台固有非缺陷）。②**缺口②（半槽写）**：IR 层注入 StorePtr 128 位传 IR 名（原恒 "ptr"）+win/linux/arm64 StorePtr i128 分支补常量子分支（parseInt 解析·splitI128 的 64 位 stoull 会截断负数符号故不用）；另 IR 层 ±字面量折叠进直存通道（Rust const-eval 同构·零运行时开销·仅 128 位走新折叠=既有 ≤64 位零回归）——−5 实测读出 −5（修前 18446744073709551611=2^64−5）。
**写集回填（实际）**：`src/cn_compiler/semantic/{type_system.hpp,type_system.cpp}`（isInt128Type/isInt128Signed/parseInt128InitText·支持十进制±/0x/0b/0o/lo:hi 常量池形态·超界 false·无符号负数拒绝）｜`src/cn_compiler/ir/ir_decl.cpp`（收集折叠+注入 StorePtr 类型）｜`src/cn_compiler/codegen/x64/{x64_codegen_data.cpp,x64_instructions_mem.cpp}`｜`src/cn_compiler/codegen/linux_x64/{linux_x64_codegen.cpp,linux_x64_instructions.cpp}`｜`src/cn_compiler/codegen/arm64/{arm64_codegen.cpp,arm64_instructions.cpp}`｜`tests/e2e/483_全局128静态初始化/`+`tests/e2e/coverage_map.md`｜`tests/unit/semantic/test_int128_static_init.cpp`+CMakeLists｜m46_01.meta 转正注记｜plans/021+025+教训.md+看板+交接+更新日志。**与计划差**：+StorePtr 常量子分支三处+coverage_map 登记（门禁拦截后补）——计划预判外两件（门禁/单测当场抓取·机制有效）。
**验证链（实测）**：Ninja+sccache 零警告（MSVC /W4 /WX·242/242）+单测 **1369/1369**（新 6 例：类型判定/十进制/前缀/超 int64 大字面量/超范围拒绝/常量池形态）+E2E 全量 **477=477/0/0**（483 单跑 PASS·锚定链含内零回归）+asm 宽度三后端 PASS+m46_01 实弹转正（「静态128小值 = 42」rc=0）。丙=扫描门禁判定：E2E 483 运行级断言即机械化门禁（恒零占位/半槽写每轮全量必转红）·check_asm_width 扩描重复其职责不另建。
**边缘回填**：①超 2^128 字面量→parseInt false→零占位（现状等价不劣化·编译期超界拒绝归 T9 方案 D 辖区·已注记）；②词法层 int64 溢出的大字面量（i128max/2^128−1 实测）词法不拒·发射层文本解析兜住=直存正确（IntegerLiteral::raw=唯一完整信息源·这一设计在本轮被实证为必要）；③**v2 侧同面缺口登记**：v2 代码生成.cn 静态槽「dq 初始值」单 quad+槽 8 字节（128 位槽尺寸不足·源码级定性·深度机 466 在飞 v2 生成层避让·归 v2 欠账族）——E2E 483 暂宿主侧锚定·v2 补齐后双测化；④arm64/linux_x64 运行级复验归跨机轮（平台固有分工）。
**交叠结论**：深度机 466/家机 460/挂起四分支零冲突（写集不同文件）——预期成立。
**事故记录**：开工时主树误执行 reset --hard 丢弃他会话未提交增量（cnsmith_hits 日志）——看板广播（4037a6cc）+教训入册（权重 9·主树禁 reset --hard/窄通道=精确 add 单文件口诀）。

### 1.0 实施计划（**471-a 开发轮·🏃 D25 后续语料轮：ir_verify 错误分支单测全 face+ast_printer 全节点 E2E 语料**·基线 339ba21d→f78e8cf7 看板先行·2026-09-20·分支 任务/家机-471-D25验证器语料·worktree=wt471）

**1. 目标与验收判据**：D25 剩余面（plans/021 §3-D·283-a 首步后续）——①**ir_verify 错误分支路径语料**：ir_verify.cpp（266 行）错误分支全 face 单测直调（verifyFunction 8 面=缺标签/重复标签/块未终止/跳转目标不存在/条件缺 termCondition/真目标不存在/假目标不存在/引用未定义寄存器+verifyConstWidths 2 面=ConstInt extra 超域/内联操作数超域含去重+verifyKnownOpcodes 1 面=未知操作码——11 面逐面构造坏 IR 断言诊断原文）；②**ast_printer 覆盖语料**：`cn ast` 命令全节点触达 E2E 语料（字面量五类/表达式全族/语句全族/类/泛型/联合体/函数指针形态）+打印输出锚定。**验收**=新单测全绿（逐面断言）+新 E2E PASS+全量门禁零新增红；诚实边界=覆盖率数字（gcov=linux 口径）提升由深度机下轮复测确认（win 无 gcov·本轮交付=语料+单测+实跑 PASS 证据）。

**2. 写集（精确文件清单）**：`tests/unit/ir/test_ir_verify_errors.cpp`（新）+`tests/unit/CMakeLists.txt`（注册）｜`tests/e2e/484_ast打印全节点语料/`（主.cn+主.expected）｜`tests/e2e/coverage_map.md`（登记）｜文档六件套。

**3. 实施步骤**：①单测：读 ir_verify.cpp 枚举错误分支（8+2+1=11 面）→IRModule/IRFunction 手工构造各违例形态→断言返回错误消息含函数/块/原因文本→健康 IR 空错误反证；②ast_printer：`cn ast` 对全语法形态源码实跑→输出固化 expected（逐节点形态在列）→472？编号取 484（483 已占）；③门禁+coverage_map 登记。**Rust 对照**：rustc verify 系（mir::verify）错误路径由 ui 测试+单测双面触达——CN 同构（E2E 触达健康面+单测直调错误面）。

**4. 验证链**：零警告构建+单测全量+E2E 全量（gate_lock 串行）+新单测逐面 PASS+新 E2E 单跑 PASS。

**5. 边缘情况与诚实边界**：①错误分支诊断文本为断言面=实现重构时须同轮迁移（先例 437 断言迁移）；②ast 输出格式=调试面非语言契约（.expected 锚定输出=回归锚·格式演进随实现迁移——注记登记）；③覆盖率数字归深度机 gcov 口径（win 无 gcov）；④verifyConstWidths 去重路径（同违例两次只报一次）显式用例。收工回填：〔待填〕

**6. 与他机交叠分析**：深度机 469/470（v2 树）零交叠（本轮写集=tests）；家机 468（九链重测集成轮·零代码）零交叠；467（本会话挂等回签·src 写集冻结在分支）零交叠。**

### 1.0 收工回填（**471-a 开发轮·✅ D25 后续语料轮：ir_verify 11 face+ast_printer 5 例单测+484 运行锚+★T71 立案**·基线 339ba21d·看板先行 f78e8cf7·2026-09-20·分支 任务/家机-471-D25验证器语料·wt471 隔离）

**实测结论**：①ir_verify 错误分支单测 **13/13**（verifyFunction 8 面=缺标签/重复标签/未终止/跳转目标不存在/条件缺 termCondition/真假目标不存在/未定义寄存器+verifyConstWidths 2 面=extra 超域/内联操作数超域含块内去重+verifyKnownOpcodes 1 面=未知操作码+健康反证 2 例）——合法 IR 生成+定点变异直调（Rust mir verify 同构）；②ast_printer 节点触达单测 **5/5**（类/结构体/泛型/联合体/结果/表达式族/控制流族）——**缺口实证=cn ast 不遍历顶层静态声明**（printer 覆盖 14.6% 的构成性缺口·登记待 printer 补齐）；③E2E 484（结构体构造字面量+数组初始化列表+泛型实例调用+下标/成员访问·输出 43）**8 连跑稳定**+单跑 PASS。④★**T71 立案**（P1 嫌疑）：全形态组合源码（类显式无参构造实例化+结构体+数组+泛型+循环/选择/三元）编译零诊断、运行**非确定 139/0 交替**（ASLR 特征=未初始化读/槽冲突）——消融三证据、复现语料 run_err 入库、484 以稳定子集上架。
**写集回填**：tests/unit/ir/test_ir_verify_errors.cpp（新）+tests/unit/parser/test_ast_printer_nodes.cpp（新）+tests/unit/CMakeLists.txt+tests/e2e/484_ast_dump/（主.cn+主.expected）+coverage_map.md+cnsmith_hits/run_err/t71（复现语料+meta）+021/025/看板/交接/更新日志。**与计划差**：ast 语料由 E2E 改单测承载（runner 不支持 ast 命令输出断言·计划预判外机制认知）；484 目录名 ASCII（runner 中文名产物实跑验证过·保守取稳）；+T71 立案（计划外捕获）。
**验证链**：零警告构建+单测 **1381/1381**（本分支基线 1363+ir_verify 13+ast 5）+E2E **477=477/0/0**+asm 宽度 PASS——4/4 全绿（gate_lock 串行）。
**边缘回填**：①runner 断言面=运行 stdout（ast 命令输出须单测承载·机制认知入册）；②ast 输出为调试面（expected 锚定=回归锚·格式演进随实现迁移）；③gcov 覆盖率数字归深度机 linux 口径（本轮交付语料+单测+实跑证据）；④printer 顶层静态缺口登记 D25 行。
**交叠结论**：全零（预期成立）；T71 触发面=宿主局部类对象构造（与深度机 v2 生产线零关）。

### 1.0 实施计划（**458-a 开发轮·🏃 D8/F1-28 第三步=x64 结果写入直写（win 主平台）**·基点=最新 develop 独立集成序〔453 已集成·x64 写集障碍解除〕·2026-09-20·分支 任务/家机-458-x64冗余mov消除·worktree=wt458）

**1. 目标与验收判据**：451/457 两轮已完成 ARM64 面冗余 mov 消除（88637→2125·-97.6%）；本轮移植同模式到 **x64（win 主平台）**——x64 发射模式=`mov rax, op1; op rax, op2; mov dst, rax`（固定临时 rax 计算→搬分配寄存器 dst=r12~r15）。**基线取证=91197 条中转 mov**（正则=`mov r12~r15, rax/eax/rcx/ecx/rdx/edx`·291 用例 win-x64 产物）。**验收实测**：①中转 mov 显著下降（目标 -90%+）；②全量 E2E win 列零新增红（406/431 已随 453 集成清零→预期全绿基线）；③单测全绿+零警告（/W4 /WX）；④asm 宽度 PASS；⑤反证闭环。
**2. 写集**：`src/cn_compiler/codegen/x64/{x64_instructions.cpp,x64_instructions_mem.cpp,x64_instructions_cast.cpp,x64_codegen.cpp,x64_codegen.hpp}`（按需·结果写入点+resultTarget 辅助）｜plans/025 本节｜文档四件套。零 v2 树零 tests 面。**注**：453（437 cherry-pick）已集成 8f5e1cb2——x64_instructions.cpp/x64_codegen_oop.cpp 交叠障碍解除（本轮基于含 437 的新 develop）。
**3. 实施步骤**：①侦察 x64 发射方法结果写入点（emitIntBinary/DivMod/Shift/ConstLoad/LoadStore/Cast/Compare/Not——grep `mov dst, rax` 模式）；②新增 `resultTargetRegX(regId, fallback)` 类辅助（分配→r12~r15 名；未分配→原槽文本）；③逐方法直写改造：**分配态**计算寄存器 rax→dst 分配寄存器（`mov r12, op1; add r12, op2` 免尾部中转）；**未分配态**保持三段式（内存槽不可作运算目的·x86 语义）产物逐字节不变；④**冲突判据**：op2 文本=dst 分配寄存器（分配器死点复用）时退回原三段（x86 两地址 `add r12, r12` 若 op2=r12 且 op1≠r12 则先 mov r12,op1 后 add r12,r12=**错**——op2 值已被覆盖）；⑤verify：MASM 语法（win 产物本地 ml64 可汇编=E2E 真跑覆盖）；⑥验证链+反证。
**4. 验证链**：Ninja+sccache 零警告+单测+E2E 全量 win（gate_lock）+check_asm_width（win 维度）+中转 mov 前后统计+反证（stash 撤改→复现→恢复）。
**5. 边缘与诚实边界**：①x64 分配池=被调用者保存 r12~r15（池小于 arm64 x19~x28）——分配覆盖面比例不同·收益量级以实测为准；②隐含返回指针/结构体返回场景分配器关闭（既有）=零影响；③装载面（`mov rax, x12` 型）=后续轮（459 候选）；④Call ABI 接收/错误路径=必要不可消除；⑤x64 运行级=本机 win 原生（E2E 真跑即运行级·无跨机欠账）；⑥i128 面不动。
**6. 与他机交叠分析**：同机 454（T63 v2 树）/456（ir/ir_oop_call.cpp）零交叠（x64 目录）；456 的写集含 plans/001？看板行=ir_oop_call.cpp+文档——零交叠；深度机 448（v2 树）/455（T62 v2 树）零交叠；单位机 329-D12（v2 树+tests）零交叠。写集=x64 后端（三平台共享层）→三平台 try-build（点名深度机 linux 回签+单位机 arm64 周一补验）。

**收工回填（458-a·实测）**：①**win 冗余 mov 91197→22960 条（-74.8%·291 用例同口径）**——`physRegOf(value)` 辅助（分配→r12~r15 名；未分配空串）+五发射方法直写：emitIntBinary（64 位主路径计算寄存器 rax→dst·8/16 位扩展分支 movsx/movzx 直落 dst 32 位形态·**冲突判据**=op2Text 与 dst 同物理寄存器〔分配器死点复用〕退回原三段——两地址 op dst, dst 先覆盖 op2 原值=错）、emitShift（计算落 dst·cl 固定约束与 dst 无冲突面）、emitConstLoad（64 位整型立即数/字符串 LEA/函数地址 LEA 三分支直装）、emitLoadStore Load（movsx/movzx 扩展直落+64 位单条 mov dst, src〔mem→reg 合法〕·未分配 mem→mem 禁故保持 rax 中转）、emitAddrOf（LEA 直装）；②**必要搬移定性（不改）**：Call 结果接收（rax=ABI·自举链残余 1100 条/用例主体=__cn_str_* 字符串调用密集）、idiv/div 商余固定 eax/edx、i128 双槽、浮点（不分配）、32 位常量装载（既有直写·前人已做）；③门禁：零警告+单测 **1363/1363**+E2E 全量 **464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线·**零新增红**）+asm 宽度 **289×3 PASS**；④**反证闭环**：stash 撤改→探针复现 9 条→恢复→2 条（余=Call 接收必要）+探针 asm 铁证 `add r12, 12` 直写；⑤**诚实边界**：x64 装载面（mov eax, r12d 型·统计口径外）+PtrLoadStore 地址面（[rax] 硬编码多·收益/成本比低）+Cast 细面（xmm0/eax 混合）=后续轮；arm64 运行级 bench=跨机复验轮。
### 1.0 收工回填（**459-a 开发轮·✅ 波 4 首件 206-d：转移表达式位真槽位交接**·基线 eeda4bde·2026-09-20·分支 任务/家机-459-波4转移真move·worktree=wt459）

**1. 目标与验收（回填实测·全达成）**：①**现状矩阵实证**（target/p459/probe.py 五判）：kind 0/2 声明初始化位已是真槽位交接（alloc_diff=0 零拷贝·ir_stmt_decl 浅交接分派既有）——**plans/022「容器声明位=深拷」记载已过时**（如实改判）；真正缺口=kind 2 表达式位拒绝+实参位无源槽清零（双 free 靠 cn_free_tracked 注册表兜底侥幸）。②**改动两处**：语义层 checkTransferCall kind 2 表达式位放行（「仅声明初始化位」拒绝解除·Rust move 语义对照）+IR 转移展开补源槽清零（Store 0——与声明位浅交接分派同构）。③**探针五判全绿**：t4 容器实参位 rc=0（解锁）+四形态 alloc_diff=0（零拷贝）+t5 moved-from 再用编译拒绝（防线保持）。④**IR 铁证**：`加载 %v15 a$15 → 常量整数 %v16 [0] → 存储 %v16 [a$15] → 调用 吃向量`（句柄加载→源槽清零→移交）。⑤**反证**：stash 回退两改动→t4 编译拒绝复现（「无法将 '未知' 隐式转换」·红绿翻转）。⑥E2E 460_转移真槽位交接 上架（四形态矩阵+alloc_diff=0 断言·runner 单跑 PASS）。
**2. 写集（实测）**：`src/cn_compiler/semantic/semantic_call.cpp`（kind 2 放行·-9+6 行）｜`src/cn_compiler/ir/ir_call.cpp`（源槽清零·+19 行）｜`tests/unit/semantic/test_move_check.cpp`（RejectClassTransferAtExprPosition→Accept 断言迁移·459-a 设计变更使拒绝断言过期·437 先例同型·族 13/13 绿）｜`scripts/check_handoff.py`（看板行上限 4→6·多开 5 任务实态）｜`tests/e2e/460_转移真槽位交接/`（主.cn+主.expected 实跑固化）｜`tests/e2e/coverage_map.md`（+1 行）｜plans/021（C3 行波 4 进展+戳 459）｜plans/025 本节｜交接/更新日志本机节｜看板。**零 v2 树**（v2 侧同构=波 4 后续轮）。
**3. 空安全链核实（回填）**：源槽清零后 moved-from 析构全链空安全——字符串 __cn_str_free(nullptr)→cn_free_tracked null 忽略；容器 __cn_vector_free_strings obj==nullptr return（io_api.cpp:177）；类 DeleteObject test/je 空安全（入口零初始化前置）——「释放+清零幂等模型」（74-a 纪律 7）全库同构，本轮=转移语义接入该模型（结构性正确升级·摆脱 tracked 表侥幸依赖）。
**4. 验证链（回填·终态全绿 4/4）**：探针五判+IR 铁证+反证+460 单跑 PASS+**全量门禁 gate_exit=0**=构建零警告+单测 **1363/1363**（断言迁移后）+E2E **465=465/0/0 零失败**（465=464+460 新例·**406/431 基线红已随 453 集成 437 修复清零=win 全量首次恒绿**）+arm64 位宽编码门禁 PASS+asm 宽度 290×3=0/0/0。
**5. 边缘与诚实边界（回填）**：①成员/下标/返回位转移（六位置余下三位置）=波 4 后续轮；②拥有形参（206-e）不在本轮；③v2 侧真 move 同构=后续轮（本轮宿主侧立锚）；④声明位走语义改写（transferDeclSources_）路径不变——本轮仅表达式位接入清零。
**6. 与他机交叠（回填）**：零交叠维持（448=v2 生成类.cn/458=x64 codegen/453=437 x64/451=arm64）。src/semantic+ir 面本轮唯一持有。**收工态**：src 面→try-build 请求点名深度机（集成序 448→447→450→452→456→459）。

### 1.0 实施计划（**459-a 开发轮·🏃 plans/022 波 4 首件（206-d）：转移表达式位真槽位交接（B4 真 move 设施）·2026-09-20·分支 任务/家机-459-波4转移真move·worktree=wt459**）

**1. 目标与验收判据**：`转移(x)` 从「零指令值交接（浅拷语义）」升级为**真槽位交接**——①句柄直拷（拥有型字符串/容器：LoadPtr 源槽句柄→目的槽 StorePtr，替代现有深拷/复制调用路径）；②**源槽清零**（moved-from：源槽写 0=所有权移出标记）；③**析构跳过已转移槽**（双释放结构性消除）；④moved-from 全链拦截（已转移源再用=编译期拒绝——语义层 169 已有「已转移检查」，补槽位级）。**验收**=转移六位置零拷贝实证（分配增量 0 探针·分配计数 __cn_alloc 计数器）+析构跳过反证（注入失败即 double free 须被门禁捕获）+性能锚（大容器转发路径分配 N→0）+全量门禁零新增红+全树零误伤（stdlib/v2 树/ tests 零回归）。
**2. 写集（预估·收工回填精确清单）**：`src/cn_compiler/semantic/`（转移调用标记面·semantic.cpp/semantic_call.cpp）｜`src/cn_compiler/ir/`（真槽位交接发射·ir_call.cpp/ir_expr*.cpp/析构注入面 ir_oop.cpp）｜`tests/e2e/<新用例>`（零拷贝探针 E2E 化+反证）｜plans/021/025/交接/更新日志/看板。**零 v2 树**（v2 侧同构=波 4 后续轮）。
**3. 实施步骤**：①现状矩阵侦察（转移六位置×现有行为：声明初始化位/表达式位/实参位/返回位/成员位/下标位——每位置分配计数实测）；②设计定稿（槽位交接协议：哪些类型〔拥有型字符串/容器/有析构类〕×哪些位置）；③实施（语义层标记→IR 发射→析构跳过）；④探针四判+反证；⑤全量门禁；⑥try-build。
**4. 验证链**：分配计数探针（六位置矩阵）+反证双件（析构跳过失败=double free 须捕获/moved-from 再用=编译期拒绝）+全量门禁（构建零警告+单测+E2E 零新增红）+锚定链（v2 树不含本轮改动=fix_p≡fix_s 不变·或如实重锚）。
**5. 边缘情况与诚实边界**：①波 4 大工程——本轮=表达式位+实参位优先（声明初始化位 102-a 已有句柄交接雏形），成员/下标/返回位如实登记后续轮；②拥有形参（206-e）不在本轮；③v2 侧真 move=后续轮（本轮宿主侧先立锚）；④若侦察发现现状已是真交接（注释与 plans/022 记载矛盾）→如实改判+登记差异。
**6. 与他机交叠**：深度机 448=v2 生成类.cn（零交叠·本轮零 v2 树）；455 已收工；同机 458=x64 codegen/453=437 cherry-pick（x64）（semantic/ir 零交叠）；451=arm64 codegen（零交叠）。**同机门禁串行**（gate_lock）。

### 1.0 实施计划（**453-a 集成轮·🏃 437 拆分集成：src r10d 修复+断言迁移 cherry-pick——清 406/431 win 基线红解锁 450**·基线 0d9a6259·2026-09-20·分支 任务/家机-453-437拆分集成·wt453）

**1. 目标与验收**：435 链集成的 win 门禁基线红 406/431（A2070 族·T43+T67）阻塞 450/447 集成链；437-a 修复在 447 分支（挂起等 448）——**拆分集成**：437 的 src 两文件（447 ASCII 修复版=paramSlot）+vtable 断言迁移单独成轮先行集成（§8.4 机制级·437 内容=深度机已签 4fe54df 组成部分）。**验收**=X64VtableTest 6/6+406/431 单跑双转绿+全量门禁零新增红（预期全绿）。
**2. 写集（实测）**：`src/cn_compiler/codegen/x64/{x64_codegen_oop.cpp,x64_instructions.cpp}`（447 版=437+paramSlot）｜`tests/unit/codegen/test_x64_vtable.cpp`（447 版=断言迁移）｜025 本节+看板。**零 v2 树**（与深度机 448-b 零交叠）。
**3. 实施步骤**：①checkout 447 版三文件→②构建+单测+406/431 单跑验证→③push→④integrate --try-build-done（437 内容=深度机已签链尾 4fe54df 组成·回签有效）。
**4. 验证链（实测）**：X64VtableTest 6/6 ✓+406 PASS ✓+431 PASS ✓（wt453 全新构建）。
**5. 边缘**：447 分支本体保持挂起（112_v2 红=独立面·归 448-b）；本拆分不预判 435 链其他潜在红（wt-base 硬证据=仅 406/431 两红）。
**6. 交叠**：深度机 448-b（v2 树 IR类.cn）零交叠；449（scripts）零交叠；450（tests 上架）零交叠。**集成后动作**：450 解锁（立即集成）→447 待 448-b。
### 1.0 收工回填（**456-a 开发轮·✅ D1 拆分：handleClassCallExpr 421 行→分派器+四调用族**·基线 017bab76·2026-09-20·分支 任务/家机-456-D1拆分·worktree=wt456）

**1. 目标与验收（回填实测·全达成）**：①handleClassCallExpr（ir_oop_call.cpp 421 行·58 超百行之首）拆为**主分派器 22 行+9 个族函数**（tryInterfaceMethodCall 接口族/emitConstructorCall 构造族〔resolveGenericCtorInstanceName 泛型实例名+findCtorMember 构造查找+emitCtorInvoke 发射〕/emitMemberMethodCall 分派+emitStaticMethodCall 静态三态/emitInstanceMethodCall 实例〔emitVirtualMethodCall+emitDirectMethodCall〕）——全函数 ≤100 行；②**等价性铁证=v2p.asm 拆分前后逐字节一致**（0683ee1e==0683ee1e·宿主 IR 生成零变更的最强证据；obj/exe md5 差异=内嵌产物路径名/时间戳非内容）；③超百行 58→57·ir_oop_call 出清（新首列=v2 树 编码算术Win 206·深度机面）。
**2. 写集（实测）**：`src/cn_compiler/ir/ir_oop_call.cpp`（+310/-183）｜`src/cn_compiler/ir/ir.hpp`（ClassInfo 前向声明+10 方法声明·六要素漏估头文件——如实修正）｜plans/021（D1 行+戳 456）｜plans/025 本节｜交接/更新日志本机节｜看板。**零 v2 树零 tests 零 runner**。
**3. 实施要点（回填）**：①python 行号切片手术（边界断言 14 处防漂移）+注释逐字保留+缩进机械降级；②**控制流语义三态保持**：接口段未命中 fall-through/静态段 return false=交回原路径（非 fall-through！三态返回 1/0/-1 表达）/构造与实例段 false=交回原路径；③手术三失误当场修：SEG_A 漏切 L373 if 壳关闭（C2601 连锁=嵌套定义信号）+C4/C5 段 void 化残留 return true+resolve 的 const 参数被赋值改值传参。
**4. 验证链（回填实测·终态）**：Ninja /W4 /WX 零警告+v2p.asm 逐字节一致+check_fn_length 57 且 ir_oop_call 出清+**全量门禁（gate_lock 含锁全链·wt456）**=单测 **1363/1363**+E2E **464=462/2/0**（2 红=406/431 基线既有同签名·**零新增红**·78_v2/79_v2 自举链双 PASS=锚定链 fix_p≡fix_s 在拆分后宿主下照常）+asm 宽度 289×3=0/0/0（E2E 红中止后单独补跑）。
**5. 边缘与诚实边界（回填）**：①obj/exe md5 不作等价判据（路径/时间戳内嵌）；②静态辅助函数未做匿名命名空间——全为私有成员函数（需访问 semantic_/发射设施），符号面=成员函数不污染；③362 行（021 旧口径）→421 行（本轮实测）差异=337/353 链集成增量（D10/D11 根治扩入）——如实记录。
**6. 与他机交叠（回填）**：零交叠维持（448=v2 树 IR类/455=v2 语法面/451=codegen arm64/453=437 cherry-pick x64+vtable 单测）。**ir.hpp 为本轮新增写集**（三机共享头——集成序需与 447/450/452 串行；435 链已集成的 ir.hpp 版本=merge-base 干净）。**收工态**：src 面=三平台共享层→try-build 请求点名深度机（集成序 448→447→450→452→456）。

### 1.0 实施计划（**456-a 开发轮·🏃 D1 拆分滚动：handleClassCallExpr 421 行族提取（ir_oop_call.cpp:296-716）·纯机械重构零行为变更·2026-09-20·分支 任务/家机-456-D1拆分·worktree=wt456**）

**1. 目标与验收判据**：D1 首列 handleClassCallExpr 421 行（check_fn_length 实测 58 超百行之首）拆至 ≤100 行——族提取（构造分派/重载消歧/泛型实例/容器/字符串拼接等调用族各成静态/成员辅助函数，签名传参禁引用捕获悬挂）。**验收（等价性硬证据·零行为变更）**：①锚定链 fix_p≡fix_s 逐字节一致（v2p 产物 md5 前后对照=宿主重构不改行为的最强证据）；②全量门禁零新增红（构建零警告+单测 1363+E2E 464=462/2/0 基线红 406/431 同签名）+拆分后 check_fn_length 该函数消失。
**2. 写集（精确清单）**：`src/cn_compiler/ir/ir_oop_call.cpp`（唯一代码文件）｜`plans/021`（D1 行更新+文首戳 456）｜`plans/025` 本节｜`交接.md`/`更新日志.md` 本机节｜看板。**零 v2 树零 tests 零 runner**。
**3. 实施步骤**：①wt456 构建基线产物（锚定链 md5 对照基线）；②handleClassCallExpr 全文精读+调用族分块（按功能边界切族）；③族提取（每族一辅助函数·先提取后调用点替换·每步可编译）；④函数主体收敛为「分派器」（≤100 行）；⑤构建+锚定链 md5 对照（必须逐字节同）；⑥全量门禁（gate_lock 排队）；⑦try-build 请求（src 面=三平台共享层→点名深度机）。
**4. 验证链**：锚定链 fix_p≡fix_s+md5 逐字节+全量门禁+check_fn_length 复测（handleClassCallExpr 出清·58→57）。
**5. 边缘情况与诚实边界**：①纯重构不改变任何 IR 生成逻辑——若锚定链 md5 变化=引入行为变更，必须回查（拆分引入的求值顺序/短路/初始化顺序差异）；②静态辅助函数须匿名命名空间或类静态（防符号污染）；③362 行的 021 旧口径与本轮 421 实测差异=其他轮增量（如实记录）。
**6. 与他机交叠**：深度机 448=v2 树 IR/IR类.cn+455=v2 语法面（零交叠）；同机 451=codegen/arm64（零交叠）；同机 453=437 cherry-pick（x64_codegen_oop/x64_instructions+vtable 单测·零交叠）；同机 452（本会话前轮）=tests 面（已完成收工）。**ir_oop_call.cpp 全库唯一持有人=本轮**。

> 〔449-a 节已滚转归档（456 代集成会话·2026-09-20）〕
### 1.1 （⏸ 待定区·2026-09-16 用户令暂缓：plans/026 体系优先，视情况再启动）案戊阶段 1——type_system 下沉 types/ 基础层 + 静态库物理边界 + 层依赖门禁（T2 用户批准案戊）＋并行 **V2 编译器优化检查**（只读）

> **状态（2026-09-16 更正）**：计划已落盘（六要素见下）；**阶段 1 代码改动曾试做（提交 67127bc）、经
> 77c0ae4 revert 撤下、未入 develop**（验证链未跑）；**实施与收口（check_layer_deps 门禁 + 验证链 +
> 文档六件套）待后续机器按本计划认领执行**。并行项 V2 优化检查已完成·产出=plans/021 §三-G2（待排班）。

**1. 目标与验收判据**

- **案戊阶段 1**（开发轮）：`type_system.{hpp,cpp}`（559 行，被 45 文件跨层消费）从 `semantic/` 下沉到
  `types/` 基础层；CMake 拆独立静态库（链接方向物理固化）；新增 `scripts/check_layer_deps.py` 层依赖门禁接入 ci.ps1。
  验收=零警告构建 + 单测全过 + 全量 E2E 全绿 + **锚定链逐字节不变**（234-a 新基线 410460 行）。
- **V2 优化检查**（只读探针）：v2p 自编译计时/产物指令统计/算法模式静态审查，优化机会落 plans/021 待定项。

**2. 写集（精确清单）**

- `src/cn_compiler/types/type_system.hpp`、`src/cn_compiler/types/type_system.cpp`（git mv 自 semantic/）
- 45 处 `#include cn_compiler/semantic/type_system.*` → `cn_compiler/types/type_system.*`（ir 18/semantic 19/codegen 6/parser 2）
- `CMakeLists.txt`（types 静态库 target + 语义库链接 types）
- `scripts/check_layer_deps.py`（新增）+ `scripts/ci.ps1`（接入）
- 文档六件套（本节/HANDOFF 家机节/更新日志/plans/021/看板/lessons 如有坑）

**3. 实施步骤**

1. `git mv` 两文件 → types/；批量改写 45 处 include 路径（python 正则改写，逐文件核对计数）；
2. CMakeLists：新增 `cn_types` 静态库（type_system.cpp）+ `cn_semantic`（或现聚合目标）链接它——保持产物二进制等价；
3. `check_layer_deps.py`：①types/ 不得 include parser/semantic/ir/codegen ②parser 不得 include semantic.hpp（type_system 除外——阶段 2 收口）③层 3 过程目录互不 include（按阶段渐进启用）；
4. 验证链全跑；5. V2 优化检查（v2p 计时/指令统计/静态审查）。

**4. 验证链**：清场重建零警告 → 单测 → 全量 E2E（313 用例口径）→ 锚定链逐字节比对（410460 基线）
→ check_layer_deps 反例自检（构造违规 include 确认拦截）→ 双文档门禁。

**5. 边缘情况与诚实边界（开工预判）**：① 234-a 刚改 v2 树+stdlib 并重锚 410460——本轮基线取其上，
   锚定链比对用新基线；② 45 处 include 中 semantic 目录内部 19 处为「同库内搬移」性质（不跨层），
   仍统一改写保一致；③ CMake 若现结构为单聚合目标，则先建 types 库+目标间链接，不动其余聚合结构
   （阶段 2~4 逐层拆）；④ check_layer_deps 首版规则面从窄（types 纯净性+parser 白名单），阶段 2~4 扩。

**6. 与他机交叠分析**：写集=src/cn_compiler 头文件路径+CMake+scripts——与深度机（234-a 已收工·下轮
候选=读取文件行 uninit/D1）、单位机（mem2reg/D8/F2-33·codegen 与 reg_alloc 面）**写集不相交**；
锚定链口径同步用 234-a 新基线，语义零变化（纯搬移），他机无需复跑。

---

## 二、深度机 linux-x86_64 节（本机维护）

> 〔2.103 T80-host 侦察/定位轮（**宿主侧「类内结构体字段」缺陷**＝最小复现+gdb+反汇编定性·零行为变更） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.100 T70 B1 三层剩余层**侦察/定位轮**（②元素钩子注入设施缺 ③127 映射链查找段错误 ④228/441/442 v2p 失败快 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.87 F1-26 侦察与设计轮（**真 mem2reg/真值传播**＝256-a 方案 A 续篇·实施前定稿） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.85 D46 第三段（**赋值位构造名裸名拒绝**＝`r = 正常;` 检查赋值语句单点） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕

### 2.127 T33 残余根治（**v2 窄宽值规范化四站点＋T36 v2 面字面量值域检查**＝宿主/v2 双编译分叉实锤矩阵）——565 开发轮（分支 `任务/深度机-565-t33窄宽回绕`·基点=develop 715f4e8a·wt565 隔离·2026-09-21）

**0. 立案证据（探针矩阵·宿主=target/cn vs v2=wt562 v2p·2026-09-21 全部实测）**：A 族（窄宽值入槽不规范化→静默错值）：op6 `整8 丙=整8(100)+整8(100)` 宿主 **-56**/v2 **200**；op7 `正16 60000+10000` 宿主 **4464**/v2 **70000**；b5 复合赋值 `甲+=整16(30000)` 宿主 **-5536**/v2 **60000**；b1 传参 `取(甲+乙)`（形参 整16）宿主 **-5536**/v2 **60000**；b2 返回位（->整16）宿主 **-5536**/v2 **60000**；h1 `整32 甲=4000000000` 宿主 **-294967296**/v2 **4000000000**；h5 `整32=6000000000` 宿主 **1705032704**/v2 **6000000000**。B 族（漏拒·宿主拒/v2 放行）：g1 `整16 甲=40000`、g2 赋值位 `甲=40000`、g4 `甲=0+40000`、g5 `正8=300`、g9 `整8=-200`、g10 `正8=-1`、h6 `正32=5000000000` 宿主全拒·v2 全放行。**已正确面（反证不误伤清单）**：显式强转（p3 六形态 ✓）、数组元素（b4 ✓）、结构体字段（c3 ✓·须显式构造器）、a1 变量窄化拒（双侧一致 ✓）、w32/正32 位与回绕 ✓、h2 整64 大字面量 ✓、f1/f2 整32 传参/返回 ✓、h7 整16=-32768 ✓。

**1. 目标与验收判据**：v2 对窄宽（整8/正8/整16/正16/整32）值汇（变量槽/形参槽/返回位）的写入必须规范化到目标宽度（回绕+符号扩展·Rust i16 wrapping 赋值同款语义），字面量对窄宽目标的隐式转换须值域检查（宿主 T36·319-a 同口径）。**验收**=①探针矩阵 A 族七例双侧逐行一致（v2 转绿）；②B 族七例 v2p 编译期拒绝（诊断含「无法将 '整32' 隐式转换为」同宿主措辞形态）；③新 E2E **502_窄宽回绕矩阵**（双编译对照·宿主=v2 逐行全等）+**503_窄宽字面量域外拒绝**（v2 负测·v2p 拒通道）上架单跑 PASS；④全量门禁零新增红+自举链 78/79 PASS+strict/matrix；⑤反证双态（撤 A 规范化→502 复现分叉→恢复复绿）。

**2. 写集（精确文件清单·预登记）**：`CN语言编译器v2/IR/生成声明.cn`（+窄宽值规范化 helper·:232 调用点换接）｜`CN语言编译器v2/IR/生成语句赋值.cn`（:121 调用点换接）｜`CN语言编译器v2/IR/生成函数.cn`（:163 区 返回窄宽码登记+形参槽入口规范化发射）｜`CN语言编译器v2/IR/IR容器.cn`（函数IR +返回窄宽码字段）｜`CN语言编译器v2/IR/生成语句.cn`（生成返回语句 标量返回规范化）｜`CN语言编译器v2/语义/语义检查表达式.cn`（隐式转换合法 字面量分支值域检查+负号字面量形态）｜`tests/e2e/502_窄宽回绕矩阵/`（新·双编译）｜`tests/e2e/503_窄宽字面量域外拒绝/`（新·v2 负测）｜`tests/e2e/coverage_map.md`（两例登记）｜文档六件套+看板。**零 src/**（宿主已正确=参照实现）。

**3. 实施步骤**：①A=生成声明.cn 增 窄宽值规范化（整8/整16/正8/正16→IR_类型转换 窄码〔窄读+符号/零扩展·与显式强转同机制三后端既有〕；整32→T_整32 转换〔movsxd 符号回绕〕；正32→既有 正32存储截断 位与零改；已规范寄存器=零开销直通），:232/:121 两调用点换接；②④=生成函数 形参登记循环后对窄宽标量形参发入口规范化三指令（槽加载→转换→槽回存·方法体同 routine 覆盖）；③⑤=生成返回语句 标量返回路径按 函数信息.返回窄宽码（新增字段·:163 区从返回类型文本登记）规范化；④B=隐式转换合法 整数字面量分支补 值域表（整8 [-128,127]/正8 [0,255]/整16 [-32768,32767]/正16 [0,65535]/正32 [0,4294967295]·整32/整64/正64 不检=h1/h5 宿主实测定案）+节点_负号+字面量形态（g9/g10）；⑤502/503 上架+coverage_map；⑥全量门禁+反证。

**4. 验证链（命令级）**：探针矩阵复验（/tmp/t33probe m/*.cn 全表 双侧逐行比对）→502/503 单跑→零警告构建→单测→全量 E2E `--jobs 8`（互踩族串行复验口径）→strict/matrix→自举链 78/79→`check_fn_length`（新函数 ≤100 行）→反证双态。

**5. 边缘情况与诚实边界（开工预判）**：①**B2 常量表达式形态**（`整16 甲=100-200` 宿主拒/v2 放行）＝v2 语义检查「检不出放行」**既有诚实边界**（推导源类型ID静默 不覆盖二元表达式类型推导）——本轮**不关**（表达式类型推导=独立能力缺口·立案 G 区 T98·非本轮半截）；②显式强转产物寄存器（窄码）经规范化入口=零开销直通（已规范守卫）→194 等显式转换用例零变化；③整32 回绕面=宿主「任意字面量放行+运行回绕」实测定案（h1/h5）——**不设值域检查**（与 整16 差异如实注记代码）；④正64 打印符号性（h3 宿主 2^63/v2 -2^63）=打印/字面量折叠面**独立缺陷另立案**（非存储面·本轮不触）；⑤形参规范化=被调方入口（覆盖全部调用点）；引用/指针/结构体形参不触。

**6. 与他机交叠分析**：写集=v2 树 5 文件（IR 生成层+语义）——家机在飞（D1 拆分=宿主 src/552 phi_lower/556/561/563/564 均不触 v2 树）、单位机 558/557/559（src/tests）**零文件交叠**；语义耦合=B 族为 v2 拒通道收紧（负路径）·正路径零变化面由 502 双编译锚定；A 族变更 v2 产物指令流（窄宽用例多 1~2 指令）——**影响面=v2 产物三平台**，集成后请家机/单位机按通告复验 194/385/386 等窄宽既有用例（本机全量先行）。
**【收工回填（实测）·565】**
**1\'. 验收（达成·含开工预判外当场根治两枚）**：①探针矩阵 A 族全绿——op6=-56/op7=4464/b1=-5536/b2=-5536/b5=-5536/h1=-294967296/h5=1705032704/h13=-294967296/h14/h15=-5536（全部与宿主逐值一致）；②B 族六形态 v2p 全部 sem5 编译期拒绝（g1/g2/g5/g9/g10/h6）·域内 g3/g6/g8/h7 零误伤；③502 双编译 PASS+503 负测 PASS（E2E runner 实跑）；④全量门禁（数字见提交信息）+194/130/05 回归全绿；⑤反证双态=修复前 v2p（develop+562）跑 502 同源输出 200/60000/70000/…/4000000000（分叉实锤·修后=期望值）。
**2\'. 写集（最终）**：CN语言编译器v2/IR/生成声明.cn（窄宽码自文本+窄宽值规范化 helper·声明站点+指针层守卫）｜CN语言编译器v2/IR/生成语句赋值.cn（赋值站点换接+复合脱糖标识符块·查指针变量类型 守卫）｜CN语言编译器v2/IR/生成表达式.cn（字面量适配整32 改值域判定）｜CN语言编译器v2/IR/生成函数.cn（返回窄宽码 登记+形参入口规范化）｜CN语言编译器v2/IR/IR容器.cn（函数IR +返回窄宽码）｜CN语言编译器v2/IR/生成语句.cn（返回位规范化）｜CN语言编译器v2/语义/语义检查表达式.cn（整字面量入域+整数字面量文本值·隐式转换合法 两分支挂接）｜tests/e2e/502_窄宽回绕矩阵/503_窄宽字面量域外拒绝+coverage_map。
**3\'. 实施增量（预判外当场修·三枚）**：①**指针声明落穿**——指针/引用声明在 生成变量声明 分派不早退·落穿 生成标量声明初值 且类型文本=基类型：站点①加 类型节点.附加==0 守卫·站点②/成员块加 查指针变量类型<0 守卫（**教训**：首版无守卫→194/130/05 全量 E2E 段错误·全量门禁当场拦截=网底价值再证；**既有着火点**：正32存储截断 对 正32* 声明同面为 46-f 起既有隐患·本轮守卫顺带消除）；②**查指针变量类型 未命中哨兵=-1 非 0**——首版 ==0 守卫误伤全部非指针赋值（b5/h13 回退实锤）·改 <0；③**复合赋值脱糖节点走成员路径**——文本=0 的合成 节点_赋值 分派进 生成成员解引用赋值·标识符左值在此补规范化（最初误判=纯赋值路径覆盖）。
**4\'. 边缘（回填）**：预判①成立（B2 常量表达式=T98 立案·g4 运行值已正确）；预判②成立（194 显式转换用例零回归·已规范直通守卫有效）；预判③成立（整32 值域不检=宿主 h1/h5 定案）；预判④成立（正64 打印符号性=T100 立案）；预判⑤成立（形参规范化被调方单点·194 读窄(正8*) 零回归）。**新发现**：v2 传参面隐式转换整体缺检=T99 立案（g11 实锤·运行值已由形参入口规范化纠正）。
**5\'. 交叠（回填）**：零冲突（写集=v2 树+tests 502/503·他机在飞不触）。

> 〔2.83 D46（**v2 结果构造名裸名编译期拒绝**＝返回位/声明位/赋值位三段·474/475/476-a） 已滚转归档（深度机 565 轮·2026-09-21·切片=plans025-深度机节-2026-09.md）〕
> 〔2.84 D46 残余收口（**构造名裸名全位拒绝**＝语义检查表达式.cn 59 行放行改拒绝） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 D10 v2 面（**泛型实例体重放语义检查**＝470-a 主件+477 环境映射随 501 链作废） 已滚转归档（深度机 565 轮·2026-09-21·切片=plans025-深度机节-2026-09.md）〕

### 2.114 D1 滚动（**v2 树 `生成表达式求值.cn` 双函数拆分**＝生成复制展开 175+生成方法调用 146·纯机械零行为变更·501 链作废后避让解除）——532-a 开发轮（分支 `任务/深度机-532-生成求值拆分`·基点=develop 9b991aa1·wt520 隔离·2026-09-21）

**1. 目标与验收判据**：021 §3-D D1 滚动第 5 弹——`生成表达式求值.cn` 两函数：生成复制展开 175 行（151-325）+生成方法调用 146 行（444-589）各拆为分派器+子函数（同族形态按源文实测）。**验收**=①新旧 v2p 编译同一改前输入 asm 逐字节 md5 一致（x64l 探针+全树双对拍）；②反证双态；③check_fn_length 出列两函数；④linux 全量门禁零新增红。
**2. 写集**：CN语言编译器v2/IR/生成表达式求值.cn（唯一代码文件）｜文档六件套｜看板清行。零 src/零 tests。
**3. 步骤**：基线→侦查整读两函数→拆分→对拍→反证→门禁→六件套。
**4. 验证链**：md5→反证→构建零警告→单测→全量 E2E→strict/matrix→check_fn_length。
**5. 边缘（预判）**：①**侦查必须整读函数**（523 教训·grep 截断曾致误判）；②两函数为独立分派结构（复制展开=节点类型分派·方法调用=调用形态分派）——若实测为共享状态顺序循环则如实登记不拆（解析类先例）；③同号假红族照旧口径。
**6. 交叠**：生成表达式求值.cn 与 513~517（生成语句赋值/生成表达式字段/生成表达式/IR生成）不同文件零交叠；501 已作废。

**【收工回填（实测）·532-a】**
**1\'. 验收（达成·范围收窄如实声明）**：①生成复制展开 175→主函数（前置可信判定 71 行+四路分派）+四路子函数（字符串改写 6/标量直通 12/容器类 27/结构体物化 47）——13 条边界断言·逐字迁移 ✓；②md5 双对拍=探针 3982d9f7+全树改前文本 111751 节点 c4355400 ✓；③**行为级反证**=④槽偏移变异（-1 删除）→probe4 结构体复制消费面产物 1c85b603（变）→恢复 d71c7563 复绿 ✓；④check_fn_length 58→57 ✓；⑤门禁=单测 1387/1387+E2E 522=516/4/2（4 假红=同编号互踩已知）+strict/matrix ✓。
**2\'. 范围收窄声明**：生成方法调用 146 行=同文件第二函数**登记下一轮**（本轮单函数已足轮量·四路精密分支的拆分验证周期超单轮预算）。
**3\'. 边缘（回填）**：①**反证迭代三才发现消费面**（探针算术面/全树自举均不消费复制展开路径·标量直通返回变异=等价变异产物不变——AST 生成层文件的反证消费面=「含目标形态调用的用户程序」probe4）②行号切片一轮漏 复obj 首行（编译期「未找到匹配的函数 IR追加」当场暴露·断言补齐重切）。
**4\'. 交叠（回填）**：全程零冲突。


**【收工回填（实测·改判）·533-a】**
**改判=设计型重构候选（不拆）**：整读 生成方法调用（456-605·151 行）——**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·嵌套 5 层+跨层 `返回;` 终结语义+共享可变状态〔表达式栈/指令/计数指针〕）——机械拆分不可行（嵌套提取需把外层条件转子函数前置守卫·跨层 返回; 转译风险高）。**登记=设计型重构候选**（与 解析类声明 185/解析选择语句 136 同类·需独立设计轮）。零代码收工。

### 2.115 D1 设计型池·方案设计轮（**解析类声明 185 行重构方案**＝五子函数划分+状态传递设计+等价验证策略·零代码·登记待实施）——535-a 设计轮（分支 `任务/深度机-535-解析类声明设计`·基点=develop 603b4745·wt520 隔离·2026-09-21）

**1. 结构分析（整读定案）**：`解析类.cn:252-436` 解析类声明——两层循环（262 继承跳过当+277 类体主循环 当(真)）+19 个局部状态·其中**布局三元组**（字段偏移/最大对齐/最大字段）跨分支累加+**位置**全程推进+**段可见性**主循环内切换。类体主循环内四类成员形态：方法（300-333·含构造）/不安全析构+函数（343-392·414-a stdlib 析构形态）/字段（397-416）/默认段守卫。**改判依据复盘（533 先例延续）**：非扁平分派但**成员形态间互斥且有清晰边界**——可拆（与 解析选择语句 的跨层中断不同·本函数无跨层 中断 语义——262 循环的 中断 限于自身块内）。
**2. 子函数划分方案（五子函数）**：①`解析类继承跳过`(记号,位置)->位置——262-269 继承/接口列表显式诊断+跳过；②`解析类可见性标签`(记号,位置,段可见性)->(新可见性,新位置)——283-289；③`解析类方法成员`(池,记号,位置,段可见性,类键基,...)->(新位置,是否消耗)——300-392 方法/构造/不安全析构形态（最大块·内部可再按 414-a 析构形态/普通函数二次切分·实施轮定）；④`解析类字段成员`(池,记号,位置,段可见性,字段偏移&,最大对齐&,最大字段&,...)->(新位置,是否消耗)——397-416 字段+布局累加；⑤`解析类布局收口`(字段偏移,最大对齐,池,...)->位置——422-433 尾部对齐+登记校验。
**3. 状态传递设计**：位置=每子函数返回值（值传递）；池/符号表=引用参数（既有惯例）；布局三元组=引用参数（`整64& 字段偏移` 三连·对齐 登记函数参数们 的引用惯例）；段可见性=值输入；主循环的 保底推进（420 前位置防死循环）保留在主函数（防死循环守卫不入子函数=职责单一）。
**4. 等价验证策略**：①新旧 v2p 编译同一改前输入 asm 逐字节 md5（探针=类/接口/继承/构造析构/运算符重载成员全形态用例 20/21/22/23/95/100/108/333+全树双对拍·518/530 口径）；②反证=变异布局累加行→产物变→恢复复绿；③全量门禁（解析类用例族 20/21/22/23/28/95/100~108/333 全在 E2E 覆盖）。
**5. 实施轮预估**：方案已定稿·实施=行号切片+签名落地+对拍反证+门禁（约 1 轮·同 532 周期）·候选=下一会话首选拾取。
**6. 交叠**：解析类.cn 零占用（329=词法/主/货舱·513~517=IR 生成层）·与全部在飞零交叠。


### 2.123 F2-33 迭代器侦察定稿轮（**T93 宿主泛型实例指针合成缺陷立案**+迭代器设计定稿·零入库代码）——548-a 侦察轮（分支 `任务/深度机-548-迭代器`·基点=develop 9901fa47·wt548·2026-09-21）

**【收工回填（实测·548-a）】**：①探针双侧分裂——迭代器最小形态（泛型类 向量迭代器{向量<T>* 源;游标}+绑定/有下一个/下一个+当循环）宿主 cn check 红「无法将 '向量<整64>*' 隐式转换为 '向量$整64*'」＝**T93 立案**（$ 合成名与 * 组合吃星号·src 类型系统合成名规范化缺口）；**v2p 同源 rc=0**（103 节点）→新用例走 v2 闭环形态（457 先例·头注宿主欠账）。②设计定稿=stdlib/迭代器.cn 泛型类 向量迭代器<T>{源指针+游标+绑定/有下一个/下一个}·525_v2_向量迭代器（借链=是）·映射集合迭代器=波2。③**549 接力计划**：宿主 T93 修复（src 类型系统独立轮）+525 上架+修后升双编译=消费者反证。④写集=零代码。⑤交叠零。
### 2.2 下一轮候选（预登记·防撞车）

- **daemon 常态化采样续跑**（已重启新版·新种子域 312000001 起自动轮换）；🔶 行扩面巡视（候选=行 53 字符串下标/子串·行 54 容器边界 T17/T18）；
- 波次3=T6+T7（嵌套深度防护+语义复杂度）；波次5=C18+B12（含 T27 全族+i128 helper 修正项）；T44/T45 修复待用户裁决（建议同轮波次清零·写集零交叠）。

> 〔2.3 A12 里程碑标签锚点（**334-a 开发轮·轻量·已收工**·2026-09-18·分支 `任务/深度机-334-A12里程碑锚点`） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.64 条件产物存储轮（**D45 根治+D44 正体收口 双销项**＝贯穿恒真三修+选择语句宿主对齐重写 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.65 D42 甲案波 1（**泛型类源码形态·解析+语义层**＝`泛型 <类型 T> 类 名 { }` 声明解析+参数序登记+类型参数名豁免） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.66 D42 甲案波 2（**IR/生成层单态化实例**＝实例收集→实例注册为完整用户类→方法 AST 克隆生成） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.67 D42 甲案波 3 前置件①（**泛型函数声明+泛型体检查延后+撤守卫侦察**＝stdlib 源码级加载缺口清单实测立案） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.68 枚举全链（**D40 最大子项**＝枚举声明/登记/成员链常量折叠/跨模块导入·stdlib 源码级加载硬前置） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.69 D42 波 3 收口（**函数版单态化+调用点解析+撤守卫+stdlib 源码级加载全通+390 根治验收**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.71 stdlib 源码级 IR 段攻坚（**克隆器标识符位替换+实例布局链审计+装箱语境**＝420 实测缺口三族 ×132） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.72 IR 诊断宿主精确定位＋按宿主逐修（**类型大小 K/V ×48 的真正生成链锁定**→撤守卫+390 终验收） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.73 族②③ IR 段收口（**克隆器修针类型感知全集＋实例表配对越界＋裸字段下标左值＋装箱语境兜底**＝撤守卫 123 终收口） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.74 向量类克隆野走定位＋raw-$T 跳过＋撤守卫 123 零诊断终收口 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.75 撤守卫 123 运行期收口＋390 终验收＋456 上架 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.76 T62 三例无诊断硬错误→可恢复诊断改造（**v2p 302/rc=1 零诊断残余面收口**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 T70 面②' 扩形态（**生成容器对象整体赋值扩「右值=调用」接管**+IR 模块根成员内置直调+面⑤' 运行级定位） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.84 T57 重设计前置（**objdel 标签撞号根因定位**＝466-a 反证撤除的生产态 3 红源〔228/283 rc=1+79 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
> 〔2.85 T70 B1 专项（**撤守卫收敛尾段**＝克隆体推导链对用户主模块结构体元素 miss 根治） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
## 三、单位机 linux-arm64 节（本机维护）

### 3.21 本轮（**341-a 制度轮·🏃 轮次号「先到先得」落地（用户裁决方案①）**·基线 620831b·分支 `任务/单位机-341-撞号防线`·2026-09-18）——开工计划（六要素）

**1. 目标与验收**：跨机轮次号撞号根治（用户 2026-09-18 裁决方案①「认领前先看板、先到先得」）。**验收**：①AGENTS §8.1 占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；②check_handoff 新增「在飞轮次号唯一性」机械检查（跨机同号拦截）+正反例实证；③**plans/025 膨胀事故同轮根治**（实测 1780 行·§二/§三 被拼接 4 份→标题级去重保留最新→493 行）；④本轮自身按新制度实践（**先直推看板占号 341 再开工**——新制度首次完整闭环）。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 分支命名条）｜scripts/check_handoff.py（查看板() 扩「在飞轮次号唯一性」）｜plans/025（去重+本节）｜plans/021（戳+§五索引）｜HANDOFF.md／更新日志.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①看板直推占号 341（**已完成**）；②AGENTS 条文（占用权威=看板在飞行）；③check_handoff 检查实现+实证——**首版正则漏大写字母致反例未拦**（`340-D38`/`329-D12` 提取为 []）→ 修为 `[a-zA-Z]|中文` 后三情形实证过；④plans/025 去重（标题级·保留最后出现版本·断言零重复标题）；⑤门禁+integrate 集成。

**4. 验证链**：check_handoff 正例 0／反例（家机行改 341）拦截 1（报「在飞轮次号 341 被多机占用」）／还原 0；plans/025 去重断言（无重复 `###` 标题）；integrate.py 快速门禁（纯文档+平台无关脚本写集·按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界**：①轮次号提取正则须覆盖大小写/中文标识（本次首版 bug 当场实证发现并修）；②plans/021 恰 600 行上限（瘦身登记下轮·用户已提）；③plans/025 膨胀根因=集成/rebase 时「拼接而非替换」（本轮去重为止血；机械防线=`### 标题重复`检测建议下轮加——与已上线「## 节标题重复」检测同族）；④去重保留「最后出现」版本，三机节内容请两机集成后复核。
**6. 与他机交叠**：家机 340-a／深度机 338-a 在飞（写集=v2 树/src 面）与本轮（文档+门禁脚本）**零文件交叠**；plans/025 三机共用（§一/§二 保留最新版·集成后在通告段请两机复核本机节）。

### 3.20 本轮（**332-a 修复轮·🏃 看板先行机械强制（用户令）**·基线 044423b→集成 b45e4ae·分支 `任务/单位机-332-看板强制`·2026-09-18）收工回填：**制度缺陷根治（开工先填板/收工即销账·机械强制）**

**收工回填（实测·目标全达成）**：①AGENTS §8.1「看板先行」（开工第一动作=直推看板本机行→才开分支；收工集成成功即清行直推）＋§8.5「强制直推、即改即推」（看板行/通告段/try-build 请求三类）落盘；②check_handoff「开工自检」上线——**双向实证**：正例（本分支 0 通过）／反例A（看板不含当前分支名→拦截·含「开工自检」字样）／反例B（🏃 行缺分支名→拦截·证明新小节结构下旧校验仍有效）／还原复跑 0；**判据修正（自建制度首日自证 bug·当场根治）**：收工销账清行后分支名自然不在看板 → 加「**分支已集成（`git merge-base --is-ancestor HEAD develop`）则放行**」豁免，三情形实证（正例放行／销账态放行／未集成+无看板行拦截）；③看板表拆三机独立小节＋查看板() 适配（4 列识别+节内恰 1 行）；④集成 b45e4ae（integrate.py 快速门禁通过；纯文档+平台无关脚本写集按 §8.3 不触发 try-build）；⑤**本轮自证实测两类冲突**：单表三行结构下与深度机看板连续直推撞 rebase 冲突两次（手工解·两行版本并存）＋push CAS 竞争一次（重试复推）——结构拆分与 CAS 均为实证驱动修复。**边缘回填**：非 git 环境开工自检自动跳过（实装）；集成后清行再跑门禁的误报已由「已集成豁免」根治（本轮实证）。**诚实边界**：①332-a 自身开工时未先直推看板（旧习惯残留·已在 HANDOFF 记录并随结构同批生效）；②plans/021 达 600 行上限（卡线·下轮需瘦身）。

**1. 目标与验收判据**：把用户令（2026-09-18）「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」落为**机械强制**。**验收**：①AGENTS §8.1 新增「看板先行」强制条（开工第一动作＝直推看板本机行；收工即销账）+§8.5 直推窄通道改「强制直推、即改即推」；②check_handoff.py 新增「开工自检」（当前分支 `任务/*` 时看板必含该分支名）+**正反例双向实证**（正例放行／反例「先动手后填板」拦截）；③看板表拆为**三机独立小节**（防相邻行 rebase 冲突——332-a 当日实测两次）+查看板() 适配；④check_handoff 全绿。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 看板先行/认领/看板行格式三条·§8.5 直推窄通道+看板结构条）｜scripts/check_handoff.py（查看板() 适配小节结构＋查开工自检()＋主流程挂载）｜三机任务看板.md（结构拆分＋本机小节＋332-a 通告段）｜plans/025 本节｜更新日志.md／HANDOFF.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①AGENTS 条文（用户令原文为依据）；②check_handoff 查看板() 改小节识别（4 列）＋查开工自检()（判据＝当前分支名出现在看板任一行·`git rev-parse --abbrev-ref HEAD` 容错）；③看板结构拆分（三机节·内容原样迁移）；④门禁正反例实证；⑤文档六件套；⑥integrate.py 集成＋通告段广播结构变更。

**4. 验证链**：`python3 scripts/check_handoff.py`（正例 0）｜反例 A＝看板不含当前分支名（拦截 1·含「开工自检」）｜反例 B＝🏃 缺分支名（拦截 1·新结构下旧校验有效性）｜恢复后复跑 0｜集成门禁（integrate.py 快速门禁——纯文档+平台无关脚本写集，按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界（开工预判→回填）**：①开工自检判据＝「分支名出现在看板任一行」——跨机 try-build 在他机分支上跑时分支名在其机行内即满足（分支主人集成前不清行）；集成后清行再跑旧分支门禁会误报（罕见场景·可接受·回填实测）；②非 git 环境（打包校验）自检自动跳过；③结构变更＝三机共享契约变更 → 通告段广播，他机 rebase 时按其行取值解冲突；④本机 329-a 在飞可见性由 044423b 承载，332-a 行随本批集成更新（结构变更同批故不再单独直推）。

**6. 与他机交叠分析**：家机 329-a（v2 语义层·D38批2）不碰 AGENTS/check_handoff/看板 → 零交叠；深度机 331-a（`src/` 三后端+IR 验证器·try-build 请求中）改其本机行/通告段，与本轮「看板结构变更」**语义相邻**（他 rebase 时按新结构取本方行）——已在 332-a 通告段写明；其余零交叠。

### 3.18 上一轮（**328-a 开发轮·🏃 协议 v2 落地**·用户授权插队·基线 93b11df→7b84b93（rebase）·分支 `任务/单位机-328-协议v2`·2026-09-18）收工回填：**协议 v2 全链落地+本轮自身走新流程端到端演练**

**收工回填（实测·目标全达成）**：①AGENTS.md §8 全文重写（六总纲+§8.1~8.7；§6 三处旧引用同步+§3 缺陷零容忍分级注记）；②`scripts/integrate.py` 新增（合并队列七步·--dry-run/--try-build-done·平台感知门禁）——**实战两遇**：本地缓存（.zcode/plans/）挡前置自检→改「未跟踪文件警告放行」+.gitignore 补忽略；**家机 326-a 收工与本案真实碰撞**（develop 前进+3 文件冲突）→integrate.py 按设计自动 abort 报告→分支主人手工解冲突（plans/021 戳合并双方信息/lessons 双段保留/看板通告段双侧拼接）→rebase 完成→--dry-run 全步骤过（退出码 0）；③check_handoff.py 扩看板 🏃 行分支名校验——**反例（330 号无分支名）拦截+正例（326 豁免/328 合规）放行双向实证**；首版曾真实拦截家机 326-a 旧格式行→加「轮次号 <328 过渡豁免」（旧协议在飞轮无分支可标·收工清行自然消亡）；④plans/027 方案文档落盘（诊断/业界对照/裁决记录/细则/试点安排与复盘区）；⑤共享文档全套更新（看板说明区 v2 口径+生效广播+试点邀请/plans/021 戳+里程碑+试点行+§五索引〔瘦身 3 行回 600 内〕/lessons 设计依据条目/HANDOFF/更新日志单位机节）。

**1. 目标与验收判据（达成）**：六项验收全达成（①AGENTS 重写②integrate.py 演练过③校验双向实证④plans/027 落盘⑤本轮走新流程⑥双文档门禁 PASS·plans/021=599 行）。

**2. 写集（最终清单·11 文件）**：AGENTS.md｜scripts/integrate.py（新）｜scripts/check_handoff.py｜.gitignore（补 .zcode/plans/）｜plans/027-三机并行协同架构v2方案.md（新）｜三机任务看板.md（本机行+说明区+通告段）｜plans/021（戳/里程碑/§三-A 试点行/§五索引+瘦身）｜plans/025 本节｜HANDOFF.md/更新日志.md 单位机节｜lessons.md（追加+rebase 合并 326-a 段双保留）。

**3. 实施步骤（回填）**：按计划①~⑩全部执行；插入项=两处 integrate.py 实战修正（未跟踪文件警告放行/冲突自动 abort）+rebase 冲突解决（与家机 326-a 收工真实碰撞）。

**4. 闅证链（回填实测）**：py_compile 双脚本过；check_handoff+check_progress_sync 全过（独立验退出码·管道吞码教训在案）；反例拦截/正例放行双向实证；integrate.py --dry-run 全步骤过（前置自检/fetch/rebase 对齐/快速门禁双过/push --dry-run）；真集成=git push gitcode develop（ff-only）+github 镜像补推。写集纯文档+平台无关脚本 ⇒ 按 §8.3 分类不触发 try-build、不重跑 E2E（基线 7b84b93=家机 326-a 收工+看板修复提交承载）。

**5. 边缘情况与诚实边界（回填）**：①**并行开发实效未验证**——本轮单分支落地，试点轮（plans/027 §六）承载并行度实测；②integrate.py 首版 try-build 回签=人工通告段+旗标声明（自动解析留试点后迭代）；③集成门禁增量判定（竞争重试轮跳过全量门禁条件）尚未经历真实竞争检验；④python 中文标识符带空格=语法错误（首版两处·py_compile 当场拦截）；⑤看板 328 行在真集成成功后经窄通道清行（收工广播随通告段）。

**6. 与他机交叠分析（回填）**：家机 326-a 收工与本案**真实碰撞一次**（develop 前进+看板/plans021/lessons 三文件冲突）——按 v2 设计路径消解（分支主人自解冲突·rebase·非 develop 手解），**此碰撞即 v2 协议设计的第一个实战案例**（旧协议下=双直推冲突+可能冲突标记入库；新协议=隔离分支+集成时一次性解决）；深度机 ⬜ 全程零交叠。

**下一轮候选（预登记）**：①**协议 v2 试点**（plans/027 §六·另两机各开真实任务分支并行→复盘）；②M3 采样轮续（B8 候选行+daemon 常态）；③T24/T25 v2 树对齐轮（随家机 v2 面轮次）。

### 3.17 上一轮（**297-a 开发轮**·🏃 用户裁「按照你的方式，全部修复」·基线 478d804→认领 ab935b4·2026-09-17）收工回填：**T25/T24/B10 三案当日根治+T26 同族判定+规范条文+406/407/408 转正·全量门禁绿**

**收工回填（实测）**：①**T25 根治**：病灶精确定位=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支向 storeVirtualResult 传 w9 → emitStackStore 浮点分支 `str w9` 32 位截断写（高 32 位丢；1.5 低 32 位恰全零故读出 0.0）——修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0·asm 实证 `str d0` 64 位完整写）；②**T25 回归矩阵 27 形态全绿**（三元 9+汇合 9+最小 2+浮32 3+数组 2+显式转换口径·C 一致 O0=O3）；③**T24 根治**：visitUnaryExpr Inc/Dec 三路径（静态/成员下标解引用/主路径）+handleClassFieldIncDec(+postfix) 按后缀返回旧值（原忽略 postfix 一律新值）——16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照·O0=O3）；④**B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻（括号/嵌套 ? 深度感知·新判据⊇旧判据=存量成功程序零语义变化论证）替换 C-1 白名单——`a ? -b : c` 双级别全绿（u1~u5）+后缀传播 `r?+1`=42 不回归+E2E 60 单跑绿；⑤**T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无截断分派同型）静态审查注记；⑥**规范条文**：plans/001 §4.5 优先级表（++/-- 后缀 13/前缀 12 级+值语义）+三元消歧条文+错误传播节更新；spec 01b 五处同步；⑦**新 E2E 三件转正**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记；⑧**全量门禁绿**：零警告构建+单测 1353/1353+全量 E2E 426=424/0/2（跳=62_ffi/69 平台败面）+**78/79 自举锚定双 PASS**+E2E 60 传播复验绿；⑨看板通告段=v2 消歧同口径广播（家机 296-a 对齐）。

**1. 目标与验收判据（达成）**：六项全达成（T25/T26/T24/B10/规范/门禁——§三.17 计划①~⑥逐项 ✓）。

**2. 写集（最终清单）**：src/cn_compiler/codegen/arm64/arm64_instructions.cpp（emitCopy）、src/cn_compiler/ir/ir_expr_unary.cpp+ir_oop_field.cpp+ir.hpp（T24）、src/cn_compiler/parser/parser_expr.cpp+parser.hpp（B10）、tests/e2e/406/407/408（新·各 2~3 文件）、tests/e2e/coverage_map.md、plans/001、.zcode/skills/cn-language-spec/specs/01b、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段。**v2 树零改动** ✓（家机 296-a 写面）。

**3. 步骤（回填）**：认领（ab935b4）→T25 侦察（emitCopy+storeVirtualResult+emitStackStore 链·IR↔asm 逐环对位）→修→27 形态回归→T26 判定（arm64 短路复现=免疫）→T24 侦察（visitUnaryExpr 忽略 postfix 实锤）→修→16 形态→B10 实现（ternaryColonAhead）→6 形态+传播回归→规范五处→E2E 三件→全量门禁→销项。

**4. 验证链（回填实测）**：零警告构建（grep error|warning=0）+单测 1353/1353+全量 E2E 426=424/0/2+78/79 双 PASS+406/407/408 单跑+60 单跑+最小复现 O0/O3 双对+asm d0 写实证+双门禁（独立退出码）。

**5. 边缘情况与诚实边界（回填）**：①T26=x86_64 单侧·精确形态验证归深度机（本机 arm64 免疫实证+静态审查注记）；②win/x64l Copy 浮点路径运行级复验归各机（静态=结构不同无同型截断）；③T24 v2 树同型对齐=待家机 296-a 收工后独立轮（通告段已广播）；④三元负号+浮点汇合的单测未加（E2E 三件为强门禁·单测面留后续）；⑤i1 Copy 在 arm64 走整数路径（is64=false→w9 32 位写）——布尔 1 字节值 32 位写安全（高位无关）·实证布尔矩阵全绿。

**6. 与他机交叠分析（回填）**：家机 296-a（v2 树）零交叠 ✓；深度机 🧪（只读+cnsmith_hits）零交叠 ✓；coverage_map 潜在文本冲突=rebase 兜底未发生。

**下一轮候选（预登记）**：①**M3 采样轮⑥**（回归 B8 候选行+daemon 常态·用户令不停）；②**T24/T25 v2 树对齐轮**（家机 296-a 收工后·自举编译器同型三缺陷）；③T26 精确形态=深度机口径（B11）。

> 〔T33 侦查注记/D36 实施设计已滚转归档（471 集成会话·2026-09-20·D36 定性详见 021 D36 行）〕