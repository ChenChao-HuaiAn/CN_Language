# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 187 轮（2026-09-15，单位机 ARM64）——建表 + 三机在飞计划落盘 + 交叠/遗漏审计初版。
> （轮次号更正：本表建立轮曾写 185-a，因该号已被深度机先占，本机认领无效 → 更正为 **187-a**；D6 轮 = **188-a**。）

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **240-a** | A10 M1 冲刺·A9 续波：73 格缺口补格（既有用例诚实补格+新用例 311 起）目标 ≥80% | tests/e2e/311~N（新）、coverage_map.md、plans/021、本表、HANDOFF/更新日志家机节、看板本机行 | 纯测试+映射轮·源码零改动·与深度机/单位机零交叠 | §1.0 | 🏃 |
| 深度机 linux-x86_64 | **232-a** | 协作协议增补（用户令两项）：角色动态优先级（开发>探针>E2E）+ 看板纯状态板（空闲行干净化+门禁扩展） | `AGENTS.md` §8、三机任务看板.md、scripts/check_handoff.py、plans/021、plans/025、HANDOFF.md、更新日志.md、lessons.md | 与家机/单位机零交叠（双 ⬜） | §二 | ✅ |
| 单位机 ARM64 | **226-a** | **D8 寄存器感知发射尝试（→按纪律回退·源码零净变更）+ F1-26 调研**；下一轮候选=**F1-26 mem2reg** / D8 重做（别名·宽度契约定位）/ F2-33 | `src/cn_compiler/codegen/reg_alloc.{cpp,hpp}`、`codegen/arm64/arm64_codegen{,_emit,_dispatch}.cpp/.hpp`、`arm64_instructions.cpp`、`arm64_codegen_oop.cpp`、`arm64_codegen_i128.cpp`、`backend_factory.cpp`、`tests/unit/codegen/test_reg_alloc.cpp` | 与家机 206-c（stdlib/v2 语义检查）、深度机 213-a（v2 IR容器释放）**零交叠** | §三.2 | 🏃 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **222-a**（本轮在飞） | D7 修正 + F1-28 接线（见 §三.2）；下一轮候选：① F1-26（Phi 落地·arm64 regalloc 已接线后同链收口）② F2-33 纯 stdlib 三项（随机数/迭代器/正则）③ D1 续波 / D3 | 用户批阅优先级①→② |
| 家机 win-x64 | 待 183-a 收工自登记 | 候选：① uD 面（`emitStructWholeAssign` 成员级级联深拷）② 波 3 ④v2 同构／⑤H7·H11 收口 ③ 波 4（真 move + `复制(x)`） | HANDOFF 家机节「四、下一步」 |
| 深度机 linux-x86_64 | 待 191-a 收工自登记 | 候选：① D1 续波（`scripts/check_fn_length.py` 实测 58 项：`ir_oop_call.cpp::handleClassCallExpr` 362 / 其余——**避开家机 183-a 写集**）② 波 7 NLL 基础设施 | HANDOFF 深度机节 + 实测清单 |

---

## 一、家机 win-x64 节（本机维护）

### 1.0 本轮（**240-a**，基线 3598130）：**A10 M1 完成度冲刺·A9 续波（73 格→目标 85%+）**

> （239-a 已收工·提交 3598130；本轮=A10 第一波。）

1. **目标与验收判据**：①**既有用例诚实补格**——逐格审查 73 缺格，确实被存量用例覆盖的边界/负例语义补映射（如 5.6 负例=101/109/112 既有、5.9.2 边界=143 等）；②**新用例一批**（311 起，编号 fetch 查重）：高价值负例/边界（函数重载歧义负例/泛型约束负例/错误传播负例/转移边界/导入边界/选择负例等），逐个实测（负例锚定诊断/正例固化输出）；③覆盖从 60.1% 显著推进（目标 ≥80%，73 格中消减 ≥40 格）；④全量 ci.ps1 绿+双文档门禁。
2. **写集**：`tests/e2e/311~N_*`（新用例）、`coverage_map.md`、plans/021、本表、HANDOFF/更新日志家机节、看板本机行——**源码零改动**（纯测试+映射轮）。
3. **实施步骤**：①认领→②逐格审查既有用例归属（模糊者抽查源码）→③映射补格→④新用例设计+逐个实测→⑤--ci→⑥全量 CI→⑦六件套+双推。
4. **验证链**：每用例 cn build/check 实测→--ci→全量 ci.ps1→双文档门禁。
5. **边缘与诚实边界（预判）**：①不确定归属的格**宁可留空**（映射质量>覆盖率数字）；②「边界例」口径=临界面（极值/空/嵌套/多形态），不得为凑格把正例重复填边界；③残余格数收工如实回填，A10 多波推进；④FFI 负例（62_ffi 平台面）与 v2 对照面缺口如实标注。
6. **与他机交叠分析**：纯 tests/e2e+映射+文档写面——与深度机/单位机零交叠；无源码/契约变更。

---


### 1.1 （⏸ 待定区·2026-09-16 用户令暂缓：plans/026 体系优先，视情况再启动）案戊阶段 1——type_system 下沉 types/ 基础层 + 静态库物理边界 + 层依赖门禁（T2 用户批准案戊）＋并行 **V2 编译器优化检查**（只读）

> **状态（2026-09-16 更正）**：计划已落盘（六要素见下）；**阶段 1 代码改动曾试做（提交 67127bc）、经
> 77c0ae4 revert 撤下、未入 develop**（验证链未跑）；**实施与收口（check_layer_deps 门禁 + 验证链 +
> 文档六件套）待后续机器按本计划认领执行**。并行项 V2 优化检查已完成·产出=plans/021 §三-G2（待排班）。

**1. 目标与验收判据**

- **案戊阶段 1**（开发轮）：`type_system.{hpp,cpp}`（559 行，被 45 文件跨层消费）从 `semantic/` 下沉到
  `types/` 基础层；CMake 拆独立静态库（链接方向物理固化）；新增 `scripts/check_layer_deps.py` 层依赖门禁接入 ci.ps1。
  验收=零警告构建 + 单测全过 + 全量 E2E 全绿 + **锚定链逐字节不变**（234-a 新基线 410460 行）。
- **V2 优化检查**（只读探针）：v2p 自编译计时/产物指令统计/算法模式静态审查，优化机会落 plans/021 待定项。

**2. 写集（精确清单）**

- `src/cn_compiler/types/type_system.hpp`、`src/cn_compiler/types/type_system.cpp`（git mv 自 semantic/）
- 45 处 `#include cn_compiler/semantic/type_system.*` → `cn_compiler/types/type_system.*`（ir 18/semantic 19/codegen 6/parser 2）
- `CMakeLists.txt`（types 静态库 target + 语义库链接 types）
- `scripts/check_layer_deps.py`（新增）+ `scripts/ci.ps1`（接入）
- 文档六件套（本节/HANDOFF 家机节/更新日志/plans/021/看板/lessons 如有坑）

**3. 实施步骤**

1. `git mv` 两文件 → types/；批量改写 45 处 include 路径（python 正则改写，逐文件核对计数）；
2. CMakeLists：新增 `cn_types` 静态库（type_system.cpp）+ `cn_semantic`（或现聚合目标）链接它——保持产物二进制等价；
3. `check_layer_deps.py`：①types/ 不得 include parser/semantic/ir/codegen ②parser 不得 include semantic.hpp（type_system 除外——阶段 2 收口）③层 3 过程目录互不 include（按阶段渐进启用）；
4. 验证链全跑；5. V2 优化检查（v2p 计时/指令统计/静态审查）。

**4. 验证链**：清场重建零警告 → 单测 → 全量 E2E（313 用例口径）→ 锚定链逐字节比对（410460 基线）
→ check_layer_deps 反例自检（构造违规 include 确认拦截）→ 双文档门禁。

**5. 边缘情况与诚实边界（开工预判）**：① 234-a 刚改 v2 树+stdlib 并重锚 410460——本轮基线取其上，
   锚定链比对用新基线；② 45 处 include 中 semantic 目录内部 19 处为「同库内搬移」性质（不跨层），
   仍统一改写保一致；③ CMake 若现结构为单聚合目标，则先建 types 库+目标间链接，不动其余聚合结构
   （阶段 2~4 逐层拆）；④ check_layer_deps 首版规则面从窄（types 纯净性+parser 白名单），阶段 2~4 扩。

**6. 与他机交叠分析**：写集=src/cn_compiler 头文件路径+CMake+scripts——与深度机（234-a 已收工·下轮
候选=读取文件行 uninit/D1）、单位机（mem2reg/D8/F2-33·codegen 与 reg_alloc 面）**写集不相交**；
锚定链口径同步用 234-a 新基线，语义零变化（纯搬移），他机无需复跑。

---

### 1.2 上一轮（探针检测轮，验证轮不占号；基线 476b7d2）收工存档（要点）

**10 项发现（T1~T10）落 plans/021 §三-G**：T1 文件级 8 个 >1000 行无门禁；T2 依赖方向漂移（**用户已批案戊**，
本轮实施阶段 1）；T3 递归结构体不拒绝（双侧）；T4 字符串下标无越界检查（双侧）；T5 超界码点（双侧）；
T6 嵌套 ≥175 层栈溢出（宿主）；T7 语义检查 O(d³)（宿主单侧·v2p 0.3s 对照）；T8 常量除零编译期不拒；
T9 整数溢出语义（**用户已裁决方案 D**：常量溢出编译期硬错误+变量面回绕+显式 API）；T10 子串越界静默截断。
正面确认：数组越界/负下标/逃逸引用/结果丢弃/空向量读取/除零运行时/超长标识符哈希短化防线均在。
探针脚本 `target/probe234/`（不入库）。提交 645e50f + 1d90d24（T9 裁决落盘），已双推。

## 二、深度机 linux-x86_64 节（本机维护）

### 2.1 本轮（**235-a**，基线 1503c2a）：**「读取文件行」uninit 独立案根治**——结果类形态双兼容（收集判定补尖括号形态）

**1. 目标与验收判据**

- 目标：根治 225-a 遗留「读取文件行」uninit（独立既有潜伏·valgrind 6 errors）——
  `结果<字符串,整32>` 局部在 EOF 态的载荷槽被无条件 `__cn_str_free` 读。
- 验收判据：**v2p valgrind 归零**（123_v2_容器 6→0 / 探针 8→0）+ 反证单变量复现 + 全量门禁全绿。

**2. 写集（精确清单）**

- `src/cn_compiler/ir/ir_fields.cpp`（collectOwnedStrFields isResult/isOptional 双形态判定）
- 文档：plans/020 第七十六节、plans/021（销项+戳）、本节、更新日志、HANDOFF、看板

**3. 根因（定位链）**

- 225-a origins 留档：uninit 诞生于 `文件.读取文件行` 栈帧、使用于 `读源文件` 的 `__cn_str_free`。
- 插桩实证：`emitOwnedStrFieldFreesAt` 对 canon=`结果<字符串,整32>`（**模板源形态**）发射
  **FREE-UNCOND off=8**（无条件版）。
- 机理：collectOwnedStrFields 的 isResult 判定手写 `rfind("结果$")`（只认合成名形态）——
  局部变量源类型为**尖括号形态** → 漏进普通结构体分支 → 字段「错误值联合」（合成联合体
  结果联合$T$E）递归展开时 164-a 豁免不适用（unionScope=假）、外层 79-a 条件未接手 →
  **cond=-1 无条件释放孤儿**——EOF 态载荷=错误码 i32 部分写（4B/8B）+栈垃圾被读。
  正常轮先行的栈残留（clean shadow）使多数场景**侥幸不报**（巧合正确又一例）。

**4. 根治**

- collectOwnedStrFields 的 isResult/isOptional 判定补 `SemanticAnalyzer::isResultType/
  isOptionalType`（语义层单点判定·认尖括号形态）——两形态同归 79-a 条件释放
  （先 Load 是否正常，真才 free——Rust Result 判别式 Drop 同构）。
- **写侧裁决**：`错误(E)` 对联合槽按 E 宽部分写**维持**（Rust enum 同构：Err 分支不初始化
  Ok 载荷=常态，判别式守卫防读）——修读侧判据后载荷永不被错误态消费。

**5. 验证链**

- v2p valgrind：123_v2_容器 **6→0**、/tmp/a7work/探针.cn **8→0** ✓
- EOF-first 探针（空文件首行即 EOF）宿主 rc=7 ✓
- 反证：回退判定补丁 → valgrind 精确复现 6 errors/2 contexts ✓ 恢复→0 ✓
- 单测 1327/1327 + 全量 E2E + 锚定链（收工回填）+ 双门禁

**6. 与他机交叠分析**

- 写集单文件（ir_fields.cpp）——与家机（ASan/DWARF）/单位机（mem2reg/D8）零交叠；
  看板空闲（本机持锁）。修复为读侧守卫收紧（错误态不再 free）——产物行为面=
  「错误态结果局部出口不再释放载荷」——无 E2E 判据依赖旧行为（全量 E2E 复跑覆盖）。

---

### 2.2 上一轮（**234-a**）收工存档（要点）

- **A7 本体根治收口**（两层）：①共享 AST 写回注记跨实例污染（recheckGenericMethodBody
  生成前重放+Diagnostics 快照回滚）；②emitStructWholeAssign 补复制调用源分派（收窄）。
  stdlib 六处切换重新应用；全量 E2E 313=311/0/2·锚定链 410460 不变·valgrind 切换态 6 errors
  （A7 差值 7 归零）；反证双件。新立案 D10/D11。详情=plans/020 第七十五节、提交 1503c2a。

