# 025 三机任务统筹与实施计划（在飞任务明细唯一入口）

> **建立**：2026-09-15（第一百八十五轮·单位机 ARM64；依据用户令——「新开的任务线必须把详细的实施步骤
> 落实在项目 `plans/` 里，让 3 个 AI 紧密协作」）。
>
> **定位**：三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机 ARM64）**在飞任务**的**详细实施计划**统一落盘处。
> 回答四个问题：**谁在做什么（写集）／怎么做（步骤）／是否打架（交叠分析）／有无遗漏（欠账与边界）**。
>
> **三者分工（勿混用）**：
>
> | 文件 | 角色 | 更新时机 |
> |---|---|---|
> | `三机任务看板.md` | **锁 + 一行状态**（push 即原子 CAS；**开发锁全表至多一机**——231-a 用户裁决恢复唯一性） | 开工认领 / 收工释放 |
> | **本表（plans/025）** | **在飞明细**：六要素详细计划 + 交叠分析 + 欠账 | 开工落计划 / 收工回填（本机节滚动替换） |
> | `plans/021` | **全局台账**：队列 / 销项 / 里程碑（只增不删） | 认领的开发轮收尾 |
>
> **强制规则**（AGENTS.md §8.6，2026-09-15 用户令）：**未在本表本机节落盘详细实施计划的开发轮，不得开工**；
> 计划**六要素**缺一不可（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／与他机交叠分析）。
> **每机只写自己节**（§8.3 分区同款），他机节只读；转写或修正他机节须在本机节注明理由并随认领提交说明。
>
> **最后更新**：第 256 轮（2026-09-16，单位机 ARM64）——256-a 收工回填：F1-26 方案 A 第一步落地（Phi 降级+使用点重写+验证器升级·单测 4/4·384 双形态）；下一轮预登记 257-a。

---

## 〇、三机在飞总览（每机只改本机行）

| 机器 | 轮次 | 任务（一句话） | 写集（精确文件） | 与另两机的关系 | 计划节 | 状态 |
|---|---|---|---|---|---|---|
| 家机 win-x64 | **662-a（已收工销项·021 任务 050=568-a runner 遗欠恢复·行为面/文档分离集成）** | run_e2e.py 四档超时防线 31 调用点+内存路径超时补真+check_registry 补 647-a 漏注册；集成 dda79f86·明细=021 §4.2+看板 662 广播 | run_e2e.py+check_registry.json（行为面）+本文档 §〇（本行） | 拷贝构造系（v2 树）零交叠；§1.0=668-a 在飞记录未动 | §〇 | ✅ |
| 深度机 linux-x86_64 | **🧪 314-a M3 采样轮·x64l 列** | 第五新种子域 310000001 差分 500+负向 400（310100001）+回归轨 114 全库+T44 x64l 跨机复验（t44scan/ext/if2）+日志日文件制首写 | 看板本机行、plans/025 本节、HANDOFF/更新日志深度机节、tests/cnsmith_hits/日志/2026-09-17.md（新）、命中样本（如有）、plans/021（T44 行证据回填·最小局部） | 家机 313-b 🏃（daemon/日志拆日写集）互认零碰；日志日文件与其拆日同向；单位机 ⬜ 空闲 | §2.1 | 🧪 在飞 |
| 单位机 ARM64 | **328-a（开发轮·已收工·分支 任务/单位机-328-协议v2 已集成 e6e22aa）** | 三机并行协同协议 v2 落地（用户批准）：分支即开发+integrate.py 队列集成+缺陷分级+分层 try-build+文档随分支走——AGENTS §8 重写/plans/027 方案/check_handoff 扩分支名校验 | 见 §3.18 收工回填 | 家机 326-a 收工与本案真实碰撞一次（rebase 消解=v2 首个实战案例）；深度机 ⬜ 零交叠 | §三.18 | ✅ 收工 |

> **已完成**：**187-a**（单位机，规则与统筹落盘轮——`AGENTS.md` §8.6 + 本表建立 + 交叠/遗漏审计）；
> **188-a**（单位机，D6 B11 变量常量传播增强 + ARM64 跨机复验欠账收口——详见 §三.2/§三.3）。
> **轮次号更正记录**：本机该轮曾以「185-a」提交推送——但 185-a 已被深度机（`cd9dce9`）先占，本机首次 push
> 被拒即**认领无效**，按协议应重取号；此后更正为 **187-a**（本轮）=规则轮、**188-a**=D6 轮（深度机连占 185/186-a）。

**下一轮预登记**（防两机撞同一队列项；开工时以看板最新状态为准——被他人先占则本机顺延队列中未占项）：

| 机器 | 预登记轮次 | 任务 | 依据 |
|---|---|---|---|
| 单位机 ARM64 | **258-a**（下一轮候选·待认领） | ①**A12 里程碑标签锚点**（B6 已批·轻量可搭车）②F1-26 后续子项（激进 SSA 管线化/mem2reg 全量·性能主线）③F2-33 拆分之三 迭代器 ④D28/D29 根治专项；257-a 已收工（§3-B 铁律根治） | 计划=开工时落盘本表 |
| 家机 win-x64 | **450-a（下一轮候选·待认领）** | 依 plans/021 §3-A 队列：M3 win 列采样（用户令不停）/清零波次余项/C20 批C·D parse 桶上架——开工以看板最新为准 | plans/021 排班+看板 |
| 深度机 linux-x86_64 | **414-a（下一轮·D42 甲案波 1）** | **泛型类源码编译全链·波1=解析+语义层**（2026-09-19 用户裁决「同意甲」·plans/021 §三-A D42甲行）：①v2 解析器 `类 名<T>` 类型参数表（解析类型文本/泛型闭合位置 既有 P7b 泛型函数设施复用）；②实例类型合成名（名$T$…·与泛型函数同款）；③语义检查类表挂接；④探针=v2p 编译 含 `类 盒子<T>` 源码不崩+实例声明语义通过。波2（后续轮）=IR/生成单态化；波3=stdlib 源码加载路线+390 根治 | 395-a 破案=plans/025 §2.46·呈报三案=plans/021 B2（已迁 §三-A） |

---

> 〔495-a 段标题占位行已随 529 开工滚转（2026-09-20·内容 git 可考）〕

## 一、家机 win-x64 节（本机维护）

### 1.0 收工回填（**672-a=缺陷本体终案精化：M2 全字段变体破解错位模式=副本「键数组正常·值数组内容=垃圾指针」〔名ID/类型ID/种类/附加 全垃圾〕——拷贝构造的键数组拷贝正确·值数组拷贝缺失/错位——指向 stdlib 映射拷贝构造的**值数组处理**〔V=32B 结构体的元素拷贝发射〕**·wt628 树·2026-09-23）

**M2 全字段实证**（994_m2全字段·已删）：副本.获取(7) 的 符号 全字段——
| 字段 | 宿主 | v2 |
|---|---|---|
| 名ID | 7 | 549770492480（垃圾指针量级） |
| 类型ID | 700 | 0 |
| 种类 | 1 | 549770513504（垃圾） |
| 附加 | 9 | 549770448632（垃圾） |

**破解**：副本的**键数组正常**（获取(7) 能按键找到=键拷贝正确）·**值数组内容=垃圾指针值**——拷贝构造的**键数组拷贝正确·值数组拷贝缺失/错位**——指向 stdlib 映射拷贝构造的**值数组处理**（V=32B 结构体的元素拷贝发射）。

**下轮专项**：读 stdlib 映射集合.cn 拷贝构造全文（L133~178 槽大小钳制+内部数组分配/拷贝）+对照 v2 发射——值数组分配/元素拷贝的字节数与指令定位→修复→113/79_v2 清偿。

**051 主线**：甲方向维持〔基准=019〕。

**写集**：021/025/看板/交接/更新日志；994 探针已删；v2 树零残留。

### 1.0 【673-a 在飞→收工回填】映射值数组双缺口根治（分支 任务/家机-673-映射值数组拷贝·wt628 复用·基线 develop 323fe3b7·2026-09-23）

**1. 目标与验收判据**：051 主线=31/155 根因底层两缺口根治。验收=M2 探针（纯映射拷贝构造 V=符号 32B）v2 侧四字段与宿主逐字段一致（7/700/1/9）；实测=修复后 v2 产物 m2c.exe rc=0 全对 ✓。

**2. 写集**：CN语言编译器v2/IR/生成表达式.cn（成员下标分支解引补全·+18 行）；CN语言编译器v2/IR/生成表达式求值.cn（生成装箱构造结构体载荷深拷·+10 行）；文档四件（025/021/交接/更新日志）。回填=与预登记一致（本轮开工直推看板先行，六要素随收工补落盘——纪律瑕疵已注记）。

**3. 实施步骤（已完成）**：
  ① M2 探针重建（target/m2probe/主1/主2.cn·单元素+原表对照双变体）+v2p 重建链打通（ninja 宿主→cn build v2 树→v2p.exe→ml64→link runner 同款 rsp→run）；
  ② v2p --verbose IR dump→asm 逐条对读：破案第一缺口=生成下标左值成员下标分支对「成员链尾字段 T\* 指针」不解引（基址=字段槽地址+i×步进·野写壳区）——444-b/433-b 同族第三缺口；修复=推导成员链类型ID 尾缀 \* 判定→IR_指针加载 解引（成员+下标两形态统一·推导 miss/非指针原路零回归）；
  ③ 原.获取 对照反转定性：原表值数组内容本身就垃圾→真根因第二缺口=生成装箱构造（71-a）载荷写入 IR_指针存储 8B 单槽——『返回 正常(值数组[位置])』的载荷实参=结构体地址语义（r22），8B 把地址当值存；修复=类型名宽度(值类型)>8 走 IR_结构体拷贝（op=61·源=o1 实参地址·目标=o2 载荷地址·rcx=宽度），≤8/未知=标量直存原路。

**4. 验证链**：M2 探针 v2 产物 rc=0 四字段全对 ✓（宿主对照 rc=0 全对）·996/113/497/127_v2 复测=段错误（行为从静默垃圾转显式崩溃·写入已激活·外层=类字段容器链缺口 662 计划主线未清）；全量 ci.ps1 跑动中（回填红面变化）。

**5. 边缘情况与诚实边界**：①装箱构造载荷宽度>8 假定实参=地址语义——结构体字面量/下标读/成员读均地址语义 ✓ 实测覆盖；标量/字符串(8B) 原路。②成员下标解引只对尾缀 \* 生效·内联数组字段 [N] 原路。③996/113/497/127_v2 段错误未清——类字段容器链（662 轮 op=44 发射修正主线）是下一层缺口，本轮两修复是其底层前置。④回填实测：全量门禁红面见收工提交。

**6. 与他机交叠分析**：深度机 550（T94 指针字段阶段二呈报态）写集=语义/呈报文档·与本轮 v2 IR 生成两文件零交叠；单位机无在飞。独立 ✓。

### 1.1 【674-a 在飞→收工回填】类字段容器链第一缺口修复+sret ABI 面立账（分支 任务/家机-674-类字段容器链·wt628 复用·基线 develop 8ca8ef75·2026-09-23）

**1. 目标与验收判据**：113/996/497/127_v2 段错误家族=类字段容器链缺口（662 op=44 主线）定位修复。验收=996 步号插桩版通过「追加」语句（实测 C1/C2 全出 rc=0·修前静默崩）。

**2. 写集**：CN语言编译器v2/IR/生成表达式字段.cn（生成字段访问 的容器壳字段判定·+20 行）；文档四件。回填=一致。

**3. 实施步骤（已完成）**：
  ① 996 插桩二分定位：变体 A（持有 类构造/析构/传参）rc=0 ✓；变体 B（追加）rc=0 ✓（674 修复生效）；变体 C/D（元素(0)）仍 0xC0000005；
  ② IR dump 实证第一缺口=生成字段访问 对「类字段容器内联壳」无条件按宽度加载首 8B（向量壳=数据指针）作 this=截断壳（662 定性实锤·但发射点=生成字段访问 L145 非 662 预判的 生成方法接收者）——修复=97-a 字段数组同款「压字段地址不加载」判定扩容器类字段（剥 */& 尾缀后 是容器类 → 字段是容器壳674）；
  ③ 元素(0) 残余崩点定性=**更深层 sret ABI 面**：返回结构体的方法调用参数位序约定（调用方 lea rcx=retbuf+实参 rdx/r8 vs 被调 IR 槽序 1=sret/2=this/3=i）在「this=字段地址」新形态下的装配正确性待专项验证——IR/asm 双层已对读（元素 被调方 IR 参数槽自洽·调用方 op=63 类型=1=retbuf 槽1·lea rcx 发射判据=代码生成 L728 指令.类型!=0）——**插桩教训**：IR层诊断 计入错误计数（23 条签名诊断=假「IR 层错误 23 个」）。

**4. 验证链（回填·全量门禁否决试修）**：996 插桩版追加 语句 rc=0（修复生效）✓；全量 528=519/9——**4 新红（147_v2/283/441/442）=试修回归**（容器字段「值读取」消费方被压地址破坏·283=字段容器接收者专项用例）→**试修已回滚**·回滚后抽验 283/147_v2/441/442 全 PASS 复绿 523/5 ✓。**下轮正确方案**：语义/解析层给「X.字段.方法()」的中间成员节点标 成员方法标记（现仅局部变量 `表.追加` 标记→走生成方法接收者·类字段链误入生成字段访问）→接收者通道内做壳地址 this（该通道 L70-81 块）→值读取消费方零扰动。

**5. 边缘情况与诚实边界**：①容器壳判定只对「字段类型文本（剥尾缀后）是容器类」生效——用户类字段（非容器）的 this 语义不变；②元素(0) 残余崩=sret ABI 面（调用方/被调方参数位序在新 this 形态下的全链一致性）·已对读 IR/asm 双层定性待修；③IR层诊断=错误计数器·插桩污染编译退出码（本轮假信号教训入册）。

**6. 与他机交叠分析**：深度机 550 呈报态写集=语义/文档·零交叠；单位机无在飞。独立 ✓。

### 1.2 【675-a 在飞→收工回填】方法接收者成员链分支=壳地址 this（分支 任务/家机-675-方法标记壳地址·wt628 复用·基线 develop eee77f15·2026-09-24）

**1. 目标与验收判据**：051 主线=类字段容器方法接收者走生成方法接收者通道做壳地址 this（值读取零扰动）。验收=996 探针 `h.表们.追加` 的 this=op=46 字段地址（IR 实证 ✓）+追加 语句全链通过+全量门禁红面不劣化。

**2. 写集**：CN语言编译器v2/IR/生成表达式字段.cn（生成方法接收者 成员链对象分支+壳判定·+40 行）；CN语言编译器v2/IR/生成表达式.cn（调用点签名同步·1 行）；文档四件。回填=一致。

**3. 实施步骤（已完成）**：
  ① 修正 674 结论：`h.表们.追加` 实际**走生成方法接收者通道**（解析层成员名后跟「(」即标 成员方法标记——674 误判走了字段访问；接收者 L25/L52 的对象解析只认标识符/调用形态，成员链对象（h.表们）落空→this=生成字段访问 已压的字段首 8B 右值）；
  ② 生成方法接收者 新增成员链对象分支：解析成员链结构体ID(对象前缀链)→查字段偏移→生成左值地址(前缀链)=基址→复用 L70 壳判定（字段类型675 判定补成员形态：查字段类型ID(前缀, 末段)·剥 */& 后 是容器类 → this=字段地址）；
  ③ 三处实错修正：字段名=对象节点m.文本（非 n.文本=方法名）；所属结构体=解析成员链结构体ID(对象节点m.子起点)（前缀链·非成员节点自身=字段类型）；链结构体675 声明提升函数头（CN 块级作用域——分支内声明跨块不可见）；
  ④ IR 实证：追加 的 this=op=40(Load h)+op=46(字段地址) 直达·原 op=44 截断加载消除 ✓；生成字段访问 右值序列原样保留=值读取零扰动 ✓。

**4. 验证链（回填·试修再被否决·零代码收口）**：IR 实证=追加 的 this=壳地址（op=44 截断消除）✓；但全量 528=519/9——**147_v2/283/441/442 四新红再现（283=字段容器接收者专项用例·基线绿）**→试修回滚·抽验四用例全 PASS 复绿 523/5 ✓。**关键新事实**：基线态 283（字段容器接收者·**局部对象** 甲.记录.追加）绿、996（**参数对象** h.表们.追加）红——二者 this 形态差异未被 674/675 两版一刀切捕获。**下轮第一步=宿主 283/996 asm 权威对照**（宿主对两种字段容器接收者各发的 this 形态逐例实锤）再定方案；sret retbuf 槽分配面（元素(0)·变体 F 丢弃返回值仍崩/G 非 sret 不崩）并行待查。

**5. 边缘情况与诚实边界**：①壳判定只影响方法 this 位·赋值/初始化/深拷消费方走原右值通道零扰动（674 一刀切教训已吸收）；②元素(0) 残余=retbuf 槽分配（累计retbuf字节数 机制的覆盖面）——下轮 676 主攻；③IR层诊断 计数器污染教训持续有效。

**6. 与他机交叠分析**：深度机 550 呈报态·零写集交叠；单位机无在飞。独立 ✓。

### 1.3 【676-a 收工回填】宿主 283 asm 权威对照实锤（分支 任务/家机-676-宿主asm对照·wt628 复用·基线 develop 204a0cab·2026-09-24）

**对照结论（asm 逐条·target/m2probe/h283.asm L175-213）**：宿主对 `甲.记录.追加(转移(值))` 发射 **call 追加 前三装载=rcx=retbuf·rdx=[壳+0]（壳内数据指针）·r8=壳地址**——宿主 ABI=追加(retbuf, 数据指针, 壳地址) 双形态参（扩容时经 r8 回写 数据/容量）。**基线 283 绿=假绿**：v2 调用方传 this=壳首 8B（恰=数据指针·与宿主 rdx 同形态）+v2 被调 stdlib 追加 按 this=壳地址读 .数据/.元素数量——错位读+错位写双错抵消·内存差值断言碰巧=0。**v2 全链自洽方向=壳地址**（v2 stdlib 追加 实现按 this=壳访问字段——674/675 方向正确）；675 试修后 283 崩 0xC00000B4=追加 本体已工作后的**块出口 drop glue/retbuf 生命周期**新面（下轮=283 崩点插桩二分：追加后/块出口/析构 三段定位）。

**写集**：021/025/交接/更新日志/看板（纯文档·探针资产 h283.asm 在 target/ 不入库）。**下轮接力**：①283 崩点插桩二分（追加后/出口/析构）②元素(0) retbuf 槽分配面（累计retbuf字节数 覆盖面）③四红清偿→零红→借链终章→001 销项。

**交叠分析**：深度机 550 呈报态·零交叠；单位机无在飞。独立 ✓。

### 1.4 【677-a 在飞→收工回填】壳地址 this 落地+283 形态二分（分支 任务/家机-677-壳地址落地·wt628 复用·基线 develop 6b7212c5·2026-09-24）

**1. 目标与验收判据**：落地 675 修复面（生成方法接收者成员链分支+壳地址 this·676 asm 实锤 v2 自洽方向）+283 崩点形态二分。验收=996 插桩追加通 ✓+283 形态①② rc=111 全通（含内存差值断言=0 ✓）。

**2. 写集**：CN语言编译器v2/IR/生成表达式字段.cn（接收者成员链分支+壳判定重落地·+55 行）；CN语言编译器v2/IR/生成表达式.cn（调用点签名同步）；文档四件。回填=一致。

**3. 实施步骤（已完成）**：
  ① 675 修复面重落地（签名扩块计数/槽计数+函数头声明提升+成员链对象分支+壳判定补成员形态）；
  ② 283 形态递进二分：形态①（转移·局部值）rc=111 全通（追加后/块出口/析构/内存断言全对）✓；形态②（字符串复制实参）rc=111 全通 ✓；**形态④（字面量 "字面量" 实参）崩 0xC00000B4**（CRT 非法参数类异常·非硬件段错误——真修复暴露的真实缺陷面：字面量借用通道的 归一化入容器实参/释放面 在壳地址 this 下）。

**4. 验证链（回填·全量 528=519/9 净增 4 红=假绿转真红·develop 恒绿不可集成·已回滚复绿 523/5·修复面完整落盘待下轮重落地）**：形态①② rc=111 全通含内存断言=0 ✓✓（674/675 方向二次实锤）；**形态④（字面量）崩 0xC00000B4=第二缺口定性：字面量驻留串入容器后块出口释放面不空安全（追加 内部 数据[写位]=值 直存驻留句柄→块出口 记录 析构 free(驻留)→CRT 异常；宿主 283 注释明言「驻留借用（释放侧空安全）」=宿主 __cn_str_free 对驻留空安全而 v2 释放链未等价）**。IR 证据：追加 的 this=op=40(Load 甲)+op=46(字段地址) 壳地址 ✓·字面量实参 op=3(驻留) 直传 ✓·返回 16B 拷贝 ✓·op=62 析构 ✓——调用链全对·崩点=释放面。

**下轮重落地清单（028 轮直接续作）**：①重落地 677 修复面（本节 §3 三处实错修正版·生成方法接收者签名扩块计数/槽计数+函数头声明 链结构体675+成员链对象分支+壳判定补成员形态——全部代码已在本节 §3 与 git 677 分支历史·任务/家机-677-壳地址落地 未含修复提交·修复代码随本回填描述重写）；②修形态④：v2 释放面（__cn_str_free 调用链）对驻留串空安全（对照宿主 io_api __cn_str_free 实现的驻留判定）；③元素(0) retbuf 槽分配面；④四红清偿→零红→借链终章→001 销项。

**5. 边缘情况与诚实边界**：①形态④字面量崩=0xC00000B4（CRT 类·字面量借用通道）——下轮专项；②元素(0) retbuf 槽分配面并行；③996 插桩追加通·元素(0) 残余（同 retbuf 面）。

**6. 与他机交叠分析**：深度机 550 呈报态·零交叠；单位机无在飞。独立 ✓。

**【678-a 回填（2026-09-24）重落地+形态④深挖·恒绿回滚】**：①677 修复面重落地完成（四部分与 677 一致·IR 已验证 this=壳地址）；②283 消融二分=c1（无追加·构造/出口/析构）rc=111 全通——**崩点锁死=追加("字面量") 调用本身**（0xC00000B4 NTSTATUS 类·stderr 空·cn_free_tracked 已空安全排除释放面·字面量常量 strData_59 落地正常·追加 内部 cn_realloc/movsb 链待 NTSTATUS 级定位）；③全量净增 4 红=假绿转真红·恒绿纪律回滚复绿 523/5。**四轮循环教训（674/675/677/678）=修复面+残余缺陷必须同轮修完集成——679 一次性修完**：壳地址面（已验证）+形态④（cdbg/逐句消融 NTSTATUS 定位）+元素 retbuf+147/441/442 →四红清偿→零红→借链终章→001 销项。

**【679-a 回填（2026-09-24）消融再进一程·恒绿回滚】**：①677 修复面重落地 ✓（0 错误）；②消融：c2（字面量赋局部）rc=111 ✓·c3（追加(s2 中转)·同 .rdata 形态）rc=111 ✓——**崩面最终锁死=追加("字面量") 字面量直传实参形态**（差异在直传求值/归一化路径·非释放面非追加本体）；③全量净增 4 红=假绿转真红·恒绿回滚复绿 523/5。**680 修法**：归一化入容器实参 的字面量直传路径（74-a 对 字面量应插 字符串复制 拥有化或标记借入+释放跳过——c 的 IR 无复制插桩=归一化 miss）+壳地址面（重落地脚本即 677/678 版）+元素 retbuf+147/441/442 一次性集成。

**【680-a 回填（2026-09-24）形态④终局消融·恒绿回滚】**：c6（追加("字面量") 后立即返回·跳过析构时点）仍崩 0xC00000B4=**追加(.rdata) 调用本身**实锤；对照=同形态 .rdata 经 s2 Load（c3）通·直传 lea（c）崩；环境无 cdb·NTSTATUS 级定位待装调试器或逐行 asm 单步（追加 316 行内的 cn_realloc+movsb 链）；全量净增 4 红=假绿转真红·恒绿回滚复绿 523/5。**681=装 cdb（winget Microsoft.WinDbg）或逐行 asm 单步定位追加(.rdata) 崩点+壳地址面+元素 retbuf+147/441/442 一次性集成**。

**【681-a 回填（2026-09-24）微型调试器实锤·架构轮立案】**：自建 mini_dbg.c（DEBUG_EVENT 循环·target/m2probe/ 可复用）定位形态④=**真实异常 0xC0000374 堆损坏**（无调试器时 0xC00000B4 为 WER 转换码）·崩在 ntdll realloc 链=追加 扩容写回堆损坏。**定性升级：壳地址 this 修复触碰 v2 容器 ABI 系统性缺口（retbuf 帧规划/字面量拥有化/壳布局写回的全链一致性）——单点 IR 补丁不可收敛·立案 001-001 架构轮**（Rust 对照=类型即布局·布局即 ABI·容器方法 ABI 单一化：this=壳地址 or 三参形态全链一致+retbuf 槽纳入帧规划+字面量借用拥有化通道）。682=架构轮方案设计（对照宿主 x64 容器 ABI 全景）→分波实施→四红清偿→零红→借链终章→001 销项。

### 1.6 【682-a 架构轮方案设计（零代码收口）v2 容器方法 ABI 单一化（分支 任务/家机-682-架构轮设计·基线 develop 6e881aac·2026-09-24）

**四维定案（676 asm 实锤+674~681 证据链）**：
1. **this 形态**：唯二合法形态=局部容器壳地址/字段容器壳地址（语义同一=壳地址）；v2 stdlib 全部容器方法按 this=壳地址访问 .数据/.元素数量/.容量——调用方三通道（生成方法调用/生成方法接收者/生成字段访问）归一为壳地址 this（生成字段访问保持右值语义不动——值读取消费方零扰动）；
2. **retbuf**：返回结构体的容器方法调用，retbuf 槽=帧规划一部分（累计retbuf字节数）；波3 审计槽冲突与帧界；
3. **字面量**：74-a ①借用直存+释放侧空安全（cn_free_tracked trackedUnregister miss 跳过——已满足）；
4. **壳写回**：扩容回写仅经壳地址（this=数据指针形态无回写通道=宿主双形态参的 v2 等价解）。

**分波计划**：**波1**=壳地址面重落地（677/678 已验证脚本·IR/asm 双实证）；**波2**=形态④堆损坏断点式定位（mini_dbg 升级 int3 断点+寄存器 dump·或逐行 asm 单步·追加 316 行 cn_realloc/movsb 链）；**波3**=retbuf 槽分配审计（元素(0) retbuf 槽冲突/帧界）；**波4**=147_v2/441/442 消费链复验（依赖波1 全链一致）→四红清偿→零红→借链 51 例→001 销项。**实施序**=波1+波3 同轮（IR/代码生成层）→波2（调试器层）→波4（E2E 复验）。

**交叠分析**：深度机 550 呈报态·零交叠；单位机无在飞。独立 ✓。

**【683-a 回填（2026-09-24）波1+波3 完成】**：波1 壳地址修复面重落地 ✓（0 错误·IR 实证同 677）——**已提交分支 任务/家机-683-波1波3（分支保留待波2/波4 同集成·develop 不含=恒绿维持）**；波3 retbuf 槽审计=**机制自洽无缺口**（代码生成共用 L200-237：槽基数=最大槽·retbuf 区追加变量槽区之上·retbuf槽位数=max(16B,字节数)/8 对齐·共享单缓冲幂等·帧=(最大寄存器+槽)*8+8 对齐）——**形态④堆损坏的根源不在 retbuf 槽分配**·嫌疑收窄至追加 内部 cn_realloc 链或波1 修复面之外的堆写。**684=波2 断点式定位**（mini_dbg 升级：int3 断点+CONTEXT 寄存器 dump·追加 内部 cn_realloc 前后单步）→波4 复验→同集成分支。

**【684-a 回填（2026-09-24）波2 定性+归一化拥有化修复·283 清偿】**：形态④崩因定案=归一化①「字面量驻留借用直存」的 .rdata 句柄与堆写回/realloc 链不安全组合（680 c6/c3 消融+681 mini_dbg 0xC0000374 实锤）；修=IR容器判定.cn 归一化① 删提前返回·字面量落 ③ 拥有化复制（__cn_str_copy·Rust &str→String::from 同构·一字面量一次拷贝）——**形态④ rc=111 全通含内存断言=0 ✓**；全量 528=520/8（283 清偿 9→8）+单测 1405。**剩 8 红=113/127_v2/497/79_v2 撤守卫基线红+147_v2/441/442 消费链（rc=1 逻辑错·同根=赋值/引用族·波4）+524=D12**。685=波4 三例定位（rc=1 语义/引用族）→零红→借链终章→001 销项。

**【685-a 回填（2026-09-24）波4 441 精确定位】**：441 A3 `盒.项 = 新表`（重填字段(表盒& 盒) 内·成员链左值容器整体赋值）IR 实证=字段写通道发 **op=45 8B 句柄写**（op=60 分配新壳 24B+op=63 拷贝构造(新壳←新表)+op=45 新壳句柄→盒.项 字段槽）——**句柄写破坏 盒.项 的 24B 壳布局**（.数据=子壳句柄·.元素数量=旧值 0）→A3 盒大小=0+元素(0) 越界（运行时错误 2）。**修法=字段写通道对容器类字段改 op=61 24B 壳拷贝**（源壳→目标字段槽·含旧数据数组释放）。147_v2/442 同族（赋值/引用族）待同法。685 上下文极限·686 续作三例清偿→零红→借链终章→001 销项。

**【686-a 回填（2026-09-24）架构轮前置收口·恒绿恢复】**：revert 波1 壳地址修复面（1e39983a）·283/147 抽验复绿 ✓·全量基线 523/5 恢复。**001-001 架构轮=独立完整任务立案**（壳地址面+驻留拥有化+retbuf 帧+消费链系统性方案·025 §1.6 四维定案+674~685 十二轮证据链+mini_dbg 调试器+283/996 探针矩阵+宿主 asm 权威对照 h283.asm 全部齐备·待完整会话一次性实施→四红清偿→零红→借链终章→001 销项）。673-a 双缺口根治（aa9fac02·M2 全绿）为本主线已集成核心产出。

**【687-a 回填（2026-09-24）方向性修正（重要）】**：重审 674~681 全部 IR/asm 证据链=**基线的 op=44 句柄加载本就正确**（v2 容器字段槽=8B 句柄→堆壳·this=Load 字段槽=堆壳地址·stdlib 按 this=堆壳访问 .数据/.元素数量 全链自洽；M2 映射 56B「壳」=堆对象壳非字段内联壳——674/675/677 的「字段槽 24B 内联壳+壳地址 this」模型=误判）——「op=44 截断壳」定性错误·「壳地址 this」试修三次被门禁否决=门禁正确。676 宿主 asm 的 rdx=[壳+0]（数据指针）恰与 v2 句柄加载同形态=表象吻合误导。**996/113/497/127_v2 基线红真因面=元素(0) 的 sret/retbuf 链**（677 插桩 C 段崩·c3 对照·与 this 形态无关——基线态也崩）·专项=op=63 retbuf 位0 装载（代码生成 L728 指令.类型!=0 判据）与被调参数槽序（1=sret/2=this/3=i）的对照定位。**已集成真产出（不受影响）=673 双缺口（aa9fac02）+684 拥有化（efb10cc7·283 清偿）**。687 收口·sret 专项待完整会话。

**【688-a 回填（2026-09-24）sret 链三重实锤】**：mini_dbg v2（继承控制台+全事件循环）+禁 ASLR 重链（/DYNAMICBASE:NO /FIXED·基址 0x140000000）定位 996 形态 C=**READ [null+0x20]**（异常地址 0x14000439C·固定偏移 0x439C）——0x20=映射壳+32=**桶数字段**=获取(7) 的 this=0（层 局部句柄=0）——**元素(0) retbuf→层 的接收链断**（56B retbuf 未正确落到 8B 句柄槽或句柄未写：IR 显示 op=61 r22→r23（取地址 槽13）56B 直接拷进句柄槽=踩相邻槽·且句柄未指向有效堆壳）。**689 修法=retbuf→句柄模型局部变量 的接收三步**（op=60 分配堆壳 56B+op=61 retbuf→堆壳+句柄 op=45 存 层 槽）→四红清偿→零红→借链终章→001 销项。mini_dbg v2 资产=target/m2probe/。

**【689-a 回填（2026-09-24）波4 根因终局定性·模型冲突】**：**容器局部变量声明通道=壳内联模型**（`映射 层 = h.表们.元素(0)`→生成声明.cn L757 结构体三形态·层=7 槽 56B·retbuf op=61 拷入槽区+取地址消费）**vs 方法接收者通道=句柄模型**（层.获取(7) 的 this=Load 层 槽=句柄→堆壳）——**同变量两模型混用**：句柄模型读 [堆壳+32=桶数] ✓ 但声明通道写的是 retbuf 56B 进槽区→槽首 8B=.数据指针→句柄模型 Load 槽=数据指针→获取 内部 [数据指针+32]=数组越界（运行时错误 2 与段错误同根·441/147/442 同族）。**修法=容器类局部声明统一句柄模型**（槽=8B·初值 retbuf→堆壳（op=60 分配 56B+op=61 拷贝）+句柄 op=45 存槽·与构造初始化 op=60 通道一致——动槽分配/登记/取地址全域=架构级）。已完整落盘待专项会话实施。

**【688-b 回填（2026-09-24）宿主 ABI 颠覆性对照】**：宿主 996 asm（h996s.asm L207-242）实锤——**宿主元素 ABI=`元素(this=rcx=[壳+0] 数据指针, rdx=i) → rax 返回 8B 句柄·无 sret 无 retbuf`**；v2 元素 ABI=`元素(rcx=retbuf, rdx=this=壳, r8=i) → sret 56B`——**两模型根本不同**（宿主=数据指针+句柄返回·零拷贝零 retbuf；v2=壳+retbuf 拷贝）。**基线四红真因=v2 的「元素 sret 56B」与下游句柄模型接收的兼容缺环**（689-a 定性·688 实锤）。**修法二选一**：A=v2 元素 返回改 8B 句柄（去 sret·对齐宿主·零拷贝高性能——**推荐**·单点改 查方法签名 返回形态+发射成员取值 压句柄）；B=句柄模型接收链完整化（688 三步·已试未生效）。A 待下会话实施。

**【690-a 回填（2026-09-24）修法 A 呈报·语义级待裁决】**：修法 A=**v2 元素 返回形态改 8B 元素地址**（去 sret·对齐宿主 688-b asm 实锤——宿主元素(this=数据指针, i)→rax 8B 地址·无 sret）——**涉及调整 639-a 的 sret 翻转**（628 波1 用户裁决链产物·语义级→呈报待裁决）。实施面=IR容器.cn L1079-1087 查方法签名 返回形态（结构体→标量句柄）+下游消费方适配（生成方法调用 目标寄存器语义/接收声明通道）。**值语义目标不变**（628 裁决的深拷值语义由 调用方 op=61 拷贝 56B 承担·仅返回形态实现路径由「被调内拷 retbuf+调用方再拷」双拷改为「返回元素地址+调用方单拷」=省一拷·性能对齐宿主）。基线四红清偿/零红/借链终章/001 销项随裁决后实施。

**【691-a 回填（2026-09-24）MAP 符号定位·因果链终局】**：link /MAP+禁 ASLR 定位=异常偏移 0x339C（段内）落在 **映射$整64$符号$映射 拷贝构造** 内 +0x3f——READ [null+0x20]=**源.桶数读·源=0**。上游因果链=元素(0) 声明初始化的 生成表达式 **静默返回 0**（嫌疑=净栈契约出口校验失败·生成表达式 L496-498 `出口深度==0 → 返回 0` 无诊断）→生成容器局部 返回接收分支「返回值reg != 0」不成立→**层 槽保持 op=41 零初始化=0**→层.获取(7) this=0→READ [0+0x20] 段错误。**689 修法候选**：①返回接收分支 对 返回值reg==0 **fail-fast 诊断**（消灭静默·先做）②净栈契约校验失败的根因定位（h.表们.元素(0) 求值的栈不平衡点·生成方法接收者 弹对象值+字段取载重发基址 与 计数的交互）③修复后 996/113/497/127_v2 清偿。mini_dbg+MAP 文件定位法可复用（target/m2probe/）。

**【693-a 回填（2026-09-24）净栈假设证伪·终态固化】**：692-a fail-fast 诊断未触发=返回值reg≠0=「生成表达式静默返回 0」假设证伪——元素(0) 接收链的 READ [null+0x20] 另有写点·需 mini_dbg 断点式升级（int3 写入+CONTEXT 寄存器 dump 逐指令）或宿主/v2 元素 ABI 全景表对照深定位。develop 恒绿 523/5 固化（692-a fail-fast+673-a 双缺口+684-a 拥有化 已集成）；基线红 113/127_v2/497/79_v2（sret/retbuf 链·T70 残余立案）+524=D12 维持。续作=断点式深定位→四红清偿→零红→借链 51 例终章→001 销项（工具/证据链 025 §1.4~§1.7+target/m2probe/ 齐备）。

**【694-a 回填（2026-09-24）断点式定位实锤】**：mini_dbg v3（int3 写入+MAP RVA+CONTEXT dump·target/m2probe/mini_dbg3.c）定位 996 形态 C 崩点=**元素 PROC 序言 push rbp（偏移 0x1BB1）WRITE at FFFFFFFFCDBFFB10 非规范地址**=**调用方 rsp 在 call 元素 前已损坏（栈平衡破坏链）**。上游=消费 的 sub rsp 影子空间/call 配对或 形态④追加 的栈使用越界——**684/685 需栈平衡审计**（消费/重填字段 全函数 sub/add rsp 配对逐对核+call 前后 rsp 断言）。修复后→四红清偿→零红→借链终章→001 销项。

**【695-a 回填（2026-09-24）断点 RVA 方法验证+探针矩阵整理立案】**：mini_dbg v3 的 int3 写入+MAP RVA 断点+CONTEXT dump 管道全部验证可用（0x1BB7 断点设置成功·orig=FF 恢复链通·断点未命中即崩=崩点在 0x1BB1 序言 push rbp·WRITE at 非规范地址=调用方 rsp 损坏）。**探针矩阵混乱立案**：p996 系（持有/表们/消费/元素(0)·崩在元素）与 p283 系（货架/记录/追加("字面量")·崩 0xC00000B4）两系在 684~695 轮间交叉误引（RVA/断点/符号跨系错配）。**696 清单**=①探针矩阵整理（每系一目录+README 标注调用链与 RVA）②栈平衡审计（sub/add rsp 配对核·消费/重填字段）③修复→四红清偿→零红→借链终章→001 销项。

**【696-a 回填（2026-09-24）O0 对照实验·排除优化器】**：v2p -O0 下 283 形态④仍 0xC0000005——**缺陷独立于优化级别·在代码生成/IR 层**（SSA/Phi/优化器已排除·D32 的「-O0/-O3 分歧先 IR 对拍」此处=一致崩故为基础缺陷）；线性栈平衡审计法对多块函数无效（分支路径重复计数）已弃——**定位手段=断点式调试（int3+CONTEXT）或 MAP+反汇编逐行**。全部证据链/修法候选/工具落盘 025 §1.4~§1.7+target/m2probe/。恒绿 523/5 维持·已集成真产出=673-a 双缺口+684-a 拥有化。基线红 113/127_v2/497/79_v2（元素返回链）+524=D12 立案→专项会话（断点式+ABI 全景）→零红→借链终章→001 销项。

**【697-a 回填（2026-09-24）探针-obj 对应混乱立案·零代码收口】**：MAP 符号定位显示异常在 映射拷贝构造 内但 p283c 源码无映射变量——**探针矩阵的 obj/asm/exe 与源码对应关系已失真**（m2probe/target/ 多轮共用·编译产物与源码版本错配）。**专项会话第一步=重建全部探针**（每探针独立目录·源码/obj/exe/map 一一对应·命名含轮次·禁跨轮共用中间产物）后再定位容器元素返回链（断点式 int3+CONTEXT 或 MAP+反汇编逐行）→修复→四红清偿→零红→借链 51 例终章→001 销项。

**【697-b 终态固化（2026-09-24）恒绿 523/5】**：崩点终局=设置 内 READ [null+8]（设置 的 this=0·层 槽未写入）；净栈假设证伪（692-a fail-fast 未触发=返回值reg≠0）；已集成真产出=673-a 双缺口根治（aa9fac02·M2 全绿）+684-a 拥有化（efb10cc7·283 清偿）+692-a fail-fast（306e6754）；基线红 113/127_v2/497/79_v2（sret/retbuf 链）+524=D12 立案；续作=专项会话（mini_dbg 断点式 int3+CONTEXT 逐指令/净栈与接收链根因）→四红清偿→零红→借链 51 例终章→001 销项。工具/证据链/探针=target/m2probe/·dbg695/+025 §1.4~§1.7。

**【697-a 续轮回填（2026-09-24）栈配对初步审计】**：货架 构造/追加 调用点 sub 32/add 32 配对合规——崩点不在影子空间配对·在**实参装载值**（mov rcx/rdx 的 this/值实参在 call 前的实际寄存器值）·需 mini_dbg v3 断点式（int3+CONTEXT dump）逐点核。**696 清单**=探针矩阵整理（p283 系已独立目录化 target/dbg695/283/+README）+栈平衡审计（初核完成·深审计待断点式）→修复→四红清偿→零红→借链终章→001 销项。

## 二、深度机 linux-x86_64 节（本机维护）

> 〔2.103 T80-host 侦察/定位轮（**宿主侧「类内结构体字段」缺陷**＝最小复现+gdb+反汇编定性·零行为变更） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.100 T70 B1 三层剩余层**侦察/定位轮**（②元素钩子注入设施缺 ③127 映射链查找段错误 ④228/441/442 v2p 失败快 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.87 F1-26 侦察与设计轮（**真 mem2reg/真值传播**＝256-a 方案 A 续篇·实施前定稿） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.85 D46 第三段（**赋值位构造名裸名拒绝**＝`r = 正常;` 检查赋值语句单点） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
> 〔2.83 D46 已滚转归档（深度机 546-a 棘轮·2026-09-21·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.131 024 D43 别名全链收口＋02_control 阻塞面真根因修复（**内置别名规范化函数单一归属＋谓词散点收敛＋检查选择类型过滤补**＝413-a 语义层遗漏·机制级当场修）——657 开发轮（分支 `任务/深度机-657-D43别名全链`·基点=develop 99b7636a·wt657 隔离·2026-09-23）

**【收工回填（实测·657-a）】**
**1'\. 验收（达成·含开工预判外新发现一枚）**：①**02_control 阻塞面真根因实锤**=非别名本身（402-a 已通）而系 **413-a 检查选择语义层漏类型过滤**——插桩五轮（查名未命中/检查块入口/主扫节点/选择字段/情况节点字段）锁定：选择节点.子起点=吃 `{` 后池位置=**情况值+体语句+体块+标签全部节点**（生成层注释明说此布局且生成层两处消费点均有「类型过滤=节点_情况标签」·语义层漏）——无过滤把如果/当节点字段 [文本,附加)（=[条件区+真块区]）当值表达式区间在**外层作用域**查名→嵌套块局部声明伪报「未声明标识符」；②修复=况i 循环加类型过滤+体块检查保留——探针矩阵 **5/5 双侧逐值一致**（p1 如果体=2/p2 情况体+当=2/p2b 情况体+如果=0/p2c 情况体本级=1/p3 直接引用=7·修复前 p2 编译失败 2 诊断/p2b 1 诊断）+宿主 cn build 同矩阵逐值全同；③02_control 转双编译通道 **runner 单跑 PASS**（v2 产物与 expected 逐行一致·含第 7 段嵌套当+第 8 段 fall-through）；④449 回归 PASS；⑤反证双态：**反证A** 撤规范化挂接→「未声明的类型: 整数」×3（行 7/17/38）复现→恢复复绿=挂接唯一权威点实锤+IR布局 死分支删除安全性背书；**反证B** 撤类型过滤→p2×3+p2b×2 伪报复现→恢复复绿；⑥单测 **1405/1405**+strict **100% rc=0**+matrix **83/83**+E2E 全量 **528=522/4/2**（4 红=113/127_v2/497/79_v2 全部撤守卫态已知基线红·与 629-a 广播/单位机 5ed202f7 验收口径一致=零新增红；78_v2 PASS；2 跳=62/69 平台固有）。
**2'\. 写集（最终）**：CN语言编译器v2/{语法/解析基础.cn（是别名关键字 新谓词）·语法/解析表达式.cn（内置别名基名规范化 新函数+解析类型文本 挂接+3 处换接）·语法/解析语句.cn（2 处换接）·语义/语义检查.cn（检查选择 类型过滤）·IR/IR布局.cn（死分支删除+勘误）}+tests/e2e/02_control/双编译对照.txt（新）+教训 2 条+文档六件套。**零 src/**。
**3'\. 实施增量（预判外·四枚）**：①**A2 拥有契约**——规范化函数初版返回 字符串（返回借用参数=契约错）→改 字符* 借用视图返回（解析基础.cn 当前文本 先例）+调用方 字符串复制 显式拥有化（单次拷贝）；②**管道吞 rc 四犯**——手动构建管道取码假绿+旧产物缓存（v2p_fix 未删）→02_control 首测跑在实施①态旧产物上——裸跑取码+删产物重验后闭环（教训入册）；③**git checkout 误丢改动**——反证A 后 checkout 恢复「变异前」把未提交实施改动一并丢失→重写应用+**立即提交**（402-a 教训二犯·教训入册=实施先提交再开反证）；④看板棘轮 201>200→深度机通告段已毕条目（546~603 全部 09-21/22 广播·影响面已执行完毕）整段清偿 188 行达标。
**4'\. 边缘（回填）**：预判①~⑤全部命中处理（泛型+别名宿主缺口维持 D42 域登记/宿主「指针→空类型*」不引入/情况值常量性=413-a 既有边界维持/死分支删除有反证A 背书/02_control 转双测随任务验收判据报备）。
**5'\. 交叠（回填）**：零文件冲突（家机 629/653 写集不触 v2 语法/语义/IR布局·02_control 无既有通道文件）；语义耦合面=选择语句族全用例（450/66/85/448/02_control）已随全量门禁覆盖验证零回归。

**1. 目标与验收判据**：①021 任务 024（原D43·09-20 用户裁决甲）收口——v2 树「整数/小数」别名处理从散点收敛为**规范化函数单一归属**：新 `内置别名基名规范化(名)`（解析类型文本 基名级挂接唯一权威点·对齐宿主 types::canonical 形态）＋新 `是别名关键字(记号,位置)` 谓词（5 处散点判定换接：解析语句 102/336-337·解析表达式 17/749/869-870）＋IR布局 是标量类型文本 死分支删除与注释勘误（源头规范化完备后「整数/小数」分支不可达）。②**02_control 阻塞面真根因修复（本轮新发现·机制级·随证据备案）**——413-a 检查选择**语义层漏类型过滤**：情况段区间=[情况值+体语句+体块+标签] 全部节点（解析层子起点=吃 `{` 后池位置·值/体语句在标签前入池·生成层 生成语句.cn:187/733 两处均有「类型过滤=节点_情况标签」）·检查选择 无过滤把区间内任意「子起≠0」节点当情况节点→其 [文本,附加) 字段被当值区间检查（如如果节点字段=[条件区+真块区]）→嵌套控制流/局部声明在**错误作用域**被查名→伪「未声明标识符」（探针 p2/p2b 实锤·02_control 第 7 段正中）。修复=况i 循环加 `类型==节点_情况标签` 过滤＋恢复体块检查。③02_control 转双编译对照通道 PASS（阻塞面解除·D44 fall-through 已对齐+别名已通+本缺陷修复）。**验收**=探针矩阵 p1/p2/p2b/p2c/p3 双侧一致＋449_整数小数别名矩阵回归 PASS＋02_control 双编译 PASS＋全量门禁零新增红＋自举链 78/79＋strict/matrix＋反证双态（撤规范化挂接→「未声明的类型」复现；撤类型过滤→p2 伪未声明复现）。
**2. 写集（精确清单·预登记）**：`CN语言编译器v2/语法/解析基础.cn`（是别名关键字 新函数）｜`CN语言编译器v2/语法/解析表达式.cn`（内置别名基名规范化 新函数+解析类型文本 挂接+3 处谓词换接）｜`CN语言编译器v2/语法/解析语句.cn`（2 处谓词换接）｜`CN语言编译器v2/语义/语义检查.cn`（检查选择 类型过滤+体块检查恢复）｜`CN语言编译器v2/IR/IR布局.cn`（是标量类型文本 死分支+勘误注释）｜`tests/e2e/02_control/双编译对照.txt`（新·借链=否·退出码=0）｜plans/021（024 行收口+戳）｜plans/025 本节｜交接/更新日志本机节｜看板清行。零 src/（宿主已是 canonical 单一归属=参照实现）。
**3. 实施步骤**：①探针矩阵定案（p1 如果体✓/p2 情况体+当×/p2b 情况体+如果×/p2c 情况体本级✓/p3 直接引用✓→插桩实锤检查选择无过滤）→②检查选择 类型过滤修复+体块检查恢复→②'探针复验转绿→③是别名关键字 抽取+5 处换接→④内置别名基名规范化 抽取+解析类型文本 挂接→⑤IR布局 死分支删除+勘误→⑥02_control 双编译上架→⑦全量门禁+反证双态→⑧六件套+integrate。
**4. 验证链（命令级）**：探针矩阵 v2p 双侧逐值→02_control/449 单跑→零警告构建→单测全量→E2E 全量 --jobs 8→strict/matrix→自举链 78/79→反证双态两件→grep 复核散点零残留（`grep -rn '"整数"' CN语言编译器v2/` 仅剩词法注册源头+新函数本体）。
**5. 边缘情况与诚实边界（预判）**：①泛型+别名（向量<整数>）=**宿主缺口**（402-a 已登记 D42 域·449 未含）——维持登记不扩面；②宿主 canonical 另有「指针→空类型*」形态=规范 50 行外宿主实现——v2 不引入（规范口径·如实注记）；③检查选择 修复后情况值常量性未限定=413-a 既有诚实边界维持；④是标量类型文本 死分支删除的行为面=变异实验背书（删除后全量门禁绿=无消费路径依赖）；⑤02_control 转双测=AGENTS §4 铁律方向的测试升级·随本任务验收判据（021 024 行明文「02_control 阻塞面」）实施+集成广播报备。
**6. 与他机交叠**：家机 629（tests/e2e 面 30/31/155+run_e2e.py）/653（run_e2e.py+021）在飞——本写集 v2 树语法/语义/IR布局+tests/02_control 与其**零文件交叠**（02_control 无通道文件=无冲突面·021 行更新区不同）；家机 652 已收工（拷贝构造系·v2 树 IR 生成层）不涉语法/语义检查文件；单位机空闲。语义耦合=检查选择 修复影响全部选择语句用例（450 矩阵/66/85 等）——全量门禁覆盖+影响面=全平台 v2p 重建生效·集成后请两机异步验收选择语句面+02_control。

### 2.128 strict 四孤儿修复＋569 runner 平台硬编码修复（576/577 两案·分支 `任务/深度机-576-covmapfix`·基点=develop b294fa48·看板占号 2b76781e·分支头 8198fb02·2026-09-21·挂等三平台回签）

**1. 目标与验收判据**：①修复 b94c37bd「572 缺口备案」strict 红（4 孤儿+310→495 悬空指针）；②569 负测双测通道平台硬编码机制级修复（轻量 v2 负编译断言无条件 win 工具链+win-x64 目标——573 集成 2d283fdf 树级带入 569 runner 后 linux 首跑 71 例负测全炸「未找到 ml64/link」）。**验收实测**=分支头 8198fb02：strict rc 1→0（179/179=100% 指针有效+零孤儿）+matrix 83/83+零警告+单测 1394/1394+E2E **525=523/0/0 零失败**（修复前 452/71/2 对照·131 单跑转绿）。
**2. 写集（回填）**：`tests/e2e/coverage_map.md`（4 孤儿登记+指针同步）＋删 `tests/e2e/310_货舱特性声明制/`（残留目录·与 495 语义重复·557 改号成复制未删·E2E 计数 526→525·删除随 577-a 提交入库〔暂存时序·576-a 提交信息已勘误〕）＋`tests/e2e/run_e2e.py`（轻量断言分支平台分派·win 路径逐参数零改动）＋本节+看板本机行/通告段。
**3. 实施步骤（回填）**：①covmap 四例登记（500=fa59f316 原文恢复·434/435/495=T54 货舱族 557 链已验证内容入册）+4.7/5.9.5 指针同步+310 残留删除；②runner 修复=平台分派三处（就绪函数/平台参数/asm 产物名·linux=确保v2p与运行时就绪+GAS 参数+v2asm.s）。性质=登记恢复+平台分派补全，无新语义。
**4. 验证链（已跑）**：strict/matrix/零警告构建/单测/E2E 全量 --jobs 8/单跑转绿——数字见 1。反证=修复前全量 71 红对照。
**5. 边缘情况与诚实边界（回填）**：发现并修正预判外事实=310 目录两存（非单纯指针悬空而是目录残留）；569 runner 实际随 573 集成才入 develop 树——**上轮 569 回签（2aa213a3）树不含负测双测通道，回签结论被 573 集成架空，本分支 577-a 修复后需三平台重签**。诚实边界=win/arm64 复验走 try-build（分支已推·通告段请求已发）；E2E 计数 525 与他机 526 差 1=310 重复例消除（请求已注明）。
**6. 交叠分析（回填）**：写集与家机（569 收官/568 挂等/573 销账）+单位机（456 侦查）零交叠；语义耦合=strict 门禁（正向转绿）+runner 负测通道（569 作者 win 侧需复跑零回归）。

### 2.130 编号勘误轮（**任务 047 改挂 001 子任务=001-001**·纯文档零代码·用户裁决）——593-a 勘误轮（分支 `任务/深度机-593-047改号`·基点=develop bcf00290·wt593 隔离·2026-09-22）

**1. 目标与验收判据**：用户裁决——047（撤守卫容器双模语义收敛·585-a 立案）是 001 波3 衍生任务，编号应体现父子关系：改挂为 **001-001**（依 plans/021 规则「子任务=父号-三位子序」）；047 号退役防复用；收敛方向方案甲（全内联·用户 2026-09-22 裁决·与 Rust Vec 布局/宿主同构）写入 001-001 立案。**验收**=五文档 14 处引用全部改挂（grep 复核仅剩 025 历史记录 1 处+021 锚行退役注记）+双机械门禁绿。
**2. 写集**：plans/021（047 行→001-001 行移至 001 行下+001 行两处+锚行退役注记+§五 585-a 行两处）｜plans/025（§2.129 两处+本节）｜三机任务看板（通告段两处）｜交接（深度机节一处）｜更新日志（深度机节两行三处）｜看板本机行。
**3. 实施步骤**：①五文件精确替换逐处计数核销；②001-001 立案追加收敛方向裁决句；③锚行退役注记（新任务号自 050 起顺延）；④双机械门禁+提交+integrate（纯文档快速门禁）+镜像推送。
**4. 验证链**：check_handoff+check_progress_sync+grep 复核+integrate 快速门禁。
**5. 边缘情况**：025 72 行「585 立号撞 047/582」=历史事实记录保留原文（考古价值·勘误由本节+提交信息承载）。
**6. 与他机交叠**：纯文档写集·与在飞（家机 589/591/592/504 等）零语义交叠；develop 期间前进=rebase 自然消化（文档分区合并友好）。

### 2.129 D42甲 波3 终章接力（**撤守卫收口=stdlib 链接期供给→源码加载切换落地**·001 任务最后一步·家机 578 同计划深度机接力）——585-a 开发轮（分支 `任务/深度机-585-撤守卫收口`·基点=develop 7ad6343a·wt585 隔离·2026-09-21·用户令「立即全量完成 001」）

**1. 目标与验收判据**：D42甲 波3（plans/021 §三 001 行）最后一步——撤 `是标准库模块` 守卫（主.cn:64 名单），v2p 对 stdlib 10 模块改走源码级加载（候选4 通道已通·517 链 T70 三层修复已入 develop），链接期供给模型退役。**验收**=①撤守卫态 linux 全量 E2E（--jobs 8）零新增红+关键探针 126/127/390/228/441/442 全 rc=0；②自举链 78/79 双 PASS（撤守卫态=v2 编译 v2 树自身 stdlib 也源码级）；③借链面 51 例（借链=是·实测清单）转自洽逐例绿；④反证双态（守卫复活→390 链接失败复现→再撤→复绿）；⑤守卫落地形态=名单函数删除+消费点死路清理（最小死代码·非恒假 TEMP）。win/arm64 侧=协议 v3 集成后异步验收（575 先例）。
**2. 写集（预登记）**：`CN语言编译器v2/主.cn`（守卫名单/消费点）｜`CN语言编译器v2/IR/IR布局.cn` 等 `是标准库模块名` 消费点（按实测定性——生产态 stdlib 源码在库后其判定恒假路径的清理范围）｜`tests/e2e/*/v2闭环.txt|双编译对照.txt` 51 例（借链=是→否·AGENTS §4 测试升级随轮报备）｜`tests/e2e/coverage_map.md`（若有新形态）｜plans/021（001 行波3 收口+戳）｜plans/025 本节｜交接/更新日志本机节｜看板行。零 src/（宿主零改动预期——撤守卫纯 v2 树行为）。
**3. 实施步骤**：①基线冒烟（v2p 构建+关键探针·develop 现态含 517 链/574/575 全集）→②TEMP 撤守卫（名单恒假）+撤守卫态关键探针矩阵→③撤守卫态 linux 全量 E2E --jobs 8（暴露面逐例定性：A 期望漂移迁移+反证/B v2 真缺口立案/C 噪音重跑）→④守卫落地形态定案（名单函数删除+消费点清理·逐消费点实测定性）→⑤借链 51 例转自洽+逐例验证→⑥生产态全量门禁（gate_lock）+自举链 78/79+strict/matrix→⑦反证双态→⑧六件套+integrate（win/arm64 集成后异步验收广播）。
**4. 验证链**：v2p 构建+探针矩阵→撤守卫态全量 E2E --jobs 8→生产态全量门禁（零警告构建+单测 1394+E2E 全量+78/79+strict 179/matrix 83）→反证双态。
**5. 边缘情况与诚实边界（预判）**：①撤守卫态 linux 新暴露面（517 链后 develop 前进了 574/575 两轮 v2 语义/语法改动——撤守卫交互未实测）；②借链 51 例转自洽后链接面符号重复消解实测；③575 新增 4 拒绝面（422/299/81/417）在 stdlib 源码级编译下的交互（stdlib 源码自身须过新检查）；④自举链 78/79 撤守卫态=v2p 编译 v2 全树（460-a 时 fix_p 曾 rc=1·517 链已通·现态复验）；⑤strict 四孤儿已随 576/577 集成消解（develop 现态 strict 179/179 绿·无豁免需要）。
**6. 与他机交叠**：家机 578（同计划 wt578 在飞·**本分支=深度机接力·两家各按本机平台验收·集成取先者·后集者 rebase 自然去重**）——写集同源（主.cn 守卫位+tests 借链面）语义同向，非冲突；家机 579/581（T100 tail/协议v3·零交叠）；深度机 565（v2 树 4 文件·零文件交叠实测）；单位机 arm64（集成后 pull 自然验证）。本分支写集触 v2 树+tests→全量门禁。

**【585-a 收工回填（撤守卫态暴露面四根因二留二回·负测锚七例适配·残余定性）】**：
**1'. 实施实录（基线 develop 7ad6343a→TEMP 撤守卫→探针矩阵 4/6→全量暴露面 21 红→逐根修复→净正收敛）**：
- **暴露面首跑**：撤守卫态（主.cn:64 守卫恒假 TEMP）全量 E2E 528=505/21/2（守卫态基线 526/0/2）——21 红三类：运行错值/崩 14+负测锚行漂移 6+自举 1。
- **修复①（留）**`IR容器.cn 签名返回字节数`：结果空 12→16（268-a 口径漏同步——错误槽@8+宽 8；retbuf 帧预留差 8B→被调 sret 写踩相邻槽）。asm 铁证=追加 sret retbuf lea [rbp-136] 写 16B 覆盖 [rbp-128]。
- **修复②（留）**`生成类.cn 生成类对象局部`：补 IR_分配（帧预留以 IR_分配 声明性指令计数——漏发=槽不进帧→与 retbuf 区重叠→441 A3「新表」句柄被清零→拷贝构造读空。gdb=[rsi]=0 铁证）。同族扫描=全树唯一漏点（标量/引用/结果/数组/结构体局部均有）。
- **修复③（回滚）**`生成表达式字段 成员载荷是结构体` 容器排除：修 441 系（结构体容器字段压地址→load）但破坏 228（类对象.容器字段 链的「字段地址即 this」内联语义）——分层实验两难实锤后回滚。
- **修复④（回滚）**容器参数句柄语义三站点（实参结构登记×2+序言拷贝字节）：修 113 this=0 但破 127/497（517-a 容器元素内联存储依赖结构体按值通道）——回滚。
- **负测锚行适配（留·A 类期望漂移）**：197/223/224/227/229/464/283 七例 主.expected「已解析…主.cn 节点=N」去节点数尾巴（撤守卫态=全树合并节点数含 stdlib·单模块锚必 miss；路径锚保留·runner v2src 适配仍在·生产态前缀匹配兼容）——6 例转绿，283 另有运行面残余。
**2'. 本质定性（残余红收敛为架构级双模语义）**：撤守卫态「容器实例」**内联/句柄双模并存**——元素/字段存储=内联整对象（517-a·宿主对齐），局部变量/参数=句柄 8B（生产态 NewObject 模型）。T70 链（505~517）按内联侧逐例打补丁，585-a 按「容器=类实例化」让布局/类表命中后，**双模判定散点全面显形**（字段载荷 46-a、参数 ABI、方法返回物化……）——**非单点 bug，是语义模型收敛问题**：收敛方向二选一（全内联对齐宿主／全句柄）=架构级大轮（T70 链同量级），505~517+585 两轮教训=打地鼠不可持续。
**3'. 验证（回填·终测实测）**：①生产态零回归（修复①②+expected 七例·守卫恢复态）=零警告构建+单测 **1394/1394**+全量 E2E **528=526/0/2 零失败**+strict **179/179 rc=0（100%）**+matrix **83/83**（gate_lock 串行·与 develop 基线逐项一致）；②撤守卫态终测 **528=513/13/2**（首跑 21→13·净绿 8=441/442/147/127/497/197/223/224/227/229/464 增量减 228 波动）。反证=修复前后红名单对照（git b1b9eee2 历史+本段实录·441 探针 rc=139→0 铁证链）。
**4'. 残余清单（定性登记·001-001 承载·下轮接力·收敛方向已裁决=方案甲全内联〔用户 2026-09-22·与 Rust Vec 布局/宿主同构〕）**：终测 13 红=容器所有权/类支持族 11（113/218/219/221/228/234/237/238/240/283/285——双模收敛同族）+67（泛型返回·T70 面⑤' 残余）+79（自举 fix_s——撤守卫自举）。观测注记=228/67 存在构建间布局漂移不稳定红（驻留序/哈希跨构建漂移·爆发点漂移·047 单点化后自然消解）。
**5'. 诚实边界**：①撤守卫落地（守卫删除=生产态切换）本轮未做——前置=撤守卫态零红，残余族清偿后下轮执行；②借链 51 例转自洽未做（同前置）；③win/arm64 侧=协议 v3 集成后异步验收。

### 2.83 D46（**v2 结果构造名裸名编译期拒绝**＝`返回 正常;`/`返回 错误;` 语义层防线）——474-a 开发轮（分支 `任务/深度机-474-d46naked`·基点=develop aef637b6·wt474 隔离·2026-09-20）

### 2.127 T33 残余根治（**v2 窄宽值规范化四站点＋T36 v2 面字面量值域检查**＝宿主/v2 双编译分叉实锤矩阵）——565 开发轮（分支 `任务/深度机-565-t33窄宽回绕`·基点=develop 715f4e8a·wt565 隔离·2026-09-21）

**0. 立案证据（探针矩阵·宿主=target/cn vs v2=wt562 v2p·2026-09-21 全部实测）**：A 族（窄宽值入槽不规范化→静默错值）：op6 `整8 丙=整8(100)+整8(100)` 宿主 **-56**/v2 **200**；op7 `正16 60000+10000` 宿主 **4464**/v2 **70000**；b5 复合赋值 `甲+=整16(30000)` 宿主 **-5536**/v2 **60000**；b1 传参 `取(甲+乙)`（形参 整16）宿主 **-5536**/v2 **60000**；b2 返回位（->整16）宿主 **-5536**/v2 **60000**；h1 `整32 甲=4000000000` 宿主 **-294967296**/v2 **4000000000**；h5 `整32=6000000000` 宿主 **1705032704**/v2 **6000000000**。B 族（漏拒·宿主拒/v2 放行）：g1 `整16 甲=40000`、g2 赋值位 `甲=40000`、g4 `甲=0+40000`、g5 `正8=300`、g9 `整8=-200`、g10 `正8=-1`、h6 `正32=5000000000` 宿主全拒·v2 全放行。**已正确面（反证不误伤清单）**：显式强转（p3 六形态 ✓）、数组元素（b4 ✓）、结构体字段（c3 ✓·须显式构造器）、a1 变量窄化拒（双侧一致 ✓）、w32/正32 位与回绕 ✓、h2 整64 大字面量 ✓、f1/f2 整32 传参/返回 ✓、h7 整16=-32768 ✓。

**1. 目标与验收判据**：v2 对窄宽（整8/正8/整16/正16/整32）值汇（变量槽/形参槽/返回位）的写入必须规范化到目标宽度（回绕+符号扩展·Rust i16 wrapping 赋值同款语义），字面量对窄宽目标的隐式转换须值域检查（宿主 T36·319-a 同口径）。**验收**=①探针矩阵 A 族七例双侧逐行一致（v2 转绿）；②B 族七例 v2p 编译期拒绝（诊断含「无法将 '整32' 隐式转换为」同宿主措辞形态）；③新 E2E **502_窄宽回绕矩阵**（双编译对照·宿主=v2 逐行全等）+**503_窄宽字面量域外拒绝**（v2 负测·v2p 拒通道）上架单跑 PASS；④全量门禁零新增红+自举链 78/79 PASS+strict/matrix；⑤反证双态（撤 A 规范化→502 复现分叉→恢复复绿）。

**2. 写集（精确文件清单·预登记）**：`CN语言编译器v2/IR/生成声明.cn`（+窄宽值规范化 helper·:232 调用点换接）｜`CN语言编译器v2/IR/生成语句赋值.cn`（:121 调用点换接）｜`CN语言编译器v2/IR/生成函数.cn`（:163 区 返回窄宽码登记+形参槽入口规范化发射）｜`CN语言编译器v2/IR/IR容器.cn`（函数IR +返回窄宽码字段）｜`CN语言编译器v2/IR/生成语句.cn`（生成返回语句 标量返回规范化）｜`CN语言编译器v2/语义/语义检查表达式.cn`（隐式转换合法 字面量分支值域检查+负号字面量形态）｜`tests/e2e/502_窄宽回绕矩阵/`（新·双编译）｜`tests/e2e/503_窄宽字面量域外拒绝/`（新·v2 负测）｜`tests/e2e/coverage_map.md`（两例登记）｜文档六件套+看板。**零 src/**（宿主已正确=参照实现）。

**3. 实施步骤**：①A=生成声明.cn 增 窄宽值规范化（整8/整16/正8/正16→IR_类型转换 窄码〔窄读+符号/零扩展·与显式强转同机制三后端既有〕；整32→T_整32 转换〔movsxd 符号回绕〕；正32→既有 正32存储截断 位与零改；已规范寄存器=零开销直通），:232/:121 两调用点换接；②④=生成函数 形参登记循环后对窄宽标量形参发入口规范化三指令（槽加载→转换→槽回存·方法体同 routine 覆盖）；③⑤=生成返回语句 标量返回路径按 函数信息.返回窄宽码（新增字段·:163 区从返回类型文本登记）规范化；④B=隐式转换合法 整数字面量分支补 值域表（整8 [-128,127]/正8 [0,255]/整16 [-32768,32767]/正16 [0,65535]/正32 [0,4294967295]·整32/整64/正64 不检=h1/h5 宿主实测定案）+节点_负号+字面量形态（g9/g10）；⑤502/503 上架+coverage_map；⑥全量门禁+反证。

**4. 验证链（命令级）**：探针矩阵复验（/tmp/t33probe m/*.cn 全表 双侧逐行比对）→502/503 单跑→零警告构建→单测→全量 E2E `--jobs 8`（互踩族串行复验口径）→strict/matrix→自举链 78/79→`check_fn_length`（新函数 ≤100 行）→反证双态。

**5. 边缘情况与诚实边界（开工预判）**：①**B2 常量表达式形态**（`整16 甲=100-200` 宿主拒/v2 放行）＝v2 语义检查「检不出放行」**既有诚实边界**（推导源类型ID静默 不覆盖二元表达式类型推导）——本轮**不关**（表达式类型推导=独立能力缺口·立案 G 区 T98·非本轮半截）；②显式强转产物寄存器（窄码）经规范化入口=零开销直通（已规范守卫）→194 等显式转换用例零变化；③整32 回绕面=宿主「任意字面量放行+运行回绕」实测定案（h1/h5）——**不设值域检查**（与 整16 差异如实注记代码）；④正64 打印符号性（h3 宿主 2^63/v2 -2^63）=打印/字面量折叠面**独立缺陷另立案**（非存储面·本轮不触）；⑤形参规范化=被调方入口（覆盖全部调用点）；引用/指针/结构体形参不触。

**6. 与他机交叠分析**：写集=v2 树 5 文件（IR 生成层+语义）——家机在飞（D1 拆分=宿主 src/552 phi_lower/556/561/563/564 均不触 v2 树）、单位机 558/557/559（src/tests）**零文件交叠**；语义耦合=B 族为 v2 拒通道收紧（负路径）·正路径零变化面由 502 双编译锚定；A 族变更 v2 产物指令流（窄宽用例多 1~2 指令）——**影响面=v2 产物三平台**，集成后请家机/单位机按通告复验 194/385/386 等窄宽既有用例（本机全量先行）。
**【收工回填（实测）·565】**
**1\'. 验收（达成·含开工预判外当场根治两枚）**：①探针矩阵 A 族全绿——op6=-56/op7=4464/b1=-5536/b2=-5536/b5=-5536/h1=-294967296/h5=1705032704/h13=-294967296/h14/h15=-5536（全部与宿主逐值一致）；②B 族六形态 v2p 全部 sem5 编译期拒绝（g1/g2/g5/g9/g10/h6）·域内 g3/g6/g8/h7 零误伤；③502 双编译 PASS+503 负测 PASS（E2E runner 实跑）；④全量门禁（数字见提交信息）+194/130/05 回归全绿；⑤反证双态=修复前 v2p（develop+562）跑 502 同源输出 200/60000/70000/…/4000000000（分叉实锤·修后=期望值）。
**2\'. 写集（最终）**：CN语言编译器v2/IR/生成声明.cn（窄宽码自文本+窄宽值规范化 helper·声明站点+指针层守卫）｜CN语言编译器v2/IR/生成语句赋值.cn（赋值站点换接+复合脱糖标识符块·查指针变量类型 守卫）｜CN语言编译器v2/IR/生成表达式.cn（字面量适配整32 改值域判定）｜CN语言编译器v2/IR/生成函数.cn（返回窄宽码 登记+形参入口规范化）｜CN语言编译器v2/IR/IR容器.cn（函数IR +返回窄宽码）｜CN语言编译器v2/IR/生成语句.cn（返回位规范化）｜CN语言编译器v2/语义/语义检查表达式.cn（整字面量入域+整数字面量文本值·隐式转换合法 两分支挂接）｜tests/e2e/502_窄宽回绕矩阵/503_窄宽字面量域外拒绝+coverage_map。
**3\'. 实施增量（预判外当场修·三枚）**：①**指针声明落穿**——指针/引用声明在 生成变量声明 分派不早退·落穿 生成标量声明初值 且类型文本=基类型：站点①加 类型节点.附加==0 守卫·站点②/成员块加 查指针变量类型<0 守卫（**教训**：首版无守卫→194/130/05 全量 E2E 段错误·全量门禁当场拦截=网底价值再证；**既有着火点**：正32存储截断 对 正32* 声明同面为 46-f 起既有隐患·本轮守卫顺带消除）；②**查指针变量类型 未命中哨兵=-1 非 0**——首版 ==0 守卫误伤全部非指针赋值（b5/h13 回退实锤）·改 <0；③**复合赋值脱糖节点走成员路径**——文本=0 的合成 节点_赋值 分派进 生成成员解引用赋值·标识符左值在此补规范化（最初误判=纯赋值路径覆盖）。
**4\'. 边缘（回填）**：预判①成立（B2 常量表达式=T98 立案·g4 运行值已正确）；预判②成立（194 显式转换用例零回归·已规范直通守卫有效）；预判③成立（整32 值域不检=宿主 h1/h5 定案）；预判④成立（正64 打印符号性=T100 立案）；预判⑤成立（形参规范化被调方单点·194 读窄(正8*) 零回归）。**新发现**：v2 传参面隐式转换整体缺检=T99 立案（g11 实锤·运行值已由形参入口规范化纠正）。
**5\'. 交叠（回填）**：零冲突（写集=v2 树+tests 502/503·他机在飞不触）。

> 〔2.83 D46（**v2 结果构造名裸名编译期拒绝**＝返回位/声明位/赋值位三段·474/475/476-a） 已滚转归档（深度机 565 轮·2026-09-21·切片=plans025-深度机节-2026-09.md）〕
> 〔2.84 D46 残余收口（**构造名裸名全位拒绝**＝语义检查表达式.cn 59 行放行改拒绝） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 D10 v2 面 已滚转归档（深度机 546-a 棘轮·2026-09-21·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.1 收工回填（**314-a 🧪 M3 采样轮·x64l 列**·基线 a33e3aa〔=src 与 312-a 基线 ce8089c 等价〕·2026-09-17 收工）：**双域零命中+T44 复验+★T45 立案** 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕

**【收工回填（实测）·521-a】**
**1\'. 定性结论（三选一取「撤销登记」）**：①探针 6 形态（下标目标=取点(10)/对表[1]=取点(20)·标识符目标=取点(30)·下标=复制(源)·结果构造=正常(50)·对照健康面）——宿主与 v2p 编译运行输出 **10 行逐行全同、rc 同 0**；②asm 铁证=「下标目标=结构体返回调用」发射 **op=61 整体赋值通道**（`mov rcx,24; rep movsb` 24 字节）而非 StorePtr 8 字节截断——234-a 时期的「emitStructWholeAssign 无源分派」现状已被后续接力链（468 九链/435/441/477 等）覆盖；③全库存量扫描=显式「下标目标=函数调用」形态 E2E/v2 树/stdlib ≈0（零覆盖=既有事实）。
**2\'. 写集（最终）**：plans/021（D11 迁 §4.2+§五 521-a+戳）｜plans/025 本节｜交接/更新日志｜看板（清行）。零 src/v2 树/tests 代码（纯侦查）。
**3\'. 边缘（回填）**：①成员链目标（`外.内 = 取点()`）依赖类内结构体字段=T80 面（513 链挂等回签）——不构成 D11 独立缺口·已注记；②**附带发现（登记）**：宿主对「下标结构体字符串字段」类型推断降级「未知」（`打印(点表[2].名)` 报「参数类型不支持：未知」·`字符串 名=点表[2].名` 同病·84-d 同族候选）——探针以字符串比较法绕过；③探针可作 E2E 正测转正候选（下一 tests 轮搭车）。
**4\'. 交叠（回填）**：517 链（生成语句赋值.cn）零冲突（本轮只读）；全程零文件碰撞。

**【收工回填（实测）·523-a】**
**1\'. 验收（达成）**：①md5 双对拍=探针 3982d9f7+全树 111038 节点 c4106a2c（新旧 v2p 编译同一改前文本）✓；②行为级反证=查ac 恒等变异→v2p_mut 编译 456_泛型指针字段下标 产物 761102ae（变）→恢复→82fc1dc0 复绿 ✓——**克隆修针消费面=泛型实例化（v2p 编译用户程序）·自举编译（v2p 编译 v2 树自身）不触发**（全树/探针对修针变异零敏感=三轮反证迭代定位）；③check_fn_length 61→60 ✓；④门禁=单测 1387/1387+E2E 522=517/3/2（3 假红=同编号互踩·串行单跑全 PASS）+strict 零孤儿+matrix 83/83 ✓。
**2\'. 写集（最终）**：CN语言编译器v2/IR/IR类.cn（+167/-134·分派器 14 行+十子函数+返回行补齐——拆分后子函数须显式 返回 n2c·v2 非空返回类型纪律）｜文档六件套。
**3\'. 边缘（回填）**：①**侦查两改方案均被否**——四槽位族分组（基于 grep 截断误读）与 else-if 重排（丢兜底先行回填=行为变更）→最终=保守分派器拆分+兜底不返回；②首轮切片漏 7 行（分支收尾 `}` 与下行行首 `} 否则` 分离）→花括号失衡编译期当场暴露·边界断言补齐重切；③返回行补齐=v2 语义要求。
**4\'. 交叠（回填）**：全程零冲突。

**【收工回填（实测）·524-a】**
**1\'. 数字（gcov 命令级可复现）**：ir_verify.cpp 行 86.58%（149 可行行）·分支执行 88.95%/走向取正 64.21%；ast_printer.cpp 行 68.78%（474 可行行）·分支取正 36.54%；全项目行 79.21%/分支 45.84%（134 编译单元）。**对比**：ir_verify 较 283-a 基线 85.59% **+0.99pp**；ast_printer 较 471-a 前 14.6% **+54.18pp**——471-a 家机语料轮 linux 侧实证有效。
**2\'. 边缘（回填）**：①总行覆盖 79.21%<基线 80.57%=**门禁语料步未跑的低估**（check_source_coverage 全链在 E2E 步被已知假红族中止 rc=1·语料步未执行·gcda 聚合完整）——非真劣化；②gcov 单文件聚合命令：`gcov -b <build-cov>/CMakeFiles/<lib>.dir/src/**/<file>.cpp.gcda`。
**3\'. 交叠**：零代码零 tests·全程零冲突。

### 2.116 D1 设计型池实施（**解析类声明 185 行按 §2.115 定稿方案拆分**＝五子函数+布局三元组引用传递·纯机械零行为变更）——537-a 实施轮（分支 `任务/深度机-537-解析类声明实施`·基点=develop 最新·wt520 隔离·2026-09-21）
> 〔2.112 501 链处置小节已滚转至归档（574-a 二次清偿·链已作废纯历史·git 可考）〕

> 〔2.113 小节已滚转至归档（574-a 三次清偿·git 可考）〕

### 2.114 D1 滚动（**v2 树 `生成表达式求值.cn` 双函数拆分**＝生成复制展开 175+生成方法调用 146·纯机械零行为变更·501 链作废后避让解除）——532-a 开发轮（分支 `任务/深度机-532-生成求值拆分`·基点=develop 9b991aa1·wt520 隔离·2026-09-21）

**1. 目标与验收判据**：021 §3-D D1 滚动第 5 弹——`生成表达式求值.cn` 两函数：生成复制展开 175 行（151-325）+生成方法调用 146 行（444-589）各拆为分派器+子函数（同族形态按源文实测）。**验收**=①新旧 v2p 编译同一改前输入 asm 逐字节 md5 一致（x64l 探针+全树双对拍）；②反证双态；③check_fn_length 出列两函数；④linux 全量门禁零新增红。
**2. 写集**：CN语言编译器v2/IR/生成表达式求值.cn（唯一代码文件）｜文档六件套｜看板清行。零 src/零 tests。
**3. 步骤**：基线→侦查整读两函数→拆分→对拍→反证→门禁→六件套。
**4. 验证链**：md5→反证→构建零警告→单测→全量 E2E→strict/matrix→check_fn_length。
**5. 边缘（预判）**：①**侦查必须整读函数**（523 教训·grep 截断曾致误判）；②两函数为独立分派结构（复制展开=节点类型分派·方法调用=调用形态分派）——若实测为共享状态顺序循环则如实登记不拆（解析类先例）；③同号假红族照旧口径。
**6. 交叠**：生成表达式求值.cn 与 513~517（生成语句赋值/生成表达式字段/生成表达式/IR生成）不同文件零交叠；501 已作废。

**【收工回填（实测）·532-a】**
**1\'. 验收（达成·范围收窄如实声明）**：①生成复制展开 175→主函数（前置可信判定 71 行+四路分派）+四路子函数（字符串改写 6/标量直通 12/容器类 27/结构体物化 47）——13 条边界断言·逐字迁移 ✓；②md5 双对拍=探针 3982d9f7+全树改前文本 111751 节点 c4355400 ✓；③**行为级反证**=④槽偏移变异（-1 删除）→probe4 结构体复制消费面产物 1c85b603（变）→恢复 d71c7563 复绿 ✓；④check_fn_length 58→57 ✓；⑤门禁=单测 1387/1387+E2E 522=516/4/2（4 假红=同编号互踩已知）+strict/matrix ✓。
**2\'. 范围收窄声明**：生成方法调用 146 行=同文件第二函数**登记下一轮**（本轮单函数已足轮量·四路精密分支的拆分验证周期超单轮预算）。
**3\'. 边缘（回填）**：①**反证迭代三才发现消费面**（探针算术面/全树自举均不消费复制展开路径·标量直通返回变异=等价变异产物不变——AST 生成层文件的反证消费面=「含目标形态调用的用户程序」probe4）②行号切片一轮漏 复obj 首行（编译期「未找到匹配的函数 IR追加」当场暴露·断言补齐重切）。
**4\'. 交叠（回填）**：全程零冲突。


**【收工回填（实测·改判）·533-a】**
**改判=设计型重构候选（不拆）**：整读 生成方法调用（456-605·151 行）——**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·嵌套 5 层+跨层 `返回;` 终结语义+共享可变状态〔表达式栈/指令/计数指针〕）——机械拆分不可行（嵌套提取需把外层条件转子函数前置守卫·跨层 返回; 转译风险高）。**登记=设计型重构候选**（与 解析类声明 185/解析选择语句 136 同类·需独立设计轮）。零代码收工。

### 2.115 D1 设计型池·方案设计轮（**解析类声明 185 行重构方案**＝五子函数划分+状态传递设计+等价验证策略·零代码·登记待实施）——535-a 设计轮（分支 `任务/深度机-535-解析类声明设计`·基点=develop 603b4745·wt520 隔离·2026-09-21）

**1. 结构分析（整读定案）**：`解析类.cn:252-436` 解析类声明——两层循环（262 继承跳过当+277 类体主循环 当(真)）+19 个局部状态·其中**布局三元组**（字段偏移/最大对齐/最大字段）跨分支累加+**位置**全程推进+**段可见性**主循环内切换。类体主循环内四类成员形态：方法（300-333·含构造）/不安全析构+函数（343-392·414-a stdlib 析构形态）/字段（397-416）/默认段守卫。**改判依据复盘（533 先例延续）**：非扁平分派但**成员形态间互斥且有清晰边界**——可拆（与 解析选择语句 的跨层中断不同·本函数无跨层 中断 语义——262 循环的 中断 限于自身块内）。
**2. 子函数划分方案（五子函数）**：①`解析类继承跳过`(记号,位置)->位置——262-269 继承/接口列表显式诊断+跳过；②`解析类可见性标签`(记号,位置,段可见性)->(新可见性,新位置)——283-289；③`解析类方法成员`(池,记号,位置,段可见性,类键基,...)->(新位置,是否消耗)——300-392 方法/构造/不安全析构形态（最大块·内部可再按 414-a 析构形态/普通函数二次切分·实施轮定）；④`解析类字段成员`(池,记号,位置,段可见性,字段偏移&,最大对齐&,最大字段&,...)->(新位置,是否消耗)——397-416 字段+布局累加；⑤`解析类布局收口`(字段偏移,最大对齐,池,...)->位置——422-433 尾部对齐+登记校验。
**3. 状态传递设计**：位置=每子函数返回值（值传递）；池/符号表=引用参数（既有惯例）；布局三元组=引用参数（`整64& 字段偏移` 三连·对齐 登记函数参数们 的引用惯例）；段可见性=值输入；主循环的 保底推进（420 前位置防死循环）保留在主函数（防死循环守卫不入子函数=职责单一）。
**4. 等价验证策略**：①新旧 v2p 编译同一改前输入 asm 逐字节 md5（探针=类/接口/继承/构造析构/运算符重载成员全形态用例 20/21/22/23/95/100/108/333+全树双对拍·518/530 口径）；②反证=变异布局累加行→产物变→恢复复绿；③全量门禁（解析类用例族 20/21/22/23/28/95/100~108/333 全在 E2E 覆盖）。
**5. 实施轮预估**：方案已定稿·实施=行号切片+签名落地+对拍反证+门禁（约 1 轮·同 532 周期）·候选=下一会话首选拾取。
**6. 交叠**：解析类.cn 零占用（329=词法/主/货舱·513~517=IR 生成层）·与全部在飞零交叠。


### 2.123 F2-33 迭代器侦察定稿轮（**T93 宿主泛型实例指针合成缺陷立案**+迭代器设计定稿·零入库代码）——548-a 侦察轮（分支 `任务/深度机-548-迭代器`·基点=develop 9901fa47·wt548·2026-09-21）

### 2.121 D12 v2 词法预处理对齐（**指令四件+条件栈嵌套+裁剪模型+主.cn 三挂点+CLI -D 通道**＝规范 4.7 v2 侧补齐·24_preprocessor 升级双编译）——546-a 开发轮（分支 `任务/深度机-546-d12preproc`·基点=develop 9ab6cca0·2026-09-21）

**1. 目标与验收判据**：v2 自举树补齐规范 4.7 词法预处理（宿主 preprocessor.cpp 210 行同构移植·裁剪模型=词法层空白替换保换行〔行号一致〕）：①指令四件=`#定义 宏名`（无值旗标）／`#如果定义(宏名)` 与 `#如果定义 宏名` 等价／`#否则`／`#结束如果`（严格配对）；②条件栈 {父活/分支已取/活} 支持嵌套+父层不活跃时内层 #定义 不生效（C 语义）；③被裁剪分支不产生任何 token（内含语法错误不诊断）；④诊断三态=未知指令/缺宏名/未闭合（编译中止·对齐宿主+词法错误计数全局态先例）；⑤CLI `-D 宏名` 注入通道（全局态模式·三处 分词 调用链零签名变更）；⑥**验收**=24_preprocessor 升级双编译对照 PASS（v2 产物运行输出=expected 7 行）+v2 全树源码裁剪幂等（零 # 行·v2p 自举链双 PASS 不变）+反证双态（撤裁剪挂点→24 预期失败）+全量门禁零新增红。
**2. 写集（精确清单）**：CN语言编译器v2/词法/预处理.cn（**新文件**·函数族 ≤400 行）｜CN语言编译器v2/主.cn（三处挂点各 +1 行·-D 参数解析段 +15 行左右）｜tests/e2e/24_preprocessor/双编译对照.txt（**新**·退出码=0/源文件=条件编译.cn/借链=否）｜tests/e2e/coverage_map.md（4.7 行 v2 列登记）｜文档六件套（021 D12 行/025 回填/交接/更新日志/看板清行）。
**3. 实施步骤**：①词法/预处理.cn 函数族（字节级逐行扫描+指令判定〔# 起始+中文关键字字节比较〕+条件栈 向量<条件帧>+宏集合 映射<整64,布尔>〔驻留 ID 键〕+提取宏名〔括号/空白双形态〕+裁剪主入口）；②主.cn 三挂点（主文件/包.cn/内置回退源 分词 前 `源码 = 预处理裁剪(源码);`）；③主.cn -D 参数解析（argv 扫描 `-D` → 设注入宏 全局态）；④24 升级双编译+coverage_map；⑤反证双态；⑥全量门禁+自举链。
**4. 验证链**：24 双编译 PASS（单跑）→ 反证（挂点改直通→24 双编译 v2 侧失败/输出错→恢复）→ 构建零警告 → 单测全量 → E2E 全量 --jobs 8（已知互踩假红单跑复验口径）→ strict/matrix → 自举链（宿主建 v2p+v2p 编 v2 全树 fix_p 双 PASS=预处理代码自身过自举）→ 探针（-D 手动命令验证注入通道）。
**5. 边缘情况（预判）**：①指令行判定=行首允许前导空白+# 起始（对齐宿主 181 行口径）——字符串字面量内的 # 不在行首不触发（宿主同款边界）；②CRLF 源（\r 在宏名终止集）；③`#如果定义(宏名)` 括号闭合即止=尾随内容忽略（宿主同构）；④裁剪后源码参与正常分词——#指令行本身被消费不产 token（指令行输出=空行保换行）；⑤未知 # 指令（如 #包含）=诊断拒绝（宿主同款·336_未闭合条件编译拒绝 宿主列已有·v2 侧随本轮对齐）；⑥条件栈未闭合=报错位置在文件末（宿主同款）。
**6. 与他机交叠**：写集=v2 词法新文件+主.cn+tests/24——家机 543=stdlib/正则 零交叠；537-definit=v2/语义 零交叠；536=v2 生成声明+宿主 semantic 零交叠；538=src linux_x64 零交叠（本轮回签面·只读其分支）。**判定=独立**。

**【收工回填（实测·546-a）】**
**1'. 验收（达成）**：①24_preprocessor 升级双编译对照 **PASS**（断言输出=是·运行级逐行全等·v2p 编译→as→g++→运行全链）；②-D 注入双态实测（无 -D→200/#否则 分支；`-D 命令注入`→100/#如果定义 命中）；③反证双态（主.cn 挂点注释→24 v2 侧词法+语法双错误红→恢复复绿）；④诊断负路径（未知指令 #包含/未闭合 #如果定义→[预处理] 诊断行+聚合守卫中止不产 asm）；⑤336_未闭合条件编译拒绝 v2 闭环负测上架 PASS（退出码=负）；⑥构建零警告+单测 1394/1394+strict/matrix PASS；⑦**78_v2/79_v2 自举链 PASS**（fix_p 全树 30s·v2p 编译预处理代码自身过自举）。
**2'. 写集（最终）**：CN语言编译器v2/词法/预处理.cn（新·270 行：指令四件+条件栈+快路径+裁剪主入口）｜CN语言编译器v2/词法/包.cn（+1 行 模块 登记）｜CN语言编译器v2/主.cn（三挂点+-D 扫描函数+主函数调用+聚合守卫·四处）｜tests/e2e/24_preprocessor/双编译对照.txt（新）｜tests/e2e/336_未闭合条件编译拒绝/v2闭环.txt（新）｜coverage_map（4.7 行负例列补 336_v2 注记）｜文档六件套。**零 src/ 宿主改动**。
**3'. 实施增量（预判外当场修·三枚）**：①目录包机制三踩——子文件须包根 模块 登记（文件名=模块名隐式绑定·无需 文件内 模块 声明）+跨模块符号须 `公开:` 段（532-a 生成表达式求值.cn 先例同构）——首编「未声明的标识符」三查后定位；②**★预处理逐行字符串拼接=arena O(n²) 中间串留存**——v2p 编 v2 全树在 4GB RLIMIT 下 std::bad_alloc（runner fix_p 卡死同源）→**快路径根治**：源码含预处理指令(源码) 零分配字节级扫描·无指令文件 `返回 字符串复制(源码)` O(n) 直通（安全区检查器拦截借用返回=防线正面实证）——修复后 4GB 限全树 rc=0·教训入册；③456 终审三连对照实验证实 537-a 引入（见看板 6bda3558·非本轮写集）。
**4'. 验证链（全绿口径）**：单测 **1394/1394**+E2E 全量 **522=516/4/2→修复后 78/79 转绿·终态=517 过/456（537-a 既有欠账·547 根治）+4 互踩假红（单跑 4/4 PASS）/2 跳**+strict 179/179+matrix PASS+自举链 78/79 双 PASS+4GB RLIMIT 复验 PASS。
**5'. 边缘情况（回填）**：预判①~⑥全部命中并处理；预判外=内存膨胀（③·arena 教训）与包机制三踩（①）。诚实边界：命令行 -D 面按 24 用例头口径=手动命令验证（探针双态实测）未入 runner 自动门禁；`#如果定义(宏名) 尾随内容忽略` 宿主同构未负测锚定。
**6'. 与他机交叠（回填）**：全程零冲突；541-revert（3d05b703）与本轮主.cn 挂点 rebase 自动合并成功。

### 2.122 456 根治轮（**537-a 布局收口拆分回归修复**＝`类体起点` 池.大小()+1 静默行为变更回退·反证双态实锤）——547-a 开发轮（分支 `任务/深度机-547-456布局修复`·基点=546 头 72f77934 接力·2026-09-21）

**【收工回填（实测·547-a）】**
**1'. 根因与修复**：537-a（baf7b2c4）拆分混入唯一行为变更——`整64 类体起点 = 池.大小();` 被改为 `池.大小() + 1;`（无说明·违反纯机械声明）——节点_类.子起点+1 错位→泛型实例布局登记/查询键错→类型大小查表 miss 返 0（456 产物 op1 常量 8→0）→rc=14。**修复=回退该行至原版语义**（diff 全量复核=其余均为机械签名/调用迁移）。**反证双态**：+1 在=456 单跑稳定红 rc=14；回退后 456 单跑 **PASS**。
**2'. 验证链（全绿）**：构建零警告+单测 **1394/1394**+E2E 全量 **523=517/4/2**（**456 转绿**·余 4 红=210_v2/457/458/459 互踩假红族·单跑 4/4 PASS 复验）+strict 100%+matrix 83/83。
**3'. 写集**：CN语言编译器v2/语法/解析类.cn（1 行）+文档六件套。**教训**：机械拆分提交 diff 中任何非签名/非搬移的行为行变更=红旗须逐行说明；「md5 双对拍」对**含探针/登记类副作用路径**的输入面覆盖不足（456 泛型实例形态未进对拍样本）——补=拆分轮对拍样本须含泛型类声明形态。
**4'. 交叠**：接力 546 分支（含其全部写集）——回签一次覆盖两轮。

**【收工回填（实测·548-a）】**：①探针双侧分裂——迭代器最小形态（泛型类 向量迭代器{向量<T>* 源;游标}+绑定/有下一个/下一个+当循环）宿主 cn check 红「无法将 '向量<整64>*' 隐式转换为 '向量$整64*'」＝**T93 立案**（$ 合成名与 * 组合吃星号·src 类型系统合成名规范化缺口）；**v2p 同源 rc=0**（103 节点）→新用例走 v2 闭环形态（457 先例·头注宿主欠账）。②设计定稿=stdlib/迭代器.cn 泛型类 向量迭代器<T>{源指针+游标+绑定/有下一个/下一个}·525_v2_向量迭代器（借链=是）·映射集合迭代器=波2。③**549 接力计划**：宿主 T93 修复（src 类型系统独立轮）+525 上架+修后升双编译=消费者反证。④写集=零代码。⑤交叠零。
### 2.125 T95 修复轮（**泛型方法返回 T 推断缺口**＝成员访问基类型实例物化单点·编译面根治/运行面合流 T94）——551-a 修复轮（分支 `任务/深度机-551-t95泛型返回`·基点=develop 0cc770fb·wt551·2026-09-21）

**【收工回填（实测·551-a）】**：①复现精化——p2 探针编译双错：「类型 \'向量<整64>\' 不是结构体/联合体/类类型，无法访问成员 \'元素\'」（根因）＋「无法将 \'未知\' 隐式转换为返回类型」（连带）——**根因=泛型方法体引用参数（向量<T>& v）类型文本经 substTypeParam 替换保留尖括号形态（向量<整64>）未经实例物化·findClass miss**。②修复单点=semantic_expr_access.cpp 成员访问 findClass miss 前 resolveGenericTypeName 物化兜底（幂等：非泛型/已物化/命中路径零变化）——p2 check 由红转绿 ✓。③**运行面 SEGFAULT=与 T94 阶段二同根合流**（引用参数句柄链 this 双解引用——v 槽→表槽→堆对象 少一层）——编译面/运行面分案：编译面本轮根治·运行面随 T94 阶段二裁决后一并修。④门禁=单测 1394/1394+E2E 522=515/5/2（456=基点既有〔547 挂等〕+210_v2/457/458/459 互踩族）+strict/matrix 绿·零回归。⑤写集=semantic_expr_access.cpp 1 处+文档。⑥交叠零。
### 2.130 strict 双红根治轮（**050 登记复登记+601 裁决甲连带簿记**＝验收机当场修·机制级）——603-a 修复轮（分支 `任务/深度机-603-strictfix`·基点=develop cd9a1bdf·wt603·2026-09-22）

**1. 目标与验收判据**：develop 头 strict 门禁双红根治——①孤儿 050_双编译对照取地址类变量（592-a 验收机补登记 ec4c2064 被 591-a「净差异版」集成再丢失·登记丢失族第二例）；②映射有效性错误 9 个（601 裁决甲删 9 例 v1 化石用例 70~76/77/89 未清 coverage_map：[2.1] 正例→89/[附录A] 正例→77_audit_dump 两引用+70~76 七行豁免）。**验收**=`check_spec_coverage.py --strict` rc=0（178/178·指针有效+零孤儿）+matrix 83/83 不回归。
**2. 写集（预登记）**：`tests/e2e/coverage_map.md`（050 行复登记+9 处 601 连带清理）｜`scripts/check_spec_coverage.py`（EXCLUDED_CELLS 增 ("2.1","正例")·573 裁决甲连带——穷举正例 89 已随甲下线·v2 等价补位后应移除）｜plans/021（戳刷 603）｜plans/025 本节｜看板行（窄通道）。零 src/零 .cn。
**3. 实施步骤**：①验收 E2E 前置 strict 发现双红→归因（-S 追踪增删提交）→②看板先行直推 603 行+549/551 过时挂等行清偿（服务轮核实均已是 develop 祖先）→③coverage_map 四处编辑+脚本排除口径→④strict 树上复验转绿→⑤integrate（写集触 tests/**→全量门禁跑合并结果）→⑥看板通告段广播+交接本机节。
**4. 验证链**：strict rc=0（178/178）＋matrix 83/83＋integrate 全量门禁（零警告构建+单测 1383+E2E 全量 526 系·合并结果）。
**5. 边缘情况与诚实边界（预判）**：①[2.1] 正例格空缺=573 裁决甲的 sanction 后果（呈报乙「迁移 v2 等价」未被采纳）——排除口径注释已注明「v2 等价补位后应移除本条」，非静默豁免；②050 登记行连续两次丢失说明「净差异版/squash 集成对登记区行有系统性丢失风险」——integrate.py 581 增量判定缺陷备案同域，本轮不动底座、只随证据再备案；③601 收官销账若家机同步清理同区将产生覆盖_map 冲突——integrate rebase 解冲突取并集。
**6. 与他机交叠**：家机 601（已集成 e1935ae4·本修复=其裁决甲落地连带簿记·语义同向非冲突）；家机 595/601 在飞其他链（零写集交叠）；单位机 596（arm64 codegen·零交叠）；acceptance E2E 由本会话 wtv598 独立树并行承载（gate_lock 串行）。

### 2.2 下一轮候选（预登记·防撞车）

- **daemon 常态化采样续跑**（已重启新版·新种子域 312000001 起自动轮换）；🔶 行扩面巡视（候选=行 53 字符串下标/子串·行 54 容器边界 T17/T18）；
- 波次3=T6+T7（嵌套深度防护+语义复杂度）；波次5=C18+B12（含 T27 全族+i128 helper 修正项）；T44/T45 修复待用户裁决（建议同轮波次清零·写集零交叠）。

> 〔2.3 A12 里程碑标签锚点（**334-a 开发轮·轻量·已收工**·2026-09-18·分支 `任务/深度机-334-A12里程碑锚点`） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.64 条件产物存储轮（**D45 根治+D44 正体收口 双销项**＝贯穿恒真三修+选择语句宿主对齐重写 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.65 D42 甲案波 1（**泛型类源码形态·解析+语义层**＝`泛型 <类型 T> 类 名 { }` 声明解析+参数序登记+类型参数名豁免） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.66 D42 甲案波 2（**IR/生成层单态化实例**＝实例收集→实例注册为完整用户类→方法 AST 克隆生成） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.67 D42 甲案波 3 前置件①（**泛型函数声明+泛型体检查延后+撤守卫侦察**＝stdlib 源码级加载缺口清单实测立案） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.68 枚举全链（**D40 最大子项**＝枚举声明/登记/成员链常量折叠/跨模块导入·stdlib 源码级加载硬前置） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.69 D42 波 3 收口（**函数版单态化+调用点解析+撤守卫+stdlib 源码级加载全通+390 根治验收**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.71 stdlib 源码级 IR 段攻坚（**克隆器标识符位替换+实例布局链审计+装箱语境**＝420 实测缺口三族 ×132） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.72 IR 诊断宿主精确定位＋按宿主逐修（**类型大小 K/V ×48 的真正生成链锁定**→撤守卫+390 终验收） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.73 族②③ IR 段收口（**克隆器修针类型感知全集＋实例表配对越界＋裸字段下标左值＋装箱语境兜底**＝撤守卫 123 终收口） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.74 向量类克隆野走定位＋raw-$T 跳过＋撤守卫 123 零诊断终收口 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.75 撤守卫 123 运行期收口＋390 终验收＋456 上架 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.76 T62 三例无诊断硬错误→可恢复诊断改造（**v2p 302/rc=1 零诊断残余面收口**） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 T70 面②' 扩形态（**生成容器对象整体赋值扩「右值=调用」接管**+IR 模块根成员内置直调+面⑤' 运行级定位） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.84 T57 重设计前置（**objdel 标签撞号根因定位**＝466-a 反证撤除的生产态 3 红源〔228/283 rc=1+79 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
> 〔2.85 T70 B1 专项（**撤守卫收敛尾段**＝克隆体推导链对用户主模块结构体元素 miss 根治） 已滚转归档（深度机 523-a·2026-09-20·内容 git 可考）〕
## 三、单位机 linux-arm64 节（本机维护）

### 3.21 本轮（**341-a 制度轮·🏃 轮次号「先到先得」落地（用户裁决方案①）**·基线 620831b·分支 `任务/单位机-341-撞号防线`·2026-09-18）——开工计划（六要素）

**1. 目标与验收**：跨机轮次号撞号根治（用户 2026-09-18 裁决方案①「认领前先看板、先到先得」）。**验收**：①AGENTS §8.1 占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；②check_handoff 新增「在飞轮次号唯一性」机械检查（跨机同号拦截）+正反例实证；③**plans/025 膨胀事故同轮根治**（实测 1780 行·§二/§三 被拼接 4 份→标题级去重保留最新→493 行）；④本轮自身按新制度实践（**先直推看板占号 341 再开工**——新制度首次完整闭环）。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 分支命名条）｜scripts/check_handoff.py（查看板() 扩「在飞轮次号唯一性」）｜plans/025（去重+本节）｜plans/021（戳+§五索引）｜HANDOFF.md／更新日志.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①看板直推占号 341（**已完成**）；②AGENTS 条文（占用权威=看板在飞行）；③check_handoff 检查实现+实证——**首版正则漏大写字母致反例未拦**（`340-D38`/`329-D12` 提取为 []）→ 修为 `[a-zA-Z]|中文` 后三情形实证过；④plans/025 去重（标题级·保留最后出现版本·断言零重复标题）；⑤门禁+integrate 集成。

**4. 验证链**：check_handoff 正例 0／反例（家机行改 341）拦截 1（报「在飞轮次号 341 被多机占用」）／还原 0；plans/025 去重断言（无重复 `###` 标题）；integrate.py 快速门禁（纯文档+平台无关脚本写集·按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界**：①轮次号提取正则须覆盖大小写/中文标识（本次首版 bug 当场实证发现并修）；②plans/021 恰 600 行上限（瘦身登记下轮·用户已提）；③plans/025 膨胀根因=集成/rebase 时「拼接而非替换」（本轮去重为止血；机械防线=`### 标题重复`检测建议下轮加——与已上线「## 节标题重复」检测同族）；④去重保留「最后出现」版本，三机节内容请两机集成后复核。
**6. 与他机交叠**：家机 340-a／深度机 338-a 在飞（写集=v2 树/src 面）与本轮（文档+门禁脚本）**零文件交叠**；plans/025 三机共用（§一/§二 保留最新版·集成后在通告段请两机复核本机节）。

### 3.20 本轮（**332-a 修复轮·🏃 看板先行机械强制（用户令）**·基线 044423b→集成 b45e4ae·分支 `任务/单位机-332-看板强制`·2026-09-18）收工回填：**制度缺陷根治（开工先填板/收工即销账·机械强制）**

**收工回填（实测·目标全达成）**：①AGENTS §8.1「看板先行」（开工第一动作=直推看板本机行→才开分支；收工集成成功即清行直推）＋§8.5「强制直推、即改即推」（看板行/通告段/try-build 请求三类）落盘；②check_handoff「开工自检」上线——**双向实证**：正例（本分支 0 通过）／反例A（看板不含当前分支名→拦截·含「开工自检」字样）／反例B（🏃 行缺分支名→拦截·证明新小节结构下旧校验仍有效）／还原复跑 0；**判据修正（自建制度首日自证 bug·当场根治）**：收工销账清行后分支名自然不在看板 → 加「**分支已集成（`git merge-base --is-ancestor HEAD develop`）则放行**」豁免，三情形实证（正例放行／销账态放行／未集成+无看板行拦截）；③看板表拆三机独立小节＋查看板() 适配（4 列识别+节内恰 1 行）；④集成 b45e4ae（integrate.py 快速门禁通过；纯文档+平台无关脚本写集按 §8.3 不触发 try-build）；⑤**本轮自证实测两类冲突**：单表三行结构下与深度机看板连续直推撞 rebase 冲突两次（手工解·两行版本并存）＋push CAS 竞争一次（重试复推）——结构拆分与 CAS 均为实证驱动修复。**边缘回填**：非 git 环境开工自检自动跳过（实装）；集成后清行再跑门禁的误报已由「已集成豁免」根治（本轮实证）。**诚实边界**：①332-a 自身开工时未先直推看板（旧习惯残留·已在 HANDOFF 记录并随结构同批生效）；②plans/021 达 600 行上限（卡线·下轮需瘦身）。

**1. 目标与验收判据**：把用户令（2026-09-18）「必须强制 AI 实际工作前先修改看板填写本机任务、完成后顺便销账」落为**机械强制**。**验收**：①AGENTS §8.1 新增「看板先行」强制条（开工第一动作＝直推看板本机行；收工即销账）+§8.5 直推窄通道改「强制直推、即改即推」；②check_handoff.py 新增「开工自检」（当前分支 `任务/*` 时看板必含该分支名）+**正反例双向实证**（正例放行／反例「先动手后填板」拦截）；③看板表拆为**三机独立小节**（防相邻行 rebase 冲突——332-a 当日实测两次）+查看板() 适配；④check_handoff 全绿。

**2. 写集（精确文件清单·预登记）**：AGENTS.md（§8.1 看板先行/认领/看板行格式三条·§8.5 直推窄通道+看板结构条）｜scripts/check_handoff.py（查看板() 适配小节结构＋查开工自检()＋主流程挂载）｜三机任务看板.md（结构拆分＋本机小节＋332-a 通告段）｜plans/025 本节｜更新日志.md／HANDOFF.md 本机节｜lessons.md（追加）。

**3. 实施步骤**：①AGENTS 条文（用户令原文为依据）；②check_handoff 查看板() 改小节识别（4 列）＋查开工自检()（判据＝当前分支名出现在看板任一行·`git rev-parse --abbrev-ref HEAD` 容错）；③看板结构拆分（三机节·内容原样迁移）；④门禁正反例实证；⑤文档六件套；⑥integrate.py 集成＋通告段广播结构变更。

**4. 验证链**：`python3 scripts/check_handoff.py`（正例 0）｜反例 A＝看板不含当前分支名（拦截 1·含「开工自检」）｜反例 B＝🏃 缺分支名（拦截 1·新结构下旧校验有效性）｜恢复后复跑 0｜集成门禁（integrate.py 快速门禁——纯文档+平台无关脚本写集，按 §8.3 不触发 try-build）。

**5. 边缘情况与诚实边界（开工预判→回填）**：①开工自检判据＝「分支名出现在看板任一行」——跨机 try-build 在他机分支上跑时分支名在其机行内即满足（分支主人集成前不清行）；集成后清行再跑旧分支门禁会误报（罕见场景·可接受·回填实测）；②非 git 环境（打包校验）自检自动跳过；③结构变更＝三机共享契约变更 → 通告段广播，他机 rebase 时按其行取值解冲突；④本机 329-a 在飞可见性由 044423b 承载，332-a 行随本批集成更新（结构变更同批故不再单独直推）。

**6. 与他机交叠分析**：家机 329-a（v2 语义层·D38批2）不碰 AGENTS/check_handoff/看板 → 零交叠；深度机 331-a（`src/` 三后端+IR 验证器·try-build 请求中）改其本机行/通告段，与本轮「看板结构变更」**语义相邻**（他 rebase 时按新结构取本方行）——已在 332-a 通告段写明；其余零交叠。

### 3.18 上一轮（**328-a 开发轮·🏃 协议 v2 落地**·用户授权插队·基线 93b11df→7b84b93（rebase）·分支 `任务/单位机-328-协议v2`·2026-09-18）收工回填：**协议 v2 全链落地+本轮自身走新流程端到端演练**

**收工回填（实测·目标全达成）**：①AGENTS.md §8 全文重写（六总纲+§8.1~8.7；§6 三处旧引用同步+§3 缺陷零容忍分级注记）；②`scripts/integrate.py` 新增（合并队列七步·--dry-run/--try-build-done·平台感知门禁）——**实战两遇**：本地缓存（.zcode/plans/）挡前置自检→改「未跟踪文件警告放行」+.gitignore 补忽略；**家机 326-a 收工与本案真实碰撞**（develop 前进+3 文件冲突）→integrate.py 按设计自动 abort 报告→分支主人手工解冲突（plans/021 戳合并双方信息/lessons 双段保留/看板通告段双侧拼接）→rebase 完成→--dry-run 全步骤过（退出码 0）；③check_handoff.py 扩看板 🏃 行分支名校验——**反例（330 号无分支名）拦截+正例（326 豁免/328 合规）放行双向实证**；首版曾真实拦截家机 326-a 旧格式行→加「轮次号 <328 过渡豁免」（旧协议在飞轮无分支可标·收工清行自然消亡）；④plans/027 方案文档落盘（诊断/业界对照/裁决记录/细则/试点安排与复盘区）；⑤共享文档全套更新（看板说明区 v2 口径+生效广播+试点邀请/plans/021 戳+里程碑+试点行+§五索引〔瘦身 3 行回 600 内〕/lessons 设计依据条目/HANDOFF/更新日志单位机节）。

**1. 目标与验收判据（达成）**：六项验收全达成（①AGENTS 重写②integrate.py 演练过③校验双向实证④plans/027 落盘⑤本轮走新流程⑥双文档门禁 PASS·plans/021=599 行）。

**2. 写集（最终清单·11 文件）**：AGENTS.md｜scripts/integrate.py（新）｜scripts/check_handoff.py｜.gitignore（补 .zcode/plans/）｜plans/027-三机并行协同架构v2方案.md（新）｜三机任务看板.md（本机行+说明区+通告段）｜plans/021（戳/里程碑/§三-A 试点行/§五索引+瘦身）｜plans/025 本节｜HANDOFF.md/更新日志.md 单位机节｜lessons.md（追加+rebase 合并 326-a 段双保留）。

**3. 实施步骤（回填）**：按计划①~⑩全部执行；插入项=两处 integrate.py 实战修正（未跟踪文件警告放行/冲突自动 abort）+rebase 冲突解决（与家机 326-a 收工真实碰撞）。

**4. 闅证链（回填实测）**：py_compile 双脚本过；check_handoff+check_progress_sync 全过（独立验退出码·管道吞码教训在案）；反例拦截/正例放行双向实证；integrate.py --dry-run 全步骤过（前置自检/fetch/rebase 对齐/快速门禁双过/push --dry-run）；真集成=git push gitcode develop（ff-only）+github 镜像补推。写集纯文档+平台无关脚本 ⇒ 按 §8.3 分类不触发 try-build、不重跑 E2E（基线 7b84b93=家机 326-a 收工+看板修复提交承载）。

**5. 边缘情况与诚实边界（回填）**：①**并行开发实效未验证**——本轮单分支落地，试点轮（plans/027 §六）承载并行度实测；②integrate.py 首版 try-build 回签=人工通告段+旗标声明（自动解析留试点后迭代）；③集成门禁增量判定（竞争重试轮跳过全量门禁条件）尚未经历真实竞争检验；④python 中文标识符带空格=语法错误（首版两处·py_compile 当场拦截）；⑤看板 328 行在真集成成功后经窄通道清行（收工广播随通告段）。

**6. 与他机交叠分析（回填）**：家机 326-a 收工与本案**真实碰撞一次**（develop 前进+看板/plans021/lessons 三文件冲突）——按 v2 设计路径消解（分支主人自解冲突·rebase·非 develop 手解），**此碰撞即 v2 协议设计的第一个实战案例**（旧协议下=双直推冲突+可能冲突标记入库；新协议=隔离分支+集成时一次性解决）；深度机 ⬜ 全程零交叠。

**下一轮候选（预登记）**：①**协议 v2 试点**（plans/027 §六·另两机各开真实任务分支并行→复盘）；②M3 采样轮续（B8 候选行+daemon 常态）；③T24/T25 v2 树对齐轮（随家机 v2 面轮次）。

### 3.17 上一轮（**297-a 开发轮**·🏃 用户裁「按照你的方式，全部修复」·基线 478d804→认领 ab935b4·2026-09-17）收工回填：**T25/T24/B10 三案当日根治+T26 同族判定+规范条文+406/407/408 转正·全量门禁绿**

**收工回填（实测）**：①**T25 根治**：病灶精确定位=arm64 `emitCopy` 按整型 is64 分派，f64 落 32 位分支向 storeVirtualResult 传 w9 → emitStackStore 浮点分支 `str w9` 32 位截断写（高 32 位丢；1.5 低 32 位恰全零故读出 0.0）——修复=浮点分支 `loadOperandToV`+`storeVirtualResultFp`（f32→s0/f64→d0·asm 实证 `str d0` 64 位完整写）；②**T25 回归矩阵 27 形态全绿**（三元 9+汇合 9+最小 2+浮32 3+数组 2+显式转换口径·C 一致 O0=O3）；③**T24 根治**：visitUnaryExpr Inc/Dec 三路径（静态/成员下标解引用/主路径）+handleClassFieldIncDec(+postfix) 按后缀返回旧值（原忽略 postfix 一律新值）——16 形态全绿（赋值/实参/比较/打印/成员/窄型/语句位不回归/前缀对照·O0=O3）；④**B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻（括号/嵌套 ? 深度感知·新判据⊇旧判据=存量成功程序零语义变化论证）替换 C-1 白名单——`a ? -b : c` 双级别全绿（u1~u5）+后缀传播 `r?+1`=42 不回归+E2E 60 单跑绿；⑤**T26 同族判定**：arm64 短路四形态 O0/O2/O3 全对=免疫（x86_64 单侧·精确验证归深度机 B11）；x64/x64l emitCopy=mov 文本拼接结构（无截断分派同型）静态审查注记；⑥**规范条文**：plans/001 §4.5 优先级表（++/-- 后缀 13/前缀 12 级+值语义）+三元消歧条文+错误传播节更新；spec 01b 五处同步；⑦**新 E2E 三件转正**：406_浮点汇合矩阵O3（编译选项.txt=-O3·13 行）/407_后缀自增矩阵（16 行）/408_三元负号矩阵（6 行）+coverage_map 4.4 登记；⑧**全量门禁绿**：零警告构建+单测 1353/1353+全量 E2E 426=424/0/2（跳=62_ffi/69 平台败面）+**78/79 自举锚定双 PASS**+E2E 60 传播复验绿；⑨看板通告段=v2 消歧同口径广播（家机 296-a 对齐）。

**1. 目标与验收判据（达成）**：六项全达成（T25/T26/T24/B10/规范/门禁——§三.17 计划①~⑥逐项 ✓）。

**2. 写集（最终清单）**：src/cn_compiler/codegen/arm64/arm64_instructions.cpp（emitCopy）、src/cn_compiler/ir/ir_expr_unary.cpp+ir_oop_field.cpp+ir.hpp（T24）、src/cn_compiler/parser/parser_expr.cpp+parser.hpp（B10）、tests/e2e/406/407/408（新·各 2~3 文件）、tests/e2e/coverage_map.md、plans/001、.zcode/skills/cn-language-spec/specs/01b、plans/021、plans/025、HANDOFF/更新日志单位机节、看板本机行+通告段。**v2 树零改动** ✓（家机 296-a 写面）。

**3. 步骤（回填）**：认领（ab935b4）→T25 侦察（emitCopy+storeVirtualResult+emitStackStore 链·IR↔asm 逐环对位）→修→27 形态回归→T26 判定（arm64 短路复现=免疫）→T24 侦察（visitUnaryExpr 忽略 postfix 实锤）→修→16 形态→B10 实现（ternaryColonAhead）→6 形态+传播回归→规范五处→E2E 三件→全量门禁→销项。

**4. 验证链（回填实测）**：零警告构建（grep error|warning=0）+单测 1353/1353+全量 E2E 426=424/0/2+78/79 双 PASS+406/407/408 单跑+60 单跑+最小复现 O0/O3 双对+asm d0 写实证+双门禁（独立退出码）。

**5. 边缘情况与诚实边界（回填）**：①T26=x86_64 单侧·精确形态验证归深度机（本机 arm64 免疫实证+静态审查注记）；②win/x64l Copy 浮点路径运行级复验归各机（静态=结构不同无同型截断）；③T24 v2 树同型对齐=待家机 296-a 收工后独立轮（通告段已广播）；④三元负号+浮点汇合的单测未加（E2E 三件为强门禁·单测面留后续）；⑤i1 Copy 在 arm64 走整数路径（is64=false→w9 32 位写）——布尔 1 字节值 32 位写安全（高位无关）·实证布尔矩阵全绿。

**6. 与他机交叠分析（回填）**：家机 296-a（v2 树）零交叠 ✓；深度机 🧪（只读+cnsmith_hits）零交叠 ✓；coverage_map 潜在文本冲突=rebase 兜底未发生。

**下一轮候选（预登记）**：①**M3 采样轮⑥**（回归 B8 候选行+daemon 常态·用户令不停）；②**T24/T25 v2 树对齐轮**（家机 296-a 收工后·自举编译器同型三缺陷）；③T26 精确形态=深度机口径（B11）。

> 〔T33 侦查注记/D36 实施设计已滚转归档（471 集成会话·2026-09-20·D36 定性详见 021 D36 行）〕
