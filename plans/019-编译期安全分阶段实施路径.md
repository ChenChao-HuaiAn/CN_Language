# 019 CN语言编译期安全：分阶段实施路径（2026-09-10 立项）

> 性质：**已批准实施**的路线文档（用户裁决 2026-09-10：「按照你说的最优解裁决」）。
> 上游呈报：会话中三轮设计呈报（①编译期安全三方案 A/B/C → ②CN 规范语法重写 → ③可行性审阅修正）。
> 本文是全部阶段的权威实施路径；执行状态见第七节跟踪表，**每完成一个阶段必须更新**。

---

## 一、立项背景与裁决记录

### 1.1 双目标定位（既有，AGENTS.md）

v2 自举双目标：① 性能超过 C++ 编译器；② 安全接近 Rust（悬垂指针/越界/泄漏零容忍）。
自举已完成（v2self 固定点 239301 行，2026-09-10 第五十六轮口径），目标①②的编译器本体
建设进入收尾，本专项承接目标②的**编译期化**——把安全检查从运行时（越界/空指针检查）升级到
编译期（使用前拦截），对标 Rust 但**不引入 Rust 语法**（CN v1 前身即 Rust 风格，已裁决废弃）。

### 1.2 用户裁决记录（2026-09-10）

| 裁决点 | 选项 | 裁决结果 |
|---|---|---|
| 总路线 | A=Rust语义内核全量移植 / **B=值语义强化+渐进检查器** / C=纯流分析 | **B 起步、向 A 演进**（B 各阶段独立可交付，终态≈A） |
| 语法点① | 显式转移的形态 | **`转移(表达式)` 内置函数**（内置构造器 正常()/错误()/某些() 同位，零新关键字；C++ `std::move` 同构） |
| 语法点② | 只读借用的形态 | **`常量 T&` 引用类型只读修饰**（复用既有 常量 关键字；「常量成员函数」修饰先例） |
| 语法点③ | 不安全区隔离形态 | **a=函数级修饰 `不安全 函数 名(...)`**（粒度清楚、审计面明确、C++ 风格） |
| 实施方式 | — | 先写本报告 → 分步骤全部实施（每阶段：规范先行→E2E 先行→宿主→v2→门禁） |

### 1.3 审阅修正并入（会话第三轮可行性审阅结论）

前两轮设计呈报有三处乐观度，已修正并入本路径：
1. **字符串整治（原阶段1子项）= 类型系统变更**而非零语法成本（规范 02 明文「字符串变量为指针」）→
   单列为阶段 4'（智能指针与拥有型字符串），置于借用互斥之后、按值语义容器化方案另行呈报。
2. **借用互斥（阶段 3）伴随规范级使用限制**（引用不得存进容器字段等限制规则），非纯检查器逻辑。
3. **联合体必须划入不安全区**（规范 02「所有字段共享同一内存区域」无 tag，Rust union 同为
   unsafe-only）——阶段 4 安全区边界清单必须含：指针算术/下标写、联合体字段访问、FFI、字符串裸释放。

## 二、总体架构：CN 编译期安全分层模型

```
┌─ 不安全区（阶段4划定）────────────────────────────┐
│  不安全 函数 内显式操作裸指针：指针算术/下标、联合体  │
│  字段访问、FFI 外部函数、释放内存。审计面=不安全函数清单│
└──────────────────────────────────────────────┘
┌─ 受检区（阶段2~3 逐步收紧）───────────────────────┐
│  转移检查（仿射）：转移(v) 后 v 禁用                  │
│  逃逸检查：引用/指针不出所指对象生命周期              │
│  借用互斥：常量 T& 只读共存 × T& 可变独占            │
└──────────────────────────────────────────────┘
┌─ 安全区（默认，已具备=阶段0）──────────────────────┐
│  值语义深拷贝（标识符拷贝/容器追加=深拷贝，无别名）    │
│  RAII 析构 + 容器元素自动析构 + 运行时越界/空指针检查  │
│  结果/可选强制检查三规则（编译期 must_use 先例）      │
└──────────────────────────────────────────────┘
```

设计哲学（区别于 Rust 的三句话）：
1. **默认路径零规则**——CN 值语义深拷贝天然无别名，安全区代码不需要学任何新规则
   （这是 CN 相对 Rust 的体验优势：没有所有权概念也能写出安全代码，代价是拷贝）。
2. **显式放弃安全=显式获得性能**——`转移()` 是用户主动声明「此处放弃拷贝换零拷贝/交接所有权」，
   编译器则以「旧名禁用」回报安全保证（双释放/UAF/转移后使用结构性根除）。
3. **危险面收口而非全语言戒严**——指针/联合体/FFI 是系统语言必需能力，收进 不安全 函数
   显式审计，而非从语言中删除。

## 三、阶段路径总表（实施顺序=依赖序）

| 阶段 | 主题 | 编译期新增拦截 | 语法变更 | 预估规模 | 状态 |
|---|---|---|---|---|---|
| 0 | 值语义+RAII+运行时检查+强制检查三规则 | （已具备） | — | — | ✅ 既有 |
| 1 | **显式转移 转移() + 已转移使用检查（仿射）** | 转移后使用/写/传参/再转移；标量误转移 | 转移() 内置函数（§5.3.1） | 周级 | ✅ 第五十七轮（浅转移优化=3b 第六十一轮） |
| 2 | **引用逃逸检查强化** | 引用局部绑定逃逸（存字段/容器/全局）；返回经多级间接逃逸 | 无（规则加强） | 周级 | ✅ 第五十八轮 |
| 3 | **常量 T& 只读借用 + 借用互斥（第一版：语句级）** | 常量引用内写拒绝；同调用可变×只读别名拒绝 | 常量 T& 类型修饰（已批实施） | 月级 | ✅ 第五十九/六十轮 + 3b 第六十一/六十五轮（剩余：跨语句 NLL ⬜ 待真实使用反馈） |
| 4 | **不安全区边界（不安全 函数 修饰）** | 裸指针算术/下标写、联合体访问、FFI、释放内存 仅限不安全函数 | 不安全 关键字+函数修饰（已批实施） | 月级+stdlib/v2 迁移 | 🔄 第一层 62 轮 + 第二层第一/二批 63/64 轮（警告 175→0）；第三批（收口硬错误）⬜ 呈报 |
| 4' | 智能指针与拥有型字符串（类型系统变更，另案呈报） | 空/悬垂字符串与裸释放 | 拥有型类型（方案另呈） | 月级 | 🔄 **方案A 已裁决**（2026-09-10）；一批 66 轮/二批 67 轮/A1 67 轮/A2 68~70 轮 已落地；第三批 ⬜ 排程 |
| 5 | 并发安全 Send/Sync 对标 | 数据竞争 | 随并发标准库（plans/003 重大缺口） | 依赖并发库 | ⏸ 外部依赖 |

> **状态列刷新说明（2026-09-13）**：本表此前停留在立项时口径（阶段 1 标「🔄 本轮启动」、4' 标
> 「待裁」）——施工作业面的**实时状态一律以 §7 执行状态跟踪表 + `plans/021 任务进度观察表`
> 为准**，本表只反映阶段级大状态。

依赖关系：1→2→3（引用分析逐级加深）；4 独立于 2/3 可并行排期但建议后置（迁移面大）；
4' 依赖 3（拥有型语义需要转移检查先行）；5 依赖并发库立项。

## 四、阶段 1 详案：显式转移 转移() + 已转移使用检查（本轮实施）

### 4.1 语言语义（规范条文，写入 plans/001 §5.3.1 与 specs/04 第五节）

**转移(表达式)** —— 内置函数（零新关键字）：
- 实参须为**变量标识符**（第一版；成员/下标/解引用形态显式拒绝，诚实边界见 4.5）。
- 实参类型须为**拥有资源语义类型**：指针（含 字符串）、容器（向量/映射/链表/栈/队列）、
  类对象、结构体、结果/可选。**标量（整数/浮点/布尔/字符）拒绝**——复制语义无转移意义
  （诊断：「'X' 具有复制语义，无需转移」；对齐 Rust Copy 类型不适用 move 的惯例）。
- 求值结果 = 实参的值（同一对象/指针的交接），**源变量自调用点起进入「已转移」状态**。
- **已转移变量的任何后续使用均为编译错误**（E0382 对标）：
  读值 / 赋值目标 / 复合赋值 / 自增自减 / 取地址 / 解引用 / 成员访问对象侧 / 下标对象侧 /
  函数调用实参 / 返回语句 / **再次转移** / 作用域出口析构以外的全部消费形态。
  诊断消息：「'v' 已转移，不能继续使用（转移发生在 …）」（含转移点行号定位）。
- 已转移状态**不因作用域退出而恢复**（除遮蔽：内层同名新声明是新变量，不受影响——
  与作用域解析同序判定，天然正确）。
- 第一版**运行行为零变更**（诚实边界 4.5）：转移() 在语义层把调用节点改写为实参标识符
  （AST 改写，wrapRefArgs 同款手法），IR/三后端/析构零改动；容器/类对象仍按现有拷贝
  语义（指针即槽位值天然零拷贝）。**安全收益为纯编译期**：转移后使用（UAF 主形态）与
  转移后再释放（双释放主形态）结构性根除。浅转移零拷贝优化=阶段 3 随借用分析一并落
  （需要 IR 层拷贝路径分派，改动面大，独立排期）。

### 4.2 宿主实施清单（src/cn_compiler）

| # | 文件 | 改动 |
|---|---|---|
| 1 | semantic.hpp | ①`std::vector<std::unordered_set<std::string>> scopeMoved_`（与 scopes_ 平行，push/popScope 同步——scopeConsts_ 同构先例）；②`void markMoved(name, loc)`：沿作用域链找到声明层写入（遮蔽正确）；③`bool isMovedVarName(name) const`：与 lookupVar 同序解析查声明层 moved 集；④`static bool isTransferCall(Expr*)` 识别辅助 |
| 2 | semantic_expr.cpp visitCallExpr | `转移(单标识符实参)` 识别：类型白名单检查（标量拒绝/资源语义放行）→ markMoved → **AST 原地改写为实参标识符**（IR 零改动）→ 返回实参类型 |
| 3 | semantic_expr.cpp 使用点 | visitIdentifierExpr（读值）、reportNonLvalueTarget 前置（赋值/复合赋值目标）、visitUnaryExpr（取地址/&、解引用 *、++/-- 操作数）——查 isMovedVarName → 硬错误 |
| 4 | semantic_expr.cpp visitMemberExpr / 下标 | 对象侧标识符已转移 → 硬错误（经 checkExpr 对象递归自然覆盖，验证即可） |
| 5 | semantic_call.cpp | 逐实参检查：实参为已转移标识符 → 硬错误 |
| 6 | semantic_stmt.cpp visitReturnStmt | 返回表达式基础标识符已转移 → 硬错误（refReturnLvalueBase 复用解剖） |
| 7 | 单测 tests/unit/semantic/ | 新增 test_move_check.cpp：负形态（读/写/传参/返回/取地址/解引用/再转移/标量误转移/成员对象侧≈9 例精确消息）+ 正形态零误伤（转移后新变量、遮蔽新声明可用、指针交接链） |

### 4.3 v2 实施清单（CN语言编译器v2，宿主先行破循环依赖）

| # | 文件 | 改动 |
|---|---|---|
| 1 | 语义/语义检查.cn | 转移调用识别（被调文本=="转移" 且单标识符实参）+ 类型白名单（标量拒绝）+ 已转移表（名→转移行登记）+ 使用点拒绝（检查表达式 标识符分支/赋值语句左值/返回分支——195 同款三面） |
| 2 | 语义/语义检查表表达式.cn | 静默推导面豁免（诊断非幂等家族教训：第五十一轮 174 双报先例——新检查走静默缓冲或语句根权威点） |
| 3 | 主.cn 尾部 | 负测自检（源码N：转移后使用须拒——195 同款自检惯例） |
| 4 | 锚定链 | v2 源码变更 → v2p 重建 → v2self 固定点重锚（X64L 口径）+ 组件对拍 29/29 + 运行级 3 用例 |

### 4.4 E2E（先行：用例先红证明缺口存在）

| 用例 | 通道 | 断言 |
|---|---|---|
| 196_显式转移 | 宿主正测 stdout | 指针转移交接链（转移后新变量解引用/释放归属新变量）+ 容器转移（新变量追加/大小锚）+ 转移后旧名不可用（负形态在 197）+ 校验和 |
| 196_v2 | v2 闭环 rc | 同源码 v2 编译运行，校验和一致 |
| 197_转移后使用拒绝 | 宿主负测 期望编译失败.txt | 四形态诊断子串「已转移，不能继续使用」（读/写/传参/返回）+ 标量误转移诊断 |
| 197_v2 | v2 负路径 None | v2 三面诊断行+中止行固化 |

### 4.5 诚实边界（第一版）

- 转移() 仅标识符形态（成员/下标/解引用转移显式拒绝并诊断）——字段级转移随阶段 3。
- 容器/类转移运行行为仍为深拷贝（安全保证=编译期使用纪律；性能优化=浅转移随阶段 3 IR 分派）。
- 指针经函数转接（返回指针的函数调用结果再转移）不受影响（右值无需转移检查）。
- v2 静默推导面（成员/下标左值末端推导兜底检不出已转移）与 195 同族诚实边界，宿主全形态覆盖。
- stdlib/v2 全树存量代码不使用 转移()（新函数），**兼容面=零**（无需迁移审计）。

### 4.6 验收口径（阶段 1 完成定义）

1. 规范条文落入 plans/001 §5.3.1 + specs/04 第五节（含设计决策与 E0382 对标说明）。
2. E2E 196/196_v2/197/197_v2 四用例注册 run_e2e.py 并 PASS（先红后绿闭环）。
3. 单测新增锚点全绿（负形态精确消息断言）。
4. 全量门禁：清场重建 GCC 零警告 + 单测 1251+N + E2E 全量（205+4 用例，仅既有 194 待批/4 平台跳）。
5. v2 侧对齐 + v2self 锚定链重锚（X64L 口径）+ 组件对拍 29/29 + 运行级 3/3。
6. plans/019 跟踪表更新 + plans/014 状态表行 + 更新日志 + HANDOFF。

## 五、阶段 2~5 实施要点（开工前按本节展开为详案）

### 5.1 阶段 2：引用逃逸检查强化（规范已有语法，规则加强）

现状锚点：返回局部引用已拒（semantic_stmt.cpp:744，E2E 103）。扩展面：
1. **引用局部绑定逃逸**：`整32& r = x;` 后 r 存入结构体字段/容器/全局静态/返回 → 悬垂
   （x 出作用域 r 仍活）。检查：引用类型变量的消费白名单收窄（读/写/传引用参/成员基址以外
   的逃逸形态拒绝）。
2. **返回引用经多级间接**：返回 `外层.字段` 形态中 外层 为局部 → 已有；返回 条件?甲:乙 引用
   分支形态补全。
3. 指针逃逸（局部的 &x 返回）已有部分覆盖（refReturnLvalueBase），补全 &局部经容器/字段转发形态。
验收：E2E 负测族 + stdlib/v2 全树零误伤（引用面 559 处 & 使用需过检——预审必须做，误伤即回炉）。

### 5.2 阶段 3：常量 T& 只读借用 + 借用互斥第一版（含转移浅拷贝优化）

1. **语法**：`常量 向量<整64>& 数据`（类型前置 C++ 风格，常量 修饰引用参数）——parser
   参数类型解析扩展；specs/04 引用参数章节扩条文。
2. **只读检查**：常量 T& 参数体内对该参数（及经其成员访问链）赋值/传可变引用/调用可变方法
   （容器 追加/设置 等变更方法清单）→ 硬错误（对齐 C++ const T& 的 constness 检查）。
3. **借用互斥第一版（语句级保守）**：同一调用语句中，可变引用实参与只读引用实参解析到同一
   基础变量 → 歧义别名拒绝（`排序(v, v.大小())` 形态类）。完整 NLL 活跃区间分析（跨语句
   借用生命周期）**不在第一版**——先保守拒绝+显式豁免形态（临时字面量/右值），误报率实测
   （stdlib/v2 全树当语料）后再决定 NLL 深度（rustc 从 AST borrowck 到 NLL 六年的教训：
   一步到位必翻车，渐进实测是正路）。
4. **转移浅拷贝优化**（并入本阶段）：转移() 在 IR 层从「AST 改写回标识符」升级为显式
   槽位交接（容器/类对象跳过深拷贝构造，槽位直拷+源槽禁用标记），运行行为=真零拷贝。
   风险点：RAII 析构路径须跳过已转移槽（语义层 moved 集传 IR）。
验收：E2E 正负双通道 + 性能锚（转移前后 bench 对比，零拷贝收益实证）+ 全树零误伤。

### 5.3 阶段 4：不安全区边界（不安全 函数 修饰，需二次呈报细则后开工）

- `不安全 函数 名(...) {}`：不安全函数体内方可使用——指针算术/指针下标写、联合体字段访问、
  外部 函数 调用、释放内存/释放内部数组 等裸操作。安全函数调用不安全函数**允许**
  （安全责任上移到不安全函数实现者，Rust unsafe fn 同模型）。
- 迁移面：stdlib + v2 全树 60 处指针声明与解引用/下标使用须包进不安全函数（或改写为安全
  抽象）——**分批迁移**：先标记不迁移只警告（观察期），再按模块收口硬错误。
- 关键字 `不安全` 入规范 01a（真实语法消费点=函数修饰位，符合保留字最小化原则）。
- CFI（plans/008 既有 --cfi 基础）与不安全区联动：不安全函数=CFI 目标表之外的外部调用面。

### 5.4 阶段 4'：智能指针与拥有型字符串（类型系统变更，方案另呈报后实施）

拥有型字符串（替代「UTF-8 裸指针+调用方释放」约定）与 唯一指针/共享指针 标准库类型。
**必须在阶段 3 落地后开工**（拥有语义依赖转移检查）。方案（析构注入/ABI/与驻留互操作/
打印链路适配）届时三方案呈报。

### 5.5 阶段 5：并发安全（依赖并发标准库立项，plans/003 批次 C）

Send/Sync 对标：类型级可跨线程标记 + 检查器拒绝违例传参。随线程/锁/原子标准库一并设计。

## 六、工程纪律（每阶段强制）

1. **宿主先行**：检查器先在 src/cn_compiler 落地（破 v2 自举循环依赖——v2 用 CN 写检查器
   时可参照宿主实现），随后 v2 对齐。
2. **E2E 先行**：每阶段先写负测用例证明缺口存在（当前静默通过=红），再实现转绿。
3. **全量门禁**：清场重建零警告 + 全量单测 + 全量 E2E（含既有基线不劣化——194 待批挂账维持）。
4. **锚定链**：v2 源码变更后 v2self 固定点重锚 + 组件对拍 29/29 + 运行级 3 用例（X64L 口径；
   win/arm64 随跨机轮）。
5. **全树零误伤预审**：检查器类改动（阶段 2/3）动工前先跑 stdlib+v2 全树过检预审，
   误伤形态回炉规则设计（rustc 误报迭代教训）。
6. **诊断质量**：负向诊断统一含行号定位与迁移提示（「已转移，不能继续使用（转移发生在 行N）」
   对标 E0382 的 borrow 定位体验）。
7. 文档同步六件套同提交：plans/019 跟踪表 + plans/020 全节 + **plans/021 总表（排班队列/里程碑
   更新）** + 更新日志 + HANDOFF + lessons。

## 七、执行状态跟踪表（每阶段更新；2026-09-12 起按机器分段——分区规则见 AGENTS.md §8.3）

### 7.0 三机任务看板（已独立成文）

> 看板于 2026-09-12 独立为根目录 **`三机任务看板.md`**（三机并行协作唯一看板，与任务类型无关，
> 任何任务开工/收工必经；本专项跟踪表不再承载看板职能）。认领协议规则见 AGENTS.md §8。

### 7.1 深度机 linux-x86_64 段（开发轮主体记录；本机只在本段末追加行）

〔§7.1 深度机段历史行（63~187-a·9-10~9-15）已滚转归档：项目记忆/归档/plans019-跟踪表与历史轮-2026-09.md（788 治理轮·当前状态以 plans/021 §三 与 plans/025 §二 承载·本段末追加行惯例不变）〕


### 7.2 家机 win-x64 段（win 跨机验证轮记录；本机只在本段末追加行）

| 日期 | 轮次 | 阶段 | 事项 | 状态 |
|---|---|---|---|---|
| 2026-09-12 | 第八十五轮 | 4'-三 | **跨机轮 win 侧动态收口（第五十六~八十五轮安全特性 win 实物全绿）**：①全量门禁=清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306** + E2E **269 用例 252 过/17 败/0 跳**——败=①既有 OOM 家族 2〈78 4137MB/79 4113MB >4096MB 上限自动终止，与第四十八轮基线 4135MB 同族零回归〉+②**双编译对照 13 用例**（win-x64 平台限制：需 linux as/g++ 链，runner 按平台设计注册为失败——深度机侧 4 跳=amr64 守卫 2+178/179 平台特性）——**非本轮缺陷**；③**85-a 特性 win 实物探针全过**（target/p55win/probe85：六形态〈形参字面量跨块/形参标识符跨块/嵌套成员链/拥有局部 move/指针解引用/字面量调用链〉折校验和 **63** 全对=返回位深拷归一化 win 实物首验+每形态搅动+内容比对）；④② 静态结构体 win 复现（ml64 报 `undefined symbol : rbp0` 汇编失败——独立缺陷形态，登记维持）；借出视图豁免 win 复验通过（probe85f 容器元素读出口径正常）；⑤v2self win 锚定链复锚 **340703 行**（新口径，246651→340703，+94052=第五十六~八十五轮 v2 侧全量安全特性源码确定性体现；绑定自检 cn_main∈cn_self46.obj ✓；fix_p≡fix_s 两代逐字节一致）；⑥组件对拍 **34/34**（含新增 5 组件 test_borrowview/const_ref/escape/move/unsafe_boundary 编译对照全归零）+ 运行级 **3/3**（hello 你好，世界/119 rc=14/173 rc=173）；⑦**待批两项**（测试文件不可擅改纪律）：**69_memory_management 期望过时**（`字符串 s = "…"` A2 后产生 1 次 `__cn_str_copy` 分配——行为正确〈探针 69-a：增量 s=1/子串=2/释放子串=1/块退出=0，两侧同构〉，期望「使用中=1」为 A2 前口径——建议期望 100→201、总分配 4→5）+ **194_v2 win 退出码口径**（511%256=255=POSIX 截断，win 为 32 位全值 511——runner win 分支步 6 直接比较，建议 v2闭环.txt 退出码 255→511〈Linux 侧 %256 后仍 255 恒成立〉）；⑧**待批两项按纪律落实**（AskUserQuestion 呈报无答复，按 AGENTS.md 第3项「参考 Rust 经验给最适合方案（性能第一/安全第二）」自行裁决留痕）：**① 69_memory_management 期望修正**——主.expected `100→201`/`4→5` + 用例源码注释同步 A2 口径（使用中=2=s 拷贝+子串、释放后=1=仅 s 尚在、块退出归零；探针 69-a 两侧同值实证）；**② 194_v2 退出码口径统一**——v2闭环.txt `255→511`（校验和逻辑值 511 为唯一权威；POSIX %256 截断后恒=255、win 32 位全值=511——两平台统一且保留全精度校验）＋文件头留痕注；两者断言语义均不弱化（69 仍锚无泄漏回落/计数累计；194_v2 仍锚九形态全对）。复验：69 PASS + 194_v2 PASS + **全量 E2E 复跑 269 用例 254 过/15 败**（失败=13 双编译对照 win 平台限制+78/79 OOM 家族——零回归且较首跑 252 过 +2=两项销项）；⑨诚实边界：本轮纯验证轮零源码改动（仅测试口径修正与文档）；13 平台对照失败为既有/平台项非缺陷；78/79 OOM 为 win 固有败面；探针留 target/p55win/ 不入库 | ✅ 跨机轮闭环（待批 2 项已落实） |
| 2026-09-12 | 第八十六~八十八轮（跨机轮） | 4'-三 | **win 侧跨机验证轮（拉取 9403e29=86-a/87-a/88-a 三轮，基线 efc0c8c）**：①全量门禁（前序会话完成构建+单测）=清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306** + E2E **275 用例 255 过/20 败/0 跳**——败=**17 双编译对照**（win 平台限制，13 既存+新增 243/244/246/248）+**78/79 OOM 家族**（既有败面 4137/4113MB）+**148_v2_函数返回结构体 新增崩溃（0xC0000005，win 侧 v2 产物；55 轮该用例通过=回归）**；②**新缺陷立案（win-x64 特有，内存安全级）**：诊断链=宿主维度 bisect（同 55 轮 v2 树四宿主对照：55 轮 ✓/86-a ✓/**87-a ✗**/当前 ✗）→ **87-a 宿主编译引入**；最小复现 min3 探针（结构体按值实参×嵌套调用 `取和(造点(30,40))`）→ cn_main 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常 `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一 v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win 侧「仅源」进入无物化分支）；平台分叉=linux 侧不触发（深度机 271 过含 148_v2）；**未确证**=具体未初始化读取点（留开发轮）；③锚定链复锚 **348908 行**（新口径，55 轮 340703→+8205=56~88-a v2 源码确定性体现）两代逐字节一致 + 绑定自检 cn_main∈cn_self46.obj ✓；④运行级 **3/3**（hello 你好，世界/119 rc=14/173 rc=173）+ 组件对拍 **34/34**（DIFF=0）；⑤**win 实物复验（run89 探针）243/244/246/248 全过**（宿主与 v2 两侧各编各跑 rc=255 一致：86-a 三元聚合全链/86-a 嵌套深拷与豁免/87-a 静态变量全链/88-a 短路求值全链——win 实物首验，弥补 runner 双编译对照 win 平台守卫面）；⑥**缺陷三方案（待用户裁决）**：甲（推荐）根除内存状态依赖=定位未初始化读取/脆弱判定（Rust 对照：确定性编译+Miri 未初始化检测；性能零开销）；乙=确定性门禁强化（同输入重复编译逐字节一致+参数模式交叉；仅防线不修复）；丙=三后端静态处理对齐审查（win/linux 逐点对照；消除分叉温床）；⑦诚实边界：本轮纯验证轮零源码改动；17 双编译对照失败为既有平台项；78/79 OOM win 固有；探针脚本留 target/p89win/ 不入库 | ⚠️ 新缺陷待修（win 侧 148_v2） |

| 2026-09-13 | 第一百零九轮 | 4'-三 | **93-a~108-a 跨机轮复跑（win-x64）+ 109-a v2 win 后端浮点 size 说明符修复**：①拉取远程 50 提交（深度机 93-a~108-a；E2E 275→292、单测 1306→1313）**无冲突合并**；**前轮立案 148_v2 崩溃已由深度机 102-a 修复**（根因=87-a `emitStaticInitsAtEntry` 「有构造初始化表达式」路径 StorePtr 丢失 → 空指针静态表 → 编译期内存状态异常 → retbuf 判定退化；本机复核 148_v2 PASS + P6a 探针 113）✓；②**全量 E2E 292 用例 257 过 / 35 败 / 0 未实现**（31 双编译对照平台限制 + 2 OOM 既有败面 + 158_v2/252 新立案）；③**109-a 修复**（v2 win 后端浮点 size 说明符 10 处——MASM movsd/movss 内存操作数须 qword/dword ptr，91-a 浮点分派同批漏点；linux GAS 无需说明符故跨机轮才暴露）→ 246 探针 v2 从 ml64 失败变 **rc=255 通过**（零回归）；④**新立案 5 项**（A 浮点说明符剩余漏点 249/250/251 调用实参装载段；B **158_v2 回归**=91-a 引入〔v2 源码维度二分：88-a ✓0 → 91-a ✗14 → 92/93/当前 ✗14〕；C **252 类名实例检查 win 侧失效**〔93-a semantic_call.cpp:820 拒绝逻辑 win 构建未生效，linux 通过——疑 UB/迭代序〕；D 260 宿主 rc=140；E 262 宿主 rc=1）；⑤**win 实物探针 11 用例**（`target/p108win/run108.py`：243/244/246/248/261/263 ✓；249/250/251〔浮点全链，250 宿主 127/251 宿主构建败 A2032〕、260/262 ✗）；⑥锚定链固定点 **403873 行**逐字节一致（109-a 后新口径；修复前 403658）+ 绑定自检 + 组件对拍 34/34（DIFF=0）+ 运行级 3/3；⑦文档四件套（HANDOFF 家机节 / 更新日志 / 本行 / 看板）| ⚠️ 部分修复（A 剩余 + B~E 待裁决） |
| 2026-09-13 | 第一百一十轮 | 4'-三 | **110-a win 侧五项立案收口 + 宿主 win 构建根治（陈旧产物假立案澄清）**：①**根因链**（本轮从清场重建开始）：**宿主构建失败被上轮忽略**——`src/cn_compiler/ir/ir_decl.cpp` **C4702**（102-a「H5 根治」有初值分支无条件 `continue` → 下方标量族段死代码；GCC 不报 / MSVC `/W4 /WX` 拦截）→ `target/Debug/cn.exe` 停留旧产物（**不含**深度机 93-a~108-a 全部宿主修复）→ 上轮全量 E2E 与 win 实物探针**全部跑在陈旧宿主上**（3 项假立案 C/D/E + A 宿主侧误判之源）；②**宿主修复**（`ir_decl.cpp`）：恢复 **isScalarStatic 守卫**（102-a 连带丢失——数组等聚合会被 8 字节 StorePtr 越界写=87-a 安全边界）——标量落标量族统一 StorePtr / 类·容器（isClassType）指针槽 StorePtr / 其余聚合保持 .data 零；清场重建**零警告** + 单测 **1313/1313**；③**A 项销项**（v2 浮点 size 说明符剩余 **4 处**：`浮宽i` 整数→浮点存回 / `宽a` 栈实参装载 / `宽b` 寄存器实参装载 / prologue **sp2** 存回——109-a「宽模式」只覆盖装载未覆盖存回）→ 249/250/251 v2 侧 ml64 **全通过** + 宿主 rc=255 双侧一致；④**B 项销项**（v2 真缺陷，源自 91-a 双计数装载改造）：**三后端结构体按值栈参数拷贝缺失**——prologue `类p==2`（栈参数）分支被标量路径截获只存 8 字节指针（调用方写指针=B3 约定）→ 被调方按内联数据访问 → 数据错（158_v2 rc=14=分歧面③六点和）；**三后端同治**（win rep movsb + X64L 逐字展开助手 + ARM64 拷贝循环助手；X64L/ARM64 拷贝逻辑提取**单点助手**供寄存器/栈两路共用——防再次分叉）+ 宿主三后端为正确参照（同一形态统一拷贝）；验证=158_v2 **rc=0** + **9 参数探针**（probe158x：win 运行 rc=0；X64L 3 处栈位 / ARM64 1 处栈位取指针+拷贝产物静态审查 ✓）；⑤**C/D/E 结案**（陈旧宿主假立案）：252 探针新宿主**正确拒绝**（93-a 语义逻辑本来已生效——上轮「win 未生效疑 UB」不成立）；260/262 双侧 rc=255——win 实物复验（run110.py）**11 用例全通过**（上轮 6/11）；⑥门禁：全量 E2E **292 用例 259 过 / 33 败 / 0 未实现**（33 败=31 双编译对照平台守卫〈win 侧需 linux as/g++ 链，非缺陷〉+ 78/79 OOM 既有败面；**158_v2/252 不再失败**——上轮 257 过/35 败）+ 锚定链固定点 **404369 行**两代逐字节一致 + 绑定自检 cn_main∈cn_self110.obj ✓ + 组件对拍 **34/34**（DIFF=0）+ 运行级 **3/3**；⑦教训（lessons 第一百一十轮段）：**陈旧产物假立案**（构建失败必须当轮根治；门禁前确认二进制时间戳晚于源码）/ MSVC·GCC 警告分叉（死代码温床——跨机轮必须清场重建）/ E2E 与对拍·锚定链**不可并行**（共享 target/v2asm.asm 冲突实证）/ size 说明符族两轮补漏（109-a 10 处 + 110-a 4 处）→静态扫描门禁登记 | ✅ 五项全收口 |
| 2026-09-13 | 第一百一十一轮 | 跨机面/门禁 | **111-a E2E runner win 双编译对照原生支持（C15 销项）+ size 说明符扫描门禁（C14 销项）+ 新缺陷 221 双侧收口**：①**C15**：win 分支 v2 产物按用例隔离（`v2work<编号>`，对齐 linux 分支）+ **双编译对照放开 win-x64**（31 个平台守卫用例转真跑）+ win v2p 预热/统一并行——win 门禁由「292 用例 259 过/33 败（31 平台守卫 + 78/79 OOM）」跃为 **290 过/2 败（仅 78/79 OOM 既有败面）**；②**C14**：runner 内联 asm size 说明符扫描器（movsd/movss 裸内存操作数；正/负样本自检——探测器本身也是被测对象）+ win 侧 ml64 前挂载（v2 产物）+ 双编译对照宿主产物挂载；③**新缺陷 221（首轮 win 真跑暴露）**：`221_双编译对照映射集合所有权` v2 退出码 254（形态①校验位丢失）——根因=**宿主 win 后端 epilogue 返回装载缺类型宽度分派**（8 字节 `mov rax,[slot]` 读 → rax 高 32 位携带栈垃圾；linux/arm64 后端均按宽度装载=win 单侧分叉）→ v2 消费 `cmp rax,0`（64 位）被污染 → 「包含(1)」误判；**双侧根治**=宿主 `x64_codegen.cpp` 按 `widthFor` 装载 + v2 三后端调用返回规范化（T_布尔 `movzx eax,al`/T_整32 **`movsxd rax,eax` 符号扩展**/T_正32 `mov eax,eax`；ARM64 对应 `uxtb`/`sxtw`/`mov w0,w0`）；**第二轮修正（锚定链为判据）**：首版 T_整32 用零扩展 → **自编译退化**（cn_self111 编译 v2 全树产出空壳函数 12969/406115 行）——v2 整32 内部为符号扩展表示（探针实证 `整32(-1)` 经调用返回 = -1），零扩展把负值变大正数破坏语义 → 改 `movsxd` 后固定点恢复；④**反证双向命中**：脏返回 obj（asm 还原 `mov rax`）+ v2 无修复 → **254 复现**；+ 有修复 → **255**（产物删指令模拟无修复 + 保留指令对照法）；⑤门禁=零警告重建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 行**两代逐字节一致 + 绑定自检 + 对拍 **34/34** + 运行级 **3/3**；⑥诚实边界=X64L/ARM64 规范化修复待 linux/arm64 跨机**动态**复验（静态产物验证 ✓：X64L 10 处 movzx / ARM64 10 处 uxtb） | ✅ 销项 |
| 2026-09-13 | 第一百一十二轮 | 工程面（D1） | **112-a D1 行数整改第三波：`ir_expr.cpp` 2798→734 行按族拆分**——四族拆出 `ir_expr_unary.cpp`（一元/三元 435）/`ir_expr_assign.cpp`（赋值 949）/`ir_expr_index.cpp`（下标 310）/`ir_expr_member.cpp`（结构体整体赋值/构造/成员访问 434）；族边界勘定=**无文件级 static/匿名命名空间依赖**（全为 IRGenerator 成员实现，声明仍在 ir.hpp）+ CMakeLists 挂载；**纯重构零行为变更（等价性硬证据=宿主编译产物 asm 五样本逐字节一致：v2 全树 404786 行 + hello/struct_enum/u240/u241）**；门禁=零警告构建 + 单测 1313/1313 + 全量 E2E 290/292 + 锚定链固定点 406097 行（与 111-a 完全一致，cn_main 地址亦同）+ 对拍 34/34 + 运行级 3/3；**勘定登记**（下波铺路）：v2 侧超长文件实为 6 个（原记 3 过时）、codegen 面 4 文件新登记、`semantic.cpp` 拆分需先建 `semantic_internal.hpp`（canonicalType 被 4 族共用） | ✅ 销项（波次） |

| 2026-09-13 | 第一百一十三轮 | 工程面（D1） | **113-a D1 行数整改第四波：`ir_oop.cpp` 1381→508 行按族拆分**——拆出 `ir_oop_container.cpp`（容器<T> 元素自动析构注入 512）/`ir_oop_field.cpp`（类字段访问 389）；族边界勘定=无匿名 ns/文件级 static 依赖（同 112-a 模式，全量复用其方法论）；**纯重构零行为变更（等价性硬证据=宿主编译产物 asm 五样本逐字节一致）**；门禁=零警告构建 + 单测 1313/1313 + 全量 E2E 290/292 + 锚定链固定点 **406097 行不变**（连续第三次）+ 对拍 34/34 + 运行级 3/3 | ✅ 销项（波次） |
| 2026-09-15 | 第一百八十三轮 | 波 3（C3） | **183-a 用户类字段泛化重试——uD/uE 根治 + 自赋值守卫 + 联合体宿主豁免补齐（家机 win-x64；基线 74946f0；先推认领 d8f4ecb）**：①**收集面泛化（142-a ① 重试·uD 根治）**：`collectOwnedStrFields` ClassObj 分支去四族 head 白名单，收集条件=「有析构 ∧ 有拷贝构造」双条件保留——142-a 崩因经 149-a 定位=uF 浅拷（180-a 已根治上游，lessons 预防 174 回退结论须标注适用期）；②**类字段位深拷通道（uE 根治）**：`emitStructWholeAssign` 新增类目标分支（源=成员/下标/三元）——preFree+NewObject+拷贝构造（源槽地址=lvalueAddress·发现二约定）+StorePtr——原路径落 CopyWithFields 而类类型顶层不展开=纯浅拷（泛化装上释放面后双释放 0xC0000374 实证后补齐）；③**聚合自赋值运行时守卫**：beginSelfAssignGuard/endSelfAssignGuard 两段式（Eq+Branch；Rust 同 place no-op 同型）落 CopyWithFields 拥有型字段路径与类目标深拷分支——纯值路径零开销；含串成员位自赋值 79-a 起既有 UAF 同轮收口；④**★116 回归定位（IR diff 纪律）**：泛化首轮 116_Feature2 稳定回归 0xC0000374 ×3 → stash 消融+IR diff=函数尾对 `出$35`（结果<资源,整32>）值字段新增 DeleteObject → 根因=「值≠字符串」顶层豁免经嵌套「结果联合$」递归绕过 → 修复=类字段收集分支补「联合体宿主不收集」（串字段 164-a 通道不动）→ 116 ×3 PASS；⑤**锚定链两层判定（预防 177）**：层 1 v2p asm 414850→416997（v2 树命中实锤）；**层 2 旧宿主 v2p 与新链 fix_p 逐字节一致（`81bef889…`·411103 行）=宿主改动对 v2 编译器行为零影响**→④v2 同构实证免做；fix_p≡fix_s（78_v2/79_v2 PASS）；⑥**探针矩阵**：userclass 形一/形二/形四残留 2→**全 0**×3 + uA~uF/chain/five/stack/count 全 0×3 + 自赋值 大小=1 残留=0 + 116 ×3 PASS；⑦**E2E 282 转正**（213 同款「残留:0」口径：形一~形四+自赋值+深拷独立性）+ 门禁=零警告+单测 1317/1317+全量 E2E 306/306（282 入列后 307 口径复跑）+运行级 3/3；⑧行为改动轮对拍口径=层 2 逐字节+E2E 全量+探针矩阵（refactor_parity 不适用，plans/020 第七十节 §六） | ✅ 波 3 ②用户类泛化收口（余=⑤H7/H11 收口→波 4） ；**⑨★合并复验揪出 190-a 拆分错位（win E2E 115/235/245 三败→根治）**：rebase 后 win 全量复验三败 → worktree bisect（184/185/186/189 绿、188/191 败）+ asm diff（寄存器指派全变）→ 真根因=190-a 把「第二遍：跨块活跃扩展」组装到 return 后=死代码（活功能静默丢失·v2p md5 假绿）→ 修复=移回 return 之前（in/blockDef/n 随活代码恢复；第一轮「当死代码删」定性错误被 E2E 纠正·lessons 预防 186~188）→ 修复后 3 用例+282 全 PASS+单测 1325/1325|
| 2026-09-15 | 第二百零四轮 | 波 3（C3） | **204-a 波 3 ⑤ H7/H11 收口——波 3 全收官（家机 win-x64；基线 94f1a8b；先推认领 4f63b3c）**：①**靶子考古→现状实测**：H7/H11（78-a B35 登记「字段容器接收者入容器位归一化保守分支」）实测——宿主有析构接收者四来源（转移真 move/调用返回接管/字面量驻留/拥有局部值语义复制）**残留全 0**；②**v2 侧同款实测**（v2p 编译+返回码承载）rc=0/22/0 与宿主逐形态一致；③**收口判定**：波 3 设施（容器字段三件套+甲通道+泛化）使保守分支前提（字段容器无释放面）消失——语义三通道齐备（转移=move/拥有返回=接管/其余=值语义复制·Rust Clone 同构）；④**反证命中**（关闭 injectFieldCascadeDestroy→E2E 283 校验和 31→0 全形态泄漏复现·唯一变量→恢复回 31）；⑤无析构面残留 2=登记设计语义两侧一致（非靶子维持）；⑥**防线=E2E 283_字段容器接收者双编译对照**（折校验和 31·借链=是）+ 全量 E2E **311/311/0/0** + 单测 1325/1325 + 零警告；⑦**波 3 全部波次完成**（余=波 4/波 7/波 8+） | ✅ 波 3 收官（B35 销项） |

### 7.3 单位机 ARM64 段（arm64 跨机验证轮记录；本机只在本段末追加行）
| 2026-09-13 | 第一百一十四轮 | 工程面（D1） | **114-a D1 行数整改第五波：`ir_oop_call.cpp` 1155→763 行按族拆分**——拆出 `ir_oop_call_release.cpp`（类析构/拥有型字符串释放族 408：genStringFrees/genClassDestructorCalls/emitStringFreeFor/emitClassDeleteFor/genBlockExitDestruct/emitStrArrayElemFreesFor/genJumpDestructFrom+ static blockExitSrcName 随族）；**纯重构零行为变更（产物 asm 五样本逐字节一致）**；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 连续第三次不变** + 对拍 34/34 + 运行级 3/3；**本会话（112-a~114-a）累计拆 3 文件 -3.3k 行** | ✅ 销项（波次） |

| 2026-09-13 | 第一百一十五轮 | 工程面（D1） | **115-a D1 行数整改第六波：`semantic_internal.hpp` 共享头建立**——11 个语义层 helper 在 5 文件重复 55 处（~600 行）收敛为 inline 单一定义（查证各副本实现逐字一致）；5 文件删副本：semantic.cpp 2102→1995 / **semantic_call.cpp 1093→988（<1000 出列）** / semantic_decl 877→767 / semantic_expr 1890→1777 / semantic_stmt 927→826；**纯去重零行为变更**（验证三重：删除段≡头核心逐行多重集 + 产物 asm 五样本逐字节一致 + 全量门禁）；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 连续第四次不变** + 对拍 34/34 + 运行级 3/3；**为 semantic 系列按族拆分铺路**（拆出族 include 头即可用 helper） | ✅ 销项（波次） |
| 日期 | 轮次 | 阶段 | 事项 | 状态 |
| 2026-09-13 | 第一百一十六轮 | 工程面（D1） | **116-a D1 行数整改第七波：`semantic_expr.cpp` 1778→654 行按族拆分**——拆出 `semantic_expr_op.cpp`（845：运算符/赋值检查——visitBinaryExpr/visitUnaryExpr/visitTernaryExpr/reportNonLvalueTarget/visitAssignmentExpr）+ `semantic_expr_access.cpp`（313：成员/下标检查——visitMemberExpr/visitIndexExpr）；115-a helper 头化铺路（族内无 static/匿名 ns 依赖）；**纯重构零行为变更（产物 asm 五样本逐字节一致 + 行级多重集校验）**；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 第五次不变** + 对拍 34/34 + 运行级 3/3 | ✅ 销项（波次） |
|---|---|---|---|---|
| 2026-09-13 | 第一百一十七轮 | 工程面（D1） | **117-a D1 行数整改第八波：`semantic.cpp` 1996→692 行按族拆分**——拆出 `semantic_types.cpp`（369：类型系统——declareTypeName→enumValueOf 类型解析 + findTopLevelComma + typeSizeOf→computeEnumValues 布局）/`semantic_sig.cpp`（455：funcReturnTypeOf→genericClassInstanceName 签名/重载/隐式转换）/`semantic_builtins.cpp`（534：registerBuiltins+registerFunction+isExternFunc 内置注册）；helper 引用矩阵=仅 cnEvalConstExpr 被主段用（留主文件）+ findTopLevelComma 随类型族；**纯重构零行为变更（产物 asm 五样本逐字节一致）**；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 第六次不变** + 对拍 34/34 + 运行级 3/3；**宿主原 7 个超长文件仅剩 ast.hpp** | ✅ 销项（波次） |
| 2026-09-11 | 第六十八轮 | — | **arm64 跨机复验（单位机麒麟）**：第六十二~六十七轮六轮改动（阶段4 不安全修饰/类方法泛型修饰/3b 传参互斥/阶段4' 拥有字符串三批+E2E runner 三重改造）arm64 动态收口——清场重建零警告+单测 1289/1289+全量 E2E linux-arm64 **228 用例 223 过**（2 败=78 OOM 已知+194 既有待批，与深度机 223 过口径对齐）+**200~209 十八用例双通道 arm64 首验全过**（常量引用/互斥/转移浅交接/安全区观察期/不安全方法/泛型修饰/传参互斥/拥有字符串/借出初始化拒绝）+**ARM64 并行 --jobs 8 首验通过**（第六十七轮三重改造 ARM64 边界清账）+锚定链 fix_p **347685 行**（arm64 新口径，上轮 322112+25573）固定点逐字节一致+绑定自检+对拍 29/29+运行级 3/3；另立案 68-a rt objs 新鲜度缺陷（plans/014 观察项表——**第六十九轮用户裁决方案A 已根治：rt objs 纳入 v2p 同款指纹缓存 cargo fingerprint 同构**） | ✅ |
| 2026-09-14 | 跨机验证轮（不占号） | — | **arm64 跨机复验（单位机麒麟）**：深度机 93-a~108-a + 家机 111-a~146-a 约 60 轮改动首次 ARM64 动态验证（基线 d25f487）——清场构建发现**「跨机-a」中文 C++ 标识符 GCC 9 构建瘫痪**（5 文件 9 名：110-a/118-a/134-a/77-a 引入；探针改名版 9 名 ASCII 化后零警告实证修复面完备）+单测 **1315/1315**+全量 E2E linux-arm64 **292 用例 288 过/1 败/3 跳**（跳=62/69/79；+64 新用例全过/194 既有待批已修复/78 通过〔上轮 OOM 败面消失〕；唯一败=14_integration2=**「跨机-b」字符串字典序幅值平台分叉立案**：__cn_str_cmp 裸返 strcmp×麒麟 glibc 对齐分叉〔rodata→1/堆→56，LD_PRELOAD 实证〕×波3 字段堆拷贝触发；规范仅定义符号语义）+**110-a 三后端栈参数修复动态收口**（9 结构体 AAPCS64 栈路径探针 rc=0 双跑+asm 栈位取指针+cpcy 拷贝环实证）+锚定链 fix_p **552331 行**（arm64 新口径，上轮 347685+204646）固定点逐字节一致+绑定自检+**对拍 44/44**（v2 树 29→44）+运行级 3/3；另：target/ 曾现 ~15 分钟空目录视图后原文件原时间戳回归（非删除，疑沙箱挂载瞬变，教训入 lessons）；两缺陷呈报待裁（三方案各附，推荐=A：ASCII 改名+门禁防复发 / 运行时归一化） | ✅（两缺陷立案待裁） |
| 2026-09-13 | 第一百一十八轮 | 工程面（D1） | **118-a D1 第九波（codegen 面开波）：`x64_instructions.cpp` 1812→921 行按族拆分**——拆出 `x64_instructions_i128.cpp`（198：i128 双槽运算 emitInt128Binary/MulDivMod/Compare）+ `x64_instructions_cast.cpp`（419：emitCast/emitCompare/emitNot）+ `x64_instructions_mem.cpp`（301：emitLoadStore/emitAddrOf/emitFieldAddr/emitPtrLoadStore）；`static shiftAmtOf` 随 emitShift 留主文件；**纯重构零行为变更（产物 asm 五样本逐字节一致）**；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 第七次不变** + 对拍 34/34 + 运行级 3/3；踩坑=LNK1168 同机另一会话占用 cn.exe（轮询等待后重试） | ✅ 销项（波次） |

| 2026-09-13 | 第一百一十九轮 | 工程面（D1） | **119-a D1 第十波（codegen 面收口）：三后端装配文件按族拆分**——`x64_codegen.cpp` 1087→**897**（拆出 `x64_codegen_data.cpp` 211）/`linux_x64_codegen.cpp` 1130→**823**（拆出 `linux_x64_codegen_emit.cpp` 329）/`arm64_codegen.cpp` 1073→**765**（拆出 `arm64_codegen_emit.cpp` 329）；**新方法论=stash 对照法**（三后端装配文件无法用 win 五样本覆盖——`git stash` 旧代码构建宿主生成 **win/linux-x86_64/linux-arm64 三目标 ×2 样本**基线 → stash pop 新宿主同生成 → **6 项逐字节一致**）；门禁=零警告 + 单测 1313/1313 + E2E 290/292 + 锚定链 **406097 第八次不变** + 对拍 34/34 + 运行级 3/3；**codegen 面全部达标**（宿主余 ast.hpp + v2 侧 + 函数级） | ✅ 销项（波次） |
〔阶段 1/2 初版诚实边界与 120~169-a 历史轮记录已滚转归档：项目记忆/归档/plans019-跟踪表与历史轮-2026-09.md（788 治理轮·两「诚实边界」标题系历史表格淹没的孤立遗迹·现行诚实边界以 plans/022 为权威）〕


## 八、风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 阶段 3 借用互斥误报率超预期 | 高 | 第一版语句级保守+豁免形态；stdlib/v2 全树当语料实测后再决定 NLL 深度 |
| 阶段 4 迁移面失控（60 处指针+联合体） | 高 | 观察期（仅警告）→分模块收口；不安全函数=安全抽象封装（迁移=把裸操作包进函数而非改写逻辑） |
| v2 自举链断供（检查器代码自身不过检） | 中 | 宿主先行；v2 检查器源码按新纪律写作（转移() 自用即 dogfooding）；过渡期豁免机制届时呈报 |
| 与既有挂账冲突（194 待批/跨机轮） | 低 | 本专项不触碰 194；win/arm64 复验随跨机轮例行 |
| 诊断质量不足（无定位/无提示） | 低 | 阶段 1 即建立「转移点定位」惯例，后续检查沿用 |

〔§八 后历史轮散段（73-a/158~165/187~226-a 等）已滚转归档：项目记忆/归档/plans019-跟踪表与历史轮-2026-09.md（788 治理轮·逐轮结论 git/plans/021 可考）〕
