# 019 CN语言编译期安全：分阶段实施路径（2026-09-10 立项）

> 性质：**已批准实施**的路线文档（用户裁决 2026-09-10：「按照你说的最优解裁决」）。
> 上游呈报：会话中三轮设计呈报（①编译期安全三方案 A/B/C → ②CN 规范语法重写 → ③可行性审阅修正）。
> 本文是全部阶段的权威实施路径；执行状态见第七节跟踪表，**每完成一个阶段必须更新**。

---

## 一、立项背景与裁决记录

### 1.1 双目标定位（既有，AGENTS.md）

v2 自举双目标：① 性能超过 C++ 编译器；② 安全接近 Rust（悬垂指针/越界/泄漏零容忍）。
自举已完成（v2self 固定点 239301 行，2026-09-10 第五十六轮口径），目标①②的编译器本体
建设进入收尾，本专项承接目标②的**编译期化**——把安全检查从运行时（越界/空指针检查）升级到
编译期（使用前拦截），对标 Rust 但**不引入 Rust 语法**（CN v1 前身即 Rust 风格，已裁决废弃）。

### 1.2 用户裁决记录（2026-09-10）

| 裁决点 | 选项 | 裁决结果 |
|---|---|---|
| 总路线 | A=Rust语义内核全量移植 / **B=值语义强化+渐进检查器** / C=纯流分析 | **B 起步、向 A 演进**（B 各阶段独立可交付，终态≈A） |
| 语法点① | 显式转移的形态 | **`转移(表达式)` 内置函数**（内置构造器 正常()/错误()/某些() 同位，零新关键字；C++ `std::move` 同构） |
| 语法点② | 只读借用的形态 | **`常量 T&` 引用类型只读修饰**（复用既有 常量 关键字；「常量成员函数」修饰先例） |
| 语法点③ | 不安全区隔离形态 | **a=函数级修饰 `不安全 函数 名(...)`**（粒度清楚、审计面明确、C++ 风格） |
| 实施方式 | — | 先写本报告 → 分步骤全部实施（每阶段：规范先行→E2E 先行→宿主→v2→门禁） |

### 1.3 审阅修正并入（会话第三轮可行性审阅结论）

前两轮设计呈报有三处乐观度，已修正并入本路径：
1. **字符串整治（原阶段1子项）= 类型系统变更**而非零语法成本（规范 02 明文「字符串变量为指针」）→
   单列为阶段 4'（智能指针与拥有型字符串），置于借用互斥之后、按值语义容器化方案另行呈报。
2. **借用互斥（阶段 3）伴随规范级使用限制**（引用不得存进容器字段等限制规则），非纯检查器逻辑。
3. **联合体必须划入不安全区**（规范 02「所有字段共享同一内存区域」无 tag，Rust union 同为
   unsafe-only）——阶段 4 安全区边界清单必须含：指针算术/下标写、联合体字段访问、FFI、字符串裸释放。

## 二、总体架构：CN 编译期安全分层模型

```
┌─ 不安全区（阶段4划定）────────────────────────────┐
│  不安全 函数 内显式操作裸指针：指针算术/下标、联合体  │
│  字段访问、FFI 外部函数、释放内存。审计面=不安全函数清单│
└──────────────────────────────────────────────┘
┌─ 受检区（阶段2~3 逐步收紧）───────────────────────┐
│  转移检查（仿射）：转移(v) 后 v 禁用                  │
│  逃逸检查：引用/指针不出所指对象生命周期              │
│  借用互斥：常量 T& 只读共存 × T& 可变独占            │
└──────────────────────────────────────────────┘
┌─ 安全区（默认，已具备=阶段0）──────────────────────┐
│  值语义深拷贝（标识符拷贝/容器追加=深拷贝，无别名）    │
│  RAII 析构 + 容器元素自动析构 + 运行时越界/空指针检查  │
│  结果/可选强制检查三规则（编译期 must_use 先例）      │
└──────────────────────────────────────────────┘
```

设计哲学（区别于 Rust 的三句话）：
1. **默认路径零规则**——CN 值语义深拷贝天然无别名，安全区代码不需要学任何新规则
   （这是 CN 相对 Rust 的体验优势：没有所有权概念也能写出安全代码，代价是拷贝）。
2. **显式放弃安全=显式获得性能**——`转移()` 是用户主动声明「此处放弃拷贝换零拷贝/交接所有权」，
   编译器则以「旧名禁用」回报安全保证（双释放/UAF/转移后使用结构性根除）。
3. **危险面收口而非全语言戒严**——指针/联合体/FFI 是系统语言必需能力，收进 不安全 函数
   显式审计，而非从语言中删除。

## 三、阶段路径总表（实施顺序=依赖序）

| 阶段 | 主题 | 编译期新增拦截 | 语法变更 | 预估规模 | 状态 |
|---|---|---|---|---|---|
| 0 | 值语义+RAII+运行时检查+强制检查三规则 | （已具备） | — | — | ✅ 既有 |
| 1 | **显式转移 转移() + 已转移使用检查（仿射）** | 转移后使用/写/传参/再转移；标量误转移 | 转移() 内置函数（§5.3.1） | 周级 | 🔄 本轮启动 |
| 2 | **引用逃逸检查强化** | 引用局部绑定逃逸（存字段/容器/全局）；返回经多级间接逃逸 | 无（规则加强） | 周级 | ✅ 第五十八轮 |
| 3 | **常量 T& 只读借用 + 借用互斥（第一版：语句级）** | 常量引用内写拒绝；同调用可变×只读别名拒绝 | 常量 T& 类型修饰（已批实施） | 月级 | ✅ 第五十九/六十轮 |
| 4 | **不安全区边界（不安全 函数 修饰）** | 裸指针算术/下标写、联合体访问、FFI、释放内存 仅限不安全函数 | 不安全 关键字+函数修饰（已批实施） | 月级+stdlib/v2 迁移 | ✅ 第一层 62 轮+第二层第一批 63 轮 |
| 4' | 智能指针与拥有型字符串（类型系统变更，另案呈报） | 空/悬垂字符串与裸释放 | 拥有型类型（方案另呈） | 月级 | 📌 三方案呈报（推荐A）待裁 |
| 5 | 并发安全 Send/Sync 对标 | 数据竞争 | 随并发标准库（plans/003 重大缺口） | 依赖并发库 | ⏸ 外部依赖 |

依赖关系：1→2→3（引用分析逐级加深）；4 独立于 2/3 可并行排期但建议后置（迁移面大）；
4' 依赖 3（拥有型语义需要转移检查先行）；5 依赖并发库立项。

## 四、阶段 1 详案：显式转移 转移() + 已转移使用检查（本轮实施）

### 4.1 语言语义（规范条文，写入 plans/001 §5.3.1 与 specs/04 第五节）

**转移(表达式)** —— 内置函数（零新关键字）：
- 实参须为**变量标识符**（第一版；成员/下标/解引用形态显式拒绝，诚实边界见 4.5）。
- 实参类型须为**拥有资源语义类型**：指针（含 字符串）、容器（向量/映射/链表/栈/队列）、
  类对象、结构体、结果/可选。**标量（整数/浮点/布尔/字符）拒绝**——复制语义无转移意义
  （诊断：「'X' 具有复制语义，无需转移」；对齐 Rust Copy 类型不适用 move 的惯例）。
- 求值结果 = 实参的值（同一对象/指针的交接），**源变量自调用点起进入「已转移」状态**。
- **已转移变量的任何后续使用均为编译错误**（E0382 对标）：
  读值 / 赋值目标 / 复合赋值 / 自增自减 / 取地址 / 解引用 / 成员访问对象侧 / 下标对象侧 /
  函数调用实参 / 返回语句 / **再次转移** / 作用域出口析构以外的全部消费形态。
  诊断消息：「'v' 已转移，不能继续使用（转移发生在 …）」（含转移点行号定位）。
- 已转移状态**不因作用域退出而恢复**（除遮蔽：内层同名新声明是新变量，不受影响——
  与作用域解析同序判定，天然正确）。
- 第一版**运行行为零变更**（诚实边界 4.5）：转移() 在语义层把调用节点改写为实参标识符
  （AST 改写，wrapRefArgs 同款手法），IR/三后端/析构零改动；容器/类对象仍按现有拷贝
  语义（指针即槽位值天然零拷贝）。**安全收益为纯编译期**：转移后使用（UAF 主形态）与
  转移后再释放（双释放主形态）结构性根除。浅转移零拷贝优化=阶段 3 随借用分析一并落
  （需要 IR 层拷贝路径分派，改动面大，独立排期）。

### 4.2 宿主实施清单（src/cn_compiler）

| # | 文件 | 改动 |
|---|---|---|
| 1 | semantic.hpp | ①`std::vector<std::unordered_set<std::string>> scopeMoved_`（与 scopes_ 平行，push/popScope 同步——scopeConsts_ 同构先例）；②`void markMoved(name, loc)`：沿作用域链找到声明层写入（遮蔽正确）；③`bool isMovedVarName(name) const`：与 lookupVar 同序解析查声明层 moved 集；④`static bool isTransferCall(Expr*)` 识别辅助 |
| 2 | semantic_expr.cpp visitCallExpr | `转移(单标识符实参)` 识别：类型白名单检查（标量拒绝/资源语义放行）→ markMoved → **AST 原地改写为实参标识符**（IR 零改动）→ 返回实参类型 |
| 3 | semantic_expr.cpp 使用点 | visitIdentifierExpr（读值）、reportNonLvalueTarget 前置（赋值/复合赋值目标）、visitUnaryExpr（取地址/&、解引用 *、++/-- 操作数）——查 isMovedVarName → 硬错误 |
| 4 | semantic_expr.cpp visitMemberExpr / 下标 | 对象侧标识符已转移 → 硬错误（经 checkExpr 对象递归自然覆盖，验证即可） |
| 5 | semantic_call.cpp | 逐实参检查：实参为已转移标识符 → 硬错误 |
| 6 | semantic_stmt.cpp visitReturnStmt | 返回表达式基础标识符已转移 → 硬错误（refReturnLvalueBase 复用解剖） |
| 7 | 单测 tests/unit/semantic/ | 新增 test_move_check.cpp：负形态（读/写/传参/返回/取地址/解引用/再转移/标量误转移/成员对象侧≈9 例精确消息）+ 正形态零误伤（转移后新变量、遮蔽新声明可用、指针交接链） |

### 4.3 v2 实施清单（CN语言编译器v2，宿主先行破循环依赖）

| # | 文件 | 改动 |
|---|---|---|
| 1 | 语义/语义检查.cn | 转移调用识别（被调文本=="转移" 且单标识符实参）+ 类型白名单（标量拒绝）+ 已转移表（名→转移行登记）+ 使用点拒绝（检查表达式 标识符分支/赋值语句左值/返回分支——195 同款三面） |
| 2 | 语义/语义检查表表达式.cn | 静默推导面豁免（诊断非幂等家族教训：第五十一轮 174 双报先例——新检查走静默缓冲或语句根权威点） |
| 3 | 主.cn 尾部 | 负测自检（源码N：转移后使用须拒——195 同款自检惯例） |
| 4 | 锚定链 | v2 源码变更 → v2p 重建 → v2self 固定点重锚（X64L 口径）+ 组件对拍 29/29 + 运行级 3 用例 |

### 4.4 E2E（先行：用例先红证明缺口存在）

| 用例 | 通道 | 断言 |
|---|---|---|
| 196_显式转移 | 宿主正测 stdout | 指针转移交接链（转移后新变量解引用/释放归属新变量）+ 容器转移（新变量追加/大小锚）+ 转移后旧名不可用（负形态在 197）+ 校验和 |
| 196_v2 | v2 闭环 rc | 同源码 v2 编译运行，校验和一致 |
| 197_转移后使用拒绝 | 宿主负测 期望编译失败.txt | 四形态诊断子串「已转移，不能继续使用」（读/写/传参/返回）+ 标量误转移诊断 |
| 197_v2 | v2 负路径 None | v2 三面诊断行+中止行固化 |

### 4.5 诚实边界（第一版）

- 转移() 仅标识符形态（成员/下标/解引用转移显式拒绝并诊断）——字段级转移随阶段 3。
- 容器/类转移运行行为仍为深拷贝（安全保证=编译期使用纪律；性能优化=浅转移随阶段 3 IR 分派）。
- 指针经函数转接（返回指针的函数调用结果再转移）不受影响（右值无需转移检查）。
- v2 静默推导面（成员/下标左值末端推导兜底检不出已转移）与 195 同族诚实边界，宿主全形态覆盖。
- stdlib/v2 全树存量代码不使用 转移()（新函数），**兼容面=零**（无需迁移审计）。

### 4.6 验收口径（阶段 1 完成定义）

1. 规范条文落入 plans/001 §5.3.1 + specs/04 第五节（含设计决策与 E0382 对标说明）。
2. E2E 196/196_v2/197/197_v2 四用例注册 run_e2e.py 并 PASS（先红后绿闭环）。
3. 单测新增锚点全绿（负形态精确消息断言）。
4. 全量门禁：清场重建 GCC 零警告 + 单测 1251+N + E2E 全量（205+4 用例，仅既有 194 待批/4 平台跳）。
5. v2 侧对齐 + v2self 锚定链重锚（X64L 口径）+ 组件对拍 29/29 + 运行级 3/3。
6. plans/019 跟踪表更新 + plans/014 状态表行 + 更新日志 + HANDOFF。

## 五、阶段 2~5 实施要点（开工前按本节展开为详案）

### 5.1 阶段 2：引用逃逸检查强化（规范已有语法，规则加强）

现状锚点：返回局部引用已拒（semantic_stmt.cpp:744，E2E 103）。扩展面：
1. **引用局部绑定逃逸**：`整32& r = x;` 后 r 存入结构体字段/容器/全局静态/返回 → 悬垂
   （x 出作用域 r 仍活）。检查：引用类型变量的消费白名单收窄（读/写/传引用参/成员基址以外
   的逃逸形态拒绝）。
2. **返回引用经多级间接**：返回 `外层.字段` 形态中 外层 为局部 → 已有；返回 条件?甲:乙 引用
   分支形态补全。
3. 指针逃逸（局部的 &x 返回）已有部分覆盖（refReturnLvalueBase），补全 &局部经容器/字段转发形态。
验收：E2E 负测族 + stdlib/v2 全树零误伤（引用面 559 处 & 使用需过检——预审必须做，误伤即回炉）。

### 5.2 阶段 3：常量 T& 只读借用 + 借用互斥第一版（含转移浅拷贝优化）

1. **语法**：`常量 向量<整64>& 数据`（类型前置 C++ 风格，常量 修饰引用参数）——parser
   参数类型解析扩展；specs/04 引用参数章节扩条文。
2. **只读检查**：常量 T& 参数体内对该参数（及经其成员访问链）赋值/传可变引用/调用可变方法
   （容器 追加/设置 等变更方法清单）→ 硬错误（对齐 C++ const T& 的 constness 检查）。
3. **借用互斥第一版（语句级保守）**：同一调用语句中，可变引用实参与只读引用实参解析到同一
   基础变量 → 歧义别名拒绝（`排序(v, v.大小())` 形态类）。完整 NLL 活跃区间分析（跨语句
   借用生命周期）**不在第一版**——先保守拒绝+显式豁免形态（临时字面量/右值），误报率实测
   （stdlib/v2 全树当语料）后再决定 NLL 深度（rustc 从 AST borrowck 到 NLL 六年的教训：
   一步到位必翻车，渐进实测是正路）。
4. **转移浅拷贝优化**（并入本阶段）：转移() 在 IR 层从「AST 改写回标识符」升级为显式
   槽位交接（容器/类对象跳过深拷贝构造，槽位直拷+源槽禁用标记），运行行为=真零拷贝。
   风险点：RAII 析构路径须跳过已转移槽（语义层 moved 集传 IR）。
验收：E2E 正负双通道 + 性能锚（转移前后 bench 对比，零拷贝收益实证）+ 全树零误伤。

### 5.3 阶段 4：不安全区边界（不安全 函数 修饰，需二次呈报细则后开工）

- `不安全 函数 名(...) {}`：不安全函数体内方可使用——指针算术/指针下标写、联合体字段访问、
  外部 函数 调用、释放内存/释放内部数组 等裸操作。安全函数调用不安全函数**允许**
  （安全责任上移到不安全函数实现者，Rust unsafe fn 同模型）。
- 迁移面：stdlib + v2 全树 60 处指针声明与解引用/下标使用须包进不安全函数（或改写为安全
  抽象）——**分批迁移**：先标记不迁移只警告（观察期），再按模块收口硬错误。
- 关键字 `不安全` 入规范 01a（真实语法消费点=函数修饰位，符合保留字最小化原则）。
- CFI（plans/008 既有 --cfi 基础）与不安全区联动：不安全函数=CFI 目标表之外的外部调用面。

### 5.4 阶段 4'：智能指针与拥有型字符串（类型系统变更，方案另呈报后实施）

拥有型字符串（替代「UTF-8 裸指针+调用方释放」约定）与 唯一指针/共享指针 标准库类型。
**必须在阶段 3 落地后开工**（拥有语义依赖转移检查）。方案（析构注入/ABI/与驻留互操作/
打印链路适配）届时三方案呈报。

### 5.5 阶段 5：并发安全（依赖并发标准库立项，plans/003 批次 C）

Send/Sync 对标：类型级可跨线程标记 + 检查器拒绝违例传参。随线程/锁/原子标准库一并设计。

## 六、工程纪律（每阶段强制）

1. **宿主先行**：检查器先在 src/cn_compiler 落地（破 v2 自举循环依赖——v2 用 CN 写检查器
   时可参照宿主实现），随后 v2 对齐。
2. **E2E 先行**：每阶段先写负测用例证明缺口存在（当前静默通过=红），再实现转绿。
3. **全量门禁**：清场重建零警告 + 全量单测 + 全量 E2E（含既有基线不劣化——194 待批挂账维持）。
4. **锚定链**：v2 源码变更后 v2self 固定点重锚 + 组件对拍 29/29 + 运行级 3 用例（X64L 口径；
   win/arm64 随跨机轮）。
5. **全树零误伤预审**：检查器类改动（阶段 2/3）动工前先跑 stdlib+v2 全树过检预审，
   误伤形态回炉规则设计（rustc 误报迭代教训）。
6. **诊断质量**：负向诊断统一含行号定位与迁移提示（「已转移，不能继续使用（转移发生在 行N）」
   对标 E0382 的 borrow 定位体验）。
7. 文档四件套同提交：plans/019 跟踪表 + plans/014 状态表 + 更新日志 + HANDOFF + lessons。

## 七、执行状态跟踪表（每阶段更新）

| 日期 | 轮次 | 阶段 | 事项 | 状态 |
|---|---|---|---|---|
| 2026-09-10 | — | — | 立项：本路径文档 + 用户裁决记录 | ✅ 本文档 |
| 2026-09-10 | 第五十七轮 | 1 | 规范条文（plans/001 §5.3.1 显式转移 + specs/04 五内置构造器扩 转移()） | ✅ |
| | | 1 | E2E 196/197 先行（红：转移未声明=缺口实证）→ 双绿（196 正测四形态 stdout 2/1/左/55；197 负测五形态诊断子串） | ✅ |
| | | 1 | 宿主实施：scopeMoved_（与 scopes_ 平行）+markMovedVar/lookupMoved/reportMovedUse/isTransferCall/transferArgKind + visitCallExpr 表达式位特判（指针/字符串放行、标量拒、拥有类型拒）+ visitVarDecl 声明位 AST 改写（initializer=实参，豁免窗口 inTransferRewrite_）+ visitIdentifierExpr/visitAssignmentExpr 使用拒绝 + 转移 内置占位注册 + ir_call 展开（lastExpr_=实参求值） | ✅ |
| | | 1 | 单测 test_move_check.cpp 13/13（负九：读值/赋值/传参/返回/再转移/取地址/标量/非变量目标/类表达式位；正四：指针链/类声明位/遮蔽/字符串） | ✅ |
| | | 1 | v2 对齐：符号.已转移 第 5 字段+表标记转移/表查已转移/转移实参分级（语义分析.cn）+ 检查调用表达式 特判/检查标识符表达式 moved 检查与 转移 占位放行（语义检查表达式.cn）+ 赋值左值 moved 检查（语义检查.cn）+ 生成调用 转移展开（生成表达式求值.cn，类型大小折叠同位，弹2压回实参值） | ✅ |
| | | 1 | E2E 196_v2（rc=7 三形态校验和：指针交接/字符串长度/遮蔽新指针）/197_v2（负路径 5 诊断+中止行，实测固化 expected）注册双绿；195/125_v2 基线回归绿 | ✅ |
| | | 1 | 锚定链重锚（X64L 新口径）：v2p→fix_p **242022 行**（239301+2721=v2 转移检查源码确定性体现）→cn_self57→fix_s **242022 行**→**固定点逐字节一致** + 绑定自检 cn_main=0x4dca4a∈cn_self57.o .text + 组件对拍 29/29 + 运行级 3/3（hello 你好，世界/119 rc=14/173 rc=173） | ✅ |
| | | 1 | 全量门禁：清场重建零警告+单测 1264/1264（1251+13）+E2E 209 用例（205+4 新增；唯一败=194 既有待批，4 平台跳） | ✅ |
| 2026-09-10 | 第五十八轮 | 2 | 预审：stdlib「= &局部」零命中；v2 引用局部 20 处全为 `向量<IR指令>& 指令 = 函数信息.指令`（绑参数引用=指向调用方存储，非拦截面）——零误伤确认；缺口实证（静态/字段/指针返回四形态静默） | ✅ |
| | | 2 | E2E 198_引用逃逸拒绝（宿主负测五形态：静态槽/静态指针字段/指针返回&局部/返回指向局部的指针/引用返回经引用局部——真先红=子串锚新消息「存入静态/全局存储」）+ 199_推断声明字符串拷贝（缺陷①正测，先红 4285000 实证） | ✅ |
| | | 2 | 宿主实施：isLocalAddressValue（&局部经 refReturnLvalueBase 解剖 + 引用局部绑局部）+ refLocalBases_/ptrLocalPointees_ 函数级表 + visitAssignmentExpr 逃逸位拒绝（目标=静态/全局标识符/非局部对象字段[自行解剖对象链——refReturnLvalueBase 对 deref 短路会放行静态指针字段]）+ 指向登记（赋值位+声明位）+ visitReturnStmt 指针返回局部地址拒绝（含经指针转接）+ 缺陷①方案A（推断回填扩展字符串，布尔维持） | ✅ |
| | | 2 | v2 实施：是局部名 + 赋值逃逸（右值=&局部×左值非局部）+ 返回逃逸（返回类型尾 */& 时拦 返回&局部；**引用参数豁免**=当前函数引用参数们 静态集[检查函数 收集 类型节点附加==256]，宿主 isRefParamForCurrentFn 对齐）+ 缺陷②（语法分析.cn 返回类型 \* 后缀并入——两路径 & 并入同款） | ✅ |
| | | 2 | E2E 198_v2（负路径 2 诊断+中止行实测固化）/199_v2（正路径 rc=3：-> 整64* 返回 &引用参数 合法+解引用读写回）注册；单测 test_escape_check.cpp 10/10（负五正五；指针数组声明语法窄面单测不锚注记） | ✅ |
| | | 2 | 验证矩阵：清场重建零警告+单测 1274/1274（1264+10）+E2E 213 用例 208 过/1 败（194 既有待批）/4 跳；锚定链 fix_p **244050 行**（242022+2028）→cn_self58→fix_s 244050→**固定点逐字节一致**+绑定自检 cn_main=0x4de72f∈cn_self58.o .text [0x402586,0x4e218e)+组件对拍 29/29+运行级 3/3 | ✅ |
| 2026-09-10 | 第五十九轮 | 3 | 宿主实施：ParamDecl.isConstParam+parseParamDecl 常量 前缀 + FunctionInfo.constParams + checkConstRefBorrowDiscipline（①只读借出可变拒 ②同调用可变×只读互斥[语句级第一版]）普通函数调用面接入 + 赋值面（标识符/成员链只读拒）+ 借出升级洞补（常量引用参数不能作 T& 返回） | ✅ |
| | | 3 | v2 实施：解析参数 常量 前缀（类型节点附加 256→257=常量引用；IR 层 >=256 判定天然按引用 ABI）+ 检查函数 收集常量引用参数 + 赋值面只读拒 | ✅ |
| | | 3 | E2E 200/201/200_v2/201_v2 四用例双通道（正测只读读链 rc=42/42 与 42+42 校验和 rc=3；负测四形态[赋值/复合/传可变/互斥]子串「只读借用」）+ 单测 test_const_ref_check.cpp 7/7 | ✅ |
| | | 3 | 验证矩阵：清场重建零警告+单测 1281/1281（1274+7）+E2E 217 用例 212 过/1 败（194 既有）/4 跳；锚定链 fix_p **244605 行**→固定点逐字节一致+绑定自检 cn_main=0x4dee8f∈.text [0x402586,0x4e291b)+对拍 29/29+运行级 3/3 | ✅ |
| 2026-09-10 | 第六十轮 | 3b | 语义面两项落地：**双可变别名拒**（同调用同一变量可变借用两次=别名可变冲突，存量预审 tests/e2e+stdlib+v2 全树零命中）+ **构造/方法调用面接入借用纪律**（ClassMemberInfo.constParams+class_resolver 两处登记+676/802/833 三处 checkConstRefBorrowDiscipline——函数指针面无 常量 形参语法天然不适用） | ✅ |
| | | 3b | E2E 202_双可变借用互斥（负测两形态：普通函数+构造面，子串「被可变借用两次」）+ 单测扩 2 例（9/9）；本轮 v2 零改动（固定点维持 244605） | ✅ |
| 2026-09-10 | 第六十一轮 | 3b | **转移浅拷贝优化（性能主项）双侧落地**：宿主=语义层登记 transferDeclSources_（VarDecl 节点→源名）+ IR genVarDecl 浅交接分支（跳过 NewObject+拷贝构造，句柄直拷+**源槽清零**——源 RAII 析构对零句柄走既有 DeleteObject 空安全跳过=恰好一次释放；条件分支两路径均正确：条件假=转移未执行=句柄仍在源槽）；v2=**声明位容器/类转移放开**（声明位转移中 标志+分级 2 放行；豁免模型浅交接=调用特判实参值压栈+按值 Store 句柄拷贝+调用初值 RAII 豁免，**零 IR 改动**） | ✅ |
| | | 3b | E2E 203_转移浅交接（宿主正测：析构计数锚定——转移恰好析构 **1 次**（深拷对照探针=2 次，tp4/tp5 实证））+203_v2（容器浅交接校验和 rc=7，注册含 v2p.obj 借链）+196/110/111/113/55 转移拷贝面回归 11/11 | ✅ |
| | | 3b | 验证矩阵：清场重建零警告+单测 1283/1283+E2E 220 用例 215 过/1 败（194 既有）/4 跳；锚定链 fix_p **245230 行**→固定点逐字节一致+绑定自检 cn_main=0x4df7c7∈.text+对拍 29/29+运行级 3/3 | ✅ |
| 2026-09-10 | 第六十五轮 | 3b | **v2 传可变/互斥面对齐落地**：①调用实参末位表（解析调用后缀 逐实参记录末节点索引——键=调用节点索引*键位移+序号，查函数公开）；②函数参数借用位表（声明收集 登记签名键→引用位[类型节点附加>=256]/常量位[==257]）；③检查调用借用纪律（主路径[键基近似=被调名]+签名回退段[键基精确]双挂点：①传可变拒[实参∈当前常量引用集×形参可变引用] ②同调用可变×只读互斥[实参末位标识符基础名比对]） | ✅ |
| | | 3b | E2E 207_v2_传参与互斥拒绝（2 诊断+中止行实测固化）+全量 224 用例 219 过零回归+锚定链 fix_p **247461 行**（+2011=末位表/借用位表/检查源码确定性）→固定点逐字节一致+对拍 29/29+运行级 3/3 | ✅ |
| | | 3b | 剩余：跨语句 NLL 活跃区间——**按误报率实测定深度**（前提=真实使用反馈期数据，当前无真实使用=NLL 实测条件不成熟，维持排程） | ⬜ |
| | | — | 【立案】类静态成员+析构写静态成员运行期崩溃（tp3 探针：无转移纯两对象构造+析构即崩——独立既有缺陷非本轮引入，随跨机轮呈报三方案） | 📌 待呈报 |
| 2026-09-10 | 第六十二轮 | 4 | **第一层（观察期）落地（方案 B 用户裁决）**：lexer Kw_Unsafe+关键字表 / parser 顶层 `不安全 函数` 前缀（FunctionDecl.isUnsafe）/ FunctionInfo.isUnsafe+currentFnUnsafe_ / warnUnsafeBoundary（观察期 Warning）五警告点：指针算术（Add/Subtract×指针类型）+指针下标写（赋值目标 Index×对象指针）+联合体字段访问（findStruct.isUnion）+外部函数调用（info.isExtern）+裸释放（callee=="释放"）/ 警告可见性（runModulePipeline+runPipeline+runCheck 仅警告也输出——原被 hasErrors 短路吞掉） | ✅ |
| | | 4 | 规范：plans/001 §5.3.1 后「不安全 函数 修饰（阶段4·观察期）」+specs/01a 关键字 59→60（安全区边界段 5.3c）；E2E 204_安全区边界观察期（正测：不安全内零警告+责任上移+联合体警告不阻断，stdout 7/3）+单测 test_unsafe_boundary.cpp 6/6（安全警告/不安全豁免/调用上移） | ✅ |
| | | 4 | 验证矩阵：清场重建零警告+单测 1289/1289（1283+6）+E2E 221 用例 216 过/1 败（194 既有）/4 跳——**观察期零误伤**（既有用例警告不阻断+产物不变）；本轮 v2 零改动=固定点维持 245230 | ✅ |
| | | 4 | 第二层（分批收口）：stdlib/v2 按模块迁移（裸操作包进 不安全 函数或安全抽象）→收口变硬错误+类方法修饰位+v2 对齐——**方案 B 排程（迁移批次呈报后执行）** | ⬜ |
| 2026-09-10 | 第六十二轮 | 4' | 三方案呈报——A 拥有型字符串（字符串 升拥有类型：析构自动释放+裸指针降为 字符*；牵动驻留/打印/stdlib/v2 全链迁移）/B 约定强化（零类型变更但防线不完备）/C 并行新类型（双轨期长）。推荐 A | 呈报完毕 |
| 2026-09-10 | — | 4' | **用户裁决：方案 A**（会话直接答复「A」）——字符串升拥有类型+裸指针降级 字符*，分批迁移过渡兼容 | ✅ 裁决落档 c4ca546 |
| 2026-09-10 | 第六十六轮 | 4'-一 | **第一批（核心语义）落地**：①IR 全自持 RAII（genStringFrees：本函数 Alloca∩oopVarSrcTypes_ 字符串槽∩非污染——入口块零初始化+每返回块注入 __cn_str_free 空安全+返回值=槽 Load 移出跳过[IR 识别]）；②初始化拥有化（字面量/标识符经 __cn_str_copy 落堆——Rust to_string 同款；调用白名单直存）；③赋值=free 旧+拥有化新（drop 旧+move 新）；④**拥有判定白名单**（字面量/标识符/内置分配族八函数——**驻留文本 返回驻留表指针=借用、v2 用户函数 当前文本=借用包装**（黑名单拦不住用户函数→白名单制根治，v2p 自举全瘫两层定位：开关二分证注入所致→形态统计锁定 当前文本）；⑤**下标字符串元素借出污染**（姓名数组[位置]=副本 指针逃逸模型兼容——38_tool 乱码根治零测试改动）；⑥stdlib 字符串扩展 替换 函数迁移（手动释放删+声明外提+缓冲=转移(拼接) 零拷贝 move） | ✅ |
| | | 4'-一 | E2E 208_拥有字符串（字面量/拷贝/赋值/返回移出/转移/比较/长度七形态）+38_tool 指针逃逸兼容零回归+全量 **225 用例 220 过**（唯一败=194 既有）+清场重建零警告+单测 1289/1289+锚定链**固定点 247461 逐字节一致**（拥有化注入未改变 v2 树产物——白名单外的 v2 借用形态全污染=零 free 注入）+对拍 29/29+运行级 3/3 | ✅ |
| | | 4'-一 | 诚实边界：转字符串族（整数/浮点/字符转字符串）不入白名单（存量手动释放兼容——38_tool 15 处手动释放依赖此判定）；循环内声明=同槽末迭代释放（中间迭代泄漏——类 RAII 同款模型限制）；参数/静态=借用不 RAII；用户函数返回拥有语义随第二批 | 📌 |
| 2026-09-11 | 第六十七轮 | 4'-二 | **第二批（v2 侧机制）落地**：①预扫白名单登记+槽预分配+入口零初始化（平铺池布局覆盖嵌套声明——与容器同模型）；②初始化拥有化（字面量/标识符 __cn_str_copy 落堆+嵌套同名借用/同名异型复槽双污染防御）；③赋值拥有化（free 旧[drop 拥有旧值——优于宿主保守面零泄漏]+先 copy 后 free 自赋值安全序+二元拼接/白名单直存[+= 语法层重析为二元同覆盖]+非白名单污染退出+下标借出污染[38_tool 模型对齐]）；④双挂点释放（返回块+块出口截断；污染名单跳过；函数IR 加 字符串变量们/字符串污染们——生成函数 显式初始化[裸声明字段无默认构造——半成品崩因根治]）；⑤调用发射统一 IR_调用实参+IR_调用参数化 对形态（IR_调用(52) 在 v2 代码生成无处理器=静默丢指令——半成品第二处修正）；⑥v2 树盘点：顶层唯一字面量初始化=返回名v（借用赋值经污染传播正确退出）——自举闭环 | ✅ |
| | | 4'-二 | E2E 208_v2_拥有字符串（七形态校验和 127 全对）+全量 226 用例 221 过（1 败=194 既有）+清场重建零警告+单测 1289/1289+锚定链 fix_p **254365**（新口径）固定点逐字节一致+对拍 29/29+运行级 3/3 | ✅ |
| | | 4'-二 | **E2E runner 三重改造（用户裁决：太慢/去硬编码/全量并行）**：v2p 构建缓存（v2 全树指纹——消除 58 用例×全树重建最大瓶颈）+**数据驱动去硬编码**（v2闭环.txt 配置文件进用例目录取代 runner 注册表——新增 v2 用例零代码）+**全量统一并行 --jobs N**（Linux v2 产物按用例隔离：v2work{编号} cwd 隔离+stdlib 软链+入口绝对+日志绝对前缀适配[.expected 不动]+v2asm_N/v2out_N 编号+池前 v2p 预热；win v2 未隔离保持桶串行兜底）——全量 18.3s/27.7s（热/冷），原串行估 20+ 分钟 | ✅ |
| | | 4'-二 | 诚实边界：用户函数返回字符串=借用（调用初值不登记——返回拥有类型区分须新语义（返回 字符串=拥有/字符*=借用）**随第三批类型区分呈报**）；转字符串族不入白名单（既有）；宿主**初始化位下标借出**（字符串 s=arr[i] 不 copy 不污染→free 悬垂——推断待实证呈报）；win 平台 v2 并行=下轮跨机轮验证项 | 📌 |
| 2026-09-11 | 第六十七轮 | 4'-A1 | **字符串借出初始化拒绝落地（方案A 用户批准——三探针实证呈报后裁决）**：宿主 semantic_decl+ v2 语义检查 双侧诊断（下标/成员/解引用初值→编译错误「须 字符串复制(...) 显式落堆」）；实证证据=驻留借出静默跳过（cn_free_tracked 注册表防御）+堆串借出重新赋值→数组元素悬垂读取乱码（UAF 三轮复现）；存量预审零冲击（纯借出形态=0）；E2E 209/209_v2 双通道负测 | ✅ |
| | | 4'-A2 | A2=第三批正文：字符* 借用视图类型化+返回 字符串=拥有/字符*=借用（缺口②用户函数返回泄漏根治）——stdlib/v2 树迁移面盘点呈报后分批 | ⬜ 排程 |
| 2026-09-11 | 第六十八轮 | 4'-A2 | **A2 迁移面盘点呈报（三方案待裁决）**：全树 `-> 字符串` 函数 52（v2 树 30+stdlib 3+tests 19）——A 类全拥有 ~25（零改动，A2 后调用方自动登记根治泄漏）/B 类混合借用分支 15 处（参数直传 9 函数+驻留绑定返回 3+静态 1[取当前模块路径]+字段借出 2[取语义诊断/货舱查找依赖]+赋值污染 1[解析目标平台]——须 字符串复制 显式拥有化）/C 类纯借用 2（当前文本/推导实参类型文本→改 `-> 字符*` 零拷贝）；**内置规范变更**：驻留文本 返回 字符串→字符*（借用显式化——v2 树 186 调用中 65 处「字符串变量=驻留文本」须复制包裹[~10 处循环内=热点审计点]）+转字符串族入拥有判定；**手动释放契约退役面**：tests 18 文件 ~106 处 字符串释放（A2 后=双重释放——38_tool 15 处转字符串族接收/35·12 stdlib C 风格契约/69 与 RAII 重叠靠 cn_free_tracked 防御兜底）须删除+4 函数体迁移（56 读名称/52 读取书名·读取ISBN·校验编号）；实现面宿主 4 点（ir_stmt:800/ir_expr:1713 白名单∪返回类型+返回位借用拒绝+返回位字面量拥有化[字面量驻只读段，调用方 free 只读段指针=崩溃，须 copy 落堆]+字符*→字符串收紧）+v2 3 点；三方案：甲=完整类型化+存量全迁移（两轮：宿主+树+tests→v2 侧+重锚）[推荐]/乙=仅登记不收紧[不可行——free 借用=UAF 比泄漏更糟]/丙=字符串视图新类型[与方案A 裁决冲突] | 呈报（待批） |
| 2026-09-11 | 第六十八轮 | 4'-A2 | **用户裁决：方案甲「全部实施」**——①**轮1（宿主侧，0ed437b）**：语义四件套（CallExpr.retOwnedString 决议标记[普通/类方法/静态/接口/变参/fallback 全路径，单态化产物签名键含$豁免=借用保守]+初始化/赋值位登记白名单∪返回类型契约+返回位借用拒绝[参数/成员/下标/解引用/字符*/全局静态×泛型体内豁免]+返回位字面量拥有化+字符*→字符串双向收紧+赋值位借出装入拒绝[「字符串变量=恒拥有槽」不变量——污染返回链洞根治]）+驻留文本改字符*（内置注册）+三树迁移（v2 树 96+v1 树 18+tests 5 文件）+**根因级修复：静态向量<字符串>追加浅共享×源 RAII 释放=悬垂**（模块路径们 键基合成垃圾键→多文件符号解析全瘫——追加处显式复制，「字符串入容器须显式复制」纪律）+E2E 210 正测（六形态）/210 返回借用拒绝（七形态负测）+门禁全绿（清场零警告+单测 1289/1289+230 用例 225 过唯一败=194 既有）；②**轮2（v2 侧，83b26ac）**：IR容器 预扫登记恢复（混乱期假&&残留修复）+契约扩展+字符*同码/IR布局 驻留文本字符*+字符串复制入内置两表/生成声明·生成语句 契约/语义检查 返回位+收紧/生成语句 返回位字面量拥有化+210_v2（六形态校验和 63）+E2E 231 用例 226 过全绿 | ✅ |
| 2026-09-11 | 第六十八轮 | 4'-A2 | **用户裁决：方案甲专门轮深潜→当轮根治（c668919）**——插桩链实证（登记/查询键集两代一致+错误点上下文+签.返回文本 两代对比）：根因=「字符串入结构体/容器浅共享×源 RAII 释放=悬垂」两处同族（IR签名 收集类型参数序 出.追加(参数序)——序出[0] 悬垂→泛型序串垃圾→T 替换失效→[T]→45 错；IR容器 查方法签名 出.返回文本/出.参数串 直存同族）——第一代堆未复用侥幸=固定点绿假象；修复=存入显式 字符串复制；验证链全绿：清场零警告+单测 1289/1289+E2E 231/226（194 既有）+锚定链 fix_p **262313 行**≡fix_s 裸逐字节一致+绑定自检+运行级 3/3（208v2/119/173）+对拍 29/29 | ✅ 根治 |
| 2026-09-11 | 第六十八轮 | — | **arm64 跨机复验（单位机麒麟）**：第六十二~六十七轮六轮改动（阶段4 不安全修饰/类方法泛型修饰/3b 传参互斥/阶段4' 拥有字符串三批+E2E runner 三重改造）arm64 动态收口——清场重建零警告+单测 1289/1289+全量 E2E linux-arm64 **228 用例 223 过**（2 败=78 OOM 已知+194 既有待批，与深度机 223 过口径对齐）+**200~209 十八用例双通道 arm64 首验全过**（常量引用/互斥/转移浅交接/安全区观察期/不安全方法/泛型修饰/传参互斥/拥有字符串/借出初始化拒绝）+**ARM64 并行 --jobs 8 首验通过**（第六十七轮三重改造 ARM64 边界清账）+锚定链 fix_p **347685 行**（arm64 新口径，上轮 322112+25573）固定点逐字节一致+绑定自检+对拍 29/29+运行级 3/3；另立案 68-a rt objs 新鲜度缺陷（plans/014 观察项表——**第六十九轮用户裁决方案A 已根治：rt objs 纳入 v2p 同款指纹缓存 cargo fingerprint 同构**） | ✅ |
| 2026-09-11 | 第七十轮 | 4'-A2 | **70-a 宿主契约二分残留根治（方案A 用户裁决）+装箱 move 语义**：①恢复 semantic_call.cpp 普通函数决议路径 retOwnedString=false&& 残留（轮1 二分禁用忘恢复——双侧语义分叉被三树显式化掩盖）+全树 false&& 复审清零；②系统::参数 argv 借用→双侧内置注册改 字符*（宿主+v2 IR布局）+stdlib 包装显式复制+v2 主.cn 3 处/tests 37·38 六处调用适配；③取语义诊断 容器元素借用返回位显式复制（B 类轮1 漏修）；④**装箱 move 语义**（ir_error_ctor）：正常/某些(拥有字符串局部) 值字段写入后源槽清零（转移() 同模型，Rust Ok(s) move 零拷贝）——根治「装箱浅共享×局部 RAII=悬垂」家族第五实例（stdlib 读取行包装链：v2p 读源文件全空实证）；⑤验证链：v2p 复活 57054 节点+清场零警告+单测 1289+E2E 唯一败=194 既有+固定点 **262321**（新口径）逐字节一致+运行级 3/3+对拍 29/29；另双机并行合并轮（9f5d22f：深度机 A2+单位机 arm64 收口/68-a） | ✅ 根治 |
| 2026-09-11 | 第七十轮下半程 | 4'-A2 | **手动释放收尾批（方案甲既有授权）**：①转字符串族经 70-a 契约自动登记（探针实证）——「入拥有判定」项天然兑现；②实删 103 处（tests 17 文件 96+stdlib 7）：局部拥有面双重释放退役（RAII 接管+cn_free_tracked 兜底消除）；保留 10 处借用面（数组元素/装箱字段/69 计数断言/分割 输出契约）；③stdlib 四文件+tests 五文件契约注释同步 A2 口径；④门禁全绿：单测 1289+E2E 唯一败=194 既有+固定点 262321 维持+对拍 29/29——**A2 拥有型字符串全链收官**（第一批 RAII→第二批 v2→A1 借出拒绝→A2 返回区分→70-a 契约根治→收尾退役） | ✅ 收官 |
| 2026-09-11 | 第七十一轮 | 4'-三 | **71-a v2 内置构造器全链补全+装箱 move（方案A 用户裁决）+71-b 二分禁用残留恢复**：①立案呈报——HANDOFF 任务#1「v2 侧装箱 move 对齐」调查发现 v2 对规范 07-五 内置构造器（正常/错误/某些）整体缺失（探针实证 `返回 正常(行)` 报「未声明函数」中止；接收侧完整、构造侧全无），三方案呈报用户裁决 A（全链+move 一步到位，Rust Ok/Err/Some 同构）；②语义层：构造器识别（转移 同位特判+被调标识符放行）+装箱语境类型（返回位=函数返回类型/声明位=声明类型，静态语境变量）写入调用节点.子终点（解析期空闲位，语义驱动 IR）；③IR：生成装箱构造（布局 正常@0+值@4/8 双侧与宿主一致、基址+字段地址+指针存储 按宽度写、零新指令=三后端零改动）+返回 结果/可选 retbuf ABI（函数IR.返回结果名ID 新字段+显式初始化清单+三后端 prologue 判定+调用方 retbuf 扩展）；④move：函数级 已装箱名们（实参单标识符登记）——出口三处（字符串释放/容器析构/块出口析构两段）跳过=所有权转交（宿主 70-a 源槽清零同效，双装箱退化借用更安全）；⑤接收侧顺带根治：.值/.错误 加载宽度按载荷类型（整32 载荷 4 字节读——原 8 字节拼垃圾既有缺口）；⑥71-b：第六十七轮「// 二分禁用」注释式残留（引用返回路径 发射字符串释放 被禁=泄漏）恢复+全树 grep 双形态复审清零；⑦E2E 211_v2（八形态校验和 255）；⑧门禁全绿：清场零警告+单测 1289+E2E 232 用例 227 过唯一败=194 既有+固定点 **266459**（新口径）逐字节一致+绑定自检+运行级+对拍 29/29 | ✅ 根治 |
| 2026-09-11 | 第七十二轮 | 4'-三 | **六枚缺陷同轮根治（F1~F6，方案A 用户默认授权）**：①**F1 跨模块拥有契约失效（静默永久泄漏）**——`semantic_call.cpp` 用 `sigKey.find('$')` 识别泛型单态化产物，但跨模块链接键也是 `模块$名` 形态（`functionLinkKey` 公式），凡导入模块的函数返回 字符串 调用方一律不登记 RAII（81 个含导入 E2E 用例 + stdlib 全部 `-> 字符串` 函数受影响；探针实证同文件有 free、跨模块无 free）；改按 `genericFuncInstances_` 精确判定（rustc DefId 同款）。v2 侧同族三处（生成声明/生成语句/IR容器）纯名查改 `查函数返回回退` 键基回退版（E2E 165 回归实证）。②**F4 赋值位转移非真 move（UAF）**——`目标 = 转移(源)` 原为共享句柄+源出口释放（stdlib `字符串扩展::替换` 的 `缓冲 = 转移(拼接)` 实测乱码）；宿主 `ir_expr.cpp` 赋值位新增真 move 分支（drop 旧目标+句柄直存+源槽清零，Rust `a = b` 对 String 同款）。③**F5 引用参数出参悬垂（UAF）**——`字符串& 出参` 写入本地拥有串后本函数出口释放源局部（v2 树 解析货舱/拆限定名 两函数踩中，被 F1 修复暴露）；按 Rust `*out = s` move 语义（句柄直写调用方槽+源槽清零）。④**F6 变参调用未设 AL（SysV ABI 违约）**——CN x64 代码生成从未发 `mov al, N`（glibc `va_arg` 按 AL 判定向量寄存器保存数），基线靠 rax 残留侥幸（`格式化("¥%.2f", 29.50)` 经包装函数输出 ¥0.00；F3 改动改变 rax 值即暴露）；`linux_x64_instructions.cpp` 调用点按实参统计发射。⑤**F2 v2 落空路径零释放（永久泄漏）**——v2 只在 返回 语句路径发射释放，体末无 返回 的函数（fall-through）完全不释放；`生成函数.cn` 体块未终止时在函数尾块前发射出口释放（已终止路径由 返回 语句路径负责——首版无条件发射致 196_v2/210_v2 回归，条件化后零回归）。⑥**F3 宿主块级作用域 RAII=呈报待裁**（探针实证循环体内声明 4 轮残留 3；实现后改变 115/116/76 既有测试断言的析构时机语义，按「测试文件不可擅自修改」纪律**回退**，重新呈报）。E2E 新增 212_跨模块契约与转移与引用参数（校验和 255）+212_v2_落空路径与拥有串释放（校验和 7）；门禁全绿：清场重建零警告+单测 1289/1289+E2E **234 用例 229 过**（唯一败=194 既有待批）/4 跳+固定点 **266964**（新口径）逐字节一致+绑定自检+对拍 29/29 | ✅ 根治 |
| 2026-09-11 | 第七十二轮 | 4'-三 | **F3 宿主块级作用域 RAII 落地（用户裁决方案A：实施+同步更新用例期望）**：①实现——`ir.hpp` 新增 作用域基线栈（串/类对象名单基线）+ `LoopContext` 块基线（中断/继续 drop-on-jump）+ `genVarDecl` 入口名单登记（genBlock 出口释放，污染名跳过）+ `genBlockExitDestruct`/`genLoopJumpDestruct`（逆序释放+名单截断）；**释放采用「free+槽清零」幂等模型**（块出口/跳出/函数级兜底多路径共享同一槽——清零后后续 free(nullptr) 空安全，杜绝条件释放与兜底释放的双重释放）；函数级 `genStringFrees`/`genClassDestructorCalls` 保留为返回路径兜底。②**连带根治 v1 树既有缺陷**：块级 RAII 使「拥有串经函数浅共享入容器=元素悬垂」立即暴露（76 自举用例产物常量池退化为指针值）——按 A2 纪律（字符串入容器须显式复制）迁移 6 处追加点（语义/符号表.列表追加、IR生成/IR1.IR列表追加、IR遍历.列表追加、语法分析.记录、x64后端.追加、arm64后端.追加）；该缺陷为**既有**（F3 关闭时探针同样悬垂，仅靠内存未复用侥幸）。③**用例期望同步（用户批准）**：115 总析构 3→6（向量操作 3 + 块出口 r1/r2/r3 3）、116 块内计数全变（链表块结束 3→6 / 栈 3→6 / 队弹出 3→9 / 队清空 5→11）——注释同步改块级 RAII 口径；76 经 v1 迁移恢复原期望（1764）**无需改测试**。④验证链：清场重建零警告+单测 1289/1289+E2E **234 用例 229 过**（唯一败=194 既有待批）+锚定链固定点 **266964** 逐字节一致+绑定自检+对拍 29/29；探针实测循环体/嵌套块/中断路径残留全归零（原 4 轮残留 3） | ✅ 根治 |
| 2026-09-11 | 第七十二轮 | 4'-三 | **72-a 收尾（用户裁决「瑕疵清理+三层防线全部实施」）**：①静态审查 4+1 瑕疵清理——`(void)0` 死代码删除、`LoopContext::scopeDepth` 改名 `classBase`（实为类名单基线）、genVarDecl 注释修正（登记+释放侧跳过双保险口径）、泛型出口补五状态 clear（实证 visitProgram 批量生成不嵌套）、switch 中断 drop-on-jump（SwitchContext 携带基线）；②**类方法返回路径缺陷当轮根治**（静态推演发现+探针 87 实证）：`emitClassMethod` 独漏函数级兜底——方法体内拥有串+中途 返回=泄漏（残留 1→0）；③三层防线落地——E2E 213（八形态残留全零）/214（容器所有权搅动）转正+**plans/020 拥有权流转矩阵建档**（空白即靶子：入容器编译器级自动化/数组元素/装箱字段/结构体字段/v2 跳出路径核验）+NLL 治本归口；④验证链：清场重建零警告+单测 1289/1289+E2E **236 用例 231 过**（唯一败=194 既有待批）+固定点 **266964** 逐字节一致+对拍 29/29 | ✅ 收尾闭环 |
| 2026-09-11 | 第七十二轮 | 4'-三 | **72-b 中断绑定 C 语义（用户裁决方案B）**：`中断` 绑定最近进入的选择或循环（enterSeq 进入序比较替换 loopStack_ 恒优先——原实现循环内选择体的 中断 误吞整个循环，探针 88 与 C 对照实证）；存量形态扫描=0 处（tests/stdlib/v1/v2 全量）迁移零影响；E2E 215 四场景固化+规格书 plans/001+specs/03 同步条款；v2 树无选择语句无同族缺口；验证链：清场重建零警告+单测 1289/1289+E2E 237 用例 232 过（唯一败=194 既有）+固定点 266964 维持+对拍 29/29 | ✅ 语义闭环 |
| | | 4'-三 | 第三批（裸指针形态显式化 字符* 清理=A2 正文） | ⬜ 排程 |
| | | 5 | **等待并发标准库立项**（线程/锁/原子=plans/003 批次 C 重大缺口）——Send/Sync 对标随库设计（外部依赖非本专项可独立推进） | ⏸ 外依 |
| | | 4 | 第二层分批收口**批次方案**：第一批 stdlib 容器内部裸操作+类方法修饰位；第二批 v2 自举树 60 处指针面（含 v2 语法层认 不安全）；第三批收口变硬错误+删除观察期。每批=独立轮次全链门禁 | 呈报（第一/二批已按用户「最优解全部实施」总授权落地——见下） |
| 2026-09-10 | 第六十三轮 | 4-二① | **第二层第一批落地**：类方法 不安全 修饰位全链（parser_oop 修饰循环+ClassMethodDecl/ClassMemberInfo.isUnsafe+checkClassMethods 上下文 currentFnUnsafe_ 设定/复位）+ stdlib 裸操作盘点迁移（算法.cn 排序整32/排序整64/变换整32/变换整64+字符串扩展.cn 分割 五函数加 不安全——裸指针算法 API 契约显式化；逆序 泛型函数零警告不迁） | ✅ |
| | | 4-二① | E2E 205_不安全方法修饰（stdout 5/6）+全量 222 用例 217 过零回归+锚定链**固定点 245230 逐字节不变**（修饰对产物语义透明的天然实证）+对拍 29/29+运行级 3/3 | ✅ |
| 2026-09-10 | 第六十四轮 | 4-二② | **第二层第二批落地**：宿主泛型函数修饰语法（泛型 <T> 不安全 函数——parser_oop 泛型分支扩展+逆序 补迁）+ **泛型类实例化修饰位拷贝补丁**（generics.cpp 实例化 mi 拷贝漏 isUnsafe——tg 探针泛型类修饰失效实证后补）+ **v2 语法层认 不安全**（词法关键字表+解析程序顶层分支——**首版分支吞掉原「函数」分支致 v2p 全解析瘫痪（hello 节点=0），双分支恢复根治**）+ **v2 树+stdlib 全量迁移**：v2 自身 43 函数+stdlib 映射集合 9 方法（容器/算法/字符串扩展 第六十三轮已迁）——**v2 主链+stdlib 警告全归零**（175→0） | ✅ |
| | | 4-二② | E2E 206_泛型与不安全修饰（用例内泛型单态化调用零警告，stdout 3/21——stdlib 泛型跨模块导入不可见为**既有独立边界**立案）+全量 223 用例 218 过零回归+锚定链 fix_p **245450 行**（+220=v2 修饰+词法表驻留源码确定性体现）→固定点逐字节一致+对拍 29/29+运行级 3/3 | ✅ |
| | | 4-二③ | 第三批收口变硬错误+删观察期（迁移已全量完成=收口前置条件就绪；**收口时点呈报**——建议真实使用反馈期后） | ⬜ 呈报 |
| | | — | 【立案·既有边界】stdlib 泛型函数跨模块导入不可见（ti 探针：修饰前后同败——32_algorithm 靠本地算法.cn 规避；与阶段4 修饰无关） | 📌 |
| | | 4~5 | （依排程：阶段 4 不安全区 → 阶段 5 并发） | ⬜ |

### 阶段 2 诚实边界（2026-09-10 第五十八轮）

- 逃逸检查覆盖面（宿主）：赋值位=静态/全局标识符+非局部对象字段/元素；返回位=T* 返回（&局部直接+引用局部绑局部+经直接 &local 登记的局部指针转接[声明位/赋值位登记]）。**检不出**：指针间传递后返回（p=q 链）、容器元素持局部地址（plans/019 §5.1 容器位收缩——容器值语义深拷为主形态）、经引用参数写回逃逸（跨函数数据流）。局部对象字段/局部数组元素接收局部地址=合法放行（随所在作用域消亡）。
- v2 逃逸检查面=赋值位（&局部×非局部标识符）+返回位（&局部，引用参数豁免）；成员/下标目标位与引用局部表 v2 随引用面扩大对齐。
- 缺陷①根治范围=字符串（布尔维持 184 契约）；缺陷②=v2 语法层返回类型 \* 后缀（多级 \* 与 IR 下游宽度面未专项验证——单 \* 锚定）。
- v2 静态标量在函数内按栈槽处理（199_v2 探针实测）——v2 静态标量语义为独立观察项（宿主全局静态 .data 语义）。

### 阶段 1 诚实边界（2026-09-10 第五十七轮）

- **支持面矩阵**：指针/字符串=任意位置（表达式/声明/赋值/传参/返回）；容器/类/结构体/结果/可选=**仅声明初始化位**（宿主=AST 改写深拷贝既有语义；赋值/传参/返回位拒绝随阶段3 浅拷贝优化）；标量/布尔/字符/枚举/函数指针=拒绝（复制语义）；v2 第一版=指针/字符串全位置（**容器/类位 v2 全拒**，随阶段3 对齐——196_v2 已注记）。
- **运行行为**：第一版转移()零运行时指令（纯编译期标记）；容器/类声明位=深拷贝（指针=槽位值天然零拷贝）；浅转移零拷贝与析构跳过=阶段 3（IR 拷贝路径分派工程）。
- **既有缺陷立案（随本轮呈报，非转移引入）**：①宿主「推断声明（变量）+字符串标识符拷贝」打印打出指针值（probe_f/h 实证：`变量 乙 = 甲` → 4285000；显式 `字符串 丙 = 甲` 正常）——推断类型未传 IR 打印分派，46-c 结构体回填同族，标量/字符串回填涉 184 布尔行为契约须裁决；②v2 语法层不支持 `-> 整64*` 指针返回类型（t2 探针；宿主支持——v2 语法层返回类型解析缺口）。
- 探针与脚本留 target/p57x64l/、/tmp/ 不入库。

## 八、风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 阶段 3 借用互斥误报率超预期 | 高 | 第一版语句级保守+豁免形态；stdlib/v2 全树当语料实测后再决定 NLL 深度 |
| 阶段 4 迁移面失控（60 处指针+联合体） | 高 | 观察期（仅警告）→分模块收口；不安全函数=安全抽象封装（迁移=把裸操作包进函数而非改写逻辑） |
| v2 自举链断供（检查器代码自身不过检） | 中 | 宿主先行；v2 检查器源码按新纪律写作（转移() 自用即 dogfooding）；过渡期豁免机制届时呈报 |
| 与既有挂账冲突（194 待批/跨机轮） | 低 | 本专项不触碰 194；win/arm64 复验随跨机轮例行 |
| 诊断质量不足（无定位/无提示） | 低 | 阶段 1 即建立「转移点定位」惯例，后续检查沿用 |
