# CN语言编译器实施计划（阶段七：自举）

> 前序文档：[`plans/002-00-总览与约束.md`](plans/002-00-总览与约束.md)（目标/架构/技术栈/全局约束/执行策略）
> 前序文档：[`plans/002-阶段六-标准库.md`](plans/002-阶段六-标准库.md)（阶段六：标准库）
> 前序文档：[plans/004-自举前置条件检查.md](plans/004-自举前置条件检查.md)（第四批入口：前置条件检查报告，2026-08）
> 后续文档：无（最终阶段，自举成功后 CN 编译器完整交付）
> 基于设计规格书：[`plans/001 CN语言编译器设计规格书.md`](plans/001%20CN语言编译器设计规格书.md)

## 阶段七：自举

> 目标：用CN语言重写编译器自身（词法→语法→语义→IR→代码生成），用CN编译器编译CN编译器。里程碑：CN语言编译器自举成功（规格书阶段七）。

### Task 7.1: CN词法分析器（自举）
**文件：**
- Create: `CN语言编译器/词法分析.cn`（用CN重写词法分析器，2026-08-16 迁移至产品目录
  `CN语言编译器/`，包入口 `CN语言编译器/包.cn` 经 候选2b 上溯加载）
- ✅ 已完成（2026-08-16，E2E 70/71 + 单测 3 项）：
  （标识符/61关键字/进制数字+后缀/浮点/字符串含转义/原始""/多行"""/字符含 \u{XXXX}/
  行注释/块注释/39运算符贪婪匹配/分隔符/行号跟踪；输出 "类型|文本|行" 与 C++ 版
  cn token 逐行一致）。顺带修复编译器缺陷：
  - crate 前缀判定用 sigKey.find('$') 误判泛型容器参数签名（记录#向量$字符串,...）
    为"已带前缀" -> 模块前缀缺失 LNK2019（semantic_call.cpp 按 $ 位于 # 前才算前缀）
  - 包目录重复拼接（<入口>/CN语言编译器//CN语言编译器/词法分析.cn）：moduleDir
    若已被模块实际目录包含则置空 + entryDir 去尾斜杠防双斜杠（driver_module.cpp）
  - **大栈帧 guard 页跳越崩溃**（自举阶段7 关键修复）：__chkstk 前写参数槽
    （[rbp-36KB]）跳过 guard 页 -> 0xC0000005；大栈帧参数寄存器预存调用方影子空间
    （[rsp+8..+40]），__chkstk 后从影子空间搬运到参数槽（x64_codegen.cpp）
  - 关键字表补全 遍历/中/每个（range-for 语法）

### Task 7.2: CN语法分析器（自举）
**文件：**
- ✅ 已完成（2026-08-16，E2E 72）：`CN语言编译器/语法分析.cn`
  （程序/导入/泛型类/结构体/联合体/枚举/类分区/方法/构造/析构/修饰/字段、函数
  声明/参数列表/默认值、语句全家（变量/如果否则链/当/循环/选择情况默认/返回/
  中断继续/遍历中每个/块）、表达式 13 级优先级/三目/赋值族/强制转换 类型(表达式)/
  原始多行前缀/初始化列表；AST dump 151 行逐行比对 E2E 72）。
  顺带修复自举组件缺陷：参数个数 配对闭括号即返回（原误扫后续逗号）、
  泛型形参 "类型 T" 仅计形参名、变量名取类型后缀后首个标识符（整64* 指针）、
  强制转换 类型(表达式) 分支、遍历/中/每个 关键字。

### Task 7.3: CN语义分析器（自举）
**文件：**
- ✅ 已完成（2026-08-16，E2E 73 + 单测 2 项）：`CN语言编译器/语义分析.cn`
  （内部 分词->语法分析（AST 行）->语义检查；符号表（函数/参数/变量/类型）
  登记、重复声明检查、未声明标识符检查；输出 符号|/错误| 诊断行 7 行逐行
  比对 E2E 73）。顺带修复自举组件缺陷：局部变量与同名函数遮蔽导致
  调用被解析为未初始化变量（call r11 垃圾地址崩溃，函数名2 规避）；
  函数参数数 提取 "参数数=" 10 字节偏移。

### Task 7.4: CN-IR生成器（自举）
**文件：**
- ✅ 已完成（2026-08-16，E2E 74 + 单测 2 项）：`CN语言编译器/IR生成.cn`
  （内部 分词->语法分析（AST 行）->IR 生成；输出简化三地址 IR 行：函数|名|参数数、
  指令|参数/分配/常量/加载/运算/调用/返回/分支，16 行逐行比对 E2E 74；
  标识符+调用 合并输出、调用行跳过、参数数 10 字节偏移提取）。
- ✅ **v2 改造（2026-08-17，E2E 74 适配双参数）**：`IR生成(源码, 模块名)` 双参数
  签名（模块名用于符号 mangling 前缀），行数 997 行（≤1000 合规）。

### Task 7.5: CN代码生成器（自举）
**文件：**
- ✅ 已完成（2026-08-16，E2E 75 + 单测 2 项）：`CN语言编译器/代码生成.cn`
  （输入 IR 行 -> 简化 x64 伪汇编行：段|.code、函数|名、汇编|参数/分配/
  mov rax/运算/call/ret/分支，17 行逐行比对 E2E 75；指令行按子类型
  行类型/行文本 二次解析提取细节）。
- ✅ **v2 改造（2026-08-17，E2E 75 重写为 IR 全文+ASM 特征摘要策略）**：
  `代码生成.cn` 重写为**真实 x64 MASM 后端**（971 行，≤1000 合规）：
  - `.data` 字符串池（UTF-8 字节编码）+ `.code` 段
  - `EXTERN` 声明 19+ 运行时符号（打印/字符串/内存等）
  - `PROC`/`ENDP` 函数框架 + 栈帧（push r12 + push rbp / mov rbp,rsp / sub rsp,N）
  - 求值栈 `[rbp-F+r12]`（r12 为栈顶指针）
  - `__chkstk` 三段式栈探测（大栈帧）
  - 参数寄存器映射 rcx/rdx/r8/r9 + 栈参 + 影子空间 + 16 字节对齐
  - 符号 mangling `?UTF8hex@@Y码@Z`（与 C++ 版 x64_codegen 对齐）

### Task 7.6: 自举验证
**文件：**
- ✅ 已完成（2026-08-16，E2E 76 + 单测 2 项）：`tests/e2e/76_self_host_bootstrap/`
  （阶段1：C++ 版 cn 编译器编译 CN 版组件（E2E 70~75 已覆盖）；阶段2：CN 组件链
  词法->语法->语义->IR->代码生成 编译 CN 组件自身源码（CN语言编译器/词法分析.cn
  477 行），各阶段产物行数确定性输出（词法 2417/语法 1657/语义 82/IR 1989/
  代码生成 7923）+ 自举|验证|通过，逐行比对 E2E 76）。
  顺带修复编译器缺陷：
  - 模板尖括号实参 类型关键字+* 后缀（结果<空类型*, 整32>）isTemplateAngleOpen
    返回 false -> 顶层声明解析失败（stdlib/文件.cn 首次编译实测）
  - 限定调用自引用遮蔽（stdlib 包装 文件::读取文件行 解析到自身包装 ->
    返回 结果 与内置 字符串 类型冲突）：当前编译模块 == 目标模块且内置存在时
    内置优先（27_module 用户模块限定调用不受影响）
  - 链接栈 1MB -> 8MB：大帧（regId 全局累计 35KB/函数）递归组件链栈溢出
    0xC00000FD（解析 477 行组件源码实测）

### Task 7.7: 自举真实闭环（v2 全链路，2026-08-17）
**文件：**
- ✅ 已完成（2026-08-17，E2E 77/78/79 + 单测 1 项契约断言同步）：
  - **E2E 77_audit_dump**：审计转储用例（补 expected，清理调试残留，保留
    主.cn/主.expected/样例.cn）
  - **E2E 78_chain_build**：CN 组件链编译 5 个组件源码 -> 落盘 5 个 `*_链.asm`
    （输出 5 行 `完成:模块名`，逐行比对）
  - **E2E 79_bootstrap_closed_loop（真实闭环）**：完整自举闭环——
    ① C++ 版编译 79 主.cn -> 运行落盘 5 个 `*_链.asm`（第一次，CN 组件链产物）
    ② ml64 汇编 5 个 .obj
    ③ 链接（链.obj 在前 + 入口 obj + 9 运行时 .obj）-> `cn_compiler_self.exe`
    ④ 运行 CN 版编译器 exe 再次落盘（第二次）
    ⑤ `fc /b` 逐字节比对两次产物一致
    ⑥ 符号自检（map 确认 5 个链.obj 贡献符号 + cn_main 来自入口 obj）
  - **run_e2e.py**：新增 79 闭环编排 + ML64/LINK 动态探测（覆盖 14.38/14.44 +
    Windows Kits 最新版）
  - **单测**：`tests/unit/semantic/test_bootstrap_lexer.cpp` 契约断言同步 v2
    （IR生成 双参数 / 真实 MASM 断言）

**验证标准：** 步骤1：用C++版cn编译器编译CN版编译器源码 → CN版编译器可运行；步骤2：用CN版编译器再次编译自身源码 → 产物行为一致（自举成功，规格书阶段七）。E2E用例回归：全部E2E用例在自举编译器下通过。

> ✅ **验收标准已达成（2026-08-17）**：
> - **步骤1 ✓**：C++ 版 cn.exe 编译 CN 版编译器源码（5 组件 + 包.cn）→ 可运行
> - **步骤2 ✓**：CN 版编译器（cn_compiler_self.exe）再次编译自身源码 → 产物与
>   C++ 版产物 **fc /b 逐字节一致**（阶段7验收步骤2「产物行为一致」）
> - **E2E 回归 ✓**：79/79 全绿（含 79 真实闭环用例）
> - **单测 ✓**：1189/1189；**构建 ✓**：0 错误 0 警告（MSVC /W4 /WX）
> - 提交：2ad4874/632fe86/a90f3ca/de482d7/b3157ee（2026-08-16）+ 本轮真实闭环提交（2026-08-17）

**阶段七完成条件：** 自举验证通过（两阶段自举）+ 全部E2E回归通过 + 更新 `plans/002` 阶段七打勾 + Git提交。

---

## B 任务：多文件组件拆分（第一阶段，2026-08-18）

> **目标**：将 CN 组件链从单文件实现拆分为「门面 + 子模块」多文件结构。本阶段拆
> 词法分析.cn、语法分析.cn、语义分析.cn 3 个组件；IR生成.cn、代码生成.cn 留到下一阶段。

### 拆分结果（全部 ≤1000 行）

| 组件 | 门面文件 | 子模块 |
|------|---------|--------|
| 词法分析.cn（478 行） | 词法分析.cn（158 行）：分词 + 记录 + 字节值 | 词法/关键字.cn（64 行）、词法/运算符.cn（94 行）、词法/扫描.cn（181 行） |
| 语法分析.cn（966 行） | 语法分析.cn（113 行）：解析 + token 工具 | 语法/表达式.cn（287 行）、语法/语句.cn（221 行）、语法/声明.cn（210 行）、语法/顶层.cn（176 行） |
| 语义分析.cn（194 行） | 语义分析.cn（98 行）：语义检查 + 检查 | 语义/符号表.cn（33 行）、语义/内置.cn（74 行） |

### 关键前置工作（用户已审批）

- **前置验证**：C++ 版 cn 支持 `模块::函数` 限定名调用 ✅；CN 版语法分析.cn/IR生成.cn
  **不支持** `::` 限定名调用 ❌
- **扩展语法分析.cn**：`解析后缀` 增加 `::` 分支，输出 `成员|::|名` 行
- **扩展 IR生成.cn**：`处理成员节点` 增加 `::` 分支，生成 `模块名$函数名` 符号
  （忽略调用文本中的模块名，用当前模块名——79 闭环合并门面+子模块源码后统一 IR 生成）

### 模块系统适配

- 新增模块声明文件：**词法.cn、语法.cn、语义.cn**（`模块 关键字` 等声明挂载子模块）
- 更新 **包.cn** 导入全部子模块（词法::关键字 等 9 个子模块）
- **子模块间不写导入**（避免 语法->语句->语法 循环依赖），函数经门面导入链合并后
  全局可见，纯名调用
- **语义门面辅助函数用完整三级路径限定名**（`语义::内置::行类型` 等），避免与
  IR生成.cn/代码生成.cn 同名函数纯名歧义

### 79/78 主.cn 改造（多文件编译）

- `编译模块` 增加 `向量<字符串> 子模块路径们` 参数：读门面源码 + 合并子模块源码后
  统一 IR 生成（保证调用符号与定义符号匹配）
- 主函数为每个组件传入子模块路径列表（词法 3 个、语法 4 个、语义 2 个、IR/代码生成 空）
- 补全 `结果.正常` 检查的 `否则` 分支

### 验证结果

- **E2E 70~79 全部通过（79/79）**
- **76 expected 更新**（门面文件变小，行数变化：词法 2417→717、语法 1657→492、
  语义 82→50、IR 1989→451、代码生成 7923→1764）
- **79 自举闭环通过**：CN 版组件链成功编译拆分后的门面+子模块合并源码，两次产物
  逐字节一致（自举固定点仍成立）

### 踩过的坑（已修复，勿再踩）

1. CN 版组件链不支持 `::` 限定名调用——拆分门面前必须先扩展语法分析.cn + IR生成.cn
2. 子模块导入父模块形成循环依赖（语法->语句->语法）——子模块间不写导入，纯名调用
3. 辅助函数名跨模块冲突（语义/内置.cn 的 行类型 与 IR生成.cn 同名）——完整三级路径限定名
4. 门面局部变量遮蔽模块名（`向量<字符串> 符号表` 与模块名 符号表 冲突）——完整路径区分
5. `结果.正常` 检查后必须处理 `否则` 分支（C++ 版语义分析器强制检查）
