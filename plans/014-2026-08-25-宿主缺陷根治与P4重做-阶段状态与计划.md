# 014 宿主缺陷根治与 P4 重做：阶段状态与计划（2026-08-25）

> 本文档按用户规则（2026-08-25）建立：缺陷状态 / 进行步骤 / 后续计划须全部写入
> plans/ 目录文档，按时间顺序从现有序号（013）之后接着排序；**每完成一个阶段任务
> 必须更新本文档**，用户据此查看总体计划防止项目走偏。

## 一、总体背景（自举重建战略）

CN 自举编译器「推倒重建」为性能最优架构（2026-08-25 用户决策）：结构化中间表示 +
arena/bump 一次性分配 + 字符串驻留(Symbol 整数ID) + 哈希符号表 + 只最后一层输出汇编文本。
对标 rustc/LLVM。核心规则：
- **宿主缺陷优先**：遇宿主（编译器/工具链）缺陷立即暂停现阶段任务，根治于 src/cn_compiler，
  禁止用牺牲性能的规避手段凑合（规则49 一步到位 / 规则60 一劳永逸）。
- **测试=规范**：测试先行，已通过测试不轻易改动，改动需用户批准。

## 二、已完善（本轮及前期）

| 编号 | 缺陷/任务 | 状态 | 验证 |
|---|---|---|---|
| H3 | 嵌套泛型参数解析 + 构造运行段错误（向量<映射<...>>）| ✅ 已根治 | 6 提交贯通 |
| H6 | 类方法引用参数（盒子& 其他）成员访问失败——ir_oop.cpp setupMethodParams 未设 byRef | ✅ 已根治 | E2E 110/111 PASS |
| H7 | 类类型栈变量无初始化器声明缺 NewObject+默认构造+RAII | ✅ 已根治（含无默认构造语义报错）| E2E 108/109 PASS |
| 方案A | 拷贝构造全链路：识别/查找/强制规则/三处接线/stdlib 深拷贝 | ✅ 已完成 | E2E 110/111/112 PASS |
| H8-① | 嵌套泛型实参映射——朴素 $ 分割截断（typeArgs 存储修复）| ✅ 已根治 | 盒子<映射>=56 / 盒子<整64>=8 |
| H8-② | sizeof 共享 AST 节点污染——IR 按实例 genericTypeParams_ 重算 | ✅ 已根治 | 同上 |
| H8-③ | ptrElemStride 类元素步长（原兜底 8）| ✅ 已根治 | 向量<简单盒> 99/3/42 |
| H8-④ | 向量类元素存储 CopyStruct + 读取返回地址（无析构类）| ✅ 已根治 | 同上 |
| H8-⑤ | **容器持有类对象所有权**：追加类元素深拷贝独立（拷贝构造 this=内联地址）+ 非拥有式绑定（跳过 RAII）+ 弹出销毁（stdlib 释放内部数组）| ✅ 已根治 | E2E 113 PASS（遮蔽/真删除/生命周期）|
| P4 | 语义分析.cn 符号表 → 向量<映射<整64,符号>> 真嵌套作用域栈（第6版）| ✅ 已完成 | v2 冒烟 [sem5] 错误数 0/1 正确 |
| 嵌套字段访问 | **结果<结构体>.值.字段 链式访问宿主缺陷**——五个叠加缺陷根治：① 结果/可选 局部变量合成结构体未提前降级（visitVarDecl ensureLoweredType）→ IR 槽 1 槽（应 2+）CopyStruct 溢出覆盖相邻变量；② visitMemberExpr 嵌套成员对象类型推导不识别 结果.值 成员映射（返回常量 0）；③ handleResultCtor 结构体值 StorePtr 存地址（改 CopyStruct 内联）+ 结构体字段读 LoadPtr（改返回字段地址）+ 错误() 存 E 而非 T（错误码当地址崩溃）+ 真实联合体偏移（整32=4/整64=8）+ 自然宽度存储；④ memberObjStructType 结果成员映射（lvalueAddress 嵌套偏移）；⑤ ? 运算符 propagateType=函数返回类型 + 读/写偏移分离 + exprSrcType 补 CallExpr + 宽错误码传播 | ✅ 已根治 | E2E 114 PASS（嵌套 5/101、临时 5、错误 7）+ 60 错误传播全 PASS |
| 回归基线 | 全量 E2E 114/116 + 单测 1196/1196（仅 78/79 已知遗留）| ✅ 保持 | 每次改动后复跑 |

新增 E2E：108_H7 / 109_H7 / 110_H6 / 111_H6 / 112_方案A强制规则负测 / 113_H8_容器持有类对象。

## 三、未完善（已知缺陷/阻塞）

| 编号 | 缺陷/任务 | 状态 | 阻塞 |
|---|---|---|---|
| — | 编译器级「向量元素自动析构」（Feature 2 完整版：~向量/删除/清空 自动销毁类元素）| ⚠ 未实现 | 当前用 stdlib 释放内部数组 + 作用域栈显式销毁（组件级）；通用版后续专项 |
| — | 链式方法调用（元素(i).设置() / 返回.取字段）寻址错（既有遗留）| ⚠ 待核验 | 专项 |
| — | 78/79 E2E 内存超限（自举组件 O(n²) 字符串处理，峰值 4.1GB>4096MB 防护）| ⚠ 已知遗留 | HANDOFF 记录，非本轮改动引入 |

## 四、当前步骤

**[嵌套结构体字段访问宿主缺陷已根治并验证]**（2026-08-25，用户批准根治后执行）：
- 复现：结果<符号,整32> 查.值.名ID 打印 0 / 地址；正常(结构体值) 破坏相邻变量。
- 根因定位：五个叠加宿主缺陷（见二、嵌套字段访问 行）——局部变量 结果 合成结构体
  未提前降级（IR 槽 1 槽溢出）、嵌套成员类型解析缺失（常量 0）、结构体值 StorePtr
  存地址（应 CopyStruct 内联）、结构体字段读 LoadPtr（应返回地址）、? 运算符
  propagateType 用操作数类型 + 偏移统一（宽错误码传播错位）。
- 修复文件：semantic_decl.cpp（visitVarDecl ensureLoweredType）、semantic.cpp
  （fieldOffsetOf 真实联合体偏移）、semantic_expr.cpp（propagateType=函数返回类型）、
  ir_expr.cpp（visitMemberExpr/memberObjStructType 结果成员映射 + 结构体字段读 +
  Propagate 读/写分离）、ir_error_ctor.cpp（CopyStruct 内联 + 错误存 E + 自然宽度）、
  ir_oop.cpp（exprSrcType 补 CallExpr 分支）。
- 新增 E2E 114_宿主缺陷_嵌套结构体字段访问（嵌套 5/101、临时 5、错误 7）；
  60_error_propagate 错误传播全 PASS（含宽错误码 整32->整64）。
- 前序：H8-⑤ 所有权 + P4 重做（语义分析.cn 第6版真嵌套作用域栈）——E2E 113 PASS、
  v2 冒烟 [sem5] 诊断正确。

## 五、后续计划（按序）

1. **自举重建推进**：v2 结构化 IR 生成 / 代码生成组件，直至真正自举闭环（性能最优架构目标）。
2. **编译器级「向量元素自动析构」**（Feature 2 完整版：~向量/删除/清空 自动销毁类元素）——当前用 stdlib 释放内部数组 + 组件显式销毁，通用化留专项。
3. **78/79 组件链内存根治**：自举组件字符串 O(n²) 处理改流式（IR 生成逐行而非整体拼接），解除 4096MB 防护瓶颈。
4. **宿主缺陷专项**：链式方法调用等既有遗留逐项根治（P4 代码可同步去除 结果.值 临时绑定规避——已根治，可清理）。
5. **文档同步**：每阶段完成后更新本文档（用户规则）。

## 六、工作区状态

- 已提交并推送 9b4a7c3（H6/H7/H8-①~④/方案A + E2E 108~112 + plans/014）。
- 当前未提交：H8-⑤ 所有权（ir_expr/ir_stmt/ir_oop/ir.hpp）+ stdlib 释放内部数组 + P4 重做（语义分析.cn 第6版）+ E2E 113 + **嵌套字段访问根治（semantic_decl/semantic/semantic_expr/ir_expr/ir_error_ctor/ir_oop）+ E2E 114 + 60 错误传播修复** + plans/014 更新。
- 回归：全量 E2E 114/116（78/79 已知遗留内存超限，环境抖动偶发 0xC0000005）、单测 1196/1196。
