# 014 宿主缺陷根治与 P4 重做：阶段状态与计划（2026-08-25）

> 本文档按用户规则（2026-08-25）建立：缺陷状态 / 进行步骤 / 后续计划须全部写入
> plans/ 目录文档，按时间顺序从现有序号（013）之后接着排序；**每完成一个阶段任务
> 必须更新本文档**，用户据此查看总体计划防止项目走偏。

## 一、总体背景（自举重建战略）

CN 自举编译器「推倒重建」为性能最优架构（2026-08-25 用户决策）：结构化中间表示 +
arena/bump 一次性分配 + 字符串驻留(Symbol 整数ID) + 哈希符号表 + 只最后一层输出汇编文本。
对标 rustc/LLVM。核心规则：
- **宿主缺陷优先**：遇宿主（编译器/工具链）缺陷立即暂停现阶段任务，根治于 src/cn_compiler，
  禁止用牺牲性能的规避手段凑合（规则49 一步到位 / 规则60 一劳永逸）。
- **测试=规范**：测试先行，已通过测试不轻易改动，改动需用户批准。

## 二、已完善（本轮及前期）

| 编号 | 缺陷/任务 | 状态 | 验证 |
|---|---|---|---|
| H3 | 嵌套泛型参数解析 + 构造运行段错误（向量<映射<...>>）| ✅ 已根治 | 6 提交贯通 |
| H6 | 类方法引用参数（盒子& 其他）成员访问失败——ir_oop.cpp setupMethodParams 未设 byRef | ✅ 已根治 | E2E 110/111 PASS |
| H7 | 类类型栈变量无初始化器声明缺 NewObject+默认构造+RAII | ✅ 已根治（含无默认构造语义报错）| E2E 108/109 PASS |
| 方案A | 拷贝构造全链路：识别/查找/强制规则/三处接线/stdlib 深拷贝 | ✅ 已完成 | E2E 110/111/112 PASS |
| H8-① | 嵌套泛型实参映射——朴素 $ 分割截断（typeArgs 存储修复）| ✅ 已根治 | 盒子<映射>=56 / 盒子<整64>=8 |
| H8-② | sizeof 共享 AST 节点污染——IR 按实例 genericTypeParams_ 重算 | ✅ 已根治 | 同上 |
| H8-③ | ptrElemStride 类元素步长（原兜底 8）| ✅ 已根治 | 向量<简单盒> 99/3/42 |
| H8-④ | 向量类元素存储 CopyStruct + 读取返回地址（无析构类）| ✅ 已根治 | 同上 |
| 回归基线 | 全量 E2E 112/114 + 单测 1196/1196（仅 78/79 已知遗留）| ✅ 保持 | 每次改动后复跑 |

新增 E2E：108_H7 / 109_H7 / 110_H6 / 111_H6 / 112_方案A强制规则负测。

## 三、未完善（已知缺陷/阻塞）

| 编号 | 缺陷/任务 | 状态 | 阻塞 |
|---|---|---|---|
| H8-⑤ | **带析构类（映射）存向量所有权**：追加浅拷贝共享内部数组指针 + 局部/绑定变量各自 RAII 析构 = 双重释放 0xC0000374 | 🔴 阻塞中 | 需「容器持有类对象」所有权语义（设计已提交，待用户批阅）|
| — | 链式方法调用（元素(i).设置() / 返回.取字段）寻址错（既有遗留）| ⚠ 待核验 | 可能影响 P4 访问路径 |
| P4 | 语义分析.cn 符号表「单映射+深度延迟删除」权宜 → 向量<映射<整64,符号>> 真嵌套作用域栈 | 🔴 阻塞中 | 依赖 H8-⑤ |
| — | 78/79 E2E 内存超限（自举组件 O(n²) 字符串处理，峰值 4.1GB>4096MB 防护）| ⚠ 已知遗留 | HANDOFF 记录，非本轮改动引入 |
| — | 链式"结构体方法返回.直接取字段"寻址错（自举组件已用临时变量规避，编译器级修复留专项）| ⚠ 遗留 | 专项 |

## 四、当前步骤（进行中）

**「容器持有类对象」所有权语义设计**已提交（2026-08-25），含三条语义：
1. 追加/插入 → 元素独立化（拷贝构造深拷贝，this=内联元素地址）；
2. 弹出/清空/析构 → 元素资源销毁；
3. 元素访问 → 非拥有式视图（链式调用 或 绑定不登记 RAII 析构）。

**状态：等待用户批阅**。批准后执行：核验链式访问可行性 → 实现元素生命周期 →
新增作用域栈 E2E → 全量回归。

## 五、后续计划（按序）

1. **[P4 前置] 容器持有类对象所有权**（待批准）：实现 H8-⑤，验证 向量<映射<整64,符号>> 作用域栈可用。
2. **P4 重做**：语义分析.cn 符号表改 向量<映射<整64,符号>> 真嵌套作用域栈（压入/注册/查找/遮蔽/弹出），替换「单映射+深度延迟删除」权宜，提交并回归。
3. **自举重建推进**：P4 完成后继续 v2 结构化 IR 生成 / 代码生成组件，直至真正自举闭环（性能最优架构目标）。
4. **78/79 组件链内存根治**：自举组件字符串 O(n²) 处理改流式（IR 生成逐行而非整体拼接），解除 4096MB 防护瓶颈。
5. **宿主缺陷专项**：链式字段寻址等既有遗留逐项根治。
6. **文档同步**：每阶段完成后更新本文档（用户规则）。

## 六、工作区状态

- 当前工作区含未提交改动：H6/H7/H8-①~④/方案A（ir_oop/ir_stmt/ir_expr/generics/class_resolver/semantic*/stdlib 等）+ 新 E2E 108~112。
- 上一提交：436975e「H3 运行时段错误根治」。
