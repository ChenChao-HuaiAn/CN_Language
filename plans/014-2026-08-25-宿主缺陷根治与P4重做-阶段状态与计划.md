# 014 宿主缺陷根治与 P4 重做：阶段状态与计划（2026-08-25）

> 本文档按用户规则（2026-08-25）建立：缺陷状态 / 进行步骤 / 后续计划须全部写入
> plans/ 目录文档，按时间顺序从现有序号（013）之后接着排序；**每完成一个阶段任务
> 必须更新本文档**，用户据此查看总体计划防止项目走偏。

## 一、总体背景（自举重建战略）

CN 自举编译器「推倒重建」为性能最优架构（2026-08-25 用户决策）：结构化中间表示 +
arena/bump 一次性分配 + 字符串驻留(Symbol 整数ID) + 哈希符号表 + 只最后一层输出汇编文本。
对标 rustc/LLVM。核心规则：
- **宿主缺陷优先**：遇宿主（编译器/工具链）缺陷立即暂停现阶段任务，根治于 src/cn_compiler，
  禁止用牺牲性能的规避手段凑合（规则49 一步到位 / 规则60 一劳永逸）。
- **测试=规范**：测试先行，已通过测试不轻易改动，改动需用户批准。

## 二、已完善（本轮及前期）

| 编号 | 缺陷/任务 | 状态 | 验证 |
|---|---|---|---|
| H3 | 嵌套泛型参数解析 + 构造运行段错误（向量<映射<...>>）| ✅ 已根治 | 6 提交贯通 |
| H6 | 类方法引用参数（盒子& 其他）成员访问失败——ir_oop.cpp setupMethodParams 未设 byRef | ✅ 已根治 | E2E 110/111 PASS |
| H7 | 类类型栈变量无初始化器声明缺 NewObject+默认构造+RAII | ✅ 已根治（含无默认构造语义报错）| E2E 108/109 PASS |
| 方案A | 拷贝构造全链路：识别/查找/强制规则/三处接线/stdlib 深拷贝 | ✅ 已完成 | E2E 110/111/112 PASS |
| H8-① | 嵌套泛型实参映射——朴素 $ 分割截断（typeArgs 存储修复）| ✅ 已根治 | 盒子<映射>=56 / 盒子<整64>=8 |
| H8-② | sizeof 共享 AST 节点污染——IR 按实例 genericTypeParams_ 重算 | ✅ 已根治 | 同上 |
| H8-③ | ptrElemStride 类元素步长（原兜底 8）| ✅ 已根治 | 向量<简单盒> 99/3/42 |
| H8-④ | 向量类元素存储 CopyStruct + 读取返回地址（无析构类）| ✅ 已根治 | 同上 |
| H8-⑤ | **容器持有类对象所有权**：追加类元素深拷贝独立（拷贝构造 this=内联地址）+ 非拥有式绑定（跳过 RAII）+ 弹出销毁（stdlib 释放内部数组）| ✅ 已根治 | E2E 113 PASS（遮蔽/真删除/生命周期）|
| P4 | 语义分析.cn 符号表 → 向量<映射<整64,符号>> 真嵌套作用域栈（第6版）| ✅ 已完成 | v2 冒烟 [sem5] 错误数 0/1 正确 |
| 嵌套字段访问 | **结果<结构体>.值.字段 链式访问宿主缺陷**——五个叠加缺陷根治：① 结果/可选 局部变量合成结构体未提前降级（visitVarDecl ensureLoweredType）→ IR 槽 1 槽（应 2+）CopyStruct 溢出覆盖相邻变量；② visitMemberExpr 嵌套成员对象类型推导不识别 结果.值 成员映射（返回常量 0）；③ handleResultCtor 结构体值 StorePtr 存地址（改 CopyStruct 内联）+ 结构体字段读 LoadPtr（改返回字段地址）+ 错误() 存 E 而非 T（错误码当地址崩溃）+ 真实联合体偏移（整32=4/整64=8）+ 自然宽度存储；④ memberObjStructType 结果成员映射（lvalueAddress 嵌套偏移）；⑤ ? 运算符 propagateType=函数返回类型 + 读/写偏移分离 + exprSrcType 补 CallExpr + 宽错误码传播 | ✅ 已根治 | E2E 114 PASS（嵌套 5/101、临时 5、错误 7）+ 60 错误传播全 PASS |
| Feature 2 | **编译器级容器<T> 元素自动析构完整版**——emitClassMethod 对 容器$T 注入元素析构（injectContainerElemDestroy，泛化 向量/链表/栈/队列）：~类名/清空 注入全量析构循环（idx Alloca 槽 0..元素数量，Call T$析构 this=数组基址+idx*步长）；向量 删除(位置)、链表 删除头部/删除尾部 注入单元素析构（守卫 越界/索引<0 跳过）；栈/队列 弹出/出队（所有权转移）不析构；仅当 T 为有析构类时注入（无析构类保持原语义）。效果：作用域栈 弹出 仅需 删除(末尾) 即自动销毁 映射 元素（语义分析.cn 表弹出 已移除显式 释放内部数组()）| ✅ 已完成 | E2E 115 PASS（向量 删除+1/清空+2）+ E2E 116 PASS（链表 删头/删尾/清空 各+1、栈/队列 弹出不析构、队列清空+2）+ 113 PASS |
| move 语义 | **向量 删除 中间位置移位槽泄漏根治**——stdlib 向量 新增 析构元素(索引)（编译器注入辅助：仅 T 为有析构类时展开为 Call T$析构 this=数据+索引*步长；无析构 no-op）+ 自身.析构元素 显式调用；删除 改 C++ erase move 语义（析构-拷贝模拟）：移位循环前 自身.析构元素(移动)（释放目标槽旧资源）+ 循环后 自身.析构元素(元素数量-1)（析构末尾源对象）；injectContainerElemDestroy 移除 删除 方法体前注入（改 stdlib 显式）、新增 析构元素 匹配（参数 索引，守卫 索引<0 或 >=元素数量；链表 删头/删尾 守卫 索引<0——链表是链式，槽索引可 >= 元素数量）| ✅ 已根治 | E2E 117 PASS（删除(1) 析构3/再删除+2/删头+2/清空+1：每个移出元素恰好析构一次，无泄漏无重复）+ 115/116/113 PASS |
| 链式访问 | **链式方法调用与类值链式访问宿主缺陷根治**——两个叠加缺陷：① 类值链式 结果<类>.值.字段（查.值.编号 生成 调用 0）——exprSrcType 的 MemberExpr 分支不识别 结果/可选 成员映射（值 非直接字段）-> 类字段访问未识别 -> findStruct(类)=null 防御 0；② 链式方法调用 元素(i).设置()（表.元素(0).设置 降级为通用间接调用 this=0 崩溃 0xC0000005）——exprSrcType 的 CallExpr 分支只处理 IdentifierExpr 被调函数，不处理 MemberExpr 被调方法。修复：exprSrcType 补 结果/可选 成员映射（.值/.错误/.正常/.有值）+ CallExpr 分支补 对象.方法() 返回类型解析（lookupClassMember）| ✅ 已根治 | E2E 118 PASS（类值链式 5、元素(0).设置 777、读取(0).值.编号 11/22）+ 60/114 回归 PASS |
| 回归基线 | 全量 E2E 118/120 + 单测 1196/1196（仅 78/79 已知遗留）| ✅ 保持 | 每次改动后复跑 |
| P3-8 | **顶层静态容器变量全链路宿主缺陷根治**——五处叠加：① parser 顶层静态分支 parseTypeName→parseTypeNameEx（泛型/指针/数组）；② semantic 无初始值静态被 continue 跳过登记 → 改仅非静态跳过；③ semantic 静态类型 canonicalType 不归一泛型 → 改 resolveGenericTypeName（向量$整64）；④ ir_oop exprSrcType 补全局静态类型（否则 全局表.大小() 走通用间接调用 → callee MemberExpr 空 extra AddrOf → [rbp0] A2006）；⑤ ir_oop_call 全局静态对象 this 须为 .data 符号地址（非 LoadPtr 读值）+ codegen .data 按 typeSizeOf 分配（向量=24 字节）| ✅ 已根治 | probe：大小=0、追加(42)+追加(7) 后 大小=2、元素(0)=42 元素(1)=7 → exit=249；E2E 30/96/111/113/115/117/104/01/05/07/09 PASS；单测 1196/1196；v2p exit=0 |
| P6e | **v2 字段偏移表按结构体分桶根治**（用户批评的「字段名全局唯一」串偏移隐患）——键改 结构体名ID*键位移+字段名ID（单映射无嵌套）；新增 变量结构体表（变量名→结构体名）成员访问时推导对象结构体。宿主顺带根治：① resultTypeArgs 嵌套泛型逗号截断（平衡扫描）+ visitVarDecl 结果/可选 内部实参递归归一；② 顶层静态类对象无构造初始化（main 注入无参构造/NewObject+构造）| ✅ 已根治 | 分桶验证：点{x,y}+矩形{x,y,w,h} 偏移表=6、位置.x=0/位置.y=8 正确；模块静态映射 登记(5,100)+查询=100；单测 1196/1196；E2E 30/96/111/113/115/01/09 PASS；v2p exit=0 |
| P6f | **v2 自举闭环第一步：外部文件读取 + 类型转换**——主() 读 target/v2src.cn（模式1读+读行拼接）；组件链补 类型(expr)：语法 调用节点子起点=被调索引、语义 调用分支+类型名合法、IR IR_类型转换=54、代码生成 mov eax+movsxd | ✅ 已完成 | v2p 读外部文件 阶乘(5) 语义错误数=0、asm 汇编通过；宿主同源基准 exit=120；单测 1196/1196；E2E 30/96/111/01/09 PASS |
| P6h | **v2 自举闭环第二步：链接环境对齐 + 多源文件支持**——① 链接环境：代码生成 符号修饰 补 主→cn_main（extern "C"，对齐宿主 x64_codegen symbolName；作 asm 入口，运行时 entry 经 __cn_cache_argv 调用）；② 多文件：语法层 扫描导入/模块登记/取走导入模块们（规范 08 导入全形式：路径/::{项}/::*，路径首段=模块名）+ 变量结构体表清空 外移（跨文件字段偏移保留）；语义层 语义分析多文件（全部文件声明收集→逐文件检查，跨文件调用符号可见）；IR 层 IR生成多文件（函数们合并）；主.cn 驱动器：入口文件=命令行参数1（系统::参数个数/参数，默认 target/v2src.cn 回归）+ 递归加载导入模块（依赖先解析、已加载表防循环、标准库模块跳过）+ 共享节点池/字段偏移表 | ✅ 已完成（⚠ 当轮仅 Linux check，CI 未真实跑通——缺陷由 P7a 轮根治，见下） | E2E 119（宿主 build v2 组件→v2p→编译 主.cn+计算.cn 多文件→ml64/link 对齐宿主链接命令→运行 加倍(7) 退出码=14）+ 单测 1196/1196 保持 + 宿主 Linux check 全部 v2 组件 PASS（cn_main PROC 对齐） |
| **宿主四缺陷**（P7a 轮，2026-09-01） | **E2E 119 从未真正通过的四叠加宿主缺陷根治**——① **无初始化器顶层静态容器双模型不一致**（有初始化=NewObject 指针入 .data 槽 / 无初始化=对象内联 .data+构造打符号地址；读取路径统一 LoadPtr 只对指针模型正确→无初始化静态被读出首 8 字节字段当对象指针——v2 取走导入模块们 原崩溃点）：**统一指针槽模型**（ir_decl 主入口 NewObject+构造+StorePtr 入槽 / x64 .data 类静态 8 字节（结构体静态保持 typeSizeOf）/ ir_oop_call 删接收者特例走通用 genExpr / genVarDecl 拷贝构造支持静态源 srcAddr=静态槽地址 / 类静态赋值右值标识符深拷贝防悬挂）；② **分支未执行类局部被无条件 RAII 析构**（genClassDestructorCalls 扫全部 Alloca 不分路径→未执行分支栈垃圾上 ~向量 野指针，5 行复现 100% 崩；新鲜零页栈侥幸为空属假阴性）：genClassDestructorCalls **函数入口块统一零初始化类局部槽** + DeleteObject **空安全跳过**（x64 test/je + arm64 cbz 双后端）；③ **块顶中途 返回 被无视**（后续语句进同一线性块、末尾返回覆盖返回值，返回 5 实得 7）：genBlock 块终止（返回/中断/继续）后跳过剩余语句；④ **stdlib 文件.cn/IO.cn 漏 导入 核心::错误码**（单独 导入 文件::* 编译失败，此前靠程序先导入 容器 侥幸掩盖）：补导入 | ✅ 已根治 | 最小复现 c2/c3/c10b/c18/c11/c12/c13~c16 全绿（target/dbg/）；**E2E 119 本地真实首次通过**（六步闭环 退出码 14）；新增 E2E 121（①②③三合一）/122（④）PASS；118/01 回归 PASS |
| P7a | **v2 自举闭环第三步之①：顶层常量声明全链路**——词法 关键字「常量」（第 25 个）；语法 节点_常量声明=31 + 解析常量声明（顶层 `常量 名 = 表达式`，负数自然成 [字面量,负号] 区间）+ 局部常量分支（按 整64 局部变量，防新关键字停滞解析）；语义 符号_常量=5 声明收集注册（多文件两趟跨模块可见）；IR 模块级 静态 常量表（名ID→值文本ID）入口清空+收集，折叠**零数值解析**（正值复用字面量 Symbol ID、负值 驻留("-"+文本)），生成表达式 标识符在参数/局部槽未命中后、函数名兜底前内联 IR_常量整数（局部遮蔽正确）；代码生成/主.cn 零修改 | ✅ 已完成 | E2E 120_v2_顶层常量 六步闭环 PASS（跨模块常量/本文件常量/同模块常量/负常量/优先级 → 退出码 62）；v2 组件 84 处顶层常量全部为整数字面量（已核实），自举硬缺口消除 |
| P7b | **v2 自举闭环第三步之②：容器类型实例化与容器方法调用全链路（标量+字符串元素）**——v2 编译器（CN 语言写的）自身实现容器支持：语法层 泛型类型解析（$ 合成名：向量$整64/映射$整64$整64/结果$空类型$整32，嵌套扁平 $ 连接对齐宿主）+ 声明前瞻（泛型闭合位置 约束扫描防 a<b 误判）+ 构造调用合成（名<实参>()）+ 成员方法标记；IR 层 新操作码 IR_分配对象=60（__cn_object_new）/IR_结构体拷贝=61（rep movsb）/IR_删除对象=62（空安全析构）/IR_调用参数化=63（this 参数位0+retbuf 隐藏返回指针+rcx..r9 四参数位）+ 容器预扫（入口零初始化）+ RAII 返回前析构 + 结果变量 2 槽内联 + .正常/.值 偏移访问；代码生成 宿主同款 nameMangle（# 参数串+类型编码 J/H/PAX/_T/A&/@Z）+ EXTERN 自动收集 + 字符串常量 .code 段内数据标签；词法 字符串/字符字面量剥引号。容器方法实现链接 v2p.obj（宿主编译 v2 组件时 stdlib 源码级并入——rustc 预编译 std 模式）。新增组件 IR容器.cn（方法签名表/对象大小表/符号合成/帧辅助），7 组件变 8 组件 | ✅ 已完成 | E2E 123_v2_容器 六步闭环 PASS（退出码 21333：向量 构造/追加/元素/大小 + 映射 设置/获取/.正常/.值/清空 + 结果<T,整32> 接收 + 向量<字符串> + RAII 双容器）；/FORCE:MULTIPLE + map 双向自检（cn_main 绑 v2asm.obj、向量方法绑 v2p.obj）；119/120 回归 PASS |
| 链式字段取值（已根治） | **容器方法返回结构体的链式字段访问**（`向量.元素(0).字段`）读错值——lvalueAddress（ir.cpp）与 visitMemberExpr（ir_expr.cpp）不认 CallExpr 对象：类型推导空 -> decl==null 防御 return base 丢字段偏移 -> LoadPtr 读结构体首 8 字节拼合值。**✅ 已根治（2026-09-01，用户令缺陷零容忍后方案 A）**：两处补 CallExpr 分支经 exprSrcType 解析返回类型；base=structReturn 隐藏返回缓冲基址（值即结构体地址） | ✅ 已根治 | E2E 124 PASS（链式首/中字段、算术参与、返回结构体函数 f().字段 同路径、设置读回、大小）；全量 126 用例 124 通过（仅 78/79 已知遗留）+ 单测 1196/1196 |
| **v2 三缺陷根治**（2026-09-02，用户裁决「先根治 v2 已立案缺陷」） | ① **中途回退穿透**：函数IR 新增 函数尾块，IR_返回 后跳转尾块（epilogue 唯一）+ 块终止感知（生成语句/块/如果/当 返回布尔，返回/中断/继续 后跳过不可达语句、终止分支不发汇合跳转/块出口析构）；② **&&/\|\| 短路**（规范 930）：语法层短路层标注操作数区间（附加=右起点），IR 层预扫描+运算符节点延迟求值（&&：a 真→求值 b；\|\|：a 假→求值 b；临时槽汇合+双逻辑非规范化 0/1；生成表达式 签名扩 块计数/槽计数 指针，mangled≈213<247）；③ **词法转义**：字符串字面量实时反转义（\n\t\r\0 及后继字符式，对齐宿主 decodeEscapes；快路径无转义单子串；\u{} 暂缓③前对齐） | ✅ 已根治 | E2E 125_v2_控制流与短路与转义（断言 201~217/全过 0：中途回退×6、短路×7 含除零右操作数不崩、嵌套优先级、转义）linux-arm64 运行时通过；119/120/123 回归 asm 级+运行时全过 |
| **阶段A：v2 Linux ARM64 GAS 后端 + 双平台闭环**（2026-09-02，用户裁决①） | 新组件 代码生成ARM64.cn（IR→AArch64 GAS，对齐宿主 arm64：stp x29,x30 帧/x0~x7+栈参数/movz·movk/cset/cbnz/sdiv+msub/后索引拷贝循环/cbz 空安全/adrp+add/-no-pie/GAS 符号修饰 _+hex+参数段）；主.cn 可选参数 2 目标平台（默认 win-x64 不变）；run_e2e.py 执行v2闭环Linux（as/g++/运行时.o 现编/muldefs+顺序绑定 cn_main/nm 双向自检/POSIX 退出码%256） | ✅ 已完成 | E2E 119(14)/120(62)/123(21333→85)/125(0) linux-arm64 运行器内真实通过；本机全量 E2E 127=122 过/4 平台跳过/仅 78 已知遗留；win-x64 门禁待 Windows 会话 |
| **后端架构方案1：共用决策层**（2026-09-02，用户裁决「抽共用决策层」） | 新组件 代码生成共用.cn（249 行）：mangle 工具/帧扫描/帧大小/槽偏移模型/调用编址（平台参数化 4+32+48 vs 8+0+16）/标签唯一化（bb<函数序号>_<块ID> 双后端统一，消除 MASM PROC 作用域依赖）/字符串常量收集；代码生成.cn 与 代码生成ARM64.cn 瘦身为纯平台发射器（472/542 行）。双目标：性能（决策单点优化一处生效双平台）、安全（ABI 单点杜绝漂移错配）；第三平台只需纯发射器 | ✅ 已完成 | 重构后 4 v2 用例全过 + 全量 E2E 127 零回归（122 过/4 跳/仅 78 遗留）；MASM 路径产出核验（统一标签生效、结构无损） |

| **③ 第一波：v2 内置函数全链（2026-09-03，单位机 Linux ARM64）** | **v2p 编译 v2 自身组件（词法分析.cn 冒烟）踩出**：语义层无内置函数白名单（66 诊断）+ 无字符串拼接 + 展开 + 无 类型大小 折叠。落地四层：① **IR容器** 新增 内置函数符号（中文→`__cn_*` ASCII 映射表，ABI 权威=宿主 asm 裸符号直调 `bl __cn_intern` 实证；`__cn_` 前缀双后端符号修饰直通）/内置函数返回类型ID/类型名宽度（原始类型表+结构体布局表）；② **语义** 标识符/调用两分支白名单；③ **IR生成** 调用分支内置直调（IR_调用参数化 附加=ASCII 符号）+ **类型大小(类型) 编译期折叠**（IR_常量整数——操作数1 须为**值文本驻留ID** 非 数值，实测踩坑根治；弹 2 压栈残留根除栈底负符号）+ **字符串拼接 +**（**寄存器→类型码登记表**：字面量/标识符加载（**标量局部+标量参数入函数级权威表**——②c 只登容器/结果/结构体，标量漏登实测补齐）/拼接产物/内置返回/比较与逻辑运算产物 T_布尔 全登记；双侧字符串性判定，**数值/布尔侧隐式转换** `__cn_str_from_int/float/bool` 后 `__cn_str_concat` 左结合——宿主 Task 2.5/2.9 全对齐）+ **打印/打印行** 复合展开（逐参类型分派 `__cn_print_str/float/bool/int`+尾随 `__cn_print_newline`）；④ 代码生成零改动。**用户审计轮（缺陷零容忍）揪出三缺陷当场根治**：打印字符串变量错分派 print_int（打指针值）、字符串+数值静默错码（数值位模式当指针传 concat）、栈底负符号残留推后——详见 lessons | ✅ 已完成 | E2E **128_v2_内置函数与字符串拼接** 六步闭环 PASS（退出码 0=**13 类断言**：驻留往返/同ID/字符串长度 UTF-8/子串/整数转字符串/字符串比较/类型大小×4/拼接×3/**字符串变量打印+混合参数/字符串+数值双向转换/布尔拼接 from_bool**）；运行时人眼审计：`打印(还原)`→「词」、`打印(还原," ",词ID)`→「词 1」；词法分析.cn 冒烟：语义 0 错误，IR 崩点定位=**字符串下标 `源码[位置]`（v2 未实现语法，下一缺口→129 用例先行）** |
| **单位机三项收口（2026-09-03）** | ① linux-arm64 全量 E2E 复确认：129 用例 124 过/1 败（78）/4 平台跳过——**127 新增 ARM64 首次运行时全绿**（A 嵌套容器/B 容器字段指针槽/C 顶层静态 GAS `.quad` `.data`/D byRef——家机对称实现未验证部分全部实证）+119~126 零回归；② **118 Linux 侧复查**：全量中绿——上轮疑点消除（第四轮宿主改动未引入回归）；③ **78 Linux -11 分诊**：dmesg 实锤 OOM killer（total-vm 26.6GB/anon-rss 11.4GB 耗尽 15GB 物理内存被 SIGKILL；E2E 内 -11=分配失败解引用形态）——**与 win 侧 201 内存超限同因**（v1 旧架构字符串行 IR 已知遗留），「级联双重释放」假设排除（无 glibc double-free 报错；第三轮已红证明非第四轮回归） | ✅ 已收口 | 见本行验证列 + 提交信息 |
| **③ 第二波：字符串下标 + 复合赋值五形态 + 方案A 登记点补全（2026-09-03，单位机，两项用户裁决方案A）** | **三件套同轮落地（E2E 129 载体）**：① **字符串下标**（宿主 A-1 语义对齐：UTF-8 字节视图步进 1 + LoadPtr i8 有符号扩展（双后端 movsx/ldrsb）+ 越界调用方约束 + 结果类型 字符）——语法 `[` 分支补记子区间（原全 0 字段丢失）/语义对象白名单（字符串标识符/字符串字段，其余显式拒绝+下标左值赋值拒绝）/IR 新宽度码 **T_字节**（ldrsb/movsx，独立于 B5 T_字符 零扩展模型）；② **复合赋值五形态 += -= \*= /= %=**（原词法仅 += 且解析零分支→**静默丢句**：分词 被截断、上轮冒烟实为**假绿**——词法补四 token + 解析兜底统一改写 `L op= R ≡ L = L op R` **回退重析左值**，赋值/IR 零改动）；③ **方案A 登记点补全**（缺陷①②同族复发根治：成员加载值/用户函数返回（池预扫映射）/容器方法返回 三登记点+拼接左侧兜底；**坑**：合成名首实参 返回首$后全部须段首剥离）；④ **连带根治两枚**：符号表 类型ID 双约定统一（局部/参数原存节点索引）；**检查块 双重处理嵌套语句**（父层原始遍历泄漏注册+遮蔽检查自注册误触发 126/127——预收集嵌套区间跳过，真实遮蔽仍正确触发）。**65 ARM64 跳过摘除**（宿主 ldrsb 符号扩展后跳过依据失效，手动实测一致）；**审计后续（用户裁决「现在补」）**：复合赋值非数值操作数显式拒绝（宿主判定对齐——字符串/布尔/字符 += 拒、整系/浮点系过；解析层合成节点 子终点=1 标记+语义层数值白名单（左值类型取区间**末**节点）+v2p 内置负测自检+129 期望固化） | ✅ 已完成 | E2E **129_v2_字符串下标与复合赋值与登记补全** 六步闭环 PASS（退出码 0=**28 类断言**：下标 7 形态（ASCII/中文符号扩展-28/循环求和-376/负值补偿228/二元下标/参数下标/字段下标-23）+复合赋值 7（五形态+嵌套右值+成员左值）+登记簇 8（成员打印/用户调用打印/四拼接/容器方法/结果.值——供给.cn 供给 映射$整64$字符串））；宿主 sanity 先行（退出码 0 同源双编译器一致）；v2 产物运行时人眼审计 题/名/词/甲；**词法分析.cn 冒烟真绿**（节点 488→556、asm 2046→2736 行——分词 首次完整编译）；全量回归见 HANDOFF |
| **③ 第三波：指针下标读写 + 宿主内存模型统一 C 布局（2026-09-03，家机 win-x64）** | **E2E 130 载体 + 验证期按缺陷零容忍纪律连根治四簇缺陷（用户纪律令：宿主缺陷优先/全部根治后再推进）**：① **宿主内存模型三套咬合统一 C 布局**（数组局部 8 槽×指针步进兜底 8「双错互洽」vs 结构体/.data C 紧凑 vs 容器 max(大小,8) 槽分配——同型指针不可能两全：结构体字段窄指针 p[1] 跨字段读脏值 130 首踩）：ptrElemStride 统一 typeSizeOf(pointee)/registerVarSlots 数组槽数 ceil(总大小/8)/数组初始化+读+lvalue 三处步进 typeSizeOf/补零按元素宽度（i128/浮点/指针保持 i64）/stdlib 容器 9 处分配紧凑化/**ir_expr A-3 隐式类字段读分支「标量 8 槽」步进同批统一**（读写两侧步进不一致=30 腐坏，asm diff 7 处 shl 3→2 锁凶）；② **宿主窄宽 StorePtr 常量**（`整8(7)` 显式强转折叠直达→`movsx rcx, byte ptr 7` A2070——常量特判 mov 直载）；③ **v2 MASM movsxd 缺 dword ptr**（129 win 首跑 A2070——单位机仅跑 GAS 侧无此歧义，双后端对称验证价值实证）；④ **v2 表达式栈卫生家族**（取地址「清空全栈」弹掉调用被调符号 f(&x) 崩 + 负号/逻辑非/解引用操作数线性+嵌套双份残留 q[-1] 弹2取残留当地址 0xC0000005——前缀一元操作数入预扫跳过+全部分支改深度相对弹出）；⑤ 池索引 0 守卫复发（IR生成 参数循环 >0 漏首类型节点——>=0+节点类型判定）；⑥ **v2 MASM db 单语句操作数上限 48**（129 win 二段错误：53 字节中文字面量 A2042；分段续行+剥离悬挂逗号——悬挂逗号同 A2042，隔离二分实测）。**v2 指针下标四层**：语义 指针变量表（类型节点附加=指针层数 1..255 登记，参数/局部）+所指类型白名单（标量/字符串；浮点/结构体/容器显式拒绝=IR 加载宽度覆盖面）+下标左值放行（指针对象）+复合赋值左值类型取所指；IR 指针变量类型们（生成函数清空）+下标读（步进=类型名宽度/LoadPtr 元素宽度码/登记类型码）+左值地址下标分支+赋值写宽度=元素宽度码；v2p 尾部两负测自检（整64 下标拒/字符串下标写拒）+130 期望固化 | ✅ 已完成 | E2E **130_v2_指针下标读写** 六步闭环 PASS（退出码 0=**17 类断言**：出参 &传参+读+循环推进/整32 步进 4 全链（写字段+读回+二元下标+负下标 q[-1]/q[-2]+复合赋值+写透函数）/整64 步进 8/强转操作数）；宿主 sanity 先行（退出码 0）；v2 产物运行时人眼审计 4/77/全部通过（与宿主一致）；容器/数组回归 30/113/111/05/09 绿；**诚实边界**：v2 整8/整16 强转白名单未覆盖（语义显式拒绝非静默，宿主侧窄宽常量已根治）｜探针级观察（未立案）：`a * a = 7` 语句歧义按声明路径解析但报错（C 式 lexer hack 歧义，换行≡空格规范行为）；F 级联静默形态 |
| **观察项裁决落地：非左值赋值立案根治（2026-09-03，单位机宿主侧 + 家机 v2 侧，✅ 双侧收口）** | 用户裁决「②立案+③顺带+①不动」（Rust E0070+分号提示实测对照后）：**②宿主侧（单位机）** visitAssignmentExpr 左值白名单重构（标识符[常量拒绝]/下标/成员/解引用 Deref/引用返回调用之外硬错误——原 else 分支只查类型不查左值性：a+1=7/a+1+=2/5=a/常量=2/c++/(a+1)++/粘连 b=((a*a)=7) 全静默通过）+ scopeConsts_ 作用域常量跟踪 + visitUnaryExpr ++/-- 左值检查；**③** 跨行赋值附「可能想写分号」提示；**①** 歧义+换行≡空格维持规范不动。**②v2 侧（家机 2026-09-03 第四轮）四件**：表查找种类（作用域栈内→外，-1 未找到）+ 标识符左值/复合标识符形态常量赋值拒（「不能给常量赋值」）+ 节点_赋值 文本==0 左值末白名单 {标识符（复合合成）/成员/解引用/下标（既有指针白名单）}其余硬拒「赋值目标必须是可写左值」+ 节点_自增 常量拒；v2 主.cn 尾部 源码6/源码7 两负测自检（132 expected 固化两行）。E2E 131 负测 + 132 v2 闭环 双绿 | ✅ 已完成 | 131 负测八形态全拒（期望编译失败）；**132 六步闭环 PASS（v2p 自检「非左值赋值负测通过」「常量赋值负测通过」+全形态不误伤+退出码 0）**；宿主单测 test_lvalue_check.cpp **11 项新增**（九负形态精确消息+跨行提示串+合法全形态零误伤，实测锚定）；单测 1207/1207 |
| **第四轮家机收口：v2 裸节点双重处理根治 + 缺陷 A/B 双侧根治（2026-09-03，win-x64，用户令「任何缺陷及时完善/参考 Rust/隐患拦在编译期」）** | **132 转绿途中踩出 v2 结构性缺陷并当场根治三件**：① **检查块 裸节点双重处理**（块区间线性含各语句 RHS 裸节点，原逐节点派发使裸一元节点在声明根注册名前先查操作数——自引用 `&(v*p)` 误报未声明 + 前向引用 `&q` **同错双诊断**（p16 探针实证报两次）；lessons 11.0 权重「区间重叠双重处理」同族）——检查块 主循环改**仅派发语句根节点类型**（块/如果/当/变量声明/赋值/自增/返回/表达式语句/中断/继续），裸节点由所属语句根区间统一检查（每节点恰在其作用域访问一次，rustc 对齐）+ 检查语句 一元分支死代码删除；② **解析器裸 `*表达式` 语句无根**（原裸节点 IR 语句派发零分支=静默死代码、语义靠裸派发兜底=①根源）——补 节点_表达式语句 根（对齐 936 兜底；裸 `*未声明x` 检查覆盖保持实证）+ **解析块 静默中断可见化**（语句零消费 stderr 诊断「块内后续语句被丢弃」——B6/+= 语句级静默中断家族，`*p += 1` 不可解析形态由静默截断变可见）；③ **132 源码换行粘连修正**（`&p.x` ⋈ `*指针` 按规范粘成 `&(p.x*指针)`+空左值 `=100`——v2 新左值检查正确拒绝空左值赋值（左值末=变量声明节点∉白名单），源码补分号=语法错误修正）。**缺陷 A（成员/下标/解引用自增自减写回缺失）**：ir_expr.cpp Increment/Decrement 写回仅 IdentifierExpr（类字段/顶层静态另有路径）——p.y--/数组[1]++/(*p)++ 只读不写=存储静默丢弃（单位机 ARM64 发现、win 同现=**双侧公共 IR 层缺陷**）；修复=lvalueAddress+Add/Sub+StorePtr 写回（宽度按操作数类型，②b B5 字段宽度纪律），全形态探针 2/11/6/9/8 全对。**缺陷 B（数组局部无初始化器栈垃圾）**：ir_stmt.cpp 无初始化器补零仅结构体分支（2026-09-02 缺陷2 根治漏数组同族）——数组[2]+=数组[1] 读未初始化内存（每次运行不同）；修复=registerVarSlots 登记槽数逐槽 Store 0（Rust 级确定性对齐），探针 5/5 对 | ✅ 已完成 | 探针实证：p1/p5 粘连形态恰一条正确诊断（非左值拒）、p13/p14 干净形态 0 错、p16 前向引用单诊断（原双报）、p17 裸解引用未声明仍拒、p18 静默中断可见；probeA `p.y--`→2、probeB `数组[2]+=数组[1]`→5、probeC 全形态 2/11/6/9/8；**A/B 修复均在公共 IR 层（双后端同享），ARM64 侧由单位机全量复验**；单测 1207/1207；全量 E2E 见 HANDOFF |
| **③ 第五波：限定名内置 IO::/文件:: 全链 + 真/假/无 字面量 + 二元链操作数位一元前缀（2026-09-03，家机 win-x64）** | **E2E 133 载体**（plans/014 序列下一缺口——v2 组件 IR签名.cn/主.cn 的 I/O 命脉）：① **限定名内置四层**——语法层 `::` 独立分支（原与 ./-> 同走成员分支：IO 崩「未声明标识符: IO」+ 成员方法标记误路由「容器方法未收录」错误 6）——仅支持「标识符::标识符(」调用形态：池尾对象标识符**原地改写**为合成限定名（局部副本+池.设置 回写——元素(i).字段=x 写非宿主支持形态，124/126 实证读=地址视图/写=设置）+ 随后 `(` 分支自然形成调用（被调=限定标识符，与容器构造合成名同构，零新节点类型）；语义层限定名经 内置函数返回类型ID 表自动放行（零改动）；IR 层 内置函数符号/返回类型 双表扩 11 限定名（IO::4+文件::7，宿主 ir_call.cpp:217~235 与 semantic.cpp:1226~1257 对齐；空类型* 按指针单槽记 整64；空类型返回跳过登记=void 语义对齐）；代码生成零改动（`__cn_` 前缀直通）；② **真/假/无 字面量**（词法关键字表已有、解析层零分支——谓词含 真/假 的源码必截断；133 EOF 判空需 无，宿主「指针只能与指针或空指针比较」实测锚定）——复用 整数字面量 节点（真=1/假=0/无=0），语义/IR 字面量路径零改动；③ **【连带揪出·当场根治】二元链操作数位一元前缀缺失**（133 首跑 `[parse]` 截断于 `追加行 == 无 \|\| !字符串比较(...)`——探针矩阵 p1~p4 实证：比较\|\|比较过/!调用单独过/组合必截断；词法分析.cn 冒烟 3 处截断同族）——**解析层_子 操作数位参数化**：二元循环右操作数位传 真（运算符已消费=操作数起点，前缀判定无歧义）、链入口传 假（v2 二元链=「入口在运算符上、左操作数已解析」的空左操作数设计——首版在子(5)入口无条件判定把二元 -/* 误当前缀，减法/乘法全灭 123/129/130 回归，消融实验+节点数同值+探针锁定后修正；紧绑定操作数=原子+后缀链递归嵌套前缀同覆），lessons 10.5 权重成文；④ v2 主.cn 源码8 限定名负测自检（未注册限定名 `不存在::打印(1)` 须拒——白名单命中制防静默放行）+ IO 正向锚点两行 | ✅ 已完成 | 宿主 sanity 先行（133 源 cn build+运行 退出码 0，17/15 字节/EOF/追加断言全对）；**E2E 133 六步闭环 PASS**（v2p 编译→ml64→link 运行时→运行 退出码 0；[sem5] 正确程序 错误数=0+限定名负测通过 错误数=1+IO 双锚点，expected 四行全命中）；v2 产物人眼审计：IO 双行+「限定名IO文件: 全部通过」齐全（二元链前缀修复前打印语句被截断丢弃=asm print 符号 0 实证，修复后恢复）；**词法分析.cn 冒烟大幅推进**：3 处 [parse] 截断→1 处（最后截断点=**三元运算符 `? :`**（行 236，规范 01b 有、v2 未实现——下一缺口）），语义 0 错误；IR 报「调用被调对象非函数名/方法」疑为该截断级联（待三元支持后复验）；全量门禁见 HANDOFF |
| **v2.1 语法规范变更：成员访问统一 "." 废除 "->" + 两处静默常量0 既有缺陷根治（2026-09-03，家机 win-x64，用户裁决）** | **用户令**「依照 Rust 与旧版 CN 规范，访问形式统一 . 去除 ->」，三项裁决：一级自动解引用（Go 先例，多级报错）/残留 -> 硬错误+迁移提示/本轮直接实施。①**规范**：plans/001 四处（运算符表 3→2/重载表删 ->/优先级表/Task 2.7 标注）+ cn-language-spec 05/01b/06（.zcode 与 .agents 双副本同步——02/04 历史分叉待收口）；②**宿主**：MemberExpr.isArrow 改名 **isDerefAccess 并改为语义层按对象类型置位**（解析层恒 false；structType 统一剥指针一级+自身专门分支并入+多级指针报错「不自动解引用」；refReturnLvalueBase 读标记）；解析层 -> 硬错误（「成员访问已废除」+迁移示例）+按 . 折叠防级联；Operator::Arrow 枚举/重载映射全删；IR 逻辑零改动仅改名——**例外 ir_oop_call 虚调用改纯类型驱动**（方法调用路径不经 visitMemberExpr，标记不可靠，VirtualCallDispatch 首败实证）；③**【连带揪出·当场根治】两处静默常量0 既有缺陷**（134 写回全形态锚定揪出，objSrcType 分支表形态落空→decl null→降级常量0 家族第三例）：嵌套链第一层指针（`wq.内.x`——嵌套分支 findStruct("外层*") 不剥指针，旧 -> 同形态同缺陷）+ 显式解引用对象（`(*q).x`——分支表不认 UnaryExpr(Deref)）；④**v2 侧零语义对齐**（原 ./-> 本就同路径同语义）：语法分析.cn 成员分支只认 .（-> 硬错误 运行时错误(302) 行号码惯例，无法进程内自检——宿主 E2E 135+v2 全源码零 -> 双覆盖）+ IR容器.cn 15 处 `出/签->字段` 改 `.`；⑤**测试**：新增 134（读/写/自增/自减/复合赋值/嵌套链/解引用等价/值对象不变式）+135（-> 拒绝负测）+136（多级指针负测）；改写 07/09/28 非法化语法 16 行（含 `(名单+(n-1))->分数` 括号前缀漏网形态——宽口径复扫根治）；单测 6 文件断言改写 | ✅ 已完成 | 构建 0 警告 + 单测 **1207/1207** + E2E **138 用例 136 过/2 败（78/79 已知遗留内存超限形态一致，78 单跑 4656MB 复证）零回归**；134 退出码锚定 11~20 全绿、135/136 负测诊断子串命中；128/133 自举链绿；lessons 两条成文（静默降级家族 9.0/迁移工程方法 6.0） |
| **③ 第六波：三元条件运算符 `? :` 全链 + 表达式入口前缀紧绑定 + 静态自增丢句根治（2026-09-03，家机 win-x64）** | **E2E 137 载体**（HANDOFF 下一步①——词法分析.cn 冒烟最后截断点 行 236，规范 01b 5.8 有/v2 未实现）：①**语法**——新节点 `节点_三元运算=33`（子起点=条件段起点/文本=真值段起点/附加=假值段起点/子终点=段末+1 三段连续区间）；解析在 解析表达式 尾段包装（二元链后）：真值/假值=完整表达式递归（假值递归=右结合 `a?b:c?d:e=a?b:(c?d:e)`）；`:` 缺失不消费（防御惯例）；v2 无后缀传播 `?` 一律三元；②**语义**——检查表达式 三元分支：两分支类型经段末端节点递归推导 + **类型可合并判定**（同ID/整数族宽化：整8/16/32/64、正*、布尔、字符互通——真/假=1/0、字符零扩展、槽 8 字节无损；浮点族互通；跨类拒绝=整vs浮位模式/字符串vs整数指针槽——Rust if 表达式纪律）；-1 跳过（级联防）；条件段类型不强制（条件跳转按非零，同 如果/当）；③**IR**——预扫3 三元三段区间整段跳过 + 主循环三元 case **复用 125 短路方案**：条件寄存器→临时槽（IR_分配 帧登记）→三块（真/假/汇合）条件跳转→选中分支求值+IR_存储→汇合 IR_加载（类型=真分支寄存器登记，未登记兜底 T_整64）→登记压栈；栈平衡=各段递归后弹到前深度再压汇合（一元模式净+1，防「弹2取残留」同族）；④**【连带根治一】表达式入口一元前缀紧绑定**（既有缺陷三元接入后可见化：原「递归 解析表达式」吞后随 `?`/低层二元——`!x ? a : b` 变 `!(x?a:b)`、`!x + 1` 变 `!(x+1)` 背离 C 一元优先级 12）——统一改 解析层_子(层5,真) 紧绑定（原子+后缀链+嵌套前缀递归），与第五波二元链操作数位前缀紧绑定收敛为全路径一致；⑤**【连带根治二】静态变量自增静默丢句**（137 踩出，节点_自增 仅认局部/参数槽，静态目标零 IR=防御分支静默降级家族第四例）——补静态槽自增：IR_静态加载+1→IR_静态存储（②c C 设施）；⑥v2 主.cn 源码9 三元负测自检（字符串 vs 整数跨类须拒，expected 固化）；⑦词法层零改动（`?` 兜底单字符既切出，注释补录） | ✅ 已完成 | 宿主 sanity 先行（137 源 cn build+运行 退出码 0）；**E2E 137 六步闭环 PASS**（20 类断言：基本/右结合三形态/优先级/惰性副作用双向/除零保护/整数族宽化/字符串分支/各语句位置+负测自检行命中）；**词法分析.cn 冒烟 [parse] 截断归零**（1595 节点完整解析/语义 0 错/IR 10 函数/asm 16345 行/**ml64 汇编通过 60863 字节 obj**——自举对拍首组件完整编译；上轮「调用被调对象非函数名」确认为三元截断级联已消失）；**语法分析.cn 冒烟启动**：[parse] 全过（4 文件 11684 节点——导入链拉入词法分析/IR签名/IR容器全解析成功），语义 54 错=**下一批缺口三簇**（①跨模块顶层常量可见性 T_*/IR_* 约25错 ②文件API标识符 打开/打开文件/关闭文件 9错 ③可写左值白名单 9错+散件 名ID/当前函数变量类型）；全量门禁见 HANDOFF |
| **灰色点①③④⑤立案根治（2026-09-04，家机 win-x64，用户重贴缺陷零容忍纪律裁决：立即全部根治不留到后面）** | 上轮 HANDOFF「灰色点披露」六点分诊：四项 v2 侧缺陷当场根治+两项呈报（②字符串==指针比较=规范语义裁决三方案待用户批；⑥三元?与错误传播?消歧=未实现特性非缺陷随错误处理特性对齐）。**① 真/假 布尔字面量**：新节点 节点_布尔字面量=34（文本=1/0）——原复用整数字面量（真=1/假=0）类型整32/IR登记 T_整64，打印(真) 输出 1（宿主输出 真，两侧行为分叉）；独立节点后语义推导 布尔/IR 登记 T_布尔，打印/拼接分派自动 __cn_print_bool/__cn_str_from_bool（规范02 布尔=基础类型1字节，对齐宿主非规范变更）；无 保持整数字面量 0（空指针语义）。**③ 解引用复合赋值 `*p op= r`**：P6c 分支原只认 =——`*p += 1` 走非赋值路径仅消费 `*p`，`+= 1` 孤儿 token 触发块循环保护中断→块内后续语句（含 返回）全部丢弃而 asm 仍生成=残缺产物（宿主分号语境支持）；补复合分支（方案A 回退重析左值副本，合成二元+赋值 子终点=1 标记，与标识符/成员/下标形态同款），语义/IR 复用既有解引用左值链路零改动。**④ 窄宽强转 整8/整16 三层**：语义白名单补两名（正8/正16 暂不补——类型码层整8||正8 同码 T_整8 无符号语义缺位，补名=静默错误代码，维持显式拒绝）；IR_类型转换 目标码特判（类型码() 将整8/16 归 T_整64，强转须窄宽码，仅强转分支特判其余消费方零变化）；win 发射 mov al/ax byte/word ptr+movsx、ARM64 sxtb/sxth（双后端对称）。**⑤ v2p 语义错误即中止**：主.cn err>0 打印诊断后返回 1 不产 IR/asm（原无条件继续=错误产物可被链接，违背生产编译器纪律；宿主 driver 语义错误即构建失败）；尾部组件自检仅在入口干净时运行，既有用例 err=0 输出流不变 | ✅ 已完成 | 探针运行时锚定四点全对（①打印(真)→真/假与宿主一致；③`*p += 1/= 10/-= 3`→6/10/7；④整8(300)→44 asm 见 mov al byte ptr+movsx；⑤退出码1且无 asm）+ 宿主 sanity 138 源退出码 0（真/前缀真）+ **E2E 138_v2_灰色点收口 六步闭环 PASS**（十断言：拼接 from_bool 锚定 T_布尔/`*p op= r` 全形态/整8·16 截断+符号保号）+ **E2E 139_v2_语义错误中止 负路径闭环 PASS**（run_e2e.py 扩展预期退出码 None 形态：v2p 退出码非0+无 asm+中止诊断行 expected 固化；linux-arm64 侧负路径待 ARM64 复验轮对齐=诚实边界提示）；②三方案已呈报待用户裁决；全量门禁见 HANDOFF |
| **灰色点②方案A落地：字符串比较运算符显式拒绝（2026-09-04，家机 win-x64，用户裁决方案A——Rust 纪律）** | `字符串/字符*` 间 `==/!=/</>/>=/<=` 硬错误+迁移提示（相等用 字符串比较、全序用 字符串字典序）——原两侧一致按指针地址放行=同内容异地址恒假（拼接产物静默陷阱）；**规范依据 plans/001 Task 2.8 已明文**「比较运算符仅整型/浮点变体，字符串不参与运算符比较」=让实现对齐既有规范非语义变更。**宿主**（semantic_expr.cpp）：字符串×字符串（含字符\* 交叉）拒绝；字符串/字符\* 与 无 判空**保留**（133 EOF 载体）；真指针间地址比较保留。**v2**（语义分析.cn 检查表达式 新增二元分支）：比较 op 且操作数区间末端递归推导均字符串→同款硬错误，返回 -1 无下游推导回归；诚实边界=拼接产物/成员字段末端推导兜底检不出（宿主已全形态覆盖，随 v2 类型推导完善收口）。**v2 组件源码连带修正 1 处**（语法分析.cn:881 `当前文本(...) != "}"` 驻留地址比较惯用法——恰为根治目标陷阱形态——改 字符串比较；全库复扫无其他形态） | ✅ 已完成 | 单测 test_string_compare_check.cpp **9/9**（5 负形态精确消息+4 合法零误伤含 行!=无 判空/真指针判空）；E2E **140**（宿主负测：期望编译失败.txt 诊断子串）+**141**（v2 负路径闭环：139 同款 runner 预期退出码 None 形态）双绿；stdlib/既有用例预检+全量门禁双证零误伤；全量门禁见 HANDOFF |

新增 E2E：108_H7 / 109_H7 / 110_H6 / 111_H6 / 112_方案A强制规则负测 / 113_H8_容器持有类对象 / 115_Feature2_向量元素自动析构 / 116_Feature2_链表栈队列元素析构 / 117_Feature2_向量删除中间位置move语义 / 118_宿主缺陷_链式方法调用与类值链式访问 / **119_v2_多文件链接闭环**（v2 自举闭环第二步：宿主编译 v2 组件→v2p→多文件编译→链接宿主运行时→运行 退出码断言）/ **120_v2_顶层常量**（P7a：顶层常量跨模块引用六步闭环，退出码 62）/ **121_宿主缺陷_静态容器与分支局部析构**（四缺陷①②③三合一）/ **122_宿主缺陷_stdlib模块独立导入**（四缺陷④）/ **123_v2_容器**（P7b：容器类型实例化与方法调用六步闭环——向量/映射/结果/字符串/RAII，退出码 21333）/ **128_v2_内置函数与字符串拼接**（③ 第一波：内置符号直调/str_concat 拼接/类型大小折叠/打印族，退出码 0）/ **129_v2_字符串下标与复合赋值与登记补全**（③ 第二波：下标 A-1 语义/复合赋值五形态/登记点三缺口，退出码 0，供给 映射$整64$字符串）/ **130_v2_指针下标读写**（③ 第三波：出参指针/步进按元素宽度/负下标/复合赋值/写透/强转操作数，退出码 0；宿主内存模型 C 布局统一+四簇缺陷根治载体）/ **133_v2_限定名内置IO与文件**（③ 第五波：IO::打印到错误/文件::六函数 裸内置直调 __cn_print_err/__cn_file_* + 真/假/无 字面量 + 二元链操作数位一元前缀，退出码 0；裸内置契约=裸指针返回/EOF=无）/ **137_v2_三元运算符**（③ 第六波：优先级1.5右结合/惰性求值副作用双向锚定/除零保护/整数族宽化/字符串分支+静态自增回归锚点，退出码 0） / **138_v2_灰色点收口**（灰色点①③④：真/假 布尔字面量 T_布尔 拼接锚定/解引用复合赋值全形态/窄宽强转截断+符号保号，退出码 0） / **139_v2_语义错误中止**（灰色点⑤负路径：v2p 语义错误退出码 1+无 asm，runner 预期退出码 None 形态） / **140_字符串比较运算符拒绝**（灰色点②方案A宿主负测：期望编译失败.txt 诊断子串） / **141_v2_字符串比较拒绝**（灰色点②v2 负路径闭环：v2p 拒字符串==且退出码 1+无 asm）。

## 三、未完善（已知缺陷/阻塞）

| 编号 | 缺陷/任务 | 状态 | 阻塞 |
|---|---|---|---|
| — | 78/79 E2E 内存超限（旧自举组件字符串行 IR，百万级分配，峰值 4.1GB>4096MB）| ⚠ 旧架构固有 | **非宿主缺陷——这正是启动 v2 自举重建的根本原因（推倒重建动机）**；随 v2 重建完成自然解决（v2 结构化 IR + arena + 符号驻留，内存降为合理水平，78/79 应通过）|
| 链式字段取值 | ✅ 已根治（2026-09-01，见二表） | — | E2E 124 |
| **签名扫描 UB（项1 立案→已根治）** | v2p 大帧函数中字符串参数跨调用被随机清空（破坏位置漂移=UB）。**体内二分定位（用户裁决）**：逐句隔离锁定 `驻留(...)` 调用后丢（常量实参也丢、字符串长度 不丢）→ **根因：宿主运行时 `__cn_intern` 的 pool 用 `std::vector<std::string>`——扩容搬移全部 string，已发放的 `c_str()` 指针悬垂**（`驻留文本` 返回指针被编译器全链长期持有；后续任何 驻留 触发扩容即读到搬家后旧内存——asm 全对因为不是写坏而是数据搬家；扫描器「注释混入方法名」同根因）。**✅ 已根治：pool 改 `std::deque<std::string>`**（push_back 不使已有元素指针失效，标准保证——对标 rustc StringPool 指针稳定性）。附带根治 2 个扫描器缺陷：关键字表无「泛型」导致整文件跳过（判定改按文本不限令牌类型）、参数序误收声明词「类型」（跳过）、返回类型 形态需归一 $ 合成名（归一结果名） | ✅ 已根治 | E2E 119/120/123/124 全绿（123 走签名表路径）；全量门禁见提交信息；签名表 57 条全部来自 stdlib 源码解析 |

## 四、当前步骤

**[2026-09-03 第九轮：③ 第五波——限定名内置 IO::/文件:: 全链 + 真/假/无 + 二元链操作数位一元前缀（E2E 133 全链绿）]**（家机 Win11 AMD64）：
- 接 HANDOFF「下一步②」：plans/014 序列下一缺口 = IO::/文件:: 限定名内置（v2 组件
  IR签名.cn/主.cn 的 I/O 命脉；IR容器.cn 内置表注释「表驱动扩表」预留接缝）。
- 侦察定界：v2 语法层 `::` 与 ./-> 同走成员分支（IO 崩未声明+方法标记误路由）；
  宿主权威 = ir_call.cpp:217~235 符号映射 + semantic.cpp:1226~1257 限定名注册；
  v2 组件实际用面 = IO::打印到错误 + 文件::六函数（裸内置契约，stdlib 包装层
  结果<> 形态属模块导入链路另案）。
- 四层落地（详见二表第五波行）：语法 `::` 合成限定名 + 真/假/无 字面量（复用
  整数字面量）+ 解析层_子 第 5 层前缀分支（133 首跑 [parse] 截断连带揪出，
  探针矩阵定位，lessons 9.5）；IR 双表扩 11 限定名；语义零改动。
- 133 六步闭环 PASS（宿主 sanity 先行）；**词法分析.cn 冒烟 3 截断→1**（最后
  截断点=三元运算符 `? :`——下一缺口立项），语义 0 错误。
- 全量门禁与提交信息见 HANDOFF。

**[2026-09-03 第八轮：③ 第三波——E2E 130 全链绿（指针下标读写）+ 宿主内存模型统一 C 布局 + 四簇缺陷当场根治]**（家机 Win11 AMD64）：
- 拉取单位机 1e8e6ea 后首跑 win-x64 门禁：129 在 win 侧首跑即炸（v2 movsxd 缺
  dword ptr——GAS 侧无歧义，单位机仅跑 ARM64 故未测出，双后端对称验证价值实证）。
- 130 立项验证期按缺陷零容忍纪律（用户重申开发纪律令）连根治四簇：
  ① 宿主内存模型三套咬合统一 C 布局（数组 8 槽×步进 8 双错互洽 vs 结构体紧凑
  vs 容器 max(大小,8)——asm diff 7 处 shl 3→2 锁定 A-3 读分支步进不一致）；
  ② 宿主窄宽 StorePtr 常量 movsx 立即数；③ v2 movsxd 缺 dword ptr；④ v2 表达式
  栈卫生家族（取地址清空全栈+前缀一元双份残留——f(&x)/q[-1] 两形态踩出）。
- v2 指针下标四层实现（语义指针变量表+白名单/IR 步进按元素宽度+左值地址+写宽度）
  ——v2p 两负测自检+130 期望固化；宿主 sanity 与 v2 闭环产物输出一致（4/77/通过）。
- 容器/数组重载用例 30/113/111/05/09 回归绿；全量门禁见 HANDOFF。

**[2026-09-03 第七轮：③ 第二波三件套——E2E 129 全链绿（字符串下标+复合赋值五形态+方案A 登记点补全），两项用户裁决方案A]**（单位机 麒麟 Linux ARM64）：
- 立项 129 验证期按缺陷零容忍纪律探针实锤两簇缺陷，用户裁决均为**方案A（推荐项）**：
  ① 类型登记表三缺口（成员加载/用户函数返回/容器方法返回——缺陷①②同族，asm
  探针实锤 print_int 指针值/from_int 指针转数字串/裸 add，符号修饰核心在用）；
  ② 复合赋值词法有 token 解析无分支→静默丢句（分词 截断、上轮冒烟假绿）。
- 三件套+连带两枚（符号表 类型ID 双约定统一、检查块 双重处理嵌套语句——遮蔽
  检查死而复生连环误伤 126/127 后根治）全部落地，详见二表 ③ 第二波行与 lessons。
- 词法分析.cn 冒烟**真绿**（分词 首次完整编译，节点 488→556）；语法分析.cn 冒烟
  启动待全量回归收口后续推。
- 65_string_index ARM64 跳过摘除（ldrsb 符号扩展后依据失效，手动实测一致）。

**[2026-09-03 第六轮：单位机三项门禁收口 + ③ 自举对拍启动（内置函数第一波落地，E2E 128 全链绿）]**（单位机 麒麟 Linux ARM64）：
- 用户裁决：三项收口后启动 ③ 首组件对拍（推荐项）。
- **三项收口**：① 清场重建（GCC 零警告）+ 单测 1196/1196 + linux-arm64 全量 E2E
  129 用例 124 过/1 败（78）/4 平台跳过——**127 新增 ARM64 首次运行时全绿**（②c
  家机对称实现的 GAS 静态槽/.data、逻辑非修正、D byRef 全部运行时实证）；② 118
  Linux 侧全量中绿（上轮疑点消除）；③ 78 -11 分诊=OOM 实锤（26.6GB VM 耗尽物理
  内存被 OOM killer），同 win 内存超限已知遗留，双重释放假设排除（详见二表）。
- **③ 启动（对拍基建）**：v2p 编译词法分析.cn 冒烟实测踩出**内置函数缺口**（语义 66
  诊断 + 链接期 `bl _hex(驻留)` 裸中文名符号）→ **E2E 128 立项**（立项期=宿主通用
  路径+宿主实际输出期望=宿主级回归；注意「空期望立项即绿」仅适用已注册闭环条目
  的 v2p 退出码断言，链接级缺口会使闭环红——本轮回退注册改走通用路径）→ **内置
  函数全链四层落地**（见二表）→ 128 注册闭环+期望改 [sem5] 自检行 → **六步闭环
  PASS**。asm 实查：`bl __cn_intern/_str_eq/_str_len/_str_sub/_str_from_int/
  _str_concat/_print_str/_print_newline/_runtime_error` 全裸 ASCII 直调。
- **首组件冒烟结论**：词法分析.cn（291 行）语义 0 错误；IR 崩点=「字节值」函数的
  **字符串下标 `源码[位置]`**——v2 未实现语法（P7b 后续清单「下标」项），**下一
  缺口 → E2E 129 用例先行再实现**（字符串下标/数组下标/指针下标三形态评估）。
- 踩坑（记 lessons）：IR_常量整数 操作数1=**值文本驻留 ID**（代码生成经 驻留文本
  还原立即数），传数值必得 0/错值；v2 源码残缺占位行（`未完`）会被当合法调用语句静默
  通过（无未定义符号链接拦截——裸调用发射为中文 hex 符号不查定义表），提交前须
  全量回归把关。
- **用户审计轮（同日，缺陷零容忍纪律执行）**：用户质询「有没有明知缺陷而妥协绕开」——
  自查揪出三缺陷并按用户裁决**全部当场根治**（探针 p5/p6 asm+运行时双实证）：
  ① 打印字符串变量错分派 `__cn_print_int`（打指针值）——根因：字符串性判定只查
  「字符串寄存器集合」（登记点缺**变量/参数**）+ 函数级权威表 ②c 只登容器/结果/
  结构体局部、**标量（字符串/浮点/布尔）漏登**→ 补标量局部/参数权威表登记；
  ② `字符串+数值` 静默错误代码（数值位模式当指针传 concat 崩溃/乱码——半实现比
  未实现更糟）→ 升级**寄存器→类型码登记表**（五登记点含比较/逻辑产物 T_布尔）+
  拼接**双侧隐式转换**（from_int/from_float/from_bool，宿主 Task 2.9 全对齐）+
  打印四分派（str/float/bool/int）；③ 类型大小折叠栈底负符号残留（原以「相对栈顶
  无害」推后——违反一劳永逸）→ 弹 2 根除。128 用例扩展固化断言（27/28/29 失败码
  +打印分派运行时载体）。

**[2026-09-02 第五轮：②c 四项一轮收官——E2E 127 全绿（win-x64）+ ③ 前三缺陷根治 + 单位机遗留三项分诊收口]**（家机 Win11 AMD64）：
- **接手第一小时收口（上轮换电脑中断项）**：win-x64 ci.ps1 全量门禁首跑 124/128——除 78/79
  已知遗留外揪出三新缺陷并全部根治：① v2 MASM `IR_逻辑非` 直接 `cmp 槽,0`——栈槽内存
  操作数 MASM 无法推断尺寸（A2070×22 处，125/126 win 全灭级）→ 改先 `mov rax,槽` 再
  `cmp rax,0`（对齐本文件比较运算惯例）；② 126 主.expected 在单位机从 v2p 实际输出直抄
  （含 `目标平台: linux-arm64`/`行数=1971`/`v2asm.s` 三处平台敏感行，违反 119-125「win
  口径+Linux 分支自动归一」惯例）→ 按 126 惯例修 3 行（删平台 echo/行数置空/.asm 口径）；
  ③ B7 供给产物目录中文名（供给126_供给）经 rsp 传 link.exe 被 GBK 误读 UTF-8 → LNK1181
  → 双平台供给产物路径一律 ASCII（supplyN_M）。118/78 分诊收口：118 win 本就绿（上轮
  Linux 侧疑点）；78 win 为内存超限（=文档化已知遗留，非段错误——上轮「级联双重释放」
  假设未获 win 侧证实，Linux -11 仍留单位机复查）。
- **②c 四项全链落地（8 组件，E2E 127 四主题单用例载体全绿 =0）**：
  - **A 嵌套容器**：实参再切分按基名元数贪婪解析（向量$映射$整64$整64 的 T -> 映射$整64$整64
    整段还原；词扫描须含数字——整64 切成「整」致符号损坏实测根治）；**ABI 实测修正**：容器/
    类元素 `元素 : ptr`（E2E 118 asm 双实锤：rax 返回元素指针、链式 .设置 this=rax 直传），
    v2 按标量指针形态非 sret——误入 结构体 形态即 this/实参错位段错误；方法对象类型统一
    递归推导（标识符/成员/调用 三形态：表.追加/b.表.追加/元素(0).设置/词池.元素(0).表.大小）。
  - **B 容器字段（指针槽模型）**：语法层登记容器字段四元组（含 24/56 对象大小布局保留）；
    字段构造赋值（NewObject+构造+StorePtr）复用既有成员赋值路径；`b.表.追加` 接收者=字段
    首 8 字节堆指针（标识符取槽址/调用用结果基址→字段地址→指针加载）；**RAII 级联**：
    含容器字段结构体局部登记（块出口/返回按字段逆序 取址→字段地址→指针加载→DeleteObject
    空安全）；拥有式供给契约 ABI：含容器字段的结构体以类形态供给（H8-⑤ 深拷贝+Feature2
    级联）——宿主按类元素 8 字节指针步长存储，v2 侧返回形态同按指针标量（B4 段错误根因）。
  - **C 顶层静态**：四层全链——语法 节点_静态声明（裸形态，121/v2 组件同款）+语义全局符号
    登记+IR 收集（容器=入口 主 注入 NewObject+构造+IR_静态存储；标量=折叠值直存 .data）+
    标识符 IR_静态加载+双后端 .data `_cn_gstatic_N`（MASM dq/GAS .quad，ASCII 序号命名）。
  - **D & 引用参数**：byRef 槽存调用方左值地址——读取分流（容器& 双重解引用得对象指针/
    结构体& 槽内容即基址/标量& 解引用）、赋值/自增 经指针写回、成员基址复用（跳过再取址）、
    prologue 跳过多槽拷贝；调用方 函数实参引用们 表驱动回扫取址（B8 扩展）；结构体& 解析
    期登记修复（附加==256 排除项）。
  - **③ 前潜伏缺陷根治（本轮实测揪出）**：解析期 变量容器表/变量结构体表 全局按名键——
    跨函数同名异型（追加表 的参数 t:向量& 与 主 的局部 t:桶）串类型 → 新增**函数级变量
    类型权威表**（生成函数 填充参数/容器/结果/结构体局部；标识符类型解析先查此表）。
  - **诚实边界**：拥有式结构体的赋值/按值传参/返回**拷贝点未实现深拷贝**（宿主该三点为浅
    拷贝共享——对齐宿主即违反安全目标，超宿主需先定拷贝构造调用 ABI 且 127 无载体断言；
    留待 ③ 自举实测踩到时以专用用例立项后实现）；ARM64 侧（GAS 后端 IR_静态加载/存储+
    .data）已对称实现但本机无 as/g++——linux-arm64 运行时验证留单位机。
- 验证：v2 全系 119/120/123/125/126/**127** 6/6 绿 + win-x64 全量门禁（构建零警告+单测+E2E）；
  127 期望文件含 v2p 自检行（win 口径，Linux 分支自动归一）。

**[2026-09-02 第四轮：②c 前置——宿主缺陷簇 1-4 根治（代码落地，双机验证进行时；换电脑中断，详见 HANDOFF「接手第一小时」）]**（单位机 Linux ARM64）：
- 用户裁决：②c 四项一轮收官（A 嵌套容器 / B 容器作结构体字段·拥有式 / C 顶层静态容器 /
  D & 引用参数；E2E 127 单用例四主题全覆盖）→ 第 0 步探针（target/dbg/probe127/p1~p24）
  实证宿主缺陷簇 → 用户裁决「先根治宿主缺陷簇」+ 缺陷4「本轮顺手根治」。
- **已根治（全部探针实证）**：①缺陷4 stdlib 向量/映射拷贝构造下分配（max(8,类型大小) 对齐
  扩容路径，p8/p9）；②缺陷1' 成员表达式下标步长兜底 8（memberFieldSrcType 统一双路径接线，
  p8/p9）；③缺陷1 类容器字段成员表达式访问统一 LoadPtr（p2/p6/p7）；④缺陷3 类字段级联析构
  注入（injectFieldCascadeDestroy）+ __cn_object_new calloc 清零 + 拥有式字段未声明析构警告
  （p10/p11 二十万循环零泄漏实证；v1 后端类补空体析构）；⑤缺陷2 结构体容器字段支持——
  语义趟序重排（泛型注册→字段实例化归一→布局，容器字段不再兜底 8）+ 迭代稳定性修复
  （ASAN 实证 emplace_back 重分配打悬 range-for）+ 结构体裸声明零初始化。
- **模型定案（勿翻案）**：结构体/类容器字段一律指针槽模型（typeSizeOf 保留+首8字节存堆对象
  指针，与 A-4/byRef/P3-8 全线一致）；内联模型被 p24 证伪（byRef 接收者双层解引用）已回退。
- 验证状态：单测 1196/1196 + 探针 17/17 + v2p-119 冒烟 PASS；**待办**：E2E linux-arm64 全量
  复确认、118 输出不一致分诊、78 段错误(-11) 分诊（嫌疑=级联双重释放）、win-x64 ci.ps1
  全量门禁（家机，②b 欠项一并）；之后 E2E 127 立项 → ②c A/B/C/D 实现。

**[2026-09-02 第二轮后半：②b 结构体元素容器收官——E2E 126 首次全绿]**（本机 Linux ARM64）：
- **126 运行时 OOM（SIGKILL）根因确诊并根治**（三重实锤：限内存 2GB 下退出 201=追加
  realloc 耗尽 → 当循环死循环；asm 级 `str x10,[x9]` 8 字节写；n 基址-1096/i 槽-1072
  逐字节对上）：**IR_指针存储/加载 无宽度概念**（IR生成 赋值存储类型写死 0、成员加载
  写死 T_整64；双后端发射固定 8 字节）——末字段 子终点@20（整32）8 字节写越出 24 字节
  结构体 4 字节，恰好清零紧邻 i 槽 → `当(i<20)` 死循环 → 无限追加 → 堆 OOM。
- **本轮落地（用户裁决计划六项 F1-F6 → 代码注释 B5-B8 编号）**：
  - B5 字段宽度读写+链式偏移收口：IR容器 分桶 字段偏移表/查字段宽度码/类型宽度码
    （窄宽码 T_整8/整16/字符）；IR生成 成员访问三形态（标识符→变量结构体表、调用对象
    →方法签名返回类型分桶收口——根治扁平回退同名字段跨结构体必错）；赋值存储按字段
    宽度；双后端 ldrb/ldrh/ldr w/strb/strh/str w 与 movzx/mov byte-word-dword ptr 对称。
  - B6 retbuf 按真实返回字节数定帧：函数IR.retbuf字节数（模块级累计回填）+ 共用层
    retbuf槽位数/retbuf基槽 单点决策——根治固定 2 槽=16 字节对 ≥17 字节结构体返回
    越界踩顶变量槽（lessons 权重 10 同族）。
  - B7 符号供给机制：run_e2e.py v2闭环用例们 第 4 元=供给源列表（双平台），供给 .cn
    拷贝为独立目录 主.cn 经宿主真实管线编译（**宿主按规范08-四 仅以 主.cn 为入口模块，
    其余按导入模块合并公开声明——非主命名入口函数会被静默过滤**），链接置于 v2p.obj
    之前（用例自有结构体布局权威）；126/供给.cn 实例化 向量$节点/$词条 全方法面。
  - B8 按值传参 ABI 补全：被调方 prologue 多槽拷贝（双后端：ARM64 字循环/MASM rep movsb，
    参数槽序们/参数拷贝字节们 声明序驱动；扫描函数 参数槽计数改按基槽最大值）+ **普通
    函数调用结构体实参传址**（函数实参结构们 表驱动回扫——B3 仅覆盖容器方法；v2 自身
    IR追加(指令,建IR指令(...)) 全是此形态，③ 自举必需）。临时用例验证（读字段/拷贝
    语义/被调方修改不影响调用方）退出码 0 后删除。
  - **连带新缺陷根治：裸块语句解析缺失**——解析语句 无 `{` 分支 → 解析块 循环保护中断
    → 块内及后续全部语句（含 返回 0）静默丢失（126 退出码 20 即垃圾返回值）。解析层
    补裸块分支 + IR生成 生成语句 预扫描跳过（体块排除防双重生成）+ 节点_块 递归生成块
    （作用域基线/块出口析构 复用）；语义层检查块 本就支持。
  - **B4 裁定（ABI 级事实）**：宿主「有析构」=显式声明析构方法（class_resolver:145）；
    字符串为驻留不朽指针无释放语义——整64+字符串 结构体（词条）无析构可注入（深拷贝=
    指针拷贝），④a 零泄漏由绿色运行证实。**B4 真正载体=拥有式字段（容器字段）在 ②c**。
- 验证：单测 1196/1196；E2E linux-arm64 v2 全系 119/120/123/125/**126** 5/5 全绿
  （126 首次：布局/视图/传参/深拷贝收缩四缺口载体）；MASM 产出 asm 级核验（dword ptr
  存储/窄读 movzx/retbuf 帧尺寸/被调方 rep movsb）——win-x64 运行时门禁待 Windows 会话。
- 下一步：②c 嵌套容器/容器作结构体字段（B4 真载体）/顶层静态容器/& 引用参数 →
  ③ v2 编译 9+组件自举对拍（ARM64 路径本机即可全程对拍，对标 79 闭环）。

**[2026-09-02 第二轮（v2 三缺陷根治 + 阶段A GAS 后端 + 后端架构方案1）]**：
- 用户裁决链：① 先做 v2 ARM64 GAS 后端（本机全程可运行时验证）→ ② 先根治 v2 已立案
  三缺陷（中途回退/短路/转义）→ ③ 后端架构质询后裁决方案1（抽共用决策层）——全部落地。
- 验证边界（诚实门禁）：本机 GCC 零警告 + 单测 1196/1196 + E2E linux-arm64 127 用例
  （122 过/4 平台跳过/仅 78 已知遗留）；**win-x64 运行时门禁（含 123=21333、125）待
  Windows 会话执行，本机无 MSVC 不写假绿**；转义内容级运行时断言待 内置函数符号映射
  （③ 前特性，125 暂以容器追加通路覆盖发射路径）。
- 下一步：**②b 结构体元素容器**（向量<Token>：布局对齐 typeSizeOf/按值传参 ABI/元素(i)
  视图/追加深拷贝/删除析构注入——含资源字段结构体一步到位，用户裁决；E2E 126 载体）
  → 嵌套容器/容器字段/静态容器/&引用 → ③ v2 编译 9+组件自举对拍（ARM64 路径本机即可对拍）。

**[P7b 后续：新规则三项裁决落地（项2/项3 完成，项1 过渡态+缺陷立案）]**（2026-09-01）：
- 用户令「缺陷零容忍·强制汇报」后对 P7b 三项轻微简化逐项裁决：
  - **项3 任意参数数（完成）**：IR_调用实参=64（声明性实参装载，位号标注）+
    IR_调用参数化=63 统一发射（rcx..r9 + 栈参数区，被调方 [rbp+48+8k] 读取 +
    被调方 prologue 参数 5+ 装载扩展）——方法/普通调用同一路径；6 参函数实测 655308 ✓。
  - **项2 块级作用域析构（完成）**：生成块 入口记录 容器变量们 基线、出口
    块出口析构（本块新增容器逆序 RAII + 列表截断）；返回语句仍析构全部（跨块跳出
    兜底）；嵌套块同名容器由语义层拒绝（P7c 完整块上下文前的防护，泄漏/双释风险）。
    循环体/分支块实测 356 ✓。
  - **项1 stdlib 签名表（✅ 完成，2026-09-01 追加）**：IR签名.cn（新组件，409 行）——token
    状态机扫描 stdlib/容器.cn+映射集合.cn 的泛型类方法签名（类头参数序/参数串/
    返回类型），查询时 T/K/V 按实例实参替换；查方法签名 先查表、未命中回退内置表
    （**缺陷未根治期保绿**）。**发现两个缺陷未根治（见三表「签名扫描 UB」）**。
- 全量：构建零警告 + 单测 1196/1196 + E2E 126 用例 124 通过（仅 78/79 已知遗留）；
  119/120/123/124 全绿。

**[P7b 容器类型实例化与容器方法调用 完成]**（2026-09-01，Windows 本机）：
- HANDOFF 之②落地：v2 自身实现容器支持（语法/IR/代码生成三层 + 新组件 IR容器.cn），
  方案=生成对宿主符号的直接调用（向量$整64$追加#整64 -> ?E59091...@@YJ@Z），链接
  v2p.obj 获得实现（rustc 预编译 std 模式；③ 对拍沿用：v2 编译 7+1 组件 -> 链接）。
- 实测根治 5 缺陷：MASM 247 符号限制（9 参函数超限 -> PROC 失效 -> bb 标签泄漏连锁
  A2005；解法=语法层预标注回退参数）/ retbuf 与结果变量 2 槽基址向上写越界覆盖
  对象指针（基址须取最低地址=最大槽号）/ 向量<字符串> 跨向量搬移行指针悬垂（RAII
  拥有式元素）/ AST 节点.子终点 复用伪造子区间（语义层误报未声明）——全部记 lessons。
- 验证：E2E 123 六步闭环 PASS（21333）；119/120 回归 PASS；全量门禁见提交信息。
- 下一轮（②b→③）：**结构体元素容器**（向量<Token>——v2 布局对齐宿主 typeSizeOf、
  结构体按值传参 ABI、元素 视图语义）→ 嵌套容器（向量<映射>）/容器作结构体字段/
  静态容器/& 引用参数 → v2 剩余语法（下标/三元/字符串拼接/内置函数映射/静态变量）
  → ③ v2 编译 8 组件自举对拍（对标 79）；顺带：链式字段取值缺陷（三、表）。

**[P7a 顶层常量全链路 + 宿主四缺陷根治 完成]**（2026-09-01，Windows 本机）：
- 接手后发现 E2E 119「全绿」基线为误信（P6h 在 Linux 开发无法运行 v2p）——本机重跑即崩，
  按宿主缺陷优先暂停 P7a，二分定位四叠加宿主缺陷并全部根治（见二、表「宿主四缺陷」行）。
- P7a 顶层常量四层实现完成（词法/语法/语义/IR，代码生成零修改）。
- 验证：E2E 119 **本地真实首次通过**；120/121/122 新增通过；118/01 回归通过；
  v2p 默认路径与 119 入口全链路退出 0；全量门禁 ci.ps1：构建零警告 + 单测 1196/1196 +
  E2E 124 用例 122 通过（仅 78/79 已知遗留内存超限，与本轮无关）。
- 下一轮：② 容器类型实例化与容器方法调用（IR 层 运行时错误(7) 是当前硬缺口）→
  ③ v2 编译 7 组件自举对拍；顺带：链式字段取值缺陷（三、表）。

**[P3-8 顶层静态容器变量根治完成]**（2026-08-30）：宿主编译器五处叠加缺陷全链路修复（parser/semantic/ir_oop/ir_oop_call/codegen），probe 验证 静态 向量<整64> 全局表 的 大小/追加/元素 全通（exit=249），E2E 关键用例 + 单测 1196/1196 + v2p exit=0 无回归。

**[v2 代码生成（P6）asm 实际运行验证成功]**（2026-08-25，用户批准自举重建）：
- **P5 IR 生成完成（6e93b62）**：AST 节点池 -> 三地址码 IR 全链路。
- **P6 代码生成完成（419a5c5 + fdc0237）**：IR -> win-x64 MASM，栈槽模型
  （寄存器 r -> [rbp-8r-8]、变量槽 -> [rbp-8(最大寄存器+s)-8]）、nameMangle
  （中文 -> ?XX@@Y）、.code 段 + END。
- **关键里程碑（fdc0237）**：v2 生成 加(a,b) asm -> ml64 汇编 -> link ->
  运行（main 调 加(3,4)）-> RESULT=7（退出码 7，加法正确）。v2 组件链生成的
  机器码**可实际运行**。
- 已修 v2 组件 bug：参数区间/引用传参/向量字段未初始化/字符串比较（1c297c0 等）。
- 已修 3 个宿主缺陷：字段类型泛型归一（1ea95ab）；共享 AST resolvedType 污染
  （8dd6722）。
- **函数调用 Call 完成（1839132）**：语法分析 调用节点 附加存实参数；IR 生成
  标识符函数名（负数符号压栈）+ 节点_调用（IR_调用：操作数1..3=实参，目标=返回，
  附加=符号）；代码生成 IR_调用（参数装载 rcx/rdx/r8 + 影子空间 + call + 返回）。
  验证：双倍(x){x*2} + 主(){双倍(3)} -> ml64/link/运行 RESULT=6（跨函数调用正确）。
- **控制流 如果/当 完成（2026-08-30）**：语法分析 如果（含 否则/否则如果 链）/
  当 节点存 条件起点/条件终点/块索引；IR 生成 块编号随建随取 + IR_跳转/IR_条件跳转/
  IR_标签 + 生成如果/生成当（循环上下文 中断/继续 目标块）+ 预扫描跳过控制流体区间
  （体语句节点在控制流语句节点之前，避免外层重复生成）；代码生成 bbN 标签 +
  jmp/jnz + 帧大小 +8（覆盖变量槽最大偏移，修复 [rbp] 越界段错误）。
  验证：主(){值=3; 如果(值==3){值=值+1}否则{值=值-1}; 返回 值} -> ml64/link/运行
  RESULT=4（如果/否则正确）；阶乘(5) 当循环 -> 120（循环/条件跳转正确）。
  附修：词法 多字符运算符（== != <= >= && || ++ += -> ::）、一元 -/! 仅表达式入口
  （避免 n-1 被解析为负号）、二元优先级 5 层。
- **指针/字段访问 完成（2026-08-30）**：语法层 指针类型 `类型*`、`&` 取地址/`*` 解引用
  前缀、结构体字段偏移表（字段名→偏移，8 字节槽对齐）、`解析后缀链` 提取（二元操作数支持
  `a + b.c`）、成员/解引用左值赋值、分号消费；IR 层 IR_取地址/指针加载/指针存储/字段地址
  发射、`生成左值地址` 辅助、成员对象索引存子起点（修基址取 Load 值崩溃）、`&对象` 子区间
  预扫描跳过；代码生成 lea/ptrload/ptrstore/fieldaddr。
  验证：点{位置.x=7; 位置.y=9; 指针=&位置.x; *指针=10; 返回 位置.x+位置.y} -> ml64/link/运行
  RESULT=19（成员赋值/取地址/解引用赋值/成员读取全正确）；控制流回归 阶乘(5)=120。
- **中断/继续 循环验证 完成（2026-08-30）**：单层 `1..10 奇数和`（继续跳过偶数、
  n>7 中断）-> RESULT=15；嵌套循环（内层 继续/中断 只影响内层）-> RESULT=12；
  循环上下文栈（函数IR 循环出口块/继续块 保存恢复）工作正常；指针/控制流回归无破坏。
- 下一步：v2 编译自身（自举闭环）——需补 数组/字符串/容器 运行时能力后推进。
- 前序：1ea95ab + 8dd6722 + 1c297c0 + 6e93b62 + 419a5c5 + fdc0237 + 1839132。
- 根因定位（两个叠加缺陷）：
  ① 类值链式：exprSrcType 的 MemberExpr 分支不识别 结果/可选 成员映射（值 不是
     合成结构体直接字段，在联合体）-> exprSrcType(查.值) 返回空 -> handleClassMemberExpr
     未识别类字段访问 -> 走 findStruct(资源=类)=null 防御返回 0。
  ② 链式方法调用：exprSrcType 的 CallExpr 分支只处理 IdentifierExpr 被调函数
     （安全除法(a,b)），不处理 MemberExpr 被调方法（表.元素(0)）-> 对象类型推导
     失败 -> handleClassCallExpr 未识别类方法调用 -> 通用间接调用。
- 修复：exprSrcType 补 结果/可选 成员映射（.值 -> T、.错误 -> E、.正常/.有值 ->
  布尔）+ CallExpr 分支补 对象.方法() 返回类型解析（lookupClassMember 沿继承链）。
- 新增 E2E 118_宿主缺陷_链式方法调用与类值链式访问（类值链式 5、元素(0).设置 777、
  读取(0).值.编号 11/22）。
- 回归：E2E 118/120（78/79 已知遗留）、单测 1196/1196。
- 前序：move 语义（117，7b4a859）。

## 五、后续计划（按序）

> **排序修正（2026-08-25 用户指正）**：按「宿主缺陷优先」「一劳永逸」规则，**已知宿主缺陷必须全部根治后才推进自举重建**——v2 自举组件本身用 CN 编写，会持续踩到宿主缺陷；带病重建必然中途反复停下或被迫妥协。故自举重建（v2 IR/代码生成）排在**已知缺陷全部根治之后**。

1. **自举重建推进**：v2 结构化 IR 生成 / 代码生成组件，直至真正自举闭环（性能最优架构目标）——宿主缺陷已全部根治，不留遗憾地重建。78/79 内存超限是旧架构固有（推倒重建动机），随 v2 完成自然解决（v2 结构化 IR + arena + 符号驻留，内存降为合理水平，78/79 应通过）；不在旧组件上做治标修补（旧架构将被 v2 替代）。
2. **P4 代码清理**：链式方法调用已根治，可去除 结果.值 临时绑定规避 + 提交。
3. **v2 自举闭环验证**：v2 组件链可编译自身（结构化 IR 全链路），78/79 用 v2 产物验证闭环（内存/性能达标）。
4. **文档同步**：每阶段完成后更新本文档（用户规则）。

## 六、工作区状态

- 13ee8cc（P6h）及之前全部已提交推送。
- 当前未提交（本轮 P7a + 宿主四缺陷根治）：src/cn_compiler 7 文件（ir_decl/ir_expr/ir_stmt/ir_oop_call/x64_codegen/x64_codegen_oop/arm64_codegen_oop）、stdlib 2 文件（文件.cn/IO.cn 补导入）、v2 组件 4 文件（词法/语法/语义/IR生成 常量支持）、E2E 新增 120/121/122 + run_e2e.py 参数化、plans/014、更新日志、lessons、HANDOFF。
- 本环境（Windows，VS2022）：构建零警告、单测 1196/1196、E2E 119/120/121/122/118/01 本地真实运行通过；全量 ci.ps1 门禁见提交信息。
- 调试产物：target/dbg/（c1~c18 最小复现与 121_备份.cn，均在 target/ 内不入库）。
