# CN 语言阶段6「标准库」完善设计方案（待批阅）

> 依据：Ask 子任务已完成对 [`plans/002-阶段6-标准库.md`](plans/002-阶段6-标准库.md)、CN 规范 specs 01a~08、[`src/runtime/`](src/runtime) 全部 5 文件、HANDOFF/lessons 的全面调研。本方案**仅设计，待您批阅后才执行**。

---

## 一、现状诊断（为什么必须重新设计）

| 维度 | 现状 | 与"C++ 不弱"的差距 |
|------|------|------------------|
| 阶段6计划 | 仅 6 个 Task（核心/IO/数学/集合/字符串/条件编译），全未打勾 | 覆盖面仅 C++ 标准库的 ~20% |
| 容器 | `stdlib/` 目录不存在，无任何容器 | 🔴 向量/列表/映射/集合/队列/栈 全缺 |
| 算法 | 完全无 | 🔴 排序/查找/变换 全缺 |
| 数学 | 仅 i128 运行时 18 个辅助；`数学.cn` 是占位（`平方根(x){返回 x}`） | 🟡 缺复数/随机数/常量/更多函数 |
| IO | 仅 stdout 打印；**无输入、无文件、无 stderr** | 🔴 输入读取行/整数/浮点、文件读写全缺 |
| 格式化 | ✅ `格式化()` sprintf 风格 9 种占位符（变参 ABI 已解决） | 🟢 基础好，缺封装与解析反向能力 |
| 字符串 | ✅ 15 个内置函数 | 🟡 缺字符串→数值解析 |
| 时间/日期 | 完全无 | 🔴 全缺 |
| 语言能力 | 泛型类/继承/接口/运算符重载(成员)/lambda/函数指针/结果可选/模块系统 **全部就绪** | ✅ 足以支撑 C++ 级标准库 |

**结论**：CN 语言的**语法能力已足够**（泛型类 + 手动内存 + 结果错误码 + 模块系统），缺的是**标准库的规模化设计**。本方案以"C++ 标准库为对标、CN 语言能力为边界、阶段资源为约束"重新规划。

---

## 二、设计目标与原则

1. **对标 C++ 标准库**：覆盖 容器/算法/迭代器/字符串/数学/IO/文件/格式化/时间/系统 十大域，逐域对齐（见第六节对标表）。
2. **CN 语法优先，运行时最小化**：能在 CN 层（`.cn` 模块）实现的绝不放运行时；运行时只补语言无法表达的部分（文件 syscall、时间、输入、数学函数底层）。
3. **模块化组织**：`stdlib/` 目录按域分文件，`公开:` 导出 API、`私有:` 隔离实现细节，用户 `导入` / `从...导入` 使用（对标 C++ 的 `std` 命名空间）。
4. **错误处理统一**：全部返回 `结果<T,整32>` / `可选<T>`，错误码沿用 1~8 约定（5=文件打开失败等），无异常。
5. **E2E 先行**：每个模块先写 E2E（`tests/e2e/NN_xxx/`），测试通过才算完成；禁止 skip、真实运行。
6. **硬性约束**：单文件 ≤1000 行、函数 ≤100 行（超限拆子模块）；中文命名 API；运行时 C++ 实现用英文 API；编译零警告；中文 Git 提交。
7. **五处同步**：新增任何内置函数必须同步 语义注册 registerBuiltins / x64 codegen / arm64 codegen / E2E / 单测 / 文档。
8. **内存纪律**：容器类全部 RAII（析构释放），调用方对 `分配`/`格式化` 返回的指针负责释放；`字符串` 释放用 `字符串释放`。

---

## 三、标准库总体架构

```
stdlib/                     （CN 层标准库模块，全部公开: 导出）
├── 核心.cn                 # 基础工具：最小/最大/交换/断言/错误码常量
├── 容器.cn                 # 向量<T> / 链表<T> / 栈<T> / 队列<T>（泛型类）
├── 映射集合.cn             # 映射<K,V> / 集合<T>（原计划"集合.cn"）
├── 算法.cn                 # 排序/查找/变换/遍历/计数/逆序
├── 迭代器.cn               # 迭代器<T> 接口 + 容器适配（可与容器.cn 合并）
├── 数学.cn                 # 常量 + 平方根/幂/三角/绝对值/取整/复数(类)/随机数
├── 字符串扩展.cn           # 数值解析(字符串转整数/浮点)/分割/替换/填充
├── IO.cn                   # 打印/打印行/格式化 封装 + 读取行/读取整数/读取浮点
├── 文件.cn                 # 打开/关闭/读/写/按行读/文件信息
├── 时间.cn                 # 时钟/时间戳/格式化时间
├── 系统.cn                 # 命令行参数/环境变量（基础版）
└── 条件编译               # 预处理器：#如果定义/#否则/#结束如果（编译期特性）

src/runtime/                （运行时 C++ 层，英文 API，新增 3 个文件）
├── math_api.cpp    [新增]  # __cn_sqrt/pow/sin/cos/tan/fabs/ceil/floor + 随机数
├── input_api.cpp   [新增]  # __cn_read_line/__cn_read_int/__cn_read_float
├── file_api.cpp    [新增]  # 文件打开/关闭/读/写/按行读（中文路径 _wfopen_s 方案）
├── time_api.cpp    [新增]  # 时钟/时间戳/时间格式化
├── i128_api.cpp    [已有]  # 18 个 i128 辅助（不动）
├── io_api.cpp      [已有]  # 打印 + 错误码（不动）
└── string_api.cpp  [已有]  # 15 个字符串函数（扩展：字符串转整数/浮点解析）
```

---

## 四、各模块详细设计（API 清单）

### 4.1 核心.cn（对标 C++ <utility>/<cassert>）
```
常量 错误.除零 = 1 / 错误.越界 = 2 / 错误.空指针 = 3 / 错误.内存 = 4
错误.文件 = 5 / 错误.参数 = 6 / 错误.未初始化 = 7 / 错误.溢出 = 8
泛型 <类型 T> 函数 交换(T* 左, T* 右)      # 值交换（示例已存在，完善）
泛型 <类型 T> 函数 最小(T 左, T 右) -> T
泛型 <类型 T> 函数 最大(T 左, T 右) -> T
函数 断言(布尔 条件, 字符串 消息)          # 失败 → 错误码6终止
```

### 4.2 容器.cn（对标 C++ vector/list/stack/queue）
全部为**泛型类**（`泛型 <类型 T> 类 向量`），内部用 `分配`/`重新分配` 管理堆内存，**析构释放**（RAII）：
```
泛型 <类型 T> 类 向量
  公开: 向量() / 向量(整64 容量)          # 构造
  析构函数()                               # 释放底层数组
  整64 大小() -> 整64 常量成员函数
  整64 容量() -> 整64 常量成员函数
  布尔 为空() -> 布尔 常量成员函数
  函数 追加(T 值) -> 结果<空,整32>         # 内存失败 → 错误.内存
  函数 插入(整64 位置, T 值) -> 结果<空,整32>
  函数 删除(整64 位置) -> 结果<空,整32>
  T 读取(整64 位置) -> 结果<T,整32>        # 越界 → 错误.越界
  T& 下标(整64 位置)（若支持引用返回，否则 读取+修改）
  函数 清空()
  函数 预留(整64 容量) -> 结果<空,整32>

泛型 <类型 T> 类 链表     # 节点结构体 + 头尾指针；追加/删除/遍历
泛型 <类型 T> 类 栈       # 压入/弹出/栈顶/为空（可用向量实现）
泛型 <类型 T> 类 队列     # 入队/出队/队首/为空（可用链表实现）
```
> 依赖：泛型类（[06-十三]）、结构体、手动内存（`分配`/`重新分配` 已内建）。

### 4.3 映射集合.cn（对标 C++ map/set，规格书 4.4「表.包含(键)」）
```
泛型 <类型 K, 类型 V> 类 映射   # 内部：键数组+值数组（线性查找，O(n)）
  # 注：CN 无红黑树库依赖，P0 用线性表实现，文档标注复杂度；P1 可选哈希表
  设置(K 键, V 值) -> 结果<空,整32>
  V 获取(K 键) -> 结果<V,整32>
  布尔 包含(K 键) -> 布尔 常量成员函数     # 规格书 4.4 明确要求
  删除(K 键) -> 布尔
  整64 大小() -> 整64 常量成员函数
  清空()

泛型 <类型 T> 类 集合       # 内部：映射<T,布尔> 或 数组+包含检查
  添加(T 值) -> 结果<空,整32>
  布尔 包含(T 值) -> 布尔 常量成员函数
  删除(T 值) -> 布尔
  整64 大小() -> 整64 常量成员函数
```

### 4.4 算法.cn（对标 C++ <algorithm>）
```
泛型 <类型 T> 函数 排序(T* 数组, 整64 长度, 函数指针 比较)   # 快速排序（迭代实现避免递归深度）
泛型 <类型 T> 函数 查找(T* 数组, 整64 长度, T 目标) -> 结果<整64,整32>   # 线性查找，返回索引/错误.参数
泛型 <类型 T> 函数 二分查找(T* 数组, 整64 长度, T 目标, 函数指针 比较) -> 结果<整64,整32>
泛型 <类型 T> 函数 逆序(T* 数组, 整64 长度)
泛型 <类型 T> 函数 变换(T* 数组, 整64 长度, 函数指针 映射)   # 原地变换
整64 计数(T* 数组, 整64 长度, T 目标)
泛型 <类型 T> 函数 最大元素(T* 数组, 整64 长度, 函数指针 比较) -> 结果<T,整32>
```
> 依赖：函数指针（[04-四]）回调 + 泛型函数（[06-十三]）。**需先做 E2E 验证「泛型函数 + 函数指针回调」组合**，这是本模块最大技术风险点。

### 4.5 数学.cn（对标 C++ <cmath>/<complex>/<random>）
```
常量 数学.π = 3.14159265358979323846
常量 数学.自然对数的底 = 2.71828182845904523536
函数 平方根(浮64 值) -> 浮64               # __cn_sqrt
函数 幂(浮64 底, 浮64 指数) -> 浮64         # __cn_pow
函数 正弦/余弦/正切(浮64) -> 浮64           # __cn_sin/cos/tan
函数 反正弦/反余弦/反正切(浮64) -> 浮64      # __cn_asin/acos/atan（P1，一次补齐）
函数 绝对值(浮64) -> 浮64                  # __cn_fabs
函数 绝对值整数(整64) -> 整64               # 重载 绝对值 的整型版本
函数 向上取整/向下取整(浮64) -> 浮64        # __cn_ceil/floor
函数 对数(浮64) -> 浮64 / 常用对数(浮64) -> 浮64    # __cn_log/log10（P1）
函数 随机数() -> 整64                      # __cn_rand（P1：种子/范围）
函数 随机浮点() -> 浮64                    # __cn_rand_double（P1）
类 复数                               # P1：实部/虚部 + 运算符重载(成员 + - *) + 模长
  # 运算符重载仅限成员函数（[06-九]），左操作数须为本类实例，属规范约束
```
> **P0 先实现原计划 8 个**（平方根/幂/正弦/余弦/正切/绝对值/向上取整/向下取整）+ 常量；复数/随机数/对数划入 P1（需运行时 `math_api.cpp` 一次建齐，E2E 验证 ABI）。

### 4.6 字符串扩展.cn（对标 C++ <string> 解析能力）
运行时 `string_api.cpp` 新增（英文 API，注册进 registerBuiltins，五处同步）：
```
整64 字符串转整数(字符串) -> 结果<整64,整32>     # __cn_str_to_int（strtoll，失败→错误.参数）
浮64 字符串转浮点(字符串) -> 结果<浮64,整32>     # __cn_str_to_double
布尔 字符串转布尔(字符串) -> 结果<布尔,整32>     # "真"/"假"/"true"/"false"
```
CN 层 `字符串扩展.cn`（可用现有 15 个内置组合实现）：
```
字符串 分割(字符串, 字符串 分隔符) 的返回方案：
  # CN 数组长度须编译期常量 → 返回「字符串数组 + 数量」用 向量<字符串> 承载（依赖容器.cn）
函数 替换(字符串, 字符串 目标, 字符串 替换为) -> 字符串   # 遍历查找+拼接
函数 填充(字符串, 整64 目标长度, 字符 填充字符) -> 字符串  # 左/右/居中 三版
函数 截断(字符串, 整64 长度) -> 字符串
布尔 以数字开头/纯数字(字符串) -> 布尔
```

### 4.7 IO.cn（对标 C++ <iostream>/<cstdio>，含原 Task 6.2 输入）
运行时 `input_api.cpp` 新增 + 语义注册（五处同步）：
```
字符串 读取行() -> 结果<字符串,整32>          # __cn_read_line（fgets 动态，EOF→错误.参数）
整64 读取整数() -> 结果<整64,整32>           # __cn_read_int（读行+strtoll）
浮64 读取浮点() -> 结果<浮64,整32>           # __cn_read_float
空 打印到错误(字符串) -> 空                  # __cn_print_err（stderr，fprintf）
```
> ⚠️ 变参/多类型输入必须复用 Win x64 变参 ABI 经验（lessons 权重 14.4）；新增输入 API 非变参，风险可控。

### 4.8 文件.cn（对标 C++ <fstream>，规格书阶段五「文件系统」）
运行时 `file_api.cpp` 新增（**中文路径用 `MultiByteToWideChar` + `_wfopen_s`**，lessons 经验）：
```
句柄 打开文件(字符串 路径, 整32 模式) -> 结果<句柄,整32>     # __cn_file_open；模式：1读/2写/3追加；失败→错误.文件
整64 读取文件(句柄, 字符* 缓冲, 整64 数量) -> 结果<整64,整32> # __cn_file_read
整64 写入文件(句柄, 字符串 数据) -> 结果<整64,整32>          # __cn_file_write
字符串 读取整行(句柄) -> 结果<字符串,整32>                  # __cn_file_read_line
整64 文件大小(句柄) -> 结果<整64,整32>                       # __cn_file_size
空 关闭文件(句柄)                                            # __cn_file_close
布尔 文件存在(字符串 路径) -> 布尔                           # __cn_file_exists
结果<空,整32> 删除文件(字符串 路径)                          # __cn_file_delete（P1）
```
> 句柄用 `空类型*`（指针）承载 FILE*；错误码 5=文件打开失败已预留。**文件 IO 是 P0 硬需求**（规格书明确），目录遍历/复制移动划 P1/P2。

### 4.9 时间.cn（对标 C++ <chrono>/<ctime>）
运行时 `time_api.cpp` 新增：
```
整64 当前时间戳() -> 整64                    # __cn_time（秒）
整64 单调时钟毫秒() -> 整64                  # __cn_clock_ms（性能计时）
字符串 格式化时间(整64 时间戳, 字符串 格式) -> 结果<字符串,整32>  # __cn_time_format（strftime；空格式→默认）
```
> 可用于命令行工具日志/基准测试（里程碑「可开发命令行工具」所需）。

### 4.10 系统.cn（对标 C++ 程序入口 argv/env）
```
# 依赖规格书 10.4：主(整32 参数个数, 字符** 参数数组) 入口签名已预留，未实现
整32 参数个数() -> 整32                     # 运行时缓存 argc
字符串 参数(整32 索引) -> 结果<字符串,整32> # 运行时缓存 argv
字符串 环境变量(字符串 名) -> 可选<string>   # P1（getenv）
```
> P0 仅实现 参数个数/参数（命令行工具必须）；环境变量 P1。

### 4.11 条件编译（原 Task 6.6，编译期特性）
```
#如果定义 宏名        # lexer/parser 支持，按宏定义裁剪代码
#否则
#结束如果
```
> 与 C++ 预处理器对齐；需 lexer（`#` 行处理）+ parser 条件裁剪 + 单测 + E2E `24_preprocessor`。属 P0（原计划已列）。

---

## 五、运行时配套扩展汇总

| 文件 | 动作 | 新增 API | 对应 CN 模块 |
|------|------|---------|-------------|
| `math_api.cpp` | **新增** | `__cn_sqrt/pow/sin/cos/tan/fabs/ceil/floor`（+P1：asin/acos/atan/log/log10/rand/rand_double） | 数学.cn |
| `input_api.cpp` | **新增** | `__cn_read_line/__cn_read_int/__cn_read_float/__cn_print_err` | IO.cn |
| `file_api.cpp` | **新增** | 文件打开/读取/写入/按行读/大小/关闭/存在（+P1：删除） | 文件.cn |
| `time_api.cpp` | **新增** | `__cn_time/__cn_clock_ms/__cn_time_format` | 时间.cn |
| `string_api.cpp` | **扩展** | `__cn_str_to_int/to_double/to_bool` | 字符串扩展.cn |
| `semantic.cpp` | 修改 | registerBuiltins 注册全部新内置（五处同步） | — |
| `CMakeLists.txt` | 修改 | 新增 4 个 runtime 源文件 | — |

**不动**：`printLineInt/printLineFloat/printLineI128/printLineU128` 防御符号、i128 18 个辅助、现有 15 个字符串函数、`格式化` 变参 ABI。

---

## 六、与 C++ 标准库对标表（P0 覆盖度）

| C++ 标准库域 | 对标 CN | 优先级 | 说明 |
|-------------|---------|--------|------|
| vector/deque/list | 容器.cn 向量/链表 | **P0** | 泛型类 + 手动内存 + RAII |
| stack/queue/priority_queue | 容器.cn 栈/队列 | **P0** | 栈/队列用向量/链表实现 |
| map/set/unordered_* | 映射集合.cn | **P0** | 线性表实现，文档标注复杂度 |
| algorithm（排序/查找/变换） | 算法.cn | **P0** | 函数指针回调；先 E2E 验证组合 |
| string（解析/分割/替换） | 字符串扩展.cn | **P0** | 3 个运行时解析函数 + CN 层组合 |
| cmath | 数学.cn | **P0**（8函数）/P1（对数反三角） | P1 补齐 |
| iostream/cstdio 输入 | IO.cn | **P0** | 读取行/整数/浮点 + stderr |
| fstream | 文件.cn | **P0** | 中文路径 _wfopen_s |
| chrono/ctime | 时间.cn | **P0** | 时间戳/计时/格式化 |
| 程序入口 argv | 系统.cn | **P0** | argc/argv |
| complex | 数学.cn 复数类 | **P1** | 运算符重载成员函数实现 |
| random | 数学.cn 随机数 | **P1** | rand/rand_double |
| 智能指针（shared/unique） | 待定 | **P1** | 手动内存模型下的 RAII 类封装，语义需明示 |
| 迭代器（标准接口） | 迭代器.cn | **P1** | 容器遍历先提供 遍历(函数指针回调) 简化版 |
| regex | 暂缓 | **P2** | 工程量巨大，原始字符串语法已就绪 |
| thread/并发 | 暂缓 | **P2** | 需平台线程 API，超出阶段6 |
| 完整文件系统（目录遍历） | 暂缓 | **P2** | 基础文件读写先行 |
| RTTI | 暂缓 | **P2** | 规格书预留 |

**「不弱于 C++」的诚实定位**：P0 完成后，CN 标准库覆盖 C++ 标准库**常用核心域的 ~80%**；P1 补到 ~90%；正则/线程/完整文件系统/RTTI 属工程量大项，建议在阶段7自举后或专门阶段推进——这是资源约束下的最优顺序，非能力不可达。

---

## 七、实施计划（按依赖排序，每步 E2E 先行 + 单测 + 零警告 + plans 打勾 + 中文提交）

| 步骤 | 内容 | E2E | 依赖 |
|------|------|-----|------|
| 1 | 条件编译（Task 6.6）：lexer/parser + 单测 | `24_preprocessor` | 无 |
| 2 | 数学.cn + `math_api.cpp`（8 函数）+ 语义注册 | `25_math/` | 无 |
| 3 | 核心.cn（交换/最小/最大/断言）+ 容器.cn（向量/链表/栈/队列） | `26_container/` | 泛型类已就绪 |
| 4 | 映射集合.cn（映射/集合） | `27_map_set/` | 步骤3 |
| 5 | 算法.cn（排序/查找/逆序/变换）——先写 E2E 验证泛型+函数指针 | `28_algorithm/` | 步骤3 |
| 6 | IO.cn 输入（读取行/整数/浮点/stderr）+ `input_api.cpp` | `29_io/` | 无 |
| 7 | 文件.cn + `file_api.cpp`（中文路径方案） | `30_file/` | 无 |
| 8 | 字符串扩展.cn + `string_api.cpp` 解析函数 | `31_string_ext/` | 步骤3（向量承载分割） |
| 9 | 时间.cn + `time_api.cpp` | `32_time/` | 无 |
| 10 | 系统.cn（参数个数/参数） | `33_system/` | 无 |
| 11 | **全链路集成审查（Code 模式）**：所有模块串联，写一个综合命令行工具示例（如「学生成绩统计工具」）走通 容器+算法+文件+格式化+字符串 全链路 | `34_综合工具/` | 全部 |
| 12 | **Debug 模式全面审查**：验证 E2E 全绿、无孤立组件、ABI 正确、内存无泄漏、plans/lessons/更新日志同步 | 全量回归 | 全部 |

---

## 八、风险与应对

| 风险 | 应对 |
|------|------|
| 泛型函数 + 函数指针回调组合无先例 | 步骤5 先写最小 E2E 验证，失败则改「具体类型重载 + 宏生成」降级方案 |
| 变参/多类型 ABI（Win x64） | 新增输入/时间 API 均设计为非变参；格式化不动；复用 lessons 14.4 经验 |
| 中文路径文件 IO | 强制 `MultiByteToWideChar` + `_wfopen_s`（lessons 已验证方案） |
| 容器内存泄漏 | 全部 RAII 析构 + E2E 用 `分配`/`释放` 配对断言；单测覆盖析构路径 |
| 文件 ≤1000 行 | 容器.cn 若超限拆 `容器.向量.cn`/`容器.链表.cn` 子模块 |
| 新增内置五处同步遗漏 | 每新增一个内置函数，核对 6 处清单（语义/x64/arm64/E2E/单测/文档） |
| 新 API 命名与关键字冲突 | `结果/错误/某些/可选/自身/父类` 等不可作变量名（lessons 15.6/12.6） |

---

## 九、请批阅

本方案已将阶段6从「6 个零星任务」重构为「**4 个运行时新文件 + 11 个 CN 标准库模块 + 12 步实施计划**」，对标 C++ 标准库十大域，P0 覆盖常用核心 ~80%。

**待您批复的方向**：
1. 是否同意本方案的整体架构与 P0/P1/P2 优先级划分？
2. 是否按第七节 12 步实施计划执行？
3. 有无需要增删的模块（例如是否要求 P1 的复数/随机数提前到 P0）？

批复后我再派发子任务执行。