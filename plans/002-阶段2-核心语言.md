# CN语言编译器实施计划（阶段二：核心语言）

> 前序文档：[`plans/002-00-总览与约束.md`](plans/002-00-总览与约束.md)（目标/架构/技术栈/全局约束/执行策略）
> 前序文档：[`plans/002-阶段一D-后端与集成.md`](plans/002-阶段一D-后端与集成.md)（Task 1.7~1.10，本阶段消费其 X64 代码生成器与 driver 流水线）
> 后续文档：[`plans/002-阶段三-OOP与错误处理.md`](plans/002-阶段三-OOP与错误处理.md)（阶段三：OOP与错误处理）
> 基于设计规格书：[`plans/001 CN语言编译器设计规格书.md`](plans/001%20CN语言编译器设计规格书.md)

## 阶段二：核心语言

> 目标：完整控制流、函数完整支持、全部位宽类型、数组指针、字符串系统、结构体枚举、基础优化。里程碑：可编写基本算法与数据结构（规格书阶段二）。

### Task 2.1: 控制流（如果/否则/当/循环/选择/中断/继续） ✅ 已完成（2026-08-12，Task 2.1 控制流增量实施）
> **增量补全说明**：`如果/否则/当/循环/中断/继续` 在阶段一（Task 1.4/1.5/1.6）已实现；本 Task 增量补全 **`选择/情况/默认`（switch）** 语句并接入全链路。
>
> **已实现**：
> - AST：新增 `SwitchStmt`/`CaseLabel`/`DefaultLabel` 节点（[`ast.hpp`](src/cn_compiler/parser/ast.hpp)）
> - Parser：`parseSwitchStmt()` 支持 情况 整型/字符常量标签、默认分支（最多一个）、fallthrough、嵌套、错误恢复（[`parser.cpp`](src/cn_compiler/parser/parser.cpp)）
> - Semantic：条件表达式整型/字符检查、case 值去重、`中断` 在循环或 switch 内合法（`switchDepth_`）、switch 内 `继续` 报错（[`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp)）
> - IR：`genSwitch()` 级联条件跳转 CFG（Eq+endBranch，判断块→体块→汇合块），fallthrough 体块跳下一体块；修复 genIf/genWhile/genFor 标签重叠潜伏 bug；Phi 节点标注为 Task 2.6 优化器预留（[`ir.cpp`](src/cn_compiler/ir/ir.cpp)）
> - Codegen：零改动（复用现有 `Eq`+`条件跳转` 终止机制，cmp+jcc 序列自动生成）
> - AST 打印器：`cn ast` 支持 选择语句/情况标签/默认标签（[`ast_printer.cpp`](src/cn_compiler/parser/ast_printer.cpp)）
> - 测试：新增 27 个（`test_parser_control.cpp` 10 + `test_semantic_control.cpp` 11 + `test_ir_control.cpp` 6），**总计 208/208 通过**；E2E `02_control` 通过（2/2）
>
> **留给后续 Task**：Phi 节点（Task 2.6 优化器）、跳转表优化（规格书优化器-选择表生成）、枚举 case 值

**文件：**
- Modify: `src/cn_compiler/parser/parser.cpp`（解析如果/当/循环/选择语句）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（条件表达式布尔检查、循环嵌套合法性、switch 检查）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（条件跳转/基本块拆分/选择级联跳转，Phi 预留 Task 2.6）✅
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（cmp/jcc 指令复用，跳转表留给优化器）✅
- Modify: `src/cn_compiler/parser/ast.hpp`（SwitchStmt/CaseLabel/DefaultLabel 节点）✅
- Modify: `src/cn_compiler/parser/ast_printer.cpp`（选择语句打印）✅
- Create: `tests/unit/parser/test_parser_control.cpp` ✅
- Create: `tests/unit/semantic/test_semantic_control.cpp` ✅
- Create: `tests/unit/ir/test_ir_control.cpp` ✅
- Create: `tests/e2e/02_control/控制流.cn` + `控制流.expected` ✅

**验证标准：** E2E用例覆盖：`如果/否则` 分支、`当` 循环、`循环(初始化;条件;更新)`、`选择/情况/默认`、`中断`、`继续`；语义检查：`当` 条件必须为布尔，`中断/继续` 只能在循环内（switch 内 `中断` 合法）；IR正确生成条件跳转与基本块。✅ 208/208 单元测试 + E2E 2/2 通过。

### Task 2.2: 函数完整支持（原型声明/递归/函数指针）
**文件：**
- Modify: `src/cn_compiler/semantic/semantic.cpp`（两趟收集函数原型、递归调用检查、函数签名重载）
- Modify: `src/cn_compiler/ir/ir_generator.cpp`（函数调用指令完善）
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（call/ret指令、前4参数寄存器传递、>4参数栈传递）
- Create: `tests/unit/semantic/test_function.cpp`
- Create: `tests/e2e/03_function/函数.cn` + `函数.expected`

**验证标准：** 前向引用（函数原型声明后使用）、递归（阶乘）、函数指针（`整32(*回调)(整32, 整32)` 声明/赋值/调用，规格书5.8）。

> ✅ **Task 2.2 已完成（2026-08-12）**：原型声明（前向引用/签名一致性/链接期缺失检测）、递归（阶乘 E2E 通过）、函数指针（C 风格声明/赋值/调用/参数传递，`FuncAddr` + `CallIndirect` IR，`lea rax,符号` + `mov r11,指针; call r11` 汇编）；同时修复 Win x64 影子空间 bug（所有调用预留 32 字节）。**236/236 单元测试 + E2E 3/3 通过**。重载决策：规格书正文未给重载规则，仅保证单签名函数，重载与名字修饰签名编码留待后续 Task。

### Task 2.3: 类型系统完善（全部位宽类型/隐式转换） ✅ 已完成
**文件：**
- Create: `src/cn_compiler/semantic/type_system.hpp/.cpp`（类型系统子模块：别名规范化、隐式转换链、字面量后缀 f/L/LL/U/UL/ULL、位宽秩）
- Modify: `src/cn_compiler/ir/ir.hpp/.cpp`（新增 Cast/BitAnd/BitOr/BitXor/Shl/Shr Opcode；mapType 补全 u8~u128/i128/f32/f64；字面量后缀类型映射）
- Modify: `src/cn_compiler/parser/parser.cpp/.hpp`（Pratt 优先级链 10→13 级：新增移位/位与/位异或/位或层级；&取地址/*解引用 一元歧义处理）
- Modify: `src/cn_compiler/codegen/x64/x64_instructions.cpp`（8/16位 movsx/movzx、SSE 浮点 addsd/cvtsi2sd/cvttsd2si/ucomisd、转换指令、移位 shl/sar/shr、浮点常量 .data 段位模式、i128 低64位运算）
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（浮点常量池预扫描 @fpN）
- Modify: `src/cn_compiler/semantic/semantic.hpp/.cpp`（类型工具委托 type_system 子模块、字面量常量窄化、同秩跨符号允许）
- Create: `tests/unit/semantic/test_types_full.cpp`（12测试）
- Create: `tests/unit/ir/test_ir_types.cpp`（12测试）
- Create: `tests/unit/codegen/test_x64_types.cpp`（9测试）
- Create: `tests/e2e/04_types/类型.cn` + `类型.expected`

**验证标准：** ✅ 全部20个类型关键字正确识别；`整数`/`小数` 别名映射；字面量后缀解析；隐式转换链；位运算/移位（& | ^ ~ << >>）；SSE 浮点算术与转换；i128 加/减/比较（低64位，值域≤2^63）；单元测试 276/276 通过；E2E 4/4 通过。
**遗留：** ~~i128 乘/除/取余与全范围128位（>2^63）需运行时辅助函数（__cn_mul_i128 等，规格书10.5），留后续 Task。~~ ✅ **已修复（2026-08-13 完善A）**：`src/runtime/i128_api.cpp` 实现全部 128 位运算辅助函数（`__cn_mul_i128/u128`、`__cn_div_i128/u128`、`__cn_mod_i128/u128`、`__cn_cmp_i128/u128`、转换/打印）；IR/codegen 双寄存器模型 + adc/sbb 进位链 + 辅助函数调用；i128 字面量全范围解析（splitI128Text）与越界检查（整128 上限 2^127-1、正128 上限 2^128-1）。E2E 11_i128 通过 + -O0/-O1 一致。

### Task 2.4: 数组与指针
**文件：**
- Modify: `src/cn_compiler/parser/parser.cpp`（`类型[长度]` 数组类型、`[]` 下标、`&`取地址、`*`解引用、`->`成员访问）
- Modify: `src/cn_compiler/semantic/semantic.cpp`（数组边界检查、指针算术类型规则）
- Modify: `src/cn_compiler/ir/ir_generator.cpp`（加载/存储/取地址指令、指针偏移计算）
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（mov加载/存储、lea取地址、地址算术）
- Create: `tests/unit/semantic/test_pointer.cpp`
- Create: `tests/e2e/05_array_pointer/数组指针.cn` + `数组指针.expected`

**验证标准：** 数组声明/初始化/下标访问；指针声明/取地址/解引用；指针算术（p+1按元素大小偏移）；空指针 `无`；空指针解引用运行时错误码3（规格书附录B）。

> ✅ **Task 2.4 已完成（2026-08-12）**：数组（`类型[长度] 名`，初始化列表 `{...}` 部分补零、下标读写 + 编译期整型检查 + 运行期越界检查错误码2）、指针（`类型* 名`、取地址 `&`/解引用 `*` 读写、指针算术 `p+n` 按元素大小 ×8、空指针 `无` 解引用错误码3、`空类型*`↔任意指针、数组名→指针退化、指针比较）、`->` 仅语法解析（结构体字段语义留 Task 2.7，本次报"暂不支持"）。
>
> **实现要点**：AST 新增 `NullLiteral/IndexExpr/InitListExpr`；Parser 新增 `parseTypeNameEx()`（`类型*`/`类型[N]` 组合后缀）；类型系统递归 canonical + isPointer/isArray/pointeeOf/arrayElemOf/arrayLenOf/typeSize；IR 新增 `AddrOf/LoadPtr/StorePtr` 三指令 + `emitBoundsCheck()` 越界插桩（错误码2）+ `lvalueAddress()` 左值地址统一；codegen `lea`/`mov` 地址访存 + 空指针检查（错误码3）；runtime 新增 `__cn_runtime_error()`（错误码2/3 打印并退出）。
>
> **栈帧设计（坑已解决）**：数组在栈上按 C 语义向上增长——元素 i 地址 = 基址 + i×8；codegen **逆序登记**（元素 N-1 先、基址槽最深），与指针算术（Add 方向）一致，保证 `&数组[0]` 基址上 `p+1` 指向下一元素、指针比较方向正确。
>
> **327/327 单元测试 + E2E 5/5 通过**。错误码验证：越界→`运行时错误(错误码2): 数组越界`；空指针解引用→`运行时错误(错误码3): 空指针解引用`。

### Task 2.5: 字符串系统 ✅（2026-08-12 完成）
**文件：**
- Create: `src/runtime/string_api.cpp`（字符串API：长度/比较/连接/复制/查找 + 打印行多参数辅助，规格书10.1）
- Modify: `src/runtime/runtime.hpp`（字符串API声明：`__cn_str_len/__cn_str_eq/__cn_str_concat/__cn_str_copy/__cn_str_find` + `__cn_print_str/int/float/newline`）
- Modify: `src/cn_compiler/semantic/semantic.cpp`（`字符串`类型映射、字符串↔字符*双向转换、`+`连接类型检查、registerBuiltins 字符串API+打印行变参）
- Modify: `src/cn_compiler/semantic/type_system.cpp`（canConvert 支持 `字符串`↔`字符*`，规格书3.7）
- Modify: `src/cn_compiler/ir/ir.cpp`（decodeString 完整实现：普通/原始/多行/原始多行 前缀剥离+转义解码+UTF-8；`+`连接→`__cn_str_concat`调用；字符串API中文名→运行时符号映射；打印行多参数展开）
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（`__cn_`前缀符号原样返回，EXTERN自动扫描）
- Modify: `src/cn_main.cpp`（compileRuntime/linkExe 加 string_api.obj）
- Create: `tests/unit/lexer/test_lexer_string.cpp`（14个测试）
- Create: `tests/unit/semantic/test_string_semantic.cpp`（14个测试）
- Create: `tests/unit/runtime/test_string_api.cpp`（21个测试）
- Create: `tests/unit/ir/test_ir_string.cpp`（14个测试）
- Create: `tests/e2e/06_string/字符串.cn` + `字符串.expected`

**验证标准：** ✅ 字符串字面量4种形态（普通/原始/多行/原始多行）；字符串连接 `+`（运行时动态分配）；`打印行` 多参数格式化（IR展开为打印序列）；运行时字符串API（长度/比较/连接/复制/查找）正确。**390/390 单元测试 + E2E 6/6 通过，cn build 冒烟输出与 .expected 一致。**

> **设计决策**：
> - `字符串` 保持独立类型（规格书3.4），不合并为 `字符*` 别名；canConvert 支持双向隐式转换（规格书3.7）
> - `+` 连接内存语义：调用 `__cn_str_concat` 动态分配（规格书10.3 "字符串连接等操作调用运行时API完成"），调用方负责释放
> - `打印行` 多参数：IR 层按参数类型展开为 `__cn_print_str/int/float` + `__cn_print_newline` 序列（避免变参ABI复杂性）；单参数字符串保持 printLine 原路径
> - 字符串长度：UTF-8 字节数（同 C strlen，规格书10.3）

### Task 2.8: 字符串系统完善（完善B） ✅ 2026-08-13
**文件：**
- Modify: `src/runtime/string_api.cpp` / `runtime.hpp`（新增13个API：子串/字典序/大写/小写/前缀/后缀/包含/修剪/反转/从整数/从浮点/从字符/释放）✅
- Modify: `src/cn_compiler/semantic/semantic.cpp`（registerBuiltins 注册新API，规格书10.1 标注"常见字符串库补充"）✅
- Modify: `src/cn_compiler/ir/ir.cpp`（中文名→运行时符号映射 + resultType 映射；字符串释放 空类型用 emit）✅
- Create: `tests/unit/runtime/test_string_api_extra.cpp` / `tests/unit/semantic/test_string_extra.cpp` / `tests/unit/ir/test_ir_string_extra.cpp`（51个测试）✅
- Create: `tests/e2e/12_string_extra/字符串完善.cn` + `字符串完善.expected`（子串/字典序/大小写/前后缀/包含/修剪/反转/数字转换/字符转换/释放/字符串数组/空字符串/值语义）✅
- Modify: `tests/unit/CMakeLists.txt`（注册3个新测试文件）✅
- Modify: `plans/001 CN语言编译器设计规格书.md`（10.1 补充API标注；10.3 值语义/比较语义决策标注）✅

**验证标准：** ✅ 新增13个运行时字符串API真实运行；字符串数组/空字符串/值语义（共享指针+显式深拷贝）验证；比较运算符按规格书未定义字符串变体→不实现，等价能力由 字符串比较/字符串字典序 提供。**600/600 单元测试 + E2E 12/12 通过，-O0/-O1 输出一致，编译零警告。**

> **设计决策**：
> - 规格书10.1 仅定义 长度/比较/连接/复制/查找 5 个字符串API；按任务指示"按常见字符串库补充并在规格书标注"新增13个
> - 字符串比较运算符（==/!=/</>/<=/>=）：规格书运算符表仅整型与浮点变体→不实现；`字符串字典序` 提供全序比较（<0/0/>0）
> - 值语义：字符串变量为指针，赋值共享（与C一致，不深拷贝）；显式深拷贝用 字符串复制；动态分配结果调用方负责 字符串释放
> - 格式化（sprintf风格）：变参ABI复杂暂缓（打印行多参数已覆盖格式化输出需求），后续 Task 标准库阶段

### Task 2.6: 优化器基础（常量折叠）
**文件：**
- Create: `src/cn_compiler/opt/pass.hpp`（Pass统一接口：`bool 运行(ir::IR模块&)`，规格书9.3）✅
- Create: `src/cn_compiler/opt/pass_manager.hpp/cpp`（Pass管理器：按依赖顺序调度至收敛，fixpoint + 上限轮次）✅
- Create: `src/cn_compiler/opt/const_fold.hpp/cpp`（常量折叠Pass：块内常量跟踪+传播+折叠）✅
- Create: `src/cn_compiler/opt/dce.hpp/cpp`（死代码消除Pass：函数级引用分析+级联收敛）✅
- Create: `tests/unit/opt/test_const_fold.cpp` / `test_dce.cpp` / `test_pass_manager.cpp` ✅
- Create: `tests/e2e/08_opt/优化.cn` + `优化.expected`（-O1 常量表达式 + 死代码用例）✅
- Modify: `src/cn_main.cpp`（-O0/-O1/-O2/-O3 优化级别分发，规格书9.1；保留 --opt 兼容）✅
- Modify: `src/cn_compiler/driver/driver.cpp`（IR 生成后、codegen 前接入 Pass 流水线）✅
- Modify: `CMakeLists.txt`（cn_compiler_opt 静态库）/ `tests/unit/CMakeLists.txt`（注册 opt 测试）✅

**验证标准：** `-O1` 下常量表达式编译期计算；`-O0` 与 `-O1` 输出结果一致（规格书12.4优化正确性测试）。

> ✅ **Task 2.6 已完成（2026-08-12）**：Pass 框架（`opt/pass.hpp` 抽象基类 `Pass::run(IRModule&)->bool`；`opt/pass_manager.hpp/cpp` 注册+fixpoint 调度+上限轮次防死循环）；常量折叠（块内 regId->常量 跟踪表 + 常量传播 + 折叠，覆盖整型算术/位运算/移位/比较/逻辑/Cast/浮点，除零不折叠，i128/浮点不传播）；DCE（函数级引用收集 + 反向扫描 + 级联收敛，副作用指令/ConstString/Alloca 永不删除）；CLI `-O0/-O1/-O2/-O3`（-O2/-O3 暂映射 -O1）+ driver 流水线接入。**488/488 单元测试（原433+新55）+ E2E 8/8（新增 08_opt）+ 01~08 全部 -O0/-O1 输出一致**（规格书12.4）。

> ✅ **优化器增强（完善C）已完成（2026-08-13）**：5 个新 Pass——① 代数简化（`opt/algebraic_simplify.cpp`：x+0/0+x/x-0/x*1/1*x/x*0/x/1/x<<0/x>>0/x|0/x^0/x&-1 恒等变换 + x-x/x^x->0 + x&&假/x||真 折叠；仅整型，浮点（+0/-0/NaN）/i128（双槽）跳过；常量结果原地替换，寄存器结果登记函数级替换链）；② 复写传播（`opt/copy_propagation.cpp`：块内 Store->Load 转发，别名按唯一内部名精确匹配，多槽/StorePtr/Call 保守清空）；③ 块内 CSE（`opt/cse.cpp`：纯运算模式哈希复用 + Load 复用，Store/Call 失效，浮点 CSE 仅 -O2 以上）；④ 跨块 DCE 增强（`opt/cross_block_dce.cpp`：常量条件跳转折叠 + 入口可达性分析 + 不可达块整块删除，label 字符串互引无需修正索引）；⑤ 全局值传播（`opt/global_value.cpp`：函数级线性扫描常量 Store->Load 安全子集，非常量重写/Call 清空）。级别区分：**-O1**=折叠+DCE+代数简化+复写传播；**-O2**=+CSE（含浮点）+跨块DCE；**-O3**=+全局值传播；driver `runOptLevel` 接入，`-O2/-O3` 不再映射 -O1。**656/656 单元测试（原600+新56）+ E2E 13/13（新增 13_opt2）+ 01~13 全部 -O0/-O1/-O2/-O3 四级别输出一致（52 组合，规格书12.4）+ 编译零警告（MSVC /W4 /WX）**。

### Task 2.7: 结构体与枚举
**文件：**
- Modify: `src/cn_compiler/parser/parser.cpp`（`结构体 名 { 类型 字段 }`、`枚举 名 { 红, 绿, 自定义 = 100 }`，规格书5.7）
- Modify: `src/cn_compiler/semantic/semantic.cpp`（结构体布局计算、字段访问检查、枚举常量符号表、负数枚举）
- Modify: `src/cn_compiler/ir/ir_generator.cpp`（字段加载/字段存储、结构体取地址）
- Modify: `src/cn_compiler/codegen/x64/x64_codegen.cpp`（结构体栈分配、字段偏移寻址）
- Create: `tests/unit/semantic/test_struct_enum.cpp`
- Create: `tests/e2e/07_struct_enum/结构体枚举.cn` + `结构体枚举.expected`

**验证标准：** 结构体定义/实例化/字段访问；枚举定义（含赋值与负数）/使用；联合体（所有字段共享内存，规格书3.5）。

> ✅ **Task 2.7 已完成（2026-08-12）**：结构体定义/实例化（`类型名{ 字段 = 值 }` 命名式初始化）/字段访问（`.` 与 `->`，后者含空指针检查错误码3）/嵌套结构体/结构体数组（按结构体大小步进）；枚举定义（自动递增/显式赋值/负数）/引用（`枚举名.成员`）/选择语句 case 枚举值；联合体新关键字（53→54，所有字段共享内存，大小=最大字段）。布局 C 风格对齐（字段按类型对齐、总大小对齐到最大对齐）；IR 新增 FieldAddr Opcode（基址+偏移）；canConvertType 支持枚举↔整数。433/433 单元测试 + E2E 7/7 通过。

> ✅ **阶段2全链路集成验证已完成（2026-08-13）**：新增 `tests/e2e/09_integration/综合集成.cn`（学生成绩管理系统，一个程序串联阶段2全部7个Task功能：结构体数组初始化列表+指针算术遍历+函数指针回调排序+递归浮点返回+枚举负数+联合体+字符串API+位宽类型+常量折叠）。集成验证发现并修复 9 个跨组件 bug（详见 更新日志.md）：① 结构体数组初始化列表只写占位0；② 结构体数组栈槽数不足（长度×元素槽数）；③ 结构体指针算术/`p[i]`/`p++` 固定8字节步进（新增 ptrElemStride 按 typeSizeOf）；④ 结构体/枚举作函数参数解析失败；⑤ 函数指针参数含结构体指针解析失败；⑥ 数组名退化缺失；⑦ 长字符串 db 单行超长 A2042+尾逗号 A2008；⑧ 用户函数返回 f64 误标 i32（除零崩溃）；⑨ 空指针常量0存32位判空失败。新增单元测试 `tests/unit/parser/test_parser_struct_param.cpp`（+4）。**492/492 单元测试 + E2E 9/9 + CLI 全命令（token/ast/check/ir/build -O0/-O1/run）+ -O0/-O1 输出一致**。

> ✅ **阶段2 Debug 全面审查已完成（2026-08-13）**：按生产环境模拟运行（10 个审查用例 + 全部 E2E 01~09 + 单测 492/492）发现并修复 **10 个 BUG**（详见 更新日志.md / lessons.md，综合教训权重 22.75）：① 除零未插桩（错误码1）→ Windows 0xC0000094 崩溃而非 CN 错误码；② idiv 立即数 A2001；③ 无符号除法用 idiv+有符号扩展（正32 4000000000/2 错算 -295M）；④ 无符号比较用 setg/setl（正32 4294967295>1 判假）；⑤ u32 LoadPtr movsxd 符号扩展；⑥ 浮点参数经 rcx 读（caller 用 xmm0-3 → callee 读到垃圾）；⑦ 浮点返回只 mov rax 未设 xmm0；⑧ const_fold Call 尾部实参传播浮点常量 → `movsd xmm2, qword ptr 4.0` A2050；⑨ const_fold 无符号除/比较用有符号语义（-O1 与运行期不一致）；⑩ 结构体数组字段（方形.顶点[0].x / 指针->顶点[1].x）把数组字段当值 LoadPtr（垃圾指针→空指针错误/访问冲突），且数组字段越界不插桩。修复涉及 `emitDivMod`（除零检查+无符号 div）/`emitParamSetup`（浮点参数从 xmmN 读）/`currentReturnType_`（浮点返回 movsd xmm0）/`ptrElemStride`（结构体数组字段步进）/`memberObjStructType`（arrow 剥指针+数组字段退化）/`funcReturnTypeOf`（调用结果类型查真实返回类型）等。**508/508 单元测试 + E2E 9/9 + -O0/-O1 输出一致 + 编译零警告（MSVC /W4 /WX）**。

**阶段二完成条件：** ✅ 全部单元测试通过（508/508）+ E2E用例 01~09 通过 + 更新 `plans/002` 阶段二打勾 + Git提交。
