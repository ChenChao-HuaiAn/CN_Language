# 021 任务进度观察表（全项目唯一总入口）

> **定位**：全项目「任务与进度」的**唯一总入口**。新会话接手第一步：读本表 + `HANDOFF.md`；
> 需要细节再进对应专项文档（导航见 §6）。
>
> **更新纪律**：每轮收尾随文档五件套（plans/019 跟踪表 + plans/020 全节 + lessons + 更新日志 +
> HANDOFF）**一并更新本表**——勾销已完成项、调整排班队列、追加轮次里程碑。本表只做汇总与导航，
> 不承载实施细节。
>
> **建立**：2026-09-13（第九十轮起点）。建立原因：此前进度散在 20 份 plans 文档与 HANDOFF 中，
> 且部分旧文档状态过时（002 索引/007 清单停在上古阶段、019 阶段总表部分未刷新），接手会话缺乏
> 统一观察入口。
>
> **口径基准**：截至 2026-09-13 第九十轮起点（89-a 收口后、下一轮开工前）。

---

## 一、总目标（北极星，不随轮次变化）

1. **终极目标**：全中文语法的系统级编程语言编译器（C++ 风格），从零用 C++17 实现，
   终极目标**真正自举**。全流水线：词法 → 语法 → 语义 → IR → 优化 → 代码生成。
2. **v2 自举双目标（2026-09-01 用户令，最高准绳）**：
   - ① **性能超过 C++ 编译器**；
   - ② **安全接近 Rust 语言**（悬垂指针 / 越界访问 / 泄漏，零容忍）；
   - 二者冲突时向用户呈报裁决。
3. **三后端**：Win x64（MASM）/ Linux ARM64（GAS）/ Linux x86_64（SysV GAS）。
4. **总任务 = 三条主线 + 支撑**（互相咬合，全部服务于双目标）：
   - **线 1｜编译器能力**：v1 推倒 → v2 自举重建（结构化 IR + arena + 符号驻留），三后端，模块系统；
   - **线 2｜编译期安全**（plans/019）：显式转移 / 引用逃逸 / 只读借用与互斥 / 不安全区边界 /
     拥有型字符串与智能指针——对标 Rust 检查器，把 UAF、双释放、泄漏**结构性挡在编译期**；
   - **线 3｜内存安全防线**（plans/020）：拥有权流转矩阵 + 每轮探针实证 / E2E 门禁 / 反证三件
     ——运行时与语义面的零容忍兜底；
   - **支撑**：缺陷根治滚动轮（a 后缀轮次）、跨机轮（win-x64 家机 / arm64 单位机实物验证）、
     测试门禁（单测 + E2E + 锚定链 + 对拍 + 释放路径对照）。

---

## 二、当前进度总览（截至 2026-09-13，第九十轮起点）

| 战线 | 状态 | 关键证据 | 权威文档 |
|---|---|---|---|
| v1 宿主编译器（`src/cn_compiler`） | ✅ 完成（阶段 0~7 全完成） | v1 自举闭环 E2E 79；当前仍为**参照基准与 v2p 编译工具** | plans/002/005 |
| v1 自举组件（`CN语言编译器/`） | ⛔ 已推倒（字符串行 IR 内存失控）→ 由 v2 替代 | E2E 78/79 在 linux 平台跳（v1 仅 MASM 后端） | AGENTS.md / plans/014 |
| **v2 自举重建（`CN语言编译器v2/`）** | ✅ **已到自编译固定点** | 锚定链 **338025 行**，fix_p ≡ fix_s（md5 `d063b698…`）两代逐字节一致 | plans/013/014/017 |
| 模块系统 P6b（plans/018） | ✅ 完成（25/26 勾选，剩 1 项核对项） | v2 加载器全链 + E2E 164/165 | plans/018 |
| 编译期安全线（plans/019） | 🔄 阶段 1/2/3/3b 完成；阶段 4 前半完成；4' 方案A 主体落地 | 各阶段 E2E 双通道 + 反证；详见 §3/§5 | plans/019 |
| 内存安全防线线（plans/020） | 🔄 滚动推进（73-a ~ 89-a，十七轮） | 各轮 E2E 门禁 + 反证 + 矩阵台账 | plans/020 |
| 测试门禁（当前口径） | ✅ 全绿 | 单测 **1306/1306**；E2E **276 用例 272 过 / 0 失败 / 0 未实现 / 4 跳**；对拍 **34/34**（DIFF=0）；释放路径两侧对照 | HANDOFF §4 |

> **4 跳口径**（平台限制，非缺陷）：`62_ffi`（依赖 Windows API）/ `69_memory_management`（运行时计数
> 平台差异）/ `78_chain_build`（v1 链仅 MASM）/ `79_bootstrap_closed_loop`（依赖 ml64/link）。
> **门禁族用例**：213~249（探针转正的防线用例；249 为最新，双编译对照第十八个）。

---

## 三、排班队列（未完成任务，按状态分组）

> 说明：这是全项目**活的待办队列**。A/B 为下一轮候选；C 为已定方案待轮次；D 为长期登记；
> E 为外部依赖；F 为 v1 时代长线清单（v2 时代待重评）。**每轮更新时增减**。

### A. 立即（下一轮起，已明确）

| # | 任务 | 内容 | 状态 |
|---|---|---|---|
| A1 | **跨机轮（家机 win-x64）** | 89-a 浮点发射 win 侧实物验证（`REAL8`/`ALIGN 8`/`floatData_` 装载三件 + E2E 249 + 246）+ 88-a（短路）/87-a（静态）遗留实物 + 全量门禁复验 | 待执行 |
| A2 | **跨机轮（单位机 arm64）** | linux-arm64 后端复验（89-a `.align 3`/`.double`/`adrp+ldr` 浮点装载 + 88-a/87-a 遗留） | 待执行 |
| A3 | **v2 浮点语义边界专项轮** | 面内=字面量装载 + 相等/不等（按位）；面外=算术（+ - * /）/大小比较/负号/打印——**需先呈报用户裁决「面收窄 vs 补全」** | 呈报前置 |

### B. 待呈报 / 待用户裁决

| # | 任务 | 来源 / 说明 |
|---|---|---|
| B1 | v2 浮点面「收窄 vs 补全」 | 89-a 登记（与 A3 同一专项；补全=IR 浮点操作码族 + 三后端 sd/ss 指令 + 语义层分派） |
| B2 | **类静态成员 + 析构写静态成员运行期崩溃** | plans/019 立案（tp3 探针：无转移纯两对象构造 + 析构即崩，独立既有缺陷）📌 待呈报 |
| B3 | B35 字段容器接收者 | plans/020 登记（待用户裁决） |
| B4 | 甲（参数 move）/ 丙（返回位拒绝） | plans/020 登记（语言级语义裁决项） |
| B5 | 阶段 4 第二层**第三批收口时点** | 收口变硬错误 + 删观察期；建议真实使用反馈期后（前提=迁移已全量完成） |

### C. 已定方案 · 排程中

| # | 任务 | 内容 |
|---|---|---|
| C1 | 阶段 4' **第三批**（A2 正文余面） | 裸指针形态显式化 / `字符*` 借用视图类型化 ⬜ |
| C2 | **跨语句 NLL 活跃区间**（3b 剩余） | 等真实使用反馈期数据，按误报率实测定深度 ⬜ 维持排程 |
| C3 | v2 静态字符语法 | `静态 字符 x = 'Z';` 语法层不识别（既有限制）待评估 |
| C4 | 多文件静态初始化注入 | 仅入口模块（含 主）注入 → 需「模块初始化函数」机制 |
| C5 | v2 打印浮点恒 `0.000000` | 与 A3 合并评估（打印需走浮点格式化） |

### D. 长期整改 / 维持登记

| # | 任务 | 说明 |
|---|---|---|
| D1 | **行数存量违规面** | 宿主 14 文件超千行、19 函数超百行；v2 3 文件（新文件均达标，存量长期整改） |
| D2 | v2 诊断无行号 | 维持登记（诊断质量提升项） |
| D3 | 未初始化槽 preFree 观察项 | 84-f 核验：误释放需三重条件，低风险（加固方向=ptr::write 式新槽写入挂点） |
| D4 | 静态槽不登记自动释放 | 进程生命周期（Rust `static` 同款，设计选择非缺陷） |
| D5 | v2 结果/可选 返回三元 | 84-e 边界（v2 值字段未登记资源字段） |

### E. 外部依赖

| # | 任务 | 说明 |
|---|---|---|
| E1 | **阶段 5 并发安全（Send/Sync 对标）** | ⏸ 等待并发标准库立项（plans/003 批次 C 重大缺口）——非本专项可独立推进 |

### F. 长线生态（v1 时代清单，v2 时代待重评/重排）

| # | 任务 | 来源 |
|---|---|---|
| F1 | P4 优化器族：SSA Phi 接线 / 全局值传播 / arm64 寄存器分配 / pass 边界放开 / DWARF+增量编译+ASan | plans/007 #26~#30 |
| F2 | P6 生态族：标准库扩展（迭代器/正则/线程/网络/随机数）/ 包管理 / 工具链（LSP/格式化/文档站）/ ARM64 原生自举验证 / 规范精化 | plans/007 #33~#37 |

> F 组为 2026-08-19 v1 时代清单，v2 时代尚未排期；其中部分条目（如自举相关 #31/#32）已由 v2
> 重建以另一路径承载。**下次规划轮需逐条重评后销项或重排。**

---

## 四、已完成里程碑（大事件线）

| 时间 | 里程碑 |
|---|---|
| ~2026-08-17 | v1 编译器阶段 0~7 全部完成；**v1 真实自举闭环**（E2E 79） |
| 2026-08-18~19 | P0~P3 收尾（负 e2e 机制、引用返回、接口 C2、实例方法作值、去虚拟化 + CFI；单测 1196） |
| 2026-08-25 | **v2 自举重建立项**（宿主缺陷根治 + P4 重做；排序修正=宿主缺陷优先） |
| 2026-09-03~07 | v2 组件持续推进（内置函数五波、字符串/指针下标、三元、IO::/文件:: 限定名）；plans/016/017 专项：**v2 LinuxX64 代码生成** + v2self 首次完整运行；重载消歧；模块系统 P6b |
| 2026-09-10 | **编译期安全专项启动**（plans/019）：阶段 1「显式转移 转移()」、2「引用逃逸」、3「常量只读借用 + 互斥」双侧落地 |
| 2026-09-10~11 | 3b（浅转移优化 / v2 借用面对齐）；阶段 4 不安全区（第一层观察期 + 第二层两批迁移，警告 175→0）；阶段 4' 拥有型字符串（一批/二批/A1/A2） |
| 2026-09-11~12 | **内存安全防线连续十七轮**（plans/020）：73-a 块级 RAII → 89-a 净栈契约（详见 §5） |

---

## 五、近期轮次索引（73-a ~ 89-a，细节见 plans/020 对应节）

| 轮次 | 主题 | 门禁 |
|---|---|---|
| 73-a | v2 块级作用域 RAII 失效根治（+跳出 drop-on-jump；「释放+清零」幂等模型） | E2E 216_v2 |
| 74-a | 容器元素所有权全链归一化（五形态：借用句柄/裸标识符/转移/释放对称） | E2E 218/219 |
| 75-a | 诚实边界清理三件套（v2 真 move 三重判定 + 污染名单镜像 + 映射串释放） | E2E 218/219 |
| 76-a | 移除-覆盖-清空路径所有权全链补齐（八枚缺陷一次修全） | E2E 220/221 |
| 77-a | A21 借出视图生命周期检查（+扩展①②：跨函数别名调用点互斥 / 成员链容器引用键） | E2E 222/222_v2 |
| 78-a | v2 用户自定义类支持（节点_类/方法协议，新增 4 文件） | E2E 223/223_v2 |
| 79-a | 结构体/装箱拥有型字符串字段 drop glue 三件套 | E2E 224/224_v2 |
| 80-a | v2 池索引 0 哨兵槽（内置构造器识别缺陷根治） | E2E 225/226 |
| 81-a | 容器元素 = 含拥有型串字段结构体 | — |
| 82-a | v2 帧尾共享 retbuf 修复（返回物化到独立槽=LLVM sret 语义） | E2E 236 |
| 83-a | 链表/队列槽分配与回收（空闲链） | — |
| 84-a~g | 结构体字面量族（作实参 / 字段校验 E0062-E0063 / 嵌套 / 成员链深度 / 串释放面 / preFree 核验） | E2E 239/240/241 |
| 85-a | 聚合返回位所有权（借用来源返回深拷归一化；借出视图豁免） | E2E 242 |
| 86-a | 复审三缺陷根治（三元×聚合 / v2 深拷守卫顺序 / 豁免类型约束）+ 两处既有缺陷 | E2E 243/244/245 |
| 87-a | 静态变量全链路根治（槽尺寸/初值三类分派/读取地址语义/写入自带 drop） | E2E 246/247 |
| 88-a | v2 实参位短路栈失衡根治（净栈契约归位 + 语法层左起点分层） | E2E 248 |
| 89-a | **净栈契约出口校验落地 + v2 浮点字面量全链根治**（探针两阶段零误伤 → 校验正式化） | E2E 249 |

---

## 六、文档导航（哪里看什么）

| 想知道什么 | 去哪里看 |
|---|---|
| **总进度 / 排班队列（本表）** | `plans/021`（本文件）——每轮更新 |
| 本轮做了什么 / 下一轮做什么 / 验证链命令 / 诚实边界 | `HANDOFF.md`（每轮必更，**最新最权威**） |
| 编译期安全各阶段（转移/逃逸/借用/不安全/拥有型） | `plans/019`（§3 阶段路径总表 + §7 执行状态跟踪表） |
| 内存安全 / 拥有权流转 / 各轮防线与矩阵 | `plans/020`（矩阵 §1 + 各轮全节 + 登记项） |
| 语法规范（唯一权威） | `plans/001` + `cn-language-spec` 技能 |
| v2 架构设计 | `plans/013` |
| v2 推进史 / 宿主缺陷史 / 轮次状态表 | `plans/014`（**注意**：§三/§五 为早期规划口径，已被 019/020 取代） |
| 宿主 LinuxX64 后端 / v2 LinuxX64 代码生成 | `plans/016` / `plans/017` |
| 模块系统 P6b | `plans/018` |
| 教训 / 预防条目 | `lessons.md`（按权重排序） |
| 最近提交说明 | `更新日志.md` |
| 历史长线清单（v1 时代） | `plans/007`（未完成任务清单，**已过时**，待重评） |
