# 007 未完成任务清单（截至 2026-08-19）

> 定位：当前唯一权威的「剩余工作清单」，与 plans/005（总路线 P0~P6）、006（P3-19 设计）、002（实施阶段）联动。
> 已完成：P0（收尾/文档/负e2e机制）、P1（补测）、P2（11~17 编译器健壮性）、P3（18~25 语言表达力，八项全收官）。
> 质量闸门当前：构建 0 警告、单测 1190/1190、E2E 103/103（含 79 自举闭环）。

---

## 一、P4 优化器与后端接线（中长线，按 pass 独立推进）

| # | 任务 | 现状/缺口 | 验收口径 |
|---|------|-----------|----------|
| 26 | SSA Phi 真正接线 | 现无汇编阶段（pass_manager.cpp:78 / x64_instructions.cpp:1696 预留未接） | IR 生成 Phi、codegen 落地；相关 e2e 性能/正确性不劣化 |
| 27 | 全局值传播改为真实数据流 | global_value.cpp:9 现非精确 | 语义等价；性能基线不劣化（scripts/bench_self_host.py） |
| 28 | arm64 寄存器分配开启 | backend_factory.cpp:39 恒关闭未接线 | arm64 后端寄存器分配启用并通过既有后端用例 |
| 29 | 各 pass 保守边界放开 | i128 折叠、浮点简化、有符号除法强度削减等 | 逐 pass 独立开启 + 全量回归 + 性能卡点 |
| 30 | 调试与工具链 | DWARF 仅行号（debug_info.cpp:8，--debug 注释）；增量编译（004 D-3）；Sanitizer/ASan（004:60-61） | 完整 DWARF / 增量编译 / ASan 接入验证 |

## 二、P5 自举组件扩大（与 P3/P4 联动）

| # | 任务 | 现状/缺口 | 验收口径 |
|---|------|-----------|----------|
| 31 | 自举 IR生成/代码生成 通用化 | 目前仅硬编码 向量$字符串 容器方法，不支持通用用户 OOP/虚表（IR生成.cn:895-918、代码生成.cn:10） | 随 P3 的 OOP/泛型扩展同步扩自举子模块；79 闭环保持 |
| 32 | 自举与 C++ 版功能对齐持续回归 | 79 真实闭环（CN 产物 vs C++ 产物逐字节一致）为最高闸口 | 每轮变更 79 闭环绿；性能基线 scripts/bench_self_host.py 卡点 |

## 三、P6 生态与远期（批次 C 及以后）

| # | 任务 | 现状/缺口 | 验收口径 |
|---|------|-----------|----------|
| 33 | 标准库扩展 | 迭代器、正则、线程、网络、随机数；FFI（EXTERN 已有基础）增强 | 每模块 ≥1 e2e；线程/网络在 win-x64 可跑 |
| 34 | 包管理完善 | 货舱.toml 本地依赖已支持 → 补版本约束解析（build script/features）、依赖缓存、离线构建、包注册中心 | 多包依赖解析 + 离线构建验证 |
| 35 | 工具链 | LSP（补全/跳转/诊断）、代码格式化、文档站/在线 Playground、语法高亮 | LSP 基本补全/诊断可用 |
| 36 | Linux ARM64 原生自举验证 | 后端已支持，需主机环境验证 | arm64 上 CN 自举闭环 + 性能 ≈1.04x |
| 37 | 规范精化与设计决策库 | spec×实现×测试逐条对账；核对字符串 UTF-8 裸指针/调用方释放等约定是否写入规范 | 可追溯设计决策库形成；P1/P2 类「规范有、验证缺」清零 |

## 四、P3 收尾增量（已明确「待做」的子项）

| 来源 | 任务 | 现状/缺口 | 验收口径 |
|------|------|-----------|----------|
| 005 P3-18 | 引用返回类型（函数 获取() -> 整32&） | **已完成（007-P3）：**e2e 102 正 / 103 负；语义 isRefReturn + 返回局部报错；调用方可绑定/写回/取址 | 引用返回可用 + 负向（返回局部变量地址报错）✅ |
| 005 P3-23 | 实例方法作值（对象.方法 作函数指针） | **已完成（007-P3）：**绑定 this 闭包（闭包变量调用 e2e 104）；D2 直传裸 fnptr 报错（e2e 107） | 设计：复用 lambda 闭包/捕获机制；实例方法回调可用 ✅ |
| 006 P3-19 C2 | 接口类型作为函数参数/返回（多态传参） | 现仅局部/顶层变量 + 指针 + 值形式；函数参数按接口传递未做 | 函数 用形状(形状* 图形) 可用 + 多实现类经参数多态 |
| 006 性能路线 | 接口调用去虚拟化（devirtualize）+ CFI | 单一实现接口/最终类(sealed)的接口调用可在编译期内联；分派目标表已知可做 CFI 校验 | 去虚拟化：热路径接口调用内联；CFI：目标表约束 |

## 五、工程与质量闸门补齐

| 任务 | 现状 | 验收口径 |
|------|------|----------|
| 负向规则承载覆盖 | run_e2e.py 负 e2e 已落地（P0-3）；但规范中「应编译失败」规则需逐条核对锚点 | 规范 06/07/08/09/01a 每条负向规则有负 e2e 或单测锚点 |
| 文档四件套同提交 | plans/002 索引+子文件、HANDOFF、README、lessons 须与代码同提交 | 每次提交含四件套同步，杜绝漂移 |
| e2e 编号空缺补齐 | 53/54 空缺（52→55） | 新用例顺延编号，保持目录名=编号约定 |

## 六、近 6 周建议执行节奏（承接 005）

| 周期 | 内容 | 验收 |
|------|------|------|
| W1 | P3 收尾增量（引用返回/接口参数 C2/实例方法作值设计） | 每项 ≥1 e2e；全量回归绿 |
| W2~W4 | P4 按 pass 独立推进（26 先 SSA Phi 接线，再 27/29） | 每 pass 独立开关 + 全量回归 + 性能卡点 |
| W4~W6 | P5 自举通用化（随 P3 OOP/泛型同步扩 IR生成/代码生成） | 79 闭环保持、自举子模块覆盖 OOP |
| W6+ | P6 生态逐项（33 标准库 → 34 包管理 → 35 工具链 → 36 arm64 → 37 规范精化） | 每项 ≥1 e2e/验证 |

---

> 说明：优先级建议 = 四（P3 收尾增量，快、复用既有机制）→ 一（P4，收益大、风险独立）→ 二（P5，随 P4 联动）→ 三（P6，生态远期）。
> 每项开工按 005「标准任务卡模板」：规范依据 → 缺口证据 → 失败用例 → 最小修复 → 全量回归 → 文档四件套。