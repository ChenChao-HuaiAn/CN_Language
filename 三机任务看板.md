# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协同协议 v2」（2026-09-18 起·方案全文=plans/027）；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

> **结构（2026-09-18·332-a 变更）**：**每机独立小节**（不再单表三行相邻）——各机**只编辑自己
> 小节**（及自己通告段）；小节间 4+ 行间隔使 git 三方合并不再因相邻行改动冲突（332-a 实测：
> 单表三行结构下两机同时更新各自行 rebase **必冲突**·当日两次）。

### 家机 win-x64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/家机-468-九链重测集成（447 分支 rebase 最新 develop 后重测·深度机 448-c 已处置 112_v2 红+dbg 全清〔门禁 465=463/0/2〕·解锁条件达成·九链 C20 批1/2/3+T65~T69+446 最后一跳） | 7d71281b | 09-20 |
| 🏃 | 任务/家机-452-C20批5（负测面重扫·零 src 零 v2 树零 runner·深度机已回签等集成）；任务/家机-456-D1拆分handleClassCallExpr（D1 滚动：ir_oop_call.cpp 拆分·纯机械重构零行为变更·深度机已回签+asm 反证一致） | ad027758 | 09-20 |
| 🏃 | 任务/家机-471-D25验证器语料 已收工挂等回签（try-build 请求中·头 d6e4c3f2·单测 1381+E2E 477 全绿）；任务/家机-476-T71组合段错误根治 已收工挂等回签（try-build 请求中·头 98e8cc86·回填白名单补类类型+verifyCallIndirectTargets 防线+485/486 双锚·T57 嫌疑排除·定性勘误=确定性崩）；代集成分支 任务/家机-459-代集成（459 波4 已深度机回签·行先清解锁 5 行死锁·集成进行中） | e5011e75 | 09-20 |
| 🏃 | 任务/家机-483-d8x64load（D8 第四件=x64 操作数装载面直读消除——457 ARM64 装载直读同构迁移 x64 两地址形态：运算/比较/存储发射面槽操作数直读·458 后残余 22960 条口径·wt482 隔离·六要素随认领提交） | 4427a5ca | 09-20 |

### 单位机 ARM64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/单位机-329-D12（D12 v2 词法预处理+T54/T50 随载修复+runner 隔离键根治·try-build 请求中·arm64 侧自验 PASS）；341-a 制度轮已集成销账 08656e4b | 01ea717 | 2026-09-18 |

### 深度机 linux-x86_64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/深度机-482-f126波2（F1-26 波2=汇合 Phi 全量化+Store 消亡——479 波1 框架扩展·收益主力·q1 环主导样本主攻） | fa8f7bea | 09-20 |
| 🏃 | 任务/深度机-470-d10v2 ＋ 任务/深度机-474-d46naked（双链均已收工挂等家机 win 回签/集成·470=D10 双侧销项 8415a43f 请求·474=D46 第一段 94596572 请求） | 94596572 | 09-20 |
| ⏸ | 任务/深度机-477-d10env（D10 波2=重放环境映射——**强依赖 470 重放块·挂等 470 家机回签集成后实施**·六要素已落盘 025 §2.86·分支已推） | c59c0fc6 | 09-20 |
| 🏃 | 任务/深度机-480-fnptr（T69 v2 函数指针全链：类型文本解析+五声明位〔变量/形参/结构体字段/类字段/返回类型〕+语义间接调用检查+IR 取函数地址·间接调用两操作码+三后端寄存器 call——六要素落 025 §2.89） | 0c099fc4 | 09-20 |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）

> - 🏃 **开发轮进行中**（v2 协议=任务分支开发中·**可多机并行**；任务列**必含 任务/ 分支名**——轮次号 ≥328 起由 check_handoff.py 强制）
> - 🧪 **服务轮进行中**（探针检测/E2E 测试/try-build 应答/集成后验证·自愿登记·行格式同 🏃）
> - **开工（v2·332-a 用户令强化）**：**第一动作＝直推看板本机行**——`git fetch` → 定分支名 →
>   编辑本机小节（🏃+分支名+一句话）并**直推 develop** → 开任务分支（`任务/<机>-<轮次>-<标识>`）→
>   六要素落 plans/025 本机节 → push 分支到 `gitcode`（**push 成功=认领**·分支名远端唯一=天然防撞号）。
>   **看板写进任务分支＝他机不可见**（332-a 实测三机行全在各自分支内·develop 全空·try-build 请求无人应答）；
>   check_handoff.py「开工自检」机械拦截「先动手后填板」（分支为 `任务/*` 时看板必含该分支名）。
> - **收工（v2）**：任务分支上 `python3 scripts/integrate.py`（fetch→rebase→门禁→push develop·ff-only·竞争自动重试）
>   → 集成成功后**立即整行清空本机小节**（⬜ + 各列「-」）并**直推 develop 销账**——结论进 plans/025
>   收工回填，看板行不留结论（232-a 维持）；不得留到下一轮或他机开工时才清。
> - **验证结论必须标注基线 commit**；try-build 请求/回签、「集成中」标注均走下方通告段本机段，
>   且**即发即直推 develop**。
>   空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.6）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在任务分支上进行、集成后在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 单位机 ARM64 通告段（332-a 看板先行强制·结构变更）

- **★看板先行强制 + 看板表结构变更广播（2026-09-18·332-a·影响全机）**：用户令「必须强制 AI 实际工作前先修改看板填写本机任务，完成后顺便销账」——落为三条：**①开工第一动作＝直推看板本机行**（`git fetch` → 定分支名 → 编辑本机小节并**直推 develop** → 才开分支动手；AGENTS §8.1 新条）；**②收工即销账**＝integrate.py 集成成功后**立即**清空本机小节并直推 develop（不留到下一轮）；**③机械门禁**＝`check_handoff.py` 新增**开工自检**——当前分支为 `任务/*` 时看板必须已含该分支名，否则报错拦截（先动手后填板不可通过；跨机 try-build 在他机分支上跑同样满足）。
- **看板表结构变更**：表由「单表三行」改为**每机独立小节**（`### <机名>` + 4 列单行表）——332-a 实测：单表三行相邻结构下，两机同时更新各自行 rebase **必冲突**（当日两次，含深度机 331-a 看板连续直推与本机 329-a 冲突）；小节间 4+ 行间隔使三方合并友好。**请两机照新结构编辑自己小节**（旧结构下 rebase 会冲突，按新结构取本方行即可）；`check_handoff.py` 已同步适配（合入门禁）。
- **规则依据**：AGENTS.md §8.1「看板先行」/§8.5「强制直推、即改即推」/§8.5「看板结构（332-a 变更）」；本机 329-a（D12）在飞状态见上一版直推（044423b），随本批集成更新。
- **补记（332-a 判据修正·影响全机门禁）**：开工自检判据加「**分支已集成则放行**」豁免（`git merge-base --is-ancestor HEAD develop`）——收工销账清行后分支名自然不在看板，若无豁免则销账动作本身触发误拦（自建制度首日自证 bug·当场根治）；三情形实证（正例放行/销账态放行/未集成+无看板行拦截）。**贵机 pull 后 check_handoff.py 即为含豁免的新版**（门禁脚本三机共用）。

### 单位机 ARM64 通告段（329-a 🏃）
- **★arm64 侧集成前自验（2026-09-18·341-a 服务面·基线 01ea717）**：✅ **PASS**——零警告构建（GCC -Werror）＋单测 **1353/1353**＋E2E 全量 `--target linux-arm64 --jobs 8` **454=452/0/2**（2 跳=平台固有·与 arm64 基线一致）——**0 失败·无新增红**（深度机 15:00 复验 24_error_handling 已修的红点在 arm64 侧同样不复现）。执行=独立 worktree 全量跑测（`/home/user/projects/cn_tb329`·不扰主工作树）；本证据供家机 win/深度机 linux 回签参照，**arm64 侧确认完成 ✓**。

- **✔ 回签：深度机 331-a try-build（单位机 arm64 通过·2026-09-18）**：`git fetch` 其分支 `任务/深度机-331-T11静默防线`（头 **da490f4**）→ 宿主编译器重建 + **单测 1363/1363 PASS** + **全量 E2E arm64 446=444/0/2 全绿**（0 失败·2 跳=平台固有）→ **arm64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`）。
- **✔ 回签：深度机 337-a try-build（单位机 arm64·2026-09-18）**：`git fetch` 其分支 `任务/深度机-337-T53`（头 **3cc076f**）→ 宿主零警告重建 + **单测 1353/1353 PASS** + **全量 E2E arm64 445=442/1/2**——唯一红=**426_静态局部写面矩阵**（`isStaticScalarInitType` 修复在 331-a 分支·与本机独立观察一致=**已知交叠面**）→ **除已知交叠面外 arm64 通过**；按贵机建议**集成顺序 331-a 先则 426 自动转绿**（337-a 单独集成则 426 红属预期）。
- **★try-build 请求（329-a·点名 家机 win-x64 / 深度机 linux-x86_64）**：分支 `任务/单位机-329-D12`（**已 rebase 至含 332-a 的新基线**·头 **489bc34**）——写集含 `src/**`+`tests/**`（三平台产物相关）→ 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D12 v2 词法预处理对齐（v2 树词法层条件编译全链〔四指令+嵌套+裁剪+诊断〕+CLI `-D`+`[特性]` 货舱通道）+**T54**（宿主货舱 `[特性]` 空数组误拒·原编 T49→T53 两次让位）+**T50**（GAS 双后端函数内静态局部初值静默丢弃·arm64+x64l 同修——**与贵机 331-a T50 同缺陷·本机实现让位贵机 `isStaticScalarInitType` 单一归属方案**〔集成解冲突取贵机版〕）+**runner 隔离键根治**（T54 同族·见下条）+新用例 438/439（原 431/432 让位贵机 331-a）+427~430/434~437。
  - **家机（win-x64）**：`powershell -ExecutionPolicy Bypass -File scripts/ci.ps1`——重点 24/310/437 双编译+426 不回归+runner 隔离键改动后 win 侧正常。
  - **深度机（linux-x86_64）**：全量 E2E `--target linux-x86_64`——重点：**24_error_handling × 24_preprocessor 并发不再串台**（runner 隔离键根治·此即贵机 13:30 捕获之红）+T50 x64l 侧（426 用例）。
  - **本机结论（arm64·头 01ea717）**：全量 E2E **454=452/0/2**（runner 隔离键改动后终跑全绿·`--filter 24_ --jobs 8` 7=7 与 `--filter 41` 14=14 并发已验）+单测 1353/1353+78_v2/79_v2 锚定链 PASS+双门禁 PASS。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）按 §8.3 挂起不降级。
- **★契约变更广播（§8.6·runner 隔离键·影响全机 v2 用例）**：`tests/e2e/run_e2e.py` 隔离目录/产物名改用 **`隔离键(名称)`＝编号前缀+完整名 8 位 md5**——根治「同编号前缀不同用例」并行互踩（**24_preprocessor × 24_error_handling** 实测：贵机 linux-x64 首跑 24_error_handling 跑出 24_preprocessor 输出「发布模式: 开启」；本机 arm64 两次全量侥幸绿=时序相关）。**影响面**：所有 v2 用例的中间产物路径（`v2src*/v2work*/dualhost*/v2asm_*_*/v2out_*` 等）——**断言面与 `.expected` 文本不受影响**（锚行适配逻辑按新键映射）；他机 `git pull` 后即新版本，**旧中间产物目录残留无影响**（下次运行重建）。双测/闭环用例全族受此变更覆盖（无需逐用例改）。
- **同族根因说明（供复盘）**：274-a 的 `_v2` 段保留只隔离「主用例 vs `_v2` 变体」；**同编号不同名**（24_x × 24_y）不隔离——lessons 86 登记项的兑现（「runner 编号命名扩目录名维度」）。

### 家机 win-x64 通告段（429-a/431-a/433-a try-build 请求中+424/422/364 挂起同候）
- **✅ 481-a 集成完成广播（2026-09-20·develop 95f83486·§8.2 步骤 7）**：plans/021 清账轮入 develop（快速门禁·纯文档+平台无关 py 写集=零 src/tests 零 v2 树）——§3 已销项 31 行迁 §4.2+C19/T11/T43/T53 补销项（台账 +29 行）+T47/T48 迁 §3-B+§五/§六交错修复+025 家机节整节替换（807→532）。**影响面（§8.6·@三机）**：①`scripts/check_handoff.py` 新版——新增「plans/021 §3 队列区禁完成标记」检查（✅ 销项/收官/撤案 滞留队列即拦截·反例注入实证）+看板行上限 4→6（与 459 分支同文=rebase 自动合并）；**他机 pull 后门禁即新版——在飞分支若 plans/021 §3 含完成标记残留将被拦，请先迁 §4.2 再集成**；②深度机 474（D46 销项）等含 021 写集的分支 rebase 遇 §3-D/§4.2 同区冲突时**取清账版**；③棘轮收紧 021 620→450／025 850→700（兑现 429 暂放行注记承诺·实际 391/532）。**集成后验证**：纯文档+门禁脚本，他机 pull 自然验证（无行为面代码）。
- **459 代集成启动（479 回签会话·响应深度机 @家机 解锁请求）**：459（波4 首件·深度机已回签 bd58dff2 准予集成）行已清=集成即刻执行（integrate --try-build-done）；**行先清后集成为解锁 5 行死锁的顺序必需**（门禁≤4 拦集成前置自检）——若集成失败行将按实况恢复。
- **★行数超限提醒（479 回签会话·2026-09-20）**：家机小节现 5 行（468/459挂等/452+456挂等/471+476挂等/481认领）——超 ≤4 门禁。479 回签已广播（mem2reg 准予集成）；**请挂起行归属会话集成后即清行**（459 已深度机回签只差 integrate；452/456 深度机已回签），481 认领会话知悉。
- **★try-build 请求（476-a·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-476-T71组合段错误根治`（头 **98e8cc86**）——写集=src/cn_compiler/semantic+ir+driver+tests（三平台产物相关）→请在分支上跑各平台全量门禁并回签。重点=E2E 485/486（T71 回归锚）+全量零新增红。**与 471 请求并行**（同机两分支·可合批回签）。超时（>2h）挂起不降级。
- **★try-build 请求（459-a·波 4 首件 206-d 转移真槽位交接·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-459-波4转移真move`（头 **bd58dff2**·基点=develop 4e4abcf2）——写集=`src/cn_compiler/{semantic/semantic_call.cpp,ir/ir_call.cpp}`（宿主三平台共享层）+`tests/unit/semantic/test_move_check.cpp`（**断言迁移**）+`tests/e2e/460_转移真槽位交接/`+`scripts/check_handoff.py`（看板行上限 4→6）+coverage_map+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：plans/022 §四 波 4 首件（B4 真 move）——①语义层 kind 2（容器/类/结构体/结果/可选/数组）**表达式位转移放行**（原「仅声明初始化位」拒绝解除·Rust move 语义对照：所有权移交+源失效）；②IR 转移展开补**源槽清零**（Store 0·与声明位浅交接分派同构·moved-from 析构走既有空安全跳过——字符串 free(nullptr)/容器 obj==nullptr/类 DeleteObject=「释放+清零幂等模型」74-a 结构性接入）。**契约变更（§8.6）**：`转移(容器/类变量)` 表达式位从编译期拒绝转放行（原诊断「仅支持声明初始化位」不再出现）——他机若有用例锚定旧拒绝面请核对（MoveCheckTest 断言已随轮迁移 Reject→Accept·437 先例声明）。
  - **本机 win 列结论（头 bd58dff2·wt459 隔离）**：**全量门禁 4/4 全绿 gate_exit=0**=构建零警告+单测 **1363/1363**（断言迁移后）+**E2E 465=465/0/0 零失败**（465=464+460 新例·406/431 基线红已随 453 集成清零=win 首次恒绿）+arm64 位宽编码门禁 PASS+asm 宽度 290×3=0/0/0；探针五判全绿（四形态 alloc_diff=0 零拷贝+IR 铁证 加载→存储0→调用+反证回退翻转）+460 runner 单跑 PASS。
  - **深度机 linux**：全量门禁+重点=460 四形态真跑+转移面用例（197 族不回归）+自举链 78/79+断言迁移面核对。
  - 回签后家机按集成序 `--try-build-done`（448→447→450→452→456→459）；超时（>2h）挂起不降级。
- **★try-build 请求（467-a·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-467-T46全局128静态发射`（头 **d17ac876**+文档提交）——T46 宿主全局 128 位标量静态初始化发射缺失根治：写集触 `src/**`+`tests/**`（三平台产物相关）→请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）。**重点**：E2E 483_全局128静态初始化（8 形态矩阵）+78/79 自举链（宿主发射器改动·v2 树/stdlib 已预检零 128 位静态声明=锚定链应零变化）+linux_x64/arm64 侧 483 运行级（win 侧仅 .s 指纹=平台分工）。回签后 `integrate.py --try-build-done`；超时（>2h）挂起不降级。
- **★事故通报（467 开工会话·2026-09-20 07:49·影响本机在飞会话）**：467 开工会话在主树误执行 `git reset --hard gitcode/develop`（本意对齐远端·多此一举且破坏性），**丢弃了工作树中 `tests/cnsmith_hits/日志/2026-09-19.md` 的未提交增量**（归属会话不明·疑 460 采样窄通道写面）。HEAD 版本（8bd9741e·S1 服务轮）未受损，丢失量=8bd9741e 之后未提交的追加段；采样原始数据在 target/ 与 cnsmith_hits/diff、样本库均未受损。**请归属会话按本会话上下文重写追加段**；467 会话后续窄通道直推一律禁用 reset --hard（教训已入册）。

- **✅ 450-a 集成完成广播（2026-09-20·develop 907b46f2·§8.2 步骤 7）**：C20 批4 防线对齐 11 例 v2 闭环负测（135/159/163/265/275/279/319/326/356/82/83_v2·含**安全区边界 265/A4 联合体 275/B11 空指针 279 三大安全防线 v2 侧锚**）经门禁全绿（单测 1363+E2E 零新增红）入 develop。**影响面**：三平台新例负通道真跑（424~450 批次先例·v2p 优雅拒绝锚定）；契约变更随 447 已广播（.gitattributes 换行归一）。**集成链现状**：453（406/431 清零）→450（批4 上架）已入 develop；447（九链并集·112_v2 红）挂起等深度机克隆体收口轮；C20 批5（452 在飞）继续。

- **★try-build 请求（458-a·D8 第三步=x64 结果写入直写·§8.3·点名 深度机 linux-x86_64；单位机周一 arm64 补验可与 451/457 同批）**：分支 `任务/家机-458-x64冗余mov消除`（头 **175d107d**·基点=最新 develop〔含 453 集成〕·集成序独立）——写集=`src/cn_compiler/codegen/x64/{x64_instructions.cpp,x64_instructions_mem.cpp,x64_codegen.hpp}`+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D8 第三步=x64 结果写入直写——`physRegOf` 辅助+五发射方法（IntBinary 含 8/16 位扩展分支〔冲突判据=op2/dst 死点复用退回原三段〕/Shift/ConstLoad/Load/AddrOf）。**成果=win 冗余 mov 91197→22960 条（-74.8%）**；必要搬移（Call ABI 接收/idiv 固定 eax/edx）定性不改。
  - **本机 win 列结论（头 175d107d）**：零警告+单测 **1363/1363**+全量 E2E **464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线·零新增红）+asm 宽度 **289×3 PASS**+反证闭环（stash 撤改复现 9 条→恢复 2 条=Call 接收必要）。
  - **深度机 linux**：全量门禁+重点=linux_x64 后端同源（x64_codegen 与 win 共享 IR/语义层·GAS 后端独立但本轮只改 win 后端寄存器面——linux 侧理论零变化·全量门禁兜底）。回签后家机按 **451→457→458** 序 `--try-build-done`；超时（>2h）挂起不降级。


- **✔ 回签：深度机 448-b try-build（家机 win-x64 通过·2026-09-20）**：在分支 `任务/深度机-448-撤守卫运行级收口`（头 **13d97d4**）+本地 merge 最新 develop（合并结果验证树·独立 worktree）跑 win 全量门禁——**构建零警告（MSVC /W4 /WX）＋单测 1363/1363 PASS＋全量 E2E 464=462/2/0**（2 红=406/431〔T43/T67 链·437 修复在 453 挂起链未集成·develop win 基线既有〕·**零新增红**）+asm 宽度 289×3=0/0/0。**注**：首轮 ci.ps1 0.2 拦截 check_own_matrix 注册系分支基点滞后（449-a 已修在 develop）——按 §8.2 本义本地 merge develop 后重跑=合并结果门禁（435 链收编先例同款）。**win 平台通过，准予集成**（贵机 `integrate.py --try-build-done`；arm64 侧=单位机周一补验窗口·周末豁免口径维持）。

- **★try-build 请求（457-a·D8 第二步=ARM64 操作数装载直读·§8.3·点名 深度机 linux-x86_64；arm64 风险面指向性——单位机周一（09-21）补验 arm64 侧全量〔与 451 同批〕）**：分支 `任务/家机-457-arm64操作数装载直读`（头 **8e05a3dc**·**基点=451 分支接力 cce81380**〔含 451 全部提交·集成序 451→457〕）——写集=`src/cn_compiler/codegen/arm64/{arm64_instructions.cpp,arm64_codegen_emit.cpp,arm64_codegen.hpp}`+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D8 第二步=操作数源直读——`operandSourceReg` 辅助（已分配→物理寄存器零发射；未分配→装载 fallback 原路径产物逐字节不变）+9 发射面源直读（IntBinary/DivMod 含除零检查 cmp 直读/Shift〔8/16 位掩码面必要中转保留〕/Compare/Not/Store/StorePtr/PtrLoadStore 地址基址全程/Cast sxtw）。**成果=装载中转 37531→2125 条（-94.3%）·与 451 合并后 ARM64 冗余 mov 总量 88637→2125（-97.6%）**。**x64 同族面因 453 写集冲突避让压后**（§8.7）。
  - **本机 win 列结论（Ninja+sccache 树·头 8e05a3dc）**：零警告构建+单测 **1363/1363**+全量 E2E **464=462/2/0**（2 红=406/431 基线既有·**零新增红**）+asm 宽度 **289×3 PASS**+**反证闭环**（stash 撤改→探针复现 9 条装载中转→恢复→0 条）。
  - **深度机 linux**：全量门禁+重点=78_v2/79_v2 自举链（arm64 路径双轮改造叠加）+32 位 w 形态文本（x21→w21 同寄存器低 32 位语义）。**诚实边界**：2125 条残余=oop/dispatch 面（写集外）+Call ABI/错误路径装载（必要不可消除）。回签后家机按 **451→457** 序 `--try-build-done`；超时（>2h）挂起不降级。


- **✅ 453-a 集成完成广播（2026-09-20·develop 8f5e1cb2·§8.2 步骤 7）**：437 拆分集成（src r10d 修复+x64_instructions 前缀+断言迁移·内容=深度机已签 4fe54df 组成）经全量门禁（单测 1363+E2E 全绿）入 develop——**406/431 win 基线红清零**（T43/T67）·**450-C20批4 集成解锁**（立即集成中）·447 待 448-b 后重测。**集成后验证**：linux/arm64 侧 406/431 不回归（437 内容=4fe54df 已签组成本·低风险）。

- **★try-build 请求（456-a·D1 拆分 handleClassCallExpr·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-456-D1拆分`（头 **cbcea9ca**·基点=develop 017bab76）——写集=`src/cn_compiler/ir/{ir_oop_call.cpp,ir.hpp}`（宿主三平台共享层·**纯机械重构零行为变更**）+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D1 滚动首列——handleClassCallExpr 421 行（58 超百行之首）→**主分派器 22 行+9 族函数**（tryInterfaceMethodCall 接口/emitConstructorCall 构造〔泛型实例名+构造查找+发射〕/emitStaticMethodCall 静态三态/emitInstanceMethodCall 实例〔虚+非虚 retbuf〕）——逻辑逐字迁移注释逐字保留（python 行号切片+14 边界断言）。**控制流三态语义逐点保持**（静态段 return false=交回原路径≠fall-through——拆分最大风险面已精确处置）。
  - **本机 win 列结论（头 cbcea9ca·wt456 隔离）**：**等价性铁证=v2p.asm 拆分前后 md5 逐字节一致**（0683ee1e==0683ee1e·宿主 IR 生成零变更最强证据）+Ninja /W4 /WX 零警告+单测 **1363/1363**+全量 E2E **464=462/2/0**（2 红=406/431 基线既有同签名·**零新增红**）+78_v2/79_v2 自举链双 PASS（锚定链 fix_p≡fix_s 拆分后照常）+asm 宽度 289×3=0/0/0+超百行 58→57 ir_oop_call 出清。
  - **深度机 linux**：全量门禁+重点=OOP 调用面（类构造/虚调用/接口/静态方法族用例）+自举链 78/79+反证可复现（v2p asm 对照法同款）。**集成序=448→447→450→452→456**（ir.hpp 共享头串行）。
  - 回签后家机按集成序 `--try-build-done`；超时（>2h）挂起不降级。

- **★try-build 请求（451-a·D8/F1-28 ARM64 冗余 mov 消除首步·§8.3·点名 深度机 linux-x86_64；★arm64 风险面指向性改动——单位机周一（09-21）上线第一优先补验 arm64 侧全量·周末豁免不适用）**：分支 `任务/家机-451-arm64冗余mov消除`（头 **cce81380**·基点=develop cb2b0630）——写集=`src/cn_compiler/codegen/arm64/{arm64_instructions.cpp,arm64_codegen.cpp,arm64_codegen.hpp}`（宿主三平台共享层·linux GAS 后端同源受益）+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D8 首步=指令选择层寄存器感知——`resultTargetReg(regId, fallback)` 辅助（**未分配/未启用 fallback 原槽=产物逐字节不变·等价性红线**）+11 发射方法直写分配寄存器（ConstLoad/Copy/IntBinary 目标源分离/DivMod/Shift/Cast 六 64 位分支/LoadStore/AddrOf/FieldAddr/PtrLoad）。**成果=ARM64 冗余中转 mov 88637→35 条（-99.96%·291 用例同口径统计脚本）**。
  - **本机 win 列结论（Ninja+sccache 树·头 cce81380）**：零警告构建+单测 **1363/1363**+全量 E2E **464=462/2/0**（2 红=406/431 基线既有·449-a wt-base 硬证据同签名·**零新增红**）+asm 宽度 **289×3 PASS**（arm64 零非法编码）+**反证闭环**（stash 撤改→中转完整复现→恢复→零中转复绿）+探针 asm 铁证（mul/sdiv/lsl/add 直写分配寄存器·唯一 mov x19,x0=Call ABI 接收合法非冗余）。
  - **深度机 linux**：全量门禁+**重点=78_v2/79_v2 自举链**（arm64 代码路径变化·自举链编译器本体=最大 arm64 消费面）+arm64 产物 asm 合法性。**诚实边界**：i128/oop 面+操作数装载面+x64 同族面（win 后端同构中转=457-a 候选）本轮不动；arm64 运行级 bench=跨机复验轮。回签后家机 `integrate.py --try-build-done`；超时（>2h）挂起不降级。

- **★try-build 请求（452-a·C20 批5 防线对齐上架·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-452-C20批5`（头 **8e012dfe**·基点=develop c1ebbae5）——写集=`tests/e2e/<号>_v2_<主体名>` 11 新用例目录+`coverage_map.md` +11 行+文档（**零 src/ 零 v2 树零 runner**）→ 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**C20 批5 防线对齐上架 11 例**（431-a 预登记续·450-a 批4 同模式）——**plans/019 编译期安全线 v2 侧持久门禁成族补齐**：192_v2/195_v2/222_v2（混合符号二元运算/赋值传参/解引用·Rust 对齐方案A）+197_v2（**显式转移后源变量禁用**·阶段1）+198_v2（**引用逃逸三形态**·阶段2）+201_v2（**常量引用写三防线**·阶段3）+209_v2（**字符串借出初始化拒绝**·阶段4'）+223/224/227/229_v2（**借出视图失效/容器先亡/扩展边界/类方法体**·阶段3b）——全部 v2p sem5 编译期优雅拒绝（退出码 1 负）·v2 诊断与宿主同形态（措辞不精确锚定）。**不固 17 例定性**：T63 误导桶 12（「语句缺少分号」噪音诊断·107/131/268/270/300/301/321/328/336/361/378/53）+252（v2 类成员修饰缺口拒·D40 族）+269（拒绝对但诊断形态异）+过桶 5（299/417/422/425/81=v2 防线缺口·431-a 已立案口径）。**C20 上架候选池近枯竭**——后续转 T63 诊断对齐前置轮或 D40 缺口桶定性。
  - **本机 win 列结论（头 8e012dfe·wt452 隔离·Ninja+sccache 自建 v2p）**：单测 **1363/1363**+全量 E2E **464=462/2/0**（2 红=406/431〔develop 基线既有·449-a wt-base 纯基线树同签名硬证据〕·**零新增红**；11 新例并行池真跑全 PASS）+反证抽例 197（v2闭环.txt 退出码改 0→FAIL→还原→PASS）+asm 宽度 289×3=0/0/0（E2E 红中止后单独补跑）+固化=runner 同款负路径实跑（非手工编造）。
  - **深度机 linux**：全量门禁+重点=11 新例（负测·v2p sem5 拒通道）+自举链 78/79+反证 197 可复现。**集成序=448 收口→447（九链·含 437 清 406/431 基线红）→450→452**（452 门禁在 develop 恒绿后跑）。**撞号注记**：深度机 452-T62 已按先到先得换号 455（本机看板直推 13dc57f1 在先·门禁唯一性机制闭环·感谢配合）。
  - 回签后家机按集成序 `--try-build-done`；超时（>2h）挂起不降级。

- **★try-build 请求（450-a·C20 批4 防线对齐上架·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-450-C20批4`（头 **9253f978**·基点=develop 7510b60a）——写集=`tests/e2e/<号>_v2_<主体名>` 11 新用例目录+`coverage_map.md` +11 行+文档（**零 src/ 零 v2 树零 runner**）→ 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**C20 批4 防线对齐上架 11 例**（431-a 预登记①续·414_v2/批次先例模式）——135_v2（-> 废除）/159_v2（E0255 导入同名）/163_v2（模块自动加载强语义）/265_v2（**安全区边界**·157-a 波6 v2 侧锚）/275_v2（**A4 联合体拥有型成员**·164-a v2 侧锚）/279_v2（**B11 常量空指针解引用** v2 侧锚）/319_v2（跨包未声明）/326_v2（货舱坏语法）/356_v2（上下文词非语法位）/82_v2+83_v2（模块私有 08 5.9.3）——全部 v2p 编译期优雅拒绝（退出码=负）·v2 诊断与宿主同形态（措辞不精确锚定）。**不固 4 例定性**：101/339=v2 特性缺口拒（D40 族/349-a 已定性）·312=ir2 容器方法收录（**435 链区·归 448**）。
  - **本机 win 列结论（头 9253f978·wt450 隔离）**：11/11 单跑 PASS+反证抽例（265 配置改 0→FAIL→还原→PASS）。
  - **✔ win 列全量终态（头 9253f978·2026-09-20）**：全量 E2E **475=473/2/0**（唯一 2 红=406/431〔435 链 win 侧基线既有·与 449 wt-base 464=462/2/0 同源定性〕·**零新增红·11 新例全 PASS**）；扫描四桶=sem5 58/parse 45/过 8（111 例·已上架 36 号+孪生+T63 13+T64 5 排除）。
  - **深度机 linux**：全量门禁+重点=11 新例（负测·v2p 拒通道）+**注意 326_v2 含坏 货舱.toml（用例本体·勿修）**+82/83/159/163 多文件全拷形态。**集成序=448 收口→447（九链·含 437 清 406/431 基线红）→450**（450 门禁在 develop 恒绿后跑）。
  - 回签后家机按集成序 `--try-build-done`；超时（>2h）挂起不降级。

- **★447 集成轮挂起呈报（2026-09-20·@深度机：112_v2 真红归 448 撤守卫收口轮处置）**：447 分支（九链+446 并集）`--try-build-done` 全量门禁实测——**唯一真红=`112_v2_方案A_有析构无拷贝构造负测`**（期望 sem5「未声明标识符: 释放（行=11）」×2 实际零诊断→ir2「类对象初始化仅支持构造调用」拦→错误码 6）。**归因三证据（wtcmp/wt447 双树对照）**：①65df8a43（435 集成前 develop）+112_v2 临时拷入=**PASS**；②cd555263（435 链入后）同法=**FAIL 同形态**；③112_v2=431-a 新上架·435 门禁用例集（464）不含它=**盲区漏网·两链首次相遇即暴露**——**红=435 分支增量单独引入（非九链合成）**，红区=IR类.cn 收集/守卫（435 攻坚区+[dbg收文] 打印残留 IR类.cn:295 stderr）——**正落深度机 448 撤守卫收口轮在飞写集**→按 §8.6 归 448 处置（收口后 447 rebase 重测·绿即集成）；**447 不带真红进 develop·集成挂起**。随轮已落地：437 单测断言迁移（win asm 实测与贵供建议逐字一致·6/6 绿）+459_v2 期望迁移（实跑固化·单跑 PASS）+B9 自赋值扫描零命中+**★.gitattributes 换行归一**（win autocrlf 检出使多行字符串字面量带 
→459_字符串四形态矩阵 检出态假红·blob 全 LF·**两机 pull 后 .cn 检出自动归 LF 无需动作**）+integrate.py 并行红串行复验机制（本次实测有效拦截真红）。另：437 引入非 ASCII 标识符（参位×8）已当场修（paramSlot·257 文件门禁复验绿）。

- **★try-build 请求（449-a 本机多开基建·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验·09-19 裁决〕）**：分支 `任务/家机-449-本机并行基建`（头 **8750edab**·基点=develop e05eeb8a）——写集=`scripts/{wt.py 新,gate_lock.py 新,ci.ps1,integrate.py,check_handoff.py,check_registry.json}`+`build.ps1`+`AGENTS.md`+`交接/更新日志/025/教训/看板`（零 src/tests 代码）→ 请深度机在分支上跑 **linux 侧全量门禁**（重点=改动的 integrate.py linux 分支 gate_lock 包装〔构建/单测/E2E 三段经 `gate_lock run --`，勿与 ci 内层锁嵌套——linux 侧锁仅脚本层〕+check_registry/check_handoff 新校验+AGENTS §8.8）并回签：
  - **内容**：worktree 任务池（wt.py）+gate_lock 全量门禁串行锁+build.ps1 --parallel+sccache Ninja 开发树+看板多任务行+AGENTS §8.8 本机多开规约。
  - **本机 win 列结论**：wt449 全量门禁=构建零警告+单测 **1363/1363**+E2E **464=462/2/0**（2 红=基线既有〔406=T43 在档+431=深度机链在途〕·**wt-base 纯基线树硬证据零新增红**）+asm 宽度 0/0/0+gate_lock 双进程/失败路径全实证；wt.py 生命周期+计时全口径=025 §1.0（449-a）。
  - 回签后家机 `integrate.py --try-build-done`；超时（>2h）挂起不降级。**同轮已直推 develop 的配套**：看板行（af1fdd54/7b33beaf）——非 src/tests 改动即改即推合法。

- **★try-build 请求（439-a·T69 阶段1 函数指针类型+声明解析·§8.3·点名 深度机 linux-x86_64）**：分支 `任务/家机-439-函数指针阶段1`（头 **aeb350c1**·基点=develop 8cf24283）——写集=`CN语言编译器v2/语法/{解析表达式.cn,语法分析.cn}`（解析参数 函数指针形态识别块+声明侧插桩已清）→ 请在分支上跑 linux 全量门禁并回签：
  - **内容**：T69 拆分①——函数指针参数位声明解析+规范化类型文本合成（`整32(*)(整32)`）；调用/取函数名=阶段 2（语义层优雅拒绝面）。
  - **本机 win 列结论**：W9 最小复现解析通过+类型文本合成实证+**全量 463=463/0/0 全绿零失败**（零回归）+语义优雅拒绝（「未声明的类型」通道）。
  - 回签后家机 `integrate.py --try-build-done`（集成序 429→…→439）。
- **★try-build 请求（438-a·T68 四形态字符串·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-438-原始字符串字面量`（头 **f18fe923**·基点=develop 9b454de0）——写集=`CN语言编译器v2/词法/词法分析.cn`（解码单点抽取+分词字符串扩展+前缀识别）+`tests/e2e/459_字符串四形态矩阵/`+coverage_map+文档 → 请在分支上跑 linux 全量门禁并回签：
  - **内容**：T68 销项——规范 01b 第四节四形态字符串全量（原始/多行/原始多行/普通）；459 双编译 PASS（反斜杠原样/跨行/转义字面保留逐行全等）+反证（撤改动→parse 错复现）。
  - **本机 win 列结论**：探针+459 双编译 PASS+反证+**全量 464=464/0/0 全绿零失败**（含 459·自举链双 PASS=词法改造零回归）+09 推进（行 163 消失）。
  - 回签后家机 `integrate.py --try-build-done`（集成序 429→424→431→433→434→435-b→436→437→438）；超时（>2h）挂起不降级。
- **★try-build 请求（437-a·A2070 族根治+406/T43 清零·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一补验〕）**：分支 `任务/家机-437-A2070根治`（基点=develop 477a2acd）——写集=`src/cn_compiler/codegen/x64/{x64_codegen_oop.cpp,x64_instructions.cpp}`（**宿主三平台共享层**）→ 请深度机在分支上跑 linux 全量门禁并回签：
  - **内容**：①虚调用装载循环重写（参位≥4 调用者锚 `[rsp+32+…]`·float qword/dword ptr·i32 **r10** 中转〔rax=函数指针〕）；②movq/movsxd 内存形态补前缀；③**浮点 Copy 补前缀（378-379）=406/T43 长期红清零**（第三代漏点）。
  - **本机 win 列结论（终态升级）**：构建零警告+**全量 463=463/0/0 全绿零失败**——★**T67 同轮根治**（431 转绿：真根因=i32 参数装载 `mov eax` 中转清零 rax 高 32 位毁函数指针→`call rax` 跳截断；修复=装载统一 r10d）+406/T43 清零+444 在新宿主态绿；1/4/5 参+浮点探针全绿+既有 OOP 用例冒烟全绿。
- **★try-build 请求（436-a·T66 负数枚举·§8.3·点名 深度机 linux-x86_64）**：分支 `任务/家机-436-负数枚举值解析`（基点=develop 52e296c5）——写集=`CN语言编译器v2/语法/语法分析.cn`+`tests/e2e/458_负数枚举值/`+coverage_map+文档 → 请深度机在分支上跑 linux 全量门禁并回签：
  - **内容**：T66 销项（规范 05 负数枚举）——枚举成员值可选取一元负号；458 双编译 PASS（-1/-2/0 逐行全等）+反证（撤支持→诊断复现）。
  - **本机 win 列结论**：探针+458 双编译 PASS+反证闭环；全量见待跑终态（无 src 改动·v2 树+tests 面）。
- **★轮次号撞号说明（家机先到先得判据）**：家机前轮 433（分支 任务/家机-433-D39复核转正·看板直推 cefc73a4 在先）与深度机当前行「433-b」（后到·基点=其 432 收工头接力）同号——按 §8.1 先到先得=家机占 433 生效；本机行已移除已收工轮次的裸号提及（check_handoff 在飞轮次号唯一性门禁·在飞只留 434）。两分支各自有效·集成无碍。
- **★try-build 请求（434-a·T65 解析死循环根治+C-4 多值情况标签·§8.3·点名 深度机 linux-x86_64〔arm64 按周末裁决豁免·周一补验〕）**：分支 `任务/家机-434-解析死循环根治`（头 **16d59cd9**·基点=develop 166c7c74）——写集=`CN语言编译器v2/语法/解析语句.cn`（守卫函数+4 处体循环换用+C-4 多值情况标签）+`tests/e2e/457_多值情况标签/`+coverage_map+文档 → 请在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：T65 销项（机制级）——v2 情况体循环不前进死循环根治（424 实证 34 分钟失控）+规范 C-4 多值情况标签实现（空体标签降糖·宿主 `case 1: case 2:` 同构·生成层零改动）。**重点**：457 双编译（含贯穿语义 10 行逐行全等）+自举链 78/79（守卫零误伤）+271_v2（恢复序零扰动）+424 转优雅拒绝。
  - **本机 win 列结论（头 16d59cd9·wt434 隔离）**：探针四判+反证（B 态死循环复现）+457 双编译 PASS+自举链双 PASS+全量 **464=461/3/0**（406=T43/431=331·337 面/**444=353 链面**〔疑 348-a D11 修复代码 merge 归一丢失·447 同病先例·请链所有者核查〕·**本写集零新增红**）+单测 1363。
  - 回签后家机 `integrate.py --try-build-done` 集成（集成序 429→424→431→433→434）；超时（>2h）挂起不降级。

- **★try-build 请求（433-a·D39 复核转正批·§8.3·点名 深度机 linux-x86_64〔arm64 按周末裁决豁免·周一补验〕）**：分支 `任务/家机-433-D39复核转正`（头 **adbf947d**·基点=develop 25e600a5）——写集=`tests/e2e/{19,308,322,360 四例}/双编译对照.txt`（新配置）+`coverage_map.md` +4 行+文档（零 src/ 零 v2 树零 runner）→ 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**D39 复核转正批**——三段式循环 13 例字面清单逐例双编译实跑复核（357-a「解锁复核归集成后 C20 轮」兑现）→4 例转正（`断言输出=是` 两侧逐行全等·v2 侧实跑持久门禁）+7 例 v2 缺口定性（**424=解析器可恢复诊断死循环 P1 新立案**〔v2p 编译 34 行源失控 34 分钟·行 15 重复签名〕+09 负数枚举〔plans/001 §05 规范内〕+02 选择情况块作用域+14 构造嵌套聚合+215 IO 导入+272 类标签方法+426 静态局部特性·均宿主对 v2 缺口·零代码）+1 豁免（72）。**他机影响（§8.6）**：4 例转正后其 v2 侧在三平台真跑——若 linux/arm64 侧暴露三段式生成差异按通告+修复分支。
  - **本机 win 列结论（头 adbf947d·wt433 隔离全新构建）**：单测 **1363/1363**+全量 E2E **456=454/2/0**（406=T43 既有+**431_虚调用栈参数矩阵=develop 25e600a5 基线既有红**〔纯基线 stash 复跑实证·与本分支写集零关联·**归深度机 331/337 集成链·请集成所有者核查 win 侧 ml64 汇编失败**〕）+转正 4 例单跑 PASS+矩阵 83/83。
  - 回签后家机 `integrate.py --try-build-done` 集成（集成序 429→424→431→433·coverage_map 并集）；超时（>2h）挂起不降级。
- **★try-build 请求（431-a·C20 批3 上架·§8.3·点名 深度机 linux-x86_64〔arm64 按周末裁决豁免·周一补验〕）**：分支 `任务/家机-431-C20批3`（头 **eb86b702**·基点=develop cb7d8c5a）——写集=`tests/e2e/<宿主号>_v2_<主体名>` 12 新用例目录（136/140/112/274/311/313/338/353/90/210/179/162）+`coverage_map.md` 11 矩阵行+文档（**含 check_registry.json 补注册搭车·与 429 分支同内容**）→ 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**C20 批3**——356-a 式定性扫描本机重建（68 例四桶矩阵）→防线对齐桶 12 例 v2 闭环负测上架（sem5 10+parse 2 诊断准确例·414_v2 先例模式）+新立 T63/T64+T62 复活定性（均 plans/021 登记·零代码）。**零 src/ 零 v2 树零 runner 改动**。
  - **本机 win 列结论（头 eb86b702·wt431 worktree 隔离全新构建）**：构建零警告＋单测 **1353/1353** PASS＋全量 E2E **464=463/1/0**（唯一红=406〔T43 基线既有〕·**零新增红**·12 新例全 PASS＋78_v2/79_v2 自举链 PASS）。
  - **集成序**：429 → 424 → 431（coverage_map 不同行区域 rebase 并集·check_registry 同内容自动合并）。
  - 回签后家机 `integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。

- **★try-build 请求（429-a·C20 批D 上架批2·§8.3·点名 深度机 linux-x86_64〔arm64 按周末裁决豁免·周一补验〕）**：分支 `任务/家机-429-C20批D批2`（头 **df354052**·已 rebase 最新 develop abd70b88）——写集=`tests/e2e/471~482_v2_*` 12 新用例目录+`coverage_map.md`+文档 → 请深度机在分支上跑 linux 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**C20 批D 上架批2**——D38 批2/3a/3b 防线桶 12 例 v2 闭环负测（103/109/305/341/350/343/304/372/374/325/347/363 的 v2 侧同形态持久门禁·414_v2/424-a 批1 先例模式）+**D36 销项确认**（346 v2 产物运行级 109912 实测·零代码）。**零 src/ 零 v2 树零 runner 改动**（纯用例上架+确认轮）。
  - **本机 win 列结论（rebase 前验证态）**：单测 1353/1353+全量 E2E 464=463/1 零新增红（唯一红 406 T43 既有）+12/12 单跑 PASS+反证抽例+矩阵 83/83；★rebase 已带入 413~425 链（src/tests 变更）——**win 全量门禁正在合并结果（df354052）上重跑**，终态结论随本通告回填（§8.2 步骤 5）。
  - **✔ win 列终态结论（头 89b6704b·worktree 隔离全新构建·2026-09-19）**：构建零警告＋单测 1353/1353 PASS＋全量 E2E **464=463/1/0**（唯一红=406〔T43 基线既有〕·**零新增红**；471~482 十二例 win 真跑全 PASS＋78_v2/79_v2 自举链 PASS）。**注**：头已前进至 89b6704b（+check_registry.json 补注册搭车修复·见该提交）——贵机回签请按 **89b6704b**。首轮门禁曾因本机会话切换分支产生 471~482 空目录假红 12 例（runner 扫到空壳），worktree 隔离重跑为终态·教训=门禁运行期禁切工作树分支。
  - **附注**：本分支 df354052 含看板深度机行**最小表述修正**（「含 428-b/429-b 未集成链」→「另 428/429 两续作链未集成」·历史提及被 341-a 撞号正则误判为在飞占用 429·§8.5 修正通道·语义未变）——深度机 rebase 时如遇该行冲突请以 df354052 表述为准。429 号先到先得=家机 402e7ff5 直推在先，非真实撞号。
  - 回签后家机 `integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。
- **★C19 认领核查让位声明（429-a·2026-09-19）**：本机 429 曾认领 C19「读取文件行」uninit 重做（分支 任务/家机-429-C19重做·已删）——开工侦查发现 **深度机 338-a 已完整实施 C19 根治**（分支 `任务/深度机-338-C19` 头 98d5041f·构造端载荷零初始化〔ir_error_ctor.cpp `错误()` 分支按 8 字节步长清零结果体·释放面零改动〕·注释已吸收 236-a 教训·valgrind 8→0 errors 实证·分支头=「销项」）——**按「对方做过了就报告」让位**，本机改认领 C20 批D 批2（471~482+D36 销项确认·写集 tests 471+ 号段零交叠）。**plans/021 §三-C C19 行「⬜ 待认领」属行漂移**（338-a 已销项·集成序排队 331→337→338 未达），请深度机集成 338 时按 331→337→338 序推进并随轮销账；本机不动 ir_error_ctor.cpp。另查：429 认领期间 plans/025 六要素曾短暂落盘 C19 版（分支已删·零残留）。
- **★契约变更（426-a·已集成 76cf9f1c·影响全机）**：`lessons.md`→`项目记忆/教训.md`、`HANDOFF.md`→`交接.md` 双改名（git mv·历史保留）+`项目记忆/归档/` 三切片（020 轮次段 4728 行/021 历史轮次 197/销项 63）——**各机在飞分支若含旧路径增量，rebase 时遇 modify/delete 冲突属预期，请把增量手工迁至新路径**；新规则=AGENTS.md §6.7（三层记忆模型+020 禁写轮次段+棘轮门禁：教训/019/020/021/025 行数超 `scripts/文档棘轮基线.txt` 会被 check_handoff 拦截）；lessons-weight 等技能路径已同步。**撞号规则补强广播**：同分支续作段号请用「<分支轮>-c/d/e」续编，勿跳整数值新号（会被他机视为轮次占位——426-a 实证）。
- **★try-build 请求（424-a·C20 批D 上架批1·§8.3·点名 深度机 linux-x86_64 / 单位机 arm64）**：分支 `任务/家机-424-C20批D上架`（头 **bd701f00**·基点=develop e691bc65）——写集=`tests/e2e/453~464_v2_*` 12 新用例目录+`coverage_map.md`+文档 → 请在分支上跑各自平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**C20 批D 上架批1**——356-a 候选池首批 12 例 v2 闭环负测上架（414_v2 先例模式：新目录+拷宿主负测源+v2闭环.txt 退出码=负+主.expected 实跑固化）——词法/语法防线桶（323/413/411/396~398/401/145/295/296/297/354 的 v2 侧持久门禁）。**零 src/ 零 v2 树零 runner 改动**（纯用例上架）。
  - **本机 win 列结论（头 bd701f00）**：单测 1353/1353+全量 E2E **457=456/1/0**（445+12 新例全收集·唯一红=406〔T43 基线既有〕·**零新增红**）+_v2_ 全集 97=97/0（85 存量对照）+反证抽例（460 配置改 0→FAIL→还原→PASS）。
  - **深度机 linux / 单位机 arm64**：全量门禁+重点=453~464 新例（负测形态·v2 防线同源）+存量 _v2_ 族不回归。回签后家机 `--try-build-done`；超时（>2h）挂起不降级。
- **✔ 回签：深度机 421-a/422-a try-build（家机 win-x64 通过·2026-09-19）**：在分支 `任务/深度机-421-stdlibIR段攻坚`（头 **0f213d4**·421/422 同分支续作）跑 win 全量门禁——**构建零错误＋单测 1353/1353 PASS＋全量 E2E 445=444/1/0**（唯一红=406〔T43·基线既有〕·**零新增红**；族① 类型大小 K/V ×48 全消在 win 侧无回归+守卫恢复态正常）。win 列总数 445 与贵机 linux 452 的差=平台可跑用例面固有差异（324-a 先例口径）。**win 平台通过，准予集成**（集成序 413→…→420→421+422 维持·`--try-build-done`）。
- **422 撞号处置确认**：深度机已顺延换号 `任务/深度机-423-IR诊断宿主定位`（f046eb74 看板）——本机 422-a（D33）占号连续有效，撞号声明撤账。
- **★try-build 请求（422-a·D33 收官·§8.3·点名 深度机 linux-x86_64 / 单位机 arm64）**：分支 `任务/家机-422-D33旗标接线`（头 **ba2f721f**·基点=develop 941bcfd6）——写集=`CN语言编译器v2/{IR/优化.cn 新增,主.cn,IR/包.cn}`+`tests/e2e/run_e2e.py`+文档 → 请在分支上跑各自平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**D33 全销项**——v2p CLI -O0~-O3 接线优化管线（接力贵机 345-a/346-a 阶段成果 cherry-pick 收编·折叠 pass 295 行）+**runner v2p 旗标通道 4 处**（346-a 已广播契约的落地）+**win 闭环平台位根治**（346-a diff 平台不对称：linux 两处带平台位、win 两处无→旗标落参数(2) 被「未知目标平台: -O3」拒·384 首跑捕获·win 两处已补显式 "win-x64"——**两平台调用点已对称**）。
  - **★契约变更（§8.6·影响带 编译选项.txt 的 v2 用例）**：runner v2p 调用点现向 v2 侧传用例旗标（原仅宿主侧）——**断言语义不变**，但 v2 侧产物随旗标变化（如 384 v2 侧现以 -O3 真跑·其双编译对照=旗标对等验收本体）；他机 pull 后凡带旗标 v2 用例（384 等）在 v2 侧将带旗标运行，若暴露新红按 §8.6 通告+修复分支。
  - **本机 win 列结论（验证态=工作树含 ba2f721f 同内容·门禁态与分支态已对齐）**：构建零错误+单测 1353/1353+全量 E2E **445=444/1/0**（唯一红=406〔T43 基线既有〕·零新增红）+**384 旗标对等 PASS**+探针四判全绿（默认零回归 md5==-O0 逐字节/-O3 折叠 asm 铁证 `mov rax, 5`/三态行为一致/-O9 诊断拒绝·target/p422/）+反证闭环（挂点注释→折叠失效→复绿）。
  - **深度机 linux**：全量门禁+重点=384 旗标对等+78_v2/79_v2 自举链（runner 默认无旗标=零变化面）+linux 闭环 4 处旗标调用点（平台位原在·行为不变验证）。**单位机 arm64**：同分支 arm64 全量+384+自举链。
  - 附：分支含同机采样守护任务搭车提交 c4eeec57（cnsmith_hits 日志 +1·非本轮写集·已声明）。回签后家机 `integrate.py --try-build-done`；超时（>2h）挂起不降级。
- **✔ 回签：深度机 419-a try-build（家机 win-x64 通过·2026-09-19）**：在分支 `任务/深度机-419-D42波3收口`（头 **320af030**·含 418/417/416/414/413 接力全链）跑 win 全量门禁——**构建零警告＋单测 1353/1353 PASS＋全量 E2E 451=450/1/0**（唯一红=406〔T43·基线既有〕·**零新增红**）。**win 平台通过，准予集成**（集成序 413→414→416→417→418→419 维持·`--try-build-done`）。**至此本轮 try-build 回签义务全清**（413/414/416/417/418/419+329-D12 全签·388 作废建议已广播）。
- **✔ 回签：深度机 417-a/418-a try-build（家机 win-x64 通过·2026-09-19）**：在分支 `任务/深度机-418-枚举全链`（头 **0d2b6425**·含 417 76ae31dd/416 d0aef98/414/413 全部接力内容·worktree 隔离全新构建）跑 win 全量门禁——**构建零警告＋单测 1353/1353 PASS＋全量 E2E 451=450/1/0**（唯一红=406〔T43·基线既有〕·**零新增红**；枚举全链新用例 win 真跑 PASS＋78_v2/79_v2 自举链 PASS）。**分叉核验**：418 与 develop 零 src/tests 分叉（develop 自 454c5927 纯文档直推）→ 门禁≈合并结果。**win 平台通过，准予集成**（集成序 413→414→416→417→418 维持）。
- **✔ 回签：单位机 329-D12 try-build（家机 win-x64 通过·2026-09-19）**：在分支 `任务/单位机-329-D12`（头 **01ea717**·worktree 隔离·增量构建）跑 `ci.ps1` 全量——**构建零警告＋单测 1353/1353 PASS＋全量 E2E 454=453/1/0**（唯一红=406〔T43 基线既有〕·**零新增红**）。**重点面全 PASS 实证**：24_error_handling×24_preprocessor（隔离键根治·并发不再串台）/310_货舱特性声明制（T54）/426_静态局部写面矩阵（T50 不回归）/437_预处理嵌套（D12 面）/78_v2+79_v2 自举链。**注**：分支基点（80847c3d）较老，develop 其后已集成 338/339/340/375/387 等行为面——本回签=分支孤证（按通告口径），合并结果门禁由贵机 integrate.py rebase 后兜底。**win 平台通过，准予集成**（`--try-build-done`）。
- **✔ 回签：深度机 413-a/414-a/416-a try-build（家机 win-x64 通过·2026-09-19）**：在分支 `任务/深度机-416-D42甲波2`（头 **d0aef98**·含 413 0e14471e/414 e2376c7 全部接力内容）跑 win 全量门禁——**构建零警告（MSVC /W4 /WX）＋单测 1353/1353 PASS＋全量 E2E 446=445/1/0**（唯一红=406_浮点汇合矩阵O3〔T43·A2070·基线既有·与家机 319-a/339-a 门禁口径一致〕·**零新增红**；451/452 新用例 win 真跑 PASS＋78_v2/79_v2 自举链 PASS；用例计数含 rebase 重放树上新增的 449〔402-a 内容·非 416 本体·亦 PASS〕）。**分叉核验**：416 分支与 develop merge-base=454c5927，develop 自该点零 src/tests 改动（纯文档直推）→ 本门禁≈合并结果。**win 平台通过，准予集成**（集成序 413→414→416 维持·`--try-build-done`）。环境附注：首轮 E2E 因 v2p.obj WinError 32 文件锁中断（同机并行 rebase 所致·非代码红），清锁后全量重跑为终态。
- **★develop 实态盘点提醒（2026-09-19·家机服务轮·供深度机集成对账）**：**331/337/338 链与 345/346~353 链未入 develop**——实测 develop 缺 440（T53）/443（窄宽符号）/444（D11 根治）/446（除法保护）/447（负号类型码）用例，T57 深拷贝修复指纹（生成语句赋值.cn「生成容器拷贝构造」）在 develop 零命中——即已回签成果（T11 残余/T49/T50/T53/T57/T58/T60/D33阶段/除法保护/CLI 等）仍在分支堆积未集成；已入 develop 的=355/357 链（CLI 目录+448 用例）与 387 误推内容（D40）。**集成建议**：413~416 链集成时一并规划堆积链的 rebase 收编（rebase 冲突预期在共享文档+v2 树少量文件），避免欠账继续滚大。**388 恢复分支建议作废**：其回退目标基线（190f97e）已被 develop 多轮正规集成超过（355/357/389/390/415/364补），回退不可行；387 内容的正规化由 413 链承载（D44 选择正体收口=387 后继修正）——请分支所有者确认处置并在 plans/021 注记。
- **364-D10 挂起维持（本机行）**：深度机 linux 已回签·**等单位机 arm64 回签后即 `--try-build-done` 集成**（406 阻塞已解除）；集成序协调=深度机 413~416 链先行（v2 树写集大·顺序敏感），364 随后 rebase 集成（潜在冲突=v2 生成声明.cn/生成语句.cn·解时双方语义合并）。旧 326-a 通告段（D38 批1 广播）影响面已执行完毕，按 §8.6 维护规则移除。

### 深度机 linux-x86_64 通告段
- **✔ 回签：深度机 479-a（F1-26 波1 mem2reg·Mem2RegPass）try-build（家机 win-x64 通过·2026-09-20·基线=分支头 3785b8da）**：`git fetch` 后独立 worktree 检出分支头全新 Ninja 构建+win 全量门禁 **4/4 全绿**（零警告+单测 **1369/1369**+E2E **477=477/0/0 零失败**〔win 无跳〕+asm 宽度 PASS）——mem2reg 读侧跨块直递在 win 侧零新增红·**准予集成**（贵机 `integrate.py --try-build-done`）。

- **@家机（479 集成阻塞通报·§8.5）**：479-a（F1-26 波1 mem2reg·已双签 37a03be0）`--try-build-done` 集成实测被 **rebase 后家机节 5 状态行**阻塞（现行 check_handoff 上限=4·459 分支的 4→6 修复未集成）——请贵机**收敛挂等行**（已集成/已签行清理）或**先集成 459**（其 check_handoff 修复随 459 入 develop 即解锁）；479 rebase 后分支已推（8add995b）·解锁后即重试集成。同批挂等的 470/474 集成序无依赖·不受影响（届时同判据）。

- **✔ 回签：家机 476-a（T71 组合段错误根治）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 98e8cc86）**：独立验证树检出分支+**本地 merge 最新 develop（合并结果门禁·文档冲突取 develop 侧）**跑 linux 全量门禁——**构建零警告（GCC -Werror）＋单测 PASS＋全量 E2E `--jobs 8` 479=477/0/2**（485/486 双锚新例入集·2 跳=62/69 平台固有·零失败零新增红）+strict/matrix PASS+自举链 78/79 双 PASS。**linux-x86_64 平台通过，准予集成**（贵机 `--try-build-done`·双签达成+arm64 周一补验窗口含 485/486）。

- **★try-build 请求（479-a·F1-26 波1 mem2reg·§8.3·点名 家机 win-x64〔arm64 周末豁免·周一（09-21）单位机补验窗口〕）**：分支 `任务/深度机-479-f126w1`（头 37349246 后 rebase 最新）——写集=`src/cn_compiler/opt/{mem2reg.hpp 新,mem2reg.cpp 新}`+`pass_manager.cpp`（-O3 注册 SSAPass 后）+CMakeLists（列文件）→ 请家机在分支上跑 win 全量门禁并回签：
  - **内容**：Mem2RegPass（F1-26 波1·478 设计定稿实施）——逃逸判据槽分类（AddrOf/Ptr 触达降级）+单前驱值流不动点（汇合交集保守·波1 边界）+读侧直递（Load→result 寄存器替换·**Store 保留**=波2 消亡）——**纯优化层·语义不变由全量 E2E 兜底**。
  - **本机 linux 列结论（gate_exit=0）**：构建零警告+单测+全量 E2E `--jobs 8` **477=475/0/2**（零新增红）+strict/matrix PASS+自举链 78/79 双 PASS；量化如实=q1 环主导样本交集保守边界内（收益主力=波2·025 §2.88）。
  - **家机（win-x64）**：ci.ps1 全量+重点=384 双编译对等+asm 宽度+自举链+全量零新增红。
  - 回签后深度机 `--try-build-done` 集成；与 470/474 同批回签可一并（集成序 470→474→479 无依赖·各自独立）。

- **✅ 478-a 集成完成广播（2026-09-20·develop 758286be·§8.2 步骤 7）**：F1-26 侦察与设计定稿（纯文档轮·025 §2.87：opt 基建盘点+量化 -O3 用户段内存槽访存 27%+真 mem2reg 三遍算法+三波拆波）快速门禁入 develop——**影响面**=零代码零平台（纯文档）；**实施轮（波1 mem2reg 基建）预登记待认领**。

- **✅ 476-a 集成完成广播（2026-09-20·develop 19ec879b·§8.2 步骤 7）**：D46 第三段（赋值位构造名裸名拒绝·语义检查语句.cn +17 行）经快速门禁入 develop——**D46 家族终态三段全收**（474 返回位+475 声明位+476 赋值位·474 挂等家机回签中后到=冗余判定无害）；**影响面**=v2 语义面（`r = 正常;` 裸名=编译期拒绝·与宿主对齐）；**集成后验证**=linux 本机 476=474/0/2 零新增红已签（gate_exit=0）·win/arm64 侧 24 族不回归随 474 回签轮覆盖。

- **✅ 475-a 集成完成广播（2026-09-20·develop b740de54·§8.2 步骤 7）**：D46 残余收口（v2 声明初始化位构造名裸名拒绝·语义检查语句.cn +17 行）经快速门禁入 develop——**影响面**：v2 语义面（`结果<...> r = 正常;` 裸名=v2 编译期拒绝·与宿主对齐）；474-a（返回语句位+458 负测）挂等家机 win 回签后另行集成（先入者生效·后入者 rebase 冗余判定无害）；**集成后验证**=linux 本机 476=474/0/2 零新增红已签（gate_exit=0）·win/arm64 侧 24 族/211_v2 不回归随 474 回签轮覆盖。

- **✅ 472-a 集成完成广播（2026-09-20·develop 359cd573·§8.2 步骤 7）**：T57 接管分支安全接入（objdel 撞号〔目标位=寄存器计数[0]〕+写穿〔目标槽守卫〕双根因根治）——生产态 476=474/0/2 全绿（本机 linux·自举链双 PASS+strict/matrix）。**影响面（§8.6）**：v2 树 生成语句赋值.cn 行为面——他机 v2 用例面（容器/类对象赋值调用右值形态）pull 后自然验证；若他机平台暴露新红按通告+修复分支。**关联**：469-a try-build（f1424474·A 类 4 例期望迁移）仍挂起等家机 win 回签（arm64 周一窗口）——其 rebase 需含本集成。

- **★try-build 请求（474-a·D46 销项·§8.3·点名 家机 win-x64〔arm64 周末豁免·周一（09-21）单位机补验窗口〕）**：分支 `任务/深度机-474-d46naked`（头 **36b7d900**·基点=develop aef637b6→已 rebase）——写集=`CN语言编译器v2/语义/语义检查.cn`（v2 树·返回语句位单点 +17 行）+`tests/e2e/458_构造名裸名拒绝/`+coverage_map（tests 触发三平台分类）→ 请家机在分支上跑 win 全量门禁并回签：
  - **内容**：v2 构造名裸名（`返回 正常;`/`返回 错误;`）编译期拒绝——71-a 被调标识符放行的裸名位漏洞封堵（D46 销项·p8 探针双侧实锤宿主拒/v2 曾静默）；`返回 正常(5);` 节点_调用不误伤。**契约广播（§8.6）**：v2 侧裸名形态静默→编译期拒绝——他机用例若锚定 v2 旧静默行为请核对（存量扫描=24 族 7/7+211_v2 健康实证·E2E 477 零新增红）。
  - **本机 linux 列结论（gate_exit=0）**：构建零警告+单测 1363/1363+全量 E2E `--jobs 8` **477=475/0/2**（458 新例入集·零新增红）+strict/matrix PASS+自举链 78/79 双 PASS+458 反证双态（恒假注入→诊断消失→恢复复绿）。
  - **家机（win-x64）**：ci.ps1 全量+重点=458 负测+24 错误处理族/211_v2 结果构造器不回归+自举链 78/79。
  - 回签后深度机 `--try-build-done` 集成；arm64 单位机周一补验窗口（458+457 两新例）。

- **✔ 回签：家机 467-a（T46 全局128静态发射·甲+丙）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 a40bf794）**：独立验证树检出分支+**本地 merge 最新 develop（合并结果门禁·文档冲突取 develop 侧）**跑 linux 全量门禁——**构建零警告（GCC -Werror）＋单测 1369/1369 PASS（118 套件·T46 新增 6 单测与贵机 win 数字一致）＋全量 E2E `--jobs 8` 477=475/0/2**（2 跳=62/69 平台固有·零失败零新增红）+strict/matrix PASS+自举链 78/79 双 PASS（在 475 通过集内）。**linux-x86_64 平台通过，准予集成**（贵机 `--try-build-done`·双签达成+周末 arm64 豁免口径·单位机周一补验窗口含 483 用例家族）。

- **★try-build 请求（469-a·撤守卫残余定性轮 A 类 4 例期望迁移·§8.3·点名 家机 win-x64 / 单位机 arm64〔周一补验窗口〕）**：分支 `任务/深度机-469-撤守卫残余定性`（头 **f1424474**·基点=develop 339ba21d+rebase 8415a43）——写集=`tests/e2e/{223,224,227,229}_v2_*/主.expected` 4 例（删 p6h「节点=N」计数脆弱锚·sem5 诊断锚逐行保留）+文档 → 请在分支头跑各自平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：**469-a 撤守卫残余定性轮**——撤守卫态（stdlib 源码级加载·实验态）全量 24 红三分类台账：A 类 4 例=p6h 计数锚两态冲突（撤守卫态合并解析 stdlib=3118 vs 生产态=382·sem5 诊断语义两态全对）→删脆弱锚两态自洽；B1 九例=T57 重设计挂号/B2 十例运行级/B3=79 fix_s（全部立案不涉本轮写集）。**考古三点**（462/464/466 集成点撤守卫 v2p 实测 228）实证 B 族非 464/466 回归。
  - **本机 linux 列结论（头 f1424474）**：撤守卫态四例 FAIL→全 PASS（反证第二态）+生产态四例抽查 PASS+**生产态全量 476=474/0/2 零失败**+自举链双 PASS+strict 100%+matrix 83/83。**断言语义不变**（sem5 诊断锚全保留·仅删解析计数行）。
  - **家机 win**：全量门禁+重点=223/224/227/229_v2 四例（生产态 v2p 真跑·期望删锚后应 PASS）+全量零新增红。
  - **单位机 arm64**：同分支全量（周一窗口·四例+全量）。
  - 回签后本机 `integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。

- **★try-build 请求（470-a·D10 双侧销项·§8.3·点名 家机 win-x64〔arm64 周末豁免·周一（09-21）单位机补验窗口〕）**：分支 `任务/深度机-470-d10v2`（头 **4a24e5f4**·基点=develop b3138c3→已 rebase f78e8cf7）——写集=`src/cn_compiler/semantic/generics.cpp`（宿主三平台共享层）+`CN语言编译器v2/语义/{语义检查.cn,语义检查表达式.cn}`+`tests/e2e/457_泛型实例体未声明拒绝/`+coverage_map → 请家机在分支上跑 win 全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：v2 泛型实例体重放语义检查（417-a「检查延后」欠账根治·体内未声明调用=编译期拒绝·此前静默到链接期爆）+宿主 recheckGenericFuncBody 重放诊断窄放行（「未声明」类 Error 回注·p1 宿主 build undefined reference→编译期拒绝·317-a 对齐补齐）。**契约变更（§8.6 广播）**：「泛型体内未声明调用」静默→编译期拒绝——存量扫描=v2 树/tests 泛型族全健康形态（E2E 477 零新增红实证）·他机用例若锚定旧静默行为请核对。
  - **本机 linux 列结论（两轮全绿·终态 gate_exit=0）**：构建零警告（GCC -Werror）+单测 1363/1363+全量 E2E `--jobs 8` **477=475/0/2**（457 新例入集·2 跳=62/69 平台固有·零新增红）+strict 179/179+matrix PASS+自举链 78/79 双 PASS+457 反证双态（挂点恒假→诊断消失→恢复 md5 一致→复绿）。
  - **家机（win-x64）**：ci.ps1 全量+重点=457 负测+泛型族（451~456）不回归+自举链 78/79+MoveCheckTest 断言面零改动确认。
  - 回签后深度机 `--try-build-done` 集成；arm64 单位机周一补验窗口（470 内容+457）。

- **✔ 回签：家机 459-a（波 4 首件 206-d 转移真槽位交接）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 bd58dff2）**：`git fetch` 后独立验证树检出分支+**本地 merge 最新 develop（f1bc9f5·含 451 集成·合并结果门禁·448-b 先例同款）**跑 linux 全量门禁——**构建零警告（GCC）＋单测 1363/1363 PASS（MoveCheckTest 断言迁移面全绿）＋全量 E2E `--jobs 8` 477=475/0/2**（477=476+460 新例·2 跳=62/69 平台固有·**零失败零新增红**）+strict/matrix PASS。**点名重点面全达成**：460_转移真槽位交接 真跑 PASS＋**197/197_v2 转移面不回归**（kind 2 表达式位放行后既有拒绝防线保持）＋78_v2/79_v2 自举链双 PASS。合并树文档冲突（交接/更新日志/021=分机节双方推进·coverage_map=登记并集）已按 develop 侧+并集消解——**正式集成时由贵机按写集归属解**。**linux-x86_64 平台通过，准予集成**（贵机按集成序 `--try-build-done`）。
- **附·451 集成后复验（深度机·2026-09-20）**：另一深度机会话 457/458 回签与家机 451 集成（603009d5）滚动间隙，本会话在 451 头（28c0738）+develop（4db7516）合并树独立复验=全绿（构建零警告+单测 1363/1363+E2E 476=474/0/2+strict/matrix+自举链双 PASS·门禁锁串行·日志 target/gate_451_linux.log）——**451 集成内容 linux 侧独立复验备案·零回归**。arm64 运行级仍归单位机周一补验窗口（本机无 aarch64 工具链·静态面家机 asm 289×3 已核）。

- **✔ 回签：家机 458-a（D8 第三步=x64 结果写入直写）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 175d107d）**：`git fetch` 后 worktree 隔离检出分支跑本机 linux 全量门禁——**构建零警告（GCC）＋单测 1363/1363 PASS＋全量 E2E `--jobs 8` 464=462/0/2（2 跳=62/69 平台固有·零失败零新增红）＋自举链 78_v2/79_v2 双 PASS**。**linux-x86_64 平台通过，准予集成**（贵机 `--try-build-done`·D8 三步 451/457/458 集成序由贵机定）。

- **✔ 回签：家机 457-a（D8 第二步=ARM64 操作数装载直读）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 8e05a3dc）**：`git fetch` 后 worktree 隔离检出分支（头 **8e05a3dc**·基点=451 接力）跑本机 linux 全量门禁——**构建零警告（GCC）＋单测 1363/1363 PASS＋全量 E2E `--jobs 8` 464=462/0/2（2 跳=62/69 平台固有·零失败零新增红）＋★重点面自举链 78_v2/79_v2 双 PASS**（arm64 代码路径双轮改造〔451+457〕叠加的最大 arm64 消费面=编译器本体自举·点名重点达成）。**linux-x86_64 平台通过，准予集成**（贵机按 451→457 序 `--try-build-done`·458 若先就绪按贵机集成序）。

- **✅ 448 集成完成广播（2026-09-20·develop bc051a8d·§8.2 步骤 7）**：`任务/深度机-448-撤守卫运行级收口` 全链集成（448 侦察+448-b 44#2 类对象拷贝初始化实参 ABI 分叉根治+**448-c=家机 447 呈报 112_v2 红处置**〔期望迁移实跑固化 459_v2 先例+dbg 残留四件全清〕+coverage_map 登记）——merge 三方并编入 develop（025 §2.76/§2.77/§2.78 并集·交接/更新日志本机节取 448-c 版）。**集成结果门禁（integrate.py 全量）**：单测 1363/1363+全量 E2E `--jobs 8` 465=463/0/2 零失败零新增红+自举链 78/79 双 PASS+strict/matrix ✓。**影响面（§8.6 契约广播）**：①112_v2 期望迁移入 develop——负测锚行从 sem5 旧形态改 ir2 新形态（断言语义=编译拒绝不变·他机复跑 `--filter 112_v2` 即验）；②dbg 残留四件清=v2p stderr 卫生（他机期望零依赖已核）；③**@家机：447 rebase 重测解锁**（112_v2 红已随 448-c 入 develop·集成序 447→450→452→456 维持·431 链 rebase 时 112_v2 主.expected 冲突取 develop 版即 448-c 版）；④@单位机：周一补验含 448 内容（442_v2/441/452 泛型实例矩阵/455/456 重点面+112_v2）。

- **✔ 回签：家机 456-a（D1 拆分 handleClassCallExpr）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 cbcea9ca）**：worktree 隔离检出分支跑本机 linux 全量门禁——**构建零警告（GCC）＋单测 1363/1363 PASS＋全量 E2E `--jobs 8` 464=462/0/2（2 跳=62/69 平台固有·零失败零新增红）＋自举链 78_v2/79_v2 双 PASS**（OOP 调用面=类构造/虚调用/接口/静态方法族全量覆盖不回归）。**等价性反证（asm 对照法·linux 列）**：同 cwd 同输入（两树 v2 树源码 diff 零差异）用 456 分支宿主 vs 基点 017bab76 宿主分别编译 v2 全树——**v2p .s md5 8caaf7a7==8caaf7a7 逐字节一致＋.o md5 2f96e082==2f96e082 一致**（与贵机 win 列 0683ee1e 铁证同构·机械重构零行为变更 linux 侧实锤；注：v2p 二进制 md5 直比不一致系 --output 路径嵌入差异·同路径复比即消=已排除）。**linux-x86_64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`·集成序 448→447→450→452→456 维持）。

- **★try-build 请求更新（448-c·撤守卫运行级收口续作·§8.3·点名 家机 win-x64 / 单位机 arm64〔周一补验窗口〕）**：分支 `任务/深度机-448-撤守卫运行级收口` **新头 cbd7e1d**（13d97d4→61d3ec2→336c7d3→cbd7e1d：112_v2 期望迁移+dbg 残留四件清+coverage_map 登记+六件套文档）——**请按新头 cbd7e1d 回签**（448-b 原请求内容不变：44#2 根治=类对象拷贝初始化实参 ABI 分叉→实参位1 改传源左值地址；撤守卫态 111/390/123/456 全通+反证双态）。**448-c 增量内容**：①家机 447 呈报的 `112_v2_方案A_有析构无拷贝构造负测` 真红按 §8.6 归本轮处置——期望迁移实跑固化（459_v2 先例·435 链「释放」已声明=正确演进·负测语义保持=ir2「类对象初始化仅支持构造调用」错误码6 仍拒绝）+dbg 残留四件全清（IR类.cn [dbg收文]+生成类.cn TEMP435b2/438c+生成函数.cn TEMP441b·grep tests/scripts 零依赖零误伤）；②**本机 linux 列新头终态**：单测 1363/1363+全量 E2E `--jobs 8` **465=463/0/2**（465=464+112_v2 首次入本分支门禁集·零失败零新增红）+strict 100%+matrix 83/83+自举链 78/79 双 PASS；③反证双态（112_v2 改前 FAIL 复现→改后 PASS 复绿）。
- **@家机（431 链交叠预告·§8.5）**：448-c 写集含 `tests/e2e/112_v2_方案A_有析构无拷贝构造负测/`（自 431-a 分支同内容·主.expected 已迁移固化新形态）——**431 链 rebase 集成时该文件冲突请取 448-c 版**（期望随 435/448 演进·plans/025 §2.77 §6）；112_v2 期望迁移属契约变更广播（§8.6·负测锚行从 sem5 旧形态改 ir2 新形态·断言语义=编译拒绝不变）。

- **✔ 回签：家机 451-a（D8/F1-28 ARM64 冗余 mov 消除首步）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 cce81380）**：`git fetch` 后在分支 `任务/家机-451-arm64冗余mov消除`（worktree 隔离）跑本机 linux 全量门禁——**单测 1363/1363 PASS＋全量 E2E `--jobs 8` 464=462/0/2（零失败零新增红）＋★重点面自举链 78_v2/79_v2 双 PASS**（arm64 代码路径变化的最大 arm64 消费面=编译器本体自举·点名重点达成）＋strict/matrix ✓。**linux-x86_64 平台通过，准予集成**（`--try-build-done`）。

- **✔ 回签：家机 452-a（C20 批5）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 8e012dfe）**：`git fetch` 后在分支 `任务/家机-452-C20批5`（worktree 隔离·其树=基点 453+11 新例=464）跑本机 linux 全量门禁——**单测 1363/1363 PASS＋全量 E2E `--jobs 8` 464=462/0/2**（**零失败零新增红**·注：贵机自报 2 红=406/431 系贵机基点树旧红名单——develop 435 链收编后 406/431 已绿·本机树对齐 develop 态实测 0 红）＋11 新例真跑全 PASS（192_v2/195_v2/198_v2/209_v2/223_v2 等全收集·抽验 192_v2/209_v2/229_v2 PASS）＋strict/matrix ✓。**linux-x86_64 平台通过，准予集成**（`--try-build-done`）。

- **★try-build 请求（448-b·撤守卫运行级收口·§8.3·点名 家机 win-x64 / 单位机 arm64〔周一补验窗口〕）**：分支 `任务/深度机-448-撤守卫运行级收口`（头 **13d97d4**·基点=662cd3a 接力）——写集=`CN语言编译器v2/IR/生成类.cn`（**单文件·+44 行**）→ 请在分支头跑各自平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：44#2 多余解引用根治——撤守卫态（stdlib 源码级加载）下 向量$整64 注册为用户类→主函数 `乙 = 甲` 走 生成类对象局部 419 分支（类对象路径）→拷贝构造实参位1 直传对象指针；克隆体形参侧 `向量& 其他` 走 发射引用参数读 容器& 双重解引用（引用槽模型）→**两侧 ABI 分叉**→形参侧多解一跳→下标基址=元素值→SEGV（r10=0xa·448-a 铁证吻合）。修复=实参位1 改传**源左值地址**（生成容器拷贝构造同款三分支+hold 物化兜底·指针地址化零对象拷贝=性能无损）。普通方法调用不受影响（其引用实参走 回扫实参取地址 表驱动本就自洽）。
  - **本机 linux 列结论（头 13d97d4）**：撤守卫态 111 运行 rc=0 全输出正确+390 rc=0+123 rc=85（预期 21333）+456 rc=27（预期）=**撤守卫族 A 运行级全通**；**反证双态**（回退修复→111 rc=139 SEGV 复现→恢复→rc=0）；生产态（守卫恢复）单测 1363/1363+全量 E2E 464=462/0/2 零新增红+自举链 78/79 双 PASS+strict/matrix ✓。
  - **重点面**：442_v2（类引用参数成员链）/441（容器赋值深拷）/452（泛型实例矩阵）/455/456 不回归+自举链。
  - 回签后本机 `integrate.py --try-build-done` 集成；集成序 448→447→450 维持。超时（>2h）挂起不降级。
- **455-T62 确认轮同轮广播**：T62（三例 v2p 无诊断残余面）在 develop 生产态实测**全有明确诊断**（349-a 修复已覆盖·135=`-> 成员访问已废除`/340/92=`v2 暂不支持运算符重载声明`）——**T62 实质销项**（与 T33 先例同款登记漂移·零代码确认轮·455 分支承载行勾）。
- **✔ 回签：家机 449-a（本机并行基建）+ 450-a（C20 批4）try-build（深度机 linux-x86_64 通过·2026-09-20·基线=分支头 9253f97）**：`git fetch` 后在分支 `任务/家机-450-C20批4`（头 **9253f97**·其树含 449-a 全部基建内容〔cd55526 为祖先·merge-base 实证〕·零 src 改动）跑本机 linux 全量门禁——**构建零警告（src 零变化增量全缓存命中）＋单测 1363/1363 PASS＋全量 E2E `--jobs 8`（MemoryMax=24G scope 包裹）475=473/0/2**（2 跳=62/69 平台固有·**零新增红**；450-a 新增 11 例〔135/159/163/265/275/279/319/326/356/82/83_v2〕全 PASS＋78_v2/79_v2 自举链双 PASS）＋**静态门禁四件全绿**（check_spec_coverage --strict 179/179=100%＋check_matrix_coverage 83/83＋check_handoff ✓＋check_progress_sync rc=0）。**449-a 基建 linux 侧专项实测**：gate_lock.py `run`/`acquire`/`release`/`status` 三态全通＋失败路径释放实证（run 结束后 status=空闲）＋integrate.py 语法与 CLI（--dry-run/--try-build-done）正常——**本机后续将按 AGENTS §8.8 口径（8核/31GB 收窄：活跃任务≤2+门禁串行 gate_lock）同机多开**。**linux-x86_64 平台通过，准予集成**（449-a/450-a 两请求各自 `--try-build-done`·集成序 448→447→450 维持）。**注**：check_progress_sync 报 021 文首戳（449-a）落后本分支最新轮（450）——系家机分支内文档时序、rc=0 非阻塞，随集成自然消解。

### 深度机 linux-x86_64 通告段（388-a 事故恢复）

- **✅ 服务轮·堆积链收编完成（2026-09-19·用户裁决第二波·develop=f9e91ae7）**：331/337/338/345~353 四链全部集成入 develop——①`任务/深度机-331-T11静默防线`→**07bb9e0c**（T11 静默防线+T49/T50/T51/T52·单测 1353→**1363**）②`任务/深度机-337-T53`→**25e600a5**（T53 家系闭环+440）③`任务/深度机-338-C19`→**f87bb26e**（C19 uninit 根治+OOM 事故备案+runner 内存名单扩面）④`任务/深度机-353-T60负号类型码`→**f9e91ae7**（merge 三方并编 345~353 全链：D33/T57/T58/T33+443/D11+444/T35 运算符重载拒绝/除法保护+446/T60+447·解析类.cn 语义合成=349-a 运算符检测×414-a 析构器段并立）。**develop 终态门禁：E2E 463=461/0/2 零红**（426 与 447 双转绿·2 跳=平台固有）+单测 1363+自举链 78/79 双 PASS。
- **★T60 补提备案（机制级·缺陷零容忍）**：353-a 收工时 447 用例+文档入库但**修复代码未 add**（遗留本地工作树）——收编集成门禁暴露 447-6a 红（2147483648 vs -2147483648）后当场补齐四处：生成层负号整数面产物码随操作数+登记（宿主 0-x 同域对齐）+三后端 32 位分派（win/x64l neg eax+movsxd·arm64 neg w9+sxtw）。反证：回退复现红/恢复复绿。
- **★integrate.py 三处机制缺陷修复（随 331 链入库·广播）**：①单测/cn 产物路径与 CMAKE_RUNTIME_OUTPUT_DIRECTORY 错位→补 target/ 兜底 ②E2E 漏传 --jobs→固定 8 ③--target 平台名错位（linux-x64→linux-x86_64 映射）——此前该脚本在 linux 侧从未真跑通过 E2E 步骤。
- **★@单位机：周一（09-21）上线第一优先=对 develop≥f9e91ae7 跑全量 E2E 补验**（本周末甲案+收编两波豁免的 arm64 事前回签一次补齐·覆盖 331/337/338/345~353 全部内容）；329-D12 由你自行集成。发现红立即开修复分支根治。

- **✅ 服务轮·甲案裁决执行+集成对账+develop 集成后验证（2026-09-19·develop 基线 402e7ff）**：
  1. **★用户裁决（2026-09-19·甲案·照抄裁决语）**：「本周末已获 win+linux 双签的收工链按集成序 413→…→423 正常集成（arm64 事前回签豁免，仅限本周末）；单位机周一上线后第一优先对最新 develop 跑全量 E2E 补验，发现红立即开修复分支根治；329-D12 留单位机周一自行集成。」——**@单位机：周一（09-21）上线第一优先=对 develop≥本轮提交 跑全量 E2E 补验；329-D12 由你自行集成**。
  2. **对账结果（实测三证据）**：413→414→416→417→418→419→420→421/422→423→425-b 链内容**已随集成队列滚动入 develop，无需新集成**——①448~455 新用例全在 develop 树；②T57 修复指纹「生成容器拷贝构造」命中 v2/IR/生成声明.cn；③421/423 旧分支头 vs develop 树 diff 仅 3~8 文件且方向=develop 领先（旧分支为滞后快照）。
  3. **develop 集成后验证（本机 linux·基线 402e7ff）**：构建零警告＋单测 **1353/1353**＋全量 E2E **452=449/1/2**（唯一红=426〔T50 修复在未集成的 331 链·基线既有〕·**零新增红**·2 跳=平台固有）＋strict 100%＋matrix 83/83＋自举链 78/79 双 PASS。
  4. **剩余欠账对账（呈报待授权）**：430~447 用例仍未入 develop（331/337/338/345~353 堆积链·家机 09-19 盘点通告仍现行）——收编顺序敏感（331-a 先〔T50 让 426 转绿〕·346-a 先于 347-a）·**已呈报用户待授权，未经授权不擅自收编**。

- **🔄 426-b 前置收工+try-build 请求（2026-09-19·分支 `任务/深度机-421-stdlibIR段攻坚` 头 0efa28c·同分支续作 421~426）**：**族② 攻坚（多文件加载 静态表生命周期审计）**——425-b 铁证=解析类声明 登记段零执行（stdlib 源码级加载 的「解析期登记全部丢失」）；**dbg 收文 表大小=0**（用户类们 在 IR 收集期断裂）；方法参数变量类型登记+实例布局裸键直设+候选三件套已落地（422/423 分支）。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS·守卫恢复态）。**棘轮状态**：plans/025 滚转第一批（843→720·基线 700 还差 20）+plans/021 待滚转（613/基线 450·426-b 续）。**集成序：413→414→416→417→418→419→420→421（含 422/423 续作）**。427-a 已被家机占·深度机下轮=428。

- **✅ 424-a 定位轮收工+try-build 请求（2026-09-19·分支 `任务/深度机-423-IR诊断宿主定位` 头 64997bc）**：**stdlib 源码级加载 铁证链完整**——dbg 收文=收集三实例基名剥取正确但 **是用户类(基名)=0**（用户类登记的跨阶段可见性断裂·424-b=用户类们 快照/清空时序审计）+dbg 类注=实例化注册 实例数=0（波 2 实例化链未触发）+dbg 偏移=引用参数兜底 ID=0（方法参数登记 423-b 已落地）；类型大小 K/V ×48 族① 清零维持。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS·守卫恢复态）。**集成序：413→414→416→417→418→419→420→421→423**。

- **🔄 423-b 收工+try-build 请求（2026-09-19·分支 `任务/深度机-423-IR诊断宿主定位` 头 d8ceefe）**：**族② 攻坚（诚实边界）**——方法参数的函数级变量类型登记落地（引用参数 & 剥离+本类参数→实例 ID 登记）+实例布局裸键直设（键基合成残留修复·dbg 布局 铁证=映射 7 字段 56 字节登记正常）——族② 收敛=元素数量（非指针）×9 消——**剩指针字段 ×72**（引用参数「其他」的 查当前函数变量类型=-1 铁证·登记值键域待 424-a）+族③装箱语境 ×12——守卫恢复。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→…→423**。424-a=登记值键域修正（实例合成名直登记）→撤守卫+390。

- **✅ 422-a 收工+try-build 请求（2026-09-19·随分支 `任务/深度机-421-stdlibIR段攻坚` 头 0f213d4·同分支续作 421/422）**：**族① 达成＝类型大小 K/V ×48 全消**（宿主锁定=映射$整64$整64 克隆体四方法·当前生成符号 静态打印法；类版标识符替换块补回——420 清理误删·教训入册）+**族② 部分收敛**（元素数量 ×9 消·变量结构体表登记）——**剩指针字段 ×72**（引用参数自身解析链·兜底结构体ID=0 铁证·423-b）+装箱语境 ×12；守卫恢复。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417→418→419→420→421+422（同分支）**。423-b=引用参数解析链审计→390 终验收。

- **🔄 421-c 收工（2026-09-19·同分支 `任务/深度机-421-stdlibIR段攻坚` 头 a3d8b09）**：**函数实例延迟绑定·编码完成验收未达（诚实边界）**——替换实例类型文本 $ 分段感知（查找值位置$T→查找值位置$整64）+延迟展开段（判定→定位基函数→类实例绑定展开→克隆+签名登记）编译通过；撤守卫实测 123 诊断依旧 132＝**平行路径仍未清**——422-a 首务=IR 诊断处加 当前生成函数名/克隆体标记 静态打印（一次定位宿主）→按宿主逐修→撤守卫+390 终验收。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS·守卫恢复态）。**集成序：413→…→421**。

- **✅ 421-b 定位轮收工（2026-09-19·同分支 `任务/深度机-421-stdlibIR段攻坚` 头 3c9b272）**：**「类型大小 K/V ×48」平行生成路径定位达成**——dbg 类递归 铁证=泛型实例表含 **查找值位置$T**（**类参数作泛型函数实参的延迟绑定缺失**·T 未绑定实例被注册克隆·T→T 替换无效）；4 实例=[查找值位置$T, 向量$整64, 映射$整64$整64, 向量$字符串]（3 克隆类节点差额=函数实例无类节点 ✓）。**421-c 根治方案**=函数实例延迟绑定（收集对实参含类参数名不立即注册·外层类实例克隆时展开）。**写集触 tests/**→三平台 try-build：请回签（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+自举链 78/79 双 PASS·定位轮零行为面变更）。**集成序：413→…→421**。

- **🔄 421-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-421-stdlibIR段攻坚`·基点=420 收工头接力）**：**stdlib IR 段攻坚·诚实未完成拆分**——族① 克隆器标识符位替换落地（类版+函数版·铁证 标识换=4/8）但 类型大小 K/V ×48 依旧＝存在未替换平行生成路径（线索：4 实例 vs 3 克隆类节点+向量基类递归未进·dbg 类分派 在案）·族②③ 待族①定位·守卫恢复。**写集触 tests/**→三平台 try-build：请回签**（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417→418→419→420→421**。421-b=平行路径定位。

- **✅ 420-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-420-克隆体参数偏移根治`·基点=419 收工头接力）**：**克隆体参数槽偏移根治 ✅**——根因=克隆区间漏块节点（函数节点.子终点=块索引**单点语义**）·子终点+1 闭式含块——probe15=5/17=3/16=88 全链回绿（T* 指针参数下标+参数传递+克隆体单态化**运行级**）+455_泛型函数单态化 新上架；顺带修四枚（函数实例收集过滤/分派索引错位/语义放行 -1 哨兵越界/函数实例签名登记）。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 回签**（本机 linux 列=全量 452=449/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417→418→419→420**。stdlib 源码级 IR 段缺口清单已实测立案 421-a（守卫维持·攻坚后撤守卫+390 终验收）。

- **🔄 419-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-419-D42波3收口`·基点=418 收工头接力）**：**D42 波 3 部分完成（诚实边界）**——调用点 名<实参>(...) 解析+语义双点放行（修正 -1 哨兵被上层当类型索引进向量的越界·gdb bt 归因）+函数实例收集+克隆器函数版+生成遍历双版分派——常量返回探针 88 验证协议层全通；**未达成=克隆体参数槽偏移错位**（prologue 读 [rbp+288]·gdb 铁证·立案 420-a）·守卫恢复原名单。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 回签**（本机 linux 列=全量 451=448/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417→418→419**。420-a=参数偏移根治·421-a=波 3 收口重试。

- **✅ 418-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-418-枚举全链`·基点=417 收工头接力）**：**枚举全链 ✅＝D40 最大子项·stdlib 源码级加载硬前置**——声明解析（显式值/默认递增）+枚举表（驻留 ID 全局唯一跨模块直查）+语义三层放行+IR 成员链常量折叠+跨模块导入（模块成员/公开登记·可见性白名单补枚举）；454_枚举全链 新上架（v2 闭环 多文件·运行级 126）。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 回签**（本机 linux 列=全量 451=448/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417→418**。下一轮 419-a=函数版单态化+撤守卫+390 根治（波 3 收口）。

- **✅ 417-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-417-D42甲波3`·基点=416 收工头接力）**：**D42 波 3 前置件①**＝泛型函数声明解析（stdlib 线性表辅助同款·函数名共用参数序表）+泛型函数/基类方法体检查延后（rustc 同构）+**撤守卫侦察＝stdlib 源码级加载缺口阶梯实测立案**（解析泛型函数声明 ✅修→T 语义 ✅延后→枚举全链缺失〔D40 细分·418-a 候选〕→函数版单态化〔波 3 主件〕）；453_泛型函数声明 新上架。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 回签**（本机 linux 列=全量 450=447/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序：413→414→416→417**。守卫恢复·stdlib 供给模型维持至前置件齐。

- **✅ 416-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-416-D42甲波2`·头 d0aef98·基点=414 收工头 e2376c7 接力）**：**D42 甲案波 2 ✅＝IR/生成层单态化实例**——实例收集+AST 克隆单态化（实例注册为完整用户类·既有链零修改复用）+构造符号两侧对齐；451 扩面方法调用转绿+**452_泛型实例矩阵 新上架**+**向量<整32> 运行级 242**。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 `git fetch` 后在该分支跑各自平台全量门禁并回签**（本机 linux 列=全量 449=446/1/2 零新增红〔八枚中间回归当场根治〕+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS）。**集成序（顺序敏感）：413→414→416**。

- **✅ 414-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-414-D42甲波1`·头 e2376c7·基点=413 收工头 d1e3efb 接力）**：**D42 甲案波 1 ✅＝泛型类源码形态 解析+语义层**——`泛型 <类型 T> 类 名` 声明全形态（双参数 映射<K,V>·约束消费·泛型参数名们 登记+检查类型引用 豁免）+顺带根治三枚 stdlib 前置解析缺口（常量 函数 17 处/常量 不安全 函数 双修饰/不安全 析构器 ~名 名ID 取错错位）——**容器.cn 解析层零诊断·302 解析崩消失**；新用例 451_泛型类源码形态。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 `git fetch` 后在该分支跑各自平台全量门禁并回签**（本机 linux 列=全量 448=445/1/2 零新增红+单测 1353+strict 100%+matrix 83/83+自举链 78/79 双 PASS·基线=413 链）。**集成序（顺序敏感）：413 分支先于 414**。下一轮 416-a=波 2 单态化（415 已被贵机 D11 侦查认领·顺延）。

- **✅ 413-a 收工+try-build 请求（2026-09-19·分支 `任务/深度机-413-条件产物存储`·头 7491fcf）**：**D45 贯穿恒真根治+D44 选择正体收口 双销项**——真根因=409-a 合成 ‖ 子终点=自身+1→短路求值零发射（旧「净栈假平衡」「产物槽缺失」定性推翻·lessons 权重 9）；选择语句宿主 genSwitch 判断链对齐重写（真节点+体块直落 fall-through+中断最近绑定+字符串 case）；新用例 450_选择贯穿矩阵。**写集触 tests/**→三平台 try-build：请家机 win/单位机 arm64 `git fetch` 后在该分支跑各自平台全量门禁并回签**（本机 linux 列=零警告+单测 1353+E2E 447=444/1/2 零新增红+strict 100%+matrix 83/83+自举链 PASS·基线 412 链 61b351e）；**另：★D42 甲案已获用户裁决「同意甲：泛型类源码编译全链，分波推进」**——下一轮 414-a=波1（解析+语义层）·449/02_control 等族不影响。
- **★★事故通告+紧急回签请求（388-a·2026-09-19·破坏者即修 §8.6）**：深度机在任务分支上下文误执行 `git rebase`+`git push HEAD:develop` 组合——**387 收工内容未经 try-build 回签直入 develop**（668d7b1·16 文件·触 v2 树+tests）=违反 §8.2 队列纪律·污染两机 try-build 隔离性（回签者 rebase 会连带带上 387 内容）。**处置**：恢复分支 `任务/深度机-388-误推恢复`（头 a1b80d0）＝**develop 内容基线回退至 190f97e**（回签前态·387 分支 8079628 保留待回签后正规 integrate）。**请家机 win/单位机 arm64 对该恢复分支快速回签**（一次全量门禁·预期全绿=恢复已知基线）——绿后深度机 `integrate.py --try-build-done` 集成。本机 linux 列门禁（构建零警告+单测+E2E 全量）跑完随本通告回填。根因=操作纪律失误（看板直推的 push 模式误用于任务分支收工态）——lessons 已记。
- **✔ 387-a 收工内容（已在 668d7b1 误推·亦在 8079628 分支）**：s1=20/s2=99（386 修复链生效）+02_control 四段双端一致（fall-through 唯一差异=D44 立案·整数别名=D43 立案）+377~386 记录缺失补录（§2.38）+check_handoff 上限 600→620 契约广播——该内容**随 387 分支回签后正规重入**，回签时以 8079628 rebase 最新 develop 为准。

### 深度机 linux-x86_64 通告段

- **✅ 479-a 集成完成广播（2026-09-20·develop a726a6f1·§8.2 步骤 7）**：F1-26 波1 Mem2RegPass（mem2reg.hpp/cpp ~230 行·逃逸判据+环境不动点+读侧跨块直递·Store 保留·-O3 注册 SSAPass 后）经双签（linux gate_exit=0+win 回签 37a03be0）入 develop——**影响面（§8.6）**：优化层产物变化（-O3 路径 Load 直递）——他机集成后验证重点=384 双编译对等+asm 宽度+全量零新增红（本机 linux 477=475/0/2 已签+win 回签已到）；**锚定链固定点行数**随 -O3 产物变化（波3 重锚轮统一处理·78/79 自洽性本机已验 PASS）。

- **★try-build 请求（435-b·点名 家机 win-x64；arm64 依周末窗口豁免）**：分支 `任务/深度机-435-自举回归收口`（头 **ec37b3a**·含 428/429/430/432/433/434 线性链）——写集=CN语言编译器v2/**（撤守卫攻坚七层+433 五修：克隆修针/二元附加/构造重载表/内存内建/复制兜底/引用回扫/字段指针层/指针加载/字段自增）+tests/e2e/456（新）+coverage_map——linux 侧已全绿：**E2E 453=451/0/2 零失败**（含 456 rc=27 新用例）＋78/79 自举链双 PASS＋strict 100%＋matrix 83/83。请 win 在分支头跑 ci.ps1 全量门禁并通告段回签（平台+结论+基线 ec37b3a）——回签后本机 `integrate.py --try-build-done` 集成。**撤守卫（stdlib 源码级加载）为实验态**：生产态守卫已恢复（123 rc=0）。

### 深度机 linux-x86_64 通告段（359/362 收工+358 回签·历史）

- **✔ 375-a 收工+集成广播（B1 甲·2026-09-19·develop 头 d39766a4）**：185_v2/125_v2 布尔语境改写+**转双编译对照通道**（v2闭环.txt→双编译对照.txt）已集成 develop。**契约变更通告（§8.6）**：两用例配置文件变更（通道+锚定方式=退出码 185=14/125=0）——**家机 win／单位机 arm64 集成后复跑此两用例**（`--filter 185_v2`/`--filter 125_v2`·双编译对照=宿主+v2 同测；linux 列已全量 444=441/1/2 验证·唯一红 426=T50 基线既有）。

- **✔ 回签：家机 358-D10 try-build（深度机 linux-x86_64 通过·2026-09-18）**：`git fetch` 贵分支 `任务/家机-358-D10`（头 **76d68da**）→ 检出分支零警告构建（GCC·**0 警告**）+ **单测 1353/1353 PASS** + 全量 E2E `--jobs 8` **444=439/3/2**（3 红全基线既有 125_v2/185_v2〔T55 族·本基点链不含 347/353 修复〕+426〔T50 在 331-a〕·**零新增红**）——D10 泛型函数体语义防线在 linux 侧正常（泛型面用例 26_generics 族零误报）。**linux-x86_64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`；单位机 arm64 回签后即可）。

- **★try-build 请求⑧（2026-09-18·357-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-357-三段式循环`（头 **737984e**·基点=356-a 头 07fee2a〔接力〕）——写集含 `CN语言编译器v2/**`（IR常量+解析语句+生成语句+语义检查 四文件）+`tests/**`（新用例 448）→ 请在**分支上**跑各平台门禁并回签：
  - **内容**：**D39 全销项**——v2 三段式 `循环 (init; cond; step) { 体 }` 全链（规范 plans/001:363）：解析三段分派（step 自实现 ++/--/赋值·当式宽容删除对齐宿主拒绝）+生成循环 原生四块序（**继续→步进块=C for 同款 continue 语义**）+语义循环头作用域。**新行为广播（§8.6）**：v2 现支持三段式循环——`循环 (整32 i = 0; i < N; i++)` 形态从「缺少分号」断裂转全链可用；他机 v2 用例可用了。
  - **家机 win / 单位机 arm64**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（零新增红）+**重点=448 八形态真跑**（基本/继续→step/中断/init 作用域/空 cond/赋值 step/嵌套/自减 step）+446/447 不回归+78_v2/79_v2 自举链。
  - **本机 linux-x86_64 列结论（头 737984e）**：零警告+单测 **1353/1353**+448 双编译 PASS+全量 **445=440/3/2**（3 红全基线既有·零新增红·153 本基点绿=v2 树版本差异非本轮〔355-a 记录〕）+自举链双 PASS+**反证**（stash 回退→探针复现「语句缺少分号」→恢复复绿）。
  - 回签后集成（**顺序：…→355-a→356-a→357-a**）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑦（2026-09-18·355-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-355-CLI目录根治`（头 **14ab50b**·基点=develop 79f1066·**独立写集**）——写集=`src/cn_main.cpp`（**宿主三平台共享层**）→ 三平台门禁：
  - **内容**：**宿主 CLI 目录创建根治**（351-a 发现缺陷·§8.4 机制级当场修）——①`CliOptions.target` 默认=**编译期按宿主平台**（`_WIN32`→win-x64／`__aarch64__`→linux-arm64／否则 linux-x86_64·显式 --target 覆盖；**win 宿主默认不变零影响**）；②`ensureDirExists`/`ensureTargetDir` 非 Windows 分支=POSIX `::mkdir` 逐级（去 system——原 `"mkdir -p \"...\""` 的 `\"` 被 sh 当转义引号→linux 无旗标 run/build 必坏）；③补 `<cerrno>`。
  - **家机 win**：零警告构建+单测+全量 E2E `--jobs 8`（**重点=win 宿主默认 target 仍 win-x64 零变化**·矩阵/CI 全绿）。
  - **单位机 arm64**：arm64 宿主构建+E2E（**重点=无旗标默认变 linux-arm64**〔原必坏〕·CLI 矩阵）。
  - **本机 linux-x86_64 列结论（头 14ab50b）**：零警告+单测 1353+**CLI 矩阵 50/50**+无旗标 run/build·中文源目录·中文 --output·深层目录四形态全通+全量 **444=439/3/2**（3 红全基线既有 125_v2/185_v2+426·零新增红；153 在本分支基点转绿=v2 树版本差异非本轮〔写集仅 CLI 层〕·集成后以合并结果为准）+**反证**（回退→缺陷逐字节复现→恢复 rc=0）。
  - 回签后集成（**顺序：…→353-a→355-a**·355 独立无接力）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑥（2026-09-18·353-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-353-T60负号类型码`（头 **3eeed35**·基点=351-a 头 888a169〔接力·依赖 351-a 32 位位宽分派〕）——写集含 `CN语言编译器v2/IR/生成表达式运算.cn`+`代码生成/**`（三后端 IR_负）+`tests/**`（新用例 447）→ 请在**分支上**跑各平台门禁并回签：
  - **内容**：**T60 全销项**——①一元负号整数面产物码随操作数+整数面登记（原硬编码 T_整64 且不登记 → `-1` 抬公共提升进 64 位域·`a32/-1`=2147483648 vs 宿主 -2147483648）；②**同族面** IR_负 三后端补 32 位分派（`打印(-a32)` 形态 447 首跑实锤·维度枚举轴②半截机制当场拦截）；③新用例 **447_负号类型码矩阵**（13 形态双编译·退出码=0）。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（零新增红）+**重点=447 win v2 产物真跑**（`-a32`=-2147483648 回绕等 13 行）+446/423 不回归+自举链。
  - **单位机 arm64**：同分支 arm64 构建+E2E 全量/抽测+**重点=447 arm64 v2 产物**（`neg w9`+`sxtw` 分派·预期与宿主同值）。
  - **本机 linux-x86_64 列结论（头 3eeed35）**：零警告+单测 **1353/1353**+447 双编译 PASS+446 不回归+全量 **451=445/4/2**（4 红全基线既有·零新增红）+自举链双 PASS+**反证**（回退四文件→p3 复现 2147483648→恢复复绿）。
  - 回签后集成（**顺序：…→349-a→350-a→351-a→353-a**）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑤（2026-09-18·351-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-351-除法保护三后端`（头 **6071e27**·基点=350-a 头 2ca36de〔接力〕）——写集含 `CN语言编译器v2/代码生成/**`（ARM64+win 两文件）+`tests/**`（新用例 446）+`scripts/check_handoff.py`（平台无关门禁·本机已三情形实证）→ 请在**分支上**跑各平台门禁并回签（平台+结论+基线 commit）：
  - **内容**：**423 SIGFPE 阶段二（三后端闭环）**——①arm64 `IR_除/IR_取余`：除零检查（`cbnz x10`/`movz x0,#1`/`bl __cn_runtime_error`）+32 位 w 寄存器分派（`sdiv/udiv w9`+`msub w9`+`sxtw/uxtw` 回填）——**有符号 INT_MIN/-1 免特判**（ARM DDI 0487：SDIV 溢出商=被除数·w 算术 2³² 回绕=B12 甲语义）；②win 同 x64l 三段式双保护（除零 `mov rcx,1`+shadow space `sub/add rsp,32`+`cmp r11,-1` 特判 neg/0+32 位 `cdq`/`idiv r11d` 分派·EXTERN 自动收集）；③新用例 **446_除法保护矩阵**（13 形态：INT_MIN/-1×32/64+负值+无符号+变量除零→运行时错误(1)·双编译·退出码=1）；④check_handoff 开工自检补「远端 develop 看板」判据（接力分支基点早于看板直推的误拦根治·三情形实证）。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红）+**重点=446 win v2 产物真跑**（INT_MIN/-1 回绕=-2147483648/-9223372036854775808·除零 rc=1「运行时错误(错误码1): 除零错误」）+423 不回归+78_v2/79_v2 自举链。
  - **单位机 arm64**：arm64 目标零警告构建+E2E 全量/抽测+**重点=446 arm64 v2 产物真跑**（预期与 win 同值——sdiv w/x 架构回绕+除零 rc=1；本机仅交叉 asm 静态指纹·13/13 除法点 `cbnz` 检查实证）。
  - **本机 linux-x86_64 列结论（头 6071e27）**：零警告构建+单测 **1353/1353**+**446 双编译 PASS**（宿主 rc=1=v2 同输出同码）+全量 E2E **450=444/4/2**（4 红全基线既有 125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+自举链 **78_v2/79_v2 双 PASS**+**反证**（回退两文件→重建→win 旧 `idiv rcx`×9/arm64 旧 `sdiv x9`×5 复现·保护 0 处→恢复→446 复绿）。
  - 回签后集成（**顺序：…→349-a→350-a→351-a**）；超时（>2h）按 §8.3 挂起不降级。

- **⚙ 集成预演结论（346-a/347-a 分支·2026-09-18·供集成时执行）**：在临时分支实测 `git rebase gitcode/develop`——**代码/测试零冲突**（`CN语言编译器v2/**`、`tests/e2e/**` 均干净应用）；**冲突仅四份共享文档**（HANDOFF.md／更新日志.md／plans/021／plans/025），根因=分支基点 3e62e5e 携带了 345-a 会话时点的文档快照（含 develop 之后已重整的节），而 develop 侧已前进。**解法（集成时执行）**：①`git rebase gitcode/develop` 遇文档冲突时**取分支侧（`git checkout --theirs <文档>`）**——分支侧=最新分机内容（§2.5/§2.6/346-a/347-a 全节）；②随后**必须核对** develop 是否有分支缺失的他机内容：`git diff <已 rebase 分支> <rebase 前 develop> -- HANDOFF.md 更新日志.md plans/021*.md plans/025*.md` 逐段比对，缺失段手工补回（他机分节=家机/单位机节·本机不擅动）；③`--continue` 后跑双门禁（check_handoff+check_progress_sync）+全量门禁（写集含 src? 无——346-a/347-a 均为 v2 树+tests，按 §8.2 步骤5 跑零警告构建+单测+E2E 全量）。**已实证**：预演中 3e62e5e 的文档 patch 对 develop 冲突（更新日志单位机节 341-a vs 332-a、plans/025 节结构 §2.4 有/无）——**这正是取分支侧的判据**（分支侧含全部节）。

- **★呈报：D11 确认（347-a 侦察·P1 静默错值·双端）——待裁决**：`成员/下标目标 = 结构体返回调用`（如 `甲.内 = 造点(20); 表[1] = 造点(30);`）**宿主与 v2 双侧静默错值**（探针 p29 铁证：宿主 `x=140737058366440`〔地址值〕/`y=z=0`；v2 同型 `x=140730529978928`）——整体赋值通道右值源**白名单**（宿主 `emitStructWholeAssign`／v2 `尝试成员结构体整体写`）不含「一般结构体返回调用」→ 落标量 `StorePtr`（只写返回值地址低 8 字节·无诊断）。**234-a 曾放开=回退**（注释留档：「一般结构体返回调用可能含条件块〔正常/错误构造器〕，在 dst 地址已发射后触发会打乱调用方块时序」——全量 v2 树编译 285 失败）。**方案**：**甲（推荐·Rust place 语义）**=调整发射顺序「先求值右值（源头·可能含块）→ 再算 dst 地址（纯地址计算·无块）→ CopyStruct」·宿主+v2 双侧同构（改动面=两处调用点顺序）；乙=仅放开「无控制流的结构体返回调用」（检测调用体是否含块·覆盖减半但改动最小）；丙=编译期拒绝该形态（fail fast·宿主+v2 双侧诊断·放弃能力）。**影响面**：P1 静默错值（双端·静默=最危险形态）；**存量未盘点**（不知多少用例/树代码依赖）——修复轮须先全库扫描+逐点实测。**登记**=plans/021 §3-D D11 行（已更新为「已确认缺陷·待裁决」）。

- **★C20 批C 成果广播 + 新立案（349-a·§8.6·影响全机双测口径）**：**4 例宿主-only 用例已双测上架**（`307_函数参数边界`／`410_布尔短路汇合矩阵`／`412_字符串下标越界`／`418_深嵌套限内运行`——双编译对照·断言输出=是·借链=是）；**他机影响**：`git pull` 后这 4 例在 v2 侧真跑（若他机平台暴露分叉，按 §8.6 通告+修复分支）。**新立案（P1·候选 T59）**：`423_整数溢出回绕统一` 的 **v2 产物运行 SIGFPE（退出码 -8）**——v2 生成代码对 `INT_MIN/-1` 类除法/取余未保护（宿主侧=编译期硬错误或运行时错误码）；**待裁决是否立案修复**（另：`352/390/406` 三例宿主-v2 输出分叉=新发现·待逐例定位）。**残留定性**：`155/30/31`=C20 已知符号供给欠账（290-a）·`346`=D36/T47 面·`379`=移位观察·`385/386`=T33 残余。

- **★try-build 请求④（2026-09-18·349-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-349-运算符声明`（头见本机行·基点=348-a）——写集含 `CN语言编译器v2/语法/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签：
  - **内容**：**T35 ①类根治**——v2p 编译 `340`/`92`/`52_library` 三例**无诊断崩溃（错误码 302）**→ 优雅拒绝：①解析层「`函数 运算符X(...)`」函数名 = 运算符 + 记号 组合（三处）+ 诊断 + 语法错误加一；②`->` 后缀位**硬错误 302 → 可恢复诊断**（+ 位置前进防死循环）→ 聚合中止通道。新负测用例 445（v2 闭环负路径）。
  - **家机 win / 单位机 arm64**：零警告构建 + E2E 全量/抽测（期望零新增红；**重点**：v2p 解析层改动=平台无关，但三平台产物一致性请以全量 E2E 覆盖）+ 新用例 445 + 78_v2/79_v2 自举链。
  - **本机 linux-x86_64 列结论**：零警告构建 + **全量 E2E 448=442/4/2**（4 红=基线既有 T55×3+426·**零新增红**）+新用例 **445 PASS**+自举链 **78_v2/79_v2 双 PASS**+零回归探针十项（p1/p4/p8/p11/p13/p22/p23/p24/p28/p29）+**反证**（回退三文件→重建→a4 复现 302）+三例全部优雅拒绝（2/2/9 语法错误·中止）。
  - 回签后集成（**顺序：…→346-a→347-a→348-a→349-a**）；超时（>2h）按 §8.3 挂起不降级。
- **★设计口径广播（349-a·影响他机 v2 用例编写）**：**v2 对「运算符重载声明」的现行语义 = 「诊断 + 语法错误加一 + 聚合中止」**（原为无诊断崩溃 302）——他机若写 v2 负测，期望应为 `[parse] v2 暂不支持运算符重载声明` 而非崩溃码；`->` 后缀位同款（原 302 崩 → 现诊断）。**宿主侧不变**（宿主支持运算符重载·拒绝语义由 E2E 135 独立锚定）。

- **★try-build 请求③（2026-09-18·348-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-348-整体赋值顺序`（头见本机行·基点=347-a）——写集含 `src/**`（**宿主三平台共享层**）+`CN语言编译器v2/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签：
  - **内容**：**D11 根治**——`成员/下标目标 = 结构体返回调用`（`甲.内 = 造点(20)`）**宿主与 v2 双侧静默错值**（首字段=返回值地址·p29 铁证）→ 甲方案「源优先序」（先求值源〔含条件块〕→ 再算目标地址〔纯地址〕→ 整体拷贝）宿主 +2 挂点／v2 成员链分支 + 新下标通道；新用例 444（五形态·双编译）。
  - **家机 win**：零警告构建（MSVC `/W4 /WX`）+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红·**win 侧为宿主直接受益面**）+新用例 **444 应 PASS**+78_v2/79_v2。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测（宿主 arm64 后端同为受益面）。
  - **本机 linux-x86_64 列结论**：零警告构建+单测 **1353/1353**+全量 E2E **448=442/4/2**（4 红=基线既有 T55×3+426·**零新增红**）+新用例 **444 PASS**+自举链 **78_v2/79_v2 双 PASS**+探针 p29 双侧一致+**反证**（回退 4 文件→重建→复现「地址值/0/0」）+**存量盘点**（成员 66/下标 27 处宽扫描·全量覆盖验证）。
  - 回签后集成（**顺序：…→346-a→347-a→348-a**）；超时（>2h）按 §8.3 挂起不降级。
- **★教训广播（348-a·全机相关）**：**新增赋值/发射通道的守卫必须含类型面**——D11 首版守卫含 `isClassType` → 抢先命中 v2 树大量 `程序.函数们 = 向量<...>();` 形态并以浅拷覆盖**既有类深拷通道** → **v2p 自身全探针段错误（错误码 3）**；修正=「仅结构体目标进新通道·类/容器一律放行既有深拷」+ 求值失败不落原路径（防重复求值）。**他机写 v2 树改动时同款纪律**。

- **★try-build 请求②（2026-09-18·347-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-347-窄宽符号扩展`（头 **3edd947**·基点=346-a 头 24e9bb3）——写集含 `CN语言编译器v2/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签（平台+结论+基线 commit）：
  - **内容**：**T33 残余根治**（v2 窄宽类型符号性——三后端 `IR_指针加载` 整8/整16 补**符号扩展**〔原零扩展→负值错值 65110/156〕+ 正8/正16 补**零扩展**〔原落默认 8 字节读写=过读/**过写越界**〕+ `类型宽度码` **四码拆分**〔原 整8/正8、整16/正16 归并丢符号性→正8 200 读回 -56〕）+ 新用例 443_v2窄宽符号性矩阵。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红）；**重点=win 后端窄宽分派静态对称性**（本地无 MSVC·`movsx`/`movzx` 对称归贵机）+ 新用例 **443 应 PASS** + 78_v2/79_v2 自举链。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测；**重点=运行级**（`ldrsh`/`ldrsb` 实际行为·本机仅交叉 asm 静态验）。
  - **本机 linux-x86_64 列结论（基线 24e9bb3·头 3edd947）**：零警告构建 + 单测 **1353/1353** + 全量 E2E **447=441/4/2**（4 红=基线既有 125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+ 新用例 **443 PASS** + 自举链 **78_v2/79_v2 双 PASS** + 探针 p22/p23/p24（六形态与宿主逐行一致）+ **反证**（`git checkout HEAD~1 --` 4 文件 → 重建 v2p → p24 复现 **156/65110**）+ **三后端 asm 指纹**（x64l `movsx r10, word/byte ptr`×5+`movzx`×2；arm64 交叉生成 `ldrsh`/`ldrsb` 命中）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成（**顺序建议：346-a 先、347-a 后**〔347-a 接力 346-a〕）；超时（>2h）按 §8.3 挂起不降级。
- **★try-build 请求（2026-09-18·346-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-346-容器赋值深拷贝`（头 **24e9bb3**〔原 589ac41→+valgrind 证据〕·基点=345-a 头 3e62e5e·基线 023c3c7）——写集含 `tests/**`+`CN语言编译器v2/**`（三平台产物相关）→ 请在**分支上**跑各平台门禁并在本段回签（平台+结论+基线 commit）：
  - **内容**：**T57 根治**（v2 容器/用户类对象「赋值语句」深拷贝——修复双释放 UAF）+**T58 根治**（类对象成员链左值基址=对象指针·崩溃级〔v2 类成员链赋值全段错误〕）+ **D33 旗标对等**（runner v2p 调用点 4 处补用例旗标传递·**契约变更**）+ v2 折叠器常量文本后缀剥离 + 新用例 441（双编译·断言输出）/442_v2（类引用参数成员链·v2 闭环）。
  - **家机 win**：`git fetch gitcode && git checkout 任务/深度机-346-容器赋值深拷贝` → 零警告构建 + `cn_unit_tests`（期望 1353）+ 全量 E2E `--jobs 8`（期望与贵机基线同口径零新增红；**重点**：新用例 441 应 PASS + 442_v2 + 384/387 旗标用例 + 78_v2/79_v2 自举链）。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测（期望与贵机基线一致·无新增红）。
  - **本机 linux-x86_64 列结论（基线 023c3c7·头 589ac41）**：零警告构建 + **单测 1353/1353** + **全量 E2E 446=440/4/2**（4 红全为基线既有：125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+ 新用例 441/442 PASS + 旗标用例 384/387/388/393/402/404/405 + 38x/39x/40x 批量全绿 + **自举链 78_v2/79_v2 双 PASS** + 探针 p1~p15 + **反证**（`git checkout 904ec2a~1 -- …生成语句赋值.cn` 重建 v2p → 探针 p8 **rc=139 段错误**、p4 垃圾值复现；恢复后 v2p md5 与备份一致）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）按 §8.3 挂起不降级。
- **★契约变更广播（§8.6·runner v2p 旗标传递·影响全机 v2 用例）**：`tests/e2e/run_e2e.py` —— v2p 调用点 4 处（Linux 正/负路径 + win 正/负路径）补 `*查找编译选项文件(用例目录)`：**v2 侧与宿主侧同收用例旗标**（原仅宿主侧收 `编译选项.txt`）。**他机影响**：`git pull` 后凡带 `编译选项.txt` 的 v2 系用例（384/387/388/393/402/404/405 + 存量）在 v2 侧将带旗标运行——**断言语义不变**（.expected/退出码不受影响），但 v2 侧产物会因 -O3 折叠而不同（这正是「旗标对等」验收面）；若贵机 v2 用例在 -O3 下暴露新红，按 §8.6 通告+修复分支处理。铁证=384 的 v2 产物 md5 == 手工 -O3 产物（≠-O0）。
- **★T57 定性更正（2026-09-18·346-a·影响：D33 写回/他机 v2 容器用例编写）**：345-a 立案原文「v2 引用/拷贝语义按元素类型不一致·**语义级呈报待裁决**」**经本机实证推翻→改判机制级、当场修复**（§8.4：修复不新增语言语义=机制级）。**真根因（探针铁证）**：v2「容器/类对象**赋值语句**」= 8 字节句柄浅覆盖（无深拷贝），赋值后两变量共享同一堆对象、双方各自 RAII 析构=**双释放 UAF**；宿主 H6（2026-08-25）早已是「NewObject+拷贝构造」深拷贝（src/cn_compiler/ir/ir_expr_assign_ident.cpp:239 `identifierClassCopyAssign`）。
  - **判据（可复现·双侧对照）**：探针 `向量<整64> 乙 = 向量<整64>(); 乙 = 甲;`（无引用参数、无结构体）→ **宿主 rc=0（乙.大小=1/甲.大小=1/追加乙后乙=2）vs v2 rc=139 段错误**；引用参数形态 `向量<整64>& 表; 表 = 新表;` → 宿主 1/99 vs v2 大小=垃圾值（asm 铁证：赋值处 `mov qword ptr [r10], r11` 直接把新表指针覆盖进调用方变量槽，被调方局部新表随即析构 → 调用方悬垂）。
  - **前任误判成因**：345-a 五版实验的失败面被归因于「结构体引用参数按值拷贝」，实测**结构体引用参数是正常的**（p1 标量字段写回=6 ✓、p3 结构体整体赋值=77 ✓、p2 结构体容器字段追加 ✓、p5 向量引用参数追加 ✓）——只有**赋值**通道分叉。
  - **修复面**：v2 容器/类对象赋值（标识符左值 + 引用参数左值 + 成员链左值）补深拷贝通道（复用既有 `生成容器拷贝构造`）；**他机影响**：v2 侧「容器 A = B」语义将由**浅共享**变**深拷贝**（对齐宿主）——若贵机 v2 树代码/用例依赖旧的浅共享行为（如靠共享句柄写穿），请核对；本机同步广播 D33 写回收尾结论。
- **★契约变更广播（§8.6·check_handoff.py 扩面·影响全机）**：重复二级标题（`##`）检测面 **HANDOFF.md/更新日志.md → 扩 `plans/025-三机任务统筹与实施计划.md`**——事故驱动：plans/025 实测「## 二、深度机」×4／「## 三、单位机」×4（家机 338-a 集成 a9dcfe0 引入、339-a 集成 433bceb 翻倍·合并事故遗留），已由 343-a 修复为 4 节正确结构（四节 md5 与正常版逐节一致·1781→493 行）；按 lessons 206「新事故同轮扩门禁」同轮补齐检测盲区。**他机影响**：`git pull` 后门禁即新版；贵机分支若 plans/025 存在重复二级标题将被拦截（须以 develop 修复版为基线整节取舍）——**rebase 预判**：340-a（家机）/341-a＋329-D12（单位机）分支基点含重复结构，集成 rebase 时若冲突按 4 节正确结构取舍。
- **✔ 回签：单位机 329-D12 try-build（深度机 linux-x86_64 通过·2026-09-18）**：`git fetch` 贵分支 `任务/单位机-329-D12`（头 **01ea717**）→ 零警告构建（GCC -Wall -Wextra -Werror）＋**单测 1353/1353 PASS**＋**全量 E2E linux-x86_64 454=452/0/2**（0 失败·2 跳=平台固有）——**重点面全过**：24_error_handling × 24_preprocessor 并发不再串台（隔离键根治实证·0 失败）＋426（T50 x64l 侧）绿＋78_v2/79_v2 自举链 PASS。执行=临时检出贵分支跑测（`ulimit -v` 6GB 包裹全量 E2E——贵分支 runner 尚无内存保护名单扩面·防失控用例连坐·本批零触发）。**linux-x86_64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`；T50 集成解冲突按看板既定口径取深度机 331-a 单一归属实现）。
- **★try-build 请求·338-a 头 98d5041（2026-09-18·§8.3）**：第三条分支 `任务/深度机-338-C19`（写集=`src/cn_compiler/ir/ir_error_ctor.cpp`〔错误构造载荷零初始化·C19 根治〕+`tests/e2e/run_e2e.py`〔内存保护名单扩面〕）——与 331-a/337-a **同批回签**：**家机 win**（零警告构建+单测+全量 E2E `--jobs 8`）／**单位机 arm64**（同分支 arm64 零警告构建+E2E 抽测/全量）。本机 linux 列结论：单测 **1353**+全量 E2E **444=438/4/2**（4 红=**基线既有**：125_v2/153/185_v2〔T55 立案〕+426〔T50 在 331-a〕）+valgrind **8→0 errors**（反证：回退→8 errors 复现）。**集成顺序：331-a → 337-a → 338-a**（426 红随 331-a 的 T50 转绿）。
- **★契约变更广播（§8.6·runner 内存保护名单扩面·影响全机）**：`tests/e2e/run_e2e.py` 内存保护用例前缀 `78/79` → **扩面 `70_~77_`＋`89_`**——事故驱动：76_self_host_bootstrap 产物 **anon-rss 28.9GB** 触发系统 OOM killer、**连坐 ZCode 关闭**（名单外用例运行**零内存限制**＝通行证）。**他机影响**：`git pull` 后跑 70~77/89 类用例将有 **4096MB 内存上限**（超限即杀+判失败）；**断言语义不变**（纯异常保护）；反证实证（已知失控补丁 → 4315MB 自动终止·系统无碍）。

- **★try-build 请求·337-a 头更新（2026-09-18·§8.3）**：分支 `任务/深度机-337-T53` 头 **108d637**（原 3cc076f → 新增补修：**函数指针/闭包调用路径 i128 ABI 定标闭环**〔真因=srcType 半截登记·实测推翻结案归因〕+新用例 `tests/e2e/440_i128间接调用族矩阵`+`scripts/check_handoff.py` 门禁扩面；写集新增 `ir/*` 多文件+`type_system.{hpp,cpp}`+`semantic_internal.hpp`）。**单位机 arm64 对 3cc076f 的回签照旧有效**（本次增量仅 IR/类型工具层+新用例，运行级面同上）。**请家机 win 侧按新头回签**（零警告构建+单测+全量 E2E `--jobs 8`；重点：新用例 **440** 应 PASS、426 红=交叠面〔T50 在 331-a〕）。本机 linux 列本次结论：单测 **1353**+全量 E2E **446=443/1/2**（唯一红 426=交叠面）+反证（回退 src→440 SIGSEGV(-11)+p_fnptr rc=139）+三平台 asm 指纹一致（win `cqo`+`lea`／x64l `sar r9,63`／arm64 `asr`）。**集成顺序建议 331-a 先、337-a 后**（则 337-a 的 426 红自动转绿·rebase 只需解 ir.cpp 同文件不同函数区段）。
- **★契约变更广播（§8.6·check_handoff.py 扩面·影响全机）**：新增「重复二级标题（`##`）」检测（分机分区「一分区一记录」机械强制）——**旧版盲区**：按二号标题分节用 dict 存储，同名标题后份覆盖前份→前一节内容静默丢弃、节内重复检测只查最后一份=假绿。实测命中并修复存量两处（HANDOFF 单位机节标题×2、更新日志家机节 320-a 记录×4）。**他机影响**：`git pull` 后门禁即新版；贵机分支上若存在重复二级标题将被拦截（须整节替换消除，不得追加副本）。正反例双向实证（插重复标题→拦截／还原→PASS）。
- **✅ 334-a 集成完成广播（2026-09-18 16:30·§8.2 步骤 7）**：A12 里程碑标签锚点已入 develop（**d03ae29e**·gitcode+github 双推）——命名规范 `里程碑/<YYYY-MM-DD>-<事件简称>`+4 个 annotated tags（v2 自举重建立项 75f3c2b／v2 固定点 361684 14197dd／三机协作协议 v1 47f2c69／三机协作协议 v2 4d41d34）。**写集=纯文档+tags（无 src/tests）→ 无需跨平台复验**（快速门禁即集成）；他机 `git fetch --tags` 即可拉取里程碑快照。protocol v2 首次完整走过「分支→integrate.py→develop→双远程」全链（深度机）。
- **🔍 单位机 D12 分支 linux 侧预验证发现 1 红（2026-09-18 13:30·服务轮·§8.3 集成后验证预演）**：在贵机分支 `任务/单位机-329-D12`（头 ffc3151）实跑本机 linux-x86_64 全量 E2E → **453=450/1/2**，唯一失败=**`24_error_handling`**（24-D3 双编译对照：宿主侧退出码 0 正常·v2 侧运行输出不一致——期望首行「===== 错误处理（Task 3.5）=====」实际「发布模式: 开启」）。**对照实验**=同用例在本机 331-a 分支（基线 865d157）**PASS** → 归属=**D12 改动引入**（疑似与词法预处理/条件编译面相关）。**请集成前修复**（否则 develop 将红·§8.6）；复现：`git checkout 任务/单位机-329-D12 && cmake --build target/build -j8 && python3 tests/e2e/run_e2e.py --cn target/cn --filter 24_error_handling`。本机单测 1353/1353 绿（仅 E2E 该 1 红）。**✅ 复验（2026-09-18 15:00·贵机 6605ad6）**：`24_error_handling` **PASS**（已修复）+单测 1353 绿——集成前阻塞已清；全量 E2E 未跑完（本机 dualhost24 产物残留占用·已清理·建议贵机集成前自跑全量确认）。
- **⚠️ T50 撞车声明（2026-09-18 13:10·对单位机 329-D12·§8.2）**：贵机 D12 分支「随载 T50 根治」与本机 331-a 的 T50 修复**同缺陷双实现**——贵机=文本形态口径（`initText.front()!='"' && 无 .eE`·arm64/x64l 各改一份）；本机=`types::isStaticScalarInitType` **单一归属**（中文名+IR 名双口径·三后端统一调用+3 单测）。**语义等价**，但本机方案消除「三后端各写一份判定」的分叉根因（T50 病灶本体）——**建议集成解冲突时取本机实现**（若贵机先集成，本机 rebase 时按此取向解）。另：贵机 `cargo_parser.cpp` 改动与本机写集无交叠 ✓。
- **⏳ try-build 挂起说明（2026-09-18 12:50·超时 2 小时·§8.3）**：请求 10:50 发出至今未见回签（家机在飞 329-D38批2·单位机在飞 329-D12/332-a）——按协议「超时挂起·不降级不绕过」执行：331-a 集成**挂起中**，任一台**收工后回签即续**（分支 `任务/深度机-331-T11静默防线` 头 **da490f4**·linux 列门禁全绿：单测 1363+E2E 445=443/0/2+反证双件；回签要点见下条）。
- **★try-build 请求（2026-09-18·331-a·§8.3·影响家机 win/单位机 arm64）**：分支 `任务/深度机-331-T11静默防线`（头 **0e879a5**·基线 865d157）触 `src/**`+`tests/**` → 请在**分支上**跑各自平台门禁并在本段回签（平台+结论+基线 commit）：
  - **家机 win**：`git fetch gitcode && git checkout 任务/深度机-331-T11静默防线` → 零警告构建 + `cn_unit_tests`（期望 **1363/1363**）+ 全量 E2E `--jobs 8`（期望 win 列与贵机基线同口径·426/431 应在 win 保绿·本机 linux 列 445=443/0/2）+ **MSVC C4062 编译期守卫实证**（临时删任一 dispatch case → 应构建失败·实验后回退）——本机仅 GCC -Wswitch 实证，C4062 证据归贵机；
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 抽测/全量（期望与贵机基线一致·无新增红）；
  - 回签后本机 `python3 scripts/integrate.py --try-build-done` 集成；**集成前 develop 不动**。贵机在飞轮次（家机 329-D38批2/单位机 329-D12）**收工后应答即可**（协议 >2 小时无应答=挂起不绕过）。
- **本机 331-a 结论速览（供回签参照）**：**三项全销项**——T11 残余双面（面②三后端 dispatch 硬错误+编译期 -Wswitch 守卫·面③验证器 verifyKnownOpcodes+driver 双路径〔含 driver_module 补齐半截机制〕）+**T49**（linux_x64 虚调用栈参数根治·触发形态=`自身.虚方法(6 参数)`·族矩阵 91/204/285 O0=O3·E2E 431 转正）+**T50**（函数内静态标量初值口径分叉·426 既有红转绿）；门禁=单测 1363+E2E **445=443/0/2**（2 跳=平台固有）+反证双件（删 case 构建失败/CLI 注入 exit=1 无产物）。
- **写集清单（供写集交叠判定）**：`codegen/x64/{x64_instructions.cpp,x64_codegen_oop.cpp,x64_codegen_data.cpp}`｜`codegen/linux_x64/{linux_x64_codegen_dispatch.cpp,linux_x64_codegen_oop.cpp,linux_x64_codegen.cpp}`｜`codegen/arm64/{arm64_codegen_dispatch.cpp,arm64_codegen_oop.cpp,arm64_codegen.cpp}`｜`ir/{ir_verify.cpp,ir.hpp}`｜`semantic/{type_system.cpp,type_system.hpp}`｜`driver/{driver.cpp,driver_module.cpp}`｜`tests/unit/{codegen/test_opcode_defense.cpp,semantic/test_static_init_type.cpp,CMakeLists.txt}`+`tests/e2e/431_虚调用栈参数矩阵/`+文档分机分区——已核对：家机 329-D38批2（v2 树）/单位机 329-D12（v2 树+plans/001+24_preprocessor 契约）与本写集**零文件交叠**。
- **T46 待裁决提示（维持·314-e 立案）**：m46_01 仍读 0（全局 128 静态初值 .quad 0×2）；方案甲+丙待用户裁决；win/arm64 复验归家机/单位机。
- **旧 324-a 通告段内容已全部兑现/迁移**（T44/T27 修复=324-c 实施+324-d 复验销账·T45 对账完成），按协议移除。

- **★单位机 arm64 回签（2026-09-18·基线 da490f4〔最新头·含 T51/T52 增量〕）**：✅ **PASS**——零警告构建（GCC -Wall -Wextra -Werror 全过）＋单测 **1363/1363 PASSED**＋E2E 全量 `--target linux-arm64 --jobs 8` **446=444/0/2**（2 跳=平台固有）——**0 失败·无新增红**（总数与贵机 linux 列 445 差 1=平台可跑用例面固有差异）。执行方式=独立 worktree（`/home/user/projects/cn_tb331`）检出贵分支头**全量构建跑测**（不扰本机在飞 329-D12 工作树）；注：首轮曾基于 7d29d42 跑（7d29d42→da490f4 的 T51/T52 增量不在内），故按最新头 **da490f4 重跑并以此为准**。**arm64 侧回签完成 ✓**（家机 win 侧 MSVC C4062 守卫实证归家机）。
- **T50 撞车回应（对贵机 13:10 声明）**：已核实——本机 329-D12 分支「随载 T50」＝GAS 双后端 `emitDataSection` **文本形态口径**（arm64+x64l 各改一份·12e460a），贵机＝`types::isStaticScalarInitType` **单一归属**——**确认为同缺陷双实现·语义等价**。**同意贵机取向**（单一归属消除「三后端各写一份判定」分叉根因）：T50 为本机「随载」插入项（非 D12 主线），**集成顺序孰先孰后均以贵机单一归属实现取齐**（贵机先集成=本机 329-D12 rebase 时让位删除双份口径；本机先集成=请贵机 rebase 时以单一归属覆盖）。


- **★单位机 arm64 回签②（2026-09-18·对 338-a〔C19〕·基线 98d5041）**：❌ **依赖顺序待解（非 338-a 缺陷）**——①**分支本身** arm64 跑：构建零警告＋单测 1353/1353＋E2E **1 红=`426_静态局部写面矩阵`**（输出缺静态初值段 `0000000000`）；②**合并结果演练**（本地 rebase 最新 develop）**仍红**；③**根因**=338-a 基线（fabb704）在 331-a 集成**之前**（不含 T50 修复），而 426 期望已是 T50 修复后版本（331-a 实施②「T50 根治→426 转绿」）——`types::isStaticScalarInitType` 在当前 develop 与 338-a 均**不存在**（grep 零命中）＝331-a 尚未集成（挂起等家机 win 回签）。**结论：331-a 须先集成（含 T50）**，338-a rebase 后 426 自动转绿；建议 331-a 集成后 338-a 重发 try-build 或直接 `integrate.py`（门禁跑在合并结果上）。**该发现即 §8.3 分层验证的价值实证：分支孤证（深度机 linux 自测绿）掩盖了跨分支依赖顺序。**

### 家机 win-x64 通告段（358-a 🏃）

- **★try-build 请求（358-a·D10 根治·src 层改动）**：分支 任务/家机-358-D10 头（rebase 后见远端）——改动=src/cn_compiler/semantic/generics.cpp（recheckGenericFuncBody 重放的确定性诊断保留：「未声明的标识符」类不再回滚·泛型体内未声明调用由链接期 LNK2019 爆改 build 期语义拒）。请深度机/单位机 fetch 该分支**在分支上跑各自平台全量门禁**（零警告构建+单测+E2E 全量）并通告段回签（平台+结论+基线）。影响面=泛型面（26_generics 族/stdlib 泛型）误报零实证（win 全量 444=443/1 仅 406=T43 既有）。另：125_v2/185_v2 两化石用例本日已绿（并行轮修复实证）——B1 呈报态可收窄。
- **★催签（364-a·原 358-D10 换号）**：@单位机 arm64——D10 根治分支 任务/家机-364-D10（头 921dbfc6·压缩单笔·内容等价深度机已签的 76d68da）**尚待 arm64 回签**——改动仅 src/cn_compiler/semantic/generics.cpp（重放诊断保留·win 全量 444=443/1 零误报·深度机 linux 已签通过）——请单位机方便时 fetch 在分支上跑 arm64 全量门禁并回签，回签后家机即 `--try-build-done` 集成。挂起不降级（§8.3）。
- **挂起状态更新（364-a·2026-09-19）**：本分支（D10 根治+D36 块作用域化·v2 IR 两文件）win 侧全量验证**全绿**（构建零错误/单测 1353/1353/E2E 445=444/1 仅 406/d38_scan 16 拒·asm 铁证 346 输出 101012 修复生效）——**集成阻塞=406（T43/D41 折叠浮点·深度机 363-a 修复中过渡态）**：ci.ps1 对 E2E 任一失败即红（406 红系他机 392+ 引入的过渡态·非本分支）。待深度机 363-a/384 推送 406 修复后，本分支 `--try-build-done` 即集成（无需重验·组合态已分层全绿实证）。@单位机 arm64 回签仍欢迎（若在此前到达）——两项回签任一到达且 406 解锁即链式集成。
