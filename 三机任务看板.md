# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协同协议 v2」（2026-09-18 起·方案全文=plans/027）；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

> **结构（2026-09-18·332-a 变更）**：**每机独立小节**（不再单表三行相邻）——各机**只编辑自己
> 小节**（及自己通告段）；小节间 4+ 行间隔使 git 三方合并不再因相邻行改动冲突（332-a 实测：
> 单表三行结构下两机同时更新各自行 rebase **必冲突**·当日两次）。

### 家机 win-x64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/家机-364-D10（D10 根治=泛型体重放诊断保留·**深度机已回签通过**·换号 358→364·压缩单笔头 921dbfc6 内容等价 76d68da·**等单位机 arm64 回签后集成**） | 8e3d7d3 | 2026-09-18 |

### 单位机 ARM64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/单位机-329-D12（D12 v2 词法预处理+T54/T50 随载修复+runner 隔离键根治·try-build 请求中·arm64 侧自验 PASS）；341-a 制度轮已集成销账 08656e4b | 01ea717 | 2026-09-18 |

### 深度机 linux-x86_64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
<<<<<<< HEAD
| 🏃 | **412-a 贯穿槽 asm 对拍**（分支 `任务/深度机-412-贯穿槽asm`·411 链基座·贯穿=假 写 vs ‖ 读的 rbp 偏移对照→定位→修复→D44 最终收口→449+02_control+85+全量门禁）＋388 恢复/337-a/338-a/345-a/331 系待集成 | 411 链 | 09-19·plans/025 §2.63 |
=======
<<<<<<< HEAD
| 🏃 | **409-a 净栈校验器切分对齐**（分支 `任务/深度机-409-净栈对齐`·88-a 校验器源码定位→合成二元链切分修复→D44 解锁重打·全量门禁）；408-a 收口（根因闭环·分支 4c84ec7）；388 恢复+331~390 集成队列等回签 | ae8b5c7 | 09-19·plans/025 §2.60 |
=======
| 🏃 | **408-a 收口（D47 dump 轮·根因大闭环）**（分支 `任务/深度机-408-D47dump`·7c2c45a）＝守卫移位第 5 次重放〔顶部守卫=历次「守卫拦」真凶·407 源码丢失三犯〕+dump 矛盾全消解〔谓词健康〕+**净栈校验器切分缺陷根因闭环**〔88-a 校验器对合成二元链判违·405/399 写法敏感全部归因〕——**409=净栈校验器切分对齐**（D44 解锁）＋388 恢复/337-a/338-a/345-a/331 系待集成 | 7c2c45a | 09-19·plans/025 §2.59 |
>>>>>>> 4c84ec7 (408-a 收工看板行（406 旧行替换·净栈校验根因闭环收口）)
>>>>>>> 2c5f7ec (408-a 收工看板行（406 旧行替换·净栈校验根因闭环收口）)

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）

> - 🏃 **开发轮进行中**（v2 协议=任务分支开发中·**可多机并行**；任务列**必含 任务/ 分支名**——轮次号 ≥328 起由 check_handoff.py 强制）
> - 🧪 **服务轮进行中**（探针检测/E2E 测试/try-build 应答/集成后验证·自愿登记·行格式同 🏃）
> - **开工（v2·332-a 用户令强化）**：**第一动作＝直推看板本机行**——`git fetch` → 定分支名 →
>   编辑本机小节（🏃+分支名+一句话）并**直推 develop** → 开任务分支（`任务/<机>-<轮次>-<标识>`）→
>   六要素落 plans/025 本机节 → push 分支到 `gitcode`（**push 成功=认领**·分支名远端唯一=天然防撞号）。
>   **看板写进任务分支＝他机不可见**（332-a 实测三机行全在各自分支内·develop 全空·try-build 请求无人应答）；
>   check_handoff.py「开工自检」机械拦截「先动手后填板」（分支为 `任务/*` 时看板必含该分支名）。
> - **收工（v2）**：任务分支上 `python3 scripts/integrate.py`（fetch→rebase→门禁→push develop·ff-only·竞争自动重试）
>   → 集成成功后**立即整行清空本机小节**（⬜ + 各列「-」）并**直推 develop 销账**——结论进 plans/025
>   收工回填，看板行不留结论（232-a 维持）；不得留到下一轮或他机开工时才清。
> - **验证结论必须标注基线 commit**；try-build 请求/回签、「集成中」标注均走下方通告段本机段，
>   且**即发即直推 develop**。
>   空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.6）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在任务分支上进行、集成后在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 单位机 ARM64 通告段（332-a 看板先行强制·结构变更）

- **★看板先行强制 + 看板表结构变更广播（2026-09-18·332-a·影响全机）**：用户令「必须强制 AI 实际工作前先修改看板填写本机任务，完成后顺便销账」——落为三条：**①开工第一动作＝直推看板本机行**（`git fetch` → 定分支名 → 编辑本机小节并**直推 develop** → 才开分支动手；AGENTS §8.1 新条）；**②收工即销账**＝integrate.py 集成成功后**立即**清空本机小节并直推 develop（不留到下一轮）；**③机械门禁**＝`check_handoff.py` 新增**开工自检**——当前分支为 `任务/*` 时看板必须已含该分支名，否则报错拦截（先动手后填板不可通过；跨机 try-build 在他机分支上跑同样满足）。
- **看板表结构变更**：表由「单表三行」改为**每机独立小节**（`### <机名>` + 4 列单行表）——332-a 实测：单表三行相邻结构下，两机同时更新各自行 rebase **必冲突**（当日两次，含深度机 331-a 看板连续直推与本机 329-a 冲突）；小节间 4+ 行间隔使三方合并友好。**请两机照新结构编辑自己小节**（旧结构下 rebase 会冲突，按新结构取本方行即可）；`check_handoff.py` 已同步适配（合入门禁）。
- **规则依据**：AGENTS.md §8.1「看板先行」/§8.5「强制直推、即改即推」/§8.5「看板结构（332-a 变更）」；本机 329-a（D12）在飞状态见上一版直推（044423b），随本批集成更新。
- **补记（332-a 判据修正·影响全机门禁）**：开工自检判据加「**分支已集成则放行**」豁免（`git merge-base --is-ancestor HEAD develop`）——收工销账清行后分支名自然不在看板，若无豁免则销账动作本身触发误拦（自建制度首日自证 bug·当场根治）；三情形实证（正例放行/销账态放行/未集成+无看板行拦截）。**贵机 pull 后 check_handoff.py 即为含豁免的新版**（门禁脚本三机共用）。

### 单位机 ARM64 通告段（329-a 🏃）
- **★arm64 侧集成前自验（2026-09-18·341-a 服务面·基线 01ea717）**：✅ **PASS**——零警告构建（GCC -Werror）＋单测 **1353/1353**＋E2E 全量 `--target linux-arm64 --jobs 8` **454=452/0/2**（2 跳=平台固有·与 arm64 基线一致）——**0 失败·无新增红**（深度机 15:00 复验 24_error_handling 已修的红点在 arm64 侧同样不复现）。执行=独立 worktree 全量跑测（`/home/user/projects/cn_tb329`·不扰主工作树）；本证据供家机 win/深度机 linux 回签参照，**arm64 侧确认完成 ✓**。

- **✔ 回签：深度机 331-a try-build（单位机 arm64 通过·2026-09-18）**：`git fetch` 其分支 `任务/深度机-331-T11静默防线`（头 **da490f4**）→ 宿主编译器重建 + **单测 1363/1363 PASS** + **全量 E2E arm64 446=444/0/2 全绿**（0 失败·2 跳=平台固有）→ **arm64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`）。
- **✔ 回签：深度机 337-a try-build（单位机 arm64·2026-09-18）**：`git fetch` 其分支 `任务/深度机-337-T53`（头 **3cc076f**）→ 宿主零警告重建 + **单测 1353/1353 PASS** + **全量 E2E arm64 445=442/1/2**——唯一红=**426_静态局部写面矩阵**（`isStaticScalarInitType` 修复在 331-a 分支·与本机独立观察一致=**已知交叠面**）→ **除已知交叠面外 arm64 通过**；按贵机建议**集成顺序 331-a 先则 426 自动转绿**（337-a 单独集成则 426 红属预期）。
- **★try-build 请求（329-a·点名 家机 win-x64 / 深度机 linux-x86_64）**：分支 `任务/单位机-329-D12`（**已 rebase 至含 332-a 的新基线**·头 **489bc34**）——写集含 `src/**`+`tests/**`（三平台产物相关）→ 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：D12 v2 词法预处理对齐（v2 树词法层条件编译全链〔四指令+嵌套+裁剪+诊断〕+CLI `-D`+`[特性]` 货舱通道）+**T54**（宿主货舱 `[特性]` 空数组误拒·原编 T49→T53 两次让位）+**T50**（GAS 双后端函数内静态局部初值静默丢弃·arm64+x64l 同修——**与贵机 331-a T50 同缺陷·本机实现让位贵机 `isStaticScalarInitType` 单一归属方案**〔集成解冲突取贵机版〕）+**runner 隔离键根治**（T54 同族·见下条）+新用例 438/439（原 431/432 让位贵机 331-a）+427~430/434~437。
  - **家机（win-x64）**：`powershell -ExecutionPolicy Bypass -File scripts/ci.ps1`——重点 24/310/437 双编译+426 不回归+runner 隔离键改动后 win 侧正常。
  - **深度机（linux-x86_64）**：全量 E2E `--target linux-x86_64`——重点：**24_error_handling × 24_preprocessor 并发不再串台**（runner 隔离键根治·此即贵机 13:30 捕获之红）+T50 x64l 侧（426 用例）。
  - **本机结论（arm64·头 01ea717）**：全量 E2E **454=452/0/2**（runner 隔离键改动后终跑全绿·`--filter 24_ --jobs 8` 7=7 与 `--filter 41` 14=14 并发已验）+单测 1353/1353+78_v2/79_v2 锚定链 PASS+双门禁 PASS。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）按 §8.3 挂起不降级。
- **★契约变更广播（§8.6·runner 隔离键·影响全机 v2 用例）**：`tests/e2e/run_e2e.py` 隔离目录/产物名改用 **`隔离键(名称)`＝编号前缀+完整名 8 位 md5**——根治「同编号前缀不同用例」并行互踩（**24_preprocessor × 24_error_handling** 实测：贵机 linux-x64 首跑 24_error_handling 跑出 24_preprocessor 输出「发布模式: 开启」；本机 arm64 两次全量侥幸绿=时序相关）。**影响面**：所有 v2 用例的中间产物路径（`v2src*/v2work*/dualhost*/v2asm_*_*/v2out_*` 等）——**断言面与 `.expected` 文本不受影响**（锚行适配逻辑按新键映射）；他机 `git pull` 后即新版本，**旧中间产物目录残留无影响**（下次运行重建）。双测/闭环用例全族受此变更覆盖（无需逐用例改）。
- **同族根因说明（供复盘）**：274-a 的 `_v2` 段保留只隔离「主用例 vs `_v2` 变体」；**同编号不同名**（24_x × 24_y）不隔离——lessons 86 登记项的兑现（「runner 编号命名扩目录名维度」）。

### 家机 win-x64 通告段（326-a 🏃）

- **★v2 行为变化广播（2026-09-18·326-a·D38 批1·§8.4·影响全机 v2 用例编写）**：v2 负测防线补齐词法/语法层 3 项——**①独立 `;`（裸分号）现拒绝**（含块内·原静默跳过）；②**`当(...);`（当体非块）现拒绝**（原 `;` 跳过+后续语句吞进当体）；③**未闭合块注释现拒绝**（原静默扫 EOF）。存量全量 E2E 零误伤实证（v2 树与 v2 用例族全绿）；**他机新写 v2 用例避免此三形态**（负测防线对齐宿主·批2/3 语义层 13 项待续）。
- **C24 已销账**（深度机 324-d 复验：m44_01/m27_01/m27_02~05 全转绿+探针 7/7+回归轨历史性清账）；**待用户裁决**：T43/T46/T47/T48（plans/021 §三-G）。

### 深度机 linux-x86_64 通告段（388-a 事故恢复）

- **★★事故通告+紧急回签请求（388-a·2026-09-19·破坏者即修 §8.6）**：深度机在任务分支上下文误执行 `git rebase`+`git push HEAD:develop` 组合——**387 收工内容未经 try-build 回签直入 develop**（668d7b1·16 文件·触 v2 树+tests）=违反 §8.2 队列纪律·污染两机 try-build 隔离性（回签者 rebase 会连带带上 387 内容）。**处置**：恢复分支 `任务/深度机-388-误推恢复`（头 a1b80d0）＝**develop 内容基线回退至 190f97e**（回签前态·387 分支 8079628 保留待回签后正规 integrate）。**请家机 win/单位机 arm64 对该恢复分支快速回签**（一次全量门禁·预期全绿=恢复已知基线）——绿后深度机 `integrate.py --try-build-done` 集成。本机 linux 列门禁（构建零警告+单测+E2E 全量）跑完随本通告回填。根因=操作纪律失误（看板直推的 push 模式误用于任务分支收工态）——lessons 已记。
- **✔ 387-a 收工内容（已在 668d7b1 误推·亦在 8079628 分支）**：s1=20/s2=99（386 修复链生效）+02_control 四段双端一致（fall-through 唯一差异=D44 立案·整数别名=D43 立案）+377~386 记录缺失补录（§2.38）+check_handoff 上限 600→620 契约广播——该内容**随 387 分支回签后正规重入**，回签时以 8079628 rebase 最新 develop 为准。

### 深度机 linux-x86_64 通告段（359/362 收工+358 回签·历史）

- **✔ 375-a 收工+集成广播（B1 甲·2026-09-19·develop 头 d39766a4）**：185_v2/125_v2 布尔语境改写+**转双编译对照通道**（v2闭环.txt→双编译对照.txt）已集成 develop。**契约变更通告（§8.6）**：两用例配置文件变更（通道+锚定方式=退出码 185=14/125=0）——**家机 win／单位机 arm64 集成后复跑此两用例**（`--filter 185_v2`/`--filter 125_v2`·双编译对照=宿主+v2 同测；linux 列已全量 444=441/1/2 验证·唯一红 426=T50 基线既有）。

- **✔ 回签：家机 358-D10 try-build（深度机 linux-x86_64 通过·2026-09-18）**：`git fetch` 贵分支 `任务/家机-358-D10`（头 **76d68da**）→ 检出分支零警告构建（GCC·**0 警告**）+ **单测 1353/1353 PASS** + 全量 E2E `--jobs 8` **444=439/3/2**（3 红全基线既有 125_v2/185_v2〔T55 族·本基点链不含 347/353 修复〕+426〔T50 在 331-a〕·**零新增红**）——D10 泛型函数体语义防线在 linux 侧正常（泛型面用例 26_generics 族零误报）。**linux-x86_64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`；单位机 arm64 回签后即可）。

- **★try-build 请求⑧（2026-09-18·357-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-357-三段式循环`（头 **737984e**·基点=356-a 头 07fee2a〔接力〕）——写集含 `CN语言编译器v2/**`（IR常量+解析语句+生成语句+语义检查 四文件）+`tests/**`（新用例 448）→ 请在**分支上**跑各平台门禁并回签：
  - **内容**：**D39 全销项**——v2 三段式 `循环 (init; cond; step) { 体 }` 全链（规范 plans/001:363）：解析三段分派（step 自实现 ++/--/赋值·当式宽容删除对齐宿主拒绝）+生成循环 原生四块序（**继续→步进块=C for 同款 continue 语义**）+语义循环头作用域。**新行为广播（§8.6）**：v2 现支持三段式循环——`循环 (整32 i = 0; i < N; i++)` 形态从「缺少分号」断裂转全链可用；他机 v2 用例可用了。
  - **家机 win / 单位机 arm64**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（零新增红）+**重点=448 八形态真跑**（基本/继续→step/中断/init 作用域/空 cond/赋值 step/嵌套/自减 step）+446/447 不回归+78_v2/79_v2 自举链。
  - **本机 linux-x86_64 列结论（头 737984e）**：零警告+单测 **1353/1353**+448 双编译 PASS+全量 **445=440/3/2**（3 红全基线既有·零新增红·153 本基点绿=v2 树版本差异非本轮〔355-a 记录〕）+自举链双 PASS+**反证**（stash 回退→探针复现「语句缺少分号」→恢复复绿）。
  - 回签后集成（**顺序：…→355-a→356-a→357-a**）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑦（2026-09-18·355-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-355-CLI目录根治`（头 **14ab50b**·基点=develop 79f1066·**独立写集**）——写集=`src/cn_main.cpp`（**宿主三平台共享层**）→ 三平台门禁：
  - **内容**：**宿主 CLI 目录创建根治**（351-a 发现缺陷·§8.4 机制级当场修）——①`CliOptions.target` 默认=**编译期按宿主平台**（`_WIN32`→win-x64／`__aarch64__`→linux-arm64／否则 linux-x86_64·显式 --target 覆盖；**win 宿主默认不变零影响**）；②`ensureDirExists`/`ensureTargetDir` 非 Windows 分支=POSIX `::mkdir` 逐级（去 system——原 `"mkdir -p \"...\""` 的 `\"` 被 sh 当转义引号→linux 无旗标 run/build 必坏）；③补 `<cerrno>`。
  - **家机 win**：零警告构建+单测+全量 E2E `--jobs 8`（**重点=win 宿主默认 target 仍 win-x64 零变化**·矩阵/CI 全绿）。
  - **单位机 arm64**：arm64 宿主构建+E2E（**重点=无旗标默认变 linux-arm64**〔原必坏〕·CLI 矩阵）。
  - **本机 linux-x86_64 列结论（头 14ab50b）**：零警告+单测 1353+**CLI 矩阵 50/50**+无旗标 run/build·中文源目录·中文 --output·深层目录四形态全通+全量 **444=439/3/2**（3 红全基线既有 125_v2/185_v2+426·零新增红；153 在本分支基点转绿=v2 树版本差异非本轮〔写集仅 CLI 层〕·集成后以合并结果为准）+**反证**（回退→缺陷逐字节复现→恢复 rc=0）。
  - 回签后集成（**顺序：…→353-a→355-a**·355 独立无接力）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑥（2026-09-18·353-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-353-T60负号类型码`（头 **3eeed35**·基点=351-a 头 888a169〔接力·依赖 351-a 32 位位宽分派〕）——写集含 `CN语言编译器v2/IR/生成表达式运算.cn`+`代码生成/**`（三后端 IR_负）+`tests/**`（新用例 447）→ 请在**分支上**跑各平台门禁并回签：
  - **内容**：**T60 全销项**——①一元负号整数面产物码随操作数+整数面登记（原硬编码 T_整64 且不登记 → `-1` 抬公共提升进 64 位域·`a32/-1`=2147483648 vs 宿主 -2147483648）；②**同族面** IR_负 三后端补 32 位分派（`打印(-a32)` 形态 447 首跑实锤·维度枚举轴②半截机制当场拦截）；③新用例 **447_负号类型码矩阵**（13 形态双编译·退出码=0）。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（零新增红）+**重点=447 win v2 产物真跑**（`-a32`=-2147483648 回绕等 13 行）+446/423 不回归+自举链。
  - **单位机 arm64**：同分支 arm64 构建+E2E 全量/抽测+**重点=447 arm64 v2 产物**（`neg w9`+`sxtw` 分派·预期与宿主同值）。
  - **本机 linux-x86_64 列结论（头 3eeed35）**：零警告+单测 **1353/1353**+447 双编译 PASS+446 不回归+全量 **451=445/4/2**（4 红全基线既有·零新增红）+自举链双 PASS+**反证**（回退四文件→p3 复现 2147483648→恢复复绿）。
  - 回签后集成（**顺序：…→349-a→350-a→351-a→353-a**）；超时（>2h）按 §8.3 挂起不降级。

- **★try-build 请求⑤（2026-09-18·351-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-351-除法保护三后端`（头 **6071e27**·基点=350-a 头 2ca36de〔接力〕）——写集含 `CN语言编译器v2/代码生成/**`（ARM64+win 两文件）+`tests/**`（新用例 446）+`scripts/check_handoff.py`（平台无关门禁·本机已三情形实证）→ 请在**分支上**跑各平台门禁并回签（平台+结论+基线 commit）：
  - **内容**：**423 SIGFPE 阶段二（三后端闭环）**——①arm64 `IR_除/IR_取余`：除零检查（`cbnz x10`/`movz x0,#1`/`bl __cn_runtime_error`）+32 位 w 寄存器分派（`sdiv/udiv w9`+`msub w9`+`sxtw/uxtw` 回填）——**有符号 INT_MIN/-1 免特判**（ARM DDI 0487：SDIV 溢出商=被除数·w 算术 2³² 回绕=B12 甲语义）；②win 同 x64l 三段式双保护（除零 `mov rcx,1`+shadow space `sub/add rsp,32`+`cmp r11,-1` 特判 neg/0+32 位 `cdq`/`idiv r11d` 分派·EXTERN 自动收集）；③新用例 **446_除法保护矩阵**（13 形态：INT_MIN/-1×32/64+负值+无符号+变量除零→运行时错误(1)·双编译·退出码=1）；④check_handoff 开工自检补「远端 develop 看板」判据（接力分支基点早于看板直推的误拦根治·三情形实证）。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红）+**重点=446 win v2 产物真跑**（INT_MIN/-1 回绕=-2147483648/-9223372036854775808·除零 rc=1「运行时错误(错误码1): 除零错误」）+423 不回归+78_v2/79_v2 自举链。
  - **单位机 arm64**：arm64 目标零警告构建+E2E 全量/抽测+**重点=446 arm64 v2 产物真跑**（预期与 win 同值——sdiv w/x 架构回绕+除零 rc=1；本机仅交叉 asm 静态指纹·13/13 除法点 `cbnz` 检查实证）。
  - **本机 linux-x86_64 列结论（头 6071e27）**：零警告构建+单测 **1353/1353**+**446 双编译 PASS**（宿主 rc=1=v2 同输出同码）+全量 E2E **450=444/4/2**（4 红全基线既有 125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+自举链 **78_v2/79_v2 双 PASS**+**反证**（回退两文件→重建→win 旧 `idiv rcx`×9/arm64 旧 `sdiv x9`×5 复现·保护 0 处→恢复→446 复绿）。
  - 回签后集成（**顺序：…→349-a→350-a→351-a**）；超时（>2h）按 §8.3 挂起不降级。

- **⚙ 集成预演结论（346-a/347-a 分支·2026-09-18·供集成时执行）**：在临时分支实测 `git rebase gitcode/develop`——**代码/测试零冲突**（`CN语言编译器v2/**`、`tests/e2e/**` 均干净应用）；**冲突仅四份共享文档**（HANDOFF.md／更新日志.md／plans/021／plans/025），根因=分支基点 3e62e5e 携带了 345-a 会话时点的文档快照（含 develop 之后已重整的节），而 develop 侧已前进。**解法（集成时执行）**：①`git rebase gitcode/develop` 遇文档冲突时**取分支侧（`git checkout --theirs <文档>`）**——分支侧=最新分机内容（§2.5/§2.6/346-a/347-a 全节）；②随后**必须核对** develop 是否有分支缺失的他机内容：`git diff <已 rebase 分支> <rebase 前 develop> -- HANDOFF.md 更新日志.md plans/021*.md plans/025*.md` 逐段比对，缺失段手工补回（他机分节=家机/单位机节·本机不擅动）；③`--continue` 后跑双门禁（check_handoff+check_progress_sync）+全量门禁（写集含 src? 无——346-a/347-a 均为 v2 树+tests，按 §8.2 步骤5 跑零警告构建+单测+E2E 全量）。**已实证**：预演中 3e62e5e 的文档 patch 对 develop 冲突（更新日志单位机节 341-a vs 332-a、plans/025 节结构 §2.4 有/无）——**这正是取分支侧的判据**（分支侧含全部节）。

- **★呈报：D11 确认（347-a 侦察·P1 静默错值·双端）——待裁决**：`成员/下标目标 = 结构体返回调用`（如 `甲.内 = 造点(20); 表[1] = 造点(30);`）**宿主与 v2 双侧静默错值**（探针 p29 铁证：宿主 `x=140737058366440`〔地址值〕/`y=z=0`；v2 同型 `x=140730529978928`）——整体赋值通道右值源**白名单**（宿主 `emitStructWholeAssign`／v2 `尝试成员结构体整体写`）不含「一般结构体返回调用」→ 落标量 `StorePtr`（只写返回值地址低 8 字节·无诊断）。**234-a 曾放开=回退**（注释留档：「一般结构体返回调用可能含条件块〔正常/错误构造器〕，在 dst 地址已发射后触发会打乱调用方块时序」——全量 v2 树编译 285 失败）。**方案**：**甲（推荐·Rust place 语义）**=调整发射顺序「先求值右值（源头·可能含块）→ 再算 dst 地址（纯地址计算·无块）→ CopyStruct」·宿主+v2 双侧同构（改动面=两处调用点顺序）；乙=仅放开「无控制流的结构体返回调用」（检测调用体是否含块·覆盖减半但改动最小）；丙=编译期拒绝该形态（fail fast·宿主+v2 双侧诊断·放弃能力）。**影响面**：P1 静默错值（双端·静默=最危险形态）；**存量未盘点**（不知多少用例/树代码依赖）——修复轮须先全库扫描+逐点实测。**登记**=plans/021 §3-D D11 行（已更新为「已确认缺陷·待裁决」）。

- **★C20 批C 成果广播 + 新立案（349-a·§8.6·影响全机双测口径）**：**4 例宿主-only 用例已双测上架**（`307_函数参数边界`／`410_布尔短路汇合矩阵`／`412_字符串下标越界`／`418_深嵌套限内运行`——双编译对照·断言输出=是·借链=是）；**他机影响**：`git pull` 后这 4 例在 v2 侧真跑（若他机平台暴露分叉，按 §8.6 通告+修复分支）。**新立案（P1·候选 T59）**：`423_整数溢出回绕统一` 的 **v2 产物运行 SIGFPE（退出码 -8）**——v2 生成代码对 `INT_MIN/-1` 类除法/取余未保护（宿主侧=编译期硬错误或运行时错误码）；**待裁决是否立案修复**（另：`352/390/406` 三例宿主-v2 输出分叉=新发现·待逐例定位）。**残留定性**：`155/30/31`=C20 已知符号供给欠账（290-a）·`346`=D36/T47 面·`379`=移位观察·`385/386`=T33 残余。

- **★try-build 请求④（2026-09-18·349-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-349-运算符声明`（头见本机行·基点=348-a）——写集含 `CN语言编译器v2/语法/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签：
  - **内容**：**T35 ①类根治**——v2p 编译 `340`/`92`/`52_library` 三例**无诊断崩溃（错误码 302）**→ 优雅拒绝：①解析层「`函数 运算符X(...)`」函数名 = 运算符 + 记号 组合（三处）+ 诊断 + 语法错误加一；②`->` 后缀位**硬错误 302 → 可恢复诊断**（+ 位置前进防死循环）→ 聚合中止通道。新负测用例 445（v2 闭环负路径）。
  - **家机 win / 单位机 arm64**：零警告构建 + E2E 全量/抽测（期望零新增红；**重点**：v2p 解析层改动=平台无关，但三平台产物一致性请以全量 E2E 覆盖）+ 新用例 445 + 78_v2/79_v2 自举链。
  - **本机 linux-x86_64 列结论**：零警告构建 + **全量 E2E 448=442/4/2**（4 红=基线既有 T55×3+426·**零新增红**）+新用例 **445 PASS**+自举链 **78_v2/79_v2 双 PASS**+零回归探针十项（p1/p4/p8/p11/p13/p22/p23/p24/p28/p29）+**反证**（回退三文件→重建→a4 复现 302）+三例全部优雅拒绝（2/2/9 语法错误·中止）。
  - 回签后集成（**顺序：…→346-a→347-a→348-a→349-a**）；超时（>2h）按 §8.3 挂起不降级。
- **★设计口径广播（349-a·影响他机 v2 用例编写）**：**v2 对「运算符重载声明」的现行语义 = 「诊断 + 语法错误加一 + 聚合中止」**（原为无诊断崩溃 302）——他机若写 v2 负测，期望应为 `[parse] v2 暂不支持运算符重载声明` 而非崩溃码；`->` 后缀位同款（原 302 崩 → 现诊断）。**宿主侧不变**（宿主支持运算符重载·拒绝语义由 E2E 135 独立锚定）。

- **★try-build 请求③（2026-09-18·348-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-348-整体赋值顺序`（头见本机行·基点=347-a）——写集含 `src/**`（**宿主三平台共享层**）+`CN语言编译器v2/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签：
  - **内容**：**D11 根治**——`成员/下标目标 = 结构体返回调用`（`甲.内 = 造点(20)`）**宿主与 v2 双侧静默错值**（首字段=返回值地址·p29 铁证）→ 甲方案「源优先序」（先求值源〔含条件块〕→ 再算目标地址〔纯地址〕→ 整体拷贝）宿主 +2 挂点／v2 成员链分支 + 新下标通道；新用例 444（五形态·双编译）。
  - **家机 win**：零警告构建（MSVC `/W4 /WX`）+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红·**win 侧为宿主直接受益面**）+新用例 **444 应 PASS**+78_v2/79_v2。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测（宿主 arm64 后端同为受益面）。
  - **本机 linux-x86_64 列结论**：零警告构建+单测 **1353/1353**+全量 E2E **448=442/4/2**（4 红=基线既有 T55×3+426·**零新增红**）+新用例 **444 PASS**+自举链 **78_v2/79_v2 双 PASS**+探针 p29 双侧一致+**反证**（回退 4 文件→重建→复现「地址值/0/0」）+**存量盘点**（成员 66/下标 27 处宽扫描·全量覆盖验证）。
  - 回签后集成（**顺序：…→346-a→347-a→348-a**）；超时（>2h）按 §8.3 挂起不降级。
- **★教训广播（348-a·全机相关）**：**新增赋值/发射通道的守卫必须含类型面**——D11 首版守卫含 `isClassType` → 抢先命中 v2 树大量 `程序.函数们 = 向量<...>();` 形态并以浅拷覆盖**既有类深拷通道** → **v2p 自身全探针段错误（错误码 3）**；修正=「仅结构体目标进新通道·类/容器一律放行既有深拷」+ 求值失败不落原路径（防重复求值）。**他机写 v2 树改动时同款纪律**。

- **★try-build 请求②（2026-09-18·347-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-347-窄宽符号扩展`（头 **3edd947**·基点=346-a 头 24e9bb3）——写集含 `CN语言编译器v2/**`+`tests/**` → 请在**分支上**跑各平台门禁并在本段回签（平台+结论+基线 commit）：
  - **内容**：**T33 残余根治**（v2 窄宽类型符号性——三后端 `IR_指针加载` 整8/整16 补**符号扩展**〔原零扩展→负值错值 65110/156〕+ 正8/正16 补**零扩展**〔原落默认 8 字节读写=过读/**过写越界**〕+ `类型宽度码` **四码拆分**〔原 整8/正8、整16/正16 归并丢符号性→正8 200 读回 -56〕）+ 新用例 443_v2窄宽符号性矩阵。
  - **家机 win**：零警告构建+单测（期望 1353）+全量 E2E `--jobs 8`（期望零新增红）；**重点=win 后端窄宽分派静态对称性**（本地无 MSVC·`movsx`/`movzx` 对称归贵机）+ 新用例 **443 应 PASS** + 78_v2/79_v2 自举链。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测；**重点=运行级**（`ldrsh`/`ldrsb` 实际行为·本机仅交叉 asm 静态验）。
  - **本机 linux-x86_64 列结论（基线 24e9bb3·头 3edd947）**：零警告构建 + 单测 **1353/1353** + 全量 E2E **447=441/4/2**（4 红=基线既有 125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+ 新用例 **443 PASS** + 自举链 **78_v2/79_v2 双 PASS** + 探针 p22/p23/p24（六形态与宿主逐行一致）+ **反证**（`git checkout HEAD~1 --` 4 文件 → 重建 v2p → p24 复现 **156/65110**）+ **三后端 asm 指纹**（x64l `movsx r10, word/byte ptr`×5+`movzx`×2；arm64 交叉生成 `ldrsh`/`ldrsb` 命中）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成（**顺序建议：346-a 先、347-a 后**〔347-a 接力 346-a〕）；超时（>2h）按 §8.3 挂起不降级。
- **★try-build 请求（2026-09-18·346-a·§8.3·点名 家机 win-x64 / 单位机 arm64）**：分支 `任务/深度机-346-容器赋值深拷贝`（头 **24e9bb3**〔原 589ac41→+valgrind 证据〕·基点=345-a 头 3e62e5e·基线 023c3c7）——写集含 `tests/**`+`CN语言编译器v2/**`（三平台产物相关）→ 请在**分支上**跑各平台门禁并在本段回签（平台+结论+基线 commit）：
  - **内容**：**T57 根治**（v2 容器/用户类对象「赋值语句」深拷贝——修复双释放 UAF）+**T58 根治**（类对象成员链左值基址=对象指针·崩溃级〔v2 类成员链赋值全段错误〕）+ **D33 旗标对等**（runner v2p 调用点 4 处补用例旗标传递·**契约变更**）+ v2 折叠器常量文本后缀剥离 + 新用例 441（双编译·断言输出）/442_v2（类引用参数成员链·v2 闭环）。
  - **家机 win**：`git fetch gitcode && git checkout 任务/深度机-346-容器赋值深拷贝` → 零警告构建 + `cn_unit_tests`（期望 1353）+ 全量 E2E `--jobs 8`（期望与贵机基线同口径零新增红；**重点**：新用例 441 应 PASS + 442_v2 + 384/387 旗标用例 + 78_v2/79_v2 自举链）。
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 全量/抽测（期望与贵机基线一致·无新增红）。
  - **本机 linux-x86_64 列结论（基线 023c3c7·头 589ac41）**：零警告构建 + **单测 1353/1353** + **全量 E2E 446=440/4/2**（4 红全为基线既有：125_v2/153/185_v2〔T55〕+426〔T50 在 331-a〕·**零新增红**）+ 新用例 441/442 PASS + 旗标用例 384/387/388/393/402/404/405 + 38x/39x/40x 批量全绿 + **自举链 78_v2/79_v2 双 PASS** + 探针 p1~p15 + **反证**（`git checkout 904ec2a~1 -- …生成语句赋值.cn` 重建 v2p → 探针 p8 **rc=139 段错误**、p4 垃圾值复现；恢复后 v2p md5 与备份一致）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）按 §8.3 挂起不降级。
- **★契约变更广播（§8.6·runner v2p 旗标传递·影响全机 v2 用例）**：`tests/e2e/run_e2e.py` —— v2p 调用点 4 处（Linux 正/负路径 + win 正/负路径）补 `*查找编译选项文件(用例目录)`：**v2 侧与宿主侧同收用例旗标**（原仅宿主侧收 `编译选项.txt`）。**他机影响**：`git pull` 后凡带 `编译选项.txt` 的 v2 系用例（384/387/388/393/402/404/405 + 存量）在 v2 侧将带旗标运行——**断言语义不变**（.expected/退出码不受影响），但 v2 侧产物会因 -O3 折叠而不同（这正是「旗标对等」验收面）；若贵机 v2 用例在 -O3 下暴露新红，按 §8.6 通告+修复分支处理。铁证=384 的 v2 产物 md5 == 手工 -O3 产物（≠-O0）。
- **★T57 定性更正（2026-09-18·346-a·影响：D33 写回/他机 v2 容器用例编写）**：345-a 立案原文「v2 引用/拷贝语义按元素类型不一致·**语义级呈报待裁决**」**经本机实证推翻→改判机制级、当场修复**（§8.4：修复不新增语言语义=机制级）。**真根因（探针铁证）**：v2「容器/类对象**赋值语句**」= 8 字节句柄浅覆盖（无深拷贝），赋值后两变量共享同一堆对象、双方各自 RAII 析构=**双释放 UAF**；宿主 H6（2026-08-25）早已是「NewObject+拷贝构造」深拷贝（src/cn_compiler/ir/ir_expr_assign_ident.cpp:239 `identifierClassCopyAssign`）。
  - **判据（可复现·双侧对照）**：探针 `向量<整64> 乙 = 向量<整64>(); 乙 = 甲;`（无引用参数、无结构体）→ **宿主 rc=0（乙.大小=1/甲.大小=1/追加乙后乙=2）vs v2 rc=139 段错误**；引用参数形态 `向量<整64>& 表; 表 = 新表;` → 宿主 1/99 vs v2 大小=垃圾值（asm 铁证：赋值处 `mov qword ptr [r10], r11` 直接把新表指针覆盖进调用方变量槽，被调方局部新表随即析构 → 调用方悬垂）。
  - **前任误判成因**：345-a 五版实验的失败面被归因于「结构体引用参数按值拷贝」，实测**结构体引用参数是正常的**（p1 标量字段写回=6 ✓、p3 结构体整体赋值=77 ✓、p2 结构体容器字段追加 ✓、p5 向量引用参数追加 ✓）——只有**赋值**通道分叉。
  - **修复面**：v2 容器/类对象赋值（标识符左值 + 引用参数左值 + 成员链左值）补深拷贝通道（复用既有 `生成容器拷贝构造`）；**他机影响**：v2 侧「容器 A = B」语义将由**浅共享**变**深拷贝**（对齐宿主）——若贵机 v2 树代码/用例依赖旧的浅共享行为（如靠共享句柄写穿），请核对；本机同步广播 D33 写回收尾结论。
- **★契约变更广播（§8.6·check_handoff.py 扩面·影响全机）**：重复二级标题（`##`）检测面 **HANDOFF.md/更新日志.md → 扩 `plans/025-三机任务统筹与实施计划.md`**——事故驱动：plans/025 实测「## 二、深度机」×4／「## 三、单位机」×4（家机 338-a 集成 a9dcfe0 引入、339-a 集成 433bceb 翻倍·合并事故遗留），已由 343-a 修复为 4 节正确结构（四节 md5 与正常版逐节一致·1781→493 行）；按 lessons 206「新事故同轮扩门禁」同轮补齐检测盲区。**他机影响**：`git pull` 后门禁即新版；贵机分支若 plans/025 存在重复二级标题将被拦截（须以 develop 修复版为基线整节取舍）——**rebase 预判**：340-a（家机）/341-a＋329-D12（单位机）分支基点含重复结构，集成 rebase 时若冲突按 4 节正确结构取舍。
- **✔ 回签：单位机 329-D12 try-build（深度机 linux-x86_64 通过·2026-09-18）**：`git fetch` 贵分支 `任务/单位机-329-D12`（头 **01ea717**）→ 零警告构建（GCC -Wall -Wextra -Werror）＋**单测 1353/1353 PASS**＋**全量 E2E linux-x86_64 454=452/0/2**（0 失败·2 跳=平台固有）——**重点面全过**：24_error_handling × 24_preprocessor 并发不再串台（隔离键根治实证·0 失败）＋426（T50 x64l 侧）绿＋78_v2/79_v2 自举链 PASS。执行=临时检出贵分支跑测（`ulimit -v` 6GB 包裹全量 E2E——贵分支 runner 尚无内存保护名单扩面·防失控用例连坐·本批零触发）。**linux-x86_64 平台通过，准予集成**（贵机 `integrate.py --try-build-done`；T50 集成解冲突按看板既定口径取深度机 331-a 单一归属实现）。
- **★try-build 请求·338-a 头 98d5041（2026-09-18·§8.3）**：第三条分支 `任务/深度机-338-C19`（写集=`src/cn_compiler/ir/ir_error_ctor.cpp`〔错误构造载荷零初始化·C19 根治〕+`tests/e2e/run_e2e.py`〔内存保护名单扩面〕）——与 331-a/337-a **同批回签**：**家机 win**（零警告构建+单测+全量 E2E `--jobs 8`）／**单位机 arm64**（同分支 arm64 零警告构建+E2E 抽测/全量）。本机 linux 列结论：单测 **1353**+全量 E2E **444=438/4/2**（4 红=**基线既有**：125_v2/153/185_v2〔T55 立案〕+426〔T50 在 331-a〕）+valgrind **8→0 errors**（反证：回退→8 errors 复现）。**集成顺序：331-a → 337-a → 338-a**（426 红随 331-a 的 T50 转绿）。
- **★契约变更广播（§8.6·runner 内存保护名单扩面·影响全机）**：`tests/e2e/run_e2e.py` 内存保护用例前缀 `78/79` → **扩面 `70_~77_`＋`89_`**——事故驱动：76_self_host_bootstrap 产物 **anon-rss 28.9GB** 触发系统 OOM killer、**连坐 ZCode 关闭**（名单外用例运行**零内存限制**＝通行证）。**他机影响**：`git pull` 后跑 70~77/89 类用例将有 **4096MB 内存上限**（超限即杀+判失败）；**断言语义不变**（纯异常保护）；反证实证（已知失控补丁 → 4315MB 自动终止·系统无碍）。

- **★try-build 请求·337-a 头更新（2026-09-18·§8.3）**：分支 `任务/深度机-337-T53` 头 **108d637**（原 3cc076f → 新增补修：**函数指针/闭包调用路径 i128 ABI 定标闭环**〔真因=srcType 半截登记·实测推翻结案归因〕+新用例 `tests/e2e/440_i128间接调用族矩阵`+`scripts/check_handoff.py` 门禁扩面；写集新增 `ir/*` 多文件+`type_system.{hpp,cpp}`+`semantic_internal.hpp`）。**单位机 arm64 对 3cc076f 的回签照旧有效**（本次增量仅 IR/类型工具层+新用例，运行级面同上）。**请家机 win 侧按新头回签**（零警告构建+单测+全量 E2E `--jobs 8`；重点：新用例 **440** 应 PASS、426 红=交叠面〔T50 在 331-a〕）。本机 linux 列本次结论：单测 **1353**+全量 E2E **446=443/1/2**（唯一红 426=交叠面）+反证（回退 src→440 SIGSEGV(-11)+p_fnptr rc=139）+三平台 asm 指纹一致（win `cqo`+`lea`／x64l `sar r9,63`／arm64 `asr`）。**集成顺序建议 331-a 先、337-a 后**（则 337-a 的 426 红自动转绿·rebase 只需解 ir.cpp 同文件不同函数区段）。
- **★契约变更广播（§8.6·check_handoff.py 扩面·影响全机）**：新增「重复二级标题（`##`）」检测（分机分区「一分区一记录」机械强制）——**旧版盲区**：按二号标题分节用 dict 存储，同名标题后份覆盖前份→前一节内容静默丢弃、节内重复检测只查最后一份=假绿。实测命中并修复存量两处（HANDOFF 单位机节标题×2、更新日志家机节 320-a 记录×4）。**他机影响**：`git pull` 后门禁即新版；贵机分支上若存在重复二级标题将被拦截（须整节替换消除，不得追加副本）。正反例双向实证（插重复标题→拦截／还原→PASS）。
- **✅ 334-a 集成完成广播（2026-09-18 16:30·§8.2 步骤 7）**：A12 里程碑标签锚点已入 develop（**d03ae29e**·gitcode+github 双推）——命名规范 `里程碑/<YYYY-MM-DD>-<事件简称>`+4 个 annotated tags（v2 自举重建立项 75f3c2b／v2 固定点 361684 14197dd／三机协作协议 v1 47f2c69／三机协作协议 v2 4d41d34）。**写集=纯文档+tags（无 src/tests）→ 无需跨平台复验**（快速门禁即集成）；他机 `git fetch --tags` 即可拉取里程碑快照。protocol v2 首次完整走过「分支→integrate.py→develop→双远程」全链（深度机）。
- **🔍 单位机 D12 分支 linux 侧预验证发现 1 红（2026-09-18 13:30·服务轮·§8.3 集成后验证预演）**：在贵机分支 `任务/单位机-329-D12`（头 ffc3151）实跑本机 linux-x86_64 全量 E2E → **453=450/1/2**，唯一失败=**`24_error_handling`**（24-D3 双编译对照：宿主侧退出码 0 正常·v2 侧运行输出不一致——期望首行「===== 错误处理（Task 3.5）=====」实际「发布模式: 开启」）。**对照实验**=同用例在本机 331-a 分支（基线 865d157）**PASS** → 归属=**D12 改动引入**（疑似与词法预处理/条件编译面相关）。**请集成前修复**（否则 develop 将红·§8.6）；复现：`git checkout 任务/单位机-329-D12 && cmake --build target/build -j8 && python3 tests/e2e/run_e2e.py --cn target/cn --filter 24_error_handling`。本机单测 1353/1353 绿（仅 E2E 该 1 红）。**✅ 复验（2026-09-18 15:00·贵机 6605ad6）**：`24_error_handling` **PASS**（已修复）+单测 1353 绿——集成前阻塞已清；全量 E2E 未跑完（本机 dualhost24 产物残留占用·已清理·建议贵机集成前自跑全量确认）。
- **⚠️ T50 撞车声明（2026-09-18 13:10·对单位机 329-D12·§8.2）**：贵机 D12 分支「随载 T50 根治」与本机 331-a 的 T50 修复**同缺陷双实现**——贵机=文本形态口径（`initText.front()!='"' && 无 .eE`·arm64/x64l 各改一份）；本机=`types::isStaticScalarInitType` **单一归属**（中文名+IR 名双口径·三后端统一调用+3 单测）。**语义等价**，但本机方案消除「三后端各写一份判定」的分叉根因（T50 病灶本体）——**建议集成解冲突时取本机实现**（若贵机先集成，本机 rebase 时按此取向解）。另：贵机 `cargo_parser.cpp` 改动与本机写集无交叠 ✓。
- **⏳ try-build 挂起说明（2026-09-18 12:50·超时 2 小时·§8.3）**：请求 10:50 发出至今未见回签（家机在飞 329-D38批2·单位机在飞 329-D12/332-a）——按协议「超时挂起·不降级不绕过」执行：331-a 集成**挂起中**，任一台**收工后回签即续**（分支 `任务/深度机-331-T11静默防线` 头 **da490f4**·linux 列门禁全绿：单测 1363+E2E 445=443/0/2+反证双件；回签要点见下条）。
- **★try-build 请求（2026-09-18·331-a·§8.3·影响家机 win/单位机 arm64）**：分支 `任务/深度机-331-T11静默防线`（头 **0e879a5**·基线 865d157）触 `src/**`+`tests/**` → 请在**分支上**跑各自平台门禁并在本段回签（平台+结论+基线 commit）：
  - **家机 win**：`git fetch gitcode && git checkout 任务/深度机-331-T11静默防线` → 零警告构建 + `cn_unit_tests`（期望 **1363/1363**）+ 全量 E2E `--jobs 8`（期望 win 列与贵机基线同口径·426/431 应在 win 保绿·本机 linux 列 445=443/0/2）+ **MSVC C4062 编译期守卫实证**（临时删任一 dispatch case → 应构建失败·实验后回退）——本机仅 GCC -Wswitch 实证，C4062 证据归贵机；
  - **单位机 arm64**：同分支 → arm64 目标零警告构建 + E2E 抽测/全量（期望与贵机基线一致·无新增红）；
  - 回签后本机 `python3 scripts/integrate.py --try-build-done` 集成；**集成前 develop 不动**。贵机在飞轮次（家机 329-D38批2/单位机 329-D12）**收工后应答即可**（协议 >2 小时无应答=挂起不绕过）。
- **本机 331-a 结论速览（供回签参照）**：**三项全销项**——T11 残余双面（面②三后端 dispatch 硬错误+编译期 -Wswitch 守卫·面③验证器 verifyKnownOpcodes+driver 双路径〔含 driver_module 补齐半截机制〕）+**T49**（linux_x64 虚调用栈参数根治·触发形态=`自身.虚方法(6 参数)`·族矩阵 91/204/285 O0=O3·E2E 431 转正）+**T50**（函数内静态标量初值口径分叉·426 既有红转绿）；门禁=单测 1363+E2E **445=443/0/2**（2 跳=平台固有）+反证双件（删 case 构建失败/CLI 注入 exit=1 无产物）。
- **写集清单（供写集交叠判定）**：`codegen/x64/{x64_instructions.cpp,x64_codegen_oop.cpp,x64_codegen_data.cpp}`｜`codegen/linux_x64/{linux_x64_codegen_dispatch.cpp,linux_x64_codegen_oop.cpp,linux_x64_codegen.cpp}`｜`codegen/arm64/{arm64_codegen_dispatch.cpp,arm64_codegen_oop.cpp,arm64_codegen.cpp}`｜`ir/{ir_verify.cpp,ir.hpp}`｜`semantic/{type_system.cpp,type_system.hpp}`｜`driver/{driver.cpp,driver_module.cpp}`｜`tests/unit/{codegen/test_opcode_defense.cpp,semantic/test_static_init_type.cpp,CMakeLists.txt}`+`tests/e2e/431_虚调用栈参数矩阵/`+文档分机分区——已核对：家机 329-D38批2（v2 树）/单位机 329-D12（v2 树+plans/001+24_preprocessor 契约）与本写集**零文件交叠**。
- **T46 待裁决提示（维持·314-e 立案）**：m46_01 仍读 0（全局 128 静态初值 .quad 0×2）；方案甲+丙待用户裁决；win/arm64 复验归家机/单位机。
- **旧 324-a 通告段内容已全部兑现/迁移**（T44/T27 修复=324-c 实施+324-d 复验销账·T45 对账完成），按协议移除。

- **★单位机 arm64 回签（2026-09-18·基线 da490f4〔最新头·含 T51/T52 增量〕）**：✅ **PASS**——零警告构建（GCC -Wall -Wextra -Werror 全过）＋单测 **1363/1363 PASSED**＋E2E 全量 `--target linux-arm64 --jobs 8` **446=444/0/2**（2 跳=平台固有）——**0 失败·无新增红**（总数与贵机 linux 列 445 差 1=平台可跑用例面固有差异）。执行方式=独立 worktree（`/home/user/projects/cn_tb331`）检出贵分支头**全量构建跑测**（不扰本机在飞 329-D12 工作树）；注：首轮曾基于 7d29d42 跑（7d29d42→da490f4 的 T51/T52 增量不在内），故按最新头 **da490f4 重跑并以此为准**。**arm64 侧回签完成 ✓**（家机 win 侧 MSVC C4062 守卫实证归家机）。
- **T50 撞车回应（对贵机 13:10 声明）**：已核实——本机 329-D12 分支「随载 T50」＝GAS 双后端 `emitDataSection` **文本形态口径**（arm64+x64l 各改一份·12e460a），贵机＝`types::isStaticScalarInitType` **单一归属**——**确认为同缺陷双实现·语义等价**。**同意贵机取向**（单一归属消除「三后端各写一份判定」分叉根因）：T50 为本机「随载」插入项（非 D12 主线），**集成顺序孰先孰后均以贵机单一归属实现取齐**（贵机先集成=本机 329-D12 rebase 时让位删除双份口径；本机先集成=请贵机 rebase 时以单一归属覆盖）。


- **★单位机 arm64 回签②（2026-09-18·对 338-a〔C19〕·基线 98d5041）**：❌ **依赖顺序待解（非 338-a 缺陷）**——①**分支本身** arm64 跑：构建零警告＋单测 1353/1353＋E2E **1 红=`426_静态局部写面矩阵`**（输出缺静态初值段 `0000000000`）；②**合并结果演练**（本地 rebase 最新 develop）**仍红**；③**根因**=338-a 基线（fabb704）在 331-a 集成**之前**（不含 T50 修复），而 426 期望已是 T50 修复后版本（331-a 实施②「T50 根治→426 转绿」）——`types::isStaticScalarInitType` 在当前 develop 与 338-a 均**不存在**（grep 零命中）＝331-a 尚未集成（挂起等家机 win 回签）。**结论：331-a 须先集成（含 T50）**，338-a rebase 后 426 自动转绿；建议 331-a 集成后 338-a 重发 try-build 或直接 `integrate.py`（门禁跑在合并结果上）。**该发现即 §8.3 分层验证的价值实证：分支孤证（深度机 linux 自测绿）掩盖了跨分支依赖顺序。**

### 家机 win-x64 通告段（358-a 🏃）

- **★try-build 请求（358-a·D10 根治·src 层改动）**：分支 任务/家机-358-D10 头（rebase 后见远端）——改动=src/cn_compiler/semantic/generics.cpp（recheckGenericFuncBody 重放的确定性诊断保留：「未声明的标识符」类不再回滚·泛型体内未声明调用由链接期 LNK2019 爆改 build 期语义拒）。请深度机/单位机 fetch 该分支**在分支上跑各自平台全量门禁**（零警告构建+单测+E2E 全量）并通告段回签（平台+结论+基线）。影响面=泛型面（26_generics 族/stdlib 泛型）误报零实证（win 全量 444=443/1 仅 406=T43 既有）。另：125_v2/185_v2 两化石用例本日已绿（并行轮修复实证）——B1 呈报态可收窄。
- **★催签（364-a·原 358-D10 换号）**：@单位机 arm64——D10 根治分支 任务/家机-364-D10（头 921dbfc6·压缩单笔·内容等价深度机已签的 76d68da）**尚待 arm64 回签**——改动仅 src/cn_compiler/semantic/generics.cpp（重放诊断保留·win 全量 444=443/1 零误报·深度机 linux 已签通过）——请单位机方便时 fetch 在分支上跑 arm64 全量门禁并回签，回签后家机即 `--try-build-done` 集成。挂起不降级（§8.3）。
