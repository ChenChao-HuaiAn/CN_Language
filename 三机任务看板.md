# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|---|
| 家机 win-x64 | ⬜ | - | - | - |
| 单位机 ARM64 | 🏃 | 328-a 协议v2落地（用户授权插队·与326-a写集零交叠）：三机全并行架构=分支即开发+integrate队列集成+分级修复——AGENTS §8重写/integrate.py/plans/027·分支 任务/单位机-328-协议v2 | 93b11df |
| 深度机 linux-x86_64 | ⬜ | - | - | - |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）
> - 🏃 **开发轮进行中**（**占锁；全表至多一机**——231-a 恢复唯一性；行=任务名+指针+基线+时间）
> - 🧪 **验证轮进行中**（探针检测/E2E 测试·自愿登记·不占锁·行格式同 🏃）
> - **开工**：`git fetch` → 改本机行（🏃 + 任务名 + 基线commit + 时间 + 指针）→ push 主远程 `gitcode`
>   ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> - **收工**：本机行**整行清空**（⬜ + 各列「-」），随本次提交一起 push——结论进 plans/025 收工回填，
>   **看板行不留结论也不留通告**（232-a 用户令收紧，原「可附一句结论指针」废止）。
> - **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.4）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在认领开发轮前提下进行，并在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 家机 win-x64 通告段（326-a 🏃）

- **★v2 行为变化广播（2026-09-18·326-a·D38 批1·§8.4·影响全机 v2 用例编写）**：v2 负测防线补齐词法/语法层 3 项——**①独立 `;`（裸分号）现拒绝**（含块内·原静默跳过）；②**`当(...);`（当体非块）现拒绝**（原 `;` 跳过+后续语句吞进当体）；③**未闭合块注释现拒绝**（原静默扫 EOF）。存量全量 E2E 零误伤实证（v2 树与 v2 用例族全绿）；**他机新写 v2 用例避免此三形态**（负测防线对齐宿主·批2/3 语义层 13 项待续）。
- **C24 已销账**（深度机 324-d 复验：m44_01/m27_01/m27_02~05 全转绿+探针 7/7+回归轨历史性清账）；**待用户裁决**：T43/T46/T47/T48（plans/021 §三-G）。

### 单位机 ARM64 通告段（297-a）

- **B10 消歧判据变更广播（2026-09-17·§8.4·影响家机 296-a v2 ? 链）**：宿主 `?` 三元/后缀传播消歧已改**同层 `:` 配对前瞻**（ternaryColonAhead——`?` 后同层有配对 `:` 判三元·否则传播；原「表达式起点白名单」废弃）——`a ? -b : c` 不再要求括号。**v2 侧解析判定分流须同口径**（家机 296-a v2 ? 链实施时对齐·否则双侧分叉）；规范条文=plans/001 §4.5+spec 01b §5.8/5.8B。后缀传播合法用法（`x? + 1`=(x?)+1）行为不变（E2E 60 复验绿）。
- **T24 ++/-- 语义补全广播**：宿主已补后缀 `i++` 返回原值/C 语义+01b 优先级表登记（后缀 13 级/前缀 12 级）——v2 树同型面（自举编译器）待对齐轮处理（登记 plans/021 T24 行）。

### 单位机 ARM64 通告段（286-a）

- **daemon 结构变更广播（2026-09-17·影响深度机/家机采样轮）**：S1b 分文件化——①日志改写 `tests/cnsmith_hits/日志/YYYY-MM.md`（旧 采样日志.md=固定指针 9 行·禁止再追加）；②记法=变化时记+汇总；③种子=时间戳到秒（不再按轮号·重启不撞）；④差分前自动删旧分歧清单+重建后核验 target/cn 新鲜度。**深度机在跑的旧版 daemon 请停→pull→重启**（旧版仍写单文件=与月文件制分裂+pull 会因工作树脏失败）；家机下次采样前 pull 即可。存量（含贵机在飞轮次）已折入 日志/2026-09.md 收档。
- **T22 呈报同步（缺陷零容忍）**：arm64 -O3 位运算常量立即数非法汇编（31 样本入库 run_err/）——贵机采样若切 arm64 目标会复现；win/x86_64 免疫。方案呈报用户裁决（plans/021 §3-G T22）。

### 深度机 linux-x86_64 通告段（324-a）

- **★x64l 复验漂移广播（2026-09-18·324-a·影响家机修复轮·T44/T27 修复未在 linux_x64 兑现）**：①**T44（316-a）**：i128 三元族 x64l 仍 O0/O3 双级 SIGSEGV——实弹 m44_01 未消+扩面 7 形态（p314a t44_ternary/nest/u128·p314b x01~x04）；根因=316-a 写集仅 `codegen/x64/`（win 专属），linux_x64 零改动 → asm 仍 `mov qword ptr [rbp], r10`（高半槽偏移 0）；非三元 5 形态健康（if2/chain3/outside/arr/arg/ret）。②**T27（319-a）**：x64l -O0 两处——整32 INT_MIN/-1 SIGFPE（`cmp r9,-1` 64 位比较 vs `mov r9d` 32 位装载→特判永不命中）+窄整负值→128 宽化零扩展（缺 movsxd·m27_02~05 O0 错值）。**修复方向=win 同款改动同步 linux_x64 后端**（emitCopy i128 双半/槽补登记/`cmp r9d,-1`/宽化 movsxd 四点·与在飞 320-a 写集零交叠可搭车）；证据=HANDOFF 深度机节+日志 2026-09-18。
- **★用户裁决落地（2026-09-18）**：上述 T44/T27 的 x64l 四点修复=**家机 320-a 收工后下一轮认领实施**（已落盘 plans/021 §三-C **C24**）；复验矩阵=target/p314a+p314b（就绪·仓库根跑）+回归轨 m44_01/m27_01~05 归零+E2E 转正；修复后 x64l 运行级复验归深度机。
- **T45 复核对账（324-a）**：实弹 m45_01~03 x64l 全转正（值正确 123456789012/42/123456789012）+p314a t45_* 9 形态全健康——C23 修复 x64l 侧兑现 ✓。
- **★T46 待裁决提示（314-e 立案·324-a 复核）**：m46_01 仍读 0（未修复·O0=O3 一致=差分轨盲区）；方案甲+丙待用户裁决；win/arm64 复验归家机/单位机。
