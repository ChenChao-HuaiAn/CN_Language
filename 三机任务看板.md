# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|---|
| 家机 win-x64 | 🧪 | M3 采样轮③（验证轮·不占号）：更大样本 1000 差分（新种子·不同规模）+T37 win 列复验（选择语句负值标签三形态） | 4eea0ee | 09-17 16:20·计划 plans/025 §一 |
| 单位机 ARM64 | 🏃 | 297-a 全修（用户裁「按你的方式全部修复」）：T25 浮点Copy汇合槽位根治+T26同族三后端同查+T24 后缀自增返回原值+B10 方甲同层:前瞻消歧+规范条文+406~408新用例 | 478d804 | 09-17 14:42 | plans/025 §三.17 |
| 深度机 linux-x86_64 | 🧪 | M3 采样轮续（增量5 收工 a53df0f+窄宽轮=T36 立案「字面量窄化缺范围检查」同值两口径·行 63 新加）；daemon 续跑+日志压缩监控 | a53df0f | 09-17 15:3x·计划 plans/025 §二 |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）
> - 🏃 **开发轮进行中**（**占锁；全表至多一机**——231-a 恢复唯一性；行=任务名+指针+基线+时间）
> - 🧪 **验证轮进行中**（探针检测/E2E 测试·自愿登记·不占锁·行格式同 🏃）
> - **开工**：`git fetch` → 改本机行（🏃 + 任务名 + 基线commit + 时间 + 指针）→ push 主远程 `gitcode`
>   ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> - **收工**：本机行**整行清空**（⬜ + 各列「-」），随本次提交一起 push——结论进 plans/025 收工回填，
>   **看板行不留结论也不留通告**（232-a 用户令收紧，原「可附一句结论指针」废止）。
> - **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.4）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在认领开发轮前提下进行，并在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 家机 win-x64 通告段（298-a 🧪）

- **T25 win 侧同挂实锤·「arm64 单侧」定性纠正（2026-09-17·影响单位机 297-a T25 修复轮）**：
  win-x64 实测 O3 三形态静默归零——k01 双分支同名单（1.5→0.000000）/k08 分支内嵌三元（7.5→0.000000）/**k04 复合赋值 +=（3.5→0.000000·arm64 免疫的 win 独有形态）**；O0 全对。三平台全景=arm64 挂 k01/k08·win 挂 k01/k04/k08·linux_x86_64（深度机批 5 三元形态）全对——**修复须三后端同查（win/arm64/x86_64）+win/arm64 运行级复验**，勿按「arm64 单侧」修漏 win。win 免疫面=k02 三分支/k05 交换/k06 循环/k07 整32/k09 浮32（与 arm64 免疫面一致）。探针=target/p298a/probes/t25_matrix.cn（9 形态 O0/O3 对照）。**win asm 铁证（t25_matrix-O3.asm 行 99~124）**=汇合槽 [rbp-960] 由前驱浮点 Copy 写入（movsd×2）而后继 `mov eax,[rbp-80]; mov [rbp-104],eax` **4 字节整型搬运写另一槽**+打印读 [rbp-104]（movsd 8 字节）→读半槽（高 4 字节 0）→0.000000——与 arm64「前驱写 [#-88]/汇合读 [#-64]」同族铁证（win 具体形态=槽位双套+宽度 4 字节半槽）。**消费位扩面**（t25_consume.cn·win O3）：实参位（v02 0.25 应 1.75）/拼接（v03 0 应 3）/转整（v05 0 应 3）/假分支（v06）/汇合后复合（v07）全归零——返回位例外（直返不经汇合 Copy·v01 对）。**k04 族扩散（t25_k04f.cn）**：复合赋值全族（+= -= *= /=·O0 3.5/2.5/5.0/2.5）+**显式 `m5=m5+1.5` 形态** win O3 全归零——win 挂面=「初值非零+分支内累加/赋值」全形态（非复合赋值特有）。**k02 免疫机制对照**（asm）：三分支三前驱均写 [rbp-968] 且打印读 [rbp-968]（一致=对）；k01 双分支 额外出现整型 4 字节搬运 `mov [rbp-104],eax` 与浮点汇合槽 [rbp-960] 分叉（打印读 -104）=修复轮直接线索（使用点重写对浮点多前驱 Copy 注入的宽度+槽位双错·仅特定分支结构触发）。**IR 层根因链（`cn ir -O3` 对照·k01.cn/k02.cn 极小形态）**：k01 bb1 尾 `搬运 %v9 %v7 (f64)`（=256-a Opcode::Copy/Phi 降级指令）而 bb3 打印直接用 **%v9**（使用点重写生效）——该搬运在 asm 发射为 **4 字节 mov**（i32 宽度·应 movsd）→槽半写→读 8 字节含 0 高位→0.000000；k02 三分支 CFG 未触发重写·bb3 保持 `加载 %v13 a$1`（movsd 8 字节）→对。**=T25 与 T26 同根因（256-a 使用点重写）两种表现**：T26=值被换常量（x86_64）/T25=值被换跨块 Copy+Copy 宽度错（win/arm64）。**面联想矩阵（t25_face.cn·载体扩散）**：变量载体全挂（w01 双变量/w05 汇合值作条件链/w06 分支内表达式）；**结构体字段（w03）/数组元素（w04）载体免疫**（字段地址直写不经变量槽 Copy）；嵌套内层（w02）例外——载体面边界完整。
- **同轮 win 列复验结论**：T24 win 同挂确认（j=i++ →4 应 3·语义层 4 形态）；T27 win 同挂（rc=0xC0000095 整数溢出异常·O0/O3 双级别——win 侧 SIGNAL 等价物）；**T26 win 免疫确认**（O0=O3 全对·x86_64 单侧成立）。面台账行 57 win 列补齐在即。

- **T39 立案广播·P1 发射缺陷（2026-09-17 🧪 300-a M3 采样独家捕获·win 侧实测·影响跨机复验）**：**整128 左移 `<<` 全线错误**——`整128(100) << 2` 低 64 位非确定（打印 3 次 3 值·栈残留 ASLR）；**asm 铁证**（i128_sh3.asm）：移位发射只处理高 64 位通道（行 68-71 读 [rbp-24] shl），**低 64 位源槽错位**（行 57 写 [rbp-32]=100 全文无读·行 72 读 [rbp-72] 全文无写=从未接入）=低通道结果读未初始化栈。O0=O3 一致；**覆盖缺口**=E2E 11_i128/324 零移位形态（全量绿未暴露·采样轮独家捕获实证）。复现：`整128 百=整128(100); 打印(百 << 2);` 三跑三值。**x86_64/arm64 复验归深度机/单位机**（同源代码路径预期同挂）；修复=发射低通道源槽对齐+负测转正。探针=target/p300a/{i128_shift,i128_sh3,t39_fam,t39_bits}.cn+asm。**族扩散矩阵（B9）**：挂=<< / >> / <<= / ^（^ 非确定·& | 却又对）；健康=& / | / + / -（__cn_* 辅助通道）/比较/乘除（E2E 11 绿）——面=**128 位内联发射类运算的部分通道缺陷**（非辅助函数通道）。

### 单位机 ARM64 通告段（286-a）

- **daemon 结构变更广播（2026-09-17·影响深度机/家机采样轮）**：S1b 分文件化——①日志改写 `tests/cnsmith_hits/日志/YYYY-MM.md`（旧 采样日志.md=固定指针 9 行·禁止再追加）；②记法=变化时记+汇总；③种子=时间戳到秒（不再按轮号·重启不撞）；④差分前自动删旧分歧清单+重建后核验 target/cn 新鲜度。**深度机在跑的旧版 daemon 请停→pull→重启**（旧版仍写单文件=与月文件制分裂+pull 会因工作树脏失败）；家机下次采样前 pull 即可。存量（含贵机在飞轮次）已折入 日志/2026-09.md 收档。
- **T22 呈报同步（缺陷零容忍）**：arm64 -O3 位运算常量立即数非法汇编（31 样本入库 run_err/）——贵机采样若切 arm64 目标会复现；win/x86_64 免疫。方案呈报用户裁决（plans/021 §3-G T22）。

### 深度机 linux-x86_64 通告段（274-a）

- **契约变更（runner·2026-09-17·§8.4 广播）**：`tests/e2e/run_e2e.py` 三处——①261-a 断言输出=是用例的 linux 分支锚行比对跳过补齐（win 既有语义对齐）；②用例编号提取保留 `_v2` 段（产物目录/文件名 v2src/v2work/dualhost/v2asm/v2out 对 _v2 用例改带 `_v2` 后缀）；③四处锚行比对加 v2src 路径适配（测试用例文件零改动）。**影响面**：三机下次全量 E2E 复跑即覆盖（产物目录名变化=首跑全重建·无存量断言变化）；家机 win 侧编号隔离/锚行适配同轮生效，下轮全量请核对 409/0/0 口径不变。影响面执行完毕后或下次本机 push 删除本段。

（深度机无其他在飞通告。）
