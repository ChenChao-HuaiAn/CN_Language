# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 | 通告 |
|---|---|---|---|---|---|
| 家机 win-x64 | 🏃 | **206-b 开工**（基线 a1eed1a）：波 4 子轮——复制(x) 泛型内置双侧实施（宿主 semantic_call 特判+genCopyBuiltin 按类型分派〔字符串/标量/结构体/容器〕；v2 内置识别+类型文本分派；泛型体单态化路径类型推导）；写集=semantic_call.cpp/ir genCopyBuiltin 新函数+v2 语义检查表达式.cn/生成调用分派（与深度机 205/206-a v2 侧解析语句.cn、单位机不相交）；验收=五类型分派探针+E2E 284 双编译对照+全量门禁 | a1eed1a | 09-15 18:00 | 行为改动轮——锚定链随层 2 实测定 |
| 单位机 ARM64 | ⬜ | **196-a 收工（尝试→按纪律回退）**（基线 cad4ea3）：**F1-28 arm64 寄存器分配接线**——接线图谱全量探明并实现（读侧 `loadOperandToX` / `operandText`·`resultText` / 结果落位 ~25-40 处 `storeResult` / `emitLoadVirtualTo` 统一 id 取数 / 返回位 `emitTerminator`+`emitEpilogue` / 序言尾声 x19~x28 + `stackParamBase()` 基址补偿 / backend_factory 联动）；**E2E 门禁暴露阻断性根因在分配器**（`reg_alloc.cpp` 活跃区间对「循环体内重定义、跨迭代使用」值判定过短 → 同寄存器双占；实证 115 清空循环少析构 1 次 / 235·245 校验和 191≠255）→ **git checkout 回退，源码零净变更**（工作树干净、探针正确、单测 1325/1325）；接线图谱 + 接线期修掉的 3 个既有缺陷（resultText 未感知分配/返回位槽文本转换/批量替换自调用）+ 四项教训已留档 plans/025 §三.2-7、lessons 196 段；**新立 D7=分配器循环携带值隐患核查（含 x64 同族核查）**；下一轮预登记 197-a（候选=D7 专项 / F2-33 纯 stdlib 三项 / D1 续波） | cad4ea3 | 09-15 15:10 | **196-a 通告（§8.4）**：**回退（零净变更）→ 三机无需复跑**（产物与 188-a 基线一致）；**新立案 D7**：分配器循环携带值隐患——**x64 后端同用该分配器且 -O2 默认启用，家机/深度机复跑时可留意 win 侧容器析构类用例**（115/235/245 若在 win 侧同型失败即同族实证） |
| 深度机 linux-x86_64 | 🏃 | **208-a**：解析表达式.cn 解析原子 149 行拆分（D1 续波·v2 语法；程序化切片+双名单差集审计+单文件快验；v2 侧锚定链重锚验证链） | efa2477 | 09-15 18:50 | - |

> 状态：⬜ 空闲 ｜ 🏃 开发轮进行中（占锁；**多机并行须任务不同、写集不相交**——2026-09-15 用户裁决）｜ 🧪 验证轮进行中（自愿登记，不占锁）。
> **开工**：`git fetch` → 改本机行（状态 🏃 + 任务名 + 基线commit + 时间）→ push 主远程 `gitcode`
> ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> **收工**：本机行改回 ⬜（可附一句结论指针），随本次提交一起 push。
> **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。

## 契约变更通告（AGENTS.md §8.4）

> 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
> **三机共享契约**后：须在认领开发轮前提下进行，并在上表本机行「通告」列写简短标记 +
> 下方详情段展开（变更内容/影响面/哪些用例与平台需复跑）；其它机器开工 fetch 即见。
> 通告由发起者维护：本机下次 push 时新覆盖旧或删除。

### 188-a 契约变更详情（B11 变量常量传播实装 + arm64 锚定链重锚，2026-09-15，单位机 ARM64）

- **变更内容**：①**语义面**——B11「编译期常量空指针」由「操作数=`无` 字面量」扩展为「**变量常量传播形态**」
  （`plans/023` §九 已批准定义实装；`plans/001` §5.3 判据终态）：恒空 ⇔ ①`无` 字面量种子 ∧ ②全函数无其他写入
  （失格=非空写入/复合赋值/自增自减/**取地址**/**引用参数实参**）∧ ③传播边不动点（`p = q`）；使用点延迟登记、
  函数/方法体**收尾统一判定**（流不敏感=零假阳性优先）；**三操作面**：解引用/成员访问/下标访问；
  **字面量面补齐**：`无.x` / `无[0]` 由间接类型错误 → B11 专文诊断。
  ②**用例集**：新增 `279_空指针常量传播负测`（宿主负测）/ `280_v2_空指针常量传播负测`（v2 负测·三面诊断固化）/
  `281_空指针常量传播双编译对照`（正测 rc=41）；**不动既有 `.expected`**。
  ③**锚定链**：**arm64 新基准 561364 行**（md5 `66105ebe…`；上基准 555442=158-a；+5922 = v2 树 159-a~190-a 累计增量）；
  **v2p 新指纹 `c5844263…`**（v2 树改动 → 各机须重建本机 v2p）。
- **影响面与复跑要求**：**新增硬错误类**——三机复跑**全量 E2E**（预期本机口径：arm64/linux-x86_64=新增 3 用例过、
  总数 309/307/0/2、0 败；win-x64=309/309/0/0〔无平台跳〕）+ **各机重建本机 v2p**（v2 树已改）+
  **各机重锚本机锚定链基准**（arm64 已给新值；win/X64L 待各自实测广播）。
- **预期风险面**：若某机树内存在「恒空指针解引用」形态（`变量 = 无` 后无其他写入再解引用/成员/下标），
  该用例将转为编译失败——按 D6 语义这是**应报的确定性 UB**；如遇请回传用例名 + 诊断行，提交者（单位机）复核。
- **本通告由单位机维护**：随下轮 push 新覆盖旧或删除。

### 187-a 协议变更详情（任务线实施计划落盘强制 + plans/025 建立，2026-09-15，单位机 ARM64）

> （本节原题 185-a——轮次号更正：该号已被深度机先占，本机认领无效 → 更正为 187-a；本机 D6 轮 = 188-a。）

- **变更内容**：①`AGENTS.md` 新增 **§8.6「任务线实施计划落盘（强制）」**——任何新任务线/开发轮，
  必须先在本机节落盘**六要素**详细实施计划（目标与验收判据／写集／实施步骤／验证链／边缘情况与诚实边界／
  与他机交叠分析）再开工；看板认领行须带**计划指针**；收工回填实测 + 整节替换为下一轮 + 预登记下一轮候选。
  ②`AGENTS.md` §8.2 开工行加「计划指针非空=认领前置条件」；§8.3 新增 `plans/025` 分区行 +
  `plans/021` 多机并行收尾纪律（最小局部编辑 + fetch/rebase）；§2.2 开工必读加读 `plans/025`。
  ③ 新建 `plans/025-三机任务统筹与实施计划.md`（三机在飞计划 + 预登记 + 交叠/遗漏审计 §四）。
- **影响面与复跑要求**：**源码零改动（文档/协议轮）→ 三机门禁无需复跑**；三机后续开发轮**开始时**
  须按新规在本机节落计划（家机/深度机当前在飞轮次可于下轮起执行；其当前计划已由 185-a 转写进
  `plans/025` §一/§二）。
- **审计结论（`plans/025` §四）**：三机在飞写集**零交叠**（家机 `ir_fields.cpp` / 深度机 `语义检查语句.cn` /
  单位机文档面），语义面互补；**遗漏项**=①ARM64 复验欠账（161-a~184-a，单位机 186-a 顺带收口）
  ②D6 未排期（单位机 186-a）③D3/D5 待排期 ④F1/F2 重评轮待认领 ⑤`plans/021` 并行收尾冲突面已入纪律。
- **本通告由单位机维护**：随下轮 push 新覆盖旧或删除。

### 163-a 契约变更详情（78/79 → v2 锚定链迁移，2026-09-14，家机 win-x64）

- **变更内容**：①**用例集**——删除 `tests/e2e/78_chain_build/`、`79_bootstrap_closed_loop/`
  （v1 组件链对象，OOM 既有败面）；新增 `78_v2_自举链构建/`、`79_v2_自举闭环/`（**无 主.cn/.expected**，
  配置驱动 `v2锚定链.txt`〔阶段=链构建|闭环〕，对象=v2 全树自身 `CN语言编译器v2/`）；
  ②**runner**（`run_e2e.py`）——删 `执行79闭环`（-190 行）+ 79 分派分支；新增 `解析v2锚定链配置`/
  `锚定链编译v2全树`/`执行v2锚定链`（+227 行）；内存保护用例前缀改 v2 名称；平台跳过清单删 v1
  78/79 项；预热面 `是v2闭环用例` 纳入 `v2锚定链.txt`；③**引用同步**：`CMakeLists.txt`（运行时 obj
  注释）/`README.md`/`plans/002`。
- **影响面与复跑要求**：三机**全量 E2E 复跑即覆盖**（无需单点复跑）。预期口径：win-x64
  **299 用例 299 过 / 0 败**（原 2 败清零）；linux-arm64 / linux-x86_64 预期 **297 过 / 0 败 / 2 跳**
  （跳=62_ffi / 69_memory_management；78_v2/79_v2 在 linux 侧为**真跑**）。
- **重点风险面（新增真跑面）**：`78_v2`/`79_v2` 的 **linux 分支（as/g++ + GNU ld -Map 归属自检）
  未在家机动态验证**（家机无 linux 工具链，纯静态对齐）——深度机/单位机全量复跑即**首次动态验证**；
  若 79_v2 失败请回传 `target/audit2/selfwork79/` 日志与 map 片段（自检失败会打印归属行）。
- **产物与口径**：产物落 `target/audit2/selfwork<78|79>/`（fix_p.asm/fix_s.asm/cn_self.exe 等）；
  win 口径 408105 行（**不跨后端比较**；arm64/X64L 重锚随跨机轮）。
- **本通告由家机维护**：随下轮 push 新覆盖旧或删除。

### 144-a 契约变更详情（关键字体系审计 + 联合体裁决落盘，2026-09-15，家机 win-x64）

- **变更内容**：`plans/001` §2.1 关键字清单同步为裁决后口径（**50 保留字**：删 `自动`/`实现`、
  `原始`/`多行` 改为上下文前缀〔非保留字〕、`保护`/`虚拟`/`重写`/`抽象`/`作为`/`模块` 六词上下文化〔2026-09-16 补正〕；补 `模块`/`作为`/`不安全`、删已删残留 `从`）；
  新增 `plans/024`（审计全案 + 实施设计 §七）；`plans/023` §五.5 更正（135-a 探针误判撤销——
  真因=字段名「原始」是字面量前缀关键字）+ §十二 联合体方案 A+D。
- **本提交影响面**：**源码零改动（纯文档轮）**——三机门禁**无需复跑**；Linux/ARM64 侧无动作。
- **实施轮（后续会话认领）影响面预告**：①删 `自动` → 迁移 `tests/e2e/16_function_extra/函数增强.cn`
  与 `tests/e2e/104_实例方法作值/主.cn`（`自动`→`变量` 同义替换）；②`89_关键字穷举` 用例串同步；
  ③`原始`/`多行` 非保留化（lexer 非前缀语境回退标识符）；④`scripts/check_keywords_sync.py` 门禁接入
  `ci.ps1`——实施轮 push 时须复跑**三平台全量 E2E + v2p 重建（若 v2 表变动）+ 锚定链/对拍**，并更新本通告。
- **联合体方案 A+D（`plans/023` §十二）**：语义层拒绝拥有型成员（+`手动释放` 标注逃生舱）+
  删 IR 无条件释放半截机制——实施轮须负测/正测/反证（v2 树无联合体用例 → 固定点预期不变，实测确认）。
