# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|---|
| 家机 win-x64 | 🏃 | 开发轮 D32：RA 宽度/槽混淆缺陷族根治（s266 定位→修复→机械校验）；计划=plans/025 §1.0b | c42a723 | 2026-09-16 20:55 |
| 单位机 ARM64 | ⬜ | - | - | - |
| 深度机 linux-x86_64 | 🧪 | E2E 测试轮：4adc7a5 全量 E2E+单测基线复验＋B7 波1 宿主-only 用例 v2 特性依赖盘点（产出喂家机实施）；结论→HANDOFF 深度机节 | 4adc7a5 | 2026-09-16 19:30 |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）
> - 🏃 **开发轮进行中**（**占锁；全表至多一机**——231-a 恢复唯一性；行=任务名+指针+基线+时间）
> - 🧪 **验证轮进行中**（探针检测/E2E 测试·自愿登记·不占锁·行格式同 🏃）
> - **开工**：`git fetch` → 改本机行（🏃 + 任务名 + 基线commit + 时间 + 指针）→ push 主远程 `gitcode`
>   ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> - **收工**：本机行**整行清空**（⬜ + 各列「-」），随本次提交一起 push——结论进 plans/025 收工回填，
>   **看板行不留结论也不留通告**（232-a 用户令收紧，原「可附一句结论指针」废止）。
> - **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.4）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在认领开发轮前提下进行，并在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 单位机 ARM64 通告段（256-a）

- **契约变更（2026-09-16·§8.4 广播）**：①**runner 新增「编译选项.txt」用例级旗标通道**（测试目录含该
  文件时，每行作为一个旗标追加到 `cn build` 命令行——如 `-O3`）——**无存量用例受影响**（无该文件=
  行为不变），新增用例 E2E 384 使用；②`cnsmith_diff.py` 跨平台化（原硬编码 win-x64→按架构探测·
  新增 `--target` 覆盖）+**矩阵 49→52 格**（+3 个 -O3 触达格）。**影响面**：他机无需复跑存量。
- **@家机 任务移交（2026-09-16 用户令）**：①**D31 根治（方案 C 已裁决）**：源头位宽截断+后端硬防线+
  **IR 位宽不变量机械检查**+负测锚定（详见 plans/021 §三-D D31 行）；②**采样轮协议实施（用户已批
  「按这个办」·排班 S1）**：`scripts/cnsmith_daemon` 自治循环+命中样本库（含抢救 D31 的 s269.cn）
  +三件套日志（**建成即 M3 计时启动**）——细则=plans/026 §2.9；**任一台空闲机可认领**（验证轮·免锁）。
  ③B6 分支策略建议**已经用户批准**（2026-09-16「同意」）→ 迁 plans/021 §3-A **A12**（里程碑标签锚点建立·待认领）；本通告段第③条随本 push 关闭。

（深度机无在飞通告。）