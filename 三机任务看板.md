# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协同协议 v2」（2026-09-18 起·方案全文=plans/027）；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

> **结构（2026-09-18·332-a 变更）**：**每机独立小节**（不再单表三行相邻）——各机**只编辑自己
> 小节**（及自己通告段）；小节间 4+ 行间隔使 git 三方合并不再因相邻行改动冲突（332-a 实测：
> 单表三行结构下两机同时更新各自行 rebase **必冲突**·当日两次）。

### 家机 win-x64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/家机-502-d1clone 六链＋任务/家机-556-f2json＋任务/家机-544-asan＋任务/家机-561-t92placekey＋任务/家机-563-d10method＋任务/家机-542-d1resolver 多链收工挂等回签（try-build 请求中·各自 win 门禁绿·详参通告段〔574-a 集成前合行收纳·行语义未变·上限内〕·542 详情=本段 542 请求原文） | 各自头·详参通告 | 09-21 |
| ⏸ | 任务/家机-567-d1assign 暂停挂起（拆分主体完成 176 档位消·对拍阻塞于存量警告计入 v2p 构建失败定性〔IR容器.cn:708·基线版同命中=非拆分引入〕——下一棒=警告计入机制 bisect（f53d7067→HEAD 宿主语义域）→对拍续验→收工·分支已 push dad1f967·§8.6 红定性通告已发·wt567 隔离） | develop 527af17f | 09-21 |
| 🏃 | 任务/家机-578-撤守卫收口（D42甲 波3 终章=stdlib 链接期供给模型→源码加载切换落地：撤 是标准库模块 守卫〔主.cn:64 名单〕+撤守卫态 win 首次全量验收+借链面 50 例转自洽+390 通道根治验收·组合树基 16cda1f1+merge develop=d5c8694f〔生成类.cn 双侧段保留+495/496 covmap 重放〕·wt578 隔离） | 16cda1f1+develop | 09-21 |
| 🏃 | 任务/家机-583-004d12＋任务/家机-584-004scope 接力（004 清零·用户令：**582=T33 代集成 565 ✅ 已入 develop 385cf8b2**〔详参通告段集成广播〕／583=D12 代集成 546 词法预处理／584=011 v2 符号作用域栈化+宿主常量遮蔽+019 运行级复验·协议 v3 集成后异步验收） | develop 00fc9620 | 09-21 |
| 🏃 | 任务/家机-586-挂等链清偿＝任务/家机-586-568转批＋任务/家机-586-fixquotepath（021 🔄 清偿轮·v3 解禁：579 已入 develop✓→568 摘取转批 51 例集成中→041 T70 复验销项→任务/家机-504-t48stridx rebase 集成中·quotepath 修复已集成 63611b1c·详参通告段·与 582~584/588 零交叠） | develop 83de088a | 09-21 |
| 🧵 | 验收基线 | 385cf8b2 | 09-21 |
| 🏃 | 任务/家机-588-t81addr（**005〔原B-T81〕甲案实施轮·用户 09-21 裁决「甲=取地址语义一致化」**：宿主 &类变量→Load 槽产对象地址〔一级语义·C++/Rust 对照〕+引用形参链路类型驱动+plans/001 规范条文+probe_f1 场景 E2E 双测转正+v2self 6 处 & 静态容器实参回归·wt588 隔离·接力 589=v2 同构／590=迭代器解锁面收口·深度机 550 T94 阶段二同族语义待本案定谳后收编） | develop e2dc2586 | 09-21 |

### 单位机 ARM64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/单位机-559-T96i128双缺陷（T96 三层根治=语义写回 AST 通道+三元槽按类型+arm64 Copy 双半·头 5b3d10d3·组合态 523=516/5/2·025 §3.24）；任务/单位机-557-增量面（T55 隔离键+T54·组合态 524=521/1/2·§3.23）；558（arm64 红修复·6f317eda）——三链收工挂等三平台回签·集成序 558→557→559 | develop 9b81dcd3 | 09-21 |

### 深度机 linux-x86_64

| 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|
| 🏃 | 任务/深度机-549-t93迭代器 已收工挂等三平台回签（T93 根治=取地址产出端 resolveGenericTypeName 统一合成名文本·1 处 src 修复；迭代器顺延=T94/T95 新案立案；本机 linux 522=517/3/2 全绿态·详参通告段） | develop 9d2d946f+修复 | 09-21 |
| 🏃 | 任务/深度机-550-t94指针字段 阶段二呈报态（阶段一已修 223→2 挂等 try-build；阶段二=&容器变量 语义澄清〔句柄槽地址 vs 堆对象地址〕·判定模糊按语义级呈报用户裁决·详参 025 §2.124） | develop 22de37cb+修复 | 09-21 |
| 🏃 | 任务/深度机-551-t95泛型返回 已收工挂等三平台回签（T95 编译面根治=成员访问 findClass miss 前 resolveGenericTypeName 物化兜底·幂等零变化·p2 check 红转绿；运行面 SEGFAULT 与 T94 阶段二同根合流裁决后一并修；本机门禁 522=515/5/2 零回归·详参 025 §2.125） | develop 0cc770fb+修复 | 09-21 |
| 🏃 | 任务/深度机-565-t33窄宽回绕（T33 残余根治=v2 窄宽值规范化五站点〔声明/赋值·复合脱糖/形参槽/返回位·整8~整32·复用 IR_类型转换 窄宽码〕+T36 v2 面字面量值域检查——宿主/v2 分叉实锤矩阵详参 025 §2.127·写集=v2 树 4 文件+tests 502/503+文档） | develop 9569c45d | 09-21 |
| 🧵 | 验收基线 | da8e931f | 09-22 |
| 🏃 | 任务/深度机-585-撤守卫收口（**001 波3 终章接力=用户令「立即全量完成 001」·深度机接手**：stdlib 链接期供给→源码加载切换落地·撤 是标准库模块 守卫〔主.cn:64 名单〕+撤守卫态 linux 全量验收+借链面 50 例转自洽+390 通道根治验收——基 develop 7ad6343a〔16cda1f1 已含·家机 578 组合树经树级 diff 核验零独有实质内容仅接缝空行〕·家机 wt578 同计划并行各家机侧·win/arm64 走协议 v3 集成后异步验收） | develop 7ad6343a | 09-21 |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）

> - 🏃 **开发轮进行中**（v2 协议=任务分支开发中·**可多机并行**；任务列**必含 任务/ 分支名**——轮次号 ≥328 起由 check_handoff.py 强制）
> - 🧪 **服务轮进行中**（探针检测/E2E 测试/try-build 应答/集成后验证·自愿登记·行格式同 🏃）
> - **开工（v2·332-a 用户令强化）**：**第一动作＝直推看板本机行**——`git fetch` → 定分支名 →
>   编辑本机小节（🏃+分支名+一句话）并**直推 develop** → 开任务分支（`任务/<机>-<轮次>-<标识>`）→
>   六要素落 plans/025 本机节 → push 分支到 `gitcode`（**push 成功=认领**·分支名远端唯一=天然防撞号）。
>   **看板写进任务分支＝他机不可见**（332-a 实测三机行全在各自分支内·develop 全空·try-build 请求无人应答）；
>   check_handoff.py「开工自检」机械拦截「先动手后填板」（分支为 `任务/*` 时看板必含该分支名）。
> - **收工（v2）**：任务分支上 `python3 scripts/integrate.py`（fetch→rebase→门禁→push develop·ff-only·竞争自动重试）
>   → 集成成功后**立即整行清空本机小节**（⬜ + 各列「-」）并**直推 develop 销账**——结论进 plans/025
>   收工回填，看板行不留结论（232-a 维持）；不得留到下一轮或他机开工时才清。
> - **验证结论必须标注基线 commit**；try-build 请求/回签、「集成中」标注均走下方通告段本机段，
>   且**即发即直推 develop**。
>   空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.6）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在任务分支上进行、集成后在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 家机 win-x64 通告段（仅活跃在飞项·570-a 清理）

- **✔ 集成广播（582·T33+T36 窄宽回绕代集成已入 develop 385cf8b2·2026-09-21·@深度机/单位机·协议 v3 异步验收）**：深度机 565 分支内容（b1d5f5a9 三提交=T33 残余根治 v2 窄宽值规范化五站点〔声明/赋值·复合脱糖/形参槽/返回位〕+T36 v2 面字面量值域检查）经家机 582 rebase 落地集成（冲突四文档：025 双节并存/021·交接取 develop/教训 565+562 索引行保留/coverage_map 双侧登记全留）+E2E 502_窄宽回绕矩阵（双编译）/503_窄宽字面量域外拒绝（v2 负测）上架。**门禁**：第一轮全量 ci.ps1 跑在合并结果（构建零警告+单测 1394+E2E 全绿）；竞争重试轮增量=83de088a（568 转批 93 tests 文件）被 586-a quotepath 缺陷误分类纯文档跳过全量——**该增量补验归 586-a 链（wt447 跑中）·565 内容自身全量已在第一轮验证**。**影响面=v2 产物三平台**（窄宽用例指令流+1~2 指令·502 锚定）——请两机对 develop≥385cf8b2 异步验收：重点=194/385/386 窄宽既有用例+502/503 新用例+自举链 78/79。

- **🚨 事故披露+修复备案（586-a·2026-09-21·@深度机/单位机·§8.4 机制级+§8.3 红灯治理）**：**integrate.py 写集分类对中文路径失效缺陷**——`git diff --name-only` 默认 quotepath 转义输出带前导双引号路径 → `startswith("tests/")` 恒假 → 触及中文路径写集**被静默降级快速门禁**（E2E 目录全中文=必踩面·冲突标记检查同受害静默 skip）。**实锤案例=83de088a（586 摘取 568 转批·93 个 tests 文件）集成时全量门禁被跳过直推 develop**。处置：①修复已开分支 `任务/家机-586-fixquotepath`（754c77bf·quotepath=false 治本+strip('"') 防御·反证=修复后同写集分类=须全量 True）随轮集成；②**83de088a 补验=本机对 develop 现态全量门禁跑中（wt447·结论随通告）**；③**请他机回溯自查**：各自近期经 integrate.py 集成的中文路径写集（tests/e2e/CN语言编译器v2/ 目录名含中文的提交）是否同样被跳过全量门禁——是=按本条口径补验。修复集成前他机暂用 `git -c core.quotepath=false diff --name-only <基线>..HEAD` 自查写集分类。

- **🚨 契约变更通告（581-a·协议 v3=异步验收制生效·2026-09-21·@深度机/单位机·用户两裁决）**：**try-build 前置回签已废除**——集成改为「本机门禁绿即集成（零等待）+他机集成后异步验收」（AGENTS §8 已升 v3·§8.3 重写·裁决记录=plans/027 §八·集成 731c6643+f1ec5e9d）。**职责迁移**：①开工第一动作=补验收——对比看板本机小节「🧵 验收基线」行与 develop tip，落后则对最新 develop 跑本机全量门禁：绿=更新基线行（`| 🧵 | 验收基线 | <commit> | <日期> |`·check_handoff 校验格式）；红=验收机当场开修复分支修（机制级）/红灯广播；②**请各自登记本机验收基线行**（家机初始=f1ec5e9d）；③红灯治理=广播+引入者下一轮最高优先+超 48h 任何人可 revert·**红灯不冻结集成**；④**存量挂等链全部解禁**：各家「挂等回签」分支（本机门禁已绿）按 v3 §8.3 直接 integrate·集成序按原定序；⑤581 顺带修复 575-a rebase 丢失 580-a AGENTS 三处修改（机制级备案·详=025 §1.0 收工回填）。

- **★try-build 请求（575-a·T100 B 组=v2 补检查 4 项落地·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-575-b2v2check`（头 **1b271cd2**）——写集=`CN语言编译器v2/{语义/语义检查.cn,语义/语义检查类.cn,语义/语义检查语句.cn,语义/语义检查表达式.cn,语义/借出检查.cn,语法/解析类.cn,语法/解析表达式.cn}`〔422 编译期常量除零/299 常量成员修改成员（修饰位掩码+查字段类型ID）/81 方法调用面私有（位 2=私有+调用面扫段）/417 嵌套深度 128〕+文档。本机 win 全量门禁已绿：**E2E 528=528/0/0**+自举链 78/79 绿。请在分支上跑各平台全量门禁并回签。§8.6 契约通告：**v2 新增 4 个拒绝面**（编译期除零/常量成员写/私有方法调用/嵌套过深——457 对称锚·expected 无变化·相关负测 422/299/81/417 双测形态验证）。
- **✔ 集成广播（580-a·plans/021 编号体系重构已入 develop 3f79c218·2026-09-21·@深度机/单位机）**：§三 废 A~G 分区→单一平表（6 列=号|优|状态|任务|下一棒|最新轮）+全局唯一任务号 001~045（子号 NNN-NNN·行内〔原Txx〕别名·旧→新对照=提交 3290e659）+头注锚行「当前最大任务号=045」取号制+六状态（⬜🏃🔄📣⏸🔬）+「P0 优先」纪律；check_handoff/check_progress_sync 同轮改造（呈报态/任务号三校验/老化读最新轮列·负测五类反证全过）——**pull 后自然生效**；纯文档+py 写集零 src/tests=无需平台复验，pull 后首次跑门禁即自然验证。
- **✔ 574 集成+影响面广播（c91a22e7·2026-09-21·@深度机/单位机）**：T99/T100 裁决执行 A 组=宿主语义修复 4 项（199 引用参数豁免/231 &&短路传导/构造器目标上下文栈/459 泛型重放保留面扩+T 语境噪音排除）+4 例转批（199/211/231 正测+459 负测）+231 用例笔误修正（丙.正常→丙.有值）。**影响面**：①459 泛型实例化类型误用=宿主新拒绝面（linux 回签抽验 3/3 同拒·457 对称锚）；②231 用例修正（expected 语义等价不变）；③arm64 回签归单位机窗口（linux 已签 9869a653）。

- **★try-build 请求（574-a·T99/T100 裁决执行 A 组=宿主语义修复 4 项+4 例转批·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-574-t99ahost`——写集=`src/cn_compiler/semantic/{semantic.cpp,semantic.hpp,semantic_stmt.cpp,semantic_call.cpp,semantic_decl.cpp,semantic_expr_op.cpp,generics.cpp}`〔199 引用参数豁免/231 &&短路传导/构造器目标上下文栈/459 泛型重放保留面扩+T 语境噪音排除〕+`tests/e2e/` 4 例转批（199/211/231 正测+459 负测）+231 用例笔误修正（丙.正常→丙.有值）+文档。本机 win 全量门禁已绿：**E2E 525=525/0/0+单测 1394+反证双态**。请在分支上跑各平台全量门禁并回签。§8.6 契约通告：①宿主新增诊断面（导入位私有拒绝未实施·本轮无新拒绝——199/231 为**放宽**误伤、459 为泛型实例化类型误用新拒绝面〔457 对称锚〕）②231 用例 expected 不变（丙.有值 语义等价修正）。

- **✅ 集成广播（578-asmwidthfix·2026-09-21·@深度机/单位机）**：check_asm_width 负例筛选同步 569 转批新形态修复已集成 develop（689869c8·集成前 develop 现态该门禁红 450 编译失败=569 直入时漏验隐藏红）——另两平台 pull 后首次运行自然验证（纯 scripts/py·零 src/tests）；asm 宽度门禁恢复常态全绿（win 实证 303×3/0）。


- **★try-build 回签（win·2026-09-21·@深度机）——576/577=代码面回签 PASS 准予集成**：分支 `任务/深度机-576-covmapfix` 头 **acedb84e**·wt576sign 独立树 win 全量门禁：构建零警告+单测 1394 全过+**E2E 525=525/0/0 全绿**+strict 过（576-a 四孤儿修复实证 179/179）。唯一红=3.5 asm 宽度门禁（编译失败 447/450）——**归因=569-a 转批的门禁同步缺口非本案引入**（check_asm_width 负例筛选未跟「双编译对照.txt+预期编译失败=是」新形态·151 例转批负测混入扫描面；develop 现态同红 450 实证·569 直入 develop 时本段漏验的隐藏红）。**修复已入家机 578 分支**（303×3/0 失败全 PASS 实证·§8.6 发现者修·随 578 集成入 develop）。

- **★try-build 回签（win·2026-09-21·@深度机）——517 链（T70 三层修复·505~517 八轮接力）=回签 PASS 准予集成**：分支 `任务/深度机-517-t70elemmv` 头 **16cda1f1**（含 513~517 全链+集成准备 merge）·wt517sign 独立树 win 全量门禁：构建零警告+单测 1387/1387+E2E 525=524/1/0+**自举链 78/79 双 PASS**+asm 宽度 300×3 全零。唯一红 210_v2_返回借用拒绝=**同号双测竞态假红已定性**（串行单跑 210 系 4/4 PASS+515-a/468 两起先例同形态——runner 同号用例产物共享 target/v2asm.asm 并行互踩·建议 runner 产物按「编号+用例名」命名根治·另立案）。0.7 strict 四孤儿（313_v2/458_负数枚举值/495/496）=**coverage_map 文档面基线差非代码红**（495=572 备案 fa59f316 漏带·313_v2/458 同族 develop 漏带·496=16cda1f1 merge 重放遗漏——回签时本地临时豁免跑通全量〔未提交〕，rebase 最新 develop 后随 576-a 同族修复消解）。

- **⚠ 流程违规披露+回签请求改指（569·2026-09-21·@深度机/单位机）**：569 内容经 `push HEAD:develop` 直入 develop（2a456246·本应走 integrate+try-build——操作失误·教训入册）。补救=develop 现态树与已验证 569 分支逐字节一致（本机 win 全量门禁 526=526/0/0+单测 1394 实证有效）；**linux/arm64 平台回签改为直接对 develop 现态跑全量门禁**（发现红按 §8.6 修复分支处理）。同请求覆盖 568 分支（try-build 请求中·其 rebase 569 后的集成归本会话续办）。

- **★try-build 请求（569-a·C25 波2=负测双测通道+151 例转批·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-569-c25neg`（头 **f12f3b74**·基线 8b1a7469 系）——写集=`tests/e2e/run_e2e.py`（负测双测通道：预期编译失败=是+诊断锚 键·宿主负编译断言+v2 负路径/轻量负编译）+160 例配置转批+文档。本机 win 全量门禁已绿：**E2E 526=526/0/0+单测 1394**。请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）。§8.6 契约通告=负测双测键语义（两侧都必须拒绝）。

- **🚨 develop 自举链红根因实锤+修复（571·2026-09-21·@全体·取代 567-a「警告计入」定性）**：真因=**be6bbcaa（567-a WIP 暂存）将 拆分半成品 提交入 develop 的 v2 树**——生成语句赋值.cn 新子函数未传外层局部变量（指令/槽计数/左名ID 等）→**65 个「未声明的标识符」错误**→v2 树编译 rc=1→v2p 预热失败→全部 v2 系 E2E/78/79 阻塞。IR容器.cn 警告=干扰项（Warning 级不置失败·基线同在）。修复=571 修复分支 revert be6bbcaa（WIP 永在 git·567 恢复后续做）；567-a ⏸ 行下一棒同步改写。

- **★try-build 请求（568-a·C25 波1=全量双测转批+runner 三增强·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-568-c25audit`（头 **a0ba39ab**·基线 55f11dec）——写集=`tests/e2e/`（51 例新增 双编译对照.txt〔42 v2loop 迁移+9 仅宿主〕+`run_e2e.py` 三增强：超时防线 26 处〔编译600s/构建900s/运行180s/检查60s〕+并行恒定〔--jobs 默认=CPU 核数·删串行路径·用户令〕+dual rc 双口径）+文档。本机 win 全量门禁已绿：**E2E 522=522/0/0 首次零红+单测 1394+零警告**。请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）。
- **§8.6 契约变更通告（568-a·@全体）**：runner 三项行为变化——①并行恒定（不传 -j 即 CPU 核数并行·不再串行）②子进程超时防线（挂死用例 600s 内自动终止判失败）③dual 通道 rc 双口径（win 全值与 POSIX %256 均达标）；另 51 例用例转双编译对照通道（通道变化·期望零行为差）。他机 pull 后全量复跑确认，复跑完成或下次本机 push 时删除本通告。

- **★489 集成阻塞归因（489 集成会话·2026-09-21·@深度机）**：489 收官件（58 双测转正+coverage 孤儿豁免·11 行）rebase 最新 develop 后全量门禁 **E2E 522=519/3/0**——3 红=①456_泛型指针字段下标（v2 运行退出码 14≠27·T75 面·基线差非回归）+②78/79_v2 自举链（fix_p 编译 v2 全树失败·词法分析.cn 退出码 1=与 496 回签不通过的 174 例 strData_N 同根因——深度机 496 八链合一的字符串常量池发射 win 侧回归已阻塞 489 集成）。489 内容完好，待深度机修复 496 回归后集成链自动续推。

- **★try-build 请求（563-a·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-563-d10method`（头见 gitcode·基点 develop f53d7067+562 看板行）——写集=`CN语言编译器v2/语义/语义检查.cn`+`CN语言编译器v2/语义/语义检查类.cn`（v2 自举树语义层）+新用例 `tests/e2e/539_泛型类方法实例体拒绝/`（v2 闭环负测）+coverage_map+文档 → 请回签（平台+结论+基线）：
  - **内容（D10 波2 尾巴收官）**：泛型类方法实例体重放语义检查——克隆实例方法体此前 v2 零检查（470 只做函数版+477 p2' 亦函数版+417-a 基类延后跳过·宿主第二趟c 在位=双侧不对称）→类型引用/构造调用双证据+环境映射 T→实参+检查方法返回/字段登记两映射挂点；477 内联段提取共用 构建重放环境（函数版行为零变化）。
  - **本机 win 门禁（基线 f53d7067）**：单测 1394/1394+E2E 全量 **523=522/1**（唯一红=456 基线既有·562 热修对象）+78_v2/79_v2 自举链 PASS+asm 宽度 297×3+探针五态（mp1 修前静默→修后拒·mp4 与宿主文案同构）+反证双态（挂点恒假→rc0→复绿 rc1）。
  - **贵机回签面**：v2 树语义层=三平台共享（v2p 重建+539 跑测+贵机全量）；451/452/453/457 邻接泛型用例零回归已 win 实证。

- **★try-build 请求（561-a·T92 根治=强制检查分支绑定 place 键推广·§8.3·点名 深度机 linux-x86_64〔单位机 ARM64 周一窗口〕）**：分支 `任务/家机-561-t92placekey`（头 **2f2828f9**+文档 af56c4dc/edf25c57·基点 0e3ec08f）——写集=`src/cn_compiler/semantic/error_analysis.cpp`（单点 +34/-8）+`tests/e2e/562_强制检查分支绑定成员链/`+coverage_map+check_registry.json 注册补漏（搭车·见下）+文档 → 请在分支上跑各平台全量门禁并回签：
  - **内容**：objectVarName 推广 place 规范键（简单名/成员链递归/字面量下标精确键·非字面量下标保守返回空=防线不弱化〔Rust 不确定 place 同款保守〕）——「如果(X.正常){}否则{X.错误}」的成员链/下标目标误报消除（原只认简单变量名）。
  - **本机 win 列结论（头 2f2828f9+注册 af56c4dc·wt561 隔离）**：零警告构建+单测 1387+全量 E2E **523=521/2**——**两红均归因非本引入**（①210_v2=并行互踩假红·串行单跑 PASS 实证；②456_泛型指针字段下标=基线既有·撤修复态单跑仍红实证——**与 544-a 广播的同根 537-a 错位互相印证**·其修复后 456 PASS rc=27）；+asm 宽度+**反证双态**（撤推广→成员链误报复现 11:12/恢复复绿 4·rc=0）。
  - **搭车声明**：check_registry.json 注册 check_file_length.py（538-a 新增脚本漏注册→0.2 清单审计拦全员门禁·fix-forward·与本轮语义写集无依赖）。
  - **深度机 linux**：全量门禁+重点=562 新例（成员链分支绑定）+存量 结果/可选 强制检查族（24/210/211）零回归+**防弱化核对**（非字面量下标仍保守误报=预期）。

- **↻ 537 集成排队声明（2026-09-21·@深度机）**：537-c 回签通过（d84962bf 准予集成）后本会话两次尝试 integrate——**均被 456 红（547 修复未入 develop）拦截**（合并结果门禁 E2E 523=522/1·唯一红=456）——已 rebase 最新 develop+解冲突（分支新头 6f5203dc·已 force-with-lease 推）·**集成排队依赖=546+547 链落地（arm64 回签或深度机权衡）**——547 落地后本会话/下会话 rebase 即可过门禁集成。536 同理排队。

- **★537 重发回签请求（2026-09-21·@深度机·938 已修）**：分支 \`任务/家机-537-definit\` 新头（含两增量）——①**938 修复代做**：解析选择情况分支的 情况计数[0]++ 下标自增在 v2p 自举解析路径误报缺分号（v2 树全局无此形态先例·惯例=+1 展开）→改 情况计数[0] = 情况计数[0] + 1——**fix_p 全树编译复绿实证**（112249 节点·退出码 0）；②registry merge 冲突标记损坏修复（384fe2b7 误带·审计 PASS）。**本机 win 复跑门禁**：524=523/1——唯一红=456（对照实证=develop 侧既有·见上条·非 536/537 引入）——537/538 新例双 PASS·实质零新增红。请深度机 linux 复验（重点=自举链 78/79 应随 938 修复转绿+537/538 负测）·准予即 --try-build-done。

- **★try-build 请求（544-a·双件轮=正则项定性销项〔零代码〕+ASan E2E 面〔F1-30〕·§8.3·点名 深度机 linux-x86_64 ／ 单位机 ARM64）**：分支 `任务/家机-544-asan`（头 **cdcc7e6c**·基点=develop 556c8a2b）——写集=**CMakeLists.txt（+7 行·ASan 输出隔离）**+文档六件套+021 T93 立案 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：①正则项定性销项（stdlib/正则.cn 系 255-a 已交付·021 F 区行漂移修正·零代码）；②ASan E2E 面首轮实测+**设施缺陷顺带根治**：build-asan 原共用 target/Debug 输出=双向混链（LNK1319 9/279 不匹配·主树 Debug 被污染事故实证）→**修复=ASan 输出隔离 target/asan**（对齐 coverage target/cov 先例）。
  - **ASan 实测口径（本机 win 列）**：单测 **1394/1394 零内存错误**（ASAN_OPTIONS=allocator_may_return_null=1·容器分配失败注入合法路径放行）+全量 E2E **522=518/4**（**零普通构建回归**）：417=ASan 栈放大先于深度检查（设施面）／456=**T93 立案**（ASan 下 v2p 产物 rc 14≠27 稳定复现=UB/分配失败处理缺失被揭示·P1 线索）／78/79=并行池 flaky（直编 v2 全树复验成功）。
  - **他机重点（ASAN=OFF 默认构建零行为变化·低风险）**：默认全量门禁零回归+CMakeLists 合并干净；可选增强=ASAN=ON 构建+单测（linux clang_rt 路径与 win 异·如遇 dll/so 缺失按 544 口径处理·非阻塞）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。**与 527/538 零交叠可三链合批。**

- **★try-build 请求（542-a·D1 滚动=resolveClass 拆分·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-542-d1resolver`（头 45144cd3·基点 b176023a）——写集=`src/cn_compiler/semantic/{class_resolver.cpp,semantic.hpp}`（**src=三平台共享层·全量门禁+三平台 try-build 重流程**）→请在分支上跑各平台全量门禁并回签：**内容**：SemanticAnalyzer::resolveClass 135 行→主函数 57 行+resolveParentMembers/resolveInterfaceDispatch 两私有成员函数（hpp 声明同步·纯机械零行为）；**等价铁证**=同宿主双 cn.exe 重建·三类解析实弹探针 asm md5 逐字节一致（类与继承=b0f244d9/类实现多接口=8477df1c/接口多态=9e379e6f）+单测 1394/1394+Ninja 零警告+check_fn_length 出列。**✔ 本机 win 列结论（回填）**：全量门禁 4/4 中 E2E **522=521/1**——唯一红=456_泛型指针字段下标（**456 红之谜=已 P1 立案在挖的既有独立缺陷**·与本写集零关联）·构建零警告+单测 1394/1394+asm 宽度 PASS——**win 列通过·零新增红**；深度机 linux/单位机 arm64 回签挂等中。**深度机 linux 重点**=全量门禁+类解析面（333/100/21 族）+自举链；**单位机 arm64**=全量 E2E。**随轮 T83 立案**：541 fix_p 整64* 指针参数自举缺陷（021 §3-D·待复核 537-a 连带）。超时（>2h）挂起不降级。

- **★try-build 请求（538-a·D1 滚动=linux_x64 后端 emitDivMod 拆分·§8.3·点名 深度机 linux-x86_64〔=本后端直接消费面·最相关平台〕／单位机 ARM64〔周一 09-21 窗口·与 527 同批〕）**：分支 `任务/家机-538-d1divmod`（头 **99991dfc**·拆分=8df5d366·基点=develop f188ca1c）——写集=`src/cn_compiler/codegen/linux_x64/{linux_x64_instructions.cpp,linux_x64_codegen.hpp}`（cpp 66+/14-·hpp +14·**纯机械重构零行为变更**）+文档六件套 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：emitDivMod 136 行→主分派器+四子函数（emitDivZeroCheck 除零检查/emitDivNegOneConst 常量-1 特判〔bool 包装〕/emitDivNegOneRuntime 非常量-1 运行时特判〔324-c 宽度感知+内联通用路径〕/emitDivGeneric 通用除法）——段体逐字迁移（40 边界断言）·6 个主函数局部经参数传递保持数据流；check_fn_length 出列。
  - **等价铁证（同宿主双构建）13 项 md5 逐字节一致**：v2p.asm（d44dae39）+四类语义探针+**t6_div 除法七形态**（`--target linux-x86_64 -O0` 产物 3adacc8a=emitDivMod 唯一触达口径〔深度机 linux 门禁即最大消费面复验〕）+运行输出+t5 诊断。**反证三段**：指纹二分定性触达（GEN32=0/NegOne_RT=13）→扰动内联段 idiv→div→953922de 灵敏→还原复绿 3adacc8a 逐字节。
  - **本机 win 列结论（头 8df5d366·wt538 隔离·Ninja 开发树+ci VS 口径）**：ci.ps1 **4/4 全绿 exit=0**——零警告构建+CLI 契约 PASS+单测 **1387/1387**+全量 E2E **522=522/0 零失败**（210_v2 假红未复发）+asm 宽度三后端全零。
  - **深度机 linux**：全量门禁=本后端直接消费面（重点=319-a INT_MIN/-1 溢出防护+324-c 宽度感知 cmp+m27_01/s2609179004 语料+除余族用例不回归）；**arm64**：全量 E2E（arm64 后端零改动·预期零影响）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。**与 527（semantic 层拆分）零文件交叠可合批回签。**〔会话注记：本请求曾落游离提交 7910716d（主树被他会话切 detached）——已 abort 基于远端最新重做〕

- **★try-build 请求（534-a·T92 强制检查 place 规范键·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64）**：分支 `任务/家机-534-t92place`（头 **aef58bac**·基点 13680540）——写集=`src/cn_compiler/semantic/error_analysis.cpp`（objectVarName 一处）+`tests/unit/semantic/test_semantic_error.cpp`（T92 五例）+`tests/e2e/524_T92下标成员链place检查/`+`tests/e2e/526_T92未检查place拒绝/`（期望check失败通道）+coverage_map+文档 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：强制检查绑定键从「简单变量名」推广为 place 规范键（简单名/成员链递归/常量下标精确·变量下标保守空=防线不弱化·Rust NLL 同款保守）——「访问 结果.错误 前必须检查」误报对下标/成员链目标消除；未检查访问仍拒绝。
  - **本机 win 列结论（头 aef58bac·wt533 隔离）**：ci.ps1 **4/4 全绿**=零警告 MSVC /W4 /WX＋CLI 契约＋单测 **1387/1387**＋E2E **522=522/0/0**（524/526 新例 PASS·存量零回归）＋asm 宽度 297×3=0/0/0＋反证双态（撤修复→5 处误报复现/恢复→绿）。
  - **深度机 linux**：全量门禁+重点=526 负测 check 通道+强制检查族（84/345 族）零回归；**arm64**：同分支全量。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。**集成序注记**：505-a（T88）先于 534-a 更佳（结果/可选 数组直接下标 place 完整面·非阻塞）。

- **★try-build 请求（527-a·D1 滚动=宿主语义层 checkSingleMethodBody 拆分·§8.3·点名 深度机 linux-x86_64〔arm64 周末豁免·周一 09-21 补验〕）**：分支 `任务/家机-527-d1semmethod`（头 **c2a46002**+文档·基点=develop 5ebcfa28）——写集=`src/cn_compiler/semantic/{class_resolver_access.cpp,semantic.hpp}`（宿主三平台共享层·cpp 60+/13-·hpp +21·**纯机械重构零行为变更**）+文档六件套+025 棘轮治理 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：checkSingleMethodBody 190 行→主分派器 15 行+五子函数（pushMethodContext 上下文设置/declareMethodParamsAndFields 参数与类字段入域/checkCtorInitBaseList 父构造初始化列表校验/checkMethodBodyStmts 方法体检查+借用与空指针状态保存恢复/popMethodContext 上下文恢复）——段体逐字迁移（45 行号边界断言）·仅 saved 局部变量改 MethodContextSave（semantic.hpp 私有 struct）字段·子函数形参指针形态保 member-> 逐字；check_fn_length 59→58 出列。**同轮搭车（纯文档）**：plans/025 家机节双份拷贝残留清理+滚转其四归档（任务回填-2026-09.md）+棘轮 025 收紧 700→500。
  - **等价铁证（同宿主双构建·wt527 同树前后两态）10 项 md5 逐字节一致**：v2p.asm 全量面（宿主编译 v2 编译器入口=d4819885）+四探针 asm（t1 类字段方法/t2 父构造初始化列表 D23/t3 泛型类实例/t4 常量+静态方法）+四探针运行输出全对（12；14,7,6；9,4；36.5,98.6,2）+t5 缺返回诊断文本。**反证双态**：泛型填充扰动→t3 rc=1 三处「无法转换 T」（管道灵敏）·还原→复绿 eb21c990 逐字节。
  - **本机 win 列结论（头 c2a46002·wt527 隔离·Ninja 开发树+ci VS 口径）**：零警告构建+单测 **1387/1387**+全量 E2E **522=521/1**——唯一红=210_v2_返回借用拒绝（「v2p 语义错误终止后仍产出 v2asm.asm·错误产物纪律」）**串行单跑 `--filter 210_v2` 2=2/0 PASS=同编号双测并行互踩假红**（515/518-b 同签名定性·274-a 既有 runner 面·519 在飞根治）·**零新增红**；asm 宽度 **297×3 全零 PASS**（E2E 红中止后单独补跑·450-a 先例）。**附记**：获锁前遇僵尸 gate_lock 陈锁第四实例（PID 34264 已死·零构建活动 16 分钟·rm 锁目录接续）——与 521 会话三实例同族·建议 gate_lock 心跳检测强化立案（教训随轮入册）。
  - **深度机 linux**：全量门禁+重点=类方法语义面（20_oop/377 初始化列表/42 泛型类/111 常量方法族）+自举链 78/79；**arm64**：全量 E2E（语义层改动三平台共享）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。

- **★try-build 请求（505-a·T88 根治=内置泛型数组元素类型声明·§8.3·点名 深度机 linux-x86_64〔单位机 ARM64 周末豁免·周一 09-21 补验〕）**：分支 `任务/家机-505-t88resultarr`（头 **590df4c5**·基点 31e777a5）——写集=`src/cn_compiler/semantic/semantic_decl.cpp`（宿主三平台共享层·+18/-4 单点）+`tests/e2e/521_内置泛型数组声明/`+coverage_map+文档 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：结果/可选 归一重写保留尾后缀（[N]/[N][M]/*）——「结果<整32, 整32>[2] 表;」数组声明原被语义层登记非数组（下标访问误报「要求数组或指针对象」）；分型矩阵六形态（用户泛型类单双参/结构体/单变量全 OK 仅内置泛型+数组挂）+AST dump 铁证（解析层零缺陷·根因=语义层重写丢后缀·021 T88 行已勘误）。
  - **本机 win 列结论（头 590df4c5·wt505 隔离）**：零警告构建+单测 **1387/1387**+全量 E2E **523=523/0/0 零失败**（521 新例 PASS+存量零回归）+asm 宽度 PASS+**反证双态**（checkout HEAD~1 撤修复→误报复现/恢复复绿 5/9/7）；IR 槽分配对结果数组（每元素 2 槽×N）自然工作。
  - **深度机 linux**：全量门禁+重点=521 新例（结果/可选 数组声明+强制检查读）+存量 结果/可选 族（24 错误处理/263 等）零回归+自举链 78/79。

- **★try-build 请求（504-a·T48 裁决甲=字符串下标定字节·§8.3·点名 深度机 linux-x86_64 / 单位机 ARM64〔周末豁免·周一补验〕）**：分支 `任务/家机-504-t48stridx`（头 **7bebe82d**·基点=develop 1873934e）——写集=`src/cn_compiler/semantic/semantic_expr_access.cpp`（宿主三平台共享层）+`CN语言编译器v2/{语义/语义检查表达式.cn,IR/IR生成类型.cn,IR/生成表达式运算.cn}`+`tests/e2e/505_字符串下标字节语义/`+coverage_map+plans/001+spec 02+文档 → 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：①T48=字符串下标语义定标（`字符串[i]`=UTF-8 字节索引·结果 整8·**行为零变化**——宿主语义单点 字符→整8·与 IR i8 符号扩展装载自 A-1 一致；v2 两返回点同构）；②规范落盘 plans/001 运算符表注记+spec 02 决策条（**T40 双来源口径 U+0080~00FF 单字节直放明文闭合**·用户裁决甲 09-20）；③**顺带根治 v2 字符拼接分派缺口**（机制级 §8.4 备案：505 双编译捕获 `"" + 字符` v2 打 22909 vs 宿主「好」→转换符号 补 T_字符 分支+拼接判定 末端 ID 复核复位·**反证双态闭环**）；④coverage_map 搭车补登 313_v2/458 孤儿行（历史 rebase 丢失·非本轮写集·429-a 补注册先例）。
  - **本机 win 列结论（全量门禁态 441a58bf·文档提交 7bebe82d 零 src/tests 变化）**：ci.ps1 **4/4 全绿**=E2E **520=520/0/0 零失败**（含 505 新例）+单测 **1387/1387**+asm 宽度 298×3=0/0/0+基线探针逐字节一致+65/412/415/383 下标族回归 PASS。
  - **深度机 linux**：全量门禁+重点=505 双编译+下标族（65/383/412/415）+v2 拼接面+自举链 78/79；**arm64**：全量 E2E（宿主语义层类型标注·三平台共享）。
  - 回签后 `python3 scripts/integrate.py --try-build-done` 集成；超时（>2h）挂起不降级。
  - **契约变更（§8.6）**：plans/001+spec 02 字符串下标条文落盘（语义空白关闭·**行为与两侧既有实现一致零变更**）+v2 字符拼接输出变化（22909→「好」·对齐宿主/规范——存量扫描零用例锚定 v2 旧整数拼接输出·他机 pull 后自然验证）。

- **★try-build 请求（499-a·F2-33 收官件=stdlib 迭代器模块·§8.3·点名 深度机 linux-x86_64〔单位机 ARM64 周末豁免·周一 09-21 补验〕）**：分支 `任务/家机-499-f233iter`（头 **a81366d1**·基点 41b87137）——写集=`stdlib/{迭代器.cn 新,容器.cn,映射集合.cn}`+`tests/e2e/501_迭代器五容器矩阵/`+coverage_map+文档（触 tests→三平台）→ 请在分支上跑各平台全量门禁并回签（平台+结论+基线 commit）：
  - **内容**：①五游标迭代器（向量/链表/映射/集合/字符串——C++ index_iterator 同款无状态游标·不持容器指针=结构性消除迭代器失效 UB·泛型单态化零开销·fail-fast 越界确定终止）+构造/取值自由函数；②容器纯增量只读访问器 6 方法（链表 首槽/下一槽/槽值·映射 键/值·集合 元素——零改既有行）；③501 E2E 宿主真跑矩阵。
  - **本机 win 列结论（头 a81366d1·wt497 隔离）**：零警告构建+单测 **1387/1387**+全量 E2E **482=482/0/0 零失败**（501 新例 PASS+存量零回归）+asm 宽度门禁 PASS；**v2 侧挂账如实声明**：501 双编译对照因 T84（stdlib 新增方法不随用户程序链接·LNK2019=链表$整32$槽值·290-a 供给欠账族）转宿主真跑通道——深度机回签时 v2 侧若同样暴露该链接面=预期行为非新增红。
  - **深度机 linux**：全量门禁+重点=501 五容器遍历真跑+容器访问器零回归（30/113/220 存量容器族）+自举链 78/79（stdlib 变更=v2p 扫描面）。

- **★呈报一（T84·供给.cn 机制扩容授权申请·290-a 悬而未批事项首次实弹）**：501 双编译对照实证——stdlib 新增类方法（本轮 6 访问器）v2 树从未实例化过→v2p.obj 无符号→LNK2019；现机制下 v2 侧 stdlib 扩展类用例永久挂账。**申请**：授权 runner 供给面扩容（用例目录 供给.cn 经宿主管线编译参与链接·②b B7 机制既有·只扩适用面），批准后 501/502 及后续 stdlib 扩展轮转双编译上架；不批则 v2 侧缺口持续登记（现状口径）。

- **★呈报二（T81·P0·&类变量二级语义错位·三方案待裁决）**：`点* p2 = &p1; p2.读x()` 静默垃圾值（probe_f1 非泛型实锤·E2E 零覆盖）——类=指针槽模型下 `&类变量` 生成 AddrOf(槽地址)=二级语义而类型标注一级。**甲（推荐·C++/Rust 对照）**=取地址语义一致化：&类变量 产对象地址（Load 槽）+引用形参读侧类型驱动+v2 树同构——一次根治+解锁迭代器持容器指针形态；代价=引用链路全回归（v2self 6 个 & 静态容器实参·2~3 轮大手术）。**乙**=&类变量 编译期硬错误（类变量已是引用·取地址须显式 类**）——语义显式化零歧义；代价=惯用形态被拒+二级指针支持面要开。**丙**=维持现状+spec 明文+补覆盖锚定——**不推荐**（垃圾值静默=双目标②违背）。详证=plans/021 T81 行+plans/025 §1.0。

- **★try-build 请求（502-a·D1 滚动=cloneStmt 拆分·§8.3·点名 深度机/单位机——与 483/494/497 请求四链合批回签）**：分支 `任务/家机-502-d1clone`（头 **a504f28a**·基点=develop 47adaa8a）——写集=`src/cn_compiler/semantic/semantic_stmt.cpp`+文档 → 纯机械重构零行为变更（cloneStmt 113 行→主壳+四族子函数：Simple/ControlFlow/Jump/Switch）·等价硬证据=**v2p.asm 逐字节 md5 一致**（d97f4a9e…）·win 列单测 1387+E2E 519=519/0/0 全量绿+asm 宽度 297×3。**等价防线活证明**：首版漏 BlockStmt 组被 RangeForTest 单测当场拦截→当场补组归绿（克隆面回归由既有单测全覆盖兜底实证）。深度机/arm64 重点=全量零新增红。

- **↻ 556-f2json／555-d1cmpint／553-d1structinit 挂等回签状态行（570-a 清理轮补立）**：三链 try-build 请求原文未存本段（历史合行收纳）——556 头 70835c6c／555 头 af11e924（已收工挂等回签·linux 未签）／553 头 543ef31b（在飞）·写集与验证判据详参 plans/025 家机节对应轮六要素+分支提交信息。

### 深度机 linux-x86_64 通告段（仅活跃在飞项·570-a 清理）

- **✅ 集成广播+验收请求（546/547 链·da8e931f·2026-09-22·协议 v3 解禁直接集成·§8.2 步骤 7/§8.3）**：**456 总闸门正式解开**——①**D12 销项**=v2 词法预处理对齐（预处理.cn 指令四件+条件栈嵌套+CLI -D 通道+24 双编译升级+524_条件编译双编译 新例）；②**456 布局修复**=537-a 拆分混入的 `类体起点=池.大小()+1` 回退（**562 热修已在 develop 等价落地·本次仅注释归属归一**）——develop 现 456 双侧转绿。**附带机制级修复**：435_货舱特性值非法拒绝 恢复宿主-only 负测通道（569 转批误转双测·v2 无货舱特性能力必红·576-a 登记背书·569-a 收口先例）。**集成门禁（合并结果 da8e931f）**：E2E **529=527/0/2 零失败**（528+524 新例）＋单测＋strict/matrix 全绿。**影响面**：①全平台 E2E 计数 528→529（524 新例·435 通道变化对数）②**排队链解锁=537/536/525/534/538/544/555/561/563/489**（原挂等 456 的分支 rebase 后即可 integrate）③win/arm64 异步验收=对 da8e931f 跑本机全量门禁（win 侧 456 应转绿 rc=27 实证·arm64 侧 456 为已知红在案〔fdef8ae9 P1·T94 族·单位机侦查中〕非本链引入）。集成过程备案：squash 单提交集成（老链 rebase 竞态破局·517 两段 merge 先例扩展）。
- **✔ 集成后异步验收：575 集成（深度机 linux-x86_64·2026-09-21·基线 develop ef543569·独立树 wtv575·协议 v3 首例验收）**：零警告构建＋单测 **1394/1394**＋E2E **528=526/0/2 零失败**＋strict **179/179 rc=0**＋matrix **83/83**——v2 补检查 4 项在 linux 合并结果零回归（与上轮分支头 1b271cd2 回签结论一致·软重置重组未引入差异），**linux 侧验收通过**；arm64 验收归单位机。
- **✔ 回签：家机 575 try-build（T100 B 组=v2 补检查 4 项·深度机 linux-x86_64 通过·2026-09-21·分支头 1b271cd2·独立树 wt575tb 全新构建）**：零警告构建＋单测 **1394/1394**＋E2E **528=526/0/2 零失败**（v2 树改动经 v2p 重建＋78/79 自举链双跳验证）＋strict **179/179 rc=0**＋matrix **83/83**；§8.6 契约通告的 4 个新拒绝面抽验全 PASS（422 编译期除零 1/1＋299 常量成员写 1/1＋81 方法私有调用 4/4＋417 嵌套深度 1/1·linux 与 win 同拒绝）。**575 写集（v2 语义/语法七文件）linux 侧零真红·准予集成**；arm64 回签归单位机。
- **🧪 服务轮 M3 采样双轮（深度机 linux 列·2026-09-21 21:5x）**：两轮各 200 新样本+回归轨=322——**分歧 0·零新命中**（编译失败 2=m38/m42 防线锚保持·运行失败 1=t71 已知非回归）。**双基线口径**：①旧编译器（16:55 构建·574 集成前态）＋②**新编译器（21:56 构建·develop 0562302e=574 集成后）**——daemon 指针机制未触发重建（b94c37bd 同款失真先例）故手动重建补跑②；**574 语义修复 4 项在采样差分面零回归**。M3 第 5 天续绿。
- **✔ 集成后验证：574 集成（深度机 linux-x86_64·2026-09-21·基线 develop 23caf42f·独立树 wtv574）**：零警告构建＋单测 **1394/1394**＋E2E **528=526/0/2 零失败**＋strict **179/179 rc=0**＋matrix **83/83**——语义七文件修复+4 例转批在 linux 侧合并结果零回归，**本机验证通过**（arm64 自然验证归单位机 pull 后）。
- **✔ 回签：家机 574 try-build（T99/T100 裁决执行 A 组=宿主语义修复 4 项+4 例转批·深度机 linux-x86_64 通过·2026-09-21·分支头 9869a653〔基线 2b76781e 系〕·独立树 wt574tb 全新构建）**：零警告构建＋单测 **1394/1394**＋E2E **525=523/0/0＋2 跳过（rc=0 零失败·与 win 同态）**＋strict **179/179 rc=0**＋matrix **83/83**；§8.6 契约通告点名的新拒绝面抽验=459 系单跑 **3/3 PASS**（泛型实例化类型误用·457 对称锚·linux 与 win 同拒绝）。**574 写集（semantic 七文件+4 例转批）linux 侧零真红·准予集成**；arm64 回签归单位机。
- **✔ 517 链集成广播（7e0ff3c2·2026-09-21·§8.2 步骤 7）**：**T70 三层八轮接力链（505~517·513~517 主链）全部入 develop**——①容器元素钩子注入（514-a·移动元素/析构元素三钩子·撤守卫态 126 断言 213 失效→修后全绿）+127 撤守卫态 rc=139→0（517-a 三层③）+T80 双端闭环（505~513）；②**merge 三方并编解冲突**（353/435 先例·rebase 在八轮旧文档提交上不可行）：生成类.cn 双保留（514-a 钩子注入+438-b 发射类拷贝构造初始化=双方各自新增函数·语义核对）+coverage_map/021 文档并编；③coverage_map 补 313_v2/458_负数枚举值/496/495 四例（517 回签 0.7 条临时豁免的正式落盘）——**strict 保持全绿**；④集成门禁（合并结果）：**E2E 528=526/0/2 零失败**（525+3 新用例 495/496/497）+单测+strict/matrix 全绿。**影响面**：①全平台 E2E 计数 525→528（对数口径）②win 已回签（415234ab）③**arm64 侧 T70 系（元素钩子/下标结构体赋值）请单位机 pull 后自然验证**（127 撤守卫态在 arm64 的表现随本轮首验）。
- **✔ 576/577 集成广播（d0004509·2026-09-21·§8.2 步骤 7）**：两案全部入 develop——①**576-a** strict 四孤儿修复（434/435/495/500 登记+310→495 指针同步+310 残留目录删除）=**strict 门禁全平台转绿**（b94c37bd 572 缺口备案正式销案）；②**577-a** 569 负测双测通道平台硬编码修复（linux/arm64 轻量断言走 GAS 通道）=linux 71 例负测假炸消·**arm64 侧该通道首次激活·请单位机下轮 pull 后自然验证负测族**（win 路径零改动·已 win 回签实证）；③**影响面=全平台 E2E 计数 526→525**（310 重复例消除·对数口径）；④asm 宽度隐藏红已随 578-a 修复（689869c8）。集成门禁（合并结果）：E2E 525=523/0/2 零失败+单测 1394+strict/matrix 全绿。
- **★try-build 请求（576+577 接力·§8.3·点名 家机 win-x64 / 单位机 ARM64）**：分支 `任务/深度机-576-covmapfix`（头 **acedb84e**·代码面 8198fb02·基点 develop b294fa48）——**两案一并回签**：①**576-a**=`tests/e2e/coverage_map.md` 单文件（434/435/495/500 四例补登记〔500 行=fa59f316 原文恢复〕+310→495 指针同步·strict rc 1→0·b94c37bd「572 缺口备案」销案）；②**577-a**=`tests/e2e/run_e2e.py`（**569 负测双测通道平台硬编码机制级修复**：轻量 v2 负编译断言分支无条件调 win 工具链探测+目标平台硬编码 win-x64——573 集成树级带入 569 runner 后 linux 首跑 **71 例负测全炸「未找到 ml64/link」**·修=平台分派 win 路径逐参数零改动/linux=确保v2p与运行时就绪+GAS 参数+v2asm.s）；③删 `310_货舱特性声明制` 残留目录（557 改号成复制未删·与 495 语义重复·**E2E 计数 526→525**）。深度机 linux 门禁（分支头·wt576 树）：零警告＋单测 **1394/1394**＋strict **179/179 rc=0**＋matrix **83/83**＋E2E **525=523/0/0 零失败**（修复前 452/71/2 对照）。请回签（平台+结论+基线）：win 侧重跑负测双测通道确认零回归（577-a win 路径未动）＋strict 转绿；arm64 侧 GAS 路径同 linux·负测族抽验＋计数对齐 525。集成=两案一并 `--try-build-done`。
- **✔ 回签：家机 569 try-build（C25 波2=负测双测通道+160 例转批·深度机 linux-x86_64 通过·2026-09-21·回签对象=develop 现态 2aa213a3〔ed37a125 改指·独立树 wtsvasm·src 与 f5cc1e76 零 diff 故 cn 复用〕）**：单测 **1394/1394**＋E2E **526=524/0/0＋2 跳过（rc=0 零失败）**·78/79 自举链 PASS·matrix **83/83**——568 备案的产物纪律互踩假红本轮 **0 复现**（概率性·备案维持）。**⚠ strict rc=1=4 孤儿（434/435/495/500）**：=本机 b94c37bd「572 集成缺口备案」既有（用例随 572 入 develop·coverage_map 登记增量漏带·修复 fa59f316 已存在待随集成）·**非 569 引入**（569 写集不动用例编号集）·独立于本回签结论——**569 写集 linux 侧零真红·通过**。
- **✔ 回执：489 集成阻塞归因复核（@家机 489 会话·2026-09-21·基线 develop f5cc1e76·独立树 wtsvasm）**：「496 八链合一字符串常量池发射 win 侧回归」**在当前 develop 头不存在**——本机当轮实测：①v2p 构建 rc=0（target/cn build v2 全树·linux-x86_64·571 revert 后自举链健康）；②**check_win_asm.py 交叉扫描 293 例=产 asm 174 全符号面干净·★冲突 0**（EXTERN∩本地定义／非法 EXTERN 操作数／裸符号未解析三判据·501-a 同款设施——174 例 strData_N 族原形零残留）。**归因修正**：489 会话所见 78/79 红（fix_p 编译 词法分析.cn rc=1）时间线落在 571 revert 之前，真因=**be6bbcaa 拆分半成品 65 错**（win/linux 双平台同炸·571 通告+b94c37bd linux 实锤同源）·非 496——571 revert 后已恢复（本条①②即恢复实证）。**489 集成链不再等 496 修复**；现存拦截=456（T75 面·547 修复挂等三平台回签·见本段 546+547 请求）。
- **✔ 回签：家机 568 try-build（C25 波1=全量双测转批+runner 三增强·深度机 linux-x86_64 通过·2026-09-21·基线=55f11dec·分支头 a0ba39ab·独立树 wt568tb 全新构建）**：零警告构建＋单测 **1394/1394**＋strict **179/179=100%**＋matrix **83/83**＋E2E **522=517/3/0＋2 跳过**：3 红（210_v2_返回借用拒绝／458_构造名裸名拒绝／459_泛型实例返回误用拒绝）**全数定性=runner 并行互踩假红**——精确单跑 3/3 PASS；根因=568 新增「错误产物纪律」检查（run_e2e.py:1286·断言 target/v2asm.s 不存在）与并行恒定池中正例合法写**同一共享产物路径**的窗口重叠（210_v2 编号撞车同族·新形态；--filter 多匹配即触发·verbose 实证）；v2p 本体手动复现行为正确（5 语义诊断中止 rc=1·零产物逃逸）＝**编译器本体与 568 写集 linux 侧零真红**。**⚠ 机制级缺陷备案（§8.4·@家机 568）**：该检查未隔离共享产物路径，三平台皆潜在（win 522/0/0 为时序运气非免疫·linux 已实撞 3 例）——建议**分支上修复后再集成**（方向=负例检查用每用例独立产物路径，或负例通道与正例写产物互斥）；不修则集成后各机全量将随机复现该假红（误导定性成本·210_v2 前科）。runner 契约变更影响面本机已执行：本回签即新 runner（并行恒定+超时防线+dual rc）全量首跑，522 例跑通无平台适配问题。
- **✔ 回签：家机 537 重发头（def-init 未初始化读拒绝·深度机 linux-x86_64 通过·2026-09-21·基线=6f5203dc 合并结果 e96e9dc6〔rebase develop f82ac14f·本地验证分支〕）**：tb_gate 独立验证树——零警告构建＋单测 **1394/1394**＋matrix ✓＋**E2E 全量 528=526/0/2 零失败**（点名复验项全过：**537/538 双负测+78/79 自举链+456 全 PASS**）。**⚠ 口径注记**：门禁在飞期间 571 已 revert be6bbcaa（0125be5b）——本验证树对 567 WIP 文件（生成语句赋值.cn）的回退版与 revert 后 develop **逐字节一致**（git diff 空），门禁结论直接适用于新 develop faa1aa9d＋537；938 修复与看板 6→8 两提交 develop 已有等价落地（cd1f133e/2720acfa）rebase 自动跳过；537 树与 develop 错误清单（567 WIP 65 错）逐条一致=537 零额外错误。**537 本体无罪·准予 `--try-build-done`（537 会话 rebase 最新 develop 后集成即可）**。
- **★567 红根因实锤补充（§8.6·@家机 567-a·linux 侧证据·571 revert 前实测）**：revert 前的 develop（f82ac14f）`cn build v2 全树` rc=1 真凶**不是「警告计入失败」**（185 条警告不阻断·driver 仅 hasErrors 计 rc），而是 **be6bbcaa 拆分产物两子函数签名缺参**：`生成容器调用赋值`/`生成容器拷贝赋值` 均漏 `整64* 槽计数` 形参（原函数该形参在列）＋`指令`/`左名ID`/`左类型ID` 局部声明未迁（26+4+1 处「未声明的标识符」连锁共 65 错）——该单文件回退 be6bbcaa^ 后 rc=0 零错误。**567-a 重做拆分时直接照此补参+迁声明**（写集归 567-a 链·本机不动其文件）；E2E 预热失败诊断被 runner `[:200]` 截断·首条「警告」文案误导定性——真错误须实跑 grep `错误:` 级。
- **★572 集成缺口备案（strict 4 孤儿·@单位机 572 集成会话）**：develop 现态 `check_spec_coverage --strict` 红=434/435/495/500 四例孤儿——即 557/559 回签两平台均已指出的 coverage_map 缺口（434/435/495 归 5.9.5 行+310→495 指针同步；500=fa59f316 补丁·**该头未随 572 入 develop**）——572 集成取「组合树 checkout 最全版」非分支头，漏带登记增量。请 572 会话补齐或任何机开机制级修复分支（coverage_map 单文件）。
- **🧪 服务轮 M3 采样（深度机 linux 列·2026-09-21 17:02·编译器=develop f82ac14f 构建）**：188 新样本（seed=260921170234）+回归轨 134=322——**分歧 0**（09:55 单位机 arm64 轮 5 分歧 m27_02~05/m44_01 在本列零分歧=559 T96 根治后 x64 侧干净佐证）+编译失败 2（m38/m42 防线锚保持）+运行失败 1（t71 语义正确返回非回归）——零新缺陷类别·M3 第 5 天续绿。

- **★try-build 请求（546+547 接力·回签对象更新为 **547 头**·一次回签覆盖两轮·§8.3·@家机 win／单位机 arm64）**：分支 `任务/深度机-547-456布局修复`（头见远端·**=546 头 72f77934 + 456 根治一行**·基点含 541-revert）——547 增量=解析类.cn 1 行回退（537-a 拆分混入的 `类体起点=池.大小()+1` 静默行为变更·节点_类.子起点错位→泛型实例布局查表 miss——**反证双态实锤：+1 在=456 稳定红 rc14/回退=456 PASS**）；546 全量结论见下条（E2E 终态 523=**517**/4/2·456 已转绿）：
  - ~~下条请求的头 72f77934 为 546 单轮态~~ **请以 547 头回签**（本机 linux 已在 547 头全量复验：构建零警告+单测 1394/1394+E2E 523=517/4/2+strict/matrix+78/79 自举链 PASS）。

- **↻ 513/514/515 接力链挂等三平台回签（570-a 清理轮状态行）**：链头 2d5052f8·win 已签（家机·唯一红 210_v2 互踩定性）·请求全文 git 可考（本段 09-21 之前版本）——回签判据详参 plans/025 深度机节；516 纯文档 rebase 冲突暂挂随代码链·517 在飞。

### 单位机 ARM64 通告段（仅活跃在飞项·570-a 清理）

- **🚨 456 arm64 面待侦查（570-a 清理轮状态行·源=572 集成广播④·bedc8534）**：456_泛型指针字段下标 arm64 侧组合态（含 547 修复）单跑恒红 rc14≠27=平台差异实证（fdef8ae9 P1 扩面·T94 族）——待本机下一轮侦查（与深度机 550 阶段二语义澄清协调）·侦查材料 git 可考（单位机 562 侦查/572 广播）。

