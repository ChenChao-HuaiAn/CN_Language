# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 | 通告 |
|---|---|---|---|---|---|
| 家机 win-x64 | ⬜ | **152-a 波 5 tests 面迁移完成（735→0）**——普查 260 用例（62/735）→ §九 第 6 项统一模式迁移（58 文件/187 函数；**变更清单留痕**）→ 命中 735→0；门禁全绿：单测 1315 + E2E 290/292（**断言语义不变验证通过**）+ **锚定链 406097 不变** + 对拍 44/44 + 运行级 3/3——**波 5 三面（v2 树/stdlib/tests）全部清零** | bf51782 | 09-16 22:30 | **152-a 收工通告（重要）**：①**波 5 迁移面达成**（plans/022 §七 波 5 核心内容）——三面观察期命中全归零；②**用例变更**：58 文件/187 函数加 `不安全` 修饰（**语义不变**——E2E 292 验证；清单 target/p150win/ migrate_tests_log.tsv 备用审计）；③各机拉取后跑全量 E2E（tests 面改动重点）；④下轮=**v2 侧挂点实装（plans/023 §八）→ 收口（波 6：警告切硬错误+删观察期）** |
| 深度机 linux-x86_64 | ⬜ | **108-a 完成**：D5 剩余面固化（赋值位结果/可选 构造器保守拒绝负测 264_v2；零源码改动；D5 面完整处理链收口=107-a 修复 + 108-a 固化 + 架构边界登记） | 2e00a87 | 09-13 17:40 | **108-a 通告**：**零源码改动**（v2p md5 `b6d8c387…` / 宿主 md5 `f1484d79…` 均维持；新增负测 264 仅测试面）——win/arm64 侧无需动作（复跑时注意 107-a v2 产物已变需重建 v2p）；下一优先=D1 后续波次与联合体条件释放设施专项（登记）；92-a 跨机轮清单续见 HANDOFF |
<<<<<<< HEAD
| 单位机 ARM64 | ⬜ | **跨机验证轮完成 + 两缺陷裁决落档**：93-a~108-a+111-a~146-a 全量 ARM64 复验（基线 d25f487，a788a8a）——单测 1315/1315+E2E 292/288+110-a 探针 rc=0+锚定链 552331+对拍 44/44+运行级 3/3；**「跨机-a」「跨机-b」用户裁决（2026-09-14）均方案A**——实施排班=**单位机待家机 152-a 释放锁后认领实施轮** | d25f487 | 09-14 11:00 | **两缺陷裁决落档通告（用户 2026-09-14 选择均方案A）**：①**跨机-a**=全树重扫中文 C++ 标识符（以实施时树为准——家机 148~152-a 新增面须覆盖）→ASCII 改名+**扫描门禁脚本接入 ci.ps1**（Rust deny(non_ascii_idents) 同构）；②**跨机-b**=string_api.cpp `__cn_str_cmp` 归一化 -1/0/+1（Rust Ordering 同构）；③**致家机**：152-a 波 5 tests 面请遵守「C++ 标识符一律 ASCII」纪律（跨机-a 教训——新引入中文标识符会在实施轮重扫中一并拦截）；④实施轮验证清单=三平台门禁+锚定链（预期 552331 不变：改名不进产物/strcmp 实现不在 asm）+14_integration2 转绿+12_string_extra 保持；⑤排班详情见 HANDOFF 单位机节「下一轮任务」 |
=======
| 单位机 ARM64 | ⬜ | **跨机验证轮完成**：93-a~108-a+111-a~146-a 全量 ARM64 复验（基线 d25f487）——单测 1315/1315+E2E 292/288 过/1 败/3 跳+110-a 9 参数栈参数探针 rc=0（动态收口）+锚定链 **552331 行**逐字节一致+对拍 44/44+运行级 3/3；**新立案两缺陷呈报待裁：「跨机-a」中文 C++ 标识符 GCC 9 构建瘫痪（5 文件 9 名，探针改名实证修复面）+「跨机-b」字符串字典序幅值平台分叉（麒麟 glibc strcmp 对齐分叉 rodata→1/堆→56，LD_PRELOAD 实证）**——三方案各附推荐 A（ASCII 改名+门禁防复发 / 运行时归一化 -1/0/+1），详见 plans/014 观察项表与 HANDOFF 单位机节 | d25f487 | 09-14 10:10 | **跨机验证轮收工通告**：①**两缺陷待用户裁决**（跨机-a 阻塞本机构建——深度机 GCC 版本自查；跨机-b 深度机 x64l 侥幸不触发〔glibc 幅值恒 ±1〕但语义面同在）；②验证基于探针改名版（9 局部/常量名 ASCII 化，不进产物=结论对任一裁决方案有效），源码已还原=零源码净改动，各机无需重跑；③78_chain_build 本轮通过（上轮 OOM 败面消失）；④家机 148~152-a 新改动（波5/B6~B11）不在本轮验证面=下轮跨机轮 |
>>>>>>> 4e2583b (跨机验证轮收工（单位机麒麟ARM64，基线d25f487）：93-a~108-a+111-a~146-a 全量 ARM64 动态复验——①门禁（探针改名版）：清场构建首跑失败=「跨机-a」立案（GCC 9.3 拒绝 110-a/118-a/134-a/77-a 引入的 5 文件 9 个中文 C++ 标识符——MSVC/GCC≥10 接受掩盖；fb265dd 基线零中文标识符=回归实锤；9 名 ASCII 化后零警告+全量门禁绿=修复面完备）+单测 1315/1315（两次）+全量 E2E linux-arm64 292 用例 288 过/1 败/3 跳（+64 新用例全过+194 既有待批已修复+78 通过〔上轮 OOM 消失〕；唯一败=14_integration2=「跨机-b」）；②110-a 三后端栈参数修复动态收口：9 结构体 AAPCS64 栈路径探针 rc=0 双跑（六断言）+asm 栈位取指针+cpcy 拷贝环实证；③「跨机-b」立案：__cn_str_cmp 裸返 strcmp×麒麟 glibc 2.31 aarch64 对齐分叉（LD_PRELOAD 同库同串：rodata 未对齐→1/堆 16 对齐→56）×波3 字段堆拷贝触发 SIMD 路径（fb265dd 旧编译器对照打印 1=历史通过解释）；规范仅定义 <0/0/>0 符号语义两侧正确，幅值跨平台漂移违反确定性；④锚定链 ARM64 重锚：fix_p 552331 行（上轮 347685+204646）固定点逐字节一致+绑定自检+组件对拍 44/44（v2 树 29→44）+运行级 3/3；⑤意外事件定性：target/ 约 15 分钟空目录视图后原文件原时间戳全部回归（非删除——本地 ext4/无其他会话进程/AV 无命中，疑沙箱挂载瞬变；教训入 lessons：视图异常先复查再响应）；⑥两缺陷三方案呈报待裁（跨机-a 推荐=ASCII 改名+扫描门禁防复发〔Rust deny(non_ascii_idents) 同构〕；跨机-b 推荐=运行时归一化 -1/0/+1〔Rust Ordering 同构，单点覆盖宿主/v2 产物〕）；⑦文档：plans/014 状态表+观察项表两立案行+plans/019 §7.3+更新日志+HANDOFF+lessons；诚实边界：两缺陷未修复待裁（探针改名已还原=零源码净改动）、验证结论对任一裁决方案有效、78 归因未深究、探针留 target/p146arm 与 probe110arm 不入库)

> 状态：⬜ 空闲 ｜ 🏃 开发轮进行中（占锁，**全表至多一行**）｜ 🧪 验证轮进行中（自愿登记，不占锁）。
> **开工**：`git fetch` → 改本机行（状态 🏃 + 任务名 + 基线commit + 时间）→ push 主远程 `gitcode`
> ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> **收工**：本机行改回 ⬜（可附一句结论指针），随本次提交一起 push。
> **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。

## 契约变更通告（AGENTS.md §8.4）

> 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
> **三机共享契约**后：须在认领开发轮前提下进行，并在上表本机行「通告」列写简短标记 +
> 下方详情段展开（变更内容/影响面/哪些用例与平台需复跑）；其它机器开工 fetch 即见。
> 通告由发起者维护：本机下次 push 时新覆盖旧或删除。

### 144-a 契约变更详情（关键字体系审计 + 联合体裁决落盘，2026-09-15，家机 win-x64）

- **变更内容**：`plans/001` §2.1 关键字清单同步为裁决后口径（**50 保留字**：删 `自动`/`实现`、
  `原始`/`多行` 改为上下文前缀〔非保留字〕、`保护`/`虚拟`/`重写`/`抽象`/`作为`/`模块` 六词上下文化〔2026-09-16 补正〕；补 `模块`/`作为`/`不安全`、删已删残留 `从`）；
  新增 `plans/024`（审计全案 + 实施设计 §七）；`plans/023` §五.5 更正（135-a 探针误判撤销——
  真因=字段名「原始」是字面量前缀关键字）+ §十二 联合体方案 A+D。
- **本提交影响面**：**源码零改动（纯文档轮）**——三机门禁**无需复跑**；Linux/ARM64 侧无动作。
- **实施轮（后续会话认领）影响面预告**：①删 `自动` → 迁移 `tests/e2e/16_function_extra/函数增强.cn`
  与 `tests/e2e/104_实例方法作值/主.cn`（`自动`→`变量` 同义替换）；②`89_关键字穷举` 用例串同步；
  ③`原始`/`多行` 非保留化（lexer 非前缀语境回退标识符）；④`scripts/check_keywords_sync.py` 门禁接入
  `ci.ps1`——实施轮 push 时须复跑**三平台全量 E2E + v2p 重建（若 v2 表变动）+ 锚定链/对拍**，并更新本通告。
- **联合体方案 A+D（`plans/023` §十二）**：语义层拒绝拥有型成员（+`手动释放` 标注逃生舱）+
  删 IR 无条件释放半截机制——实施轮须负测/正测/反证（v2 树无联合体用例 → 固定点预期不变，实测确认）。
