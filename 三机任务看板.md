# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 |
|---|---|---|---|---|
| 家机 win-x64 | 🏃 | 315-a 清零波次3：T6 嵌套深度防护+T7 语义 O(d³) 根治+HANDOFF 粘连事故/check_handoff 门禁同轮根治+E2E 417 | 8e2865e | 09-17 22:1x·计划 plans/025 §1.0 |
| 单位机 ARM64 | ⬜ | - | - | - |
| 深度机 linux-x86_64 | ⬜ | - | - | - |

> - **状态**：⬜ 空闲（行=各列「-」·**空闲行不留任何任务/结论/通告**——232-a 用户令：有任务才填行）
> - 🏃 **开发轮进行中**（**占锁；全表至多一机**——231-a 恢复唯一性；行=任务名+指针+基线+时间）
> - 🧪 **验证轮进行中**（探针检测/E2E 测试·自愿登记·不占锁·行格式同 🏃）
> - **开工**：`git fetch` → 改本机行（🏃 + 任务名 + 基线commit + 时间 + 指针）→ push 主远程 `gitcode`
>   ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> - **收工**：本机行**整行清空**（⬜ + 各列「-」），随本次提交一起 push——结论进 plans/025 收工回填，
>   **看板行不留结论也不留通告**（232-a 用户令收紧，原「可附一句结论指针」废止）。
> - **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。空闲行干净已入 check_handoff.py 门禁。

## 契约变更通告（AGENTS.md §8.4）

> - 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
>   **三机共享契约**后：须在认领开发轮前提下进行，并在**下方本机通告段**写明（变更内容/影响面/哪些用例
>   与平台需复跑）——**看板表内不承载通告**（232-a 用户令：空闲行必须干净，广播独立成段）；
>   其它机器开工 fetch 即见。
> - 通告由发起者维护：**影响面执行完毕后（或最迟下一次本机 push）删除本机通告段**——不得长期挂账。

### 家机 win-x64 通告段（313-b 🏃）

- **daemon 结构变更广播（2026-09-17·313-b·§8.4·影响深度机 daemon/所有写日志轮）**：采样日志**月文件→日文件制**（用户令）——①daemon 写点 `_切日`（`日志/YYYY-MM-DD.md`·S1b 记法不变）；②**深度机在跑的旧版 daemon 请停→pull→重启**（旧版仍写月文件=与日制分裂+check_handoff 日文件名校验将拦截月名文件）；③单日上限 1500 行（膨胀日即压缩信号）；④存量 2026-09.md 已拆日（2026-09-17.md·明细归 git）。

### 家机 win-x64 通告段（305-a 🧪）

- **★T43 新立案广播（2026-09-17·305-a 立案+307-a 族扩散·406=win 全量唯一败面·待根治）**：win -O3 浮点汇合 Copy A2070——根因=297-b emitCopy 浮点分支（x64_instructions.cpp:306-308）movsd/movss mem 缺 qword/dword ptr（家族第五犯·静态审查未真跑 ml64=T25 win 修复从未运行级工作）；族扩散=7/8 形态 O3 挂（f64 全操作数+f32·当循环免疫）+静态 24 发射点全保护=**单点修全治（甲 ~2 行）**；立案=plans/021 §三-G T43 行；修复后 win 复验归家机。
- **★T44 新立案广播（2026-09-17·309/310-a 类型宽度轴+扩轴矩阵·影响深度机/单位机跨机复验+306-a 搭车决策）**：**i128/u128 双前驱汇合 Copy 高半槽错**——①**三元全族**（简单/嵌套/作实参/参与算术·**整128+正128 双符号**）→ win **O0/O3 双级 A2006 undefined symbol rbp0**（asm 铁证：高半 `mov [rbp0],rax` 应为 `[rbp-232]`=槽偏移 0 拼接 bug·rbp0 家族第四形态〔顶层静态/嵌套 lambda/未登记槽之后〕）；②**双分支 if-else（含汇合块外隔语句消费）→ O3 非确定静默错值**（三跑三值 946…/795…/1062…=栈地址样 UB 铁证·O0 对）。**挂面=仅 128 位×双前驱汇合**（bool/整8~64 十六形态全✓+三前驱链✓+i128 单分支✓）；x64l/arm64 复验归深度机/单位机（探针=target/p305a/{t44scan,t44ext,t44if2}）；立案=plans/021 §三-G T44 行（甲=槽名修正·乙=E2E 转正·丙=rbp0 静态扫描·建议三合一）。
- **T26 win 挂面修正（本段前播·已被 304-a 收工印证消化）**：win 回归轨 4 实弹（m26_01+s2609179001~003）基线 58b7e70 上 O0 对/O3 错（298-a「win 免疫」仅成立运行时输入形态·asm 铁证 call 前硬编码常量）——304-a 根治（GlobalValuePass 汇合块·平台无关）后**家机 305-a 复验 4/4 绿+410 PASS+全量 427/428**（T26 win 侧闭环✓·败面仅余 T43/406）。

### 单位机 ARM64 通告段（297-a）

- **B10 消歧判据变更广播（2026-09-17·§8.4·影响家机 296-a v2 ? 链）**：宿主 `?` 三元/后缀传播消歧已改**同层 `:` 配对前瞻**（ternaryColonAhead——`?` 后同层有配对 `:` 判三元·否则传播；原「表达式起点白名单」废弃）——`a ? -b : c` 不再要求括号。**v2 侧解析判定分流须同口径**（家机 296-a v2 ? 链实施时对齐·否则双侧分叉）；规范条文=plans/001 §4.5+spec 01b §5.8/5.8B。后缀传播合法用法（`x? + 1`=(x?)+1）行为不变（E2E 60 复验绿）。
- **T24 ++/-- 语义补全广播**：宿主已补后缀 `i++` 返回原值/C 语义+01b 优先级表登记（后缀 13 级/前缀 12 级）——v2 树同型面（自举编译器）待对齐轮处理（登记 plans/021 T24 行）。

### 单位机 ARM64 通告段（286-a）

- **daemon 结构变更广播（2026-09-17·影响深度机/家机采样轮）**：S1b 分文件化——①日志改写 `tests/cnsmith_hits/日志/YYYY-MM.md`（旧 采样日志.md=固定指针 9 行·禁止再追加）；②记法=变化时记+汇总；③种子=时间戳到秒（不再按轮号·重启不撞）；④差分前自动删旧分歧清单+重建后核验 target/cn 新鲜度。**深度机在跑的旧版 daemon 请停→pull→重启**（旧版仍写单文件=与月文件制分裂+pull 会因工作树脏失败）；家机下次采样前 pull 即可。存量（含贵机在飞轮次）已折入 日志/2026-09.md 收档。
- **T22 呈报同步（缺陷零容忍）**：arm64 -O3 位运算常量立即数非法汇编（31 样本入库 run_err/）——贵机采样若切 arm64 目标会复现；win/x86_64 免疫。方案呈报用户裁决（plans/021 §3-G T22）。

### 深度机 linux-x86_64 通告段（314-a）

- **★T45 新立案广播（2026-09-17·314-a·影响家机/单位机跨机复验+清零波次排班）**：**128 位字面量实参 ABI 契约分叉**——`制造(123456789012)`（i128/正128 字面量〔含常量折叠〕实参）→ x64l **O0/O3 双级 SIGSEGV**：调用侧 `mov rsi,[槽]` 值直传低 64 位 vs 被调方 `mov r10,rsi; [r10]/[r10+8]` 按指针解引用（字面量数值被当地址）；asm 铁证 mov vs lea 对照。**挂面=128 位字面量实参×任意返回类型/参数位次/丢弃返回**（7 形态全挂双级）；健康=变量/表达式/结构体字面量实参（16B 槽+lea·84-a/88-a 路径）；字面量值恰落合法页→静默读垃圾=UB 双态（安全面）。**win/arm64 复验归家机/单位机**（探针=target/p314a/t45_*〔仓库根跑·cn build 相对路径〕+实弹 run_err/m45_01/02）；立案=plans/021 §三-G T45 行（甲=字面量实参统一 16B 槽物化+取地址〔推荐〕·乙=ABI 改值传双寄存器·丙=静态扫描门禁；建议甲+丙·与 T44 修复写集零交叠可同轮波次清零）；面台账行 82 已加。
- **T44 x64l 复验完成销账（家机 309/310-a 广播点名项）**：三元族（整128/正128/嵌套）x64l 双级 SIGSEGV（asm `mov qword ptr [rbp],r10` 高半槽偏移 0=GAS 版 rbp0 同根因·低半 [rbp-248] 正常对照）；**双分支 if-else（含汇合块外消费）x64l 免疫**（O0=O3 全对·asm 零 [rbp] 基址写=静默错值为 win 单侧）；三前驱链✓——T44 行证据已回填（arm64 复验仍归单位机）。
- **check_handoff 门禁盲区通告（随 T45 同轮登记）**：HANDOFF 深度机节曾现「标题六连写粘连」（306-a 收工引入·本轮整节替换根治）——check_handoff「三机节齐全」按行首前缀判定未拦同行重复标题；**扩「标题连写检测」条归下一开发轮**（本轮验证轮禁改 scripts/·lessons 314-a 坑③已载）。

- **★T44/T45 裁决广播（2026-09-17 用户令「同意，T44 三合一 + T45 甲丙」·三机可见）**：**T44=甲+乙+丙三合一**（槽名修正+形态 E2E 转正+rbp0 静态扫描门禁）；**T45=甲+丙**（128 位字面量实参统一 16B 槽物化+取地址〔乙 ABI 改值传不取〕+静态扫描门禁）。已落盘 plans/021 §三-C **C22/C23**（⬜ 待认领·与 315-a 写集〔parser/semantic/check_handoff〕零交叠）；建议与 C22/C23 同轮波次清零；win/arm64 复验归家机/单位机（探针=target/p314a/t45_*·仓库根跑）。任一空闲机待 315-a 收工后可认领开发轮实施。

（深度机无其他在飞通告。）
