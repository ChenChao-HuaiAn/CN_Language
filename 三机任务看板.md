# 三机任务看板

> 三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行协作的**唯一看板**，
> 与任务类型无关（编译期安全专项、自举、性能、标准库……任何任务都认这里）。
> 协议规则详见 AGENTS.md §8「三机并行协作协议」；本文件只放看板表与通告，不写其他内容。
> 每机**只改自己那一行**（及自己的通告段）；任何机器开工前 fetch 看此表，收工时更新自己行。

## 看板表

| 机器 | 状态 | 当前任务 | 基线commit | 时间 | 通告 |
|---|---|---|---|---|---|
| 家机 win-x64 | ⬜ | 88-a 跨机验证轮完成（E2E 275/255 过；锚定链 348908 行固定点；实物复验 243/244/246/248 全过）——结论见 plans/019 §7.2 家机段 | efc0c8c | 09-13 01:08 | **win 侧 148_v2 崩（0xC0000005，87-a 宿主引入，linux 不触发）——新缺陷待修，见 HANDOFF 家机节；家机第二会话独立复核补充（宿主维度严格单变量二分）：触发变量=回退 `src/cn_compiler/ir/ir_expr.cpp` 87-a「静态聚合赋值」块即恢复正常（P6a 探针 113/148 形态 255），`codegen/x64/x64_codegen.cpp` 已排除（回退仍崩）；触发形态=`f(...).成员`（结构体返回+链式成员访问）之后的结构体返回调用在 IR 层退化为无 retbuf（被调符号正确、仅 retbuf/实参起始位丢失；同源码 linux 后端亦退化=IR 层平台无关路径）；旧宿主 vs 87-a 宿主同源码对照：683bef5/86-a 宿主 ✓ 113 vs 87-a/当前 ✗ 崩溃；探针与证据留 target/p88win/** |
| 深度机 linux-x86_64 | ⬜ | **94-a 完成**：v2 数组声明语法缺口（C6）销项——v2 栈数组全链（语法/语义/IR + 越界检查） | c7e705b | 09-13 10:52 | **94-a 完成通告**：v2 语法面自此支持栈数组（规格书 3.5 `类型[长度] 名`：声明+初始化列表+下标读写+循环动态下标+作实参退化+结构体元素成员访问+字符串/浮点元素+**越界检查**〈下标≥长度→运行时错误(2) 数组越界，宿主同码〉）；**宿主侧零改动**（纯 v2 侧功能轮）；新增 E2E **253/254** 双编译对照门禁 + 反证双件 + 探针 11 件双侧一致；**v2p 二进制已变**，win/arm64 侧请重建并复跑 E2E；新登记 C7（v2 字符字面量 `'A'` 缺口）/C8（v2 结构体字段数组）/C9（字符串元素数组释放，两侧同缺）；92-a 跨机轮清单续见 HANDOFF 深度机节 |

> 状态：⬜ 空闲 ｜ 🏃 开发轮进行中（占锁，**全表至多一行**）｜ 🧪 验证轮进行中（自愿登记，不占锁）。
> **开工**：`git fetch` → 改本机行（状态 🏃 + 任务名 + 基线commit + 时间）→ push 主远程 `gitcode`
> ——push 成功=锁到手；被拒（non-fast-forward）=他人抢先，合并后重看再决策。验证轮（🧪）免认领。
> **收工**：本机行改回 ⬜（可附一句结论指针），随本次提交一起 push。
> **验证结论必须标注基线 commit**；远程已前进时 rebase 后复跑增量。

## 契约变更通告（AGENTS.md §8.4）

> 改 `.expected` / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001` 规范 / cn-language-spec 等
> **三机共享契约**后：须在认领开发轮前提下进行，并在上表本机行「通告」列写简短标记 +
> 下方详情段展开（变更内容/影响面/哪些用例与平台需复跑）；其它机器开工 fetch 即见。
> 通告由发起者维护：本机下次 push 时新覆盖旧或删除。

（暂无）
