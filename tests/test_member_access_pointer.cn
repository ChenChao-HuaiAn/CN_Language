// 测试指针类型的成员访问
// 问题：当成员访问的结果是指针类型时，后续访问应该使用 -> 而不是 .

结构体 编译选项 {
    字符* 输入文件路径;
    整数 详细输出;
}

结构体 编译上下文 {
    编译选项* 选项;
}

整数 测试成员访问(编译上下文* 上下文) {
    // 上下文->选项 返回 编译选项* 类型
    // 然后访问 输入文件路径 应该使用 ->
    返回 上下文->选项->输入文件路径 != 0;
}

整数 主程序() {
    返回 0;
}
