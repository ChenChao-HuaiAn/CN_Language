// 测试指针变量的类型传播
// 问题：函数声明* 函数声明 = 声明.作为函数声明;
// 生成的C代码中 r40 被错误声明为 struct 函数声明 而不是 struct 函数声明*

结构体 声明节点 {
    整数 类型;
    整数* 作为函数声明;  // 指针成员
}

函数 测试指针变量(声明节点* 声明) : 整数 {
    如果 (声明 != 无) {
        整数* 函数声明 = 声明.作为函数声明
        如果 (函数声明 != 无) {
            返回 1
        }
    }
    返回 0
}
