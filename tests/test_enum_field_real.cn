// 测试目的：验证结构体中枚举类型字段的比较操作
// 问题：编译器不支持结构体中枚举类型字段的比较

// 定义枚举类型
枚举 颜色 {
    红,
    绿,
    蓝
};

// 定义包含枚举字段的结构体（使用枚举类型）
结构体 物体 {
    颜色 颜色值;
    整数 大小;
};

函数 主程序() -> 整数 {
    物体 球;
    球.颜色值 = 红;
    
    // 测试枚举值比较（使用枚举字段）
    如果 (球.颜色值 == 红) {
        返回 1;
    }
    
    返回 0;
}
