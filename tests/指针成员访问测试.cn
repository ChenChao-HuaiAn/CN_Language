// 指针成员访问语法验证测试
// 目的：验证CN语言中指针类型访问成员使用 "." 还是 "->"

结构体 点 {
    整数 x;
    整数 y;
};

函数 主程序() -> 整数 {
    // 测试1：对象类型使用 . 访问成员
    点 p;
    p.x = 10;
    p.y = 20;
    
    // 测试2：指针类型使用 . 访问成员（用户认为的正确语法）
    点* ptr = &p;
    ptr.x = 30;  // 如果编译通过，说明用户是对的
    ptr.y = 40;
    
    返回 p.x + p.y;
}
