// 简化版词元类型定义 - 使用整数替代枚举字段

公开:

// 简化的枚举定义
枚举 托肯类型 {
    托肯如果,
    托肯否则,
    托肯标识符,
    托肯结束,
    托肯错误
};

// 简化的结构体定义 - 使用整数类型存储枚举值
结构体 托肯 {
    整数 类型字段;  // 使用整数存储枚举值
    字符串 值;
    整数 行号;
};

// 主函数
函数 主程序() -> 整数 {
    托肯 测试托肯;
    测试托肯.类型字段 = 托肯如果;  // 枚举值自动转换为整数
    测试托肯.值 = "测试";
    测试托肯.行号 = 1;
    
    // 比较时使用枚举值
    如果 (测试托肯.类型字段 == 托肯如果) {
        打印("匹配成功");
    }
    
    返回 0;
}
