// 测试指针成员访问类型推断修复
// 使用现有的 -> 语法测试代码生成器

结构体 数据 {
    整数 值;
    小数 数值;
};

函数 主程序() -> 整数 {
    // 指针类型成员访问 - 使用 -> 语法
    数据* ptr = 分配内存(8);
    ptr->值 = 20;
    ptr->数值 = 6.28;
    
    // 验证修改是否生效
    整数 结果 = ptr->值 + (整数)ptr->数值;
    
    释放内存(ptr);
    返回 结果;
}
