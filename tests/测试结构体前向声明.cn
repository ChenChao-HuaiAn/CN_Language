// 测试结构体前向声明顺序
// 结构体A引用结构体B（通过指针），但B的定义在A之后

结构体 A {
    B* 指针;
};

结构体 B {
    整数 值;
};

函数 主程序() -> 整数 {
    A 变量A;
    B 变量B;
    
    变量B.值 = 100;
    变量A.指针 = &变量B;
    
    返回 变量A.指针->值;
}
