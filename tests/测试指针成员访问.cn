// 测试指针类型成员访问操作符选择
// CN语言中指针使用 "." 语法，但生成的C代码应该使用 "->"

结构体 作用域 {
    整数 种类;
    整数 父作用域;
};

函数 主() -> 整数 {
    // 使用类型转换表达式创建指针
    作用域* 作用域指针 = (作用域*)分配内存(8);
    
    // CN语言中指针使用 "." 语法
    作用域指针.种类 = 1;
    作用域指针.父作用域 = 0;
    
    返回 作用域指针.种类;
}
