// j：可选<盒子> 拆包（同族面：可选降级合成结构体的类宽度）
导入 映射集合::映射;

类 盒子 {
    公开:
    整32 值;
    函数 盒子() { 值 = 5; }
    函数 盒子(盒子& 其他) { 值 = 其他.值; }
}

函数 主() -> 整32 {
    可选<盒子> 槽 = 某些(盒子());
    如果 (槽.有值) {
        盒子 b = 槽.值;
        打印("b.值: ", b.值);
    } 否则 { 返回 2; }
    打印("到主末尾");
    返回 0;
}
