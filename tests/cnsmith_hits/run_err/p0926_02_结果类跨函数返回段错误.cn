// e：正常(盒子()) 自持装箱拆包（甄别：拆包双份所有权是否与容器借用无关、任何类装箱拆包即双主）
// 预期：若结果析构也删值字段=双释放 rc=134；若仅拆包侧删=rc=0（泄漏型）
导入 映射集合::映射;

类 盒子 {
    公开:
    整32 值;
    函数 盒子() { 值 = 5; }
    函数 盒子(盒子& 其他) { 值 = 其他.值; }
}

函数 造盒() -> 结果<盒子, 整32> {
    结果<盒子, 整32> r = 正常(盒子());
    返回 r;
}

函数 主() -> 整32 {
    结果<盒子, 整32> r = 造盒();
    如果 (r.正常) {
        盒子 b = r.值;
        打印("b.值: ", b.值);
    } 否则 { 返回 2; }
    打印("到主末尾");
    返回 0;
}
