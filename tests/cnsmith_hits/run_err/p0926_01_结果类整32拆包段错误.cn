// f：主内直接 正常(盒子()) 拆包（甄别：跨函数返回是否为 e 崩溃必要条件）
导入 映射集合::映射;

类 盒子 {
    公开:
    整32 值;
    函数 盒子() { 值 = 5; }
    函数 盒子(盒子& 其他) { 值 = 其他.值; }
}

函数 主() -> 整32 {
    结果<盒子, 整32> r = 正常(盒子());
    如果 (r.正常) {
        盒子 b = r.值;
        打印("b.值: ", b.值);
    } 否则 { 返回 2; }
    打印("到主末尾");
    返回 0;
}
