// 测试结构体依赖关系
// 结构体A包含结构体B类型的成员，但结构体B定义在A之后
// 通过前向声明解决依赖问题

结构体 结构体A {
    整数 值;
    结构体B* 指向B;  // 指针类型，需要前向声明
};

结构体 结构体B {
    整数 数据;
    结构体A* 指向A;  // 循环引用
};

函数 主程序() -> 整数 {
    结构体A 变量A;
    结构体B 变量B;
    
    变量A.值 = 10;
    变量B.数据 = 20;
    
    返回 0;
}
