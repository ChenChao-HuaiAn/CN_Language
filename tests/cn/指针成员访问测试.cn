// 指针成员访问测试

结构体 测试结构 {
    整数 值;
    字符串 名称;
};

函数 测试指针访问(测试结构* 指针) -> 整数 {
    整数 结果 = 指针.值;  // 指针使用 "." 语法，但应生成 "->"
    返回 结果;
}

函数 主程序() -> 整数 {
    测试结构 数据;
    数据.值 = 42;
    数据.名称 = "测试";
    
    整数 结果 = 测试指针访问(&数据);
    返回 结果;
}
