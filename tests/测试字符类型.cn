// tests/测试字符类型.cn
// 测试字符类型支持

函数 主程序() -> 整数 {
    // 字符变量
    字符 ch = 'A';
    如果 (ch == 'A') {
        打印("字符比较成功");
    }
    
    // 字符数组
    字符 缓冲区[64];
    缓冲区[0] = 'H';
    缓冲区[1] = 'i';
    缓冲区[2] = '\0';
    
    打印(缓冲区);
    返回 0;
}
