// E2E 285 符号供给（v2 自举链，plans/020 移植纪律）：v2p.obj 只含 v2 自身实例化
//   的容器类型——本用例的 向量<整64>（构造/追加/大小/拷贝构造）符号须由本文件经
//   **宿主真实管线**编译为 .o 参与链接（置于 v2p.obj 之前，符号权威）。与
//   126/218/219/221 同款机制（②b B7）。
导入 容器::向量;

// 实例化 向量<整64>（含拷贝构造——复制(容器) 分派依赖）
函数 供给向量() -> 整64 {
    向量<整64> 甲 = 向量<整64>();
    结果<空类型, 整32> 加 = 甲.追加(7);
    如果 (!加.正常) { 返回 101; }
    向量<整64> 乙 = 甲;
    如果 (乙.大小() != 1) { 返回 102; }
    返回 0;
}

// 宿主编译入口（cn build 需要主函数；链接 muldefs 下 v2asm 在前，cn_main 归 v2 产物）
函数 主() -> 整32 {
    返回 整32(供给向量());
}
