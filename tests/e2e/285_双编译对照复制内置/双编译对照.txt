# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   285 复制(x) 泛型克隆内置四类型分派（206-c 波 4 子轮销项）——同一源码两侧各编
#   各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 校验和 15（①标量直通1+②字符串独立落堆2+③结构体深拷4+④容器拷贝构造8
#   各形态「新增分配数+值正确」置位；新增分配数实测两侧同值 ①0 ②1 ③1 ④1）。
#   供给=供给.cn：向量<整64> 全方法面符号（含拷贝构造）由宿主真实管线编译供给
#   （v2p 只实例化自身用到的类型——126/219 同款）。
退出码=15
源文件=主.cn
借链=是
供给=供给.cn
