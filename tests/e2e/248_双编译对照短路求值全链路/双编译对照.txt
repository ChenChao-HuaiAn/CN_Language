# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   248 短路求值全链路（栈净契约/实参位/惰性求值/区间标注）——同一源码两侧各编
#   各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   88-a 修复面：v2 语法层 解析二元链/解析层_子/解析二元层/解析二元循环 左起点
#   显式传递（短路左操作数区间不再为空）+ IR 层 表达式栈净栈契约归位原语
#   （IR生成.表达式栈弹到深度/表达式栈归位压回）+ 短路求值归位（净 +1）。
#   本用例不依赖容器符号（无 借链/供给）；不含字符/浮点打印（v2 既有语法与
#   打印限制）。
退出码=255
源文件=主.cn
