// H7 补完负测（2026-08-25）：类仅有带参构造时，裸声明（类名 变量）无默认构造，
//   须编译报错（与 C++ 语义一致）——防止 IR 层静默分配未初始化对象。
// 语义依据：H7 根治补完——类类型栈变量无初始化器声明须可默认构造；
//   无构造类允许默认构造仅分配（与 类名() 语义一致），仅有带参构造类则报错。
类 仅带参 {
公开:
    整64 值

    函数 仅带参(整64 初始) {
        值 = 初始
    }

    函数 获取() -> 整64 {
        返回 值
    }
}

函数 主() -> 整32 {
    仅带参 x
    x.值 = 1
    打印("x: ", x.获取())
    返回 0
}
