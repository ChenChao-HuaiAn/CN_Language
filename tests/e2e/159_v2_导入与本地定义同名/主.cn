// plans/018 P6b 工作流2（呈报一B，2026-09-07 用户终裁）：E0255 负测——
//   「导入 m::符号」= 具名绑定②（Rust use m::f 一致），与本文件本地定义
//   同名 = 编译错误「导入与本地定义同名」（规格08-三 §5.9.7 冲突矩阵 ①×②）。
//   本用例一次覆盖两种 ② 形态：路径导入（工具库::版本）与花括号导入
//   （工具库::{价格}）。旧实现（路径导入=模块级通配）对两形态均静默放行。
导入 工具库::版本;
导入 工具库::{价格};

// 本地定义 版本()——与 路径导入 工具库::版本 撞名 → E0255
函数 版本() -> 整64 {
    返回 100;
}

// 本地定义 价格()——与 花括号导入 工具库::{价格} 撞名 → E0255
函数 价格(整64 金额) -> 整64 {
    返回 金额;
}

函数 主() -> 整32 {
    返回 0;
}
