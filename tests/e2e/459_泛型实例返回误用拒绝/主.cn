// E2E 459_泛型实例返回误用拒绝（477-a D10 波2·重放环境映射持久门禁）
// 泛型体内返回类型误用（T=整32 实例 返回 "文本"）：470-a 重放=T 豁免语境
//   （转换检查对类型参数放行·p2' 探针双侧静默）→477-a 环境映射（T→实参）
//   =返回比对落真实类型→编译期拒绝。宿主同形态=470 顺带件登记（重放回滚）。
// v2 负测（v2闭环.txt 退出码=负·v2p sem5 拒通道）；诊断行号不精确锚定。
泛型 <类型 T>
函数 取值(T 源) -> T {
    返回 "文本";
}

函数 主() -> 整32 {
    返回 整32(取值<整32>(5));
}
