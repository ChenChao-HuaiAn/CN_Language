// E2E 232_v2（2026-09-12 第八十轮·池索引 0 哨兵根治）：类型大小 编译期折叠
//   首节点形态回归——入口文件第一个函数体首条语句的条件表达式里 类型大小
//   的被调标识符恰落节点池索引 0 时（本用例：主() 首条语句即 如果(类型大小…)，）
//   IR 层 类型大小 折叠特判守卫 `子起点 > 0` 修复前误判「无被调」→ 退化为
//   普通调用 → 未定义符号 `_类型大小` 链接失败（探针 P24 实证：HEAD 链接失败 /
//   修复后退出码 0）。同族守卫与构造器特判同一根因（池索引 0 哨兵），本用例为
//   类型大小 面的对应防线。反证实证：HEAD 版本用例链接失败（v2asm 含
//   `call _类型大小`），修复版 PASS。
//   ⚠ 首条语句不能是变量声明——声明语句的类型节点先入池会抢占索引 0
//   （反证时首版用例因此未命中，已由本版本修正；剔除变量声明后 类型大小
//   的标识符即文件首个入池节点）。校验和：三条断言全过返回 7。
函数 主() -> 整32 {
    如果 (类型大小(整64) != 8) { 返回 0; }
    如果 (类型大小(整32) != 4) { 返回 0; }
    如果 (类型大小(字符串) != 8) { 返回 0; }
    返回 7;
}
