# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十五个对照用例）：
#   257 结构体字段数组全链（元素写读/零初始化/循环动态下标/嵌套结构体字段/
#   两层数组字段链/结构体元素字段/作实参退化/与字段标量混排）——同一源码两侧
#   各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   97-a（2026-09-13 C8）门禁：修前 v2 侧语义层误拒（「下标对象须为字符串或指针:
#   .分数」——成员对象只认字符串字段）。
退出码=255
源文件=主.cn
