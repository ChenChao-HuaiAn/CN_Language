// 工具 模块（第 7 层：50_class_private 类内默认私有 E2E 依赖模块）
// 类内默认私有（v2.0 变更，规格书08-四/06-六）：类内无标签成员默认为私有——
//   ① 私有: 显式段：字段 计数（外部不可访问，仅类内方法可读写）
//   ② 无标签段：函数 增加() 默认私有（v1.0 默认公开）
//   ③ 公开: 段：函数 读取()（外部可调用）
// 类内方法（读取）可访问私有字段 计数（类内语义正常，访问控制按类上下文判定）
// 错误路径（类外访问默认私有成员 -> 语义报错）由单测 SemanticClassDefaultPrivate 覆盖
公开:
类 计数器 {
    // 私有: 显式段——字段仅类内可访问
    私有:
    整64 计数;

    // 无标签段：默认私有（v2.0），仅类内方法可调用
    函数 计数器() -> 空类型 {
        自身.计数 = 0;
    }

    函数 增加() -> 整64 {
        自身.计数 = 自身.计数 + 1;
        返回 自身.计数;
    }

    // 公开: 段——外部可调用
    公开:
    函数 读取() -> 整64 {
        // 类内方法访问私有字段 计数（类内语义正常）
        返回 自身.计数;
    }
}
