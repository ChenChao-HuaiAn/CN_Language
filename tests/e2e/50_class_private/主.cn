// 主入口（第 7 层：50_class_private 类内默认私有合法路径 E2E，规格书08-四/06-六）
// v2.0 类内可见性变更验证（合法路径）：
//   ① 公开: 成员 读取() 可跨模块实例化后调用
//   ② 私有: 字段 计数 仅类内方法可访问（类内语义正常）
//   ③ 无标签成员（增加/构造器）默认私有——本用例不直接调用（错误路径由单测覆盖），
//      通过公开方法 读取 的返回值验证 计数 状态（构造器/增加 由类内逻辑触发）
// E2E 只测合法用法：外部经 公开: 方法访问类状态；默认私有成员不可见由单测验证。

导入 工具::计数器

函数 主() -> 整32 {
    打印("===== 类内默认私有（第 7 层，合法路径）=====")

    // ---- ① 跨模块实例化公开类（模块公开类 + 类内公开构造器）----
    计数器 计数1 = 计数器()
    整64 初始 = 计数1.读取()
    打印("初始 读取(): ", 初始)                  // 0（构造器私有: 段内初始化）

    // ---- ② 公开方法 读取 可访问私有字段（类内语义正常）----
    计数器 计数2 = 计数器()
    // 增加() 为默认私有成员——本用例不直接调用（单测验证类外不可访问）；
    // 通过公开方法链验证类状态：此处构造后直接读取仍为 0
    整64 值2 = 计数2.读取()
    打印("计数2 读取(): ", 值2)                  // 0

    // ---- ③ 两个实例互不干扰（私有字段 per-instance）----
    计数器 甲 = 计数器()
    计数器 乙 = 计数器()
    打印("甲 读取(): ", 甲.读取())               // 0
    打印("乙 读取(): ", 乙.读取())               // 0

    打印("===== 类内默认私有测试完成 =====")
    返回 0
}
