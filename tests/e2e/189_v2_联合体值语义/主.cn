// 189_v2：联合体值语义 v2 闭环（46-e 观察项根治，2026-09-10 第四十九轮；
//   宿主侧同源用例=189）——v2p 编译本源码 rc=31=五形态校验和全过：
//   A 成员写读往返（+1；修复前「未声明的类型」可见失败=46-e 立案形态）/
//   B 内存共享 写 整64 读 整32 低 4 字节（+2）/ C 声明位整体赋值（+4）/
//   D 赋值位整体拷贝（+8）/ E 值传参（+16）。
//   实现：解析结构体声明 联合体关键字分支——字段偏移全 0+大小=最大字段
//   向上对齐（C 布局，宿主 computeLayout isUnion 同构）；仍产 节点_结构体，
//   语义登记/成员访问/整体赋值/传参全链复用零新节点零新 IR。
联合体 数值 {
    整32 a;
    整64 b;
}

函数 读甲(数值 v) -> 整32 {
    返回 v.a;
}

函数 主() -> 整32 {
    数值 n;
    整32 c = 0;
    n.a = 42;
    如果 (n.a == 42) { c = c + 1; }
    n.b = 42;
    如果 (n.a == 42) { c = c + 2; }
    n.b = 4408;
    数值 m = n;
    如果 (m.b == 4408) { c = c + 4; }
    数值 k;
    k = m;
    如果 (k.a == 4408) { c = c + 8; }
    n.a = 99;
    如果 (读甲(n) == 99) { c = c + 16; }
    返回 c;
}
