# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十七个对照用例）：
#   259 结构体字段数组元素释放 + 元素下标缩放（字段数组写读/元素释放/内容独立性/
#   非字符串对照/嵌套字段数组/作实参/函数尾释放/标量混排）——同一源码两侧各编各跑，
#   退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   99-a（2026-09-13）门禁：①C11 字段数组元素释放（修前双侧同缺）；②**宿主缺陷 H4**
#   （字段数组元素步进用 types::typeSize=0 -> 下标不缩放，形①③失败；v2 侧本已正确）。
退出码=255
源文件=主.cn
