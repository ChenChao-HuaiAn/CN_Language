// 模块X（第 7 层：44_crate_isolate 命名空间隔离 E2E 依赖模块之一）
// 公开: 函数 双倍(整64) -> 整64 —— 与 模块Y.cn 的同名同签名函数**互不冲突**
// v2.0 crate 隔离（规格书08-一/09-四）：每个模块是独立命名空间（crate 分桶），
//   跨模块同名符号允许，仅同一模块内重名报错。X 的 双倍 返回 ×2。
// 第 9 层 Debug 核实：函数跨模块同名已隔离（本用例验证）；**类型/常量跨模块
//   同名尚未隔离**（语义层 typeNames_/globalConstValues_ 全局去重）——已知限制，
//   完整修复（类型级 crate 分桶 + 限定名解析）列入后续，见 HANDOFF/plans/lessons。
公开:
函数 双倍(整64 数值) -> 整64 {
    返回 数值 * 2;
}
