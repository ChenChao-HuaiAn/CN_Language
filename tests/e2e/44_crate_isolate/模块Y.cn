// 模块Y（第 7 层：44_crate_isolate 命名空间隔离 E2E 依赖模块之二）
// 公开: 函数 双倍(整64) -> 整64 —— 与 模块X.cn 同名同签名，各自独立命名空间
// Y 的 双倍 返回 ×3（与 X 的 ×2 区分，验证限定调用解析到正确模块）
// 第 9 层 Debug 核实：函数跨模块同名已隔离（本用例验证）；类型/常量隔离限制见 模块X 注释。
公开:
函数 双倍(整64 数值) -> 整64 {
    返回 数值 * 3;
}
