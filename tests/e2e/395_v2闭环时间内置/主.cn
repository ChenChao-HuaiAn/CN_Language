// 395_v2闭环时间内置（276-a D29②验收）：
//   时间::当前时间戳/单调时钟毫秒 限定名内置直调（276-a 内置表注册——
//   宿主 ir_call.cpp:283 对齐）。两断言累加全过=退出码 18（9+9）。
函数 主() -> 整32 {
    整64 现在 = 时间::当前时间戳();
    整64 毫秒 = 时间::单调时钟毫秒();
    整32 校验 = 0;
    如果 (现在 > 1700000000) { 校验 = 校验 + 9; }
    如果 (毫秒 > 0) { 校验 = 校验 + 9; }
    返回 校验;
}
