# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   238 结构体字面量作实参（84-a）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   修复前 v2 侧**解析失败**（[parse] 语句缺少分号 → 中止不产 asm）；任一侧退回该
#   缺口或字面量字段错位/临时槽泄漏 → 该校验位丢失 → 退出码变化 → 对照失败。
#   供给=供给.cn：向量<盒子> 方法符号（v2p 只实例化自身用到的类型）。
退出码=255
源文件=主.cn
借链=是
供给=供给.cn
