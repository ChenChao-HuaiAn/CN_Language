// E2E 122（宿主缺陷回归，2026-09-01）：stdlib 模块独立导入。
//   文件.cn / IO.cn 使用 错误码（定义于 核心.cn 的枚举）但漏 导入 核心::错误码
//   ——此前仅当程序恰好先导入 容器（容器 导入 核心）时符号才侥幸可见；
//   单独 导入 文件::* 的程序直接编译失败（未声明的标识符 '错误码'）。
//   根治：文件.cn / IO.cn 补 导入 核心::错误码（与 容器.cn/映射集合.cn 同款）。
//   本用例刻意不导入 容器，单独使用 文件 模块（打开/关闭）验证独立可编译运行。
导入 文件::打开文件;
导入 文件::关闭文件;

函数 主() -> 整32 {
    结果<空类型*, 整32> 打开 = 打开文件("target/e2e_122_不存在的文件.cn", 1);
    如果 (打开.正常) {
        关闭文件(打开.值);
        返回 1;
    }
    打印("文件模块独立导入编译运行通过");
    返回 0;
}
