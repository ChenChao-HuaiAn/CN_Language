// E2E 139（v2 灰色点⑤根治锚定，2026-09-04 用户裁决·缺陷零容忍立案）：
//   语义错误即中止——v2p 对错误程序：打印诊断后退出码 1、不产 IR/asm（原无条件
//   继续 IR 生成与代码生成，错误产物可被外部编排链接=违背生产编译器纪律，
//   宿主 driver 语义错误即构建失败不产物）。
//   本源码故意含非左值赋值 5 = 3（v2 语义必拒——「赋值目标必须是可写左值」），
//   本用例走 v2 负路径闭环：预期 v2p 编译失败（退出码非 0 且 target/v2asm.asm
//   不产出），中止诊断行由 .expected 固化。**不可**经宿主 sanity（源码即坏）。
函数 主() -> 整32 {
    5 = 3
    返回 0
}
