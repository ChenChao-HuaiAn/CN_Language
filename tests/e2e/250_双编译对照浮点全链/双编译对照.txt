# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   250 浮点语义面全链（算术/混合宽化/比较/负号/转换/参数返回 ABI/静态/循环/NaN）——
#   同一源码两侧各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   91-a 修复面：v2 IR 层浮点分派（二元类型字段浮点优先 + 混合隐式转换 +
#   负号/转换 源类型码 + 调用实参类型码/返回类型码）+ 三后端浮点发射
#   （SSE/NEON 指令 + SysV/AAPCS64 双计数参数寄存器类 + xmm0/d0 浮点返回）；
#   宿主 H1（x86 两后端 NaN 比较非 IEEE）同轮根治——两侧统一 IEEE ordered。
#   浮32 仅转换+打印（宿主浮32 算术缺陷 H2 另案登记，不在面内）。
退出码=255
源文件=主.cn
