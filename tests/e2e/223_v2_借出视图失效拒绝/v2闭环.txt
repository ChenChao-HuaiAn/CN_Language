# v2 闭环负测配置（2026-09-12 第七十七轮 A21 借出视图生命周期）：
#   223_v2 借出视图失效拒绝——与宿主 223 同源；六形态（向量删除/向量设置覆盖/
#   栈弹出/队列出队/链表删除头部/映射删除）在 v2 侧同款拦截（v2 语义检查
#   模块 语义/借出检查.cn：NLL 顺序近似，节点索引序代替源码行序）。
#   退出码=负（v2p 须编译失败：非 0 退出 + 不产出 v2asm.s + 诊断锚行在输出中，
#   主.expected 固化）。
退出码=负
源文件=主.cn
借链=否
供给=
