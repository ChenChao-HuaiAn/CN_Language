函数 可能耗败(整32 模式) -> 结果<整32, 字符串> {
    如果 模式 == 0 {
        返回 正常(42);
    }
    返回 错误("失败原因");
}
函数 主() -> 整32 {
    结果<整32, 字符串> 甲 = 可能耗败(0);
    如果 甲.正常 {
        打印行(甲.值);
    }
    结果<整32, 字符串> 乙 = 可能耗败(1);
    如果 乙.正常 {
        打印行(乙.值);
    } 否则 {
        打印行(乙.错误);
    }
    返回 0;
}
