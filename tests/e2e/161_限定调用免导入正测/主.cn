// plans/018 P6b 工作流2（呈报一B，2026-09-07 用户终裁）：限定调用免导入正测。
//   P1-1「先导入才能限定调用」废止——限定调用 模块::符号 按「模块已加载」
//   放行（Rust 习惯：路径项 m::f() 恒可用，导入 use 只影响不带前缀的名字）。
//   本用例主.cn 零导入语句：①同包模块限定调用（数学::平方）；②父挂子
//   子模块多级路径限定调用（数学::几何::面积）；③未加载模块限定调用仍报
//   错（负覆盖在单测 SemanticQualifiedCallWithoutImportRegression）。
// 模块加载源：主.cn 无导入，数学 经 模块 声明挂载（crate 根成员聚合）
//   由 driver 自动加载；几何 经 数学.cn 模块 声明挂载（父挂子）。
模块 数学;

函数 主() -> 整32 {
    // ① 已加载模块免导入限定调用
    打印("数学::平方(7): ", 数学::平方(7));
    // ② 多级路径（子模块）免导入限定调用
    打印("数学::几何::面积(3,4): ", 数学::几何::面积(3, 4));
    返回 0;
}
