# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十八个对照用例）：
#   260 嵌套结构体字段数组元素释放（两层结构体字段数组元素串/元素内多字段/下标≥1
#   读写/纯标量对照/三层链/函数尾/多元素全释放/字段混排）——同一源码两侧各编各跑，
#   退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   100-a（2026-09-13 C12）门禁：修前双侧同残留 2（元素=含串结构体字段数组的
#   串字段未递归释放——探针 fldmem4 实证）。
退出码=255
源文件=主.cn
