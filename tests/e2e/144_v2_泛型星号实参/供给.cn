导入 容器::向量;

// E2E 144 符号供给（簇④/簇⑥，2026-09-04）：v2 生成代码引用的 向量$整64* 方法
//   符号（追加/元素/大小 mangle 名）由本文件经宿主真实管线编译为 .obj 供给
//   （链接置于 v2p.obj 之前——②b B7 惯例）。v2 组件（v2p_linux.o）自身只实例化
//   标量元素容器（向量$整64 等），指针元素容器 向量<整64*> 未实例化——用例的
//   指针们.追加/元素/大小 符号须供给。
//   供给函数本体=宿主侧 星号泛型全链（簇⑥ 根治后的正路径自证：原名查类命中
//   修复对 向量$整64* 实例方法调用生效）；断言值 101 起。
不安全 函数 供给指针向量() -> 整64 {
    整64 数 = 42;
    向量<整64*> 指针们 = 向量<整64*>();
    结果<空类型, 整32> 加 = 指针们.追加(&数);
    如果 (!加.正常) { 返回 101; }
    如果 (指针们.大小() != 1) { 返回 102; }
    整64* 取出 = 指针们.元素(0);
    如果 (*取出 != 42) { 返回 103; }
    返回 0;
}

函数 主() -> 整32 {
    整64 结果值 = 供给指针向量();
    如果 (结果值 != 0) { 返回 整32(结果值); }
    返回 0;
}
