函数 问候(字符串 名字 = "世界") -> 空类型 {
    打印行("你好，" + 名字);
}
函数 计算(整32 a, 整32 b = 2, 整32 c = 10) -> 整32 {
    返回 a * 100 + b * 10 + c;
}
函数 主() -> 整32 {
    问候();
    问候("张三");
    打印行(计算(1));
    打印行(计算(1, 5));
    打印行(计算(1, 5, 9));
    返回 0;
}
