// E2E 93_类跨模块同名（P2-16）：模块甲 与 模块乙 各定义 类 计数器，模块限定共存
// 语法：类型位置 模块::类（模块甲::计数器 变量）；构造 模块::类()；成员调用 变量.方法()
// 链接：类方法符号带 模块 前缀（模块甲::计数器$获取 与 模块乙::计数器$获取）互不冲突
导入 模块甲
导入 模块乙

函数 主() -> 整32 {
    打印("===== 类跨模块同名（P2-16）=====")
    模块甲::计数器 甲计 = 模块甲::计数器()
    模块乙::计数器 乙计 = 模块乙::计数器()
    整32 甲取 = 甲计.获取()
    整32 乙取 = 乙计.获取()
    打印("甲::计数器.获取(): ", 甲取)   // 10
    打印("乙::计数器.获取(): ", 乙取)   // 20
    打印("===== 完成 =====")
    返回 0
}
