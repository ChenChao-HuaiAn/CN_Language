导入 容器::向量

// E2E 126 符号供给（②b B5/B7，2026-09-02）：v2 生成代码引用 向量$节点 / 向量$词条
//   全方法面符号（构造/追加/设置/元素/大小/删除/析构——含资源字段深拷贝与析构
//   注入）。v2p_linux.o 只含 v2 自身实例化的类型（词条 未用过）——本文件经宿主
//   真实管线编译为 .o 参与链接（置于 v2p_linux.o 之前：用例自有结构体布局权威，
//   不依赖 v2 内部结构形状）。结构体定义与 主.cn 逐字段一致（同符号同布局）。

结构体 节点 {
    整32 类型
    整32 文本
    整64 附加
    整32 子起点
    整32 子终点
}

结构体 词条 {
    整64 编号
    字符串 名字
}

// 实例化 向量<节点> 全方法面（追加/设置/元素 链式读/大小/删除 + RAII 构造析构）
函数 供给节点容器() -> 整64 {
    向量<节点> 池 = 向量<节点>()
    节点 n
    n.类型 = 1
    n.文本 = 2
    n.附加 = 3
    n.子起点 = 4
    n.子终点 = 5
    结果<空类型, 整32> 加 = 池.追加(n)
    如果 (!加.正常) { 返回 101 }
    结果<空类型, 整32> 设 = 池.设置(0, n)
    如果 (!设.正常) { 返回 102 }
    整64 和 = 池.元素(0).类型 + 池.元素(0).附加
    如果 (池.大小() != 1) { 返回 103 }
    结果<空类型, 整32> 删 = 池.删除(0)
    如果 (!删.正常) { 返回 104 }
    如果 (池.大小() != 0) { 返回 105 }
    返回 和
}

// 实例化 向量<词条> 全方法面（含字符串字段：追加深拷贝/元素读/删除收缩/RAII）
函数 供给词条容器() -> 整64 {
    向量<词条> 表 = 向量<词条>()
    词条 t
    t.编号 = 7
    t.名字 = "供给"
    结果<空类型, 整32> 加 = 表.追加(t)
    如果 (!加.正常) { 返回 106 }
    如果 (表.大小() != 1) { 返回 107 }
    如果 (表.元素(0).编号 != 7) { 返回 108 }
    结果<空类型, 整32> 设 = 表.设置(0, t)
    如果 (!设.正常) { 返回 109 }
    结果<空类型, 整32> 删 = 表.删除(0)
    如果 (!删.正常) { 返回 110 }
    返回 0
}

// 宿主编译入口（cn build 需要主函数；链接 muldefs 下 v2asm 在前，cn_main 归 v2 产物）
函数 主() -> 整32 {
    返回 整32(供给节点容器() + 供给词条容器())
}
