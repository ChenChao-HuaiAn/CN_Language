导入 容器::向量;
导入 映射集合::映射;

// 517-a 符号供给（链接期）：强制实例化 向量<映射<整64,整64>> 家族符号
//   （生产态 v2 不发 stdlib 实例符号——与 126 供给同款机制）
函数 供给_容器元素链式(向量<映射<整64, 整64>> 表) -> 整64 {
    返回 表.大小();
}

函数 主() -> 整32 {
    返回 0;
}
