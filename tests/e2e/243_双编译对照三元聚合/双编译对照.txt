# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   243 三元运算符 × 聚合类型（消费位全链）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对；
#  ⑧=返回 (条?借用形参:借用形参) 缺陷①原始形态）。
#   86-a 修复面：宿主（exprSrcType/赋值位/返回判定/memberObjStructType 三元形态）+
#   v2（解析节点类型ID/三元生成地址化/声明位/赋值位/成员偏移/实参回扫 T_对象）。
#   本用例不依赖容器符号（无 借链/供给）——容器插入位=宿主单侧 E2E 245。
退出码=255
源文件=主.cn
