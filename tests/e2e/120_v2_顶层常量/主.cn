导入 常量库;

// E2E 120（v2 自举第三步①：顶层常量声明全链路）：
//   覆盖三种常量引用形态：本文件常量（系数）、跨模块常量（增量，来自
//   常量库.cn）、定义模块内常量（常量和() 内的 基数/增量/负偏移，含负常量）。
//   v2p 编译本入口 + 自动加载 导入模块 常量库.cn（多文件常量表跨文件可见）
//   -> target/v2asm.asm -> ml64/link -> 运行：
//   整32(常量和() + 系数 * 增量) = 38 + 3 * 8 = 62 -> 期望退出码 62
常量 系数 = 3;

函数 主() -> 整32 {
    返回 整32(常量和() + 系数 * 增量);
}
