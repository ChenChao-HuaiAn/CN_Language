公开:

// E2E 120（v2 自举第三步①：顶层常量声明全链路）：
//   常量库.cn 定义三个顶层常量（含负常量），常量和() 在定义模块内引用它们。
//   v2 编译器须将常量标识符内联为 IR_常量整数（负值 mov rax, -10），
//   ml64 汇编通过 -> 链接运行退出码 62（主.cn 计算式见下）。
//   常量和() = 基数 + 增量 + 负偏移 = 40 + 8 - 10 = 38
常量 基数 = 40
常量 增量 = 8
常量 负偏移 = -10

函数 常量和() -> 整64 {
    返回 基数 + 增量 + 负偏移
}
