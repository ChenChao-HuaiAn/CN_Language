// 189：联合体值语义宿主锚定（46-e 观察项宿主侧，2026-09-10 第四十九轮）——
//   规范 02 复合类型：联合体所有字段共享同一内存区域，同一时间只有一个字段
//   有效；大小=最大字段向上对齐（C 布局）。五形态：成员写读往返 / 内存共享
//   （写 整64 读 整32=低 4 字节确定截断）/ 声明位整体赋值 / 赋值位整体拷贝 /
//   值传参（按联合体大小传递）。v2 侧同源用例=189_v2（rc=31 校验和闭环）。
联合体 数值 {
    整32 a;
    整64 b;
}

函数 读甲(数值 v) -> 整32 {
    返回 v.a;
}

函数 主() -> 整32 {
    数值 n;
    n.a = 42;
    打印(n.a);
    n.b = 42;
    打印(n.a);
    n.b = 4408;
    数值 m = n;
    打印(m.b);
    数值 k;
    k = m;
    打印(k.a);
    n.a = 99;
    打印(读甲(n));
    返回 0;
}
