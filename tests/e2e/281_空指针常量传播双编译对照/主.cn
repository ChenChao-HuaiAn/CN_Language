// E2E 281（双编译对照，2026-09-15 第一百八十八轮 188-a D6·plans/023 B11 变量常量传播）：
//   边界正例（零假阳性锚）——判空比较合法 / 非空写入即失格 / 传播边经失格源不判：
//   `整64* p = 无;` → `如果 (p == 无)` 判空合法 → `p = &甲;` 非空写入（p 失格）
//   → `整64* q = p;` 传播边（源失格 → q 不判）→ `*q` 合法。宿主/v2 双侧同值。
//   期望退出码 = 40 + 1 = 41。
不安全 函数 主() -> 整32 {
    整64 甲 = 40;
    整64* p = 无;
    如果 (p == 无) { 甲 = 甲 + 1; }
    p = &甲;
    整64* q = p;
    整64 v = *q;
    返回 整32(v);
}
