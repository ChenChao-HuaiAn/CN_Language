# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   244 嵌套含串结构体的整体拷贝 + 借出豁免的容器类型约束——同一源码两侧各编各跑，
#   退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   86-a 修复面：v2 IR容器.cn 发射串字段深拷 守卫顺序（递归在前）+
#   宿主 ir_fields.cpp / v2 生成语句.cn 豁免容器类型约束（方法类名85 + 是容器类）。
#   本用例不依赖容器符号（无 借链/供给）。
退出码=255
源文件=主.cn
