// E2E 279（宿主负测，2026-09-15 第一百八十八轮 188-a D6·plans/023 B11 变量常量传播）：
//   变量形态常量传播——`整64* p = 无;` 后解引用 `*p` 必须编译失败。
//   B11 原判据=操作数为 `无` 字面量（`*无`）；plans/023 §九 定义含「空指针字面量
//   （无）**或编译期常量** 空类型* 值」（C16 落盘于 plans/001 §5.3）——本形态由
//   D6 落实（Rust deref_nullptr lint 同构：常量传播式空指针解引用=deny-by-default；
//   判定=无字面量种子 + 全函数无其他写入 + 传播闭包 + 取地址/引用实参失格）。
//   期望诊断子串「编译期常量空指针解引用」（期望编译失败.txt）。
不安全 函数 主() -> 整32 {
    整64* p = 无;
    整64 v = *p;
    返回 整32(v);
}
