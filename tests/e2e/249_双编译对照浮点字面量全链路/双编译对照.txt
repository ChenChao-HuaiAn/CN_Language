# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   249 浮点字面量全链路（值装载/相等比较/实参/返回/三元/短路/静态初值/循环）——
#   同一源码两侧各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   89-a 修复面：v2 IR 层主循环 节点_浮点字面量 分派 + 生成字面量 浮点分支
#   （IR_常量浮点）+ 三后端 .double/REAL8 文本直通发射（生成表达式.cn /
#   代码生成共用.cn 浮点文本净+收集浮点常量 / X64Linux / ARM64 / win）。
#   88-b 防线：净栈契约出口校验（生成表达式 出口）——本用例编译期经 v2 侧
#   出口校验（修复前该形态报 `[ir2] 表达式净栈契约违反(多压) 增量=2`）。
#   本用例不依赖容器符号（无 借链/供给）；不含浮点算术/大小比较/打印（v2 既有
#   边界不在面内——面内仅相等/不等：v2 按位模式比较，同值字面量语义与宿主一致）。
退出码=255
源文件=主.cn
