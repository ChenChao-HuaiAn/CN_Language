// E2E 367：友元边界（规格书 5.6 友元）
// 友元函数对私有成员拥有读写双向授权（最小授权面=整个类）：写私有成员
// + 作用于多个实例（授权按类不按对象）；非友元的公开成员函数经公开
// 接口读回锚定。
类 保险箱 {
私有:
    整32 密码
公开:
    函数 保险箱(整32 p) { 密码 = p; }
    函数 读密码() -> 整32 { 返回 密码; }
    友元 函数 改密码(保险箱& 箱, 整32 新密码)
}

函数 改密码(保险箱& 箱, 整32 新密码) -> 空类型 {
    箱.密码 = 新密码;
}

函数 主() -> 整32 {
    保险箱 甲 = 保险箱(111);
    保险箱 乙 = 保险箱(222);
    改密码(甲, 999);
    改密码(乙, 888);
    打印("甲: ", 甲.读密码());
    打印("乙: ", 乙.读密码());
    返回 0;
}
