// 模块丙（Task 6.11 补测：模块导入语法全形式 E2E 依赖模块之三）
// 公开: 函数 减半 —— 依赖 模块乙（被导入模块可再导入其他模块）
// 用途：验证 主.cn 用「导入 模块丙::减半」多级路径形式导入，driver 递归加载
//       依赖（模块丙.cn 内的 导入 模块乙 一并加载），拓扑序 [模块乙, 模块丙, 主]
// 说明：被导入模块内部使用限定调用（模块乙::减）时，自身也必须写导入语句
//       （importedModules_ 仅收集合并到 Program 的导入声明）
导入 模块乙::减;

公开:
函数 减半(整32 n) -> 整32 {
    返回 模块乙::减(n, 1);
}
