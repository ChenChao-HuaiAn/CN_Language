// 模块乙（Task 6.11 补测：模块导入语法全形式 E2E 依赖模块之二）
// 公开: 函数 减/拼接 —— 供 模块丙 依赖（验证 导入 模块丙::子符号 多级路径的
//       依赖链：主.cn 导入 模块丙::子符号 -> 首段 模块丙 -> 模块丙.cn 导入 模块乙
//       -> 拓扑排序 [模块乙, 模块丙, 主]）
// 说明：多级路径（导入 A::B::C）的文件映射仅取首段 A.cn（同目录平铺），
//       后续段在语义层限定调用重写时匹配 模块名::符号
公开:
函数 减(整32 左, 整32 右) -> 整32 {
    返回 左 - 右;
}

函数 拼接(字符串 左, 字符串 右) -> 字符串 {
    返回 左 + 右;
}
