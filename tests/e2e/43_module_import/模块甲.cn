// 模块甲（Task 6.11 补测：模块导入语法全形式 E2E 依赖模块之一）
// 公开: 函数 加/乘/阶乘（函数重载演示）/ 枚举 颜色 / 类 计数器 / 泛型 最大值
// 私有: 函数 秘密平方 —— 验证私有符号不跨模块（E2E 只测公开正向链路）
// 说明：
//   - 顶层不支持 常量 声明（Program 无全局常量存储，规范08-二 的 常量 PI
//     为文档示例，编译器实际不支持）——导入"常量"不在 E2E 覆盖范围
//   - 模块文件即模块：模块名 = 文件名（不含 .cn），同目录平铺解析
公开:
函数 加(整32 左, 整32 右) -> 整32 {
    返回 左 + 右
}

函数 乘(整32 左, 整32 右) -> 整32 {
    返回 左 * 右
}

// 函数重载（同模块内两个 加 签名不同）
函数 加(浮64 左, 浮64 右) -> 浮64 {
    返回 左 + 右
}

// 公开函数体引用私有符号（私有依赖闭包合并，缺陷4 修复验证）
函数 阶乘(整32 n) -> 整32 {
    如果 (n <= 1) {
        返回 1
    }
    返回 n * 阶乘(n - 1)
}

枚举 颜色 {
    红 = 1,
    绿 = 2,
    蓝 = 3
}

类 计数器 {
    公开:
    函数 计数器() -> 空类型 {
        自身.数值 = 0
    }

    函数 计数器(整32 初始值) -> 空类型 {
        自身.数值 = 初始值
    }

    函数 增加() -> 整32 {
        自身.数值 = 自身.数值 + 1
        返回 自身.数值
    }

    函数 当前值() -> 整32 {
        返回 自身.数值
    }

    私有:
    整32 数值
}

泛型 <类型 T>
函数 最大值(T 左, T 右) -> T {
    如果 (左 > 右) {
        返回 左
    }
    返回 右
}

私有:
函数 秘密平方(整32 n) -> 整32 {
    返回 n * n
}
