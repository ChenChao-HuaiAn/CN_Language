# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第三十个对照用例）：
#   262 多文件静态初值注入（非入口模块 容器/字符串/标量静态 + 入口模块对照）——
#   同一源码两侧各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   借链=是（v2 生成代码调用宿主编译的容器方法符号——v2p.obj 供给）。
#   102-a（2026-09-13 C4）门禁：宿主修前「类/容器静态有初始化表达式」路径缺 StorePtr
#   （对象已构造但指针未入 .data 槽 → 形①崩「运行时错误(错误码3)」）。
退出码=255
源文件=主.cn
借链=是
供给=
