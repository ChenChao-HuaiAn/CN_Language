// 非入口模块（262 用例）：容器/字符串/标量静态（初值需运行期求值/构造）
公开:
静态 向量<整64> 表 = 向量<整64>();
静态 字符串 库名 = 字符串复制("库串名");
静态 整64 库计数 = 42;

函数 取大小() -> 整64 {
    返回 表.大小();
}
函数 追加值(整64 值) -> 空类型 {
    结果<空类型, 整32> 加 = 表.追加(值);
    如果 (!加.正常) { 运行时错误(4); }
}
函数 取名() -> 字符串 {
    返回 字符串复制(库名);
}
函数 取计数() -> 整64 {
    返回 库计数;
}
