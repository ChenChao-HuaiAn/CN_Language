// 主入口（第 7 层：51_visibility_intersection 可见性交集合法路径 E2E，规格书08-四）
// v2.0 可见性交集矩阵——E2E 只测合法路径（矩阵 ✅ 象限）：
//   模块公开 × 类内公开 → 跨文件可访问：
//     导入 内部::可见类 后实例化，调用 类内公开 方法 方法()（读取私有字段 标记）
// 错误路径（矩阵 ❌ 象限：模块公开×类内私有、模块私有×类内公开、
//   模块私有×类内私有 跨文件不可见/不可访问）由单测
//   VisibilityIntersectionXxx 系列覆盖（run_e2e.py 不支持预期编译错误）。

导入 内部::可见类

函数 主() -> 整32 {
    打印("===== 可见性交集（第 7 层，合法路径）=====")

    // ---- 模块公开类 + 类内公开成员：跨文件可实例化、可调用 ----
    可见类 实例 = 可见类()
    整64 值 = 实例.方法()
    打印("可见类.方法(): ", 值)                  // 42（公开方法读取私有字段）

    // ---- 再次验证多实例 ----
    可见类 实例2 = 可见类()
    打印("实例2.方法(): ", 实例2.方法())         // 42

    打印("===== 可见性交集测试完成 =====")
    返回 0
}
