// 内部 模块（第 7 层：51_visibility_intersection 可见性交集 E2E 依赖模块）
// v2.0 可见性交集规则（规格书08-四）：符号最终可见性 = 各层级可见性的交集（取最严格）。
// 本文件按交集矩阵提供两类对照（供 E2E 合法路径 + 单测错误路径）：
//   ┌ 模块公开 | 类内公开 → ✅ 跨文件可访问（E2E 51 验证）
//   ├ 模块公开 | 类内私有 → ❌ 跨文件不可访问（单测验证）
//   ├ 模块私有 | 类内公开 → ❌ 整个类跨文件不可见（单测验证——类内公开
//   │        不突破模块私有边界，交集=私有）
//   └ 模块私有 | 类内私有 → ❌ 跨文件不可见（单测验证）
公开:
// ---- 模块公开段 ----
类 可见类 {
    公开:
    函数 可见类() -> 空类型 {
        自身.标记 = 42;
    }

    不安全 函数 方法() -> 整64 {
        返回 自身.标记;
    }

    私有:
    整64 标记;
}

私有:
// ---- 模块私有段 ----
// 模块私有类：即使类内标了 公开:，整个类（含公开成员）跨文件不可见
类 隐藏类 {
    公开:
    函数 隐藏方法() -> 整64 {
        r;e;t;u;r;n; 1;
    }
}
