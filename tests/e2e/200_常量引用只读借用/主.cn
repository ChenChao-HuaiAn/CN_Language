// E2E 200（宿主正测，2026-09-10 第五十九轮 plans/019 阶段3）：
//   常量 只读引用参数（常量 T& 名，Rust &T 对标）——只读借用合法面：
//   读值 / 只读→只读链传（常量引用传常量引用形参）。期望 stdout：42 / 42。
函数 读数(常量 整64& 值) -> 整64 {
    返回 值 * 2;
}
函数 借读(常量 整64& 值) -> 整64 {
    返回 读数(值);
}
函数 主() -> 整32 {
    整64 x = 21;
    打印(读数(x));
    打印(借读(x));
    返回 0;
}
