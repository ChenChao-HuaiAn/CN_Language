// E2E 131_非左值赋值拒绝（缺陷②根治，用户裁决 2026-09-03：②立案+③顺带+①不动）
// 背景：宿主语义层 visitAssignmentExpr 的 else 分支只查类型不查左值性——
//   a + 1 = 7 / a + 1 += 2 / 5 = a 检查通过（退出码 0），存储被静默丢弃；
//   常量重赋值 c = 2 / 自增非左值 (a+1)++ 同族静默通过。
// 权威锚定：Rust E0070（invalid left-hand side of assignment，编译期硬错误）；
//   CN 规范声明赋值运算符与 C/C++ 一致（01b §5.5/优先级），C/C++ 同为编译错误。
// 本用例：预期编译失败（负 e2e 通道），期望诊断含「赋值目标必须是可赋值的左值」。
// 注：b = a 换行 *a = 7 为换行≡空格粘连形态（规范行为）——粘连成 b = ((a*a)=7)，
//   内层非左值赋值被本检查拦截并附「想写分号」提示（诊断③）。
函数 主() -> 整32 {
    变量 a = 3;
    变量 b = 0;
    常量 c = 1;
    a + 1 = 7;            // 二元运算结果不可赋值
    a + 1 += 2;           // 复合赋值目标同为非左值
    5 = a                // 字面量不可赋值
    (a + 1)++;            // 自增/自减目标须为左值
    c = 2;                // 常量初始化后不可修改
    c++;                  // 常量不可自增
    a * a = 7;            // 语句歧义形态（声明路径解析，另报未知类型类错误）
    b = a
    *a = 7;               // 与上一行粘连（换行≡空格）：b = ((a*a) = 7)
    返回 0;
}
