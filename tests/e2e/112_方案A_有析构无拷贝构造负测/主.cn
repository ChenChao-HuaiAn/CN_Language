// 方案A 强制规则负测（2026-08-25）：有析构类按值拷贝（初始化/赋值）无拷贝构造
//   须编译报错——浅拷贝裸指针字段析构双释放 0xC0000374。
类 资源 {
公开:
    整64* 数据
    函数 资源() {
        数据 = 无
    }
    函数 ~资源() {
        如果 (数据 != 无) {
            释放(数据)
        }
    }
}

函数 主() -> 整32 {
    资源 甲 = 资源()
    资源 乙 = 甲
    返回 0
}
