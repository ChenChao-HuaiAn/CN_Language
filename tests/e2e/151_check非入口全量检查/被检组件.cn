// E2E 151：check 命令对非主.cn 入口文件必须全量检查（簇⑥ 假绿根治验证）
//
// 场景（簇⑥ 现场）：开发期组件自检姿势——文件名非 主.cn、含 导入（依赖使
// 模块图 > 1）、函数在 私有: 标签段（真私有，默认私有同效）。
// 修复前：命令行入口被误判为「被导入模块」，私有函数体被模块可见性过滤
//   静默丢弃 -> 语义检查没发生 -> 「检查通过」假绿。
// 修复后：命令行显式入口全量合并（isEntryUnit），私有函数体 私有含错 内的
//   未声明标识符必须被 check 报出（期望check失败.txt 断言）。
//
// 注意：可见性标签为粘性作用域（规范 08）——私有函数必须显式位于 私有:
// 段（或无任何标签段），写在 公开: 段之后即公开、不触发簇⑥。
导入 本地依赖;

公开:
函数 公开计算() -> 整32 {
    返回 甲() * 2;
}

私有:
函数 私有含错() -> 整32 {
    整32 x = 未声明变量乙;
    返回 x;
}
