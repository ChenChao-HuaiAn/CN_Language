// 394_双编译stdlib随机数兜底（276-a D29①验收）：
//   纯 stdlib 依赖形态——用例目录不含 随机数.cn，宿主/v2 两侧均须走
//   「stdlib 兜底」加载 stdlib/随机数.cn（宿主 driver 候选4 既有；
//   v2p 276-a 新增同款候选4）。断言面全为确定性（同种子序列一致+范围界内）。
导入 随机数::随机数生成器;
函数 主() -> 整32 {
    随机数生成器 甲 = 随机数生成器(42);
    随机数生成器 乙 = 随机数生成器(42);
    整64 a1 = 甲.下一个();
    整64 b1 = 乙.下一个();
    打印(a1 == b1);
    整64 c = 甲.下一个范围(100);
    打印(c < 100);
    打印(乙.下一个范围(50) < 50);
    返回 0;
}
