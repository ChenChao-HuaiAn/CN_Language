# v2 闭环用例配置（数据驱动——存在本文件即走 v2 自举链：宿主构建 v2p
#   -> v2p 编译用例 -> as -> g++ -> 运行 -> 退出码比对；2026-09-11
#   用户裁决去硬编码：本文件取代 runner 内注册表，新增 v2 用例零代码改动）
#   退出码=511=九形态校验和逻辑值（累计31+累计2 15*32）；POSIX 8 位截断后=255，
#   win 为 32 位全值（runner 按平台分派：Linux %256 / win 全值）——两平台统一口径
退出码=511
源文件=主.cn
借链=否
供给=
