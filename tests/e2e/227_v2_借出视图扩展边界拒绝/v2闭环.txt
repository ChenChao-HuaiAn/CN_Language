# v2 闭环负测配置（2026-09-12 第七十八轮更新：v2 类支持落地后的期望同步）：
#   227_v2 借出视图扩展边界拒绝——与宿主 227 同源；**两条边界 v2 侧现均真拦截**：
#   边界① 调用点同源互斥（实参末位表定位 + 借出登记键比对）；
#   边界② 成员链接收者（`架.表`——v2 类支持落地后字段接收者类型可推导，
#   借出检查 查声明类型 类字段回退 + 设置借出检查类 上下文）。
#   历史（第七十七轮）：v2 类支持缺口期，形态二报「未声明的类型: 货架」等缺口
#   诊断；本轮缺口销项后期望同步为两条真拦截（用例价值提升——从缺口固化到
#   双向防线固化）。退出码=负（同 223_v2）。
退出码=负
源文件=主.cn
借链=否
供给=
