// E2E 198_v2（v2 负路径闭环，2026-09-10 第五十八轮 plans/019 阶段2）：
//   引用/指针逃逸检查 v2 侧——三负形态（适配 v2 第一版检查面）：①静态变量
//   持局部地址（赋值位）②指针返回直接 &局部（缺陷② -> 整64* 返回类型解析
//   修复后的新能力面）③引用返回 &局部。v2 语义诊断+中止（195_v2 同款）。
静态 整64* 槽 = 无;
不安全 函数 坏1() -> 整32 {
    整64 x = 5;
    槽 = &x;
    返回 0;
}
函数 坏2() -> 整64* {
    整64 x = 5;
    返回 &x;
}
函数 主() -> 整32 {
    坏1();
    返回 0;
}
