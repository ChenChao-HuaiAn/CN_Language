# CN语言 E2E 测试框架

E2E（端到端）测试用于验证CN语言编译器的完整链路：**编译源文件 → 链接生成可执行文件 → 运行 → 比对输出**。

遵循"E2E先行"原则：每个功能必须先写E2E测试，测试通过后才算完成。

---

## 一、目录结构

```
tests/e2e/
├── run_e2e.py                  # E2E测试运行器（Python 3）
├── README.md                   # 本文档
├── 01_hello/                   # 第一个用例：Hello World
│   ├── hello.cn                # CN语言源文件
│   └── hello.expected          # 预期输出（逐行比对）
├── 02_xxx/                     # 后续用例（阶段一之后逐步添加）
│   ├── xxx.cn
│   └── xxx.expected
└── ...
```

### 用例目录命名约定

- 目录名：`<编号>_<名称>`，编号为两位数字（如 `01_hello`、`02_变量声明`）
- 运行器只识别目录名以两位数字开头的目录作为用例
- 源文件名：`<用例>.cn`（目录内第一个 `.cn` 文件即源码）
- 期望文件名：与源文件**同名**的 `.expected` 文件（`hello.cn` ↔ `hello.expected`）

---

## 二、如何添加新测试

1. 在 `tests/e2e/` 下创建目录 `NN_名称`（NN为下一个可用编号）
2. 编写CN语言源文件 `tests/e2e/NN_名称/名称.cn`：

   ```cn
   // 示例：两数相加
   函数 主() -> 整32 {
       整32 a = 1
       整32 b = 2
       打印行(a + b)
       返回 0
   }
   ```

3. 创建同名期望输出文件 `名称.expected`，内容为程序运行后的预期输出（UTF-8编码）：

   ```
   3
   ```

4. 运行验证（见下节）。

> 注意：`hello.expected` 最后一行应有换行；比对时逐行比较并忽略行尾空白差异。

---

## 三、如何运行测试

### 直接运行（自动探测编译器）

```bash
python3 tests/e2e/run_e2e.py
```

自动按顺序探测：`target/Debug/cn.exe` → `target/Release/cn.exe` → `target/cn.exe`

### 指定编译器路径

```bash
python3 tests/e2e/run_e2e.py --cn target/Debug/cn.exe
```

### 常用参数

| 参数 | 说明 |
|------|------|
| `--cn <路径>` | 指定编译器可执行文件路径 |
| `--verbose` / `-v` | 详细输出（显示编译/运行命令） |
| `--filter <模式>` | 仅运行目录名包含指定模式的用例（如 `--filter 01_hello`） |
| `--target-dir <目录>` | 可执行文件输出目录（默认 `target/`） |
| `--strict` | 将"未实现"用例视为失败（阶段一完成后全量验证用） |

### 通过CMake运行

```bash
cmake --build target/build --target e2e     # 自定义目标（自动携带编译器路径）
ctest --test-dir target/build -R e2e        # CTest集成
```

---

## 四、输出与状态说明

每个用例输出三种状态之一（彩色文本：PASS绿 / FAIL红 / SKIP黄）：

| 状态 | 含义 | 退出码影响 |
|------|------|-----------|
| `PASS` | 编译→运行→输出比对全部通过 | 无 |
| `FAIL` | 编译失败 / 运行失败 / 输出不一致 / 用例结构错误 | 最终返回1 |
| `SKIP 未实现` | 编译器返回"尚未实现"（阶段零预期状态，框架本身可用） | 默认不计数，`--strict` 时返回1 |

退出码约定：

- `0`：全部通过（或仅存在"未实现"用例且未启用 `--strict`）
- `1`：存在失败用例（或 `--strict` 下有未实现用例）
- `2`：参数错误（如指定的编译器不存在）

> **阶段零说明**：当前编译器尚未实现 `build` 命令，Hello World 用例会显示
> `SKIP 未实现`。这是**预期状态**——用例将在阶段一（Task 1.9）完成后转为 `PASS`。
> E2E框架本身（用例发现、编译调用、运行比对、退出码）从阶段零起即可用。

---

## 五、测试原则

- **E2E先行**：每个功能必须先写E2E测试，测试通过后才算完成
- **禁止跳过**：所有测试必须真正运行，不使用Mock
- **真实配置**：测试使用真实编译器与真实可执行文件
- **编译产物**：可执行文件统一输出到 `target/` 目录，不污染源码目录

---

## 六、自举相关用例（70~79）

| 用例 | 说明 |
|------|------|
| 70/71 | CN 版词法分析器（分词逐行比对） |
| 72 | CN 版语法分析器（AST dump 逐行比对） |
| 73 | CN 版语义分析器（符号/诊断行比对） |
| 74 | CN 版 IR 生成器（双参数 `IR生成(源码, 模块名)`，IR 行比对） |
| 75 | CN 版代码生成器（真实 x64 MASM 后端，IR 全文 + ASM 特征摘要比对） |
| 76 | 两阶段自举验证（CN 组件链编译自身源码，各阶段行数确定性比对） |
| 77 | 审计转储（audit dump，补 expected） |
| 78 | 链构建（CN 组件链编译 5 组件 -> 落盘 5 个 `*_链.asm`，输出 `完成:模块名`） |
| 79 | **真实闭环**（仅 win-x64）：C++ 版编译 79 主.cn -> 运行落盘（第一次）-> ml64 汇编 -> 链接（链.obj 在前 + 入口 obj + 运行时 .obj）-> `cn_compiler_self.exe` -> 再次落盘（第二次）-> `fc /b` 逐字节比对一致 -> 符号自检（map 确认链.obj 贡献符号 + cn_main 来自入口 obj） |

> 79 闭环用例依赖 ml64/link（VS 2022 MSVC 工具链），run_e2e.py 内置动态探测
> （覆盖 14.38/14.44 + Windows Kits 最新版）；非 win-x64 平台返回「失败」并说明原因。
