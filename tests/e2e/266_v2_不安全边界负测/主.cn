// E2E 266_v2（v2 负测，2026-09-17 第一百五十七轮 157-a）：安全区边界硬错误
//   ——安全函数内裸操作=v2 语义错误中止（收口后观察期警告计入错误数；
//   plans/023 §十 步骤 5）。覆盖 B6/B7/A1/B10（A5 释放=v2 内置未支持，
//   面外登记——plans/023 §八）。
静态 整64 计数 = 0;
函数 主() -> 整32 {
    整64 x = 5;
    整64* p = &x;
    整64 v = *p;           // B6 裸指针解引用读
    *p = 7;                // B7 裸指针解引用写
    p = p + 1;             // A1 指针算术
    计数 = 计数 + 1;       // B10 静态变量写
    打印(v);
    返回 0;
}
