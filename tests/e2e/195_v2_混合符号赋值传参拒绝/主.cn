// E2E 195（宿主负测，2026-09-10 第五十六轮 55-c 方案A 用户裁决·Rust E0308 对齐）：
//   赋值初始化/函数传参/返回面的「变量间」混合符号隐式转换编译期拒绝（§3.7 条文
//   扩展——第五十一轮只拦二元运算面，本用例为赋值/传参/返回面补拦；字面量豁免
//   与二元面同构：正32 小 = 4000000000 等字面量初始化形态保留）。四形态：声明
//   初始化（正32 变量→整64）/赋值（正32 变量→整64 左值）/传参（整32 变量→
//   正32 形参）/返回（正32 变量→整64 返回类型）。本用例预期编译失败
//   （期望编译失败.txt 固化诊断子串）。v2 侧同源用例=195_v2（v2 本轮落地
//   声明/赋值/返回三面，传参面随实参边界记录另轮——诚实边界）。
函数 读值(正32 源) -> 整32 {
    返回 整32(源);
}
函数 取值() -> 整64 {
    正32 小 = 4000000000;
    返回 小;
}
函数 主() -> 整32 {
    正32 小 = 4000000000;
    整64 负 = -1;
    整64 甲 = 小;
    负 = 小;
    整32 值 = 100;
    读值(值);
    返回 0;
}
