// E2E 98_方法作值（P3-23）：静态方法（类名.方法）可用作函数指针值回调
// 顶层函数作值（既有）+ 静态方法作值（新增）；实例方法作值需绑定 this（待后续）。
函数 乘二(整32 x) -> 整32 { 返回 x * 2 }
函数 应用(整32(*f)(整32), 整32 v) -> 整32 { 返回 f(v) }

类 计算器 {
公开:
    静态 函数 双倍静态(整32 x) -> 整32 { 返回 x * 2 }
}

函数 主() -> 整32 {
    打印("===== 方法作值（P3-23）=====")
    整32 顶层 = 应用(乘二, 3)
    打印("顶层函数: ", 顶层)                      // 6
    整32 静值 = 应用(计算器::双倍静态, 5)
    打印("静态方法: ", 静值)                      // 10
    打印("===== 完成 =====")
    返回 0
}
