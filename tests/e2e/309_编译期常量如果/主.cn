// 309：编译期内建常量 调试模式（规格书 3.8，239-a）——普通 如果 语义：
//   两分支都经过完整语义检查（与条件编译的本质差异）；未选分支由
//   IR 常量条件直取消除（asm 互斥包含分支常量——收工回填手工实测：默认配置
//   asm 仅含调试分支字符串、--release 配置 asm 仅含发布分支字符串，rc=0 双向正确）。
// 本用例固化默认（调试构建）配置输出。
函数 主() -> 整32 {
    如果 (调试模式) {
        打印行("调试分支运行");
    } 否则 {
        打印行("发布分支运行");
    }
    如果 (调试模式 == 真) {
        打印行("显式比较真: 命中");
    }
    返回 0;
}
