// E2E 270（宿主负测，2026-09-14 第一百六十一轮 161-a A8 冒号后置类型标注移除）：
//   顶层常量冒号后置标注 常量 MAX: 整32 = 100; 必须编译失败——
//   类型前置为唯一规范形态（spec 03；A8 收口移除兼容分支）。
//   期望诊断子串「预期顶层声明，实际为 ':'」（期望编译失败.txt）。
常量 MAX: 整32 = 100;
函数 主() -> 整32 {
    返回 MAX - 100;
}
