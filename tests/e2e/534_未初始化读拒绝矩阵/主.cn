// 010（def-init 负测矩阵）：未初始化读=编译期拒绝（宿主+v2p 双侧同拒）
// 形态：①裸标量读 ②数组未初始化元素读 ③部分分支赋值后读 ④循环位无初值读。
// 诊断锚=「未初始化」（双侧文案同构）。
函数 形态一() -> 整32 {
    整32 x;
    返回 x;
}
函数 形态二() -> 整32 {
    整32[3] a;
    返回 a[1];
}
函数 形态三() -> 整32 {
    整32 x;
    如果 (1 == 1) { x = 1; } 否则 { }
    返回 x;
}
函数 形态四() -> 整32 {
    循环 (整32 j; j < 3; j++) { }
    返回 0;
}
函数 主() -> 整32 {
    返回 形态一();
}
