# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   241 成员链嵌套深度（三级及以上，84-d）——同一源码两侧各编各跑，退出码须一致。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   修复前**宿主**实测 ①②③④⑦⑧ 位丢失（三级链字段读降级常量 0=静默错行为）；
#   任一侧退回「一层推导」→ 该校验位丢失 → 退出码变化 → 对照失败。
#   供给=供给.cn：深A/B/C + 串A/B/C 实例化符号。
退出码=255
源文件=主.cn
借链=是
供给=供给.cn
