// E2E 135_箭头成员访问拒绝（v2.1，2026-09-03 用户裁决：硬错误+迁移提示）
// 规范：-> 成员访问形式废除（成员访问统一 .，指针自动解引用一级）。
//   期望编译失败且诊断含「成员访问已废除」迁移提示（"可能想写"提示家族）。
//   注意：-> 记号本身仍用于函数返回类型（-> 整32），仅后缀成员访问位非法。
结构体 点 {
    整32 x
    整32 y
}
函数 主() -> 整32 {
    点 p = 点{ x = 10, y = 20 }
    点* q = &p
    整32 v = q->x
    返回 v
}
