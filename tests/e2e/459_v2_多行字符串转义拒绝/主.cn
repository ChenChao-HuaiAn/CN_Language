// E2E 401（282-a T5 多行字符串余项·宿主多行段三态校验锚定）：
//   多行字符串（三引号）体内 \u{D800} 代理区码点——宿主词法多行段
//   结构感知+三态拒绝（对齐普通段 278-a 同款；原宽松收集不识别结构）。
//   本源码故意含代理区转义，预期编译失败（诊断=代理区码点不可直接使用）。
函数 主() -> 整32 {
    字符串 文本 = 多行"""
甲\u{D800}乙
""";
    打印(文本);
    返回 0;
}
