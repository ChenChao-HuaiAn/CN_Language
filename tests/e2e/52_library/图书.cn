// 图书 模块（模块系统 v2.0 第 8 层：52_library 图书管理系统依赖模块之一）
// v2.0 特性覆盖：
//   ① 默认私有（规格书08-四）：文件顶部显式 公开: 导出 API；无标签段默认私有
//   ② 可见性交集（⑪）：模块公开类 × 类内公开方法 → 跨文件可实例化/可调用
//   ③ 类内默认私有（⑩）：私有: 字段 + 公开: 方法（类内无标签成员默认私有）
//   ④ 默认私有辅助函数：创建图书 公开函数体内调用 校验编号（私有依赖闭包
//      合并——缺陷4 修复：私有不跨模块但随公开函数带入编译）
公开:
// ---- 可见性交集：模块公开类 × 类内公开方法（主.cn 跨文件可访问）----
类 图书 {
    私有:
    字符串 书名;
    浮64 价格;
    字符串 ISBN编号;

    公开:
    函数 图书(字符串 名, 浮64 价, 字符串 编号) -> 空类型 {
        书名 = 名;
        价格 = 价;
        ISBN编号 = 编号;
    }

    函数 读取书名() -> 字符串 {
        返回 书名;
    }

    函数 读取价格() -> 浮64 {
        返回 价格;
    }

    函数 读取ISBN() -> 字符串 {
        返回 ISBN编号;
    }
}

// 模块公开函数：跨模块可调（图书 工厂，规格书08-四）
函数 创建图书(字符串 名, 浮64 价, 字符串 编号) -> 图书 {
    图书 新书 = 图书(名, 价, 校验编号(编号));
    返回 新书;
}

// ---- 无标签段：默认私有（v2.0 变更，仅本模块可见）----
// 校验编号 为默认私有辅助函数：同文件公开函数体内调用（闭包合并带入）
函数 校验编号(字符串 编号) -> 字符串 {
    返回 编号;
}
