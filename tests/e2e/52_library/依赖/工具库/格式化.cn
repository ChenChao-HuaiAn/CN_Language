// 工具库::格式化 模块（依赖包 工具库 的格式化模块，规格书09-三）
// v2.0 特性覆盖：
//   ⑨ prelude：格式化（sprintf 风格）+ 整数转字符串（字符串 API 族）无需导入
//   ⑫ crate 隔离：本模块 版本() 与 主.cn 版本() 同名同签名，跨 crate 独立命名空间
//     （工具库 经 包.cn 再导出；模块名=文件名主干 格式化，跨 crate 按名匹配）
//   %.2f 精度：__cn_format 基于 vsnprintf，标准精度语法天然支持
公开:
函数 价格(浮64 金额) -> 字符串 {
    返回 格式化("¥%.2f", 金额);
}

函数 ISBN(字符串 编号) -> 字符串 {
    返回 "ISBN: " + 编号;
}

// crate 隔离验证：与 主.cn 版本() 同名同签名，各自独立命名空间
函数 版本() -> 整64 {
    返回 200;
}
