# 货舱.toml — 图书管理系统包元数据与依赖（模块系统 v2.0 第 8 层，规格书09）
# v2.0 特性 ⑧：货舱.toml 依赖声明 + 自动发现（编译入口同目录即自动加载）
[货舱]
名称 = "图书管理系统"
版本 = "1.0.0"

[依赖]
核心 = "内置"        # 内置依赖：编译器 stdlib 目录（prelude 来源）
工具库 = "本地"      # 本地依赖：依赖/工具库/ 子目录（库 crate 根 包.cn）
