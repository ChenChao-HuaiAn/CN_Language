// E2E 199（宿主正测，2026-09-10 第五十八轮 缺陷①方案A 落地载体）：
//   推断声明（变量）+字符串标识符拷贝的打印分派修复——原缺陷：变量 乙 = 甲;
//   打印(乙) 打出指针值（推断类型未传 IR 打印分派，46-c 结构体回填同族）；
//   方案A（用户裁决）=类型回填扩展至字符串（布尔维持现状——184 行为契约
//   不动）。期望 stdout：左 / 左（链式拷贝再显式拷贝）。
函数 主() -> 整32 {
    变量 甲 = "左";
    变量 乙 = 甲;
    打印(乙);
    字符串 丙 = 乙;
    打印(丙);
    返回 0;
}
