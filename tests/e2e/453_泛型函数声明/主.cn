// 453 泛型函数声明形态（417-a D42 波 3 前置件①防线·双编译对照）
// 覆盖：泛型 <类型 T> 函数 名(参数...) 声明解析（规范 06-十三 函数分支·
//       stdlib 容器/映射集合 线性表辅助同款形态）+ 泛型类并存
// 边界（诚实边界·随轮登记）：显式实例化调用（名<实参>(...)）的单态化生成
//       与调用点解析=波 3 主件（418+ 轮）——本用例锁定声明形态不回归
泛型 <类型 T>
类 盒子 {
私有:
    T 数据;
公开:
    不安全 函数 盒子() {
        数据 = 0;
    }
    函数 放(T 值) {
        数据 = 值;
    }
    常量 函数 取() -> T {
        返回 数据;
    }
}

泛型 <类型 K>
函数 查找位置(K* 数组, 整64 长度, K 目标) -> 整64 {
    整64 索引 = 0;
    当 (索引 < 长度) {
        如果 (数组[索引] == 目标) {
            返回 索引;
        }
        索引++;
    }
    返回 0 - 1;
}

函数 主() -> 整32 {
    盒子<整32> 盒;
    盒.放(53);
    返回 盒.取();
}
