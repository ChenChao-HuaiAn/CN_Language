// E2E 192（宿主负测，2026-09-10 方案A 用户裁决·混合符号二元运算拒绝）：
//   规范 plans/001 §3.7 新增条文（Rust 对齐）——有符号（整N）与无符号（正M）
//   的「变量间」比较/算术/位运算编译期拒绝，须显式 类型名(表达式) 构造转换。
//   根因实证（target/p50x64l/mixed_cmp 探针）：隐式宽化在混合符号下静默改变
//   值语义——正64(2^63) vs 整64(-1) 比较值域反转（宿主/v2 同错）；正32 vs
//   整64 负值形态宿主 rc=9 / v2 rc=11 / 数学真值 13 双侧行为分叉。
//   本用例预期编译失败（期望编译失败.txt 固化诊断子串）。
函数 主() -> 整32 {
    正32 小 = 4000000000;
    整64 负 = -1;
    正64 大 = 9223372036854775808;
    // 比较族：正32 变量 vs 整64 变量（两个方向）
    如果 (小 < 负) { 返回 1; }
    如果 (负 > 小) { 返回 2; }
    // 比较族：整64 变量 vs 正64 变量（同秩跨符号，值域反转形态）
    如果 (负 < 大) { 返回 3; }
    // 算术族：正32 变量 + 整64 变量
    返回 整32(小 + 负);
}
