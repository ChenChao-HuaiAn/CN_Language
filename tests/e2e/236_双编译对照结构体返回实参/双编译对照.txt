# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   236 结构体返回值的存放独立性（v2 帧尾共享 retbuf 缺陷回归锚，82-a 修复门禁）——
#   同一源码两侧各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   ② = `取两号(造盒(1),造盒(2))`（两个返回值同时活跃）为**核心回归锚**：v2 修复前
#   实测 0（共享 retbuf 被第二次返回覆盖=数据损坏）；任一侧退回共享缓冲 → 该校验位
#   丢失 → 退出码变化 → 对照失败。
#   本用例不含容器：无供给、不需要借链（纯 retbuf/结构体返回面）。
退出码=255
源文件=主.cn
借链=否
供给=
