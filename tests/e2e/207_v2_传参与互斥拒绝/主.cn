// E2E 207_v2（v2 负路径闭环，2026-09-10 第六十五轮 plans/019 阶段3b 残余）：
//   调用借用纪律 v2 对齐——①传可变拒（常量引用参数借出为可变引用）②同调用
//   可变×只读互斥（实参末位表+函数参数借用位表载体）。v2 语义诊断+中止。
函数 改(整64& 目标) -> 空类型 {
    目标 = 9;
}
函数 混(整64& 可写位, 常量 整64& 只读位) -> 空类型 {
}
函数 坏1(常量 整64& 值) -> 整32 {
    改(值);
    返回 0;
}
函数 坏2(整64& 甲) -> 整32 {
    混(甲, 甲);
    返回 0;
}
函数 主() -> 整32 {
    返回 0;
}
