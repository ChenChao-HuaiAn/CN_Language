// 455 泛型函数单态化（420-a D42 波 3 防线·双编译对照）
// 覆盖：泛型 <类型 T> 函数 声明/显式实例化调用 名<实参>(...)/T* 指针参数
//       下标读/参数传递（数组退化指针+标量）/克隆体单态化生成（符号含实例参数）
泛型 <类型 T>
函数 首个(T* 数组, 整64 长度) -> T {
    返回 数组[0];
}

函数 主() -> 整32 {
    整32[3] 数据;
    数据[0] = 5;
    数据[1] = 6;
    数据[2] = 7;
    返回 整32(首个<整32>(数据, 3));
}
