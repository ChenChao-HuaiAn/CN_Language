# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十个对照用例）：
#   251 浮32 语义面全链（算术四则+复合赋值/混合宽化/比较+负号/转换四向/
#   参数返回 ABI/结构体字段/自增自减/大整数混合精度）——同一源码两侧各编各跑，
#   退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   92-a（2026-09-13）宿主浮32 面缺陷族根治（H2）门禁：修前本用例宿主侧形态
#   ①③失败（f32 op f32 走 f64 指令算位模式）、⑤失败（实参无条件提升 f64）、
#   ⑥编译失败（LoadPtr 宽度前缀 qword 配 movss）、⑦失败（自增按 ptr 整型）；
#   v2 侧形态②⑧失败（整侧窄化到 f32）、⑦失败（自增恒 T_整64）。
退出码=255
源文件=主.cn
