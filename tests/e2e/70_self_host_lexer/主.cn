// E2E 70_self_host_lexer：自举 Task 7.1——CN 词法分析器（2026-08）
// 用 CN 语言重写词法分析器（自举第一步）：标识符/61 关键字/整数（十进制
//   十六进制/二进制/八进制+后缀）/浮点/字符串（含转义）/字符/行注释/块注释/
//   39 运算符（多字符贪婪匹配）/分隔符/行号跟踪。
// 验证：对固定 CN 源码分词，输出 "类型|文本|行" 逐行比对。
导入 容器::向量
导入 CN语言编译器::词法分析::分词

函数 主() -> 整32 {
    字符串 源码 = "// 注释：行注释\n函数 主() -> 整32 {\n    整64 数量 = 42\n    浮64 比率 = 3.14\n    字符串 名 = \"你好\\n世界\"\n    字符 字 = '中'\n    布尔 标志 = 真\n    如果 (数量 >= 42 && 标志) {\n        数量++\n    } 否则 {\n        数量--\n    }\n    返回 0\n}\n/* 块注释\n   跨行 */\n"
    向量<字符串> 输出 = 分词(源码)
    整64 i = 0
    当 (i < 输出.大小()) {
        打印(输出.元素(i))
        i++
    }
    返回 0
}
