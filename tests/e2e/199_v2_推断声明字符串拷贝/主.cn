// E2E 199_v2（v2 正路径闭环，2026-09-10 第五十八轮）：
//   缺陷②载体——v2 语法层 -> 整64* 指针返回类型（修复前 [parse] 缺分号）：
//   返回 &引用参数（指向调用方存储=合法非逃逸+返回逃逸检查的引用参数
//   豁免分支锚定）+ 解引用读写回校验和：*p==7 +1 / v=9 后 *p==9 +2 → rc=3。
函数 取基(整64& 源) -> 整64* {
    返回 &源;
}
不安全 函数 主() -> 整32 {
    整64 v = 7;
    整64* p = 取基(v);
    整64 累计 = 0;
    如果 (*p == 7) { 累计 = 累计 + 1; }
    v = 9;
    如果 (*p == 9) { 累计 = 累计 + 2; }
    返回 整32(累计);
}
