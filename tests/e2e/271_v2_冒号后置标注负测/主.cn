// E2E 271_v2（v2 负测，2026-09-14 第一百六十一轮 161-a A8 冒号后置类型标注移除）：
//   v2 语法层零支持冒号后置（既有行为锚定）——防 v2 侧将来私加同款兼容通道，
//   与 268（宿主同形态负测）构成双侧一致拒绝契约。代表形态=参数冒号后置。
//   期望诊断（主.expected 固化）：语法错误中止、不产 asm（v2闭环.txt 退出码=负）。
函数 加倍(x: 整32) -> 整32 {
    返回 x * 2;
}
函数 主() -> 整32 {
    返回 加倍(21);
}
