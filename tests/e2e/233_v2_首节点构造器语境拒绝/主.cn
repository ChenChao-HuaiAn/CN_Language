// E2E 233_v2 负测（2026-09-12 第八十轮·池索引 0 哨兵根治）：内置构造器
//   语境纪律的**首节点形态**负测——`返回 错误(5);` 出现在返回 整32 的函数体
//   （非 结果/可选 语境）时，语义层 检查调用表达式 构造器分支须识别并拒绝
//   （内销于语义错误即中止纪律）。修复前（HEAD）池索引 0 守卫误判「无被调」
//   → 构造器分支整段跳过 → 不报错 → IR 层退化为普通调用 `call _错误` →
//   链接失败（本用例 HEAD 实测：v2p 「成功」产出 asm，与本负测期望相反）；
//   修复后 v2p 语义错误中止（退出码 1、不产 asm、诊断锚行固化）。
//   本用例同时锚定「池索引 0 形态下语义层确实执行了构造器识别」——
//   比正测更直接地断言识别路径（正测 231 覆盖装箱产物面）。
函数 主() -> 整32 {
    返回 错误(5);
}
