# v2 闭环负测配置（2026-09-12 第八十轮 池索引 0 哨兵根治）：
#   233_v2 首节点构造器语境拒绝——`返回 错误(5);` 落在非 结果/可选 语境
#   （返回 整32）且 错误 恰落池索引 0：语义层须报「构造器 错误 须用于
#   结果/可选 语境」并非 0 退出（修复前静默放行→链接失败）。
#   退出码=负（v2p 须编译失败：非 0 退出 + 不产出 v2asm.s + 诊断锚行固化）。
退出码=负
源文件=主.cn
借链=否
供给=
