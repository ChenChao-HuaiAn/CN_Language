// E2E 84_强制检查_规则1丢弃（负面倒）：规范07 四、强制检查规则 规则1
// 规则1：结果<T,E> 类型的返回值如果未被检查就被丢弃 -> 编译错误
// 依据：.Agents/skills/cn-language-spec/specs/07-错误处理.md 第 159-193 行
// 触发写法规格单测 tests/unit/semantic/test_semantic_error.cpp Rule1ResultDiscarded：
//   函数 除法(...) -> 结果<整32, 整32> { 返回 正常(a/b) }
//   主 中直接调用 除法(10, 2) 丢弃返回值（不赋值、不检查）=> 编译期错误。
// 期望诊断子串：被丢弃未检查
函数 除法(整32 a, 整32 b) -> 结果<整32, 整32> {
    返回 正常(a / b)
}
函数 主() -> 整32 {
    除法(10, 2)
    返回 0
}
