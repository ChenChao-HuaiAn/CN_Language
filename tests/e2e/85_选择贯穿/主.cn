// E2E 85_选择贯穿（正面倒）：规范03 五、选择 语句的 fall-through 语义
// 语法依据：.Agents/skills/cn-language-spec/specs/03-语句与控制流.md——
//   「情况 分支不以 中断 结束则落入下一情况」（C fallthrough，对标 Rust match 前 Rust 的 C 语义）
// 参考：tests/e2e/63_pattern_match/主.cn 与 tests/e2e/02_control/控制流.cn 中的 选择 写法
// 覆盖：整数值 选择，验证「无 中断 的分支体贯穿到下一分支」的执行顺序。
// 注意：CN 中 打印 自动换行；逐行输出断言贯穿顺序。
函数 主() -> 整32 {
    打印("===== 选择 fall-through 顺序断言 =====");

    // 场景1：中间分支无中断，贯穿到下一情况（情况2 -> 情况3）
    整32 a = 2;
    选择(a) {
        情况 1:
            打印("A1");
            中断;
        情况 2:
            打印("A2进入-无中断");
        情况 3:
            打印("A3被贯穿进入");
            中断;
        情况 4:
            打印("A4不应执行");
            中断;
        默认:
            打印("AD不应执行");
    }

    // 场景2：首个分支无中断，贯穿到下一有中断的分支后停止
    整32 b = 1;
    选择(b) {
        情况 1:
            打印("B1进入-无中断");
        情况 2:
            打印("B2被贯穿进入");
            中断;
        默认:
            打印("BD不应执行");
    }

    // 场景3：带 中断 的分支正常停止，不会贯穿（对照验证中断边界）
    整32 c = 3;
    选择(c) {
        情况 3:
            打印("C3正常执行");
            中断;
        情况 5:
            打印("C5不应执行");
            中断;
        默认:
            打印("CD不应执行");
    }

    // 场景4：贯穿到默认分支（末尾 情况 无 中断）
    整32 d = 7;
    选择(d) {
        情况 7:
            打印("D7进入-无中断");
        情况 8:
            打印("D8贯穿");
            中断;
        默认:
            打印("DD默认执行");
    }

    打印("===== 选择 fall-through 测试完成 =====");
    返回 0;
}
