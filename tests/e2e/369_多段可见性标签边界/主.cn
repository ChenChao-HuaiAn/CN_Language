// E2E 369：可见性标签分段切换边界（规格书 5.9.3）
// 类体内 公开:/私有: 标签可多段切换，逐段生效：私有段成员（字段+方法）
// 类外不可见，公开段成员可调用；私有段方法可被同类公开方法经 自身. 调用。
类 计数器 {
私有:
    整32 私有值
公开:
    函数 计数器() { 私有值 = 1; }
    函数 读甲() -> 整32 { 返回 私有值; }
私有:
    函数 隐藏() -> 整32 { 返回 99; }
公开:
    函数 读乙() -> 整32 { 返回 自身.隐藏() + 私有值; }
}

函数 主() -> 整32 {
    计数器 表 = 计数器();
    打印("甲: ", 表.读甲());
    打印("乙: ", 表.读乙());
    返回 0;
}
