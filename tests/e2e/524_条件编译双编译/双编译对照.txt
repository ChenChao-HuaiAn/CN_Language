# 双编译对照配置（546-a D12：条件编译 v2 侧对齐——宿主+v2 同测·退出码=0）
# v2p 词法预处理（规范 4.7 裁剪模型）与宿主 preprocessor.cpp 同构。
# ★独立编号 524 上架（勿与 24_preprocessor/24_error_handling 撞主编号——
#   runner 隔离键=主编号·同编号并发走 v2 池=dualhost24 目录互踩·546-a 实测
#   Text file busy+宿主侧 -11）。源内容与 24_preprocessor 同源（条件编译全形态）。
# .expected=程序运行输出 → 断言输出=是（449 同款·步骤6 运行级逐行全等）
退出码=0
断言输出=是
源文件=条件编译.cn
借链=否
供给=
