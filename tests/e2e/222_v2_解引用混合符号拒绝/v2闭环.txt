# v2 闭环负测配置（2026-09-12 第七十六轮后续 用户裁决方案甲）：
#   222_v2 解引用右值形态的混合符号隐式转换拒绝——与宿主 222 同源；
#   v2 侧修复（解引用类型推导改返真实指向类型，查语义指针变量）后两侧一致
#   拒绝该形态。退出码=负（v2p 须编译失败：非 0 退出 + 不产出 v2asm.s +
#   诊断锚行在输出中，主.expected 固化）。
退出码=负
源文件=主.cn
借链=否
供给=
