// E2E 276（宿主正测，2026-09-14 第一百六十四轮 164-a A4·plans/023 §十二 方案D）：
//   联合体成员标注「手动释放」= 逃生舱（对标 Rust ManuallyDrop<T>）——语义层放行
//   拥有型成员、编译器不生成自动释放；用户在 不安全 函数 内显式释放后残留 0。
//   字段访问仍受 A3（联合体字段访问=不安全区）约束——本用例全程在 不安全 函数 内。
导入 IO::打印;
联合体 数值 {
    手动释放 字符串 名;
    整64 数;
}
不安全 函数 主() -> 整32 {
    整64 基线 = 内存::活动分配数();
    数值 u;
    u.名 = 字符串复制("数据甲");
    打印("长度:", 字符串长度(u.名));
    字符串释放(u.名);
    u.数 = 777;
    打印(" 残留:", 内存::活动分配数() - 基线);
    返回 0;
}
