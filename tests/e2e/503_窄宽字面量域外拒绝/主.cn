// E2E 503_窄宽字面量域外拒绝（565 轮 T36 v2 面·宿主 319-a 统一严格同口径）
// 整字面量→窄宽目标隐式转换须过值域检查：域内豁免（整8 a=10 合法）、域外
//   编译期拒绝（宿主 T36 实测：整16 甲=40000 超 32767 拒）。v2 曾全放行
//   （g1/g5/g9/g10/h6 探针实锤）——本用例=v2 拒通道持久门禁（四类型代表形态）。
函数 主() -> 整32 {
    整16 甲 = 40000;
    返回 0;
}
