# v2 闭环用例配置（565 轮 T36 v2 面·窄宽字面量域外拒绝持久门禁）：
#   整字面量→窄宽目标（整8/正8/整16/正16/正32）隐式转换须值域检查——域外
#   编译期拒绝（宿主 T36·319-a 同口径）。v2p 编译须失败（退出码非 0=负）。
#   姊妹面 502=域内回绕行为双编译锚；负号+字面量/赋值位/正32 形态同函数覆盖。
退出码=负
源文件=主.cn
借链=否
供给=
