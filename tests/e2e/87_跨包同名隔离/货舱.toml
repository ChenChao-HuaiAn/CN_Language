# 货舱.toml — 跨包同名隔离演示（规格书09-四 crate 边界/链接隔离）
# [货舱] 名称/版本 + [依赖] 甲库/乙库 两个本地库 crate
[货舱]
名称 = "主程序"
版本 = "0.1.0"

[依赖]
甲库 = "本地"      # 本地依赖：依赖/甲库/ 子目录（库 crate 根 包.cn）
乙库 = "本地"      # 本地依赖：依赖/乙库/ 子目录（库 crate 根 包.cn）
核心 = "内置"      # 内置依赖：编译器 stdlib 目录（prelude 来源）
