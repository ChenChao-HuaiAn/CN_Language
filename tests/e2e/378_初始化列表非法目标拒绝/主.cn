// E2E 378：父类构造初始化列表负例（规格书 06-三 P3-20）——
//   初始化列表目标必须为直接父类：甲 无基类而初始化列表写 : 非父类名，
//   编译期拒绝。D22 修复不得放松本负例（真非法目标仍拒）。
类 甲 {
公开:
    整32 值
    函数 甲(整32 n) : 乙(n) { 值 = n; }
}

函数 主() -> 整32 {
    甲 对象 = 甲(1);
    返回 0;
}
