// E2E 415_v2（311-a=306-b 波次2 v2 侧同构）：T4 字符串下标越界运行期拦截——
//   界内读打印正常值后越界读（s[5] 于长度 3）触发 运行时错误(2)（错误码与
//   94-a 数组防线对称；宿主侧同构=E2E 412）。v2p 编译期放行（检查为运行期
//   插桩：__cn_str_len 长度比较），运行退出码=2。
函数 主() -> 整32 {
    字符串 s = "abc";
    打印(s[0]);
    打印(s[2]);
    字符 c = s[5];
    打印(c);
    返回 0;
}
