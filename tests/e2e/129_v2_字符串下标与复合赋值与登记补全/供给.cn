导入 映射集合::映射;

// E2E 129 符号供给（③ 第二波，2026-09-03）：v2 生成代码引用的 映射$整64$字符串
//   方法符号由本文件经宿主真实管线编译为 .obj 供给（链接置于 v2p.obj 之前——
//   用例自有类型布局权威）。v2 组件自身只实例化 映射<整64,整64>/<整64,符号>，
//   字符串值映射未实例化，用例的 名表.设置/获取 符号须供给。
//   断言值 101 起仅当独立运行时可见；E2E 链接编排下本产物 .obj 仅供给符号，
//   cn_main 归 v2 产物（muldefs 下 v2asm 在前）。

函数 供给字符串映射() -> 整64 {
    映射<整64, 字符串> 名表 = 映射<整64, 字符串>();
    结果<空类型, 整32> 设 = 名表.设置(1, "甲");
    如果 (!设.正常) { 返回 101; }
    结果<字符串, 整32> 取 = 名表.获取(1);
    如果 (取.正常) {
        如果 (!字符串比较(取.值, "甲")) { 返回 103; }
    } 否则 {
        返回 102;
    }
    返回 0;
}

// 宿主编译入口（cn build 需要主函数；链接 muldefs 下 v2asm 在前，cn_main 归 v2 产物）
函数 主() -> 整32 {
    返回 整32(供给字符串映射());
}
