// E2E 193：混合符号显式转换正测（2026-09-10 第五十一轮 方案A，Rust 对齐）：
//   192 负测的对偶——显式 类型名(表达式) 构造转换后语义正确 + 字面量豁免形态
//   保留 + 同符号混合宽度宽化保留。五锚校验和（累计=29）：显式转换后比较
//   （整64(小)>负 真 +1 / 整64(小)<负 假 +0）+ 字面量豁免（大>100 真 +4 /
//   负<100 真 +8）+ 同符号宽化对照（小<大 真 +16）。v2 侧同源用例=193_v2。
函数 主() -> 整32 {
    正32 小 = 4000000000;
    整64 负 = -1;
    正64 大 = 9223372036854775808;
    整32 累计 = 0;
    如果 (整64(小) > 负) { 累计 = 累计 + 1; }
    如果 (整64(小) < 负) { 累计 = 累计 + 2; }
    如果 (大 > 100) { 累计 = 累计 + 4; }
    如果 (负 < 100) { 累计 = 累计 + 8; }
    如果 (小 < 大) { 累计 = 累计 + 16; }
    打印(整64(小) + 负);
    打印(累计);
    返回 0;
}
