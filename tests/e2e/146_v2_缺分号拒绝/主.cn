// E2E 146（plans/015 方案B 强制分号 v2 侧，2026-09-04 用户裁决）：缺分号负路径
//   ——v2 语法分析 吃分号：缺失即 stderr 诊断+语法错误数 计数；主.cn 语法错误
//   即中止（不产 IR/asm）。本文件为负测源码——各语句**故意缺分号**，任何迁移
//   工具不得对其补分号。与 139/141 同款负路径闭环。
函数 主() -> 整32 {
    整64 甲 = 1
    整64 乙 = 甲 + 1
    返回 甲 + 乙
}
