// E2E 445（2026-09-18 第 349 轮·T35 ①类根治门禁）：**v2 对运算符重载声明的优雅拒绝**
//   ——v2 暂不支持运算符重载；本用例锚定「**诊断 + 聚合中止**」而非旧行为的
//   「无诊断崩溃（错误码 302）」（348-a 探针 a4/340/92 实证）。
//   v2 闭环负路径（退出码=负）：v2p 须非零退出、不产 asm、诊断行在输出中。
//   宿主侧 v2.0 支持运算符重载（E2E 92/340 锚定），故本用例只跑 v2 自举链。
类 温度 {
公开:
    整32 值;

    函数 温度(整32 v) {
        值 = v;
    }

    函数 运算符-() -> 温度 {
        返回 温度(-值);
    }
}

函数 主() -> 整32 {
    温度 正 = 温度(23);
    打印("值=", 正.值);
    返回 0;
}
