# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   242 聚合返回位所有权（借用来源返回）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   两侧任一退回「按位 memcpy 直传借用来源」（返回位未深拷归一化）→ ①~⑤⑧ 位
#   丢失（悬垂字段经内存搅动读出乱码 → 内容比较失败）→ 退出码变化 → 对照失败。
#   85-a 修复：宿主 ir_fields.cpp（isBorrowedAggregateSource / genOwnedAggregateReturn）
#   + ir_stmt.cpp visitReturnStmt 单点挂接；v2 生成语句.cn（是借用来源返回 +
#   结构体返回位深拷）。
#   本用例不依赖容器符号（无 借链/供给）——仅 结构体/字符串/内存 内置面。
退出码=255
源文件=主.cn
