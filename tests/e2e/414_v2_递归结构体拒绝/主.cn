// E2E 414_v2（311-a=306-b 波次2 v2 侧同构）：T3 值字段递归环编译期拒绝——
//   **间接环**形态（甲↔乙·比直接环更强：声明序依赖——甲的字段乙在乙声明前
//   =未登记引用，v2 收集+收尾全量判定捕获）。宿主侧同构=E2E 411（直接环）；
//   指针递归（甲* 下一个）=合法放行形态（探针 t3c 实证·不入本用例因编译失败
//   用例单中止点）。
//   v2p 预期：解析后 T3 聚合守卫诊断「引用了后声明的结构体」+中止（退出码=负）。
结构体 甲 {
    乙 b;
}
结构体 乙 {
    甲 a;
}
函数 主() -> 整32 {
    返回 0;
}
