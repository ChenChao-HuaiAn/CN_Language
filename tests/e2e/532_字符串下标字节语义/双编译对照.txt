# 双编译对照用例配置（T48 裁决甲 2026-09-20·字符串下标字节语义 505）：
#   同一源码宿主/v2 双侧等效——下标=UTF-8 字节索引·结果 整8 符号值（-27 形态）；
#   逐字节重建走 字符转字符串（值<0 单字节直放分支=T40 双来源闭合口径）；
#   码点路径（0x597D）走 >0xFF UTF-8 编码分支。断言=退出码+运行 stdout 逐行。
退出码=0
源文件=主.cn
断言输出=是
