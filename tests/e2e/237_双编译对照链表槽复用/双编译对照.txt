# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   237 链表/队列 槽分配与回收（空闲链，83-a）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   修复前实测：队列 入队 11/22/33 + 出队 + 入队 44 → 33 丢失（出队序 22/44/44）、
#   链表<字符串> 头部内容错 + 残留 1——任一侧退回 `新索引=元素数量` 槽分配 →
#   该校验位丢失 → 退出码变化 → 对照失败。
#   供给=供给.cn：链表/队列<整64/字符串> 方法符号（v2p 只实例化自身用到的类型）。
退出码=255
源文件=主.cn
借链=是
供给=供给.cn
