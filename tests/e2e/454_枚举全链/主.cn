导入 工具::错误码;

// 454 枚举全链（418-a D40 细分防线·v2 闭环多文件）
// 覆盖：枚举声明（显式值）/跨模块导入（模块::枚举名+公开性）/成员链常量
//       折叠（错误码.成员→编译期常量）/枚举变量类型位/转换 整32()
// 退出码校验和 = 越界(2)*10 + 参数(6) + 除零(1)*100 = 126
函数 主() -> 整32 {
    错误码 e = 错误码.越界;
    返回 整32(e) * 10 + 整32(错误码.参数) + 整32(错误码.除零) * 100;
}
