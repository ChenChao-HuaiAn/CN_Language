// E2E 357：默认类型别名跨位置等价（规格书 3.1）
// 整数≡整32、小数≡浮64：别名与位宽类型在形参/实参/返回/局部声明
// 全位置混用零转换（同一类型两套名字）。
函数 加(整数 a, 整32 b) -> 整数 {
    返回 a + b;
}

函数 半(小数 x) -> 浮64 {
    返回 x / 2.0;
}

函数 主() -> 整32 {
    整数 a = 40;
    整32 b = 2;
    打印("和: ", 加(a, b));
    小数 s = 5.0;
    打印("半: ", 半(s));
    返回 0;
}
