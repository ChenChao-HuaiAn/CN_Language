// E2E 269（宿主负测，2026-09-14 第一百六十一轮 161-a A8 冒号后置类型标注移除）：
//   局部变量冒号后置标注 变量 局部: 整32 = 10; 必须编译失败——
//   类型前置为唯一规范形态（spec 03；A8 收口移除兼容分支）。
//   期望诊断子串「预期表达式，实际为 ':'」（期望编译失败.txt）。
函数 主() -> 整32 {
    变量 局部: 整32 = 10;
    打印行(局部);
    返回 0;
}
