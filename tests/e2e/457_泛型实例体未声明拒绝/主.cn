// E2E 457_泛型实例体未声明拒绝（470-a D10 v2 面·泛型实例体重放语义检查持久门禁）
// 泛型函数体内未声明调用：417-a「检查延后」=实例体零检查（未声明调用静默过→
//   链接期爆 undefined reference/产物垃圾——宿主同形态 p1 探针实锤·非泛型同
//   形态编译期拒绝=检查缺口非泛型专属语义）。470-a=语义阶段重放检查（rustc
//   typeck instantiate+check 一体同构·宿主 317-a 重放模型对齐）→编译期拒绝。
//   宿主侧残余面（重放诊断回滚吞真诊断）=470 顺带件登记 plans/021 D10 行。
// v2 负测（v2闭环.txt 退出码=负·v2p sem5 拒通道）；诊断行号/双诊断形态不精确锚定。
泛型 <类型 T>
函数 取值(T 源) -> T {
    返回 幽灵函数(源);
}

函数 主() -> 整32 {
    返回 整32(取值<整32>(5));
}
