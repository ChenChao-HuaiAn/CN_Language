// E2E 149_v2_IR层错误中止（2026-09-04，IR生成.cn 冒烟 [ir2] 纪律收口配套）
// IR 层错误即中止——对齐 139 语义错误即中止纪律：[ir2] 级诊断=IR 不完整
//   （形态不支持/目标未解析），残缺 asm 可被链接=错误产物。主.cn 于 IR 生成
//   后检查 取IR层错误数()>0 → 打印中止诊断+返回 1（不产 asm）。
//   触发形态：类型大小(变量)——语义层内置白名单放行、IR 层「类型大小: 未知
//   类型」诊断（实参非类型名，编译期无从折叠）。
// 负路径闭环（139 同款 runner 形态）：v2p 退出码非 0 + target/v2asm.asm 不产出
//   + 中止诊断行 expected 固化。
函数 主() -> 整32 {
    整64 x = 5;
    整64 n = 类型大小(x);
    返回 0;
}
