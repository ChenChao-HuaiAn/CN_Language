# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十二个对照用例）：
#   254 数组越界保护——越界下标（运行期值 5 >= 长度 3）触发运行时错误(2)
#   「数组越界」，宿主与 v2 两侧运行退出码须一致（均 rc=1）。
#   94-a（2026-09-13）v2 侧越界检查为新增能力（原缺=内存安全缺口）：
#   发射形态 = 长度常量 + IR_大于等于 + 条件跳转（越界块调 __cn_runtime_error(2)）。
#   退出码=1（runtime 错误码 1 退出——两侧同）。
退出码=1
源文件=主.cn
