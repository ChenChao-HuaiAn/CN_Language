// E2E 254：数组越界保护双编译对照（plans/020 移植纪律 9 第二十二个对照用例）
//   94-a（2026-09-13 第九十四轮）：数组下标越界检查（下标 >= 长度 → 运行时
//   错误(2)「数组越界」，宿主 ir.cpp 同码同文案）——两侧运行退出码须一致
//   （宿主 rc=1 == v2 rc=1：runtime 错误码 1 退出）。
//   安全性：越界读/写是内存安全零容忍面的运行时兜底（v2 双目标②）——
//   本用例以运行期拦截为锚（编译期不可判定运行期下标值）。
函数 主() -> 整32 {
    整32[3] 表 = { 1, 2, 3 };
    整32 下标 = 5;
    打印(表[下标]);
    返回 0;
}
