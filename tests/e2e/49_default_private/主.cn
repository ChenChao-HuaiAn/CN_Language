// 主入口（第 7 层：49_default_private 模块默认私有合法路径 E2E，规格书08-四）
// v2.0 可见性变更验证（合法路径）：
//   ① 公开: 函数 公开函数 可跨模块导入并调用（显式 公开: 导出 API）
//   ② 默认私有函数 同文件内可用（数据.cn 公开函数体内调用，闭包合并带入）
// 错误路径（导入默认私有符号 -> 语义报错）由单测 SemanticDefaultPrivateNotExported 覆盖，
//   本 E2E 只测合法用法（run_e2e.py 编译失败即 FAIL，不支持预期编译错误）。

导入 数据::公开函数

函数 主() -> 整32 {
    打印("===== 模块默认私有（第 7 层，合法路径）=====")

    // ---- ① 显式 公开: 的函数跨模块导入 ----
    整64 值1 = 公开函数(5)
    打印("公开函数(5): ", 值1)                    // 105（5 + 100，默认私有函数闭包）

    // ---- ② 再验证：公开函数 内部调用默认私有函数结果 ----
    整64 值2 = 公开函数(42)
    打印("公开函数(42): ", 值2)                   // 142

    打印("===== 模块默认私有测试完成 =====")
    返回 0
}
