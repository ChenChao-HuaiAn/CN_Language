// 数据 模块（第 7 层：49_default_private 模块默认私有 E2E 依赖模块）
// v2.0 默认私有（规格书08-四）：文件顶部无标签时声明默认为私有——
//   显式 公开: 的 公开函数 可跨模块导入；无标签的 默认私有函数 仅本模块可见。
// 本文件验证合法路径：
//   ① 公开: 段：公开函数 跨模块导入使用
//   ② 无标签段：默认私有函数 同文件内可调用（信息隐藏，不跨模块）
//   ③ 公开函数 调用 默认私有函数（私有依赖闭包合并，缺陷4 修复）
公开:
函数 公开函数(整64 数值) -> 整64 {
    // 公开函数体内调用同文件默认私有函数（闭包合并：私有不跨模块但随公开函数带入）
    返回 默认私有函数(数值)
}

// ---- 无标签段：以下声明默认私有（v2.0 变更，v1.0 为默认公开）----
函数 默认私有函数(整64 数值) -> 整64 {
    返回 数值 + 100
}
