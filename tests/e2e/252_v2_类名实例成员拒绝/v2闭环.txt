# v2 闭环负测配置（2026-09-13 第九十三轮 93-a）：
#   252_v2 类名.实例成员 拒绝——三形态（类名.实例方法()/类名.实例字段读/写）
#   在 v2 侧同款拦截（v2 语义检查模块 语义/语义检查.cn：检查类名成员访问——
#   用户类名 且 非变量遮蔽 → 诊断；遮蔽判据=符号种类 ∈ {符号_变量, 符号_参数}，
#   类名自身登记为 符号_结构体）。
#   退出码=负（v2p 须编译失败：非 0 退出 + 不产出 v2asm.s + 诊断锚行在输出中，
#   主.expected 固化）。
退出码=负
源文件=主.cn
借链=否
供给=
