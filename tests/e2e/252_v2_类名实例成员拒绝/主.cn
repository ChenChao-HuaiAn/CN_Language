// E2E 252_v2（v2 负测，2026-09-13 第九十三轮 93-a）
//   主题：类名.实例成员 拒绝（用户类名直接访问实例成员）—— v2 侧同款纪律
//   形态（三形态，v2 类支持第一版可表达面）：
//     ① 类名.实例方法()   ② 类名.实例字段（读）   ③ 类名.实例字段（写）
//   修复前：v2p 编译成功（rc=0）→ 运行输出垃圾值 / 静默写入（探针 M1/M3/M4 实证）。
//   纪律依据：宿主 93-a 同款（Rust「类名.成员 仅静态成员合法」）；v2 类支持
//     第一版无静态成员（IR类.cn）→ 用户类名.成员 任何形态皆非法（须经实例）。
类 盒子 {
公开:
    整64 值;
    函数 盒子(整64 v) {
        值 = v;
    }
    函数 读值() -> 整64 {
        返回 值;
    }
}

函数 主() -> 整32 {
    盒子 甲 = 盒子(7);
    整64 甲读值 = 盒子.读值();
    整64 甲字段 = 盒子.值;
    盒子.值 = 1;
    返回 0;
}
