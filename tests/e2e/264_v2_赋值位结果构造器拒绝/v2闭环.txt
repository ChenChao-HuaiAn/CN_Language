# v2 闭环负测配置（D5 剩余面固化，108-a）：
#   264_v2 赋值位结果/可选 构造器语境拒绝——`r = 正常(...)`（已有变量重新赋值）
#   不在 v2 构造器合法语境（返回位 / 结果/可选 变量声明初始化位）内：语义层须报
#   「构造器 正常 须用于 结果/可选 语境」并以语义错误中止（退出码 1、不产 asm）。
#   保守拒绝＝安全（完整实现需联合体条件释放设施=84-e 架构边界，登记另轮）；
#   本负测固化拒绝行为防将来误放行致静默截断/泄漏。
退出码=负
源文件=主.cn
借链=否
供给=
