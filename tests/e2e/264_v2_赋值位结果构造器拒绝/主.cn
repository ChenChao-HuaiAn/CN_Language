// E2E 264_v2 负测（2026-09-13 第一百零八轮 108-a，D5 剩余面固化）：
//   赋值位结果/可选 构造器语境纪律——`r = 正常(...)`（已有变量重新赋值）不在
//   v2 构造器合法语境（返回位 / 结果/可选 变量声明初始化位）内，语义层须拒绝
//   （保守拒绝＝安全：完整实现需「结果/可选 赋值位覆盖 + 旧值联合体条件释放」
//   设施——84-e 架构边界〈结果/可选 值字段释放需联合体条件分支=面外〉，
//   登记另轮；本负测固化拒绝行为，防将来误放行致静默截断/泄漏）。
//   宿主侧支持该形态（探针 d5c 实证：覆盖后旧值释放、残留归零）；两侧差异=
//   已登记等价性缺口（非缺陷），本用例为 v2 单侧负测。
函数 造正常() -> 结果<字符串, 整32> {
    返回 正常(字符串复制("E264载荷内容"));
}
函数 主() -> 整32 {
    结果<字符串, 整32> r = 造正常();
    r = 正常(字符串复制("E264覆盖内容"));
    返回 0;
}
