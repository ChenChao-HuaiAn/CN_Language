// 344：入口 主 返回 结果<T,E> 的退出码语义（plans/001 附录B 扩展·246-a 根治）——
//   wrapper：正常→.值 截断为退出码、错误→.错误 码（用户裁决 2026-09-16·Rust
//   Termination 同构）。本用例锚定正常路径：? 解包+wrapper 返回 正常(0)→rc=0。
//   错误路径退出码手工探针回填：主 返回 错误(3) → 退出码=3。
函数 工作(整32 模式) -> 结果<整32, 整32> {
    如果 模式 == 0 {
        返回 正常(7);
    }
    返回 错误(3);
}
函数 主() -> 结果<整32, 整32> {
    整32 甲 = 工作(0)?;
    打印行(甲);
    返回 正常(0);
}
