// E2E 362：外部 函数 重复声明边界（规格书 5.3 FFI）
// 「同名同签名重复声明允许（C 语义）」——同一 C 符号多次声明同一链接
// 目标；链接符号=函数名本身（无 mangling）。strlen 为 CRT 默认链接库函数
// （双平台同符号）。
外部 函数 strlen(字符串 s) -> 整64
外部 函数 strlen(字符串 s) -> 整64

不安全 函数 主() -> 整32 {
    打印("长度: ", strlen("中文"));
    返回 0;
}
