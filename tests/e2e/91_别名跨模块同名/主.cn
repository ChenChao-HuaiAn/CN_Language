// E2E 91_别名跨模块同名（P2-12）：花括号项别名 + 跨模块同名函数
// 模块X/模块Y 各定义 公开 函数 双倍(整64)->整64（×2 与 ×3）。
// 主.cn 用 花括号项重命名 导入 模块Y::{双倍 作为 Y双倍}：
//   语义层应把别名调用按来源模块过滤（moduleFilter=模块Y），不歧义到 模块X 的双倍。
// 对照：导入 模块X::双倍 限定调用 模块X::双倍。
导入 模块X::双倍;
导入 模块Y::{双倍 作为 Y双倍};

函数 主() -> 整32 {
    打印("===== 别名跨模块同名（P2-12）=====");
    整64 限定X = 模块X::双倍(21);
    整64 别名Y = Y双倍(21);
    打印("模块X::双倍(21): ", 限定X);   // 42
    打印("Y双倍(21): ", 别名Y);          // 63
    打印("===== 完成 =====");
    返回 0;
}
