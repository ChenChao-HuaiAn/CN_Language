// E2E 399（278-a T5 v2 拒绝面·280-a 诊断通道补齐锚定）：
//   \u{} 代理区码点（\u{D800}）——v2p 词法期拒绝：诊断行（[词法] 行 N +
//   词法错误计数）+ 编译中止不产 IR/asm（278-a 前该形态静默生成孤立代理
//   码点数字文本；280-a 词法错误聚合守卫承担中止）。
//   本源码故意含代理区转义，不可经宿主 sanity（源码即坏——宿主侧 397 负测
//   已锚定同款拒绝）。
函数 主() -> 整32 {
    字符 c = '\u{D800}';
    打印(c);
    返回 0;
}
