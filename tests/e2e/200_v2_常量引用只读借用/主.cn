// E2E 200_v2（v2 正路径闭环，2026-09-10 第五十九轮 plans/019 阶段3）：
//   常量 只读引用参数 v2 侧（语法层 常量 前缀+赋值面只读检查）——只读读链
//   校验和：读数(21)=42 +1 / 借读(21) 经只读链=42 +2 → 全对 rc=3。
函数 读数(常量 整64& 值) -> 整64 {
    返回 值 * 2;
}
函数 借读(常量 整64& 值) -> 整64 {
    返回 读数(值);
}
函数 主() -> 整32 {
    整64 x = 21;
    整64 累计 = 0;
    如果 (读数(x) == 42) { 累计 = 累计 + 1; }
    如果 (借读(x) == 42) { 累计 = 累计 + 2; }
    返回 整32(累计);
}
