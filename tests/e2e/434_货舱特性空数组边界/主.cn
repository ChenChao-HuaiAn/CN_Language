// E2E 434（329-a D12 随载修复）：货舱 [特性] 启用 = [] 空数组边界——
//   TOML 空数组合法（宿主 parseQuotedArray「空数组 [] 合法」注释口径）；
//   修复前宿主 parseQuotedValue 抢先误拒（与 v2 分叉），329-a 修复后双侧一致接受。
//   空特性集合 = 无任何 #如果定义 命中（该分支被裁剪）。
函数 主() -> 整32 {
    #如果定义 网络
    打印行("网络: 命中");
    #结束如果
    打印行("空数组边界: 通过");
    返回 0;
}
// 注：本用例宿主-only（T54 宿主 cargo_parser 空数组误拒修复锚定）——双编译面待 v2 [特性] 通道轮。
