# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   246 静态变量全链路（槽尺寸/初值/读写/实参/返回/成员链/哨兵+重复赋值）——
#   同一源码两侧各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   87-a 修复面：宿主 ir_decl/ir_expr/ir.cpp 静态聚合全链 + 三后端 .data 槽尺寸与
#   标量初值（linux_x64/arm64/x64）；v2 静态表类型/初值区间/注入 + 静态结构体
#   地址语义（读取/目标/成员基址）+ .data 槽尺寸（三后端）。
#   本用例不依赖容器符号（无 借链/供给）；不含字符/浮点打印（v2 既有语法与
#   打印限制，字符初值面由宿主单侧 247 承载）。
退出码=255
源文件=主.cn
