// plans/018 P6b 工作流2（第 0 步实证锚定）：模块 声明自动加载强语义负测。
//   「模块 工具件;」声明即自动加载（Rust mod 对齐：被声明子模块恒编译，
//   哪怕无人导入）——工具件.cn 内的未声明标识符错误必须暴露（rc=1）。
//   对照：删除该 声明 → 工具件.cn 不加载 → rc=0（第 0 步探针B 实证）。
模块 工具件;

函数 主() -> 整32 {
    返回 0;
}
