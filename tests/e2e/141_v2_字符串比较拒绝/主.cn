// E2E 141（v2 负路径闭环，2026-09-04 用户裁决·缺陷零容忍）：
//   灰色点②方案A v2 侧——字符串比较运算符显式拒绝（v2 语义 检查表达式 二元分支：
//   操作数区间末端递归推导，两侧均字符串 → 硬错误+迁移提示）。与 139 同款负路径：
//   v2p 编译失败（退出码 1）+ 无 asm 产出 + 诊断行 expected 固化。
//   v2 检查面诚实边界：拼接产物/成员字段末端推导 -1/整64 兜底=检不出（字面量/变量
//   形态已覆盖主踩坑面；随 v2 语义类型推导完善收口，宿主侧已全形态覆盖）。
函数 主() -> 整32 {
    字符串 a = "甲";
    字符串 b = "乙";
    如果 (a == b) { 返回 1 }
    返回 0;
}
