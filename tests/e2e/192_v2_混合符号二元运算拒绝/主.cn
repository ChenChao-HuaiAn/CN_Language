// E2E 192_v2（v2 负路径闭环，2026-09-10 方案A 用户裁决·混合符号二元运算拒绝）：
//   与宿主 192 同规范依据（plans/001 §3.7，Rust 对齐）——v2 语义层同款拒绝
//   （语义检查表达式.cn 检查混合符号二元 + 语义检查.cn 二元分支扩算术/位运算
//   调用；字面量豁免含 -1 形态；类型推导 -1 跳过=检不出诚实边界）。v2p 对
//   本源码预期语义错误中止（退出码非 0、不产 asm、诊断行固化下方）。
//   **不可**经宿主 sanity 之外的编译产物验证（源码即坏）。
函数 主() -> 整32 {
    正32 小 = 4000000000;
    整64 负 = -1;
    正64 大 = 9223372036854775808;
    如果 (小 < 负) { 返回 1; }
    如果 (负 > 小) { 返回 2; }
    如果 (负 < 大) { 返回 3; }
    返回 整32(小 + 负);
}
