# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   221 映射/集合/向量/链表/队列 移除-覆盖-清空路径字符串所有权——同一源码两侧
#   各编各跑，退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   两侧任一形态的释放/归一化机制分叉 → 该位丢失 → 退出码变化 → 对照失败。
#   供给=供给.cn：集合/映射/链表/队列<字符串> 方法符号（含 76-a 新挂点方法）
#   由宿主真实管线编译供给（v2p 只实例化自身用到的类型——126/218/219 同款机制）。
退出码=255
源文件=主.cn
借链=是
供给=供给.cn
