# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   234 容器元素 = 含拥有型串字段结构体——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）；
#   两侧任一形态的所有权机制分叉（元素字段串不释放 / 移位深拷泄漏 / 调用返回
#   临时泄漏 / 链式容器平铺析构 / 结构体字面量实参空指针）→ 该校验位丢失 →
#   退出码变化 → 对照失败。宿主侧 81-a 修复：# 元素字段串释放面（平铺/链游）、
#   元素移动挂点（memcpy + 源槽清零）、调用返回实参临时清理、块终止字段名单
#   不截断；v2 侧：同源修复 + 元素释放经供给 obj 的 ~容器 注入。
#   供给=供给.cn：向量/链表/栈/队列/集合<盒子> 方法符号（v2p 只实例化自身
#   用到的类型——126/218/219/221 同款机制）。
退出码=255
源文件=主.cn
借链=是
供给=供给.cn
