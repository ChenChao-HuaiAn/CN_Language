// E2E 173（v2 闭环）：目录包根与成员模块路径撞名 + 纯名调用同名字函数
//   锚定 plans/018 v2self 锚定轮根治：模块路径们 是文件级快照（包根 包.cn 与
//   成员 设备.cn 同为「设备」路径），③ 合并全局可见构建 全部模块ID们 须经
//   模块集合去重——修复前同签名双计数假报 E0659「符号歧义: 设备」（来源们=
//   自拼接「设备/设备」），合法程序被拒。期望：编译通过，产物运行 rc=173。
导入 设备;

函数 主() -> 整32 {
    返回 设备();
}
