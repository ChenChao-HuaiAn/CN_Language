// E2E 355：语句终止边界形态（规格书 4.6 规则1）
// ① 行内块 { 语句; } 内部语句同样须分号；② 控制体 } 自终结后可直接跟
// 下一条语句；③ 函数体 } 自终结后不加也是边界（结构体体同）。
结构体 点 {
    整32 x
    整32 y
}

函数 主() -> 整32 {
    变量 a = 1;
    如果 a == 1 { 变量 t = 5; a = t; }
    打印("a: ", a);
    返回 0;
}
