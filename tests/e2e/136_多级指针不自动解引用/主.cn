// E2E 136_多级指针不自动解引用（v2.1，2026-09-03 用户裁决：一级自动解引用）
// 规范：. 对指针自动解引用仅一级（p.字段 ≡ (*p).字段，Go 先例）；多级指针
//   （点**）不继续解引用——编译期硬错误，提示显式 * 解引用一级
//   （错误可见化，防自动魔法掩盖真实层级意图）。
结构体 点 {
    整32 x
    整32 y
}
函数 主() -> 整32 {
    点 p = 点{ x = 10, y = 20 }
    点* q = &p
    点** qq = &q
    整32 v = qq.x
    返回 v
}
