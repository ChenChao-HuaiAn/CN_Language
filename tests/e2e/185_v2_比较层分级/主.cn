// 185：v2 相等(6)/比较(7)两级拆分闭环（观察项①，规范 01b§6 对齐）——
//   a<b == c<d 按 (a<b)==(c<d) 分组；修复前合并单层左结合
//   r1=((a<b)==c)<d、r2=((a<a)==c)>d → 错误分组校验和 1+0+4+8=13；
//   正确分组 r1=假/r2=真/r3=真/r4=真 → 0+2+4+8=14（退出码=14 锚定）。
//   【375-a B1 甲改写（2026-09-18 用户批准）】比较结果为布尔：旧版赋给整32
//   变量属「布尔→整32」隐式转换，宿主语义收紧后与 v2 均拒绝（化石成因）；
//   现改存布尔变量、用如果逐位累加校验和——两级分组差分锚（13≠14）保留。
//   通道由 v2闭环转双编译对照（宿主+v2 同测·根治与宿主规则脱节）。
函数 主() -> 整32 {
    整32 a = 5;
    整32 b = 2;
    整32 c = 3;
    整32 d = 4;
    布尔 r1 = a < b == c < d;
    布尔 r2 = a < a == c > d;
    布尔 r3 = 2 + 3 * 4 == 14;
    布尔 r4 = 1 << 2 + 1 == 8;
    整32 校验 = 0;
    如果 (r1) { 校验 = 校验 + 1; }
    如果 (r2) { 校验 = 校验 + 2; }
    如果 (r3) { 校验 = 校验 + 4; }
    如果 (r4) { 校验 = 校验 + 8; }
    返回 校验;
}
