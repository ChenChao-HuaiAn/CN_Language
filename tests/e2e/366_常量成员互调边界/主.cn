// E2E 366：常量成员函数边界（规格书 5.6 常量成员）
// ① 常量成员互调：常量 函数 读数 内经 自身. 调另一常量 函数 原始值
// （常量承诺传递）；② 常量引用形参（常量 温度计&）仅能调常量成员。
类 温度计 {
私有:
    整32 刻度
公开:
    函数 温度计(整32 v) { 刻度 = v; }
    常量 函数 读数() -> 整32 { 返回 自身.原始值(); }
    常量 函数 原始值() -> 整32 { 返回 刻度; }
}

函数 打印温度(常量 温度计& 表) -> 空类型 {
    打印("温度: ", 表.读数());
}

函数 主() -> 整32 {
    温度计 表 = 温度计(36);
    打印温度(表);
    返回 0;
}
