# 双编译对照用例配置（plans/020 移植纪律 9 门禁化·325-a D37）：
#   39 大栈帧 __chkstk 栈探测——1200 局部变量 9600 字节栈帧（>4KB guard 页）。
#   宿主侧早已三段式（2026-08-15）；v2 win 后端 325-a 补齐（影子空间预存 +
#   mov rax,N/call __chkstk/sub rsp,rax + retbuf/参数改从影子读）。
#   两侧各编各跑，退出码须一致（0）且 stdout 逐行等于 .expected（校验和 719400）。
退出码=0
源文件=主.cn
借链=否
供给=
断言输出=是
