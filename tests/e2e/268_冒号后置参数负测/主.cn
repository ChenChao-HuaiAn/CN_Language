// E2E 268（宿主负测，2026-09-14 第一百六十一轮 161-a A8 冒号后置类型标注移除）：
//   参数冒号后置标注 函数 加倍(x: 整32) 必须编译失败——宿主私藏规范外语法
//   根治（spec 03 明确否决后置标注；v2 语法层零支持，本用例锚定双侧一致拒绝）。
//   期望诊断子串「预期参数名」（期望编译失败.txt）。
函数 加倍(x: 整32) -> 整32 {
    返回 x * 2;
}
函数 主() -> 整32 {
    返回 加倍(21);
}
