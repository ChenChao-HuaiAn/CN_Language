// E2E 400（281-a T5 字符串面 v2 诊断余项锚定）：
//   字符串字面量内 \u{D800} 代理区码点——v2p 词法期拒绝（280-a 词法错误
//   通道复用：诊断行+计数+中止不产 IR/asm）。278-a 前该形态在字符串内
//   静默生成孤立代理字节；宿主侧字符串面同款拒绝（278-a readStringBody）。
//   本源码故意含代理区转义，不可经宿主 sanity（源码即坏）。
函数 主() -> 整32 {
    字符串 s = "甲\u{D800}乙";
    打印(s);
    返回 0;
}
