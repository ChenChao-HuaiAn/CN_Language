// plans/018 P6b 工作流2（定位收紧，规格08-二）：负测——普通文件（非包根/
//   非父模块）的 模块 声明挂载到非子模块位置 = 编译诊断。
//   形态：网络库.cn（普通依赖文件，非入口/包.cn）声明 模块 兄弟件;——
//   兄弟件.cn 是其同级文件而非 网络库/ 子目录成员，违背「模块路径由文件名
//   +目录唯一决定」→ 专用定位诊断（替换旧误导性「无法打开源文件」）。
导入 网络库;

函数 主() -> 整32 {
    返回 0;
}
