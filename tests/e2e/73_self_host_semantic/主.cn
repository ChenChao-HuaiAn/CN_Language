// E2E 73_self_host_semantic：自举 Task 7.3——CN 语义分析器（2026-08）
// 输入：CN 源码；内部 分词->解析->语义检查；输出：符号表 + 诊断行。
// 覆盖：函数/变量/类型符号登记、未声明标识符、重复声明、参数个数、
//   赋值类型匹配（基本）、返回类型匹配、中断/继续 位置检查。
导入 容器::向量;
导入 CN语言编译器::语义分析;

函数 主() -> 整32 {
    字符串 源码 = "函数 双倍(整64 值) -> 整64 {\n    返回 值 * 2;\n}\n函数 主() -> 整32 {\n    整64 数量 = 双倍(21);\n    打印(数量);\n    整64 数量 = 3;\n    浮64 比率 = 未声明变量;\n    返回 0;\n}\n";
    向量<字符串> 诊断 = 语义检查(源码);
    整64 i = 0;
    当 (i < 诊断.大小()) {
        打印(诊断.元素(i));
        i++;
    }
    返回 0;
}
