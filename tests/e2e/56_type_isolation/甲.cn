// E2E 56_type_isolation：跨模块同名类型/常量隔离（A-2 修复，2026-08）
// 本模块与 主.cn 各自定义 结构体 记录（不同字段）与 常量 版本（不同值）——
// 类型级 crate 分桶：各模块函数体内的裸名引用解析到本模块的定义。
// 注：函数名保持模块内唯一（crate 隔离同名函数纯名调用歧义是既有已知限制#7，
//   与本用例的类型隔离主题无关，故 甲 的函数以 甲 前缀命名）
公开:
结构体 记录 {
    整64 标识
}

公开:
常量 版本 = 100

公开:
函数 甲模块版本() -> 整64 {
    返回 版本
}

公开:
函数 创建甲记录(整64 标识) -> 记录 {
    返回 记录{ 标识 = 标识 }
}

公开:
函数 读甲标识(记录 r) -> 整64 {
    返回 r.标识
}
