// plans/018 P6b 工作流2/4（呈报二 A′ + 呈报一B 锚定正测）：
//   ①×③ 遮蔽 + 函数链接键公式化（A′）错编修复锚定。
//   形态=「依赖与入口同名同签名 + 入口纯名调用」——A′ 之前注册键归属顺序
//   依赖（依赖先注册抢裸键 → 入口函数注册为 主$版本）而定义侧 主 恒不加
//   前缀 → 链接 undefined reference 主$版本（base3 探针汇编实证的既有错编
//   缺陷，E2E 矩阵零覆盖）；A′ 后注册/定义双侧同源公式，本用例钉死不复发。
//   三级解析（规格08-三 §5.9.7）：①本模块显式项 遮蔽 ③glob 导入 = 唯一
//   合法遮蔽——纯名 版本() 命中本模块（100），限定调用 工具库::版本() 恒
//   明确（200），glob 导入的 双倍 纯名直调（50）。
// 注意：主.cn 故意不写 导入 工具库::版本（具名导入会 E0255，见 159），
//   只写 整体导入 导入 工具库（③ 通配层）。
导入 工具库;

// 本模块 版本()——与 工具库 版本() 同名同签名（crate 隔离 + ① 遮蔽 ③）
函数 版本() -> 整64 {
    返回 100;
}

函数 主() -> 整32 {
    // ① 本模块显式项优先（A′ 锚定：旧错编形态在本行链接期爆炸）
    打印("主 版本(): ", 版本());
    // 限定调用恒明确（不受遮蔽影响）
    打印("工具库::版本(): ", 工具库::版本());
    // ③ glob 导入符号纯名直调（本模块无同名，无歧义）
    打印("工具库 双倍(5): ", 双倍(5));
    返回 0;
}
