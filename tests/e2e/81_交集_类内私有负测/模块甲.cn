// 81 模块甲（模块公开 × 类内私有 负测依赖模块）
// 类内 私有: 方法 秘密加 仅类内可调用；模块公开类 计算器 可跨模块导入。
公开:
类 计算器 {
    公开:
    函数 计算器() -> 空类型 {
        自身.数值 = 0;
    }

    函数 公开加(整64 x) -> 整64 {
        自身.数值 = 自身.数值 + x;
        返回 自身.数值;
    }

    私有:
    函数 秘密加(整64 x) -> 整64 {
        自身.数值 = 自身.数值 + x;
        返回 自身.数值;
    }

    整64 数值;
}
