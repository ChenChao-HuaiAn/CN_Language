// 81_交集_类内私有负测（负 e2e：预期编译失败）
// 矩阵 ❌ 象限：模块公开 × 类内私有 -> 类可跨模块实例化，但类内私有方法
// 跨文件不可调用（访问控制：私有成员仅类内方法可访问）。
// B 模块导入 模块甲::计算器，跨文件调用其 私有: 方法 秘密加 -> 编译失败。
导入 模块甲::计算器

函数 主() -> 整32 {
    计算器 计 = 计算器()
    整64 值 = 计.秘密加(5)
    打印("秘密加: ", 值)
    返回 0
}
