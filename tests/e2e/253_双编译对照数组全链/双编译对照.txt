# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十一个对照用例）：
#   253 数组全链（声明+初始化列表/部分初始化补零/下标写/循环动态下标/作实参退化/
#   结构体元素数组成员访问/字符串元素数组/浮点元素数组）——同一源码两侧各编各跑，
#   退出码须一致（宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   94-a（2026-09-13）v2 栈数组全链落地门禁：修前 v2 侧语法错误（`浮32[2] 表;`
#   报「语句缺少分号」——92-a 登记缺口）；数组越界检查错误码与宿主同码（2=数组越界）。
退出码=255
源文件=主.cn
