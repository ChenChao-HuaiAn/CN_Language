// 181：v2 结构体构造字面量闭环（2026-09-09 用户裁决，规范 05 二穷举纪律）——
//   类型{字段=值} 解析期脱糖为 裸声明(B1 零初始化)+逐字段成员赋值；
//   缺字段/重复字段=编译错误（语法错误数中止通道）。
结构体 点 {
    整32 x
    整32 y
}

结构体 线 {
    整64 a
    整64 b
    整32 c
}

函数 主() -> 整32 {
    点 p = 点{ x = 3, y = 4 };
    线 w = 线{ a = 10, b = 20, c = 5 };
    返回 p.x + p.y + 整32(w.a) + 整32(w.b) + w.c;
}
