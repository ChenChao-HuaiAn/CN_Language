// 工具模块（第 7 层：45_import_syntax 导入语法全形式 E2E 依赖模块）
// 公开: 函数 加/乘/减 + 常量 版本 + 函数 获取版本（跨模块常量引用演示）
// v2.0 默认私有（规格书08-四）：文件顶部显式 公开: 导出 API。
// 顶层常量（v2.0 决策9）：公开 常量 跨模块合并，经公开函数体引用验证。
公开:
函数 加(整64 左, 整64 右) -> 整64 {
    返回 左 + 右;
}

函数 乘(整64 左, 整64 右) -> 整64 {
    返回 左 * 右;
}

函数 减(整64 左, 整64 右) -> 整64 {
    返回 左 - 右;
}

常量 版本 = 100;

函数 获取版本() -> 整64 {
    返回 版本;
}
