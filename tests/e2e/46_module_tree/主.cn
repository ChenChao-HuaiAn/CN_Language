// 主入口（第 7 层：46_module_tree 模块树 E2E，规格书08-二/五）
// 验证 v2.0 模块树三要素：
//   ① 模块 声明挂载：网络.cn 内 `模块 传输控制` 声明引用子模块
//   ② 子目录递归加载：网络/传输控制.cn（:: 对应 /，模块名 网络::传输控制）
//   ③ 再导出链：网络.cn 内 `公开 导入 网络::传输控制::连接` 把子模块公开
//      符号提升为 网络 模块 API——主.cn `导入 网络::连接` 触发依赖链加载后
//      纯名 连接() 可用（与 47_package_cargo 的再导出用法一致）
// 覆盖调用：
//   - 连接()                          （再导出链合并后纯名直调）
//   - 网络::传输控制::发送()          （多级路径：完整子模块路径限定调用）
//   - 发送()                          （子模块公开函数纯名）
// 编译器边界（2026-08-15 实测）：父模块名限定调用子模块函数（网络::连接()）
//   时 moduleFilter=网络 不匹配子模块 moduleName=网络::传输控制，resolveOverload
//   过滤失败报"未找到匹配函数"。再导出场景应使用纯名调用（连接()）或多级路径
//   限定（网络::传输控制::发送()，subModule 完整匹配）。本用例按编译器
//   支持路径编写，边界记录于注释（单测 SemanticModuleTreeReexportChain 覆盖语义层）。
// v2.0 默认私有：本文件顶层 主 为入口全部保留

导入 网络::连接
导入 网络::传输控制::发送

函数 主() -> 整32 {
    打印("===== 模块树（模块声明 + 子目录 + 再导出）=====")

    // ---- ① 再导出链：连接 经 网络.cn 的 公开 导入 再导出后纯名直调 ----
    整64 连接码 = 连接()
    打印("连接(): ", 连接码)                      // 42

    // ---- ② 多级路径：网络::传输控制::发送（完整子模块路径限定调用）----
    整64 发送码 = 网络::传输控制::发送()
    打印("网络::传输控制::发送(): ", 发送码)      // 99

    // ---- ③ 子模块公开函数纯名 ----
    整64 发送2 = 发送()
    打印("纯名 发送(): ", 发送2)                  // 99

    打印("===== 模块树测试完成 =====")
    返回 0
}
