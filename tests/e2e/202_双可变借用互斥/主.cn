// E2E 202（宿主负测，2026-09-10 第六十轮 plans/019 阶段3b）：
//   可变×可变双别名互斥——同一调用中同一变量被可变借用两次（别名可变冲突，
//   Rust 同款纪律；存量预审 tests/e2e+stdlib+v2 全树零命中）。两负形态：
//   ①普通函数 ②类构造调用面（3b 接入面锚定）。
//   期望编译失败.txt 固化诊断子串「被可变借用两次」。
类 计数 {
公开:
    整64 值;
    函数 计数(整64& 甲, 整64& 乙) { 值 = 甲 + 乙; }
}
函数 双改(整64& 甲, 整64& 乙) -> 空类型 {
    甲 = 甲 + 乙;
}
函数 主() -> 整32 {
    整64 x = 5;
    双改(x, x);
    计数 c = 计数(x, x);
    返回 0;
}
