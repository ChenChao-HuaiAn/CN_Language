// E2E 140（宿主负测，2026-09-04 用户裁决·缺陷零容忍）：
//   灰色点②方案A——字符串比较运算符显式拒绝（规范 plans/001 Task 2.8：比较运算符
//   仅定义整型与浮点变体，字符串不参与运算符比较；等价能力由 字符串比较（相等）与
//   字符串字典序（全序）提供）。原「指针间（含字符串）按地址比较」放行=同内容异地址
//   的 ==/!= 恒假（拼接产物静默陷阱）。字符串/字符* 与 无 的判空比较保留（133 载体）。
//   本用例预期编译失败（期望编译失败.txt 固化诊断子串）。
函数 主() -> 整32 {
    字符串 a = "甲乙" + "丙";
    字符串 b = "甲乙" + "丙";
    如果 (a == b) { 返回 1; }
    返回 0;
}
