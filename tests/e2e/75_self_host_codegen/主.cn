// E2E 75_self_host_codegen：自举 Task 7.5——CN 代码生成器（2026-08）
// 输入：IR 行（IR 生成器输出）；输出：简化 x64 伪汇编行。
// 覆盖：函数头/参数/分配/常量/加载/运算/调用/返回/分支 的汇编映射。
导入 容器::向量
导入 CN语言编译器::IR生成
导入 CN语言编译器::代码生成

函数 主() -> 整32 {
    字符串 源码 = "函数 双倍(整64 值) -> 整64 {\n    返回 值 * 2\n}\n函数 主() -> 整32 {\n    整64 数量 = 双倍(21)\n    打印(数量)\n    返回 0\n}\n"
    向量<字符串> IR行 = IR生成(源码)
    向量<字符串> asm行 = 代码生成(IR行)
    整64 i = 0
    当 (i < asm行.大小()) {
        打印(asm行.元素(i))
        i++
    }
    返回 0
}
