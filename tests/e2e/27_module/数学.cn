// 数学模块（Task 3.6 模块系统 E2E）
// 公开: 平方根/双倍——跨模块可见；私有: 内部辅助——仅本模块内
// 说明：被导入模块的公开函数体引用本模块私有函数（私有符号合并后被过滤，
//   公开函数体找不到私有符号）是模块系统合并的边界场景（E2E 实测报
//   "未声明 内部辅助"），本用例公开函数仅依赖参数/自身逻辑，私有符号
//   可见性由语义层单测覆盖；私有函数引用边界留待 Debug 全面审查子任务。
公开:
函数 平方根(浮64 x) -> 浮64 {
    返回 x
}

函数 双倍(整32 n) -> 整32 {
    返回 n * 2
}

// 公开函数体引用私有符号（缺陷4 修复验证）：合并后私有依赖闭包须一并带入
函数 三倍(整32 n) -> 整32 {
    返回 内部辅助(n)
}

私有:
函数 内部辅助(整32 n) -> 整32 {
    返回 n * 3
}
