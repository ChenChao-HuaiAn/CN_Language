导入 计算;

// E2E 119（v2 自举闭环第一步：链接环境对齐 + 多源文件支持）：
//   v2p 编译本入口文件 + 自动加载 导入模块 计算.cn（多文件全局符号表，
//   跨文件函数调用）-> target/v2asm.asm（入口符号 cn_main 对齐宿主）
//   -> ml64/link（对齐宿主链接命令：/ENTRY:WinMainCRTStartup + 运行时 obj）
//   -> 运行：加倍(7)=14 -> 期望退出码 14
函数 主() -> 整32 {
    返回 整32(加倍(7));
}
