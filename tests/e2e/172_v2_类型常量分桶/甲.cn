// E2E 172_v2_类型常量分桶（plans/018 类型/常量布局分桶，2026-09-08）
// 甲 模块：16 字节布局同名结构体 + 同名常量（与 主 的 4 字节/200 值 独立分桶）
// 修复前：常量表纯名平铺被 主 覆盖（甲版本 读到 200）；结构体大小表被覆盖
//   （16B sret 降级 4B 标量返回 → 甲端到端 ABI 错乱读垃圾值）
公开:
结构体 记录 {
    整64 标识;
    整64 数值;
}

公开:
常量 版本 = 100;

公开:
函数 甲版本() -> 整64 {
    返回 版本;
}

公开:
函数 造甲(整64 a, 整64 b) -> 记录 {
    记录 r;
    r.标识 = a;
    r.数值 = b;
    返回 r;
}

公开:
函数 甲求和(记录 r) -> 整64 {
    返回 r.标识 + r.数值;
}

公开:
函数 甲端到端() -> 整64 {
    记录 r = 造甲(42, 99);
    返回 甲求和(r);
}
