// E2E 280_v2（v2 负测，2026-09-15 第一百八十八轮 188-a D6·plans/023 B11 变量常量传播）：
//   v2 侧恒空判定同构——三操作面（解引用/成员访问/下标访问）变量形态各自硬错误
//   （诊断文案与宿主逐字一致=双侧同构；行号=使用点语句行）。
//   期望=退出码负 + 无 asm 产出 + 三条诊断行固化（主.expected）。
结构体 点 { 整64 x; }
不安全 函数 主() -> 整32 {
    整64* p = 无;
    整64 v = *p;
    点* q = 无;
    整64 w = q.x;
    整64 z = p[0];
    返回 整32(v + w + z);
}
