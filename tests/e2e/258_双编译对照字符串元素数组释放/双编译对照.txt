# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十六个对照用例）：
#   258 字符串元素数组释放（块出口/循环跳出/嵌套块/非字符串对照/多数组叠加/
#   元素读正确性/首末元素/函数尾）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   98-a（2026-09-13 C9）门禁：修前**双侧同缺**（块出口残留 2——宿主 79-a 靶子面
#   「数组元素残留 2」；形①③⑤⑦⑧失败）。
退出码=255
源文件=主.cn
