# 78_v2 自举链构建（v2 锚定链用例，2026-09-14 自 v1 78_chain_build 迁移）
# 对象 = v2 自举编译器全树自身（CN语言编译器v2/，无用例源码——特殊编排见 run_e2e.py）
#   阶段=链构建：宿主编译 v2 全树建 v2p（指纹缓存复用）-> v2p 编译 v2 全树自身
#   -> fix_p.asm（断言：退出码 0 + 产物含 cn_main + 行数不小于防假绿下界）
# v1 对应物 78_chain_build：v1 组件链编译 5 大组件源码（因字符串行 IR 内存失控
#   OOM 失败——正是 v2 重建的动机）；v2 版以内存健康的 v2 全树编译承载同一语义。
# 三平台：win-x64 ml64/link；linux-arm64 / linux-x86_64 as/g++。
阶段=链构建
