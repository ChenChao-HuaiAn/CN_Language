// E2E 84_强制检查_规则3可选（负面倒）：规范07 四、强制检查规则 规则3
// 规则3：可选<T> 类型的值在访问 .值 前必须检查 .有值 -> 编译错误
// 依据：.Agents/skills/cn-language-spec/specs/07-错误处理.md 第 159-193 行
// 触发写法规格单测 tests/unit/semantic/test_semantic_error.cpp Rule3OptionalValueBeforeCheck：
//   函数 查找(...) -> 可选<整32> { 返回 某些(键) }
//   主 中取得 可选 后未检查 .有值 直接访问 .值 => 编译期错误。
// 期望诊断子串：必须检查 可选.有值
函数 查找(整32 键) -> 可选<整32> {
    返回 某些(键);
}
函数 主() -> 整32 {
    变量 查找结果 = 查找(42);
    打印(查找结果.值);
    返回 0;
}
