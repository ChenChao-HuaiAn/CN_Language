# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；键与 v2闭环.txt 同格式）：
#   263 可选/结果 无值全链（H6 根治门禁）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对；
#   ⑧=块级析构残留归零）。
#   107-a 修复面：v2（生成语句.cn 返回位裸「无」零初始化 + 语义检查.cn 可选「无」
#   声明位放行 + 生成声明.cn 生成结果局部 裸「无」零初始化）。
#   本用例不依赖容器符号（无 借链/供给）。
退出码=255
源文件=主.cn
