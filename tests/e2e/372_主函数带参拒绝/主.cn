// E2E 372：入口函数签名约束负例（规格书 附录A 命令行接口）
// 入口 主 不接受 C 风格 argc/argv 形态——命令行参数经 系统::参数个数/
// 系统::参数 内置获取（37_system），入口签名恒为无参；偏离形态在链接期
// 被拒（入口符号 cn_main 由编译器按无参约定合成）。
函数 主(整32 个数, 字符串* 表) -> 整32 {
    返回 个数;
}
