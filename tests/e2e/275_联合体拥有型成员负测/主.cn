// E2E 275（宿主负测，2026-09-14 第一百六十四轮 164-a A4·plans/023 §十二 方案A）：
//   联合体成员为拥有型（字符串）→ 编译期硬错误（对标 Rust union 成员须 Copy；
//   原实现放行并按偏移 0 无条件释放 = p13 泄漏 / p14 误释放 UAF 的根因）。
//   期望诊断子串「联合体成员」（期望编译失败.txt）。
联合体 数值 {
    字符串 名;
    整64 数;
}
函数 主() -> 整32 { 返回 0; }
