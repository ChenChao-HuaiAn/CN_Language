// E2E 193_v2：混合符号显式转换 v2 闭环（2026-09-10 第五十一轮 方案A，Rust 对齐）：
//   与宿主 193 同源五锚校验和（rc=29）：显式转换后比较（整64(小)>负 真 +1 /
//   整64(小)<负 假 +0）+ 字面量豁免（大>100 真 +4 / 负<100 真 +8）+ 同符号
//   宽化对照（小<大 真 +16）。v2 语义层拒绝「变量间」混合符号（192_v2 负路径
//   同款），显式转换/字面量豁免/同符号形态放行且发射语义正确。
函数 主() -> 整32 {
    正32 小 = 4000000000;
    整64 负 = -1;
    正64 大 = 9223372036854775808;
    整32 累计 = 0;
    如果 (整64(小) > 负) { 累计 = 累计 + 1; }
    如果 (整64(小) < 负) { 累计 = 累计 + 2; }
    如果 (大 > 100) { 累计 = 累计 + 4; }
    如果 (负 < 100) { 累计 = 累计 + 8; }
    如果 (小 < 大) { 累计 = 累计 + 16; }
    返回 累计;
}
