# 双编译对照用例配置（plans/020 移植纪律 9 门禁化；第二十九个对照用例）：
#   261 字符* 借用视图全链 + 指针数组（变量/作参/返回/显式复制/自动降级/比较/
#   结构体字段/字符*[N] 指针数组）——同一源码两侧各编各跑，退出码须一致
#   （宿主侧 == 用例期望值，v2 侧 == 宿主侧实测值）。
#   退出码 = 八形态折校验和 255（①1+②2+③4+④8+⑤16+⑥32+⑦64+⑧128 全对）。
#   101-a（2026-09-13 C1）门禁：修前 v2 `字符*` 置指针层（打印指针值——形①⑤⑦⑧
#   失败）+ `类型*[N]` 指针数组声明缺失（形⑧解析错误）。
退出码=255
源文件=主.cn
