// E2E 106_接口去虚拟化（006 性能路线 D3A）：单一实现接口 —— 编译期直调（正确性锁定）
// 覆盖：指针形式/值形式唯一实现接口调用；多接口方法；无多态丢失。
接口 通讯 { 虚拟 函数 发(整32 数据) -> 整32 }
类 串口 : 通讯 {
公开:
    整32 累计
    函数 串口() { 累计 = 0 }
    重写 函数 发(整32 数据) -> 整32 { 累计 = 累计 + 数据; 返回 累计 }
}
函数 主() -> 整32 {
    打印("===== 接口去虚拟化（D3A）=====")
    串口 对象 = 串口()
    通讯* 设备 = 对象
    打印("指针累计1: ", 设备.发(10))          // 10
    打印("指针累计2: ", 设备.发(5))           // 15
    通讯 设备2 = 串口()
    打印("值累计1: ", 设备2.发(7))            // 7
    打印("值累计2: ", 设备2.发(3))            // 10
    打印("===== 完成 =====")
    返回 0
}
