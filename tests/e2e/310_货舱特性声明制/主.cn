// 310：货舱 [特性] 声明制（规格书 4.7 特性小节，239-a）——
//   启用特性=条件编译旗标（与 -D 同通道）；未启用特性被裁剪。
函数 主() -> 整32 {
    #如果定义(网络)
    打印行("特性-网络: 命中");
    #结束如果
    #如果定义 压缩
    打印行("特性-压缩: 命中");
    #结束如果
    #如果定义(图形)
    打印行("特性-图形: 命中");
    #结束如果
    打印行("主程序运行");
    返回 0;
}
