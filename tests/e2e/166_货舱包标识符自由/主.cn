// plans/018（2026-09-07 用户裁决方案A）：包/货舱 死保留字摘除——恢复标识符自由。
//   两词在 CN 源码语法中无任何语法位置（包.cn/货舱.toml 是文件名约定，
//   [货舱]/[依赖] 是 TOML 数据文本，语法层从未消费），保留字化徒然剥夺
//   标识符空间（Rust 对照：cargo 非保留字）。本用例钉死：包/货舱 可作
//   变量名/函数名/参数名/结构体名——与普通标识符完全同权；
//   模块/作为 仍是关键字（有真实语法：模块 X; / 作为 别名）。
结构体 货舱 {
    整64 量;
}

函数 货舱统计(整64 包, 整64 货舱依赖表) -> 整64 {
    返回 包 + 货舱依赖表;
}

函数 主() -> 整32 {
    整64 包 = 3;          // 包 作变量名
    货舱 总量;            // 货舱 作结构体类型名
    总量.量 = 4;
    打印(货舱统计(总量.量, 包));
    返回 0;
}
