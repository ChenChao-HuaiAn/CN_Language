// E2E 209_v2（v2 负测，2026-09-11 第六十七轮 plans/019 阶段4' A1 方案A 用户批准）：
//   v2 侧字符串借出初始化拒绝（与宿主 209 同源语义）——指针下标/成员借出
//   （v2 语法面：无栈数组，借出载体=字符串* 指针下标）双形态诊断+语义中止。
结构体 条目 {
    字符串 名;
}
不安全 函数 借出读(字符串* 池) -> 空类型 {
    字符串 s = 池[0];              // 坏1：指针下标借出
    打印(s);
}
函数 主() -> 整32 {
    条目 t;
    字符串 b = t.名;               // 坏2：成员借出
    打印(b);
    返回 0;
}
