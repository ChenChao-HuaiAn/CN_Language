// 179：构造字面量穷举纪律负测（2026-09-09 用户裁决，Rust E0063 同构）——
//   类型{字段=值} 必须穷举全部字段，缺字段=编译错误（修复前静默零填）。
//   未知字段/重复字段拒绝为既有诊断（semantic_expr visitStructInitExpr）。
结构体 点 {
    整32 x
    整32 y
}

函数 主() -> 整32 {
    点 a = 点{ x = 1 };
    返回 a.y;
}
