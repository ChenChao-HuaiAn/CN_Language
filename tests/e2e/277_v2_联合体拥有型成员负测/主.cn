// E2E 277_v2（v2 负测，2026-09-14 第一百六十四轮 164-a A4·plans/023 §十二 方案A）：
//   联合体成员为拥有型（字符串）→ v2 语义层硬错误（宿主 isTriviallyCopyable 同构）——
//   防 v2 侧放行同缺陷形态（p13 泄漏/p14 误释放 UAF 的编译期结构性挡死）。
//   期望诊断（主.expected 固化）：语义错误中止、不产 asm（v2闭环.txt 退出码=负）。
联合体 数值 {
    字符串 名;
    整64 数;
}
函数 主() -> 整32 { 返回 0; }
