// E2E 458_构造名裸名拒绝（474-a D46 销项·v2 侧持久门禁）
// `返回 正常;`/`返回 错误;` 构造名裸名：71-a 被调标识符放行=调用位专属·裸名
//   位经同一路径静默过→产物垃圾（p8 探针双侧实锤：宿主 check rc=1 拒绝·
//   v2 rc=0 静默）→474-a 返回语句位单点拒绝（构造须带参调用）。
//   宿主同形态已拒（函数指针转换错形态·24 族先例）——本用例=v2 侧同源防线。
// v2 负测（v2闭环.txt 退出码=负·v2p sem5 拒通道）；双诊断行号不精确锚定。
函数 取值(整32 源) -> 结果<整32, 整32> {
    如果 (源 > 3) {
        返回 正常;
    }
    返回 错误;
}

函数 主() -> 整32 {
    结果<整32, 整32> r = 取值(5);
    返回 0;
}
