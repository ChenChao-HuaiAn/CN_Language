# v2 闭环用例配置（474-a D46 销项·构造名裸名 v2 侧持久门禁）：
#   `返回 正常;` 裸名=编译期拒绝（宿主同形态已拒·函数指针转换错形态）——
#   71-a 被调标识符放行的裸名位漏洞封堵。构造须带参调用 正常(值)/错误(码)。
#   v2p 编译须失败（退出码非 0=负）——双诊断行号不精确锚定（防线演进容错）。
退出码=负
源文件=主.cn
借链=否
供给=
