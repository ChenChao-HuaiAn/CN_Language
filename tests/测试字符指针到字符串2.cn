// 测试字符*到字符串的隐式转换

公开:

函数 主() -> 整数 {
    // 测试空类型*到字符*的转换
    空类型* ptr = 分配内存(100);
    字符* buf = ptr;
    
    // 测试字符*到字符串的隐式转换
    // 根据设计文档，字符*应该可以隐式转换为字符串
    字符串 s = buf;
    返回 0;
}
