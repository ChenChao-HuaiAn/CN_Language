// 测试全局结构体数组运行时初始化

结构体 测试结构 {
    字符* 名称;
    整数 值;
};

// 全局数组（不初始化）
测试结构 全局数组[2];
布尔 已初始化 = 假;

// 运行时初始化函数
函数 初始化数组() -> 空类型 {
    如果 (已初始化) { 返回; }
    全局数组[0].名称 = "测试1";
    全局数组[0].值 = 1;
    全局数组[1].名称 = "测试2";
    全局数组[1].值 = 2;
    已初始化 = 真;
}

函数 主程序() -> 整数 {
    初始化数组();
    返回 0;
}
