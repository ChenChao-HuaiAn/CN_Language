// 测试枚举导入访问语法

// 从词元模块导入枚举类型
从 ../src_cn/词法分析器/词元 导入 { 词元类型枚举 };

函数 主程序() -> 整数 {
    // 测试跨模块枚举访问
    词元类型枚举 类型 = 词元类型枚举.错误;
    
    // 枚举值可以直接比较
    如果 (类型 == 词元类型枚举.错误) {
        返回 1;
    }
    返回 0;
}
