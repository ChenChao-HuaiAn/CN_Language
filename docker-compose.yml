version: '3.8'

services:
  cn-lang-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: cn-lang-dev:latest
    container_name: cn-lang-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash
