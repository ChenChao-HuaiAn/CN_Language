007-P3 收尾 C2 接口传参锁定 + D 接口去虚拟化与 --cfi（E2E 109/109、单测 1196/1196、性能≈1.04x 全绿）

C2 接口类型作为函数参数/返回（006 P3-19，多态传参）：
- 实证指针/值参数、值返回、链式、成员方法形参、继承实现类全可用（A1 同地址，零调整）——
-  e2e 105_接口参数多态 锁定；规范 skill 06-六 +《001》补句；排除 `&类变量` 取地址既有边界。

D 接口调用去虚拟化 + CFI（006 性能路线，最小实现）：
- D3A 去虚拟化：ClassInfo.ifaceNames 登记（含继承链并入）+ interfaceImplClasses 语义统计
-  （跳过抽象类）→ ir_oop_call handleInterfaceCall 唯一实现时接口调用点直接 Call 实现类方法
-  （省 1 次间接；多实现回退既有 CallIndirect，语义不变）；IR 单测 InterfaceDevirt* 断言直调/间接。
- D4 CFI：`--cfi` 开关（默认关保 1 间接）——接口间接调用分派目标 ∈ 已知实现目标表 且 非空，
-  否则 __cn_runtime_error(3)；emitCfiCheck 照 emitBoundsCheck 错误块模式。driver/cn_main 透传。
- 延后：`最终` sealed（D3B）、类级单派生去虚（D5）——设计见 plans/008。
- 性能：scripts/bench_self_host.py CN≈1.04x 不劣化，自举逐字节一致。

e2e：102/103（引用返回）、104/107（实例方法作值）、105（接口传参）、106（去虚拟化）全绿；
文档四件套：006/007/008、更新日志、README（--cfi/接口）、HANDOFF、lessons。