# AGENTS.md — CN 语言编译器项目规则（ZCode）

> 本文件是 ZCode 工作区指令文件：ZCode 原生自动加载项目根目录 AGENTS.md（无需插件），
> 用户级默认指令在 `~/.zcode/AGENTS.md`，本文件后加载、可覆盖其默认。
> 历史规则体系 `.ai-coder/`（旧 aicode 客户端，含其 AI 记忆体 memory.db）自 2026-09-03
> 起不入库（gitignore，本地保留供 aicode 软件使用）——其 25 条 user_rules 与项目规则
> **已提炼并入本文**（16/17 条 Orchestrator 协作规范经用户裁决不适用于 ZCode，未并入；
> 项目收尾机制以 §5 门禁为准）；error-detection.md 规则随
> `.zcode/skills/ai-error-record/` 技能自包含。
> 历史记忆体系见`项目记忆/教训.md`（任务前必读·L1 语义记忆主文件·426-a 前为根目录 项目记忆/教训.md）；项目技能集在 `.zcode/skills/`（cn-language-spec 等）。
> 语法规范以 plans/001 与 cn-language-spec 为准。
> 本文件只写规则、不记进度：项目进度与现状一律以 plans/ 内对应任务文档（随任务创建，不固定某一份）、
> `交接.md` 与 **`plans/021-任务进度观察表.md`（任务进度唯一总入口：总目标/当前进度/排班队列/
> 里程碑，每轮必更）** 为准。

## 1. 项目是什么

全中文语法的系统级编程语言（C++ 风格）编译器，从零用 C++17 实现，终极目标是**真正自举**：
**词法 → 语法 → 语义 → IR → 优化 → 代码生成**（Win x64 MASM / Linux ARM64 GAS 双后端）。

**当前正处在 v2 自举重建阶段（2026-08-25 起）**：v1 自举组件（`CN语言编译器/`）因"字符串行"中间表示
性能/内存失控被**推倒重建**为 v2（`CN语言编译器v2/`）：结构化中间表示（Token/AST/IR 全用结构体数组）、
arena 一次性分配、字符串驻留（Symbol 整数 ID）、哈希符号表，对标 rustc/LLVM。

**v2 自举双目标（用户令，2026-09-01）**：① **性能超过 C++ 编译器**；② **安全接近 Rust 语言**
（内存安全：悬垂指针/越界访问/泄漏 零容忍，见 项目记忆/教训.md 各实测案例）。v2 全部设计与实现决策
（架构、IR、代码生成、stdlib 交互）必须同时以这两条为准绳，二者冲突时向用户汇报裁决。

- 规格书：`plans/001 CN语言编译器设计规格书.md`（语法规范唯一权威）
- 实施计划：`plans/002*.md`（阶段拆分，复选框跟踪）＋专项：`plans/013-自举重建-性能最优架构.md`（v2 架构设计）、`plans/014-...-宿主缺陷根治与P4重做-阶段状态与计划.md`（v2 推进状态）
- 分析报告：`plans/003`、`plans/004`、`plans/011`（E2E实测与C++/Rust对比）
- 经验教训：`项目记忆/教训.md`（高权重条目优先避坑）｜ 交接：`交接.md`（会话结束前必须更新）

## 2. 任务开始前（强制）

1. **先查规范再动手**：任何与 CN 语言相关的任务（语法/语义/IR/代码生成/标准库/测试用例/缺陷修复）
   必须先调用 `cn-language-spec` skill 查询语法规范，或查阅 `plans/001` 规格书。不得凭记忆编造语法。
   **文档/呈报/方案中的 CN 示例代码同样受此约束**：① 必须符合规范（构造一律用规范语法——结构体
   `类型{ 字段 = 值 }` 构造字面量〔字段穷举〕、类 `类名(实参)` 构造调用、结果/可选 `正常/错误/某些/无`）；
   ② **禁止自造语法或自造「构造名」**（如把工厂函数起名成看似构造语法的形式而不加说明）——示例中的
   辅助函数必须命名清晰并注释声明「示例自定义普通函数，非语言构造语法」；③ 示例必须**逐块实测**
   （`cn.exe check`/`build`），文档标注实测口径与结果（负例须标注「预期失败」并随注实测诊断原文）。
2. **了解项目现状**：先读 **`plans/021-任务进度观察表.md`（任务进度唯一总入口：总目标/当前进度/
   排班队列/里程碑）**，再读 `交接.md`（本机节 → 深度机节）与根目录 `三机任务看板.md`；
   再读 **`plans/025-三机任务统筹与实施计划.md`（三机在飞任务明细：谁在做什么/写集/步骤/交叠/欠账）**；
   随后按需读 `plans/` 专项（尤其 plans/013、plans/014；历史文档先看 plans/021 §6.1 索引状态——○历史/⚠superseded 不作现行依据）；
   阅读 `项目记忆/教训.md` 高权重区吸取教训（高权重条目必须提及预防）。**文档读策略=三层记忆模型**（§6.7）：
   L0 整读（交接.md/看板/plans/025）→ L1 按需（项目记忆/教训.md/021/020 矩阵/019）→ L2 不读（项目记忆/归档/·考古才查）。
3. **新增语法规范须审批**：新增语法规范（新关键字/新结构/语义变化）必须先暂停任务征求用户意见，
   同意后才能写入 `plans/001` 与 cn-language-spec；未经同意不得自行添加、不得以"兼容旧行为"等理由绕过。

## 3. 开发纪律

- **尽善尽美，禁止妥协（最高优先级，2026-09-13 用户令深化）**：所有任务必须一步到位、全链路完整实现，
  绝不为赶进度简化或妥协。例：自举中发现宿主编译器缺陷时，**必须暂停自举、优先根治宿主编译器
  （src/cn_compiler），禁止用牺牲性能/正确性的规避手段凑合**（如为绕过宿主缺陷而降低自举编译器性能）。
  - **折中即妥协（判定清单，一律禁止）**：以「保守分支／宁漏勿错／暂时保守／巧合正确／维持现状＋登记／
    既有边界／下轮再说／范围外／影响面大／风险可控」等名义，搁置或降级任何**已确认**的缺陷、缺口、
    语义不完整（含「半截机制」——只覆盖部分位置/部分类型/部分路径的设施），都是妥协。
    已确认缺陷按「缺陷零容忍·强制汇报」立即暂停、呈报、根治，不得自行搁置。
  - **双目标优先于进度**：v2 双目标（① 性能超过 C++ 编译器；② 安全接近 Rust——悬垂指针/越界访问/
    泄漏 零容忍）是全部设计与实现决策的最高准绳。凡能提升编译器性能、增强语言安全的改动，
    必须进行到底（十全十美）；**不得以工作量大、涉及推倒重来、需要跨多轮为由降级方案**。
  - **推倒重来授权**：既有实现若不完美（半成品、临时手法、类型专用补丁、按文本判定的脆弱实现、
    只覆盖部分位置的设施），其**原设计目标的完整体**优先于「保留现状」——允许推倒重做。但必须：
    ① 涉语言级语义/规范新增时先呈报获批准；② 保留既有门禁全绿（等价性或改进性证据：产物逐字节
    一致／性能锚／反证实验）；③ 走完整验证链（单测＋E2E＋锚定链＋对拍＋跨机实物）。
  - **呈报必须给最完美方案（禁止折中推荐）**：给用户的方案必须是最彻底、最完整的那个；若列多方案
    对比，**推荐项必须是最完美者**，折中项只能作为对比存在、不得作为推荐。「先做一半」「维持现状＋
    登记观察」不是方案——分步只能作为实施路线，且每步自身完整闭环（不得在任一步留下已知不完美）。
  - **诚实边界 ≠ 放弃**：可用「诚实边界」如实说明**某轮**未覆盖的面，但每项边界必须同时登记为
    带根治计划的任务（`plans/021` §3 排班队列）并按期销项，不得让「边界／观察项／靶子」长期挂账。
    自检问句：「这条边界什么时候关死？关死之前它算不算妥协？」
  - **本条的完整释义与执行细则**（所有权/安全体系的目标蓝图、三项合一的收官方案、波次路线、
    十全十美验收标准）：见 `plans/022-所有权与安全体系收官方案.md`（本项目「不允许妥协」的落地范本）。
- **缺陷零容忍·强制汇报（最高优先级，用户令 2026-09-01）**：出现缺陷——**不论何时遗留、
  不论是否阻塞当前任务**——必须**立即暂停任务**，向用户汇报（现象/根因/影响面），并给出
  **至少 3 种**解决或优化方案（含推荐项与代价分析），经用户选择后方可继续。
  禁止以「顺带」「不阻塞」「下轮再说」「范围外」等理由自行搁置或延后任何已确认缺陷。
  （2026-09-18 用户裁决补充·三机并行口径：呈报时机分级=§8.4——**语义/规范级缺陷维持本条**
  「先呈报后修」；**机制级缺陷**〔实现 bug·根因唯一·不新增语言语义〕改为「当场开修复分支修复、
  修完随证据备案」——呈报义务不减，时机后移至修复完成时。）
- **宿主缺陷优先**：遇宿主（编译器/工具链）缺陷立即暂停当前阶段任务，先根治宿主；已知缺陷必须全部根治后再推进自举重建（见 plans/014 排序修正）。
- **单一职责**：每个源文件 ≤1000 行；每个函数 ≤100 行。超出拆分为子模块目录（`词法/`、`语法/` 等），并删除旧文件。
- **中文注释**：代码注释、提交信息、更新日志均使用中文。
- **模块化**：frontend → ir → opt → codegen 单向依赖（禁止反向），driver 是唯一组合入口。
- **技术路线**：遇路线选择时，优先给出 Rust 语言的解决方案供用户选择。
- **不轻易创建新文件/新目录**，除非用户要求。
- **报告类产出必须等用户批阅**：分析报告、架构设计、实施计划等交付后暂停，用户同意后才继续执行。
- **一劳永逸**：发现问题当场解决，不留到以后（后面会忘记）。

## 4. 测试纪律（E2E 先行）

- **E2E 先行**：每个功能先写 E2E 用例（`tests/e2e/NN_名称/`），测试通过才算完成；禁止 `.skip()`，所有测试必须真正运行，使用真实配置（不用 Mock）。
- **测试文件不可擅自修改（最高优先级）**：测试文件一经验收，AI 不得擅自修改——
  除非文件内容存在语法错误（此时可直接修正），其余任何情况下必须先暂停任务、向用户报告修改原因与修改目标，征求用户同意后才能修改。
  测试文件中添加调试语句（不改变断言语义）定位问题是被允许的。
- 新增 E2E 用例目录约定：`tests/e2e/<编号>_<名称>/<用例>.cn` + 同名 `.expected`（逐行比对）；
  多文件用例入口必须叫 `主.cn`（定义 `函数 主() -> 整32`）；可选 `.input`（标准输入）与 `.args`（每行一个参数）。
- **防虚假验收**：测试必须走真实生产链路（编译→链接→运行→比对），禁止直接构造组件实例绕过编译流程；
  编译产物（`.exe/.asm/.obj`）一律放 `target/`（已 gitignore，不进版本库），不得散落工作区。
- **中间产物与实验清理**：调试中间产物、实验性脚本/临时用例，任务完成时必须删除；
  确有保留价值的，转正为正式 E2E 用例（`tests/e2e/`）或移入专门位置，禁止以「临时/实验」名义遗留在工作区。
- 缺陷修复后，必须把 `plans/*` 与 `项目记忆/教训.md` 中对应条目**显著标注为已修复**（避免后续 AI 误判为未修复）。

## 5. 构建与验证门禁（提交前必须全绿）

Windows（本机默认，需 VS2022 MSVC 工具链）：
```powershell
powershell -ExecutionPolicy Bypass -File scripts/ci.ps1        # 构建(零警告)+单测+E2E 全量门禁
powershell -ExecutionPolicy Bypass -File build.ps1 -Test       # 构建 + 测试（等效）
# 手动分步：
cmake -S . -B target/build && cmake --build target/build --config Debug
target/Debug/cn_unit_tests.exe                                  # 单测
python tests/e2e/run_e2e.py --cn target/Debug/cn.exe            # E2E 全量
python tests/e2e/run_e2e.py --filter 01_hello --verbose         # 单用例调试
```

Linux ARM64 后端验证：`python tests/e2e/run_e2e.py --target linux-arm64 --cn <编译器>`（需对应构建）。

门禁要求：CMake 零警告（MSVC `/W4 /WX`，GCC `-Wall -Wextra -Werror`）+ 全量单测 PASS + 全量 E2E 全绿。
**任何代码修改后必须编译验证通过才能宣告完成；未验证不得声称"任务完成"。**

## 6. 提交与文档同步（每次提交前）

1. **Git 提交信息必须用中文**，格式参考 `阶段X TaskY：描述`。
2. **更新 `更新日志.md`**：按机器分节覆盖写入**本机节**本次修改内容（节内格式以 `## 功能完善总结`
   开头、只保留本机本次说明；他机节不动——见 §8.5，2026-09-12 起取代原「整体覆盖写入」规则），提交前必须更新。
3. **plans 文档实时打勾**：完成任务对应 Task 复选框，并确认 plans/001 里程碑与 plans/014 状态表同步；
   **同时更新 `plans/021-任务进度观察表.md`**（勾销排班项/调整队列/追加里程碑/刷新文首「最后更新」戳）
   ——总表落后即视为文档漂移，先修表再推进；自检：`python3 scripts/check_progress_sync.py`。
4. **交接.md**：会话结束前写交接文档（做什么/已完成/发现的问题/卡点/下一步/踩过的坑），写给无上下文的新会话看；
   按机器分节、每机只整体替换**本机节**（见 §8.5）。
5. 推送远程仓库：远程固定两个、三机同名——`gitcode`（**主仓库**，gitcode.com）与 `github`
   （**镜像仓库**，github.com），默认分支 `develop`，不设其他 remote 名。**每次提交后两个远程
   同时推送**（`git push gitcode develop` 与 `git push github develop`）；github 当时网络不好
   推不动属正常，说明一下即可、下次提交再补推。若某机 remote 名不符（如残留 `origin`），
   先 `git remote rename` 统一为上述两名。（v2 协议下 develop 推送须经 `scripts/integrate.py`·§8.2；
   任务分支只推 gitcode。）
6. **共享文档结构门禁**：改动 `交接.md`／`更新日志.md` 后，除 `check_progress_sync.py` 外还须运行
   `python3 scripts/check_handoff.py`（§8.5 共享文档编辑六纪律的机械自检）；不过不得提交。

### 6.7 文档生命周期（状态/事件分层·2026-09-19 用户裁决四点+开工令·426-a 立）

> 背景：教训/019/020/021 四个 append-only 台账曾无淘汰机制，膨胀至失控（教训.md 曾达 4662 行/881KB
> ——超出任何会话可整读规模；020 曾达 4862 行其中 97% 为逐轮过程段）。治理=**三层记忆模型**。

- **三层文档模型（读策略）**：**L0 工作记忆**（每轮整读·恒定）＝`交接.md`（原 HANDOFF.md）、
  `三机任务看板.md`、`plans/025`；**L1 语义记忆**（任务开始读+按需检索·**有界**）＝`项目记忆/教训.md`
  （高权重精华区+主题分节）、`plans/021`（排班+近 40 轮索引+近 15 销项）、`plans/020`（纯矩阵状态层）、
  `plans/019`（设计+近月记录）；**L2 情节记忆**（**默认不读**·考古才查·自由累积）＝`项目记忆/归档/`
  月切片＋git 提交历史。归档累积无害：不进上下文＋封存不改（git delta 友好）＋单片有界。
- **状态/事件分离**：活跃文档只承载**状态**（矩阵格子/活跃队列/有效教训）；**逐轮过程记录禁止写入
  状态文档**——020 禁写「第 N 轮更新」段（更新格子/防线/观察项合法）；过程记录合法落点＝`plans/025`
  收工回填＋git 提交信息＋`plans/019` §7 近月行。
- **月切片滚转**：滚转型文档（教训/019/020/021）超棘轮基线或月末收尾 → 当月增量切至
  `项目记忆/归档/<名称>-<YYYY-MM>.md`（纯中文切片名·来源映射由各主文件归档索引表承载）。
  **切片封存**：合入后不再改动（实质错误在主文件勘误节登记，不在切片改）。
- **教训淘汰三判据**（其一即退役迁归档·非删除可找回）：①条目所述坑已被 `check_*.py` 门禁机械化
  拦截（「门禁替你记得」）；②已修复且确认（famscan 归零＋E2E 负测转正＋连续两轮无复发）；
  ③语义已被 plans/001/cn-language-spec/本文件收录。
- **棘轮门禁**：`scripts/文档棘轮基线.txt` 登记滚转型文档行数上限；`check_handoff.py` 校验当前行数
  ＞基线 → 拦截提交（**先滚转再追加**）。基线**只紧不松**：收割/滚转轮随写集收紧；放宽须在提交信息
  中说明理由。
- **plans/ 存量治理**：状态由 `plans/021` §6.1 全量索引承载（●现行/○历史/⚠superseded）；历史文档
  不逐轮维护、**原地 ADR 勘误**（不删不移·superseded=头部注记取代者与勘误一句话）；新计划不得写入
  ○历史/⚠ 文档——新计划一律新建编号文档或落 025/021。
- **plans/021 定位修订**（修订 §8.5 表内「只增不删」口径）：**活跃区**（排班队列+近轮索引+近月销项）
  只增不删；**历史区**（更早轮次行/销项行）月度滚转至归档切片。

## 7. 错误与诚实报告

- 工具/命令出错时：记录具体命令、完整错误输出、根因分析与预防措施到 `项目记忆/教训.md`（避免模板化描述，同一错误不重复记录）。
- 每次工具调用后检查结果；**未完成不得声称完成**。
- 完成前确认：所有子任务完成、所有测试通过、plans/更新日志/交接 已同步、无未处理错误。

## 8. 三机并行协同协议 v2（2026-09-18 用户批准实施·328-a 落地：分支即开发·队列即集成）

三机（家机 win-x64 / 深度机 linux-x86_64 / 单位机麒麟 ARM64）并行工作时遵守本协议。
目标：并行开发/验证**零 git 冲突、零「结果打架」**，且**三机可同时开发**。
核心思想：**分支即开发 + 队列即集成 + 门禁即审查 + 缺陷分级修复 + 文档随分支走 + develop 恒绿**。

（沿革：2026-09-12 立 v1 协作协议 → 2026-09-15 试「多机写集不相交并行直推 develop」失败
〔写集不相交只防 git 文本冲突，防不住语义耦合/验证基线漂移/多锁一致性真空——项目记忆/教训.md 211〕→
2026-09-16 用户裁决回退单锁〔231-a/232-a〕→ **2026-09-18 用户批准 v2**：三大根因各有业界
成熟解、无需退回单锁——①语义耦合→门禁跑在**合并结果**上；②基线漂移→集成时强制 rebase 最新
develop；③一致性真空→develop 唯一事实源+串行集成。业界对照=rustc/bors 合并队列+try build、
Chromium Commit Queue、Google trunk+revert 文化、GitHub merge queue。**方案全文、判定依据与
试点记录=plans/027**。）

**六条总纲**：

1. **分支即开发**：废除开发锁。每机每轮一条任务分支，推分支到 gitcode=认领生效，三机可同时开发互不干扰（§8.1）。
2. **队列即集成**：develop 是唯一事实源，入 develop 必须经 `scripts/integrate.py`（脚本化合并队列），全表至多一个集成在飞（push 原子 CAS，输家自动 rebase 重试，§8.2）。
3. **门禁即审查**：门禁跑在**合并结果**（rebase 到最新 develop 之后）上，不在分支孤证上（§8.2）。
4. **修复权分级放开**：机制级缺陷任何机当场开修复分支、修完随证据备案；语义/规范级先呈报三方案经用户裁决再动（§8.4）。
5. **文档随分支走**：共享文档开发期改动全进分支，集成时随代码串行合并——共享文档并发写归零（§8.5）。
6. **develop 恒绿 + fix-forward**：谁的分支谁负责到集成；集成后 develop=全员共有，红了全员停、破坏者优先修、任何人可开修复分支（含 revert）（§8.6）。

**两态角色**：**开发轮**（在自己的任务分支上，**可多机并行**）｜**服务轮**（探针检测/E2E 测试/
try-build 应答/集成后验证——免分支免轮次号，仅写 §8.5 窄通道）。v1「先到先得优先级」**作废**：
按任务量自选组合（3 开发、2 开发+1 服务均可）；B8 面台账巡视仍为服务轮默认动作（B9 族扩散
检验同维持，见 §8.3 末条）。

### 8.1 分支即开发（开发轮认领=推分支）

- **分支命名**：`任务/<机>-<轮次>-<标识>`（机∈{家机,单位机,深度机}；如 `任务/家机-330-C26`）。
  轮次号纪律（**2026-09-18 用户裁决方案①：认领前先看板、先到先得·341-a 落地**）：`git fetch` 后以
  **远端 develop 看板在飞行的轮次号**为占用权威（不看 git 历史——历史滞后于并行现实），取
  **未占用**的号（可跳号，深度机惯跳 2~4）；**分支名远端唯一**仍防同机重复取号（推分支被拒即撞号，
  当场换号重推——185-a 教训）。**跨机同号由看板先行天然拦截**：先直推看板行者占号生效，后行者
  fetch 即见、当场换号；check_handoff「在飞轮次号唯一性」检查机械兜底（看板两行同号即报错——
  332-a 实证「全表最大+1」三机并行必撞：家机/单位机双 329-a）。
- **看板先行（用户令 2026-09-18·332-a，强制第一动作）**：开工的**第一动作**＝`git fetch` → 定分支名
  → **直推看板本机行**（🏃+`任务/…` 分支名+一句话）到 develop（§8.5 窄通道①），**然后**才开分支动手
  ——看板是本机在飞的**唯一实时广播**，写进任务分支＝他机看不到（332-a 实测：三机看板行均只存在于
  各自分支，远端 develop 三行全空，深度机 try-build 请求因此无人应答；家机/深度机在飞亦只能靠
  `git show <分支>:三机任务看板.md` 才读到）。**收工销账**：集成成功后**立即**把本机行清空
  （⬜+各列「-」）并直推 develop，不得留到下一轮或他机开工时才清。
- **认领=推分支**：`git fetch` → 看板行已直推（上条）→ 开分支 → 六要素计划落盘 plans/025 本机节
  （§8.7）→ 首次提交并 push 分支到 gitcode——**push 成功=认领生效**；分支未推=未认领（他机不感知）。
  plans/025 §〇 预登记表仍用于防两机撞同一队列任务项。github 镜像只推 develop，**任务分支不推镜像**。
- **看板行格式**：🏃+分支名+一句话（**纯信息板，无锁语义**；check_handoff.py 校验 🏃 行必含
  `任务/` 分支名）。**机械门禁（332-a 增）**：check_handoff.py「开工自检」——当前分支为 `任务/*`
  时，看板文件必须已含该分支名（＝开工前已直推看板行），否则报错拦截（「先动手后填板」不可通过
  门禁；跨机 try-build 在他机分支上跑同样满足）。收工清行规则不变（⬜+各列「-」，232-a「有任务
  才填行」维持）。
- **分支生命周期**：单分支 ≤1 个工作日，长任务拆多分支接力；**每日至少 rebase 一次 develop**
  （防长分支漂移——持续集成本义）。
- **修复分支**：任何机（含服务轮当场的发现）可从最新 develop 开修复分支（缺陷分级见 §8.4）；
  紧急时可**基于他人在飞分支**开自己的修复分支——**禁止直接改他人分支、禁止 force push 他人分支**。

### 8.2 队列即集成（develop 唯一入关口=scripts/integrate.py）

集成=收工的最后一跳。**一切入 develop 的行为面改动必须经 `python3 scripts/integrate.py`**
（脚本固化流程杜绝手滑；在任务分支上运行。步骤=）：

1. **前置自检**：当前分支名合法（§8.1 格式）、工作树干净、分支已推远端；
2. `git fetch`；
3. **rebase 最新 develop**——冲突=分支主人在自己分支上解（脚本自动 abort 并报告），
   **禁止在 develop 上手解冲突**（233-a 冲突标记入库事故铁律）；
4. **写集分类**（脚本自动判定，规则见 §8.3）：三平台共享层改动→须完成 try-build 并以
   `--try-build-done` 声明后方可继续；
5. **全量门禁（跑在合并结果上）**：冲突标记 grep＋check_handoff＋check_progress_sync＋写集核对
   （`git status` 出现非本轮写集文件=事故信号·项目记忆/教训.md 218）；写集触及 `src/` 或 `tests/` 时另跑
   零警告构建＋单测＋E2E 全量（纯文档/平台无关脚本写集=快速门禁）；
6. `git push gitcode develop`（**ff-only 保线性历史**）——被拒=他人刚集成，回到 2 自动重试 ≤3 次
   （重试 rebase 带入新的 src/tests 提交时，全量门禁重跑；纯文档增量只重跑快速门禁）；
7. 推送成功→补推 github 镜像（失败仅警告、下次补推）→看板通告段广播影响面（哪些平台需集成后验证）。

**全表至多一个集成在飞**：push 本身即原子 CAS（v1 看板锁洞察的延续），竞争输家自动重试；
集成前在看板通告段标「集成中」可减少撞车。**禁止 force push 共享分支 `develop`**（铁律保留）。
78/79 OOM 等平台固有败面与并行无关，按已知败面口径处理，不算打架。

### 8.3 三平台分层验证（按风险分层 try-build·2026-09-18 用户裁决）

- **try-build 触发面（写集自动分类）**：改动触及 `src/**`、`tests/**`、`CMakeLists.txt`、`build.ps1`、
  `scripts/ci.ps1`（=三平台产物相关）→ **三平台 try-build**；纯文档与平台无关 python 脚本
  （`plans/**`、`*.md`、`scripts/*.py`）→ 本机门禁即可（他机 pull 后首次运行自然验证，另有
  集成后验证兜底）。
- **try-build 流程**（rustc/bors try build 同款）：集成分支推远端 → 看板通告段点名另两机 →
  另两机 `git fetch` 该分支、**在分支上**跑各自平台全量门禁、通告段**回签**（平台+结论+基线
  commit）→ 全绿才准 `integrate.py --try-build-done` 集成。历史跨平台漏修（T44/T27 只修 win
  漏 x64l 类）在此模式被前置拦截。
- **try-build 应答超时**（>2 小时无应答）：集成**挂起**并在通告段说明，机器上线即续——
  **不降级、不绕过**（分层裁决的严格执行）。
- **集成后验证常设**：develop 每前进，另两平台机做增量验证（B8 采样/回归轨/全量门禁——既有
  跨机复验惯例的制度化），发现红→通告+修复分支/revert。
- **B8 面台账巡视**（服务轮默认动作）与 **B9 缺陷族面扩散检验**（`scripts/famscan.py`·根治轮
  复跑归零+命中形态 E2E 转正）维持原职责挂点不变（规则原文见 plans/021 相应节与 2026-09-16/17
  批准记录）。

### 8.4 缺陷分级处置（2026-09-18 用户裁决·修订呈报时机）

- **机制级缺陷**（实现 bug、根因唯一、不新增语言语义——如代码生成错指令/解析漏分支/优化错值）：
  发现机**当场开修复分支**：根因分析＋B9 famscan 复跑归零＋负测/E2E 转正＋全量门禁 → 走合并队列。
  呈报改为**修完随证据备案**（现象/根因/影响面/修复方案——plans/021 对应行随分支更新，不再
  阻塞等裁决）。
- **语义/规范级缺陷**（语言语义、新关键字/新结构、规范缺口、架构决策）：**先呈报三方案+推荐项，
  经用户裁决后**开分支实施——原「缺陷零容忍」流程不变（§3 注记同源）。
- **判定标准**：修复会改变 `plans/001` 规范条文/新增语法=语义级；其余=机制级；**判定模糊按
  语义级**（宁严勿松）。测试文件不可擅改等纪律不变。

### 8.5 共享文档：随分支走 + 直推窄通道

**开发期一切共享文档改动随任务分支走**（plans/025 六要素+收工回填、交接 本机节、更新日志
本机节、plans/021 改动、项目记忆/教训.md 追加），集成时随代码串行合并入 develop——**共享文档并发写从根上
消除**；分机分区规则保留，语义从「并发纪律」升级为「合并友好结构保证」（不同区域 git 三方合并
自动成功）：

| 文件 | 分区方式 |
|---|---|
| `三机任务看板.md` | 三机并行唯一看板（根目录，任何任务通用），每机只改自己行与自己通告段 |
| `plans/025-三机任务统筹与实施计划.md` | 在飞任务明细：§〇 总览每机只改本机行；§一/§二/§三 每机只写自己节（§8.7） |
| `交接.md` | 按「家机win / 深度机 / 单位机」三节，每机**整体替换自己节**，他机节不动 |
| `plans/019` 跟踪表 | §7.1/§7.2/§7.3 按机器分段，**各段末追加**本机轮次行 |
| `更新日志.md` | 按机器三节，每机节内覆盖写入本机本次说明（§6.2 新规则） |
| `项目记忆/教训.md`（原 lessons.md·426-a 迁入改名） | 追加段天然分 hunk；棘轮基线约束总量（§6.7·超限先淘汰/滚转再追加） |

**禁止**跨区修改他机分区内容；确需修正他机内容（如发现事实错误），须在自己的任务分支上进行，
并在提交信息中说明。

**直推 develop 窄通道**（不经 integrate.py，push 前 `git fetch`+rebase 复推兜底）：
① **看板本机行+通告段——强制直推、即改即推**（2026-09-18 用户令·332-a）：**开工先推本机行
（🏃+分支名）→ 收工即推清行 → try-build 请求/回签即发即推**；这三类信息写进任务分支＝他机
不可见＝等于没写（332-a 实测：三机行只在各自分支内，远端 develop 全空，深度机 try-build 请求
无人应答）。② **服务轮写面**＝tests/cnsmith_hits/面台账/plans/021
追加行/交接 本机节（探针检测轮/E2E 测试轮既辖）。**服务轮同轮多结论合并为一次提交**（治
281-a 同信息 30 连推噪音）；验证结论必须标注基线 commit。

**看板结构（332-a 变更）**：看板表为**每机独立小节**（`### <机名>` + 4 列单行表·不再单表三行
相邻）——各机只编辑自己小节，小节间 4+ 行间隔使 git 三方合并不再因相邻行改动冲突（332-a 实测：
单表三行结构下两机同时更新各自行，rebase 必冲突·当日两次；结构变更广播见看板通告段）。

**plans/021 更新时机（v2）**：① 开发轮**集成时**随分支合并（销项/里程碑/文首戳——集成串行天然
防冲突）；② 服务轮向 §三追加待定项（最小局部编辑·禁止整节重排/格式化）。**§五轮次索引按轮次号
排序插入**（降低分支合并时的相邻行冲突）。**§三-B 生命周期铁律**（2026-09-16 用户令·B5 三犯后立）
与**空白铁律**（C21）：B 区条目一经用户裁决必须当轮迁出（已明确实施迁 §三-A、已定方案待排程迁
§三-C、完成销项进 §4.2 台账），本区禁止残留任何已裁决内容及「原文留档」，无条目时须完全空白
（区内非空行必须 `> **B` 开头）——check_handoff.py 已机械化拦截。呈报类条目（含经看板通告段
发起的呈报）必须同步登记本区。

**共享文档编辑六纪律**（2026-09-16 增补·用户令；交接.md 指数膨胀事故后立——「锚点局部插入＋
旧文保留」曾把该文件逐轮翻倍至 642 处重复标题／9268 行，事故已入册 项目记忆/教训.md）：

1. **锚点=节标题，整节替换**：更新分机分区（交接.md／更新日志.md 等）的**唯一合法编辑方式**＝
   old_string 与 new_string 都从「`## 本机节` 标题行」起、到「下一个 `## ` 标题前」止，整段替换；
   **禁止**以节内任意行（尤其上轮记录的行尾句）为锚点做局部插入——那会保留旧文，把「替换」退化成「追加」。
2. **一分区一记录**：分机分区任何时刻只保留**最新一轮**的完整记录，上一轮记录随整节替换自然消失
   （历史由 git 历史＋plans/019／plans/021 承载）；禁止「新轮记录照写、旧轮记录也留着」。
3. **标题必须行首独立成行**：`#` 标题前必须换行、独占一行；「正文。### 标题」式粘连是本事故的
   直接签名——出现即说明编辑锚点选错，必须回滚重做，不得提交。
4. **写后即检（机械门禁）**：每次编辑共享文档后、提交前必须运行 `python3 scripts/check_handoff.py`
   （校验面=交接.md／更新日志.md／plans/021／三机任务看板四文件结构与冲突标记；任一不过
   禁止提交），与 check_progress_sync.py 同列 §6 提交前自检。**门禁覆盖面=出过事故的文件全集
   （项目记忆/教训.md 206）——新文件出现同类事故时必须同轮扩门禁，禁止「登记待办」。**
5. **diff 规模警戒线**：共享文档分区单次编辑若「只增不删」（新增 >100 行而删除 <50 行）或净增
   >+80 行，即是「追加而非替换」的事故签名——必须停下核对，不得直接提交（健康的整节替换
   删除≈新增，净变化在 ±60 行内）。
6. **模板复制必删旧**：以旧记录为模板写新记录时，new_string 里只准出现**一份**记录；编辑后若文件
   行数或标题数翻倍，立即回滚重做，禁止「先提交、下轮再清理」。

### 8.6 职权与 fix-forward（治「他机看到错误不修」）

- 「不在职权范围」概念**废除**，改为「**一切行为面改动必须走自己的分支**」。
- **分支所有权**：谁的分支谁负责到集成；集成后 develop=全员共有。
- 看到他机分支/develop 的问题：在看板通告段提醒对方；紧急时可**基于其分支**开自己的修复分支
  （禁止直接改他人分支）。
- **develop 红=全员最高优先**：发现者通告（看板+通告段），破坏者优先响应；超时未响应任何人可
  revert（revert 也走门禁+合并队列）。
- **契约变更广播**：改 `.expected` 期望文件 / E2E runner（`tests/e2e/run_e2e.py`）/ `plans/001`
  规范 / cn-language-spec 等**三机共享契约**时：必须在开发分支上进行（禁止服务轮顺手改），
  集成后在看板「契约变更通告」区本机段标注变更内容与影响面（哪些用例/哪些平台需复跑）；
  其它机器开工 fetch 即见通告，按标注复跑相关面而非盲目全量。通告由发起者维护：影响面执行
  完毕后（或最迟下一次本机 push）删除本机通告段。**看板表内不承载通告**（232-a 用户令维持）。

### 8.7 任务线实施计划落盘（强制，2026-09-15 用户令；v2 随分支）

**任何新任务线／新开发轮，必须先把「详细实施计划」落盘到 `plans/025-三机任务统筹与实施计划.md`
本机节，再开工**——目的：三机与用户随时看得清「**谁在做什么／怎么做／是否打架／有无遗漏**」。
未落盘=不得开工（分支首次 push 须已含该计划）。

实施计划**六要素**（缺一不可）：

1. **目标与验收判据**：本轮达成什么、以什么**实测证据**判定完成（可复现命令/断言）；
2. **写集（精确文件清单）**：本轮将改动的**全部入库文件**逐一点名（收工时回填最终清单）；
3. **实施步骤**：编号逐步，含关键设计决策与理由（**技术路线优先 Rust 对照**，性能第一、安全第二）；
4. **验证链**：命令级门禁（零警告构建／单测／E2E／锚定链／对拍／运行级／**反证**）；
5. **边缘情况与诚实边界**：开工前**预判** → 收工**回填实测**（不得只写「注意边界」这类空话）；
6. **与他机交叠分析**：与另外两机在飞任务（以远端分支+看板为准）的**写集交叠 + 语义耦合**判定
   （独立／互补／冲突）；冲突则先避让（改文件落点或压后）或在看板与对方协调，禁止带冲突开工。

纪律：

- 本表**每机只写自己节**，他机节只读；转写/修正他机节须在本机节注明理由并随提交说明；
- **收工**：本机节回填实测结论（随分支提交、集成时合并）→ **整节替换为下一轮计划**；**下一轮
  候选须预登记**（§〇 预登记表本机行，防两机撞同一队列项）；历史轮次明细由提交信息／`plans/019`／
  `plans/020`／`交接` 承载，不在本表堆叠；
- 本表 ≠ `plans/021`：本表=**在飞明细**（滚动替换），`plans/021`=**全局台账**（活跃区只增不删·历史区月度滚转至 `项目记忆/归档/`——§6.7·426-a 修订）；
- **§四 审计段**（交叠/遗漏/边缘情况）由持有在飞分支的机器在发现新交叠或遗漏时更新，更新处标注轮次。
