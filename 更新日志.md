# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段3b 语义面：双可变别名互斥 + 构造/方法调用面接入借用纪律（2026-09-10 第六十轮 深度机 linux-x86_64 · 宿主侧 v2 零改动）

### 一、语言语义增补

- **双可变别名互斥**：同一调用中同一变量被可变借用两次（两处 `T&` 形参位同基础名实参）→ 编译错误「被可变借用两次（别名可变冲突）」（Rust 纪律；存量预审全树零命中）。
- **构造/方法调用面借用纪律**：`常量 T&` 纪律（只读借出可变拒+互斥）自普通函数面扩至构造与方法调用面（三处接入）。

### 二、实施（宿主三文件）

- semantic.hpp：ClassMemberInfo.constParams；semantic.cpp：checkConstRefBorrowDiscipline 扩双可变；class_resolver.cpp：接口/类方法两处登记；semantic_call.cpp：构造（676）/方法（802/833）三处接入。

### 三、测试与验证

- E2E 202_双可变借用互斥（负测两形态：普通函数+构造面）+ 单测 ConstRefCheckTest 扩至 9/9。
- 清场重建零警告 + 单测 1283/1283 + E2E 218 用例 213 过（唯一败=194 既有待批）/4 跳。
- 本轮 v2 零改动：固定点维持 244605 行（锚定链免跑）。

### 四、阶段状态（plans/019）

- 已完成：阶段 1（显式转移）/阶段 2（引用逃逸）/阶段 3 第一版+3b 语义面——全部双侧（宿主+v2）或宿主全链落地。
- 3b 剩余另轮：NLL 跨语句活跃区间、v2 传可变/互斥面（随实参边界记录）、**转移浅拷贝优化**（IR 槽位交接=性能主项）。
- 阶段 4（不安全区）：细则呈报后开工（§5.3 呈报点）；阶段 4'（拥有型字符串）：阶段 3 后另案三方案呈报；阶段 5（并发）：依赖并发标准库立项（外部依赖）。
