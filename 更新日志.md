# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 426-a 开发轮：文档生命周期治理·结构骨架轮·2026-09-19·用户裁决四点+开工令「分轮次不停歇全部实施」）

- **三层记忆模型落地**：新建 `项目记忆/`（`教训.md`＝lessons.md git mv 迁入改名〔历史保留〕＋`归档/` 月切片目录·封存不改）；`HANDOFF.md`→`交接.md`（改名·git 历史保留）；AGENTS.md **§6.7「文档生命周期」**新条文（L0 工作记忆整读/L1 语义记忆有界/L2 情节记忆默认不读+状态/事件分离+月切片滚转+教训淘汰三判据+plans 存量 ADR 勘误+021 活跃区/历史区定位修订）+§2 读策略改三层加载。
- **020 滚转与根治**：4862→145 行——§四起 60 节逐轮更新段（4728 行·含 91-a 起非所有权面轮次·020 曾兼任轮次工作台账）整体滚转至 `项目记忆/归档/矩阵轮次日志-2026-09.md`（纯搬运零改写·行数守恒断言）；**主文件回纯状态层**（矩阵+防线+方法论+归档索引）+**禁写轮次段**新规；**表格结构缺陷修复**（B 表 5/7 列混态→统一 7 列·A#21 拆列·数据零丢失）。
- **021 滚转**：603→380 行——§五轮次索引留最近 40 轮（197 行迁归档）+§4.2 销项台账留最近 15 项（63 行迁归档）+**§6.1 新增 plans/ 全量索引**（40 份·●现行 15/○历史 24/⚠superseded 1）。
- **门禁机械化**：check_handoff.py **新增 查棘轮()**（`scripts/文档棘轮基线.txt`·教训 4662/019 524/020 200/021 450/025 700·只紧不松·超限拦截「先滚转再追加」）+双改名路径同步；新增 `scripts/check_own_matrix.py`（020 矩阵解析器：结构校验+靶子清单+v2 验证缺口——首跑即捕获 B 表 5/7 列混态存量缺陷）。
- **plans 存量**：002 总纲补「📌 文档状态」戳（历史快照）；012 superseded 原地勘误（字符串行中间表示已被 v2 结构化 IR 取代·不删不移）；全库路径同步 15 文件（脚本 4+技能 9+AGENTS）。
- **撞号处置**：原 425 与深度机 423 分支续作段号 425-a 撞（后到者顺延·先到先得）→改号 **426**（远端 425 分支已删·看板通告段广播+建议续作段号用「<分支轮>-c/d」续编防占位）。
- **验证链**：check_handoff ✓（含棘轮+撞号消解+交接/看板/021 结构）+check_own_matrix ✓（A/B 表 7 列统一）+check_progress_sync（收尾刷戳）+行数守恒断言（020：删 4728=切片正文 4728）。**教训.md 逐条分拣收割留 427 轮**（4662 行内容判读·棘轮已锁上限防涨）。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 388-a 事故恢复+389-a D45 定位·2026-09-19）

- **388-a 事故恢复**：387 收工内容因操作失误未经回签直入 develop（668d7b1）——恢复分支回退基线至 190f97e·等两机回签后 integrate；本机恢复态门禁=零警告+单测 1353+E2E 444=441/1/2（恢复正确性证实）；lessons 权重 10（push 前三问自查）。
- **389-a D45 定位**：D43 别名映射单点实证正确（5/1.500000）但三写法均触发 v2p 自身向量越界（gdb 栈=向量$节点$元素·组合二分锁定谓词调用链）——★D45 新立 P1·改动撤下（patch 备份）·根治后 D43 重打。
- 零行为面进 develop（389 源码撤下·388 恢复待回签）；文档面六件套同步。

**门禁**：check_handoff/check_progress_sync 绿；推 gitcode（分支+看板直推）。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
