# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 94-a v2 数组声明语法缺口（C6）销项——v2 栈数组全链（2026-09-13 第九十四轮，深度机 linux-x86_64）：
> 92-a 登记缺口（`浮32[2] 表;` 规格书 3.5 `类型[长度] 名` v2 解析报「语句缺少分号」——v2 语法面
> 无栈数组）本轮全链销项；**宿主侧零改动**（宿主数组支持完整=对齐参照：类型文本合成 `整32[5]` +
> typeSizeOf 数组分支 + registerVarSlots C 布局槽数 + 数组名退化元素指针）。

> **语法层**：①类型位 `解析类型文本` 加数组后缀消费（前瞻三 token `[` 整数字面量 `]` → 合成
> `"整32[5]"`，与宿主 canonical 同格式）；②声明位判定加 `标识符 [ 整数字面量 ] 标识符` 四 token
> 前瞻形态；③初始化列表 `= { e1, e2, ... }` 新增（`节点_数组初始化`(45) + **元素包装
> `节点_数组元素`(46)**——元素表达式长度任意，IR 层按包装节点可靠切分）。

> **语义层**：④`类型大小与对齐` 数组分支（总大小=元素大小×长度/对齐=元素对齐）；⑤`检查类型引用`
> 剥 `[长度]` 递归检查元素；⑥下标白名单**数组对象豁免**（元素类型任意）+ 数组声明登记**元素类型**
> 到语义指针变量表（下标检查/左值写/作实参全链复用指针路径）。

> **IR 层**：⑦新增 `生成数组局部`（C 布局多槽：槽数=ceil(元素大小×长度/8)、基址槽=最大槽号=最低
> 地址、逐槽 IR_分配、零初始化、逐元素 store；登记指针变量表=元素类型→**数组名退化元素指针**）；
> ⑧`生成标识符加载` 数组分支（值=**基址地址**——原 Load 槽值=数组首 8 字节数据当指针，arr1 探针
> 段错误实证）；⑨下标左值路径数组分支（基址=槽地址）；⑩`类型码`/`类型宽度码` 数组分支（按元素类型）；
> ⑪**结构体元素成员链**（`表[0].x` 读写）：新增 `数组元素结构体表` + `解析成员链结构体ID` 下标分支 +
> `顺序链下标地址`（左值链上中间跳下标步进——**原缺则索引静默归零**：arr5 探针 `表[1].x=30`
> 落表[0].x，输出 30/0/30 vs 期望 10/40/50）+ `生成字段访问` 下标对象分派。

> **越界检查（安全面）**：⑫新增 `发射数组越界检查`（下标 >= 长度 → 运行时错误(**2**)「数组越界」，
> 宿主 ir.cpp 同码同文案；长度常量 + IR_大于等于 + 条件跳转 + 越界块）——数组有静态长度=发射，
> 指针无长度信息=既有边界。

> **门禁**：E2E **253_双编译对照数组全链**（移植纪律 9 第二十一个：八形态折校验和 **255**——
> 声明+初始化列表/部分初始化补零/下标写/循环动态下标/作实参退化/结构体元素成员访问/字符串元素/
> 浮点元素）+ **254_双编译对照数组越界**（第二十二个：越界运行期拦截两侧 rc=1）。**反证双件全命中**：
> A 数组声明分派失效注入→253 v2 侧 IR 错误链；B 越界检查失效注入→254 v2 退出码 0（期望 1）。
> **探针矩阵 11 件双侧一致**（arr1~arr10 宿主 vs v2 逐行 diff）。

> **验证链全绿**：清场重建**零警告**（宿主 md5 `10607b8501aef9996ca5372e6602a590` 与 93-a 一致=
> 宿主零改动）+ 单测 **1313/1313** + E2E **282 用例 278 过 / 0 失败 / 0 未实现 / 4 跳**（+253/254；
> 其余 280 过=零回归）+ 锚定链固定点 **371923 行**（93-a 后 362243；+9680=数组全链确定性）两代
> 逐字节一致 + 绑定自检 + 组件对拍 **34/34**（DIFF=0）；v2p md5
> `e07dfa7ae0b91fcdb642ba0f89ce035b`、cn_self94 md5 `387e7a8a3a1bd737eff2a1ed22709b2c`。

> **诚实边界（登记）**：结构体字段数组 v2 拒绝（宿主支持）；多维数组两侧不支持（宿主段错误=
> 既有形态）；**字符字面量 `'A'` v2 既有缺口**（与数组无关，独立登记 C7）；字符串元素数组释放
> 两侧同残留 2（宿主同缺=79-a 既有靶子）；负下标两侧同边界（仅上界检查）。**C6 销项**（plans/021
> §4.2 台账）。详见 `plans/020` 第三十四节与 git 提交。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
