## 功能完善总结

> 86-a 复审三缺陷根治（2026-09-12 第八十六轮，深度机 linux-x86_64）：用户令「独立复审
> 74-a~85-a（23 提交）」，构建四条代理审查线 + 探针实证，结论=**验证链声明属实、规范与
> 流程纪律整体合规**，但证伪静态审查的两项存疑并**实证揪出三个新缺陷（全部两侧同现或
> v2 单侧，内存安全级）**，本轮全部根治；扩展探针又发现并修复两处**既有**聚合写入位
> 缺陷（成员位覆盖写 preFree 缺失／结构体字面量字段值=标识符的整体拷贝缺失）。

> ① **缺陷一：三元运算符 × 聚合类型（两侧，最高优先）**——`返回 (条件 ? 借用形参 :
> 借用形参)` 悬垂（P41 宿主搅动后乱码 `名=??r%`、**v2 段错误 rc=139**）。根因三层：
> 宿主 `isBorrowedAggregateSource` 判定表 default 把三元当「拥有来源」（返回位不深拷）；
> 宿主与 v2 各消费位的类型推导/形态白名单**全面缺三元**（赋值位落标量 8 字节路径静默
> 损坏／声明位 v2「结构体整体初始化暂不支持」拒绝／成员偏移 v2「成员字段偏移未知」／
> 实参回扫把三元地址误改「取临时槽地址」→ 乱码）；v2 三元分支对聚合恒发射 8 字节
> `IR_加载`（首字段值非地址）。**修复（甲，完整支持，语言面不收窄）**：三元聚合=
> **地址透传**（选中 place 地址，宿主/ v2 同构）——宿主 `exprSrcType` / 赋值位 /
> `isBorrowedAggregateSource` / `memberObjStructType` / `emitStructWholeAssign` 五处
> 补三元；v2 `解析节点类型ID` / `生成三元运算`（`区间末为聚合左值` 判定 + 分支左值地址化
> + 汇合 `T_对象` 登记）/ 声明位 / 赋值位 / 成员偏移 / 成员位赋值通道 / 实参回扫七处补三元。
> 探针 P41/P46/P47 八形态矩阵宿主 **255** / v2 七形态（无容器面）**247**。

> ② **缺陷二：84-e 深拷守卫插入顺序错误（v2 单侧）**——`IR容器.cn` `发射串字段深拷` 的
> 「查结构体大小>0→跳过」守卫插在**递归分支之前**（另两处正确在后）→ 嵌套含串结构体
> 递归深拷恒不可达 → `箱 乙 = 甲;` 整体赋值后搅动读乱码（P42：宿主 RC=1 vs v2 RC=0）。
> 修复=守卫两行交换（资源字段判定在前，三处同序）。

> ③ **缺陷三：85-a 借出豁免无容器类型约束（两侧）**——`isBorrowViewMethod` /
> `是借出方法名85` 只匹配函数名末段 → **用户顶层函数恰名「获取」**（合法标识符）+
> `返回 借用形参` 被误豁免深拷 → 悬垂（P44 两侧 RC=0 乱码）。修复=豁免加容器类型前置
> （宿主 `currentClass_` 经 `isStringElemContainer`/`isOwnedStrFieldElemContainer`/
> `isStringValuedMap` 判定；v2 新增 `方法类名85` + `是容器类`）——与 77-a
> `isBorrowSourceContainer`、72-a「精确判定替代符号名模式」同源纪律。

> ④ **扩展发现（复审探针连带，既有缺陷同轮修复）**：**宿主成员位赋值 preFree 缺失**
> （`emitStructWholeAssign` 两调用点 preFree 默认 false → `乙箱.内盒 = …` 覆盖旧串泄漏，
> P42/t244c 残留=1 实证；修复=成员位调用点 preFree=true + v2 同通道对齐）＋
> **结构体字面量字段值=标识符的结构体字段落标量 8 字节路径**（`emitStructInitTo` 原只
> 处理嵌套字面量形态；`箱{ 内盒 = 收, … }` 写入静默损坏，t244v 实证；修复=结构体字段
> 值整体拷贝（深拷=非调用来源））。

> ⑤ **防线（四件套）**：**正测** E2E **243_双编译对照三元聚合**（移植纪律 9 **第十四个**
> 对照用例：八形态〈赋值位／声明位／实参+形参返回／实参成员链／成员位赋值／嵌套三元／
> 三元成员基／返回三元借用〉折校验和 **255** 两侧一致）+ **244_双编译对照嵌套深拷与豁免**
> （**第十五个**：八形态〈嵌套整体赋值／嵌套声明位／嵌套成员位／嵌套返回位／用户函数名
> 获取/元素/读取 返回借用／循环 5 万轮〉折 **255**）+ **245_三元聚合容器消费位**（宿主
> 单侧八形态折 **255**：向量追加/插入、链表追加/头部追加+删除、栈压入+弹出、队列入队+
> 出队、读出链再入容器、循环 5 万轮）。**反证五件**（全部精准命中、注入已恢复复验）：
> A 宿主三元判定恒假 → 243 宿主 **255→127**（丢 ⑧）；B v2 深拷守卫顺序回退 → 244 v2
> **255→246**（丢 ①④）；C v2 豁免去类型约束 → 244 v2 **255→143**（丢 ⑤⑥⑦）；D 宿主
> 成员位 preFree 回退 → 244 宿主 **255→251**（丢 ③）；E 宿主字面量字段整体拷贝失效 →
> 244 宿主 **255→127**（丢 ⑧）。

> ⑥ **验证链全绿**：清场重建**零警告** + 单测 **1306/1306** + E2E **272 用例 268 过
> 0 失败**/4 跳（+243/244/245；其余 269 过＝零回归）+ 锚定链固定点 **330412 行**（新口径，85-a 后 327355；+3057＝v2 侧 86-a 代码）两代**逐字节一致** +
> 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

> ⑦ 文档五件套：plans/019（跟踪表第八十六轮行）+ plans/020（第二十七节 86-a 全节 +
> 第二十六节扩展登记）+ lessons（86-a 段，权重 8，预防新增 82~86：「复审发现=新缺陷
> 的合法来源，静态审查发现必须探针实证（证伪同价）」／「三元/条件表达式的值语义=
> 选中 place 的地址透传（消费位必须全链补形态，漏一即静默 8 字节）」／「豁免/清单类
> 判定的第二维=类型约束（名字单独不构成依据）」／「守卫插入位置=分派链语义的一部分
> （插入前先读全链顺序，三处同构点必须同序）」／「局部修复必须全量 E2E 回归（本轮的
> 成员位 preFree 与字面量字段整体拷贝=探针连带发现）」）+ 本文件（标题层级修正为
> `##`）+ HANDOFF。**诚实边界**：v2 容器插入位三元面需供给机制（245 宿主单侧承载）；
> v2 结果/可选 返回三元不在面内（84-e 边界）；② 静态结构体等登记维持；探针留
> /tmp/p86probe 不入库。
