# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 95-a v2 字符字面量缺口（C7）销项 + 宿主缺陷 H3 根治（2026-09-13 第九十五轮，深度机 linux-x86_64）：
> C7 缺口（94-a 登记：`字符 甲 = 'A';` v2 报「语句缺少分号」）全链销项；探针双侧 diff 同时
> **实证宿主缺陷 H3**——字符字面量三处消费点同款「去引号取首字节」：`'中'`=**228**（UTF-8
> 首字节）、`'\n'`=**92**（反斜杠）、`'\u{4E2D}'`=**92**（注释声称全解码实为未实现），违反规范
> 01b 三「字符类型为 4 字节 Unicode 标量值」且与 v2 侧静默分叉（存量零依赖：差异仅在转义/
> 多字节，86 用例仅 'A'/'0' 单字节）。

> **宿主 H3 修复**：新增单一归属 `charLiteralCodePoint(raw)`（ir.hpp 声明 / ir.cpp 实现：剥引号 +
> 复用既有 decodeEscapes 转义/`\u{XXXX}` + UTF-8 2/3/4 字节解码），3 处消费点统一
> （`ir_decl.cpp charLiteralCodeText` / `ir_expr.cpp evalDefaultExpr` / `ir_expr.cpp visitCharLiteral`）。

> **v2 侧（4 处）**：词法新增 `字符码点`（转义 + `\u{}` + UTF-8 全解码 → Unicode 码点数字文本）
> 与 `十六进制值`（字符扫描处改产码点）；解析原子加 `令牌_字符字面量` 分支（`节点_字符字面量` 47）；
> IR 生成字面量加分支（`IR_常量整数` + `T_字符`——零新 IR/后端设施）；语义 检查表达式 返回
> 驻留("字符")。**顺带销项 C3**（v2 静态字符语法）：`静态 字符 全局符 = 'Z';` 探针 ch3 双侧一致。

> **门禁**：E2E **255_双编译对照字符字面量全链**（移植纪律 9 第二十三个：八形态折校验和 **255**
> ——ASCII/UTF-8 多字节/转义序列/Unicode 转义/字符数组/作实参/静态字符/字符算术比较）。
> **反证双件全命中**：A 宿主 charLiteralCodePoint 回退首字节 → 255 宿主 **241 = 255−14**
> （缺 ②2+④4+⑧8 **精确匹配**转义/多字节面）；B v2 解析字符分支失效 → v2 语法错误。
> **探针 3 件双侧一致**（ch1/ch2/ch3）。

> **验证链全绿**：清场重建**零警告**（宿主 md5 `7ac6697a6f7e5eb150cb64efefface17`）+ 单测
> **1313/1313** + E2E **283 用例 279 过 / 0 失败 / 0 未实现 / 4 跳**（+255；其余 282 过=零回归）+
> 锚定链固定点 **373952 行**（94-a 后 371923；+2029）两代逐字节一致 + 绑定自检 + 组件对拍
> **34/34**（DIFF=0）；v2p md5 `5c76c0266873c71019cf156695b4ad95`、cn_self95 md5
> `35660b59808cdf3c0e6b6c5c8c2fab54`。

> **诚实边界（登记）**：字符打印=数值输出（86 用例既有口径，两侧同款）；**v2 字符串字面量
> `\u{XXXX}` 未解码**（词法 分词字符串 既有边界，独立登记 C10）；非法 UTF-8 防御返回首字节。
> **C7 销项（+C3 顺带销项）**（plans/021 §4.2 台账）。详见 `plans/020` 第三十五节与 git 提交。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
