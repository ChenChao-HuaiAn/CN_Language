## 功能完善总结

**plans/019 阶段4' A2 契约根治 70-a：宿主 retOwnedString 二分残留恢复+系统::参数 借用改 字符\*+装箱 move 语义（悬垂家族第五实例根治）+双机并行合并轮（2026-09-11 第七十轮，深度机 linux-x86_64，方案A 用户裁决）**

- **双机并行合并（9f5d22f）**：深度机第六十八轮 A2 三提交（0ed437b/83b26ac/c668919）+单位机第六十八~六十九轮（arm64 跨机收口 6e53242+68-a rt objs 指纹缓存根治 7b76bdf）——merge 保留双方哈希（文档引用不失效），plans/014/019 状态行双侧保留+HANDOFF 双机口径融合，源码零冲突（单位机两轮为纯验证/测试基础设施轮）。
- **70-a 立案（缺陷呈报→用户裁决方案A 两步走）**：盘点手动释放重叠面时发现宿主 semantic_call.cpp:963 普通函数决议路径 retOwnedString=`(false && ...)` 二分残留——轮1 排查 119/167-172 回归临时禁用，真根因（IR容器 字符串入容器浅共享悬垂）已另行根治（c668919），该行忘恢复即提交。探针实证：`字符串 名 = 取名();` 宿主产物 str_free=0（调用方不登记 RAII）vs v2 侧同款契约活着——**双侧语义分叉**（宿主=借用泄漏/v2=登记释放），被「三树全显式化迁移」掩盖（E2E 全绿+固定点一致都不触发分叉路径）。
- **契约恢复+连环两枚潜伏缺陷当轮根治**：
  - 恢复契约一行+全树 grep `false &&` 复审清零（无其他残留）；
  - `系统::参数` 返回 argv 借用但签名 字符串（类型谎言）→双侧内置注册改 字符\*（宿主 semantic.cpp regSysFn+v2 IR布局 内置表）+stdlib 系统.cn 包装显式复制装箱+v2 主.cn 3 处调用点适配（字符\* 借用接收/字符串复制 传参）+tests 37_system 四处/38_tool 两处适配；
  - `取语义诊断` 返回容器元素借用（A2 呈报 B 类列了但轮1 漏修）→返回位显式 字符串复制（货舱查找依赖 同款）。
- **装箱 move 语义（v2p 空转真根因，Rust Ok(s) move 同款零拷贝）**：`正常/某些(拥有字符串局部)` 装箱（结果构造器 StorePtr）浅拷字符串指针入联合体字段×genStringFrees 返回块 free 局部=装箱值悬垂——stdlib 读取行包装链实证（v2p 读源文件全空→解析节点=0 假成功 rc=0）。修复（ir_error_ctor.cpp）：值字段写入后**源槽清零**（转移() 浅交接同模型）——genStringFrees 对已清零槽空安全跳过；污染名单内借用装箱不清零（指针共享随源存活）。「装箱浅共享×局部 RAII 释放」=悬垂家族**第五实例**。
- **验证链全绿**：v2p 复活（57054 节点全链解析 rc=0，修复前节点=0）+清场重建零警告+单测 1289/1289+E2E 全量唯一败=194 既有待批+锚定链 fix_p **262321 行**（新口径，+8=装箱 move 清零+树适配源码确定性）≡fix_s 裸逐字节一致+绑定自检 cn_main∈.text+运行级 3/3（208v2 rc=127/119 rc=14/173 rc=173）+组件对拍 29/29。
- **lessons 第七十轮权重8**：二分禁用残留必须「根因修复后全树 grep 复审清零」（禁用是分布式的，修复是单点的，二者天然不对账）；新启用 RAII/所有权面必须枚举「值经装箱/容器/结构体转交」全部分派点并逐个定拥有语义。
- 诚实边界：v2 侧装箱 move 未对齐（v2 签名表查不到 stdlib 包装=保守不登记，侥幸安全泄漏方向——随收尾批对齐）；装箱后源使用无 use-after-move 检查（清零后读 NULL=空安全崩溃非悬垂）；手动释放收尾批（tests 19 文件 124 处+stdlib 7 处调用+7 处注释）排下一批次。
