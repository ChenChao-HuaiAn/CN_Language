# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 91-a v2 浮点语义面全链补全 + 宿主浮点 NaN 比较缺陷根治（2026-09-13 第九十一轮，深度机
> linux-x86_64）：89-a 登记「v2 浮点面外」（算术/大小比较/负号/打印）**专项补全** + 顺藤
> 发现并根治**宿主缺陷 H1**（x86 两后端 NaN 比较非 IEEE=三后端语义打架）。用户令「尽善尽美
> 禁止妥协 + 参考 Rust + 性能第一、安全第二」——两侧统一为 IEEE ordered（Rust f64 语义）。

> ① **探针实证（P91 十件，修复前）**：v2 浮点算术=整数指令算 IEEE754 位模式（`3.5+2.0`
> 打印 -9219994337134247936、`0.0/0.0` 走 idiv 触发 **SIGFPE**）；runtime 浮点实参未走 xmm0
> （打印/拼接恒 `0.000000`）；比较=整数比较（仅正数巧合正确）；负号=neg 位模式错；转换无
> 浮点分派。
> ② **宿主 H1 根治**：`ucomisd` 后 setcc 直配在 NaN 时 ZF=CF=PF=1 → `NaN==NaN` 真 /
> `NaN!=NaN` 假 / `NaN<1.0` 真（arm64 后端 `mi/ls/gt/ge` 本已正确=三后端打架）。修复：
> `<`/`<=` 交换操作数走 `seta/setae`（NaN 自然判假，LLVM ordered 同款）+ `==`/`!=` 加
> `setnp/setp` 组合——linux_x64/win/arm64 三后端统一。
> ③ **v2 IR 层浮点分派**：`函数IR.参数类型们` 新字段（声明序类型码）；二元类型字段**浮点
> 优先**；混合运算**隐式宽化**（整侧插转换，对齐宿主 `乙+2`=5.5）；浮点算术产物登记（原
> 「算术产物不登记」→ 打印浮点走 `__cn_print_int`）；负号/转换带源类型码；`IR_调用实参`
> 实参类型码 + `IR_调用参数化` 返回类型码；`IR_返回.类型` 浮点走 xmm0/d0；浮32 实参经内置
> 边界提升 f64。
> ④ **三后端发射 + 双计数 ABI（共用层 `分配参数位置` 单点）**：整型/浮点独立计数（SysV 6/8、
> win 4/4、ARM64 8/8，宿主 emitParamSetup 同构）；add/sub/mul/div + ucomisd/fcmp（NaN 安全）
> + btc 符号位翻转/fneg + cvtsi2sd/cvttsd2si/cvtss2sd/cvtsd2ss（x86 u64 无符号走符号位分支
> 修正）/scvtf/fcvtzs/ucvtf；浮点返回 xmm0/d0；浮点栈参/参数/返回全链。
> ⑤ **防线 E2E 250**（移植纪律 9 第十九个对照用例）：八形态折 **255**（算术/混合/比较+负号/
> 转换/参数返回 ABI〈单浮/整浮混排/10 浮栈溢出〉/静态/打印拼接 %f/循环+NaN 全谱）——**独立
> 锚定 + 值形态**（反证 C 首版实证 ==/!= 双回退互相补偿=盲区，修正后命中）。**反证三件全
> 命中**：A 二元类型字段回退→v2 SIGFPE 136；B 分配表浮点判定失效→v2 143；C 宿主 NaN 组合
> 回退→宿主 127。
> ⑥ **过程中修订（lessons 新增 104~107）**：序言三游标重构为分配表单点（首版手误
> `整数游标p = 6` 致多结构体参数全落栈 SEGV，E2E 全量复跑捕获 148/158/236/238/240/243，
> 修复后零失败）；**反证备份顺序教训**（备份须先于注入、以「注入前 md5」为恢复基准——
> 首版备份在注入后=自证陷阱）。
> ⑦ **验证链全绿**：清场重建零警告（`-Wall -Wextra -Werror`）+ 单测 **1306/1306** + E2E
> **277 用例 273 过 / 0 失败 / 0 未实现 / 4 跳**（+250 零回归）+ 锚定链固定点 **360800 行**
> 两代逐字节一致 + 对拍 **34/34**（DIFF=0）；v2p md5 `3e38fb40…`、宿主 `06eadacf…`。
> ⑧ **诚实边界**：宿主浮32 面缺陷 **H2 新立案**（`1.5f*2.5f`=0、浮32 赋值读回 0——另案）；
> v2 `1.5f` 后缀语法缺口 / `%` 浮点未拒绝 / 数学库内置表缺位（登记）；win/ARM64 浮点发射
> 实物=跨机轮（代码已三后端同构实现）。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
