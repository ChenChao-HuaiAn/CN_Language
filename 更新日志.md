# 功能完善总结

> 80-a v2 池索引 0 哨兵槽（2026-09-12 第八十轮，深度机 linux-x86_64）：
> 79-a 登记的「缺陷 A：v2 内置构造器识别依赖文件含结构体声明」销项——用户令「继续
> 后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案（性能第一、安全
> 第二）」。诊断注入三步收敛出**真根因＝池索引 0 哨兵冲突**（非结构体依赖——结构体
> 声明只是把首个入池叶节点推离索引 0 的掩盖变量）；以 **rustc `NodeId` 0 保留同款
> 的哨兵槽**根治：1 行代码 + 1 个常量，全树零处守卫改动，整族一次收口。

## 一、缺陷实证（现象 → 真根因，诊断注入 dbg/dbg2/dbg3）

| 层 | 观察 | 证据 |
|---|---|---|
| 现象 | `结果<字符串,整32>` 构造器 `错误(5)` 在「全文件无结构体声明」时退化为普通调用 → 链接失败（`undefined reference to _错误`） | 探针 P10b asm `call _错误` vs P10c 装箱 IR（标志@0 + 值@8） |
| 二分对照 | 「结构体声明」是**掩盖变量**：加结构体声明/垫语句/字符串声明任一前置节点，缺陷即消失 | 变体矩阵 V1~V6 实测 |
| 真根因 | **池索引 0 哨兵冲突**：入口文件**首个入池节点**（首个函数体首条语句的最早叶子，如 `返回 错误(5);` 的 错误）恰为被调标识符时 `子起点 == 0`；语义层 `检查调用表达式`（`调用起点 > 0`）与 IR 层 `生成调用` 三特判（构造器/转移/类型大小，`n.子起点 > 0`）把 0 当「无」哨兵 → 识别整段跳过 | dbg：P10c 打印 `调用名=[错误] 语境=63`、P10b 无该行；dbg2：返回语句区间 [0,3) 节点 0=「错误」 |

## 二、方案裁决（三案；性能第一、安全第二，Rust 经验）

| 案 | 内容 | 代价 | 裁决 |
|---|---|---|---|
| 甲 | 调用/下标/成员族 24 处守卫逐处改 `>= 0` | 「0 歧义」隐患留存（新代码写 `> 0` 即复发）；分散修改 | ✗ |
| **乙** | **池索引 0 哨兵槽**：池首元素占位（`节点_哨兵`，类型 0=未使用），真实节点索引恒 ≥1 | **1 行 + 1 常量**；零处守卫改动；零运行时开销（纯解析期占位） | ✅ **实施** |
| 丙 | 池索引基址改从 1 起（所有索引消费点 +1 偏移） | >200 处改动；自举产物大改 | ✗ |

Rust 对照：rustc `NodeId` 0 保留（`CRATE_NODE_ID = 1`）——哨兵槽使 0 成为索引域不可达
值，`> 0` 与 `>= 0` 两写法**同时正确**，整族缺陷一次收口、未来新代码不会再踩。

## 三、实施（v2 树 2 文件）

`IR/IR常量.cn`：新增 `常量 节点_哨兵 = 0` + 节点结构体协议注释（池索引 0 保留不变量、
修复前症状、Rust 对照）。`主.cn`：池创建后 `池追加(池, 建节点(节点_哨兵, 0, 0, 0, 0))`
（编译任务单点插入，全树无第二处池创建）。

## 四、首节点形态矩阵（HEAD ↔ 修复，编译+汇编+链接+运行全链）

| 形态（被调落池索引 0） | HEAD | 修复后 |
|---|---|---|
| `返回 错误(5);`（结果） | 链接失败 `_错误` | 退出码 1（预期） |
| `返回 正常("好");` | 链接失败 `_正常` | 退出码 0 |
| `返回 某些("好");`（可选） | 链接失败 `_某些` | 退出码 0 |
| `如果 (类型大小(整64) == 8)` | 链接失败 `_类型大小` | 退出码 0 |
| 普通函数调用 | 正常（路径不依赖被调索引） | 正常（回归对照） |
| 转移/下标/成员（对象须已声明变量） | 索引 0 不可构造（守卫不可达） | 同 |

同族扫面：全树 `子起点 > 0` 23 处 + `子起点 <= 0` 4 处 + `附加 <= 0` 1 处逐处可达性
判定；`池.元素(0)` 直接访问与全池遍历**零命中**（哨兵槽可行性前提）。

## 五、防线与反证

**E2E 231_v2_首节点构造器形态**（错误/正常/某些/声明位/类型大小 折校验和 **31**）+
**232_v2_首节点类型大小形态**（**7**）+ **233_v2_首节点构造器语境拒绝**（负测：非结果
语境须语义拒绝）。

**反证实证**：`git stash` 回退修复重建 v2p → ① 231 **链接失败**（`_错误`）；② 232
**链接失败**（`_类型大小`）；③ 233 **v2p 退出码 0**（静默放行）——三项全 FAIL；恢复后
三项全 PASS。（232 首版用例因「首条语句=变量声明 → 类型节点先入池抢占索引 0」未命中，
反证时揪出并修正。）

## 六、验证链（本机口径）

宿主编译 v2 树**零新增警告**（189 行 ≡ HEAD 189 行逐行一致）+ 单测 **1306/1306** +
E2E **260 用例 256 过 0 失败**/4 跳（+231/232/233，其余 253 过=零回归）+ 锚定链固定点
**309133 行**（新口径，上轮 309101；+32=哨兵代码确定性增量）两代逐字节一致 + 运行级
3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照（宿主单点清零 + v2 零寄存器引用 36 处）。

## 七、诚实边界（登记）

转移/下标/成员形态的索引 0 在语言层面**不可构造**（对象/实参必为已声明变量 ⇒ 索引
≥1）——修复对它们为「写法脆弱性消除」而非「活缺陷修复」，未加单独 E2E（哨兵槽使其
自动正确）；普通函数调用形态修复前也正常（仅作回归对照）；v2 数组类型整体缺口（矩阵
#15）、容器元素=含串结构体、字符串键映射、结构体容器字段浅拷等 79-a 边界维持；
win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。
