# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（287-a·2026-09-17）

**T13 根治（用户裁决方案A·linux_x64 后端 O0 移位发射 i32 零扩展污染）**：emitShift i32 非常量路径补 `movsxd r10, r10d`（对齐 270-a T14 同族先例）——M3 采样 s2026091705 asm 三件套定案（-426>>13 曾出 524286 应为 -1·O3 常量路径巧合对·285-a「O0 对 O3 错」误判反转·归 T13 非 T12）；免疫面四重实证（i8/i16 movsx 装载·win sar eax·arm64 asr w9·v2 D34 规范化）=三后端+v2 树零改动。新 E2E **403_移位负值矩阵**（11 行·i32 负值变量/常量 sar+shl+正值零回归+i16/i8/u32 免疫+mod 取模+污染值判定面·expected win 实跑固化）+coverage_map 登记。验证链全绿：零警告+单测 1351/1351+E2E 422=422/0/0（含 79 自举闭环=win 锚定链逐字节不变）+CLI 50/50+面覆盖 PASS+修后回归轨 38/38+修前修后 .s diff 仅 4 行 movsxd 插入；v2p 重建实证 v2 免疫（附带发现 v2 移位量掩码 63/31 分支疑似 mod 分叉面·观察待独立甄别）。跨机项=X64L 锚定链重锚+运行级复验归深度机。

## 深度机 linux-x86_64 节

## 功能完善总结（291-a·2026-09-17）

**全项目文档同步至最新状态（用户令「各个文档严重落后，请更新最新版本状态」）+ 测试资产登记面修复 + T23 复验归零销项**：①**活文档**=根 `README.md` 全面重写（测试基线旧口径 1317/306 → **当轮实测 1353 单测/423 E2E**；特性对账=上下文关键字 17、标准库 12 模块〔补随机数/正则〕、测试度量设施；CLI 补 `--验证-ir` 等；**Linux 实测须知「build 必须显式 `--target`」**）+`tests/e2e/README.md`（编号 2~3 位/参数表/控制文件/删「阶段零」远古说明）+`plans/013` P7 已达成+`plans/024` 口径注+`plans/005`/`002-00` 现状注。②用户点名 **`plans/003`**=「📌 文档状态」块+**现状校正表（14 行）**——模式匹配/`?` 传播/迭代器/FFI/所有权借用/UAF 检测/智能指针/A 级缺陷/C 级缺口/性能口径逐条对照现状（未关闭项如实标注：「`?` 传播 v2 侧仍缺失」「映射集合仍 O(n)」）。③**31 份历史文档统一状态戳**（plans 30+docs 1）+**002 索引死链修正**（阶段名与文件名不符+多余 `plans/` 前缀；29 链接脚本核验全可达）+关键字 54→46+`.ai-coder`→`.zcode`。④**测试资产登记面**=coverage_map 补登记 `402_验证IR旗标触达`（283-a 欠账）+`404_位运算立即数矩阵O3`（288-a 欠账）——**`check_spec_coverage --strict` 红牌（孤儿 2·退出码 1）→ PASS（退出码 0）**；面台账统计行修正（🔶 20→18）。⑤**T23 famscan 复跑 0/47 归零→销项**（289-a 判据 16→0 达成·287-a 根治验证·B9 制度首个「立案→根治→复跑归零」全链闭环）+迁 §4.2。⑥**呈报**=402 编号撞车（plans/021 §3-B 三方案·推荐 A=本机改 405·待用户裁决；改动涉测试文件须同意）。

**验证链**：增量重建零警告+单测 **1353/1353**+全量 E2E **423=421/0/2**+CLI 矩阵 50/50+`--strict` PASS（修复证据）+matrix PASS 57/60+famscan **0/47**+check_handoff/check_progress_sync 双绿+002 索引链接 0 失效。写集=README+plans 31 份+docs 1+tests/e2e/README+coverage_map+面台账+共享文档本机节；**编译器源码/测试用例文件零改动**；与家机 290-a/单位机采样轮最小交叠（面台账/021 最小局部编辑·push 零冲突）。

## 单位机 ARM64 节

## 功能完善总结

### 🧪 M3 正确性收敛采样轮（验证轮·不占号·2026-09-17，基线 46662c3→认领 9ef6a92·源码零改动）

1. **M3 采样 2 轮全绿**：daemon 新种子每轮 200 样本+回归轨全量——293×2 一致 293/分歧 0/编译失败 0/运行失败 0（T22 零复发·零新类别·月文件制正常记轮）。
2. **B8 面台账巡视三矩阵三方对拍全绿（gcc C oracle vs CN-O0 vs CN-O3）**：行 9 T13 移位面 16 形态复验闭环（升 ✅+403 正门 PASS）｜行 12 T12 布尔 10 消费位×6 输入矩阵复验绿（285-a 折叠病灶不复现）｜行 52 连带 T16 负零 6 形态复验绿（-(-0)=+0 IEEE 正确）｜行 57 arm64 列 T12/T13/T16/T22 四缺陷闭环（⬜→🔶）。
3. **呈报 B10（plans/021 §3-B·待用户裁决）**：三元 ? 歧义裁决代价面——真分支一元前缀（-/+/!~/++/--）按后缀传播解析=解析失败须加括号；定性非编译缺陷（e3bb421 实现裁决与规范 01b §5.8 自洽）·扩散矩阵 8 形态实测；候选=甲同层 `:` 前瞻消歧（推荐）/乙维持+规范明细化/丙回溯。
4. **台账漂移修正**：面台账行 6/52 系他机根治后未刷（279/284-a、270/272/273-a 销项）——一并刷新+统计（✅ 39→40·⬜ 3→2）。
