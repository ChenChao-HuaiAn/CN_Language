# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百三十七轮 137-a：波 3 实施 1/3——设施探摸与实施路线定稿**（2026-09-14 上午，家机 win-x64；基线 a29eb45）：
> ①**存储形态探针**（决定性分叉；`target/p137win/probe_layout.cn` 不入库）：`向量<字符串>`=24；
> **`货架`（结构体）=32**（=24 内联容器 + 4 + padding）→ **结构体容器字段=内联存储**；
> **`账本`（类）=24** → 类字段=**指针槽**（由类侧级联 `LoadPtr + DeleteObject` 机制交叉确证）；
> ②**设施模板摸底**：类侧级联 `injectFieldCascadeDestroy`（`ir/ir_oop.cpp:196`——字段逆序、仅「有析构类」、
> `FieldAddr→LoadPtr→DeleteObject`）；**容器拷贝构造已存在**（`stdlib/容器.cn:51` `不安全 函数 向量(向量& 其他)`
> 逐元素 T 赋值语义深拷）；容器析构全在（`~向量()` 等）；**链表/栈/队列缺拷贝构造**（波 3 轮 C 补齐）；
> ③**实施路线定稿**（`plans/020` 第四十九节）：轮 A=载体 `kind` 扩展 + 收集扩展 + **拷贝位与消亡位同轮**
> （浅拷共享下只加释放=双释放）+ 探针复验 + 全链门禁（锚定链**重锚**=修复引入新代码，预期）；
> 轮 B=写入位归一化 + H7/H11 + v2 同构；轮 C=stdlib 拷贝构造补齐；
> ④**两查证点已标注**（避免下轮重复探摸）：析构调用的 IR 原语（MethodCall this=字段地址 / DeleteObject 就地变体）、
> 构造调用的 IR 发射方式（参照 ir_expr_assign.cpp 类深拷贝段）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
