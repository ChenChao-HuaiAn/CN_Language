## 功能完善总结

### v2 自举重建：控制流 如果/当 完成（P6b，2026-08-30）

**目标**：按 plans/014 下一步，v2 编译器（CN语言编译器v2/）实现 控制流（如果/当 → 跳转/条件跳转），向「v2 编译自身」迈进。

**变更内容**（6 个 v2 组件，+479/-30 行）：

1. **词法分析.cn**：运算符最长匹配——新增 双字符运算符（`==` `!=` `<=` `>=` `&&` `||` `++` `+=` `->` `::`），单字符兜底；`->` 由「`-`+`>` 两 token」改为单 token（函数返回类型解析同步适配）。

2. **语法分析.cn**：
   - 节点扩展：`节点_赋值`/`节点_自增`/`节点_逻辑非`/`节点_负号`/`节点_中断语句`/`节点_继续语句`
   - `如果` 支持 条件区间（文本=条件起点、子起点=条件终点）+ 真块（附加）+ 否则链（子终点，递归 否则如果）；`当` 同理（附加=体块、子起点=条件终点）
   - 赋值语句（左值 = 右值）、自增（前缀 `++变量` 与后缀 `变量++`）
   - 一元 `!`/`-` 仅表达式入口解析（避免 `n-1` 的 `-` 被误当负号）
   - 二元优先级 5 层（|| / && / 比较 / 加减 / 乘除），支持 `==` `!=` `<` `>` `<=` `>=`

3. **语义分析.cn**：新增 `检查如果`/`检查当`（条件表达式检查 + 块递归 + 否则链）；`检查块` 分发 如果/当；支持 赋值/自增/一元/中断/继续 节点检查；函数定义顺序调整（检查块 提前）。

4. **IR生成.cn**：
   - `函数IR` 新增 块计数/当前块/循环出口块/循环继续块 字段
   - `新块`/发射跳转/发射条件跳转/发射块标签/切换块 辅助（标签随建随取，嵌套安全）
   - `生成如果`（条件跳转 真/假块 → 真块 → jmp 汇合 → 假块/否则链 → 汇合）、`生成当`（jmp 条件块 → 条件 → 条件跳转 体/出口 → 体 → jmp 条件块；循环上下文 中断→出口、继续→条件块）
   - `生成语句` 支持 赋值/自增/中断/继续；**预扫描控制流体块区间**（体语句节点在池中位于控制流语句节点之前，外层遍历跳过避免重复生成）
   - IR 新增 `IR_标签`（49）、`IR_负`（31）；表达式支持 一元 逻辑非/负号

5. **代码生成.cn**：发射 `bbN:` 标签、`jmp`、条件跳转（`mov eax,cond; test eax,eax; jnz true; jmp false`）；一元 逻辑非/负号；**帧大小 +8**（覆盖变量槽最大偏移，修复 [rbp] 越界段错误）；扫描函数排除常量操作数（Symbol ID 非寄存器）。

6. **主.cn**：控制流验证源码（`阶乘` 当循环 + `主` 如果/否则）。

**验证**（真实生产链路：C++ 版编译 v2 组件 → 运行 → 生成 asm → ml64 汇编 → link → 运行）：
- `主()`：值=3，`如果(值==3)` 真 → 值+1 → **返回 4**（如果/否则正确）
- `阶乘(5)`：`当(n>1){结果*=n; n=n-1}` → **返回 120**（当循环/条件跳转正确）
- v2 组件链（词法→语法→语义→IR→代码生成）对控制流源码全链路正确

**教训**（写入 lessons 思路）：`n-1` 的 `-` 被一元负号分支拦截是解析器经典歧义——一元前缀只能在表达式入口处理；栈槽帧大小必须覆盖最大偏移（+8）；体语句节点物理位置在控制流语句节点之前导致外层块区间误包含——需预扫描跳过。
