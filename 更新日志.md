# 更新日志

## 功能完善总结

### 缺陷零容忍收口轮：引用返回全链+裸引用+下标绑定+无符号窄宽（2026-09-04 第十四轮，家机 win-x64，用户裁决「搁置条目全部立即根治」+拆分方案A）

用户审计揪出 HANDOFF「另立案/待同步扩/不变」式搁置条目违反尽善尽美/缺陷零容忍/一劳永逸纪律——四件立即根治，连带根治宿主引用返回读值缺陷一件，新立案呈报一件。

#### 一、引用返回全链（宿主 P3-18 对齐，E2E 150）
- 语法层：返回类型 `&` 并入（`-> 整64&`）。
- 语义层：`是引用返回调用` 判定（符号表返回类型尾 &）+赋值目标/引用初始化两白名单扩（普通调用仍硬拒）。
- IR 层：函数IR.返回引用 标记；返回语句 生成左值地址（被调方 rax=左值地址）；调用点默认右值读（IR_指针加载+剥 & 基类型登记）；生成左值地址 调用分支（回扫 IR_指针加载.操作数1=左值地址——赋值目标/复合赋值/绑定复用）。

#### 二、【连带揪出·当场根治】宿主引用返回读值缺陷
- 现象：`整64 a = 取值(p);`（f 引用返回）读出**地址**当值（150 宿主 sanity exit=11 实锤）——visitCallExpr 对引用返回调用返回裸 ptr 被一切右值消费。
- 修复：`suppressRefDeref_` 标志——读值默认 lvalue-to-rvalue（LoadPtr，C++ 语义）；赋值目标/复合赋值（ir_expr CallExpr 目标路径）/返回转发（ir_stmt isRefReturnFn）/引用局部绑定（ir_stmt callInit）四处上下文抑制（ir.hpp/ir_call/ir_expr/ir_stmt 四文件）。

#### 三、裸类型引用局部（宿主 parser 形式6 + v2 语法层对齐）
- 宿主：`typeRefVar` 形态（`标识符 & 标识符`；a&b 位与歧义与 a*b 同款既有惯例）——实测 exit=207 全对（结构体引用读写/引用传参/标量引用写回）。
- v2：局部声明检测 `标识符 & 标识符 =` 形态（后随 `=` 前置消歧，较宿主更严）+变量结构体表引用登记（对齐参数位惯例）。

#### 四、引用初始化下标形态 + 正8/正16 无符号窄宽
- 下标绑定（`整64& 绑定 = 指针[0]`）：语义白名单扩（IR 生成左值地址 指针下标分支既有）。
- 正8/正16（原「显式拒绝」系无符号语义缺位搁置）：T_正8=18/T_正16=19 零扩展标注+win movzx/ARM64 uxtb 对称+语义强转白名单——正8(200)=200（符号扩展会 -56）。

#### 五、【新立案缺陷呈报】v2 常量系统同值常量诡异触发（根因未明）
- 现象：IR常量.cn 新增任意常量对（八组名×值消融：14/15/16/17/18/19/98/99×T_正8/U_正8/U正8/U正九/U8/零宽码甲/U正宽8）使冒烟链（语法分析/语义分析/IR容器——共用 IR容器.cn 链）IR 生成各多 1 条「方法调用的对象类型未知: 元素」（DBG 定位 fn=发射容器字段析构）→ IR 层中止纪律触发 → 冒烟 asm 断供。
- 唯一不触发组合=T_测试甲=98/T_测试乙=99+代码生成字面量化（消融期污染，不能构成干净反例）；147~150 全 PASS（产品正确性不受影响）。
- 三方案呈报（HANDOFF）：A 专项深挖（dump 节点池/字段类型表现场）；B 值域隔离纪律（T_/IR_ 常量值域文档化约束）；C 常量收集重复值编译期校验。

#### 六、验证
- E2E 150 六步闭环 PASS（宿主 sanity exit=0/140/207 三探针先行锚定）；147/148/149 回归绿；词法/IR签名/IR生成 冒烟 0 诊断维持；全量门禁见 HANDOFF。
- 裁决入档：v2 组件拆分（规则3 单一职责）采用方案 A——自举闭环后统一拆分（用户批准）。
