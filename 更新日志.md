# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 495-a 开发轮：G 区 T10 收口=字符串子串越界口径文档化·2026-09-20）

- **行为矩阵十例实测**：越界长度钳位尾/越界起始空串/负起始钳 0/负长度空串/多字节按字节劈开——全 rc=0
  静默（安全方向）；实现唯一归属 `__cn_str_sub` 确认；
- **spec 02 口径条文**：字节级+钳位规则+劈开警示+Rust 对照（静默钳位=T9 同族哲学）——行为文档化
  零语义变更；
- **021 G 区 T10 销项**+勘误登记（string_api.cpp:97 注释与实现矛盾·随 src 触碰轮顺带修）；
- 纯文档快速门禁（写集=spec specs+plans+看板/交接/日志本机节）。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 470-a D10 双侧销项：泛型实例体重放语义检查〔v2 主件+宿主顺带件〕+459 回签服务轮·2026-09-20）

- **服务轮（会话开局·看板通告 2d76d82）**：459-a try-build linux 回签（合并结果=分支头 bd58dff2+develop f1bc9f5·构建零警告+单测 1363/1363+E2E 477=475/0/2+197 族不回归+460 真跑+自举链双 PASS·准予集成）+451 集成后独立复验备案全绿（arm64 运行级归单位机周一补验窗口）。
- **470 v2 主件**（语义检查.cn +84 行·语义检查表达式.cn 两处豁免）：泛型函数实例体重放语义检查——417-a「检查延后」欠账根治（体内未声明调用此前静默到链接期爆/产物垃圾·非泛型同形态编译期拒绝=检查缺口非泛型专属语义）。全池扫收集调用点实例（顶层区间扫不到体内调用·实测修正）+映射去重+挂点=表弹出前（全局符号层）；T 豁免语境（零环境映射·类型参数放行复用）；安全区/T* 下标元素类型=重放语境豁免（静态不可判定·455 形态保持过）。**457_泛型实例体未声明拒绝** v2 负测上架+反证双态（挂点恒假 rc 1→0→恢复复绿）。
- **470 宿主顺带件**（generics.cpp·机制级备案）：recheckGenericFuncBody 重放诊断窄放行——「未声明」类 Error 不再随快照回滚吞没（p1 宿主 build undefined reference→**编译期拒绝**·317-a 对齐补齐）；转换/推断类仍回滚（p2' =波2 双侧同收）。
- **门禁（两轮全绿）**：v2 件态+终态均=构建零警告（GCC -Werror）+单测 1363/1363+全量 E2E --jobs 8 **477=475/0/2**（477=476+457 新例·2 跳=62/69 平台固有·零新增红）+strict 179/179+matrix PASS+自举链 78/79 双 PASS。实施增量四枚（静态标量写触发 v2 自身安区检查=防线正面实证·标 不安全 对齐惯例/挂点前移/全池扫/映射去重）。
- **诚实边界（021 D10 行登记）**：p2' 返回类型误用+泛型类方法实例体重放=波2（环境映射 T→实参后双侧收口）；重放诊断行号=基类 AST 行。
- **下一步**：466（同机他会话）在飞=面②' 扩形态；本会话按排班队列继续认领。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
