# CN语言编译器 更新日志

## 功能完善总结

# 第四十一轮（2026-09-09 家机 win-x64）：挂账4 v2self 长符号面自查收口——v2 win 后端超长符号收缩+常量发射归一根治 + 宿主 cmp imm32 域检查补全 + 挂账2 abi辅助.cn 处置

按第四十轮 HANDOFF 挂账4 执行 v2 自举编译器 win 符号发射面自查。**先重建验证基线再实证**：家机 v2p.exe 系 9月5日 旧产物（第三十一~四十轮 v2 源码已演进，家机无 v2p 构建链），新建 target/rebuild_v2p.py（宿主编译 v2/主.cn → ml64 → link 10 运行时 obj，含第三十七轮新增 intern_api）重建 v2p 后开展实验。

## 一、v2 win 后端两枚缺陷根治（方案B 纯算术模拟，Rust 准绳：性能第一/安全第二）

1. **超长符号收缩缺失**（挂账4 本体）：50 汉字函数名探针（hex-mangle ≈319 字符）实证 v2p 产物 319 字符原样发射、ml64 **A2043 rc=1**——与宿主第四十轮缺陷同款；宿主产物 202 字符（$L319H 形态），两侧 mangled 原串同构（319=319）实锤 v2 缺短化。修复：`符号修饰` 单点包裹 `超长收缩(符号修饰实现(名))`（宿主 nameMangle→shortenLongSymbol 逐环同构）。
2. **常量发射未归一**：i64 探针实证 v2 词法原样存字面量文本，win 直发——`mov rax, 0L` 触发 ml64 A2206、超 32 位十进制触发截断；**组件对拍 30/30 全绿对此零暴露的根因=对拍输入面是编译器自身源码，而自身源码恰好不用 0x 前缀/带后缀大字面量**（验证面盲区结构性存在）。修复：win 层 `常量立即数文本`（真/假→1/0、位模式剥离、fit32 十进制、域外 MASM hex「数字开头+h/A-F 补 0」与宿主 uint64HexText 同构）；linux/arm64 本地解析删除、换共用层 `字面量位模式`（根治 0x 前缀静默解析为 0 的潜伏缺陷；静态论证十进制行为逐点等价，跨机轮动态复验）。

**实现要点**：FNV-1a 需位运算而 v2 全链路无位运算（IR_位与/位或/位异或 常量已备、语法/IR/后端零实现——补齐=大特性轮另案呈报）；短化仅超长符号低频路径触发（编译期微秒级），故以 **/ % 正数域逐位模拟**：共用层新增 十六进制数值/解析无符号/字面量位模式/位模式十六进制 四纯函数，win 层 字节异或（每字节 8 次迭代）+低八位/高五十六位/异或一字节+超长收缩。**黄金对拍：v2 收缩符号与宿主逐字节一致（`$L319HEFF9F723240DB1BE`）——纯算术模拟与 uint64 位运算逐位等价实证**。

## 二、宿主第四枚缺陷补全（写 v2 修复代码反哺实锤）

v2 源码新常量 36028797018963968(2^55) 使宿主编译 v2 源码产物即爆 ml64 **A2084**——`emitCompare` 立即数无 imm32 域检查（x64 cmp reg64 仅 imm32 形态；2026-08 imul 同族修过、cmp 旁路漏）。修复：op2 域外先 `mov rcx, uint64HexText` 再 `cmp w, rcx`（emitBinary 同构）+op1 hex 归一+0x/0b/0o 前缀防御解析。

## 三、验证矩阵

- **黄金对拍**：长名探针 v2p 产物收缩符号与宿主逐字节一致 + ml64 rc=0；
- **i64 探针全链 rc=7**：归一后 `mov rax, 14650FB0739D0383h` 与宿主同款+链接运行数值语义正确；
- **30 组件批量**：v2p 编译+ml64 全过；
- **新旧 v2 源码产物对拍**（worktree HEAD 重编 v2p_old vs 工作区 v2p_new 逐组件 diff）：词法/语法/语义/IR/货舱解析五包**逐字节一致**；代码生成包 328 共有函数中 324 仅确定性编号平移（strData/bb/cpcy 序号）、4=改动本体（符号修饰 包裹重构/两调用点换 字面量位模式/编码数据移动Win 栈布局随新调用）、+11 新函数/−2 删除函数账目严格吻合——**改动面外零扰动实锤**；
- **全量门禁**：构建零警告（MSVC /W4 /WX）+ 单测 1251/1251 + 全量 E2E win-x64 177 用例（败=78/79 已知 OOM 家族，零回归）。

## 四、挂账2 处置 + 遗留清理

- **abi辅助.cn 删除**（v1 遗留探针 13 行：点 结构体+两演示函数）——零引用、无用例覆盖需求（结构体/容器能力已有 07/30 等正式 E2E 锚定）、且作为代码生成包成员污染产物；文件+包.cn 挂载行移除+冒烟通过；
- 顶层遗留清理：commit_msg*.txt 三文件（历史提交信息误入库）git rm；test_input_api_*.txt（input_api 运行时输入残留）/v2asm.obj（手工探针汇编残留，均 gitignore 级）本地删除。

## 五、另案呈报（待用户裁决）

1. **v2 位运算/移位全链路缺失**：规范 01b §5.4 已定义 `& | ^ ~ << >>`、宿主 parser/IR 已实现，v2 自举侧语法/语义/IR/三后端零实现（IR 常量 15/16/17 已备未用）——自举编译器无法表达位运算，建议独立特性轮（语法解析+语义+IR 降级+三后端编码+正32/正64 无符号语义+对拍）；
2. **v2 结构体构造字面量 `类型{字段=值}` 语法缺口**（沿挂账3）：规范 05 二已定义、宿主 parseStructInit 完整、v2 语法层无解析——建议随 v2 语法覆盖轮一并实施；
3. **v2 主.cn 路径文本匹配对反斜杠脆弱**：win 反斜杠路径使恢复包上下文失配（双载→符号歧义），正斜杠正常——建议路径身份统一归一 `/`（linux 侧不可见，跨机轮注意）。

文档：plans/014 状态表第四十一轮行 + lessons 三条（编译器自身源码=对拍输入盲区·权重10 / 同族修复旁路穷尽再演·权重8 / 路径文本匹配平台分隔符脆弱·权重6）+ HANDOFF 重写。
