# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结
> **247-a A10 第五波·M1 收官（2026-09-17，家机 win-x64；基线 a1e0174·认领 3d91531）**：
> ① **B3 口径落脚本**：check_spec_coverage.py 加 EXCLUDED_CELLS（A 类 7 格·分母 183→176·排除格报告标注「口径排除」）——**ci.ps1 门禁升级 --strict**（可测格 100% 硬门禁）。
> ② **19 新用例（354~374）补齐覆盖矩阵残余 21 格**（另 2 格复用 304/37_system）：负例 7+边界例 12，逐个 runner 实测 PASS——**M1 判据①=可测格 176/176=100% 达成**（支柱一闭环）。
> ③ **探针新立案 4 项**（plans/021 §三-D·待用户裁决排班）：D21 类继承析构链断裂（P1 资源面）/D22 三级链初始化列表误拒/D23 构造默认参数+初始化列表失效/D24 v2 侧 主 返回 结果 崩溃（D17 v2 面）。
> ④ 全量 CI：零警告构建+单测 **1327/1327**+E2E **394=394/0/0**+覆盖率 --strict PASS。

## 深度机 linux-x86_64 节

## 功能完善总结

> **234-a A7 本体根治收口（2026-09-16，深度机 linux-x86_64；基线 476b7d2，认领 8bb5511 前 ecae5ed rebase 后）**：
> ①**第一层根治：共享 AST 写回注记跨实例污染**——实例化类方法体 AST 全实例共享（mi.ast=模板成员），
> 语义检查写回型注记（resolvedType/resolvedSignature/retOwnedString/node->size）在共享节点上被
> 「最后检查实例」残留，IR 层逐实例生成全读残留值（实证：v2 树 9 处 `复制(下标链)` resolvedType
> 全=IR指令 56B → 误走分派④ CopyStruct 56B+写回槽地址；单实例程序巧合正确=225-a「同模块用户
> 泛型类正确」对照真因）。根治=emitClassMethod 生成前按本实例 typeArgs 重放方法体检查
> （checkSingleMethodBody 原样提取复用）+诊断快照回滚；方案裁决=重放（方案U）胜深拷 AST（方案A），
> 详见 plans/025 §二。
> ②**第二层根治（连带发现）：整体赋值源分派漏复制调用形态**——`数据[索引]=复制(...)` 元素槽=
> 栈地址 8 字节（乙0号=-1347670232 实锤）；emitStructWholeAssign 补分派（**收窄纪律**：仅复制
> 调用进新分支——宽版补丁致 v2p 产物坏 285 失败，单变量二分定位后收窄；一般调用返回形态=D11 待查）。
> ③**stdlib 六处深拷切换重新应用**（206-c 立案收口）：向量/栈/链表/队列/映射键/映射值（映射范围
> 收为 [0,元素数量)）；E2E 285 切换态双侧全绿（宿主 15=v2 同值）。
> ④验证链：零警告全量重建 + 单测 1327/1327 + **全量 E2E 313 用例 311 过/0 失败/2 跳** +
> 锚定链 **410460 行 fix_p≡fix_s 逐字节不变**（v2 树不含拷贝初始化形态=自举链零扰动） +
> valgrind 切换态 6 errors（=225-a 回退态基线，**A7 差值 7 归零**）+ 反证双件（回退补丁 rc=136
> 复现 / 插桩双态 IR 实证）。
> ⑤新立案：**D10 泛型函数体零检查**（泛型自由函数体从未语义检查，`复制(源)` 落普通调用链接期爆）、
> **D11 一般调用返回整体赋值待查**——均登记 plans/021 §三-D；源码改动=宿主 5 文件+stdlib 2 文件，
> 详情=plans/020 第七十五节 + plans/025 §二。

## 单位机 ARM64 节

## 功能完善总结

### 探针检测轮：B4 立案 + 检查网十层总图三项批准 + 249-a 预登记（2026-09-16/17，单位机麒麟 ARM64，基线 3d91531；验证轮不占轮次号·源码零改动）

1. **B4 立案**（提交 935ded0·plans/021 §3-B）：`cn run` 退出码透传全废（linux 双后端）——`cn_main.cpp:846` 直接
   `return std::system(...)` 返回 POSIX waitstatus（退出码 N→N<<8）经 main 截断低 8 位，任意退出码恒 0；
   反证三连（源码返回 1/3/5→包装后全 rc=0·假绿）；win 侧恰=退出码（246-a 探针 rc=3 真实·平台掩盖缺陷）；
   同族 5 消费点穷尽判定（508/512 诊断打印失真·280 恰好正确·253 无害）；E2E 门禁不受影响（subprocess 直跑产物）。
2. **检查网呈报→用户三项全批**：十层对照总图（Rust 检查设施↔CN 现状↔缺口动作↔建成判据）落 plans/026
   +新增第 2 层工具链 CLI 契约/第 3 层编译器源码覆盖率两维度；B4 根治绑定 CLI 契约矩阵设施
   （`scripts/check_cli_contract.py` 进 ci）同轮建成；「冒一个→校准一层」条款写入 026 收敛度量。
3. **249-a 预登记**（提交 c922d8a）：plans/025 新建 §三 单位机节（六要素齐备：026 总图修订先行→CLI 矩阵设施→
   B4 #ifdef 分治根治→508/512 失真修→ci 接入→反证+全量门禁）+§〇 总览/预登记表本机行刷新+B4 行裁决回填
   ——等家机 248-a 释锁后看板认领开工。

**诚实边界**：win 侧 CLI 契约形态本机不可实测（无 MSVC·249-a 登记跨机复验欠账）；github 镜像本机无凭据未推；
本验证轮源码零改动（`cn_main.cpp` 修复随 249-a 开发轮实施）。
