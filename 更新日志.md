# 更新日志

## 功能完善总结

# plans/016 linux-x86_64 后端专项落地（2026-09-05 用户裁决方案A，深度系统 x86_64 本机 首个原生闭环平台）

宿主编译器新增第三代码生成后端 `linux-x86_64`（GAS `.intel_syntax noprefix` + System V AMD64 ABI），
CN 程序首次在本机（x86_64 Linux）实现 **编译→汇编→链接→运行** 原生全链闭环。

## 一、新增后端（src/cn_compiler/codegen/linux_x64/，7 文件，独立目录零回归面）

- **架构**：对齐 ARM64 后端六文件结构（hpp+codegen+instructions+dispatch+i128+oop+vtable）；
  虚拟寄存器全栈槽映射（%vN → [rbp-8N-8]）；临时寄存器 r10/r9/r11 全 caller-saved；
  符号地址 `lea reg,[rip+sym]`（-no-pie/PIE 双兼容）；中文名修饰/类型编码/运行时符号表与 ARM64 完全一致。
- **SysV AMD64 ABI**：整型参数 rdi,rsi,rdx,rcx,r8,r9（第7起栈上 [rbp+16+8k]）；
  **浮点参数 xmm0~xmm7 整型/浮点独立计数**（Win 共享槽位号之关键差异）；
  隐藏返回指针 rdi（第1整型参数位）+ 返回 rax；16 字节栈对齐纪律（needHiddenRet 分配量 +8 偶数化）。
- **i128**：双槽模型（add/adc、sub/sbb 进位传播；乘除余/比较走 __cn_*_i128 运行时辅助）；
  i128 常量走 **.data 全局常量池**（L128cN，lea rip 相对取址）——不复制 ARM64 [sp,#0..31] 栈临时区
  与最深变量槽的理论重叠隐患。
- **retbuf 帧内固定区**（retbufFrameOffset_，对齐 win x64 2026-08 自举修复）：调用点隐藏指针
  rdi 指向 [rbp+retbufFrameOffset_]（rbp 相对持久），杜绝 rsp 临时区 add 后悬垂。
- **形态 A retbuf 契约锚定**：用户结构体返回调用由 IR 层预插 retbuf 地址为 operands[0]
  （`调用 %v62 10 2 (void)` 实证），后端原样传递自然落 rdi——对齐 ARM64（单位机全量绿 + 本机全量绿双平台锚定）。

## 二、实施中根治的六枚后端缺陷（汇编器/运行时/gdb 三层实证，全入 lessons）

1. x86_64 无 `imul r64,r64,r64` 三寄存器形式（仅立即数三操作数）→ 乘法一律双操作数 `imul r10, r9`。
2. 32 位类型比较位模式不一致：i32 变量装载清高32（零扩展位模式）vs 64 位常量全1（mov r9,-1），
   64 位 cmp 把负数枚举 `-1 == -1` 误判不等 → 非宽类型一律 `cmp r10d, r9d`（32 位比较，
   对齐 ARM64 w9/w10 策略本质）。
3. 32 位类型除法同因：i32 负数 64 位 `cqo+idiv r64` 按正数除（-8/2 商 2147483644）→
   32 位类型 `cdq+idiv r9d`（含商/余双路径 store，修复途中补上提前 return 丢 store 的回归）。
4. prologue/epilogue 配对：push rbx 先、push rbp 后（rbp 紧邻返回地址），epilogue
   `mov rsp,rbp / pop rbp / pop rbx / ret` 逐一还原——顺序颠倒 ret 读垃圾（gdb 实证 rip=0）。
5. movss/movsd 宽度前缀按类型分派（movss 配 qword ptr 汇编报 operand size mismatch，两处装载点）。
6. 空块条件跳转防御：先 `mov r10, 0` 再 `test`（空块终止保汇编合法性）。

## 三、平台接入（全部纯增量，win-x64/linux-arm64 代码路径零变化）

- backend_factory.cpp：`linux-x86_64` 分支（寄存器分配沿阶段C 决策默认关）。
- cn_main.cpp：--target 校验/工具链守卫（isGnuToolchain 复用 as/g++/-no-pie 链路）/帮助文案。
- run_e2e.py：平台跳过 linux-x86_64（62/69/78/79 + v2 闭环 23 例——v2 编译器自身暂无 x86_64 后端）
  + --target 校验 + 本机平台自动推断（x86_64 → linux-x86_64）。
- CMakeLists.txt：cn_compiler_codegen 库 +7 文件；tests/unit +test_linux_x64_codegen.cpp（15 测试）。

## 四、门禁（全绿）

- 构建零警告（GCC 12.3 `-Wall -Wextra -Werror`）+ 单测 **1238/1238**（原 1223 + 新增 15）。
- E2E linux-x86_64 全量 **154 用例：127 过 / 0 败 / 27 平台跳过**（本机原生闭环）。
- 新增 E2E **152_linuxx64_系统V调用约定**（SysV 分歧面：7 整型参数走栈/浮点整型混合独立计数/
  结构体返回隐藏 rdi/浮点返回 xmm0/结构体按值参数/递归对齐，7 断言全绿）。
- 专项计划 plans/016（架构决策/ABI 差异清单/任务拆分/实施结果）。

## 五、遗留（呈报）

- v2 自举编译器的 linux-x86_64 代码生成（代码生成X64Linux.cn）=后续专项（本轮 v2 闭环用例按平台跳过）。
- win x64 第4路 calleeReturnsStruct 与形态 A retbuf 契约的交互建议家机（MSVC）复核一轮（本机无 MSVC）。
