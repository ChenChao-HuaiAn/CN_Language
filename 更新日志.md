# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（529-a·D1 滚动=分词拆分·2026-09-20）

- **D1 行数滚动**：v2 树 `分词`（词法分析.cn）151 行→主循环+两分支函数（分词字符字面量/分词标识符·
  R3 惯例签名）——纯机械重构零行为变更；内联块「继续」→「返回 位置」等价变换。
- **等价铁证**：同宿主双 v2p 重建对拍——四产 asm 探针 md5 逐字节一致（t_call/t_copy/**t_lex 词法
  敏感**=932e40ca/230 全链）；Ninja 零警告构建+check_fn_length 出列；集成时全量门禁+develop 现态
  补跑兜底。
- **同会话前半程（522-a）**：编码对象调用ARM64 拆分已集成（eeac863c·develop 现态全量门禁补跑 4/4
  全绿=单测 1387+E2E 522 全 PASS）+三链集成让位协调+僵尸 gate_lock 陈锁×3 清除通报。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 528-a 501 链处置·裁决留痕·2026-09-20）

- **裁决=501 链作废**：全链 merge 实测 8 红（5 双编译真红=501 旧词法/语法/解析与 develop 现行系统性冲突·不可调和）——重做十余轮修复成本>>价值。
- **只提取有效增量**：EXTERN 收集口径修正（win 174 例 A2005 根治）+check_win_asm.py 三判据门禁——cherry-pick a2241af2；linux 全量 522=516/4/2 零新增红+单测 1387/1387。
- **487~494 用例**=501 v2 增量功能依赖（函数指针局部声明/枚举/浮点宽窄化=D40/D43 族）——随功能实现轮重新上架（501 分支 git 可考）。
- 459_v2 行号对齐=词法取 develop 现行即达成（行 6=438-a 口径 ✓）。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
