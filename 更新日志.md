# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（296-a·2026-09-17）

**D35 波B 销项：v2 错误传播运算符 `?` 全链实现（v2 语言特性缺口清偿·364/344/60 三实弹双编译转正）+T32 完整闭环**。宿主 C-1（2026-08 落地）后 v2 侧「`?` 一律三元」缺口根治，六文件：①语法/解析表达式.cn——`?` 后表达式起点判定（宿主 ternaryQuestionNext 同构：字面量/标识符/(/[/真/假/无/自身/父→三元·否则→错误传播）+节点_错误传播 建节点+建节点后二元链续接（`(x?) + 1` 宿主 postfix 同语义）；②IR/IR常量.cn——节点_错误传播=48；③IR/生成表达式.cn——收集错误传播区间+主循环分派；④IR/生成表达式运算.cn——生成错误传播（操作数两形态分流〔调用=retbuf 地址/变量=左值地址〕+条件分块+err 块 retbuf 就地构造 `{标志=0@0, 错误=E@联合偏移}`+IR_返回+跳尾块·ok 块读值压栈·宽错误码 IR_类型转换 桥接·读/写偏移按 max(结果值偏移(T),结果值偏移(E)) 双侧独立）；⑤IR/生成函数.cn——当前函数尾块ID 全局登记（不安全 访问器·当前方法类名ID 同款）；⑥语义/语义检查.cn——检查错误传播表达式（操作数须 结果/可选+当前函数返回类型须 结果/可选 双检查+附加/文本回填标注〔装箱构造 子终点 标注同款「语义驱动 IR」〕）。修前指纹（364 出 0/344 出 962132692608 垃圾/60 段错误 0xC0000005）→修后三用例逐字节对齐 .expected（364=链式好 11/链式坏 6·344=7·60 八形态全集：多层链/传播后运算/宽错误码整32→整64/可选传播/实参位）。三用例上架双编译对照.txt（断言输出=是·借链=否）；T32（主返回结果段错误）随 344 转正完整销项。宿主 src/runner/stdlib 零改动（纯 v2 树对齐轮）。验证链=单测 1353/1353+全量 E2E（三新上架零回归）+双文档门禁；教训两条入册（v2 节点池「子终点=自身索引」惯例/平铺解析后缀优先级须显式续接二元链）。明细=plans/025 §1.0+HANDOFF 家机节。
## 功能完善总结（287-a·2026-09-17）

**T13 根治（用户裁决方案A·linux_x64 后端 O0 移位发射 i32 零扩展污染）**：emitShift i32 非常量路径补 `movsxd r10, r10d`（对齐 270-a T14 同族先例）——M3 采样 s2026091705 asm 三件套定案（-426>>13 曾出 524286 应为 -1·O3 常量路径巧合对·285-a「O0 对 O3 错」误判反转·归 T13 非 T12）；免疫面四重实证（i8/i16 movsx 装载·win sar eax·arm64 asr w9·v2 D34 规范化）=三后端+v2 树零改动。新 E2E **403_移位负值矩阵**（11 行·i32 负值变量/常量 sar+shl+正值零回归+i16/i8/u32 免疫+mod 取模+污染值判定面·expected win 实跑固化）+coverage_map 登记。验证链全绿：零警告+单测 1351/1351+E2E 422=422/0/0（含 79 自举闭环=win 锚定链逐字节不变）+CLI 50/50+面覆盖 PASS+修后回归轨 38/38+修前修后 .s diff 仅 4 行 movsxd 插入；v2p 重建实证 v2 免疫（附带发现 v2 移位量掩码 63/31 分支疑似 mod 分叉面·观察待独立甄别）。跨机项=X64L 锚定链重锚+运行级复验归深度机。

## 深度机 linux-x86_64 节

## 功能完善总结（🧪 M3 采样轮·增量5+B9 扩散轮·验证轮不占号·2026-09-17，基线 1903a2f→478d804·源码零改动）

**daemon 67 轮对账 + T28 族双实例立案 + T27 B9 补轴 72 形态**（家机 🏃 296-a D35 波B 开发期间持续采集·零交叠互认）：

- **daemon 在飞 67 轮**：命中集合不变 5 项=T26×3+T27 双位宽×2（**s2609179005 整64=族内新实弹**）——已立案缺陷持续暴露（回归轨正常·待根治）；月日志 6212 行触发门禁→按 S1b 记法本意手工压缩为汇总段（3939 行）。
- **T28 族双新实例（plans/021 §三-G·设施判据与语义脱节族·验证轮只登记）**：①daemon 归类判据文本反推断裂（cnsmith_diff.py 清单行只写 rc=-8→daemon「运行失败」字样匹配落空→误归 diff/+重复副本）；②S1b 汇总记法结构性失效（消失回归恒非空→无变化永不成立→每轮全量记录=日志膨胀根因）——方案甲/乙/丙已列待设施轮根治。
- **T27 B9 补轴（面测试网·点→线→面）**：72 形态三方对拍（双位宽×÷/％×8 求值位置×变量/字面×O0/O3）→ **26 分歧两大新形态**：①**整32 变量形 O0 idiv 崩/O3 回绕不崩=级别间行为分叉**（14 形态）；②**整64 字面形静默错值**（O0=-2147483648=INT64_MIN 低 32 位符号扩展精确位样/O3=2^63 位样或 0·双级别互斥·12 形态）。甄别链：纯字面打印/变量中转打印 O0 全对→截断仅挂「字面常量直连÷/％」通道；emitDivMod 两后端真 idiv 已排除→挂点=IR 构造/常量装载层（留根治轮）。**正面**：短路惰性正确豁免溢出（4 形态）·整64 变量双级别崩一致·除零防护✓。**B12 呈报已扩注**：四路行为不一致（O0 陷阱/O3 回绕/字面截断/位样直通）——唯甲（统一回绕）同时消崩溃+分叉+错值。

**验证链**：B9 补轴 72 形态三方对拍实测（分歧 26·一致 42·剔除探针生成器 bug 4）+判型/通道甄别实验+门禁三件独立执行退出码=0（含总表戳漂移修复=刷 296 在飞）；落盘=面台账行 62 扩注+plans/021（§三-G+T27 行+B12）+月日志汇总段/扩散轮段+plans/025 §二+HANDOFF 本机节。

**诚实边界**：整64 字面错值的确切生成层未读到行（两假设并立·已注记为挂点假设非定论）；oracle 为自建硬件语义模型非 gcc 对拍（根治轮须双 oracle 复验）；T27 arm64 未测（归单位机）；daemon 汇总记法未根治前重启仍退化全量记录（每 ~45 分钟压缩监控·红线 5000）。

## 单位机 ARM64 节

## 功能完善总结

### 🧪 M3 采样轮⑤+五连轮总账（验证轮·不占号·2026-09-17，源码零改动·全部已推送）

1. **轮⑤**：daemon 293×2 全绿+行 22 常量折叠/行 17 优先级结合性 arm64 矩阵零命中（15 形态三方对拍全一致·顶层常量推断形态口径确认）——台账行 17/22 注记。
2. **五连轮总账**：①轮①行 9/12/52/57 arm64 复验闭环+B10 呈报（三元 ? 歧义代价面）；②轮② daemon 393×2+T24 立案（后缀自增 i++≡++i·规范缺口）；③轮③ T25 立案（浮64 三元 -O3 归零·双铁证）；④轮④ T25 面扩充（if/else 浮点同名单同挂·最小日常形态实锤）；⑤轮⑤ 零命中。daemon 累计 10 轮全绿·T22 零复发。
3. **待用户裁决**：B10（§3-B）/T24/T25（§3-G 双案·T25 日常高频紧急）——根治方向与方案甲/乙/丙均已在案。
