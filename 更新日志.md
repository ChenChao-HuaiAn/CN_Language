# 功能完善总结

## plans/017 呈报①方案A 第五轮：第 16 枚【根因级】根治——v2 指针参数三重缺陷（2026-09-06 第二十三轮 深度系统 x86_64）

### 一、定位方法学（v2 源码分步插桩对拍法，三轮夹逼）

- 插桩（[d16] 打印）注入 IR容器/IR签名，宿主编译版 v2p 与 v2p 编译版 v2self 跑
  **同一插桩源码**逐行 diff stderr——分歧行即污染时点。
- 第 1 轮夹逼到 查方法签名 pre1 与 替换函数 e1 之间；第 2 轮细粒度拆句证明**替换
  函数全程正常**（元素() 读、拼接、返回值干净），崩在返回后；第 3 轮 gdb watchpoint
  时点锁定写者：`mov %r11,(%r10)`，r10=**实参们槽地址+7**。

### 二、根因（两层缺陷叠加，均根因级）

1. **毒地址发射**：`查方法签名(方法签名* 出)` 的 `出.参数串 = ...` 左值发射——
   ①指针参数误走 `IR_取地址`（取指针槽自身地址）少一层解引用；②成员偏移语法层
   未标注（-1）直发 `IR_字段地址.附加=-1`。合成目标=`实参们槽+7`，8 字节写把返回值
   LE 首字节（0xd0）叠入句柄槽最高字节 → 句柄 `0xd00000000177eca0` 解引用崩
   （v2self IR 生成 SIGSEGV 全链根因）。
2. **参数槽分配错**：`方法签名*` 被当结构体值参数（32 字节 4 槽+prologue 解引用
   拷贝）→ `出.基槽 = *签 首 8 字节`（=签.参数串 值 0）而非签地址。毒地址修复后
   由第二层暴露（成员写空指针）。

### 三、修复五处（Rust 准绳：单一语义 + fail fast + 零开销）

1. `语法分析.cn` 三处（参数/局部/静态）：`变量结构体表` 登记放宽 `附加 <= 256`
   （指针形态同登记）——成员偏移语法层即标真值。
2. `IR生成.cn` 新增 `指针参数槽们` 机制（映射+查询函数+清空+参数收集登记）。
3. 参数收集：指针参数**单槽直存**（拷贝字节=0、参数槽数=1），不再多槽解引用拷贝。
4. `生成左值地址`：标识符分支三态（引用槽→栈顶 / 指针槽→IR_加载 / 其余→取地址）；
   成员偏移分桶兜底+未知即 `IR层诊断` 拒绝发射（毒值 fail fast）。
5. 右值成员访问：指针参数与引用参数同款（栈顶即基址）+同款兜底与拒绝。

`p.字段 ≡ (*p).字段`（v2.1 用户裁决指针自动解引用）至此在 IR 层真正落地；偏移未知
=编译期 [ir2] 错误，绝不发射 -1 毒指令；单槽直存与标量同款零运行时开销。

### 四、验证（全部实证）

- **毒指令全文清零**：objdump 形态 `add $0xffffffffffffffff,%r10` 修复前 60 处 → 0。
- **插桩对拍**：v2p 与 v2self 的 [d16] 序列 56 行逐行一致。
- **v2self 编译 f2.cn 全链 exit=0**（修复前必崩 139）——第 16 枚闭环。
- **九探针三方对拍**（宿主直编 / v2p 编译 / v2self 编译 → as → 链接 → 运行）：
  min1/f2/f3/alias/vieww/retown/elecalc/freg **8 项 PASS**（rc 一致+输出逐字节一致），
  **v2self 首次直编容器系探针并运行成功**。
- **min1 里程碑保持**：v2self 产物与 v2p 逐字节一致。
- **门禁全绿**：构建零警告 + 单测 1240/1240 + E2E 157 用例 130 过/0 败/27 平台跳过。

### 五、新立案：第 17 枚（v2self 第 9 层）

mapref2 探针（`灌(映射<整64,整64>& 表, ...)` + `表.设置/表.获取`）v2p 产物运行正常，
**v2self 产物运行时 `链查找` this=0（运行时错误3）**。IR dump 对拍实证：v2p 发射
`op=40 → op=44`（引用参数双层解引用），v2self **漏发 op=44**。解析层一致（103 节点）、
池一致、前 17 条 IR 一致——`槽是引用` 判定在 v2self 运行时 miss，嫌疑=编译器内部
`引用参数槽们` 映射运行时状态异常（第 16 枚同族内存问题另一形态）。下轮插桩续挖。

文档同步：plans/017 第十二节 + plans/014 状态表第二十三轮 + lessons.md（权重 10）
+ HANDOFF.md。
