## 功能完善总结

### 一、单位机三项门禁收口（linux-arm64 全量复确认/118 复查/78 分诊）

> 本轮在单位机（麒麟 Linux ARM64）完成第五轮（家机）HANDOFF 列明的三项待办，全部闭环。

- 清场重建（GCC `-Wall -Wextra -Werror` 零警告）+ 单测 **1196/1196**。
- **linux-arm64 全量 E2E：129 用例 124 过/1 败（78）/4 平台跳过**——上轮欠账全清：
  **127 新增用例 ARM64 首次运行时全绿**（A 嵌套容器/B 容器字段指针槽/C 顶层静态
  GAS `.quad` `.data`/D 引用参数 byRef——家机只对称实现未验证的部分全部运行时实证）；
  119~126 v2 全系零回归；逻辑非修正 GAS 形态绿。
- **118 Linux 侧复查：全量中绿**——上轮疑点消除，第四轮宿主缺陷簇改动未引入回归。
- **78 Linux -11 分诊定案**：dmesg 实锤 OOM killer（`total-vm 26.6GB / anon-rss
  11.4GB` 耗尽 15GB 物理内存被 SIGKILL；E2E 内 -11=分配失败解引用形态）——**与
  win 侧 201 内存超限同因**（v1 旧架构字符串行 IR 已知遗留，v2 完成后自然解决）；
  「级联双重释放」假设排除（无 glibc double-free 报错；第三轮已红证明非第四轮回归）。

### 二、③ 自举对拍启动：v2 内置函数全链（E2E 128 六步闭环绿）

- **冒烟踩缺口**：v2p 编译 词法分析.cn 实测——语义 66 诊断（驻留/运行时错误/字符串
  长度/类型大小未声明）+ 链接期裸中文 hex 符号（`bl _hex(驻留)` undefined reference）。
- **E2E 128 立项**（`tests/e2e/128_v2_内置函数与字符串拼接/`，立项期=宿主通用路径+
  宿主实际输出期望=宿主级回归；实现绿后注册 v2 闭环，期望=`[sem5] 正确程序 错误数=0`）。
- **v2 四层实现**：
  - `IR容器.cn`：`内置函数符号`（中文→`__cn_*` ASCII 映射表）/`内置函数返回类型ID`/
    `类型名宽度`（原始类型宽度表+结构体布局表）；
  - `语义分析.cn`：检查表达式 标识符/调用两分支内置白名单；
  - `IR生成.cn`：调用分支内置直调（附加=ASCII 符号）；**类型大小(类型) 编译期折叠**
    （IR_常量整数 操作数1=值文本驻留 ID——传数值必错，实测根治）；**字符串拼接 +**
    （`__cn_str_concat`，右末端节点字符串形态+字符串寄存器集合三登记点，左结合链式）；
    **打印/打印行** 复合展开（逐参 `__cn_print_str/int`+尾随 `__cn_print_newline`）；
  - 代码生成零改动（`__cn_` 前缀双后端符号修饰直通；ABI 权威=宿主 asm 裸符号直调）。
- **验证**：E2E 128 六步闭环 PASS（运行退出码 0=10 类断言全过）；asm 实查 `bl __cn_*`
  九种符号全直调；**全量回归 130 用例 125 过/1 败（78 已知遗留）/4 跳——零回归**。
- **首组件冒烟结论**：词法分析.cn 语义 0 错误；IR 崩点=字符串下标 `源码[位置]`
  （v2 未实现语法）——**下一缺口，E2E 129 用例先行**。

### 诚实边界（用户审计点）

- 本轮 v2 改动的 **win-x64（MASM）侧未运行验证**（单位机无 MSVC；对称实现+共用决策
  层，风险低）——家机下轮跑 ci.ps1 全量门禁确认。
- 字符串拼接首版仅覆盖**字符串+字符串**（左右任一字符串形态）；`字符串+数值` 隐式转换
  （宿主 Task 2.9）未实现，v2 组件未用到（诚实记录，踩到再补）。
- 打印参数类型分派首版为 字符串/整数 两类（浮点/布尔/i128 的 `__cn_print_float/bool`
  未接，v2 组件用面未涉及）。
