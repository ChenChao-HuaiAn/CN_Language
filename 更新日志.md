# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百四十轮 140-a：波 3 续进——残余澄清（探针口径）+ 栈族达成（全链门禁绿）**（2026-09-14 上午，家机 win-x64；基线 0d72943）：
> ①**残余澄清（重要）**：形二/形四「增量 3」=**探针口径**（P136 的打印在块**内**=块内当前活动数）——
> 精确计数探针（`probe_f2count`）显示**块出口后残留 0**（完整释放）；**修正探针**（打印全移块外）：
> **形一~形五块出口后残留全 0** ✓✓ ——**139-a 四处分派的完整性得到全形态验证**；
> ②**元素级共享登记**（非泄漏）：乙深拷 +1 而非 +2（拷贝构造的 `数据[索引] = 其他.数据[索引]` 未深拷
> 元素串——句柄共享）；runtime「仅在册释放 + 清槽」保证无 double-free（残留 0 实证）；**语义别名**待专项
> （元素级深拷：下标写位字符串来源归一化核查）；
> ③**栈族达成**（扩容器族第一族）：**既有防线发现**——语义层对「无拷贝构造的有析构类」禁止按值拷贝
> （错误级）→ 扩族真正前置=stdlib 拷贝构造；补 `不安全 函数 栈(栈& 其他)`（照向量同款）+ 宿主收集面
> head 集合 {向量, 栈}（写入位同步）→ **栈探针（形一/形二/形四）全 0** ✓；
> ④**门禁全绿**：E2E **290/292**（78/79 OOM 既有败面）+ 单测 **1315/1315** + **锚定链 406097 逐字节一致**
> （栈拷贝构造未影响 v2 树产物）+ 对拍 **44/44** + 运行级 **3/3**。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
