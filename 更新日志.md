# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百三十五轮 135-a：波 2（plans/022 §七）——CN 不安全操作清单完整化（plans/023 编制完成，待批准规范）**（2026-09-14 上午，家机 win-x64；基线 8b87196）：
> ①**产出 `plans/023-CN不安全操作清单与双侧检查设计.md`**（plans/022 §五 缺口 4 + §七 波 2 + §九 第 4 项授权）：
> **CN 不安全操作清单 v1 = 11 类**——A 组现行 5 类（指针算术／指针下标写／联合体字段访问／FFI／裸释放）+
> **B 组新编 6 类**（裸指针解引用读 `*p`／解引用写 `*p = x`／指针下标读 `p[i]`／指针成员访问 `p.字段`／
> 可变静态变量写／常量空指针使用〔硬错误〕）；
> ②**Rust unsafe 面对照表 18 项**（14 纳入 / 4 不纳入含理由：指针比较=Rust 安全〔235 处存量惯用法〕、
> 指针类型转换=Rust as 安全、取地址、不安全函数调用=责任上移）——「最小不安全面」原则；
> ③**边界判定细则**：字符串语义排除（波 1 单一事实源承接：字符* 视图读/下标读安全；**B7-a 待裁决**=字符* 解引用写
> 是否豁免）、解引用糖优先级（避免重复报告）、常量空指针判定范围（一层常量传播；硬错误非观察期）、静态变量写
> （读安全——Rust 对照的 CN 裁剪）；
> ④**存量粗扫三面**（stdlib 10 / v2 树 45 / tests 384 文件）：`*p` 119、指针下标 162、成员访问 223、
> 指针比较 235、静态变量写 238、联合体 17（粗口径）；**现状探针六形态全零检查**（确认扩充必要性）；
> 迁移序=tests→stdlib→v2 树；
> ⑤**v2 挂点设计**（§八）：9 类挂点表（`推导二元类型ID`/`检查下标表达式`/`检查赋值语句`/`推导成员类型ID`/
> `检查表达式` 等）——v2 按类型 ID 集合判定（天然符合「类型类别枚举」方向，不做文本后缀）；
> ⑥**规范草案 §九**（待用户批准，AGENTS.md §2.3）+ 实施序列 §十（步骤 2=宿主 B6~B10 实装 136-a 候选）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
