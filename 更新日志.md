## 功能完善总结

### CN 语言编译器模块系统 v2.0 全面改造 — 第 4 层「crate 模型核心重构」✅

**时间**：2026-08-15

**核心价值**：命名空间隔离（用户核心诉求）——跨包同名符号不冲突、显式导入才可见（P1-1 修复）。

---

### 一、本层改动清单（A~H 全部分层完成）

| 子项 | 改动内容 | 文件 |
|------|---------|------|
| **A. crate 分桶** | mergeModuleDecls 的 seenTypes 全局去重 → 按模块分桶（跨模块同名类型/函数允许，仅同模块内重名报错）；FunctionDecl/StructDecl/EnumDecl/ClassDecl/InterfaceDecl/GenericDecl/VarDecl 增加 `moduleName` 字段 | module.cpp、module.hpp、ast.hpp、semantic.cpp |
| **B. 目录层级** | loadModuleTree 支持 `net/transport.cn` 目录形态（模块名 = 相对入口完整路径 `net::transport`，修正 pathStem 去目录 bug）；`模块 net` 声明 → 候选1 `net.cn` / 候选2 `net/transport.cn` 双路径搜索 | driver_module.cpp |
| **C. use 导入表** | importedModules_（首段集合）→ useImports_（模块名 → {符号集合, 别名映射, 通配符}）；visitImportDecl 解析 segments/alias/names/wildcard 全形式；花括号别名纯名调用重写 | semantic.hpp、semantic.cpp |
| **D. prelude + 内置 key `::` 化** | registerBuiltins 27 个限定名 `.` → `::`（数学/IO/文件/解析/时间/系统）；IR 层映射同步双兼容（`::` 新版 + `.` 旧版）；内置限定名无需导入即可用（prelude） | semantic.cpp、ir.cpp |
| **E. IR/codegen 包前缀** | 多模块场景函数链接符号加 `模块名$` 前缀（定义侧 mangledName + 调用侧 resolvedSignature 同步）；**单文件（无导入）与 主 函数不加前缀**（保持 cn_main/内置映射） | ir.cpp、semantic.cpp |
| **F. 顶层常量/静态** | parser 顶层循环新增 `常量 名 = 值` / `静态 [类型] 名` 分支（Program::globals）；语义层编译期常量折叠（globalConstValues_）；IR 层常量引用生成 ConstInt/ConstFloat/ConstString | ast.hpp、parser.cpp、semantic.cpp、ir.cpp |
| **G. 可见性交集检查** | ClassInfo 增加 moduleName/moduleAccess；模块私有类在 merge 阶段整体不导出（类内公开成员不单独导出）；checkAccess 类内访问控制拦截 | semantic.hpp、class_resolver.cpp |
| **H. 单测更新与新增** | 反转 4 个测试（跨模块重名/重复类型/限定调用 P1-1/交集）；新增 9 个测试（crate 分桶×2、use 别名/通配符、内置 `::`/点号兼容、顶层常量、交集、目录层级） | test_module.cpp、test_module_driver.cpp |

### 二、单测结果

| 指标 | 结果 |
|------|------|
| 全量单元测试 | **1087/1087 全绿** ✅（原 1078 + 新增 9，反转 4） |
| 编译警告 | **0**（MSVC /W4 /WX，全量 clean 构建验证） |

### 三、E2E 结果（45 例）

| 指标 | 结果 |
|------|------|
| 通过 | **35/45**（第 3 层基线 34/44；新增 39_chkstk 通过） |
| 预期失败 | **10 个，全部为第 6 层迁移项**（旧语法 8 个：27_module/29_core/30_container/31_map_set/32_algorithm/35_string_ext/38_tool/43_module_import 仍用 `导入 模块.符号`/`从` 语法；默认私有 2 个：41_ctor_overload/42_generic_field_loop 类内无 `公开:`） |
| **内置调用回归** | **25_math/33_io_input/34_file/36_time/37_system 全部通过** ✅（prelude `::` 化关键验证；第 3 层基线这些全失败） |

### 四、关键 BUG 修复（本层踩坑）

1. **pathStem 去目录**：loadModuleTree 用 pathStem 计算模块名，`net/transport.cn` 只取 `transport` → 改为完整相对路径去扩展名 + 斜杠转 `::`
2. **单文件链接前缀**：单文件场景（无导入）也加模块前缀破坏 cn_main/内置映射 → mergeModules singleModule 时不写入 moduleName（crateName 空）
3. **泛型实例名 `$` 误剥**：resolveOverload 把 `排序$整32` 当模块前缀剥离 → 仅当 `#` 存在且 `$` 在 `#` 前才剥离
4. **对象方法调用误判 P1-1**：`动物.描述()` 中 动物 是变量被当未导入模块 → 加 lookupVar 排除
5. **内置 `::` 化同步**：registerBuiltins + IR 映射 + 语义限定名拼接三处同步（含旧点号兼容）
6. **MSVC 时间戳**：apply_diff 后构建可能未重编（时间戳粒度）→ 需 PowerShell touch 强制重编

### 五、提交

- 提交哈希：待提交（develop 分支）
- 推送：gitcode（如 403 保留本地）
