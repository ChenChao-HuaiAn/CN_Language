# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 452-a 开发轮：C20 批5=编译期安全防线族 11 例 v2 闭环负测上架·2026-09-20·分支 任务/家机-452-C20批5·worktree=wt452 隔离·同机三并行之一）

- **负测面重扫（基点 develop 含 435 链）**：静态筛排除 develop 带 v2 配置 236 例+批1~4 宿主号 47 → 30 候选 → cn check 宿主拒 30 → v2p 同源编译四桶=sem5 11/parse 14/过 5（扫描脚本 wt452/target/p452/ 不入库；v2p=worktree 自建〔Ninja+sccache 242/242+cn build v2 树〕·与主树 451 会话完全隔离）。
- **批5 上架 11 例（sem5 桶全清）**：192/195/222（混合符号二元运算/赋值传参/解引用·Rust 对齐方案A）+197（转移后使用）+198（引用逃逸三形态）+201（常量引用写三防线）+209（字符串借出初始化）+223/224/227/229（借出视图失效/容器先亡/扩展边界/类方法体）——plans/019 编译期安全线（阶段1/2/3/3b/4'）v2 侧持久门禁成族补齐；v2 诊断与宿主同形态（措辞不精确锚定·450-a 先例口径）。
- **甄别不固 17 例**：parse 桶 T63 误导 12（「语句缺少分号」噪音诊断）+252（v2 类成员修饰缺口拒）+269（诊断形态异）+过桶 5（299/417/422/425/81=v2 防线缺口立案面·431-a 口径）。**C20 上架候选池近枯竭**——后续轮转 T63 诊断对齐前置轮或 D40 缺口桶定性。
- **固化方法学**：runner 同款负路径实跑固化主.expected（v2src<号>_v2 隔离键+v2work cwd+stdlib junction+输出路径归一→全量非空行锚定·非手工编造）；反证抽例 197（退出码改 0→FAIL→还原→PASS 实测闭环）。
- **验证**：11/11 runner 单跑 PASS+coverage_map 11 行+plans/021 C20 行/文首戳 452；全量门禁 gate_lock 排队跑（与同机 451 串行·终态随 try-build 通告回填）。
- **多开实况**：认领时 451 已被另一会话直推占号（fetch 改 452 免撞）——Edit「modified since read」=对方活跃信号 §8.8 实锤；主树 daemon 日志脏文件全程精确 add 单文件防裹挟。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 479-a F1-26 波1：Mem2RegPass 落地＋本会话七轮·2026-09-20）

- **服务轮三签**：459-a/467-a linux 回签（准予集成）+451 集成后复验备案。
- **470-a D10 双侧销项/474-a D46 第一段**（挂等家机 win 回签）：请求 8415a43f/94596572。
- **475/476（已集成）**：D46 二三段家族终态；**478（已集成）**：F1-26 侦察设计定稿。
- **479-a F1-26 波1**：**Mem2RegPass 落地**（mem2reg.hpp/cpp ~230 行·-O3 注册 SSAPass 后）——逃逸判据（AddrOf/Ptr 降级→Call 摸不到可提升槽=跨块直继正确性前提·相对 CopyProp 保守清空的关键增强）+环境不动点交集（汇合保守·波1 边界）+读侧直递（Load→result 寄存器替换·Store 保留=波2 消亡）；正确性=全量门禁 477=475/0/2 零新增红+自举链双 PASS；量化如实=q1 环主导样本 22%→22%（交集保守边界·收益主力=波2 Phi 全量化+Store 消亡）。
- **诚实边界**：环主导样本收益=波2 目标；常量不直递（白名单留 CopyProp）；477 挂等 470。
- **下一步**：470/474/479 家机回签→集成；波2 实施轮；477 解挂。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
