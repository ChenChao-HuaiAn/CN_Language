# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结
> **第一百五十二轮 152-a：波 5 tests 面迁移（735→0；波 5 三面全部清零）**（2026-09-16，家机 win-x64；基线 bf51782）：
> ①**普查**（260 用例批量 check）：**62 用例 / 735 行**六类观察期命中（Top：113_H8 容器持有类对象 61、
> 38_tool 59、111_H6 容器拷贝构造 57、237 双编译对照链表槽复用 46、30_container 36…）；
> ②**迁移**（plans/022 §九 第 6 项一次性授权：裸操作包进不安全函数、**断言语义不变**、**用例变更清单逐项
> 留痕**）：统一模式（按命中行反查函数定义行 → 加 `不安全` 修饰）——**58 文件 / 187 函数**；清单=
> `target/p150win/migrate_tests_log.tsv`（187 行，不入库）+ 本节+提交信息留痕；
> ③**复验**：tests 面命中 **735 → 0** ✓；
> ④**门禁全绿**：单测 **1315/1315** + 全量 E2E **290/292**（**「断言语义不变」验证通过**）+ **锚定链
> 406097 不变** + 对拍 **44/44** + 运行级 **3/3**；
> ⑤**波 5 三面状态**：v2 树+stdlib ✅（151-a：87→0）/ **tests ✅（152-a：735→0）**——**迁移面全部达成**
> （plans/022 §七 波 5 的核心内容）；`plans/020` 第六十二节。

> **第一百五十一轮 151-a：波 5 迁移起步——v2 树面清零（87→0；全链门禁绿）**（2026-09-16，家机 win-x64；基线 32d1d5a）：
> ①**迁移策略**：迁移=**加 `不安全` 修饰**（plans/019 既定：v2 不查安全区边界=修饰零语义；宿主零产物）；
> **试点先验假设**（IR容器.cn 4 函数 → check 14→0 + **锚定链 406097 逐字节不变**）成立后才全量
> （「小步验证假设」纪律）；
> ②**全量迁移（25 函数 / 73 处）**：stdlib/容器.cn 18（2 函数）+ v2/语义/借出检查.cn 16（2）+
> v2/IR/IR容器.cn 14（4，试点）+ v2/语义/语义分析.cn 8（4）+ stdlib/映射集合.cn 8（2）+
> v2/语义/语义检查.cn 5（3）+ 语义检查语句.cn 5（1）+ 语法/解析基础.cn 5（3）+ 语法分析.cn 3（3）+
> IR布局/IR生成 2/2 + IR常量 1；**stdlib 26 处随 v2 树编译一并迁移**（波 5 的 stdlib 面亦达成）；
> ③**结果**：**v2 树命中 87 → 0**（六类全清零）；**错误 0**；变更面 12 文件 / 29 行（纯函数定义行加修饰）；
> ④**门禁全绿**：单测 **1315/1315** + **锚定链 406097 逐字节不变**（零产物实证）+ 全量 E2E **290/292**
> + 对拍 **44/44** + 运行级 **3/3**；`plans/020` 第六十一节。

> **第一百五十轮 150-a：波 2 步骤 2——宿主 B6~B11 六类检查实装（plans/023 获批实施；全链门禁绿）**（2026-09-16，家机 win-x64；基线 8ab8509）：
> ①**用户裁决**（2026-09-16）：**`plans/023` 可以实施**——本轮落地 §十 步骤 2；
> ②**六类实装**（semantic 层单点）：B6 裸指针解引用读（Deref 分支）/ B7 解引用写（UnaryExpr(Deref) target
> case）/ B8 指针下标读（visitIndexExpr 指针分支）/ B9 指针成员访问（visitMemberExpr isDerefAccess）/
> B10 可变静态变量写（IdentifierExpr case isGlobalStatic）/ **B11 常量空指针解引用（Error 硬错误）**；
> **统一豁免** `isStringSemanticType`；**`assignmentTargetDepth_`**（赋值目标求值期抑制读判——同形态单报）；
> ③**探针全中**（B6/B7/B11 + B10/B8〔`p[i]=9` 抑制 B8 只报 A2〕/B9；字符* 场景零警告）；
> ④**v2 树命中面**（波 5 迁移面）：**静态变量写 54 + 指针下标读 26 + 指针成员访问 7 = 87 处**观察期
> （总警告 126→213）；**错误 0**；
> ⑤**门禁全绿**：单测 1315/1315 + E2E 290/292（六类零回归）+ 锚定链 406097 不变 + 对拍 44/44 +
> 运行级 3/3；`plans/020` 第六十节。

> **144-a 补正（2026-09-16）：关键字阶段 3（6 词上下文化）批准补登**——`保护`/`虚拟`/`重写`/`抽象`/`作为`/`模块` 六词上下文化经用户 userselect 批准，已补登 `plans/021` §3-A **A5** + `plans/024` §十（改造设计）+ `plans/001` §2.1（**50 保留字目标态**）；附 `plans/024` §10.5 **同机制邻项呈报**（`公开`/`私有`/`静态`/`常量` 是否一并上下文化待裁决）。源码零改动。

> **第一百四十八轮 148-a：波 3 泛化前置（uF）专项——成对修试验→回退 + 三连同型收敛**（2026-09-16，家机 win-x64；基线 cacebe6）：
> ①**成对修试验**（#1 读 + #2 左值共用 `classRefParamBase` helper——同式读写基址一致）：**uD/uE ✓** 但
> **uF 仍崩 + probe_chain 仍退化（拷贝=0）** → **按纪律回退**（源码零净变更）；
> ②**三连试验同型收敛（本轮最重要结论）**：146-a 全局（byRef 判定）/ 147-a 特判（#1 读）/ 148-a 成对
> （#1+#2）——**三个层次的修复结果完全同型**（uD/uE ✓、uF ✗、chain ✗）⇒ **chain 退化源不在「类引用
> 参数求值」的任何层次**（改动的存在使其出现，但退化点在别处）——**必须 IR diff 定位**（149-a：链表
> 拷贝构造 IR 基线 vs 修复版逐指令对比——首个差异指令=退化点）；
> ③**uF 路径新定位**：「标识符目标」→ `handleClassFieldAssign`（`ir_oop_field.cpp:168-179`——对类对象
> 字段生成 `StorePtr(genInstanceFieldAddr, genExpr(值))`，**该段本身正确**）；uF 崩因在别处
> （149-a 候选：`genInstanceFieldAddr` 或 `其他.表` 求值——probe_ctor vs uF IR 对比）；
> ④**源码零净变更** = 145-a 全绿基线（五形态全 0 + chain 拷贝=2）；`plans/020` 第五十九节。

> **第一百四十七轮 147-a：波 3 泛化前置（uF）专项——类引用参数全路径盘查 + 特判试验→回退**（2026-09-15，家机 win-x64；基线 7205317）：
> ①**全路径盘查（五路径清单）**：①成员访问基址（读，`handleClassMemberExpr`）②成员访问基址（左值，
> `handleClassMemberLvalue` line 355）③下标基址（`其他.值表[游]`——经②）④普通求值（`visitIdentifierExpr`
> byRef 分支——**漏类类型**=146-a 根因①）⑤调用实参（经④）；
> ②**特判试验**（#1 基址对「类引用参数」直接一层 Load——不动全局）：**uD/uE ✓** 但 **uF 仍崩** +
> **probe_chain 退化（链式拷贝=0）=回归** → **按纪律回退**（源码零净变更）；
> ③**退化根因（本轮关键结论）**：**#1 与 #2 的「读/写基址不一致」**——改 #1（读）后，同一表达式链
> `其他.值表[游]` 的读（一层 ✓）与写/左值（经 #2 仍两级 ✗）基址错配 → 链式容器拷贝构造（读写同式）
> 退化；**正解=成对修**（#1+#2+#3 同步——「读路径 × 写路径」成对约束，与 143-a「收集面 × 消费面」同型）；
> ④**uF 另有路径**（两次试验均未覆盖——「标识符目标」的类字段赋值 `表 = 其他.表` 仍是最可能，
> 148-a IR 对比定位）；⑤两次试验（146-a 全局修 / 147-a 特判修）记录入库（uD/uE ✓、uF ✗、chain ✗ 全同型）；
> ⑥源码零净变更 = 145-a 全绿基线；`plans/020` 第五十八节。

> **第一百四十六轮 146-a：波 3 泛化前置（uF）专项——根因双重定位 + 修复试验→回退**（2026-09-15，家机 win-x64；基线 5ececa4）：
> ①**候选②排除**：IR dump 显示 `盒子.盒子`（构造）/`盒子.~盒子`/`盒子.盒子`（**拷贝构造**）三函数体齐备
> ——此前按 mangled 符号名搜索未匹配 dump 显示名（搜索口径修正）；
> ②**根因①=类引用参数求值多解一层**（结构体场景 uD/uE 崩因）：`ir_expr.cpp` 的 byRef 标识符求值判定
> `isArray || isStructType` **只认数组/结构体**——**类引用参数**（`盒子& 其他`）落入标量分支（`Load + LoadPtr`
> **两层**）→ **IR 实证**（拷贝构造体）：`加载 其他 → 指针加载 → 字段地址 → 指针加载 → 存储`——存入表槽的是
> **数据指针**（非表对象指针）→ 块出口 `DeleteObject(数据指针)`=0xC0000374；
> ③**修复试验**（byRef 判定加 `isClassType`）：**uD/uE 修复** ✓；**但 uF（纯类局部拷贝构造）仍崩**（另有路径）
> + **probe_chain 退化**（链式容器拷贝后大小=0；原=2）= **回归** → **按纪律回退**（`git checkout`）——
> 四族探针全绿恢复（五形态全 0 + 链式拷贝=2）；**单点改法牵动面超预期**（类引用参数遍布方法调用/成员访问/
> lvalue 路径——「一层」并非对所有路径普适）；
> ④**双重定位结论**（下轮基石）：uD/uE=已定位可修（需**全路径盘查**后启用）；uF=**另有路径**（修复①不覆盖——
> 疑与「类局部变量拷贝构造的调用/实参约定」相关，147-a IR 对比定位）；
> ⑤**源码零净变更**（试验回退）= 145-a 全绿基线；`plans/020` 第五十七节。


> **第一百四十五轮 145-a：波 3 泛化专项——崩因链定位（uF 既有缺陷=泛化前置）+ 构造体修复**（2026-09-15，家机 win-x64；基线 3892771）：
> ①**崩因链完整定位**（单变量 + 二分）：单变量 A（仅写入位拦截泛化）**不崩**；B（A+收集面泛化=完整泛化）
> **崩（2/2 稳定——复现 142-a）**；二分 D（整体拷贝）/E（字段位赋值）**均崩**；**uF（纯类局部 `盒子 b = a;`
> ——不经结构体）崩（2/2）→ **既有缺陷**（与泛化/结构体无关）；uF 在「四族+构造体修复」下仍崩 →
> **确认=泛化的前置缺陷**；
> ②**机制候选**（待 146-a 细查，二选一）：①「标识符目标」路径（裸字段名赋值 `表 = 其他.表`——AST 实证
> 目标=IdentifierExpr，不走成员拦截）对类对象字段未深拷（浅拷共享→块出口双删）；②拷贝构造体生成的 IR 缺失
> （IR dump 未见 `盒子$盒子#盒子&` 函数体）；
> ③**构造体修复落地**（保留；Rust 初始化语义）：`currentMethodIsCtor_`（ir.hpp + emitClassMethod 设置/恢复）
> + 写入位拦截跳过条件（构造体内对 this（自身）字段的赋值=初始化语义→跳过 preFree；非 this 接收者仍完整
> preFree）；
> ④**泛化暂缓**（纪律：前置未清的启用=回归——138-a/142-a 教训）；泛化路线（146-a+）：修 uF → 用户类字段
> 单形态→逐形态（多次复跑）→ 泛化启用；
> ⑤**门禁全绿**：四族探针全 0 + E2E **290/292** + 单测 **1315/1315** + **锚定链 406097 不变** + 对拍 **44/44**
> + 运行级 **3/3**。


> **第一百四十四轮 144-a：关键字体系审计 + 联合体处置裁决登记（文档落盘轮，源码零改动）**（2026-09-15，家机 win-x64；基线 42bf9e9）：
> **背景**：用户审阅 `plans/023` 时质询「`原始` 凭什么成了关键字？AI 在用户不知情下加了多少关键字？」
> → 触发关键字体系全量审计（60 个逐项）+ 135-a §五.5 联合体条目复核。
> ①**关键字全量审计**：逐项使用审计（**`实现`/`原始`/`多行` parser/semantic 零使用**）+ git 来源追溯
> （全部可溯：`aae4b70` 初始 / `a6031c9` 泛型·友元·常量 / `c5fc875` 自动 / `b00733f` 模块·作为 /
> `a454206` 不安全——**无无记录私加**）+ **四处清单互不一致实证**（规范 §2.1=58〔含已删「从」〕/
> lexer 实现=60〔头注释写 59、表注释写 61〕/ 89 用例串=61〔含已删「包·货舱」〕/ v2 表=28〔注释写 25〕）+
> 占用面实测（21 个常用词作标识符 ❌〔原始/实现/变量/保护/抽象…〕；10 词 ✅ 对照——含上下文关键字
> 「运算符/遍历/每个/属于」=非保留化先例）
> ②**135-a §五.5 误判翻转（根因）**：`target/p135win/probe_union_in_union.cn` 字段名 `整64 原始;`——
> 「原始」是**字面量前缀关键字**（`lexer.cpp:96`），报错与联合体成员类型无关（T 系列实测：结构体/
> 结构体指针/字符串/数组成员**全放行**；结构体下同报错）；`lexer.cpp:235-247` 缺「非前缀语境回退标识符」
> 分支 = **真实设计缺陷**（注释即意图、实现走偏）
> ③**联合体真实缺口（asm 铁证）**：含串联合体消亡**无条件按偏移 0 释放**（p13 asm:627——整数被当句柄，
> 类型混淆·被 tracked 守卫兜住）+ 非字符串成员覆写**泄漏 1**（p13）+ 指针成员持活句柄 → **误释放活对象
> = 静默 UAF**（p14 asm:704）；Rust 对照（union 成员须 Copy/ManuallyDrop）；影响面=全仓 5 处联合体
> **全纯标量**（零迁移成本）
> ④**用户裁决落盘**：**移除 `自动`**（`变量`/`自动` 二合一；存量迁移=2 用例 `16_function_extra`/
> `104_实例方法作值`，`自动`→`变量`）＋阶段 1 批准（删死关键字 `实现`、`原始`/`多行` 非保留化、注释修正、
> 四处清单同步 + `check_keywords_sync.py` 自检）＋**联合体方案 A+D**（成员类型限定·默认拒绝 + 手动释放
> 标注逃生舱）；产出：**`plans/024` 新建**（审计全案·实施依据）+ `plans/001` §2.1 同步（**60→56 保留字
> 目标态**）+ `plans/023` §五.5 更正/§十二 + `plans/021` §3-A **A3/A4 排班**。
> ⑤**源码零改动**（纯文档轮）；实施由后续会话认领（编号见 `plans/021` §3-A）。

> **第一百四十三轮 143-a：链式族专项——Bisect 澄清（141-a 无回归；142-a 的回退=误判纠正；四族全齐重立）**（2026-09-15，家机 win-x64；基线 42bf9e9）：
> ①**Bisect 两实验**（116_Feature2 各 3 次）：A=仅 stdlib 拷贝构造（链表/队列）→ **PASS ×3**；
> B=A + head 收集面扩 {向量, 栈, 链表, 队列}（=**完整 141-a 状态**）→ **PASS ×3** → **141-a 链式族无回归** ✓；
> ②**142-a「116 回归」真根因=半回退混合态**：142-a 的「回退泛化」脚本**中途崩溃**（ir_expr_assign 的 assert
> 失败）——ir_fields 已回退并写盘、ir_expr_assign 未回退（仍泛化：写入位拦截对**任意类**）→ 该**混合源码**
> 构建 → **116 崩（3/3 稳定）**；真崩因候选=**ir_expr_assign 泛化 × 用户类场景交互**（116 的 `资源` 类=最小
> 复现面）；142-a 的「链式族回退」=基于混合态的误判——**本轮回滚**（恢复 141-a 状态）；
> ③**恢复与验证（全绿）**：链式探针全 0（含空闲链状态；拷贝大小=2）；116 **PASS ×3**；全量 E2E **290/292**；
> 单测 **1315/1315**；**锚定链 406097 逐字节不变**；对拍 **44/44**；运行级 **3/3**——**四族全齐重立**；
> ④**教训（纪律补强）**：**诊断前必须确认工作区/构建完整性**——多文件回退脚本中途失败留下的**混合源码**
> 构建出「既不是改前也不是改后」的二进制，**基于它的回归结论无效**；**「稳定复现」≠「状态正确」**
> （混合态同样稳定崩）——构建前 `git diff --stat` + 关键判定行核对（一秒成本避免整轮误判）。
> **第一百四十二轮 142-a：波 3 专项核查 + 两次尝试两次回退（链式族 116 回归实锤清除）**（2026-09-15，家机 win-x64；基线 69e0214）：
> ①**元素级深拷核查**（专项）：下标写语义=「浅拷 + 源污染豁免释放」（plans/019 阶段4' 方案A「指针逃逸模型」
> ——`markStringTainted`；38_tool 形态）**是设计而非遗漏**；元素级共享根源=容器拷贝构造的
> `数据[索引] = 其他.数据[索引]`（下标写）✓；**UAF 面实测**（`probe_uaf2`）：`乙.记录.清空()` 释放共享句柄
> → `甲.记录.元素(0)` 读已释放内存（静默「正确」=堆未覆写；风险真实）；**修复正解 = 波 4 的 `复制(x)`**
> （泛型深拷——plans/022 §四已批准）→ 拷贝构造改 `数据[索引] = 复制(其他.数据[索引])`（Rust Vec::clone 同款）；
> ②**泛化尝试→回退**：收集条件撤销 head 收窄（任意「有析构类」）→ 用户类字段探针 **0xC0000374** → 按纪律回退；
> ③**链式族（141-a）116 回归实锤**：`116_Feature2` **3/3 稳定 0xC0000374**（141-a 的 E2E 单次通过系堆布局运气
> ——**「一次通过」不足以证明内存正确性**）→ **回退**（stdlib 链表/队列拷贝构造删除 + 宿主 head 集合回
> {向量, 栈}）；回退后 116 **3/3 PASS** ✓ + 向量五形态/栈探针全 0 + E2E **290/292** + 单测 **1315/1315**；
> ④**波 3 当前基线 = {向量, 栈}**（139-a/140-a 全绿面）；**专项登记**：链式族（先做「局部容器 vs 结构体
> 字段」交互面分析）、用户类泛化（先分析拷贝构造 this 初值 + `injectFieldCascadeDestroy` 重叠面）、
> 元素级深拷（前置=波 4）、v2 同构、H7/H11 收口。
## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
