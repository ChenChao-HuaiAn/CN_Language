## 功能完善总结

### plans/019 阶段4' A1：字符串借出初始化拒绝（方案A 用户批准——呈报三探针实证后裁决）
- **实证证据链**（呈报前置，探针留 target/p67x64l/ 可复核）：①驻留借出 `字符串 s=名单[0]` IR 实证被注入 `__cn_str_free`，运行侥幸不崩纯因 `cn_free_tracked` 注册表防御静默跳过非堆指针（语义错位）；②堆串借出+重新赋值→赋值位 free 旧→数组元素悬垂于函数中途→**读取乱码（UAF 实测三轮复现）**。
- **宿主**（semantic_decl.cpp）：声明类型=字符串且初始化器=下标/成员/解引用 → 编译错误「须 字符串复制(...) 显式落堆拥有化（字符* 借用视图随 plans/019 第三批）」；推断声明形态不在此拦（类型回填在检查后——与 v2 同边界，随 A2 收口）。
- **v2**（语义检查.cn 检查变量声明语句）：同款诊断（v2 语法面借出载体=字符串* 指针下标/成员）；负路径语义中止纪律。
- **存量预审零冲击**：tests/e2e+stdlib+v2 树全扫，纯借出初始化形态=0（命中 6 处均为二元拼接恒堆/调用污染借用——安全形态）。
- **E2E 209（宿主负测：三坏形态）/209_v2（v2 负测：双形态+诊断行固化）**双通道注册。

### 门禁（全绿）
清场重建零警告+单测 1289/1289+E2E 全量 **228 用例 223 过**（1 败=194 既有待批，4 跳）+锚定链 fix_p **254771**（新口径）固定点逐字节一致+运行级 3/3+对拍 29/29。

### A2 排程（第三批正文，规范变更已随方案A 总裁决）
字符* 借用视图类型化+返回 字符串=拥有/字符*=借用区分（stdlib/v2 树迁移面先出盘点呈报）——缺口②（用户函数返回泄漏）随 A2 根治。
