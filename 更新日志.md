# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（287-a·2026-09-17）

**T13 根治（用户裁决方案A·linux_x64 后端 O0 移位发射 i32 零扩展污染）**：emitShift i32 非常量路径补 `movsxd r10, r10d`（对齐 270-a T14 同族先例）——M3 采样 s2026091705 asm 三件套定案（-426>>13 曾出 524286 应为 -1·O3 常量路径巧合对·285-a「O0 对 O3 错」误判反转·归 T13 非 T12）；免疫面四重实证（i8/i16 movsx 装载·win sar eax·arm64 asr w9·v2 D34 规范化）=三后端+v2 树零改动。新 E2E **403_移位负值矩阵**（11 行·i32 负值变量/常量 sar+shl+正值零回归+i16/i8/u32 免疫+mod 取模+污染值判定面·expected win 实跑固化）+coverage_map 登记。验证链全绿：零警告+单测 1351/1351+E2E 422=422/0/0（含 79 自举闭环=win 锚定链逐字节不变）+CLI 50/50+面覆盖 PASS+修后回归轨 38/38+修前修后 .s diff 仅 4 行 movsxd 插入；v2p 重建实证 v2 免疫（附带发现 v2 移位量掩码 63/31 分支疑似 mod 分叉面·观察待独立甄别）。跨机项=X64L 锚定链重锚+运行级复验归深度机。

## 深度机 linux-x86_64 节

## 功能完善总结（🧪 M3 正确性收敛采样轮·验证轮不占号·2026-09-17，基线 9b2e2fd→rebase 至远程最新·源码零改动）

**daemon 常态多轮 + 定向形态探针六族 900 实例 → 双 P1 立案（T26/T27）**：①daemon 后台 23 轮（343 样本/轮=250 新时间戳种子+93 回归轨）**7889 次差分全一致·零命中**（生成器盲区=不打裸布尔/极值形态）+深语句轨 500 样本全一致；②六族定向探针 900 形态（极值/移位/布尔/无符号/转换/回绕，补 daemon 盲区）**命中 2 类**：
- **T26（P1·x86_64 单侧·-O3）布尔短路值位常量化**：左操作数非常量时 `||`→恒真 `&&`→恒假（值位消费全挂·条件位正确·O0~O2 全对·v2 免疫·比较左操作数免疫）；铁证=IR 使用点被替换为常量「搬运 %v6 真」+asm `mov rdi,1`；根因=256-a F1-26「SSA 使用点重写×多前驱 Copy 注入」常量传播缺多前驱一致性检查——**与单位机 T25（浮点三元 -O3 归零）同根因族**→§3-B B11 合并根治呈报。
- **T27（P1·崩溃族）整数除法溢出 SIGFPE**：`INT_MIN/-1` 与 `INT_MIN%-1`（宿主 -O0 32/64 位均崩 rc=136·-O3 64 位崩·v2 64 位崩/32 位未回绕）；对照=除零已有错误码防护✓、非 -1 分母全对→§3-B B12（并入 C18）呈报。
③B9 族扩散=30+14 形态矩阵×O0~O3 四级×消费位（值位/条件位/实参/返回/算术/三元）×v2 侧对照；④落盘=plans/021 T26/T27 立案+处置段+B11/B12、面台账行 58 ⬜→🔶（daemon 常态化实证）+行 61/62 新加+统计（61/62 PASS）、plans/025 §二回填、HANDOFF 本机节。

**验证链**：daemon 月文件连续+900 形态三方对拍 diff+IR/asm 双铁证+v2p 自举链四样本对照（T26 免疫/T27 64 位同病）+门禁三件独立执行显式验退出码=0。

**诚实边界**：T26 win/arm64 未测（同源代码路径·复验归跨机项）；T27 arm64 未测；生成器补布尔/移位形态=面台账行 58 欠账（本轮以定向探针替代覆盖）；同机 292-a 会话（402→405 改号）已收工在先，明细=plans/025 §2.1b 与 git。

## 单位机 ARM64 节

## 功能完善总结

### 🧪 M3 采样轮④（验证轮·不占号·2026-09-17，基线 5d718d1→认领 f745a0e·源码零改动）

1. **daemon 2 轮全绿**：293×2 一致/分歧 0。
2. **行 52 浮点汇合族 9 形态三方对拍**：**命中 k01/k08**——如果/否则双分支浮点赋值同名单、分支内嵌三元在 O3 归零。
3. **T25 面重大扩充（同号归并）**：最小日常形态 `如果(k>1){x=1.5}否则{x=2.5} 打印(x)` O3=0.000000·O0=1.500000·变量源同挂——**T25 从「三元」扩充为「浮64 汇合赋值通用（if/else 同名单+三元）」=日常高频·紧急度升级**；免疫面划清（三分支/循环携带/复合赋值/交换/整32/浮32 变量/常量条件全对）。根因证据链不变（IR 保守重写×asm 槽位不一致）。
4. 立案行扩充+面台账行 52 注记+文首戳刷新；T25 根治待用户裁决（浮点 Copy 发射链三后端同查）。
