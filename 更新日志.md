## 功能完善总结

### 一、v2 三缺陷根治（中途回退穿透 / &&·|| 短路 / 词法转义，E2E 125 验证）

- **缺陷A 中途回退穿透**（IR生成.cn）：函数IR 新增 函数尾块；IR_返回 后发射 跳转(函数尾块)，
  epilogue 唯一——中途 返回 不再穿透到后续语句（旧：最后的 返回 覆盖返回值）。配套块终止
  感知：生成语句/生成块/生成如果/生成当 返回 布尔（返回/中断/继续 后跳过不可达语句、
  终止分支不发汇合跳转、终止块不发块出口析构）。
- **缺陷B &&/|| 短路**（规范 930 短路求值语义；旧按 IR_相等 发射——0&&0 误真、右操作数
  急切求值除零即崩）：语法分析 二元链为短路层（||/&&）标注操作数后缀区间（附加=右起点）；
  IR生成 预扫描收集区间、运算符节点处延迟求值降低（&&：a 真→求值 b；||：a 假→求值 b；
  临时槽汇合两分支值 + 双逻辑非规范化 0/1 对标 C 语义）。生成表达式/生成左值地址 签名
  扩 块计数/槽计数 指针（mangled 核算约 213 < 247，MASM 符号限制安全）。
- **缺陷C 词法转义**（词法分析.cn）：字符串字面量转义实时反转义（对齐宿主 decodeEscapes：
  \n=10 \t=9 \r=13 \0；\" \' \\ 及未知转义取后继字符——未知保留反斜杠，C 语义）。
  快路径无转义单次子串（性能与原实现一致）；\u{XXXX} 暂不解码（v2 无字节级字符串构造，
  ③ 前特性补齐轮对齐，如实标注）。
- **E2E 125_v2_控制流与短路与转义** 新用例（断言失败 201~217 / 全过 0）：中途回退×6、
  短路逻辑×7（含除零右操作数不崩溃）、嵌套优先级、转义编译期回归。

### 二、v2 Linux ARM64 GAS 后端（阶段A，双后端第一步）

- **新组件 代码生成ARM64.cn**：IR→AArch64 GAS，对齐宿主 arm64 后端 ABI/指令选择
  （stp x29,x30/mov x29,sp/sub sp 帧 16 对齐；x0~x7 前 8 参、第 9 起 [sp+8k]/被调方
  [x29,#16+8k]；movz/movk 立即数分片；cset 比较、cbnz 条件跳转、sdiv+msub 取余、
  后索引 8 字节循环结构体拷贝、cbz 空安全删除；adrp+add 数据寻址（-no-pie）；
  GAS 符号修饰 _+hex+参数段（合法字符 字母/数字/._$）。位号约定（retbuf=0/this=1/
  实参从 2 起）恰与 AAPCS64 寄存器编号一致。
- **主.cn 目标平台参数**：可选参数 2（默认 win-x64 行为不变；linux-arm64 分派 GAS 后端，
  输出 target/v2asm.s）。
- **run_e2e.py v2 闭环双平台化**：执行v2闭环Linux（工具链探测 CN_AS/CN_CXX/PATH/
  gcc7；运行时 .o 缺则现编（对齐宿主命令）；as 汇编 + g++ -no-pie 链接；容器用例
  -Wl,-z,muldefs + v2asm.o 在前绑定 cn_main；nm 符号双向自检；POSIX 退出码 8 位截断
  取 预期值%256）；注册 125。119/120/123/125 在 linux-arm64 运行器内真实通过。
- 本机全量门禁：GCC 零警告 + 单测 1196/1196 + E2E 127 用例（122 过/4 平台跳过/仅 78
  已知遗留内存超限）；win-x64 运行时门禁待 Windows 会话（本机无 MSVC，如实标注）。

### 三、后端架构方案1：共用决策层（用户裁决）

- **新组件 代码生成共用.cn**（249 行）：符号修饰工具（十六进制位/字节位置/分割参数串/
  类型编码）、帧扫描、帧大小计算、槽偏移模型（寄存器/变量/retbuf）、调用编址（调用栈区
  大小/位号是栈参/调用方·被调方栈参偏移，平台参数化：win64 寄存器数 4+影子 32+锚定 48 /
  arm64 寄存器数 8+0+16）、标签唯一化（块标签 bb<函数序号>_<块ID> 双后端统一，消除
  MASM PROC 作用域依赖）、字符串常量收集去重。
- **代码生成.cn / 代码生成ARM64.cn 瘦身为纯平台发射器**（472/542 行）：助记符/寄存器名/
  平台符号修饰/段指令；双目标准绳——性能（决策单点，优化一处生效双平台）、安全（ABI
  决策单点，杜绝双份漂移静默错配）；第三平台只需纯发射器。重构后全量 E2E 零回归。
