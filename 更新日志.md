# 功能完善总结

> 85-a 聚合返回位所有权链断裂根治（2026-09-12 第八十五轮，深度机 linux-x86_64）：
> 复核 84-g「宿主字面量实参冗余复制」登记项时**将其证伪**（探针 P36/P37：宿主/v2
> **分配事件数完全相同**＝9 次；「+9 vs +6」的差 3 是**释放时机**——v2 调用后立即释放
> 实参临时字段串，宿主按 79-a 名单推迟到块出口），并在此过程中**揪出真缺陷**：
> **聚合返回位所有权链断裂**——返回类型含拥有型串字段（结构体/结果/可选）时，
> `返回 <借用来源>`（按值形参=调用方 place 的浅拷副本／全局静态／成员链／下标／
> 解引用）只做按位 memcpy → 返回值句柄与调用方 place **共享** → 调用方释放后返回值
> 字段**悬垂**（探针 P38：跨块读取 + 内存搅动后打印乱码；**宿主与 v2 两侧同现**；
> 数据正确性/安全级 UAF）。

> ① **复核证伪**：84-g 登记的「宿主每字段多一次 `__cn_str_copy`」不成立——运行时复制
> 次数两侧同为 8（4 次 `字符串复制` + 4 次元素深拷，汇编逐点核对）；P37「总分配次数」
> 口径两侧逐形态一致（形一 3／形二 1／形三 1／形四 2／形五 2）。登记项在 plans/020
> 第二十五节加**更正标注**、第二十六节给完整分解式。

> ② **三方案裁决（自行裁决乙，留痕）**：甲=**参数 move 语义**（零拷贝、Rust 原生，但
> 形参不再是借用视图、调用方实参变 moved-from＝**语言级语义变更**）→ 登记待裁决；
> **乙=返回位深拷归一化**（借用来源 → 独立副本：`Alloca` 临时 + memcpy + 字段级
> `__cn_str_copy`，直接写调用方 retbuf＝sret 就地构造；拥有局部/调用返回/字面量保持
> 零拷贝）⇒ **实施**（零语义变更、零存量破坏、与 79-a 写入位归一化同哲学；Rust 对照：
> `-> T` 返回值必须有所有权，借用来源须 clone）；丙=**编译期拒绝**（与 A2/E2E 210N
> 字符串政策同款）→ 聚合**无简洁显式复制语法**（无 `结构体复制` 内置），用户无法写
> 等价代码 → 登记不推荐。

> ③ **附带揪出两个独立缺陷**：**④ 基线段错误 rc=139**（`返回 *p`：`genExpr` 对解引用
> place 读值降级＝把结构体首字段当地址，汇编实证 `mov r9,[r10]; mov r10,r9; mov [rbx],…`）
> **同轮修复**（源地址一律取 `lvalueAddress`＝左值地址单一事实源）；**② 静态结构体不支持**
> （`.data` 槽恒 8 字节 `.quad 0` + `静态盒 = 盒子{…}` 编译为 `store 0`）**登记为下一轮
> 第一优先候选**（三方案已呈报 plans/020 第二十六节）。

> ④ **借出视图方法豁免（关键约束）**：容器读出接口（元素/读取/栈顶/队首/头部元素/
> 读取头部/读取尾部/获取）**设计上**返回借出视图（调用方不登记释放，生命周期由 77-a
> 检查器保证）＝Rust `Vec::get -> &T` 同款、与 A2 字符串「泛型单态化体内豁免」同理；
> 宿主把 `isBorrowViewMethod` **上提 public** 成单一事实源（v2 因 IR 层不得反向依赖
> 语义层而并列一份并互相注释）。漏此豁免 → 235/234/238/240 残留断言全失
> （实测 212/152/77/183）。

> ⑤ **实施两侧同构**：宿主 `ir/ir_fields.cpp` 新增 `isBorrowedAggregateSource` +
> `genOwnedAggregateReturn`（79-a 拥有权单一归属文件），`ir/ir_stmt.cpp` `visitReturnStmt`
> 单点挂接，`semantic/semantic.hpp` 上提共享；v2 `IR/生成语句.cn` 新增 `方法段名` /
> `是借出方法名85` / `是借用来源返回`，结构体返回位改 `发射结构体整体拷贝(深拷=真)`
> （直接写入调用方 retbuf，零额外 memcpy）。

> ⑥ **防线**：E2E **242_双编译对照聚合返回位所有权**（移植纪律 9 **第十三个**对照用例：
> 八形态〈形参返回·字面量临时／形参返回·标识符局部（跨块）／形参嵌套成员链／拥有局部
> 成员链／指针解引用／拥有局部 move 对照／字面量+调用返回链／循环 5 万轮〉折校验和
> **255** 两侧一致；每形态含跨块读取 + 内存搅动 + 内容比较 + 残留归零）+ **三项反证**：
> A 宿主借用判定恒假 → 242 宿主 rc=**-11**；C v2 借用判定恒假 → 242 v2 **160**（丢失
> ①②③④⑤⑦、保留 ⑥⑧ 对照位）；B 宿主借出豁免恒假 → 235 **212**（期望 255）——三注入
> 均已恢复复验、零残留。

> ⑦ **验证链全绿**：清场重建**零警告** + 单测 **1306/1306** + E2E **269 用例 265 过
> 0 失败**/4 跳（+242；其余 264 过＝零回归）+ 锚定链固定点 **327355 行**（新口径，
> 84-g 后 326126；+1229＝v2 侧返回位代码）两代**逐字节一致** + 运行级 3/3 + 对拍
> **34/34**（DIFF=0）+ 释放路径两侧对照（计数非零、无分叉）。

> ⑧ 文档五件套：plans/019（跟踪表第八十五轮行）+ plans/020（第二十六节 85-a 全节 +
> 第二十五节 84-g **更正标注**）+ lessons（85-a 段，权重 8，预防新增 78~81：
> 「返回值所有权＝独立检查面，写入位与返回位必须同一套来源分级」／同类检查面「上提共享」
> 而非并列复制（跨层依赖方向 > DRY）／新规则必须比对「设计上的借用返回」清单且用全量
> E2E 暴露、用注入反证必要性／复合缺陷先做形态矩阵再动手）+ 本文件 + HANDOFF。
> **诚实边界**：甲（参数 move）与丙（拒绝）登记；② 静态结构体登记；v2 结果/可选 返回
> 不在面内；84-g 性能登记项证伪；探针留 /tmp/p82probe 不入库。
