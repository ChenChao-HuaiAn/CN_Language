## 功能完善总结

### Task 完善C：优化器增强（代数简化 / CSE / 复写传播 / 跨块DCE / 全局值传播 / -O2/-O3 级别区分）

**2026-08-13 完成。** 在 Task 2.6 优化器（ConstFold + DCE + fixpoint PassManager）基础上新增 5 个优化 Pass，实现 -O1/-O2/-O3 分级优化，全部输出与 -O0 一致（规格书12.4）。

#### 新增 Pass（src/cn_compiler/opt/）
1. **代数简化 `algebraic_simplify.hpp/.cpp`（~230行）**
   - 恒等变换：`x+0`/`0+x`/`x-0`/`x*1`/`1*x`/`x/1`/`x<<0`/`x>>0`/`x|0`/`x^0`→`x`；`x*0`/`0*x`→`0`；`x&-1`→`x`；`x-x`/`x^x`→`0`；`x&&假`→`假`；`x||真`→`真`
   - **保守**：仅整型（浮点 +0/-0/NaN 语义跳过；i128 双槽无法单一常量跳过）；仅纯运算指令（副作用保留）
   - 结果替换：常量结果原地替换为 ConstInt/ConstBool（保留结果寄存器与 Branch 尾部条件）；寄存器结果登记函数级替换链（SSA 下无条件安全），原指令由 DCE 清理
2. **复写传播 `copy_propagation.hpp/.cpp`（~130行）**
   - 块内 Store→Load 转发：跟踪 唯一内部名→最近存储值，Load 命中 → 替换结果引用
   - **别名保守**：按唯一内部名精确匹配（不同槽互不影响）；多槽变量（结构体/数组）跳过；StorePtr/LoadPtr/Call/FieldAddr/AddrOf 后清空
3. **块内 CSE `cse.hpp/.cpp`（~120行）**
   - 纯运算"操作码+类型+操作数身份"哈希模式表，相同模式复用第一个结果
   - Load CSE（同槽无写内存时）；Store/StorePtr/Call 后失效；Store 本身不 CSE
   - **浮点 CSE 仅 -O2 以上启用**（-O1 与 -O0 输出一致性不受影响）
4. **跨块 DCE 增强 `cross_block_dce.hpp/.cpp`（~110行）**
   - 常量条件跳转折叠（Branch 尾部条件为"真"/"假"常量 → 无条件跳转，使恒真/恒假分支另一侧不可达）
   - 入口可达性分析（BFS 沿 Jump/Branch 目标）+ 不可达块整块删除（label 字符串互引，无需修正索引）
5. **全局值传播 `global_value.hpp/.cpp`（~100行）**
   - 函数级线性扫描常量 Store→Load 安全子集；非常量重写/Call 清空；多槽/浮点/i128 跳过

#### 共享工具 `opt_common.hpp`（~120行）
- `isPureArith`/`constCanPropagate`/`normalizeIntText`（0b/0x 归一化）/`RegRewriteMap`/`ConstRewriteMap`/`replaceUses`（寄存器无条件替换 + 常量白名单替换）

#### 级别区分（driver `runOptLevel`）
- **-O1**：ConstFold + DCE + 代数简化 + 复写传播（块内转发）
- **-O2**：-O1 + 块内 CSE（含浮点）+ 跨块 DCE（不可达块删除）
- **-O3**：-O2 + 全局值传播（常量 Store→Load 安全子集）
- `cn_main.cpp`：-O0/-O1/-O2/-O3 不再映射，`--opt 0/1/2/3` 支持 3；帮助文本更新

#### 测试
- 新增单元测试 6 个：`test_algebraic.cpp`（20）/`test_cse.cpp`（11）/`test_copy_prop.cpp`（8）/`test_cross_block_dce.cpp`（7）/`test_global_value.cpp`（8）/`test_opt_levels.cpp`（6），共 **+56**
- **656/656 单元测试通过**（原 600 + 新 56）
- E2E 新增 `13_opt2/优化增强.cn`（代数简化/CSE/复写传播/跨块DCE/全局传播用例）→ **E2E 13/13 通过**
- **优化正确性（规格书12.4）**：01~13 全部用例 × -O0/-O1/-O2/-O3 = **52 组合输出完全一致**（`target/opt_verify/verify_all.py` 验证 FAILURES=0）
- 冒烟：`-O2 编译 13_opt2` 运行输出与 .expected 一致；编译零警告（MSVC /W4 /WX）

#### 踩坑与解决
- `copy_propagation.hpp` apply_diff 误删类声明行 → 重新补全类定义（编译 C2059 系列错误）
- `patternOf` 静态成员访问非静态 `allowFloat_` → 改为成员函数
- 测试块构造缺终止信息（真实 IR 契约）→ DCE/跨块 DCE 误删 → 测试补 `endReturn`/`endJump`
- CopyPropagation/GlobalValue 登记替换无条件报 changed → fixpoint 永不收敛 → 登记不报修改，仅 replaceUses 实际替换报修改
