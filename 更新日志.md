# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第九十三~一百零八轮跨机轮复跑 + 109-a 修复**（2026-09-13，家机 win-x64；基线 4524049）：
> ①拉取远程 50 提交（深度机 93-a~108-a）**无冲突合并**；**前轮立案的 148_v2 崩溃已由深度机
> 102-a 修复**（根因链闭合：87-a 静态初始化有初值路径 StorePtr 丢失 → 空指针静态表 →
> 编译期内存状态异常 → retbuf 判定静默退化；本机复核 PASS + 最小探针 113 正确）。
> ②**全量 E2E** 292 用例 257 过 / 35 败（31 双编译对照平台限制 + 78/79 OOM + **158_v2 / 252 两项新立案**）。
> ③**109-a 修复（v2 win 后端浮点 size 说明符，10 处）**：MASM 的 movsd/movss 内存操作数须
> `qword ptr`/`dword ptr`（缺 → A2070；linux GAS 无需说明符 → 跨机轮才暴露）；91-a 增浮点分派
> 时仅浮32 加了说明符 —— 修复后 246 探针 v2 从 ml64 失败变 **rc=255 通过**，243/244/248/261/263 零回归。
> ④**新立案 5 项**（A 浮点说明符剩余漏点；B 158_v2 回归=91-a 引入；C 252 类名实例检查 win 侧失效
> =93-a 逻辑 win 未生效；D 260 宿主 rc=140；E 262 宿主 rc=1——后四项均 win/linux 分叉，疑 UB 类）。
> ⑤锚定链固定点 **403873 行**逐字节一致（109-a 后新口径）+ 组件对拍 **34/34** + 运行级 **3/3** +
> win 实物探针 11 用例（6 通过）。⑥诚实边界：**A 剩余漏点与 B~E 未修复**（已立案，待裁决）；
> 单位机 arm64 跨机轮未做；探针/二分产物留 `target/p108win/`（不入库）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
