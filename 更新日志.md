# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🧪 预置轮·用户批量裁决落盘+清零波次启动·不占号·2026-09-17，基线 216f9aa·源码/测试/门禁脚本零改动）

**用户 2026-09-17 两项裁决落盘（探测维持现状 + 待裁决批按方案甲批量批准约 16 项）**：

- **裁决落盘**：plans/021 §3-B 清空（B11→§3-A 波次行=B11 甲合并根治；B12→§3-C C18 行=整数语义统一族并入）；T21/T26/T27/T28/T36~T42 状态与处置段全量更新；**§3-A 新增清零波次行（8 波：1=B11 T26/T25 同族→2=T3/T4/T21→3=T6/T7→4=T19/T20→5=C18+B12〔含 T8/T27/T36〕→6=T37/T38/T40/T41/T42→7=v2 欠账族→8=C20 波2+批C/D）**；§3 优先序速览改写；文首戳刷新。
- **B 区纪律强化（用户现场质询）**：「无待裁决项就该彻底清空」——B 区收为纯标题；lessons 录第四形态（迁出说明残留）+登记 **C21**（check_handoff 增强：B 区非空行须以呈报条目格式开头·搭车任一开发轮）。
- **304-a 波次1 计划预置**（六要素落 plans/025 §1.0a·待深度机 303-a 释放锁认领）：B11 合并根治（T26=x86_64 -O3 布尔短路值位常量化）。
- **B11 侦查产出（实测）**：①win 侧确定性真值表探针（target/t26probe/p3.cn·标记文件+`||`/`&&` 四组合）**O0=O3 全一致**→复验「T26 win 免疫」成立；②IR 形态确认（短路汇合→ssa.cpp 使用点重写+Phi 降级 `搬运 %vN 常量`→使用点直用 %vN·与 T26 铁证同签名·平台无关）；③三后端 Copy 支持面（win✓/x64l 258-a 补齐✓/x64l default 仍静默=T11 残余）；④分叉点收敛=**x64l 后端发射**（优化层平台无关）→运行级复现归深度机。
- **甄别（诚实记录）**：初版时间戳探针「O3 恒假」系输入不确定假象（非缺陷·教训入 lessons「探针须确定性输入」）。

**门禁**：check_handoff EXIT=0（B 区清空后复核·首跑曾捕获单行 403 字符超限当场缩修）+check_progress_sync EXIT=0；双推 gitcode+github。

## 深度机 linux-x86_64 节

## 功能完善总结（303-a·T28 族三实例根治+T39 x64l 复验捕获回归根治·2026-09-17，基线 260a94e→认领 216f9aa）

1. **T28①注入器根治**（scripts/cnsmith_gen.py）：`# 注入类型` 标记行废除（预处理指令污染判据）+候选行筛选必真注入+`.cn.neg` 注入位置元数据——验收 400/400 全拒绝·0 崩溃·诊断行号零偏离。
2. **T28②归类判据根治**（scripts/cnsmith_diff.py+cnsmith_daemon.py）：差分清单行带 `[build_err/run_err/diff]` 结构化标签+daemon 删「运行失败」文本反推——rc=-8 实测归 run_err/；存量甄别：diff/ 4 件误归副本删除+m27_04/05 归位 diff/（输出分歧≠run_err）。
3. **T28③汇总记法根治**（cnsmith_daemon.py 记轮()）：消失回归改增量语义（与上轮未命中集合差集）——连跑 3 轮仅汇总（日志增量 22 行 vs 根治前 144 行），月日志爆炸根因消除。
4. **T39 x64l 运行级复验捕获同构回归并根治**（linux_x64_codegen_i128.cpp）：409 真跑 5 断言红→根因=emitInt128Shift 右移路径寄存器角色与左移相反而存储行共用→双半写反（位样全吻合：大>>1→INT128_MIN 等 6 断言）→三处存储按 isShl 分派；arm64 审查免疫/win 运行级已绿；修复前红/修复后绿=天然反证对。
5. **上轮采样收尾**：新命中 6 件入库+月日志 20 轮全量段压缩（5891→4857 回红线）+汇总段含 T39 修复后 daemon 6 轮零回归侧证。
6. **门禁**：全量 E2E 427=425/0/2（零失败）+单测 1353+spec --strict 100%+面覆盖 80/81+提交前双门禁；plans/001 零改动（无规范面变更）。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
