# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 339-a 开发轮 D38 批3a=入口函数签名检查三合一·2026-09-18·基线 1c58120）

- **304 无主拒**：「未定义入口函数 主」（宿主措辞对齐）。
- **372 主带参拒**：入口约定无参（v2 编译期拒=优于宿主链接期）。
- **374 主返回白名单**：整32 ∨ 结果<T,E>（T/E 均整数族·wrapper 退出码语义 344 对齐·宿主矩阵 z01 整64 拒/z02 过实测定规）。
- **实现=自包含现场扫描**：语义分析多文件 聚合后扫 顶层声明区（静态写须 不安全=沿收集链传染·零静态零传染）。
- **验证**：d38_scan 13 拒/4 缺（批3a 三项全转拒）+v2 树零错误编译+单测 1353/1353+全量 E2E（数字见提交信息·零新增回归）。

**门禁**：快速+全量门禁+双文档门禁；integrate.py 集成（协议v2）。

## 深度机 linux-x86_64 节

## 功能完善总结（🏃 349-a T35 ①类根治〔v2 解析层运算符声明 + `->` 后缀硬错误·无诊断崩溃 302 → 优雅拒绝〕·2026-09-18·分支 任务/深度机-349-运算符声明）

- **现象**：v2p 编译 `340_运算符重载参数边界`/`92_单目重载`/`52_library` 三例 → `运行时错误(错误码302)`（**v2p 自身崩·零诊断**）。
- **根因**：①解析层「`函数 运算符X(...)`」只取 `运算符` 为名、不消费运算符记号 → 参数表解析失败 → 后缀解析遇 `->` 触发硬错误；②`解析表达式.cn` 的 `->` 后缀位是**硬错误 302**（原设计「无法进程内自检」）→ 任何误遇场景直接崩。
- **修复**：三处「函数名 = 运算符 + 记号」组合 + 诊断 + `语法错误加一`；`->` 后缀 **硬错误 → 可恢复诊断**（+位置前进防死循环）→ 聚合中止通道。类型细节：`当前文本` 返 `字符*` 用零拷贝接；两文件补 `导入 IO;`。
- **验证**：a4 优雅拒绝（反证：回退→复现 302）；a3 正常；三例 2/2/9 语法错误·中止；新负测 **445** PASS；全量 **448=442/4/2**（零新增红）+自举链双 PASS+零回归探针十项。

## 功能完善总结（🏃 348-a D11 根治〔结构体返回调用整体赋值·甲方案源优先序·双侧同构〕·2026-09-18·分支 任务/深度机-348-整体赋值顺序）

- **现象与根因**：`成员/下标目标 = 结构体返回调用`（`甲.内 = 造点(20)` / `表[1] = 造点(30)`）→ **宿主与 v2 双侧静默错值**（首字段=返回值地址、余字段 0·探针 p29 铁证）；整体赋值通道右值白名单不含「一般结构体返回调用」→ 落标量 `StorePtr` 8 字节；234-a 曾放开=回退（调用含条件块在目标地址已发射后打乱块时序·全量 v2 树 285 失败）。
- **甲方案（Rust place 语义·源优先序）**：①先求值源（可能含条件块）②再算目标地址（纯地址计算）③整体拷贝。**宿主**：新增 `structWholeAssignSrcFirst` + 挂点 2 处（成员位/下标位·类型推导提取 `indexTargetElemSrcType`）；**v2**：成员链「节点_调用」分支 + 新增 `尝试下标结构体整体写`。
- **★回归当场根除**：首版守卫含 `isClassType` → 抢先覆盖 v2 树 `程序.函数们 = 向量<...>();` 的既有类深拷通道（浅拷语义）→ **v2p 自身全探针段错误（错误码 3）** → 修正守卫「仅结构体目标」+ `genExpr` 失败不落原路径。
- **验证**：p29 双侧 20 21 22 / 30 31 32；E2E 444（五形态·双编译）PASS；全量 E2E **448=442/4/2**（零新增红·4 红=基线既有 T55×3+426）+自举链 78/79_v2 双 PASS+单测 1353+反证（回退→复现）+存量盘点（成员 66/下标 27 处宽扫描·全量覆盖验证）。

**门禁**：零警告构建+全量单测 1353+全量 E2E 448=442/4/2+自举链双 PASS+反证+双文档门禁。双推 gitcode（github 待补推）。

## 功能完善总结（🏃 347-a T33 残余根治〔v2 窄宽类型符号性·三后端+宽度码〕·2026-09-18·分支 任务/深度机-347-窄宽符号扩展·头 18e7131）

- **根因（两处同族）**：①三后端 `IR_指针加载` 把 `T_整8` 与 `布尔/字符` 同组走**零扩展**、`T_整16` 走零扩展 → 结构体字段/数组元素/指针解引用**负值读回错值**（探针 p23：-426→**65110**、-100→**156**；**266-a 波B 只修了整32**，整8/整16 旁路未穷尽）+ `T_正8/T_正16` 落默认 8 字节读写（过读/**过写越界**）；②`类型宽度码` 把 **整8 与 正8 归并**、**整16 与 正16 归并**——符号性丢失 → 无符号窄**字段**被当有符号（探针 p24：正8 200→**-56**、正16 60000→**-5536**）。
- **修复**：三后端指针加载补符号性分派（整8→`movsx`/`ldrsb`、整16→`movsx`/`ldrsh`、**新增** 正8→`movzx`/`ldrb`、正16→`movzx`/`ldrh`）+ 存储侧正8/正16 宽度补（原 8 字节写）+ `类型宽度码` **四码拆分**。
- **验证**：探针 p22/p23/p24；E2E 443_v2窄宽符号性矩阵 PASS；全量 447=441/4/2 零新增红+自举链+反证+三后端 asm 指纹。
- **产出②**：集成预演结论（rebase 演练=代码/测试零冲突）+D10 定性更正（v2 泛型自由函数=显式拒绝→特性缺口级）+D11 确认呈报。

## 功能完善总结（🏃 346-a T57/T58 根治＋D33 旗标对等·2026-09-18·分支 任务/深度机-346-容器赋值深拷贝·头 24e9bb3）

- **T57 根治**：v2 容器/用户类对象「赋值语句」深拷贝（标识符四目标形态〔局部/参数/**引用参数**/静态〕+成员链左值；旧对象释放=Rust `a = b` drop 旧值·仅拥有型）——原 8 字节句柄浅覆盖致**双释放 UAF**（p8 rc=139）。**T58 同轮根治**（家族扩散探得·崩溃级）：类对象成员链左值基址=对象指针非槽地址。
- **D33 旗标对等**：runner v2p 调用点 4 处补用例旗标传递（**契约变更**·铁证=产物 md5 == 手工 -O3）+ 折叠器常量文本后缀剥离（387 转绿）。
- **门禁**：全量 E2E **446=440/4/2**（零新增红）+新用例 441（双编译）/442_v2+valgrind 双侧对照（v2 零 Invalid/UAF·泄漏面与宿主逐字节一致）+自举链+反证+单测 1353。

## 单位机 ARM64 节

## 功能完善总结

### 332-a 修复轮：看板先行机械强制（用户令）+ 看板表结构防冲突（2026-09-18，基线 044423b·分支 任务/单位机-332-看板强制）

1. **用户令落地**（2026-09-18「必须强制 AI 实际工作前先修改看板填写本机任务，完成后顺便销账」）：AGENTS §8.1 新增「**看板先行**」强制条——开工**第一动作**＝直推看板本机行到 develop（`git fetch`→定分支名→推板→才开分支动手）；**收工即销账**＝integrate.py 集成成功后立即清行并直推。§8.5 直推窄通道改「**强制直推、即改即推**」：看板行/通告段/try-build 请求三类信息写进任务分支＝他机不可见＝等于没写（当日实测根因）。
2. **check_handoff.py 新增「开工自检」**：当前分支为 `任务/*` 时看板必含该分支名，否则报错拦截「先动手后填板」——正例放行/反例拦截双向实证（含新结构下旧校验「🏃 行缺分支名」有效性）。
3. **看板表结构变更**：单表三行 → **每机独立小节**（`### <机名>`+4 列单行表）——实测单表三行相邻结构下两机同时更新各自行 rebase 必冲突（当日两次：本机 329-a 直推 × 深度机 331-a 连续直推）；小节间隔 4+ 行使三方合并友好；查看板() 同步适配。
4. **看板直推实战**（044423b·窄通道）：本机 329-a 在飞行直推 develop——发现三机看板行此前全在各自分支内、远端 develop 三行全空（「三机不知彼此在干什么」根因）；期间两次 push 被拒（深度机同日连续直推）→按协议 CAS 重试 rebase 解冲突复推。
5. **实测记录**：家机 329-a（D38批2）/深度机 331-a（T11·已发 try-build 请求）/单位机 329-a（D12+货舱延伸）三机同时在飞；跨机轮次号撞号实锤（家机与本机同为 329-a）——机制缺陷已在 HANDOFF 登记待裁。
6. 文档同步：plans/025 §3.20 六要素+HANDOFF/更新日志本机节+lessons。