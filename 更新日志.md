# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十五轮 125-a：D1 残余波——宿主 `ast.hpp` 1032→9 行（按层拆分 + 转发 include）**（2026-09-14 凌晨，家机 win-x64；基线 b21ae87）：
> ①**拆分**（纯重构零行为变更）：`ast.hpp` **1032→9 行**（**聚合转发层**）——按层拆出 `ast_base.hpp`（**285 行**：
> 前向声明 + NodeType/Operator 枚举 + AstVisitor + AstNode/Expr/Stmt 基类）/`ast_expr.hpp`（**309 行**：表达式节点族
> 20 类）/`ast_stmt.hpp`（**227 行**：语句节点族 11 类）/`ast_decl.hpp`（**259 行**：声明节点族）；统一依赖链
> base→expr→stmt→decl；**包含面零改动**——所有 `#include "cn_compiler/parser/ast.hpp"` 的消费方无需任何修改 ✓；
> ②**验证**：构建零警告 + **stash 对照法**（旧宿主 vs 新宿主编译 v2 全树 → 产物同 md5 `8e08d1da…`；4 用例样本
> 逐字节一致）+ **锚定链固定点 406097 行不变** + **对拍 44/44** + 单测 1313/1313 + E2E 290/292 + 运行级 3/3；
> ③**踩坑（入 lessons）**：拆分脚本**非幂等**（二次运行读到已转发版 ast.hpp → 产出垃圾头，已 git 恢复重跑）；
> 生成类脚本必须**带防重跑断言**（读源前断言原版行数）；**heredoc 中文编码**致 replace 未生效（改用 Write 工具写脚本）；
> 空行漏分配**第三次**被分段覆盖校验拦截（118/124/125-a）。
> ④**D1 现状**：宿主 + v2 侧全部文件级达标——**仅剩函数级 19 超百行**（`visitCallExpr` 885 行 / `visitAssignmentExpr` 931 行 /
> `injectContainerElemDestroy` 326 行 / `emitCast` 270 行等——提取子函数重构，属独立波次）。
>
> **⚠️ 待用户裁决（最高优先级 actionable，已八度呈报）**：`plans/022` §九 **8 项**（含第 8 项「波 1 修 `字符*` 拼接误报——可立即开工」）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
