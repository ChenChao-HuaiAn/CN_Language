# 更新日志

## 功能完善总结

# plans/016 呈报复核轮：win 第4路 calleeReturnsStruct 死代码归真（2026-09-05 方案A 用户裁决 家机 win-x64）

深度系统机 plans/016 专项呈报「win x64 第4路 calleeReturnsStruct 与形态A retbuf 契约交互待家机复核」——本机拉取后复核闭环，**呈报结论：win 侧不存在形态 A 实参错位**；连带揪出并根治**双后端同族死代码+假修复注释**。

## 一、呈报复核结论（四重实证，24 错误处理等全量锚定）

- 探针（结果+结构体双形态带参直调）IR dump/调用方 asm/被调方 asm/运行输出（7/30/40）四重实证：形态A契约（IR 层预插 retbuf=operands[0]、result.type=void）下 win 调用方原样传递自然落 rcx（Win ABI 隐藏指针位），与被调方 paramOffset=1 协议对齐，语义正确。
- 本机全量门禁：构建零警告+单测 1238/1238+E2E 154 用例 152 过/2 败（78/79 已知 OOM 家族零回归）——f9fe211 win 侧零回归实证（对方机器无 MSVC 未测面）。

## 二、新缺陷（复核连带揪出，当轮根治）

- **win 第4路 `calleeReturnsStruct` 从未生效**：`activeModule_` 成员全仓无赋值恒 nullptr，函数首行短路恒返 false；2026-08「自举检查修复」注释声称的「按被调函数 structReturn 判定」从未运行——当年 E2E 转绿真因是 IR 层形态A预插（双巧合互洽掩盖死代码）。危险反事实：若激活第4路，形态A调用会 argOffset=1 把预插 retbuf 当实参推到 rdx 全错位。
- **linux_x64 同族死代码**：新后端按「对齐 win x64」移植了同款函数（有赋值=活的）但零调用点——复制叙事未核实被移植侧行为。

## 三、方案A 根治（行为零变化，三后端判定同构）

1. win：删第4路调用+`calleeReturnsStruct`+`activeModule_` 成员（x64_codegen.hpp/x64_instructions.cpp），hasBigRet 收敛三路（i128/u128/struct* result.type）——与 linux-x86_64/arm64 完全同构；注释归真记录形态A契约与假修复史（勿复辙：激活会与形态A预插双重传参）。
2. linux_x64：删同族死函数（linux_x64_codegen.cpp 617-625）与 generateAssembly 赋值行（1050）+ hpp 声明与成员。
3. ir_decl.cpp/linux_x64_instructions.cpp 注释归真：形态A契约如实描述（IR 层预插 operands[0]、后端原样传递自然落隐藏指针位 win=rcx/SysV=rdi/arm64=x0；structReturn 标志仅供被调方 paramOffset 协议）。

## 四、验证与文档

- 构建零警告（MSVC /W4 /WX）+ 单测 1238/1238 + E2E 全量 154 用例 152 过/2 败（78/79 已知 OOM 家族与基线一致零回归）。
- lessons 高权重条目（死代码+假修复：回滚测试是「修复生效」的唯一可信验收；移植复制行为不是注释叙事；死代码当缺陷对待）。
- plans/016 第八节复核闭环 + HANDOFF 第十七轮。
