# 更新日志

## 功能完善总结

# plans/017 首轮收官：v2 linux-x86_64 代码生成专项（T1/T2/T5/T7 完成）+ 宿主重大缺陷「栈参锚定基」根治 + v2 四缺陷根治（2026-09-05~06 深度系统 x86_64，方案A 用户裁决；v2self 完整运行新缺陷呈报待裁）

## 一、专项主体完成（plans/017）

- **T1 新组件 `代码生成X64Linux.cn`（约 570 行 SysV 纯发射器）**：位号即 SysV 参数序（retbuf=rdi/this=rsi/实参 rdx,rcx,r8,r9/位6+栈）；数据临时纪律 r10/r11；结构体按值参数=r10+r11 逐 8 字节展开（宿主 emitParamSetup 修复后同款）；除/余=cqo+idiv（有符号正确）；rip 相对寻址；X64L 后缀防符号冲突。
- **T2 主.cn 三分派**+目标平台校验（未知平台显式报错）。
- **T5 冒烟 13 组件全绿**（exit=0+sem5=0+ir2=0+asm 产出）；**主.cn 产物（x86_64 GAS 418388 行）as 汇编（2.55MB .o）+g++ -no-pie 链接通过**——v2 编译 v2 自身的产物首次在本机完成汇编+链接；v2self（v2 编译的 v2 编译器）启动执行至读文件/分词/IR 阶段。
- **T7 门禁全绿**：构建零警告+单测 **1240/1240**+E2E linux-x86_64 **156 用例 129 过/0 败/27 跳过**。

## 二、宿主重大缺陷根治：linux_x64 被调方栈参数锚定基（冒烟揪出）

- **根因**：emitParamSetup 三处锚恒 16+8k——needHiddenRet（返回结构体/i128）时 prologue push rbx 使栈参数实际在 **[rbp+24+8k]**，旧锚把**返回地址当第 7 参数读**（v2p 建IR指令=7 参+返回结构体正中：类型字段=返回地址污染、IR 全链损坏、产物 asm 无法汇编；gdb watch+objdump 三方对表闭环）。
- **触发空白**：返回结构体+整型参≥7（E2E 153 仅 6 参；单位机历史绿=arm64 宿主构建——同源码不同宿主后端构建的 v2p 行为分叉掩盖缺陷；arm64 侧已按 needHiddenRet 补偿、win 无 push rbx 不受影响）。
- **修复**：锚=16+8*paramOffset（与 arm64 16+16*needHiddenRet 同构）。
- **验证**：E2E **154_linuxx64_隐藏返回指针栈参数锚定** 红→绿闭环（回滚 420598354321 污染形态→修复 87654321）+ 单测 HiddenRetStackParamAnchor24。

## 三、v2 四缺陷根治（冒烟连带揪出，全部实证）

1. **静态赋值存储丢失**：静态目标「目标槽==0」静默丢存储——补 静态序号们 查表→IR_静态存储（计数器=计数器+阶乘(5) 打印恒 0→120）。
2. **限定名内置 结果 接收 ABI 错配**：`结果<字符串,整32> 行 = 文件::读取文件行(h)` 把裸 char* 当 16 字节结构体拷贝——改合成构造（判空→正常@0、指针→值@8）。
3. **返回含容器字段结构体所有权悬垂**：浅拷 retbuf 后 RAII 析构局部向量=悬垂（函数数=垃圾）——析构器加跳过名ID（所有权转移；E2E 148 无容器字段未暴露）。
4. **跨文件同名函数符号冲突**：两个「生成函数」重载 asm 重复定义——改名 生成函数W（对齐 ARM64/X64L 平台后缀命名惯例）；「跨文件同名重载无消歧机制」呈报项③。

## 四、呈报（缺陷零容忍，待用户裁决）

- **①v2self 完整运行缺陷（新立案）**：文件路径段错误（崩点 向量$节点.追加，栈被清覆盖形态）；内置回归 IR 函数数=0（v2p 同路径=215 正常——v2 编译版容器方法/巨型函数形态残留；探针级形态宿主与 v2 对拍全一致）。三方案：A=专项 gdb 深挖（推荐）/B=先做 T3/T4 用例矩阵驱动/C=跨机对拍。
- **②【已当场根治】v2 win 发射器负数除法**：xor rdx,rdx+idiv 负被除数→128 位正数位模式→商溢出 #DE——修复=cqo+idiv（代码生成.cn 除/余两分支；win asm 形态验证两处 cqo+linux-x86_64 运行零回归；win 运行级验证待家机=诚实边界）。
- **观察项**：cn check 对 IR容器.cn 单文件「节点 不是结构体」（登记源单文件链不可达，主链绿）。

## 五、验证与文档

- 门禁：构建零警告（GCC -Wall -Wextra -Werror）+ 单测 1240/1240（+1）+ E2E 全量 156 用例 129 过/0 败/27 平台跳过（新增 154 PASS）；呈报②根治后门禁复跑同基线全绿。
- 文档：plans/017（第八节实施结果+呈报）+ plans/014 状态表第十九轮行 + plans/016 第十节 + lessons 高权重条目（权重 10）+ HANDOFF 第十九轮。
