## 功能完善总结

### 一、②c 四项一轮收官：E2E 127 全绿（v2 嵌套容器/容器字段/顶层静态/引用参数）

> 本轮在家机（Win11 AMD64）完成上轮换电脑中断的 win-x64 门禁收口，根治三项新缺陷后，
> 按已批准计划实现 ②c A/B/C/D 四项（8 个 v2 组件改造），E2E 127 四主题单用例
> v2 闭环（编译→ml64→供给链接→运行=0）首次全绿，v2 全系 119~127 回归 6/6 零回归。

**接手第一小时收口（上轮中断项）**
- win-x64 ci.ps1 全量门禁首跑 124/128：除 78/79 已知遗留（v1 旧架构内存超限，文档化不修补）
  外揪出三项新缺陷，全部根治：
  1. **v2 MASM 逻辑非 A2070**（125/126 win 全灭）：`代码生成.cn` IR_逻辑非 直接发射
     `cmp 槽, 0`——操作数为栈槽内存时 MASM 无法推断尺寸；改先 `mov rax, 槽` 再 `cmp rax, 0`
     （对齐同文件比较运算先装载惯例）。
  2. **126 期望文件平台敏感行**：主.expected 在单位机从 v2p 实际输出直抄（含
     `目标平台: linux-arm64`/`行数=1971`/`v2asm.s`），违反 119-125「win 口径 + 运行器
     Linux 分支自动归一」惯例，win 上必败；按惯例修 3 行（删平台 echo、行数置空、.asm 口径）。
  3. **B7 供给中文路径 LNK1181**：供给产物目录中文名（供给126_供给）写入 rsp 后被 link.exe
     按系统代码页（GBK）误读 UTF-8 成乱码；双平台供给产物路径一律 ASCII（supplyN_M）。
- 118/78 分诊收口：118 在 win 本就绿（上轮为 Linux 侧疑点）；78 在 win 为内存超限终止
  （=文档化已知遗留），上轮「级联析构双重释放」假设未获 win 侧证实，Linux -11 留单位机复查。

**②c 实现（CN语言编译器v2/ 8 组件：IR容器/IR签名/语法分析/语义分析/IR生成/代码生成共用/代码生成/代码生成ARM64）**
- **A 嵌套容器（向量<映射<整64,整64>>）**
  - 实参再切分按基名元数贪婪解析（`向量$映射$整64$整64` 的 T → `映射$整64$整64` 整段还原，
    替换类型参数带段们 重写）；词扫描补数字区间——「整64」曾被切成「整」致方法符号损坏。
  - **ABI 实测修正**：宿主对容器/类元素 `元素 : ptr`（E2E 118 asm 双实锤：rax 返回元素
    指针、链式 `.设置` this=rax 直传）——v2 按标量指针形态，误入 sret 即 this/实参错位段错误。
  - 方法对象类型统一递归推导 解析节点类型ID/解析方法对象类型（标识符/成员/调用 三形态：
    `表.追加`/`b.表.追加`/`元素(0).设置`/`词池.元素(0).表.大小`）；类型大小与对齐 对容器
    $ 合成名按容器对象大小（24/56）保留布局。
- **B 容器字段（指针槽模型）**：语法层登记容器字段四元组；字段构造赋值走既有成员赋值；
  `b.表.追加` 接收者=字段首 8 字节堆指针（字段地址→指针加载）；RAII 级联析构
  （发射容器字段析构：块出口/返回按字段逆序，空安全）；拥有式供给契约 ABI——含容器字段的
  结构体以类形态供给（H8-⑤ 深拷贝+Feature2 级联），宿主按类元素指针步长存储，
  v2 侧返回形态同按指针标量。
- **C 顶层静态**：四层全链——语法 节点_静态声明（裸形态）+ 语义全局符号登记 + IR 收集
  （容器=入口 主 注入 NewObject+构造+IR_静态存储；标量=折叠值直存 .data）+ 标识符
  IR_静态加载 + 双后端 `.data _cn_gstatic_N`（MASM dq / GAS .quad，ASCII 序号命名）。
- **D & 引用参数**：byRef 槽存调用方左值地址——读取分流（容器& 双重解引用/结构体& 槽内容
  即基址/标量& 解引用）、赋值与自增经指针写回、成员基址复用、prologue 跳过多槽拷贝；
  调用方 函数实参引用们 表驱动回扫取址（B8 扩展）；结构体& 解析期登记修复。
- **③ 前潜伏缺陷根治**：解析期 变量容器表/变量结构体表 全局按名键——跨函数同名异型
  （参数 t:向量& 与 局部 t:桶）串类型；新增**函数级变量类型权威表**（生成函数 填充全部
  参数/容器/结果/结构体局部，标识符类型解析先查此表）。
- **诚实边界**：拥有式结构体的赋值/按值传参/返回**拷贝点未实现深拷贝**（宿主该三点为浅
  拷贝共享，对齐宿主即违反安全目标；超宿主需先定拷贝构造调用 ABI 且 127 无载体断言——
  留待 ③ 自举实测踩到时以专用用例立项后实现）；ARM64 侧（GAS 静态槽+.data）已对称实现，
  运行时验证留单位机（本机无 as/g++）。

**测试与验证**
- 新增 E2E `127_v2_嵌套容器与容器字段与静态与引用`（主.cn 四主题断言 201 起 + 供给.cn
  类形态契约 + 主.expected v2p 自检行）并注册 v2 闭环运行器；立项期以空期望走通用路径
  兼作宿主级回归（立项即绿）。
- v2 全系 119/120/123/125/126/127 回归 6/6 绿；win-x64 全量门禁（构建零警告 + 单测 +
  E2E 全量）见提交信息；MASM 产出 asm 级核验（.data 静态槽/字段级联析构/引用写回）。
