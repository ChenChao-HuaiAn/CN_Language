## 功能完善总结

**plans/019 阶段4' A2「返回拥有类型区分」两轮落地（方案甲 用户批准「全部实施」，2026-09-11 第六十八轮）**

- **语义核心**：返回 字符串=拥有契约（调用方按被调者返回类型登记 RAII 自动释放——根治缺口②用户函数返回堆串泄漏，Rust `fn f() -> String` 同款）/返回 字符*=借用视图（零拷贝不登记，`&str` 同款）；拥有→借用自动（deref 安全方向）、借用→拥有须显式 字符串复制(...)（to_string 哲学，分配成本可见）。
- **宿主轮1（0ed437b）**：①CallExpr.retOwnedString 决议标记（普通函数/类方法/静态/接口/变参/fallback 全路径置位；泛型单态化产物签名键含 `$` 豁免=借用保守——容器元素访问 Rust Vec::get→&T 同款）；②初始化位/赋值位登记=白名单∪返回类型契约；③返回位借用拒绝（参数/成员/下标/解引用/字符*/全局静态，泛型体内豁免）；④返回位字面量拥有化（只读段 copy 落堆）；⑤字符*→字符串 初始化/赋值双向收紧；⑥赋值位借出装入拒绝（「字符串 变量=恒拥有槽」不变量——污染变量经返回位移出后调用方 free=悬垂的链式洞根治）；⑦驻留文本 返回类型改 字符*（借用显式化）。
- **三树迁移**：v2 树 96 处（65 初始化+10 赋值复制包裹+19 返回位+2 C 类签名改字符*：当前文本/推导实参类型文本）+v1 树 18 处返回位复制+tests 5 文件（06 字符*收紧显式化/52 三函数/56 读名称）。
- **根因级修复**：静态向量<字符串>追加浅共享×源变量 RAII 释放=元素悬垂（模块路径们→键基合成垃圾键→v2 编译器多文件符号解析全瘫 119/167-172 六用例）——追加处显式复制；「字符串入容器须显式复制」纪律（容器 move 语义随后续批次）。
- **v2 侧轮2（83b26ac）**：预扫登记恢复+调用初值白名单∪返回类型契约（查函数返回类型）+类型码字符*同码/驻留文本字符*+字符串复制入内置两表（自举链必需）/初始化与赋值位契约/语义检查返回位+字符*收紧/返回位字面量拥有化（IR_调用实参+IR_调用参数化 对形态）。
- **E2E**：210_返回拥有字符串（六形态：拼接建造/字面量/移出/字符*借用/调用链转发/显式复制）+210_返回借用拒绝（七形态负测：参数/成员/下标/字符*调用/静态/初始化/赋值收紧）+210_v2（六形态校验和 63）注册；全量 231 用例 226 过（唯一败=194 既有待批）；单测 1289/1289。
- **【缺陷立案】**：自举第二代（cn_self68 编译 v2 全树）IR 层 45 个「成员字段偏移未知」中止——第一代全绿+两代解析一致+无堆破坏+单变量二分不根治；固定点锚定链+对拍挂起，三方案呈报裁决（见 plans/019）。
