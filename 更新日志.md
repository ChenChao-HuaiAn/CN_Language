# 更新日志

## 功能完善总结

**plans/017 呈报①方案A 第六轮：第 17 枚收口——第 18 枚【根因级】结构体整体赋值单槽浅拷根治（2026-09-06 第二十四轮，深度系统 x86_64，Rust 准绳：性能第一/安全第二）**

1. **根因（插桩对拍四轮夹逼 + min17 最小探针）**：v2 标识符左值赋值 `甲 = 乙`（结构体
   整体赋值）只发 IR_加载/IR_存储 **单槽 8 字节浅拷**——24B 节点首槽（类型+文本）以外
   字段（附加@8）原值残留；宿主为整结构体字节拷贝，编译器分叉实锤。声明位三形态已有
   IR_结构体拷贝，唯赋值语句路径漏；全链传导：IR生成.cn 参数收集 `参数类型节点 =
   参数类型候选` 浅拷 → 引用参数（附加>=256）判定恒假 → 引用参数槽们 不登记 →
   `表.获取(k)` 漏发 op=44 双层解引用 → v2self 产物运行时 链查找 this=0（第 17 枚
   v2self 第 9 层崩点原发点）。
2. **修复（CN语言编译器v2/IR生成.cn 赋值分支一处，63 行）**：结构体类型左值赋值改发
   IR_取地址/IR_加载（引用槽→装载槽内容绑定对象地址、其余→取槽地址分流）×2 +
   **IR_结构体拷贝(类型=结构大小)** 整体字节拷贝；右值非同型变量标识符/槽缺失 →
   IR层诊断 fail fast 拒绝（绝不静默浅拷=错误产物）。Rust 对齐：POD 按值赋值=逐字节
   Copy（memmove 语义零开销）+ 类型不匹配=编译期错误。
3. **新增 E2E**：`tests/e2e/156_v2_结构体整体赋值/`——嵌套块内 24B 结构体整体赋值
   三字段断言（宿主 sanity + v2 编译运行双绿）。
4. **验证**：v2self 重编后编译 mapref2 与 v2p **逐字节一致（diff=0）**+运行 引用和=99
   与宿主一致；**九探针 v2p/v2self 对拍 9/9 PASS**（上轮 8/9）；min1 产物逐字节一致
   保持；门禁全绿：构建零警告 + 单测 1240/1240 + E2E 158 用例 131 过/0 败/27 平台跳过。
