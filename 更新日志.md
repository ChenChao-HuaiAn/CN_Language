# CN语言编译器 更新日志

## 功能完善总结

# 第四十六轮（2026-09-09 单位机 ARM64 开工 → 深度机 linux-x86_64 续作收口）：构造字面量宿主三缺陷 D1/D2/D3 根治+v2 观察项①比较层两级+③构造赋值位落地+186 双根因根治（嵌套下钻残链 AST+成员链宽度码）

接第四十六轮 WIP 提交（9a9425d，单位机麒麟 ARM64 实测）：观察项③主体完成但 E2E 186 已知红（rc=68≠84，根因 asm 级初判）。本轮在深度机（linux-x86_64）续作：深挖发现**双根因**、两处修复、全量门禁+X64L 自举锚定链全部收口。

## 一、186 双根因深挖与根治（深度机轮）

- **初判根因修复后 186 仍红**：按 WIP 交接实施「取成员宽度码 成员对象无分支」修复（见二），186 仍 rc=68——初判只是表象层根因。
- **最小探针二分**：t1 Form A 标识符目标（rc=16 ✓）/t2 手写链读（rc=1=「成员字段偏移未知」可见失败=观察项 46-b 已立案面，非本轮修复面）/t3 Form B 成员目标（全 4 字节写 ✓）→ asm 锁定：嵌套声明位下钻中 `r.左上.x=1/2` 写 4 字节 ✓ 而 `r.右下.x=3/4` 写 8 字节 ✗（同构链一过一败）。
- **真根因（解析层）**：解析构造字段赋值们 嵌套下钻递归（解析语句.cn）把本字段追加进 链名们/链偏移们 后**递归返回未弹出**——第二嵌套字段起合成**残链 AST**（`r.左上.右下.x` 形态）；地址偏移 0+8+0 恰逢前层偏移=0 **数值巧合掩盖**（写位置碰巧正确），宽度码按残链中间层查 点.右下 miss 落 T_整64 → 叶字段 8 字节越界写踩邻字段（186 s1 被踩 rc=68 实锤）。arm64 轮探针 F 单嵌套字段无后续字段故未暴露，186 双嵌套字段暴露。**修复=递归返回后弹出链两元素**（递归下降栈帧纪律，Rust 解析器同构）。
- **表根因（IR 层）**：取成员宽度码（IR生成.cn）对象解析只认 标识符/调用 两形态，合法成员链写（t3 的 `r.右下.x`）也落 T_整64 8 字节越界。**修复=新增 解析成员链结构体ID**（标识符→解析对象结构体ID/成员→父链递归+查字段类型ID+查结构体类型 键基归一/调用→解析调用返回结构体 守卫保持；0=非结构体落 T_整64 旧行为，对旧两形态逐点等价）+**取成员宽度码 主干替换**。消费点 grep 穷尽=生成语句.cn 赋值存储宽度唯一；读路径手写链走「成员字段偏移未知」可见失败（46-b，与修复面无交叠）。
- 两处修复缺一不可：残链修复治本（AST 正确），宽度码修复治表（合法成员链写）。修复后 186 **rc=84** ✓，产物错误 8 字节写清零。

## 二、本轮完整改动面

- **宿主 C++（arm64 轮，本轮随门禁 linux-x86_64 首验）**：D1 推断声明位 genVarDecl 顶部回填 typeName/D2-ident 与 D2-member visitAssignmentExpr 特判/D3 实参位 __arginit 临时物化（段错误 rc=139 内存安全违例根治）——构造字面量任意值上下文=目标 place 逐字段写，单点 emitStructInitTo 零新 IR。
- **v2 源码（arm64 轮+深度机轮）**：解析表达式.cn 比较层拆两级（6=相等/7=比较/8=移位/9=加减/10=乘除/原子=11，`a<b==c<d`=(a<b)==(c<d) 与宿主对齐，E2E 185 绿）；解析语句.cn 构造赋值单一助手（穷举/重复纪律 179/182 锚定）+Form A/B 钩子+嵌套下钻+**链状态恢复**；IR生成.cn 解析成员链结构体ID+取成员宽度码 主干替换。

## 三、验证矩阵（深度机 linux-x86_64）

- 全量门禁：清场重建 GCC **零警告** rc=0+单测 **1251/1251**+全量 E2E **189 用例 185 过/0 败/4 跳**（跳=62/69/78/79 既有平台限制；新增 183~187 五例全过，零回归）。
- v2self 锚定链：v2p 编译 v2 自身→fix_p.s（**227614 行**）→as+g++ 链接 cn_self46→**绑定自检** cn_main=0x4cf96d ∈ cn_self46.o .text [0x402586,0x4D3363)（第二代自举编译器成立）→cn_self46 编译 v2 自身→fix_s.s→**固定点 fix_p≡fix_s 裸逐字节一致**（227614 行=X64L 新锚定；上轮 224118+3496=两修复+新特性源码确定性净增量）。
- 运行级三用例：hello rc=0（你好，世界）/119 rc=14/173 rc=173 全对齐既有口径。
- 组件对拍 **29/29 全量逐字节归零**（v2p vs cn_self46，零 DIFF 零 rc 失败）。

## 四、诚实边界与新立案

- 诚实边界：win 侧门禁（ci.ps1）与 arm64 侧复验/固定点复锚=跨机轮；探针 target/probe46x、cn_self46、rt46 不入库。
- 本轮新发现 v2 既有缺陷四项已立案（plans/014 三表）：46-a 成员结构体作值读写段错误（rc=139，独立缺陷轮）/46-b 手写链式成员读可见失败/46-c `变量` 关键字解析分歧/46-d 全局位构造双侧一致拒绝（低优先）。
- plans/018 工作流1 §5.9.1/§5.9.7 两复选框核对勾选（条文已在 plans/001，仅勾选滞后）。
