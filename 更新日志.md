# 功能完善总结

> 75-a 诚实边界清理三件套（2026-09-12 第七十五轮，深度机 linux-x86_64）：用户裁决方案甲 + 相邻完善全选——v2 侧真 move、宿主污染单一入口、映射字符串元素释放。

## 一、立案：74-a 遗留诚实边界 + 同族缺口（用户选中"诚实边界"段征询优化）

| # | 项 | 性质 | 来源 |
|---|---|---|---|
| ① | v2 侧 `转移(拥有局部)` 入容器**未走真 move**（统一复制，多一次分配） | 性能面缺口 + 语义声明与实现不一致 | 74-a HANDOFF 诚实边界 |
| ② | 宿主污染名单 5 处写入点分散（漏登记→两向错误风险） | 可维护性 | 74-a 呈报的相邻完善项 |
| ③ | **映射<K,V> 字符串键/值从不释放**（~映射/清空 只释放四个内部数组） | 泄漏（矩阵 A17 同族） | 74-a 呈报的相邻完善项 |

## 二、v2 侧真 move（方案甲：污染名单镜像 + 三重判定）

**结构性约束**：`归一化入容器实参` 位于 `生成表达式求值`（深层），拿不到 `函数信息`（判定「源是否拥有」需 字符串变量们/污染们）；穿透需改表达式生成族 12 函数 + 22 调用点签名（风险大于收益）；静态结构体指针方案被阶段 2 逃逸检查拒绝（实测："不能将局部变量的地址存入静态/全局存储"）。

**解法**（v2 惯例 `已装箱名们` 同款三件套）：
- 静态 `当前串污染们` + **唯一写入入口** `登记串污染(函数信息, 名ID)`（5 处污染登记点收口）+ `是当前串污染(名ID)` 查询 + `清空函数级槽表()` 函数级重置（与权威**同生同灭**）；
- 真 move **三重判定**（全命中才真 move，任一不满足回落复制）：实参形态=`转移(标识符)` ∧ 源在**局部槽** ∧ 源**不在参数槽**（形参=借用视图，容器不得接管）∧ 源不在污染镜像；
- 源槽清零用现发常量寄存器（本函数无 `函数信息.零寄存器`）。

**实测**：预分配后 `转移(拥有局部)` 入容器分配增量 **0**（零拷贝）；`转移(借用形参)` 入容器走复制——调用方串经内存搅动后内容完好（安全方向）。

## 三、宿主污染名单收单一入口

`markStringTainted` 唯一入口（ir.hpp），5 处登记点（初始化非拥有/结构体字段借出/下标借出/转移污染传播/赋值非拥有）统一经此——纯可维护性重构，**行为零变化**（单测 1289/1289 与全量 E2E 基线不变）。污染名单是「释放侧跳过」与「入容器位拥有判定」的共同依据，分散写入的漏项会产生双向错误（误释放借用视图=悬垂 / 误判拥有=UAF）。

## 四、映射<K,V> 字符串键/值释放

- 运行时新增 `__cn_map_free_strings(obj, 键偏移, 值偏移, 元素数量偏移, freeKeys, freeValues)`——**平铺 `[0, 元素数量)`**：映射的删除用「与末元素交换」→ 有效元素恒为前缀（与链表/队列的链式模型不同，无需链游）；`freeKeys/freeValues` 由 IR 层按实例化实参 K/V 是否为字符串编译期决定；**释放后槽清零**（幂等不变量）。
- 宿主 `injectContainerElemDestroy` 增映射分支：`~映射`/`清空` **方法体前**注入（清空先释放后 `元素数量=0`）——注入在**方法体**而非调用点，因此 `向量<映射<…,字符串>>` 的嵌套场景经元素析构链（注入的 `Call ~映射`）自动覆盖。
- 实测三形态：映射析构释放 / 清空释放 / 嵌套容器元素析构链释放 → 校验 7/7（内存差值归零）。

## 五、防线（门禁口径更新）

- **E2E 218**（容器元素所有权归一化）：五形态 → **七形态**，校验和 31 → **127**（新增 ⑥映射值串随析构/清空释放、⑦嵌套元素析构链释放）；两侧供给.cn 扩 映射<整64,字符串> 与 向量<映射<…,字符串>> 符号。
- **E2E 219**（双编译对照）：六形态 → **七形态**，退出码 63 → **127**——新增 ⑦「转移真 move 零拷贝 + 借用形参走复制」两子检查合并折 64，**这是 74-a 诚实边界的正式门禁**：v2 真 move 若回退为复制，分配增量 ≠ 0 → 折位丢失 → 对照失败。

## 六、验证链全绿

清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 **1289/1289** + 全量 E2E **241 用例 236 过**（唯一败=194 既有待批）/4 跳 + 锚定链固定点 **271876 行**（新口径）逐字节一致 + 绑定自检（cn_main=0xf6b8c ∈ .text[0,0xfab4c)）+ 运行级 3/3 + 组件对拍 **29/29** + 释放路径两侧对照脚本（v2 零寄存器引用 25→27）。

## 七、诚实边界

- **映射 `删除(键)` 路径**：交换式删除把被删项的键/值槽覆盖丢弃（字符串键/值泄漏）——本轮未处理（需 stdlib 改动 + 新注入辅助方法，与 `向量.析构元素` 同模式），登记为后续靶子。
- 数组元素/装箱字段/结构体字段持串面无 RAII（既有边界维持）；194 待批维持；win 并行隔离（家机轮）。
- 探针 75 系列（真 move 验证 / 借用形参安全方向 / 映射三形态）留 /tmp/p75probe* 不入库。
