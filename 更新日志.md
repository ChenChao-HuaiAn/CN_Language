# CN语言编译器 更新日志

## 功能完善总结

**第三十四轮（2026-09-08，深度系统 x86_64）**：千行组件拆分 + 阶段目录包化（用户裁决形态A）+ 宿主目录包加载四缺陷根治——v2 编译器目录结构对齐行业规范（clang/rustc/宿主 src 顶层组织），四超线文件拆至 ≤1000 行。

### 一、阶段目录包化（CN语言编译器v2/）

- **顶层只剩 主.cn + 6 目录**：词法/、语法/、语义/、IR/、代码生成/、货舱解析/——每目录一个 `包.cn` 库包根聚合声明子模块（宿主 driver_module 候选2b 目录包同款语义，零 C++ 改动即被宿主加载）。
- **既有组件名=模块路径逐字节不变**：包.cn 挂载名保持原组件名（模块 词法分析; 等）——键基/符号 mangling/三级解析对既有文件零变化；回归面收缩到新拆文件。
- **新拆子模块**（全部 公开: 段）：语法/解析基础+解析表达式+解析语句、语义/语义检查、IR/生成表达式+生成语句+生成函数+IR布局。

### 二、千行拆分（四超线文件 → 九文件）

| 拆前 | 拆后 |
|---|---|
| 语法分析.cn 1468 | 语法分析 626 + 解析表达式 573 + 解析语句 280 + 解析基础（新，破环） |
| 语义分析.cn 1620 | 语义分析 670 + 语义检查 986（含主流程） |
| IR容器.cn 1299 | IR容器 807 + IR布局 538（布局表族+键基基础设施+常量表） |
| IR生成.cn 3182 | IR生成 861（权威表+访问函数）+ 生成表达式 1261 + 生成语句 853 + 生成函数 274 |

- **破环搬移**：键基基础设施（键基合成/拆限定名/当前模块路径）IR容器→IR布局；语义主流程 语义分析→语义检查；语法解析基础设施→新 解析基础（池追加/建节点/记号访问/三登记表+访问函数）——包.cn 声明序按依赖排列（宿主逐文件流水检查，依赖先解析），三包均无环。
- **跨文件静态触点函数化 52 处**：静态表=文件级私有，52 处直接触点改走公开访问函数（查函数签名个数/查静态序号/登记指针变量类型/查语义指针变量 等 20 个）；查询未命中统一 -1 哨兵。
- **诚实边界**：生成表达式.cn 1261 行仍超线（生成表达式 单函数 1080 行，文件级拆分无法解决，函数级拆分独立成轮挂账）。

### 三、v2 自举加载器（主.cn）对齐

- 候选1b：入口目录子目录包 `<名>/包.cn`（库包根 是包根=1，包内模块按包目录同级聚合）；候选2b：当前文件目录逐级上溯（对齐宿主归正后语义）。
- 组件间导入改目录包名（导入 词法/语法/语义/IR/代码生成/货舱解析 六包）；主.cn 集中导入按依赖序（词法→IR→语法→语义→代码生成→货舱解析）。

### 四、宿主加载器/语义四缺陷根治（目录包场景探针实证）

1. **目录包根撞名**：包.cn 的 moduleName=pathStem=「包」——全部目录包撞名，graph.findModule 去重把第二个及以后的包**整树静默跳过**（成员不加载、公开符号不合并）。修复=moduleName=包目录名。
2. **挂载去重误伤**：挂载循环前置 findModule 去重把「成员挂载名==包根名」（代码生成/包.cn 声明 模块 代码生成;）误跳过 → 成员永不加载。修复=前置去重仅限非挂载依赖。
3. **自包导入拓扑环**：目录包成员 导入 自包名（IR::IR生成 导入 IR）与包根聚合边构成环（IR→IR布局→IR）。修复=自包/包祖先导入跳过（非挂载限定，符号经合并全局可见）。
4. **imports 自引用过滤误伤**：`dep != moduleName` 把目录包根的同名成员挂载从 imports 丢弃 → 挂载循环不可见。修复=isModuleDecl（挂载声明）恒登记。
5. **语义尾段消歧扩展**：两段导入（a::b，b=已加载 unit 名，v1 再导出包形态）判③模块导入——旧只查完整路径致 74 失败；三段及以上保持原判定（47 回归实测收窄）。

### 五、验证（全绿）

- 单测 **1249/1249**；E2E linux-x86_64 **174 用例 170 过/0 败/4 跳**（62/69/78/79 为显式平台跳过清单，与基线一致零回归）；47/52/74/171/172 逐例复验。
- v2 主链：宿主 check ✓ + build 出 v2p ✓ + v2p 编译用例（130）as/链接/运行 rc=0 ✓；组件冒烟 **21/21**（abi辅助.cn=v1 遗留 ABI 探针非组件，HEAD 同红排除）。
- **挂账呈报（v2self 锚定）**：v2p 编译 v2 主链自身在默认 8MB 栈下段错误（解析二元层↔解析二元循环 深递归栈溢出，ulimit unlimited 后消除）；其后暴露 IR 层「向量$Token.追加 容器方法未收录」（跨模块泛型实例签名收录缺口）——v2self 三级锚定（组件对拍/字节级固定点）待专项治理，下轮首务。
