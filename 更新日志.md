## 功能完善总结

### 实现 --project 目录扫描功能

**修改文件**: [`src/cli/cnc/main.c`](src/cli/cnc/main.c)

**新增功能**:

1. **目录扫描函数**
   - 添加 `CnFileList` 结构体用于动态管理文件列表
   - 实现 `scan_cn_files_recursive()` 递归扫描目录
   - 支持 Windows (FindFirstFileA/FindNextFileA) 和 Linux (dirent.h) 平台
   - 自动识别 `.cn` 扩展名的源文件

2. **主入口文件识别**
   - 优先查找 `main.cn` 作为入口文件
   - 其次查找 `主程序.cn`
   - 如果都没有，使用第一个找到的 `.cn` 文件

3. **项目模式处理**
   - 当使用 `--project <目录>` 参数时，自动扫描目录下所有 `.cn` 文件
   - 将扫描到的文件添加到源文件列表
   - 正确设置入口文件并开始编译

**测试结果**:
- ✅ 编译成功
- ✅ 项目模式扫描功能正常工作
- ✅ 成功扫描 `CN语言验收/01_基础语法` 目录（7个源文件）
- ✅ 正确识别入口文件并编译生成可执行文件

**已知问题**:
- src_cn 项目编译时崩溃，这是由于模块导入时类型信息丢失的已知问题，与本次 `--project` 功能实现无关
