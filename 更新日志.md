# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段3「常量 只读引用参数 + 借用互斥第一版」双侧落地 + v2self 固定点复锚 244605 行（2026-09-10 第五十九轮 深度机 linux-x86_64）

编译期安全专项阶段 3 第一版全链闭环。

### 一、语言语义（经用户批准写入规范）

`常量 类型& 名` = **只读借用**（Rust `&T` 对标 / C++ `const T&` 同构）：
- 体内赋值/复合赋值/经成员链赋值 → 编译错误；
- 传给可变引用形参（`T&`）→ 编译错误（只读借出可变拒）；
- 同一调用中某变量的可变引用实参与常量引用实参同基础变量 → 编译错误（借用互斥第一版：语句级）；
- 常量引用参数作 `T&` 返回（借出升级）→ 编译错误。

### 二、实施

- 宿主六文件：ast.hpp / parser.cpp（常量 前缀）/ semantic.hpp（FunctionInfo.constParams + currentConstRefParams_）/ semantic.cpp（checkConstRefBorrowDiscipline）/ semantic_decl.cpp / semantic_call.cpp（普通函数调用面接入）/ semantic_expr.cpp（赋值面）/ semantic_stmt.cpp（借出升级拒）。
- v2 三处：解析参数 常量 前缀（类型节点附加 256→257，IR 层 >=256 天然按引用 ABI）+ 检查函数 收集 + 赋值面只读拒。
- 规范：plans/001 引用参数章节 + specs/04 同步。

### 三、测试与验证

- E2E 200/201/200_v2/201_v2 四用例双通道 + 单测 test_const_ref_check.cpp 7/7。
- 清场重建零警告 + 单测 1281/1281 + E2E 217 用例 212 过（唯一败=194 既有待批）/4 跳。
- v2self 锚定链：fix_p 244605 行 → 固定点逐字节一致 + 绑定自检 cn_main=0x4dee8f∈.text + 对拍 29/29 + 运行级 3/3。

### 四、诚实边界（阶段 3b 余项另轮）

构造/方法/函数指针调用面接入借用纪律；可变×可变双别名拒；跨语句 NLL 活跃区间；v2 传可变/互斥面（随实参边界记录）；转移浅拷贝优化（IR 槽位交接）。
