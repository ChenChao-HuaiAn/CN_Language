# CN语言编译器 更新日志

## 功能完善总结

**第三十五轮（2026-09-08，单位机 麒麟 ARM64）**：R0→R4 源码优化全链实施（用户批准「全部实施，不允许拖延」）——R0 测试面解耦 + R1 速净 + R2 诊断分级 + R3 函数拆分（生成表达式 1084→76 行+新文件拆分）完成，R3 剩余 13 函数+R4 三后端挂账回家继续（见 HANDOFF.md）。

### 一、R0 测试面解耦（E2E 与编译器解耦）

- **根因**：v2 闭环 28 用例（119~172）把编译器 stderr 诊断流固化进 expected（`[ir1]`/`[p6h]`/`[sem5]` 内部进度行）——临时锚定滚成永久契约，反向绑架编译器（60 处打印不能清、`主()` 13 个内置负测自检每次编译全跑，顶撞「性能超 C++」目标）。
- **实施**：`主.cn` 删 13 个内置负测自检+映射自检+复位死代码（~190 行）；新建 `tests/e2e/173_v2_语义拒绝合集`（负路径合集用例，12 拒绝形态一个源码全量诊断，承接自检删除后的拒绝能力锚定——不丢覆盖）；28 个 v2 expected 收敛为契约行/空文件（负路径 7 用例保留 `[错误]` 诊断行，正路径 21 用例保 `[p6h] 已解析` 加载器行为锚定+退出码断言）；`run_e2e.py` 注册表加 173（机制零改动，只收敛内容）。

### 二、R1 速净（同族穷尽确认）

- `主.cn` 删 10-14 重复导入（千行拆分轮插注释漏删原块）。
- `IR/IR常量.cn` 删冗余 `导入 容器::向量`（纯常量文件未使用）。
- `IR/IR签名.cn 签名读文件` 换行逻辑对齐缺陷③（读取文件行 含换行契约——2026-09-04 只修 主.cn 侧，IR签名 漏网；同族旁路穷尽 grep 全源码仅两处读行拼接，均已对齐）。

### 三、R2 诊断分级（--verbose 开关）

- `IR/IR常量.cn` 新增跨组件协议：`详细诊断开关`+`设详细诊断`+`详细诊断开启`（协议单一归属惯例，同 当前函数变量类型）。
- `主.cn` 第 3 参数 `--verbose` 开启；6 处内部进度打印开关化（入口/目标平台/asm 行数/IR dump 每函数每指令/正确程序错误数）；错误契约与加载器行为行（`[p6h] 已解析`/`[p6b] 货舱`/`[parse]`/`[sem5] 正确诊断 [错误]`/`[ir2]` 中止行）无条件保留。
- **实效**：v2p 编译 172 用例 stderr 从数千行降至 2 行——自举输出税清零，双目标准绳解绑。

### 四、R3 函数拆分（生成表达式.cn 最大件）

- `生成表达式` 单函数 1084 行 → 76 行；文件 1263 → 933 行 + 新文件 `IR/生成表达式求值.cn` 440 行（`IR/包.cn` 加 `模块 生成表达式求值;` 声明+导入——模块路径=原组件名，键基零变化）。
- 拆出 22 个 ≤100 行函数（列表见 HANDOFF）；接口约定：分支函数返回消费 token 数（恒 1，0=整体返回 0）、容器出参一律 `向量&`（按值返回禁用）、`生成下标左值地址` 调 `生成表达式` 递归故留原文件破循环依赖、新文件六函数均不调 `生成表达式` 单向依赖。
- 顺带消重：左/右末端类型推导两段同构合并 `推导末端类型码`。
- 验证：v2 闭环 23 用例快测零回归（119/123/125/126/127/129/130/132/133/137/143/144/147/148/150/156/158/164/165/167/171/172/173）。

### 五、门禁（提交前全绿）

- 构建：`cmake --build target/build` 零警告；`target/cn build CN语言编译器v2/主.cn --target linux-arm64` v2p 构建零错误。
- 单测 **1249/1249**；E2E linux-arm64 全量 **175 用例 171 过/1 败/3 跳**（78 为本机内存环境性 OOM：4112MB>4096MB 保护性终止，非代码回归——深度机同代码 0 败）。

### 六、挂账（回家继续）

R3 剩余 13 个超 100 行函数（生成语句 708/检查表达式 334/检查语句 265/解析语句 247/扫描签名文件 232/分词 193/编译文件 185/生成函数 185/主 ~190/解析后缀链 147/回扫实参取地址 114/决议函数符号 102/声明收集 105）；R4 三后端 `生成指令`（314/321/292）同构拆编码函数组；x86_64 复验留深度机轮；plans/014+018 文档同步下轮补；lessons 踩坑三条已入库。
