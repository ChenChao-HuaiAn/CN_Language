## 功能完善总结

### 阶段6「标准库」第5步：字符串扩展 + 时间 + 系统库（Task 6.5 字符串处理补充 + Task 6.9 时间库 + Task 6.10 系统库，2026-08-15）

**新增标准库模块：**

1. **`stdlib/字符串扩展.cn`**（Task 6.5 字符串处理库补充，对标 C++ string 解析能力）：
   - `字符串转整数(字符串)`/`字符串转浮点(字符串)`/`字符串转布尔(字符串)`：数值解析，返回 `结果<整64/浮64/布尔,整32>`；非法输入/空串/范围错误返回 `错误(6)`（无效参数）
   - `替换(输入, 目标, 替换为)`：循环 字符串查找+子串+连接 拼接全部替换；目标不存在返回原样副本；安全计数器防"替换为包含目标"无限循环
   - `填充左(输入, 目标长度, 填充字符)`/`填充右(...)`：不足长度补 填充 字符；已超长返回原样
   - `分割(输入, 分隔符, 字符串* 结果数组, 整64 数组长度)`：按分隔符切分写入调用方数组（CN 数组长度须编译期常量故用 C 风格数组），返回实际数量 `结果<整64,整32>`；数组不足 错误(6)

2. **`stdlib/时间.cn`**（Task 6.9，对标 C++ chrono/ctime）：
   - `当前时间戳()`：Unix 纪元秒级（time(nullptr)）
   - `单调时钟毫秒()`：单调时钟毫秒（计时差值安全，不受系统时间调整影响）
   - `格式化时间(时间戳, 格式)`：strftime 格式串本地时区格式化，返回 `结果<字符串,整32>`；非法时间戳返回 `错误(6)`；返回字符串调用方 `字符串释放()`

3. **`stdlib/系统.cn`**（Task 6.10，规格书10.4 命令行参数，对标 C++ argv）：
   - `参数个数()`：含可执行文件名本身（至少 1）
   - `参数(索引)`：按索引取 argv，返回 `结果<字符串,整32>`；越界返回 `错误(6)`；字符串由运行时持有不得释放

**新增运行时 API（英文命名，extern "C"）：**

1. **`src/runtime/string_api.cpp` 扩展**：`__cn_str_to_int`（strtoll 整串解析 + 整32* 成功标志，拒绝 "123abc" 部分解析）/`__cn_str_to_double`（strtod 整串解析）/`__cn_str_to_bool`（"真"/"假"/"true"/"false" 精确匹配，英文不区分大小写）
2. **`src/runtime/time_api.cpp`**（新建）：`__cn_time`（time(nullptr) 秒级）/`__cn_clock_ms`（Windows QueryPerformanceCounter / POSIX clock_gettime(CLOCK_MONOTONIC)，#ifdef _WIN32 分支）/`__cn_time_format`（localtime_s/localtime_r + strftime，失败返回 nullptr，动态分配调用方释放）
3. **`src/runtime/system_api.cpp`**（新建）：`__cn_argc`/`__cn_argv`（全局缓存命令行参数；Windows 用 `GetCommandLineW` + `CommandLineToArgvW` 从进程原始命令行解析 + `WideCharToMultiByte(CP_UTF8)` 转 UTF-8 缓存，中文参数无乱码；Linux 直接缓存 entry 的 argv）

**五处同步（新增内置函数全链路）：**

| 同步点 | 内容 |
|--------|------|
| ① 语义注册 | `semantic.cpp` registerBuiltins：`解析.转整数/转浮点/转布尔` + `时间.当前时间戳/单调时钟毫秒/格式化时间` + `系统.参数个数/参数`（前缀限定名，与 stdlib 模块公开纯名不冲突；**`字符串` 是类型关键字不能作前缀故用 `解析.`**） |
| ② IR 名称映射 | `ir.cpp` visitCallExpr：`解析.xxx`→`__cn_str_to_*`、`时间.xxx`→`__cn_time/__cn_clock_ms/__cn_time_format`、`系统.xxx`→`__cn_argc/__cn_argv` |
| ③ IR 结果类型 | 转整数/时间戳/时钟毫秒/参数个数→i64、转浮点→f64、转布尔→i64（CN 层 Cast 布尔）、格式化时间/参数→ptr |
| ④ 运行时声明与构建 | `runtime.hpp` 声明 + `CMakeLists.txt` cn_runtime 源文件 + `cn_main.cpp` compileRuntime 源文件数组 + linkExe .obj 列表（x64/arm64）+ shell32.lib（Windows CommandLineToArgvW） |
| ⑤ 测试/文档 | E2E 35_string_ext/36_time/37_system + 单测 test_string_parse_api/test_time_api/test_system_api + plans/更新日志/HANDOFF |

**E2E 运行器扩展：** `run_e2e.py` 支持同名 `.args` 文件注入命令行参数（每行一个参数，首行=argv[1]，argv[0]=exe 名；空白行与 # 注释跳过），与 `.input` stdin 注入同模式——37_system 完整验证 参数个数=3 + 中文参数 "CN语言" UTF-8 无乱码。

**验证结果：** 编译 0 警告 0 错误（MSVC /W4 /WX）；单元测试 **1048/1048**（1005 前序 + 43 新增：字符串解析 24 + 时间 9 + 系统 10）；E2E **39/39**（36 前序 + 35_string_ext + 36_time + 37_system）。
