# CN语言编译器 更新日志

## 功能完善总结

# 第四十九轮（2026-09-10 深度系统 x86_64）：类型系统轮——46-e 联合体类型面 v2 全链根治 + 46-c 变量推断声明双侧根治（宿主连带揪出推断回填缺口）+ 46-f 无符号语义面双侧定性呈报三方案 + v2self 固定点复锚（232743 行）+ 组件对拍 29/29 + 运行级 3/3

按 HANDOFF 挂账二.2 执行类型系统/语法覆盖轮（深度机本机开发 + X64L 侧验证；arm64 复锚仍留单位机跨机轮）。三项观察项中 46-e/46-c 当轮双侧根治，46-f 实证定性升级为双侧静默语义缺陷并呈报三方案待用户裁决。

## 一、46-e 联合体类型面（v2 侧根治，规范 02 既有语法实现补齐）

- 解析结构体声明 扩联合体关键字分支：字段偏移全 0（不推进不对齐）+ 大小 = 最大字段大小向上对齐（C 布局语义，宿主 computeLayout isUnion 同构）。
- 构造字段表不登记 = 构造字面量穷举纪律豁免（C 单成员激活惯例，规范 05；字段数 miss=0 构造路径自然不进入，宿主 visitStructInitExpr 同语义）。
- 解析程序 顶层加「联合体」声明分派（与结构体同入口）；仍产 节点_结构体——语义登记/类型引用/成员访问/整体赋值/实参传地址全链复用，零新节点零新 IR。
- 修复前 `联合体 数值 {...}` 报「未声明的类型」可见失败。

## 二、46-c 变量推断声明（双侧根治，规范 03 既有语法实现补齐）

- **v2 四层联动**：解析层「变量」分支（类型节点文本=空串哨兵，宿主 varType.empty 同构；无 `=` 语法错误 fail fast 对齐规范 EBNF）→ 语义层推导族（表达式类型ID：字面量/标识符/调用/下标/三元经 检查表达式 既有精确推导；成员/二元新推导函数——检查表达式 对二者恒 整64/-1 半实现，推断声明不可静默降级否则结构体值截断=46-a 家族静默错误；成员推导同构 IR 解析成员链结构体ID 范本）→ 推断声明类型表（IR布局 跨层静态表——主.cn 层间池按值传递语义回填对 IR 不可见，表是唯一通道）→ IR 生成变量声明 查表回填布局分派（miss=IR 层可见诊断 fail fast）。
- **宿主连带揪出（双实现一侧修复必须审计另一侧纪律兑现）**：语义层推断回填仅更新符号表、未写回 AST 节点 typeName——语义与 IR 共享同一 AST，结构体值源（成员读/标识符拷贝/调用返回）在 IR 层落空 typeName → 单 i32 槽 + 结构体拷贝截断 = 静默垃圾（t_min 探针 `变量 t = r.左上` 后 t.x 垃圾值 rc=16 实锤；显式 `点 t = r.左上` 同源码 rc=0 对照）。修复 = 结构体类型回填写回 node->typeName（限定仅结构体：标量回填会改既有打印分派——E2E 184 expected 固化 0/1 为既有形态，测试文件纪律不擅改，标量推断 IR 类型面另案呈报）。

## 三、46-f 无符号类型语义面：定性升级 + 三方案呈报（待用户裁决）

矩阵探针双侧对照实证：v2 无符号除法静默错误（2^63/2 有符号出负 rc=12）、宿主无符号比较静默错误（2^63>100 有符号读判假 rc=11）、v2 32 位截断读回缺零扩展——双侧行为互有分叉 = 同一源码两侧不同结果，从「类型面缺失可见失败」升级为**双侧静默语义缺陷**。按缺陷零容忍纪律呈报：方案A（推荐）双侧全链对齐 Rust u32/u64 硬件无符号语义（性能第一/安全第二双达标，工作量≈位运算轮）；方案B 规范文档化现状（不推荐，与移位语义定义化裁决方向倒退）；方案C v2 先行分摊工作量。详见 plans/014 观察项表 46-f 行。

## 四、新增 E2E 四用例（双通道）

- **189_联合体值语义**（宿主 stdout 五锚 42/42/4408/4408/99）/ **189_v2_联合体值语义**（v2 闭环 rc=31 五形态校验和：成员写读 +1/内存共享写整64读整32低4字节 +2/声明位整体赋值 +4/赋值位整体拷贝 +8/值传参 +16）。
- **190_变量推断声明**（宿主 stdout 九锚）/ **190_v2_变量推断声明**（v2 闭环 rc=255 八形态校验和：整数字面量 +1/字符串字面量 +2/标识符拷贝 +4/L 后缀 +8/调用返回 +16/成员读→结构体 +32/嵌套成员读 +64/二元 +128）。

## 五、全量门禁与自举锚定链

- 清场重建 GCC 零警告 rc=0 + 全量单测 **1251/1251 PASS** + 全量 E2E linux-x86_64 **195 用例 191 过 / 0 败 / 4 跳**（跳=62/69/78/79 既有平台限制；195 = 上轮 191 + 本轮新增四用例；首跑 184 红系标量回填行为变化 → 限定结构体回填后转绿，三轮 E2E 全程诚实记录）。
- v2self 锚定链（X64L 口径复锚）：v2p 编译 v2 自身 → fix_p.s **232743 行**（上轮 229236 + 第四十九轮净增 3507 = 联合体+推断声明特性源码确定性体现）→ as + g++ 链接 cn_self49（cn_self49.o 在前 + v2p_linuxx64.o 借链 + rt48 objs 现编 + muldefs）→ 绑定自检（决定性）：cn_main=0x4d45f5 ∈ cn_self49.o .text [0x402586, 0x4d8027) → cn_self49 编译 v2 自身 → fix_s.s（232743 行）→ **固定点 fix_p ≡ fix_s 裸逐字节一致**。
- 组件对拍 **29/29 全量逐字节归零**（6 包根 + 23 成员，find 实数核对；strData 平移规范化未触发）。
- 运行级三用例 3/3：hello rc=0（你好，世界）/ 119 多文件 rc=14 / 173 目录包根撞名 rc=173，全对齐既有口径。

## 六、踩坑入库（lessons 权重 6）

v2 新源码 `类型节点 = 建节点(...)` 赋值位调用形态触发第四十八轮结构体整体拷贝拦截（右值白名单无调用）——单文件探针全绿、v2self 锚定链 fix_p 才爆：**自举链是自写源码最全语法面门禁，v2 源码改动必须当轮跑「v2p 编译 v2 全树 rc=0」冒烟再进全量 E2E**。

## 诚实边界

- arm64/win 侧复验与固定点复锚（win=235968 / arm64=302433 行均因本轮 v2 源码变更待重锚）= 下一跨机轮；arm64 复锚（302433 行口径）仍为当前唯一跨机挂账主线。
- 46-f 无符号语义面三方案待用户裁决后实施（静默语义缺陷不擅自动工）。
- 联合体构造字面量双侧均未锚定（随构造字面量覆盖轮）；标量推断 IR 类型面（布尔打印分叉/调用返回大值截断）另案呈报随标量推断轮。
- 探针与验证脚本留 target/p49x64l/ 不入库（run_chain49.sh / map_check49.py / runlevel49.sh / cmp29.sh）。
