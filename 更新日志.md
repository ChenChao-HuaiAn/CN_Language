## 功能完善总结

### 一、观察项裁决落地：非左值赋值立案根治（宿主侧完成，v2 侧接手续推）

> 本轮在单位机（麒麟 ARM64）执行用户裁决「②立案+③顺带+①不动」：`a * a = 7` 歧义家族中，
> **非左值赋值静默接受**（a + 1 = 7 / a + 1 += 2 / 5 = a / 常量 c = 2 / c++ / (a+1)++ /
> 换行粘连 b = ((a*a) = 7) 全部检查通过退出码 0）按 Rust E0070 同款硬错误根治（宿主完成）；
> **报错误导**以 rustc「you might have meant to write a semicolon here」同款提示顺带收口；
> **歧义本身**（标识符*标识符 声明优先 + 换行≡空格）维持规范行为不动。

- **E2E 用例先行**：131_非左值赋值拒绝（负 e2e，八负形态，已验证通过）+ 132_v2_可写左值
  全形态闭环（v2 闭环正例+自检行固化，待 v2 侧实现后转绿）+ run_e2e.py 注册。
- **宿主语义层**（semantic.hpp/cpp/semantic_decl.cpp/semantic_expr.cpp）：
  - visitAssignmentExpr 左值白名单重构：标识符（常量拒绝）/下标/成员/解引用（Operator::Deref）
    /引用返回调用之外一律硬错误「赋值目标必须是可赋值的左值（…）」；
  - 常量跟踪：scopeConsts_ 与 scopes_ 平行维护（push/popScope 镜像+visitVarDecl 登记+
    isConstVarName 内→外同序解析）——常量赋值/自增拒绝「常量初始化后不可修改」；
  - visitUnaryExpr ++/-- 左值检查（同白名单）；
  - 诊断③：赋值号行号>目标起始行号 → 附「提示：赋值语句跨行——CN 换行等同空格，行首
    运算符会并入上一行；若本意为两条语句，请在上一行末尾加分号」。
- **验证**：构建零错误+单测 1196/1196+E2E 冒烟子集 05/30/113/130/131 全绿（130=宿主编译
  v2 全部源码=误伤检测最强用例）；负探针八形态全部正确拒绝（消息+提示实测）。
- **新发现两枚 ARM64 既有缺陷（非本轮回归，git stash 对照实证）**：A. `p.y--` 成员后缀自减
  存储无效；B. `数组[2] += 数组[1]` 局部数组下标复合赋值垃圾值（疑似上轮内存模型统一 ARM64
  未复验面）——已记 HANDOFF 待分诊（缺陷零容忍纪律）。
- **未完成（交接）**：v2 语义分析.cn 四件（表查找种类/左值白名单/常量拒/自增常量拒）+
  v2 主.cn 两负测自检（132 转绿前提）+ 宿主单测文件 + 全量门禁（家机 ci.ps1）+ plans 打勾。
  详细接手指引见 HANDOFF.md「下一步」。
