# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段4 第一层「不安全区边界·观察期」落地 + E2E 221 用例零误伤（2026-09-10 第六十二轮 深度机 linux-x86_64 · 宿主侧 v2 零改动）

### 一、语法（经用户批准的三语法点之三）

`不安全 函数 名(...) { ... }`——`不安全` 真关键字（specs/01a 关键字 59→60）。安全区边界五类操作仅限不安全函数体内：指针算术、指针下标写、联合体字段访问、外部函数调用、裸释放。

### 二、观察期语义（方案 B）

- 安全函数内五类操作 → 编译**警告**不报错（分批收口后变错误）；
- 不安全函数体内 → 零警告；
- 安全函数调不安全函数 → 允许（责任上移，审计面=不安全函数清单）。

### 三、实施（宿主）

lexer/parser（顶层前缀）/semantic（isUnsafe 链路+warnUnsafeBoundary 五警告点）/driver（警告可见性：仅警告无错误也输出——runModulePipeline 为 check/build 实际路径）。类方法修饰位随收口轮；v2 对齐随收口轮。

### 四、验证

E2E 204_安全区边界观察期（stdout 7/3）+单测 test_unsafe_boundary.cpp 6/6；清场重建零警告+单测 1289/1283+6+全量 E2E 221 用例 216 过（唯一败=194 既有待批）——**观察期零误伤**。v2 零改动=固定点维持 245230。
