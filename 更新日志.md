## 功能完善总结

### 方案C全链路审查修复（2026-08-14）

**背景**：对"删除遗留 `打印行整数`/`打印行浮点` 内置函数"方案C实施结果进行独立全链路审查（不轻信子任务自报），发现并修复 2 处遗留缺陷，其余全部核实通过。

#### 一、审查发现的缺陷与修复

| # | 缺陷 | 根因 | 修复 |
|---|------|------|------|
| 1 | `打印('A')` / `打印(字符变量)` 报"打印行 参数类型不支持：'字符'"——旧写法 `打印行整数('A')`（字符→整64隐式转换，输出65）替换为 `打印('A')` 后行为回归被误拒 | 语义层变参检查 `okNum` 缺 `字符`（`isNumeric("字符")`=false 且 `okStr` 只收字符串系）；IR 层本已支持（字符=i32 Cast i64 走 `__cn_print_int` 输出ASCII码） | [`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp:2004) `okNum` 补 `argType == "字符"`；`格式化` 共用此分支同时修复 |
| 2 | 常量折叠 f32→f64 Cast 精度丢失：`格式化("%.10f", 3.14f)` 输出 `3.1400000000`（正确 `3.1400001049`）——`(double)(3.14f)` 被折叠成 double 3.14 | `foldCast` 浮→浮分支直接 `parseDouble("3.14")` 按 double 解析，未先按 f32 精度截断再提升；%f 6位小数恰好掩盖差异 | [`const_fold.cpp`](src/cn_compiler/opt/const_fold.cpp:379) f32→f64 先 `static_cast<double>(static_cast<float>(d))` 再 `formatDouble` |

#### 二、核实通过项（无缺陷）

- **源码层**：`registerBuiltins()` 仅注册 `打印`/`打印行`/`格式化` 变参函数，`打印行整数`/`打印行浮点` 已彻底删除；x64/arm64 `symbolName()` 均无 `printLineInt`/`printLineFloat` 映射；`src/`、`tests/` 下无实际代码引用（仅注释历史标注"✅ 已修复"）
- **运行时层**：`printLineInt`/`printLineFloat` 保留合理——[`test_runtime.cpp`](tests/unit/runtime/test_runtime.cpp:44) 直接引用，防御 ABI 稳定；`runtime.hpp` 注释已同步
- **IR 层**：`打印`/`打印行` 变参展开 f32→f64 Cast 正确（[`ir.cpp`](src/cn_compiler/ir/ir.cpp:2756)）；整型（整8/16/32/64/128）Cast i64/i128 正确；无符号（正8/16/32/64）Cast u64 走 `__cn_print_uint`（%llu，正64 超 2^63 不打印成负数）；布尔（i1）、字符串、浮64 正确；枚举（i32）走整型分支正确；指针/结构体被语义层拒绝（合理，`打印(任意值...)` 仅限标量+字符串）
- **测试层**：`打印行整数(X)`→`打印(X)` 后 `.expected` 无需修改结论成立（两者均输出 `X\n`，13_opt2/15_expressiveness 实际输出与 .expected 逐行一致）；E2E 28/28、单元测试 965/965 全部通过（重新编译 + 全新输出目录验证）
- **行为一致性**：`打印`=换行、`打印行`=不换行语义在 15_expressiveness（"打印自动换行/第二行/打印行不换行继续同一行/分隔完成"）正确体现；`打印(3.14f)`/`打印(浮32变量)` 输出 `3.140000`（%f）与 `格式化("%.10f")` 输出 `3.1400001049`（float 精确值）均正确

#### 三、文档同步

| 文档 | 修改内容 |
|------|---------|
| [`lessons.md`](lessons.md:22) | 新增全链路审查发现条目（✅ 已修复，权重9.1：语义层字符误拒 + 常量折叠 f32 精度） |
| [`001 CN语言编译器设计规格书.md`](plans/001 CN语言编译器设计规格书.md:990) | 10.6 节已标注方案C已修复（上轮完成），本次核实通过 |
| [`002-阶段1D-后端与集成.md`](plans/002-阶段1D-后端与集成.md:270) | 已标注遗留函数已删除/已修复（上轮完成），本次核实通过 |

#### 四、验证结果

- **构建**：0 错误 0 警告（MSVC /W4 /WX，VS2022）
- **单元测试**：965/965 通过（81 个测试套件）
- **E2E**：28/28 通过（全新输出目录 `target/e2e_review`，排除旧产物干扰）
- **回归用例**：`打印(字符)`/`打印(3.14f)`/`打印(浮32变量)`/`格式化("%.10f", 3.14f)` 输出正确
- **结论**：方案C无遗留缺陷，全链路集成正确
