# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百三十六轮 136-a：波 3 开工——B3 资源字段统一化（五形态探针 + 设施盘点 + 三件套实施设计）**（2026-09-14 上午，家机 win-x64；基线 0ddcfae）：
> ①**五形态探针复现**（plans/022 §三 基线；探针 `target/p136win/b3_字段容器.cn` 不入库）：形一成员位赋值＋块出口
> 增量 **2**；形二整体拷贝增量 **2**（乙读到甲元素=浅拷共享）；形三成员覆盖旧值增量 **2**；形四字段位赋值增量 **2**；
> **形五（对照）类字段容器增量 0**（类声明析构口径——缺口**只限结构体侧**）；
> ②**设施盘点**（宿主）：字符串字段三件套 = `ir/ir_fields.cpp`（451 行）——①判定收集（`OwnedStrField`：
> offset/condOffset/arrayLen/arrayStride/elemCanon）+ ②释放发射（`emitFieldStringFree*`/`emitOwnedStrFieldFreesAt`
> 等，单点事实源）+ ③深拷（`emitOwnedStrFieldPreFree` → `CopyStruct` → `emitOwnedStrFieldPostCopy`）；
> 入口 `emitStructCopyWithFields`；**类侧级联** `genClassDestructorCalls`（ir_oop_call_release.cpp:164）闭环
> （容器=类，`~向量()` 析构）；**缺口**：`ownedStrFieldsOf` 仅收集字符串字段 → 容器字段裸 memcpy 浅拷 + 无释放；
> ③**三件套实施设计**（`plans/020` 第四十八节）：载体扩展（`kind` 枚举 Str/ClassObj/NestedAgg/Arr + canon）→
> **拷贝位先行**（消除共享，中途态安全）→ 消亡位（复用类侧析构调用协议）→ 写入位归一化 → H7/H11 收口 →
> v2 同构（后续轮）；**容器深拷通道三选一**（甲=容器拷贝构造〔建议〕/乙=编译器生成〔否决〕/丙=内置 复制(x)
> 〔波 4 统一〕）；验收面=全形态残留归零 + 双编译对照 + 反证 + 搅动内容校验。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
