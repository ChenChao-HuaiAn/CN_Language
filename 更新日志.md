# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结
> **242-a A10 第三波（2026-09-16，家机 win-x64；基线 037297f·认领 fef52bc）**：
> ① **D13 根治**：driver_module 合并后遍历 program->declarations 查 主（入口 unit isEntryUnit）——入口缺失编译期诊断（304 期望迁移·原链接期 LNK2019）；关键教训：mergeModules release 摘除后 unit->ast 空壳不可回看。
> ② **新用例 341~343/345~347**：浮点作条件拒绝/重载参数个数边界/结构体转整数拒绝/可选无值判断（.有值 形态）/遮蔽恢复/未授权访问私有——覆盖率 **79.2%→82.5%**（151/183·三态全 34 单元）。
> ③ **新立 D17**（主 返回 结果 类型 ? 传播退出段错误·单层即崩）/ **D18**（常量成员经 自身 调非常量成员绕过拦截）。
> ④ **B3 呈报**（待用户裁决）：低语义密度格口径——A 类建议排除 7 格/B 类需设施 4 格/C 类可下波补 4 格。
## 深度机 linux-x86_64 节

## 功能完善总结

> **234-a A7 本体根治收口（2026-09-16，深度机 linux-x86_64；基线 476b7d2，认领 8bb5511 前 ecae5ed rebase 后）**：
> ①**第一层根治：共享 AST 写回注记跨实例污染**——实例化类方法体 AST 全实例共享（mi.ast=模板成员），
> 语义检查写回型注记（resolvedType/resolvedSignature/retOwnedString/node->size）在共享节点上被
> 「最后检查实例」残留，IR 层逐实例生成全读残留值（实证：v2 树 9 处 `复制(下标链)` resolvedType
> 全=IR指令 56B → 误走分派④ CopyStruct 56B+写回槽地址；单实例程序巧合正确=225-a「同模块用户
> 泛型类正确」对照真因）。根治=emitClassMethod 生成前按本实例 typeArgs 重放方法体检查
> （checkSingleMethodBody 原样提取复用）+诊断快照回滚；方案裁决=重放（方案U）胜深拷 AST（方案A），
> 详见 plans/025 §二。
> ②**第二层根治（连带发现）：整体赋值源分派漏复制调用形态**——`数据[索引]=复制(...)` 元素槽=
> 栈地址 8 字节（乙0号=-1347670232 实锤）；emitStructWholeAssign 补分派（**收窄纪律**：仅复制
> 调用进新分支——宽版补丁致 v2p 产物坏 285 失败，单变量二分定位后收窄；一般调用返回形态=D11 待查）。
> ③**stdlib 六处深拷切换重新应用**（206-c 立案收口）：向量/栈/链表/队列/映射键/映射值（映射范围
> 收为 [0,元素数量)）；E2E 285 切换态双侧全绿（宿主 15=v2 同值）。
> ④验证链：零警告全量重建 + 单测 1327/1327 + **全量 E2E 313 用例 311 过/0 失败/2 跳** +
> 锚定链 **410460 行 fix_p≡fix_s 逐字节不变**（v2 树不含拷贝初始化形态=自举链零扰动） +
> valgrind 切换态 6 errors（=225-a 回退态基线，**A7 差值 7 归零**）+ 反证双件（回退补丁 rc=136
> 复现 / 插桩双态 IR 实证）。
> ⑤新立案：**D10 泛型函数体零检查**（泛型自由函数体从未语义检查，`复制(源)` 落普通调用链接期爆）、
> **D11 一般调用返回整体赋值待查**——均登记 plans/021 §三-D；源码改动=宿主 5 文件+stdlib 2 文件，
> 详情=plans/020 第七十五节 + plans/025 §二。

## 单位机 ARM64 节

## 功能完善总结

### 226-a D8 寄存器感知发射尝试（→ 按纪律回退·源码零净变更）+ F1-26 调研（2026-09-15，单位机麒麟 ARM64，基线 8bfcc67）

1. **改造面**：6 处发射路径（常量装载 4 类 / 整数二元运算 / Load / LoadPtr / AddrOf / FieldAddr）改为
   「结果与操作数直接以分配寄存器为目标」，消除「算到固定临时寄存器（x9/x10）再搬运」的冗余。
2. **验证过程**：分 5 组改造，**每组改完单独跑 94/97/99/89 → 全部 rc=0**。
3. **收益实测（改造生效时，245 用例口径）**：**行数 21204→19996（-5.7%）**、**冗余 `mov xN,临时` 1121→699（-37.6%）**。
4. **全量 E2E → 7 例失败**：`78_v2`/`79_v2`（v2p 二进制 `malloc` 堆断言＝堆损坏）+ `88_错误码契约`/`94_引用变量`/
   `97_遍历表达式`/`99_泛型函数指针多态`/`89_关键字穷举`（段错误 -11）——**同样用例单跑通过、全量失败＝UB 非确定性**。
5. **处置**：按纪律 `git checkout` 回退（**源码零净变更**）→ 重建（零警告）→ 单测 **1327/1327** + 全部失败用例 rc=0 复核。
   收益基线与失败现象已留档（下一轮可直接沿用）；已修的两处别名/宽度陷阱记入 lessons 223（残余未定位）。
6. **F1-26 调研（顺带完成）**：`ssa.cpp` 的 Phi 为**纯装饰**——只在汇合点为每条 Load 插 Phi（复用其结果 id）、
   不删 Load、不改任何引用，codegen 对 Phi 输出「阶段一预留，无实际汇编」注释；实测 `-O2 ≡ -O3` 行为一致（3 用例）
   → 对值流零影响（无正确性风险，但也不是任何优化）。**规格 §7.1/§7.4 要求 IR 本身为 SSA**（「每个虚拟寄存器恰好赋值一次」）
   ——现状＝实现与规格偏离。**下一轮方案（对照 Rust/LLVM）＝ mem2reg**：① 地址逃逸分析 ② 迭代支配边界插 Phi
   ③ 变量重命名（Load→到达定义值）+ 删 Load/Store ④ SSA destruction（Phi→前驱块尾并行复制或 codegen 消费 Phi）。
7. **教训（lessons 223）**：代码生成层的寄存器契约改造**必须以全量 E2E 为唯一门禁**——逐组抽样验证＝假绿陷阱
   （UB 在单跑时侥幸正确、全量并行下暴露）。


### 222-a F1-28 arm64 寄存器分配接线落地 + D7 归因修正（2026-09-15，单位机麒麟 ARM64，基线 28d6c02；认领 fb28ccd）

**依据**：用户批阅优先级①（F1-28 arm64 寄存器分配启用；`性能第一`）+ `plans/021` §3-D D7（196-a 新立待核查）。

1. **★D7 归因被实测推翻（本轮最重要结论）**：原归因「`reg_alloc.cpp` 活跃区间过短 → 同寄存器双占」**不成立**。
   实测＝写「区间覆盖校验器」（对每个可分配 vreg 断言全部 def/use 点 ∈ `[start,end]`），在**原分配器未改**前提下跑
   **v2 全树编译 + 全量 E2E 语料 → 违例 0**。机理＝区间 `end` 取全部 def/use 点的最大序数（第一遍 def/use 精确遍历
   **结构性保证**），无需 `out[b]` 保守扩展。**真因＝196-a 接线实现的消费面遗漏**（写/读覆盖不全 → 值流分叉）。
   据此**未实施**计划中的 `out[b]` 修正（无正确性收益且降低分配质量），改为把不变量固化为单测门禁。
2. **F1-28 接线（以结构性设计消除值流分叉）**：读侧统一入口 `loadOperandToX`/`loadOperandToV` 感知分配；
   **结果落位唯一通道 `storeVirtualResult`/`storeVirtualResultFp`**（57 处程序化替换，助手自身排除防自调用递归）；
   `operandText`/`resultText` 感知；**`regSlotOffset`/`regSlotMem` 对已分配寄存器返回 0**（弃用槽的取地址/取文本
   旁路自动退化为无害空操作）；序言成对 `stp`/尾声 `ldp` 逆序（正常/结构体返回/i128 返回**三处返回路径全覆盖**）+
   `stackParamBase()` 按压栈对数补偿；返回位 `emitTerminator` 直传物理寄存器；`backend_factory` arm64 启用 `-O2` 联动
   （隐藏返回指针场景后端内部 forceDisable，与 x64 保留 r12 同款）；结果落位覆盖 oop/i128/dispatch 等全部族文件。
3. **门禁（arm64 口径）**：零警告构建（GCC 9.3）+ 单测 **1327/1327**（新增 2 例不变量用例）+ 全量 E2E
   **312 用例 310 过 / 0 失败 / 2 跳**（rc=0；含 D7 原实证用例 115/235/245）+ **锚定链重锚 561364→558209 行**
   （md5 `534c3dd8…`，`fix_p ≡ fix_s` 逐字节；-3155 行＝寄存器驻留消除栈访存）+ ASCII 门禁 252 文件合规。
4. **性能锚 + 反证**：栈访存 **1077 vs `--no-regalloc` 2379（-54.7%）**；235 用例运行时间 0.36s vs 0.37s（不劣化）；
   `--no-regalloc` 两态**输出逐字节一致**（行为等价）+ 产物回全栈帧形态（唯一变量=开关）。
5. **诚实边界**：冗余 `mov` **1121 条**（指令先算固定临时寄存器 x9/x10 再搬运到分配寄存器）——净收益仍为正，
   彻底消除需**指令选择层寄存器感知改造**（登记 `plans/021` **D8**）；win-x64 同族动态核查本机无工具链
   （结论「D7 撤销 ⇒ 无同族缺陷」，家机复跑全量 E2E 即可复核）；linux-x86_64 后端仍写死关闭（同款接线可复用，登记候选）。
6. **契约面（§8.4 广播）**：arm64 产物结构性变化 → 三机复跑全量 E2E + 各机重建本机 v2p + 各机重锚锚定链
   （arm64 新锚 558209 行已给）；`.expected` 零改动。
7. **文档缺陷发现**：`plans/021` §一~§五 存在**历史重复段**（第二份为旧副本）——本轮按最小局部编辑纪律
   **两处同步本机相关行**，并登记后续专项清理（避免与在飞机器冲突）。


### 193-a F1/F2 长线清单重评（2026-09-15，单位机麒麟 ARM64 规划轮，基线 ecaf5a5；源码零改动）

**依据**：`plans/021` §3-F 明文「下次规划轮需逐条重评后销项或重排」（`plans/007` v1 时代清单，v2 时代待重评）。
**重评口径**：逐条**当场实测**（禁凭记忆/旧文档誊写）+ 双目标准绳（性能超 C++ / 安全近 Rust）。

1. **销项 1 项**：**F2-36a ARM64 原生自举闭环**——`78_v2_自举链构建` / `79_v2_自举闭环` 在 ARM64 **真跑通过**
   （188-a 全量 E2E 309 用例 307 过/0 败/2 跳；锚定链 arm64 **561364 行** `fix_p ≡ fix_s` 逐字节）；原条目另一半
   「性能≈1.04x」系 win-x64 口径 → 拆出 F2-36b 留在重排。
2. **重排 9 项**（各附首步/前置/验收口径）：
   - **F1-26 SSA Phi=半接线**（实测：-O3 产出 Phi，asm 仅 `// Phi节点（阶段一预留，无实际汇编）` ×3）——前置=F1-28；
   - **F1-27 全局值传播=保守子集**（常量 Store→Load）——前置=F1-26（真实数据流须 SSA 形式）；
   - **F1-28 arm64 寄存器分配=写死关闭**（`backend_factory.cpp:44`；win-x64 -O2+ 已启用=模块可用）——与 F1-26 同链；
   - **F1-29 pass 边界=三处显式保守**（i128 不折叠 `const_fold.cpp:66` / 浮点跳过 `algebraic_simplify.cpp:3`〔+0/-0/NaN〕/
     有符号除法跳过 `strength_reduce.cpp:71`）——放开需逐条语义证明（浮点依 91-a IEEE ordered 口径）；
   - **F1-30 DWARF/增量编译/ASan=三项均无**（`debug_info.cpp` 仅映射+预留；004 D-3 后置；ASan 未接入）——拆三项独立出轮，优先 ASan；
   - **F2-33 标准库=十模块**（核心/容器/时间/数学/算法/文件/系统/映射集合/字符串扩展/IO），缺迭代器/正则/线程/网络/随机数
     ——随机数/迭代器/正则=纯 stdlib 可独立推进；线程/网络=随 E1 并发安全立项；
   - **F2-34 包管理=TOML 子集 + 依赖查找，无 semver/锁/缓存/注册中心**——按需触发；
   - **F2-35 工具链=无 LSP/格式化/文档站**——独立专项（首步=诊断/IR 机器可读输出）；
   - **F2-36b arm64 自举性能基线未测**——首步=arm64 跑 `scripts/bench_self_host.py`；
   - **F2-37 规范精化**：设计决策库机制已成形（plans/001 唯一权威 + cn-language-spec 技能 + C16/A3/A4/A5/A8/D6 系列落盘），
     剩余「规范有·实装/验证缺」存量随各专项轮清零。
3. **优先级建议（待用户批阅）**：① F1-26+F1-28（Phi 落地 + arm64 寄存器分配·性能第一·同链）② F2-33 纯 stdlib 三项
   ③ F1-30-ASan ④ F1-29 ⑤ F2-36b ⑥ F2-35 ⑦ F2-34。
4. **文档**：`plans/021`（§3-F 重写为十项结论表 + §4.2 销项行 + §5 索引 + 首行戳双口径）/ `plans/007` 头部状态指引
   （历史原文保留、以 plans/021 为准）/ `plans/025`（本机节回填）/ `plans/019` §7.3 / 本日志 / HANDOFF / 看板（收工）。
   **源码零改动**（`git diff --stat` 仅文档）。

**诚实边界**：深水项（Phi 落地形态 / arm64 regalloc 正确性）以实测为准，未当场确证者标注「待开发轮核实」；
**优先级与是否实施属用户裁决面——本轮只出结论与建议，不擅自开工**（报告待批阅）；github 镜像本机无凭据未推。

### 196-a arm64 寄存器分配接线（尝试→按纪律回退；接线图谱与根因留档）（2026-09-15，单位机麒麟 ARM64，基线 cad4ea3；**源码零净变更**）

**依据**：用户批阅优先级①（F1-28 arm64 寄存器分配启用；`性能第一`）。

1. **接线全量实现过一版**（镜像 x64 语义）：读侧统一入口 `loadOperandToX`（已分配 → `mov`）；**多形态消费点**全覆盖
   （`operandText`/`resultText` 文本形态、结果落位 ≈25~40 处经 `storeResult` 助手、`emitLoadVirtualTo` 统一 id 取数、
   返回位 `emitTerminator` 的 `%vN`→槽文本转换点 + `emitEpilogue` 接受物理寄存器名）；序言/尾声被调用者保存对
   （x19~x28；隐藏返回指针场景 x19 已被占用 → forceDisable 同 x64 的 r12）；**栈参数基址 `stackParamBase()` 按压栈对补偿**；
   开关 `backend_factory.cpp` arm64 分支改 `regAllocOn`。
2. **接线期修掉 3 个既有缺陷**（与根因无关，已留档可复用）：① `resultText` 未感知分配（常量/转换类指令写槽、读者读寄存器
   → 值流分叉）；② `emitTerminator` 把 `%vN` 先转槽文本（返回位丢失寄存器驻留）；③ 批量替换脚本误将助手自身改为自调用
   → 无限递归（编译器编译 v2 全树段错误；ASAN 栈溢出定位）。
3. **探针验证（接线正确性局部成立）**：最小三例（常量算术/函数调用/循环）rc 全对；递归+循环大探针与 `--no-regalloc`
   输出逐字节一致；**访存指令 ldr/str 计数 93 → 33（-64%）**=性能通道方向正确。
4. **★阻断性根因（转专项轮 D7）**：E2E 3 例偏差（115 向量元素析构 `清空` 循环少析构 1 次 = 总析构 5≠6；235/245
   校验和 191≠255）→ 定位=**分配器 `reg_alloc.cpp` 活跃区间对「循环体内被重定义、跨块/跨迭代使用」的值判定过短**
   （同一物理寄存器被两处同时使用）；最小对照=简单循环正常、容器元素析构循环异常。
5. **处置=按纪律回退**（`plans/025` §三.2 §5 既定口径：「门禁暴露深层不兼容 → git checkout 回退，不留半截」）：
   **源码零净变更**（工作树干净）；探针正确 + 单测 1325/1325 复验通过；接线图谱、3 缺陷、四项教训全部留档。
6. **★新立案 D7**（`plans/021` §3-D）：**分配器循环携带值隐患核查**——x64 后端**同用该分配器且 -O2 默认启用**，
   须核查 win-x64 是否同族（其 E2E 306/306 通过=形态未触发或 wiring 规避）；处置=区间保守扩展 + 三平台复跑 + 反证探针。
7. **文档**：`plans/025` §三.2-7（接线图谱/根因/留档）+ `plans/021`（F1-28 改写 + **D7 新立** + 看板广播）+
   lessons 196 段（预防 179 批量替换三自检 / 180 接线多形态消费点 / 181 非标准构建产物隔离 / 182 线性活跃区间在回边上不充分）+ 看板收工。

**诚实边界**：本轮**未交付功能**（按纪律回退）；性能收益未兑现（待 D7 专项后重接）；github 镜像本机无凭据未推。
