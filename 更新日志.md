# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十六轮 116-a：D1 行数整改第七波——`semantic_expr.cpp` 1778→654 行按族拆分**（2026-09-13，家机 win-x64；基线 0185c9e）：
> ①**拆分**（纯重构零行为变更）：`semantic_expr.cpp` **1778→654 行**，拆出 `semantic_expr_op.cpp`
> （**845 行**：运算符/赋值检查——visitBinaryExpr/visitUnaryExpr/visitTernaryExpr/reportNonLvalueTarget/
> visitAssignmentExpr）+ `semantic_expr_access.cpp`（**313 行**：成员/下标检查——visitMemberExpr/visitIndexExpr）；
> 族边界勘定=无 static/匿名 ns 依赖（**115-a helper 头化直接铺路**）；CMakeLists 挂载 2 文件。
> ②**等价性**：产物 asm 五样本（v2 全树 404786 行 + hello/struct_enum/u240/u241）**逐字节一致** + 行级多重集校验。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 连续第五次不变** +
> 对拍 **34/34** + 运行级 **3/3**。
> ④教训：本轮无新坑——D1 拆分 checklist 已收敛为标准流程；**前置投资（115-a 共享头）对后续每波复利**。
>
> **本会话（111-a~116-a）累计**：
> - **111-a**：C15 win 双编译对照原生支持 + C14 size 说明符门禁 + 新缺陷 221 双侧根治（win 门禁 **259 过/33 败 → 290 过/2 败**）
> - **D1 六波**：`ir_expr.cpp` 2798→734 / `ir_oop.cpp` 1381→508 / `ir_oop_call.cpp` 1155→763 /
>   `semantic_internal.hpp` 共享头（11 helper × 5 文件去重，-536 行，`semantic_call.cpp` 988 出列）/
>   `semantic_expr.cpp` 1778→654——**宿主 4 大文件全部达标 + 语义层去重**
> - **全程锚定链固定点 406097 行不变**（五次复验）+ 产物 asm 五样本逐字节一致 + 全量零回归
>
> ⑤诚实边界：D1 剩余（`semantic.cpp` 1995〔可直接按族拆〕/`ast.hpp` 1032 + codegen 面 4 文件 + v2 侧 6 文件 +
> 函数级 visitCallExpr 885 行等）登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
