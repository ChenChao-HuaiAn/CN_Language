# 功能完善总结

> **本轮（第八十二轮）含两个闭环**：82-a v2 帧尾共享 retbuf 根治（数据损坏级）＋
> 82-b v2 赋值位接收结构体返回（82-a 登记边界销项）。

> 82-a v2 帧尾共享 retbuf 根治（2026-09-12 第八十二轮，深度机 linux-x86_64）：
> HANDOFF 第八十一轮「下一轮任务 #1（第一优先，**数据损坏级**）」销项——用户令
> 「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案（性能第一、
> 安全第二）」。以**返回物化到独立槽**（**LLVM sret 各自分配语义**）根治 v2 的
> 帧尾共享 retbuf：每返回值独立 + 实参位置调用后释放 + 跨模块返回类型按定义模块限定。

## 一、缺陷实证（探针 P15/P17/P19/P20 + E2E 172）

| 形态 | 宿主 | v2（修复前） | 触发条件 |
|---|---|---|---|
| `表.追加(造盒())`（单实参） | 0 | 残留 **1**（句柄泄漏） | 外层调用返回值也用 retbuf（结果返回） |
| `取大(a, 造点(1,2))`（标识符+返回混合） | 7 ✓ | 7 ✓ | 外层调用返回**标量** → 无覆盖（本轮探明，收窄面） |
| **`合并(造盒(1),造盒(2))`（双返回实参）** | 1 ✓ | **0（数据损坏）** | 第二次返回覆盖第一次 |
| E2E 172 跨模块 `甲::记录 甲记录 = 甲::造甲(42,99);` | 141 ✓ | **84（数据损坏）** | 跨模块返回类型按纯名解析到当前模块**同名异布局**类型（主 4B vs 甲 16B） |

## 二、根因（三步收敛）

1. **共享单缓冲**：v2 全部结构体/结果返回共用帧尾 retbuf（`retbuf序号`+`累计retbuf字节数`）；
   宿主相反——每次调用独立 `__retbufN` Alloca 槽。
2. **槽基址约定**（首版修复失败点）：v2 结构体**基址槽 = `槽起 + 槽数 - 1`**（最大槽号=最低
   地址=结构体基址，②b B1 约定）——误用 `槽起` 当基址 → 写穿相邻变量槽（P20 实测物化槽
   -704 与 t 槽 -696 重叠 8 字节）；且只发一条 `IR_分配` → `扫描函数` 漏统计=帧尺寸不足。
3. **跨模块返回类型**：函数键格式为 `模块$名#参数串`，返回类型表只存**纯名** → 调用方模块
   上下文解析到同名异布局类型（→ retbuf 尺寸/物化长度错）。

## 三、方案与实施（甲案 = LLVM sret 语义；v2 树 3 文件）

- `IR容器.cn`：新增 `物化返回值到独立槽`（槽起/基址=槽起+槽数-1、逐槽 `IR_分配`、memcpy、
  返回槽地址寄存器并登记）/`清理调用实参临时`/`发射串字段释放顺序`/`是纯串字段结构体`/
  `限定返回类型ID`（跨模块：键文本首 `$` 前=模块前缀，重查结构体大小命中才用限定名）/
  `子串位置` + 静态 `结构体返回临时们`。
- `生成表达式求值.cn`：`发射普通函数调用`/`生成方法调用`（签名加 `整64* 槽计数`）在
  `IR_调用参数化` 后物化真结构体返回、以**槽地址**压栈；`生成调用` 两处调用点在外层调用
  发射后清理实参临时。
- `IR生成.cn`：`清空函数级槽表` 加登记表清空（防跨函数寄存器号误命中）。
- **槽分配纪律四条（本轮订立）**：预扫分配参数/变量槽（槽 1=retbuf 指针保留）→ 生成期临时槽
  只递增不复用不交错 → retbuf 区由代码生成动态计算（`槽基数=最大槽`）随之上移**不可能重叠**
  → 结构体基址槽=`槽起+槽数-1` 且**逐槽** `IR_分配`。

三案对照：乙（retbuf 全量独立）因帧尺寸=Σ返回尺寸落选；丙（所有返回后物化）与甲实质等价。

## 四、防线与反证实证

- **E2E 236_双编译对照结构体返回实参**（移植纪律 9 **第八个**对照用例：八形态折 **255**，
  ②`取两号(造盒(1),造盒(2))` 为**核心回归锚**；不含容器=无供给/无需借链）；**E2E 234 形六
  改回「调用返回实参入容器」**（八形态折 255 不变，v2 已支持该形态）。
- **反证实证**：v2 注入「物化恒不物化」→ 236 v2 退出码 255→**68**（① ② ⑤ ⑥ ⑧ 位全失）；
  恢复后两侧 255 ✓。E2E 172（跨模块类型分桶）修复过程中回归（v2=199）→ 根因③修复后复原 ✓。

## 五、验证链（本轮终值：82-a + 82-b 两闭环）

清场重建**零警告** + 单测 **1306/1306** + E2E **263 用例 259 过 0 失败**/4 跳（+236；
234 形六改回实参形态；其余 258 过=零回归，含 172 复原）+ 锚定链固定点 **320424 行**
（82-a 后 319557；上轮 308719）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+
释放路径两侧对照（脚本新增 82-a 段：物化/清理/限定单点 + 槽分配纪律）。

## 六、82-b v2 赋值位接收结构体返回（82-a 登记边界销项，v2 树 1 文件）

- **缺陷**：`点 a; a = 造点(1,2);`（赋值右值=调用返回）宿主支持、v2 诊断中止
  （`[ir2] 结构体赋值右值须为同型变量标识符: a`）；含串字段同款。
- **实施**（`IR/生成语句.cn` `生成结构体整体赋值` 新增「右值=节点_调用」分支）：求值右值
  表达式序列（→ 82-a 返回物化 → 表达式栈顶=物化槽地址）→ 目标=左槽地址 → **浅拷接管**
  （物化槽=一次性返回值，句柄移交目标=**零拷贝**；与 79-a 声明位「返回接收接管」同语义）
  + 目标拥有槽 preFree + 同型校验用物化登记表 fail fast。
- **防线**：**E2E 236 形四改版**（赋值位接收 + 跨越另一次返回调用，折 **255** 两侧一致）；
  **反证实证**：注入「调用返回右值分支失效」→ 236 v2 侧编译失败（IR 层错误中止），恢复后 255 ✓。
- **验证链（本轮终值）**：清场重建零警告 + 单测 **1306/1306** + E2E **263 用例 259 过 0 失败**/4 跳
  + 锚定链固定点 **320424 行**（82-a 后 319557；+867）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**
  + 释放路径两侧对照 ✓。

## 七、登记（下一轮候选）

- v2 **结果/可选 返回**不物化（合成布局；返回值语言层不可作实参 → 覆盖不可达）——登记；
- v2 **赋值位接收结构体返回**（`盒子 甲; 甲 = 造盒(5);`）=IR 层不支持（宿主支持）——登记；
- 物化性能优化（延迟物化 + 活跃性分析）——登记（非必需）；
- 81-a 余项：B35 字段容器接收者（语言级裁决）、stdlib 链表槽复用、v2 结构体字面量实参（语法缺口）、未初始化槽 preFree。
