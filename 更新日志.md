# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（724-a·cn_realloc 异常尺寸防御·2026-09-25·任务/家机-724-拷贝构造四循环对拍修复·wt698）

**行为面（src/runtime/io_api.cpp）**：cn_realloc 增异常大尺寸可观测拒绝（size>2GB→stderr 打印+NULL·Rust alloc 合同同款）——**实测 cn_self 编译 t4 从崩溃转为干净诊断**（语义错误 4 个退出·形参装载失效面首次可观测）·全量 E2E 528=524/4 零新增·79_v2=干净退出 1。下一棒=725 t4 4 错根因修复（标量形参装载失效）→t4→0→fix_s 判据→001 销项。

## 深度机 linux-x86_64 节

## 功能完善总结（721-a 验收回签服务轮·2026-09-25·窄通道②·零分支零轮次号）

**家机 721-a 验收请求 linux 侧应答（基线 develop 87870e7c·独立树 wtsvc721 全新构建）**：

1. **变更面先核后跑**：721-a io_api.cpp VEH 增强（ASLR RVA 换算＋rbp 链回溯 16 层·34 行）全部位于既有 `#ifdef _WIN32` 守卫段内（585→667 行）；runtime.cpp entry() 两调用点已由 722-a 守卫收口——linux/arm64 侧零行为面＋零编译面影响（GCC 条件编译不进入该段）。
2. **全量门禁铁证**：独立 worktree（距 Documents 两级）全新构建——零警告＋单测 1405/1405＋strict 181/181＋matrix 83/83＋E2E 528=523/3/2——红名单与已知基线完全同构（113 值差/127_v2 rc206/79_v2 fix_s rc1·零新增红零回归）；E2E 显式 --jobs 8＋gate_lock 串行锁。
3. **诚实边界**：VEH 输出形态（RVA 换算与 frame 回溯打印格式）为 Windows 运行时行为，运行级复验归 win 侧（家机自有面）；linux 可验面=「变更不破坏 linux 构建与测试」已闭环。
4. **B8 巡视**：daemon 存活＋今日已有一轮完整采样（新轨 40/40＋回归轨 56·零新缺陷类别·M3 计时连续）。
5. 零 src/tests 行为面；窄通道②一次提交直推（看板回签＋021 文首戳与 §五 服务轮行＋交接/更新日志本机节）；快速门禁 check_handoff＋check_progress_sync 全绿后推送。

## 单位机 ARM64 节

## 功能完善总结

### 服务轮：698-a/702-a/703-a 三轮批量 arm64 异步验收+M3 采样补跑（2026-09-24·基线 develop 5ed202f7→2a96c036）

1. **批量验收**：清场重建＋单测 1405/1405＋E2E 全量 **528=523/3/2 净增绿**（基线 4 红→3 红·497 转绿=698-a 装配修复 arm64 实证）。
2. **红面形态对表全一致**：113 段错误→值差（待 052 裁决）；79_v2 段错误→fix_s 编译失败（703-a 定性吻合·025 §1.11 专项）；127_v2 段错误→退出码 206（红维持·如实记录）；524 arm64 绿维持；218 PASS（702-a 复绿同侧成立）；容器析构面 12 例回归全绿·零特有红。
3. **M3 采样补跑**（626 波2 后首次·两日空窗补记）：零新缺陷类别——m27 系 T27/T96 已知族复现（m27_03 与 09-21 meta 逐值一致）+存量 m38/m42/t71；日文件 2026-09-24.md 已记。
4. **看板**：基线行登记 2a96c036；通告段写三轮批量验收广播、删 596+601 旧通告。

