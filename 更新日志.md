# 更新日志

## 功能完善总结

### 灰色点②方案A落地：字符串比较运算符显式拒绝（2026-09-04 家机 win-x64，用户裁决）

用户裁决方案A（Rust 纪律）：`字符串/字符*` 之间的 `==/!=/</>/>=/<=` 比较运算符**显式拒绝**
（硬错误+迁移提示）——原两侧一致「按指针地址比较放行」=同内容异地址的 `==/!=` 恒假
（拼接产物静默陷阱）。**规范依据**：plans/001 Task 2.8 已明文「比较运算符仅定义整型与浮点
变体，字符串不参与运算符比较；等价能力由 字符串比较（相等）与 字符串字典序（全序）提供」
——本实施是**让实现对齐既有规范**（非语义变更）。

- **宿主**（semantic_expr.cpp 比较分支）：字符串×字符串（含 字符* 交叉）比较运算符
  硬错误「字符串不支持 ==/!=/</> 等比较运算符（按地址比较而非内容）——相等用
  字符串比较(a,b)，全序用 字符串字典序(a,b)」；**字符串/字符* 与 无（空类型*）的
  判空比较保留**（Task 6.2，133 EOF 载体）；真指针类型间按地址比较保留（判空/同址）。
- **v2**（语义分析.cn 检查表达式 新增 节点_二元运算 分支）：比较 op 且两操作数区间
  末端递归推导均为字符串 → 同款硬错误；返回 -1（比较产物不参与下游推导，三元末端
  为二元时原亦 -1 跳过，无回归）。**诚实边界**：拼接产物/成员字段末端推导 -1/整64
  兜底=检不出（字面量/变量形态覆盖主踩坑面，随 v2 语义类型推导完善收口，宿主侧
  已全形态覆盖）。
- **v2 组件源码修正 1 处**（语法分析.cn:881）：`当前文本(记号, 位置) != "}"` 原按
  驻留地址比较（依赖驻留恒等实现细节——恰为本根治目标的陷阱用法）——改
  `!字符串比较(当前文本(记号, 位置), "}")`（返回语句边界判定，非热区零性能影响）。
  全库复扫：v2 组件与 stdlib/既有 E2E 用例源码无其他字符串比较运算符形态（预检+全量门禁双证）。
- **测试**：宿主单测 **test_string_compare_check.cpp** 9 项（5 负形态精确消息：变量==/!=、
  字面量直比、顺序比较 <、字符* 交叉 + 4 合法零误伤：字符串比较/字典序 内置、
  `字符串 != 无` 判空、真指针判空、拼接语境）+ E2E **140_字符串比较运算符拒绝**
  （宿主负测：期望编译失败.txt 固化诊断子串）+ **141_v2_字符串比较拒绝**（v2 负路径
  闭环：与 139 同款 runner 预期退出码 None 形态——v2p 退出码 1+无 asm+诊断行固化）。
- **验证**：单测 9/9 新增全绿；E2E 140/141 双绿；全量门禁见 HANDOFF。
