## 功能完善总结

### 一、③ 第三波：指针下标读写（E2E 130 六步闭环绿）+ 宿主内存模型统一 C 布局

> 本轮在家机（Win11 AMD64）拉取单位机 1e8e6ea 后推进 ③：先补 win-x64 对称门禁
> （踩出 129 win 首败），立项 E2E 130（指针下标读写——语法分析.cn 冒烟崩点
> `位置[0]` 读写的载体），验证期按缺陷零容忍纪律（用户重申开发纪律令）当场
> 根治四簇缺陷，宿主/v2 两侧同轮收口。

#### 1. 宿主内存模型统一 C 布局（缺陷③，asm diff 实证定位）
- **现象**：指向结构体紧凑字段的 整32* p[1] 步进 8 跨两字段读脏值（130 首踩）。
- **根因**：三套咬合约定不一致——数组局部 8 字节槽/元素（非 C）× 指针步进
  ptrElemStride 兜底 8「双错互洽」（05 用例恰同错而对）vs 结构体/.data C 紧凑
  vs 容器堆分配 max(类型大小,8) 槽模型。
- **根治**（全部统一 C 布局，p[i]≡*(p+i) 步进=sizeof(元素)）：
  - `ir.cpp ptrElemStride`：统一 `typeSizeOf(pointee)`（特判 结构体/类/i128 全收敛）；
  - `ir.cpp registerVarSlots`：数组槽数=ceil(typeSizeOf(数组)/8)（与 .data 一致）；
  - 数组初始化（ir_stmt）/读（ir_expr）/lvalue（ir.cpp）三处步进统一 typeSizeOf；
  - 补零写入按元素宽度（i128/浮点/指针保持 i64 原行为——常量无双槽/movss 不收立即数）；
  - `stdlib/容器.cn + 映射集合.cn` 9 处分配 `max(类型大小,8)`→`类型大小(T)` 紧凑；
  - **`ir_expr.cpp A-3 隐式类字段读分支**自带「标量 8 槽」步进（元素()/获取() 走它、
    写路径走 ptrElemStride——读写步进不一致=30_container 腐坏；asm diff 7 处
    `shl rax,3→2` 锁凶）同批统一 typeSizeOf。
- **回归**：容器 30/113/111、数组 05/09 全绿；130 宿主 sanity 28 项断言全过。

#### 2. 宿主窄宽 StorePtr 常量（缺陷①）+ v2 movsxd 缺 dword ptr（缺陷②）
- 宿主 `x64_instructions.cpp emitPtrLoadStore`：窄宽（i8/i16/u8/u16）StorePtr 值为
  立即数常量（显式强转 `整8(7)` 折叠直达）时发 `movsx rcx, byte ptr 7`——movsx/
  movzx 不收立即数（A2070）。常量特判 `mov rcx, <imm>` 直载（写 cl/cx 取低字节=
  补码截断，负常量同正确）。
- v2 `代码生成.cn IR_类型转换`：`movsxd rax, [槽]` 缺 `dword ptr`（MASM 无法判定
  尺寸 A2070；GAS 侧 sxtw 无此歧义——**单位机仅跑 ARM64 故 129 win 首跑即炸，
  「ARM64 全绿≠win 对称已验证」实证**）。

#### 3. v2 MASM db 超限（缺陷⑤，129 win 二段错误）
- **db 超限**：v2 字符串字面量逐字节 db 单行发射——MASM db 单语句操作数上限 48
  （隔离二分实测；GAS .byte 无此限，129 win 首跑被 movsxd 先拦、修后才暴露）；
  53 字节中文字面量首踩。分段续行 + **剥离行尾悬挂逗号**（悬挂逗号=不完整
  操作数，同 A2042——48 值行隔离复测铁证）。

- **取地址分支「清空全栈」**：`f(&x)` 调用实参形态把先序压栈的被调负符号一并
  弹掉→`[ir2] 调用被调对象非函数名`。
- **负号/逻辑非/解引用操作数双份残留**：操作数被主遍历线性压栈+分支嵌套生成
  两份，`q[-1]` 负下标弹2取到残留常量当地址→解引用垃圾地址 0xC0000005。
- **根治**：前缀一元（负号/逻辑非/解引用）操作数区间并入 取址区间们 预扫跳过；
  三分支+生成左值地址 解引用/下标 早退全部改「弹到进入前深度」（禁清空全栈）。
- 连带：IR生成 参数循环池索引 0 守卫复发（`>0`→`>=0`+节点类型判定，lessons
  同日第二次发作）。

#### 5. v2 指针下标读写四层实现（E2E 130 载体）
- **语义**：指针变量表（类型节点附加=指针层数 1..255 登记，参数/局部，检查函数
  清空）+所指类型白名单（标量/字符串——浮点/结构体/容器显式拒绝=IR 加载宽度
  覆盖面）+下标左值放行（指针对象）+复合赋值左值类型取所指。
- **IR**：指针变量类型们（生成函数清空）+下标读分支（步进=类型名宽度、步进1 免乘；
  LoadPtr 元素宽度码；结果登记类型码）+生成左值地址 下标分支（读对象槽值+下标
  缩放）+赋值写宽度=元素宽度码（窄元素 4 字节写防越界）。
- **自检**：v2p 尾部两负测（整64 变量下标拒/字符串下标写拒）每次编译自证拒绝
  能力；130 期望固化。
- **闭环**：宿主 sanity（退出码 0，输出 4/77/全部通过）↔ v2 六步闭环产物运行
  输出**完全一致**（同源双编译器一致性锚定）。

#### 6. 边界与观察（诚实披露）
- v2 整8/整16 强转白名单未覆盖（语义**显式拒绝**非静默；宿主侧窄宽常量已根治）；
  130 用例剥离 整8* 节并注明。
- 探针级观察（未立案，报用户知悉）：`a * a = 7` 语句按声明路径解析但**有报错**
  （C 式 lexer hack 歧义，换行≡空格为规范行为）；级联诊断质量（F 形态静默）。
- 78/79 win 侧内存超限为已知遗留（v1 字符串行 IR，v2 重建动机）。
