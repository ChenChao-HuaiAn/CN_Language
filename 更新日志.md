# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 424-a 波次8 C20 批D 上架批1+同日服务轮·2026-09-19）

- **424-a 开发轮·C20 批D 上架批1 ✅（波次8）**：356-a 批C「防线对齐 ~85 候选池」首批 12 例 v2 闭环负测上架（453~464·414_v2 先例模式）——词法/语法防线桶（323 未闭合注释/413 T21 标识符/411 T3 递归结构体/396~398+401 T5 转义/145+295+296+297+354 分号族）宿主负测的 v2 侧同形态持久门禁（v2闭环.txt 退出码=负+主.expected 实跑固化·措辞不精确锚定=防线演进容错）。**验证**：_v2_ 全集 97=97/0（85 存量对照零回归）+12 例单跑全 PASS+反证抽例（460 配置改 0→FAIL→还原→PASS）+coverage_map 8 行同步。上架定式沉淀：expected 路径行须写 v2src{主段}/ 形态（runner 适配器命中）；多文件用例导入模块的已解析行同样规整（462 首跑捕获）。剩余候选 ~70（sem5 语义桶）分批续。**try-build 请求中**（写集触 tests/）。
- **同日服务轮（累计）**：深度机 413/414/416/417/418/419/**421+422**（0f213d4·族① 类型大小 ×48 消 win 零回归）+单位机 329-D12 的 win try-build 回签**全清**（唯一红 406=T43 基线·零新增红）——集成链解锁；422 撞号处置确认（深度机顺延 423·本机 D33 占号有效）；develop 缺账盘点+388 作废建议已广播。
- **挂起态**：422-D33（D33 全销项·try-build 请求中）/364-D10（等 arm64 回签）/424-a（try-build 请求中）——三者均待他机回签后 `--try-build-done` 集成。
- **操作事故如实记录**：批量脚本 ROOT 参数化 sed 失效+heredoc \U 转义损伤再犯→12 例两度误建主树（0f213d4 检出态·421/422 门禁 E2E 在飞中）→两度即删，门禁终态 445=444/1 未受污染（总数与深度机链固有口径一致实证）；教训=跨工作树脚本 Write 生成+参数默认值防误。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 388-a 事故恢复+389-a D45 定位·2026-09-19）

- **388-a 事故恢复**：387 收工内容因操作失误未经回签直入 develop（668d7b1）——恢复分支回退基线至 190f97e·等两机回签后 integrate；本机恢复态门禁=零警告+单测 1353+E2E 444=441/1/2（恢复正确性证实）；lessons 权重 10（push 前三问自查）。
- **389-a D45 定位**：D43 别名映射单点实证正确（5/1.500000）但三写法均触发 v2p 自身向量越界（gdb 栈=向量$节点$元素·组合二分锁定谓词调用链）——★D45 新立 P1·改动撤下（patch 备份）·根治后 D43 重打。
- 零行为面进 develop（389 源码撤下·388 恢复待回签）；文档面六件套同步。

**门禁**：check_handoff/check_progress_sync 绿；推 gitcode（分支+看板直推）。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
