# 更新日志

## 功能完善总结

### IR生成.cn 冒烟 96 错收口 + [ir2] 819→0 + IR 层错误即中止纪律（2026-09-04 第十三轮，家机 win-x64）

接 HANDOFF 下一步①（单位机立项遗留：IR生成.cn 冒烟 96 错消化+IR 层纪律），全链落地：

#### 一、96 错根因：泛型引用局部声明形态不被识别（三层定位：文件分摊+函数二分+探针矩阵）
- 根因：`向量<IR指令>& 指令 = 函数信息.指令;`（IR生成.cn 三处）泛型闭合后是 `&` 非标识符——
  是泛型声明 返回假 → 整条声明落表达式解析成垃圾（84 指令+9 向量未声明+3 左值全为级联）。
- 语法层：是泛型声明 闭合后 `& 标识符` 同认（引用后缀留给 解析类型 引用登记循环）。
- 语义层：引用初始化纪律——必须初始化+左值白名单（标识符/成员/解引用，与 IR 生成左值地址 能力对齐）。
- IR 层：引用局部分支——单槽=绑定对象左值地址（生成左值地址 → IR_存储 入槽），槽登记
  引用参数槽们/类型们——读取分流/成员基址/赋值写回/再传引用实参数据流与引用参数完全同构复用。
- 连带根治两枚：预扫容器声明 误把引用局部登 RAII 名单（绑定字段地址当 this 析构=堆损坏
  0xC0000374 实证）→ 附加==256 跳过；回扫实参取地址 补指针加载形态（IR_指针加载.操作数1
  即左值地址，直接复用零新指令）。

#### 二、[ir2] 819→0：v2 自举刚需形态全链（语义 0 错后 IR 层缺口可见化）
- 用户函数返回结构体 retbuf ABI 全链：函数IR.返回结构体ID / 被调方槽 1=rcx retbuf 指针 +
  返回语句 IR_结构体拷贝+rax / 调用点帧尾共享 retbuf+位 0 装载+实参起始位 1 /
  解析调用返回结构体 补普通函数分支（链式 造点(1,2).x）/ win+ARM64 双后端 prologue 对称。
- 结构体初始化两形态：调用返回（IR指令 项 = 建IR指令(...)）拷贝入基槽；标识符拷贝
  （节点 参数类型节点 = 参数——生成函数 参数循环自用形态）。
- 容器初始化两形态：函数返回=接收转移（不 NewObject，被调方构造对象所有权移交）；
  标识符拷贝=浅共享指针（对齐宿主「按值传 向量 共享数据指针」语义）。
- 回扫三误报根治（实参数值本已正确，仅诊断误报）：调用返回 rax=retbuf 地址即按值实参要传的
  地址（655 条）；IR_取地址 &标识符引用位（26 条——查方法签名(...,&签)）；成员指针加载=字段地址。

#### 三、IR 层错误即中止纪律（对齐 139 语义错误即中止）
- IR容器.cn 新增 IR层诊断 单点计数（13 处 [ir2] 诊断全部接入）；IR生成多文件 入口清空。
- 主.cn IR 生成后 取IR层错误数()>0 → 中止诊断+返回 1 不产 asm（残缺 asm 可被链接=错误产物）。
- 类型大小 未知分支的 运行时错误(6) 立即崩改为诊断后继续收集（一次报全量对齐）。

#### 四、测试（+3 E2E 用例六步闭环全绿）
- 147_v2_局部引用声明：引用局部绑定/经引用成员调用/再传引用参数/绑定同一对象验证+
  供给 向量$项（B7）；宿主 sanity 双文件先行锚定（exit=42/0）。
- 148_v2_函数返回结构体：初始化/链式字段/按值实参标识符+嵌套调用/双结构体实参/retbuf
  复用不串扰；宿主 sanity exit=0。
- 149_v2_IR层错误中止：负路径闭环（139 同款 runner 形态：退出码 1+无 asm+三诊断行）。

#### 五、冒烟与门禁
- IR生成.cn 冒烟全绿：语义 0 错+IR 0 诊断+asm 6.35MB+ml64 汇编通过（1.37MB obj）——
  v2 自举第 6 组件完整编译（七组件中词法/语法/语义/IR签名/IR容器/IR生成 六个已全绿）。
- 五组件冒烟维持 0 错；147/148/149 单跑绿；全量门禁见 HANDOFF。

#### 六、边界入档（诚实边界）
- 裸类型引用局部（项& q = p）宿主 parser 亦不支持——双侧一致不支持，如需属宿主先行新特性另立案。
- 引用初始化下标形态（a[0]）待 IR 生成左值地址 支持下标时同步扩（语义白名单未含）。
- 结构体整体初始化的字面量/成员拷贝形态、容器拷贝源未解析形态保持 [ir2] 诊断（诚实可见化）。
