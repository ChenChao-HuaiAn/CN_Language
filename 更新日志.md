# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段4 第二层第一批：类方法 不安全 修饰位 + stdlib 裸指针 API 迁移 + 固定点不变实证（2026-09-10 第六十三轮 深度机 linux-x86_64）

### 一、类方法修饰位

`类 X { 不安全 函数 裸清() { 释放(数据); ... } }`——不安全方法体内五类越界操作豁免观察期警告；parser/ast/class_resolver/checkClassMethods 全链。

### 二、stdlib 迁移（裸指针算法 API 契约显式化）

算法.cn 排序整32/排序整64/变换整32/变换整64 + 字符串扩展.cn 分割，五函数加 不安全 修饰；stdlib 全模块警告归零。逆序 泛型函数零警告不迁（泛型函数修饰语法随第二批）。

### 三、验证

E2E 205+全量 222 用例 217 过零回归+单测 1289/1289；锚定链**固定点 245230 逐字节不变**（修饰零 IR 影响的天然实证）+对拍 29/29+运行级 3/3。

### 四、状态

阶段4' 三方案（推荐 A）两轮征询无答复维持挂账；阶段5 外部依赖维持；第二批（v2 树+v2 语法层认 不安全+泛型函数修饰语法）第三批（收口硬错误）随批次推进。
