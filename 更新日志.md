## 功能完善总结

### arg_kind=20类型问题修复

**问题描述**：模块导入时，结构体类型的字段信息没有完整保留，导致成员访问表达式的类型推导失败，参数类型被标记为CN_TYPE_UNKNOWN(kind=20)。

**修复内容**：

1. **增强函数调用参数类型检查时的动态解析** - [`src/semantics/checker/semantic_passes.c:1635`](src/semantics/checker/semantic_passes.c:1635)
   - 在函数调用参数类型检查前设置动态解析作用域
   - 确保导入的结构体类型能正确解析字段信息

2. **增强函数指针调用的动态解析** - [`src/semantics/checker/semantic_passes.c:1609`](src/semantics/checker/semantic_passes.c:1609)
   - 在函数指针调用参数类型检查前设置动态解析作用域

3. **修复选择性导入时结构体/枚举类型的module_scope复制** - [`src/semantics/resolution/scope_builder.c:3765`](src/semantics/resolution/scope_builder.c:3765)
   - 选择性导入时，确保结构体和枚举类型的module_scope被正确复制
   - 支持跨模块的结构体成员访问

**验证结果**：
- 编译器编译成功
- 简单模块导入测试通过
