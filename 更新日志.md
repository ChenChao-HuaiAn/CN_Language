# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十三轮 113-a：D1 行数整改第四波——`ir_oop.cpp` 1381→508 行按族拆分**（2026-09-13，家机 win-x64；基线 6be2254）：
> ①**拆分**（纯重构零行为变更；成员函数声明仍在 `ir.hpp`）：`ir_oop.cpp` **1381→508 行**，拆出
> `ir_oop_container.cpp`（容器<T> 元素自动析构注入——injectContainerElemDestroy 等 5 函数，**512 行**）/
> `ir_oop_field.cpp`（类字段访问——classFieldType/genInstance·StaticFieldAddr/handleClassField* 等
> 11 函数，**389 行**）；族边界勘定=无匿名 ns/文件级 static 依赖（112-a 模式全量复用）；CMakeLists 挂载。
> ②**等价性硬证据**：产物 asm 五样本（v2 全树 **404786 行** + hello/struct_enum/u240/u241）**逐字节一致**。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292**（78/79 OOM 既有，零回归）+ 锚定链固定点
> **406097 行不变**（连续第三次）+ 对拍 **34/34** + 运行级 **3/3**。
> ④教训（lessons 第一百一十三轮段）：**后台长任务必须单独一条命令启动**（`&&` 链实测不启动——
> 单测 rc=0 但 E2E 从未运行）+ 同模式拆分复用收益（112-a→113-a 成本 ~2h→~40min）。
> **同日上一轮 112-a**：`ir_expr.cpp` 2798→**734 行**（四族拆分 `ir_expr_unary/assign/index/member.cpp`
> 435/949/310/434——同款等价性验证；提交 6be2254）。
> ⑤诚实边界：D1 后续波次（semantic 系列需先建 `semantic_internal.hpp`、ir_oop_call 3 个 static 待勘定、
> v2 侧 6 文件、codegen 面 4 文件、函数级拆分）未做——登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
