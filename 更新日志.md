## 功能完善总结

### 模块系统 v2.0 全面改造——第 8 层「全链路集成串联」完成 ✅

**提交**：`（本层待提交）`（develop，中文标题+描述）
**测试**：E2E **54/54**（+1：52_library）、单测 **1108/1108**（+2 回归）、编译零警告

---

### 一、新增 E2E 52_library「图书管理系统」综合工具

用 v2.0 全特性（13 项）重写真实综合命令行工具，证明模块系统端到端可用：

```
tests/e2e/52_library/
├── 货舱.toml              # [货舱] 名称/版本 + [依赖] 核心=内置 / 工具库=本地
├── 主.cn                  # 入口（crate 根）：创建图书→加入馆藏→打印→版本限定调用
├── 图书.cn                # 图书 类（私有字段+公开方法）+ 创建图书 + 默认私有辅助
├── 管理.cn                # 模块树根：模块 馆藏 声明
├── 管理/馆藏.cn           # 管理::馆藏 子模块（子目录）：馆藏 类（手动动态数组）
└── 依赖/工具库/
    ├── 包.cn              # 工具库 crate 根：公开 导入 格式化::价格/ISBN 再导出
    └── 格式化.cn          # 工具库::格式化：价格(浮64)->字符串（%.2f）+ ISBN + 版本
```

### 二、13 项 v2.0 特性覆盖对照

| # | 特性 | 用例位置 |
|---|------|---------|
| ① | `::` 路径导入 | `导入 管理::馆藏::馆藏` / `导入 图书::创建图书` |
| ② | 花括号多符号 | `导入 工具库::格式化::{价格, ISBN}` |
| ③ | 重命名 | `导入 工具库::格式化::{价格 作为 格式价格, ISBN 作为 格式ISBN}`（花括号项内重命名） |
| ④ | 通配符 | `导入 管理::馆藏::*`（馆藏 类纯名可用） |
| ⑤ | 模块 声明 | `管理.cn` 内 `模块 馆藏`（模块树） |
| ⑥ | 子目录 | `管理/馆藏.cn` → 模块名 `管理::馆藏` |
| ⑦ | 包.cn 再导出 | 工具库 `包.cn` `公开 导入 格式化::价格` |
| ⑧ | 货舱.toml | `[货舱] 名称/版本` + `[依赖] 核心=内置 / 工具库=本地`（自动发现） |
| ⑨ | prelude | 打印/格式化/整数转字符串/分配/释放 无导入直调 |
| ⑩ | 默认私有 | 图书.cn `校验编号` 默认私有（公开函数闭包）；类内私有字段仅类内可访问 |
| ⑪ | 可见性交集 | 图书 模块公开类 × 类内公开方法 → 主.cn 跨文件实例化/调用 |
| ⑫ | crate 隔离 | 主 `版本()`=100 与 工具库 `版本()`=200 同名同签名互不冲突（限定调用） |
| ⑬ | 顶层常量 | `常量 默认馆藏容量 = 3` 跨函数引用 |

### 三、最小修复（v2.0 实测缺陷，2 处）

1. **无参函数跨模块注册 key 解析**（[`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp:361)）：
   - 缺陷：无参函数（sigKey 无 `#`）跨模块注册 key=`格式化$版本`，resolveOverload/hasFunctionName 的 key 解析只处理 `模块名$名#参数`（`$` 在 `#` 之前），无 `#` 时 keyModule 不提取 → base=`格式化$版本` ≠ `版本` → 限定调用报"未找到匹配"
   - 修复：`#` 不存在且调用名不含 `$`（区别于泛型实例 `排序$整32`）时剥离 `$` 前缀
2. **跨 crate 限定调用 moduleFilter 最后段匹配**（[`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp:377)）：
   - 缺陷：`工具库::格式化::版本()` 的 subModule=`工具库::格式化`，但外部依赖模块注册 moduleName=`格式化`（文件主干）→ resolveOverload 精确匹配失败
   - 修复：moduleFilter 末段（`::` 之后）== entryModule 即视为同一模块

### 四、编译器边界规避（非缺陷）

| 边界 | 规避方案 |
|------|---------|
| 容器 `向量<T>` 8 字节槽（结构体/类 >8B 越界） | 馆藏 类用手动动态数组（`整64*` + 容量/数量，stdlib 同款）；向量 由主.cn 局部变量承担 |
| 跨模块泛型类字段（`向量<整64>` 作类字段）构造返回 mangled 名不匹配 | 馆藏 类不组合泛型字段 |
| 子目录模块（馆藏.cn）无法导入父目录 图书 | 图书 类经 主.cn 导入链全局合并，馆藏 类直接引用 |
| crate 隔离同名函数纯名调用歧义（resolveOverload 无 moduleFilter） | 限定调用：`主::版本()`（自导入）+ `工具库::格式化::版本()`（多级路径） |
| 整路径重命名（`导入 路径 作为 别名`）绑定模块级别名（编译器已知边界） | 用花括号项内重命名 `{价格 作为 格式价格}` |
