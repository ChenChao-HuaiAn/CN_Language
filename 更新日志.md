# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 467-a 开发轮：T46 宿主全局 128 位标量静态初始化发射缺失根治〔甲+丙〕·2026-09-20·wt467 隔离）

- **双缺口根治**：①三后端 .data 128 分支由硬编码零占位（dq/.quad 0×2）改发射初值双 quad（低 64 位在前）——42/-5/i128max/−2^127/0x 前缀/u128max/2^64+1 实测全对；②staticCtor 注入 StorePtr 128 位传 IR 名（原恒 ptr=8 字节半槽写·-5 读出 2^64−5）+三后端 StorePtr i128 常量子分支；另 IR ±字面量折叠直存（Rust const-eval 同构·仅 128 位走新折叠=零回归面）。
- **types 单一归属三辅助**：isInt128Type/isInt128Signed/parseInt128InitText（纯 uint64·十进制±/0x/0b/0o/lo:hi 常量池形态·超界拒绝·无符号负数拒绝）——替代三后端四处四连 ‖ 同串判定。
- **测试**：单测 test_int128_static_init 6 例（**1369/1369**）+E2E **483** 矩阵上架（**477=477/0/0**）+m46_01 实弹转正（42 rc=0）+coverage_map 登记+三后端 .s 指纹全对+锚定链零回归（v2 树/stdlib 零 128 静态声明·预检）。
- **边界与登记**：v2 侧同面缺口（静态槽单 dq+8 字节）登记归 v2 欠账族（避让深度机 466 在飞写面）；arm64/linux_x64 运行级归跨机轮；超界字面量编译期拒绝归 T9 方案 D 辖区。
- **事故**：开工时主树误 reset --hard 丢弃他会话 cnsmith_hits 日志未提交增量——看板广播+教训入册（权重 9·主树禁 reset --hard）。

## 功能完善总结（✅ 450-a 开发轮：C20 批4 防线对齐桶 11 例 v2 闭环负测上架·2026-09-20·wt450 隔离）

- **win 主平台性能正主**：`physRegOf` 辅助+五发射方法直写（IntBinary 含 8/16 位扩展分支/Shift/ConstLoad/Load/AddrOf）——**win 冗余 mov 91197→22960 条（-74.8%）**。
- **x86 两地址特有冲突判据**：op2 与 dst 同物理寄存器（死点复用）退回原三段；未分配态 mem→mem 禁止保持 rax 中转=产物逐字节不变。
- **必要搬移定性**：Call ABI 接收/idiv 固定 eax/edx/i128 双槽/浮点=不可消除。
- **验证链全绿**：零警告+单测 1363/1363+E2E **464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线）+asm 宽度 289×3 PASS+反证闭环。
- 门禁/try-build/集成结果：见 plans/025 §1.0（458-a）收工回填。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 476-a D46 家族终态：赋值位构造名裸名拒绝·三段全收＋本会话五轮连续·2026-09-20）

- **服务轮三签**：459-a linux 回签+451 集成后复验备案+467-a linux 回签（准予集成）。
- **470-a D10 双侧销项**（挂等家机 win 回签）：v2 泛型实例体重放语义检查+457 负测+宿主 recheck 诊断窄放行。
- **474-a D46 第一段**（挂等家机 win 回签）：返回语句位裸名拒绝+458 负测。
- **475-a D46 第二段**（已集成 b740de54）：声明初始化位裸名拒绝+59 行实测定案注记。
- **476-a D46 第三段（家族终态）**：赋值位构造名裸名拒绝——语义检查语句.cn 检查赋值语句 单点 +17 行（右值=末子节点·与混合符号/标量转换/逃逸检查既有三处先例同模式）·p10 rc 0→1+反证双态+复合赋值族同路径覆盖；健康面 24 族 7/7+211_v2；**D46 三段全收**=返回位（474）+声明初始化位（475）+赋值位（476）。
- **诚实边界**：实参位裸名未实弹未立案（构造器特判已挡构造场景）；「某些」裸名=可选枚举值语义维持放行。
- **下一步**：470/474 家机回签→集成；队列续认领。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
