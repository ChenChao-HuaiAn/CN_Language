## 功能完善总结

### 新增 DSH 项目规则（AGENTS.md）

**目标**：补齐 DSH 工作流下的项目规则文件。本项目此前仅有 Claude Code 风格的 `.ai-coder/rules/*`（DSH 不加载），本次新增根目录 `AGENTS.md`，由 DSH agent-instructions 插件自动加载。

**变更内容**：
- 新增根目录 `AGENTS.md`（100 行），内容涵盖：
  - 项目定位：CN 语言编译器（C++17，Win x64 / Linux ARM64 双后端），当前处于 **v2 自举重建**阶段（v1 字符串行架构因性能/内存失控推倒重建，v2 结构化 IR + arena + 字符串驻留，对标 rustc/LLVM）
  - 任务开始前强制流程：cn-language-spec 规范查询、plans/lessons/HANDOFF 阅读、新增语法规范须审批
  - 开发纪律：**尽善尽美禁止妥协**（宿主缺陷优先，禁止牺牲性能的规避）、单一职责、中文注释、模块化、Rust 方案优先、报告类产出须批阅、一劳永逸
  - 测试纪律：E2E 先行、**测试文件验收后不得擅自修改**、防虚假验收、缺陷修复标注
  - 构建与验证门禁：scripts/ci.ps1、build.ps1 -Test、单测（1196/1196）、E2E（122 用例）
  - 提交与文档同步：中文提交、更新日志覆盖更新、plans 打勾、HANDOFF、gitcode/develop
  - 错误与诚实报告：lessons.md 记录、未完成不得声称完成
  - 项目现状速查：v2 P6 代码生成 asm 运行验证成功、函数调用 Call 完成、宿主缺陷 H3~H8/方案A/Feature2/move/链式访问全部根治、78/79 内存超限为 v1 旧架构固有（随 v2 完成自然解决）
- 更新 `.gitignore`：追加忽略 `.dsh/`（DSH 会话回滚记录）与 `.mnemon/`（本地 AI 记忆库），避免误提交本机工作数据。

**验证**：纯文档变更，无需编译/测试。
