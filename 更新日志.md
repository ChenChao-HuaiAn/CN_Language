# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段3b 性能主项「转移浅拷贝优化」双侧落地 + v2self 固定点复锚 245230 行（2026-09-10 第六十一轮 深度机 linux-x86_64）

### 一、语言行为升级

容器/类对象的声明位转移从深拷贝（NewObject+拷贝构造）升级为**槽位交接**：目标接管句柄 + 源槽清零；源析构对零句柄走既有空安全跳过 = 恰好一次释放。条件分支两路径均正确（条件假=转移未执行=句柄仍在源槽）。Rust move 运行时语义同构。析构计数实证：转移=1 次（深拷对照=2 次）。

### 二、实施

- 宿主：semantic.hpp transferDeclSources_ + isTransferDecl（public）；semantic_decl.cpp 收尾登记；ir_stmt.cpp genVarDecl 浅交接分支。
- v2：声明位转移中 标志 + 分级 2 放行（豁免模型浅交接零 IR 改动）——v2 容器/类声明位转移自本轮放开。
- E2E 203（析构计数）+203_v2（容器校验和 rc=7）+回归 11/11。

### 三、验证

清场重建零警告+单测 1283/1283+E2E 220 用例 215 过（唯一败=194 既有）；锚定链固定点 245230 行逐字节一致+对拍 29/29+运行级 3/3。

### 四、立案（独立既有缺陷）

类静态成员+析构函数写静态成员=运行期崩溃（tp3 探针：无转移纯构造析构即崩）——随跨机轮呈报三方案。
