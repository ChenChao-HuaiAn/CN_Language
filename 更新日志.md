# CN语言编译器 更新日志

## 功能完善总结

# 第五十三轮（2026-09-10 单位机麒麟 ARM64）：52-a 用户裁决方案A 实施——v2 ARM64 取片 移位+掩码归一（宿主同构）单点根治 + 2^63 锚补全 + v2self 固定点重锚（322112 行）+ 组件对拍 29/29 + 运行级 3/3

## 一、缺陷与裁决

- 第五十二轮 arm64 跨机轮 E2E 首验暴露 52-a：191_v2/193_v2 汇编失败「立即数越界」——装载 2^63 字面量（唯一 bit63 置位位模式）发射 `movk x9, #-32768, lsl #48`，越 GNU as movk 无符号 16 位域。
- 根因：代码生成ARM64.cn 取片() 有符号除法/取模（`v%65536` 对负 v 保号出负片）×常量整数负值补码分支（字面量位模式 读负→`0-值` 回绕仍负）。位模式正确、发射文本越域。
- 三方案呈报后用户裁决**方案A**（取片 位掩码归一·宿主同构·性能第一/安全第二）。

## 二、修复实施（单点）

- `取片()` 重写为 `返回 (值 >> (片位 * 16)) & 65535;`——移位+掩码=位模式语义：
  - 与宿主 emitMovImm `(value>>shift)&0xFFFF` 严格同构；
  - 与第四十三轮 v2 既有范本 位模式十六进制 `(h >> 移) & 15` 同款；
  - 算术右移+掩码对任意整64 位模式正确（掩码截低 16 位，符号填充位不落入片内）；
  - 原 除法/取模 实现系「无移位运算」时代权宜（`>>`/`&` 自第四十三轮 v2 位运算全链落地后可用）。
- git diff 仅 代码生成ARM64.cn 一文件（+6/-8）；取片 全树 grep 零跨文件消费=win/X64L/宿主零影响静态论证。

## 三、验证矩阵（闭环清单逐项兑现）

- v2p53 重建：宿主编译 v2 全树 rc=0（rt53 objs 按当前源码现编 10/10）。
- **191_v2/193_v2 arm64 单跑转绿**；全量 E2E **201 用例 197 过/1 败（78 已知 OOM 4099MB 同族基线）/3 跳**——与深度机第五十一轮 197 过口径对齐（三平台 197 过首次同步）。
- cast 探针补 2^63 锚三形态双侧逐字节一致：4000000000 零扩展读 / 10^18 全精度直拷 / 满界 9223372036854775808 装载+整64(满界) 重解释 **-9223372036854775808**（Rust as 语义）；asm 实证 `movk x9, #32768, lsl #48` 无符号形态（修复前 #-32768 越域行同位置对照）。
- 单测 1251/1251 复跑 PASS。
- **v2self 锚定链重锚**：fix_p.s = **322112 行**（上轮 322167 净减 55=取片 9 行循环体缩为单行 return 的源码确定性体现）→ 链接 cn_self53 → 绑定自检 cn_main=0x4e64f0 ∈ cn_self53.o .text [0x4020bc,0x4ea010) → fix_s.s 322112 行 → **固定点 fix_p≡fix_s 裸逐字节一致**。
- 运行级三用例 3/3（hello 你好，世界 rc=0 / 119 rc=14 / 173 rc=173）；组件对拍 **29/29 全量逐字节归零**。

## 四、诚实边界

- X64L/win 侧：取片 不存在于两后端（grep 实证）零影响静态论证，动态复验随跨机轮例行。
- win 侧第五十一轮方案甲 mov rax 直拷新分支动态首验仍挂（家机轮）。
- 探针与脚本留 target/probe53/ 不入库（chain53.sh / map_check53.py / cmp29_53.sh / runlevel53.sh / cast/ 含 2^63 锚版）。
