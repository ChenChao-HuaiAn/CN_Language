# 更新日志

## 功能完善总结

### 单位机 linux-arm64 复验轮：139/141 负路径闭环 linux 侧对齐 + 六轮家机变更全量复验 + 缺陷A/B ARM64 复验（2026-09-04 单位机 麒麟 ARM64）

承接家机第八/九轮（HANDOFF 下一步②，单位机专属任务）：

- **run_e2e.py linux 侧负路径闭环对齐**（上轮诚实边界收口）：`执行v2闭环Linux` 新增
  `预期退出码 None` 分支（win64 同款三断言）——v2p 运行带 `linux-arm64` 第 2 参数
  （GAS 后端分派；win 默认后端无需参数）、退出码非 0、`target/v2asm.s` 不产出、中止
  诊断行（.expected 固化，平台适配 replace `.asm`→`.s` 与正路径步骤 3 同款）；
  dispatch 删除「待 ARM64 复验轮对齐」占位拦截。**E2E 139/141 单跑双绿**——
  linux 侧负路径闭环（语义错误即中止纪律）首次实证。
- **全量 E2E linux-arm64 复验**：六轮家机变更（缺陷②v2四件+缺陷A/B 根治+裸节点双重
  处理、限定名内置+前缀紧绑定、`.` 统一+两静默常量0 根治、三元运算符+静态自增、
  真/假 字面量+`*p op=`+整8/16 强转+v2p 中止、字符串比较拒绝）首次 ARM64 全量复验：
  **143 用例 139 过 / 1 败 / 3 跳零回归**——跳过 3=62_ffi/69_memory/79（平台限制
  结构性跳过，与历史基线一致）；唯一败 78_chain_build 运行期 SIGSEGV(-11)（单跑复证
  同形态；src/ 本轮零改动、上轮单位机基线同为 1 败(78)——v1 链内存失控的 ARM64 表现，
  win 侧同族 0xC0000005/4101MB 已知遗留，随 v2 重建自然解决）。
- **缺陷 A/B ARM64 侧复验**（上轮单位机发现、家机第四轮 IR 层根治后的 ARM64 复验）：
  A=`p.y--` 成员后缀自减写回——134 用例退出码断言（q.y==19）+ 探针输出 2 双证；
  B=局部数组无初始化 + `数组[2]+=数组[1]`——无既有用例覆盖，/tmp 探针补验输出 5
  （补零确定性，不再垃圾值），验后即删。**双缺陷 ARM64 侧根治生效确认**。
- 验证：清场重建（gcc ARM64 Debug）零错误；单测 **1216/1216**（与家机基线一致）；
  E2E 全量见上。
