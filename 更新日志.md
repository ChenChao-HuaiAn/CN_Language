# CN语言编译器 更新日志

## 功能完善总结

# 第四十四轮（2026-09-09 家机 win-x64）：跨机轮 win 侧动态收口——第四十三轮四项改动 win 实物全绿+收缩符号黄金值三方锚定+路径归一双侧复验+E2E 176~182 win 首验（纯验证轮零源码改动）

按 HANDOFF 挂账1 拉取 32b84a9+009368d（第四十二/四十三轮 单位机麒麟 ARM64）执行本机（家机 win-x64）跨机轮动态收口，第四十三轮诚实边界三项（win 后端收缩符号黄金值/路径归一/176~182 win 首验）全部当轮兑现。

## 一、全量门禁（清场重建）

- 构建：CMake 清场重建，MSVC /W4 /WX **零警告**（日志 0 处 warning）。
- 单测：**1251/1251 全过**。
- 全量 E2E win-x64：**184 用例 182 过 / 2 败 / 0 跳**。败面=78（4116MB）/79（4112MB）已知 OOM 家族，与第四十一轮基线（4117/4101MB）、第四十轮（4108/4130MB）同族，**零回归**。

## 二、E2E 176~182 win 侧首验 7/7 PASS

- **180_v2_位运算形态（rc=176 闭环）**：第四十三轮在单位机 ARM64 上静态编写的 **v2 win 后端位运算发射**（and/or/xor 单指令+shl/sar 移位量经 cl）首次经 ml64 实物验证通过——跨机轮拦截面当轮归零。
- 181_v2_构造字面量形态（rc=42 全字段赋值语义）/182_v2_构造穷举负测（None 通道+诊断行）win 闭环。
- 178_移位掩码语义（宿主六形态）/179_构造字面量穷举负测（宿主缺字段拒绝）win 首验。
- 30 个 v2 闭环用例全绿=v2p（第四十三轮大改后 v2 全树：位运算五算子+构造字面量+路径归一+FNV 族/位模式族/+256 族 35 处原生位运算重写）win 后端编译 rc=0 全链实证。

## 三、收缩符号黄金值复验（FNV 原生位运算重写等价性 win 实物闭环）

54 汉字函数名探针（mangled 原串 ?+324hex+@@Y=328 字符>MASM 247 上限，触发收缩路径）：

1. **宿主↔v2p 产物对拍**：收缩符号逐字节一致 `$L328HAEAEDCD4D2F8988F`（PROC/ENDP/call 三处引用一致）。
2. **python 独立复算**：按代码内常量（seed 1469598103934665603/prime 1099511628211）对重构原串复算=AEAEDCD4D2F8988F，三方一致。注意标准 FNV basis=14695981039346656037（尾多一个 7），本项目为自定义 seed 变体——首跑复算用教科书常量得假阴性，教训入 lessons（独立复算必须用被验系统代码内常量，权重 6）。
3. 探针产物 ml64 汇编 rc=0（无 A2043）。
4. 等价性传递链闭合：第四十一轮（v2 纯算术模拟↔宿主位运算，黄金锚 EFF9F723240DB1BE）→第四十三轮（重写版↔模拟版 3000 组 LCG 逐位等价）→本轮（重写版↔宿主 win 实物直接一致）。

## 四、路径归一复验（第四十一轮挂账③根治动态兑现）

| 探针形态 | 结果 |
|---|---|
| v2p 反斜杠相对路径 `CN语言编译器v2\主.cn` | rc=0（第四十一轮此场景 183 错双载歧义） |
| v2p 反斜杠绝对路径 | rc=0 |
| v2p 正斜杠对照 | rc=0 无回归 |
| 宿主 cn.exe 反斜杠绝对路径（cn_main.cpp #ifdef _WIN32 归一） | rc=0 |
| 宿主 cn.exe 反斜杠相对路径 | rc=0 |
| v2p 反斜杠产物（4.5MB asm）ml64 | rc=0 |

## 五、诚实边界与文档

- X64L 侧 176~182 首验/组件 30/30 对拍/v2self 固定点复现=深度机轮（下一跨机轮）。
- 文档：plans/014 状态表第四十四轮行+更新日志+HANDOFF 重写+lessons 一条（独立复算必须用被验系统代码内常量·权重 6）。
- 本轮零编译器源码改动（纯验证轮）；探针与脚本留 target/probe44/（不入库）。
