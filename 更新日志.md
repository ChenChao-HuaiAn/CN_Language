# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十五轮 115-a：D1 行数整改第六波——`semantic_internal.hpp` 共享头建立（语义层去重）**（2026-09-13，家机 win-x64；基线 efa1ced）：
> ①**去重**：11 个语义层 helper（isComparisonOp 族/isPointerType/isArrayType/arrayTotalSize/canonicalType/
> isFuncPtrTypeStr/funcPtrReturn/funcPtrParams）在 semantic/semantic_call/semantic_decl/semantic_expr/
> semantic_stmt **5 文件重复 55 处（~600 行）** → 新建 `semantic_internal.hpp`（119 行，**inline 单一定义**；
> 查证各副本实现**逐字一致**）；5 文件删副本 + include（调用点零改动）。
> ②**行数**：semantic.cpp **2102→1995** / semantic_call.cpp **1093→988（<1000 出列）** / semantic_decl.cpp
> 877→767 / semantic_expr.cpp **1890→1777** / semantic_stmt.cpp 927→826——**为 semantic 系列按族拆分铺路**
> （拆出的族 include 头即可用 helper）。
> ③**三重等价验证**：删除段≡头核心**逐行多重集**（5 文件 × 60 行全等）+ 产物 asm 五样本**逐字节一致** +
> 全量门禁（零警告 + 单测 **1313/1313** + E2E **290/292** + 锚定链固定点 **406097 连续第四次不变** +
> 对拍 **34/34** + 运行级 **3/3**）。
> ④教训（lessons 第一百一十五轮段）：扫描正则须覆盖全部函数定义形态（本轮首扫漏 `[[maybe_unused]]` 前缀与
> 模板返回类型——9→11 修正）/ **「删除段≡新位置内容」校验法**（去重搬迁类硬校验）/ 头文件化匿名 ns helper 的
> 等价模式（inline + 全项目同名扫描前置；Rust rustc 内部共享模块对照）。
> ⑤**本会话（111-a~115-a）累计**：111-a 门禁跃变（C14/C15 销项 + 新缺陷 221 双侧根治）+ D1 四波拆分
> （112-a~115-a：ir_expr 2798→734 / ir_oop 1381→508 / ir_oop_call 1155→763 / 语义层去重 -536）+ 锚定链
> 固定点 406097 全程一致。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
