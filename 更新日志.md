## 功能完善总结

**时间**: 2026-08-18（性能基线对比 Debug 审查完成，plans/004 D-1 审查闭环）

## 性能基线对比审查（plans/004 D-1）✅

对 Code 子任务实现的性能基线对比（`scripts/bench_self_host.py` + `target/bench/性能基线报告.md`）
进行 Debug 审查，逐项验证测量真实性、口径对齐、可复跑性、集成回归、文档一致性、结论合理性。

### 一、审查结论（逐项 ✅）

| 审查项 | 结论 |
|--------|------|
| M2/M5 真实执行组件链编译（删旧产物防蒙混） | ✅ 运行前删除旧 `*_链.asm`，存在=本次真的写了 |
| M5 运行 cn_compiler_self.exe（非误用 C++ 版） | ✅ map 确认 5 个 chain.obj 贡献符号 + cn_main 来自入口 obj |
| M6 逐字节比对真实执行 | ✅ 两次落盘产物存在且被 read_bytes 比对 |
| 3 次取中位数实现正确 | ✅ 各测 3 次排序取中间值（非平均/最大） |
| 计时点包裹正确操作范围 | ✅ perf_counter 包裹 exe 运行，无无关操作 |
| M2/M5 同口径 | ✅ 均运行 exe 落盘 5 个 `*_链.asm` |
| 比值计算 | ✅ M5/M2 = 727.932/697.714 ≈ 1.04x |
| 与 004 数据对比口径标注 | ✅ 标注了 30 行小样本 vs 3604 行组件链差异 |
| 脚本可复跑 | ✅ 复跑成功，报告重新生成，M6 一致 |
| ML64/LINK 动态探测 | ✅ 找不到时清晰报错退出非零码 |
| 产物统一 target/bench/ | ✅ 规则 19 合规 |
| 无残留依赖 | ✅ 无 Code 子任务手动中间文件依赖 |
| E2E 79 回归 | ✅ 通过 |
| run_e2e.py 未被意外修改 | ✅ d4fe8c6 未改动 run_e2e.py |
| target/bench/ 产物齐全 | ✅ 报告 + 两次落盘产物 |
| 文档一致性 | ✅ 004 D-1 打勾 + 执行情况四 + 更新日志 + HANDOFF + lessons |
| .gitignore __pycache__/ | ✅ 已有 |
| 结论合理性 | ✅ 达到「性能达 C -O0」里程碑站得住（远优于 10min 理想目标） |

### 二、发现并修复的问题

1. **报告未展示 M2/M5 的 3 次样本**（中位数正确但不可审计）→ 脚本新增
   `测量结果["M2样本"]`/`测量结果["M5样本"]` 存储，报告测量点表格展示样本数组
2. **6 万行推演未标注线性外推假设**（任务明确要求）→ 报告第五节新增「推演假设」块：
   按「编译耗时与源码行数成正比」线性外推（`耗时 × 60000/3604`），并说明哈希映射
   O(1) 查找增长影响小、量级判断不受影响
3. **与 004 推演差异未解释**（004 推演 80s~5min vs 本轮 11s）→ 报告说明：004 基于
   30 行小样本外推（39ms/330ms），本轮基于 3604 行实测组件链（更接近真实规模），
   推演值更低更可信

### 三、复验结果

- `python scripts/bench_self_host.py --cn target/Release/cn.exe` 复跑成功：
  M2 中位数 669.261ms（样本 [669.075, 669.261, 3714.411]）、
  M5 中位数 692.191ms（样本 [688.06, 692.191, 3432.052]）、比值 ≈1.03x、M6 逐字节一致
- `python tests/e2e/run_e2e.py --cn target/Release/cn.exe --filter 79` 通过
- 报告重新生成：`target/bench/性能基线报告.md`（含样本 + 推演假设标注）

### 四、文档同步

- `plans/004-自举前置条件检查.md`：执行情况四新增「审查确认」小节
- `HANDOFF.md`：记录本轮审查结论与修复
- `lessons.md`：记录审查发现（样本可审计性 / 推演假设标注）
- `scripts/bench_self_host.py`：样本存储 + 推演假设标注

### 五、Git 提交与推送

- 中文提交（标题+描述），推送 gitcode develop
