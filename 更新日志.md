## 功能完善总结

**时间**: 2026-08-13

**范围**: 基础语言缺陷完善A（i128 完整支持 + 结构体按值传参/返回 + 结构体整体赋值）、Task 2.8 字符串系统完善B（13 个运行时字符串 API）、优化器增强完善C（代数简化/CSE/复写传播/跨块DCE/全局值传播 + -O1/-O2/-O3 级别区分）、完善功能集成验证（14_integration2 综合用例 + 6 个跨组件 BUG 修复）、缺陷完善 Debug 全面审查（9 个边界用例 + 5 个 BUG 修复）。最终状态：**单元测试 665/665、E2E 14/14、56 组合 -O0/-O1/-O2/-O3 输出一致、编译零警告（MSVC /W4 /WX）**。

---

### 一、基础语言缺陷完善A（2026-08-13）

#### 1. i128 完整支持（规格书10.5）

- **运行时辅助函数** `src/runtime/i128_api.cpp`：全部 128 位运算（`__cn_add/sub/mul/div/mod_i128`、`__cn_div/mod_u128`、`__cn_cmp_i128/u128`、转换、打印），MSVC `_umul128` 4 次乘加 + 128 次迭代二进制长除法，指针式 API（`a[0]=低64位/a[1]=高64位`）
- **IR/codegen**：双寄存器模型（id=高64位/id+1=低64位）、add/adc、sub/sbb 进位/借位链、`emitInt128MulDivMod` 调辅助函数、`emitInt128Compare`、打印行展开走 `__cn_print_i128/u128`、i128→i64 截断 Cast
- **语义层**：`isI128/textExceedsInt64/splitI128Text`；字面量越界检查（整128 上限 2^127-1、正128 上限 2^128-1，含无后缀超 int64 自动提升场景）

#### 2. 结构体按值传参/返回（规格书7.4，Win x64 ABI）

- 传参：`structParamIndexes` + `emitParamSetup` rep movsb 从指针拷贝到参数槽（精确字节数）
- 返回：Win x64 隐藏返回指针方案——`IRFunction.structReturn/structReturnSize`；调用方分配 `__retbufN` 返回缓冲区（64 字节）作隐藏参数（rcx）；被调方 prologue `mov r12, rcx` 保存（非易失寄存器）；epilogue 按 `structReturnSize` **精确字节数**拷贝到缓冲区

#### 3. 结构体整体赋值

- `CopyStruct` IR 指令（extra=字节数）+ codegen `rep movsb` 内联拷贝；含数组字段结构体整体拷贝

#### 4. 完善A 修复的 4 个 BUG

| # | 问题 | 修复 |
|---|------|------|
| 1 | epilogue structReturn 分支漏 ret → 执行流落入下一函数无限递归 → 0xC00000FD | structReturn 分支补 `mov rsp,rbp/pop rbp/ret` |
| 2 | 64 字节硬编码拷贝越界：16 字节班级结构体被写 64 字节破坏相邻栈变量 | `structReturnSize` 精确字节拷贝 |
| 3 | StorePtr 目标类型 i64 覆盖：`出.分数[1] = ...`（IndexExpr 对象为结构体数组字段 MemberExpr）targetType 推导遗漏 → 8 字节写入覆盖相邻数组元素 | 补 MemberExpr 分支 |
| 4 | i128 字面量越界未报错（2^127 被接受） | 语义层加 整128/正128 上限检查 |

---

### 二、Task 2.8 字符串系统完善B（2026-08-13）

- **规格书核对**：10.1 仅定义 5 个字符串 API（长度/比较/连接/复制/查找）；比较运算符仅整型与浮点变体，未定义字符串变体 → 不实现；按"常见字符串库补充并在规格书标注"新增 13 个 API
- **新增 13 个运行时字符串 API**（`src/runtime/string_api.cpp`）：
  - 子串 `__cn_str_sub`(字符串, 整64, 整64)→字符串（字节偏移，UTF-8 按字节）
  - 字典序 `__cn_str_cmp`(字符串, 字符串)→整64（<0/0/>0）
  - 大写 `__cn_str_upper`/小写 `__cn_str_lower`（ASCII，非 ASCII 原样）
  - 前缀 `__cn_str_starts_with`/后缀 `__cn_str_ends_with`/包含 `__cn_str_contains`→布尔
  - 修剪 `__cn_str_trim`（空格/制表/换行/回车）
  - 反转 `__cn_str_reverse`（UTF-8 安全：按字符逆序不拆字节）
  - 从整数 `__cn_str_from_int`/从浮点 `__cn_str_from_float`/从字符 `__cn_str_from_char`
  - 释放 `__cn_str_free`（封装 cn_free，nullptr 安全）
  - 内存语义：动态分配结果（子串/大写/小写/修剪/反转/从整数/从浮点/从字符）调用方负责释放
- **值语义决策**：字符串变量为指针，`字符串 t = s` 共享同一指针（与 C 一致，不深拷贝）；显式深拷贝用 `字符串复制`
- **格式化函数**：sprintf 风格变参 ABI 复杂暂缓（打印行多参数已覆盖格式化输出需求），后续标准库阶段
- **修改**：`semantic.cpp`（registerBuiltins 13 新 API）、`ir.cpp`（中文名→符号映射 + resultType 映射；字符串释放 空类型用 emit）、`runtime.hpp`、`tests/unit/CMakeLists.txt`、规格书 10.1/10.3 标注、阶段2文档 Task 2.8 条目
- **测试**：`test_string_api_extra.cpp`（runtime 29）/`test_string_extra.cpp`（semantic 10）/`test_ir_string_extra.cpp`（ir 9）共 48+3 个；`tests/e2e/12_string_extra/字符串完善.cn`（27 行期望）

---

### 三、优化器增强完善C（2026-08-13）

- **新增 5 个优化 Pass**（`src/cn_compiler/opt/`）：
  - **代数简化** `algebraic_simplify.hpp/.cpp`：整型恒等变换（x+0/0+x/x-0/x*1/1*x/x*0/x/1/x<<0/x>>0/x|0/x^0/x&-1→x 或 0；x-x/x^x→0；x&&假→假；x||真→真）；浮点/i128 保守跳过；常量结果原地替换、寄存器结果函数级替换链
  - **复写传播** `copy_propagation.hpp/.cpp`：块内 Store→Load 转发（唯一内部名精确匹配）；多槽/StorePtr/Call 保守清空
  - **块内 CSE** `cse.hpp/.cpp`：纯运算"操作码+类型+操作数身份"哈希复用；Load 复用（无写内存时）；浮点 CSE 仅 -O2 以上
  - **跨块 DCE 增强** `cross_block_dce.hpp/.cpp`：常量条件跳转折叠（真/假→无条件跳转）+ 入口可达性分析 + 不可达块整块删除（label 字符串互引无需修正索引）
  - **全局值传播** `global_value.hpp/.cpp`：函数级线性扫描常量 Store→Load 安全子集
- **共享工具** `opt_common.hpp`：isPureArith/constCanPropagate/normalizeIntText/replaceUses（寄存器无条件替换 + 常量白名单）
- **-O1/-O2/-O3 级别区分**（`runOptLevel`，driver 接入）：-O1=折叠+DCE+代数简化+复写传播；-O2=+CSE（含浮点）+跨块DCE；-O3=+全局值传播；`cn_main` -O2/-O3 不再映射 -O1，`--opt 0/1/2/3`
- **测试**：6 个单元测试（`test_algebraic` 20/`test_cse` 11/`test_copy_prop` 8/`test_cross_block_dce` 7/`test_global_value` 8/`test_opt_levels` 6，+56）；`tests/e2e/13_opt2/优化增强.cn` + `.expected`

---

### 四、完善功能集成验证（2026-08-13）

- **新增综合用例** `tests/e2e/14_integration2/员工档案.cn`：员工档案管理系统——48B 大结构体（i128 年薪 + 字符串姓名 + 整32[3] 数组）按值传参/返回、整体赋值（含修改隔离）、结构体指针数组按年薪冒泡排序（CopyStruct 交换 48B 元素）、i128 大数运算（乘/除/取余/负数）、字符串 13 API 组合、小结构体寄存器传参、优化 Pass 验证
- **修复 6 个跨组件 BUG**：

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| 1 | `x64_codegen.cpp` | i128 返回 + 结构体按值参数 ABI 错位：paramOffset 未涵盖 i128 返回 + prologue 未保存 rcx→r12 → 0xC0000005 | paramOffset 涵盖 i128/u128 返回；prologue `mov r12, rcx`；epilogue 用 r12 恢复缓冲区 |
| 2 | `x64_instructions.cpp` | LoadPtr/StorePtr 漏 i128 双槽分支（结构体 i128 字段读写只 8 字节） | LoadPtr/StorePtr 加 i128/u128 双槽分支（低64↔regSlot(id+1)、高64↔regSlot(id)） |
| 3 | `x64_instructions.cpp` | emitCast 无"整数→i128"分支（i32→i128 高64 垃圾） | 加整数→i128 分支（符号扩展低64 + sar 63 高64） |
| 4 | `x64_codegen.cpp` | i128 参数 emitParamSetup 只存 8 字节 | i128 参数（寄存器/栈）从指针 rep movsb 16 字节 |
| 5 | `ir.cpp` | 指针下标元素成员 `名单[j].年薪` objSrcType 推导漏指针 | visitMemberExpr IndexExpr 分支补 `types::isPointer`（pointeeOf） |
| 6 | `ir.cpp` | 结构体下标赋值 `名单[j] = 名单[j+1]` 走 StorePtr 只存 8 字节 | 非标识符左值赋值加结构体检测 → CopyStruct（按 typeSizeOf） |

- **其他**：`semantic.cpp` 补 `funcParamTypesOf` 实现（声明未定义 → 链接错误）；`ir.cpp` i128 实参仅当目标参数非 i128 时才截断为 i64
- **新增单元测试** `tests/unit/codegen/test_x64_i128_struct.cpp`（+4）：LoadPtrI128DoubleSlot/StorePtrI128DoubleSlot/I128ParamCopies16Bytes/I128ReturnSavesR12

---

### 五、缺陷完善 Debug 全面审查（2026-08-13）

- **审查方式**：按生产环境模拟运行——`target/debug_audit/` 9 个边界用例（p1_i128_boundary/p2_struct_boundary/p3_string_boundary/p4_opt_boundary/p5_error_path/p6_opt_boundary/p7_error_path/p8_cli_opt/p9_combo）+ 全部 E2E 01~14 + 单测 660/660
- **修复 5 个 BUG**（综合教训权重 27.3）：

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| 1 | `x64_instructions.cpp` | **i128→浮64 Cast 无分支**：`整128 * 1.0` 输出 0.000000（emitCast 整→浮只处理 i64/u64，i128 落默认 32 位 mov 读垃圾） | 加 i128/u128→f64 分支（调 `__cn_i128_to_f64/__cn_u128_to_f64` 双槽地址 + xmm0） |
| 2 | `x64_codegen.cpp` | **i128 混合参数 + i128 返回 ABI 错位崩溃**（0xC0000005）：`emitParamSetup` 用 `if (i < 4)` 判定参数位未加 paramOffset（i128 返回占 rcx 后第4参数实际位4=栈）；`parameterRegister(4)` 返回 `[rsp+40]`（rsp 锚定，被调方 sub frameSize 后读自己栈帧垃圾） | 参数位判定改 `i + paramOffset < 4`（浮点独立 i<4）；parameterRegister index≥4 改 `[rbp+48+(index-4)*8]`（rbp 锚定） |
| 3 | `ir.cpp` | **链式结构体赋值 `链3 = 链2 = 链1` 只拷贝 8 字节**：外层赋值右值为 AssignmentExpr 时 valueSrcType 推导失败 → 不走 CopyStruct 落简单 Store | 结构体赋值检测补 AssignmentExpr 右值（内层返回值即源地址） |
| 4 | `x64_instructions.cpp` | **i1→i64 Cast 无分支**：字符串后缀/包含打印 4393751543809 垃圾（i1 落默认 32 位 mov 读槽高32位垃圾） | emitCast 加 i1→i64/u64/i32 分支（mov eax 零扩展） |
| 5 | `ir.cpp` | **i128 数组元素 stride 8**：`整128[3] 大数; 大数[1]` 读到 大数[0] 高64槽，求和 92233720368547758086000000000000000000 错误 | `visitIndexExpr/ptrElemStride/数组初始化 elemStride` 补 `types::isI128 → 16` |

- **新增单元测试** `tests/unit/codegen/test_audit_regressions.cpp`（+5）
- **修复文件**：`src/cn_compiler/codegen/x64/x64_codegen.cpp`、`src/cn_compiler/codegen/x64/x64_instructions.cpp`、`src/cn_compiler/ir/ir.cpp`、`tests/unit/CMakeLists.txt`、`tests/unit/codegen/test_audit_regressions.cpp`

---

### 六、最终验证结果

- **全量构建**：零警告（MSVC /W4 /WX）
- **单元测试**：**665/665**（原 660 + Debug 审查新增 5）
- **E2E**：**14/14**（01~13 无回归 + 14_integration2 员工档案系统）
- **优化正确性**：**56 组合（01~14 × -O0/-O1/-O2/-O3）输出完全一致，FAILURES=0**（`target/opt_verify/verify_all.py`，规格书12.4）
- **冒烟**：`14_integration2/员工档案.cn` -O2 编译运行输出与 `.expected` 一致
- **全 CLI 命令**：`cn token/ast/check/ir/run/build` 可用

**结论**：缺陷完善全部工作（i128/结构体值/字符串 13 API/优化器增强 + 集成验证 + Debug 审查）已完成并通过全部验证，阶段二核心语言全部完成，可提交推送。
