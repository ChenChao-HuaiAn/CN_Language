# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 457-a 开发轮：D8/F1-28 第二步=ARM64 操作数装载直读·2026-09-20·分支 任务/家机-457-arm64操作数装载直读·worktree=wt457·基点=451 分支接力）

- **性能第一准绳正主续轮**：已分配操作数直用作源（`operandSourceReg` 辅助·AArch64 三地址源任意寄存器·LLVM 同向），免「mov x9, x21」装载中转。**装载中转 37531→2125 条（-94.3%）**；与 451 结果写入面合并后 ARM64 冗余 mov 总量 **88637→2125（-97.6%）**。
- **覆盖面**：IntBinary/DivMod（含除零检查 cmp 直读）/Shift（8/16 位掩码面必要中转保留）/Compare/Not/Store/StorePtr/PtrLoadStore 地址基址全程/Cast sxtw——九发射面源直读；统计阶梯捕获四处遗漏当场补齐。
- **形态纪律**：32 位运算源文本统一 w 形态（x21→w21 同寄存器低 32 位）；装载 fallback 保持 x 形态（符号扩展语义不变）；未分配路径 fallback 原样=产物逐字节不变。
- **验证链全绿**：零警告+单测 1363/1363+E2E 464=462/2/0（零新增红）+asm 宽度 289×3 PASS+反证闭环（撤改复现 9 条→恢复 0 条）。
- **诚实边界**：2125 条残余=oop/dispatch 面（写集外）+Call ABI/错误路径装载（必要）；x64 同族面=453 集成后压后；arm64 运行级 bench=跨机复验轮。
- 门禁/try-build/集成结果：见 plans/025 §1.0（457-a）收工回填。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 服务轮第三波：九分支串联链回签全签＋446 UAF 当场根治＋435 链收编集成 bbded82·2026-09-19）

- **九分支串联链回签（429~439 全签·@家机）**：九个积压 try-build 分支按集成序 merge 为本地验证链（diff 清单链尾核验零缺失·产物层零冲突）→链尾全新门禁：**E2E 502=500/2/2 零代码新增红**+单测 1363=1362/1+自举链双 PASS——证据=合并结果门禁。3 红全归因：①459 主用例=438 词法自赋值 UAF（**当场根治**）②459_v2=期望过期（438 防线增强·迁移建议已附）③210_v2 两例=并行互踩假红（329-D12 隔离键欠账·集成后自愈）；437 单测断言过期（用户已批迁移·patch 已附）。
- **446 修复分支（机制级 §8.4·头 8194399）**：词法分析.cn 分词字符串扩展 多行非原始路径 `内容 = 内容;` 自赋值→v2 字符串赋值无自赋值守卫→UAF 悬垂垃圾（数据段 .byte 铁证+垃圾漂移+对比矩阵三层证据）——**修复=删 1 行**·反证=红绿翻转确由该行驱动·446 态全量 464=462/0/2+自举链双 PASS。B9 预判=v2 全库「X = X;」扫描待实施。
- **435 链收编集成（develop bbded82）**：26 提交 WIP 链逐提交 rebase 不可行（integrate.py 自动 abort）→merge 三方并编（冲突仅看板 2 块）→合并结果门禁全绿（单测 1363/1363+E2E 464=462/0/2+自举链双 PASS）→ff push（与家机 447 认领 CAS 竞争获胜）。家机回签终版（65df8a4·准予集成）先于收编到达。
- **下一步**：家机 447 集成轮待命回签；本机下轮 448=撤守卫残留 111/68（D42 波 3 续）；单位机周一补验≥bbded82+329-D12 自集成。

## 功能完善总结（✅ 服务轮第二波：堆积链收编 331/337/338/345~353→develop f9e91ae7·2026-09-19）

- **444 修法实施（双源类型解析）**：生成下标左值地址 字段路径 类型来源改为双源——布局字段类型表（查字段类型ID·星号已同步）优先＋函数级权威表兜底——**撤守卫态 111_H6_容器拷贝构造 编译零诊断 rc=0**（此前运行 SEGV）。
- **撤守卫编译巡检扩展**：111/68/203/123/390/456 全部 COMPILE-OK（此前 52/87 → 关键面全通）。
- **门禁（本链历史最优）**：全量 E2E --jobs 8 **464=462/0/2 零失败**＋78/79 自举链双 PASS＋strict 零孤儿＋matrix 83/83＋守卫恢复态 111/68/123/390 全 rc=0。
- **诚实边界**：①撤守卫态 111 运行仍需完整验证（编译已通但运行级 LNK 后 SEGV 根因=接收者求值链容器/值结构体分派混淆的完整修复待下轮）；②390 撤守卫运行残留同族；③integrate 等 win 回签。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
