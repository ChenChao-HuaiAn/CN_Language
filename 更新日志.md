## 功能完善总结

### 阶段零 Task 0.1：CMake项目骨架 + CLI框架 + 诊断系统

本次提交完成了CN语言编译器工程搭建的第一步：

#### 新增文件
- `CMakeLists.txt`：顶层构建配置（C++17、target/输出目录、Google Test集成、Debug/Release支持、零警告编译选项）
- `src/cn_main.cpp`：编译器命令行入口（build/compile/run/check/ir/ast/token命令、--target/--opt/--output/--verbose选项、帮助与版本信息、错误处理）
- `src/cn_compiler/common/source_location.hpp`：源码位置类（文件名/行/列，toString格式化输出，比较运算符）
- `src/cn_compiler/common/diagnostics.hpp`：诊断系统头文件（DiagnosticLevel枚举、Diagnostic结构体、Diagnostics类）
- `src/cn_compiler/common/diagnostics.cpp`：诊断系统实现（report/hasErrors/getErrorCount/format/clear）
- `tests/unit/CMakeLists.txt`：单元测试构建配置
- `tests/unit/common/test_source_location.cpp`：SourceLocation测试（4个用例）
- `tests/unit/common/test_diagnostics.cpp`：Diagnostics测试（6个用例）

#### 技术要点
- 使用MSVC `/utf-8` 编译选项解决中文注释的代码页解析问题（C4819警告）
- CMake变量名不能使用中文（避免编码解析错误）
- 编译产物统一输出到 `target/` 目录

#### 验证结果
- 编译通过（零警告）：`cn.exe`、`cn_unit_tests.exe`
- 单元测试全部通过：10个测试（SourceLocation 4个 + Diagnostics 6个）
- CLI功能验证通过：`--version`、`--help`、未知命令报错、缺少文件报错、选项解析、无效选项报错
