# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 449-a 开发轮：本机多开基建（worktree 任务池+gate_lock+sccache+--parallel+看板多任务行）·2026-09-20·用户三裁决批准·分支 任务/家机-449-本机并行基建·worktree=wt449 自举试点）

- **worktree 任务池制度化**（`scripts/wt.py`·AGENTS.md §8.8）：每并行任务=1 worktree+1 任务分支+1 会话，根治同机互踩（429-a/431-a 根因=共享工作树与 target/）；主树职责收窄（集成+窄通道，禁开任务分支）；树位=主仓库同级（距 Documents 两级·gtest `../../third-party` 依赖）；create/list/remove 全链实测。容器方案否决（win 门禁死绑 MSVC+MASM）。
- **gate_lock.py 全量门禁串行锁**：主树 `target/gate.lock` 跨树共享目录锁（陈锁 4h 心跳接管），挂 ci.ps1（acquire/finally-release）+integrate.py linux 分支（run 包装）；同机至多一个全量门禁在飞（防 78/79 式互抢）。双进程实测 A 持锁 8s/B 排队 8.4s 切入。
- **构建提速**：build.ps1 补 `--parallel`；**sccache 0.17.0**（winget·Documents/sccache-cache·5GB）——**VS 生成器不支持 CMAKE_CXX_COMPILER_LAUNCHER（平台事实）→ Ninja 开发树**（/Z7 替代 /Zi 防 C1041 并发 PDB 竞争；勿覆盖 CMAKE_CXX_FLAGS 防丢 /EHsc）。实测（28 核）：VS 冷 127s｜Ninja+sccache 冷 23s｜同树热重建 10.7s｜跨树首建 ~21s（48% 命中：共享路径第三方命中/树内源码 miss）。
- **看板多任务行**：check_handoff 每机小节 1~4 行逐行校验（本机多开=每任务一行）；**AGENTS.md §8.8 本机多开规约**（28 核/128GB 口径：活跃任务 ≤3+门禁 ≤1·每树 ~1.7GB）。
- **develop 基线红修复**：check_own_matrix.py 漏注册（426-a 建脚本）→ check_registry.json 补登（449 首跑门禁拦截发现·机制级当场修）。
- 门禁/try-build/集成结果：见 plans/025 §1.0（449-a）收工回填。

## 深度机 linux-x86_64

## 功能完善总结（448-c 112_v2 期望迁移+dbg 残留全清+456 回签·2026-09-20）

- **448-c（家机 447 呈报红处置·§8.6 归 448 轮）**：112_v2 期望迁移实跑固化（459_v2 同款）——435 链 stdlib 源码级加载后「释放」已声明=正确演进，负测语义保持（ir2 错误码6 仍拒绝）；dbg 残留四件全清（[dbg收文]+TEMP435b2/438c/441b 三块 IR dump·448-b WIP 载荷债务兑现·grep 零依赖零误伤）。反证双态+全量门禁（465=463/0/2 零新增红）。
- **456 回签（窄通道·服务轮）**：家机 456-D1拆分 linux 全量门禁全绿（零警告+单测 1363+E2E 464=462/0/2+自举链双 PASS+v2p 等价 asm 对照反证）→准予集成。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
