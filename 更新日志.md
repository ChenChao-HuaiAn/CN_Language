# 更新日志

## 功能完善总结

### ③ 第五波：限定名内置 IO::/文件:: 全链 + 真/假/无 字面量 + 二元链操作数位一元前缀（E2E 133 全链绿，2026-09-03 家机 win-x64）

**载体动因**（plans/014 序列下一缺口）：v2 组件 IR签名.cn/主.cn 的 I/O 命脉 =
限定名内置 IO::打印到错误 + 文件::打开文件/读取文件行/写入文件/关闭文件/文件大小/
文件存在（宿主 ir_call.cpp:217~235 符号映射 + semantic.cpp:1226~1257 限定名注册权威）。
v2 语法层 `::` 此前与 `./->` 同走成员分支——IO 崩成「未声明标识符: IO」+ 成员方法
标记误路由（「容器方法未收录」运行时错误 6）。

**四层落地**：
1. **语法层 `::` 独立分支**：仅支持「标识符::标识符(」调用形态——池尾对象标识符
   原地改写为合成限定名（局部副本 + 池.设置 回写——元素(i).字段=x 写非宿主支持
   形态，124/126 实证读=地址视图/写=设置），随后 `(` 分支自然形成 节点_调用
   （被调=限定标识符，与容器构造合成名同构，零新节点类型）；非调用 `::` 形态
   中断后缀链（错误路径由语义诊断/解析中断诊断可见化）。
2. **真/假/无 字面量**（词法关键字表已有、解析层零分支——谓词含 真/假 的源码必
   截断）：复用 整数字面量 节点（真=1/假=0/无=0），语义/IR 字面量路径零改动；
   无 与 指针/字符串 == 比较走指针语义（宿主「指针只能与指针或空指针比较」实测锚定）。
3. **IR 双表扩 11 限定名**：内置函数符号（IO::4+文件::7 → __cn_print_err/__cn_file_*
   等）+ 内置函数返回类型ID（宿主 regIoFn/regFileFn 对齐；空类型* 按指针单槽记
   整64；空类型返回跳过登记=void 语义）；语义层限定名经返回表自动放行（零改动）。
4. **【连带揪出·当场根治】二元链操作数位一元前缀缺失**（133 首跑 [parse] 截断于
   `追加行 == 无 || !字符串比较(...)`，探针矩阵 p1~p4 交叉二分定位；词法分析.cn
   冒烟 3 处截断同族）：解析层_子 操作数位参数化——二元循环右操作数位传 真（运算符
   已消费=操作数起点，前缀判定无二元歧义）、链入口传 假（v2 二元链=「入口在运算符
   上、左操作数已解析」空左操作数设计）。**首版在子(5)入口无条件判定踩二次坑**：
   链入口的二元 -/* 被误当一元前缀——减法/乘法全灭（123/129/130 全量门禁回归实证；
   133 侥幸绿——其算术全是 +），消融实验（禁用分支重建对照）+节点数同值+探针矩阵
   三步锁凶后修正。lessons 10.5 权重成文。

**v2 主.cn 自检扩容**：源码8 限定名负测自检（未注册限定名 `不存在::打印(1)` 须拒
——白名单命中制防 :: 合成静默放行任意 名::名( 形态）+ IO::打印到错误 正向锚点两行。

**验证**：宿主 sanity 先行（133 源 cn build+运行 退出码 0，17 字节写入/逐行内容比较/
EOF 判空/文件大小/存在性负路径/追加模式全对）；**E2E 133 六步闭环 PASS**（expected
四行全命中）；v2 产物人眼审计三锚点齐全；**词法分析.cn 冒烟大幅推进**：3 处 [parse]
截断→1 处（最后截断点=三元运算符 `? :`（行 236，规范 01b 有、v2 未实现——下一缺口
立项）），语义 0 错误，asm 7357 行完整产出且 ml64 汇编通过（30KB obj——v2p 完整
编译自家词法器的最强信号）；全量门禁见 HANDOFF。
