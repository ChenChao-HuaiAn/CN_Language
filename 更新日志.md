# CN语言编译器 更新日志

## 功能完善总结

# 第五十二轮（2026-09-10 单位机麒麟 ARM64）：跨机轮 arm64 动态收口——全量门禁复验 + E2E 189~193 双通道 arm64 首验 + v2self 固定点复锚（322167 行）+ 组件对拍 29/29 + 运行级 3/3 + 方案甲 arm64 双锚实证；新暴露 52-a v2 ARM64 取片负片缺陷立案待裁

## 一、全量门禁与第四十九~五十一轮改动的 arm64 动态收口

- 按 HANDOFF 挂账二.1 拉取 f478ab0（第四十七~五十一轮）执行跨机轮 arm64 动态收口，纯验证轮零源码改动。
- **全量门禁**：清场重建 GCC 零警告 rc=0 + 单测 **1251/1251** + 全量 E2E linux-arm64 **201 用例 195 过/3 败/3 跳**。
  - 跳 = 62/69/79 既有平台限制；败 = 78 已知 OOM 4157MB（与第四十~四十六轮基线 4108~4149MB 同族零回归）+ **191_v2/193_v2 两红 = 新暴露 52-a 缺陷**（见第三节）。
  - **189/189_v2/190/190_v2/192/192_v2/191/193 双通道其余全过** = 第四十九~五十一轮 ARM64 后端新发射（udiv/lsr/lo·hi·ls·hs 无符号族、ldr w 零扩展读、uxtw、正32 存储截断、方案甲直拷）动态验证通过。

## 二、v2self 锚定链（arm64 口径复锚）

- v2p52（宿主编译 v2 全树 rc=0；rt52 objs 按当前源码现编 10/10，旗标对齐 run_e2e.py：`g++ -c -std=c++17 -fno-exceptions -fno-rtti -DCNRT_LINUX_MAIN -Isrc`）编译 v2 自身 → fix_p.s **322167 行 = arm64 新锚定**（第四十三轮 302433 + 三轮净增 19734 = 联合体/推断声明/无符号类型面/混合符号检查+转换分派源码确定性体现）。
- as rc=0（**v2 全树自身源码不含 2^63 形态 = 52-a 结构性验证盲区佐证**）+ g++ 链接 cn_self52（cn_self52.o 在前 + v2p52.o 借链容器符号 + muldefs）→ **绑定自检**：cn_main=0x4e6574 ∈ cn_self52.o .text [0x4020bc,0x4ea094)。
- cn_self52 编译 v2 自身 → fix_s.s（322167 行）→ **固定点 fix_p ≡ fix_s 裸逐字节一致**。
- 运行级三用例 3/3：hello rc=0（你好，世界；入口实名 hello.cn 非 主.cn——早期用例踩坑）/ 119 多文件 rc=14 / 173 目录包根撞名 rc=173。
- 组件对拍 **29/29 全量逐字节归零**（6 包根 + 23 成员，find 实数核对）。

## 三、52-a 缺陷立案（遵缺陷裁决纪律，征询已发起无答复，不单方面落实）

- **现象**：191_v2/193_v2 arm64 首验 as 汇编失败「立即数越界」——`movk x9, #-32768, lsl #48`（GNU as movk 域 = 无符号 16 位 0~65535）。
- **根因**：代码生成ARM64.cn **取片() 用有符号除法/取模**（`v%65536` 对负 v 保号出负片）× 常量整数 **负值补码分支**——2^63 是唯一 bit63 置位位模式，字面量位模式 读整64 为负，`0-值` 回绕仍为 -2^63 → 最高片 = -32768。**位模式正确、发射文本越域**。
- **影响面**：全树唯一爆点 = 装载 0x8000000000000000 位模式（取片 全部消费者 = 装载立即数，调用点穷尽审计：常量整数/槽地址/字段地址，后两者值域恒小）；X64L/win 后端 mov 全范围立即数免疫 = 深度机三轮全绿掩盖原因；宿主 emitMovImm `uint64_t` 无符号提取天然免疫（宿主 191/193 全过对照）；v2 全树自身源码不含该形态 = 结构性验证盲区（lessons 权重10 预防①「边界形态探针」再演，新条目权重 7 入库）。
- **三方案呈报（性能第一/安全第二·Rust 对齐）**：
  - **A（推荐）**：取片 返回前 位与 65535 位掩码归一——宿主 `(value>>shift)&0xFFFF` 严格同构；单点修一函数、三消费者自动全覆盖；零新增指令零性能代价；v2 位运算第四十三轮已全链 `&` 可直接用。
  - **B**：装载立即数 片文本十六进制化 `#0x8000`——win 后端常量归一/位模式十六进制 共用层同族；同样单点；产物文本形态与宿主十进制不同（specs/08 批注允许）。
  - **C**：负值补码分支特判 -2^63——改动面最小但特判式修复 = 半实现陷阱家族，不推荐。
- **修复后闭环清单**：191_v2/193_v2 arm64 转绿重验 + 2^63 重解释锚（-9223372036854775808）探针补 + 锚定链重锚 + 组件对拍复跑；取片 为 ARM64 层独有函数 = win/X64L/宿主零影响。

## 四、方案甲 arm64 动态实证

- 探针（target/probe52/cast，值域刻意避开 2^63 防与 52-a 根因混淆）：整64(正32 变量)=4000000000 零扩展读（movsxd 旧缺陷形态出 -294967296 可鉴别）+ 整64(正64 变量)=10^18 全精度直拷（低32截断旧缺陷出 3567587330 可鉴别）——宿主/v2p52 双侧输出逐字节一致。
- asm 实证：uxtw x9,w9 零扩展读分支在场 / 全文件 sxtw=0 / 正64 ldr x9 64 位直通 = 与第五十一轮静态论证一致。

## 五、诚实边界

- 52-a 未修复：191_v2/193_v2 arm64 红待用户裁决后根治 + 重验 + 2^63 锚补 + 锚定链重锚。
- win 侧第五十一轮方案甲 mov rax 直拷新分支动态首验仍挂（家机轮）。
- 探针与脚本留 target/probe52/ 不入库（chain52.sh / map_check52.py / cmp29_52.sh / runlevel52.sh / cast/）。
