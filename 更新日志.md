# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十九轮 119-a：D1 行数整改第十波（codegen 面收口）——三后端装配文件按族拆分**（2026-09-13，家机 win-x64；基线 5c169dd）：
> ①**拆分**（纯重构零行为变更）：`x64_codegen.cpp` 1087→**897**（拆出 `x64_codegen_data.cpp` 211：escapeString/
> hexBytesString/uint64HexText/floatBitsHex/registerFloatConstant/emitDataSection）/`linux_x64_codegen.cpp` 1130→**823**
> （拆出 `linux_x64_codegen_emit.cpp` 329：常量+I128 常量+栈访存+loadOperandToV+isStructParam+数据段）/
> `arm64_codegen.cpp` 1073→**765**（拆出 `arm64_codegen_emit.cpp` 329：同族）；三文件均无匿名 ns/static（零依赖障碍）；
> CMakeLists 挂载 3 文件。
> ②**新方法论（重要）：stash 对照法**——三后端装配文件改动**无法被 win 五样本覆盖** → `git stash` 旧代码构建宿主
> → 生成 **win/linux-x86_64/linux-arm64 三目标 ×2 样本**产物基线（win 项与既有基线一致=流程自检）→ `stash pop`
> 构建新宿主 → 同样生成 → **6 项全部逐字节一致** ✓（~6 分钟）。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 连续第八次不变** +
> 对拍 **34/34** + 运行级 **3/3**。
> ④教训：**win 门禁绿灯不覆盖非 win 后端路径**——凡改「只影响非本机后端」的宿主文件，验证必须显式补目标平台产物对照。
>
> **本会话（111-a~119-a）累计**：111-a 门禁跃变 + **D1 九波**（ir_expr 2798→734 / ir_oop 1381→508 / ir_oop_call 1155→763 /
> 语义层去重 -536 / semantic_expr 1778→654 / semantic.cpp 1996→692 / x64_instructions 1812→921 /
> 三后端装配 1087→897·1130→823·1073→765）——**codegen 面全部达标，宿主仅剩 ast.hpp**；
> 锚定链固定点 406097 全程一致（八次复验）+ 产物等价（win 五样本 + 119-a 三后端 6 项）+ 全量零回归。
>
> ⑤诚实边界：D1 剩余（`ast.hpp` 1032 + v2 侧 6 文件 + 函数级 19 超百行）登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
