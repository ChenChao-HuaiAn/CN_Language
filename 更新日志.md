# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 338-a 开发轮 T56=v2 词法浮点 f/F 后缀识别·2026-09-18·基线 e24819e）

- **词法 分词数字**：浮点形态后缀循环扩 f/F（消费入文本·整数形态不扩面维持现状）。
- **生成端 浮点文本净**：剥尾扩 f/F——净文本 REAL8/.double 直通（三后端共用设施一处改全平台生效）。
- **语义分型面零改动**：329-a 已按文本尾 f 分型（推导源类型文本/隐式转换合法）——本轮两端口径闭合。
- **验收铁证**：w11/w31 对拍转绿（`浮32 f = 1.5f;` 编译过）+asm 数据段 `REAL8 1.5`/`REAL8 0.5`（与宿主同源 IEEE754 确定性）。
- **诚实边界**：值恒浮64 编码+转换链落浮32（精度=double→float 舍入与宿主一致·词法令牌无类型字段）；整数 `5f` 形态不扩面（宿主行为未测）。
- **验证**：v2 树零错误编译+d38_scan 10 拒/7 缺保持+crosscheck 全一致+单测 1353/1353+全量 E2E（数字见提交信息）。

**门禁**：快速门禁+全量门禁（写集触 v2 树）+双文档门禁；integrate.py 集成（协议v2）。

## 深度机 linux-x86_64 节

## 功能完善总结（⚡ 334-a 轻量轮 A12 里程碑标签锚点·2026-09-18·基线 e11c346）

- **命名规范落定**：`里程碑/<YYYY-MM-DD>-<事件简称>`；**4 个 tags 推两远程**（v2 重建立项 75f3c2b/固定点 361684 14197dd/协议v1 47f2c69/协议v2 4d41d34·ls-remote 核验含 peeled）；tags=独立 refs 不碰 develop；历史里程碑增量补打机制随规范。
- **无 src/tests 写集** → 快速门禁即集成。

## 功能完善总结（🧪 324-a M3 采样验证轮·2026-09-18，基线 0eaff2b4）

- **采样双轨全绿**：正向 1000（seed=1789687508）差分 2000/2000 全一致零命中；负向 400（seed=1789687530）全拒绝·零崩溃·诊断含行号（连续第七轮）。
- **回归轨对账 129 件**：9 命中全旧类零新类别——**两处漂移（修复未兑现·x64l 运行级）**：★T44（316-a 修复仅落 win 后端 `codegen/x64/`·linux_x64 未同步→i128 三元族仍双级 SIGSEGV·实弹 m44_01 未消+扩面 7 形态）；★T27（x64l -O0 整32 INT_MIN/-1 SIGFPE=`cmp r9,-1` 64 位比较 vs `mov r9d` 32 位装载→特判永不命中；窄整负值→128 宽化缺 movsxd→m27_02~05 O0 错值）。
- **T44/T45/T46 实弹对账**：T45 全转正 ✓（值正确·p314a t45_* 9 形态全健康）；T44 未消；T46 仍读 0（未修复·待裁决·差分轨盲区）。
- 根因铁证=asm+gdb（`[rbp]` 槽偏移 0 / `idiv r9d` 陷阱点）；修复方向=win 同款改动同步 linux_x64 后端四点（emitCopy i128 双半/槽补登记/`cmp r9d,-1`/宽化 movsxd）。

**门禁**：check_handoff/check_progress_sync 绿；双推 gitcode+github。

## 单位机 ARM64 节

## 功能完善总结

### 332-a 修复轮：看板先行机械强制（用户令）+ 看板表结构防冲突（2026-09-18，基线 044423b·分支 任务/单位机-332-看板强制）

1. **用户令落地**（2026-09-18「必须强制 AI 实际工作前先修改看板填写本机任务，完成后顺便销账」）：AGENTS §8.1 新增「**看板先行**」强制条——开工**第一动作**＝直推看板本机行到 develop（`git fetch`→定分支名→推板→才开分支动手）；**收工即销账**＝integrate.py 集成成功后立即清行并直推。§8.5 直推窄通道改「**强制直推、即改即推**」：看板行/通告段/try-build 请求三类信息写进任务分支＝他机不可见＝等于没写（当日实测根因）。
2. **check_handoff.py 新增「开工自检」**：当前分支为 `任务/*` 时看板必含该分支名，否则报错拦截「先动手后填板」——正例放行/反例拦截双向实证（含新结构下旧校验「🏃 行缺分支名」有效性）。
3. **看板表结构变更**：单表三行 → **每机独立小节**（`### <机名>`+4 列单行表）——实测单表三行相邻结构下两机同时更新各自行 rebase 必冲突（当日两次：本机 329-a 直推 × 深度机 331-a 连续直推）；小节间隔 4+ 行使三方合并友好；查看板() 同步适配。
4. **看板直推实战**（044423b·窄通道）：本机 329-a 在飞行直推 develop——发现三机看板行此前全在各自分支内、远端 develop 三行全空（「三机不知彼此在干什么」根因）；期间两次 push 被拒（深度机同日连续直推）→按协议 CAS 重试 rebase 解冲突复推。
5. **实测记录**：家机 329-a（D38批2）/深度机 331-a（T11·已发 try-build 请求）/单位机 329-a（D12+货舱延伸）三机同时在飞；跨机轮次号撞号实锤（家机与本机同为 329-a）——机制缺陷已在 HANDOFF 登记待裁。
6. 文档同步：plans/025 §3.20 六要素+HANDOFF/更新日志本机节+lessons。
