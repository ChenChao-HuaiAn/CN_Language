# CN语言编译器 更新日志

## 功能完善总结

# 第四十二轮（2026-09-09 单位机 麒麟 ARM64）：跨机轮 arm64 动态收口——第四十/四十一轮挂账全量门禁复验 + 字面量位模式 arm64 探针锚定 + v2self 字节级固定点复现 + 探针转正 E2E 176/177

按第四十/四十一轮 HANDOFF 挂账1（跨机轮三平台半程）拉取 c9945ca 在本机（单位机 麒麟 ARM64）执行动态收口。本轮为纯验证轮零宿主/v2 源码改动，唯一代码面=验证资产转正（新增 E2E 176/177 + run_e2e.py 注册一行）。

## 一、全量门禁（挂账1 本体：本机动态收口）

- **清场重建**（rm target/build 全新 CMake + GCC -Wall -Wextra -Werror）零警告 rc=0——第四十轮 intern_api.cpp 编译器内建平台分流的 GCC 路径（`__builtin_return_address` 原样保留）在本机编译运行零回归；第四十一轮宿主改动（x64_instructions.cpp 为 win 独立目录）对 arm64 零影响的静态论证兑现；
- **单测 1251/1251 全过**；
- **全量 E2E linux-arm64：177 用例 173 过/1 败/3 跳**——败=78_chain_build（已知 OOM 家族 4149MB 被内存保护终止，与第二十九轮基线 4126MB 同形态，v1 旧架构固有零回归）；跳=62/69/79 平台限制（与历史基线一致）；v2 闭环 24+ 用例（含 119~158/164~174）arm64 动态全绿。

## 二、字面量位模式 arm64 动态复验（第四十一轮静态论证→动态实锤）

按 lessons 权重10 预防①（「凡编译器支持的语法必须有自身源码之外的探针锚定后端发射」）构造八形态探针：`0xFF`/`0b1010`/`0o777`/`42L`/`7U`/`36028797018963968`(2^55)/`0x7FFFFFFFFFFFFFFF`(2^63−1)/`-255`。

- **宿主 vs v2p(arm64) 产物运行输出逐字节一致**（rc 全 0，八值全对）；
- v2 产物 asm 实证：`movz x9, #255`（0xFF 正确解码——修复前 arm64 本地 解析数值 将其**静默解析为 0** 的潜伏缺陷确认已根治）、2^55 与 2^63−1 经 movz+movk 全 64 位装载、负常量走 `neg` 补码路径；
- **X64L 后端静态旁证**（深度机动态复验前）：`movabs r10, 36028797018963968`、`movabs r10, 9223372036854775807`、`mov r10, 255`、`neg r10`——位模式解码三后端单点生效。

## 三、v2self arm64 锚定复现（第四十一轮 v2 共用层改动后）

- v2p 重建：宿主编译 v2 全树（主.cn 49437 节点→arm64）rc=0；
- cn_self 一代链接成功（cn_self.o 在前 + v2p.o `-Wl,-z,muldefs` 供容器方法符号——rustc 预编译 std 模式，nm 自检 cn_main 唯一绑定 cn_self.o）；
- **字节级固定点 fix_p ≡ fix_s 复现**：v2p 编译 v2 自身源码（fix_p，294888 行）与 cn_self 编译同一源码（fix_s）**裸逐字节一致**——第三十五轮（深度机）/第二十九轮（arm64）锚定在第四十一轮 字面量位模式/超长收缩 改动后于 arm64 侧保持，v2self 自举封闭性完好；
- 组件单文件入口冒烟 5/5 rc=0（词法分析/解析基础/IR容器/代码生成共用/货舱解析——第三十八轮 包上下文恢复 路径 arm64 实证；旁注：解析基础与 IR容器 两入口 asm 行数相同而函数发射顺序随入口模块序不同，各自自洽，非缺陷）。

## 四、探针转正 E2E 176/177（验证资产入库，防「一次性探针用后即弃」）

- **176_字面量进制形态**（宿主侧）：八形态 打印 逐行 expected（255/10/511/42/7/36028797018963968/9223372036854775807/-255）+ 程序内八断言（全过返回 0）双保险；
- **177_v2_字面量进制形态**（v2 闭环注册，run_e2e.py `v2闭环用例们` 增量一行）：同源码载体，空 expected=rc 门控（对齐 E2E 与编译器解耦方向），v2 四层（词法原样存文本→共用层 字面量位模式→后端发射→运行数值）全链锚定；
- 两用例单跑双绿；三平台通用（win 侧随家机下轮门禁首验，X64L 随深度机轮）。

## 五、诚实边界与挂账

- 78 OOM（v1 旧架构字符串行 IR 固有，随 v2 重建完成自然解决）；79 在 arm64 平台跳过（既有基线形态）；
- 组件 30/30 全量逐字节对拍、X64L 动态运行复验=深度机跨机轮（本轮已做 5 入口抽样冒烟+固定点强锚定）；win 侧 176/177 首验随家机轮；
- **呈报三项维持第四十一轮原状待用户裁决**：①v2 位运算/移位全链路缺失（建议独立特性轮）；②v2 结构体构造字面量语法缺口（建议随语法覆盖轮）；③v2 主.cn 路径文本匹配反斜杠脆弱（建议路径身份归一 `/`）。

文档：plans/014 状态表第四十二轮行 + HANDOFF 重写。
