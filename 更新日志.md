# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（530-a·D1 滚动=生成类对象局部拆分·2026-09-21）

- **D1 行数滚动（目标变更）**：原目标解析类声明 185 实测=链式结构不宜机械切片（定性=专用设计
  轮）→改拆 `生成类对象局部` 147 行→主函数 93 行+`发射类拷贝构造初始化`（不安全修饰随迁）——
  纯机械重构零行为变更。
- **等价铁证**：同宿主双 v2p 重建对拍——五产 asm 探针 md5 逐字节一致（含 t_cpy 拷贝构造=2dd4131a
  直接锚定）；Ninja 零警告构建+check_fn_length 出列；集成时全量门禁+develop 现态补跑兜底。
- **同会话前半程（522/529）**：编码对象调用ARM64 拆分（eeac863c·全量门禁补跑全绿）+分词拆分
  （1005ac33·3 假红串行全 PASS）+三链集成让位协调+僵尸锁×3 清除。

## 深度机 linux-x86_64 节

## 功能完善总结（🏃 537-a 解析类声明部分拆分 185→162·挂等呈报·2026-09-21）

- **部分拆分**：继承跳过+布局收口两边界块出列（子函数带 是否公开/池/类键基/类体起点参数）——主函数 162 行未出列（方法成员五形态嵌套链+字段成员块=下一轮）。
- **等价铁证**：md5 双对拍（探针 3982d9f7+全树改前文本 111825 节点 9eb05e11 双一致）。
- **★456 runner 环境红之谜挂等呈报**：456-6 rc=14 单跑稳定复现+清缓存重建仍红 vs **手动同源编译运行 rc=27 正确**（产物仅 1 行 IR 常量差·非 537 引入）——挂等不进 develop·待 runner 环境定位。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
