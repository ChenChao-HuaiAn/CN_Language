## 功能完善总结

### 本次完成：基础语言缺陷完善（i128 完整支持 + 结构体按值传参/返回 + 结构体整体赋值）

#### 一、i128 完整支持（规格书10.5）

- **运行时辅助函数**：新建 [`src/runtime/i128_api.cpp`](src/runtime/i128_api.cpp)，实现全部 128 位运算：
  - 加减乘除取余（有符号/无符号）：`__cn_add_i128`、`__cn_sub_i128`、`__cn_mul_i128/u128`（4 次 `_umul128` 乘加实现 128×128）、`__cn_div_i128/u128`、`__cn_mod_i128/u128`（128 次迭代二进制长除法）
  - 比较/转换/打印：`__cn_cmp_i128/u128`、`__cn_i128_to_f64`、`__cn_u128_to_f64`、`__cn_f64_to_i128`、`__cn_print_i128/u128`、`printLineI128/U128`
  - 指针式 API（`a[0]=低64位`、`a[1]=高64位`，与编译器 i128 双槽变量布局一致）
- **IR/codegen 支持**：
  - `emitResult` 对 i128/u128 结果分配 2 个连续虚拟寄存器（高64位 id、低64位 id+1，小端连续16字节）
  - `registerVarSlots`：i128 变量按 2 槽
  - `visitIntegerLiteral`：无后缀超 int64 自动提升 i128；`splitI128Text` 拆分 "LO:HI" 十六进制
  - `emitInt128Binary`：add/adc、sub/sbb 全128位进位/借位链（MASM 内存操作数显式 qword ptr）
  - `emitInt128MulDivMod`：调辅助函数（常量落 `[rsp+32]/[rsp+48]` 临时区 + lea 寄存器地址）
  - `emitInt128Compare`：调 `__cn_cmp_i128/u128`
  - 打印行展开对 i128 走 `__cn_print_i128/u128`
  - `emitCast`：i128→i64 截断取低64位槽
- **语义层**：`isI128`、`textExceedsInt64`、`splitI128Text`；i128/正128 字面量越界检查（整128 上限 2^127-1、正128 上限 2^128-1）

#### 二、结构体按值传参/返回（规格书7.4，Win x64 ABI）

- **按值传参**：`structParamIndexes` 标记结构体参数；`emitParamSetup` 从指针 `rep movsb` 拷贝到参数槽（按 varSlots×8 字节，精确大小）
- **按值返回**（Win x64 隐藏返回指针方案）：
  - `IRFunction.structReturn/structReturnSize`；调用方分配 `__retbufN` 返回缓冲区（64 字节）作为隐藏参数（rcx）
  - 被调方 `emitPrologue` 用非易失寄存器 r12 保存隐藏返回指针（rcx 会被函数体破坏）
  - `emitEpilogue` 按 `structReturnSize` **精确字节数**拷贝到返回缓冲区（修复 64 字节硬编码越界写破坏相邻栈变量）
  - `emitParamSetup` 真实参数从 index 1 起（paramOffset）
- **结构体整体赋值**：新增 `CopyStruct` IR 指令（operand[0]=dst、operand[1]=src、extra=字节数），codegen 用 `rep movsb` 内联拷贝；含数组字段结构体整体拷贝

#### 三、新增/修改文件

| 文件 | 说明 |
|------|------|
| [`src/runtime/i128_api.cpp`](src/runtime/i128_api.cpp) | 新建：128 位运算辅助函数（~345 行） |
| [`src/runtime/runtime.hpp`](src/runtime/runtime.hpp) | i128 辅助函数声明 |
| [`src/cn_compiler/ir/ir.hpp`](src/cn_compiler/ir/ir.hpp) | CopyStruct 指令；structReturn/structReturnSize/structParamIndexes |
| [`src/cn_compiler/ir/ir.cpp`](src/cn_compiler/ir/ir.cpp) | i128 双寄存器；结构体参数/返回/整体赋值 IR |
| [`src/cn_compiler/codegen/x64/x64_codegen.cpp/.hpp`](src/cn_compiler/codegen/x64/x64_codegen.cpp) | i128 运算/结构体 ABI 代码生成 |
| [`src/cn_compiler/codegen/x64/x64_instructions.cpp`](src/cn_compiler/codegen/x64/x64_instructions.cpp) | emitInt128Binary/MulDivMod/Compare；CopyStruct |
| [`src/cn_compiler/semantic/semantic.cpp`](src/cn_compiler/semantic/semantic.cpp) | i128 字面量越界检查；结构体初始化/成员类型推导 |
| [`src/cn_compiler/semantic/type_system.cpp/.hpp`](src/cn_compiler/semantic/type_system.cpp) | isI128/textExceedsInt64/splitI128Text |
| [`src/cn_main.cpp`](src/cn_main.cpp) | 链接 i128_api.obj |
| [`CMakeLists.txt`](CMakeLists.txt) | cn_runtime 加入 i128_api.cpp |

#### 四、测试

- **E2E**：新增 [`tests/e2e/10_struct_value/结构体值.cn`](tests/e2e/10_struct_value/结构体值.cn)（结构体传参/返回/整体赋值，41 行输出）与 [`tests/e2e/11_i128/128位.cn`](tests/e2e/11_i128/128位.cn)（i128 全功能，34 行输出）
- **单元测试**：新增 5 个测试文件（`test_i128_api`、`test_i128_semantic`、`test_ir_i128`、`test_ir_struct_value`、`test_x64_struct_value`、`test_x64_i128`），**549/549 全部通过**（原 508 + 新增 41）
- **全量 E2E 11/11 通过**（01~09 无回归 + 10/11 通过）
- **-O0/-O1 输出一致性**验证通过（10/11 两个新用例）

#### 五、修复的 BUG（Debug 审查）

1. **epilogue 漏 ret**：structReturn 分支直接 return 漏掉 `mov rsp,rbp/pop rbp/ret` → 执行流落入下一函数 PROC 无限递归 → 0xC00000FD 栈溢出
2. **64 字节硬编码拷贝越界**：`mov rcx, 64` 硬编码，16 字节班级结构体被写 64 字节越界 48 字节破坏相邻栈变量 → 改为 `structReturnSize` 精确字节数
3. **StorePtr 目标类型 i64 覆盖**：`出.分数[1] = ...`（对象为结构体数组字段 MemberExpr）targetType 推导遗漏 → 8 字节写入覆盖相邻数组元素 → 补 MemberExpr 分支（元素类型 i32）
4. **i128 字面量越界未报错**：语义层加 整128/正128 上限检查

#### 六、验收状态

- 构建：MSVC /W4 /WX **零警告**
- 单元测试：**549/549** 通过
- E2E：**11/11** 通过（01~09 回归无破坏 + 10/11 新用例）
- -O0/-O1：输出一致
