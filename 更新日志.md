## 功能完善总结

### 2026-08-15 阶段6第6步：全链路集成串联（Task 6.11 综合命令行工具）

**本次修改内容：**

1. **新增 `tests/e2e/38_tool/` 全链路集成 E2E（学生成绩统计工具）**：
   - `主.cn`：综合命令行工具源码——覆盖全部 9 个标准库模块（系统/文件/字符串扩展/容器/映射集合/算法/数学/IO/时间）
   - `成绩数据.txt`：测试数据（5 行有效 + 1 行非法格式）
   - `主.args`：命令行参数注入（数据文件 + 输出文件路径）
   - `主.expected`：精确输出比对（整数 %lld、浮点 %f 6 位）
   - `容器.cn`/`映射集合.cn`/`算法.cn`/`字符串扩展.cn`：stdlib 模块副本（35_string_ext 同模式）

2. **工具功能全链路**：
   - 命令行参数校验（`系统.参数个数` ≥3 → 用法提示 → 非零退出）
   - 打开数据文件（`文件.打开文件` 模式1读）→ 逐行读取 → 分割/解析（`分割` + `解析.转整数`，非法行跳过计数）
   - 存储：`向量<整64>` 总分 + `集合<整64>` 去重 + `映射<整64,整64>` 分布
   - 统计：`排序整64`/`最大元素`/`计数`/`二分查找整64`/`线性查找` + `数学.平方根` 标准差
   - 时间戳断言（`时间.当前时间戳`/`格式化时间`，内部断言输出 真/假）
   - 报告写入（模式2写截断 + `整数转字符串`）→ 读回验证（模式1读 + 打印）

3. **新发现编译器缺陷（⚠️ 已规避，记录待 Debug 子任务修复）**：
   - **大栈帧无 `__chkstk` 栈探测崩溃**：codegen 栈帧（寄存器槽区 + 变量槽区）超 Windows 栈 guard 页（约 8KB）时 `sub rsp, N` 越过未提交栈页 → 0xC0000005。规避：每个函数拆到 <8KB（少打印、少局部变量、容器单函数仅 1 个实例）。根治方向：栈帧 >4KB 发射 `call __chkstk`

4. **已知边界（复用前序经验）**：
   - 跨模块重复声明 `错误码` 枚举冲突 → 用例目录副本改 `导入 容器.错误码` 共享
   - `结果` 关键字不可作变量名 → 用 `统计`；模块级变量/泛型类参数不支持 → 数组指针参数传递
   - 容器按 8 字节槽存储（结构体入向量越界）→ 向量存标量；姓名深拷贝防悬垂

**验证结果：**
- 单元测试 **1048/1048** 通过
- E2E **40/40** 通过（39 前序 + 38_tool 综合工具）
- 编译 **0 警告**（MSVC /W4 /WX）
