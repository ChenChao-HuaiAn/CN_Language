# 功能完善总结

> 83-a 链表/队列 槽分配与回收（空闲链）（2026-09-12 第八十三轮，深度机 linux-x86_64）：
> 81-a 登记的既有缺陷「stdlib 链表 `追加` 槽复用」销项——用户令持续多轮推进。
> **空闲链复用 + 高水位计数**（O(1)，Rust/C++ list 节点复用同款）根治「元素丢失」级缺陷。
> （上一轮 82-a/82-b v2 帧尾共享 retbuf 根治与赋值位接收见提交 5331de6 / 56b0fe4）

## 一、缺陷实证（探针 P22，宿主基线）

| 形态 | 修复前 | 期望 |
|---|---|---|
| `链表<整64>`：追加 11/22/33 + 删除头部 + 追加 44 | 33 被 44 覆盖（`next` 自环） | [22,33,44] |
| `队列<整64>`：入队 11/22/33 + 出队 + 入队 44 | 出队序 **22/44/44（33 丢失）** | 22/33/44 |
| `链表<字符串>`：追加 2 + 删头 + 追加 1 | 头部=[丙串]（应 [乙串]，内容错）+ **残留 1** | 头部=[乙串]、残留 0 |

**根因**：`新索引 = 元素数量` 假设「槽 0..元素数量-1 全被占用」——删除头部/尾部后该不变量
破坏（被摘槽仍占槽号但不在计数内）→ 新槽号与在链槽冲突。

## 二、方案与实施（甲：空闲链；性能第一）

- **stdlib/容器.cn（链表 + 队列）**：布局 **+2 字段**（`空闲头`=空闲链首槽 / `已用槽数`=
  高水位，**追加在末尾→老字段偏移不变**）；`自身.取空闲槽()`（空闲链优先 O(1) 复用，否则
  高水位递增）；`自身.归还槽(槽)`（删除头部/尾部/出队 把被摘槽挂链——**在单槽释放注入
  之后**）；扩容条件改 `已用槽数 >= 值容量`；`清空` 重置两字段（槽从 0 重分配）；
  构造/析构初始化与重置。
- **v2（IR/IR容器.cn）**：`容器对象大小` 链表/队列 **48 → 64**（布局同步，手工契约）。
- **CN 语法约定**：类内方法互调须 `自身.` 前缀（原写法被语义层判「未声明的标识符」）。

乙（已用槽计数不复用）因内存膨胀落选；丙（删除时压缩）因链式需修前驱 O(n) 落选。

## 三、防线与反证实证

- **E2E 237_双编译对照链表槽复用**（移植纪律 9 **第九个**对照用例）：八形态（链表追加/
  队列入队 + 槽复用、链/队<字符串> 内容 + 残留归零、删除尾部复用、头部追加复用、循环
  20 万轮、清空后复用）折校验和 **255**，两侧一致；供给=链/队<整64/字符串>。
- **反证实证**：注入「`取空闲槽` 退回旧逻辑（返回 `元素数量`）」→ **237 宿主退出码
  255→240**（①②③④ 位丢失）；恢复后 255 ✓。

## 四、验证链（本轮终值）

清场重建**零警告** + 单测 **1306/1306** + E2E **264 用例 260 过 0 失败**/4 跳（+237；
其余 259 过=零回归，**stdlib 布局变更零连带**）+ 锚定链固定点 **320424 行**（与 82-b 后
一致=v2 树源码未变，纯 stdlib 改动对自举产物透明）两代逐字节一致 + 运行级 3/3 + 对拍
**34/34**（DIFF=0）+ 释放路径两侧对照 ✓。

## 五、登记（下一轮候选）

- **B35 字段容器接收者**（残留 2）——涉「字段赋值语义 move vs 深拷」**语言级裁决，须先向
  用户呈报**（阻塞项）；
- v2 结构体字面量作实参（语法缺口）；物化性能优化（延迟物化 + 活跃性分析，可选）；
- 未初始化槽 preFree 隐患（观察项）；跨机轮：arm64 复验与 E2E 234/235/236/237。
