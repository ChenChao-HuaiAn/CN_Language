## 功能完善总结

### 本次提交（2026-08-14）：阶段6「标准库」第1步——条件编译（Task 6.6）+ 数学库（Task 6.3）

**一、条件编译与预处理（Task 6.6）**

- 新增 `src/cn_compiler/lexer/preprocessor.hpp/cpp`：条件编译预处理器，支持 `#定义 宏名` / `#如果定义(宏名)`（或 `#如果定义 宏名`）/ `#否则` / `#结束如果` 指令
- 设计：行级裁剪（不活跃行替换为空白保留换行，词法/语法/IR 行号与原始源码一致）；条件栈支持任意嵌套；父层不活跃时内层 `#定义` 不生效（C 语义）
- 宏来源：命令行 `-D 宏名`（可多次，CliOptions→DriverOptions→Lexer 全链路透传）+ 源码内 `#定义`
- 修复：指令行前导空白缩进、中文字节长度常量比较、CRLF 行尾宏名带 `\r` 问题
- 单测 `tests/unit/lexer/test_preprocessor.cpp`（13 用例）+ E2E `tests/e2e/24_preprocessor/`

**二、数学库（Task 6.3）**

- 新增 `src/runtime/math_api.cpp`：8 个数学运行时函数（英文 API）`__cn_sqrt/pow/sin/cos/tan/fabs/ceil/floor`，封装 `<cmath>`，在 `runtime.hpp` 声明
- `semantic.cpp` registerBuiltins 注册 8 个中文限定名内置函数（`数学.平方根` 等）；visitCallExpr 模块限定重写特判（用户模块公开函数优先，无同名时保留限定名走内置路径）
- `ir.cpp` 名称映射（8 处中文名 → `__cn_*`）+ 结果类型映射（f64）
- 新建 `stdlib/数学.cn`：公开 8 个中文函数（平方根/幂/正弦/余弦/正切/绝对值/向上取整/向下取整）
- `cn_main.cpp`：compileRuntime 源文件数组 + linkExe .obj 列表加入 `math_api`（x64/arm64 同步）
- 单测 `tests/unit/runtime/test_math_api.cpp`（9 用例）+ E2E `tests/e2e/25_math/`

**三、验证结果**

- 单元测试：**987/987 通过**（原 965 + 新增 22）
- E2E：**30/30 通过**（原 28 + 新增 24_preprocessor、25_math）
- 编译：0 错误 0 警告（MSVC /W4 /WX）

**四、文件清单**

| 类型 | 文件 |
|------|------|
| 新增 | `src/cn_compiler/lexer/preprocessor.hpp/cpp`、`src/runtime/math_api.cpp`、`stdlib/数学.cn`、`tests/unit/lexer/test_preprocessor.cpp`、`tests/unit/runtime/test_math_api.cpp`、`tests/e2e/24_preprocessor/`、`tests/e2e/25_math/` |
| 修改 | `lexer.hpp/cpp`、`semantic.cpp`、`ir.cpp`、`driver.hpp/cpp/driver_module.cpp`、`module.hpp/cpp`、`cn_main.cpp`、`runtime.hpp`、`CMakeLists.txt`、`tests/unit/CMakeLists.txt`、`plans/002-阶段6-标准库.md` |
