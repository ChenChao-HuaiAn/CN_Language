# CN语言编译器 更新日志

## 功能完善总结

**本轮（2026-09-07 第三十一轮上半程，单位机 麒麟 ARM64）**：P6b 模块系统启动轮——plans/018 立项、第 0 步实证、规范立规、定位收紧诊断落地、E0255 基建写就（停用）；中途新发现一枚独立既有宿主错编缺陷并呈报，按用户指令**停于「呈报一待终裁」交接点**（呈报二 A′ 已批待实施，呈报一 A/B 待家机面询后终裁），交接全文见 HANDOFF。

### 一、实证与翻案（零改动项）

- **第 0 步三探针**：`模块` 声明自动加载宿主已实现且触发源=声明本身（声明在→未导入子模块错误暴露 rc=1；声明删→rc=0；基线 build→run rc=0）——工作流该勾销。
- **A1 翻案**：类型/常量跨模块隔离已在 fecab2b（A-2）修复——跨模块同名 rc=0、glob 歧义诊断（函数/类型/常量）三维度齐全 rc=1；lessons 过时「📌 已知限制」条目标注 ✅ 已修复。
- **E0659 零改动**：三维度歧义诊断实证齐全。

### 二、规范立规（plans/001 + cn-language-spec specs/08）

- plans/001 新增 **§5.9.7 名称解析（遮蔽与歧义）**：三级优先级（本模块项＞具名导入＞通配引入）+ 冲突遮蔽判定矩阵（①×②/②②同名=错误、③③使用点歧义=错误、①遮蔽③=合法）+ 链接符号契约（多模块 `模块$名#参数串`、单文件/主不加）。
- plans/001 **§5.9.1 模块声明定位收紧**（用户已批）：仅限包根成员聚合/父模块挂子模块两种树结构位置，普通文件使用=编译诊断；自动加载强语义（Rust `mod` 对齐）写入。
- specs/08 第二节（定位收紧两行规则）/3.6（名称解析全节）/第七节（货舱.toml 纳入 v2 范围注记）三处同步。
- ⚠️ §5.9.7/3.6 现为**呈报一 B 方向暂定稿**（「导入 m::符号」=具名绑定、P1-1 废止注记），终裁待家机面询——过渡期偏差已标注，见 HANDOFF 第一节。

### 三、宿主实现（门禁全绿）

- **定位收紧诊断**：driver_module.cpp 模块声明挂载清单收紧解析——仅允许 crate 根（入口/包.cn）同级聚合与父模块子目录挂载两种形态，其余报专用诊断「模块声明 'X' 非当前模块 'Y' 的子模块」（替换误导性「无法打开源文件」）；ModuleUnit.moduleMounts 新字段。
- **E0255 基建（停用待呈报一终裁）**：ImportDecl.ownerModule 来源归属（mergeModules 写入）+ checkImportLocalConflicts（semantic_decl.cpp，①×② 导入与本地定义同名 / ②×② 多次显式导入同名 / 自导入豁免）；semantic.cpp visitProgram 调用点注释停用。
- 改动面：ast.hpp、module.{hpp,cpp}、driver_module.cpp、semantic.{hpp,cpp}、semantic_decl.cpp——全在共享层，win 侧须家机 ci.ps1 实证。

### 四、新发现宿主错编缺陷（独立既有，呈报二已裁 A′ 待实施）

依赖模块与入口文件同名同签名函数 + 入口纯名调用 → 链接 `undefined reference 主$X`。汇编实证根因：注册键归属顺序依赖（首注册模块占裸键，后注册者得 模块$键）vs 定义侧「主 恒不加前缀」两侧不对称；52_library 现通过属侥幸路径；全量 E2E 对此形态零覆盖。根治方案 A′（用户已批）：链接键单一公式化——共享函数 `函数链接键(模块名, 签名键)` 注册/定义双侧同源（Rust 符号=f(路径) 对照论证）。详情与实施点位见 plans/018 第九节。

### 五、验证（当前树，E0255 停用态）

构建零警告（GCC -Wall -Wextra -Werror）+ 单测 **1243/1243** + E2E linux-arm64 全量 160 用例 **156 过/1 败（78 已知 OOM 家族）/3 跳 = 与第三十轮基线完全一致，零回归**。七项基线探针（模块自动加载/A1/E0255 形态/E0659 三维/定位收紧）配方存 plans/018 第二、四、九节（探针在 target/dbg/p6b_*，不入库）。

### 六、诚实边界与下一步

呈报一（「导入 m::符号」语义 A/B）**待用户家机面询后终裁**（HANDOFF 第一节含通俗说明）；呈报二 A′ + E0255 启用 + 宿主侧新 E2E（159 起）+ 家机 ci.ps1 门禁 = 下轮首务；随后进入工作流 3（v2 全链，含货舱.toml 纯 CN 解析器——用户已批纳入）。arm64/x86_64 侧复验随跨机轮。
