## 功能完善总结

本次推送共两笔提交：

### 一、宿主 GCC 构建兼容修复（ARM64 计划阶段0，本机验证后入库）

- `src/runtime/io_api.cpp`：`__cn_alloc_reset` 内中文标识符（待释放）改为 ASCII（to_free）——
  GCC 9/7 不支持 UTF-8 标识符，中文标识符会中断 Linux 构建；改动行为中立。
- `tests/unit/runtime/test_input_api.cpp`：文件描述符操作宏化（TEST_DUP/TEST_DUP2/TEST_FILENO/
  TEST_CLOSE），兼容 MSVC 下划线名（_dup/_dup2/_fileno/_close）与 POSIX 无下划线名。
- 本机验证（Linux ARM64）：GCC `-Wall -Wextra -Werror` 零警告构建通过 + 单测 1196/1196 全绿；
  win-x64 运行时门禁待 Windows 会话（本机无 MSVC），如实标注不写假绿。

### 二、AGENTS.md 改造为 ZCode 规范

- 文件头从 DSH/agent-instructions 插件表述改为 ZCode 工作区指令文件机制（ZCode 原生加载，
  用户级默认指令在 ~/.zcode/AGENTS.md，本文件后加载可覆盖），并注明项目技能集 `.zcode/skills/`。
- 删除第 8 节「项目现状速查」全部进度内容，确立**「本文件只写规则、不记进度」**约定——
  进度一律以 plans/ 内对应任务文档（随任务创建，不固定某一份）与 HANDOFF.md 为准。
- 新增**「中间产物与实验清理」**规则：调试中间产物、实验性脚本/临时用例任务完成时必须删除，
  有保留价值的转正为正式 E2E 用例（tests/e2e/）或移入专门位置；`target/` 标注已 gitignore。
- 第 5 节去除时效性注释（单测/E2E「当前 N」数量不再写入规则）；推送 remote 名按实际修正
  （gitcode.com，本机 remote 名 origin）。
- 附带纳入：`.ai-coder/rules/project_rules.md` 补规则 9/10（宿主缺陷优先/测试=规范）；
  `.zcode/plans/` 两个会话计划（git 拉取计划 + v2 ARM64 GAS 后端阶段A/B 计划）随库同步。
