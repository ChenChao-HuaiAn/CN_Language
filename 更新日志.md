# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（261-a·2026-09-16）

**C20 波1：E2E 双测断言升级 + A 类首批双编译转正 + Cx 定性 + B 类批次表**（基线 5c8009f）：

1. **runner 断言升级四件（向后兼容）**：`双编译对照.txt` 新键 `断言输出=是`（两侧运行 stdout
   逐行比对 .expected——原口径只锚退出码+编译日志锚行，打印型用例会假绿；默认否=存量不变）；
   宿主侧编译旗标通道；两侧 .input/.args 注入；v2 侧入口按配置源文件（根治 win/linux 正路径
   写死 主.cn 的存量缺陷——非主.cn 用例此前无法双测）。
2. **A 类 63/72 上架双编译**（批量脚本逐个实测双侧绿才上架；含 01_hello 首例+384 旗标面）；
   全量 E2E 407=407/0/0 零回归；负例咬合实证（破坏 expected 一字→断言精确咬住→还原）。
3. **9 个 v2 分叉定性立案**：D34 整型语义族（178/191/05）·D35 错误处理族（320/344/60/364）·
   D36 遮蔽恢复（346）·D37 chkstk（39）——宿主全对=v2 单侧，用例保持原编排待根治转正。
4. **Cx17 复核 17/17 COMPILES 实锤**（v2 漏检全集）→立案 D38；v2p 旗标空转实测→立案 D33。
5. **B 类 129 批次表**落 plans/021 C20 行（波3+ 排班）；plans/021 §三-D D31 漂移行顺手修正。
6. 门禁：零警告+单测 1351/1351+全量 E2E 407=407/0/0+存量 40 双编译复跑 40/40+双文档门禁。

## 深度机 linux-x86_64 节

## 功能完善总结

> **🧪 面台账巡视轮·行52 浮点全算符回归矩阵（2026-09-17，深度机 linux-x86_64；免锁验证轮不占轮次号·基线 9dbe307·家机 263-a D34 开发轮持锁在飞）**：
> ①**矩阵建成并全量跑通**：/tmp/cn_matrix/fpscan.py——179 形态（浮64/浮32 四则×折叠/运行双路径×比较六算符×NaN/Inf 特殊值×精度判别式×转换×复合赋值/自增/f 后缀/取模观察）×-O0/-O3 三方对拍（python IEEE 期望+struct 单精度舍入模拟）——**174 全对**（H1/H2 修复面主体无回归）；
> ②**三缺陷立案**（plans/021 §三-G T14/T15/T16·asm 级定性+源码挂点定位）：**T14**（P1）整32→浮64 转换负数错=cvtsi2sd 64 位读×32 位零扩展装填宽度错配（linux_x64_instructions.cpp:413~417·变量路径 O0/O3 一致错·正数巧合正确家族）；**T15**（P1）浮64 恒等转换套混合表达式误插 cvtsd2ss 单精度舍入（:425~433·7.0→7.000001 双级别一致·可精确表示值掩盖=隐蔽）；**T16**（轻）一元负号 0-x subsd 实现丢 -0.0（E2E 250 形③注释「fpneg/btc」与 x64 实测不符=后端语义打架嫌疑·arm64 待复验）；观察项：浮点 % 拒绝=行为安全但规范 01b 未明写 % 限整型（归 F2-37 规范精化）；
> ③**台账与基线同步**：行 52 ⬜→🔶（含欠账标注）·面覆盖 **53/60（未测 7）**·覆盖基线.txt 8→7（脚本 --init）·plans/021 文首戳带覆盖数（B8 制度第 4 条）；
> 门禁=check_matrix_coverage PASS（基线 7）+双文档门禁显式退出码绿——编译器源码零改动（E2E/单测不受影响·未重跑·口径如实注明）；探针留档 /tmp/cn_matrix/{fpscan.py,fp/,fpdiag/,fp_results.json}。

## 单位机 ARM64 节

## 功能完善总结

### 257-a 插队修复轮：plans/021 §3-B 生命周期铁律根治（2026-09-16，单位机麒麟 ARM64，基线 f0f0696·认领 9818aa8·用户质询「B5 三犯」驱动）

1. **修复**：§3-B 清空 B5 整段「已裁决原文留档」（裁决/实施记录本已由 §3-F F1-26 行/§5 256-a 行承载）；§3-G 同型滞留的 T2（案戊）/T9（整数溢出方案 D）迁 §3-C（C17/C18）+处置建议段同步。
2. **立规**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」——条目一经用户裁决**当轮迁出**（实施明确的迁 §三-A、定案待排程的迁 §三-C、完成销项进 §4.2），禁留任何留档形式；呈报类条目须同步登记 §3-B。
3. **机械门禁**：check_handoff.py 扩 §3-B 拦截（「已裁决/已批/已选定/✅」完成标记即禁止提交）——反例（修复前 L80 B5 双标记命中·退出码 1）/正例（修复后 PASS）双向实证；源码零改动（纯文档+门禁脚本轮）。
4. **B6 裁决闭环**：分支策略建议（develop 单主干+里程碑标签强推+不采用功能分支）经用户批准（「同意」）当轮迁 §3-A **A12**（里程碑标签锚点建立·待认领）；B 区回空态。

**门禁**：check_handoff.py（新检查双向实证）+check_progress_sync.py PASS。

**诚实边界**：机械检查为字面拦截非语义判定（完成态措辞须避让·宁可严格）；github 镜像本机无凭据未推。
