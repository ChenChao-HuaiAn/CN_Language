# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（629-a 开发轮·2026-09-23·任务/家机-629-供给扩容·wt629）

**021 任务 006 供给.cn 机制扩容实施轮（用户裁决已批）——30/31/155 恢复双测通道**：

1. **供给机制扩容至 stdlib 类方法面·实测打通**：31 号 LNK2019（v2p.obj 缺用例所需 stdlib 泛型实例）→ 供给.cn 按 126 同款形态（stdlib 面实例化调用+宿主编译入口 主()）经宿主真实管线编译 supply.obj 链接补符号 → 链接成立。stdlib 面=006 扩容新增覆盖域（原机制只覆盖用例自有类型）。
2. **runner 共享契约修复（6 处）**：v2src/dualhost/负测目录的 copytree dirs_exist_ok=True 叠加拷贝会残留「源目录已删文件」——31 删漂移副本后 v2src 仍编残留副本 → v2p 引用副本内部函数面而供给 obj 提供内置实现面 → LNK1120 混合态假象。新增 拷贝用例树干净()（先 rmtree 再 copytree）6 处替换；快照=源目录镜像（防虚假验收红线）。**契约变更通告已发看板**。
3. **30/31 副本漂移清除**：30 副本容器.cn 32 签名与内置逐字一致（真子集）；31 副本映射集合实现已被内置链桶版重写（内部函数面完全不同）——两副本删除、用例回归内置 stdlib 单一事实源。
4. **三例双测上架**：30/31/155 各建 双编译对照.txt（借链=是·断言输出=是·31 带供给）。
5. **三例 v2 侧残余红=622/627/628 修复家族投影**（实测定性）：30=按值返回 结果<T,E> 同变量二次接收判别不更新（6 行自由函数探针复现·sret/按值域·确定性）；31=映射.获取 随机值 UB（同二进制 300/失败7 漂移）；155=容器引用参数段错误——挂等 006-001（随 627/628 集成后复验转绿），本轮不与在飞修复轮撞车。

## 深度机 linux-x86_64 节

## 功能完善总结（服务轮+603-a 机制级修复轮：协议 v3 补验收+strict 双红根治·2026-09-22）

**本轮修改内容**（免号服务轮+任务 603 分支 `任务/深度机-603-strictfix`·集成 c5bf935d）：

1. **拉取+补验收（协议 v3）**：develop d3737cdf→e1935ae4 四链代码集成（591-a/592-a/597/601）linux 异步验收=零警告+单测 1383/1383+E2E 526=524/0/2 零失败+matrix 83/83（wtv598 独立树全新构建）；win 侧 050/524 备案红 linux 均绿=win 单侧现象。
2. **603 strict 双红根治（机制级·当场修）**：①050 coverage_map 登记行复登记（ec4c2064 被 591-a 净差异集成再丢失·登记丢失族第二例备案）②601 裁决甲 9 例 v1 化石下线连带簿记（[2.1]/[附录A] 两正例引用+70~76 七豁免行清理+[2.1] 正例格入 EXCLUDED_CELLS·随裁决甲·v2 等价补位后应移除）——strict rc 1→0（178/178）；零 src 零行为面。
3. **附带**：看板 549/551 过时挂等行清偿（均已是 develop 祖先）+本机验收基线行→c5bf935d+M3 采样补轮（322 样本分歧 0 零新命中·06:01 后守护未跑·主树重建后手动补）。

**净效果**：develop strict 全平台恢复绿（178/178）；601 下线 9 例后的规范覆盖簿记闭合；登记丢失族风险第二次备案（净差异/squash 集成后 strict 必跑）。

## 单位机 ARM64 节

## 功能完善总结

### 596+604 双轮（604 原取 601·家机撞号让位改号）：arm64 补验收红灯根治+runner 并行恒定/进程池根治（2026-09-22·基线 develop 9a4764cf→c35b527f）

1. **596 红灯根治**（v3 补验收实锤）：v2p 自举编译器 arm64 全输入崩（空指针/段错误）——引入=525-a 两处半截改动（随 572 收拢越挂等入干）：emitFieldAddr 零偏移分支 res 垃圾+emitParamSetup 拷贝基址硬编码 [x10]；双点根治+反证双态（撤 525-a→复绿）。**456 随修转绿=fdef8ae9 P1 同根闭环**（批间分叉=未初始化寄存器垃圾二级表象·定性勘误）。
2. **604 runner 根治**（用户令质询响应）：568-a「并行恒定·删串行」从未入 develop（586-a 摘取集成遗漏 runner）——恢复 --jobs 默认 CPU 核数+删串行路径；线程池→**进程池**（POSIX 多线程 fork 死锁实锤根治·win 免疫=平台盲区）+worker 保险 1800s。
3. **终验**：arm64 全量 **535=533/0/2 首次零失败**（默认并行·约 25 分钟）+单测 1394+零警告+78/79 自举链 PASS。
4. 遗欠立账：**050**=568-a 超时防线 26 处+dual rc 恢复（从 a0ba39ab 摘取·对齐 604 进程池版）。

