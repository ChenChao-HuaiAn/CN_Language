# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（589-a 开发轮：005 甲案 v2 树同构+050 双测上架=全链销项·2026-09-22）

- **v2 同构三层**：取地址分支（&类变量→IR_加载 产对象地址）+生成声明 类/结构体分派补
  指针层数排除（类指针声明此前编译拒绝=纯新增支持面）；
- **050 双编译对照上架**（双侧 42/42/99·501 号被 499 链占用改 050）；
- **005 销项迁 §4.2**（宿主 588+v2 589+050+条文全链完成）；591=解锁面验证随 033 接力；
- 门禁：全量 E2E 534=534/0/0+自举链绿；坑=跨轮复用 worktree 必须先重编宿主（陈旧 exe 假红
  346/504 定性实录）。

## 深度机 linux-x86_64 节

## 功能完善总结（585-a 开发轮：D42甲 波3 终章接力=撤守卫收口·2026-09-22）

**本轮修改内容**（001 任务·用户令全量完成·深度机接手家机 578 同计划）：

1. **撤守卫态全量首验（linux）**：TEMP 撤守卫（是标准库模块 恒假）→全量 E2E 528=505/21/2——暴露面 21 红完整清单（运行错值/崩 14+负测锚漂移 6+自举 1）。
2. **四根因二留二回（机制级 §8.4·随证据备案）**：留=①`签名返回字节数` 结果空 12→16（268-a 口径漏同步）②`生成类对象局部` 补 IR_分配（帧预留缺失·同族扫描唯一漏点）；回=③字段载荷容器排除（修 441 破 228）④容器参数句柄语义（修 113 破 127/497）——分层实验两难实锤。
3. **负测锚七例适配**：197/223/224/227/229/464/283 expected「已解析…节点=N」去节点数尾巴（撤守卫态=全树合并节点数·纯锚行漂移·v2 行为正确）。
4. **本质定性立案 047**：撤守卫容器「内联/句柄」双模语义并存（517-a vs 生产态模型）——架构级收敛轮（P0·001 波3 剩余）。
5. **生产态零回归**：守卫恢复态全量门禁全绿（零警告+单测 1394/1394+E2E 528=526/0/2+strict 100%+matrix 83/83）。

**净效果**：撤守卫态残余红 21→定性清单（047 承载）；生产态零回归；撤守卫落地+借链 51 例转自洽=047 清偿后下轮执行。

## 单位机 ARM64 节

## 功能完善总结

### 560 开发轮：F2-36b 首步=bench v2 口径三平台改造+arm64 自举性能基线入库（2026-09-21，基线 develop 0e3ec08f）

1. **范围修正（开工侦查实锤）**：原 bench_self_host.py 测量对象=v1 组件链（CN语言编译器/ 五组件·已推倒）——对象整体过时；改造为 **v2 自举树口径**（CN语言编译器v2 全树·fix_p≡fix_s 固定点·对齐 78_v2/79_v2 锚定链编排）+`--platform` 三平台工具链分派（win=ml64/link 原路径保留·linux=as/g++ -no-pie〔CN_AS/CN_CXX 环境变量+PATH 探测·对齐 runner〕）。
2. **arm64 基线实测入库**（编译器=组合态 558+559 修复态构建）：M1=22295.7ms（C++ 宿主 Debug build v2 全树）/M2=42608.4ms（CN 版编译同树 742237 行·3 次中位）/**比值 1.91x vs C++ Debug**/M3=2194.8ms/M4=146.4ms+绑定自检 cn_main∈cn_self.obj ✓/**M5=32309.0ms·M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建的同一编译器快 24%**（重大正面信号）/M6 ✅ fix_p≡fix_s 逐字节（自举固定点成立）。
3. **实施修两处**：M4 借链对象（初版错用 fix_p 自汇编→undefined 词法$Token 追加符号·grep 实锤 fix_p 缺跨模块符号定义→改 cn build 中间产物 v2p.o〔run_e2e「容器符号提供者」原义〕）；基线测量编译器依赖序（主树无 558 修复→改用组合态构建·口径注明）。
4. **口径诚实边界**：C++ 宿主=Debug 构建（-O0）——1.91x 非 vs Release 口径；单机基线（跨机比较无意义）；win 分支静态核验（跨机复验归家机）；深度机 x64l 基线可复用同脚本（三机通用）。
