# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🏃 311-a 开发轮=306-b·波次2 v2 侧同构销项·2026-09-17，基线 3124d25·宿主 src/ 零改动）

**T3/T4/T21 三案 v2 侧同构（宿主 306-a+v2 双侧全链闭环）**：

- **T21**：词法分析.cn 区段化（是汉字码点/位置码点/序列长/是标识符起始单点共用+区段外序列整体跳过诊断+BOM 跳过）——t21a/b 探针+416_v2 转正。
- **T3**：IR布局.cn 收集设施+语法分析.cn 字段收集（排除清单含节点附加指针层）+主.cn 聚合守卫——解析期登记模型等价完备解（环必产生未登记引用=同源捕获零误报）——t3a/b/c 探针（指针递归放行）+414_v2 转正（间接环）。
- **T4**：生成表达式求值.cn 发射字符串越界检查（__cn_str_len+错误码 2 对称·字符* 不误伤）——415_v2 转正（含越界负形态真触发）。

**门禁**：全量 E2E 434=433/0/1（唯一败=406〔T43 已立案待裁决〕）+单测 1353/1353+v2 族 88/88+78/79 自举链 PASS；收尾两事故同轮根治（陈旧 cn.exe 重建+BOM 跳过·lessons 三条补录）；双推 gitcode+github。

## 深度机 linux-x86_64 节

## 功能完善总结（306-a·波次2 宿主侧销项 T3/T4/T21+OOM 事故根治·2026-09-17，基线 dbd0b88→认领 4d7371c）

1. **OOM 事故根治**（18:58 cn 28.7GB OOM 连坐 ZCode 关闭）：T21 首版主分发循环未同步收紧→全角字符空 token 无限空转——口径单点化（isIdentifierStartChar/ContChar）根治·死循环回归验证；lessons 入册。
2. **T3**（semantic_types.cpp）：computeLayout visiting 环检测+惰性递归布局——递归结构体编译期拒绝「无穷大小」（直接+间接环）·类引用语义豁免·SelfReference 单测断言随用户裁决更新。
3. **T4**（ir.cpp+ir_expr_index.cpp）：emitStrBoundsCheck 运行时长度版+三读路径插桩——字符串越界 O0=O3 拦截错误码 2（与数组防线对称）。
4. **T21**（lexer.cpp）：标识符字符区段化（CJK+扩展A/B·方案甲）——全角/标点/假名拒绝·存量零误伤。
5. **411/412/413 三 E2E 转正**+coverage_map；**daemon 防线加固**（cnsmith_diff 子进程 RLIMIT_AS 6GB）+重启。
6. **门禁**：零警告构建+单测 1353+全量 E2E 431=429/0/2+strict/matrix/CLI+双文档门禁。诚实边界=v2 树三案同构=306-b（立案面双侧·本轮宿主侧完整闭环）。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
