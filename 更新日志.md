# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🧪 四链回签服务轮：深度机在飞链 win 侧 try-build 应答·2026-09-20）

- **三链合一（470/474/477）win 回签通过**（19973973·门禁 4/4 全绿 E2E 481=481/0/0）→深度机即集成销账
  （develop bc97d61a·D10 双侧销项+D46 收官）；
- **501（T69 win EXTERN 修正·八链超集）回签不通过**（E2E 483=475/8）：A2005×174 全消=修复有效+真红
  7 例 A2008 `EXTERN r11`（T69 间接调用收集器误收寄存器·check_win_asm 三判据合取漏形）+488 CRLF
  检出假红（LF 实证 PASS）+check_win_asm 未注册——修复建议随广播，等深度机重发起；
- **505（T70 B1 二层·`表[i]=<结构体>` 右值整体拷贝）win 回签通过**（门禁 4/4 全绿·E2E 482=482/0/0
  含 495 新例 win 真跑）——准予集成；
- 同轮处置同机门禁死锁（gate_lock run×ci.ps1 双重 acquire·85 分钟零工作）+教训入册+plans/021 §五
  服务轮行。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 523-a D1 滚动：克隆节点修针 138 行拆分〔零行为变更〕·2026-09-20）

- **拆分**：`IR类.cn` 克隆节点修针→主分派器 14 行+十子函数（7 语句族提前返回+兜底回填不返回续检+声明族）；check_fn_length 61→60。
- **等价证据链三层**：md5 双对拍（探针+全树改前文本 c4106a2c）+行为级反证（查ac 恒等变异→456 泛型用例产物变→恢复复绿）；侦查两改方案被否（四槽位族误读/else-if 重排丢兜底先行回填）。
- **门禁**：单测 1387/1387+E2E 522=517/3/2（3 假红单跑 PASS）+strict/matrix 绿。
- **522 并行挂等**：497 转正轮 try-build 请求已发挂等三平台回签。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
