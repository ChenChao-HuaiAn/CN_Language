# CN语言编译器 更新日志

## 功能完善总结

# 第四十三轮（2026-09-09 单位机 麒麟 ARM64）：三项裁决落地+④妥协重做——v2 位运算五算子全链+结构体构造字面量穷举纪律+路径归一+移位语义定义化+原生位运算重写全部妥协实现

按第四十一/四十二轮呈报三项的用户裁决（①位运算独立特性轮+移位语义跟 Rust 学习立即执行；②结构体构造字面量+穷举纪律跟 Rust 对齐立刻执行；③路径归一+宿主同型面发现问题立刻参考 Rust 完善；附加第 4 条：之前因为缺陷而妥协、绕过的实施方案全部重做，性能不打折）执行。本轮跨宿主/v2/规范/三后端/测试五面。

## 一、v2 位运算/移位五算子全链（规范 01b§5.4）

- **词法**：`<<=` `>>=` 三字符 + `<<` `>>` `&=` `|=` `^=` 双字符最长匹配（`^` `~` `&` `|` 单字符兜底既有）。
- **语法**：二元优先级层重编号 1=|| 2=&& 3=| 4=^ 5=& 6=比较 7=移位 8=加减 9=乘除、原子=层10（对齐规范 01b§6 十三级中 v2 已实现层；`1<<2+1`=8 优先级正确）。`~x` 解析期脱糖为 `x ^ (0-1)`（宿主 ir_expr visitUnaryExpr 同构，零新节点/零新 IR）。
- **嵌套泛型 `>>` 连带根治**：`>>` 词法合并单 token 后，泛型闭合位置（扫描按两层计）与解析类型文本（闭合位置化容错消费）双层升级——宿主 H3「GreaterGreater 按 2 层闭合」同构，`向量<映射<K,V>>` 任意嵌套深度位置对齐。
- **IR**：IR_左移=68/IR_右移=69（尾部空区，避开 14~17/20+ 同值常量跨文件解析缺陷敏感区）；IR_位与/位或/位异或=15/16/17 既有预留启用。
- **三后端**：win（and/or/xor、shl/sar 经 cl）、ARM64（and/orr/eor、lsl/asr）、X64L（同 win GAS）——全部寄存器槽形式。

## 二、移位语义定义化（规范层，用户裁决「跟 Rust 学习立即执行」）

- **立规**（plans/001 §4.4 + specs/01b §5.4 同步）：移位量按操作数类型位宽取模（`a << k` ≡ `a << (k mod 位宽)`）——Rust release 掩码语义同构（Rust debug 陷落需构建模式概念，CN 单一模式取定义掩码分支）；右移有符号=算术/无符号=逻辑；位运算仅整型。
- **宿主改造**：折叠趟 const_fold 既有 mod 位宽实现；三后端 emitShift 常量路径补编译期掩码（修复 arm64 常量移位 ≥64 直接汇编报错=编译失败、两 x86 靠硬件 mod64 侥幸的平台分叉）；≤32 位类型寄存器路径补 and 掩码（修复折叠 mod 位宽 vs 运行期 mod64 语义分叉——整32 `5<<33` 折叠=10 运行期原=0）。E2E 178（常量/变量移位溢出+算术/逻辑右移+位复合赋值五形态）锚定。

## 三、v2 结构体构造字面量 + 穷举纪律（规范 05 二）

- **语法**：`类型{字段=值,...}` 解析期脱糖为 裸声明（B1 零初始化打底）+逐字段 成员左值赋值——复用既有成员偏移标注/赋值/IR 全链，零新节点/零新 IR 面。
- **穷举纪律**（Rust E0063/E0062 同构，用户裁决 Rust 对齐）：解析期比对 构造字段名表/数表（解析结构体声明 新登记，键基域），缺字段/重复字段=编译错误（语法错误数中止通道，stderr 诊断 rc=1）；豁免联合体（C 单成员激活惯例）。
- **宿主侧同轮收紧**：visitStructInitExpr 补缺字段=编译错误（原静默零填探针实锤：`点{x=1}` 编译通过且 a.y=0）；未知/重复字段既有诊断确认✓；联合体豁免保持 UnionFieldAccess 单测绿。E2E 179 负测转正。

## 四、路径分隔符归一（第四十一轮挂账③根治）

- **v2**：归一路径分隔符（快路径无反斜杠原样返回=linux 常态零分配零拷贝）挂 主() argv 入口单点，下游字符串路径身份自然一致（win 反斜杠→包上下文恢复失配双载歧义 183 错根治；动态复验=家机轮）。
- **宿主**：cn_main.cpp argv `#ifdef _WIN32` 同款单点归一（unix 下 `\` 是合法文件名字符不动——Rust std::path 平台分隔符语义同构）。

## 五、④妥协重做（用户令：妥协方案全部重做、性能不打折）

- 审计枚举 v2 因「无位运算」妥协的实现四项，全部原生位运算重写：
  1. FNV 族四函数（字节异或/低八位/高五十六位/异或一字节，40+ 行负数位模式手工推理）→ `h=(h^b)*prime` 单行；
  2. 位模式十六进制（高低 32 拆分 /% 逐半字节+负数两分支修正）→ 移位+掩码 4 行无分支；
  3. 字节十六进制 /% → 移位；
  4. +256 符号修正 35 处（含 字节值 助手与共用层）→ `& 255` 位掩码（受控脚本精确替换+逐处 diff review+残余清零）。
- **等价性实证**：新旧实现逐位等价探针 rc=0（3000 组 LCG 全模式扫描，覆盖 int64 最小值/2^63 边界/黄金 FNV 轨道抽样）——超长收缩黄金值 EFF9F723240DB1BE 由第四十一轮锚定经等价性传递（win 侧动态复验=家机轮）。

## 六、验证与文档

- **v2self 固定点**：v2p 重建（宿主编译 v2 全树 rc=0）→ cn_self 一代链接 → **fix_p ≡ fix_s 302433 行裸逐字节一致**（第四十二轮 294888 行锚定经全部改动保持；行数增长=新特性源码的确定性体现）。
- **E2E 转正**：180_v2_位运算形态（rc=176：五算子+复合赋值+移位掩码 `1<<70`=64）、181_v2_构造字面量形态（rc=42：全字段赋值语义）、182_v2_构造穷举负测（None 通道：缺字段诊断行固化）三用例单跑全绿。
- **门禁**：清场重建 GCC 零警告 rc=0 + 单测 1251/1251 + 全量 E2E linux-arm64（结果见提交信息）。
- **文档**：plans/001 §4.4 移位语义立规、specs/01b §5.4/specs/05 §2 同步、plans/014 第四十三轮状态行、HANDOFF 重写。
