# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十七轮 117-a：D1 行数整改第八波——`semantic.cpp` 1996→692 行按族拆分**（2026-09-13，家机 win-x64；基线 a28c59b）：
> ①**拆分**（纯重构零行为变更）：`semantic.cpp` **1996→692 行**，拆出 `semantic_types.cpp`（**369 行**：类型系统
> ——类型名解析 declareTypeName→enumValueOf + 族内 findTopLevelComma + 布局计算 typeSizeOf→computeEnumValues）/
> `semantic_sig.cpp`（**455 行**：函数签名/重载/隐式转换 funcReturnTypeOf→genericClassInstanceName，含
> resolveOverload/canConvertType）/`semantic_builtins.cpp`（**534 行**：内置函数注册 registerBuiltins +
> registerFunction + isExternFunc）；**helper 引用矩阵**=仅 `cnEvalConstExpr` 被主段用（留主文件）+
> `findTopLevelComma` 随类型族——115-a 头化使本波零依赖障碍；CMakeLists 挂载 3 文件。
> ②等价性=产物 asm 五样本逐字节一致 + 行级多重集校验（族跨文件中间段：types 族两段拼接亦覆盖）。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 连续第六次不变** +
> 对拍 **34/34** + 运行级 **3/3**。
> ④教训：本轮无新坑；第七度复用 D1 checklist（特有点=族跨段拼接由多重集校验覆盖）。
>
> **本会话（111-a~117-a）累计**：111-a 门禁跃变（C14/C15 销项 + 新缺陷 221 双侧根治；win 门禁 **259 过/33 败 → 290 过/2 败**）
> + **D1 七波**（ir_expr 2798→734 / ir_oop 1381→508 / ir_oop_call 1155→763 / semantic_internal.hpp 共享头去重 -536 /
> semantic_expr 1778→654 / semantic.cpp 1996→692）——**宿主原 7 个超长文件仅剩 `ast.hpp` 1032**；
> 锚定链固定点 406097 全程一致（六次复验）+ 产物 asm 五样本逐字节一致 + 全量零回归。
>
> ⑤诚实边界：D1 剩余（`ast.hpp` 1032〔头文件策略不同〕+ codegen 面 4 文件 + v2 侧 6 文件 + 函数级 visitCallExpr 885 行等）
> 登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
