# 更新日志

## 功能完善总结

**plans/017 呈报③方案A 实施：v2 函数重载消歧机制全链落地（2026-09-06 第二十五轮，深度系统 x86_64，用户裁决方案A·Rust 准绳：性能第一/安全第二）**

1. **规格定性**：函数重载=规格书既有承诺（plans/001:358 同名按参数类型与个数
   区分+附录C 签名编码），宿主编译器全链达标（signatureKey/resolveOverload/
   nameMangle）；v2 规范级未达标——语义层函数登记键=纯声明名（同名映射覆盖
   静默吞前）+IR 层函数名/调用点纯名（无实参决议，跨文件同名重载 asm 符号
   重复定义，第一轮冒烟靠源码改名 规避）。
2. **实施（三文件，登记/决议/符号三层）**：①IR容器.cn 函数签名表机制
   （候选数表+参数串表、函数参数串提取[指针 */引用 & 后缀自类型节点附加重建]、
   函数签名键[空参=纯名，对齐宿主 signatureKey]、幂等登记——语义/IR 两冒烟
   子链登记源恒可达）；②语义分析.cn 声明收集 登记键改签名键+函数重定义诊断
   （原注册失败被静默吞）+检查表达式 标识符/调用两分支签名表回退；③IR生成.cn
   生成函数 IR 名=签名键（asm 消歧，三后端 符号修饰 P7b 已支持零改动）+
   决议函数符号（候选 0→纯名【内置/打印/类型转换/构造路径零变化】/个数过滤→
   精确→字面量宽匹配→个数唯一兜底/**歧义与失败=IR层诊断 fail fast 绝不静默
   选**）+三张表双键登记（签名键恒有+纯名键同名唯一——既有纯名消费点零回归）
   +调用点集成。
3. **fail fast 两次当场拦截（机制价值实证）**：①参数计数假匹配（初版误用
   实参数初始化从未真数——改名回归实测 3 错中止拦住，改逗号数+1 真数）；
   ②实参索引偏移（调用节点 [子起点]=被调标识符，实参从 +1 起——min18 探针
   [d18] 插桩实证后修正）。Rust 对齐：编译期决议+符号编码一次拼接零运行时
   开销（rustc/宿主 mangle 同构）；歧义/失败=编译期错误，绝不静默错决议。
4. **验证（全绿）**：新增 E2E 157_v2_函数重载消歧（同文件 2/3 参+整32/整64+
   字面量宽匹配重载+跨文件同名双版本）五断言 42/6/27/70/600 宿主与 v2 产物
   完全一致；生成函数W→生成函数 改名回归——asm 消歧符号实证（两签名独立
   符号+同签名跨文件逐字节一致）；门禁全绿（构建零警告+单测 1240/1240+
   E2E 159 用例 132 过/0 败/27 平台跳过）；九探针 v2p_re vs v2p_fix 对拍
   9/9+min1 逐字节一致（零回归）；**v2self 重编（191956 行 asm）+新一代
   自举锚定**：12 组件冒烟 PASS+min1/min18 产物 v2p_re vs v2self_new 逐字节
   一致+九探针真三方对拍 9/9。
5. **踩坑（lessons 权重 10）**：假对拍 cwd 陷阱二度复现（/tmp cwd 下 cp
   项目根旧产物假 PASS+脚本 sed 改造判定行自比假绿）——对拍脚本每次改造后
   必须通读判定链确认两产物来自两个不同编译器。
6. **文档同步**：plans/017 第十五节+plans/014 状态表第二十五轮+lessons
   权重 10+HANDOFF。

**上一轮（第二十四轮）结论备查**：第 17/18 枚闭环（结构体整体赋值单槽浅拷
根治）——九探针 9/9 全绿；v2self 首次与 v2p 全探针逐字节/行为一致；第 7~18
枚 12 层缺陷链全部闭环。详见 plans/017 第十三节。
