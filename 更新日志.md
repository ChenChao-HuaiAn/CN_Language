# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十轮 120-a：D1 行数整改第十一波（v2 侧开波）——`生成表达式.cn` 1512→738 行按族拆分**（2026-09-13，家机 win-x64；基线 34c3f28）：
> ①**拆分**（纯重构零行为变更）：`生成表达式.cn` **1512→738 行**，拆出 `生成表达式运算.cn`（**429 行**：
> 生成短路求值 + 生成三元运算 + 生成一元运算）/`生成表达式字段.cn`（**367 行**：生成方法接收者 + 生成字段访问 +
> 发射成员取值 + 重取成员对象基址 + 对象槽是类对象 + 成员载荷是结构体）+ **`IR/包.cn` 注册 2 新模块**。
> **v2 侧拆分三约束（本轮勘定，重要）**：①静态变量=**文件级私有**（族不能跨静态耦合——本文件 0 静态 ✓ 故选它开波；
> IR容器.cn 有 12 个静态变量且 `IR追加/建IR指令` 被 8 模块共用——需先做「工具函数下沉基础模块」才可拆）；
> ②**同包「多文件合并全局可见」**——函数跨文件互调无障碍（无需改导入）；③新模块须在 `包.cn` 按加载序注册。
> ②**验证（v2 侧新口径）**：v2p 重建成功（零错误）+ **v2 产物 4 样本逐字节一致**（hello/221/253/259——v2 编译器
> 行为不变的直接证据）+ **锚定链固定点 406097 行不变**（v2 拆分不改语义 → 自编译产物逐字节不变）+ 对拍 **36/36**
> （**组件数 34→36——新文件自动纳入对拍面**）+ 单测 1313/1313 + E2E 290/292 + 运行级 3/3。
> ③诚实边界：v2 侧余 **5 文件**（IR容器 1918〔静态耦合，需工具下沉先行〕/语义检查 1242/生成语句 1188/
> IR生成 1171/代码生成 1067）登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
