# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十二轮 122-a：D1 行数整改第十三波（v2 侧）——`语义检查.cn` 1243→787 行**（2026-09-14 凌晨，家机 win-x64；基线 eebe438）：
> ①**拆分**（纯重构零行为变更）：`语义检查.cn` **1243→787 行**，拆出 `语义检查语句.cn`（**469 行**：语句类检查族——
> 检查推断声明语句 + 检查变量声明语句 + 检查赋值语句 + 检查复合赋值操作数 + 登记跳过区间）+ `语义/包.cn` 注册；
> **静态变量归属勘定**（本文件 3 个文件级静态：当前函数返回类型/当前函数引用参数们/当前函数常量引用们）——
> 拆出的族**不直接访问任何静态**（仅经函数接口）✓，静态使用者（检查语句/检查函数等 8 函数）留主文件。
> ②**验证（v2 侧口径）**：v2p 重建零错误 + **v2 产物 4 样本逐字节一致** + **锚定链固定点 406097 行不变** +
> **对拍 40/40**（组件 39→**40** 自动纳入）+ 单测 1313/1313 + E2E 290/292 + 运行级 3/3。
> ③v2 侧进度：**生成表达式/生成语句/代码生成/语义检查 四文件达标**；余 **2 文件**（IR容器 1918〔静态耦合——需
> 「工具函数下沉」先行〕/IR生成 1171〔11 静态〕）+ 宿主 `ast.hpp` 1032 + 函数级 19 超百行。
>
> **⚠️ 待用户裁决（最高优先级 actionable）**：`plans/022` §九 **8 项**（含第 8 项「波 1 修 `字符*` 拼接误报——可立即开工」）——
> 裁决后按 §七 波次路线推进。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
