# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🏃 317-a 清零波次4 部分=T19②+T20①·2026-09-18·基线 fa1db7c）

- **T20① 根治**：泛型函数体重放检查基建（recheckGenericFuncBody·A7 同构）+实例 srcType 物化（向量$整32）——体内方法调用从「间接调用 0」段错误转正确解析。
- **T19② 根治**：推断式泛型调用单态化（保守判据推断段+共用 helper+不动点实例循环）——体内嵌套 恒等(值) 链接爆修复。
- **两 UB 根治**：functions_ 迭代器悬垂（值拷贝）+genericFuncInstances_ vector 扩容悬垂（值拷贝传入）。
- **E2E 421** 转正（O0=O3）；T19①③ 续轮登记（诚实边界）；T20② win 免疫实证。

**门禁**：零警告构建+单测+全量 E2E（438→439 含 421）+双文档门禁；双推 gitcode+github。


## 深度机 linux-x86_64 节

## 功能完善总结（🧪 314-a M3 采样轮 x64l 列首测·2026-09-17，基线 a33e3aa）

- **M3 采样双域零命中**：第五种子域 310000001×500（33s）+第六 311000001×1000（70s）差分 O0/O3 全一致——x64l 列比 win 快 ~19 倍（后续轮可加量）。
- **负向轨 400**：400/400 全拒绝·0 崩溃·0 行号偏离（连续第五轮）；**回归轨全库 124 件**：命中全旧类零新类别（T27×8+T37/T38/T42）·跨平台呈现差注记（m27_02~05 x64l 分歧/win 一致·T27 平台分叉口径）。
- **T44 x64l 复验**（家机点名）：三元族双级 SIGSEGV（asm `mov [rbp],r10` 高半偏移 0=GAS 版 rbp0 同根因）·双分支族 x64l 免疫（静默错值=win 单侧）·三前驱✓——T44 行证据回填。
- **★T45 新立案**（T44 复验 B9 联想扩面撞出）：128 位字面量实参 ABI 契约分叉（调用侧值直传低半 vs 被调方指针解引用）→双级 SIGSEGV/静默错值 UB 双态；挂面=字面量（含折叠）实参×7 形态全挂·变量/表达式/结构体实参健康；实弹 run_err/m45_01/02+面台账行 82+方案甲/乙/丙（推荐甲+丙）；win/arm64 复验归家机/单位机。
- **HANDOFF 深度机节标题六连写粘连事故根治**（306-a 引入·本轮整节替换消除）+**check_handoff 门禁盲区登记**（未拦「标题连写」形态·待开发轮扩条）。
- daemon 已按 313-b 新版（日文件制）重启常态化采样。

**门禁**：check_handoff/check_progress_sync/check_matrix_coverage（82 行）三绿；双推 gitcode+github。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
