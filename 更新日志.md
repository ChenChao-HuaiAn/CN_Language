# 更新日志

## 功能完善总结

### 呈报①②方案A实施轮：cn check 假绿根治 + 常量诡异触发归因反转根治（2026-09-05 第十五轮，家机 win-x64，用户裁决两项均方案A）

#### 一、呈报②方案A：cn check 非入口文件假绿根治（簇⑥，E2E 151 红→绿）
- **根因**：`mergeModules` 以「模块名==主 + 单模块特判」判定入口——命令行入口为非主.cn 且含导入时被误判为被导入模块，私有函数体被模块可见性过滤静默丢弃，「检查通过」实为没检查（E2E 26 与簇⑥ 同根两发作，第一次修法是症状补丁）。
- **修复**：`ModuleUnit::isEntryUnit` 命令行入口标记（loadModuleTree 根调用 `isRootCall=true` 写入，递归依赖恒 false）；mergeModules 据此判定，文件名判定与单模块特判一并归一删除；isEntryModule 保留为规范08-四 文件名约定查询（非入口语义依据）。Rust/C++ 对照语义落地：可见性只影响 import 消费方视角，编译自身文件永远全量。
- **E2E**：run_e2e.py 新增 `期望check失败.txt` 负用例通道（与 期望编译失败.txt 同构：首行被检文件名+次行期望诊断子串）；新增用例 **151_check非入口全量检查**（非主.cn+导入本地依赖+私有函数含未声明标识符，check 必须报错），修复前红（假绿被抓）、修复后绿。

#### 二、呈报①方案A：v2「同值常量诡异触发」归因反转 + 根治（六链冒烟全绿）
- **归因反转（重大）**：原「IR常量.cn 新增任意常量对使三链冒烟多 1 条诊断」定性**完全错误**。现场 DBG 三层下钻（解析方法对象类型 miss→节点值全部正确→基类型 函数IR 解析成功→**查结构体大小=0**）+ 结构体登记流水实证（冒烟链全程仅登记 Token/方法签名）锁定真根因=**IR容器.cn 反向引用 IR生成.cn 的 IR指令/函数IR 结构体**——IR生成 导入 IR容器=反向不可导，主链靠多文件合并全局可见掩盖；冒烟子链（语法分析/语义分析/IR容器 入口，链上无 IR生成.cn）登记源缺失→`函数信息.容器字段局部们.元素(i)` 方法对象解析 miss→「方法调用的对象类型未知: 元素」误诊。
- **潜伏实证**：v2@13 基线复核（checkout 9efb5d8 重编 v2p）同样红——缺陷自 IR容器.cn 诞生（P7b 2026-09-01）起长期潜伏：历史冒烟只统计 sem5，ir2 计数纪律第十三轮才照见，第一次照见即被归因给当轮新增的常量对；第十四轮消融「删常量对不触发」系陈旧产物/检查面假绿。
- **根治**：IR指令/函数IR 结构体定义自 IR生成.cn **下沉 IR容器.cn**（引用方 IR生成/代码生成族/主 的导入链恒含 IR容器——登记源在任意子链可达；程序IR 留 IR生成.cn 因其引用方链恒含 IR生成）。
- **原子化消融证伪**：修复版+T_测试甲=98/乙=99 常量对→四链 0 诊断（快照→单变量→回滚）——「常量对是开关」假设正式证伪。
- **连带根治**：miss 根治后语义分析链跑得更远，暴露**结构体初始化第三形态**（`符号 找到 = 查.值;` 结果<T>.值 整体接收=成员表达式）不在白名单——扩 节点_成员 形态（生成表达式 成员分支压字段地址→IR_结构体拷贝 源地址）。
- **验证**：六链冒烟（语法分析/语义分析/IR容器/IR生成/IR签名/词法分析）全部 exit=0+ir2=0+asm 产出+**ml64 汇编全过**；IR生成链 asm 6.36MB 与十三轮基线一致——第十四轮「冒烟 asm 断供」就此恢复。

#### 验证
构建零警告（MSVC /W4 /WX）+ 全量单测 PASS + 全量 E2E（含新增 151）——78/79 已知 OOM 家族与基线一致零回归。测试面披露：test_module.cpp makeUnit 加 `isEntryUnit=(moduleName=="主")` 一行（单测直构图模拟命令行入口，判定源等价迁移，断言零变化）。
