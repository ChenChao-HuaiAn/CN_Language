# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十八轮 128-a：D1 函数级续波——`injectContainerElemDestroy` 326 行提取映射族**（2026-09-14 凌晨，家机 win-x64；基线 3b1b09e）：
> ①**函数级重构**（纯重构零行为变更）：`injectContainerElemDestroy`（**326→262 行**）——提取映射族
> （**55 行** → `injectMapElemDestroy(canonClass, mi, loc)` 私有方法）；**新模式**：「**if 包 + return**」——
> 主函数 `if (canonClass.rfind("映射$", 0) == 0) { injectMapElemDestroy(...); return; }`，子方法内
> 各 `return;` 保持——**语义严格等价**（原族内所有路径均 return，由外层 if 块的 return 承接；**比返回布尔模式
> 更简洁且无「末尾 return false 不可达」的 C4702 风险**）；
> ②**验证**：构建零警告 + **产物 md5 4 样本逐字节一致** + **锚定链固定点 406097 行不变** + 对拍 44/44 +
> 单测 1313/1313 + E2E 290/292 + 运行级 3/3；
> ③**踩坑（入 lessons）**：**多行声明的插入锚点必须取「声明末行」**——本轮 hpp 锚匹配了 3 行声明的首行 →
> 声明被插入到多行签名中间（C2143/C2079 连锁）；修复=锚定位到含 `const SourceLocation& loc);` 的末行。
> 另：**「if 包 + return」模式**（void 子方法）适用于「族内全路径 return」的独立块——比 bool 模式更省（无
> 冗余布尔 + 无不可达 return 风险）；**bool 模式**（126/127-a）适用于「族是顺序尾、后面还有主流程」的情形。
> ④D1 剩余：函数级 **16** 超百行（injectContainerElemDestroy 余下族〔移动元素/字符串元素/含串字段/有析构类〕
> 可续拆；`visitCallExpr` 885 / `visitAssignmentExpr` 931 等）。
>
> **⚠️ 待用户裁决（最高优先级 actionable，已十一度呈报）**：`plans/022` §九 **8 项**（含第 8 项「波 1 修 `字符*` 拼接误报——可立即开工」）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
