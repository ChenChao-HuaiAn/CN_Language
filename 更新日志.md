# 阶段P7b续：新规则三项裁决落地 + 链式字段取值根治（2026-09-01）

**背景**：用户令两条新规则入 AGENTS.md——①v2 自举双目标（性能超 C++ 编译器/安全接近 Rust）；
②缺陷零容忍·强制汇报（任何遗留缺陷必须暂停汇报 ≥3 方案经裁决）。

## 一、新规则首次执行：链式字段取值缺陷根治（方案 A）

`向量.元素(i).字段` 读错值——lvalueAddress 与 visitMemberExpr 不认 CallExpr 对象
（类型推导空→防御 return base 丢字段偏移→LoadPtr 读结构体首 8 字节拼合值）。两处补
CallExpr 分支经 exprSrcType 解析返回类型。**历史三次同族缺口（赋值/链式方法/字段访问）
全为漏 CallExpr**——lessons 已记六形态核查清单。E2E 124 PASS。

## 二、三项轻微简化逐项裁决落地

1. **项3 任意参数数（完成）**：IR_调用实参=64（声明性装载）+ IR_调用参数化=63 统一
   发射（寄存器位+栈参数区+被调方 [rbp+48+8k] 双向）；6 参实测 655308 ✓。
2. **项2 块级作用域析构（完成）**：生成块 作用域基线 + 块出口析构（逆序 RAII+截断）；
   嵌套同名容器语义层防护；循环/分支块实测 356 ✓。
3. **项1 stdlib 签名表（过渡态+缺陷立案）**：IR签名.cn（新组件）扫描器+查询+类型参数
   替换全部就绪；**发现 v2p 大帧函数字符串参数跨调用 UB**（asm 全对/最小复现失败/
   破坏位置随机漂移）——按新规则**暂停并立案**（plans/014 三表），查方法签名 回退
   内置表保绿，待根治后切回。

## 三、验证

全量门禁：构建零警告 + 单测 1196/1196 + E2E 126 用例 124 通过（仅 78/79 已知遗留）；
119/120/123/124 全绿。v2 组件 9 个（+IR签名.cn），行数全部 ≤1000。
