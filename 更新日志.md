# 更新日志

## 功能完善总结

# plans/016 第九节：v2p 本机冒烟全 12 组件完整编译 + linux_x64 参数拷贝临时寄存器缺陷根治（2026-09-05 深度系统 x86_64，方案A 用户裁决）

## 一、拉取 7650713（第十七轮 家机呈报复核轮）本机门禁复验

- 构建零警告（GCC 12.3 -Wall -Wextra -Werror）+ 单测 1238/1238 + E2E 全量 154 用例 127 过/0 败/27 平台跳过——与第十六轮基线完全一致，第十七轮删除性变更（win 第4路死代码归真）linux-x86_64 侧零回归实证。

## 二、里程碑：v2 自举编译器全 12 组件首次完整编译（本机 linux-x86_64）

- 宿主一次编译 v2 全链（主.cn 导入 12 组件 8441 行）构建 v2p 可执行成功——linux-x86_64 后端扛住自举编译器规模的真实代码。
- **v2p 冒烟最后四组件全绿**（HANDOFF 下一步② 完成）：
  - 代码生成共用.cn：语义 0 错 + asm 365641 行
  - 代码生成ARM64.cn：语义 0 错 + asm 380544 行
  - 代码生成.cn：语义 0 错 + asm 381453 行
  - **主.cn：语义 0 错 + asm 441591 行（v2 编译 v2 自身的完整产物）**
- 主.cn 缺口 `系统::参数个数/系统::参数` 未注册当轮收口：IR容器.cn 内置函数符号表 + 返回类型ID 表按第五波限定名模式扩两名（`__cn_argc`/`__cn_argv`；宿主 ir_call.cpp:251~252 与 semantic.cpp regSysFn 对齐）——语义/IR 层零改动（表驱动自动放行）。
- v2p 产物汇编验证（MASM=家机 ml64 / GAS arm64=单位机）待跨机；「v2 编译 v2 自身」可运行对拍=后续专项（v2 linux-x86_64 代码生成，需立项呈报）。

## 三、宿主缺陷根治：emitParamSetup 参数拷贝临时寄存器 r9 冲突（缺陷零容忍呈报，方案A）

- **现象**：v2p 冒烟 词法分析.cn 段错误 exit 139（IR 生成中段）；同源码 win/arm64 产物六链全绿。
- **根因**：`LinuxX64CodeGenerator::emitParamSetup` 结构体按值参数拷贝循环与 i128 参数拷贝用 r9 当数据临时——r9 是 SysV 第 6 整型参数寄存器；参数在前 + 整型参数满 6 时，第 6 参数被拷贝残留覆盖（v2「生成如果」：指令引用=参数6 被覆盖为节点池容量 41，作指针解引用崩）。gdb 五层下钻闭环（崩点→槽→prologue→调用点→上层 prologue）。
- **三后端对照**：win（rsi/rdi/rcx rep movsb，参数寄存器 4 个）与 ARM64（x10/x11/x12）结构性安全——仅 linux_x64 冲突（拷贝临时恰在 6 参数寄存器集合内）。
- **修复（方案A）**：三处拷贝数据临时 r9→r11（prologue 阶段 r11 无职责；与 ARM64 x10/x11/x12 同构）。
- **验证**：E2E 新增 **153_linuxx64_参数拷贝临时寄存器**（结构体按值/i128 两形态 × 满 6 参数位）**红→绿→红→绿闭环**（修复前两形态均输出错值 706549=拷贝残留，修复后 706554/706557）；单测新增 StructParamCopyMustNotClobberR9。

## 四、门禁（第十八轮最终）

- 构建零警告 + 单测 **1239/1239**（原 1238+1）+ E2E 全量 **155 用例 128 过/0 败/27 平台跳过**（新增 153 PASS）。
- 文档：plans/016 第九节 + plans/014 状态表第十八轮行 + lessons 高权重条目（权重 9.5）+ HANDOFF 第十八轮。
