# 阶段P7a：v2 顶层常量全链路 + 宿主四缺陷根治（2026-09-01）

**目标**：HANDOFF「下一步：v2 编译自身（自举闭环）」第三步之①——顶层 `常量 名 = 值` 声明全链路（v2 组件源码 84 处顶层常量是自举硬缺口）；途中暴露并根治 4 个宿主缺陷（宿主缺陷优先，暂停自举先治宿主）。

## 一、宿主缺陷根治（4 项，全部最小复现 + E2E 回归）

推进 P7a 时发现 **E2E 119 从 P6h 起在本机从未真正通过**（P6h 会话在 Linux 无法运行 v2p，CI「通过」是误信）——v2p 编译自身组件链时接连崩溃。逐层定位出 4 个叠加宿主缺陷：

### 1. 无初始化器顶层静态容器「双模型不一致」（v2 崩溃主因）
- **现象**：`静态 向量<整32> 表`（无初始化器）顶层声明后，复制/赋值/方法调用全错——`运行时错误(3) 空指针`（v2 的 `取走导入模块们` 原崩溃点），最小复现 5 行。
- **根因**：宿主对类静态按「有无初始化表达式」分裂两种模型——**有**初始化 = NewObject 后 StorePtr 指针入 .data 槽（指针槽模型）；**无**初始化 = 对象内联 .data 本体 + 构造函数打在符号地址（对象内联模型）。而标识符读取路径统一 LoadPtr（只对指针模型正确）——无初始化静态被读出**首 8 字节字段**（如 数据 指针）当对象指针。
- **根治（统一指针槽模型，5 处）**：ir_decl.cpp 主 入口统一 NewObject+无参构造+StorePtr 指针入槽；x64_codegen .data 类静态只分配 8 字节指针槽（结构体静态保持 typeSizeOf 值语义）；ir_oop_call 删除「方法接收者传符号地址」特例（genExpr 通用路径即正确）；ir_stmt genVarDecl 拷贝构造支持静态源（srcAddr=静态槽地址，与局部变量槽同构）；ir_expr 类静态赋值深拷贝（右值标识符时 NewObject+拷贝构造，防共享指针悬挂）。

### 2. 分支未执行的类局部被无条件 RAII 析构
- **现象**：`如果 (假分支) { 向量<整64> 分支局部 = ... }` 分支未走时，函数收尾仍析构该局部——**未初始化栈垃圾上的 ~向量 野指针**，5 行最小复现 100% 崩溃；叠加前序调用污染栈后必崩（新鲜零页栈时侥幸为空而「通过」）。
- **根因**：genClassDestructorCalls 函数末扫描**全部** Alloca（不分作用域/执行路径），把析构追加到每个返回块。
- **根治（2 处）**：genClassDestructorCalls 在**函数入口块最前**统一零初始化全部类局部槽（Store 空指针——声明处初始化在未执行分支里不运行，入口零初始化才与路径无关）；codegen DeleteObject 空安全跳过（x64 `test/je` + arm64 `cbz`，双后端）。

### 3. 块顶层级中途 返回 被无视（死代码照跑）
- **现象**：`返回 5` 后跟语句与 `返回 7`——实际返回 7（首个返回的块级终止被后续语句覆盖，全部语句进同一线性块顺序执行）。
- **根治**：genBlock 在当前块已终止（返回/中断/继续）后跳过剩余语句（死代码不生成）。

### 4. stdlib 文件.cn / IO.cn 漏 `导入 核心::错误码`
- **现象**：程序只 `导入 文件::*`（不导入 容器）时编译失败（`未声明的标识符 '错误码'`）——错误码 定义于 核心.cn，此前仅当程序先导入 容器（容器 导入 核心）时符号才侥幸可见。
- **根治**：文件.cn / IO.cn 补 `导入 核心::错误码`（与 容器.cn/映射集合.cn 同款）。

### 已知遗留（记录待修）
- **容器方法返回结构体的链式字段访问**（`向量.元素(0).字段`）读错值/崩溃（两步写法 `取出 = 元素(0); 取出.字段` 正确）——v2 组件源码 0 处使用、无 E2E 覆盖，未阻塞自举；复现于 target/dbg/c9、c4d，下一轮根治。

## 二、P7a：v2 顶层常量声明全链路

- **词法分析.cn**：关键字表加「常量」（第 25 个关键字）。
- **语法分析.cn**：`常量 节点_常量声明 = 31`（文本=名ID，子区间=初始化表达式）；解析常量声明（`常量 名 = 表达式`，负数自然成 [字面量, 负号] 区间）；解析程序 顶层分支 + 解析语句 局部常量分支（按 整64 局部变量处理，防新关键字停滞解析）。
- **语义分析.cn**：`常量 符号_常量 = 5`；声明收集注册（类型ID=整64）——多文件两趟结构天然跨模块可见。
- **IR生成.cn**：模块级 静态 常量表（名ID→值文本ID），IR生成/IR生成多文件 入口清空+收集；折叠零数值解析（正值复用字面量 Symbol ID、负值 `驻留("-"+文本)`）；生成表达式 标识符在参数槽/局部槽未命中后、函数名兜底前查常量表内联 `IR_常量整数`（局部遮蔽语义正确）。
- **代码生成.cn / 主.cn**：零修改。

## 三、E2E（新增 3 例，119 为真实首次通过）

- **119_v2_多文件链接闭环**：修复宿主缺陷后**真实通过**（此前从未真正执行成功——见 lessons）。
- **120_v2_顶层常量**（新增）：v2p 编译 主.cn+常量库.cn（跨模块常量/本文件常量/同模块常量引用/负常量/优先级）→ ml64/link → 运行 退出码 62。
- **121_宿主缺陷_静态容器与分支局部析构**（新增）：缺陷 1/2/3 三合一验收（静态容器全链路 + 分支未执行局部析构安全 + 中途返回生效）。
- **122_宿主缺陷_stdlib模块独立导入**（新增）：不导入 容器、单独使用 文件 模块（缺陷 4 验收）。
- **run_e2e.py**：执行119v2闭环 参数化为通用 执行v2闭环（119/120 共用，119 断言语义不变）。

## 四、验证

- 最小复现全绿：c2（静态复制）、c3（组合静态 216）、c10b（中途返回=5）、c18（分支局部不崩）、c11/c12（文件读+字符串返回）、c13/c14/c16（签名/读源文件镜像）。
- **全量门禁 ci.ps1（Windows 本机）**：构建零警告（/W4 /WX）+ 单测 **1196/1196** + E2E **124 用例 122 通过**（仅 78/79 内存超限为文档记载的旧架构已知遗留，与本轮改动无关）。
- E2E：119（真实首次通过）/120/121/122/118/01 全部 PASS。
- v2p 默认路径与 119 入口全链路（解析/语义/IR/代码生成/落盘）干净退出 0。

## 五、后续

HANDOFF 第三步之②：**容器类型 向量<T>/映射<...> 实例化与容器方法调用**（v2 组件源码大量使用 .追加/.设置/.获取/.元素/.大小，当前 IR 层 运行时错误(7)）；③ v2 编译 7 组件 → 7 asm → 链接 v2p2 对拍（对标 79）。另：链式元素访问缺陷根治（上文遗留）、P4 代码清理（结果.值 临时绑定规避可去除）。
