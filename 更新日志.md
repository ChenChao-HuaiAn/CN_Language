## 功能完善总结

### 阶段3「OOP 与错误处理」全部实施完成（2026-08-14，9 个 Task 全完成）

**里程碑**：阶段3 全部 9 个 Task（3.1~3.9）实施完成并修复全部缺陷，全量验证通过：**单测 901/901、E2E 28/28、编译零警告（/W4 /WX）**。可编写中型面向对象程序（规格书阶段三目标达成）。

#### 9 个 Task 完成情况

| Task | 内容 | 状态 | 验证 |
|------|------|------|------|
| 3.1 | 类与继承（类定义/实例化/字段/方法/单继承/构造析构/自身/父类） | ✅ | E2E 20 |
| 3.2 | 虚函数与多态（虚拟/重写/父类指针虚调用/父类.方法()） | ✅ | E2E 21 |
| 3.3 | 接口与实现（接口定义/类实现多接口/接口虚调用） | ✅ | E2E 22 |
| 3.4 | 访问控制（公开/保护/私有标签式可见性） | ✅ | E2E 23 |
| 3.5 | 错误处理（结果<T,E>/可选<T>/内置构造器/3条强制检查规则） | ✅ | E2E 24 |
| 3.6 | 模块系统（文件即模块/导入/从导入/公开私有可见性/主.cn 入口/依赖拓扑排序） | ✅ | E2E 27 |
| 3.7 | 运算符重载（上下文关键字 运算符/重载决议/降级为方法调用/链式表达式） | ✅ | E2E 25 |
| 3.8 | 泛型与模板（泛型类/函数/单态化/多类型参数/接口约束） | ✅ | E2E 26 |
| 3.9 | 类成员增强（静态字段/静态方法/常量成员函数/友元） | ✅ | E2E 28 |

#### E2E 用例（编号顺延方案，用户确认）

| 编号 | 目录 | 覆盖 Task | 关键语法点 |
|------|------|----------|-----------|
| 20 | `20_oop_class` | 3.1 | 类/实例化/字段/方法/单继承/构造析构/自身/父类/类对象深拷贝 |
| 21 | `21_virtual` | 3.2 | 虚拟/重写/父类指针虚调用/父类.方法()/多级继承虚表 |
| 22 | `22_interface` | 3.3 | 接口定义/类实现多接口/重写接口方法/类实例虚调用 |
| 23 | `23_access` | 3.4 | 公开/保护/私有标签/子类访问保护 |
| 24 | `24_error_handling` | 3.5 | 结果<T,E>/可选<T>/正常/错误/某些/无/.正常/.值/.错误/.有值/双分支检查 |
| 25 | `25_operator` | 3.7 | 运算符+/==/!= 重载/返回类对象/链式表达式/字符串拼接不受影响 |
| 26 | `26_generics` | 3.8 | 泛型类单态化/盒子<整32>/<字符串>/多类型参数/浮点字段/方法返回 T |
| 27 | `27_module` | 3.6 | 多文件 主.cn+数学.cn/导入/从导入/公开符号跨模块/私有依赖闭包 |
| 28 | `28_class_members` | 3.9 | 静态字段/静态方法/常量成员函数/友元授权/静态字段自增/引用参数 |

#### 12 项编译器缺陷（E2E 实测发现，全部已修复）

1. **继承析构符号不一致**（codegen）：DeleteObject 硬编码 `~类名`，子类无自身析构时符号为 `子类$~父类析构名` → 沿继承链解析实际析构名
2. **虚表段指令 `.rdata` 非法**（codegen）：ml64 只读段指令为 `.const` → A2008 修复
3. **内置构造器未降级**（IR）：正常/错误/某些 被当外部函数调用 → 链接失败；实现合成结构体构造
4. **构造器返回上下文推断**（semantic）：`返回 错误(5)` 从当前函数返回类型 结果<T,E> 推断
5. **结果/可选合成结构体映射**（semantic）：模板类型名映射合成结构体 `结果$T$E`；.值/.错误 映射内层联合体
6. **`返回 无` 空可选崩溃**（IR）：`无` 常量 0 被当返回地址 → 构造空可选结构体
7. **模块合并遗漏泛型**（module）：泛型声明未合并 → 增加 generics 合并
8. **单模块非 主 文件判定**（module）：单模块时视为入口，泛型/私有不被过滤
9. **varDecl 泛型类型替换时序**（semantic）：初始值检查先于类型替换 → 替换提前
10. **IR 层泛型类型映射**（IR）：`盒子<整32>` → `盒子$整32`（字符串映射）
11. **静态字段符号未修饰**（codegen）：IR 生成原始中文符号 → ml64 A2044；应用 nameMangle
12. **parser 泛型实例化不改名**（parser）：消费 `<实参>` 后 IdentifierExpr 名字更新为 `名<实参>`

#### 5 项 Debug 全面审查缺陷（2026-08-14 Debug 子任务，全部已修复 ✅）

| # | 缺陷 | 根因 | 修复方案 |
|---|------|------|---------|
| 1 | 类对象指针共享 + RAII 重复释放堆损坏（0xC0000374） | 类对象变量槽存堆指针，赋值浅拷贝 Store 源指针，两变量共享同地址，RAII DeleteObject double free | genVarDecl（声明初始化）+ visitAssignmentExpr（赋值）两处改为 NewObject+CopyStruct 深拷贝 |
| 2 | 运算符重载链式（甲+乙+丙）中间对象字段错乱（2564+128i） | IR 层 exprSrcType 不识别内层 BinaryExpr，外层 + 落 ptr+ptr 字符串连接 | BinaryExpr 新增 resolvedType 字段，语义层写回重载结果类型、IR 层读取 |
| 3 | 泛型带参构造字段写入失效 / 多类型参数 / 浮点字段 | ①构造查找用 className（实例化名）find 失败；②方法参数类型用 AST typeName（类型参数 T） | ①遍历 methods 找 isConstructor；②setupMethodParams 用 mi.paramTypes（已替换实参） |
| 4 | 模块公开函数体引用私有符号 → 合并后未声明 | 被导入模块仅合并公开声明，公开函数体依赖的私有函数被过滤 | mergeModuleDecls 增加私有依赖闭包（AST 遍历收集 + 迭代到不动点） |
| 5 | 静态字段自增（总数++ 恒 0）/ 友元引用参数（账户& 报错） | ①类字段不在 varStack_，自增只读不写；②canonical 不剥引用后缀 & | ①新增 handleClassFieldIncDec 读-算-写回；②types::canonical/substTypeParam 剥 & |

> 注：缺陷 5 中的"&表达式传参读 0"经最小复现验证本就正常（输出 42），非缺陷。

#### 修改文件清单（Debug 审查子任务）

| 文件 | 变更 |
|------|------|
| [`src/cn_compiler/ir/ir_oop.cpp`](src/cn_compiler/ir/ir_oop.cpp) | setupMethodParams 用 mi.paramTypes；exprSrcType 增加 BinaryExpr 分支；新增 handleClassFieldIncDec |
| [`src/cn_compiler/ir/ir_oop_call.cpp`](src/cn_compiler/ir/ir_oop_call.cpp) | 构造查找改遍历 isConstructor |
| [`src/cn_compiler/ir/ir.cpp`](src/cn_compiler/ir/ir.cpp) | 类对象赋值/初始化深拷贝；visitUnaryExpr 接入 handleClassFieldIncDec |
| [`src/cn_compiler/ir/ir.hpp`](src/cn_compiler/ir/ir.hpp) | handleClassFieldIncDec 声明 |
| [`src/cn_compiler/parser/ast.hpp`](src/cn_compiler/parser/ast.hpp) | BinaryExpr 新增 resolvedType 字段 |
| [`src/cn_compiler/semantic/semantic.cpp`](src/cn_compiler/semantic/semantic.cpp) | visitBinaryExpr 运算符重载命中写回 resolvedType |
| [`src/cn_compiler/semantic/type_system.cpp`](src/cn_compiler/semantic/type_system.cpp) | canonical 剥引用后缀 & |
| [`src/cn_compiler/semantic/generics.cpp`](src/cn_compiler/semantic/generics.cpp) | substTypeParam 增加引用后缀递归替换 |
| [`src/cn_compiler/module/module.cpp`](src/cn_compiler/module/module.cpp) | 私有依赖闭包收集 + mergeModuleDecls 闭包合并 |

#### 补强的回归测试

- **E2E 加强**：20（类对象深拷贝）、25（链式运算符）、26（带参构造+多类型+浮点字段）、27（私有依赖闭包）、28（静态自增+友元引用参数）——5 个用例的 .cn 源文件 + .expected 已追加边界场景。
- **单测新增**：`tests/unit/module/test_module.cpp` 新增 `MergePrivateDependencyClosure`（模块私有闭包）。

#### 测试基线（最终）

| 指标 | 数值 |
|------|------|
| 单元测试 | **901/901**（73 个测试套件，0 失败） |
| E2E | **28/28**（19 既有 + 9 阶段3 新增，无回归） |
| 编译警告 | 0（/W4 /WX，clean-first 全量重编译验证） |
| 优化级别回归 | -O2/-O3 下 5 项缺陷复现程序全部正确 |

#### 组件集成确认

5 项缺陷均通过 `cn.exe run` 真实编译+汇编+链接+运行验证（非孤立组件测试），覆盖 driver 多文件流水线（缺陷4）、语义单态化（缺陷3）、IR 指令发射（缺陷1/2/5）、codegen 汇编（虚表/静态字段/深拷贝 CopyStruct/引用参数）、运行时（对象内存）。
