# CN语言编译器 更新日志

## 功能完善总结

# 第四十八轮（2026-09-09 家机 win-x64）：观察项 46-a/46-b 双侧根治——成员结构体值语义（宿主写静默损坏 + v2 读段错误）+ 手写链式读 + 46-c/46-e 定性立案

按第四十七轮 HANDOFF 挂账二.2 根治第四十六轮立案观察项（46-a 成员结构体作值读写段错误为最高优先，内存安全面）。E2E 先行：先建最小探针矩阵复现，再根治，最后转正 E2E 188/188_v2 双通道。

## 一、立案定性修正（消融探针矩阵 5 形态 × 双侧）

46-a 原立案定性「v2 既有缺陷」**不准确，实为双侧缺陷**：

| 形态 | 宿主（修复前） | v2（修复前） |
|---|---|---|
| A 写 `r.左上 = a` | **静默垃圾**（求和验证 125818528≠132） | PASS（第四十六轮顺带已修） |
| B 声明位读 `点 t = r.左上` | PASS | **段错误 0xC0000005**（立案 rc=139 的 win 同族实锤） |
| C 赋值位读 `u = r.左上` | PASS | 可见失败（右值限定标识符） |
| D 成员→成员 `r.右下 = r.左上` | **静默垃圾** | PASS（8 字节巧合覆盖） |
| E 成员实参（回归锚） | PASS | PASS |

- **宿主缺陷根因**：MemberExpr 赋值目标无结构体整体赋值通道——标量通用路径 StorePtr 只写 8 字节（源地址截断入首字段+其余字段栈残留垃圾）。**静默数据损坏比段错误更危险**（内存安全违例）。
- **v2 缺陷根因（B 段错误）**：生成结构体局部 对成员源调 生成表达式——成员分支 LoadPtr 取 8 字节**值**被下游当**源地址**做 IR_结构体拷贝=解引用垃圾地址。

## 二、宿主修复（Rust place 拷贝同构）

- 提取 **emitStructWholeAssign 单一事实源助手**（ir_expr.cpp + ir.hpp 声明）：源三形态取址（IndexExpr/MemberExpr→lvalueAddress 内联地址；IdentifierExpr→结构体 AddrOf 槽/类 Load 槽）+ 类拷贝构造深拷贝 + CopyStruct 按语义大小整体拷贝。
- **visitAssignmentExpr 双分支接入**：MemberExpr 目标分支（D2 构造字面量特判后新增，字段为结构体/类→整体赋值）+ IndexExpr 目标分支（H8 类补完机制原位改调助手，行为逐点不变）。

## 三、v2 修复（四处联动，Rust 同构：结构体值以地址传递的既定约定）

1. **成员载荷是结构体 扩条件**（生成表达式.cn）：成员字段类型是结构体即压字段地址不 LoadPtr（原仅 结果.值 特判）；实参通道回扫「字段地址形态」天然兼容，E 回归锚 PASS。
2. **生成字段访问 对象形态补 节点_成员 分支**：解析成员链结构体ID 递归查 偏移/宽度/类型码 三元组（46-b 读路径根治）。
3. **顺序链成员地址 偏移兜底统一换 解析成员链结构体ID**：多级左值链（a.b.c=地址）同步打通。
4. **生成结构体整体赋值 右值扩 节点_成员 形态**（右末自身类型同型校验+生成左值地址源）+ **生成赋值语句 成员左值分支结构体整体拷贝拦截**（左右两侧生成左值地址+IR_结构体拷贝——原标量宽度通道对 8 字节结构体恰好覆盖、**>8 字节静默截断=修复过程披露的既有缺陷家族**，一并根治；构造字面量右值不拦截，186 通道不受扰）。

## 四、踩坑入库（拦截首版假阴性→父链索引误写）

- `解析成员链结构体ID(左末自身索引)` 返回**左末字段自身类型**（.右下→点=键基 29），再查一层 `查字段类型ID(点, 右下)` 必 miss=0 → 拦截永不命中（D 形态假阴性）。
- 识破手段：临时诊断打印双侧对照——语法层登记侧（结构体键基 29=点/35=矩形）vs IR 期查询侧（左链ID=29）数值吻合暴露「29 是左末自身类型而非父链类型」。
- 范本：取成员宽度码（父链=传 `m.子起点`——成员节点 子起点=对象节点索引）。已修正并在代码留注记。

## 五、验证矩阵

- **探针矩阵双侧 20/20 全绿**（修复前宿主 2/5 红=静默垃圾、v2 3/5 红=段错误+可见失败）：形态矩阵 A/B/C/D/E 5/5×2 + 消融矩阵 A2/B2/C2/D2/L1 5/5×2。
- **E2E 188_成员结构体值读写**（宿主通道：stdout 11/22/63 三锚+rc=0）+ **188_v2_成员结构体值读写**（v2 闭环：rc=63 六形态校验和 A写+1/+2、B声明位读+4、C赋值位读+8、D成员成员+16、E实参回归锚+32）双通道 PASS。
- **联合体同族旁路探针**（同族修复旁路穷尽纪律）：宿主 U1 整体赋值/U2 成员写/U3 成员读全对（rc=0，out=42/42/42，零修复需要）；v2 联合体声明语义层「未声明的类型」可见失败 → 立案 **46-e**（随类型系统/语法覆盖轮）。
- **46-c 变量关键字实证定性**：宿主 PASS（`变量 x=41 → rc=42`）；v2 可见失败（[parse] 语法错误中止不产 asm，fail fast 非静默）；v2 补齐=完整类型推断特性面（解析+语义+IR 三层联动）→ 归属语法覆盖轮，定性完成。
- 全量门禁（win-x64）：构建零警告（/W4 /WX；增量构建，ir.hpp 变更触发依赖链重编——警告检查覆盖全部改动文件）+ 单测 **1251/1251** + 全量 E2E **191 用例 189 过/2 败/0 跳**——败=78_chain_build（4135MB）/79_bootstrap_closed_loop（4135MB）已知 OOM 家族（>4096MB 自动终止防卡死），与第四十四~四十七轮基线同族**零回归**；**新增 188/188_v2 双通道用例全过**。

## 六、文档同步

- plans/014：状态表第四十八轮行 + 观察项表 46-a/46-b 更新「✅ 已根治（双侧）」、46-c 定性完成、新增 46-e。
- HANDOFF.md 重写（第四十八轮交接）。
- 探针与脚本留 target/p48win/ 不入库（探针矩阵 probe48a/b/u.py+重建 rebuild_v2p48.py+v2p_48.exe）。
