# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（689-a 定性轮·2026-09-24·波4 根因终局·wt628 复用）

**波4 根因终局定性·模型冲突**：容器局部变量声明通道=壳内联模型（7 槽 56B retbuf 拷入槽区）vs 方法接收者通道=句柄模型（Load 槽=句柄）——同变量两模型混用（层.获取 的 this=数据指针→+32 越界崩·441/147/442 同族）。修法=容器类局部声明统一句柄模型（架构级·动槽分配全域）待完整会话专项实施。

## 深度机 linux-x86_64 节

## 功能完善总结（657-a 开发轮·2026-09-23·任务/深度机-657-D43别名全链·wt657）

**021 任务 024（原D43）整数/小数内置别名 v2 全链收口销项+02_control 阻塞面真根因修复**：

1. **02_control 阻塞面真根因（机制级·随证据备案）**：413-a 检查选择语义层漏「类型过滤=节点_情况标签」——情况段区间=[情况值+体语句+体块+标签] 全部节点，无过滤把如果/当节点字段 [文本,附加)（=[条件区+真块区]）当值区间在错误作用域查名→嵌套块局部声明伪报「未声明标识符」（探针矩阵 5/5+插桩五轮实锤；反证B 撤过滤复现/恢复复绿）。修复=况i 循环加类型过滤。
2. **别名全链单一归属收口（09-20 用户裁决甲）**：内置别名基名规范化 唯一权威定义点（解析类型文本 基名级挂接·字符* 借用返回）+是别名关键字 谓词（5 处散点换接）+IR布局 是标量类型文本 不可达「整数/小数」防御分支删除（反证A=撤挂接「未声明的类型: 整数」×3 复现背书）。零 src/。
3. **02_control 转双编译对照上架 PASS**（宿主-only 存量 -1·阻塞面解除·002 D43 面清偿）。
4. 门禁：单测 1405/1405+strict 100% rc=0+matrix 83/83+E2E 全量 528=522/4/2（4 红=撤守卫态已知基线红零新增·78_v2 PASS·选择语句族零回归）+v2p 构建 4GB RLIMIT rc=0。
5. 看板棘轮清偿（深度机通告段已毕条目 201→188）；教训 2 条入册（git checkout 误丢未提交改动二犯/管道吞 rc 四犯×旧产物缓存假绿）。

## 单位机 ARM64 节

## 功能完善总结

### 596+604 双轮（604 原取 601·家机撞号让位改号）：arm64 补验收红灯根治+runner 并行恒定/进程池根治（2026-09-22·基线 develop 9a4764cf→c35b527f）

1. **596 红灯根治**（v3 补验收实锤）：v2p 自举编译器 arm64 全输入崩（空指针/段错误）——引入=525-a 两处半截改动（随 572 收拢越挂等入干）：emitFieldAddr 零偏移分支 res 垃圾+emitParamSetup 拷贝基址硬编码 [x10]；双点根治+反证双态（撤 525-a→复绿）。**456 随修转绿=fdef8ae9 P1 同根闭环**（批间分叉=未初始化寄存器垃圾二级表象·定性勘误）。
2. **604 runner 根治**（用户令质询响应）：568-a「并行恒定·删串行」从未入 develop（586-a 摘取集成遗漏 runner）——恢复 --jobs 默认 CPU 核数+删串行路径；线程池→**进程池**（POSIX 多线程 fork 死锁实锤根治·win 免疫=平台盲区）+worker 保险 1800s。
3. **终验**：arm64 全量 **535=533/0/2 首次零失败**（默认并行·约 25 分钟）+单测 1394+零警告+78/79 自举链 PASS。
4. 遗欠立账：**050**=568-a 超时防线 26 处+dual rc 恢复（从 a0ba39ab 摘取·对齐 604 进程池版）。

