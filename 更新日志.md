## v2 自举闭环第二步：链接环境对齐 + 多源文件支持（P6h，2026-08-31）

**目标**：HANDOFF「下一步：v2 编译自身（自举闭环）」的前置两步——① 链接环境对齐（v2 产物 asm 可链接进宿主运行时）；② 多源文件/导入支持（v2 编译器编译多文件程序）。

**改动（5 个 v2 组件 + E2E 119）**：
1. **代码生成.cn**：符号修饰 补 主→cn_main（extern "C" 裸符号，对齐宿主 x64_codegen symbolName）——v2 产物 asm 含 cn_main PROC，可由宿主运行时 entry 经 __cn_cache_argv 调用。
2. **语法分析.cn**：新增 扫描导入（规范 08 导入全形式：路径/::{项}/::*，轻量预扫登记模块名）+ 模块登记/取走导入模块们（静态导入表）；变量结构体表清空 外移为 清空变量结构体表()（多文件跨文件保留字段偏移推导）。
3. **语义分析.cn**：新增 语义分析多文件（全部文件声明收集 → 逐文件检查函数体；跨文件函数/结构体符号可见）。
4. **IR生成.cn**：新增 IR生成多文件（各文件 程序 节点函数们合并进同一 程序IR）。
5. **主.cn**：真实编译器驱动器——入口文件 = 命令行参数 1（系统::参数个数/参数，默认 target/v2src.cn 回归）+ 递归加载 导入 模块（依赖先解析保证结构体偏移推导顺序、已加载表防循环、标准库模块跳过）+ 共享节点池/字段偏移表；语义/IR 改用多文件入口。
6. **E2E 119_v2_多文件链接闭环**（tests/e2e/119_v2_多文件链接闭环/）：宿主编译 v2 组件（入口 主.cn）→ v2p → 编译多文件程序（主.cn 导入 计算.cn）→ v2asm.asm（断言 cn_main PROC）→ ml64 → link（对齐宿主链接命令：/ENTRY:WinMainCRTStartup + 10 个运行时 obj + CRT 库）→ 运行 退出码=14（加倍(7)）；run_e2e.py 新增 执行119v2闭环 特例（仅 win-x64）。

**验证**：本环境（Linux）宿主 Linux 版构建 PASS、全部 v2 组件 check/compile PASS（宿主编译主.cn 产物 asm 头部 cn_main PROC 与 v2 对齐）、119 用例宿主语义 check PASS、run_e2e.py py_compile PASS；**ml64/link/运行 由 CI（windows-latest）全量门禁真实执行**（119 依赖 Windows 工具链与运行时 obj）。

**后续**：HANDOFF 第三步——v2 编译自身（自举闭环），需逐步补齐组件自身语法面（字符串方法/容器方法/常量等）+ 链接 7 组件 产物。

## 复核整改：Token 布局消除历史规避（2026-08-30）

**背景**：用户要求复核因宿主缺陷而妥协的写法。发现 v2 词法分析 Token 结构体仍为 3×整64（注释：规避宿主编译器 4 字节字段 ABI 错位）。

**复核结论**：宿主 computeLayout 为标准 C 对齐（整32 字段 4 字节、整64 8 字节），探测 3×整32 结构体 按值传参/返回/向量<Token> 容器 全链路正确（7+9+100=116、传参+1=117）——**当初规避已无必要**（宿主已支持或系误判）。

**整改**：Token 改为 3×整32（类型/文本/行），赋值处补 整32() 转换（令牌常量/行号为整64）。v2p 全链路验证：读外部 阶乘(5) 语义错误数=0、asm 汇编通过；单测 1196/1196；E2E 30/96/111/01/09 PASS。

## v2 自举闭环第一步：外部文件读取 + 类型转换支持（P6f，2026-08-30）

**目标**：v2 自举闭环第一步——v2 主() 从「固定字符串源码」改为「读取外部 .cn 文件」编译；过程中补齐 v2 组件链（语法/语义/IR/代码生成）的**类型转换 类型(expr)** 支持（v2 组件自身源码 247 处使用，自举硬前置）。
