# CN语言编译器 更新日志

## 功能完善总结

**第三十五轮（2026-09-08，深度系统 x86_64）**：v2self 锚定专项治理——上轮两项挂账当轮根治，且 gdb 分步插桩实证**纠偏了上轮的根因定位**：栈溢出真根因是宿主 IR 层寄存器号跨函数累积导致栈帧线性膨胀（非解析深递归）。

### 一、③合并假歧义根治（v2 语义层）

- **现象**：v2p 编译 v2 自身（v2self），解除栈限制后报 `[sem5] 符号歧义: 货舱查找依赖/解析货舱/代码生成（来自 货舱解析/货舱解析）`——合法程序被拒，编译中止。
- **根因**（插桩实证：歧义来源=同一模块自拼接）：`模块路径们` 是**文件级**快照（每文件一条）；阶段目录包化后，候选1b 加载的包根（`代码生成/包.cn`、`货舱解析/包.cn`）与撞名成员（`代码生成.cn`、`货舱解析.cn`）共享模块路径 → `全部模块ID们` 构建时未去重 → ③ 合并全局可见遍历对**同一模块双计数**，同签名第二命中 `参串标==首参串标` 恒真 → 误判真歧义（E0659）。
- **修复**：语义检查.cn 构建 `全部模块ID们` 加 `映射` O(1) 查重——模块集合语义归一（宿主 findModule 模块表天然唯一化的 v2 对应物）。

### 二、栈溢出真根因：宿主 IR 寄存器号跨函数累积【缺陷零容忍·根因级根治】

- **纠偏过程**：上轮定位「解析深递归栈溢出，ulimit unlimited 可消除」为**误诊**——gdb 回溯实证段错误时**全栈仅 31 帧**；objdump 显示 `解析二元层` 函数帧 `sub rsp, 0x48880`（290KB），20 函数同构探针帧从 80B 线性膨胀到 1152B。
- **根因**：`IRGenerator::regCounter_` 仅在 `generate()` 全模块入口清零，**跨函数累积**；代码生成 `computeFrameSize` 按 maxRegId 定帧 → **越靠后的函数帧越大**。v2self 18 万行源码后段函数帧达 290KB，解析递归链 8MB 栈必爆；ulimit unlimited 只是让巨大帧侥幸放进内核自动增长的栈。
- **修复**（Rust 对照：rustc MIR LocalId 每函数独立；v2 编译器先例=寄存器计数每函数从 1 起）：`regCounter_` 与 `blockCounter_` 同点每函数复位，三处同改——`visitFunctionDecl`（ir_decl.cpp）、`emitClassMethod`（ir_oop.cpp）、泛型实例生成（ir_generic_func.cpp）。虚拟寄存器是函数内 SSA 值，跨函数仅经符号名引用，复位语义安全；块标签唯一性由 `currentBlockPrefix_` 函数名前缀既有保证。三后端（win-x64/linux-arm64/linux-x86_64）共用 IR 层同源受益。
- **修复后**：探针 20 函数帧恒定 80B；v2p 产物 4.4MB asm 中后段函数帧恢复常量级。
- **上轮「向量$Token.追加 容器方法未收录」未再复现**：为假歧义中止后 IR 表不完整的中间形态假象，随①根治消失。

### 三、E2E 173（目录包根撞名成员假歧义锚定）

- 形态：`设备/包.cn`（包根，候选1b 传模块名）+ `设备/设备.cn`（成员挂载名同名）+ 主程序 `导入 设备;` 后纯名调用同名字函数 `设备()`。
- 修复前负验证实锤：报 `[错误] 符号歧义: 设备（来自 设备/设备）`；修复后编译通过、产物运行 rc=173。

### 四、v2self 锚定全链重建（历史性进展）

- **默认 8MB 栈下 v2self 全链 rc=0**（历史首次，此前必须 `ulimit -s unlimited`）。
- cn_self（自举编译器）运行级：单文件用例 rc=42、多文件用例（E2E 119 同源）rc=14。
- **二级自举**：cn_self 编译 v2 全集 rc=0。
- **字节级固定点 fix_p ≡ fix_s**：v2p 编译 v2 产物与 cn_self 编译 v2 产物逐字节一致。
- **三级不动点 g2 ≡ g3**：cn_self2 编译 v2 产物与上一代逐字节一致（自举封闭）。
- 组件对拍 24/28 逐字节一致；4 入口（语法/包.cn、IR/包.cn、解析基础.cn、代码生成共用.cn）差异仅「&」字符串池编号差 2 的纯标签重编号（诊断流/池内容/运行 rc 全一致）——灰色点挂账下轮快照插桩专项。

### 五、验证（全绿）

- 单测 **1250/1250**（新增 `IRFunctionTest.RegIdResetPerFunction` 锚定寄存器号每函数复位）。
- E2E linux-x86_64 **175 用例 171 过/0 败/4 跳**（含新 173，与基线一致零回归）。
- v2self 三级锚定全链见上节。

### 六、诚实边界与挂账

- 组件对拍 4 入口字符串池编号灰色点（语义零差，下轮专项定位）。
- 8 用例运行级对拍矩阵（119/123/126/128/130/148/150/158）下轮收口。
- win/arm64 复验随跨机轮（宿主 IR 层改动三后端共享）。
- 生成表达式.cn 1261 行超线（单函数 1080 行）函数级拆分继续挂账。
- v2 结构体构造字面量 `记录{...}` 语法缺口继续挂账（随语法覆盖轮）。
