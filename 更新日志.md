# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结
> **第一百八十轮 180-a：甲通道实装——方法体内容器字段直接赋值深拷（uF 根治）+ 第一百四十九轮 149-a 补记（IR diff 专项定位）**（2026-09-15，家机 win-x64）：
> ①**149-a 定位**（源码零改动，详见 plans/020 第六十八节）：直接 diff 148-a 遗留 `target/p149win/ir_{base,fix}.txt`
> + `cn ir` 补测调用点——**三铁证**：类引用参数实参=源槽地址（二级·138-a 发现二一致）／基线两层求值=正确匹配／
> 148-a 一层修复=读相邻槽（chain 拷贝=0 精确复现）；**uF 真崩因=拷贝构造内容器字段赋值指针共享（浅拷）→ 出口双重
> 释放 0xC0000374**；★146~148-a「求值多解一层」系误诊（三轮回退正确）。教训入 lessons 149 段（补记）·预防 166~168。
> ②**180-a 实装**（单点 `ir_oop_field.cpp` `handleClassFieldAssign`）：方法体内无前缀字段赋值（隐式 this 形态 a）
> 的**拥有型容器字段深拷**——对齐形态 b（memberClassFieldAssign，139-a 已有）：四族收窄（向量/栈/链表/队列）+
> 构造内初始化豁免（currentMethodIsCtor_ 跳过 preFree）+ 源分级（构造调用接管/左值 NewObject+拷贝构造〔实参=源槽
> 地址〕/防御直存）。修复后 `表 = 其他.表` 生成 新建对象+拷贝构造+存槽 三指令，零共享。
> ③**行为验证**：uF 0xC0000374 崩 → **`F ok 大小=1` rc=0**；uA~uE 全 rc=0；probe_chain 链表/队列拷贝=2、
> 残留全 0（146~148-a 退化面零复发）；五形态/栈族/计数探针全 0；116_Feature2 ×3 PASS（138-a 回归面零复发）。
> ④**门禁**：零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过 / 0 败 / 0 跳过**（含 78_v2/79_v2 自举链——**锚定链 fix_p≡fix_s 自洽**
> =新宿主 v2 编译产物零变化裁决）+ 组件对拍 **45/45** + 运行级 **3/3**。门禁七面全绿。
> ⑤**诚实边界**：uD（壳 d=a 结构体整体拷贝浅共享）未在本轮范围——emitStructWholeAssign 成员级级联深拷
> 登记 181-a 候选（uD 当前 rc=0 泄漏不崩，runtime 在册兜底，非阻塞）。
> ⑥文档六件套 + 看板收工。
## 深度机 linux-x86_64 节

## 功能完善总结

> 191-a D1 函数级续波——visitFunctionDecl 171→分派+2 流水段方法 ≤100 达标（2026-09-15
> 第一百九十一轮，深度机 linux-x86_64；基线 3c57f00）：
> **拆分面（三明治形态·纯搬运零改写）**：`ir_decl.cpp` 的 visitFunctionDecl（171 行）按
> 「注册流水线」提取为 **2 个流水段族子方法**（ir.hpp +2 声明）：`registerFunctionParams`
> （参数循环+varStack_ 压栈+引用/结构体/函数指针参数形态+默认参数值收集，58 行）/
> `finishFunctionEmit`（析构 DeleteObject RAII+串释放+名单复位+IR 函数入列+作用域弹出，
> 21 行）。主函数保留「原型拦截+基础信息/linkName/返回类型/structReturn/function_ 绑定+
> 计数器复位+入口块+静态注入+函数体+默认返回」94 行。
> **多重集核验缺失 0 行**+新增 7 行全为签名/分派/闭合。一次构建失败即修正（keep_head
> 段内 func 声明重复+finishFunctionEmit 的 node 参数未使用——签名收窄+三处同步）。
> lessons 191 段（三明治形态正向记录）。

> **验证链（宿主纯重构·v2 树零变化）**：零警告构建 + 单测 **1317/1317** + **v2p 产物 .s md5
> 不变**（`3bebee1b…`=186-a 版本）+ 全量 E2E **306 用例 304 过 / 0 失败 / 2 跳**（44 双编译
> 对照全过）+ **锚定链不变**（fix_p ≡ fix_s 395505 行，md5 `02d691aa…`）。**D1 计数实测 57**
> （visitFunctionDecl 出列）。ir_decl.cpp 518 行 ≤1000 维持。github 镜像推送=本机无凭据，
> 待配置或他机代推（170~191-a 二十一轮待补推）。

## 单位机 ARM64 节

## 功能完善总结

### 188-a D6 落地：B11 变量常量传播增强（2026-09-15，单位机麒麟 ARM64 开发轮，基线 f00ca17；plans/023 §九 批准定义实装）

**用户令（多轮次实施 plans/021 队列 + 优先性能/安全）**：`plans/021` §3-D **D6**——B11 原判据仅
「操作数为 `无` 字面量」，而 `plans/023` §九 已批准定义为「空指针字面量（`无`）**或编译期常量** 空类型*
值」；`整64* p = 无; *p` 形态为**半截机制**。本轮按**性能第一/安全第二**落地（编译期判定、零运行期开销；
Rust `deref_nullptr` lint 同构）。

1. **判定模型（双侧同构）**：变量判「恒空」⇔ ① 至少一次以 `无` 字面量初始化/赋值（种子）∧
   ② **全函数内无其他写入**（失格集：非 `无` 赋值 / 复合赋值 / 自增自减 / **取地址** / **引用参数实参**）
   ∧ ③ 传播闭包不动点（`整64* q = p;` 且 p 恒空 → q 恒空）。使用点在遍历中**延迟登记**，函数/方法体
   检查**收尾统一判定**——流不敏感（全函数视角）=**零假阳性优先**；保守漏报登记为诚实边界。
2. **三操作面 + 字面量对齐**：变量形态在**解引用 / 成员访问 / 下标访问**三面均报硬错误（宿主 `Error` /
   v2 `表诊断`，**文案逐字一致**：`编译期常量空指针<解引用|成员访问|下标访问>（确定性错误；plans/023 B11）`）；
   **字面量形态补齐**——`无.x` / `无[0]` 原报间接类型错误（「类型 '空类型' 不是结构体…」/ 转换错误），
   现报 B11 专文诊断（三面齐备，杜绝半截）。
3. **改动面**：宿主 7 文件（`semantic.hpp` 判定表与助手声明 / `semantic_decl.cpp` 助手实现+`checkFunctionBody`
   入口清表与收尾判定+`visitVarDecl` 声明登记 / `semantic_expr_unary.cpp` 解引用使用点+取地址失格+自增自减失格 /
   `semantic_expr_access.cpp` 成员与下标使用点+字面量 B11 / `semantic_expr_op.cpp` 赋值登记 /
   `semantic_call.cpp` 引用实参失格 / `class_resolver_access.cpp` 方法体清表+收尾（保存/恢复外层状态））；
   v2 4 文件（`语义检查.cn` 判定表+访问器+`检查函数` 挂点+解引用/成员登记+自增自减失格+取地址失格 /
   `语义检查语句.cn` 声明位与赋值位登记 / `语义检查表达式.cn` 下标面+引用实参失格 / `语义检查类.cn`
   方法体清表+收尾）；**v2 单点去重**（语句区间遍历对同一 AST 节点二次到达 → 同名×行×面 去重）。
4. **测试**：单测 **+8 例**（`tests/unit/semantic/test_pointer.cpp`：解引用/判空合法/写入失格/取地址失格/
   传播边/成员面/下标面/字面量面）；E2E **279**（宿主负测·子串「编译期常量空指针解引用」）/ **280_v2**
   （v2 负测·三面诊断行固化 + 无 asm 产出）/ **281**（双编译对照正测·rc=41：判空合法 + 非空写入失格 +
   传播边经失格源不判）。
5. **反证双态命中**：A=关闭收尾判定 → 279 转「预期编译失败但编译成功」（复现原缺口，唯一变量）；
   B=关闭失格登记 → p5（取地址）/p6（引用实参）探针**出现假阳性**——证明失格逻辑承重（零假阳性的关键）。
6. **门禁七面全绿（ARM64 口径；合并树=本轮 + 深度机 186/189/190-a 结果）**：零警告构建（GCC 9.3 清场）+
   单测 **1325/1325** + **v2p 重建**（md5 `c5844263…`，v2 树改动=预期变化）+ 全量 E2E **309 用例 307 过 /
   0 失败 / 2 跳**（35 双编译对照用例全过 + `78_v2`/`79_v2` 锚定链自洽）+ **锚定链重锚 555442 → 561364 行**
   （arm64 新基准；`fix_p ≡ fix_s` 逐字节 md5 `66105ebe…`；+5922 = v2 树 159-a~190-a 累计增量）+
   运行级 3/3（01_hello / 119_v2 / 173_v2）。
7. **★顺带收口 ARM64 跨机复验欠账**：161-a~191-a（A8/A5/A3/A4/C16 + 173~190-a D1 波 + 180-a 甲通道 +
   183-a 待定 + 186/189/190-a）**全量首验**——三平台门禁「逐机兑现」链补全。
8. **零假阳性证据**：宿主对 v2 全树 `check` 通过；76 个既有负测用例零 D6 命中；E2E 全量 0 败。
9. **文档六件套**：`plans/021`（戳 188 + §5 索引 + §3-D D6 出列→§4.2 台账 + §2 门禁数字）+ `plans/001` §5.3
   （B11 **判据终态**：字面量 + 变量常量传播 + 三操作面；诚实边界改写）+ `plans/023` §八 B11 行补注 +
   `plans/019` §7.3 188-a 行 + `plans/025`（§〇/§三实测回填/§四审计复核 + U1 收口）+ 本日志 + HANDOFF +
   lessons 188 段 + 看板收工（含 §8.4 广播：新增硬错误面 → 三机复跑全量 E2E + 各机重建 v2p；arm64 新锚 561364）。

**诚实边界**：① 保守漏报（登记）：`p = 无` 之前的非空赋值后再置空（流敏感未做）、跨函数/静态/全局写入不可见、
多级指针别名写入；② `字符*` 类型在 v2 侧不支持（宿主报 B11 / v2 报既有诊断——两侧同为编译失败，诊断不同）；
③ github 镜像本机无凭据未推（沿前轮口径）。
