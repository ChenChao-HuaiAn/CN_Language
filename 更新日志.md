## 功能完善总结

### 修复 reg_alloc 线性扫描寄存器分配器 -O2 崩溃缺陷（x64 栈对齐，2026-08-14）

**背景**：用户换 Win10 系统（MSVC 2022 工具链）后，环境恢复基线验证发现 E2E **21/28 用例崩溃**（退出码 0xC0000005 = 访问违规）。对照实验锁定根因范围：`-O0`（无优化）→ 输出全对；`-O2 --no-regalloc`（关闭寄存器分配）→ 输出全对；默认 `-O2`（启用寄存器分配）→ 21 用例崩溃。结论：线性扫描寄存器分配器（reg_alloc）生成的 x64 汇编有缺陷。

#### 一、缺陷根因（已定位到具体指令序列）

**x64 prologue 栈 16 字节对齐破坏**——[`x64_codegen.cpp`](src/cn_compiler/codegen/x64/x64_codegen.cpp:525) `emitPrologue` 执行顺序：

```asm
push rbp          ; 入口 rsp≡8 → push 后 rsp≡0
mov rbp, rsp
sub rsp, 128      ; frameSize=128（16对齐）→ rsp≡0
push r12          ; ← rsp≡8！破坏 16 字节对齐
```

- `sub rsp, frameSize` 时 frameSize 已 16 对齐（≡0 mod 16）；
- **之后** `emitSaveCalleeSaved` 再 push 被调用者保存寄存器（rbx/r12~r15）；
- 当 **push 数量为奇数**（N=1/3/5）时，函数体内 `rsp ≡ 8 (mod 16)`；
- `emitCall` 的 `sub rsp, 32`（16 的倍数）不改变余数 → **call 前 rsp≡8 (mod 16)，违反 Win x64 ABI**（call 时 rsp 必须 16 字节对齐）→ 被调方（MSVC 编译的运行时/用户函数）用 `movaps`/`movdqa` 对齐访问立即 0xC0000005。

失败用例统计证实：05_array_pointer / 07_struct_enum 有函数 `push r12/r13/r14`（3 个奇数）、09_integration 有函数 `push rbx/r12/r13/r14/r15`（5 个奇数）。旧环境（GCC7 Linux 无 MSVC 对齐敏感运行时路径）未暴露，新 MSVC 环境暴露。

#### 二、修复内容（最小化改动）

| 文件 | 修改 |
|------|------|
| [`src/cn_compiler/codegen/x64/x64_codegen.cpp`](src/cn_compiler/codegen/x64/x64_codegen.cpp:525) | `emitPrologue`：`calleeSavedRegs_.size() % 2 == 1` 时 `frameSize += 8`（正确约束 `F + 8N ≡ 0 mod 16`，computeFrameSize 已保证 F≡0 mod 16，故 N 奇数时 +8） |
| [`tests/unit/codegen/test_x64_codegen.cpp`](tests/unit/codegen/test_x64_codegen.cpp) | 新增回归单测 `PrologueStackAlignWithCalleeSaved`：1 个 i64 虚拟寄存器 → 分配 1 个被调用者保存寄存器（奇数 push）→ 断言 `sub rsp, 24`（16 对齐 + 奇数补 8）与 `push r12`；关闭 regalloc 时无 push、`sub rsp, 16` |

未修改 reg_alloc.cpp 分配器本身（分配逻辑正确，缺陷在 codegen prologue 消费端）。

#### 三、验证结果

- **构建**：`cmake --build target/build --clean-first --config Debug` 零错误零警告（/W4 /WX）
- **单测**：`target/Debug/cn_unit_tests.exe` → **965/965 通过**（原 964 + 新增 1 个栈对齐回归测试）
- **E2E**：`python tests/e2e/run_e2e.py --cn target/Debug/cn.exe` → **28/28 通过**（含默认 -O2 路径）
- **三级别一致性**：28 用例 × {-O0, -O2, -O3} = **84 组合全部通过**（输出与期望一致）

#### 四、文档更新

- `lessons.md`：权重 20.8 的 reg_alloc 待修复条目已标记 **已修复 ✅**，写清根因（prologue push 后未补对齐）、修复内容、预防措施（`F + 8N ≡ 0 mod 16` 公式）
- `plans/002-阶段4-优化与Winx64.md`：Task 4.3 缺陷已修复标注
- 本文件（覆盖写入本轮修改内容）
