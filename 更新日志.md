# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 438-a 开发轮：T68 原始/多行字符串四形态·规范 01b 全量销项·2026-09-19·wt438 隔离）

- **T68 销项**：v2 原始/多行字符串四形态全量——`原始"..."`（反斜杠原样·正则/路径场景）/`多行` + 三连引号包裹（跨行·内转义同普通）/`原始多行` + 三连引号（完全原样）/普通（对照）。
- **实现**：`CN语言编译器v2/词法/词法分析.cn` 三层——解码字符串内容 单点函数抽取（普通/多行共用）+分词字符串扩展（四形态扫描）+主循环前缀识别（前缀标识符+紧邻引号·零歧义零开销）。
- **验证**：四形态探针（v2 输出与宿主逐行一致）+**459_字符串四形态矩阵** 双编译 PASS（四形态字节级基准·expected 实跑固化）+反证（撤改动→parse 错复现）+09 推进（行 163 阻塞消失·错误 9→6·下一层=函数指针面）。
- **09 剩余缺口（下轮候选）**：行 41/237 函数指针类型参数（规范 04·大特性）。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 服务轮第二波：堆积链收编 331/337/338/345~353→develop f9e91ae7·2026-09-19）

- **用户裁决（收编波）**：四链按「331-a 优先、346-a 先于 347-a」rebase 后经门禁集成（arm64 豁免同甲案·仅限本周末）。
- **四链集成**：331→07bb9e0c（T11/T49/T50/T51/T52·单测 1363）；337→25e600a5（T53+440）；338→f87bb26e（C19+OOM 备案+runner 名单扩面）；353→f9e91ae7（345~353 全链 merge 三方：D33/T57/T58/T33+443/D11+444/T35/除法保护+446/T60+447）。
- **T60 补提（机制级备案）**：353-a 修复代码未入库（447 用例已入库）——收编门禁暴露 447-6a 红后补齐：生成层负号整数面产物码随操作数+登记+三后端 32 位分派；反证回退红/恢复绿。
- **integrate.py 三处修复（随 331 入库·广播）**：产物路径兜底/--jobs 8/平台名映射——此前 linux 侧从未真跑通过 E2E 步骤。
- **develop 终态门禁（全绿）**：全量 E2E --jobs 8 **463=461/0/2 零红**（426/447 双转绿）；单测 **1363/1363**；自举链 78_v2/79_v2 双 PASS。
- **教训**：rebase 循环误丢代码（指纹审计抓获→改 merge 三方）；冲突解决后必 grep 标记+核对行数；收工前 git status 必查预期写集=实际写集。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
