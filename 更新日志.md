## 功能完善总结

### ① plans/019 阶段4' 第二批：v2 侧拥有型字符串 RAII 全链落地（方案A 用户裁决）
- **预扫登记**（IR容器.cn 预扫容器声明）：拥有型字符串局部白名单登记（无初值/字面量/标识符/内置八函数=拥有；驻留文本 等非白名单调用与下标/成员/解引用初值=借用不登记——保守安全）+ 槽预分配 + 入口零初始化（未执行声明路径槽=确定性 0，free 空安全）。
- **初始化拥有化**（生成声明.cn 生成变量声明）：名单内字面量/标识符经 __cn_str_copy 落堆（Rust "x".to_string() 同款）；嵌套同名借用声明污染防御；同名异类型复用槽污染防御。
- **赋值拥有化**（生成语句.cn 生成赋值语句）：free 旧（drop——拥有链维护下旧值必堆串，优于宿主保守面零泄漏）+ copy/直存新；字面量/标识符**先 copy 后 free**（自赋值 s = s 安全）；二元拼接/白名单八函数=拥有直存（+= 语法层重析为二元同覆盖）；非白名单调用污染退出；下标字符串元素借出污染（38_tool 指针逃逸模型对齐）。
- **双挂点释放**：返回块 发射字符串释放（IR_调用实参+IR_调用参数化 直调形态——concat 先例；跳过名=返回标识符移出）+ 块出口析构 字符串段（作用域截断）；函数IR 新增 字符串变量们/字符串污染们（生成函数 显式初始化——裸声明字段不默认构造）。
- **自举安全**：v2 树顶层唯一字面量初始化 返回名v（后续 驻留文本 借用赋值）经污染传播正确退出 RAII——固定点新口径 254365 逐字节一致实证闭环。

### ② E2E 测试运行器三重改造（用户裁决：太慢/去硬编码/全量并行）
- **v2p 构建缓存**：指纹=v2 全树 .cn（路径+大小+mtime）+编译器 mtime/size，一致不重建（cargo 增量理念）——消除「每个 v2 用例全树重建 v2p（数十秒×58）」最大瓶颈。
- **数据驱动去硬编码**：v2 用例注册表自 runner 删除，改用例目录 v2闭环.txt 配置文件（退出码/源文件/借链/供给）——新增 v2 用例零代码改动（58 个既有用例已迁移）。
- **全量统一并行**（--jobs N，默认 1=原串行）：Linux 双平台 v2 产物按用例隔离（v2work{编号} cwd 隔离——v2p 的 asm 输出为相对 cwd 的 target/v2asm.s；stdlib 签名扫描软链适配；v2asm_N/v2out_N 按编号；入口绝对路径+日志锚行绝对前缀适配回相对——.expected 不动）；池启动前 v2p 预热消除并发构建竞态；**226 用例统一并行（01 起）**。win-x64 v2 产物未隔离（ml64/link 原编排）保持 v2 桶串行兜底。
- **提速实证**：全量 18.3s（热缓存）/27.7s（冷缓存），对照原串行（无缓存，估 20+ 分钟）数十倍；结果与串行逐用例一致（221/226，唯一败=194 既有待批）。

### ③ 验证矩阵（全绿）
- 清场重建零警告+单测 1289/1289+E2E 全量 226 用例 221 过（1 败=194 既有待批，4 跳）。
- v2self 锚定链：fix_p **254365 行**（新口径）→固定点逐字节一致+绑定自检+运行级 3/3（hello 你好，世界/119 rc=14/173 rc=173）+组件对拍 29/29。
- E2E 新增 208_v2_拥有字符串（七形态校验和 127 全对锚定）。
