# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 88-a v2 实参位短路栈失衡根治（2026-09-12 第八十八轮，深度机 linux-x86_64）：87-a 登记项
> 「v2 实参位短路栈失衡」（**下一轮第一优先**）**销项**——先做**栈模型普查**（「表达式求值
> 净栈 +1」隐式契约），定位**两层叠加根因**：①语法层短路节点左操作数区间恒为空（左操作数被
> 主循环平铺求值=多压一项 + 短路内左递归退化为「取栈顶」）；②IR 层短路求值「只压不弹」
> （净 +3）破坏净栈契约 → 弹栈消费错位 → `f(a || b)` 报 `[ir2] 调用被调对象非函数名/方法`
> + 运行时错误6。**两层同治**后形态矩阵 12 消费位两侧一致，防线（E2E 248 双编译对照 +
> 反证三件）就位；验证链全绿。

> ① **根因与形态矩阵（探针 P88 系列实证）**：
> **S1 语法层**——`解析二元链`/`解析二元层` 的「左起点」= `池.大小()`（左操作数**之后**，
> 左操作数已由 `解析表达式` 前段解析）→ `||`（层1）/`&&`（层2）短路节点左操作数区间
> `[子起点, 附加)` **恒为空**（探针 dbg：`标 || 假` 收集 `子起点=28=附加`，左操作数在 27）；
> 连锁：左操作数被主循环**平铺求值**（多压一项残留）+ 短路求值内左递归空区间退化「取栈顶」。
> **S2 IR 层**——`生成短路求值`「只压不弹」（左+1/右+1/末尾结果+1 = 净 **+3**）破坏「表达式
> 求值净栈 +1」契约（探针弹前栈 `[1 2 -30 3 4 8]`：实参弹 2 后被调取到 `3` 应为 `-30`）。
> **形态矩阵（12 消费位两侧对照）**：实参位（用户函数/打印内置/`&&`/双实参）修复前 ✗ 中止 →
> 修复后两侧一致；赋值位/返回位/条件位/当条件位/短路嵌套/一元嵌套/三元嵌套 ✓（修复前=「取
> 栈顶」**巧合正确**）；惰性求值（副作用计=0）/左操作数只求值一次（副作用计=1）/循环 2000 轮
> （和=5984）✓。

> ② **实施（性能第一、安全第二；v2 单侧）**：
> **语法层**（`语法/解析表达式.cn`）——`解析二元链` 增 `表达式起点` 参数（`解析表达式` 传
> `起始`）+ `解析二元层` 左起点参数化（链入口=表达式起点 / 左操作数路径=父层左起点 / 右操作数
> 路径=父层右起点）+ `解析层_子` 增 `子层左起点` 参数并**分层**（`层 <= 1` 用「左操作数起点」
> 语义；层3+ 保持「左操作数之后」= `池.大小()`——语义层 `子起点-1`=左末端契约，**首版全层
> 一刀切触发 141_v2/190_v2/192_v2 三处回归已纠正**）。
> **IR 层**（`IR/IR生成.cn` + `IR/生成表达式.cn`）——新节**净栈契约单一事实源原语**
> （`表达式栈弹到深度`/`表达式栈归位压回`，注释固化契约 + 「禁止裸循环弹栈复制」纪律）+
> `生成短路求值` 归位（进入深度 → 左/右递归后弹到深度〈子区间残留自愈清理〉→ 末尾压唯一
> 结果 = 净 +1；与 `生成三元运算`/`生成一元运算` 同构）。
> **注释更新**：`生成语句.cn` 静态 preFree 预计算的过时约束注释（原「不可在实参位直接写
> `||`」）撤销——预计算保留为性能选择（一次求值供多个调用点复用）。

> ③ **防线（四件套）**：**正测** E2E **248_双编译对照短路求值全链路**（plans/020 移植纪律 9
> **第十七个**对照用例：八形态〈①实参位 ②打印族实参位+条件位 ③`&&` 实参位+双实参+结构体
> 实参 ④嵌套 `||`×`&&` ⑤惰性求值 ⑥左操作数只求值一次 ⑦短路×三元 ⑧循环 2000 轮〉折
> **255** 两侧一致）；**反证三件全命中**（A 语法层左起点回退→248 v2 复现 `[ir2] 调用被调对象
> 非函数名/方法`；B IR 净栈归位回退→248 v2 同复现；C 分层回退→192_v2 语义错误消失退出码 0），
> 均已恢复复验。

> ④ **验证链**：清场重建**零警告**（0 warning/0 error；宿主产物 md5 `56f55e1c…`/`6ffd9dc7…`
> 重建前后一致）+ 单测 **1306/1306** + E2E **275 用例 271 过 0 失败 0 未实现 4 跳**（+248；
> 其余 274 过=**零回归**）+ 锚定链固定点 **335263 行**（新口径，87-a 后 334821；+442=v2 侧
> 88-a 代码）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

> ⑤ **文档五件套**：plans/019（跟踪表第八十八轮行）+ plans/020（**第二十九节 88-a 全节**）+
> lessons（88-a 段，权重 8，预防新增 92~95：隐式不变量必须显式化+单一事实源+校验／字段多用途
> 改语义前必须普查全部消费者／「看似正确」是巧合正确·消费位矩阵铺面／区间标注是层间契约·
> 双向排查）+ 本文件 + HANDOFF。

> ⑥ **诚实边界**：87-a 规避写法（静态 preFree 预计算布尔）保留（性能选择，非缺陷规避）；
> win-x64（家机）/arm64（单位机）侧 v2 实物复验=跨机轮；v2 静态字符语法限制 / v2 打印浮点
> 既有边界 / 多文件静态注入仅入口模块 / 静态槽不登记自动释放 / v2 诊断无行号维持登记；
> 探针 P88 系列留 /tmp/p88probe（不入库）。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
