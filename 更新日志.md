# CN语言编译器 更新日志

## 功能完善总结

**续2批（2026-09-07 第三十一轮下半程·续2，深度系统 x86_64）**：包/货舱 死保留字摘除（用户质疑「01标准里没有货舱关键字」触发复查→裁决方案A）——复查实证：specs/01a 关键字表确有 货舱（2026-08-15 v2.0 立规 61 个），但 parser 对 Kw_Package/Kw_Cargo **零语法消费点**（包.cn/货舱.toml 是文件名约定，[货舱]/[依赖] 是 TOML 数据文本；Rust 对照 cargo 非保留字）——立规与实现一致但立规本身过度保留。摘除：lexer 三处+单测四文件反转（61→59）+specs/01a/plans/001 注记+E2E 166 钉死 货舱/包 四形态标识符同权（变量/函数/参数/结构体名）+v2 主.cn 舱单→货舱 改名归真+lessons 预防条目（**立规关键字必须有真实语法消费点**）。模块/作为 有真实语法保留。门禁：零警告+单测 1249/1249+E2E 168 用例 164 过/0 败/4 跳+v2p 重建 13 组件冒烟全绿。

**续轮（2026-09-07 第三十一轮下半程·续，深度系统 x86_64）**：P6b 工作流 3 前半落地——v2 侧词法/语法/加载器/货舱.toml 解析器（第 1/2/6 层），新增 v2 闭环 E2E 164/165；门禁全绿（构建零警告+单测 1249/1249+E2E linux-x86_64 167 用例 163 过/0 败/4 跳+v2p 重建 13 组件冒烟全绿）。语义/IR 模块分桶（第 3/4 层）待下轮。

### 续轮改动明细

- **词法**（词法分析.cn）：关键字表补 `模块`/`作为`（plans/018 预计零改动翻案，实测缺口）；`作为` 升格后语法分析.cn 三处文本判定同步改 是关键。
- **语法**（语法分析.cn）：`模块 X;` 顶层声明（扫描导入+解析程序 双点登记、语法校验）+ 挂载清单机制（模块声明们/登记/取走）。
- **加载器**（主.cn）：`编译文件` 加 是包根 参数——父挂子自动加载强语义（包根→同级聚合/普通文件→`<主干>/X.cn`；挂载不存在=定位诊断经语法错误中止）；货舱.toml 发现解析+依赖查找链（入口同目录→[依赖] 内置跳过/本地 `依赖/<名>/`、包.cn 根→缺失中止）。
- **新组件 货舱解析.cn**（211 行，纯 CN TOML 子集）：[货舱]/[依赖] 节+行内注释+宽容未知节；依赖表用 `向量<货舱依赖>&` 平铺签名（含容器字段结构体裸声明不触发容器构造=悬垂，探针实证后避开）。
- **E2E 164/165**（v2 闭环）：模块自动加载强语义/货舱依赖发现；run_e2e.py v2 闭环步骤 2 扩整树复制（既有平铺用例等价）。
- **踩坑**：`货舱` 为宿主既有关键字不可作 CN 标识符（改 舱单）；包根聚合/父挂子/定位负/货舱全链四探针全绿。

**本轮（2026-09-07 第三十一轮下半程，深度系统 x86_64）**：P6b 宿主收口轮——用户终裁呈报一 **方案 B（Rust 语义，「我选Rust语言的做法」）**，完成呈报一 B 全量实施（具名绑定+P1-1 废止+E0255 启用）与呈报二 A′ 错编根治（函数链接键公式化），新增宿主侧 E2E 五例与单测六项；门禁全绿（构建零警告+单测 1249/1249+E2E linux-x86_64 165 用例 161 过/0 败/4 跳=基线一致零回归）。

### 一、呈报一 B 实施（「导入 m::符号」= 具名绑定，Rust 一致）

- **visitImportDecl 重写**（semantic_decl.cpp）：路径导入（≥2 段）尾段为符号 → **真搬名字**（绑定名+来源模块完整路径过滤，与花括号导入同构）；尾段为已加载模块（knownModules_ 消歧）→ 保持模块导入③；单段/模块重命名/未合并单文件保持旧行为；**自导入**（来源首段==归属模块）跳过绑定不构成冲突。
- **P1-1 废止**：限定调用 `m::符号()` 按「模块已加载」放行——knownModules_ = 合并声明 moduleName 全集 + `Program::loadedModules`（ast.hpp 新字段，driver 注入依赖图全部模块名+货舱 [依赖] 包名）；未加载模块仍报「未声明的标识符」（保护性负覆盖保留于单测）。
- **E0255 启用**：checkImportLocalConflicts 按现写法启用（路径导入=具名绑定②）——①×② 导入与本地定义同名=编译错误、②×② 多次显式导入同名=错误、自导入豁免。
- **内置 prelude 例外保持**：数学::平方根 等内置限定名恒可用（独立于模块加载判定）。
- **测试适配（呈报一B 批准代价项）**：E2E 52 删除撞名的 `导入 工具库::格式化::版本;`（限定调用按加载判定放行）；单测 CrateIsolateNoParamQualifiedCall 同款适配；SemanticQualifiedCallWithoutImport / -Regression 两测反转为 B 行为锚定（已加载免导入通过+未加载仍报错）。

### 二、呈报二 A′ 实施（错编缺陷根治：函数链接键单一公式化）

- **全编译器唯一公式** `SemanticAnalyzer::functionLinkKey(模块名, 函数名, 签名键)`（semantic.hpp public）：`(模块名空/=="主"/函数名=="主"/__cn_ 前缀) ? 签名键 : 模块名$签名键`。
- **三处同源**：registerFunction 注册键、checkFunctionBody 查找键（旧按裸 sigKey 查漏跨模块条目=函数体静默跳检，当轮修复）、ir_decl.cpp 定义侧 mangledName；调用侧 resolvedSignature=注册键（旧「按条目模块补拼前缀」补丁块删除）；公式键碰撞（裸键归一形态）fail fast 诊断绝不静默覆盖。
- **顺序依赖消灭**：注册键不再依赖「谁先注册占裸键」——依赖与入口同名同签名+入口纯名调用的 undefined reference 主$版本 错编形态结构上不存在（Rust 符号=f(def-id) 同构）。
- **单文件自限定调用既有边界一并收口**：resolveOverload 对 moduleName 空的条目（单文件管线唯一模块）恒命中过滤器——`导入 主::版本;` + `主::版本()` 首次可用。

### 三、B 落地暴露两缺陷当轮根治（绑定重写踩踏）

- ① 纯名别名重写覆盖限定调用已设 moduleFilter（路径导入绑定名使 44_crate_isolate 的 `模块Y::双倍` 解析到模块X）——修复=限定调用过滤器优先（已设则跳过重写）。
- ② itemAliasModules_ 按重写后名字查询误取同名片来源模块（91_别名跨模块同名 的 Y双倍 取 双倍 的来源模块）——修复=按原绑定名查询、未命中回退重写名。

### 四、新增测试（宿主侧）

- **E2E 五例（159~163）**：159 E0255 负测（路径+花括号两形态）/ 160 三级解析遮蔽正测（**A′ 错编形态 compile+link+run 全通**，旧代码必现链接失败）/ 161 限定调用免导入正测（同包+父挂子多级路径）/ 162 模块定位收紧负测 / 163 模块自动加载强语义负测。
- **单测 +6**：ImportConflictPathFormE0255 / ImportConflictBraceFormE0255 / ImportSelfImportNoConflict / ImportDoubleExplicitSameNameE0255 / FunctionLinkKeyFormula / LinkKeyDependencyEntrySameSigPureCall。

### 五、规范终裁化

- plans/001 §5.9.7 与 specs/08 3.6 摘除「过渡期偏差」标注——B 终裁+宿主实施完成，规范与实现一致（E2E 161 正测+单测负覆盖锚定）。

### 六、诚实边界与门禁

- 门禁：构建零警告 + 单测 **1249/1249**（+6）+ E2E linux-x86_64 **165 用例 161 过/0 败/4 跳**（62/69/78/79 平台跳过=基线一致零回归）。
- v2 主链（CN语言编译器v2/）构建+运行在本轮宿主改动下零影响（冒烟实证）；win/arm64 侧复验=跨机轮（改动全在共享层 semantic/driver/ir/ast）。
- 已知既有（非本轮引入，stash 实证 HEAD 同态）：CN语言编译器/（v1 旧自举物）4 组件 `cn check` 失败（定位收紧 isRootUnit 形态误判+组件级既有缺口）——v1 旧物不修，v2 侧随工作流3 处理。
- 详见 plans/018 第九节（裁决与实施全记录）。
