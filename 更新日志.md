# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🧪 305-a M3 采样轮·不占锁·2026-09-17，基线 58b7e70 采样段→4d7371c 复验段·源码零改动·验证轮）

**深度机 304-a（B11）在飞期间 M3 采样验证轮，三件主产出**：

- **T26 win 挂面发现+根治复验闭环**：回归轨 114 件（基线 58b7e70）9 命中全旧类，其中 T26×4 win 列同挂（`假||假` O0=假对/O3=真错·asm 铁证=call 前硬编码常量）——**纠正 298-a「T26 win 免疫」定性**（免疫仅成立运行时输入形态）；广播 e195389 与 304-a 收工（GlobalValuePass 汇合块根治·平台无关）交叉印证；重建 4d7371c 复验=**四实弹 4/4 绿+410 PASS+全量 E2E 427/428**（T26 win 侧闭环）。
- **T43 新立案（§三-G·待用户裁决）**：win -O3 浮点汇合 Copy 汇编失败（A2070）——406（T25 守护）win 首跑 A2070×8=win 全量唯一败面；根因=297-b emitCopy 浮点分支 movsd/movss mem 操作数缺 qword/dword ptr（尺寸说明符家族第五犯·**静态审查未真跑 ml64·T25 win 修复从未运行级工作**）；方案甲/乙/丙（建议三合一·写集与 306-a 零交叠可搭车）。
- **B8 面台账 ⬜ 行清零达成**（81/81·✅40·🔶41·check_matrix_coverage PASS）：行 16 溢出语义面 C18 验收基线（编译期 12 形态全静默=根治面 c01~c11；运行期 10 形态 O0=O3=oracle 全对=变量回绕现状已正确）。

**采样轨道**：新种子 500（305000001 域）差分全一致零命中；负向 400（305100001 域）400/400 拒绝·0 崩溃·0 行号偏离（T28① 新口径首跑全绿）。

**门禁**：check_handoff+check_matrix_coverage EXIT=0；月日志 4922 行红线内；双推 gitcode+github。

## 深度机 linux-x86_64 节

## 功能完善总结（304-a·清零波次1=B11 销项：T26 根治·2026-09-17，基线 58b7e70→认领 92570ba·执行家机预置计划 §1.0a）

1. **T26 x64l 复现+根因甄别**：三实弹 O3 稳定挂/O1 O2 免疫；asm `mov rdi,1` 忠实发射 IR 层已折叠常量→分叉点不在后端——**根因=GlobalValuePass（-O3 独有）跨块线性序常量传播在多前驱汇合块失效**（未执行分支常量 Store 覆盖执行分支寄存器 Store→汇合 Load 错误折叠·机制平台无关）。
2. **根治**（src/cn_compiler/opt/global_value.cpp）：汇合块（≥2 前驱·终止跳转入度统计）入口清空常量表——保守正确·块内转发保留·单前驱线性块语义不动。
3. **410_布尔短路汇合矩阵 E2E 转正**：11 形态 O0=O3 逐字节（布尔短路 7+整型汇合 3〔B9 扩面〕+整64 负值）+coverage_map 登记；**反证双件**（回退修复多形态变红→恢复全绿）。
4. **对账**：T26 系 4 实弹回归轨消失（daemon 增量语义实战）；B11 完整闭环=T25（406）+T26（410）双构件负测齐。
5. **门禁**：零警告构建+单测 1353+全量 E2E 428=426/0/2+strict 0+matrix 80/81+CLI 0+双文档门禁；v2 树零改动（无此 pass=天然免疫）。
6. **跨机请求**：win/arm64 运行级复验（opt 修复自动同修定性·看板通告段广播）。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
