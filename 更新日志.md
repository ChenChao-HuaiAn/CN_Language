# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 525-a 开发轮：D8 第五件=arm64 装载直读收官·2026-09-20/21）

- **arm64 临时面装载中转 mov 同口径 488 用例 2359→4（-99.8%）**——八点直读改造（CopyStruct 基址/emitCopy 写回/FieldAddr 基址/Cast 窄截断/Cast scvtf/VtableAddr/Call 栈参/prologue 结构体参数）；
- 未分配 fallback 逐字节不变红线全点保持；条件跳转直读经定性撤销（i1 不分配零收益）；
- Ninja+VS 双构建树全绿：单测 1387+E2E 522=522/0/0+asm 宽度 297×3；stash 双态反证闭环；
- 深度机 linux 回签通过（522=517/3/2·假红互踩口径）——随 --try-build-done 集成；残余 4 条=诚实边界保留项。

## 深度机 linux-x86_64 节

## 功能完善总结（562 热修轮·develop 红 fix-forward=456 一行回退·2026-09-21）

- **§8.6 develop 红处置**：`456_泛型指针字段下标` 稳定红（v2 产物 rc=14≠27）阻塞 537/536/489/549 及 546+547 链集成排队——破坏者=本机 537-a（baf7b2c4），机械拆分时**误引入行为行变更** `整64 类体起点 = 池.大小() + 1;`（537 前态 9d760efb=无 `+1`）→节点_类 首子节点被跳过→泛型实例布局登记/查询键错位→类型大小查表 miss 返 0→`槽[数量]` 步进 0（rc=7+7=14）。
- **修复**=一行回退（`池.大小()`，与原码逐字一致）+ 原地注释说明语义（类体起点=类节点自身入池前池大小）。
- **验证**：456 单跑 2/2 PASS（修前/修后反证双态）＋零警告构建＋单测 1394/1394＋全量 E2E（见提交信息）＋strict/matrix＋自举链 78_v2/79_v2。
- **教训入册（权重 9）**：机械拆分的「顺手修正」＝静默行为变更；对拍样本必须覆盖被拆函数的完整消费面；拆分轮先串行复验再判「零新增红」；develop 红了先 `git log -S` 自查自家近轮提交。
- 写集=CN语言编译器v2/语法/解析类.cn 单行＋文档六件套；**零 src/零 tests**（测试文件未动）。


## 单位机 ARM64 节

## 功能完善总结

### 560 开发轮：F2-36b 首步=bench v2 口径三平台改造+arm64 自举性能基线入库（2026-09-21，基线 develop 0e3ec08f）

1. **范围修正（开工侦查实锤）**：原 bench_self_host.py 测量对象=v1 组件链（CN语言编译器/ 五组件·已推倒）——对象整体过时；改造为 **v2 自举树口径**（CN语言编译器v2 全树·fix_p≡fix_s 固定点·对齐 78_v2/79_v2 锚定链编排）+`--platform` 三平台工具链分派（win=ml64/link 原路径保留·linux=as/g++ -no-pie〔CN_AS/CN_CXX 环境变量+PATH 探测·对齐 runner〕）。
2. **arm64 基线实测入库**（编译器=组合态 558+559 修复态构建）：M1=22295.7ms（C++ 宿主 Debug build v2 全树）/M2=42608.4ms（CN 版编译同树 742237 行·3 次中位）/**比值 1.91x vs C++ Debug**/M3=2194.8ms/M4=146.4ms+绑定自检 cn_main∈cn_self.obj ✓/**M5=32309.0ms·M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建的同一编译器快 24%**（重大正面信号）/M6 ✅ fix_p≡fix_s 逐字节（自举固定点成立）。
3. **实施修两处**：M4 借链对象（初版错用 fix_p 自汇编→undefined 词法$Token 追加符号·grep 实锤 fix_p 缺跨模块符号定义→改 cn build 中间产物 v2p.o〔run_e2e「容器符号提供者」原义〕）；基线测量编译器依赖序（主树无 558 修复→改用组合态构建·口径注明）。
4. **口径诚实边界**：C++ 宿主=Debug 构建（-O0）——1.91x 非 vs Release 口径；单机基线（跨机比较无意义）；win 分支静态核验（跨机复验归家机）；深度机 x64l 基线可复用同脚本（三机通用）。
