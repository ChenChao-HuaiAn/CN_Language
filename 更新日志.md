## v2 字段偏移表按结构体分桶根治 + 宿主嵌套泛型/静态构造初始化（P6e，2026-08-30）

**目标**：根治用户批评的 v2 语法层「字段名全局唯一」扁平字段偏移表隐患（两个结构体同名字段会串偏移）——按结构体分桶；过程中发现并根治宿主两处缺陷。

**v2 字段表分桶（语法分析.cn）**：
- 字段偏移表键改为 结构体名ID * 键位移 + 字段名ID（单映射无嵌套，键位移=2^20 无碰撞）。
- 新增 变量结构体表（变量名ID → 结构体名ID）：参数/局部变量声明时若类型是结构体（值语义）登记；成员访问 对象.字段 时先推导对象结构体名 → 分桶键查偏移；对象非标识符/未登记回退旧扁平键。
- 验证：源码 结构体 点{x,y} + 结构体 矩形{x,y,w,h}，位置.x=7 位置.y=9 框.x=3 框.y=4 返回 位置.x+位置.y → 偏移表大小=6（分桶正确）、位置.x 偏移 0/位置.y 偏移 8 正确生成、asm 汇编通过、v2p 自检无回归。

**宿主缺陷根治**（过程中发现）：
1. resultTypeArgs 嵌套泛型逗号截断（error_analysis.cpp）：结果<映射<整64, 整64>, 整32> 的 find(',') 在嵌套实参内部逗号误切 → t 截断成 映射<整64（成员访问报「不是类类型」）。改平衡扫描找外层逗号。同修 visitVarDecl 对 结果/可选 内部实参递归归一（结果<映射<...>> → 结果<映射$...$...>）。
2. 顶层静态类对象无构造初始化（ir.hpp/ir_decl.cpp）：静态 映射<...> 模块表（无初始值）或 = 映射<...>() 的 .data 只分配零字节、构造函数不调用 → 首次 设置/获取 写 null 崩溃。main 函数开头注入：无 init → 在 .data 符号地址上直接调用无参构造（对象内联 .data）；有 init 构造调用 → NewObject + 构造 + StorePtr。

**验证**：被导入模块 静态 映射<整64, 整64> 模块表 登记(5,100)+查询(5) → 值=100 输出正确；单测 1196/1196；E2E 30/96/111/113/115/01/09 全 PASS；v2p exit=0。
## 宿主缺陷根治：顶层静态容器变量全链路（P3-8，2026-08-30）

**目标**：根治宿主编译器（src/cn_compiler）对「顶层静态容器变量」（静态 向量<整64> 全局表）的完整支持——此前 声明/登记/类型归一/IR 引用/方法调用/.data 分配 五处缺陷叠加，导致 v2 自举所需全局容器不可用。
