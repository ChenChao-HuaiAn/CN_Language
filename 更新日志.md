# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🏃 326-a 开发轮 D38 批1=v2 负测防线词法/语法层 3 项·2026-09-18·基线 5ce0b07）

- **323 未闭合块注释**（词法）：静默扫 EOF → 诊断行+`词法错误加一()`（280-a 通道）。
- **295 裸分号空语句**：解析语句 顶部+**解析块 防御分支改拒绝**（双层·宿主「预期表达式」对齐）——原静默跳过=防线缺失。
- **296 当后分号**：当体须 `{`（宿主「预期 '{'」对齐）——原 `;` 被跳过+后续语句吞进当体。
- **侦查**（d38_scan）：17 用例 v2p 实测=16 缺/1 已拒；批1 三项全转拒（4 拒/13 缺·批2/批3 待续）。
- **验证**：v2 树零错误编译+单测 1353/1353+全量 E2E（存量 v2 用例零误伤·自举链实证）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 325-a 开发轮 D37=v2 win 后端 __chkstk 大帧栈探测·2026-09-18·基线 25cda2b）

- **v2 prologue 三段式**：帧 >4096 发射 `mov rax,N / call __chkstk / sub rsp,rax`（MSVC 契约只 touch 页）+入口影子空间预存（rcx/rdx/r8/r9·[rsp+8..+40]）+retbuf/参数改从影子读（[rbp+16+位*8]）；EXTERN 自动收集（P7b）实测命中。
- **39_chkstk 上架双编译对照**（断言输出=是）→ 双编译 PASS（校验和 719400·9600 字节栈帧段）。
- **runner 顺带根治**：v2闭环 分支漏传「断言输出」配置键（261-a 遗留·85 存量零变更·§8.4 广播）。
- **验证**：v2 树零警告编译+单测 1353/1353+asm 指纹（48064 帧三段式）+全量 E2E。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 324-c 开发轮 C24=T44/T27 修复补 linux_x64 后端·2026-09-18·基线 0a9aef8）

- **x64l 四点同步**（用户裁决「家机下一轮认领即」·win 316-a/319-a 蓝本）：①槽补登记（Store.extra+$s1——裸 [rbp] 偏移 0 根治）；②emitCopy i128 双半（判据扩源类型+变量形态双槽）；③除法特判宽度对齐（`cmp r9d,-1`——INT_MIN/-1 SIGFPE 根治）；④窄→128 宽化 movsxd（-1 变 +4294967295 根治）。
- **验证（本机静态面）**：零警告构建+单测 1353/1353+check_asm_width 四维+win E2E 444=443/1（唯一败 406=T43）+x64l .s 四点指纹（c24_probe2 双级别）。
- **运行级复验归深度机**（p314a/p314b 矩阵全绿+回归轨归零=C24 验收原文）。

**门禁**：零警告构建+单测+全量 E2E+四维 asm 宽度+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项+收工补强·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码+**双来源兼容**（字节直放/码点编码——214 用例逐字节重建回归根治·`s[i]` int8 符号扩展实锤）；**T41** 静态局部一次性初始化（?gstatic 全局化）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **T41 写面完备（收工补强·272 死循环实锤）**：复合赋值/自增自减原为半截机制（`桩+=1` 编译为 `*桩=1` 死循环、`桩++` 静默不写回）→ 三写路径统一读-算-写回+findVarEntry 辅助。
- **A2022 收缩族统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；-O3 regAlloc 面 424/426 双绿。
- **E2E 424/425/426** 转正+coverage_map 登记；单测 1353/1353。
- **随载立案 T47（常量遮蔽语义空白）/T48（字符串下标语义空白）**——三方案呈报待裁决（均推荐甲）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项+收工补强·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码+**双来源兼容**（字节直放/码点编码——214 用例逐字节重建回归根治·`s[i]` int8 符号扩展实锤）；**T41** 静态局部一次性初始化（?gstatic 全局化）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **T41 写面完备（收工补强·272 死循环实锤）**：复合赋值/自增自减原为半截机制（`桩+=1` 编译为 `*桩=1` 死循环、`桩++` 静默不写回）→ 三写路径统一读-算-写回+findVarEntry 辅助。
- **A2022 收缩族统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；-O3 regAlloc 面 424/426 双绿。
- **E2E 424/425/426** 转正+coverage_map 登记；单测 1353/1353。
- **随载立案 T47（常量遮蔽语义空白）/T48（字符串下标语义空白）**——三方案呈报待裁决（均推荐甲）。
- **v2p 构建指纹根治**（runner 契约变更·§8.4）：runtime 源纳入指纹——原指纹漏 runtime 致改 runtime 后 v2p 复用旧 obj（256 用例假红实证），删缓存重建复绿。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项+收工补强·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码（'好' 出「好」）；**T41** 静态局部一次性初始化（?gstatic 全局化）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **T41 写面完备（收工补强·272 死循环实锤）**：复合赋值/自增自减原为半截机制（`桩+=1` 编译为 `*桩=1` 死循环、`桩++` 静默不写回）→ 三写路径统一读-算-写回+findVarEntry 辅助。
- **A2022 收缩族统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；-O3 regAlloc 面 424/426 双绿。
- **E2E 424/425/426** 转正+coverage_map 登记；单测 1353/1353。
- **随载立案 T47**（常量遮蔽语义空白·三方案呈报待裁决）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码（'好' 出「好」）；**T41** 静态局部一次性初始化（?gstatic 全局化·2_4_6）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **附**：A2022 收缩族第三犯修复（条件跳转+case 比较物理寄存器收缩·O3 regAlloc 首跑暴露）。
- **E2E 424/425** 转正（424 正例 15 值 O0=O3）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。


## 深度机 linux-x86_64 节

## 功能完善总结（🧪 324-a M3 采样验证轮·2026-09-18，基线 0eaff2b4）

- **采样双轨全绿**：正向 1000（seed=1789687508）差分 2000/2000 全一致零命中；负向 400（seed=1789687530）全拒绝·零崩溃·诊断含行号（连续第七轮）。
- **回归轨对账 129 件**：9 命中全旧类零新类别——**两处漂移（修复未兑现·x64l 运行级）**：★T44（316-a 修复仅落 win 后端 `codegen/x64/`·linux_x64 未同步→i128 三元族仍双级 SIGSEGV·实弹 m44_01 未消+扩面 7 形态）；★T27（x64l -O0 整32 INT_MIN/-1 SIGFPE=`cmp r9,-1` 64 位比较 vs `mov r9d` 32 位装载→特判永不命中；窄整负值→128 宽化缺 movsxd→m27_02~05 O0 错值）。
- **T44/T45/T46 实弹对账**：T45 全转正 ✓（值正确·p314a t45_* 9 形态全健康）；T44 未消；T46 仍读 0（未修复·待裁决·差分轨盲区）。
- 根因铁证=asm+gdb（`[rbp]` 槽偏移 0 / `idiv r9d` 陷阱点）；修复方向=win 同款改动同步 linux_x64 后端四点（emitCopy i128 双半/槽补登记/`cmp r9d,-1`/宽化 movsxd）。

**门禁**：check_handoff/check_progress_sync 绿；双推 gitcode+github。

## 单位机 ARM64 节

## 功能完善总结

### 328-a 开发轮：三机并行协同协议 v2 落地（2026-09-18，用户批准方案，基线 93b11df·分支 任务/单位机-328-协议v2）

1. **协议 v2 落地**（解决用户三痛点：单开发锁浪费两机/测试机发现缺陷不能当场修/旧多锁公共文件写入出错）：开发锁废止——**分支即开发**（任务分支推 gitcode=认领·三机可同时开发·分支名远端唯一=撞号机制化根治）+**队列即集成**（`scripts/integrate.py`=develop 唯一入关口·门禁跑在合并结果上·push CAS 竞争输家自动 rebase 重试≤3·ff-only）+**缺陷分级处置**（机制级当场开修复分支修完随证据备案/语义规范级先呈报三方案经用户裁决）+**按风险分层 try-build**（src/tests 等三平台产物面=另两机在分支上预验证全绿才集成——T44/T27 类「只修 win 漏 x64l」被前置拦截）+**文档随分支走**（共享文档并发写归零·分区升级为合并友好结构保证）+**fix-forward 职权**（develop 红=全员停·任何人可开修复分支/revert）。
2. **AGENTS.md §8 全文重写**（v1 单锁协议〔231-a/232-a〕废止；B8 面台账/B9 famscan/共享文档六纪律/B 区生命周期铁律/禁止 force push develop 等全部承继并入新结构）。
3. **scripts/integrate.py 新增**（合并队列七步脚本·--dry-run 演练/--try-build-done 声明·平台感知门禁 win=ci.ps1·linux=cmake+单测+E2E 显式 --target·门禁增量判定=竞争重试轮 develop 增量纯文档时跳过全量）。
4. **check_handoff.py 扩展**：看板 🏃 行必含 任务/ 分支名（≥328 强制·<328 旧协议在飞轮过渡豁免·收工清行自然消亡）——反例拦截/正例放行双向实证。
5. **plans/027-三机并行协同架构v2方案.md 落盘**（诊断/业界对照〔rustc-bors·Chromium CQ·Google trunk·GitHub merge queue〕/用户三项裁决记录/机制细则/试点安排与复盘区/回退路径）。
6. 看板说明区 v2 口径+协议生效广播+试点邀请；plans/021 文首戳/§4.1 里程碑/§三-A 试点行/§五索引；lessons.md 设计依据条目；HANDOFF/更新日志单位机节。
7. **本轮自身走新流程**：分支开发（认领=推分支）→integrate.py 集成=端到端演练（吃自己狗粮）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
