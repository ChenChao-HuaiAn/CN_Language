# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> 三机并行协作协议落地（2026-09-12 夜，用户批准方案实施）：目标是三机并行开发/验证
> **零 git 冲突、零「结果打架」**，核心思想=**写集不相交 + 看板认领 + 契约广播**。

> ① **AGENTS.md 新增 §8 三机并行协作协议**：§8.1 任务二分——验证轮（只写 `target/` 与
> `/tmp`）免认领随时并行、不占轮次号；开发轮（改任何入库文件）须看板认领、同一时刻至多
> 一机（判据=要 push 的提交含看板行以外的仓库文件改动）。§8.2 看板认领——开工 fetch →
> 改本机行 → push（**push 成功=锁到手**，git 推送即原子 compare-and-swap，零额外工具）；
> 状态符号 ⬜/🏃/🧪；验证结论必须标注基线 commit。§8.3 共享文件分机分区——HANDOFF.md 三节
> 各机整体替换自己节、plans/019 跟踪表三段各追各段、更新日志.md 三节本机覆盖写入、
> lessons.md 维持现状。§8.4 契约变更广播——改 `.expected`/E2E runner/plans/001/spec 须
> 开发轮认领 + 看板通告列标注影响面。§8.5 冲突兜底——手工合并、禁止 force push `develop`。

> ② **远程统一规范（用户裁决）**：远程固定两个、三机同名——`gitcode`（主仓库）与 `github`
> （镜像），**不设其他 remote 名**；每次提交后两远程**同时推送**，github 网络不通说明即可、
> 下次补推。本机 develop 上游已从 github/develop 切到 gitcode/develop。

> ③ **新建根目录 `三机任务看板.md`**（用户裁决：看板是全局协作机制，不绑在 plans/019 专项
> 文档里——三机并行唯一看板，与任务类型无关，任何任务开工/收工必经）；plans/019 §7.0 改为
> 指针，原 §七 跟踪表（90 行）拆为 §7.1 深度机段（开发轮主体）+ §7.2 家机段（85 轮 win
> 收口行）+ §7.3 单位机段（arm64 跨机复验行），各机此后只在自己段末追加。

> ④ **HANDOFF.md 与本文件分机三节迁移**：88-a 交接/更新内容整体归入深度机节，家机节记录
> 本次协议落地与 88-a win 验证轮（并行会话）进行中，单位机节占位。

> ⑤ **诚实边界**：本轮纯文档/规则改动，零编译器源码改动、零测试改动（不触发编译验证门禁）；
> 88-a win 全量门禁由另一并行会话负责，结论出来后按新协议收口；存量表格行仅做了归段移动、
> 内容零改动（git diff 可核）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 89-a 净栈契约出口校验落地 + 浮点字面量全链根治（2026-09-12 第八十九轮，深度机
> linux-x86_64）：88-a 登记观察项「净栈契约出口校验」（**下一轮任务**）**销项**——先以
> **探针两阶段**（自举树全量 + 90 用例批量）验证零误伤 → 出口校验正式化（**当场暴露**
> 取代「下游症状」）；探针同时**抓到 1 处真实契约违反**（88-a 预期「违例面应为零」被
> 证伪），顺藤根治 **v2 浮点字面量全链静默跳过缺陷**。验证链全绿。

> ① **探针零误伤验证（先探针、后转正）**：`生成表达式` 出口挂临时调试输出（入口记进入
> 深度 → 出口算 Δ → 多压/吞外层/零增量分类 + 区间与节点明细）。**结论两件**：（a）**零增量
> 37 处全部为 void 方法调用**——`生成方法调用` 返回形态=空时 `目标寄存器==0` 不压栈
> （「void 不压栈」设计）→ 契约口径确认为 **Δ ∈ {0, +1}**，若直接上「恒净 +1」硬断言会
> **误杀全部 void 调用**（探针先行的价值实证）；（b）**违例面非零**——246 用例
> `标量浮 == 3.5` Δ=+2、区间 [133,136)、进入深度 0。

> ② **缺陷根治：v2 浮点字面量全链（89-a）**：`节点_浮点字面量` 在 IR 层**全部分派点缺失**
> （主循环字面量分支 + `生成字面量` 无浮点分支）→ ①字面量零指令：二元弹栈判定退化（栈上仅
> 1 项 → 左右操作数均取 0）→ 汇编 `mov r10,[rbp-8]; cmp r10,[rbp-8]`（寄存器 0 自比）**恒真**；
> ②净栈 +2 契约违反；③静态浮点初值注入落空（87-a 注入经 `生成表达式` 求值 → 无值可返回 →
> `.data` 恒 0 且无写回，asm 实证 `_cn_gstatic_4` 只有读无写）。**246 v2 侧此前「通过」=
> 双重侥幸**（恒真恰好命中分位 + 初值 0 未被断言）。修复：IR 分派 + `IR_常量浮点` + 共用层
> `浮点文本净`（剥 L/U 尾缀）/`收集浮点常量`（去重）+ 三后端 `.double/REAL8` **文本直通**
> （IEEE754 位模式由汇编器解析——编译期零开销；x64 `.align 8` / ARM64 `.align 3` /
> win `ALIGN 8`）。修复后 asm 实证：`cmp r10,[rbp-160]` 真双操作数 + `floatData_53:
> .double 3.5` + 静态槽入口写回。

> ③ **出口校验正式化（88-b 转正；探针撤除）**：`生成表达式` 唯一正常出口加契约校验：多压
> （Δ>1）/ 吞外层（Δ<0）两条 `IR层诊断`（消息含 增量/区间/进入深度）——IR 层错误即中止纪律
> （计数 → 主.cn 检查 >0 中止，不产 asm）；错误路径（`生成调用 返回 0`）不校验（避免二次
> 噪音）。**性能**：1 次函数调用 + 1 次减法 + 2 次比较（出口深度顺带回传省一次 `大小()`）——
> 终版与基线**同时段**各 3 连测：终版中位 **11.691s** vs 基线中位 **11.611s**（+0.7%，
> 单次波动 ±2% 大于差值=噪声内；跨时段 6+6 次测量中位数差 ≤1%）。Rust 对照：rustc MIR
> validator / ICE——不变量违反立刻报告，不让错误产物进入下游。

> ④ **防线（四件套）**：**正测** E2E **249_双编译对照浮点字面量全链路**（plans/020
> 移植纪律 9 **第十八个**对照用例：八形态〈①静态浮点初值+真/假**双向锚定** ②局部变量
> ③实参 ④返回值 ⑤三元分支 ⑥短路右操作数 ⑦字面量在左 ⑧循环 2000 轮〉折 **255** 两侧
> 一致）；**反证三件全命中**（A 浮点分派回退→246 v2 复现 `[ir2] 表达式净栈契约违反
> (多压): 增量=2 区间=[133,136) 进入深度=0` + 中止；B 吞外层注入→v2 树 `[ir2] 吞外层
> 增量=-1` 44 条 + 中止（两条诊断分支均可达）；C 回退版跑 249→`[ir2] 多压 增量=2
> 区间=[30,33)` + 下游旧症状 `[ir2] 调用被调对象非函数名/方法`〈根因在前、下游在后〉），
> 均已恢复复验零残留。

> ⑤ **验证链**：清场重建**零警告**（0 warning/0 error；宿主产物 md5 `56f55e1c…`/
> `6ffd9dc7…` 重建前后一致=宿主源码零改动）+ 单测 **1306/1306** + E2E **276 用例 272 过
> 0 失败 0 未实现 4 跳**（+249；其余 275 过=**零回归**）+ 锚定链固定点 **338025 行**
> （新口径，88-a 后 335263；+2762=v2 侧 89-a 代码）两代逐字节一致 + 对拍 **34/34**
> （DIFF=0）+ 释放路径两侧对照。

> ⑥ **文档五件套**：plans/019（跟踪表第八十九轮行）+ plans/020（**第三十节 89-a 全节**）+
> lessons（89-a 段，权重 8，预防新增 96~99：防线落地必须先探针量化再转正·输入面含用例
> 批量／分派点按类型全集核对／「修复」与「侥幸」机器码级区分·不变的绿灯也要查／对照用例
> 形态双向锚定）+ 本文件 + HANDOFF。

> ⑦ **诚实边界**：**v2 浮点语义边界新登记**（无浮点指令——面内=字面量装载 + 相等/不等
> 按位模式；面外=算术/大小比较/负号/打印=待补面专项轮）；win-x64（家机）/arm64（单位机）
> 侧浮点发射实物=跨机轮；前轮登记维持（v2 静态字符语法 / 多文件静态注入仅入口模块 /
> 静态槽不登记自动释放 / 诊断无行号 / 行数存量违规面）；探针与反证脚本留 /tmp/p90probe
> （不入库）。

---

> 建立任务进度总入口 + 三处文档同步（2026-09-13 第九十轮起点，**纯文档轮，零代码改动**）：
> 用户指出现有进度体系「翻不到、看不清」——进度散在 20 份 plans 文档与 HANDOFF 中，且部分
> 旧文档停在过时口径。本轮建立**全项目唯一总入口**并修正三处文档与事实不符处。

> ① **新建 `plans/021-任务进度观察表.md`（唯一总入口）**：六节结构——§1 总目标（北极星：
> 自举双目标「性能超 C++ / 安全接近 Rust」+ 三条主线 + 支撑）／§2 当前进度总览（七条战线
> 状态 + 关键证据 + 权威文档指针；含门禁口径与 4 跳说明）／§3 **排班队列**（A 立即 3 项、
> B 待呈报/裁决 5 项、C 已定方案排程 5 项、D 长期登记 5 项、E 外依 1 项、F 长线生态 2 组）
> ／§4 已完成里程碑时间线／§5 近期轮次索引（73-a~89-a 十七轮 + 门禁）／§6 文档导航（哪里
> 看什么）。**更新纪律**：每轮收尾随文档五件套一并更新本表（勾销/调整/追加）。

> ② **`plans/019` 阶段路径总表状态列刷新**：原表停留在立项口径（阶段 1 标「🔄 本轮启动」、
> 4' 标「待裁」）→ 按 §7 执行状态跟踪表实测口径改：阶段 1 ✅ 第五十七轮（浅转移优化=3b
> 六十一轮）／阶段 3 ✅ 五十九至六十五轮（剩余跨语句 NLL ⬜ 待真实使用反馈）／阶段 4 🔄
> 第一层 62 + 第二层两批 63/64（第三批收口 ⬜ 呈报）／阶段 4' 🔄 方案A 已裁决 + 一/二批 +
> A1/A2 落地（第三批 ⬜ 排程）；并加「实时状态以 §7 跟踪表 + plans/021 为准」说明。

> ③ **HANDOFF 锚定链固定点数字勘误**：原文**三处**写「337973 行」（其一含增量 +2710）
> 为笔误——以 `target/p71x64l/work/fix_p.s` 实测为准（`wc -l` = **338025**，与 plans/019
> 跟踪表/plans/020 §30/更新日志三处一致；fix_p≡fix_s md5 `d063b698…`），已改正并留勘误注记。

> ④ **`plans/002` 索引加导航**：该索引为 v1 时代（阶段 0~7）实施计划，加醒目指针指向
> plans/021（避免新会话误将其当作当前总表）。

> **诚实边界**：第九十轮零代码改动、零测试影响——验证链未重跑（无源码变更，按纪律不需要）；
> 固定点数字为产物实测复核（`wc -l`+`md5sum`），未重跑锚定链。`plans/007`（v1 时代未完成
> 任务清单）仍为过时状态，已在 plans/021 §6 标注「待重评」；其 F 组长线条目（P4 优化器族/
> P6 生态族）待下次规划轮逐条重评后销项或重排。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
