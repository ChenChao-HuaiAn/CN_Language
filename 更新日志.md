# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十八轮 118-a：D1 行数整改第九波（codegen 面开波）——`x64_instructions.cpp` 1812→921 行按族拆分**（2026-09-13，家机 win-x64；基线 6da51e8）：
> ①**拆分**（纯重构零行为变更）：`x64_instructions.cpp` **1812→921 行**，拆出 `x64_instructions_i128.cpp`
> （**198 行**：i128 双槽全 128 位运算 emitInt128Binary/emitInt128MulDivMod/emitInt128Compare）/
> `x64_instructions_cast.cpp`（**419 行**：类型转换 emitCast〔270 行〕+ 比较 emitCompare + 逻辑非 emitNot）/
> `x64_instructions_mem.cpp`（**301 行**：变量加载存储 emitLoadStore + 取地址 emitAddrOf + 字段地址
> emitFieldAddr + 指针访存 emitPtrLoadStore）；`static shiftAmtOf` 仅被 emitShift（留主文件）引用→留主；
> CMakeLists 挂载 3 文件。
> ②等价性=产物 asm 五样本逐字节一致（v2 全树 404786 行等）。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 连续第七次不变** +
> 对拍 **34/34** + 运行级 **3/3**。
> ④**踩坑（新族）**：首次构建 **LNK1168**（无法写 cn.exe）——`tasklist` 查出**同机另一 ZCode 会话**正并发跑
> `cn.exe check`（`target/probe_b345/scan_warn.py`，早于本机认领启动，属验证轮免认领）→ 不干扰、轮询等待
> ~3 分钟后重试构建成功。预防：LNK1104/1168 先 `tasklist`+`Get-CimInstance Win32_Process`（CommandLine 含目标
> 路径=决定性证据）排查占用，勿先疑自身代码。
>
> **本会话（111-a~118-a）累计**：111-a 门禁跃变（C14/C15 销项 + 新缺陷 221 双侧根治） + **D1 八波**
> （ir_expr 2798→734 / ir_oop 1381→508 / ir_oop_call 1155→763 / 语义层去重 -536 / semantic_expr 1778→654 /
> semantic.cpp 1996→692 / x64_instructions 1812→921）——**宿主原 7 个超长文件仅剩 `ast.hpp`**；
> 锚定链固定点 406097 全程一致（七次复验）+ 产物 asm 五样本逐字节一致 + 全量零回归。
>
> ⑤诚实边界：D1 剩余（`ast.hpp` 1032 + codegen 面 3 文件 + v2 侧 6 文件 + 函数级）登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
