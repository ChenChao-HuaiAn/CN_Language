## 功能完善总结

### 一、②c 前置：宿主缺陷簇 1-4 根治（代码落地，双机验证进行时）

> 背景：用户裁决 ②c 四项一轮收官（嵌套容器/容器作结构体字段/顶层静态/引用参数，E2E 127 载体）；
> 第 0 步探针（target/dbg/probe127/p1~p24）实证出宿主缺陷簇，用户裁决「先根治宿主缺陷簇」。
> 验证状态（诚实门禁）：单位机（麒麟 ARM64）构建零警告 + 单测 1196/1196 + 探针 17/17 +
> v2p-119 冒烟 PASS；E2E linux-arm64 全量复确认与 118/78 分诊未完（换电脑中断）；
> win-x64 全量门禁（ci.ps1）待 Windows 家机执行（②b 起欠项一并还）。

- **缺陷4 stdlib 拷贝构造下分配（越界写+读损坏）**：向量 拷贝构造 `分配(数组容量 * 8)`
  （容器.cn:55）与 映射 拷贝构造 键数组/值数组（映射集合.cn:156/164）对 >8 字节元素下分配
  （p8 实证 3×24 字节写进 4×8=32 字节缓冲，b[2].丙=2 应为 3；v2 组件自用 映射<整64,符号>
  拷贝非空映射即越界）——对齐同文件扩容路径 `max(8, 类型大小(T/K/V))`；链表/栈/队列无拷贝
  构造不触发。
- **缺陷1' 成员表达式下标步长兜底 8**：类/结构体对象的指针字段下标（拷贝构造
  `其他.数据[索引]`，元素 >8 字节）原只查 StructDecl 数组字段——读源错位重叠写入（p9 实证
  b=[11,12,13,12,13,21,...]）。新增 `memberFieldSrcType(MemberExpr*)` 统一解析（结构体查
  StructDecl、类查 classFieldType），接线 lvalueAddress 与 visitIndexExpr 的步长+元素形态
  双分支（ir.cpp / ir_expr.cpp）。
- **缺陷1 类容器字段成员表达式访问统一**：handleClassMemberExpr 类值字段读取改返回 LoadPtr
  对象指针（对齐方法内直访 A-4 语义）——原返回字段地址致 `e.表.大小()` 空指针崩溃（p2）、
  `其他.表.大小()` 静默 0 深拷贝失效（p6/p7，向量<词条> 追加元素内层为空）。
- **缺陷3 类字段级联析构 + 对象零化分配**：① `__cn_object_new` malloc→calloc（未构造字段
  =null 空安全，原裸 malloc 槽垃圾野指针）；② 新增 `injectFieldCascadeDestroy`——~类名()
  体后按字段逆序对「有析构类」字段发射 DeleteObject（对标 C++ 成员析构；p10 二十万次
  创建/销毁峰值 RSS 1.3MB、p11 向量<词条> 全链级联 20 万内层向量零泄漏实证）；③ 语义警告
  「拥有式字段建议声明析构」（不能 Error——会打爆已验收 E2E 58）；④ v1 组件 x64后端/arm64后端
  补声明空体析构（其 输出行 向量原先真泄漏）。
- **缺陷2 结构体容器字段全链支持（函数IR.指令 = 向量<IR指令>，v2 自举组件核心用法）**：
  ① 语义趟序重排：registerGenerics → 结构体字段实例化归一（resolveGenericTypeName）→
  computeLayout——原布局计算在泛型注册前，容器字段 typeSizeOf 防御兜底 8（字段 24 被算成 8，
  总大小/槽区/零初始化全错位）；② 迭代稳定性修复（ASAN 实证 heap-use-after-free：
  ensureLoweredType 向 node->structs emplace_back 触发 vector 重分配打悬 range-for 引用——
  改裸指针+索引循环）；③ 结构体裸声明逐槽零初始化（genVarDecl，对齐 v2 B1 语义——裸声明
  未构造字段=确定性错误码3，原为栈垃圾段错误）。
- **⭐ 模型定案（勿翻案）**：结构体/类容器字段一律**指针槽模型**——字段区按 typeSizeOf(容器)
  保留、首 8 字节存堆对象指针，与类字段 A-4、byRef 机制、静态容器 P3-8 全线一致；构造赋值
  `f.表 = 向量<整64>()` 走 NewObject+构造+StorePtr、读取 LoadPtr、方法 this=对象指针。
  （中途试过内联模型——构造调用以字段地址为 this 直接构造——被 p24 最小复现证伪：
  `填(f.a, 42)` 中 byRef 容器参数作方法接收者双层解引用，已全部回退。）

### 二、遗留分诊（下一会话）

- E2E 118「输出不一致」未看 diff（高嫌疑=成员表达式统一改动影响）；
- E2E 78 由内存超限(-9) 变段错误(-11)（高嫌疑=级联析构注入双重释放，v1 后端对象可能已被
  既有路径删除）——确认后按缺陷零容忍上报 ≥3 方案；
- E2E linux-arm64 全量复确认（回退指针槽后）；win-x64 全量门禁 ci.ps1（家机）；
- 之后按批准计划继续 ②c：E2E 127 立项（供给类契约已探明：p4/p6/p7——值字段+构造内
  `表 = 向量<整64>()`+拷贝构造逐元素法+声明空体析构）→ A 嵌套容器 → B 容器字段（v2 侧
  指针槽模型）→ C 顶层静态 → D 引用参数 → 注册门禁 → 文档收官。
