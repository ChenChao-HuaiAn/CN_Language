# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项+收工补强·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码+**双来源兼容**（字节直放/码点编码——214 用例逐字节重建回归根治·`s[i]` int8 符号扩展实锤）；**T41** 静态局部一次性初始化（?gstatic 全局化）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **T41 写面完备（收工补强·272 死循环实锤）**：复合赋值/自增自减原为半截机制（`桩+=1` 编译为 `*桩=1` 死循环、`桩++` 静默不写回）→ 三写路径统一读-算-写回+findVarEntry 辅助。
- **A2022 收缩族统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；-O3 regAlloc 面 424/426 双绿。
- **E2E 424/425/426** 转正+coverage_map 登记；单测 1353/1353。
- **随载立案 T47（常量遮蔽语义空白）/T48（字符串下标语义空白）**——三方案呈报待裁决（均推荐甲）。
- **v2p 构建指纹根治**（runner 契约变更·§8.4）：runtime 源纳入指纹——原指纹漏 runtime 致改 runtime 后 v2p 复用旧 obj（256 用例假红实证），删缓存重建复绿。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项+收工补强·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码（'好' 出「好」）；**T41** 静态局部一次性初始化（?gstatic 全局化）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **T41 写面完备（收工补强·272 死循环实锤）**：复合赋值/自增自减原为半截机制（`桩+=1` 编译为 `*桩=1` 死循环、`桩++` 静默不写回）→ 三写路径统一读-算-写回+findVarEntry 辅助。
- **A2022 收缩族统一设施**：shrunkOperand/narrowOperand 两辅助+win 后端 30+ 装载/出口点收口（含 258-a 遗留 `e14` 拼名修正）；-O3 regAlloc 面 424/426 双绿。
- **E2E 424/425/426** 转正+coverage_map 登记；单测 1353/1353。
- **随载立案 T47**（常量遮蔽语义空白·三方案呈报待裁决）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。

## 功能完善总结（🏃 320-a 清零波次6=语句表达式语义五项·2026-09-18·基线 d6ac72a）

- **T37** 选择负值/常量表达式标签（parseCaseValue 窄面）；**T38** 循环头作用域限定（同名两循环过·泄漏读拒）；**T40** 字符拼接码点 UTF-8 编码（'好' 出「好」）；**T41** 静态局部一次性初始化（?gstatic 全局化·2_4_6）；**T42** 空类型参与表达式拒绝（Rust () 同款）。
- **附**：A2022 收缩族第三犯修复（条件跳转+case 比较物理寄存器收缩·O3 regAlloc 首跑暴露）。
- **E2E 424/425** 转正（424 正例 15 值 O0=O3）。

**门禁**：零警告构建+单测+全量 E2E+双文档门禁；双推 gitcode+github。


## 深度机 linux-x86_64 节

## 功能完善总结（🧪 324-a M3 采样验证轮·2026-09-18，基线 0eaff2b4）

- **采样双轨全绿**：正向 1000（seed=1789687508）差分 2000/2000 全一致零命中；负向 400（seed=1789687530）全拒绝·零崩溃·诊断含行号（连续第七轮）。
- **回归轨对账 129 件**：9 命中全旧类零新类别——**两处漂移（修复未兑现·x64l 运行级）**：★T44（316-a 修复仅落 win 后端 `codegen/x64/`·linux_x64 未同步→i128 三元族仍双级 SIGSEGV·实弹 m44_01 未消+扩面 7 形态）；★T27（x64l -O0 整32 INT_MIN/-1 SIGFPE=`cmp r9,-1` 64 位比较 vs `mov r9d` 32 位装载→特判永不命中；窄整负值→128 宽化缺 movsxd→m27_02~05 O0 错值）。
- **T44/T45/T46 实弹对账**：T45 全转正 ✓（值正确·p314a t45_* 9 形态全健康）；T44 未消；T46 仍读 0（未修复·待裁决·差分轨盲区）。
- 根因铁证=asm+gdb（`[rbp]` 槽偏移 0 / `idiv r9d` 陷阱点）；修复方向=win 同款改动同步 linux_x64 后端四点（emitCopy i128 双半/槽补登记/`cmp r9d,-1`/宽化 movsxd）。

**门禁**：check_handoff/check_progress_sync 绿；双推 gitcode+github。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
