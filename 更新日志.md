## 功能完善总结

### 阶段6收尾子任务（2026-08-15）——gitcode 推送重试 + 最终状态确认

**背景**：阶段6标准库全部 7 个子任务完成后收尾：重试 gitcode 远程推送（前序因服务端 403 资源限制受阻）+ 全量文档/模块核查。

- **推送结果**：`git push gitcode develop` 重试 3 次（间隔探活 `git ls-remote gitcode develop`），均被服务端 403 资源限制拒绝（`No available resources at the moment`；读操作正常、写资源不可用）。**本地 develop 领先 origin/develop 9 个提交已安全保存**，待服务端恢复后重推即可。
- **文档核查结果**：plans/002-阶段6-标准库.md Task 6.1~6.12 全部 [x] ✅；更新日志已覆盖阶段6内容；HANDOFF.md 补齐 gitcode 推送状态 + P1 待办（复数）；lessons.md 缺陷标注完整（4 项 ✅已修复 / 📌已知限制）。
- **文件齐全性**：stdlib/ 10 模块（核心/容器/数学/算法/映射集合/IO/文件/字符串扩展/时间/系统）+ src/runtime/ 10 文件（含 input_api/math_api/file_api/time_api/system_api）全部齐全。
- **测试基线**：单测 1050/1050、E2E 44/44、编译 0 警告。

### Debug 全面审查子任务（2026-08-15）——阶段6「标准库」前序 4 项记录缺陷全部根治

**背景**：阶段6标准库 6 个子任务完成后，按用户规则16 强制 Debug 全面审查，逐一修复前序子任务记录的编译器缺陷。全量回归：单测 **1050/1050**（1048 + 2 新增）、E2E **44/44**（40 + 4 新增）、编译 0 错误 0 警告（MSVC /W4 /WX）。

#### 1. 大栈帧无 __chkstk 栈探测崩溃（高优先级，根治）
- **根因**：x64 codegen 栈帧（寄存器槽区 + 变量槽区，含打印变参展开临时寄存器）超 Windows 栈 guard 页（约 8KB）时，`sub rsp, N` 一次性越过未提交栈页 → 0xC0000005（首个打印前即崩）
- **修复**：`src/cn_compiler/codegen/x64/x64_codegen.cpp` `emitPrologue` 栈帧 >4KB 发射 MSVC 三段式 `mov rax, N / call __chkstk / sub rsp, rax`（__chkstk 按 4KB 页探测提交，符号由 libcmt.lib 提供）；`emitCodeHeader` 加 `EXTERN __chkstk:PROC`；`emitParamSetup` 提前到 chkstk 之前（__chkstk 破坏 rcx 参数寄存器，参数槽 rbp 相对寻址安全）
- **验证**：单测 `BigFrameEmitsChkstk`/`SmallFrameNoChkstk`（test_x64_codegen.cpp）+ E2E 39_chkstk（1200 局部变量 >9KB 栈帧，修复前崩溃、修复后输出 校验和 719400）

#### 2. 泛型 + 函数指针回调组合（中优先级，根治）
- **根因**：`substTypeParam`（语义层 generics.cpp）/`substGenericType`（IR 层 ir_generic_func.cpp）的模板分支丢弃 `<...>` 之后的后缀（函数指针参数列表 `(T,T)`），实例化签名缺函数指针参数（`函数指针<整32>` vs `函数指针<整32>(整32,整32)`）→ "未找到匹配的函数"
- **修复**：两处替换函数保留模板后缀并递归替换内嵌类型参数（T→实参）；泛型函数参数用 `funcPtr.toString()` 规范化字符串
- **验证**：E2E 40_generic_funptr（泛型冒泡排序 + 函数指针回调，升序 1 2 5 8 9 正确）

#### 3. 构造函数重载覆盖（中优先级，根治）
- **根因**：`ClassInfo.methods` 按方法名（name）索引，同名构造函数（无参/带参）互相覆盖
- **修复**：class_resolver.cpp 构造/析构 methods 表 key 改为 sigKey（名#参数串）多版本共存（普通方法仍按名，虚表/接口不受影响）；语义层 semantic.cpp 构造查找按 isConstructor+ownerClass+实参个数+类型匹配最优并记录 resolvedSignature；IR 层 ir_oop_call.cpp 按 resolvedSignature/实参个数兜底匹配；泛型实例化类（generics.cpp）同样 sigKey key
- **验证**：E2E 41_ctor_overload（无参/带参分别实例化：无参容量 0 / 带参容量 42 正确）

#### 4. 泛型类方法内循环遍历第一个泛型字段（中优先级，根治）
- **根因**：IR 层 `emitClassMethod`（ir_oop.cpp）提升泛型实例化类方法体时未设置 `genericTypeParams_`（T→实参）——`T 总和` 局部变量映射为 ptr，`总和 + 数据[索引]` 被当指针算术（值左移3当地址）→ 索引≥2 读垃圾；语义层 `resolveGenericTypeName` 对裸类型参数 T/T* 未替换
- **修复**：`emitClassMethod` 开头从 className 解析 `$` 实参设置 genericTypeParams_（尾部恢复）；`resolveGenericTypeName` 补裸参数与指针替换；`findGeneric` 提为 public 供 IR 层访问
- **验证**：E2E 42_generic_field_loop（循环遍历求和 210 正确 + 打印变参内嵌字段下标 元素0/1/2 正确——同时修复 lessons 记录的"打印变参内嵌泛型类字段下标损坏"）

#### 5. 低优先级项核实（全部确认状态）
- Store 物理寄存器宽度（r12→r12d）/ emitCast 截断：✅ 已修复（代码核实 x64_instructions.cpp widthFor 全覆盖）
- 语义层 字符串/字符* 与 空类型* 比较误拒：✅ 已修复（semantic.cpp isPointerType 补 字符串/字符*）
- `字符串` 类型关键字不能作限定名前缀：📌 词法限制非缺陷（已文档标注，用 解析. 前缀规避）

#### 新增回归测试
- 单测：`X64CodegenTest.BigFrameEmitsChkstk`、`X64CodegenTest.SmallFrameNoChkstk`
- E2E：39_chkstk（大栈帧 __chkstk）、40_generic_funptr（泛型+函数指针）、41_ctor_overload（构造重载）、42_generic_field_loop（泛型类字段循环+打印变参）

#### 文档更新
- `plans/002-阶段6-标准库.md`：Task 6.1/6.4/6.7/6.11 缺陷标注 ✅已修复 + 新增 Task 6.12 Debug 子任务记录
- `lessons.md`：5 条"待修复"记录标题标注 ✅已修复（含根因/修复/验证摘要）
- `HANDOFF.md`：当前状态/测试基线/关键架构约定/踩过的坑/下一步计划全面更新
