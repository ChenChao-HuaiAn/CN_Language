# CN语言编译器 更新日志

## 功能完善总结

**本轮（2026-09-07 第二十九轮，单位机 麒麟 ARM64）**：跨机轮 arm64 半程——两枚跨机首跑暴露缺陷根治（用户裁决均方案A·Rust 准绳：性能第一/安全第二）+ v2self 三级锚定 arm64 首达。

### 一、E2E 154 arm64 首跑失败 → 同族缺陷根治（方案A·单一锚定源）

- **现象**：`隐藏返回指针栈参数锚定`（第二十二轮 linux_x64 缺陷的回归锚定用例）arm64 首跑即红：期望 87654321/8/87654321，实得 5497169789127654321/549716978912/87654321；垃圾值 0x7FFDAF6CE0=被调方保存的旧 x19（栈地址）。
- **根因**：arm64 后端 emitParamSetup 标量/浮点栈参旁路硬编码锚定基 16，无视 needHiddenRet（sret/i128/u128 返回时 prologue 多压 `stp x19,xzr` 16B，真实栈参在 [x29,#32]）；结构体栈参路径恰走 parameterRegister=正确——第二十二轮修 linux_x64 时以其为「同构对照」漏查旁路。
- **修复**（src/cn_compiler/codegen/arm64/arm64_codegen.{hpp,cpp}+linux_x64/{hpp,cpp}）：arm64 新增 `stackParamBase()`（needHiddenRet?32:16）三处收敛；浮点槽序改按参数位号 paramPos 与调用方统一；linux_x64 `intParameterRegister` 栈分支收敛 `stackParamAnchorBase()`+被证伪注释归真；新增单测 3 项。影响面：v2/stdlib 全源码扫描零踩中，唯一触发面即 154 本身。

### 二、v2self 锚定链链接失败 → v2/宿主静态槽对齐双侧归正（方案A，Rust 对照后裁决）

- **现象**：v2self_gen1.o 链接 `R_AARCH64_LDST64_ABS_LO12_NC against '.data'` 截断——arm64 全链自举首次链接。
- **根因**：v2 ARM64 后端静态槽（本地标签 `_cn_gstatic_N`）紧跟任意长度字符串 `.byte` 无对齐，GAS 折叠成 `.data+非对齐addend`（实证 0x2b9a）→ 64 位 ldr/str LO12 重定位要求 8 对齐=链接必败；宿主同位置靠全局符号侥幸可链接但运行期静态访存全非对齐。x86_64 无此限制=平台差异掩盖面。
- **修复**（CN语言编译器v2/代码生成ARM64.cn + src/cn_compiler/codegen/arm64/arm64_codegen.cpp）：静态槽区各补 `.align 3`。Rust 对照：rustc/LLVM 逐全局按 align_of(T) 发射 .align（对齐=发射期布局契约），非对齐访存=UB 绝不靠链接器宽容——方案C（符号全局化绕过检查）被否。

### 三、v2self 三级锚定 arm64 首达

13 组件冒烟×2 全绿（**IR容器 子链转绿跨平台成立**）→ 组件对拍 13/13（4 裸逐字节+9 strData 平移规范化一致）→ 全链对拍 PASS → **字节级固定点 g1=g2 裸逐字节一致（299587 行 asm）** → 8 用例运行级对拍 8/8（119/123/126/128/130/148/150/158）。锚定三件套（anchor.sh+norm.py+产物）target/dbg/p29/ 不入库。

### 四、门禁

清场重建零警告 + 单测 **1243/1243**（+3 锚定测）+ **全量 E2E linux-arm64 160 用例 156 过/1 败（78 已知遗留内存超限）/3 跳（62/69/79 平台限制）**——24 例 v2 闭环（含 23 例解锁+158）首次 arm64 动态全绿、145/146 分号专项负路径 arm64 首验。诚实边界：win 家机侧复验=跨机轮剩余半程。

### 五、文档

plans/014 第二十九轮行 + plans/017 第十九节 + lessons 三条（同族修复旁路穷尽/静态对齐布局契约/假对拍第五案·规范化基准跨文件确定性）+ HANDOFF 第二十九轮。
