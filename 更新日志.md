# CN语言编译器 更新日志

## 功能完善总结

# 第四十七轮（2026-09-09 家机 win-x64 纯验证轮零源码改动）：跨机轮 win 侧动态收口——第四十六轮四项改动 win 实物全绿 + v2self win 口径锚定首立（235968 行）+ 组件对拍 29/29 + E2E 183~187 win 首验

按第四十六轮 HANDOFF 挂账二.1，拉取 cb7a106（第四十五轮 深度机 X64L 收官）+ 9a9425d + b23ef7a（第四十六轮 构造字面量宿主 D1/D2/D3 + v2 观察项①③ + 186 双根因根治）快进合并，执行本机（win-x64）动态收口。

## 一、全量门禁（win 侧首验）

- 清场重建 MSVC **零警告**（/W4 /WX，日志 warning 字样仅单测用例名）rc=0。
- 单测 **1251/1251** PASSED。
- 全量 E2E win-x64 **189 用例 187 过/2 败/0 跳**——败=78_chain_build（4102MB）/79_bootstrap_closed_loop（4105MB）已知 OOM 家族，与第四十四轮基线（4116/4112MB）同族**零回归**。
- **E2E 183~187 win 侧首验 5/5 PASS**：
  - **186_v2_构造赋值位（rc=84）**=第四十六轮 186 双根因修复（解析层嵌套下钻链弹出+IR 层 解析成员链结构体ID/取成员宽度码 主干替换）win 实物首验——s1=16+s2=12+s3=56 全对，残链 8 字节越界写修复在 win 后端 ml64 实物闭环；
  - 183_构造字面量形态矩阵（宿主 D1/D2/D3 四形态）/184_宿主比较层分级 win 首验；185_v2_比较层两级/187_v2_构造赋值穷举负测经 v2p win 后端编译实物验证。

## 二、v2self win 口径锚定首立

- v2p_46 重建：宿主编译 v2 全树 rc=0 → ml64 → link（10 运行时 obj 含 intern_api）。
- **fix_p**：v2p 编译 v2 自身 → **235968 行**（win 口径首立；分后端口径不跨后端比较——X64L=227614 行第四十六轮/arm64=302433 行第四十三轮待复锚）。
- cn_self46 链接：ml64 + link `/FORCE:MULTIPLE`（**cn_self46.obj 在前** + v2p_46.obj 借链容器方法符号 + 10 运行时 obj + /MAP）。
- **绑定自检（决定性证据）**：map 行 `0001:000d4764 cn_main ... f cn_self46.obj`——可执行 CN 代码全部来自自举第一代产物。
- **fix_s**：cn_self46 编译 v2 自身 → 235968 行 → **固定点 fix_p ≡ fix_s 裸逐字节一致**（win 口径首次确立，自举封闭性在 win 后端成立）。

## 三、组件对拍与运行级

- **组件对拍 29/29 全量逐字节一致**（v2 全树非 主.cn 文件 29 个：6 包根+23 成员；v2p_46 与 cn_self46 各编译后 asm cmp 零差异零失败）。
- **运行级三用例 3/3**：cn_self46 编译 → ml64 → link → 运行——hello rc=0 / 119_v2_多文件链接闭环 rc=14 / 173_v2_目录包根撞名成员 rc=173，全对齐深度机 X64L 口径。

## 四、诚实边界

- 本轮纯验证轮：零编译器源码改动；探针与脚本留 target/p46win/（self_chain.py / comp_diff.py / run_level.py 及产物，不入库）。
- **arm64 侧复验/固定点复锚（302433 行口径预期增长）= 单位机下一跨机轮**（第四十六轮 HANDOFF 挂账二.2）。
- 78/79 OOM 为 v1 旧架构固有败面（推倒重建动机），不计入回归。
