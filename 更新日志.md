## 功能完善总结

**时间**: 2026-08-16（规格书同步批次）

## 新增语法规范入规格书 + 文档/行为统一 ✅（用户批复后执行）

**基线**：单测 1159/1159、E2E 63/63、编译零警告（MSVC /W4 /WX）。

### 一、cn-language-spec skill specs 更新（用户批复同意）

| 规格 | 新增/修改 |
|------|----------|
| 07-错误处理 | 新增「一B 错误传播运算符 表达式?」（Rust ? 同语义：结果/可选取值、失败构造错误结果并返回、返回类型兼容规则、后缀优先级）；删除「新版无 ? 运算符」句 |
| 03-语句与控制流 | 新增「3.3B 对...属于 迭代语句」（数组→下标、类容器→大小()/元素()）；「3.4 选择语句」扩展：字符串字面量/多值分组/裸枚举成员名 + 一致性约束；fall-through 改为**与 C 一致**（用户裁决） |
| 04-函数与函数指针 | 新增「一F 引用参数」（T& 语义：读取解引用/赋值写回/仅参数/泛型 T&）；新增「一G 外部 函数 声明（FFI）」（C 链接符号=纯名、--链接 附加库） |
| 02-类型系统 | 新增「六B 类型大小 内建」（C++ sizeof 等价物，**明确非函数**：编译期求值/不可取地址/不可作函数指针） |
| 01b-字面量与运算符 | 优先级13 增加后缀 ?（错误传播）；5.8B 说明 ? 与三元 ? 的区分 |
| 01a-词法结构 | 新增「5.9B 上下文关键字」（对/属于/外部 非保留字）+ 变更记录 |

### 二、plans/001 CN语言编译器设计规格书.md 同步

- 5.3 函数定义：引用参数 + 外部 函数 声明
- 5.4 错误处理：错误传播运算符 表达式?
- 5.5 控制流：对...属于 + 选择 增强（字符串/多值/裸枚举）+ fall-through 与 C 一致
- 3.6B 类型大小 内建（注明非函数）；4.5 优先级表加后缀 ?

### 三、测试/行为统一（用户批复一并处理）

- **布尔打印统一**：打印 布尔表达式（比较/逻辑结果 i1）统一输出 真/假（新增运行时 __cn_print_bool；此前输出 1/0 与布尔转字符串不一致）；更新 34_file/36_time/37_system/38_tool/48_prelude/62_ffi 共 6 个用例 expected（1 → 真）
- **注释修正**：55_reference 规格书引用 04-一D → 04-一F（一D 实为 lambda）；28_class_members 过时注释更新（A-1 修复后 账户& 成员访问已支持）
