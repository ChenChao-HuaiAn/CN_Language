# 功能完善总结

> 73-a v2 侧块级作用域 RAII 失效根治（2026-09-11 第七十三轮，深度机 linux-x86_64）：用户裁决方案 A——登记时机对齐宿主 + 跳出 drop-on-jump + 「释放+清零」幂等模型。

## 一、立案：plans/020 矩阵靶子核验揪出两枚 P0（探针量化实证）

本轮任务=矩阵第 27 格「v2 侧跳出路径（中断/继续）drop-on-jump 专项核验」。核验中揪出的问题比靶子更深一层——**v2 侧块级 RAII 从未生效**。

| 探针形态 | 分配次数 | 退出时活动分配数 | 判定 |
|---|---|---|---|
| 循环体内声明拥有串 + `继续`（20 万轮） | 200,000 | **199,999** | 每轮泄漏 |
| 循环体内声明拥有串 + 自然回边（对照） | 200,000 | **199,999** | 每轮泄漏 |
| **如果体**内声明拥有串（无任何跳出语句） | 200,000 | **199,999** | 每轮泄漏 |
| 循环体内声明容器（2 万轮） | 20,000 | 对象未析构 | asm 中 `~向量` 仅 1 次（函数级） |

生成物层实证：`继续`/`中断` 跳转前**无任何 `__cn_str_free`**（正常路径每轮都有）。30000 轮 ×14.4KB 探针泄漏上限 411MB（内存上限下 OOM）——与 v2「安全接近 Rust、泄漏零容忍」双目标直接冲突。

## 二、根因（诊断注入实证，两枚，均属 v2 树，宿主不受影响）

**缺陷甲（P0）：块出口析构的基线机制与预扫登记相互抵消 → v2 块级 RAII 从未生效**

`预扫容器声明` 在函数体生成**之前**做函数级一次性扫描，把函数内所有（含全部嵌套块）拥有型容器/字符串登记进释放名单；而 `生成块` 入口取基线 `= 名单.大小()`，出口只释放「基线之后新增项」→ 进块时名单**早已包含**本块变量，基线恒等于边界，**永远无新增项可释**。诊断注入输出：`[诊断] 登记拥有串: 体内` 出现在预扫阶段（生成之前），随后 `块出口析构: 字符串基线=1 现名单=1` 证明零释放。对照：结构体容器字段（生成期登记）基线机制正常——同一函数内两种登记时机构成天然对照实验。

**缺陷乙（P0）：跳出路径无 drop-on-jump**

`生成语句` 的 `中断`/`继续` 分支只发跳转、不释放（宿主 72-a 已用 `genJumpDestructFrom` 实现）。

**修复首版自身缺陷（当轮实证闭环）**：登记时机+跳出释放落地后，容器跳出形态 **SIGSEGV**——跳出释放与函数级兜底各释放一次（gdb 栈 `free <- cn_free <- ~向量 <- bb0_4`），根因=「编译期名单不截断 + 释放点不清零」。

## 三、实施（v2 树 IR 4 文件，方案 A）

1. **登记时机对齐宿主**（`IR容器.cn`/`生成声明.cn`）：`预扫容器声明` 职责收敛为「槽分配 + 入口零初始化 + 类型登记」；释放名单追加移至生成期——`生成变量声明`（字符串，拥有化初始化后）+ `生成容器局部`（容器，非返回接收分支）；新增 `名单追加去重`（v2 局部槽=函数级映射，同名合槽须去重防多次释放）。
2. **跳出 drop-on-jump**（`IR容器.cn`/`生成语句.cn`/`生成函数.cn`）：`函数IR` 新增三枚循环体基线（`循环串基线`/`循环容基线`/`循环字段基线`），`生成当` 进入体块前记录、退出后恢复（嵌套循环自然正确）；新增 `循环跳出析构`——按最近循环体基线释放，编译期名单**不截断**（同块内互斥路径如「如果{中断}」与自然结束各自仍须发射释放，截断会让另一路径无变量可释）。
3. **「释放+清零」幂等模型**（宿主 72-a 同款，实证补入）：`函数IR` 新增 `零寄存器`（预扫回填），三处释放点（块出口析构 容器/字符串段、结构体容器字段级联）释放后**槽清零**——多路径共享槽，清零后空安全跳过，杜绝双释（容器字段须先另存字段地址寄存器再清零——加载会把值寄存器指向对象）。
4. **`块出口析构` 新增 `截断` 参数**：块出口传 真（释放并截断名单，保证外层块基线正确）；循环跳出传 假。不截断模式遍历用下标递减（不以名单长度作循环条件，避免死循环）。
5. **连带**（`IR布局.cn`）：v2 内置表补 `内存::活动分配数`（符号+返回类型两处，宿主 `ir_call.cpp:266` 对齐）——E2E 216_v2 的泄漏断言口径需要它。

## 四、成效与防线

- **归零闭环**：五形态（继续/中断/自然回边/如果体/容器跳出）20 万轮级残留 **199,999 → 0**（LD_PRELOAD 读 `__cn_alloc_live`）；v2p 编译 v2 树退出残留 2,755,596 → 2,757,891（同量级=编译器自身常驻结构，泄漏主体已消除）。
- **影响面**：v2 树 63 处循环体内声明（20 处拥有型）/2585 个嵌套块；**悬垂风险扫描 = 0 命中**（全树「块内声明 → 容器追加/静态存储/返回」零处，v2 树已按 A2 纪律迁移，修复不引入悬垂）。
- **新防线**：**E2E 216_v2_块级作用域RAII内存回归**（七形态：继续/中断/自然回边/如果体/容器跳出/嵌套块/内容校验，每形态调用前后 `内存::活动分配数()` 差值恒 0 且内容校验全对，退出码 127）——宿主 E2E 213 的 v2 对应件，两侧防线自此对称。

## 五、验证链（全绿）

清场重建零警告 + 单测 1289/1289 + E2E **238 用例 233 过**（唯一失败=194 既有待批）/4 跳 + 锚定链固定点 **266845**（新口径）逐字节一致 + 绑定自检 + 运行级 + 组件对拍 29/29。

## 六、移植纪律入册（plans/020 第三节三-B，用户要求）

本轮把「为什么会出现这些问题、怎么防」的调查结论落为**强制自检清单**（改任一侧所有权/释放机制时逐条执行）：

1. **移植清单两件套（第 7 条）**：移植机制先写「机制主体 + 依赖不变量」两行，不变量逐条验证落地，禁止凭「看起来像冗余」跳过——73-a 首版 SIGSEGV 的直接原因即读宿主实现时把「释放后槽清零」当防御性冗余而只搬主体。
2. **释放路径两侧对照（第 8 条）**：新增脚本 `scripts/cn_release_parity.sh`（配套落地件）——拉出两侧释放挂点清单（宿主 6 个 vs v2 5 个）并逐条核对「时机 / 条件 / 不变量」三列，末尾含「释放+清零」两测非零计数报警。脚本明示「清单是核对入口，不是结论」——语义等价性须人工判读。
3. **同结构双编译器对照（第 9 条）**：同一最小程序两侧各编一次，比对生成物层释放调用与运行级 `内存::活动分配数()` 差值（而非仅比程序输出）——发现重实现分叉的唯一系统性手段；连带原则：防线不对称=缺陷候选、文档声明须带验证状态。
4. **两侧同缺一次修全（第 10 条）**：lessons 第七十二轮预防⑥已载仍复发，故落为当轮动作（改了任一侧释放机制，当场核对另一侧同族缺口）。

同条纪律已同步 plans/020 文件头、lessons 第七十三轮段、HANDOFF 下轮任务。

## 七、诚实边界

- 结构体容器字段「声明即用」（`货架 s; s.表.追加(...)`）报「空指针解引用」——**既有边界**（三方对照实证：修复前 v2p / 修复后 v2p / 宿主同形态**均失败**，字段未自动构造），非本轮引入，靶子归属「结构体字段初始化 / NLL 轮」。
- 装箱字段/数组元素/静态面无 RAII（plans/020 矩阵靶子维持）；v2 侧「返回接收形态」不登记释放（原预扫豁免语义原样搬移，行为不变）。
- 194 待批维持；win-x64 v2 并行未隔离（家机轮）。
