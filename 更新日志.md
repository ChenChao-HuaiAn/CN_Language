# CN语言编译器 更新日志

## 功能完善总结

## 编译期安全专项启动：plans/019 立项 + 阶段 1「显式转移 转移() + 已转移使用检查」双侧全链落地（2026-09-10 第五十七轮 深度机 linux-x86_64）

用户裁决「按最优解（方案 B 值语义强化+渐进检查器路线）立项并全部实施」。本轮完成路线文档与阶段 1 全链闭环，v2self 固定点复锚 242022 行新口径。

### 一、路线文档（plans/019-编译期安全分阶段实施路径.md）

- 总体架构：安全区分层模型（安全区默认深拷贝零规则 / 受检区转移+逃逸+借用 / 不安全区显式隔离）。
- 五阶段路径：①显式转移（本轮✅）→ ②引用逃逸强化 → ③常量 T& 只读借用+互斥（含浅转移优化）→ ④不安全区边界（不安全 函数 修饰；指针算术/联合体/FFI/裸释放收口）→ ⑤并发安全；4'智能指针与拥有型字符串另案呈报。
- 三语法点裁决记录：转移() 内置函数（std::move 同构，零新关键字）/ 常量 T& 只读借用 / 不安全 函数级修饰。

### 二、规范同步（新增语法规范，经用户批准）

- plans/001 §5.3.1 新增「显式转移 转移()」：实参须变量标识符；资源语义类型白名单（标量拒绝=复制语义）；源变量自转移点任何使用=编译错误（E0382 对标，诊断含转移点定位）；遮蔽语义；第一版运行行为零变更。
- cn-language-spec specs/04 第五节内置构造器扩 转移()（EBNF+规则+身份说明四构造器并列）。

### 三、宿主实施（src/cn_compiler，六文件）

- semantic.hpp/cpp：scopeMoved_（名→转移行号，与 scopes_ 平行）+ markMovedVar/lookupMoved（与 lookupVar 同序=遮蔽正确）+ reportMovedUse（「已转移，不能继续使用（转移发生在 行N）」）+ isTransferCall/transferArgKind。
- semantic_call.cpp：visitCallExpr 表达式位特判——指针/字符串放行（resolvedType 传 IR）+ 标量拒（复制语义）+ 拥有类型拒（仅声明位）+ 再转移拒。
- semantic_decl.cpp：visitVarDecl 声明位先行拦截——引用+转移拒/白名单/标记/AST 改写（initializer=实参标识符，深拷贝既有路径零 IR 改动）+ 豁免窗口 inTransferRewrite_（改写产物常规检查不误报）+ 收尾标记。
- semantic_expr.cpp：visitIdentifierExpr 使用根拦截（读值/实参/返回/操作数全覆盖）+ visitAssignmentExpr 左值拒绝。
- error_analysis.cpp：转移 内置占位注册（防重定义，四构造器独占惯例）。
- ir_call.cpp：转移展开特判（lastExpr_=实参求值，零运行时指令）。

### 四、v2 实施（CN语言编译器v2，四文件）

- 语义分析.cn：符号 结构体第 5 字段 已转移（表注册显式清零）+ 表标记转移/表查已转移（同序）+ 转移实参分级（语义指针变量表先行排除指针基名误判）。
- 语义检查表达式.cn：检查调用表达式 转移特判（放行+标记+返回实参类型；线性顺序天然不误报首次转移）+ 检查标识符表达式 moved 检查 + 转移 被调占位放行。
- 语义检查.cn：赋值左值 moved 检查。
- IR/生成表达式求值.cn：生成调用 转移展开（类型大小折叠同位，弹2压回实参值）。

### 五、测试锚定

- E2E 196_显式转移（宿主正测：容器声明位深拷/遮蔽/字符串/指针四形态，stdout 2/1/左/55）+ 197_转移后使用拒绝（宿主负测：读/写/传参/返回/标量五形态）+ 196_v2（v2 正路径 rc=7 校验和）+ 197_v2（v2 负路径 5 诊断+中止行实测固化）四用例注册双通道。
- 单测 test_move_check.cpp 13/13（负九正四）。

### 六、验证矩阵

- 构建零警告（GCC）+ 单测 1264/1264（1251+13）+ E2E 209 用例（205+4；唯一败=194 既有待批，跳 4=平台限制）。
- v2self 锚定链：v2p 编译 v2 全树 rc=0 → fix_p 242022 行（239301+2721=阶段 1 源码确定性体现）→ as+g++ 链接 cn_self57 → 绑定自检 cn_main=0x4dca4a∈cn_self57.o .text → cn_self57 编译 v2 自身 fix_s 242022 行 → 固定点逐字节一致 + 组件对拍 29/29 + 运行级 3/3（hello 你好，世界/119 rc=14/173 rc=173）。

### 七、诚实边界与立案呈报

- 转移支持面：指针/字符串全位置；容器/类仅声明初始化位（宿主深拷贝语义；v2 容器位全拒随阶段3 对齐）；标量拒绝。浅转移零拷贝=阶段3。
- 【既有缺陷立案呈报①】宿主推断声明+字符串标识符拷贝打印错分派（变量 乙 = 甲 → 打出指针值；显式类型正常；46-c 标量回填涉 184 行为契约须裁决）。
- 【既有缺陷立案呈报②】v2 语法层不支持 -> 整64* 指针返回类型（宿主支持；197_v2 返回形态经强转适配）。
- 探针与脚本留 target/p57x64l/ 不入库。
