# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十三轮 123-a：D1 行数整改第十四波（v2 侧）——`IR生成.cn` 1172→731 行**（2026-09-14 凌晨，家机 win-x64；基线 3f66cda）：
> ①**拆分**（纯重构零行为变更）：`IR生成.cn` **1172→731 行**，拆出 `IR生成类型.cn`（**461 行**：类型解析/推导族**两段**——
> 段 1 解析调用返回结构体/解析成员链结构体ID/推导下标元素类型ID/推导成员链类型ID/推导下标数组长度/取成员宽度码；
> 段 2 实参类型码们/转换符号/解析标识符类型ID/解析节点类型ID/推导实参类型文本/实参段匹配/决议函数符号/
> 解析方法对象类型/解析方法对象类型扩展）+ `IR/包.cn` 注册；**11 静态变量矩阵勘定**（24 个零静态函数分 5 个连续块；
> 静态使用者〔retbuf 字节数族/寄存器类型族/函数签名表族/引用参数槽族/串污染族——31 函数〕全部留主文件）。
> ②验证：v2p 重建零错误 + **v2 产物 4 样本逐字节一致** + **锚定链固定点 406097 行不变** + **对拍 41/41**（组件 40→**41**）+
> 单测 1313/1313 + E2E 290/292 + 运行级 3/3。
> ③v2 侧进度：**五文件达标**（生成表达式/生成语句/代码生成/语义检查/IR生成）；余 **1 文件**（IR容器 1918〔静态耦合——
> 需「工具函数下沉」先行〕）+ 宿主 `ast.hpp` 1032 + 函数级 19 超百行。
>
> **⚠️ 待用户裁决（最高优先级 actionable，已六度呈报）**：`plans/022` §九 **8 项**（含第 8 项「波 1 修 `字符*` 拼接误报——可立即开工」）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
