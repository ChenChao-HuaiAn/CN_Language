# 功能完善总结

## plans/017 呈报①方案A 第四轮：v2self 第 7 层根治（第 14/15 枚）+首次完整运行里程碑+第 16 枚立案（2026-09-06 第二十二轮 深度系统 x86_64）

### 一、缺陷根治两枚（用户指令准绳：参考 Rust 经验，性能第一/安全第二）

1. **第 14 枚【根因级】元素() 返回含容器字段结构体→标量判定与宿主 sret 分叉**
   （`CN语言编译器v2/IR容器.cn` 查方法签名）：
   - 旧判定 `查结构体大小!=0 && 容器字段数==0` 才走 sret——「含容器字段的结构体→标量」
     是 E2E 127 时代「供给.cn 类形态供给」专属契约（主.cn 结构体声明×供给类实现=同名双 ABI）。
   - 宿主真语义三方 asm 实证：调用方 `rdi=retbuf/rsi=this/rdx=位置` 三装载 sret、被调方
     同款、127 直编运行 exit=0；v2 旧判定发射 `rdi=this/rsi=实参`（无 retbuf 位）→
     this/实参整体错位（gdb 实证 元素 方法入口 this=0/位置=0xffffffff→运行时错误3，
     =v2self 第 7 层崩点 `程序.函数们.元素(f)`）。
   - 修复=判定改 `查结构体大小(返回ID)!=0`（T=结构体一律 sret；容器类不在结构体大小表
     →自动仍标量）。Rust 准绳：类型即布局、布局即 ABI（同名类型禁止双 ABI）；大对象按值
     返回=sret 与 Rust/LLVM codegen 一致，零额外开销。
   - 配套：`tests/e2e/127_*/供给.cn` 词条 类→结构体（与主.cn 同种类同布局；w.表 显式
     构造初始化；cn check 绿+独立构建运行 exit=0）——消灭同名双 ABI。
2. **第 15 枚 含容器字段结构体「调用返回初值」被 RAII 级联析构**（`CN语言编译器v2/IR生成.cn`）：
   - `函数IR 扫函数 = 程序.函数们.元素(f);`（sret 浅拷贝自调用方容器元素）被
     容器字段局部们 无条件登记→出口级联析构=free 调用方容器数据→后续迭代 invalid free
     SIGABRT（收集字符串常量 崩点回溯实证）。
   - 修复=登记条件加「初值末节点=调用且非构造（结构体无构造调用）→不登记」——与第 9 枚
     「容器变量调用返回初值不入 RAII」同款豁免（宿主语义=接收不析构，泄漏对齐现状）。

### 二、里程碑：v2self 首次完整运行

v2 编译的 v2 编译器（184139 行 asm）**编译运行 min1.cn 全链 exit=0**（IR 生成→打印
函数IR→代码生成→asm 写出），**产物与 v2p（宿主编译版）逐字节一致**（asm diff 为空）。

### 三、第 16 枚立案（未收口，下轮续）

容器系探针（f2/f3/alias/vieww/retown/elecalc/freg/mapref）v2self 内崩于 IR 生成
（v2p 编译同源全绿=九探针对拍一致，v2self 独有）：查方法签名 帧实参们槽高 32 位被
运行时小整数污染（0xa/0xb/0xd，低 48 位=上次调用的有效堆指针）；写入点每轮 gdb 实测
干净+帧布局审计全正常+装载链无中间写——写者未定位（嫌疑 v2 编译版被调链深处栈写越界；
下轮 v2 源码分步插桩续挖）。

### 四、灰色点披露

- IR容器.cn 单组件子链冒烟 exit=1（节点_调用 未声明）——既有状态非本轮回归（git stash
  实证 HEAD 同红；上轮第 9 枚豁免代码引用 语法分析.cn 常量，主链合并可见/子链不可见；
  上轮「13 组件全绿」口径存疑）；主链编译（主.cn 全链）exit=0 全绿。
- mapref 探针目录副本修正导入（容器→映射集合，映射在映射集合模块——上轮已判明的源
  程序导入错误）；修正后宿主可编、引用和=99 对拍一致。

### 五、验证（门禁全绿）

- 构建零警告（GCC -Wall -Wextra -Werror）；
- 单测 **1240/1240**；
- E2E linux-x86_64 全量 **157 用例 130 过/0 败/27 平台跳过**（与上轮基线一致；
  127 供给修订的 win/arm64 侧闭环复验为跨机项）；
- v2p 主链冒烟全绿；**九探针**（min1/f2/f3/alias/vieww/retown/elecalc/freg/mapref）
  v2 编译→as→g++→运行与宿主直编**对拍一致全 PASS**；
- v2self min1 全链 exit=0+产物逐字节一致；127 供给修订 cn check 绿+独立运行 exit=0。
