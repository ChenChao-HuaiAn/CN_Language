# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（581-a 开发轮：三机协同协议 v3=异步验收制落地·2026-09-21）

- **协议 v3（用户两裁决：①彻底废除 try-build 前置回签 ②红灯不冻结广播后继续集成）**：
  跨平台正确性从「合并前三平台同步回签」改为「集成后异步验收+修复义务」（Linux 主线模式本土化）；
- **AGENTS.md §8**：标题升 v3+总纲六条→七条（新增「异步验收·零等待=任何任务不因等待他机而阻塞
  收工」与「develop 尽绿+fix-forward」红灯 48h revert SLA）+§8.2 删 try-build 拦截（步骤 7 改
  验收请求）+§8.3 整节重写（开发轮零等待/补验收=每机开工第一动作/红灯治理/跨平台静态补偿
  check_asm_width+check_win_asm 常驻）+§8.5 窄通道增验收基线登记；
- **scripts/integrate.py**：删 try-build 触发面与拦截（写集分类仅区分快速/全量门禁）；
  --try-build-done/--win-verified 废弃兼容（接受即忽略+打印 v3 说明）；
- **scripts/check_handoff.py**：看板 🧵 验收基线行校验（每机 ≤1·commit/日期格式·缺失容忍）；
- **三机任务看板.md**：家机小节增 🧵 验收基线行（初始 c9c847b5）；
- **plans/027**：头部 v3 状态注记+§八 v3 裁决记录（质询原文/侦查要点/业界对照/两裁决/风险明码化/
  存量挂等链处置）；
- **顺带机制级修复**：575-a rebase 把 580-a 对 AGENTS.md 的三处修改（§4 别名体系/§6.8 P0 纪律/
  §8.5 📣 态铁律）覆盖回旧版——git diff 3f79c218 实证后恢复（纯回退·无 575-a 正向改动丢失）；
- **plans/021**：046 立行+锚行 046+文首戳 581；
- 门禁=快速门禁面（纯文档+py 写集·协议 v3 下零 try-build 首例）。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 535-a 解析类声明设计方案轮〔零代码〕·2026-09-21）

- **方案定稿**：解析类声明 185 行五子函数划分（继承跳过/可见性标签/方法成员/字段成员/布局收口）+状态传递设计（位置值返回/布局三元组引用参数/保底推进守卫留主函数）+等价验证策略（asm md5 双对拍+布局累加变异反证）。
- 方案文档落盘 plans/025 §2.115·登记待实施（下一会话首选拾取）。
- 零代码轮（快速门禁）。


## 单位机 ARM64 节

## 功能完善总结

### 560 开发轮：F2-36b 首步=bench v2 口径三平台改造+arm64 自举性能基线入库（2026-09-21，基线 develop 0e3ec08f）

1. **范围修正（开工侦查实锤）**：原 bench_self_host.py 测量对象=v1 组件链（CN语言编译器/ 五组件·已推倒）——对象整体过时；改造为 **v2 自举树口径**（CN语言编译器v2 全树·fix_p≡fix_s 固定点·对齐 78_v2/79_v2 锚定链编排）+`--platform` 三平台工具链分派（win=ml64/link 原路径保留·linux=as/g++ -no-pie〔CN_AS/CN_CXX 环境变量+PATH 探测·对齐 runner〕）。
2. **arm64 基线实测入库**（编译器=组合态 558+559 修复态构建）：M1=22295.7ms（C++ 宿主 Debug build v2 全树）/M2=42608.4ms（CN 版编译同树 742237 行·3 次中位）/**比值 1.91x vs C++ Debug**/M3=2194.8ms/M4=146.4ms+绑定自检 cn_main∈cn_self.obj ✓/**M5=32309.0ms·M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建的同一编译器快 24%**（重大正面信号）/M6 ✅ fix_p≡fix_s 逐字节（自举固定点成立）。
3. **实施修两处**：M4 借链对象（初版错用 fix_p 自汇编→undefined 词法$Token 追加符号·grep 实锤 fix_p 缺跨模块符号定义→改 cn build 中间产物 v2p.o〔run_e2e「容器符号提供者」原义〕）；基线测量编译器依赖序（主树无 558 修复→改用组合态构建·口径注明）。
4. **口径诚实边界**：C++ 宿主=Debug 构建（-O0）——1.91x 非 vs Release 口径；单机基线（跨机比较无意义）；win 分支静态核验（跨机复验归家机）；深度机 x64l 基线可复用同脚本（三机通用）。
