## 功能完善总结

### 阶段6「标准库」第3步：映射集合库 + 算法库（Task 6.4 + Task 6.7，2026-08-15）

**新增标准库模块：**

1. **`stdlib/映射集合.cn`**（Task 6.4 集合库，对标 C++ map/set）：
   - `映射<K,V>`：K* 键数组 + V* 值数组 平行线性表（O(n)，P0 不做哈希）；`设置`（键存在→覆盖值/不存在→追加扩容翻倍 0→4→8）/ `获取`（找不到→错误码.未初始化=7）/ `包含`（规格书 4.4「表.包含(键)」）/ `删除`（存在则删除前移返回真）/ `清空`/`大小`/`为空`/RAII 析构；键比较须支持 `==`（P0 数值/字符/枚举键，字符串键 P1）
   - `集合<T>`：独立 T* 数据数组（值唯一，添加幂等），不组合 `映射<T,布尔>` 字段（规避嵌套泛型缺陷）

2. **`stdlib/算法.cn`**（Task 6.7 算法库，对标 C++ `<algorithm>`）：
   - `排序整32`/`排序整64`：冒泡升序（比较内联）
   - 泛型 `线性查找<T>`（找到→索引，找不到→-1）；`二分查找整32`/`二分查找整64`（前提：数组已升序）
   - 泛型 `逆序<T>`（原地反转）、`变换整32`/`变换整64`（原地 ×2）、泛型 `计数<T>`（出现次数）、泛型 `最大元素<T>`

**新增 E2E：** `tests/e2e/31_map_set/`（映射 设置/包含/获取/覆盖/删除/清空 + 集合 幂等添加/删除，17 项断言）+ `tests/e2e/32_algorithm/`（排序/线性查找/二分查找/逆序/变换/计数/最大元素 + 整64 版本，15 项断言），均 PASS。

**技术风险点实测结论（泛型 + 函数指针回调）：**
- `泛型 <类型 T> 函数 排序(T* 数组, 整64 长度, 整32(*比较)(T, T))` **编译失败**——单态化后签名不匹配（`未找到匹配的函数 '排序$整32'`）；泛型类方法带函数指针参数形参类型解析为空。**P0 降级**：排序/二分用具体类型（整32/整64）重载 + 比较内联；泛型自由函数（无函数指针）全部正常。留待 Debug 子任务修复泛型函数参数中的函数指针类型单态化替换。

**编译器缺陷规避（本轮实测）：**
- 泛型类方法体内「循环遍历第一个泛型字段」运行时损坏（N/P/Y/AC/AG 实验复现垃圾值）——规避：循环查找逻辑放模块级泛型自由函数（参数数组实测正常），类方法委托调用
- 「打印变参内嵌泛型类字段下标」损坏——E2E 先存局部变量再打印

**验证结果：** 单测 987/987、E2E 34/34 全通过、编译 0 警告（MSVC /W4 /WX）。
