## 功能完善总结

### 本次提交（2026-08-14）：阶段6「标准库」第2步——核心库（Task 6.1：核心.cn + 容器.cn）

**一、核心库（stdlib/核心.cn）**

- 新增 `错误码` 枚举：除零=1/越界=2/空指针=3/内存=4/文件=5/参数=6/未初始化=7/溢出=8（值域与规格书附录B一致）
- 新增泛型函数：`交换(T* 左, T* 右)`（用 `左[0]` 下标写法规避行首 `*` 歧义）/ `最小(T 左, T 右)` / `最大(T 左, T 右)`（编译期单态化）
- 新增 `断言(布尔 条件, 字符串 消息)`：假条件调用 `运行时错误(错误码.参数)` 终止进程
- E2E：`tests/e2e/29_core/`（主.cn + 核心.cn + 主.expected）

**二、容器库（stdlib/容器.cn）**

- `向量<T>`：T* 数据 + 整64 元素数量 + 整64 数组容量；扩容翻倍（0→4→8）；RAII 析构释放底层数组；追加/插入/删除/读取/元素/设置/清空/预留/大小/容量/为空
- `链表<T>`：索引链表（T* 值表 + 整64* 下一索引，-1 表链尾）；追加/头部追加/删除头部/删除尾部/读取头部/读取尾部/清空/大小/为空
- `栈<T>`：独立 T* 动态数组（压入/弹出/栈顶/为空/大小），规避嵌套泛型组合缺陷
- `队列<T>`：独立索引链表（入队/出队/队首/清空/大小/为空），规避嵌套泛型组合缺陷
- E2E：`tests/e2e/30_container/`（主.cn + 容器.cn + 主.expected）

**三、编译器缺陷修复（本轮）**

1. **结构体返回调用赋值缺陷（关键）**：`ir.cpp` 赋值路径漏 CallExpr 右值——`读取结果 = 数据.读取(99)` 只存 8 字节 retbuf 地址而非 CopyStruct 12 字节内容，导致 `.正常`/`.值` 读到地址值、越界判断失效（链表/栈/队列全部错乱）。修复：结构体返回调用右值走 CopyStruct 按值拷贝
2. `x64_instructions.cpp` Store 路径物理寄存器 32 位宽度（`mov eax, r12` → `mov eax, r12d`，A2022 汇编错误）
3. `x64_instructions.cpp` emitCast 物理寄存器源 32 位名转换（i64→i32 截断路径）

**四、编译器缺陷规避（记录，留待 Debug 子任务修复）**

- **构造函数重载缺陷**：语义层 `ClassInfo.methods` 按方法名（name）索引，同名构造函数（无参/带参）互相覆盖，带参构造覆盖无参构造导致无参调用链接到带参体（读垃圾 rdx 当参数）。容器库暂不用构造重载（预分配由 `预留()` 方法提供）。修复方向：methods 表改按 sigKey 索引或构造函数专用多版本存储

**五、验证结果**

- 单元测试：**987/987 通过**（含语义/泛型/错误处理 226 项重点回归）
- E2E：**32/32 通过**（原 30 + 新增 29_core、30_container）
- 编译：0 错误 0 警告（MSVC /W4 /WX）

**六、文件清单**

| 类型 | 文件 |
|------|------|
| 新增 | `stdlib/核心.cn` |
| 新增 | `stdlib/容器.cn` |
| 新增 | `tests/e2e/29_core/主.cn` + `核心.cn` + `主.expected` |
| 新增 | `tests/e2e/30_container/主.cn` + `容器.cn` + `主.expected` |
| 修改 | `src/cn_compiler/ir/ir.cpp`（结构体返回调用赋值 CopyStruct 修复） |
| 修改 | `src/cn_compiler/codegen/x64/x64_instructions.cpp`（Store/emitCast 物理寄存器 32 位宽度） |
| 修改 | `plans/002-阶段6-标准库.md`（Task 6.1 打勾 + 实施记录） |
