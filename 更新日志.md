# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（718-a·运行时错误退出路径修复+崩溃处理器设施·2026-09-24·任务/家机-718-WER抓dump定点修复·wt698）

**行为面（src/runtime/io_api.cpp·全平台编译器进程受益）**：①**__cn_runtime_error 终止路径修复**：std::exit(1)→ExitProcess(1)+fflush——堆损坏场景 std::exit 死锁挂住根除（717「错误码 4 后挂住」实测根因）；②**cn_crash_auto_install 崩溃处理器设施**：UEF 抓 C0000005 等向 stderr 直写 code/RIP/RSP/fault（WriteFile 不经 stdio 锁）·自动安装。**侦查三线收束**：WER LocalDumps 权限受阻落账/DEBUG 堆干扰已证/UEF 静态初始化在 cn_self 不执行（入口 shim 不跑 _initterm=719 切入点）；另实锤 v2 树「外部 函数」语义登记缺口（592-a 只做语法接受·归 002 收口域登记）。下一棒=719 cn_main shim 显式调用 UEF→抓真 RIP→定点修复→t4/fix_s 判据解锁→001 销项。

## 深度机 linux-x86_64 节

## 功能完善总结（720-a＋722-a＋723-a 开发轮·2026-09-25 凌晨·任务/深度机-720-性能基线＋722-linux链接修复＋723-integrate红解析·wt720）

**036 linux-x64 自举性能基线入库（M2/M1=2.09x）＋713-a 平台覆盖缺口补修（机制级当场修）＋719-a linux 构建红 fix-forward＋integrate 点名安全网修复＋042 销项**：

1. **bench 全链实测（口径与 560 arm64 逐项对齐）**：M1=19490.5ms（宿主 cn build v2 全树）／M2=40728.6ms（3 次中位·样本 40615.033/40728.56/40760.931·产物 636179 行）／M3=900.6ms／M4=204.0ms（链接·绑定自检 cn_main∈cn_self.obj ✓）——**M2/M1=2.09x**（对照 arm64 560-a 1.91x·同口径量级可比）；**M5/M6 未达成**=cn_self 自身编译 v2 全树失败（[sem5] 语义错误 67223 个·家机 708-c/d/e 链域既有缺陷）；降级报告 target/bench/性能基线报告.md（脚本增强：M5 失败降级出报告保 M1~M4＋落错取输出尾部）。
2. **机制级缺陷当场修（713-a 平台覆盖缺口）**：M4 链接失败（undefined `向量$映射$整64$符号$追加_A_T…` vs 定义 `__T…`）——独立定位=713-a 第③项「符号修饰参数段剥 &」只改 MASM 路径（符号修饰X64L/符号修饰ARM64 GAS 后端未覆盖）+IR 层真根因（取实例实参们 尾 & 泄漏：引用接收者类型文本尾 & 被 解析实参段 尾缀并入吃进末段实参→类型实参替换后参数串带 &→符号多编码一个 A）；修复=IR 层精确剥尾 &（IR容器.cn·实例身份不含引用修饰·勿在符号修饰层剥——真引用参数段 A 编码须保留）；**效果=79_v2 -4 链接失败→链接全通·进入 -6 fix_s 第二跳**（与 win 形态同构）。
3. **722-a develop 红 fix-forward（平台分叉）**：集成 720 后 linux 单测链接失败的 719-a 缺陷——runtime.cpp entry() 无条件调用 win-only `cn_install_crash_handler`/`cn_install_veh`（定义在 io_api.cpp `#ifdef _WIN32` 段内）；修复=`#ifdef _WIN32` 守卫收口（调用点全库排查仅 2 处）；验证=零警告构建+单测 1405/1405+E2E 528=523/3/2 零新增；win 侧复验请家机。
4. **回签家机 713-a 异步验收**（看板通告段·直推 develop 53a95618）：含平台覆盖缺口+IR 层根因+宿主 linux 三方对齐实证（语义分析$表压入〔引用形参〕=带 A／向量$映射$整64$符号$追加〔按值形参〕=无 A）。**042 销项**（511-a 后 512/513-a 已完成 T80 双端闭环·517 链集成 7e0ff3c2·586-a 复验零回归→§4.2 台账）；棘轮滚转（025 §2.114~2.130 八节入归档）。
5. 门禁全绿：零警告构建+单测 1405/1405+spec（strict）100% 指针有效+零孤儿+matrix 83/83+E2E 全量 528=523/3/2（3 红=已知基线红零新增）；教训 2 条入册（bench 平台分派回归／平台分叉·症状层 vs 源头层·权重 8）。
6. **723-a integrate.py `--allow-known-red` 安全网失效修复（门禁工具·机制级）**：`运行()` 默认继承 stdout ⇒ `结果.stdout=None` ⇒ 点名清单恒空 ⇒ 未点名红亦放行（722 集成实测「串行红 [] 全部命中」=空集恒真签名·机制存活约 20 轮期间点名环节实为无条件放行）；修复=新增 `运行捕获()`（capture+回放）＋三调用点换接；双验证=旧路径 None 复现／新路径解析命中；教训 1 条入册（权重 8）。

## 单位机 ARM64 节

## 功能完善总结

### 服务轮：698-a/702-a/703-a 三轮批量 arm64 异步验收+M3 采样补跑（2026-09-24·基线 develop 5ed202f7→2a96c036）

1. **批量验收**：清场重建＋单测 1405/1405＋E2E 全量 **528=523/3/2 净增绿**（基线 4 红→3 红·497 转绿=698-a 装配修复 arm64 实证）。
2. **红面形态对表全一致**：113 段错误→值差（待 052 裁决）；79_v2 段错误→fix_s 编译失败（703-a 定性吻合·025 §1.11 专项）；127_v2 段错误→退出码 206（红维持·如实记录）；524 arm64 绿维持；218 PASS（702-a 复绿同侧成立）；容器析构面 12 例回归全绿·零特有红。
3. **M3 采样补跑**（626 波2 后首次·两日空窗补记）：零新缺陷类别——m27 系 T27/T96 已知族复现（m27_03 与 09-21 meta 逐值一致）+存量 m38/m42/t71；日文件 2026-09-24.md 已记。
4. **看板**：基线行登记 2a96c036；通告段写三轮批量验收广播、删 596+601 旧通告。

