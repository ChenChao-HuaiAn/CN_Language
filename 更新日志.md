## 功能完善总结

**时间**: 2026-08-18（性能基线对比执行完成，plans/004 D-1 预留项闭环）

## 性能基线对比（plans/004 D-1）✅

自举真实闭环达成后，执行 D-1 性能基线对比：自举后跑 CN 版编译器对比 C++ 版，
验证「性能达 C -O0」里程碑。

### 一、新增性能测量脚本 `scripts/bench_self_host.py`

基于 run_e2e.py 的 79 闭环编排，聚焦性能测量，6 个测量点：

| 测量点 | 说明 |
|--------|------|
| M1 | C++版完整 build（cn.exe build 79主.cn，含编译+链接） |
| M2 | C++版组件链落盘（运行 79 exe 第一次落盘 5 个 *_链.asm，3 次取中位数） |
| M3 | ml64 汇编（5 个 .asm -> 5 个 .obj） |
| M4 | link 链接（-> cn_compiler_self.exe） |
| M5 | CN版组件链落盘（运行 cn_compiler_self.exe 第二次落盘，3 次取中位数） |
| M6 | 正确性验证（两次落盘产物逐字节一致，保证性能对比有意义） |

- 计时用 `time.perf_counter()` 精确计时（毫秒，保留 3 位小数）
- M2/M5 各测 3 次取中位数（消除系统负载抖动）
- 复用 run_e2e.py 的 ML64/LINK 动态探测逻辑（14.38/14.44 + Win Kits 最新版）
- 找不到 ml64/link / 编译失败 / 链接失败时给出清晰错误并退出非零码
- 产物统一放 target/bench/（规则 19）

### 二、实测数据（Release 版 cn.exe，MSVC 14.44.35207）

输入规模：5 组件源码 3604 行（词法 477 / 语法 965 / 语义 194 / IR 997 / 代码生成 971）

| 测量点 | 耗时(ms) |
|--------|---------:|
| M1 C++版完整 build | 2291.635 |
| M2 C++版组件链落盘（中位数） | 697.714 |
| M3 ml64 汇编 | 51.719 |
| M4 link 链接 | 45.086 |
| M5 CN版组件链落盘（中位数） | 727.932 |
| M6 正确性验证 | ✅ 逐字节一致 |

**核心结论**：CN版/C++版 组件链编译比值 ≈ **1.04x**（几乎 1:1）。
CN 版推演 6 万行 ≈ **12s**，远优于 004 可接受边界（≤100min 硬上限 / 10min 理想目标）。
**✅ 达到「性能达 C -O0」里程碑（优于 10min 理想目标）**。

### 三、与 004 已有数据对比

| 数据来源 | 输入规模 | 耗时 |
|----------|---------|-----:|
| 004 2.5 节：C++版纯编译 30 行无依赖 | 30 行 | 39 ms |
| 004 2.5 节：C++版含 映射集合 模块依赖 | 30 行 | 330 ms |
| 本轮实测：C++版组件链编译 5 组件 | 3604 行 | 697.714 ms |
| 本轮实测：CN版组件链编译 5 组件 | 3604 行 | 727.932 ms |

### 四、环境准备（gtest 缺失修复）

- 本机 CMake 配置因缺少 Google Test 源码失败（`Documents/third-party/unittest/googletest` 不存在）
- 从 GitHub codeload 下载 googletest v1.14.0 源码，解压到
  `C:\Users\ChenChao\Documents\third-party\unittest\googletest`（CMakeLists.txt 期望路径）
- 调整目录结构（v1.14.0 嵌套一层 googletest/，需将 src/include 上移）
- Debug + Release 双配置构建成功，cn.exe 与运行时 .obj 就绪

### 五、验证

- `python scripts/bench_self_host.py --cn target/Release/cn.exe` 运行成功，报告生成
- `target/bench/性能基线报告.md` 产物齐全
- E2E 79 仍通过（`python tests/e2e/run_e2e.py --cn target/Release/cn.exe --filter 79`）

### 六、文档同步

- `plans/004-自举前置条件检查.md`：D-1 标注 ✅ + 新增「执行情况四：性能基线对比」小节
- `HANDOFF.md`：记录本轮性能基线对比完成情况
- `lessons.md`：记录 gtest 缺失踩坑

### 七、Git 提交与推送

- 中文提交（标题+描述），推送 gitcode develop
