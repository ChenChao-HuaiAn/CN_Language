# 功能完善总结

> 78-a v2 用户自定义类支持全链（2026-09-12 第七十八轮，深度机 linux-x86_64）：77-a 扩展③
> 遗留「v2 类支持专项」销项——用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust
> 经验给最适合方案（性能第一、安全第二）」。探针 D 实证 v2 对用户自定义类整体不支持
> （语法层不解析 `类` 声明）；三方案裁决后实施**类支持全链**（v2 侧新增 4 文件 + 改 9 文件，
> 宿主零改动；对象模型与容器同款=IR/代码生成零新指令）。

## 一、缺口实证（探针 D 复现）

v2p 编译最小类程序报 3 错：`未声明的类型: 盒子` / `赋值目标未声明: 值` / `未声明标识符: 值`
——根因=v2 的 AST 节点表只有 节点_结构体、**无 节点_类**（顶层循环静默跳过类声明），
语义/IR 层全缺。对照：同结构体程序 v2 全链通过。

## 二、方案裁决（三方案；性能第一、安全第二，Rust 对象模型经验）

| 方案 | 性能 | 安全 | 裁决 |
|---|---|---|---|
| **甲：v2 类支持全链**（类声明→类型登记→字段表→方法体检查→IR→代码生成；对象=堆对象指针，与容器同模型） | **最优**：IR/代码生成零新指令零新后端面；编译期 O(节点数) | 高：编译期检查（字段解析/借出检查挂载/不支持面显式拒绝）；RAII 与宿主同口径 | ✅ 实施 |
| 乙：结构体 + 命名约定模拟类 | 中（编译器级改写层新分叉面） | 低（方法体字段/自身语义需模拟） | ✗ |
| 丙：只做语义检查面 | 高（工作量小） | 低：类程序仍编译不通过（无实际可用面） | ✗ 违反全链纪律 |

**范围（第一版）**：支持 类声明（字段/方法/构造/析构/自身）+ 堆对象指针语义 + 方法体字段
裸名读写 + 借出检查挂载 + RAII（有析构类，含 ~类 体末字段级联析构）。
不支持面**显式诊断不静默**：继承/虚函数/接口/泛型类/静态成员/常量成员/运算符重载/访问控制
（语法层诊断+中止）；类对象拷贝构造/方法返回结构体/结构体字段整体赋值（IR 层诊断）。

## 三、实施（v2 侧；宿主零改动）

**新增 4 文件**：
- `IR/IR类.cn`——类元数据表（类名表/方法签名表/构造参数串/析构位；语法层登记、语义/IR 查询）
- `语法/解析类.cn`——类声明解析（字段布局登记**完全复用结构体设施**：登记结构体布局/字段类型/
  字段偏移/容器字段；方法/构造/析构；不支持修饰诊断+恢复跳过）
- `语义/语义检查类.cn`——方法体检查（第三类函数级作用域单元：参数+字段裸名登记+「自身」+
  借出视图生命周期检查挂载）
- `IR/生成类.cn`——生成方法（this 槽=参数位0+字段读写+自身）、类对象局部（分配+构造+RAII 登记）、
  构造/方法调用发射、**析构字段级联注入**（宿主 injectFieldCascadeDestroy 同款：~类 体末按字段
  逆序对有析构类型字段发射 IR_删除对象）

**修改 9 文件**：IR常量（节点_类/方法/类字段 40/41/42）、三个包.cn 注册、语法分析（顶层类分支+
可见性）、语义分析（声明收集类分支+收集类方法）、语义检查（参数登记提取 `登记函数参数们`+
返回类型访问器+第二趟类分支）、借出检查（类字段类型回退+借出检查类名ID 上下文）、IR 生成族
四处分派（标识符字段读/自身、成员基址**指针语义修复**、构造与方法调用、字段左值赋值、类遍历）、
主.cn（清空类表）。

**两侧机制对照**（移植纪律 7）：对象生命周期 `NewObject+call 类$类名[#参数串]+DeleteObject(有析构)`
↔ `IR_分配对象+IR_调用参数化+IR_删除对象`（代码生成同展开）；析构字段级联两侧同为「~类 体内、
字段逆序、空安全」；方法符号公式两侧同源（类$方法[#参数串]/构造=类$类名/析构=类$~类名）。

## 四、防线

- E2E **228_双编译对照类支持**（移植纪律 9 **第五个**对照用例）：六形态折校验和 **31**，两侧退出码一致
- E2E **229_类方法体借出视图拒绝** / **229_v2_…**（宿主/v2 同源负测，诊断锚+中止行固化）
- E2E **227_v2 期望同步**：类缺口销项后两边界均真拦截（用例价值提升；72-a 先例）
- **反证实证**：注入「是用户类 恒假」→ 228 失败；注入「借出检查方法体挂载失效」→ 229_v2 失败
  （两侧已恢复，残留=0）
- **两侧对照实测**（探针 H1~H11 各编各跑）：退出码/输出/分级分配计数全一致
- 释放路径两侧对照（脚本；v2 新增挂点 生成类.cn:184/185）

## 五、验证链（本轮新口径）

宿主零改动（单测 1306/1306 维持）+ E2E **256 用例 252 过 0 失败**/4 跳 + 锚定链固定点
**301619 行**（新口径，上轮 281093）逐字节一致 + 绑定自检 cn_main=0x111b18 + 对拍 **34/34**（DIFF=0）
+ 自举闭环（v2p 编译 v2 树 65751 节点 → cn_self71 固定点 → 自举产物再编类程序 H1=7/H4=2 实测）
+ 释放路径两侧对照。

## 六、诚实边界（第一版登记）

继承/虚函数/接口/泛型类/静态成员/运算符重载/访问控制强制（语法层显式诊断）；类对象拷贝构造/
方法返回结构体/结构体字段整体赋值（IR 层诊断）；字段级联仅覆盖容器/结果/有析构用户类字段
（字符串字段不级联=宿主同）；类对象空指针字段访问=段错误（宿主=受控错误码3）；
字段容器接收者的入容器位归一化=保守复制分支（H7/H11 两侧同残留 1，登记为靶子，非本轮引入）。
