## 功能完善总结

### 一、③ 第二波三件套：字符串下标 + 复合赋值五形态 + 方案A 登记点补全（E2E 129 六步闭环绿）

> 本轮在单位机（麒麟 Linux ARM64）按 HANDOFF 计划立项 E2E 129（字符串下标），验证期
> 按缺陷零容忍纪律探针实锤两簇缺陷并经用户裁决（方案A 两次：登记点补全 / 复合赋值
> 一次根治五形态），三项同轮落地于 129 单用例载体。

- **字符串下标（宿主 A-1 语义对齐，plans/004）**：`字符串[i]` = UTF-8 字节视图步进 1 +
  LoadPtr i8 **有符号扩展**（双后端 `movsx`/`ldrsb`，与 C 的 s[i] 一致；65 用例字节和
  -376 实证），越界由调用方按 字符串长度 约束，结果类型 字符。v2 四层：
  - **语法**：解析后缀链 `[` 分支补记 子起点=对象节点索引/子终点=下标表达式区间终点
    （原建节点全 0 字段——子区间信息丢失，语义/IR 两层无从消费）；
  - **语义**：检查表达式 下标分支——对象白名单（字符串标识符（局部/参数）/字符串成员
    字段（变量结构体表→查字段类型ID）），其余对象形态（字面量/调用/解引用/容器/结构体）
    **显式拒绝**（半实现纪律：错误放行=IR 按字符串指针生成垃圾地址）；下标左值赋值
    （s[0]=x）同样显式拒绝（宿主 A-1 只读语义）；标注成员偏移 栈平衡补 下标（弹2压1）；
  - **IR 生成**：下标分支——弹 下标+对象 两值 → `IR_加`（步进 1）→ `IR_指针加载`
    **T_字节**（新宽度码 13：1 字节符号扩展——独立于 B5 字段 T_字符 零扩展模型）；
    产物登记 T_整64（符号扩展字节值按整数消费：打印/比较/拼接 from_int）；
  - **双后端**：ARM64 `ldrsb x9,[x9]`（64 位符号扩展）/ MASM `movsx rax, byte ptr [rax]`。
- **复合赋值五形态 += -= *= /= %=（用户裁决方案A：一次根治）**：原词法层仅 += 单 token
  且解析层零分支——+= 不消费 → 解析块循环保护**静默中断** → 块内后续语句丢弃（最小探针
  `a += 2` 后 返回 0 消失、sem5 零诊断；**上轮「词法分析.cn 冒烟语义 0 错误」实为假绿——
  分词 函数在首个 += 后被截断，节点 488→556、asm 2046→2736 行实证**）。本轮：
  - **词法**：补 -= *= /= %= 四双字符 token（原以 - = 双 token 进入同样静默断路）；
  - **语法**：解析语句兜底 复合赋值分支——`L op= R ≡ L = L op R` 统一改写（**回退重析
    左值**作右值首操作数，成员偏移解析期重标注两份独立正确），合成 二元(op)+赋值 节点，
    **赋值/IR 两层零改动**；标识符/成员/解引用左值全形态覆盖；
  - 探针实证：五形态+嵌套右值 `b += a * 3`+成员左值 `名片.计数 += 2` 端到端全对。
- **方案A 登记点补全（用户裁决，缺陷①②同族根治）**：类型登记表三缺口 asm 探针实锤
  （打印(名片.标题)/打印(取名()) → `bl __cn_print_int` 打指针值；`十六进制位(..)+十六进制位(..)`
  → 裸 add 指针+指针；`"x"+取名()` → `from_int(指针)` 转数字串——代码生成共用.cn 符号
  修饰核心/主.cn 读源文件/IR签名.cn 全部在用）。修复（改动集中 IR生成.cn）：
  - **成员加载值登记**：字段声明类型（查字段类型ID→类型码）/结果 .正常→T_布尔/.值→首实参
    首段类型码（实测踩坑：合成名首实参 返回首 $ 后**全部**如"字符串$整32"，须再经 段首
    剥离——漏则 T_未知 不登记 → print_int 指针值）；
  - **用户函数返回登记**：池预扫 函数返回类型们（函数名ID→返回类型名ID），普通调用后
    登记类型码（IR生成/IR生成多文件 双入口收集）；
  - **容器方法返回登记**：签.返回文本（替换后类型）→类型码（元素(i) 返回字符串等；
    结果/结构体返回=retbuf 地址不做标量登记）；
  - **拼接左侧末端推导兜底**（与右侧对称）：普通二元节点补 子起点=左起点/附加=右起点
    标注（子终点恒 0 与短路双条件区分）；左右兜底均补用户函数返回查表。
  - 探针实证：print_str×5/concat×3/零错误 from_int，运行时 题/名/x名/名名/你好名 退出码 0；
    结果.值 链路 `打印(取名2.值)`→「甲」（根治前打指针值 5493312）。
- **符号表 类型ID 约定统一（顺带根治）**：局部变量/参数原登记类型**节点索引**、顶层静态/
  常量/函数登记类型**名ID**——两套约定消费端无从分辨（块级遮蔽检查 驻留文本(节点索引)
  取错文本静默失效）。本轮局部/参数注册时归一为类型名ID。
- **E2E 129**（`tests/e2e/129_v2_字符串下标与复合赋值与登记补全/`，注册 v2 闭环
  （主.cn, 0, 链接v2p, 供给.cn））：断言 28 项——下标 7 形态（ASCII/中文符号扩展/循环
  求和-376/负值补偿+256/二元表达式下标/参数下标/结构体字符串字段下标）+复合赋值 7 断言
  （五形态+嵌套右值+成员左值）+登记簇 8 断言（成员打印/用户调用打印/四拼接形态/容器方法
  拼接打印/结果.值 拼接打印）；供给.cn 供给 映射$整64$字符串 符号（v2 组件未实例化）。
  宿主 sanity 先行（退出码 0 输出 题/名/词/甲）——同源双编译器一致性锚定。六步闭环 PASS，
  v2 产物运行时人眼审计：题/名/词/甲/全部通过。
- **65_string_index ARM64 平台跳过摘除**：原「char 符号扩展差异」在宿主 LoadPtr i8 双后端
  统一符号扩展后不再成立（手动实测输出与期望逐行一致含 字节和 -376），随全量回归复验。

### 二、词法分析.cn 冒烟真绿 + 语法分析.cn 冒烟启动

- **词法分析.cn（291 行）**：下标+复合赋值落地后 v2p 编译全绿（10 函数/asm 2736 行）——
  分词 本体首次被 v2p 完整编译（原 += 处截断）。
- 语法分析.cn（935 行）冒烟与全量回归结果见 plans/014 与 HANDOFF。

### 三、边界（诚实记录）

- v2 组件 `+=` 左值全部为简单标识符（20 处 grep 实证）；成员左值复合赋值已实现但组件
  未用、用例已覆盖。

### 四、审计后续：复合赋值非数值操作数显式拒绝（用户裁决「现在补」）

- 用户质询纪律执行（灰色点二举例说明后裁决）：`字符串 +=` 宿主拒绝（实测「复合赋值
  要求数值操作数」——字符串/布尔/字符均拒、整系/浮点系过、显式 `名 = 名 + "乙"` 合法）
  而 v2 接受=判定分歧（v2 是「没查」非「决定支持」——半实现形态）。
- 实现：解析层合成二元节点 子终点=1 标记（与手写 `名 = 名 + "乙"` 可区分；&&/|| 预扫
  按 子终点!=0+op 名双条件识别不受影响）+ 语义层左值类型白名单（整系/浮点系 10 名单；
  左值类型取左值区间**末**节点——变量=标识符/成员链=成员节点，取首节点会把对象结构体
  当操作数类型误拒，129 回归实测踩坑即修）。
- 固化：v2p 内置负测自检（`[sem5] 复合赋值负测通过`——每次编译自证拒绝能力）+ 129
  主.expected 增该行（闭环强制断言）。三态探针：拒绝 字符串+=/放行 显式拼接赋值/
  自检通过；12 系列 13 用例全过。
