# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 450-a 开发轮：C20 批4 防线对齐桶 11 例 v2 闭环负测上架·2026-09-20·wt450 隔离）

- **批4 上架 11 例**：135_v2（-> 废除）/159_v2（E0255 导入同名）/163_v2（模块自动加载强语义）/265_v2（**安全区边界**·157-a 波6 v2 侧锚）/275_v2（**A4 联合体拥有型**·164-a v2 侧锚）/279_v2（**B11 常量空指针**·v2 侧锚）/319_v2（跨包未声明）/326_v2（货舱坏语法）/356_v2（上下文词非语法位）/82_v2+83_v2（模块私有）——v2p 编译期优雅拒绝持久门禁（414_v2 先例模式·零 src/ 零 v2 树零 runner）。
- **扫描与甄别**：静态筛 116→check 实测 111→四桶（sem5 58/parse 45/过 8）→排除已上架 36 号+孪生+T63 13+T64 5→待看 14 逐例完整输出甄别→上架 11+不固 4 定性（101/339=v2 特性缺口拒·312=ir2 区归 448）。
- **验证**：11/11 单跑 PASS+反证抽例闭环+coverage_map +11 行。
- **前情（447-a·挂起）**：九链集成唯一真红 112_v2 归 448；随轮=437 断言迁移/459_v2 迁移/B9 零命中/gitattributes/integrate 串行复验/ASCII 修复。

## 深度机 linux-x86_64

## 功能完善总结（448-c 112_v2 期望迁移+dbg 残留全清+456 回签·2026-09-20）

- **448-c（家机 447 呈报红处置·§8.6 归 448 轮）**：112_v2 期望迁移实跑固化（459_v2 同款）——435 链 stdlib 源码级加载后「释放」已声明=正确演进，负测语义保持（ir2 错误码6 仍拒绝）；dbg 残留四件全清（[dbg收文]+TEMP435b2/438c/441b 三块 IR dump·448-b WIP 载荷债务兑现·grep 零依赖零误伤）。反证双态+全量门禁（465=463/0/2 零新增红）。
- **456 回签（窄通道·服务轮）**：家机 456-D1拆分 linux 全量门禁全绿（零警告+单测 1363+E2E 464=462/0/2+自举链双 PASS+v2p 等价 asm 对照反证）→准予集成。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
