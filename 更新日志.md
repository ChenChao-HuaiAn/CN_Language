# 功能完善总结

> 第七十二轮下半程收尾（2026-09-11，深度机 linux-x86_64）：上轮工作核实 + 用户裁决「瑕疵清理 4+1 与三层防线全部实施」。

## 一、上轮核实（先核后改）

复跑上轮全验证链与探针，**结论：上轮工作成立**——清场重建零警告（4 处匹配=文件名含 error 进度行）、单测 1289/1289、E2E 234 用例 229 过（唯一败=194 既有待批）、锚定链固定点 266964 逐字节一致、对拍 29/29；探针 66 六形态残留全零、探针 86 搅动后元素完好；115/116 期望逐行语义核对全部可解释；文档五件套同步完整。核实另发现 4 处非缺陷瑕疵 + 1 处口径观察（见下）。

## 二、瑕疵清理 5 处（用户批准）

1. **`(void)0` 死代码删除**（`ir_oop_call.cpp` genBlockExitDestruct 两个 while 之间残留）。
2. **`LoopContext::scopeDepth` 改名 `classBase`**——原命名失准（实际存类对象名单基线，非作用域栈深度），注释同步修正；`genLoopJumpDestruct` 重构为按基线参数的通用版 `genJumpDestructFrom(stringBase, classBase)`，循环/选择两调用方共用。
3. **`genVarDecl` 登记处注释修正**——原注释「污染名不登记」与代码不符（实际登记、释放侧统一跳过=双保险），改为与实现一致的口径。
4. **泛型单态化出口补五状态 clear**（`ir_generic_func.cpp`：stringTainted_/ownedStringOrder_/ownedClassOrder_/scopeStringBase_/scopeClassBase_）——与 visitFunctionDecl 出口对称；**补齐前实证调用时机**：泛型实例由 visitProgram 第 4 步批量生成、不嵌套于其他函数体生成中，clear 安全（若嵌套则照抄反而破坏宿主活状态——stringTainted_ 跨函数共享即此类，对称性判断以调用时机实证为准）。
5. **switch 中断 drop-on-jump 落地**——选择分支（情况/默认）不走 genBlock（语句直接生成、无块出口析构），原仅函数级兜底；现 `switchStack_` 由纯标签栈改为 `SwitchContext{exitLabel, classBase, stringBase}`（与 LoopContext 同构），中断 跳出前按分支基线发射块级释放，与循环口径对齐；fallthrough/汇合路径仍由函数级兜底覆盖。

## 三、清理中静态推演发现真缺陷，当轮根治（探针实证闭环）

**类方法返回路径泄漏**：`emitClassMethod` 为三类函数级生成单元（visitFunctionDecl / emitGenericFuncInstance / emitClassMethod）中**独漏函数级兜底者**——类方法体内声明拥有串后中途 `返回`，绕过方法体 genBlock 出口析构（已终止），又无返回块兜底 → 泄漏。**探针 87 修复前实测残留 1**；补 `genClassDestructorCalls` + `genStringFrees` + 五状态 clear（与前两类生成单元完全同构）后**归零**。

**连带文档口径修正**：块级析构实现实际对**所有**类对象发射 DeleteObject（含无析构类的内存释放，探针 87 的无析构类对象残留 0 印证）——原 HANDOFF 诚实边界「限有析构函数类」声明落后于实现，已回改（函数级兜底 genClassDestructorCalls 仍限有析构类，与块级的统一=观察项在册 plans/020）。

## 四、三层防线（用户批准，全部实施）

1. **探针转正（门禁替你记得）**：新增 E2E 用例——
   - `tests/e2e/213_块级作用域RAII内存回归/`：八形态（循环体字符串/循环体容器/如果体/裸块/循环内嵌套块/循环内中断/**选择体中断**/**类方法返回路径**）调用前后 `内存::活动分配数()` 差值恒 0；
   - `tests/e2e/214_容器元素所有权回归/`：拥有串入容器（显式复制）+32 轮搅动制造内存复用压力，元素内容完好=不悬垂。
2. **plans/020 拥有权流转矩阵**：字符串 20 格+类对象容器 7 格（位置×语义×验证证据×待办），F4/F5/F1 类缺口自此有「空白即靶子」的台账；4 个在册靶子：入容器编译器级自动 copy/move（容器析构轮）、数组元素/装箱字段/结构体字段（NLL 轮）、v2 跳出路径核验；2 个语义观察项在册（循环内选择体 中断 绑定、v2 契约键基回退近似）。
3. **治本归口**：NLL 借用检查（v2「安全接近 Rust」目标）为这批 UAF 的终极解，矩阵靶子归口排程。

## 五、验证链全绿

清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 1289/1289 + E2E **236 用例 231 过**（唯一败=194 既有待批）/4 跳 + 锚定链 fix_p=fix_s=**266964 行**逐字节一致（v2p 经新宿主重建后固定点维持=宿主语义改动对 v2 自举产物透明实证）+ 绑定自检 + 运行级 3/3 + 组件对拍 29/29；探针 87 修复前后残留 1→0 实证入提交。

## 六、诚实边界

函数级兜底 genClassDestructorCalls 仍限有析构类（块级已覆盖内存释放，两口径统一=观察项在册）；循环内选择体 `中断` 绑定循环（实现 loopStack_ 优先）vs C 语义最近绑定——规格书 3.5 未明确，**语义变更须用户裁决**（观察项在册 plans/020）；数组元素/装箱字段/结构体字段/静态=借用无 RAII（NLL/容器析构轮靶子）；v2 侧跳出路径 drop-on-jump 待专项核验；194 待批维持；win 并行隔离（家机轮）；探针留 /tmp/p72probe 不入库。
