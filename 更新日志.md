## 功能完善总结

### 模块导入语法全形式测试补全（2026-08-15）——E2E 覆盖缺陷修复

**背景**：用户指出测试未彻底——现有 E2E（如 [`tests/e2e/29_core/主.cn`](tests/e2e/29_core/主.cn:10)）只测试了 `导入 模块.符号` 这一种导入形式，`从 模块 导入 名`、整模块导入、多级路径、边界错误等全部导入语法未测试。本轮补全模块导入语法的 E2E 全形式测试 + 单测错误用例。全量回归：单测 **1057/1057**（1050 + 7 新增）、E2E **45/45**（44 + 1 新增）、编译 0 错误 0 警告（MSVC /W4 /WX）。

#### 一、调研结论：CN 语言模块导入语法全集（规格书08-二）

| 语法形式 | 示例 | 编译器实现 | 本轮测试 |
|---------|------|-----------|---------|
| `导入 模块.符号` | `导入 数学.平方根` | importPath="数学.平方根" → 限定调用重写 | E2E 43 ① |
| `导入 模块`（整模块） | `导入 数学` | importPath="数学" → 公开符号合并 | E2E 43 ② |
| `导入 多级路径` | `导入 网络协议.HTTP.请求` | 依赖取首段「网络协议」+ 递归加载 | E2E 43 ③（依赖链） |
| `从 模块 导入 名` | `从 数学 导入 正弦` | fromImport=true → 公开符号合并 | E2E 43 ④ |
| `从 模块 导入 名1, 名2` | `从 数学 导入 正弦, 余弦` | names=[正弦,余弦] | E2E 43 ⑤（泛型跨模块） |
| 公开符号纯名访问 | `导入 数学` 后 `双倍(21)` | 公开声明合并 | E2E 43 ⑥ |
| 函数重载跨模块 | 同模块双签名 | signatureKey 区分 | E2E 43 ⑦ |
| 泛型函数跨模块 | `最大值<整32>(10, 42)` | 单态化 | E2E 43 ⑤ |
| 私有符号不跨模块 | `私有:` 段 | 合并时过滤 | 单测（既有 SemanticPrivateNotVisibleAcrossModules） |
| 导入不存在符号 | `导入 数学.不存在` 后调用 | 语义层报「没有公开符号」 | 单测 SemanticImportMissingSymbol ✅ |
| 从导入不存在名字 | `从 数学 导入 不存在名` | 未合并 → 未声明 | 单测 SemanticFromImportMissingName ✅ |
| 导入不存在模块文件 | `导入 不存在模块` | driver 加载失败 | 单测 ModuleDriverTest.ImportMissingModuleFileFails ✅ |
| 跨模块函数重名 | 两模块同签名 | 报「重复定义函数」 | 单测 SemanticDuplicateFunctionAcrossModules ✅ |
| 跨模块重载共存 | 整32/浮64 双签名 | 不冲突 | 单测 SemanticOverloadAcrossModules ✅ |

**不支持**：通配符 `导入 全部`、顶层 `常量` 声明（Program 无全局常量存储）、别名导入。

#### 二、新增测试

- **E2E [`tests/e2e/43_module_import/`](tests/e2e/43_module_import/主.cn)**（多文件：主.cn + 模块甲.cn + 模块乙.cn + 模块丙.cn）：
  - 模块甲：公开 函数（含重载/阶乘递归）+ 枚举 颜色 + 类 计数器（构造重载）+ 泛型 最大值 + 私有 秘密平方
  - 模块乙/丙：依赖链 `主 → 模块丙 → 模块乙`（验证多级路径递归加载 + 拓扑排序）
  - 主.cn 覆盖 ①~⑨ 全部导入语法形式（限定调用/纯名/多级路径/多符号/泛型/枚举/类/重载）
- **单测 [`tests/unit/module/test_module.cpp`](tests/unit/module/test_module.cpp)**（+5）：
  - `SemanticImportMissingSymbol` / `SemanticFromImportMissingName` / `SemanticDuplicateFunctionAcrossModules` / `SemanticOverloadAcrossModules` / `SemanticQualifiedCallWithoutImport`（已知缺陷 P1 显式记录）
- **单测 [`tests/unit/module/test_module_driver.cpp`](tests/unit/module/test_module_driver.cpp)**（新增文件，+2）：
  - `ModuleDriverTest.ImportMissingModuleFileFails`（导入不存在模块文件 → 编译失败）
  - `ModuleDriverTest.ImportExistingModuleFileSucceeds`（driver 正向链路）
  - 链接 `cn_compiler_driver`（tests/unit/CMakeLists.txt 更新）

#### 三、⚠️ 发现的真实编译器缺陷（P1，记录未修复）

**未导入模块的限定调用被静默接受**：`数学.平方根(16.0)` 在**无任何导入语句**（甚至单文件无 数学.cn）时编译成功，未报「未声明的标识符 数学」。
- 实测：`target/import_test/单独.cn` 构建成功（退出码 0）
- 根因：语义层 `visitCallExpr` 模块限定重写块（semantic.cpp 1984-2031）条件 `userFuncExists || moduleImported || hasFunctionName(qualified)` 为 false 时不进块，落入成员方法调用分支（2262）但 `checkExpr(object)` 对未声明标识符未报错
- **处理**：本子任务聚焦测试补全，不做高风险语义层修复（避免误伤标准库内置限定名 `数学.平方根`/`IO.读取行` 等）；缺陷记录于 HANDOFF/lessons，供阶段7 Debug 子任务根治
- **回归保护**：单测 `SemanticQualifiedCallWithoutImport` 显式断言当前行为并打印诊断

#### 四、测试基线

| 指标 | 结果 |
|------|------|
| 单元测试 | **1057/1057**（1050 + 7 新增） |
| E2E | **45/45**（44 + 43_module_import） |
| 编译警告 | **0**（MSVC /W4 /WX） |

#### 五、文档更新

- [`plans/002-阶段6-标准库.md`](plans/002-阶段6-标准库.md)：Task 6.11 补记「模块导入语法全形式测试补充」✅ + P1 缺陷记录
- [`HANDOFF.md`](HANDOFF.md)：本轮补测记录 + P1 缺陷交接
- [`lessons.md`](lessons.md)：记录「测试未彻底/测试覆盖不足」错误（E2E 只测一种导入形式）
