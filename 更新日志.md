# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🧪 M3 采样测试轮·2026-09-17）

**284-a 病灶② 根治的 win-x64 平台独立复验（双轨采样全绿·零新命中零新类别）**：轮 A 回归轨＝命中库 38 全量一致（病灶② 预期 7/7→0 实证达成）；轮 B/C 不同采样＝新种子 2026091711/12 各 100 新样本（避开深度机同日种子 01~03）各自 100/100 零分歧零失败，C 轮复放二次验证一致。前置：cn.exe 全量重建（原产物早于 97ca5c1 提交·按假重建陷阱教训）＋402 布尔短路矩阵O3 E2E 双绿确认根治在位。插曲：`target/cnsmith/work/分歧清单.txt` 为 09-16 残留（258-a 时代 A2070 旧现场·diff 零分歧时不清理）致编排脚本首轮误读 1360 条假命中——逐项证伪后删除残留并复放闭环；daemon 改进建议（--seed 自定义防三机撞种子＋零分歧清旧清单＋target-cn 同步重建）已留 HANDOFF 待开发轮采纳。**rebase/merge 深度机 285-a 两提交后交叉甄别→【定案】（已呈报）**：深度机两次判定（「O0 对 O3 错」→「O3 IR=常量布尔无条件存储·修优化层」）经本机 asm 三件套实证**反转**——真缺陷=**linux_x64 后端 O0 移位发射操作数宽度缺陷**（`mov r10d` 32 位零扩展装载负数+`sar r10` 64 位移位→-426 污染成 524286→变7=1；O3 用 64 位装载=-1=规范正确·win 32 位 sar eax 正确→变7 规范值=0）——修复面=linux_x64 发射层（32 位值用 32 位 sar 或 movsxd·非优化层·与 284-a 无关）·初判面=有符号右移 32 位负值 O0 直译（shl 免疫/win 免疫/arm64 待查）；E2E 420 未捕获=存量无此形态·**M3 采样独家捕获实证**。源码/测试/规范零改动（纯验证轮·基线 de4c0b6→终态含 285-a/286-a 认领与 4ad94f6 合并）。

## 深度机 linux-x86_64 节

## 功能完善总结（🧪 缺陷族面扩散检验轮·2026-09-17·免锁验证轮）

**用户令「点→线→面」制度性检验首案（源码零改动·基线 3e21ca0）**：建成缺陷族面扩散探针 famscan（/tmp/cn_family/famscan.py·根因模式→扩散轴〔求值位置×类型位宽×消费指令×符号性×优化级别〕→双 oracle〔gcc 同语义对拍+python C 语义模拟〕→O0/O3 实测→跨后端 asm 定性），以 s2026091705/T13/T14 三点缺陷为种子扩散实测（4 族 47 用例）——**T23 立案：linux_x64 有符号右移负值 14/17 形态错**：11 形态 O0+O3 双错（局部=287-a 已知点＋静态变量/数组元素/结构体字段/函数实参/三元值位/负移位量/整16×2/整8/INT_MIN/整16边界 **10 个新形态=「面」的实证**）+3 形态 O0-only 错（返回值/复合赋值>>=/负字面量·IR 实证 O0 不折叠走运行时发射）；根因=emitShift 单点模板（asm 17 处全同「mov r10d 零扩展装载+sar r10 64 位消费」·面宽挂点同源）；win/arm64 32 位寄存器运算免疫（asm 静态实证）；**无病面实证 31 用例**（整64 右移/左移全家/除法/取模/比较/位运算/转换/无符号正32正64 全 12 例——修复边界不扩大化）。处置=T23 落 plans/021 §三-G+287-a 修复轮必带 16 形态扩面复验；制度固化三落点呈报 §三-B B9（AGENTS §8 扩条/探针转正 scripts//plans/026 §2.11·与 B8 面台账互补）。对家机 M3 定案的细化印证：变量路径 O3 同错（s1705 的 O3 对=字面量走立即数装载）。原始输出与 asm 留档 /tmp/cn_family（rt_*.out/fam_o0.s/fam_o3.s/fam_win.asm/fam_arm.s）。

## 单位机 ARM64 节## 单位机 ARM64 节

## 功能完善总结

### 286-a 开发轮：S1b 采样日志分文件化根治 + M3 采样 arm64 首轨（2026-09-17，单位机麒麟 ARM64，基线 c37e1ac→rebase 82bdbdd·认领 f4c6626·用户质询「日志会爆炸」+令「需要现在修改」驱动）

1. **S1b 根治**：daemon 日志按月分文件（日志/YYYY-MM.md）+「变化时记/定时汇总」+命中去重；存量 2583 行原样迁移 2026-09.md（三机在飞增量折入）；采样日志.md 指针化（≤15 行）；check_handoff.py 扩采样日志门禁（反例三连拦截+正例 PASS 双向实证）；两防御（差分前删旧分歧清单+重建产物新鲜度核验）；种子改时间戳到秒根治重启撞号。
2. **M3 采样 arm64 首轨**：三轮 351 新样本输出分歧 0；**独家捕获 T22**（arm64 -O3 位运算常量立即数非法汇编·win/linux_x64 双免疫·31 样本入库）呈报待裁；arm64 移位零扩展定点探针免疫实证（-426>>13 O0/O3 双=-1）。
3. **收尾**：plans/021 戳 286-a+T22 立案+S1b 销项+索引；编译器源码零改动（E2E/单测基线=82bdbdd 未重跑）；双文档门禁 PASS。
