# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（✅ 458-a 开发轮：D8/F1-28 第三步=x64 结果写入直写·2026-09-20·分支 任务/家机-458-x64冗余mov消除·worktree=wt458·集成序独立）

- **win 主平台性能正主**：`physRegOf` 辅助+五发射方法直写（IntBinary 含 8/16 位扩展分支/Shift/ConstLoad/Load/AddrOf）——**win 冗余 mov 91197→22960 条（-74.8%）**。
- **x86 两地址特有冲突判据**：op2 与 dst 同物理寄存器（死点复用）退回原三段；未分配态 mem→mem 禁止保持 rax 中转=产物逐字节不变。
- **必要搬移定性**：Call ABI 接收/idiv 固定 eax/edx/i128 双槽/浮点=不可消除。
- **验证链全绿**：零警告+单测 1363/1363+E2E **464=464/0/0 全绿**（406/431 已随 453 集成清零=新基线）+asm 宽度 289×3 PASS+反证闭环。
- 门禁/try-build/集成结果：见 plans/025 §1.0（458-a）收工回填。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 475-a D46 残余收口：声明初始化位构造名裸名拒绝＋本会话四轮连续·2026-09-20）

- **服务轮三签**：459-a linux 回签+451 集成后复验备案+467-a linux 回签（1369/1369+E2E 477=475/0/2 零新增红·准予集成）。
- **470-a D10 双侧销项**（挂等家机 win 回签）：v2 泛型实例体重放语义检查+457 负测+宿主 recheck 诊断窄放行——门禁 477=475/0/2 零新增红。
- **474-a D46 销项**（挂等家机 win 回签）：v2 构造名裸名返回语句位拒绝+458 负测——门禁 477=475/0/2 零新增红。
- **475-a D46 残余收口**：声明初始化位构造名裸名拒绝（语义检查语句.cn +17 行·p9 rc 0→1+反证双态）——**59 行实测定案**=callee/裸名共用路径维持放行（方案 A 24 族 callee 位回归实证→方案 C 语句级单点·025 §2.84 演进全记录）；门禁 476=474/0/2 零新增红+自举链双 PASS。
- **诚实边界**：赋值位/实参位裸名=再残余登记（赋值节点结构·独立轮）；「某些」裸名=可选枚举值语义宿主放行先例（不收口）。
- **下一步**：470/474 家机回签→集成；队列续认领。


## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
