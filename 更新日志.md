# 功能完善总结

> 76-a 移除-覆盖-清空路径所有权全链补齐（2026-09-12 第七十六轮，深度机 linux-x86_64）：plans/020 矩阵靶子「映射 `删除(键)` 释放被删项字符串」销项——执行中发现同族缺口一族（八枚，探针全量实证），按「一劳永逸」一次修全。

## 一、缺陷（八枚，探针 76 实证，全部可复现）

| # | 形态 | 性质 |
|---|---|---|
| ① | 映射 删除(键)：交换式删除覆盖被删槽 → 被删项值串泄漏 | 泄漏（本轮立案靶子） |
| ② | 映射 设置覆盖已有键：旧值句柄被丢弃 | 泄漏 |
| ③ | 映射 释放内部数组（H8-5 显式销毁路径）：漏注入 | 泄漏 |
| ④ | 集合<T> 析构/清空：元素串原无释放面 | 泄漏 |
| ⑤ | 向量 删除(位置)/设置覆盖：移位浅拷覆盖旧句柄 | 泄漏 |
| ⑥ | 链表 删除头部/删除尾部/清空：被摘槽出链、链头归 -1 | 泄漏 |
| ⑦ | 队列 清空 | 泄漏 |
| ⑧ | **映射入容器位未归一化**：借用来源句柄浅存 → 容器析构释放调用方串 | **UAF（安全级）** |

## 二、修复（Rust 容器所有权模型 + C++ erase/insert_or_assign 就地析构对照）

- **入容器位归一化扩到 映射（值侧）与 集合**：借用来源复制落堆（容器独立拥有）、拥有返回与
  转移零拷贝接管（Rust `push(s.clone())` / `push(s)` 同款）——UAF 销项。宿主 `isStringValuedMap`
  新增 + `isStringElemContainer` 加集合；v2 侧 `是字符串值映射` 新增 + `是字符串元素容器` 加集合。
- **stdlib 挂点按「槽的持有者状态」分工**：新增 `析构被移除(索引)`（唯一持有者槽——字符串注入
  单槽释放 `__cn_seq_free_slot`；有析构类零注入=零行为变化）、映射 `析构键值/析构值`
  （删除/覆盖写单槽，`__cn_map_free_slot`）；既有 `析构元素` 保持「有析构类」语义（移位目标槽/
  末尾移出槽——**浅拷模型下与有效槽重复引用，字符串不注入**，首版修复悬垂实证）。
- **全量路径扩到 集合**（`__cn_vector_free_strings` 平铺同款）+ 映射 释放内部数组 并入全量注入。
- 所有释放点沿用「释放后槽清零」幂等不变量。
- **连带修复**：v1 组件 IR4.cn「先 `类型栈顶` 后 `类型弹出`」= 借出视图跨容器移除点悬垂
  （新语义下）——74/75/76 自举链用例实证回归（IR dump 乱码 + 产物行数漂移），修 `字符串复制`
  取拥有副本；矩阵新增 A21 格（借出视图 × 容器移除，NLL/借用检查轮靶子）。

## 三、防线（新增两用例 + 反证实证）

- **E2E 220_映射集合所有权移除路径**（宿主八形态折校验和 **255**；每形态经内存搅动制造复用压力）；
- **E2E 221_双编译对照映射集合所有权**（同一源码两侧各编各跑，**退出码须一致=255**；移植纪律 9
  第三个对照用例；供给.cn 供 集合/映射/链表/队列<字符串> 符号含 76-a 新挂点方法）；
- **反证实证**（门禁有效性）：注入「删除路径缺释放」→ 宿主 220 校验和 255→**254**（位1 捕获）；
  注入「v2 归一化失效」→ v2 侧退出码 255→**127**（位128 精准捕获 UAF 内容损坏）；缺陷副本已恢复。

## 四、验证链（全绿）

清场重建零警告（4 处匹配=文件名含 error 既有口径）＋单测 **1289/1289** ＋ E2E **243 用例 238 过**
（唯一败=194 既有待批）/4 跳 ＋ 锚定链固定点 **272094 行**（新口径）逐字节一致 + 绑定自检 ＋
组件对拍 **29/29** ＋ 释放路径两侧对照（脚本扩 76-a 挂点清单与清零计数）。

## 五、文档

plans/020（矩阵 A21/B32/B33 新增、A14/B30 更新、防线表、第七十六轮段含方法论与诚实边界）＋
plans/019（跟踪表行）＋ lessons（第七十六轮段，权重 8/8/7/6 四条）＋ 更新日志 ＋ HANDOFF。

**诚实边界**：集合<有析构类> 删除前移的旧对象析构（既有边界）；数组元素/装箱字段/结构体字段
持串/静态面无 RAII（既有）；字符串键（`映射<字符串,...>`）当前不可编译——键侧归一化/释放随
字符串键支持轮；194 待批维持；win 并行隔离（家机轮）。探针留 /tmp/p76probe 不入库。
