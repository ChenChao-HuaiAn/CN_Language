## 宿主缺陷根治：顶层静态容器变量全链路（P3-8，2026-08-30）

**目标**：根治宿主编译器（src/cn_compiler）对「顶层静态容器变量」（静态 向量<整64> 全局表）的完整支持——此前 声明/登记/类型归一/IR 引用/方法调用/.data 分配 五处缺陷叠加，导致 v2 自举所需全局容器不可用。

**根因链与修复**（五处叠加，全部根治）：

1. **parser**（parser.cpp）：顶层静态分支用 parseTypeName()（只吃标识符）→ 改 parseTypeNameEx()，支持泛型/指针/数组类型（静态 向量<整64> 全局表 原报「预期变量名，实际为 '<'」）。
2. **semantic**（semantic.cpp）：① 无初始值静态被 initializer==nullptr 跳过登记 → 改 无初始值且非静态 才跳过；② 静态类型用 canonicalType（不归一泛型）→ 改 resolveGenericTypeName（向量<整64> → 向量$整64），成员访问 findClass 不再报「不是类类型」。
3. **IR 表达式类型推导**（ir_oop.cpp exprSrcType）：IdentifierExpr 分支补全局静态类型解析（isGlobalStatic + globalStaticType）——否则 全局表.大小() 的对象类型解析失败 → handleClassCallExpr 走通用间接调用 → callee MemberExpr 被当字段访问生成空 extra 的 AddrOf → [rbp0] A2006 汇编错误。
4. **IR 实例方法 this**（ir_oop_call.cpp）：全局静态对象方法调用 this 须为 .data 符号地址（?gstatic_名）而非 LoadPtr 读值——否则把对象首 8 字节（数据指针）当 this 传入，大小() 判空/读偏移全错（空指针解引用）。
5. **codegen .data 分配**（x64_codegen.cpp）：容器/结构体静态按 typeSizeOf 分配完整对象字节（向量=24 字节：数据/元素数量/容量），原固定 8 字节导致对象越界/相邻符号污染。

**验证**（真实生产链路：编译 → 汇编 → 链接 → 运行）：
- 静态 向量<整64> 全局表：全局表.大小()=0、追加(42)+追加(7) 后 大小()=2、元素(0)=42、元素(1)=7 → **exit=249（2*100+42+7）** ✓
- 回归：E2E 30_container/96_顶层常量/111/113/115/117/104/01/05/07/09 全 PASS；单测 **1196/1196 PASS**；v2 自举编译器构建+运行 exit=0。

**下一步**：v2 编译自身（自举闭环）——需补 数组/字符串/容器 等运行时能力后推进。
## 功能完善总结

### v2 自举重建：中断/继续 循环验证 完成（P6d，2026-08-30）

**目标**：按 plans/014 下一步，验证 v2 编译器（CN语言编译器v2/）的 中断/继续 循环控制（IR 层已实现，本轮实际运行验证）。
