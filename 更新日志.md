# CN语言编译器 更新日志

## 功能完善总结

# 第三十七轮（2026-09-08 深度系统 x86_64）：宿主 &&/|| 短路求值缺失根治——组件对拍灰色点归零 + 8 用例运行级对拍矩阵收口

## 一、灰色点专项取证（上轮挂账「4 入口字符串池编号差 2——未初始化读取嫌疑」）

- **驻留轨迹插桩**（src/runtime/intern_api.cpp，诊断资产永久入库）：`__cn_intern` 加
  `CN_INTERN_TRACE` 环境变量开关——仅新驻留分支（重复驻留不打印）输出 ID+内容+调用者
  返回地址；static 缓存判定，默认关闭零输出零开销。v2p 与 cn_self 共链同一份运行时
  源码，一条插桩两侧通用。
- **轨迹 diff 实锤（非多驻留而是顺序互换）**：v2p/cn_self 编译 语法/包.cn 各 3265 条
  驻留，diff 仅 3 行——`&` 在 p 侧 ID 272、s 侧 274（`词法分析$字符串`/`词法分析$整64`
  与之互换），其余逐条一致。产物侧 strData_272/274 错位同源。
- **gdb 断点+反汇编定位根因**：`&` 首驻留 p 侧 @解析类型（解析 词法分析.cn Token
  结构体字段时，ra 反解+当前 token 现场实证）、s 侧 @解析函数声明（字节值 返回类型
  判定）。v2p 的 解析类型 bb11 **无条件 `call __cn_intern("*")`**——
  **宿主把 &&/|| 发射为单条 And/Or 指令（全求值，RHS 无条件求值）**；v2 编译器
  发射=标准条件块短路（bb140 结构条件跳转守卫）——**两编译器发射语义分歧**，
  驻留池副作用（驻留("&") 提前入池）使其可观测为编号漂移。诊断流/运行 rc 全同的
  「良性灰色点」表象实为语义分歧指纹。

## 二、根治（rustc 同构手法，用户准则：性能第一/安全第二）

- **ir_expr.cpp visitBinaryExpr 头部 AndAnd/OrOr 专用路径**：IR 生成期脱糖为控制流
  **三块结构**——主块（LHS 求值→endBranch）→ {RHS 块（求值 RHS 存槽）｜默认块
  （存短路值 真/假）} → 汇合块（Load 结果 i1）。
  - `a && b`：a 真→RHS 块（结果=b）；a 假→默认块（结果=假）
  - `a || b`：a 真→默认块（结果=真）；a 假→RHS 块（结果=b）
  - **性能**：跳过不需 RHS 求值（比全求值更快）；**安全**：RHS 副作用（函数调用/
    除法/驻留）按短路语义执行——对齐 Rust `&&`/`||` 语言规范规定的短路语义与
    rustc HIR→THIR 的控制流脱糖。
- **And/Or 指令保留**：编译器内部布尔组合（空检查/边界检查 ir.cpp/ir_oop*.cpp）
  操作数均为纯值，全求值语义等价，不受影响。
- 语义层保证逻辑操作数恒为布尔（IR i1），LHS/RHS 均无需真值转换。

## 三、修复过程踩坑（当轮识破，已入 lessons）

1. **endBranch 条件寄存器挂载契约**：条件寄存器 push 到「块最后一条指令的最后一个
   操作数」（codegen emitTerminator 同位置读取）——首版在条件求值后夹入
   Alloca/Store，条件挂到 Store 上被 codegen 读成数据操作数（`真||假` 判假）。
   改为三块结构让条件指令天然居块尾（与 genIf 惯例同构）。
2. **i1 常量文本契约**：loadOperandToX 对 i1 常量仅识别 `"真"`（装 1），其余一律装 0——
   `constant("1","i1")` 实际发射 `mov $0x0`。改用 `constant("真"/"假","i1")`。

## 四、验证（门禁全绿+灰色点归零+矩阵收口）

- 单测 **1251/1251**：LogicalIR 反转为短路形态锚定（块数 ≥3 + And/Or 指令=0 + Not 保留，
  行为反转防回归）+ 新增 LogicalShortCircuit（RHS 调用形态块结构锚定）。
- E2E linux-x86_64 全量 **177 用例 173 过/0 败/4 跳**（基线零回归+新增
  **175_短路求值副作用**：计数器副作用四路矩阵运行级锚定——假&&/真|| 不调、
  真&&/假|| 各调一次，调用数=2；全求值回归即失败）。
- **灰色点归零**：v2p/cn_self 全组件对拍 **26/26 逐字节一致**——原 4 灰色点入口
  （语法/包.cn、IR/包.cn、解析基础.cn、代码生成共用.cn）全部归零。
- **固定点复验**：fix_p ≡ fix_s 逐字节一致（短路发射后 v2p 与 cn_self 语义对齐，
  自举封闭性保持）。
- **8 用例运行级对拍矩阵 8/8**（119/123/126/128/130/148/150/158，上轮挂账收口）：
  v2p 与 cn_self 分别编译+链接+运行——asm 逐字节一致且运行 rc 一致
  （119=14、123/126=127、其余=0）。
- 诊断工具转正：scripts/intern_trace_resolve.py（nm 排序表二分反解驻留轨迹 ra 为
  CN 函数名，局部标签向上回退，hex 段 UTF-8 解码）。

## 五、诚实边界与挂账

- **5 个组件文件单文件入口编译失败**（语义检查.cn/语义检查表达式.cn/代码生成三后端）：
  R3 拆分后包内兄弟符号对独立入口不可见（未声明语义错误 rc=1）——**worktree 基线
  对照实证与短路修复无关**（HEAD 即失败）；单文件入口的包上下文恢复机制挂账。
- win/arm64 运行级复验随跨机轮（短路发射三后端共享 IR 层，已由共享层修复覆盖）。
- abi辅助.cn（v1 遗留探针 13 行）转正或删除（顺延）。
- v2 结构体构造字面量 `记录{...}` 语法缺口（随语法覆盖轮）。
