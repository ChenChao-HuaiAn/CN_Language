# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十二轮 112-a：D1 行数整改第三波——`ir_expr.cpp` 2798→734 行按族拆分**（2026-09-13，家机 win-x64；基线 4a3218f）：
> ①**拆分**（纯重构零行为变更；成员函数声明仍在 `ir.hpp`，实现搬迁）：`ir_expr.cpp` **2798→734 行**，四族
> 拆出 `ir_expr_unary.cpp`（一元/三元 435）/`ir_expr_assign.cpp`（赋值 949）/`ir_expr_index.cpp`（下标 310）/
> `ir_expr_member.cpp`（结构体整体赋值/构造/成员访问 434）——**全部 <1000 行**；族边界勘定=无文件级 static/
> 匿名命名空间依赖 → 可自由拆；CMakeLists 挂载 4 文件。
> ②**等价性硬证据**：重构前采集宿主编译产物 asm **五样本**（v2 全树 **404786 行** + hello/struct_enum/u240/u241）
> md5 → 重构后**逐字节一致**（比单点 md5 覆盖面强）。
> ③门禁：零警告构建 + 单测 **1313/1313** + 全量 E2E **290/292**（78/79 OOM 既有败面，零回归）+ 锚定链固定点
> **406097 行不变**（与 111-a 完全一致，cn_main 地址亦同）+ 对拍 **34/34** + 运行级 **3/3**。
> ④**勘定登记**（为后续波次铺路）：v2 侧超长文件实为 **6 个**（原记 3 个已过时）、**codegen 面 4 文件**新登记
> （x64_instructions 1811/linux_x64_codegen 1129/x64_codegen 1086/arm64_codegen 1072）、`semantic.cpp` 拆分
> 需先建 `semantic_internal.hpp`（`canonicalType` 被 4 族共用，匿名 ns 须 inline 头化）。
> ⑤教训（lessons 第一百一十二轮段）：匿名 ns = 拆分隐藏依赖点（helper 引用族数 ==1 随族 / ≥2 头化）/
> 行级多重集校验法 / 产物 asm 多样本等价验证 / 拆分脚本按 CRLF 处理 + `format` 与 C++ `{}` 冲突用 replace。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
