# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百三十八轮 138-a：波 3 轮 A 实施尝试——实现打通、回归回退与发现四则**（2026-09-14 上午，家机 win-x64；基线 513443a）：
> ①**实施范围**（四文件：ir.hpp `OwnedStrField.kind` + ir_fields.cpp 收集/消亡位/preFree/拷贝位 +
> ir_expr_assign.cpp 成员赋值位归一化）——**实现打通**：形一/形三**归零** ✓、崩溃全消（f1~f4 全 rc=0）、
> **深拷语义正确**（拷后 甲=1 保持拥有 / 乙=1 独立——无共享）；
> ②**回归**：全量 E2E **116_Feature2_链表栈队列元素析构 = 0xC0000374**（1 处）→ **按「缺陷零容忍」立即回退**
> 三文件；回退复验：116 **PASS** ✓ + 五形态回基线（2/2/2/2、形五 0）+ 单测 1315 ✓——**源码零净变更**；
> ③**发现四则**（下轮基石）：**存储形态真值=指针槽**（IR 铁证：StorePtr/LoadPtr 访问、NewObject 构造——
> 137-a 的「内联」推断被推翻，`类型大小` 不足以断形态）；**CN 引用参数约定=槽地址**（callee 一层 Load 得
> 对象——传对象指针=多解一层崩，对照工作代码 probe_ctor IR 交叉验证）；**浅拷共享下不能只加释放**（形四双删
> 0xC0000374 二次实证）；**元素释放协议**（`emitContainerElemFreeFor` + DeleteObject 组合=局部消亡模式）；
> ④**既有缺陷 2 项登记**：声明初始化（`货架 乙 = 甲;`）varSlots 疑云（1 槽 vs 4 槽）；
> `emitStructWholeAssign` 对「类 canon + 指针槽目标」的 CopyStruct 误用（越界写）；
> ⑤**路线修正**（`plans/020` 第五十节）：最小闭环递进——单一容器（向量）+ 单形态（形一）→ 形三 → 形四 →
> 形二（含 varSlots 修）→ 扩容器族 → v2 同构；**每步探针+全量 E2E 后再进**。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
