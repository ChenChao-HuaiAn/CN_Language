# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 92-a 宿主浮32 面缺陷族根治（H2 销项）+ v2 侧浮点宽化面分叉收口（2026-09-13 第九十二轮，
> 深度机 linux-x86_64）：91-a 立案的「宿主浮32 面缺陷 H2」本轮专项根治，探针 P92 十三件实测
> ——**宿主四缺陷**：①二元算术结果类型恒 `f64`（「阶段一简化」）：`浮32 op 浮32` 的 movss
> 装载（xmm 高 96 位清零）+ addsd 把 f32 位模式当 f64 读（值域退化 ~1e-315；`甲/乙`=0.994152
> =位模式比），且结果按 8 字节存 4 字节槽（**栈溢出**）；②IR 调用实参**无条件 f32→f64 提升**
> （注释「Win x64 ABI 浮点参数按 xmm 传双精度」=错误假设——被调方按 f32 读 xmm 低 32 位 =
> double 位模式低 32 位 = 参数恒 0）；③linux_x64 `emitPtrLoadStore` 宽度前缀写死 `qword ptr`
> ——f32 发射 `movss` + qword **非法汇编编译失败**（win-x64 同函数本已按类型分派）；④自增/
> 自减结果类型硬编码 `"ptr"`——浮点走整型加法（位模式 +1 只改尾数最低位，`甲++` 静默失效）。

> **v2 侧三处分叉同轮收口**（移植纪律 10「两侧同缺一次修全」）：混合运算「单侧浮点取该侧」
> 把整型**窄化到 f32**（有损，违反规范「仅允许无损或宽化」；`甲+16777217` 宿主 16777218.5 /
> v2 16777218.0）；`发射自增自减` 硬编码 `T_整64`（自增同款失效）；赋值/初始化与拼接路径缺
> 浮点宽化（`浮64 宽 = 浮32(1.5);` 读回 0、`"值=" + 浮32变量` 恒 0.000000）。

> 修复：宿主 IR 层浮点类型精确化（新增 `floatCommon`：f32 op f32 单精度运算、跨类型统一 f64
> 保持=整32 无损转安全方向）+ 实参按形参类型分派（浮64 提升/浮32 原样/变参未知提升）+ 访存
> 宽度按类型分派 + 自增全链按操作数类型（含浮点增量 1.0）；v2 混合浮码升 f64 + 「码 ≠ 公共
> 浮码统一插入转换」+ 自增类型码分派（新 `发射自增增量`：浮64 常量 1.0 经 IR_类型转换 降精度
> =**零后端改动**）+ 新 `浮点宽化存储` 助手（声明初始化/赋值两存储点共用）+ 拼接浮32 侧先宽化。

> **三方案呈报与裁决留痕**（用户令「性能第一、安全第二、参考 Rust」）：**甲（采纳）**=IR 层
> 浮点类型精确化 + ABI 修正（零额外转换指令=**性能最优**；消除 8 存 4 栈溢出 + 严格 IEEE754
> 单精度语义=Rust f32=**最安全**）/ 乙=统一提升 f64 计算（C `FLT_EVAL_METHOD=1` 风格：转换
> 指令税 + 中间精度污染）/ 丙=收窄为仅存储转换打印（功能缺失，规格书有「加浮32」变体）。

> **防线 E2E 251**（移植纪律 9 **第二十个**对照用例）：浮32 语义面全链八形态折 **255**
> （算术四则+复合赋值 1 / 混合宽化 2 / 比较+负号 4 / 转换四向 8 / 参数返回 ABI 16 / 结构体
> 字段 32 / 自增自减 64 / 大整数混合精度 128）；**反证三件全命中**（A 宿主 D1 回退→宿主 8
> 〈形态操作数经 `折半()` 打破 O2 常量折叠强化后：①~⑧除④全捕获；强化前 207〉；B 宿主 D2
> 回退→宿主 239；C v2 宽化存储回退→v2 247）。**验证链全绿**：清场重建零警告（宿主 md5
> `f4f6999049ed98efda99e7b3b02c22af`）+ 单测 **1306/1306** + E2E **278 用例 274 过 / 0 失败 /
> 0 未实现 / 4 跳** + 锚定链固定点 **361684 行**（91-a 后 360800）两代逐字节一致 + 对拍
> **34/34**（DIFF=0）+ 释放路径两侧对照；v2p md5 `99e97e9ea8627148663988c2c1ff8d6e`。

> 新登记：**v2 数组声明语法缺口**（`浮32[2] 表;` 解析报「语句缺少分号」——节点_下标/后缀
> 设施在，缺声明位语法）；lessons 新增预防 **108~112**（结果类型字段 vs 操作数类型同源校准 /
> 访存宽度前缀姊妹后端对照 / -O0 探针与防线用例防折叠 / 反证备份清单全覆盖 / 隐式转换方向按
> 无损逐组合审查）。详见 `plans/020` 第三十二节与 git 提交。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
