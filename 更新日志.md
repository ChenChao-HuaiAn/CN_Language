# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 340-a 开发轮 D38 批3b=325/347/363 收官·2026-09-18·基线 620831b）

- **325 模块循环导入**：编译文件 的 已加载 值编码（1 完成/2 加载中）——原递归前标记=循环被去重静默吞；在途再入→「[parse] 模块循环依赖: 路径」+语法错误中止（宿主措辞对齐）；失败/完成双出栈点。
- **347 类私有可见性**：解析类声明 段可见性记录（显式 私有:/公开:·缺省公开维持存量）+字段节点子起点位+类节点索引表（语义分析）+检查成员访问表达式 设防（私有字段跨类访问拒·宿主措辞逐字对齐；构造器内裸名赋值放行=零豁免设计）。
- **363 外部函数带体**：顶层解析兜底静默吞「外部」（该拒不拒不扩面修复）——外部 函数 分支+解析外部函数声明（带体=「[parse] 外部 函数 声明不能有函数体」+跳到体尾；签名不登记=单池合并语义）。
- **D38 收官**：17 项防线经 326-a 批1/329-a 批2/339-a 批3a/340-a 批3b 四轮全闭合（16 拒/1 缺=317 销账锚）。
- **验证**：d38_scan 16 拒/1 缺+三探针宿主措辞逐字对齐+零误伤反证（p1~p3 过/p4 拒）+v2 树零错误编译+单测 1353/1353+全量 E2E（数字见提交信息·含 78/79 自举链）。

**门禁**：快速+全量门禁+双文档门禁；integrate.py 集成（协议v2）。
## 深度机 linux-x86_64 节

## 功能完善总结（✅ 375-a B1 化石改写·2026-09-19·基线 2373afa）

- **B1 甲落地（用户裁决 2026-09-18）**：185_v2/125_v2 化石用例布尔语境改写——185_v2 比较结果改存布尔变量+如果累加校验（退出码=14 锚定·13≠14 差分锚保留）；125_v2 短路段 6 行 `整数!=0` 布尔语境化（行55 短路本质/行62 比较链原样保留）。
- **两例转双编译对照通道**（v2闭环.txt→双编译对照.txt·宿主+v2 同测）——呼应用户 C20 双测方向，根治「v2 闭环无宿主对照→与宿主规则脱节」化石成因。
- **验证链**：零警告构建+单测 1353/1353+全量 E2E 444=441/1/2（唯一红 426=T50 基线既有·零新增红·两化石转绿）+反证（同编译器旧源码精确复现布尔拒绝）+strict+矩阵 83/83。
- 零 src 写集；触 tests/**→三平台 try-build（集成挂起等回签）。

**门禁**：check_handoff/check_progress_sync 绿；推 gitcode（分支）。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
