# 功能完善总结

> 84-a/84-b/84-c v2 结构体字面量作实参 + 字段校验 + 嵌套字面量（2026-09-12 第八十四轮，深度机 linux-x86_64）：
> 81-a 登记的 v2 缺口销项——v2 原仅在「解析期脱糖」三位（声明/赋值/成员赋值）支持
> 结构体字面量，**表达式/实参位解析失败**（`[parse] 语句缺少分号` 中止）。本轮以
> **表达式值节点 + 临时槽生成**补齐（复用 82-a 槽分配纪律与实参清理）。

## 一、缺陷实证（探针 P23）

| 形态 | 宿主 | v2（修复前） |
|---|---|---|
| `收(盒子{ 名 = "…", 号 = 6 })`（普通函数实参） | ✓ 6 | ✗ `[parse] 语句缺少分号 ';'` → 中止（不产 asm） |
| `表.追加(盒子{ 名 = "…", 号 = 7 })`（容器方法实参） | ✓ 7 | ✗ 同款 |

**根因**：v2 的结构体字面量是**解析期脱糖**（`解析构造字段赋值们`：裸声明 + 逐字段成员
赋值）——只在**声明初始化位/标识符赋值位/成员赋值位**三位接入；`解析原子` 的标识符分支
只处理 `名<…>(` 构造调用，遇 `标识符 {` 落入「语句缺少分号」。

## 二、实施（v2 树 4 文件）

- **节点协议**（`IR/IR常量.cn`）：`节点_结构体构造 = 43`（文本=类型名ID、附加=字段数、
  子区间=[值表达式与字段初值]**整段**）+ `节点_字段初值 = 44`（文本=字段名ID、附加=字段
  偏移、子区间=值表达式区间）——**不复用 `节点_成员`**（其 `子起点`=对象表达式，语义冲突）。
- **语法**（`语法/解析表达式.cn` `解析原子`）：`标识符 {` 且 `查结构体类型 > 0` → 逐字段
  解析（后缀序）+ 收尾构造节点；未闭合/字段名非标识符走语法错误通道。
- **IR 生成**（`IR/生成表达式求值.cn` `生成结构体构造`）：临时槽（**82-a 槽分配纪律四条**）
  + 零初始化（字节逐 8，未赋值字段=0 空安全）+ 逐字段（值区间 `生成表达式` 求值 → 字段
  地址 + 指针存储（宽度=查字段宽度码））+ 登记 `结构体返回临时们` → 压槽地址（**实参位置
  由 82-a `清理调用实参临时` 调用后释放串字段——零新清理机制**）。
- **主循环区间跳过**（`IR/生成表达式.cn`）：`收集构造区间` + 主遍历跳过构造值区间
  （**必须**——否则值表达式被主循环与生成函数**重复求值**；首版实证「调用被调对象非函数名/
  方法」）；跳过终点设计=构造节点池索引（零成本衔接）。

## 三、防线与反证实证

- **E2E 238_双编译对照结构体字面量实参**（移植纪律 9 **第十个**对照用例）：八形态（普通
  函数实参/容器实参/混合实参/字段值=调用返回/循环 20 万轮/删除·设置组合/双字面量实参/
  残留归零）折 **255**，两侧一致；供给=向量<盒子>。
- **反证实证**：注入「解析原子 字面量分支失效」→ 238 v2 侧**解析失败**（节点 581→319、
  `[parse] 语法错误 36 个` 中止）→ 用例 FAIL；恢复后两侧 255 ✓。

## 四、验证链（本轮终值）

清场重建**零警告** + 单测 **1306/1306** + E2E **265 用例 261 过 0 失败**/4 跳（+238；
其余 260 过=零回归）+ 锚定链固定点 **323750 行**（新口径，上轮 320424；+3326=v2 侧语法/IR
新代码）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照 ✓。

## 五、84-b v2 字面量字段校验（本轮后续，同权记录）

- **缺陷**：84-a 新增的「结构体字面量作实参」（表达式位）**未带字段校验**——宿主与 v2
  旧脱糖位（声明/赋值/成员赋值三位）均有 **Rust E0062（重复字段）/E0063（缺字段）= 编译
  错误** 同构纪律；新路径缺失 → 重复字段静默取后值、缺字段静默留零值（**静默语义偏差**）。
- **实施**（v2 `语法/解析表达式.cn` `解析原子` 字面量分支）：重复校验（已见向量查重）+
  穷举校验（`构造字段数`/`构造字段名` 逐序比对）——**与旧脱糖位逐字同诊断文本**；零新机制。
- **防线**：**E2E 239_v2_字面量字段校验**（v2 闭环负测：单文件两错误形态 → v2p 非零退出 +
  不产 v2asm.s + 三条锚行固化）；**反证**：注入「两校验同时失效」→ 239 FAIL（v2p 退出码 0
  静默放行），恢复后 PASS ✓。
- **验证链（本轮终值）**：清场重建零警告 + 单测 **1306/1306** + E2E **266 用例 262 过
  0 失败**/4 跳 + 固定点 **324373 行**（84-a 后 323750；+623）逐字节一致 + 对拍 **34/34**
  （DIFF=0）+ 释放路径两侧对照 ✓。

## 六、84-c 嵌套结构体字面量（本轮后续，同权记录）

- **缺陷实证**（探针 P24~P29，嵌套字面量逐层验证）：① 嵌套结构体字段值按**宽度存储**
  （只写指针 8 字节 → 读取按内联偏移=垃圾：宿主 10 / v2 140735369840171）；② 嵌套结构体
  字段**未登记资源字段**（注释称支持但条件未收 → 嵌套串无释放面，残留 3）；③ 外层构造
  遍历**把内层字段初值当自己的字段**（后缀序 → 二次求值 + 按内层偏移写外层槽，残留 1）。
  另揪出两枚跨侧缺陷：**宿主三层嵌套字面量丢最内层字段**（`丙.d.b.a` 读 0，写入均已发射
  =数据正确性级）与 **v2 纯标量嵌套结构体局部引用 `类型$~类型`**（供给无符号 → 链接失败）。
- **修复**（v2 三项落地）：① 字段类型分派（结构体字段→`IR_结构体拷贝` 整体拷贝=内联值语义；
  标量/字符串保留宽度存储）；② 构造遍历跳过嵌套区间（`收集构造区间`+`区间终点`）；③ 区间收集
  只留最外层（防主循环取更短的内层区间致内层先行求值）。**④ 嵌套字段资源登记（类别 2）= 回退**
  （`资源字段数` 多处消费 → 纳入后 E2E 190 等缺符号回归 → 回退 + 登记下一轮；嵌套串维持泄漏方向）。
- **防线**：**E2E 240_双编译对照嵌套字面量**（移植纪律 9 **第十一个**对照用例：八形态折 **255**
  两侧一致；嵌套形态只校验语义/内容=残留登记边界）；**反证实证**：注入「嵌套区间跳过失效」→
  240 v2 255→**52**，恢复后 255 ✓。
- **验证链（本轮终值）**：清场重建零警告 + 单测 **1306/1306** + E2E **267 用例 263 过 0 失败**/4 跳
  + 固定点 **325982 行**（84-b 后 324373；+1609）逐字节一致 + 对拍 **34/34**（DIFF=0）+ 释放路径对照 ✓。

## 七、84-d 成员链嵌套深度（本轮后续，**数据正确性级缺陷**，同权记录）

- **缺陷**（探针 P29/P30）：`深C 丙 = 深C{ d = 深B{ b = 深A{ a = 1 }, c = 2 }, e = 3 };` 后
  `丙.d.b.a` 读 **0**（应 1）、`收深(丙)`=5（应 6）；逐字段写读同款；二级/一级正常。
- **根因**：**宿主** `visitMemberExpr`（读取路径）的对象类型推导**只手写「一层嵌套」**
  → 三级链 `objSrcType` 退化空 → `decl == nullptr` → **字段读降级常量 0**（与
  2026-08-25/09-03 两次已修缺陷**同一范式**）；而**既有递归辅助 `memberObjStructType`
  早已存在且多处使用——读取路径未用它**。v2 天然递归=仅宿主。
- **修复（甲案，空值兜底）**：`if (objSrcType.empty()) { objSrcType = memberObjStructType(node); }`
  （一行级、零结构改动、零运行时开销）+ `memberObjStructType` 增强「裸指针对象统一剥一级」。
  乙（整段替换）因改动面大落选；丙（语义层 resolvedType 回填）登记后续。
- **防线**：**E2E 241_双编译对照成员链嵌套深度**（移植纪律 9 **第十二个**对照用例：八形态折
  **255** 两侧一致）；**反证实证**：注入「兜底失效」→ 241 宿主 255→**32**（⑥ 二级/一级
  对照保留=精准命中三级面），恢复后 255 ✓。
- **验证链（本轮终值）**：清场重建零警告 + 单测 **1306/1306** + E2E **268 用例 264 过 0 失败**/4 跳
  + 固定点 **325982 行**（纯宿主改动=与 84-c 后一致）逐字节一致 + 对拍 **34/34** + 释放路径对照 ✓。

## 八、登记（下一轮候选）

- **B35 字段容器接收者**（残留 2）——涉「字段赋值语义 move vs 深拷」**语言级裁决**，
  **已呈报 plans/020 第十七节，待用户裁决**（阻塞项）；
- v2 结构体字面量补强：嵌套字面量字段值/作返回值（观察项）、字段重复与穷举校验（宿主
  E0062/E0063 同构纪律，v2 未拦=下一轮候选）；
- 物化性能优化（延迟物化 + 活跃性分析，可选）；未初始化槽 preFree 观察项；
- 跨机轮：arm64 复验与 E2E 234/235/236/237/238。
