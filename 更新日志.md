# 功能完善总结

> 77-a 借出视图生命周期检查（2026-09-12 第七十七轮，深度机 linux-x86_64）：plans/020 矩阵
> A21 格靶子「借出视图 × 容器移除（NLL/借用检查轮）」销项——用户令「继续后续任务；遵守
> AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案（性能第一、安全第二）」，三方案呈报后
> 实施**编译期检查器（零运行时开销）**，宿主 + v2 两侧落地；随后按用户令「全部实施。
> 尽善尽美。」把诚实边界中的可完善项逐项落地（扩展①跨函数别名窄面、②成员链接收者）。

## 一、缺陷（七形态，探针 77/77-2 实证，宿主侧内容损坏）

借出视图 = 字符串元素容器的 `元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取` 返回值
（容器内元素句柄的**浅拷**——容器拥有元素模型下与容器槽共享所有权）；容器失效点释放该元素后，
借出视图悬垂（UAF）：

| # | 形态 | 失效机制 | 探针校验位 |
|---|---|---|---|
| ① | 向量 元素 × 删除 | 被移除槽释放 | 0（内容损坏） |
| ② | 向量 读取(结果) × 设置覆盖 | 唯一持有者槽释放 | 0 |
| ③ | 栈顶 × 弹出 | 所有权移交调用方（双持有） | 0 |
| ④ | 队首 × 出队 | 同上 | 0 |
| ⑤ | 链表 头部元素 × 删除头部 | 被摘槽释放 | 0 |
| ⑥ | 映射 获取 × 删除 | 交换式删除释放被删项值串 | 0 |
| ⑦ | **跨作用域（容器先亡）** | 块内容器析构释放元素，外层借出仍用 | 0 |

对照（正确行为）：借出变量在块出口未被误判拥有（校验位 1）；借出→使用→容器修改
（顺序正确，NLL 活跃区间已结束）校验位 1。

## 二、方案裁决（三方案；性能第一、安全第二，Rust 经验）

| 方案 | 性能 | 安全 | 裁决 |
|---|---|---|---|
| **甲：编译期借出视图生命周期检查**（语义层来源登记 + NLL 顺序近似：引用活跃区间=绑定→最后一次使用；两形态同作用域失效/容器先亡） | **零运行时开销**（生成物零变化，纯编译期） | 编译期拒绝（Rust 借用检查器 E0502/E0597 语义同构） | ✅ 实施 |
| 乙：借出方法返回拥有副本（API 级） | 每次借出堆分配+拷贝；破坏容器视图语义（写回不可行） | 高 | ✗ 违背性能第一 |
| 丙：视图类型（指针+世代）+ 运行时检查 | 8→16 字节新类型（ABI 全链改动）+ 每次访问比较 | 最高（含跨函数动态） | ✗ 违背性能第一（登记为阶段 4' 潜在扩展） |

## 三、检查器设计（两侧同构）

- **借出方法（8）**：元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取。
- **失效方法（11，按容器头分派——同名「清空」语义按容器区分）**：向量 删除/设置；集合 删除；
  链表 删除头部/删除尾部/清空；队列 出队/清空；栈 弹出；映射 删除/设置/清空/释放内部数组。
- **不在面内（语义判定）**：扩容类（追加/插入/压入/入队/头部追加/添加——字符串元素是独立堆块，
  槽位搬移不使句柄失效）；向量 清空（仅计数归零不释放）；值语义元素容器（`向量<整64>` 等，
  元素() 返回深拷贝）。
- **判据**：失效点落在（绑定，最后一次使用）之间 → 编译错误；或使用点发生时容器已出作用域 → 错误。

## 四、实施

- **宿主（src/cn_compiler）**：新文件 `semantic/semantic_borrowview.cpp`（借出/使用/失效点登记 +
  函数级结算）；`semantic.hpp` 加状态；六处挂点（`visitCallExpr` 方法分支 / `visitVarDecl`
  初始化位 / `visitAssignmentExpr` 赋值位含复合赋值 / `visitIdentifierExpr` 读值根拦截点 /
  `checkFunctionBody` + `checkClassMethods` 结算，方法体保存-恢复嵌套安全）；变量身份 ID
  （declareVar 分配，同名遮蔽防误配）；`types::isStringElemContainer/isStringValuedMap` 上提
  共享（IR 层改委托调用——单一事实源）。
- **v2（CN语言编译器v2）**：新模块 `语义/借出检查.cn`（预扫描块区间+声明表 → 主扫描三类点 →
  结算两形态；**节点索引序代替源码行序**，v2 节点无行号）；`语义/包.cn` 加模块；
  `语义/语义检查.cn` 加导入 + `检查函数` 尾部挂点。

## 五、防线与反证实证

| 防线 | 口径 |
|---|---|
| E2E 223 / 223_v2 | 同作用域失效**六形态**负测（锚「容器失效点」；v2 侧 6 诊断 + 中止行固化） |
| E2E 224 / 224_v2 | 容器先亡**三形态**负测（块内/循环体内/嵌套块，锚「容器作用域结束后」） |
| E2E 225 | 宿主正测：合法**九形态**折校验和 **511**（含「借出未被误判拥有」） |
| E2E 226 | **双编译对照（移植纪律 9 第四个）**：八形态两侧退出码一致 **255**（零误伤同构） |
| E2E 227 / 227_v2 | 扩展边界负测：跨函数别名（调用点同源互斥）+ 成员链接收者（锚「借出视图」） |
| 单测 test_borrowview_check.cpp | **17/17**（负 7 + 正 10，含扩展①②四例） |

**反证实证（门禁有效性）**：① 宿主注入「借出方法识别恒假」→ E2E 223 失败；② v2 注入
「是借出方法名 恒假」→ E2E 223_v2 失败。两侧注入已恢复并复验（残留检测=0）。

## 六、全树零存量违规（零迁移成本）

- 作用域感知审计（v2 树/v1 树/stdlib/tests）零真命中；宿主编译 v2 树零诊断；v2p 编译 v2 树零诊断。

## 七、验证链

清场重建零警告 + 单测 **1306/1306**（1289+13+4）+ E2E **253 用例 249 过 0 失败**/4 跳
+ 锚定链固定点 **281093 行**（新口径，扩展后）逐字节一致 + 绑定自检 + 组件对拍 **30/30**（DIFF=0；
> 注意：对拍与锚定链不可并发——共享 work 目录会踩出 16/30 DIFF 假警，串行即一致）。

## 七A、第七十七轮后续扩展（用户令「全部实施。尽善尽美。」）

**扩展① 跨函数别名窄面（调用点同源互斥）**——探针 A 实证 `帮忙改(表, 借出)`
（被调函数删除元素后调用方借出视图读取长度 3 vs 期望 13=UAF）；实现：同一调用中
「容器引用键 C」与「来源为 C 的借出视图实参」并存即拒绝（宿主挂点=`checkExpr` 出口
单点覆盖全调用形态；v2=扫描的 节点_调用 分支 + 解析期调用实参末位表定位）。
Rust 对照：`f(&mut v, &v[0])` 被借用检查器拒绝。

**扩展② 成员链接收者**——探针 B 实证 `架.表.元素(0)` × `架.表.删除(0)`（长度 3 vs 13=UAF）；
实现：容器身份升级为**「容器引用键」**（标识符=名；成员链=基础名.成员路径文本——
宿主 `resolveContainerRef`+`buildPathText`；v2 `路径文本递归`+`接收者类型名ID` 经
查结构体类型/查字段类型ID 逐层推导）；诊断直接展示键（`架.表`）。

**③ v2 类方法体 = v2 编译器独立缺口（非本检查器边界）**——探针 C/D 实证：宿主侧类
方法体借出形态**已拦截**（checkClassMethods）；v2 侧连最简单的用户自定义类都不支持
（探针 D：`类 盒子 { 整64 值; }` → v2 报「未声明的类型: 盒子」）→ 登记为「v2 类支持
专项」（类声明→类型登记→字段表→方法体检查→借出检查挂载），下一轮实施。

**④ 值语义元素容器**：`向量<整64>` 等 `元素()` 返回值拷贝（无别名）→ 设计上不拦
（拦即误报）；门禁 225 形态⑤/226 校验位 16 守此边界。

**防线**：E2E **227/227_v2**（扩展边界负测：跨函数别名 + 成员链）+ E2E **225 十一形态**
（校验和 511→**2047**，新增⑩借出单独实参、⑪容器与无关串同调用=不误报对照）+ 单测
**13→17 例**。

## 八、诚实边界

- 跨函数别名：**调用点同源互斥已覆盖**（`f(表, 借出)`）；仍检不出=内联形态
  `f(表, 表.元素(0))` 与经结构体/全局中转到别的函数修改容器的形态。
- 成员链：**已覆盖**（键=基础名.成员路径）；下标不展开（`p[0].表`/`p[1].表` 同键保守合并）；
  v2 侧依赖字段表可解析（用户类不受支持时自然不覆盖=③）。
- v2 侧类方法体经借出检查=**待 v2 类支持专项**（探针 D：v2 对用户自定义类整体不支持）。
- 值语义元素容器不在面内；向量「清空」不在失效面（仅计数归零，泄漏面另计）。
- 探针留 /tmp/p77probe 与 /tmp/p77b（不入库）；反证注入副本已恢复。
