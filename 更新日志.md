# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百四十一轮 141-a：波 3 续进——链式族达成（链表/队列；四族全齐；全链门禁绿）**（2026-09-15，家机 win-x64；基线 383441e）：
> ①**stdlib 链式容器拷贝构造**（**链游复制**语义）：`不安全 函数 链表(链表& 其他)` + `队列(队列& 其他)`
> ——值表/下一索引按**已用槽数**复制（含空闲链状态：头/尾/数量/空闲头/已用槽数）；**链上游有效元素**
> 逐元素按 T 拷贝语义深拷；**空闲槽值不复制**（无效区，与 `__cn_chain_free_strings` 同口径）；
> ②**宿主扩族**：收集面 head 集合扩至 {向量, 栈, 链表, 队列}（收集 + 写入位拦截同步）——**四族全齐**；
> ③**探针**（链式场景刻意含空闲链状态）：链表 形一 0 / 形二 0（追加2+删头+追加1 **拷贝后大小=2** ✓）；
> 队列 形一 0 / 形二 0（入队3+出队1 大小=2 ✓）——四族全部通过；
> ④**门禁全绿**：E2E **290/292**（**116_Feature2 PASS**——链式族启用后无回归，138-a 回归面在「收窄+逐族」
> 策略下完全消除）；单测 **1315/1315**；**锚定链 406097 逐字节一致**；对拍 **44/44**；运行级 **3/3**。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
