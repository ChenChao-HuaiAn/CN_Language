## 功能完善总结

**时间**: 2026-08-13

**主题**: CN 语言相对 C++ 表达能力增强规划（P0~P3 全覆盖路线图写入 plans）

### 一、背景与诉求

用户反馈：CN 语言相比 C++ "太简单"，希望支持 `打印("你好，世界" + 123)` 这类字符串 + 数值拼接打印，并要求全面检测能力缺口，将增强写入 plans 各文件。

### 二、能力缺口检测结论（Ask 子任务产出）

CN 语言内核不弱（位宽整型到 128 位、指针/数组/结构体/枚举/联合体、函数指针、字符串 18 API、5 优化 Pass），"太简单"源于：
1. **语法层缺"甜点"**：无运算符重载、无字符串+数值隐式拼接、无格式化打印、无三元表达式；
2. **规格书已规划未实现**：OOP、错误处理、模块系统、标准库是"纸面能力"。

### 三、本次写入 plans 的增强项（分优先级）

**P0（速效语法糖）**：
- 字符串 `+` 数值隐式拼接（数值→字符串自动转换）
- `格式化()` 打印函数（sprintf 风格）
- 三元条件表达式 `? :`

**P1（兑现规格书已规划 + 函数特性）**：
- 函数重载、默认参数、强制类型转换、lambda
- OOP 全套、错误处理（结果/可选）、模块系统（原规格书已规划）

**P2（补 C++ 有而规格书无的）**：
- 运算符重载、泛型/模板、类成员增强（静态/常量/友元）
- 标准库输入 API、数学函数、文件 IO、条件编译

### 四、文档变更清单

| 文档 | 变更 |
|------|------|
| `plans/001 CN语言编译器设计规格书.md` | 3.7 补"数值→字符串"隐式转换；4.4 补三元运算符/运算符重载说明；4.5 补三元优先级；5.2 移除类型别名；5.3 补函数重载/默认参数/lambda/强制转换；5.6 补运算符重载/静态成员/常量成员/友元/泛型模板；10.3 补跨类型拼接语义；10.6 补格式化与IO API；关键字表 54→58（新增友元/泛型/自动/常量，不引入别名/运算符保留字） |
| `plans/002-阶段2-核心语言.md` | 新增 Task 2.9 语言表达力增强、Task 2.10 函数特性增强 |
| `plans/002-阶段3-OOP与错误处理.md` | 新增 Task 3.7 运算符重载、Task 3.8 泛型模板、Task 3.9 类成员增强 |
| `plans/002-阶段6-标准库.md` | 补 Task 6.2 输入、Task 6.3 数学函数、Task 6.5 格式化；新增 Task 6.6 条件编译 |

### 五、用户批阅修正（三点）

1. **不引入 `运算符` 保留关键字**：运算符重载采用上下文关键字识别（`函数 运算符+`），非保留字，不占用用户标识符命名空间；
2. **不引入 `别名` 关键字与功能**：类型等价替换由现有 `整数`/`小数` 内置别名覆盖，用户自定义别名暂缓；
3. **打印换行语义**：`打印` 自动换行，`打印行` 不换行（修正原规格书描述）。

### 六、待办

- 本批次仅完成**规划写入**，代码实现待后续按 Task 顺序执行（Task 2.9 为下一实施起点）。
