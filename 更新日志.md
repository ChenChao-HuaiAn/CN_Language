## 功能完善总结

> 87-a 静态变量全链路根治（2026-09-12 第八十七轮，深度机 linux-x86_64）：85-a/86-a 登记项 ②
> （静态结构体，第一优先候选）**销项**——但按「探针实证优先」铺开静态变量形态矩阵后，缺陷面
> **比登记项更宽**（两侧 × 全形态 × 全链条）：`.data` 槽尺寸、标量初值、声明位、语句位、
> 成员位、读取基址、实参、返回、重复赋值 drop 共 **10 处**（宿主 5 + v2 5），宿主基线校验位
> **0 全败**（含越界写段错误），本轮**全链根治**（两侧同构），防线四件套（E2E 246/247 + 反证
> 五件）就位。

> ① **缺陷面（探针 P50 系列实证）**：
> **宿主**——H1 `.data` 静态槽恒 8 字节（linux_x64/arm64）→ 结构体 16/24… 字节整体写
> **越界相邻 .data 符号**（P50b 宿主 rc=139 段错误；x64 侧已有尺寸逻辑=三后端分叉）；
> H2 静态**标量初值被无条件忽略**（恒 `.quad 0`——`静态 整64 x = 11111` 读出 0；布尔/字符/
> 字符串同，仅浮点幸存=校验位 8）；H3 语句位 `静态盒 = <结构体表达式>` 落 8 字节 StorePtr
> （丢值 + 越界）；H4 声明位初值零初始化（用户写初值得零值）；H5 静态结构体字段读取基址落
> 无效槽（`memberObjStructType`/`lvalueAddress` 缺静态回退 → 字段读降级常量 0）。
> **v2**——V1 槽恒 8 字节（三后端）；V2 静态结构体**被降级为函数局部栈槽**（读取恒
> `IR_静态加载` 值语义 + 赋值位无静态目标 → 跨函数不共享、静态槽零写）；V3 布尔/浮点/
> 字符串初值折叠失败静默落 0（探针：布尔=假/浮=0/串=[0]）；V4 静态字符串赋值无 drop 旧值
> （循环 300 次残留 **+600**）+ 借用来源共享句柄；V5 成员位字段 preFree 判据只认「父∈资源
> 字段名单」→ 静态字段重复赋值泄漏。

> ② **实施（性能第一、安全第二；两侧同构）**：
> **宿主**——`ir_decl.cpp` 静态登记扩展（结构体/字符串/字符字面量/兜底表达式）+ 新
> `emitStaticInitsAtEntry()` 三类分派（结构体=**原地逐字段构造** `emitStructInitTo`〈零临时槽
> 零多余拷贝〉/其他表达式=整体拷贝〈调用返回浅拷接管〉；字符串=求值 + 来源分级归一化；
> 类容器=P3-8 指针槽）；`ir_expr.cpp` 静态赋值分支新增「结构体整体值写」（preFree 恒真——
> 静态槽长期存活，重复赋值当场 drop；字面量=原地构造/调用=浅拷/其余=深拷）+「静态字符串
> 赋值」（drop 旧 + 来源分级）+ 静态标识符**读取返回地址语义**（结构体）+ `emitStructWholeAssign`
> 静态源 + `memberObjStructType` 静态回退；`ir.cpp` `lvalueAddress` 静态分支（静态左值地址=
> 符号地址）；codegen 三后端（槽按 `typeSizeOf` 取整 8 倍数 + 标量初值直存 + 每槽
> `ALIGN 8`/`.align 8`/`.align 3`）。
> **v2**——静态表扩展（初值区间/需注入 + 查询）+ `生成表达式`（结构体→`IR_静态地址` +
> `T_对象` 登记；类型码按静态类型表分派〈字符串打印/拼接正确〉；两处成员基址静态分支）+
> `生成语句`（`发射结构体目标地址` 助手 + 字面量右值分支 + 静态 preFree〈预计算避开实参位
> 短路〉+ `归一化静态串赋值`）+ `IR生成` 类型解析静态级 + `生成函数` 注入扩展 + 三后端槽尺寸。

> ③ **防线（E2E 246/247 + 反证五件）**：**246_双编译对照静态变量全链路**（移植纪律 9
> **第十六个**对照用例：八形态〈①静态标量初值 ②结构体声明位 ③语句位·跨函数 ④整体赋值·跨块
> ⑤按值实参 ⑥返回全局静态 ⑦嵌套成员链 ⑧哨兵完整性 + 循环 2000 轮残留归零〉折校验和 **255**
> 两侧一致）+ **247_静态变量初值与字符串赋值全链**（宿主单侧六形态折 **63**：字符初值 +
> 字符串来源分级〈字面量驻留零分配/拥有返回接管/借用来源复制+drop 净零〉+ 重复赋值残留归零 +
> 跨函数写读 + 成员位重复赋值 + 自赋值 s=s）；**反证五件全部精准命中并已恢复复验**（A 宿主
> 槽尺寸回退 → 246 宿主 **-11**；B 宿主标量初值回退 → 247 校验和 **62**；C v2 读取回退 →
> 246 v2 **-11**；D v2 字段 preFree 回退 → 246 v2 **127**；E 宿主静态串 drop 回退 → 247
> 残留 **500**/校验和 **57**）。

> ④ **验证链**：清场重建**零警告**（GCC `-Wall -Wextra -Werror`）+ 单测 **1306/1306** +
> E2E **274 用例 270 过 0 失败**/4 跳（+246/247；其余 272 过=**零回归**）+ 锚定链固定点
> **334821 行**（新口径，86-a 后 330412；+4409=v2 侧 87-a 代码）两代逐字节一致 + 运行级 3/3 +
> 对拍 **34/34**（DIFF=0）+ 释放路径两侧对照。

> ⑤ **诚实边界与新登记**：**v2 实参位短路栈失衡**（`f(a || b)` → 生成调用被调错位 →
> `[ir2] 调用被调对象非函数名/方法` + 运行时错误6；**既有缺陷**，由 v2 树自编译暴露）——
> 栈基线净化尝试**未解决即回退**（残留在本层之外，不做半吊子修复），87-a 调用点改预计算布尔
> 避开（更清晰写法、无性能损失），**登记为下一轮第一优先**；v2 静态字符语法限制、v2 打印浮点
> 既有边界维持；多文件静态初始化仅入口模块注入（与类容器静态既有同限制）；静态槽不登记自动
> 释放（进程生命周期，Rust `static` 同款，退出不释放=设计口径）。
> **文档五件套**：plans/019（跟踪表第八十七轮行）+ plans/020（第二十八节 87-a 全节）+
> lessons（87-a 段，权重 8，预防新增 87~91）+ 本文件 + HANDOFF。
