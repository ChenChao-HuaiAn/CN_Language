## 功能完善总结

### 2026-08-24 运行时内存管理重构（tracked注册表方案）

#### 修改内容

1. **`src/runtime/io_api.cpp`** - 核心修改：
   - 添加 `TrackedNode` 链表结构（`ptr` + `next`）
   - 添加 `trackedRegister()`/`trackedUnregister()` 链表管理函数
   - `cn_alloc`/`cn_realloc`/`cn_free` 恢复 `std::malloc`/`std::realloc`/`std::free`（正常 RAII）
   - `cn_alloc_tracked` 分配时注册到链表
   - `cn_free_tracked` 从链表移除并 `std::free`
   - `cn_realloc_tracked` 更新链表中的指针
   - `__cn_object_new`/`__cn_object_delete` 恢复 `std::malloc`/`std::free`
   - 实现 `__cn_alloc_reset()` 遍历链表逐个 `std::free`，然后清空链表

2. **`src/runtime/time_api.cpp`** - `std::malloc` 改为 `cn_alloc_tracked`

#### 背景

78_chain_build 编译 5 大组件时 OOM（12.3GB 内存峰值）。根因：`__cn_alloc_reset()` 头文件声明了但运行时 `.cpp` 中未实现，`内存::释放全部()` 调用了未定义的函数。

#### 方案演进

1. **arena 统一分配方案（已废弃）**：让 `cn_alloc`/`cn_realloc`/`__cn_object_new` 全部使用 arena bump 分配，`cn_free` 变空操作。结果：内存峰值降到 193MB，但 arena 释放后向量数据数组失效，段错误。
2. **tracked 注册表方案（当前）**：`cn_alloc`/`cn_realloc`/`cn_free` 保持 `std::malloc`/`std::realloc`/`std::free`（正常 RAII），`cn_alloc_tracked` 注册到链表供 `内存::释放全部()` 批量释放。

#### 当前状态

- **单元测试**：60个内存/运行时/对象/字符串测试全部通过
- **78_chain_build**：内存峰值 193MB（之前 12.3GB），运行时间 3秒（之前 2分34秒），但第三次 `内存::释放全部()` 段错误
- **段错误根因**：`cn_realloc_tracked` 的链表更新可能有 bug（`std::realloc` 内存复用导致重复节点），待在 Win10 x86 环境继续调试

#### 设计决策（学习 C++/Rust 成功经验）

- C++ `std::vector` 扩容时用 `std::allocator`（底层 `new`/`delete`），旧块在扩容后立即 `delete`
- Rust `Vec<T>` 扩容时用 `alloc::realloc`，旧块在 `realloc` 内部释放
- 两者都不需要"批量释放"--每个对象独立管理自己的内存
- CN 语言应保持 `std::malloc`/`std::realloc`/`std::free` 的正常 RAII 语义，`内存::释放全部()` 仅作为字符串等无 RAII 类型的兜底批量释放
