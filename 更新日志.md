# 更新日志

## 功能完善总结

# plans/017 呈报①方案A 第二轮：v2「否则如果链双重生成」根治 + v2 IR 净化 56%（2026-09-06 深度系统 x86_64，用户双方案A 裁决：v2self 专项 gdb 深挖 + 重载消歧机制层）

## 一、呈报②补根治（纪律重贴后合规收口，b1c7f7d）

v2 win 发射器除/余 `xor rdx,rdx+idiv` 负被除数 #DE——修复=cqo+idiv（win asm 两处 cqo 形态实证+零回归；win 运行级验证待家机=诚实边界）。

## 二、呈报①方案A 深挖：第 6、7 枚缺陷根治（本轮主战）

- **【根因级】第 6 枚：否则如果链双重生成**（IR生成 生成语句 预扫描）：
  预扫描只收集首层体块，「否则如果」的嵌套如果根未被跳过——主遍历与外层
  生成如果 递归各生成一遍（chain 探针 4 层链实测 7 次生成如果，字段值序列
  63,52,63,41,52,63,30 与递归结构精确吻合）。**v2 IR 自源头坏的真相、历史
  冒烟全假绿**（重复块恰为幂等赋值时行为不变）。修复=沿「子终点」整链收集
  嵌套根+内层真块。**效果：主.cn asm 418388→183238 行（IR 净化 56%）**，
  13 组件 asm 普遍减半（IR生成 332204→114460），13 组件冒烟全绿。
- **第 7 枚：限定内置判定的 字节位置 依赖**：代码生成共用.cn 的函数在
  IR生成.cn 不可见（依赖环）——IR生成.cn 冒烟「未声明: 字节位置」2 错实证；
  修=内联首冒号扫描。

## 三、剩余层（呈报①方案A 进行中，已收敛到具体面）

v2self 内置回归仍崩于 映射$链查找——mapref 最小探针（映射& 引用参数+循环
设置）复现：灌 的 IR 中「表」读取走普通 IR_加载×2（引用分流未触发=引用槽
登记 miss）+ this 对 i 的值做指针加载（求值序错位）——「容器& 引用参数的
方法调用」路径缺陷（下轮继续）。**v2 产物从未被真实运行过，首跑逐层暴露
存量缺陷符合预期规律。**

## 四、观察项（新立案待查）

- 宿主对 mapref 形态（映射& 参数与 映射<>() 构造共存的文件）报「无法将
  未知 隐式转换为 映射」——宿主「引用参数类型解析×构造调用」交互缺陷。
- 宿主「表.大小() 赋 结果<整64,整32>」报「未知→结果」——映射& 对象的
  方法返回类型推导失败（同族）。

## 五、验证与文档

- 门禁全绿：构建零警告+单测 **1240/1240**+E2E linux-x86_64 **156 用例
  129 过/0 败/27 跳过**。
- 冒烟：13 组件全绿（exit=0+sem5=0+asm 产出）；探针矩阵（结构体返回/读
  文件/所有权转移/映射直用/9 参+结构体第 2 参）v2 编译运行全对。
- 文档：plans/017 第九节+plans/014 状态表行+lessons 权重 10 条目（双重
  生成）+HANDOFF。
