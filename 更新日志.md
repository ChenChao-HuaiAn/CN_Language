# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结
<<<<<<< HEAD
> **第一百八十轮 180-a：甲通道实装——方法体内容器字段直接赋值深拷（uF 根治）+ 第一百四十九轮 149-a 补记（IR diff 专项定位）**（2026-09-15，家机 win-x64）：
> ①**149-a 定位**（源码零改动，详见 plans/020 第六十八节）：直接 diff 148-a 遗留 `target/p149win/ir_{base,fix}.txt`
> + `cn ir` 补测调用点——**三铁证**：类引用参数实参=源槽地址（二级·138-a 发现二一致）／基线两层求值=正确匹配／
> 148-a 一层修复=读相邻槽（chain 拷贝=0 精确复现）；**uF 真崩因=拷贝构造内容器字段赋值指针共享（浅拷）→ 出口双重
> 释放 0xC0000374**；★146~148-a「求值多解一层」系误诊（三轮回退正确）。教训入 lessons 149 段（补记）·预防 166~168。
> ②**180-a 实装**（单点 `ir_oop_field.cpp` `handleClassFieldAssign`）：方法体内无前缀字段赋值（隐式 this 形态 a）
> 的**拥有型容器字段深拷**——对齐形态 b（memberClassFieldAssign，139-a 已有）：四族收窄（向量/栈/链表/队列）+
> 构造内初始化豁免（currentMethodIsCtor_ 跳过 preFree）+ 源分级（构造调用接管/左值 NewObject+拷贝构造〔实参=源槽
> 地址〕/防御直存）。修复后 `表 = 其他.表` 生成 新建对象+拷贝构造+存槽 三指令，零共享。
> ③**行为验证**：uF 0xC0000374 崩 → **`F ok 大小=1` rc=0**；uA~uE 全 rc=0；probe_chain 链表/队列拷贝=2、
> 残留全 0（146~148-a 退化面零复发）；五形态/栈族/计数探针全 0；116_Feature2 ×3 PASS（138-a 回归面零复发）。
> ④**门禁**：零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过 / 0 败 / 0 跳过**（含 78_v2/79_v2 自举链——**锚定链 fix_p≡fix_s 自洽**
> =新宿主 v2 编译产物零变化裁决）+ 组件对拍 **45/45** + 运行级 **3/3**。门禁七面全绿。
> ⑤**诚实边界**：uD（壳 d=a 结构体整体拷贝浅共享）未在本轮范围——emitStructWholeAssign 成员级级联深拷
> 登记 181-a 候选（uD 当前 rc=0 泄漏不崩，runtime 在册兜底，非阻塞）。
> ⑥文档六件套 + 看板收工。

<<<<<<< HEAD
<<<<<<< HEAD
## 深度机 linux-x86_64 节

## 功能完善总结

> 200-a D1 v2 侧续波——生成变量声明 164→前置分派+3 族方法 ≤100 达标（2026-09-15 第两百轮，
> 深度机 linux-x86_64；避让家机 183-a ir_fields 与单位机 arm64 后端写集）：
> **顺序段切片**（端点=完整块边界双断言——无 if-else 链「} 否则」双重身份问题）：`生成声明.cn`
> 生成变量声明标量通道三段提取——**分配标量声明槽**（槽/预扫已分配 整64&/布尔& 引用输出·
> 声明行留主函数 27 行）/**生成标量声明初值**（初值+字符串拥有化〔白名单八函数∪返回类型契约〕+
> 正32 截断/浮点宽化+存储 68 行）/**登记声明字符串RAII**（73-a 名单登记 17 行）；主函数收缩为
> 前置类型解析+六形态分派链+三调用 **74 行**。
> **快验两次拦截即修**（预防 185 三道防线流程价值实证）：①拼装清单注释行漏 `//` 前缀（解析失败
> 位置精确）；②族①体内引用 类型节点 未入参（审计脚本 decl_re2 误吞致 frees 漏报——**审计非完备，
> 单文件 compile 快验=最后防线**）。
> **验证链（v2 侧=锚定链重锚路径）**：单测 **1325/1325** + 全量 E2E **309 用例 307 过 / 0 失败 /
> 2 跳** + 锚定链**重锚 401575→401904 行**（fix_p ≡ fix_s 逐字节，md5 `f2f7d104…`）；多重集核验
> 缺失 0 行；**D1 计数实测 51**。
## 单位机 ARM64 节

## 功能完善总结

### 193-a F1/F2 长线清单重评（2026-09-15，单位机麒麟 ARM64 规划轮，基线 ecaf5a5；源码零改动）

**依据**：`plans/021` §3-F 明文「下次规划轮需逐条重评后销项或重排」（`plans/007` v1 时代清单，v2 时代待重评）。
**重评口径**：逐条**当场实测**（禁凭记忆/旧文档誊写）+ 双目标准绳（性能超 C++ / 安全近 Rust）。

1. **销项 1 项**：**F2-36a ARM64 原生自举闭环**——`78_v2_自举链构建` / `79_v2_自举闭环` 在 ARM64 **真跑通过**
   （188-a 全量 E2E 309 用例 307 过/0 败/2 跳；锚定链 arm64 **561364 行** `fix_p ≡ fix_s` 逐字节）；原条目另一半
   「性能≈1.04x」系 win-x64 口径 → 拆出 F2-36b 留在重排。
2. **重排 9 项**（各附首步/前置/验收口径）：
   - **F1-26 SSA Phi=半接线**（实测：-O3 产出 Phi，asm 仅 `// Phi节点（阶段一预留，无实际汇编）` ×3）——前置=F1-28；
   - **F1-27 全局值传播=保守子集**（常量 Store→Load）——前置=F1-26（真实数据流须 SSA 形式）；
   - **F1-28 arm64 寄存器分配=写死关闭**（`backend_factory.cpp:44`；win-x64 -O2+ 已启用=模块可用）——与 F1-26 同链；
   - **F1-29 pass 边界=三处显式保守**（i128 不折叠 `const_fold.cpp:66` / 浮点跳过 `algebraic_simplify.cpp:3`〔+0/-0/NaN〕/
     有符号除法跳过 `strength_reduce.cpp:71`）——放开需逐条语义证明（浮点依 91-a IEEE ordered 口径）；
   - **F1-30 DWARF/增量编译/ASan=三项均无**（`debug_info.cpp` 仅映射+预留；004 D-3 后置；ASan 未接入）——拆三项独立出轮，优先 ASan；
   - **F2-33 标准库=十模块**（核心/容器/时间/数学/算法/文件/系统/映射集合/字符串扩展/IO），缺迭代器/正则/线程/网络/随机数
     ——随机数/迭代器/正则=纯 stdlib 可独立推进；线程/网络=随 E1 并发安全立项；
   - **F2-34 包管理=TOML 子集 + 依赖查找，无 semver/锁/缓存/注册中心**——按需触发；
   - **F2-35 工具链=无 LSP/格式化/文档站**——独立专项（首步=诊断/IR 机器可读输出）；
   - **F2-36b arm64 自举性能基线未测**——首步=arm64 跑 `scripts/bench_self_host.py`；
   - **F2-37 规范精化**：设计决策库机制已成形（plans/001 唯一权威 + cn-language-spec 技能 + C16/A3/A4/A5/A8/D6 系列落盘），
     剩余「规范有·实装/验证缺」存量随各专项轮清零。
3. **优先级建议（待用户批阅）**：① F1-26+F1-28（Phi 落地 + arm64 寄存器分配·性能第一·同链）② F2-33 纯 stdlib 三项
   ③ F1-30-ASan ④ F1-29 ⑤ F2-36b ⑥ F2-35 ⑦ F2-34。
4. **文档**：`plans/021`（§3-F 重写为十项结论表 + §4.2 销项行 + §5 索引 + 首行戳双口径）/ `plans/007` 头部状态指引
   （历史原文保留、以 plans/021 为准）/ `plans/025`（本机节回填）/ `plans/019` §7.3 / 本日志 / HANDOFF / 看板（收工）。
   **源码零改动**（`git diff --stat` 仅文档）。

**诚实边界**：深水项（Phi 落地形态 / arm64 regalloc 正确性）以实测为准，未当场确证者标注「待开发轮核实」；
**优先级与是否实施属用户裁决面——本轮只出结论与建议，不擅自开工**（报告待批阅）；github 镜像本机无凭据未推。

### 196-a arm64 寄存器分配接线（尝试→按纪律回退；接线图谱与根因留档）（2026-09-15，单位机麒麟 ARM64，基线 cad4ea3；**源码零净变更**）

**依据**：用户批阅优先级①（F1-28 arm64 寄存器分配启用；`性能第一`）。

1. **接线全量实现过一版**（镜像 x64 语义）：读侧统一入口 `loadOperandToX`（已分配 → `mov`）；**多形态消费点**全覆盖
   （`operandText`/`resultText` 文本形态、结果落位 ≈25~40 处经 `storeResult` 助手、`emitLoadVirtualTo` 统一 id 取数、
   返回位 `emitTerminator` 的 `%vN`→槽文本转换点 + `emitEpilogue` 接受物理寄存器名）；序言/尾声被调用者保存对
   （x19~x28；隐藏返回指针场景 x19 已被占用 → forceDisable 同 x64 的 r12）；**栈参数基址 `stackParamBase()` 按压栈对补偿**；
   开关 `backend_factory.cpp` arm64 分支改 `regAllocOn`。
2. **接线期修掉 3 个既有缺陷**（与根因无关，已留档可复用）：① `resultText` 未感知分配（常量/转换类指令写槽、读者读寄存器
   → 值流分叉）；② `emitTerminator` 把 `%vN` 先转槽文本（返回位丢失寄存器驻留）；③ 批量替换脚本误将助手自身改为自调用
   → 无限递归（编译器编译 v2 全树段错误；ASAN 栈溢出定位）。
3. **探针验证（接线正确性局部成立）**：最小三例（常量算术/函数调用/循环）rc 全对；递归+循环大探针与 `--no-regalloc`
   输出逐字节一致；**访存指令 ldr/str 计数 93 → 33（-64%）**=性能通道方向正确。
4. **★阻断性根因（转专项轮 D7）**：E2E 3 例偏差（115 向量元素析构 `清空` 循环少析构 1 次 = 总析构 5≠6；235/245
   校验和 191≠255）→ 定位=**分配器 `reg_alloc.cpp` 活跃区间对「循环体内被重定义、跨块/跨迭代使用」的值判定过短**
   （同一物理寄存器被两处同时使用）；最小对照=简单循环正常、容器元素析构循环异常。
5. **处置=按纪律回退**（`plans/025` §三.2 §5 既定口径：「门禁暴露深层不兼容 → git checkout 回退，不留半截」）：
   **源码零净变更**（工作树干净）；探针正确 + 单测 1325/1325 复验通过；接线图谱、3 缺陷、四项教训全部留档。
6. **★新立案 D7**（`plans/021` §3-D）：**分配器循环携带值隐患核查**——x64 后端**同用该分配器且 -O2 默认启用**，
   须核查 win-x64 是否同族（其 E2E 306/306 通过=形态未触发或 wiring 规避）；处置=区间保守扩展 + 三平台复跑 + 反证探针。
7. **文档**：`plans/025` §三.2-7（接线图谱/根因/留档）+ `plans/021`（F1-28 改写 + **D7 新立** + 看板广播）+
   lessons 196 段（预防 179 批量替换三自检 / 180 接线多形态消费点 / 181 非标准构建产物隔离 / 182 线性活跃区间在回边上不充分）+ 看板收工。

**诚实边界**：本轮**未交付功能**（按纪律回退）；性能收益未兑现（待 D7 专项后重接）；github 镜像本机无凭据未推。
