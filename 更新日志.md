## 功能完善总结

**时间**: 2026-08-16（第四批检查完成）

## 自举前置条件检查 完成 ✅（plans/004 全项打勾）

按用户批复执行 A~E 五组检查（小样实验 + 实测 + 盘点），输出**自举前置条件检查报告**。

### 检查结果一览

| 组 | 结果 |
|----|------|
| A. 5大件完备性 | 小样通过（递归/结果+?/字符串/结构体遍历/枚举分发）；**发现 4 缺口**：① 无 字符串[i] 逐字节访问 ② 选择全分支返回不识别 ③ 泛型实例类型不能作函数参数 ④ 自定义泛型类返回类型不匹配 |
| B. 标准库 | **B-1 实测**：映射 O(n) 2000插入 11ms/2000查找 9ms → 5万符号分钟级，**哈希映射=硬前置确认**；字符串/容器/文件IO ✅；随机数/正则/线程 后置 |
| C. 内存管理 | stdlib 释放/析构 32 处手写模式确认；一次性进程 arena 可行性 ✅；建议前置 自动释放+泄漏检测 |
| D. 工具链 | **D-1 基线**：纯编译 39ms/含模块 330ms → 6万行自举 ≈ 80s~5min（C++版）；Sanitizer 前置建议 |
| E. 生态 | LSP/文档站/包注册中心 确认后置 |

### 缺口修复建议顺序（待用户批复）

1. 哈希映射（O(1) 重写 stdlib/映射集合.cn）
2. 语言 4 缺口一批（字符串[i]/选择返回/泛型参数/泛型返回）
3. 内存管理（自动释放 + 泄漏检测）
4. 阶段 7 自举（7.1~7.6）

### 其他

- SSH 推送通道已打通（用户添加公钥，remote 改 SSH：git@gitcode.com）
- 检查产物：target/exp/自举检查/主.cn（可复跑小样）
