# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结
> **第一百八十轮 180-a：甲通道实装——方法体内容器字段直接赋值深拷（uF 根治）+ 第一百四十九轮 149-a 补记（IR diff 专项定位）**（2026-09-15，家机 win-x64）：
> ①**149-a 定位**（源码零改动，详见 plans/020 第六十八节）：直接 diff 148-a 遗留 `target/p149win/ir_{base,fix}.txt`
> + `cn ir` 补测调用点——**三铁证**：类引用参数实参=源槽地址（二级·138-a 发现二一致）／基线两层求值=正确匹配／
> 148-a 一层修复=读相邻槽（chain 拷贝=0 精确复现）；**uF 真崩因=拷贝构造内容器字段赋值指针共享（浅拷）→ 出口双重
> 释放 0xC0000374**；★146~148-a「求值多解一层」系误诊（三轮回退正确）。教训入 lessons 149 段（补记）·预防 166~168。
> ②**180-a 实装**（单点 `ir_oop_field.cpp` `handleClassFieldAssign`）：方法体内无前缀字段赋值（隐式 this 形态 a）
> 的**拥有型容器字段深拷**——对齐形态 b（memberClassFieldAssign，139-a 已有）：四族收窄（向量/栈/链表/队列）+
> 构造内初始化豁免（currentMethodIsCtor_ 跳过 preFree）+ 源分级（构造调用接管/左值 NewObject+拷贝构造〔实参=源槽
> 地址〕/防御直存）。修复后 `表 = 其他.表` 生成 新建对象+拷贝构造+存槽 三指令，零共享。
> ③**行为验证**：uF 0xC0000374 崩 → **`F ok 大小=1` rc=0**；uA~uE 全 rc=0；probe_chain 链表/队列拷贝=2、
> 残留全 0（146~148-a 退化面零复发）；五形态/栈族/计数探针全 0；116_Feature2 ×3 PASS（138-a 回归面零复发）。
> ④**门禁**：零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过 / 0 败 / 0 跳过**（含 78_v2/79_v2 自举链——**锚定链 fix_p≡fix_s 自洽**
> =新宿主 v2 编译产物零变化裁决）+ 组件对拍 **45/45** + 运行级 **3/3**。门禁七面全绿。
> ⑤**诚实边界**：uD（壳 d=a 结构体整体拷贝浅共享）未在本轮范围——emitStructWholeAssign 成员级级联深拷
> 登记 181-a 候选（uD 当前 rc=0 泄漏不崩，runtime 在册兜底，非阻塞）。
> ⑥文档六件套 + 看板收工。
## 深度机 linux-x86_64 节

## 功能完善总结

> 185-a D1 v2 侧续波——检查赋值语句 178→47 行 ≤100 达标（2026-09-15 第一百八十五轮，
> 深度机 linux-x86_64；基线 422e13f）：
> **拆分面（左值形态分派·纯搬运零改写）**：`语义检查语句.cn` 的检查赋值语句（178 行）提取为
> **2 个族子方法**（同文件）：`检查标识符左值赋值`（未声明/常量/已转移/常量引用/B10 静态写/
> A2 字符* 收紧，43 行）/`检查下标左值形态`（字符串下标只读拒/指针下标写 B10+数组豁免/
> 字段数组元素可写 97-a，48 行）。主函数收缩为分派+偏移/混合/逃逸/表达式循环 47 行。
> **多重集核验缺失 0 行**+新增 10 行全为签名/分派/闭合。本轮顺利（181/184 轮方法论组合
> 首次一轮通过——lessons 185 段正向记录+预防 173 核验边界双侧同界 v2 版）。

> **验证链（v2 侧=锚定链重锚路径）**：零警告 + 单测 **1317/1317** + 全量 E2E **306 用例 304 过 /
> 0 失败 / 2 跳**（44 双编译对照全过）+ 锚定链**重锚 395347→395505 行**（fix_p ≡ fix_s 逐字节，
> md5 `02d691aa…`）。**D1 计数 63→62**（检查赋值语句出列；新首列=`parser_oop.cpp` 运算符X 198）。
> github 镜像推送=本机无凭据，待配置或他机代推（170~185-a 十五轮待补推）。

## 单位机 ARM64 节

## 功能完善总结

### 187-a 规则与统筹落盘轮：三机任务实施计划强制落盘（2026-09-15，单位机麒麟 ARM64，文档/协议轮，基线 3361b22；源码零改动）

**用户令（2026-09-15）**：三机（AI）在飞任务的具体实施计划此前散落在看板一行与提交信息里，用户与三机都看不清
「谁在做什么/怎么做/是否打架/有无遗漏」——**新任务线必须把详细实施步骤落盘在 `plans/` 里**。

1. **AGENTS.md 新增 §8.6「任务线实施计划落盘（强制）」**：任何新任务线/开发轮，必须先把**六要素**详细实施计划
   （①目标与验收判据 ②写集〔精确文件清单〕③实施步骤〔含设计决策与理由，技术路线优先 Rust 对照〕④验证链
   〔命令级门禁〕⑤边缘情况与诚实边界〔预判→收工回填〕⑥与他机交叠分析〔写集交叠 + 语义耦合：独立/互补/冲突〕）
   落盘到 `plans/025` 本机节再开工；**未落盘=不得开工**（看板认领行须带**计划指针**）；收工回填实测 + 整节替换为
   下一轮 + **预登记下一轮候选**（防两机撞同一队列项）。配套：§8.2 开工行加「计划指针=认领前置条件」；§8.3 新增
   `plans/025` 分区行 + `plans/021` 多机并行收尾纪律（最小局部编辑 + fetch/rebase）；§2.2 开工必读加读 `plans/025`。
2. **新建 `plans/025-三机任务统筹与实施计划.md`**：§〇 三机在飞总览（+ 下一轮预登记表）；§一/§二 家机 183-a 与
   深度机 186-a 计划转写（含 184/185-a 已完成轮折入：D1 计数 64→62、锚定链 394959→395505）；§三 单位机节
   （187-a 记录 + **188-a D6 六要素计划** + 欠账 U1~U3）；§四 **交叠与遗漏审计**——写集交叠矩阵（三机零交叠）、
   语义耦合判定（互补不打架；共同面=锚定链各机自重锚）、**遗漏清单 8 项**（ARM64 复验欠账 161-a~186-a／D6 未排期／
   D3·D5 待排期／F1·F2 重评轮待认领／plans/021 并行收尾冲突面已入纪律／契约广播／波 4·波 7 未开工／C2 维持）、
   边缘情况清单（按在飞任务分列）。
3. **188-a 认领=D6**（B11 变量常量传播增强：`整64* p = 无; *p` 形态双侧硬错误；依据 `plans/023` §九 已批准定义 +
   `plans/001` §5.3 诚实边界；Rust 对照 `deref_nullptr`；**顺带收口 ARM64 复验欠账**〔arm64 锚定仍 158-a 的 555442〕）；
   写集=宿主 `semantic/*` + v2 `语义检查.cn`（硬约束：不落 `语义检查语句.cn`）+ `tests/e2e/` 新用例；核证与家机 183-a
   （`ir_fields.cpp`）、深度机 186-a（`parser_oop.cpp`）零交叠。
4. **★轮次更正（教训）**：本机规则轮首次以「185-a」提交推送被拒——该号已被深度机（cd9dce9/3361b22）先占，按协议
   **push 被拒=认领无效**，须重取号；更正为 **187-a**（规则轮）、**188-a**（D6 轮）。教训入 `lessons.md` 187 段。
5. **ARM64 基线实测（顺带）**：清场构建（GCC 9.3）**零警告** + 单测 **1317/1317** 通过——为 188-a 全链门禁与
   跨机复验欠账收口打底。

**诚实边界**：本轮纯文档/协议（源码零改动，无门禁面）；家机/深度机节为 185-a→187-a 转写（信息粒度受其一手指控
记录限制）；github 镜像本机无凭据未推（沿前轮口径）。
