# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（302-a·T39 根治·2026-09-17，基线 126baff·用户授权接管开发轮）

**T39 根治销项：三后端 i128/u128 位运算/移位/Cast 通道补全（P1·采样独家捕获→同会话根治）**：根因=三后端 dispatch 对 `Shl/Shr/BitAnd/BitOr/BitXor` 缺 i128 分派（Add/Sub/Mul/Div/Mod/比较均有）→落 64 位通用路径=**低 64 位槽从未被写**（消费侧读未初始化·asm 铁证 i128_sh3.asm·三跑三值非确定）。实施三后端：win（`splitI128` 成员提取+`emitInt128Bitwise` 双半独立 and/or/xor+`emitInt128Shift` 完整 128 位〔`shld`/`shrd` 双字移位·移位量按位宽 mod 128〔plans/001:289 用户裁决〕·常量编译期分路+变量 and 127/cmp 64 二分路·有符号 sar/无符号 shr〕+dispatch 五类+128→窄 Cast 扩展〔i16 等原读高半槽〕）；x64l 同构（emitStackLoad/Store 适配+`Lsh128_b/e` 标签+cast 分支前移）；arm64 同构（移位量寄存器自由·lsl/lsr/asr+strh 窄写）。**修复中自查三处当场修正**（移位量双槽读高半=恒 0→按操作数类型分流/窄 Cast 分支顺序→i128 分支前移/`widthFor("i16","[槽]")` 兜底 "eax" 空写→槽文本勿经 widthFor）。验证=win 全组探针 **8/8 全绿且 3 次确定**（隔离位运算/族矩阵/12 形态移位矩阵〔含 0/31/32/63/64/65/127/130 mod〕/cast/变量移位量/复合）+新 E2E **409_整128位运算移位**（27 断言·O0=O3 逐字节·expected win 实跑固化）+**全量 E2E 424=424/0/0**（零回归）+单测 1353/1353+CLI 50/50+spec --strict 100%（409 登记 4.4 单元）+面覆盖 68/69+双文档门禁；x64l/arm64 静态形态核验（shld/shrd=19·lsl/lsr/asr=104+strh）。**诚实边界**：v2 侧不支持 `整128()` 转换（对齐欠账另登记·409 宿主单侧）；x64l/arm64 运行级复验=跨机项（请求已广播）。明细=plans/025 §1.0+HANDOFF 家机节。

## 深度机 linux-x86_64 节

## 功能完善总结（🧪 M3 采样轮·增量5+B9 扩散轮·验证轮不占号·2026-09-17，基线 1903a2f→478d804·源码零改动）

**daemon 67 轮对账 + T28 族双实例立案 + T27 B9 补轴 72 形态**（家机 🏃 296-a D35 波B 开发期间持续采集·零交叠互认）：

- **daemon 在飞 67 轮**：命中集合不变 5 项=T26×3+T27 双位宽×2（**s2609179005 整64=族内新实弹**）——已立案缺陷持续暴露（回归轨正常·待根治）；月日志 6212 行触发门禁→按 S1b 记法本意手工压缩为汇总段（3939 行）。
- **T28 族双新实例（plans/021 §三-G·设施判据与语义脱节族·验证轮只登记）**：①daemon 归类判据文本反推断裂（cnsmith_diff.py 清单行只写 rc=-8→daemon「运行失败」字样匹配落空→误归 diff/+重复副本）；②S1b 汇总记法结构性失效（消失回归恒非空→无变化永不成立→每轮全量记录=日志膨胀根因）——方案甲/乙/丙已列待设施轮根治。
- **T27 B9 补轴（面测试网·点→线→面）**：72 形态三方对拍（双位宽×÷/％×8 求值位置×变量/字面×O0/O3）→ **26 分歧两大新形态**：①**整32 变量形 O0 idiv 崩/O3 回绕不崩=级别间行为分叉**（14 形态）；②**整64 字面形静默错值**（O0=-2147483648=INT64_MIN 低 32 位符号扩展精确位样/O3=2^63 位样或 0·双级别互斥·12 形态）。甄别链：纯字面打印/变量中转打印 O0 全对→截断仅挂「字面常量直连÷/％」通道；emitDivMod 两后端真 idiv 已排除→挂点=IR 构造/常量装载层（留根治轮）。**正面**：短路惰性正确豁免溢出（4 形态）·整64 变量双级别崩一致·除零防护✓。**B12 呈报已扩注**：四路行为不一致（O0 陷阱/O3 回绕/字面截断/位样直通）——唯甲（统一回绕）同时消崩溃+分叉+错值。

**验证链**：B9 补轴 72 形态三方对拍实测（分歧 26·一致 42·剔除探针生成器 bug 4）+判型/通道甄别实验+门禁三件独立执行退出码=0（含总表戳漂移修复=刷 296 在飞）；落盘=面台账行 62 扩注+plans/021（§三-G+T27 行+B12）+月日志汇总段/扩散轮段+plans/025 §二+HANDOFF 本机节。

**诚实边界**：整64 字面错值的确切生成层未读到行（两假设并立·已注记为挂点假设非定论）；oracle 为自建硬件语义模型非 gcc 对拍（根治轮须双 oracle 复验）；T27 arm64 未测（归单位机）；daemon 汇总记法未根治前重启仍退化全量记录（每 ~45 分钟压缩监控·红线 5000）。

## 单位机 ARM64 节

## 功能完善总结

### 297-a 开发轮：T25/T24/B10 三案当日根治（2026-09-17，用户裁「按照你的方式，全部修复」，基线 478d804）

1. **T25 根治**：arm64 `emitCopy` 补浮点分支（loadOperandToV+storeVirtualResultFp）——原整型 is64 分派致 f64 `str w9` 32 位截断写（高 32 位丢=汇合值归零）；27 形态回归矩阵全绿+406_浮点汇合矩阵O3 转正（编译选项.txt=-O3）。
2. **T24 根治**：后缀自增/自减表达式值=自增前原值（visitUnaryExpr 三路径+handleClassFieldIncDec 按 postfix·原一律新值）；01b 优先级表补 `++`/`--`（后缀 13/前缀 12 级+值语义条文）；407_后缀自增矩阵 16 形态转正。
3. **B10 根治（方案甲）**：parser `ternaryColonAhead()` 同层 `:` 配对前瞻消歧（新判据⊇旧判据零回归论证）——真分支一元运算不再要求括号；408_三元负号矩阵 6 行转正；后缀传播 E2E 60 复验绿。
4. **T26 同族判定**：arm64 短路免疫实证（x86_64 单侧归深度机 B11）。
5. **规范条文**：plans/001+spec 01b 六处（消歧规则/++/-- 登记/条件须布尔/值语义）。
6. **全量门禁绿**：零警告+单测 1353/1353+E2E 426=424/0/2+78/79 自举锚定双 PASS；看板通告=v2 消歧同口径广播（家机 296-a 对齐）。
