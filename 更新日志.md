# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百三十四轮 134-a：波 1（plans/022 §七 首波）——修宿主 字符* 拼接误报，check v2 树 75→0**（2026-09-14 上午，家机 win-x64；基线 3b33cce）：
> ①**缺陷根治**（宿主 `semantic_expr_op.cpp` + `semantic_internal.hpp`）：安全区边界「指针算术」检查原按
> 类型文本 `*` 后缀判指针——而 `字符*` 是**字符串视图**（IR 层同为 ptr、+ 按拼接分派），致 v2 树全部
> `驻留文本(...)/字符串` 拼接被误报（75 处）。修法：新增 `isStringSemanticType`（字符串/字符* —
> **单一事实源**，拼接分派与指针检查共用，防漂移）；`+` 涉及字符串语义类型时不算指针算术（对齐语义层
> 拼接分派）；`字符*` 的 `-`（指针步进）与真指针（整N*/类*）的 +/- 保持原判；
> ②**验收三项全过**：check v2 树 **75→0**（其余警告 126 处、错误 0 不变）＋ **反证**（`git stash push` 单文件
> 回退复现 75 处——唯一变量）＋ E2E 全量零回归；
> ③**单测**：新增 2 例（拼接零警告 + 字符* 减法保留警告）→ **1315/1315**；探针：拼接 4→0、减法/真指针 2 处保留；
> ④**零行为变更**：锚定链固定点 406097 行逐字节一致 + 对拍 44/44 + 运行级 3/3（诊断层改动不影响代码生成）；
> ⑤**Rust 对照**：rustc 以 TyKind 结构化枚举判类型（非文本后缀）；本轮先落地「语义集合单点判定」（方案甲），
> 类型类别枚举（方案丙）随波 6 落地（plans/022 §七）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
