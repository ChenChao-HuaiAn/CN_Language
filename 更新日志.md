# 阶段P7b收尾：__cn_intern 指针悬垂根治 + stdlib 签名表启用（2026-09-01）

## 一、宿主运行时缺陷根治：__cn_intern 驻留池指针悬垂（UB）

用户裁决「体内二分最小化」执行：逐句隔离锁定 `驻留(...)` 调用后字符串参数丢
（常量实参也丢、字符串长度 不丢 → 驻留 特有）→ **根因：pool 用
std::vector<std::string>，扩容搬移全部 string，已发放的 c_str() 指针悬垂**
（驻留文本 返回指针被编译器全链长期持有；破坏位置随扩容时机漂移=UB 三特征：
人工复现全失败 + asm 全对 + 位置随机）。**根治：pool 改 std::deque<std::string>**
（push_back 不使已有元素指针失效，标准保证——对标 rustc StringPool）。

## 二、项1 完成：v2 解析 stdlib 建签名表（消除硬编码）

- IR签名.cn（新组件，4xx 行）：token 状态机扫描 stdlib/容器.cn + 映射集合.cn
  的泛型类方法签名（类头参数序/参数串/返回类型），查询时 T/K/V 按实例实参替换；
  惰性加载。内置硬编码 if 链（~150 行）已删除。
- 附带根治扫描器三缺陷：①关键字表无「泛型」→ 判定改按文本匹配；②参数序误收
  声明词「类型」；③返回类型 归一 $ 合成名（归一结果名）。
- 维护契约消除：stdlib 容器改签名/加方法 v2 自动跟随（57 条签名全部来自源码解析）。

## 三、验证

E2E 119/120/123/124 全绿（123 走签名表路径）；全量门禁（零警告+单测+E2E）见
提交信息。v2 组件 9 个，行数全部 <=1000。
