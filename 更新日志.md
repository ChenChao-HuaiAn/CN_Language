# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 93-a 类名/实例成员静态性不匹配六形态编译期拒绝（B2 销项）（2026-09-13 第九十三轮，深度机
> linux-x86_64）：第六十一轮立案的「类静态成员+析构写静态成员运行期崩溃」（B2，tp3 探针）本轮
> 经**探针四步二分定位真根因**——①遗留产物 `target/tp3` 段错误复现 + 符号反解探针真形态 →
> ②当前宿主重建同形稳定复现 → ③变体矩阵逐要素消除（两对象/独立函数/类静态/拷贝构造/静态方法
> **全不是根因**）→ ④**真根因 = `类名.实例方法()` 形态语义层漏检**（**与类静态成员无关**——
> tp3 探针偶然同时含两要素致归因偏移）。宿主 `semantic_call.cpp` 成员调用路径直查成员表命中后
> 即按实例方法调用生成（无 this）→ IR 产生「间接调用 NULL」→ **段错误**（IR dump 实证）；
> v2 侧同形态退化生成（对象=未声明标识符，标注静默失败）→ 运行输出**垃圾值**
> （`5548464235739974476` 实证）。

> **六形态一族同族扫面（探针矩阵）**：①`类名.实例方法()`（宿主段错误/v2 垃圾值）②`类名.实例
> 字段`读（既有拒绝/v2 垃圾值）③`类名.实例字段`写（既有拒绝/v2 静默写）④`实例.静态方法()`
> （宿主段错误）⑤`实例.静态字段`（宿主静默错误：读恒 0/写不生效）⑥`接口名.方法()`（宿主段
> 错误）⑦`子类名.父类实例方法()`（宿主段错误，同①根因）。

> **方案裁决（用户令：参考 Rust、性能第一、安全第二）**：**甲（采纳）=编译期拒绝**——纯编译期
> 检查**零运行期开销**（性能第一）+ 消除静默错误（读垃圾/暗写）与崩溃（安全第二）；
> Rust 对标 E0599（关联函数不经实例访问）/E0061（带 self 方法须经实例）=两种错配形态均编译期
> 拒绝。乙（支持 实例.静态成员，C++ 语义：需 IR 层新分支 + receiver 求值规则，存量零使用）/
> 丙（仅修单形态：留三形态缺陷）未采纳。

> **实施（宿主 4 处 + v2 1 处）**：宿主 `semantic_call.cpp` 成员调用路径新增 **receiver 类别
> 判定**（`IdentifierExpr` 且 `isClassType`=类名语义；`自身`/`父类` receiver 非类名→实例语义
> ——20/21 用例 `父类.名称`/`父类.叫声()` 合法形态不受影响）→ ①拒绝 + ④拒绝；接口分支新增
> ⑥拒绝（receiver=接口类型名无对象可分派）；`semantic_expr.cpp` 成员访问路径新增 ⑤拒绝（补齐
> 既有「类名.实例成员」对侧）。v2 `语义/语义检查.cn` 新增 `检查类名成员访问`（对象=标识符 且
> `是用户类ID` 且 **非变量遮蔽** → 诊断）+ 挂载 `检查表达式` 的 `节点_成员` 分支（语句根区间
> 线性遍历=字段读/写/方法调用全形态覆盖）；**遮蔽判据调试实证**=符号种类 ∈ {`符号_变量`,
> `符号_参数`}——类名自身在符号表登记为 `符号_结构体`（种类 4），首版按「表查找类型ID != -1」
> 判定会把类名自身误判为变量致检查**永不命中**（调试注入定位）。

> **防线**：E2E **252_类名实例成员拒绝**（宿主负测：六形态 + 诊断子串锚定）+
> **252_v2_类名实例成员拒绝**（v2 负测：三形态 + 三诊断行固化）；单测 **+7**
> （`test_semantic_oop.cpp`：六拒绝形态 + 一合法回归锚定）。**反证双件全命中**：A 宿主三处
> 拒绝条件失效注入（`false &&`）→ 252 失败（缺期望诊断）→ 恢复（注入前 md5
> `58799ed0d453b6a46b9a733d85f344d9` 复核）复绿；B v2 检查体 `返回 假` 注入 → v2p 重建 →
> 252_v2 失败（预期中止但退出码 0）→ 恢复（注入前 md5 `51788a5738eed8a0546452e36134c0c0`
> 复核）复绿。

> **验证链全绿**：清场重建**零警告**（宿主 md5 `10607b8501aef9996ca5372e6602a590`）+ 单测
> **1313/1313**（1306+7）+ E2E **280 用例 276 过 / 0 失败 / 0 未实现 / 4 跳**（+252/252_v2；
> 其余 278 过=零回归）+ 锚定链固定点 **362243 行**（92-a 后 361684；+559=v2 检查源码确定性）
> 两代逐字节一致 + 绑定自检（cn_main=0x54b255 ∈ cn_self93.o .text [0x402586, 0x54f838)）+
> 组件对拍 **34/34**（DIFF=0）；v2p md5 `847e17c22ce0a32f4114ab383fcdab54`、cn_self93 md5
> `341d2e026dcb6df3afbaf69f450299d4`。

> **诚实边界**：v2 侧 ④⑤⑥ 三形态不可表达（v2 类支持第一版无静态成员/接口/继承）——宿主侧
> 拒绝已覆盖，随 v2 类能力扩展（第二版待做面）需同步；跨机轮（win/arm64）复跑 E2E 即可覆盖
> 本门禁（宿主改动在 semantic 层、v2 改动在语义层=平台无关；**v2p 二进制已变需重建**）；
> **B2 销项**（plans/021 §4.2 台账）。详见 `plans/020` 第三十三节与 git 提交。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
