## 功能完善总结

### 缺陷② v2 侧收口 + v2 裸节点双重处理根治 + 缺陷 A/B 双侧根治（家机 win-x64 第四轮）

> 本轮在家机续推单位机交接（7a85751）：完成缺陷②「非左值赋值拒绝」的 v2 侧四件对齐、
> 132 用例转绿；过程中按用户令「任何缺陷及时完善/参考 Rust/隐患拦在编译期」当场根治
> v2 结构性缺陷「检查块裸节点双重处理」与单位机遗留的缺陷 A/B（双侧公共 IR 层）。

- **缺陷② v2 侧四件**（CN语言编译器v2/语义分析.cn）：
  - 新增 `表查找种类`（作用域栈内→外走查，-1 未找到——常量判定与 表查找类型ID 同序，
    内层变量遮蔽外层常量按内层解析）；
  - 节点_赋值 标识符左值（文本!=0）声明检查后加 `种类==符号_常量` →「不能给常量赋值
    （常量初始化后不可修改）」；复合标识符形态（名+=值 左值末=标识符）同款常量拒；
  - 节点_赋值 非标识符左值（文本==0）：左值末节点白名单 {标识符（复合合成）/成员/
    解引用/下标（既有指针白名单保留）}——其余（二元运算结果/字面量/负号/取地址/调用/
    空左值粘连形态）硬拒「赋值目标必须是可写左值（变量/成员/下标/解引用）」；
  - 节点_自增 常量拒「不能对常量自增」。
- **v2 主.cn 尾部两负测自检**（源码6：`a + 1 = 7`；源码7：`常量 甲 = 5 … 甲 = 7`）——
  132 用例 主.expected 固化的「[sem5] 非左值赋值负测通过」「[sem5] 常量赋值负测通过」
  两行落地，**132 六步闭环 PASS（退出码 0）**。
- **【新缺陷根治①】检查块 裸节点双重处理**（lessons 11.0 权重「区间重叠双重处理」同族）：
  块区间线性包含各语句 RHS 裸节点，原逐节点派发使裸一元节点（取地址等）在**声明根注册
  名字之前**先查操作数区间——自引用 `&(v*p)` 误报未声明、前向引用 `&q` **同错双诊断**
  （p16 探针实证报两次）。根治：检查块 主循环改**仅派发语句根节点类型**（块/如果/当/
  变量声明/赋值/自增/返回/表达式语句/中断/继续），裸节点一律由所属语句根的区间检查统一
  覆盖（每节点恰在其作用域访问一次，rustc 对齐）；检查语句 一元分支死代码删除。
- **【新缺陷根治②】解析器裸 `*表达式` 语句无根 + 静默中断不可见**（语法分析.cn）：
  `*p` 非赋值语句原裸节点入池无语句根——IR 语句派发零分支=静默死代码，语义靠裸派发
  兜底（=缺陷①根源）。补 节点_表达式语句 根（对齐 936 行兜底；裸 `*未声明x` 检查覆盖
  保持实证）；解析块 循环保护静默中断加 stderr 诊断「语句解析中断于不可解析 token，
  块内后续语句被丢弃」（B6/+= 语句级静默中断家族可见化，`*p += 1` 形态由静默截断变可见）。
- **【缺陷 A 根治】成员/下标/解引用自增自减写回缺失**（ir_expr.cpp）：Increment/Decrement
  写回仅 IdentifierExpr（类字段/顶层静态另有专用路径）——`p.y--`/`数组[1]++`/`(*q)++`
  只读不写=存储静默丢弃（单位机 ARM64 探针发现、win-x64 同现=**双侧公共 IR 层缺陷**，
  非单后端）。修复：Member/Index/Deref 目标 lvalueAddress+Add/Sub+StorePtr 写回（宽度按
  操作数类型，②b B5 字段宽度纪律）。全形态探针 `--p.y`/`p.x++`/`数组[1]++`/`(*q)++`/
  `--(*q)` = 2/11/6/9/8 全对。
- **【缺陷 B 根治】数组局部无初始化器栈垃圾**（ir_stmt.cpp）：无初始化器补零仅有结构体
  分支（2026-09-02 缺陷2 根治漏数组同族形态）——`数组[2] += 数组[1]` 读未初始化内存
  （每次运行不同）。修复：registerVarSlots 登记槽数逐槽 Store 0（Rust 级确定性对齐），
  探针输出 5 正确。
- **132 源码换行粘连修正**：`&p.x` 换行 `*指针 = 100` 按规范（换行≡空格，131 裁决①）
  粘连成 `&(p.x * 指针)` + 空左值 `= 100`——v2 新左值检查恰好正确拒绝空左值赋值（左值末
  =变量声明节点∉白名单），源码补分号=语法错误修正（规则允许直接修正），附注释说明。
- **宿主单测新增** test_lvalue_check.cpp（11 项）：九种负形态精确消息断言（二元/复合/
  字面量/自增非左值/一元负号/非引用调用/常量赋值/常量自增+粘连形态）+ 跨行「想写分号」
  提示串 + 合法全形态零误伤（变量/成员/下标/解引用 × 简单/复合/自增 + 常量读取）——
  消息文本以 cn check 实测输出锚定。注册 tests/unit/CMakeLists.txt。
- **验证**：构建零警告；单测 **1207/1207**（新增 11）；E2E 132 六步闭环 PASS；探针
  p1/p5/p13/p14/p16/p17/p18 行为全部符合设计（粘连恰一条正确诊断/前向引用单诊断/干净
  形态零误报/裸解引用检查覆盖/静默中断可见）；缺陷 A/B 修复在公共 IR 层，双后端同享，
  ARM64 侧由单位机全量复验。
