# CN语言编译器 更新日志

## 功能完善总结

# 第四十五轮（2026-09-09 深度系统 x86_64）：跨机轮 X64L 侧动态收口——全量门禁复验+E2E 176~182 X64L 首验 7/7+组件对拍 29/29 全量归零+v2self 固定点 fix_p≡fix_s（224118 行 X64L 锚定）+字面量位模式 movabs 实物落定（纯验证轮零源码改动）

按 HANDOFF 挂账1 拉取 5fa9a66（第四十~四十四轮）执行本机（深度系统 linux-x86_64）跨机轮动态收口，第四十四轮诚实边界五项全部当轮兑现。三平台验证矩阵在本轮后处于全绿同步态。

## 一、全量门禁（清场重建）

- 构建：CMake 清场重建，GCC **零警告** rc=0（日志中 4 处 warning/error 字样全为文件名含 "error" 的编译进度行，真实警告 0）。
- 单测：**1251/1251 全过**。
- 全量 E2E linux-x86_64：**184 用例 180 过 / 0 败 / 4 跳**。跳=62/69/78/79 既有平台限制（与历史基线一致）；深度机内存充裕，无 win 侧 78/79 OOM 败面。零回归。

## 二、E2E 176~182 X64L 侧首验 7/7 PASS

- 176_字面量进制形态（宿主八形态）/177_v2_字面量进制形态（v2 闭环 rc 门控）。
- 178_移位掩码语义（宿主六形态）/179_构造字面量穷举负测（宿主缺字段拒绝）。
- **180_v2_位运算形态（rc=176 闭环）**：第四十三轮 v2 X64L 后端位运算发射（and/or/xor+shl/sar）实物首验通过；181_v2_构造字面量形态（rc=42 全字段赋值语义）/182_v2_构造穷举负测（None 通道+诊断行）闭环。

## 三、v2self 锚定链（固定点 + 绑定自检 + 运行级）

1. **fix_p**：v2p（E2E 步骤1 产出 target/audit2/v2p_linuxx64，全树编译 rc=0）编译 v2 自身 → fix_p.s（**224118 行**）。
2. **cn_self45 链接**：as 汇编 + g++ 链接（cn_self45.o 在前 + v2p_linuxx64.o 借链容器符号 + rt objs 按当前源码现编 + `-Wl,-z,muldefs`）。rt objs 现编原因：audit2 旧 .o 早于第四十轮 intern_api 平台分流改动，按产物新鲜度纪律清场现编（target/rt45/）。
3. **绑定自检（决定性）**：链接 map 实证 cn_main 地址 0x4cc3c8 落在 cn_self45.o 的 .text 区间 [0x402586, 0x4CFDBE) 内（v2p_linuxx64.o 区间自 0x4cfdbe 起）——可执行 CN 代码全部来自自举第一代产物，cn_self45 是真正的第二代自举编译器（muldefs 借链仅供容器符号）。
4. **固定点**：cn_self45 编译 v2 自身 → fix_s.s（224118 行），`cmp fix_p.s fix_s.s` **裸逐字节一致**——X64L 后端侧锚定确立（第四十三轮 302433 行为 arm64 后端口径，行数不跨后端比较）。
5. **运行级三用例**：hello 单文件 rc=0（输出 你好，世界）/ 119 多文件 rc=14 / 173 目录包根撞名成员 rc=173，全对齐第三十八轮口径。

## 四、组件对拍 29/29 全量逐字节归零

- v2 全树非主.cn 文件 **29 个**（6 包根 + 23 成员）——历史口径 30→29 系第四十一轮挂账2 收口删除 abi辅助.cn（v1 遗留探针 13 行），find 实数核对非遗漏。
- v2p 与 cn_self45 各编译全部 29 组件后 asm 逐字节 cmp：**29/29 全过、零 DIFF、零 rc 失败**。「strData 编号平移规范化对拍」预留手段未触发（同源同代两侧本就逐字节一致）。

## 五、字面量位模式 X64L 动态运行复验（第四十二轮静态旁证 → 实物落定）

- E2E 176 八形态探针源（0xFF/0b1010/0o777/42L/7U/2^55/0x7FFFFFFFFFFFFFFF/-255）：宿主与 v2p 各编译 → as → 链接 → 运行，**八行输出逐字节一致** + 双侧 rc=0（程序内八断言全过）。
- v2 产物 asm 实证 `movabs r10, 36028797018963968` / `movabs r10, 9223372036854775807`（与第四十二轮静态旁证逐字吻合）；宿主产物同值 `mov r10, imm64` 形态——specs/08 批注「宿主与自举键形态细节允许各自定义」，语义等价由运行输出一致锚定。
- 对照实验记录：入口文件名非 主.cn 或不存在 → v2p/cn_self45 双侧 rc=2 失败语义一致（01_hello 用例文件名为 hello.cn 非主.cn，引用错名即得双侧一致对照实证）。

## 六、诚实边界与文档

- 无新增挂账：三平台验证矩阵（linux-x86_64 / linux-arm64 / win-x64）在本轮后处于全绿同步态。
- 文档：plans/014 状态表第四十五轮行 + 更新日志 + HANDOFF 重写。
- 本轮零编译器源码改动（纯验证轮）；探针产物 target/probe45/、cn_self45 链接物、rt objs（target/rt45/）均不入库。
