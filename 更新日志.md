## v2 自举闭环第一步：外部文件读取 + 类型转换支持（P6f，2026-08-30）

**目标**：v2 自举闭环第一步——v2 主() 从「固定字符串源码」改为「读取外部 .cn 文件」编译；过程中补齐 v2 组件链（语法/语义/IR/代码生成）的**类型转换 类型(expr)** 支持（v2 组件自身源码 247 处使用，自举硬前置）。

**改动（5 个 v2 组件）**：
1. **主.cn**：主() 读取 target/v2src.cn（打开文件模式 1=读 + 读取文件行 循环拼接）；asm 写入句柄改名避免变量冲突。
2. **语法分析.cn**：节点_调用 子起点=被调标识符节点索引（后缀链原子先入池，参数起点-1）。
3. **语义分析.cn**：检查表达式 补 节点_调用 分支（被调是类型名→类型转换；否则函数调用查符号表返回类型）；标识符分支补类型名合法（整32 等）。
4. **IR生成.cn**：新常量 IR_类型转换=54；节点_调用 被调是类型名且单实参→IR_类型转换（附加=目标类型码）。
5. **代码生成.cn**：IR_类型转换→ mov eax + movsxd（整64→整32 截断符号扩展；整64 透传）。

**验证**：v2p 读取外部文件 阶乘(5)（含 整32(阶乘(5)) 转换）→ 语义错误数=0、IR 2 函数、asm 94 行汇编通过（阶乘递归+movsxd 正确）；宿主同源基准 exit=120（阶乘(5)=120）；单测 1196/1196；E2E 30/96/111/01/09 PASS；v2p 错误程序诊断仍正确。
## v2 字段偏移表按结构体分桶根治 + 宿主嵌套泛型/静态构造初始化（P6e，2026-08-30）

**目标**：根治用户批评的 v2 语法层「字段名全局唯一」扁平字段偏移表隐患（两个结构体同名字段会串偏移）——按结构体分桶；过程中发现并根治宿主两处缺陷。
