# 功能完善总结

> 74-a 容器元素所有权全链归一化（2026-09-11 第七十四轮，深度机 linux-x86_64）：用户未答复四方案呈报 → 按 AGENTS.md 第 3 项纪律（性能第一、安全第二、参考 Rust）自行裁决**方案 A**（入容器位所有权归一化 + 元素释放面补全）。

## 一、立案：plans/020 矩阵靶子 #1 揪出机制级矛盾（五枚形态探针实证）

任务 = 矩阵靶子 #1「入容器编译器级自动 copy/move」。核验中确定根因是**两个半截机制**：容器析构**无条件释放全部元素串**（`__cn_vector_free_strings`，假定「元素所有权归容器」），而**入容器位不接管所有权**（实参句柄被浅存）。两者各自「看起来正常」，合起来是一族静默缺陷：

| # | 缺口形态 | 侧 | 探针实证 | 判定 |
|---|---|---|---|---|
| ① | 借用句柄入容器 → 容器析构释放**调用方拥有**的串 | 宿主 | 探针 74-L：`存入(表,本地)` 后打印「容器销毁后 本地」输出乱码 | **P0（跨函数 UAF）** |
| ② | 局部拥有串裸标识符入容器 → 源 RAII 释放 + 元素浅共享=悬垂 | 宿主 | 探针 74-A：3 元素内容全乱码（搅动后仍损坏） | **P0** |
| ③ | `转移(源)` 入容器**非真 move**（源槽未清零） | 宿主 | 探针 74-J：元素内容乱码 | P1 |
| ④ | **v2 侧容器元素从不释放**（v2 无任何元素释放机制） | v2 | 探针 74-G：残留 2 | P1（防线不对称） |
| ⑤ | **链表/栈/队列元素串从不释放**（宿主只覆盖 `向量`） | 宿主 | 探针 74-N：链/栈/队各残留 1 | P1 |

存量影响面：字符串容器「裸标识符入容器」全树仅 **4 处**，全部是字符串形参（借用来源）——正是最危险的形态。

## 二、方案裁决（用户未答复 → 按项目纪律自行裁决）

Rust 对照：`Vec<String>::push(s)` 是 **move**（源失效），`push(s.clone())` 是深拷贝，容器 drop 时逐个 drop 元素——**签名即契约**。CN 的缺口是「入容器位无所有权归一化」+「A2 人工纪律（字符串入容器须显式复制）不是门禁」。

呈报四方案后用户未答复（工具返回「未提供答案」），按 AGENTS.md 第 3 项纪律（①性能最好 ②最安全 ③参考 Rust）**裁决方案 A**：入容器位按实参来源分级归一化 + 元素释放面补全，双侧对称。

## 三、宿主实施（src/cn_compiler + src/runtime）

**入容器位所有权归一化**（`ir_oop_call.cpp`）：

| 实参来源 | 语义 | 开销 |
|---|---|---|
| 字面量/驻留文本 | 驻留借用（进程生命周期，元素永不悬垂） | **零复制** |
| 调用返回（A2 拥有契约） | 直接接管 | **零拷贝** |
| `转移(拥有局部)` | **真 move**（句柄直存 + 源槽清零） | **零拷贝** |
| 标识符（拥有局部/形参/静态） | `__cn_str_copy` 落堆（容器独立拥有，源照常可用） | 一次分配 |
| 成员/下标/借出视图 | 同上（借用句柄不得共享给容器） | 一次分配 |

**元素释放面补全**（`io_api.cpp` + `emitContainerElemFreeFor` 单点事实源）：
- 平铺模型（向量/栈）：`__cn_vector_free_strings(obj, 数据偏移, 元素数量偏移)`；
- **链式模型（链表/队列）：新增 `__cn_chain_free_strings`**——出队/删除头部**已把元素所有权转移给调用方**，槽序号可能 < 元素数量，按计数平铺释放会误释放已移交的串（UAF），故按链游释放（自 头索引 起沿 下一索引 走，计数作上界防御）；
- 两个辅助均含**元素槽清零**（「释放+清零」幂等不变量，块出口/跳出/函数级兜底/清空多路径共享同一元素数组）。

## 四、v2 实施（IR容器.cn + 生成表达式求值.cn；移植纪律 7~10 条逐条执行）

- 新增 `发射容器元素释放`（按元素模型分派同一运行时辅助）挂**三处释放路径**（块出口析构/发射容器析构/容器字段级联）；
- 新增 `归一化入容器实参`（同宿主分级，`转移` 分支因拿不到 函数信息 统一走复制）；
- **移植纪律 7（清单两件套）**：机制主体=入容器位分级归一化；依赖不变量=①容器元素恒为堆串或驻留常量 ②真 move 源槽必须清零 —— 逐条落地并写入代码注释；
- **移植纪律 8（三列核对）**：`scripts/cn_release_parity.sh` 拉两侧释放挂点，人工核对时机/条件/不变量，生成物层计数两侧均有平铺+链式释放；
- **两个 v2 特有实施不变量**（本轮踩出并固化）：①**调用发射时序**——插入式调用须在 `IR_调用实参` 收集**之前**发射（否则其 `IR_调用参数化` 把已收集实参一并展开，位数错乱）；②**节点池后缀序**——实参**根**节点在调用节点前一位置，不可用 `子起点+1+序`（探针 74-H：误判借用 → 多一次复制 + 原值泄漏，残留 3）。

## 五、成效（量化归零）

- E2E **218** 校验和 **24 → 31**（五形态全对，含四容器释放面残留归零）；
- 真 move 零拷贝实证：预分配后 `转移(x)` 入容器分配增量 **0**，标识符入容器增量 1（深拷贝），源仍可用；
- **E2E 219 双编译对照两侧退出码 63 一致**（移植纪律 9 第二个对照用例：六形态）；
- 生成物层两侧均有元素释放调用：宿主 平铺 20 + 链式 7；v2 平铺 13 + 链式 2。

## 六、验证链全绿

清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 **1289/1289** + 全量 E2E **241 用例 236 过**（+218/219；唯一败=194 既有待批）/4 跳 + 锚定链固定点 **271056 行**（新口径）逐字节一致 + 绑定自检（cn_main=0xf6015 ∈ .text[0,0xf9fa8)，readelf 实证）+ 运行级 3/3 + 组件对拍 **29/29** + 释放路径两侧对照脚本三列核对。

## 七、诚实边界

- **v2 侧 `转移(拥有局部)` 入容器未走真 move**：判定「源是否拥有」需 `函数信息.字符串变量们/污染们`，穿透需改表达式生成族 12 函数 + 22 调用点签名（风险大于收益）→ 统一复制，**语义安全等价**（容器独立拥有、源照常释放，无悬垂无泄漏），仅多一次分配；真 move 优化登记为后续项。
- 嵌套容器元素（`向量<映射<整64,字符串>>`）不释放元素串（元素是容器对象，按 char* 语义释放对象指针会损坏堆）。
- 数组元素/装箱字段借用面无 RAII（既有边界维持）；194 待批维持；win 并行隔离（家机轮）。
- 探针 74 系列（A/J/L/N/G/H）留 /tmp/p74probe 不入库。
