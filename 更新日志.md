## 功能完善总结

### 阶段A+B+C 串联集成 + 最终审查（2026-08-14 收尾存档）

**任务**：为「优先 Linux ARM64 后端 + 合并阶段4/阶段5」大任务做 Git 提交存档与文档同步，覆盖阶段A（Linux ARM64 后端）、阶段B（优化 Pass 补全）、阶段C（寄存器分配 + 调试信息 + 优化级别框架）、串联集成修复、最终全面审查。本次为收尾提交，不新增功能。

#### 一、三阶段成果总览

| 阶段 | 内容 | 提交 |
|------|------|------|
| 阶段A | Linux ARM64 后端（GAS+AAPCS64）+ 运行时平台无关改造 + CLI/驱动分发（createBackend）+ E2E 适配 | `c828b4f`（已推送） |
| 阶段B | 优化 Pass 补全：SSA（支配树+Phi）、LICM、强度削减、内联、TCO；-O0/-O1/-O2/-O3 组合 | `b46f33d` 相关（阶段C 一并） |
| 阶段C | 寄存器分配（reg_alloc 线性扫描）+ 调试信息（debug_info）+ 优化级别框架 | `b46f33d`（已推送） |
| 串联集成 | 修复 2 处链路断裂（parser location、debug_info 双前缀） | **本次收尾提交** |
| 最终审查 | 通读源码 + grep 交叉验证 + 编译/单测/E2E/边界抽查 → 未发现功能 bug | **本次收尾提交** |

#### 二、Linux ARM64 后端全链路跑通（本机 aarch64）

- **后端**：`codegen/arm64/arm64_codegen.cpp`（含 dispatch/i128/oop/vtable 子模块）+ `arm64_instructions.cpp`，GAS 汇编 + AAPCS64 调用约定
- **接入**：`backend_factory.cpp` 按目标平台分发（win-x64 → X64、linux-arm64 → ARM64）；`driver` 经 createBackend 指针分发
- **CLI/工具链**：linux-arm64 走 `as`（GAS）+ `g++ -c -std=c++17 -fno-exceptions -fno-rtti -DCNRT_LINUX_MAIN` + `g++ -no-pie` 链接；支持 `--target` 参数
- **E2E**：28/28（Linux ARM64 本机真实运行）；-O0 与 -O2 输出完全一致

#### 三、优化 Pass 补全（阶段B，平台无关）

| Pass | 挂载 | 说明 |
|------|------|------|
| SSA（opt/ssa + opt/cfg 支配树/自然循环） | -O3 | 汇合点变量级 Phi（复用 Load 结果寄存器，语义等价、幂等） |
| LICM（opt/licm） | -O2 | 循环不变量外提；Load/副作用指令结果不加入不变集合（排序/数组正确性关键） |
| 强度削减（opt/strength_reduce） | -O2 | `Mul x 2^n -> Shl`、无符号 `Div x 2^n -> Shr` |
| 内联（opt/inline） | -O3 | 阈值 16 指令；排除递归/入口/OOP/lambda/模块函数/参数 Store/AddrOf |
| TCO（opt/tail_call） | -O2 | 尾递归→参数槽 Store+跳回入口 |

**优化级别组合**：-O0 无优化；-O1 折叠+简化+复写+DCE；-O2 +CSE+跨块DCE+LICM+强度削减+TCO；-O3 +全局值传播+SSA+内联。三级别 E2E 输出完全一致（28/28）。

#### 四、寄存器分配 + 调试信息 + 优化级别框架（阶段C）

- **reg_alloc**：线性扫描分配器（活跃区间 def-use + 跨块活跃传播 → 按 start 扫描 → 溢出到栈槽）；仅 i64/u64/ptr 参与、仅被调用者保存寄存器；structReturn/i128/u128 返回函数强制关闭；-O2 起启用（x64），`--no-regalloc` 可关
- **debug_info**：源码行号映射表 + 汇编注释（x64 `; src:` / arm64 `// src:`）+ DWARF 扩展位预留；`--debug` 开关
- **优化级别框架**：CLI `-O0~-O3`/`--opt` 与 runOptLevel 正确联动；-O0/-O1 全栈帧完全不变（不引入回归）

#### 五、串联集成修复的 2 处链路断裂

1. **parser 表达式节点缺 location（源头断裂，两平台受影响）**
   - 现象：`--debug` 后汇编源码注释 0 条（x64/arm64 全优化级别），无任何报错（静默断裂）
   - 根因：`parser_expr.cpp` 中 CallExpr/IdentifierExpr/字面量/BinaryExpr/UnaryExpr/IndexExpr/MemberExpr/AssignmentExpr/TernaryExpr/CastExpr 未设置 `location`，IR 指令 `inst.loc` 无效，`DebugInfoCollector::isValidLoc` 过滤全部注释
   - 修复：全部表达式节点补充 `location` 赋值（叶子取 token 位置、复合取左操作数位置）
   - 验证：arm64/x64 `--debug` 均产出正确注释（行号正确）
2. **debug_info 双前缀注释**
   - 现象：修复①后注释显示为 `// // src:`（双斜杠）
   - 根因：`commentFor` 返回带前缀文本，而两个后端 `AsmWriter::comment()` 又自动加前缀
   - 修复：`commentFor` 改为返回纯文本 `src: 文件.cn:行`，前缀由 writer 统一添加；删除 `AsmCommentStyle`/`setCommentStyle` 死代码；同步更新单测
   - 验证：arm64 `// src:` 单斜杠、x64 `; src:` 单分号

#### 六、最终审查结论

- 审查方式：通读新增模块源码 + grep 交叉验证引用关系 + 实际执行编译/单测/E2E/边界抽查
- **结论：未发现会导致功能错误的 bug，无需修改任何代码**；需求真正实现、代码真正集成（非孤立组件）

#### 七、验证基线

- ✅ 编译零警告（-Wall -Wextra -Werror，Linux ARM64 本机）
- ✅ 单元测试 **964/964**（81 套件）
- ✅ E2E **28/28**（-O0 与 -O2 输出完全一致）
- ✅ 手动：hello 构建运行输出「你好，世界」；`--debug` 注释嵌入；x64 -O2 寄存器分配 push/pop 配对正确

#### 八、遗留项（诚实标注，不影响本次收尾）

1. **arm64 寄存器分配保守默认关闭**：arm64 后端结果写回点分散于 35 处 emitStackStore（未收敛于 resultText 单点），全量接入回归风险高；已提供 `setRegAllocMap` 注入 API（模块独立可用、单测充分），默认保持全栈帧——待写回点收敛后启用
2. **x64_instructions.cpp 存量超行数**：单文件超 1000 行约束，待后续拆分（本收尾不改动）
