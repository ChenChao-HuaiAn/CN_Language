# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十一轮 111-a：E2E runner win 双编译对照原生支持（C15）+ size 说明符扫描门禁（C14）+ 新缺陷 221 双侧收口**
> （2026-09-13，家机 win-x64；基线 fc5c20f）：
> ①**C15 落地**：win 分支 v2 产物按用例隔离（`target/audit2/v2work<编号>`，对齐 linux——独立 cwd +
> stdlib junction + 入口绝对路径）；**双编译对照放开 win-x64**（守卫三平台化；宿主侧 `hostout.exe`
> 适配——win 无后缀 exe 不可运行）；win 侧 v2p 单点就绪函数 + 池前预热 + **统一并行**（v2 桶由串行
> 放开）→ **31 个平台守卫用例全部转真跑通过**，win 门禁由「292 用例 259 过/33 败」跃为
> **290 过 / 2 败 / 0 未实现**（2 败=78/79 OOM 既有败面）。
> ②**C14 落地**：runner 内联 asm size 说明符扫描器（movsd/movss 裸内存操作数）+ 正/负样本自检
> （探测器本身也是被测对象——110-a 教训）+ 挂点：win v2 闭环 ml64 前 / 双编译对照宿主产物。
> ③**新缺陷 221 双侧根治**（首轮 win 真跑暴露）：`221_双编译对照映射集合所有权` v2 退出码 254
> （形态①校验位丢失）——根因=**宿主 win 后端 epilogue 返回装载缺类型宽度分派**（8 字节
> `mov rax,[slot]` → rax 高 32 位栈垃圾；宿主 linux/arm64 后端均按宽度装载=win 单侧分叉）→
> v2 消费 `cmp rax,0` 被污染。**修复**=宿主 `x64_codegen.cpp` 按 `widthFor` 装载 + **v2 三后端
> 调用返回规范化**（T_布尔 `movzx eax,al`｜**T_整32 `movsxd rax,eax` 符号扩展**｜T_正32
> `mov eax,eax`；ARM64 `uxtb`/`sxtw`/`mov w0,w0`；X64L 同 win 语法）。
> ④**第二轮修正（锚定链为判据）**：v2 首版 T_整32 零扩展 → **自编译退化**（cn_self111 编译 v2
> 全树产出 12969/406115 行空壳函数）——v2 内部整32 为符号扩展表示（探针实证 `整32(-1)`→-1），
> 零扩展破坏负值语义 → 改 `movsxd` 后固定点恢复。
> ⑤**反证双向命中**：脏返回 obj（asm 还原 `mov rax` 重汇编）+ v2 无修复（产物删指令）→ **254 复现**；
> + 有修复 → **255**（产物级注入/剔除法，不依赖旧二进制）。
> ⑥门禁：宿主零警告重建 + 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 行**
> （两代逐字节 + 绑定自检）+ 组件对拍 **34/34** + 运行级 **3/3**。
> ⑦诚实边界：X64L/ARM64 规范化修复待 linux/arm64 跨机**动态**复验（静态产物已验证：X64L 10 处
> movzx / ARM64 10 处 uxtb）；探针脚本留 `target/p111win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
