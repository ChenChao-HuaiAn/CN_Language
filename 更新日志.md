# CN语言编译器 更新日志

## 功能完善总结

# 第五十轮（2026-09-10 深度系统 x86_64）：无符号语义轮——46-f 用户裁决方案A 实施，v2 全链对齐 Rust u32/u64 硬件无符号语义 + 宿主侧假阳性定性修正（零改动实证健全）+ v2self 固定点复锚（235667 行）+ 组件对拍 29/29 + 运行级 3/3

按第四十九轮 46-f 三方案呈报，用户裁决**方案 A（双侧全链对齐 Rust u32/u64 硬件无符号语义，性能第一/安全第二双达标）**。实施前复核揪出第四十九轮呈报中的宿主侧假阳性并修正定性；v2 侧四缺口全链根治。

## 一、宿主侧定性修正（实施前复核，零源码改动）

- **真分叉探针实证宿主无符号语义健全**：构造「有符号/无符号解释结果相异」的鉴别形态（`2^63 < 2^63-1`：有符号读为真、无符号为假；u32 max 边界；`2^63/3` 全精度除法；逻辑右移）——宿主 emitCompare setb/seta/setbe/setae 分派（修复4）、emitDivMod div+xor edx,edx（修复3）、emitShift shr、emitCast u32→u64 零扩展（mov eax）全部正确工作。
- **第四十九轮「宿主比较 rc=11 判假」系矩阵探针断言方向读反的假阳性**：`如果 (大 > 100) { 返回 11; }` rc=11 是进了分支=比较为真=行为正确。教训入 lessons（权重 7）：正/负断言方向必须显式核对；鉴别形态必须两侧解释相异——同真/同假形态对分派缺陷零鉴别力。
- u32 零扩展+i64 有符号比较=保序数学正确；混合符号比较（u32 vs i64 变量）语义维持宽松宽化现状（Rust 拒绝该形态，严格化随类型系统严格轮须规范审批）。

## 二、v2 侧根治（方案 A 全链实施）

- **独立类型码**：T_正32=70 / T_正64=71（IR 操作码尾部空区 68/69 顺延，全表零同值——同值常量缺陷防御；原与 T_整32/64 同码归并，运算层无从分派无符号）。
- **类型传播链拆分**：类型码() 归一拆分（正32/正64 自 T_整64 分支出——寄存器类型登记链自动传播）+ 类型宽度码 拆分（正32 字段 4 字节标注）+ 生成表达式 二元指令类型字段=操作数寄存器登记类型（原恒 T_整64，无符号性在发射点丢失；比较产物仍 T_布尔）。
- **三后端按码分派**：X64L 与 win——除/余 `xor edx,edx+div`（原恒 cqo+idiv）、右移 `shr`（原恒 sar）、比较 `setb/seta/setbe/setae`（原恒 setl 族）；ARM64——`udiv`（原 sdiv）、`lsr`（原 asr）、条件码 `lo/hi/ls/hs`（原 lt/gt/le/ge）。
- **正32 存储截断**：正32存储截断 单一事实源助手（目标类型=正32 时存储前 IR_位与 0xFFFFFFFF——wrapping mod 2^32，对齐宿主 u32 4 字节槽天然截断；接入变量声明/赋值两存储点，非正32 零开销直通）。
- **32 位零扩展读回**：三后端访存（指针加载/存储）与类型转换的 T_正32 分派并入 32 位分支——`mov eax` 清高 32/`ldr w` 天然零扩展/`uxtw`（原 movsxd/sxtw 符号扩展会把 4000000000 读成负）。

## 三、新增 E2E 双用例

- **191_无符号语义**（宿主 stdout 六锚）：`2^63/3`=3074457345618258602、`2^63>>1`=4611686018427387904、`(4000000000+4000000000) mod 2^32`=3705032704、`2^63>100`=1、`2^63<2^63-1`=0、`u32 max+1 边界`=0。
- **191_v2_无符号语义**（v2 闭环 rc=127 七面校验和）：除法+1/右移+2/截断+4/无符号比较+8/小于为假+16/u32 max 边界+32/除法全精度+64。修复前 v2 除法 rc=12（idiv 出负）、截断 rc=13（64 位全宽）实锤形态全部转绿。

## 四、全量门禁与自举锚定链

- 清场重建 GCC 零警告 rc=0 + 全量单测 **1251/1251 PASS** + 全量 E2E linux-x86_64 **197 用例 193 过 / 0 败 / 4 跳**（跳=62/69/78/79 既有平台限制；197 = 上轮 195 + 本轮新增 191/191_v2）。
- v2self 锚定链（X64L 口径复锚）：v2p 编译 v2 自身 → fix_p.s **235667 行**（上轮 232743 + 净增 2924 = 无符号类型面源码确定性体现）→ as + g++ 链接 cn_self50（cn_self50.o 在前 + v2p_linuxx64.o 借链 + rt48 objs 现编 + muldefs）→ 绑定自检：cn_main=0x4d701c ∈ cn_self50.o .text [0x402586, 0x4daa4e) → cn_self50 编译 v2 自身 → fix_s.s（235667 行）→ **固定点 fix_p ≡ fix_s 裸逐字节一致**。
- 组件对拍 **29/29 全量逐字节归零**；运行级三用例 3/3（hello rc=0 你好，世界 / 119 rc=14 / 173 rc=173）。

## 诚实边界

- win/ARM64 后端无符号发射经静态论证（汇编形态与宿主既有分派对称——本机无 ml64、单位机不在手），动态验证 = 下一跨机轮。
- arm64 侧复验/固定点复锚（302433 行口径因第四十九/五十轮 v2 源码变更预期增长）= 单位机下一跨机轮主线；win 侧（235968 行）同理待重锚。
- 混合符号比较（u32 vs i64 变量）语义维持宽松宽化现状；严格化（Rust 对齐=拒绝）随类型系统严格轮，涉规范变更须用户审批。
- 探针与验证脚本留 target/p50x64l/ 不入库（run_chain50.sh / map_check50.py / runlevel50.sh / cmp29.sh）。
