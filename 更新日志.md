# 更新日志

## 功能完善总结

### v2.1 语法规范变更：成员访问统一 "."，废除 "->"（用户裁决 2026-09-03，家机 win-x64）

**变更内容**（对齐 Rust/Go 与旧版 Rust 风格 CN——用户提出「统一规范，访问形式统一用 . 去除 ->」，
三项裁决：一级自动解引用 / 硬错误+迁移提示 / 本轮直接实施）：

- **`.` 左侧为指针时自动解引用一级**（`点* q` 可写 `q.x`，≡ `(*q).x`，Go 先例）；
  多级指针（`点**`）不继续解引用——语义层报错可见化。类/结构体/联合体/接口/自身指针
  行为统一（原接口指针与自身本就用 `.`，结构体/类指针须 `->` 的双轨制废除）。
- **`->` 成员访问硬错误**：宿主解析层报「'->' 成员访问已废除（v2.1 成员访问统一 '.'，
  指针自动解引用一级）：请改用 '.'……」+ 迁移示例；恢复策略按 . 折叠防级联报错。
  v2 侧同构硬错误（运行时错误(302) 行号码面包屑惯例）。**注意：`->` 记号仍用于
  函数返回类型与 lambda 返回标注，与成员访问无关。**

**实现要点**：
- 宿主：`MemberExpr.isArrow` 改名 `isDerefAccess` 并改为**语义层按对象类型置位**
  （解析层恒 false，语法上只剩 `.`）；语义层 structType 统一剥指针一级（自身 this
  专门分支并入）+ 多级指针报错；IR 层逻辑零改动仅跟随改名（虚调用处改纯类型驱动
  ——方法调用路径不经过 visitMemberExpr，不能依赖标记）；运算符重载表删 `->` 项
  （Operator::Arrow 枚举移除）；可重载集合同步收窄。
- v2：语法分析.cn 成员分支只认 `.`（`->` 硬错误）；IR容器.cn 15 处 `出->字段`/
  `签->字段` 改写为 `.`（原 ./-> 本就同路径同语义，语义/IR 零对齐工作）。

**连带当场根治两处既有宿主缺陷**（E2E 134 写回全形态锚定当场揪出，缺陷零容忍）：
- ① 嵌套成员链第一层为指针（`外层* wq; wq.内.x`）——ir_expr 嵌套分支对
  `findStruct("外层*")` 不剥指针 → objSrcType 空 → 字段读**静默降级常量 0**
  （旧 `->` 语法同形态同缺陷，旧用例嵌套链均隔下标层走 IndexExpr 分支未踩中）。
- ② 显式解引用成员 `(*q).x`——objSrcType 分支表不认 UnaryExpr(Deref) 对象 →
  同族静默常量 0（v2.1 后 `p.字段` ≡ `(*p).字段` 两形态必须等价互换）。

**测试与验证**：
- 新增 E2E：134_成员访问统一点号（读/写/自增/自减/复合赋值/嵌套链/解引用等价/
  值对象不变式，退出码锚定）、135_箭头成员访问拒绝（负测，诊断含「成员访问已废除」）、
  136_多级指针不自动解引用（负测，诊断含「不自动解引用」）。
- 改写既有用例源码中 16 行非法化语法：07（2）、09（12，含 1 处 `))->分数` 括号前缀
  漏网形态）、28（1）。
- 单测更新 6 文件：isArrow 断言 → isDerefAccess/`.` 断言 + `->` 拒绝负测。
- 全量门禁：构建零警告 + 单测 **1207/1207** + E2E **138 用例 136 过 / 2 败**
  （78/79 已知遗留内存超限，单跑复现 4656MB 超限与上轮形态一致，零回归）。

**规范文档同步**：plans/001 四处（4.4 运算符表/重载表、4.5 优先级表、5.7 记录标注）；
cn-language-spec 05（EBNF+示例）/01b（指针运算符 3→2/重载表/优先级表）/06（跨引用行）
——.zcode 与 .agents 双副本已同步（注意：两副本在 02/04 尚有历史分叉待另行收口）。
