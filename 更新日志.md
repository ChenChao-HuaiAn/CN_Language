# 功能完善总结

> 79-a 聚合拥有型字符串字段 drop glue（2026-09-12 第七十九轮，深度机 linux-x86_64）：
> plans/020 矩阵靶子 #16（装箱字段接收）/ #17（结构体字段持串）销项——用户令「继续后续
> 任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案（性能第一、安全第二）」。
> 探针 P1~P4 实证：结构体字段持串残留 1、装箱值字段残留 1（宿主/v2 同）；同族容器元素缺口
> 一并收口；实施中揪出并当轮根治宿主/v2 两枚既有缺陷（`无` 初始化段错误、`.错误` 载荷类型分派）。

## 一、缺陷实证（探针 P1~P4；每形态块出口 `内存::活动分配数()` 差值）

| 形态 | 宿主 | v2 |
|---|---|---|
| 结构体字段持串（`盒子 箱 = 盒子{ 名 = 字符串复制(...) }`） | **残留 1** | **残留 1** |
| 装箱值字段（`结果<字符串,整32> 果 = 正常(字符串复制(...))`） | **残留 1** | **残留 1** |
| 数组元素持串（`字符串[3] 数组`） | 残留 2 | **数组类型整体缺口**（语法层不解析） |
| 字段容器接收者（B35） | 残留 2 | —（靶子维持） |

实施中另发现两枚既有缺陷（探针 P5/P11，基线复现）：
① 宿主 `可选<T> 空 = 无;` 按 CopyStruct **从地址 0 拷贝=段错误**（gdb 实证）；
② v2 `结果<字符串,E>.错误` 载荷类型误取 T + 联合体偏移按当前载荷单侧定
（写入偏移 4 / 读取偏移 8）→ **错误码判别静默失效**（三侧对照：宿主 5 / v2 空或垃圾）。

## 二、方案裁决（三方案；性能第一、安全第二，Rust drop glue 经验）

| 方案 | 性能 | 安全 | 裁决 |
|---|---|---|---|
| **甲：聚合拥有型字符串字段 drop glue 全链**（写入位归一化 + 拷贝位深拷 + 消亡位释放 + 返回移出豁免；联合体条件释放） | **最优**：编译期展开零运行时元数据；无串字段类型零额外指令（纯 memcpy）；`转移()` 保留零拷贝 move 路径 | 高：与 74-a/76-a 容器归一化同构、与规格书 10.3 拥有型字符串裁决一致；浅拷共享→深拷（唯一持有者）杜绝悬垂/双释 | ✅ 实施 |
| 乙：只加释放面（浅拷 + 唯一持有者判定） | — | 低：无静态唯一持有者判据（`乙=甲` 双端句柄，释放任一即悬垂、都不释放即泄漏） | ✗ |
| 丙：Move 化（结构体赋值/传参/返回改 move，源失效） | 最优（零拷贝） | 需语言级语义变更（值语义契约/ABI/既有用例全受影响） | ✗ 登记后续专项 |

## 三、实施（宿主 + v2 双侧；机制主体 + 不变量两行清单）

**机制主体**：聚合类型（结构体/结果/可选）的字符串字段 = 拥有型槽位——写入位归一化
（转移=move／拥有返回=接管／字面量=驻留／其余=复制落堆）、拷贝位深拷（编译器代写
`__cn_str_copy`）、消亡位按偏移释放（free+清槽）。
**不变量**：①释放+清槽幂等（多路径共享槽）；②返回移出跳过释放（所有权移交调用方接管）；
③联合体字段条件释放（结果/可选 值/错误 同偏移，非激活分支值位=整数，无条件 free 即崩）；
④深拷先复制后释放 + 自赋值 no-op；⑤入容器共享源字段污染（宁漏勿错）。

**宿主**：新增 `ir/ir_fields.cpp`（收集/释放发射/深拷三件套单一归属）；`ir.hpp` 名单与
基线（`ownedFieldOrder_`/`scopeFieldBase_`/上下文 fieldBase/`fieldTainted_`）；
`ir_stmt.cpp` 登记 + 块基线 + 声明位深拷 + `无` 初始化修复；`ir_oop_call.cpp` 三处释放
路径 + 归一化重构（已求值版本 `normalizeStringValueSource`——避免二次求值泄漏）+ 入容器
字段污染；`ir_expr.cpp` 字段写入归一化（先求值+复制后 free 旧值）/字面量字段归一化/
整体赋值深拷（自赋值 no-op）。

**v2**：资源字段族命名归一（容器字段→资源字段，字符串字段纳入同表同名单）+ 细分计数；
语法层串字段登记；`IR容器.cn` 释放分派（字符串=free+清槽 / 嵌套=递归 / 容器=原路径 /
结果可选=**条件释放**）+ 串字段清旧/深拷/整体拷贝三助手；`生成声明/生成语句/生成表达式
（求值）` 三处归一化与浅拷/深拷分派；`.错误` 载荷类型按成员名分派 + 联合体偏移
max(T,E) 读写两侧一致（含 `生成装箱构造` 写入侧）。

## 四、防线与反证

**E2E 230_双编译对照字段所有权**（移植纪律 9 第六个对照用例）：八形态（结构体字段初始化/
整体拷贝深拷+字段写入归一化+自赋值/结构体返回接收/装箱正常/装箱错误（联合体条件释放）/
可选<字符串>/循环体 20 万轮/装箱返回接收双路）折校验和 **255**，两侧退出码一致。

**反证实证**：注入「宿主聚合登记失效」→ 宿主退出码 255→**80**；注入「v2 资源字段登记
失效」→ v2 退出码 255→**248**（两侧已恢复，残留=0）。

## 五、验证链（本机口径）

清场重建**零警告** + 单测 **1306/1306** + E2E **257 用例 253 过 0 失败**/4 跳
（+230 新用例）+ 锚定链固定点 **309101 行**（新口径，上轮 301619；+7482=v2 源码增量）
逐字节一致 + 运行级 3/3 + 对拍 + 释放路径两侧对照。

## 六、诚实边界（登记）

结构体按值传参浅拷（参数=借出视图，被调方不释放=零拷贝，与 A2 契约一致）；容器元素=
含串字段结构体（字段污染+元素字段释放面未开=靶子，深拷随容器元素专项）；数组元素持串
（宿主靶子维持；**v2 数组类型整体缺口**独立立项）；结构体容器字段拷贝浅拷共享（两侧同缺
既有边界）；**v2 内置构造器识别依赖文件含结构体声明**（既有缺陷，基线复现：全文件无
结构体声明时 `错误(...)` 未展开 → 未定义符号链接失败）=下一轮 v2 独立缺陷专项。
