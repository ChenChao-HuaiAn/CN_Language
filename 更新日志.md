## 功能完善总结

**本轮（2026-09-07 第二十八轮，深度系统 x86_64）**：plans/017 两灰色点根治（用户裁决两项均方案A·Rust 准绳：性能第一/安全第二）+ 文档收口。

### 一、缺陷 1 根治：IR容器 子链冒烟 exit=1（第二十二轮披露灰色点闭环）

- **根因两层**：①簇①根治·A2（协议常量单一归属）迁移遗漏 5 常量——节点_调用=17/节点_成员=18/节点_下标=19/成员方法标记=-2/节点_二元运算=15 滞留语法分析.cn（IR常量.cn 节点表恰缺 15/17/18/19）；②布局层——常量补迁后语义过、暴露 IR 层 64 错「成员字段偏移未知」（与 节点 五字段完全一致）：`结构体 节点` 布局定义滞留语法分析.cn，变量结构体表 登记源不在 IR容器 导入链上。
- **修复**（CN语言编译器v2/IR常量.cn+语法分析.cn）：5 常量+`结构体 节点` 布局整体迁入 IR常量.cn「AST 节点类型」区（协议常量与协议布局同区单一归属，呈报①A 下沉模式）；语法分析.cn 删除本地定义（已导入 IR常量 消费）。Rust 对齐：single source of truth+编译期常量折叠零性能开销+缺定义=编译期错误 fail fast，绝不依赖多文件合并可见性掩盖。
- **验证**：13 组件+min 冒烟 **13/13 全绿（IR容器 子链自第二十一轮以来首次转绿）**；组件产物对拍 vs 上代：5 文件裸逐字节一致+7 文件差异 100%=strData 驻留 ID 平移（规范化后逐字节一致=指令语义零差异）。

### 二、缺陷 2 根治：入口打开失败回退内置源码掩盖编排错误（第二十七轮观察项闭环）

- **修复**（CN语言编译器v2/主.cn）：新增 显式入口 标记（命令行参数 1 提供时置真）；显式入口打开失败 → [p6f] 打印后返回 2 中止（不产 IR/asm）——对齐 rustc「入口不存在=编译失败退出」绝不静默编译别的源码；仅无参自检模式（默认 target/v2src.cn）保留内置回退（设计特性不变）。
- **验证**：显式不存在入口 rc=2+无 asm ✓；无参模式 rc=0+回退+asm ✓；E2E 全部用例入口真实存在零影响。

### 三、v2self 新一代重编锚定复验（改动后）

v2p_gen3 → v2self_gen3 重编 193068 行 asm（上代 192288，+780=本轮净增量）→ 全链产物规范化一致 + **家族字节级固定点保持（g4=g5 裸逐字节一致）** + 8 用例运行级对拍 8/8 PASS（119/123/126/128/130/148/150/158）。

### 四、文档收口

plans/017 任务复选框补勾 T1/T2/T5/T6/T7/T8（第八节+历轮实证）；plans/016 遗留项勾销注记；plans/014 状态表第二十八轮行；lessons 假对拍第四案（对拍脚本目录迁移路径依赖未随迁）。

### 五、门禁

构建零警告（GCC -Wall -Wextra -Werror）+ 单测 1240/1240 + E2E linux-x86_64 全量（见提交信息）；诚实边界：win/arm64 侧复验=跨机轮（本轮 v2 源码三文件改动为非后端组件，arm64 路径静态论证零影响）。
