# CN语言编译器 更新日志

## 功能完善总结

**本轮（2026-09-07 第三十轮，家机 win-x64）**：跨机轮 win 半程收官——拉取第二十九轮（605fc3a）后按 HANDOFF「下一步」执行家机全量门禁复验，纯验证轮零代码改动。跨机轮（第二十九轮 arm64 + 第三十轮 win）至此全半程闭环。

### 一、静态论证：第二十九轮两修复对 win 路径零改动（三点确认）

1. **win_x64 后端目录零改动**：`git diff 7650713 605fc3a -- src/cn_compiler/codegen/win_x64/` 为空——本轮变更面仅 arm64 后端 3 文件 + linux_x64 后端 2 文件 + 单测 2 文件 + run_e2e.py 平台参数化 + v2 CN 源码 9 文件。
2. **共享层（ir/driver/frontend/opt）零改动**：`git diff --stat` 为空，宿主 win 链路（词法→语法→语义→IR→win 代码生成→ml64/link）输入完全不变。
3. **两修复本身与 win 无交集**：①linux_x64 `stackParamAnchorBase()`（needHiddenRet?24:16）与原 `stackAnchor = 16 + 8*paramOffset` 数学等价（paramOffset 0→16 / 1→24）=纯死分支加固行为零变化，且仅存在于 linux_x64 独立目录；②arm64 `.align 3` 仅触 arm64 后端与 v2 代码生成ARM64.cn，win 后端（MASM）无 .data 对齐重定位问题面。

### 二、家机全量门禁（ci.ps1）：全绿

- **构建零警告**（MSVC /W4 /WX）。
- **单测 1243/1243**（与 arm64 侧一致，含第二十九轮新增 3 项 arm64 锚定测——纯代码生成文本断言，win 上同跑同过）。
- **E2E win-x64 全量 160 用例 158 过/2 败/0 跳**——败仅 78_chain_build（工作集 4131MB）/79（4132MB）已知 OOM 家族（v1 链内存超限被内存保护终止，基线 78=4115MB/79=4097MB，零回归）。

### 三、新增 6 用例 win 侧首验全过（本轮门禁增量面）

- **153/154**（linuxx64_ 参数拷贝临时寄存器/隐藏返回指针栈参数锚定）：win-x64 无平台跳过规则，以普通用例在 win 后端全链编译运行 PASS——运行级实证「win 后端栈参锚定本来就正确」+「linux_x64 收敛修复对 win 零影响」。
- **155 容器引用参数与所有权语义**（跨平台用例）win 首验 PASS。
- **156/157**（v2 结构体整体赋值/函数重载消歧）+ **158**（v2 SysV ABI 探针，win 走 ml64/link v2 闭环原生路径）全过——v2 闭环 26 例 win 侧动态全绿。

### 四、跨机轮收官（三平台门禁矩阵）

| 平台 | 轮次 | E2E 全量 | 唯一败因 |
|---|---|---|---|
| linux-x86_64（深度机） | 第二十六~二十八轮 | 160 用例 156 过/0 败/4 跳 | 无（4 跳=62/69/78/79） |
| linux-arm64（单位机） | 第二十九轮 | 160 用例 156 过/1 败/3 跳 | 78 已知 OOM |
| win-x64（家机） | 第三十轮（本轮） | 160 用例 158 过/2 败/0 跳 | 78/79 已知 OOM |

三平台唯一失败面收敛于已知 OOM 家族（旧架构固有，v2 重建动机），第二十九轮两修复经三平台动态验证零回归。v2self 三级锚定（组件对拍 13/13+全链+字节级固定点 g1=g2）已在 arm64 与 x86_64 双架构达成。
