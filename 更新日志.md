# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百二十六轮 126-a：D1 函数级残余波开波——`emitCast` 270 行提取 3 子方法**（2026-09-14 凌晨，家机 win-x64；基线 6754aca）：
> ①**函数级重构**（纯重构零行为变更）：`x64_instructions_cast.cpp` 的 `emitCast`（**270 行**）→ 主函数 **~30 行**
> + 3 个私有子方法：`emitCastFloatFamily`（86）/`emitCastPtrAndFloatPair`（32）/`emitCastIntWidth`（121）；
> 变换方式=**early-return 链机械改为返回布尔**（`return;` → `return true;`；调用处 `if (子方法(...)) return;`）——
> **各段原顺序、原条件、原指令序列逐字保持不变**；hpp 加 3 处声明（**按各子方法实际用到的参数裁剪签名**——
> C4100 未引用参数在 /WX 下即错误，首跑即被拦截并修正）；
> ②**验证**：构建零警告 + **产物 md5 4 样本逐字节一致** + **锚定链固定点 406097 行不变** + 对拍 44/44 +
> 单测 1313/1313 + E2E 290/292 + 运行级 3/3；
> ③**踩坑（入 lessons）**：生成脚本签名模板换行符与文件实际不一致（`
` vs `
`）导致修正脚本 assert 失败——
> 生成/修正脚本对同一文件必须统一换行基准。
> ④D1 剩余：函数级 18 超百行（**提取模式已跑通，可直接复用**）。
>
> **⚠️ 待用户裁决（最高优先级 actionable，已九度呈报）**：`plans/022` §九 **8 项**（含第 8 项「波 1 修 `字符*` 拼接误报——可立即开工」）。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
