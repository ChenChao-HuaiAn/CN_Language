# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十四轮 114-a：D1 行数整改第五波——`ir_oop_call.cpp` 1155→763 行按族拆分**（2026-09-13，家机 win-x64；基线 acaf51c）：
> ①**拆分**（纯重构零行为变更）：`ir_oop_call.cpp` **1155→763 行**，拆出 `ir_oop_call_release.cpp`
> （类析构/拥有型字符串释放族 **408 行**：genStringFrees + genClassDestructorCalls + emitStringFreeFor +
> emitClassDeleteFor + genBlockExitDestruct + emitStrArrayElemFreesFor + genJumpDestructFrom + static
> blockExitSrcName〔族内唯一引用点随族〕）；②等价性=产物 asm 五样本逐字节一致；③门禁：零警告 +
> 单测 **1313/1313** + 全量 E2E **290/292** + 锚定链固定点 **406097 连续第三次不变** + 对拍 **34/34** +
> 运行级 **3/3**；④教训：本轮无新坑（112-a 方法论第三次复用——**标准化 checklist 让第三轮零意外**）。
>
> **本会话 D1 三轮累计**（同款验证口径）：
> - **112-a**：`ir_expr.cpp` 2798→**734 行**（四族拆出 `ir_expr_unary/assign/index/member.cpp`）——提交 6be2254
> - **113-a**：`ir_oop.cpp` 1381→**508 行**（拆出 `ir_oop_container.cpp` 512/`ir_oop_field.cpp` 389）——提交 acaf51c
> - **114-a**：`ir_oop_call.cpp` 1155→**763 行**（拆出 `ir_oop_call_release.cpp` 408）——本轮
> - 三轮合计 **-3.3k 行**（宿主三大文件全部 <1000；锚定链固定点 406097 连续一致、全量零回归）
>
> ⑤诚实边界：D1 后续波次未做——`semantic.cpp`（需先建 `semantic_internal.hpp`：`canonicalType` 四族共用）、
> `semantic_expr.cpp`/`semantic_call.cpp`/`ast.hpp`、codegen 面 4 文件、v2 侧 6 文件、函数级拆分
> （`visitAssignmentExpr` 931 行等）——均登记 plans/021 §3-D1。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
