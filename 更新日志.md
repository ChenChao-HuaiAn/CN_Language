# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段2「引用逃逸检查强化」双侧落地 + 两枚立案缺陷根治 + v2self 固定点复锚 244050 行（2026-09-10 第五十八轮 深度机 linux-x86_64）

编译期安全专项阶段 2 全链闭环（预审→E2E 双先红→宿主→v2→单测→全量门禁→锚定链）。

### 一、阶段 2 语言语义（引用逃逸编译期拒绝）

- **局部地址逃逸**：右值求值为当前函数局部的地址（`&局部` / 引用局部绑局部）而流入更长寿命存储时编译期拒绝——赋值位（静态/全局标识符、非局部对象的字段/元素）+ 返回位 T*（`返回 &x` / 返回指向局部的局部指针）。
- 合法面零误伤（预审+单测双证）：局部指针指向局部、局部对象字段/局部数组元素接收局部地址（随所在作用域消亡）、引用局部读写与传引用参、指向全局。

### 二、宿主实施（五文件）

- semantic.hpp/cpp：refLocalBases_（引用局部绑定表）+ ptrLocalPointees_（局部指针指向表）+ isLocalAddressValue（&局部经 refReturnLvalueBase 解剖 / 引用局部绑局部）+ 函数收尾清表。
- semantic_expr.cpp：visitAssignmentExpr 逃逸检查（非局部对象字段自行解剖对象链——refReturnLvalueBase 对指针解引用访问短路会放行静态指针字段，实测漏检后修正）+ 指向登记（赋值位）。
- semantic_decl.cpp：引用局部登记 + 指针指向声明位登记（`整64* p = &x;` 补丁）+ **缺陷①方案A**（推断回填扩展字符串——`变量 乙 = 甲;` 打印打指针值 4285000 根治；布尔维持 184 契约）。
- semantic_stmt.cpp：visitReturnStmt 指针返回局部地址拒绝（含经指针转接）。

### 三、v2 实施（缺陷②+逃逸三处）

- 语义分析.cn：是局部名（局部层判定）。
- 语义检查.cn：赋值逃逸（右值=&局部 × 左值非局部）+ 返回逃逸（返回类型尾 */& 时拦，**引用参数豁免**=当前函数引用参数们 静态集，宿主 isRefParamForCurrentFn 对齐）。
- 语法分析.cn：**缺陷②** 返回类型 `*` 后缀并入（`-> 整64*` 修复前 [parse] 缺分号；& 并入同款两路径）。

### 四、测试锚定

- E2E 198_引用逃逸拒绝（宿主负测五形态，真先红=子串锚新消息「存入静态/全局存储」）+ 199_推断声明字符串拷贝（缺陷①正测 左/左，先红 4285000）+ 198_v2（负路径 2 诊断+中止行实测固化）+ 199_v2（正路径 rc=3：指针返回 &引用参数+解引用读写回）四用例注册双通道。
- 单测 test_escape_check.cpp 10/10（负五正五）。

### 五、验证矩阵

- 清场重建零警告 + 单测 1274/1274（1264+10）+ E2E 213 用例 208 过/1 败（=194 既有待批）/4 跳（平台限制）。
- v2self 锚定链：v2p→fix_p **244050 行**（242022+2028）→cn_self58→fix_s 244050→**固定点逐字节一致** + 绑定自检 cn_main=0x4de72f∈cn_self58.o .text + 组件对拍 29/29 + 运行级 3/3（hello 你好，世界/119 rc=14/173 rc=173）。

### 六、诚实边界

- 逃逸检不出面：指针间传递链（p=q 后返回）、容器元素位（值语义深拷为主形态）、跨函数引用写回——阶段 3 随借用分析深化；v2 成员/下标目标位随引用面扩大对齐；v2 静态标量函数内按栈槽=独立观察项（宿主 .data 语义分叉）。
- 探针与脚本留 target/p58x64l/ 不入库。
