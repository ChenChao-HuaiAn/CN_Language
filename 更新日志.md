## 功能完善总结

### 一、②b 结构体元素容器收官：126 运行时 OOM 根治 + 四缺口一步到位（E2E 126 首次全绿）

- **OOM 根因确诊（三重实锤）**：v2 的 IR_指针存储/加载 无宽度概念——赋值存储类型写死 0、
  成员加载写死 T_整64，双后端固定 8 字节发射。126 段① `n.子终点 = 3`（整32@20）按 8 字节写
  越出 24 字节结构体 4 字节，恰好清零紧邻 `i` 槽 → `当(i<20)` 死循环 → 无限追加 → 堆 OOM
  SIGKILL（限内存 2GB 复现为干净退出码 201；asm 级 `str x10,[x9]` 证实；槽地址逐字节对上）。
- **B5 字段宽度读写 + 链式偏移收口**（IR容器/IR生成/语法分析/双后端）：IR容器 新增分桶
  字段偏移表、类型宽度码（窄宽码 T_整8/T_整16/T_字符）、查字段宽度码；成员访问三形态解析
  （标识符对象→变量结构体表；调用对象 `池.元素(i).字段`→方法签名返回类型分桶收口——根治
  语法层扁平回退同名字段跨结构体必错）；赋值存储按字段宽度；双后端 ldrb/ldrh/ldr w、
  strb/strh/str w 与 movzx/mov byte-word-dword ptr 对称发射。
- **B6 retbuf 按真实返回字节数定帧**（IR生成/代码生成共用/双后端）：函数IR 新增 retbuf字节数
  （模块级累计、生成函数回填）；共用层新增 retbuf槽位数/retbuf基槽 单点决策——根治固定
  2 槽=16 字节对 ≥17 字节结构体返回越界踩顶变量槽。
- **B7 符号供给机制**（run_e2e.py + 126/供给.cn）：v2闭环用例们 注册项扩展第 4 元=供给源列表
  （双平台），供给 .cn 拷贝为独立目录 主.cn 经宿主真实管线编译（宿主按规范08-四仅以 主.cn
  为入口模块），链接置于 v2p.obj 之前——用例自有结构体布局权威；供给文件实例化 向量$节点/
  向量$词条 全方法面（含资源字段深拷贝）。
- **B8 按值传参 ABI 补全**（IR生成/双后端 prologue）：被调方多槽拷贝（参数槽序们/参数拷贝
  字节们 声明序驱动；ARM64 字循环、MASM rep movsb）+ 普通函数调用结构体实参传址（函数
  实参结构们 表驱动回扫——B3 原仅覆盖容器方法；v2 自身 IR追加(指令,建IR指令(...)) 全是
  此形态，③ 自举必需）。临时用例验证读字段/拷贝语义（被调方修改不影响调用方）退出码 0。
- **连带新缺陷根治：裸块语句 `{}` 解析缺失**（语法分析/IR生成）：原 解析语句 无 `{` 分支 →
  解析块 循环保护静默中断 → 块内及后续全部语句（含 返回 0）丢失（126 垃圾退出码 20 的来源）。
  补裸块解析分支 + IR生成 预扫描跳过（排除控制流体块防双重生成）+ 节点_块 递归生成块。
- **B4 裁定（ABI 级事实）**：宿主「有析构」=显式声明析构方法；字符串为驻留不朽指针无释放
  语义——整64+字符串 结构体（词条）无析构可注入（深拷贝=指针拷贝），④a 零泄漏由绿色运行
  证实；B4 真正载体（拥有式容器字段）在 ②c。

### 二、验证（本机 Linux ARM64，诚实门禁）

- 单测 1196/1196；E2E linux-arm64 全量 128 用例 = 123 过 / 4 平台跳过（62/65/69/79）/
  1 失败（78 已知遗留内存超限，与本轮无关）；v2 全系 119/120/123/**125/126** 5/5 全绿——
  **126（布局/视图/传参/深拷贝析构四缺口载体）首次全绿**。
- MASM 产出 asm 级核验（dword ptr 存储/窄读 movzx/retbuf 帧尺寸/被调方 rep movsb prologue）；
  **win-x64 运行时门禁（119/120/123/125/126 含供给路径）待 Windows 会话执行，不写假绿**。
