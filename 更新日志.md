# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（🧪 M3 采样测试轮·2026-09-17）

**284-a 病灶② 根治的 win-x64 平台独立复验（双轨采样全绿·零新命中零新类别）**：轮 A 回归轨＝命中库 38 全量一致（病灶② 预期 7/7→0 实证达成）；轮 B/C 不同采样＝新种子 2026091711/12 各 100 新样本（避开深度机同日种子 01~03）各自 100/100 零分歧零失败，C 轮复放二次验证一致。前置：cn.exe 全量重建（原产物早于 97ca5c1 提交·按假重建陷阱教训）＋402 布尔短路矩阵O3 E2E 双绿确认根治在位。插曲：`target/cnsmith/work/分歧清单.txt` 为 09-16 残留（258-a 时代 A2070 旧现场·diff 零分歧时不清理）致编排脚本首轮误读 1360 条假命中——逐项证伪后删除残留并复放闭环；daemon 改进建议（--seed 自定义防三机撞种子＋零分歧清旧清单＋target-cn 同步重建）已留 HANDOFF 待开发轮采纳。**rebase/merge 深度机 285-a 两提交后交叉甄别→【定案】（已呈报）**：深度机两次判定（「O0 对 O3 错」→「O3 IR=常量布尔无条件存储·修优化层」）经本机 asm 三件套实证**反转**——真缺陷=**linux_x64 后端 O0 移位发射操作数宽度缺陷**（`mov r10d` 32 位零扩展装载负数+`sar r10` 64 位移位→-426 污染成 524286→变7=1；O3 用 64 位装载=-1=规范正确·win 32 位 sar eax 正确→变7 规范值=0）——修复面=linux_x64 发射层（32 位值用 32 位 sar 或 movsxd·非优化层·与 284-a 无关）·初判面=有符号右移 32 位负值 O0 直译（shl 免疫/win 免疫/arm64 待查）；E2E 420 未捕获=存量无此形态·**M3 采样独家捕获实证**。源码/测试/规范零改动（纯验证轮·基线 de4c0b6→终态含 285-a/286-a 认领与 4ad94f6 合并）。

## 深度机 linux-x86_64 节

## 功能完善总结（285-a·2026-09-17）

**380 升级 stdlib 导入形态（用户已批·全量 421=419/0/2 绿）**：

1. 380 用例删内联随机数类约 70 行+两行导入（随机数生成器/播种扩散）——双编译对照 PASS·校验和 31 不变（stdlib 接口同构·宿主+v2p 双侧候选4 兜底链路实证）。
2. T12 回归轨独立复验甄别立案：s2026091705 仍分歧（变7=1/0）=家机 284-a 修复未覆盖形态·待家机 marker 二分继续（深度机不越界）。
3. 教训：daemon 采样编译器=target/cn 构建产物·HEAD 前进后须重建再采样（滞后基线教训已留痕）。


## 单位机 ARM64 节

## 功能完善总结

### 257-a 插队修复轮：plans/021 §3-B 生命周期铁律根治（2026-09-16，单位机麒麟 ARM64，基线 f0f0696·认领 9818aa8·用户质询「B5 三犯」驱动）

1. **修复**：§3-B 清空 B5 整段「已裁决原文留档」（裁决/实施记录本已由 §3-F F1-26 行/§5 256-a 行承载）；§3-G 同型滞留的 T2（案戊）/T9（整数溢出方案 D）迁 §3-C（C17/C18）+处置建议段同步。
2. **立规**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」——条目一经用户裁决**当轮迁出**（实施明确的迁 §三-A、定案待排程的迁 §三-C、完成销项进 §4.2），禁留任何留档形式；呈报类条目须同步登记 §3-B。
3. **机械门禁**：check_handoff.py 扩 §3-B 拦截（「已裁决/已批/已选定/✅」完成标记即禁止提交）——反例（修复前 L80 B5 双标记命中·退出码 1）/正例（修复后 PASS）双向实证；源码零改动（纯文档+门禁脚本轮）。
4. **B6 裁决闭环**：分支策略建议（develop 单主干+里程碑标签强推+不采用功能分支）经用户批准（「同意」）当轮迁 §3-A **A12**（里程碑标签锚点建立·待认领）；B 区回空态。

**门禁**：check_handoff.py（新检查双向实证）+check_progress_sync.py PASS。

**诚实边界**：机械检查为字面拦截非语义判定（完成态措辞须避让·宁可严格）；github 镜像本机无凭据未推。
