# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结
> **第一百六十六轮 166-a：D1 函数级拆分第一波（`visitAssignmentExpr` 1034 行 → 25 个族子方法）**（2026-09-14，家机 win-x64；基线 586bff7）：
> ①**拆分面**：D1 排班「最大优先」首项——`src/cn_compiler/ir/ir_expr_assign.cpp` 的 `visitAssignmentExpr`（1034 行，全文件仅此一函数）
> 按「左值形态 → 处理族」两层提取为 **25 个子方法**（最大 96 行）：主入口分派（成员 / 下标·解引用 / 调用 / 标识符）+
> 成员族 6（构造字面量 / 类字段归一化 / 整体赋值 / 一般赋值 / 对象类型推导 / 字符串字段归一化）+ 下标族 4 + 调用族 1 +
> 标识符族 11（全局静态 / 类静态深拷 / 类对象深拷 / 构造字面量 / 一般赋值 / 右值类型推导 / 结构体整体赋值 / 字符串三形态）；
> `ir.hpp` 新增 23 个私有方法声明。**族边界勘定**：族内无文件级 static/匿名命名空间依赖。
> ②**纯重构纪律**：机械搬运（按段切分 + `return;` → `return true;` 等价包装：段内 return 原即整函数返回，调用点随即 return）
> + 编译告警双坑校正（C4702 不可达 / C4715 路径缺返回值——按「段末尾语句形态」区分是否补 `return false;`）。
> ③**★过程中捕获并根治一处拆分引入的语义偏差**（等价性硬证据的价值实证）：`identifierStringAssign`（byRef 引用参数 /
> transfer 转移 / 一般 三分支）的调用点 `identifierGenericAssign` **漏判返回值** → 引用参数字符串赋值（`字符串& 出 = 源`）
> 命中 byRef 分支后**继续执行**尾部「引用参数写回」（重复写回 + 栈帧 +16 字节 + 2 个多余标签）。
> **发现路径**：4 个赋值密集用例 asm 逐字节一致，但 **v2 全树对拍 DIFF** → `cn ir` 输出对比定位（多出「加载 包名称 / 指针存储」两条）
> → 修正调用点 → **`解析货舱.cn` IR 逐字节一致**；修复后全量对拍一致。
> ④**等价性硬证据（新旧宿主编译器产物对拍，asm 逐字节 md5）**：① v2 全树（8.25MB asm）；② v2 树全部组件 + 主.cn；
> ③ 赋值密集 E2E 用例（183/253/116/276 等）；④ 暴露点组件 IR 输出逐字节一致（修复后）。
> ⑤**D1 残余实测校正**：扫描脚本固化 `scripts/check_fn_length.py`（口径 = `{}` 配对 ≥100 行）——**宿主 27 + v2 树 50 = 77 个**
> 超百行函数（165-a 记「70」系扫描脚本未留档的口径差异，本轮以脚本实测为准），首列 `semantic_call.cpp` visitCallExpr 885
> （原首列 `visitAssignmentExpr` 1034 已清零）。
> ⑥**门禁七面全绿**：零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过 / 0 败 / 0 跳过**（含 78_v2/79_v2 自举链）+
> 锚定链 fix_p ≡ fix_s 逐字节自洽（410415 行 wc 口径）+ 组件对拍 44/44（v2p vs cn_self；并发跑时 `IR容器.cn` 一度显示差异=与运行级脚本共用 `target/v2asm.asm` 的污染，**顺序复验逐字节一致**消解——纪律入 lessons 166 段）+ 宿主产物对拍全一致（含 **v2p_base.asm ≡ v2p.asm**）+ 运行级 3/3。
> ⑦**文档六件套**（本日志 + HANDOFF 家机节 + plans/021 戳/D1 行/索引 + plans/019 §7.2〔本轮行 + 补记 165 轮〕+ lessons 166 段 +
> 看板收工）。**D1 为长期滚动任务**：残余 77 个按「最大优先」继续排班（下一批首列 semantic_call.cpp visitCallExpr 885 /
> `生成语句.cn` 生成赋值语句 635 / ir_oop_call.cpp handleClassCallExpr 362）。
## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

## 功能完善总结

### 160-a 文档轮：三项批准落盘（2026-09-14，单位机麒麟 ARM64，文档轮，基线 ebf99ab；源码零改动；实施由家机认领）

1. **A5 扩围 +4 词（userselect 批准）**：`公开`/`私有`（冒号标签位，与已批 `保护` 同机制——消除同类不对称）+ `静态`/`常量`（声明前缀位）随 A5 同轮上下文化（6 词 → 10 词）；目标态保留字 **50 → 46**。落盘：`plans/024` §10.5 批准批注 + `常量` 判据勘误（**前瞻 1 token**：后随标识符/`:` → 声明；后随运算符 → 表达式——非此前「前瞻 2 token 三元序列」表述）+ 声明位清单补全（顶层＋局部＋for-init；宿主局部常量=真实 isConst 语义 vs v2 防御分支按 整64，实施轮盘点对齐）+ `plans/001` §2.1 四处同步（总数/子类目/上下文清单/变更记录）。
2. **新排班 §3-A A8（userselect 批准方案A）**：**冒号后置类型标注移除**——宿主 3 处私藏规范外语法（参数 parser.cpp:361 / 变量·常量声明通道 :598-599 / 顶层常量 :830-833；实测 `函数 加倍(x: 整32)` 等全通过），cn-language-spec 全集与 plans/001 均无此语法且 spec 03 明确否决后置标注；双侧分叉（v2 语法层零支持）+ 自举隐患 + **本仓库存量零使用**（移除零迁移）。实施=移除 3 处 Colon 分支 + 负测锚定（冒号形态必须报错，宿主/v2 双侧）+ 全链门禁；**顺序约束：A8 先行、A5 随后**（A5 歧义判据不建在待移除形态上）。
3. **C16 追加（userselect 批准）**：spec 03 循环 EBNF 细化（init 位声明三形态：`变量`/`常量`/类型前缀——现 EBNF 仅「初始化表达式」未细化）随 C16 规范落盘轮一并补写。
4. 同步：`plans/021`（戳刷 160 + A5 扩围 + A8 新排班 + C16 追加 + §5 索引）/ `plans/019` §7.3 追加 160-a 行 / lessons 第一百六十轮段 / HANDOFF 单位机节（家机实施入口）。
