# 功能完善总结

## 72-a 六枚缺陷同轮根治：跨模块拥有契约失效 / 赋值位转移非真 move / 引用出参悬垂 / 变参 AL 契约 / v2 落空路径零释放（方案A 用户默认授权）

**立案背景**：按 HANDOFF「4'-三 收尾评估」清单逐项写探针实测（不读代码推断），发现五枚**静默缺陷**（三枚 UAF/永久泄漏级别）+ 一枚需裁决（F3 改变既有测试语义）：

| 编号 | 缺陷 | 现象/实证 | 修复 |
|---|---|---|---|
| F1 | **跨模块拥有契约失效**（静默永久泄漏） | `sigKey.find('$')` 判泛型单态化产物，但跨模块链接键也是 `模块$名` → 凡导入模块函数返回 字符串 调用方不登记 RAII。IR 对照：`[本地造串]` 有 free / `[工具$造串]` 无；stdlib `替换` 跨模块调用活动分配残留 1 | 改按 `genericFuncInstances_` 精确判定（rustc DefId 同款）；v2 侧三处纯名查改 `查函数返回回退` 键基回退版 |
| F4 | **赋值位转移非真 move**（UAF） | `目标 = 转移(源)` 共享句柄 + 源出口释放 → 悬垂。stdlib `字符串扩展:72` `缓冲 = 转移(拼接)` 实测输出乱码 | 赋值位新增真 move 分支（drop 旧目标 + 句柄直存 + 源槽清零；自转移豁免） |
| F5 | **引用参数出参悬垂**（UAF） | `字符串& 输出` 写本地拥有串后本函数出口释放源局部。探针读「73」；v2 树 解析货舱/拆限定名 踩中（E2E 165 回归） | Rust `*out = s` move 语义（句柄直写调用方槽 + 源槽清零） |
| F6 | **变参调用未设 AL**（SysV ABI 违约） | AMD64 SysV 要求变参调用点 AL=向量寄存器个数（glibc va_arg 据此判定）——CN 从未发射，靠 rax 残留侥幸。`格式化("¥%.2f", 29.50)` 经包装函数输出 ¥0.00 | 调用点按实参统计发射 `mov eax, N` |
| F2 | **v2 落空路径零释放**（永久泄漏） | v2 只在 返回 语句路径发射释放，体末无 返回（fall-through）的函数完全不释放（asm 无 `__cn_str_free`） | `生成函数` 体块未终止时在函数尾块前发射出口释放（已终止路径由 返回 语句路径负责——首版无条件发射致 10 个 v2 用例回归=二次释放，条件化后零回归） |
| F3 | 宿主块级作用域 RAII（循环体内声明泄漏） | 探针：循环体内声明 4 轮残留 3（字符串/容器/嵌套块）；v2 侧有块出口析构模型、宿主为函数级 | **回退呈报**：实现后改变 115/116/76 既有测试断言的析构时机语义，按「测试文件不可擅自修改」纪律回退，待用户裁决 |

**验证链全绿**：清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 1289/1289 + 全量 E2E **234 用例 229 过**（唯一败=194 既有待批）/4 跳 + 锚定链 fix_p **266964 行**（新口径，266459+505=v2 树修复源码确定性）≡fix_s 逐字节一致 + 绑定自检 + 运行级 3/3 + 组件对拍 29/29。

**E2E 新增**：212_跨模块契约与转移与引用参数（八形态校验和 255）+ 212_v2_落空路径与拥有串释放（校验和 7）。

**诚实边界**：F3 未生效（循环体内声明仍为函数级释放=中间迭代泄漏，待裁决）；构造器实参类型匹配不检查（v2 既有）；数组元素/装箱字段借用面无 RAII（既有）；194 待批；win 并行隔离（家机轮）；探针留 /tmp/p72probe 不入库。

## 71-a v2 侧内置构造器（正常/错误/某些）全链补全 + 装箱 move 语义（方案A 用户裁决）+ 71-b 二分禁用残留恢复

**71-a 立案背景**：按 HANDOFF 下一轮任务#1「v2 侧装箱 move 对齐」调查发现缺口远大于预期——v2 对规范 07-五 内置构造器（编译器预置、用户不可重定义）**整体缺失**：探针实证 `返回 正常(行)` 语义层报「未声明函数: 正常」中止（连构造识别都没有，非 move 微调）。接收侧（结果/可选变量声明+`.正常/.值/.错误` 字段访问）v2 完整支持，缺口=构造侧。影响面：v2 通道用户代码任何装箱形态编译失败；当前在跑链路（v2self/E2E v2 用例/借链容器用例）零影响（均不含装箱构造）；stdlib 由宿主源码级并入编译（70-a 已根治）不受影响。呈报三方案（A 全链+move 推荐/B 补全+保守污染/C 仅返回位），用户裁决方案A。

**71-a 实施（v2 树 8 文件，宿主源码零改动）**：
1. **语义层**（语义检查表达式.cn/语义检查.cn）：构造器识别分支（转移 特判同位，名字恒走构造器=预置符号表语义）+被调标识符放行（`转移` 同款）+装箱语境类型推导（返回位=当前函数返回类型、声明位=声明目标类型，静态语境变量两处设置区间检查后复位）+推导类型写入调用节点.子终点（解析期恒 0=空闲位，字段访问对象类型预标注同款模式——语义驱动 IR 同构）。
2. **IR 层**（生成表达式求值.cn）：`生成调用` 构造器特判+`生成装箱构造` 新函数——合成布局 正常@0（4字节）+值@4（整32/正32）/@8（其余）与宿主 typeSizeOf/接收侧布局双侧一致；槽组=结果槽位数 个 8 字节槽、基槽=最大槽号低地址；标志/值经 取地址+字段地址+指针存储 按宽度写入（整32 载荷与标志同槽互不覆盖）；压栈产物=临时基址。零新指令（复用 IR_分配/取地址/字段地址/指针存储/常量整数/结构体拷贝）=三后端代码生成零改动面。
3. **返回 ABI**（生成函数.cn/生成语句.cn/三后端代码生成）：函数返回 结果/可选 与结构体返回同款 retbuf ABI（函数IR 新字段 返回结果名ID+显式初始化清单同步[lessons 权重7 纪律]、槽1=retbuf 保留、返回语句拷贝字节数分流 结果大小、三后端 prologue 判定 `|| 返回结果名ID != 0`）；调用方（发射普通函数调用）retbuf 扩展覆盖结果返回类型。
4. **装箱 move 语义**（IR生成.cn/IR容器.cn）：函数级 已装箱名们 集合（登记/查询/随 清空函数级槽表 重置——v2 既有函数级表模式，零 生成调用/生成表达式 签名改动）；实参单标识符形态（正常(行)/某些(表)）登记——出口三处（发射字符串释放/发射容器析构/块出口析构 字符串段+容器段）跳过已装箱名=所有权转交装箱值（Rust Ok(s) move 同款零拷贝；宿主 70-a 源槽清零同效，v2 跳过模型双装箱退化为借用共享而非 NULL——更安全）；表达式实参/污染名/名单外登记无副作用（出口本就不释放）。
5. **接收侧缺口顺带根治**（生成表达式.cn）：`.值/.错误` 加载宽度按载荷类型（原恒 T_整64 八字节读，整32 载荷值@4 拼垃圾高位——首个整32 载荷用例暴露的既有缺口）。
6. **E2E 新用例** 211_v2_结果构造器与装箱move（八形态校验和 255：①返回位拥有串 move+内容校验（悬垂即错）②整32 载荷③错误分支+.错误 读取④某些()/可选⑤无参 正常()⑥赋值位语境⑦结果变量转发⑧二次装箱借用退化）。

**71-b 二分禁用残留恢复**（生成语句.cn:241）：引用返回路径的 发射字符串释放 调用被第六十七轮带入的「// 二分禁用」注释禁用（引用返回函数的拥有串局部出口不释放=泄漏方向；其上方注释还声明「引用返回路径同样释放字符串」的意图）——第七十轮「全树 grep false && 复审清零」查不到**注释式禁用**（同族漏网形态）。恢复调用+全树 grep 复审（二分禁用/false && 双形态清零，三处命中均为说明性文字或正常代码）。

**验证链全绿**：v2 全树宿主构建 rc=0（177 警告=既有观察期面零新增）+清场重建零警告 rc=0（4 处匹配=文件名含 error 进度行既有口径）+单测 1289/1289+全量 E2E 232 用例 227 过/唯一败=194 既有待批/4 跳（71-b 恢复零回归：150_v2_引用返回 等全过）+211_v2 PASS+**v2self 固定点 266459 行**（新口径，262321+4138=新代码确定性净增）自举第二代逐字节一致+绑定自检 cn_main∈cn_self71.o .text+运行级（208v2/119/173 经 E2E 覆盖）+组件对拍 29/29 全量逐字节归零。

**诚实边界**：构造器实参类型匹配不检查（T=字符串 传整数不诊断——v2 语义层半实现面既有边界）；构造器名重定义被遮蔽不诊断（预置符号表语义，第一版边界）；装箱结构体实参=CopyStruct 深拷（内含容器字段浅共享=宿主同边界，随 NLL/容器析构轮）；装箱值载荷接收面（.值 装入字符串局部）=借用面无 RAII（宿主同边界）；跨编译器 ABI 互操作（v2 编译代码调宿主 stdlib 结果返回函数）布局双侧一致但未单独 E2E 锚定；负路径（构造器用于非结果语境/多实参）已诊断未单独 E2E 用例固化。
