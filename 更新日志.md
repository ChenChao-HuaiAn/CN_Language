## 功能完善总结

**完成时间**: 2026-08-14（阶段C：寄存器分配 + 调试信息 + 优化级别框架）

### 一、阶段C 完成内容（规格书阶段四最后一块）

#### 1. 线性扫描寄存器分配器（Task 4.3，规格书8.3）
- 新建 `src/cn_compiler/codegen/reg_alloc.hpp/.cpp`：可插拔独立模块
  - **活跃区间计算**：def-use 链 + 跨块活跃传播（迭代数据流 in/out 集合）
  - **线性扫描**：区间按 start 升序，活跃集合按 end 维护；空闲物理寄存器分配；不足时溢出到栈槽
  - **保守策略**：仅 i64/u64/ptr 虚拟寄存器参与分配（浮点/i128/小位宽保持栈槽）；仅被调用者保存寄存器（x64 rbx/r12~r15、arm64 x19~x28）
  - **溢出**：spillSlot 递增（复用栈槽区，不与变量槽/寄存器槽冲突）
- 新建 `tests/unit/codegen/test_reg_alloc.cpp`：9 个测试（活跃区间/无冲突/溢出/被调用者保存/类型过滤/保留寄存器）

#### 2. 调试信息（Task 4.4，规格书8.5/8.6 预留）
- 新建 `src/cn_compiler/codegen/debug_info.hpp/.cpp`：源码行号映射表（SourceLocation → 汇编位置）
- x64 嵌入 `; src: 文件.cn:行`（MASM）；arm64 嵌入 `// src: 文件.cn:行`（GAS，`#` 与立即数冲突）
- DWARF 扩展位预留：`DwarfSection` 枚举 + `dwarfLineProgram()` + `mappings()` 导出
- 新建 `tests/unit/codegen/test_debug_info.cpp`：5 个测试（注释格式/映射表/去重/无效位置/DWARF 预留）

#### 3. 接入 codegen（保守：-O0/-O1 全栈帧完全不变）
- **x64**：-O2 起启用（`--no-regalloc` 可关）；operandText/resultText/emitTerminator 三收敛点映射物理寄存器；
  被调用者保存寄存器 prologue push / epilogue pop（逆序）；structReturn/i128 返回函数强制关闭
- **arm64**：提供 `setRegAllocMap` 注入 API（reg_alloc 模块独立可用），默认关闭（结果写回点分散 35 处，保守全栈帧）
- **优化级别联动**：`--no-regalloc` CLI 开关 + DriverOptions.useRegAlloc + createBackend 传参

#### 4. 验证结果
- 全量单元测试 **964/964**（原 949 + reg_alloc 9 + debug_info 5 + 其他）
- 编译**零警告**（-Wall -Wextra -Werror）
- E2E **28/28**（Linux ARM64 本机）；-O0 与 -O2 **输出完全一致**（28 用例 0 不一致）
- x64 -O0 汇编无物理寄存器、-O2 有（push/pop 配对正确）

### 二、新增/修改文件

| 类别 | 文件 |
|------|------|
| 新增 | `src/cn_compiler/codegen/reg_alloc.hpp/.cpp`、`debug_info.hpp/.cpp` |
| 新增 | `tests/unit/codegen/test_reg_alloc.cpp`、`test_debug_info.cpp` |
| 修改 | `x64_codegen.hpp/.cpp`、`x64_instructions.cpp`（物理寄存器映射） |
| 修改 | `arm64_codegen.hpp/.cpp`（regAlloc 注入 API + debug 注释） |
| 修改 | `backend_factory.hpp/.cpp`、`driver.hpp`、`driver.cpp`、`driver_module.cpp`、`cn_main.cpp`（开关联动） |
| 修改 | `CMakeLists.txt`、`tests/unit/CMakeLists.txt`（新源文件/测试注册） |
| 文档 | `plans/001`、`plans/002-阶段4`（Task 4.3/4.4 打勾）、`HANDOFF.md`、`lessons.md` |

### 三、遇到的问题与解决

1. **线性扫描活跃区间 regId 未初始化**（单测全红）→ def/use 登记时设置 regId + start/end 首次创建逻辑
2. **线性扫描 freeRegs 恒空（全溢出）** → `allocableCalleeSavedRegs` 被当作"保留集合"传入构造（参数语义反了），
   改直接传默认寄存器集（`LinearScanAllocator(TargetArch::X64)`）
3. **真实 IR 变量生命周期长导致全溢出** → 正确行为（非 SSA 复用 + 溢出到栈槽）；Alloca 结果排除（无实际值）
4. **prologue 保存区与局部帧不重叠验证** → push 在 sub rsp,frameSize 之后，pop 从 rsp（frame 底部）逆序弹出，
   `mov rsp, rbp` 跳过 frame，顺序正确
5. **E2E 手动脚本误报**（04/05 FAIL）→ diff 行尾空白差异（官方脚本 rstrip 处理）；实际输出与 expected 完全一致

### 四、下一步
- 阶段6「标准库」或 阶段5 阶段B/C（交叉编译/QEMU 验证）
- 优化级别框架：arm64 寄存器分配可后续按需启用（写回点收敛后）
