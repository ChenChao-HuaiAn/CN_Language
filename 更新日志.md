# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（287-a·2026-09-17）

**T13 根治（用户裁决方案A·linux_x64 后端 O0 移位发射 i32 零扩展污染）**：emitShift i32 非常量路径补 `movsxd r10, r10d`（对齐 270-a T14 同族先例）——M3 采样 s2026091705 asm 三件套定案（-426>>13 曾出 524286 应为 -1·O3 常量路径巧合对·285-a「O0 对 O3 错」误判反转·归 T13 非 T12）；免疫面四重实证（i8/i16 movsx 装载·win sar eax·arm64 asr w9·v2 D34 规范化）=三后端+v2 树零改动。新 E2E **403_移位负值矩阵**（11 行·i32 负值变量/常量 sar+shl+正值零回归+i16/i8/u32 免疫+mod 取模+污染值判定面·expected win 实跑固化）+coverage_map 登记。验证链全绿：零警告+单测 1351/1351+E2E 422=422/0/0（含 79 自举闭环=win 锚定链逐字节不变）+CLI 50/50+面覆盖 PASS+修后回归轨 38/38+修前修后 .s diff 仅 4 行 movsxd 插入；v2p 重建实证 v2 免疫（附带发现 v2 移位量掩码 63/31 分支疑似 mod 分叉面·观察待独立甄别）。跨机项=X64L 锚定链重锚+运行级复验归深度机。

## 深度机 linux-x86_64 节

## 功能完善总结（289-a·2026-09-17）

**B9 缺陷族面扩散检验制度落地（用户批准·源码/测试用例零改动·基线 217d132）**：三落点入库——①`scripts/famscan.py` 转正（参数化 --cn/--out/--keep+gcc 探测降级+首案 T23 的 47 用例固化=复验基准在库·287-a 修复轮判据=命中 **16→0**）；②AGENTS §8.1 探针检测轮段新增「缺陷族面扩散检验（B9·立案与根治双挂点）」条款（新点缺陷立案须附族扩散矩阵+根治轮 famscan 复跑归零+命中形态 E2E 转正·与 B8 面台账互补：自顶向下 vs 自底向上）；③plans/026 §2.11 四件套条文（扩散探针/立案挂点/根治归零/用例库滚动）。**转正轮修掉工具自身两处首案遗留 bug**：命中清单报告三列错位（tuple 索引 vs 标签——flags 与命中集合一直正确·16 命中定案不受影响）+C 生成器浮点用例误包 (long long)（假 oracle 分叉）——修复后实测 16 命中与首案定案逐项一致、oracle 零分叉（/tmp/famscan_verify2 留档）。plans/021：B9 迁出 B 区（生命周期铁律）+§4.2 销项+§5 索引+文首戳 289-a+T23 行工具指针切 scripts 版。验证=py_compile+famscan 实测复现+check_handoff/check_progress_sync 双绿。与家机 287-a（linux_x64/*+403）/单位机 288-a（arm64/*+404）写集零交叠（用户授权并行锁之三）。

## 单位机 ARM64 节

## 功能完善总结

### 288-a 开发轮：T22 根治——arm64 逻辑立即数 bitmask 编码器（2026-09-17，单位机麒麟 ARM64，基线 7962579·用户裁方案 A·与 287/289 并行授权）

1. **根治**：isLogicalBitmaskImmediate 编码器（元素铺满全集·GNU as 实测校准）+emitIntBinary 按助记符分派（原逻辑指令误用 add/sub 12 位判定直发=病灶）+恒等折叠（and0→mov0·或0/异0→直通）+不可编码 movz 装载回退；修中自查移位宽度 UB（单测拦截）。
2. **验证**：单测 1353/1353（+2 编码器边值）+新 E2E 404 位立即数矩阵O3+全量 422=420/0/2+复现探针双对+daemon 回归轨 93/93 归零（31 命中样本全转一致）。
3. **收尾**：plans/021 T22 销项+台账+索引+戳；编译器改动面=arm64 后端 2 文件（linux_x64/x64/v2 零改动）。
