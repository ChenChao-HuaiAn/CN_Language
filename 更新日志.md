## 功能完善总结

### 本轮改动：语句终止与续行规则正式文档化 + 测试补充（2026-08-13 收尾）

**核心结论（语言设计层面）**：CN 语言语句**既不是"回车结尾"也不是"分号结尾"**。真实机制是：
- **换行在词法层被当空白丢弃**——无语法意义，等同空格，天然支持续行（跨行表达式自动合并）
- **语句无终止符**，语句边界靠**下一条语句起始关键字**识别（如 `变量`/`返回`/`当`/`选择` 等）
- **分号 `;` 可选**——单行多语句时用显式分号分隔；裸分号、双分号、`当(x);` 均报错
- **循环头 `循环(...;...;...)` 的分号是唯一强制分号**（三段缺一不可）

**文档改动（4 处）**：
1. [`plans/001 CN语言编译器设计规格书.md`](plans/001 CN语言编译器设计规格书.md:230)：新增 **4.6 节"语句终止与续行"**，正式确立语言规范
2. [`.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md`](.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md:53)：新增第三节"语句终止与续行"，后续章节序号顺延
3. [`.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md`](.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md:10)：新增第零节"语句终止与续行"
4. [`.ai-coder/skills/cn-language-spec/SKILL.md`](.ai-coder/skills/cn-language-spec/SKILL.md:95)：索引表更新（01a/03 描述 + EBNF 计数）

**测试补充（2 处）**：
1. [`tests/unit/parser/test_parser.cpp`](tests/unit/parser/test_parser.cpp)：第8节新增 **17 个单测**——无分号声明/返回/表达式、可选分号、关键字边界（`变量 x = 1 整32 y = 2`）、跨行续行（调用/下标/分组/行尾运算符）、行首运算符续行、单行多语句、裸分号/双分号非法、`当(x);` 非法、空循环体合法、循环头三段分号
2. [`tests/e2e/19_语句终止/语句终止.cn`](tests/e2e/19_语句终止/语句终止.cn) + `.expected`：新建 **E2E 用例**，9 行期望输出，综合演示四规则

**验证基线**：
| 指标 | 数值 |
|------|------|
| 单元测试 | **755/755**（62 套件，738+17） |
| E2E 用例 | **19/19**（新增 19_语句终止） |
| 编译警告 | 0（/W4 /WX） |

**发现的问题**：无 bug。文档描述与实际诊断完全一致（裸`;`→"预期表达式"、`当(x);`→"预期 '{'"、循环缺分号→"预期 ';'"）。
