# CN语言编译器 更新日志

## 功能完善总结

## plans/019 阶段4' 第一批「拥有型字符串」（方案A 用户裁决）落地：RAII 自动释放+拥有化发射+白名单判定+固定点 247461 不变（2026-09-10 第六十六轮 深度机 linux-x86_64）

### 一、语言语义（经用户批准的方案 A）

`字符串` 局部变量升**拥有类型**：字面量/拷贝初始化落堆（`__cn_str_copy`）；赋值=free 旧+拥有化新；函数返回块 RAII 自动释放（`__cn_str_free` 空安全，返回=移出）；`转移()` 零拷贝衔接；裸视图用 `字符*`。拥有判定白名单（字面量/标识符/内置分配族八函数），其余调用污染退出 RAII。

### 二、实施（IR 全自持）

genStringFrees（类 RAII 同模型：Alloca 收集+入口零初始化+返回块注入+移出跳过）+初始化/赋值拥有化+stringTainted_ 污染集+stdlib 替换函数迁移。

### 三、验证

E2E 208（七形态）+38_tool 指针逃逸兼容零回归+全量 225 用例 220 过（唯一败=194 既有）+清场重建零警告+单测 1289/1289+**固定点 247461 逐字节不变**（v2 借用形态全污染=零注入天然实证）+对拍 29/29+运行级 3/3。

### 四、诚实边界

转字符串族不入白名单（存量手动释放兼容）；循环内声明中间迭代泄漏（类 RAII 同款）；参数/静态=借用；用户函数返回拥有随第二批。
