# 功能完善总结

> 72-b 中断绑定 C 语义（2026-09-11 第七十二轮下半程收尾·续，深度机 linux-x86_64）：用户裁决方案 B——`中断` 绑定最近进入的选择或循环。

## 一、语义变更（用户裁决，存量零影响）

**变更内容**：`中断` 由「恒绑定循环」（原实现 loopStack_ 恒优先）改为**绑定最近进入的选择或循环**（谁近绑谁，与 C 的 `break` 一致）。

**动机（探针 88 与 C 对照实测）**：循环内选择体写 `中断`，原实现直接终止整个循环且**静默无错**——i=1 时 5 轮循环只剩 1.5 轮；C 的 `break` 只跳出 switch、循环继续。C 背景使用者的直觉陷阱=静默逻辑错误（「安全接近 Rust」目标要防的形态）。选择语句 fall-through 已裁决对齐 C（2026-08），绑定规则同向对齐才自洽。

**存量影响面实测=0**：tests/e2e + stdlib + v1 树 + v2 树全量扫描「循环内选择体内 中断」形态零命中——迁移零影响，未碰任何既有行为依赖。

## 二、实施（宿主 IR 3 文件）

1. `ir.hpp`：LoopContext/SwitchContext 各增 `enterSeq` 进入序；IRGenerator 增 `breakScopeSeq_` 单调取号（generate() 每模块复位）。
2. `ir_stmt.cpp`：genWhile/genFor/genSwitch（情况+默认两处）压栈时取号；`visitBreakStmt` 判定改「最近结构优先」（enterSeq 大者=最近进入）；drop-on-jump 释放范围随绑定目标走（绑选择=分支基线、绑循环=循环体基线，72-a 模型不动）。
3. `ir_decl.cpp`：generate() 复位补 `switchStack_.clear()`（原漏，防御性）+ `breakScopeSeq_ = 0`。

**v2 侧核验**：v2 树无选择语句（`节点_选择` 全树零命中）——无同族缺口，单侧修全即修全。

## 三、用例与规格书

- **E2E 215_中断绑定最近结构** 四场景固化：①循环内选择体 `中断`=只跳出选择（5 轮全跑=C 核心）；②嵌套链（外循环→选择→内循环）：内循环里的 `中断` 绑内循环、分支尾 `中断` 绑选择（enterSeq 双向验证）；③无循环包裹的选择体 `中断` 行为不变；④循环体裸 `中断` 既有语义锚定。
- **规格书同步**（用户裁决=授权）：plans/001 第 5.5 节与 cn-language-spec specs/03-3.5 补「`中断` 绑定最近进入的选择或循环；`继续` 仅对循环有意义」条款。
- 探针 88 复验：修改后输出与同结构 C 程序（gcc 实测）**逐行一致**。

## 四、验证链全绿

清场重建零警告（4 处匹配=文件名含 error 进度行既有口径）+ 单测 1289/1289 + E2E **237 用例 232 过**（唯一败=194 既有待批）/4 跳 + 锚定链 fix_p=fix_s=**266964 行**逐字节一致（宿主绑定语义改动对 v1/v2 自举产物透明——v1 树 0 命中实证）+ 运行级 3/3 + 组件对拍 29/29。

## 五、诚实边界

从选择内跳出外层循环无直接语法（与 C 相同，须经条件变量或重构）；嵌套选择（选择内选择）语义上由最近绑定规则自然覆盖，未单独 E2E 固化；194 待批维持；win 并行隔离（家机轮）；探针 88 留 /tmp/p72probe 不入库。
