# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结（260-a·2026-09-16）

**D32 根治（P1·真根因=SSA 占位 Phi 化·非 RA）+工具链并行架构（用户令「每次运行太慢」）**：

1. **D32 根治**：s266 定位链反转——双级别对拍（-O0 变13=-91471 对/-O3=37903 错）+`--no-regalloc`
   正确（初判 RA）→ **IR 对拍揭示 IR 层已错**（%v128 同号冲突+bb13 病态轰炸）→ 真根因=**SSA Pass
   （256-a F1-26 方案A）占位 Phi 化**：汇合块 Load 的槽其 Store 在其后顺序段（前驱块无 Store）时，
   「占位沿用槽值」兜底在**前驱块尾注入 Load 读尚未 Store 的栈槽**（UB）→垃圾值经 Phi 降级拷贝
   污染变8/变13；asm「读 r12（被变10 复用）」=下游投影。**根治**=`buildFunction` Phi 化前提收紧
   （`hasStoreTo` 全前驱判据）；副作用病态消除（IR 877→219 行）。
2. **防线与资产**：check_asm_width 第三维 **win-x64 mov 宽度混配**扫描（r64↔r32 非法·内存操作数/
   movsxd 豁免·负样本自检双向）+**并行化 `--jobs`（344.7s→62.0s）**；**cnsmith_diff 并行架构**
   （用户令：线程池+预热〔运行时 obj 缓存并发写防护〕+有序聚合＝输出与串行逐字节一致；500 采样
   485s≈8×）；387/388 双级别锚定（s266 转正·显式 -O0/-O3 旗标·同源同 expected）。
3. **T11 联动**（深度机 x86_64 定性）：面①「优化链产出未支持形态」随同根消除（384 .s
   「未支持操作码」指纹清零实证）；**撞号修正 T10→T11**（与子串截断 T10 重复）；残余面②发射器
   default 静默→硬错误收口/面③ IR 验证器支持面=立案。
4. **测试**：单测 +2（Phi 前提负例两件）+s266/s268 双样本 O0/O3 逐字节一致。
5. 验证链：零警告构建+单测 **1351/1351**+E2E **407=407/0/0**（含 78_v2/79_v2 锚定链自洽）+
   CLI 矩阵 **50/50**+check_asm_width **249×3 PASS**+CN-Smith 500（seed=258）**零分歧 500/500**+
   双文档门禁。

## 深度机 linux-x86_64 节

## 功能完善总结

> **B8「构件面检测制度」呈报（2026-09-16，深度机 linux-x86_64；基线 a7ffb59·测试轮无认领·用户三次质询驱动：点还是面→面不能只针对&&→要制度不能我提醒才测）**：
> 根因反思=T12/T13 都是用户提醒后补测——「面」只存在于用户脑中与 AI 临场发挥，仓库无权威清单无机械完成度保证；
> 制度五件套=①面台账（plans/001 文法导出构件×值域×消费位×级别全格子·新增语法同轮加格挂 §8.4）②空闲机测试/探针轮默认动作=按台账巡视最前未测格（用户指令=插队不指方向）③命中缺陷根治轮同轮矩阵转 E2E 关死 ④check_matrix_coverage.py 机械门禁（未测格只减不增+覆盖数注入 plans/021 戳每轮可见）⑤缺陷立案必修两问（本构件格齐否/同族加格否·lessons 93-a 制度化）；
> **待裁决**=制度本身/台账初版由开发轮或文档轮建/AGENTS §8 角色职责修订——全文=HANDOFF 深度机节·登记 plans/021 §3-B B8；
> 本轮实测底座=T12 面 73 形态（两病灶）+T13 运算符全景 102 形态（负数右移 -O0 逻辑移位）+溢出观察 2 归 C18+采样三轨（165 分歧/负向 1000 达标/回归绿）——源码零改动。
## 功能完善总结

> **234-a A7 本体根治收口（2026-09-16，深度机 linux-x86_64；基线 476b7d2，认领 8bb5511 前 ecae5ed rebase 后）**：
> ①**第一层根治：共享 AST 写回注记跨实例污染**——实例化类方法体 AST 全实例共享（mi.ast=模板成员），
> 语义检查写回型注记（resolvedType/resolvedSignature/retOwnedString/node->size）在共享节点上被
> 「最后检查实例」残留，IR 层逐实例生成全读残留值（实证：v2 树 9 处 `复制(下标链)` resolvedType
> 全=IR指令 56B → 误走分派④ CopyStruct 56B+写回槽地址；单实例程序巧合正确=225-a「同模块用户
> 泛型类正确」对照真因）。根治=emitClassMethod 生成前按本实例 typeArgs 重放方法体检查
> （checkSingleMethodBody 原样提取复用）+诊断快照回滚；方案裁决=重放（方案U）胜深拷 AST（方案A），
> 详见 plans/025 §二。
> ②**第二层根治（连带发现）：整体赋值源分派漏复制调用形态**——`数据[索引]=复制(...)` 元素槽=
> 栈地址 8 字节（乙0号=-1347670232 实锤）；emitStructWholeAssign 补分派（**收窄纪律**：仅复制
> 调用进新分支——宽版补丁致 v2p 产物坏 285 失败，单变量二分定位后收窄；一般调用返回形态=D11 待查）。
> ③**stdlib 六处深拷切换重新应用**（206-c 立案收口）：向量/栈/链表/队列/映射键/映射值（映射范围
> 收为 [0,元素数量)）；E2E 285 切换态双侧全绿（宿主 15=v2 同值）。
> ④验证链：零警告全量重建 + 单测 1327/1327 + **全量 E2E 313 用例 311 过/0 失败/2 跳** +
> 锚定链 **410460 行 fix_p≡fix_s 逐字节不变**（v2 树不含拷贝初始化形态=自举链零扰动） +
> valgrind 切换态 6 errors（=225-a 回退态基线，**A7 差值 7 归零**）+ 反证双件（回退补丁 rc=136
> 复现 / 插桩双态 IR 实证）。
> ⑤新立案：**D10 泛型函数体零检查**（泛型自由函数体从未语义检查，`复制(源)` 落普通调用链接期爆）、
> **D11 一般调用返回整体赋值待查**——均登记 plans/021 §三-D；源码改动=宿主 5 文件+stdlib 2 文件，
> 详情=plans/020 第七十五节 + plans/025 §二。

## 单位机 ARM64 节

## 功能完善总结

### 257-a 插队修复轮：plans/021 §3-B 生命周期铁律根治（2026-09-16，单位机麒麟 ARM64，基线 f0f0696·认领 9818aa8·用户质询「B5 三犯」驱动）

1. **修复**：§3-B 清空 B5 整段「已裁决原文留档」（裁决/实施记录本已由 §3-F F1-26 行/§5 256-a 行承载）；§3-G 同型滞留的 T2（案戊）/T9（整数溢出方案 D）迁 §3-C（C17/C18）+处置建议段同步。
2. **立规**：AGENTS.md §8.3 明文「§三-B 生命周期铁律」——条目一经用户裁决**当轮迁出**（实施明确的迁 §三-A、定案待排程的迁 §三-C、完成销项进 §4.2），禁留任何留档形式；呈报类条目须同步登记 §3-B。
3. **机械门禁**：check_handoff.py 扩 §3-B 拦截（「已裁决/已批/已选定/✅」完成标记即禁止提交）——反例（修复前 L80 B5 双标记命中·退出码 1）/正例（修复后 PASS）双向实证；源码零改动（纯文档+门禁脚本轮）。
4. **B6 裁决闭环**：分支策略建议（develop 单主干+里程碑标签强推+不采用功能分支）经用户批准（「同意」）当轮迁 §3-A **A12**（里程碑标签锚点建立·待认领）；B 区回空态。

**门禁**：check_handoff.py（新检查双向实证）+check_progress_sync.py PASS。

**诚实边界**：机械检查为字面拦截非语义判定（完成态措辞须避让·宁可严格）；github 镜像本机无凭据未推。
