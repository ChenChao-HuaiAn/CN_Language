## 功能完善总结

### 模块系统 v2.0 规范文档更新（2026-08-15）——第 1 层：规范先行

**背景**：用户已批复「模块系统 v2.0」全面改造方案（10 项决策 + 可见性交集 + 无强制分号）。本层（第 1 层）只更新全部规范文档，使 v2.0 语法先行确立，为后续词法/语法/语义改造（第 2~10 层）提供依据。**本层不修改编译器源码。**

#### 一、v2.0 核心决策（12 项，全部写入规范）

| # | 决策 | 规范落点 |
|---|------|---------|
| 1 | 路径分隔 `::`（替代 `.`） | specs/08 第三节 |
| 2 | 新增 `模块`/`作为`/`包`/`货舱`；保留 `导入`；删除 `从`；不引入 `使用` | specs/01a 5.3b/5.9 |
| 3 | 标签式 `公开:`/`私有:` 保留 + **模块内默认私有** | specs/08 第四节、specs/03 三b |
| 3b | **类内访问控制默认私有** | specs/06 第二节 |
| 4 | crate 隔离（包 = crate = 命名空间边界） | specs/08 第五节、specs/09 第四节 |
| 5 | `包.cn` 根 + 模块树（`模块 网络` 声明引用） | specs/08 第五节、specs/09 第三节 |
| 6 | 子目录 `网络/传输控制.cn` → `网络::传输控制` | specs/08 第三节 |
| 7 | 核心包 prelude 基础项默认可见 | specs/08 第六节、specs/09 第五节 |
| 8 | 导入全形式（路径/作为/花括号/通配符） | specs/08 第三节、specs/09 |
| 9 | `货舱.toml`（`[货舱]` 名称/版本 + `[依赖]`） | specs/09 第二节 |
| 10 | **可见性交集规则**（各层级取最严格 + 四象限矩阵） | specs/08 第四节（重点章节） |
| 11 | 不强制行尾 `;`（声明以换行终止，`;` 仅同行多语句分隔） | specs/08 第八节 |

#### 二、文档变更清单

| 文件 | 变更 |
|------|------|
| `.ai-coder/skills/cn-language-spec/specs/08-模块系统.md` | **全面重写 v2.0**：设计原则（文件即模块+包=crate）、模块声明、`::` 路径、导入全形式 EBNF、默认私有、**可见性交集规则（四象限矩阵）**、crate 模型、包.cn、prelude、货舱.toml、迁移映射表（v1.0→v2.0）、无强制分号 |
| `.ai-coder/skills/cn-language-spec/specs/01a-词法结构-总览与关键字.md` | 关键字 58→61：**删除 `从`**、新增 5.3b 模块系统关键字（`模块`/`作为`/`包`/`货舱`）；设计决策表 #8 标注 v2.0；变更记录 v2.0；顶层声明补模块/常量/静态声明 |
| `.ai-coder/skills/cn-language-spec/specs/03-语句与控制流.md` | 新增「三b、模块级可见性（默认私有，v2.0）」：模块级顶层声明默认私有 + 示例 |
| `.ai-coder/skills/cn-language-spec/specs/06-OOP面向对象.md` | 类内访问控制 **默认私有**（v2.0 变更，原默认公开）：访问控制表加「无标签 → 默认私有」+ 可见性交集说明 |
| `.ai-coder/skills/cn-language-spec/specs/09-包与依赖.md` | **新增**：货舱.toml 完整规范（`[货舱]`/`[依赖]`）、包.cn 结构、crate 边界（命名空间隔离/`包名$` 前缀）、核心包 prelude、依赖解析流程 |
| `plans/001 CN语言编译器设计规格书.md` | 决策表 #8 标注 v2.0；**5.9 模块系统章节全面重写**（5.9.1~5.9.6：模块声明/导入全形式/可见性交集矩阵/crate 模型/货舱.toml/无分号） |
| `.ai-coder/skills/cn-language-spec/SKILL.md` | 索引表：01a 关键字 61、06 默认私有、08 v2.0 描述、**新增 09 行**；查询关键词（删 `从`、增 模块/作为/包/货舱/交集/货舱.toml/依赖/crate/prelude）；差异表同步 v2.0 |
| `.ai-coder/skills/cn-language-spec/README.md` | 版本 v2.0、目录 10 文档、索引表、差异表同步 |
| `.ai-coder/skills/cn-language-spec/config/default.config.json` | specVersion v2.0、specDocumentIndex 新增 09、keywordMapping 删 `从` 增 v2.0 关键词 |
| `.ai-coder/skills/cn-language-spec/templates/*.md`（3 个） | 版本标注 v1.0 → v2.0 |

#### 三、v2.0 语法要点速览

```
模块声明 = "模块" 标识符
路径 = 标识符 ("::" 标识符)*
导入声明 = "导入" 路径 ["作为" 标识符]
          | "导入" 路径 "::" "{" 导入项 ("," 导入项)* "}"
          | "导入" 路径 "::" "*"
导入项 = 标识符 ["作为" 标识符]
可见性：公开:/私有: 标签式 + 默认私有（模块级 + 类内）+ 可见性交集（取最严格）
crate：包 = crate = 命名空间边界；包.cn 聚合再导出；跨包 导入 包名::模块::符号
prelude：核心包基础项默认可见；导入 核心::* 可扩展
货舱.toml：[货舱] 名称/版本 + [依赖] 名称 = "版本"
无强制分号：声明以换行终止，; 仅同行多语句分隔
```

#### 四、迁移速查（v1.0 → v2.0）

| v1.0 | v2.0 |
|------|------|
| `导入 数学.平方根` | `导入 数学::平方根` |
| `从 数学 导入 正弦, 余弦` | `导入 数学::{正弦, 余弦}` |
| 默认公开（无标签） | **默认私有**（文件顶部显式 `公开:`） |
| 类内默认公开 | **类内默认私有**（显式 `公开:` 标注公共成员） |

#### 五、说明

- 本层为纯文档更新，**未修改任何编译器源码**；编译/测试基线不变（单测 1057、E2E 45）。
- 全部规范文档一致反映 v2.0 决策，作为第 2~10 层（词法/语法/语义/货舱.toml/迁移/测试/集成/Debug/收尾）实现的唯一依据。
- 后续层按方案实施计划派发：第 2 层词法（ColonColon + 4 新关键字 + 删 `从`）。
