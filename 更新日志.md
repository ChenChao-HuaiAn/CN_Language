# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（554-a·D1 滚动=生成下标左值地址拆分·2026-09-21）

- **D1 行数滚动**：v2 树 `生成下标左值地址`（生成表达式.cn）170 行→主函数骨架+三子函数（标识符/
  成员下标/裸字段分支）——纯机械重构零行为变更；**配对计数动态边界**切片（一次通过）。
- **等价铁证**：同宿主双 v2p 重建对拍——六产 asm 探针 md5 逐字节一致（含 t_idx 下标左值实弹
  =98f9835b 直接锚定拆分消费面）；Ninja 零警告+单测 1394/1394+check_fn_length 出列；集成时
  全量门禁+develop 现态补跑兜底。
- **同会话累计**：522/529/530 三轮集成闭环+541 拆分缺陷定性（T83 两层假象剥离+re-apply 复绿）
  +542 src 轮挂等 arm64 回签+check_win_asm 注册修复+行数门禁放宽待追认。

## 深度机 linux-x86_64 节

## 功能完善总结（🏃 548-a F2-33 迭代器侦察定稿轮·零入库代码·2026-09-21）

- **T93 立案（宿主）**：泛型实例取地址赋泛型类指针字段报「向量$整64*」合成名吃星号——src 类型系统合成名规范化缺口；v2p 同源 rc=0（双侧分裂）。
- **迭代器设计定稿**：泛型类 向量迭代器<T>+525_v2 闭环上架形态（借链=是）；映射集合迭代器=波2；**549=宿主 T93 修复+实施接力**。
- 挂等外部：546+547 接力链三平台回签（D12 预处理销项+456 根治）。
## 功能完善总结（🏃 537-a 解析类声明部分拆分 185→162·挂等呈报·2026-09-21）

- **部分拆分**：继承跳过+布局收口两边界块出列（子函数带 是否公开/池/类键基/类体起点参数）——主函数 162 行未出列（方法成员五形态嵌套链+字段成员块=下一轮）。
- **等价铁证**：md5 双对拍（探针 3982d9f7+全树改前文本 111825 节点 9eb05e11 双一致）。
- **★456 runner 环境红之谜挂等呈报**：456-6 rc=14 单跑稳定复现+清缓存重建仍红 vs **手动同源编译运行 rc=27 正确**（产物仅 1 行 IR 常量差·非 537 引入）——挂等不进 develop·待 runner 环境定位。


## 单位机 ARM64 节

## 功能完善总结

### 560 开发轮：F2-36b 首步=bench v2 口径三平台改造+arm64 自举性能基线入库（2026-09-21，基线 develop 0e3ec08f）

1. **范围修正（开工侦查实锤）**：原 bench_self_host.py 测量对象=v1 组件链（CN语言编译器/ 五组件·已推倒）——对象整体过时；改造为 **v2 自举树口径**（CN语言编译器v2 全树·fix_p≡fix_s 固定点·对齐 78_v2/79_v2 锚定链编排）+`--platform` 三平台工具链分派（win=ml64/link 原路径保留·linux=as/g++ -no-pie〔CN_AS/CN_CXX 环境变量+PATH 探测·对齐 runner〕）。
2. **arm64 基线实测入库**（编译器=组合态 558+559 修复态构建）：M1=22295.7ms（C++ 宿主 Debug build v2 全树）/M2=42608.4ms（CN 版编译同树 742237 行·3 次中位）/**比值 1.91x vs C++ Debug**/M3=2194.8ms/M4=146.4ms+绑定自检 cn_main∈cn_self.obj ✓/**M5=32309.0ms·M5/M2≈0.758=CN 自举产物编译器比宿主 Debug 构建的同一编译器快 24%**（重大正面信号）/M6 ✅ fix_p≡fix_s 逐字节（自举固定点成立）。
3. **实施修两处**：M4 借链对象（初版错用 fix_p 自汇编→undefined 词法$Token 追加符号·grep 实锤 fix_p 缺跨模块符号定义→改 cn build 中间产物 v2p.o〔run_e2e「容器符号提供者」原义〕）；基线测量编译器依赖序（主树无 558 修复→改用组合态构建·口径注明）。
4. **口径诚实边界**：C++ 宿主=Debug 构建（-O0）——1.91x 非 vs Release 口径；单机基线（跨机比较无意义）；win 分支静态核验（跨机复验归家机）；深度机 x64l 基线可复用同脚本（三机通用）。
