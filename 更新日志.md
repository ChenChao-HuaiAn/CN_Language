## 功能完善总结

### 阶段6「标准库」第4步：IO 输入 + 文件读写（Task 6.2 输入部分 + Task 6.8，2026-08-14）

**新增标准库模块：**

1. **`stdlib/IO.cn`**（Task 6.2 输入，对标 C++ cin，规格书 10.6）：
   - `读取行()`：从标准输入读一整行（不含换行），返回 `结果<字符串,整32>`；EOF/失败返回 `错误(错误码.文件=5)`；返回的动态字符串调用方负责 `字符串释放()`
   - `读取整数()`/`读取浮点()`：strtoll/strtod 整行解析，成功标志经 `&整32` 输出（C 风格 int*），失败返回 `错误(错误码.文件)`
   - `打印到错误(字符串)`：fprintf(stderr) 不换行（与 `打印行` 不换行语义一致）

2. **`stdlib/文件.cn`**（Task 6.8 文件库，对标 C++ fstream，规格书阶段五「文件系统」）：
   - `打开文件(路径, 模式)`：模式 1=读"rb"/2=写"wb"（截断）/3=追加"ab"，失败返回 `错误(错误码.文件)`
   - `读取文件(句柄, 缓冲, 数量)`/`写入文件(句柄, 数据)`/`读取文件行(句柄)`（EOF 返回错误码）/`文件大小(句柄)`/`关闭文件(句柄)`/`文件存在(路径)`
   - **中文路径**：运行时内部 `MultiByteToWideChar(CP_UTF8)` + `_wfopen_s`（lessons.md 已验证方案，E2E 用 `target/测试文件.txt` 端到端验证通过）

**新增运行时 API（英文命名，extern "C"）：**

1. **`src/runtime/input_api.cpp`**：`__cn_read_line`（动态分配 UTF-8 不含换行，EOF 返回 nullptr）/`__cn_read_int`（strtoll + 整32* 成功标志）/`__cn_read_float`（strtod + 整32* 成功标志）/`__cn_print_err`（fprintf stderr）
2. **`src/runtime/file_api.cpp`**：`__cn_file_open`/`__cn_file_read`（fread）/`__cn_file_write`（fwrite）/`__cn_file_read_line`（fgets 动态分配含换行，EOF nullptr）/`__cn_file_size`（fseek end+ftell+恢复，失败 -1）/`__cn_file_close`/`__cn_file_exists`；句柄 `void*`（FILE*）

**五处同步（新增内置函数全链路）：**

- 语义注册：`semantic.cpp` registerBuiltins 注册 `IO.读取行`/`IO.读取整数`/`IO.读取浮点`/`IO.打印到错误` + `文件.打开文件` 等 7 个（带 `IO.`/`文件.` 前缀限定名，数学库同模式）
- IR 映射：`ir.cpp` 名称映射 11 处（`IO.读取行`→`__cn_read_line` 等）+ 结果类型映射（读取行→ptr、读取整数→i64、读取浮点→f64、打开文件→ptr、读写/大小→i64、读取文件行→ptr、文件存在→i1）
- 运行时声明：`runtime.hpp`；构建：`CMakeLists.txt` cn_runtime 源文件 + `cn_main.cpp` compileRuntime 源文件数组 + linkExe .obj/.o 列表（x64/arm64 两处同步）

**E2E stdin 注入（run_e2e.py 扩展）：**

- 检测同名 `.input` 文件，经 subprocess `input` 参数注入标准输入（`33_io_input` 用例：读取行/读取整数/读取浮点 + EOF 语义）

**编译器缺陷修复（本轮）：**

- **字符串/字符\* 与 空类型\*（无）比较被误拒**：语义层比较运算 `isPointerType` 只认 `*` 结尾，`字符串 != 无`（读取行 EOF 判定）报"指针只能与指针或空指针比较"。修复：`字符串`/`字符*` 视为指针（本质 char\*），指针间比较按地址放行

**实测边界：**

- E2E 直接调用内置限定名（`IO.读取行` 等，25_math 数学库同模式）——stdlib 模块公开函数与内置限定名在"模块已导入"时冲突（用户模块函数优先）；模块体内 `IO.xxx` 走内置路径
- 变量名避免以类型关键字开头（`整数成功` 词法拆分为 关键字+标识符 报"预期标识符"）
- `读取文件行` 返回含 `\n` 的字符串，`打印()` 会额外换行（expected 按实际行为匹配）

**新增测试：**

- `tests/unit/runtime/test_input_api.cpp`（11 用例：读取行含空格/空行/EOF、读取整数合法/非法/EOF、读取浮点、打印到错误 _dup2 重定向 stderr）
- `tests/unit/runtime/test_file_api.cpp`（7 用例：写读/大小 UTF-8/原始读取/追加/中文文件名/打开失败/非法模式）
- `tests/e2e/33_io_input/`（IO 输入 E2E，stdin 注入）+ `tests/e2e/34_file/`（文件读写 E2E，中文文件名）

**验证结果：** 单测 **1005/1005**（987 前序 + 18 新增）、E2E **36/36**（34 前序 + 33_io_input + 34_file）、编译 0 警告（MSVC /W4 /WX）。
