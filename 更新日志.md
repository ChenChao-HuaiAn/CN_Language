# 更新日志

## 功能完善总结

**plans/017 T3/T4 实施：run_e2e.py v2 闭环平台参数化+23 例本机解锁+解锁暴露两枚回归根治（2026-09-06 第二十六轮，深度系统 x86_64，两缺陷用户裁决均方案A·Rust 准绳：性能第一/安全第二）**

1. **T3（runner 平台参数化）**：`执行v2闭环Linux` 新增 目标平台 参数
   （linux-arm64 | linux-x86_64）——宿主编译 `--target` 两处（v2p 构建+供给
   源编译）与 v2p 运行第 2 参数两处（负路径+步骤3）随平台分派；产物命名
   arm64 保持 `v2p_linux*` 原名（行为零变化）、x86_64 用 `v2p_linuxx64*`/
   `v2asm_linuxx64.o`/`v2out_linuxx64`（两平台先后全量中间产物不互踩）；
   `执行v2闭环` linux 分支并入 x86_64+`执行用例` 守卫扩三平台；**平台跳过
   清单摘除 v2 闭环 23 例**（119~150），跳过仅剩 62/69/78/79 四例平台限制
   ——v2 闭环 23 例本机（linux-x86_64）自平台跳过清单诞生起首次真实运行。
2. **T4（E2E 158_v2_linuxx64_系统V调用约定，原拟编号 154 已占用顺延）**：
   v2 版 SysV AMD64 ABI 分歧面专项（三平台语义一致，重点锚定 X64L 后端），
   返回码 0=全过+11~20 失败码逐分歧面定位：①7 整型参数（第 7 走栈
   [rbp+16+8k]）；②结构体返回（rdi 隐藏 retbuf）；③结构体按值参数×满 6
   整型寄存器位（宿主第十八轮 r9 拷贝临时缺陷的 v2 侧同款探针——prologue
   逐个 r10/r11 拷贝互不破坏）+嵌套调用实参（rax=retbuf 地址即按值实参
   地址）；④结构体返回+7 参数（retbuf 占 rdi 后实参整体后移——宿主 154
   同款触发面）；⑤递归对齐。注册 `(["主.cn"], 0)`（无容器无打印）。
3. **解锁即暴露两枚回归（第二十五轮引入被平台跳过掩盖，均用户裁决方案A
   当轮根治）**：①【150 语义回归】声明收集登记键改签名键后 `是引用返回
   调用`（独立查纯名表、未随调用分支一起加签名表回退）恒判假——引用返回
   赋值目标/复合赋值/引用初始化三形态全拒（150 解锁首跑 3 错中止；
   v2p_fix 基线对拍实锤；win 侧同源码同红非平台特异）——修复=**声明收集
   双键登记**（签名键恒登记+纯名键同名唯一时补登记，与 IR生成.cn 三张表
   双键同构；真重载候选>1 不登纯名键仍走签名表回退——157 行为不变；跨
   文件同纯名先文件赢=旧静默吞同款=模块遮蔽语义 P6b 注记；Rust 准绳：
   唯一绑定优先查询更快+歧义绝不引入错误绑定）；②【125 自检行】打印函数IR
   打 IR 名（=签名键）带 `#整64` 后缀 vs 期望行固化旧文本（非运行缺陷）——
   修复=主.cn `自检显示名` 剥首个 `#` 后缀打声明名（CN 标识符无 # 剥离
   安全；125 期望文件零改动）。
4. **验证（全绿）**：150/125/157/158 单跑四绿（157 重载消歧零回归锚定）；
   全量 E2E linux-x86_64 **160 用例 156 过/0 败/4 跳过**（解锁 23 例+158
   全绿；159→160=新增 158）；构建零警告（GCC -Wall -Wextra -Werror）+
   单测 1240/1240（C++ 侧零改动，本轮变更=v2 源码+runner+新用例）；
   诚实边界：arm64 路径行为零变化=静态对表论证（本机无 ARM64 运行能力，
   动态复验=单位机轮），158 三平台跑随跨机轮验证。
5. **教训（已入 lessons 权重 10）**：平台跳过的用例不是不存在的用例——
   解锁日=欠账日；机制性改动落地轮必须全平台矩阵验证或显式审计被跳过
   用例的形态覆盖面；新机制上线自查「哪些既有消费点直接查表？各自有没有
   回退链？」。
