# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结（🏃 多轮会话收官：329~364-a·D38 十七项防线全闭合+D10/D36 根治·2026-09-18~19）

- **已集成六轮**：329-a（D38批2 六项+317 销账）/338-a（T56 f 后缀）/339-a（批3a 入口三合一）/340-a（批3b 收官）/352-a（D36 侦查）/354-a（T33 销项确认）。
- **364-a 分支挂起等回签+解锁**：D36 块作用域化（346 修复·asm 铁证）+D10 诊断保留（泛型体未声明调用 build 期拒）——深度机已签·单位机 arm64 待签·且 406（D41 过渡态红）解锁后即集成。
- **M3 采样双窗**：差分 1000/1000 全一致+负向 800/800 全拒绝·零新存零新类别。
- **验证终态**：组合态全绿=构建零错误+单测 1353/1353+d38_scan 16 拒/1 缺+E2E 445=444/1（仅 406=T43 既有）。

## 深度机 linux-x86_64 节

## 功能完善总结（✅ 420-a 克隆体参数槽偏移根治·D42 波 3 攻坚·2026-09-19）

- **根治（本体）**：克隆体参数槽偏移根因=**克隆区间漏块节点**——函数节点.子终点=块索引**单点语义**（非区间终点），克隆区间 [旧起,旧终) 漏块→克隆体无体块→生成块拿参数节点当块=空体——修=子终点+1 **闭式含块**。**probe15=5/probe17=3 全链回绿**（T* 指针参数下标+参数传递+克隆体单态化运行级）+probe16=88 回归 ✓。
- **顺带修四枚**：①函数实例收集过滤（被调标识符在 调用节点.子起点——节点自身文本=0）+基名过滤扩函数参数序表；②函数分派遍历改函数节点表自身（全集索引错位的潜在越界）；③语义放行返回值修正（**-1 哨兵被上层当类型索引进向量越界**→返回 真实类型 整32）；④函数实例签名表登记（调用侧 决议函数符号 与定义侧同公式——缺失=调用裸名 vs 定义类型编码的链接 undefined）。
- **stdlib 源码级 IR 段缺口清单实测**（撤守卫侦察）：类型大小 K/V 表达式位 ×48（克隆器标识符位替换缺）+成员字段偏移 miss ×72+装箱语境 ×12——**守卫恢复**，421-a 攻坚面。
- **新用例**：tests/e2e/455_泛型函数单态化（双编译·显式实例化调用+T* 参数下标·运行级 5）。

**门禁（全绿）**：全量 E2E --jobs 8 **452=449/1/2**（唯一红 426=基线既有 T50·零新增红）；单测 **1353/1353**；strict 100%+matrix 83/83；自举链 78_v2/79_v2 双 PASS。

## 单位机 ARM64 节

## 功能完善总结

### 341-a 制度轮：轮次号「认领前先看板、先到先得」落地（用户裁决方案①）+plans/025 膨胀根治（2026-09-18，基线 620831b）

1. **用户裁决方案①落地**：AGENTS §8.1 轮次号占用权威改为「远端 develop 看板在飞行」（不看 git 历史——历史滞后于并行现实）；取未占号、先到先得（332-a 实证「全表最大+1」三机并行必撞：双 329-a；本日又现双 338-a）。
2. **check_handoff 新增「在飞轮次号唯一性」检查**：跨机同号即拦截（正则覆盖大写/中文标识——首版漏 `340-D38` 反例未拦·当场修）——正例/反例/还原三情形实证。
3. **plans/025 膨胀事故根治**（1780→493 行）：§二/§三 被拼接 4 份（集成/rebase 追加而非替换）——标题级去重保留最新；根因防线登记（`### 标题重复`检测·下轮）。
4. **本轮按新制度完整实践**：`git fetch` → 看板直推占号 341 → 开分支动手 ✓（首次完整闭环）。
5. **arm64 侧服务**：深度机 338-a（C19）回签=❌ 依赖顺序待解（338-a 基线含 T50 前的代码而 426 期望是 T50 后版本→331-a 须先集成）；单位机 329-D12 自验=✅ PASS（零警告+单测 1353/1353+E2E 454=452/0/2）。
