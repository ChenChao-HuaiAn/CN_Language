# 更新日志

## 功能完善总结

### 语法分析.cn 冒烟 55 错四簇消化 + 簇⑥宿主星号泛型实例名误剥根治（2026-09-04，单位机 麒麟 ARM64）

本轮以「语法分析.cn 冒烟 55 错」为目标启动，经三层定位法（文件分摊→函数二分→形态探针矩阵）分诊出四簇根因，实施中连带揪出并根治簇④⑤⑥，最终 **5 个 v2 组件冒烟语义 0 错**（词法分析/IR签名/IR容器/语法分析 + 连带的语义分析），新增 E2E 142/143/144 三用例全绿，全量门禁零回归。

#### 一、跨模块符号下沉（簇①，用户裁决 A2：新建 IR常量.cn）
- 新建 `CN语言编译器v2/IR常量.cn`：IR_* 操作码（33 个）/T_* 类型码（14 个）/节点_* AST 类型（35 个）/键位移/当前函数变量类型 静态映射——**单一归属**，无依赖纯常量+静态表，`公开:` 段。
- 原定义文件（IR生成.cn/语法分析.cn）删除常量区；七个组件（语法分析/语义分析/IR生成/IR容器/代码生成/代码生成ARM64/代码生成共用）显式 `导入 IR常量`——依赖图单向无环（原 IR容器 引用 IR生成/语法分析 符号不可导=循环，主链靠 P6h 全局符号表合并掩盖，冒烟链 4 文件暴露）。
- 消错：冒烟链 35 错 + IR容器 单独入口 14 错（节点_*/键位移，被语法分析.cn 在链自然抵消的部分一并根治）。

#### 二、导入项登记+裸名改写（簇②，用户裁决 B1 + B1a 边界）
- 语法分析.cn 新增 `导入项们` 表（项名ID→限定名ID）+ `登记导入项们`（路径::项 / ::{项列表} / 项 作为 别名 三形态；通配 `*` 不登记）；`扫描导入` 调用 + **解析程序 入口清空+重扫为权威登记点**（依赖先解析窗口会抹掉 扫描导入 时的登记——实测 4 错残留定位；花括号形态限定名构造改显式保存模块名，防位置偏移反查取到 `::`）。
- 调用形成时（解析后缀链 `(` 分支）被调标识符（**参数起点-1**，非池尾）命中表即原地改写合成限定名——第五波限定名机制同构，语义白名单/IR 映射零改动即通。
- **B1a 诚实边界（用户裁决）**：宿主 `导入 文件::打开文件` 绑定 stdlib/文件.cn 包装函数（返回 `结果<空类型*, 整32>`），v2 现实现改写为限定名内置直调（返回裸指针）——**返回契约分叉**入档 plans/014 灰色点，随「v2p 源码级并入 stdlib」专项（依赖枚举/正常-错误构造器/核心错误码模块三层，v2 皆未实现）收口。E2E 143 只锚定裸指针契约形态（`空类型* 句柄 = 打开文件(...)`）。

#### 三、自减全链（簇③，用户裁决 C1）
- 词法分析.cn 双字符表补 `--`（45,45——原 ++/+=/-=/-> 齐独缺，`i--` 切双 `-` 换行粘连成非左值赋值/吞声明，冒烟 11 错同根因；规范 03:44/61 语句级 `--i;` 明文合法、宿主已支持 E2E 134）。
- 语法分析.cn 新增 `节点_自减 = 35` + 前缀/后缀语句分支（与自增对称）；语义分析.cn 自减分支（未声明/常量拒绝）；IR生成.cn 抽 `发射自增自减` 共享函数（局部槽/参数槽/引用参数指针写回/静态槽读写对四路径，运算码 IR_加/IR_减 分派）——自增分支体平移，行为零变化。
- v2p 尾部负测自检：常量自减/未声明自减（主.expected 固化）。

#### 四、泛型实参星号后缀（簇④，簇②再分诊揪出）
- 语法分析.cn `解析类型文本` 加 `是实参` 参数：递归实参位消费后缀 `*`/`&` 并入合成名（对齐宿主 canonical 保留尾 `*`；外层类型位的指针层数/引用登记由 解析类型 循环处理，参数化防抢吃——`整64* p` 指针层数归零误伤实测后修正）。
- `结果<空类型*, 整32> 名 = ...` 声明错位（`,` `整32` `>` 成孤儿 token 切垃圾语句：赋值左值错位「可写左值」+声明名吞掉级联未声明）根治——A/B 探针实证无星号 0 错/星号 3 错。

#### 五、结果字面量初始化拒绝（簇⑤，E2E 144 探针揪出·132 半实现纪律）
- 语义分析.cn：结果/容器类型声明的**字面量**初始化（`= 无`/`= "串"`/`= 123`）显式拒绝——原错误放行+IR 按「结构体拷贝」瞎发射（字面量槽越界读 12 字节+垃圾值解引用=运行期 SIGSEGV，探针 P2 实证；宿主同款源码显式拒绝「无法隐式转换」）。拷贝（`= 导入模块们`）/内置直调（B1a 边界）形态放行，边界注释入档。
- v2p 尾部负测自检：结果字面量初始化（主.expected 固化）。

#### 六、宿主星号泛型实例名误剥（簇⑥，宿主缺陷优先纪律当场根治）
- **现象**：`盒子<整64*>`（泛型+指针实参）成员访问/方法调用报「类型 '盒子$整64' 不是结构体/联合体/类类型」；最小复现 11 行（泛型类+指针实参）。**根因**：泛型实例名含实参星号（盒子$整64*——合成名按 canonical 保留尾 `*`），语义/IR 三处按「尾 *=对象指针」误剥（semantic_expr.cpp visitMemberExpr / semantic_call.cpp clsName / ir_oop_call.cpp canonObjForMethod）；调试打印实证 varType 全程正确、丢点全在消费侧。
- **修复**（三处同款）：**原名查类命中即用原名**——真指针（盒子$整64**）不会命中类表，自然落入剥指针分支，v2.1 自动解引用行为不回退。全链探针（字段+方法+解引用+写透+向量<整64*> 容器）exit=0。
- **连带发现（观察项入档）**：`cn check` 对含导入的非入口文件名（≠主.cn）私有函数体被模块可见性过滤**静默丢弃=检查假绿**（簇⑥ 定位过程中多个「检查通过」实为没检查——最小复现必须用 主.cn 文件名让检查真实发生）。

#### 七、语句边界模型裁决（规范级，专项立案呈报）
- 用户裁决**方案B 强制分号（Rust 式）**：语句必须 `;` 终结（Rust/C++ 同款；CN 语句式语言无块尾表达式语义=全强制）——规范变更（plans/001 与 cn-language-spec 01a/03「换行≡空格/无强制分号」条目反转）+ 宿主/v2 双侧实现 + 全库存量迁移（146 E2E 用例+11 v2 组件+stdlib）。**专项计划文档已立案呈报（等用户批阅后实施）**；与三簇独立，实施顺序=本轮收尾后专项轮。

#### 验证（单位机 麒麟 ARM64）
- 冒烟：**5 组件语义 0 错**（词法分析/IR签名/IR容器/语法分析/语义分析——第 5 组件为 IR常量 下沉连带达成）；IR生成 入口 96 错=下一批冒烟目标。
- 运行时探针：C1 自减四路径（a=3 b=98 静态=9）、簇⑥ 全链、向量<整64*> 容器全链全部 exit=0。
- E2E：新增 **142_v2_自减全链 / 143_v2_导入项裸名改写 / 144_v2_泛型星号实参**（+供给 B7：向量$整64* 方法符号宿主实例化）六步闭环全绿；v2p 尾部四负测自检行（常量自减/未声明自减/未导入裸调/结果字面量初始化）固化。
- 门禁：宿主清场重建（gcc ARM64 Debug）零警告 + 单测 **1216/1216** + E2E 全量 **146 用例 142 过/1 败(78 已知遗留)/3 跳(平台限制) 零回归**。
