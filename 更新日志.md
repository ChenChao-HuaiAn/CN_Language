# 更新日志

## 功能完善总结

### 强制分号语句边界改造（plans/015 方案B·Rust 式，2026-09-04 家机 win-x64，用户裁决批复实施）

**规范级反转**：CN 语言语句边界从「可选分号+换行≡空格+关键字边界识别」反转为「**语句必须 `;` 终结**（Rust/C++ 同款）」——行尾与下行行首词法可拼导致的静默粘连事故链（泛深-- 粘连/&v 粘调/3-- 等，plans/015 立案陈词）就此结构性根除。裁决明细：`导入 路径;`（Rust use 同款）、顶层 `常量 T = 2;`/`静态 类型 名 = 值;`（C++ const 同款）强制；枚举/结构体/类/接口体 `}` 自终结不加；表达式内部换行仍≡空白（运算符悬挂续行不受影响）；循环头三段分号保持分隔符语义。

#### 一、规范侧（plans/001 + cn-language-spec 双侧）
- plans/001 §4.6「语句终止与续行」整体改写（三层机制表/EBNF/规则1-5：强制分号/续行不变/单行多语句/空语句/模块级声明分号裁决）+ §5.9.6「无强制分号」反转为「分号模型」+ §754 v2.0 变更行同步；顺带修正 §5.9.6 过时 .ai-coder 引用为 .zcode。
- cn-language-spec（单一事实源 .zcode，.agents junction 自动同步）：01a 第三节/03 第零节同构反转；03 示例 37 行补分号；08 第八节「无强制分号」改「分号模型」（模块声明无分号/导入与常量静态须分号）+ 头部范围行+对照表；09 示例导入分号（全 spec 导入/常量/静态示例 32 处批量修正）。

#### 二、宿主实现（src/cn_compiler）
- `consumeSemicolon()`（parser.cpp:190）语义反转为**强制**：缺失即报「语句缺少分号 ';'」（诊断位置=语句结尾 Token，行列定位），不消费当前 Token——上层循环继续解析，一次编译报全量缺分号位置。
- 非语句终结场景改 `match(TokenType::Semicolon)` 保持可选：结构体字段分隔/模块声明/接口方法签名/函数原型后（未裁决项维持现状）；导入语句（parser_oop.cpp:168）保持 consumeSemicolon=**强制**（裁决）。
- 顶层常量/静态声明（parseProgram）补 require——原**无任何分号消费**（迁移后 `;` 成顶层孤儿报「预期顶层声明」的根因，前置门禁 6 个 v2 闭环用例失败同因实证）。
- parseBlockStmt 补零消费防御（孤立运算符/逗号防死循环，对齐 v2 解析块循环保护纪律）；连带根治 parseSwitchStmt 语句后冗余 consumeSemicolon（原可选无害、新语义下双重要求第二个分号误报，449 行删除）。

#### 三、v2 实现（CN语言编译器v2）
- 语法分析.cn：新增顶层 `静态 整64 语法错误数 = 0` + `吃分号` 函数（缺失=stderr 诊断「[parse] 语句缺少分号 ';'（行=N）」+计数，不推进位置——解析块 循环继续解析下条，一次编译报全量）；**18 处接入**：解析语句 全部简单语句分支（返回/中断/继续/局部常量/变量声明/赋值/解引用赋值/解引用复合/解引用表达式/自增×2/自减×2/复合赋值/成员赋值/表达式语句兜底）+ 解析常量声明/解析静态声明（顶层裁决）；控制流分支（如果/当/裸块）不接（} 自终结）。
- 主.cn：语义分析前检查 `语法错误数 > 0` 即打印中止诊断并 `返回 1`（不产 IR/asm——对齐宿主 diagnostics.hasErrors 构建失败纪律与 c3d96a9 语义错误即中止同族）；内置回退源码补分号合规。
- **连带根治**：解析结构体声明 字段循环补字段间分号宽松消费（原遇 `;` 走 解析类型 零前进=**死循环**——127 嵌套容器用例 v2p 编译卡死 40 分钟实证，tasklist 定位 v2p.exe 挂起+限时复现）。

#### 四、全库存量迁移（token 级迁移引擎，一次性脚本用后删）
- **范围与量**：271 个 .cn 文件 15038 行（E2E 用例 194 个**expected 零漂移**/v2 组件 12/**v1 组件 45 连带**——ModuleDriverTest 编译 CN语言编译器/包.cn 等在用/stdlib 10）+ 单测嵌入源码 2085 处（.cpp raw string `R"CN(...)`/普通串/相邻字符串拼接序列——拼接整体 token 级+插入分号按字符归属拆回各原串）+ 自举内嵌源码字符串（72-75 用例 `\n` 切段 token 级；**71 词法用例除外**——token 流计数锚定，加 `;` 产新 token 破坏断言）+ 19 语句终止用例人工改造（单行双语句补分号+注释文案随规范反转，expected 零变化）。
- **token 级引擎要点**（行级模型能力外的全覆盖）：注释（`//` 到行/NL 转义）与字符串（普通/原始/多行三引号/`\n` 转义归空白）感知；花括号语境栈 block/lambda/literal/enum 四态——函数定义头回扫参数表 `(` 前的 `函数` 关键字与 lambda `](`/`-> 返回类型` 链区分（类内成员函数/运算符重载头/继承头/无括号控制流头各自回扫判定）、嵌套字面量内层 `}` 防误插；圆括号深度（跨行调用/下标内部不判）；跨行边界的语句起点判定（`名=`/`名[`/`名++`/`名 op=`/`名(`等标识符形态**仅跨行判**——同行与类型前缀声明/链式访问中段同构歧义留人工，2 处单行双语句人工修）；行内块（`如果 (...) { 返回 "++" }`）与后缀错误传播 `表达式?` 语句尾；`外部` 上下文关键字双语义（`外部 函数`=FFI 头不插 vs 普通变量名正常判定）。
- **迁移正确性证据**：枚举体零污染（成员逗号分隔独立语境）、`R"CN(` 定界完整保留、断言期望串（含 CN 关键字短语）不误改（单段门槛须块结构）、预处理指令段（`#如果定义`）跳过、双分号零引入。

#### 五、测试
- **单测新增 7**（test_parser.cpp）：StmtMissingSemicolonVarDecl/Return/ExprIllegal（缺分号报错+「语句缺少分号」诊断子串锚定）+ TopLevelConst/Static/ImportMissingSemicolonIllegal（三项裁决负测）+ StmtSemicolonTerminatedAllLegal（全形态正测：常量/导入/块内/控制流/返回）。既有「无分号合法」测试经源码迁移后通过（断言未改，名称语义漂移随轮披露）。
- **E2E 新增 2**：145_语句分号强制拒绝（宿主负测·期望编译失败通道：导入/常量/静态/变量/打印/返回六负形态一次编译报全量，「语句缺少分号」子串锚定）+146_v2_缺分号拒绝（v2 负路径闭环：v2p 退出码 1+无 asm+三诊断行 expected 固化——139/141 同款 runner None 形态注册）。
- v2p 冒烟五组件（词法/语法/语义/IR签名/IR容器）**语法 0 错+语义 0 错**；127 卡死复现修复后限时 [sem5] 正确程序错误数=0。

#### 六、验证（家机 win-x64）
- 构建零警告（MSVC /W4 /WX）+ 单测 **1223/1223**（1216+新增 7）+ E2E 全量 **148 用例 146 过/2 败**（78/79 已知遗留 OOM 家族，78 单跑复证 4110MB 超限同形态非回归）+ v2p 冒烟 5 组件 语法 0 错+语义 0 错 + 145/146 单跑双绿。
- 前置门禁（单位机变更复验）：构建+单测 1216+135 用例绿零回归（6 个 v2 闭环失败=本轮中间态撞车非单位机回归，已随终轮收口；78/79 已知 OOM 遗留单跑复证同形态）。
