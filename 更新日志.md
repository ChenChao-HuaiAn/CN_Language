# CN语言编译器 更新日志

## 功能完善总结

# 第四十轮（2026-09-09 家机 win-x64）：跨机轮 win 收官——三平台不可移植缺陷四枚根治 + 全量门禁复验零回归

按 HANDOFF 挂账1 拉取 1a12bc3（第三十一~三十九轮，深度系统 x86_64 8 轮+单位机麒麟 ARM64 1 轮+两机合并）执行跨机轮 win 侧收官。本轮 win 门禁连拦**四枚平台缺陷**（深度机/单位机侧不可见——跨机轮价值实证），四枚均按用户裁决准则（Rust 准绳：性能第一/安全第二）选方案A 根治。

## 一、四枚缺陷与根治

1. **intern_api.cpp GCC 内建不可移植**（第三十七轮 CN_INTERN_TRACE 插桩）：
   `__builtin_return_address(0)` 为 GCC/Clang 专属，MSVC error C3861 构建即败。
   方案A：编译器内建平台分流——MSVC `<intrin.h>` `_ReturnAddress()` intrinsic，
   GCC/Clang 保持原内建；两者均编译期展开零运行时开销，诊断 ra 反解能力全平台
   对等（rustc 同构：平台差异用原生内建分流，不设运行时抽象层）。
2. **同段 getenv 触发 MSVC 弃用**：C4996 在 /WX 下视为错误（首错 C3861 遮蔽、
   修复后才暴露——同段代码两枚平台缺陷串联）。方案A：跟随仓内 cn_main.cpp
   getEnvVar 安全 CRT 先例，`getenv_s` 空值查询只判存在性（免分配免释放）。
3. **win 全量 E2E 40+ 用例齐爆 ml64 汇编失败**（首发缺陷）：
   - 表象：73 个 A2005 symbol redefinition（bb1/bb2...块标签）——误导面。
   - 实锤根因（看首错）：27 个 **A2043 identifier too long**——289 字符 mangled
     符号超过 **MASM 247 字符标识符硬上限**。第三十四/三十八轮「项目内包根成员
     统一 包名::主干 命名」使组件链限定名加前缀，hex-mangle 每汉字 6 字符膨胀后
     超限；GAS 无符号长度限制故 linux/arm64 侧全绿掩盖。
   - **A2043 的失效形态不是单点报错而是连锁崩坏**：MASM 遇超长 PROC 定义行即
     弃置（PROC 未打开/ENDP 未闭合），后续全部函数的「PROC 内局部标签」落入同一
     未闭合作用域 → 同作用域同名 bbN → A2005 雪崩。
   - 实证链：git worktree 检出 HEAD(831a165) 同用例对拍——HEAD 产物 PROC/ENDP
     符号最长 231/超 247 数 0/ml64 RC=0；本轮产物最长 289/超限 34/RC=1——产物
     差异与第三十四/三十八轮前缀命名严格对应。
   - 方案A：nameMangle 单点包裹 shortenLongSymbol——输出 >200 时收缩为
     「头段(180)$L<原长>H<FNV-1a64>」：内容哈希含参数串（重载不冲突）、嵌入原长
     使碰撞面 2^-64、头段保留包名/模块路径 hex 头供人工归类；LLVM/rustc 对超长
     mangle 用内容哈希截断同构。仅超长符号短化=短符号与既有产物逐字节一致
     （字节级对拍纪律零扰动）；nameMangle 单点包裹保证 PROC/ENDP/call/EXTERN/
     虚表/静态全部引用一致。
4. **run_e2e.py win 负路径整树复制漏支**（第三十一轮改动面）：copytree 四象限
   （linux 负/linux 正/win 正/win 负）加三漏一——168/169/170 期望 sem5 语义诊断行，
   实际 v2p 停在 p6b「无法打开依赖模块」（依赖 .cn 未随入口复制）。补同款
   copytree（同款 ignore 模式）后单跑三用例全绿。

## 二、门禁收官（全绿口径与第三十轮基线一致）

- 构建零警告（MSVC /W4 /WX）；单测 **1251/1251**。
- E2E win-x64 全量 **177 用例 175 过/2 败/0 跳**——唯一败面=78/79 已知 OOM 家族
  （4108/4130MB 被 4096MB 内存保护自动终止，v1 旧架构固有= v2 重建动机；与第三十轮
  基线 4131/4132MB 一致，零回归）。修复前状态：172 过/5 败（168~170 + 78/79）。
- 三平台门禁矩阵第四十轮收口。win 侧三处代码修复对 linux/arm64 零改动静态论证：
  ①②`#if defined(_MSC_VER)` 分流、GCC 路径逐字节原样；③nameMangle 短化仅
  x64_codegen.cpp（win 后端独立目录）；④run_e2e copytree 平台中性（linux 侧
  既有行为不变）。arm64 侧动态复验=单位机下一轮。

## 三、文档

- lessons.md 新增三条：MASM 247 字符上限与 A2043 连锁失效形态（权重 10）/GCC 内建
  与安全 CRT 跨平台入库纪律（权重 8）/多分支对称改动四象限穷尽（权重 7）。
- plans/014 状态表第四十轮行、HANDOFF.md 重写。
