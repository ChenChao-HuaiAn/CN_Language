# 更新日志

## 功能完善总结

### v2 灰色点①③④⑤立案根治（缺陷零容忍纪律执行，2026-09-04 家机 win-x64，用户裁决）

用户重贴 AGENTS.md 开发纪律（缺陷零容忍·强制汇报/尽善尽美/一劳永逸）裁决：HANDOFF 灰色点披露
各缺陷立即全部根治、不留到后面。本轮将上一轮披露的五个灰色点中的四项 v2 侧缺陷（①③④⑤）
全链根治并 E2E 锚定；②（字符串 `==` 指针比较）属规范语义裁决，三方案已呈报待批；
⑥（三元 `?` 与错误传播 `?` 消歧）为未实现特性非缺陷，随 v2 错误处理特性推进对齐。

- **① 真/假 布尔字面量**（语法分析.cn + 语义分析.cn + IR生成.cn 三层）：新节点
  `节点_布尔字面量=34`（文本=1/0 值）——原第五波复用整数字面量（真=1/假=0），类型推导整32、
  IR 登记 T_整64，`打印(真)` 输出 `1`（宿主输出 `真`，两侧行为分叉）。独立节点后：语义推导
  布尔、IR 登记 T_布尔，打印/拼接分派自动走 `__cn_print_bool`/`__cn_str_from_bool`
  （规范 02：布尔=基础类型 1 字节——对齐宿主，非规范变更）。`无` 保持整数字面量 0
  （空指针语义，指针判空消费）。
- **③ 解引用复合赋值 `*p op= r`**（语法分析.cn P6c 分支）：原分支只认 `=`——`*p += 1`
  走「非赋值」路径仅消费 `*p`，`+= 1` 成孤儿 token 触发块循环保护中断，块内后续语句
  （含 返回）全部丢弃而 asm 仍生成=残缺产物（宿主分号语境支持，两侧行为分叉）。补复合
  赋值分支（方案A 对齐兜底复合分支——回退重析左值副本，合成 二元(op)+赋值，子终点=1
  合成标记，与标识符/成员/下标形态完全同款）；语义/IR 复用既有解引用左值链路零改动。
- **④ 窄宽强转 整8/整16**（语义分析.cn 白名单 + IR生成.cn 目标码特判 + 代码生成.cn/
  代码生成ARM64.cn 窄宽发射）：原语义白名单九类型名缺 整8/整16（报「未声明函数: 整8」，
  宿主整8(300)=44 正常）。三层补齐：① 白名单补两名（正8/正16 暂不补——类型码层
  整8||正8 同码 T_整8 无符号语义缺位，补名=静默错误代码，维持显式拒绝待类型码体系支持）；
  ② IR_类型转换 目标码特判（类型码() 将整8/16 归 T_整64，强转须窄宽码——仅强转分支特判，
  其余类型码消费方零变化）；③ win 发射 `mov al/ax, byte/word ptr`+`movsx`、ARM64 发射
  `sxtb/sxth`（双后端对称）。
- **⑤ v2p 语义错误即中止**（主.cn 驱动器）：原 `err = 语义分析多文件(...)` 只打印错误数、
  无条件继续 IR 生成/代码生成（错误程序仍产 asm 可被链接=错误产物，违背生产编译器纪律；
  宿主 driver 语义错误即构建失败不产物）。补 `err>0` 打印诊断后返回 1（不产 IR/asm；
  尾部组件自检仅在入口干净时运行——既有 E2E 用例 err=0 输出流不变）。
- **测试**：新增 E2E **138_v2_灰色点收口**（①③④ 正路径：拼接 from_bool 分派锚定 T_布尔/
  `*p += 1` 全形态/整8(300)=44·整16(70000)=4464·整8(-1)=-1 符号保号，退出码 0 十断言；
  宿主 sanity 同源跑通）+ **139_v2_语义错误中止**（⑤ 负路径闭环：run_e2e.py 扩展
  预期退出码 None 形态——v2p 退出码非 0、无 asm 产出、中止诊断行 expected 固化；
  linux-arm64 侧负路径待 ARM64 复验轮对齐，已加诚实边界提示）。
- **验证**：四项修复均经独立探针运行时锚定（①打印(真)→`真`/`假` 与宿主一致；③`*p += 1/
  *p = 10/*p -= 3`→6/10/7 与宿主一致；④整8(300)→44 asm 可见 `mov al, byte ptr`+`movsx`；
  ⑤退出码 1 且无 asm）+ E2E 138/139 六步闭环 PASS + 宿主 sanity 先行；全量门禁见 HANDOFF。
