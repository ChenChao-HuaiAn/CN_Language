# 更新日志

## 功能完善总结

**plans/017 v2self 下一代重编锚定：双键登记后自举+字节级固定点达成+8 用例运行对拍全绿（2026-09-07 第二十七轮，深度系统 x86_64，纯验证轮零代码改动·Rust 准绳：性能第一/安全第二）**

1. **新一代构建链（全部本机实证）**：v2p_gen2（宿主 cn 编译 v2 源码，HEAD
   含第二十五轮重载消歧+第二十六轮双键登记修复）→ v2self_gen2 重编 rc=0
   **192288 行 asm**（上代 191956 行，+332=签名键/双键登记/自检显示名净增量）
   → as → g++ 链接（v2self obj 在前+muldefs，对齐 run_e2e.py 编排）成功；
   12 组件+min1 冒烟逐项一致（IR容器.cn rc=1 失败形态逐字一致=既有灰色点
   非回归）。
2. **对拍三锚定（本轮核心成果）**：
   - **组件产物对拍 12+1 全 PASS**：词法/语法/IR常量/abi辅助/min 裸逐字节
     一致；语义分析/IR签名/IR生成/代码生成系 7 文件差异 **100% 均为
     strData_N 驻留 ID 平移**（宿主与 v2 编译器的字符串驻留序差 1，产物内
     自洽零语义影响），按「定义内容重映射」规范化后全部逐字节一致。
   - **全链自举产物对拍**：v2self_gen2 编译 v2 源码（192288 行）vs v2p_gen2
     编译 v2 源码规范化后逐字节一致。
   - **字节级固定点（里程碑）**：g3（v2self_gen2 编 v2 源码产物）链接为
     v2self_gen3 → 再编 v2 源码 → g4，**g3 与 g4 裸逐字节一致**——自举产物
     代际完全稳定（gN=gN+1）；上代锚定口径为探针级（min1/min18+九探针），
     本轮升级为全链+代际+固定点三级。
3. **8 用例运行级对拍 8/8 PASS**（v2self 产物 as→链接→运行 rc 对比预期+
   产物 v2p vs v2self 规范化一致）：119 多文件（14）/123 容器大用例（85）/
   126 供给（0）/128 内置拼接（0）/130 指针下标（0）/148 retbuf（0）/
   150 引用返回（第二十六轮修复锚定，0）/158 SysV ABI 五分歧面（0）——
   补齐「v2self 产物可正确运行」实证（冒烟只证编译行为一致）。
4. **观察项两条（非缺陷，披露）**：①strData 驻留 ID 宿主-v2 差 1（组件级
   asm 对比首次执行才暴露，非本轮回归；宿主-v2 产物逐字节对齐=远期专项；
   v2self 家族内部已字节级稳定——Rust 对齐：rustc 构建可重现纪律=同输入
   必同输出，v2 家族内已达标）；②入口打开失败回退内置源码（[p6f] 打印后
   继续——本轮探针脚本入口路径双拼 target/target/ 亲踩：假「全绿」实为
   内置回归结果 rc=19；编排类对拍必须核对 [p6h] 入口行+[p6f] 回退行+
   函数数）。
5. **门禁全绿**：构建零警告（GCC -Wall -Wextra -Werror，HEAD 零改动）+
   单测 1240/1240+E2E linux-x86_64 全量 160 用例 156 过/0 败/4 平台跳过
   （与第二十六轮基线一致，本轮零代码改动零回归）。
6. **诚实边界**：win/arm64 侧 v2self 重编锚定=跨机轮；strData 驻留序差异
   定性为观察项未根治（无语义影响）。
