# CN语言编译器 更新日志

## 功能完善总结

# 第五十一轮（2026-09-10 深度系统 x86_64）：类型系统严格化轮——46-f 挂账混合符号二元运算拒绝（用户裁决方案A·Rust 对齐）双侧实施 + v2 类型转换发射族源类型分派根治（用户裁决方案甲）+ v2self 固定点复锚（238017 行）+ 组件对拍 29/29 + 运行级 3/3

## 一、方案A：混合符号二元运算拒绝（规范变更，用户审批后落地）

- **规范**：plans/001 §3.7 新增「混合符号二元运算禁止」——有符号（整N）与无符号（正M）的**变量间**比较 `== != < > <= >=`、算术 `+ - * / %`、位运算 `& | ^ << >>` 编译期拒绝，须显式 `类型名(表达式)` 构造转换；**字面量豁免**（一侧为整数字面量含 `-1` 一元负号形态时按另一侧类型参与——Rust 字面量推断惯例，`大 > 100` 等惯用形态保留）；同符号混合宽度（正32 与 正64）维持既有宽化。依据=第五十一轮呈报探针（target/p50x64l/mixed_cmp 四形态校验和）：隐式宽化在混合符号下静默改变值语义——`整64(-1) < 正64(2^63)` 值域反转双侧同错、`正32(4e9) < 整64(-1)` 宿主 rc=9 / v2 rc=11 / 数学真值 13 双侧分叉。cn-language-spec specs/02-类型系统 同步。
- **宿主**：semantic_expr.cpp visitBinaryExpr 前置检查（比较/算术/位运算三族共用；isIntLiteralExpr helper 判字面量豁免），报错文案固化逃生通道指引。
- **v2**：语义检查表达式.cn 新增 检查混合符号二元（表达式类型ID 推导族+静默诊断缓冲——检查表达式 有诊断副作用非幂等，操作数已由语句根线性遍历权威检查，二次推导诊断吞弃）+整数族/无符号/字面量末端三判定 helper；语义检查.cn 二元分支检查范围自比较符扩至算术/位运算符，**带诊断操作数递归限定回比较符**（根治 174 误伤：算术二元二次递归致 未声明标识符 双报 14→15，全量 E2E 揪出）。

## 二、方案甲：v2 类型转换发射族源类型分派根治（Rust as 语义对齐）

- 193_v2 正测红暴露既有缺陷：IR_类型转换 指令类型字段只承载目标码，发射层按它选读取扩展方式——`整64(正32变量)` 落 movsxd 符号扩展兜底（4000000000 出 -294967296）、`整64(正64变量)` 低 32 截断（2^63 出 0），宿主 emitCast 按源分派正确，同一源码两侧不同结果。方案A 报错文案推荐的逃生通道 `整64(表达式)` 在 v2 侧本身是坏的——呈报三方案，用户裁决方案甲。
- **修复**：生成转换或构造调用 目标 64 位码时查源寄存器登记类型改标指令类型字段（源正32→T_正32 零扩展读；源整64/正64→T_正64 64 位直拷=同宽位模式重解释；源整32/未登记→维持 movsxd 兜底），原目标码另存供产物寄存器登记（正32(x) 产物参与二元无符号性不再丢失）；三后端发射：X64L `mov r10, qword ptr`、win `mov rax` 直拷新分支，ARM64 x 寄存器 64 位直通=直拷（注释固化）。探针实证双侧一致：4000000000 / -9223372036854775808。

## 三、E2E 与验证矩阵

- 新增四用例双通道 PASS：**192_混合符号二元运算拒绝**（宿主负测 期望编译失败.txt）/ **192_v2**（v2 负路径 4 诊断行固化+中止纪律）/ **193_混合符号显式转换**（宿主 stdout 双锚 3999999999/29）/ **193_v2**（rc=29 五锚校验和：显式转换真+1/假+0+字面量豁免+4+8+同符号宽化+16）。
- 全量门禁：构建零警告+单测 1251/1251+全量 E2E **201 用例 197 过/0 败/4 跳**（跳=62/69/78/79 既有平台限制；201=上轮 197+本轮新增四用例）；宿主编译 v2 全树 rc=0（存量零踩线实证）。
- v2self 锚定链：v2p_new51d 编译 v2 自身→fix_p.s（**238017 行=X64L 新锚定**；上轮 235667+净增 2350）→as+g++ 链接 cn_self51（cn_self51.o 在前+第五十轮 v2p.o 借链+rt48 objs 现编+muldefs）→**绑定自检 cn_main=0x4d9027 ∈ cn_self51.o .text [0x402586,0x4dca59)**→cn_self51 编译 v2 自身→fix_s.s（238017 行）→**固定点 fix_p≡fix_s 裸逐字节一致**；运行级三用例 hello rc=0（你好，世界）/119 rc=14/173 rc=173 全对齐；组件对拍 **29/29 逐字节归零**。

## 四、诚实边界

- win/ARM64 后端方案甲直拷分支与无符号发射经静态论证（汇编形态与 X64L 对称），动态验证=跨机轮；arm64 侧复验/固定点复锚=单位机下一跨机轮。
- 残余观察项（随类型系统严格轮）：宿主 canConvert 注释与实现不符的一致性收口；v2 正8/正16→64 位目标源分派未标注（值域小兜底恰好正确，无独立锚定）；赋值初始化/函数传参面混合符号严格化（本轮按挂账原文只拦二元运算）；整32→正64 语义=符号扩展位模式（Rust as 同款）文档化。
- 探针与脚本留 target/p50x64l/ 不入库。
