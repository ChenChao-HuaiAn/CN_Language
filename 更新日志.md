# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第一百一十轮 110-a：win 侧五项立案收口 + 宿主 win 构建根治（陈旧产物假立案澄清）**
> （2026-09-13，家机 win-x64；基线 a6a933c）：
> ①**根因链**：上轮宿主构建失败（`ir_decl.cpp` **C4702** 无法访问的代码——102-a 有初值分支
> 无条件 `continue` 使下方标量族段成死代码；GCC 不报 / MSVC `/W4 /WX` 拦截）**未被注意** →
> `cn.exe` 停留陈旧产物（不含深度机 93-a~108-a 宿主修复）→ 上轮 E2E 与 win 探针**全部跑在
> 陈旧宿主上**（立案 C/D/E + A 宿主侧 = **假立案**；仅 A v2 侧与 B 为真缺陷）。
> ②**宿主修复**：恢复 isScalarStatic 守卫（102-a 连带丢失——数组等聚合 8 字节 StorePtr 越界
> 风险=87-a 安全边界）——标量统一 StorePtr / 类·容器指针槽 / 其余聚合保持 .data 零；
> 清场重建**零警告** + 单测 **1313/1313**。
> ③**A 项销项**：v2 浮点 size 说明符剩余 **4 处**（`浮宽i`/`宽a`/`宽b`/prologue `sp2`——
> 109-a 只覆盖装载未覆盖存回）→ 249/250/251 v2 ml64 全通过 + 宿主 rc=255。
> ④**B 项销项**（v2 真缺陷，源自 91-a）：**三后端结构体按值栈参数拷贝缺失**——prologue
> `类p==2` 分支被标量路径截获只存 8 字节指针 → 被调方按内联数据访问 → 158_v2 rc=14；
> **三后端同治**（win rep movsb / X64L·ARM64 拷贝助手单点）+ 宿主三后端为正确参照；
> 158_v2 **rc=0** + 9 参数探针三目标验证。
> ⑤**C/D/E 结案**：252 新宿主正确拒绝；260/262 双侧 rc=255；win 实物复验 **11/11**。
> ⑥门禁：全量 E2E **292 用例 259 过 / 33 败 / 0 未实现**（33 败=31 双编译对照平台守卫〈win 侧需 linux as/g++ 链，非缺陷〉+ 78/79 OOM 既有败面——**158_v2/252 已修复不再失败**；对比上轮 257 过/35 败） + 锚定链固定点 **404369 行**两代逐字节一致 +
> 组件对拍 **34/34** + 运行级 **3/3**。
> ⑦教训：陈旧产物假立案（构建失败必须当轮根治；门禁前核对二进制时间戳）/ MSVC·GCC 警告分叉 /
> E2E 与对拍不可并行（共享 target/v2asm.asm 实测冲突）/ size 说明符族两轮补漏→静态扫描门禁登记。

## 深度机 linux-x86_64 节

## 功能完善总结

> 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（零源码改动）（2026-09-13 第一百零八轮，深度机 linux-x86_64）：
> 107-a 遗留「赋值位构造器（`r = 正常(...)` 已有变量）v2 保守拒绝（宿主支持）」评估——完整实现需
> 「赋值位覆盖 + 旧值**联合体条件释放**」（84-e 架构边界：结果/可选 值字段释放需联合体条件分支
> =面外，设施级改造）+ 语料零使用形态（107-a 探针矩阵）→ 裁决**保守拒绝维持 + 负测固化**：
> E2E **264_v2_赋值位结果构造器拒绝**（诊断锚行「构造器 正常 须用于 结果/可选 语境（返回位或
> 结果/可选 变量声明初始化位）（行=14）」+ 退出码=负 + 不产 asm）——防将来误放行致静默截断/泄漏。

> **验证（零源码改动）**：v2p md5 `b6d8c387…`（与 107-a 一致）+ 宿主 md5 `f1484d79…` +
> 全量 E2E **292 用例 288 过 / 0 失败 / 0 未实现 / 4 跳**（+264；其余 291 过=零回归）。
> **D5 面完整处理链收口（107-a+108-a）**：崩溃修复（返回位/声明位）✓ + 拒绝行为固化（赋值位）✓ +
> 架构边界登记（联合体条件释放设施）✓。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
