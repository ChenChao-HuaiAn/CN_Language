## 功能完善总结

**完成时间**: 2026-08-15 14:45

## 第 6 层「全量迁移」完成 ✅

CN 语言编译器模块系统 v2.0 全面改造第 6 层已全部完成：将现有全部 .cn 测试源码与单测迁移到 v2.0 语法（`::` 路径 / 花括号导入 / 默认私有 + `公开:` 显式化），恢复全部预期失败用例。E2E **36/46 → 46/46** 全绿。

---

### 一、改动清单（23 文件）

| 文件 | 改动 |
|------|------|
| [`tests/e2e/27_module/主.cn`](tests/e2e/27_module/主.cn) | `导入 数学.平方根` → `导入 数学::平方根`；`从 数学 导入 双倍, 三倍` → `导入 数学::{双倍, 三倍}`；限定调用 `数学.平方根()` → `数学::平方根()` |
| [`tests/e2e/29_core/主.cn`](tests/e2e/29_core/主.cn) | 5 条 `核心.符号` → `核心::符号` |
| [`tests/e2e/30_container/主.cn`](tests/e2e/30_container/主.cn) | 5 条 `容器.符号` → `容器::符号` |
| [`tests/e2e/31_map_set/主.cn`](tests/e2e/31_map_set/主.cn) | 3 条 `映射集合.符号` → `映射集合::符号` |
| [`tests/e2e/32_algorithm/主.cn`](tests/e2e/32_algorithm/主.cn) | 10 条 `算法.符号` → `算法::符号` |
| [`tests/e2e/32_algorithm/算法.cn`](tests/e2e/32_algorithm/算法.cn) | **补 `公开:` 标签**（v2.0 默认私有，否则全部函数跨模块不可见） |
| [`tests/e2e/35_string_ext/主.cn`](tests/e2e/35_string_ext/主.cn) | 4 条 `字符串扩展.符号` → `::`；`解析.转整数` 等内置调用 → `解析::转整数` |
| [`tests/e2e/38_tool/主.cn`](tests/e2e/38_tool/主.cn) | 9 条模块导入 `容器.向量` 等 → `::`；内置调用 `数学.`/`文件.`/`IO.`/`时间.`/`系统.`/`解析.` 全部 → `::` |
| [`tests/e2e/38_tool/算法.cn`](tests/e2e/38_tool/算法.cn) | 补 `公开:` 标签（内嵌副本） |
| [`tests/e2e/38_tool/映射集合.cn`](tests/e2e/38_tool/映射集合.cn) | `导入 容器.错误码` → `导入 容器::错误码` |
| [`tests/e2e/43_module_import/主.cn`](tests/e2e/43_module_import/主.cn) | `模块甲.加` → `模块甲::加`；`从 模块甲 导入 乘, 最大值` → `导入 模块甲::{乘, 最大值}`；`从 模块甲 导入 颜色` → `导入 模块甲::{颜色}`；`模块丙.减半` → `模块丙::减半`；全部限定调用 `模块X.函数()` → `::` |
| [`tests/e2e/43_module_import/模块丙.cn`](tests/e2e/43_module_import/模块丙.cn) | `导入 模块乙.减` → `导入 模块乙::减`；`模块乙.减()` → `模块乙::减()` |
| [`tests/e2e/41_ctor_overload/主.cn`](tests/e2e/41_ctor_overload/主.cn) | 类内补 `私有:` + `公开:` 标签（v2.0 类内默认私有） |
| [`tests/e2e/42_generic_field_loop/主.cn`](tests/e2e/42_generic_field_loop/主.cn) | 两个泛型类补 `私有:` + `公开:` 标签 |
| [`tests/e2e/25_math/主.cn`](tests/e2e/25_math/主.cn) | 8 个数学内置调用 `数学.` → `数学::`（规范统一） |
| [`tests/e2e/33_io_input/主.cn`](tests/e2e/33_io_input/主.cn) | `IO.` → `IO::` |
| [`tests/e2e/34_file/主.cn`](tests/e2e/34_file/主.cn) | `文件.` → `文件::` |
| [`tests/e2e/36_time/主.cn`](tests/e2e/36_time/主.cn) | `时间.` → `时间::` |
| [`tests/e2e/37_system/主.cn`](tests/e2e/37_system/主.cn) | `系统.` → `系统::` |
| stdlib 10 模块 | 全部文件顶部已有 `公开:`（算法.cn 原无 → **补 `公开:`**）；头部注释"使用方式"示例统一 `导入 模块::符号` / `导入 模块::{符号}`；模块函数体内内置调用 `数学.`/`文件.`/`IO.`/`时间.`/`系统.`/`解析.` → `::` |
| [`tests/unit/module/test_module.cpp`](tests/unit/module/test_module.cpp) | 5 处旧点号限定调用（`数学.平方根` 等）→ `数学::平方根`；保留 `BuiltinQualifiedDotCompat`（专门验证旧点号兼容） |
| [`src/cn_compiler/ir/ir.cpp`](src/cn_compiler/ir/ir.cpp) | **crate 前缀缺陷修复**：入口文件 `主.cn` 的辅助函数不再加 `主$` 前缀（调用侧按纯名重写，此前定义侧 `主$解析单行` vs 调用侧 `解析单行` 链接失败 LNK2019，被 38_tool 旧语法编译失败掩盖） |

### 二、单测结果

| 指标 | 结果 |
|------|------|
| 全量单元测试 | **1097/1097 全绿** ✅ |
| 编译警告 | **0**（MSVC /W4 /WX） |

### 三、E2E 结果

| 指标 | 结果 |
|------|------|
| E2E 总数 | **46/46 全绿** ✅（36/46 → 46/46，10 个预期失败全部恢复） |
| 27_module | ✅ 数学::平方根 + 花括号导入 + 公开/私有可见性 |
| 29_core / 30_container / 31_map_set | ✅ 核心::/容器::/映射集合:: 纯名调用 + 泛型跨模块 |
| 32_algorithm | ✅ 算法::排序整32 等 10 条 + 算法.cn 补 公开: |
| 35_string_ext | ✅ 字符串扩展::替换 等 + 解析::内置 |
| 38_tool | ✅ 全链路集成（容器/映射集合/算法/字符串扩展 + 内置 :: 统一） |
| 41_ctor_overload / 42_generic_field_loop | ✅ 类内 公开: 标签后构造重载/泛型字段循环正常 |
| 43_module_import | ✅ 模块甲::加 + 花括号导入 + 模块乙/丙 依赖链 |

### 四、stdlib 公开性确认（10 模块）

| 模块 | 公开: 标签 | 说明 |
|------|-----------|------|
| 核心.cn | ✅ 已有 | 错误码枚举 + 泛型 交换/最小/最大 + 断言 |
| 容器.cn | ✅ 已有 | 错误码 + 向量/链表/栈/队列（类内 私有:/公开: 完备） |
| 映射集合.cn | ✅ 已有 | 错误码 + 映射/集合（类内 私有:/公开: 完备） |
| 算法.cn | ✅ **本轮补** | 原无标签 → 文件顶部显式 `公开:`（7 个函数全部导出） |
| 数学.cn / 文件.cn / IO.cn / 时间.cn / 系统.cn / 字符串扩展.cn | ✅ 已有 | 全部公开函数在 公开: 块内 |

### 五、坑与解决（关键）

1. **入口文件 crate 前缀缺陷（LNK2019）**：38_tool 迁移后首个暴露——入口 `主.cn` 的辅助函数（统计计算/解析单行/工具主体等 17 个）定义侧链接符号带 `主$` 前缀（第 4 层 crate 前缀机制），但同文件调用侧按纯名重写 → 符号不匹配链接失败。此缺陷被旧语法编译失败掩盖至今。修复：IR 层 `visitFunctionDecl` 加前缀时跳过 `moduleName=="主"`（与语义层调用侧已有判断对齐）。
2. **expected 输出契约**：迁移 43_module_import 时误改输出标签 `从导入乘` → `花括号导入乘`，与 expected 文件不匹配（输出不一致 FAIL）。恢复后通过——**E2E 迁移只改语法不改行为**。
3. **内置函数 `::` 统一**：第 4 层已将内置 key `::` 化（语义层注册 `数学::平方根` 等 24 个），IR 层双映射（`::`/`.`）兼容。本轮把 E2E 与 stdlib 全部内置调用统一为 `::`（25/33/34/36/37/38 与 stdlib 模块体），旧点号仅保留在 `BuiltinQualifiedDotCompat` 单测验证兼容。
