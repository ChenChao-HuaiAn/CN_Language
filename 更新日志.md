# 更新日志

## 功能完善总结

### v2 三元条件运算符 `? :` 全链实现（③ 第六波，2026-09-03 家机 win-x64）

**特性**（规范 01b 5.8：优先级 1.5——低于逻辑或、高于赋值；右结合；惰性求值仅对
选中分支求值）：词法层零改动（`?` 单字符兜底既切出，注释补录），语法/语义/IR 三层
落地 + v2p 负测自检。E2E **137_v2_三元运算符** 六步闭环 PASS；**词法分析.cn 冒烟
[parse] 截断归零**（自举对拍首组件完整编译：1595 节点/语义 0 错/IR 10 函数/asm
16345 行/ml64 汇编通过 60863 字节 obj）。

**三层实现**：
- **语法**（语法分析.cn）：新节点 `节点_三元运算 = 33`（字段：子起点=条件段起点、
  文本=真值段起点、附加=假值段起点、子终点=段末+1，三段连续区间）。解析在
  解析表达式 尾段包装（二元链之后）：真值/假值均为完整表达式递归（假值递归实现
  右结合 `a?b:c?d:e = a?b:(c?d:e)`）；`:` 缺失不消费（防御惯例：块循环保护中断
  stderr 可见化）；v2 无后缀错误传播 `?`，`?` 一律三元。
- **语义**（语义分析.cn）：检查表达式 新增三元分支——两分支类型经段末端节点递归
  推导，**类型可合并判定**（同 ID / 整数族宽化：整8/16/32/64、正8/16/32/64、布尔、
  字符互通——真/假=1/0 字面量、字符零扩展，槽统一 8 字节 Load 无损；浮点族
  浮32/64 互通）/ 跨类拒绝（整 vs 浮位模式不同、字符串 vs 整数指针槽 vs 数值槽
  ——Rust if 表达式同款纪律）。未知(-1)任一侧跳过（级联防）；条件段类型不强制
  （条件跳转按非零判，同 如果/当 惯例）。
- **IR**（IR生成.cn）：预扫 3 跳过三元三段区间（[子起点,子终点) 整段延迟生成），
  主循环三元 case——**复用 125 短路方案**：条件寄存器 → 临时槽（IR_分配 帧登记）
  → 三块（真/假/汇合）条件跳转分派 → 选中分支求值 + IR_存储 → 汇合块 IR_加载
  （类型=真分支寄存器登记类型，未登记兜底 T_整64）→ 登记类型压栈。栈平衡：各段
  递归后弹到进入前深度再压汇合结果（一元模式净 +1，防「下标弹2取到残留」同族）。

**连带根治一：表达式入口一元前缀操作数紧绑定**（既有语义缺陷，三元接入后可见化）：
解析表达式 前缀段（!/-/&/*）原「递归 解析表达式」把后随 `?` 与低层二元吞进操作数
（`!x ? a : b` 解析为 `!(x?a:b)`、`!x + 1` 解析为 `!(x+1)`——均背离 C 一元优先级
12 语义）。统一改**紧绑定**：操作数=解析层_子(层5, 真)（原子+后缀链，嵌套前缀
递归同覆），二元/三元由尾段统一处理——与上轮二元链操作数位前缀紧绑定（第五波）
同一设计收敛为全路径一致。

**连带根治二：静态变量自增静默丢句**（既有缺陷，137 用例踩出当场根治）：IR生成
节点_自增 分支仅认局部/参数槽，静态目标**静默跳过零 IR**（「防御分支静默降级」
家族第四例）。补静态槽自增分支：IR_静态加载 +1 → IR_静态存储（②c C 既有设施，
静态赋值同款读写对）。

**测试**：E2E 137 用例 20 类断言（基本/右结合三形态/优先级两形态/惰性求值副作用
双向锚定/除零保护/整数族宽化/字符串分支/各语句位置）+ v2p 组件侧负测自检
（字符串 vs 整数跨类须拒，expected 固化）。宿主 sanity 先行（退出码 0）。

**冒烟推进**（HANDOFF 下一步 ①② 完成）：词法分析.cn [parse] 归零+语义 0 错+IR
asm 全绿（上轮「调用被调对象非函数名」确认为三元截断级联，已消失）；**语法分析.cn
冒烟启动**：[parse] 全过（4 文件 11684 节点完整解析——语法分析.cn 导入链拉入
词法分析/IR签名/IR容器 全解析成功），语义 54 错——**下一批缺口三簇**：① 跨模块
顶层常量可见性（T_*/IR_* 约 25 错——常量表单文件级，跨文件用常量报未声明）；
② 文件 API 标识符（打开/打开文件/关闭文件 9 错）；③ 可写左值白名单 9 错 +
散件（名ID/当前函数变量类型）——下轮任务面。

**门禁**：构建零警告 + 单测全绿 + 全量 E2E 零回归（78/79 已知遗留内存超限除外）。
