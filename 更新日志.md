# 功能完善总结

## plans/017 呈报①方案A 第三轮：容器所有权语义对齐根治六枚 + v2self 推进至 IR 生成（2026-09-06 第二十一轮 深度系统 x86_64）

### 观察项①②撤销（家机验证完成）
第二十轮立案的「宿主引用参数×构造交互缺陷」「映射& 方法返回类型推导失败」**撤销**——宿主对 mapref 形态的报错系正确诊断：mapref 探针 `导入 容器;` 用 映射 是源程序导入错误（映射在 映射集合 模块）；改正确导入后宿主全绿（引用和=99）。

### 根治第 8~13 枚 v2 缺陷（全部最小探针复现+宿主对拍一致闭环）
1. **池索引 0 参数类型节点漏登记三处同族**（IR生成 收集函数实参结构/权威表登记 + 语义分析 静态声明）：守卫 `>0` → `>=0`——mapref 探针「灌 的 映射& 表」引用实参位漏登记（首函数首参数类型节点恰在池 0）→调用方不取址→被调方双重解引用→映射$链查找 崩（v2self 同点）。
2. **容器=调用返回接收却析构**：宿主实证 元素(i) 返回内联元素地址（视图、不析构）；v2 出口 RAII 析构=对向量内部元素 DeleteObject→double free。修复=预扫「调用返回初值」不入 RAII 名单。
3. **容器=标识符拷贝浅共享→深拷贝构造**：对齐宿主 NewObject+拷贝构造（源=T& 传左值地址）；新增 **IR_静态地址** 指令（三后端 lea/adrp+add）支持静态源。
4. **返回容器局部仍被析构**：跳过名泛化（末节点=标识符即设）——修复返回悬垂指针。
5. **结果<T=结构体> 大小/槽位 16B 截断**：结果大小/结果槽位数 支持结构体载荷（(大小+7)/8*8+8）+结果变量逐槽分配+基槽=最大槽号——修复 retbuf 越界写与变量槽写穿（freg 探针顶层槽被清零→设置 this=null）。
6. **结果.值 结构体载荷被 LoadPtr**：载荷是结构体时压字段地址（注释声明语义至此真正实现）+回扫实参取地址 补 IR_字段地址 复用分支。

### v2self 推进（部分达成）
映射自检通过（二十轮崩溃点）→ 语义分析符号表链全绿（sem5=0）→ **IR 生成阶段崩**（第 7 层新缺陷：min1 四节点最简源复现——v2 编译版 IR 生成基础路径/限定内置层，下轮呈报①继续）。

### 宿主容器值语义四探针 asm 判明（对齐基线）
标识符拷贝=深拷贝；元素(i) 接收=视图（写共享、不析构）；追加=深拷贝入容器；函数返回接收=转移不析构。

### 验证（门禁全绿）
- 构建零警告（GCC -Wall -Wextra -Werror）
- 单测 **1240/1240**
- E2E linux-x86_64 **157 用例 130 过/0 败/27 平台跳过**（新增 **155_容器引用参数与所有权语义**：引用参数/元素视图写共享/深拷贝独立/返回接收四语义锚定）
- v2 13 组件冒烟 rc=0 全绿
- 九探针矩阵（mapref/vcopy/alias/vieww/retown/elecalc/freg/f2/f3）v2 编译→as→g++ 链接→运行与宿主直编**对拍一致全 PASS**

### 文档
plans/017 第十节 + plans/014 状态表第二十一轮 + lessons（权重 10）+ HANDOFF。
