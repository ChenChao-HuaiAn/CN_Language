# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结

> **第八十六~八十八轮跨机验证轮（2026-09-12 深夜；基线 efc0c8c，拉取 9403e29）**：
> 纯验证轮零源码改动——完成 win-x64 侧对深度机 86-a/87-a/88-a 三轮的全部实物验证，
> 并拦截一个 **win 平台特有的内存安全级回归**（完整诊断链 + 三方案呈报）。

> ① **全量门禁**：清场重建 MSVC /W4 /WX **零警告** + 单测 **1306/1306**（前序会话完成，
> 日志 `target/gate_88a_win.log`）+ E2E **275 用例 255 过 / 20 败 / 0 跳**——败=**17 双编译
> 对照**（win 平台限制：13 既存 + 新增 243/244/246/248）+ **78/79 OOM 家族**（win 固有
> 败面）+ **148_v2 崩溃（新缺陷，见③）**。

> ② **win 实物验证链全绿**：锚定链复锚 **fix_p ≡ fix_s 348908 行逐字节一致**（新口径，
> 55 轮 340703 → +8205=56~88-a v2 源码；绑定自检 cn_main∈cn_self46.obj ✓）+ 运行级
> **3/3**（hello 你好，世界 / 119 rc=14 / 173 rc=173）+ 组件对拍 **34/34**（DIFF=0）+
> **win 实物复验 243/244/246/248 全过**（run89 探针：宿主与 v2 两侧各编各跑 rc=255 一致
> ——86-a 三元聚合全链 / 86-a 嵌套深拷与豁免 / 87-a 静态变量全链 / 88-a 短路求值全链
> win 实物首验，弥补 runner 双编译对照 win 平台守卫面）。

> ③ **新缺陷立案：148_v2 win 侧崩溃（0xC0000005，回归）**——诊断链：宿主维度 bisect
> （同 55 轮 v2 树四宿主对照：55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗）→ **87-a 宿主编译
> 引入**；最小复现 min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main
> 帧 384（正常）/368（崩版）；机制=**崩版该调用点缺 retbuf 隐藏参数**（正常
> `lea rcx,[rbp-384]`+返回物化拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf
> 指针写入 → 0xC0000005；**深层特征=v2 编译器「返回物化判定」对内存状态敏感**（同一
> v2p 在「仅源」（E2E 调用方式）/「源+平台」参数下判定翻转；87-a 静态初始化改动使 win
> 侧「仅源」进入无物化分支）；linux 侧不触发（深度机 271 过含 148_v2）=**win-x64 特有的
> 跨机轮拦截成果**。

> ④ **三方案呈报（待用户裁决）**：**甲（推荐）** 根除内存状态依赖=定位 v2 编译器
> 未初始化读取/脆弱判定（Rust 对照：确定性编译 + Miri 未初始化检测；性能零开销）；
> **乙** 确定性门禁强化（同输入重复编译逐字节一致 + 参数模式交叉；仅防线不修复）；
> **丙** 三后端静态处理对齐审查（win/linux/arm64 逐点对照，消除 87-a 三后端分叉温床）。

> ⑤ **诚实边界**：本轮纯验证轮零源码改动（回归缺陷根因未确证到"未初始化读取点"——
> 留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；17 双编译
> 对照失败为既有平台项；78/79 OOM 为 win 固有败面；探针脚本留 `target/p89win/` 不入库。

## 深度机 linux-x86_64 节

## 功能完善总结

> 98-a 字符串元素数组释放（C9）销项——双侧（宿主+v2）数组元素 RAII（2026-09-13 第九十八轮，
> 深度机 linux-x86_64）：栈数组字符串元素（`字符串[2] 表 = { 字符串复制("甲"), ... };`）在
> 块出口/函数尾**双侧同缺**释放（宿主 79-a 靶子面「数组元素残留 2」）；本轮按 Rust
> `[String; N]` 作用域 drop 语义双侧实现。**宿主与 v2 均变更**。

> **宿主实施（ir 层）**：新名单 `ownedStrArrayOrder_` + 基线栈 `scopeStrArrayBase_` + 上下文
> （LoopContext/SwitchContext）字段 `strArrayBase`；`emitStrArrayElemFreesFor`（编译期展开逐元素：
> 数组基址 + i×元素大小 → LoadPtr → `__cn_str_free` → StorePtr 0 清槽=幂等模型）；三挂点
> （块出口 genBlockExitDestruct / 跳出 genJumpDestructFrom / 函数返回 genStringFrees 返回块注入——
> 收集方式=扫 Alloca ∩ oopVarSrcTypes_，不依赖被截断的名单）。

> **v2 实施（4 文件）**：函数IR 加 `字符串数组变量们`/`循环数组串基线`（生成函数 **与 生成类**
> 两构造点同步）；`发射数组串元素释放`/`发射数组串释放全部`；块出口析构/循环跳出析构加数组串
> 基线段；生成块/生成当 基线传递；生成数组局部登记。

> **踩坑五连（全部现场根治）**：(a) 宿主登记点置于 genVarDecl 后段——数组初始化列表分支以
> `return` 结束=**不可达**（asm 零 free）→ 移到 allocVar 之后；(b) v2 返回释放只加**引用返回**
> 分支——普通返回漏网（mem6「后=2」）→ 补；(c) v2 函数尾兜底疑「名单截断」实为 (b)，干净版复核
> 排除假象；(d) **类方法构造点漏同步**（生成类.cn 初始化清单缺新字段）→ 编译 228 类用例时 **v2p
> 自身空指针崩溃**——**lessons 67 坑再现，被全量 E2E 当场拦截**；(e) 调试注入 `IR层诊断` 会
> **中止编译**（调试版产不出 asm=「未释放」假象）→ 「干净版复核」纪律。

> **门禁**：E2E **258_双编译对照字符串元素数组释放**（移植纪律 9 第二十六个：八形态折校验和
> **255**——块出口/循环跳出/嵌套块/非字符串对照/多数组叠加/元素读正确性/首末元素/函数尾，全部
> 以 `内存::活动分配数` 差值锚定）。**反证双件全命中（缺失位数精确）**：A 宿主登记失效 →
> 宿主 **168 = 255−87**（缺 ①1+②2+③4+⑤16+⑦64）；B v2 登记失效 → v2 **40 = 8+32**。
> **探针 6 件双侧一致**（mem1~mem6）。

> **验证链全绿**：清场重建**零警告**（宿主 md5 `0d83b77535d9bffb6d864d315001cd05`）+ 单测
> **1313/1313** + E2E **286 用例 282 过 / 0 失败 / 0 未实现 / 4 跳**（+258；其余 285 过=零回归）+
> 锚定链固定点 **380003 行**（97-a 后 377843；+2160）两代逐字节一致 + 绑定自检 + 组件对拍
> **34/34**（DIFF=0）；v2p md5 `8c2884a377b25ebfe79ebcd4c0ccc159`、cn_self98 md5
> `ea6f7d0a813771f9a8c8c2e61f16afcf`。

> **诚实边界（登记）**：结构体**字段数组**元素=字符串的释放面未接（本轮限局部数组，登记 C11）；
> 元素=含串字段结构体的数组逐元素字段递归释放登记（C12）；元素=驻留字面量（非拥有）不释放
> （污染模型同字符串变量）。**C9 销项**（plans/021 §4.2 台账）。详见 `plans/020` 第三十八节。

## 单位机 ARM64 节

（待单位机下次轮次覆盖写入）
