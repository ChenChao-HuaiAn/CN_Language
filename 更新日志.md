## 功能完善总结

### 集成验证子任务：完善功能串联到生产链路

**时间**: 2026-08-13

**背景**: 三个完善子任务已完成（i128 完整支持 + 结构体按值传参/返回/整体赋值、Task 2.8 字符串 13 API、优化器增强 -O1/-O2/-O3），本次将完善后的功能串联到生产链路，扩展综合用例验证所有组件真正集成可用，并修复集成中发现的 6 个跨组件 BUG。

### 一、新增综合用例

- **`tests/e2e/14_integration2/员工档案.cn` + `.expected`**（245 行 CN 源码）：
  - 程序逻辑：员工档案管理系统——48B 大结构体（i128 年薪字段 + 字符串姓名 + 整32[3] 数组字段）按值传参/返回、结构体整体赋值（含修改隔离）、结构体指针数组按年薪冒泡排序（CopyStruct 交换 48B 元素）、i128 大数运算（乘/除/取余/负数）、字符串 13 API 与结构体字段/数组/指针组合（姓名大写/反转/字典序/编号转字符串等）、小结构体（8B 点对）寄存器传参、优化器 Pass（CSE/代数简化/复写传播/跨块DCE/全局传播/死代码）在真实程序 -O0/-O1/-O2/-O3 输出一致

### 二、集成发现并修复的 6 个 BUG

| # | 位置 | 问题 | 修复 |
|---|------|------|------|
| 1 | `x64_codegen.cpp` | i128 返回 + 结构体按值参数 ABI 错位：paramOffset 未涵盖 i128 返回 + i128 返回 prologue 未保存 rcx→r12（函数体内调用破坏 rcx → 0xC0000005） | paramOffset 涵盖 i128/u128 返回；prologue `mov r12, rcx`；epilogue 用 r12 恢复缓冲区 |
| 2 | `x64_instructions.cpp` | LoadPtr/StorePtr 漏 i128 双槽分支（结构体 i128 字段读取只 mov 8 字节，高64 残留垃圾） | LoadPtr/StorePtr 加 i128/u128 双槽分支（低64↔regSlot(id+1)、高64↔regSlot(id)） |
| 3 | `x64_instructions.cpp` | emitCast 无"普通整数→i128"分支（i32→i128 落默认 32 位 mov，高64 垃圾） | 加整数→i128 分支（符号扩展低64 + sar 63 高64） |
| 4 | `x64_codegen.cpp` | i128 参数 emitParamSetup 只存 8 字节（调用方传 16 字节双槽地址指针） | i128 参数（寄存器/栈）从指针 rep movsb 16 字节 |
| 5 | `ir.cpp` | 指针下标元素成员 `名单[j].年薪` objSrcType 推导只认数组变量，指针参数推导失败 → 字段访问降级为 0 → 排序比较恒假 | visitMemberExpr IndexExpr 分支补 `types::isPointer`（pointeeOf） |
| 6 | `ir.cpp` | 结构体下标赋值 `名单[j] = 名单[j+1]` 走 StorePtr 只存 8 字节（应 CopyStruct 48B） | 非标识符左值赋值加结构体检测 → CopyStruct（按 typeSizeOf） |

**其他修复**：`semantic.cpp` 补 `funcParamTypesOf` 实现（声明但未定义导致链接错误）；`ir.cpp` i128 实参截断逻辑——目标参数为 i128/u128 时不截断为 i64。

### 三、新增单元测试

- **`tests/unit/codegen/test_x64_i128_struct.cpp`**（+4 测试）：
  - `LoadPtrI128DoubleSlot`：i128 指针加载双槽
  - `StorePtrI128DoubleSlot`：i128 指针存储双槽
  - `I128ParamCopies16Bytes`：i128 参数 16 字节拷贝
  - `I128ReturnSavesR12`：i128 返回 prologue 保存 r12

### 四、验证结果

- 全量构建：零警告（MSVC /W4 /WX）
- 单元测试：**660/660**（原 656 + 新增 4）
- E2E：**14/14**（01~13 无回归 + 新增 14_integration2）
- 优化正确性：**56 组合（01~14 × -O0/-O1/-O2/-O3）输出完全一致，FAILURES=0**
- 全 CLI 命令：`cn token/ast/check/ir/run` 对扩展用例全部可用（AST 含结构体/i128 节点、IR 含 CopyStruct/FieldAddr/辅助函数调用）

**结论**：完善后的功能（i128、结构体按值、字符串 API、优化器增强）已在生产链路真正可用，综合用例验证全部组件组合运行正确。
