# 功能完善总结

> 81-a 容器元素 = 含拥有型串字段结构体（2026-09-12 第八十一轮，深度机 linux-x86_64）：
> plans/020 矩阵靶子「容器元素 = 含串字段结构体」（79-a 边界）销项——用户令「继续
> 后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案（性能第一、安全
> 第二）」。以 **drop glue 编译期展开全链**根治：元素释放面（平铺/链游遍历 + 单槽
> 唯一持有者）+ **元素移动挂点**（memcpy + 源槽清零＝Rust `ptr::copy` 零拷贝）+
> 调用返回实参临时清理 + 字面量实参物化；**同轮根治 4 枚既有缺陷**。

## 一、缺陷实证（探针 P1~P17，块出口 `内存::活动分配数()` 差值 + 分步打点）

| 形态 | 宿主 | v2 | 归属 |
|---|---|---|---|
| 向量/链表/栈/队列/集合<盒子> 元素字段串（析构/清空/删除/设置） | 每元素残留 1 | 每元素残留 1~2 | 元素字段串无释放面（两侧同缺） |
| 移位（`数据[移动] = 数据[移动+1]`） | 残留 3 | 残留 4 | 79-a 后走结构体整体赋值**深拷**：多余分配 + 超范围槽句柄丢弃 |
| 方法调用 + 结构体**字面量实参** | **段错误 rc=139**（asm `mov rdx, 0`） | v2 语法层不支持 | 方法调用路径缺字面量物化（普通调用路径 D3 已修） |
| 方法调用 + **调用返回实参** | 残留 1 | 残留 1 | 返回临时（retbuf）句柄悬空 |
| 链表<向量<字符串>> 3 元素删头×2 | 残留 1 + 越界槽二次析构 | — | 有析构类元素全量析构**平铺**（链式容器应链游） |
| `存入(表,值)` 内 `盒子 局` + `返回 X;` | 残留 1（函数体零 `__cn_str_free`） | — | 块终止**截断字段名单** → 函数级兜底失效 |
| `合并(造盒(1),造盒(2))` | 1（正确） | **0（数据损坏）** | v2 帧尾共享 retbuf × 嵌套结构体返回实参（登记下一轮） |

## 二、方案裁决（三案；性能第一、安全第二，Rust 经验）

| 案 | 内容 | 裁决 |
|---|---|---|
| **甲** | **编译期展开 drop glue 全链**：元素释放面（平铺/链游 + 单槽）+ 元素移动挂点（memcpy + 源槽清零）+ 返回实参临时清理 + 字面量实参物化 + 名单不截断 | ✅ **实施**（零运行时元数据/表/间接调用；移动=零拷贝） |
| 乙 | 运行时辅助 + 元素释放函数指针 | ✗ v2 无 `FuncAddr`/`CallIndirect`（需新增操作码 + 两后端面） |
| 丙 | 运行时辅助 + 字段偏移变参打包 | ✗ 嵌套结构体/结果联合体条件字段表达困难 + 变参上限 |

## 三、实施（宿主改 5 文件 + v2 改 2 文件）

- **宿主**：`ir_oop.cpp` 新增 `emitContainerElemWalk`（元素遍历循环单点：平铺/链游分派
  + 步数上限防环）/`emitSingleElemRelease`/`isOwnedStrFieldElemContainer` + 含串结构体
  元素注入分支（全量+单槽）+ **有析构类元素全量链游化**（修 P7）+ `移动元素` 挂点注入；
  `ir_call.cpp` `materializeStructInitArg`（字面量物化共享助手 + 临时登记释放）+
  `genArgValueWithCleanup`/`flushPendingArgCleanups`（返回实参临时清理）；`ir_oop_call.cpp`
  `genStringFrees` 提前返回修复 + `fieldTainted_` 死代码整链删除；`ir_stmt.cpp` 块终止
  **字段名单不截断**；`stdlib/容器.cn`+`映射集合.cn` 移位循环改用 `移动元素` 挂点。
- **v2**：删除 `标记入容器结构体污染` 整链（元素槽由 stdlib 写入深拷独立拥有、源照常释放）；
  元素释放经供给 obj 的 `~容器` 注入单点复用。

## 四、防线与反证实证

- **E2E 234_双编译对照容器元素结构体字段**（移植纪律 9 **第七个**对照用例，八形态折
  校验和 **255**，两侧退出码一致）；**E2E 235_容器元素实参形态**（宿主单侧八形态 **255**）。
- **反证实证**：宿主注入「元素判定恒假」→ 234 宿主 255→**128**；v2 注入「实参清理失效」
  → 234 v2 255→**223**（两侧注入已恢复，残留=0）。

## 五、验证链（本轮新口径）

清场重建**零警告** + 单测 **1306/1306** + E2E **262 用例 258 过 0 失败**/4 跳（+234/235；
其余 256 过=零回归）+ 锚定链固定点 **308719 行**（新口径，上轮 309133；-414＝v2 侧污染
标记删除等的确定性体现）两代逐字节一致 + 运行级 3/3 + 对拍 **34/34**（DIFF=0）+ 释放路径
两侧对照（脚本新增 81-a 段 + 修正 v2 侧函数名）。

## 六、登记（下一轮第一优先）

- **v2 帧尾共享 retbuf × 嵌套结构体返回实参**（单实参泄漏 / 双实参数据损坏）——
  三方案呈报（甲=实参物化到独立槽〈推荐，需先定 v2 槽分配纪律〉/乙=retbuf 全量独立/
  丙=所有返回调用后物化）；首版实现（生成期递增槽计数）与预扫槽分配冲突 → E2E 148/158
  回归，已回退（见 lessons ㊽）。
- B35 字段容器接收者（涉字段赋值语义 move vs 深拷 = 语言级裁决）；stdlib 链表 `追加`
  槽复用缺陷（删头后槽号冲突，元素丢失）；v2 结构体字面量作实参（语法缺口）；未初始化槽
  preFree 隐患（观察项）。
