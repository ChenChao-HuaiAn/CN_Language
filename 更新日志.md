# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。

## 家机 win-x64 节

## 功能完善总结
> **第一百八十轮 180-a：甲通道实装——方法体内容器字段直接赋值深拷（uF 根治）+ 第一百四十九轮 149-a 补记（IR diff 专项定位）**（2026-09-15，家机 win-x64）：
> ①**149-a 定位**（源码零改动，详见 plans/020 第六十八节）：直接 diff 148-a 遗留 `target/p149win/ir_{base,fix}.txt`
> + `cn ir` 补测调用点——**三铁证**：类引用参数实参=源槽地址（二级·138-a 发现二一致）／基线两层求值=正确匹配／
> 148-a 一层修复=读相邻槽（chain 拷贝=0 精确复现）；**uF 真崩因=拷贝构造内容器字段赋值指针共享（浅拷）→ 出口双重
> 释放 0xC0000374**；★146~148-a「求值多解一层」系误诊（三轮回退正确）。教训入 lessons 149 段（补记）·预防 166~168。
> ②**180-a 实装**（单点 `ir_oop_field.cpp` `handleClassFieldAssign`）：方法体内无前缀字段赋值（隐式 this 形态 a）
> 的**拥有型容器字段深拷**——对齐形态 b（memberClassFieldAssign，139-a 已有）：四族收窄（向量/栈/链表/队列）+
> 构造内初始化豁免（currentMethodIsCtor_ 跳过 preFree）+ 源分级（构造调用接管/左值 NewObject+拷贝构造〔实参=源槽
> 地址〕/防御直存）。修复后 `表 = 其他.表` 生成 新建对象+拷贝构造+存槽 三指令，零共享。
> ③**行为验证**：uF 0xC0000374 崩 → **`F ok 大小=1` rc=0**；uA~uE 全 rc=0；probe_chain 链表/队列拷贝=2、
> 残留全 0（146~148-a 退化面零复发）；五形态/栈族/计数探针全 0；116_Feature2 ×3 PASS（138-a 回归面零复发）。
> ④**门禁**：零警告构建 + 单测 **1317/1317** + 全量 E2E **306 用例 306 过 / 0 败 / 0 跳过**（含 78_v2/79_v2 自举链——**锚定链 fix_p≡fix_s 自洽**
> =新宿主 v2 编译产物零变化裁决）+ 组件对拍 **45/45** + 运行级 **3/3**。门禁七面全绿。
> ⑤**诚实边界**：uD（壳 d=a 结构体整体拷贝浅共享）未在本轮范围——emitStructWholeAssign 成员级级联深拷
> 登记 181-a 候选（uD 当前 rc=0 泄漏不崩，runtime 在册兜底，非阻塞）。
> ⑥文档六件套 + 看板收工。
## 深度机 linux-x86_64 节
## 深度机 linux-x86_64 节

## 功能完善总结

> 181-a D1 函数级续波——visitUnaryExpr 204→42 行 ≤100 达标 + 文件级达标（2026-09-15 第一百八十一轮，
> 深度机 linux-x86_64；基线 8c8862e）：
> **拆分面（switch 薄化·程序化切片重建）**：`semantic_expr_op.cpp` 的 visitUnaryExpr（204 行）按操作符 case
> 提取为 **5 个族子方法**（semantic.hpp +5 声明）：`checkUnaryOperatorOverload`（P2-14 重载·返回 bool）/
> `checkAddressOfUnary`（取地址 &）/`checkDerefUnary`（解引用 *·B11/B6 警告）/`checkPropagateUnary`
> （错误传播 ?·propagateType=当前返回类型）/`checkIncDecUnary`（自增自减校验）。主 switch 薄化 42 行。
> **★整族迁出至新文件 `semantic_expr_unary.cpp`**（269 行·CMakeLists 注册）——visitUnaryExpr 函数级
> 拆分使 semantic_expr_op.cpp 1007 行超门禁，整族迁出后 **752/269 双 ≤1000**。
> **★组装三次失败→程序化切片重建（lessons 预防 168/169）**：整族段尾锚误用文件尾 namespace 闭合卷入
> 后续函数 500 行、多次盲改累积文件损坏——git checkout 回退后改两步法（第一步文件级纯搬移+v2p md5
> 不变验证；第二步新文件内程序化切片拆分）成功。

> **验证链（宿主纯重构·v2 树零变化）**：零警告构建 + 单测 **1317/1317** + **v2p 产物 .s md5 不变**
> （`324ed472…`）+ 全量 E2E **306 用例 304 过 / 0 失败 / 2 跳** + **锚定链不变**（fix_p ≡ fix_s
> 394959 行，md5 `10f24dbb…`）。**D1 计数 65→64**（visitUnaryExpr 出列；新首列=`semantic_expr.cpp`
> visitIdentifierExpr 198）。github 镜像推送=本机无凭据，待配置或他机代推（170~181-a 十一轮待补推）。

## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节
## 单位机 ARM64 节

## 功能完善总结

### 160-a 文档轮：三项批准落盘（2026-09-14，单位机麒麟 ARM64，文档轮，基线 ebf99ab；源码零改动；实施由家机认领）

1. **A5 扩围 +4 词（userselect 批准）**：`公开`/`私有`（冒号标签位，与已批 `保护` 同机制——消除同类不对称）+ `静态`/`常量`（声明前缀位）随 A5 同轮上下文化（6 词 → 10 词）；目标态保留字 **50 → 46**。落盘：`plans/024` §10.5 批准批注 + `常量` 判据勘误（**前瞻 1 token**：后随标识符/`:` → 声明；后随运算符 → 表达式——非此前「前瞻 2 token 三元序列」表述）+ 声明位清单补全（顶层＋局部＋for-init；宿主局部常量=真实 isConst 语义 vs v2 防御分支按 整64，实施轮盘点对齐）+ `plans/001` §2.1 四处同步（总数/子类目/上下文清单/变更记录）。
2. **新排班 §3-A A8（userselect 批准方案A）**：**冒号后置类型标注移除**——宿主 3 处私藏规范外语法（参数 parser.cpp:361 / 变量·常量声明通道 :598-599 / 顶层常量 :830-833；实测 `函数 加倍(x: 整32)` 等全通过），cn-language-spec 全集与 plans/001 均无此语法且 spec 03 明确否决后置标注；双侧分叉（v2 语法层零支持）+ 自举隐患 + **本仓库存量零使用**（移除零迁移）。实施=移除 3 处 Colon 分支 + 负测锚定（冒号形态必须报错，宿主/v2 双侧）+ 全链门禁；**顺序约束：A8 先行、A5 随后**（A5 歧义判据不建在待移除形态上）。
3. **C16 追加（userselect 批准）**：spec 03 循环 EBNF 细化（init 位声明三形态：`变量`/`常量`/类型前缀——现 EBNF 仅「初始化表达式」未细化）随 C16 规范落盘轮一并补写。
4. 同步：`plans/021`（戳刷 160 + A5 扩围 + A8 新排班 + C16 追加 + §5 索引）/ `plans/019` §7.3 追加 160-a 行 / lessons 第一百六十轮段 / HANDOFF 单位机节（家机实施入口）。
