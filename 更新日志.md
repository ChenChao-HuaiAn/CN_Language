# 更新日志

> **分机分区（2026-09-12 起，AGENTS.md §6.2/§8.3）**：本文件按机器分节，每机**节内覆盖写入
> 本机本次说明**（节内格式以 `## 功能完善总结` 开头，只保留本机本次内容），他机节不动。


## 家机 win-x64 节

## 功能完善总结
> **248-a D21/D22/D23 三缺陷根治（2026-09-16，家机 win-x64；基线 3f86bdd·认领 7760275）**：
> ① **D21 类继承析构链断裂根治（P1 资源面）**：三后端 emitDeleteObject 沿 baseName 链**逐级调用各级自身析构**（「~本级类名」精确匹配消除 unordered_map 非确定命中——基类析构随机缺失，反证回退态序列改变实证）。
> ② **D22 三级链初始化列表误拒根治**：校验限定自身构造（mi.name==info.name，并入副本已在原类语境校验）；378 负例防放松。
> ③ **D23 构造默认参数根治（四处链路）**：defaultCount 字段+收集/checkCtorCall 放宽/visitProgram 预收集+handleClassCallExpr 补缺/初始化列表匹配放宽（语义+IR 同口径）。
> ④ **新用例 375~378**（E2E 394→398）；**D24 转 v2 欠账族独立轮**（v2 不支持类继承实证·wrapper=池级大工程）。
> ⑤ 全量 CI：零警告+单测 **1327/1327**+E2E **398=398/0/0**+--strict PASS（176/176 维持）。

## 深度机 linux-x86_64 节

## 功能完善总结

> **234-a A7 本体根治收口（2026-09-16，深度机 linux-x86_64；基线 476b7d2，认领 8bb5511 前 ecae5ed rebase 后）**：
> ①**第一层根治：共享 AST 写回注记跨实例污染**——实例化类方法体 AST 全实例共享（mi.ast=模板成员），
> 语义检查写回型注记（resolvedType/resolvedSignature/retOwnedString/node->size）在共享节点上被
> 「最后检查实例」残留，IR 层逐实例生成全读残留值（实证：v2 树 9 处 `复制(下标链)` resolvedType
> 全=IR指令 56B → 误走分派④ CopyStruct 56B+写回槽地址；单实例程序巧合正确=225-a「同模块用户
> 泛型类正确」对照真因）。根治=emitClassMethod 生成前按本实例 typeArgs 重放方法体检查
> （checkSingleMethodBody 原样提取复用）+诊断快照回滚；方案裁决=重放（方案U）胜深拷 AST（方案A），
> 详见 plans/025 §二。
> ②**第二层根治（连带发现）：整体赋值源分派漏复制调用形态**——`数据[索引]=复制(...)` 元素槽=
> 栈地址 8 字节（乙0号=-1347670232 实锤）；emitStructWholeAssign 补分派（**收窄纪律**：仅复制
> 调用进新分支——宽版补丁致 v2p 产物坏 285 失败，单变量二分定位后收窄；一般调用返回形态=D11 待查）。
> ③**stdlib 六处深拷切换重新应用**（206-c 立案收口）：向量/栈/链表/队列/映射键/映射值（映射范围
> 收为 [0,元素数量)）；E2E 285 切换态双侧全绿（宿主 15=v2 同值）。
> ④验证链：零警告全量重建 + 单测 1327/1327 + **全量 E2E 313 用例 311 过/0 失败/2 跳** +
> 锚定链 **410460 行 fix_p≡fix_s 逐字节不变**（v2 树不含拷贝初始化形态=自举链零扰动） +
> valgrind 切换态 6 errors（=225-a 回退态基线，**A7 差值 7 归零**）+ 反证双件（回退补丁 rc=136
> 复现 / 插桩双态 IR 实证）。
> ⑤新立案：**D10 泛型函数体零检查**（泛型自由函数体从未语义检查，`复制(源)` 落普通调用链接期爆）、
> **D11 一般调用返回整体赋值待查**——均登记 plans/021 §三-D；源码改动=宿主 5 文件+stdlib 2 文件，
> 详情=plans/020 第七十五节 + plans/025 §二。

## 单位机 ARM64 节

## 功能完善总结

### 252-a F2-33 拆分之一·stdlib 随机数模块（2026-09-16，单位机麒麟 ARM64，基线 1f5ba50·认领 0676d15·src 零改动）

1. **`stdlib/随机数.cn`**（新建）：类 随机数生成器——xoshiro256++ 算法+splitmix64 播种扩散（播种/下一个/
   下一个范围/下一个浮点）+模块级 播种扩散（熵源混合·Rust SeedableRng 同构）。纯 CN 实现、零编译器改动。
   规范对齐：[01b] 算术右移→旋转掩码清符号扩展+掩码基数整64（字面量 1=整32·移位按 32 位宽取模陷阱
   当轮踩坑当轮修）；T9 方案 D 常量溢出→黄金常数两补码负数十进制形式。
2. **E2E 379**（宿主逐行锚定）：固定种子确定性序列/范围/浮点/校验和/重播种复现——期望=python CN 语义
   独立参考实现预计算（非跑出什么写什么）。
3. **E2E 380**（双编译对照·退出码 31==31）：五项位权断言——**v2 位运算旋转掩码/两补码回绕/移位取模
   语义运行级对齐**；类内联形态（v2 导入类构造初始化判定缺口=D28 登记）。
4. **新立 D28/D29**（v2 对齐欠账族）：D28=导入类构造初始化判定失配；D29=v2 模块加载器无 stdlib 兜底+
   时间内置面缺失。
5. **lessons 252-a 条目**：打印行=print 不换行（打印=println·想当然二次复发）、整32 字面量移位取模陷阱、
   双编译 expected=锚行口径、stdlib 新模块排障三板斧（公开: 标签/stdlib 兜底/导入类判定）。

**门禁**：全量 E2E **400=398/0/2**+单测 1327/1327+CLI 矩阵 49/49+覆盖率门禁 PASS（79.5%/48.26% 基线
维持·src 零改动兑现）+双文档门禁。

**诚实边界**：D28/D29 未根治（v2 树改动+锚定链重锚=独立专项轮）；取模法轻偏差（文档注明）；非加密用途；
github 镜像本机无凭据未推。
