# 阶段P7b：v2 容器类型实例化与容器方法调用全链路（2026-09-01）

**目标**：HANDOFF「下一步②」——v2 编译器（CN 语言写的自举重建版）自身实现容器支持，
让 v2p 能编译含 向量<T>/映射<K,V> 用法的源码并真实运行（此前 v2 的 IR 层遇成员方法调用
直接 运行时错误(7)、解析类型 只认 标识符+*）。方案：v2 生成对宿主符号的直接调用
（向量$整64$追加#整64 -> nameMangle -> ?E59091...@@YJ@Z），实现链接宿主编译 v2 组件时
产出的 v2p.obj（stdlib 源码级并入、内含全部容器方法机器码）——rustc 链接预编译 std 的
同款模式；③ 对拍（v2 编译 8 组件 -> 链接）直接沿用。

## 一、v2 四层实现（+新组件 IR容器.cn，7 组件变 8）

1. **语法分析.cn**：泛型类型解析——解析类型文本 递归合成 $ 实例名（向量$整64 /
   映射$整64$整64 / 结果$空类型$整32，嵌套扁平 $ 连接，对齐宿主 generics.cpp）；
   泛型闭合位置 约束扫描（尖括号内白名单 token，防 a<b 比较表达式误判）+ 是泛型声明
   （声明前瞻）/是构造调用（名<实参>( 识别）；& 引用后缀登记（附加=指针层|引用<<8）；
   返回类型支持泛型；词法 字符串/字符字面量剥引号；静态 变量容器表 + 查容器类型 导出。
2. **IR容器.cn（新）**：容器基名白名单（向量/映射/链表/栈/队列/集合——结果/可选 不在
   列，由 是结果类型 判定）；对象大小表（NewObject 分配字节数，对标宿主布局）；
   方法签名表（类基名+方法名+实参数 -> 参数串（T/K/V 实例替换）+返回形态
   标量/结果空12B/结果值/空类型 + 值偏移）；构造/析构/方法符号合成；预扫容器声明
   （入口零初始化）+ 发射容器析构（RAII）；IR追加/建IR指令/类型码/常量折叠辅助移入。
3. **IR生成.cn**：新操作码 IR_分配对象=60（__cn_object_new(大小)）/ IR_结构体拷贝=61
   （rep movsb）/ IR_删除对象=62（空安全：test/je + ~类 + __cn_object_delete）/
   IR_调用参数化=63（this 参数位0、结果返回走 retbuf 隐藏返回指针（类型=retbuf 序号，
   共享单 retbuf 复用）、rcx..r9 四参数位）；容器变量预扫分配槽+入口零初始化；构造
   调用（NewObject+无参构造+Store，H7 同款）；方法调用（表达式栈方法标记编码
   -(10^6+方法名ID)）；结果变量 2 槽内联 + .正常（偏移0 布尔宽度）/ .值（偏移 4/8）
   访问 + IR_结构体拷贝接收；每个 IR_返回 前发 RAII 析构；字符串字面量 IR_常量字符串；
   运行时错误(7) 兜底收敛为诊断+错误码6。
4. **代码生成.cn**：符号修饰 重写为宿主同款 nameMangle（# 参数串切分 + 类型编码表
   整32=H/整64=J/字符串=PAX/空类型=X/布尔=_N/类=_T+hex/&=A 前缀 + @@Y[@Z]；主->cn_main、
   __cn_ 原样）；4 条新指令汇编展开；retbuf 帧区（槽基数+序号*2 起始——最低地址）；
   布尔宽度指针加载（movzx byte）；EXTERN 自动收集（call 符号 - 本地 PROC 差集）；
   字符串常量数据行（.code 段内 strData_<ID> db 字节，主函数末尾扫描 IR 发射去重）。

## 二、实测根治 5 缺陷（全部记 lessons 高权重）

1. **MASM 247 符号限制**：生成表达式 9 参 mangled 超限 -> A2043 -> PROC 注册失败 ->
   bb 标签泄漏全局连锁 A2005。解法：类型信息改语法层预标注，回退 8 参。
2. **retbuf 起始槽错**：12 字节从 [rbp-96] 向上写越界覆盖表对象指针 -> SIGSEGV。
   解法：retbuf 槽号=槽基数+序号*2（最低地址起始）。
3. **结果变量 2 槽基址错**（同族）：基址须取高槽号（低地址），12/16 字节向上写不越界。
4. **向量<字符串> 跨向量搬移悬垂**：输出行指针搬进局部重排向量后源 RAII 释放 ->
   asm 乱码。解法：零搬移（主函数末尾扫描 IR 直接追加数据行）。
5. **AST 节点.子终点 复用伪造子区间**：语义层当子表达式区间遍历 -> 未声明误报。
   解法：查容器类型 导出函数，子终点 恒 0。
   （另有：容器白名单误含 结果/可选 -> 结果变量被 NewObject；objdel 标签多容器重复
   ——目标字段用唯一序号。）

## 三、E2E 与验证

- 新增 **123_v2_容器**（六步闭环，退出码 21333）：向量<整64> 构造/追加/元素/大小 +
  映射<整64,整64> 设置/获取/.正常/.值 条件分支/清空/大小 + 结果<空类型|整64,整32>
  接收 + 向量<字符串> 追加/大小 + RAII 双容器析构。run_e2e.py 执行v2闭环 扩展
  链接v2pobj 模式（/FORCE:MULTIPLE + v2asm.obj 在前保证 cn_main 绑 v2 产物 +
  map 双向自检：向量方法符号必须来自 v2p.obj——79 防虚假验收同款）。
- E2E 119/120 回归 PASS（v2 语法层改动零破坏）；全量门禁 ci.ps1 结果见提交信息。

## 四、下一步（②b→③）

结构体元素容器（向量<Token>：v2 布局对齐宿主 typeSizeOf、结构体按值传参 ABI、元素
视图语义）→ 嵌套容器（向量<映射>）/容器作结构体字段/静态容器/& 引用参数语义 →
v2 剩余语法（下标/三元/字符串拼接/内置函数符号映射/顶层静态变量）→ ③ v2 编译 8
组件自举对拍（对标 79）；顺带：宿主 元素(0).字段 链式取值缺陷（plans/014 三表）。
