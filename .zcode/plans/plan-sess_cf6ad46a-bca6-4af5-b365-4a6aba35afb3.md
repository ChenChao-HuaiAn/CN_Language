# ②c 四项一轮收官（E2E 127 载体）——嵌套容器 / 容器字段拥有式 / 顶层静态 / 引用参数

## 目标与依据

按 HANDOFF/plans/014 下一步：②c 四项 → 为 ③ 自举对拍铺路。已核实的 ③ 硬前置：
- **A 嵌套容器**：语义分析.cn 核心结构就是 `向量<映射<整64, 符号>>`（嵌套+结构体值元素，按值传参遍布全部 表* 函数）
- **C 顶层静态**：v2 组件遍布 `静态 映射<整64,整64> 结构体大小表`、`静态 整64 当前retbuf字节数=0` 等顶层静态（容器+标量）
- **D 引用参数**：主.cn:90 `映射<整64, 整64>& 已加载`（编译文件 签名）
- **B 容器字段**：③ 非硬前置，但为安全目标② B4 真载体（拥有式析构/深拷贝）

**B4 模型（用户委派按双目标裁定）：指针槽模型**——结构体容器字段=8字节槽存对象指针；声明注入 NewObject+构造，拷贝调拷贝构造，作用域出口注入析构；供给源以「指针字段类」承载实现（复用宿主 H8-⑤ 深拷贝+级联析构成熟机制），零宿主改动。

## 步骤

### 第0步 事实探针（target/dbg，不入库，完成即删）
- 0a 供给类契约探针：宿主编译 `类 词条{向量<整64>* 表;...构造/~词条/词条(词条&)}` + `向量<词条>` 追加深拷贝/级联析构端到端，产出 mangle 符号形态与布局契约
- 0b CN 级对象销毁原语核实（指针字段析构体写法：删除/析构调用形态）
- 0c 静态槽 asm 形态对拍：GAS `.globl _cn_gstatic_名/.quad 0`、MASM `?gstatic_名/DQ`（与宿主产物核对）
- 0d ③ 缺口清单盘点表（v2 组件 & 参数/静态/嵌套容器/容器字段用法）记入 plans/014 作 ③ 排期依据
- 若探针推翻指针槽前提 → 暂停上报重裁（缺陷裁决纪律）

### 第1步 E2E 127 立项（0f39be3 模式：先立项不注册，门禁保持绿）
`tests/e2e/127_v2_嵌套容器与容器字段与静态与引用/`：主.cn + 供给.cn + 主.expected（v2p 编译诊断行模式）。
断言四主题块（201 起编号，全过返回 0）：
- A：向量<映射<整64,整64>> 构造/追加(映射)/大小/元素(i).设置(k,v)/.获取.正常/.值/RAII 级联
- B：结构体{标量;向量<整64> 表;字符串} 布局(表=8字节槽)/字段.追加().元素()/深拷贝独立性/块作用域析构
- C：顶层 静态 向量<整64> 跨函数追加/读取/大小 + 静态 整64 计数
- D：交换(整64&,整64&) 写回/结构体& 字段写回/映射& 引用（主.cn:90 同款）
供给.cn：词条类形态（指针字段+构造/析构/拷贝构造）+ 向量$映射$整64$整64 / 向量$词条 全方法面实例化 + 主 入口函数（B7 模式）。

### 第2步 A 嵌套容器（v2 组件）
- IR容器.cn `类型大小与对齐`：$ 合成名命中 `容器对象大小`（映射56…）——消灭 8/8 防御默认，元素步长/字段布局数据驱动
- 返回形态分类：元素(i) 元素为容器 → 容器返回_结构体（B6 retbuf 按字节数定帧复用）
- 链式 `元素(i).设置(k,v)`：调用对象=CallExpr 的方法调用类型推导（查方法签名返回类型→this=retbuf 基址），对标宿主 E2E 118
- 嵌套追加(映射) 实参：容器变量槽=对象指针直传（核实，不符则并入 B8 回扫取址）
- 级联析构由供给 obj（宿主 injectContainerElemDestroy）承担，v2 只调外层析构符号

### 第3步 B 容器作结构体字段（指针槽模型）
- 布局：登记字段类型 容器分支 → 宽度码=8/槽1（与供给类指针字段对齐）
- 生命周期注入：
  - 声明：各容器字段 NewObject+无参构造+存槽（叠加 B1 多槽零初始化）
  - 拷贝点改调拷贝构造：赋值/按值传参 prologue/返回 —— 拥有式识别=查字段类型表含容器
  - 析构：块出口+返回前按字段逆序 ~容器(字段指针)+删除（IR_删除对象 通路泛化）
- 访问：`a.表.追加()` 字段读=LoadPtr 得指针→方法 this=指针（成员访问三形态扩展）
- 双源契约：主.cn 写 `向量<整64> 表`（v2 判拥有式），供给.cn 写 `向量<整64>* 表`+三件套——0a 产出契约为准

### 第4步 C 顶层静态容器
- 语法：节点_静态声明（顶层 `静态 类型 名 [= 表达式]`，spec 03）+ 解析程序分派；局部 静态 明确诊断（不静默吞）
- 语义：静态登记+类型归一 $；多文件跨模块可见（对齐宿主 semantic.cpp:1664 模式）
- IR：静态槽表+主 入口注入（有初始化→构造调用；无→NewObject+无参构造+StorePtr 入槽，统一指针槽模型 P3-8）；标识符读取：局部未命中→槽符号地址+LoadPtr；方法 this=读出指针
- 代码生成：共用层决策+双后端 .data 发射（GAS `_cn_gstatic_名`/MASM `?gstatic_名`，按 0c 形态）；静态标量同链路
- 多文件：符号唯一化（模块名$名，对齐宿主 staticModules_）

### 第5步 D & 引用参数
- IR 参数：附加含引用(×256)→byRef：槽存地址，读=LoadPtr/写=StorePtr（对齐宿主 A-1）
- 结构体&：登记 变量结构体表（修「附加==0 才登记」排除）+ 跳过 prologue 多槽拷贝 + 成员访问基址=槽内地址；容器&=指针直传语义等价
- 调用方：引用形参→实参回扫取址（扩展 B8 表驱动）；非左值→语义诊断
- 符号：v2 函数按名 mangle 不受影响；stdlib & 编码已预留

### 第6步 注册+门禁+文档收官
- run_e2e.py 注册 127：`("主.cn", 0, True, ["供给.cn"])`
- 单测全量 + E2E linux-arm64 全量（119/120/123/125/126/**127** 全绿，全量零回归=123过/4跳/78已知遗留）
- MASM 产出 asm 级核验（嵌套符号 vs 247 限制记录风险）；win-x64 运行时门禁留家机会话（双机门禁拆分既定，不写假绿）
- 文档：plans/014 ②c 条目+状态表、更新日志.md 覆盖、lessons.md 新坑、HANDOFF.md；中文提交+推送 origin/develop；target/dbg 探针清理

## 风险与预案
- 嵌套容器 mangle 符号超 MASM 247（win 家机风险；ARM64 GAS 无限制）——记录不阻塞本机门禁
- 供给/主源布局契约漂移——0a 先行+布局断言入 .expected
- 宿主指针字段成员访问若有坑（0b 核实）→ 缺陷零容忍上报，不绕过
- 拷贝点遗漏（返回值场景）——断言覆盖赋值/传参/块作用域三点
- 文件行数纪律：新增逻辑优先入 IR容器.cn(472行)/新小文件，IR生成.cn(已1387行)只做接线；若继续膨胀则拆子模块（如 IR生成静态.cn），拆分不绑架核心目标

## 边界（不做什么）
- 不动宿主编译器（指针槽零宿主改动；探针发现宿主缺陷→暂停上报）
- 不做 win-x64 运行时门禁、不做 ③ 自举对拍（下一轮，本轮备齐硬前置）
- 不改已验收测试文件（127 是新立项）