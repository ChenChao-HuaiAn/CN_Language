# plans/025 家机节切片：708-c/710/709 落账段（2026-09·718 棘轮滚转·纯搬运零改写）

### 1.20 【708-c 阶段回填】语义层 & 化实验：语义错全消（重大）但暴露引用透传断裂前置缺陷→回滚（分支 `任务/家机-708-形参引用化大迁移`·2026-09-24）

**语义层 & 化 88 处实装结果（双面）**：
- ✅ **决定性进展**：cn_self 语义检查器**复活**——79_v2-6 失败形态从「语义错误 67157（退出码 1）」变为「段错误（代码生成期）」——**副本语义下形参装载/声明收集写穿恢复·语义错全消**。
- ❌ **全量红面 4→9 劣化**：218（rc 127→1）/219/221/234/67_generic_param 新红——**引用透传断裂缺陷**实锤：& 形参作为实参再传给 & 形参时，调用点回扫取地址对「引用槽」再取地址→被调方多一次解引用=**每层传递少解引用一层**→多级辅助函数链（检查函数→登记函数参数们→表注册→映射.设置）层级错位。

**两个前置缺陷锁定（056 全面铺开的阻塞）**：①**生成层引用透传断裂**（& 形参→& 形参 多级传递的解引用层级错位·ir_call 回扫/生成方法调用 的 & 实参处理）；②**B8 表容器扩面被调方配套缺失**（容器形参取地址后被调方句柄/壳解引用层级配套·218 实验实证）。

**处置**：语义层 & 化 88 处+B8 扩面**分支保全**（6e29520e·语义错全消成果不丢）+工作树回滚（develop 红面恢复 4 例基线·218 绿）。

**001 清偿路线终版（全部 fresh context 专项链）**：
- **708-c**：生成层引用透传断裂修复（ir_call.cpp 回扫/生成方法调用 的 & 实参层级·最小复现=两级 & 链探针）→**前置**
- **708-d**：语义层 & 化重上（6e29520e 分支直用）→语义错 0 复验
- **708-e**：生成器层容器形参 & 化（IR/生成*.cn 第二批·同法脚本）→79_v2 -6（代码生成段错误）清偿→-7 固定点
- **709**：055 宿主改造重上+113 升级源码集成→113/127_v2 双侧绿
- **054/524** 并行→零红→借链 51 例终章→随父 001 销项

**5. 边缘情况**：①语义层 & 化的 池 形参未动（只读无害）；②引用透传断裂的最小复现探针待 708-c 首步（两级 & 链：辅助函数 A(&) 内调 B(&) 传同一容器）。

### 1.21 【710 分支生命周期治本】远程任务分支清理+集成即删机制（用户裁决「按甲办」·运维+机制轮·分支 `任务/家机-710-分支生命周期治本`·基线 26e76b8b·主树·2026-09-24）

**1. 目标与验收判据**：①远程死分支清理落地——gitcode 任务分支 367→20（已执行·347 条删=244 已合并/零差量+56 台账核实+33 develop 含该轮提交+14 取代性集成/零代码定性；逐条三重核实=git cherry 差量→develop 提交 grep→源码特征 grep）；②治本机制入库——integrate.py 集成成功后自动删该远程任务分支（dry-run 跳过·失败仅警告）+scripts/branch_cleanup.py 兜底工具（默认 dry-run 只删「已并入 develop 且不在看板」的安全面）+AGENTS §8.1 补「集成即删分支」规则。验收=710 自身集成成功且其远程分支被 integrate.py 自动删除（机制自举实测）+快速门禁全绿+双推齐平。

**2. 写集**：`scripts/integrate.py`（集成成功钩子）｜`scripts/branch_cleanup.py`（新增·兜底工具）｜`AGENTS.md`（§8.1 分支生命周期补条）｜`plans/025` 本机节（本节+收工回填）｜`plans/021`（挂账 9 分支登记待处置行+§五 710 行+戳）｜`交接.md`/`更新日志.md` 本机节｜`三机任务看板.md`（开工行已直推+收工清行）。

**3. 实施步骤**：①看板先行直推（已做 26e76b8b）→②开分支落盘本计划→③integrate.py `[集成成功]` 后插入远程分支自动删除（容错·dry-run 跳过）→④branch_cleanup.py（fetch→排除看板在飞行→git merged 判定→分批 push --delete·默认 dry-run）→⑤AGENTS §8.1 补条→⑥文档同步（021 挂账登记/更新日志/交接）→⑦快速门禁→integrate.py 集成（自举实测自动删分支）→双推。

**4. 验证链**：`python3 -m py_compile scripts/integrate.py scripts/branch_cleanup.py`＋`python scripts/branch_cleanup.py`（dry-run 输出与保留清单核对）＋check_handoff/check_progress_sync/check_language_philosophy＋integrate.py 快速门禁（写集无 src/tests）＋710 集成后 `git ls-remote --heads gitcode` 实测分支已自动删除（机制自举证据）。

**5. 边缘情况与诚实边界**：①**挂账 9 分支**（家机 497/500/502/538/542/555/556+单位机 557/558——挂等回签通过/功能未集成的真成果）**本轮不删不集成**，登记 021 待用户裁决处置（补集成=独立任务轮）；②687/699/578/595/627 等本地状态不明树/脏树保留未动（wt526tb 的未提交单测改动与挂账 500 相关·已保护）；③`Documents/CN/.git` 历史误操作残留壳已随磁盘清扫删除（主仓库 `CN_Language_C/.git` 完好·git 全命令正常）；④github 镜像 3 条历史误推任务分支已删；⑤branch_cleanup.py 只删已合并安全面——差量分支的甄别（本轮人工做的三重核实）不属于工具自动化范围，防误删。

**6. 与他机交叠分析**：写集=scripts/AGENTS/plans/共享文档本机节——深度机在飞 550-t94/657（src 写集）零交叠；708 系（家机另一会话·wt698·v2 树写集）零交叠（其看板行宣告 709=055+113 留号·我取 710 让位）；单位机无在飞行。develop 期间被 708 窄通道推进 2 次（441f8f06/51e16de2）——已 fetch 对齐，集成时 rebase 兜底。

### 1.22 【709 落账补录】两级 & 链探针证伪「透传断裂」+218 非确定红发现（分支 `任务/家机-709-引用透传修复`·wt698 树·2026-09-24·rebase 冲突致本段曾在窄通道丢失·本条补录）

**两级 & 链最小探针（t7·target/p704/t7dir/）**：`A(向量& 表) 内调 B(向量& 表)→表.追加(7)`→v2p 编译+运行=**大小: 1 rc=0 完全正常**——「引用透传断裂」假说**证伪**（回扫取地址的 槽是引用 分支/取地址分支在两级链上行为正确）。

**新发现=218 非确定红**：语义层 & 化 88 处下，218 单跑**双跑全绿**但全量门禁（并行）红（rc=1）——**v2 产物运行非确定**=& 形参装载链存在**未初始化/条件路径敏感**读取（某条件分支下 引用槽未装载即读）。定位抓手=①218 多跑 diff（红/绿分叉点·步号插桩）；②检查 & 形参的所有条件装载路径（登记函数参数们 的 附加 256/257 分支+槽是引用 分支的组合覆盖）。

**708-c 修订**：前置工程从「引用透传断裂修复」改为「& 形参装载链的条件路径完备性审计+非确定消除」——t4/t5/t7/t3 探针矩阵齐备（target/p698/707t4/+p704/t7dir/）。

