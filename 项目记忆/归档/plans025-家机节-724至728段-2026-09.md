# plans/025 家机节切片：724/726/728 段（2026-09·734 棘轮滚转·纯搬运零改写）

### 1.34 【724 阶段回填】cn_realloc 异常尺寸防御=cn_self 从崩溃转为干净诊断（行为面·接 723）（分支 `任务/家机-724-拷贝构造四循环对拍修复`·wt698 树·基线 develop 0de63c90·2026-09-25）

**1. 行为面改动（src/runtime/io_api.cpp）**：cn_realloc 增**异常大尺寸可观测拒绝**（size>2GB→stderr 打印 `[cnrt] cn_realloc 拒绝异常尺寸 N`+返回 NULL——Rust alloc 合同同款：Layout 超限返回分配错误不 UB）。
**2. 实测效果（重大）**：cn_self 编译 t4 **从崩溃（C0000409/C0000005）转为「语义错误 4 个·干净退出 1」**——诊断输出=`未声明标识符: 甲/乙`+`未声明函数: 加`——**证实原崩溃路径含 cn_realloc(垃圾大尺寸)**：拒绝后 设置 走 错误(内存)→调用方 干净失败路径（不再写 NULL 堆）。**形参装载失效面（t4 4 错=707 定性本体）首次可观测**——t4→0 判据的修复目标显形。
**3. 全量 E2E：528=524/4**（4 红=113/127_v2/524/79_v2 已知基线恒定零新增）；79_v2 形态=fix_s 编译失败退出码 1（非崩溃——ExitProcess+realloc 防御生效）。
**4. 下一棒（725）**：**t4 4 错根因修复**（标量形参 甲/乙/加 未声明=函数声明收集+形参装载失效面·707 定性本体）→t4→0→fix_s 67157→0→-7 固定点→056 传址化→709=055+113→零红→终章→001 销项。
**5. 交叠**：写集=src/runtime/io_api.cpp+文档件——src 写集全量门禁（收工跑·已知红点名放行）。

### 1.35 【725 阶段回填】设置 字段偏移对拍初探+形态差异发现（纯侦查·接 724）（分支 `任务/家机-725-056传址化实施Ⅰ`·wt698 树·基线 develop 4134ed24·2026-09-25）

**1. 产出**：offcmp_725.py（设置 PROC 字段偏移序列计数对拍）——初版数据：v2 版偏移族=8/16/24/32/40/48（寄存器加法形态·与映射 7 字段序吻合）；宿主版=位移寻址形态（无 add rax,N）——**两版代码形态根本不同，计数对拍无效，需语义级对拍**（按源码语句对位指令序列）。工具留存。
**2. 056 实施现状**：传址化主手术未开始——本轮为前置侦查。t4 4 错（形参装载失效）与 设置 垃圾尺寸 realloc 的共同上游=**映射字段读写链的发射缺陷**·需语义级对拍定位。
**3. 下一棒（726）**：语义级对拍（源码语句→指令序列人工对位·设置 的重哈希/扩容段优先）或直接实施 056 传址化主手术（v2p 调用点聚合拷贝移除+传址）——二选一由下轮开工时定。
**4. 交叠**：纯侦查零写集（脚本留存）。

### 1.32b 【726 侦查定案】t4 4 错根因=表注册 写「元素 返回的副本」注册丢失（接 725）（分支 `任务/家机-726-表注册插桩二分`·wt698 树·基线 develop 6a3b8ff2·2026-09-25）

**1. 根因定案（语义层·源码证据链闭合）**：表注册（语义分析.cn:282·& 化函数）的 `映射<整64, 符号> 顶层 = 符号表.元素(大小-1)`——**容器.cn 元素（:189）`返回 数据[位置]`=T 按值副本**——顶层=完整副本（指针字段浅拷共享内部）——`顶层.设置(名ID, s)`：设置 的 realloc 释放共享旧块→**原映射数组悬垂**（636-a 定性吻合）+**副本 新指针随函数退出丢弃=注册丢失**。**宿主=元素 句柄语义写穿碰巧正确（052 甲前模型）·cn_self=副本语义注册丢**——两代语义差正中心·t4 4 错（甲/乙/加 未声明=注册全丢）完整解释。
**2. 修复方案（727 实施）**：**容器.cn 向量<T> 增 引用返回元素访问**（`函数 元素引用(整64 位置) -> T&`·语言 isRefReturn 已支持 55-c/P3-18）+**表注册 改用**（写穿原映射）——候选次选=引用局部变量绑元素。语言级零新增（& 返回/引用局部既有）·stdlib 扩展（机制级）。
**3. 验收**：表注册 写穿生效→cn_self 编译 t4 诊断 4 错→0（甲/乙/加 全部登记可见）→fix_s 67157→0→-7 固定点。
**4. 下一棒（727）**：实施 元素引用+表注册 改造→重建链→t4→0 验证→fix_s 67157→0→全量门禁→集成。
**5. 交叠**：写集=stdlib/容器.cn+语义分析.cn（表注册）——他机零交叠。

### 1.36 【724 补充回填】交叉回归定位=「元素引用 方法」与「表注册 写回」不可分离共存（接 §1.34）（分支 `任务/家机-724-拷贝构造四循环对拍修复`·wt698 树）

**实施过程回滚记录（诚实披露）**：724 曾并行尝试①容器.cn 增 元素引用（-> T& 引用返回方法）+②语义分析.cn 表注册 写回——**全量 E2E 大面积回归（528=235/293·v2p 运行 C0000005）**→分步回退定位：撤 元素引用 仅留写回→**v2p 编译 v2 全树即崩（C0000005）**；恢复基线（两者均撤）→宿主编译 v2 全树 rc=0 ✓+t4 rc=0 ✓=**基线稳定恢复**。**定性**：①「元素引用（-> T& 泛型方法引用返回）」与「表注册 写回」**交叉组合触发 v2p 生成层克隆体发射缺陷**（697-b 域·单独形态待 723 对拍深挖）；②基线（无两者）稳定 524/4 ✓——**724 集成版（44e67967·仅 cn_realloc 防御）为当前安全基线**；③元素引用+写回组合在 724 轮内曾观测 524/4（不稳定·布局敏感）——**引用返回克隆体发射缺陷的确证样本**（723 对拍的材料）。
**下一步不变**：725=拷贝构造四深拷循环对拍（含 元素引用 引用返回克隆体的发射缺陷样本）→定点修复。**修复双件（元素引用+写回）待发射缺陷根治后回归实施。**

### 1.37 【728 侦查定位】写回单件触发链=向量.设置→元素写深拷→映射拷贝构造·桶字段缺陷（接 §1.36）（分支 `任务/家机-728-写回修补精验轮`·wt698 树·基线 develop 33897c60·2026-09-25）

**1. 实验矩阵（分步精验）**：①**写回单件（无元素引用）**：宿主全量重建 v2p **rc=0** ✓（727 的崩=元素引用在场）；②**cn_self 跑 t4 rc=0 零诊断**（写回生效·t4→0 ✓✓ 维持）；③**v2p 跑 t4 崩**（C0000005·VEH 现场：fault=**0x1FD0**=1018×8·调用链 RVA 已录）；④v2p 编译 v2 全树崩（同源）。
**2. 崩溃机理（fault 地址推理）**：fault=0x1FD0=1018×8——**桶数组[1018] 读**（1018<1024=合法桶索引）→**this 映射「桶数组=NULL 但桶数量=1024」**——映射 构造器顺序（桶数量=1024 后立即分配桶数组）保证正常对象不可能——**缺陷对象来源=拷贝构造克隆体**（写回链：符号表.设置(大小-1, 顶层)→向量.设置→**元素写深拷（702-a）→映射拷贝构造**→克隆体字段处理缺陷）。**与 722 崩溃（设置+0x67 值数组覆盖写·拷贝构造产物）同源收敛**：**映射拷贝构造克隆体的字段深拷段（桶/键/值/链）有缺陷**——716 尺寸验真（40/56）不矛盾（尺寸对·**语句级字段处理**错）。
**3. 下一棒（729）**：**映射拷贝构造克隆体语句级对拍**（四段深拷循环·重点桶段：`桶数组=分配(桶数量×8)`+`桶数组[桶索引]=其他.桶数组[桶索引]` 的发射）——v2p.asm（宿主编译·在案）与 cn_self 侧 fix_p_new.asm 双版对照→定点修复→**v2p 跑 t4 零错+全量 524/4**→双件（写回+元素引用）回归→t4→0→fix_s 67157→0 判据。
**4. 交叠**：写集=语义分析.cn（写回单件·暂存待缺陷修复后同批集成）——他机零交叠。

