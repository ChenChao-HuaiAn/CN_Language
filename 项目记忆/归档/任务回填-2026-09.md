# 025 任务回填归档切片·2026-09（纯搬运零改写·封存不改·来源映射=plans/025 各机节滚转记录）

> 本切片承接 plans/025 各机节滚动替换下来的历史轮回填（棘轮滚转·AGENTS §6.7）。

### 1.0 收工回填（**354-a 开发轮·🏃 T33 销项确认轮：385/386 PASS 实测=已实质销项·零代码改动**·基线 79f1066·2026-09-18·分支 任务/家机-354-T33阶段一）

**1. 侦查结论（反转）**：认领后实测 **385/386 双编译对照 PASS**（整16 f 打印 -25536 正确）——读侧窄宽 movsx 设施**已在库**（v2asm 实证 `mov ax, word ptr [rbp-232]; movsx rax, ax`=293-a 后续轮实现）——T33 立案面（整16 f 打印错值）**已实质销项**，plans/021 T33 行漂移未勾（290-a 立案至 354-a 间他机实现未回写）。**354-a 定性=销项确认轮·零代码改动**。
**2. 产出**：T33 行勾销（勾据=runner 385/386 PASS 实测+asm movsx 指纹）+T33 侦查注记（前轮落盘）归档。
**3. 交叠**：零交叠。**4. 下一轮候选**：①win 列 M3 采样；②B1/T43/T46/T47/T48 待用户裁决；③波次7 余项=D36 实施大轮（352-a 方案就绪）/C19/D10~D11；④波次8 批C/D。### 1.0 实施计划（**354-a 开发轮·🏃 T33 阶段一：v2 读侧窄宽 win 后端**·基线 79f1066·2026-09-18·分支 任务/家机-354-T33阶段一）

**1. 目标与验收**：T33 残余（读侧窄宽）渐进式第一阶段——**win 后端**编码LoadStoreWin 的 IR_加载 按指令.类型分派窄宽符号扩展装载（movsx rax, word/byte ptr 槽·T_整16/整8/字符；T_整32 movsxd dword ptr；正系 movzx；缺省=T_整64 维持现状）——**验收**=①385/386 的 整16 f 打印 -25536 正确（现 40000）+两用例全段输出一致；②零回归=v2 树编译+78/79 自举+全量 E2E+单测（**加载分派改窄=存量窄类型槽读面全面变化——E2E 是主安全网·预判存量槽写入已由存储侧保证窄值有效**）；③X64L/ARM64 留阶段二三（诚实边界=单后端先行·跨机轮对齐）。
**2. 写集**：CN语言编译器v2/代码生成/代码生成.cn（编码LoadStoreWin 加载分派）｜文档六件套+看板。
**3. 实施步骤**：①侦查 385 数据路径（存储侧写入形态→加载侧读出形态→打印消费）+IR 指令.类型 在加载点的可用性；②实现加载窄宽分派（movsx/movzx 按类型）；③三探针（385/386/存量窄类型用例）+全链验证。
**4. 验证链**：v2 树零错误编译｜385/386 输出正确｜d38_scan 16 拒保持｜crosscheck｜78/79 自举｜全量 E2E+单测。
**5. 边缘与诚实边界**：①存储侧槽写入形态依赖侦查结论（若存储已 64 位符号扩展则加载窄读冗余——以实测为准调整方案）；②布尔类型加载（T_布尔→byte）随族处理；③X64L/ARM64=阶段二三；④指针加载（B5）不涉（类型=指针·维持 64 位）。
**6. 与他机交叠**：353-a 深度机（T60 负号·IR 生成面）/329-D12 单位机——零交叠预期。

