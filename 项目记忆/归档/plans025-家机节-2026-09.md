

<!-- 616-a 滚转：615-a/614-a 已收官轮次家机节回填（指针留 025） -->

### 1.0 实施计划（**615-a 开发轮·🏃 001-001 续=返回 sret 机制级修复**·看板先行 3eb12dac·基线 develop 657a6281·2026-09-23·分支 `任务/家机-615-返回sret修复`·wt615 隔离·用户令多轮不间断至 001 销项）

**1. 目标与验收判据**：承接 614-a（asm 铁证：被调「返回语句分支判定」与调用方「sret 判定」不一致——被调返回=标量值 mov rax/[基槽]、调用方=sret lea rcx 物化+rdx=&实参→形参 rep movsb 拷调用方物化槽垃圾→b=垃圾指针）。**修复靶=生成返回语句 的 返回结构体值 判定/发射单点**（生成语句.cn）——定位「返回 类对象/结构体 标识符」时 返回结构体值=假 的确切条件并修（强制走 retbuf 分支：结构体拷贝到 retbuf+rax=retbuf 回写）。**验收**=①N2→rc=0（b1~b4=1/101/3/4）②O 保持 0③L/B 推进④113 单跑对照⑤撤守卫态全量无新增红⑥反证双态⑦生产态门禁 4/4。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成语句.cn`（修复单点·或连带 初始化签名与返回字段）｜六件套。TEMP 撤守卫不提交；宿主 src 零改动。
**3. 实施步骤**：①读 生成返回语句 完整分支条件（301 行 返回结构体值 判定输入）+对照 N2 产物的层拷S（判定假实锤）→锁定判定 miss 的确切条件②修复（判定补全或返回分支强制）③重建 v2p→N2/O→L/K→113→全量→反证→生产态→integrate。
**4. 验证链**：探针 N2/O/L/K→113→撤守卫态全量→反证双态→生产态 ci.ps1→asm 宽度。
**5. 边缘情况与诚实边界（预判）**：①修复只动「返回语句分支判定」单点·不碰 prologue/调用方（两侧其余已对齐）②若判定条件本身正确而 miss 在上游（函数声明.附加 语义）→顺藤修正并记录③113 若余元素读取面=第二波登记。
**6. 与他机交叠分析**：远端 develop=3eb12dac（本机 615 看板修正·最新）；同机 609/611 零写集交叠；深度机 550 呈报态零交叠。冲突判定：独立。

### 1.0 阶段回填（**615-a=N2 打印版铁证复现+读码深化：修复靶维持「生成返回语句 判定/发射」·实证细节补全**·wt615·2026-09-23）

**1. N2 打印版复现（615 树）**：重建探针跑通——v2 产物 rc=0 但打印 **b1=140697991846872（0x7FF6 指针值）/b2=0/b3=846523790880（栈地址）/b4=0·a2=101 正确**——b 四字段=垃圾指针/栈值·字段访问=对垃圾指针二次解引用（614 铁证复现 ✓·O 对照 rc=0 全对）。113 基线复现 0xC0000374 ✓。
**2. asm 复证（N2 版层拷S）**：prologue `mov rsi,rcx; lea rdi,[rbp-72]; mov rcx,32; rep movsb`（结构体按值副本发射在）+返回=`mov rax,[rbp-72]`（标量值）——调用点 `lea rcx,[&b物化]; mov rdx,[&a]; call`（sret 形态）——**错位机制与 614 铁证一致**：被调把 rcx（&b 物化）当「表 基址」rep movsb 拷了未初始化物化槽垃圾→返回 rax=垃圾→b.字段=二次解引用读栈。
**3. 读码深化（初始化签名与返回字段·解析函数声明）**：函数声明.附加=返回类型驻留 ID（语法 199 行 建节点 确认·返回类型ID=驻留(解析类型文本)）——判定输入无误；**修复点收窄到生成返回语句 的 返回结构体值 判定/发射序**（或其上游 层拷S 生成时的 实例/布局查询窗口——需 asm/诊断逐点）。
**4. 下一棒（001-001 续）**：①生成返回语句 判定/发射单点修复（+必要时 初始化签名与返回字段 联动）→N2/O/L/K 回归→113；②79_v2 二分；③524 随 592。→撤守卫态零红→守卫删除+借链 51 例转自洽→随父 001 销项。
**5. 诚实边界**：本轮=铁证复现+读码深化（修复未出——asm 逐行对照需专注会话）；探针资产 target/p614c（614 树·已删）/p612c 源码 025 可考·p614c probe_run.py 流程脚本模式可复用。




<!-- 617-a 滚转：614-a/613-a/612-a 已收官轮次家机节回填（指针留 025） -->


