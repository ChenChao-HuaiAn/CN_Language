# plans/025 深度机节历史段（546-a 棘轮滚转·2026-09-21·封存不改）



---

### 2.115 D1 设计型池·方案设计轮（**解析类声明 185 行重构方案**＝五子函数划分+状态传递设计+等价验证策略·零代码·登记待实施）——535-a 设计轮（分支 `任务/深度机-535-解析类声明设计`·基点=develop 603b4745·wt520 隔离·2026-09-21）

**1. 结构分析（整读定案）**：`解析类.cn:252-436` 解析类声明——两层循环（262 继承跳过当+277 类体主循环 当(真)）+19 个局部状态·其中**布局三元组**（字段偏移/最大对齐/最大字段）跨分支累加+**位置**全程推进+**段可见性**主循环内切换。类体主循环内四类成员形态：方法（300-333·含构造）/不安全析构+函数（343-392·414-a stdlib 析构形态）/字段（397-416）/默认段守卫。**改判依据复盘（533 先例延续）**：非扁平分派但**成员形态间互斥且有清晰边界**——可拆（与 解析选择语句 的跨层中断不同·本函数无跨层 中断 语义——262 循环的 中断 限于自身块内）。
**2. 子函数划分方案（五子函数）**：①`解析类继承跳过`(记号,位置)->位置——262-269 继承/接口列表显式诊断+跳过；②`解析类可见性标签`(记号,位置,段可见性)->(新可见性,新位置)——283-289；③`解析类方法成员`(池,记号,位置,段可见性,类键基,...)->(新位置,是否消耗)——300-392 方法/构造/不安全析构形态（最大块·内部可再按 414-a 析构形态/普通函数二次切分·实施轮定）；④`解析类字段成员`(池,记号,位置,段可见性,字段偏移&,最大对齐&,最大字段&,...)->(新位置,是否消耗)——397-416 字段+布局累加；⑤`解析类布局收口`(字段偏移,最大对齐,池,...)->位置——422-433 尾部对齐+登记校验。
**3. 状态传递设计**：位置=每子函数返回值（值传递）；池/符号表=引用参数（既有惯例）；布局三元组=引用参数（`整64& 字段偏移` 三连·对齐 登记函数参数们 的引用惯例）；段可见性=值输入；主循环的 保底推进（420 前位置防死循环）保留在主函数（防死循环守卫不入子函数=职责单一）。
**4. 等价验证策略**：①新旧 v2p 编译同一改前输入 asm 逐字节 md5（探针=类/接口/继承/构造析构/运算符重载成员全形态用例 20/21/22/23/95/100/108/333+全树双对拍·518/530 口径）；②反证=变异布局累加行→产物变→恢复复绿；③全量门禁（解析类用例族 20/21/22/23/28/95/100~108/333 全在 E2E 覆盖）。
**5. 实施轮预估**：方案已定稿·实施=行号切片+签名落地+对拍反证+门禁（约 1 轮·同 532 周期）·候选=下一会话首选拾取。
**6. 交叠**：解析类.cn 零占用（329=词法/主/货舱·513~517=IR 生成层）·与全部在飞零交叠。

---

### 2.111 D25 剩余面复验（**linux_x64 gcov 覆盖率采集**＝471-a 家机语料轮指定的深度机任务·🧪 零代码）——524-a 服务轮（分支 `任务/深度机-524-d25gcov`·基点=develop 1d8f969d·wt520 隔离·2026-09-20）

**【收工回填（实测·改判）·533-a】**
**改判=设计型重构候选（不拆）**：整读 生成方法调用（456-605·151 行）——**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·嵌套 5 层+跨层 `返回;` 终结语义+共享可变状态〔表达式栈/指令/计数指针〕）——机械拆分不可行（嵌套提取需把外层条件转子函数前置守卫·跨层 返回; 转译风险高）。**登记=设计型重构候选**（与 解析类声明 185/解析选择语句 136 同类·需独立设计轮）。零代码收工。

---

### 2.114 D1 滚动（**v2 树 `生成表达式求值.cn` 双函数拆分**＝生成复制展开 175+生成方法调用 146·纯机械零行为变更·501 链作废后避让解除）——532-a 开发轮（分支 `任务/深度机-532-生成求值拆分`·基点=develop 9b991aa1·wt520 隔离·2026-09-21）

**1. 目标与验收判据**：021 §3-D D1 滚动（`IR类.cn:450-588` 克隆节点修针 138 行·泛型克隆体指针修正·调用方 2 处=627 泛型类方法/805 函数克隆器 432-b）。**拆分方案（调用方语义侦查后定稿）**：主分派器+十子函数——7 语句族（如果/当/循环/三元/二元/赋值/选择·**提前返回**=跳过后续类型检查必假=等价）+兜底回填（**不返回**·声明族依赖兜底回填先行——方法/类字段/参数类型的子起·子终回填在 else 兜底执行后再入专属分支·重排为 else-if 会丢回填=行为变更·侦查捕获）+类字段参数+方法（返新类型文本替换）。**验收**=①新旧 v2p 编译同一改前输入 asm 逐字节 md5 一致（x64l 探针+全树双对拍·518/520 口径）；②反证双态；③check_fn_length 出列（61→60）；④linux 全量门禁零新增红。
**2. 写集（预登记）**：CN语言编译器v2/IR/IR类.cn（**唯一代码文件**·501/513~517 均不触）｜plans/021/025/更新日志/交接｜看板清行。零 src/零 tests。
**3. 实施步骤**：①基线（v2p_base x64l 探针+全树 asm md5）②python 行号切片+边界断言（452~586 逐分支断言）③重建对拍④反证双态⑤全量门禁⑥六件套。
**4. 验证链**：md5 双对拍→反证→构建零警告→单测→全量 E2E→自举链 78/79（克隆器=泛型克隆主路径·自举链=最大消费面）→strict/matrix→check_fn_length。
**5. 边缘（预判）**：①**执行顺序敏感点已识别**——兜底 else 对方法/类字段先行回填子起·子终（声明族槽位若恒 0 则 no-op·但不静态假设·保守保留原顺序）②节点=值传递（无 &）·子函数值传/返回等价③二元分支注释（432-b 野走第二族）随块迁移④同号互踩假红（210_v2/457/458/459）按已知根因串行复验口径。
**6. 交叠**：IR类.cn 与 501（IR布局/常量/生成/生成类型/声明/类=其他 IR 文件）零交叠；513~517（生成层+src）零交叠；522（tests）零交叠。

---

### 2.82 D10 v2 面（**泛型实例体重放语义检查**＝417-a「检查延后」欠账根治·宿主 317-a 对齐）——470-a 开发轮（分支 `任务/深度机-470-d10v2`·基点=develop b3138c3·wt470 隔离·2026-09-20）

**【收工回填（实测）·470-a】**
**1'. 验收（达成·验收②口径修订如实声明）**：①探针四实弹双侧定性矩阵落定——p0 非泛型未声明调用=宿主/v2 双侧编译期拒绝（对照基线）；p1 泛型体未声明调用=双侧静默（宿主链接期爆 undefined reference·v2 全静默产垃圾）→**根治后双侧编译期拒绝**；p2' 泛型体返回类型误用（T=整32 返回字符串字面量）=**双侧仍静默→修订登记波2**（零环境映射下转换检查对类型参数 T 放行——环境映射=T→实参替换后双侧一起收·见 5'）；p3 复制内置/p4 普通语句/p6 T* 下标（455 形态）=健康面全保持 ✓；②457_泛型实例体未声明拒绝 v2 负测上架（v2p sem5 拒通道·单跑 PASS+反证双态）✓；③全量门禁零新增红 ✓（见 4'）；④反证=挂点改恒假→457 探针 rc 1→0（诊断消失）→恢复 md5 一致→rc 复绿 1 ✓。
**2'. 写集（最终）**：CN语言编译器v2/语义/语义检查.cn（+重放检查函数块 84 行：静态 已重放函数实例表/清空重放检查状态/取重放豁免安全区/重放检查泛型函数实例+主流程两挂点〔入口清空+表弹出前调用〕）｜CN语言编译器v2/语义/语义检查表达式.cn（两处豁免：错误不安全边界+指针下标元素类型·T 语境=静态不可判定·波2）｜src/cn_compiler/semantic/generics.cpp（recheckGenericFuncBody 重放诊断窄放行：「未声明」类 Error 不再随快照回滚吞没·report 回注+计数）｜tests/e2e/457_泛型实例体未声明拒绝/（新 3 文件）｜tests/e2e/coverage_map.md（457 登记·5.6 负测列+说明表）｜文档六件套。**IR/生成函数.cn/生成类.cn 零改动**（预判成立·重放挂语义侧零 IR 面改动）。
**3'. 实施增量（预判外当场修·四枚）**：①**静态标量写触发 v2 自身安区检查**——重放豁免标志 `重放豁免安全区 = 1` 被语义检查.cn 自身编译期拦截「[安全区边界] 静态变量写应在 不安全 函数 内」→两函数标 不安全（对齐 检查函数 既有惯例·**防线工作正常的正面实证**）；②挂点初版放 主流程末（表弹出后）→参数层失效误报「未声明标识符: 源」→**前移至 表弹出 前**（全局符号层须活着）；③收集口径初版=顶层区间扫→体内调用语句形态漏收（p2 表达式语句形态零收集）→**改全池扫**（体内子树节点同在池·O(池) 不变）；④去重表初版=向量+.包含→向量无该 API→改 映射.获取/设置（既有 API 惯例）。
**4'. 验证链（全绿）**：探针六态=p0~p6（对照基线/根治目标/健康面/豁免面）终态 p1 rc=1+p2 rc=0（波2）+p3/p4/p6 rc=0+p5 rc=1（既有防线不回归）；**457 单跑 PASS+反证双态**；**第一轮门禁（v2 件）**=构建零警告+单测 1363/1363+E2E **477=475/0/2**（477=476+457 新例·零失败零新增红）+strict 179/179+matrix PASS；**宿主件**=增量构建零警告+p1 宿主 build **链接爆→编译期拒绝**（「未声明的标识符」rc=1）+p3/p4/p6 check 不回归；**终态门禁（v2+宿主件）**=构建零警告+单测 1363/1363+E2E 477=475/0/2+strict/matrix PASS（数字见提交信息）。
**5'. 边缘情况（回填）**：预判①成立且扩面——T 表达式位之外**返回 vs T 误用也归波2**（转换检查对类型参数放行·双侧同族：宿主重放回滚+v2 豁免）；预判②③④成立（去重安全/编译期一次性/自举链零影响实证）；⑤交叠预判成立（零文件交叠）。**诚实边界（随轮登记 021 D10 行）**：p2' 返回类型误用=波2（环境映射 T→实参后双侧收口）；泛型类方法实例体（416-a 克隆）重放=波2；重放诊断行号=基类 AST 行（v2 侧·实例名定位靠诊断文本）。
**6'. 与他机交叠（回填）**：全程零冲突；466（同机会话）IR 面零交叠实证；459/451 集成滚动与本轮 rebase 无遇（基点 b3138c3 已含 451）。

---
# plans/025 深度机节 2026-09 归档切片

> 滚转月切片（§6.7）：深度机节已完成/已集成轮次的在飞明细原文。
> 565 轮滚转（2026-09-21·棘轮 724>720 先滚转再追加）；546 链同名切片集成时 add/add 追加合并。

### 2.83 D46（**v2 结果构造名裸名编译期拒绝**＝`返回 正常;`/`返回 错误;` 语义层防线）——474-a 开发轮（分支 `任务/深度机-474-d46naked`·基点=develop aef637b6·wt474 隔离·2026-09-20）

**1. 目标与验收判据**：v2 对「返回 正常;」/「返回 错误;」构造名裸名静默过+产物垃圾（p8 探针实锤：宿主 check rc=1 拒绝〔函数指针转换错形态〕·v2p rc=0 全静默）→v2 语义层编译期拒绝。**验收**=①p8 探针 v2 侧 rc 0→1（诊断含构造名与带参提示）；②新负测 E2E 上架（v2 拒通道·单跑 PASS+反证双态）；③全量门禁零新增红+自举链 78/79 双 PASS+strict/matrix。
**2. 写集（预登记）**：CN语言编译器v2/语义/语义检查*.cn（返回语句/标识符检查单点）｜tests/e2e/<新号>_构造名裸名拒绝/（新）｜tests/e2e/coverage_map.md（登记）｜plans/021/025/更新日志/交接（六件套）｜三机任务看板.md（收工清行）。注：本节落盘随分支二次提交补推（首次 push 分支名先占防撞·合规精神=合并前计划在分支）。
**3. 实施步骤**：①p8 定性 v2 解析形态（裸名=标识符节点路径实锤）→②语义检查单点：标识符名∈{正常,错误} 且非调用 callee 位（返回语句表达式等）→诊断拒绝（对齐宿主拒绝语义·提示带参形态 正常(值)/错误(码)）→③负测 E2E 上架+coverage_map→④全量门禁+反证（撤诊断点→p8 复现静默→恢复复绿）。
**4. 验证链**：p8 双侧对照→新负测单跑→全量门禁（构建零警告+单测+全量 E2E `--jobs 8`+strict/matrix+自举链·gate_lock 串行）→反证双态。
**5. 边缘情况与诚实边界（开工预判）**：①表达式位裸名（`r = 正常;` 等）=同族残余（本轮返回语句位·表达式位登记）；②「正常/错误」作普通标识符名=不误伤（判定=构造名集合成员+非调用位）；③394-a 设想的预扫描框架=若单点覆盖 D46 登记实弹则不引入（简化·回填写实）。
**6. 与他机交叠**：深度机 469/472（同机他会话·撤守卫 IR 面/T57）写集不同文件零交叠；家机 471（D25 tests 单测语料）零交叠；本会话 470（已收工挂回签）同文件 语义检查.cn 先后改=rebase 顺序消解合并友好。

**【收工回填（实测）·474-a】**
**1'. 验收（达成）**：①p8 探针 v2 侧 rc 0→1 ✓（双诊断=正常/错误 两处裸名各一条·「[错误] 构造名裸名须带参调用: 正常（ 正常(值) / 错误(码) / 某些(值) ）」）；②458_构造名裸名拒绝 v2 负测上架（v2p 拒通道·单跑 PASS+反证双态）✓；③全量门禁零新增红+自举链双 PASS（数字见提交信息/门禁日志）✓；④反证=诊断条件 恒假 注入→p8 rc 1→0（诊断消失）→恢复 md5 一致→复绿 1 ✓。
**2'. 写集（最终）**：CN语言编译器v2/语义/语义检查.cn（返回语句检查单点 +20 行·D46 裸名判定）｜tests/e2e/458_构造名裸名拒绝/（新 3 文件）｜tests/e2e/coverage_map.md（458 登记·5.6 负测列+说明表）｜plans/021/025/更新日志/交接（六件套）。
**3'. 实施要点（实测修正）**：根因=71-a 被调标识符放行（语义检查表达式.cn 59）对**裸名位**经同一路径静默放行——修复=返回语句位单点判定（返回表达式=节点_标识符 且 文本∈{正常,错误,某些}→拒绝）·`返回 正常(5);`=节点_调用 不入判定不误伤（24 族/211_v2 单跑健康实证）。负测 expected 行号以 runner 真跑为准（runner 环境 v2src 拷贝行号≠直跑·两次修正后 PASS）。
**4'. 边缘情况（回填）**：预判①成立——表达式位裸名（`r = 正常;` 等）=同族残余·021 D46 行登记；预判②成立（构造名保留语义优先·用户同名变量误伤面=负测矩阵未暴露·诚实边界注记）；预判③成立——**预扫描框架未引入**（单点覆盖 D46 登记实弹·简化落地）。
**5'. 与他机交叠（回填）**：零冲突；470 集成后 语义检查.cn rebase 顺序消解（D46 单点与 470 重放块不同区）。

> 历史轮次明细（274-a~295-a）由 git 提交信息+plans/021 §5 索引承载——本节按整节替换惯例轮出。

**1. 目标与验收**：D46 家族第三段——赋值位裸名（p10 探针：`r = 正常;` 宿主拒/v2 静默）→v2 编译期拒绝（475 同款模式·检查赋值语句 单点）。**验收**=①p10 v2 rc 0→1；②健康面（24 族/211_v2/344）不回归；③全量门禁零新增红+自举链+strict/matrix；④反证双态。
**2. 写集**：CN语言编译器v2/语义/语义检查语句.cn（检查赋值语句 右值位判定 ~17 行）｜plans/021/025/更新日志/交接（六件套）｜三机任务看板.md（收工清行）。
**3. 实施步骤**：①p10 探针双侧定性→②读 检查赋值语句 右值定位（左值区间=[文本!=0?子起点:附加, 子终点)——右值=末子节点假设·实测定案）→③单点插入→④健康面+门禁+反证。
**4. 验证链**：p10 双侧→健康面单跑→全量门禁（gate_lock 串行）→反证双态。
**5. 边缘预判**：①赋值节点=复合赋值族（+=/-=）同族面（同函数收口）；②右值定位若为多节点形态（成员链赋值）→以末子节点实测为准（475 模式）；③实参位裸名=继续登记（函数调用实参区间=构造器调用特判已挡·普通调用实参位形态未实弹——若 p10 轮实测有实弹再立案）。
**6. 与他机交叠**：470/474（本会话·挂等回签）写集=语义检查.cn/语义检查表达式.cn/测试——本轮=语义检查语句.cn **不同文件零冲突**；469/472 零交叠。

**【收工回填（实测）·476-a】**
**1'. 验收（达成）**：①p10 探针 rc 0→1 ✓（`r = 正常;` 赋值位拒绝·双诊断文案与 474/475 同族）；②健康面=24 族 7/7+211_v2 PASS ✓；③全量门禁零新增红（数字见提交信息）✓；④反证=恒假注入 rc 1→0→恢复 md5 一致→复绿 1 ✓。
**2'. 写集（最终）**：CN语言编译器v2/语义/语义检查语句.cn（检查赋值语句 右值位判定 +17 行）｜plans/021/025/更新日志/交接（六件套）。
**3'. 实施要点**：右值定位=末子节点（与 检查混合符号赋值/标量隐式转换/逃逸检查 同款取法·既有三处先例同模式零歧义）；插入点=逃逸检查后、检查表达式循环前。**D46 家族终态**=返回位（474）+声明初始化位（475）+赋值位（476）三段全收——实参位裸名（`f(正常)` 形态）未实弹未立案（构造器调用特判已挡构造场景·普通调用实参位=诚实边界注记）。
**4'. 边缘（回填）**：复合赋值族（+=/-=）经 检查赋值语句 同路径=同款覆盖 ✓；「某些」裸名不收（可选枚举值语义·先例维持）。
**5'. 交叠（回填）**：零冲突。

---

### 2.82 D10 v2 面（**泛型实例体重放语义检查**＝417-a「检查延后」欠账根治·宿主 317-a 对齐）——470-a 开发轮（分支 `任务/深度机-470-d10v2`·基点=develop b3138c3·wt470 隔离·2026-09-20）

**【收工回填（实测）·470-a】**
**1'. 验收（达成·验收②口径修订如实声明）**：①探针四实弹双侧定性矩阵落定——p0 非泛型未声明调用=宿主/v2 双侧编译期拒绝（对照基线）；p1 泛型体未声明调用=双侧静默（宿主链接期爆 undefined reference·v2 全静默产垃圾）→**根治后双侧编译期拒绝**；p2' 泛型体返回类型误用（T=整32 返回字符串字面量）=**双侧仍静默→修订登记波2**（零环境映射下转换检查对类型参数 T 放行——环境映射=T→实参替换后双侧一起收·见 5'）；p3 复制内置/p4 普通语句/p6 T* 下标（455 形态）=健康面全保持 ✓；②457_泛型实例体未声明拒绝 v2 负测上架（v2p sem5 拒通道·单跑 PASS+反证双态）✓；③全量门禁零新增红 ✓（见 4'）；④反证=挂点改恒假→457 探针 rc 1→0（诊断消失）→恢复 md5 一致→rc 复绿 1 ✓。
**2'. 写集（最终）**：CN语言编译器v2/语义/语义检查.cn（+重放检查函数块 84 行：静态 已重放函数实例表/清空重放检查状态/取重放豁免安全区/重放检查泛型函数实例+主流程两挂点〔入口清空+表弹出前调用〕）｜CN语言编译器v2/语义/语义检查表达式.cn（两处豁免：错误不安全边界+指针下标元素类型·T 语境=静态不可判定·波2）｜src/cn_compiler/semantic/generics.cpp（recheckGenericFuncBody 重放诊断窄放行：「未声明」类 Error 不再随快照回滚吞没·report 回注+计数）｜tests/e2e/457_泛型实例体未声明拒绝/（新 3 文件）｜tests/e2e/coverage_map.md（457 登记·5.6 负测列+说明表）｜文档六件套。**IR/生成函数.cn/生成类.cn 零改动**（预判成立·重放挂语义侧零 IR 面改动）。
**3'. 实施增量（预判外当场修·四枚）**：①**静态标量写触发 v2 自身安区检查**——重放豁免标志 `重放豁免安全区 = 1` 被语义检查.cn 自身编译期拦截「[安全区边界] 静态变量写应在 不安全 函数 内」→两函数标 不安全（对齐 检查函数 既有惯例·**防线工作正常的正面实证**）；②挂点初版放 主流程末（表弹出后）→参数层失效误报「未声明标识符: 源」→**前移至 表弹出 前**（全局符号层须活着）；③收集口径初版=顶层区间扫→体内调用语句形态漏收（p2 表达式语句形态零收集）→**改全池扫**（体内子树节点同在池·O(池) 不变）；④去重表初版=向量+.包含→向量无该 API→改 映射.获取/设置（既有 API 惯例）。
**4'. 验证链（全绿）**：探针六态=p0~p6（对照基线/根治目标/健康面/豁免面）终态 p1 rc=1+p2 rc=0（波2）+p3/p4/p6 rc=0+p5 rc=1（既有防线不回归）；**457 单跑 PASS+反证双态**；**第一轮门禁（v2 件）**=构建零警告+单测 1363/1363+E2E **477=475/0/2**（477=476+457 新例·零失败零新增红）+strict 179/179+matrix PASS；**宿主件**=增量构建零警告+p1 宿主 build **链接爆→编译期拒绝**（「未声明的标识符」rc=1）+p3/p4/p6 check 不回归；**终态门禁（v2+宿主件）**=构建零警告+单测 1363/1363+E2E 477=475/0/2+strict/matrix PASS（数字见提交信息）。
**5'. 边缘情况（回填）**：预判①成立且扩面——T 表达式位之外**返回 vs T 误用也归波2**（转换检查对类型参数放行·双侧同族：宿主重放回滚+v2 豁免）；预判②③④成立（去重安全/编译期一次性/自举链零影响实证）；⑤交叠预判成立（零文件交叠）。**诚实边界（随轮登记 021 D10 行）**：p2' 返回类型误用=波2（环境映射 T→实参后双侧收口）；泛型类方法实例体（416-a 克隆）重放=波2；重放诊断行号=基类 AST 行（v2 侧·实例名定位靠诊断文本）。
**6'. 与他机交叠（回填）**：全程零冲突；466（同机会话）IR 面零交叠实证；459/451 集成滚动与本轮 rebase 无遇（基点 b3138c3 已含 451）。

> 〔2.1 收工回填（**314-a 🧪 M3 采样轮·x64l 列**·基线 a33e3aa〔=src 与 312-a 基线 ce8089c 等价〕·2026-09-17 收工）：**双域零命中+T44 复验+★T45 立案** 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕

**【收工回填（实测）·521-a】**
**1\'. 定性结论（三选一取「撤销登记」）**：①探针 6 形态（下标目标=取点(10)/对表[1]=取点(20)·标识符目标=取点(30)·下标=复制(源)·结果构造=正常(50)·对照健康面）——宿主与 v2p 编译运行输出 **10 行逐行全同、rc 同 0**；②asm 铁证=「下标目标=结构体返回调用」发射 **op=61 整体赋值通道**（`mov rcx,24; rep movsb` 24 字节）而非 StorePtr 8 字节截断——234-a 时期的「emitStructWholeAssign 无源分派」现状已被后续接力链（468 九链/435/441/477 等）覆盖；③全库存量扫描=显式「下标目标=函数调用」形态 E2E/v2 树/stdlib ≈0（零覆盖=既有事实）。
**2\'. 写集（最终）**：plans/021（D11 迁 §4.2+§五 521-a+戳）｜plans/025 本节｜交接/更新日志｜看板（清行）。零 src/v2 树/tests 代码（纯侦查）。
**3\'. 边缘（回填）**：①成员链目标（`外.内 = 取点()`）依赖类内结构体字段=T80 面（513 链挂等回签）——不构成 D11 独立缺口·已注记；②**附带发现（登记）**：宿主对「下标结构体字符串字段」类型推断降级「未知」（`打印(点表[2].名)` 报「参数类型不支持：未知」·`字符串 名=点表[2].名` 同病·84-d 同族候选）——探针以字符串比较法绕过；③探针可作 E2E 正测转正候选（下一 tests 轮搭车）。
**4\'. 交叠（回填）**：517 链（生成语句赋值.cn）零冲突（本轮只读）；全程零文件碰撞。

**【收工回填（实测）·523-a】**
**1\'. 验收（达成）**：①md5 双对拍=探针 3982d9f7+全树 111038 节点 c4106a2c（新旧 v2p 编译同一改前文本）✓；②行为级反证=查ac 恒等变异→v2p_mut 编译 456_泛型指针字段下标 产物 761102ae（变）→恢复→82fc1dc0 复绿 ✓——**克隆修针消费面=泛型实例化（v2p 编译用户程序）·自举编译（v2p 编译 v2 树自身）不触发**（全树/探针对修针变异零敏感=三轮反证迭代定位）；③check_fn_length 61→60 ✓；④门禁=单测 1387/1387+E2E 522=517/3/2（3 假红=同编号互踩·串行单跑全 PASS）+strict 零孤儿+matrix 83/83 ✓。
**2\'. 写集（最终）**：CN语言编译器v2/IR/IR类.cn（+167/-134·分派器 14 行+十子函数+返回行补齐——拆分后子函数须显式 返回 n2c·v2 非空返回类型纪律）｜文档六件套。
**3\'. 边缘（回填）**：①**侦查两改方案均被否**——四槽位族分组（基于 grep 截断误读）与 else-if 重排（丢兜底先行回填=行为变更）→最终=保守分派器拆分+兜底不返回；②首轮切片漏 7 行（分支收尾 `}` 与下行行首 `} 否则` 分离）→花括号失衡编译期当场暴露·边界断言补齐重切；③返回行补齐=v2 语义要求。
**4\'. 交叠（回填）**：全程零冲突。

**【收工回填（实测）·524-a】**
**1\'. 数字（gcov 命令级可复现）**：ir_verify.cpp 行 86.58%（149 可行行）·分支执行 88.95%/走向取正 64.21%；ast_printer.cpp 行 68.78%（474 可行行）·分支取正 36.54%；全项目行 79.21%/分支 45.84%（134 编译单元）。**对比**：ir_verify 较 283-a 基线 85.59% **+0.99pp**；ast_printer 较 471-a 前 14.6% **+54.18pp**——471-a 家机语料轮 linux 侧实证有效。
**2\'. 边缘（回填）**：①总行覆盖 79.21%<基线 80.57%=**门禁语料步未跑的低估**（check_source_coverage 全链在 E2E 步被已知假红族中止 rc=1·语料步未执行·gcda 聚合完整）——非真劣化；②gcov 单文件聚合命令：`gcov -b <build-cov>/CMakeFiles/<lib>.dir/src/**/<file>.cpp.gcda`。
**3\'. 交叠**：零代码零 tests·全程零冲突。

### 2.112 501 链处置轮（**全链 merge 实测→裁决作废·只提取有效增量**＝EXTERN 修复+check_win_asm.py）——528-a 裁决轮（分支 `任务/深度机-528-501提取`·基点=develop eee970ef·wt501 隔离·2026-09-20）

**1. 背景与裁决（留痕）**：518-b 回签不通过（40/99+459_v2 三例）→深度机接手整改——全链 merge 实测 **8 红（5 个双编译真红）**：501 的旧词法/语法/解析/语义实现与 develop 现行演进（434~438/468/470/477 十余轮修复）**系统性冲突**（487=函数指针局部声明位/488=枚举字段/494=浮点宽窄化=501 v2 增量功能 develop 无；40/99=两套实现的 T 处理差异）。**裁决=501 链作废**（重做十余轮修复成本>>价值·develop 现行功能等价或更优）+**只提取有效增量**：EXTERN 收集口径修正（win 174 例 A2005 根治）+check_win_asm.py 三判据门禁（cherry-pick a2241af2·唯一冲突=HEAD 空取 pick 侧）。
**2. 实测证据**：①merge 态全量 528=518/8/2（03/09/487/488/489/491 六双编译真红=501 旧实现破坏 develop 行为·复现命令=/tmp/p501fix/e2e.log）②提取态全量 **522=516/4/2 零新增红**（4 假红=同编号互踩已知根因·与 develop 基线同签名）+单测 1387/1387 ③459_v2 行号对齐实证=merge 态词法取 develop 现行后输出「行 6」=438-a 口径期望 ✓。
**3. 写集**：CN语言编译器v2/代码生成/代码生成.cn（EXTERN 修复·cherry-pick）｜scripts/check_win_asm.py（新·三判据门禁）｜文档六件套。零 src/零 tests。
**4. 边缘（留痕）**：①40/99 假性转换拒的整改改由「弃用 501 旧实现」达成（merge 态曾试 查点①①b② 三处映射补丁·隐式转换拒消但其余真红仍在·证实系统性冲突）②487~494 用例随对应功能（函数指针局部声明=D43 族/枚举全链=D40 族/浮点宽窄化）在 develop 现行架构的实现轮重新上架③win 侧 EXTERN 修复的 ml64 验证=归家机下轮全量（本机无 ml64）。

> 〔2.107 D1 行数滚动续列（**v2 树 `编码算术ARM64` 166 行拆分**＝分派器+子函数·纯机械重构零行为变更） 已滚转归档（深度机 528-a·2026-09-20·已集成·内容 git 可考）〕
> 〔2.108 D11 悬案侦查轮（**「下标/成员目标 = 结构体返回调用」整体赋值通道存量盘点**＝探针定性+全库扫描·零代码） 已滚转归档（深度机 530-a·2026-09-20·已集成·内容 git 可考）〕
> 〔2.110 D1 滚动（**v2 树 `克隆节点修针` 138 行拆分**＝类型分派器+十子函数·纯机械重构零行为变更） 已滚转归档（深度机 530-a·2026-09-20·已集成·内容 git 可考）〕
### 2.113 D1 滚动收口（**v2 树 `编码算术Win` 206 行拆分**＝分派器+子函数·纯机械零行为变更·501 链作废后避让解除）——530-a 开发轮（分支 `任务/深度机-530-编码算术win拆分`·基点=develop fb5eebc9·wt520 隔离·2026-09-20）

**1. 目标与验收判据**：021 §3-D D1 滚动第 4 弹（518 X64L/520 ARM64/523 克隆修针 同款工法·`代码生成.cn:62-267` 编码算术Win 206 行——**501 链作废后代码生成.cn 避让解除**）。**验收**=①新旧 v2p 交叉编译同一改前输入 **win-x64 asm 文本**逐字节 md5 一致（探针+全树双对拍——win asm=纯文本发射无需 ml64·520 交叉口径）；②反证双态；③check_fn_length 出列；④linux 全量门禁零新增红。
**2. 写集**：CN语言编译器v2/代码生成/代码生成.cn（唯一代码文件）｜文档六件套｜看板清行。零 src/零 tests。
**3. 步骤**：基线→拆分（行号切片+边界断言）→对拍→反证→门禁→六件套。
**4. 验证链**：win asm md5→反证→构建零警告→单测→全量 E2E→strict/matrix→check_fn_length。**诚实边界**：win asm 文本等价=产物不变的充要铁证（文本逐字节一致则 ml64 输入相同·输出必同）；win 真汇编仍归家机复验面。
**5. 边缘（预判）**：①编码算术Win 与 X64L 同构（MASM 语法差异）——分块按源文实测；②win 侧 ml64 汇编验证=家机下轮全量（本机无 MASM）。
**6. 交叠**：代码生成.cn 避让解除（501 链作废裁决留痕）；与其余在飞零交叠。

**【收工回填（实测）·530-a】**
**1\'. 验收（达成）**：①md5 双对拍=探针 901ed2bd+全树 111674 节点 26815334（新旧 v2p 编译同一改前文本·**win-x64 asm 交叉文本口径**——纯文本发射无需 ml64）✓；②反证双态=addZ 注入 03657d14→恢复 901ed2bd 复绿 ✓；③check_fn_length 59→58 ✓；④门禁=单测 1387/1387+E2E 522=517/3/2（3 假红=同编号互踩已知根因）+strict 零孤儿+matrix 83/83 ✓。
**2\'. 写集（最终）**：CN语言编译器v2/代码生成/代码生成.cn（+191/-178）｜文档六件套。
**3\'. 边缘（回填）**：①win asm 文本等价=ml64 输入相同的充要铁证（真汇编归家机复验面·诚实边界保持）；②D1 滚动第 4 弹完成——**v2 树可机械拆分的算术三后端族全部出列**（X64L/ARM64/Win），剩余大函数=避让中或设计型/不可拆（解析选择语句 136=跨层中断语义已登记不可拆）。
**4\'. 交叠（回填）**：全程零冲突。

### 2.131 024 D43 别名全链收口＋02_control 阻塞面真根因修复（**内置别名规范化函数单一归属＋谓词散点收敛＋检查选择类型过滤补**＝413-a 语义层遗漏·机制级当场修）——657 开发轮（分支 `任务/深度机-657-D43别名全链`·基点=develop 99b7636a·wt657 隔离·2026-09-23）

**【收工回填（实测·657-a）】**
**1'\. 验收（达成·含开工预判外新发现一枚）**：①**02_control 阻塞面真根因实锤**=非别名本身（402-a 已通）而系 **413-a 检查选择语义层漏类型过滤**——插桩五轮（查名未命中/检查块入口/主扫节点/选择字段/情况节点字段）锁定：选择节点.子起点=吃 `{` 后池位置=**情况值+体语句+体块+标签全部节点**（生成层注释明说此布局且生成层两处消费点均有「类型过滤=节点_情况标签」·语义层漏）——无过滤把如果/当节点字段 [文本,附加)（=[条件区+真块区]）当值表达式区间在**外层作用域**查名→嵌套块局部声明伪报「未声明标识符」；②修复=况i 循环加类型过滤+体块检查保留——探针矩阵 **5/5 双侧逐值一致**（p1 如果体=2/p2 情况体+当=2/p2b 情况体+如果=0/p2c 情况体本级=1/p3 直接引用=7·修复前 p2 编译失败 2 诊断/p2b 1 诊断）+宿主 cn build 同矩阵逐值全同；③02_control 转双编译通道 **runner 单跑 PASS**（v2 产物与 expected 逐行一致·含第 7 段嵌套当+第 8 段 fall-through）；④449 回归 PASS；⑤反证双态：**反证A** 撤规范化挂接→「未声明的类型: 整数」×3（行 7/17/38）复现→恢复复绿=挂接唯一权威点实锤+IR布局 死分支删除安全性背书；**反证B** 撤类型过滤→p2×3+p2b×2 伪报复现→恢复复绿；⑥单测 **1405/1405**+strict **100% rc=0**+matrix **83/83**+E2E 全量 **528=522/4/2**（4 红=113/127_v2/497/79_v2 全部撤守卫态已知基线红·与 629-a 广播/单位机 5ed202f7 验收口径一致=零新增红；78_v2 PASS；2 跳=62/69 平台固有）。
**2'\. 写集（最终）**：CN语言编译器v2/{语法/解析基础.cn（是别名关键字 新谓词）·语法/解析表达式.cn（内置别名基名规范化 新函数+解析类型文本 挂接+3 处换接）·语法/解析语句.cn（2 处换接）·语义/语义检查.cn（检查选择 类型过滤）·IR/IR布局.cn（死分支删除+勘误）}+tests/e2e/02_control/双编译对照.txt（新）+教训 2 条+文档六件套。**零 src/**。
**3'\. 实施增量（预判外·四枚）**：①**A2 拥有契约**——规范化函数初版返回 字符串（返回借用参数=契约错）→改 字符* 借用视图返回（解析基础.cn 当前文本 先例）+调用方 字符串复制 显式拥有化（单次拷贝）；②**管道吞 rc 四犯**——手动构建管道取码假绿+旧产物缓存（v2p_fix 未删）→02_control 首测跑在实施①态旧产物上——裸跑取码+删产物重验后闭环（教训入册）；③**git checkout 误丢改动**——反证A 后 checkout 恢复「变异前」把未提交实施改动一并丢失→重写应用+**立即提交**（402-a 教训二犯·教训入册=实施先提交再开反证）；④看板棘轮 201>200→深度机通告段已毕条目（546~603 全部 09-21/22 广播·影响面已执行完毕）整段清偿 188 行达标。
**4'\. 边缘（回填）**：预判①~⑤全部命中处理（泛型+别名宿主缺口维持 D42 域登记/宿主「指针→空类型*」不引入/情况值常量性=413-a 既有边界维持/死分支删除有反证A 背书/02_control 转双测随任务验收判据报备）。
**5'\. 交叠（回填）**：零文件冲突（家机 629/653 写集不触 v2 语法/语义/IR布局·02_control 无既有通道文件）；语义耦合面=选择语句族全用例（450/66/85/448/02_control）已随全量门禁覆盖验证零回归。

**1. 目标与验收判据**：①021 任务 024（原D43·09-20 用户裁决甲）收口——v2 树「整数/小数」别名处理从散点收敛为**规范化函数单一归属**：新 `内置别名基名规范化(名)`（解析类型文本 基名级挂接唯一权威点·对齐宿主 types::canonical 形态）＋新 `是别名关键字(记号,位置)` 谓词（5 处散点判定换接：解析语句 102/336-337·解析表达式 17/749/869-870）＋IR布局 是标量类型文本 死分支删除与注释勘误（源头规范化完备后「整数/小数」分支不可达）。②**02_control 阻塞面真根因修复（本轮新发现·机制级·随证据备案）**——413-a 检查选择**语义层漏类型过滤**：情况段区间=[情况值+体语句+体块+标签] 全部节点（解析层子起点=吃 `{` 后池位置·值/体语句在标签前入池·生成层 生成语句.cn:187/733 两处均有「类型过滤=节点_情况标签」）·检查选择 无过滤把区间内任意「子起≠0」节点当情况节点→其 [文本,附加) 字段被当值区间检查（如如果节点字段=[条件区+真块区]）→嵌套控制流/局部声明在**错误作用域**被查名→伪「未声明标识符」（探针 p2/p2b 实锤·02_control 第 7 段正中）。修复=况i 循环加 `类型==节点_情况标签` 过滤＋恢复体块检查。③02_control 转双编译对照通道 PASS（阻塞面解除·D44 fall-through 已对齐+别名已通+本缺陷修复）。**验收**=探针矩阵 p1/p2/p2b/p2c/p3 双侧一致＋449_整数小数别名矩阵回归 PASS＋02_control 双编译 PASS＋全量门禁零新增红＋自举链 78/79＋strict/matrix＋反证双态（撤规范化挂接→「未声明的类型」复现；撤类型过滤→p2 伪未声明复现）。
**2. 写集（精确清单·预登记）**：`CN语言编译器v2/语法/解析基础.cn`（是别名关键字 新函数）｜`CN语言编译器v2/语法/解析表达式.cn`（内置别名基名规范化 新函数+解析类型文本 挂接+3 处谓词换接）｜`CN语言编译器v2/语法/解析语句.cn`（2 处谓词换接）｜`CN语言编译器v2/语义/语义检查.cn`（检查选择 类型过滤+体块检查恢复）｜`CN语言编译器v2/IR/IR布局.cn`（是标量类型文本 死分支+勘误注释）｜`tests/e2e/02_control/双编译对照.txt`（新·借链=否·退出码=0）｜plans/021（024 行收口+戳）｜plans/025 本节｜交接/更新日志本机节｜看板清行。零 src/（宿主已是 canonical 单一归属=参照实现）。
**3. 实施步骤**：①探针矩阵定案（p1 如果体✓/p2 情况体+当×/p2b 情况体+如果×/p2c 情况体本级✓/p3 直接引用✓→插桩实锤检查选择无过滤）→②检查选择 类型过滤修复+体块检查恢复→②'探针复验转绿→③是别名关键字 抽取+5 处换接→④内置别名基名规范化 抽取+解析类型文本 挂接→⑤IR布局 死分支删除+勘误→⑥02_control 双编译上架→⑦全量门禁+反证双态→⑧六件套+integrate。
**4. 验证链（命令级）**：探针矩阵 v2p 双侧逐值→02_control/449 单跑→零警告构建→单测全量→E2E 全量 --jobs 8→strict/matrix→自举链 78/79→反证双态两件→grep 复核散点零残留（`grep -rn '"整数"' CN语言编译器v2/` 仅剩词法注册源头+新函数本体）。
**5. 边缘情况与诚实边界（预判）**：①泛型+别名（向量<整数>）=**宿主缺口**（402-a 已登记 D42 域·449 未含）——维持登记不扩面；②宿主 canonical 另有「指针→空类型*」形态=规范 50 行外宿主实现——v2 不引入（规范口径·如实注记）；③检查选择 修复后情况值常量性未限定=413-a 既有诚实边界维持；④是标量类型文本 死分支删除的行为面=变异实验背书（删除后全量门禁绿=无消费路径依赖）；⑤02_control 转双测=AGENTS §4 铁律方向的测试升级·随本任务验收判据（021 024 行明文「02_control 阻塞面」）实施+集成广播报备。
**6. 与他机交叠**：家机 629（tests/e2e 面 30/31/155+run_e2e.py）/653（run_e2e.py+021）在飞——本写集 v2 树语法/语义/IR布局+tests/02_control 与其**零文件交叠**（02_control 无通道文件=无冲突面·021 行更新区不同）；家机 652 已收工（拷贝构造系·v2 树 IR 生成层）不涉语法/语义检查文件；单位机空闲。语义耦合=检查选择 修复影响全部选择语句用例（450 矩阵/66/85 等）——全量门禁覆盖+影响面=全平台 v2p 重建生效·集成后请两机异步验收选择语句面+02_control。

### 2.128 strict 四孤儿修复＋569 runner 平台硬编码修复（576/577 两案·分支 `任务/深度机-576-covmapfix`·基点=develop b294fa48·看板占号 2b76781e·分支头 8198fb02·2026-09-21·挂等三平台回签）

**1. 目标与验收判据**：①修复 b94c37bd「572 缺口备案」strict 红（4 孤儿+310→495 悬空指针）；②569 负测双测通道平台硬编码机制级修复（轻量 v2 负编译断言无条件 win 工具链+win-x64 目标——573 集成 2d283fdf 树级带入 569 runner 后 linux 首跑 71 例负测全炸「未找到 ml64/link」）。**验收实测**=分支头 8198fb02：strict rc 1→0（179/179=100% 指针有效+零孤儿）+matrix 83/83+零警告+单测 1394/1394+E2E **525=523/0/0 零失败**（修复前 452/71/2 对照·131 单跑转绿）。
**2. 写集（回填）**：`tests/e2e/coverage_map.md`（4 孤儿登记+指针同步）＋删 `tests/e2e/310_货舱特性声明制/`（残留目录·与 495 语义重复·557 改号成复制未删·E2E 计数 526→525·删除随 577-a 提交入库〔暂存时序·576-a 提交信息已勘误〕）＋`tests/e2e/run_e2e.py`（轻量断言分支平台分派·win 路径逐参数零改动）＋本节+看板本机行/通告段。
**3. 实施步骤（回填）**：①covmap 四例登记（500=fa59f316 原文恢复·434/435/495=T54 货舱族 557 链已验证内容入册）+4.7/5.9.5 指针同步+310 残留删除；②runner 修复=平台分派三处（就绪函数/平台参数/asm 产物名·linux=确保v2p与运行时就绪+GAS 参数+v2asm.s）。性质=登记恢复+平台分派补全，无新语义。
**4. 验证链（已跑）**：strict/matrix/零警告构建/单测/E2E 全量 --jobs 8/单跑转绿——数字见 1。反证=修复前全量 71 红对照。
**5. 边缘情况与诚实边界（回填）**：发现并修正预判外事实=310 目录两存（非单纯指针悬空而是目录残留）；569 runner 实际随 573 集成才入 develop 树——**上轮 569 回签（2aa213a3）树不含负测双测通道，回签结论被 573 集成架空，本分支 577-a 修复后需三平台重签**。诚实边界=win/arm64 复验走 try-build（分支已推·通告段请求已发）；E2E 计数 525 与他机 526 差 1=310 重复例消除（请求已注明）。
**6. 交叠分析（回填）**：写集与家机（569 收官/568 挂等/573 销账）+单位机（456 侦查）零交叠；语义耦合=strict 门禁（正向转绿）+runner 负测通道（569 作者 win 侧需复跑零回归）。

### 2.130 编号勘误轮（**任务 047 改挂 001 子任务=001-001**·纯文档零代码·用户裁决）——593-a 勘误轮（分支 `任务/深度机-593-047改号`·基点=develop bcf00290·wt593 隔离·2026-09-22）

**1. 目标与验收判据**：用户裁决——047（撤守卫容器双模语义收敛·585-a 立案）是 001 波3 衍生任务，编号应体现父子关系：改挂为 **001-001**（依 plans/021 规则「子任务=父号-三位子序」）；047 号退役防复用；收敛方向方案甲（全内联·用户 2026-09-22 裁决·与 Rust Vec 布局/宿主同构）写入 001-001 立案。**验收**=五文档 14 处引用全部改挂（grep 复核仅剩 025 历史记录 1 处+021 锚行退役注记）+双机械门禁绿。
**2. 写集**：plans/021（047 行→001-001 行移至 001 行下+001 行两处+锚行退役注记+§五 585-a 行两处）｜plans/025（§2.129 两处+本节）｜三机任务看板（通告段两处）｜交接（深度机节一处）｜更新日志（深度机节两行三处）｜看板本机行。
**3. 实施步骤**：①五文件精确替换逐处计数核销；②001-001 立案追加收敛方向裁决句；③锚行退役注记（新任务号自 050 起顺延）；④双机械门禁+提交+integrate（纯文档快速门禁）+镜像推送。
**4. 验证链**：check_handoff+check_progress_sync+grep 复核+integrate 快速门禁。
**5. 边缘情况**：025 72 行「585 立号撞 047/582」=历史事实记录保留原文（考古价值·勘误由本节+提交信息承载）。
**6. 与他机交叠**：纯文档写集·与在飞（家机 589/591/592/504 等）零语义交叠；develop 期间前进=rebase 自然消化（文档分区合并友好）。

### 2.116 D1 设计型池实施（**解析类声明 185 行按 §2.115 定稿方案拆分**＝五子函数+布局三元组引用传递·纯机械零行为变更）——537-a 实施轮（分支 `任务/深度机-537-解析类声明实施`·基点=develop 最新·wt520 隔离·2026-09-21）
> 〔2.112 501 链处置小节已滚转至归档（574-a 二次清偿·链已作废纯历史·git 可考）〕

> 〔2.113 小节已滚转至归档（574-a 三次清偿·git 可考）〕

### 2.114 D1 滚动（**v2 树 `生成表达式求值.cn` 双函数拆分**＝生成复制展开 175+生成方法调用 146·纯机械零行为变更·501 链作废后避让解除）——532-a 开发轮（分支 `任务/深度机-532-生成求值拆分`·基点=develop 9b991aa1·wt520 隔离·2026-09-21）

**1. 目标与验收判据**：021 §3-D D1 滚动第 5 弹——`生成表达式求值.cn` 两函数：生成复制展开 175 行（151-325）+生成方法调用 146 行（444-589）各拆为分派器+子函数（同族形态按源文实测）。**验收**=①新旧 v2p 编译同一改前输入 asm 逐字节 md5 一致（x64l 探针+全树双对拍）；②反证双态；③check_fn_length 出列两函数；④linux 全量门禁零新增红。
**2. 写集**：CN语言编译器v2/IR/生成表达式求值.cn（唯一代码文件）｜文档六件套｜看板清行。零 src/零 tests。
**3. 步骤**：基线→侦查整读两函数→拆分→对拍→反证→门禁→六件套。
**4. 验证链**：md5→反证→构建零警告→单测→全量 E2E→strict/matrix→check_fn_length。
**5. 边缘（预判）**：①**侦查必须整读函数**（523 教训·grep 截断曾致误判）；②两函数为独立分派结构（复制展开=节点类型分派·方法调用=调用形态分派）——若实测为共享状态顺序循环则如实登记不拆（解析类先例）；③同号假红族照旧口径。
**6. 交叠**：生成表达式求值.cn 与 513~517（生成语句赋值/生成表达式字段/生成表达式/IR生成）不同文件零交叠；501 已作废。

**【收工回填（实测）·532-a】**
**1\'. 验收（达成·范围收窄如实声明）**：①生成复制展开 175→主函数（前置可信判定 71 行+四路分派）+四路子函数（字符串改写 6/标量直通 12/容器类 27/结构体物化 47）——13 条边界断言·逐字迁移 ✓；②md5 双对拍=探针 3982d9f7+全树改前文本 111751 节点 c4355400 ✓；③**行为级反证**=④槽偏移变异（-1 删除）→probe4 结构体复制消费面产物 1c85b603（变）→恢复 d71c7563 复绿 ✓；④check_fn_length 58→57 ✓；⑤门禁=单测 1387/1387+E2E 522=516/4/2（4 假红=同编号互踩已知）+strict/matrix ✓。
**2\'. 范围收窄声明**：生成方法调用 146 行=同文件第二函数**登记下一轮**（本轮单函数已足轮量·四路精密分支的拆分验证周期超单轮预算）。
**3\'. 边缘（回填）**：①**反证迭代三才发现消费面**（探针算术面/全树自举均不消费复制展开路径·标量直通返回变异=等价变异产物不变——AST 生成层文件的反证消费面=「含目标形态调用的用户程序」probe4）②行号切片一轮漏 复obj 首行（编译期「未找到匹配的函数 IR追加」当场暴露·断言补齐重切）。
**4\'. 交叠（回填）**：全程零冲突。


**【收工回填（实测·改判）·533-a】**
**改判=设计型重构候选（不拆）**：整读 生成方法调用（456-605·151 行）——**深度嵌套过程式流程**（方法签名查→返回形态分派→用户类分派→内置函数·嵌套 5 层+跨层 `返回;` 终结语义+共享可变状态〔表达式栈/指令/计数指针〕）——机械拆分不可行（嵌套提取需把外层条件转子函数前置守卫·跨层 返回; 转译风险高）。**登记=设计型重构候选**（与 解析类声明 185/解析选择语句 136 同类·需独立设计轮）。零代码收工。

### 2.115 D1 设计型池·方案设计轮（**解析类声明 185 行重构方案**＝五子函数划分+状态传递设计+等价验证策略·零代码·登记待实施）——535-a 设计轮（分支 `任务/深度机-535-解析类声明设计`·基点=develop 603b4745·wt520 隔离·2026-09-21）

**1. 结构分析（整读定案）**：`解析类.cn:252-436` 解析类声明——两层循环（262 继承跳过当+277 类体主循环 当(真)）+19 个局部状态·其中**布局三元组**（字段偏移/最大对齐/最大字段）跨分支累加+**位置**全程推进+**段可见性**主循环内切换。类体主循环内四类成员形态：方法（300-333·含构造）/不安全析构+函数（343-392·414-a stdlib 析构形态）/字段（397-416）/默认段守卫。**改判依据复盘（533 先例延续）**：非扁平分派但**成员形态间互斥且有清晰边界**——可拆（与 解析选择语句 的跨层中断不同·本函数无跨层 中断 语义——262 循环的 中断 限于自身块内）。
**2. 子函数划分方案（五子函数）**：①`解析类继承跳过`(记号,位置)->位置——262-269 继承/接口列表显式诊断+跳过；②`解析类可见性标签`(记号,位置,段可见性)->(新可见性,新位置)——283-289；③`解析类方法成员`(池,记号,位置,段可见性,类键基,...)->(新位置,是否消耗)——300-392 方法/构造/不安全析构形态（最大块·内部可再按 414-a 析构形态/普通函数二次切分·实施轮定）；④`解析类字段成员`(池,记号,位置,段可见性,字段偏移&,最大对齐&,最大字段&,...)->(新位置,是否消耗)——397-416 字段+布局累加；⑤`解析类布局收口`(字段偏移,最大对齐,池,...)->位置——422-433 尾部对齐+登记校验。
**3. 状态传递设计**：位置=每子函数返回值（值传递）；池/符号表=引用参数（既有惯例）；布局三元组=引用参数（`整64& 字段偏移` 三连·对齐 登记函数参数们 的引用惯例）；段可见性=值输入；主循环的 保底推进（420 前位置防死循环）保留在主函数（防死循环守卫不入子函数=职责单一）。
**4. 等价验证策略**：①新旧 v2p 编译同一改前输入 asm 逐字节 md5（探针=类/接口/继承/构造析构/运算符重载成员全形态用例 20/21/22/23/95/100/108/333+全树双对拍·518/530 口径）；②反证=变异布局累加行→产物变→恢复复绿；③全量门禁（解析类用例族 20/21/22/23/28/95/100~108/333 全在 E2E 覆盖）。
**5. 实施轮预估**：方案已定稿·实施=行号切片+签名落地+对拍反证+门禁（约 1 轮·同 532 周期）·候选=下一会话首选拾取。
**6. 交叠**：解析类.cn 零占用（329=词法/主/货舱·513~517=IR 生成层）·与全部在飞零交叠。

### 2.123 F2-33 迭代器侦察定稿轮（**T93 宿主泛型实例指针合成缺陷立案**+迭代器设计定稿·零入库代码）——548-a 侦察轮（分支 `任务/深度机-548-迭代器`·基点=develop 9901fa47·wt548·2026-09-21）

### 2.121 D12 v2 词法预处理对齐（**指令四件+条件栈嵌套+裁剪模型+主.cn 三挂点+CLI -D 通道**＝规范 4.7 v2 侧补齐·24_preprocessor 升级双编译）——546-a 开发轮（分支 `任务/深度机-546-d12preproc`·基点=develop 9ab6cca0·2026-09-21）

**1. 目标与验收判据**：v2 自举树补齐规范 4.7 词法预处理（宿主 preprocessor.cpp 210 行同构移植·裁剪模型=词法层空白替换保换行〔行号一致〕）：①指令四件=`#定义 宏名`（无值旗标）／`#如果定义(宏名)` 与 `#如果定义 宏名` 等价／`#否则`／`#结束如果`（严格配对）；②条件栈 {父活/分支已取/活} 支持嵌套+父层不活跃时内层 #定义 不生效（C 语义）；③被裁剪分支不产生任何 token（内含语法错误不诊断）；④诊断三态=未知指令/缺宏名/未闭合（编译中止·对齐宿主+词法错误计数全局态先例）；⑤CLI `-D 宏名` 注入通道（全局态模式·三处 分词 调用链零签名变更）；⑥**验收**=24_preprocessor 升级双编译对照 PASS（v2 产物运行输出=expected 7 行）+v2 全树源码裁剪幂等（零 # 行·v2p 自举链双 PASS 不变）+反证双态（撤裁剪挂点→24 预期失败）+全量门禁零新增红。
**2. 写集（精确清单）**：CN语言编译器v2/词法/预处理.cn（**新文件**·函数族 ≤400 行）｜CN语言编译器v2/主.cn（三处挂点各 +1 行·-D 参数解析段 +15 行左右）｜tests/e2e/24_preprocessor/双编译对照.txt（**新**·退出码=0/源文件=条件编译.cn/借链=否）｜tests/e2e/coverage_map.md（4.7 行 v2 列登记）｜文档六件套（021 D12 行/025 回填/交接/更新日志/看板清行）。
**3. 实施步骤**：①词法/预处理.cn 函数族（字节级逐行扫描+指令判定〔# 起始+中文关键字字节比较〕+条件栈 向量<条件帧>+宏集合 映射<整64,布尔>〔驻留 ID 键〕+提取宏名〔括号/空白双形态〕+裁剪主入口）；②主.cn 三挂点（主文件/包.cn/内置回退源 分词 前 `源码 = 预处理裁剪(源码);`）；③主.cn -D 参数解析（argv 扫描 `-D` → 设注入宏 全局态）；④24 升级双编译+coverage_map；⑤反证双态；⑥全量门禁+自举链。
**4. 验证链**：24 双编译 PASS（单跑）→ 反证（挂点改直通→24 双编译 v2 侧失败/输出错→恢复）→ 构建零警告 → 单测全量 → E2E 全量 --jobs 8（已知互踩假红单跑复验口径）→ strict/matrix → 自举链（宿主建 v2p+v2p 编 v2 全树 fix_p 双 PASS=预处理代码自身过自举）→ 探针（-D 手动命令验证注入通道）。
**5. 边缘情况（预判）**：①指令行判定=行首允许前导空白+# 起始（对齐宿主 181 行口径）——字符串字面量内的 # 不在行首不触发（宿主同款边界）；②CRLF 源（\r 在宏名终止集）；③`#如果定义(宏名)` 括号闭合即止=尾随内容忽略（宿主同构）；④裁剪后源码参与正常分词——#指令行本身被消费不产 token（指令行输出=空行保换行）；⑤未知 # 指令（如 #包含）=诊断拒绝（宿主同款·336_未闭合条件编译拒绝 宿主列已有·v2 侧随本轮对齐）；⑥条件栈未闭合=报错位置在文件末（宿主同款）。
**6. 与他机交叠**：写集=v2 词法新文件+主.cn+tests/24——家机 543=stdlib/正则 零交叠；537-definit=v2/语义 零交叠；536=v2 生成声明+宿主 semantic 零交叠；538=src linux_x64 零交叠（本轮回签面·只读其分支）。**判定=独立**。

**【收工回填（实测·546-a）】**
**1'. 验收（达成）**：①24_preprocessor 升级双编译对照 **PASS**（断言输出=是·运行级逐行全等·v2p 编译→as→g++→运行全链）；②-D 注入双态实测（无 -D→200/#否则 分支；`-D 命令注入`→100/#如果定义 命中）；③反证双态（主.cn 挂点注释→24 v2 侧词法+语法双错误红→恢复复绿）；④诊断负路径（未知指令 #包含/未闭合 #如果定义→[预处理] 诊断行+聚合守卫中止不产 asm）；⑤336_未闭合条件编译拒绝 v2 闭环负测上架 PASS（退出码=负）；⑥构建零警告+单测 1394/1394+strict/matrix PASS；⑦**78_v2/79_v2 自举链 PASS**（fix_p 全树 30s·v2p 编译预处理代码自身过自举）。
**2'. 写集（最终）**：CN语言编译器v2/词法/预处理.cn（新·270 行：指令四件+条件栈+快路径+裁剪主入口）｜CN语言编译器v2/词法/包.cn（+1 行 模块 登记）｜CN语言编译器v2/主.cn（三挂点+-D 扫描函数+主函数调用+聚合守卫·四处）｜tests/e2e/24_preprocessor/双编译对照.txt（新）｜tests/e2e/336_未闭合条件编译拒绝/v2闭环.txt（新）｜coverage_map（4.7 行负例列补 336_v2 注记）｜文档六件套。**零 src/ 宿主改动**。
**3'. 实施增量（预判外当场修·三枚）**：①目录包机制三踩——子文件须包根 模块 登记（文件名=模块名隐式绑定·无需 文件内 模块 声明）+跨模块符号须 `公开:` 段（532-a 生成表达式求值.cn 先例同构）——首编「未声明的标识符」三查后定位；②**★预处理逐行字符串拼接=arena O(n²) 中间串留存**——v2p 编 v2 全树在 4GB RLIMIT 下 std::bad_alloc（runner fix_p 卡死同源）→**快路径根治**：源码含预处理指令(源码) 零分配字节级扫描·无指令文件 `返回 字符串复制(源码)` O(n) 直通（安全区检查器拦截借用返回=防线正面实证）——修复后 4GB 限全树 rc=0·教训入册；③456 终审三连对照实验证实 537-a 引入（见看板 6bda3558·非本轮写集）。
**4'. 验证链（全绿口径）**：单测 **1394/1394**+E2E 全量 **522=516/4/2→修复后 78/79 转绿·终态=517 过/456（537-a 既有欠账·547 根治）+4 互踩假红（单跑 4/4 PASS）/2 跳**+strict 179/179+matrix PASS+自举链 78/79 双 PASS+4GB RLIMIT 复验 PASS。
**5'. 边缘情况（回填）**：预判①~⑥全部命中并处理；预判外=内存膨胀（③·arena 教训）与包机制三踩（①）。诚实边界：命令行 -D 面按 24 用例头口径=手动命令验证（探针双态实测）未入 runner 自动门禁；`#如果定义(宏名) 尾随内容忽略` 宿主同构未负测锚定。
**6'. 与他机交叠（回填）**：全程零冲突；541-revert（3d05b703）与本轮主.cn 挂点 rebase 自动合并成功。

### 2.122 456 根治轮（**537-a 布局收口拆分回归修复**＝`类体起点` 池.大小()+1 静默行为变更回退·反证双态实锤）——547-a 开发轮（分支 `任务/深度机-547-456布局修复`·基点=546 头 72f77934 接力·2026-09-21）

**【收工回填（实测·547-a）】**
**1'. 根因与修复**：537-a（baf7b2c4）拆分混入唯一行为变更——`整64 类体起点 = 池.大小();` 被改为 `池.大小() + 1;`（无说明·违反纯机械声明）——节点_类.子起点+1 错位→泛型实例布局登记/查询键错→类型大小查表 miss 返 0（456 产物 op1 常量 8→0）→rc=14。**修复=回退该行至原版语义**（diff 全量复核=其余均为机械签名/调用迁移）。**反证双态**：+1 在=456 单跑稳定红 rc=14；回退后 456 单跑 **PASS**。
**2'. 验证链（全绿）**：构建零警告+单测 **1394/1394**+E2E 全量 **523=517/4/2**（**456 转绿**·余 4 红=210_v2/457/458/459 互踩假红族·单跑 4/4 PASS 复验）+strict 100%+matrix 83/83。
**3'. 写集**：CN语言编译器v2/语法/解析类.cn（1 行）+文档六件套。**教训**：机械拆分提交 diff 中任何非签名/非搬移的行为行变更=红旗须逐行说明；「md5 双对拍」对**含探针/登记类副作用路径**的输入面覆盖不足（456 泛型实例形态未进对拍样本）——补=拆分轮对拍样本须含泛型类声明形态。
**4'. 交叠**：接力 546 分支（含其全部写集）——回签一次覆盖两轮。

**【收工回填（实测·548-a）】**：①探针双侧分裂——迭代器最小形态（泛型类 向量迭代器{向量<T>* 源;游标}+绑定/有下一个/下一个+当循环）宿主 cn check 红「无法将 '向量<整64>*' 隐式转换为 '向量$整64*'」＝**T93 立案**（$ 合成名与 * 组合吃星号·src 类型系统合成名规范化缺口）；**v2p 同源 rc=0**（103 节点）→新用例走 v2 闭环形态（457 先例·头注宿主欠账）。②设计定稿=stdlib/迭代器.cn 泛型类 向量迭代器<T>{源指针+游标+绑定/有下一个/下一个}·525_v2_向量迭代器（借链=是）·映射集合迭代器=波2。③**549 接力计划**：宿主 T93 修复（src 类型系统独立轮）+525 上架+修后升双编译=消费者反证。④写集=零代码。⑤交叠零。

### 2.125 T95 修复轮（**泛型方法返回 T 推断缺口**＝成员访问基类型实例物化单点·编译面根治/运行面合流 T94）——551-a 修复轮（分支 `任务/深度机-551-t95泛型返回`·基点=develop 0cc770fb·wt551·2026-09-21）

**【收工回填（实测·551-a）】**：①复现精化——p2 探针编译双错：「类型 \'向量<整64>\' 不是结构体/联合体/类类型，无法访问成员 \'元素\'」（根因）＋「无法将 \'未知\' 隐式转换为返回类型」（连带）——**根因=泛型方法体引用参数（向量<T>& v）类型文本经 substTypeParam 替换保留尖括号形态（向量<整64>）未经实例物化·findClass miss**。②修复单点=semantic_expr_access.cpp 成员访问 findClass miss 前 resolveGenericTypeName 物化兜底（幂等：非泛型/已物化/命中路径零变化）——p2 check 由红转绿 ✓。③**运行面 SEGFAULT=与 T94 阶段二同根合流**（引用参数句柄链 this 双解引用——v 槽→表槽→堆对象 少一层）——编译面/运行面分案：编译面本轮根治·运行面随 T94 阶段二裁决后一并修。④门禁=单测 1394/1394+E2E 522=515/5/2（456=基点既有〔547 挂等〕+210_v2/457/458/459 互踩族）+strict/matrix 绿·零回归。⑤写集=semantic_expr_access.cpp 1 处+文档。⑥交叠零。

### 2.130 strict 双红根治轮（**050 登记复登记+601 裁决甲连带簿记**＝验收机当场修·机制级）——603-a 修复轮（分支 `任务/深度机-603-strictfix`·基点=develop cd9a1bdf·wt603·2026-09-22）

**1. 目标与验收判据**：develop 头 strict 门禁双红根治——①孤儿 050_双编译对照取地址类变量（592-a 验收机补登记 ec4c2064 被 591-a「净差异版」集成再丢失·登记丢失族第二例）；②映射有效性错误 9 个（601 裁决甲删 9 例 v1 化石用例 70~76/77/89 未清 coverage_map：[2.1] 正例→89/[附录A] 正例→77_audit_dump 两引用+70~76 七行豁免）。**验收**=`check_spec_coverage.py --strict` rc=0（178/178·指针有效+零孤儿）+matrix 83/83 不回归。
**2. 写集（预登记）**：`tests/e2e/coverage_map.md`（050 行复登记+9 处 601 连带清理）｜`scripts/check_spec_coverage.py`（EXCLUDED_CELLS 增 ("2.1","正例")·573 裁决甲连带——穷举正例 89 已随甲下线·v2 等价补位后应移除）｜plans/021（戳刷 603）｜plans/025 本节｜看板行（窄通道）。零 src/零 .cn。
**3. 实施步骤**：①验收 E2E 前置 strict 发现双红→归因（-S 追踪增删提交）→②看板先行直推 603 行+549/551 过时挂等行清偿（服务轮核实均已是 develop 祖先）→③coverage_map 四处编辑+脚本排除口径→④strict 树上复验转绿→⑤integrate（写集触 tests/**→全量门禁跑合并结果）→⑥看板通告段广播+交接本机节。
**4. 验证链**：strict rc=0（178/178）＋matrix 83/83＋integrate 全量门禁（零警告构建+单测 1383+E2E 全量 526 系·合并结果）。
**5. 边缘情况与诚实边界（预判）**：①[2.1] 正例格空缺=573 裁决甲的 sanction 后果（呈报乙「迁移 v2 等价」未被采纳）——排除口径注释已注明「v2 等价补位后应移除本条」，非静默豁免；②050 登记行连续两次丢失说明「净差异版/squash 集成对登记区行有系统性丢失风险」——integrate.py 581 增量判定缺陷备案同域，本轮不动底座、只随证据再备案；③601 收官销账若家机同步清理同区将产生覆盖_map 冲突——integrate rebase 解冲突取并集。
**6. 与他机交叠**：家机 601（已集成 e1935ae4·本修复=其裁决甲落地连带簿记·语义同向非冲突）；家机 595/601 在飞其他链（零写集交叠）；单位机 596（arm64 codegen·零交叠）；acceptance E2E 由本会话 wtv598 独立树并行承载（gate_lock 串行）。
### 2.129 D42甲 波3 终章接力（**撤守卫收口=stdlib 链接期供给→源码加载切换落地**·001 任务最后一步·家机 578 同计划深度机接力）——585-a 开发轮（分支 `任务/深度机-585-撤守卫收口`·基点=develop 7ad6343a·wt585 隔离·2026-09-21·用户令「立即全量完成 001」）

**1. 目标与验收判据**：D42甲 波3（plans/021 §三 001 行）最后一步——撤 `是标准库模块` 守卫（主.cn:64 名单），v2p 对 stdlib 10 模块改走源码级加载（候选4 通道已通·517 链 T70 三层修复已入 develop），链接期供给模型退役。**验收**=①撤守卫态 linux 全量 E2E（--jobs 8）零新增红+关键探针 126/127/390/228/441/442 全 rc=0；②自举链 78/79 双 PASS（撤守卫态=v2 编译 v2 树自身 stdlib 也源码级）；③借链面 51 例（借链=是·实测清单）转自洽逐例绿；④反证双态（守卫复活→390 链接失败复现→再撤→复绿）；⑤守卫落地形态=名单函数删除+消费点死路清理（最小死代码·非恒假 TEMP）。win/arm64 侧=协议 v3 集成后异步验收（575 先例）。
**2. 写集（预登记）**：`CN语言编译器v2/主.cn`（守卫名单/消费点）｜`CN语言编译器v2/IR/IR布局.cn` 等 `是标准库模块名` 消费点（按实测定性——生产态 stdlib 源码在库后其判定恒假路径的清理范围）｜`tests/e2e/*/v2闭环.txt|双编译对照.txt` 51 例（借链=是→否·AGENTS §4 测试升级随轮报备）｜`tests/e2e/coverage_map.md`（若有新形态）｜plans/021（001 行波3 收口+戳）｜plans/025 本节｜交接/更新日志本机节｜看板行。零 src/（宿主零改动预期——撤守卫纯 v2 树行为）。
**3. 实施步骤**：①基线冒烟（v2p 构建+关键探针·develop 现态含 517 链/574/575 全集）→②TEMP 撤守卫（名单恒假）+撤守卫态关键探针矩阵→③撤守卫态 linux 全量 E2E --jobs 8（暴露面逐例定性：A 期望漂移迁移+反证/B v2 真缺口立案/C 噪音重跑）→④守卫落地形态定案（名单函数删除+消费点清理·逐消费点实测定性）→⑤借链 51 例转自洽+逐例验证→⑥生产态全量门禁（gate_lock）+自举链 78/79+strict/matrix→⑦反证双态→⑧六件套+integrate（win/arm64 集成后异步验收广播）。
**4. 验证链**：v2p 构建+探针矩阵→撤守卫态全量 E2E --jobs 8→生产态全量门禁（零警告构建+单测 1394+E2E 全量+78/79+strict 179/matrix 83）→反证双态。
**5. 边缘情况与诚实边界（预判）**：①撤守卫态 linux 新暴露面（517 链后 develop 前进了 574/575 两轮 v2 语义/语法改动——撤守卫交互未实测）；②借链 51 例转自洽后链接面符号重复消解实测；③575 新增 4 拒绝面（422/299/81/417）在 stdlib 源码级编译下的交互（stdlib 源码自身须过新检查）；④自举链 78/79 撤守卫态=v2p 编译 v2 全树（460-a 时 fix_p 曾 rc=1·517 链已通·现态复验）；⑤strict 四孤儿已随 576/577 集成消解（develop 现态 strict 179/179 绿·无豁免需要）。
**6. 与他机交叠**：家机 578（同计划 wt578 在飞·**本分支=深度机接力·两家各按本机平台验收·集成取先者·后集者 rebase 自然去重**）——写集同源（主.cn 守卫位+tests 借链面）语义同向，非冲突；家机 579/581（T100 tail/协议v3·零交叠）；深度机 565（v2 树 4 文件·零文件交叠实测）；单位机 arm64（集成后 pull 自然验证）。本分支写集触 v2 树+tests→全量门禁。

**【585-a 收工回填（撤守卫态暴露面四根因二留二回·负测锚七例适配·残余定性）】**：
**1'. 实施实录（基线 develop 7ad6343a→TEMP 撤守卫→探针矩阵 4/6→全量暴露面 21 红→逐根修复→净正收敛）**：
- **暴露面首跑**：撤守卫态（主.cn:64 守卫恒假 TEMP）全量 E2E 528=505/21/2（守卫态基线 526/0/2）——21 红三类：运行错值/崩 14+负测锚行漂移 6+自举 1。
- **修复①（留）**`IR容器.cn 签名返回字节数`：结果空 12→16（268-a 口径漏同步——错误槽@8+宽 8；retbuf 帧预留差 8B→被调 sret 写踩相邻槽）。asm 铁证=追加 sret retbuf lea [rbp-136] 写 16B 覆盖 [rbp-128]。
- **修复②（留）**`生成类.cn 生成类对象局部`：补 IR_分配（帧预留以 IR_分配 声明性指令计数——漏发=槽不进帧→与 retbuf 区重叠→441 A3「新表」句柄被清零→拷贝构造读空。gdb=[rsi]=0 铁证）。同族扫描=全树唯一漏点（标量/引用/结果/数组/结构体局部均有）。
- **修复③（回滚）**`生成表达式字段 成员载荷是结构体` 容器排除：修 441 系（结构体容器字段压地址→load）但破坏 228（类对象.容器字段 链的「字段地址即 this」内联语义）——分层实验两难实锤后回滚。
- **修复④（回滚）**容器参数句柄语义三站点（实参结构登记×2+序言拷贝字节）：修 113 this=0 但破 127/497（517-a 容器元素内联存储依赖结构体按值通道）——回滚。
- **负测锚行适配（留·A 类期望漂移）**：197/223/224/227/229/464/283 七例 主.expected「已解析…主.cn 节点=N」去节点数尾巴（撤守卫态=全树合并节点数含 stdlib·单模块锚必 miss；路径锚保留·runner v2src 适配仍在·生产态前缀匹配兼容）——6 例转绿，283 另有运行面残余。
**2'. 本质定性（残余红收敛为架构级双模语义）**：撤守卫态「容器实例」**内联/句柄双模并存**——元素/字段存储=内联整对象（517-a·宿主对齐），局部变量/参数=句柄 8B（生产态 NewObject 模型）。T70 链（505~517）按内联侧逐例打补丁，585-a 按「容器=类实例化」让布局/类表命中后，**双模判定散点全面显形**（字段载荷 46-a、参数 ABI、方法返回物化……）——**非单点 bug，是语义模型收敛问题**：收敛方向二选一（全内联对齐宿主／全句柄）=架构级大轮（T70 链同量级），505~517+585 两轮教训=打地鼠不可持续。
**3'. 验证（回填·终测实测）**：①生产态零回归（修复①②+expected 七例·守卫恢复态）=零警告构建+单测 **1394/1394**+全量 E2E **528=526/0/2 零失败**+strict **179/179 rc=0（100%）**+matrix **83/83**（gate_lock 串行·与 develop 基线逐项一致）；②撤守卫态终测 **528=513/13/2**（首跑 21→13·净绿 8=441/442/147/127/497/197/223/224/227/229/464 增量减 228 波动）。反证=修复前后红名单对照（git b1b9eee2 历史+本段实录·441 探针 rc=139→0 铁证链）。
**4'. 残余清单（定性登记·001-001 承载·下轮接力·收敛方向已裁决=方案甲全内联〔用户 2026-09-22·与 Rust Vec 布局/宿主同构〕）**：终测 13 红=容器所有权/类支持族 11（113/218/219/221/228/234/237/238/240/283/285——双模收敛同族）+67（泛型返回·T70 面⑤' 残余）+79（自举 fix_s——撤守卫自举）。观测注记=228/67 存在构建间布局漂移不稳定红（驻留序/哈希跨构建漂移·爆发点漂移·047 单点化后自然消解）。
**5'. 诚实边界**：①撤守卫落地（守卫删除=生产态切换）本轮未做——前置=撤守卫态零红，残余族清偿后下轮执行；②借链 51 例转自洽未做（同前置）；③win/arm64 侧=协议 v3 集成后异步验收。

### 2.83 D46（**v2 结果构造名裸名编译期拒绝**＝`返回 正常;`/`返回 错误;` 语义层防线）——474-a 开发轮（分支 `任务/深度机-474-d46naked`·基点=develop aef637b6·wt474 隔离·2026-09-20）

### 2.127 T33 残余根治（**v2 窄宽值规范化四站点＋T36 v2 面字面量值域检查**＝宿主/v2 双编译分叉实锤矩阵）——565 开发轮（分支 `任务/深度机-565-t33窄宽回绕`·基点=develop 715f4e8a·wt565 隔离·2026-09-21）

**0. 立案证据（探针矩阵·宿主=target/cn vs v2=wt562 v2p·2026-09-21 全部实测）**：A 族（窄宽值入槽不规范化→静默错值）：op6 `整8 丙=整8(100)+整8(100)` 宿主 **-56**/v2 **200**；op7 `正16 60000+10000` 宿主 **4464**/v2 **70000**；b5 复合赋值 `甲+=整16(30000)` 宿主 **-5536**/v2 **60000**；b1 传参 `取(甲+乙)`（形参 整16）宿主 **-5536**/v2 **60000**；b2 返回位（->整16）宿主 **-5536**/v2 **60000**；h1 `整32 甲=4000000000` 宿主 **-294967296**/v2 **4000000000**；h5 `整32=6000000000` 宿主 **1705032704**/v2 **6000000000**。B 族（漏拒·宿主拒/v2 放行）：g1 `整16 甲=40000`、g2 赋值位 `甲=40000`、g4 `甲=0+40000`、g5 `正8=300`、g9 `整8=-200`、g10 `正8=-1`、h6 `正32=5000000000` 宿主全拒·v2 全放行。**已正确面（反证不误伤清单）**：显式强转（p3 六形态 ✓）、数组元素（b4 ✓）、结构体字段（c3 ✓·须显式构造器）、a1 变量窄化拒（双侧一致 ✓）、w32/正32 位与回绕 ✓、h2 整64 大字面量 ✓、f1/f2 整32 传参/返回 ✓、h7 整16=-32768 ✓。

**1. 目标与验收判据**：v2 对窄宽（整8/正8/整16/正16/整32）值汇（变量槽/形参槽/返回位）的写入必须规范化到目标宽度（回绕+符号扩展·Rust i16 wrapping 赋值同款语义），字面量对窄宽目标的隐式转换须值域检查（宿主 T36·319-a 同口径）。**验收**=①探针矩阵 A 族七例双侧逐行一致（v2 转绿）；②B 族七例 v2p 编译期拒绝（诊断含「无法将 '整32' 隐式转换为」同宿主措辞形态）；③新 E2E **502_窄宽回绕矩阵**（双编译对照·宿主=v2 逐行全等）+**503_窄宽字面量域外拒绝**（v2 负测·v2p 拒通道）上架单跑 PASS；④全量门禁零新增红+自举链 78/79 PASS+strict/matrix；⑤反证双态（撤 A 规范化→502 复现分叉→恢复复绿）。

**2. 写集（精确文件清单·预登记）**：`CN语言编译器v2/IR/生成声明.cn`（+窄宽值规范化 helper·:232 调用点换接）｜`CN语言编译器v2/IR/生成语句赋值.cn`（:121 调用点换接）｜`CN语言编译器v2/IR/生成函数.cn`（:163 区 返回窄宽码登记+形参槽入口规范化发射）｜`CN语言编译器v2/IR/IR容器.cn`（函数IR +返回窄宽码字段）｜`CN语言编译器v2/IR/生成语句.cn`（生成返回语句 标量返回规范化）｜`CN语言编译器v2/语义/语义检查表达式.cn`（隐式转换合法 字面量分支值域检查+负号字面量形态）｜`tests/e2e/502_窄宽回绕矩阵/`（新·双编译）｜`tests/e2e/503_窄宽字面量域外拒绝/`（新·v2 负测）｜`tests/e2e/coverage_map.md`（两例登记）｜文档六件套+看板。**零 src/**（宿主已正确=参照实现）。

**3. 实施步骤**：①A=生成声明.cn 增 窄宽值规范化（整8/整16/正8/正16→IR_类型转换 窄码〔窄读+符号/零扩展·与显式强转同机制三后端既有〕；整32→T_整32 转换〔movsxd 符号回绕〕；正32→既有 正32存储截断 位与零改；已规范寄存器=零开销直通），:232/:121 两调用点换接；②④=生成函数 形参登记循环后对窄宽标量形参发入口规范化三指令（槽加载→转换→槽回存·方法体同 routine 覆盖）；③⑤=生成返回语句 标量返回路径按 函数信息.返回窄宽码（新增字段·:163 区从返回类型文本登记）规范化；④B=隐式转换合法 整数字面量分支补 值域表（整8 [-128,127]/正8 [0,255]/整16 [-32768,32767]/正16 [0,65535]/正32 [0,4294967295]·整32/整64/正64 不检=h1/h5 宿主实测定案）+节点_负号+字面量形态（g9/g10）；⑤502/503 上架+coverage_map；⑥全量门禁+反证。

**4. 验证链（命令级）**：探针矩阵复验（/tmp/t33probe m/*.cn 全表 双侧逐行比对）→502/503 单跑→零警告构建→单测→全量 E2E `--jobs 8`（互踩族串行复验口径）→strict/matrix→自举链 78/79→`check_fn_length`（新函数 ≤100 行）→反证双态。

**5. 边缘情况与诚实边界（开工预判）**：①**B2 常量表达式形态**（`整16 甲=100-200` 宿主拒/v2 放行）＝v2 语义检查「检不出放行」**既有诚实边界**（推导源类型ID静默 不覆盖二元表达式类型推导）——本轮**不关**（表达式类型推导=独立能力缺口·立案 G 区 T98·非本轮半截）；②显式强转产物寄存器（窄码）经规范化入口=零开销直通（已规范守卫）→194 等显式转换用例零变化；③整32 回绕面=宿主「任意字面量放行+运行回绕」实测定案（h1/h5）——**不设值域检查**（与 整16 差异如实注记代码）；④正64 打印符号性（h3 宿主 2^63/v2 -2^63）=打印/字面量折叠面**独立缺陷另立案**（非存储面·本轮不触）；⑤形参规范化=被调方入口（覆盖全部调用点）；引用/指针/结构体形参不触。

**6. 与他机交叠分析**：写集=v2 树 5 文件（IR 生成层+语义）——家机在飞（D1 拆分=宿主 src/552 phi_lower/556/561/563/564 均不触 v2 树）、单位机 558/557/559（src/tests）**零文件交叠**；语义耦合=B 族为 v2 拒通道收紧（负路径）·正路径零变化面由 502 双编译锚定；A 族变更 v2 产物指令流（窄宽用例多 1~2 指令）——**影响面=v2 产物三平台**，集成后请家机/单位机按通告复验 194/385/386 等窄宽既有用例（本机全量先行）。
**【收工回填（实测）·565】**
**1\'. 验收（达成·含开工预判外当场根治两枚）**：①探针矩阵 A 族全绿——op6=-56/op7=4464/b1=-5536/b2=-5536/b5=-5536/h1=-294967296/h5=1705032704/h13=-294967296/h14/h15=-5536（全部与宿主逐值一致）；②B 族六形态 v2p 全部 sem5 编译期拒绝（g1/g2/g5/g9/g10/h6）·域内 g3/g6/g8/h7 零误伤；③502 双编译 PASS+503 负测 PASS（E2E runner 实跑）；④全量门禁（数字见提交信息）+194/130/05 回归全绿；⑤反证双态=修复前 v2p（develop+562）跑 502 同源输出 200/60000/70000/…/4000000000（分叉实锤·修后=期望值）。
**2\'. 写集（最终）**：CN语言编译器v2/IR/生成声明.cn（窄宽码自文本+窄宽值规范化 helper·声明站点+指针层守卫）｜CN语言编译器v2/IR/生成语句赋值.cn（赋值站点换接+复合脱糖标识符块·查指针变量类型 守卫）｜CN语言编译器v2/IR/生成表达式.cn（字面量适配整32 改值域判定）｜CN语言编译器v2/IR/生成函数.cn（返回窄宽码 登记+形参入口规范化）｜CN语言编译器v2/IR/IR容器.cn（函数IR +返回窄宽码）｜CN语言编译器v2/IR/生成语句.cn（返回位规范化）｜CN语言编译器v2/语义/语义检查表达式.cn（整字面量入域+整数字面量文本值·隐式转换合法 两分支挂接）｜tests/e2e/502_窄宽回绕矩阵/503_窄宽字面量域外拒绝+coverage_map。
**3\'. 实施增量（预判外当场修·三枚）**：①**指针声明落穿**——指针/引用声明在 生成变量声明 分派不早退·落穿 生成标量声明初值 且类型文本=基类型：站点①加 类型节点.附加==0 守卫·站点②/成员块加 查指针变量类型<0 守卫（**教训**：首版无守卫→194/130/05 全量 E2E 段错误·全量门禁当场拦截=网底价值再证；**既有着火点**：正32存储截断 对 正32* 声明同面为 46-f 起既有隐患·本轮守卫顺带消除）；②**查指针变量类型 未命中哨兵=-1 非 0**——首版 ==0 守卫误伤全部非指针赋值（b5/h13 回退实锤）·改 <0；③**复合赋值脱糖节点走成员路径**——文本=0 的合成 节点_赋值 分派进 生成成员解引用赋值·标识符左值在此补规范化（最初误判=纯赋值路径覆盖）。
**4\'. 边缘（回填）**：预判①成立（B2 常量表达式=T98 立案·g4 运行值已正确）；预判②成立（194 显式转换用例零回归·已规范直通守卫有效）；预判③成立（整32 值域不检=宿主 h1/h5 定案）；预判④成立（正64 打印符号性=T100 立案）；预判⑤成立（形参规范化被调方单点·194 读窄(正8*) 零回归）。**新发现**：v2 传参面隐式转换整体缺检=T99 立案（g11 实锤·运行值已由形参入口规范化纠正）。
**5\'. 交叠（回填）**：零冲突（写集=v2 树+tests 502/503·他机在飞不触）。

> 〔2.83 D46（**v2 结果构造名裸名编译期拒绝**＝返回位/声明位/赋值位三段·474/475/476-a） 已滚转归档（深度机 565 轮·2026-09-21·切片=plans025-深度机节-2026-09.md）〕
> 〔2.84 D46 残余收口（**构造名裸名全位拒绝**＝语义检查表达式.cn 59 行放行改拒绝） 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕
> 〔2.82 D10 v2 面 已滚转归档（深度机 546-a 棘轮·2026-09-21·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.1 收工回填（**314-a 🧪 M3 采样轮·x64l 列**·基线 a33e3aa〔=src 与 312-a 基线 ce8089c 等价〕·2026-09-17 收工）：**双域零命中+T44 复验+★T45 立案** 已滚转归档（深度机 470-a 集成会话·2026-09-20·内容 git 可考）〕

**【收工回填（实测）·521-a】**
**1\'. 定性结论（三选一取「撤销登记」）**：①探针 6 形态（下标目标=取点(10)/对表[1]=取点(20)·标识符目标=取点(30)·下标=复制(源)·结果构造=正常(50)·对照健康面）——宿主与 v2p 编译运行输出 **10 行逐行全同、rc 同 0**；②asm 铁证=「下标目标=结构体返回调用」发射 **op=61 整体赋值通道**（`mov rcx,24; rep movsb` 24 字节）而非 StorePtr 8 字节截断——234-a 时期的「emitStructWholeAssign 无源分派」现状已被后续接力链（468 九链/435/441/477 等）覆盖；③全库存量扫描=显式「下标目标=函数调用」形态 E2E/v2 树/stdlib ≈0（零覆盖=既有事实）。
**2\'. 写集（最终）**：plans/021（D11 迁 §4.2+§五 521-a+戳）｜plans/025 本节｜交接/更新日志｜看板（清行）。零 src/v2 树/tests 代码（纯侦查）。
**3\'. 边缘（回填）**：①成员链目标（`外.内 = 取点()`）依赖类内结构体字段=T80 面（513 链挂等回签）——不构成 D11 独立缺口·已注记；②**附带发现（登记）**：宿主对「下标结构体字符串字段」类型推断降级「未知」（`打印(点表[2].名)` 报「参数类型不支持：未知」·`字符串 名=点表[2].名` 同病·84-d 同族候选）——探针以字符串比较法绕过；③探针可作 E2E 正测转正候选（下一 tests 轮搭车）。
**4\'. 交叠（回填）**：517 链（生成语句赋值.cn）零冲突（本轮只读）；全程零文件碰撞。

**【收工回填（实测）·523-a】**
**1\'. 验收（达成）**：①md5 双对拍=探针 3982d9f7+全树 111038 节点 c4106a2c（新旧 v2p 编译同一改前文本）✓；②行为级反证=查ac 恒等变异→v2p_mut 编译 456_泛型指针字段下标 产物 761102ae（变）→恢复→82fc1dc0 复绿 ✓——**克隆修针消费面=泛型实例化（v2p 编译用户程序）·自举编译（v2p 编译 v2 树自身）不触发**（全树/探针对修针变异零敏感=三轮反证迭代定位）；③check_fn_length 61→60 ✓；④门禁=单测 1387/1387+E2E 522=517/3/2（3 假红=同编号互踩·串行单跑全 PASS）+strict 零孤儿+matrix 83/83 ✓。
**2\'. 写集（最终）**：CN语言编译器v2/IR/IR类.cn（+167/-134·分派器 14 行+十子函数+返回行补齐——拆分后子函数须显式 返回 n2c·v2 非空返回类型纪律）｜文档六件套。
**3\'. 边缘（回填）**：①**侦查两改方案均被否**——四槽位族分组（基于 grep 截断误读）与 else-if 重排（丢兜底先行回填=行为变更）→最终=保守分派器拆分+兜底不返回；②首轮切片漏 7 行（分支收尾 `}` 与下行行首 `} 否则` 分离）→花括号失衡编译期当场暴露·边界断言补齐重切；③返回行补齐=v2 语义要求。
**4\'. 交叠（回填）**：全程零冲突。

**【收工回填（实测）·524-a】**
**1\'. 数字（gcov 命令级可复现）**：ir_verify.cpp 行 86.58%（149 可行行）·分支执行 88.95%/走向取正 64.21%；ast_printer.cpp 行 68.78%（474 可行行）·分支取正 36.54%；全项目行 79.21%/分支 45.84%（134 编译单元）。**对比**：ir_verify 较 283-a 基线 85.59% **+0.99pp**；ast_printer 较 471-a 前 14.6% **+54.18pp**——471-a 家机语料轮 linux 侧实证有效。
**2\'. 边缘（回填）**：①总行覆盖 79.21%<基线 80.57%=**门禁语料步未跑的低估**（check_source_coverage 全链在 E2E 步被已知假红族中止 rc=1·语料步未执行·gcda 聚合完整）——非真劣化；②gcov 单文件聚合命令：`gcov -b <build-cov>/CMakeFiles/<lib>.dir/src/**/<file>.cpp.gcda`。
**3\'. 交叠**：零代码零 tests·全程零冲突。

### 2.135 001 接手复现轮（**linux 侧形态同构性验证**＝§1.45 交接包接手四步①·基线态判据＋写回态现场）——739-a 开发轮（分支 `任务/深度机-739-001接手复现`·基点=develop ac899e48·wt739 隔离·2026-09-25·738 用户裁决「深度机立即全盘接手」）

**1. 目标与验收判据**：按 §1.45 复现链在 linux-x86_64 侧完整重放——①基线态（不加写回）：v2p 构建＋cn_self 构建（v2p 编译 v2 全树→as→链接）＋cn_self 编译 t4 判「语义错误 4 个（甲/乙/加 未声明）」干净退出＋v2p 编 t4 运行 rc=0 输出 0；②写回态（语义分析.cn 表注册≈293 行后施加写回单件两行·§1.45 二.2 原文）：重建打补丁 v2p→编 t4 记录形态（rc/诊断/异常现场——win 实测=收集EXTERN符号+0x14ea RAX=8 崩·linux 无 VEH 用 gdb 抓）＋重建 cn_self→编 t4 判 rc=0 零诊断（t4→0）；③产出 **linux/win 形态同构性结论**（同构→接手四步②定位「8」场景；不同构→看板向家机索取 win 现场物＋按 linux 形态独立定位）。**验收**=三态实测记录逐条落盘本节回填＋同构性结论＋下一步建议。
**2. 写集（预登记）**：纯文档（plans/025 本节＋plans/021 §五 739 行＋文首戳＋交接/更新日志本机节＋看板清行）——**写回单件为实验性施加不入库**（收工撤销·t4/产物在 target/ gitignore 面）；复现中若撞出机制级缺陷当场修则另行登记写集。
**3. 实施步骤**：①工件就绪（主树 target/cn 有效拷贝〔develop src 零变更实证 737/738 纯文档〕＋audit2 rt objs/v2p 基线缓存复用核验）②基线三态实测③施加写回单件④重建打补丁 v2p→编 t4 记录⑤重建 cn_self→编 t4 记录（预期 rc=0 零诊断）⑥同构性结论＋回填。
**4. 验证链**：逐步 rc＋诊断原文记录；崩现场=gdb bt（linux 无 VEH·722-a 守卫实证 win-only）；ulimit -v 6GB 防失控（daemon 同款防线）；快速门禁（纯文档写集）＋check_handoff/check_progress_sync。
**5. 边缘情况与诚实边界（预判）**：①720-a 时 linux cn_self 第二跳=语义错 67223（与 win 崩溃链根本不同）——写回态 cn_self 可能仍 67223 而非 t4→0（linux 侧写回不生效或先撞语义墙·如实记录即本轮核心产出）；②linux 崩现场=core/gdb 形态与 win VEH 输出不同构（RVA 对位法不可用·改 gdb 符号对位）；③v2p 编 v2 全树内存风险→ulimit 6GB；④t4 产物运行判据（加(1,2)-3=0）顺带记录。
**6. 与他机交叠**：家机 738 已收工无在飞（701/705~708 行=挂等/保全态·写集零交叠）；单位机空闲；写回单件只存本机 wt739 工作树不入库=他机零冲突。独立 ✓。

**【739-a 收工回填（实测·同构性实锤）】**
**1'. 基线三态（linux-x86_64·wt739 target/p739/·ulimit 6GB 防线全程在位）**：
- ①a 基线 v2p（develop 缓存直用）编 t4：**rc=0**·v2asm.s 产出·as+g++ 链接 rc=0·运行 rc=0（加(1,2)-3=0 正确）；
- ①b 基线 fix_p：v2p 编 v2 全树 **rc=0·636472 行·40.4s**（720-a 基线 636179 行·差异=源码正常演进）；
- ①c 基线 cn_self（fix_p 汇编＋cn_self.o 前置+v2p.o 借链 -Wl,-z,muldefs）编 t4：**「[sem5] 语义错误 4 个，中止 IR 生成与代码生成（退出码 1）」（未声明标识符 甲/乙＋未声明函数/标识符 加·行=2/5）·无 asm 产出**＝**判据①与交接包逐字吻合**。
**2'. 写回态（语义分析.cn 表注册后插入写回单件两行·§1.45 二.2 原文）**：
- ②a 打补丁 v2p 编 t4：**段错误 rc=139（SIGSEGV）**＝**win C0000005 形态同构实锤**；
- **gdb 现场**：崩点符号 `_E698A0E5B08424E695B4363424E7ACA6E58FB724E8AEBEE7BDAE_J__TE7ACA6E58FB7`＝**映射$整64$符号$设置_J 克隆体**（与家机 721-a「映射$整64$符号$设置 克隆体 内+0x67B 崩」/722-a「设置 克隆体重哈希循环 mov [rax],rcx」**同一符号**）；rax=0xffffffffffffffff（-1·恰=732-a 链查找空桶守卫返回值域）；rsi=0x1a(26)=符号表规模量级；**#1 返回地址=0x27 垃圾＝栈帧被写穿**（与 win「偏移 8 被当地址解引用」栈污染形态同族）；
- ②b 打补丁 v2p 编 v2 全树：**rc=139@9.9s**（解析完成 126337 节点后语义阶段早期崩·同 gdb 崩点=同一 设置_J 克隆体）——**linux 侧打补丁 cn_self 无法构建＝判据②（t4→0）被第一代崩溃前置挡住**（诚实边界：非「写回无效」·win 侧同代亦崩＝两机一致）。
**3'. 同构性结论与下一步**：①linux/win 第一代（宿主发射）写回态均崩=**同构 ✓**；②linux gdb 直接符号对位（设置_J 克隆体）比 win RVA 对位（收集EXTERN符号+0x14ea·疑为调用链上游帧）更贴近真崩点；③**四步②定点修复可在 linux 本机直做**（无需向家机索取 win 现场物·gdb 符号/源码双对位条件齐备）——定位方向=「表注册 写回调用 设置 克隆体」的实参/栈帧布局（733/734 三侧配对矩阵结论可复用）；④740 轮=定位修复轮（基线重施加写回单件·/tmp/p739_语义分析_写回态.cn.bak 留档）。
**4'. 写集（最终）**：纯文档（025 本节＋021 §五 739 行+文首戳＋交接/更新日志本机节＋看板清行）——写回单件已撤销不入库（git checkout·备份 /tmp）·探针产物留 wt739 target/p739/（gitignore 面）。
**5'. 边缘（回填 vs 预判）**：预判①命中（linux 写回态非 t4→0 而是第一代崩溃前置·形态如实记录）；预判②命中（gdb 形态非 VEH·符号对位成功）；预判③未触发（ulimit 6GB 内全程稳定）；预判④部分（基线 t4 运行 rc=0·写回态不可达）。

### 2.134 integrate.py `--allow-known-red` 安全网失效修复（**运行() 默认继承 stdout ⇒ 点名清单恒空放行**＝门禁解析静默失效）——723-a 修复轮（分支 `任务/深度机-723-integrate红解析`·基点=develop ff3e638c·wt720 复用·2026-09-25）

**1. 目标与验收判据**：722 集成实测暴露——integrate.py 的「已知红点名」机制解析 `结果.stdout` 提取失败用例名，而 `运行()` 默认继承 stdout/stderr ⇒ `CompletedProcess.stdout` 恒为 None ⇒ 失败清单恒空 ⇒ `未点名`恒空 ⇒ **未点名红也被放行**（安全网形同虚设·722 集成日志「串行红 [] 全部命中 --allow-known-red 点名清单」实为解析空集的签名）。修复=新增 `运行捕获()`（capture+回放·保持可见性）并换接三处 E2E 门禁调用（win 串行／linux 并行／linux 串行）。**验收**=①缺陷复现（旧 运行() stdout=None → 解析空）；②修复后解析正确（模拟失败输出 → 用例名清单）；③集成走通（scripts/*.py 平台无关·快速门禁面）。
**2. 写集**：`scripts/integrate.py`（+`运行捕获()`·3 调用点换接）｜plans/021（§五 723-a 行+文首戳）｜plans/025 本节｜交接/更新日志本机节｜教训 1 条（权重 8）。零 src/tests。
**3. 实施步骤**：①根因定位（运行() 继承 stdout → stdout=None）→②新增 运行捕获（capture_output+text·stdout 原样回放 stderr 归错误流）→③三调用点换接→④双验证（缺陷复现+修复生效）→⑤集成。
**4. 验证链**：py_compile→行为双验证（旧路径 None=缺陷面／新路径解析命中用例名）→integrate（快速门禁·写集非 src/tests 触发面）。
**5. 边缘情况与诚实边界（预判）**：①win 分支同修（同一解析逻辑）——win 侧实跑验证=家机面（诚实边界·逻辑同构）；②捕获后输出回放（延迟打印·可见性保留）；③本修复**不改变门禁判定结果**（只让点名机制真正生效）——722 集成按正确点名放行（结论无误·过程解析失效）；④「空集合恒真」判据的加固（清单非空自证）列为后续候选（本轮只保证解析正确）。
**6. 与他机交叠**：integrate.py=三机共享工具——无并发写集（家机 721 为 v2 树/调试链·单位机空闲）；@家机/单位机：后续集成将**真正**校验点名清单（未点名红会拦截）——请确保点名依据充分、披露完整。

**【收工补记（723-a 实测）】**：①**点名机制端到端验证**（集成后函数级全量门禁实跑·01:20~01:34）——构建+单测 1405/1405+E2E 并行/串行同 **523/3/2** → 解析得到**非空**清单 `['113_H8_容器持有类对象','127_v2_嵌套容器与容器字段与静态与引用','79_v2_自举闭环']` 全部命中点名 → `全量门禁` 返回值 None（放行）=**修复生效铁证**（缺陷期该清单恒空）；②develop 集成后门禁复验（零警告构建+单测+E2E 全量·3 红=已知红全点名）同批完成；③教训含 **722-a 同族第二例**（新增平台专属设施未守卫·并入 720 平台分叉条目）随本轮入册。

### 2.133 develop 红 fix-forward：719-a linux 构建红修复（**runtime.cpp UEF/VEH 显式安装平台守卫**＝win-only 设施无条件调用）——722-a 修复轮（分支 `任务/深度机-722-linux链接修复`·基点=develop 10eb17da·wt720 复用·2026-09-25）

**1. 目标与验收判据**：develop 红（§8.6 fix-forward·集成 720 后发现）——linux 全量门禁单测链接失败（`libcn_runtime.a(runtime.cpp.o): in function entry: undefined reference to cn_install_crash_handler / cn_install_veh`·719-a 在 entry() 无条件调用两函数·定义在 io_api.cpp `#ifdef _WIN32` 段内）。修复=声明与调用加 `#ifdef _WIN32` 收口。**验收**=①linux 零警告构建通过；②单测全绿；③E2E 全量零新增红（对照 528=523/3/2 基线）；④win 行为逐字节不变（零语义变更）。
**2. 写集**：`src/runtime/runtime.cpp`（单文件·守卫 2 处）｜plans/021（§五 722-a 行+文首戳）｜plans/025 本节｜交接/更新日志本机节｜看板（红定性通告 10eb17da+清行）。
**3. 实施步骤**：①调用点全库排查（grep 三符号=仅 runtime.cpp:24/25 两处调用+io_api.cpp 定义）→②守卫收口（声明与调用同守卫）→③linux 构建+单测+E2E 验证→④integrate。
**4. 验证链**：零警告构建→单测全量→E2E 全量 --jobs 8→integrate（快速门禁+写集分类含 src/ ⇒ 全量门禁自动触发）。
**5. 边缘情况与诚实边界（预判）**：①**win 侧验证=家机面**（本机无法跑 win 门禁）——守卫为 `#ifdef _WIN32` 真分支保留，win 编译产物不受影响；win 全量复验请在通告段回签（诚实边界）；②潜在其他 win-only 调用点=构建错误全量列出仅 2 符号·已 grep 三符号佐证无余面；③家机 721 链（PROC 内二分·src/runtime 同域）在飞——若其新提交触碰 runtime.cpp 同区域，集成时 rebase 复核。
**6. 与他机交叠**：家机 719-a/721-a（src/runtime 同域·**同文件交叠**）——本修复仅加平台守卫（最小差量）；红定性通告已发（看板·develop 10eb17da）·修复后 @家机 复核 win 侧。

### 2.132 036 任务 linux-x64 自举性能基线实测（**bench_self_host.py M1/M2/M5/M6 全链＋三平台口径对齐**·顺带修正 042 行 513 已完成漂移）——720 开发轮（分支 `任务/深度机-720-性能基线`·基点=develop 0d0258c1·wt720 隔离·2026-09-24·用户已批）

**1. 目标与验收判据**：036 任务深度机份（021 036 行明文「深度机 x64l 基线·bench_self_host.py 三平台口径对齐」）——在 linux-x86_64 平台跑通 bench 全链（M1 宿主编译 1 遍／M2 CN 版编译 3 遍取中位／M5 第二代编译 3 遍取中位／M6 fix_p≡fix_s 逐字节断言），成绩与口径入库，三平台基线补齐 linux 块。**验收**=①bench 四步全通（M6 逐字节一致=自举固定点在 linux-x64 成立）；②linux-x64 成绩（M1/M2/M5 绝对值+M2/M1 与 M5/M2 比值+环境信息）落 021 036 行与本节回填；③口径与 560 arm64 逐项对齐（Debug 构建／同 v2 全树 fix_p≡fix_s 口径／3 次取中位／报告产物 target/bench/）；④042 行漂移修正随分支入库；⑤纯文档写集快速门禁全绿。

**2. 写集（预登记）**：plans/021（036 行成绩+042 行修正+§五 720 行+文首戳）｜plans/025（本节六要素+收工回填）｜交接.md 深度机节｜更新日志.md 深度机节｜看板清行（窄通道直推·不占分支）。**预期零 src/tests**；若 bench 脚本 linux-x64 分派暴露缺陷则顺带修 scripts/bench_self_host.py（平台无关脚本=快速门禁面·修必随实测证据）。

**3. 实施步骤**：①wt720 建 Debug 构建树（CMakeLists 默认 CMAKE_BUILD_TYPE=Debug·与 560「vs C++ Debug」口径天然一致）构建 cn；②`python3 scripts/bench_self_host.py`（平台自动探测=linux-x86_64）；③核对 M2/M1 与 M5/M2 比值对 arm64（1.91x／0.758）的量级合理性；④成绩+环境（CPU 型号/核数/内存/GCC 版本/构建 commit）入库；⑤042 行修正（git 实证：513-a 已完成 T80 宿主读路径修复+双端闭环+E2E 496 升双测 PASS·行述「待认领」系文档漂移）。

**4. 验证链**：bench 脚本退出码+M6 逐字节断言（自举固定点）；报告产物 target/bench/性能基线报告.md 生成核验；check_handoff+check_progress_sync 快速门禁；口径对齐核对表随回填。

**5. 边缘情况与诚实边界（预判）**：①linux-x64 路径 560-a 三平台改造后**从未实测**（单位机只跑 arm64·win 原生）——脚本 bug 风险真实存在，遇错当场修脚本+教训入册；②M2/M5 取 3 次中位为脚本内置，机器负载波动仍可影响绝对值——成绩随附环境信息；③M6 若不一致=自举固定点在 linux-x64 破裂=重大发现，当场停下呈报（预期一致——linux 门禁自举链 78/79 历史全绿背书）；④绝对数值跨机不可比（不同 CPU）——三平台对齐只对「口径/尺子」不对「数值」，数值纵向比较以同平台历史为准；⑤bench 中间产物在 target/bench/（gitignore 面·不入库）。

**6. 与他机交叠**：家机在飞 701/705~708/718（v2 树语义/生成层+win 专属调试链）——本任务写集=纯文档（或有 bench 脚本）·**零交叠**；家机 709 系集成后 v2p 行为若有变属编译器本体演进（重构建即取最新 develop），不影响测量口径；单位机无在飞任务。判定=**独立**。

**【收工回填（实测·720-a）】**
**1'\. 验收（部分达成·含开工预判外的机制级缺陷当场修）**：①bench linux-x64 全链口径与 560 arm64 逐项对齐（Debug 宿主／v2 全树 fix_p≡fix_s／3 次取中位／报告 target/bench/）：
**M1=19490.5ms｜M2=40728.6ms（样本 40615.033·40728.56·40760.931·产物 636179 行）｜M3=900.6ms｜M4=204.0ms（链接通过·绑定自检 cn_main ∈ cn_self.obj ✓）｜M2/M1=2.09x**（对照 arm64 560-a：M1=22295.7／M2=42608.4／1.91x——同口径量级可比）；
**M5/M6 未达成**=cn_self 自身编译 v2 全树失败（[sem5] 语义错误 67223 个·中止 IR 生成与代码生成）——既有缺陷域（家机 708-c/d/e 链在飞·713-a 已使链接全通）·非本任务口径问题；降级报告已落盘（720-a 脚本增强：M5 失败降级出报告保 M1~M4 成果＋落错取输出尾部）。
②**开工预判①命中并升级=机制级缺陷当场修（家机 713-a 平台覆盖缺口）**：linux-x64 路径 560-a 三平台改造后从未实测——首跑 M4 链接失败（undefined `向量$映射$整64$符号$追加_A_T…` vs 定义 `__T…` 一字差）；独立定位=713-a 第③项「符号修饰参数段剥 &」只改 MASM 路径（代码生成符号.cn）·符号修饰X64L/符号修饰ARM64 GAS 后端未覆盖＋**IR 层真根因**（取实例实参们 尾 & 泄漏：引用接收者类型文本尾 & 被 解析实参段「尾缀并入」吃进末段实参→类型实参替换后参数串带 &→符号修饰多编码一个 A）；
修复=IR 层精确剥尾 &（IR容器.cn 单文件·实例身份不含引用修饰·713 ① 同源口径·**勿在符号修饰层剥**——真引用参数段 A 编码须保留，宿主 linux 三方对齐实证：语义分析$表压入〔引用形参〕=带 A／向量$映射$整64$符号$追加〔按值形参〕=无 A）；**效果=linux 与 win 形态同构：79_v2 -4 链接失败→链接全通·进入 -6 fix_s 第二跳**（第二跳既有形态=语义错 67223）。
③**042 行漂移修正（预登记项）**：§三 042 行「待认领」系文档漂移（511-a 定位后 512/513-a 已完成 T80 双端闭环·517 链集成 7e0ff3c2·586-a 复验零回归）——已销项移入 §4.2 台账。④两条挂账老化 WARN 处置（007 行＋006-001 行 720-a 复核刷新）。
**2'\. 写集（最终）**：scripts/bench_self_host.py（linux 面 准备运行时objs 恢复＋M5 降级报告＋落错取尾）｜CN语言编译器v2/IR/IR容器.cn（取实例实参们 剥尾 & 归一）｜plans/021（036 行成绩＋042 销项＋§五 720 行＋文首戳＋老化刷新）｜plans/025（本节＋棘轮滚转 2.114~2.130 八节入归档）｜交接/更新日志本机节｜看板（713-a 回签＋清行·窄通道）｜教训 2 条。**零 src/tests**（测试文件零改动）。
**3'\. 边缘（回填）**：①预判①命中并升级（见 1'-②）；②预判③（M6 不一致=重大发现）未触发（M5 前置失败）；③比值 M2/M1=2.09x 与 arm64 1.91x 量级可比（跨机数值不可比·尺子对齐 ✓）；④绝对数值含本机负载波动（3 次中位抗噪·三跑 M1 19490~19678／M2 40582~40783 稳定）；⑤bench 中间产物在 target/bench/（gitignore 面·不入库）。
**4'\. 交叠（回填）**：家机 708-e 链同域（v2 树 IR 层）——**语义互补非冲突**（本修复=IR 层剥 & 补 713-a 平台覆盖缺口）；713-a 异步验收回签已发（看板通告段·develop 53a95618）。

> 〔2.131 024 D43 别名全链收口＋02_control 真根因修复（657 开发轮） 已滚转归档（深度机 720-a 棘轮·2026-09-24·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.128 strict 四孤儿修复＋569 runner 平台硬编码修复（576/577 两案） 已滚转归档（同上·720-a 棘轮）〕
> 〔2.130 2.130 编号勘误轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.116 2.116 D1 设计型池实施 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.114 2.114 D1 滚动 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.115 2.115 D1 设计型池·方案设计轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.123 2.123 F2-33 迭代器侦察定稿轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.121 2.121 D12 v2 词法预处理对齐 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.122 2.122 456 根治轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.125 2.125 T95 修复轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.130 2.130 strict 双红根治轮 已滚转归档（深度机 720-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.129 D42甲 波3 终章接力（撤守卫收口=stdlib 链接期供给→源码加载切换落地·585-a） 已滚转归档（740-a 棘轮·2026-09-25·内容 git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.83 D46 v2 结果构造名裸名编译期拒绝（474-a） 已滚转归档（同上）〕
> 〔2.127 T33 残余根治（v2 窄宽值规范化四站点＋T36 v2 面字面量值域检查·565） 已滚转归档（同上）〕
### 2.136 001 定点修复轮（**「表注册 写回→映射$整64$符号$设置 克隆体」崩溃定位与修复**＝§1.45 四步②·739 同构实锤后 linux 本机直做）——740-a 开发轮（分支 `任务/深度机-740-001定点修复`·基点=develop 720d820c·wt740 隔离·2026-09-25）

**1. 目标与验收判据**：linux gdb 精确定位写回态 v2p 崩点（739 实锤=映射$整64$符号$设置_J 克隆体·rax=-1·栈帧写穿）的指令→对位 CN 源码行（stdlib/映射集合.cn 设置：链查找/重哈希循环/数组扩容三候选）→**溯源坏状态来源**（this 指针/键数组/桶数组/链数组 值·实参装配 asm 回溯·家机 733/734 三侧配对矩阵结论复用验证）→定点修复（宿主发射层缺陷=机制级 §8.4 当场修随证据备案；触及容器 ABI 语义=升级呈报）→**验证**=①写回态 v2p 编 t4 rc=0 不崩；②打补丁 v2p 编 v2 全树 rc=0（739 ②b 解锁·cn_self 可构建）；③cn_self 编 t4 rc=0 零诊断（**t4→0 判据②达成**）；④全量门禁零新增红（红名单=113/127_v2/79_v2 基线同构）；⑤反证双态（撤修复→崩复现→修→绿）。
**2. 写集（预登记）**：`src/`（按定位实测定性·逐文件登记——预期=宿主发射层容器/聚合实参装配面）｜`CN语言编译器v2/语义/语义分析.cn`（写回单件**维持实验态不入库**——t4→0 达成后按 §1.45 四步③双件回归轮再定转正）｜文档六件套。tests 面本轮预期零改动（无新用例——修复锚=739 复现链）。
**3. 实施步骤**：①工件迁移（wt739 target/p739/ v2p_patch/t4.cn/日志 复用；写回单件从 /tmp 备份重施加）②gdb 崩点反汇编逐指令（设置_J+offset·地址/寄存器/内存操作数三维记录）＋源码行对位③坏状态溯源（frame 检查 this 解引用·键/桶/链数组值·向上回溯 表注册 写回调用点实参装配 asm）④根因定性⑤定点修复＋B9 同族面扫描（famscan 语境=聚合实参装配）⑥验证链全跑。
**4. 验证链**：gdb 反汇编对位记录→修复后 v2p t4 rc=0→fix_p rc=0（~636k 行）→cn_self 构建→cn_self 编 t4 rc=0（t4→0）→零警告构建＋单测＋E2E 全量 --jobs 8（gate_lock）→strict/matrix→反证双态→integrate。
**5. 边缘情况与诚实边界（预判）**：①崩点可能在内联序列（宿主 Debug=-O0 口径·内联少·利好逐指令对位）；②设置_J=克隆体后缀符号——先确认崩的是「编译器自身运行的实例」非「发射到 t4 的实例」（t4 无容器调用·预期前者）；③**若根因触及容器按值形参/元素读取 ABI 全链（051/052/056 甲案域）→不强行单点修·升级架构轮呈报**（056 in-out 写回机制=既定路线）；④win 侧复验=家机异步验收（协议 v3）；⑤修复若需 v2 树 stdlib 同改→三平台复跑面广播。
**6. 与他机交叠**：家机 738 已收工（701/705~708=挂等/保全·零交叠）；单位机空闲；本机 057/550 排队未开。独立 ✓。

**【740-a 阶段回填①（gdb 深定位·铁证五件）】**
- **铁证1 崩点指令**：设置_J 克隆体+5592 `mov %r9,(%r10)` 写 [0x8] 崩——r10=基址(NULL)+1×8（+5509~+5553 装载链：`[rbp-0x7b8]` 数组基址槽=0＋索引×8）·**test 仅拦 NULL·0+8 漏过**＝**win fault=0x8 RAX=8 完全同构实锤**（linux r10=0x8=win RAX=0x8）。
- **铁证2 源码行对位**：+5212~+5592=直线字段拷贝序列（无循环回边·逐解引用 NULL 防护＋__cn_runtime_error(3)）＝**设置 覆盖分支 `值数组[已有位置] = 值`**（V=符号 40B 结构体逐 8B 拷贝·写值 0x1a=26=符号 字段驻留 ID 量级·rsi=r9 同值）。
- **铁证3 this 坏对象现场**（0x7098e0·无符号名区）：**键数组/值数组/链数组/桶数组全 NULL·桶数量=1024·元素数量=1·数组容量=4**——「构造器默认值在·分配数组被清 NULL」＝与 722-a win 定性（「桶数组=NULL 但桶数量=1024」「元素数量>0 而链数组=NULL」）**完全同构**。
- **铁证4 时序矛盾**：覆盖分支前置=链查找 返回 已有位置=1≥0（**该时刻看到过可用数组/至少桶链在**）→覆盖写时刻 值数组=NULL——**两时刻/两实例状态不一致**（候选：①浅壳拷贝（元素 句柄浅传×副本语义·610-a 终局定性）②析构清零后复用（「释放+清零」幂等纪律）③realloc 悬垂变体·726-a 定案链「写副本=注册丢失·副本 realloc 释放共享旧块→原映射悬垂」）。
- **铁证5 符号/声明线索**：语义分析.cn:223 `全局层查类型: 映射<整64,符号> 全局层 = 符号表.元素(0);`（元素 副本链实弹）·:235 注释「**顶层静态无初始化**」——顶层/符号表[0] 的静态存储零初始化×构造器语义交互=头号嫌疑域。
- **下一迭代**：①watch 断点观察 0x7098e0 值数组槽（+8）何时被写 0（写者=谁）②分配/释放/拷贝构造/析构 挂点③符号名匹配（CN 静态发射命名 vs 0x7098e0）④根因定性后定点修复。
- 产物：wt740 target/p740/（v2p_patch/t4.cn/wk 工作目录·gitignore）。

**【740-a 阶段回填②（决定性突破·根因主链闭合）】**
- **铁证6 断点观测**（设置_J 入口无条件断）：hit1/2→rdi=0x7fffffff9e58（栈）·rsi=**0x7098e0**（静态区）·rdx=**0x46/0x1a**（hit2=26）·rcx=0x7fffffff9e78（栈）——崩点 rdi=0x7098e0 系函数体中途复用（入口条件断点 this=0x7098e0 零命中→证伪「this=坏映射」假设·铁证3 重新归属：坏对象=rsi 指向的静态对象被误当 this 读）。
- **铁证7 调用方身份+装配**（ret=0x58e5d1 反汇编）：调用方=**语义分析$表注册_A_T$向量$映射$整64$符号 克隆体**（泛型克隆实例）·call 前装配 **4 参**=rdi=[lea -0x358 栈]·rsi=[[rbp-0x2b0]]·rdx=[[rbp-0x290]]·rcx=[lea -0x338 栈]。
- **铁证8 方法解析串线实锤**：写回单件 `符号表.设置(符号表.大小()-1, 顶层)` 接收者=**向量<映射<整64,符号>>**（表注册 引用形参）·应命中 **向量$映射$整64$符号$设置**（容器.cn:198 存在 `设置(整64 位置, T 值)`·查证 ✓）——实际 call 目标=**映射$整64$符号$设置_J**（**元素类型 映射 的同签名方法**·K=整64/V=符号 形参个数恰好兼容）＝**接收者类型少剥一层容器·方法表串线到 T**。
- **铁证9 ABI 错位吻合**：被调（映射.设置·3 参 this/键/值&）把 rsi（=0x7098e0·疑=&顶层 静态槽或向量字段）当键·rdx（=26=位置整数）当值地址——prologue 垃圾拷贝+向量的字段被按映射布局误读（「值数组基址」槽=0）→`值数组[1×8]=写 [0x8]` 崩＝铁证 1/2/3 全部自洽；win 728「桶数量=1024 但桶数组=NULL」=同构误读形态。
- **根因定性（机制级·宿主发射器）**：泛型克隆实例体内 `接收者.方法(...)` 的成员链方法解析——接收者静态类型=容器泛型实例（向量<映射<...>>）·方法查找**穿透到元素类型 T 的方法表**（映射 也恰有 设置(K,V)）→错符号发射+实参装配按被调错签名→ABI 全错位。语义层漏检（应编译期拒绝或正确命中向量方法）+发射层串线=双重缺陷面。
- **修复方向（下迭代源码级闭合）**：①语义/IR 层成员链方法解析的「容器→元素」剥层判定（为何 顶层.设置(名ID,s)〔映射接收者·正确〕与 符号表.设置(...)〔向量接收者·串线〕分叉——顶层 局部=类对象局部·符号表=引用形参，**接收者类别分叉嫌疑=引用形参解引用后类型取错**）②对照 hit1/hit2 前两次未崩路径（错误键哈希恰走安全分支→返回错误→表注册 失败链=语义破坏面）③修复后 v2p t4 零错+fix_p+cn_self 链+全量门禁+反证。
- 附注：0x7098e0 静态对象身份待源码匹配（候选=表注册 静态局部/驻留表·rsi 装载源=[rbp-0x2b0] 槽写入点待查）。

**【740-a 阶段回填③（源码级审查进行时·跨上下文续作入口）】**
- 宿主语义层已排除：semantic_call.cpp:171 objType=checkExpr(接收者)→semantic_expr.cpp:117 引用形参剥 & 返回「向量$映射$整64$符号」✓ 正确；checkMemberCallCore（:770~830·findClass→lookupClassMember）按此类型应命中向量.设置——语义层解析对。
- **串线嫌疑收敛=IR 生成层克隆体发射**：emitInstanceMethodCall（ir_oop_call.cpp:679~700·canonObj 上游待查）+**泛型实例克隆重放路径**（ir_oop_call.cpp:398~510 泛型实例名解析/NewObject/构造 Call 段·317/318-a registerGenericFuncInstance 链）——表注册 调用方符号带 _J×4 尾缀（泛型实例 mangling）·其体内 CallExpr 的方法目标在**克隆重放生成时**可能按替换后类型重解析（候选=重放把「向量<映射<...>>」形参类型经 substTypeParam 处理时误触「泛型实参扁平化/容器合成名解析」路径→接收者被解析为元素类型）。
- 同文件对照锚：语义分析.cn:277 `符号表.删除(大小()-1)`（1 实参·基线大量执行不崩=解析对）vs 写回 `.设置(大小()-1, 顶层)`（2 实参·第 2=类对象局部 顶层）——差异轴=「实参含类对象局部」或「方法重载二义（向量.设置×映射.设置 同 (整64,T) 签名）」，审查时按此二分。
- 续作步骤：①grep canonObj 产出点（emitClassCallExpr 上游）→②读克隆重放对 CallExpr 方法名的重解析点（318-a rewriteGenericFuncCall/registerGenericFuncInstance 附近）→③v2 树 表注册 插桩打印两 设置 调用点解析类名→④定点修复+反证+门禁（六要素 §2.136 验证链）。
- 工件：wt740（分支 任务/深度机-740-001定点修复·已推 c26af912）·写回单件已施加态+备份 /tmp/p739_语义分析_写回态.cn.bak·探针 target/p740/（v2p_patch 可直接 gdb 复跑）。

**【740-a 阶段回填④（修复点锁定·90%）】**
- 插桩实证（emitInstanceMethodCall 入口·ir_oop_call.cpp·临时不入库）：cn.exe 编译 v2 树（写回态）时 方法=设置 的接收者分布=125×映射$整64$整64/10×向量$节点/**1×向量$映射$整64$符号（=写回调用·接收者类型正确！）/2×映射$整64$符号（=顶层.设置）**——语义/IR 首环解析对。
- **错环定位**：emitInstanceMethodCall 内 `findClassMethod("向量$映射$整64$符号","设置")`——**容器泛型实例类是否注册进类表**为疑点：若 findClass 未注册该实例→m==nullptr→return false→上层（ir_call.cpp:71 handleClassCallExpr）fall-through 到**普通函数符号查找**→按名「设置」命中唯一同签名符号=映射$整64$符号$设置→错发射。此假设同时解释「顶层.设置 解析对（映射$整64$符号 类已注册）vs 符号表.设置 串线（向量$映射$整64$符号 实例未注册）」。
- mangling 定音（nm 实证）：崩点符号 `_映射$整64$符号$设置_J__T$符号` =类.方法+_J(整64 参数标记·对照调用方 _J×4)+__T$(类型段)=「映射.设置(整64,符号)」无歧义；调用方 `_语义分析：：表注册_A_T$向量$映射$整64$符号_J×4`=表注册(向量<映射<整64,符号>>&,整64×4)——**表注册 非泛型克隆·_J=整64 修饰**（修正回填②「克隆体」表述）。
- **修复方向定稿**：容器泛型实例（向量$映射$整64$符号 等）未注册类表时的方法解析不得 fall-through 裸名函数查找——正解=按 316/417-a 单态化注册机制让容器实例类进类表（findClass 命中→向量.设置 实例解析·318-a rewriteGenericFuncCall/GFI 链同款）或解析层对「接收者=容器实例+方法∈容器类」强校验。实施时先验证 findClass("向量$映射$整64$符号") 返回值（一行探针）再动刀。
- 插桩已撤计划：验证完毕即撤（ir_oop_call.cpp 恢复·临时调试不入库）。

**【740-a 阶段回填⑤（最终环锁定·修复实施入口）】**
- fall-through 证伪：探针打印 findClass("向量$映射$整64$符号")命中=1（大小/元素/追加/删除/设置 全部正常进 emitInstanceMethodCall）——回填④假设证伪·修正。
- **最终嫌疑环=emitDirectMethodCall 的 Call 目标构造 `methodSymbolKey(owner, m->sigKey)`（ir_oop_call.cpp ≈860/865 行）**：owner=lookupClassMember 出参（声明类）——若对「泛型实例类 向量$映射$整64$符号 的成员 设置」owner 被填错（=「映射$整64$符号」——嫌疑机制：实例类成员表的 owner 归属解析在泛型替换语境误取实参类/基类）→methodSymbolKey 产出「映射$整64$符号$设置」符号→恰与 stdlib 已实例化符号撞名→**链接通过·运行 ABI 错位崩**＝铁证 6~9 全自洽（含「为何链接期不爆」）。
- 次嫌疑环（同段连带审查）：`buildCallArgsForMethod(node, canonObjForMethod, methodName)`（≈767 行）装配出 4 参（观测 this+rsi+rdx+rcx vs 向量.设置 应 3 参）——实参多装 1 个·或「T 值=映射 56B 按值形参」的取址/拷贝双推——与 owner 环同轮源码审查（lookupClassMember 出参填点+sigKey 构造+buildCallArgsForMethod 内部对类对象实参的处理）。
- **修复实施入口（下一迭代直接从这里开始）**：①grep methodSymbolKey 定义（ir.hpp/ir.cpp）看 key 格式→②grep lookupClassMember 定义（semantic_sig.cpp/class_resolver.cpp）看 owner 出参填点·重点=泛型实例类成员表注册时 owner 记的是什么→③对照「映射$整64$符号 类（命中对）」与「向量$映射$整64$符号 类（owner 疑错）」的注册路径差异→④修 owner 或 sigKey→⑤撤插桩（ir_oop_call.cpp 临时 fprintf 两处）→⑥v2p t4 零错+fix_p+cn_self+全量门禁+反证（§2.136 验证链）。
- 插桩现状：ir_oop_call.cpp 两处临时 fprintf（入口行+findClass命中）未撤·随修复轮一并撤除。

**【740-a 阶段回填⑥（重大反转·错位收敛到代码生成层）】**
- 回填⑤假设证伪：IR 解析桩（emitInstanceMethodCall 内 findClassMethod 后）实测——写回调用 **owner=向量$映射$整64$符号·sigKey=设置#整64,映射$整64$符号·key=向量$映射$整64$符号$设置#整64,映射$整64$符号＝IR 层完全正确**；lookupClassMember 查空探针零输出（未命中路径未走）；nm 实证两符号均在 v2p 内（向量$…$设置 1 个·映射$…$设置=0x62610b 1 个）。
- **反转结论**：IR 层调用目标名正确 ⇒ 错位在**更下游=代码生成层**（IR_调用 的 name→汇编 call 符号 的映射环节·候选=代码生成/代码生成符号.cn 的符号修饰/对位段〔713-a 剥 & 同域〕——把「向量$映射$整64$符号$设置#整64,映射$整64$符号」的调用错对位到「映射$整64$符号$设置」符号）；亦不能排除「0x58e5cc 崩点 call 的 IR 源头另有其调用」——需在**插桩版 v2p**（地址全变）重抓崩点+调用点对位。
- 下一迭代：①插桩版 v2p_probe 复现崩→新调用点→确认 call 目标仍是映射.设置 还是已变②若仍指向映射.设置→审 代码生成符号.cn 对 key「类$sigKey」的修饰装配（#号/逗号 的 mangle 处理）③若已变→重新按新目标定音。
- 插桩现状：ir_oop_call.cpp 三处＋class_resolver.cpp 一处（限流）临时 fprintf·修复完成后统一撤销不入库；工作树尚有 写回单件（语义分析.cn）实验态。

**【740-a 阶段回填⑦（错位 100% 定音·修复点=代码生成符号修饰层）】**
- objdump 全镜像统计（决定性）：`call 向量$映射$整64$符号$设置`＝**0 次**（正确目标已定义但从未被调用）·`call 映射$整64$符号$设置`＝**2 次**（错误目标被调用·含表注册 写回点 0x58e5cc）——**代码生成层把两个不同 IR key 错映射/撞名实锤**。
- 机理候选（与 713-a「取实例实参们 尾 & 泄漏」同域）：代码生成符号修饰层对 sigKey「设置#整64,映射$整64$符号」的**参数段编码时把参数类型「映射$整64$符号」错误剥层/丢容器前缀**→产物符号=`_映射$整64$符号$设置_J__T$符号`（含 _T$符号 类型段·0x62610b）——与「映射$整64$符号$设置#整64,符号」（顶层.设置·正确存在）撞名→后发射者/先定义者胜→写回调用跳入错误实现→ABI 错位崩。
- **修复实施入口（下迭代直接执行）**：①读 CN语言编译器v2/代码生成/代码生成符号.cn（+代码生成共用.cn）的「方法调用符号 mangle」函数（key「类$sigKey」→汇编名·找 # 与 , 的分段处理+参数类型编码）②复现「参数=容器实例」的编码路径·定位剥层点③修复（参数类型=容器实例时保留完整实例名编码）④反证（撤修复→撞名复现→修复→两符号分离）⑤v2p t4 零错→fix_p→cn_self→t4→0 判据②→全量门禁→win 侧家机异步验收。
- 注意：撞名双符号形态=**链接期不可见**（两 IR key 各自 mangle 后恰同串）——B9 扩散检验候选：全库 sigKey 含容器实例参数的调用点扫描。
- 插桩现状：ir_oop_call.cpp 三处＋class_resolver.cpp 一处临时 fprintf·修复完成后统一撤销不入库；工作树尚有 写回单件（语义分析.cn）实验态+target/p740/ 探针（/tmp/v2p_probe.dis 全镜像反汇编留档）。

**【740-a 阶段回填⑧（类型编码机制闭合·剩余单点=写回调用 name→call 之间被改写）】**
- 类型编码/符号修饰机制全看清（代码生成共用.cn:132 类型编码·代码生成符号.cn:57 符号修饰实现·代码生成X64Linux.cn:62 符号修饰X64L）：sigKey 段「整64」→`_J` 形态 J·类实例段→`_T`+逐字节 hex·基名→逐字节 hex——**62610b=`_映射$整64$符号$设置`+`_J`(段1 整64)+`__T$符号`(段2 符号)=「映射$整64$符号$设置#整64,符号」的合法修饰**；向量版修饰（含 向量$ 前缀 hex·_T+hex(映射$整64$符号)）与它**不撞名**——回填⑦「撞名」表述修正：非撞名，是**写回调用点的 call 目标整个错指**。
- 2 次 call 62610b 归属=表注册 函数体内**两个调用点**：①293 行 顶层.设置(名ID,s)（正确·接收者=映射）②写回 符号表.设置（**错指·应为向量$映射$整64$符号$设置**）——与「顶层.设置 解析正确/写回 IR key 正确」全部自洽。
- **剩余单点（修复就差这最后一环）**：写回调用点的 IR_调用 name（=向量$映射$整64$符号$设置#整64,映射$整64$符号·回填⑥实证）到 v2p 汇编 call 62610b 之间**被改写**——环节=v2 树生成层的 调用发射（CN语言编译器v2/IR/生成调用 或 生成表达式求值.cn 的 调用名消费）或 IR布局 的 函数符号表对位（按名查 IRFunction 时「设置」短名 miss/前缀匹配错）——**审查点=「IR_调用.name 如何查到目标 IRFunction 并取其 mangledName」**（候选=按「$ 分段后末段方法名」查找→「设置」命中首个注册的 映射$整64$符号$设置 实例）。
- 下一迭代：①grep v2 树 IR/生成调用·生成表达式求值.cn 的 IR_调用 发射与 name 消费②查 IRFunction 登记表（IR布局/IR生成）按名检索逻辑（「完整 key vs 末段」匹配）③修复=完整 key 匹配（或注册表以 mangledName 单一归属）④反证+验证链（§2.136 回填⑦所列全链）。

**【740-a 阶段回填⑨（.s 源级全景定音·两度反转修正）】**
- .s 源级核对（v2p_probe.s 22MB·中文注释可读）：①**写回调用点（表注册 内·.s 行 352034）call 目标=向量$映射$整64$符号$设置＝目标正确**（回填⑧「错指」表述修正——objdump 的 2 次 call 62610b=①表注册 顶层.设置（.s 351949）②表标记转移 层.设置（.s 352277）＝**均为存量正确调用**（接收者=映射$整64$符号 类局部·目标=映射.设置 正确））；②调用约定定音=（rdi=retbuf 隐藏指针·rsi=this·rdx=键·rcx=&值）——回填⑥观测的 hit 参数形态**全部正常**（rdi=栈 retbuf·非 this）。
- **剩余真问题收窄为一条**：崩点（映射.设置 体内+5592 写 [0x8]）时刻 **rsi(this)=0x7098e0=.data 静态区对象**（内容=四槽全 0·偏移 32/40/48=1024/1/4——恰似相邻静态数据·映射布局误读成立）＝**某次 层.设置/顶层.设置 的 this 指向了静态区**——候选：①「层=符号表.元素(i)」副本的存储槽被静态提升/越界读（元素 句柄语义×i 越界：符号表.大小() 与静态表错配）②静态映射（如 指针变量表·语义分析.cn:237「静态 映射<整64,整64> 指针变量表·顶层静态无初始化·宿主 P3-8 入口自动构造」——但 V 类型不符·需核）③retbuf/槽偏移踩踏。
- **下一迭代（收束路径）**：①gdb 在 62610b 入口条件断 `*$rsi==0x7098e0` 抓该次调用的**返回地址**→定位是 351949（表注册 顶层.设置）还是 352277（表标记转移 层.设置）→②读对应调用点的 .s 装配段（rsi 槽的装载链）→追 this=0x7098e0 的写入者→③修复→④撤插桩（ir_oop_call.cpp 三处+class_resolver.cpp 一处）+写回单件处置→⑤反证+验证链（回填⑦所列）。
- 工具链沉淀：v2p 产物 .s（22MB）含中文注释「分配变量 X$N」+Lptr_ok 标签·配合 nm/objdump 三向对位=定位利器（比纯反汇编快一个量级·本轮已实测）。

**【740-a 阶段回填⑫（watch 定音·清空者=拷贝构造·根因链终局闭合）】**
- 硬件 watch（0x7098f0=全局层映射 链数组槽）写入序列：memset 清 0（分配零初始化）→std 容器早期复用痕迹→**映射$整64$符号$映射_A_T$映射$整64$符号+880 写 0（＝「映射$整64$符号」类拷贝构造 把链数组清 0）**→设置_J+5235 写合法桶数组→**拷贝构造再次写 0**→SIGSEGV——**清空者=拷贝构造（映射(映射&)）·其执行默认构造式清零而非拷贝源字段**＝715-a「拷贝构造克隆体字段深拷缺陷/桶拷贝循环未生效」+722-a「元素 返回 T 拷贝路径的拷贝构造克隆体字段错位产物」**完全同构实锤**。
- **根因终局一句话**：表注册 的 顶层=元素(i)（句柄返回模型）后，编译器注入的「拷贝构造」把全局层映射的字段清零（拷贝循环未生效），后续 顶层.设置 在坏对象上写→段错误；写回单件只是**让该既有坏对象被读到的探针**（不加写回时坏对象不被触碰·基线不崩）。
- **修复归属（终局）**：＝**052 甲「元素读取副本全链」+055 宿主容器类实例值语义 ABI 专项的既定实施域**（拷贝构造链修复）——家机 727 轮已给绕行方案（容器.cn 增 元素引用(位置)->T&+表注册 改用·727 试过因交叉回归撤·待本修复后回归＝§1.45 四步③双件回归）。
- **下一迭代（真修复轮）**：①读 宿主发射的「映射 拷贝构造克隆体」（.s 符号 `_映射$整64$符号$映射_A_T$映射$整64$符号`·+880 写 0 处）+stdlib/映射集合.cn 拷贝构造源码对位→定位「拷贝循环未生效/字段错位」发射缺陷→②定点修复（域=055 ABI 手术或单点·按源码审查定）→③撤插桩（ir_oop_call.cpp 三处+class_resolver.cpp 一处）+写回单件转双件回归→④反证双态+验证链（回填⑦清单）→⑤集成·win 侧家机异步验收。
- 本轮（740-a 深定位）成果保全：十（二）份回填全推·分支最新=回填⑫本提交。

**【740-a 阶段回填⑭（根因 100% 定音＝self-copy 摧毁·修复三路线）】**
- 两次命中对象字段对比（gdb x/7gx $rsi）：hit1（键=70「加」）桶数组=0x7135f0 合法·元素=0·容量=0；hit2（键=26「主」）**四数组全 0**·元素=1·容量=4（hit1 注册痕迹）——**hit1 与 hit2 之间对象被自我摧毁**。
- **机理终局**：写回单件 `符号表.设置(大小()-1, 顶层)`——052 甲前 元素=句柄语义 ⇒ 顶层=元素(0) 返回句柄=**池[0] 本体地址** ⇒ 写回=「值数组[0]=顶层」＝**池[0] 拷贝进池[0]（self-copy）**——拷贝构造/类赋值发射**无自赋值守卫**：先执行「桶数组=无;键数组=无;值数组=无;链数组=无」清自身→再读「其他.×」＝读已清零的自身→四数组不拷→对象摧毁→下次表注册 写坏对象崩（[0x8]=键数组[1]）。
- Rust 对照：`a = a.clone()` 类库层 ptr::eq 自赋值守卫（String/Vec 同款）——**CN 发射层缺自赋值守卫＝缺陷本点**（宿主 183-a 结构体字段自赋值守卫先例·446-a v2 字符串自赋值先例——本处=容器元素赋值通道漏同款）。
- **修复三路线（下迭代按此实施）**：**甲（推荐·发射器根治）**=v2 树 容器元素/类赋值通道（生成语句赋值.cn 698-b 元素写深拷分支+IR容器.cn 拷贝构造调用点）前置自赋值守卫（源地址==目标地址→跳过拷贝·零开销）＝覆盖全容器全类·一劳永逸；**乙**=stdlib 各类拷贝构造体内加自判定返回（语言层补丁·每类重复·不可持续·不推荐单用）；**丙**=表注册 改用 元素引用（家机 727 方案·绕开 self-copy·属 052 甲双件回归正办·与甲并行不冲突）。
- 实施序：甲（发射器守卫）→丙（表注册 改 元素引用·需 容器.cn 先增 元素引用 接口＝双件回归②）→撤插桩→反证双态（撤守卫→崩复现→修复→绿）→验证链（v2p t4 零错→fix_p→cn_self→t4→0→全量门禁）→集成 740。

**【740-a 阶段回填⑮（乙线实验失败→甲线发射器确认·新事实）】**
- 乙线实验（映射 拷贝构造体加自赋值守卫 2 行）：**未生效仍崩**（同点 0x6276e3）——**机理=发射器在调用拷贝构造前已对目标 memset 清零**（watch 首条 memset 佐证）——self-copy 时源（=目标）先被清·语言层守卫读到的源已是 0·不可拦截——**守卫必须前置到发射层（memset/清零之前）＝甲线确认**。守卫实验已撤销（stdlib 恢复）。
- **新事实（win/linux 分叉点）**：win 侧（家机 728-a）打补丁 v2p 编 v2 全树 rc=0·linux 侧（739）同实验 rc=139@9.9s——**「编 t4 崩」同构·「编 v2 全树」两机分叉**（候选=wt698 v2 树基线（旧 965 提交）与 linux develop 树差异·非平台分叉）——739 回填「第一代同构」表述限编 t4 场景·编全树场景待重验。
- **修复实施定稿（甲线·下迭代直接执行）**：定位「容器元素/类赋值 → memset+拷贝构造」的发射链（cn.exe ir_expr_assign.cpp / 生成语句赋值.cn 的 元素写深拷分支·memset 来源二选一）→**在 memset/清零发射之前插源==目标比较跳过**（IR 层 比较+条件跳转·零开销）→反证（撤守卫崩复现）→验证链全跑（回填⑦清单）→集成。
- 工件状态：v2p_fix（守卫实验产物·gitignore）·stdlib 已恢复·写回单件仍在（语义分析.cn·实验态）·插桩三处+一处仍在（ir_oop_call.cpp/class_resolver.cpp）。

**【740-a 阶段回填⑯（终局机理实锤＝析构顺序·丙线定稿）】**
- 容器.cn:198 向量.设置 源码定音：`自身.析构被移除(位置); 数据[位置] = 值;`——**self 覆盖（源=值=目标槽自身·写回单件场景）时 析构被移除 先调 ~映射（释放四数组+置无＝观测「计数残留」全吻合）→后续 数据[位置]=值 读已毁对象**＝经典 self-assignment destroy-then-copy 顺序缺陷（76-a 注释「T=字符串 注入·有析构类 no-op」与实态不符——有析构 T 的覆盖写 注入了析构+拷贝）。
- 观测勘误：崩点写 [8]=追加段 键数组[元素数量=1] 写（非覆盖分支拷贝）·乙线失败根因=析构先于守卫可达点（守卫在拷贝构造体内·析构在其前）。
- **修复定稿＝丙线（052 甲双件回归②既定项·唯一干净解）**：①容器.cn 向量 增 `元素引用(整64 位置) -> T&`（返回 数据[位置] 地址·引用返回框架=T95/551-a「返回类型名尾 &」判定既成）②语义分析.cn 写回行改 `符号表.元素引用(符号表.大小()-1).设置(名ID, s)`（直达池·无 T 按值中转·无析构旧值）③链验证 t4→0。
- 下一迭代：①容器.cn 元素引用 实现（对齐 元素 的越界行为）②写回行改造③v2p 重建→t4 链④撤插桩+反证+验证链（回填⑦清单）→集成 740。

**【740-a 阶段回填⑰（丙线受阻·受阻点=宿主内置类表同步·收工态）】**
- 丙线实测：容器.cn 增 元素引用+写回行改链式调用→v2p 构建 rc=1「类 '向量$映射$整64$符号' 没有成员 '元素引用'」——**守卫态（stdlib 链接期供给）下内置容器类的方法集由宿主内置类表承载·容器.cn 源码新增方法不被收录**＝727 轮「交叉回归」的复杂度根源实锤。实验已撤销（写回行恢复原样·容器.cn checkout）。
- **丙线完整实施清单（修正）**：①宿主内置容器类表增 元素引用 签名（semantic 内置表/IR签名 扫描口径二选一·随 001 波3 撤守卫后自然转源码级）②容器.cn 元素引用 ③写回行改 ④验证——**即丙线依赖撤守卫（001 波3 本体）或宿主同步·非独立小改**。
- **740-a 收工定调（诚实边界）**：根因链 100% 闭合（回填⑭⑯：向量.设置 析构被移除 先毁 self 覆盖源＝写回单件触发）·修复需动 ①宿主发射器自赋值守卫（甲·根治）或 ②宿主内置类表+元素引用（丙·052 双件回归②）——**均跨入 052/055 既定专项域·单点小改不可达**。740-a 转入收工：撤全部插桩+写回单件处置+反证（崩复现双态已实证=回填⑫ watch 序列）+验证链生产态部分+集成回填（本提交）·修复实施移交 **741 轮（052 甲/055 域·fresh context）**。
- 740-a 产出总账：16 份回填·崩点/调用约定/self-copy/析构顺序四级定性全实证·探针与 .s 留档（target/p740/·/tmp/v2p_probe.dis）。

**【740-a 阶段回填⑬（源坏链判定·修复决策点）】**
- 拷贝构造源码审查（stdlib/映射集合.cn:133-176·2026-08-25 方案A 深拷贝）：**逻辑正确**（清四指针→按源非空逐数组分配+复制+桶/链循环）——watch 观测「拷贝构造只清零无分配拷贝」＝**`如果 (其他.链数组 != 无)` 判假＝源（其他）的链数组=NULL**——坏不在拷贝构造本身·在**源（符号表[0] 全局层）从入池起链数组即 NULL**。
- 入池链=主流程 push 全局层（空映射 构造→向量.追加 进池·拷贝构造/存储发射）——**714-a 锁定「表压入→向量<映射<整64,符号>>$追加 克隆体运行时崩」同族**（追加 克隆体/进池存储的发射缺陷→池内元素字段不全）。
- **修复决策（两线并进·按既定波次）**：**A（主线）＝052 甲/055 宿主 ABI 专项**（元素副本+拷贝构造链宿主手术·001 链既定波次 709）——本崩溃是其治理域的又一实证；**B（前置单点·可先行）**＝「进池拷贝构造后池内元素字段不全」的发射单点定位（对照 追加 克隆体 vs 拷贝构造的发射差异·ir_oop_container/生成容器 通道）——B 修好后 A 的地基更稳·且 B 独立可反证（崩/不崩双态）。
- **下一迭代**：①B 线=向量.追加（T=映射 实例）进池存储段的 .s 对位（352034 邻域函数族）＋构造完成态逐字段核对→单点修复②A 线=055 开工（fresh context·025 §1.13 既定六要素）③两线集成后 t4→0→fix_s→验证链→001 波次推进。

**【740-a 阶段回填⑪（段属性定音·归属 052/055 既定治理域）】**
- 段属性核（objdump -h）：0x709410/0x7098e0 均在 **.bss 尾之后的堆区**（.data=0x66e240~0x67451c·.bss 至 0x6746c0）——回填⑨「静态区」表述修正＝**堆上对象**。
- 元素_J 首调实测：this=0x709410（堆·符号表 向量的数据池）·位置=0——顶层 句柄=元素(0) 返回 rax=0x7098e0（**同堆区+0x4D0**）＝**向量数据池内第 0 个映射元素的地址**（052 甲前的句柄返回模型·非副本）。
- **0x7098e0 对象内容终解**=「向量数据池第 0 元素」＝**符号表[0] 全局层映射**——其「四数组 NULL+桶 1024/元素 1/容量 4」状态＝**全局层映射被清空数组但保留计数**（候选：①退出作用域/清空逻辑对 [0] 全局层误清 ②P3-8 静态构造链该元素初始化不完整 ③前序编译阶段（多文件第二趟清空时）析构/清零误伤 [0]）→表注册 顶层.设置 在此坏元素上写→崩。
- **归属定音**：0x7098e0=**符号表[0] 全局层**（表注册 检查「大小()==0 返回 0」通过=大小≥1·元素(大小-1) 在循环多次表注册 中 i 递减到 0 时取到全局层）——**全局层映射的数组为何 NULL**＝最后待解单点（候选=弹出作用域/表清空 对 [0] 误用「析构值+释放」·或 多文件 第二趟 重置逻辑）。
- **下一迭代（3 步收束）**：①gdb watch 0x7098e0+16（链数组槽）抓写 0 指令→定位清空者（源码行）→②定性（机制级当场修/或确认=052 甲 055 宿主 ABI 专项既定域则合并实施）→③修复+撤插桩+反证+验证链（回填⑦清单）→④集成。

**【740-a 阶段回填⑩（崩溃调用点定音·585-a 同族残留）】**
- 条件断（rsi==0x7098e0）实测：**两次 BAD-THIS 返回地址均=0x58e5d1＝表注册 体内**·rdx=70/26＝**键=名ID（「加」「主」的驻留 ID）**·rcx=&s（栈）——**崩溃调用=表注册 的 顶层.设置(名ID, s)（存量代码·非写回单件）**·其 this（rsi）=0x7098e0=.data 静态地址＝**类对象局部「顶层」的句柄槽值错误**。
- .s 装配段（表注册「分配变量 加$8479」下）：rsi 槽 ← `[rbp-688]`·rdx 槽 ← `[rbp-656]`·rcx ← lea -0x824(&s ✓)·rdi ← lea -0x856(retbuf ✓)——**rsi/rdx 两槽装载自 [rbp-688]/[rbp-656]**＝「顶层」句柄与键 名ID 的来源槽——0x7098e0 静态值即从 [rbp-688] 链路来＝**顶层 声明初始化（=符号表.元素(大小()-1)）把静态地址写进了顶层 句柄槽**。
- **定性（机制级·585-a 修复②同族残留）**：585-a 定案「生成类对象局部补 IR_分配（帧预留缺失→槽与 retbuf/形参区重叠→句柄被清/错位）」——本处=类对象局部 顶层 的句柄槽值=静态地址（.data 区·疑=元素(i) 返回接收的壳/静态槽·或 槽重叠读到形参区）——**052 甲「元素读取副本全链」的宿主侧改造（055 宿主 ABI 专项）正治此域**。
- **修复实施入口（最终）**：①gdb watch [rbp-688]（表注册 帧）抓 0x7098e0 写入指令→②对位 .s 顶层=符号表.元素(大小()-1) 的发射段（元素 返回接收→拷贝构造→句柄存槽 链）→③定点修复（宿主发射·元素 返回接收/类对象局部槽）→④撤插桩+写回单件处置→⑤反证双态+验证链（v2p t4 零错→fix_p→cn_self→t4→0→全量门禁）→⑥集成·B9 扩散=「类对象局部句柄静态值」全库扫描。

> 〔2.135 001 接手复现轮（linux 形态同构性实锤·739-a） 已滚转归档（740-a 棘轮·2026-09-25·内容=develop 435512e4+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.139 055 波1 接收面副本轮（**容器.元素(i) 初始化从「非拥有式视图」改「NewObject+memcpy+析构登记 独立副本」＋表标记转移 同步写回**＝四面矩阵化设计完成后的单端可达波·741 定音 t4→0 唯一路径落地波1）——743-a 开发轮（分支 `任务/深度机-743-055接收面副本`·基点=develop 2e15378e·wt743 隔离·2026-09-25·P0）

**1. 目标与验收判据**：四面矩阵化设计完成（返回面/接收面/传参面/字段写——波序=接收面先行的依据：**单端即可达成 t4→0**，返回面 retbuf 浅值=后续性能优化向独立波）——①接收面改造：ir_stmt_decl.cpp 声明初始化 isContainerElementView 命中形态·从「句柄直存+跳过析构登记（视图）」改「NewObject(类大小)+memcpy 句柄内容→独立堆副本+句柄存槽+析构登记（ownedClassOrder_）」＝**顶层=独立副本 → 写回=合法跨对象拷贝（self-copy 毁源根除）**；②存量语义保持：表标记转移（语义分析.cn:304 `层.设置(名ID, 找到)` 写副本）同步加写回（`符号表.设置(i, 层)`）＝转移标记不再丢失；③**验收**=写回态 v2p 编 t4 rc=0（740 崩消除）→fix_p rc=0→cn_self 构建→cn_self 编 t4 rc=0 零诊断（**t4→0 判据②**）→全量门禁红名单同构（113/127_v2/79_v2 基线）→反证双态（撤接收面改造→崩复现→恢复→绿）。
**2. 写集（预登记）**：`src/cn_compiler/ir/ir_stmt_decl.cpp`（接收面分支）｜`CN语言编译器v2/语义/语义分析.cn`（写回单件+表标记转移 写回·052 甲双件）｜文档六件套。tests 面预期零改动（113/127_v2=已知基线红·随 052/055 后续波清偿）。
**3. 实施步骤**：①ir_stmt_decl.cpp isContainerElementView 命中分支改造（genExpr 取句柄→NewObject+memcpy→Store 槽→析构登记）②表标记转移 加写回（同款 符号表.设置(i, 层)）③写回单件重施加（/tmp 备份）④写回态 v2p 重建+编 t4⑤逐级验证链⑥反证双态。
**4. 验证链**：零警告构建→写回态 v2p t4 rc=0→fix_p rc=0（~636k 行）→cn_self→cn_self 编 t4 rc=0（t4→0）→单测+E2E 全量 --jobs 8（gate_lock）→strict/matrix→反证双态→integrate（win 侧家机异步验收）。
**5. 边缘情况与诚实边界（预判）**：①接收面改拥有式后 **223 行 全局层查类型 等只读形态不受影响**（读副本=读快照）；②表标记转移 加写回后 转移标记 生效路径变化→E2E 相关用例（转移类）复验；③NewObject+memcpy 的 56B×每表注册 次性能开销（编译时长 M2 随验证链记录）；④ **t4→0 达成后 写回单件/元素引用 转正问题**随 052 双件回归②收口（§1.45 四步③）。
**6. 与他机交叠**：家机 738 收工无在飞；单位机空闲；057/550 排队未开。独立 ✓。
**6. 与他机交叠**：家机 738 收工无在飞（701/705~708 挂等/保全·写集零交叠）；单位机空闲；057/550 排队未开。独立 ✓。

**【747-a 阶段回填（引用化实验→T94 阶段二缺口实证·止损收工）】**
- 引用形参实验（113 源码 & 化 4 处）：单测 1405 全绿（零回归）·**113 v2 侧仍 -1**——引用形参下 v2 的 符号表.追加/成员调用 **不生效**＝**T94 阶段二缺口实证**（v2 引用形参容器成员调用的 this 句柄链·「v 槽→表槽→堆对象 少一层」·550 收工回填既知修复方向：语义追踪 ptrLocalPointees_ 判槽指向）——**127_v2 形态独立**（其函数全为引用形参·红在 全局静态容器+容器字段+引用 组合·亦 T94/T100 邻域）。
- **止损决策（744 教训）**：引用化修复依赖 T94 阶段二攻坚（非单点）——实验撤销（checkout·113 红维持基线）——**立案汇总（001 销项链剩余依赖链）**：①**T94 阶段二**（v2 引用形参容器成员调用 this 链·修后 113 引用化即绿）②**056 甲**（容器按值形参 in-out·语义级统一·T102）③**T100**（泛型 T& 返回）④**T101**（拷贝构造分配 NULL 判）⑤**127_v2 静态容器组合缺口**——五件均=052/055/056 域深水·各自独立轮攻坚。
- 747 收工：零代码变更（实验撤销）·纯分析轮·快速门禁·基线红维持 2（113/127_v2）。

### 2.141 055 波2 宿主元素副本轮（**113/127_v2 清偿**＝052 甲域·波2a=宿主 元素 返回发射改深拷副本·波2b=127_v2 v2 侧 206 域）——746-a 开发轮（分支 `任务/深度机-746-055波2宿主元素副本`·基点=develop 17c85c41·wt746 隔离·2026-09-25·P0）

**1. 目标与验收判据**：052 甲副本语义完整落地最后一环——**波2a**：宿主 `容器.元素(i)`（类实例 T）返回发射从「rax 句柄（池内地址·写穿视图）」改「深拷副本」（NewObject+拷贝构造→rax=独立对象·与 v2 副本语义对齐）——**验收**=113 双编译对照双侧一致（v2 副本语义 expected·宿主同步）；**波2b**：127_v2 v2 侧运行退出码 206 域诊断清偿（嵌套容器/容器字段/静态/引用形态）——**总验收**=113/127_v2 双侧转绿→全量门禁红名单收敛（79_v2 已绿·113/127_v2 清偿后=零红向）→反证双态。
**2. 写集（预登记）**：`src/cn_compiler/ir/ir_oop_container.cpp`（元素 返回发射·波2a）｜按 127_v2 诊断定（v2 树 IR 侧·波2b）｜`tests/e2e/`（113/127_v2 expected 若随语义升级需报备）｜文档六件套。
**3. 实施步骤**：①定位 元素 发射点（ir_oop_container.cpp 元素 分派/ir_call）②波2a 改造（T=有拷贝构造类时 NewObject+拷贝构造→返回新对象·标量/字符串保持原路径）③113 单跑双侧验证④波2b 诊断（127_v2 206 复现+定位）⑤全量门禁。
**4. 验证链**：113/127_v2 单跑→零警告构建+单测 1405→E2E 全量 --jobs 8（gate_lock）→strict/matrix→反证双态（撤波2a→113 回归红→恢复→绿）→integrate。
**5. 边缘情况与诚实边界（预判）**：①元素 返回改副本=**全库语义变化**（写穿消费点全变副本·218/155 等 元素 消费用例回归面）——逐例过全量②性能=每 元素 一次深拷（高频消费·v2p/cn_self 编译时长锚记录）③元素引用（550 甲后）不受影响④127_v2 的 206=嵌套容器深拷完整性（波2a 或连带治愈·按诊断）。
**6. 与他机交叠**：家机无在飞；单位机空闲；057/550 排队未开。独立 ✓。

**【746-a 阶段回填①（波2a 实施成功·113 现状=宿主绿 v2 差）】**
- 波2a 实施成功：ir_stmt.cpp visitReturnStmt 插「容器元素方法返回=NewObject+拷贝构造 独立堆副本」分支（function_->name==元素 且 retCanon 是类且有拷贝构造·srcSlot=lvalueAddress）——单测 1405 全绿+写回态 v2p 编 t4 rc=0（与 743 修复共存无回归）+**宿主侧 113 输出 101=期望达成**（波2a 副本语义生效实锤）。
- **113 现状反转**：红=「v2 侧 -1」（宿主 101 ✓）——v2 侧的「取出副本→设置→写回」链中 **写回未生效**＝向量.设置 覆盖写（析构被移除+拷贝）在 v2 发射下的缺陷域＝**与 127_v2 的 206 同族**（波2b 诊断域·v2 树 IR/析构链注入+设置 覆盖写）。
- 下一迭代（波2b）：①v2 侧跑 113 复现 -1→定位 向量.设置 覆盖写 的 v2 发射（CN语言编译器v2/IR/生成语句赋值.cn 698-b 元素写深拷分支+析构链注入）②修复→113/127_v2 双绿→验证链→集成。
- 下一迭代（波2b）：①v2 侧跑 113 复现 -1→定位 向量.设置 覆盖写 的 v2 发射（CN语言编译器v2/IR/生成语句赋值.cn 698-b 元素写深拷分支+析构链注入）②修复→113/127_v2 双绿→验证链→集成。

**【746-a 阶段回填②（113 最终定音·两代按值形参语义分叉＝056 甲域）】**
- 113 源码审读（升级版）：压入/注册/查找 三函数的 符号表 形参＝**容器按值形参**（无 &）——按值语义：注册 全部落在形参副本→主 的原向量不变→查找=-1（v2 深拷独立语义·**v2 自洽正确**）；宿主语义：容器按值形参=句柄浅拷（共享池）→写穿→查找=101（宿主实测）。
- **113 红的最终定性＝非发射器 bug·容器按值形参语义两代分叉＝056 甲（容器形参 in-out 写回/COW·用户 2026-09-24 已裁决）未落地**——expected（101）=宿主共享语义产物；056 甲落地（两代统一）后 113 按统一语义重新定 expected。
- 波2a 产出保持有效：宿主 元素 返回深拷副本＝副本语义对齐·为 056 甲落地奠定同构基础。
- **113/127_v2 清偿路径修正（056 甲承接）**：①056 甲实施（容器按值形参 in-out 写回/COW·025 §1.18/§1.26 既定·跨宿主+v2）→②113/127_v2 按统一语义重定 expected→③双侧绿。127_v2 的 206=同域（嵌套容器+按值形参+静态 交互·随 056 甲一并）。
- 746 轮收工定调：波2a 已实施（宿主 元素 副本·保留）＋113 定音落盘→**746 收工**（波2b 转 056 甲专项·下会话 fresh context 矩阵化实施·025 §1.18/§1.26 既定）。

> 〔2.139 055 波1 接收面副本轮（743-a·t4→0 判据②达成·复制(顶层) 修复） 已滚转归档（748-a 棘轮·2026-09-25·内容 develop c3f04be0+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.140 055 三面收敛轮（**拷贝构造崩面定位与修复**＝052/055 后续波·写回态 cn_self 编 v2 全树 rc=139 消除）——744-a 开发轮（分支 `任务/深度机-744-055三面收敛`·基点=develop 4962be81·wt744 隔离·2026-09-25·P0）

**1. 目标与验收判据**：743 t4→0 达成后全树场景诊断——写回态 cn_self 编 v2 全树 rc=139（解析 126339 节点后语义阶段·740 同形态）·gdb 定位崩点=**映射$整64$符号$映射(映射&) 拷贝构造 内 bb246_30+300**（fix_p .s 反查·57920 行·所属 globl 56975）——本轮=①崩点指令级对位（拷贝构造内哪个循环/访存）②坏状态溯源（this/源 引用形参·输入对象状态）③矩阵化修复（拷贝构造/传参面）④**验收**=写回态 cn_self 编 v2 全树 rc=0（崩消除）→fix_s 产物→67157→0 收敛推进→全量门禁→反证双态。
**2. 写集（预登记）**：`src/cn_compiler/ir/`（按崩面对位实测定·候选=拷贝构造发射/传参面）｜文档六件套。
**3. 实施步骤**：①崩点 .s 对位（bb246_30+300 指令→拷贝构造源码行·映射集合.cn:133-176）②坏输入溯源（gdb 断 拷贝构造入口·dump this/其他 字段态）③修复④验证链。
**4. 验证链**：写回态 cn_self 编 v2 全树 rc=0→fix_s 产物→语义错误数对比（67157 基线）→t4→0 维持→单测+E2E --jobs 8→strict/matrix→反证→integrate。
**5. 边缘情况与诚实边界（预判）**：①cn_self=第二代二进制·符号=fix_p 产（bb 局部标签需 .s 反查·已建方法）②崩点可能=拷贝构造的 分配/循环/字段写入 三处之一③修复若触及 052/055 传参面=矩阵波②④win 侧家机异步验收。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。

**【748-a 阶段回填①（诊断完成·v2p 错值复现+接收链结构差异实锤）】**
- **113 引用化（&形参 4 处·已随 0905fbad 入库=波1 目标形态保留）两侧对照实测**：①**cn.exe 发射产物（wk/target/主.o→主_exe）＝运行完全正确**（101/102/-1/202/303/404/恢复链/最终大小 0·与 expected 逐行一致）＝**cn.exe 引用形参发射无缺陷**；②**v2p（develop v2p_fix）发射产物＝全 -1 复现**（注册不生效·压入 生效＝大小 2/3 对）——**两代发射差异实锤·真战场=v2p 的代码生成**。
- 调用方装配双端核对：主() 调 压入＝lea 槽地址（引用 ✓）·被调 装载＝Load 解引（✓）——**引用传递协议本身无缺陷**。
- **v2p 的 顶层=元素(i) 接收序列捕获**（v2asm.s 注册 函数 8358 起）：元素_J 调用→**op42×7（句柄所指内容 7 字段展开存储）**→op43 取址→op61→__cn_object_new→**call 映射$整64$符号$映射_A_T$…（拷贝构造）**→获取_J——**多层拷贝链**（栈槽展开→结构体拷贝→NewObject→拷贝构造）·与 cn.exe 单段形态**结构性差异**——错值段在多层链中（逐指令对位=下迭代）。
- 下一迭代：①v2p 注册 的 顶层 接收多层链逐指令对位（源/目标寄存器与槽）②定位错值段（op61 结构体拷贝的源/目标错位·或 展开存储 的源句柄误用）③修复（v2 树 IR/生成返回语句 或 生成声明 的接收链）④113 引用化 v2 侧绿→验证链→集成。
- 下一迭代：①v2p 注册 的 顶层 接收多层链逐指令对位（源/目标寄存器与槽）②定位错值段（op61 结构体拷贝的源/目标错位·或 展开存储 的源句柄误用）③修复（v2 树 IR/生成返回语句 或 生成声明 的接收链）④113 引用化 v2 侧绿→验证链→集成。

**【748-a 阶段回填②（错位 100% 定位·修复方案锁定）】**
- v2p 注册 的 顶层 接收序列逐指令解读（v2asm.s 8425~8475 纯指令）：①元素_J→rax=句柄（池[0] 地址）②`mov [rbp-104],rax`→**rep movsb 56B：栈槽[rbp-504] ← 池[0] 内容**（retbuf 式浅拷·第一步正确）③**__cn_object_new→新堆对象**④`mov r10,[rbp-112]（retbuf 地址）→[rbp-512]→lea r10,[rbp-512]→rsi=[rbp-128]`＝**拷贝构造的「其他」实参被装成「&存 retbuf 地址的槽」（二级指针）**——**应=retbuf 槽地址（一级）**——拷贝构造 读 其他.字段=从「retbuf 地址值」的各字节段当字段读＝**字段全错位**（740 铁证3 形态的真因·两代同一根）。
- **修复方案（锁定）**：v2 树生成层「类实例调用返回的声明接收」——**拷贝构造的「其他」实参=retbuf 槽地址（一级·即 [rbp-112] 的值·去掉 `mov [rbp-512]/lea` 的二次间接）**——定位=v2 树 生成语句赋值.cn（或 生成声明.cn）的「调用源→源指针」装配段（源=结构体调用返回时的临时槽地址二次取址逻辑）。
- **cn.exe 对照勘误**：cn.exe genCopyBuiltin 的 CallExpr 分支（hold 槽+AddrOf）＝**同构二级形态**（t4 未触达因 113 顶层=IdentifierExpr 走 lvalueAddress 分支）——**修复应两代同改**（v2 树 生成语句赋值.cn＋cn.exe genCopyBuiltin CallExpr 分支：CallExpr 返回句柄=地址·直接作 T& 实参·删 hold 二次间接）——B9 扩散=「调用返回源+拷贝构造」组合全域扫描。
- 下一迭代：①v2 树定位「调用源→临时槽→二次取址」发射代码（生成语句赋值.cn 308~355 段扩展）②删二次间接③cn.exe genCopyBuiltin 同修④113 引用化 v2 侧绿+回归⑤验证链→集成。
- 下一迭代（定位修正）：错位段=**生成声明.cn 的「类声明初始化=调用返回接收」通道**（113 的 顶层 是声明初始化·非赋值语句——生成语句赋值.cn 287~355 段已排除·其源指针装配为标识符源形态）——v2p asm 序列（元素_J→rep movsb 56B 栈槽→__cn_object_new→call 拷贝构造(新对象, &存retbuf地址的槽)）＝该通道的「临时栈槽→二次取址」实参装配——修复=拷贝构造「其他」实参改 retbuf 槽地址（一级）·cn.exe genCopyBuiltin CallExpr 分支同构同修——②113 引用化 v2 侧绿+回归⑤验证链→集成。
- 下一迭代：①v2 树定位「调用源→临时槽→二次取址」发射代码（生成语句赋值.cn 308~355 段扩展）②删二次间接③cn.exe genCopyBuiltin 同修④113 引用化 v2 侧绿+回归⑤验证链→集成。

**【748-a 阶段回填④（改造已实施·113 配套写回待加）】**
- 改造已实施（生成语句.cn 455 分流处·元素 方法专用分支）：`函数信息.名字==驻留("元素") && 返回结构体ID!=0 && 查结构体大小>0` → **IR_分配对象+NewObject、调用实参(this=新对象·源=值寄存器 T&)、IR_调用参数化 拷贝构造（方法符号 API 与生成语句赋值:344 逐参一致）→值寄存器=新对象**——v2p 重建 rc=0（CN 编译验证 API 形态正确）。
- **113 引用化实测仍 -1**＝**副本语义下「无写回」的正确行为**（顶层=NewObject 独立副本·顶层.设置 写副本·无写回=丢弃·查找=-1）——**113 源码还需配套写回行**（`符号表.设置(符号表.大小()-1, 顶层)`·747 撤销的写回实验在 T94 改造后：顶层=NewObject 独立副本≠池[0]·写回=合法跨对象拷贝·self 毁源已根除）——**下迭代=113 加写回行→t4→0 全链绿→验证链**。
- 附注：743 的 复制(顶层) 写回修复（已入 develop）与 113 的写回=同一模式（独立副本+写回＝052 甲语义完整闭环）。
- 附注（abb8c9bc 勘误）：该提交实际携带=**波2a v2 树镜像改造本体**（生成语句.cn 455 分流处·元素 方法 NewObject+拷贝构造 22 行·提交信息误标「回填⑤」）＝**核心改造已入库**；写回行实验（113 加 符号表.设置(0,顶层)）→宿主侧产物运行 -11 段错误＝**向量.设置 覆盖写链（析构被移除+拷贝构造）在「引用形参+副本写回」场景仍有缺口**（740 watch 同域）——**止损撤销写回行**（基线红 2 维持·零实验态残留）。
- **结论沉淀**：①「self/跨对象覆盖写的析构顺序」缺陷面未随副本化自动消解——**覆盖写通道（向量.设置）的「先拷贝后析构」改造**＝056 甲 in-out 落地的一部分（容器.cn 设置 改 copy-swap·T102 扩展）②T94 改造（元素 返回副本）本身正确且必要③**113/127_v2 清偿依赖链最终形态三件递进**=T94 改造（已实施✓）+覆盖写 copy-swap+056 甲 in-out 统一——下会话按序实施。
- 748 收工：生成语句.cn 改造+113 & 化入库（波1 形态）·写回实验撤销·基线红 2 维持·纯文档+tests 收工。
- 下一迭代（定位修正）：错位段=**生成声明.cn 的「类声明初始化=调用返回接收」通道**（113 的 顶层 是声明初始化·非赋值语句——生成语句赋值.cn 287~355 段已排除·其源指针装配为标识符源形态）——v2p asm 序列（元素_J→rep movsb 56B 栈槽→__cn_object_new→call 拷贝构造(新对象, &存retbuf地址的槽)）＝该通道的「临时栈槽→二次取址」实参装配——修复=拷贝构造「其他」实参改 retbuf 槽地址（一级）·cn.exe genCopyBuiltin CallExpr 分支同构同修——②113 引用化 v2 侧绿+回归⑤验证链→集成。

**【748-a 阶段回填③（T94 修复本体定位·v2 树镜像缺失实锤）】**
- 生成容器局部（CN语言编译器v2/IR/生成声明.cn:449+）接收形态判定源码：`顶层 = 符号表.元素(...)` →**是返回接收=真→「接收转移：rax 直接入槽（不 NewObject·RAII 不登记·视图/转移语义）」**＝**v2p 的 顶层=retbuf 栈临时**（元素 的 structReturn retbuf 浅拷）——顶层.设置 写 retbuf 对象→**不写回池[0]**→注册不生效（-1）＝**v2p 错值机理完整闭合**（740 铁证3 的「四数组 NULL」＝另一独立面 T101·本根因=接收转移语义）。
- **T94 修复本体（波2a 的 v2 树镜像）**：cn.exe 波2a 已改 visitReturnStmt（容器元素返回=NewObject+拷贝构造·独立堆副本）——**v2 树的对应物=生成返回语句（CN语言编译器v2/IR/生成语句.cn 或生成声明 内）缺同款**：容器实例返回现=rax=数据[位置] 句柄直返（视图）→**加 NewObject+拷贝构造→返回新对象句柄**（对齐 cn.exe 波2a）＝v2p 的 元素 返回独立副本→表注册 副本写+复制(顶层) 写回→**t4→0 维持+113 引用化绿**。
- 下一迭代：①v2 树 生成返回语句 定位（grep 生成返回语句/IR_返回）→②容器类实例返回加 NewObject+拷贝构造③113 引用化 v2 侧绿验证→t4→0 维持核验→全量门禁→反证→集成 748。
- 下一迭代（修复点锁定）：**生成返回语句（生成语句.cn:254+）的「返回结构体值」retbuf 路径**（:302 判定=返回结构体ID≠0 或 返回结果名ID≠0 且有返回表达式·retbuf 拷贝字节数分流）——v2 树改造点=该路径内「容器类实例（返回结构体ID=类）且有拷贝构造」时 **NewObject(类大小)+call 拷贝构造(新对象, 源地址)→返回新对象句柄**（对齐 cn.exe 波2a·源=lvalueAddress(下标/成员链)）——替换现「源句柄浅拷 retbuf」段。
- 下下迭代：改造后重建链（v2p 重建→113 引用化 v2 侧绿→fix_p≡fix_s 维持→t4→0 维持）→全量门禁→反证（撤改造→113 引用化红复现）→集成 748。

**【744-a 诊断数据（开工即得·gdb+.s 双对位）】**：崩点=cn_self_fix（写回态第二代）0x4341d2=bb246_30+300·.s 反查=**映射$整64$符号$映射(映射$整64$符号&) 拷贝构造体内**·rsp=[0]=0（栈顶返回地址读 0=或栈已破坏）。fix_p 产物=cnself_fix/target/v2asm.s（636564 行·743 产）。诊断入栈=wt744 延续 743 工件（target/p743/）。

**【744-a 阶段回填①（崩点指令级对位完成·缺陷形态实锤）】**
- objdump 对位（cn_self_fix bb246_30 块·0x43409f 起）：崩点 0x4341d2 `mov %r11,(%r10)`——序列=「[rbp-0x660] 装载→写 [r10]」前有 `add r10,16`+`[r10]` 载入＝**this.链数组（映射 offset16）**·.s 源（57920~）对应=**拷贝构造 链数组循环 `链数组[链索引] = 其他.链数组[链索引]`**（映射集合.cn:170-175）。
- **缺陷形态实锤**：读侧完好（其他.链数组[链索引] 读出值）·**写侧 this.链数组=NULL**（写 [NULL+链索引×8] 崩）＝**「链数组 = 分配(数组容量*8);」的字段写丢失/槽错位**（拷贝构造体内 this.链数组 赋值未生效）——**585-a 修复②「帧预留缺失→槽错位」同族确认＝055 传参/字段写面缺陷点**。
- **修复方向（下迭代）**：①对位 .s 拷贝构造体内「链数组 = 分配(...);」的发射（this.链数组 字段写的 目标槽 vs 拷贝构造 this 形参装载——cn.exe ir_expr_assign/ir_fields 的 成员链字段写 路径）②对照 t4 场景（cn_self 编 t4 的拷贝构造正常）与全树场景（崩）的差异输入③修复④验证链。
- 工件：wt744 延续 wt743 target/p743/（v2p_fix/cn_self_fix/fix_p .s）。

**【744-a 阶段回填②（重大突破·三判据连锁达成+防线误伤勘误）】**
- **决定性实验**：去 ulimit 6GB 重跑 写回态 cn_self 编 v2 全树 → rc=0（1m22s·语义错误 0）——740/741/743 的「编全树 rc=139 崩」＝**ulimit 6GB 内存防线误伤**（编译全树峰值 >6GB·拷贝构造的 键/值/链数组 分配（无 NULL 判）返回 NULL→循环写 [NULL+i×8] 崩＝740 铁证3「四数组 NULL+计数残留」的真因）——回填①「字段写丢失/槽错位」假设撤销（勘误）。
- **三判据连锁达成**：①写回态 v2p 编 t4 rc=0（743）②**fix_s 67157→0**（cn_self 编 v2 全树 rc=0·语义错误 0）③**-7 固定点＝fix_p ≡ fix_s 逐字节一致**（md5 双侧同=94a9e15584e00b7def3d5a6953d37633·636564 行·/tmp/fixpoint_743.md5 留档）——**78_v2/79_v2 自举闭环在写回态首次全达成**！
- 附带健壮性立案（T101 候选）：映射/向量 拷贝构造的 键/值/链数组 分配无 NULL 判（桶数组有判）＝分配失败即崩（OOM 防线场景）——语言层健壮性·独立小修。
- **下一迭代**：①全量门禁（E2E 全量含 79_v2 在 runner 内存配置下的真实结果·单测·strict/matrix）②若 79_v2 因 runner 内存上限假红→调 runner 内存名单（79_v2 类）③反证+集成 744。



## 〔滚转追加〕2.144 113 清偿轮（749-a·已集成 develop·2026-09-25·753-a 棘轮滚转）

### 2.144 113 清偿轮（**用例重写为当前已实现语义＋expected 重固化**＝746 波2b 顺延·052 甲副本语义验证落地·测试升级报备）——749-a 开发轮（分支 `任务/深度机-749-113清偿`·基点=develop 4bf11663·wt749 隔离·2026-09-25·P0）

**1. 目标与验收判据**：113_H8 用例重写——**避开未实现的按值形参 in-out（056 甲域·T102）**·改「主局部直接操作」形态验证「向量<映射> 容器持有类对象」核心能力（追加 类元素深拷独立/元素 读取/设置/获取/删除/大小）——**双侧实测固化 expected**（宿主 cn.exe 与 v2p 各自输出→一致才固化＝双编译对照本义）——**验收**=113 双侧绿（单跑）→E2E 全量 528=529/0/2 向（113 转绿净增·新增重写用例计数随实测定）→单测/strict/matrix→反证→集成。
**2. 写集（预登记）**：`tests/e2e/113_H8_容器持有类对象/`（主.cn 重写+主.expected 重固化·测试升级报备随轮）｜文档六件套。**零 src/零 v2 树**。
**3. 实施步骤**：①重写 主.cn（全局/主局部直接操作·覆盖 追加/设置/获取/大小/删除 能力面）②宿主 cn.exe 编译运行→输出 A③v2p 编译运行→输出 B④A==B→固化 expected；A≠B→差异=新缺陷立案（不固化）⑤全量门禁。
**4. 验证链**：113 双侧绿→单测 1405→E2E 全量 --jobs 8→strict/matrix→反证（重写前形态红复现对照）→integrate。
**5. 边缘情况与诚实边界（预判）**：①重写后 113 验证能力面缩减（原 遮蔽/恢复/传参 面=依赖 in-out/覆盖写修复·随 056 甲落地后以新用例恢复·立案 T102 关联）②「设置 后 获取」在副本语义=快照读 ✓ 两代一致预期③追加（向量 深拷 独立）✓ 已验证形态④127_v2=独立诊断（本轮第二段或下轮）。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。

**【749-a 阶段回填①（重写完成+双侧实测·差异收敛到单值）】**
- 113 重写完成（全局/主局部直接操作·覆盖 设置/获取/大小/追加/元素/删除 能力面·原 传参面 随 056 甲恢复·报备随轮）。
- **双侧实测**：宿主（cn.exe）与 v2p（develop v2p_linuxx64）编译运行重写版——**11 行逐行一致**（设置 真×2/层大小 2/获取[1]=101/获取[3] 假/追加 真/向量大小 1/元素[0].大小 2/删除 真/大小 0）·**单点差异**＝「元素[0][1].类型ID」：宿主=101·**v2=0**。
- **差异定性＝追加 深拷（向量.追加(映射)）的值数组内容拷贝缺失**（672-a「映射拷贝构造值数组拷贝缺失」同型·v2p 场景再现）——键命中（获取(1).正常=真）而 值.类型ID=0＝**键数组拷贝✓·值数组[0] 内容未拷**——**052/055 域残留单点·修复位置=容器.追加 的 T=映射 结构体赋值发射（值数组[元素数量]=值 的 拷贝构造/字段拷）**。
- 中途勘误：①重写版初版用「查1.值.类型ID」二级成员链→v2 IR 层「成员字段偏移未知」＝**134 家族成员链深度缺口再现**（已立案域）→改单层形态（符号 找到=查.值）②产物 cwd=wt749 根（v2p 输出相对 cwd·非 wk）。
- 下一迭代：①定位 容器.cn 追加 的「值数组[元素数量]=值」在 cn.exe 编译下的发射（结构体赋值→拷贝构造调用/字段拷·746 波2a 同族）→②修复（副本深拷完整）→③「元素[0][1]」行双侧一致→113 expected 固化（12 行）→④全量门禁→集成。
 - 下一迭代（对照深化）：①追加 的「值数组[元素数量]=值」发射对位（cn.exe 编译 容器.cn 的 拷贝构造值数组段·宿主/v2p 同机器码）②运行时状态对照（值数组[0] 32B 全零的写入时机）③修复→expected 固化→集成。

**【749-a 阶段回填②（对照深化·差异收敛到运行时状态）】**
- 事实链精化：v2 输出「向量大小=1/元素[0].大小=2/获取(1).正常=真」＝**键数组[0]=1 ✓ 拷贝 ✓ 元素数量=2 ✓**·唯「值.类型ID=0」＝**值数组[0] 的 32B 全零**（拷贝构造 源=全局层·其 值数组[0] 在 v2p 运行时=全零）——**设置 返回正常（元素数量++）但 值数组内容未写入**＝**设置 的「值数组[已有位置]=值」结构体赋值在 v2p 场景未生效**（或写后被清）。
- **两代「同机器码」悖论的解**：容器 方法体=cn.exe 编译 容器.cn（宿主 113 与 v2p 同源）——**行为分叉必来自运行时状态差异**：①**扩容段的 重新分配 返回**（v2p 堆状态·分配清零 vs 宿主复用）②**设置 的 值数组[已有位置]=值 的 写入基址**（v2p 的 全局层=NewObject 对象·其 值数组 字段加载链）③**113 的 全局层 构造**（v2p 发射 vs cn.exe 发射·构造后字段完整性）——gdb 内存 watch 值数组[0]（写入/清零时机）可一次定位。
- 下一迭代：①gdb watch 全局层.值数组[0]（v2p 场景）抓写入/清零序列②定位后修复③expected 固化→全量门禁→集成。
- 下一迭代：①gdb watch 全局层.值数组[0]（v2p 场景）抓写入/清零序列②定位后修复③expected 固化→全量门禁→集成。

**【749-b 阶段回填（E2E 复测·状态精确化）】**
- **主树（develop·748 T94 改造集成后）E2E 113 单跑复测**：「期望 全局[1]:101 / 实际 初始大小:0」＋「D2a 宿主侧 运行输出不一致」——**确认两事实**：①develop 的 113 源码=**重写版**（749 集成 6b541a3e 携带·「初始大小:0」=重写版第 1 行输出）②**expected=旧 101 形态未随重写版固化**＝**红的主因=expected 滞后**＋「元素[0][1].类型ID」v2=0 宿主=101 单点差异待修（追加 深拷的值数组内容拷贝缺失·672-a 同型）。
- **清偿精确路径（三步）**：①修「追加 深拷值数组内容拷贝」单点（v2p 的 拷贝构造/结构体赋值 发射·定位=生成复制展开 ④ 物化 或 容器.cn 追加 的赋值发射——T103 域）→②expected 重固化（12 行·以双侧一致为准）→③113 绿→E2E 全量（528=528/0/2 向）→集成。
- 并行推进：127_v2（206·静态组合）独立诊断、057/550 按甲。
- 下一迭代：①gdb watch 全局层.值数组[0]（v2p 场景）抓写入/清零序列②定位后修复③expected 固化→全量门禁→集成。

**【749-a 阶段回填③（探针二分·问题锁定在 追加 的拷贝构造值拷贝）】**
- **「设置 后立即获取」探针**（双侧）：**v2p=101 即时正确**（设置 即时生效 ✓）——问题收敛到 **追加(全局层) 的拷贝构造值拷贝**：`值数组[值索引] = 复制(其他.值数组[值索引])` 的 **「复制(符号 结构体)」＝genCopyBuiltin 无 struct 分支**（⓪ 调用返回/① 字符串/② 标量指针/③ 类对象——struct 32B 四不靠→返回垃圾＝值[0].类型ID=0）——**cn.exe 缺口实锤（T103 候选·genCopyBuiltin struct 分支）**。
- **待解悖论**：宿主 113 的 副本.获取(1)=101 正确·与 v2p 同「追加」来源——下一步 **objdump 对比两产物 追加 函数机器码**（同码=运行时状态·异码=编译分叉）。
- 下一迭代：①objdump 对比 追加 机器码→②修复→③expected 固化（含「立即[1]」探针行·已入源码）→④全量门禁→集成。
- 下一迭代：①objdump 对比 追加 机器码（宿主 113 产物 vs v2p.o）→②若异码=定位编译分叉（注入版差异）→若同码=gdb watch 运行时③修复→expected 固化→集成。

**【749-a 阶段回填④（悖论破解·根因定音＝空检查防线 v2 版缺失·T104 立案）】**
- **objdump 对比实锤（两产物「追加」机器码完全不同）**：宿主 113 产物（cn.exe 直接编译·0x4043cc）＝**有空检查防线**（test/jne→__cn_runtime_error(3)·逐层指针解引防护）；**v2p 产物（0x402ded）＝无空检查**（直接 mov/加/load）——**「同机器码」悖论破解：两产物容器方法=不同编译产物**（宿主 113 的容器方法=cn.exe 编译 113 时源码级一并编译·含空检查防线；v2p 的=v2p 构建时编译·**「宿主空检查防线 v2 版全无」**＝**714-a 定性原话实锤**）。
- **根因链完整闭合**：v2p 的 容器方法无空检查防线→**设置 的 扩容/写入段在无防护下静默失败**（值数组[0] 全零·元素数量++ 却）→追加 拷贝构造 从坏源拷→副本 值=0→**元素[0][1]=-1 域**＝113 红·127_v2 的 206=同族（静态容器组合无防线）——**T104 立案（空检查防线 v2 版补全·语言层健壮性·逐方法注入 test/jne）**。
- **749-a 收工定调**：113 重写（值语义形态·报备）+双侧对照+差异单值定位+根因定音（T104）全完成——**113 expected 固化与绿=T104 修复后**（空检查防线补全前 v2p 无法正确执行 设置 写入）·749 保持未集成（expected 未固化·实验探针行已入源码随 T104 一并处置）·基线红 2 维持。
- **001 销项链依赖链最终排序（T104 承接后）**：**T104（空检查防线 v2 补全）**→113 引用化/重写形态绿→127_v2 静态组合→零红→借链 51 例终章→001 销项；并行独立=T101（拷贝构造 NULL 判）/T102（056 甲 in-out）/T100（泛型 T&）。


## 〔滚转追加〕2.138 055 宿主 ABI 手术分析轮（742-a·纯分析·已收工·2026-09-25·755-a 棘轮滚转）

### 2.138 055 宿主容器类实例值语义 ABI 手术（**元素 返回/声明接收 双段模型**＝052 甲落地核心·741 定音 t4→0 唯一路径·025 §1.13 既定六要素承接）——742-a 开发轮（分支 `任务/深度机-742-055宿主ABI手术`·基点=develop dd6c47fb·wt742 隔离·2026-09-25·P0）

**1. 目标与验收判据**：宿主容器类实例从「指针返回模型」（元素 返回 rax 句柄+声明接收句柄直存·688-b 实证）改为**双段模型**（元素→retbuf 浅值按值返回+声明接收 NewObject+拷贝构造→句柄存槽·v2 同构）——改造两点=①元素返回发射（ir_stmt.cpp return 对容器类）②声明接收（ir_stmt_decl.cpp 生成容器局部 返回接收）——**验收**=①写回态（§1.45 复现链）v2p 编 t4 rc=0（740 崩复现消除）②打补丁 v2p 编 v2 全树 rc=0→cn_self 构建→cn_self 编 t4 rc=0 零诊断（**t4→0 判据②**）③113/127_v2 双侧对照绿④全量门禁红名单同构⑤反证双态。
**2. 写集（预登记）**：`src/cn_compiler/ir/ir_stmt.cpp`＋`src/cn_compiler/ir/ir_stmt_decl.cpp`（两主改造点）｜连带=ir_expr/ir_oop 调用侧接收形态（按实测定）｜`CN语言编译器v2/语义/语义分析.cn`（写回单件实验态）｜文档六件套。
**3. 实施步骤**：①元素 返回路径审查（ir_stmt.cpp return 对容器类·现=rax 句柄）→改 retbuf 按值（浅值=56B 结构体复制）②声明接收路径（ir_stmt_decl.cpp 生成容器局部·现=句柄直存）→改 NewObject+拷贝构造→句柄存槽③写回单件重施加（/tmp 备份）④逐级验证⑤B9 扩散=元素 返回全部消费点扫描。
**4. 验证链**：写回态 v2p t4 rc=0→fix_p rc=0→cn_self 构建→cn_self 编 t4 rc=0（t4→0）→零警告构建+单测+E2E 全量 --jobs 8（gate_lock）→strict/matrix→反证双态→integrate（win 侧家机异步验收）。
**5. 边缘情况与诚实边界（预判）**：①**大手术**——容器类实例=映射/向量/自定义容器 全族·两代发射交织·按 705-a「fresh context 专项」预期多轮②T80/T70 链已修面（517-a 元素内联口径）与本次改造的交互需逐点核③性能锚（asm 访存/编译时长）随验证链记录④单轮完不成则按波次接力（每波独立可验证）。
**6. 与他机交叠**：家机 738 收工无在飞（挂等/保全·零交叠）；单位机空闲；057/550 排队未开。独立 ✓。

**【742-a 阶段回填①（源码审查·改造模板定位）】**
- visitReturnStmt（ir_stmt.cpp:21~135）结构已清：①genOwnedAggregateReturn（85-a·借用来源物化副本→endReturn(地址)）＝**现成改造模板**（retbuf 式按值返回先例·ownedRetAddr 形态）②isRefReturnFn 分支（地址返回）③一般形态 value=genExpr→endReturn。
- **波1 切口定案（最小可验证）**：返回类型=容器实例 且 值=下标/成员链（内部元素·句柄形态）→**仿 85-a 模式**：lvalueAddress(源)→结构体浅拷到 retbuf→endReturn(retbuf)＝「retbuf 浅值按值」；接收侧 ir_stmt_decl.cpp 生成容器局部·初始化=调用返回（retbuf 形态）→NewObject+字节拷贝 retbuf→堆对象→句柄存槽。
- 判定谓词候选=「isClassType(retType) 且 是容器类」（复用 IR容器.cn/容器变量们 的判定·或 类有拷贝构造+析构=拥有型）——实施时先跑「现状汇编对照」（元素 返回的 rax 句柄 vs 改造后 retbuf）锚定差异。
- 下一迭代：①ir_stmt_decl.cpp 生成容器局部现状段审查②改造①②③写回单件重施加（/tmp 备份）→写回态 v2p t4 验证→逐级验证链。

**【742-a 阶段回填②（接收侧改造点精确定位·语义收益定音）】**
- ir_stmt_decl.cpp:229~234 H8-5 段=接收侧改造点：`类名 顶层 = 容器.元素(i)` 现状=**isContainerElementView 判定为「非拥有式视图」（句柄直存+跳过析构登记）**——055 双段模型改造=**恢复拥有式（NewObject+拷贝构造+析构登记 ownedClassOrder_）**→**「写回」从 self-copy 变为合法跨对象拷贝 → t4→0 可达（语义收益定音）**。
- 改造基础设施既有：514-a/517-a 元素钩子注入（ir_oop_container.cpp·注入 元素析构链/移动体）＋727 家机 元素引用 实验资产——改造=在 isContainerElementView 命中形态改为「深拷独立对象」（发射层·ir_stmt_decl 的初始化接收分支）。
- 下一迭代：①isContainerElementView 判定函数定位（ir_stmt_decl.cpp/语义层）→②接收分支改造（NewObject+拷贝构造+析构登记）③写回单件重施加→写回态 v2p t4 验证④逐级验证链+反证。

**【742-a 阶段回填③（改造原语定位）】**
- isContainerElementView（ir_oop.cpp:562）判定=「CallExpr·成员名=元素·接收者前缀 向量$/链表$/栈$/队列$」——t4 场景命中 ✓（判定本身无缺·改造=命中后的发射形态）。
- **改造原语定位＝genOwnedAggregateReturn（ir_fields.cpp·79-a「物化独立副本」单一归属）**：签名 bool(Expr*, SourceLocation, std::string& ownedAddr)——「memcpy+字段级 __cn_str_copy 独立副本→ownedAddr」——**正是「源→独立副本地址」现成原语**（visitReturnStmt 已在用·85-a 模式）。
- 波1 实施形态定稿：ir_stmt_decl.cpp 声明初始化 isContainerElementView 命中分支→调 genOwnedAggregateReturn(源)取 ownedAddr→NewObject(类大小)+字节拷贝 ownedAddr→堆对象→句柄存槽+析构登记（ownedClassOrder_）——四步·全部原语既有（NewObject=IR_分配对象·字节拷贝=IR_结构体拷贝·登记=ownedClassOrder_.push_back）。
- 下一迭代（续作）：①写波1 diff（四步·ir_stmt_decl.cpp 单文件）②编译+写回态 v2p t4 验证③fix_p→cn_self→t4→0→全量门禁④反证双态→集成 742。

**【742-a 阶段回填④（波1 切口重设计结论·防打地鼠）】**
- 深查后修正：波1「仅改声明接收」为**伪切口**——顶层 接收形态改变（句柄→栈副本/内联）将**牵动其全部后续使用点发射**（顶层.设置 的 this 形态、写回 实参形态、作用域析构）＝**四面联动模型切换**（声明接收/返回/传参/字段写）——单动一面必破坏另一面＝705~719 打地鼠重演风险实证预警。
- **055 正确实施形态（下会话 fresh context 完整设计）**：①先定「双段模型」的完整规范面（哪四面各改什么·矩阵化）②每波=一个面收敛+全量回归③波序建议=返回面→接收面（依赖返回形态）→传参面→字段面④每波带 asm 对拍+性能锚。
- 742-a 本轮实际产出：审查完成（visitReturnStmt/isContainerElementView/genOwnedAggregateReturn 原语/H8-5 接收点·四点全定位）+改造模板确认（85-a）+四面联动设计约束实证——**零代码变更**（防止未设计完整的模型切换入库）·纯分析轮。工作树净·验证链=快速门禁。


## 754~768a 发射层攻坚实验过程段（768 续棘轮滚转·2026-09-26·终局=主文件 §2.147【768 续终局回填】·「读侧 T94」定性已推翻勿据此接力）

**【755-a 阶段回填（修复①②维持生效·崩链再推进＝写通道二次覆写实锤·实验态分支保全·不集成）】**

**新增铁证（gdb 三次 hit 全数据+watchpoint）**：113 v2 产物映射拷贝构造 3 次调用——HIT1（追加 形参拷贝·this=0x693420·源=栈槽 ✓ 装配正确·产物键数组=alloc 值 ✓）→**HIT1 产物键数组字段被二次覆写**（watch：写1=alloc 值 ✓·后续 0x695520 覆写＝**写通道错位族实锤**——某个「元素写/字段写」把句柄值写进了产物键数组字段）→HIT2（元素 方法内 748-a·源=池[0] 槽 ✓ 装配正确·但壳=被污染的 HIT1 产物→X2.数组容量=垃圾→分配巨大→循环崩 rc=-11）。**排除面扩**：发射类元素拷贝构造写/生成下标左值裸字段分支（this.数据 读一层解引 ✓ 步进 56 ✓）装配均正确——**病灶=「某写通道的地址计算/字段偏移错位」对产物键数组字段的二次覆写**（749 披露 T104 空检查缺失使该错位静默）。

**【755-a 追加勘误（watch 扩面复核·崩点 40644a 对位）】**：「二次覆写」定性**作废**——watch 实测 HIT1 产物键数组字段恒定（写1=alloc 值后无覆写·「写2」触发实为 SIGSEGV 非 watchpoint）；崩点=bb24_12 右侧第二层解引读出 **壳[0]=1**。壳[0]=1 的两候选待 756 第一步一次 gdb 定案：读壳[0x28]（元素数量）——A 壳=合法映射但键数组=垃圾（其产物轮坏·向上追一层）；B 壳本身=错位读值（源槽语义/层数问题·回到装配）。**【755-a 终批数据（756 第一步预执行完成）】**：崩点读壳[0x28]（元素数量）=**145（垃圾·全局层实为 2）**·壳[0]=1·壳[0x20]=0·壳[0x30]=1——**定案=候选 A：壳=带病映射副本（HIT1 产物·拷贝构造写出的字段部分垃圾）**——「形参拷贝构造（装配正确）产物字段含垃圾」指向**拷贝构造体内读源（全局层）或写 this 的字段级错位**（T94/字段偏移族·候选=泛型实例 K 的「元素数量@0x28」读写错位）。**【756-a 前哨实验（源/产物双好·精确修复点锁定）】**：gdb finish 对照＝**全局层（源）与 HIT1 产物双完好**（键/值/链/桶/桶数=1024/元数=2/容量=4 全对）——**拷贝构造本体无罪·755「候选 A」的「产物字段级垃圾」修正为：崩点读的「壳」=HIT2 源槽（=「数据[0] 的值」＝句柄/X1 对象地址）少一层**——asm 铁证：HIT2 rsi=0x693420=X1 对象地址（应为「数据[0] 槽地址=池数组基址」）＝**「生成下标左值地址(数据[0])」多发一层解引·返回了「元素的值（句柄）」而非「元素槽地址」（T94 精确点）**。**修复点=生成下标左值裸字段分支（生成表达式.cn:915+）**：指针字段 IR_指针加载 后对「下标链左值」的返回值语义核对——预期=「基址+下标×步进」（地址）·实测多一层。**【756-a 实施回填（修复③尝试→引入回归→已撤销·维持 754 修复①②态）】**：①748-a 位1 改物化源句柄槽（发射类元素拷贝构造写 同款）→113 仍 rc=-11·崩点**推进至 bb24_6 桶段循环**（r10=0x7ffffffff000＝桶索引×8 越界形态·源桶数量=垃圾→循环越界读）＝环节又推进但物化式**引入新回归**（崩提前到断点前）→**已撤销**（生成语句.cn checkout 回 8fe2e556·113 崩点回到 bb24_12 循环·754 已验证形态）。②756-c 实验（源/产物 finish 双好）维持有效。**757 轮精确接力**：a) gdb 断 HIT2 拷贝构造头三句「桶数量=其他.桶数量」读·逐槽追「壳+0x20 读出 0」的数据流（壳=X1·X1[0x20]=1024·读出 0＝**解引层数或偏移错位单点**·两候选各一个 gdb 实验可分）→b) 按 a 定案修 IR 发射→c) 113 rc=0 对齐 101→反证双态→全量门禁→integrate。**教训**：物化式改造前必须先定「值寄存器语义」（句柄 vs 槽地址）——盲改即回归。→③T104 空检查防线（749 立案·静默→可观测）→④113 rc=0 对齐 101→反证双态→全量门禁→integrate。
**【757-a 实施回填（两件确定性修复落地·实验态分支不集成）】**：①**发现并修复「修复①匹配过宽」**——基名「$元素」命中把编译器自举容器（向量$IR指令$元素 等·返回=非容器结构体）也改成 retbuf[0]=句柄·其调用方按 56B 值物化消费 retbuf→自举行为全乱＝756「修复③崩提前」真因——**已加 是容器类(返回文本) 守卫**（757-a·仅容器类实例元素方法命中）。②物化源句柄槽+守卫组合实测：113 仍 rc=-11·崩=bb24_6 桶段循环越界（r10=0x7ffffffff000·this.桶数量=2^61 级垃圾→循环无界跑）——**「桶数量 = 其他.桶数量」读出垃圾**：物化后源槽=[0]=句柄·两跳=句柄→壳.桶数量——读出垃圾＝**「其他」两层解引的实现/契约仍有单点错位**（两跳 asm：405c63 (rsi 槽)·405c72 (·)·+0x20·405c89 (·)——物化态下逐槽数据待续追）。**【758-a 观测幻影重大勘误（printf 参数笔误）】**：755/756 的 gdb printf 脚本把「源槽」参数误写为 `*(rsi)`（与「壳」同参·少打 rsi 本身）——**「rsi=句柄值/自指/分配重叠」等地址巧合推理全部不可信**（观测幻影·教训：批量 printf 必须逐参核对 `$rsi` vs `*(rsi)`）；HIT2 壳全字段（键=0x696520 值 ✓ 链 ✓ 桶 ✓ 桶数=1024 元数=2 容量=4）与键数组[0..3]=1 2 0 0 均健康。**758 会话第一步（正确脚本）**：①`printf "rsi=%p", $rsi`（先打 rsi 本身·再打 *(rsi)·两参分开）重跑三次 hit→②以正确 rsi 定「两层解引」错位点（物化生效性：rsi 应=栈物化槽地址；若=堆句柄值=物化 IR 未生效·查 748-a 命中）→③定点修→④113 rc=0 对齐 101→反证双态→全量门禁→integrate。**【758-a 终批观测】**：守卫+物化态 113 崩在**首次映射拷贝构造之前（main 早期）**——比 754 态（崩在拷贝构造循环）更早＝**命中面问题比守卫更深：v2p 自举容器（向量$映射$..$元素·编译器内部使用）也被改 retbuf[0]=句柄·其内部调用方仍按 56B 值消费 retbuf→自举乱**——「是容器类守卫」不足以隔离自举面。**759 轮方向（二选一·需用户裁决语义面则呈报）**：甲=「元素 方法」返回还原 56B 物化（撤销 748-a 命中）·仅「生成声明 接收」侧改「读 retbuf[0] 句柄物化+NewObject+拷贝构造（源=物化槽地址）」——单侧适配·v2p 自举零影响·宿主 055 对齐点=接收段；乙=「retbuf[0]=句柄」全调用点统一（v2p 自举内部调用方逐点改·面大）。**推荐甲**（单点·自举零风险·Rust 对照=接收方所有权物化）。**当前分支状态**：修复①②+守卫+物化（四件·113 main 早期崩·勿直接集成——需按甲先撤 748-a 命中或改接收面）。

**【754-a 阶段回填（修复①② asm 实锤生效·崩链推进至 056 甲域·实验态分支保全·不集成）】**

**已实施（2 文件·分支保全）**：①生成语句.cn 748-a 名字匹配改基名（「$元素」含判定·原==驻留("元素") 对克隆体全名恒不命中=**分支自 748 集成起从未生效**）＋命中时 **retbuf[0]=句柄**（8B·替代 56B 值物化）②生成声明.cn 返回接收按被调分派（「元素」→NewObject+拷贝构造·源=retbuf·两跳契约成立；其余被调保持直存零变化）——**asm 实锤**：元素 方法=__cn_object_new+拷贝构造+retbuf[0]=新对象（原 rep movsb 56B 浅拷消失）。

**崩链推进（gdb 新崩点数据）**：113 v2 侧仍 rc=-11·但崩环节已推进——**「符号表.追加(全局层)」的按值形参拷贝构造副本（X1）带病**（X1.数组容量=垃圾→元素 方法内 X2=NewObject+拷贝构造(源=池[0] 槽 ✓ 装配正确) 拷入垃圾数组容量→分配(垃圾×8)=巨大 mmap 块→循环体读崩）——**X1 的病=形参拷贝构造的源装配**（主 的全局层→追加 形参槽）＝**056 甲域（容器按值形参语义·用户 2026-09-24 已裁决）**·与本轮返回 ABI 正交。

**下一棒（依赖即任务号）**：**755 轮=形参拷贝构造源装配修复**（实参装载的类按值形参→拷贝构造调用源=「实参槽地址」两跳契约核对·生成表达式求值.cn 实参装载段）→X1 转好→113 全链 rc=0 对齐 101→反证双态→全量门禁→集成；056 甲 in-out 写回（语义级·已裁决）随其后波次。**实验态保全**=分支 `任务/深度机-754-返回ABI统一`（修复①②两文件改动·勿丢弃）。

**【760-a 修复方案定稿（源码已读·两处改动点精确锁定·留 761 会话执行）】**：读 推导下标元素类型ID/推导成员链类型ID（IR生成类型.cn:107+）——432-b 分支对「值 数组」类型「V*」剥 * 返回**未实例化泛型参数文本「V」**→「元素是类x＝是容器类(「V」)||是用户类(「V」)」=假→595-a「类元素+调用右值」通道（源句柄 32B 整体拷）未进→落 8B 标量写（值数组[0]=地址值·类型ID=0）——**两处改动**：①生成语句赋值.cn 595-a 段「元素是类x」判定加克隆体回退（推导 miss+当前方法类名ID 含 $＝泛型实例克隆体→按类元素候选）②同段「尺寸k=类型大小与对齐(元素文本x)」=0 时回退 **结构体返回临时们.获取(源句柄k)** 取复结构ID 尺寸（复制 展开物化 462-a 已登记·符号=32B）→发射整体拷。**验证链**：113 rc=0 第 11 行=101→反证双态→全量门禁→integrate。**教训强化**：泛型实例克隆体的类型推导 miss（「V」文本外泄）＝T94/466-a/438-b 同族——根治面=推导链的实例感知解析（独立轮）。

**【760-b 统一通道已实施（待验·分支保全）】**：595-a 段条件改「右是调用x」统一整体写（元素是类x 判定移除·尺寸回退 结构体返回临时们·标量尺寸等价原宽度）——113 仍值差 0·**下一实验=值段写侧确认（统一通道是否生效于值段循环·源=「其他.值数组」读侧亦嫌疑）**。**根因收窄链**：键段=1 2 ✓/值段=指针形态——「值 数组[值索引] = 复制(...)」的读源（其他.值数组 两跳）或写侧（物化槽 32B）——正确脚本 gdb（rsi 与 *(rsi) 分打）对照值段循环的源物化槽 32B 内容即可定案。

**【760-c watch 终局数据（use-after-free 族实锤·759 按甲态）】**：gdb watch HIT0 产物字段写入序列——值数组字段写1=bb24_14 alloc 值 ✓（写序列正确）·写2=**tcache_put（glibc malloc free 内部·chunk=产物头）＝产物对象已被 free**——**「值[0]=0/指针形态」读自已释放内存＝use-after-free 族实锤**（非字段错位·非 348-a miss）。**根因收敛**：「追加 形参副本（056 甲域·按值形参）生命周期」与「元素写深拷/复制 展开物化槽」的时序——**759 按甲的 接收侧 retbuf 值物化同样读「已 free 区域」（值=0 的直接来源）**。**759 方向修正**：按甲接收侧的 retbuf 内容＝「元素 方法」85-a 段 56B 拷——源=生成左值地址（760-a 已修下标分派）——**「值=0」须先验 retbuf 56B 内容健康**（元素 方法内源=池[0] 槽·读时 池[0] 是否已被形参副本 free 污染）→760 续第一实验=gdb 断 85-a retbuf 写完成点·读 retbuf 56B 内容（好/坏定「元素 方法上游」vs「接收侧」）。**关联裁决域**：056 甲 in-out（用户已裁决）＝形参副本生命周期根治本体。

**【760-b 补记（760 续实验环境警示）】**：760 续执行时发现 /tmp/dis.txt 为旧产物反汇编（多次重建后未刷新）——**bb24_12/bb24_6 等历史观测对应旧产物布局·当前产物（统一通道版）函数已重排（拷贝构造 123 条指令）**——值段对照实验必须先 `objdump -d <当前 v2out> > 新文件` 再解析。**760 续实验步骤（下一会话执行）**：①`objdump -d <v2out>` 刷新→②python 解析拷贝构造（映射$整64$符号$映射& 段）内全部 alloc 配对段（源读/目标写/尺寸）→③gdb 断值段写指令·对照源物化槽 32B 与目标元素槽 32B→定读写错位→定点修→113 全绿。

**【760 续插桩定案（通道命中·源读侧错位＝T94 精确点）】**：插桩实测「下标整体写 元素=符号 尺寸=32」**命中统一通道**（整体写已发射）——但值仍 0＝**读侧错位**：「复制(其他.值数组[值索引])」的源=「其他.值数组[值索引]」的左值地址——观测源值[0]=指针形态＝**「生成下标左值成员下标分支」对「引用形参.数组字段[i]」的解引层数错**（T94 家族·748 轮同域：this/引用句柄链——读少一层=读到「值数组指针字段」而非元素）。**757 修复（761 会话执行）**：生成下标左值成员下标分支（生成表达式.cn）加 引用形参成员链解引层数核对（对齐 748 的 ptrLocalPointees_ 语义追踪·550-a 清单同源）→源读=元素真值→32B 物化正确→113 第 11 行=101→反证双态→全量门禁→integrate。**链路总结（753~760 八轮）**：rc=-11 崩溃已根除（按甲三件）·值差收敛至「引用形参.数组字段读」单点＝T94 精确修复。

**【761-a 前哨（673 补丁已在·剩余=链类型推导实返值核对）】**：读 生成下标左值成员下标分支（生成表达式.cn:856+）——**673 补丁已在位**（下标对象=成员链·尾字段 T* 指针→IR_指针加载 解引·注释明点名「映射 拷贝构造 值数组[i]=复制(其他.值数组[i]) op=61 源地址落壳+8+i×32」＝本场景原话）——**剩余未知=「推导成员链类型ID(其他.值 数组)」的实返值**：正确应=「V*」（尾 * →解引生效）；若 466-a/438-b 剥层误剥（返回「V」无 *）→解引不发→读壳+8 键数组=垃圾。**761 续实验（下一会话·单点）**：插桩打印 链类型c（一行）→按实返修（若=「V*」则解引应生效须查 IR_指针加载 的槽号错位；若=「V」则修推导的剥层顺序）→113 第 11 行=101→反证双态→全量门禁→integrate。

**【761-a 前哨数据（插桩一行定案·插桩已撤销）】**：`[d761]673判定 链类型=符号*`——**推导成员链类型ID 返回正确（符号*·尾 * →673 解引判定命中）**——673 补丁工作正常。**剩余单环重定位**：d760 三行（整64 8B/映射 56B/符号 32B）＝统一通道命中·但值仍 0——**「值 数组[值索引] = 复制(...)」的写侧物化槽 32B 内容=0**＝「发射复制结构体深拷」的源读（复源地址=生成左值地址(其他.值 数组[值索引])）仍有错位——**即「其他.值数组[值索引]」左值地址计算**（757 精确点·生成下标左值成员下标分支/裸字段分支族）——761 续=对 生成复制展开结构体物化 的 复源地址 加逐槽 gdb 对照（断 发射复制结构体深拷 入口·读 源地址 32B 前后值）→定错位→修→113 全绿→反证→全量门禁→integrate。

**【760 续-a 值段循环完整形态落盘（761 续产物·405c4a 函数 630 条·4 alloc 全在）】**：值段循环（bb24_17 4065cd~406799）＝①读 this+0x30（数组容量）②CONST 0x20 + imul（×32 步进 ✓）③读 -0x4a8 槽基址 → 写 -0x4c8 槽 → 循环变量 -0x650 递增 → jmp bb24_17——**写指令=406754「mov %r11,(%r10)」·无 rep movsb＝8B 标量写形态确认**（值段循环的「其他.值数组[i] 读→this.值数组[i] 写」仍 8B——统一通道（760-b）对值段循环**未生效或效果≠预期**）。**760 续实验精确化**：gdb 断 406754（值段写）·打印 r10（目标地址）与 r11（写入值）与 -0x4c8/-0x480 槽——对照「源物化槽 32B」健康状态→定「8B 标量写」的精确环节（统一通道未命中？还是命中后写值错）→定点修→113 全绿。

**【761 续决定性实验定案（gdb 断值段写 406754·三 hit 数据）】**：写1/2/3＝目标 0x694550/0x694570/0x694590（步进 0x20=值数组元素槽 ✓）·**写入值=0x6932C0/0x6932E0/0x693300（地址序列·步进同 0x20）**——**＝「复制 展开的物化槽地址」被 8B 直写进值数组元素**（应写「物化槽的 32B 内容」）＝**「尝试下标结构体整体写」348-a miss 实锤**——miss 原因定案＝「元素是类x＝是容器类(「V」)||是用户类(「V」)」判定失败（推导下标元素类型ID 在泛型克隆体返回未实例化「V」→「V」查用户类=假）。**761 续修复（单点·下一会话执行）**：在 尝试下标结构体整体写（生成语句赋值.cn:750+）的「元素是类x」判定加克隆体回退——`如果 (!元素是类x && 当前方法类名ID != 0 && 查结构体大小(元素类型IDx) == 0 && 推导返回的文本为未实例化参数) { 元素类型IDx = 结构体返回临时们 查 右值产物 }` 或更简：判定改用「右值求值后查 结构体返回临时们.获取(源句柄) →命中=结构体→整体写 32B」（对齐 595-a 统一通道同款·已验证装配）→值数组=32B 符号值→113 第 11 行=101→反证双态→全量门禁→integrate。

**【761 续决定性实验数据（gdb 断 406754 值段写·三 hit）】**：写1 目标=0x694550 值=0x6932C0；写2 目标=0x694570 值=0x6932E0；写3 目标=0x694590 值=0x693300——目标步进 0x20（值数组元素槽 ✓）·**写入值=步进 0x20 的地址序列**（＝「复制 展开的物化槽地址」被 8B 直写·应写物化槽的 32B 符号值）。**761 续修复（下一会话·单点已定）**：在「尝试下标结构体整体写」（生成语句赋值.cn:750+）的判定/段内加 **结构体返回临时们 回退**——右值求值后 `结构体返回临时们.获取(源句柄k)` 命中（复制 展开登记·复结构ID=符号 32B）→按登记尺寸 IR_结构体拷贝 整体写 32B（对齐 595-a 统一通道同款装配）→值数组=符号值→113 第 11 行=101→反证双态→全量门禁→integrate。

**【761 续单点修复实施回填（写侧整体写已加·值差仍在＝读侧 T94 定案·实验态分支不集成）】**：761 续修复已实施（生成语句赋值.cn 生成成员解引用赋值 尾段：下标左值+结构体返回临时们 命中→按登记尺寸 IR_结构体拷贝 整体写）——113 仍第 11 行值差 0＝**错位点定案=读侧**：「复制(其他.值数组[值索引])」的 复源地址=生成左值地址(其他.值 数组[值索引]) 读出垃圾（T94 家族·引用形参句柄链解引层数——748 ptrLocalPointees_ 同族）。**762 修复本体**：生成下标左值成员下标分支（生成表达式.cn:856+·673 判定后）的**引用形参成员链解引核对**——对齐 748 的 ptrLocalPointees_ 语义追踪/550-a 清单（用户已裁决甲）→源读=元素真值→113 第 11 行=101→反证双态→全量门禁→integrate。

**【762-a 决定性观测（副本字段窜位实锤·修复方向锁定）】**：gdb 读 HIT2 源壳（形参副本产物）字段——**键 数组=0x1·值 数组=0x2（＝元素数量的值窜位）**＝**「拷贝构造头三句标量字段的写偏移」存在字段级窜位**（桶数量@0x20/元素数量@0x28 的值被写到 @0/@8 附近＝**写偏移差 -0x18/-0x20**·与 四段 alloc 的段序耦合）——**T94 家族精确定案＝拷贝构造体「标量字段写的偏移」在 泛型实例克隆体 下窜位**（键/值段@0/@8 的指针字段写正常·标量段@0x20/@0x28/@0x30 窜位）——**763 修复本体（单点）**：拷贝构造的「标量字段写」发射序列（生成语句赋值/生成类 的 字段偏移解析——对照 673/裸字段分支的 同函数同形态 正确段）→修偏移→113 全绿→反证双态→全量门禁→integrate。**全部实验数据（753~762 十轮）已落盘 §2.147·零损耗接力就绪。**

**【762-b 761 续实验终值（bb24_18 写三 hit·760 续-a 修正）】**：写1/2/3＝目标 0x694550/70/90（值数组元素槽·步进 0x20 ✓）·写入值=0x6932C0/E0/300（**步进 0x20 的地址序列＝源元素槽地址序列**）——**＝「写入了源元素槽地址（8B）」而非「32B 符号值」＝统一段（760-b）未生效于值段循环·或 bb24_18 非统一段产物**（760 续-a 的「无 rep movsb」观察维持）。**761 续下一实验（763 轮·精确到指令）**：①刷新 objdump→②断 bb24_17 循环头（4065cd）读循环条件·确认循环语义与 bb 编号→③断循环体读指令（4065xx 区·其他.值 数组 两跳）与写指令（406754）·对照读值/写值→④定位「读=地址序列」的发射层（生成下标左值/生成表达式的地址vs值语义）→定点修→113 全绿。**数据保全确认**：761 续实验全部原始数据（三 hit 寄存器）已入册 025 §2.147。

**【761 续决定性数据终版（读链完整对照·修正前期误读）】**：当前产物（按甲+统一通道）值段循环完整读链（4066b7~406754）＝**三层解引全部正确**：(其他)→壳→+8→值数组指针→+索引×32→源元素地址——**写的 8B=0x6932C0＝源元素的首 8B＝符号.名 ID 的驻留指针（✓ 合法值·非垃圾）**——**值差真相修正＝「值 数组[值索引] = 复制(其他.值数组[值索引])」的「复制」只拷了符号的首 8B（名 ID）·类型 ID/种类/附加 3 字段未拷（保持 0）**——即「复制」展开产物=8B 而非 32B 物化——**「顶层.获取(1)」找到键→返回 值[0] 的类型 ID 读=值 数组+8+8=0**。**761 续修复（下一会话执行）**：值段循环写从 8B 扩到 32B（IR_结构体拷贝 32B 或 4×8B 字段拷）——写指令=406754·源读链已验证正确·只差写宽度→113 第 11 行=101→反证双态→全量门禁→integrate。

**【763-a 插桩定案（推翻 348-a miss 推断·精确环节重定位）】**：d763 插桩实测——「尝试下标结构体整体写」统一段**命中**（d760 三行：整64 8B/映射 56B/符号 32B·尺寸=32 整体写发射 ✓）·**「结构体返回临时们.获取」17 次全 miss（下标写宽通道）**——**348-a miss 推断作废**。**值=0 的精确定位（重定）**：统一段整体写 32B 发射后值仍 0＝**「写的内容（源句柄k 起的 32B）=垃圾」＝源句柄k=生成表达式(复制调用) 的产物＝③ 用户类分支（NewObject+拷贝构造）的新对象句柄**——**拷贝构造源=「其他.值数组[值索引]」（T94 读错位·761 前哨锁定）→新对象内容=垃圾→值数组[0]=垃圾句柄**——**＝根因回归＝「其他.值数组[值索引] 读错位」（T94·761 前哨锁定）——**修复本体＝762 已登记（生成下标左值成员下标分支的引用形参解引核对·ptrLocalPointees_ 语义追踪）**。**762 续实验**：gdb 断 HIT1 拷贝构造值段读（bb24_12 区·(壳+8)+i×32 解引）·打印解引前后值→定解引层数→修。**插桩保留**（d760/d763·修复验证用）。

**【763-a 续插桩+③登记（修复链推进·113 仍值差 0·实验态分支不集成）】**：①③ 容器/类分支（生成复制展开容器类）补登记 结构体返回临时们（对齐 462-a 单一归属）②d763 插桩实测「下标写宽通道」18 处全 miss（值段走统一段·不落该通道）③d760 三行维持（整64 8B/映射 56B/符号 32B·统一段命中）。**根因终局收敛**：统一段整体写 32B 的源=源句柄k=「复制(其他.值数组[值索引])」③ 分支的新对象句柄——**新对象内容=垃圾＝拷贝构造源（其他.值数组[值索引]）读错位（T94）**——**修复本体=762 已登记（生成下标左值成员下标分支的引用形参解引核对·ptrLocalPointees_ 语义追踪）**。**762 续实验**：gdb 断 HIT1 拷贝构造值段读（bb24_12 区 (壳+8)+i×32 解引）·打印解引前后值→定解引层数→修→113 全绿→反证→全量门禁→integrate。
**【765-a 两键对照终值（读侧 T94 终局确认·插桩撤销前留证）】**：③登记键=63（1 次）·d763 下标写宽通道 17 次全 miss（与③登记无关）——**值段统一段命中确认（元素=符号 尺寸=32）**——「值[0]=0x6932C0（指针）·类型 ID=0」的终局＝**「发射复制结构体深拷」的复源地址（生成左值地址(其他.值 数组[值索引])）读出垃圾**＝**读侧 T94（762 修复本体已登记·单环）**——**762 续实验（下一会话执行）**：gdb 断「发射复制结构体深拷」入口·读 复源地址 与 源 32B 前后值（对照「其他.值数组[0]」正确地址=壳+8+索引×32）→定解引层数→修 生成下标左值成员下标分支→113 全绿→反证双态→全量门禁→integrate。**本会话插桩（d760/d762/d763）撤销留待修复验证后统一清理（分支保全）。**
**【765 续实验数据（拷贝构造完整函数体 630 条·解析修正后）】**：映射$整64$符号$映射& 函数体 630 条（修正 bb 标签 break bug 后完整）——**cn_alloc×4（桶/键/值/链四段 ✓）·rep movsb=0**——**值段统一段（760-b 右是调用x·IR_结构体拷贝 32B）的发射缺失/形态待查**（IR_结构体拷贝 的 x64 发射=rep movsb·四段循环内 0 处＝**「值 数组[值索引] = 复制(...)」的统一段整体写未以 rep movsb 形态出现**）——**761 续修复（生成成员解引用赋值 尾段：下标左值+临时表命中→按登记尺寸整体写）已实施·17 miss＝临时表键（复新基址）与查键（值寄存器）不一致或 ③ 分支登记（762-a）时机**。**762 续实验（下一会话执行）**：①d763 打印扩为「值reg 十六进制」＋③登记打印「键=十六进制」对照（761 续已有 ③键=63 十进制）→定键不一致→修 462-a/③登记键域→临时命中→整体写 32B→113 全绿→反证→全量门禁→integrate。
**【765 续-a IR 发射对照终值（763 决定性实验收口）】**：IR_结构体拷贝 的 x64 发射=「mov rdi 目标+mov rsi 源+mov rcx 尺寸+rep movsb」（代码生成X64Linux.cn:795~803·尺寸=指令.类型）——**拷贝构造 630 条内 rep movsb=0·cn_alloc=4**＝**值段循环（bb24_18·406754 写=8B 源元素槽地址）确非统一段整体写·亦非四段 alloc**——**＝**「**「值 数组[值索引] = 复制(...)」的统一段（右是调用x·d760 插桩命中尺寸=32）**——**与**「**bb24_18 的 8B 写**——**为**「**同一赋值的两次不同发射**」——**＝**「**d760 插桩命中的下标=**「**另一处下标（非值段循环）**」——**＝**「**值段循环（bb24_18）**未进统一段**＝**「**「尝试下标结构体整体写」对值段返回假——**＝**「**768~780 某判定对值段返回假（候选=元素类型IDx 推导/剥 & 循环）——**762 续第一实验=gdb 断 尝试下标结构体整体写 入口+各返回假点·打印返回路径→定 miss 判定→修→113 全绿→反证双态→全量门禁→integrate。
**【763 收口实验终值（d760 命中确认·值=0 环节定位完成·764 接力）】**：重读 尝试下标结构体整体写（生成语句赋值.cn:789 起·750~787=尝试成员结构体整体写尾部·此前混淆已正）——**「值 数组[值索引] = 复制(其他.值数组[值索引])」命中统一段（d760「元素=符号 尺寸=32」✓）·整体写 32B 发射**——**值[0]=0x6932C0（指针）·类型 ID=0 ＝**「**「源句柄k 起 32B 的内容=0/垃圾」＝**「**「源句柄k=③ 分支（生成复制展开容器类）的 NewObject 新对象**——**「**新对象内容=垃圾 ＝**「**「③ 分支拷贝构造的源读错位」**——**「**＝**「**「hold 槽[0]＝**「**「其他.值数组[值索引]」的求值产物——**「**「**求值产物＝符号句柄（垃圾→T94 读错位上游）**——**「**——**764 接力（下一会话·修复本体单点）**：**「**生成下标左值成员下标分支（生成表达式.cn:856+·673 判定后）的**「**「其他.值 数组」两层解引读**（*(源槽)=壳·壳+8 解引=值数组）**——**gdb 断该读（4066cc/4066ef）·打印两层解引值→定 T94 错位层→修（对齐 748 ptrLocalPointees_ 语义追踪）→113 第 11 行=101→反证双态→全量门禁→integrate。**十三轮全部数据已落盘 §2.147（760 续-a/b/c·761 续·762-a·763 收口）·零损耗接力就绪。**
**【766-a d764 插桩数据（8 次早退A·值段下标写不走 348-a 定案）】**：d764 入口插桩实测——「尝试下标结构体整体写」被调 8 次全部「早退A 非下标左值」＝**值段循环（bb24_18）的「值 数组[值索引] = 复制(...)」未调本函数**（或左末非下标形态）——**值段的 8B 写（名 ID）走「下标写宽通道」（生成成员解引用赋值 尾段·761 续回退所在）**——**761 续回退（查临761 命中→整体写 32B）在此通道·17 miss＝值寄存器≠结构体返回临时们 的键（复新基址）**——**762 续修复精确点（下一会话执行）**：①d763 打印扩「值寄存器 十六进制」＋「发射复制结构体深拷 的 复新基址 十六进制」对照→定键差异（寄存器号偏移/复制调用未经 462-a 登记路径）→②按差异修（登记时机/键域/或 值寄存器 链）→③113 第 11 行=101→反证双态→撤插桩→全量门禁→integrate。**插桩保留**（d760/d762/d763/d764）。
**【766-c d760 扩展终值（符号 32B 值段下标写·源句柄k=165·目标=172）】**：d760 扩展实测——值段统一段命中（元素=符号 尺寸=32）·**源句柄k(寄存器号)=165·目标地址k=172**——③登记键=63（1 次）＝**值段下标写的源句柄k（165）≠③登记键（63）**＝值段下标写的源=「另一处产物」（非 ③ 容器/类分支·候选=④ 结构体物化或 ③ 容器类分支的另一实例）——**762 续收口实验（下一会话执行·单点）**：d760 打印再扩「源句柄k 的 类型大小与对齐 前·查 结构体返回临时们.获取(165)」→命中=登记键域含 165→按登记尺寸整体写即修复；miss=165 的产物未经 462-a/③ 登记→在 ③ 分支（生成复制展开容器类）补登记（762-a 已加·核对命中时机）→113 第 11 行=101→反证双态→撤插桩→全量门禁→integrate。
**【767-a 收口实验终值（临时命中·键域定案·读侧 T94 维持）】**：统一段查临时实测——映射 56B 源句柄k=63 命中（③登记 ✓）·整64 8B 源句柄k=110 miss（标量 ✓）·**符号 32B 值段源句柄k=165 命中 ✓**（结构体返回临时们 含键 165·③ 分支登记正确生效）——**键域定案＝临时表正常·统一段整体写 32B 全链正常**——「值[0]=0x6932C0 指针·类型 ID=0」的根源＝**165 号栈槽的内容**（「其他.值数组[值索引]」的求值产物=句柄序列而非元素值）＝**读侧 T94（762 修复本体已登记）终局维持**——**765 续实验（下一会话执行）**：gdb 断「发射复制结构体深拷」入口·读 复源地址 与 源 32B 前后值（对照「其他.值数组[0]」正确地址=壳+8+索引×32）→定解引层数→修 生成下标左值成员下标分支→113 全绿→反证双态→撤插桩→全量门禁→integrate。
**【768-a 前置发现（拷贝构造 630 条内 rep movsb=0）】**：765 续实验前置——映射$整64$符号$映射& 函数体 630 条（完整·cn_alloc×4）内 **rep movsb=0**＝值段的「复制 展开结构体物化→发射复制结构体深拷(32B)」**未以 rep movsb 形态出现在拷贝构造内**（发射形态/位置=765 续第一步待查·候选=IR_结构体拷贝 32B 的发射优化或统一段源=③分支句柄的非整体写路径）。**768 会话第一步**：①objdump 刷新→②全产物搜 rep movsb 所在函数→③定位值段「复制 展开物化」的实际发射→④对照宿主 32B 位拷贝→定点修→113 第 11 行=101→反证双态→撤插桩→全量门禁→integrate。**本会话（753~767）全部成果已保全**（分支 1a4af356+develop 双远程同步·025 §2.147·021·看板·交接）。
