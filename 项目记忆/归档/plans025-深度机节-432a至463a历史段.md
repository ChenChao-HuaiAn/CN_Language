### 2.32 收工回填（**375-a B1 化石改写 ✅ 销项·用户裁决甲**·基点=develop 2373afa〔detached·同机 374 会话占用主树与分支引用〕·分支 `任务/深度机-375-B1化石改写`·2026-09-19）

**1. 目标与验收判据**：B1 化石用例族处置（用户裁决「B1 选甲：改写 185_v2 与 125_v2 为布尔语境写法、保留比较层分级考点、expected 重新固化」）+**转双编译对照通道**（用户 PS 关切 C20 双测方向——根治「v2 闭环通道无宿主对照→与宿主规则脱节」的化石成因）。**验收**：①`--filter 185_v2`/`--filter 125_v2` 单跑双绿（对照=宿主+v2 两侧）；②全量 E2E `--jobs 8` 零新增红（基线 444=442/2 → 两化石转绿）；③零警告构建+单测 1353（4 红基线既有）；④check_spec_coverage --strict+check_matrix_coverage 绿；⑤**考点锚定保留**：185_v2 错误合并分组=13 ≠ 正确分组=14 的差分锚不丢。

**2. 写集（精确文件清单·预登记）**：tests/e2e/185_v2_比较层分级/（主.cn 改写+v2闭环.txt→双编译对照.txt）｜tests/e2e/125_v2_控制流与短路与转义/（主.cn 6 行改写+同上换通道）｜plans/025 §2.32｜plans/021（B1 销项+戳 375）｜HANDOFF/更新日志深度机节｜看板通告段（已直推 2373afa）。**零 src 写集**；触 tests/**→按 §8.3 三平台 try-build。

**3. 实施步骤**：①185_v2 主.cn：r1~r4 改 `布尔` 声明+如果逐位累加校验和（退出码=14 锚定：错误合并分组=1+0+4+8=13≠14 ✓）；②125_v2 主.cn：短路段 6 行布尔语境化（行46/47/49/51/58/60：`整数&&整数`→`整数!=0 && 整数!=0`·结果存布尔+`如果(布尔)`/`如果(!布尔)`）；行55（除数短路本质）与行62（比较结果作操作数）本合法原样保留；缺陷A/C 段不动；③v2闭环.txt→双编译对照.txt（键迁移：185=退出码14/借链=否；125=退出码0/借链=是；均不加断言输出=校验面不变）；④改写后逐用例实测重固化。

**4. 验证链**：worktree 自建编译器（零警告构建）→单测→单用例 --filter×2→全量 E2E --jobs 8→check_spec_coverage --strict→check_matrix_coverage→check_handoff→check_progress_sync；**反证**：stash 改写→两用例复现「布尔隐式转换」红→恢复复绿（证明红/绿翻转确由本轮改动驱动）。

**5. 边缘情况与诚实边界**：预判①宿主对 125_v2 全文（容器追加/转义段）应绿——宿主为原实现，双测下首验；②同族化石残留=无需另扫：v2 已对齐宿主（329-a），同形态必红，全量仅此 2 红（340-a 复核「均化石族」）→ 无第三例；③对照通道宿主侧对 借链=是 向量模块支持有 402 先例；④125_v2 主.expected（[p6h] 行）在对照通道不消费（无断言输出键）·保持不动；⑤同机 374 会话占用主树+分支引用被其自动化移动 → 本轮 worktree+detached HEAD 钉 2373afa 隔离工作、按 SHA 推送分支。

**6. 与他机交叠分析**：家机358（src semantic）·单位机329（v2 词法预处理）·同机374（T63 侦察·零源码）——写集零交叠（本轮仅 tests/e2e 两目录+文档分区）；语义耦合=改用例不改编译器·无。

**7. 收工实测回填（全部达成）**：①零警告构建（GCC -Werror）✓；②单测 **1353/1353 全 PASS**（基线 4 红已被前轮修复·记忆旧口径「4 红基线」作废）；③单用例 `--filter 185_v2`/`--filter 125_v2` 双绿（双编译对照=宿主+v2 两侧）✓；④全量 E2E `--jobs 8` **444=441/1/2**——两化石转绿 ✓、唯一红 426=T50 基线既有（修复在未集成 331-a）·零新增红 ✓；⑤反证：同一新编译器对改写前旧源码精确复现「无法将'布尔'隐式转换为'整32/整64」+「逻辑运算符要求布尔操作数」（红→绿翻转确由本轮改动驱动）✓；⑥check_spec_coverage --strict+check_matrix_coverage 83/83 ✓。**写集（最终）**=与预登记一致（两用例目录各 3 文件变更+四共享文档）+零意外文件。**边缘情况实测**：预判①成立（宿主对 125_v2 全文绿）；预判②成立（无第三例化石）；预判⑤触发（worktree+detached 兜底实际启用·lessons 已记）。**下一轮候选**：375-a 集成（try-build 回签后）→T63 终修（374-a 定案·objdump 逐指令）→D42 缺口轮。

### 2.36 T63 深挖轮（**跨帧对照定位**＝390 主.cn 调用序列 vs 删除_J 内部·gdb 单步/插桩打印·零源码倾向侦察）——382-a 定位轮（分支 `任务/深度机-382-T63深挖`·基点=develop 4653661·2026-09-19）【387-a 补录：该节原文在 382 分支，377~386 D40 线分支的 plans/025 未含（双线并行各写各节未合流）·387-a 合流时搬入】

**1. 背景回顾（380-a 定案）**：删除_J 错误构造序列**完整正确**（objdump 完整函数体·标志 0+E 值 2+retbuf 拷贝+rax）——**368-a「缺 E 值写」判定证伪**——T63 回未定位态·垃圾来源候选=**调用方侧**（390 主.cn 的 v2p 编译产物·retbuf 生命期/物化时序）。
**2. 目标与验收判据**：**定位垃圾来源**（跨帧对照：gdb 单步或插桩打印）——**验收**：①垃圾来源锁定（具体环节：retbuf 地址错位/物化时序/返回值寄存器/调用方读取偏移）②定位结论落盘（修复轮立案或直接修复）③双门禁。
**3. 实施步骤**：①390 主.cn 的 v2p 产物 gdb 单步（负删调用前后：rdi/retbuf 内容/错误槽值对照）②或插桩打印（临时调试 .cn 或 asm 注释）③对照锁定环节④立案/修复。
**4. 验证链**：定位结论+对照实证+双门禁（零源码则快速门禁）。
**5. 边缘情况**：①gdb 对 v2p 产物（无符号）单步=汇编级（asm 对照辅助）②插桩打印=临时 .cn 或 asm 修改（不入库）。
**6. 与他机交叠**：零源码倾向（侦察轮）·零交叠 ✓。

### 2.37 收工回填（**382-a gdb 断点铁证**＝负删 retbuf：标志@0=0 ✓+E 值@4=2 ✓+**@8 被污染为栈地址 0x7fffffffd504**——污染源=删除_J 正常路径或调用方物化·深挖待续·零源码）——382-a（分支 `任务/深度机-382-T63深挖`·基点=develop 4653661·2026-09-19）【387-a 补录·同 §2.36 缘由】

**1. gdb 断点铁证（负删 call 后 retbuf@rax）**：`x/2gx rax`=**0x0000000200000000 | 0x00007fffffffd504**——**标志@0=0（错误 ✓）+E 低 4@4=2 ✓**——**错误构造写入正确**——**@8=0x00007fffffffd504=栈地址**（污染实锤·值随运行漂移）——**调用方读 .错误@8=栈地址**（打印垃圾）。
**2. 污染源候选**：①删除_J 正常路径（bb8/bb9 移位循环）在错误分支后误执行/覆盖 ②调用方物化序列（rep movsb 16 字节）的源/目标错位 ③retbuf 生命期（后续调用覆盖）——**深挖=下一会话**（gdb 断点序列+bb8 后续逐指令）。
**3. 与他机交叠**：零源码 ✓。

### 2.38 补录（**377~386 D40 选择语句线记录缺失补齐**·依提交信息+dbg 实测数据如实登记）——387-a 补录【⚠️该线各轮提交信息声称「六要素落盘/收工回填」但实际 diff 无文档改动——记录缺失事故·lessons 已记】

- **376-a**（`任务/深度机-376-D40选择语句`）：D40 立项=选择语句降糖 If 否则链（解析期合成）+选择如果索引们 顺序合成+否则链回填+**选择出口块机制**（生成语句 中断 优先跳选择出口·C break 同款）。
- **377-a**（8af4136）：二元参数序修正（附加/子起点=右起点/左起点·对照解析表达式 653 行标准）+If 条件终点=体起点（case 值区间并入条件）——s1/s2 探针仍输出 10（条件区间语义深层未解）。
- **384-a**（3079e08）：**池序重排**（v' 复制→case 值→体序列→合成块→If 全连续有序·倒挂根治）——sem5 暴露「== 产物类型推导缺口」（如果条件须布尔·实测整32）。
- **385-a**（分支 `任务/深度机-385-D40语义层`·看板行直推 5dc6367 但分支未推远端·工作经 checkout 恢复并入 386）：语义层 == 产物=布尔推导核对（推导源类型ID静默 329-a 已有 == 分支·核查确认覆盖）。
- **386-a**（ab8ae88·已推远端）：**== 合成位置修正**（case 值解析移至 v' 复制后·值后立即合成 == ·条件末节点=布尔合法）+**384 旧 == 合成残留删除**+[dbg386] 插桩（收工未删·387-a 清理）——s1 复测仍 10（当时 v2p 未按最终代码重建·陈旧产物假象）+T63 深挖 dbg 数据落盘。

### 2.39 D40 选择语句闭环验证（**s1/s2+02_control 实测定案**＝基本/默认/字符/嵌套四段双端一致·fall-through 缺口定性立案 D44·386 修复链生效确认）——387-a 开发轮（分支 `任务/深度机-387-D40选择闭环`·基点=develop 893d6d3+净差异单提交重建·2026-09-19）

**1. 背景与目标**：386 收工遗留「s1 复测=条件区间语义仍需深挖」——387 重建验证：①386 修复链（池序重排+== 合成位置修正）实测定案；②02_control 解锁验证（385 看板行目标）；③缺口完整定性。**验收**=s1/s2 输出正确+02_control 双端 diff 逐段定性+门禁全绿+缺口立案。
**2. 写集（精确清单）**：`CN语言编译器v2/语义/语义检查.cn`（dbg386 插桩清理=回到 385 净态）｜`CN语言编译器v2/语法/解析语句.cn`（386 修复链保留）｜`CN语言编译器v2/IR/{IR容器,IR常量,生成语句}.cn`（384/376 链继承）｜`tests/e2e/448_三段式循环矩阵/`（357 成果随链继承·develop 无此目录）｜共享文档四件+看板行。**回滚三处**：125_v2/185_v2 主.cn+v2闭环.txt（386 链旧态倒退 375-a 已集成成果·恢复 develop 版）+cnsmith 采样日志（同因恢复 354-c 窗记录）。
**3. 实施步骤与关键发现**：①基底重建——386 链 rebase 冲突（共享文档多提交重放）→改**净差异单提交**重建（源码面 checkout 386+文档面手工合流）；②**倒退防线**：checkout 旧链文件前先核对 develop 已集成成果（375-a 布尔语境改写+354-c 采样窗两处实际触发倒退·当场恢复）；③dbg 插桩清理+**v2p 按当前代码重建**（14s·陈旧产物假象排除）；④s1（情况 2 命中）=**20 ✓**+s2（默认）=**99 ✓**——377/378/379 时代「恒输出 10」顽疾实证已解；⑤02_control 宿主对照=**`整数` 别名 v2 缺失**（sem5 拒「未声明的类型: 整数」·规范 50 行内置别名宿主 type_system canonical 化 v2 无）；⑥整32 副本双端对照=**4~7 段全一致**（基本中断/默认缺省/字符匹配/嵌套）·**唯一差异第 8 段 fall-through**（宿主 fall1+fall2·v2 仅 fall1——降糖 If 链无贯穿跳转）。
**4. 验证链**：宿主 check s1 ✓；v2p 编译链接运行 s1/s2/02_control ✓；全量门禁（零警告构建+单测+全量 E2E --jobs 8）+静态双门禁+自举链=收工回填；宿主 genSwitch 铁证=判断链模型（非跳转表）+fall-through=体块未终止 endJump 下一体块（ir_stmt.cpp:400）——v2 If 否则链**语义同构**、缺的只是贯穿跳转。
**5. 边缘情况与诚实边界**：①**D43 立案**=`整数`/`小数` 内置别名 v2 全链缺失（非仅诊断放行——布局/位宽/分派全链消费面须 canonical 化单一归属·独立轮）；②**D44 立案**=选择语句 fall-through（规范 755 用户裁决语义·v2 缺失）+字符串 case（宿主 __cn_str_eq·v2 == 对 ptr=指针比较缺口）——修复方案甲（推荐）=生成层选择语境查 选择如果索引们 栈·真块未终止跳下一 case 真块（宿主 ir_stmt.cpp:400 同构·376-a 出口块框架已在）；方案乙=原生选择节点+跳转表（规范 1356 性能终态·工程量大·宿主亦未做非对齐欠账）；③v 为副作用表达式的多次求值=MVP 已登记（382 前注）。
**6. 与他机交叠**：家机 364-D10（src semantic·零交叠）/单位机 329-D12（v2 词法·零交叠·解析语句.cn 语义耦合=其 T50 让位面已按集成序消解）——写集零交叠 ✓。

### 2.41 D43 别名规范化实施→D45 缺陷定位收口（**三写法均触发 v2p 自身向量越界·gdb 栈锁定 向量$节点$元素**·改动撤下 patch 备份 /tmp/p387/389改动备份.patch）——389-a 定位轮（分支 `任务/深度机-389-D43类型别名`·基点=develop ed44ea8·2026-09-19）

**1. 实施与实测（全 record）**：①词法 关键字ID表+整数/小数（宿主 lexer.cpp:68 同构）②解析类型文本 基名映射（整数→整32/小数→浮64·限定/泛型/数组/指针 合成形态自动正确）③是变量声明前瞻 首token 放行（谓词 是声明类型起始）④语句位/原子位别名守卫（宿主「预期表达式」对齐）。**功能面单点已实证**：词法+映射两改动态下 alias1（整数/小数 声明+打印）v2p 编译运行=**5/1.500000 双对**（宿主一致）——映射本身正确。
**2. D45 阻塞（★新立 P1·v2 编译器自举面）**：**v2p 编译含「整数」关键字声明源码时自身运行时崩「数组越界」**（gdb 栈#1=`_E59091E9878F24E88A82E782B924E58583E7B4A0_J`=向量$节点$元素——v2 编译器某处 记号/池 向量 元素(位置) 越界·防线触发）。三种实现写法（条件内联 !(A||B)／布尔局部初始化 A||B／整64 状态+驻留ID 比较）**均崩**；组合二分：词法表+解析类型文本=不崩〔sem5 拒=老行为〕；+解析语句（谓词/守卫任一）=崩——崩因在**谓词调用链**但与写法无关（指向更深的 v2 表达式/实参编译缺陷·如 布尔 返回值函数×字符串字面量实参 的调用序）。**复现**=a_int 探针+389 改动集（patch 备份）。
**3. 处置**：改动全量撤下（工作树回 ed44ea8 源码态·patch 备份）——**D45 独立轮根治后再重打**（重打清单=本节 1 的四件·半日工作量）；D43 立案维持（plans/021 §三 三案合一行的 D43 部分）。
**4. 诚实边界**：①D45 崩点精确定位（哪个调用点越界/为何越界）=独立轮 gdb+插桩下钻；②本分支 plans/025 §2.36~2.39（387 内容）随基点存在·与 388-a 恢复分支的回退在集成序上自洽（388 先回退→387 回签正规重入→389 重打）。


### 2.42 D45 精确定位（**调用方传参错位实锤**＝解析变量声明语句 收到大小=1 错误向量·多返回点布尔谓词形态触发）——390-a 定位轮（分支 `任务/深度机-390-D45根治`·2026-09-19）

**1. 复现矩阵（四配置重建实测）**：①基座源码（词法+类型文本+谓词返回||形态+顶部守卫）=**不崩**（顶部守卫拦截整数首语句·声明路径未走）②基座+守卫移位 patch=崩 ③词法+类型文本（无解析语句改动）=不崩〔sem5 拒=老行为〕④patch 全态=崩——**崩=声明路径被走通+谓词多返回点形态**。
**2. gdb 铁证（崩态）**：断 元素_J 入口——rdi=retbuf(栈)、rsi=this=0x66bc10（**向量头=[数据=0x66bc30, 大小=1, 容量=4]**＝非主池的错误向量）、rdx=下标=1——**参数寄存器分配正确、收到的 this 是错向量**＝调用方（解析语句→解析变量声明语句）**传参错位**（池 形参收到大小 1 的无关向量）；bt#1=解析变量声明语句$…$映射$整64$整64 ✓。
**3. D45 精确形态（立案行已更新）**：v2 编译器对「**多返回点布尔函数**（逐步如果·4 返回点）作为调用实参判定」的调用序缺陷——谓词改回基座**内联表达式形态**（返回 (A || B)·单返回点）或解析类 151「如果内联」形态即不触发。**为何基座不崩的真相**：顶部守卫误伤=声明路径未走（非安全·恰是误伤掩盖了缺陷）。
**4. D43 重打方向（半日·下一轮）**：谓词=如果内联形态（`如果 (当前类型==标识符) 返回 真; 如果 (是关键…整数) 返回 真; …` 之外的**单返回点内联**——对照解析类 151 已实证安全形态）；守卫置声明前瞻之后；映射/词法表两件照 389（已实证正确面）。验收=alias 矩阵+449+门禁全链。
**5. 诚实边界**：v2 编译器缺陷本体（多返回点布尔函数调用序的 IR/寄存器分配根因）=独立根治轮（需最小 .cn 复现固化+宿主/v2 对拍）——389 patch 已在 /tmp/p387/ 与分支历史（8ab9039 基座+守卫移位 diff）。


### 2.43 D45 下钻第二轮（**根因链四级闭环+归档 D36 同族**＝栈槽复用/分配冲突·写法敏感性机制查明）——391-a 定位轮（分支 `任务/深度机-391-D45根治`·worktree 隔离〔主树被守护自动化 393 轮占用·374-a 先例〕·2026-09-19）

**1. 复现矩阵扩充（391 实测·崩态=基座三文件+389 patch 重建）**：候选一（多返回点布尔×字面量实参·基线 v2p 独立程序）=不崩（10 正确）；候选二（向量参+三层调用链·借链 v2p.obj）=不崩（10/88 粘连全对·CN 打印无换行坑）；**朴素直判形态**（解析类型文本 类型比较直入条件·无中间变量）=**仍崩**——**写法假设全灭→崩与判定写法无关**。
**2. gdb/asm 双证据链（闭环）**：①断 解析变量声明语句 入口——rdx=池=0x66bc10 **[数据=0x66bc30 全 0·size=1·cap=4]＝空壳向量·非主池**（rdi=记号向量 [数据/size=22/cap=32] ✓ 正确·rsi=位置 7 ✓）——**传参错位实锤**；②解析类型 返回点断点——空壳池 size 仍=1（**追加与读取不同池**：解析类型 追加进了正确主池·调用方读的是空壳）；③asm 调用点（解析语句 0x588228 区）：rdx=-0x5b8(%rbp) 槽值——**-0x5b8 槽存的是空壳向量≠主池**；④（391 补）早前 bt#2 的 0x670ec0 非噪音=记号向量地址。
**3. 归档判定（★D45 并入 D36 同族）**：写法敏感性（谓词/守卫/朴素任一改动→崩显隐变化）+「同一槽读出错误对象」＝**v2 编译器局部栈槽分配冲突**（不同变量复用同一栈槽·改代码即改槽布局）——与 **D36（352-a 定性：局部槽函数级平铺无块作用域）同根因族·根治=D36 架构轮（符号作用域栈化）**。389 崩机制=词法表+2 重排槽布局→解析语句 的池槽被覆盖为无关向量→解析变量声明语句 读到空壳。
**4. 处置与协调**：①D45 归档 D36 域（plans/021 D45 行更新）；②**D43 挂起等 D36 架构轮**——**触发面=词法表+解析语句任一改动重排槽·朴素写法也无法绕行**（393 轮 D43 重打会撞同一崩·已在看板通告协调）；③389 patch+asm/复现资产=分支历史（8ab9039 基座+3fc687e 链）+/tmp/p387/；④**D36 架构轮升 P1 排程建议**（352-b §一 块新名清单栈方案已有设计）。
**5. 诚实边界**：槽冲突的精确变量对（哪两个变量同槽）需 D36 轮槽分配审计工具固化后定位；证据链到 asm 槽级、未到 IR 指令级——归 D36 轮输入。

### 2.53 D45 根治轮（**D43 全链落地收口**＝解析类型 入口缺口修复+449 用例上架+全量门禁全绿·D45 撤案）——402-a 开发轮（分支 `任务/深度机-402-D45函数级`·基点=develop 474866b·2026-09-19）

**1. 函数级 trace 三级定位（决定性）**：①解析块 pos=7 语句级〔401-a〕②**解析变量声明语句 函数级**：类型节点=1/池后=1（**解析类型 未追加**·越界源坐实）③**解析类型文本 trace**：通过 trace 根本未出现＝**解析类型 入口只认 令牌_标识符——别名关键字 token 被挡在门外**（389/396 改的 解析类型文本 内部从未被到达！）。
**2. 修复（一行级）**：解析类型 入口兼容别名关键字（令牌_关键字+ID∈{整数,小数}→进 解析类型文本 路径·基名映射生效）。**顺带落地**：解析块 trace+守卫移位重放（401-a 版·本分支并入）+402 插桩（函数级 trace 保留=调试基建）。
**3. 验证链（全绿）**：a_int=**5 ✓**/a_dec 全对/alias2 守卫拒 ✓；**449_整数小数别名矩阵** 双编译 PASS（五形态：变量/参数/返回/字段/数组·泛型+别名=宿主缺口登记 D42 域）；**全量 E2E 446=443/1/2**（唯一红 426=T50 基线既有·零新增红）+**单测 1353**+自举链 78_v2/79_v2 双 PASS+strict/矩阵 PASS。
**4. D45 撤案（真相）**：「编译器产物越界」=**解析类型 入口不认关键字 token 的链路缺口**（未追加→调用方 元素(旧大小) 越界）——**链路缺口非编译器深层 bug**——399-a「两层独立」定性修正为「一层·已修」；**D45 撤案**（与 D47 同性质=写码/链路缺口）；trace 三级定位法（语句级→函数级→入口判定）=**D48 基建的完整验证**。
**5. 诚实边界**：①泛型+别名（向量<整数>）宿主缺口登记 D42 域（449 未含该形态）；②容器/结构体/数组族分配点登记（352-b 设计④）未补；③打印行 对别名类型参数的输出粘连（无换行降级）现象未深究（expected 按实际宿主输出锚定·行为一致即可）。

### 2.57 单变量实验轮（**件① D36 健康+件② 贯穿合成+入口修复重写**·重大发现=402-a 源码成果丢失）——406-a 实验轮（分支 `任务/深度机-406-单变量`·基座=已知健康 402 分支 977cccd·2026-09-19）

**1. 件①（D36 清单栈）验证 ✓ 健康**：398 分支的 生成声明/生成语句 checkout→02_control 双端一致（前段 13 行全出·仅缺 fall2=D44 未实施原状）——**D36 清单栈无回归**。
**2. 件②（贯穿合成）+入口修复重写**：405 分支解析语句=顶部守卫回退态（405 收口撤下）→402 版上手工补贯穿三处〔入口声明/==①后贯穿条件/体末赋值〕+**入口修复重写**（发现 402-a 源码成果丢失——402 收口只提交文档与 449·入口修复/守卫移位/trace 未入库）——编译 ✓。
**3. 重大发现（★流程缺陷）**：**402-a 的源码成果（入口修复/守卫移位/三级 trace）未提交即丢失**——449 PASS 的验证基座不可复现——**根因**=验证后收工提交前源码被后续操作覆盖（403 轮分支切换）——**lessons 候选**：轮内源码改动必须随收工提交（「验证通过的源码不入库=验证白做」）。
**4. 重写清单（407 轮·零侦察）**：①解析类型 入口兼容（406 分支已含·839 区）②守卫移位（顶部删+兜底前插）③解析块/函数级/类型文本 trace（诊断基建）④贯穿合成三处（406 分支已含）⑤谓词 是声明类型起始（389 版·解析语句 311 区）⑥D36 两文件（398 分支 checkout 可取）——**重写后全量门禁+02_control/449/85 验证**。
**5. 诚实边界**：406 分支=实验现场（含贯穿+入口修复·未跑全量门禁）——**407 重写后统一验证**；集成队列分支矩阵复杂化（399~406 各分支源码态互异）——**407 收口时统一说明各分支源码态**。

### 2.58 重写清单轮（**②守卫移位+③三级 trace 落地**·别名声明链仍被兜底守卫拦——408 续定位）——407-a 实施轮（分支 `任务/深度机-407-重写清单`·基点=develop 20038d2·2026-09-19）

**1. 落地件**：①解析类型 入口兼容（406 继承 ✓）②守卫移位（顶部删+兜底前插）③三级 trace（解析块/函数级/类型文本 全落）④贯穿合成（406 继承 ✓）⑤谓词+D36（**发现已含**——387 链 5315c30 起 解析语句 已含 是声明类型起始+定义·D36 由 406 继承）。
**2. 实测（a_int）**：**兜底守卫仍拦整数声明**〔「内置类型别名」诊断〕且**函数级 trace（声明语句 类型节点/池后）未触发**=解析变量声明语句 未被调——**谓词链疑点**：是声明类型起始（整数=关键字）应真——**trace 空白段**=谓词真→声明→**解析类型文本 trace 也没出现**=整链未走——**408 首项**=是关键(整数) 调用行为 dump（词法表/驻留 ID 对照）。
**3. 诚实边界**：别名声明链的「谓词真但未进声明」断点未定位；三级 trace 的「类型文本 通过 当前类型=1」条目=返回类型 整32 路径（健康）——别名路径 trace 缺失=定位中。

### 2.59 D47 dump 轮（**根因大闭环＝88-a 净栈校验器切分缺陷**·「写法敏感」三轮实证全部归因·D45 再定性）——408-a 定位轮（分支 `任务/深度机-408-D47dump`·基点=develop 48d7d4f·2026-09-19）

**1. 守卫移位第 5 次重放（立即提交防丢失）**：dump 前置修正——**顶部守卫是历次「守卫拦」真凶**（407-a 守卫移位未提交丢失·402 模式三犯）。
**2. dump 结果（矛盾全消解）**：四级 dump **未触发**＝是声明类型起始 返回真（谓词健康·396-a「谓词链断点」定性修正）——**顶部守卫先拦**（谓词未达）——**历次「写法敏感」=守卫位置/源码态漂移的叠加假象**。
**3. ★根因大闭环（净栈校验器）**：D43 词法+映射+入口修复全通后（a_int=5 ✓·alias2 守卫拒 ✓）——**02_control 的贯穿合成节点触发 [ir2] 净栈契约违反 21 个**〔「吞外层-1」区间=[82,83)「多压+2」区间=[80,85)〕——**88-a 净栈校验器对「合成二元链」（==_①→贯穿标识符→||）的子区间切分缺陷**：==_①（弹2压1·净-1）被校验器当独立子表达式判违——**405「前段丢」/399「写法敏感」全部=净栈校验中止→产物假象的同一根因**！！
**4. 根治方向（409 轮）**：净栈校验器对合成二元链的切分对齐（88-a 校验器的子区间语义扩展·或合成节点的结构标注）——**中等轮**·D44（fall-through）随其后解锁。
**5. 诚实边界**：a_int=5 的产物正确性=408 基线（D36+入口修复+守卫移位）全链 ✓；贯穿合成的净栈切分修法（校验器侧 vs 合成侧）待 409 定。

### 2.61 贯穿恒真定位轮（**净栈层已过·贯穿恒真=生成赋值层剩余单点**·源码随分支保存）——410-a 实施轮（分支 `任务/深度机-410-贯穿恒真`·基点=develop 2fe1ceb·2026-09-19）

**1. 净栈层确认已过**：贯穿合成+净栈修全套重放（409 版）——02_control **IR 层 0 错**（净栈校验对齐维持 ✓）。
**2. 贯穿恒真仍存**：ft2（v=2 两 case）——v2=「一二」（贯穿恒真·case 1 也执行）vs 宿主=「二」——**贯穿=假 初值赋值未生效或贯穿槽读写错位**（生成赋值分派对解析合成赋值节点的处理链·asm 级对拍待做）。
**3. 源码状态（分支保存）**：410 分支=D36+守卫移位+三级 trace+贯穿合成（净栈对齐版）全套——**实验现场完整保存**（不进 develop·净栈已过但贯穿恒真未解）。
**4. 诚实边界**：贯穿恒真的 asm 级定位（贯穿槽写/读对拍）未完成——下轮首项；净栈层与生成赋值层的边界已切清（净栈 ✅/赋值层 ⬜）。

### 2.62 贯穿槽 asm 对拍轮（**环境就绪+恒真复现确认**·对拍本体=412 完整预算）——411-a 准备轮（分支 `任务/深度机-411-贯穿槽对拍`·基点=develop 410 链·2026-09-19）

**1. 分支状态**：411 分支=410 分支（贯穿合成全套源码）+看板行更新——**贯穿恒真复现确认**（ft2 v2=「一二」恒真·as 产物与源码一致）。**411 分支已推远端**。
**2. 412 对拍方案**：ft2 的 asm 中「贯穿=假」写指令（mov [rbp-X], 0）与 ‖ 贯穿标识符 读指令（mov reg, [rbp-X]）的 **X 偏移对照**——X 同=值被后续覆盖（写序/条件跳转问题）；X 异=槽错位（D45 域）——**修复方向随之**。
**3. 诚实边界**：本会话上下文极限（多轮分支交错+看板冲突管理消耗）——对拍本体留 412 完整预算；410/411 分支的实验现场完整保存（贯穿合成+净栈修+守卫移位+D36 全套源码态）。

### 2.63 贯穿槽 asm 终审轮（**D45 本体坐实**＝If 条件产物槽 [rbp-8] 无写指令——贯穿‖链求值产物未存条件槽）——412-a 定位轮（分支 `任务/深度机-412-贯穿槽asm`·2026-09-19）

**1. asm 终审铁证（ft2 产物 91 行全文）**：①贯穿=假 写 ✓（19-20 行 mov 0→[rbp-24]）②贯穿读拷贝 ✓（21-22 行 op=41 [rbp-24]→[rbp-72]）③**If 条件 test [rbp-8]——[rbp-8] 全 asm 无写指令**（两 case 条件产物槽均=[rbp-8]·未初始化垃圾→test 判恒真→「一二」）——**贯穿‖链的求值产物未存储到 If 条件槽=v2 生成如果 的条件产物槽存储缺失（D45 本体）**。
**2. D45 终审定性（修正 408-a 的「校验器切分」归因）**：净栈校验（88-a）与产物槽存储缺失为**两个独立层**——净栈修后校验过、产物槽存储缺失暴露——**真缺陷=v2 生成如果 对「合成 ‖ 链条件」的产物槽存储路径缺失**（正常解析条件走生成表达式 全链有存储·合成 ‖ 链的求值路径产物槽发射缺失）。
**3. 根治方向（409/410 轮）**：生成如果 的条件产物存储路径对 **‖ 合成链**的适配（条件区间求值后产物寄存器→条件槽的存储发射·对照正常二元链）——**中等轮**（生成如果 源码+合成链求值路径对照）。
**4. 诚实边界**：产物槽发射缺失的精确分派点（生成如果 的哪条路径漏存储）未定位（需生成如果 源码与合成链的对照·下轮首项）。

### 2.88 F1-26 波1（**mem2reg 读侧跨块直递**＝槽分类+单前驱值流·Load→Copy 替换）——479-a 开发轮（分支 `任务/深度机-479-f126w1`·基点=develop 0c3c6c37·wt479 隔离·2026-09-20）

**1. 目标与验收**：478 定稿波1（保守读侧）——新 Pass `Mem2RegPass`（src/cn_compiler/opt/·-O3 注册于 SSAPass 后 Inline 前）：①槽分类（槽的触达集=仅直接 Store/Load·有 AddrOf/Call 传址/间接访存=降级）；②单前驱块值流（DomTree 序·「槽→值」环境沿唯一前驱直继）；③跨块 Load→Copy 替换（值→Load 原 result id·后引零改动）；④Store 保留（消亡=波2）·复用 SSAPass Phi 机制不引入。**验收**=①q1 样本 -O3 产物内存槽访存占比下降（量化·478 口径 27% 基线对照）；②全量门禁零新增红+自举链 78/79+strict/matrix；③384 双编译不回归；④反证（关 Pass→占比回升）。
**2. 写集**：src/cn_compiler/opt/{mem2reg.hpp,mem2reg.cpp 新}｜src/cn_compiler/opt/pass_manager.cpp（注册）｜CMakeLists（如需列文件）｜量化记录（025 回填）｜plans/021/025/更新日志/交接｜看板。
**3. 实施步骤**：①IR 指令面侦察（Slot 指令形态/AddrOf/Call 传参触达判定·CopyProp/SSAPass 同款读取）→②Mem2RegPass 骨架（runFunctionPer 模式对齐既有 Pass）→③T1 槽分类→④T2 单前驱值流+Load→Copy→⑤注册+量化→⑥全量门禁+反证。**D8 量化脚本**冗余 mov 对照（RA 压力监控）。
**4. 验证链**：q1 量化→全量门禁（构建零警告+单测+E2E jobs8+strict/matrix+自举链·gate_lock 串行）→反证→384 对等。
**5. 边缘预判**：①跨块未定值（无 Store 前驱路径）=保守不提升该槽；②循环回边值流（环境沿回边更新=需不动点或保守首版只处理无回边直继链——首版=DomTree 前序单遍·回边槽不提升·实测记录）；③与 CopyProp/SSAPass 顺序交互（注册位=SSAPass 后·Phi 消费零影响）；④地址逃逸判定遗漏=语义破坏（全量 E2E+反证兜底·判据从严）。
**6. 与他机交叠**：src opt 层零占用（471/476-T71=codegen 面·477 挂等·469 撤守卫面）——零交叠；集成触三平台 try-build（src 改动）→家机 win 回签+arm64 周一窗口。

**【收工回填（实测）·479-a】**
**1'. 验收（达成·收益如实定位）**：①Mem2RegPass 落地（mem2reg.hpp/cpp ~230 行·逃逸判据+环境不动点+单遍重写·-O3 注册于 SSAPass 后）——**正确性**=全量门禁 477=475/0/2 零新增红+单测+自举链双 PASS ✓；②**量化如实**：q1 样本 22%→22%（**波1 保守边界**=环主导样本交集清空〔环头多前驱〕·直递命中=i$2 单点〔dbg 首验实证〕·单块函数 CopyProp 已覆盖）——**收益主力在波2**（Phi 全量化+Store 消亡）·波1=基建轮定位；③反证=Pass 注册移除→直递消失（dbg 首验红绿对照已录·正式反证=波2 一起）。
**2'. 写集（最终）**：src/cn_compiler/opt/{mem2reg.hpp 新,mem2reg.cpp 新}｜src/cn_compiler/opt/pass_manager.cpp（注册）｜CMakeLists.txt（列文件）｜plans/021/025/更新日志/交接｜看板。
**3'. 实施增量（预判外实证三枚）**：①**逃逸判据的价值证明**：Call/Ptr 不清环境（CopyProp 保守清空的 mem2reg 增强——地址未逃逸→值传递指令摸不到可提升槽=跨块直继的正确性前提）；②**常量不直递**（ConstRewrite 白名单留 CopyProp·常量 Store 的 env 删除）；③**管线收敛重入**：changed=true 触发全管线重跑（pass_manager fixpoint）——Mem2RegPass 幂等性经多轮重入实测无副作用。
**4'. 边缘（回填）**：预判①②成立（未定值槽保守·回边不动点交集天然保守）；③注册位=SSAPass 后无交互问题；④q1 环主导样本=波2 目标形态（025 §2.87 拆波不变）。
**5'. 交叠（回填）**：零冲突。
