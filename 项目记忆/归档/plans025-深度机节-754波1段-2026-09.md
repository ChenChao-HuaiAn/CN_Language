# plans025-深度机节-754波1段-2026-09（788 治理轮滚转切片·封存不改）

> 来源=plans/025 §2.147（754 波1 定稿实施轮=768 续波1 终局清偿回填·已集成 a381784d·021 001 行有案）；滚转判据=AGENTS.md §6.7。

### 2.147 754 波1 定稿实施轮（**v2p 侧「元素/类实例 返回」retbuf[0]=句柄收敛＋接收源装配两跳化**＝753-a 三重铁证定音的修复实施·001 主线）——754-a 开发轮（分支 `任务/深度机-754-返回ABI统一`·基点=develop f14939e7·wt754 隔离·2026-09-25·P0）

**1. 目标与验收判据**：修复 753-a 定音的两代 ABI 错位链。验收=①**751 实验态**（cherry 751 分支生成声明.cn 24 行）下 113 重写版 v2 侧 **rc=0 输出对齐 expected（101 形态）**＝崩点根治因果实证②生产态 113 v2 侧「元素[0][1].类型ID」**101**（基线红清偿向）③反证双态（撤修复→崩/值差复现）④全量门禁红名单不劣化。
**2. 写集（预登记·归因后回填）**：`CN语言编译器v2/IR/生成声明.cn`（751 接收段源装配）＋`CN语言编译器v2/IR/生成语句.cn`（748-a 分支/返回语句·归因定）＋`CN语言编译器v2/IR/生成表达式求值.cn` 或 `生成左值地址` 所在文件（this.字段 下标链读修复·归因定）＋文档六件套。**零 src/**。
**3. 实施步骤**：①**t5 探针**＝最小「元素 方法返回类实例」（向量 样板+返回 数据[i]）实测 this.数据 字段读发射（753-a 铁证：rsi=栈 0x7fffffffd710·池数组应在堆＝**元素 方法体内 this.数据 下标链左值地址读失效（T94 家族）**）→②按根因修复（候选=生成左值地址 的 this.泛型字段 偏移解析/this 槽链）→③751 接收段（生成声明.cn）源装配=物化临时槽 [0]=句柄·传槽地址→④深拷态 113 跑通＋反证双态。
**4. 验证链**：v2p 重建 rc=0→113 双侧单跑→t4 探针→全量 E2E --jobs 8＋单测＋strict/matrix→反证双态→integrate。
**5. 边缘情况与诚实边界（预判）**：①this.数据 失效若落 T94 深水（泛型字段偏移表实例化 miss）→按 550-a 清单（用户裁决甲）顺路实施②127_v2=邻域顺带核③win 侧家机异步验收。
**6. 与他机交叠**：写集=CN语言编译器v2/IR 2~3 文件——家机 701=代码生成/ 3 文件**不同域**；语义耦合=v2p 重建面（集成串行）。独立 ✓。

### 2.146 052/055 完整深拷波 fresh context 完整设计＋实施（751 defer 承接·001 主线收官总设计）——753-a 开发轮（分支 `任务/深度机-753-完整波设计`·基点=develop 09cd0ec0·wt753 隔离·2026-09-25·P0）

**0. 总设计（fresh context·收束 740~751 全部实证）**——目标语义=**副本语义全链统一**（051 甲/052 甲·用户 2026-09-23 裁决；056 甲 in-out 正交·用户 2026-09-24 裁决）：「元素 读取/返回=独立深拷副本；容器按值形参=in-out 写回；类实例赋值/返回/传参=拷贝构造深拷」。改造分三面（〔基准=019〕§3.1 四句：默认路径零规则=副本天然无别名·拷贝消除是编译器义务·显式 转移() 另册既有）：
- **甲面（宿主 cn.exe·055 ABI 手术）**：①元素 返回发射（ir_stmt.cpp return 容器类→retbuf 按值返回）②声明接收（ir_stmt_decl.cpp isContainerElementView→NewObject+**拷贝构造深拷**〔非字节拷贝——751 实证浅拷共享内部数组×链查找崩〕+析构登记）——742-a 已定位四面联动（visitReturnStmt/isContainerElementView/genOwnedAggregateReturn/H8-5 接收点）。
- **乙面（v2 树镜像同构）**：①生成声明.cn 返回接收分支=NewObject+拷贝构造（751 实验+24 行已有底·崩在内层）②生成返回语句 双点③「复制(struct)」内置展开两代分叉修复（生成表达式求值.cn:250+·751 锁定入口）④IR 生成器层 & 化第二批+诊断写穿链 7 断点（708-c 审计清单→cn_self -6 清偿向）。
- **丙面（stdlib/v2 树源码）**：「元素取回-写入」别名模式→写回写法（707 语义层 88 处已入 develop·IR 层未做）；写回接口=向量.设置(位置,值) 现成。
- **波次路线（每波自身完整闭环）**：波1=751 崩点根治（本债）→波2=055 宿主 ABI（甲面）→波3=乙面④+丙面 IR 层（cn_self 链）→波4=全量门禁+借链 51 例+撤守卫零红→001 销项。t4→0/fix_s 67157→0/-7 固定点判据挂波3。

**1. 本轮目标与验收判据（波0+波1）**：完整设计落盘（上）＋751 崩点根治——751 实验态（生成声明.cn 深拷返回接收·cherry 751 分支 dfc241c6 的 24 行）下 **113 重写版 v2 侧 rc=0 输出对齐 expected（101 形态）**＝崩点根治因果实证；反证双态（撤崩点修复→rc=139 复现·撤 751 改造→回生产态基线红「元素[0][1]=0」）。

**2. 写集（预登记·归因后回填）**：`CN语言编译器v2/IR/生成声明.cn`（751 改造 cherry-pick）＋崩点修复面（候选=`生成表达式求值.cn`「复制」展开／`生成类.cn` 拷贝构造发射／`代码生成*.cn`·归因定）＋文档六件套。**零 src/**（波1 仅 v2 树）；波2 起 src/ 另轮（try-build 三平台）。

**3. 实施步骤**：①wt751 复现 751 崩态（重建 v2p→113 跑→gdb 崩点精确归因）→②「复制(struct)」两代展开对拍（宿主 cn.exe asm vs v2p asm·同源码同函数）→③定点修复→④深拷态 113 跑通+反证双态→⑤（本轮若顺）波2 055 宿主手术开工。

**4. 验证链（命令级）**：v2p 重建 rc=0→113 双侧单跑（`run_e2e.py --filter 113`）→深拷态 v2 侧 rc=0+输出对齐→反证双态→全量 E2E --jobs 8（红名单=113/127_v2·波1 后向 113 绿演进）＋单测＋strict/matrix→integrate.py（v2 树写集=纯 CN 树·linux 本机全量门禁+他机异步验收）。

**5. 边缘情况与诚实边界（预判）**：①751 崩点归因未定（「复制(struct)」分支待查=751 遗留）——波1 第一步=独立归因不许预设；②若归因落 T94 家族（this 句柄链/成员基址）→按 550-a 清单实施（用户 2026-09-25 裁决甲·看板 550 行既定）③深拷态下的 值数组=NULL 三候选根因：拷贝构造体内 this 字段写失效（T94）/「复制(struct)」展开返回垃圾（复制分叉）/拷贝构造调用实参装配错位（698-a T& 契约·「传指向句柄槽的指针」）④win 侧=家机异步验收⑤波1 只清 113 v2 侧崩面；127_v2=邻域顺带核（不承诺本轮清偿）。

**6. 与他机交叠**：家机 701（静态128·v2 代码生成 3 文件）/567-d1assign（⏸ 挂起）；单位机空闲。波1 写集=IR/生成声明.cn+生成表达式求值.cn 或生成类.cn（归因定）——与 701 写集（代码生成/代码生成X64Linux.cn/代码生成ARM64.cn·静态段发射）**文件不同域**；语义耦合=v2p 重建面（集成串行·全量门禁各自把关）。独立 ✓。

**【753-a 阶段回填（波1 归因收官·三重铁证·零代码变更纯分析轮·实验态已撤净）】**

归因方法链：751 实验态复现（runner 113 v2 侧 rc=-11 复现 ✓）→gdb 崩点对位（bb24_12=映射$整64$符号$映射(映射&) 拷贝构造循环体·406430「mov (%r10),%r10」·r10=0x1）→断点计数法（到达#1~#10 逐次寄存器/槽）→watchpoint 写入序列→gdb 全槽 dump（源槽/壳/循环索引/this 四槽）→探针矩阵排除（t1 非泛型字段写/t2 守卫块偏移 0/t3 泛型类/t4 完整仿映射拷贝构造·**四探针 4 段 alloc 直写形态全对**·wt753 target/p753probe/）→插桩双分支（生成标识符赋值·整体赋值分支零命中=508-a 排除·字段写分支全命中）→**终局=gdb 全槽数据闭环**。

**三重铁证（同根=「类实例返回/接收」两代 ABI＋T& 源槽契约层数未统一〔基准=019〕）**：
1. **751 崩机理（gdb 计数+watch+全槽）**：113 v2 产物映射拷贝构造恰 3 次调用——①追加 内（this=0x693420·源槽=retbuf·**正常**）②元素 方法内 748-a 镜像分支（this=0x6955b0·源槽=retbuf=0x7fffffffd710·**retbuf[0]=0x7fffffffd718=retbuf+8＝栈内部指针≠句柄**＝「元素 返回」retbuf 物化形态内部错位→副本 X2.键数组=栈垃圾）③751 接收（this=0x697690·源=返回值reg=**X2 对象地址**——拷贝构造 T& 契约=「指向壳的槽」（698-a 教训·两跳=源槽→壳→壳+off）·传对象地址=**少一层解引**→*(X2)=X2.键数组[0]=键值 1 被当壳→循环读 (0x1) 崩 rc=139）。
2. **748-a 镜像分支同病**：元素 方法内 NewObject+拷贝构造（生成语句.cn 447+·748 集成）的副本 X2 键数组=retbuf+8（栈垃圾）——「元素 返回」的 retbuf 物化（签名 56B）与「句柄值」两代形态错位（宿主=rax 句柄值·v2p=retbuf 槽）。
3. **生产态值差同根**：顶层=符号表.元素(0) 生产态=「rax(retbuf) 直存槽」（749 定音）→顶层=栈临时·内容=物化错位值→「元素[0][1].类型ID」v2=0 宿主=101（develop 基线红名单）。

**排除面（本轮实证·后续轮勿重查）**：v2p 发射器的字段写/整体赋值分派/守卫块/泛型实例均无缺陷（四探针+插桩实证）；750「浅拷」与 751「深拷」的**对接层（类返回 ABI）**才是病灶——740~751「修一层崩一层」三度实证的统一解释。

**波次修正（下一棒·完整波波1 定稿）**：「类实例返回 ABI 两代统一」——**v2p 侧波1**：①「元素/类实例 返回」retbuf 物化收敛=**retbuf[0]=句柄**（对齐宿主「retbuf 浅值」·748-a 分支源装配随行核）②751 接收段（生成声明.cn）源装配=**物化临时槽 [0]=句柄·传槽地址**（满足 T& 两跳契约·698-a 原案）；**宿主侧波2（055）**：ir_stmt.cpp return 容器类→retbuf 按值返回＋ir_stmt_decl.cpp 接收=NewObject+拷贝构造（源=T& 槽指针契约）——两代同一「retbuf[0]=值＋接收点两跳深拷」模型。判据链不变：113 双侧绿→127_v2→t4→0→fix_s→零红→借链 51 例→001 销项。

### 2.145 T94 本体改造轮（**v2 树生成声明.cn 返回接收分支=NewObject+结构体拷贝**＝retbuf 栈临时被后续调用覆盖根除·749 定音「顶层=retbuf 栈临时」机理落地）——750-a 开发轮（分支 `任务/深度机-750-T94本体`·基点=develop 9656d3db·wt750 隔离·2026-09-25·P0）

**1. 目标与验收判据**：生成声明.cn 的 是返回接收 分支（生成容器局部）——**原=「rax（retbuf 地址）直存槽」＝函数帧共享 retbuf 被后续调用覆盖→顶层 内容毁**（749 定音：元素[0][1]=0）——**改造=NewObject(类大小)+结构体拷贝(retbuf 内容→堆对象)+槽存新对象句柄**——**验收**=①113 重写版 v2 侧「元素[0][1].类型ID=101」（与宿主一致）②expected 固化（12 行双侧一致）③t4→0 维持④全量门禁（528=529/0/2 向·113 净增绿）⑤反证双态（撤改造→红复现）。
**2. 写集（预登记）**：`CN语言编译器v2/IR/生成声明.cn`（是返回接收 分支·单点）｜`tests/e2e/113_H8_*/主.expected`（重固化）｜文档六件套。**零 src/**（v2 树单文件）。
**3. 实施步骤**：①读 是返回接收 分支精确文本（生成声明.cn 449+）②改造=IR_分配对象(类大小)+IR_结构体拷贝(新对象←retbuf 内容 56B)+IR_存储(新对象→槽)③重建 v2p→113 v2 侧验证④expected 固化⑤全量门禁+反证。
**4. 验证链**：113 双侧绿（重写版）→t4→0 维持→零警告构建+单测 1405→E2E 全量 --jobs 8→strict/matrix→反证双态→integrate（win 侧家机异步验收）。
**5. 边缘情况与诚实边界（预判）**：①**浅拷到堆**＝副本与池[0] 共享内部数组指针（池[0] 析构→副本悬垂——113 场景副本先于池析构使用完毕=安全 ✓·深拷=052 甲完整波②该改造=**全库 返回接收 形态行为变化**（类实例返回接收=从视图/转移变独立副本）——**回归面=全量 E2E 的 返回接收 用例**（结果/可选 返回接收=H7 既有形态**不受影响**——仅类实例）③**类大小=0 的类**（空类）防御④win 侧家机异步验收。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。

**【750-a 阶段回填（浅拷到堆实验→链查找崩·深拷必要性实证·止损收工）】**
- 波2a v2 树镜像改造实施（生成声明.cn 返回接收=IR_分配对象+IR_结构体拷贝 56B 浅拷到堆+槽存新对象）→v2p 重建 rc=0→113 重写版运行 **rc=139 崩在 映射$整64$符号$链查找 内**（0x406cea·_J_AJ）＝**浅拷副本（共享池[0] 内部数组指针）×链查找/设置 交互的深层耦合崩**——**浅拷到堆不可解（740 教训重演：单点浅拷引入新耦合）**。
- **最终定音（三重实证收敛）**：①740 铁证「self-copy 毁源」②749 定音「顶层=retbuf 栈临时被覆盖」③750 浅拷到堆→链查找崩——**唯一正解=052 甲完整深拷波**（元素 返回=NewObject+**拷贝构造深拷**（非浅拷）·对齐 cn.exe 波2a 形态——**v2 树的 拷贝构造调用发射**（方法符号+调用实参）为本体·涉及 生成返回语句+生成声明 双点）——**defer 至独立专项**（052/055 完整波·fresh context）。
- 750 收工：改造实验撤销（checkout·回 749 收工态=基线红 2）·零代码变更·纯分析轮·快速门禁。
- **001 销项链依赖（最终）**：**052/055 完整深拷波（v2 树 拷贝构造发射双点）**→113 重写版绿→127_v2 静态组合→零红→借链终章→001 销项；并行=057/550 按甲+T100/T101/T102 立案。

> 〔2.144 113 清偿轮（749-a·用例重写+expected 固化·已集成） 已滚转归档（753-a 棘轮·2026-09-25·内容 develop aefaabc6+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.143 T94 阶段二攻坚轮（**v2 引用形参容器成员调用 this 句柄链修复**＝001 销项链依赖链之首·747 实验实证缺口·550 收工回填既知修复方向）——748-a 开发轮（分支 `任务/深度机-748-T94阶段二`·基点=develop 4148a621·wt748 隔离·2026-09-25·P0）

**1. 目标与验收判据**：v2（v2p 发射）的 **引用形参容器成员调用 this 链**修复——747 实验实证：113 源码引用化（&形参 4 处）后 v2 侧「符号表.追加/设置」**不生效**（this 链「v 槽→表槽→堆对象 少一层」·T94 定性）——修复方向（550 收工回填既知）＝**语义追踪 ptrLocalPointees_ 判槽指向**（「指向变量槽的指针」成员调用多一层 load）——**验收**=①113 引用化（&形参）双侧绿（v2 侧输出对齐宿主 101 形态）②113 恢复按值形态维持基线（不回归）③零红向（全量红名单=113/127_v2→收敛）④全量门禁→反证双态。
**2. 写集（预登记）**：`CN语言编译器v2/语义/`（语义追踪 ptrLocalPointees_·或 生成层 this 装载修正·按诊断定）｜`CN语言编译器v2/IR/`（生成函数 形参装载/生成表达式 成员调用 this 链）｜`tests/e2e/113_H8_*/主.cn`（引用化形态·测试升级报备）｜文档六件套。
**3. 实施步骤**：①诊断=113 引用化最小复现（压入+注册 两函数）+v2p 发射 asm 对位（this 装载链）→②根因定位（语义追踪缺失点 vs 生成层 this 装载）→③修复（ptrLocalPointees_ 判槽指向·550 方向）→④113 引用化即绿验证⑤回归（非引用形态不回归）⑥全量门禁+反证。
**4. 验证链**：113 引用化双侧绿→单测 1405→E2E 全量 --jobs 8→strict/matrix→反证双态（撤修复→113 引用化红复现→恢复→绿）→integrate。
**5. 边缘情况与诚实边界（预判）**：①ptrLocalPointees_ 若为 cn.exe semantic 既有机制=v2 树语义层镜像实现；若需新建=独立追踪表（引用形参→目标槽映射）②修复波及=引用形参容器成员调用 全域（std 编译器内部+用户代码）——全量回归面③T94 原始场景（泛型类指针字段）顺带验证④127_v2 静态组合=邻域顺带核。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。

### 2.142 113 引用形参修正轮（**113/127_v2 清偿最小波**＝746 定音承接·用例源码 容器按值形参改引用形参 &·测试升级报备＋056 甲 in-out 语义级 defer 立案）——747-a 开发轮（分支 `任务/深度机-747-113引用形参修正`·基点=develop 10394c6c·wt747 隔离·2026-09-25·P0）

**1. 目标与验收判据**：113/127_v2 用例源码的 压入/注册/查找（及 127_v2 对应函数）容器按值形参改**引用形参 &**（正确系统写法：容器按值传=整体深拷低效且语义分叉源·引用传递=CN 既有支持〔表注册/表压入 编译器内部先例〕）——**expected 保持 101 形态不变**（引用下注册直达生效·两代一致）——**验收**=113/127_v2 双侧绿（单跑+全量）→零红确认（528=528/0/2 向）→反证（撤 & 恢复按值→红复现）→全量门禁→集成。
**2. 写集（预登记）**：`tests/e2e/113_H8_容器持有类对象/主.cn`＋`tests/e2e/127_v2_*/主.cn`（源码形参 & 化·测试升级报备随轮）｜`plans/021`（056 甲 defer 立案·T102）｜文档六件套。**零 src/零 v2 树代码**（纯用例修正）。
**3. 实施步骤**：①113 源码 形参 & 化（压入/注册/查找）②127_v2 源码同款（按其结构）③双侧单跑验证④全量门禁⑤反证。
**4. 验证链**：113/127_v2 单跑双侧绿→单测 1405→E2E 全量 --jobs 8（gate_lock·**零红向 528=528/0/2**）→strict/matrix→反证双态→integrate（win 侧家机异步验收）。
**5. 边缘情况与诚实边界（预判）**：①引用形参的 嵌套容器（向量<映射$..>&）v2 发射=T93 家族风险（550 阶段二域·实测见分晓）②127_v2 的静态/引用交互形态——若 & 化后仍红=该面独立缺口立案（不阻塞 113）③056 甲（in-out 语义级·T102 立案）=defer 独立专项（既定裁决不变·025 §1.18/§1.26 承接）④测试升级报备随轮（AGENTS §4）。
**6. 与他机交叠**：家机无在飞；单位机空闲。独立 ✓。

> 〔2.141 055 波2 宿主元素副本轮（746-a·波2a 落地+113 定音＝两代按值形参语义分叉＝056 甲域） 已滚转归档（749-a 棘轮·2026-09-25·内容 develop 5e4a4235+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
> 〔2.138 055 宿主 ABI 手术分析轮（742-a·四面联动设计约束·零代码） 已滚转归档（755-a 棘轮·2026-09-25·内容 develop dd6c47fb 承接+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
### 2.137 元素引用直达轮（**052 甲双件回归②＝宿主内置类表增 元素引用＋容器.cn 源码＋写回单件链式化**·根除 self 覆盖析构顺序毁源〔740-a 回填⑯定音〕）——741-a 开发轮（分支 `任务/深度机-741-元素引用直达`·基点=develop d0653b48·wt741 隔离·2026-09-25）

**1. 目标与验收判据**：写回单件由 `符号表.设置(0, 顶层)`（self 覆盖·析构先毁源）改为 `符号表.元素引用(0).设置(名ID, s)`（直达池·零中转）——**验收**=①写回态 v2p 编 t4 rc=0（不崩·740 复现态消除）②打补丁 v2p 编 v2 全树 rc=0→cn_self 构建→cn_self 编 t4 rc=0 零诊断（**t4→0 判据②**）③全量门禁红名单同构（113/127_v2/79_v2 基线）④反证双态（撤 元素引用 守卫链→崩复现→修复→绿）。
**2. 写集（预登记）**：`src/cn_compiler/semantic/`（内置容器类表增 元素引用 签名·仿 追加/设置 注册形态）｜`stdlib/容器.cn`（向量 元素引用(位置)->T& 源码·对齐 元素 越界行为）｜`CN语言编译器v2/语义/语义分析.cn`（写回行链式化·实验态）｜文档六件套。
**3. 实施步骤**：①grep 宿主内置容器方法注册点（「追加」签名注册形态）→②同步增 元素引用（T& 返回·复用 T95/551-a 引用返回框架）③容器.cn 源码同步④写回行改⑤重建验证链⑥t4→0 后评估 写回单件/元素引用 转正（052 双件回归②收口）。
**4. 验证链**：写回态 v2p t4 rc=0→fix_p rc=0→cn_self 构建→cn_self 编 t4 rc=0（t4→0）→零警告构建+单测+E2E 全量 --jobs 8（gate_lock）→strict/matrix→反证双态→integrate。
**5. 边缘情况与诚实边界（预判）**：①内置类表同步可能不止 semantic 一处（IR签名扫描/类解析 双轨·740 回填⑰实证「没有成员」来自语义层）②元素引用 的 T& 返回在 泛型实例（向量$映射$整64$符号）的 mangling 需与 550 阶段二「_J 修饰」家族核对③链式调用 `元素引用(...).设置(...)` 的成员链解析（引用返回再调方法）依赖 是引用返回调用/551-a 框架·若不支持则降级两步形态④win 侧复验=家机异步验收。
**6. 与他机交叠**：家机 738 收工无在飞（701/705~708 挂等/保全·写集零交叠）；单位机空闲；057/550 排队未开。独立 ✓。

**【741-a 阶段回填（实施受阻·新缺口立案·状态保全）】**
- 实施①实测：容器.cn 增 元素引用(位置)->T&+写回行链式化→cn.exe 编 v2 树 rc=1「类 '向量$映射$整64$符号' 没有成员 '元素引用'」——**新缺口立案（T100 号候选）＝stdlib 首例「泛型方法返回 T&」**：grep stdlib 全库「T& 返回」零先例——泛型 T& 返回在 方法收集/泛型实例化成员克隆（generics.cpp）/签名扫描 链上存在缺口（T95「返回 T」551-a 修过返回不带 &·T& 组合=新边界）。
- 实验已撤销（容器.cn/语义分析.cn checkout 恢复）——**修复实施需要的攻坚面**：①generics.cpp 实例化成员克隆对「返回类型=泛型 T+&」的处理（substTypeParam 后形态「映射$整64$符号&」的 findClass/物化）②方法签名收集的 T& 过滤点③签名扫描（IR签名.cn）对 T& 的编码——三层任一缺口即阻断。
- **结构性说明（诚实边界）**：写回单件「self 覆盖毁源」在 052 甲（副本全链）落地后自然消解（副本≠池本体·设置 跨对象拷贝合法）——**T& 首例攻坚非 t4→0 的唯一路**：备选=甲线（发射器 self 守卫·cn.exe 单点）与 055 宿主 ABI 手术（既定波次）均可独立达成 t4→0。
- 下一迭代入口：①T& 缺口三层定位（generics.cpp substTypeParam→方法收集过滤点→签名扫描）或②甲线 cn.exe 赋值发射 self 守卫③择一完成→验证链（回填⑯清单）→集成。

**【741-a 阶段回填②（结构性定音·t4→0 依赖本质）】**
- 甲线注入点分析：析构被移除 注入体（ir_oop_container.cpp:149+·155 行「析构被移除 return true=对有析构类 no-op（76-a 设计）」）——**注入体只有一个参数（位置）·无法判定 新值地址==槽地址**（self 检测需调用方层·容器.cn 设置 语言层无法取地址比较〔&容器=550 甲未落地〕）——**甲线单点不可达·与丙线同源**＝**根因收敛为双模语义未统一（052 甲未落地）**。
- **结构性定音**：写回单件语义随两代模型相反——**v2p（句柄写穿模型）**：顶层.设置 已直达池[0]·写回=纯冗余+self 毁（740 实测崩）；**cn_self（副本模型·v2p 发射）**：顶层=真副本·设置 写副本丢弃→**必须写回**才能 t4→0——**同一源码两代语义相反 ⇒ t4→0 判据② 的唯一达成路径=052 甲副本语义统一落地（055 宿主 ABI 手术）**·任何单点守卫/改写均跳步不可达（740~741 五种修复尝试全数实证此结论）。
- **741 轮收工定调（诚实边界）**：本轮产出＝「T& 首例缺口立案（T100 候选）+t4→0 依赖本质定音」——**修复实施正确归属=055 宿主容器类实例值语义 ABI 手术**（025 §1.13 既定六要素·fresh context 专项·P0）·**055 落地即同时消除：self-copy 崩（本根因）+113 值差+127_v2 域（001 主攻面三合一）**。741 撤销全部实验（工作树净）·验证链=无代码变更纯分析轮（快速门禁）。
- **队列建议（下会话）**：**055 专项开工**（025 §1.13 六要素既定·宿主 ir_stmt/ir_stmt_decl 两点改造）→052 甲双件回归（元素引用·依赖本体的 T& 缺口随宿主副本模型一并攻坚）→t4→0→fix_s 链→001 销项；057 按甲/550 阶段二按甲随队列。

> 〔2.136 001 定点修复轮（崩溃定位·回填①~⑰·740-a） 已滚转归档（742-a 棘轮·2026-09-25·内容 develop 3d28f530+git 可考·切片=项目记忆/归档/plans025-深度机节-2026-09.md）〕
