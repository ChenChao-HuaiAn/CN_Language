# plans/025 家机节切片：729/730/731/732/733 侦查段（2026-09·738 棘轮滚转·纯搬运零改写）

### 1.38 【729 侦查】v2p 崩溃链全帧符号对位（接 728）（分支 `任务/家机-729-v2p崩溃链符号对位`·wt698 树·基线 develop 66aa925d·2026-09-25）

**1. 成果（方法学）：手动重链 v2p 加 /MAP**（79 rsp 模板换 v2p.obj·/OUT=v2p_mapped.exe·**/MAP 未生效但原 rsp 的 /MAP 指向被同次链接覆盖成 v2p 映射**——79_a0306084_self_link.map 06:01 版=5157 符号的 v2p 映射）→**崩溃链 9 帧全对位**：
```
0x20eff3 ∈ 代码生成$代码生成 (+0xc7)              ← 崩（fault=0x1FD0）
0x2111d0 ∈ 代码生成$是裸符号操作数 (+0x7c4)
0x1621eb ∈ 生成语句赋值$尝试下标结构体整体写 (+0x2291)
0x16ae04/0x1687e6 ∈ 生成类$生成方法 (+0x4191/+0x1b73)
0x185242 ∈ 生成类$发射类拷贝构造初始化 (+0x117)
0x1063f8 ∈ 生成表达式$生成表达式 (+0x2e2e)
0x102764 ∈ 生成表达式$生成标识符加载 (+0x1fc2)
```
**2. 语义链定性**：**v2p 的生成层自崩溃链**=发射类拷贝构造初始化（给 映射 类发拷贝构造方法体）→生成方法→**尝试下标结构体整体写**（生成语句赋值.cn:750·处理 `桶数组[桶索引]=...` 形态·命中后发射 **IR_结构体拷贝**）→**代码生成$代码生成 +0xc7 崩**（fault=0x1FD0=读偏移 0x1FD0 的表/字段·NULL 基址）——**发射了畸形 IR（操作数/宽度异常）致代码生成分派读表越界崩**——与 728 的 fault=0x1FD0 完全同址（同一崩溃点两种触发路径：728=执行期·729=编译期）。
**3. 下一棒（730）**：读 **代码生成.cn 的 代码生成 函数 +0xc7 附近**（前 30 行·定位读哪个表/字段）+**尝试下标结构体整体写 的 IR_结构体拷贝 发射段**（源句柄/目标地址/宽度的值构造——宽度=垃圾嫌疑）→定点修复（宽度/操作数守卫）→v2p 跑 t4 零错→双件回归→t4→0→fix_s 判据。
**4. 交叠**：纯侦查零写集（v2p_map_link.rsp/map 工具留存）。

### 1.39 【730 侦查】代码生成 PROC +0xc7 段在案+v2p.asm 直读（接 729）（分支 `任务/家机-730-代码生成定点修复`·wt698 树·基线 develop 41180856·2026-09-25）

**1. 成果**：①**PROC 起点精确定位**（map 反推 0x20eff3−0xc7=0x20ef2c=「代码生成$代码生成」）→**v2p.asm 直读该 PROC 前 50 行（在案）**：序言=**结构体参数 程序（程序IR）按值 56B rep movsb 拷贝**→`__cn_object_new(24)` 分配 输出 向量→`向量$字符串$向量` 构造→**追加×2**（`@str545="…P6…生成"`/`@str546=".code"`·各带 __retbuf 12B）→后续 函数们 循环；**+0xc7=199 字节≈行 46-56 区=第二个追加（".code"）的调用/复核段**——**崩溃点≈第二追加区**（fault=0x1FD0 读）。
**2. 旁证**：「是裸符号操作数」（+0x7c4 引擎位）源码审查=**纯字符串处理**（长度/字节访问/寄存器判据）——**无大表读取**——fault=0x1FD0（0x8032 变址）**指向悬垂/垃圾字符串实参或临时生命周期**（A2 借用契约域）——与 rbp 链 frame00/frame01 的归属矛盾并列为待解项（帧链方向/符号区间归属需复核）。
**3. 下一棒（731）**：**代码生成.cn 追加点插桩**（前段三处追加 + 函数们循环前·stderr 打印步号/参数字符串长度）→宿主重建 v2p→跑 t4→**源码级执行序列直击**（哪个追加/哪一步崩·参数是什么）——必然有效路径（不依赖帧链/符号归属）。
**4. 交叠**：纯侦查零写集（v2p.asm 段+map 工具在案）。

### 1.40 【731 插桩直击】[cg] 未打=代码生成未执行·崩溃在发射链更内层（接 730）（分支 `任务/家机-731-追加点插桩直击`·wt698 树·基线 develop 9e6801c7·2026-09-25）

**1. 实验**：写回单件（触发条件）+代码生成.cn 追加点 5 处 stderr 插桩（[cg1]~[cg5]）→宿主重建 v2p rc=0→跑 t4 崩（RVA=0x20f089·fault=0x1FD0·布局因插桩漂移 +0x96·链帧与 730 全同构）。
**2. 决定性排除**：**[cg1]~[cg5] 零输出=「代码生成」顶层函数从未执行**——崩溃发生在**发射链更内层**（生成类$发射类拷贝构造初始化→生成方法→尝试下标结构体整体写 链内）——**730 的「0x20eff3 ∈ 代码生成+0xc7」归属被本轮证伪**（map 区间在 PROC 相邻带不可靠·以插桩实测为准）。
**3. 高嫌疑机理（源码级）**：尝试下标结构体整体写（生成语句赋值.cn:750·v2p.asm PROC=247548 行 3477 行）内部关键调用=**「IR容器释放$发射结构体整体拷贝」**（行 2368/3332）——其源码：`IR追加(建IR指令(IR_结构体拷贝, 源地址, 目标地址, 0, 0, **查结构体大小(结构体ID)**, 0))`——**IR 宽度=查结构体大小(结构体ID)**——**结构体ID 若为垃圾/越界 → 查表读 [表基+ID×元素大小]=偏移 0x1FD0 → 越界读崩**（与 fault=0x1FD0 完全吻合）——**结构体ID 的推导来源**（调用点传参）为下一侦查靶点。
**4. 下一棒（732）**：读 尝试整体写 的 发射结构体整体拷贝 调用点上下文（生成语句赋值.cn 2368/3332 区·结构体ID 的来源推导链：推导下标元素类型ID/推导成员链类型ID）+查结构体大小 实现（IR布局.cn）→**结构体ID 垃圾来源定位→修复**（守卫：ID 未登记/0 时拒绝发射并诊断）→v2p t4 零错→双件回归→t4→0→fix_s 判据。
**5. 交叠**：实验改动（写回单件+插桩）当前在树·待修复确认后清理。

### 1.41 【732 实施】映射未构造防御（链查找守卫）+崩点前移实证（接 731）（分支 `任务/家机-732-结构体ID守卫修复`·wt698 树·基线 develop 6fbb96b0·2026-09-25）

**1. 机理闭合（fault 反推）**：fault=0x1FD0=1018×8——**散列值 h=1018**——**「静态映射 结构体大小表 未构造（零初始化：桶数量=0·桶数组=无）→ 桶索引 掩码 (0-1)=全1 → h=散列 → 桶数组[h] 读 [NULL+h×8]→崩」**（映射集合.cn 链查找 实测路径）。
**2. 修复（行为面·stdlib/映射集合.cn）**：**链查找 入口未构造/空桶防御**（`桶数量 == 0 || 桶数组 == 无 → 返回 -1`·视为空映射）——Rust 风格「未初始化容器安全失败」。
**3. 验收**：①守卫态全量 E2E **528=524/4 零回归** ✓（4 红=已知基线）；②「写回触发+守卫」组合实验：**崩点前移**（fault 0x1FD0→**0x8**·RVA=0x210d01=**新 NULL+8 读**）——**第一崩点被守卫挡住·露出下一环**（同一缺陷链的下一节点）。
**4. 下一棒（733）**：对位 0x210d01（新 map 重链）→**NULL+8 读的下一守卫/修复**→迭代至 v2p t4 零错→全量 524/4→写回+元素引用双件回归→t4→0→fix_s 判据。
**5. 交叠**：写集=stdlib/映射集合.cn（守卫·v2 树写集）-全量门禁已过。

### 1.42 【733 侦查】按值聚合实参形态错位点精确定位（生成表达式求值.cn:513·595-a 立案的「第三回扫源」）（接 732）（分支 `任务/家机-733-按值聚合实参形态侦查`·wt698 树·基线 develop c842cdca·2026-09-25）

**1. 定位（源码直读）**：生成表达式求值.cn:513-530（容器方法调用实参发射·B3 结构体按值实参通道）：
```
如果 (!是容器类(段文本) && 查结构体大小(驻留(段文本)) != 0) {
    回扫实参取地址(指令, 弹出们, 实i, 寄存器计数);   // ← 调用方取地址
}
```
**595-a 立案注释原文**：「此处按 签.参数串 段 查结构体大小 判取地址——**撤守卫态容器实参段命中 → 取地址传栈槽、被调方 fix5 单槽当句柄 → 元素槽伪对象**。直改（!是容器类 排除）触发 v2p 自身编译期运行时错误 1024（机制待独立定性）——**本点撤守卫态容器方法容器实参面=下轮主攻面**」。
**2. 定性（与 732 fault=0x8 同族）**：**「调用方取地址」与「被调方 fix5 单槽当句柄」的形态配对错位**——被调方（参数装载发射/生成函数）对「结构体/容器按值实参」按句柄单槽装载（读 形参+8 字段=NULL→fault=0x8 ✓ 完全吻合）——**056「容器按值实参传址化」的实施本体=两侧配对统一**（取地址方向 or 句柄方向·取址化=056 甲裁决方向）。
**3. 下一棒（734）**：读 **回扫实参取地址 实现**（生成表达式求值.cn:1083 族①）+**被调方 fix5 单槽句柄装载**（生成函数/生成声明 的形参装载发射）→**配对矩阵定案**→**056 实施：容器按值实参传址化两侧统一**→v2p 跑 t4 零错→全量 524/4→双件回归→t4→0→fix_s 判据。
**4. 交叠**：纯侦查零写集。

