# plans/025 家机节切片：719/721 侦查段（2026-09·732 棘轮滚转·纯搬运零改写）

### 1.31 【719 阶段回填】入口 shim 显式安装 UEF/VEH 完成+观测面收敛定性（接 718）（分支 `任务/家机-719-shim显式装UEF`·wt698 树·基线 develop eec44e75·2026-09-24）

**1. 行为面改动（src/runtime/io_api.cpp + src/runtime/runtime.cpp）**：①**入口 shim 显式安装**：runtime.cpp 的 entry() 开头加 cn_install_crash_handler()+cn_install_veh() 调用（extern "C" 声明）——绕开「静态初始化器在 cn_self 不执行」（718 缺口·实测 [crash] UEF installed 标记已随 shim 调用出现=安装成功）；②io_api.cpp 增 cn_install_veh（AddVectoredExceptionHandler(1, cn_veh_filter)·第一顺位）+cn_veh_filter（打印 code/RIP/RSP/fault）。
**2. 观测面收敛定性（三路全试·关键事实）**：①插桩态裸跑=C0000005 稳定复现；②UEF installed 标记在（安装执行）**但 [crash]/[veh] 均未打印**——C0000005 发生时 VEH/UEF 均未获调用=**异常未走用户态分发**（嫌疑=异常发生点在 CRT/系统层已消费，或 cn_self 进程的异常分发链被破坏）；③DEBUG 模式（minidbg_716b）=DEBUG 堆下进程挂住不崩（观测干扰）。
**3. 收敛定性**：崩溃点位于「用户态异常分发不可达」的执行域+故障形态随布局/观测漂移——**697-b「结构性缺陷」定性的强佐证**（发射质量缺陷的多面显形）。破局路径=不依赖异常观测，转**纯静态逐指令核验**（326 行追加+1026 行拷贝构造 vs 宿主版语义序列）或**插桩细化到 PROC 内部**（追加/拷贝构造 源码级 stderr 打印·克隆体继承）。
**4. 下一棒（720 家机）**：追加/拷贝构造 源码级插桩（stdlib/容器.cn 的 追加+映射集合.cn 的 拷贝构造·IO 打印需借 容器.cn 可用的打印面或整64 哨兵返回）→重建链→t4→PROC 内二分→定位首条错指令→对位发射源→修复。
**5. 交叠**：写集=src/runtime/{io_api,runtime}.cpp（退出路径修复+UEF/VEH 设施·编译器进程受益）+文档件——src 写集全量门禁（本轮收工跑）。

### 1.32 【721 阶段回填】VEH 抓到真实现场：崩溃链完整解码=设置 克隆体内空指针访问（接 720 撞号换 721）（分支 `任务/家机-721-PROC内二分定位`·wt698 树·基线 develop 2314d445·2026-09-25）

**1. 行为面改动（src/runtime/io_api.cpp·本轮唯一代码写集）**：cn_veh_filter 增强——ASLR 下 RVA 换算（GetModuleHandleExW FROM_ADDRESS 取模块基址）+**rbp 链回溯 16 层**（ReadProcessMemory 读 [rbp+8] 返回地址·ReadProcessMemory GetCurrentProcess 伪句柄）。720 撞号勘误：720 已被深度机-720-性能基线占用（先到先得）→家机当场换号 721。
**2. 决定性侦查成果（VEH 现场捕获成功）**：①**崩溃链完整解码**（[veh] RIP/RVA + rbp 链回溯→map 符号对位）：`语义分析多文件 → 表查已转移(符号表&·& 化 88 处之一) → 向量$映射$整64$符号$元素 克隆体(+0x7d) → **映射$整64$符号$设置 克隆体 内 +0x67B 处崩**（C0000005·fault=0=空指针访问）`；②**关键疑点=元素 克隆体内 call 设置**（语义上 元素 读数组元素不该调 设置——克隆体发射串线/调用错位的直接证据·697-b 缺陷精确化）；③故障形态与布局/观测的相关性最终定性：VEH（分发链头）可抓（UEF 不触发原因=异常在 VEH CONTINUE_SEARCH 放行后由系统 fastfail 终结·不再回用户态）。
**3. 对位数据（718/719 工具链产物）**：RIP=0x7FF6D53C1256 base=0x7FF6D5390000 RVA=0x31256→map 对位=设置@0x311ef；调用链 frame00=元素@0x13630+0x7d / frame01=表查已转移@0x1997b1+0x250 / frame02=语义分析多文件@0x1e1510+0x10d6。工具=target/audit2/minidbg_attach_717.py+cccmp/asmcmp+ripmap。
**4. 下一棒（722）**：dump v2 版 设置 PROC（fix_p_new.asm 57978 起 1377 行）+0x67B 附近指令与 元素 PROC（+0x7d 处 call 目标）→判定「克隆体调用错位」精确形态（发射源=IR 生成层 方法分派 or 代码生成层 call 目标构造）→定点修复→cn_self t4 跑通→t4→0/fix_s 67157→0 判据解锁。
**5. 交叠**：写集=src/runtime/io_api.cpp（VEH 增强）+文档件——src 写集全量门禁（收工跑）。

### 1.33 【722 阶段回填】崩溃指令精确定位=设置 克隆体重哈希循环·链数组=NULL 写入（接 721）（分支 `任务/家机-722-设置PROC定点修复`·wt698 树·基线 develop 87870e7c·2026-09-25）

**1. 决定性定位（dumpbin /DISASM 140 万行全反汇编）**：崩溃指令=设置 克隆体 RVA 0x31256 处 `mov qword ptr [rax],rcx`（**设置 起始 0x311ef+0x67·前段覆盖写位**）——对应源码 `值数组[已有位置] = 值`（链查找 命中已有键后的覆盖写）——**rax=值数组+已有位置×40=NULL 或越界**：this 映射处于坏状态（值数组=NULL 或 已有位置 巨大）。〔勘误：初版误写「+0x67B 重哈希循环」——精确偏移=+0x67·按 map 起址重算〕
**2. 机理闭合（语义层推理）**：存在「元素数量>0 而 链数组=NULL」的坏映射对象——正常映射不可能（有元素必有链数组）——坏对象来源=**元素 返回 T 拷贝路径的拷贝构造克隆体产物**（拷贝构造克隆体的字段拷贝错位：元素数量 拷到了·链数组 没拷/拷错）——与 716 拷贝构造尺寸验真（40/56 ✓）不矛盾（尺寸对但**字段循环体的源/目标基址或边界发射**可能错）。**表查已转移**（& 化函数）调 元素 读层映射→元素 返回拷贝→后续对坏拷贝调 设置→重哈希崩。调用链全对位：语义分析多文件→表查已转移→元素(+0x7d)→设置(+0x67B 崩)。
**3. 下一棒（723）**：对拍 拷贝构造克隆体的**三个深拷循环**（桶/键/值+链·基址装载/步进/边界发射）vs 宿主版——重点=链数组 深拷循环的基址装载与 元素数量/数组容量 边界源（cn_self 的设置 前置状态）→找字段错位点→定点修复→cn_self t4 跑通→t4→0/fix_s 67157→0 判据解锁。
**4. 交叠**：纯侦查零写集（反汇编=cnself_disasm.txt 140 万行留存）。

