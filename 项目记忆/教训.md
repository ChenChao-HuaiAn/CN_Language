# 教训（L1 语义记忆主文件）

> **读法（三层记忆模型·AGENTS.md §6.7·427-a 分拣收割后形态）**：
> ① **高权重全文区**（权重 ≥8）——任务开始必读；② **全量索引区**（时间倒序·每条 1 行）——
> 扫读触发「有这个坑」的记忆，需要现象/根因/修复细节时按条目名查 `项目记忆/归档/教训-2026-09.md`；
> ③ 归档切片=全文（纯搬运零改写·封存不改）。新增教训=追加索引行+全文写当月归档切片
> （或直接全文追加本文件末尾「新增待分拣区」·月末收尾轮统一收割）。
> **淘汰三判据**（AGENTS §6.7）：门禁覆盖／修复确认／上位收录——其一成立即在索引行换标注。
> **棘轮**：本文件 ≤800 行（scripts/文档棘轮基线.txt·超限先淘汰再追加）。

## 一、高权重全文区（权重 ≥8·24 条·任务开始必读）
## 工程纪律（第 604 轮：挂等分支「摘取式集成」=其余内容静默遗留在分支——用户令改动「从未生效」的最隐蔽形态；权重 9，2026-09-22）

**现象**：568-a 三增强（并行恒定/超时防线/dual rc·用户令「删串行路径」）在 568 分支验证全绿后挂等；586-a 清偿时只「摘取」tests 转批（83de088a）集成——runner 部分遗漏在分支，develop 的 --jobs 默认至今=1 串行（用户令从未生效 24h+，各机门禁全在串行口径跑）。**Why**：部分集成后未对「分支全写集 vs develop」做 diff 收敛核对；用户令落地判定靠「分支上有提交」而非 develop 现态机械特征。**How to apply**：①挂等分支任何部分集成后必须 `git diff <分支> develop -- <全写集>` 核对，区分「有意弃置/遗漏」；②用户令级改动落地判定=develop 现态机械特征可检（如 --jobs default 值）；③分支销账（清看板行）前先做该核对。同轮连带：gate_lock 被 TaskStop 杀门禁任务后锁不释放（finally 未及执行·4h 心跳太慢）——确认无构建活动后 rm 锁目录接续（527-a 先例二次实证）；陈锁表象=新门禁「等待中 NNNs」刷屏而产物零更新。

## 工程纪律（第 604 轮：POSIX 下线程池并发 subprocess=「多线程进程 fork」死锁面——并行调度一律进程池隔离；权重 9，2026-09-22）

**现象**：E2E runner --jobs 8 线程池版 arm64 挂死：8 线程并发 subprocess.run，fork 出的子进程 exec 前撞其他线程持有的内部锁→futex 挂死 36 分钟零 CPU（ps 签名=两个相同 cmdline 的 python3·无编译器子进程活动）。win 的 CreateProcess 无 fork=免疫→568-a win 验证全绿仍埋 POSIX 雷（平台盲区三例）。**How to apply**：①多任务并发跑外部进程的调度一律进程池（worker 单线程·fork 无锁竞争·win/POSIX 同构）+外层保险 get(timeout)；②「win 全绿」对 POSIX fork 语义零背书力——并发原语类改动三平台语义审查优先于实测。

## 工程纪律（第 596 轮：「删中转/改分支」型发射优化的半截改动=静默垃圾代码——全消费点同步改+目标平台产物级验证缺一即红灯；权重 9，2026-09-22）

**现象**：525-a（D8 arm64 装载直读）两处半截改动致 v2p 自举编译器全输入崩（572 收拢入 develop 后·arm64 门禁全红 40h 未觉）：①emitFieldAddr「基址装载到 res」改「直读物理寄存器」只改了 offset!=0 分支——零偏移时 res 从未写入=垃圾字段地址；②emitParamSetup 删「mov x10, srcReg」中转却未改拷贝循环体仍硬编码 [x10]——寄存器参数时 x10 未初始化=垃圾拷贝基址（段错误）。**Why**：①「删中转/改条件」未 grep 该中转/寄存器全部消费点逐一同步（循环体就在 20 行外）；②发射层优化验证以 win 门禁跑=不触 arm64 产物零验证力，「未分配 fallback 逐字节不变」红线≠已分配路径正确；③垃圾值缺陷二级表象=「构建不确定性」（未初始化寄存器残留随批次变→md5/行为批间分叉——fdef8ae9「456 批次抽签红」P1 定性实为此表象，596 根治后 456 转绿闭环）。**How to apply**：①「删中转/改分支」改动必须全消费点同步改（含 20 行外的循环体）；②后端发射改动验证=目标后端产物级（编译+运行），单侧平台门禁不算数；③遇「批次间 md5/行为分叉」类不定性缺陷，优先排查未初始化寄存器/内存路径。

## 工程纪律（第 586 轮：git quotepath 转义使中文路径写集被静默降级快速门禁——集成工具的门禁分类必须有反证自检；权重 9，2026-09-21）

**现象**：586-a 摘取 568 转批面（93 个 tests/e2e 通道文件·目录名全中文）走 integrate.py 集成，日志显示「快速门禁」后直接 push 成功（83de088a）——按 §8.2 写集触及 tests/ 应跑全量门禁。根因=git diff --name-only 默认 core.quotepath=true 对非 ASCII 路径输出带前导双引号的八进制转义形态，分类写集() 的 startswith("tests/") 对带引号路径恒假→**触及中文路径的写集全部被静默判为纯文档**。连带受害=冲突标记检查的 仓库根/文件 拼装（带引号路径 exists()=False→检查静默 skip）。本项目 tests/e2e 与 CN语言编译器v2 目录名以中文为主——**自设施启用以来所有中文路径写集的集成都被降级**（仅当写集恰好含 src/ 等 ASCII 路径文件时才侥幸触发全量）。

**Why**：①「写集分类」是集成的安全分级器，其输入（git 输出解析）无任何自检——git 的显示层配置（quotepath）漏进了机器判定层，显示形态与真实路径的偏差直接变成安全判定的偏差；②本项目工具链中文路径为主而分类判据用 ASCII 前缀——配置组合的暗雷；③83de088a 集成时「快速门禁（改动 93 个文件）」的日志本身就是异常信号（93 个 tests 文件不可能是快速门禁），但无人复核日志语义=缺少「分类结果×写集面」一致性抽查。

**How to apply**：①所有解析 git 输出做机器判定的脚本，git 调用一律显式 -c core.quotepath=false（或在解析端 strip 引号+转义还原）——显示配置不得影响判定逻辑；②集成/门禁工具的写集分类要有**反证自检**（用已知中文路径写集验证分类结果=须全量）；③看集成日志时对「门禁深度×写集构成」做一致性复核（tests 文件数>0 却只跑快速门禁=立即报警），该异常信号本案已出现但被忽略。


## 工程纪律（第 570-a 轮：共享文档治理门禁必须覆盖全部增长通道——看板通告段「用完即删」只有纪律无拦截器=4 天 230→832 行；权重 9，2026-09-21）

**现象**：426-a 文档专项整治后五份滚转型台账全在棘轮基线内，但 `三机任务看板.md` 不在棘轮清单——§8.6 通告段「影响面执行完毕后删除」仅有纪律条文、check_handoff 只查状态行结构不查通告段；try-build/回签/集成广播高频走通告段，4 天膨胀 3.6 倍（832 行中 768 行=通告，含单段 288 行、同机 4 段并存违反一分区一记录）。

**Why**：①与 HANDOFF 粘连事故（本区 387-a 三连条目）同一规律——**只靠纪律的增长通道终被高频协作流量灌爆**；②206 条早已预言「门禁覆盖面=出过事故的文件全集·新文件出现同类事故必须同轮扩门禁」，但看板膨胀是**渐变**（每天 +150 行）不触发单次事故信号，渐进通道更易漏防；③T81/T84 待批呈报仅存看板、未按铁律同步登记 plans/021 §三-B——若看板清理轮未甄别「活跃呈报」即删，用户裁决材料直接丢失。

**How to apply**：①新建共享文档或新增 append 型分区（通告/台账/队列）时，当轮必须同步给出行数上限或删除义务的机械门禁（棘轮/结构检查），禁止「先跑起来后补门禁」；②清理类轮次动手前必须逐条甄别完结性（已集成/已回签=删·在飞请求/红定性/待批呈报=留），判定模糊一律保留，并核对 021 台账承载；③呈报类信息落看板的同时必须同步登记 021 §三-B（铁律既有·本案漏登实证——570-a 已补登）。

## 工程纪律（第 558 轮：跨后端发射面改造的「产物逐字节不变」声明必须覆盖直读路径全形态——fallback 孤证=假绿；权重 9，2026-09-21）

**现象**：457-a（D8 ARM64 操作数装载直读）集成时声明「未分配 fallback 产物逐字节不变+E2E 464 零新增红」，实际引入两处 arm64 产物缺陷（emitPtrLoadStore 7 处丢 `[`→v2 全树 7455 个汇编错误；emitLoadStore Store 分支 wideStore 漏 f64/f32→浮点位模式 w9 截断 31 红实弹）——win/x64l 门禁不执行 arm64 产物，两处均假绿通过三平台集成链 10+ 轮。

**Why**：①「fallback 逐字节不变」只约束了 x9 退化路径；直读路径（新代码面）的字符串形态（括号拼接/w 寄存器名转换）是**全新发射面**，没有任何「不变」声明覆盖；②arm64 产物验证只有单位机能做（三机唯一 aarch64 实机），该机离线窗口期集成链照常滚动=arm64 复验真空；③win E2E 绿对 arm64 后端文件是**结构性孤证**（该文件不参与 win 产物）——「零新增红」的分母里根本没有 arm64。

**How to apply**：①改造某后端发射面时，「产物不变」声明必须穷举**全部代码路径**（直读/回退/类型分派各分支），逐分支给出等价论证或实机验证——单路径孤证不构成声明；②触及 `*_arm64*`/`*_x64l*` 等平台专属文件的轮次，收工呈报必须显式标注「他平台门禁=结构性孤证·实机复验归 XX 机」，且该机在线时补验义务不因集成完成而消失；③平台专属文件的大改尽量安排在对应实机在线窗口，或在看板通告段挂「待实机复验」标签阻断后续依赖它的集成链。

## 工程纪律（第 467-a 轮：主树「对齐远端」禁用 reset --hard——窄通道直推只需精确 add 单文件；脏文件先甄别归属再动手；权重 9，2026-09-20）

- **现象**：467 开工会话在主树 develop 直推看板前执行 `git reset --hard gitcode/develop`（本意「对齐远端」——实际本地与远端同指针、该操作纯属多余且破坏性），丢弃工作树中 `tests/cnsmith_hits/日志/2026-09-19.md` 的**他会话未提交增量**（460 采样窄通道写面）；HEAD 已提交内容未受损。
- **根因**：①对「窄通道直推」的机械流程理解错误——主树与远端一致时仅需「精确 add 单文件→commit→push」，任何 reset/checkout 都属多余动作；②同机多开（§8.8）下主树工作树是**共享面**，脏文件可能是任一会话的在制品（本次归属会话不明），`git status` 的 M 标记必须先甄别归属（会话日志 sess_*/call_*.log+文件 mtime+看板行写面对照）再做任何可能丢弃工作树内容的操作。
- **预防**：①主树禁跑 `git reset --hard`/`git checkout -- <文件>`——即使本地「看起来落后」，也只允许 `git pull --ff-only`（有本地提交时会拒绝而非覆盖）；②窄通道直推口诀=「fetch→只 add 自己的文件→commit→push」，push 被拒才 rebase；③误操作后处置=立即看板通告段广播（丢的是什么/HEAD 是否受损/归属会话按上下文重写），不得静默——本次 467 已广播（4037a6cc）。

## 工程纪律（2026-09-19 387-a 三连：提交信息与实际 diff 不符的记录缺失·旧链文件 checkout 倒退已集成成果·共享文档标题粘连再现；权重 9，2026-09-19）

**事件**（387-a D40 闭环验证轮）：①377~386 各轮（D40 选择语句线）提交信息均声称「六要素落盘
plans/025/收工回填+六件套」，但 386 收工提交（ab8ae88）实际 diff **只有 2 个源码文件**——385 看板行
声称的「plans/025 §2.38」与 386 声称的「§2.36~§2.37」在分支里均不存在（§2.36/§2.37 实际在 382 分支
的 T63 线上，双线并行各写各节未合流）；②387 基底重建 checkout 386 链文件时，125_v2/185_v2 主.cn
+cnsmith 采样日志**倒退**了 develop 已集成的 375-a 布尔语境改写与 354-c 采样窗记录（当场核对恢复）；
③382/384/386 链上 plans/025 文末「## 三、单位机…」标题三连粘连一行（共享文档编辑事故形态复发，
develop 版无此损伤）。

**根因**=①「计划落盘」只有提交信息的自我声称、没有 diff 级的机械核对——收工门禁
（check_handoff 等）不校验「声称落盘的文档节是否真的在 diff 里」；②跨链 checkout 文件把
「分支链旧态」当「工作成果」搬运，未先核对 develop 已集成面——375-a（用户裁决改写）险被静默
覆盖；③plans/025 双节编辑的锚点错选（节尾句粘连）在 377~386 线上多轮未被发现（该线记录缺失
使其连 check_handoff 的结构校验都没跑到）。

**预防**=①收工提交前 `git show --stat HEAD`（或 `git diff --stat`）机械核对：提交信息声称的
每一件文档交付物都必须在写集清单里——声称与 diff 不符=虚假验收（236-a 事故族），发现即补提交
并在下一轮如实补录（387-a §2.38 模式）；②checkout 他链文件三问：这个文件在 develop 上有没有
更新的已集成版本？checkout 会不会倒退他机/已裁决成果？——凡 tests/e2e 用例+采样日志+共享文档，
checkout 前先 `git diff develop <分支> -- <文件>` 核方向；③共享文档编辑后立即跑 check_handoff.py
（本轮 377~386 线因记录缺失连机械门禁都没触发=双重失守），标题粘连（「## A## A## A」形态）是
锚点选错的直接签名，出现即回滚重做。


（「当前无待呈报项。B11/B12 经用户…批量裁决…当轮迁出…」+ 历史 B8 说明与铁律复述）——用户当场质询
「既然没有待裁决项，就应该彻底清空！！！括号里面还留着干嘛？？？？」。

**根因**=把 B 区当成「状态说明区」而非「纯待裁决队列」：check_handoff.py 机械门禁只拦截
「已裁决/已批/已选定/✅」完成标记字样，对**说明性残留**（无等待裁语义的迁移说明/纪律复述/
「（无）」占位）不设防——B5 三犯（留档）之后又出现**第四形态**（迁出说明），且靠用户人工发现。

**预防**=①§3-B 区语义=「只准有待裁决条目」：有条目=呈报格式（`> **Bxx` 开头）；无条目=**完全空白**
（标题下不写任何字符——不写「（无）」、不留迁出说明、不复述纪律）。②迁出信息写进**迁移目标行**
（§3-A/§3-C 行内注明来源），不在原区留痕——「历史由 git 承载」的实义。③登记 C21（plans/021 §3-C）：
check_handoff.py 增强检查（B 区非空行必须以呈报条目格式开头）+AGENTS §8.3 补一句口径——随任一开发轮
实施（机械关死第四形态）。


## 工程纪律（第 290-a 轮：共享分支 rebase 未完成前禁止推任何远程 + 自动化重试梯度每一档失败都必须回滚现场；权重 8，2026-09-17）

**事件一**（看板认领 push 被拒后）：`git rebase gitcode/develop` 停在冲突中间态时，同一复合命令里的 `git push github develop` 仍继续执行并把 rebase 前的旧提交推上镜像（gitcode 正确被拒·github 却接受）——镜像短时处于与主远程分叉的改写版本，事后只能 force 对齐（`git push github +gitcode/develop:develop`）。**根因**=把「rebase→push」写进同一条 `&&` 复合命令时误用了 `;`/并行心智：rebase 失败不阻断后续 push（退出码又被管道吞掉）。**预防**=rebase 冲突解决+`git rebase --continue` 是一切 push 的前置原子步骤，冲突未解时不发任何远程写命令（含镜像）；复合命令一律 `set -e` 语义或显式判退出码。

**事件二**（290-a 双测上架轮·全量 E2E 首跑 419/423）：批量上架脚本的借链重试梯度（否→是→是+供给）里，「失败自动回滚」只写在最终失败分支——中间档（是+供给）失败打印 ROLLBACK 却**没删配置文件**，30/31/379/50 四个用例残留双编译对照.txt，串行全量以红牌暴露。**根因**=回滚动作绑在「档」的概念上而非「用例最终状态」上。**预防**=自动化重试的清理逻辑必须挂在「整体失败」终态（finally 语义），任何一档失败都先恢复现场再继续；上架类脚本上全量门前先跑 `git status --short <写集目录>` 核对残留。


## 工程纪律（第 291-a 轮：新增用例必须同轮闭环「登记面」与「编号唯一性」——只在一台机器上跑的静态门禁=其余机器的盲区；权重 8，2026-09-17）

**事件**（291-a 文档同步轮·用户令「文档严重落后」触发）：①`check_spec_coverage --strict`
在 develop HEAD 上持续**红牌**（孤儿用例 2 个：283-a 深度机 `402_验证IR旗标触达`（**292-a 已改号 405**·用户裁决方案 A）、288-a 单位机
`404_位运算立即数矩阵O3` 均未登记 `coverage_map.md`）——该门禁只挂在 `scripts/ci.ps1`（win 家机）
链内，深度机/单位机收工验证链（重建+单测+E2E+双文档门禁）**不含它**：红牌跨两台机器、跨多轮次
无人发现（家机 284-a/287-a 与单位机 288-a 收工均报「门禁全绿」；家机 284-a 改 coverage_map 时
只登记了自己新增的 402、漏了深度机已在库的 402）。②同为 **402 号**的两个用例并存（家机 284-a
`402_布尔短路矩阵O3` 撞深度机 283-a 已入库的 `402_验证IR旗标触达`·前者基线已含后者=可见后仍
占用）——撞车即孤儿红牌的来源（引用「402 用例」产生歧义）。

**教训一：静态门禁（不依赖平台的 check_*.py）必须进每台机器的收工验证链**——单测/E2E 全平台跑，
但规范覆盖/矩阵覆盖/关键字同步等静态检查此前只挂 win 的 ci.ps1；任何「只在一台机器上跑的检查」
= 其余机器的盲区，缺陷会在别人轮次以「门禁红」爆出。深度机收工自检应含：
`python3 scripts/check_spec_coverage.py --strict && python3 scripts/check_matrix_coverage.py`。

**教训二：新增 E2E 用例必须同轮完成「三件事」**——①编号全局唯一（跨机并行时以 push 后 fetch
所见为准：`ls tests/e2e/ | grep -E '^NNN'`，或查 plans/021 §5 索引）②coverage_map 登记（映射单元
或豁免区带理由）③跑一遍孤儿检测（`check_spec_coverage.py --strict`）。缺 ②③ = B3 登记面漂移，
以「门禁红」形式在他人轮次爆出（本次即由 291-a 文档轮意外触发发现）。


## 工程纪律（第八十四轮：修改「碰巧正确」的行为前必须枚举其当前承担的隐式覆盖；权重 8，2026-09-17）

**事件**（278-a T5）：v2 字符串反转义的未知转义分支原为「丢反斜杠只取后继字符」——
与函数头注「未知保留反斜杠前缀——C 语义，宿主同款」不符。T5 连带把它改为
「保留反斜杠」（对齐宿主 decodeEscapes default 分支）后，79_v2 自举闭环
固定点分叉：`.note.GNU-stack` 行变 `.note.GNU-stack,\"",...`——根因=v2 转义表
缺 `\"`/`\\`/`\'` 三已知转义，旧「丢反斜杠取后继」对这三形态**歪打正着**
产出与宿主合法转义相同的结果（`\"`→`"` 碰巧同果），缺陷被隐式覆盖掩盖；
修正未知转义行为即揭开缺口（`\"` 保留反斜杠=与宿主合法转义 `"` 分叉）。

**教训一：「碰巧正确」=被隐式覆盖的缺陷**——行为对但路径错（未知转义分支
承载着已知转义的实际处理）的代码，任何行为修正都必须先枚举「该路径当前
吞掉了哪些形态、各形态的正确归宿」——枚举齐全后同轮补齐缺失的正规分支
（本次=补 \" \\ \' 三已知转义），禁止只改被立案的那半个行为。

**教训二：注释宣称「宿主同款」必须实测对拍**——该分支注释写明宿主同款
但实现分叉两处（丢反斜杠+转义表缺三形态），256 用例以 `\u{}` 形态恰好
绕开该路径=门禁未暴露。同族=lessons 92「判据查种类非查得到」——对照性
注释是断言，不是装饰。


## 工程纪律（第八十三轮：bisect 间歇 checkout 后必须重建编译器再做一切实验；产物路径隔离键不得用会退化的编号提取；权重 8，2026-09-17）

**事件**（274-a T15 残余面轮）：①bisect 归因 D3 全挂时在 261-a/266-a/267-a 三个
锚点间反复 `git checkout`+重建，收尾 `checkout develop && stash pop` 后**忘记
重建**——target/cn 仍是 261-a 基线，接下来一轮 IR 检查与 393 失败归因全在旧
编译器上做出（393 -O3 挂实为旧编译器无修复的预期行为，虚惊一场）；②全量 E2E
10 个 _v2 用例稳定挂、单跑必过——归因两小时，根因=runner `编号=名称.split("_")[0]`
把 `194_v2_x` 与 `194_x` 都提为 194，宿主版与 v2 版用例共享 v2src/v2work/
dualhost/v2asm/v2out 全部产物路径，并行池下互踩（-5.5 符号自检/-5 未生成 exe/
-6 rc=0 空产物三形态）。

**教训一：checkout 之后不重建=一切实验作废**——`git checkout` 切分支/锚点后
target/ 产物与源码脱钩；凡基于 target/cn 的 IR/运行实验，前置断言必须是
「刚重建过且 git status 与实验预期一致」。bisect 脚本化（checkout→build→实验
→收尾 build 一条链）可根治。

**教训二：产物路径隔离键=语义身份，禁止用会丢失信息的 split 提取**——
`194_v2_x` split("_")[0]=194 与宿主版同键；隔离键退化=并行互踩（单跑全绿
的全量假绿假红最难归因）。同族=lessons 211（写集不相交非并行充分条件）——
**路径键唯一性必须按用例全名或语义完整段**；改动时锚行文本（写死旧编号路径）
须同步做运行时适配而非改测试文件（测试文件不可擅改纪律）。

**教训三：跨平台双分支逻辑改动必须双侧同查**——261-a 断言升级 win 分支补了
`if not 断言运行输出` 跳过而 linux 分支漏补，linux 侧 74 个假败潜伏两轮，
由深度机跑全量首次暴露（平台单侧缺陷=姊妹分支照妖镜的 runner 版）。


## 工程纪律（第八十二轮：门禁命令必须 && 串联——分号链会把「拦截信号」当背景噪音吞掉；rebase --continue 前必须 grep 复查冲突标记；权重 9，2026-09-17）

**事件**（266-a 收工）：rebase 冲突的清标记 python 脚本因转义序列 SyntaxError 中断
→后续 check_handoff 正确拦截（冲突标记 ✗）——但命令链用分号串联，拦截显示后
`git add && rebase --continue && push` 照跑——**带冲突标记的 plans/021 入库推送**
（gitcode/github 双落）。当轮插队修复（清标记+双门禁+新提交双推）清零。

**教训一：门禁与后续动作必须 && 串联**——分号链=「拦截显示归显示、动作照跑」；
check_handoff 的 ✗ 必须直接终止链（python 脚本异常退出同理）。与既有「见 ✗ 字样
必停」同族——本条落点=**命令链写法**层面：`check && commit && push`，禁止 `;`。

**教训二：python 内嵌脚本的 print/注释/正文中也要避免反斜杠转义序列**（非 raw
字符串 SyntaxError 直接炸掉清标记步骤——本次断点恰在关键清理动作前）。含反斜杠
文本一律 raw 三引号或外部文件——本条教训正文首版即被同一坑炸掉，raw 重写方成文
（现挂现用实证）。

**教训二扩（272-a 四犯后定则）：向 C++ 源码插入含 
 的 fprintf/诊断串必须用
Edit 工具直写**——python heredoc 的 "
" 会经 python 转义层变成真换行写进 C++
字符串（字符串字面量断行=C2001；同一次插入连犯四处）。python 层写 C++ 转义
的正确写法=chr(92)+'n' 拼接或 Edit 直写；插入后必须 sed 读回核验行完整性。

**教训三：push 后必须读回远程内容**（本次脏数据=push 成功输出掩盖了内容带标记的
事实；check_handoff 事前拦截显示被吞=唯一拦截点丢失）。收工核验=本地 grep 标记+
远程内容抽查，两者都过才算闭环。


## 工程纪律（第八十一轮：类型规则替换必须枚举全部消费面——「取左」改「提升」漏浮点前置的当轮自捕；无符号胜是移位分派的生命线；权重 8，2026-09-17）

**事件**（265-a 波A）：二元类型推导从 46-f「取左白名单」改公共类型提升时，首版只在
整型族内实现、非整型（浮点码）落入 T_整64 兜底——浮点面六用例（250/251/253/303/
08_opt）全量回归；当轮定位=补浮点优先分支全消。同轮：公共提升首版把正64 归并整64，
178 形5（全一>>60 依赖 shr 无符号移位）误走 sar=-1——补「无符号胜」规则后全对。

**教训一：替换一条类型规则前，先枚举它的全部消费分支**（浮点/无符号/指针/未知兜底
四大面）——白名单式旧规则往往隐含「未被白名单命中的码全部走兜底」的隐式行为，新规则
必须显式复刻每个隐式面。「只改主干、兜底自然继承」在类型系统里不成立。

**教训二：无符号胜（unsigned wins）是移位/除法分派的生命线。** 正64/正32 的码一旦被
「提升归并」吞掉，46-f 的 shr/lsr/udiv/setb 族分派全部静默退化（178 形5 -1 实证）。
提升规则的正确形态=宽度大者胜→**同宽内符号性保留**→混合=无符号胜（C/Rust 同构）。

**教训三：范围排除要复核依赖闭包。** 264-a 把「移位取模」从算术截断范围排除（当时
判断=独立面），波A 实施时 178 仍错——移位取模是移位语义面的组成部分而非独立可选面。
拆波施工图的每一项要标注**依赖闭包**，排班时闭包内项同波交付。


## 工程纪律（第八十轮：自举树上的「局部语义修复」=全树行为面——四绿之后的八回归与纪律回退；权重 9，2026-09-16）

**事件**（263-a D34 试做）：v2 自举树五件整型语义修复（解引用类型码/字面量位宽分流/
算术截断/移位取模/自增步进+三后端同构）逐用例实测四绿（05/178/191/389）后，全量 E2E
实证 **8 处回归**（380 校验和/194_v2/204 输出面/261 段错误/130_v2 段错误/78/79 链）——
最终纪律回退（未提交态 checkout 全量还原）复绿 407=407/0/0。

**教训一：自举编译器的语义修复=全树语义重定义。** 字面量位宽分流（T_整32）在「178 一例
正确」的同时改变 v2 全树所有字面量算术的行为（v2 自身源码就是最大的 CN 程序）——逐用例
验证永远不够，**必须全量门禁先行**（本轮四绿后才跑全量=验证序错误；正确序=小步改动→
立即全量→再决定下一步）。

**教训二：语义修复的挂点选择=「类型系统层面」而非「代码生成层面」。** 字面量缺省类型是
**语义层表达式类型推导**的属性（宿主在语义/IR 类型系统实现）；在 IR 生成面按节点类型
分流=挂点太晚，传播面失控。根治=波A 语义层实现（拆波施工图）。

**教训三：跨文件静态表必须走访问器。** 直接摸他文件 静态 映射（指针变量类型们）出现
「指纹指令间歇消失」的未定义行为面（合并编译符号解析时序）；访问器函数（登记/查/清空
三件·寄存器类型们 先例）是唯一正解。新代码评审点：grep 静态表直接引用。

**教训四：试做-回归-回退是合法轮次形态。** 八回归实证本身=高价值产出（D34 耦合面的
完整清单=拆波施工图的依据）——「不带病收工」的正确姿势不是硬修到底，是**回退+清单化+
拆波重排**（保留认知资产、退回行为面零污染）。


## 工程纪律（第七十九轮：双编译对照「只锚退出码」=打印型分叉的系统性盲区；批量升级必须「逐个实测·双侧绿才上架」；AI 会话中文字形显示疑云必须字节级机械核验；权重 8，2026-09-16）

**缺陷**（261-a C20 波1 定性面）：存量双编译对照编排两侧只断言**退出码**（.expected 充当 v2p
编译日志锚行）——打印型用例（.expected=运行 stdout）若直接挂双编译会**假绿**（v2 打印内容
分叉但退出码同为 0）。同时 v2 侧入口写死 `主.cn`，非主.cn 源名用例结构性无法双测。

**教训一：断言面必须与用例形态匹配**——正测双测=两侧运行 stdout 逐行比对（新键 断言输出=是），
退出码锚定只适合「断言折算进退出码」的存量形态；负测双测=中止诊断锚行。三类形态一器一断，
禁止混用（261-a runner 已分型落地）。

**教训二：批量升级纪律=「逐个实测·双侧绿才上架·失败自动回滚+定性立案」**——A 类 72 个批量
升级 63 成功/9 失败，9 个失败全部自动回滚且每一个都定性立案（D34~D37）；严禁「生成配置就算
升级完成」的脚本式假验收。升级过程顺带产出 17/17 Cx 实锤与 v2p 旗标空转两个新案=双测网络的
附带价值面。

**教训三：AI 会话里对中文字形/数字的「视觉核对」不可信**——本轮多次出现「读回内容与我写的
new_string 视觉不一致」的假象（同码点字形疑云），而**字节级比对全部一致**；但同法也真抓到过
「plans 名单誊写错位」（手写 A 类名单与磁盘 20 处不匹配）。处置铁律：①共享文档/代码编辑后
必须**读回+机械核验**（python 字节比对/AST/门禁脚本），禁止靠眼睛；②从权威源（plans 名录）
机械提取名单后必须与磁盘实际**对账**（本轮 72/72 全匹配才敢开工）；③数字类事实（用例数/
门禁数）一律从实测输出誊写并交叉核对，禁凭记忆。



## 工程纪律（第七十八轮：优化 Pass 的「保守兜底」必须验证前提——占位兜底=把语义缺口静默植入产物；定位链先做 IR 层对拍再进后端面；权重 9，2026-09-16）

**缺陷**（D32·s266·CN-Smith 差分首采捕获·260-a 根治）：SSA Pass（F1-26 方案 A）对汇合块 Load 生成
Phi 时，对「前驱块内无该槽 Store」的前驱走「占位：沿用槽值（id=-1）」兜底——lowerPhis 据此在
**前驱块尾注入 `加载 槽`** 兜底。但该兜底仅在「槽值沿路径必达定义」时正确；对「Store 位于汇合块
之后顺序段」的槽（典型=分支汇合之后才定义的变量），前驱块的 Load 读到**尚未 Store 的栈槽**（UB）
——s266 -O3 变8 被写成变10 的值（读栈残留）、-O0 正确；asm 面表现为「RA 读错寄存器」（r12 被
变10 复用），**IR 层已错**（%v128 同号冲突+bb13 病态轰炸 60+ 重复加载搬运）——停在 asm 面归因
会误修 RA。

**教训一：定位链必须先做「IR 层对拍」再进后端面。** -O0/-O3 分歧 + `--no-regalloc` 正确只能指向
「优化链或其下游」；下一步必须是**双级别 IR dump 对拍**（本案 IR 已错→修优化面；若 IR 一致→才轮到
RA/发射面）。跳过 IR 对拍直接读 asm（本案 asm 症状极像 RA 槽混淆）会浪费一整轮在错误的层。

**教训二：优化 Pass 的「保守兜底/占位」分支=高危面——写它时必须证明兜底前提。**「占位：沿用槽值」
把「该槽此刻可能未初始化」**静默合法化**（编译期零诊断）=AGENTS §3「折中即妥协」清单里的
「宁漏勿错/保守分支」形态。修复方向=**机制前提收紧**（Phi 化前提=全前驱块内有该槽 Store，任一
miss 即跳过该 Load 的 Phi 化、保留原栈语义），不是对已知错形态打补丁。

**教训三：锁死为「正确性边界内的保守」不算妥协，兜底读 UB 才是。** 收紧后少 Phi 化一部分形态
（优化机会损失）=可接受（栈语义保持正确）；「读未定义槽」=UB 不可接受。判据：**兜底分支必须能
说出「它依赖什么不变式成立」**——说不出=不该有兜底。

**教训四：CN-Smith 差分是「单实例+特定 CFG 形态才触发」缺陷的唯一网。** E2E 407 用例全绿+
单测 1351 全绿均未命中（-O3+汇合点+后置定义组合），差分 500 采样抓到——M3 支柱价值再实证；
差分轨并行化后（同轮·485s/500≈8×）纳入日常回归成本极低。


## 工程纪律（第七十七轮：在飞改动跑全量验证链前必做 stash 基线对照；验证链未完成严禁写收工记录；E2E 面构成必须呈现；权重 10，2026-09-16）

**缺陷**（235-a/236-a→259-a 归因处置）：深度机工作区在飞改动（ir_fields.cpp 双形态判定 13 行·从未
push）引入运行时内存失控回归——76 号产物 30MB→4.3GB 失控段错误；全量 E2E 下 29GB 三次触发
OOM killer 连坐 ZCode agent（用户视角=ZCode 反复退出，跨 9/15~9/16 三天未定位）；且同轮留下
「全量 E2E 311/0/2 绿」虚假收工记录。2026-09-16 用户裁决方案 B 归零（用户令：家机在做任务，
本机全部归 0 不做开发任务）。

**教训一：回归归因第一手段=stash 基线对照实验。**「一直没问题突然出问题」时，第一嫌疑=工作区
dirty 改动（未提交未 push·只有本机有·远程与他机全干净），第二嫌疑=最近合入提交。对照口径=同机
同用例同命令、唯一变量为该改动（`git stash push -- <文件>` → 重建 → 复跑 → `stash pop`＋md5 复核）；
本例一次定位（4285MB vs 30MB）。**重验证（全量 E2E/锚定链）一律独立 scope 跑**
（`systemd-run --user --scope -p MemoryMax=8G …`）——防失控进程触发全局 OOM 连坐宿主会话。

**教训二：验证链未完成严禁写收工记录。**236-a 在 OOM 三连、全量 E2E 根本没跑完的情况下写下
「全量 E2E 313=311/0/2 绿」——「未完成不得声称完成」的虚假验收，与 journal OOM 铁证并存跨两天
无人察觉。门禁数字必须来自**当轮构建的完整实测**，任何「绿」都要能给出可复现命令与日志去向；
收工汇报前自问「这条全绿的复现命令是什么」。

**教训三：E2E 面构成必须呈现。**E2E 由四类构成（宿主-only／v2 闭环 79／双编译对照 35／v1 挂账
70~77＋89 共 9＋单测 test_bootstrap_lexer.cpp 1）——v2 重建启动（8-25）以来收工汇报从未呈现此
构成，v1 已废仍在测、覆盖面不对称长期无人报告（用户质询三感叹号）。**收工汇报固定呈现四类构成
与数量**；v1 挂账退役=plans/021 §三-C C20。

**教训四（技术沉淀·C19 重做时用）：形态二分世界必须有单点判定。**`结果<字符串,整32>`（声明源
形态）与 `结果$字符串$整32`（canonical 合成名）是同一类型的两种文本形态——手写 rfind("结果$")
只认后者、isResultType 只认前者，各认一半互相不知（findStruct 却对两形态归一）。凡类型存在
源码形态/canonical 形态二分，判定必须收敛到单一谓词函数，任何手写前缀匹配都是孤儿路径候选。


## 工程纪律（第七十六轮：泛型单态化=共享 AST 写回注记跨实例污染——「单实例巧合正确」是最危险的验证假象；宽补丁爆炸半径必须收窄实证；权重 8，2026-09-16）

**缺陷**（A7 本体·234-a 根治）：实例化类方法体 AST 为全实例共享（mi.ast=模板成员），语义检查的
写回型注记（CallExpr::resolvedType/resolvedSignature、retOwnedString、node->size 等）写在共享节点
上——逐实例检查互相覆盖，IR 层生成时全部读到「最后检查实例」的残留值。v2 树 9 处 `复制(下标链)`
resolvedType 全=IR指令（56B）→ 误走结构体深拷分派④（CopyStruct 56B+写回槽地址）。

**教训一：单实例探针=巧合正确陷阱。** 225-a「同模块用户泛型类同形态完全正确（直通 i32）」的
对照结论把根因误导向「导入模块 vs 同模块」——真变量是**实例数量**：单实例程序最后检查者=唯一
实例=注记恰好正确。**多实例 must be 探针形态的组成部分**：凡涉泛型/单态化/注册表迭代序的缺陷
验证，探针必须≥2 个不同实例实例（最好元素类型异构：标量+结构体+类），并在多实例下断言逐实例
行为，否则任何「通过」都不可信。

**教训二：共享可变注记×多消费者=家族性缺陷面，逐消费点补丁是半截机制。** 本家族已有三爆点
（H8 node->size、returnTypeSrc、本案 resolvedType），此前均为单点补丁。根治选型时优先
「覆盖所有现有与未来消费点的机制」：重放检查（生成前刷新注记，一次覆盖全部写回点）优于
深拷 AST（98 节点 clone 族=新遗漏面）；判据=修复面是否随代码增长自动保持完整。

**教训三：宽补丁的爆炸半径必须用全量编译验证收窄。** emitStructWholeAssign 源分派放开「所有
CallExpr」在单探针上全绿，却编坏 v2p（v2 树既有「下标目标=调用」形态依赖 StorePtr 现状发射）
——**宿主编译器的补丁要以「编译整个 v2 树+全量 E2E」为回归面**，单变量二分（stash 分组）定位
后收窄到精确形态（复制调用·无控制流）。新分支的触发条件必须论证「发射时序/控制流」安全性，
不能只论证「结果类型正确」。

**教训四：stdlib 实现是编译器自身的运行时。** v2p/自举链/编译器内部容器全部链接 stdlib 实现——
stdlib 任何行为变更（哪怕「只影响用户程序」的拷贝构造）都会改变编译器自身运行行为，验证面=
编译器编译任意程序（含不含 stdlib）×运行时自身内存（valgrind）。


## 编译器缺陷（第七十四轮：容器「无条件释放元素」×「入容器不接管所有权」=一族静默缺陷；链式容器不能平铺释放；v2 后缀序节点定位；权重 8，2026-09-11）

- [2026-09-11 第七十四轮] **问题类型**: 编译器缺陷（容器元素所有权机制级矛盾，五枚形态）+ v2 侧防线不对称，**✅ 当轮根治（74-a 方案A：用户未答复呈报→按 AGENTS.md 第3项纪律自行裁决）**
  - **描述**: 按 plans/020 矩阵靶子 #1「入容器编译器级自动 copy/move」立案，探针（全部可复现）实测五枚形态：
    ①**借用句柄入容器 → 容器析构释放调用方拥有的串（跨函数 UAF）**——探针 74-L：`存入(表, 本地)`（形参直传入容器）后，容器块退出时打印「容器销毁后 本地」输出乱码；
    ②**局部拥有串裸标识符入容器 → 源 RAII 释放 + 元素浅共享=悬垂**——探针 74-A：`表.追加(值)` 后 3 元素内容全乱码（搅动后仍损坏）；
    ③**`转移(源)` 入容器非真 move**——源槽未清零，源出口 `__cn_str_free` 释放已移交句柄，探针 74-J 元素内容乱码；
    ④**v2 侧容器元素从不释放**（防线不对称）——v2 无任何元素释放机制（宿主有 `__cn_vector_free_strings`），探针 74-G 残留 2（两个 `字符串复制` 元素）；
    ⑤**链表/栈/队列元素串从不释放**——宿主只覆盖 `向量`，探针 74-N：链/栈/队各残留 1。
  - **根因**: **「容器析构无条件释放元素串」（假定「元素所有权归容器」）与「入容器位不接管所有权」（实参句柄被浅存）是一对必须同时成立或同时不成立的假设**——两个半截机制各自「看起来正常」：容器析构的逐元素 free 独立看是 RAII 正确实现，入容器位的浅存独立看是「CN 值语义深拷贝」的自然形态；合起来则产生悬垂/双释放/UAF。修任一侧的补丁都不能使假设一致（宿主 72-a 的 A2 人工纪律「字符串入容器须显式复制」是**约定不是门禁**，故静默失效——全树 4 处裸标识符入容器全部是字符串形参，即最危险的借用来源）。v2 侧同族缺口的根源是 72-a/73-a 只做了释放侧对齐，未做「写入侧约定」对齐（释放侧的假设没有对应的写入侧建立点）。附带两枚 v2 特有实施不变量：**调用发射时序**（v2 调用展开是「收集 IR_调用实参，遇 IR_调用参数化 统一展开」——插入式调用须在收集之前发射，否则其 IR_调用参数化 把已收集实参一并展开）、**节点池后缀序**（实参根节点在调用节点前一位置，不可用 子起点+1+序——复合实参时指向最内层子节点，判定失真，探针 74-H 残留 3 实证）。
  - **解决**: 按 Rust `Vec<String>` 语义（`push(s)`=move／`push(s.clone())`=编译器代写深拷贝／字面量=驻留借用）做**入容器位所有权归一化**——宿主 `buildCallArgsForMethod`+`normalizeContainerInsertArg` 按实参来源分级（字面量→驻留借用零复制；调用返回（拥有契约）→直接接管零拷贝；`转移(拥有局部)`→真 move（句柄直存+源槽清零）零拷贝；其余（标识符/成员/下标/借出）→`__cn_str_copy` 落堆）；v2 侧 `归一化入容器实参` 同构（`转移` 分支因拿不到 函数信息 统一走复制=诚实边界）；**释放面按元素模型分派**——向量/栈=`__cn_vector_free_strings`（平铺 0..计数），链表/队列=新增 `__cn_chain_free_strings`（**按链游**：出队/删除头部已把元素所有权转移给调用方，槽序号可能 < 元素数量，平铺释放会误释放已移交的串）；两辅助均含**元素槽清零**幂等不变量；单点事实源 `emitContainerElemFreeFor`（宿主）/`发射容器元素释放`（v2）统三处释放路径（块出口析构/容器析构/容器字段级联）。
  - **成效与防线**: E2E **218** 校验和 **24→31**（五形态全对；含四容器释放面残留归零）；真 move 零拷贝实证（预分配后分配增量 **0**，深拷贝增量 1，源仍可用）；**E2E 219 双编译对照两侧退出码 63 一致**（移植纪律 9 第二个对照用例：六形态含借用来源/借出视图/元素守恒/四容器释放面）；生成物层两侧均有平铺+链式元素释放调用（宿主 20+7 / v2 13+2）。验证链：清场重建零警告 + 单测 1289/1289 + E2E **241 用例 236 过**（唯一败=194 既有待批）/4 跳 + 固定点 **271056 行**（新口径）逐字节一致 + 绑定自检（cn_main=0xf6015 ∈ .text[0,0xf9fa8)，readelf 实证）+ 运行级 3/3 + 对拍 29/29 + 释放路径两侧对照脚本三列核对。
  - **预防**: **⑱「A 处假定 X、B 处负责建立 X」的分工必须先写清「X 由谁建立/在哪一行建立/建立失败会怎样」三问**——本轮两个半截机制（释放侧假定元素归容器、写入侧未接管所有权）各自独立看都正确，合起来是一族静默缺陷（悬垂/双释放/UAF，内容测试全部抓不到）；**释放侧的假设必须与写入侧的约定成对出现**（plans/020 移植纪律 7「这个机制假设的另一半在哪里？」的又一实例）；**⑲容器元素释放必须按元素模型分派，不能按计数平铺**——凡「槽序号空间 ≠ 有效元素集合」的容器（链式：出队/删除头部已转移所有权给调用方）一律按链游/有效枚举释放；**新增容器类型时先问「有效元素怎么枚举」**，释放辅助的参数表就是元素模型的声明；**⑳v2 侧实施前先确认两个不变量**——调用发射时序（插入式调用须在 IR_调用实参 收集之前）+ 节点池后缀序（实参**根**节点在调用节点前一位置，不是 子起点+1+序）；判定失真的表现常是「多一次操作」而非崩溃，只能靠内存差值探针发现；**㉑人工纪律不是门禁**——A2「字符串入容器须显式复制」是文档约定，编译器不强制 → 静默失效；凡「约定式安全」都要问「编译器能不能强制」，能则落为编译器机制（本轮把约定升级为编译器接管所有权）。
  - **诚实边界**: v2 侧 `转移(拥有局部)` 入容器**未走真 move**（判定源是否拥有需 函数信息.字符串变量们/污染们，穿透需改表达式生成族 12 函数+22 调用点签名，风险大于收益）——统一复制，语义安全等价（容器独立拥有、源照常释放，无悬垂无泄漏），仅多一次分配，真 move 优化登记后续项；嵌套容器元素（`向量<映射<整64,字符串>>`）不释放元素串（元素是容器对象，按 char* 语义释放对象指针会损坏堆）；数组元素/装箱字段借用面维持既有边界；探针 74 系列留 /tmp/p74probe 不入库。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）


## 编译器缺陷（第七十二轮：符号名模式判型=跨模块整体失效；SysV 变参 AL 契约；转移/引用出参 UAF 三形态；权重 8，2026-09-11）

- [2026-09-11 第七十二轮] **问题类型**: 编译器缺陷（拥有型字符串 RAII 五枚静默缺陷）+ 规划处置（F3 回退呈报），**✅ 五枚当轮根治（F1/F2/F4/F5/F6），F3 回退待裁**
  - **描述**: 按 HANDOFF「4'-三 收尾评估」逐项实测（写探针跑真实链路，不读代码推断）发现：
    ①**F1 跨模块拥有契约失效（静默永久泄漏）**——`semantic_call.cpp` 用 `sigKey.find('$')` 区分「泛型单态化产物」，但跨模块函数链接键也是 `模块$名` 形态（functionLinkKey 公式）→ 凡导入模块的函数返回 字符串，调用方一律不登记 RAII（IR 对照：同文件 `调用 [本地造串]` 有 free，跨模块 `调用 [工具$造串]` 无 free；运行级：stdlib `替换` 跨模块调用活动分配残留 1）。影响面=81 个含导入 E2E 用例 + stdlib 全部 `-> 字符串` 函数。
    ②**F4 赋值位转移非真 move（UAF）**——`目标 = 转移(源)` 原实现当普通右值（实参值加载）→ 目标/源共享同一堆句柄，源仍在拥有名单中于出口释放 → 目标悬垂（stdlib `字符串扩展.cn:72` `缓冲 = 转移(拼接)` 实测输出乱码，被 E2E 用例目录内的旧副本掩盖）。
    ③**F5 引用参数出参悬垂（UAF）**——`字符串& 输出` 写入本地拥有串后本函数出口释放源局部（探针 `输出 = 甲;` 修复前读「73」；v2 树 解析货舱/拆限定名 两函数踩中，被 F1 修复暴露成 E2E 165 回归）。
    ④**F6 变参调用未设 AL（SysV ABI 违约）**——AMD64 SysV 要求变参调用点 AL=使用的向量寄存器个数（glibc `va_arg` 据此判定 xmm 保存区），CN x64 代码生成从未发射 → 基线靠 rax 残留侥幸（`格式化("¥%.2f", 29.50)` 经包装函数输出 ¥0.00、直连形态正确）。
    ⑤**F2 v2 落空路径零释放（永久泄漏）**——v2 只在 返回 语句路径发射释放，体末无 返回 语句（fall-through）的函数完全不释放（探针 asm 无 `__cn_str_free`）；Rust 语义=函数体末尾隐式 drop。
  - **根因**: ①**符号名模式判型**（`$` 既是泛型实例分隔符又是跨模块链接键分隔符——同一字符两义，模式匹配必然误伤）；②**转移语义只实现在声明位**（阶段3b 浅交接是 `生成变量声明` 分支，赋值位/表达式位留「值加载」形态=共享句柄；Rust 对照：`a = b` 对 String 本身就是 move）；③**引用参数写回未定所有权**（写入调用方槽后源局部是否仍释放=未定义；Rust `*out = s` 是 move）；④**C ABI 变参契约缺失**（clang/gcc 变参调用点恒有 `xor eax,eax`/`mov al,imm`）；⑤**v2 RAII 出口覆盖面遗漏**（返回路径三出口都补了，fall-through 尾块漏）。
  - **解决**: F1 改按 `genericFuncInstances_` 精确判定（rustc DefId 同款）+ v2 侧三处纯名查改 `查函数返回回退` 键基回退版；F4 赋值位真 move 分支（drop 旧目标+句柄直存+源槽清零；自转移豁免）；F5 引用出参 move（句柄直写调用方槽+源槽清零，污染名不转移）；F6 调用点统计向量实参个数发 `mov eax, N`；F2 `生成函数` 体块未终止时在函数尾块前发射出口释放（**首版无条件发射 → 196_v2/210_v2/203_v2/208_v2/123_v2 等十用例回归=已返回路径二次释放**，条件化后零回归）；E2E 212 两新用例固化；门禁全绿（清场重建零警告+单测 1289+E2E 234 用例 229 过唯一败=194 既有+固定点 266964 逐字节一致+对拍 29/29）。
  - **预防**: **①同一符号/字符承载两义（`$`=泛型实例分隔符 vs 跨模块链接键分隔符）时判型必须查权威表（实例表/DefId），禁止符号名模式匹配**——模式匹配在「新增第三种用法」时静默失效，且失效方向是「不登记 RAII」这类静默泄漏；**②「显式 move/赋值语义」必须在全部位置（声明位/赋值位/返回位/传参位/引用出参位）逐一确认拥有归属**——写全一处就以为写全了是漏网主因；**③C ABI 契约（变参 AL/调用约定/栈对齐）属「调用点发射」必查清单**——新增运行时函数（尤其 C 变参）逐项核对 ABI 手册，不靠实测侥幸（「能跑」=rax 残留值恰好正确属未定义行为）；**④RAII 出口覆盖面按「函数所有控制流出口」枚举（返回/落空 fall-through/中断/继续）**，每出口逐一确认；**⑤修复改变既有测试断言的语义（时机/计数）时，按「测试文件不可擅自修改」纪律回退并呈报**——不得为实现新语义自行改测试期望（F3 处置范本）；**⑥宿主与 v2 双侧同族的缺口要一次修全**（F1/F2 都是双侧同缺，只修一侧=下一次回归）。
  - **F3 落地（同轮下半程，用户裁决方案A）**: 宿主块级作用域 RAII 实施（作用域基线栈+genBlock 出口析构+中断/继续 drop-on-jump，**free+槽清零幂等模型**杜绝多路径双重释放）+**连带根治 v1 树「字符串入容器浅共享=元素悬垂」既有缺陷**（6 处追加点按 A2 纪律显式复制；76 自举用例产物常量池退化为指针值实证；F3 关闭时探针同样悬垂=既有缺陷甄别）+用例期望同步（115/116，用户批准）。
  - **F3 追加预防**: **⑦「新语义使旧潜伏缺陷暴露」时先甄别既有 vs 新引入**——判据=关闭新特性复测（F3 关时探针 86 元素同样损坏=既有缺陷，只是内存未复用侥幸；若关后正常则是新引入的回归）；**⑧块级/函数级多路径释放共享槽时用「释放+清零」幂等模型**，而非「谁释放谁登记」的排除法（后者在条件分支/跳出/兜底三路径交织时必然出现两次释放）；**⑨测试期望的更新必须用户批准且逐条核对语义**（115 的 6=向量操作 3+块出口 3 逐项可解释，不是"跑出什么写什么"）。
  - **72-a 收尾（2026-09-11 下半程收尾轮，用户裁决「瑕疵清理+三层防线全部实施」）**: 核实上轮工作后静态审查揪出 4+1 瑕疵（`(void)0` 死代码/`LoopContext::scopeDepth` 命名失准实际为类名单基线/genVarDecl 注释与代码不符/泛型出口状态清理不对称/switch 中断无 drop-on-jump）当轮清理；清理中静态推演发现**类方法返回路径真缺陷**——`emitClassMethod` 为三类函数级生成单元（visitFunctionDecl/emitGenericFuncInstance/emitClassMethod）中独漏函数级兜底者，方法体内声明拥有串后中途 返回 绕过 genBlock 出口析构=泄漏（探针 87 修复前实证残留 1，补 genStringFrees/genClassDestructorCalls 同构收尾后归零）；探针转正 E2E 213（块级 RAII 八形态残留全零）/214（容器元素所有权搅动）；plans/020 拥有权流转矩阵建档。
  - **收尾追加预防**: **⑩探针实测与静态审查两条腿缺一不可**——探针测得出泄漏/悬垂/残留（活动分配数差值量化），测不出死代码/命名失准/注释与实现不符/状态清理不对称（不影响运行行为但误导下一个读代码的 AI，本轮类方法真缺陷恰由静态审查的推演引出、再由探针 87 实证闭环）；探针全绿≠diff 干净，每轮修改后的 diff 审读不可省；**⑪「对称补齐」前先核验生成单元的嵌套关系**——visitFunctionDecl 出口 clear 的状态，泛型实例（visitProgram 第 4 步批量生成、不嵌套）可照抄补齐，但若单元是嵌套生成（如泛型若嵌套于宿主函数体内）照抄 clear 反而清掉宿主活状态（stringTainted_ 跨函数共享即此类）；对称性判断以调用时机实证为准，不以「长得像」为准；**⑫文档声明与实现不符时以探针实测为准并回改文档**（块级析构实现实际覆盖无析构类的内存释放，比 HANDOFF 诚实边界的「限有析构函数类」声明更完整——声明落后于实现同样是文档缺陷）；**⑬写 CN 代码严防 C++ 习惯带入**——类/结构体/枚举体后 `}` 自终结**不加 分号**（规范 03 规则1），本轮连踩两次（探针 87 与 E2E 213 均因 `};` 报「预期顶层声明」）；写 CN 类型定义后自查该点。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）


## 编译器缺陷（第七十三轮：v2 块级 RAII 从未生效=「一次性预扫 + 生成期基线」互斥；多路径共享槽不清零=双释 SIGSEGV；权重 8，2026-09-11）

- [2026-09-11 第七十三轮] **问题类型**: 编译器缺陷（v2 侧两枚 P0：块级作用域 RAII 静默失效 + 跳出路径无 drop-on-jump），**✅ 当轮根治（73-a 方案A 用户裁决）**
  - **描述**: 按 plans/020 矩阵第 27 格靶子（「v2 侧跳出路径 drop-on-jump 专项核验」）实测，核验中揪出更深两层：
    ①**缺陷甲（P0）v2 块级 RAII 从未生效**——`预扫容器声明` 在函数体生成**之前**做函数级一次性扫描，把函数内所有（含全部嵌套块）拥有型容器/字符串登记进释放名单；`生成块` 入口取基线 `= 名单.大小()`，出口只释放「基线之后新增项」→ 进块时名单**早已包含**本块变量，基线恒等于边界，**永远无新增项可释**（诊断注入实证：`[诊断] 登记拥有串: 体内` 出现在预扫阶段、随后 `块出口析构: 字符串基线=1 现名单=1` 零释放）。探针量化：20 万轮循环体拥有串残留 **199,999**（继续/自然回边/如果体 三形态同，即**不限跳出路径**）；30000 轮 ×14.4KB 泄漏上限 411MB（内存上限下 OOM）。
    ②**缺陷乙（P0）中断/继续 无 drop-on-jump**——`生成语句` 两分支只发跳转不释放（宿主 72-a 已有 `genJumpDestructFrom`）；asm 实证：跳出路径无 `__cn_str_free`，自然路径每轮都有。
    ③**修复首版自身缺陷（当轮实证闭环）**：登记时机+跳出释放落地后，容器跳出形态 **SIGSEGV**——跳出释放与函数级兜底各释放一次（gdb 栈 `free <- cn_free <- ~向量 <- bb0_4`），根因=「编译期名单不截断 + 释放点不清零」。
  - **根因**: ①**「一次性预扫 + 生成期基线」两种设计共存时预扫污染了基线依据**——预扫登记先于生成，使「基线 = 当时名单大小」的语义失效（基线机制的前提是「名单随生成进度增长」）；这类失效**静默**（编译通过、运行不崩、只是不释放）；②**RAII 出口覆盖面按路径枚举时漏了跳出路径**（宿主 72-a 补齐，v2 侧未同步——双侧同族缺口只修一侧是复发主因，lessons 第七十二轮预防⑥同族）；③**多路径共享槽不清零**——不截断名单意味着同一变量会被「跳出释放」与「函数级兜底」**两条路径**经过，若释放后不清零，第二次就是 double free（宿主 72-a 用「释放+清零」幂等模型已给出范式，v2 未同步）。
  - **解决**: ①**登记时机对齐宿主**——预扫职责收敛为「槽分配 + 入口零初始化 + 类型登记」，名单追加移至生成期（`生成变量声明` 字符串拥有化初始化后 / `生成容器局部` 非返回接收分支）+ 新增 `名单追加去重`（v2 局部槽=函数级映射，同名合槽须去重）；②**跳出 drop-on-jump**——`函数IR` 新增三枚循环体基线（`生成当` 进入体块前记录/退出后恢复，嵌套循环自然正确）+ 新增 `循环跳出析构`（按最近循环体基线释放，编译期名单**不截断**——同块内互斥路径（如「如果{中断}」与自然结束）各自仍须发射释放，截断会让另一路径无变量可释）；③**「释放+清零」幂等模型**——`函数IR.零寄存器`（预扫回填，函数入口常量 0 的寄存器号）+ 三处释放点（块出口析构容器/字符串段、结构体容器字段级联）释放后槽清零（容器字段须先另存字段地址寄存器再清零——加载会把值寄存器指向对象）；④`块出口析构` 新增 `截断` 参数（块出口=真/跳出=假，不截断模式遍历用下标递减防死循环）；⑤连带 v2 内置表补 `内存::活动分配数`（E2E 断言口径）。
  - **成效与防线**: 五形态（继续/中断/自然回边/如果体/容器跳出）20 万轮级残留 **199,999 → 0**（归零闭环，LD_PRELOAD 读 `__cn_alloc_live`）；v2p 编译 v2 树退出残留 2,755,596 → 2,757,891（同量级=编译器自身常驻结构）；**E2E 216_v2_块级作用域RAII内存回归**（七形态差值恒 0 + 内容校验，退出码 127）转正为门禁（宿主 213 的 v2 对应件，两侧防线自此对称）；悬垂风险扫描 = 0 命中（全树「块内声明→容器追加/静态存储/返回」零处，v2 树已按 A2 纪律迁移）。验证链：清场重建零警告 + 单测 1289/1289 + E2E **238 用例 233 过**（唯一败=194 既有待批）/4 跳 + 固定点 **266845**（新口径）逐字节一致 + 对拍 29/29。
  - **预防**: **⑭「一次性预扫 + 生成期基线」共存时，预扫不得触碰基线所依赖的列表**——基线机制的前提是「列表随生成进度增长」，预扫提前登记会让基线恒等而静默失效；举凡「预扫做 A、生成期读 A 的大小作界」的设计，先问「A 的增长时机与读者时机谁先」；**⑮修释放路径先问「同一槽/同一名单会被几条路径经过」**——多条路径共享时必须选「截断名单」或「释放后清零」之一（宿主 72-a / v2 73-a 现同为「释放+清零」幂等模型），否则双释 SIGSEGV（本轮首版实证）；**⑯旧修复未同步到对侧即复发**——宿主 72-a 的 drop-on-jump 与幂等模型，v2 侧隔一轮才补（双侧同缺的教训第七十二轮预防⑥已载，仍需按「修复台账逐侧核对」执行，勿因单侧绿就收工）；**⑰修复后暴露的失败必须做「修复前版本同形态实测」三方对照**（本轮结构体容器字段「声明即用」形态：修复前 v2p / 修复后 v2p / 宿主**均失败** → 判定既有边界，非本轮引入；不此对照就会把既有边界误记为本轮回归或反之）。
  - **诚实边界**: 结构体容器字段「声明即用」（`货架 s; s.表.追加(...)`）报「空指针解引用」=既有边界（三方对照实证，字段未自动构造），靶子归属「结构体字段初始化 / NLL 轮」；装箱字段/数组元素/静态面无 RAII（plans/020 矩阵靶子维持）；v2 侧「返回接收形态」不登记释放（原预扫豁免语义原样搬移，行为不变）。
  - **移植纪律订立（plans/020 第三节三-B，用户要求入册）**: 本轮把可复用防护提炼为 4 条强制自检（改动所有权/释放机制时逐条执行）——**⑦移植清单两件套**（写「机制主体 + 依赖不变量」两行，不变量逐条验证，禁止凭「看起来像冗余」跳过→73-a 首版 SIGSEGV 根因）；**⑧释放路径两侧对照**（`scripts/cn_release_parity.sh` 拉两侧释放挂点清单，人工核对「时机/条件/不变量」三列，脚本含清零幂等两测计数报警）；**⑨同结构双编译器对照（已门禁化：E2E 217 + runner「双编译对照」编排）**——同一源码两侧各编各跑、断言退出码一致（宿主侧锚定用例期望值防两侧同错，v2 侧以宿主实测值为期望=等价性判据）；用例目录放 `双编译对照.txt` 即生效（零 runner 注册）；反证实证=注入「缺 drop-on-jump」「释放点缺清零」两形态退出码 31→0 均被捕获；手工手法（`grep` 释放调用计数 + `LD_PRELOAD` 读 `__cn_alloc_live`）保留为补充场景；防线不对称=缺陷候选；文档声明须带验证状态；**⑩两侧同缺一次修全**（预防⑥已载仍复发，故落为当轮动作）。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）


## 既有经验（继承自 HANDOFF，权重 10）

- **GCC 7 不支持中文标识符**：所有标识符（含 gtest 测试名）必须英文，中文仅用于注释/字符串
- **无外网**：googletest 用本地 `~/third-party/unittest/googletest`（LLVM定制版，需 `-DGTEST_NO_LLVM_SUPPORT`）
- **本机构建命令**：`export PATH=$HOME/gcc7/usr/bin:$PATH && cmake -B target/Debug -S . -DCMAKE_CXX_COMPILER=$HOME/gcc7/usr/bin/g++`
- **编译产物在 target/**：测试二进制在 `target/cn_unit_tests`
- **pkill 自匹配**：`pkill -f` 会匹配自身 shell，先 `pgrep -af` 再精确 kill
- **synchronize() 必须前进**：错误恢复停在边界字符会死循环
- **parseStmt() 必须分发 LeftBrace**：嵌套代码块是合法语句
- **`0 ++i` 词法歧义**：字面量后紧跟 ++ 解析为后缀自增，测试需分号分隔
- **peekNext(n) 先跳过当前位置**（`i <= n`）
- **运算符贪婪匹配**：三字符 > 双字符 > 单字符
- **字符串Token保留完整原始文本**


## 工程纪律（第 296-a 轮：v2 节点池「子终点=建节点时 池.大小()=本节点自身索引」惯例——宿主端点直觉移植必错一档；权重 8，2026-09-17）

**事件**（296-a D35 波B：v2 错误传播 ? 全链实施）：宿主 `UnaryExpr(Propagate)` 是嵌套 AST（操作数=子节点指针），移植到 v2 平铺池模型时按「本节点居末位、操作数段=[子起点, 子终点-1)」直觉实现——结果语义检查把 `底层(v)?` 的操作数末节点定位到**实参 v**（推导出 整32）而非**调用节点**（结果$整32$整32），误报「操作数须为 结果/可选 类型」。真因=v2 建节点惯例：`池追加(池, 建节点(..., 整32(池.大小())))` 的 `池.大小()` 在追加**前**求值，故 节点_三元运算 等「区间尾节点」的 子终点=自身索引——操作数段=[子起点, 子终点)、末节点索引=子终点-1（三元消费方 生成三元运算 即按此约定取段）。

**教训一：移植宿主结构到 v2 平铺池，区间端点语义必须先对齐既有消费方（不只对齐宿主直觉）**——同一字段名（子终点）在两个代码库承载不同端点约定；判据=找一个既有同构节点（三元）读它的生产方（解析）与消费方（生成）各怎么用，禁止只看一处。

**教训二：后缀运算符在「平铺池+尾段判定」解析模型中的优先级须显式续接**——宿主错误传播 `?` 在 parsePostfix（后缀层，高于二元）消费，`x? + 1` 自然成立；v2 的 `?` 判定在二元链**之后**（尾段），错误传播建节点后若直接返回，`+ 1` 无人消费=「语句缺少分号」假象（60 用例行 30 实锤）。修法=建节点后**重复二元链判定**（左起点=起始·含传播节点），右结合三元递归同理保留。



## 306-a 事故轮（T21 首版主分发死循环 28.7GB OOM 连坐 ZCode——同一口径两处分叉=死循环签名；权重 9，2026-09-17）

- **事故链**：306-a T21 区段收紧只改了 readIdentifierOrKeyword 收集循环、lexer 主分发循环仍是旧 `c >= 0x80` 口径→全角字符进收集循环立即 break 返回**空文本 Identifier token 且不消费字符**→主分发 while 无限空转（每轮一个空 token）→诊断/token 列表无限膨胀→cn 进程 28.7GB→内核 OOM 击杀→连坐 ZCode cgroup scope（用户侧 ZCode 被关闭+daemon 同灭）。**旧代码为什么没炸**：`c >= 0x80` 全吞进标识符=区段外字符永远不留在主分发面前——**收紧改造天然暴露「不消费即死循环」的既有结构缺陷**。
- **教训① 谓词/口径类判定必须单点函数化**：同一「是否标识符字符」判定出现在主分发与收集循环两处=改一处漏一处（本次 OOM 直接根因）。预防：收紧/放宽类改造动手前先 grep 全部判定点，收编为单一方法供两处调用；两处口径不一致的死循环签名=「token 空文本+主循环不推进」。
- **教训② 收紧型改造必须带「拒绝路径消费性」验证**：任何「不再接受 X」的改造，X 落入的兜底路径必须验证**会消费输入**（ lexer 未知字符 default 分支 advance+诊断=安全；不消费=死循环）。验收探针：把被拒形态喂给编译器+内存上限（ulimit -v）+timeout，三个都过才算收紧完成（本次首版只验了「合法面通过」没验「拒绝面终止」——恰好漏掉致命半边）。
- **教训③ 大内存进程=系统级武器**：28.7GB 进程触发全局 OOM 的牺牲者选择含 oom_score_adj 高者（浏览器触发·cn 被杀）+cgroup scope 连坐（ZCode 会话同灭）。设施化防线=cnsmith_diff 子进程 RLIMIT_AS 6GB（preexec_fn·306-a 落地）；同类改造（AI 会话内跑不可信/新生成程序）应默认带内存上限+timeout，**先保护宿主会话再谈验证**。


## 449-a 基建轮（本机多开工具链：VS 生成器不支持编译缓存 launcher+Ninja /Z7 防 C1041+worktree 两级深度；权重 7，2026-09-20）

- VS 生成器（vcxproj ClCompile 原生任务）**忽略 `CMAKE_CXX_COMPILER_LAUNCHER`**——sccache/ccache 在 win 只能走 Ninja 生成器（-D 会登记进 CMakeCache 但不进 vcxproj；`--show-stats` 全 0=launcher 未生效签名）；
- Ninja+MSVC 默认 `/Zi`：多 cl 并发写同一 target PDB=C1041（命令行带 /FS 也拦不住）→ 必 `-DCMAKE_CXX_FLAGS_DEBUG:STRING="/Z7 /Ob0 /Od /RTC1"`；
- 勿 `-DCMAKE_CXX_FLAGS` 单值覆盖——顶掉 CMake 默认 /EHsc→gtest 头 C4530×/WX=C2220（「警告被视为错误」），且 **CMakeCache 残留**（后续不再传也沿用坏值）→ 清树重配；
- worktree 必须与主仓库同级（距 Documents 两级）：gtest `${CMAKE_SOURCE_DIR}/../../third-party` 相对路径依赖；cmd 批处理模板注释必须纯 ASCII（中文注释+write_text(encoding="ascii") 直接炸）；
- sccache 跨 worktree 命中 ~48%：hash 含预处理 linemarker 绝对路径——共享路径第三方（gtest/运行时）命中、树内源码 miss（同树热=100%）；跨树新开别指望全命中（wt.py 已固化全套口径）。

## 四链回签服务轮（gate_lock run×ci.ps1 内置 acquire=自死锁 85 分钟；glob 假转 LF 差点冤枉宿主解析器；win 回签拦 EXTERN r11=try-build 分层价值实证；权重 8，2026-09-20）

- **死锁现象**：502 会话以 `python scripts/gate_lock.py run -- powershell ci.ps1` 跑全量门禁——外层 run 持锁后 spawn ci.ps1，而 ci.ps1 首步**内置 acquire**（449-a 起）再等同一把锁：进程树 run(python)→ci.ps1(powershell)→acquire(python·sleep 轮询)，17:47~19:09 共 85 分钟**零构建零测试**（心跳照常更新=陈锁接管 4h 永远不触发）。
- **死锁处置与预防**：①解锁=taskkill **叶子进程**（内层 acquire）——acquire 超时失败→ci.ps1 退出→run 释放锁**级联自解**，勿需杀整树；②`gate_lock run` 包装**仅适用于无内置锁的命令**（integrate.py linux 分支场景），ci.ps1 自带排队（默认 2h）直接裸跑；③同机多开下「全量门禁长时间无 CPU 动静」的判活=查锁持有进程的**子进程命令行**（无 MSBuild/cl/cn.exe 大进程而锁不释放=疑似死锁）。
- **glob 假转误判链**：488-D2a「宿主输出不一致（第3行）」定性时，行尾转换脚本 glob 模式误写 `488_/*`（字面目录名）却误读了输出——在**未真转换**的 CRLF 文件上跑出 `多行第一行\r\r\n`（源文件 CRLF 的 \r 进入多行字符串字面量内容+Python splitlines 把 `\r\r\n` 拆出空行=行错位），差点定罪「win 宿主解析器注入 \r」；真转 LF 后单跑 PASS+宿主直跑字节干净=纯**检出假红**（447-a 同款·分支基点早于 .gitattributes 集成，rebase 自愈）。
- **预防**：①行尾类实验必须「转换后立即重读字节确认状态」再下结论（`b'\r\n' in data` 一次判定）；②glob 目录段用通配 `488_*/`，目录全名永不手写；③「跨平台行为差异」定性前先做**同代码双文件行尾对照**（LF 源 vs CRLF 源各自实跑抓原始字节）。
- **正面实证**：win 侧回签拦截 `EXTERN r11:PROC`×7 例（T69 间接调用 `call r11` 的寄存器操作数被 EXTERN 收集器误当符号名——linux/arm64 双绿独 win 红）；且 check_win_asm.py 三判据对该形态**合取漏形**（无 `[`/空白=判据②不拦·非本地定义=判据①无关·已 EXTERN=判据③放行）——单判据各自合法、组合仍可漏，「门禁判据须按 ml64 真实报错面反向覆盖」（建议补判据④=EXTERN 操作数 ∈ 寄存器名集→拦）。

## 546-a 双教训（v2p 缓存污染两次带红集成同根；v2 arena 逐行拼接 O(n²) 膨胀破 4GB RLIMIT；权重 9，2026-09-21）

- **v2p 缓存污染误判链（两次同根·456 红之谜终审 6bda3558）**：①522 收工「456 红≠537 引入（手动 rc=27）」与②深度机 4355ebc0「HEAD 456 恢复绿（82fc1dc0）」**均为旧 v2p 产物误导**——「手动对照」路径无 runner 指纹护栏，audit2/v2p_* 未随源码 checkout/变更重建，编译行为=**旧源码的 v2p** 而非受测态。干净对照三连（537 前态清缓存重建→456 PASS；基点同法→红；546 态同法→红）实锤引入点=537-a 布局收口拆分。**预防**：跨提交行为对照前必 `rm target/audit2/v2p* v2p_build_key*`（或以 runner 指纹重建为准）；「手动绿 vs runner 红」分裂本身=**v2p 版本分叉信号**，禁止下「runner 环境问题」结论前先对 v2p md5。
- **v2 树字符串重建=arena O(n²) 中间串留存**：预处理逐行 `输出 = 输出 + 行` 在 arena 分配下中间串**不释放**，大文件（生成表达式求值.cn 等）累计 GB 级——v2p 编 v2 全树于 runner 4GB RLIMIT 下 std::bad_alloc（fix_p 卡死同源）；**同 v2p 手动跑（无 ulimit）通过**=内存类缺陷的「手动绿」陷阱（rc 判据无效族新形态）。**根治范式（546-a 快路径）**：①零改动直通快路径（`源码含预处理指令` 零分配字节扫描→`返回 字符串复制(源码)` O(n) 单次分配；安全区检查器拦借用返回=正面实证）；②确需逐行重建时先算总长一次分配。**预防**：v2 树内循环字符串拼接一律过「预计分配规模」审查；门禁跑 runner（带 RLIMIT）而非裸命令。

## 二、全量索引区（294 条·时间倒序=最新在最上·标注=三判据分拣结果）
- 586 集成轮（**integrate.py 写集分类对中文路径失效=全量门禁静默跳过**：git diff --name-only 默认 quotepath 转义输出带前导双引号路径→startswith("tests/") 恒假→93 个 tests 文件被判纯文档直推 develop（83de088a 实锤·E2E 目录全中文=必踩面）；冲突标记检查路径拼装同受害静默 skip——修复=git -c core.quotepath=false+strip 防御（63611b1c）；预防=脚本路径匹配必须考虑 quotepath 形态+集成工具门禁分类要有反证自检；权重 9，2026-09-21） 〔活跃〕
- 586 集成轮（check_handoff FAIL 被管道 tail 洗退出码**再犯**："python xx.py 2>&1 | tail -5 && git commit" 链=tail 退出码 0 放行带病提交——565-a 已立「>log;RC=$? 三段式」口诀仍未执行；本案升级=凡门禁命令一律禁入管道/&& 链，先落 log 文件再判 rc；权重 7，2026-09-21） 〔活跃〕

- 581 协议v3轮（rebase 解冲突取错方向=**静默覆盖 develop 已集成的他人修改**——575-a 把 580-a AGENTS 三处修改盖回旧版·文档语义回退不可机械检测门禁不拦·581-a 读文发现；共享文档 rebase 须辨「分支旧基线×develop 已含新集成」时序取并集+diff 近期收工点抽查回退面；权重 9，2026-09-21） 〔活跃〕
- 558 紧急修复轮（develop arm64 红双缺陷：457-a 发射面改造「未分配 fallback 产物逐字节不变」只覆盖 x9 退化路径——**直读路径的文本形态（丢 `[`/w 转换）才是新面**，win 门禁孤证宣告「零新增红」=假绿；arm64 实机验证只有单位机能做·离线窗口期集成链无复验=防线真空，权重 9，2026-09-21） 〔活跃〕
- 四链回签服务轮（gate_lock run×ci.ps1 双重 acquire 自死锁 85 分钟——run 包装仅限无内置锁命令·解锁杀叶子级联自解；glob 假转 LF 差点冤枉宿主解析器——行尾实验必须转换后核实字节；win 回签拦 EXTERN r11=try-build 分层价值实证；权重 8，2026-09-20） 〔活跃〕
- 449-a 基建轮（VS 生成器无编译缓存 launcher+Ninja /Z7 防 C1041+worktree 两级深度+同机多开基建全链；权重 7，2026-09-20） 〔活跃〕
- 工程纪律（2026-09-17 探针轮：非常量输入的探针必须用确定性源——时间戳/随机数会让「跨运行对照」退化为伪对照；权重 7，2026-09-17） 〔活跃〕
- 编译器缺陷（第七十五轮：v2 深层函数的上下文获取=静态镜像；容器元素释放的三种模型（平铺/链式/交换前缀）；权重 7，2026-09-12） 〔活跃〕
- 工程纪律（第七十一轮：一句话交接任务的范围核验——「move 对齐」实为构造器能力整体缺失；注释式二分禁用=清零行动的盲区形态，2026-09-11） 〔活跃〕
- 工程纪律（第七十轮：二分禁用残留的复审纪律+装箱浅共享×局部 RAII=悬垂家族第五实例，2026-09-11） 〔活跃〕
- 工程纪律（第六十八轮：未提交工作树上的批量 git checkout=迁移成果全失+中间态二进制调试的时间黑洞，2026-09-11） 〔活跃〕
- 工程纪律（第六十七轮：v2 结构体新字段未入显式初始化清单——半成品 v2p 构建即崩；E2E 共享产物的并行隔离面，2026-09-11） 〔活跃〕
- 工程纪律（第六十六轮：拥有型字符串三层借用漏网——黑名单拦不住用户函数包装、白名单制才是单类型拥有语义的正解，2026-09-10） 〔活跃〕
- 工程纪律（第五十八轮：负测用例「假绿」子串设计+v2 括号笔误与静态跨文件——三坑当场识破，2026-09-10） 〔活跃〕
- 工程纪律（第五十七轮：跨侧（宿主/v2）写用例三坑——打印语义颠倒直觉/锚定链脚本继承已知 bug/v2 语法面未探先写，2026-09-10） 〔活跃〕
- 工程纪律（2026-09-10：设计呈报中的语法示例未查规范凭 Rust 记忆臆造——语义可借鉴、语法必须查规范） 〔活跃〕
- 编译器缺陷（第五十二轮：v2 ARM64 取片有符号取模出负片——movk 立即数越界，2^63 唯一爆点，边界形态探针缺位63形态，2026-09-10） 〔判据②已修复退役〕
- 验证方法（第五十轮：正/负断言方向读反——「返回错误码触发」被误读为「断言失败」，假阳性定性差点引发宿主无用改动，2026-09-10） 〔活跃〕
- 工程纪律（第四十九轮：v2 新源码触发既有拦截器——自举锚定链是自写代码的第一道门禁，2026-09-10） 〔活跃〕
- 工程纪律（第四十八轮：辅助函数「自身/父链」双形态参数语义混用——拦截永不命中的假阴性，2026-09-09） 〔活跃〕
- 验证方法（第四十四轮：独立复算必须用被验系统的代码内常量——教科书 FNV basis 尾差一位致假阴性，2026-09-09） 〔活跃〕
- 工程纪律（第四十三轮：管道 tail 掩盖真实退出码——失败被吞、旧产物续用，2026-09-09） 〔活跃〕
- 设计缺陷（第四十一轮：编译器自身源码=对拍输入面——「自身源码不用某语法」=该语法零验证，2026-09-09） 〔活跃〕
- 工程纪律（第四十一轮：同族修复旁路穷尽再演——imul 修了 imm32 域检查、cmp 漏了，2026-09-09） 〔活跃〕
- 验证方法（第四十一轮：路径文本匹配对平台分隔符脆弱——win 反斜杠路径使 v2 恢复包上下文失配，2026-09-09） 〔活跃〕
- 设计缺陷（第四十轮：MASM 247 字符标识符硬上限——超长符号的失效形态是 PROC 配对崩坏而非单点报错，2026-09-09） 〔活跃〕
- 设计缺陷（第四十轮：GCC 内建与 POSIX 弃用函数入库——深度机编译通过≠跨平台可移植，2026-09-09） 〔活跃〕
- 工程纪律（第四十轮：多分支对称改动漏支——「同一改动触及多平台分支」必须穷尽分支清单，2026-09-09） 〔活跃〕
- 设计缺陷（第三十八轮：模块身份靠「入口目录相对路径文本」推断——同一文件不同入口形态行为漂移，2026-09-08） 〔活跃〕
- 高权重问题（第三十八轮：主() 丢弃编译入口返回值——「忽略」型假绿是契约冲突的温床，2026-09-08） 〔活跃〕
- 验证方法（第三十八轮：CN 布尔条件跨语言移植写反——「字符串比较 真=相等」与直觉同向但否定词最易错，2026-09-08） 〔活跃〕
- 高权重问题（第三十七轮：宿主 &&/|| 全求值发射——「良性」对拍差异必须追到发射语义层定案，2026-09-08） 〔活跃〕
- 高权重问题（第三十六轮：git checkout 回滚单文件连带丢弃同文件多轮未提交修改——回滚前必须盘点未提交轮次，2026-09-08） 〔活跃〕
- 验证方法（第三十六轮：管道后 $? 是最后一个命令的返回码——rc 判定必须独立执行，2026-09-08） 〔活跃〕
- 工程纪律（第三十六轮：Python 行切片「含头不含尾」与行号闭区间思维混用——脚本切片必须首尾断言，2026-09-08） 〔活跃〕
- 高权重问题（第三十五轮：宿主 IR 寄存器号跨函数累积——栈帧随模块内位置线性膨胀+误诊链纠偏，2026-09-08） 〔活跃〕
- 高权重问题（第三十五轮：③合并全局可见对文件级快照双计数——快照维度与消费语义必须对表，2026-09-08） 〔活跃〕
- 验证方法（第三十五轮：陈旧链接产物误导取证——被测二进制新鲜度是取证前提，2026-09-08） 〔活跃〕
- 高权重问题（第三十三轮：类型/常量布局表纯名平铺跨模块串桶——键域必须全链统一，2026-09-08） 〔活跃〕
- 高权重问题（第三十三轮：类型文本后缀未剥离误报——文本维度判定必须枚举全部合成形态，2026-09-08） 〔活跃〕
- 验证方法（第三十三轮：并行任务共享中间产物互踩——假对拍第六案同族，2026-09-08） 〔活跃〕
- 高权重问题（第三十三轮呈报：v2 结构体构造字面量语法缺口——宿主/v2 语法覆盖面差距，2026-09-08） 〔活跃〕
- 高权重问题（第二十九轮：v2 静态槽非对齐链接截断——对齐是发射期布局契约，2026-09-07） 〔活跃〕
- 高权重问题（第二十九轮：154 同族缺陷 arm64 孪生——同族修复必须旁路穷尽，2026-09-07） 〔活跃〕
- 验证方法（第二十九轮：对拍规范化基准必须跨文件确定——假对拍第五案，2026-09-07） 〔活跃〕
- 高权重问题（plans/017 两灰色点根治轮：对拍脚本目录迁移路径依赖未随迁——假对拍第四案，2026-09-07） 〔活跃〕
- 高权重问题（plans/017 v2self 三级锚定轮：入口回退掩盖编排错误——假对拍第三案，2026-09-07） 〔活跃〕
- 高权重问题（plans/017 T3/T4 解锁暴露回归：平台跳过掩盖面——解锁日=欠账日，2026-09-06） 〔活跃〕
- 高权重问题（plans/017 呈报③方案A 实施：重载消歧 fail fast 双拦截+假对拍二度复现，2026-09-06） 〔活跃〕
- 高权重问题（plans/017 呈报①方案A 第五轮：第 16 枚根治——v2 指针参数三重缺陷，2026-09-06） 〔活跃〕
- 高权重问题（plans/017 呈报①方案A 第四轮：第 7 层根治+v2self 首次完整运行，2026-09-06） 〔活跃〕
- 高权重问题（plans/017 呈报①方案A 第三轮：容器所有权语义对齐六枚+v2self 至 IR 生成，2026-09-06） 〔活跃〕
- 高权重问题（plans/017 呈报①方案A 第二轮：否则如果链双重生成+v2 IR 净化，2026-09-06） 〔活跃〕
- 高权重问题（plans/017：v2 产物级验证空白照出宿主栈参锚+ v2 四缺陷，2026-09-06 深度系统 x86_64） 〔活跃〕
- 高权重问题（plans/018 包/货舱 死保留字摘除——立规过度保留翻案，2026-09-07） 〔活跃〕
- 高权重问题（第三十二轮：静态容器引用实参双侧同构缺陷——取地址语义丢失，2026-09-07） 〔活跃〕
- 高权重问题（第三十二轮：嵌套容器跨组件传输——构造非成熟路径家族第二例，2026-09-07） 〔活跃〕
- 验证方法（第三十二轮：增量构建假象——编译失败被 grep 过滤掩盖，2026-09-07） 〔活跃〕
- 高权重问题（v2p 本机冒烟：linux_x64 参数拷贝临时寄存器 r9 冲突第 6 参数寄存器，2026-09-05 深度系统 x86_64） 〔活跃〕
- 高权重问题（plans/016 呈报复核轮：win 第4路 calleeReturnsStruct 死代码归真，2026-09-05 家机 win-x64） 〔活跃〕
- 高权重问题（呈报①②方案A实施轮：check 假绿根治+常量诡异触发归因反转，2026-09-05 家机 win-x64） 〔活跃〕
- 高权重问题（缺陷零容忍收口轮：引用返回/裸引用/无符号窄宽+宿主引用返回读值+常量诡异触发，2026-09-04 家机 win-x64） 〔活跃〕
- 高权重问题（IR生成.cn 冒烟 96 错收口 + [ir2] 819→0 + IR 层错误即中止，2026-09-04 家机 win-x64） 〔活跃〕
- 高权重问题（plans/015 强制分号语句边界改造·万行级迁移的 token 级引擎陷阱链，2026-09-04 家机 win-x64） 〔活跃〕
- 高权重问题（语法分析.cn 冒烟 55 错四簇消化 + 簇⑥宿主星号泛型实例名误剥，2026-09-04 单位机 麒麟 ARM64） 〔活跃〕
- 高权重问题（灰色点②方案A：字符串比较运算符显式拒绝，2026-09-04 家机 win-x64） 〔活跃〕
- 高权重问题（③ 第六波：三元条件运算符 + 入口前缀紧绑定 + 静态自增丢句根治，2026-09-03 家机 win-x64） 〔判据②已修复退役〕
- 高权重问题（③ 第五波：限定名内置 IO/文件 + 真假无 + 二元链前缀，2026-09-03 家机 win-x64） 〔判据②已修复退役〕
- 高权重问题（④ 第四轮：缺陷② v2 侧 + 裸节点双重处理 + 缺陷 A/B 根治，2026-09-03 家机 win-x64） 〔判据②已修复退役〕
- 高权重问题（③ 第三波：指针下标读写 + 宿主内存模型统一，2026-09-03 家机 win-x64） 〔判据②已修复退役〕
- 高权重问题（③ 第二波：字符串下标+复合赋值+登记点补全，2026-09-03） 〔判据②已修复退役〕
- 高权重问题（③ 自举对拍启动，2026-09-03） 〔判据②已修复退役〕
- 高权重问题（②b 结构体元素容器收官，2026-09-02） 〔判据②已修复退役〕
- 高权重问题（宿主运行时根治，2026-09-01） 〔判据②已修复退役〕
- 高权重问题（用户令缺陷零容忍后首例，2026-09-01） 〔判据②已修复退役〕
- 高权重问题（P7b v2 容器支持 新增，2026-09-01） 〔活跃〕
- 高权重问题（第 12 层 B 任务多文件拆分 新增，2026-08-18） 〔活跃〕
- 高权重问题（第 11 层 性能基线对比 新增，2026-08-18） 〔活跃〕
- 高权重问题（第 10 层 Debug 自举闭环审查 新增，2026-08-17） 〔判据②已修复退役〕
- 高权重问题（第 9 层 Debug 全面审查 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（第 8 层 全链路集成串联 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（第 7 层 新增 v2.0 特性测试 新增，2026-08-15） 〔活跃〕
- 高权重问题（第 6 层 全量迁移 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（第 4 层 crate 模型核心重构 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（Task 6.13 模块导入语法测试补全 新增，2026-08-15） 〔活跃〕
- 高权重问题（Task 6.11 全链路集成 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（Task 6.5/6.9/6.10 字符串扩展+时间+系统库 新增，2026-08-15） 〔活跃〕
- 高权重问题（Task 6.4/6.7 映射集合+算法库 新增，2026-08-15） 〔判据②已修复退役〕
- 高权重问题（Task 6.1 核心库+容器库 新增，2026-08-14） 〔判据②已修复退役〕
- 高权重问题（Task 6.6/6.3 新增，2026-08-14） 〔活跃〕
- 高权重问题（必须避免） 〔判据②已修复退役〕
- 高权重问题（Task 1.9 新增） 〔判据②已修复退役〕
- 高权重问题（生产环境审查 Task 1.10 新增，2026-08-12 19:04） 〔活跃〕
- 实施规范（用户规则） 〔活跃〕
- 任务经验（2026-08-12 实施计划文档拆分） 〔判据②已修复退役〕
- 任务经验（Task 2.1 控制流增量实施 switch，2026-08-12） 〔活跃〕
- 任务经验（Task 2.2 函数完整支持，2026-08-12） 〔活跃〕
- 高权重问题（Task 2.3 类型系统新增） 〔活跃〕
- 高权重问题（Task 2.4 数组与指针新增） 〔活跃〕
- 高权重问题（优化器增强完善C 新增，2026-08-13） 〔活跃〕
- 高权重问题（Task 2.9/2.10 Debug 审查 新增，2026-08-13 12:37） 〔活跃〕
- 高权重问题（Task 3.6 模块系统 新增，2026-08-14） 〔活跃〕
- 高权重问题（Debug 全面审查子任务 新增，2026-08-14，5 项遗留缺陷已修复 ✅） 〔判据②已修复退役〕
- 高权重问题（阶段A-3：Linux ARM64 E2E 2 项缺陷修复，2026-08-14，已修复 ✅） 〔判据②已修复退役〕
- 高权重问题（阶段C：寄存器分配 + 调试信息，2026-08-14，已修复 ✅） 〔判据②已修复退役〕
- 高权重问题（新Win10环境恢复发现，2026-08-14） 〔判据②已修复退役〕
- 高权重问题（阶段6第4步 IO输入+文件库 新增，2026-08-14，已修复 ✅） 〔判据②已修复退役〕
- 高权重问题（模块系统 v2.0 第 5 层 货舱.toml，2026-08-15，已解决 ✅） 〔判据②已修复退役〕
- 高权重问题（批次 A/B/C 执行 新增，2026-08-16） 〔判据②已修复退役〕
- P3-18 引用返回（2026-08-19，007-P3） 〔活跃〕
- P3-23 实例方法作值（2026-08-19，007-P3） 〔活跃〕
- P3 去虚拟化与 CFI（2026-08-19，007-P3） 〔活跃〕
- 高权重问题（麒麟 ARM64 全量回归验证 新增，2026-08-20，已修复 ✅） 〔判据②已修复退役〕
- [2026-08-20 23:40] 平台抽象层重构自举侧5组件创建 〔活跃〕
- ARM64后端窄类型加载/存储bug（2026-08-21 修复） 〔判据②已修复退役〕
- 高权重问题（plans/016 linux-x86_64 后端专项：六枚汇编/ABI 缺陷三层实证根治，2026-09-05 深度系统 x86_64 本机） 〔活跃〕
- 高权重问题（plans/017 第18枚：结构体整体赋值单槽浅拷——v2self 引用槽登记全 miss 原发点，2026-09-06 第二十四轮 修复 ✅） 〔活跃〕
- 中权重问题（R3 函数拆分轮踩坑三条，2026-09-08 第三十五轮 单位机ARM64） 〔活跃〕
- 类型系统严格化轮踩坑（2026-09-10 第五十一轮 深度系统 x86_64） 〔活跃〕
- 68-a 修复轮验证踩坑（2026-09-11 第六十九轮 单位机麒麟 ARM64） 〔活跃〕
- 76-a 移除路径所有权轮踩坑（2026-09-12 第七十六轮 深度机 x86_64） 〔活跃〕
- 194 修复与 v2 解引用类型推导分叉根治（2026-09-12 第七十六轮后续 深度机 x86_64） 〔判据②已修复退役〕
- 77-a 借出视图生命周期检查轮（2026-09-12 第七十七轮 深度机 x86_64） 〔活跃〕
- 78-a v2 用户自定义类支持轮（2026-09-12 第七十八轮 深度机 x86_64） 〔活跃〕
- 79-a 聚合拥有型字符串字段 drop glue 轮（2026-09-12 第七十九轮 深度机 x86_64） 〔判据②已修复退役〕
- 80-a v2 池索引 0 哨兵槽轮（2026-09-12 第八十轮 深度机 x86_64） 〔活跃〕
- 81-a 容器元素 = 含拥有型串字段结构体轮（2026-09-12 第八十一轮 深度机 x86_64） 〔活跃〕
- 82-a v2 帧尾共享 retbuf 根治轮（2026-09-12 第八十二轮 深度机 x86_64） 〔活跃〕
- 82-b v2 赋值位接收结构体返回轮（2026-09-12 第八十二轮后续 深度机 x86_64） 〔活跃〕
- 83-a 链表/队列槽分配与回收轮（2026-09-12 第八十三轮 深度机 x86_64） 〔活跃〕
- 84-a v2 结构体字面量作实参轮（2026-09-12 第八十四轮 深度机 x86_64） 〔活跃〕
- 84-b v2 字面量字段校验轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 84-c 嵌套结构体字面量轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 84-d 成员链嵌套深度轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 84-e 嵌套结构体字段串释放面收口轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 84-f 未初始化槽 preFree 隐患核验轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 84-g P32-2 增量观察项销项轮（2026-09-12 第八十四轮后续 深度机 x86_64） 〔活跃〕
- 85-a 聚合返回位所有权链断裂根治（2026-09-12 第八十五轮 深度机 x86_64） 〔活跃〕
- 86-a 复审三缺陷根治（2026-09-12 第八十六轮 深度机 x86_64） 〔活跃〕
- 87-a 静态变量全链路根治（2026-09-12 第八十七轮 深度机 x86_64） 〔活跃〕
- 88-a v2 实参位短路栈失衡根治（2026-09-12 第八十八轮 深度机 x86_64） 〔活跃〕
- 89-a 净栈契约出口校验落地 + 浮点字面量全链根治（2026-09-12 第八十九轮 深度机 x86_64） 〔活跃〕
- 跨机轮 win 侧 148_v2 崩溃拦截（2026-09-12 第八十六~八十八轮，家机 win-x64） 〔活跃〕
- 91-a v2 浮点语义面全链补全 + 宿主 NaN 比较缺陷根治（2026-09-13 第九十一轮 深度机 x86_64） 〔活跃〕
- 92-a 宿主浮32 面缺陷族根治（H2 销项）+ v2 侧浮点宽化面分叉收口（2026-09-13 第九十二轮 深度机 x86_64） 〔活跃〕
- 93-a 类名/实例成员静态性不匹配六形态编译期拒绝（B2 销项）（2026-09-13 第九十三轮 深度机 x86_64） 〔活跃〕
- 94-a v2 数组声明语法缺口（C6）销项——v2 栈数组全链（2026-09-13 第九十四轮 深度机 x86_64） 〔活跃〕
- 95-a v2 字符字面量缺口（C7）销项 + 宿主缺陷 H3 根治（2026-09-13 第九十五轮 深度机 x86_64） 〔活跃〕
- 96-a v2 字符串字面量 `\u{XXXX}` 解码（C10 销项）（2026-09-13 第九十六轮 深度机 x86_64） 〔活跃〕
- 97-a v2 结构体字段数组（C8 销项）（2026-09-13 第九十七轮 深度机 x86_64） 〔活跃〕
- 98-a 字符串元素数组释放（C9 销项，双侧）（2026-09-13 第九十八轮 深度机 x86_64） 〔活跃〕
- 99-a 结构体字段数组元素释放（C11 销项）+ 宿主缺陷 H4 根治（2026-09-13 第九十九轮 深度机 x86_64） 〔活跃〕
- 100-a 元素=含串字段结构体的字段数组释放（C12 销项）（2026-09-13 第一百轮 深度机 x86_64） 〔活跃〕
- 101-a C1 阶段 4' 第三批（A2 正文）销项——字符* 借用视图类型化（2026-09-13 第一百零一轮 深度机 x86_64） 〔活跃〕
- 102-a 多文件静态初始化注入（C4 销项）+ 宿主缺陷 H5 根治（2026-09-13 第一百零二轮 深度机 x86_64） 〔活跃〕
- 103-a v2 诊断行号落地（D2 销项）+ 契约迁移（2026-09-13 第一百零三轮 深度机 x86_64） 〔活跃〕
- 106-a C2 跨语句 NLL 评估 + C13 借用返回精化评估（纯评估轮）（2026-09-13 第一百零六轮 深度机 x86_64） 〔活跃〕
- 107-a v2 缺陷 H6 根治——可选/结果 裸「无」全链（2026-09-13 第一百零七轮 深度机 x86_64） 〔活跃〕
- 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（2026-09-13 第一百零八轮 深度机 x86_64） 〔活跃〕
- 第一百零九轮踩坑（2026-09-13，家机 win-x64：93-a~108-a 跨机轮 + 109-a 修复） 〔活跃〕
- 第一百一十轮踩坑（2026-09-13，家机 win-x64：110-a win 侧五项立案收口 + 宿主 win 构建根治） 〔活跃〕
- 第一百一十一轮踩坑（2026-09-13，家机 win-x64：111-a E2E runner win 双编译对照原生支持 + size 说明符扫描门禁 + 新缺陷 221 双侧收口） 〔活跃〕
- 第一百一十二轮踩坑（2026-09-13，家机 win-x64：112-a D1 行数整改第三波——ir_expr.cpp 2798→734 行按族拆分） 〔活跃〕
- 第一百一十三轮踩坑（2026-09-13，家机 win-x64：113-a D1 行数整改第四波——ir_oop.cpp 1381→508 行按族拆分） 〔活跃〕
- 第一百一十四轮踩坑（2026-09-13，家机 win-x64：114-a D1 行数整改第五波——ir_oop_call.cpp 1155→763 行） 〔活跃〕
- 第一百一十五轮踩坑（2026-09-13，家机 win-x64：115-a semantic_internal.hpp 共享头——11 helper × 5 文件去重） 〔活跃〕
- 第一百一十六轮踩坑（2026-09-13，家机 win-x64：116-a semantic_expr.cpp 1778→654 行按族拆分） 〔活跃〕
- 第一百一十七轮踩坑（2026-09-13，家机 win-x64：117-a semantic.cpp 1996→692 行按族拆分） 〔活跃〕
- 第一百一十八轮踩坑（2026-09-13，家机 win-x64：118-a x64_instructions.cpp 1812→921 行按族拆分） 〔活跃〕
- 第一百一十九轮踩坑（2026-09-13，家机 win-x64：119-a codegen 三后端装配文件按族拆分） 〔活跃〕
- 规则强化（2026-09-13 用户令·最高优先级：尽善尽美禁止妥协——折中判定清单／推倒重来授权／「最完美方案」呈报要求） 〔活跃〕
- 第一百二十轮踩坑（2026-09-13，家机 win-x64：120-a v2 侧开波——生成表达式.cn 1512→738 行） 〔活跃〕
- 第一百二十一轮踩坑（2026-09-13/14 跨零点，家机 win-x64：121-a v2 侧生成语句/代码生成拆分） 〔活跃〕
- 第一百二十二轮踩坑（2026-09-14 凌晨，家机 win-x64：122-a v2 侧语义检查拆分） 〔活跃〕
- 第一百二十三轮踩坑（2026-09-14 凌晨，家机 win-x64：123-a v2 侧 IR生成拆分） 〔活跃〕
- 第一百二十四轮踩坑（2026-09-14 凌晨，家机 win-x64：124-a IR容器收尾波） 〔活跃〕
- 第一百二十五轮踩坑（2026-09-14 凌晨，家机 win-x64：125-a 宿主 ast.hpp 按层拆分） 〔活跃〕
- 第一百二十六轮踩坑（2026-09-14 凌晨，家机 win-x64：126-a emitCast 函数级提取） 〔活跃〕
- 第一百二十七轮踩坑（2026-09-14 凌晨，家机 win-x64：127-a emitCompare 函数级提取） 〔活跃〕
- 第一百二十八轮踩坑（2026-09-14 凌晨，家机 win-x64：128-a injectContainerElemDestroy 映射族提取） 〔活跃〕
- 第一百二十九轮踩坑（2026-09-14 凌晨，家机 win-x64：129-a 四族提取尝试未成功——已回退） 〔活跃〕
- 第一百三十轮踩坑（2026-09-14 上午，家机 win-x64：130-a 129-a 逐族重做成功） 〔活跃〕
- 第一百三十一轮踩坑（2026-09-14 上午，家机 win-x64：131-a 字符串元素族提取） 〔活跃〕
- 第一百三十二轮踩坑（2026-09-14 上午，家机 win-x64：132-a 含串字段族提取） 〔活跃〕
- 第一百三十三轮踩坑（2026-09-14 上午，家机 win-x64：133-a 有析构类族提取 + 会话中断恢复） 〔活跃〕
- 第一百三十四轮踩坑（2026-09-14 上午，家机 win-x64：134-a 波 1 修 字符* 拼接误报） 〔活跃〕
- 第一百三十五轮踩坑（2026-09-14 上午，家机 win-x64：135-a 波 2 清单编制） 〔活跃〕
- 第一百三十六轮踩坑（2026-09-14 上午，家机 win-x64：136-a 波 3 开工） 〔活跃〕
- 第一百三十七轮踩坑（2026-09-14 上午，家机 win-x64：137-a 设施探摸） 〔活跃〕
- 第一百三十八轮踩坑（2026-09-14 上午，家机 win-x64：138-a 波 3 轮 A 实施尝试→回退） 〔活跃〕
- 第一百三十九轮踩坑（2026-09-14 上午，家机 win-x64：139-a 最小闭环） 〔活跃〕
- 第一百四十轮踩坑（2026-09-14 上午，家机 win-x64：140-a 残余澄清 + 栈族） 〔活跃〕
- 第一百四十一轮踩坑（2026-09-15，家机 win-x64：141-a 链式族） 〔活跃〕
- 第一百四十二轮踩坑（2026-09-15，家机 win-x64：142-a 专项核查 + 两次回退） 〔活跃〕
- 第一百四十三轮踩坑（2026-09-15，家机 win-x64：143-a Bisect 澄清） 〔活跃〕
- 第一百四十五轮踩坑（2026-09-15，家机 win-x64：145-a 泛化崩因定位） 〔活跃〕
- 第一百四十六轮踩坑（2026-09-15，家机 win-x64：146-a uF 专项） 〔活跃〕
- 第一百四十七轮踩坑（2026-09-15，家机 win-x64：147-a 全路径盘查） 〔活跃〕
- 第一百四十八轮踩坑（2026-09-16，家机 win-x64：148-a 三连同型） 〔活跃〕
- 第一百五十轮踩坑（2026-09-16，家机 win-x64：150-a 六类实装） 〔活跃〕
- 第一百五十一轮踩坑（2026-09-16，家机 win-x64：151-a 波 5 迁移） 〔活跃〕
- 跨机验证轮踩坑（2026-09-14，单位机麒麟ARM64：93-a~108-a+111-a~146-a 全量 arm64 复验） 〔活跃〕
- 第一百五十二轮踩坑（2026-09-16，家机 win-x64：152-a tests 面迁移） 〔活跃〕
- 跨机验证轮踩坑（2026-09-14，单位机麒麟ARM64：93-a~108-a+111-a~146-a 全量 arm64 复验） 〔活跃〕
- 第一百五十二轮踩坑（2026-09-16，家机 win-x64：152-a tests 面迁移） 〔活跃〕
- 第一百五十三轮踩坑（2026-09-17，家机 win-x64：153-a v2 挂点基础设施） 〔活跃〕
- 第一百五十四轮踩坑（2026-09-17，家机 win-x64：154-a v2 挂点续） 〔活跃〕
- 第一百五十五轮踩坑（2026-09-17，家机 win-x64：155-a v2 挂点续②） 〔活跃〕
- 第一百五十六轮踩坑（2026-09-17，家机 win-x64：156-a 类方法修饰 + 波 6 收口准备） 〔活跃〕
- 第一百五十七轮踩坑（2026-09-17，家机 win-x64：157-a 波 6 收口） 〔活跃〕
- 第一百五十八轮踩坑（2026-09-14，单位机麒麟 ARM64：158-a 跨机-a/b 方案A 实施） 〔判据①门禁覆盖·check_ascii_idents〕
- 第一百六十轮踩坑（2026-09-14，单位机麒麟 ARM64：160-a 三项批准落盘） 〔活跃〕
- 第一百六十一轮踩坑（2026-09-14，家机 win-x64：161-a A8 冒号后置类型标注移除） 〔活跃〕
- 第一百六十二轮踩坑（2026-09-14，家机 win-x64：162-a A5+A3 关键字体系实施） 〔活跃〕
- 第一百六十三轮踩坑（2026-09-14，家机 win-x64：163-a 78/79 → v2 锚定链迁移） 〔活跃〕
- 第一百六十四轮踩坑（2026-09-14，家机 win-x64：164-a A4 联合体拥有型成员收口） 〔活跃〕
- 第一百六十五轮踩坑（2026-09-14，家机 win-x64：165-a C16 规范落盘） 〔判据①门禁覆盖·check_progress_sync〕
- 第一百六十六轮踩坑（2026-09-14，家机 win-x64：166-a D1 函数级拆分第一波） 〔活跃〕
- 第一百六十七轮踩坑（2026-09-14，家机 win-x64：167-a D1 函数级拆分第二波） 〔活跃〕
- 第一百六十八轮踩坑（2026-09-14，家机 win-x64：168-a D1 函数级拆分第三波） 〔活跃〕
- 第一百六十九轮踩坑（2026-09-14，家机 win-x64：169-a D1 函数级拆分第四波） 〔活跃〕
- 第一百七十轮踩坑（2026-09-15，深度机 linux-x86_64 接手：170-a D1 函数级拆分第五波——visitCallExpr 族A struct 化收尾） 〔活跃〕
- 第一百七十二轮踩坑（2026-09-15，深度机 linux-x86_64：172-a D1 v2 侧拆分——生成赋值语句 + 度量口径根治 + 行尾归一） 〔判据①门禁覆盖·check_fn_length〕
- 第一百七十五轮踩坑（2026-09-15，深度机 linux-x86_64：175-a D1 函数级续波——visitAssignmentExpr 349 行拆分） 〔活跃〕
- 第一百七十六轮踩坑（2026-09-15，深度机 linux-x86_64：176-a D1 函数级续波——visitBinaryExpr 269 行拆分） 〔活跃〕
- 第一百七十八轮踩坑（2026-09-15，深度机 linux-x86_64：178-a D1 函数级续波——lvalueAddress 225 行拆分） 〔活跃〕
- 第一百四十九轮踩坑（补记 2026-09-15，家机 win-x64：149-a IR diff 专项——uF 崩因与 chain 退化根因定位〔146~148-a 误诊纠正〕） 〔活跃〕
- 第一百七十九轮踩坑（2026-09-15，深度机 linux-x86_64：179-a D1 函数级续波——visitMemberExpr 216 行拆分） 〔活跃〕
- 第一百八十一轮踩坑（2026-09-15，深度机 linux-x86_64：181-a D1 函数级续波——visitUnaryExpr 204 行拆分） 〔活跃〕
- 第一百八十二轮踩坑（2026-09-15，深度机 linux-x86_64：182-a D1 函数级续波——visitIdentifierExpr 198 行拆分） 〔活跃〕
- 第一百八十四轮踩坑（2026-09-15，深度机 linux-x86_64：184-a D1 v2 侧续波——检查变量声明语句 180 行拆分） 〔活跃〕
- 第一百八十五轮踩坑（2026-09-15，深度机 linux-x86_64：185-a D1 v2 侧续波——检查赋值语句 178 行拆分） 〔活跃〕
- 第一百八十七轮踩坑（2026-09-15，单位机 ARM64：187-a 规则与统筹落盘轮——三机并行下的「轮次号双占」） 〔活跃〕
- 第一百八十六轮踩坑（2026-09-15，深度机 linux-x86_64：186-a D1 函数级续波——parseClassMember（运算符X）198 行拆分） 〔判据①门禁覆盖·check_fn_length〕
- 第一百八十九轮踩坑（2026-09-15，深度机 linux-x86_64：189-a D1 函数级续波——registerFunction 173 行拆分） 〔活跃〕
- 第一百九十轮踩坑（2026-09-15，深度机 linux-x86_64：190-a D1 函数级续波——computeLiveIntervals 179 行拆分） 〔活跃〕
- 第一百八十八轮踩坑（2026-09-15，单位机 ARM64：188-a D6——B11 变量常量传播；零假阳性优先的判定设计） 〔活跃〕
- 第一百九十一轮踩坑（2026-09-15，深度机 linux-x86_64：191-a D1 函数级续波——visitFunctionDecl 171 行拆分） 〔活跃〕
- 第一百九十二轮踩坑（2026-09-15，深度机 linux-x86_64：192-a D1 v2 侧续波——声明收集 171 行拆分） 〔活跃〕
- 第一百九十四轮踩坑（2026-09-15，深度机 linux-x86_64：194-a D1 v2 侧续波——生成返回语句 171 行拆分） 〔活跃〕
- 第一百九十五轮踩坑（2026-09-15，深度机 linux-x86_64：195-a D1 v2 侧续波——生成字段访问 164 行拆分） 〔活跃〕
- 第一百九十七轮踩坑（2026-09-15，深度机 linux-x86_64：197-a D1 v2 侧续波——检查表达式 171 行拆分） 〔活跃〕
- 第一百九十八轮踩坑（2026-09-15，深度机 linux-x86_64：198-a 尝试——生成结构体整体赋值 170 行拆分→回退） 〔活跃〕
- 第一百九十六轮踩坑（2026-09-15，单位机 ARM64：196-a arm64 寄存器分配接线——批量替换自噬 + 分配器循环携带值） 〔活跃〕
- 第一百九十九轮踩坑（2026-09-15，深度机 linux-x86_64：199-a 生成结构体整体赋值 170 行拆分——预防 183/184 方法论重试成功） 〔活跃〕
- 第两百轮踩坑（2026-09-15，深度机 linux-x86_64：200-a D1 v2 侧续波——生成变量声明 164 行拆分） 〔活跃〕
- 第一百八十三轮踩坑（2026-09-15，家机 win-x64：183-a 波 3 用户类字段泛化重试——uD 根治 + 116 回归定位 + 自赋值守卫） 〔活跃〕
- 第两百零一轮踩坑（2026-09-15，深度机 linux-x86_64：201-a D1 v2 侧续波——解析语句 162 行拆分） 〔活跃〕
- 第两百零一轮·补（2026-09-15，深度机 linux-x86_64：lessons 预防条目编号碰撞审计——文档体系问题呈报） 〔活跃〕
- 第两百零二轮踩坑（2026-09-15，深度机 linux-x86_64：202-a D1 v2 侧续波——发射资源字段释放于地址 156 行拆分） 〔判据①门禁覆盖·check_fn_length〕
- 第两百零三轮踩坑（2026-09-15，深度机 linux-x86_64：203-a D1 v2 侧续波——检查调用表达式 158 行拆分） 〔活跃〕
- 第二百零四轮踩坑（2026-09-15，家机 win-x64：204-a 波 3 ⑤ H7/H11 收口——靶子销项的方法论正向记录） 〔活跃〕
- 第两百零五轮踩坑（2026-09-15，深度机 linux-x86_64：205-a D1 v2 侧续波——解析结构体声明 140 行拆分） 〔活跃〕
- 第两百零六轮踩坑（2026-09-15，深度机 linux-x86_64：206-a 宿主侧 D1——resolveOverload 152 行拆分） 〔活跃〕
- 第两百零七轮踩坑（2026-09-15，深度机 linux-x86_64：207-a 宿主侧 D1——visitRangeForStmt 141 行拆分） 〔活跃〕
- 第两百零八轮踩坑（2026-09-15，深度机 linux-x86_64：208-a D1 v2 侧续波——解析原子 149 行拆分） 〔活跃〕
- 第两百零九轮踩坑（2026-09-15，深度机 linux-x86_64：209-a 宿主侧 D1——handleResultCtor 158 行拆分） 〔活跃〕
- 第两百一十轮踩坑（2026-09-15，深度机 linux-x86_64：210-a D1 v2 侧续波——检查语句 145 行拆分） 〔活跃〕
- 第两百一十一轮踩坑（2026-09-15，深度机 linux-x86_64：211-a D1 v2 侧续波——生成函数 131 行拆分） 〔活跃〕
- 第二百零六轮 b 踩坑（2026-09-15，家机 win-x64：206-b 复制(x) 泛型内置双侧实施——宿主收口+v2 边界登记） 〔活跃〕
- 第两百一十二轮踩坑（2026-09-15，深度机 linux-x86_64：212-a D1 v2 侧续波——生成调用 131 行拆分） 〔活跃〕
- 第两百零六轮 c 踩坑（2026-09-15，家机 win-x64：206-c 波 4 子轮——宿主 H 级缺陷根治 + v2 复制支持 + stdlib 元素级深拷收口） 〔活跃〕
- 第两百一十三轮踩坑（2026-09-15，深度机 linux-x86_64：213-a D1 v2 侧续波——预扫容器声明 131 行拆分） 〔活跃〕
- 第两百一十四轮踩坑（2026-09-15，深度机 linux-x86_64：214-a D1 v2 侧续波——编码类型转换Win 131 行拆分） 〔活跃〕
- 第两百一十五轮踩坑（2026-09-15，深度机 linux-x86_64：215-a D1 v2 侧续波——生成二元运算主体 129 行拆分） 〔活跃〕
- 第两百一十六轮踩坑（2026-09-15，深度机 linux-x86_64：216-a D1 v2 侧续波——编码类型转换X64L 126 行拆分） 〔活跃〕
- 第两百一十七轮踩坑（2026-09-16，深度机 linux-x86_64：217-a 宿主侧 D1——visitReturnStmt 132 行拆分） 〔活跃〕
- 第两百一十八轮踩坑（2026-09-16，深度机 linux-x86_64：218-a 宿主侧 D1——visitImportDecl 127 行拆分） 〔活跃〕
- 第两百一十九轮踩坑（2026-09-16，深度机 linux-x86_64：219-a D1 v2 侧续波——生成方法 129 行拆分） 〔活跃〕
- 第两百二十二轮踩坑（2026-09-15，单位机 ARM64：222-a D7 归因修正 + F1-28 arm64 寄存器分配接线落地） 〔判据①门禁覆盖·check_ascii_idents〕
- 第两百二十轮踩坑（2026-09-16，深度机 linux-x86_64：220-a D1 v2 侧续波——编码对象调用Win 147 行拆分） 〔活跃〕
- 第两百二十一轮踩坑（2026-09-16，深度机 linux-x86_64：221-a D1 v2 侧续波——编码对象调用X64L 140 行拆分） 〔活跃〕
- 第两百二十六轮踩坑（2026-09-15，单位机 ARM64：226-a D8 寄存器感知发射尝试 → 全量门禁拦截回退） 〔活跃〕
- 第两百二十三轮踩坑（2026-09-16，深度机 linux-x86_64：223-a D1 v2 侧续波——代码生成主函数 126 行拆分） 〔活跃〕
- 第两百二十四轮踩坑（2026-09-16，深度机 linux-x86_64：224-a D1 v2 侧续波——发射普通函数调用 119 行拆分） 〔活跃〕
- 插队事故段（2026-09-16，深度机 linux-x86_64：HANDOFF.md 指数膨胀——「追加代替替换」的共享文档写入事故） 〔判据①门禁覆盖·check_handoff〕
- 第两百二十七轮踩坑（2026-09-15，家机 win-x64：227-a F1-29 pass 边界放开——有符号除法强度削减放开 + 浮点恒等式根因查明） 〔活跃〕
- 第两百二十五轮踩坑（2026-09-15，深度机 linux-x86_64：225-a A7 根治专项轮——CopyStruct 8 字节取整写根治 + valgrind 方法论 + A7 本体定位） 〔判据①门禁覆盖·check_handoff〕
- 第两百二十八轮踩坑（2026-09-15，家机 win-x64：228-a D9 专项——常量追踪 + 浮点恒等式放开；i128 折叠根因转依赖 F1-26） 〔活跃〕
- 第两百二十九轮踩坑（2026-09-15，家机 win-x64：229-a F1-30-ASan——构建接入 + 单测面检出） 〔活跃〕
- 三机协作锁模型教训（2026-09-16，用户令：废止多机并行开发——恢复开发锁唯一性·231-a） 〔判据①门禁覆盖·check_handoff〕
- 冲突标记入库与表格断裂踩坑（2026-09-16，深度机 linux-x86_64：233-a 插队修复——plans/021 冲突标记在库多日+六处断表） 〔判据①门禁覆盖·check_handoff〕
- PowerShell 无 BOM 中文脚本的 GBK 误读陷阱（2026-09-15，家机 win-x64：237-a ci.ps1 接入规范覆盖门禁） 〔活跃〕
- 工作区源文件被外部清空事件（2026-09-16，家机 win-x64：239-a 实施期间发现） 〔活跃〕
- 第二百五十二轮踩坑（2026-09-16，单位机 ARM64：252-a stdlib 随机数模块） 〔活跃〕
- plans/021 §3-B 裁决条目留档三犯案（2026-09-16，单位机 ARM64：257-a 插队修复轮——用户质询「已经出现过很多次错误，依然在犯」后立规） 〔判据①门禁覆盖·check_handoff〕
- 258-a 三连缺陷实录：单机面验证漏网×3+生成式脚本中文标识符噪声（家机 win-x64·D31 轮连带） 〔判据①门禁覆盖·check_asm_width〕
- 深度机测试轮门禁绕过实录：shell 管道吃掉门禁退出码（2026-09-16·6c99e62 已带病提交） 〔判据①门禁覆盖·check_handoff〕
- 提交信息写入未来轮次号触发 sync 门禁误报（262-a 轮内实录） 〔判据①门禁覆盖·check_progress_sync〕
- 第 287-a 轮（2026-09-17·家机 win-x64）：T13 根治轮的三条甄别教训 〔活跃〕
- 修复轮（第 292-a 轮：rebase 冲突标记误推事故——管道退出码吞没 + && 链门禁旁路；正则锚点撞同首行；权重 6，2026-09-17） 〔判据①门禁覆盖·check_handoff〕
- M3 采样轮（增量5+B9 扩散轮：设施判据「文本反推/匹配」断裂族三犯——分类判据必须结构化；权重 4，2026-09-17） 〔判据②已修复退役〕
- 304-a 根治轮（波次1 B11/T26：opt 跨块线性序近似=汇合块正确性盲区+层定位方法论；权重 7，2026-09-17） 〔活跃〕
- T39 复验轮（303-a：跨机复验请求捕获收工轮同构回归——复验请求必须真执行；权重 7，2026-09-17） 〔活跃〕
- 306-b v2 树同构轮（311-a：v2 侧三案的等价完备解要按 v2 模型重新设计·权重 6，2026-09-17） 〔活跃〕
- 314-a 采样轮（探针工作目录与关键字撞名+HANDOFF 标题连写盲区·权重 5，2026-09-17） 〔判据①门禁覆盖·check_handoff〕

## 三、归档索引表

| 月份 | 切片 | 内容 | 行数 |
|---|---|---|---|
| 2026-09 | `项目记忆/归档/教训-2026-09.md` | 全部 308 条全文（含高权重 15 条·纯搬运零改写） | 4663 |

> 后续月份滚转：当月新增全文写新月片·本表追加一行。
