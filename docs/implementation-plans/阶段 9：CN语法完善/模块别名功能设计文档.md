# 模块别名功能设计文档

> **设计版本**: v3.0  
> **设计日期**: 2026年1月27日  
> **语法方案**: 方案A - 括号语法  
> **状态**: ✅ 实现完成（包括词法、语法、语义、IR生成、C代码生成）

---

## 📋 设计概述

模块别名功能允许为导入的模块指定一个简短的别名，以简化长模块名的访问。本设计采用括号语法，无需引入新关键字。

### 核心语法

```cn
导入 模块名(别名);
```

**优势**：
- 不需要新增关键字（已删除的"为"关键字）
- 语法简洁直观，类似函数调用
- 与现有 `{ }` 选择性导入语法形成互补
- 括号在词法层已有支持（`CN_TOKEN_LPAREN`/`CN_TOKEN_RPAREN`）

---

## 🎯 设计目标

1. **简化访问**：为长模块名提供短别名
2. **避免冲突**：别名系统需要检测并报告符号冲突
3. **保持一致**：与现有模块系统无缝集成
4. **清晰诊断**：冲突时提供详细的错误信息

---

## 📐 语法规范

### 1. 基本语法

```cn
// 为模块指定别名
导入 非常长的模块名称(短名);

函数 主程序() {
    变量 x = 短名.数据;  // 使用别名访问
    返回 0;
}
```

### 2. 组合使用

**别名与选择性导入组合**：

```cn
导入 模块A(别A);          // 设置别名，不导入成员
导入 模块B { 函数X };    // 导入指定成员

函数 主程序() {
    变量 a = 别A.函数Y();  // 通过别名访问
    变量 b = 函数X();      // 直接使用导入的成员
    返回 0;
}
```

**注意**：别名与选择性导入 `{ }` 是互斥的，不能在同一条导入语句中混用：

```cn
导入 模块A(别名) { 成员1, 成员2 };  // ❌ 错误：语法冲突
```

### 3. 约束规则

- 别名必须是有效的标识符
- 别名不能与现有符号冲突（变量、函数、模块等）
- 同一个别名不能被多次使用
- 别名只是模块的引用，不会自动导入模块成员

---

## 🔧 实现架构

### 整体流程

```
词法分析 → 语法解析 → AST构建 → 语义分析 → 符号表注册
```

### 1. 词法层（Lexer）

**无需修改** - 括号Token已存在：
- `CN_TOKEN_LPAREN` - 左括号 `(`
- `CN_TOKEN_RPAREN` - 右括号 `)`

### 2. 语法层（Parser）

**文件**: `src/frontend/parser/parser.c`

**函数**: `parse_import_stmt`

**解析逻辑**：

```c
// 解析流程
导入 → 模块名(IDENT) → [(别名)]? → [选择性导入]? → ;

// 伪代码
if (current == CN_TOKEN_LPAREN) {
    advance();
    expect(CN_TOKEN_IDENT);      // 别名
    alias = current.lexeme;
    advance();
    expect(CN_TOKEN_RPAREN);
}
```

**修改位置**: L3542-L3572

```c
// 模块别名支持：导入 模块名(别名);
if (parser->current.kind == CN_TOKEN_LPAREN) {
    parser_advance(parser);
    
    if (parser->current.kind != CN_TOKEN_IDENT) {
        report_error("语法错误：缺少别名");
        return NULL;
    }
    
    alias = parser->current.lexeme_begin;
    alias_length = parser->current.lexeme_length;
    parser_advance(parser);
    
    if (!parser_expect(parser, CN_TOKEN_RPAREN)) {
        return NULL;
    }
}
```

### 3. AST层

**文件**: `include/cnlang/frontend/ast.h`

**结构体**: `CnAstImportStmt` (L221-229)

```c
typedef struct CnAstImportStmt {
    const char *module_name;      // 被导入的模块名称
    size_t module_name_length;    // 模块名称长度
    const char *alias;            // 模块别名（NULL表示不使用别名）✅
    size_t alias_length;          // 别名长度 ✅
    CnAstImportMember *members;   // 要导入的成员列表
    size_t member_count;          // 成员数量
} CnAstImportStmt;
```

**说明**: `alias` 和 `alias_length` 字段已存在，无需修改。

### 4. 语义层（Semantic Analysis）

**文件**: `src/semantics/resolution/scope_builder.c`

**函数**: `cn_sem_build_scopes`

**处理逻辑**: L234-259

```c
// 如果指定了别名，则以别名注册模块符号
if (import->alias) {
    // 1. 检查别名冲突
    CnSemSymbol *existing_alias = cn_sem_scope_lookup_shallow(
        global_scope, import->alias, import->alias_length);
    
    if (existing_alias) {
        // 输出详细诊断信息
        char error_msg[512];
        snprintf(error_msg, sizeof(error_msg),
                "语义错误：模块别名 '%s' 已存在（导入模块 '%s' 时发生冲突）",
                alias_name, module_name);
        report_error(error_msg);
        continue;
    }
    
    // 2. 以别名注册模块符号
    CnSemSymbol *alias_sym = cn_sem_scope_insert_symbol(
        global_scope, import->alias, import->alias_length, 
        CN_SEM_SYMBOL_MODULE);
    
    if (alias_sym) {
        // 复制模块符号信息
        alias_sym->type = module_sym->type;
        alias_sym->is_public = module_sym->is_public;
        alias_sym->as.module_scope = module_sym->as.module_scope;
    }
    
    continue;  // 使用别名时不进行成员导入
}
```

**关键改进**: 
- ✅ 详细的冲突诊断信息（包含别名和模块名）
- ✅ 别名符号正确引用模块作用域
- ✅ 使用别名时跳过成员导入

---

## 📊 符号表设计

### 别名符号注册

```
全局作用域 (CN_SEM_SCOPE_GLOBAL)
├── 模块A (CN_SEM_SYMBOL_MODULE)
│   ├── as.module_scope → 模块A的作用域
│   └── type → void
├── 别名X (CN_SEM_SYMBOL_MODULE) ✅ 新增
│   ├── as.module_scope → 指向模块A的作用域（共享）
│   └── type → void
└── ...
```

**设计要点**：
1. 别名符号类型为 `CN_SEM_SYMBOL_MODULE`
2. `as.module_scope` 指向原模块的作用域（共享引用）
3. 别名和原模块名可以同时存在于全局作用域
4. 通过别名访问等同于通过原模块名访问

---

## 🧪 测试策略

### 测试用例设计

#### 1. 正向测试 - `test_module_alias.cn`

```cn
模块 非常长的模块名称 {
    公开: 整数 数据 = 100;
    公开: 函数 计算() { 返回 数据 * 2; }
}

导入 非常长的模块名称(短名);

函数 主程序() {
    变量 x = 短名.数据;      // ✅ 应该成功
    变量 y = 短名.计算();    // ✅ 应该成功
    返回 0;
}
```

**验证点**：
- ✅ 别名成功注册
- ✅ 通过别名访问模块成员
- ✅ 通过别名调用模块函数

#### 2. 冲突测试 - `test_module_alias_conflict.cn`

```cn
模块 模块A { 公开: 整数 数据A = 1; }
模块 模块B { 公开: 整数 数据B = 2; }

整数 别M = 10;
导入 模块A(别M);  // ❌ 应该报错：别名与变量冲突

导入 模块A(共同别名);
导入 模块B(共同别名);  // ❌ 应该报错：别名已存在
```

**验证点**：
- ✅ 检测别名与现有变量冲突
- ✅ 检测重复别名冲突
- ✅ 输出详细错误信息

#### 3. 组合测试 - `test_module_alias_combined.cn`

```cn
导入 配置模块(配置);           // 别名导入
导入 工具模块 { 函数A, 函数B }; // 选择性导入

函数 主程序() {
    变量 v = 配置.版本;         // ✅ 通过别名访问
    变量 a = 函数A();           // ✅ 直接使用导入的成员
    变量 c = 工具模块.函数C();  // ✅ 通过原模块名访问未导入的成员
    返回 0;
}
```

**验证点**：
- ✅ 别名与选择性导入可以共存
- ✅ 别名不影响原模块名的使用
- ✅ 未导入成员仍可通过原模块名访问

---

## ⚠️ 错误场景

### 1. 别名与变量冲突

```cn
整数 短名 = 5;
导入 长模块名(短名);  // ❌ 错误
```

**错误信息**：
```
语义错误：模块别名 '短名' 已存在（导入模块 '长模块名' 时发生冲突）
```

### 2. 重复别名

```cn
导入 模块A(别名);
导入 模块B(别名);  // ❌ 错误
```

**错误信息**：
```
语义错误：模块别名 '别名' 已存在（导入模块 '模块B' 时发生冲突）
```

### 3. 语法错误

```cn
导入 模块A();  // ❌ 错误：缺少别名
```

**错误信息**：
```
语法错误：缺少别名
```

---

## 📝 代码修改清单

### 已修改文件

1. ✅ **语言规范** - `docs/specifications/CN_Language 语言规范草案（核心子集）.md`
   - 更新第6.3节：模块别名语法（L802-851）
   - 更新第6.4节：实现状态标记（L861）

2. ✅ **Parser** - `src/frontend/parser/parser.c`
   - 启用模块别名解析逻辑（L3542-3572）
   - 采用括号语法替代"为"关键字

3. ✅ **语义分析** - `src/semantics/resolution/scope_builder.c`
   - 改进别名冲突诊断信息（L234-259）
   - 添加详细的冲突模块名输出

### 新增文件

4. ✅ **测试示例** - `examples/tests/modules/test_module_alias.cn`
   - 基本别名功能测试

5. ✅ **冲突测试** - `examples/tests/modules/test_module_alias_conflict.cn`
   - 别名冲突场景测试

6. ✅ **组合测试** - `examples/tests/modules/test_module_alias_combined.cn`
   - 别名与选择性导入组合测试

7. ✅ **设计文档** - 本文档

---

## 🚀 后续工作

### 立即可做

1. **单元测试** - 为parser和scope_builder添加C单元测试
2. **集成测试** - 将测试示例加入自动化测试流程
3. **文档完善** - 更新用户指南和API文档

### 未来扩展

1. **跨文件导入** - 支持从外部文件导入模块
2. **别名链** - 考虑是否支持 `别名.子模块(子别名)` 的嵌套别名
3. **IDE支持** - LSP服务器添加别名补全和跳转

---

## 🎓 设计决策记录

### 决策1: 为什么选择括号语法？

**候选方案**：
- 方案A: `导入 模块(别名);` ✅ 已选择
- 方案B: `导入 模块 作为 别名;` ❌ 需要新关键字
- 方案C: `导入 模块 : 别名;` ❌ 冒号语义不清晰

**选择理由**：
1. 无需引入新关键字（"为"已删除，"作为"需新增）
2. 括号在词法层已支持，实现成本低
3. 语法简洁直观，类似函数调用
4. 与 `{ }` 选择性导入形成良好互补

### 决策2: 别名与选择性导入是否可以混用？

**决定**: ❌ 不混用

**理由**：
1. **语义清晰**: 别名用于模块本身，选择性导入用于成员
2. **实现简单**: 两者互斥避免复杂的优先级问题
3. **用户友好**: 避免 `导入 模块(别名) { 成员 }` 的混淆语法

**使用模式**：
```cn
// 模式1：使用别名，访问时需要前缀
导入 模块A(别A);
变量 x = 别A.成员;

// 模式2：选择性导入，直接使用成员
导入 模块B { 成员Y };
变量 y = 成员Y;

// 模式3：两种模式并存（不同导入语句）
导入 模块A(别A);
导入 模块B { 成员Y };
```

### 决策3: 别名是否可以再次被别名？

**决定**: ✅ 可以，但不推荐

**实现**: 别名本身就是模块符号，可以被再次导入：

```cn
模块 原始模块 { ... }
导入 原始模块(别名1);
导入 别名1(别名2);  // ✅ 技术上可行，但不推荐
```

**建议**: 在文档中说明这种用法虽然可行但不推荐，避免维护困难。

---

## 📚 参考资料

- [CN_Language 语言规范草案（核心子集）](../../../specifications/CN_Language%20语言规范草案（核心子集）.md) - 第6.3节
- [精简关键字完成报告](../../阶段%209：CN语法完善/精简关键字完成报告.md) - 模块别名功能背景
- [CN_Language 开发计划](../../../design/CN_Language%20开发计划.md) - 阶段7后模块系统

---

> **文档作者**: Qoder AI  
> **最后更新**: 2026年1月27日  
> **文档版本**: v1.0
