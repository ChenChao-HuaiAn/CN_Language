基于您提供的第二阶段词法分析器接口定义内容，我制定了详细的实现步骤计划。首先，我分析了项目现状：从设计文档中了解了三层架构和编码规范，并通过文件探索确认了`src/core/lexer/`目录存在，表明已有词法分析器代码基础。

以下是词法分析器接口定义的详细实现步骤：

## **总体目标**
在第4周内，完成词法分析器抽象接口的定义、适配器实现和测试验证，使核心层通过统一接口访问词法分析功能。

## **前置条件检查**
1. 确认现有词法分析器代码位置：`src/core/lexer/`目录
2. 确认基础设施层依赖：`CN_dynamic_array.h`和`CN_token.h`头文件存在
3. 建立工作环境：确保构建系统（Makefile）能正常编译现有代码

## **详细实现步骤**

### **任务1：分析现有词法分析器代码（第1-2天）**
**目标**：理解当前实现，识别需要抽象的核心功能

**具体步骤**：
1. **代码审查**：
   - 阅读`src/core/lexer/`目录下所有`.c`和`.h`文件
   - 识别主要数据结构（如`Stru_CN_Lexer_t`、`Stru_CN_Token_t`）
   - 记录现有函数接口（初始化、获取token、状态查询、资源清理）

2. **功能分析**：
   - 列出所有公有函数（可被外部调用的）
   - 列出所有内部辅助函数
   - 分析数据流：输入源→词法分析→token输出

3. **依赖分析**：
   - 确认对基础设施层（如内存管理、动态数组）的依赖
   - 检查是否存在循环依赖或紧耦合

4. **产出文档**：
   ```
   docs/analysis/lexer_current_state.md
   - 现有文件列表和功能描述
   - 关键数据结构定义
   - 函数接口清单
   - 依赖关系图
   ```

### **任务2：定义`Stru_CN_LexerInterface_t`接口（第3-4天）**
**目标**：设计符合SOLID原则的抽象接口

**具体步骤**：
1. **创建接口头文件**：
   - 路径：`src/core/lexer/CN_lexer_interface.h`
   - 遵循项目命名规范：`Stru_CN_LexerInterface_t`
   - 包含防止重复包含的保护宏

2. **设计接口结构**：
   ```c
   // src/core/lexer/CN_lexer_interface.h
   #ifndef CN_LEXER_INTERFACE_H
   #define CN_LEXER_INTERFACE_H
   
   #include "infrastructure/containers/CN_dynamic_array.h"
   #include "core/token/CN_token.h"
   
   // 词法分析器抽象接口
   typedef struct Stru_CN_LexerInterface_t
   {
       // 初始化函数 - 使用源字符串初始化词法分析器
       bool (*initialize)(const char* source, size_t length);
       
       // 核心功能 - 获取下一个token
       Stru_CN_Token_t* (*next_token)(void);
       
       // 状态查询 - 是否还有更多token
       bool (*has_more_tokens)(void);
       
       // 位置信息 - 获取当前行号和列号
       void (*get_position)(size_t* line, size_t* column);
       
       // 源文件信息 - 获取源文件名称（用于错误报告）
       const char* (*get_source_name)(void);
       
       // 重置状态 - 重新开始分析同一源
       void (*reset)(void);
       
       // 资源管理 - 销毁词法分析器实例
       void (*destroy)(void);
       
   } Stru_CN_LexerInterface_t;
   
   #endif // CN_LEXER_INTERFACE_H
   ```

3. **定义配套API函数**：
   - 创建`CN_lexer_interface.c`实现工厂函数
   - 设计接口创建函数：`Stru_CN_LexerInterface_t* F_create_lexer_interface(void)`
   - 考虑支持不同的词法分析器实现（如标准实现、调试实现）

4. **编写接口文档**：
   ```
   docs/api/core/lexer/CN_lexer_interface_API.md
   - 每个函数指针的详细说明（参数、返回值、异常）
   - 使用示例
   - 实现契约（前置条件、后置条件）
   ```

### **任务3：创建接口适配器包装现有实现（第5-7天）**
**目标**：实现适配器模式，将现有代码包装为接口兼容的实现

**具体步骤**：
1. **创建适配器源文件**：
   - 路径：`src/core/lexer/adapter/CN_lexer_adapter.c`
   - 遵循单一职责原则：每个适配器函数不超过50行

2. **实现适配器函数**：
   ```c
   // 适配器内部状态结构
   typedef struct Stru_CN_LexerAdapterState_t
   {
       Stru_CN_Lexer_t* legacy_lexer;  // 包装的旧版词法分析器
       char* source_name;              // 源文件名称缓存
   } Stru_CN_LexerAdapterState_t;
   
   // 实现接口函数
   static bool adapter_initialize(const char* source, size_t length)
   {
       // 调用旧版初始化函数，适配参数
       // 创建并配置内部状态
   }
   
   static Stru_CN_Token_t* adapter_next_token(void)
   {
       // 调用旧版next_token函数
       // 处理可能的API差异
   }
   ```

3. **实现工厂函数**：
   ```c
   Stru_CN_LexerInterface_t* F_create_standard_lexer(void)
   {
       Stru_CN_LexerInterface_t* interface = cn_malloc(sizeof(Stru_CN_LexerInterface_t));
       interface->initialize = adapter_initialize;
       interface->next_token = adapter_next_token;
       // ... 设置其他函数指针
       return interface;
   }
   ```

4. **处理资源管理**：
   - 确保`destroy()`函数正确释放所有资源
   - 实现引用计数或所有权转移机制

### **任务4：编写接口单元测试（第8-10天）**
**目标**：确保接口的可靠性和契约遵守

**具体步骤**：
1. **创建测试目录**：
   ```
   tests/unit/core/lexer/
   ├── test_lexer_interface.c
   ├── test_lexer_adapter.c
   └── test_mock_lexer.c
   ```

2. **设计测试用例**：
   - **接口完整性测试**：验证所有函数指针非NULL
   - **契约测试**：验证前置条件/后置条件
   - **边界测试**：空输入、超长输入、非法字符
   - **内存测试**：内存泄漏检测

3. **实现模拟词法分析器**（用于测试）：
   ```c
   // tests/mocks/mock_lexer.c
   Stru_CN_LexerInterface_t* F_create_mock_lexer(void)
   {
       // 返回预定义token序列的模拟实现
   }
   ```

4. **编写集成测试**：
   ```c
   // 测试接口与适配器的集成
   void test_lexer_adapter_integration(void)
   {
       Stru_CN_LexerInterface_t* lexer = F_create_standard_lexer();
       assert(lexer->initialize("测试代码", 12));
       Stru_CN_Token_t* token = lexer->next_token();
       assert(token != NULL);
       lexer->destroy();
   }
   ```

5. **设置测试覆盖率目标**：
   - 接口函数覆盖率：100%
   - 适配器代码覆盖率：≥90%
   - 分支覆盖率：≥85%

## **文件结构变更**
```
src/core/lexer/
├── CN_lexer_interface.h          # 接口定义（新增）
├── adapter/                      # 适配器实现（新增）
│   ├── CN_lexer_adapter.c
│   └── CN_lexer_adapter.h
├── legacy/                       # 现有代码（可能重构）
│   ├── CN_lexer.c
│   └── CN_lexer.h
└── public/                       # 对外头文件（可选）
    └── CN_lexer.h

tests/unit/core/lexer/
├── test_lexer_interface.c        # 接口测试
├── test_lexer_adapter.c          # 适配器测试
└── mocks/
    └── mock_lexer.c              # 模拟实现
```

## **编码规范检查点**
1. 每个`.c`文件不超过500行（适配器可能拆分为多个文件）
2. 每个函数不超过50行
3. 遵循命名规范：`Stru_`前缀、`F_`前缀、`CN_`常量
4. 使用4空格缩进，Allman括号风格
5. 所有公有函数必须有Doxygen格式注释

## **验收标准**
1. **功能正确性**：
   - 现有测试用例全部通过
   - 新接口能正确处理所有合法输入
   - 错误处理符合规范

2. **架构合规**：
   - 无循环依赖
   - 接口只暴露必要函数
   - 依赖方向正确：核心层→基础设施层

3. **质量指标**：
   - 单元测试覆盖率≥90%
   - 静态分析无严重警告
   - 内存检测工具（如Valgrind）报告无泄漏

4. **文档完整**：
   - 接口API文档齐全
   - 适配器设计文档更新
   - 示例代码可用

## **风险缓解**
1. **现有功能破坏**：保持旧API可用，逐步迁移
2. **性能下降**：适配器增加间接调用开销，通过性能测试监控
3. **接口设计缺陷**：先实现原型，进行设计评审

这个计划遵循了项目的SOLID原则和分层架构规范。实施过程中需要密切关注现有测试的通过情况，确保不破坏现有功能。您对这个计划有什么修改意见吗？