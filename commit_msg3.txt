Feature 2 完整版补完：链表/栈/队列 元素自动析构（injectContainerElemDestroy 泛化）

编译器级注入泛化（2026-08-25，用户批准执行）：
- injectVectorElemDestroy -> injectContainerElemDestroy：向量/链表/栈/队列 统一注入。
- ~类名/清空：全量元素析构循环（idx Alloca 槽 0..元素数量，
  Call T$析构 this=数组基址+idx*typeSizeOf(T)；数组字段 向量/栈=数据、链表/队列=值表）。
- 向量 删除(位置)：单元素析构（守卫 位置<元素数量）。
- 链表 删除头部/删除尾部：单元素析构（守卫 索引<0，读 头/尾索引 字段）。
- 栈/队列 弹出/出队：返回元素=所有权转移给调用方，不析构。
- 仅当元素 T 为有析构类时注入（无析构类保持原语义）。

新增 E2E 116_Feature2_链表栈队列元素析构（链表 删头/删尾/清空 各+1、
栈/队列 弹出不析构、队列清空+2）。115/113 回归 PASS。
全量 E2E 116/118（78/79 已知遗留内存超限）、单测 1196/1196。
plans/014 已更新（三 未完善 移除其余容器析构、五 后续计划 重排）。
