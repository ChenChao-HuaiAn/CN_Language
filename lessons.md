## 编译器缺陷（第七十五轮：v2 深层函数的上下文获取=静态镜像；容器元素释放的三种模型（平铺/链式/交换前缀）；权重 7，2026-09-12）

- [2026-09-12 第七十五轮] **问题类型**: 优化完善（74-a 诚实边界清理 + 相邻缺口），**✅ 当轮闭环（用户裁决方案甲 + 相邻完善全选）**
  - **描述**: ①**v2 侧 `转移(拥有局部)` 入容器未走真 move**（74-a 登记的诚实边界：统一复制、多一次分配）；②宿主污染名单 5 处写入点分散（漏登记风险）；③**映射<K,V> 字符串键/值从不释放**（映射析构只释放四个内部数组；探针验证：值串随 ~映射/清空 泄漏）。
  - **根因**: ①**v2 结构性约束**——`归一化入容器实参` 位于 生成表达式求值（深层），拿不到 `函数信息`（判定「源是否拥有」所需的 字符串变量们/污染们）；穿透需改表达式生成族 12 函数 + 22 调用点签名，风险大于收益（实测静态结构体指针方案被阶段 2 逃逸检查拒绝：不能把局部地址存静态）；②污染名单是释放侧跳过与入容器位「拥有」判定的**共同依据**，分散写入=两向错误风险（误释放借用视图 / 误判拥有=UAF）；③映射只释放内部数组不释放字符串元素——**「容器持有元素」与「元素本身是堆资源」是两个独立的释放义务**，前者做了后者漏。
  - **解决**: ①**污染名单镜像**（v2 惯例三件套：静态 `当前串污染们` + 唯一写入入口 `登记串污染`（5 处收口）+ `是当前串污染` 查询 + `清空函数级槽表` 重置）+ 真 move 分支**三重判定**（转移形态 ∧ 局部槽 ∧ 非参数槽 ∧ 非污染；不满足回落复制）；②宿主 `markStringTainted` 唯一入口（纯重构，行为零变化）；③运行时 `__cn_map_free_strings`（**平铺 0..元素数量**——映射删除用「与末元素交换」→ 有效元素恒为前缀；`freeKeys/freeValues` 按实例化实参编译期决定；释放后槽清零幂等）+ 宿主注入到 `~映射`/`清空` 方法体前（**注入方法体**而非调用点 → 嵌套 `向量<映射<…,字符串>>` 的元素析构自动受益）。
  - **成效与防线**: 真 move 分配增量 **0**（零拷贝实测）；借用形参转移走复制（安全方向：调用方串经搅动完好）；映射三形态（析构/清空/嵌套）校验 7/7；**E2E 218 五→七形态折 127、E2E 219 六→七形态折 127**（后者=74-a 诚实边界的正式门禁：v2 真 move 若回退为复制 → 分配增量≠0 → 折位丢失 → 对照失败）。验证链：清场重建零警告 + 单测 1289/1289 + E2E 241 用例 236 过（唯一败=194 既有）+ 固定点 **271876 行**（新口径）逐字节一致 + 绑定自检 + 对拍 29/29 + 释放路径对照。
  - **预防**: **㉒v2 深层函数需要函数级上下文时，优先「静态镜像 + 唯一写入入口 + 函数级重置」而非穿透参数**——成本差一个数量级（数处 vs 数十处），但**前提是权威列表单调写入且无截断语义**（污染名单满足；字符串变量们有块级截断→不适合镜像）；选镜像必须写清「**漏项方向是否保守**」：漏项=误判拥有=UAF（不保守）→ 必须强制唯一入口 + 门禁用例反证；**㉓「容器持有元素」与「元素是堆资源」是两个独立释放义务**——只释放容器内部数组≠释放元素本身（映射/嵌套容器均如此）；**㉔新增容器类型先确认删除策略再选释放遍历方式**——平铺数组（向量/栈）/链式槽（链表/队列，出队已转移所有权给调用方）/交换式删除的前缀数组（映射，平铺安全）；**㉕释放注入放「容器方法体」而非「调用点」**——方法体注入覆盖面天然含全部持有者（嵌套元素析构链、字段容器），调用点插入需枚举持有者（易漏）。
  - **诚实边界**: 映射 `删除(键)` 路径被删项字符串不释放（交换式删除覆盖，需 stdlib 改动 + 新注入辅助，与 `向量.析构元素` 同模式）= 后续靶子；数组元素/装箱字段/结构体字段持串面无 RAII（既有）；探针 75 系列留 /tmp/p75probe* 不入库。
  - **权重**: 7（优化完善 7 × 预防价值1.4 × 当轮根治0.85）

## 编译器缺陷（第七十四轮：容器「无条件释放元素」×「入容器不接管所有权」=一族静默缺陷；链式容器不能平铺释放；v2 后缀序节点定位；权重 8，2026-09-11）

- [2026-09-11 第七十四轮] **问题类型**: 编译器缺陷（容器元素所有权机制级矛盾，五枚形态）+ v2 侧防线不对称，**✅ 当轮根治（74-a 方案A：用户未答复呈报→按 AGENTS.md 第3项纪律自行裁决）**
  - **描述**: 按 plans/020 矩阵靶子 #1「入容器编译器级自动 copy/move」立案，探针（全部可复现）实测五枚形态：
    ①**借用句柄入容器 → 容器析构释放调用方拥有的串（跨函数 UAF）**——探针 74-L：`存入(表, 本地)`（形参直传入容器）后，容器块退出时打印「容器销毁后 本地」输出乱码；
    ②**局部拥有串裸标识符入容器 → 源 RAII 释放 + 元素浅共享=悬垂**——探针 74-A：`表.追加(值)` 后 3 元素内容全乱码（搅动后仍损坏）；
    ③**`转移(源)` 入容器非真 move**——源槽未清零，源出口 `__cn_str_free` 释放已移交句柄，探针 74-J 元素内容乱码；
    ④**v2 侧容器元素从不释放**（防线不对称）——v2 无任何元素释放机制（宿主有 `__cn_vector_free_strings`），探针 74-G 残留 2（两个 `字符串复制` 元素）；
    ⑤**链表/栈/队列元素串从不释放**——宿主只覆盖 `向量`，探针 74-N：链/栈/队各残留 1。
  - **根因**: **「容器析构无条件释放元素串」（假定「元素所有权归容器」）与「入容器位不接管所有权」（实参句柄被浅存）是一对必须同时成立或同时不成立的假设**——两个半截机制各自「看起来正常」：容器析构的逐元素 free 独立看是 RAII 正确实现，入容器位的浅存独立看是「CN 值语义深拷贝」的自然形态；合起来则产生悬垂/双释放/UAF。修任一侧的补丁都不能使假设一致（宿主 72-a 的 A2 人工纪律「字符串入容器须显式复制」是**约定不是门禁**，故静默失效——全树 4 处裸标识符入容器全部是字符串形参，即最危险的借用来源）。v2 侧同族缺口的根源是 72-a/73-a 只做了释放侧对齐，未做「写入侧约定」对齐（释放侧的假设没有对应的写入侧建立点）。附带两枚 v2 特有实施不变量：**调用发射时序**（v2 调用展开是「收集 IR_调用实参，遇 IR_调用参数化 统一展开」——插入式调用须在收集之前发射，否则其 IR_调用参数化 把已收集实参一并展开）、**节点池后缀序**（实参根节点在调用节点前一位置，不可用 子起点+1+序——复合实参时指向最内层子节点，判定失真，探针 74-H 残留 3 实证）。
  - **解决**: 按 Rust `Vec<String>` 语义（`push(s)`=move／`push(s.clone())`=编译器代写深拷贝／字面量=驻留借用）做**入容器位所有权归一化**——宿主 `buildCallArgsForMethod`+`normalizeContainerInsertArg` 按实参来源分级（字面量→驻留借用零复制；调用返回（拥有契约）→直接接管零拷贝；`转移(拥有局部)`→真 move（句柄直存+源槽清零）零拷贝；其余（标识符/成员/下标/借出）→`__cn_str_copy` 落堆）；v2 侧 `归一化入容器实参` 同构（`转移` 分支因拿不到 函数信息 统一走复制=诚实边界）；**释放面按元素模型分派**——向量/栈=`__cn_vector_free_strings`（平铺 0..计数），链表/队列=新增 `__cn_chain_free_strings`（**按链游**：出队/删除头部已把元素所有权转移给调用方，槽序号可能 < 元素数量，平铺释放会误释放已移交的串）；两辅助均含**元素槽清零**幂等不变量；单点事实源 `emitContainerElemFreeFor`（宿主）/`发射容器元素释放`（v2）统三处释放路径（块出口析构/容器析构/容器字段级联）。
  - **成效与防线**: E2E **218** 校验和 **24→31**（五形态全对；含四容器释放面残留归零）；真 move 零拷贝实证（预分配后分配增量 **0**，深拷贝增量 1，源仍可用）；**E2E 219 双编译对照两侧退出码 63 一致**（移植纪律 9 第二个对照用例：六形态含借用来源/借出视图/元素守恒/四容器释放面）；生成物层两侧均有平铺+链式元素释放调用（宿主 20+7 / v2 13+2）。验证链：清场重建零警告 + 单测 1289/1289 + E2E **241 用例 236 过**（唯一败=194 既有待批）/4 跳 + 固定点 **271056 行**（新口径）逐字节一致 + 绑定自检（cn_main=0xf6015 ∈ .text[0,0xf9fa8)，readelf 实证）+ 运行级 3/3 + 对拍 29/29 + 释放路径两侧对照脚本三列核对。
  - **预防**: **⑱「A 处假定 X、B 处负责建立 X」的分工必须先写清「X 由谁建立/在哪一行建立/建立失败会怎样」三问**——本轮两个半截机制（释放侧假定元素归容器、写入侧未接管所有权）各自独立看都正确，合起来是一族静默缺陷（悬垂/双释放/UAF，内容测试全部抓不到）；**释放侧的假设必须与写入侧的约定成对出现**（plans/020 移植纪律 7「这个机制假设的另一半在哪里？」的又一实例）；**⑲容器元素释放必须按元素模型分派，不能按计数平铺**——凡「槽序号空间 ≠ 有效元素集合」的容器（链式：出队/删除头部已转移所有权给调用方）一律按链游/有效枚举释放；**新增容器类型时先问「有效元素怎么枚举」**，释放辅助的参数表就是元素模型的声明；**⑳v2 侧实施前先确认两个不变量**——调用发射时序（插入式调用须在 IR_调用实参 收集之前）+ 节点池后缀序（实参**根**节点在调用节点前一位置，不是 子起点+1+序）；判定失真的表现常是「多一次操作」而非崩溃，只能靠内存差值探针发现；**㉑人工纪律不是门禁**——A2「字符串入容器须显式复制」是文档约定，编译器不强制 → 静默失效；凡「约定式安全」都要问「编译器能不能强制」，能则落为编译器机制（本轮把约定升级为编译器接管所有权）。
  - **诚实边界**: v2 侧 `转移(拥有局部)` 入容器**未走真 move**（判定源是否拥有需 函数信息.字符串变量们/污染们，穿透需改表达式生成族 12 函数+22 调用点签名，风险大于收益）——统一复制，语义安全等价（容器独立拥有、源照常释放，无悬垂无泄漏），仅多一次分配，真 move 优化登记后续项；嵌套容器元素（`向量<映射<整64,字符串>>`）不释放元素串（元素是容器对象，按 char* 语义释放对象指针会损坏堆）；数组元素/装箱字段借用面维持既有边界；探针 74 系列留 /tmp/p74probe 不入库。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）

## 编译器缺陷（第七十二轮：符号名模式判型=跨模块整体失效；SysV 变参 AL 契约；转移/引用出参 UAF 三形态；权重 8，2026-09-11）

- [2026-09-11 第七十二轮] **问题类型**: 编译器缺陷（拥有型字符串 RAII 五枚静默缺陷）+ 规划处置（F3 回退呈报），**✅ 五枚当轮根治（F1/F2/F4/F5/F6），F3 回退待裁**
  - **描述**: 按 HANDOFF「4'-三 收尾评估」逐项实测（写探针跑真实链路，不读代码推断）发现：
    ①**F1 跨模块拥有契约失效（静默永久泄漏）**——`semantic_call.cpp` 用 `sigKey.find('$')` 区分「泛型单态化产物」，但跨模块函数链接键也是 `模块$名` 形态（functionLinkKey 公式）→ 凡导入模块的函数返回 字符串，调用方一律不登记 RAII（IR 对照：同文件 `调用 [本地造串]` 有 free，跨模块 `调用 [工具$造串]` 无 free；运行级：stdlib `替换` 跨模块调用活动分配残留 1）。影响面=81 个含导入 E2E 用例 + stdlib 全部 `-> 字符串` 函数。
    ②**F4 赋值位转移非真 move（UAF）**——`目标 = 转移(源)` 原实现当普通右值（实参值加载）→ 目标/源共享同一堆句柄，源仍在拥有名单中于出口释放 → 目标悬垂（stdlib `字符串扩展.cn:72` `缓冲 = 转移(拼接)` 实测输出乱码，被 E2E 用例目录内的旧副本掩盖）。
    ③**F5 引用参数出参悬垂（UAF）**——`字符串& 输出` 写入本地拥有串后本函数出口释放源局部（探针 `输出 = 甲;` 修复前读「73」；v2 树 解析货舱/拆限定名 两函数踩中，被 F1 修复暴露成 E2E 165 回归）。
    ④**F6 变参调用未设 AL（SysV ABI 违约）**——AMD64 SysV 要求变参调用点 AL=使用的向量寄存器个数（glibc `va_arg` 据此判定 xmm 保存区），CN x64 代码生成从未发射 → 基线靠 rax 残留侥幸（`格式化("¥%.2f", 29.50)` 经包装函数输出 ¥0.00、直连形态正确）。
    ⑤**F2 v2 落空路径零释放（永久泄漏）**——v2 只在 返回 语句路径发射释放，体末无 返回 语句（fall-through）的函数完全不释放（探针 asm 无 `__cn_str_free`）；Rust 语义=函数体末尾隐式 drop。
  - **根因**: ①**符号名模式判型**（`$` 既是泛型实例分隔符又是跨模块链接键分隔符——同一字符两义，模式匹配必然误伤）；②**转移语义只实现在声明位**（阶段3b 浅交接是 `生成变量声明` 分支，赋值位/表达式位留「值加载」形态=共享句柄；Rust 对照：`a = b` 对 String 本身就是 move）；③**引用参数写回未定所有权**（写入调用方槽后源局部是否仍释放=未定义；Rust `*out = s` 是 move）；④**C ABI 变参契约缺失**（clang/gcc 变参调用点恒有 `xor eax,eax`/`mov al,imm`）；⑤**v2 RAII 出口覆盖面遗漏**（返回路径三出口都补了，fall-through 尾块漏）。
  - **解决**: F1 改按 `genericFuncInstances_` 精确判定（rustc DefId 同款）+ v2 侧三处纯名查改 `查函数返回回退` 键基回退版；F4 赋值位真 move 分支（drop 旧目标+句柄直存+源槽清零；自转移豁免）；F5 引用出参 move（句柄直写调用方槽+源槽清零，污染名不转移）；F6 调用点统计向量实参个数发 `mov eax, N`；F2 `生成函数` 体块未终止时在函数尾块前发射出口释放（**首版无条件发射 → 196_v2/210_v2/203_v2/208_v2/123_v2 等十用例回归=已返回路径二次释放**，条件化后零回归）；E2E 212 两新用例固化；门禁全绿（清场重建零警告+单测 1289+E2E 234 用例 229 过唯一败=194 既有+固定点 266964 逐字节一致+对拍 29/29）。
  - **预防**: **①同一符号/字符承载两义（`$`=泛型实例分隔符 vs 跨模块链接键分隔符）时判型必须查权威表（实例表/DefId），禁止符号名模式匹配**——模式匹配在「新增第三种用法」时静默失效，且失效方向是「不登记 RAII」这类静默泄漏；**②「显式 move/赋值语义」必须在全部位置（声明位/赋值位/返回位/传参位/引用出参位）逐一确认拥有归属**——写全一处就以为写全了是漏网主因；**③C ABI 契约（变参 AL/调用约定/栈对齐）属「调用点发射」必查清单**——新增运行时函数（尤其 C 变参）逐项核对 ABI 手册，不靠实测侥幸（「能跑」=rax 残留值恰好正确属未定义行为）；**④RAII 出口覆盖面按「函数所有控制流出口」枚举（返回/落空 fall-through/中断/继续）**，每出口逐一确认；**⑤修复改变既有测试断言的语义（时机/计数）时，按「测试文件不可擅自修改」纪律回退并呈报**——不得为实现新语义自行改测试期望（F3 处置范本）；**⑥宿主与 v2 双侧同族的缺口要一次修全**（F1/F2 都是双侧同缺，只修一侧=下一次回归）。
  - **F3 落地（同轮下半程，用户裁决方案A）**: 宿主块级作用域 RAII 实施（作用域基线栈+genBlock 出口析构+中断/继续 drop-on-jump，**free+槽清零幂等模型**杜绝多路径双重释放）+**连带根治 v1 树「字符串入容器浅共享=元素悬垂」既有缺陷**（6 处追加点按 A2 纪律显式复制；76 自举用例产物常量池退化为指针值实证；F3 关闭时探针同样悬垂=既有缺陷甄别）+用例期望同步（115/116，用户批准）。
  - **F3 追加预防**: **⑦「新语义使旧潜伏缺陷暴露」时先甄别既有 vs 新引入**——判据=关闭新特性复测（F3 关时探针 86 元素同样损坏=既有缺陷，只是内存未复用侥幸；若关后正常则是新引入的回归）；**⑧块级/函数级多路径释放共享槽时用「释放+清零」幂等模型**，而非「谁释放谁登记」的排除法（后者在条件分支/跳出/兜底三路径交织时必然出现两次释放）；**⑨测试期望的更新必须用户批准且逐条核对语义**（115 的 6=向量操作 3+块出口 3 逐项可解释，不是"跑出什么写什么"）。
  - **72-a 收尾（2026-09-11 下半程收尾轮，用户裁决「瑕疵清理+三层防线全部实施」）**: 核实上轮工作后静态审查揪出 4+1 瑕疵（`(void)0` 死代码/`LoopContext::scopeDepth` 命名失准实际为类名单基线/genVarDecl 注释与代码不符/泛型出口状态清理不对称/switch 中断无 drop-on-jump）当轮清理；清理中静态推演发现**类方法返回路径真缺陷**——`emitClassMethod` 为三类函数级生成单元（visitFunctionDecl/emitGenericFuncInstance/emitClassMethod）中独漏函数级兜底者，方法体内声明拥有串后中途 返回 绕过 genBlock 出口析构=泄漏（探针 87 修复前实证残留 1，补 genStringFrees/genClassDestructorCalls 同构收尾后归零）；探针转正 E2E 213（块级 RAII 八形态残留全零）/214（容器元素所有权搅动）；plans/020 拥有权流转矩阵建档。
  - **收尾追加预防**: **⑩探针实测与静态审查两条腿缺一不可**——探针测得出泄漏/悬垂/残留（活动分配数差值量化），测不出死代码/命名失准/注释与实现不符/状态清理不对称（不影响运行行为但误导下一个读代码的 AI，本轮类方法真缺陷恰由静态审查的推演引出、再由探针 87 实证闭环）；探针全绿≠diff 干净，每轮修改后的 diff 审读不可省；**⑪「对称补齐」前先核验生成单元的嵌套关系**——visitFunctionDecl 出口 clear 的状态，泛型实例（visitProgram 第 4 步批量生成、不嵌套）可照抄补齐，但若单元是嵌套生成（如泛型若嵌套于宿主函数体内）照抄 clear 反而清掉宿主活状态（stringTainted_ 跨函数共享即此类）；对称性判断以调用时机实证为准，不以「长得像」为准；**⑫文档声明与实现不符时以探针实测为准并回改文档**（块级析构实现实际覆盖无析构类的内存释放，比 HANDOFF 诚实边界的「限有析构函数类」声明更完整——声明落后于实现同样是文档缺陷）；**⑬写 CN 代码严防 C++ 习惯带入**——类/结构体/枚举体后 `}` 自终结**不加 分号**（规范 03 规则1），本轮连踩两次（探针 87 与 E2E 213 均因 `};` 报「预期顶层声明」）；写 CN 类型定义后自查该点。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）

## 编译器缺陷（第七十三轮：v2 块级 RAII 从未生效=「一次性预扫 + 生成期基线」互斥；多路径共享槽不清零=双释 SIGSEGV；权重 8，2026-09-11）

- [2026-09-11 第七十三轮] **问题类型**: 编译器缺陷（v2 侧两枚 P0：块级作用域 RAII 静默失效 + 跳出路径无 drop-on-jump），**✅ 当轮根治（73-a 方案A 用户裁决）**
  - **描述**: 按 plans/020 矩阵第 27 格靶子（「v2 侧跳出路径 drop-on-jump 专项核验」）实测，核验中揪出更深两层：
    ①**缺陷甲（P0）v2 块级 RAII 从未生效**——`预扫容器声明` 在函数体生成**之前**做函数级一次性扫描，把函数内所有（含全部嵌套块）拥有型容器/字符串登记进释放名单；`生成块` 入口取基线 `= 名单.大小()`，出口只释放「基线之后新增项」→ 进块时名单**早已包含**本块变量，基线恒等于边界，**永远无新增项可释**（诊断注入实证：`[诊断] 登记拥有串: 体内` 出现在预扫阶段、随后 `块出口析构: 字符串基线=1 现名单=1` 零释放）。探针量化：20 万轮循环体拥有串残留 **199,999**（继续/自然回边/如果体 三形态同，即**不限跳出路径**）；30000 轮 ×14.4KB 泄漏上限 411MB（内存上限下 OOM）。
    ②**缺陷乙（P0）中断/继续 无 drop-on-jump**——`生成语句` 两分支只发跳转不释放（宿主 72-a 已有 `genJumpDestructFrom`）；asm 实证：跳出路径无 `__cn_str_free`，自然路径每轮都有。
    ③**修复首版自身缺陷（当轮实证闭环）**：登记时机+跳出释放落地后，容器跳出形态 **SIGSEGV**——跳出释放与函数级兜底各释放一次（gdb 栈 `free <- cn_free <- ~向量 <- bb0_4`），根因=「编译期名单不截断 + 释放点不清零」。
  - **根因**: ①**「一次性预扫 + 生成期基线」两种设计共存时预扫污染了基线依据**——预扫登记先于生成，使「基线 = 当时名单大小」的语义失效（基线机制的前提是「名单随生成进度增长」）；这类失效**静默**（编译通过、运行不崩、只是不释放）；②**RAII 出口覆盖面按路径枚举时漏了跳出路径**（宿主 72-a 补齐，v2 侧未同步——双侧同族缺口只修一侧是复发主因，lessons 第七十二轮预防⑥同族）；③**多路径共享槽不清零**——不截断名单意味着同一变量会被「跳出释放」与「函数级兜底」**两条路径**经过，若释放后不清零，第二次就是 double free（宿主 72-a 用「释放+清零」幂等模型已给出范式，v2 未同步）。
  - **解决**: ①**登记时机对齐宿主**——预扫职责收敛为「槽分配 + 入口零初始化 + 类型登记」，名单追加移至生成期（`生成变量声明` 字符串拥有化初始化后 / `生成容器局部` 非返回接收分支）+ 新增 `名单追加去重`（v2 局部槽=函数级映射，同名合槽须去重）；②**跳出 drop-on-jump**——`函数IR` 新增三枚循环体基线（`生成当` 进入体块前记录/退出后恢复，嵌套循环自然正确）+ 新增 `循环跳出析构`（按最近循环体基线释放，编译期名单**不截断**——同块内互斥路径（如「如果{中断}」与自然结束）各自仍须发射释放，截断会让另一路径无变量可释）；③**「释放+清零」幂等模型**——`函数IR.零寄存器`（预扫回填，函数入口常量 0 的寄存器号）+ 三处释放点（块出口析构容器/字符串段、结构体容器字段级联）释放后槽清零（容器字段须先另存字段地址寄存器再清零——加载会把值寄存器指向对象）；④`块出口析构` 新增 `截断` 参数（块出口=真/跳出=假，不截断模式遍历用下标递减防死循环）；⑤连带 v2 内置表补 `内存::活动分配数`（E2E 断言口径）。
  - **成效与防线**: 五形态（继续/中断/自然回边/如果体/容器跳出）20 万轮级残留 **199,999 → 0**（归零闭环，LD_PRELOAD 读 `__cn_alloc_live`）；v2p 编译 v2 树退出残留 2,755,596 → 2,757,891（同量级=编译器自身常驻结构）；**E2E 216_v2_块级作用域RAII内存回归**（七形态差值恒 0 + 内容校验，退出码 127）转正为门禁（宿主 213 的 v2 对应件，两侧防线自此对称）；悬垂风险扫描 = 0 命中（全树「块内声明→容器追加/静态存储/返回」零处，v2 树已按 A2 纪律迁移）。验证链：清场重建零警告 + 单测 1289/1289 + E2E **238 用例 233 过**（唯一败=194 既有待批）/4 跳 + 固定点 **266845**（新口径）逐字节一致 + 对拍 29/29。
  - **预防**: **⑭「一次性预扫 + 生成期基线」共存时，预扫不得触碰基线所依赖的列表**——基线机制的前提是「列表随生成进度增长」，预扫提前登记会让基线恒等而静默失效；举凡「预扫做 A、生成期读 A 的大小作界」的设计，先问「A 的增长时机与读者时机谁先」；**⑮修释放路径先问「同一槽/同一名单会被几条路径经过」**——多条路径共享时必须选「截断名单」或「释放后清零」之一（宿主 72-a / v2 73-a 现同为「释放+清零」幂等模型），否则双释 SIGSEGV（本轮首版实证）；**⑯旧修复未同步到对侧即复发**——宿主 72-a 的 drop-on-jump 与幂等模型，v2 侧隔一轮才补（双侧同缺的教训第七十二轮预防⑥已载，仍需按「修复台账逐侧核对」执行，勿因单侧绿就收工）；**⑰修复后暴露的失败必须做「修复前版本同形态实测」三方对照**（本轮结构体容器字段「声明即用」形态：修复前 v2p / 修复后 v2p / 宿主**均失败** → 判定既有边界，非本轮引入；不此对照就会把既有边界误记为本轮回归或反之）。
  - **诚实边界**: 结构体容器字段「声明即用」（`货架 s; s.表.追加(...)`）报「空指针解引用」=既有边界（三方对照实证，字段未自动构造），靶子归属「结构体字段初始化 / NLL 轮」；装箱字段/数组元素/静态面无 RAII（plans/020 矩阵靶子维持）；v2 侧「返回接收形态」不登记释放（原预扫豁免语义原样搬移，行为不变）。
  - **移植纪律订立（plans/020 第三节三-B，用户要求入册）**: 本轮把可复用防护提炼为 4 条强制自检（改动所有权/释放机制时逐条执行）——**⑦移植清单两件套**（写「机制主体 + 依赖不变量」两行，不变量逐条验证，禁止凭「看起来像冗余」跳过→73-a 首版 SIGSEGV 根因）；**⑧释放路径两侧对照**（`scripts/cn_release_parity.sh` 拉两侧释放挂点清单，人工核对「时机/条件/不变量」三列，脚本含清零幂等两测计数报警）；**⑨同结构双编译器对照（已门禁化：E2E 217 + runner「双编译对照」编排）**——同一源码两侧各编各跑、断言退出码一致（宿主侧锚定用例期望值防两侧同错，v2 侧以宿主实测值为期望=等价性判据）；用例目录放 `双编译对照.txt` 即生效（零 runner 注册）；反证实证=注入「缺 drop-on-jump」「释放点缺清零」两形态退出码 31→0 均被捕获；手工手法（`grep` 释放调用计数 + `LD_PRELOAD` 读 `__cn_alloc_live`）保留为补充场景；防线不对称=缺陷候选；文档声明须带验证状态；**⑩两侧同缺一次修全**（预防⑥已载仍复发，故落为当轮动作）。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 当轮根治0.85）

## 工程纪律（第七十一轮：一句话交接任务的范围核验——「move 对齐」实为构造器能力整体缺失；注释式二分禁用=清零行动的盲区形态，2026-09-11）

- [2026-09-11 第七十一轮] **问题类型**: 工程纪律（交接范围核验+二分残留复审盲区，权重 7）**✅ 当轮呈报根治（71-a 方案A 用户裁决+71-b 恢复）**
  - **描述**: ①HANDOFF 下一轮任务#1「v2 侧装箱 move 对齐」按字面理解为 move 语义微调——探针实证 v2 对规范 07-五 内置构造器（正常/错误/某些）**整体缺失**（`返回 正常(行)` 语义层报「未声明函数」中止），move 语义只是缺失链的最末一环；②`生成语句.cn` 引用返回路径的 发射字符串释放 被「// 二分禁用」注释禁用（第六十七轮 910e234 带入）——第七十轮「全树 grep false && 复审清零」对**注释式禁用**天然盲区（删掉调用+留注释，grep 任何标记词都查不到「被禁用的调用」本身）。
  - **根因**: ①交接文档的一句话结论（上轮 AI 对 v2 现状的推断「签名表查不到=保守不登记」）未经探针核验就被当作任务边界——推断描述的是「若 v2 支持装箱」的假想态，实际 v2 在语义层就拒绝；②二分禁用的形态有 `false &&` 前缀/注释掉调用/#if 0 等多种，清零行动只 grep 了第一种。
  - **解决**: ①范围核验后按缺陷纪律呈报三方案（A 全链+move/B 补全+保守污染/C 仅返回位），用户裁决 A——语义层构造器识别+语境类型推导（写入节点空闲字段）+IR 装箱发射（零新指令）+retbuf 返回 ABI（三后端 prologue 判定）+函数级 已装箱名 跳过模型（出口三处释放/析构跳过=move，优于宿主源槽清零：双装箱退化借用而非 NULL）+E2E 211_v2 八形态；②恢复被禁调用+全树 grep「二分禁用/false &&」双形态复审清零；③验证链全绿（单测 1289+E2E 227/232 唯一败=194 既有+固定点 266459 新口径+对拍 29/29）。
  - **预防**: **①接手交接文档的一句话任务，动手前先用最小探针核验任务边界——上轮的机制描述是推断不是实证，「能力缺失」与「能力存在但有缺口」的修复方案与工作量天差地别（呈报裁决的方案才有正确前提）**；②二分残留复审必须枚举禁用形态（false&& 前缀/注释掉调用/#if 0/空行替代），最可靠的是**对「声明了意图的注释」与「实际代码」做一致性核对**（注释说要释放而循环体里没有调用=红旗），grep 关键词只是初筛；③v2 侧新增「接收侧已有、发射侧缺失」的不对称能力时，先把接收侧的布局/ABI 常量（结果值偏移/结果大小）直接复用为发射侧常量——双侧一致性靠共享函数而非双侧各写一份。
  - **权重**: 7（工程纪律7 × 预防价值1.4 × 当轮根治0.85）

## 工程纪律（第七十轮：二分禁用残留的复审纪律+装箱浅共享×局部 RAII=悬垂家族第五实例，2026-09-11）

- [2026-09-11 第七十轮] **问题类型**: 工程纪律（二分残留复审）+设计缺陷（装箱 move 语义缺失，权重 8）**✅ 当轮根治（70-a 立案→用户裁决方案A→全链落地）**
  - **描述**: ①宿主 A2「返回 字符串=拥有」契约（semantic_call.cpp 普通函数决议路径 retOwnedString）在轮1 排查多文件回归时被二分禁用（`false &&` 前缀），真根因（IR容器 字符串入容器浅共享悬垂，c668919）另行根治后**该行忘了恢复**即提交（0ed437b）——v2 侧同款契约活着，双侧语义分叉（宿主=借用泄漏/v2=登记释放），被「三树全显式化迁移」掩盖（E2E 全绿+固定点一致都不触发分叉路径）；②恢复契约后连环暴露两枚潜伏缺陷：`系统::参数` 返回 argv 借用但签名 字符串（类型谎言，3 调用点 free=悬垂）+`取语义诊断` 返回容器元素借用（B 类呈报列了但轮1 漏修）；③修复后 v2p 仍空转（读源文件 全空）——真根因=「装箱浅共享×局部 RAII 释放=悬垂」家族**第五实例**：stdlib 读取行包装 `字符串 行 = 文件::读取文件行(句柄); ... 返回 正常(行);`——装箱（结果构造器 StorePtr）浅拷字符串指针入联合体字段，genStringFrees 移出识别只认「返回值=裸 Load 槽」不认装箱形态→返回块 free 局部→装箱值悬垂。
  - **根因**: ①二分调试的禁用开关散落源码各处，修复真根因后无「全树 grep 复审清零」步骤（c668919 只修了 v2 侧假&&，宿主侧同族漏网）；②A2 拥有契约启用面（返回类型=字符串）与「返回值经装箱/容器/结构体转交」的所有权重叠，浅共享×RAII 的悬垂家族在**每个新启用面**都会再演；③契约恢复=行为变化，其验证必须是「自举链运行级」（v2p 编译 v2 树节点数）而非仅 E2E（存量用例已显式化=盲区）。
  - **解决**: ①恢复契约一行+全树 grep `false &&` 复审清零；②系统::参数 双侧改 字符*（内置注册+IR布局 内置表）+stdlib 包装显式复制+调用点适配（v2 主.cn 3 处+tests 37/38 六处）+取语义诊断 返回位显式复制；③**装箱 move 语义**（ir_error_ctor.cpp）：`正常/某些(拥有字符串局部)` 值字段写入后**源槽清零**（转移() 浅交接同模型，Rust Ok(s) move 同款零拷贝）——genStringFrees 对已清零槽空安全跳过，根治悬垂且零运行时开销；④验证链全绿（v2p 复活 57054 节点+单测 1289+E2E 唯一败=194 既有+固定点 262321 新口径逐字节一致+对拍 29/29）。
  - **预防**: **①二分调试的任何禁用标记（false &&/注释块/开关）必须在根因修复后全树 grep 复审清零——禁用是分布式的，修复是单点的，二者天然不对账**；②新启用任何 RAII/所有权面时，必须枚举「值经装箱/容器/结构体/返回转交」的全部分派点并逐个定拥有语义（move/copy/借用）——浅共享默认=悬垂预备役；③契约类语义开关的验证载体=自举链运行级（两代编译器行为对拍），存量测试全显式化时它是唯一能暴露分叉的探针。
  - **权重**: 8（工程纪律8 × 预防价值1.4 × 当轮根治0.85）

## 工程纪律（第六十八轮：未提交工作树上的批量 git checkout=迁移成果全失+中间态二进制调试的时间黑洞，2026-09-11）

- [2026-09-11 第六十八轮] **问题类型**: 工程纪律（git 操作纪律+调试基线纪律，权重 8）**✅ 当轮恢复（脚本化重做）**
  - **描述**: ①v2 树迁移 96 处（未提交工作树改动）在「asm 函数级二分」调试中被二分脚本的 `git checkout -- CN语言编译器v2`（当恢复机制用）**整树丢弃**——stash 已 pop=无备份，迭代脚本+行级清单在对话中才得以 30 分钟重做；②多轮「好/坏二进制」对比实验（v2p_g 坏/v2p_i 好）中，构建非确定性（cmake --build 未查退出码+部分编译失败被 `grep -c " error"` 掩盖+产物构建于宿主中间态）导致「同源码不同产物」的假象，二分结论（IR容器.cn）在重做后不可复现——状态漂移下消耗 2 小时+。
  - **根因**: ①对未提交工作树使用破坏性 git 命令（checkout -- =丢弃而非恢复）；②调试实验未固化「源码态↔二进制态」的一一对应（每次实验后宿主/树状态未回基点，产物归属漂移）；③构建验证只 grep 错误串不查 rc（并行构建的部分失败形态）。
  - **解决**: ①迁移重做（迭代红名单驱动脚本+手工清单）；②后续每个二分实验前 `git add -A`（暂存保护）+构建后必查 rc；③重要节点即 commit（快照 39cc48d 的教训反向利用）。
  - **预防**: **①长调试链中任何 git checkout/reset/stash 操作前，工作树必须先 commit 或 stash 且核对 stash list（未提交改动=唯一的迁移成果，破坏性命令是原子弹）**；**②二分实验的每一步都要「实验前快照源码态（git diff>文件）+构建后查 rc + 产物标记来源」，产物无法追溯到源码态的实验结论一律作废**；**③cmake --build 的验证=退出码+错误串双查（并行部分失败不打印到 grep 口径）**。
  - **权重**: 8（工程纪律8 × 预防价值1.4 × 当轮恢复0.8）

## 工程纪律（第六十七轮：v2 结构体新字段未入显式初始化清单——半成品 v2p 构建即崩；E2E 共享产物的并行隔离面，2026-09-11）

- [2026-09-11 第六十七轮] **问题类型**: 设计缺陷（v2 结构体字段初始化契约+并行共享产物，权重 7）**✅ 当轮根治**
  - **描述**: ①上会话遗留半成品（函数IR 加 字符串变量们 字段但 生成函数 的显式初始化清单未同步）——用该源码构建的 v2p（target/audit2）编译任何程序都在解析后「运行时错误3 空指针」（未初始化映射/向量字段的 .获取/.大小 崩），掩盖为「环境损坏」误判近半小时；②半成品的 发射字符串释放 用 IR_调用(52) 单指令形态——v2 三后端代码生成只处理 IR_调用实参+IR_调用参数化 两指令形态，52 无处理器静默忽略（发射了也不生效）；③E2E 并行化首版只隔离 v2asm.s，漏 v2p 的 stdlib 签名扫描（IR签名.cn:358 按**相对 cwd** 读 stdlib/容器.cn）——workdir 隔离后 17 个借链容器用例全红「签名扫描打开失败」。
  - **根因**: ①v2 结构体裸声明**不调用字段默认构造**（生成函数.cn:18 注释在案）——「加字段须同步显式初始化清单」的契约靠记忆不靠机制；②v2 调用发射有两套形态（52/参数化对），新代码照宿主 IR 直觉写了旧形态；③**共享产物的 cwd 依赖面**（相对路径产物/相对路径依赖）在隔离设计时未全量盘点。
  - **解决**: ①字段补入显式初始化清单；②统一改 IR_调用实参+IR_调用参数化（__cn_str_concat 先例）；③workdir 内软链 stdlib+入口绝对路径+日志锚行绝对前缀适配回相对（.expected 不动）。
  - **预防**: **①v2 源码给既有结构体加字段，当场同步 生成函数 等构造点的显式初始化清单**（裸声明字段无默认构造——上会话半成品崩因+本轮 v2p 损坏同根因）；**②v2 新发射代码的调用一律走 IR_调用实参+IR_调用参数化 对形态**（grep __cn_str_concat 范本；IR_调用(52) 在 v2 代码生成无处理器=静默丢指令）；**③并行化改造先全量盘点共享面：固定路径产物（target/v2asm.s）、相对 cwd 依赖（stdlib 签名扫描）、共享名产物（v2asm_X.o/v2out_X）三类逐一隔离或预热**；**④测试 runner 的用例注册表是「每新增用例改代码」的反模式——配置文件进用例目录（数据驱动）**。
  - **权重**: 7（设计缺陷7 × 预防价值1.3 × 当轮根治0.85）

## 工程纪律（第六十六轮：拥有型字符串三层借用漏网——黑名单拦不住用户函数包装、白名单制才是单类型拥有语义的正解，2026-09-10）

- [2026-09-10 第六十六轮] **问题类型**: 设计缺陷（拥有型语义判定，权重 7）**✅ 三层定位根治**
  - **描述**: 阶段4' 拥有型字符串落地过程三层借用漏网连环：①oopVarSrcTypes_ 模块级表直接扫=跨函数串槽（主函数 free 造函数槽段错误）；②黑名单制（拦 驻留文本）漏网 **v2 用户函数 当前文本=驻留文本 的借用包装**（用户函数返回借用指针无法按内置名拦）→v2p 自举全瘫（节点=0）；③38_tool 姓名数组[位置]=副本 的指针逃逸模型（值被外部槽持有）出口 free=悬垂乱码。
  - **根因**: 单 `字符串` 类型同时承载拥有/借用两义（Rust 是 String/&str 两类型）——静态层面**调用返回的拥有性不可判定**（内置可枚举、用户函数不可）；跨函数共享表与函数级状态的边界意识。
  - **解决**: ①收集改扫本函数 Alloca 指令（oopVarSrcTypes_ 仅作类型查询）；②**白名单制**（字面量/标识符/内置分配族八函数——其余调用一律污染退出 RAII，宁可不自动释放保安全）；③下标字符串元素赋值的右值标识符污染（借出语义）——三层均为「编译器侧精准兼容、零测试文件改动」。
  - **预防**: **单类型承载两种所有权语义时，拥有判定必须白名单制**（可枚举的拥有源）而非黑名单（不可枚举的借用源）；**跨函数共享表（模块级静态）用于查询可以、用于遍历收集必串函数**；值流入外部槽（下标/成员/全局）= 借出，源变量退出 RAII。
  - **权重**: 7（设计缺陷7 × 预防价值1.3 × 当轮根治0.85）

## 工程纪律（第五十八轮：负测用例「假绿」子串设计+v2 括号笔误与静态跨文件——三坑当场识破，2026-09-10）

- [2026-09-10 第五十八轮] **问题类型**: 工程纪律（用例设计与 v2 写作，权重 5）**✅ 当轮识破修正**
  - **描述**: ①198 负测首版期望子串「局部变量的地址」——五形态混合文件中**既有检查（引用返回 r）的错误先命中子串**使用例 PASS=假绿，掩盖了新增四形态（静态/字段/指针返回）的静默缺口（check 实证诊断仅 1/5）；②v2 缺陷②修复代码 `整32(驻留("*"))))` 多写一个右括号——宿主编译 v2 全树在语法分析.cn:96 报「预期 {」三层连锁诊断；③199_v2 首版静态标量作指针指向源——v2 静态标量在函数内按**栈槽**处理（宿主 .data 语义分叉），返回的是栈地址（产物 asm lea [rbp-16] 实证）；豁免判定按「类型名尾 &」也失败（v2 引用参数符号类型ID=基名，引用性在类型节点.附加=256——两处机制都想当然）。
  - **根因**: ①负测子串未与「新增诊断独有消息」对齐（混合文件里既有错误可撑起宽子串）；②v2 源码多层嵌套条件的括号肉眼对不齐（CN 全角名+三层 驻留 嵌套）；③对 v2 符号表「引用性承载位」未查实现就按宿主心智写判定。
  - **预防**: ①**负测用例（尤其混合形态）的期望子串必须取自「新增检查独有消息片段」**，先 check 实测确认当前诊断不含该子串（假绿识别法）；②v2 源码嵌套括号写完即构建 v2 全树冒烟（宿主编译器 rc=0 是最低门槛——本轮三处连锁错 30 秒暴露）；③**跨侧写检查/判定前先 grep v2 符号表的实际承载字段**（引用=类型节点.附加 256、指针=语义指针变量表按名ID——与宿主 T& 后缀直觉不同构）。
  - **权重**: 5（工程纪律5 × 预防价值1.3 × 当轮识破0.8）

## 工程纪律（第五十七轮：跨侧（宿主/v2）写用例三坑——打印语义颠倒直觉/锚定链脚本继承已知 bug/v2 语法面未探先写，2026-09-10）

- [2026-09-10 第五十七轮] **问题类型**: 工程纪律（用例写作与脚本复用，权重 5）**✅ 当轮全部识破修正**
  - **描述**: 阶段1 转移检查落地过程三坑：①196 用例首版用 打印行 输出多行期望——实际 CN 语义 **打印=println（末尾换行）、打印行=print（不换行）**（ir_call.cpp:110-111 明文），与「行」字直觉相反，期望文件按打印行写=单行输出比对失败；②锚定链脚本从 run_chain55.sh 复制改名，[6] 运行级三用例段**继承了已知错误口径**（直接运行 cn_self 当被编译程序，rc=编译退出码非产物运行码——55 轮 HANDOFF 踩坑记录在案但脚本未修），首跑 119 rc=0 期望 14 误报失败；③197_v2 用例首版含 `-> 整64\*` 指针返回类型与单行函数体——v2 语法层两者皆不支持（宿主支持；单行体 t1 探针过/返回指针 t2 探针红），二分探针定位后适配。
  - **根因**: ①「打印行」命名望文生义（中文语感「打印一行」=带换行，实际语义相反）——写用例没查规范/没看既有用例的函数选择（02_control 全用 打印+多行 expected）；②复用脚本只改名没通读（已知 bug 清单在 HANDOFF 而非脚本内，跨轮传递断链）；③为 v2 写用例按宿主能力面脑补（v2 语法层子集未探——「自身源码不用某语法=零验证」的镜像：用例用了 v2 不支持的语法=解析期红）。
  - **预防**: ①**输出类用例的打印函数选择必须先看一个同形态既有用例**（多行输出=打印/紧凑=打印行——02_control 是范本）；②**复用跨轮脚本必须通读全文**，已知 bug 要当场修掉或在新脚本头部标注禁用段（这次 [6] 段当场改用 runlevel50.sh 口径）；③**v2 侧新用例先用最小探针过 v2 语法面**（单行函数体/指针返回/其他宿主有而 v2 无的形态），宿主≠v2 能力等价。
  - **权重**: 5（工程纪律5 × 预防价值1.2 × 当轮识破0.8）

## 工程纪律（2026-09-10：设计呈报中的语法示例未查规范凭 Rust 记忆臆造——语义可借鉴、语法必须查规范）

- [2026-09-10] **问题类型**: 工程纪律（语法示例编造，权重 6）**✅ 用户当场识破纠正**
  - **描述**: 回答「CN 如何做编译期安全」设计呈报时，四段语法示例（`转移 v` 前缀形态、`常量 数据 : 向量<整64>&` 冒号参数形态、`-> 常量 整64&` 返回修饰、`不安全...结束` 块）全部按 Rust 心智拼凑，无一符合现行 CN 规范——CN 参数形态=`类型 标识符`（无冒号，规范 04 函数定义 EBNF）；且 CN v1 前身正是 Rust 风格（让/可变/匹配/所有权），用户已裁决推倒为 C++ 风格 v2.0，规范 01a §5.9 明列移除关键字、skill 明文「禁止混用旧版 Rust 风格语法」。
  - **根因**: 把「借鉴 Rust 语义」做成了「照抄 Rust 语法」——编译期安全语义规则（转移/借用互斥/逃逸）可穿任何语法外衣，但写示例时未按 AGENTS.md 第 2 条「先调 cn-language-spec 查规范，不得凭记忆编造语法」执行，用最熟悉的 Rust 表面从记忆重构。
  - **预防**: ①**任何设计呈报/报告中的语法示例，每个关键字与语法形态必须先对照 spec EBNF 核实并标注规范出处**（如「规范 04 引用参数」），核实不了的明确标「拟新增待审批」；②**CN 的前身恰是 Rust 风格 v1——凡设计向 Rust 靠拢，落笔前必查 01a §5.9 移除关键字表**，防止把已废弃范式当新提案端回（用户对 Rust 语法的敏感=项目既有裁决，不是口味问题）；③语义可以借鉴，语法不许发明。
  - **权重**: 6（工程纪律6 × 预防价值1.3 × 用户识破0.8）

## 编译器缺陷（第五十二轮：v2 ARM64 取片有符号取模出负片——movk 立即数越界，2^63 唯一爆点，边界形态探针缺位63形态，2026-09-10）

- [2026-09-10 第五十二轮立案→第五十三轮根治] **问题类型**: 编译器缺陷（v2 ARM64 后端常量发射，权重 7）**✅ 已根治（2026-09-10 第五十三轮，用户裁决方案A：取片 移位+掩码归一·宿主同构，闭环清单全绿见 plans/014 52-a 行根治记录）**
  - **描述**: arm64 跨机轮全量 E2E 首验 191_v2/193_v2 双红（X64L 同用例全绿）：as「立即数越界」——v2 ARM64 后端装载 2^63 字面量（9223372036854775808，唯一 bit63 置位位模式）发射 `movk x9, #-32768, lsl #48`，GNU as movk 域=无符号 16 位（0~65535）。根因=代码生成ARM64.cn 取片() 有符号除法/取模（`v%65536` 对负 v 保号出负片）×常量整数 负值补码分支（字面量位模式 读整64 为负→`0-值` 回绕仍为 -2^63）——**位模式正确、发射文本越域**；宿主 emitMovImm `uint64_t` 无符号提取天然免疫（arm64_codegen.cpp:347 对照）。
  - **根因**: ①「分片位模式提取」按宿主语言有符号算术直译（/ %），未做无符号位模式归一——位模式类运算（分片/掩码/移位）在有符号整64 世界里的 / % 不是位模式语义；②2^63 形态在「自身源码+既有探针」双盲区——第四十二轮八形态探针覆盖 0x7FFFFFFFFFFFFFFF（正上界）与 -255（普通负数）但缺 bit63 置位/负下界形态，lessons 权重10 预防①「凡编译器支持的语法必须有自身源码之外的探针锚定后端发射」的再演；③X64L/win mov 全范围立即数=两平台结构性免疫，深度机三轮全绿掩盖（平台差异掩盖家族再+1）。
  - **预防**: **①位模式类运算（分片/掩码/提取）实现一律以无符号语义表达（掩码归一/无符号通道），宿主语言有符号算术是陷阱默认**；**②整数边界探针必须含位域两端+符号位翻转形态（2^63/负下界），正上界+普通负数不足以覆盖补码分支的回绕形态**；**③跨机轮 E2E 首跑红=拦截面价值兑现（第五十轮同向），任一平台全绿不构成其它平台免疫证据**。
  - **解决**: 取片() 单点重写 `返回 (值 >> (片位 * 16)) & 65535;`（移位+掩码=位模式语义：宿主 `(value>>shift)&0xFFFF` 严格同构、与第四十三轮 位模式十六进制 `(h >> 移) & 15` 范本同款；掩码截低 16 位故算术右移的符号填充位不落入片内=任意整64 位模式正确；原 除法/取模 系「无移位运算」时代权宜，`>>`/`&` 自第四十三轮 v2 位运算全链落地后即可用——**权宜实现的退役时机=所依赖能力落地时，立案审计应连同标注**）；191_v2/193_v2 arm64 转绿+2^63 装载/重解释三锚双侧一致+锚定链重锚 322112 行+组件对拍 29/29（第五十三轮用户裁决方案A 闭环全绿）。
  - **权重**: 7（编译器缺陷7 × 预防价值1.3 × 已定位待裁0.7）

## 验证方法（第五十轮：正/负断言方向读反——「返回错误码触发」被误读为「断言失败」，假阳性定性差点引发宿主无用改动，2026-09-10）

- [2026-09-10 第五十轮] **问题类型**: 验证方法（探针断言方向误读，权重 7）**✅ 当轮识破修正（呈报定性修正）**
  - **描述**: 第四十九轮 46-f 呈报定性「宿主无符号比较静默错误：`如果 (大 > 100) { 返回 11; }` rc=11=判假」——第五十轮实施前复核实锤**假阳性**：rc=11 是**进了分支**=比较为真=行为正确；真分叉探针（`大 < 2^63-1`——有符号读 2^63 为负应判真、无符号判假，两侧结果相异的形态）实证宿主 setb/setae 分派正确工作，宿主零改动。
  - **根因**: 矩阵探针混用了两种断言方向——190 系的 `如果 (a != 41) { 返回 11; }`（不等才返回=失败方向）与 u64_matrix 的 `如果 (大 > 100) { 返回 11; }`（**真才返回=正确性正断言**）形态同构但语义相反；读结果时按「返回错误码=失败」惯性解读，把正确行为定性成缺陷并写入呈报。
  - **解决**: 呈报定性修正入 plans/014（宿主侧健全、假阳性归因）；v2 侧四缺口（div/shr/setb 族/32 位截断）不受影响照常根治；「真分叉」探针（有符号/无符号解释结果相异的输入）补入验证矩阵——分叉形态才能区分分派是否生效，同值形态（2^63>100 两解释同真）无鉴别力。
  - **预防**: **探针每个返回码的触发条件必须显式注释「何时返回」（正断言真返回 vs 负断言假返回），读结果前先核方向**；**定性「某侧有缺陷」前必须构造「两侧解释结果相异」的鉴别形态**——同真/同假形态对分派缺陷零鉴别力；**呈报中的缺陷证据链要能被独立复核**（rc 值与断言方向的对照表），否则假阳性会传导成错误工作项（本轮差点对健全的宿主做无用改造）。
  - **权重**: 7（验证方法7 × 预防价值1.3 × 当轮识破0.8）

## 工程纪律（第四十九轮：v2 新源码触发既有拦截器——自举锚定链是自写代码的第一道门禁，2026-09-10）

- [2026-09-10 第四十九轮] **问题类型**: 工程纪律（新增源码触发既有拦截，权重 6）**✅ 当轮识破修正**
  - **描述**: 46-c 推断声明的 IR 回填代码写了 `类型节点 = 建节点(节点_类型, ...)`（节点 结构体整体赋值、右值=调用形态）——单文件探针/组件编译全绿，**v2self 锚定链 fix_p（v2p 编译 v2 全树）才爆 `[ir2] 结构体赋值右值须为同型变量标识符: 类型节点` rc=1**：第四十八轮结构体整体拷贝拦截的右值白名单只有 标识符/成员/构造字面量，调用返回结构体的赋值位形态被拒。
  - **根因**: 写新 v2 源码时的验证面只有「宿主编译该文件（rc=0）+目标探针」，而**「v2 编译器编译自身全树」才是新源码的全语法面门禁**——自身源码里的赋值/声明/表达式形态千千万，逐文件编译探针覆盖不了组合面；该缺陷代码恰好不在任何探针路径上、只在编译 v2 自身时执行到（生成声明 对 v2 全树的每处变量声明跑推断回填拦截）。
  - **解决**: `类型节点.文本 = 整32(推断回填ID)` 单字段标量通道替代整体赋值（节点 字段全标量，成员赋值不触发拦截）；连带修复 run_chain49.sh 的 map_check48.py 文件名残留引用。
  - **预防**: **凡 v2 源码改动，宿主编译 rc=0+探针绿只是半程，「v2p 编译 v2 全树 rc=0」必须当轮跑（锚定链步骤2 提前做冒烟）再进全量 E2E**——自举编译器的自举链是自写源码最全的语法面消费者；**「结构体整体赋值右值白名单（标识符/成员/构造字面量）不含调用」是 v2 源码写作约束**——需要调用返回结构体的场景用声明位初始化（生成结构体局部 返回接收形态）或逐字段标量赋值，勿写赋值位调用形态。
  - **权重**: 6（工程纪律6 × 预防价值1.3 × 当轮识破0.8）

## 工程纪律（第四十八轮：辅助函数「自身/父链」双形态参数语义混用——拦截永不命中的假阴性，2026-09-09）

- [2026-09-09 第四十八轮] **问题类型**: 工程纪律（修复假阴性，权重 7）**✅ 当轮识破修正**
  - **描述**: v2 成员左值赋值结构体拷贝拦截首版：`解析成员链结构体ID(池, 左末自身索引)` 后又查一层 `查字段类型ID(返回值, 左末.文本)`——D 形态 `r.右下 = r.左上` 探针始终 FAIL（静默写地址值），无任何诊断报错，修复「看似已实施」实则死代码。
  - **根因**: `解析成员链结构体ID(池, 索引)` 的语义是「池[索引] 节点自身的类型」——传成员节点自身索引得**该字段自身类型**（.右下→点），不是父链类型；父链类型须传 `m.子起点`（成员节点 子起点=对象节点索引）。双重解析（自身类型再查一层字段类型）必然 miss=0 → 拦截分支永不命中，且 miss 静默回落主路径=**假绿形态的修复**（代码在、行为旧）。
  - **解决**: 临时诊断打印双侧对照识破——语法层登记侧打印（结构体键基 29=点/35=矩形）与 IR 期查询侧打印（左链ID=29）数值吻合，暴露「29 是左末自身类型而非父链类型」；修正为 `解析成员链结构体ID(池, 左末.子起点)`（范本=取成员宽度码 的既有正确用法）。
  - **预防**: **复用带「自身/父链/根」多形态索引语义的辅助函数时，必须对照既有范本调用点核参数（grep 首个消费者），不得按直觉补一层查询**；「修复实施后行为不变」本身是高危信号——拦截型修复应先验证拦截命中（负形态探针）再验证拦截正确（正形态探针），两步缺一不可。
  - **权重**: 7（工程纪律7 × 预防价值1.3 × 当轮识破0.8）

## 验证方法（第四十四轮：独立复算必须用被验系统的代码内常量——教科书 FNV basis 尾差一位致假阴性，2026-09-09）

- [2026-09-09 第四十四轮] **问题类型**: 验证方法（黄金值独立复算假阴性，权重 6）**✅ 当轮澄清**
  - **描述**: 家机轮黄金值三方对拍中，python 独立复算首跑 False——按教科书 FNV-1a 64 offset basis 14695981039346656037（0xCBF29CE484222325）对重构原串（54 汉字函数名 hex-mangle 328 字符）复算得 05121C41929B36C9，与产物尾串 AEAEDCD4D2F8988F 不符；换代码内 seed 1469598103934665603（第四十轮 shortenLongSymbol 既定原值，**尾少一个 7**）复算逐位一致。v2↔宿主产物对拍本身是一致的，假阴性纯出在复算脚本的常量选择。
  - **根因**: 复算脚本作者以「教科书常量」替代「被验系统实际常量」——本项目 FNV 是变体（自定义 seed+标准 prime 1099511628211），注释里的「FNV-1a 64」只说明算法家族不锁定常量值；当被验系统常量本身非标准时，独立复算会**系统性假阴性**，且方向危险：要么误判正确实现有缺陷（浪费排查），要么在真有缺陷时被「对拍一致所以脚本错了」的反向推理掩盖。
  - **解决**: 黄金值语义回归本位——「黄金值=对被验代码内常量对的确定性函数值」，复算以 grep 代码内字面量常量为准（seed=1469598103934665603/prime=1099511628211），三方（v2 产物/宿主产物/复算）一致闭环。
  - **预防**: **独立复算/参考实现必须先提取被验代码的实际常量与算法形态（grep 字面量），不得从记忆/教科书填常量**；复算不一致时先排查「参数域差异」（常量/字节序/编码）再怀疑「实现缺陷」，且不一致本身无论哪边错都是有效信号须归因到底。
  - **权重**: 6（验证方法6 × 预防价值1.2 × 当轮澄清1.1 × 已解决0.85）

## 工程纪律（第四十三轮：管道 tail 掩盖真实退出码——失败被吞、旧产物续用，2026-09-09）

- [2026-09-09 第四十三轮] **问题类型**: 工程纪律（验证链 rc 失真，权重 7）**✅ 当轮纠正**
  - **描述**: `cmd | tail -1` 后以 `$?` 判断成败——取的是 tail 的 rc（恒 0），宿主重建 v2p 实际失败（v2 源码 `结果` 保留名冲突）被掩盖，后续探针跑的是旧 v2p 产物；直到长名探针行为异常单跑补测才暴露。
  - **根因**: 管道中前端命令的退出码被丢弃；`&&` 链条基于 tail 的 rc 继续——「构建失败→旧二进制续用→新代码验证假绿/错乱」三连。
  - **解决**: 长命令一律 `cmd > log 2>&1; echo rc=$?` 或 `set -o pipefail`；关键构建步骤后必须核对 rc 再继续。
  - **预防**: **凡「构建→立刻使用产物」的链条，产物新鲜度必须以构建 rc 为准前置校验**（与第三十四轮「对拍产物新鲜度/清场重建」纪律同族——那次防的是旧产物文件时间戳，这次防的是旧产物二进制被静默续用）。
  - **权重**: 7（流程缺陷7 × 预防价值1.2 × 已解决0.85）

## 设计缺陷（第四十一轮：编译器自身源码=对拍输入面——「自身源码不用某语法」=该语法零验证，2026-09-09）

- [2026-09-09 第四十一轮] **问题类型**: 设计缺陷（v2 win 后端常量发射+超长符号收缩，权重 10）**✅ 当轮根因级根治**
  - **描述**: 挂账4 v2self 长符号面自查，探针实证三枚同域缺陷：①v2 win 后端 `符号修饰` 无超长收缩（宿主第四十轮已修 shortenLongSymbol，v2 对齐的是旧版 nameMangle）——50 汉字探针产物 319 字符>247，ml64 A2043 rc=1；②整数字面量文本原样直发——`mov rax, 0L`（词法保留 L 后缀）触发 ml64 A2206；③超 32 位常量无 MASM hex 归一（宿主 uint64HexText 有）。**组件对拍 30/30 全绿对此零暴露的原因：对拍输入面=v2 编译器自身源码，而自身源码恰好不用 0x 前缀/带后缀大字面量/超长标识符**——「编译器编译自己」的验证面天然=自身语法使用面，自身不用的语法全链路零验证（词法存了 L 后缀、语义放行、IR 原样、后端直发，四层全盲）。
  - **根因**: ①宿主修复未同步审计自举侧同款（第四十轮 shortenLongSymbol 只修宿主 x64_codegen.cpp——「双实现一侧修复必须审计另一侧同款缺口」，与第三十八轮预防③同族但针对平台约束类修复未触发意识）；②v2 win 后端常量发射从未建立「非默认字面量」的测试锚（词法→后端四层贯通面无 E2E/探针覆盖）。
  - **解决**: ①共用层新增 十六进制数值/解析无符号/字面量位模式/位模式十六进制 四纯函数（三后端单点）；win 层 常量立即数文本 归一（真/假→1/0+位模式剥离+fit32 十进制+域外 MASM hex 与宿主 uint64HexText 逐环同构）+超长收缩函数组（FNV-1a 纯算术逐位模拟——v2 无位运算全链路，/ % 正数域提取+每字节 8 次迭代异或，低频路径微秒级；**黄金对拍 EFF9F723240DB1BE 与宿主逐字节一致=纯算术模拟与 uint64 位运算逐位等价实证**）；linux/arm64 本地解析函数删除换 字面量位模式（根治 0x 前缀静默解析为 0 的潜伏缺陷）。②「符号修饰」单点包裹（宿主 nameMangle→shortenLongSymbol 同构）。
  - **预防**: **①「编译器自举源码+组件对拍」的输入面=自身语法使用面——凡编译器支持的语法（尤其字面量形态/标识符长度边界），必须有「自身源码之外」的探针/用例锚定后端发射**（验证面盲区是结构性的：自举全绿≠语法全验证）；**②宿主的平台约束类修复（符号上限/常量格式/编码域）必须当轮审计双实现另一侧同款缺口**（对拍逐字节基线只锁「两侧一致」的形态，不锁「另一侧缺失的防御」）；**③探针复现要按「成因公式」构造输入**（hex-mangle 每汉字 6 字符×50 汉字≈319>247——首轮 37 汉字探针 226<247 未触发，探针长度必须按公式核过阈值）。
  - **权重**: 10（设计缺陷10 × 根因级1.5 × 预防价值1.4 × 已解决0.85）

## 工程纪律（第四十一轮：同族修复旁路穷尽再演——imul 修了 imm32 域检查、cmp 漏了，2026-09-09）

- [2026-09-09 第四十一轮] **问题类型**: 编译器缺陷（宿主 emitCompare，权重 8）**✅ 当轮根治**
  - **描述**: 为写 v2 修复代码（`高 < 2^55` 判定）引入 v2 源码常量 36028797018963968，宿主编译 v2 源码产物即爆 ml64 **A2084 constant value too large**——`cmp rax, 36028797018963968`。宿主 emitCompare 对常量操作数无 imm32 编码域检查（x64 cmp reg64 只有 imm32 符号扩展形态，域外必须先装载寄存器）；而 2026-08 emitBinary 同族路径已修过同款（imul/and/or 立即数超 int32 先 mov rcx——x64_instructions.cpp:314 注释在案）。
  - **根因**: 同族指令（二元算术 vs 比较）的立即数域检查只修了触发过的那条——「同族修复旁路穷尽」纪律（第三十一轮 copytree 四象限、第二十九轮栈参锚定同族）在指令编码层的再演：**A2084 与 A2070/A2206 同为 MASM 立即数编码约束，修一处时未对全指令族对表**。
  - **解决**: emitCompare 三段归一——op2 域外先 `mov rcx, uint64HexText(v)` 再 `cmp w, rcx`（emitBinary 同构）；op1 64 位域大值 hex 归一（十进制立即数被 ml64 按 32 位截断同源）；0x/0b/0o 前缀原始文本防御解析（ConstInt 路径同款）。32 位类型比较值域恒在 imm32 内，不触发中转（无尺寸错配面）。
  - **预防**: **①「立即数编码域」是全指令族约束（mov/add/sub/imul/and/or/cmp 各自的 imm32 形态表）——任一指令新增立即数路径时对全族对表而非只改触发者**；**②「写另一侧代码」是激活宿主潜伏缺陷的高概率事件（v2 源码新常量即刻触发）——自举编译器的源码演进会持续以新语法形态打宿主后端，宿主编码域防御要先行于 v2 语法面扩张**。
  - **权重**: 8（编译器缺陷8 × 预防价值1.3 × 当轮根治1.1 × 已解决0.85）

## 验证方法（第四十一轮：路径文本匹配对平台分隔符脆弱——win 反斜杠路径使 v2 恢复包上下文失配，2026-09-09）

- [2026-09-09 第四十一轮] **问题类型**: 平台可移植缺陷（v2 主.cn 模块路径匹配，权重 6）**⚠ 已定位挂账（本轮未修——属 v2 模块系统另案）**
  - **描述**: 批量组件验证脚本以 Windows 反斜杠相对路径（`CN语言编译器v2\语法\解析基础.cn`）调 v2p，报「符号歧义 183 错」；同一路径改正斜杠 rc=0。根因：v2 主.cn 恢复包上下文/路径去重按**文本匹配**判定入口与包根成员同源性，反斜杠与内部正斜杠规范形态失配 → 入口双载 → 同名符号双登记 → 歧义。深度机 linux 天然正斜杠不可见——「平台差异掩盖」家族再+1。
  - **预防**: **①凡以「路径文本」为身份判定的逻辑，入口处必须归一分隔符（统一 `/`）再比较——路径身份结构化（第三十八轮 findByPath 路径ID）是终态，文本归一是底线**；**②跨机轮脚本对路径参数统一 as_posix()**；**③「同一输入两种写法行为不同」用双形态对照实验最快定位（正反斜杠各跑一遍 rc 对比）**。
  - **权重**: 6（平台缺陷6 × 预防价值1.3 × 挂账未修0.6）



## 设计缺陷（第四十轮：MASM 247 字符标识符硬上限——超长符号的失效形态是 PROC 配对崩坏而非单点报错，2026-09-09）

- [2026-09-09 第四十轮] **问题类型**: 设计缺陷（win 后端符号 mangling，权重 10）**✅ 当轮根因级根治（nameMangle 单点超长符号哈希短化）**
  - **描述**: 拉取第三十一~三十九轮后 win 全量 E2E 40+ 用例齐爆「汇编失败（ml64 退出码 1）」。错误日志尾部 73 个 A2005 symbol redefinition（bb1/bb2...）+27 个 A2043 identifier too long——表象指向「块标签跨函数重定义」，实锤根因在**第一条错误**：`A2043 identifier too long`（289 字符 mangled 符号 > MASM 247 硬上限）。第三十四/三十八轮「项目内包根成员统一 包名::主干 命名」使组件链限定名加前缀，hex-mangle 每汉字 6 字符膨胀（1 汉字 3 UTF-8 字节→6 hex 字符）后超限。
  - **根因**: **A2043 的失效形态不是「该符号单独报错」而是连锁崩坏**——MASM 遇超长 PROC 定义行即弃置：PROC 未打开/ENDP 未闭合，后续全部函数的「PROC 内局部标签」落入同一未闭合作用域 → 同作用域同名 bbN → A2005 雪崩。错误表象（bb 重定义）与根因（超长标识符）相距数百行，且 linux_x64/arm64（GAS）无 247 限制全绿——平台差异再次掩盖。
  - **解决**（LLVM/rustc 同构：超长 mangle 内容哈希截断，性能第一/安全第二）: nameMangle 单点包裹 shortenLongSymbol——输出 >200 时收缩为「头段(180)$L<原长>H<FNV-1a64>」；内容哈希含参数串（重载不冲突）、嵌入原长使碰撞面 2^-64；头段保留包名/模块路径 hex 头供人工归类；阈值 200 留足 247 内余量（短化后 ≤202）；仅超长符号短化=短符号与既有产物逐字节一致（字节级对拍纪律零扰动）；单点包裹保证 PROC/ENDP/call/EXTERN/虚表/静态全部引用一致。**实证链**: git worktree 检出 HEAD(831a165) 同用例对拍——HEAD 产物符号最长 231/超 247 数 0/ml64 RC=0；本轮产物最长 289/超限 34/RC=1——产物差异与第三十四/三十八轮前缀命名严格对应。
  - **预防**: **①三后端共存的编译器，任何「使命名变长」的改动（加前缀/限定化/签名入名）必须对三后端符号长度上限对表**（MASM 247 硬上限 vs GAS 无限）；**②汇编器错误日志必须看第一条**——首错是根因，尾部数百条全是级联（A2043→PROC 配对崩坏→A2005 雪崩）；**③「符号上限」类约束要在 mangling 层设自适应收缩机制而非依赖名字不变**——命名策略会随模块系统演进持续变长。
  - **权重**: 10（设计缺陷10 × 根因级1.5 × 预防价值1.4 × 已解决0.85）

## 设计缺陷（第四十轮：GCC 内建与 POSIX 弃用函数入库——深度机编译通过≠跨平台可移植，2026-09-09）

- [2026-09-09 第四十轮] **问题类型**: 平台可移植缺陷（runtime 诊断资产，权重 8）**✅ 当轮双缺陷根治（编译器内建平台分流+安全 CRT）**
  - **描述**: 拉取后 win 构建 1/3 即败：①`intern_api.cpp(65): error C3861: "_builtin_return_address": 找不到标识符`——第三十七轮 CN_INTERN_TRACE 驻留轨迹插桩用 GCC/Clang 专属内建取调用者返回地址，MSVC 无此内建；②修复后 `error C2220: warning C4996 'getenv' 被视为错误`——同段插桩的 getenv 触发 MSVC 安全 CRT 弃用警告，/WX 下视为错误（首错 C3861 先中止，第二枚延迟暴露——同段代码两枚平台缺陷串联）。
  - **根因**: 第三十七轮在深度机（Linux/GCC）实现诊断资产，两处平台专属用法（GCC 内建+getenv）未经 MSVC 编译即入库；「诊断代码默认零开销零输出」的运行时属性掩盖了「编译期不可移植」属性。
  - **解决**: ①编译器内建平台分流（`#if defined(_MSC_VER)` → `<intrin.h>` `_ReturnAddress()`，GCC/Clang 保持 `__builtin_return_address(0)`，显式 `(void*)` 转换同时消除 %p 级联警告）——两编译器均为编译期展开零运行时开销，诊断 ra 反解能力全平台对等（rustc 同构：平台差异用原生内建分流不设运行时抽象层）；②getenv 跟随项目既有先例（cn_main.cpp getEnvVar 安全 CRT 纪律）——只判存在性用 `getenv_s` 空值查询（免分配免释放）。
  - **预防**: **①「GCC/Clang 内建、POSIX API、编译器扩展」入库前必须在双平台编译验证**（跨机轮的拦截面价值正是于此）；**②MSVC 下 getenv=C4996/WX 即错误是既有事实，新代码用 getenv_s/_dupenv_s（仓内 cn_main.cpp 有同款先例可锚）**；**③同一段新增代码的多个平台问题会被首错遮蔽——修复首错后必须立即重编译而非假设「就这一个」**（本轮 C3861 修复后 C4996 才暴露，多跑一轮门禁）。
  - **权重**: 8（平台缺陷8 × 预防价值1.3 × 当轮根治1.1 × 已解决0.85）

## 工程纪律（第四十轮：多分支对称改动漏支——「同一改动触及多平台分支」必须穷尽分支清单，2026-09-09）

- [2026-09-09 第四十轮] **问题类型**: 编排遗漏（run_e2e.py，权重 7）**✅ 当轮收口（win 负路径分支补整树复制，168/169/170 转绿）**
  - **描述**: 第三十一轮为模块系统负测（168~170）加「用例目录整树复制」（copytree）——linux 分支负路径、linux 分支正路径、win 分支正路径三处都加了，**win 分支负路径漏加**。后果：win 侧 168/169/170 期望 sem5 语义诊断行，实际 v2p 停在 p6b「无法打开依赖模块」（依赖 .cn 文件未被复制到位）。
  - **根因**: 「负路径整树复制」的改动面按「平台×路径类型」=2×2 四象限分布，改动时按用例触发顺序逐处加（linux 负→linux 正→win 正），第四格（win 负）在 linux 门禁全绿下不可见——**未触发的分支不会在当轮门禁暴露**。
  - **解决**: win 负路径分支（预期退出码 None 分支）补同款 copytree（含同款 ignore 模式与注释），168/169/170 单跑全绿。
  - **预防**: **①平台×路径类型的正交改动，实施时必须列出四象限清单逐格勾稽**（与第三十一轮「同族修复旁路穷尽」同族——旁路/分支的穷尽清单化）；**②「某分支未跑过」的改动在合入时标注显式待验清单**（深度机 linux 门禁无法覆盖 win 分支代码路径，跨机轮必测）。
  - **权重**: 7（编排遗漏7 × 预防价值1.3 × 当轮收口1.0 × 已解决0.85）

## 设计缺陷（第三十八轮：模块身份靠「入口目录相对路径文本」推断——同一文件不同入口形态行为漂移，2026-09-08）

- [2026-09-08 第三十八轮] **问题类型**: 设计缺陷（宿主模块系统，权重 9）**✅ 当轮结构性根治（pkgRoot 结构归属+路径身份+命名只由包结构决定）**
  - **描述**: 挂账1「5 组件文件单文件入口编译失败」深挖实锤**三缺陷同源**：①目录包成员 `导入 自包名`（IR布局 导入 IR）与包根挂载边构成拓扑环——整体构建不爆是**侥幸**（整体形态成员名带包前缀 `IR::IR布局`，topoSort 沿挂载边文本 `IR布局` 查表落空而忽略；单文件形态成员名=文件主干 `IR布局` 恰好查得到 → 假环暴露）；②代码生成/包.cn 挂 `模块 代码生成;` 与包根目录名撞名 → findModule 名字去重把成员永久吞掉；③命令行入口绕过包根挂载链 → 包内兄弟文件不在依赖闭包 → 「未声明函数」137 错。
  - **根因**: **模块名 = f(入口目录, 文件路径)**——同一文件在不同入口下模块名不同（整体形态 `IR::IR布局` / 单文件形态 `IR布局`），而自包导入豁免（前缀文本判定）、拓扑边解析（挂载名文本查表）、同名去重（主干名）全部依赖命名形态 → 行为随入口漂移；v2 编译器按**文件路径**去重+递归后序（无拓扑阶段）天然免疫前两缺陷——两实现「包语义」并不同构。
  - **解决**（rustc crate 模型同构，性能第一/安全第二）: ①ModuleUnit 加 `pkgRoot`（所属包根名的结构归属字段，非文本推断）——dfsTopo 与加载器对 `dep==pkgRoot` 的自包导入边双点豁免（rustc：crate 成员引用本 crate 符号无需跨 crate 依赖边）；②ModuleGraph 加 `findByPath` 路径索引 O(1) 去重（源文件是模块身份唯一真相，v2 按 路径ID 去重同构；包根挂载链与命令行入口加载同一文件防双载）；③项目内目录包根挂载成员统一 `包名::主干` 命名（与整体形态逐字节对齐，命名只由包目录结构决定不随入口漂移；货舱依赖包成员豁免保持 bare，既有符号面零变化）；④`restorePackageContext` 包上下文恢复——入口若是包成员（所在目录 包.cn 声明入口主干挂载）则入口先载、包根后聚合（兄弟补入依赖闭包，入口文件经路径去重不双载）；⑤决议末段匹配兼容包前缀条目名（v1 组件 `语义::语义::内置::行类型` 的末段匹配兜底按条目名末段对齐）。
  - **预防**: **①「同一实体在不同上下文下获得不同标识符」是行为漂移之母——模块/符号的身份判定优先用结构关系（归属字段/路径索引）而非可变文本形态**；**②侥幸不爆≠无环**：整体形态拓扑环不可见靠的是「挂载边文本查表落空」，换入口形态即爆——图算法的边集合必须与加载器的跳过集合逐条对表；**③双实现（宿主 C++/自举 CN）的模块语义改动必须两侧同步审计**——本轮宿主三缺陷中两个在 v2 天然免疫、一个（兄弟不可达）在 v2 同样存在，仅修一侧则对拍基线漂移。
  - **权重**: 9（设计缺陷9 × 根因级1.5 × 预防价值1.3 × 已解决0.85）

## 高权重问题（第三十八轮：主() 丢弃编译入口返回值——「忽略」型假绿是契约冲突的温床，2026-09-08）

- [2026-09-08 第三十八轮] **问题类型**: 编译器缺陷（v2 主干，权重 8）**✅ 当轮根治（返回值消费+契约三层重整）**
  - **描述**: 显式入口不存在时 v2p 诊断打印「中止编译」但 **rc=0 且产空程序 asm**——第二十八轮 fail-fast 裁决（方案A）的 rc=2 语义实际从未传导：主() 写 `整64 忽略编译 = 初始化并编译入口(...)` **丢弃返回值**继续空跑。E2E 139/141 负路径只覆盖语法/语义错误中止（那些路径在主() 有独立判定），「入口不存在」分支零锚定故漏网至今。本轮为修挂账1 给主() 加返回值消费后，**三层契约冲突连环爆**：初始化并编译入口 尾部 `返回 编译结果`（编译文件 契约 1=成功）与主() 判定（0=成功）冲突 → v2p 编译 v2 假失败 rc=1；恢复包上下文 透传 编译文件 的 1（成功）作「非0=失败」→ 全部成员入口假失败。
  - **根因**: 三个函数三套返回值契约（编译文件 0=败/1=成；初始化并编译入口 尾部透传内部契约；主() 按 0=成功消费）且**长期靠「不消费返回值」掩盖**——契约冲突在假绿下不可见，一旦任何一层开始消费即连环暴露。
  - **解决**: 主() 消费返回值（`入口结果 != 0 → 返回`）；初始化并编译入口 尾部改 `返回 0`（本函数契约：0=成功含内置回退/1=货舱失败/2=显式入口不存在）；恢复包上下文 内部显式转换（`包编译==0 → 返回 1；否则 返回 0`）。双侧失败语义验证 rc=2 ✓。
  - **预防**: **①「忽略XX」命名的返回值丢弃是契约冲突的温床——任何函数返回值要么消费、要么注释声明丢弃理由**；**②函数返回值契约（0/1 哪个是成功）必须在函数头注释显式声明，跨层透传必须显式转换而非透传**；**③失败分支的 E2E 锚定要按「失败原因」分型**（语法错/语义错/入口缺失/依赖缺失各一条负路径），「报错」聚合验证防不住单型回归。
  - **权重**: 8（编译器缺陷8 × 预防价值1.4 × 根因级1.1 × 已解决0.85）

## 验证方法（第三十八轮：CN 布尔条件跨语言移植写反——「字符串比较 真=相等」与直觉同向但否定词最易错，2026-09-08）

- [2026-09-08 第三十八轮] **问题类型**: 翻译缺陷（权重 6）**✅ 当轮识破（行为二分定位，歪打正着的假绿曾掩盖）**
  - **描述**: 宿主 restorePackageContext（C++ `importPath == stem`）翻译为 CN 时写成 `如果 (!字符串比较(主干, 挂载名))`——`字符串比较` 真=相等（`主()` 的 --verbose 判定同款用法），加 `!` 后变成**「不等即触发」**：全部入口（含包根自身、非包文件）都误编译包根，组件对拍 28/30 假失败。此前一轮对拍 30/30「全绿」实为假绿——当时主() 还不消费返回值（上一条教训），假失败被吞掉。
  - **根因**: C++ `==` 到 CN `字符串比较` 的翻译中，C++ 侧「命中条件=相等」移植成 `!字符串比较`（潜意识把 字符串比较 当 strcmp 的 0=相等语义）；错误的恢复触发被「无诊断 rc=1」暴露后，靠 173（主干不触发）/词法包根（触发）二分对照快速定位。
  - **解决**: 去掉 `!`（相等触发），并在条件旁注释真值语义与同款用法佐证。
  - **预防**: **①CN `字符串比较(A,B)` 返回 真=相等（非 strcmp 的 0=相等），写条件时以仓内同款用法为锚**（主() --verbose 判定、是主模块路径 判定）；**②「命中才触发」类条件写完必须自问自答一个反例**（包根入口主干=「包」不在挂载清单，理应不触发——按错误条件它会触发）；**③假绿的连锁性**：布尔条件错 + 返回值不消费两层缺陷叠加时对拍「全绿」不可信，修复其中一层往往让另一层暴露——分层修、每层全量复验。
  - **权重**: 6（翻译缺陷6 × 预防价值1.3 × 当轮识破0.75）

## 高权重问题（第三十七轮：宿主 &&/|| 全求值发射——「良性」对拍差异必须追到发射语义层定案，2026-09-08）

- [2026-09-08 第三十七轮] **问题类型**: 编译器缺陷（宿主 IR 层，权重 10）**✅ 当轮根因级根治（短路脱糖三块结构+双层锚定）**
  - **描述**: 上轮挂账「组件对拍 4 入口字符串池编号差 2（v2p=269/cn_self=271，未初始化读取嫌疑）」。本轮驻留轨迹插桩取证：**非多驻留而是「驻留顺序互换」**（`&` p 侧 272/s 侧 274，其余 3262 条逐条一致）——gdb 断点+反汇编实锤：v2p 的 解析类型 bb11 **无条件 call __cn_intern("*")**——**宿主把 &&/|| 发射为单条 And/Or 指令（RHS 无条件求值）**；v2 编译器发射=条件块短路。两编译器发射语义分歧，驻留() 池副作用使其可观测为编号漂移——诊断流/池内容/运行 rc 全同的「良性灰色点」表象实为**语义分歧指纹**。
  - **根因**: visitBinaryExpr 通用二元路径先 genExpr 两侧再发单条 And/Or 指令——无短路控制流。RHS 无副作用时布尔等价（故全量门禁零暴露），RHS 带副作用（驻留/函数调用/除法）即行为分歧。
  - **解决**（rustc 同构，用户准则性能第一/安全第二）: visitBinaryExpr 头部 AndAnd/OrOr 专用路径——IR 生成期脱糖**三块结构**：主块（LHS 求值→endBranch）→{RHS 块｜默认块（Store「真」/「假」）}→汇合块（Load i1）；And/Or 指令保留给编译器内部纯值布尔组合。门禁全绿+组件对拍 26/26 归零+固定点复验+E2E 175 运行级锚定。
  - **预防**: **①对拍「良性」差异（标签重编号/纯编号漂移/诊断全同）必须追到发射语义层定案**——两编译器语义分歧的第一现场往往只剩「无害」指纹；**②发射器新增路径前核对消费契约**（本轮连环踩两枚：endBranch 条件寄存器挂「块最后指令的最后操作数」——条件求值后夹 Alloca/Store 即挂错指令分支读错条件；i1 常量文本契约=「真」/「假」而非 "1"/"0"——loadOperandToX 仅识别「真」装 1 其余装 0）；**③驻留轨迹插桩是编译器行为对拍的标准取证法**（CN_INTERN_TRACE 环境变量开关+仅新驻留打印+ra 反解，scripts/intern_trace_resolve.py 已转正）。
  - **权重**: 10（编译器缺陷10 × 根因级1.5 × 预防价值1.4 × 已解决0.85）

## 高权重问题（第三十六轮：git checkout 回滚单文件连带丢弃同文件多轮未提交修改——回滚前必须盘点未提交轮次，2026-09-08）

- [2026-09-08 第三十六轮] **问题类型**: 工具操作事故（权重 7）**✅ 当轮识破重做（内容锚脚本可重放，损失一个重跑周期）**
  - **描述**: 拆分轮内对 语义检查.cn 连续做了 R3-2（检查表达式拆分）与 R3-3（检查语句拆分）两轮修改均未提交；R3-3 首版拼装出错后执行 `git checkout -- 语义/语义检查.cn` 想「回滚这一步重切」——结果把 **R3-2 的成果一并回滚到 HEAD**（checkout 恢复的是最后提交版，不是「上一轮修改前」）。锚断言失败报错暴露了文件已回到旧版（L176 锚对不上）。
  - **根因**: git checkout -- 的语义是「恢复到最后 commit」，而心理模型误当成了「撤销最近一次编辑」；同文件累积多轮未提交修改时，两者差距=丢失全部中间轮次。
  - **解决**: 重跑 R3-2 脚本（内容锚断言型脚本天然可重放）再跑 R3-3，两轮全部恢复。
  - **预防**: **①回滚前必须 `git diff --stat <文件>` 盘点该文件上有几轮未提交修改**——多轮未提交时禁用 checkout 整文件回滚，改用 Edit 精确撤销或先阶段性提交；**②机械拆分用「内容锚断言脚本」而非一次性手改**——脚本可重放性让回滚损失从「手工重做半天」降为「重跑一个命令」；**③拆分后每步立即构建+快测再进下一步**（本轮 24 用例快测作为每步验收门，事故隔离在单步内）。
  - **权重**: 7（工具事故7 × 可重放补救1.2 × 预防价值1.1 × 当轮识破0.8）

## 验证方法（第三十六轮：管道后 $? 是最后一个命令的返回码——rc 判定必须独立执行，2026-09-08）

- [2026-09-08 第三十六轮] **问题类型**: 验证方法缺陷（权重 6）**✅ 当轮识破**
  - **描述**: `./target/cn build ... | grep -E "错误" | head` 后接 `echo rc=$?` 得 0 误判「构建通过」——$? 取的是 head 的退出码而非构建器；且 grep "错误" 会匹配警告文本中的「错误分支」字样（v2 警告「未处理错误分支」），双重假绿。后续用 `> log 2>&1; echo rc=$?` 独立执行+grep -v 警告 过滤才见真实错误。
  - **根因**: bash 管道返回码=管道最后一个命令（除非 set -o pipefail）；关键词匹配不做语义分级（错误/警告文本互含关键词）。
  - **预防**: **①构建成败判定只用独立重定向+紧邻 $?**（`cmd > log 2>&1; rc=$?`），禁止经管道后取码；**②错误判定用行首锚定或排除警告行**（grep -v 警告 | grep 错误），关键词级别不做验收门；**③产物存在性是 rc 的第二证据**（构建 rc=0 且产物文件出现才算过——本轮以 ls 产物双确认收口）。
  - **权重**: 6（验证方法6 × 预防价值1.3 × 当轮识破0.75）

## 工程纪律（第三十六轮：Python 行切片「含头不含尾」与行号闭区间思维混用——脚本切片必须首尾断言，2026-09-08）

- [2026-09-08 第三十六轮] **问题类型**: 脚本缺陷（权重 5）**✅ 当轮全数兜住（构建语法错误即时暴露+锚断言前置）**
  - **描述**: 拆分脚本用 `行[起:止]`（Python 含头不含尾）切函数段，但行号规划按「1-based 闭区间」思维给出——`行[177:186]` 实取 L178-L186，把原分支闭括号也切进来，再手动补一个闭括号 → 双闭括号（或反向缺闭括号），R3-3/R3-4/R3-5/生成函数 各踩一次。
  - **根因**: 两种半开/闭区间口径混用且无机械防错；行号漂移后肉眼核对切片边界不可靠。
  - **解决**: 每处切片前后补内容断言（`assert 行[x] == 期望内容`），构建器语法检查作为最后防线兜底；四起全部当轮修复。
  - **预防**: **①切片辅助函数统一封装口径**（本仓库最终统一为 1-based 闭区间 切(起,止) 函数，内部做 -1 换算），调用点不再直接写原生切片；**②切片首尾行内容断言是脚本的组成部分而非可选项**——锚断言失败立即中止不写文件（无损重跑），比写出坏文件再修省一个调试周期；**③拆分类脚本的四类高频笔误清单化**：闭括号 [:-1] 误删/切片含尾/外层变量未随迁（槽c、字节）/指针参数解引用漏改（槽计数→槽计数[0]）——每类都在本轮出现过且都被构建器拦住，同类拆分可按清单自查。
  - **权重**: 5（脚本缺陷5 × 预防价值1.3 × 当轮识破0.75）

## 高权重问题（第三十五轮：宿主 IR 寄存器号跨函数累积——栈帧随模块内位置线性膨胀+误诊链纠偏，2026-09-08）

- [2026-09-08 第三十五轮] **问题类型**: 编译器缺陷（宿主 IR 层，权重 10）**✅ 当轮根因级根治（regCounter_ 每函数复位三处+单测锚定）**
  - **描述**: v2self（v2p 编译 v2 自身）默认 8MB 栈 SIGSEGV，上轮定位「解析深递归栈溢出，ulimit unlimited 可消除」——本轮 gdb 回溯实证**纠偏**：段错误时全栈仅 31 帧（根本不是深递归！）；objdump 显示 解析二元层 函数帧 `sub rsp, 0x48880`=**290KB**，20 个同构小函数探针帧从 80B 线性膨胀到 1152B——**越靠后的函数帧越大**。8MB 栈下解析递归链（每表达式嵌套层 ~10 帧×巨帧）必爆；ulimit unlimited 只是让巨大帧侥幸放进内核自动增长的栈（碰撞检测放宽），**掩盖了真因**。
  - **根因**: `IRGenerator::regCounter_` 仅在 `generate()` 全模块入口清零、**跨函数累积**；代码生成 `computeFrameSize=(maxRegId+槽)*8` 按**本函数指令中出现的最大寄存器号**定帧 → 后段函数继承了此前全部函数的寄存器号 → 帧线性膨胀。v2self 18 万行后段函数帧 290KB 实锤。**而块号 blockCounter_ 有每函数复位先例（标签唯一性由函数名前缀保证）——regCounter_ 是唯一漏网的计数器**。
  - **解决**: regCounter_ 与 blockCounter_ 同点每函数复位三处（visitFunctionDecl/emitClassMethod/泛型实例生成）——虚拟寄存器=函数内 SSA 值、跨函数仅经符号名引用，复位语义安全（v2 编译器先例=寄存器计数每函数从 1 起；rustc MIR LocalId 每函数独立同构）；修复后探针帧恒定 80B+v2self 默认栈全链 rc=0（历史首次）+字节级固定点 fix_p≡fix_s+三级不动点 g2≡g3；新增单测 `RegIdResetPerFunction` 锚定（尾部函数 maxRegId ≤ 头部+4 且 <32）。
  - **预防**: **①「段错误+ulimit 可消除」≠ 栈溢出**——巨大单帧（sub rsp 一次性越过 guard page）与深递归在 unlimited 下同样「消失」，必须 gdb bt 看**实际帧数**（31 帧 vs 数万帧一秒定性）+objdump 序言看**单帧大小**；**②计数器类静态/成员变量新增时必须回答「生命周期是什么」**——与同文件既有复位点对表（blockCounter_ 每函数复位就在旁边）；**③帧大小是 IR 分配策略的下游**——IR 层改编号语义时必须审计三后端帧计算公式的全部输入。
  - **权重**: 10（编译器缺陷10 × 根因级1.5 × 预防价值1.4 × 已解决0.85）

## 高权重问题（第三十五轮：③合并全局可见对文件级快照双计数——快照维度与消费语义必须对表，2026-09-08）

- [2026-09-08 第三十五轮] **问题类型**: 设计缺陷（权重 8）**✅ 当轮根治（全部模块ID们 映射 O(1) 去重+E2E 173 锚定）**
  - **描述**: v2self 编译 v2 自身报「符号歧义: 货舱查找依赖/解析货舱/代码生成（来自 货舱解析/货舱解析）」——定义唯一、合法程序被拒。插桩实证：歧义来源们=**同一模块文本自拼接**（「货舱解析」+"/"+"货舱解析"）。
  - **根因**: `模块路径们` 是**文件级**快照（每文件一条）；阶段目录包化后候选1b 包根（包.cn 本模块路径=模块名）与撞名成员（挂载名=同名）对同一模块路径产生**两条文件记录**；语义分析多文件 构建 `全部模块ID们` 时**直接逐条追加未去重** → ③ 合并全局可见遍历对同一模块**双计数**：同签名第二命中 `参串标==首参串标` 恒真 → 误判真歧义 E0659。宿主 findModule 模块表天然唯一化所以无此形态。
  - **解决**: 语义检查.cn 构建 全部模块ID们 加 `映射<整64,整64>` O(1) 查重（模块集合语义归一）；E2E 173（目录包根撞名成员+纯名调用同名字函数）双向锚定：修复前报「符号歧义: 设备（来自 设备/设备）」负验证实锤、修复后 rc=173。
  - **预防**: **①快照的维度（文件级/模块级）与消费语义的维度必须在接口注释里显式声明**——「每文件一条」的表被「每模块一条」的消费假设使用=静默重复计数；**②遍历集合类消费先问「来源是否含重复」**（文件清单→模块集合的归一化是加载器与语义层的接口契约）；**③「撞名」形态（目录包根=成员名）是包化的固有产物**——新包结构上线时探针必须含 撞名包+纯名调用同名字函数 全链。
  - **权重**: 8（设计缺陷8 × 预防价值1.3 × 已解决0.75）

## 验证方法（第三十五轮：陈旧链接产物误导取证——被测二进制新鲜度是取证前提，2026-09-08）

- [2026-09-08 第三十五轮] **问题类型**: 验证方法缺陷（权重 7）**✅ 当轮识破并归真**
  - **描述**: 栈帧取证中途，objdump 磁盘上的 v2p 显示 解析二元层 帧 290KB，但 v2p 刚产出的汇编（编译同一源码）同函数帧仅 256B——矛盾排查半天后发现：**磁盘 v2p 是 `cn build` 多次迭代中较早版本的链接产物**（源码插桩改动多轮，每轮 build 的链接输入含前轮 obj），拿旧二进制反汇编「定位」了半天。
  - **定位法**: 同一函数在「编译器产出的汇编」与「被测二进制反汇编」中帧大小不一致 → 必有一旧 → `nm`/时间戳核对被测产物构建链（asm→as→obj→link 各环节时间戳）。
  - **解决**: 每轮取证前强制全链重建（宿主 cmake→cn build→v2p），并在「汇编产物」与「二进制反汇编」间做同函数交叉核对后再下结论。
  - **预防**: **①假对拍家族第七案：取证对象必须是「本轮构建链」的产物**——任何 objdump/gdb/性能测量前先核对二进制时间戳 ≥ 源码改动时间戳；**②cn build 链路的中间 obj 缓存复用语义要显式确认**（lessons「增量构建假象」同族第三案：假绿/误诊都源于「以为在测新的，其实在测旧的」）。
  - **权重**: 7（验证方法缺陷9 × 预防价值1.2 × 当轮识破0.65）

## 高权重问题（第三十三轮：类型/常量布局表纯名平铺跨模块串桶——键域必须全链统一，2026-09-08）

- [2026-09-08 第三十三轮] **问题类型**: 设计缺陷（权重 10）**✅ 当轮根治（布局表/常量表键基化+三级查询+限定类型语法+类型引用诊断）**
  - **描述**: plans/018 第三十二轮完成函数符号键基化后，**结构体大小/对齐/字段类型/字段偏移/容器字段/常量 六张表仍纯名平铺**（诚实边界挂账）——跨模块同名结构体/常量登记互相覆盖：探针实测 甲 的 常量 版本=100 被主 的 版本=200 覆盖（甲模块函数读到 200）；甲 的 16 字节 记录 被主 的 4 字节 覆盖后，**sret 返回形态判定降级**（查结构体大小=4B≤8B→标量 rax 返回）→跨函数端到端读垃圾值 281458003872474（宿主 141）——布局表是「大小-槽位-拷贝-ABI 形态」的单一数据源，串桶=ABI 级错乱（非仅数值错）。
  - **根因**: 函数符号分桶时只改了「符号注册/链接键」维度，**布局数据面（表键）未随键域统一**——「分桶」不是符号表一件事，是**全编译器所有以名为键的表的键域迁移**；漏网表在依赖先解析+后登记覆盖的时序下静默串值（设置 覆盖语义），主链单模块源码（v2self 18 万行）零暴露——跨模块同名是唯一触发形态。
  - **解决**: ①全部布局/常量表登记键=键基合成(当前模块路径,名)（主模块=纯名逐字节不变）；②查询侧 结构体布局键基()/查常量值() 三级解析（限定文本拆分直查＞①本模块键基＞②直查[主模块公开设防+**键基幂等直通**]＞③纯名候选唯一公开）——**查询函数本体升级而非逐消费点改造**（查结构体大小 20+ 消费点零改动）；③分桶键形态查询（查字段偏移/查字段类型ID）改两参签名（结构体维度+字段名，内部幂等解析）12 处机械替换；④当前模块路径 静态单一归属 IR容器（语法解析窗口+IR 生成窗口共用，原 IR生成 本地静态 归入防双静态漂移）；⑤限定类型语法 甲::记录（解析类型文本 :: 合成+语句位三标识符消歧）+类型引用三级解析与未声明/私有/歧义诊断 fail fast。
  - **预防**: **①「键域迁移」类改造必须先盘点全部以名为键的表**（grep 静态 映射/静态 向量 登记点逐一判定键域），符号面/数据面/诊断面三面同步；**②查询收口优先于消费点散改**——查询函数本体三级化（幂等：直查命中=已键基/主模块纯名）让 20+ 消费点零改动，漏改面归零；**③布局表串桶的可观测形态是 ABI 变形**（sret↔标量）而非仅数值错——跨模块同名类型探针必须含**跨函数结构体传返全链**（单函数内偏移自洽=侥幸假绿，本轮红基线首版即踩）。
  - **权重**: 10（设计缺陷9 × 详细分析1.5 × 预防价值1.4 × 已解决0.85）

## 高权重问题（第三十三轮：类型文本后缀未剥离误报——文本维度判定必须枚举全部合成形态，2026-09-08）

- [2026-09-08 第三十三轮] **问题类型**: 诊断误报（回归）（权重 7）**✅ 当轮根治（E2E 150 兜住）**
  - **描述**: 新增 类型引用存在性诊断 上线轮全量 E2E 兜出 150 误报：`[错误] 未声明的类型: 整64&`——裸类型引用/泛型实参位语法消费 '&' 并入合成名（"整64&"），检查函数的内置类型清单按**裸名**比对，带后缀文本漏放行→合法程序被拒。
  - **根因**: 类型文本的合成形态不止「裸名」：实参位 * / & 并入（簇④先例）、限定名 :: 合成、容器 $ 合成——**文本维度判定的放行清单必须枚举全部合成形态**（剥后缀→基础名→$ 合成名→限定名四层），只列裸名清单=按首个用例形态编码。
  - **解决**: 检查类型引用 头部剥尾部 '&'/'*' 循环后再走四层判定；150 单用例复跑 PASS+全量 174 用例 170 过零回归。
  - **预防**: **①新诊断上线=全量 E2E 是验收门**（本轮 150/172 两枚均在全量兜出——诊断类改动「冒烟绿」不等于「全量绿」，合法形态遗漏=假阳误报）；**②文本判定函数写前先 grep 词法/语法层对该文本的全部写入点**（合成名消费惯例注释先行）。
  - **权重**: 7（诊断缺陷7 × 预防价值1.4 × 已解决0.7）

## 验证方法（第三十三轮：并行任务共享中间产物互踩——假对拍第六案同族，2026-09-08）

- [2026-09-08 第三十三轮] **问题类型**: 验证方法缺陷（权重 8）**✅ 当轮识破并定性（非编译器缺陷，串行复跑全绿）**
  - **描述**: 全量门禁轮为省时将「宿主 E2E 全量」与「v2self 组件对拍」并行后台运行——两者都在项目根操作**全局单例中间产物 target/v2asm.s**（v2 闭环用例的 v2p 编译产物 vs 对拍脚本的组件编译产物）：E2E 126 报「v2p 未生成 target/v2asm.s」（刚写完被对拍脚本 rm）、对拍 代码生成 组件 a 产物仅 1443 行（cp 时拿到 E2E 某小用例产物）——两处失败形态互为因果，同根=编排竞态。
  - **定位法**: 对拍失败侧产物行数量级异常（1443 vs 14 万）→ 查编译日志 `[cg1] asm 行数=146871` 正常 → 判定「产物在编译后被调包」→ 关联并行任务写入同一路径。
  - **解决**: 串行复跑：E2E 全量 174 用例 170 过/0 败/4 跳+组件对拍 13/13+字节级固定点全绿。
  - **预防**: **①v2 编排的一切产物路径（target/v2asm.s、target/主、target/audit2/）在同一工作区是全局单例——任何两个涉及 v2 编译的任务禁止并行**（asm 相对 cwd 纪律的编排面推广）；**②对拍产物行数量级突变=「产物被调包」指纹**（编译器日志正常+产物异常→查写者而非查编译器）。
  - **权重**: 8（验证方法缺陷9 × 预防价值1.2 × 已解决0.75）

## 高权重问题（第三十三轮呈报：v2 结构体构造字面量语法缺口——宿主/v2 语法覆盖面差距，2026-09-08）

- [2026-09-08 第三十三轮] **问题类型**: 语法覆盖缺口（呈报，权重 6）**⏳ 挂账待裁决（自举刚需面外，随语法覆盖轮评估）**
  - **描述**: 探针实测 v2 对 `记录 r = 记录{ 数量 = 7 };`（宿主 E2E 56 使用的结构体构造字面量）解析失败（语句缺少分号+后续语句丢弃级联）——v2 结构体初始化仅支持 声明+逐字段赋值 形态（E2E 148 模式）。宿主已支持构造字面量（E2E 56 锚定），v2 语法覆盖面差距=自举目标差距的既有类型（与 字符串下标 129、结构体返回 148 等同谱系，非本轮引入）。
  - **呈报理由**: 缺陷零容忍纪律——本轮类型分桶 E2E 172 若用构造字面量形态写将不可编译；缺口已探明（语法层 解析后缀链 无 `{` 构造分支），修复=语法构造分支+IR 聚合初始化（预估中等改动面），随下一语法覆盖轮排期。
  - **权重**: 6（语法缺口6 × 自举影响1.3 × 已呈报挂账0.75）

## 高权重问题（第二十九轮：v2 静态槽非对齐链接截断——对齐是发射期布局契约，2026-09-07）

- [2026-09-07 第二十九轮] **问题类型**: 代码生成缺陷（权重 9）**✅ 当轮根治（用户裁决方案A·双侧归正；v2self 锚定链跑通）**
  - **描述**: v2self 锚定链（v2p 编译 v2 主.cn 产物 as+g++ 链接）失败：`relocation truncated to fit: R_AARCH64_LDST64_ABS_LO12_NC against '.data'`。根因=v2 ARM64 后端静态槽（`_cn_gstatic_N` **本地标签**）紧跟任意长度字符串 `.byte` 之后无对齐——GAS 把本地符号引用折叠成 `.data+非对齐addend`（readelf 实证 `_cn_gstatic_0`=0x2b9a，非 8 对齐），64 位 ldr/str 的 LO12 重定位要求 addend 8 对齐 → 链接截断。宿主 arm64 后端同位置同样无对齐，但静态符号是 **全局符号**（命名重定位，汇编期不做对齐检查）侥幸可链接——代价=运行期静态访存全非对齐（ARM64 硬件容忍但性能损耗）。小用例靠字符串池凑巧对齐从未暴露；x86_64 RIP 相对寻址无此限制（深度机 v2self 锚定三轮全绿）——**arm64 全链自举首次链接即现形=又一例「平台差异掩盖面」**。
  - **定位法**: readelf -sW 查符号落位（LOCAL+section 3+offset）→ objdump -dr 反汇编引用点（`adrp x9, .data+0x2baa` + `ldr x9,[x9]`）→ 对位回 .s 源（`adrp x9, _cn_gstatic_2`）→ 手动插 `.align 3` 重汇编链接运行全通=假设验证闭环。
  - **解决**（用户裁决方案A，Rust 对照后）: v2 代码生成ARM64.cn 静态槽区前补 `.align 3`（根治）+ 宿主 arm64_codegen.cpp 静态槽循环前补 `.align 3`（运行期对齐访存归正）。**Rust 对照**：rustc/LLVM 为每个全局按 `align_of(T)` 自动发射 `.align`（对齐=发射期布局契约的一部分），字符串常量进 .rodata 按 align 1、可变静态按元素宽度对齐；安全语义上非对齐访问=UB（须显式 `read_unaligned`），绝非「能用但慢」。方案C（v2 符号 .globl 全局化绕过检查）被否=「让检查看不见」不是「把布局做对」。
  - **预防**: 新后端/新数据段发射必须遵守「每个 8 字节及以上的全局/槽自带对齐指令」（LLVM 逐全局 .align 模型）；链接器不报错≠布局正确（全局符号命名重定位会掩盖非对齐）；跨后端对表审计清单新增「数据段对齐」维度。
  - **权重**: 9（代码生成缺陷9 × 预防价值1.2 × 已解决0.85）

## 高权重问题（第二十九轮：154 同族缺陷 arm64 孪生——同族修复必须旁路穷尽，2026-09-07）

- [2026-09-07 第二十九轮] **问题类型**: 代码生成缺陷（权重 9）**✅ 当轮根治（用户裁决方案A·单一锚定源；E2E 154 arm64 转绿）**
  - **描述**: 跨机轮全量 E2E 中 E2E 154（linux_x64 隐藏返回指针栈参数锚定，第二十二轮修复的回归锚定用例）在 arm64 首跑失败：期望 87654321/8/87654321，实得 5497169789127654321/549716978912/87654321；垃圾值 0x7FFDAF6CE0=**栈地址**（被调方保存的旧 x19）。根因=arm64 后端 emitParamSetup 标量/浮点栈参旁路**硬编码锚定基 16**，无视 needHiddenRet（sret/i128/u128 返回时 prologue 多压 `stp x19,xzr` 16B，真实栈参上移到 [x29,#32]）——第二十二轮修 linux_x64 三处时以 arm64 的 `parameterRegister` 为「同构正确对照」，**漏查了 arm64 不走 parameterRegister 的两条旁路**；结构体栈参路径恰好走 parameterRegister=正确，故仅标量/浮点栈参形态翻车。
  - **教训链**: ①「对照点正确 ≠ 该侧全路径正确」——引用某侧一处实现作对照即断言该侧无恙=结构性漏检（同「抽样 grep 缩窄=漏检」家族）；②三后端同一 IR 语义的 ABI 实现，修复后必须**逐锚点/逐寄存器对表穷尽**（对既有「逐寄存器角色对表」教训补锚点维度）；③新用例不进平台跳过清单=全平台承诺，跨机轮首跑即兑现日（154 创建于无 arm64 运行能力的机器，首跑在下一台机器=机制必然）；④定位法：垃圾值 0x7FFDAF6CE0 识别为栈地址→推断读到 saved x19→asm 直查 prologue 压栈序 vs 锚定偏移夹逼。
  - **解决**（用户裁决方案A）: arm64 新增 `stackParamBase()`（needHiddenRet?32:16，与 parameterRegister 同源单一归属），标量/浮点两旁路改用之；浮点槽序从源序号 i 改按参数位号 paramPos（与调用方 v<位号> 装载统一）；linux_x64 `intParameterRegister` 栈分支收敛至 `stackParamAnchorBase()`+注释归真（原「与 rbx 压栈无关」系第二十二轮证伪的旧叙事）。新增单测 3 项（sret 标量锚 [x29,#32] 反证 [x29,#16] / 平凡路径锚 16 不受扰 / sret 浮点位号+栈锚双断言）。
  - **预防**: 同族缺陷修复的验收=「全后端同型消费点 grep 穷尽+逐点判定」，禁止以单点对照替代；修复轮对非本机后端的旁路=诚实边界呈报（对照点≠全路径）。
  - **权重**: 9（代码生成缺陷9 × 预防价值1.2 × 已解决0.85）

## 验证方法（第二十九轮：对拍规范化基准必须跨文件确定——假对拍第五案，2026-09-07）

- [2026-09-07 第二十九轮] **问题类型**: 验证方法缺陷（权重 6）**✅ 当轮识破并修正（13 组件对拍全 PASS）**
  - **描述**: 重写 strData 驻留 ID 规范化工具（norm.py 未随迁，p29 自建）时，规范 ID 取「**本文件内**首次出现 ID」——但 v2p 与 v2self 两产物族的 ID 基准本身相差 1，同一内容在两文件分别规范化为 242/241 → 规范化后仍不一致（9 文件假 FAIL）。失败方向=假阴，未污染结论。
  - **解决**: 规范 ID 改为「**内容键排序秩**」（跨文件确定命名空间：同内容必得同秩）后 9 文件全部规范化一致。**假对拍第五案**（①cwd 陷阱/②判定行自比/③入口回退/④路径依赖未随迁/⑤规范化基准不跨文件确定）：共同根因仍是判定链完整性——规范化工具的映射目标空间必须与被比较文件族无关（确定性）。
  - **预防**: 对拍判定工具的中间表示（规范化 ID/标签名）必须由「内容派生」而非「文件内位置派生」；新写对拍工具先用已知平移样本自检（构造 ±1 平移样例验证工具本身）。
  - **权重**: 6（验证方法缺陷9 × 预防价值1.0 × 已解决0.7）

## 高权重问题（plans/017 两灰色点根治轮：对拍脚本目录迁移路径依赖未随迁——假对拍第四案，2026-09-07）

- [2026-09-07 第二十八轮] **问题类型**: 验证方法缺陷（权重 9）**✅ 当轮识破并修正（norm.py 随迁后 8 用例对拍 8/8 PASS）**
  - **描述**: 第二十七轮 probe.sh 复制改造为第二十八轮版本时（sed 替换 D=/tmp/v2dbg/p27→p28），脚本内 `python3 $D/norm.py` 的**路径依赖文件 norm.py 未随脚本复制** → 8 用例全部「产物不一致 FAIL」（失败形态=norm.py 打不开而非产物真差异）。幸好失败方向是假阴（FAIL 而非假 PASS），未污染结论。
  - **解决**: cp norm.py 随迁后重跑 8/8 PASS。**假对拍第四案与前两案同根（①cwd 陷阱/②判定行自比/③入口回退/④路径依赖未随迁）：共同根因=判定链的完整性依赖未随脚本迁移**——脚本改造（复制/sed/搬目录）后必须通读判定链上全部文件依赖（脚本/判定工具/基线产物三件套缺一不可）。
  - **预防**: 复用历史对拍脚本时清点三件套：脚本本体+判定工具（norm.py 等）+基线产物（p27/*.s），全部随迁或改绝对路径；假阴失败（工具缺失=FAIL）也要当轮定位，禁止「先跑通再说」式跳过。
  - **权重**: 9（验证方法缺陷9 × 预防价值1.1 × 已解决0.85）

## 高权重问题（plans/017 v2self 三级锚定轮：入口回退掩盖编排错误——假对拍第三案，2026-09-07）

- [2026-09-07 第二十七轮] **问题类型**: 验证方法缺陷（权重 10）**✅ 当轮识破并修正（8 用例真对拍 8/8 PASS；v2self 字节级固定点达成）**
  - **描述**: v2self 探针对拍脚本入口路径双拼（变量已含 target/ 前缀又拼 target/$srcdir → `target/target/audit2/...`）→ v2 编译器打开入口失败后**回退内置回归源码**（[p6f] 打印一行但继续编译，rc=0 产 asm）→ 8 用例「产物一致+运行 rc 全对」假全绿（rc=19 实为内置回归断言值，产物只有主函数、缺导入模块函数）。同脚本另有 glob 被引号包裹不展开（cp 失败）——两处均在重跑后暴露。
  - **解决**: 修正路径拼接+glob 引号；**识别线索固化为纪律**：编排类对拍（手工跑 v2 编译器）必须核对 `[p6h] 入口:` 行（路径正确）与 `[p6f]` 回退行（不得出现），多文件用例另查 `[ir1] IR生成调用后 函数数=N`（N=期望函数数，单函数=回退铁证）。
  - **预防**: **假对拍三案谱系：①cwd 陷阱（asm 相对 cwd，cp 到旧产物）→②判定行自比（脚本改造把判定链改成同编译器自比）→③入口回退（编译器对坏路径静默换源继续）——共同根因=「编译成功」信号被劫持**。对拍 PASS 前必须确认三个真实：入口真实解析（[p6h]/函数数）、产物真实新鲜（时间戳/行数匹配刚编译源）、判定真实双向（两个产物来自两个编译器）。另：v2 编译器「打开失败回退内置源码」对无参自检模式是设计特性，但对编排错误有掩盖性——负路径用例（139 等）不受影响（入口真实存在），手工编排必查回退行。
  - **权重**: 10（验证方法缺陷9 × 详细分析1.5 × 预防价值1.5 × 已解决0.8）

## 高权重问题（plans/017 T3/T4 解锁暴露回归：平台跳过掩盖面——解锁日=欠账日，2026-09-06）

- [2026-09-06 第二十六轮] **问题类型**: 验证方法缺陷+机制改动回归（权重 10）**✅ 当轮两枚全根治（用户裁决均方案A：声明收集双键登记+自检剥签名后缀；全量 160 用例 156 过/0 败/4 跳）**
  - **描述**: 第二十五轮重载消歧机制（声明收集登记键改签名键+生成函数 IR 名=签名键）落地时门禁全绿（linux-x86_64 平台 159 用例 132 过/0 败）——但 v2 闭环 23 例在 linux-x86_64 平台跳过清单里从未真跑，机制回归被掩盖：①【150 语义回归】`是引用返回调用`（语义分析.cn 独立查纯名表）未随调用分支一起加签名表回退→纯名查不到恒判假→引用返回赋值目标/复合赋值/引用初始化三形态全拒（150 解锁首跑 3 错中止；v2p_fix 基线对拍实锤为第二十五轮引入；win 侧同源码同红=非平台特异）；②【125 自检行】打印函数IR 打 IR 名（=签名键）带 `#整64` 后缀 vs 期望行固化旧文本（非运行缺陷）。
  - **解决**: ①声明收集**双键登记**（签名键恒登记+纯名键同名唯一时补登记——与 IR生成.cn 三张表双键同构；真重载候选>1 不登纯名键仍走签名表回退，157 行为不变；跨文件同纯名先文件赢=旧静默吞同款=模块遮蔽语义 P6b 注记）；②主.cn `自检显示名` 剥首个 # 后缀打声明名（CN 标识符无 #，剥离安全；125 期望文件零改动）。
  - **预防**: **平台跳过的用例不是不存在的用例——解锁日=欠账日**：影响所有程序语义/符号/输出文本的机制性改动，落地轮必须在全平台矩阵验证，或显式审计「被平台跳过用例」的形态覆盖面（列出跳过用例清单逐一问「本机制是否触及其锚定形态」）——「本机门禁全绿」不等于「机制无回归」，只等于「本机可见面无回归」。新机制上线自查问题：哪些既有消费点直接查表？各自有没有回退链？（本轮 `是引用返回调用` 正是「独立查表无回退」的漏网点——与调用分支同源数据不同路径）。
  - **权重**: 10（验证方法缺陷9 × 详细分析1.5 × 预防价值1.5 × 已解决0.8）

## 高权重问题（plans/017 呈报③方案A 实施：重载消歧 fail fast 双拦截+假对拍二度复现，2026-09-06）

- [2026-09-06 第二十五轮] **问题类型**: 设计缺陷+验证方法缺陷（权重 10）**✅ 当轮全部根治（E2E 157 双绿+九探针真三方对拍 9/9+v2self 新一代自举锚定）**
  - **描述**: ①重载决议初版两个语义缺陷均被自建 fail fast 机制当场拦截而非静默错误产物：参数计数假匹配（候选参数个数误用实参数初始化从未真数——任何非空参数串候选都判「个数匹配」，2/3 参重载误报「决议失败」，改名回归实测 3 错中止）+实参索引偏移（调用节点子区间 [子起点]=被调标识符节点（节点_标识符=10），实参从 [子起点+1] 起——初版首实参读到被调节点推导失败、字面量重载全落宽匹配→假「决议歧义」，min18 最小探针 [d18] 插桩实证 实参0 段=[]+节点类型=10 后修正）。②假对拍二度复现：v2self 对拍在 /tmp cwd 下跑编译器、cp 的却是项目根 target/v2asm.s 旧产物（asm 输出相对 cwd）——min1「逐字节一致」实为主.cn 产物自比；对拍脚本 sed 改造时又把判定行改成同编译器自比（reg3.sh 9/9 假绿）——两次假绿均靠重读脚本全文识破。
  - **解决**: ①参数个数=逗号数+1 真数（无参串=0）；实参索引=子起点+1+i（与 类型大小 折叠分支同款，注释锚定）。②重写干净三方对拍脚本（reg4.sh：v2p_re vs v2self_new 判定链通读确认），min1/min18 产物逐字节一致+九探针 9/9。
  - **预防**: **①fail fast 诊断是缺陷探测器不是障碍**——决议类新机制上线时「歧义/失败诊断触发」优先怀疑机制 bug 而非源码（本轮两次触发=两次真 bug，静默版将产出错决议产物三层后才显形）；**②调用节点子区间约定=[子起点]被调+[子起点+1..]实参——任何消费实参节点区间的新代码必须按 +1 偏移（类型大小 折叠分支为既存权威先例）**；**③假对拍 cwd 陷阱升格为强制纪律：对拍脚本每次改造后必须通读「编译→cp→判定」全链，确认两个产物来自两个不同编译器；asm 相对 cwd 纪律在临时对拍（非正式脚本）中同样强制——cp 前必须确认 target/v2asm.s 的时间戳/行数与刚编译源匹配**。
  - **权重**: 10（设计缺陷9 × 详细分析1.5 × 验证方法价值1.5 × 预防措施1.3 × 已解决0.8）

## 高权重问题（plans/017 呈报①方案A 第五轮：第 16 枚根治——v2 指针参数三重缺陷，2026-09-06）

- [2026-09-06 第二十三轮] **问题类型**: 设计缺陷（权重 10）**✅ 第 16 枚根治（v2self 编译 f2 全链 exit=0+九探针三方对拍 8/9）；第 17 枚立案（mapref2 引用参数槽 miss）**
  - **描述**: v2self 容器探针 IR 生成阶段 SIGSEGV（第 16 枚）根因=**指针参数三重缺陷**：①`出.参数串=`（出=方法签名*）左值发射误走 IR_取地址（取指针槽自身地址，少一层解引用）+成员偏移语法层未标注（-1 未知标记）直发 IR_字段地址.附加=-1 → 目标地址=实参们槽+7，8 字节写把返回值 LE 首字节 0xd0 叠入句柄槽最高字节 → 句柄 0xd00000000177eca0 解引用崩；②参数收集把 方法签名* 当结构体值参数（32B 4 槽+prologue 解引用拷贝）→ 出.基槽=*签 首 8 字节（=签.参数串 值 0）而非签地址；③右值成员访问同款基址缺陷。
  - **解决**: 修复五处=语法三处 变量结构体表 登记放宽 `附加<=256`（指针形态登记→成员偏移真值标注）；IR生成 指针参数槽们 机制+指针参数单槽直存（拷贝字节=0/槽数=1）；生成左值地址 三态基址（引用槽→栈顶/指针槽→IR_加载/其余→取地址）+成员偏移分桶兜底+毒值 IR层诊断拒绝；右值成员访问同款。**Rust 准绳**：p.字段≡(*p).字段（v2.1 裁决真正落地）；偏移未知=编译期错误 fail fast，绝不发射 -1 毒指令；单槽直存零运行时开销。
  - **预防**: **①「add $0xffffffffffffffff,%r10」（objdump）=IR_字段地址.附加=-1 毒指令的物化形态——修复后必须全文 grep 审计清零（60→0），新增成员访问发射点必须带偏移未知拒绝**；**②v2 源码分步插桩对拍法=编译器分叉缺陷首选**：v2p 与 v2self 跑同一插桩源码逐行 diff stderr，分歧行即污染时点，每轮一次重建推进一层（比 gdb 单打快一个量级）；**③watchpoint 触发报告必须配值形态判读**——污染值=旧值低位+新写单字节=1 字节写形态，且 8 字节写目标可能错位 ±7；gdb hw watchpoint 报告 PC=写指令下一条，SIGSEGV 会混入伪触发（须检查 new_byte 是否可读）；**④重建链原子性**：v2p_rt.o 重编后必须重链 v2p 可执行——漏链=旧编译器编译旧产物=「修复无效」假象（本轮浪费一次全链对拍）；**⑤指针层信息在类型节点附加，权威表存类型文本会丢失——跨表传递指针语义必须显式（指针参数槽们），不得靠「文本带星」**。
  - **权重**: 10（设计缺陷10 × 详细分析1.6 × 验证方法价值1.4 × 预防措施1.4 × 已解决0.8）

## 高权重问题（plans/017 呈报①方案A 第四轮：第 7 层根治+v2self 首次完整运行，2026-09-06）

- [2026-09-06 第二十二轮] **问题类型**: 设计缺陷（权重 10）**✅ 第 14/15 枚根治（v2self min1 全链 exit=0+产物逐字节一致）；第 16 枚立案（实参们槽高半污染写者未定位，下轮插桩续挖）**
  - **描述**: v2self 第 7 层（min1 最简源 IR 生成阶段崩）两层根因：**第 14 枚**=`查方法签名` 返回形态判定「含容器字段的结构体→标量」（无 retbuf 位）——系 E2E 127 时代「供给.cn 类形态供给」专属契约（主.cn 结构体声明×供给类实现=**同名双 ABI**），与宿主真语义（sret：rdi=retbuf/rsi=this/rdx=位置，三方 asm 实证+127 直编 exit=0）分叉→v2 发射 rdi=this/rsi=实参→this/实参整体错位（gdb 实证 元素 方法入口 this=0/位置=0xffffffff→运行时错误3）；**第 15 枚**=含容器字段结构体「调用返回初值」（`函数IR 扫函数 = 程序.函数们.元素(f);` sret 浅拷贝）被 容器字段局部们 无条件登记 RAII→出口级联析构=free 调用方容器数据→后续迭代读/再析构已释放内存→invalid free SIGABRT（收集字符串常量 bb177_7 向量$整64$析构 崩点回溯）。
  - **解决**: 第 14 枚=判定改 `查结构体大小!=0` 一律 sret（容器类不在结构体大小表→自动仍标量）+127 供给.cn 词条 类→结构体（同种类同布局，w.表 显式构造初始化）；第 15 枚=登记条件加「初值末节点=调用且非构造（结构体无构造调用）→不登记」——与第 9 枚「容器变量调用返回初值不入 RAII」同款豁免。**Rust 准绳落地：类型即布局、布局即 ABI（同名类型禁止双 ABI）；大对象按值返回=sret 与 Rust/LLVM codegen 一致，零额外开销（性能无损）+单一 ABI 消灭 this/实参错位家族（安全）**。
  - **预防**: **①「供给契约」类双态形态（声明×实现不同 ABI）是分叉温床——历史规避性判定必须回溯宿主 asm 真语义再定夺（asm 三方实证：被调方签名+调用方装载+直编运行）**；**②跨编译层豁免要成对：容器变量的豁免（第 9 枚）落地后，同语义的「含容器字段结构体」形态（第 15 枚）必在首个大规模程序暴露——修一枚查同族**；**③gdb 断点读参数必须断在 call 指令前（断在下一条=读到被调方执行后的残值，本轮曾因此误判 str_sub 参数错乱浪费一轮）**；**④链接 muldefs 双 obj 的可执行文件运行地址≠obj 偏移+基址——反汇编定位一律用可执行文件本身（.o 换算必错位）**；**⑤watchpoint 是绝对地址跨函数存活——被调函数返回后栈复用的正常写入会触发（误报），监控「活跃帧内写入」须结合栈帧生命周期判读**；**⑥单组件子链冒烟与主链合并可见性不同——冒烟口径必须在加代码后重跑，「全绿」记录才可信（本轮 IR容器.cn 单组件红系上轮遗留：引用未导入模块常量，主链掩盖）**。
  - **权重**: 10（设计缺陷10 × 详细分析1.6 × 验证方法价值1.2 × 预防措施1.4 × 已解决0.8）

## 高权重问题（plans/017 呈报①方案A 第三轮：容器所有权语义对齐六枚+v2self 至 IR 生成，2026-09-06）

- [2026-09-06 第二十一轮] **问题类型**: 设计缺陷（权重 10）**✅ 六枚全部根治（九探针对拍一致全 PASS+E2E 155 转正）；v2self 第 7 层（IR 生成基础路径）下轮继续**
  - **描述**: v2 容器所有权语义与宿主系统性分叉六枚：①池索引 0 参数类型节点被 `>0` 守卫漏登记**三处同族**（收集函数实参结构/生成函数权威表/语义分析静态声明——首函数首参数类型节点恰在池 0 时引用实参位漏收集→调用方不取址→被调方双重解引用崩）；②`容器=方法返回` 接收却 RAII 析构（宿主元素(i) 返回**内联元素地址视图**不析构——v2 对向量内部元素 DeleteObject=double free）；③`容器=标识符` 浅共享（宿主=NewObject+拷贝构造**深拷贝**，asm `_A_T`=`_`+A(引用)+_T(hex) 实证）；④`返回 容器局部` 仍被析构（跳过名只覆盖结构体返回→返回悬垂指针）；⑤`结果<T=结构体>` 大小/槽位 16B 截断（结果大小 只算 12/16→retbuf 越界写+变量槽被 24/40B 拷贝写穿）；⑥`结果.值` 结构体载荷被 LoadPtr（读载荷首 8 字节当地址——rep movsb rsi=47 小整数实证；注释声明「压字段地址」语义但实现缺失）。
  - **解决**: 三处 `>=0`；预扫容器声明「调用返回初值」不入 RAII；标识符拷贝改 NewObject+拷贝构造（源=T& 传**左值地址**，静态源新增 IR_静态地址 指令三后端 lea/adrp+add）；跳过名泛化（末节点=标识符即设）；结果大小/槽位数支持结构体载荷+逐槽 IR_分配+基槽=最大槽号；载荷结构体成员压字段地址+回扫补 IR_字段地址 复用。**宿主容器值语义四探针 asm 判明**：标识符拷贝=深拷贝／元素(i) 接收=视图（写共享、不析构）／追加=深拷贝入容器／函数返回接收=转移不析构——「调用返回不析构、标识符拷贝深拷贝」为 v2 对齐基线。
  - **预防**: **①「所有权」必须以被调方 ABI 实证为准（asm 级读被调参数语义）——「接收=拥有」想当然假设在元素(i)（返回内部元素地址）形态下=对宿主数据结构 double free**；**②容器/结构体「大小-槽位-拷贝字节」三量同源（一处算错=帧布局连环越界写穿相邻槽——asm 帧偏移逐步核对 [rbp-N] 写穿方向是定位金手段）**；**③「映射在映射集合模块」（容器模块无映射）——探针的 导入 错误会让 v2（语义无校验）假绿而宿主真红，「对拍基线」必须先验证探针源本身合法**；**④变量名可能触发宿主解析边界（「加载荷」三字名声明后「未声明的标识符」，改名即过）——遇诡异未声明先试改名隔离**；**⑤shell 管道会吞真实退出码（`cmd | tail` 后 `$?`=tail 的）——验证命令必须独立跑 exit 或用 PIPESTATUS**。
  - **权重**: 10（设计缺陷10 × 详细分析1.6 × 验证方法价值1.2 × 预防措施1.4 × 已解决0.8）

## 高权重问题（plans/017 呈报①方案A 第二轮：否则如果链双重生成+v2 IR 净化，2026-09-06）

- [2026-09-06 04:30] **问题类型**: 设计缺陷（权重 10）**✅ 根治（IR 净化 56%：主.cn asm 418388→183238 行；13 组件冒烟全绿）；剩余「容器& 引用方法调用」层下轮继续（呈报①方案A 进行中）**
  - **描述**: v2 生成语句 预扫描只收集「如果语句」的**首层体块**，「否则如果」的**嵌套如果根未被跳过**——主遍历遇内层根先整链生成一遍、外层 生成如果 递归再生成一遍=**双重/多重生成**（chain 探针 4 层链：7 次生成如果而非 4 次；IR 里同块字面量/指令重复）。**这是「v2 编译的 v2 自身（v2self）崩溃」的主根因之一**：重复块执行+寄存器/槽错位级联。**历史全部冒烟假绿**（exit=0+语义 0 错+asm 行数=「产出存在」——重复生成恰为幂等赋值时行为不变；E2E 125 控制流用例未覆盖嵌套链副作用形态；产物从未汇编/运行）。
  - **解决**: 预扫描沿「子终点（否则索引）」整链收集嵌套如果根+内层真块入跳过集。dbg6/dbg5/dbg7 三层插桩闭环（字段值序列 63,52,63,41,52,63,30 与递归结构精确吻合=确认遍历类缺陷的金标准）。
  - **预防**: **①「控制流体」的跳过收集必须覆盖「链式嵌套语句根」（否则如果/否则）——只收集体块会漏根，主遍历与递归各生成一遍**；**②「asm 行数」是质量信号：同源码产物行数突变（-56%）=深层结构错误的暴露面，行数基线应纳入冒烟比对**；**③分步打印插桩（在关键函数入口逐层加 IO 打印）比纯 gdb 在「无符号 v2 代码」上快一个量级——「解析后池 dump」一步分辨「池错 vs IR 生成读错」**；**④v2 产物从未被真实运行过——首跑逐层暴露存量缺陷是预期规律（首个大规模程序是最强探针），逐层修复不要试图一次收口**。
  - **权重**: 10（设计缺陷10 × 详细分析1.6 × 验证方法价值1.2 × 预防措施1.4 × 已解决0.8）

## 高权重问题（plans/017：v2 产物级验证空白照出宿主栈参锚+ v2 四缺陷，2026-09-06 深度系统 x86_64）

- [2026-09-06 01:30] **问题类型**: 设计缺陷+验证方法缺陷（权重 10）**✅ 当轮全部根治（单测 1240/1240+E2E 156 用例零回归+探针对拍一致）；v2self 完整运行新缺陷另行呈报**
  - **描述**: plans/017 v2 linux-x86_64 专项 T5 冒烟揪出五层叠加缺陷：①**宿主 linux_x64 被调方栈参锚缺 needHiddenRet 补偿**（push rbx 使栈参在 [rbp+24+8k]，旧锚 [rbp+16] 读**返回地址**当第 7 参——建IR指令 7 参+返回结构体正中，IR 全链污染产物无法汇编；单位机历史绿=arm64 宿主构建，**同源码不同宿主后端构建的 v2p 行为分叉掩盖缺陷**）；②v2 静态赋值存储丢失（防御分支静默降级家族）；③v2 限定名内置 结果 接收把裸 char* 当 16 字节结构体 IR_结构体拷贝；④v2 返回含容器字段结构体：浅拷 retbuf 后 RAII 析构局部向量=悬垂（E2E 148 结构体无容器字段故未暴露）；⑤跨文件同名函数 IR 名无参数签名→asm 重复定义＋**②' win 发射器除/余 xor rdx,rdx 负被除数 #DE（纪律重贴后当场根治=cqo+idiv）**。**共因=v2 产物级验证历史空白**（asm 只验产出未汇编、v2 闭环 E2E 平台跳过、冒烟=「exit=0+语义 0 错」假绿）。
  - **解决**: ①锚 16+8*paramOffset（arm64 同构对照）+E2E 154 红→绿+单测；②静态序号们 查表→IR_静态存储；③限定内置初始化合成构造（判空→正常@0、指针→值@8）；④析构器加跳过名ID（所有权转移）；⑤生成函数→生成函数W。定位方法：**gdb 硬件 watch 目标槽的写入序列 + objdump 写方/读方/调用点三方偏移对表 + 「栈参数错位 8 字节=读到返回地址」指纹**（污染值全为代码段地址=栈上方数据指纹）。
  - **预防**: **①「产物验证」必须到「汇编通过+真实运行」层——「产出存在+语义 0 错」是假绿（本缺陷自 plans/016 落地起潜伏 3 轮，跨机冒烟全部漏网）**；②**多后端移植的「栈锚/帧布局」类参数必须按 prologue 的实际 push 序列重新推导**（arm64 有补偿而 linux_x64 漏——「win 没炸」不能当移植依据的同族教训再+1）；③**「同源码不同宿主后端构建的产物」是不同测试对象**——平台绿基线不能互相背书（单位机 arm64-v2p 绿≠本机 linux_x64-v2p 正确）；④返回值含资源字段（容器/指针）的结构体，**返回路径必须做所有权决策**（转移=跳过析构/深拷贝），浅拷+析构=悬垂；⑤C ABI 内置（返回裸指针）与 CN 层 结果<> 接收的**边界契约在 IR 层要有显式合成点**（不能套用「返回结构体」通用拷贝路径）。
  - **权重**: 10（设计缺陷10 × 详细分析1.6 × 验证方法价值1.2 × 预防措施1.4 × 已解决0.8）

## 高权重问题（plans/018 包/货舱 死保留字摘除——立规过度保留翻案，2026-09-07）

- [2026-09-07 第三十一轮下半程] **问题类型**: 设计缺陷（权重 8）**✅ 用户裁决方案A 当轮摘除（规范 61→59+实现+单测反转+E2E 166 锚定）**
  - **描述**: v2.0 模块系统立规（2026-08-15，specs/01a 5.3b）把 `包`/`货舱` 立为关键字（关键字 58→61），但两词在 CN 源码语法中**没有任何语法位置**——`包.cn`/`货舱.toml` 是文件名约定、`[货舱]`/`[依赖]` 是 TOML 数据文件文本，parser 全源码对 Kw_Package/Kw_Cargo **零消费点**（纯死保留字）；实际剥夺标识符自由（v2 源码 `货舱配置& 货舱` 参数名当场报「预期参数名」，被迫改名 舱单 规避）。用户质疑「01标准里没有货舱关键字」触发复查——事实是 01a 写了（立规与实现一致），但**立规本身过度保留**，用户裁决方案A 摘除。Rust 对照：`cargo` 非保留字（Cargo.toml 只是文件名）。
  - **解决**: 摘除 lexer 关键字表/枚举/名字表三处+单测四文件按「从」删除先例反转（isKeyword 断言不得引用已删枚举）+specs/01a 关键字表 61→59+变更记录补条+plans/001 注记+E2E 166 钉死 货舱/包 可作变量名/函数名/参数名。`模块`/`作为` 有真实语法（模块 X; / 作为 别名）保留。
  - **预防**: **立规关键字必须有真实语法消费点**——立规前先写语法/解析消费设计，文件名约定/数据文件词汇不进保留字表（保留字最小化，C++/Rust 同纪律）；复查「X 是不是关键字」类争议先查三处：规范关键字表（立规事实）→ lexer 表（实现事实）→ parser 消费点（语法必要性）——三者齐全才合法保留，缺第三者即死保留字候选。
  - **权重**: 8（设计缺陷8 × 预防价值1.2 × 已解决0.85）

## 高权重问题（第三十二轮：静态容器引用实参双侧同构缺陷——取地址语义丢失，2026-09-07）

- [2026-09-07 第三十二轮] **问题类型**: 宿主缺陷+v2 同构缺陷（权重 10）**✅ 双侧当轮根治（宿主 ir_expr AddressOf 静态分支 + v2 回扫实参取地址 静态分支；14 组件冒烟+三级锚定全绿）**
  - **描述**: v2self 编译「语义分析多文件(7 参含 6 个 `&` 引用形参)」首跑 [ir2]×5 结构体实参拒绝+内置源码冒烟错误码3。根因=**顶层静态容器作引用实参**时双侧同构缺陷：①宿主——语义层 wrapRefArgs 将实参包装为 `&静态`，IR 层 AddressOf 分支 `lookupVarName` 对静态变量为空落 `genExpr` 读值兜底（lea 符号+LoadPtr 读句柄值），取地址退化为读值——引用形参收到句柄值，被调方对引用形参的读取=再解引用一次：空容器句柄 0=错误码3、非空=读写容器对象头错位（**侥幸路径家族**：局部容器走「结构体按值语义=AddrOf 槽」天然正确，故同模块/纯局部形态从未暴露）；②v2——回扫实参取地址 白名单只有 IR_加载/指针加载/字段地址/调用参数化/取地址，静态标识符实参发射 IR_静态加载（读句柄）不被回扫→引用形参同样收到句柄值。**「add 层解引用丢失」与宿主 154 同族教训同源：修复轮对非本机后端/非本路径的旁路=诚实边界呈报——本例=同缺陷双侧各一处，一侧修复不覆盖另一侧，双侧穷尽才算根治**。
  - **定位法**: ①运行时错误(3) 是受控错误非 SIGSEGV，gdb 断 __cn_runtime_error 回溯→#1 向量$整64$大小（this 空）→#2 语义分析多文件→反汇编被调方引用参数槽读取（mov (%r10) 解引用句柄）与调用方实参装载（lea 后又 LoadPtr=读值）→判定「传句柄当地址」；②v2 侧=调用决议桩（回扫前后比对 IR层错误数）精确定位 5 个调用点全部指向新 7 参函数的静态实参。
  - **解决**: ①宿主 ir_expr.cpp AddressOf 分支补「操作数=顶层静态标识符」→直接以 `?gstatic_名` 符号地址为取地址结果（.data 槽本身=存储位置，绝不 LoadPtr）；②v2 回扫实参取地址 补回扫5：IR_静态加载&&目标==实reg→改发 IR_静态地址（附加=同一静态序号）。Rust 准绳：`&static` 的引用恒指向存储位置本身，绝不解引用重解释；C++ `T& x = 静态变量` 绑定对象而非其值。
  - **预防**: **「引用语义=传存储位置地址」在每一层（语义包装/IR 发射/回扫白名单/代码生成）都要有静态变量分支**——新增引用传参链路的验收=「静态容器实参+局部容器实参+表达式实参」三形态探针各一；**受控错误码（错误码3）也是崩**——gdb 断 __cn_runtime_error 而非等 SIGSEGV；**探针对拍脚本改造前先核对其宿主依赖**（probe 的 supply 编译误用 v2p[预编译 std 模式不注入容器方法定义]→须宿主 cn build+--output 副产品 .o；cn build 无 --output 时 asm=target/主.s 而非 v2asm.s 且会覆盖 target/主——探针 v2p 必须独立拷贝）。
  - **权重**: 10（宿主缺陷10 × v2同构1.3 × 预防价值1.3 × 已解决0.85）

## 高权重问题（第三十二轮：嵌套容器跨组件传输——构造非成熟路径家族第二例，2026-09-07）

- [2026-09-07 第三十二轮] **问题类型**: 设计缺陷（权重 9）**✅ 当轮改平铺方案根治（14 组件+全量 E2E 锚定）**
  - **描述**: 模块分桶需「每文件四快照」（模块路径/导入项/导入模块/公开表）跨组件（主.cn→语义分析.cn）传输——初版用 `向量<向量<整64>>`/`向量<向量<整32>>`/`向量<映射<整64,整64>>` 嵌套容器静态+单文件包装 `空容器们.追加(向量<整64>())` 临时值追加，v2p 首跑语义阶段即空指针（错误码3）。lessons 第 15 枚（含容器字段结构体裸声明不触发容器构造）同族第二例：**嵌套容器（向量的向量/向量的映射）构造与临时值追加均为非成熟路径**。同轮并发现 `查.值.类型ID` 链式成员（两层）亦为半实现面（[ir2] 成员字段偏移未知拒发）——逐步提取（先 `符号 找到s = 查.值;` 再 `.类型ID`）为既有成熟惯例。
  - **解决**: 快照传输改**标量平铺**：导入项平铺总表（交替 [名,限定名]）+边界向量们[f]（每文件起始偏移，段=[边界[f],边界[f+1]或总表尾)）+公开平铺总表（节点索引全池唯一无需边界）——语义分析多文件 入口从平铺构建局部映射供点查。全部容器走「局部构造+变量追加」成熟路径。
  - **预防**: **v2 容器成熟路径白名单：标量向量/单层容器局部构造后按名追加/映射局部构造+点查**——跨组件传结构化数据=标量平铺+边界；**链式成员（两层以上）禁用**，逐步提取；**新增容器用法的验收=先在最小探针验证构造/追加/枚举三件套**再进主链（本轮若先探针可省 3 轮调试）。
  - **权重**: 9（设计缺陷9 × 预防价值1.2 × 已解决0.85）

## 验证方法（第三十二轮：增量构建假象——编译失败被 grep 过滤掩盖，2026-09-07）

- [2026-09-07 第三十二轮] **问题类型**: 验证方法缺陷（权重 8）**✅ 当轮识破（构建 rc 判定纪律固化）**
  - **描述**: 宿主 AddressOf 修复后连续两次「重建+探针仍崩」——复盘发现 `cmake --build | grep -cE "error"`（`error` 前带空格的过滤）与 `| grep -E "错误" | head`（管道+head 吞退出码）让 **ir_call.cpp 缺 iostream 的编译失败**被过滤吞掉，`构建成功` 尾行来自上一次成功产物——修复代码从未真正进入 cn，浪费两轮「修复无效」假象排查。
  - **解决**: 构建验证固化为 `cmake --build target/build -j8 2>&1 | grep -E " error|Built target cn$"` 双查（错误零命中+目标行出现）；探针验证先看构建行再看运行。
  - **预防**: **「构建成功」必须以构建系统自身输出为准**——管道过滤诊断行会吞失败信号（假对拍第六案：判定链被过滤器劫持）；** lessons ⑤ 管道吞退出码 的同族：grep -c 的退出码语义（0 命中=exit 1）也会污染 `&&` 链**；改 C++ 后必须看到 `Built target` 行才允许跑探针。
  - **权重**: 8（验证方法缺陷9 × 预防价值1.1 × 已解决0.8）

# lessons.md - AI错误记录与经验教训

## 高权重问题（v2p 本机冒烟：linux_x64 参数拷贝临时寄存器 r9 冲突第 6 参数寄存器，2026-09-05 深度系统 x86_64）

- [2026-09-05 18:30] **问题类型**: 设计缺陷（权重 9.5）**✅ 方案A用户裁决当轮根治（v2p 冒烟段错误 139→全绿；单测 1239/1239+E2E 155 用例 128 过零回归）**
  - **描述**: `LinuxX64CodeGenerator::emitParamSetup` 的**结构体按值参数拷贝循环与 i128 参数拷贝用 r9 当数据临时寄存器**——而 r9 是 SysV 第 6 整型参数寄存器（rdi,rsi,rdx,rcx,r8,r9）。当结构体/i128 参数位于第 6 参数位之前且函数整型参数满 6 个时，prologue 按序装载到该参数时 r9 中尚未保存的第 6 参数值已被拷贝残留覆盖——参数 6 恒为垃圾。**触发条件三重合取**（结构体/i128 参数在前+整型参数满 6+第 6 位被占用）使 E2E 127 例全部漏网；v2 自举编译器（8441 行、首个真实大规模 CN 程序）的「生成如果」（池=参数2 结构体按值 3 字拷贝、指令引用=参数6）正中全条件——v2p 冒烟 词法分析.cn 段错误 exit 139。**gdb 五层下钻链**（崩点 `mov (%r10),%r9` 且 r10=41 → 崩溃槽 −0x22648=prologue 自 0x10(%rbp) 装载的第 7 参数（`整64* 寄存器计数`）→ 该槽由调用方「生成如果」栈参数槽 1 装入 → 调用点实参来自 −0x29a80 → **生成如果 prologue：参数 2（结构体按值）拆字段拷贝用 r10+r9，r9 终值=池容量 41，随后 `mov %r9,−0x29a80` 保存参数 6 装进的却是 41**）——数据流完全闭环。
  - **解决**: 方案A（用户裁决）：三处拷贝数据临时 r9→r11（结构体按值循环/i128 寄存器参数/i128 栈参数）——r11 在 prologue 参数装载阶段无任何职责（仅虚调用间接跳转 emitVirtualCall 使用，发生在函数体执行期），与 ARM64 的 x10/x11/x12 拷贝模式同构。**回滚测试闭环**：E2E 153 修复前两形态均输出 706549（第 6 参数=拷贝残留「丙=3」）、修复后 706554/706557 全对。
  - **预防**: **①手写 ABI 实现中的「拷贝/中转临时寄存器」必须取「参数寄存器集合之外」的空闲寄存器，且按阶段核对职责**（prologue 阶段 r11 空闲≠执行阶段空闲——临时寄存器的安全性是「阶段」属性不是「全局」属性；本轮 r9 在 win x64 后端对应位置不在 win 参数寄存器集合（rcx,rdx,r8,r9 仅 4 个）内，直译到 SysV（6 个参数寄存器）后落入集合——**同一 IR 语义的多个后端移植时，寄存器角色表要逐寄存器重新对表，不能沿用「win 没炸」的直觉**）；**②触发条件多重合取的缺陷靠常规 E2E 抓不住，首个真实大规模程序（自举编译器）是最强探针——冒烟推进暴露的崩溃先按 ABI 实现审计（寄存器角色/参数区布局）再怀疑被编译程序**；**③崩溃地址是小整数（41）=「整数值被当指针解引用」的指纹，从崩点槽位向上追「谁写入」比向下猜形态快（本轮 objdump 全量反汇编 grep 槽写入点 + 逐层 prologue 核对，40 分钟闭环）**。
  - **权重**: 9.5（设计缺陷9 × 详细分析1.6 × 解决方案1.0 × 预防措施1.5 × 已解决0.8）

## 高权重问题（plans/016 呈报复核轮：win 第4路 calleeReturnsStruct 死代码归真，2026-09-05 家机 win-x64）

- [2026-09-05 15:30] **问题类型**: 死代码+虚假注释（假修复）（权重 9.5）**✅ 方案A用户裁决当轮根治（构建零警告+单测 1238/1238+E2E 全量 78/79 已知家族零回归）**
  - **描述**: plans/016 深度系统机呈报「win 第4路 calleeReturnsStruct 与形态A retbuf 契约交互待家机复核」——复核揪出**双后端同族死代码+假修复注释**：①win 侧 `calleeReturnsStruct`（x64_instructions.cpp，2026-08「自举检查修复」注释声称「按被调函数 structReturn 标志判定」）因 `activeModule_` 成员**全仓无赋值恒 nullptr** 首行短路**从未生效**——hasBigRet 恒三路；②linux_x64 新后端按「对齐 win x64」移植了同款函数（这次 activeModule_ 有赋值=活的）**但零调用点**——复制了叙事而未核实被移植侧行为。**双巧合互洽掩盖死代码**：win 侧 IR 层形态A预插（ir_call.cpp hiddenArgs 首插 retbuf、result.type=void）恰好使 operands[0] 在 hasBigRet=false 下原样落 rcx（Win ABI 隐藏指针位）= 与被调方 paramOffset=1 协议对齐——「假修复」与「真机制」当时同期上线，删除 purported 修复无任何行为变化。**危险反事实**：若按注释激活第4路（补赋值），形态A调用 hasBigRet 变 true → argOffset=1 → 预插 retbuf 被当普通实参推到 rdx → 实参全错位（探针 IR dump/调用方/被调方 asm/运行输出四重实证定位）。
  - **解决**: 方案A（用户裁决「立刻修复」）：①win 删第4路调用+`calleeReturnsStruct`+`activeModule_` 成员，hasBigRet 收敛三路（i128/u128/struct* result.type）——**行为零变化**（现状第4路恒 false）；②linux_x64 删同族死函数与赋值行——三后端（win/SysV/arm64）判定逻辑完全同构；③ir_decl.cpp/linux_x64_instructions.cpp 注释归真（如实记录形态A契约：IR 层预插 operands[0]、后端原样传递自然落隐藏指针位 win=rcx/SysV=rdi/arm64=x0；structReturn 标志仅供被调方 paramOffset 协议）。
  - **预防**: **①「修复生效」的验收标准是可观测的行为变化或直接实证，不是注释与测试转绿**——若某修复上线时「另一机制同时生效」（本例=形态A预插），回滚测试（删掉 purported 修复看是否回归）是唯一可信验收：本例删第4路行为零变化即证明其从未起效；**②「对齐/移植」复制的是行为不是注释叙事**——移植前核实目标函数在被移植侧是否真的活着（有赋值/有调用点/单测可达），注释声称的行为与 grep 事实矛盾时以事实为准；**③死代码（无调用点函数/恒短路分支/恒 null 成员）当缺陷对待**——它与「从不触发的检查」同族，掩盖机制真貌并误导后续移植（本例误导跨机跨后端两轮）。
  - **权重**: 9.5（设计缺陷9 × 详细分析1.6 × 解决方案1.0 × 预防措施1.5 × 已解决0.8）

## 高权重问题（呈报①②方案A实施轮：check 假绿根治+常量诡异触发归因反转，2026-09-05 家机 win-x64）

- [2026-09-05 09:30] **问题类型**: 设计缺陷+归因错误（权重 10）**✅ 两项同轮根治（E2E 151 六链冒烟全绿）；呈报①定性反转：与常量对无关**
  - **描述**: **①呈报② cn check 假绿（簇⑥）**：`mergeModules` 入口判定=`模块名=="主"||单模块特判`——命令行入口为非主.cn 且含导入时被误判为「被导入模块」，私有函数体被可见性过滤静默丢弃=「检查通过」假绿；E2E 26（单文件形态）与簇⑥（组件自检形态）**同根两发作**——第一次修法是「单模块特判」症状补丁而非根治来源判定。**②呈报①「同值常量诡异触发」归因反转**：原定性「IR常量.cn 新增任意常量对使三链冒烟多 1 条诊断」**完全错误**——真根因=**IR容器.cn 反向引用 IR生成.cn 的 IR指令/函数IR 结构体**（IR生成 导入 IR容器=反向不可导，主链靠多文件合并全局可见掩盖）；冒烟子链（语法分析/语义分析/IR容器 入口，链上无 IR生成.cn）**结构体登记源缺失**→`解析方法对象类型` 对 `函数信息.容器字段局部们.元素(i)` 查结构体大小 miss→误诊。该缺陷**自 IR容器.cn 诞生（P7b，2026-09-01）起长期潜伏**：历史冒烟只统计 sem5（语法/语义），ir2 计数纪律第十三轮才建立，第一次照见它却被归因给当轮新增的常量对。**第十四轮消融的「删常量对不触发」是假绿**（消融未删旧 asm/检查面不全——本轮重做原子化消融：修复版+任意常量对=0 诊断，常量对正式证伪）。**③实验前提踩坑**：复现簇⑥ 用例时把「私有」函数写在 `公开:` 标签段之后——CN 可见性标签为粘性作用域（规范08），该函数实为公开，假绿不触发；丁/戊对照实验（唯一变量=有无公开函数）行为相反曾使逻辑陷入死局，根因即标签粘性。**④连带暴露第三初始化形态**：miss 根治后语义分析链跑得更远，暴露 `符号 找到 = 查.值;`（结果<T>.值 整体接收=成员表达式形态）不在结构体初始化白名单（原仅调用返回/标识符拷贝两形态）。
  - **解决**: ①`ModuleUnit::isEntryUnit` 命令行入口标记（loadModuleTree 根调用写入，递归依赖恒 false）+mergeModules 据此判定——E2E 26 特判与簇⑥ 一并归一删除；E2E 151 check负用例（新通道 `期望check失败.txt`：被检文件名+期望诊断子串，与 `期望编译失败.txt` 同构）红→绿闭环。②IR指令/函数IR 结构体定义自 IR生成.cn **下沉 IR容器.cn**（引用方导入链恒含 IR容器——登记源在任意子链可达；程序IR 留 IR生成.cn 因其引用方链恒含 IR生成）——六链冒烟 0 诊断+asm 产出+ml64 全过（IR生成链 6.36MB 与十三轮基线一致）。③白名单扩成员表达式形态（生成表达式 成员分支压字段地址→IR_结构体拷贝 源）。④原子化消融（快照→加 T_测试甲=98/乙=99→四链 0 诊断→回滚）证伪常量对假设。
  - **预防**: **①「入口/身份」类判定必须以「来源事实」（命令行显式指定/构造来源）为准，不得以「命名约定」（文件名==主）为准——命名约定只可作查询接口，作语义判定必在约定外形态误判（同根两发作的根源）**；**②消融实验的「不触发/绿」结论必须先验证产物新鲜度（删旧产物再跑/以产出物时间戳为准），存在性检查会被陈旧 asm 污染成假绿——「计数>0 中止」类纪律引入当轮就要审计既有产物的陈旧残留**；**③「全局合并掩盖反向依赖」的组件库：新链（冒烟子链/独立编译）上要对引用符号做全量登记源审计（结构体查结构体大小/函数查函数表），一个符号缺失=链上必炸点，与「改了什么」无关——「改 A 出 B」的第一假设应是「B 本来就坏只是检查面没照见」，而非「A 触发 B」**；**④可见性/作用域类实验用例先实测前提**（公开:/私有: 粘性段位、默认访问级），前提错误时对照实验会得出自相矛盾的数据徒耗定位时间**；**⑤测试断言的判定源迁移（isEntryModule→isEntryUnit）属 API 变更连带，等价迁移须断言零变化并向用户披露**；**⑥stash 栈与 checkout 目录回退交错操作后必须实质核对基线（grep 特性标记如 T_正8/必查字段），git status 的 M 清单与文件实际内容可能不一致——本轮「checkout HEAD -- 目录」在 stash 栈未清状态下疑似未完全生效，后续三处修复做在 v2@13 旧基线上（第十四轮特性丢失），靠 T_正8 缺失当场识破、停门禁重做；门禁跑错误基线=结果整体作废**。
  - **权重**: 10（设计缺陷9.5 × 详细分析1.8 × 归因反转实证价值1.2 × 预防措施1.2 × 已解决0.8）

## 高权重问题（缺陷零容忍收口轮：引用返回/裸引用/无符号窄宽+宿主引用返回读值+常量诡异触发，2026-09-04 家机 win-x64）

- [2026-09-05 00:30] **问题类型**: 设计缺陷+使用错误（权重 9.5）**✅ 四件特性+宿主缺陷当场根治（E2E 150 六步闭环+147~149 回归绿）；常量诡异触发呈报挂起（根因未明）**
  - **描述**: **①宿主引用返回读值缺陷**（连带揪出）：visitCallExpr 对引用返回调用返回裸 ptr——被一切右值消费（`整64 a = 取值(p)` 读出地址，sanity exit=11 实锤）；宿主 P3-18 只实现了赋值目标路径。**②消融实验污染链**（本轮最大时间黑洞）：v2 常量系统同值常量诡异触发的归因过程——T_正8/T_正16 值 14/15 与 IR_取余/IR_位与 撞值→冒烟链多 1 条「方法调用的对象类型未知」；八组消融（值 14~19/98/99×名字 T_正8/U_正8/U正8/U正九/U8/零宽码甲/U正宽8）**全部触发**、唯一不触发组合=T_测试甲=98+乙=99——但该组合实验时代码生成已被前序消融改成字面量（**实验间状态泄漏**：每组消融只改一个变量却遗留前组的字面量化/第二个常量未同步，导致「T_测试甲不触发」是污染态假反例）；且**换值 16/17/18/19 后仍触发**推翻「撞值」初判——真凶未归因。**③代码生成字面量化恢复遗漏**：消融时把 IR生成/代码生成/ARM64 的 T_正8 引用换成字面量 14/15，恢复脚本只恢复了 IR生成——代码生成两文件的 `指令.类型 == 14`（14=IR_取余）永不命中→movzx 分支死代码→150 失败码 17 追查半轮。
  - **解决**: ①suppressRefDeref_ 标志（读值默认 LoadPtr；赋值目标/复合赋值/返回转发/引用绑定四上下文抑制）；②T_正8/T_正16 定值 18/19（空闲区）+呈报三方案；③恢复脚本 grep 全仓 `== 14`/`== 15`/临时名 逐一核对。
  - **预防**: **①消融实验必须「快照→单变量→快照回滚」原子化**——严禁跨组累积状态（本轮八组实验四组被前组遗留污染，假反例误导归因方向 2 小时）；**②「改 A 出 B」类诡异缺陷：先跑最小复现探针（独立源码），探针不复现=上下文依赖（链序/全局表）——直接 dump 中间状态（节点池/表内容）比继续黑盒消融快一个量级**；**③批量字面量化/重命名的恢复用「符号全仓引用清单」核对**（改名脚本必须列 out 清单，恢复时逐项对账）；**④宿主特性移植 v2 前先跑宿主全形态 sanity**（本轮 P3-18 只验了赋值目标——读值缺陷在 sanity 第一版就暴露，若先全形态探针可少一轮返工）。
  - **权重**: 9.5（设计缺陷9 × 详细分析1.6 × 解决方案1.2 × 预防措施1.4 × 已解决0.8——常量触发未归因）

## 高权重问题（IR生成.cn 冒烟 96 错收口 + [ir2] 819→0 + IR 层错误即中止，2026-09-04 家机 win-x64）

- [2026-09-04 21:30] **问题类型**: 设计缺陷+使用错误（权重 9.0）**✅ 当轮全部根治（E2E 147/148/149 六步闭环全绿；IR生成.cn 冒烟语义 0 错+IR 0 诊断+ml64 通过；五组件冒烟维持）**
  - **描述**: **冒烟 96 错单根因**（泛型引用局部声明 `向量<IR指令>& 指令 = 函数信息.指令;` 泛型闭合后是 `&` 非标识符，是泛型声明 返回假→整条声明落表达式成垃圾——96 错全为级联）；**语义 0 错后 IR 层暴露 [ir2] 819 条**=「IR 层错误即中止」缺失面可见化；实现期三坑：①**预扫容器声明 把引用局部误登 RAII 名单**——绑定字段地址当 this 析构=堆损坏 0xC0000374（asm 审读定位：返回分支析构序列 IR_加载 r 槽内容 L→DeleteObject(L)）；②**回扫实参取地址 的「实参数值恰好正确」误报家族**——调用返回结构体（rax=retbuf 地址即按值实参要传的地址，655 条）/&标识符 引用位（值即左值地址，26 条 查方法签名(...,&签)）/指针加载（操作数1 即左值地址）：三类形态实参数值本已正确、诊断误报掩盖「其实能用」；③**正则批量替换自伤三连**——统一 [ir2] 诊断为 IR层诊断 计数调用时：替换函数体自身（`IO::打印到错误("[ir2] "+消息...` 匹配模式与新调用同构→递归破坏）+消息字符串引号丢失（`(消息:  +` 形态）+尾部 `+ "\n"` 双拼/误删（fix_nl 连函数体换行也吃）。
  - **解决**: 96 错=语法层 `& 标识符` 同认+语义层引用初始化纪律+IR 层引用局部分支（槽=绑定对象左值地址，引用参数槽们 全套同构复用）；[ir2] 收口=用户函数返回结构体 retbuf ABI 全链（被调方槽 1=rcx+拷贝+rax/调用点帧尾区+位0/链式 解析调用返回结构体 补普通函数分支/双后端 prologue 对称）+结构体初始化两形态+容器初始化两形态（返回=接收转移/拷贝=浅共享）+回扫三误报根治；IR 层纪律=IR层诊断 单点计数+主.cn 中止不产 asm（E2E 149 负路径）；正则坑=改后 grep 全点人工核对+断言式替换（assert old in s）+**heredoc 内嵌转义一律写临时 .py 文件**（本轮 chr(92) 构造法实证可靠）。
  - **预防**: **①「诊断与实际数据流不符」时先实证数据流（asm/IR dump）再定性「缺口」还是「误报」**——本轮 655 条诊断实为误报（值已正确），若按「缺口」实现会做无用功；**②新增变量生命周期形态（引用/视图/转移）必查 RAII 登记点的拥有性判定**——非拥有式绑定入析构名单=双重释放家族；**③批量正则改造代码：先排除与目标模式同构的既有实现（本次封装函数自身），改后全点 grep 核对**；**④「上轮已清零的层」之下还有下一层**（语义 0 错→IR 层 819 条）——冒烟推进按层准备，每层清零后立即复跑看下层暴露面。
  - **权重**: 9.0（设计缺陷9 × 详细分析1.4 × 解决方案1.2 × 预防措施1.2 × 已解决1.0）

## 高权重问题（plans/015 强制分号语句边界改造·万行级迁移的 token 级引擎陷阱链，2026-09-04 家机 win-x64）

- [2026-09-04 15:30] **问题类型**: 设计缺陷+使用错误（权重 10.0）**✅ 当轮全部根治（1216/1216+五组件冒烟 0 错+145/146 双绿；终门禁见 HANDOFF）**
  - **描述**: **plans/015 方案B（强制分号）万行级迁移的四个结构性陷阱**：①**v2 结构体字段分号死循环**——解析结构体声明 字段循环原不消费 ';'，迁移后字段带分号→遇 ';' 走 解析类型 零前进→127 嵌套容器用例 v2p 编译**卡死 40 分钟**（tasklist 定位 v2p.exe 挂起+零编译活动）；②**宿主顶层常量/静态原无任何分号消费**——迁移后 ';' 成顶层孤儿报「预期顶层声明」（前置门禁 6 个 v2 闭环用例失败同因）；③**git checkout 目录级回滚误伤工作区改动**——迁移中途 git checkout 目录把 v2 改造（吃分号 18 处）一并回滚，v2p 冒烟「通过」实为旧二进制假绿；④**token 级迁移引擎的语境歧义家族**——花括号语境（-> 类型 { 函数头 vs lambda vs 类 名 : 父类 { 继承头 vs 名 { 字面量）、'外部' 上下文关键字双语义（FFI 头 vs 变量名）、NL 转义在 tokenize 归 str（END 类）误触插入、同行 名 名 = 声明与「上句尾+新声明」同构。
  - **解决**: ①字段循环补 ';' 宽松消费（声明体成员分隔不强制不诊断）；②宿主 parseProgram 常量/静态分支补 consumeSemicolon；③v2 改造按迁移后源码重做（行级状态机：建节点 特征行→最近 返回 位置 行替换）；④语境栈四态（block/lambda/literal/enum）+参数表回扫（'(' 前是 函数 名→函数头 / ']'→lambda）+'外部' 按下一 token（函数/类）分流+标识符形态起点**仅跨行判定**（同行歧义留人工，2 处单测人工修）。
  - **预防**: **①规范反转类全库迁移，「既有语法元素在新语法下的消费缺口」（原可选无消费的 token）是死循环/孤儿 token 的温床——改造前对每个语法位置列出「谁消费它」清单（本缺陷三发作：v2 结构体死循环+v1 dump 错位+宿主顶层孤儿——一次列清单可全防）**；**⑥逐行读文件再拼接时先查底层 API 换行契约（fgets 系=含换行）——盲目补 "
" 致每行双换行，词法行号翻倍 2n+1（146 用例实证，LF/CRLF 无关）**；②**git checkout 目录回滚前必须确认该目录无未提交的功能改动**（迁移与功能改造同目录并行时）；③**后台长跑 E2E 挂起判定**：tasklist 查编译器进程（零 cn.exe/link.exe=卡死而非慢）+CIM 查子进程命令行定位具体用例；④**python -c/heredoc 内嵌脚本的转义在多层工具间折损**——结构修改一律写临时 .py 文件执行（本次 6 次折损 3 次静默产出损坏文件）；⑤token 级迁移引擎的「同行标识符形态歧义」宁可漏（门禁失败暴露人工修）不可误插（点; p 类破坏）。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.5 × 解决方案1.4 × 预防措施1.2 × 已解决1.0）

## 高权重问题（语法分析.cn 冒烟 55 错四簇消化 + 簇⑥宿主星号泛型实例名误剥，2026-09-04 单位机 麒麟 ARM64）

- [2026-09-04 11:00] **问题类型**: 设计缺陷（权重 10.0）**✅ 当轮四簇+连带簇④⑤⑥全部根治（四组件冒烟 55→0 错，语义分析.cn 冒烟连带 0 错；E2E 142/143/144 新增三用例六步闭环全绿；全量门禁见 HANDOFF）**
  - **描述**: **冒烟 55 错四簇根因各不相同**：①跨模块符号（T_*/IR_*/节点_* 定义与引用跨文件、循环依赖不可导——主链靠 P6h 全局合并掩盖）②文件 API 导入项（`导入 文件::打开文件` 语义层零登记——裸调报未声明）③词法独缺 `--`（++/+=/-=/-> 齐——i-- 切双 - 与下行粘连成非左值赋值/吞声明）④（分诊揪出）泛型实参星号后缀不消费（`结果<空类型*, 整32>` 声明整体错位）。**簇⑥（宿主缺陷，宿主缺陷优先纪律当场根治）：泛型实例名含实参星号（盒子<整64*>→盒子$整64*）时，语义/IR 三处按「尾 *=对象指针」误剥**（visitMemberExpr 成员访问/semantic_call 方法调用/ir_oop_call 实例方法）——实例名被剥成 盒子$整64 查类失败「不是结构体/联合体/类类型」。
  - **解决**: 簇①=新建 IR常量.cn 单一归属（用户裁决 A2）+七组件显式导入；簇②=导入项登记+裸名改写限定名（B1，权威登记点在 解析程序 入口清空+重扫——**依赖先解析窗口会抹掉 扫描导入 时的登记**）+B1a 边界入档（宿主=stdlib 包装 结果<> 契约 vs v2=内置直调裸指针，随 stdlib 并入专项收口）；簇③=自减全链（C1：词法 --/节点_自减/语义拒绝/IR 发射自增自减 共享函数四路径）；簇④=解析类型文本 加 是实参 参数（实参位消费后缀并入合成名，外层指针层数登记不抢吃）；簇⑤=结果/容器**字面量**初始化语义拒绝（=无 原错误放行+IR 结构体拷贝瞎发射=运行期 SIGSEGV）；簇⑥=三处消费点「**原名查类命中即用原名**」（真指针不命中类表自然落入剥分支，行为不回退）。
  - **预防**: **①「类型名字符串的尾 *」有两种语义（对象指针 vs 实例名合成成分）——任何「isPointer 判定后剥 * 查类/查方法」的消费点，必须先按全名查类命中再剥**（新泛型实参形态接入时全库扫同款模式）；②`cn check 非入口文件名（≠主.cn）且含导入`时私有函数体被模块可见性过滤静默丢弃=**检查假绿**（簇⑥ 定位过程中 H1.cn「检查通过」实为没检查——最小复现必须用 主.cn 文件名让检查真实发生；此检查器诚实性缺陷另入观察项）；③冒烟错误按「文件分摊+函数二分+形态探针矩阵」三层定位法（本轮流水分诊全程实证）；④导入项/符号登记类机制的**登记时机必须紧贴消费点**（扫描登记+窗口穿插=丢失温床）。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.6 × 解决方案1.3 × 预防措施1.2 × 已解决1.0）

## 高权重问题（灰色点②方案A：字符串比较运算符显式拒绝，2026-09-04 家机 win-x64）

- [2026-09-04 02:30] **问题类型**: 使用错误/评估方法缺陷（权重 6.0）**✅ 当轮根治（24 用例迁移后全量门禁复绿；连带揪出 v2 组件驻留地址比较惯用法 1 处一并迁移）**
  - **描述**: **方案A「现有源码零影响」评估由抽样 grep 得出——模式只扫了 `当前文本(...)/驻留文本(...)` 等已知惯用法形态，漏了普通变量形态 `路径 == ""`**（24_error_handling:31，既有验收用例）——全量门禁立刻打脸（143 用例 140 过）。同轮连带实证：新语义检查上线→先跑受影响代码库全量编译（宿主拒绝上线即打挂 v2p 构建：语法分析.cn:881 驻留地址比较惯用法——**依赖驻留地址恒等的写法=潜伏陷阱**，被检查器揪出是好事）。
  - **解决**: 24:31 迁移 `如果 路径 == ""` → `如果 字符串比较(路径, "")`（用户批准，expected 零变化——首版 `!字符串比较` 取反写反致两分支对调，E2E 逐行比对当场抓住后修正）；语法分析.cn:881 迁移 `当前文本(...) != "}"` → `!字符串比较(...)`（此处的 ! 是对的：不等判定）。
  - **预防**: **「零影响/影响面」结论必须由全量门禁（或至少全库编译）实测背书，抽样 grep 只能作初筛**——grep 模式按已知样例缩窄=结构性漏检（普通变量/参数 vs 字面量是最常见形态反而不在模式里）；**布尔内置迁移必须逐分支推演真值表**（`==` → `字符串比较` 同向，`!=` → `!字符串比较` 反向——24 首版按 `!=` 惯性写反）；新检查落地当轮即跑全量=最快自伤自测。
  - **权重**: 6.0（使用错误6 × 详细分析1.0 × 解决方案1.0 × 预防措施1.0）

## 高权重问题（③ 第六波：三元条件运算符 + 入口前缀紧绑定 + 静态自增丢句根治，2026-09-03 家机 win-x64）

- [2026-09-03 23:50] **问题类型**: 设计缺陷（权重 10.0）**✅ 已根治（表达式入口前缀改 解析层_子(层5,真) 紧绑定——与二元链操作数位前缀（10.5）收敛为全路径一致）**
  - **描述**: **v2 解析表达式 前缀段（!/-/&/*）操作数用「递归 解析表达式（完整）」**——把后随低层二元与三元吞进操作数：`!x ? a : b` 解析为 `!(x?a:b)`、`!x + 1` 解析为 `!(x+1)`，均背离 C 一元优先级 12（前缀只绑操作数）。三元实现前不可见（`?` 本身截断、`!x+1` 数值等价侥幸未踩）；三元接入（优先级 1.5 低于一元）后语义缺陷可见化——137 首版若不修，`!x ? a : b` 条件语义直接错。与 10.5（二元链右操作数位前缀**缺失**）互为镜像：一个是该判不判、一个是判了吞太多，**同根因=「前缀操作数边界」无统一定义**。
  - **解决**: 入口前缀操作数统一改**紧绑定**：解析层_子(层5, 真)（原子+后缀链，嵌套前缀递归同覆）；二元/三元由 解析表达式 尾段统一处理——前缀节点建完继续走尾段（二元判定+三元包装）。至此前缀操作数边界全路径一致：入口位/二元右操作数位/嵌套前缀位 均为「层5 紧绑定」。
  - **预防**: **新增优先级层次（如三元插入 1.5）时，必须重审「既有各位置的操作数解析路径」是否与该优先级兼容**——尤其一元前缀的贪婪度；「递归完整表达式」作操作数解析=隐藏的优先级吞噬（等效把前缀优先级降到最低层），新语法接入前先按 C 优先级表逐形态推演 `前缀 × 新语法` 组合。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.0 × 解决方案1.0 × 预防措施1.0）

- [2026-09-03 23:55] **问题类型**: 设计缺陷（权重 9.0）**✅ 已根治（补静态槽自增分支：IR_静态加载 +1 → IR_静态存储）**
  - **描述**: **v2 节点_自增 语句分支仅认 局部槽/参数槽——目标槽==0 且非静态时静默跳过（零 IR、零诊断）**：`静态 整64 c = 0` + 函数内 `c++` 整条语句静默丢失（探针 IR dump 实证：函数体只有 op=65 静态加载+op=53 返回，自增无痕）。「防御分支静默降级」家族**第四例**（同族：BUG10b、结果/可选 .值、objSrcType 分支表形态落空——HANDOFF v2.1 轮）。
  - **解决**: 目标槽==0 时查 静态序号们——命中则 IR_静态加载（附加=静态序号）→ 常量1 → IR_加 → IR_静态存储（②c C 既有设施，静态赋值同款读写对）。
  - **预防**: **语句/表达式的目标解析链（局部→参数→常量→静态→函数名）每新增一类存储位置，全量核查所有「按名取目标」的既有分支**（自增/赋值/取地址/传引用——本轮静态是 ②c C 新增位置，自增分支未随迁）；家族纪律再确认：**静默跳过=最差失败形态**，不可解析的目标必须可见化（诊断或硬错误）。
  - **权重**: 9.0（设计缺陷10 × 详细分析1.0 × 解决方案0.9 × 预防措施0.8 × 已解决1.0）

## 高权重问题（③ 第五波：限定名内置 IO/文件 + 真假无 + 二元链前缀，2026-09-03 家机 win-x64）

- [2026-09-03 23:30] **问题类型**: 设计缺陷（权重 10.5）**✅ 已根治（解析层_子 操作数位参数化——前缀只在二元右操作数位判定）**
  - **描述**: **v2 一元前缀（!/-/&/*）仅在 解析表达式 入口解析**——二元链右操作数位（`X || !f(...)`/`a * -b`）经 解析层_子 落到 解析原子（不认运算符）→ 位置零消费 → 语句层把前缀起头内容当新语句 → 外层 `)` 不可解析 → **块截断**（词法分析.cn 冒烟 3 处截断与 133 用例 `|| !字符串比较(...)` 同族，探针矩阵 p1~p4 交叉二分实证）。**首版修复踩二次坑**：前缀分支放在 子(5) 入口无条件判定——而 v2 二元链是「**入口在运算符上、左操作数已由 解析表达式 前段解析**」的空左操作数设计，链入口的 子(5) 恰落在二元 `-`/`*` 上被误当一元前缀——**减法/乘法全灭**（`0 - 28` 变 [0,负号(28)]、`f() * 10000` 变 解引用(10000)，123/129/130 全量门禁回归实证；133 侥幸绿——其算术全是 `+` 不在误判集）。**消融实验**（禁用分支重建对照）+ 节点数同值（20==20）+ asm diff 三步锁凶。
  - **解决**: 解析层_子 增 操作数位 参数（旗标经 子→二元层→子 下降链贯通）：二元循环右操作数位传 真（运算符已消费——当前位置必为操作数起点，前缀判定无二元歧义）、二元链入口传 假（位置可能在运算符上——空左操作数设计，不判定）；前缀操作数紧绑定（子(5,真) 递归=原子+后缀链，嵌套前缀同覆，对齐 C 一元优先级）；表达式入口的全表达式贪婪语义保持不变。
  - **预防**: **语法分层解析器新增语法类时按「全部可达位置」枚举核对，且必须先弄清每层入口的位置约定**（v2 二元链入口=运算符位/空左操作数——与教科书「入口在左操作数」相反！）；「某形态单独可用、组合形态截断」= 位置完整性缺口指纹（探针矩阵交叉二分最快）；**修改解析器后必须跑含 减法/乘法/复合比较 的既有用例**（123/129/130 即为本轮守门员——`+` 独大的新用例会漏检）；消融实验（禁用新分支重建对照）是「无新形态却回归」类缺陷的定位利器。
  - **权重**: 10.5（设计缺陷10 × 详细分析1.6 × 解决方案1.2 × 预防措施1.4 × 已解决1.0）

- [2026-09-03 23:40] **问题类型**: 使用错误（权重 5.0）**✅ 已修正（真/假 字面量 + 局部副本改写+设置 回写 + 字符串比较 内置）**
  - **描述**: v2 组件源码编辑三连小坑：① `布尔 x = 0` 宿主拒绝（布尔初始化/赋值须 真/假——整数 0/1 不隐式转换）；② `池.元素(i).字段 = x` 链式字段**写**非宿主支持形态（124/126 实证：元素(i) 读=返地址视图、写=设置(i,v)——对返回值视图的字段赋值报「类型 '未知' 不是结构体」）；③ v2 二元 `==` 对字符串是**指针比较**（内容比较须 字符串比较 内置——v2 组件自身全用 字符串比较 的原因），133 用例首版 `行.值 != "甲"` 直踩。
  - **解决**: ① 改 真/假；② 局部副本改写 + 池.设置(索引, 值) 回写；③ 内容比较一律 字符串比较(a, b)。
  - **预防**: **CN 写编译器组件/用例时：布尔字面量纪律（真/假，禁 0/1）；容器元素字段写 = 副本+设置 两步（读视图≠写视图）；字符串内容比较 = 字符串比较 内置（==/!= 是指针语义，字面量与运行时串永不等）**——三者都是「形态相近但语义不同」的 CN 语言边界，写完先 cn check 再断言。
  - **权重**: 5.0（使用错误6 × 详细分析1.0 × 解决方案1.0 × 预防措施1.2）

## 高权重问题（④ 第四轮：缺陷② v2 侧 + 裸节点双重处理 + 缺陷 A/B 根治，2026-09-03 家机 win-x64）

- [2026-09-03 19:30] **问题类型**: 设计缺陷（权重 11.0）**✅ 已根治（检查块 仅派发语句根节点 + 解析器裸 *表达式 补根）**
  - **描述**: **v2 检查块 裸节点双重处理**——块区间 [块起点,块终点) 线性包含各语句的全部内部节点（RHS 裸标识符/一元/成员…先于语句根入池），原实现逐节点派发：裸一元节点（取地址/解引用/负号/逻辑非）在**声明根注册名字之前**先查操作数区间——`整64* p = &v` 换行粘连 `*p = 55` 粘成 `&(v * p)` 后自引用 p 误报「未声明」；前向引用 `整64* p = &q` + 后声明 q **同错双诊断**（p16 探针实证同一错误报两次）。与 2026-09-03 16:40（权重 11.0）「父块区间含嵌套块语句双重处理」**同族第二次发作**——区间重叠的双重处理是结构性陷阱。连带揪出：解析器 `*表达式` 非赋值语句裸节点入池**无语句根**——IR 语句派发零分支=静默死代码，语义靠裸派发兜底（互为因果的一对半实现）。
  - **解决**: 检查块 主循环仅派发语句根节点类型（块/如果/当/变量声明/赋值/自增/返回/表达式语句/中断/继续），裸节点一律由所属语句根的区间检查统一覆盖（每节点恰在其作用域访问一次，rustc 对齐）；解析器 `*` 分支非赋值路径补 节点_表达式语句 根（裸 `*未声明x` 检查覆盖保持实证）；解析块 静默中断加 stderr 诊断（B6/+= 语句级静默中断家族可见化）。
  - **预防**: **「块/区间遍历器」新增节点类型消费点时，必须显式区分语句根 vs 语句内部裸节点**——裸节点永远经所属语句根处理（单次、正确时序）；「同一表达式区间被两个遍历器覆盖」即为双重处理（自引用误报/双诊断是它的可观测指纹——**负探针要专门构造前向引用与自引用形态**）；解析器任何「表达式已入池但无语句根」的返回路径=语义/IR 双侧半实现温床，新解析分支收尾必须问「这条路径的节点有没有根」。
  - **权重**: 11.0（设计缺陷10 × 详细分析1.5 × 解决方案1.1 × 预防措施1.3）

- [2026-09-03 19:40] **问题类型**: 设计缺陷（权重 9.0）**✅ 已根治（写回按左值形态枚举补全 + 数组补零补齐）**
  - **描述**: **宿主自增自减写回与局部补零两枚同族半实现**（单位机 ARM64 发现、win 同现=双侧公共 IR 层）：① ir_expr.cpp Increment/Decrement 写回**仅 IdentifierExpr**（类字段/顶层静态另有专用路径）——`p.y--`/`数组[1]++`/`(*q)++` 只生成「读+算」**静默跳过写回**（存储丢弃，p.y 恒 3）；② ir_stmt.cpp 无初始化器逐槽补零**仅结构体分支**（2026-09-02 缺陷2 根治漏数组同族）——`数组[2] += 数组[1]` 读未初始化栈内存（每次运行不同）。共性：**按「已知操作数/类型形态」特判实现，新增形态（成员/下标/解引用左值、数组类型）静默漏网**。
  - **解决**: ① Member/Index/Deref 目标经 lvalueAddress + Add/Sub + StorePtr 写回（与赋值语句同款；宽度按操作数类型——②b B5 字段宽度纪律）；② 数组按 registerVarSlots 登记槽数逐槽 Store 0（Rust 级确定性对齐）。
  - **预防**: **凡「读写对称」的操作（自增自减=读+写、复合赋值、捕获），实现时按左值形态全枚举核对**（标识符/成员/下标/解引用/引用捕获/静态/类字段——逐形态列清单，缺一即半实现）；**「只对 X 生效」的 if 形态特判，升级为「按形态枚举的 switch + 其余显式拒绝/诊断」**（未覆盖形态静默跳过=错误边界）；同族历史缺陷（缺陷5 类字段只读不写/缺陷2 结构体补零）修复时**必须扫描同族操作数/类型形态是否同样漏网**（本两枚均可在前次修复时顺带根治）。
  - **权重**: 9.0（设计缺陷10 × 详细分析1.3 × 解决方案1.0 × 预防措施1.2）

- [2026-09-03 19:50] **问题类型**: 使用错误（权重 5.0）**✅ 同日第三踩后成文（探针/测试源语句间必须加分号）**
  - **描述**: **换行≡空格粘连探针陷阱一日三踩**（HANDOFF 已列「本轮自己连踩两次」，家机本轮又踩三次）：`&v` ⋈ `(*q)++` 粘成 `&v(*q)`=调用（「无法调用非函数类型」）；`*q = 100` ⋈ `(*q)++` 粘成 `100(*q)` 调用+`++写透(q)`；`p.y = 3` ⋈ `--p.y` 粘成 `3--`。**行首是 运算符/`(` 且上一行以 标识符/字面量/`)` 结尾的任何边界都高危**。
  - **解决**: 探针/测试源**全部语句**加分号（不止取地址行）；测试源写完先 `cn check` 跑一遍预期零错再进断言（本轮单测首跑两失败即测试源自身粘连+类型不匹配，非编译器误伤）。
  - **预防**: **写探针默认全语句分号**（省心且与粘连形态测试隔离——需要测粘连时再专门构造无分号变体）；「无法调用非函数类型」报错位置在前一行表达式=粘连铁证；**测试源先 check 后断言**两步走，防把测试源自身 bug 当编译器误伤。
  - **权重**: 5.0（使用错误6 × 详细分析1.0 × 解决方案1.0 × 预防措施1.0）

## 高权重问题（③ 第三波：指针下标读写 + 宿主内存模型统一，2026-09-03 家机 win-x64）

- [2026-09-03 21:30] **问题类型**: 设计缺陷（权重 12.0）**✅ 已根治（C 布局三处统一）**
  - **描述**: **宿主内存模型三套咬合约定不一致**——① 数组局部：标量元素固定 8 字节槽/元素（非 C 布局）；② 指针步进 ptrElemStride：仅特判 结构体/类/i128、**其余兜底 8**（整32*/整16*/整8* 全错）；③ 容器堆数组：分配 `max(类型大小(T),8)` 8 字节槽模型。①②「双错互洽」（05 用例 `q+1` 数组 8 槽×步进 8 恰好同错而对）长期掩盖；③ 与②同类咬合。**同型指针不可能两全**：指向结构体紧凑字段（0/4/8 C 布局）的 整32* p[1] 步进 8 跨两字段读脏值（130 用例首踩，`11 33 垃圾` 实证）；.data 静态数组本就按 typeSizeOf C 布局——栈/.data/结构体三处只有数组局部是孤例。
  - **解决**: **全部统一 C 布局**（规范语义 p[i]≡*(p+i) 步进=sizeof(元素)）：ptrElemStride 统一 typeSizeOf(pointee)；registerVarSlots 数组槽数=ceil(typeSizeOf(数组)/8)；数组初始化/读/lvalue 三处步进 typeSizeOf；补零写入宽度按元素宽度（i128/浮点/指针保持 i64 原行为——常量无双槽/movss 不收立即数）；**stdlib 容器分配 9 处 `max(类型大小,8)`→`类型大小(T)` 紧凑**；**连带揪出 ir_expr.cpp A-3 隐式类字段读分支自带「标量 8 槽」步进**（元素()/获取() 走它、写路径走 ptrElemStride——读写步进不一致=30_container 腐坏 `删除后[0]:3`+链表/栈垃圾值，asm diff 7 处 `shl rax,3→2` 铁证），同批统一 typeSizeOf。
  - **预防**: **「互洽的双错」是最高危形态**——单侧修复必炸（本轮先改步进→30/v2p 全线腐坏，asm diff 定位读写两侧步进源不同才收口）；改任何「布局/步进/分配」三角之一，必须三处+读写两侧+静态/栈两侧全枚举核对；**asm diff（好构建 vs 坏构建）是定位 v2p/宿主行为漂移的最快手段**（3.3MB asm 仅 7 行差异即锁凶）。
  - **权重**: 12.0（设计缺陷10 × 详细分析1.8 × 解决方案1.4 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 21:40] **问题类型**: 设计缺陷（权重 9.5）**✅ 已根治（前缀一元操作数入预扫跳过+深度相对弹出）**
  - **描述**: **v2 表达式栈卫生缺陷家族（三处同族）**——① 取地址分支「清空全栈」：`f(&x)` 调用实参形态把先序压栈的被调负符号一并弹掉→`[ir2] 调用被调对象非函数名`（130 首踩，此前 v2 源码/用例无 & 作实参）；② 负号/逻辑非/解引用 分支：操作数被主遍历线性压栈+分支嵌套生成**双份**，栈必多一份残留——`q[-1]` 负下标弹2取到残留常量当地址→解引用垃圾地址 0xC0000005（asm `add rax,[常量槽]` 铁证）；③ 残留长期无害的原因：语句级消费取栈顶+语句尾清栈掩盖——**任何「弹 N 取操作数」的新消费点（下标弹2）即翻车**。
  - **解决**: 负号/逻辑非/解引用 操作数区间并入既有 取址区间们 预扫跳过（操作数只由前缀节点嵌套生成一次）；三分支+生成左值地址 解引用/下标 早退全部改「弹到进入前深度」（替代清空全栈）。
  - **预防**: **表达式栈消费点新增（弹N）必须审计同栈一切生产者的压栈数不变量**；「嵌套生成+线性遍历」双轨制节点（前缀一元/取地址/短路）要么预扫跳过要么深度相对弹栈，禁止裸清空；栈残留是「语句级侥幸无害、子表达式级必炸」型债务。
  - **权重**: 9.5（设计缺陷10 × 详细分析1.3 × 解决方案1.2 × 预防措施1.0）

- [2026-09-03 21:50] **问题类型**: 实现错误（权重 8.5）**✅ 已根治（双后端对称验证价值实证）**
  - **描述**: **两枚汇编发射缺陷**——① 宿主 x64 emitPtrLoadStore 窄宽（i8/i16/u8/u16）StorePtr 值操作数为立即数常量（显式强转 `整8(7)` 折叠直达）时发 `movsx rcx, byte ptr 7`——movsx/movzx 不收立即数（A2070）；裸常量被语义窄化检查拦截到不了代码生成，131 用例无一踩中（潜伏）；② v2 MASM 后端 IR_类型转换 发 `movsxd rax, [槽]` 缺 `dword ptr`——MASM 无法判定操作数尺寸（A2070；GAS 侧 sxtw 无此歧义，单位机仅跑 ARM64 故 129 win 首跑即炸——**「ARM64 已全绿≠win 侧对称已验证」的实证**，HANDOFF 诚实边界条目兑现）。
  - **解决**: ① 常量特判 `mov rcx, <imm>` 直载全 64 位（写 cl/cx 取低字节=补码截断，负常量同正确）；② 补 `dword ptr`。
  - **预防**: **双后端指令发射必须双侧对称验证**（单侧绿只是半绿）；movsx/movzx/movsxd 操作数合法性（立即数×、内存需尺寸前缀）双后端各自核对；窄宽常量路径无既有覆盖=必踩雷（显式强转是新可达路径）。
  - **权重**: 8.5（实现错误8 × 详细分析1.2 × 解决方案1.0 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 22:40] **问题类型**: 实现错误（权重 8.0）**✅ 已根治（分段续行 + 剥离悬挂逗号）**
  - **描述**: **v2 MASM 字符串字面量 db 单语句超限（缺陷⑤）**——`strData_N db <全文字节>,0` 一行发射：MASM ml64 **db 单语句操作数上限 48**（隔离二分实测 48 OK/49+ A2042 statement too complex），129 用例 53 字节中文字面量（strData_156）首踩；GAS 侧 .byte 无此限制故单位机全绿未测出（129 win 首跑被 movsxd A2070 先拦，修后才暴露=同轮第二枚 win 专属）。**连带坑**：分段续行发射时行尾逗号未剥离——悬挂逗号=不完整操作数，同样 A2042（48 值行隔离复测铁证）。
  - **解决**: db 按 48 值分段续行（`label db <48值>` 换行 `db <余值>`），满段行以 字符串子串 剥离尾逗号再发射。
  - **预防**: **汇编器隐性限制（操作数上限/行长度/悬挂符号）要用隔离最小复现钉死边界**（本轮 48 上限即二分实测得出，非文档可查）；逐字节 db 发射类后端代码默认按固定段长分块；「修一枚错后同文件再爆下一枚」=错误被先爆者遮挡，修后必须原路径复跑到绿。
  - **权重**: 8.0（实现错误8 × 详细分析1.2 × 解决方案1.0 × 预防措施1.0 × 已解决1.0）

- [2026-09-03 21:55] **问题类型**: 使用错误（权重 6.0）**✅ 已根治（守卫 >=0 + 节点类型判定，同日第二次发作）**
  - **描述**: **池索引 0 守卫复发**——IR生成 生成函数 参数循环 `参数.附加 > 0` 守卫：首函数首参数的类型节点恰在池索引 0（无前置结构体的最小源码）时漏解析，指针参数登记随之漏（`[ir2] 下标左值对象未支持` + 级联被调错误）。上轮（检查块 类型ID 归一）同日刚根治过同款。
  - **解决**: `>= 0` + `池.元素(索引).类型 == 节点_类型` 判定。
  - **预防**: 池索引 0 合法已是成文教训——**新写任何「解引用池索引」守卫时先查 lessons 本条**，统一模板 `>= 0 && < 池.大小() && 节点类型匹配`；最小源码（无结构体前置）是索引 0 的天然触发器，新功能探针应包含一个无结构体变体。
  - **权重**: 6.0（使用错误6 × 详细分析1.0 × 解决方案1.0 × 预防措施1.0 × 已解决1.0）

## 高权重问题（③ 第二波：字符串下标+复合赋值+登记点补全，2026-09-03）

- [2026-09-03 16:30] **问题类型**: 设计缺陷（权重 10.5）**✅ 已根治（方案A 用户裁决）**
  - **描述**: **类型登记表三缺口（缺陷①②同族复发）**——③ 第一波登记点枚举漏「成员加载值」「用户函数调用返回」「容器方法调用返回」三条产生路径：`打印(名片.标题)`/`打印(取名())` 错分派 `__cn_print_int` 打指针值；`十六进制位(..)+十六进制位(..)`（代码生成共用.cn:30 符号修饰核心）双侧未登记→拼接判定不命中→裸 IR_加 指针+指针；`"x"+取名()` 右侧未登记→`__cn_str_from_int(指针)` 转数字串。**同一陷阱第二次发作**（第一波教训 9.0 后仅隔数小时），且 v2 组件核心路径（符号修饰/读源文件/读签名）全部在用。
  - **解决**: 方案A 登记点补全——成员字段类型（查字段类型ID→类型码）/用户函数返回（池预扫 函数名→返回类型名ID 映射）/容器方法返回（签.返回文本→类型码）+拼接左侧末端推导兜底（普通二元节点补 子起点/附加 标注）。**附带坑**：结果.值 载荷类型推导——`合成名首实参` 返回首 `$` 后**全部**（如"字符串$整32"），须再经 `段首` 剥离成"字符串"再查类型码；漏段首则 T_未知 静默不登记（打印仍打指针值，而 `.值` 偏移恰好正确极具迷惑性）。
  - **预防**: **登记制机制的登记点清单要「按产生路径全枚举」验收**：字面量/标识符加载/成员加载/调用返回（内置+用户+容器方法）/运算产物/类型转换产物——每上一条新登记表时对照此六类清单；**合成名函数（首实参/段首/基名）语义各异，拼装类型名时核对该函数返回的精确形态**（首 $ 后全部 vs 首段），asm 级验证登记是否生效（print_int vs print_str）而非只看运行不崩。
  - **权重**: 10.5（设计缺陷10 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 16:35] **问题类型**: 设计缺陷（权重 10.0）**✅ 已根治（方案A 用户裁决：五形态一次收口）**
  - **描述**: **复合赋值词法有 token、解析无分支 → 静默丢句 → 冒烟假绿**：词法层双字符匹配实存 `+=`（2026-08 起），解析层 解析语句 零消费分支——`a += 2` 表达式兜底解析 `a` 后 `+=` 不消费 → 解析块循环保护**静默中断** → 块内后续语句（含 返回 0）全部丢弃、sem5 零诊断。**后果**：上轮「词法分析.cn 冒烟语义 0 错误」实为假绿——分词 函数在首个 +=（line 121）后被截断（节点 488→556、asm 2046→2736 行实证），词法器本体从未被 v2p 完整编译；既有 E2E 119~128 零 += 使用故从未测出。同族变体：-= *= /= %= 词法层无单 token，`a -= 1` 以 `-` `=` 双 token 进入同样静默断路。
  - **解决**: 词法补四 token + 解析兜底统一分支：`L op= R ≡ L = L op R` **回退重析左值**作右值首操作数（成员偏移解析期重标注，两份独立正确），合成 二元+赋值 节点——赋值/IR 两层零改动，标识符/成员/解引用左值全形态覆盖。
  - **预防**: **「冒烟绿」≠「编译对」**——无诊断通过只证明「已解析前缀干净」，不证明「全文件完整」；**冒烟结论必须核对解析节点数/函数数/asm 行数与源码规模的相称性**（本轮 488 节点 vs 556 节点即截断铁证）；**新增词法 token 时必须同轮核对解析层是否有消费分支**（词法层先行=制造「已支持」假象）；语句级静默中断家族（B6 裸块/本轮 +=）共享同一检测手段：退出码为非断言垃圾值 + 尾部语句 IR 缺失。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.5 × 解决方案1.0 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 16:40] **问题类型**: 设计缺陷（权重 11.0）**✅ 已根治（检查块 跳过嵌套区间 + 守卫 >=0）**
  - **描述**: **语义检查块双重处理嵌套语句（长期结构性缺陷，被死代码掩盖）**：解析块 的父块子区间按池线性包含嵌套块（当体/如果体/裸块）的全部语句——检查块 父层原始遍历先把嵌套语句**在父层作用域处理一遍**，控制节点递归再处理一遍：嵌套声明**泄漏注册到父层作用域**（作用域语义错误）；「块级遮蔽检查」被**自注册误触发**（126/127 无任何真实遮蔽却报错——该检查自 P7b 上线以来因 类型ID 取垃圾文本**从未真正触发过**，本轮 类型ID 约定统一后复活即连环误伤）。**双重掩盖**：死检查掩盖双重处理、双重处理又以「幂等无害」掩盖泄漏。
  - **解决**: 检查块 预收集嵌套体块区间（裸块 [子起点,自身)/当与如果 [体块.子起点,体块节点]），主循环跳过——嵌套语句只在递归作用域内处理一次；真实遮蔽最小用例（外层+嵌套同名容器）验证仍正确触发。**附带坑**：类型节点索引 0 是合法池位——归一守卫 `> 0` 会漏首个声明（按节点索引查 驻留文本 取垃圾文本），须 `>= 0` 且以 池.元素(索引).类型==节点_类型 判定。
  - **预防**: **「从不触发的检查」要当缺陷对待**（用最小正例验证检查真的会触发——本轮遮蔽检查死而复生即翻车）；**区间重叠的树形结构（父区间含子区间）在遍历器里必须显式跳过嵌套区间**，否则双重处理是必然；**池索引 0 是合法值**，`> 0` 守卫对索引语义错误（用 `>= 0`+节点类型判定）。
  - **权重**: 11.0（设计缺陷10 × 详细分析1.8 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 16:45] **问题类型**: 设计缺陷（权重 7.5）**✅ 已根治（约定统一）**
  - **描述**: **v2 符号表 类型ID 双约定并存**——局部变量/参数登记类型**节点索引**（变量声明.附加 原样入表）、顶层静态/常量/函数登记类型**名ID**——消费端（本轮下标对象白名单判定）无从分辨取哪套；既有块级遮蔽检查 `驻留文本(节点索引)` 取错文本**静默失效**（从不触发，真假阳性皆无）。
  - **解决**: 局部/参数注册时归一为类型名ID（池.元素(附加).类型==节点_类型 → 取 .文本），全套符号统一名ID 约定。
  - **预防**: **跨层 ID 约定（节点索引 vs 名ID vs 类型码）必须全局唯一**——新增表登记点时先盘点既有同字段约定再落笔；「从不触发的检查」要当缺陷对待（用最小正例验证检查真的会触发）。
  - **权重**: 7.5（设计缺陷8 × 详细分析1.2 × 解决方案1.0 × 预防措施1.3 × 已解决1.0）

## 高权重问题（③ 自举对拍启动，2026-09-03）

- [2026-09-03 11:20] **问题类型**: 使用错误（权重 7.0）**✅ 已根治（③ 第一波）**
  - **描述**: **IR_常量整数 的操作数1 是「值文本驻留 ID」而非数值**（P7a 语义：代码生成经 驻留文本 还原立即数）——类型大小 折叠直接传宽度数值 4，asm 里 movz #0（驻留 ID 4 还原失败）；调试中 dbg 打印宽度=4 与 asm 常量=0 并存，极具迷惑性（打印的是同一变量，坏的是序列化约定）。
  - **解决**: `驻留(整数转字符串(宽度))` 先转文本再驻留（对齐整数字面量分支的 n.文本 形态）。
  - **预防**: **v2 IR 指令的「整64 字段」约定不一：操作数多为寄存器号/槽号，但 IR_常量整数 操作数1 与 IR_常量字符串 相同=驻留 ID**——发射 IR 前核对该 opcode 的字段语义（查既有分支怎么填），勿按直觉传数值；「dbg 值对而产物错」优先怀疑跨层序列化约定（ID/文本/数值），不是逻辑错。
  - **权重**: 7.0（使用错误6 × 详细分析1.5 × 解决方案1.2 × 预防措施1.0 × 已解决1.0）

- [2026-09-03 11:25] **问题类型**: 逻辑错误（权重 6.5）**✅ 已根治（全量回归把关）**
  - **描述**: v2 源码编辑中残留的**残缺占位行**（`如果 (...) { 未完 }`）被宿主当合法语句静默编译：`未完` 走「未定义标识符→函数名兜底负压栈→裸调用发射中文 hex 符号」链路，构建成功无任何警告，产物行为不可预测（本轮表现为常量错位）——未定义符号在链接期也未必暴露（中文 hex 符号无定义表拦截）。
  - **解决**: 定位后删除占位行；以「重建 v2p + 冒烟 + 全量 E2E 回归」三连验证。
  - **预防**: **编辑 v2 组件（或任何 CN 源码）后禁止只看构建成功**——冒烟用例+全量回归必须跟上；AI 编辑产生的不完整代码（含 TODO/占位）在提交前用 git diff 逐行自审；「构建成功但行为诡异」先怀疑源码里有非预期形态的合法解析。
  - **权重**: 6.5（逻辑错误8 × 详细分析1.2 × 解决方案1.0 × 预防措施1.0 × 已解决1.0）

- [2026-09-03 12:10] **问题类型**: 设计缺陷（权重 9.0）**✅ 已根治（用户审计轮当场）**
  - **描述**: **「按用面缩窄」的半实现产生静默错误代码**（两处，探针 asm+运行时实证）：① 打印字符串变量错分派——字符串性判定用「登记集合」但登记点漏了变量/参数（根因更深：函数级变量类型权威表 ②c 只登容器/结果/结构体局部与参数，**标量漏登**），`打印(s)` 打印指针值；② `字符串+数值` 拼接判定「任一侧字符串即拼接」但数值侧未做 `__cn_str_from_int` 转换——42 位模式被当指针传 `__cn_str_concat`，运行时段错误。两处均因 128 载体未覆盖该形态而潜伏。
  - **解决**: 字符串寄存器集合升级**「寄存器→类型码登记表」**（登记点全产生路径：整数/字符串字面量、标识符加载（标量局部/参数入权威表补齐）、拼接产物、内置返回、比较六运算与逻辑与/或/非产物 T_布尔）；拼接双侧隐式转换（from_int/float/bool）+ 打印四分派（print_str/float/bool/int）；128 用例扩展 27/28/29 失败码硬断言+打印分派运行时载体固化。
  - **预防**: **登记制机制（类型/符号表）上线时登记点必须枚举全部产生路径**（字面量/变量/参数/运算产物/调用返回），否则消费端必然错分派；**缩窄实现面时未覆盖形态必须显式拒绝（诊断）或补全语义**——判定逻辑对未覆盖形态错误放行=静默错误代码，比未实现更糟；E2E 载体只断言已覆盖形态，**潜伏缺陷要靠「写探针主动证伪」而非等载体**（本轮 p5/p6 三行探针即实锤）。
  - **权重**: 9.0（设计缺陷10 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-03 12:15] **问题类型**: 流程违规（权重 8.0）**✅ 已根治（当场修复+纪律重申）**
  - **描述**: 自审 diff 发现「类型大小折叠栈底负符号残留」后，以「所有栈消费相对栈顶、128 十连断言实证无害」为由**记入 HANDOFF 推给 ③ 后续**——正是 AGENTS.md 明令禁止的「下轮再说」模式（一劳永逸：发现问题当场解决）；且该判断依赖「未来消费逻辑不改绝对位置」的脆弱假设。
  - **解决**: 用户质询后当场弹 2 根除（实参+被调残留）；三缺陷一并按「全部当场根治」裁决闭环。
  - **预防**: **「无害」论证不能成为推后理由**——影响面判断会随后续重构失效；发现即修（改动小+回归验证兜底），推前的唯一合法形态是「用户明示裁决」而非 AI 自判。
  - **权重**: 8.0（流程问题按设计缺陷8 × 详细分析1.5 × 解决方案1.0 × 预防措施1.3 × 已解决1.0）

## 高权重问题（②b 结构体元素容器收官，2026-09-02）

- [2026-09-02 13:30] **问题类型**: 设计缺陷（权重 12.0）**✅ 已根治（②b B5）**
  - **描述**: **字段按统一 8 字节读/写宽 → 末字段越界清零相邻槽 → 死循环 → 堆 OOM SIGKILL**——v2 的 IR_指针存储/加载 无宽度概念（赋值存储 类型 写死 0、成员加载写死 T_整64，双后端固定 `str x10,[x9]`/`mov [rax],rcx` 8 字节）。126 段① `n.子终点=3`（整32@20）8 字节写越出 24 字节结构体 4 字节，恰好清零紧邻 i 槽低 4 字节 → `当(i<20)` 每轮归零 → 无限 追加 → 堆无限增长。三重实锤：限内存 2GB 退出 201（追加 realloc 耗尽）；asm 级 8 字节 str；n 基址/i 槽地址逐字节对上。
  - **解决**: IR容器 分桶 字段类型表→类型宽度码（窄宽码 T_整8/整16/字符）+ 分桶字段偏移表；IR生成 成员访问三形态解析（标识符→变量结构体表；调用对象→方法签名返回类型分桶收口，根治扁平回退同名字段跨结构体必错）；赋值存储按字段宽度；双后端 ldrb/ldrh/ldr w、strb/strh/str w（MASM movzx/mov byte-word-dword ptr 对称）。
  - **预防**: **凡结构体字段访问（读/写/传参步长），宽度一律查字段类型表，禁止默认 8 字节**；「死循环/OOM」分诊先查**写宽越界破坏相邻变量**再查循环逻辑本身（本例 i 槽被末字段击穿而循环代码完全正确）；限内存 ulimit -v 把 OOM SIGKILL 转为可断言的干净退出码，是 OOM 分诊首选手段。
  - **权重**: 12.0（设计缺陷10 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-02 13:35] **问题类型**: 设计缺陷（权重 11.0）**✅ 已根治（②b B6 + 连带裸块缺陷）**
  - **描述**: 两枚静默地雷：① **retbuf 固定 2 槽=16 字节**——≥17 字节结构体返回（元素(i)->节点=24）每次向上越界踩顶变量槽（126 恰踩未存活槽侥幸不炸；与 2026-09-01 20:35 同族——固定尺寸假设对新增尺寸失效）；② **裸块语句 `{}` 解析缺失**——解析语句 无 `{` 分支 → 表达式兜底不消费 → 解析块 循环保护**静默中断**，块内及后续全部语句（含 返回 0）丢失，函数以垃圾寄存器值退出（126 退出码 20）。
  - **解决**: ① 函数IR.retbuf字节数（生成时累计最大返回字节）+ 共用层 retbuf槽位数/retbuf基槽 单点决策（帧尺寸与基槽地址同一函数，杜绝漂移）；② 解析语句 补裸块分支（递归 解析块）+ IR生成 预扫描跳过裸块内部语句（排除 如果/当 体块防双重生成）+ 节点_块 递归生成块（作用域基线/块出口析构复用）。
  - **预防**: **缓冲区尺寸凡按已知形态硬编码（2 槽/16 字节），新增更大形态必炸——尺寸必须数据驱动**（返回字节数查表）；**「语句被静默丢弃」的信号是退出码为非断言小整数**（垃圾返回值低 8 位）——遇非常规退出码先查解析是否提前中断；循环保护中断处应打诊断（本案静默中断吞掉半个函数）。
  - **权重**: 11.0（设计缺陷10 × 详细分析1.5 × 解决方案1.2 × 预防措施1.1 × 已解决1.0）

- [2026-09-02 13:40] **问题类型**: 使用错误（权重 8.5）**✅ 已规避（②b B7 供给机制）**
  - **描述**: **宿主 cn build 对非 主.cn 命名入口静默丢弃全部函数**——供给.cn 直接 build 只产出 断言 一个函数（1.3KB 空壳 obj、0 个向量$ 符号）且无诊断；改名 主.cn 后同文件完整编译（30 符号）。根因：宿主按规范08-四 仅以「模块名==主」为入口模块，其余按导入模块**只合并公开声明**（私有函数与 主 函数全过滤）——规范行为而非缺陷，但静默性极具迷惑性。
  - **解决**: run_e2e.py 供给机制将供给源拷贝为独立目录 主.cn 再编译（代码注释记录规范依据）。
  - **预防**: **给 cn build 的入口文件必须命名 主.cn**（或全部声明 公开）；「编译成功但产物空」先查入口模块名判定，勿怀疑代码生成。
  - **权重**: 8.5（使用错误6 × 详细分析1.5 × 解决方案1.3 × 预防措施1.2 × 已解决1.0）

## 高权重问题（宿主运行时根治，2026-09-01）

- [2026-09-01 23:50] **问题类型**: 设计缺陷（权重 13.0）**✅ 已根治（用户裁决体内二分）**
  - **描述**: **__cn_intern 驻留池指针悬垂（UB）**——pool 用 `std::vector<std::string>`，扩容搬移全部 string，已发放的 `c_str()` 指针悬垂。表现为 v2p 大帧函数「字符串参数跨调用被随机清空」（破坏位置随扩容时机漂移）、签名扫描器「注释文本混入方法名」——asm 层全对（不是写坏而是数据搬家），人工最小复现全部失败（复现者驻留量不足触发扩容）。
  - **解决**: pool 改 `std::deque<std::string>`（push_back 不使已有元素指针失效，C++ 标准保证）。体内二分定位法（v2p 逐句隔离：常量实参也丢、字符串长度 不丢 → 锁定 驻留 特有）是布局敏感 UB 的高效定位手段。
  - **预防**: **任何对外发放裸指针的常驻池（符号表/字符串池/AST 池）必须用指针稳定容器（deque/arena/unique_ptr），禁止 vector<string> 直存**；「UB+复现失败+asm 全对」三特征优先怀疑指针悬垂而非写越界；CN 语义中 驻留文本 返回值被长期持有是硬契约。
  - **权重**: 13.0（设计缺陷10 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-09-01 23:55] **问题类型**: 逻辑错误（权重 8.0）**✅ 已根治**
  - **描述**: v2 签名扫描器三连缺陷：①判定用 令牌_关键字 但 v2 词法关键字表无「泛型」→ stdlib 整文件跳过（表空）；②泛型参数序误收声明引导词「类型」（`泛型 <类型 T>` 的 类型）→ 参数序多段、命中错位（T 不替换）；③返回类型为源码形态 `结果<空类型,整32>`（尖括号），v2 是结果类型 判定基于 $ 合成名 → 追加 误判标量（无 retbuf → 崩溃）。
  - **解决**: ①判定按文本匹配不限令牌类型；②跳过「类型」标识符；③归一结果名（< > , 一律换 $）。
  - **预防**: v2 扫描类组件对 stdlib 源码的 token 判定**不得假设关键字表覆盖**（按文本匹配）；语法引导词（类型/作为 等）在收集器中显式排除；**跨层类型名必须归一到统一形态（$ 合成名）再判定**。
  - **权重**: 8.0（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

## 高权重问题（用户令缺陷零容忍后首例，2026-09-01）

- [2026-09-01 21:40] **问题类型**: 逻辑错误（权重 9.5）**✅ 已根治（方案 A）**
  - **描述**: **链式字段取值 `向量.元素(i).字段` 读错值**——lvalueAddress 与 visitMemberExpr 的对象类型推导只认 标识符/嵌套成员/下标/指针算术 四种形态，不认 CallExpr 对象：类型推导空 -> decl==null 防御 return base（丢字段偏移）-> LoadPtr 读结构体首 8 字节拼合值（元素(19).附加 读出 19|(1<<32)）。与 2026-08-14「赋值路径漏 CallExpr 右值」同族——**同一缺口第三次出现在不同路径**（赋值→118 链式方法→本次字段访问）。
  - **解决**: 两处补 CallExpr 分支经 exprSrcType 解析返回类型；structReturn 调用的 lastExpr_=隐藏返回缓冲基址，值即结构体地址。
  - **预防**: **凡「按对象形态分派」的 IR 路径（类型推导/左值地址/赋值右值/传参），形态集合必须覆盖：标识符/成员/下标/解引用/二元指针算术/CallExpr（返回结构体）六种**——新增消费路径时对照此清单核查；历史三次缺陷全是漏 CallExpr。
  - **权重**: 9.5（逻辑错误8 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）


## 高权重问题（P7b v2 容器支持 新增，2026-09-01）

- [2026-09-01 20:30] **问题类型**: 设计缺陷（权重 12.0）**✅ 已解决（P7b 轮）**
  - **描述**: **MASM 符号 247 字符限制被多参函数击穿**——v2 的 生成表达式 加第 9 个参数（映射<整64,整64>& 变量类型）后 mangled 符号（?…@@Y + 7 个 _T/A_T 参数编码）恰超 247 -> ml64 A2043 identifier too long -> **PROC 符号注册失败 -> 函数体内 bb 标签泄漏全局作用域 -> 后续函数同名 bb 标签连锁 A2005**。症状极具迷惑性：汇编错误是 bb 标签重定义而非符号超长本身。
  - **原因**: 宿主 nameMangle 无长度截断；v2 组件函数参数含多个容器/结构体类型（每个 _T+hex 编码 40-60 字符），9 参即超限。
  - **解决**: 参数类型信息改为语法层预标注（变量容器表 + 查容器类型 导出函数），生成表达式/生成左值地址 回退 8 参（符号 约 208 字符）。
  - **预防**: v2 组件函数参数 >=8 个时必须核算 mangled 长度（每容器/结构体参数约 +40~60 字符）；类型信息传递优先语法层预标注（节点字段/导出查询函数），而非扩参数表。
  - **权重**: 12.0（设计缺陷10 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-01 20:35] **问题类型**: 逻辑错误（权重 10.0）**✅ 已解决（P7b 轮）**
  - **描述**: **v2 帧布局两处「向上写越界」**——retbuf 起始取高槽号（12 字节从 [rbp-96] 向上写到 [rbp-84)，覆盖槽1 的表对象指针）；结果变量 2 槽基址取低槽号（12 字节从 [rbp-104] 向上写覆盖相邻变量槽）。两者都导致后续方法调用 this 为垃圾 -> SIGSEGV。
  - **原因**: v2 槽地址公式 [rbp-8*(最大寄存器+槽)-8] 中槽号越大地址越低；**多字节写入（rep movsb 12/16 字节）从基址向上增长**——基址必须取占用的**最低地址**（即最大槽号），否则向上越界覆盖槽号更小的变量。
  - **解决**: retbuf 槽号=槽基数+序号*2；结果变量 局部槽 登记 槽r+1（高槽号）。
  - **预防**: v2 帧内任何「多槽变量/缓冲区」的基址一律取最低地址（最大槽号）；单槽变量无方向性无此问题。手搓 MASM 对照实验是隔离「生成代码 vs 链接环境」的高效手段。
  - **权重**: 10.0（逻辑错误8 × 详细分析1.5 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-01 20:40] **问题类型**: 设计缺陷（权重 9.0）**✅ 已解决（P7b 轮）**
  - **描述**: **向量<字符串> 跨向量搬移行指针悬垂**——重排 asm 输出时把 输出 的行指针 追加 进局部 重排 向量后 返回 重排；输出 是局部变量，函数结束 RAII 析构释放其字符串元素（向量<字符串> 拥有式 char*），返回值里的指针全部悬垂 -> 写文件读到已释放内存 -> asm 乱码/错行。
  - **原因**: 向量<字符串> 元素为拥有式（RAII 释放）；浅拷贝指针进另一向量=双重拥有；v2 原代码只在**单个输出向量内**追加（拼接产生新串），无跨向量搬移故未触发。
  - **解决**: 去掉重排向量；数据行由 代码生成 主函数末尾扫描 IR（IR_常量字符串）直接 输出.追加——零搬移。
  - **预防**: v2 组件中**禁止把字符串元素从一个向量搬进另一个向量后让源析构**；跨向量共享字符串须确认所有权；输出变换优先「单向量内构造」而非多向量搬运。
  - **权重**: 9.0（设计缺陷10 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-09-01 20:45] **问题类型**: 逻辑错误（权重 7.0）**✅ 已解决（P7b 轮）**
  - **描述**: **AST 节点字段复用伪造子区间**——成员节点的 子终点 从 0 改存「对象类型ID」后，语义层把 [子起点,子终点) 当子表达式区间遍历（跨节点扫到尚未声明的标识符）-> 提前检查未注册变量 -> 大量「未声明标识符」误报。
  - **解决**: 类型查询改语法层导出函数 查容器类型（变量容器表），成员节点 子终点 恒 0。
  - **预防**: v2 后缀池 AST 的 子起点/子终点 是「子表达式区间」语义——任何字段复用都会被语义层当区间遍历；附加信息走导出查询函数而非占用区间字段。
  - **权重**: 7.0（逻辑错误8 × 详细分析1.2 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 12 层 B 任务多文件拆分 新增，2026-08-18）

- [2026-08-18 05:00] **问题类型**: 设计缺陷（权重 10.0）**✅ 已解决（第 12 层）**
  - **描述**: **CN 版组件链不支持 `::` 限定名调用**——前置验证发现 `关键字::查找关键字`
    在 CN 版语法分析.cn 中被解析为两个独立标识符（`标识符|关键字` + `标识符|查找关键字`），
    而 C++ 版解析为 `成员访问`；CN 版 IR生成.cn 的 `处理成员节点` 硬编码为容器方法
    （`向量$字符串$方法`），`调用符号` 只处理已知符号。拆分门面文件（含 `::` 调用）后
    79 闭环编译门面文件必然失败。
  - **原因**: CN 版语法分析.cn 的 `解析后缀` 只处理 `.`/`->`/`[`/`(`/`++`/`--`，不处理
    `::`；IR生成.cn 未实现模块限定调用符号生成。
  - **解决**: 扩展语法分析.cn `解析后缀` 增加 `::` 分支（输出 `成员|::|名` 行）；扩展
    IR生成.cn `处理成员节点` 增加 `::` 分支（生成 `模块名$函数名` 符号，忽略调用文本中
    的模块名，用当前模块名——79 闭环合并门面+子模块源码后统一 IR 生成）。
  - **预防**: 拆分 CN 组件前必须先验证 CN 版组件链对目标语法特性的支持；`::` 限定名
    调用是模块拆分的基础能力，须先扩展。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 05:00] **问题类型**: 设计缺陷（权重 10.0）**✅ 已解决（第 12 层）**
  - **描述**: **子模块导入父模块形成循环依赖**——`语法/语句.cn` 写 `导入 语法::表达式`
    后，依赖收集取首段 `语法`，与 `语法.cn` 声明子模块（`模块 语句`）形成
    `语法 -> 语句 -> 语法` 循环依赖，编译报「模块循环依赖」。
  - **原因**: 子模块导入父模块（`语法::表达式` 首段 `语法`）与父模块声明子模块互为依赖。
  - **解决**: 子模块间不写导入语句，函数经门面导入链合并后全局可见，纯名调用。
  - **预防**: 子模块间互相调用不写导入（避免循环依赖），靠门面导入链合并后纯名调用。
  - **权重**: 10.0（设计缺陷10 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 05:00] **问题类型**: 集成问题（权重 7.0）**✅ 已解决（第 12 层）**
  - **描述**: **辅助函数名跨模块冲突**——`语义/内置.cn` 的 `行类型`/`行文本`/`函数名`
    与 IR生成.cn 同名，`语义/符号表.cn` 的 `列表追加` 与 代码生成.cn 同名，纯名调用
    合并后歧义（`函数 '行类型' 调用存在歧义`）。
  - **原因**: 拆分后子模块辅助函数与未拆分组件（IR生成.cn/代码生成.cn）同名，纯名
    调用在合并阶段歧义。
  - **解决**: 语义门面辅助函数用完整三级路径限定名（`语义::内置::行类型` 等），
    避免纯名歧义。
  - **预防**: 拆分时注意子模块辅助函数名与未拆分组件同名冲突，用完整路径限定名区分。
  - **权重**: 7.0（集成问题7 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 05:00] **问题类型**: 逻辑错误（权重 8.0）**✅ 已解决（第 12 层）**
  - **描述**: **门面局部变量遮蔽模块名**——`语义分析.cn` 门面 `检查` 函数局部变量
    `向量<字符串> 符号表` 与模块名 `符号表` 冲突，`符号表::符号查找` 被解析为
    `向量$字符串` 的成员访问（`类 '向量$字符串' 没有成员 '符号查找'`）。
  - **原因**: 局部变量名与模块名同名，限定名调用被解析为成员访问。
  - **解决**: 用完整三级路径 `语义::符号表::符号查找` 区分。
  - **预防**: 门面局部变量名避开模块名，或限定名用完整路径。
  - **权重**: 8.0（逻辑错误8 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 05:00] **问题类型**: 工具执行错误（权重 2.0）**✅ 已解决（第 12 层）**
  - **描述**: **PowerShell 正则替换引入反引号**——批量替换 `语义分析.cn` 辅助函数调用
    时，PowerShell `-replace` 正则中的反引号转义被误写入文件，导致 `非法字符: \`` 编译错误。
  - **原因**: PowerShell 正则替换复杂转义易出错。
  - **解决**: 重写文件为干净的限定名版本。
  - **预防**: 批量替换后必须重新读取文件确认无非法字符；复杂替换优先用 write_to_file 重写。
  - **权重**: 2.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 11 层 性能基线对比 新增，2026-08-18）

- [2026-08-18 08:20] **问题类型**: 工具执行错误（权重 4.0）**✅ 已解决（第 11 层）**
  - **描述**: **gtest 源码缺失导致 CMake 配置失败**——`cmake -S . -B target/build` 报
    `FATAL_ERROR: 未找到系统 Google Test 且本地源码不可用`。CMakeLists.txt 期望
    `${CMAKE_SOURCE_DIR}/../../third-party/unittest/googletest`（即
    `C:\Users\ChenChao\Documents\third-party\unittest\googletest`），新机器/新环境无此目录。
  - **原因**: gtest 源码不在 CMakeLists.txt 期望路径（`Documents/third-party`，注意不是
    `gitcode/third-party`——`../../` 从 CN_Language_C 上两级是 Documents）。
  - **解决**: 从 GitHub codeload 下载 googletest v1.14.0 源码
    （`https://codeload.github.com/google/googletest/tar.gz/refs/tags/v1.14.0`），解压到
    `C:\Users\ChenChao\Documents\third-party\unittest\googletest`。注意 v1.14.0 嵌套一层
    `googletest/`，需将内层 `src`/`include` 上移一层（CMakeLists.txt 期望
    `googletest/src/gtest-all.cc` 与 `googletest/include/gtest/gtest.h`）。
  - **预防**: 换机器/换环境先检查 gtest 源码路径是否存在；下载 googletest 后核对目录
    结构（src/gtest-all.cc + include/gtest/gtest.h 在期望位置）。
  - **权重**: 4.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 08:20] **问题类型**: 工具执行错误（权重 4.0）**✅ 已解决（第 11 层）**
  - **描述**: **运行时 .obj 手动复制导致链接失败**——把 CMake 构建的 9 个运行时 .obj
    复制到 target/ 后，cn build 链接报 `LNK2019: 无法解析的外部符号 __imp_fputs` 等
    15 个错误。cn build 的 compileRuntime 会自己编译 target/ 下运行时 .obj（带缓存：
    obj 新于 cpp 则跳过），手动复制的 .obj 时间戳较新导致缓存跳过；且 CMake 默认 /MD
    （动态 CRT）与 cn build 链接 `/DEFAULTLIB:libcmt.lib`（静态 CRT）冲突。
  - **原因**: 手动复制 .obj 干扰 compileRuntime 缓存机制 + CRT 模式不匹配（/MD vs /MT）。
  - **解决**: 删除手动复制的 .obj，让 cn build 自己编译运行时 .obj（cl 默认 /MD 与
    link /DEFAULTLIB:libcmt.lib 实际可链接，之前失败纯因缓存跳过用了 CMake 的 .obj）。
  - **预防**: 运行时 .obj 一律由 cn build 的 compileRuntime 生成，不要手动复制；
    若需手动准备，须确认 CRT 模式与链接参数一致。
  - **权重**: 4.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 08:20] **问题类型**: 逻辑错误（权重 4.0）**✅ 已解决（第 11 层）**
  - **描述**: **性能测量单次波动大，需取中位数**——M2/M5 单次测量波动大
    （M2: 1615→2501ms，M5: 1619→1629ms），首次运行冷启动/系统负载导致数据不可靠。
  - **原因**: 单次测量受冷启动、系统负载、磁盘缓存等影响，波动可达 50%+。
  - **解决**: M2/M5 各测 3 次取中位数（排序后取中间值），数据稳定复现
    （M2: 697.714ms / M5: 727.932ms，比值稳定 ≈1.04x）。
  - **预防**: 性能基准测量必须多次采样取中位数（或最小值），单次测量不可信。
  - **权重**: 4.0（逻辑错误8 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-18 08:29] **问题类型**: 逻辑错误（权重 4.0）**✅ 已解决（第 11 层）**
  - **描述**: **性能报告样本不可审计 + 推演假设未标注**——bench_self_host.py 报告只写
    M2/M5 中位数，未展示 3 次样本（中位数正确但不可复现验证）；6 万行推演按
    「耗时与行数成正比」线性外推但未标注假设，与 004 推演（80s~5min）差异也未解释。
  - **原因**: 报告只输出结论性中位数，未保留原始样本；线性外推假设与 004 小样本
    外推的口径差异未在报告中说明。
  - **解决**: 脚本存储 M2/M5 样本数组并写入报告测量点表格；报告第五节新增「推演假设」
    块（耗时×60000/3604，哈希映射 O(1) 增长影响小）；说明 004 基于 30 行小样本外推、
    本轮基于 3604 行实测更可信。
  - **预防**: 性能报告必须展示原始样本（可审计）+ 标注外推假设与局限；与历史数据
    对比须解释口径差异（小样本 vs 实测规模）。
  - **权重**: 4.0（逻辑错误8 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 10 层 Debug 自举闭环审查 新增，2026-08-17）

- [2026-08-17 23:30] **问题类型**: 逻辑错误（权重 9.6）**✅ 已修复（第 10 层）**
  - **描述**: **79 自举闭环用例是虚假验收——链接顺序导致 CN 组件链产物被整体忽略**。`run_e2e.py` 链接响应文件把 79 入口 obj（C++ 版 `cn build` 产物，其中内联了全部组件函数）排在 5 个 `*_chain.obj`（CN 组件自编译产物）之前，配合 `/FORCE:MULTIPLE` 时 MSVC link 保留命令行靠前的第一个定义 → 组件符号全部绑定 C++ 内联版，链.obj 被 LNK4006 整体忽略。map 铁证：`词法分析$分词` 等全部符号绑定 `79_bootstrap_closed_loop.obj`；nochain 实验：去掉全部 5 个链.obj 仍链接成功且产物不变。第二次落盘实际由 C++ 版组件执行，「CN vs CN 固定点」沦为「C++ vs C++」假验证。
  - **原因**: 链接顺序错误 + 无符号保留方向自检。`/FORCE:MULTIPLE` 下重复符号保留「命令行靠前定义」，入口 obj 在前即覆盖链.obj。
  - **解决**: ① 链.obj 提到入口 obj 之前（决定性修复）；② 链接加 `/MAP` 并新增步骤 4.5 符号自检（每个链.obj 必须贡献符号 + cn_main 来自入口 obj，否则判失败）；③ 步骤 5 前删除旧 .asm（防第二次未写、旧文件假通过）；④ 判据表述纠正为「CN 自编译版组件产物与 C++ 版逐字节一致 = 阶段7验收步骤2 产物行为一致」；⑤ LIB 路径探测健壮化（MSVC 版本目录 + Windows Kits 最新版）。
  - **预防**: 凡 `/FORCE:MULTIPLE` 链接多份同名定义，必须验证符号保留方向（map/dumpbin）+ 做 nochain 对照实验（去掉候选 obj 仍能链接=该 obj 被忽略）；自举闭环验收必须含「符号绑定 CN 产物」自检，不能只比对两次产物。
  - **权重**: 9.6（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-17 23:30] **问题类型**: 工具执行错误（权重 4.0）**✅ 已解决（第 10 层）**
  - **描述**: **MSVC 增量编译未重编导致「改后仍失败」假象**——修改 `test_bootstrap_lexer.cpp` 后 `cmake --build target` 报 `Error: could not load cache`（target 不是 cmake 缓存目录，正确构建目录是 `target/build`），且 exe 时间戳未变（旧二进制）。全量单测仍报旧断言失败，而 filter 运行因只匹配到部分测试误判「已修复」。
  - **原因**: 构建目录用错（`target` vs `target/build`）+ 未确认 exe 时间戳更新。
  - **解决**: 用 `cmake --build target/build --config Debug --target cn_unit_tests` + touch 源文件 + 确认 exe LastWriteTime 更新后再跑测试。
  - **预防**: 改 C++ 源后：① 用正确构建目录（target/build）② touch ③ build 并确认 exe 时间戳变化 ④ 跑全量测试（勿用 filter 误判）。
  - **权重**: 4.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 9 层 Debug 全面审查 新增，2026-08-15）

- [2026-08-15 16:30] **问题类型**: 逻辑错误（权重 8.4）**✅ 已修复（第 9 层）**
  - **描述**: **顶层静态变量 P3-8 未实现（孤立代码）**——`静态 整64 计数器 = 0` 语义层仅登记符号名（`globalStaticNames_`），IR 层零消费：`visitProgram` 不遍历 `node->globals`，函数体内引用全局静态名落入 FuncAddr 分支（当函数名处理），生成 `mov [rbp0], rax` 未分配槽汇编（ml64 A2006 undefined symbol: rbp0）。实测编译失败：`target\错误: 汇编失败（ml64 退出码 1）`
  - **原因**: 第 4 层实现 P3-8 时只做了语义登记（globalStaticNames_ + declareVar），IR/codegen 落地缺失——「语义登记 + IR 零消费」= 孤立代码；函数体内静态名 lookupVar 失败后无全局静态识别分支
  - **解决**: ① semantic `globalStaticNames_` → `globalStatics_`（名→源码类型）+ `globalStaticType()` 查询；② IRModule 加 `globalStatics`/`globalStaticInits`；③ IR `visitProgram` 遍历 globals 登记 + `visitIdentifierExpr`（读，放 lookupVar 失败后）/`visitAssignmentExpr`（写+复合赋值）/`visitUnaryExpr`（自增自减）三路径识别全局静态 → `?gstatic_名` 符号 LoadPtr/StorePtr；④ x64/arm64 codegen `.data` 段发射 `?gstatic_`/`_cn_gstatic_` 全局符号（含整/浮/字符串初始值）+ nameMangle/GAS 化；回归单测 TopLevelStaticDecl + 实测（5/8、100→102→97、2.5、真）全对
  - **预防**: 凡「语义层登记符号（globalStaticNames_ 类）」必须有 IR 层消费（visitProgram 遍历 + 读/写/自增路径）+ codegen 发射三件套；E2E 只测合法路径，缺陷先实测编译确认再修
  - **权重**: 8.4（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 16:30] **问题类型**: 设计缺陷（权重 9.2）**✅ 已修复（fecab2b A-2：类型/常量 crate 分桶，plans/018 第 31 轮实证翻案）**
  - **描述**: **跨模块同名类型/常量未隔离**——merge 阶段已按模块分桶允许（CrateTypeBuckets，模块X/模块Y 各定义 结构体 记录 合并成功），但语义层 typeNames_（declareTypeName）/globalConstValues_（declareVar）/scopes_ 仍全局去重 → 语义阶段报「重复声明类型 '记录'」「重复声明变量 '常量值'」+ 类型混淆（模块Y 的记录 读到 X 的字段）。v2.0 用户核心诉求「跨包同名符号不冲突」的**函数维度已隔离**（44 验证），类型/常量维度缺失
  - **原因**: 第 4 层 crate 分桶只覆盖函数（moduleName 前缀）与 merge 阶段类型（CrateTypeBuckets），语义层类型表（typeNames_/结构体符号表）与顶层常量/静态表未按模块分域——类型系统核心改造未同步
  - **解决**: ✅ **已修复（提交 fecab2b，A-2）**：类型级 crate 分桶（declareTypeName 按模块+typeModules_/constModules_/staticModules_ + 限定键 globalConstValuesQualified_）+ 限定类型语法 `甲::记录` + 引用按当前模块解析重写；锚点单测翻转为正断言 + E2E 56_type_isolation。**plans/018（2026-09-07）实证复核**：类型/常量跨模块同名 rc=0、glob 歧义三维度（函数/类型/常量）诊断齐全 rc=1——本条原「📌 已知限制」状态过时，防后续 AI 误判为未修（教训：已知限制条目在修复提交后必须同步翻状态，且「锚点单测消失」=状态已变的信号）
  - **预防**: 写跨模块同名测试前先实测语义层行为（merge 通过 ≠ 语义通过）；类型/常量隔离须三处同步（merge 分桶 + 语义分域 + IR mangling）
  - **权重**: 9.2（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已评估0.6）

## 高权重问题（第 8 层 全链路集成串联 新增，2026-08-15）

- [2026-08-15 15:55] **问题类型**: 逻辑错误（权重 9.0）**✅ 已修复（第 8 层）**
  - **描述**: **无参函数跨模块注册 key 解析缺陷**——52_library 中 `工具库::格式化::版本()` 报「未找到匹配的函数 '版本'」。无参函数（sigKey 无 `#`）跨模块同名注册 key=`格式化$版本`，但 resolveOverload/hasFunctionName 的 keyModule 提取条件只处理 `模块名$名#参数`（`$` 在 `#` 之前，`hashFirst != npos`），无 `#` 时 `hashFirst=npos` 使条件 false → keyModule 不提取、base=`格式化$版本` ≠ `版本` → 限定调用匹配失败
  - **原因**: 无参函数 sigKey 无 `#`，跨模块 key=`模块名$函数名` 与带参 key=`模块名$名#参数` 形态不同；第 4 层修复「泛型实例 排序$整32 不剥离」时只覆盖了带 `#` 形态，未覆盖无参跨模块形态
  - **解决**: resolveOverload/hasFunctionName 补 `hashFirst == npos && dollarPos != npos && name.find('$') == npos`（调用名不含 `$`，区别于泛型实例）时剥离 `$` 前缀；新增单测 CrateIsolateNoParamQualifiedCall
  - **预防**: `$` 前缀剥离的判定必须覆盖全部 key 形态（带参/无参/泛型实例）；无参函数跨模块同名是 crate 隔离的常见场景（如 版本/数量）
  - **权重**: 9.0（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 15:55] **问题类型**: 逻辑错误（权重 8.5）**✅ 已修复（第 8 层）**
  - **描述**: **跨 crate 限定调用 moduleFilter 精确匹配失败**——52_library 中 `工具库::格式化::版本()` 重写后 moduleFilter=`工具库::格式化`（subModule 完整路径），但外部依赖模块（依赖/工具库/格式化.cn）注册 moduleName=`格式化`（文件主干，loadModuleTree externalModule 取 pathStem），resolveOverload 精确匹配 `entryModule != moduleFilter` 直接 continue → 报「未找到匹配的函数 '版本'」
  - **原因**: 同包子模块（网络::传输控制）moduleName=完整路径与 moduleFilter 精确匹配；跨 crate 外部依赖模块 moduleName=文件主干，调用路径 subModule=包名::模块名 —— 两者形态不一致
  - **解决**: resolveOverload moduleFilter 分支增加最后段匹配——moduleFilter 末段（`::` 之后）== entryModule 即视为同一模块（跨 crate 限定调用解析到依赖包内同名模块）；新增单测 CrateIsolateCrossCrateLastSegmentFilter
  - **预防**: 跨 crate 模块的 moduleName 与调用路径前缀不一致（文件主干 vs 包名::模块名）；限定调用解析须兼容外部依赖模块（最后段匹配），同包内仍精确匹配
  - **权重**: 8.5（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 15:50] **问题类型**: 集成问题（权重 7.5）**✅ 边界已规避（第 8 层）**
  - **描述**: **跨模块泛型类字段类型不匹配**——馆藏类 `向量<整64>` 字段 + 构造 `图书ID表 = 向量<整64>()` 报「无法将 '向量$整64' 隐式转换为 '向量<整64>'」。泛型类实例化（跨模块合并）返回类型是 mangled 名 `向量$整64`，而字段声明/局部变量类型是模板名 `向量<整64>`——编译器类型表对两者未统一
  - **原因**: 跨模块泛型类（stdlib/容器.cn）实例化的类型键与源码模板语法 `向量<整64>` 形态不一致；38_tool 只在局部变量用向量（已验证），类字段场景未覆盖
  - **解决**: 馆藏 类改用手动动态数组（`整64*` + 容量/数量，stdlib 容器.cn 同款模式）；向量<整64> 由主.cn 局部变量承担（30_container 已验证）
  - **预防**: 跨模块泛型类实例化只放局部变量/参数；类字段需要动态容器时用手动动态数组或固定数组；向量<T> 8 字节槽约束下类对象不能存向量（指针/ID 标量可以）
  - **权重**: 7.5（集成问题7 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 7 层 新增 v2.0 特性测试 新增，2026-08-15）

- [2026-08-15 15:10] **问题类型**: 设计缺陷（权重 9.2）**✅ 边界已记录（第 7 层，非缺陷）**
  - **描述**: **花括号项别名在跨模块同名场景下歧义**——E2E 44_crate_isolate 中 `导入 模块Y::{双倍 作为 Y双倍}` 后纯名 `Y双倍(10)` 报「函数 '双倍' 调用存在歧义（模块Y$双倍#整64 与 双倍#整64 均可匹配）」。别名重写回原符号名 `双倍` 后未设置 moduleFilter，resolveOverload 对 模块X$双倍/模块Y$双倍 无法区分
  - **原因**: 语义层别名重写（semantic.cpp:2252）只把别名映射回原符号名，未携带原模块上下文（moduleFilter）——单模块别名正常（无歧义），跨模块同名时原符号名在多个 crate 分桶下多义
  - **解决**: E2E 聚焦「限定调用隔离」主题删除别名调用段；单测 UseImportRenameConflictLastWins 覆盖单模块别名语义（编译器实现为后写覆盖不报错）；边界记录于 44 注释 + HANDOFF + 更新日志
  - **预防**: 写 v2.0 测试前先实测编译器行为边界（重命名/通配符/交集），E2E 只用支持路径；跨模块同名 + 别名是已知边界，留待编译器完善（moduleFilter 传播）
  - **权重**: 9.2（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6）

- [2026-08-15 15:15] **问题类型**: 逻辑错误（权重 8.0）**✅ 边界已记录（第 7 层，非缺陷）**
  - **描述**: **父模块名限定调用子模块函数 moduleFilter 不匹配**——E2E 46_module_tree 中 `网络::连接()` 报「未找到匹配的函数 '连接'」。`导入 网络::连接` 触发依赖链加载后，语义层 visitCallExpr 把 moduleFilter 设为 `网络`（moduleName 首段），但函数实际注册 moduleName=`网络::传输控制`（子模块目录层级），resolveOverload 按 moduleFilter 过滤不匹配
  - **原因**: 再导出（公开 导入 网络::传输控制::连接）合并后函数 moduleName 是完整子模块路径，而调用侧 `网络::连接` 只提供父模块前缀
  - **解决**: 再导出场景按编译器支持路径编写——纯名 `连接()`（与 47_package_cargo 一致）+ 多级路径 `网络::传输控制::发送()`（subModule 完整匹配）；边界记录
  - **预防**: 子模块符号的限定调用必须用**完整路径**（父::子::符号）或纯名；父模块名单独限定子模块函数是编译器边界（需 moduleFilter 前缀匹配）
  - **权重**: 8.0（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6）

- [2026-08-15 15:05] **问题类型**: 工具执行错误（权重 4.0）**✅ 已解决（第 7 层）**
  - **描述**: **整路径重命名纯名调用不可用**——E2E 44 初稿 `导入 模块Y::双倍 作为 Y双倍` 报「未声明的标识符 'Y双倍'」。语义层 visitImportDecl 把整路径重命名绑定为"模块级别名"（use.aliases[别名]=moduleName），纯名调用走别名重写时 `aliasIt->second == ui.first`（别名映射目标是模块名自身）被跳过
  - **原因**: 重命名导入的编译器语义 = 模块级别名（`别名::符号` 限定访问），非符号纯名别名；花括号项重命名（`{双倍 作为 Y双倍}`）才是符号别名
  - **解决**: 44 改为花括号项重命名形式（与 45/单测 UseImportBraceAlias 一致）
  - **预防**: v2.0 重命名导入：花括号项 `::{名 作为 别名}` → 纯名别名可用；整路径 `导入 模块 作为 别名` → 仅 `别名::符号` 限定访问
  - **权重**: 4.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

## 高权重问题（第 6 层 全量迁移 新增，2026-08-15）

- [2026-08-15 14:40] **问题类型**: 逻辑错误（权重 9.6）**✅ 已修复（第 6 层）**
  - **描述**: **入口文件 主.cn 辅助函数被加 crate 前缀导致 LNK2019**——38_tool 迁移到 v2.0 语法后首个暴露：17 个辅助函数（统计计算/解析单行/工具主体等）定义侧链接符号带 `主$` 前缀（第 4 层 crate 前缀机制，IR visitFunctionDecl `node->moduleName != "主"` 未排除入口模块名），但同文件调用侧语义层按纯名重写（semantic.cpp:2613 已有 `moduleName != "主"` 判断）→ 定义侧 `主$解析单行` vs 调用侧 `解析单行` 符号不匹配，link 退出码 1120（17 个未解析外部符号）
  - **原因**: 第 4 层 crate 前缀规则只排除了 `name=="主"`（主 函数本身），未排除**入口文件模块名=="主" 的辅助函数**——语义层与 IR 层前缀规则不对称（语义调用侧已跳过 moduleName=="主"，IR 定义侧未跳）。该缺陷被 38_tool 旧语法（`.`/`从`）编译失败掩盖（38_tool 从未进入链接阶段）
  - **解决**: ir.cpp visitFunctionDecl 前缀条件补 `node->moduleName != "主"`：`if (!node->moduleName.empty() && node->moduleName != "主" && node->name != "主" && ...)`
  - **预防**: 链接符号前缀规则必须**定义侧（IR）与调用侧（语义）对称**；入口文件 主.cn 内全部函数（含辅助函数）永不加 crate 前缀；迁移旧语法用例后链接类缺陷可能暴露——E2E 编译失败类错误要区分编译期 vs 链接期（link 1120 是链接期）
  - **权重**: 9.6（逻辑错误8 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 14:45] **问题类型**: 任务未完成（测试行为契约破坏，权重 7.0）
  - **描述**: **E2E 迁移误改打印输出标签导致 expected 不匹配**——迁移 43_module_import 时把打印标签 `从导入乘` 改成 `花括号导入乘`（认为旧语法描述应改），与 expected 文件逐行比对失败（输出不一致 FAIL）。E2E 迁移的契约是**只改语法不改行为**——打印输出是程序行为一部分，expected 是契约
  - **原因**: 迁移时把注释/标签一并"现代化"，未意识到 expected 文件比对的是程序实际输出
  - **解决**: 恢复 `打印("从导入乘: ", 积3)` 原标签后通过
  - **预防**: E2E 迁移只动导入语句/限定调用（`::`）与可见性标签（`公开:`），**不改任何打印输出**；迁移后必须跑 E2E 确认逐行比对通过
  - **权重**: 7.0（任务未完成9 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决0.6）

## 高权重问题（第 4 层 crate 模型核心重构 新增，2026-08-15）

- [2026-08-15 05:00] **问题类型**: 逻辑错误（权重 9.8）**✅ 已修复（第 4 层）**
  - **描述**: **模块名前缀计算用了 pathStem（去目录）**——目录层级 `net/transport.cn` 加载时模块名只取 `transport`（pathStem 去掉 net/ 前缀），导致子模块符号 `send` 注册 moduleName="transport" 而非 "net::transport"，`net::transport::send()` 限定调用 resolveOverload 过滤不匹配报「未找到匹配的函数 'send'」。诊断打印 loadModuleTree 的 relPart="net/transport.cn" 但 moduleName="transport" 确证
  - **原因**: loadModuleTree 用 `pathStem(relPart)` 计算模块名，pathStem 用 find_last_of 去目录——目录层级模块名必须保留完整相对路径
  - **解决**: 模块名改用 relPart 完整路径（去扩展名 + 斜杠转 `::`）：`net/transport.cn` → `net::transport`
  - **预防**: 目录层级模块名 = 文件相对入口目录的完整路径，禁止用去目录的 pathStem；子模块符号归属完整路径模块名
  - **权重**: 9.8（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 05:10] **问题类型**: 设计缺陷（权重 11.2）**✅ 已修复（第 4 层）**
  - **描述**: **单文件场景也加 crate 前缀导致全量 E2E 链接失败 1120**——单文件（无导入，如 03_function 的 函数.cn）经 runModulePipeline merge 后 moduleName=文件主干（"函数"），IR 层给函数加 `函数$双倍` 前缀，codegen 找不到（`?E5878F@@YHH@Z` 无法解析）；同时 `主` 函数被加前缀破坏 `主→cn_main` 映射（入口无 cn_main 符号）
  - **原因**: crate 前缀只应在多模块（有导入）场景启用；单文件/主 函数须保持纯名链接（codegen symbolName 的 主→cn_main、打印→__cn_print_* 映射基于纯名）
  - **解决**: mergeModules singleModule=true 时 crateName 置空（不写入 moduleName）；IR 层 `node->name != "主"` 排除 主 函数；语义层 resolvedSignature 同规则（calleeName != "主"）
  - **预防**: 链接符号前缀改动后必须全量 E2E 回归（单文件场景最易被破坏）；主/内置符号永不加前缀
  - **权重**: 11.2（设计缺陷10 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 05:20] **问题类型**: 逻辑错误（权重 9.0）**✅ 已修复（第 4 层）**
  - **描述**: **resolveOverload 把泛型实例名 `排序$整32` 误当模块前缀剥离**——`$` 剥离条件 `key.find('#') > dollarPos`（hashPos=npos 时比较 true）导致 `排序$整32` 被剥成 keyModule="排序"/key="整32"，base="整32" ≠ 调用名 "排序$整32" → 40_generic_funptr E2E 报「未找到匹配的函数 '排序$整32'」
  - **原因**: 泛型实例化名（名$实参）与模块前缀（模块名$名#参数）都含 `$`，需区分：模块前缀必有 `#` 且 `$` 在 `#` 前
  - **解决**: 剥离条件限定 `hashFirst != npos && dollarPos < hashFirst`（`#` 存在且 `$` 在 `#` 前）
  - **预防**: `$` 在符号名中多义（模块前缀/泛型实例/内部唯一名），剥离模块前缀必须结合 `#` 存在性判定
  - **权重**: 9.0（逻辑错误8 × 详细分析1.8 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 05:30] **问题类型**: 集成问题（权重 7.8）**✅ 已修复（第 4 层）**
  - **描述**: **MSVC 增量编译时间戳粒度导致 apply_diff 后构建不重编**——多次改动 semantic.cpp/driver_module.cpp 后 `cmake --build` 显示无编译行（"BUILD_DONE" 无输出），运行单测仍用旧库（反复"改后仍失败"假象），需 PowerShell `(Get-Item file).LastWriteTime = Get-Date` touch 强制重编
  - **原因**: apply_diff 写文件时间戳与 MSVC 依赖检查粒度相同，可能不触发重编；findstr 过滤构建输出看不到编译行
  - **解决**: 改动后构建须确认目标源文件重编（findstr 编译行）；必要时 PowerShell touch 强制
  - **预防**: 改 C++ 源后：① touch（PowerShell LastWriteTime）② build 并确认编译行 ③ 跑测试；避免"改了没编"浪费数轮调试
  - **权重**: 7.8（集成问题7 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 05:40] **问题类型**: 集成问题（权重 7.5）**✅ 已修复（第 4 层）**
  - **描述**: **Windows 中文临时文件写入踩坑**——单测 writeTempFile 用 `std::ofstream` 窄字符写中文文件名失败（ANSI 代码页），改用 `_wfopen_s` 宽字符写但内容误写 UTF-16（`\0` 乱码）；最终方案 `_wfopen_s` 宽路径 + UTF-8 字节直写
  - **原因**: Windows ofstream 窄字符路径按 ANSI（GBK）解释，UTF-8 中文文件名找不到；`_wfopen_s` 宽路径正确但内容须保持 UTF-8 字节（不转 UTF-16）
  - **解决**: `MultiByteToWideChar(CP_UTF8)` 转宽路径 + `_wfopen_s` + `fwrite` UTF-8 字节
  - **预防**: Windows 测试写中文文件名一律 `_wfopen_s` 宽路径 + UTF-8 字节；验证写入成功（文件存在 + 内容字节）；`结果` 等关键字不可作变量名
  - **权重**: 7.5（集成问题7 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

## 高权重问题（Task 6.13 模块导入语法测试补全 新增，2026-08-15）

- [2026-08-15 07:10] **问题类型**: 任务未完成（测试覆盖不足，权重 9.0）
  - **描述**: **E2E 测试只覆盖一种导入语法形式**——标准库 E2E（`tests/e2e/29_core/主.cn` 等）只用了 `导入 模块.符号`（`导入 核心.交换`）这一种形式，`从 模块 导入 名`、整模块导入（`导入 模块`）、多级路径（`导入 A.B.C`）、多符号逗号分隔、边界错误（导入不存在符号/模块/私有符号/跨模块重名）等**全部未测试**。用户指出测试未彻底
  - **原因**: 标准库各模块 E2E 以"验证功能"为目标，未系统性覆盖导入语法本身的全形式与边界——`导入 核心.交换` 恰好是功能测试所需的最小导入形式，掩盖了其余语法未被验证的事实
  - **解决**: 本轮补全——E2E `tests/e2e/43_module_import/`（主.cn + 模块甲/乙/丙，覆盖 ① 导入 模块.符号 ② 导入 模块 ③ 导入 多级路径 ④ 从 模块 导入 名 ⑤ 从 模块 导入 名1,名2 ⑥ 公开纯名/类/枚举 ⑦ 重载 ⑧ 泛型跨模块）+ 单测 7 用例（导入不存在符号/从导入不存在名/跨模块重名/重载共存/未导入限定调用 P1/导入不存在模块文件/正向链路）；run_e2e.py 不支持预期编译失败 → 边界错误用单测覆盖
  - **预防**: 新增功能/库模块的 E2E 必须覆盖**全部公开语法形式 + 边界**：E2E 测合法路径全形式，错误/边界路径用单测（若 E2E 运行器不支持预期编译失败）；测试清单核对"语法全集清单"而非"首个可用写法"
  - **权重**: 9.0（任务未完成9 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-15 07:20] **问题类型**: 设计缺陷（P1 已知缺陷，权重 10.0）
  - **描述**: **未导入模块的限定调用被静默接受**——`数学.平方根(16.0)` 在**无任何导入语句**（真实编译器验证：单文件无 数学.cn 也编译成功）时未报「未声明的标识符 数学」，构建成功退出码 0。语义层对"模块名.函数"形式的 object 标识符未校验是否已声明/已导入
  - **原因**: 语义层 `visitCallExpr` 模块限定重写块（semantic.cpp 1984-2031）条件 `userFuncExists || moduleImported || hasFunctionName(qualified)` 为 false 时不进块，落入成员方法调用分支（2262），`checkExpr(object)` 对未声明标识符（数学）未报错（该分支只对类类型校验成员）
  - **解决**: 未修复（本轮聚焦测试补全）。**根治方向（供阶段7 Debug）**：模块限定重写块后、成员方法调用分支前，补 object 为标识符时的声明检查——非类型名/非已导入模块/非内置限定名（数学.平方根/IO.读取行 等全局注册）→ 报「未声明的标识符」；单测 `SemanticQualifiedCallWithoutImport` 已显式断言当前行为做回归锚点
  - **预防**: 语义分析对"标识符.成员"形式必须校验标识符声明性（变量/类型名/已导入模块名/内置限定名）；补错误路径测试时用真实编译器验证（单测 analyzeModules 的 r.ok 只反映语义阶段，driver 完整流水线才能暴露解析/加载错误）
  - **权重**: 10.0（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6）

## 高权重问题（Task 6.11 全链路集成 新增，2026-08-15）

- [2026-08-15 04:30] **问题类型**: 设计缺陷（权重 12.0）**✅ 已修复（2026-08-15 Debug 子任务）**
  - **描述**: **大栈帧无 __chkstk 栈探测崩溃**——全链路集成工具（38_tool 学生成绩统计）首跑 0xC0000005 且**首个打印前即崩（无任何输出）**。逐级二分定位：命令行/文件/分割/转整数/容器/算法/数学/时间/结构体返回/文件写入 单测全部通过（诊断 1-15 版），合并后崩溃。汇编分析发现 codegen 栈帧 = 寄存器槽区 + 变量槽区（含打印变参展开的临时寄存器），单函数栈帧 12488/11992/11864 字节 > 8KB 时崩溃；诊断13版各函数 <8KB 通过。Windows 栈 guard 页机制：`sub rsp, N`（N>8KB）越过已提交栈页 → 访问冲突（无 __chkstk 探测）
  - **原因**: codegen 未实现 MSVC 的 `__chkstk` 栈探测——大栈帧函数 `sub rsp, 大立即数` 一次性越过 64KB 栈保留区 + guard 页，未提交栈页被访问 → 0xC0000005。与函数行数/局部变量/打印次数正相关（打印变参展开生成大量临时寄存器槽）
  - **解决**: **✅ 根治（2026-08-15）**：codegen `emitPrologue` 栈帧 >4KB 时发射 MSVC 三段式 `mov rax, N / call __chkstk / sub rsp, rax`（__chkstk 按 4KB 页探测提交，符号由 libcmt.lib 提供）；`EXTERN __chkstk:PROC` 声明；`emitParamSetup` 提前到 chkstk 之前（__chkstk 破坏 rcx 参数寄存器，参数槽 rbp 相对寻址不依赖 rsp 已分配，安全）。回归：单测 BigFrameEmitsChkstk/SmallFrameNoChkstk + E2E 39_chkstk（1200 局部变量 >9KB 栈帧，修复前崩溃修复后正常输出 校验和 719400）
  - **预防**: 编写 CN 语言程序时单函数局部变量+打印总量控制在栈帧 <8KB；凡"程序无输出即崩"先检查各函数汇编 `sub rsp, N` 是否 >8KB；大程序必须拆分多小函数（也符合用户规则单函数≤100行）
  - **权重**: 12.0（设计缺陷10 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决0.6 × 影响度0.7）

- [2026-08-15 04:00] **问题类型**: 集成问题（权重 8.5）
  - **描述**: **跨模块重复声明 `错误码` 枚举冲突**——38_tool 用例目录同时复制 容器.cn + 映射集合.cn（两个模块各自 `枚举 错误码`），编译报「跨模块重复声明类型 '错误码'（模块 映射集合）」。30_container/31_map_set 各自单模块导入未冲突（首次同时导入两个含同枚举模块暴露）
  - **原因**: 标准库 容器.cn/核心.cn/映射集合.cn 各自定义 错误码 枚举（设计时每模块独立可用，未考虑同目录多模块同时导入的合并冲突）
  - **解决**: 用例目录副本 映射集合.cn 删除自身枚举，改 `导入 容器.错误码` 共享
  - **预防**: 多模块同时导入时，若各模块重复定义同名枚举/类型，用例副本可改导入共享（根治方向：错误码枚举集中到核心模块供其他模块 导入）
  - **权重**: 8.5（集成问题7 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

## 高权重问题（Task 6.5/6.9/6.10 字符串扩展+时间+系统库 新增，2026-08-15）

- [2026-08-15 03:20] **问题类型**: 集成问题（权重 10.8）
  - **描述**: **Windows 系统库命令行参数三连坑**——① `/ENTRY:WinMainCRTStartup` 下 WinMain 形参无 argc/argv（第4参是 nCmdShow），原 `entry(0, nullptr)` 导致 `系统.参数个数()` 恒 0（E2E 37_system 首跑失败）；② 改转发 CRT 全局 `__argv` 后中文参数乱码（`__argv` 是 ANSI/GBK 代码页，CN 字符串按 UTF-8 处理）；③ 再改 `__wargv`（CRT 宽参数）后 argc 归零（`__wargv` 仅 wmain 入口初始化，WinMain 下 nullptr）
  - **原因**: Windows CRT 入口模型与 POSIX main 不同：WinMain 形参无 argv；`__argc/__argv` 为 ANSI 版（GBK 代码页）；`__wargv` 只在 wmain 路径初始化（/ENTRY:WinMainCRTStartup 不走该路径）
  - **解决**: `GetCommandLineW` + `CommandLineToArgvW` 从进程原始命令行（Unicode）解析 + `WideCharToMultiByte(CP_UTF8)` 转 UTF-8 缓存（依赖 shell32.lib，linkExe 与 CMake 同步补充）；Linux 直接缓存 entry 的 argv（POSIX 命令行按字节传递）
  - **预防**: Windows 命令行参数获取一律用 `CommandLineToArgvW(GetCommandLineW())` + CP_UTF8 转换，勿依赖 `__argc/__argv/__wargv`（入口模型差异 + 代码页问题）；新增 Windows 平台运行时功能先验证入口（WinMain/main/wmain）差异
  - **权重**: 10.8（集成问题7 × 详细分析1.8 × 解决方案1.3 × 预防措施1.3 × 已解决0.9）

- [2026-08-15 03:10] **问题类型**: 设计缺陷（权重 10.5）
  - **描述**: **内置函数限定名前缀不可用类型关键字**——注册 `字符串.转整数` 后 E2E 35_string_ext 编译报「预期表达式，实际是 '字符串'」（35_string_ext/主.cn:24:14）；`字符串` 是类型关键字（Kw_String），词法器将 `字符串.转整数` 拆为 关键字+标识符；同族问题：`结果` 关键字（Kw_Result）不可作变量名（字符串扩展.cn 局部变量 `字符串 结果` 报「预期变量名，实际是 '结果'」）
  - **原因**: 词法器最长匹配优先——类型关键字 `字符串`/`结果` 等在任何位置都先被识别为关键字，`字符串.转整数` 中的 `字符串` 不可能是模块名/标识符；标准库模块公开纯名 `字符串转整数`（无点号）不受影响
  - **解决**: 字符串解析内置改注册为 `解析.转整数`/`解析.转浮点`/`解析.转布尔`（`解析` 非关键字）；stdlib/字符串扩展.cn 局部变量 `结果` 改 `缓冲`
  - **预防**: 新增带前缀限定名的内置函数，前缀必须是**非关键字标识符**（`数学`/`IO`/`文件`/`时间`/`系统`/`解析` 安全；`字符串`/`整数`/`浮点` 等类型关键字禁止）；标准库局部变量避免用 `结果`/`错误`/`正常`/`某些` 等内置构造器关键字
  - **权重**: 10.5（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.9）

- [2026-08-15 03:00] **问题类型**: 逻辑错误（权重 8.2）
  - **描述**: **strto* 仅空白输入误判合法**——单测 `StrToIntWhitespaceOnly`（`"   "`）期望失败实际 ok=1（返回 0）：strtoll 对仅空白输入不消费任何字符（end 指向开头），但代码先跳过尾部空白（跳过的是开头空白）后判定 `end != str` 误以为"解析了数字"，空白被当作尾部空白吞掉
  - **原因**: `end != str` 判定在"跳过尾部空白"之后执行——仅空白输入时 end==str（未解析），跳过空白后 end 指向串尾，`*end=='\0'` 成立 → 误判合法
  - **解决**: `end != str`（parsed 判定）移到跳过尾部空白**之前**——`const bool parsed = (end != str);` 再跳空白判定 `*end == '\0'`（与 input_api.cpp 的 __cn_read_int 同 bug，一并防御）
  - **预防**: strto* 整串合法性判定必须"先判 parsed（end != str）再跳尾部空白"；仅空白/空串输入 end==str 恒成立
  - **权重**: 8.2（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

## 高权重问题（Task 6.4/6.7 映射集合+算法库 新增，2026-08-15）

- [2026-08-15 02:40] **问题类型**: 设计缺陷（权重 11.0）**✅ 已修复（2026-08-15 Debug 子任务：substTypeParam/substGenericType 保留函数指针后缀，E2E 40_generic_funptr）**
  - **描述**: **泛型函数 + 函数指针参数不可用**——`泛型 <类型 T> 函数 排序(T* 数组, 整64 长度, 整32(*比较)(T, T))` 编译报「未找到匹配的函数 '排序$整32'（参数个数或类型不匹配）」（验证B/C 实验）；泛型类方法带函数指针参数报「无法将 '函数指针<整32>(整32,整32)' 隐式转换为 ''」（验证E 实验，形参类型解析为空）。非泛型函数指针参数正常（验证A 通过）
  - **原因**: 泛型函数单态化时，参数中的函数指针类型 `整32(*比较)(T, T)` 内嵌的 T 未被替换为实参类型（或类型 key 编码与调用方不匹配），导致实例化后签名与调用实参不符；泛型类方法形参类型解析路径对函数指针参数返回空类型
  - **解决**: 规避——算法库 P0 排序/二分 用具体类型（整32/整64）重载 + 比较内联（冒泡 `数组[内层] > 数组[内层+1]`）；泛型自由函数（无函数指针参数，线性查找/计数/最大/逆序）全部正常（验证D/G/AI 通过）。**根治方向**：泛型函数单态化时对函数指针类型参数做 T 递归替换，或函数指针类型用统一 key 编码
  - **预防**: 标准库/测试代码**不要在泛型函数/泛型类方法参数中使用 C 风格函数指针类型**（T 内嵌在函数指针签名里无法单态化替换）——需回调时用具体类型重载或比较内联；新增泛型函数先跑最小验证（泛型+函数指针组合）再写正式代码
  - **权重**: 11.0（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6 × 影响度0.6）

- [2026-08-15 02:50] **问题类型**: 逻辑错误（权重 12.0）**✅ 已修复（2026-08-15 Debug 子任务：IR emitClassMethod 设 genericTypeParams_ + 语义 resolveGenericTypeName 裸参数替换，E2E 42_generic_field_loop）**
  - **描述**: **泛型类方法内循环遍历第一个泛型字段运行时损坏**——双字段（或双类型参数）泛型类方法内 `当(索引<长度){ 如果(键数组[索引]==目标) 返回 索引 }` 恒返回 -1/假（验证N/P/Y/AC/AG 实验）；S/T/U/AA 实验打印 `数据[索引]` 索引≥2 时读垃圾值（如 1632087572483），索引 0/1 正常；O/AE（固定读取 `数据[位置]`）正常、X/J/AF（单字段+循环）正常、Z（循环读第二字段 整64*）正常——**仅「第一个泛型字段 + 循环」组合损坏**
  - **原因**: 泛型类方法体内第一个 T* 字段的 LoadPtr 循环场景地址计算/寄存器分配缺陷（具体待 Debug 子任务定位 ir_oop_call/codegen；与 26_generics 记录的多类型参数泛型类构造字段写入异常同源家族）
  - **解决**: 规避——映射/集合的循环查找逻辑放**模块级泛型自由函数**（`查找键位置<K>(K* 键数组, 整64 长度, K 目标)`，参数数组 D/G 模式实测正常），类方法仅委托调用（验证AH 通过）；打印先存局部变量（规避打印变参内嵌字段下标）
  - **预防**: 泛型类方法体内**禁止循环遍历第一个泛型字段**（`当(索引<长度){ 字段[索引] }`）——循环逻辑移入泛型自由函数（T* 参数数组）；固定下标读取可留在类方法；打印泛型类字段下标先存局部变量
  - **权重**: 12.0（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决0.6 × 影响度0.7）

- [2026-08-15 02:55] **问题类型**: 设计缺陷（权重 10.5）**✅ 已修复（2026-08-15 Debug 子任务：同泛型字段循环根因，E2E 42 打印盒.打印全部() 输出 元素0/1/2 正确）**
  - **描述**: **打印变参内嵌泛型类字段下标表达式损坏**——泛型类方法内 `打印("元素", 索引, ": ", 数据[索引])` 索引≥2 时读垃圾（验证U/S/AA 实验；验证V 打印局部变量正常、验证H 固定读取再打印正常）。泛型自由函数内打印数组下标正常（验证AI）
  - **原因**: 打印变参展开（多参数求值序列）与泛型类方法内 T* 字段 LoadPtr 组合时寄存器/槽区冲突（具体待 Debug 子任务定位）
  - **解决**: 规避——E2E/标准库打印泛型类字段值前先存局部变量（`T 值 = 表.取值(1)` 再 `打印(值)`）；泛型自由函数打印数组元素不受影响
  - **预防**: 打印变参表达式含"泛型类字段下标"时必须先存局部变量；调试泛型类输出异常先检查打印行是否内嵌字段下标
  - **权重**: 10.5（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6 × 影响度0.5）

## 高权重问题（Task 6.1 核心库+容器库 新增，2026-08-14）

- [2026-08-14 18:20] **问题类型**: 逻辑错误（权重 9.5）✅ 已修复
  - **描述**: 容器库 E2E（30_container）结构体变量**赋值**路径错误——`读取结果 = 数据.读取(99)`（结构体返回方法调用赋值）生成的汇编只存 8 字节 retbuf 地址（`mov [槽], rax`）而非 CopyStruct 12 字节内容，`.正常`/`.值` 读到地址值（非零）导致越界判断失效，链表/栈/队列全部错乱（头部追加/删除头尾/弹出/出队读旧值）
  - **原因**: ir.cpp 结构体赋值路径（2589 行起）只处理 4 种右值（标识符/成员/下标/链式赋值），**漏了 CallExpr（结构体返回调用）**——右值类型推导为空 → 落入默认 Store 只存 8 字节地址。声明初始化路径（1572 行）已有 CallExpr→CopyStruct 处理，赋值路径遗漏
  - **解决**: ir.cpp 赋值路径补 CallExpr 右值分支：`valueSrcType = targetSrcType`、`srcAddr = value`（调用返回的结构体地址），走 CopyStruct 按值拷贝；单文件 exp41 与跨模块 exp42/43/44/46 逐级二分定位后修复，E2E 32/32 通过
  - **预防**: 凡"结构体/联合体类型赋值"路径（b = 表达式），右值种类必须完整覆盖：标识符/成员/下标/链式赋值/**结构体返回调用（CallExpr）**——新增赋值场景后先搜索 ir.cpp 该分支对照；结构体字段错乱先查"只存了地址没拷内容"
  - **权重**: 9.5（逻辑错误8 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.6）

- [2026-08-14 18:30] **问题类型**: 设计缺陷（权重 12.0）**✅ 已修复（2026-08-15 Debug 子任务：构造/析构 methods 表 sigKey key + 语义/IR 按实参匹配，E2E 41_ctor_overload）**
  - **描述**: **构造函数重载缺陷**——语义层 `ClassInfo.methods` 按方法名（name）索引（`unordered_map<string, ClassMemberInfo>`），同名构造函数（无参 `向量()` / 带参 `向量(整64 初始容量)`）互相覆盖，带参构造覆盖无参构造 → 无参调用 `向量<整32>()` 链接到带参构造体（读未初始化 rdx 当初始容量），数组容量字段写入垃圾（每次运行值不同，如 393242/655376），`容量()` 读出垃圾。单文件 exp41（无带参）正常，exp46（含带参）复现——二分定位为带参构造存在即破坏无参构造
  - **原因**: methods 表 key 用方法名而非签名 key（sigKey=名#参数串），不支持方法/构造函数重载——普通方法重载因 functions_ 表（按 sigKey）正常，构造函数走 methods 表（按名）被覆盖
  - **解决**: 规避——容器库移除带参构造（预分配由 `预留()` 方法提供），E2E 32/32 通过；**根治方向**：ClassInfo.methods 改按 sigKey 索引（构造函数多个版本并存），或构造函数专用 multi-version 容器，供 Debug 子任务实施
  - **预防**: 标准库/测试代码**不要写构造函数重载**（无参+带参同名）——编译器会静默覆盖导致无参调用读垃圾；如必须带参构造，用无参构造 + 显式方法（如 预留()）替代；泛型类尤其注意
  - **权重**: 12.0（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决0.6 × 影响度0.7）

- [2026-08-14 18:40] **问题类型**: 逻辑错误（权重 8.0）✅ 已修复
  - **描述**: x64 codegen 物理寄存器宽度 A2022——泛型类方法体内 `mov eax, r12`（32 位目标 eax、64 位源 r12 尺寸不匹配）汇编失败。两处：① Store 路径（i32/u32 源为物理寄存器 r12 未转 r12d）；② emitCast 路径（i64→i32 截断 `mov eax, r12`）。Load 路径（1027 行）已有宽度处理，Store/emitCast 缺失
  - **原因**: 寄存器分配（-O2 默认启用）将 32 位值放入物理寄存器（r12 等 64 位名），代码生成 `mov eax, r12` 时未用 `widthFor` 转 32 位名 r12d
  - **解决**: ① Store 路径 src 取出后判断物理寄存器转 32 位名（与 Load 对称）；② emitCast 函数开头统一处理（srcReg 检测 + to 为 32 位整型时 widthFor 转换）
  - **预防**: codegen 中所有"32 位寄存器 ← 物理寄存器"的 mov 必须用 32 位名（r12d）；新增代码生成路径后先 grep `mov eax, r1` 类模式；泛型类方法体是触发密集区
  - **权重**: 8.0（逻辑错误8 × 详细分析1.3 × 解决方案1.3 × 预防措施1.3 × 已解决1.0 × 影响度0.5）

## 高权重问题（Task 6.6/6.3 新增，2026-08-14）

- [2026-08-14 23:10] **问题类型**: 逻辑错误（权重 8.0）
  - **描述**: 条件编译 `#如果定义 DEBUG` 命令行宏注入失效——`-D DEBUG` 传入 macros_（macros=1 实测）但 `#如果定义` 判定未命中，`#否则` 分支被保留。调试定位：extractMacroName 提取的宏名是 `DEBUG\n`（len=6，含 CR）——Windows 源文件为 CRLF（`\r\n`），process() 按 `\n` 分割行，行内容含尾部 `\r`，宏名收集循环的终止字符集合只有 空格/制表/括号，`\r` 被并入宏名 → `"DEBUG\r" != "DEBUG"` 不匹配
  - **原因**: 词法层逐行处理时未处理 CRLF（Windows `\r\n`），`\r` 混入宏名
  - **解决**: extractMacroName 宏名收集终止集合补 `\r`/`\n`（`if (c==' '||c=='\t'||c=='\r'||c=='\n'||c=='('||c==')') break`）
  - **预防**: 逐行扫描源码（按 `\n` 分割）时，行内容可能带 CRLF 的 `\r`——凡按"终止字符集合"收集标识符/宏名/符号的循环必须含 `\r`；中文关键字前缀比较必须用完整字节长度（"如果定义"=12 字节）且先比较较长关键字避免前缀截断
  - **权重**: 8.0（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0 × 影响度0.5）

> 本文件记录项目开发中遇到的问题、原因与解决方案，防止重复犯错。
> 权重规则：≥15 必须提及；10-14 应该阅读；5-9 可选；<5 可跳过。

## 高权重问题（必须避免）

- [2026-08-14 21:10] **问题类型**: 设计缺陷（权重 15.0）✅ 已修复
  - **描述**: 打印函数族命名规律不一致——`打印`（换行，Task 2.9 新语义）、`打印行`（**不换行**，"行"=逐行连续输出），但遗留的 `打印行整数(整64)`（`printf "%lld\n"` **换行**）与 `打印行浮点(浮64)`（`printf "%f\n"` **换行**）与 `打印行` 语义**恰好相反**，造成命名二义性（"行"到底代表换行还是不换行？）
  - **原因**: 阶段一内置 IO 函数 `打印行整数/打印行浮点` 沿用旧语义（"行"=换行），Task 2.9 将 `打印行` 语义改为"行"=逐行连续输出（不换行）后，未同步清理遗留函数，两套语义并存
  - **解决**: 方案C（2026-08-14）✅ 已修复：**彻底删除** `打印行整数`/`打印行浮点`——① 语义层 `registerBuiltins()` 删除注册；② x64/arm64 `symbolName()` 删除映射（`打印行整数`→`printLineInt`、`打印行浮点`→`printLineFloat`）；③ E2E/单元测试全部改用变参 `打印`（自动换行，行为完全等价：`打印(42)` 输出 `42\n`）；④ 运行时 `printLineInt/printLineFloat/printLineI128/printLineU128` **保留**（单元测试直接引用 + 防御 ABI 稳定）；⑤ 规格书10.6 明确打印函数族仅 `打印`/`打印行`（+`格式化`），"行"语义统一为不追加换行
  - **预防**: 内置函数命名一旦确定语义（尤其"行"这类多义词），后续调整必须同步清理遗留实现；删除内置函数前先搜索全项目引用（语义注册/代码生成映射/运行时导出/测试/文档五处）；"行"语义统一：`打印行`=不换行，换行一律用 `打印`
  - **权重**: 15.0（设计缺陷10 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.7）

- [2026-08-14 21:25] **问题类型**: 逻辑错误（权重 8.5）✅ 已修复
  - **描述**: 方案C替换后 04_types E2E 失败——`打印(浮32 fv)` 输出 `0.000000`（期望 `3.500000`）：变参展开对 f32 参数未 Cast 浮64 就直接调用 `__cn_print_float(double)`，x64 ABI xmm0 低32位垃圾被当 double 读
  - **原因**: 原 `打印行浮点(f32)` 靠语义层隐式宽化（浮32→浮64，codegen cvtss2sd）生成正确代码；替换为 `打印(f32)` 变参展开后，IR 层整型分支有显式 Cast i64、浮点分支漏了 f32→f64 显式 Cast，参数类型 f32 与函数签名 double 不匹配
  - **解决**: [`ir.cpp`](src/cn_compiler/ir/ir.cpp:2744) 变参展开浮点分支补 `Cast f32→f64`（emitResult Opcode::Cast），x64/arm64 codegen 的 cvtss2sd/fcvt 已有实现；重建后 E2E 28/28 通过
  - **预防**: 变参展开（打印/格式化等）的每个参数分支必须与运行时函数签名类型完全一致——f32 先 Cast f64、整型 Cast i64、无符号 Cast u64、i128 走专用辅助；替换遗留函数后必须跑全量 E2E 验证输出等价（不能只靠编译通过）
  - **权重**: 8.5（逻辑错误8 × 详细分析1.3 × 解决方案1.3 × 预防措施1.3 × 已解决1.0 × 影响度0.6）

- [2026-08-14 21:45] **问题类型**: 逻辑错误（权重 9.1）✅ 已修复（全链路审查发现）
  - **描述**: 全链路审查（方案C独立复核）发现两处遗留缺陷：① 语义层变参检查 `okNum` 缺 `字符`（`isNumeric("字符")`=false），`打印('A')`/`打印(字符变量)` 报"打印行 参数类型不支持：'字符'"——旧写法 `打印行整数('A')`（字符→整64隐式转换）替换为 `打印('A')` 后被误拒，IR 层本已支持（字符=i32 Cast i64 走 `__cn_print_int` 输出ASCII码）；② 常量折叠 `foldCast` f32→f64 直接按 double 解析（`(double)(3.14f)` 得 3.1400000000000001，正确应为 3.1400001049041748），f32 精度在折叠时丢失——%f 6位小数恰好掩盖，但 `格式化("%.10f", 3.14f)`/`浮点转字符串` 输出错误
  - **原因**: ① 变参类型白名单枚举遗漏 `字符`（isNumeric 不含字符，okStr 只收字符串系）；② 浮→浮折叠未先按源类型 f32 截断再提升
  - **解决**: ① [`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp:2004) `okNum` 补 `argType == "字符"`；② [`const_fold.cpp`](src/cn_compiler/opt/const_fold.cpp:379) f32→f64 先 `static_cast<float>` 再提升；回归验证 `打印(3.14f)`/`打印(fv)`/`格式化("%.10f", 3.14f)` 均输出 3.1400001049，E2E 28/28、单元 965/965 全通过
  - **预防**: ① 变参内置函数的类型白名单必须与 IR 展开分支一一对应（IR 支持的每个 IR 类型都必须在语义白名单放行）；② 浮点类型转换折叠必须保持"源精度 → 目标精度"语义（f32→f64 先截断 f32），不能跳过中间精度；删除内置函数/替换调用后，除 E2E 全量外还要用边界类型（字符/浮32）补最小用例验证
  - **权重**: 9.1（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.3 × 已解决1.0 × 影响度0.6）

- [2026-08-14 16:10] **问题类型**: 集成问题（权重 16.6）
  - **描述**: 阶段C 串联集成验证发现 `--debug` 源码注释链路两处断裂：① **parser 表达式节点缺 location**——`parser_expr.cpp` 的 CallExpr/IdentifierExpr/字面量/BinaryExpr/UnaryExpr/IndexExpr/MemberExpr/AssignmentExpr/TernaryExpr/CastExpr 创建时未设置 `location`（默认空 SourceLocation），IR 指令 `inst.loc` 全无效，`DebugInfoCollector::isValidLoc` 过滤全部注释——`--debug` 后 x64/arm64 全优化级别汇编中源码注释 0 条，且**无任何报错（静默断裂）**；② **debug_info 双前缀注释**——`commentFor` 返回带注释前缀文本（`// src:`），而 x64/arm64 的 `AsmWriter::comment()` 又自动加前缀（`; `/`// `），产出 `// // src:` 双前缀
  - **原因**: ① parser 表达式节点创建时只关心语法正确性，location 字段被遗漏（语句/声明节点有设置、表达式节点全漏）；调试信息消费端 `isValidLoc` 静默过滤无效位置，无诊断——链路断裂无感知；② `commentFor` 与 writer 职责边界不清，两处都加前缀
  - **解决**: ① `parser_expr.cpp` 全部表达式节点补 `location` 赋值（叶子取 token 位置、复合取左操作数/运算符位置）；② `commentFor` 改为返回纯文本 `src: 文件.cn:行`，注释前缀由各后端 AsmWriter 统一添加，删除 `AsmCommentStyle`/`setCommentStyle` 死代码并同步单测
  - **预防**: **任何 AST 节点创建后必须设置 location**（特别是表达式节点）——IR 指令 loc、未来 DWARF 定位全部依赖它；调试信息消费端遇到无效位置应可诊断（当前静默过滤是隐患）；生成注释文本时先确认 writer 是否已自动加前缀（避免双前缀）；每次新增 CLI 开关（--debug/--no-regalloc）必须实际运行验证产物（grep 汇编注释数），不能只靠单测
  - **权重**: 16.6（集成问题7 × 详细分析1.8 × 解决方案1.5 × 预防措施1.5 × 已解决1.0 × 影响度0.8）

- [2026-08-14 15:20] **问题类型**: 逻辑错误（权重 31.2）
  - **描述**: 阶段B 优化器（LICM）两次引入破坏正确性的外提：① 循环体漏收集 back edge tail 块——`findNaturalLoops` BFS 未 `body.insert(tail)`，循环内 Store 槽未入 writtenSlots → 循环变量 Load 误判"值不变" → 依赖指令外提 → 02_control 打印垃圾值（-1868819032）；② 外提指令引用"循环内 Load 定义寄存器"——Load 结果加入不变集合但 Load 指令留在循环内，`减 %v108 %v106 1` 外提到 preheader 后引用未定义的 %v106 → 14_integration2 排序错乱（员工0 年薪 0）
  - **原因**: ① 自然循环 body 集合算法缺 tail（自环 tail==header 也被 `t!=h` 误排除）；② 不变集合混入"值不变但定义留在循环内"的副作用指令结果（Load），外提引用它们的指令造成使用前未定义
  - **解决**: ① `body.insert(tail)` + 允许自环（tail==header 时 body 仅含 header）；② Load/副作用指令结果**不加入不变集合**，外提阶段仅纯运算且操作数全为常量/循环外寄存器/未写槽
  - **预防**: LICM 外提前必须保证"被外提指令的操作数在 preheader 处已定义"——凡依赖循环内 Load/副作用指令定义的寄存器一律不外提；自然循环 body 必须含 back edge tail；每个新 Pass 挂载 -O2/-O3 后必须全量 E2E（-O0/-O2/-O3 三级别一致性）验证
  - **权重**: 31.2（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.8）

- [2026-08-14 15:20] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: 阶段B 函数内联三连炸：① `取学号#学生`（含 `AddrOf 参数`）内联后 `取地址 入$6` 引用不存在的参数槽 → 10_struct_value 垃圾值；② lambda 匿名函数 `?lambdaN` 被内联，捕获参数（值快照/引用捕获）被参数折叠破坏 → 18_缺陷修复 lambda值快照垃圾值；③ 模块/重载函数 `平方根#浮64` 被内联，跨文件运行时库调用语义错误 → 27_module 平方根输出 0.0
  - **原因**: 内联 isInlineable 白名单过宽：未排除 AddrOf 参数（地址逃逸）、lambda（捕获语义特殊）、mangledName 含 `#` 的模块/重载函数（跨文件符号与重载决议复杂）
  - **解决**: isInlineable 追加排除：含 `AddrOf 参数`、`?lambda` 前缀、mangledName 含 `#`；此前已排除递归/入口/OOP（`$`）/Alloca/参数 Store
  - **预防**: 函数内联的排除清单必须覆盖"参数地址逃逸（AddrOf）、值传递副本写（Store 参数）、特殊调用语义（lambda 捕获/模块函数/重载/OOP 方法）"；新 Pass 优化对象必须白名单化（只内联"纯值传递 + 纯运算 + 单块"的平凡函数）
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.8）

- [2026-08-14 15:20] **问题类型**: 集成问题（权重 12.0）
  - **描述**: 阶段B 单测初版全红：SSA/LICM/内联/TCO 测试构造 IR 时，把 Call 函数名放 `type` 参数、Store 槽名放 `type` 参数——但 IR 契约是 Call 符号名存 `inst.extra`、Store 槽名存 `inst.extra`，导致 isInlineable/TCO 匹配失败（`last.extra != selfSym`）；LICM 测试误用"依赖循环内 Load 的 Mul"期望外提（与安全设计矛盾）
  - **原因**: 测试构造未遵循 IRGenerator 实际契约（Call/Store 的 extra 字段语义），凭直觉构造 IR 与真实 IR 不匹配
  - **解决**: 统一按契约构造（Call.extra=函数名、Store.extra=槽名），LICM 测试改用"循环外寄存器"作不变源
  - **预防**: 构造测试 IR 前先读 ir.cpp 的 emit 契约（Call/Store/Alloca 的 extra 字段语义），勿凭直觉；优化 Pass 单测的"期望外提/内联/优化"用例必须与 Pass 的保守设计一致
  - **权重**: 12.0（集成问题7 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-14 01:08] **问题类型**: 逻辑错误（权重 18.2）
  - **描述**: 阶段3 串联集成子任务首跑 6 个 IR OOP 发射测试失败：① 类方法重复提升（visitClassDecl 遍历全部 semantic classes() + visitProgram 对每类再调 → 动物.方法 出现 2 次）；② 重写方法虚调用误判（`狗.介绍` 中 `自身.叫声()` 走直接 Call `狗$叫声` 而非 VirtualCall——重写方法 isVirtual=false 但 vtableIndex>=0 覆盖父类槽位）；③ 析构函数与构造函数同名冲突（parser 对 `函数 ~资源` 记录 name=资源 无~ + kind=Destructor，语义层构造判定 `name==类名 && name[0]!='~'` 把析构误判为构造 → "重复定义方法"）；④ 语义层 visitCallExpr 成员调用对 `自身`/`父类`（类型 类名*）不剥指针（`自身.叫声()` 报"类 '动物*' 没有成员"）；⑤ 静态方法体内直接访问静态字段（`返回 总数`）IR 未处理（仅处理 类名.静态字段）；⑥ 实例方法调用 `->` 未剥指针（`动物* 实例; 实例->叫声()` 报"类型 '动物*' 不是类类型"）
  - **原因**: ① visitClassDecl 设计为遍历语义全部类（为覆盖泛型实例化类），但 visitProgram 又对 AST 类逐个调用 → 双重遍历；② 虚调用判定用 `m->isVirtual` 漏掉重写方法（语义层重写方法 isVirtual=false、vtableIndex>=0）；③ parser 析构 name 不带 ~，语义层构造判定 name[0]!='~' 失效；④/⑥ 语义层与 IR 层的 自身/父类/-> 指针剥除逻辑未对齐（visitMemberExpr 已剥但 visitCallExpr 未剥）；⑤ 静态字段直接访问路径缺失
  - **解决**: ① visitClassDecl 只处理 node 对应类（findClass），visitProgram 对 AST 类 + 泛型内嵌类 + 单态化实例化类（去重）分三路提升；② 虚调用判定改 `semantic_->classVtableIndex(类名, 方法名) >= 0`（重写方法同样虚分派）；③ 语义层析构 name 规范为 `~类名`（mi.name 加 ~ 前缀），构造判定排除 Destructor kind；④ 语义层 visitCallExpr 成员调用对 自身/父类 剥指针取类类型；⑤ IR handleClassFieldRead/Assign 补静态字段直接访问（沿继承链查 isStatic）；⑥ IR 实例方法调用 canonObjForMethod 对 -> 剥指针
  - **预防**: 跨模块（语义层/IR/codegen）的 OOP 判定逻辑必须统一——重写方法虚分派判据用 vtableIndex（不是 isVirtual）、自身/父类/-> 一律剥指针取类类型、析构 name 规范为 ~类名；类方法提升路径须去重（AST 类 + 泛型实例化类分路，避免双重遍历）；新增能力先写 E2E 冒烟（cn ir 命令）验证语义层通过再断言 IR 指令
  - **权重**: 18.2（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.8）

- [2026-08-14 01:07] **问题类型**: 逻辑错误（权重 12.5）
  - **描述**: codegen OOP 子任务单测首跑失败 5 类：① 虚表/静态字段符号断言用 `?vtable_动物`（实际 `vtableSymbol` 走 nameMangle 生成 `?vtable_?XX..@@Y`，`?动物` 中文字节被 UTF-8 十六进制修饰）；② 结果<T,E> 测试源码 `正常(a/b)` 推导 `结果<整32,整32>` 与返回类型 `结果<整32,字符串>` 不匹配（语义层报类型错误）；③ DeleteObject 无 semantic 时跳过析构调用（测试手工 IR 无 semantic 匹配不到 `call ?`）；④ 运行时错误码 4~8 死亡测试 `EXPECT_DEATH` 匹配中文消息失败（`std::printf` + `exit` 经 gtest 死亡测试管道无法捕获中文 UTF-8 输出）；⑤ `ObjectNewHugeSize` 触发真实分配失败 → `__cn_runtime_error(4)` → `exit(1)` 终止整个测试进程（而非仅该测试）
  - **原因**: ① 符号断言未理解 nameMangle 的 UTF-8 十六进制修饰规则（`?vtable_?` 前缀 + 类名修饰）；② 结果类型参数必须与返回类型完全一致（`错误(-1)` 推断错误值类型=整32，返回类型错误值也应整32）；③ emitDeleteObject 的析构调用被 `semantic_ != nullptr` 条件门控（测试手工 IR 无 semantic）；④ Windows gtest 死亡测试子进程管道对中文 UTF-8 输出捕获不可靠（正则匹配失败）；⑤ 测试设计错误——`__cn_object_new` 对超大尺寸 malloc 失败按设计调用错误码4终止，不该在单测进程内触发
  - **解决**: ① 断言改 `?vtable_?`/`?static_?` 前缀；② 测试源码统一错误类型为整32；③ emitDeleteObject 析构符号生成不依赖 semantic（classMethodSymbol 纯字符串拼装，无条件输出）；④ 新增 `__cn_error_message(code)` 导出（错误码→消息文本，不终止进程），测试直接 `EXPECT_STREQ` 验证消息表；⑤ 删除 ObjectNewHugeSize 真实分配测试，改签名可链接验证
  - **预防**: 符号断言一律按 nameMangle 修饰规则匹配前缀（`?vtable_?`/`?static_?`）而非原始中文；错误码消息验证用导出的 `__cn_error_message`（避免死亡测试管道中文捕获问题）；凡"分配失败会 exit"的辅助函数不得在单测进程内触发真实失败路径；测试源码的结果/可选类型参数必须与函数返回类型完全一致
  - **权重**: 12.5（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.2 × 已解决1.0）

- [2026-08-14 01:07] **问题类型**: 设计缺陷（权重 10.4）
  - **描述**: 阶段3 codegen 子任务发现 IR 层 OOP 指令发射（visitClassDecl/visitSelfExpr/visitSuperExpr 与 NewObject/VirtualCall 指令生成）尚未实现——IRGenerator 仅定义 Opcode 枚举（NewObject/DeleteObject/VirtualCall/VtableAddr），`visitProgram` 只遍历 FunctionDecl，类方法体未提升为 IRFunction。codegen 侧只能按约定契约展开指令，无法全链路验证类实例化/虚调用运行行为
  - **原因**: 前序语义+IR 子任务只保证"IR 层可生成模块不崩溃"（语义层通过后 IR 不崩），未实现 OOP 指令发射与类方法体生成；codegen 子任务边界是"把语义层/IR 层的类产物落地"，IR 层 OOP 发射属后续 IR 子任务/模块集成
  - **解决**: codegen 按契约实现指令展开（NewObject.extra="类名|大小字节"、VirtualCall.extra="类名.虚方法名" operand[0]=this、VtableAddr.extra=类名）；测试用"手工 IR 构造"验证展开正确性（test_x64_vtable.cpp），"全链路语义+IR+codegen"验证虚表/静态字段/结构体降级文本生成（test_x64_oop.cpp）；plans 文档明确遗留风险与后续 IR 子任务注意事项
  - **预防**: 跨子任务接口（IR 指令 operand/extra 格式、符号命名）必须在前序子任务文档中明确契约；codegen 子任务用"手工 IR 指令"测试绕过 IR 发射缺口，用"全链路"测试验证已实现部分；driver 未接入 semantic 指针（默认构造无 OOP 查询）需模块系统子任务补齐
  - **权重**: 10.4（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.2 × 已解决0.9）

- [2026-08-13 22:55] **问题类型**: 逻辑错误（权重 9.4）
  - **描述**: E2E 用例 `19_语句终止` 编写"空循环体合法"演示时，用 `当 (次数 >= 3) { }` 作恒真空循环体——条件恒真且体内无任何语句/跳出手段，运行期死循环挂起进程，E2E 脚本超时。已改为 `当 (次数 > 99) { }`（恒假条件，循环体一次都不执行，退出后继续后续断言）
  - **原因**: 编写"空循环体合法"演示时只关注"循环体为空"，未意识到**空循环体必须配合恒假条件**（或体内有 `跳出`）才可执行终止；恒真空循环在 C/CN 语言中都是死循环（合法但永不终止）
  - **解决**: 空循环体演示统一用恒假条件（`当 (次数 > 99)`），循环不执行即通过；避免任何"恒真 + 空体"组合
  - **预防**: 编写 E2E/演示代码时，凡涉及空循环体/空块，先确认循环条件在有限步内为假（或体内有终止路径），严禁恒真条件空体；E2E 用例加入后必须真实运行验证进程能正常退出
  - **权重**: 9.4（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0 × 影响度0.6）

- [2026-08-13 21:55] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: 缺陷修复针对性复核（模拟生产环境实测）发现 2 个新缺陷：① **lambda `[=]` 值捕获结构体字段读垃圾值**——`点 p = 点{x=1,y=2}; 自动 fp=[=](){返回 p.x}; p.x=100; fp()` 输出 `553448424`（应为 1）；② **嵌套 lambda 汇编失败**——外层 `[=]` lambda 体内定义内层 `[=]` lambda 引用外层变量，生成 `[rbp0]` 非法汇编（缺负号），ml64 退出码 1
  - **原因**: ① `[=]` 值捕获结构体时 `genVarDecl` 的 captureArgs 只存 `AddrOf(结构体)` 指针（指向原变量，定义后修改破坏快照），且闭包函数未标记 `structParamIndexes`——`emitParamSetup` 不会从指针 rep movsb 拷贝结构体数据到参数槽，闭包体把参数槽（存的是指针值本身）当结构体数据，字段访问读到地址值字节=垃圾；② 语义层 `collectLambdaCaptures` 的表达式展开 switch **漏 `LambdaExpr` case**——内层 lambda 引用的外层变量未被合并到本层捕获集，内层 lambda 体生成时该变量穿透本层匿名函数边界，直接引用更外层函数栈槽（跨函数槽越界 → `[rbp0]`）
  - **解决**: ① captureArgs 固化时对结构体值捕获走"定义处深拷贝快照"（Alloca 临时缓冲区 + CopyStruct，存缓冲地址），闭包参数循环里对非 byRef 结构体捕获插入 `func.structParamIndexes`；② `collectLambdaCaptures` 补 `LambdaExpr` case——递归收集内层捕获集（内层参数并入 paramNames 去重）再合并到本层 `explicitCaptures`
  - **预防**: lambda 值捕获结构体/数组等值类型必须"定义处深拷贝"，不能只存地址指针（否则破坏快照语义且闭包参数槽类型错配）；语义层捕获收集的表达式展开 switch 必须覆盖**所有表达式节点类型**（尤其 LambdaExpr——嵌套闭包场景），漏一个 case 就会导致捕获集缺失、跨函数槽引用；codegen 结构体参数拷贝依赖 `structParamIndexes` 标记，凡"以指针传入需拷贝到参数槽"的参数（含 lambda 结构体值捕获参数）都要登记
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.8）

- [2026-08-13 21:25] **问题类型**: 设计缺陷（权重 22.5）
  - **描述**: 缺陷修复审查发现 2 个规格符合性缺陷：① **lambda `[=]` 值捕获与 `[&]` 引用捕获未区分**——闭包在"调用点"读取捕获变量（ir.cpp visitCallExpr genExpr(IdentifierExpr(cap))），`[=]` 值捕获在捕获变量后续被修改时读到最新值（实测 `lambda值快照: 1000` 应为快照 101）；② **无符号大值打印负数**——`正64` 值超 2^63 经 `__cn_print_int`（%lld 有符号）打印负数（`18446744073709551615` 打印成 `-1`），字符串拼接走 `__cn_str_from_int` 同错；顺带发现 **i128 捕获垃圾值**（`1234567890123456789LL` 输出 `22773757910726981403490738691264577813`——`maxRegIdIn` 未计入 i128/u128 双寄存器 `id+1`，槽区未预留落变量槽区与捕获参数高64位槽重叠）
  - **原因**: ① 捕获实参求值时机错误——规格书04-一D"值捕获=捕获时复制"，但实现把求值放在调用点（等价 `[&]` 引用语义）；② 打印/拼接对无符号类型（正8~正64）未按无符号路径分派，统一 Cast i64 走 %lld；③ codegen 栈帧寄存器槽区按 `maxRegId+1` 预留，i128 双寄存器结果（占用 id 与 id+1）只按 id 计，尾寄存器场景 id+1 槽与变量槽区重叠
  - **解决**: ① 捕获实参求值从调用点提前到 **lambda 定义处**（genVarDecl 登记闭包时固化 `captureArgs`）：`[=]` 存值快照（genExpr）、`[&]` 存地址指针（AddrOf）；`[&]` 捕获参数类型 ptr，体内读取 LoadPtr 解引用、赋值 StorePtr 写回、取地址/左值/数组基址 Load 参数槽；② 新增 `__cn_print_uint`/`__cn_str_from_uint`（%llu），IR 打印/拼接按无符号类型分派；emitCast 加 i64/u64 同类型 64 位直通、emitCall u32 参数零扩展（防 movsxd 符号扩展）；③ `maxRegIdIn` 对 i128/u128 值按 `id+1` 计
  - **预防**: lambda 捕获实参必须在**定义处**求值固化（值快照/引用指针），调用点只做展开；无符号整数（正N）打印/拼接必须走无符号路径（%llu），禁止统一 Cast i64 走 %lld；codegen 寄存器槽区预留必须计入 i128/u128 双寄存器（id+1），凡 `result.type` 为 i128/u128 的指令结果都要占 2 槽；测试字符串捕获注意指针共享（复制指针非深拷贝）+ 常量池字面量不可释放
  - **权重**: 22.5（设计缺陷10 × 详细分析2.0 × 解决方案1.5 × 预防措施1.5 × 已解决1.0）

- [2026-08-13 18:30] **问题类型**: 逻辑错误（权重 14.4）
  - **描述**: Task 2.9 实现 `格式化("%d 浮点%f", 42, 3.14)` 时 `%f` 输出 0.000000：emitCall 手写汇编传浮点参数，被调方 `__cn_format`（MSVC 编译的 C 变参）va_arg(args, double) 读到未初始化槽。单测 `FormatFloat`（C++ 调用同一函数）通过，E2E（emitCall 调用）失败——两种调用路径差异。调试过程试错三种方案（浮点序号 xmm0 / 参数位 xmmN / shadow 写入）均失败，最终反汇编 MSVC 生成的调用与 `__cn_format` prologue 定位根因。
  - **原因**: MSVC x64 变参机制——① 浮点参数按**参数位**用 xmmN（N=位置，反汇编实证 `movsd xmm1` 传单浮点参数）；② 变参函数 prologue 只把 **rcx/rdx/r8/r9（GPR）保存到 shadow space**，va_list 从保存槽连续读参数；③ **浮点参数的位模式必须用 `movq rdx, xmm1` 复制到同参数位 GPR**，否则 va_arg(double) 从 GPR 保存槽读到未初始化垃圾（0.000000）。同时 IR 层"字符串+数值拼接"分支用 `left.type=="ptr"` 判定，误把 `指针 + 整数`（q+1 指针算术）当拼接展开为 `__cn_str_from_int`+concat → 指针值破坏（05_array_pointer 回归）。
  - **解决**: ① emitCall 浮点参数分支：`movsd xmmN(参数位), [槽]` + `movq parameterRegister(参数位), xmmN`（浮点位模式入 GPR，供变参 va_arg 读取）；② IR 拼接分支加 `isStringTypedExpr`（字符串字面量/字符串变量/连接结果/字符串数组元素）限定，普通指针走指针算术。
  - **预防**: Win x64 调用 C 变参函数（printf 家族）时，浮点参数必须同时设 xmmN 并 movq 到同参数位 GPR（位模式），否则 va_arg 读垃圾；调试 ABI 问题先反汇编 MSVC 自己生成的调用序列对比（dumpbin /disasm），勿反复试错；IR 层 `ptr` 类型无法区分字符串与普通指针，字符串拼接判定必须依赖源码类型（isStringTypedExpr），禁止仅凭 IR 类型。
  - **权重**: 14.4（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.8）

- [2026-08-13 16:56] **问题类型**: 设计缺陷（权重 10.4）
  - **描述**: 规划"语言表达力增强"时，未经用户确认便引入两个新保留关键字 `运算符`（运算符重载）和 `别名`（类型别名），并错误定义 `打印`/`打印行` 的换行语义（写成 `打印` 不换行、`打印行` 换行）。用户逐条批阅后否决：① 不引入 `运算符` 保留关键字；② 不引入 `别名` 关键字与功能；③ `打印` 应自动换行、`打印行` 应不换行。
  - **原因**: ① 盲目对标 C++ 的 `operator` 保留字策略，未考虑 CN 语言中文命名的"上下文关键字"更优方案（`函数 运算符+` 可凭上下文识别，无需占用标识符命名空间）；② 类型别名功能被误认为必需，实际现有 `整数`/`小数` 内置别名已覆盖主要需求，用户明确"不引入别名功能"；③ 打印换行语义想当然套用 C 的 printf/puts 惯例，未遵循 CN 语言"打印=println、打印行=print"的直觉命名。
  - **解决**: ① 运算符重载改为上下文关键字识别，`运算符` 不进保留字表（关键字 59→58）；② 删除类型别名小节，关键字表移除 `别名`；③ 修正 10.6 API 表：`打印` 自动换行、`打印行` 不换行，并在变更记录中标注三点修正。
  - **预防**: 规划语言特性时，凡涉及"新增保留关键字"或"改变既有内置函数语义"的决策，必须先向用户确认，不得对标其他语言想当然；中文编程语言应优先"上下文关键字"（节省标识符命名空间）而非照搬 C/C++ 保留字；打印/输入等 IO 函数的换行语义以"直觉命名"为准（打印=完整输出一行）。
  - **权重**: 10.4（设计缺陷10 × 详细分析1.5 × 解决方案1.3 × 预防措施1.2 × 已修正0.9 × 待实施0.7）

- [2026-08-13 12:50] **问题类型**: 集成问题（权重 27.3）
  - **描述**: 缺陷完善 Debug 全面审查（模拟生产环境，target/debug_audit/ 9 个边界用例）发现 5 个 BUG：① `i128→浮64` Cast 无分支（`整128 * 1.0` 输出 0.000000——emitCast 整→浮只处理 i64/u64，i128 落默认 32 位 mov 读垃圾低32位）；② i128 混合参数 + i128 返回 ABI 错位崩溃（0xC0000005）——`emitParamSetup` 用 `if (i < 4)` 判定参数位未加 paramOffset（i128 返回占 rcx 后第4参数实际位4=栈），且 `parameterRegister(4)` 返回 `[rsp+40]`（rsp 锚定，被调方 sub frameSize 后读自己栈帧垃圾，应为 rbp 锚定 `[rbp+48]`）；③ 链式结构体赋值 `链3 = 链2 = 链1` 只拷贝 8 字节（外层赋值右值为 AssignmentExpr 时 valueSrcType 推导失败 → 不走 CopyStruct 落简单 Store）；④ `i1→i64` Cast 无分支（字符串后缀/包含 打印 4393751543809 垃圾——i1 落默认 32 位 mov 读槽高32位垃圾）；⑤ i128 数组元素 stride 8（`整128[3] 大数; 大数[1]` 读到 大数[0] 高64槽，求和 92233720368547758086000000000000000000 错误——visitIndexExpr/ptrElemStride/初始化列表 elemStride 漏 i128=16）
  - **原因**: i128 双槽约定（%vN=高/%vN+1=低）在 Cast 整→浮、i1→整 路径未全覆盖；Win x64 参数位判定（i<4）与 paramOffset 分离导致 i128 第4参数错判；parameterRegister 栈偏移 rsp 锚定（被调方 rsp 已 sub frameSize）应 rbp 锚定；链式赋值的结构体源推导未处理 AssignmentExpr 形态；i128 数组/指针 stride 只处理结构体类型漏 i128（16字节）
  - **解决**: ① emitCast 加 i128/u128→f64 分支（调用 __cn_i128_to_f64/__cn_u128_to_f64 双槽地址 + xmm0）；② emitParamSetup 参数位判定改 `i + paramOffset < 4`（浮点独立 i<4），parameterRegister index≥4 改 `[rbp+48+(index-4)*8]`；③ IR 结构体赋值检测补 AssignmentExpr 右值（内层返回值即源地址）；④ emitCast 加 i1→i64/u64/i32 分支（mov eax 零扩展）；⑤ visitIndexExpr/ptrElemStride/数组初始化 elemStride 补 `types::isI128 → 16`
  - **预防**: emitCast 必须全覆盖 i128↔浮/i1↔整 转换矩阵（缺分支落默认 32 位 mov 读垃圾）；被调方参数栈偏移恒用 rbp 锚定（rbp+48 为第5参数位）；参数位判定必须含 paramOffset（整型/i128），浮点独立编址（xmmN）；链式赋值右值形态（AssignmentExpr）必须纳入结构体源推导；i128 数组/指针 stride 恒 16（与 typeSize 一致）
  - **权重**: 27.3（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 10:30] **问题类型**: 集成问题（权重 27.3）
  - **描述**: 完善功能集成验证（14_integration2 员工档案系统）发现 6 个跨组件 BUG：① i128 返回 + 结构体按值参数 ABI 错位——被调方 `paramOffset = structReturn?1:0` 未涵盖 i128 返回（结构体参数应从 rdx 读但被调方从 rcx 读）+ i128 返回 prologue 未保存 rcx→r12（函数体内调用破坏 rcx，epilogue 用已破坏的 rcx → 0xC0000005）；② LoadPtr/StorePtr 漏 i128 双槽分支（`档案.年薪` 读取只 mov 8 字节，高64 残留垃圾）；③ emitCast 无"普通整数→i128"分支（i32→i128 落默认 mov 32位，高64 垃圾）；④ i128 参数 emitParamSetup 只 `mov slot, reg` 存 8 字节（调用方传 16 字节双槽地址指针，被调方只存低 8 字节）；⑤ 指针下标元素成员 `名单[j].年薪` objSrcType 推导只认数组变量（`types::isArray`），指针参数（`员工档案* 名单`）推导失败 → 字段访问降级为 0 → i128 比较恒假排序失效；⑥ 结构体下标赋值 `名单[j] = 名单[j+1]` 走 StorePtr 只存 8 字节（应 CopyStruct 48B）——结构体指针数组元素交换破坏数据
  - **原因**: 完善 A（i128/结构体值）与完善 C（优化器）各自独立测试通过，但"i128 字段 + 结构体按值 + 指针数组 + 整体赋值 + 比较排序"组合场景未覆盖——Win x64 隐藏返回指针（rcx）对 i128/结构体返回的 paramOffset 处理不一致；codegen 各访存/传参路径对 i128 双槽约定（%vN=高64、%vN+1=低64）未全覆盖；IR 层 objSrcType 推导与结构体赋值检测对"指针下标"形态（IndexExpr of ptr）遗漏
  - **解决**: ① paramOffset 涵盖 i128/u128 返回 + prologue `mov r12, rcx` + epilogue 用 r12；② LoadPtr/StorePtr 加 i128/u128 双槽分支；③ emitCast 加整数→i128 分支（符号扩展低64 + sar 63 高64）；④ emitParamSetup i128 参数（寄存器/栈）从指针 rep movsb 16 字节；⑤ visitMemberExpr objSrcType 推导 IndexExpr 分支补 `types::isPointer`（pointeeOf）；⑥ 非标识符左值赋值加结构体检测（CopyStruct 按 typeSizeOf）
  - **预防**: i128 双槽约定（%vN=高/%vN+1=低）必须覆盖全部访存/传参/转换路径（LoadPtr/StorePtr/Cast→i128/i128参数/返回）；隐藏返回指针（rcx→r12）对 i128/结构体返回统一处理；IR 层自定义类型推导必须覆盖 IdentifierExpr/MemberExpr/IndexExpr（含指针下标 pointeeOf）；结构体赋值（变量/下标/成员）统一 CopyStruct；综合用例必须组合 i128 字段 + 结构体值 + 指针数组 + 排序
  - **权重**: 27.3（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 09:00] **问题类型**: 逻辑错误（权重 22.75）
  - **描述**: 结构体按值返回（Win x64 隐藏返回指针）实现中发现 4 个 BUG：① epilogue structReturn 分支直接 return 漏掉 `mov rsp,rbp/pop rbp/ret` → 执行流落入下一函数 PROC 无限递归 → 0xC00000FD 栈溢出；② `mov rcx, 64` 硬编码拷贝 64 字节，16 字节班级结构体越界写 48 字节破坏相邻栈变量（翻倍 160/0/0）；③ `出.分数[1] = ...`（IndexExpr 对象为结构体数组字段 MemberExpr）targetType 推导遗漏 → StorePtr 用 i64 8 字节写入覆盖相邻数组元素；④ i128 字面量越界未报错（2^127 被接受）
  - **原因**: ① structReturn 分支是 emitEpilogue 的提前 return，未走公共尾部；② 结构体返回大小未记录到 IRFunction（codegen 无从得知精确字节）；③ visitAssignmentExpr IndexExpr 分支只处理 IdentifierExpr 对象，未处理 MemberExpr（出.分数）；④ 语义层 literalTypeOf 对无后缀超 int64 提升整128 但无越界检查
  - **解决**: ① structReturn 分支补 `mov rsp,rbp/pop rbp/ret`；② IRFunction 新增 structReturnSize（语义层 typeSizeOf），epilogue 按精确字节数 `mov rcx, N` 拷贝；③ IndexExpr 目标类型推导补 MemberExpr 分支（memberObjStructType 找字段数组元素类型）；④ 语义层 visitIntegerLiteral 加 整128（2^127-1）/正128（2^128-1）上限检查（含无后缀超 int64 提升场景）
  - **预防**: 提前 return 的分支必须补函数尾部（ret）；结构体返回/拷贝大小必须精确记录（IRFunction 字段），禁止 64 字节硬编码；IndexExpr 目标类型推导必须覆盖 MemberExpr 数组字段对象；i128 字面量越界检查放语义层（literalTypeOf 之后，含无后缀自动提升场景）
  - **权重**: 22.75（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 03:34] **问题类型**: 逻辑错误（权重 22.75）
  - **描述**: 阶段2全面审查（模拟生产环境）发现 10 个 BUG：① 除零未插桩（错误码1）→ Windows 0xC0000094 崩溃而非 CN 错误码；② idiv 立即数 A2001；③ 无符号除法用 idiv+有符号扩展（正32 4000000000/2 错算 -295M）；④ 无符号比较用 setg/setl（正32 4294967295>1 判假）；⑤ u32 LoadPtr movsxd 符号扩展；⑥ 浮点参数经 rcx 读（caller 用 xmm0-3 → callee 读到垃圾）；⑦ 浮点返回只 mov rax 未设 xmm0；⑧ const_fold Call 尾部实参传播浮点常量 → `movsd xmm2, qword ptr 4.0` A2050；⑨ const_fold 无符号除/比较用有符号语义（-O1 与运行期不一致）；⑩ 结构体数组字段（方形.顶点[0].x / 指针->顶点[1].x）把数组字段当值 LoadPtr（垃圾指针→空指针错误/访问冲突），且数组字段越界不插桩
  - **原因**: ① 除零检查设计缺失（codegen 只对 LoadPtr/FieldAddr 插桩错误码）；② div/idiv 不接受立即数；③④⑤ 无符号类型（正N/uN）在除法/比较/加载路径未按无符号语义分派；⑥⑦ Win x64 浮点调用约定（xmm0-3 参数/xmm0 返回）callee 侧未实现；⑧ 尾部操作数传播无浮点过滤（Call 第3+实参落尾部）；⑨ 折叠期未区分符号；⑩ IR 层数组字段退化/步进/越界推导缺失（memberObjStructType 未处理 arrow 剥指针）
  - **解决**: ① emitDivMod 除零检查（常量非零跳过、寄存器低32位检查后 div ecx）；② 常量除数先 mov ecx；③ 无符号 div + xor edx,edx；④ 无符号 seta/setb 系列；⑤ u32 LoadPtr mov eax 零扩展；⑥ emitParamSetup 浮点参数从 xmmN 读；⑦ epilogue currentReturnType_ 浮点 movsd xmm0；⑧ 尾部传播排除浮点/i128；⑨ foldIntBinary/compareInt 无符号分支；⑩ visitMemberExpr 数组字段退化返回字段地址 + lvalueAddress/visitIndexExpr 数组字段步进按元素大小 + 越界检查插桩 + memberObjStructType 递归辅助（arrow 剥指针）
  - **预防**: 除零必须插桩错误码1（常量零编译期报错、运行期寄存器检查）；无符号类型（正N）在除/比较/加载路径必须按无符号指令分派（div/seta/movzx）；Win x64 浮点参数走 xmm0-3、返回走 xmm0（callee 侧必须实现）；const_fold 尾部操作数传播必须排除浮点/i128；折叠语义必须与运行期指令一致（有符号 idiv/setg vs 无符号 div/seta）；结构体数组字段必须退化为地址（非 LoadPtr 值）+ 按元素大小步进 + 越界检查；自定义类型推导必须处理 arrow 指针剥除（memberObjStructType 模式）
  - **权重**: 22.75（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 02:56] **问题类型**: 集成问题（权重 22.75）
  - **描述**: 阶段2全链路集成验证（09_integration 学生成绩系统）发现 9 个跨组件集成 bug：① 结构体数组初始化列表 `学生[5] 名单 = { 学生{...} }` 只写占位0（visitStructInitExpr 无法作值）；② 结构体数组栈槽只登记 len 个（元素24字节=3槽，5元素需15槽）；③ 结构体指针算术/`p[i]`/`p++` 固定8字节步进（应24）；④ 结构体/枚举作函数参数（学生* 名单）解析失败；⑤ 函数指针参数含结构体指针（整32(*比较)(学生*, 学生*)）解析失败；⑥ 数组名退化缺失（名单 + 人数 报"学生[5]与整32算术"）；⑦ 长字符串 db 单行超长 A2042 且行尾逗号 A2008；⑧ 用户函数返回 f64 结果类型误标 i32（eax 读 xmm0 除零崩溃）；⑨ 空指针常量 0 存32位（mov eax,0）64位比较读到高位残留判空失败
  - **原因**: 各 Task 独立测试未覆盖"组合场景"——结构体数组+指针遍历+函数指针回调+字符串字段+枚举+递归返回浮点同时出现时，IR/codegen 对复合类型的假设（8字节步进/单值传递/单行db/32位常量）逐一暴露
  - **解决**: ① genVarDecl 数组初始化列表遇 StructInitExpr 改 emitStructInitTo 逐字段写入、步进按元素大小；② registerVarSlots 数组槽数 = 长度×元素槽数；③ 新增 ptrElemStride（结构体指针按 typeSizeOf）+ visitBinaryExpr/visitUnaryExpr/visitIndexExpr/lvalueAddress 统一使用；④⑤ parseParamDecl/parseFuncPtrType 参数类型改用 parseTypeNameEx；⑥ 语义层 visitBinaryExpr 数组名退化（学生[5]→学生*）；⑦ 长字符串 db 按24字节/行拆分且行尾不加逗号；⑧ IR 调用结果类型查 funcReturnTypeOf；⑨ ConstInt ptr 类型强制 mov rax 64位
  - **预防**: 综合用例必须覆盖"组合场景"（结构体数组+指针遍历+函数指针+字符串字段+递归浮点返回）；结构体相关 IR/codegen 一律按 typeSizeOf 计算步进/槽数；函数调用结果类型须查语义层真实返回类型；长字符串拆行 db；ptr 常量恒64位
  - **权重**: 22.75（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 02:30] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: Task 2.6 优化器 -O1 下 05/07 E2E 崩溃（0xC0000005）——① 常量折叠把指令替换为 ConstInt/ConstBool 后结果寄存器仍保留，后续不可折叠指令（ptr 加法）引用该寄存器 → codegen 读未初始化的栈槽；② Branch 条件寄存器挂在块内最后一条指令 operands 尾部，被折叠为 ConstBool 后尾部仍是 %vN → emitTerminator 读槽垃圾
  - **原因**: 折叠只改指令本身不替换引用点；非 SSA IR 中"寄存器=常量"的传播必须同步替换所有使用处
  - **解决**: run() 第一步做常量传播（命中常量表的寄存器操作数原地替换为常量值，含尾部 Branch 条件）；只向纯运算指令传播（Load/Store/Call 等保持寄存器引用）
  - **预防**: 非 SSA IR 做折叠必须带"寄存器→常量"跟踪 + 引用点同步替换；Branch 条件尾部操作数是 codegen 的特殊契约（last.operands.back()），折叠后必须同步替换
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 02:30] **问题类型**: 逻辑错误（权重 16.8）
  - **描述**: Task 2.6 DCE 纯块内引用分析误删跨块引用——越界检查的边界常量在 bb0（`常量整数 %v25 (i64) [2]`），被 bb2 的 `转换 %v34 %v25` 引用；块内 DCE 看不到跨块引用 → 删除 → 运行崩溃
  - **原因**: IR 寄存器可被其他基本块引用（CFG 数据流），块内引用集合不完整
  - **解决**: DCE 改为函数级引用收集（先遍历函数所有块的指令操作数+终止信息，再逐块反向扫描删除）
  - **预防**: DCE/活性分析必须函数级（或跨块）引用集合，禁止纯块内分析；任何删除前先确认无跨块引用
  - **权重**: 16.8（逻辑错误8 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 02:30] **问题类型**: 集成问题（权重 14）
  - **描述**: Task 2.6 常量传播到操作数位置破坏 codegen 假设：① 浮点常量 `movsd xmm0, qword ptr 3.5`（A2050，MASM 无浮点立即数）；② i128 常量 `cmp rax, 100000000000000001`（A2084 超 32 位）；③ 小位宽 Store `movsx eax, byte ptr 100`（A2070 大小前缀+立即数非法）；④ 0b/0x 前缀 `mov eax, 0b1100`（A2048）
  - **原因**: codegen operandText 对 isConstant 直接输出文本，未区分操作数上下文（浮点需 @fpN 池标签、i128 拆双槽、小位宽 Store 需 movsx 语义、MASM 无 0b/0x 前缀）
  - **解决**: 常量传播加白名单——浮点/i128/u128 不传播；只向纯运算指令传播（Load/Store/StorePtr/Call/LoadPtr/FieldAddr/AddrOf 不传播）；传播时 normalizeIntText 规范化 0b/0x/0o 为十进制
  - **预防**: 优化器传播常量前必须核对 codegen 各指令对"常量操作数"的支持（浮点池/大值/大小前缀/进制前缀）；保守策略：只向纯运算指令传播
  - **权重**: 14（集成问题7 × 详细分析2.0 × 解决方案1.0 × 预防措施1.0 × 已解决1.0）

- [2026-08-13 01:30] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: Task 2.7 `r.左上.x`（嵌套成员）与 `点数组[1].x`（数组元素成员）字段访问输出 0——IR 层 `visitMemberExpr`/`lvalueAddress` 对 object 是 MemberExpr/IndexExpr 时查不到结构体类型（lookupSrcType 只认 IdentifierExpr），导致 decl==nullptr fallback 返回 ConstInt 0（地址未加偏移）
  - **原因**: IR 层无类型推导，嵌套/数组元素成员的类型需从 AST 递归推导（外层结构体字段类型/数组元素类型），遗漏即静默错误
  - **解决**: visitMemberExpr 与 lvalueAddress 的 MemberExpr 分支补充 object 为 MemberExpr（查外层字段类型）与 IndexExpr（查数组元素类型）的类型推导；字段地址统一走 lvalueAddress 递归
  - **预防**: 凡 IR 层访问自定义类型（结构体）信息，必须处理 object 的三种形态（IdentifierExpr/MemberExpr/IndexExpr）；输出异常先查 IR（cn.exe ir 命令）定位哪条指令错误
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 01:30] **问题类型**: 逻辑错误（权重 16.8）
  - **描述**: Task 2.7 自定义类型名变量声明探测歧义——`点[3] 点数组` 是数组声明（`]` 后跟变量名），但 `点数组[0] = v` 下标赋值也被误判为数组类型声明（peek(1)==[ && peek(2)==数字 命中），报"预期变量名，实际为 '='"
  - **原因**: parseStmt 探测自定义类型声明只看前3个 token，未区分"声明"（`]` 后跟变量名）与"赋值"（`]` 后跟 `=`）
  - **解决**: 数组类型声明探测加 peek(3)==RightBracket && peek(4)==Identifier 条件
  - **预防**: 语法探测必须看完整模式（含结尾界定符）；声明 vs 表达式歧义用"后随 token 类别"区分
  - **权重**: 16.8（逻辑错误8 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 01:30] **问题类型**: 逻辑错误（权重 12.5）
  - **描述**: 初始值 `{` 双重语义——`整32[5] 数据 = {1,2,3}` 是数组 InitListExpr，`点 p = 点{ x = 1 }` 是 StructInitExpr；parseTypePrefixVarDecl 一律走 parseInitList 导致结构体初始化被当数组列表（嵌套/数组赋值输出 0）
  - **原因**: `=` 后 `{` 的处理未区分目标类型（数组 vs 结构体）
  - **解决**: `if (check(LeftBrace) && types::isArray(decl->typeName)) parseInitList() else parseExpr()`（parseExpr 的 parsePrimary 识别 类型名{...} 为 StructInitExpr）
  - **预防**: 语法歧义场景先查规格书示例与既有类型处理；`{` 既是数组初始化列表也是结构体命名式初始化
  - **权重**: 12.5（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

- [2026-08-13 01:30] **问题类型**: 逻辑错误（权重 12.5）
  - **描述**: 枚举↔整数隐式转换失败——`打印行整数(颜色.蓝)` 报"无法将 '颜色' 隐式转换为 '整64'"，因为静态 types::canConvert 不认识枚举类型（无枚举表访问）
  - **原因**: 语义层 canConvert 是 type_system 静态函数，无法感知自定义枚举类型；枚举本质为整32，需特判
  - **解决**: 语义层新增非静态 `canConvertType`（枚举↔整数双向、枚举间须同名、结构体须同名），替换 visitVarDecl/visitCallExpr/visitAssignmentExpr/visitReturnStmt/visitStructInitExpr 的 canConvert 调用点
  - **预防**: 凡涉及自定义类型（枚举/结构体）的隐式转换必须走语义层 canConvertType，禁止直接调 type_system 静态 canConvert
  - **权重**: 12.5（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

- [2026-08-13 00:47] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: IR `decodeString` 用 `raw.compare(0, 4, "原始多行")` 判断前缀失败——`compare` 的第2参是**字节数**，而"原始"是 UTF-8 多字节（6字节），4 只覆盖了前4字节，导致原始/多行前缀无法识别（测试失败：字符串常量保留前缀未剥离）
  - **原因**: 中文前缀为 UTF-8 多字节编码，compare/substr 的偏移与长度参数都是字节计数，不能按"字符数"写
  - **解决**: 改用 `raw.rfind("原始多行", 0) == 0` 前缀匹配 + `start = 12`（"原始多行"=12字节）/`start = 6`（"原始"/"多行"=6字节）字节偏移
  - **预防**: 处理中文前缀/子串时，偏移量必须用 `prefix.size()`（字节数），禁止按字符数硬编码；凡 UTF-8 字符串索引一律字节语义
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 17:59] **问题类型**: 集成问题（权重 27.3）
  - **描述**: 换电脑（Linux GCC7 → Windows MSVC）后 Task 1.7 构建失败：1) `ir.hpp` 的 `IRFunction`/`IRModule` 含 `vector<unique_ptr>`，MSVC traits 探测实例化隐式拷贝构造触发 C2280；2) `test_lexer.cpp` raw string 内 `\"` 序列 MSVC 报 C2017 非法转义；3) 测试变量名 `asm` 是 MSVC 关键字报 C2059；4) cmake 不在 PATH，需用 VS 自带完整路径；5) CMakeLists gtest 本地源码回退路径指向不存在的 `~/third-party`，实际缓存在 `target/build/_deps`
  - **原因**: 项目原在 Linux GCC7 开发，未覆盖 MSVC 编译差异（unique_ptr容器拷贝/raw string转义/MSVC关键字/工具链路径）
  - **解决**: 1) `ir.hpp` 显式移动语义（拷贝删除）；2) raw string 含 `\"` 改普通字符串；3) 变量避免用 `asm` 等 MSVC 关键字；4) 用 VS 自带 cmake 完整路径；5) gtest 回退路径增加 `target/build/_deps/googletest-src/googletest` 检查
  - **预防**: 新代码避免 `asm`/`cl` 作标识符；raw string 含引号转义用普通字符串；含 unique_ptr 的容器成员类显式移动语义；构建前先确认工具链与依赖缓存路径
  - **权重**: 27.3（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 16:44] **问题类型**: 工具执行错误（权重 15）
  - **描述**: `TEST(SemanticTest, 递归函数)` 中文测试名导致编译失败：`expected initializer before ':' token`（GCC 7 不支持中文标识符）
  - **原因**: GCC 7 的 gtest 宏 `GTEST_TEST_` 用测试名生成 C++ 类名/函数名，中文标识符不合法；注释中的中文虽可编译，但宏展开的标识符不行
  - **解决**: 全部测试名改为英文（如 `TEST(SemanticTest, RecursiveFunction)`），中文仅用于注释/字符串
  - **预防**: 新建测试文件时测试名一律英文；标识符（变量/函数/类/枚举）一律英文

- [2026-08-12 16:44] **问题类型**: 工具执行错误（权重 12）
  - **描述**: `from[0] == '正'` 触发 `-Werror=multichar` 和 `-Werror=type-limits` 编译失败
  - **原因**: '正' 是 UTF-8 多字节字符（3字节），不能与单字节 char 比较；GCC 将多字符常量视为警告并在 -Werror 下报错
  - **解决**: 改用 `from.compare(0, 3, "正") == 0` 字符串前缀比较
  - **预防**: 任何中文字符比较必须用 std::string 的 compare/substr，禁止 `str[i] == '中文字符'`

- [2026-08-12 16:44] **问题类型**: 集成问题（权重 10）
  - **描述**: IRGenerator 继承 AstVisitor 后未实现 `visitIfStmt/visitWhileStmt/visitForStmt`，链接报 `undefined reference to ... vtable`
  - **原因**: 三个虚函数在头文件声明但实现遗漏，vtable 引用缺失
  - **解决**: 补充实现，转发到 genIf/genWhile/genFor
  - **预防**: 继承 AstVisitor 后逐一核对所有纯虚函数是否实现；链接错误先看 undefined reference 到哪个类

- [2026-08-12 16:44] **问题类型**: 逻辑错误（权重 8）
  - **描述**: 测试中 `变量 结果 = 阶乘(5)` 解析失败："预期变量名，实际为 '结果'"
  - **原因**: `结果`（Kw_Result）是 CN 语言53个保留关键字之一（结果<T,E>类型），不能作标识符
  - **解决**: 测试源码改用 `答案` 等非关键字变量名
  - **预防**: 编写 CN 测试代码时避免使用53个关键字作变量/函数名（结果/变量/选择/情况/默认等）

## 高权重问题（Task 1.9 新增）

- [2026-08-12 18:22] **问题类型**: 集成问题（权重 24.6）
  - **描述**: Task 1.9 `cn build` 链接阶段 LNK2019 无法解析 CRT 符号：1) `/ENTRY:WinMain` 时 link 不自动注入 CRT 默认库，puts/printf/malloc 等全部未解析；2) 改显式 `/DEFAULTLIB:ucrt.lib` 后仍缺 `memcpy/memset`（它们在静态 `libucrt.lib`，导入库 `ucrt.lib` 无定义）；3) 即使链接成功，`/ENTRY:WinMain` 跳过 CRT 启动代码，stdout 未初始化导致 `printLine`（puts）输出为空
  - **原因**: 自定义 `/ENTRY` 绕过了 CRT 启动（mainCRTStartup/WinMainCRTStartup），需自行注入默认库；puts 依赖 CRT 初始化 stdout
  - **解决**: 链接命令改为 `link /ENTRY:WinMainCRTStartup /SUBSYSTEM:CONSOLE /DEFAULTLIB:libcmt.lib /DEFAULTLIB:libucrt.lib /DEFAULTLIB:kernel32.lib`（CRT 初始化 stdout 后调用用户 WinMain）
  - **预防**: 自定义入口时必须用 `WinMainCRTStartup`（而非 WinMain）；静态 CRT 配套 `libucrt.lib` 而非 `ucrt.lib`；显式 `/DEFAULTLIB` 注入 CRT/内核库
  - **权重**: 24.6（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 18:22] **问题类型**: 配置错误（权重 15.6）
  - **描述**: ml64 汇编 UTF-8 文件失败 A2044（invalid character in file）：1) 带 UTF-8 BOM 的 asm 文件从第1行起全部 A2044；2) `db "你好，世界"` 中文原始 UTF-8 字节触发 A2044；3) 中文块标签 `块0:` 也是 A2044
  - **原因**: 此 ml64 版本不识别 UTF-8 BOM，且把原始 UTF-8 中文字节当非法字符；仅 UTF-8 无 BOM 的纯 ASCII 代码 + 中文注释可接受
  - **解决**: 1) asm 写 UTF-8 无 BOM；2) 字符串常量改用十六进制字节 `db 0E4h,0BDh,0A0h,0`（代码生成器 `hexBytesString()`）；3) IR 块标签 块N→bbN（ml64 不识别中文标识符）
  - **预防**: 生成汇编一律 UTF-8 无 BOM；中文内容只出现在注释和 `db` 十六进制中；符号/标签必须 ASCII
  - **权重**: 15.6（配置错误6 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 18:22] **问题类型**: 集成问题（权重 13.7）
  - **描述**: 语义分析器报"未声明的函数 '打印行'"——`打印行` 是 CN 内置函数，但 SemanticAnalyzer 未注册其符号，visitCallExpr 找不到
  - **原因**: 阶段一语义分析器只注册源码中的函数声明，未内置 IO 函数符号表
  - **解决**: semantic.cpp 新增 `registerBuiltins()`，在 visitProgram 第零趟注册 `打印行(字符串)->空类型`/`打印行整数(整64)`/`打印行浮点(浮64)`；代码生成器 `symbolName()` 映射到运行时 `printLine` 系列
  - **预防**: 新增内置函数必须同时注册语义符号表 + 代码生成器符号映射 + 运行时 extern "C" 导出三处
  - **权重**: 13.7（集成问题7 × 详细分析2.0 × 解决方案1.5 × 已解决1.0）
  - **⚠️ 方案C（2026-08-14）✅ 已修复**：本条目中的 `打印行整数`/`打印行浮点` 遗留函数已按方案C彻底删除（"行"语义统一为不换行，见顶部 2026-08-14 21:10 条目）；注册与映射均已移除，运行时 `printLine` 系列符号防御保留

- [2026-08-12 18:22] **问题类型**: 工具执行错误（权重 3.9）
  - **描述**: MSVC 将 `getenv` 视为不安全函数，C4996 警告在 /WX 下报 C2220 错误
  - **原因**: MSVC 建议用 `_dupenv_s`，/W4 /WX 把 C4996 提升为错误
  - **解决**: 封装 `getEnvVar()` 用 `_dupenv_s` 实现
  - **预防**: Windows 代码避免直接 getenv/strcpy 等弃用函数，用 _s 安全版
  - **权重**: 3.9（工具执行错误2 × 详细分析2.0 × 解决方案1.5 × 已解决1.0）

## 高权重问题（生产环境审查 Task 1.10 新增，2026-08-12 19:04）

- [2026-08-12 19:04] **问题类型**: 设计缺陷（权重 31.9）
  - **描述**: 生产环境模拟发现编译器8个BUG：1)语义层 `整数`/`小数` 类型别名未规范化，`整数 x = 10` 报"无法将 '整32' 隐式转换为 '整数'"；2) x64_codegen 未登记局部变量槽，全部变量访问生成 `[rbp0]`（A2006 undefined symbol: rbp0）；3) `idiv [rbp-X]` 内存操作数缺 size 前缀（A2023）；4) Win x64 调用约定违反：调用侧未预留32字节影子空间、接收侧栈参偏移 `[rbp+40]` 错位（应 `[rbp+48]`）；5) IR 层变量遮蔽失效：`varRegs_` 平铺映射被内层同名覆盖，块外读错值；6) i32 参数传 `printLineInt(long long)` 未符号扩展，负数变巨大正数（4294967291）；7) `--output` 深层目录不自动创建（LNK1104）；8) driver 流水线 IR/codegen 阶段未检查 `diagnostics.hasErrors()` 仍返回0
  - **原因**: 类型别名用字符串相等比较、槽登记遗漏 Alloca、MASM 内存操作数需显式 size、调用约定实现未按 MS ABI（影子空间+栈参布局）、作用域用平铺 map、C ABI 整型参数需64位+符号扩展、路径处理未建目录、错误检查遗漏
  - **解决**: 1) `canonicalType()` 规范化（整数→整32/小数→浮64），declareVar/registerFunction/canConvert/commonNumericType 统一使用；2) `generateFunctionAssembly` 扫描全部 Alloca 登记变量槽；3) `idiv dword ptr/qword ptr [rbp-X]`；4) `emitCall` 一次性 `sub rsp,(32+栈参+对齐)` 预留影子空间，接收侧 `[rbp+48+(i-4)*8]`；5) IR 作用域栈 + 唯一内部名 `name$N`，func.params 保留源码名 + paramUniques 存唯一名；6) 传参前 `movsxd` 符号扩展 i32→i64；7) `ensureDirExists()` 逐级创建目录，中间文件与 exe 同目录；8) runPipeline 补 IR/codegen 后 hasErrors 检查
  - **预防**: 类型别名必须统一规范化后再比较；Alloca 必须登记槽；MASM 内存操作数带 size 前缀；Win x64 调用必须预留影子空间+16对齐+符号扩展；作用域必须用栈结构；`--output` 路径必须自动建目录；流水线每个阶段后检查错误
  - **权重**: 31.9（设计缺陷10 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

## 既有经验（继承自 HANDOFF，权重 10）

- **GCC 7 不支持中文标识符**：所有标识符（含 gtest 测试名）必须英文，中文仅用于注释/字符串
- **无外网**：googletest 用本地 `~/third-party/unittest/googletest`（LLVM定制版，需 `-DGTEST_NO_LLVM_SUPPORT`）
- **本机构建命令**：`export PATH=$HOME/gcc7/usr/bin:$PATH && cmake -B target/Debug -S . -DCMAKE_CXX_COMPILER=$HOME/gcc7/usr/bin/g++`
- **编译产物在 target/**：测试二进制在 `target/cn_unit_tests`
- **pkill 自匹配**：`pkill -f` 会匹配自身 shell，先 `pgrep -af` 再精确 kill
- **synchronize() 必须前进**：错误恢复停在边界字符会死循环
- **parseStmt() 必须分发 LeftBrace**：嵌套代码块是合法语句
- **`0 ++i` 词法歧义**：字面量后紧跟 ++ 解析为后缀自增，测试需分号分隔
- **peekNext(n) 先跳过当前位置**（`i <= n`）
- **运算符贪婪匹配**：三字符 > 双字符 > 单字符
- **字符串Token保留完整原始文本**

## 实施规范（用户规则）

- 中文回复用户；代码添加中文注释但标识符英文
- 文件 ≤1000行、函数 ≤100行
- E2E先行；禁止 skip()；产物在 target/
- 编译零警告（-Wall -Wextra -Werror）
- plans 文档完成后打勾
- 提交前更新"更新日志.md"（覆盖写入本次内容）
- 会话结束前写 HANDOFF.md 交接
- 实施计划示例代码用中文API命名，但**用户明确要求英文API命名**（指令优先）

## 任务经验（2026-08-12 实施计划文档拆分）

- [2026-08-12 20:36] **问题类型**: 任务经验（设计缺陷，权重 6.0）
  - **描述**: 大型计划文档（3968行）拆分重构的正确做法沉淀：按阶段边界切割（`### Task` 标题行处），拆分后内容零丢失（88个`[x]`勾选框、全部代码块、"已实现/已修复"历史备注、Task 1.1a 特殊任务均保留），交叉引用双向成对，原文件改写为导航索引而非删除
  - **原因**: 拆分大型文档时，若直接复制粘贴整段内容，勾选框与历史备注块容易被误删，导致后续 AI 误判任务状态；单文件超 1000 行违反项目文件约束
  - **解决**: 1) 在 `### Task` 标题行处按阶段边界切割；2) 拆分后逐段核对勾选框/历史备注完整性；3) 每个子文件头部带"前序文档/后续文档"双向链接；4) 原文件改写为索引文件（≤200行），保留既有引用
  - **预防**: 后续任何大型计划文档拆分，先识别 `### Task` 边界，再逐段搬移并保留全部 `[x]` 与"已实现/已修复"备注；拆分完成后运行链接校验（双向成对 + 索引可达）
  - **权重**: 6.0（设计缺陷10 × 低影响0.6）

## 任务经验（Task 2.1 控制流增量实施 switch，2026-08-12）

- [2026-08-12 21:30] **问题类型**: 逻辑错误（权重 31.2）
  - **描述**: IR 控制流生成标签 bug 链：1) genIf/genWhile/genFor 用 `"bb" + (blockCounter_ + N)` **预计算标签但不递增 blockCounter_**，嵌套控制流（否则-如果链/体内嵌套）会从旧编号开始分配块，导致标签重叠（ml64 A2005 symbol redefinition）与跳转错位；2) genSwitch 首版按"预留 caseCount*2+1 个编号"计算绝对标签，case 体块内嵌套控制流新增块后编号错位；3) 修复方案统一为**标签随建随取**（每个标签 `blockCounter_++` 预消费），保证嵌套任意深度标签唯一
  - **原因**: 原实现假设"块编号连续且无嵌套"，未考虑控制流语句体内还会递归生成新块
  - **解决**: genIf/genWhile/genFor/genSwitch 全部改为先 `blockCounter_++` 预消费标签再引用；genSwitch 改为两阶段（先预分配全部标签字符串，再逐个 newBlock 填充），杜绝与嵌套控制流编号冲突
  - **预防**: 生成基本块标签时必须"预消费计数器"（取完即 ++），严禁基于 `blockCounter_ + N` 预计算未来编号后不推进；新增控制流结构后必须跑 E2E 覆盖嵌套场景
  - **权重**: 31.2（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 21:30] **问题类型**: 逻辑错误（权重 15.6）
  - **描述**: IR `visitCharLiteral` 对字符字面量 `'B'` 用 `decodeString()` 解码，但该函数只剥离**双引号**，字符是**单引号**，结果 `text="'B'"` 取 `text[0]='`（39），switch 字符匹配全部落默认分支；parser `parseCaseValue` 正确取 `text[1]`（66）导致两侧不一致
  - **原因**: 字符字面量复用字符串解码函数，未单独处理单引号剥离；阶段一 IR 层只取首字节简化实现
  - **解决**: `visitCharLiteral` 单独剥离单引号（`raw.front()=='\'' && raw.back()=='\''`），与 parser 的 case 值解析保持一致
  - **预防**: 字符字面量（单引号）与字符串字面量（双引号）必须分开处理；新增字面量解析时两端（parser 常量求值 / IR 表达式求值）必须使用同一解码规则
  - **权重**: 15.6（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 已解决1.0）

- [2026-08-12 21:30] **问题类型**: 工具执行错误（权重 3.9）
  - **描述**: `Token::getText()` 不存在——Token 的 getter 是 `getValue()`（parser 里 `current().getValue()` 已在既有代码使用），新写的 parseCaseValue 误用 getText 导致 C2039 编译失败
  - **原因**: 未先确认 Token 类的实际 API 就使用推测方法名
  - **解决**: 改为 `current().getValue()`
  - **预防**: 调用既有类方法前先 grep 该类头文件确认成员名；新代码与既有代码风格保持一致
  - **权重**: 3.9（工具执行错误2 × 详细分析2.0 × 已解决1.0）

- [2026-08-12 21:30] **问题类型**: 逻辑错误（权重 7.8）
  - **描述**: parser `parseCaseValue` 解析成功（读整型/字符字面量）后**未 advance() 消费 Token**，调用方随后 `consume(Colon)` 时 current 仍是字面量，报"预期 ':'，实际为 '1'"
  - **原因**: 解析函数只读不改指针，调用方误以为已消费
  - **解决**: parseCaseValue 成功分支补充 `advance()`
  - **预防**: 解析"取值并推进"的函数必须消费 Token；调用 `consume()` 前确认 current 已推进到期望 Token
  - **权重**: 7.8（逻辑错误8 × 详细分析2.0 × 已解决1.0）

## 任务经验（Task 2.2 函数完整支持，2026-08-12）

- [2026-08-12 21:57] **问题类型**: 逻辑错误（权重 31.2）
  - **描述**: E2E 03_function 运行崩溃（0xC0000005 访问冲突）：emitCall 只在 `argCount > 4` 时预留影子空间，参数≤4 的直接调用/间接调用完全不留 32 字节影子空间。Win x64 ABI 要求**所有调用方必须在 call 前预留 32 字节影子空间**（无论参数多少），否则被调 C 函数（如 printLine）将参数写入栈顶踩坏调用方栈帧；间接调用 `call r11` 后该问题必然触发
  - **原因**: 阶段一 emitCall 简化为"仅栈参数时分配"，未遵循 Win x64 影子空间硬性要求；直接调用 printLine(1参) 此前侥幸通过（printLine 未写影子空间），函数指针间接调用打破侥幸
  - **解决**: emitCall 重构——参数≤4 也 `sub rsp, 32`（call 后 `add rsp, 32`）；argCount>4 时额外分配栈参数区；分配/恢复严格对称
  - **预防**: 任何 call（直接/间接）前必须预留影子空间 32 字节并保持 16 对齐；新增调用形式（间接调用/虚调用）后必须跑 E2E 验证运行期不崩溃
  - **权重**: 31.2（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 21:57] **问题类型**: 逻辑错误（权重 15.6）
  - **描述**: 语义单元测试 `整32 结果 = 回调(10, 20)` 报"预期变量名，实际为 '结果'"：`结果`（Kw_Result）是 CN 语言 53 个保留关键字之一（结果<T,E>类型），lexer 将其识别为关键字而非标识符，parser 的 `check(Identifier)` 失败
  - **原因**: 编写测试源码时未查阅关键字表，`结果`/`错误`/`某些` 等内置构造器相关词与错误处理关键字不能作变量名
  - **解决**: 测试源码变量名改用非关键字 `r`
  - **预防**: 编写 CN 测试/示例代码时避开 53 关键字（结果/错误/某些/变量/选择/情况/默认等）；报"预期变量名"先查该 token 是否被 lexer 识别为关键字
  - **权重**: 15.6（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 已解决1.0）

- [2026-08-12 21:57] **问题类型**: 工具执行错误（权重 3.9）
  - **描述**: `check(TokenType::LeftParen, /*unused*/ 0)` 误写——check 只接受 1 个参数，编译报错；这是编写 parseParamDecl 函数指针探测时残留的占位代码
  - **原因**: 编辑中途插入占位未清理，编译才暴露
  - **解决**: 删除占位，直接使用完整探测条件（peek(1)..peek(5)）
  - **预防**: apply_diff 修改后立即检查是否有残留占位/半成品代码；编译错误 C2059 等先看是否为编辑残留
  - **权重**: 3.9（工具执行错误2 × 详细分析2.0 × 已解决1.0）

## 高权重问题（Task 2.3 类型系统新增）

- [2026-08-12 22:59] **问题类型**: 集成问题（权重 31.2）
  - **描述**: ml64 汇编 SSE/8位指令大量 A2070/A2022/A2048：1) `movss xmm0, @fp0`（.data 段标签）报 A2070 invalid instruction operands——MASM 无法从标签推断内存宽度，需 `movss xmm0, dword ptr @fp0`；2) `movsx eax, [rbp-X]`（8/16位内存操作数）报 A2070——需 `movsx eax, word ptr [rbp-X]`；3) `movss [rbp-X], xmm0`（SSE 内存目标）同样需 `movss dword ptr [rbp-X], xmm0`；4) `mov eax, 0b1100`/`0xFF` 报 A2048/A2206——MASM 不支持 0b/0x 前缀，需转十进制；5) `@fp0 dd 0x3FF8...` 报 A2206——MASM 十六进制必须 `3FF8...h` 格式
  - **原因**: MASM（ml64）对内存操作数**从不推断宽度**，所有 movsx/movzx/movss/movsd/ucomisd/cvttsd2si 的 [rbp-X] 或标签操作数必须显式 byte/word/dword/qword ptr；MASM 数字字面量语法与 C 不同
  - **解决**: codegen 新增 `memSizePtr()` 辅助（byte/word ptr）；所有 SSE 指令内存操作数加 dword/qword ptr；浮点常量位模式改 `%016llXh`；0b/0x 前缀统一转十进制
  - **预防**: 生成汇编时凡涉及 [rbp-X]/标签的 movsx/movzx/SSE 指令必须加大小前缀；数字字面量只用十进制；浮点位模式用 `xxxh` 格式
  - **权重**: 31.2（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 发生频率2）

- [2026-08-12 22:59] **问题类型**: 逻辑错误（权重 16.8）
  - **描述**: 8/16/32位值（u32/i32/u8 等）存入 8 字节栈槽后，以整64（打印行整数参数）读取时读到槽中高位栈残留垃圾（如 123U 打印成 549755814011、4000000000U 打印成 33214485043554305）
  - **原因**: 编译器所有虚拟寄存器/变量槽固定 8 字节，小宽度 Store 只写低 4/2/1 字节，高字节是栈残留；Win x64 ABI 整参按 64 位传递，emitCall 直接 mov rcx, [槽] 读满 8 字节
  - **解决**: 1) Store 时小宽度值先符号/零扩展存满 8 字节（movsx/movsxd 或 movzx）；2) emitCall 实参统一 Cast 到 i64（IR 层 visitCallExpr 对所有 <64位 整数实参生成 Cast）
  - **预防**: 涉及多宽度共存的栈槽必须"存满 8 字节"（扩展后再存）；实参传递统一 Cast 到 ABI 宽度；出现巨大/负数打印值先怀疑槽高位垃圾
  - **权重**: 16.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 22:59] **问题类型**: 逻辑错误（权重 12.6）
  - **描述**: 无后缀大整数字面量 `5000000000` 默认整32 导致截断（mov eax 溢出成 705032704）；需按值自适应提升为整64
  - **原因**: 规格书默认无后缀字面量为整32，但值超出 int32 范围时应自动提升（C++ 字面量规则）
  - **解决**: IR 层 visitIntegerLiteral 对无后缀且值>int32 范围的提升为整64；emitConstLoad 对超32位值用 mov rax
  - **预防**: 字面量类型 = 后缀决定 + 值自适应（超范围自动提升）；codegen 常量加载按值大小选寄存器宽度
  - **权重**: 12.6（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 已解决1.0）

## 高权重问题（Task 2.4 数组与指针新增）

- [2026-08-12 23:00] **问题类型**: 逻辑错误（权重 20.8）
  - **描述**: StorePtr（指针存储 `*p = v`、`数据[i] = v`）运行时崩溃 0xC0000005：`emitPtrLoadStore` 先用 `mov rax, addr` 把目标地址放 rax，随后 i32 分支用 `mov eax, value` 加载值——**写 32 位寄存器 eax 会清零 rax 高 32 位**，最终 `mov [rax], eax` 写入地址 0x000000000000000A（值本身）而非真实地址，访问冲突。i8/i16（movsx eax）、u8/u16（movzx eax）分支同样存在
  - **原因**: x86-64 经典陷阱：32 位写清零高 32 位。LoadPtr 不崩是因为读完后地址不再需要；StorePtr 必须地址与值分寄存器保存
  - **解决**: StorePtr 值统一经 `rcx/ecx/cl/cx` 加载（`mov ecx, value` + `mov [rax], ecx`），rax 保留地址
  - **预防**: 代码生成中"地址与值共存"时，值加载禁止用 eax/rax（会破坏地址），用 rcx 系列；凡 `mov [rax], e` 前出现 `mov e`（32位）都要警惕清零
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 23:00] **问题类型**: 设计缺陷（权重 26）
  - **描述**: 数组元素布局方向与指针算术方向不一致导致 E2E 失败：局部数组曾用 Sub（元素 i = 基址 - i*8），而指针算术用 Add（p+1 = p+8）。`&数据3[0]` 基址上 `q = q+1` 指向未分配的栈区（打印垃圾 1044304），`r < s`（r=base, s=base-16）比较方向反转
  - **原因**: 数组槽登记（顺序/逆序）与 IR 地址计算（Add/Sub）需联动一致；曾尝试 4 种组合（Add/Sub × 顺序/逆序），方向与 C 语义（数组向上增长、p+1 指向下一元素）不符
  - **解决**: 统一为 C 语义——IR 元素地址 = base + i*8（Add），codegen 数组槽**逆序登记**（元素 N-1 先、基址槽最后=最深），与参数数组登记方式一致
  - **预防**: 数组布局是跨 IR/codegen 的全局决策，必须先定方向（C 语义 Add + 逆序登记）再实现；改方向必须同步 IR 全部地址计算（下标/lvalueAddress/初始化列表/补零）与 codegen 登记顺序；验证用 `&a[0]` 基址上 p+1 指向 a[1]
  - **权重**: 26（设计缺陷10 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-12 23:00] **问题类型**: 逻辑错误（权重 15.6）
  - **描述**: emitBoundsCheck 生成 CFG 时 okLabel 块被跳过：手动预取 `okLabel = bb(blockCounter_+1)` 后调用 `newBlock(errLabel)`（内部 blockCounter_++），再 `newBlock(okLabel)`（内部再 ++），导致 okLabel 实际 = bb(N+2) 而非 bb(N+1)，jmp 到错误块地址崩溃
  - **原因**: newBlock 内部递增 blockCounter_ 与手动预取标签双重递增，标签错位
  - **解决**: 先 `endBranch(bad.toString(), errLabel, okLabel)`（用预取标签）再 `setCurrentBlock(newBlock(errLabel))`、`setCurrentBlock(newBlock(okLabel))`——newBlock 内部递增推进计数器，与预取标签对齐
  - **预防**: 涉及"预取标签 + newBlock"的模式必须确认计数器递增次数与标签数一致；CFG 生成后可用 `cn ir` 检查块号是否连续
  - **权重**: 15.6（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

## 高权重问题（优化器增强完善C 新增，2026-08-13）

- [2026-08-13 09:30] **问题类型**: 逻辑错误（权重 16.8）
  - **描述**: 优化器新 Pass 单元测试构造的 IR 基本块缺少终止信息（terminated=false），导致：① DCE 认为返回寄存器 %v9 无人引用 → 级联删除全部 Add/Mul（O1BasicPasses 失败）；② 跨块 DCE 可达性分析从入口沿跳转目标遍历"断链" → 误删可达块（O2CSEAndCrossBlockDCE 失败，块数剩1）
  - **原因**: 真实 IR 契约要求每块 terminated=true + termKind（跳转/条件跳转/返回）；测试构造图省事省略，违反契约
  - **解决**: 测试 makeBlock 后补 endReturn/endJump 终止信息（块0→跳转块1、块1→返回 %v9、块2→返回）
  - **预防**: 凡手工构造 IRModule 的测试，每个块必须带终止信息；优化 Pass 依赖块终止（DCE 引用收集 / 跨块可达性）——无终止的块行为未定义
  - **权重**: 16.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

- [2026-08-13 09:30] **问题类型**: 逻辑错误（权重 12.5）
  - **描述**: CopyPropagation/GlobalValue Pass 的 Load 命中槽值表登记替换时**无条件** changed=true，导致 fixpoint 永不收敛：第二次运行 Load 结果已无引用，但仍报告"修改"→ PassManager 无限迭代（O3RunTwiceStable 失败）
  - **原因**: 登记映射与"实际替换引用点"混淆——登记本身不是修改，replaceUses 实际替换才是
  - **解决**: 登记映射不设 changed；仅 replaceUses（替换引用点）返回 true 才置 changed
  - **预防**: fixpoint Pass 的 changed 语义 = "模块实际被修改"（指令/操作数变化），登记中间数据结构不算修改；新 Pass 必须验证"运行两次第二次返回 false"（收敛性测试）
  - **权重**: 12.5（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

- [2026-08-13 09:30] **问题类型**: 工具执行错误（权重 4）
  - **描述**: apply_diff 修改 copy_propagation.hpp 的 include 块时误删类声明起始行（class CopyPropagationPass : public Pass），导致 C2059/C2653/C2146 一串语法错误；cse.hpp 静态成员函数 patternOf 访问非静态成员 allowFloat_ 报 C2597
  - **原因**: ① include 附近插入点与类定义行相邻，diff 匹配边界误伤；② patternOf 声明为 static 却需访问成员变量
  - **解决**: ① read_file 复核后补全类定义；② patternOf 改为成员函数（去掉 static）
  - **预防**: apply_diff 修改头文件后立即 read_file 复核结构完整性；静态成员函数不能访问非静态成员（需改成员函数或传参）
  - **权重**: 4（工具执行错误2 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

## 高权重问题（Task 2.9/2.10 Debug 审查 新增，2026-08-13 12:37）

- [2026-08-13 12:37] **问题类型**: 集成问题（权重 27.3）
  - **描述**: Task 2.9/2.10 Debug 全面审查（模拟生产环境，target/audit29 边界用例）发现并修复 **8 个 BUG**：
    ① **i128 拼接 0xC0000005 崩溃**：IR 层 [`ir.cpp:1666`](src/cn_compiler/ir/ir.cpp:1666) i128 拼接分支用 `LoadPtr` 把双槽寄存器值当指针解引用（槽值当地址访问）→ 改为 `Cast i128->i64` 取低64位；
    ② **布尔→整数显式转换被拒**：语义层 `visitCastExpr` 的 `srcNumeric` 判断漏 `"布尔"`，`整32(真)`/`整64(假)` 被误拒 → 补 `src == "布尔"`；
    ③ **显式捕获无参 lambda 解析失败**：`peekLambdaCapture` 显式捕获分支 `]` 后仅认 `(`，`[外层] { ... }` 无参 lambda 被误判为数组下标 → `]` 后同时允许 `(`/`{`；
    ④ **负浮点常量 A2006**：`floatBitsHex` 用 `%016llXh` 未给以字母开头的十六进制补前导0，`-3.75` 位模式 `C00E...` 被当标识符 → 与 `uint64HexText` 一致补前导0；
    ⑤ **无符号字面量超位宽不提升**：`正32`/`正64` 值超出对应位宽（如 `9000000000000000000U`）走十进制文本直接存，codegen `std::stoull` 溢出回绕 → 加 `textExceedsU32/U64` 提升 `正32->正64`/`正64->正128`；
    ⑥ **浮→i128 转换被拦截**：emitCast 的"浮→整（cvttsd2si）"分支用 `!toFloat` 误捕 i128 目标，落 32 位截断 → 将"浮→i128"分支移到"浮→整"之前；
    ⑦ **i128→i128 同类型 Cast 落默认32位**：`整128(整128值)`（LL 后缀即 i128）无同类型分支，落 `mov eax` 读低32位垃圾 → 加双槽复制分支；
    ⑧ **u64→i64 Cast 落默认32位截断**：打印 `正64 5000000000` 走 `u64->i64` Cast 落默认 `mov eax` 输出 705032704 → 加 64 位位重解释分支；且超32位十进制立即数 ml64 按32位截断 → 转十六进制 `uint64HexText`。
  - **原因**: ① IR 层混淆"双槽寄存器值"与"指针地址"（LoadPtr 只用于解引用指针，i128 值取低64位应用 Cast）；② 语义层数值族判定漏布尔；③ parser 显式捕获探测与 `[]`/`[=]`/`[&]` 分支不一致（后者都允许 `{`）；④ `floatBitsHex` 与 `uint64HexText` 对"十六进制以 A-F 开头"的处理不一致；⑤ 无符号字面量提升逻辑缺失；⑥ emitCast 分支顺序错误（浮→整的 `!toFloat` 条件过宽）；⑦ emitCast 缺同类型 i128 分支；⑧ emitCast 缺 u64→i64 分支 + MASM 十进制立即数截断。
  - **解决**: 见上 8 点，均定位根因后最小修复；新增回归测试：语义 `test_cast_expr.cpp`（BoolToIntConversion）+ `test_lambda.cpp`（ExplicitCaptureNoParamOk）+ E2E `17_audit/审查回归.cn`（8 场景）。
  - **预防**: ① i128 双槽值是"寄存器值"非"指针"，取低64位必须 Cast 而非 LoadPtr；② 语义层类型判定（数值族/可转换）须覆盖布尔/枚举/字符全类别；③ parser 各 lambda 捕获形态的探测条件必须一致（`]` 后 `(` 或 `{`）；④ 十六进制生成器统一走 `uint64HexText`（含前导0逻辑），禁止各自 `%llX`；⑤ 无符号字面量同样需要值域自适应的位宽提升；⑥ emitCast 分支按"更具体类型优先"排序（i128 目标先于通用 !toFloat）；⑦ emitCast 必须覆盖同类型与 u64↔i64 转换矩阵；⑧ MASM 超32位立即数用十六进制。
  - **权重**: 27.3（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0）

## 高权重问题（Task 3.6 模块系统 新增，2026-08-14）

- [2026-08-14 03:00] **问题类型**: 集成问题（权重 16.8）
 - **描述**: 模块系统多文件编译首次集成验证：依赖模块（中文文件名 `数学.cn`）加载后编译器**挂起**（无输出、CPU 100%）。排查发现 Windows 上 `std::filesystem::u8path(path)` + `std::ifstream` 对**中文文件名路径**挂起（不返回也不报错）；改用 `MultiByteToWideChar(CP_UTF8)` + `_wfopen_s` 后正常。另一处：语义层模块限定调用重写 `node->callee = make_unique<IdentifierExpr>(memberName)` 后**继续用旧 MemberExpr 指针 mem**（`mem->memberName`/`moduleName` 引用），use-after-free → bad allocation 崩溃——重写销毁旧节点后必须先用局部变量缓存字段值
 - **原因**: ① MSVC 的 `std::filesystem::u8path` 已弃用且实现有缺陷——对 UTF-8 中文路径转换后 ifstream 构造/读取挂起（未返回）；② AST 节点重写（`unique_ptr` 赋值销毁旧节点）后，保存的旧节点裸指针/引用全部悬垂，访问即 UB（此处表现为 bad allocation）
 - **解决**: ① `module::readSourceFile` 用 `MultiByteToWideChar(CP_UTF8)` 转 UTF-16 宽路径 + `_wfopen_s` 读取；且**先试窄字符（fopen_s）再试 UTF-8 转换**（入口路径来自 cmd 为 GBK，依赖路径为源码内 UTF-8，两种编码兼容）；② 重写前 `const std::string funcName = mem->memberName;`（值拷贝），`moduleName` 同样值拷贝
 - **预防**: Windows 读取 UTF-8 中文路径禁止用 `std::filesystem::u8path`（MSVC 挂起），统一 `MultiByteToWideChar(CP_UTF8)` + `_wfopen_s`；AST 节点重写/替换（`unique_ptr` 赋值、vector 移动）后禁止继续访问旧节点指针/引用，字段值必须先拷贝到局部变量；凡"重写 + 引用旧节点"模式（模块限定调用/类型替换/泛型实例化）都要遵守"先取值后替换"
 - **权重**: 16.8（集成问题7 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.6）

- [2026-08-14 03:00] **问题类型**: 逻辑错误（权重 12.6）
 - **描述**: 模块系统单测 3 个失败：① `SemanticPrivateNotVisibleAcrossModules` 断言"跨模块私有函数不可见应报错"但 r.ok=true——测试变量名用 `结果`（结果<T,E> 模板关键字）导致语法错误，diags 有错但 makeUnit 不检查，parseSourceText 返回 false 后 unit->imports 空 → 主模块无依赖 → 拓扑序错位（数学模块被当入口，私有函数被合并）；② `ModuleUnit.imports`（parseSourceText 第5参数）与 `ast->imports`（parser 填充）不同步——driver 用 `unit->imports` 做依赖，若 parseSourceText 因语法错误提前 return，imports 空而 ast->imports 非空
 - **原因**: ① CN 语言 `结果`/`可选` 是保留关键字（错误处理模板），不能作变量名；测试源码未遵守；② 依赖收集有"AST 缓存"与"单元字段"两份，解析失败时不同步
 - **解决**: ① 测试变量名改 `数值`/`结果值`；② `topoSort` 的依赖源统一从 `ast->imports`（唯一权威，parser 填充）读取，废弃 `ModuleUnit.imports` 冗余字段（保留但不再作为依赖依据）
 - **预防**: CN 语言测试源码变量名避开保留字（结果/可选/正常/错误/某些/自身/父类等）；模块依赖收集只依赖 AST 的 imports（parser 权威），任何"缓存字段"都可能因解析失败不同步；makeUnit 类测试辅助须检查 parseSourceText 返回值与 diags
 - **权重**: 12.6（逻辑错误8 × 详细分析1.5 × 解决方案1.3 × 预防措施1.2 × 已解决1.0）

## 高权重问题（Debug 全面审查子任务 新增，2026-08-14，5 项遗留缺陷已修复 ✅）

> 本子任务系统性修复 E2E 子任务遗留的 5 项缺陷。以下逐项记录根因/解决/预防，
> **全部已修复**（单测 901/901、E2E 28/28、编译零警告 /W4 /WX，clean-first 全量验证）。

- [2026-08-14 05:00] **问题类型**: 逻辑错误（权重 16.8）**已修复 ✅**
  - **描述**: 类对象指针共享 + RAII 自动释放 → 重复释放堆损坏（0xC0000374）。`资源 乙 = 甲` 类对象赋值浅拷贝——两个变量槽存同一堆指针，函数返回时 `genClassDestructorCalls` 对两个变量各发射一次 DeleteObject，同一地址被 double free。
  - **原因**: 类对象是堆指针语义（变量槽存对象指针），但赋值走 genVarDecl/visitAssignmentExpr 的普通 Store 路径，直接把源指针拷给目标变量，无拷贝构造语义；RAII 析构扫描见两个"类类型变量"各释放一次。
  - **解决**: 类对象赋值改为深拷贝——genVarDecl（声明初始化 `资源 乙 = 甲`）与 visitAssignmentExpr（`乙 = 甲`）两处均：NewObject 新建独立堆对象 + CopyStruct 逐字节拷贝字段（含虚表指针），再 Store 新指针到目标变量槽。
  - **预防**: 类对象变量槽存的是**堆指针**而非值，赋值/初始化必须 NewObject+CopyStruct 深拷贝（C++ 拷贝语义），禁止直接 Store 源指针；RAII DeleteObject 与深拷贝语义必须配套，否则同一地址重复释放。

- [2026-08-14 05:00] **问题类型**: 逻辑错误（权重 14.4）**已修复 ✅**
  - **描述**: 运算符重载链式 `甲+乙+丙` 中间对象字段错乱（输出 2564+128i 而非 14+26i）。内层 `甲+乙` 的 BinaryExpr 结果类型在 IR 层无法识别，外层 `+` 落入 ptr+ptr 字符串连接分支（__cn_str_concat）。
  - **原因**: IR 层 `exprSrcType` 只认 IdentifierExpr/SelfExpr/SuperExpr/MemberExpr，内层 BinaryExpr 返回空串 → handleOperatorOverload 的 `isClassType(canonLeft)` 判假 → 走 ptr+ptr 字符串连接；语义层虽推导了重载结果类型但未回传 IR 层。
  - **解决**: BinaryExpr 新增 `resolvedType` 字段，语义层 visitBinaryExpr 命中运算符重载时写回重载方法返回类型；IR 层 `exprSrcType` 增加 BinaryExpr 分支读取该字段。
  - **预防**: 语义层推导出的中间表达式类型（运算符重载/构造器等）须经 AST 节点字段回传给 IR 层（resolvedType/resolvedSignature 模式），不能只依赖 IR 层从 AST 形态反向推导；链式表达式场景必须测试内层表达式类型传递。

- [2026-08-14 05:00] **问题类型**: 逻辑错误（权重 16.8）**已修复 ✅**
  - **描述**: 泛型类带参构造字段写入未生效（读垃圾）、多类型参数/浮点字段错乱。`盒子<整32>(42)` 带参构造调用后字段读 5834336；`容器<浮64>.设置(3.5)` 读 0.000000；多类型参数读 4362144,128。
  - **原因**: ① 构造函数查找用 `ci->methods.find(className)`——泛型实例化类构造方法名是原始泛型类名（盒子），className 是实例化名（盒子$整32），find 失败 → NewObject 后无构造体 Call，字段未初始化；② 泛型方法体提升时 `setupMethodParams` 用 AST 参数 typeName（仍是类型参数 T/U），未用 mi.paramTypes（已按实参替换），mapType(T) 误判为 ptr → 浮点/多类型参数位模式错乱。
  - **解决**: ① 构造函数查找改为遍历 methods 找 isConstructor（不再用 className 作 key）；② setupMethodParams 优先用 mi.paramTypes（泛型单态化已替换）作为参数类型。
  - **预防**: 泛型单态化后，实例化类的**构造方法名仍是原始泛型类名**（不是 类名$实参），凡按 className 查构造/方法名处须改用 isConstructor 标记遍历；泛型方法体 IR 提升的参数类型必须用 mi.paramTypes（已替换实参），禁止用 AST 原始 typeName（仍是类型参数）。

- [2026-08-14 05:00] **问题类型**: 集成问题（权重 14.4）**已修复 ✅**
  - **描述**: 被导入模块公开函数体引用私有符号 → 合并后未声明。`数学.cn` 公开函数 `三倍` 体内调用私有 `内部辅助`，合并时私有函数被过滤 → 语义层报"未声明 内部辅助"。
  - **原因**: module mergeModuleDecls 对被导入模块只合并公开声明（私有不跨模块），但公开函数体可能依赖本模块私有函数，私有函数未随闭包带入导致公开函数体找不到符号。
  - **解决**: mergeModuleDecls 增加"私有依赖闭包"——先收集公开函数体引用的函数名（AST 遍历 CallExpr），再迭代补齐被引用的私有函数（私有函数可能引用其他私有函数，迭代到不动点），公开函数 + 闭包私有函数一并合并。
  - **预防**: 模块可见性过滤不能简单"只留公开"——公开声明体引用的私有依赖必须做闭包合并（否则公开函数体链接失败）；闭包收集须迭代到不动点（私有函数可引用其他私有函数）；AST 函数引用收集要覆盖全部语句/表达式节点类型（含 lambda 体）。

- [2026-08-14 05:00] **问题类型**: 逻辑错误（权重 16.8）**已修复 ✅**
  - **描述**: 静态字段自增 `总数++` 计数恒 0；友元引用参数 `账户& 账` 成员访问报"类型 '账户&' 不是类类型"。&表达式传参读 0 属既有指针语义（实测正常，非缺陷）。
  - **原因**: ① visitUnaryExpr 的自增写回只处理 varStack_ 命中的普通变量（lookupVar），类字段（静态/实例）不在 varStack_ 中 → 只生成 LoadPtr 读值、无 StorePtr 写回；② types::canonical 不剥引用后缀 `&`，友元参数 `账户&` 经 findClass 判类类型失败。
  - **解决**: ① 新增 handleClassFieldIncDec——类字段（静态/实例）自增自减走"读-算-写回"三段（LoadPtr + Add/Sub + StorePtr）；② types::canonical 剥引用后缀 `&`（账户& → 账户），substTypeParam 增加引用后缀递归替换（T& → 实参&）。
  - **预防**: 类字段（静态/实例）不在 varStack_，凡"读取/赋值/自增"操作须经专用 handle 钩子（handleClassFieldRead/Assign/IncDec），不能依赖 lookupVar 命中；引用类型 `类型&` 在类型比较/类类型判定前必须先剥 `&`（canonical 统一处理）；自增自减必须"读-算-写回"三段，禁止只读不写。

## 高权重问题（阶段A-3：Linux ARM64 E2E 2 项缺陷修复，2026-08-14，已修复 ✅）

> 本子任务修复 Linux ARM64 后端接入 CLI 后 E2E 剩余的 2 个失败用例，
> **全部已修复**（单测 918/918、E2E 28/28、编译零警告 GCC 7 -Werror，Linux ARM64 本机真实运行）。

- [2026-08-14 11:07] **问题类型**: 逻辑错误（权重 16.8）**已修复 ✅**
  - **描述**: E2E `20_oop_class` 子类构造 `狗("旺财",3,"金毛")` 被解析为父类构造（2 参），第 3 参 `种` 被忽略 → `品种` 字段未初始化（输出空）。`动物`（无继承）构造正确，`狗 : 动物` 子类构造错误。
  - **原因**: IR 层 [`ir_oop_call.cpp`](src/cn_compiler/ir/ir_oop_call.cpp) 构造解析遍历 `ci->methods` 找"首个 isConstructor"，但继承并入的父类构造（`动物` 的 ownerClass="动物"）也在子类 methods 表中，与子类自身构造（`狗` 的 ownerClass="狗"）共存。`unordered_map` 遍历顺序在 GCC（Linux）与 MSVC（Windows）下不同——Windows 恰好先命中子类构造（x64 全过），Linux 先命中父类构造，行为差异。
  - **解决**: 构造匹配增加 `ownerClass == className` 限定，只匹配"本类自己声明"的构造（泛型实例化类 ownerClass=实例化名，同样成立；父类构造被排除）。
  - **预防**: 遍历 `unordered_map` 做"首个命中"类选择必须不依赖遍历顺序（跨编译器/平台行为不一致），须加确定性限定条件（如 ownerClass 匹配）；继承并入父类成员时，"构造/析构"这类与类身份强绑定的成员应排除或用 ownerClass 区分，不能简单并入后靠遍历顺序兜底。

- [2026-08-14 11:07] **问题类型**: 逻辑错误（权重 14.4）**已修复 ✅**
  - **描述**: E2E `14_integration2` 冒泡排序后数据错乱（150 排最前）。排序比较链（i128）经 `__cn_cmp_i128` + cset 判断，CopyStruct（48B）交换正常，问题在比较结果判读。
  - **原因**: arm64 端 [`arm64_codegen_i128.cpp`](src/cn_compiler/codegen/arm64/arm64_codegen_i128.cpp) 对 `__cn_cmp_i128` 返回的 32 位 `int`（AAPCS64 下 x0 高 32 位未定义）用 `cmp x0, #0`（64 位）比较。返回 -1 时若 x0 高 32 位残留 0，x0=0xFFFFFFFF 被读成正数 → `cset x9, gt` 把"小于"误判为"大于"，升序退化。x64 端用 `test ecx, ecx`（32 位）正确，arm64 移植时误用 64 位比较。
  - **解决**: 改 `cmp w0, #0`（32 位比较），与 x64 的 `test ecx, ecx` 语义对齐。
  - **预防**: 调用返回 32 位 `int` 的运行时辅助后，判读必须用 32 位寄存器（w0/ecx），禁止用 64 位（x0/rax）——AAPCS64/x64 均不保证返回 int 时高 32 位清零；从 x64 移植比较/判读逻辑时，须逐条核对 32/64 位宽一致性（test ecx 对应 cmp w0、setcc 对应 cset）。

## 高权重问题（阶段C：寄存器分配 + 调试信息，2026-08-14，已修复 ✅）

> 本子任务实现线性扫描寄存器分配器（reg_alloc）与调试信息（debug_info），
> **全部完成**（964/964 单测、28/28 E2E、-O0 与 -O2 输出一致、编译零警告）。

- [2026-08-14 16:10] **问题类型**: 逻辑错误（权重 12.0）**已修复 ✅**
  - **描述**: 线性扫描分配器首次接入真实 IR 时**全部虚拟寄存器溢出到栈槽**（无物理寄存器被使用）——调试发现 `LinearScanAllocator` 构造时把 `allocableCalleeSavedRegs(forceDisable)` 的返回值（可分配寄存器集合）当作"保留寄存器集合"传入，导致 5 个被调用者保存寄存器全被当作保留扣除，`regs_` 为空 → `freeRegs` 恒空 → 所有区间溢出。
  - **原因**: 参数语义混淆——辅助函数名 `allocableCalleeSavedRegs` 返回"可用集合"，但构造参数名是 `reservedRegs`（保留集合），调用方误把可用集当保留集传入。
  - **解决**: 直接 `LinearScanAllocator(TargetArch::X64)`（默认被调用者保存寄存器集），不再传错位参数。
  - **预防**: 寄存器分配器构造的 `reservedRegs`（保留集合）与"可用集合"语义相反，调用方必须明确区分；分配器集成后必须验证汇编**实际出现物理寄存器**（grep push rbx），不能只验证模块单测通过。

- [2026-08-14 16:10] **问题类型**: 逻辑错误（权重 9.6）**已修复 ✅**
  - **描述**: reg_alloc 单测初版 4 个失败——`LiveInterval.regId` 未初始化（默认 -1）、`start` 被 `std::min(0, point)` 恒为 0（首次创建未直接设 start）。手工构造 IR 与真实 IR 的区间计算行为不一致导致误判。
  - **原因**: `intervalMap[regId]` 用 `operator[]` 默认构造 LiveInterval（regId=-1、start=0），def 登记用 `std::min(li.start, point)` 恒 0；真实 IR 因有跨块活跃扩展掩盖了问题。
  - **解决**: def/use 登记改为"首次创建直接设 start/end/regId，后续才 min/max"。
  - **预防**: 数据结构默认值（-1/0）在 `operator[]` 隐式插入时必须显式初始化字段；活跃区间计算的单测必须覆盖"首次创建"路径（不能只测后续更新）。

- [2026-08-14 16:10] **问题类型**: 集成问题（权重 8.4）**已修复 ✅**
  - **描述**: x64 -O2 汇编 prologue `push r12/r13/r14/r15`（4 个被调用者保存寄存器），epilogue 逆序 pop 配对正确——但 E2E 手动脚本误报 04_types/05_array_pointer FAIL（diff 行尾空白差异），官方脚本（rstrip 忽略行尾空白）PASS。
  - **原因**: 手动脚本用 `diff -q` 严格比较，expected 文件行尾有空格而程序输出无空格；官方脚本逐行 `rstrip()` 后比较。
  - **解决**: 用官方脚本验证（忽略行尾空白），或比较前 `sed 's/[[:space:]]*$//'` 规范化。
  - **预防**: E2E 一致性验证必须用官方脚本（rstrip 语义）或规范化行尾；手动 diff 严格比较会误报行尾空白差异（`整8 运算:` vs `整8 运算: `）。

- [2026-08-14 16:10] **问题类型**: 设计缺陷（权重 10.4）**已解决（保守降级）**
  - **描述**: arm64 后端结果写回点分散于 35 处 `emitStackStore(writer, regSlotOffset(inst.result.id))`（不收敛于 resultText），全量接入寄存器分配需改 35 处且回归风险高；arm64 E2E 无法在本机跑 x64 验证分配正确性。
  - **解决**: arm64 提供 `setRegAllocMap` 注入 API（reg_alloc 模块独立可用、单测充分），默认关闭（全栈帧不变）——按指令授权"可降级为可开关特性，默认可关"条款执行，正确性最高优先。
  - **预防**: 多后端架构接入平台相关优化时，先评估"写回点收敛性"——后端若将结果写回硬编码在 35 处（未收敛于 resultText 等单点），全量接入成本高；保守策略：模块独立可用 + 单测充分 + 注入 API 清晰 + 默认关闭，待写回点收敛后再启用。

## 高权重问题（新Win10环境恢复发现，2026-08-14）

> 新 Win10 系统环境恢复构建基线时发现 2 个缺陷（均与新环境 MSVC 相关），
> **当前状态：umul128 缺陷已修复 ✅（2026-08-14 19:05）；reg_alloc -O2 崩溃缺陷已修复 ✅（2026-08-14 19:50，栈对齐根因）。**
> 构建基线：CMake 配置成功、全量构建零错误零警告；单测 965/965（修复后含新增回归测试）、E2E 28/28（-O0/-O2/-O3 三级别输出一致）。

- [2026-08-14 19:01] **问题类型**: 逻辑错误（权重 20.8）**已修复 ✅（2026-08-14 19:50）**
  - **描述**: 换新 Win10 环境后 E2E 21/28 崩溃（退出码 3221225477=0xC0000005），单测 963/964。对照实验定位：`-O0` 全部用例输出正确、`-O2 --no-regalloc` 全部用例输出正确、**默认 `-O2`（启用寄存器分配）崩溃**——根因是阶段C 线性扫描寄存器分配器（reg_alloc，[`cn_main.cpp`](src/cn_main.cpp:192) `dopts.useRegAlloc = (optLevel >= 2) && useRegAlloc`）生成的 x64 汇编在新 MSVC 环境下错误。
  - **原因**: **x64 prologue 栈 16 字节对齐破坏**——[`x64_codegen.cpp`](src/cn_compiler/codegen/x64/x64_codegen.cpp:525) `emitPrologue` 先 `sub rsp, frameSize`（frameSize 已 16 对齐 ≡0 mod 16），**之后** `emitSaveCalleeSaved` 再 push 被调用者保存寄存器（rbx/r12~r15）。当 push 数量为奇数（N=1/3/5）时，函数体内 `rsp ≡ 8 (mod 16)`；`emitCall` 的 `sub rsp, 32`（16 倍数）不改变余数，**call 前 rsp≡8 (mod 16) 违反 Win x64 ABI**（call 时 rsp 必须 16 对齐）→ 被调方（MSVC 编译的运行时/用户函数）用 `movaps`/`movdqa` 对齐访问立即 0xC0000005。旧环境（GCC7 Linux 无 MSVC 对齐敏感运行时路径）未暴露，新 MSVC 环境暴露。失败用例统计证实：05/07 有函数 `push r12/r13/r14`（3 个奇数）、09 有函数 `push rbx/r12/r13/r14/r15`（5 个奇数）。
  - **解决**: **最小化修复（仅 x64_codegen.cpp emitPrologue +3 行）**：`calleeSavedRegs_.size() % 2 == 1` 时 `frameSize += 8`（保持 `sub rsp` 后 + 奇数 push 总和 ≡0 mod 16）。正确约束：sub 大小 F 满足 `(F + 8N) ≡ 0 (mod 16)`；computeFrameSize 已保证 F≡0 mod 16，故 N 奇数时 F+8。修复后「阶乘」`sub rsp,128→136`、「主」`552→560`、`664→672` 全部对齐。新增回归单测 `X64CodegenTest.PrologueStackAlignWithCalleeSaved`（test_x64_codegen.cpp）：1 个 i64 虚拟寄存器 → 分配 1 个被调用者保存寄存器（奇数 push）→ 断言 `sub rsp, 24`（16 对齐 + 奇数补 8）与 `push r12`；关闭 regalloc 时无 push、`sub rsp, 16`。
  - **预防**: ① 凡 prologue 在 `sub rsp, frameSize` **之后** push 被调用者保存寄存器（或任何 8 字节压栈），栈帧大小必须计入 push 字节数——正确公式 `F + 8N ≡ 0 (mod 16)`，不能只对 frameSize 单独 16 对齐；② 每次寄存器分配器集成后，必须验证函数体内 `call` 前 `rsp ≡ 0 (mod 16)`（可 grep prologue 的 `sub rsp` 与 push 数量手动核对）；③ 跨环境（GCC→MSVC、工具链升级）后必须重跑全量 E2E 验证 -O2 与 -O0 一致性；④ 优化器/寄存器分配器这类"代码生成优化"默认启用前必须在目标环境全量回归。
  - **权重**: 20.8（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.5）

- [2026-08-14 19:01] **问题类型**: 逻辑错误（权重 8.4）**已修复 ✅**
  - **描述**: 单元测试 `I128ApiTest.MulCrossTerms` 失败——`__cn_mul_u128`（`0xFFFFFFFFFFFFFFFF * 2`）输出高低 64 位互换（期望 out[0]=0xFFFFFFFFFFFFFFFE/out[1]=1，实际 out[0]=1/out[1]=0xFFFFFFFFFFFFFFFE）。[`i128_api.cpp`](src/runtime/i128_api.cpp:151) `umul128` 的 MSVC 分支 `hi = _umul128(a, b, &lo)` **参数语义写反**——`_umul128` 签名是"返回值=低64位、第三参数输出=高64位"，应写 `lo = _umul128(a, b, &hi)`。GCC 分支（__int128）写法正确，故旧 Linux GCC7 环境全过、新 MSVC 环境暴露（与 lessons 权重 27.3"换电脑构建失败"同类 MSVC 差异）。
  - **原因**: MSVC intrinsic `_umul128` 与 GCC __int128 的返回/输出语义不同，代码把"返回值（低）赋给 hi、第三参数（高）写给 lo"，高低互换。
  - **解决**: ✅ **已修复（2026-08-14 19:05）**：`umul128` MSVC 分支改为 `lo = _umul128(a, b, &hi);`（[`i128_api.cpp`](src/runtime/i128_api.cpp:151)），并补充注释说明 `_umul128` 返回值=低64位、out 参数=高64位。修复后全量构建零错误零警告，`I128ApiTest.MulCrossTerms` 等全部 I128 单测（42/42）通过，全量单测 **964/964 通过**。
  - **预防**: 移植 MSVC intrinsic（_umul128/_udiv128 等）时必须先确认参数语义（返回值 vs 输出参数），禁止凭 GCC 分支直觉写；换环境后重跑全部 i128 单测与 E2E 的 i128 用例（11_i128/14_integration2）。
  - **权重**: 8.4（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.0 × 已解决1.0 × 影响度0.4）

## 高权重问题（阶段6第4步 IO输入+文件库 新增，2026-08-14，已修复 ✅）

> 本子任务实现 IO 输入（读取行/读取整数/读取浮点/打印到错误）与文件读写（打开/读取/写入/读取文件行/大小/关闭/存在），
> **全部完成**（1005/1005 单测、36/36 E2E、编译零警告 MSVC /W4 /WX）。

- [2026-08-14 19:20] **问题类型**: 逻辑错误（权重 12.6）**已修复 ✅**
  - **描述**: E2E `34_file` 编译失败——`字符串 != 无`（读取文件行 EOF 判定）报"指针只能与指针或空指针比较，实际为 '字符串' 与 '空类型*'"。语义层比较运算 `isPointerType` 只认 `*` 结尾，`字符串`/`字符*` 不被视为指针，与空指针字面量 `无`（空类型*）比较被误拒。
  - **原因**: 字符串类型（字符串/字符*）本质是 char* 指针，但 `isPointerType` 仅按类型名尾字符 `*` 判定；IO/文件库的 `读取行`/`读取文件行` 返回字符串、EOF 返回 nullptr，CN 层必须写 `字符串 行 = ...; 如果 (行 == 无)` 判定，语义层误拒。
  - **解决**: ✅ **已修复**（[`semantic.cpp`](src/cn_compiler/semantic/semantic.cpp) 比较运算分支）：`leftPtr`/`rightPtr` 判定补充 `字符串`/`字符*`；指针间（含字符串）比较按地址放行（跳过 canConvert 检查，字符串 vs 空类型* 均以 ptr 表示）。
  - **预防**: 凡"返回字符串 + EOF/失败返回 nullptr"的 API（读取行/读取文件行/子串等），CN 层判定 `字符串 == 无` 必须被语义层放行——字符串本质指针，比较运算的指针判定须含 `字符串`/`字符*`；新增字符串返回 API 后先验证 `== 无` 判定可编译。
  - **权重**: 12.6（逻辑错误8 × 详细分析2.0 × 解决方案1.5 × 预防措施1.0 × 已解决1.0）

- [2026-08-14 19:20] **问题类型**: 逻辑错误（权重 8.4）**已修复 ✅**
  - **描述**: E2E `33_io_input` 编译失败——`整32 整数成功 = 0` 报"预期标识符，实际为 '整数'"（第 31 行 9 列）。变量名 `整数成功`/`浮点成功` 以保留字 `整数`/`浮点` 开头，词法分析最长匹配拆分为 关键字（整数）+标识符（成功）。
  - **原因**: CN 语言关键字（整数/浮点/字符串/结果/可选 等）是完整保留字，标识符不能**包含**它们作为前缀；`整数成功` 被 lexer 拆为 Kw_Int + 标识符，语法层"变量类型后需标识符"失败。
  - **解决**: ✅ 变量名改为 `整数标志`/`浮点标志`/`结束标志`（避免以类型关键字开头）。
  - **预防**: CN 测试/标准库变量命名避开类型关键字开头（整数/浮点/字符串/布尔/字符/结果/可选/错误/正常/某些/自身/父类/类/结构体/枚举 等）；词法对中文关键字最长匹配，任何"关键字+后缀"组合都会被拆词。
  - **权重**: 8.4（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.0 × 已解决1.0）

- [2026-08-14 19:20] **问题类型**: 工具执行错误（权重 4）**已修复 ✅**
  - **描述**: 单测 `test_input_api.cpp`/`test_file_api.cpp` 初版用 `std::fopen`/`std::freopen`（stderr 重定向捕获），MSVC /W4 /WX 下 `fopen`/`freopen` 触发 C4996 警告即错误（C2220）；且 `stderr = newErr` 赋值报 C2106（`stderr` 是 `__acrt_iob_func(2)` 宏右值，不可赋值）。
  - **原因**: ① MSVC 安全函数警告（C4996）在 /WX 下升级为错误；② `stderr` 在 MSVC CRT 中是函数调用宏（`__acrt_iob_func(2)`），不是可赋值左值；`freopen_s(&f, ..., stderr)` 参数语义也不支持重定向 stderr。
  - **解决**: ✅ ① 单测统一用 `fopen_s`/`freopen_s`（TEST_FOPEN/TEST_FREOPEN 宏封装，POSIX 分支回退 std::fopen）；② stderr 捕获改用 `_dup`/`_dup2` 文件描述符重定向（`_dup(_fileno(stderr))` 保存 → `_dup2(_fileno(file), _fileno(stderr))` 重定向 → 恢复 → `_close`）。
  - **预防**: MSVC 单测/运行时避免 `fopen`/`freopen`（/W4 /WX 下 C4996 即错误），用 `fopen_s`/`freopen_s` 或 `_dup`/`_dup2`；stderr 重定向禁止 `stderr = ...` 赋值（宏右值），用文件描述符级重定向；新增依赖文件操作的测试先确认 /W4 /WX 可编译。
  - **权重**: 4（工具执行错误2 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

## 高权重问题（模块系统 v2.0 第 5 层 货舱.toml，2026-08-15，已解决 ✅）

> 第 5 层实现 货舱.toml 依赖管理（cargo_parser + CLI 自动发现 + 依赖查找 + 包.cn 再导出），
> **全部完成**（1097/1097 单测、E2E 36/46（10 失败均第 6 层迁移项）、编译零警告 MSVC /W4 /WX，推送 gitcode eef0cd9）。

- [2026-08-15 14:00] **问题类型**: 集成问题（权重 10.8）**已修复 ✅**
  - **描述**: Windows 中文路径三重编码——命令行 argv（GBK）↔ 源码 UTF-8 字面量（货舱.toml/依赖/）↔ 磁盘 UTF-8 文件名。① 自动发现 货舱.toml 用 GBK 入口目录 + UTF-8 文件名混拼打不开；② 尝试 argv 整体转 UTF-8 后 E2E 18/19（中文目录名）回归——`pathStem(file)` 变 UTF-8 后中间文件名（.asm/.exe）传给 ml64/link 乱码；③ argv 保持 GBK 后 47_package_cargo 链接失败（LNK1120 无 cn_main）——入口 `主.cn` 模块名变 GBK 字节，`isEntryModule` 与 UTF-8 字面量 "主" 比较失败，主函数被过滤。
  - **原因**: 三条路径编码不一致：工具链（ml64/link）按 ANSI 代码页 936 解释路径；driver 模块名判定按 UTF-8 字面量；磁盘文件名为 UTF-8。
  - **解决**: ✅ **分层策略**——argv **保持 GBK**（工具链路径）；`ansiToUtf8()` 将入口文件路径转 UTF-8 传给 `runModulePipeline`（driver 层统一 UTF-8，模块名判定/依赖查找正确）；`applyCargoConfig` 自动发现时入口目录 ANSI→UTF-8 后拼 UTF-8 "货舱.toml"（纯 UTF-8 路径，readTomlFile 宽路径可打开）。
  - **预防**: Windows 下凡新增"命令行路径 + 源码字面量中文 + 工具链"三方交互：**不要整体转 argv 为 UTF-8**（工具链按 ANSI）；driver 层（模块名/依赖查找）统一 UTF-8，工具链层保持 GBK；入口文件读入 driver 前转 UTF-8。
  - **权重**: 10.8（集成问题7 × 详细分析1.8 × 解决方案1.3 × 预防措施1.3 × 已解决0.9）

- [2026-08-15 14:00] **问题类型**: 逻辑错误（权重 8.4）**已修复 ✅**
  - **描述**: `依赖/网络库/网络.cn`（货舱本地依赖子目录）以入口目录（entryDir）前缀开头被误判为入口 crate 内部模块，`moduleDir` 与 depRoot 重复拼接 → 加载路径变成 `依赖/网络库/依赖/网络库/网络.cn`。
  - **原因**: loadModuleTree 的 external 判定只看"文件是否在入口目录下"（relPart == filePath），货舱 `依赖/` 目录在入口目录树内（tests/e2e/47_package_cargo/依赖/...）故未被识别为外部 crate。
  - **解决**: ✅ external 判定补 `依赖/`、`stdlib/` 前缀——凡在货舱依赖目录或编译器 stdlib 目录内的文件一律视为外部 crate 模块（moduleName = 文件名主干，moduleDir 为空）。
  - **预防**: 凡按 entryDir 前缀判定"crate 内部模块"的逻辑，必须排除 `依赖/`、`stdlib/` 等外部依赖目录前缀；新增依赖查找目录后同步更新外部模块判定。
  - **权重**: 8.4（逻辑错误8 × 详细分析1.8 × 解决方案1.5 × 预防措施1.0 × 已解决1.0）

- [2026-08-15 14:00] **问题类型**: 工具执行错误（权重 4）**已修复 ✅**
  - **描述**: cargo_parser.cpp 新增 `readTomlFile` 的 `error` 形参未引用 → MSVC /WX 下 C4100 警告即错误（C2220），`--clean-first` 全量构建失败。
  - **原因**: /W4 /WX 严格警告策略下，任何未引用形参都升级为编译错误。
  - **解决**: ✅ 移除 readTomlFile 的 error 参数（loadCargoConfig 统一负责写 error）。
  - **预防**: 新增内部辅助函数时，形参要么使用要么删除；/WX 下先验证签名无未引用形参（C4100）再提交。
  - **权重**: 4（工具执行错误2 × 详细分析1.5 × 解决方案1.3 × 预防措施1.0 × 已解决1.0）

## 高权重问题（批次 A/B/C 执行 新增，2026-08-16）

- [2026-08-16 03:40] **问题类型**: 集成问题（权重 7.0）**✅ 已修复（C-2）**
  - **描述**: **入口文件必须命名 主.cn（带 导入 的单文件用例）**——E2E 61 起初命名 对属于.cn（含 导入 容器::向量），编译"成功"但生成空 IR（汇编仅 EXTERN 无任何 PROC），链接报 cn_main 未解析；改名为 主.cn 后语义错误才正常显现。模块系统按入口文件主干定模块名，非 主 的入口 + 导入 组合语义/IR 静默空转
  - **原因**: 入口模块约定为 主（驱动/模块合并按 主 识别）；单文件无导入时模块名=文件主干可用（55_reference 引用.cn 无导入正常），导入存在时模块合并路径依赖 主
  - **解决**: 新 E2E 用例目录入口文件统一命名 主.cn（与 30_container 等既有用例一致）；排查"编译无错但输出空/链接缺 cn_main"先检查入口文件名
  - **预防**: 带 导入 的用例入口必须 主.cn；空 IR（0 函数）优先怀疑入口模块名/导入解析而非代码生成
  - **权重**: 7.0（集成问题7 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-16 03:30] **问题类型**: 逻辑错误（权重 7.2）**✅ 已修复（C-2）**
  - **描述**: **降级 AST 漏构 元素表达式（elemExpr 未赋值）→ 静默错值**——对...属于 语义降级只填了 lenExpr（数组常量/大小() 调用），漏填元素表达式（数组下标/元素() 调用），IR 生成"分配变量槽后无 Store"，循环变量读未初始化槽输出 0；语义零报错、编译零警告，仅 E2E 值断言（1 2 3 vs 0 0 0）暴露
  - **原因**: 手工拼接 AST 时多字段降级树（长度/元素/索引三表达式）只构造了前两个；编译器对"声明了槽但从未写入"无静态检查
  - **解决**: 补齐 elemExpr 构造（数组 IndexExpr / 类 CallExpr 元素()）；IR 转储（--ir）复查可见 分配 后无 Store 的异常形态
  - **预防**: 降级/合成 AST 完成后先 --ir 目检关键路径（分配→存储→使用）；语义零错 ≠ 正确，值断言 E2E 是最后防线
  - **权重**: 7.2（逻辑错误8 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-16 03:20] **问题类型**: 集成问题（权重 6.8）**✅ 已修复（C-2）**
  - **描述**: **如果 <裸标识符> { 与 结构体初始化 歧义**——parsePrimary 把 标识符+{ 贪心判为 类型名{字段=值}，如果 ok { / 对 x 属于 数据 { 中迭代对象后的 { 被吞为结构体初始化（"结构体初始化预期字段名"）。既有代码全部写 如果 (条件) {（括号规避），裸标识符条件从未被测试
  - **原因**: 结构体初始化探测无前瞻约束（标识符后任意 { 即触发）；对...属于 的迭代对象后必然跟 { 循环体
  - **解决**: C-2 为 对...属于 迭代对象解析加 suppressStructInit_ 开关（上下文内禁止结构体初始化探测）；用户侧裸标识符 if 条件仍须括号
  - **预防**: 新增 标识符+{ 上下文（语句级关键字结构）必须考虑 parsePrimary 结构体初始化探测；测试优先覆盖裸标识符形态
  - **权重**: 6.8（集成问题7 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-16 03:10] **问题类型**: 工具链（权重 6.5）**✅ 已规避（C-3）**
  - **描述**: **pwsh/JS 双层转义改 C++ 源码极易错**——cn_main.cpp 链接命令行含 \" 序列，经 run_code(JS)→pwsh/JS 字符串→edit 工具双层转义后反斜杠数量错乱（C2017 非法转义序列 + 误替换 assembleAsm 调用），反复 4 次才修对
  - **原因**: 转义层数多（JS 源码 → 工具参数 → 文件内容），肉眼核对困难；replace() 仅替换首个匹配在多次编辑间互相污染
  - **解决**: 改用 Node fs 直接按行索引替换（读行号 → 精确改该行）或唯一短锚点（不跨行、不含转义序列）；commit 消息含引号时用 -F 文件提交
  - **预防**: 含转义序列/长文本的源码修改用 fs 按行改写；编辑后立即构建验证（C2017 类错误当场暴露）；replace 锚点选无转义的短文本
  - **权重**: 6.5（工具链6 × 详细分析1.3 × 解决方案1.3 × 预防措施1.3 × 已解决1.0）

- [2026-08-16 03:00] **问题类型**: 语法认知（权重 5.5）**✅ 已记录（C-3/C-4）**
  - **描述**: **CN 语法细节三则**——① 强制转换无 (类型)(表达式) 形式，须 类型名(表达式)（(整32)(x) 报"预期表达式"）；② 打印 布尔运算结果输出 1/0（仅 布尔 变量/字面量经字符串+ 输出 真/假）；③ 选择 分支须显式 中断 结束（C fallthrough，漏写会贯穿执行后续分支）
  - **原因**: 既有语法语义如此（转换语法与 C 不同、打印布尔转换只对 布尔 值生效、switch 为 C 风格）
  - **解决**: 测试与文档按实际语义书写（E2E 63 每个分支 中断；预期文件按 1/0 断言）
  - **预防**: 新写 CN 测试前先小样本实测语法边界；选择 分支模板统一带 中断
  - **权重**: 5.5（认知5 × 详细分析1.2 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）
## P3-18 引用返回（2026-08-19，007-P3）

- [ ] 问题类型: 设计缺陷  ✅ 已解决（P3-18 补完）
  - 描述: 引用返回（`函数 获取() -> 整32&`）被 `registerFunction` 整体拒绝（语义 1318 报"函数返回类型暂不支持引用"）；且规范 04-一F 与实现（引用变量 e2e 94 已支持）脱节；调用方（`整32& r=获取()`/`获取()=值`/取址）需识别"引用返回调用"。
  - 原因: 返回类型 canonical 剥 `&`，解析后无 ref 信息可查；IR 引用变量条目类型错置为 ptr 而非被引用基础类型（byRef 读解引用类型漂移）；直接调用发射用 canonical 返回类型 mapping 成 i32。
  - 解决: FunctionInfo.isRefReturn + `CallExpr.isRefReturnCall` 标记 + `funcReturnsRef` 语义助手 + IR ref-var 条目 type=stripRef 基类型 + ref-return 函数 IR 返回 ptr + return 左值地址化 + 返回语句"解剖 base 判局部"（引用参数/全局/静态可返回，局部/按值参数报错）。
  - 预防: 引用类特性必须保住"被引用基础类型"贯穿 IR 条目；返回类型剥 `&` 后须额外字段携带 ref 性。
  - 权重: 6.0


## P3-23 实例方法作值（2026-08-19，007-P3）

- [ ] 问题类型: 设计缺陷  ✅ 已解决（P3-23 补完）
  - 描述: `对象.实例方法` 作值目前编译通过但运行崩溃（IR 发裸 FuncAddr，`this` 缺失）；fnptr 是裸地址无法携带 this，加"绑定 this 闭包"（复用 lambda 捕获机制）。
  - 原因: 实例方法有隐藏 `this` 参数，裸函数指针按用户签名调用时第一个实参被当 this；闭包登记（closureInfo_）此前只对 lambda 初始值开放。
  - 解决: MemberExpr.isMethodValue 标记 + genVarDecl 对方法值初始值登记 closureInfo_（captureArgs=对象地址 Load 槽）+ 闭包调用展开 this 直接 Call 方法符号 + D2 诊断阻止裸 fnptr 直传。
  - 预防: "方法函数指针" 类特性先确认实例/静态（隐藏 this）差异；闭包 = 函数符号 + 捕获实参 是既有机制，新增可调值形态（方法值）须同步开放登记路径。
  - 权重: 6.0


## P3 去虚拟化与 CFI（2026-08-19，007-P3）

- [ ] 问题类型: 设计决策  ✅ 已实施（D3A 最小实现）
  - 描述: 接口调用现每次 1 次间接（LoadPtr+CallIndirect）；单一实现接口可编译期直调省 1 间接；CFI 目标表约束默认关。
  - 解决: ClassInfo.ifaceNames 登记 + interfaceImplClasses 语义统计（跳过抽象）→ IR handleInterfaceCall 唯一实现直 Call 实现类方法；--cfi 时间接路径发"目标∈已知实现表"运行时校验（Or/Not/Eq 链 + 错误块，照 emitBoundsCheck 模式）。
  - 预防: 去虚拟化必须以"全程序唯一非抽象实现（含继承并入）"为准，多实现/m抽象 一律回退间接；CFI 与"1 次间接"性能目标冲突 → 默认关开关。
  - 权重: 5.0

## 高权重问题（麒麟 ARM64 全量回归验证 新增，2026-08-20，已修复 ✅）

> 本子任务在麒麟操作系统 ARM64 上运行全量回归验证，发现并修复 ARM64 后端 BUG，
> **全部已修复**（编译零警告、单测 1189/1190、E2E 96 通过/0 失败/7 跳过）。

- [2026-08-20 16:01] **问题类型**: 逻辑错误（权重 16.8）**已修复 ✅**
  - **描述**: E2E `100_接口多态` 在 ARM64 平台运行时报段错误（SIGSEGV）。ARM64 后端 [`arm64_codegen_oop.cpp`](src/cn_compiler/codegen/arm64/arm64_codegen_oop.cpp) 的 `emitNewObject` 在分配对象后只填充了虚表指针（vtablePtr），未填充接口分派区（interface dispatch area）。x64 后端 [`x64_codegen.cpp`](src/cn_compiler/codegen/x64/x64_codegen.cpp) 的 `emitNewObject` 在填充虚表指针后，会遍历类的接口表（`classInterfaces`），为每个接口在对象头部写入对应的接口分派表地址。ARM64 后端遗漏此步骤，导致通过接口指针调用虚方法时，从接口分派区读取的地址为垃圾值，跳转到非法地址触发段错误。
  - **原因**: ARM64 后端实现 `emitNewObject` 时只参照了 x64 后端的部分逻辑（虚表指针填充），遗漏了接口分派区填充步骤。两后端的实现未做完整的对称性检查。
  - **解决**: 在 [`arm64_codegen_oop.cpp`](src/cn_compiler/codegen/arm64/arm64_codegen_oop.cpp) 的 `emitNewObject` 中，虚表指针填充后增加接口分派区填充逻辑：遍历 `classInterfaces`，为每个接口计算偏移（`sizeof(void*) * (1 + i)`），使用 `adrp + add` 加载接口分派表地址，使用 `str` 写入对象头部的接口分派区。
  - **预防**: 新增 ARM64 后端功能时，必须对照 x64 后端的完整实现逐行核对，确保两后端功能对称。特别关注 OOP 相关功能（NewObject/VirtualCall/接口分派等），x64 已实现的功能 ARM64 必须同步实现。
  - **权重**: 16.8（逻辑错误8 × 详细分析1.5 × 解决方案1.5 × 预防措施1.3 × 已解决1.0 × 影响度0.7）

- [2026-08-20 16:01] **问题类型**: 集成问题（权重 7.0）**已修复 ✅**
  - **描述**: 7 个 E2E 用例在 ARM64 Linux 平台上无法运行：33_io_input（依赖 stdin 交互输入）、34_file（依赖文件系统路径差异）、35_string_ext（依赖 Windows 编码行为）、36_time（依赖系统时间 API）、37_system（依赖 Windows 命令行参数编码）、38_tool（依赖 Windows 控制台编码）、39_chkstk（x64 专用 __chkstk 栈探测）。这些用例在 Windows x64 上正常，在 ARM64 Linux 上因平台差异无法运行。
  - **原因**: E2E 测试用例最初为 Windows x64 平台设计，部分用例依赖 Windows 特定功能（stdin 交互/__chkstk/控制台编码等），在 ARM64 Linux 上无对应实现。
  - **解决**: 在 [`run_e2e.py`](tests/e2e/run_e2e.py) 中添加 `PLATFORM_SKIP` 字典，按用例目录名匹配平台跳过。跳过用例计入"跳过"计数，不影响"通过/失败"统计。
  - **预防**: 平台限制用例应通过跳过机制处理，而非硬编码为失败或修改 expected 文件。新增 E2E 用例时，若依赖平台特定功能，应同时在 `PLATFORM_SKIP` 中注册跳过条件。
  - **权重**: 7.0（集成问题7 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）

- [2026-08-20 16:01] **问题类型**: 工具执行错误（权重 4.0）**已修复 ✅**
  - **描述**: GCC 7.5.0（aarch64）在 `-Wall -Wextra -Werror` 下报编译警告即错误：[`semantic_call.cpp`](src/cn_compiler/semantic/semantic_call.cpp) 有未使用的形参名，[`test_bootstrap_lexer.cpp`](tests/unit/semantic/test_bootstrap_lexer.cpp) 有未使用的变量。这些在 MSVC 下仅为警告（C4100），在 GCC 7 的 `-Werror` 下升级为错误。
  - **原因**: GCC 7 的 `-Wall -Wextra` 比 MSVC `/W4` 更严格，且 `-Werror` 将所有警告升级为错误。ARM64 上 `char` 默认为 `unsigned char` 也可能导致额外的类型警告。
  - **解决**: 移除未使用的形参名和变量，确保代码在 GCC 7 严格模式下零警告。
  - **预防**: 跨平台代码须同时满足 GCC 和 MSVC 的严格警告级别。在 ARM64 上提交前，先用 `-Wall -Wextra -Werror` 全量编译验证。
  - **权重**: 4.0（工具执行错误2 × 详细分析1.3 × 解决方案1.2 × 预防措施1.3 × 已解决1.0）


---

## [2026-08-20 23:40] 平台抽象层重构自举侧5组件创建

- **问题类型**: 设计缺陷
- **权重**: 10
- **描述**: 实施CN语言编译器平台抽象层重构（plans/009），自举侧后端抽象层5组件已创建（后端接口.cn/IR遍历.cn/x64后端.cn/arm64后端.cn/后端工厂.cn），代码生成.cn从971行硬编码重写为30行门面。但尚未验证（E2E 75/76/78未运行）。
- **原因**: 自举代码生成器100%硬编码x64 MASM，自举用例75/76/78在ARM64上无法运行。通过后端接口抽象+后端工厂分发解决。
- **解决**: 创建后端接口（29个虚拟函数）、IR遍历引擎（平台无关）、x64后端（Win x64 MASM）、arm64后端（Linux ARM64 GAS）、后端工厂（按目标分发）。代码生成.cn改为门面调用后端工厂+IR遍历。
- **预防**:
  1. 编写CN语言代码前必须调用cn-language-spec skill查询spec 06 OOP规范
  2. 接口用`接口`关键字（非`类`），虚函数用`虚拟 函数 方法名(参数) -> 返回类型`（无`= 0`、无分号、无函数体）
  3. 空类型用`空类型`（非`空`/`void`），语句无分号，返回类型后置`->`
  4. 代码生成.cn拆分可能触发GS崩溃，5组件结构需验证
  5. ARM64后端emitNewObject必须填充接口分派区（权重16.8）
- **状态**: 自举侧5组件已创建，待验证；C++侧平台抽象尚未开始

---

- [2026-08-21 10:22] **问题类型**: 逻辑错误（权重 17.0）
  - **描述**: **C++编译器IR生成bug——函数体合并导致栈溢出**。在ARM64上编译自举用例（E2E 75/76/78）时，
    C++编译器将多个函数的IR基本块合并到了一个IRFunction对象中。例如`解析数字`函数（10行代码）的IR有2094行、
    79个分配指令、最大寄存器ID 9573，导致栈帧大小64800字节（约63KB），运行时SIGSEGV栈溢出。
  - **原因**: C++编译器`IRGenerator::visitFunctionDecl`中`function_`指针在函数之间可能没有正确隔离，
    导致一个函数的基本块被追加到了前一个函数的`IRFunction`对象中。旧版代码生成.cn（971行单文件）和
    新版平台抽象层（5组件）都有同样的问题——这是原有bug，不是平台抽象层重构引入的。
  - **解决**: 需要在Windows x86环境上重新编译C++编译器，修复`function_`指针隔离问题。
    当前ARM64环境无法安装g++（麒麟OS仓库404），无法重新编译C++编译器。
    临时方案：将75/76/78加入`平台跳过`列表。
  - **预防**:
    1. 自举用例在ARM64上受限于C++编译器IR生成bug，需在Windows环境修复
    2. 跨模块同名函数（如`解析整数`/`字符值`）应重命名避免冲突（已将IR遍历.cn的`解析整数`→`解析数字`、`字符值`→`取字符值`）
    3. 麒麟OS仓库可能404，需提前安装g++或准备备用编译器
  - **状态**: 已加入平台跳过列表，待Windows环境修复C++编译器后移除

## ARM64后端窄类型加载/存储bug（2026-08-21 修复）

- [2026-08-21 11:50] **问题类型**: 逻辑错误（权重 16.8）**✅ 已修复**
  - **描述**: ARM64代码生成器在加载窄类型（i8/i16/u8/u16）时使用 `ldrsb w9`（32位目标寄存器），
    导致负数字节被错误地零扩展为正数。`ldrsb w9` 将字节符号扩展到32位w9，但写入w9会自动清零x9的高32位，
    使得负数字节（如 -32 = 0xE0）变成正数 0x00000000FFFFFFE0（4294967264）。
    后续 `值 < 0` 检查失败，返回错误的正值，导致 `字节十六进制2` 中 `字节 / 16` 产生巨大值，
    `字节值(表, 字节/16)` 访问越界，段错误。
  - **原因**: AArch64 ABI 规定写入 wN（32位）会自动清零 xN 的高32位。`ldrsb wN` 符号扩展到32位后，
    高32位被清零，等效于零扩展而非符号扩展。必须用 `ldrsb xN`（64位目标）才能正确符号扩展到完整64位。
  - **解决**: 
    - 有符号类型（i8/i16）：使用64位目标寄存器 `ldrsb x9`/`ldrsh x9`
    - 无符号类型（u8/u16）：使用32位w寄存器 `ldrb w9`/`ldrh w9`（零扩展到32位，写入w9自动清零高32位）
    - 存储时统一用64位 `str x9`（避免 `strb`/`strh` 只写部分字节导致高字节残留垃圾）
  - **预防**: AArch64窄类型加载必须区分有符号/无符号——有符号用xN目标（ldrsb xN/ldrsh xN），
    无符号用wN目标（ldrb wN/ldrh wN）。存储窄类型结果时用64位str xN避免截断。
  - **影响**: 修复了E2E 75/76（自举用例）在ARM64上的段错误，修复了04_types/18_缺陷修复的汇编错误

- [2026-08-21 11:50] **问题类型**: 配置错误（权重 6.0）**✅ 已修复**
  - **描述**: 麒麟ARM64系统缺少g++符号链接，导致cmake无法找到C++编译器
  - **原因**: 系统已安装g++-9但未创建/usr/bin/g++符号链接
  - **解决**: `sudo ln -sf /usr/bin/g++-9 /usr/bin/g++`
  - **预防**: 麒麟系统编译前检查 `which g++`，若不存在但 `which g++-9` 存在则创建符号链接

- [2026-08-21 11:50] **问题类型**: 工具执行错误（权重 2.0）**✅ 已修复**
  - **描述**: GCC 9 的 `-Werror=unused-result` 对 `std::system()` 返回值未使用报错
  - **解决**: 将 `std::system(...)` 改为 `int ret = std::system(...); (void)ret;`
  - **预防**: 跨平台代码中 `system()` 调用必须处理返回值

- [2026-08-21 11:50] **问题类型**: 设计缺陷（权重 8.0）
  - **描述**: CN运行时不释放内存（`cn_alloc_tracked` 无GC），78_chain_build编译5大组件时内存累积超过系统限制（25GB虚拟内存）
  - **原因**: CN语言目前没有垃圾回收机制，所有字符串拼接操作不断累积内存
  - **解决**: 将78_chain_build加入PLATFORM_SKIP（平台资源限制，非代码bug）
  - **预防**: 大规模自举用例在内存受限平台上需注意OOM问题

- [2026-08-24 16:44] **问题类型**: 设计缺陷（权重 12.0）
  - **描述**: arena统一分配方案破坏RAII--cn_free变空操作导致向量扩容旧数据不释放，78_chain_build编译5大组件时内存累积12.3GB
  - **原因**: 试图用arena替代所有malloc/realloc，但arena模式下cn_free是空操作，C++ vector/Rust Vec式的正常RAII内存管理被破坏。向量每次扩容（4->8->16->...->N）旧数据不释放，内存累积约2N
  - **解决**: 回退arena方案，改用tracked注册表--cn_alloc/cn_realloc/cn_free保持std::malloc/realloc/free（正常RAII），cn_alloc_tracked注册到链表供内存::释放全部()批量释放
  - **预防**: 学习C++/Rust成功经验：容器用std::malloc/realloc/free保持正常RAII，批量释放仅针对无RAII的原始类型（字符串）。权重12.0

- [2026-08-24 16:44] **问题类型**: 逻辑错误（权重 10.0）
  - **描述**: `__cn_alloc_reset()` 头文件声明、IR层映射（内存::释放全部 -> __cn_alloc_reset），但运行时.cpp中未实现，导致78_chain_build OOM
  - **原因**: 头文件runtime.hpp第42行声明了`__cn_alloc_reset()`，ir_call.cpp第257行映射了`内存::释放全部` -> `__cn_alloc_reset`，但io_api.cpp中没有实现该函数。链接时可能被当作空操作，内存从未被释放
  - **解决**: 在io_api.cpp中实现`__cn_alloc_reset()`，遍历tracked链表逐个std::free
  - **预防**: 新增API必须同时实现声明和定义。权重10.0

- [2026-08-24 16:44] **问题类型**: 逻辑错误（权重 8.0）
  - **描述**: tracked注册表方案中`cn_realloc_tracked`的链表更新可能有bug--`std::realloc`内存复用导致链表中出现重复节点，`__cn_alloc_reset`释放时double free段错误
  - **原因**: `cn_realloc_tracked`扩容时调用`trackedUnregister(ptr)`移除旧指针+`trackedRegister(new_p)`注册新指针，但如果`new_p`恰好等于链表中已存在的某个指针（内存复用），会导致重复节点
  - **解决**: 待解决。可考虑`cn_realloc_tracked`不更新链表，改用`cn_alloc_tracked`新块+memcpy+`cn_free_tracked`旧块
  - **预防**: 内存复用场景下的链表更新需特别注意重复节点问题。权重8.0

- [2026-09-01 15:10] **问题类型**: 流程缺陷（权重 12.0）**✅ 已修复（宿主四缺陷）**
  - **描述**: E2E 119 自 P6h 起从未真正通过——P6h 会话在 Linux 环境开发（无法运行 win-x64 的 v2p），HANDOFF 写「CI 全量门禁真实执行 119」仅为预期而非结果，后续会话误信「E2E 122 全绿」基线。实际 v2p 编译自身组件链在 4 个叠加宿主缺陷上接连崩溃（无初始化器类静态双模型不一致 / 分支未执行类局部被无条件析构 / 块顶中途返回被无视 / stdlib 漏导入错误码）。
  - **解决**: 逐层最小复现定位（c1~c18），四缺陷全部根治于 src/cn_compiler + stdlib，E2E 119 本地真实通过；新增 120/121/122 回归。
  - **预防**: 「无法在本地运行的验证」不得写入门禁结论——HANDOFF 必须区分「已验证」与「预期 CI 验证」；接手会话须先重跑关键门禁用例确认基线真实，再开始新任务。权重 12.0

- [2026-09-01 15:10] **问题类型**: 调试方法论（权重 6.0）
  - **描述**: 二分定位中两次得到错误结论——①构建命令输出重定向到 /dev/null 后未检查退出码，构建失败时测的是**旧二进制**；②新鲜进程的栈是零页，「分支未执行局部析构野指针」在浅调用栈下碰巧表现为空指针被空安全跳过而「通过」，叠加前序调用污染栈后才必崩。
  - **解决**: 每次重建前删除旧产物（rm 后构建并显示尾部输出）；崩溃类缺陷的最小复现必须叠加「前序调用污染栈」场景（如先跑一轮容器操作再触发未执行分支）再下结论。
  - **预防**: 二分脚本三要素：删旧产物 + 显示构建结果 + 退出码断言；「恰好通过」的复现要追问为什么——侥幸通过 ≠ 修复。权重 6.0

- [2026-09-01 15:10] **问题类型**: 工具使用（权重 3.0）
  - **描述**: bash heredoc 内 python 字符串的 `\n` 转义两次被吞（CN 源码里出现真实换行破坏字符串字面量），且 git stash push 部分文件后 stash pop 顺序易错。
  - **解决**: CN 源码类文本改用 Edit 工具直写；临时验证用例（target/dbg/）用 cat heredoc 纯文本（不含转义序列）生成。
  - **预防**: 含反斜杠转义的内容一律不走 heredoc+python 拼接。权重 3.0

- [2026-09-02 11:30] **问题类型: 工具链差异（权重 10.0）** —— v2 ARM64 GAS 后端落地四坑（新后端首验实录）
  - **描述**: v2 新增 GAS 后端（代码生成ARM64.cn）首日连踩四个平台差异坑：
    ① GAS 符号名合法字符仅 字母/数字/._$——MASM 修饰名（?XX@@Y）直接搬用即报「expected symbol name」（@ 还是 GAS 注释符）；
    ② GAS 无 MASM 的 PROC 标签作用域——各函数 bb 标签从各自块计数 0 起，跨函数重名即「symbol already defined」（MASM 靠 PROC 作用域侥幸成立，属隐性平台依赖）；
    ③ 分割参数串("") 会产出 1 个空段（边界含 i==长度）——符号修饰对空参数串不判空即拼出假 _T 后缀，链接 undefined reference（符号看似存在实则错名，定位绕远）；
    ④ POSIX 退出码 8 位截断——E2E 期望值 21333 在 Linux 实测得 85（=21333%256），Windows 退出码 32 位无此问题（123 用例「看似算错」实为正确）。
  - **解决**: ① 平台各自 nameMangle 同签名体系（GAS：_+hex+参数段_类型码，类型编码表与 MASM 一致）；② 块标签统一 bb<函数序号>_<块ID>（共用层策略，双后端一致，消除 PROC 依赖）；③ 空参数串不拼参数段（调用方判空）；④ 运行器 linux 分支比对 预期值%256 并注释说明。
  - **预防**: 跨后端/跨平台迁移时逐项核对：符号合法字符集、标签作用域模型、字符串工具边界条件、进程退出码位宽；符号类 undefined 优先 hex 解码比对两侧实际符号名（nm 对拍），勿凭肉眼。权重 10.0

- [2026-09-02 19:40] **问题类型: ABI 对齐方法论（权重 10.0）** —— v2 对宿主 ABI 的三处误判与「asm 对拍」根治法（②c A/B 实录）
  - **描述**: v2 接宿主编译 stdlib 符号时对返回/元素 ABI 连判错三次，全部表现为链接符号错位或运行时段错误：
    ① MASM 逻辑非 `cmp [rbp-x],0`——内存操作数+立即数无法推断尺寸（A2070×22 处），GAS 侧寄存器形态合法故 Linux 验收从未暴露；
    ② 容器/类元素返回形态误入 sret（隐藏返回指针）——宿主实测 `元素 : ptr`（rax 返回元素指针、链式 .设置 this=rax 直传，E2E 118 asm 双实锤）；含容器字段的结构体以类形态供给（H8-⑤ 契约）后同按类元素指针步长存储，v2 仍按纯结构体 sret 处理即 this/实参错位；
    ③ $ 扁平合成名实参再切分用朴素 '$' split 且词扫描不含数字——「整64」被切成「整」、嵌套实参截断，方法符号（#参数串）损坏成 undefined。
  - **解决**: ① 发射前自问「每个内存操作数尺寸可推断吗」，MASM 侧先装载寄存器再比较；② **ABI 疑问一律宿主编译最小同形源码看 asm**（宿主产物 asm 是唯一权威——函数注释 `: ptr`/`: 结构体名` 直接给出返回分类，调用方序列给出寄存器约定），不靠推理；③ 扁平名切分按基名元数递归消费（词字节含 128+ / A-Z / a-z / 0-9 / _）。
  - **预防**: v2 与宿主交互的每个 ABI 决策（返回形态/参数传递/元素步长）落码前先在宿主 asm 里找到对应证据；「双平台一绿一红」的缺陷优先怀疑平台差异（指令合法性/代码页/符号集），不是逻辑错误。权重 10.0

- [2026-09-02 19:40] **问题类型: 架构潜伏缺陷（权重 9.0）** —— 解析期按名全局表的跨函数同名异型串类型（③ 自举必踩）
  - **描述**: v2 的 变量容器表/变量结构体表 在解析期按名登记、任务级全局生命周期——不同函数对同名标识符声明不同类型（追加表 的参数 `t:向量<整64>&` 与 主 的局部 `t:桶`）时后写覆盖/互相污染，IR 层按名字查到**别的函数的类型**（本例：桶.t 被解析成向量，链式 .表.大小 对象类型推导失败；更险形态是解析到「恰好存在的错误类型」静默错码）。
  - **解决**: 新增函数级变量类型权威表 当前函数变量类型（生成函数 按当前函数填充全部参数/容器/结果/结构体局部），标识符类型解析先查此表、解析期全局表退为顶层静态/兜底；类型推导统一入口 解析节点类型ID 递归覆盖 标识符/成员/调用 三形态。
  - **预防**: 任何「按名键、跨作用域共用」的表都要问：同名不同绑定（跨函数/跨块遮蔽）时谁赢？解析期便利表必须让位于按作用域/按函数的权威表；新特性（引用参数）引入新登记点时同步审视全部旧登记路径。权重 9.0

- [2026-09-02 19:40] **问题类型: 测试基建（权重 7.0）** —— 平台敏感期望行与中文产物路径（立项跨平台即红）
  - **描述**: ① v2 闭环用例期望文件若直抄某平台实际输出（`目标平台: linux-arm64`/`asm 行数=1971`/`v2asm.s`），换平台必败——行数与产物名天然平台相关；② E2E 供给产物目录用中文名（供给126_供给），路径写入 rsp 后 link.exe 按系统代码页（GBK）误读 UTF-8 → LNK1181 乱码。
  - **解决**: 期望文件一律 win 口径（数值列置空），平台适配在运行器分支做（Linux 分支已有 .s 归一）；一切传给 ml64/link/as/g++ 的路径（目录/产物名）一律 ASCII。
  - **预防**: 新增跨平台用例时对照 119-125 惯例三查：期望文件平台敏感行、产物路径字符集、退出码位宽；「在 A 机写用例」默认假设 B 机也要跑。权重 7.0

- [2026-09-03 21:30] **问题类型: 静默降级缺陷家族（权重 9.0）** —— objSrcType 对象形态分支表的第三例静默常量 0（v2.1 统一 . 连带揪出）
  - **描述**: ir_expr.cpp 成员读取的 objSrcType 按对象节点形态枚举（标识符/嵌套成员/下标/调用/isDeref二元）——形态落空即 objSrcType 空 → decl==nullptr → 防御分支**静默降级常量 0**（比较恒真/打印恒 0，无任何诊断）。本轮统一 . 后新用例 134 当场揪出两处：① 嵌套链第一层为指针（`外层* wq; wq.内.x`——嵌套分支 findStruct("外层*") 不剥指针）；② 显式解引用对象（`(*q).x`——分支表不认 UnaryExpr）。旧 `->` 语法同形态同缺陷，既有用例嵌套链均隔下标层走 IndexExpr 分支侥幸未踩。
  - **解决**: ① lookupSrcType 后 isPointer → pointeeOf 剥一级再查；② 补 UnaryExpr(Deref) 分支（类型=所指）。根治关键=新用例锚定写回全形态矩阵（读/写/自增/自减/复合赋值/嵌套/解引用等价），单形态用例测不出链路断点。
  - **预防**: 同族三例（BUG10b、2026-08-25 结果/可选 .值、本次两处）——**凡「按形态枚举对象」的分支表，接入新对象形态（解引用/指针/链式）时全表核查**；防御分支静默降级常量 0 应改为可见化（stderr/诊断）或至少单测锚定。另：方法调用路径（visitCallExpr 只查 object）**不经过 visitMemberExpr**——IR 层依赖语义层 AST 标记的分支须纯类型驱动（VirtualCallDispatch 首败实证），标记位只在表达式完整访问路径可靠。权重 9.0

- [2026-09-03 21:30] **问题类型: 语法迁移工程方法（权重 6.0）** —— 全量迁移的扫描漏形态与行号改写误覆盖（-> 废除实录）
  - **描述**: 废除 `->` 成员访问的全仓迁移两踩：① 首轮按 `[\w]->[\w]` 正则扫源码，漏掉 `)`/`]` 前缀形态（`(名单 + (n-1))->分数`）——E2E 编译失败才暴露；② python 按行号批量改写未先 assert 目标行内容，07 用例把注释行下方的**声明行**（`点* 指针 = &p`）误覆盖为注释，跑挂才发现（行号偏移一位）。
  - **解决**: ① 迁移扫描用「**去字符串/去注释后仍含 `->`**」宽口径复核（返回类型箭头按声明位特征排除），人工分类剩余；② 行号定位改写一律先 assert 目标行含预期特征串再改（本轮 09 有 assert 一次通过、07 无 assert 翻车，正反对照实证）。
  - **预防**: 全仓语法类迁移收尾必跑宽口径复扫（零残留断言）；按行号写文件 = 无校验的盲改，assert 行内容是最低成本防线。权重 6.0

- [2026-09-03 22:40] **问题类型: 文档双副本同步（权重 5.0）** —— cn-language-spec .zcode/.agents 双副本三向分叉收口（用户裁决实施）
  - **描述**: cn-language-spec 技能同时存在于 .zcode/skills/（ZCode 加载）与 .agents/skills/（旧 agent 体系）两处——历史演进各改一侧形成**三向分叉**：02（.zcode 独有字符串驻留条目，2026-08-25）、04（.agents 独有 P3-18 引用返回/P3-23 方法作值，.zcode 侧还留着已被推翻的「引用返回编译报错」旧文案）、06（.agents 独有 P3-19 接口分派三行）。同一规范两处异版=AI 按加载侧不同得出不同答案。
  - **解决**: 逐文件以 plans/001+实现现状（E2E）判权威方向定向 cp（02 用 .zcode、04/06 用 .agents——**不可整目录单向覆盖**），diff -rq 零差异收口；全 skills 树排查确认仅此一处双副本。
  - **预防**: 修改 cn-language-spec 任一副本时必须同步另一副本（提交前 `diff -rq .zcode/skills/cn-language-spec/ .agents/skills/cn-language-spec/` 须零输出）；两副本出现差异时先逐文件对 plans/001 判方向，禁止默认单侧权威。权重 5.0


## 高权重问题（plans/016 linux-x86_64 后端专项：六枚汇编/ABI 缺陷三层实证根治，2026-09-05 深度系统 x86_64 本机）

**权重：10.5（新后端首发轮——六枚缺陷同根链：MASM 直译≠x86_64 汇编器语义 + 32 位类型的 64 位运算位模式坑）**

### 事件
plans/016 linux-x86_64 后端（GAS Intel 语法 + SysV ABI）首轮实施，E2E 全量跑出六枚缺陷，
汇编器报错（imul/movss）/运行时结果错（负数枚举/除法/retbuf 悬垂）/gdb rip=0（push-pop 配对）三层症状齐全。
全部当场根治，最终门禁全绿（单测 1238/1238 + E2E 127 过/0 败/27 跳过）。

### 教训（预防要点）
1. **MASM 直译≠x86_64 汇编器语义，逐形态过 as**：x86_64 无 `imul r64,r64,r64` 三寄存器形式
   （仅立即数三操作数）——乘法一律双操作数 `imul dst, src`；`movss` 配 `qword ptr` 报
   operand size mismatch——宽度前缀（byte/word/dword/qword）必须与助记符宽度精确匹配。
   **新指令形态发射后立即本机 as 验证**（本机原生闭环是 x86_64 后端的独有优势，勿攒批验证）。
2. **32 位类型的 64 位运算「看似可行」暗藏位模式坑（比较/除法双双实证）**：
   i32 变量装载 `mov r10d`（清高32=零扩展位模式）与 64 位常量 `mov r9, -1`（全1）在 64 位
   `cmp` 下位模式不一致——负数枚举 `-1 == -1` 误判不等（选择语句落默认分支）；i32 负数
   `cqo+idiv r64` 按正数除（-8/2 商 2147483644）。**宽度语义跟随源类型**：非宽类型
   `cmp r10d, r9d` + `cdq+idiv r9d`（ARM64 w9/w10 策略的本质原因——此前只知其形不知其因）。
3. **push/pop 严格逆序配对**：push rbx 先、push rbp 后（rbp 紧邻返回地址），epilogue
   `mov rsp,rbp / pop rbp / pop rbx / ret`——顺序颠倒时 rbp 弹到返回地址、ret 读垃圾
   （gdb 实证 rip=0 + 栈顶 3/4 相邻位模式锁定参数槽写入区）。**写 prologue 时同步写 epilogue
   并配对审计**（gdb batch + dmesg segfault ip/sp 三分钟定位，勿肉眼对汇编猜）。
4. **跨调用持久的缓冲区必须帧内固定区（rbp 相对）**：emitCall 的 rsp 临时区 `add rsp` 后
   悬垂——win x64 2026-08 同款修复（retbufFrameOffset_）先查已有平台实现再动手（本轮
   该坑因未先读 win 的 emitCall 全文而重踩，gdb 两轮才收敛；**跨平台后端先行通读兄弟平台
   同名函数全文是零成本预防**）。
5. **IR 层调用契约先 dump 实证再写调用方**：结构体返回调用（形态 A）由 IR 层预插
   retbuf 地址为 operands[0]、result.type=void——`cn ir` dump + ARM64 双平台汇编对照
   锁定「原样传递自然落位0」的正确语义（arm64 版注释/单位机全量绿双锚定）；凭 win 注释
   （第4路 calleeReturnsStruct）推测会做出 argOffset=1 的错位实现。
6. **终端肉眼验证输出会被 echo 拼接误导**（换行缺失不可见，"1407参数"实为 140 无换行接
   echo 文本）——E2E 逐行比对是唯一可信验收，探针期就应走 expected 比对而非肉眼。

### 关联
plans/016 第七节（实施结果+八项差异清单）；形态 A 契约与 win 第4路差异的呈报见 HANDOFF「下一步2」。

## 高权重问题（plans/017 第18枚：结构体整体赋值单槽浅拷——v2self 引用槽登记全 miss 原发点，2026-09-06 第二十四轮 修复 ✅）

**现象**：v2self（v2 编译的 v2）编译 mapref2 产物运行时 链查找 this=0（错误码3）——
漏发 op=44 引用双层解引用；v2p 同源全绿=编译器分叉。**权重 10**。

1. **结构体整体赋值（标识符左值 `甲 = 乙`）在 v2 只发 IR_加载/IR_存储 单槽 8 字节浅拷**——
   24B 节点只拷首槽（类型+文本），附加@8 等后续字段原值残留；宿主为整结构体 rep movsb。
   声明位三形态（调用返回/标识符拷贝/成员字段）已有 IR_结构体拷贝，**唯赋值语句路径漏**；
   「结构体整体初始化暂不支持」诊断只挡了声明位，赋值位静默浅拷=错误产物。**修复**：
   赋值分支先查 解析标识符类型ID→查结构体大小!=0 → IR_结构体拷贝(大小) 整体拷贝，
   引用槽分流装载（与 生成左值地址 同款）；非同型/非标识符/槽缺失 IR层诊断 fail fast。
   **预防**：语言中每个「按值语义」的形态（声明/赋值/传参/返回）必须逐一枚举审计——
   修一处时同步 grep 其余同语义位点，诊断挡住的位置≠相邻位置已安全。
2. **静默浅拷的传导链极长（本枚跨三层才显形）**：参数收集浅拷→引用判定恒假→登记缺失→
   漏发解引用→运行时 this=0——「值看似正确」的残缺拷贝比崩溃更危险（下游距原发点三层）。
   插桩对拍 [d17g]（赋值后左值 vs 右值并列打印）是拷贝类缺陷的最快收口形态。
3. **对拍脚本两个假 PASS 陷阱**：①v2 的 asm 输出 target/v2asm.s **相对 cwd**——必须在
   项目根跑+编译后即时 cp（曾取到上一探针旧产物=全员假 PASS）；②容器系探针链接需宿主
   同源 ${p}_h_nom.o 补容器方法实现（v2p_rt_nommain.o 不含用例专属实例化）。
4. **宿主解析器 `结果` 不可作变量名**（与 结果<T,E> 泛型保留冲突）——用例命名避开。

## 中权重问题（R3 函数拆分轮踩坑三条，2026-09-08 第三十五轮 单位机ARM64）

1. **脚本切块锚行号漂移**：用固定行号锚定切函数段，文件被前一步 Edit 改动后行号即漂移→
   锚断言失败→脚本退出（文件未写入无损，但反复浪费时间；本轮 编译文件 拆分三次锚失败）。
   **预防**：切块脚本先用 grep 文本锚动态定位（`next(i for ... if 条件 in 行)`），禁止固定
   行号索引；或每次切块前重新 grep 确认行号再写脚本。
2. **手写 Edit 占位符笔误**：重构时留 `池0占位()`/`新行占位 = 0;` 等占位符忘记替换→
   编译错误（本轮两处：生成短路求值 漏 池 参数、发射成员取值 指令0占位）。**预防**：
   占位符用独特大写标记（如 TODO_REPLACE_xxx），提交前 `grep -rn '占位'` 清查。
3. **换机先验架构再跑门禁**：单位机 麒麟 ARM64 上误跑 `--target linux-x86_64` 全量——
   x86 汇编落 arm64 as 全错（`.intel_syntax` unknown pseudo-op 洪流）。**预防**：跨机轮
   第一步 `uname -m` 确认架构，门禁目标平台随机型（家机 win/单位机 arm64/深度机 x86_64）。

## 类型系统严格化轮踩坑（2026-09-10 第五十一轮 深度系统 x86_64）

1. **检查表达式的诊断副作用非幂等——二次递归=重复诊断假阳性**：v2 检查表达式/
   检查标识符表达式 每次调用都会向诊断数组追加（未声明等），对同一节点二次递归
   即双报。本轮混合符号检查最初把二元分支原有的带诊断操作数递归扩到算术位符，
   174 负测合集「未声明标识符: x」立即 14→15（x 同时被语句根线性遍历+新增递归
   各查一次）。**修复**：新增检查的推导一律走 表达式类型ID 推导族+静默诊断缓冲
   （局部向量吞弃，语句根线性遍历才是权威诊断点），且不动既有带诊断递归（其双报
   形态已被 174 expected 固化=行为契约）。**预防**：给带副作用的检查函数新增调用
   点时，必须区分「要诊断」与「只要类型值」两种用途；负测合集类 expected 固化了
   诊断计数=行为契约，扩检查范围后必须先跑全量 E2E 看计数漂移（本轮 174 即在此
   揪出）。**权重 6**。
2. **IR 指令字段承载双重语义的隐患（类型转换 指令.类型=目标码 同时充当发射层
   读取方式选择器）**：目标码 T_整64 落 movsxd dword 兜底——对整32 源正确（符号
   扩展）、对正32 源错误（应零扩展）、对 64 位源错误（应全读直拷）；发射层「读法」
   本质取决于**源类型**而非目标类型。**修复**：IR 层按源寄存器登记类型改标指令
   类型字段（正32→T_正32 零扩展读/64 位源→T_正64 直拷），原目标码另存供产物
   登记。**预防**：新增指令字段语义时单一职责；「按 X 分派的 switch」新增码值时
   旁路穷尽审计所有落兜底的形态（正64→整64 截断即兜底吞掉的错形态，193_v2 探针
   实证才显形——纯目标侧测试抓不到，必须源类型×目标类型矩阵探针）。**权重 6**。

## 68-a 修复轮验证踩坑（2026-09-11 第六十九轮 单位机麒麟 ARM64）

1. **分钟级时间戳掩盖同分钟内重编——缓存/增量逻辑验证假阴性**：验证 rt objs
   指纹缓存「touch 后应触发重编」时，`ls -la` 默认分钟级显示（14:53）与重编
   完成时刻（14:53:3x）同分钟，「obj 时间不变」假象一度误判为「未触发重编」；
   且缓存命中分支有 verbose 输出、重编分支无输出，grep 计数 0 同样支持误判。
   **修正**：改用秒级取证——`ls --time-style=+%T`/full-iso 对比精确秒，并以
   「追加注释→还原内容」的确定性内容变更替代 touch（还原后键不回旧值=mtime
   已变，mtime/size 指纹固有特性，宁可伪重编不漏重编），键值+时间戳双证据
   三态闭环（命中/变更/还原）。**预防**：验证缓存/增量类逻辑（指纹、mtime、
   构建缓存）必须用秒级时间戳或指纹值对比，禁止分钟级 ls 下结论；两个分支
   的可观测输出不对称（一边有 print 一边没有）时，grep 无输出不能作为「未
   走该分支」的证据。**权重 6**。


## 76-a 移除路径所有权轮踩坑（2026-09-12 第七十六轮 深度机 x86_64）

1. **浅拷赋值模型下「移位槽/末尾移出槽」是重复引用——释放即悬垂（首版修复实证）**：
   为补「向量.删除 被移除元素串泄漏」，首版在 stdlib 删除路径给 `析构元素(位置)` +
   循环内/末尾的移位槽析构都注入字符串释放——实测元素内容立即损坏（探针 76-V1：
   析构末尾移出槽释放了仍被有效槽持有的同一句柄）。根因：**字符串下标赋值=浅拷
   句柄**（探针实证无新分配），`数据[移动] = 数据[移动+1]` 后源槽与目标槽共享句柄、
   末尾移出槽亦与有效槽共享——「移位目标槽/末尾移出槽」**不是**唯一持有者。
   **修复**：挂点按「槽的持有者状态」分工——`析构元素`（有析构类：移位槽旧对象；
   字符串=不注入）+ 新增 `析构被移除`（字符串：唯一持有者槽=被移除槽，注入单槽
   释放；有析构类=不注入）。**预防**：设计释放点时必须先回答「这个槽的句柄还有
   没有别的持有者」——浅拷模型下有且仅当「槽是句柄的最后持有者」才可释放；
   新增释放注入后必须用「删除后元素内容校验 + 搅动」探针验证（仅靠残留计数
   会漏掉悬垂——双释/悬垂的净计数可能恰好为 0）。**权重 8**。
2. **「容器拥有元素」与「借出视图」的交互边：先读后删 = UAF（v1 组件实证）**：
   `元素(i)`/`栈顶()` 返回**容器内借出视图**（浅拷句柄，不复制）；容器拥有元素
   模型下「删除/弹出/清空」会**释放**该句柄——借出视图随即悬垂。v1 组件
   （CN语言编译器/IR生成/IR4.cn）的「先 `类型栈顶` 再 `类型弹出`」模式即此形态：
   74/75/76 自举链用例出现 IR dump 乱码（`运算|*`→乱码）+ 产物行数漂移（-406 行）。
   Rust 对照：借用检查器直接拒绝该模式（借用存活期覆盖可变操作）；CN 现阶段无
   借用检查 → 用**显式复制**（`字符串复制(栈顶())`）取得拥有副本再弹出。**预防**：
   「借出视图」类接口（元素/读取/栈顶）的返回值，凡要跨「容器修改点」存活，必须
   先复制；全树审计问句「这个借出视图在它的容器被修改之前还用不用？」。借用检查/
   NLL 登记为后续靶子（plans/020 矩阵新增格）。**权重 8**。
3. **stdlib 语义改动的连带面：v1 自举链用例的参照产物与语义绑定**：本轮改 stdlib
   所有权语义后，74/75/76（v1 组件 IR/代码生成自举链）集体失败——两者叠加：
   ①上面第 2 条的借出悬垂（真缺陷）；②产物形状漂移（新增指令改变 ASM 行数期望）。
   **预防**：改 stdlib 任何语义/结构后，必须复跑自举链与产物锚定类用例（74~77），
   并用**基线对照实验**（`git stash` + 重编译 + 复跑）区分「既有失败 vs 本轮回归」
   ——本轮即靠它确认 74/75/76 是回归而非既有（HANDOFF 的「唯一败=194」口径不含它们）。
   **权重 7**。
4. **挂点命名必须承载「槽的状态」语义**：`析构元素`（移位槽：重复引用风险）与
   `析构被移除`（唯一持有者槽）若同名或命名含糊，注入分派就会写错方向（双重析构
   或悬垂，且都有静默形态）。**预防**：引入注入挂点时，方法名直接编码语义
   （被移除/移位槽/唯一持有者），并在宿主分派处用布尔变量显式命名
   （`strOwnedSlot`）而非靠方法名列表隐式表达。**权重 6**。

## 194 修复与 v2 解引用类型推导分叉根治（2026-09-12 第七十六轮后续 深度机 x86_64）

1. **类型推导里的「暂」字兜底会系统性弱化所有下游检查面**：v2 `检查表达式` 对
   解引用恒返回 `整64`（注释自称「暂标量」）——一切经 `表达式类型ID` 的检查
   （混合符号赋值/二元运算等）对解引用右值都拿到 整64（有符号），而真实指向
   类型是 正8（无符号）→ 混合符号漏检（实证：同源码 `返回 *q;` 宿主报错 /
   v2p 编译成功）。修复=复用现成「指针变量表」（`查语义指针变量`）取真实指向
   类型——**该机制在指针下标 `p[i]` 路径早已在用**（语义检查.cn:158），解引用
   分支只是漏用。**预防**：①代码里任何「暂 X」标注必须写清影响面并列入专项
   审计（「暂」=已知不精确=潜在漏检源）；②新增类型推导分支时对照同族已有实现
   （p[i] 正确 → 解引用应同款），同构机制一处漏用即分叉温床。**权重 7**。
2. **检查面分叉的发现依赖「同源码双编译对照」，单侧用例长期共存不等于等价**：
   194（宿主）与 194_v2（v2 闭环）是两个独立用例、各自只测「能编译且输出对」，
   解引用形态的漏检在两侧共存多轮未被发现；本轮用三行最小源两侧各编一次即
   暴露（`v2gap2.cn`：宿主 1 错 / v2p 成功）。**预防**：语义检查（拒绝面）应在
   两侧**配对建设负测**（195/195_v2 + 本轮 222/222_v2 模式：同一源码、宿主
   `期望编译失败.txt` + v2 `退出码=负`+诊断锚行）；「两侧同源用例」的期望若只
   断言正向输出，检查面差异不在其覆盖内。**权重 6**。
3. **已验收用例的「修复」必须是用户批准的显式动作，且两侧同步改**：194/194_v2
   的写法（`返回 *q;`）在方案A（Rust 对齐）落地后不再合法，但 AGENTS.md §4
   「测试文件不可擅自修改（最高优先级）」使其保持失败多轮（既有待批）；本轮
   经用户批准后修改——改动最小（仅违规行加显式转换）+ 留注释说明原因与日期，
   两侧同源用例同步改（194 与 194_v2），并新建 222/222_v2 配对负测把行为差异
   变成门禁。**预防**：修复既有失败用例时，一次动作做完四件：改用例（留痕）+
   两侧同步 + 补对应负测 + 文档标注「已修复」（防后续 AI 误判未修复）。**权重 6**。

---

## 77-a 借出视图生命周期检查轮（2026-09-12 第七十七轮 深度机 x86_64）

- [2026-09-12 第七十七轮] **问题类型**: 编译期安全新增（plans/020 矩阵 A21 格靶子「借出视图 × 容器移除」，NLL/借用检查轮），**✅ 当轮销项（用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案——性能第一、安全第二」）**。**权重 8**。

  **一、缺陷实证（探针 77/77-2，宿主侧，/tmp/p77probe 不入库）**

  借出视图 = 字符串元素容器的 `元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取` 返回值
  （容器内元素句柄的**浅拷**——容器拥有元素模型下与容器槽共享所有权）。实测内容损坏（校验位 0）：

  | 形态 | 失效机制 |
  |---|---|
  | 向量 元素 × 删除 | 被移除槽释放（76-a 的 `析构被移除` 注入） |
  | 向量 读取(结果) × 设置覆盖 | 唯一持有者槽释放 |
  | 栈顶 × 弹出 | 所有权移交调用方（借出视图与调用方**双持有**同一句柄） |
  | 队首 × 出队 | 同上 |
  | 链表 头部元素 × 删除头部 | 被摘槽释放 |
  | 映射 获取 × 删除 | 交换式删除释放被删项值串 |
  | **跨作用域（容器先亡）** | 块内容器析构释放元素，外层借出视图仍用（形态G 校验位 0） |

  对照（正确行为，证明检查器不该拦）：借出变量在块出口**未被**误判拥有（校验位 1，编译器既有污染机制正确）；
  借出→使用→容器修改（顺序正确）校验位 1。

  **二、方案裁决（三方案；性能第一、安全第二，Rust 经验）**

  | 方案 | 性能 | 安全 | 裁决 |
  |---|---|---|---|
  | **甲：编译期借出视图生命周期检查**（语义层来源登记 + NLL 顺序近似：活跃区间=绑定→最后一次使用） | **零运行时开销**（生成物零变化；编译期 O(函数节点数)） | 编译期拒绝，Rust 借用检查器 E0502/E0597 语义同构 | ✅ 实施 |
  | 乙：借出方法返回拥有副本（API 级） | 每次借出堆分配+拷贝（218/220 搅动形态显著变慢）；破坏容器视图语义（写回不可行） | 高 | ✗ 违背性能第一 |
  | 丙：视图类型（指针+世代）+ 运行时检查 | 8→16 字节新类型（ABI/全链改动巨大）+ 每次访问比较 | 最高（含动态跨函数） | ✗ 违背性能第一 + 改动面失控（登记为阶段 4' 潜在扩展） |

  **三、两侧落地与防线**

  - 宿主：新文件 `semantic/semantic_borrowview.cpp` + 六处挂点（`visitCallExpr` 方法分支 /
    `visitVarDecl` 初始化位 / `visitAssignmentExpr` 赋值位 / `visitIdentifierExpr` 读值根拦截点 /
    `checkFunctionBody` + `checkClassMethods` 结算）+ 变量身份 ID（同名遮蔽防误配）+
    `types::isStringElemContainer/isStringValuedMap` 上提共享（IR 层改委托 = 单一事实源）。
  - v2：新模块 `语义/借出检查.cn`（预扫描块区间+声明表 → 主扫描三类点 → 结算两形态；
    **节点索引序代替源码行序**——v2 节点无行号）+ `包.cn` 注册 + `检查函数` 尾部挂点。
  - 防线：E2E **223/223_v2**（六形态负测）、**224/224_v2**（容器先亡三形态负测）、
    **225**（合法九形态正测 511）、**226_双编译对照借出视图合法形态**（移植纪律 9 第四个，两侧 255 一致）
    + 单测 **13/13**。
  - **反证实证**：宿主注入「借出方法识别恒假」→ 223 失败；v2 注入「是借出方法名 恒假」→ 223_v2 失败
    （两侧均恢复并复验，残留检测=0）。

  **四、预防（本轮新证）**

  - **㉖ NLL「活跃区间=到最后一次使用」是零误报的关键，保守到作用域末=全树误报**：本轮落地前
    先做作用域感知全树审计（v2/v1/stdlib/tests 零存量违规）；审计脚本本身经三轮修正才可信
    （函数边界隔离消同名遮蔽误报、AST 节点身份判定消包裹形态误报）。**判据设计与误报面必须
    一起实证**，尤其「把既有全树当语料」是唯一的误报率检验。
  - **㉗ 包裹形态必须按 AST 节点身份判定，不能靠「最近一次是借出」标记**：
    `字符串复制(表.元素(0))` 的内层借出调用会把标记残留到外层（宿主初版误报实证），
    必须要求「顶层表达式**就是**该借出调用」（宿主=节点指针比对；v2=后缀序末节点解剖）。
  - **㉘ v2 侧判定的字段语义先小样实测再写**：赋值节点 `文本`=左值名ID、`子起点`=右值区间起点
    （初版按 `子起点` 取左值节点 → 跨作用域形态**漏检**；`返回 假` 类注入实测定位后三形态全拦截）。
    与「v2 结构性约束」同族：**先实测字段语义，再写判定**，不要凭注释推断。
  - **㉙ 「新增防线」与「修复缺陷」的验证口径不同**：新增检查器以「全树零存量违规 + 零迁移成本」
    为目标（本轮达成）；同时必须配**正测**（合法形态不得误报——225/226），否则检查器的
    误报会静默收窄语言可用面。**负测证明能拦，正测证明不多拦，两者缺一不可**。

- [2026-09-12 第七十七轮后续（扩展①②）] **操作坑**：**验证链脚本一律串行**——`cmp29_71.sh`（组件对拍）与 `run_chain71.sh`（锚定链）共享 `target/p71x64l/work/`（p71.s/s71.s/cn_self71），后台并发跑会互相踩出 **16/30 DIFF 假警**（当时误判为「扩展引入 v2 分叉」，串行复跑 30/30 一致）。**预防**：同一 work 目录的验证脚本禁止并发；后台任务完成通知后先确认其内部脚本已全部结束再手工复跑。**权重 4**。

---

## 78-a v2 用户自定义类支持轮（2026-09-12 第七十八轮 深度机 x86_64）

- [2026-09-12 第七十八轮] **问题类型**: v2 编译器能力缺口专项（77-a 扩展③遗留「v2 类支持」；探针 D 实证 v2 对用户自定义类整体不支持），**✅ 当轮销项（用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案——性能第一、安全第二」）**。**权重 8**。

  **一、缺口实证（探针 D 复现 + 基线对照）**

  v2p 编译最小类程序（`类 盒子 { 整64 值; 函数 盒子(){值=0;} 函数 取值()->整64{返回 值;} }`）
  报 3 错：`未声明的类型: 盒子` / `赋值目标未声明: 值` / `未声明标识符: 值`——
  根因=v2 语法层**根本不解析 `类` 声明**（AST 节点表只有 节点_结构体，无 节点_类；
  顶层循环 `否则 { 位置++ }` 静默跳过），语义/IR 层随之全缺。
  对照：同结构体程序 v2 全链通过（结构体=值语义支持完备）。

  **二、范围裁决（第一版）**

  支持：类声明（字段/方法/构造/析构/自身）+ 堆对象指针语义 + 方法体字段裸名 +
  借出检查挂载 + RAII（有析构类，含 ~类 体末字段级联）。
  不支持=**显式诊断不静默**：继承/虚函数/接口/泛型类/静态成员/常量成员/运算符重载/
  访问控制（语法层诊断+中止）；类对象拷贝构造/方法返回结构体/结构体类型字段整体赋值
  （IR 层诊断）。

  **三、实施（v2 侧新增 4 文件 + 改 9 文件；宿主零改动）**

  - 新增：`IR/IR类.cn`（类名表/方法签名表/构造参数串/析构位）、`语法/解析类.cn`、
    `语义/语义检查类.cn`（检查方法=第三类函数级作用域单元）、
    `IR/生成类.cn`（生成方法/类对象局部/构造与方法调用发射/析构字段级联注入）。
  - 节点协议：节点_类(40) 子区间=类体；节点_方法(41) 与函数节点同布局
    （子起点=参数起点、子终点=块索引——函数参数串/参数登记全复用）；
    节点_类字段(42) 与方法参数节点区分（类区间内两类节点混居）。
  - 布局：字段登记**完全复用结构体设施**（登记结构体布局/字段类型/字段偏移/容器字段）
    ——字段访问/对象大小/宽度/RAII 全链零新代码。
  - 对象模型：类对象=堆对象指针（同容器）；this=参数位0（方法生成时先登记 this 槽+
    参数槽序们 打头）；方法符号=类$方法[#参数串]、构造=类$类名[#参数串]、
    析构=类$~类名（宿主 classMethodSymbol 同公式）。
  - 代码生成**零改动**（IR_分配对象/IR_删除对象/IR_调用参数化 三指令复用）。

  **四、防线**

  E2E **228_双编译对照类支持**（移植纪律 9 第五个对照用例，六形态折校验和 31 两侧一致）；
  E2E **229/229_v2 类方法体借出视图拒绝**（宿主/v2 同源负测）；**227_v2 期望同步**
  （类缺口销项后两边界均真拦截——从「缺口诊断固化」升级为「双向防线固化」；
  72-a 先例：实施+同步更新用例期望）。
  反证实证：注入「是用户类 恒假」→ 228 失败；注入「借出检查方法体挂载失效」→
  229_v2 失败（两侧已恢复，残留=0）。两侧对照实测 H1~H11（退出码/输出/分级分配计数全一致）。

  **五、预防（本轮新证）**

  - **㉚ 「对象语义」的成员访问必须显式分派「槽里存的是对象还是对象地址」**：v2 结构体=
    值语义（槽内联、基址=取槽地址）；容器/类=指针语义（槽存指针、基址=Load 指针值）。
    类对象成员访问沿用值语义路径 → 把栈地址当对象地址传给字段访问/方法 this →
    **首版段错误实证**（E2E 228 gdb 栈 `向量$字符串$追加`；H1~H3 单类单方法形态未暴露，
    多形态组合才触发）。新增对象类型时先问「这个槽存什么」，成员基址路径要有显式分支
    （v2 修法：重取成员对象基址 加「对象槽是类对象→IR_加载」分支）。
  - **㉛ 编译器级联释放必须注入在「析构体内部」而非「释放调用点」**：宿主
    injectFieldCascadeDestroy 把字段级联注入 ~类 体末（C++ 成员析构语义：析构体先执行、
    再成员逆序析构）。若注入到 RAII 释放点（IR_删除对象 展开处）→ 顺序反了：用户 ~类 体
    访问字段时字段已被释放=UAF。**判据=宿主同款注入位置**（生成方法 体末、默认返回前；
    体尾提前 返回 不覆盖=两侧同边界）。
  - **㉜ 既有检查器的「上下文缺口」用静态上下文+单点回退补**：v2 借出检查的接收者类型
    解析原只认局部声明（查声明类型 声明表）——类方法体内 `表.元素(0)` 的 表=字段 → miss →
    整段检查静默失效（H5 初测 v2 编译成功而宿主拒绝）。修法=「借出检查类名ID」静态上下文
    （检查方法 设置/复位）+ 查声明类型 miss 时回退查本类字段类型——扫描主体零改动。
    **新函数级单元接入既有检查器时，先问「该检查器的哪些解析入口依赖『局部声明』假设」**。
  - **㉝ 用例期望的「缺口固化」在缺口销项时必须同步（并甄别方向）**：227_v2 原期望含
    3 条类支持缺口诊断（未声明的类型/赋值目标未声明/未声明标识符）——缺口销项后全量 E2E
    唯一失败即此用例。**判据=新诊断是否更强/更准**：本例从「未声明的类型: 货架」（解析层面
    失败）升级为两条真拦截（调用点同源互斥 + 成员链 `架.表` 失效），属用例价值提升 →
    同步更新期望（72-a 先例）。反向（新诊断更弱）则必须先查回归。
  - **㉞ v2 新文件首编译的「未声明标识符 'X::设置'（模块 'X' 未加载）」= 函数参数漏写**：
    新函数用 `字段偏移表.设置(...)` 而参数表漏了 `字段偏移表` → 宿主把 `名字.方法`
    误解析为模块限定名 → 报「模块未加载」。**预防**：写函数时先对齐调用点实参表与形参表；
    看到「模块 X 未加载」先查 X 是否本应是局部/参数名（诊断文本与根因错位族）。
  - **㉟ v2 类对象析构复用容器 RAII 的三列核对**（移植纪律 8 应用实例）：类对象析构直接
    复用 容器变量们 名单 + 发射容器析构/块出口析构/循环跳出析构 →「时机/条件/不变量」
    三列与容器逐条一致（有析构才登记=条件；块出口/跳出/函数尾=时机；释放+清零幂等=
    不变量由既有路径承担）→ 移植成本近零。**新资源类型先找「形状相同的既有 RAII 路径」
    再决定是否新开路径**。

- [2026-09-12 第七十八轮] **验证链新口径**：宿主零改动（单测 1306/1306 维持）+ E2E **256 用例 252 过 0 失败**/4 跳 + 锚定链固定点 **301619 行**（上轮 281093；+20526=v2 类支持源码的确定性体现）逐字节一致 + 绑定自检 + 对拍 **34/34** + 自举闭环（v2p 65751 节点 → cn_self71 → 自举产物再编类程序 H1=7/H4=2 实测）+ 释放路径两侧对照（v2 新增挂点 生成类.cn:184/185）。**权重 4**。

## 79-a 聚合拥有型字符串字段 drop glue 轮（2026-09-12 第七十九轮 深度机 x86_64）

- [2026-09-12 第七十九轮] **问题类型**: 语义缺口专项（plans/020 矩阵靶子 #16 装箱字段接收 / #17 结构体字段持串，同族容器元素缺口一并收口），**✅ 当轮销项（用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案——性能第一、安全第二」）**。**权重 8**。
  - **描述**: 按矩阵靶子立案，探针 P1~P4（每形态块出口 `内存::活动分配数()` 差值）实证——**结构体字段持串残留 1（宿主/v2 同）**、**装箱（结果/可选）值字段残留 1（宿主/v2 同）**、数组元素残留 2（宿主；v2 侧数组类型整体缺口：语法层不解析 `数组[N]`）、字段容器接收者残留 2（B35 靶子维持）。另在实施中发现并当轮根治两枚**既有缺陷**：①宿主 `可选<T> 空 = 无;`（NullLiteral 初始化）按 CopyStruct **从地址 0 拷贝=段错误**（探针崩溃实证，gdb `mov r9,[rbp-200(=0)]`）；②v2 `结果<字符串,E>.错误` **载荷类型误取 T**（原实现按成员名不区分 `.值`/`.错误`，一律用首实参类型码）→ 错误码被当字符串分派，**错误码判别静默失效**（探针 P11 三侧对照：宿主 5 / v2 空或垃圾）+ 写入侧联合体偏移按当前载荷单侧定（错误(5) 写偏移 4 而读取偏移 8=读写不一致）。
  - **根因**: **「字符串局部变量=拥有型」裁决（规格书 10.3）只覆盖了「局部槽」一种存放位置**——字符串存入聚合（结构体字段/装箱值字段）后既无归一化（写入位）也无释放（消亡位），是「拥有型字符串 RAII」的**位置形态缺口**（与 74-a 的「入容器位」同族）。更深的根因是**浅拷共享 × 释放面互斥**：加释放面而不改拷贝语义 = 悬垂（`乙=甲` 双端释放）；不改释放面 = 泄漏——**必须三件套同轮落地**（写入位归一化 + 拷贝位深拷 + 消亡位释放），缺一即出 UAF；这正是「拥有权归一化」在不同槽位上的统一模型（74-a 容器元素 / 79-a 聚合字段同一套分级）。
  - **解决**: **方案甲（Rust drop glue 编译期展开）**——①收集（`ownedStrFieldsOf`/`取资源字段们`：串字段偏移 + **条件字段偏移**，递归展开值语义嵌套，环/深度防御）；②写入位归一化（按来源分级：转移=move／拥有返回=接管／字面量=驻留／其余=`__cn_str_copy` 落堆——与 74-a 同款）；③拷贝位深拷（`preFree` 清旧 + memcpy + `postCopy` 字段级复制三件套，**自赋值 no-op 前置**）；④消亡位释放（块出口/跳出 drop-on-jump/函数尾三路径 + **返回移出豁免**——`返回 甲;` 经 AddrOf 反查跳过，所有权移交调用方接管）；⑤**联合体条件释放**（结果/可选 的值/错误 共用联合体偏移：非「正常/有值」分支下值位是错误码整数，无条件 free 即崩——IR 条件分支发射）。两侧同构落地（宿主新增 `ir/ir_fields.cpp`；v2 资源字段族扩容 + 串字段清旧/深拷/整体拷贝三助手 + 条件释放特例）。
  - **成效与防线**: 探针 P5/P6 双侧全形态残留 **1→0**（P6 八形态折校验和 **255 两侧一致**）；循环体 20 万轮残留 **199999→0**。**E2E 230_双编译对照字段所有权**（移植纪律 9 **第六个**对照用例，八形态折 255 两侧一致）。**反证实证**：注入「宿主聚合登记失效」→ 宿主退出码 255→**80**；注入「v2 资源字段登记失效」→ v2 退出码 255→**248**（两侧已恢复，残留=0）。验证链：清场重建零警告 + 单测 **1306/1306** + E2E **257 用例 253 过 0 失败**/4 跳 + 锚定链固定点逐字节一致 + 对拍 + 释放路径两侧对照。
  - **预防**: **㉞「拥有型资源」的裁决要按「存放位置形态」逐格核对，不能只覆盖局部槽**——规格书裁决「字符串局部变量为拥有类型」时，聚合字段/容器元素/静态是三类独立位置，各有独立的写入/拷贝/消亡三路径，任一未落地都是静默泄漏或 UAF；**㉟加释放面之前先问「这个槽位的唯一持有者是谁」**——浅拷共享的槽位必须先归一化（编译器代写深拷）再释放；**「先复制后释放」的顺序与「自赋值 no-op」是深拷的两个必备安全边**（`甲=甲` 时 preFree 会释放待复制的源，memcpy 把已释放句柄拷回 → UAF）；**㊱联合体字段的释放必须带条件**——值/错误同偏移的布局下，非激活分支的值位是整数/垃圾，无条件 `free` 必崩；**㊲修改装箱构造/读取任一侧的字段偏移时同步核对另一侧**（v2 的 `错误(5)` 写偏移 4 而 `.错误` 读偏移 8 = 侧间偏移分叉，症状是「错误码读垃圾」而非崩溃，只能靠三侧对照探针发现）；**㊳二次求值=多余分配泄漏**——归一化助手必须提供「已求值版本」（`normalizeStringValueSource(value)`）供「值已由调用方 genExpr 求值」的路径复用；把「求值 + 分级」捆在一个函数里会让只能复用一半的调用点默默多求值一次（P6/P7 探针实证：字段写入泄漏 1，汇编可见两次 `__cn_str_call` copy）。
  - **诚实边界**: 结构体**按值传参**浅拷（参数=借出视图、被调方不释放=与字符串参数 A2 契约一致，零拷贝；被调方写参数字段的副本不释放=宁漏勿错）；容器元素=含串字段结构体（入容器位标记源为字段污染 + 元素字段释放面未开=靶子，深拷随容器元素专项）；数组元素持串（宿主靶子维持；**v2 数组类型整体缺口**=语法层不解析 `数组[N]`，独立立项）；结构体容器字段拷贝浅拷共享（宿主/v2 同缺=既有边界）；~~**v2 内置构造器识别依赖文件含结构体声明**（缺陷 A，既有基线复现：全文件无结构体声明时 `错误(...)` 不展开为内置构造器 → 未定义符号链接失败）=下一轮 v2 独立缺陷专项~~ **→ 已修复（80-a，2026-09-12）：真根因＝池索引 0 哨兵冲突（非结构体依赖）；池索引 0 哨兵槽根治 + E2E 231/232/233 + 反证实证，见第八十轮段**。
  - **权重**: 8（语义缺口专项 8 × 预防价值1.4 × 当轮根治0.85）

## 80-a v2 池索引 0 哨兵槽轮（2026-09-12 第八十轮 深度机 x86_64）

- [2026-09-12 第八十轮] **问题类型**: 编译器内部索引哨兵歧义（＝79-a 登记的「缺陷 A：v2 内置构造器识别依赖文件含结构体声明」）
  - **描述**: `结果<字符串,整32>` 的构造器 `错误(5)` 在「全文件无结构体声明」时**退化为普通函数调用** → 产物含 `call _错误` → 链接失败（未定义符号）。宿主无此问题。79-a 登记的根因候选（「合成结构体布局表依赖结构体声明解析路径」）经本轮实证为**误判**。
  - **根因**（诊断注入 dbg/dbg2/dbg3 三步收敛）: **池索引 0 哨兵冲突**——入口文件**首个入池节点**（首个函数体首条语句的最早叶子，如 `返回 错误(5);` 的 错误）恰为被调标识符时 `子起点 == 0`；而语义层 `检查调用表达式`（守卫 `调用起点 > 0`）与 IR 层 `生成调用` 三特判（构造器/转移/类型大小，守卫 `n.子起点 > 0`）把 0 当「无被调」哨兵 → 构造器识别整段跳过。**「结构体声明」只是把首叶节点推离索引 0 的掩盖变量**（插入一条无关垫语句等效——V5 变体实证）；诊断实证：P10c 打印 `调用名=[错误] 语境=63`，P10b 同位置**无该行**。
  - **解决**: **池索引 0 哨兵槽**（方案乙，rustc `NodeId` 0 保留同款）——`IR/IR常量.cn` 新增 `常量 节点_哨兵 = 0`、`主.cn` 池创建后 `池追加(池, 建节点(节点_哨兵, 0, 0, 0, 0))`：真实节点索引恒 ≥1，全树 `> 0` 与 `>= 0` **两写法同时正确**、零处守卫改动、零运行时开销。三案对照：甲（24 处守卫逐处 `>= 0`）隐患留存（新代码写 `> 0` 即复发）落选；丙（池基址改 1 起）改动面大落选。
  - **成效与防线**: 首节点形态矩阵 HEAD↔修复实测——错误/正常/某些（P20/P21：HEAD 链接失败 `_正常`/`_某些` → 修复退出码 0）、类型大小（P24：HEAD `_类型大小` → 修复 0）；探针转正 **E2E 231_v2_首节点构造器形态**（校验和 31）+ **232_v2_首节点类型大小形态**（7）+ **233_v2_首节点构造器语境拒绝**（负测）；**反证实证**（stash 回退重建 v2p → 231/232 链接失败、233 v2p 静默放行退出码 0，三项全 FAIL；恢复后全 PASS）。验证链：v2 树构建零新增警告（189≡189 逐行同）+ 单测 1306/1306 + E2E 260 用例 256 过 0 失败/4 跳 + 固定点 **309133**（新口径，+32）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **㊴ 「0 当哨兵」先证「0 不可达」；修法优先哨兵槽而非逐处改守卫**：索引域（池索引/槽号/表键）用 `> 0` 判「有效」时，必先问「0 是不是合法值」。CN v2 池索引 0 是合法节点位（入口文件首个入池节点）→ 与「0=无」直接冲突。**根治顺序**：先做哨兵槽（让 0 在索引域不可达，一处改动全族自愈、新旧两种写法同时正确），逐处 `>= 0` 只作为「0 不可达且已被证明」时的等价整理。Rust 对照：rustc `NodeId` 0 保留（`CRATE_NODE_ID=1`）。
    - **㊵ 「有 X 就正常、无 X 就失败」先做单变量二分，再读代码**：本缺陷的 X（结构体声明）实为**时序副作用**（把首叶节点推离池索引 0）——插入一条无关垫语句同样「修复」。**判据问句**：「X 改变了什么隐性状态？」（本例=节点池索引偏移）。不做二分直接按「X 的语义」猜根因（79-a 的「构造器识别依赖结构体声明」）会修错方向。
    - **㊶ 首节点/首元素形态用例的构造纪律（反证揪出）**：验证「池索引 0 / 首元素」形态的用例，必须保证**被测节点是文件首个入池节点**——变量声明语句的**类型节点先入池**会抢占索引 0（232 首版用例因此未命中：反证跑出 PASS 而非 FAIL，暴露用例无效）；函数参数同样入池（P23 实证）；**可达形态只有「无前置依赖的调用」**（实参=字面量/类型名）。写此类用例后**必须跑反证**（修复回退后应 FAIL）——正测全绿不代表用例命中了目标形态。
    - **㊷ 同族守卫修复前先做「可达性判定」，按可达面选方案**：不是所有 `> 0` 都是活缺陷——下标/成员对象、赋值左值末、二元左末的索引**必为已声明变量**（其节点必在前）或右值起点 ≥1 ⇒ 0 不可达（P23/P26/P27 实证不可构造）。判定依据=各节点字段协议实测（dbg3 实测：二元/赋值 `子起点`=右值起点）而非注释（注释与实现不符时以实测为准——二元节点注释写反了左右起点）。
    - **㊸ 诊断注入走既有诊断通道（零依赖定位法）**：语义层文件无 IO 导入时，`表诊断(诊断, "[dbg] ...")` 直接进 `[sem5] 正确诊断:` 输出流（无条件打印）——不改任何语义、不加导入、零工具依赖完成定位；**定位后立刻删除**（删除判据：`git diff` 为空）。本轮 dbg/dbg2/dbg3 三步定位全程未引入 IO。
  - **诚实边界**: 转移/下标/成员形态的索引 0 **不可构造**（对象/实参必为已声明变量 ⇒ 索引 ≥1）：修复对它们为「写法脆弱性消除」而非「活缺陷修复」，未加单独 E2E（哨兵槽使其自动正确）；普通函数调用形态修复前也正常（IR 普通调用路径不依赖被调索引），仅作回归对照；数组类型缺口/容器元素=含串结构体/字符串键映射等 79-a 边界维持；win-x64 v2 并行未隔离（家机轮）；arm64 复验随跨机轮。
  - **权重**: 8（编译器内部缺陷 8 × 预防价值1.5 × 当轮根治0.85）

## 81-a 容器元素 = 含拥有型串字段结构体轮（2026-09-12 第八十一轮 深度机 x86_64）

- [2026-09-12 第八十一轮] **问题类型**: 语义缺口专项（plans/020 矩阵靶子「容器元素 = 含串字段结构体」＝79-a 边界销项；同轮根治 4 枚既有缺陷），**✅ 当轮销项（用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案——性能第一、安全第二」）**。**权重 8**。
  - **描述**: 按矩阵靶子立案，探针 P1~P17（每形态块出口 `内存::活动分配数()` 差值 + 分步打点 + 变体矩阵）实证一族缺口：①**容器元素（结构体含串字段）的字段串无释放面**（向量/链表/栈/队列/集合 × 析构/清空/删除/设置，宿主/v2 同缺，每元素泄漏 1）；②**移位走结构体整体赋值深拷**（`数据[移动] = 数据[移动+1]` 在 79-a 后是 preFree+memcpy+__cn_str_copy——多余分配 + 源槽句柄留在超范围槽=泄漏 1/元素）；③**方法调用路径的结构体字面量实参=空指针**（`表.追加(盒子{...})` → asm `mov rdx, 0` → 被调方按值拷贝即解引用空指针，rc=139 段错误；普通调用路径 D3 根治时已修、方法调用路径漏）；④**调用返回实参的返回临时无释放面**（`表.追加(造盒())` 的 retbuf 句柄悬空=泄漏 1/次）；⑤**链表（有析构类元素）全量析构平铺**（删除头部摘链后槽内容仍在且槽序号可能 ≥ 元素数量：越界槽二次 `~T` + 有效元素漏释）；⑥**块终止截断字段名单 → 函数级兜底失效**（`存入(表,值)` 内 `盒子 局` + `返回 X;` → 泄漏 1；asm 对照函数体零 `__cn_str_free`）；⑦**v2 帧尾共享 retbuf × 嵌套结构体返回实参**（单实参=泄漏 1、双实参=**数据损坏**：`合并(造盒(1),造盒(2))` 宿主 1 / v2 0）——登记下一轮第一优先；⑧B35 字段容器接收者（残留 2=字段容器对象+元素串）靶子维持（涉字段赋值语义语言级裁决）；⑨stdlib 链表 `追加` 槽复用缺陷（删除头部后用 `元素数量` 作新槽号与在链槽冲突：追加 11/22/33+删头+追加 44 → 大小=3、头部=22、44 丢失）——独立缺陷登记。
  - **根因**: **「拥有型资源」的槽位形态清单仍不完整**（79-a 补了「聚合字段」，本轮补「容器元素」）——元素的**释放义务**与**移动语义**是两件事：①释放侧缺「元素=值语义聚合」的第三类模型（既有只覆盖 字符串元素〔运行时辅助〕与 有析构类元素〔平铺循环〕）；②结构体赋值语义（深拷）被误用于**结构性写入**（移位）——那是「搬移」不是「复制」，Rust 在 `Vec` 内部用 `ptr::copy`（move）而非 `Clone`；③**名单式 RAII 的截断纪律不彻底**：72-a 的「跳出路径不截断名单」只落在一处，块终止（返回）路径仍截断 → 函数级兜底（按名单释放）看不到该项；④**提前返回守卫的连带跳过**：`genStringFrees` 里 `if (ownedSlots.empty()) return;` 位于字符串段与字段兜底段之间，字符串为空即静默跳过字段兜底（长函数中段 early return 的经典陷阱）；⑤v2 的 retbuf 是**帧尾单缓冲**——「返回值跨越另一次调用」的形态必然被覆盖（宿主侧每次调用独立 `__retbufN` 槽，无此问题）。
  - **解决**: **方案甲（编译期展开 drop glue 全链，Rust `Drop`/`ptr::copy` 对照，性能第一/安全第二）**——①**元素释放面**：新增 `emitContainerElemWalk`（单点事实源：平铺 `0..元素数量`／链式 `头索引` 沿 `下一索引` 链游 + 步数上限防环 + `数组==无` 守卫）与 `emitSingleElemRelease`（单槽唯一持有者槽；参数索引带越界守卫、字段索引仅负值守卫），含串结构体元素走 `emitOwnedStrFieldFreesAt`（嵌套递归 + 结果/可选联合体条件释放）；**有析构类元素全量同时改链游**（修 P7）；②**移动挂点** `移动元素(目标,源)`：注入 `CopyStruct + 源槽字段清零`（Rust `ptr::copy` 同款零拷贝；调点=stdlib 移位单点：向量 插入/删除、集合 前移；无串字段元素=纯 memcpy 零行为变化）；③**字面量实参物化**提取共享助手（`materializeStructInitArg`）+ 方法调用路径复用 + 物化临时登记为含串字段聚合局部（块出口释放，修既有泄漏）；④**调用返回实参临时清理**（`genArgValueWithCleanup`/`flushPendingArgCleanups`：RAII 守卫单点覆盖全部 return + 嵌套调用基准式隔离 + 返回类型 dict 回退查）；⑤**名单不截断**（块终止路径）；⑥`genStringFrees` 提前返回改条件包裹；⑦79-a 死代码清理（`fieldTainted_` 双死代码整链删除）；⑧v2 删除 `标记入容器结构体污染` 整链（元素槽由 stdlib 写入深拷独立拥有，源照常释放；元素释放经供给 obj 单点）。
  - **成效与防线**: 探针全形态残留 **1/2/3 → 0**（宿主 + v2 双侧；P1 全形态 + P3/P4/P7/P8/P9/P10 + P14 变体矩阵）；**E2E 234_双编译对照容器元素结构体字段**（移植纪律 9 **第七个**对照用例，八形态折 **255** 两侧一致）+ **235_容器元素实参形态**（宿主单侧八形态 **255**）；**反证实证**：宿主注入「元素判定恒假」→ 234 宿主 255→**128**（①~⑦ 位全失）、v2 注入「实参清理失效」→ 234 v2 255→**223**（缺 ⑥32 位）——两侧注入已恢复（残留=0）。验证链：清场重建**零警告** + 单测 **1306/1306** + E2E **262 用例 258 过 0 失败**/4 跳 + 锚定链固定点 **308719 行**（新口径，-414）逐字节一致 + 运行级 3/3 + 对拍 **34/34** + 释放路径两侧对照（脚本扩 81-a 段）。
  - **预防**:
    - **㊹ 槽位形态清单要按「存放位置」穷举，不能按「类型」二分**：字符串元素/有析构类元素的二分假设漏掉「元素=值语义聚合（含资源字段）」第三类——新容器/新元素类型落地时，先问「元素的释放义务由谁承担、按什么模型遍历（平铺/链式/交换前缀/逐字段展开）」。
    - **㊺ 结构性写入（移位/搬移）不能用「赋值语义」实现**：CN 结构体赋值=深拷（79-a），用于移位即「多余分配 + 源槽句柄丢弃」；正确模型是 **move（memcpy + 源槽清零）**。判据问句：「这里是在『复制一个值』还是在『把元素搬到另一个槽』？」Rust 对照：`Vec::remove` 用 `ptr::copy`（move），`push(s.clone())` 才 clone。
    - **㊻ 名单式 RAII 的截断纪律（72-a 的补漏）**：凡「编译期名单 + 函数级兜底」组合，**终止路径（返回/中断/继续）一律不截断名单**——截断会让兜底按名单扫描时看不到该项（本轮 P14 实证：`返回 X;` 令 `盒子 局` 从名单消失 → 泄漏）。清名单只允许发生在「真释放过」的路径上（幂等清槽使重复释放安全）。
    - **㊼ 长函数中段的 early return 会连带跳过兄弟段**：`if (A.empty()) return;` 若位于 A 段与 B 段之间，A 为空即静默跳过 B 段（本轮 `genStringFrees` 实证：字符串名单空 → 字段兜底段永不执行）。修法：把 A 段包进 `if (!A.empty()) { ... }`，让后续段无条件执行。
    - **㊽ v2 帧尾共享 retbuf 的消费纪律（下一轮专项前置）**：v2 全部结构体返回共用**单缓冲**——新增「返回结构体」消费点时必须问「这个值能活过下一次调用吗」；**修法首版教训**：在「实参求值后物化」时递增 `槽计数` 分配新槽 → 与预扫槽分配冲突/帧布局破坏 → E2E 148/158 回归（已回退）。生成期新增槽必须先设计「独立于预分配槽区」的机制，或改为「所有返回调用后立即物化（丙案）」的全量口径。
    - **㊾ 反证实验要逐注入点对应位**：一个门禁用例可能有多个修复面，注入不同点应丢不同校验位（本轮宿主注入丢 ①~⑦=128、v2 注入丢 ⑥=223）——若注入后退出码不变或丢位不符，说明用例未覆盖该面，需补强。
    - **㊿ 新用例命中既有登记边界时的处理**：E2E 235 首版 ⑧ 命中 79-a 已登记边界（被调方写按值参数字段=归一化落堆不释放）→ 调整用例只覆盖读取面（避免把「已登记边界」混入门禁期望），而不是改实现去迎合用例。
  - **诚实边界**: ①v2 帧尾共享 retbuf × 嵌套结构体返回实参（单实参泄漏/双实参数据损坏）=**下一轮第一优先**（三方案呈报：甲=实参物化到独立槽〈推荐，需先定槽分配纪律〉/乙=retbuf 全量独立〈帧膨胀〉/丙=所有返回调用后物化〈改动最大〉）；②v2 结构体字面量作实参=**v2 语法层缺口**（宿主已修）；③B35 字段容器接收者（残留 2）维持靶子——涉「字段赋值语义 move vs 深拷」语言级裁决，随用户裁决实施；④stdlib 链表 `追加` 槽复用缺陷（删头后槽号冲突）登记下一轮；⑤被调方写按值参数字段=归一化落堆不释放（79-a 边界维持）；⑥未初始化槽 preFree 隐患（依赖 `cn_free_tracked` 在册判定空安全，实测未触发）登记观察项；⑦数组元素/装箱字段接收/静态面/容器字段浅拷 等 79-a 边界维持；探针留 /tmp/p81probe 不入库。
  - **权重**: 8（语义缺口专项 8 × 预防价值 1.4 × 当轮根治 0.85）

## 82-a v2 帧尾共享 retbuf 根治轮（2026-09-12 第八十二轮 深度机 x86_64）

- [2026-09-12 第八十二轮] **问题类型**: 编译器内存模型缺陷（v2 帧尾共享 retbuf × 嵌套结构体返回实参，**数据损坏级**；81-a 登记为下一轮第一优先），**✅ 当轮销项（用户令「继续后续任务；遵守 AGENTS.md 第3项纪律，参考 Rust 经验给最适合方案——性能第一、安全第二」）**。**权重 9**。
  - **描述**: v2（自举编译器）的**全部结构体/结果返回共用帧尾 retbuf 单缓冲**（`retbuf序号` + `累计retbuf字节数` 定尺寸；宿主相反——每次调用独立 `__retbufN` Alloca 槽）。后果：任何「返回值跨越另一次返回调用」的形态被覆盖——①`表.追加(造盒())`（单实参）=返回句柄被外层调用的结果返回覆盖=**泄漏 1/次**；②**`合并(造盒(1),造盒(2))`（双实参）=第二次返回覆盖第一次=数据损坏**（实测宿主 1 / v2 0）；③E2E 172 跨模块 `甲::记录 甲记录 = 甲::造甲(42,99);` → v2 得 84（应 141，第二字段被同名异布局类型误算）。**触发条件**（本轮探明，收窄了面）：外层调用返回值也走 retbuf（结构体/结果返回）时才覆盖——`取大(a, 造点(1,2))`（外层返回标量）修复前即正确。
  - **根因**: 三层递进——Ⅰ**共享单缓冲**（设计选择）；Ⅱ**槽基址约定**（首版修复失败点：v2 结构体基址槽 = `槽起 + 槽数 - 1`〈最大槽号=最低地址=结构体基址，②b B1 约定〉，误用 `槽起` 当基址 → 写穿相邻变量槽：P20 实测物化槽 -704 与 `t` 槽 -696 重叠 8 字节；且只发一条 `IR_分配` → `扫描函数` 漏统计 → 帧尺寸不足）；Ⅲ**跨模块返回类型**（函数键格式 `模块$名#参数串`，返回类型表只存**纯名**「记录」→ 在调用方模块上下文解析到**同名异布局类型**：主模块 4B vs 甲模块 16B → retbuf 尺寸/物化长度错）。
  - **解决**: **甲案（返回物化到独立槽 = LLVM sret 各自分配语义，性能第一）**——①真结构体返回调用**发射后立即物化**：新分配的生成期临时槽 + `IR_结构体拷贝`（retbuf → 槽）+ 压栈值换**槽地址寄存器**（登记供清理）；②**实参位置**的物化槽其串字段在**外层调用发射后**释放（`清理调用实参临时`：命中登记表即释放，匹配即删除）；③**跨模块返回类型按定义模块限定**（键文本首 `$` 前=模块前缀 → 重查结构体大小命中才用限定名；不改返回类型表=零消费点影响）；④**槽分配纪律四条**（预扫分配参数/变量槽〈槽1=retbuf 指针保留〉→ 生成期临时槽只递增不复用不交错 → retbuf 区由代码生成动态计算〈`槽基数=最大槽`〉随之上移**两者不可能重叠** → 结构体基址槽=`槽起+槽数-1` 且**逐槽** `IR_分配`）。乙（retbuf 全量独立）因帧尺寸=Σ返回尺寸落选；丙（所有返回后物化）与甲实质等价（甲即全量；非实参位置不释放=句柄归接收方，零泄漏）。
  - **成效与防线**: 探针 P15/P17/P19/P20 全归零/全正确（`合并(...)` 宿主 1 = v2 1；`甲限定读` 141 ✓）；**E2E 236_双编译对照结构体返回实参**（移植纪律 9 **第八个**对照用例：八形态折 **255**，②双实参为核心回归锚；不含容器无供给）+ **E2E 234 形六改回「调用返回实参入容器」**（八形态折 255 不变）；**反证实证**：v2 注入「物化恒不物化」→ 236 v2 255→**68**（①②⑤⑥⑧ 位全失），恢复后两侧 255 ✓；E2E 172 修复过程中回归（v2=199）→ 根因Ⅲ 修复后复原 ✓。验证链：清场重建**零警告** + 单测 **1306/1306** + E2E **263 用例 259 过 0 失败**/4 跳 + 锚定链固定点 **319557 行**（新口径，+10838）逐字节一致 + 运行级 3/3 + 对拍 **34/34** + 释放路径两侧对照（脚本扩 82-a 段）。
  - **预防**:
    - **（续㊽）返回值的存放位置必须与「每次调用」绑定，不能与「函数帧」绑定**：共享返回值缓冲（retbuf 单缓冲）在「返回值跨越另一次返回值产生」时必被覆盖——Rust/LLVM 的 sret 是**每次调用各自分配**。审查问句：「这个返回值能活过下一次返回调用吗？谁保证它的内容不被覆盖？」
    - **多槽对象（结构体）的基址槽约定必须先查再写**：v2 约定 `基址槽 = 槽起 + 槽数 - 1`（最大槽号=最低地址），想当然用 `槽起` 会写穿相邻槽（借出证据：内存 dump 里两个对象重叠 8 字节）。**新增生成期槽分配时：①沿同一计数器递增；②逐槽发 `IR_分配`（扫描函数按 `附加` 统计最大槽=帧尺寸依据）；③与 retbuf 区不可能重叠（后者随最大槽上移）。**
    - **跨模块同名类型必须按「定义模块」解析**：v2 的函数键格式 `模块$名#参数串`——凡「按函数键反查其类型」的消费点（retbuf 尺寸/物化长度/字段布局）都要把类型名限定到定义模块（同名异布局：主 4B vs 甲 16B，症状是「大小对一半」而非崩溃，只能靠跨模块用例捕获）。
    - **诊断打印不要走「计数型」通道**：v2 的 `IR层诊断` 会 +1 错误计数并触发「IR 层错误即中止」——调试信息须用 `IO::打印到错误`（不计数）；本轮首次调试因此把「2 个真错误」放大成「4 个含诊断」的中止。
    - **修一个缺陷时用「最小形态矩阵」逐项回归**：本轮 P17 修好（双实参）后 P20 暴露「接收值错位」（槽基址）、P20 修好后 E2E 172 暴露「跨模块大小」（限定）——三层缺陷叠加，只跑单一探针会漏。
  - **诚实边界**: ①v2 **结果/可选 返回**不物化（合成布局；其返回值语言层不可作实参 → 覆盖不可达，登记）；②`造盒(5).号`（调用返回直接成员访问）=两侧共同边界（宿主同不支持）；③**赋值位接收结构体返回**（`盒子 甲; 甲 = 造盒(5);`）宿主支持、**v2 IR 层不支持**（诊断中止）——登记下一轮；④物化代价=每结构体返回 +1 memcpy + 帧尺寸增长（固定点 +10838 行）；后续可优化为「延迟物化 + 活跃性分析」（登记，非必需）；⑤v2 结构体字面量作实参（语法缺口）维持；B35/链表槽复用/未初始化槽 preFree 等 81-a 登记项维持；探针留 /tmp/p81probe 不入库。
  - **权重**: 9（编译器内存模型缺陷 9 × 预防价值 1.5 × 当轮根治 0.85）


## 82-b v2 赋值位接收结构体返回轮（2026-09-12 第八十二轮后续 深度机 x86_64）

- [2026-09-12 第八十二轮后续] **问题类型**: 语义缺口（v2 结构体赋值右值形态不全；82-a 登记边界销项），**✅ 当轮销项**。**权重 6**。
  - **描述**: `点 a; a = 造点(1,2);`（赋值右值=调用返回）宿主支持、**v2 IR 层诊断中止**（`结构体赋值右值须为同型变量标识符: a`）；含串字段同款（`盒 甲; 甲 = 造盒(9);`）。v2 原实现只支持「标识符」与「成员链」右值。
  - **解决**: 新增「右值=节点_调用」分支——求值右值表达式序列（`生成表达式(子起点, 子终点)` → 实参 + 调用 + 82-a 返回物化 → 表达式栈顶=**物化槽地址**）→ 目标=左槽地址 → **浅拷接管**（`深拷=假`，物化槽为一次性返回值、句柄移交目标=**零拷贝**，与 79-a 声明位「返回接收接管」同语义）+ 目标拥有槽 preFree + **同型校验用物化登记表**（`结构体返回临时们`）fail fast。
  - **成效与防线**: 探针 P21 两侧一致（a=(1,2)/b=(1,2)/甲.号=9/残留 0）；**E2E 236 形四改版**（赋值位接收 + 跨越另一次返回调用，折 255 两侧一致）；**反证实证**（注入「调用返回右值分支失效」→ 236 v2 编译失败，恢复后 255）。验证链：零警告 + 单测 1306/1306 + E2E 263 用例 259 过 0 失败 + 固定点 **320424**（+867）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 51）赋值右值形态要按「求值入口」穷举**：v2 的 `生成结构体整体赋值` 不走 `生成表达式`（自持右值形态分派）——新增右值形态（调用/三元/转换…）时，**先问「这个右值的求值入口在哪、能否直接调用 `生成表达式` 子区间」**（本轮复用同款入口，避免手写求值链）。
    - **（新增 52）v2 语义检查对 `结果.值` 的形态约束**：`.值` 访问须在 `如果 (r.正常) { ... }` 的**正向真分支直系**——`如果 (!r.正常) {...} 否则 { r.值 }` 与「嵌套条件里的 .值」均被拒；写含结果解包的新代码按正向形态书写（本轮三次编译失败均因此）。
    - **（新增 53）接管 vs 深拷的判据（复用 79-a/82-a）**：源为「一次性返回值/临时」→ **浅拷接管**（零拷贝，源不再被引用）；源为「标识符/成员」→ 深拷（源保持拥有）。实现处写清「源的唯一持有者是谁」。
  - **诚实边界**: `造盒(5).号`（调用返回直接成员访问）仍为两侧共同边界；「赋值右值=嵌套调用链」按表达式序列统一求值应支持（未单独加用例，观察项）；82-a 其余登记项维持。
  - **权重**: 6（语义缺口 6 × 预防价值 1.2 × 当轮根治 0.85）


## 83-a 链表/队列槽分配与回收轮（2026-09-12 第八十三轮 深度机 x86_64）

- [2026-09-12 第八十三轮] **问题类型**: 容器实现缺陷（stdlib 链表/队列 槽分配与「有效元素集合」不一致；81-a 登记项销项），**✅ 当轮销项**。**权重 7**。
  - **描述**: `新索引 = 元素数量` 假设「槽 0..元素数量-1 全被占用」——删除头部/尾部后被摘槽仍占槽号但不在计数内，该不变量破坏 → 新槽号与在链槽冲突：`队列<整64>` 入队 11/22/33 + 出队 + 入队 44 → 出队序 **22/44/44（33 丢失）**；`链表<整64>` 同款（33 被 44 覆盖 + `next` 自环）；`链表<字符串>` 头部内容错 + 残留 1（泄漏）。
  - **解决**: **空闲链复用 + 高水位计数**（布局 +2 字段 `空闲头`/`已用槽数`；`取空闲槽()` 空闲链优先 O(1) 否则高水位递增；`归还槽(槽)` 删头/删尾/出队挂链；扩容按 `已用槽数`；清空重置两字段=槽从 0 重分配）——Rust/C++ list 节点复用同款；v2 侧同步 `容器对象大小`（链表/队列 48→64）。
  - **成效与防线**: 探针 P22 两侧归零/序正确；**E2E 237_双编译对照链表槽复用**（移植纪律 9 **第九个**对照用例，八形态折 **255** 两侧一致）；**反证实证**：注入「`取空闲槽` 退回旧逻辑」→ 237 宿主 255→**240**（①②③④位丢失），恢复后 255 ✓。验证链：零警告 + 单测 1306/1306 + E2E 264 用例 260 过 0 失败 + 固定点 320424（不变=纯 stdlib 改动对自举产物透明）+ 对拍 34/34。
  - **预防**:
    - **（新增 54）容器槽号 ≠ 元素计数**：任何「用元素计数推槽号」的实现都隐含「槽 0..n-1 全占用」不变量——**删除类操作（删头/删尾/删中间）会破坏它**。新增容器/新增删除路径时先问：「被移除的槽号会被谁复用？复用时会不会撞上在用的槽？」正确模型 = 空闲链（O(1) 复用）或已用槽计数（不复用，内存膨胀）。
    - **（新增 55）容器字段布局变更的同步清单**：stdlib 容器类字段变更须同步 ①v2 `容器对象大小` 表（手工维护契约）②v2 `容器元素释放参数们`/硬编码字段偏移 ③宿主 `classFieldOffset` 消费点（动态 ✓）。**把新字段追加在末尾**可保老偏移不变（本轮实证：值表 0/下一索引 8/头索引 24/元素数量 40 全部稳定），是布局演进的低风险做法。
    - **（新增 56）归还槽的时机必须在「单槽释放注入」之后**：注入（`析构被移除`/`删除头部`）在方法体**前**按旧 头/尾索引 释放元素资源；归还槽会改写 `下一索引[槽]`（空闲链指针）——顺序颠倒会破坏注入读取（本轮实现按此顺序书写并注释）。
    - **（新增 57）泛型类内方法互调须 `自身.` 前缀**：CN 语义层对类内裸名方法调用判「未声明的标识符」（stdlib 既有惯例 `自身.析构元素(...)`）——新增私有辅助方法时按此书写。
  - **诚实边界**: 链表无随机访问（顺序校验用「读取头部+删除头部」迭代）；`空闲头`/`已用槽数` 为纯槽管理元数据（无堆资源，不进释放面）；v2 容器对象大小表是手工契约（后续字段变更须同步，登记维护纪律）；B35（待用户裁决）/v2 字面量实参/物化优化/未初始化槽 preFree 等登记项维持。
  - **权重**: 7（容器实现缺陷 7 × 预防价值 1.3 × 当轮根治 0.85）


## 84-a v2 结构体字面量作实参轮（2026-09-12 第八十四轮 深度机 x86_64）

- [2026-09-12 第八十四轮] **问题类型**: 语法/全链缺口（v2 表达式位不支持结构体字面量；81-a 登记项销项），**✅ 当轮销项**。**权重 7**。
  - **描述**: `收(盒子{ 名 = "…", 号 = 6 })` 与 `表.追加(盒子{…})` 宿主 ✓、v2 ✗（`[parse] 语句缺少分号 ';'` 中止不产 asm）。根因：v2 的结构体字面量是**解析期脱糖**（`解析构造字段赋值们`：裸声明 + 逐字段成员赋值），**只在三位接入**（声明初始化/标识符赋值/成员赋值）——`解析原子` 的标识符分支只处理 `名<…>(` 构造调用，遇 `标识符 {` 落入「语句缺少分号」。
  - **解决**: 表达式位把字面量解析为**表达式值**——新节点 `节点_结构体构造`（文本=类型ID、子区间=值表达式+字段初值**整段**）+ `节点_字段初值`（文本=字段名ID、附加=字段偏移、子区间=值表达式）；IR 层 `生成结构体构造`（临时槽 + 零初始化 + 逐字段写；**登记 `结构体返回临时们` → 实参位置由 82-a 清理机制释放串字段**）；主循环新增**构造值区间跳过**（`收集构造区间`）。
  - **成效与防线**: 探针 P23 两侧一致；**E2E 238_双编译对照结构体字面量实参**（移植纪律 9 **第十个**对照用例，八形态折 **255**）；**反证实证**：注入「解析原子 字面量分支失效」→ 238 v2 侧解析失败（节点 581→319、语法错误 36 中止），恢复后 255 ✓。验证链：零警告 + 单测 1306/1306 + E2E **265 用例 261 过 0 失败** + 固定点 **323750**（+3326）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 58）「解析期脱糖」机制的接入位要按「语法位置」穷举**：脱糖（把新语法改写为既有节点序列）天然只在**语句位**工作（它产出「语句序列」）；一旦该语法出现在**表达式位**（实参/字段值/返回），脱糖范式即失效——须改为**表达式值节点 + IR 生成**。判据问句：「这个新语法会有哪些出现位置？其中哪些是表达式位？」
    - **（新增 59）表达式值节点的 IR 生成必须配套「主循环区间跳过」**：v2 的主遍历按节点类型逐 token 处理——新增的「复合节点」（持子区间＝**值表达式序列**）若不登记跳过，其子表达式会被**主循环与生成函数各求值一次**（副作用/多次分配；首版症状=栈失衡「调用被调对象非函数名/方法」）。跳过区间终点设计为「复合节点自身的池索引」可零成本衔接（`子终点 = 构造节点索引`）。
    - **（新增 60）新增节点类型先查「语义相近的既有节点」的字段协议**：`节点_成员` 的 `子起点`=对象表达式、`文本`=字段名——直接复用于「字段初值」会让 `取成员宽度码`/成员链解析**误读**（静默取错宽度/类型）；宁新增类型码（44）也不要复用语义冲突的节点。
    - **（新增 61）复用既有机制的边界要写清**：本轮临时槽**完全复用** 82-a 的槽分配纪律四条 + 实参清理（`结构体返回临时们`）→ 零新清理代码；新增「一次性值」生成器时先问「它是不是又一个『返回物化』形态」（若是 → 直接登记同一张表，免费获得清理/释放面）。
  - **诚实边界**: 嵌套字面量字段值（值区间递归处理，未加专门用例=观察项）；字面量作返回值（未专门验证=观察项）；v2 语义层未对新节点做字段穷举/重复校验（未知字段走 附加=-1 既有语义；重复字段未拦=登记下一轮）；B35（待用户裁决）等其余登记项维持。
  - **权重**: 7（语法/全链缺口 7 × 预防价值 1.3 × 当轮根治 0.85）


## 84-b v2 字面量字段校验轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 检查纪律补齐（新语法路径缺同构校验；84-a 登记边界销项），**✅ 当轮销项**。**权重 6**。
  - **描述**: 84-a 新增「结构体字面量作实参」（表达式位 → 新节点 + IR 生成）时**未带字段校验**——同样的字面量在宿主与 v2 **旧脱糖位**（声明/赋值/成员赋值三位）都有 Rust **E0062（字段重复）/E0063（缺字段）= 编译错误**的同构纪律；新路径缺失 → 重复字段静默取后值、缺字段静默留零初始化值（**静默语义偏差**）。
  - **解决**: 在 `解析原子` 的字面量分支补齐两校验（**与旧脱糖位逐字同诊断文本**）：重复字段（局部已见向量查重）+ 穷举（`构造字段数`/`构造字段名` 逐序比对）。
  - **成效与防线**: **E2E 239_v2_字面量字段校验**（v2 闭环负测：重复/缺字段 → v2p 非零退出 + 不产 v2asm.s + 三条锚行固化）；**反证实证**：注入「两校验同时失效」→ 239 FAIL（v2p 退出码 0 静默放行），恢复后 PASS ✓。验证链：零警告 + 单测 1306/1306 + E2E **266 用例 262 过 0 失败** + 固定点 **324373**（+623）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 62）同一种语法新增「路径」时要同步移植既有「检查纪律」**：同一语法（结构体字面量）在两处实现（旧脱糖位 / 新表达式位）→ 校验（E0062/E0063）、诊断文本、穷举语义都必须**同源**，否则同一源码在不同位置行为分叉（新路径静默放行=最危险的形态）。落地判据：**新路径提交前 grep 旧路径的 `语法错误加一()` 调用点，逐条对照**。
    - **（新增 63）「静默语义偏差」类缺陷的门禁写法**：重复字段取后值 / 缺字段留零值都不会崩溃、也不影响其它用例——只能用**负测用例**（预期编译失败 + 诊断锚行固化）锚定；新增语法路径时**同时写正测与负测**（正测=合法形态两侧一致，负测=违规形态必拒）。
  - **诚实边界**: 嵌套字段值/字面量作返回值（观察项维持）；新旧路径的「缺字段诊断行号口径」微差（旧位报构造起始行、新位报末位置行，同语句范围内，登记）；B35（待用户裁决）等维持。
  - **权重**: 6（检查纪律补齐 6 × 预防价值 1.3 × 当轮根治 0.85）

## 84-c 嵌套结构体字面量轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 嵌套语义/释放面/遍历范围三处 v2 缺口（84-a 余项专门验证揪出）+ 两项跨侧登记，**⚠️ 部分闭环（三项修复 + 两项登记下一轮）**。**权重 8**。
  - **描述**: 嵌套字面量验证逐层展开暴露：①**嵌套结构体字段值按宽度存储**（只写指针 8 字节，读取按内联偏移=垃圾：宿主 10 / v2 140735369840171）；②**嵌套结构体字段未登记资源字段**（v2 注释称「含资源字段的嵌套结构体（递归释放）」但登记条件只收 容器/字符串 → 嵌套串无释放面，残留 3）；③**外层构造遍历把内层字段初值当自己的字段**（后缀序下内层字段初值落在外层区间内 → 二次求值 + 按内层偏移写外层槽 → 残留 1）。另揪出：**宿主三层嵌套字面量丢最内层字段**（`丙.d.b.a` 读 0，写入均已发射=数据正确性级）与 **v2 纯标量嵌套结构体局部引用 `类型$~类型`**（供给无符号 → 链接失败）。
  - **解决**: v2 三项落地——**①字段类型分派**（结构体字段 `IR_结构体拷贝` 整体拷贝=内联值语义；标量/字符串保留宽度存储）；**②遍历跳过嵌套构造区间**（`收集构造区间` + `区间终点`）；**③区间收集只留最外层**（过滤被包含区间）。**④嵌套字段资源登记 = 回退**：`资源字段数` 被多处消费（释放分派「否则=容器路径」→ objdel / 局部登记判定「资源字段数−串字段数」）→ 纳入嵌套结构体后 E2E 190 等缺 `类型$~类型` 符号回归 → 回退 + 登记下一轮；嵌套串释放面维持**泄漏方向**（宁漏勿错）。
  - **成效与防线**: 探针 P24~P27 全归零/全正确；**E2E 240_双编译对照嵌套字面量**（移植纪律 9 **第十一个**对照用例，八形态折 **255** 两侧一致；嵌套形态只校验语义/内容=残留登记边界）；**反证实证**：注入「嵌套区间跳过失效」→ 240 v2 255→**52**，恢复后 255 ✓。验证链：零警告 + 单测 1306/1306 + E2E **267 用例 263 过 0 失败** + 固定点 **325982**（+1609）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 64）「值语义内联」字段写入必须按字段类型分派**：结构体字段（内联区）用整体拷贝，标量/字符串用宽度存储——统一按宽度存储=把地址当值写（读取按内联偏移=垃圾，症状「离奇大整数」）。
    - **（新增 65）后缀序节点区间与嵌套的交互**：复合节点的子区间含内层节点（后缀序：内层字段初值在内层构造节点之前）→ 外层遍历/主循环**必须跳过嵌套整段**。判据问句：「这个遍历区间里会不会出现别的复合节点的内部节点？」
    - **（新增 66）计数表的「消费点全清单」纪律**：给共享计数表（如 `资源字段数`）新增一类条目时，**先 grep 全部消费点**并逐一评估语义——本轮为修「嵌套串释放面」扩表未先梳理 → 引发 E2E 回归 → 回退。**表语义变更 = 至少 3 处消费点的连锁审查**。
    - **（新增 67）「回归修复优先于新形态修复」**：新修复引发既有用例回归时**先回退到绿**再重设计（本轮：登记类别 2 → 回归 → 回退 + 登记下一轮，保留其余两项修复 ✓ 净收益为正）。
  - **诚实边界**: v2 嵌套结构体字段的串释放面（回退项，泄漏方向）；宿主三层嵌套字面量丢字段（数据正确性级，**下一轮第一优先**）；v2 纯标量嵌套结构体局部析构符号引用（下一轮）；B35（待用户裁决）等维持。
  - **权重**: 8（嵌套语义/释放面/遍历三处缺口 8 × 预防价值 1.4 × 当轮部分根治 0.7）

## 84-d 成员链嵌套深度轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 宿主「类型推导失败→静默降级 0」范式缺陷（**数据正确性级**；84-c 登记项销项），**✅ 当轮销项**。**权重 8**。
  - **描述**: `深C 丙 = 深C{ d = 深B{ b = 深A{ a = 1 }, c = 2 }, e = 3 };` 后 `丙.d.b.a` 读 **0**（应 1）、`收深(丙)`=5（应 6）；逐字段写读同款；二级/一级正常。**根因**：宿主 `visitMemberExpr`（读取路径）的对象类型推导**只手写「一层嵌套」**（`inner->object->getType() == IdentifierExpr`）→ 三级链 `objSrcType` 退化空 → `decl == nullptr` → **字段读降级为常量 0**（与 2026-08-25「嵌套成员链第一层为指针」、2026-09-03「UnaryExpr 对象」两次已修缺陷**同一范式**）；而**既有递归辅助 `memberObjStructType`（任意深度 + 下标 + 指针剥离 + 结果/可选映射）早已存在且被多处调用——读取路径未使用它**。v2 侧类型推导天然递归（探针实证两侧全对）=**仅宿主**。
  - **解决**: **甲案（空值兜底）**：`objSrcType` 计算后插 `if (objSrcType.empty()) { objSrcType = memberObjStructType(node); }`——一行级、零结构改动、零运行时开销、复用已验证辅助；连带 `memberObjStructType` 增强「**裸指针对象统一剥一级**」（`p.字段 ≡ (*p).字段`）。
  - **成效与防线**: 探针 P30 全对（a=1/11/41/51）；**E2E 241_双编译对照成员链嵌套深度**（移植纪律 9 **第十二个**对照用例，八形态折 **255** 两侧一致）；**反证实证**：注入「兜底失效」→ 241 宿主 255→**32**（**⑥ 二级/一级对照保留**=精准命中三级面），恢复后 255 ✓。验证链：零警告 + 单测 1306/1306 + E2E **268 用例 264 过 0 失败** + 固定点 **325982**（纯宿主改动=不变）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 68）「类型推导失败 → 静默降级 0」是宿主的高频缺陷范式（第三次复发）**：`decl == nullptr` 后 `emitResult(ConstInt, "0")` = 错误值静默流动（比较恒真/打印 0/参数传 0）。**纪律**：任何「查类型失败」分支都应（a）尝试**递归兜底**，或（b）**诊断报错**——绝不可静默产 0。新增类型推导点时 grep 同文件既有 `== nullptr` 降级点，逐一评估。
    - **（新增 69）「同一语义存在两个实现（手写快路径 + 递归辅助）」时必须交叉验证**：本项目 `memberObjStructType`（递归）与 `visitMemberExpr` 手写推理（一层）长期并存 → 深度差异静默分叉。**纪律**：新增/修改成员类型推导时**先查是否已有递归辅助**；手写快路径要么与辅助同构、要么在其失败时兜底。
    - **（新增 70）数据正确性级缺陷的用例形态设计**：三级链缺陷**不崩溃、不影响其它用例**——必须专门用例（本用例 ⑥ 二级/一级对照位=**回归锚**：证明修复只影响三级面，反证时该位保留=精准命中）。
  - **诚实边界**: 乙（整段替换递归辅助）/丙（语义层 resolvedType 回填=单点事实源）登记后续；v2 无此缺陷；84-c 登记的 v2 两项（纯标量嵌套析构符号、嵌套串释放面）维持；B35（待用户裁决）等维持。
  - **权重**: 8（数据正确性级 8 × 预防价值 1.5 × 当轮根治 0.85）

## 84-e 嵌套结构体字段串释放面收口轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 共享计数表扩展的兜底分支陷阱（84-c 回退项销项），**✅ 当轮销项**。**权重 8**。
  - **描述**: 84-c 为修「嵌套结构体字段的串释放面」给 `资源字段数` 表新增「嵌套结构体（类别 2）」条目 → **引发回归**（E2E 190 缺 `点$~点`、241 缺 `深B$~深B`）→ 当时回退并登记。本轮**先做消费点全清单梳理**（4 点）后重新收口：**根因=释放分派的「否则=容器路径」兜底分支**——纯标量结构体字段（无资源字段）落进兜底 → `IR_指针加载(值当对象指针)` + 元素释放 + **`IR_删除对象`（生成 `call 类型$~类型`）** → 缺符号链接失败。另复核：余项①「v2 纯标量嵌套结构体局部引用 `类型$~类型`」是「类别 2 登记 + 兜底分支」的**联合产物**，84-c 回退后即消失（探针 P31/P29 复核编译成功）。
  - **解决**: 两件套——**登记**（结构体类型字段 → 类别 2 重新收口，附消费点核对注释）+ **三处分派守卫**（`发射资源字段释放于地址`/`发射串字段清旧`/`发射串字段深拷`：在容器路径前插 `查结构体大小(字段类型ID) > 0` → 跳过）。
  - **成效与防线**: 探针 P25/P26/P27 残留 **0**（嵌套串释放 ✓）；**E2E 240 残留检查全面恢复**（84-c 放宽项 → 恢复完整内存断言，八形态折 255 两侧一致）；E2E **190/241 回归锚全过**；**反证实证**：注入「类别 2 登记失效」→ 240 v2 255→**4**（仅无嵌套形态保留），恢复后 255 ✓。验证链：零警告 + 单测 1306/1306 + E2E **268 用例 264 过 0 失败** + 固定点 **326126**（+144）逐字节一致 + 对拍 34/34。
  - **预防**:
    - **（新增 71）共享计数表的兜底分支是表扩展的经典陷阱**：`如果 (特殊类别) {...} 否则 如果 (计数 > 0) {...} 否则 {兜底}`——新增表条目会让**新的类别落进兜底分支**（兜底通常假定「剩下的都是容器/对象」）→ 把值当对象指针（`call 类型$~类型` 缺符号/段错误）。**纪律**：扩表时**同时**给每个分派点加显式类别守卫（或把兜底改为 fail-fast 诊断），并跑「回归锚用例」（本轮 190/241）。
    - **（新增 72）回退项的「二次收口」必须先补上约束的完整清单**：84-c 回退的直接原因是「未先梳理消费点」；本轮先列 4 消费点 → 定位到兜底分支 → 一次收口且零回归。**纪律**：登记「回退项」时**写明重试前置条件**（如「先 grep 消费点」），它是下一轮的施工图。
    - **（新增 73）现象消失 ≠ 修复**：余项①（缺符号）在回退后自行消失——**必须复核成因**（联合产物：登记 + 兜底分支），否则可能误以为「已修」而在下一轮把登记加回来时不带守卫、缺陷重现。
  - **诚实边界**: 「纯标量结构体字段跳过」为语义判定；结果/可选 字段仍未登记（释放需联合体条件分支=面外）；84-c 其余登记项 + B35（待用户裁决）维持。
  - **权重**: 8（共享表扩展陷阱 8 × 预防价值 1.4 × 当轮根治 0.85）

## 84-f 未初始化槽 preFree 隐患核验轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 观察项核验（零代码改动），**📋 未能构造（维持观察项）**。**权重 5**。
  - **描述/核验**: 79-a/81-a 登记的「未初始化槽 preFree 隐患」——stdlib `数据[n] = 值` 深拷的 preFree 对扩容新槽「释放」垃圾句柄，依赖 `cn_free_tracked`（仅在册释放）空安全；**风险=垃圾值恰为在册串地址 → 误释放（UAF）**。探针 P32 两版构造尝试（跨容器类型复用 64 字节块 + 弹出句柄留在槽内 + 偏移对位）**均未触发**。
  - **预防**:
    - **（新增 74）「理论隐患」的核验要写出「可构造性论证」而非只报结论**：本轮给出三重条件 + 未触发的机制原因（对位耦合/在册性耦合/`realloc` 搬移）→ 使「维持观察项」有据（下一轮 AI 不必重复构造）；**核验轮的产出=证据 + 可构造性分析 + 加固方向**（即使零代码改动也有价值）。
    - **（新增 75）「新槽写入」的语义应收编为独立构造语义（Rust `ptr::write` 对照）**：容器「写入新元素」与「赋值覆盖旧元素」在所有权语义上不同（前者无 preFree 义务）——当代语义混同（都走赋值深拷）时，未初始化槽会被当作「有旧值」。加固方向：编译器挂点区分两种写入（登记，若风险确认再实施）。
  - **诚实边界**: 观察项维持（未证伪）；加固三方向登记；P32-2 增量 +7 的额外分配来源未定位=下一轮观察项。
  - **权重**: 5（核验为主、零改动 × 预防价值 1.2 × 未根治 0.6）

## 84-g P32-2 增量观察项销项轮（2026-09-12 第八十四轮后续 深度机 x86_64）

- [2026-09-12 第八十四轮后续] **问题类型**: 观察项核验与销项（零代码改动），**✅ 销项（核算误差）+ 📋 性能项登记**。**权重 5**。
  - **描述/核验**: 84-f 登记的「P32-2 三追加增量 +7 有额外 3 个分配」经分步探针（P33/P34）实证为**核算误差**——正确分解=数组 1 + 字面量字段 `字符串复制` 1 + 元素副本 1（首追 +3）／后续每次 +2（字面量复制 1+元素副本 1）／删除逐次 −1（P34 四元素 −4 实证）；汇编 `__cn_str_copy` 恰 8 次与预期逐项吻合 ✓ **观察项销项**。
  - **附带定位（性能项）**: 宿主 P34「构造后」+9 vs **v2 +6**——**v2 表达式位字面量字段写入=零拷贝接管**（省 3 次复制），宿主 `emitStructInitTo` 走**归一化复制**（每字段多一次 `__cn_str_copy`）→ **纯性能差异**（语义均正确：副本归属各自清晰）→ 登记宿主优化项（与物化性能优化同批）。
  - **预防**:
    - **（新增 76）「分配增量异常」定位三步法**：①分步打点（每操作一次测量）②**汇编计数**（`__cn_str_copy`/`realloc` 调用次数 vs 逐项预期）③**两侧对照**（宿主/v2 同一探针）——本轮三步在 2 个工具调用内把「额外 3」判为核算误差并同时揪出真实性能差异；**结论必须给「分解式」**（每个分配对应哪条语义），否则「异常」会反复被误报。
    - **（新增 77）两侧实现的「复制次数」是性能对齐的观察面**：语义正确性对齐（E2E 对照）之外，**同一探针的分配增量差**能暴露一侧的冗余复制（本轮宿主字面量实参每字段多 1 次 copy）——把「同探针两侧增量对照」纳入性能核对习惯（不改变门禁，作为优化线索）。
  - **诚实边界**: 本轮零改动；性能差异项登记（宿主优化）；B35（待用户裁决）等维持。
  - **权重**: 5（核验销项 + 性能线索 × 预防价值 1.2 × 当轮根治 0.6）

## 85-a 聚合返回位所有权链断裂根治（2026-09-12 第八十五轮 深度机 x86_64）

- [2026-09-12 第八十五轮] **问题类型**: 数据正确性/安全级缺陷（UAF，**宿主与 v2 两侧同现**）+ 上一轮登记项复核证伪，**✅ 根治**。**权重 8**。
  - **缺陷**: 返回类型含拥有型串字段（结构体/结果/可选）时，`返回 <借用来源>`（按值形参=调用方 place 的浅拷副本／全局静态／成员链／下标／解引用）只做按位 memcpy → **返回值句柄与调用方 place 共享**；调用方各接收位一律按 owned 处理（浅拷接管/元素槽独立）→ 调用方释放其持有者后返回值字段**悬垂**（探针 P38：跨块 + 搅动后读出乱码，宿主与 v2 同现）。**独立于 84-g 的「性能差异」登记项**——复核该登记项时揪出。
  - **修复**: 返回位深拷归一化（借用来源 → 独立副本：`Alloca` 临时 + memcpy + 字段级 `__cn_str_copy`，直接写调用方 retbuf）；拥有局部/调用返回/字面量保持零拷贝；**借出视图方法（77-a 清单）豁免**（元素/读取/栈顶/队首/头部元素/读取头部/读取尾部/获取＝设计上返回借出视图，调用方不登记释放）。
  - **预防**:
    - **（新增 78）「返回值所有权」是独立检查面：任何「按位拷贝返回」都必须先问「句柄归谁」**——CN 的聚合返回契约是「调用方拥有」（各接收位一律 owned），故被调方必须保证返回值自带所有权（Rust `-> T` 同款）。归纳：**写入位**（声明/赋值/入容器/字段）与**返回位**必须用同一套「来源分级」判定（借用→复制 / 拥有→move），漏任一面即 UAF——本轮缺陷正是「写入位早已归一化（79-a），返回位漏了同一规则」。
    - **（新增 79）同类检查面「上提共享」而非并列复制**：容器借出视图清单（77-a `isBorrowViewMethod`）在语义层已存在，IR 侧需要时必须**上提 public 共用**（单一事实源）——本轮宿主这样做；v2 因 **IR 层不得反向依赖语义层** 而并列一份并在两处注释互指（跨层依赖方向 > 字面重复的 DRY 收益）。
    - **（新增 80）新规则必须比对「设计上的借用返回」清单**：容器读出接口设计上返回借出视图（`Vec::get -> &T`），若被新规则「拥有化」→ 调用方「不释放」口径冲突 → 残留断言全失（实测 235=212/234=152/238=77/240=183）。**暴露手法**＝跑全量 E2E（单跑新用例看不出来）；**反证手法**＝把豁免条件注入为恒假，看旧用例是否失败（本轮反证 B 实证豁免是必需的）。
    - **（新增 81）复合缺陷先做「形态矩阵」再动手**：P40 八形态（形参/全局/成员链/局部成员链/解引用/局部 move/字面量/借出视图）把「哪些该修、哪些是设计豁免」一次划清；其中**两个形态是独立缺陷**（④ `返回 *p` 基线段错误 rc=139；② 静态结构体槽尺寸 8 字节 + 字面量赋值丢值）——**矩阵的价值=把「同一症状的不同根因」分开**，避免只修一个形态就收工。
  - **附带教训（结构性）**: ④ 的根因是 **`genExpr` 对 place 的读值语义与「取地址」混用**——需要「地址」时一律用 `lvalueAddress`（左值地址单一事实源）；这正是 84-d「类型推导失败→静默降级」范式的近亲（此处是「读值→当地址」的静默降级，症状=段错误而非静默错值）。
  - **诚实边界**: 甲（参数 move 语义）/丙（编译期拒绝）登记；② 静态结构体登记（下一轮第一优先候选）；v2 结果/可选 返回不在面内；84-g 的性能登记项**证伪**（无冗余复制，差异=释放时机）。
  - **权重**: 8（数据正确性/安全级 × 两侧同现 × 根治 × 预防价值 1.3 × 面广 1.2）

## 86-a 复审三缺陷根治（2026-09-12 第八十六轮 深度机 x86_64）

- [2026-09-12 第八十六轮] **问题类型**: 用户令**独立复审 74-a~85-a（23 提交）**——验证链声明属实、规范/纪律整体合规，但探针实证揪出**三缺陷**（两侧同现/v2 单侧，内存安全级）+ 探针连带发现**两处既有缺陷**，**✅ 全部根治**。**权重 8**。
  - **缺陷一（三元 × 聚合，两侧）**: `返回 (条件 ? 借用形参 : 借用形参)` 悬垂——宿主搅动后乱码、**v2 段错误 rc=139**。三层根因：①宿主返回位判定 default 把三元当「拥有来源」（85-a 的形态矩阵没折三元）；②宿主+v2 各消费位（类型推导/赋值位/声明位/成员偏移/实参回扫）**全面缺三元形态**（落标量 8 字节静默损坏 / 各类拒绝诊断）；③v2 三元分支对聚合恒 8 字节 `IR_加载`（首字段值非地址）。修复=三元聚合**地址透传**（选中 place 地址）+ 十二处消费点补三元（宿主 5 + v2 7）。
  - **缺陷二（84-e 深拷守卫顺序，v2 单侧）**: `发射串字段深拷` 的「查结构体大小>0→跳过」插在递归分支**之前**（另两处正确在后）→ 嵌套递归深拷恒不可达（P42：宿主 RC=1 vs v2 RC=0）。修复=两行交换（三处同序）。
  - **缺陷三（85-a 豁免无类型约束，两侧）**: 用户顶层函数恰名「获取」+`返回 借用形参` 被误豁免深拷（P44 两侧乱码）。修复=豁免加容器类型前置（宿主 `currentClass_` × 三类容器；v2 `方法类名85`+`是容器类`）。
  - **扩展发现（既有缺陷同轮修复）**: 宿主成员位赋值 preFree 缺失（覆盖旧串泄漏，t244c 残留=1）；结构体字面量字段值=标识符落标量 8 字节路径（t244v 实证）。
  - **预防**:
    - **（新增 82）复审发现=新缺陷的合法来源；静态审查发现必须探针实证（证伪同价）**：四条代理审查线（规定/规范/diff 审读/纪律）产出 4 真缺陷 + 3 存疑——探针实证后 2 项存疑**证伪**（结果<盒>装箱=构造器内部深拷掩护；映射 V=盒=编译期拒绝不可达），3 项**实证成立**。**没有探针的静态发现的置信度不足**：报告缺陷时必须标注「实证/存疑」，且存疑项要写清「未命中的形态与原因」。
    - **（新增 83）条件表达式（三元/如果表达式）的值语义=「选中 place 的地址透传」**——但**消费位必须全链补形态**（类型推导/返回判定/赋值位/声明位/成员偏移/实参回扫/容器插入）：漏任一处即落「标量 8 字节」路径静默损坏（宿主）/ 段错误（v2）。**新值形态立项检查单**：grep 全树的「形态白名单」（`== 节点_X || == 节点_Y` 家族）与「类型推导 switch」，逐点确认。
    - **（新增 84）豁免/清单类判定必须有第二维=类型约束**：纯名字匹配的豁免清单 = 用户同名函数的隐形雷区（P44：顶层函数名 获取）。与 77-a `isBorrowSourceContainer` 前置、72-a「精确判定替代符号名模式」同源纪律——**新增豁免时先问「这个判定的作用域是什么」**。
    - **（新增 85）守卫插入位置=分派链语义的一部分**：84-e 在三个同构分派点插守卫，两处正确、一处插错（递归分支被跳过分支遮蔽）——**多分派点插入同款守卫时必须逐点核对全链顺序**（守卫的条件是否被前序分支的子集覆盖）；死分支（恒不可达）不会报错，只会在特定形态静默失效。
    - **（新增 86）局部修复必须全量 E2E 回归 + 探针矩阵**：本轮修复三元面时，探针矩阵连带暴露「成员位 preFree 缺失」「字面量字段=标识符标量路径」两处**既有**缺陷——矩阵/全量的价值=把「同症状不同根因」分离（与 85-a 新增 81 同款纪律的强化）。
  - **诚实边界**: v2 容器插入位三元面需供给机制（245 宿主单侧承载）；v2 结果/可选 返回三元不在面内；② 静态结构体等前轮登记维持；行数存量违规面登记（宿主 14 文件超千行等）。
  - **权重**: 8（数据正确性/安全级 × 两侧同现 × 根治 × 复审面广 1.3 × 预防价值 1.3）

## 87-a 静态变量全链路根治（2026-09-12 第八十七轮 深度机 x86_64）

- [2026-09-12 第八十七轮] **问题类型**: 85-a/86-a 登记项 ②（静态结构体，第一优先候选）销项——勘察中探针矩阵（P50 系列）揭示缺陷面**比登记项更宽**（两侧 × 全形态 × 全链条：槽尺寸/声明位/语句位/成员位/读取/实参/返回/重复赋值），宿主基线校验位 **0 全败**、v2 校验位 768（仅哨兵位），**✅ 全链根治**。**权重 8**。
  - **缺陷面（宿主 H1~H5 / v2 V1~V5）**: ①`.data` 静态槽恒 8 字节（linux_x64/arm64）——结构体 16/24… 字节整体写**越界相邻 .data 符号**（P50b rc=139 段错误；x64 侧已有尺寸逻辑=三后端分叉）；②静态**标量初值**被无条件忽略（恒 `.quad 0`——`静态 整64 x = 11111` 读出 0，布尔/字符/字符串同）；③语句位 `静态盒 = <结构体表达式>` 落 8 字节 StorePtr 丢值；④声明位初值零初始化（用户写初值得到零值）；⑤静态结构体字段读取基址落无效槽（`memberObjStructType`/`lvalueAddress` 缺静态回退 → 字段读降级常量 0）；⑥v2 静态结构体**降级为函数局部栈槽**（读取恒 `IR_静态加载` 值语义 + 赋值位无静态目标 → 跨函数不共享、静态槽零写）；⑦v2 布尔/浮点/字符串初值折叠失败静默落 0；⑧v2 静态字符串赋值无 drop（循环 300 次残留 +600）+ 借用来源共享句柄；⑨v2 成员位字段 preFree 判据只认「父∈资源字段名单」。
  - **修复要点**: 宿主 IR 层「静态聚合三类分派」（结构体=**原地逐字段构造**〈零临时槽零多余拷贝〉/字符串=来源分级归一化/类容器=P3-8 指针槽）+ `lvalueAddress` 静态分支 + `emitStructWholeAssign` 静态源 + 三后端「槽按 `typeSizeOf` + 标量初值直存 + 每槽对齐」；v2 静态表扩展（初值区间/需注入）+ 读取地址语义（`IR_静态地址` + `T_对象` 登记）+ 目标地址助手 + 字面量右值分支 + 静态串归一化 + 注入扩展 + 三后端槽尺寸。
  - **预防**:
    - **（新增 87）「登记项」只是缺陷面的一个采样点，不是边界**：登记项写着「槽尺寸 + 字面量赋值」两处，实测全链 **10 处**（5 宿主 + 5 v2）——含「标量初值被忽略」「字段基址落无效槽」「v2 降级为局部」等**登记项未提及**的面。**纪律：以「形态矩阵」铺面（全类型 × 全消费位），不以登记项措辞为范围**——与 85-a 新增 78「返回值所有权=独立检查面」同源（用矩阵而非直觉画边界）。
    - **（新增 88）后端分叉是缺陷的温床：同族逻辑三处实现必须逐点对齐**：x64 侧静态标量初值 ✓ 正确、linux_x64/arm64 侧恒 0（H2）；v2 三后端同款槽尺寸缺失（V1）——**三后端同族发射点（.data 段/静态槽）任何改动必须三处同治**，且以「探针矩阵 × 三后端」验证（本轮先以 linux 侧实证 + 跨机轮覆盖 win/arm64）。
    - **（新增 89）静态变量的「生命周期=程序全程」决定了 drop 语义必须显式补偿**：局部按块出口/函数尾释放（RAII 已在）；静态无出口 → **重复赋值必须当场 preFree/drop**（否则每次赋值泄漏；s9-v2 残留 +600 实证）。**新拥有槽形态立项先问「谁负责 drop，何时」**——无出口的槽=写入点自带 drop。
    - **（新增 90）「值即地址」约定必须配套回扫豁免（T_对象 登记）**：静态结构体读取生成「符号地址」后，实参回扫（`回扫实参取地址`）会把它当「值装载」再改取槽地址 → 段错误（86-a 三元同款）。**新增地址化值形态时，登记寄存器类型（T_对象）与消费端回扫跳过必须成对出现**。
    - **（新增 91）遇「既有缺陷被新代码触发」时的处置纪律**：本轮 v2 树自编译失败，根因=**v2 既有缺陷**（实参位短路 `f(a || b)` 栈失衡）+ 新代码引入该形态。处置=①新代码改预计算布尔**避开**（不留规避式 hack，且是更清晰写法）；②**栈基线净化尝试未解决即回退**（残留在本层之外——不做半吊子修复）；③探针与诊断链完整登记为下一轮第一优先。**「改一半的修复」比不改更危险**（制造「已修」假象）。
  - **诚实边界**: v2 实参位短路栈失衡（新登记，下一轮第一优先）；v2 静态字符语法限制；v2 打印浮点既有边界；多文件静态初始化仅入口模块注入（既有同限制）。
  - **权重**: 8（内存安全级〈越界写/悬垂/泄漏〉× 两侧全链 × 根治 × 预防价值 1.3）

## 88-a v2 实参位短路栈失衡根治（2026-09-12 第八十八轮 深度机 x86_64）

- [2026-09-12 第八十八轮] **问题类型**: 87-a 登记项「v2 实参位短路栈失衡」（第一优先）销项——栈模型普查定位**两层叠加根因**（语法层短路区间左操作数缺失 + IR 层净栈契约破坏），形态矩阵 12 消费位两侧对照全绿，**✅ 根治**（E2E 248 双编译对照门禁 + 反证三件）。**权重 8**。
  - **缺陷面（两层叠加）**: ①**语法层 S1**——`解析二元链`/`解析二元层` 的「左起点」= `池.大小()`（左操作数**之后**：左操作数已由 `解析表达式` 前段解析）→ `||`（层1）/`&&`（层2）短路节点左操作数区间 `[子起点, 附加)` **恒为空**（探针 dbg：`标 || 假` 收集 `子起点=28=附加`，左操作数在 27）→ 左操作数被主循环**平铺求值**（多压一项残留）+ 短路求值内左递归空区间退化「取栈顶」；②**IR 层 S2**——`生成短路求值`「只压不弹」（左+1/右+1/结果+1=净 **+3**）破坏「表达式求值净栈 +1」契约（弹前栈 `[1 2 -30 3 4 8]`：实参弹 2 后被调取到 `3` 应为 `-30`）；③两处叠加 → `f(a || b)` 弹栈错位 `[ir2] 调用被调对象非函数名/方法`+运行时错误6（宿主同形态正确=基准对照）。
  - **修复要点**: 语法层——`解析二元链` 增 `表达式起点` 参数 + `解析二元层` 左起点参数化（链入口=表达式起点/左操作数路径=父层左起点/右操作数路径=父层右起点）+ `解析层_子` 增 `子层左起点` 并**分层**（`层 <= 1` 用左操作数起点；层3+ 保持「左操作数之后」——语义层 `子起点-1`=左末端契约）；IR 层——`IR生成.cn` 新节**净栈契约单一事实源原语**（`表达式栈弹到深度`/`表达式栈归位压回`）+ `生成短路求值` 归位（左/右递归后弹到进入深度 → 末尾压唯一结果=净 +1）。
  - **预防**:
    - **（新增 92）隐式不变量必须显式化 + 单一事实源 + 校验**：「表达式求值净栈 +1」契约在 v2 存在多年却无文档无校验，短路求值破坏它（净 +3）长期无人发现——直到 87-a 由 v2 树自编译在**实参位**暴露症状。**纪律：栈式/状态式 IR 的核心不变量必须 ① 注释固化（契约点）② 单一实现点（归位原语，禁裸循环复制）③ 消费端按契约显式工作**。参考 Rust：rustc MIR 每值唯一 def-use（无隐式残留面）+ `-Zvalidate-mir` 校验器——CN 沿后缀栈模型，故以「净 +1 + 归位原语 + 出口校验」等价收口。
    - **（新增 93）字段多用途时改语义必须先普查全部消费者**：`子起点` 同时承载两种契约——短路区间左端（IR 层 `[子起点, 附加)`）与左操作数末端+1（语义层 `子起点 - 1`，混合符号检查/E0062 面）。首版把**所有层**左起点统一改为「左操作数起点」→ 层3+ 的 `子起点-1` 落到左操作数**之前** → 141_v2/190_v2/192_v2 三处 E2E 回归。**纪律：改字段语义前 grep 全部消费点（`子起点`/`附加` 的语义层 + IR 层用法清单），分层处理而不是一刀切**。
    - **（新增 94）「看似正确」是巧合正确，必须区分**：短路在赋值位/返回位/条件位「通过」是空区间退化取栈顶恰好=左操作数——缺陷被消费模式掩盖（取栈顶 vs **弹 N 个**）。**纪律：定位缺陷必须铺「消费位矩阵」（同一值形态 × 全部消费模式），只测一个消费位等于没测**——与 87-a 新增 87「形态矩阵铺面」同源，本例矩阵 12 位才暴露「只有弹栈消费错位」。
    - **（新增 95）区间标注（子起点/附加/子终点）是层间契约，标注错误在下游表现为无关症状**：语法层一个 `左起点=池.大小()` 的错误，症状却是 IR 层的「调用被调对象非函数名/方法」（栈失衡），跨越词法/语法/IR 三层。**纪律：排查此类症状须「自上而下 dbg 定位 + 自下而上核对区间语义」双向下钻**（本轮 dbg 打印收集短路区间即一击定位：子起点=附加=28）。
  - **诚实边界**: 87-a 规避写法（静态 preFree 预计算）保留（性能选择非缺陷规避）；win-x64（家机）/arm64（单位机）v2 实物复验=跨机轮；v2 静态字符/打印浮点/多文件静态注入/静态槽不登记释放/诊断无行号维持登记。
  - **权重**: 8（正确性级〈跨三层栈契约破坏 + 静默错位〉× 两侧对照 × 根治 × 预防价值 1.3）

## 89-a 净栈契约出口校验落地 + 浮点字面量全链根治（2026-09-12 第八十九轮 深度机 x86_64）

- [2026-09-12 第八十九轮] **问题类型**: 88-a 登记观察项「净栈契约出口校验」销项——探针两阶段（自举树全量 + 90 用例批量）验证中**抓到 1 处真实契约违反**，顺藤根治 **v2 浮点字面量全链静默跳过缺陷**（88-b 出口校验当场诊断 + 89-a 浮点修复），**✅ 根治**（E2E 249 双编译对照门禁 + 反证三件）。**权重 8**。
  - **观察项落地（88-b）**: `生成表达式` 出口加净栈契约校验（入口记进入深度 → 出口算 Δ → 多压 Δ>1 / 吞外层 Δ<0 两条 `IR层诊断`，消息含 增量/区间/进入深度）——把契约违反从「下游症状」提前到「当场暴露」。探针阶段（先探针后转正）实证：**零增量（Δ==0）37 处全部为 void 方法调用**（「void 不压栈」设计——`生成方法调用` 返回形态=空时 `目标寄存器==0` 不压栈，`生成表达式求值.cn` 注释明确）——契约口径确认为 **Δ ∈ {0, +1}**（0=void 无值、+1=有值）；若直接上「必须净 +1」的硬断言会**误杀全部 void 调用语句**。
  - **探针战果（浮点字面量缺陷）**: 246 用例 `标量浮 == 3.5` Δ=+2（区间 [133,136)、进入深度 0）——`节点_浮点字面量` 在 IR 层**全部分派点缺失**（主循环字面量分支 + `生成字面量` 无浮点分支）→ ①字面量零指令（二元弹栈判定退化〈栈上仅 1 项〉→ 左右操作数均取 0 → 汇编 `mov r10,[rbp-8]; cmp r10,[rbp-8]` **恒真**）②净栈 +2 契约违反 ③静态浮点初值注入落空（87-a 注入经 `生成表达式` 求值 → 无值可返回 → `.data` 恒 0 且无写回）。**246 v2 侧此前「通过」=双重侥幸**（比较恒真恰好命中分位 + 初值 0 未被断言）。修复：IR 分派 + `IR_常量浮点` + 三后端 `.double/REAL8` 文本直通（IEEE754 位模式由汇编器解析——编译期零开销）。
  - **预防**:
    - **（新增 96）防线落地必须先探针量化再转正；探针输入面必须含「用例批量」而非只烤自举树**：出口校验若直接上线且口径错误（如要求恒净 +1），会把 **37 处合法 void 调用**全部误杀——**先以调试输出跑全量输入面 → 量化「违例 / 合法例外」分布 → 确认口径 → 转正式硬诊断**。本例探针 1 击命中潜伏缺陷，而该缺陷**只烤自举树会漏**（v2 树无浮点字面量语料）——防线验证面=自举树 + 用例批量（两者互补）。
    - **（新增 97）分派点必须按「类型全集」核对，不能按「当前用到的」**：v2 的 `生成表达式` 主循环对字面量四族（整/浮/布尔/字符串）只分派三族——浮点缺位因「v2 树无浮点语料 + 246 双重侥幸」长期潜伏。**纪律：新增节点类型/新增类型族时，grep 全部分派点核对（主循环 / 生成字面量 / 类型推导 / 折叠路径 / 消费位白名单）**——与 86-a 新增 83「新值形态立项检查单」、88-a 新增 93「字段多用途先普查消费者」同源（分派点=消费点的一种）。
    - **（新增 98）「修复」与「侥幸」必须用机器码级证据区分——不变的绿灯也要查**：246 修复前后 v2 侧退出码都是 255（通过），但语义天壤之别（恒真比较 → 真比较）。**纪律：行为修复的验收不能只看用例绿**，要给出 asm 级证据（本例：`cmp r10,[rbp-152]` 寄存器 0 自比=恒真 → `cmp r10,[rbp-160]` 真双操作数 + `floatData_53: .double 3.5` + 静态槽入口写回）；与 88-a 新增 94「看似正确=巧合正确」同源——**绿灯不变时，看产物不看灯**。
    - **（新增 99）对照用例的每个形态必须双向锚定（真向进入 + 假向进入即错）**：246 的浮点分位只验真向（`如果 (x == 3.5) 加分`）——恒真缺陷照样拿分；249 采用**位计数双向锚定**（真向加分 + 假向加分即错，校验和 255）——恒真/恒假类缺陷双向都逃不掉。**纪律：新增对照用例形态至少含一个「不应进入」负向分支**（与 88-a 新增 94「消费位矩阵」互补：一个管消费模式全覆盖、一个管真值双向）。
  - **诚实边界**: v2 浮点语义边界（无浮点指令——面内=字面量装载 + 相等/不等按位模式比较；面外=算术/大小比较/负号/打印，登记待补面）；win-x64/arm64 侧浮点发射实物=跨机轮；前轮登记维持。
  - **权重**: 8（正确性级〈静默错误产物 + 契约违反〉× 潜伏面广 × 根治 × 预防价值 1.3）

## 跨机轮 win 侧 148_v2 崩溃拦截（2026-09-12 第八十六~八十八轮，家机 win-x64）

- [2026-09-12 跨机轮] **问题类型**: **跨机轮拦截的 win 平台特有回归**（内存安全级 0xC0000005；87-a 宿主引入）——**根因定位完成、修复留开发轮**（本轮纯验证轮零源码改动）。**权重 8**。
  - **现象**: E2E `148_v2_函数返回结构体` 的 v2 产物（win 后端 asm→ml64→link）运行 0xC0000005，期望 0；55 轮该用例通过 = **回归**；**linux 侧不触发**（深度机同期 E2E 271 过含 148_v2）——同源码三后端编译产物**行为分叉**。
  - **诊断链（可复现，探针留 target/p89win/ 不入库）**:
    1. **复现**：v2p 编译→ml64→link→运行稳定崩。
    2. **宿主维度 bisect（单变量手法）**：`git worktree` 检出历史提交（55 轮基线/86-a/87-a）+ 各自构建宿主 → **用不同宿主编译同一份 55 轮 v2 树**（`try_host.py`）→ 55 轮 ✓ / 86-a ✓ / **87-a ✗** / 当前 ✗ → **一步把 3 轮提交范围缩到 87-a**（与 v2 源码树无关）。
    3. **最小复现**：min3 探针（结构体按值实参 × 嵌套调用 `取和(造点(30,40))`）→ cn_main 帧 384（正常）/368（崩版）。
    4. **机制**：崩版该调用点**缺 retbuf 隐藏参数**（正常 `lea rcx,[rbp-384]`+返回物化 `rep movsb` 拷贝 vs 崩版 `mov rcx,30` 直传）→ 被调方把实参值当 retbuf 指针写 → 0xC0000005。
    5. **深层特征**：v2 编译器「返回物化判定」**对内存状态敏感**——同一 v2p 二进制在「仅源」（E2E 调用方式）与「源+平台」命令行参数下判定**翻转**（384↔368；仅差一次 `字符串复制` 堆分配）；87-a 的静态初始化改动（`emitStaticInitsAtEntry`/`ALIGN 8`/初值直存）改变了 win 侧 v2p 启动内存布局 → 「仅源」下稳定进入「无物化」分支。
  - **预防**:
    - **（新增 100）跨机轮的真实价值=平台分叉缺陷的唯一拦截面**：本缺陷 linux 全绿（深度机 271 过）而 win 崩——同源码在 win MASM / linux GAS 两后端（且宿主二进制内存布局不同）下的**编译产物行为分叉**，只有「本平台跑本平台产物」能暴露。与 87-a 新增 88「后端分叉是缺陷的温床」互为印证：**三后端同族改动的验证必须三平台各自实物跑**（跨机轮=该面唯一防线）。
    - **（新增 101）「内存状态敏感的判定」是 UB 类缺陷的指纹**：同一二进制在不同命令行参数（仅差一次堆分配）下判定翻转 = **症状确定性但依赖环境**（连跑多次结果相同、换参数/换宿主即变）——这与「未初始化读取/越界读残留」同族。排查手法：**参数矩阵**（同输入 × 参数组合）+ **宿主维度 bisect**（同源码树 × 不同宿主二进制）。Rust 对照：rustc 对同一输入输出完全确定（不依赖内存布局），Miri 直接检测未初始化读取——**v2 编译器需要「确定性编译」这一显式不变量**（编译输出不得依赖任何运行时内存状态）。
    - **（新增 102）多宿主对照法是「宿主 codegen 引入缺陷」的最快定位手段**：`git worktree add <dir> <commit>` + 各 worktree 独立构建宿主（googletest 需 junction：worktree 的 CMake 回退路径=`<worktree>/target/build/_deps/googletest-src/googletest`）→ **固定 v2 源码树不变、只换宿主编译器** → 直接分离「宿主改动」与「源码改动」两个变量。附带：`build --output X`（不带扩展）= 可执行 `X`（Windows 执行前须点名 `.exe`）+ `X.obj`；ml64/link 直调一律经 python subprocess（Git Bash 会把 `/nologo` 当路径转换）。
    - **（新增 103）「工具行为面」也是验证面**：v2p 的编译结果随命令行参数（「仅源」vs「源+平台」）变化——**编译器的「输入」不止源文件**（还有参数/环境），E2E 只覆盖了其中一种调用方式（仅源）。**新缺陷立案时把「调用方式矩阵」纳入复现面**（本轮 148 的 E2E 失败只在「仅源」下稳定复现；「源+平台」下反而正常——若 E2E 用后者将漏报）。
  - **诚实边界**: 未确证到「未初始化读取点」（留开发轮；已排除静态布尔初值/诊断开关误置/寄存器类型表/伪指针初值四项）；三方案（甲根除内存状态依赖〈推荐〉/乙确定性门禁强化/丙三后端对齐审查）呈报待裁决。
  - **权重**: 8（内存安全级 × 跨平台特有 × 跨机轮拦截 + 诊断链完整 × 预防价值 1.3）


## 91-a v2 浮点语义面全链补全 + 宿主 NaN 比较缺陷根治（2026-09-13 第九十一轮 深度机 x86_64）

- [2026-09-13 第九十一轮] **问题类型**: 89-a 登记「v2 浮点面外」面**专项补全**（算术/比较/负号/转换/ABI/打印拼接全链）+ 顺藤发现并根治**宿主缺陷 H1**（x86 两后端 NaN 比较非 IEEE，arm64 后端本已正确=三后端语义打架），**✅ 根治**（E2E 250 双编译对照门禁 + 反证三件全命中 + 序言手误修复）。**权重 9**。
  - **H1 缺陷形态**: `ucomisd` 后 `setcc` 直配（Lt->setb / Le->setbe / Eq->sete / Ne->setne）——NaN 时 x86 置 ZF=CF=PF=1 → **`NaN==NaN` 真 / `NaN!=NaN` 假 / `NaN<1.0` 真**（非 IEEE；arm64 `mi/ls/gt/ge` 天然正确）——**同一程序跨后端结果不同**（「结果打架」的平台内形态，三机协议防的是跨机打架，此为**跨后端**同族）。修复：`<`/`<=` 交换操作数走 `seta/setae`（NaN 时 CF=1 自然判假，LLVM ordered 同款 2 指令）+ `==`/`!=` 加 `setnp/setp` 组合——三后端统一 IEEE ordered（Rust f64 语义）。
  - **v2 面缺陷形态（探针 P91 十件）**: 浮点算术=整数指令算 IEEE754 位模式（`3.5+2.0` 打印 -9219994337134247936；`0.0/0.0` 走 idiv **SIGFPE**）；runtime 浮点实参放 rdi 未走 xmm0（打印/拼接恒 `0.000000`）；比较=整数比较；负号=neg；转换无分派。修复=IR 层浮点分派（二元类型字段浮点优先 + 混合隐式宽化 + 负号/转换源类型码 + 调用实参/返回类型码）+ 三后端 SSE/NEON 发射 + **SysV/AAPCS64/Win64 双计数 ABI**（共用层 `分配参数位置` 单点：整型/浮点独立计数，宿主 emitParamSetup 三计数器同构）。
  - **预防**:
    - **（新增 104）反证/实验的备份必须在「注入之前」完成，恢复基准必须是「注入前 md5 存档」**：91-a 反证 B 的备份命令误置于注入之后 → `bak` 文件本身是注入态 → 「恢复」写回注入态，且 md5 复核用**被污染的基准自证通过**（自证陷阱）——残留注入态源码被后续 E2E 全量/锚定链/对拍当作正式态使用。**纪律：① 备份与 md5 存档在注入前一次完成；② 恢复后以「注入前存档的 md5」比对（而非「与 bak 文件一致」）；③ 反证收尾三验证：备份基准 md5 + 恢复后文件 md5 + 关键产物 md5（如 v2p 二进制）三者独立核对**。
    - **（新增 105）重构后必须立即跑全量 E2E，禁止「重构 + 只跑专项用例」**：序言三游标重构为分配表后只跑了 250（浮点面，不涉结构体）→ 手误 `整数游标p = 6`（应为 `++`）致**全部多结构体参数用例 SEGV**（148/158/236/238/240/243 六个）潜伏到下一轮验证链才暴露。**纪律：任何「参数/寄存器分配」类重构的收尾验证面 ≥ 全量 E2E**（小改动专项用例足矣；**分配类改动触及所有函数**——专项用例无代表性）。
    - **（新增 106）同一逻辑的三份内联实现必须单点化**：91-a 首版三后端序言各自内联手写「整型/浮点/栈三游标」——X64Linux 手误（`游标 = N` 误写）而 win/ARM64 因写法不同（直接用分配表结果）无恙——**同逻辑多份手写=手误放大器**。修复：共用层 `分配参数位置` 单一归属，三后端只消费（类/号）结果。**纪律：跨后端重复的分配/编址逻辑一律下沉共用层**（与「后端架构方案1 平台无关决策单点」同源）。
    - **（新增 107）防线用例每个形态必须「独立锚定」，禁止可互相补偿的判定**：250 形八首版 `如果 (钠==钠) 位+1; 如果 (钠!=钠) 位+1;` 合并判定 `位==2`——==/!= **双回退互相补偿**（真+假 → 位值恰好不变）→ 反证 C 未命中（防线盲区）。修正=**值形态布尔变量**分列各运算 + 每运算独立折位（==/+8、!=/+16…）。**纪律：① 每个被测语义一个独立折位（禁止「或」式合并）；② 比较类判定必须走「值形态」（`布尔 x = a == b;`）锚定发射路径**（与 89-a 新增 99「双向锚定」互补：99 管真值双向、103 管多运算不可补偿）。
  - **诚实边界**: 宿主浮32 面缺陷 **H2 新立案**（`1.5f*2.5f`=0、浮32 赋值读回 0；浮32 算术/存储路径——另案待呈报）；v2 `1.5f` 后缀语法缺口、v2 `%` 浮点语义层未拒绝、v2 数学库内置表缺位（登记）；win/ARM64 浮点发射实物=跨机轮。
  - **权重**: 9（正确性级〈SIGFPE + 静默错误 + 跨后端语义打架〉× 面广〈三后端 × 全链〉× 根治 × 预防价值 1.4）

## 92-a 宿主浮32 面缺陷族根治（H2 销项）+ v2 侧浮点宽化面分叉收口（2026-09-13 第九十二轮 深度机 x86_64）

- [2026-09-13 第九十二轮] **问题类型**: 91-a 立案「宿主浮32 面缺陷 H2」**专项根治**（四缺陷：二元算术结果类型恒 f64 / 调用实参无条件 f32→f64 提升 / linux_x64 访存宽度前缀写死 qword / 自增结果类型硬编码 ptr）+ 双侧对照排查中发现 **v2 侧三处分叉**（移植纪律 10「两侧同缺一次修全」同轮收口）。**✅ 根治**（E2E 251 双编译对照门禁——移植纪律 9 **第二十个**对照用例 + 反证三件全命中）。**权重 9**。
  - **宿主缺陷形态（探针 P92，-O0 面）**: ①`浮32 甲=1.5f; 打印(甲+乙)` = **0.000000**（`甲/乙` = 0.994152 = 两个非规格化数位模式之比）——结果类型恒 `"f64"` 而操作数保持 f32（4 字节槽）：`movss` 装载清零 xmm 高 96 位 + `addsd` 把 f32 位模式当 f64 读（值域退化 ~1e-315）；且结果按 8 字节存 4 字节槽（**栈溢出破坏**）。②`恒等(1.5f)` = **0.000000**——实参无条件提升 f64 而形参 f32（被调方读 xmm 低 32 位 = double 位模式的低 32 位 = 0）。③结构体 f32 字段装载 **汇编失败**（`operand size mismatch for movss`）——宽度前缀写死 `qword ptr`（f32 应 dword；win-x64 同函数本已按类型分派、arm64 用 `ldr s/d` = **linux_x64 漏网**）。④`甲++` 对 f32 **静默失效**（打印仍 1.500000）——结果类型硬编码 `"ptr"` 走整型加法（位模式 +1 只改尾数最低位）。
  - **v2 分叉形态（双侧对照实测）**: ①混合运算「单侧浮点取该侧」把整型**窄化到 f32**（有损；`甲+16777217`：宿主 16777218.5 / v2 16777218.0）——违反规范「仅允许无损或宽化」；②`发射自增自减` 硬编码 `T_整64`（与宿主 D4 同族，`甲++` 恒 1.5 不生效）；③赋值/初始化位缺浮点宽化（`浮64 宽 = 浮32(1.5);` 读回 0）；④拼接路径缺 91-a「内置边界提升」（`"值=" + 浮32变量` 恒 0.000000）；⑤转换判定「**仅非浮点侧**插入转换」漏 f32→f64（双侧浮点混合 f32+f64 原有同款漏网）。
  - **修复（宿主 4 处 + v2 5 处）**: 宿主 = IR 二元结果类型精确化（`floatCommon`：f32 op f32 单精度运算 / 跨类型 f64 保持）+ 实参按形参类型分派（浮64 提升/浮32 原样/变参未知提升）+ LoadPtr/StorePtr 宽度按类型分派 + 自增结果类型/增量常量/写回宽度按操作数类型。v2 = 混合浮码升 f64 + 「码 ≠ 公共浮码统一插入转换」+ `发射自增自减` 类型码分派（新 `发射自增增量`：浮64 常量 1.0 经 `IR_类型转换` 降精度 = **零后端改动**）+ 新 `浮点宽化存储` 助手（声明初始化/赋值两存储点共用，与 `正32存储截断` 同款单一事实源）+ 拼接浮32 侧先宽化 f64。
  - **预防**:
    - **（新增 108）「结果类型字段」被后端用于选指令族时，必须与「操作数类型」同源校准**：宿主 D1/D4 同根——IR 指令的类型字段被后端当作「指令族依据」，而操作数另有独立类型；两者不一致时分派分裂（f32 操作数 + f64 结果 → `movss` 装 + `addsd` 算）。**纪律：浮点/窄类型算术的类型字段 = 操作数公共类型（同宽运算不扩张）；新增后端分派点必须同时核对「操作数类型」与「结果类型」两个来源**（91-a 预防 106「同一逻辑多份手写=手误放大器」的数据流版本）。
    - **（新增 109）访存宽度前缀必须与指令族同源分派，姊妹后端是漏网的照妖镜**：linux_x64 `emitPtrLoadStore` 写死 `qword ptr` 而 win-x64 同函数早有 `mp` 三元分派——**同族函数在两侧后端不同步=缺陷温床**。**纪律：新增/修改访存发射点时逐项对照姊妹后端（win↔linux_x64↔arm64）同名函数**（「三后端同构」不只指功能，也含**缺陷**——一侧写了分派、另一侧漏=下次分叉）。
    - **（新增 110）类型/codegen 类探针一律 `-O0` 复跑；防线用例的操作数必须落运行时路径**：O2 **常量折叠**把 `甲+乙`（常量可见）折成正确值——251 形态①在 D1 注入态**未被捕获**（反证 A 首跑 207 = 缺 16+32，形一侥幸通过）。修复 = 形态操作数改经 `折半(浮32(x))`（函数调用返回打破折叠传播）→ 反证 A 复跑宿主 **8**（①~⑧除④全部捕获）。**纪律：① 验证 codegen/类型缺陷的探针必须 -O0；② 防线用例每个形态都要问「这个表达式会不会被编译期折叠掉」，用函数调用/运行时输入构造不可折常量**。
    - **（新增 111）反证/实验的备份清单必须覆盖「全部将被注入的文件」，注入脚本必须可逆向**：本轮反证 B 注入前只备份了一个文件（`ir_expr.cpp`），注入 `ir_call.cpp` 时无备份 → 恢复只能靠注入脚本逆向替换 + 「注入前 md5」复核。**纪律：① 一次备份本轮全部改动文件（宁多勿少）+ md5 存档；② 注入脚本必须可推导注入前后文本，供逆向恢复；③ 恢复复核基准永远是「注入前 md5 存档」**（91-a 预防 104 的清单化加强版）。
    - **（新增 112）隐式转换的方向审查必须按「无损/宽化」逐组合过——「取该侧」是危险默认**：v2 二元「单侧浮点取该侧」对 `整 + 浮64` 正确（整32 无损转 f64），但对 `整 + 浮32` 是**窄化**（有损，违反规范「仅允许无损或宽化」）。**纪律：任何「按另一侧类型参与」的隐式转换规则，逐组合（整↔浮32 / 整↔浮64 / 浮32↔浮64）验算无损性；有损组合必须升到能无损容纳的类型（或编译期拒绝）**。
  - **诚实边界**: E2E 251 为两侧共有语义面（v2 `1.5f` 后缀语法缺口维持登记）；**新登记 v2 数组声明语法缺口**（`浮32[2] 表;` 解析报「语句缺少分号」——节点_下标/后缀设施在，缺声明位语法）；win/arm64 实物=跨机轮（D1~D4 在 IR/共用层，三后端同构受益）；探针留 /tmp/p92probe 不入库。
  - **权重**: 9（正确性级〈静默错误 + 编译失败 + 双侧语义分叉〉× 面广〈宿主 4 缺陷 × v2 5 处 × 类型系统/ABI/codegen 全链〉× 根治 × 预防价值 1.4）

## 93-a 类名/实例成员静态性不匹配六形态编译期拒绝（B2 销项）（2026-09-13 第九十三轮 深度机 x86_64）

- [2026-09-13 第九十三轮] **问题类型**: 第六十一轮立案 B2「类静态成员+析构写静态成员运行期崩溃」（tp3 探针）——本轮**探针二分定位真根因**并双侧根治：真根因与类静态成员**无关**，而是 `类名.实例方法()` 形态语义层漏检（宿主段错误「间接调用 NULL」/v2 侧垃圾值）。
  - **B2 定位链（探针四步）**: ①遗留产物 `target/tp3` 段错误复现（rc=139）+ `tp3.s` 符号反解真形态（`_cn_static__计数次数`/`_计数读次数` → 类{静态成员+析构写+读次数()} + 造两个() 两对象）→ ②当前宿主重建同形稳定复现 → ③**变体矩阵逐要素消除**（两对象/独立函数/类静态/拷贝构造/静态方法/块作用域全排除；**纯 `盒子.读值()` 无静态成员 → 段错误**=根因锁定）→ ④结论：立案描述两要素均非根因（tp3 探针偶然同时含「类静态成员」与「`类名.实例方法()`」两要素，当时归因偏移）。
  - **真根因与机制（双侧分叉）**: 宿主 `semantic_call.cpp` 成员调用路径 `checkExpr(对象)` 取类名类型 → `lookupClassMember` 命中 → **直接按实例方法调用生成（无 this）** → IR「间接调用 NULL」→ 段错误（IR dump 实证 `取地址 %v3` + `间接调用 %v5 0`）；v2 侧对象=未声明标识符（成员偏移标注 `返回 -1` 静默）→ 退化生成 → 输出垃圾值 `5548464235739974476`。**同族六形态**（探针 M1~M10）：①类名.实例方法()（崩/垃圾）②③类名.实例字段读写（既有拒绝/v2 垃圾与静默写）④实例.静态方法()（宿主崩）⑤实例.静态字段（宿主静默错：读恒 0/写不生效）⑥接口名.方法()（宿主崩）⑦子类名.父类实例方法()（同①根因）。
  - **修复（宿主 4 处 + v2 1 处）**: 宿主 `semantic_call.cpp` 成员调用路径新增 **receiver 类别判定**（`IdentifierExpr` 且 `isClassType`=类名语义；`自身`/`父类` receiver 非类名→实例语义，`父类.名称`/`父类.叫声()` 合法形态不受影响）→ ①④拒绝；接口分支新增 ⑥拒绝；`semantic_expr.cpp` 新增 ⑤拒绝（补齐既有「类名.实例成员」对侧）。v2 `语义/语义检查.cn` 新增 `检查类名成员访问` + 挂载 `检查表达式` 的 `节点_成员` 分支（语句根区间线性遍历=字段读/写/方法调用全形态覆盖）。
  - **预防**:
    - **（新增 113）立案描述的「要素」不等于「根因」——探针同时含多要素时必须做变体矩阵逐要素消除**：B2 立案把「类静态成员+析构写静态」当根因（tp3 探针恰好同时含 `类名.实例方法()`），隔 32 轮无人质疑；真修复只需一行判定。**判据：任何"最小复现"都要先做单变量消除（每要素一个变体），能删掉的要素一概不算根因**；尤以「老探针 + 时隔多轮」为高危（探针构造意图会掩盖真实触发要素）。
    - **（新增 114）「语义层已有同款拒绝」不等于「该纪律已覆盖」——同一形态的不同语法位置走不同代码路径，须逐路径核对**：`visitMemberExpr` 对 `类名.实例成员` 早有拒绝，但 `类名.实例方法()` 走 `semantic_call` 独立路径（直查成员表后即生成调用）=**漏检**。**预防：新增/审查拒绝纪律时，先枚举该形态的全部消费路径（读/写/调用/作值/实参/返回/初始化），逐路径确认挂点**；「访问位有」不能推「调用位也有」。
    - **（新增 115）「接收者类别」判定是类名.成员 纪律的关键，特例必须显式排除**：`自身`/`父类` receiver 非 IdentifierExpr（实例语义/父类限定语义，合法）——若一律"非类名即拒绝实例成员"，会误伤 `父类.方法()`（20/21 用例）；判定公式=「IdentifierExpr 且 isClassType」（与既有 visitMemberExpr 同款），自身/父类自然落实例语义。
    - **（新增 116）「符号表查得到类型ID」≠「名字是变量」——遮蔽判据必须查符号种类，禁用「类型ID != -1」近似**：v2 类名自身在符号表登记为 `符号_结构体`（种类 4）；首版 `检查类名成员访问` 按「表查找类型ID != -1 → 是变量」判定，把类名自身误判为变量 → **检查永不命中**（调试注入 `[调试] 条件4失败(是变量)` 定位）。**预防：凡"名字是变量吗"的判定一律用符号种类白名单（{符号_变量, 符号_参数}）；判定公式落笔前先在真实符号表上跑一次探针**（本次若先探针，可省一轮重建）。
    - **（新增 117）遗留探针产物是定位历史缺陷的富矿——先反解产物再重造探针**：`target/tp3`（二进制+汇编）保留了 32 轮前的真实探针形态，`tp3.s` 的 mangled 符号（UTF-8 hex 编码中文名）直接反解出类名/成员名/函数名（`_cn_static__计数次数` 等），比凭立案描述猜形态快一个数量级；配合「旧产物段错误复现 + 当前宿主重建复现」双确认即可锁定。**预防：历史轮次的探针/脚本一律留 `target/pXX.../`（gitignore 但保命），不要以"中间产物清理"名义删除**。
  - **诚实边界**: v2 侧 ④⑤⑥ 三形态不可表达（v2 类支持第一版无静态成员/接口/继承）——宿主侧拒绝已覆盖，随 v2 类能力扩展需同步；泛型类名形态（`盒子<整32>.方法()`）宿主由模块解析既有拒绝（不崩）非本族；变量名与类名同名遮蔽时按实例语义（两侧同款判定）。**B2 销项**（台账 plans/021 §4.2）；E2E 252/252_v2 + 单测 +7 + 反证双件全命中；固定点 362243 行。

## 94-a v2 数组声明语法缺口（C6）销项——v2 栈数组全链（2026-09-13 第九十四轮 深度机 x86_64）

- [2026-09-13 第九十四轮] **问题类型**: 92-a 登记的 C6 缺口（规格书 3.5 `类型[长度] 名` v2 解析报「语句缺少分号」）全链销项——v2 语法面自此有栈数组；宿主零改动（宿主数组支持完整=对齐参照）。
  - **实施要点（v2 11 文件 +499 行）**: 语法（类型位数组后缀合成 `"整32[5]"` + 声明位四 token 前瞻 + 初始化列表**元素包装节点**=`节点_数组初始化`/`节点_数组元素`）；语义（类型大小与对齐数组分支 + 类型引用剥 `[长度]` + 下标白名单数组豁免 + 元素类型登记指针变量表）；IR（`生成数组局部` C 布局多槽 + **数组名退化元素指针** + 下标读写复用指针路径 + **结构体元素成员链**（数组元素结构体表 + 解析成员链结构体ID 下标分支 + 顺序链下标地址）+ **越界检查**（下标 >= 长度 → 运行时错误(2)））；门禁 E2E 253（八形态折 255）/254（越界 rc=1）双编译对照 + 反证双件全命中 + 探针 11 件双侧一致。
  - **踩坑与根治（三类静默/崩溃，全在探针矩阵暴露）**: ①`生成标识符加载` 数组分支缺失时 Load 槽值=数组首 8 字节数据当指针 → 段错误（`r10=0x200000009`=数组内容）；**下标左值路径**（`生成下标左值地址`）是**另一条独立路径**，同样要数组基址=槽地址（首版只改右值=左值仍崩）；②左值**顺序链**（结构体元素成员链 `表[1].x = 30`）原处理器只认 标识符/成员/解引用——**下标被静默跳过**，索引恒 0（写入落 `表[0].x`；退出码仍 0，只有输出 diff 暴露 30/0/30 ≠ 10/40/50）；③越界检查首版错误码凭直觉用 1，宿主 runtime 契约表为 **2**（`src/runtime/io_api.cpp` 附录B：1=除零 2=数组越界 3=空指针解引用）——探针 diff 暴露文案不符。
  - **预防**:
    - **（新增 118）新增数据类型的「值语义」必须在所有消费路径统一核对（读/写/实参/返回）**：数组名退化元素指针的落点=**标识符加载**（值=基址地址），但**左值路径**（`生成下标左值地址`/顺序链）是独立代码路径——只改读路径则 `arr[i]=v` 仍按"指针槽 Load"取数组数据当指针（段错误）。**预防：新类型/新语义落地时先列消费路径清单（右值读/左值写/实参/返回/成员/下标），逐路径确认并跑通；"读对了"不代表"写对了"**。
    - **（新增 119）链式处理器（顺序链/后缀链/类型栈）遇到未覆盖节点类型会「静默跳过」——新增节点类型必须逐类核对分支**：`生成左值地址` 的顺序链原只处理 标识符/成员/解引用，**下标中间跳被静默跳过**（`表[1].x=30` 的步进丢失→索引归零，且**无任何诊断**——退出码 0 掩盖）。**预防：凡"按节点类型分派的链式循环"，新增节点类型时先枚举全部节点类型清单逐个核对；未覆盖形态应显式诊断而非静默跳过**（半实现纪律）。
    - **（新增 120）两侧对照探针必须做「输出逐行 diff」——退出码一致是最弱判据**：arr5（结构体数组）首版两侧退出码都是 0，但**输出 30/0/30 vs 期望 10/40/50**（写入错位）——只有逐行 diff 暴露。**预防：双编译对照/双侧探针脚本一律做输出 diff（本轮 `diff94.sh`），禁止只看退出码**；本项目 E2E 双编译对照的退出码判据是"折校验和"设计（每形态折一位=把值正确性编码进退出码）——新用例沿用该设计。
    - **（新增 121）跨语言「授权错误码/枚举」必须查契约表，禁止凭直觉取值**：越界检查首版用错误码 1（"通用错误"直觉），宿主 runtime 契约表为 2=数组越界——错误码是**跨层契约**（runtime/诊断/文档同源），不是实现细节。**预防：新增运行时错误/退出码/操作码等跨层取值前，先在权威表（`src/runtime` 附录B / IR容器 操作码表）查证并注释出处**。
  - **诚实边界**: 结构体字段数组（v2 拒绝）/多维数组（两侧不支持，宿主段错误=既有）/字符字面量 `'A'`（v2 既有缺口，登记 plans/021 C7）/字符串元素数组释放（两侧同残留 2=宿主同缺=79-a 靶子面，登记 C9）/负下标（两侧同边界=仅上界检查）。**C6 销项**；E2E 253/254 + 反证双件 + 探针 11 件；固定点 371923 行。

## 95-a v2 字符字面量缺口（C7）销项 + 宿主缺陷 H3 根治（2026-09-13 第九十五轮 深度机 x86_64）

- [2026-09-13 第九十五轮] **问题类型**: C7 缺口（`字符 甲 = 'A';` v2 报「语句缺少分号」）全链销项；探针双侧 diff 同时**实证宿主缺陷 H3**（字符字面量三处消费点同款「去引号取首字节」——违反规范 01b 三「4 字节 Unicode 标量值」）。
  - **H3 形态与实证**: 宿主 `'中'`=**228**（UTF-8 首字节 0xE4）、`'\n'`=**92**（反斜杠 0x5C）、`'\u{4E2D}'`=**92**——三处同源（`ir_decl charLiteralCodeText` / `ir_expr evalDefaultExpr` / `ir_expr visitCharLiteral`）；ir_expr 注释声称「`\u{4E2D}` 全解码」**实为未实现**（注释=意图非事实）。
  - **修复**: 宿主单一归属 `charLiteralCodePoint`（剥引号 + decodeEscapes 转义/`\u{XXXX}` + UTF-8 2/3/4 字节解码）3 处统一；v2 词法 `字符码点`（同口径 → 码点数字文本）+ 解析原子分支 + IR `IR_常量整数`+`T_字符` + 语义 驻留("字符")；C3（v2 静态字符语法）顺带销项。
  - **预防**:
    - **（新增 122）「注释声称的能力」必须与实现核对——注释里的 TODO 会在多轮后变成"以为已实现"**：ir_expr 的 `'\u{4E2D}' 全解码` 注释误导了 N 轮（实现是取首字节）；本轮若非双侧 diff 探针（v2 走规范口径 20013 vs 宿主 228）几乎不会暴露。**预防：凡注释出现「全解码/全处理」等绝对措辞，写探针验证一次；修 bug 时注释与实现同改**。
    - **（新增 123）同一语义的「多消费点同源实现」必须收敛为单一归属**：字符字面量值有 **3 处**独立实现（静态初值/默认值表达式/表达式求值），全部同款缺陷——改一处漏两处即半修复。**预防：语义工具（字面量解码/类型归一/大小计算）一律单一归属函数（本轮 charLiteralCodePoint），消费点只调用不复制**。
    - **（新增 124）与规范明文冲突的"历史口径"须以规范+参考语言裁决，并先做存量影响面扫描**：宿主「首字节」口径与规范 01b 三冲突（Rust char=Unicode 标量值）——但修复前先扫描存量（grep 转义/多字节字符字面量使用：**零依赖**，86 用例仅 'A'/'0' 单字节两口径同名），确认"修复=纯纠正"后再动手。**预防：改语义前先跑「存量影响面扫描」（使用点清单+两口径差异清单），零依赖即可安全纠正；有依赖则须呈报裁决**。
    - **（新增 125）跨侧（宿主/v2）行为分叉是缺陷发现的富矿——双侧对照探针要覆盖"规范边界形态"**：H3 的暴露完全依赖探针同时覆盖 ASCII（两口径同名）与转义/多字节（分叉）——只测 'A' 则永远绿。**预防：双侧对照探针的形态矩阵必含"规范边界形态"（转义序列/多字节/Unicode 转义/空值/极值），并对每个形态做逐行 diff**（与 94-a 预防 120 同源加强）。
  - **诚实边界**: 字符打印=数值输出（86 用例既有口径，两侧同款）；**v2 字符串字面量 `\u{XXXX}` 未解码**（词法 分词字符串 既有边界，独立登记 C10）。**C7 销项 + C3 顺带销项**；E2E 255（八形态折 255）+ 反证双件全命中（A 宿主回退→241=255−14 精确）+ 探针 3 件双侧一致；固定点 373952 行。

## 96-a v2 字符串字面量 `\u{XXXX}` 解码（C10 销项）（2026-09-13 第九十六轮 深度机 x86_64）

- [2026-09-13 第九十六轮] **问题类型**: C10 缺口（v2 词法 分词字符串 转义慢路径不解码 `\u{XXXX}`——与 95-a 字符字面量同源边界）销项：词法 `\u` 分支 + 内置 `字符转字符串`（0-255 单字节构造）+ `码点转UTF8`（宿主 appendUtf8 同口径）。
  - **实现要点**: v2 无字节级字符串构造原语 → UTF-8 编码经 runtime 单字节函数（`__cn_str_from_char`）**逐字节拼接**（最小实现；宿主 C++ 直接追加字节）；解码在**编译期**（词法阶段）→ 目标产物纯字节串零运行期成本；非法形态（缺 `{`/无十六进制位）保留反斜杠=宿主同款。
  - **预防**:
    - **（新增 126）「无底层原语」时优先复用 runtime 已有最小构造器组合，而非新增后端能力**：v2 无字节级字符串构造 → `字符转字符串`（0-255 单字节）+ 拼接 = UTF-8 编码全表（1/2/3/4 字节）——零后端改动、零新 ABI。**预防：缺能力时先盘点 runtime/内置表现有最小构造器（单字节/单元素/单槽），组合优先于新增**。
    - **（新增 127）Python 注入脚本处理 CN 源码中的 `\u`/`\n`/`\t` 字面量必须用 raw 字符串锚点**：本轮首版锚点 `'// 字符串字面量 "..."：含转义…\n\t…'` 被 Python 转义解释 → 锚点未命中（AssertionError），且 **patch 函数"逐对应用后统一写"=异常时前序修改一并丢失**（词法文件完全未改，构建却"成功"= 用旧源码）。**预防：①含反斜杠的 CN 锚点一律 `r'''...'''`；②多段 patch 改为「逐段立即写盘」（或先全部校验再写），避免半途异常静默丢改动；③改完源码先 `git diff --stat` 确认落盘**（本轮靠二次核对发现）。
    - **（新增 128）「两侧对照门禁」的反证要双向做——宿主侧回退同样须被捕获**：本轮反证 A（v2 失效→v2 校验和 0）与 B（宿主失效→宿主 **16 = 31−15** 精确缺 ①~④）双向均命中，证明门禁对任一侧回归敏感。**预防：双编译对照类门禁的反证清单固定含「宿主侧回退」与「v2 侧回退」两件**（95-a 的 A/B 同款结构）。
  - **诚实边界**: 码点上界未校验（> 0x10FFFF 按 4 字节截断=宿主 char32_t 同边界）；`字符转字符串` 单字节语义（0-255）=宿主 runtime 契约。**C10 销项**；E2E 256（五形态折 31）+ 反证双件全命中 + 探针 2 件双侧一致；宿主零改动；固定点 374986 行。

## 97-a v2 结构体字段数组（C8 销项）（2026-09-13 第九十七轮 深度机 x86_64）

- [2026-09-13 第九十七轮] **问题类型**: C8 缺口（`结构体 行 { 整32[3] 分数; }` 的 `r.分数[0] = 11` v2 语义误拒「下标对象须为字符串或指针: .分数」——成员对象下标只认字符串字段）全链销项：语义放行 + 类型级成员链推导（新 `推导成员链类型ID`）+ 字段地址化 + 下标左值链根重建。
  - **实施要点**: ①语义成员对象分支「字段类型是数组 → 元素类型」+ 左值白名单「可判定且非数组才拒绝」（不可判定放交 IR 判定=判定点后移而非放行静默）；②`生成字段访问` 数组字段**压地址不加载**（宿主「数组字段退化」同款——原按元素宽度加载首元素值当地址 → 下标解引用崩溃）；③新增 `推导成员链类型ID`（类型级三形态递归）统一两个下标 helper；④`生成下标左值地址` 加对象=成员/下标分支（`对象链根索引` 递归找链根 + 基址经 生成左值地址(根, 对象+1) 全链重建）。
  - **踩坑（当场诊断根治）**: 首版下标分支在上层又做一次「字段查询（父结构体=行, 字段名=两行）」——**恒 miss 返回 0**（fld7 探针「字段类型=0」；调试注入显示键基=36 正确、第二次查询组合错误）；修正=删除多余查询层（元素结构体直接经 解析成员链结构体ID(成员) 取得——成员分支已对数组字段返回元素）。
  - **预防**:
    - **（新增 129）「推导链」分层必须单一职责：类型级推导与结构体键级推导不得互相嵌套调用**：首版在「结构体键推导」的下标分支里又做「字段查询」，把两类推导混在一层——父结构体已解析出的「行」被当作「总表」再查字段（恒 miss）。**预防：一个推导函数只回答一个问题（类型是什么 / 结构体键是什么）；跨层需要另一类信息时新开函数（本轮 `推导成员链类型ID` 即为解耦产物），禁止在同层塞入"顺手查一下"**。
    - **（新增 130）IR 层推导失败优先用「调试注入打印中间值」而非读汇编反推**：本轮 fld7 的根因（键基 36 vs 被误用的 30）在 asm/IR dump 里完全不可见（都只是寄存器/槽号）；注入 `IR层诊断("调试: 键基=..., 字段=...")` 三轮即定位（每轮 1-2 分钟）。**预防：IR/语义推导链缺陷先注入诊断（备份+恢复+git diff 复核三步纪律），再看产物**。
    - **（新增 131）新增支持面要让「既有同族探针」与「新探针」同时回归**：本轮在跑新 fld 系列的同时复跑 arr1/arr2/arr5/arr6（数组面）+ v2 自编译（80066 节点）——立即确认「统一推导 helper 替换各形态手写」没有破坏既有数组/结构体面。**预防：改动共享推导/共享 helper 时，回归清单必须同时含"同族新面"与"原消费面"+自编译链**。
  - **诚实边界**: 字段数组越界检查经 推导下标数组长度 覆盖（与 94-a 数组同码）；多维数组仍不支持（两侧同边界）；字段数组元素为字符串时下标读=借用视图（释放面随 C9）。**C8 销项**；E2E 257（八形态折 255）+ 反证双件全命中（A 语义失效→运行失败；B 字段地址化失效→段错误 -11）+ 探针 5 件双侧一致 + v2 自编译回归；宿主零改动；固定点 377843 行。

## 98-a 字符串元素数组释放（C9 销项，双侧）（2026-09-13 第九十八轮 深度机 x86_64）

- [2026-09-13 第九十八轮] **问题类型**: C9 缺口（栈数组字符串元素在块出口/函数尾**双侧同缺**释放——宿主 79-a 靶子面「数组元素残留 2」）销项：按 Rust `[String; N]` 作用域 drop 语义双侧实现（宿主 ir 层 4 处 + v2 4 文件）。
  - **实施要点**: 宿主/ v2 同构——名单（`ownedStrArrayOrder_` / `字符串数组变量们`）+ 块基线 + 编译期展开逐元素（基址 + i×元素大小 → LoadPtr → `__cn_str_free` → StorePtr 0 清槽=幂等）+ 三挂点（块出口/跳出 drop-on-jump/函数返回）。
  - **踩坑五连（含一条 lessons 67 坑再现）**:
    - **(a) 登记点被「提前 return 分支」挡**：宿主登记置于 genVarDecl 后段，而数组初始化列表分支以 `return` 结束=**不可达**（产物零 `__cn_str_free`）→ 移到 `allocVar` 之后（**声明点登记一律放函数前段**）。
    - **(b) 双返回路径只改一条**：v2 返回释放只加在**引用返回**分支，普通返回漏网（mem6「后=2」实证）。
    - **(c) 名单截断假象**：v2 函数尾兜底疑「名单被块出口截断」——实为 (b)；**干净版复核**排除。
    - **(d) 类方法构造点漏同步**（v2 `函数IR` 加字段只同步 生成函数.cn，漏 **生成类.cn**）→ 编译 228 类用例时 **v2p 自身空指针崩溃**——**lessons 67 坑第 N 次再现**，被**全量 E2E 当场拦截**后补同步。
    - **(e) 调试注入会改变行为**：`IR层诊断` 在 v2 **中止编译**（调试版产不出 asm = 「未释放」假象，误导排查两轮）。
  - **预防**:
    - **（新增 132）给 v2 `函数IR` 等共享结构体加字段，必须 grep 全部构造点同步初始化清单**（lessons 67 升级为**机械动作**）：本轮又漏 `生成类.cn`（类方法构造点），症状=v2p 编译类用例时自身崩溃（空指针），**靠全量 E2E 拦截**。**预防：加字段后 `grep -n "函数信息\.指令 = "`（或任一同批字段）列出全部构造点逐一核对；已有 lessons 67 但两次再现=纪律须落到 grep 动作而非记忆**。
    - **（新增 133）登记/挂点类改动先确认「代码路径可达性」——提前 return 的分支会吞掉后段登记**：宿主登记点在 genVarDecl 后段=数组初始化分支 return 之后（零 free 产物实证）；同族风险=任何"声明点登记"（释放名单/类型表/借用表）放在带提前 return 的分支族之后。**预防：登记代码放函数前段（alloc/建槽之后立刻登记），并在 asm/IR 产物层验证「登记确实生效」（grep 期望指令）**。
    - **（新增 134）「多返回路径」面必须逐路径核对（引用返回/普通返回/结构体返回）**：v2 的首版只覆盖引用返回（(b)）。**预防：释放/清理类挂点改动后，逐路径清单核对（普通/引用/结构体/结果 返回 + 块出口 + 跳出），每条路径一个探针**。
    - **（新增 135）调试注入（诊断/打印）会改变被调试系统行为——结论必须用「干净版复核」**：`IR层诊断` 中止编译使产物缺失，导致连续两轮错误结论（「v2 未释放」）。**预防：调试版只用于"定位"，任何结论前先恢复+重建+复跑；把"干净版复核"作为调试流程的最后一步固定动作**。
    - **（新增 136）程序化门禁的"缺失位数精确性"是强验证信号——反证应校验缺失模式而非仅"失败"**：本轮反证 A/B 分别得 **168 = 255−87**（缺 ①②③⑤⑦）与 **40 = 8+32**（保留与释放无关的 ④⑥）——**精确匹配"哪些形态受影响"**，比"用例失败"强得多。**预防：折校验和类用例的反证要预先算出"注入失效后的期望缺失集合"，并把它写入文档/提交信息**。
  - **诚实边界**: 结构体字段数组元素=字符串释放未接（C11）；含串字段结构体数组（C12）；驻留字面量元素不释放（污染模型）。**C9 销项**；E2E 258（八形态折 255）+ 反证双件全命中（缺失位数精确）+ 探针 6 件双侧一致；**宿主+v2 双侧改动**；固定点 380003 行。

## 99-a 结构体字段数组元素释放（C11 销项）+ 宿主缺陷 H4 根治（2026-09-13 第九十九轮 深度机 x86_64）

- [2026-09-13 第九十九轮] **问题类型**: ①C11 缺口（字段数组字符串元素双侧同缺释放）；②探针顺藤**实证宿主缺陷 H4**——字段数组元素步进用 `types::typeSize`（该表无「字符串」返回 0）→ **下标不缩放**（`r.名[1]` 读写恒落元素 0；v2 侧同源正确=宿主单侧分叉）。
  - **H4 实证链**: 探针 fldmem2（`读0=甲/读1=乙/再读0=乙`——写 名[1] 覆盖元素 0）→ IR dump（`加 %v + 0` 而非 `左移 3`）→ 定位 4 消费点（写侧 ir.cpp 3 处 + 读侧 ir_expr.cpp 3 处）→ 统一 `semantic_->typeSizeOf` + `<=0→8` 防御。
  - **预防**:
    - **（新增 137）「类型大小」查询禁止用 `types::typeSize`——该表缺「字符串」等指针类（返回 0），一律 `semantic_->typeSizeOf`**：H4 根因=字段数组元素步进三元表达式对非结构体元素走 `types::typeSize`（字符串=0）；同族 4 消费点（一处有 `<=0→8` 防御侥幸正确）。**预防：全树 grep `types::typeSize(` 审计（本轮 4 处，ir 层）；新写"步进/大小"计算一律 `typeSizeOf` 并带 `<=0` 防御**。
    - **（新增 138）同一语义在该路径的「姊妹实现」要横向对照（局部数组 vs 字段数组）**：局部数组路径用 `typeSizeOf`（正确）而同文件字段数组路径用 `typeSize`（错）——**一个文件内两套写法**是分叉温床。**预防：新增/修改任一"元素步进"点，先 grep 同族实现横向对照（本轮 6 处：ir.cpp 3 + ir_expr.cpp 3），不一致即为缺陷候选**。
    - **（新增 139）「修复即纠正」类改动以全量 E2E 零回归作为存量无依赖证据；仍须在提交信息与文档标注影响面**：H4 影响面=所有「结构体字段数组 + 索引≥1」代码（修复前错位）——全量 287 用例零回归=存量无依赖 ✓；但**下游用户代码可能有依赖**（如以错位行为"碰巧工作"），须显式标注。**预防：纠正型修复一律（a）扫描存量使用点（b）全量门禁（c）提交/文档标注影响面与语义变化**。
    - **（新增 140）反证的门禁设计让「缺失位数」编码"哪些形态受影响"——多注入点缺陷应设计多反证件**：本轮三件反证（A H4 回退→缺①③⑧=133；B 宿主 C11 失效→缺②⑦=66；C v2 C11 失效→缺②⑦=66）——分别锚定"下标缩放面"与"元素释放面"，**两侧失效同码（189）证明宿主/v2 门禁对称**。**预防：一个用例覆盖多个独立机制时，反证件数=机制数（至少每机制一件），并预先算出各自期望缺失集合**。
  - **诚实边界**: 元素=含串字段结构体的字段数组（C12）；字段数组整体拷贝承接既有聚合深拷（本轮未扩）。**C11 销项 + H4 根治**；E2E 259（八形态折 255）+ 反证三件全命中（缺失位数精确）+ 探针 3 件双侧一致；**宿主+v2 双侧改动**；固定点 381935 行。

## 100-a 元素=含串字段结构体的字段数组释放（C12 销项）（2026-09-13 第一百轮 深度机 x86_64）

- [2026-09-13 第一百轮] **问题类型**: C12 缺口（字段数组元素=含串字段结构体的串字段双侧同残留 2）销项——双侧逐元素递归字段释放（97-a × 98-a × 79-a 合龙）。
  - **实施**: 宿主（OwnedStrField.elemCanon + 收集扩展 + emitStrArrayStructFreesAt 逐元素递归）；v2（登记类别 2 + 两处遍历加元素结构体递归分支）。
  - **关键教训（反证形态盲区）**: 首版门禁用例 `行{字符串 名; 点 p;}`（串字段**偏移 0**）——C11 的「元素首 8 字节 free」**巧合正确**（反证 A 未命中，260 仍通过）；改**区分形态** `行{点 p; 字符串 名;}`（偏移非 0：C11 free 到 `p.x` ✗ / C12 递归 ✓）后反证双件全命中（**140 = 255−115** 精确）。
  - **预防**:
    - **（新增 141）门禁用例形态必须能区分「旧机制巧合正确」与「新机制真正正确」——反证未命中时先检查形态区分度**：C12 首版用例（串字段偏移 0）在 C11 机制下巧合正确（free 到首字段句柄）；注入新机制失效后用例仍通过（反证未命中=**假绿**）。**预防：新增机制的门禁用例先做"机制失效注入"自查——若注入后仍通过，立即改形态（加"旧机制会错"的区分维度）**；区分维度例：字段顺序（偏移非 0）/多字段计数/边界值。
    - **（新增 142）「元素首字节/首字段」类巧合通路是缺陷发现的盲区，形态设计要主动打穿**：凡"对元素整体做操作"的机制（free/拷贝/清零），当元素首成员恰是目标字段时都巧合正确——**必须用"目标字段不在首位置"的形态验证**（同族先例：84-d 成员链嵌套深度、88-a 短路净栈）。**预防：聚合元素机制的验证形态固定含「目标字段偏移非 0」一维**。
    - **（新增 143）长文本 Python 注入脚本注意英文双引号与字符串定界的冲突**：本轮 plans 行注入因中文夹杂英文 `"` 导致 SyntaxError（重写一次）。**预防：生成长文档文本的脚本统一用单引号定界 + 文本内英文双引号改中文引号或转义**。
  - **诚实边界**: 字段数组元素深层递归经 79-a 单点自动覆盖（深度 4 防御）；字段数组整体拷贝承接既有聚合深拷（未扩）。**C12 销项**；E2E 260（八形态折 255，区分形态）+ 反证双件全命中（140=255−115 精确）+ 探针 2 件双侧一致；**宿主+v2 双侧改动**；固定点 383269 行。

## 101-a C1 阶段 4' 第三批（A2 正文）销项——字符* 借用视图类型化（2026-09-13 第一百零一轮 深度机 x86_64）

- [2026-09-13 第一百零一轮] **问题类型**: C1 缺口（v2 侧 `字符*` 被置**指针层**→ 按指针路径处理，**打印指针值**=借用视图类型未类型化；`类型*[N]` 指针数组缺失）销项——A2 语义（`字符串`=拥有/`字符*`=借用视图）全链落地与验证。
  - **实施**: ①`解析类型` 对「基类型=字符 + 单 `*` + 无引用」并入合成名 `"字符*"` 清零指针层（多级 `字符**` 保持指针）；②指针数组 `类型*[长度]` 合成（类型位）+ 声明位五 token 前瞻。
  - **踩坑（当场修正）**: **消费顺序**——首版把「字符* 特判」置于「指针数组后缀」之前：`字符*[2]` 被特判先消费（清指针层）→ 数组后缀条件（指针层>0）落空 → 声明解析失败。修正=数组后缀先于特判。
  - **预防**:
    - **（新增 144）类型位多形态后缀（`*`/`[N]`/`&`）的消费顺序必须显式设计并测试组合形态**：`字符*[2]`（借用视图×数组）与 `整32*[3]`（指针×数组）在同一段代码——特判清零指针层后，后续依赖"指针层>0"的分支即失效。**预防：后缀消费链按「最具体形态优先」排序（数组后缀先于类型特判），并加组合形态探针**（`字符*[2]`/`字符**`/`整32*[3]`）。
    - **（新增 145）双侧对照探针的"形态盘点"要先于缺陷定位——一处分叉往往连着多个同族缺口**：cstar1（字符* 打印分叉）→ 盘出 cstar2（八形态）/cstar4（驻留文本）/cstar5（指针数组）共 5 探针，暴露 2 个 v2 缺口（类型化 + 指针数组）。**预防：发现分叉先写"全形态矩阵探针"（声明/参数/返回/字段/数组/边界），再逐形态定位**。
  - **诚实边界**: `字符*[N]` 元素写经数组左值链承接；`字符**` 维持；A2 迁移面收紧登记 C13（风格精化）。**C1 销项**；E2E 261（八形态折 255）+ 反证双件全命中（A 特判失效→段错误 -11；B 指针数组失效→语法错误）+ 探针 5 件双侧一致；宿主零改动；固定点 384552 行。

## 102-a 多文件静态初始化注入（C4 销项）+ 宿主缺陷 H5 根治（2026-09-13 第一百零二轮 深度机 x86_64）

- [2026-09-13 第一百零二轮] **问题类型**: ①C4 登记「仅入口模块注入静态初值」经反证核验**在现架构下不成立**（宿主全量合并单模块 + v2 全树合并）；②探针顺藤**实证宿主缺陷 H5**——类/容器静态「有初始化表达式」路径缺 `StorePtr`（对象已构造但指针未写入 .data 槽 → 空指针崩溃；对任何模块成立）→ 根治。
  - **H5 机制链**: 探针 multi4（非入口模块容器静态）→ `打印("初始大小=", 库::取大小())` 运行时错误(错误码3) → IR dump（主入口有 NewObject+构造但**无 StorePtr**）→ 定位 `emitStaticInitsAtEntry` 的 ④ 分支（有初值路径 `obj=genExpr(...)` 后落尾端**标量族** `isScalarStatic=false → continue`）→ 补 StorePtr。
  - **预防**:
    - **（新增 146）对拍/固定点验证必须独立串行，不得与其它重负载链交错**：本轮首跑对拍 13 DIFF 为**假象**（与锚定链交错的状态污染）——手动单组件复现 diff=0 + 独立重跑 34/34。**预防：对拍跑前确认无并行链、复用同一 work 目录时先清残（本轮 work 复用 p101 目录需先清 v2asm.s 残留）**；发现 DIFF 先做"单组件手动复现"再定性。
    - **（新增 147）「多分支生成函数」的尾端统一处理是漏网温床——每个分支的出口必须显式**：`emitStaticInitsAtEntry` 的类/容器分支「有初值」路径依赖尾端标量族段的 StorePtr（而该段以 `continue` 过滤非标量）→ 静默跳过。**预防：生成函数的每个分支要么自闭合（自带 continue/return + 完整处理），要么显式注释其依赖的后续段；"依赖尾端统一处理"的分支必须验证尾端过滤条件与自己相容**（同族先例：98-a 登记点被提前 return 挡、101-a 消费顺序）。
  - **诚实边界**: 静态聚合（数组等）初值注入维持「宁漏勿错」；静态槽不登记自动释放（D4）。**C4 销项 + H5 根治**；E2E 262（八形态折 255，借链）+ 反证双件；探针 multi4 三形态双侧一致；宿主层修复（v2 零改动→固定点不变 384552 行）。

## 103-a v2 诊断行号落地（D2 销项）+ 契约迁移（2026-09-13 第一百零三轮 深度机 x86_64）

- [2026-09-13 第一百零三轮] **问题类型**: D2 缺口（v2 语义诊断无行号——语法层早有「（行=N）」而 sem5 面缺）销项，含**三机共享契约迁移**（§8.4）。
  - **实施**: 节点加 `行` 字段（默认 0=未知，增量填充=存量零影响）+ 解析块**单点回填**（语句根）+ 检查语句**单点设诊断行** + `表诊断` 输出「（行=N）」（行>0 时）——**性能零开销**（不引入任何运行期成本；行号只在编译期诊断路径消费）。
  - **契约迁移踩坑**: 迁移脚本用**裸文件名**（cwd=隔离目录跑 v2p）→ 输出 `[p6h] 已解析: 主.cn …` ✗ 而 runner 用**项目根相对路径**（`target/audit2/v2src<N>/主.cn`）→ 首版 expected 全部不匹配（139 报"缺少期望行"）→ 加路径前缀归一修正 ✓。**契约迁移脚本必须模拟 runner 的真实调用形态**（调用参数/相对基准/输出捕获三者都要对齐）。
  - **预防**:
    - **（新增 148）共享契约类改动（.expected/runner/规范）的「影响面盘点 → 迁移脚本 → 全量复跑」三步不可省**：本轮 20 文件/55 行迁移靠脚本批量完成；盘点=按诊断模式 grep 受影响 .expected；迁移=模拟 runner 环境重跑取实测；复跑=全量 E2E 零回归。**预防：契约改动前先 `grep -rl <受影响模式>` 出清单并写入文档（影响面可审计）**。
    - **（新增 149）「增量字段 + 单点挂点」是给既有数据结构加能力的低风险范式**：节点加 `行`（默认 0）+ 两个单点（解析块回填 / 检查语句设行）——**未改任何既有构造点**（75 处 建节点 零改动）、**未改任何既有输出**（行=0 走原路径）→ 存量零影响、门禁零回归。**预防：给高频结构体加信息时优先"默认值 + 增量填充 + 消费点单点"，避免全构造点签名改造**（对照 98-a 的 函数IR 加字段教训：那必须全构造点同步=高风险）。
  - **诚实边界**: 行号=语句根粒度（表达式级/列号不做）；行=0 保持无行号（兼容）。**D2 销项**；全量 E2E 290 用例零回归（20 迁移用例全绿）+ 反证 A 命中（行挂点失效→139_v2/252_v2 失败）+ 探针 d2a/d2b；宿主零改动；固定点 386463 行。

## 106-a C2 跨语句 NLL 评估 + C13 借用返回精化评估（纯评估轮）（2026-09-13 第一百零六轮 深度机 x86_64）

- [2026-09-13 第一百零六轮] **问题类型**: 评估轮——把「待数据登记项」（C2/C13）转为「评估现场」时的探针脚本踩坑四类 + 评估结论的数据化模式。
  - **踩坑 1（正则嵌套拼接失衡）**: Python 拼接正则手写为 `(?:[=;,)]))` → `re.error: unbalanced parenthesis`（字符类含 `)` 时层次数错，改了三次才对）。**预防：正则含字符类的嵌套组一律改用无嵌套形态**（可选结尾 `[=;,)]?` 替代 `(?:[=;,)]? )`），或先在 REPL 单独验证 pattern 再入脚本。
  - **踩坑 2（match/search 语义差异致假零）**: 去掉行首锚点 `^\s*` 后仍用 `re_decl.match(ln)` → 缩进声明行全不匹配 → 统计值假零（真借出 80→0）。**预防：正则改动（尤其去锚点）必须同步检查匹配 API（match=行首锚定 / search=任意位置）；统计值突变（80→0）先疑脚本而非数据**。
  - **踩坑 3（指标口径未对齐语义面致大量假阳性）**: 首版把**所有** `.元素()/.读取()/.获取()` 调用算作「借出绑定点」→ `向量<节点> 池.元素(f)` 全量计入 → 342 处假阳性；77-a 面内仅**字符串元素容器**（诚实边界第 4 条：元素为值语义/无所有权语义）。**预防：探针指标定义必须逐条对齐检查器的语义边界（先读检查器头部「诚实边界」清单定义指标），再做类型过滤；假阳性先做样本人工判读（本次 12 样本全错=口径错）**。
  - **踩坑 4（跨函数同名变量误配）**: 正则层「最后使用行」按全文件向后扫 → 同名变量在**其它函数**中的使用被误计为同一活跃区间（假阳性 136→92 靠函数边界限定收敛；v2 树顶格函数常带 `不安全` 修饰前缀，首版边界判定漏判）。**预防：行序近似类分析必须做函数/块作用域配对；边界判定覆盖修饰前缀形态（`不安全|静态|公开|私有|导出`）**。
  - **踩坑 5（Edit 局部替换表格行致行尾残留）**: 用 Edit 替换看板表格行时 old_string 只取了行首片段 → 替换后**行尾残留旧通告文本**（行长 1062 异常），立即发现并用脚本截断修复。**预防：表格行/长文本行的整体替换一律取整行（含行尾 `|`）作 old_string，或直接用脚本按行号重写；替换后校验行长/行尾锚点**。
  - **预防（新增 150）**: **「待数据登记项」的评估交付三件套 = 指标口径（对齐语义边界）+ 精度边界声明（近似层次与下界）+ 量化单位（μs 或占比）**——结论可以是「维持登记」，但必须有可复核的数字与可重跑探针（本次：C2=645/79/0、C13=22函数/93调用点/~10μs）。**先写"结论需要什么数字"再设计探针，避免"测了但答不了问题"**。

## 107-a v2 缺陷 H6 根治——可选/结果 裸「无」全链（2026-09-13 第一百零七轮 深度机 x86_64）

- [2026-09-13 第一百零七轮] **问题类型**: D5 面评估探针实测发现的 v2 真缺陷（`返回 无` 段错误）——含**「早期简化表示」的潜伏缺陷**与**求值路径把值当地址**的通用陷阱。
  - **根因**: v2 语法层把关键字「无」解析为 **节点_整数字面量（值 0）**（早期简化：空指针=整数 0）→ 返回位/声明位把该「0」当**结构体地址** → `IR_结构体拷贝(源=0, …, 16 字节)` 自 NULL 读 → 段错误（asm 实证 `mov rsi,[rbp-16]=0; rep movsb`）。宿主侧「无」是独立空值节点 → 返回位构造零值结构体 ✓ 无此问题。
  - **诊断手法（可复用）**: ①**双侧最小复现**（d5f_min 双侧对照：宿主 ✓/v2 rc=139）→ ②**gdb 定位崩溃点**（`_E980A0+46` 在 rep movsb）→ ③**asm 逐指令判读**（源寄存器=0 即「值当地址」）→ ④**语义层三处同根因核对**（返回位/声明位语义/声明位 IR）→ ⑤**歧义实证定特判边界**（宿主拒绝 `返回 0`/`可选<整64> r = 0` → 合法源码该位字面量只可能是「无」）。
  - **修复范式**: 「语法层早期简化表示」的缺陷，优先在**消费点判据**加特判（本轮：返回位/声明位「整数字面量=无」零初始化），而非改造语法层表示（新节点类型=全链分派改造，登记为可选重构）——**判据边界用宿主行为实证**（拒绝形态=无歧义来源）。
  - **预防（新增 151）**: **「值语义表示」与「地址语义消费」交界的形态必须双向实测**——任何「表达式求值结果被当指针/地址使用」的路径（结构体拷贝源/指针初始化/解引用），都要有「值为 0 / 字面量」形态的探针（本轮 `返回 0` 形态久未暴露=探针矩阵缺位）；**测试用例族缺形态 = 缺陷潜伏期**（v2 用例族 211/231/230 覆盖构造器形态但缺裸「无」——补齐后 263 门禁固化）。
  - **预防（新增 152）**: **D5 类「登记边界」的评估轮必须做探针矩阵（≥10 形态）而非单点验证**——本轮 15 探针中 4 个暴露缺陷/缺口（返回位崩溃/声明位拒绝/赋值位拒绝/嵌套链保守），单点验证（只测「结果×三元」）会得出「无缺陷」的错误结论。**评估轮的价值=把「边界」翻译成「可判定矩阵」并逐格取实测值**。

## 108-a D5 剩余面固化——赋值位结果/可选 构造器保守拒绝负测（2026-09-13 第一百零八轮 深度机 x86_64）

- [2026-09-13 第一百零八轮] **问题类型**: 评估后「保守拒绝维持」的规范化处理（非缺陷固化）。
  - **预防（新增 153）**: **「保守拒绝 + 语料零使用 + 完整实现需设施级改造」三者同现时，正确做法=拒绝行为负测固化（防误放行）+ 架构边界登记（不为其打单形态补丁）**——赋值位结果/可选 构造器：完整实现需「联合体条件释放」设施（84-e 面外），若只放行语义不补释放面 → 静默泄漏；本轮的替代=**负测 264 把「当前拒绝行为」写成契约锚**（诊断锚行 + 退出码=负）——将来设施到位、有意放行时，负测同步迁移=变更受控。**判据：放行的前提是"该形态的所有权语义闭环"，而非"宿主也支持"**（宿主支持=等价性参考，不构成放行充分条件）。

## 第一百零九轮踩坑（2026-09-13，家机 win-x64：93-a~108-a 跨机轮 + 109-a 修复）

- **MASM 的 movsd 双义**（字符串指令 vs SSE 标量移动）：内存操作数**必须**带 size 说明符
  （`movsd qword ptr [mem], xmm0`），否则 ml64 报 A2070「invalid instruction operands」——
  隔离实测：`movsd xmm0, [mem]` 报错、`movsd xmm0, qword ptr [mem]` 合法。**linux GAS 无此要求**
  → 三后端温床（91-a 增浮点分派时仅浮32 加了 dword ptr，浮64 漏加 → 跨机轮才暴露）。
  预防：win 后端新增任何 SSE 内存操作数发射时一律显式带 `qword ptr`/`dword ptr`；
  双编译器 E2E 用例（win 平台守卫外）是唯一拦截面。
- **v2 编译隔离 cwd**：v2 driver 输出 `target/v2asm.asm` 为**相对 cwd** 路径——多会话/多工具
  并行时在独立目录跑（含 `stdlib` junction 指向项目 stdlib），彻底避免共享写入冲突。
- **v2 树版本二分法**：`git archive <sha> "CN语言编译器v2" | tar -x -C <dir>` + **当前宿主**编译
  → 精确定位"v2 源码维度"的引入轮次（~2.5 分钟/次）；配合"同 v2 源码换宿主"可分离
  "宿主维度"与"v2 源码维度"两个变量。
- **平台分叉指纹三件套**：win 崩/linux 绿 + 换无关变量即改变结果 + 版本二分"某提交一刀切"
  ——同时出现时优先按"内存状态敏感/UB"排查（而非逻辑错误）；148_v2 与 252 均为该族。
- **Git Bash 直调 ml64/link 被 `/nologo` 路径转换坑**（前轮已有教训，本轮复现）——
  一律 python subprocess 调用，勿经 bash 拼接。

## 第一百一十轮踩坑（2026-09-13，家机 win-x64：110-a win 侧五项立案收口 + 宿主 win 构建根治）

- **陈旧产物假立案（本轮最大教训）**：上轮门禁/探针**全部跑在陈旧宿主上**——宿主构建被 MSVC
  `/WX` 以 C4702 拦截（构建失败），但失败未被检查，后续全量 E2E 与 11 项实物探针照跑 →
  产出 3 项假立案（252/260/262）+ 2 项宿主侧误判（250 rc=127 / 251 A2032）。预防：
  (a) **每次门禁/探针之前先跑构建并核对退出码与日志尾部**（不要只看后续输出）；
  (b) 核对 `cn.exe`/`v2p` 时间戳晚于最新源码修改时间；(c) 构建失败必须**当轮根治**，
  禁止「先跑其他再看」。
- **MSVC C4702（unreachable code）在 GCC 下不报**：102-a 在 if 分支末尾加无条件 `continue`
  使后方代码死掉——linux（GCC）零感知全绿，win（MSVC /WX）直接构建失败（C2220）。
  预防：改动控制流后自查「后置代码可达性」；跨平台改动后 win 侧**必须清场重建**
  （不要复用旧 binary）。
- **「顺手删守卫」式修复的风险**：102-a 修复 H5（补 StorePtr）时用无条件 `continue` 替代原有
  「标量族守卫判定」（isScalarStatic）——修复了目标缺陷但**丢失安全边界**（数组等聚合会被
  8 字节 StorePtr 越界写）。预防：修复「某分支漏处理」时，先确认原分支的**守卫/边界判定**在
  新结构下仍然生效（Rust 对照：match 穷尽性 + 显式 else 分支审计）。
- **v2 三后端 prologue 分叉（结构体按值栈参数）**：91-a 双计数装载改造后，`类p==2`（栈参数）
  分支只处理浮点/标量，**结构体按值参数被截获**（只存 8 字节指针）——寄存器路径
  （`拷贝字节>0`）的 rep movsb 逻辑在栈路径缺失。win 触发面=4 寄存器（158 用例 6 结构体
  → 2 走栈）；SysV/AAPCS64 需 ≥7/≥9 结构体才触发（潜在面）。预防：移植/改造参数装载时对
  「来源（寄存器/栈）× 种类（标量/浮点/结构体）」矩阵逐格核对；**宿主三后端同形态实现是
  现成参照**（统一拷贝）；拷贝逻辑提取单点助手（本轮已做）。
- **E2E 与对拍/锚定链不可并行**（本轮实测）：两者共享 `target/v2asm.asm`（ROOT cwd 相对
  路径）——E2E 在 142_v2 处因 PermissionError 中断（对拍正写该文件）。预防：对拍/锚定链/
  探针与 E2E 串行，或统一隔离 cwd（ISO 目录 + stdlib junction）。
- **探测器的有效性也是被测对象**：`scan_size.py`（movsd/movss 裸内存操作数扫描）首版计数占位
  （-1）+ 误扫 v2p110.asm（编译器代码，movsd 发射模板为 0 行≠编译产物）——经「真实产物
  （250_v2.asm 194 行 movsd 全带说明符）+ 注入反证（构造违规行判定=True）」两项校验后才可信。

## 第一百一十一轮踩坑（2026-09-13，家机 win-x64：111-a E2E runner win 双编译对照原生支持 + size 说明符扫描门禁 + 新缺陷 221 双侧收口）

- **宿主 win 后端 epilogue 返回装载缺类型宽度分派（三后端分叉）**：`mov rax, [slot]`（8 字节读）
  使 ≤32 位返回值的 rax 高 32 位携带栈垃圾（ABI 只保证窄返回值低位有效）——linux/arm64 后端
  均按类型宽度装载（`emitStackLoad(..., currentReturnType_)`；linux 实测 `mov eax, [slot]`），
  win 后端单侧分叉。触发前置=消费者按 64 位使用该值（v2 win 后端 `cmp rax, 0`）→ 首次 win 真跑
  双编译对照用例时暴露（221：映射$包含 布尔返回被高位栈垃圾污染 → 校验位丢失）。
  预防：三后端「返回装载」一律按类型宽度分派（本轮已修 win 对齐另两后端）；改动 ABI 相关生成
  后对照另两后端同段实现；**「平台守卫解除」时须把守卫期积压用例视为未验证面**（本轮首跑即捕获）。
- **窄返回规范化：扩展方式必须匹配「被规范化方」的内部值表示**：v2 首版对 T_整32 用零扩展
  （mov eax, eax）→ **自编译退化**（cn_self111 编译 v2 全树只产出 12969/406115 行空壳函数、
  IR 生成为空）。根因=v2 内部整32 为**符号扩展表示**（类型转换生成 `mov eax`+`movsxd`；探针
  实证 `整32(-1)` 经调用返回 = -1），零扩展把 -1 变 4294967295 破坏语义。改 `movsxd rax, eax`
  后固定点恢复。预防：写「规范化/扩展/截断」指令前先**实证该编译器的值表示约定**（读类型转换
  生成代码 + 写最小探针）；**锚定链是唯一能发现「自编译层退化」的判据**（E2E 全绿也发现不了
  ——v2p 正常，唯自编译链退化）。
- **「自编译退化」诊断法**：症状=自编译产物编译大输入时**函数体为空壳**（IR 空）；判据=锚定链；
  定位=**停用/启用二分**（把可疑条件改恒假 → 只留单一类型 → 逐类型放行，每轮 ~4 分钟）；辅助=
  残缺产物函数行数分布（正常中位 342 行/函数 vs 空壳 17-22 行）。勿「全类型一起上」——按类型
  独立触发才能快速二分。
- **反证双向对照法（不依赖旧二进制）**：改 **asm 产物**模拟缺陷（`mov eax, [slot]` → `mov rax,
  [slot]` 重新汇编 = 脏返回 obj）+ 从产物**删除**修复指令模拟「无修复」→ 254 复现 / 255 通过。
  比找旧二进制或回退源码更快且可重复（产物级注入/剔除）。
- **win 无后缀 exe 不可直接运行**（`CreateProcess` WinError 2——即使文件存在且为 PE）——双编译
  对照宿主侧输出须 `.exe`；宿主 build 中间产物 asm 落在输出路径旁（**stem + `.asm`**，非
  `out.exe.asm`）。
- **E2E 产物隔离 = 并行安全的前提**：win v2 产物按用例 cwd 隔离（`v2work<编号>`）后，v2 桶由
  串行放开为统一并行；31 个双编译对照用例转真跑后全量 ~35 分钟（jobs 8）。隔离三件套=独立 cwd
  + stdlib junction（`mklink /J` 无需管理员）+ 入口绝对路径（日志比对前把绝对前缀适配回相对）。

## 第一百一十二轮踩坑（2026-09-13，家机 win-x64：112-a D1 行数整改第三波——ir_expr.cpp 2798→734 行按族拆分）

- **匿名命名空间是拆分的隐藏依赖点**（104-a/105-a「static 辅助函数跨族即禁拆」教训的升级版）：拆前必须做
  「匿名 ns helper × 族」依赖矩阵扫描（helper 是内部链接，跨文件不可见）——`semantic.cpp` 的 `canonicalType`
  被 sig/layout/主/builtins **四族共用** → 不能跟着任何单族走（须 inline 头化 = 独立一波）；而
  `findTopLevelComma` 只被 types 族用 → 可随族走。**判据：helper 引用族数 == 1 → 随族；≥2 → 头化**。
- **行级多重集校验法**（拆分完整性硬校验）：拆出后核 `Counter(主文件保留行 + 各新文件行) == Counter(原文件全部行)`
  ——一行不多一行不少无错位（比 diff 目测可靠；104-a/105-a 的手工核对升级为脚本校验）。
- **产物 asm 多样本逐字节等价**（重构验证的最强低成本手段）：重构前后各编同批样本（**大输入如 v2 全树
  40 万行 asm** + 各特性面用例），md5 逐字节比对——覆盖面远强于单点 v2p md5，且能同时抓住「生成逻辑错位」
  与「条件编译/警告抑制差异」。样本至少含「最大输入 + 每类特性各一」。
- **拆分脚本必须按原始换行符处理**：`read_bytes` 读入 + 显式 `

` 拼接（项目源码为 CRLF）——用
  text 模式会全文件换行归一 → 制造零信息量的巨大 diff。另：Python `str.format` 与 C++ 代码里的 `{`/`}`
  冲突（`namespace cn_compiler {`）→ 生成模板用 `replace` 占位符而非 format。
- **拆分不变量**：新文件 include 块 = 主文件同款（多余无害）；族内函数只能引用「类成员（hpp 声明）+
  同文件匿名 helper + 本文件 include 的标准库」三者——超出的即拆错边界。

## 第一百一十三轮踩坑（2026-09-13，家机 win-x64：113-a D1 行数整改第四波——ir_oop.cpp 1381→508 行按族拆分）

- **后台长任务必须单独一条命令启动**：`前置命令 && nohup 长任务 > log 2>&1 &` 的后台链在工具调用
  返回后未实际启动长任务（本轮实测：单测 rc=0 但 E2E 从未运行——日志文件都不存在，导致一轮空等）。
  预防：长任务一律 `nohup ... > log 2>&1 &` **单独**调用；启动后**立即核验日志文件已创建** +
  隔时查进程存活，再进入等待。**不要用 `&&` 把「准备命令」与「后台任务」串在一条调用里**。
- **同模式拆分的复用收益**（112-a → 113-a 实测）：族边界勘定脚本化（匿名 ns/static 扫描）+
  行级多重集校验 + 产物 asm 多样本等价 + CRLF 感知生成脚本，使第二个文件的拆分成本从 ~2h 降至
  ~40min——**方法论沉淀的第一价值是「下一轮更快」**；同类轮次应收敛为标准 checklist 执行。

## 第一百一十四轮踩坑（2026-09-13，家机 win-x64：114-a D1 行数整改第五波——ir_oop_call.cpp 1155→763 行）

- 本轮**无新坑**（112-a 方法论第三次复用：族边界勘定含「static 引用点唯一性」检查——`blockExitSrcName`
  仅被族内两处引用 → 随族走；`findClassMethod`/`operatorSymText` 被非拆段引用 → 留主文件）。
  三条 D1 拆分轮（112-a~114-a）验证了 checklist 的完备性：①匿名 ns/static 依赖矩阵 ②行级多重集校验
  ③产物 asm 多样本等价 ④CRLF 感知生成脚本 ⑤全量门禁。**教训：标准化的重构 checklist 让第三轮零意外**。

## 第一百一十五轮踩坑（2026-09-13，家机 win-x64：115-a semantic_internal.hpp 共享头——11 helper × 5 文件去重）

- **扫描正则必须覆盖全部函数定义形态**：首轮「匿名 ns 函数」扫描正则漏掉两类——`[[maybe_unused]] int
  arrayTotalSize(...)`（属性前缀）与 `std::vector<std::string> funcPtrParams(...)`（模板返回类型）→
  重复面被低估为 9 个（实为 11 个）。预防：**宽判据**（匿名 ns 区间内「列 0 起始 + 含 `(` + 非注释/预处理行」）
  + 扫描结果**人工复核**（对照文件实际结构抽查）；凡「统计类」脚本结果先自问「判据是否覆盖全部形态」。
- **「删除段 ≡ 新位置内容」校验法**（去重/搬迁类改动的硬校验）：把每个文件的删除段与目标位置内容各自
  规范化（去注释/空白/注记差异）后逐行多重集比对——本次 5 文件 × 60 行全等，一次坐实「纯去重」。
  比 diff 目测可靠，且能抓住「删多/删少/错位」。
- **头文件化匿名 ns helper 的等价替代模式**：原内部链接 → 头文件 `inline`（ODR 齐一、多 TU 安全、
  未使用不警告）；**前提是先用全项目扫描确认无同名外部链接定义**（本轮 11 个名字只在 semantic 目录出现）；
  调用点零改动（同名可见）。Rust 对照：rustc crate 内共享工具模块（rustc_middle/rustc_index 模式）。

## 第一百一十六轮踩坑（2026-09-13，家机 win-x64：116-a semantic_expr.cpp 1778→654 行按族拆分）

- 本轮**无新坑**——115-a 的 helper 头化使 semantic 系列的族拆分成本显著下降（族内无匿名 ns/static 依赖 ⇒
  纯「成员函数实现搬迁」）。D1 拆分 checklist（112-a 立、113~116-a 四度复用）已收敛为标准流程；
  **前置投资（共享头/依赖矩阵）对后续每一波都复利**——这也是 Rust 式「先建共享模块再拆文件」的工程收益。

## 第一百一十七轮踩坑（2026-09-13，家机 win-x64：117-a semantic.cpp 1996→692 行按族拆分）

- 本轮**无新坑**——第七度复用 D1 checklist（本波特有点：**族跨越文件中间段**——types 族由 [295,523] 类型解析与
  [961,1082] 布局两段组成，拆出后新文件内两段拼接、主文件三段拼接，多重集校验覆盖 ✓）。
  **115-a 的 helper 头化是本波零障碍的直接原因**（仅 cnEvalConstExpr 因被主段 visitProgram 引用而留主文件）——
  再次验证「先建共享层再拆文件」的工程次序价值（Rust 式：共享模块先行）。
- **经验固化**：含多段族/跨段拼接的拆分，「行级多重集校验」是唯一可靠的完整性保证（目测必错）。

## 第一百一十八轮踩坑（2026-09-13，家机 win-x64：118-a x64_instructions.cpp 1812→921 行按族拆分）

- **LNK1168「无法打开 cn.exe 写入」= 同机另一会话占用**（LNK1104 同族）：本轮首次构建失败，`tasklist /FI
  "IMAGENAME eq cn.exe"` 查出 **8 个 cn.exe 进程**——另一 ZCode 会话（`target/probe_b345/scan_warn.py`，
  启动时间早于本机看板认领）正并发跑 `cn.exe check`（v2 全树）。**处理原则**：对方为验证轮（免认领）且先启动，
  **不干扰**——轮询等其释放（~3 分钟）后重试构建成功。**预防**：构建/链接失败先查占用进程（`tasklist` /
  `Get-CimInstance Win32_Process` 看 CommandLine 必含目标路径），**不要先怀疑自己的代码**；跨会话并行时
  构建类操作应预期偶发占用（重试+等待即可，非缺陷）。
- **经验**：`Get-CimInstance Win32_Process` 的 CommandLine 是判定「谁在用什么」的决定性证据（含目标文件路径
  与启动时间）——比猜「是不是我自己的残留」可靠。
- **边界勘定新形态**：族区间之间可能夹着**单行空行**（本轮 L1113）——分段覆盖校验（六段总和=原行数）能立即
  抓住「漏分配一行」；**多重集校验在写盘前拦截**（本轮第一次尝试即被拦，文件未被破坏）。

## 第一百一十九轮踩坑（2026-09-13，家机 win-x64：119-a codegen 三后端装配文件按族拆分）

- **非 win 目标改动的等价性验证 = stash 对照法**（本轮新方法论，重要）：三后端装配文件（x64/linux_x64/arm64_codegen）
  的改动**无法被既有 win 五样本覆盖**——采用 `git stash` 暂存改动 → 构建**旧宿主** → 生成 **win + linux-x86_64 +
  linux-arm64 三目标 × 2 样本**产物 asm 基线（win 项与既有基线一致 = 流程自检）→ `git stash pop` → 构建**新宿主**
  → 同样生成 → **6 项全部逐字节一致** ✓。成本 ~6 分钟（两次增量构建 + 6 次产物生成），**比 worktree 重建快且更聚焦**。
  预防（推广）：凡改动「只影响非本机后端的代码路径」的宿主文件（linux_x64/arm64 后端、SysV/AAPCS64 相关），
  验证必须显式补「目标平台产物对照」——win 门禁绿灯**不覆盖**这些路径。
- **分段覆盖校验**（118-a 立、本轮复用）：拆分区间之间的单行空行等易漏行——「各段行数总和 == 原行数」的
  覆盖校验 + 写盘前多重集拦截，是零事故的保证。

## 规则强化（2026-09-13 用户令·最高优先级：尽善尽美禁止妥协——折中判定清单／推倒重来授权／「最完美方案」呈报要求）

- [2026-09-13 用户令] **问题类型**: 协议级规则（用户直接指令，纠偏「AI 反复产出妥协型建议/产物」的系统性倾向），**✅ 即刻生效（写入 AGENTS.md §3 深化版 + plans/022 落地范本）**
  - **背景**: 用户在多轮呈报中持续发现 AI 给出「折中／保守／维持现状＋登记观察」型建议与产物（B3/B4/B5 三项呈报即为其典型：AI 首轮给的是「三选一折中项」，用户令明确要求改为「合三为一的体系工程＋最完美形态」）。
    用户令原文要点：「尽善尽美，禁止妥协（最高优先级）……只要能提升 CN 语言编译器性能、增加 CN 语言编译器安全的，我们应该进行到底，十全十美，而不是对现有实现路径进行妥协。现有实现不完美都可以推倒重来」。
  - **规则要点（AGENTS.md §3 第一条深化版全文）**: ①**折中即妥协**——以「保守分支／宁漏勿错／暂时保守／巧合正确／维持现状＋登记／既有边界／下轮再说／范围外／影响面大／风险可控」名义搁置或降级任何**已确认**的缺陷、缺口、半截机制（只覆盖部分位置/类型/路径的设施）＝妥协，一律禁止；②**双目标优先于进度**——v2 双目标（性能超 C++／安全接近 Rust，内存安全零容忍）是最高准绳，不得以工作量大、涉推倒重来、跨多轮为由降级方案；③**推倒重来授权**——既有实现不完美（半成品/临时手法/类型专用补丁/文本判定脆弱性）时，原设计目标的完整体优先于保留现状（须：呈报获批准＋门禁全绿等价证据＋完整验证链）；④**呈报必须给最完美方案**——多方案对比时推荐项必须是最完美者，折中项只能作对比、不得作推荐；「先做一半／维持现状＋登记」不是方案（分步只能是实施路线，且每步完整闭环）；⑤**诚实边界 ≠ 放弃**——边界必须同时登记为带根治计划的排期任务并按期销项，自检问句「这条边界什么时候关死？关死之前它算不算妥协？」。
  - **落地范本（plans/022）**: 五层所有权模型（①默认深拷零规则 ②显式转移真 move ③借用只读 ④显式克隆 `复制(x)` ⑤编译器自动省略）＋ B3/B4/B5 三项最完美形态（资源字段统一化／真 move 设施补完＋统一克隆／安全边界一次性收口）＋ 推倒重来清单 3 项（转移第一版 AST 改写／字符串专用资源字段设施／观察期文本后缀判定）＋ 波次路线（每波独立全链门禁）＋ 十全十美验收标准（plans/020 矩阵全格闭环、不安全清单 100% 拦截、无「保守分支」占位）。
  - **预防（后续 AI 自检三问，呈报前必过）**: ①「我推荐的这条是最完美形态吗，还是最省事的？」——若列举了「甲/乙/丙」而推荐不是最彻底者，自我否决重写；②「我的方案里有没有『保守分支／宁漏勿错／暂时/下轮/范围外』字样？」——有即改（要么当轮根治，要么登记为带期限的排期任务）；③「这条『诚实边界』什么时候关死、关死计划在哪？」——答不出即属妥协，先补计划再呈报。
  - **与既有规则的关系**: 本规则**不削弱**流程纪律——新增语法/语言级语义仍须先呈报获批（AGENTS.md §2.3），「呈报」只是审批门槛，不是降级理由；「报告类产出等批阅」同样维持。用户批准后的实施必须做到极致（全链＋双侧宿主/v2 同构＋门禁＋反证＋跨机实物）。
  - **权重**: 10（协议级最高权重，用户直接指令）

## 第一百二十轮踩坑（2026-09-13，家机 win-x64：120-a v2 侧开波——生成表达式.cn 1512→738 行）

- **v2 侧拆分的三条硬约束（勘定结论，后续 v2 波次必读）**：①**静态变量=文件级私有**——族不得跨静态耦合
  （IR容器.cn 12 个静态变量 + `IR追加/建IR指令` 被 8 模块共用 → **不可直接拆**，须先做「工具函数下沉基础
  模块」的独立重构；生成表达式.cn **0 静态** → 首选开波对象）；②**同包「多文件合并全局可见」**——同包模块
  函数互调**无需改导入**（与常见模块系统不同，v2 注释实证「主链靠多文件合并全局可见掩盖」），故
  「主文件↔族文件互调」不构成循环依赖障碍；③新模块须在 `包.cn` 按**加载序（依赖先解析）**注册。
- **v2 侧产物等价验证法（新口径）**：与宿主侧「五样本 asm」同理——**采 v2p 编译用例的 asm 基线**（4 样本，
  跨容器/数组/字段数组/基础形态）→ 拆分+重建 v2p → 同样编译 → **逐字节对比**。本轮 4/4 一致 +
  **锚定链固定点 406097 意外不变**（v2 拆分不改语义 → v2 自编译产物逐字节不变——比预期更强的证据）
  + **对拍组件数 34→36 自动纳入新文件**（组件对拍面自动随树扩展）。

## 第一百二十一轮踩坑（2026-09-13/14 跨零点，家机 win-x64：121-a v2 侧生成语句/代码生成拆分）

- 本轮**无新坑**——v2 侧三约束（静态私有/同包全局可见/包.cn 注册）与「v2 产物基线对比 + 锚定链固定点不变 +
  对拍组件数自动扩展」验证口径完整复用（120-a 立）。**补一条正面确认**：跨包调用方（如 主.cn 调 `代码生成`、
  IR 生成函数.cn 调 `生成函数`）导入的是**包名**而非文件名——包内再拆文件**零改动**调用方 ✓（这是 v2 侧可持续
  拆分的结构性优势，后续 v2 波次可直接依赖）。

## 第一百二十二轮踩坑（2026-09-14 凌晨，家机 win-x64：122-a v2 侧语义检查拆分）

- 本轮**无新坑**——**「静态变量 × 函数」矩阵**（本族不直接访问任何静态 → 可拆；静态使用者留主文件）是 v2 侧
  带静态文件拆分的标准前置检查（120-a 立约束、T121/T122 两度复用）。**判据一句话**：拆出的族只能经
  「函数接口」触达文件级静态，**不得在族内直接出现静态变量名**。
- v2 侧四文件（生成表达式/生成语句/代码生成/语义检查）已连续三波拆分零意外——**v2 侧拆分 checklist 与宿主侧
  完全同构**（依赖矩阵 → 多重集校验 → 产物基线对比 → 锚定链 → 对拍自动扩展 → 全量门禁），仅「静态矩阵」
  一步为 v2 特有（宿主对应物=匿名 ns/static 矩阵）。

## 第一百二十三轮踩坑（2026-09-14 凌晨，家机 win-x64：123-a v2 侧 IR生成拆分）

- 本轮**无新坑**——第三度复用 v2 侧静态矩阵（11 静态 × 55 函数 → 24 个零静态函数分 5 连续块 → 取两段组族 441 行）。
  **新形态：族跨「静态使用者段」两段拼接**（[145,312] 与 [363,635] 之间夹着取下标元素宽度码/寄存器类型族——
  主文件保留）——行级多重集校验 + 分段覆盖校验完整承接（同 117-a 的宿主 types 族两段先例）。
  **v2 侧拆分 checklist 至此五文件（120~123-a）零意外**——可直接推广至最后的 IR容器（待工具下沉解耦后）。

## 第一百二十四轮踩坑（2026-09-14 凌晨，家机 win-x64：124-a IR容器收尾波）

- **「工具函数下沉」的重构面比预想小得多**：因 v2 同包「多文件合并全局可见」，`IR追加`/`建IR指令` 移到新模块
  `IR基础.cn` 后，**8 个使用模块的导入面零改动**（只需 包.cn 注册）——**结构性优势把「跨 8 模块的导入改动」
  消解为零**。教训（推广）：**评估重构面时，先验证「命名/可见性机制是否已提供零改动路径」**——不要按
  「常规模块系统」的直觉估工作量（本轮勘定 vs 实际：预估涉 8 模块导入面 → 实际 0）。
- **空行漏分配形态二度复现**（118-a 之后）：区间拼接近邻处（[1651]/[1652]/[1653]）的单行空行——**分段覆盖校验
  （总和==原行数）在写盘前拦截**，文件未被破坏。**判据**：任何多段拆分必先跑「覆盖校验」再写盘。

## 第一百二十五轮踩坑（2026-09-14 凌晨，家机 win-x64：125-a 宿主 ast.hpp 按层拆分）

- **生成/拆分脚本必须带「防重跑断言」**（本轮实证的破损路径）：拆分脚本第二次运行时读到的是**已拆分后的
  转发版 ast.hpp**（10 行）→ 按原行号切出**垃圾头文件**（21/15 行）并覆盖好文件。**修复**：读源文件后
  **先断言原版特征**（如行数 > 500 / 含原版首行注释），不满足即中止并提示 `git checkout` 恢复。
  预防：**所有写文件的脚本 = 幂等或显式防重**——这是继「多重集校验」之后的第二道自保闸门
  （校验防「内容错」，断言防「重复执行」）。
- **bash heredoc 传中文脚本的编码陷阱（本轮二度踩中）**：`python - << 'PYEOF'` 执行含中文的脚本时，
  个别位置出现 mojibake（表现为 SyntaxError 或 replace 静默不匹配）→ **可靠做法：含中文的生成类脚本一律
  用 Write 工具落盘为 .py 再执行**（hheredoc 仅用于短小、纯 ASCII 或已验证安全的命令）。
- **头文件拆分的关键检查点**：①**命名空间开头**（原单文件只有一处 `namespace cn_compiler {`——拆出的
  后续段必须补上，否则类落在全局命名空间 → "未定义基类" 连锁报错；本轮第一次生成即因此失败）；
  ②**依赖 include 链**（base→expr→stmt→decl）；③**聚合转发**（原入口保留为 include 四层——消费方零改动，
  这是头文件拆分相对 .cpp 拆分的独有优势）。
- **空行漏分配形态第三次复现**（118-a/124-a/125-a）：分段覆盖校验（总和==原行数）三度在写盘前拦截——
  **该闸门已证明为拆分流程的必需项**。

## 第一百二十六轮踩坑（2026-09-14 凌晨，家机 win-x64：126-a emitCast 函数级提取）

- **函数级提取的可复用模式（本轮跑通，供后续 18 个超百行函数使用）**：对「if 块 + early return」链式大函数，
  提取为**返回布尔**的私有子方法——段内 `return;` → `return true;`（子方法末尾补 `return false;`），
  调用处 `if (子方法(args)) return;`。**各段原顺序/条件/语句逐字不动** → 产物 md5 逐字节一致即完成证明
  （代码生成函数尤其强：产物等价 ⇒ 行为等价）。**子方法签名按段内实际引用的局部变量裁剪**（全传会触发
  C4100 未引用参数——/WX 下即构建失败；本轮首跑被拦截）。
- **生成/修正脚本对同一文件必须统一换行基准**：split 脚本用 `
` 写签名行、文件其余部分为 CRLF →
  后续 fix 脚本按 `

` 匹配 assert 失败（改按 `
` 匹配则 cpp 部分又失配）。**规则**：脚本模板一律
  **显式指定完整换行**（与目标文件一致）；修正脚本先**探测**目标行实际换行再构造匹配串。

## 第一百二十七轮踩坑（2026-09-14 凌晨，家机 win-x64：127-a emitCompare 函数级提取）

- **提取脚本必须保留被提取函数原体的全部「外层闭合」**（本轮实测的破损路径）：`emitCompare` 的体 = 浮点段
  （含自己 if 的 `}`）+ 整数段（**顺序尾，函数体的闭合 `}` 在它末尾**）。脚本把两段整体搬走后，替换处只写了
  调用行而**漏掉函数体的闭合 `}`** → 后续函数（emitNot/新方法）全部被嵌套进未闭合的 emitCompare → 6 处
  **C2601「本地函数定义非法」**连锁报错。**修复**：git 恢复 + 替换头末尾补 `}`。**规则**：整段搬运类脚本，
  必须在「替换处」显式重建**被搬走段的全部边界闭合**（函数体/命名空间），并用「闭合计数」（`{`/`}` 平衡）
  自检落盘前的产物。
- **0-based 与 1-based 索引混用**（本轮两度 off-by-one 断言失败）：dump 输出用 1-based 行号，脚本内
  `split()` 后用 0-based 索引——**换算必须显式**（本轮 `行N` → `L[N-1]`），且**边界断言宜用「内容特征」
  而非纯行号位置**（如 `assert 行们[k].strip() == "return;"` 前先打印该行确认）。

## 第一百二十八轮踩坑（2026-09-14 凌晨，家机 win-x64：128-a injectContainerElemDestroy 映射族提取）

- **提取模式二（「if 包 + return」，本轮立）**：当族是「独立 if 块且块内所有路径都 return」时，提取为 **void
  子方法**，主函数写 `if (进入条件) { 子方法(...); return; }`——子方法内 `return;` 原样保留。
  **优于 bool 模式之处**：无「族内全 return → 子方法末尾 return false 不可达」的 C4702 风险（/WX 下即错误）、
  无冗余布尔流转。**两模式选用判据**：族是「顺序尾、后接主流程」→ bool 模式（126/127-a）；族是「独立 if 块、
  块内全 return」→ if 包 + return 模式（128-a）。
- **多行函数声明的 hpp 插入锚点必须取「声明末行」**：本轮 hpp 锚匹配了 3 行声明的**首行**
  （`void injectContainerElemDestroy(const std::string& className,`）→ 新声明被插入到多行签名**中间** →
  C2143/C2079「ClassMemberInfo 未定义」等连锁 5 错。**规则**：头文件声明插入前，先**向下扫描到含结尾分号的
  那行**作锚（或用「整段多行声明」作为待替换文本、把它整体重写）。

## 第一百二十九轮踩坑（2026-09-14 凌晨，家机 win-x64：129-a 四族提取尝试未成功——已回退）

- **大函数「一次拆多族」的高风险路径（本轮实证，止损回退）**：`injectContainerElemDestroy` 余下**四族**（移动元素/
  字符串元素/含串字段/有析构类）一次提取——脚本**三次受挫**：(a) **硬编码行号断言 off-by-one 两度**（0-based 换算 +
  族边界含注释行/函数闭合行错判）；(b) 改用内容搜索后**插入点误判**——新的子方法被插到**相邻子方法（injectMapElemDestroy）
  内部** → C2601「本地函数定义非法」。**止损**：`git checkout` 回退（构建恢复、工作树干净），下轮重做。
- **纪律（下一步方法论）**：①大函数多族提取**必须逐族推进**（一族 = 一次提取 + 一次构建验证，**不要一次脚本做 N 族**）；
  ②锚定**一律用内容搜索**（`next(i for i,l ... if l.strip()==<精确签名>)`），**禁止硬编码行号**；
  ③插入新方法前**必须验证插入点 = 目标函数闭合 `}` 之后**（本轮教训：`block_end` 从函数定义起数花括号，若起始行
  本身是多行签名的首行或体内含未配对花括号的注释/字符串，会数错——改用「先定位目标函数的**闭合行**（其下一行应为
  空行/下一函数注释），再插入」的显式校验）；④每族提取后**立即构建**（构建失败 = 立即回退重来，不叠加）。

## 第一百三十轮踩坑（2026-09-14 上午，家机 win-x64：130-a 129-a 逐族重做成功）

- **129-a 的四条方法论在 130-a 一次跑通（正面验证）**：①**单族一次提取 + 立即构建**（不再 4 族一把梭）；
  ②**内容搜索锚定**（精确 if 行匹配 + 花括号深度找闭合——零硬编码行号）；③**插入点三步显式校验**
  （定位主函数闭合右花括号：从其后的「空行 + 注释块 + 下一函数定义」反向走回，断言三者全对才插入）；
  ④**落盘前闭合平衡自检**（全文花括号计数差 == 0）。**结论：复杂提取的可靠性来自「显式不变量断言」而非
  「小心谨慎」**——每个可能错的假设都写成 assert，错了在写盘前被拦截（本轮断言一次全过）。
- **中文 f-string 的右花括号陷阱（本轮脚本失败根因）**：在 f-string 正文里描述「右花括号」时直接写了字面
  `}`（未转义）→ SyntaxError → **整个脚本未执行**（Python 先编译全文）。**规则**：含花括号/代码片段的中文
  文档写入脚本**一律不用 f-string**（用字符串拼接 + `.replace("@N@", ...)` 占位符）；且脚本失败后**先核对
  「是否整段未执行」**（本轮自检立即报「总表戳落后」= 未执行坐实）。

## 第一百三十一轮踩坑（2026-09-14 上午，家机 win-x64：131-a 字符串元素族提取）

- **「族块整体搬入方法体」时，方法体保留原 if 判定行 → 该判定所用的变量必须补为参数**（本轮实证）：
  提取模板 body = 含 `if (elemCanon == "字符串") {` 行 → 方法内引用 `elemCanon` → C2065「未声明的标识符」。
  **130-a 未暴露纯属侥幸**（其 if 判定的 `mi` 恰好在参数表中）——**同一模板在参数集不同的族上会翻车**。
  **规则**：提取前**逐族列出「族块内引用的全部外部标识符」（含 if 判定行本身）**，方法签名一次给全（宁可多传，
  多传至多 C4100 提示、少传直接编译错）；或**剥掉 if 行与闭合、只搬内部代码**（更干净，但需同步调整调用处结构）。
- **本轮流程仍是四条方法论**（单族一次提取 + 立即构建 / 内容搜索锚定 / 插入点三步显式校验 / 闭合平衡自检）——
  **首跑构建即暴露 C2065（而不是像 129-a 那样静默产出坏结构）**，正是「立即构建验证」纪律的价值。

## 第一百三十二轮踩坑（2026-09-14 上午，家机 win-x64：132-a 含串字段族提取）

- **131-a 的「全标识符预算」规则首度生效——构建一次通过**（正面验证）：提取前扫描族块内引用的全部外部标识符
  （脚本用候选清单逐名计数 + 人工核对），9 个参数一次给全（含 if 判定行的 `canonClass`）。对比 131-a 首跑
  C2065 返工——**规则价值实证：提取类脚本的失败面主要在「参数集不全」，前置扫描把它消灭在写盘前**。
- **脚本小坑**：生成脚本忘记 `import re`（NameError）——**脚本模板复制时须连同 import 段整体复制**
  （本轮从 split_strfam 复制时漏了 import 行；补救=在脚本头部统一放 import 段并核对）。
- **commit message 的反引号/`$()` 陷阱（131-a 教训复习）**：131-a 的提交信息里 `反引号包裹的代码片段` 被 bash
  命令替换解释（报 syntax error 且 message 片段丢失）——**本轮起：commit message 一律用 `git commit -F -` 读
  引号 heredoc，且正文避免反引号**（用全角引号或直接去掉）。

## 第一百三十三轮踩坑（2026-09-14 上午，家机 win-x64：133-a 有析构类族提取 + 会话中断恢复）

- **后台长任务禁止接管道（本轮最重教训，113-a 的强化版）**：锚定链以 `nohup python x.py 2>&1 | tail -1`
  形式挂在 bash 管道上——会话中断 → bash 被杀 → **管道读端消失** → 脚本下一次 print 抛 BrokenPipeError
  **中途静默死亡**（本轮死在 [1] 宿主 compile 完成、ml64 之前；日志与产物时间戳停在那一刻，无任何报错线索）。
  **正确姿势：`nohup python x.py > 日志文件 2>&1 &`（输出重定向到文件、不接管道）**；恢复后**先核产物
  时间戳链**判进度（asm/obj/exe/log 逐级 mtime），再决定续跑。
- **产物名须从脚本源码核对，勿按惯例猜**：恢复时按惯例查 `v2p.asm/v2p.exe` 全部 MISSING，一度误判卡死——
  实际 111-a 脚本的产物名是 `self111_v2p.asm/self111_v2p.exe`（前缀 self111_）。**判进度前先读脚本的
  产物变量定义**（30 秒成本换一次误判）。
- **插入点断言必须「列 0 闭合」**：`new_main[k2].strip() == "}"` 太弱——if 块/嵌套块闭合行同样满足；
  133-a 首跑 C2601（新方法被插进相邻子方法体内）根因即此。升级为 `new_main[k2] == "}"`（无前导空白）。
- **运行中改源码的边界**：门禁脚本（锚定链/对拍）只读源码、用已构建二进制——**改源码可以，重新构建不可以**
  （构建会替换 cn.exe/LNK1104 风险）；并行推进时严格遵守此边界（本轮 133-a 待验期间并行写了波 1 修复，
  零冲突）。

## 第一百三十四轮踩坑（2026-09-14 上午，家机 win-x64：134-a 波 1 修 字符* 拼接误报）

- **「文本后缀判类型」陷阱的根治模式**：`isPointer(t) = t.back()=='*'` 对 `字符*` 是「形似指针、语义是
  字符串视图（+ 按拼接分派）」——凡按形态后缀判定的检查点都会误伤。**根治=语义集合单点判定**：
  `isStringSemanticType`（字符串/字符*）helper + **判定方与分派方共用同一 helper**（本处=安全区边界检查
  与算术分支拼接分派共用）——集合扩展时两处同步，永不漂移。Rust 对照：rustc TyKind 结构化枚举，
  杜绝字符串比较判类型（本项目类型层仍是文本，方案丙=类型类别枚举随波 6 落地）。
- **反证的精准手法（波 1 版）**：`git stash push -- <单文件>` 只回退唯一变量（保留 helper 定义与单测）
  → 构建 → check 复现 75 处 → `git stash pop` → 重建复验 0——比整树 stash 更精准，且顺带证明
  「helper 存在但未启用时仍 75」（排除了 helper 自身引入副作用的可能）。
- **`A && nohup B &` 坑复现（113-a 教训复习）**：`./单测 && nohup python e2e &` 把**整体**后台化——
  单测在后台跑、E2E 尚未启动，25 秒后查进程「没有任务」误判失败；**正确=两条独立命令**：
  单测前台跑完 → 下一条命令单独 `nohup python e2e > log 2>&1 &` + 30 秒后核验进程与日志。
- **v2 源文件是 `\r\r\n` 行尾**：`splitlines()` 会把 `\r\r\n` 拆成两行（多出空行）致行号偏移——
  **按 check 诊断行号定位源码须用 `split('\n')`**（诊断行号=按 \n 计数）；用错会让「行号-内容」对不上，
  误以为是诊断错位。

## 第一百三十五轮踩坑（2026-09-14 上午，家机 win-x64：135-a 波 2 清单编制）

- **清单编制的顺序纪律（本轮方法论）**：先**现状探针**（六形态实测宿主行为——`*p`/`p[i]`/`p.x`/静态写/`无`解引用
  全零检查）+ 再**存量粗扫**（破坏面量级）+ 最后**写清单**——顺序颠倒会让「纳入与否」的决策没有事实依据
  （本轮六类新编全部有探针实证支撑）。
- **Rust 对照要「敢不纳入」**：指针比较/类型转换在 Rust 是 **safe**——若照「看起来危险」的直觉纳入，会把
  235 处判空/比较惯用法逼进 unsafe 且零安全收益（比较不解引用不产生 UB）。**「最小不安全面」= 逐条问
  「它真的能产生越界/悬垂/UB 吗」**（Rust RFC 2585 同向）。
- **粗扫正则的误报要在文档里声明**：「指针成员访问 223 处」是「指针变量名后跟 .」的粗口径（含同名非指针变量
  混淆）——**文档必须写明口径与「精确扫描留迁移轮」**，否则后续轮次会把粗数当准数引用（plans 数字漂移之源）。
- **观察期机制对新类是必需品而非可选项**：B8/B9/B10 存量（v2 树 43/23/48 处）直接切硬错误=自举链断裂——
  新类必须走与 A 组同一的「观察期 → 迁移清零 → 硬错误」通道（plans/019 阶段4 模式复用）。

## 第一百三十六轮踩坑（2026-09-14 上午，家机 win-x64：136-a 波 3 开工）

- **对照形态是缺口定位的最强工具**：五形态里形五（类字段容器=0）一步锁定「缺口只限结构体侧」——
  **同类对象两条实现路径（类/结构体）的对照探针**是「定位缺口边界」的标配（比通读代码快且无遗漏）。
- **探针口径必须与原基线逐字对齐**：形五首跑增量 2（plans/022 基线=0）——差异之源=类未声明析构（宿主出
  「建议声明析构」警告）；补 `函数 ~账本() { }` 后复现 0。**复现他人基线时先核对口径差异（声明/构造/析构/
  初始化形态），再怀疑结论**。
- **三件套类工程的实施顺序=安全中间态优先**：浅拷共享下「只加释放面=双释放悬垂」——所以**拷贝位先行**
  （消除共享后中途态自洽）、消亡位随后、写入位收尾；**换序会制造可崩溃的中间态**（不是「先做哪个都行」）。
- **深拷通道的选型要防「被后续波阻塞」**：容器深拷若等波 4 的 `复制(x)` 内置才做，波 3 就卡在依赖上——
  **先以容器拷贝构造（甲）落地，波 4 再统一到 `复制(x)`（丙）**：两步走而语义不回退。

## 第一百三十七轮踩坑（2026-09-14 上午，家机 win-x64：137-a 设施探摸）

- **存储形态 = 所有权实现的第一分叉**：「字段含容器」在类与结构体两侧的**内存形态不同**（类=指针槽、
  结构体=内联）——释放路径因此**本质不同**（`DeleteObject(LoadPtr)` vs 原地析构调用）。**判定工具=`类型大小`
  探针**（一次测量 30 秒，胜过通读布局代码）；**先探形态再定实现**，否则把类侧机制直接照搬到结构体=按错误
  形态生成代码。
- **大型工程轮要「探摸→定稿→实施」显式分段**：波 3 这类跨轮工程，前轮交付「探摸结论+路线定稿+**待查证点清单**」
  ——把未知点写成「实施轮第一/第二查证点」，下轮开工不需要重新探（本轮实测：设施模板 30 分钟摸清，若混在实施轮
  里会显著抬高单轮上下文占用）。
- **复用优于新造（本轮实得）**：波 3 的「三件套」一半已存在——向量拷贝构造（stdlib:51）、类侧字段级联
  （injectFieldCascadeDestroy）、容器析构族——**真实工作量=「把已有机制接到结构体侧」**；先摸清「已有的有什么」
  再设计「要造的缺什么」。

## 第一百三十八轮踩坑（2026-09-14 上午，家机 win-x64：138-a 波 3 轮 A 实施尝试→回退）

- **IR 是存储形态的唯一铁证，尺寸探针不是**：137-a 按 `类型大小(货架)=32` 推断「容器字段内联」——**错**；
  IR dump 显示 `NewObject + StorePtr/LoadPtr`（**指针槽**），32 只是保守布局。**教训：凡「字段/变量的内存
  形态」判定，必须 dump IR 看访问指令（地址直算 vs 指针解引用），尺寸只能提示不能定论**。
- **「引用参数」的传递约定要交叉验证**：CN 引用参数实参=**槽地址**（callee 内一层 Load 得对象）——我照
  「直觉」传了对象指针（LoadPtr 结果）→ 多解一层 → 0xC0000374。**定位手法（高效）= 对照工作代码的 IR**：
  `probe_ctor`（编译器生成的拷贝构造调用）显示 `新建对象 + 取地址(源槽) + 调用`——一望即知。**新写调用
  序列前，先找「同语义的既有工作代码」对照 IR**，比读约定文档快且准。
- **浅拷共享下加释放=双删**（不变量二次实证）：形四「两字段共享同一对象指针 + 块出口各自 DeleteObject」
  = 0xC0000374。**三件套（写入/拷贝/消亡）必须同轮**——这是设计不变量，不是可选项。
- **回归面前回退是纪律不是失败**：实施打通（形一/形三归零、深拷正确）但 116 回归 → **立即回退**、
  保留发现（不带着回归前进）。**「尝试轮」的价值在发现与教训**：本轮四则发现（IR 铁证/引用约定/
  不变量/释放协议）是下轮最小闭环重做的直接输入。
- **容器族形态差异必须有独立验证**：116（链表栈队列）回归提示——**平铺（向量/栈）与链式（链表/队列）
  的元素释放协议不同**（`__cn_vector_free_strings` vs `__cn_chain_free_strings`，槽序/所有权转移语义
  不同）；「向量通了」不等于「容器族通了」——**扩族时逐族全量 E2E**。
- **大面积启用前先做最小闭环**：「一次全开」（收集面全类型+四形态+写入位）会让缺陷面叠乘（本轮 4 个
  交互问题同时暴露）；**改为「单容器+单形态→逐形态→逐族」递进**，每步可归因。

## 第一百三十九轮踩坑（2026-09-14 上午，家机 win-x64：139-a 最小闭环）

- **最小闭环策略实证有效（138-a 路线修正的胜利）**：138-a「一次全开」（全类型族+全形态）→ 116 回归；
  139-a「收窄到向量族 + 四处分派」→ **全链绿**（形一/形三归零、E2E 290/292、116 PASS）。**同一份设计**，
  差别只在**启用面的粒度**——**大改动的第一步应该是「最小启用面」**（单族/单形态），把「设计正确性」与
  「族/形态差异」两类问题解耦。
- **收集面=类型级 → 消费面必须全分派**（关键设计约束）：一旦某类字段被收集，**所有消费点**（释放/拷贝位/
  写入位）都会遇到它——只做 FreesAt 会让 preFree/postCopy **按字符串路径误处理容器字段**（把对象指针当
  字符串 free=崩）。**所以「收窄」的维度只能是类型族，不能是形态**——形态的递进靠「探针验证顺序」而非
  「代码开关」。
- **「零初始化计数 ≠ varSlots」**（138-a 误读修正）：`货架 乙 = 甲;`（带初始化声明）**跳过零初始化**是
  合理优化，**不暗示槽数**；138-a 据此推断的「乙单槽→memcpy 越界」为**误读**——**读 IR 时要区分「什么
  被生成」与「什么被省略（且省略合理）」**。
- **锚定链「不变」也是宝贵结果**：139-a 改动宿主产物**对 v2 树零影响**（v2 树无该场景）——**锚定链 406097
  逐字节一致**=「该修复的影响面确界」的硬证据（而非「跑过了」）；**跨机轮可据此免于重锚**。

## 第一百四十轮踩坑（2026-09-14 上午，家机 win-x64：140-a 残余澄清 + 栈族）

- **「残留」测量必须在释放路径之后（探针口径纪律）**：形二/形四的「增量 3」追了一轮——根因=探针的
  打印**在块内**（块出口前），读的是中间态（甲 2 + 乙 1）；把打印移到块外后**五形态全 0**。
  **教训：泄漏探针的「起点-终点」必须夹住「释放路径」（块出口/函数尾），否则读到的是持有量而非残留**——
  探针设计先定「测量的语义」，再写代码。
- **扩族的真正前置是「语义层防线」而非「收集面开关」**：语义层对「无拷贝构造的有析构类」**禁止按值拷贝**
  （错误级，既有防线）——所以「栈/链表/队列字段在结构体里的拷贝场景」**根本不可构造**（探针编译失败即
  证明）；**扩族=补 stdlib 拷贝构造**（按值拷贝才有可能）→ 收集面自动纳入。**先探「能不能构造」，
  再谈「接不接入设施」**。
- **「元素级共享」与「容器级共享」是两回事**：139-a 修掉了容器级共享（乙有独立容器=数组独立），但
  元素串句柄仍共享（拷贝构造里 T 赋值语义未深拷）——**残留 0 证明它不泄漏**（runtime 防御），
  但**语义上违反「深拷」契约**（写时共享风险）——**分层登记**（容器级 ✓ / 元素级 ⬜ 专项）。
- **stdlib 改动对 v2 树可能零影响（锚定链不变的解读）**：新增 `栈(栈&)` 后锚定链 406097 **逐字节一致**
  ——说明 v2 树无「栈字段拷贝」场景（**该改动的影响面=用栈拷贝的代码**）——**「不变」也是影响面确界的
  证据**（不是「没生效」）。

## 第一百四十一轮踩坑（2026-09-15，家机 win-x64：141-a 链式族）

- **链式容器的拷贝=「链游复制 + 状态携带」而非「数组平推」**：链表/队列的内部状态含**空闲链**
  （头/尾/数量/空闲头/已用槽数）——拷贝构造必须**整套携带**（只复制有效元素=空闲链丢失→后续追加
  复用已占槽=链结构损坏）；**探针必须刻意构造空闲链状态**（追加+删除+追加 再拷贝）——「空链表拷贝」
  测不出这类缺陷。**验收探针要覆盖「非平凡内部状态」**。
- **「收窄+逐族」策略的完整胜利**：138-a（全族一次开）→ 116 回归；141-a（逐族扩至四族全齐）→
  **116 PASS**——同一份实现、同一回归面，**策略差异决定结果**。**回归面要在启用时逐个面对**，
  不要试图一次吞下。
- **类型细节的探针返工（两次）**：`链表.删除头部()` 返回 `结果<空类型,整32>` vs `队列.出队()`
  返回 `结果<字符串,整32>`——**同名语义的方法返回类型不同**；写探针前先读被调 API 的签名
  （**这次返工两轮**——小成本但可避免）。

## 第一百四十二轮踩坑（2026-09-15，家机 win-x64：142-a 专项核查 + 两次回退）

- **「一次通过」的 E2E 不足以证明内存正确性（本轮最重教训）**：141-a 的链式族启用后 E2E **单次 290/292
  通过**（116 PASS）——142-a 复跑 **116 稳定 3/3 崩**（0xC0000374）。**堆布局/分配序会让内存错误
  「时隐时现」**——**新族/新设施启用后的验证必须：①多次复跑**（≥2 次）**②探针刻意覆盖非平凡内部状态**
  （空闲链/多元素/删除后再追加）。「E2E 绿」是必要不充分条件。
- **交互面分析先于启用（两次回退同因）**：泛化（用户类）与链式族两次尝试都在**未分析交互面**时启用——
  ①链式族：**「局部容器 vs 结构体字段」**（我的设施只覆盖结构体字段路径，为何局部容器 116 崩？——**须查**：
  可能与「`injectContainerElemDestroy` 注入的局部容器消亡」与「新收集面」的**符号/方法表交互**相关）；
  ②泛化：**「用户类拷贝构造 this 字段初值」+「`injectFieldCascadeDestroy` 字段级联重叠」**。
  **启用前先把「谁会与它交互」列表化**——不列不开。
- **回退也是净收获**：142-a 净效果=清除 141-a 的**潜伏回归**（远程分支上的 e87c0b9 带回归）——
  **「带回退的轮次」比「带回归的绿灯」有价值**；**代码回退但发现与教训入库**（plans/020 第五十四节）。
- **脚本原子性纪律（本轮实得）**：改多文件前**先把全部锚点 exists 验证完再统一写**——本轮两次脚本崩溃
  （锚点不匹配）但**零副作用**（write 在所有 assert 之后）；**若边改边写，第二次崩溃会留下半成品状态**
  （本轮一度出现「stdlib 已回退 + 宿主未回退」的不一致构建——**靠 git diff 才发现**）。

## 第一百四十三轮踩坑（2026-09-15，家机 win-x64：143-a Bisect 澄清）

- **「稳定复现的回归」也要先核对构建状态（本轮最重教训）**：142-a 基于**半回退混合源码**（ir_fields 已回退 +
  ir_expr_assign 未回退——脚本中途崩溃的产物）构建，得到**116 稳定 3/3 崩**——据此判定「链式族回归」并回退；
  143-a 的 A/B 两实验（含**完整 141-a 状态**）**均 PASS ×3** → **原结论完全错误**。
  **纪律：任何「回归」结论落地前，先 `git diff --stat` + 核对关键判定行**——**多文件脚本崩溃后的工作区
  是「可疑物证」，不是「现场」**。
- **Bisect 的正确姿势=「完整状态的最小分解」**：A（stdlib 侧）/ B（A+宿主侧）两实验**双向验证**——
  **bisect 的两个分支都必须是「可构建的完整状态」**（而非「半回退的中间产物」）——本轮的错误正是源于
  「把中间产物当分支」。
- **混合态的崩样本反而有价值**：142-a 的混合态（ir_expr_assign 泛化 + 116）**稳定崩 3/3**——是
  「**泛化（任意类写入位拦截）× 用户类场景**」的**现成最小复现**（144-a 泛化专项的起点）——
  **错误分析的副产品可以是正确方向的线索**（前提=复盘出「崩的是什么状态」）。

## 第一百四十五轮踩坑（2026-09-15，家机 win-x64：145-a 泛化崩因定位）

- **「先拆变量再改」的完整样板（本轮实证）**：面对「泛化崩」——①**单变量**（A=仅写入位泛化 / B=完整泛化）
  **二分出崩因所在文件**；②**二分最小场景**（纯类局部 uF vs 结构体场景）**分离出「既有 vs 新引入」**；
  ③**在已知好的基线（四族）上复测 uF** → 确认「既有缺陷」（泛化前置）。**四步全部由实验驱动，零猜测**。
- **「既有缺陷」的确认手法**：新功能（泛化）崩时，先问「**去掉新功能后同一场景崩不崩**」——uF 在四族
  （无泛化）下**仍崩** → **前置缺陷**（不是回退泛化就完事——**要修前置**）。
- **AST 实证推翻「入口假设」**：我预设「`表 = 其他.表` 走成员赋值拦截（入口=MemberExpr）」——AST 显示
  目标=**IdentifierExpr**（裸字段名）→ **拦截根本不进入**——**「我以为它走了哪条路径」必须先 dump 确认**
  （与 143-a 的「状态核对」同源纪律）。
- **构造体=初始化语义（Rust 对照）**：类构造体（含拷贝构造）内对 this 字段的赋值是**初始化**（无旧值）——
  「赋值前释放旧值」的通用规则在构造体内**必须关闭**（否则读未初始化的 NewObject 字段=崩）；
  实现=编译期标志（`currentMethodIsCtor_`）+ 「接收者是 this」判定——**零运行时开销**。

## 第一百四十六轮踩坑（2026-09-15，家机 win-x64：146-a uF 专项）

- **「单点判定的遗漏」要按「路径全集」审**：`ir_expr.cpp` 的 byRef 求值判定（`isArray || isStructType`）
  **漏了类类型**——一个判定分支的形态枚举不全，后果是「类引用参数」多解一层（IR 实证）。**修订类判定时
  必须列全形态**（数组/结构体/类/引用/指针……）——**与 141-a「head 集合」同型教训**（枚举面=缺陷面）。
- **改判定的「牵动面」必须先盘查（chain 退化教训）**：把「类引用参数」改成一层后，**uD/uE 修复但
  chain 退化（链式容器拷贝=0）**——说明**有路径依赖旧的两层行为**（或需分场景）；**「先全路径盘查、
  再分场景改」**——**这类「基础设施级判定」的修改不能只用一个场景验证**（138-a/141-a/143-a 之后的
  第 N 次同型教训：**面越基础，越要先盘**）。
- **dump 搜索口径要跟实现名**：按 mangled 符号（`盒子$盒子#盒子&`）搜 IR dump 无果 → 误判「函数体缺失」；
  dump 用的是**显示名**（`盒子.盒子`）——**搜不到先换口径**（符号名/显示名/前缀），别急着下「缺失」结论。

## 第一百四十七轮踩坑（2026-09-15，家机 win-x64：147-a 全路径盘查）

- **「读路径 × 写路径」是成对约束（本轮核心教训）**：改「成员访问基址」的读路径（#1）后，同一表达式链
  `其他.值表[游]` 的**读**（一层 ✓）与**写/左值**（经 #2 仍两级 ✗）基址错配 → 链式容器拷贝退化（=0）。
  **改「基址/寻址类」逻辑时必须同时盘查读侧与写侧（lvalueAddress）**——两侧不一致=错配（比两侧都错更隐蔽：
  两侧都错时同式自洽、意外可用；单侧改对反而暴露）。**与 143-a「收集面 × 消费面」、146-a「判定形态枚举」
  合为同族纪律：基础设施点改一处=必须盘全族**。
- **「特判修」与「全局修」同型失败=根因在结构层**：146-a 全局修（byRef 判定）与 147-a 特判修（成员基址）
  **结果完全同型**（uD/uE ✓、uF ✗、chain ✗）——**两次不同层次的修复同型失败 ⇒ 问题不在「哪一层」，而在
  「读/写/求值三路未统一」**——**同型失败是「方向性信号」**（换层次没用，要换维度）。
- **验证探针要「同式读写」**：链式拷贝探针（`值表[游] = 其他.值表[游]`）之所以能捕获退化，因为它
  **在同一表达式里同时走了读与写路径**——**探针设计应刻意覆盖「读写同行」形态**（单读或单写探针
  测不出错配）。

## 第一百四十八轮踩坑（2026-09-16，家机 win-x64：148-a 三连同型）

- **「同型失败」的下一层解读=「退化源不在改动处」（本轮收敛）**：三连试验（全局/特判/成对——三个层次
  的修复）结果完全同型 ⇒ 不是「哪一层没修对」，而是**「任何改动都会触发同一个别处的退化」**——
  **锁定手段=IR diff**（修复版 vs 基线的同一函数 IR 逐指令对比，首个差异指令=退化点）。**「换层次无效」
  时不要再换层次——去 diff 产物找「改动波及其他路径」的证据**。
- **改动「使别的缺陷显形」与「引入缺陷」要区分**：chain 在基线（无修复）下 `拷贝=2` ✓——**修复版=0**——
  但三次修复内容各不相同、结果相同 ⇒ **高置信不是「修复算错」而是「修复改变了某个共享前提」**
  （如：两处基址从「同错」变「一对一错」，或优化/分支选择变了）。**IR diff 能一刀切开两种可能**。

## 第一百五十轮踩坑（2026-09-16，家机 win-x64：150-a 六类实装）

- **「同形态单报」靠状态位而非语法判定**：`*p = x`（B7）与 `p[i] = x`（A2）的「写」在不同层的不同报点
  ——**用 `assignmentTargetDepth_`（赋值目标求值期）**统一抑制读判（B6/B8/B9）——**状态位 > 语法穷举**
  （一改覆盖六类）。
- **豁免清单=单一 helper**：六类共用 `isStringSemanticType`（字符串/字符*）——新检查一律先过它；
  **新检查实施模板（四件套）= 报点 + 语义豁免 + 深度抑制 + kind 文案**。
- **v2 树命中面=实施后的必测项**：实装后立刻跑 v2 树 check（**87 处观察期 + 错误 0**）——**比等收口时
  才发现面大量广，提前一个波次拿到波 5 的输入数字**。

## 第一百五十一轮踩坑（2026-09-16，家机 win-x64：151-a 波 5 迁移）

- **「先验假设 + 试点」流程（本轮核心方法论）**：全量 73 处迁移前——**用最小单元（4 函数）验证关键假设**
  （「加修饰=零产物」→ 锚定链逐字节不变）——**假设成立后才全量**。**若假设不成立**，全量 73 处的返工
  成本远高于一次 15 分钟的锚定链。**「大规模机械改动」的标配：抽 1 个样本验完整链，再铺开**。
- **「按命中行反查函数定义行」的批量迁移法**：命中清单（文件:行）→ 向上找最近 `函数` 定义行 → 函数去重
  → 批量加修饰——**73 处 → 25 函数**（**去重率 3:1**）——**批量改的最小单元是「函数」而非「行」**
  （修饰挂在函数上）。
- **「修饰零产物」的三重前提要写清**：(1) 宿主 `不安全` 修饰零产物（plans/001）；(2) v2 侧修饰零语义
  （plans/019——v2 不查安全区边界）；(3) 锚定链逐字节验证。**三重任一不成立则迁移=行为变更**（须当
  修复轮处理）——本轮 (1)(2) 是文档前提、(3) 是实测。

## 跨机验证轮踩坑（2026-09-14，单位机麒麟ARM64：93-a~108-a+111-a~146-a 全量 arm64 复验）

- **「源码可移植性」没有门禁时，跨机轮是唯一拦截面（本轮双缺陷同源）**：家机（MSVC）与深度机
  （GCC≥10）都接受的中文 C++ 标识符，把麒麟 GCC 9.3 的构建彻底打挂（`stray '\346'`，UTF-8 标识符
  GCC 10 才支持）——**「我的机器构建绿」≠「源码可移植」**；既有纪律「C++ 标识符一律 ASCII」被
  110-a/118-a/134-a/77-a 四轮无声违反（每轮都在自家机器门禁全绿）。预防：①纪律升格为扫描门禁
  （Rust `#![deny(non_ascii_idents)]` 同构）；②改 C++ 源码时自查标识符形态（注释中文合法、
  标识符必须 ASCII）。
- **libc 边界函数的「幅值」是平台细节，语言边界必须归一（跨机-b）**：`strcmp` 返回值 C 标准
  只保证符号——本轮实证**同一 libc（麒麟 glibc 2.31 aarch64）同一对字符串**，实参指针未对齐
  （rodata）返回 1、16 对齐（堆）返回 56（对齐分叉走不同 SIMD/字节路径）。「只依赖符号」的
  调用方永远安全，但**把幅值漏给用户程序打印=同一程序跨平台输出漂移**。Rust 的 `Ordering`
  （Less/Equal/Greater 全归一）是正解——运行时边界归一化（-1/0/+1，两条指令）一劳永逸。
  定位方法论：**LD_PRELOAD 拦截层**（打印实参指针/对齐/返回值——不动被测二进制）+ **旧版本
  编译器对照实验**（git worktree 建基线编译器跑同用例——分离「编译器变化」与「环境变化」）。
- **工作区「目录变空」先复查再响应（文件系统视图瞬断）**：本轮 target/ 出现 ~15 分钟空目录
  视图（本地 ext4、无删除进程、无其他会话）后**原文件带原时间戳全部回归**——期间一度按「被
  清空」处置（rm 残留+启动重建）。预防：**异常视图先跨命令/延时复查（stat 时间戳+再 ls），
  确认持久性后再破坏性响应**；沙箱/挂载命名空间环境（ZCode Bash 工具）下尤其如此。


## 第一百五十二轮踩坑（2026-09-16，家机 win-x64：152-a tests 面迁移）

- **「测试文件不可擅改」与「§九 第 6 项授权」的边界**：用例迁移**合法**的唯一依据=**用户一次性授权**
  （plans/022 §九 第 6 项：裸操作包进不安全函数、**断言语义不变**、**变更清单留痕**）——**三条件同时满足**
  （本轮：修饰零产物=语义不变〔E2E 验证〕+ 清单 187 行留痕）——**缺「留痕」即违约**（清单必须随轮次文档）。
- **批量迁移的「错误行」噪声**：复验时「错误 256 行」**不是回归**（负测用例的预期失败）——**统计口径要
  分清「观察期命中」与「错误」**（迁移目标=前者归零，后者=用例设计）。
- **`A && rm && nohup B &` 组合坑（第三次复现）**：门禁链首次未启动（无日志无进程）——**长任务必须
  `nohup ... &` 单独一行**（前置命令用 `;` 分开，禁用 `&&` 链）——**113-a 教训的持续复现说明：养成
  「长任务单独起 + 25 秒核验」的肌肉记忆**（本轮核验发现即重跑，零损失）。


## 跨机验证轮踩坑（2026-09-14，单位机麒麟ARM64：93-a~108-a+111-a~146-a 全量 arm64 复验）

- **「源码可移植性」没有门禁时，跨机轮是唯一拦截面（本轮双缺陷同源）**：家机（MSVC）与深度机
  （GCC≥10）都接受的中文 C++ 标识符，把麒麟 GCC 9.3 的构建彻底打挂（`stray '\346'`，UTF-8 标识符
  GCC 10 才支持）——**「我的机器构建绿」≠「源码可移植」**；既有纪律「C++ 标识符一律 ASCII」被
  110-a/118-a/134-a/77-a 四轮无声违反（每轮都在自家机器门禁全绿）。预防：①纪律升格为扫描门禁
  （Rust `#![deny(non_ascii_idents)]` 同构）；②改 C++ 源码时自查标识符形态（注释中文合法、
  标识符必须 ASCII）。
- **libc 边界函数的「幅值」是平台细节，语言边界必须归一（跨机-b）**：`strcmp` 返回值 C 标准
  只保证符号——本轮实证**同一 libc（麒麟 glibc 2.31 aarch64）同一对字符串**，实参指针未对齐
  （rodata）返回 1、16 对齐（堆）返回 56（对齐分叉走不同 SIMD/字节路径）。「只依赖符号」的
  调用方永远安全，但**把幅值漏给用户程序打印=同一程序跨平台输出漂移**。Rust 的 `Ordering`
  （Less/Equal/Greater 全归一）是正解——运行时边界归一化（-1/0/+1，两条指令）一劳永逸。
  定位方法论：**LD_PRELOAD 拦截层**（打印实参指针/对齐/返回值——不动被测二进制）+ **旧版本
  编译器对照实验**（git worktree 建基线编译器跑同用例——分离「编译器变化」与「环境变化」）。
- **工作区「目录变空」先复查再响应（文件系统视图瞬断）**：本轮 target/ 出现 ~15 分钟空目录
  视图（本地 ext4、无删除进程、无其他会话）后**原文件带原时间戳全部回归**——期间一度按「被
  清空」处置（rm 残留+启动重建）。预防：**异常视图先跨命令/延时复查（stat 时间戳+再 ls），
  确认持久性后再破坏性响应**；沙箱/挂载命名空间环境（ZCode Bash 工具）下尤其如此。

## 第一百五十二轮踩坑（2026-09-16，家机 win-x64：152-a tests 面迁移）

- **「测试文件不可擅改」与「§九 第 6 项授权」的边界**：用例迁移**合法**的唯一依据=**用户一次性授权**
  （plans/022 §九 第 6 项：裸操作包进不安全函数、**断言语义不变**、**变更清单留痕**）——**三条件同时满足**
  （本轮：修饰零产物=语义不变〔E2E 验证〕+ 清单 187 行留痕）——**缺「留痕」即违约**（清单必须随轮次文档）。
- **批量迁移的「错误行」噪声**：复验时「错误 256 行」**不是回归**（负测用例的预期失败）——**统计口径要
  分清「观察期命中」与「错误」**（迁移目标=前者归零，后者=用例设计）。
- **`A && rm && nohup B &` 组合坑（第三次复现）**：门禁链首次未启动（无日志无进程）——**长任务必须
  `nohup ... &` 单独一行**（前置命令用 `;` 分开，禁用 `&&` 链）——**113-a 教训的持续复现说明：养成
  「长任务单独起 + 25 秒核验」的肌肉记忆**（本轮核验发现即重跑，零损失）。

## 第一百五十三轮踩坑（2026-09-17，家机 win-x64：153-a v2 挂点基础设施）

- **「基础设施先行」的 v2 挂点顺序**：9 类挂点表（plans/023 §八）的第一步不是任何一类检查，而是
  **「修饰不再丢弃」**（原 v2 语法层「放行即丢」）——**修饰存储（节点字段）+ 语义标志（当前函数不安全）**
  是 9 类共用的地基；**先地基后检查**（一次 5 处改动，后续每类检查 1-2 处）。
- **改 v2 共享结构体（节点）的锚定链预期**：节点加字段 → 布局变 → **产物变 → 锚定链重锚**（406097→406317，
  +220 行）——**「重锚」不是失败**（自洽性〔fix_p≡fix_s〕才是判定）；**但要在提交信息/文档标注行数变化
  与原因**（跨机轮核对方便）。
- **「无」字面量的标记法（最小侵入）**：v2「无」原=整数字面量 0（无标记）——B11 识别需区分——**用「附加=1」
  标记**（保持既有语义零改动，一处解析+一处语义）——**代替「独立节点类型」**（后者要动推导/IR/多处）。
  **「标记位」替代「新节点类型」= 最小侵入模式**（与宿主 B11 的「操作数=NullLiteral」直判不同——v2 的
  「无」无独立节点，按平台现实选手段）。
- **`A && nohup B &` 组合坑（本轮第五次复现）**：v2 树 check 与锚定链连写 → check 跑完链没起——**纪律重申：
  长任务 `nohup ... &` 单独一行；验证类命令（check）与长任务分开两条**。

## 第一百五十四轮踩坑（2026-09-17，家机 win-x64：154-a v2 挂点续）

- **「观察期警告计入错误数」——v2 诊断无分级的连带缺陷**：新挂点（B8）一上线，209 用例立变 FAIL
  （「语义错误 2 个」→「3 个」）——**v2 的「错误数」=诊断列表长度**（含警告）——**新「警告类」诊断
  必须同步澄清「计数口径」**（宿主 diagnostics 有分级故无此问题；v2 侧补=排除「[安全区边界·观察期]」
  前缀）。**「引入一类新诊断」时先看「谁在数诊断」**——本轮 E2E 当场揪出（290→289）即修。
- **「判定复用」降低挂点成本**：四类挂点全部复用 `查语义指针变量`（指针判定现成）——**每类=1 处插入
  （分支内 2-4 行）**；**挂点前先搜「现成判定」**（v2 的类型栈/指针表/结构体表）——比新造判定函数
  快且与既有机制一致（不漂移）。
- **v2p 构建缓存机制自然生效**：改 v2 源码后 run_e2e 的「v2p 构建缓存」自动失效重建（209 复验直接
  PASS）——**验证链无需手工清缓存**（与锚定链的「[1] 重建」同向）。

## 第一百五十五轮踩坑（2026-09-17，家机 win-x64：155-a v2 挂点续②）

- **「符号种类不可区分」时的替代判定=建名集**：v2 的静态登记与普通变量同种类（符号_变量）——B10
  无法从符号表区分——**在声明收集点顺手建「静态名ID集」**（单点写入 + 单点查询）——**「登记元数据不足」
  的最轻补救是「收集时建集」**（不改成符号结构体=零连带）。
- **「面外」要显式登记而非留空**：A3 联合体/A4 FFI 在 v2 面未支持（解析/语义都无）——**显式写「面外 + 原因
  + 补的时机」**（vs 默默跳过）——**挂点表的完整性=「哪几类做了、哪几类为什么没做」都写清**（否则后续轮
  会重复排查「A3/A4 是不是漏了」）。
- **CRLF 文件的行插法**：正则/多行字符串锚点在 CRLF 文件里易失配（`
` vs `

`）——**行级定位 + 行数组
  插入**（`lines[:i] + insert + lines[i:]`）比多行串替换稳（本轮 A1 两次锚点失败后改用行插，一次成功）。

## 第一百五十六轮踩坑（2026-09-17，家机 win-x64：156-a 类方法修饰 + 波 6 收口准备）

- **看板嵌套冲突标记“入库”事故（本轮新发现，历史遗留多轮）**：`三机任务看板.md` 有 **8 行嵌套冲突标记**
  （`<<<<<<< HEAD` 套 `<<<<<<< HEAD` … `>>>>>>> 4e2583b` … `>>>>>>> eb3e27b`）**且已随多轮认领提交入库**
  ——根因=多次 rebase 冲突用脚本“自动解”时**外/内两层只解了一层**（脚本保留双方行但漏删标记），
  此后每轮看板提交都原样带走。**教训：①冲突解决脚本收尾必须断言 `'<<<<<<<' not in text`（写入前自检）；
  ②“每轮都动的热点文件”（看板/HANDOFF/五件套）应定期 `git grep '^<<<<<<< '` 全仓扫一遍**——
  标记混在长表格里肉眼极易漏（本轮看板表格 40 行、标记藏在三机行之间）。清理时保留单位机**最新裁决落档版**
  （两个候选文本均可，取含后续裁决信息者），并在提交信息说明“跨区修正”（AGENTS.md §8.3 要求）。
- **跨文件设置静态标志 = 加访问器（v2 无友元）**：类方法修饰需要从 `语义检查类.cn` 设置 `语义检查.cn` 的
  文件级静态 `当前函数不安全`——v2 语法无 `类内 static` 之外的跨文件“包级”可见性，**正解=在原文件加
  `不安全 函数 设当前函数不安全(布尔 值)` 访问器**（与宿主 `currentFnUnsafe_` 私有成员 + 方法调用同构；
  比“把静态提升为全局变量”安全——写点单一）。**“新挂点要设置旧文件的静态”时先想访问器，别动静态归属**。
- **类方法修饰的“4 调用点代数”**：`解析方法体` 加参数后调用点 = 顶层 dispatch 两类（不安全 方法→1；
  函数/析构→0）——**改函数签名后先 `grep -n '解析方法体'` 数调用点再动手**（本轮 4 处一次全改，
  CRLF 行级操作零失配——延续 155-a 行插法）。

## 第一百五十七轮踩坑（2026-09-17，家机 win-x64：157-a 波 6 收口）

- **观察期两阶段设计的价值实证（收口即放大器）**：观察期（警告不阻断）→ 收口（硬错误）的两阶段设计，
  让**所有挂点漏点在非阻塞期暴露、在阻塞期前修完**——本轮 V2（A2 数组误报）/V3（A1 挂点死码）两个
  v2 漏点**全靠收口前后对比暴露**（观察期时它们只是多余警告=无人受伤；收口后变编译失败=用例当场拦下）。
  **「先警告后错误」不是拖延，是把「挂点正确性验证」推到有真实压力的时点**——但前提是**收口轮必须
  逐漏点根治**（禁止直接放宽判定凑绿）。
- **「挂点位置对」≠「挂点有效」——判定机制是更隐蔽的错源**：V3 的 A1 挂点在正确函数（推导二元类型ID）、
  正确分支（+/-），**判定机制错了**（`表达式类型ID` 对指针变量返 -1 → `左ID>=0` 恒假=死码）——155-a
  实装、156-a 复核均未发现，157-a 探针实测三形态全零命中才暴露。**纪律：挂点交付=代码+探针双件**
  （每个挂点至少一个「命中/豁免」探针实测，静态审查不算）。
- **探针打印中间值 > 读代码**（v2 语义层定位法，本轮两次关键突破）：①临时表诊断打印 `op/子起点/节点索引`
  → 确认二元分支到达；②打印 `表查找类型ID` 返回值+`驻留文本` → 发现**v2 符号登记语义：指针变量登记
  「所指类型」**（`整64* q` → `整64`；故「是裸指针」判定必须用 `查语义指针变量` 而非类型文本）。**v2 的
  类型登记语义分散在多个助手**——用错一个（类型文本）就静默失效；**判定助手选择前先探针验证其返回值语义**。
- **测试片段形态三种（单测迁移三遍才干净）**：raw string（`R"CN(...)CN"`）、普通字符串拼接
  （`"函数 主() {
"`）、**字符串段内前导空格**（`"    函数 计数器() ..."`）——迁移脚本单一形态扫描
  （只匹配 `"函数 `）会漏前导空格形态（ModuleTest 1 个测试残留）。**多形态迁移=全量测试兜底**
  （34 失败 → 1 → 0 三遍收敛）；**每遍跑全量单测**比静态核对快且可靠。
- **新增修饰关键字分支须对照既有分支的全部动作**：parser 的 `不安全` 分支（720-728）只差一个
  `match(TokenType::Semicolon)`（对照 748 行 函数 分支）——**原型+修饰组合**即触发顶层解析错位
  （「预期顶层声明，实际为 ';'」）。**「两个分支长得像」时逐行动作对齐**（diff 思想），漏一个 match
  就是一类语法形态不可用。

- **收口盘点口径必须覆盖「全部源码文件」而非「用例入口」**：157-a 首轮盘点按用例入口（主.cn/唯一 .cn）扫 293 个文件
  ——**供给源**（v2 用例的 `供给=` 辅助源）/多文件辅助源不在挑选面 → 144_v2 的 `供给.cn` 漏迁移，E2E 全量暴露
  （144-2.5 供给源编译失败）。**改正=对 tests/e2e 全部 387 个 .cn 复盘**（此口径下唯一剩余命中=265/266 负测的
  故意违规 ✅）。**「按用例聚合」的盘点天然漏「非入口文件」**——清单类迁移（修饰/标记/契约变更）必须**按文件全量**
  而非按用例。

## 第一百五十八轮踩坑（2026-09-14，单位机麒麟 ARM64：158-a 跨机-a/b 方案A 实施）

- **扫描器类门禁工具必须「先备正/负样本自检，再上树扫描」**：`check_ascii_idents.py` 首版带两个缺陷直接跑全树——
  ①UTF-8 **BOM（U+FEFF）被误报为代码区非 ASCII 字符**（3 个 unit 文件行 1 假违例——BOM 是编码标记，
  MSVC/GCC 无感，读取后须剥）；②raw string 剥离**未保留内部换行 → 报告行号错位**（`k映射定义` 报在 57 行、
  源行却是 raw string 内容——剥离器把跨行 raw string 换成 `""` 使后续行号整体前移）。两缺陷都是第二轮扫描
  对着报告核源行才暴露。**纪律（C14「探测器本身也是被测对象」的具体化）**：剥离类工具交付前先用
  「已知违例 + 已知豁免（注释中文/字符串中文/字符字面量/跨行 raw string/BOM）」双样本自检 + **报告行号与源行
  逐条核对**；剥离器必须保持行结构（换行原位保留）。
- **sed 全局替换会误伤注释中的同名词**：`交换` 既是违例标识符又是注释常用词（「交换装载」「交换后 Lt=右>左」
  同文件共存）——盲目 `sed s/交换/swapped/g` 会改掉中文注释（违反「中文注释」纪律）。**先 `grep -n` 列出全部
  命中行、区分代码位/注释位，代码位少用带上下文精确替换、代码位多且纯（如 kVectorDef 三名只在代码位）才用 sed**。
- **门禁回退验证（反证）自身会被零警告门禁拦截——是好事，但回退要完整**：跨机-b 反证临时回退 `__cn_str_cmp`
  时只替换了 return 行、留下 `const int r` 未使用 → GCC -Werror `unused-variable` 构建失败。**临时回退必须
  以「函数级完整回退」为单位**（连声明一起），否则构建失败会伪装成「回退无效」。旁证：-Werror 门禁在位有效。
- **锚定链固定点变化的归因要先分清「本轮改动」与「基线前进」**：本轮预判「改名/strcmp 不进产物 → 固定点
  不变」，实测 552331→555442（+3111）——增量来自**基线前进（157-a 的 v2 树变更，家机 win 已重锚 408105）**
  而非本轮（跨机-a 改宿主 C++ 局部名、跨机-b 改运行时 C++ 实现，均不在 v2 产物 asm 面）。跨机验证的锚定链
  预判必须**以「实施时基线的预期固定点」为对照**，而非上一轮本机的历史值。
- **（159-a 更正）「环境异常」真因=同工作树双会话并行，非环境问题**：158-a 把工作树中一份 plans/023 改动
  判为「来源不明/环境异常」回滚（归因于上轮「target/ 视图瞬变」同类），实为**另一并行会话已完成的正当
  文档修正**——判据不足即归因环境，会误伤他轮工作并误导跨机请托（0619c47 ⑥请家机核对漏 add=无效告警）。
  **纪律**：处置工作树中「非本会话」改动前，先 `diff` 其内容与最近提交的文档口径（是否=某轮已批改动的
  未入库部分）、核对 /tmp 与其他会话线索；确属异常才回滚，且回滚必须留备份+在下一轮显式销案。

## 第一百六十轮踩坑（2026-09-14，单位机麒麟 ARM64：160-a 三项批准落盘）

- **「兼容写法」不走规范审批＝实现私定语法，终会被用户当缺陷质询**：宿主 parser 的冒号后置类型标注
  （`x: 整32`）自阶段一混入（aae4b70 之前），从未写入 plans/001 与 cn-language-spec，而 spec 03 设计决策
  明确否决后置标注——潜伏至 160-a 前夕被用户质询「常量正确用法」时暴露（实现与规范分叉+v2 不支持=双侧
  对拍契约隐患）。**纪律**：任何 parser 宽容/兼容分支（多余形态、可省略 token、替代写法）入库前必须过
  「新增语法须审批」关（AGENTS.md §2.3）——规范外形态不是「宽容」是「私货」；新会话接手时对
  「实现接受而规范无载」的形态应按缺陷呈报而非按特性文档化（159-a~160-a「常量用法查证」即此路径）。
- **commit message 含反引号必须单引号包裹 `-m`**：`git commit -m "……`公开`……"` 中反引号被 bash 当
  命令替换执行——4 个关键词（`公开`/`私有`/`静态`/`常量`）从提交信息里被吞（stdout 为空），7f9bf83
  信息残缺。已 push 的共享分支禁 force push（§8.5）不可 amend，只能下条提交补语境。**纪律**：
  含反引号/`$`/`!` 的提交信息一律**单引号**包裹 `-m '...'`，或 `--cleanup` 前先 `git log -1` 核对
  信息原文；提交信息里的关键词缺失比代码缺失更隐蔽（diff 完整、信息失真）。


## 第一百六十一轮踩坑（2026-09-14，家机 win-x64：161-a A8 冒号后置类型标注移除）

- **expected 固化必须取 runner 同环境的实际输出——v2p 有「同名双版本」陷阱**：271_v2 负测的
  `主.expected` 首跑固化时，手工跑 `target/v2p.exe`（9 月 9 日旧产物）得「节点=11」，runner 实跑得
  「节点=12」——runner 的 `确保v2p就绪win` 用的是 **`target/audit2/v2p.exe` 指纹缓存版**（v2 树或宿主
  编译器任一变更即重建），与根目录 `target/v2p.exe`（手工重建产物，常落后多轮）不是同一文件。两版
  v2p 对同一源文件的内部节点计数可差 1（157-a 改 v2 解析器所致），逐行比对即失败。**纪律**：凡固化
  v2 系 `.expected`，以 runner 错误信息回显的「实际输出」为准，或手工调用 `target/audit2/v2p.exe`；
  `target/v2p.exe` 只用于手工探针，不用于契约固化。
- **移除「实现私藏语法」时，unit 断言该语法的用例=必迁面，须与负测同轮迁移并留痕**：A8 移除冒号后置
  分支后 `ParserTest.FunctionColonParams`/`VarDeclColonType`/`MixedTypeSyntax` 三单测即红——它们锚定的
  正是被移除的私藏语法。迁移模式=负断言（`EXPECT_TRUE(hasErrors)`）+ 注释注明收口轮次与对应 E2E
  负测编号（268/269/270），与 157-a「unit 353 处迁移」同纪律：断言语义对齐新规范、变更逐项留痕。
- **单文件反证回退用 `git stash push -- <path>` 最稳**：回退→构建→负测复现（「预期编译失败但编译成功」
  ×3）→`stash pop`→重建→负测转绿，全程唯一变量=被回退文件；比手工改回原代码更不易引入二次差异。


## 第一百六十三轮踩坑（2026-09-14，家机 win-x64：163-a 78/79 → v2 锚定链迁移）

- **E2E 断言取「自洽性/关系」而非「绝对量」——绝对量会把被测对象的正常演化误判为失败**：79_v2 自举闭环的
  可断言量有两类：①两代产物逐字节一致（fix_p ≡ fix_s）②产物绝对行数（win 首锚 408105）。②随 v2 树每次
  演化（加文件/改语法）必然漂移——写进用例即「每次 v2 树变更都要同步改期望」（把「用例失败」与「树演化」
  混为一谈，且易被顺手改期望掩盖真问题）。**纪律**：E2E 断言锚定「不变量/关系」（一致、符号归属、可编译、
  退出码）；绝对量只用于防假绿**保守下界**（行数 ≥100000）与文档口径记录（精确值写文档、用例里只写下界）。
  同款：v1 79 的 5 行「完成:模块名」输出锚定 vs v2 的固定点自洽——后者对演进免疫。
- **「自检类」断言必须配反证实验，只看 PASS 不构成防伪证据**：79_v2 的绑定自检（map 查 cn_main 归属
  cn_self.obj——防 v1 79「链接顺序错→CN 产物被整体忽略→CN vs CN 沦为 C++ vs C++」教训复现）首跑即 PASS，
  但 PASS 只证明「没拦我」，不证明「拦得住」。**反证**=单变量反转 rsp 的 obj 顺序（v2p.obj 在前）→ 用例须
  失败且原因恰为绑定自检（map 铁证 `cn_main→v2p.obj`）→ 恢复转绿。**纪律**：凡防伪/防假绿自检（归属、下界、
  指纹、净栈），交付前必做一次单变量反证；「自检绿了所以防伪有效」是循环论证。
- **同机双会话并行（三机协议未覆盖情形）= git worktree 隔离实施 + 推送时序化**：用户同机并行开两会话
  （162-a 持主工作区与开发锁）时，第二会话（本轮）实施路径：①`git worktree add <Documents 下两层> -b <新分支>`
  物理隔离检出（共享 .git 对象库）；②**文件集与对方不相交**（本轮只触 tests/e2e 78/79 区 + run_e2e.py + 文档；
  对方触 lexer/parser/v2 词法）；③验收断言选对方改动**不敏感**的口径（79_v2 自洽性对 v2 树演化稳定——
  故可在对方落地前先跑通，落地后 rebase 复跑确认）；④**不代跑对方门禁**（避免资源竞争），推送前
  fetch+rebase，文档同区（看板行/plans/021/HANDOFF 家机节）手工合并；⑤提交信息/看板通告显式声明
  「另一会话在制品范围」给合并方留判据。**纪律**：若两会话文件集相交（改同一源码），必须停一个、不得并行；
  同机第二会话**不得**以「同机无需认领」为由跳过 worktree 隔离直接改主工作区。
- **worktree 里的首次构建须检查「运行时 obj」等构建副产物是否齐备**：v2 锚定链/v2 闭环链接需要
  `target/<名>.obj`（10 个运行时 obj，CMake 构建副产物）——worktree 全新检出时 target/ 为空，须先
  完整构建（`build.ps1`）再跑 E2E，否则 runner 报「缺少运行时 .obj」。
