// CN 自举编译器 v2 · 语义组件包根（阶段目录包形态，2026-09-08 用户裁决）
// 职责：语义组件的库包根聚合声明——包根（是包根=1）的 模块 声明按同级
//   目录聚合解析（宿主 driver_module 候选2b 目录包同款语义，零 C++ 改动）。
//   声明序=加载序（依赖先解析）：语义分析（表/声明收集/类型解析）→
//   语义检查表达式（R3 拆分：检查表达式三分支族）→
//   语义检查（检查族+主流程，调主干公开函数）。
//   既有主干文件保持原组件名=模块路径逐字节不变（键基/mangling 零变化）。
模块 语义分析;
模块 语义检查表达式;
模块 语义检查;
模块 借出检查;
