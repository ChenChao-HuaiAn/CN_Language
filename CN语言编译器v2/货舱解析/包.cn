// CN 自举编译器 v2 · 货舱解析组件包根（阶段目录包形态，2026-09-08 用户裁决）
// 职责：货舱.toml 解析组件的库包根聚合声明——包根（是包根=1）的 模块 声明
//   按同级目录聚合解析（宿主 driver_module 候选2b 目录包同款语义，零 C++ 改动）。
//   子模块保持原组件名=模块路径逐字节不变（键基/mangling 零变化）。
模块 货舱解析;
