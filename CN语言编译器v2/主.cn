导入 容器::向量
导入 词法分析
导入 语法分析

函数 主() -> 整32 {
    打印("Token 字节大小(词法模块)=", 令牌字节大小())
    字符串 源码 = "a"
    向量<Token> 记号 = 向量<Token>()
    分词(源码, 记号)
    打印("本模块 记号.大小()=", 记号.大小())
    向量<节点> 池 = 向量<节点>()
    整64 结束 = 解析表达式(记号, 0, 池)
    打印("跨模块 解析 结束=", 结束, " 池=", 池.大小())
    返回 0
}