// CN 自举编译器 v2 · IR 组件包根（阶段目录包形态，2026-09-08 用户裁决）
// 职责：IR 层组件的库包根聚合声明——包根（是包根=1）的 模块 声明按同级
//   目录聚合解析（宿主 driver_module 候选2b 目录包同款语义，零 C++ 改动）。
//   声明序=加载序（依赖先解析）：IR常量（操作码/节点_* 常量+函数级类型
//   权威表）→ IR签名（签名辅助）→ IR布局（布局表族+键基基础设施+常量表）
//   → IR容器（IR 结构/方法签名/调用决议）→ IR生成（权威表+决议）→
//   生成表达式 → 生成语句 → 生成函数（入口，调全部）。
//   既有组件文件保持原名=模块路径逐字节不变（键基/mangling 零变化）。
模块 IR常量;
模块 IR签名;
模块 IR布局;
模块 IR容器;
模块 IR生成;
模块 生成表达式求值;
模块 生成表达式;
模块 生成语句;
模块 生成函数;
