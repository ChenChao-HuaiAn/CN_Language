// CN 自举编译器 v2 · 语法组件包根（阶段目录包形态，2026-09-08 用户裁决）
// 职责：语法组件的库包根聚合声明——包根（是包根=1）的 模块 声明按同级
//   目录聚合解析（宿主 driver_module 候选2b 目录包同款语义，零 C++ 改动）。
//   声明序=加载序（宿主逐文件流水检查，依赖先解析）：解析基础（零反向依赖
//   的解析基础设施）→ 解析表达式 → 解析语句 → 语法分析（主干，调前两者）。
//   既有主干文件保持原组件名=模块路径逐字节不变（键基/mangling 零变化）。
模块 解析基础;
模块 解析表达式;
模块 解析语句;
模块 解析类;
模块 语法分析;
