# 换行归一管控（447-a·机制级修正·三机共享契约广播见看板通告段）
# 背景：core.autocrlf=true 的 win 检出把 *.cn 转为 CRLF——**跨行字符串字面量
#   （多行"""..."""等·规范 01b）的内容按源文件字节原样收录**，\r 进入字面量
#   → 程序输出 \r\r\n → 459_字符串四形态矩阵 等用例逐行比对多空行假红
#   （win 检出态必红·linux 检出绿——447-a 集成门禁首次实证）。
# 对策：字节敏感面强制 eol=lf（blob 本已全 LF·仅管检出方向）——Rust 仓库同款。
*.cn text eol=lf
*.expected text eol=lf
*.meta text eol=lf
