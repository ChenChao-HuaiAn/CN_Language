// CN 语言标准库：数学模块（Task 6.3，规格书10.5 数学库）
// 公开: 平方根/幂/正弦/余弦/正切/绝对值/向上取整/向下取整（8 个函数）
// 实现：每个函数内部调用数学库内置函数（数学.xxx，全局注册、无需导入），
//   内置函数由编译器 IR 层映射到运行时符号 __cn_sqrt/__cn_pow/__cn_sin/
//   __cn_cos/__cn_tan/__cn_fabs/__cn_ceil/__cn_floor（math_api.cpp）。
// 设计说明：
//   - 内置函数名带 "数学." 前缀（如 数学.平方根），与本模块公开函数
//     纯名（平方根）不冲突——模块函数走"公开符号合并"，内置走"限定名直调"
//   - 使用方式：导入 数学.平方根 后 数学.平方根(值)，或 从 数学 导入 平方根
//     后 平方根(值)；本模块文件也可复制到项目目录直接导入（文件即模块）
公开:
函数 平方根(浮64 值) -> 浮64 {
    返回 数学.平方根(值)
}

函数 幂(浮64 底数, 浮64 指数) -> 浮64 {
    返回 数学.幂(底数, 指数)
}

函数 正弦(浮64 弧度) -> 浮64 {
    返回 数学.正弦(弧度)
}

函数 余弦(浮64 弧度) -> 浮64 {
    返回 数学.余弦(弧度)
}

函数 正切(浮64 弧度) -> 浮64 {
    返回 数学.正切(弧度)
}

函数 绝对值(浮64 值) -> 浮64 {
    返回 数学.绝对值(值)
}

函数 向上取整(浮64 值) -> 浮64 {
    返回 数学.向上取整(值)
}

函数 向下取整(浮64 值) -> 浮64 {
    返回 数学.向下取整(值)
}
