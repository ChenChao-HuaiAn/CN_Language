// CN 语言标准库：时间模块（Task 6.5，对标 C++ chrono/ctime）
// 公开: 当前时间戳/单调时钟毫秒/格式化时间（3 个函数）
// 实现：每个函数内部调用时间库内置函数（时间::xxx，全局注册、无需导入），
//   内置函数由编译器 IR 层映射到运行时符号 __cn_time/__cn_clock_ms/
//   __cn_time_format（time_api.cpp）。
// 设计说明：
//   - 内置函数名带 "时间::" 前缀（如 时间::当前时间戳），与本模块公开函数纯名
//     （当前时间戳）不冲突——模块函数走"公开符号合并"，内置走"限定名直调"
//   - 当前时间戳：秒级（Unix 纪元，UTC）；单调时钟毫秒：单调不减（计时差值安全）
//   - 格式化时间：格式为 strftime 格式串（如 "%Y-%m-%d %H:%M:%S"），本地时区；
//     失败（时间戳非法）返回 错误(6)（无效参数）
//   - 内存语义：格式化时间 返回动态分配字符串，调用方负责 字符串释放()
//   - 使用方式（v2.0）：导入 时间::当前时间戳 后 当前时间戳()，或 导入 时间::{当前时间戳}
公开:
函数 当前时间戳() -> 整64 {
    返回 时间::当前时间戳();
}

函数 单调时钟毫秒() -> 整64 {
    返回 时间::单调时钟毫秒();
}

函数 格式化时间(整64 时间戳, 字符串 格式) -> 结果<字符串, 整32> {
    字符串 格式化结果 = 时间::格式化时间(时间戳, 格式);
    如果 (格式化结果 == 无) {
        返回 错误(6);
    }
    返回 正常(格式化结果);
}
