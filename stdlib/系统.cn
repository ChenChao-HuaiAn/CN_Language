// CN 语言标准库：系统模块（Task 6.5，规格书10.4 命令行参数；对标 C++ argv）
// 公开: 参数个数/参数（2 个函数）
// 实现：每个函数内部调用系统库内置函数（系统::xxx，全局注册、无需导入），
//   内置函数由编译器 IR 层映射到运行时符号 __cn_argc/__cn_argv（system_api.cpp）。
// 设计说明：
//   - 内置函数名带 "系统::" 前缀（如 系统::参数个数），与本模块公开函数纯名
//     （参数个数）不冲突——模块函数走"公开符号合并"，内置走"限定名直调"
//   - 参数个数：含可执行文件名本身（对标 C argv[0]），至少为 1
//   - 参数：按索引取参数字符串（argv[索引]），越界返回 错误(6)（无效参数）
//   - 内存语义（A2 70-a 2026-09-11 更新）：内置 系统::参数 返回 字符*（argv
//     由 CRT 持有的借用视图，调用方不得 字符串释放/复制——按 字符* 消费）；
//     本模块包装 参数() 返回 结果<字符串>（显式 复制 落堆=拥有，调用方 RAII）
//   - 使用方式（v2.0）：导入 系统::参数 后 参数()，或 导入 系统::{参数}
公开:
函数 参数个数() -> 整32 {
    返回 整32(系统::参数个数());
}

函数 参数(整32 索引) -> 结果<字符串, 整32> {
    // 70-a（2026-09-11 方案A 补完）：内置 系统::参数 已改 字符*（argv=CRT
    //   持有借用）——装箱拥有串须显式 复制 落堆（A2 借用→拥有收紧）
    字符* 值 = 系统::参数(索引);
    如果 (值 == 无) {
        返回 错误(6);
    }
    返回 正常(字符串复制(值));
}
